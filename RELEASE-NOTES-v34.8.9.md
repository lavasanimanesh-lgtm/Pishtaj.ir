# یادداشت انتشار CRM v34.8.9

تاریخ: ۱۴۰۵/۰۶/۴ (۲۰۲۶-۰۸-۲۶)

## پایان وابستگی به localStorage (STORAGE-INDEPENDENCE)

### RCA کامل
بن‌بست «۱۰۰٪ پر بودن localStorage» که ابزارهای پاک‌سازی آن را رد می‌کردند، یک زنجیرهٔ معماری بود: حالت سرور-محور (فاز B) هرگز روی دستگاه‌های دارای دادهٔ قدیمی فعال نمی‌شد؛ «همگرایی» موفق هم پرچم فاز را روشن نمی‌کرد؛ «پاک‌سازی کش» با گارد «فاز فعال نیست» رد می‌شد؛ «پاک‌سازی امن» فقط کلیدهای فرّار را هرس می‌کرد و MBهای دادهٔ کسب‌وکار را دست نمی‌زد؛ و نشانگر هم‌گرایی per-user با تعویض اکانت ریست می‌شد. جزئیات در `ARENA-STORAGE-INDEPENDENCE-RCA-2026-08-26.md`.

### رفع‌ها
- **موفقیت هم‌گرایی = پایان وابستگی:** بلافاصله پس از ACK کامل، فاز B خودکار فعال، هوک getData/setData نصب و کلیدهای کسب‌وکار (>۲۴KB) از localStorage به IndexedDB تخلیه می‌شوند.
- **تخلیهٔ هر بوت:** هر ردی از مسیرهای قدیمی در localStorage دوباره به IDB منتقل می‌شود (خودترمیم).
- **نگهبان سهمیهٔ فعال:** در ≥۸۵٪، به‌جای هشدار منفعل، تخلیهٔ خودکار اجرا می‌شود.
- **گارد چندکاربره:** پاک‌سازی کش به کاربرِ فعلی وابسته نیست؛ هم‌گرایی موفقِ همین دستگاه با هر کاربری کافی است.
- گاردهای داده (صف آفلاین غیرخالی، نیاز به ACK سرور) دست‌نخورده باقی‌مانده‌اند — هیچ داده‌ای بدون تضمین سرور حذف نمی‌شود.

### کنترل کیفیت
- tester514 (شبیه‌سازی چرخهٔ عمر دستگاه قدیمی/جدید + گاردها)
- گیت CI: ۱۳۰ PASS / 0 FAIL؛ نگهبان معماری PASS
