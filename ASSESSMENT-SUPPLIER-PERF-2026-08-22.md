# ASSESSMENT — ماژول تامین‌کنندگان و ثبت‌نام‌های سایت (PERF)

**تاریخ:** 2026-08-22 (۱۴۰۵/۰۵/۳۱)  
**نسخه:** v34.7.81  
**برنچ:** `arena/01a02872-pishtaj-ir`

## ۱. هدف

بررسی علت دیر لود شدن ماژول تامین‌کنندگان، به‌ویژه تب «🌐 درخواست‌های سایت» (ثبت‌نام‌شده
از سایت)، و ارائه راهکار افزایش سرعت.

## ۲. مسیر داده در قاب بازی

- CRM یک SPA است و داده‌ها در فایل‌های JSON (`crm/data`) و اخیراً از طریق
  `api/db-lib.php` (MySQL اختیاری) نگهداری می‌شوند.
- صندوق ثبت‌نام سایت (`get_inbox`) شامل `suppliers.json` + «rfqs با src=site» است.
- `bridge.js` این صندوق را در بوت CRM و هر ~۴۵ ثانیه (`syncServerInbox`) می‌خواند.
- تب سایت (`cheques.js` → `ptfSupTab('site')`) جدول `supPendWrap` را نمایش می‌دهد.

## ۳. یافته‌ها

| № | یافته | اثر | شدت |
|---|---|---|---|
| 1 | `get_inbox` بدون گزیپ و بدون نسخه/امضا، هر بار کل دو فایل را می‌فرستد | ترافیک/پردازش با تعداد رکورد و کاربران خطی رشد می‌کند | بالا |
| 2 | `renderSupPending` همیشه همه رکوردها را می‌سازد حتی در تب مخفی | سنگین‌ترین رندر پنل | بالا |
| 3 | `renderSuppliers` پایه با `innerHTML +=` در حلقه | O(n²) در DOM | متوسط |
| 4 | `box()` مالی تامین‌کنندگان `balance()` را دوبار برای هر رکورد محاسبه می‌کند | هزینه‌ی O(n×فاکتور×پرداخت) روی هر باز شدن پنل | بالا (با رشد داده) |
| 5 | مسیر ثبت/تایید (public_tracking_code + add_supplier + set_status) چند بار فایل کامل را می‌خواند | کندی در ثبت/تایید، نه مستقیماً لود پنل | متوسط |

## ۴. اقدام انجام‌شده

1. **اعمال شد:** `get_inbox` گزیپ + امضای `since` بر پایه `mtime:size` دو فایل؛ پاسخ
   `fresh` وقتی تغییری نیست.
2. **اعمال شد:** `syncServerInbox` ارسال `since`، ذخیره امضا، و رندر مجدد فقط در تغییر.
3. **اعمال شد:** صفحه‌بندی ۵۰تایی `renderSupPending` + «نمایش بیشتر» + رندر فقط در تب سایت.
4. **اعمال شد:** ساخت یک‌باره `renderSuppliers` و جستجوی سمت کلاینت.
5. **اعمال شد:** `balanceHtmlFrom` برای حذف محاسبهٔ دوباره در کادر مالی تامین‌کنندگان.

## ۵. چک‌لیست اعتبارسنجی

- [x] `node --check` روی `bridge.js`, `cheques.js`, `supplier-finance.js` سبز.
- [x] `tester483-v34.7.81-supplier-perf.js` در CI گیت ثبت شد.
- [x] تمام تسترهای موجود به `v34.7.81` و cache-bust جدید هم‌گام شدند.
- [ ] اندازه‌گیری با بزرگ‌کردن داده واقعی (توصیه: مقایسه Network/Performance قبل/بعد).

## ۶. گام‌های بعدی (به‌ترتیب سود)

1. **Lazy-load مالی تامین‌کنندگان** — `balance()`‌ها فقط هنگام باز شدن `<details>`.
2. **صفحه‌بندی فهرست تاییدشده** — `renderSuppliers2` به ۵۰ رکورد در هر گام.
3. **جدول سایت با صفحه‌بندی سروری** — فقط pending و محدود از `get_inbox` خواسته شود.
4. **MySQL نقطه‌ای** — `get_inbox` به کوئری ایندکسی با `src='site'` منتقل شود.
5. **استارت سرد SPA** — جدا کردن moduleهای سنگین از مسیر لود اولیه.

## ۷. پایش

- پس از استقرار، در DevTools → Network فیلتر `get_inbox` را ببینید؛ در حالت بدون
  تغییر باید پاسخ `{fresh:true}` و حجم بسیار کم باشد.
- با رشد رکوردها، زمان `renderSupPending`/`renderSuppliers2` را در Performance Profile
  مقایسه کنید تا تصمیم گام ۳/۴ تایید شود.
