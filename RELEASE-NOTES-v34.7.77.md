# RELEASE NOTES — v34.7.77 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## دانلود گروهی ضمایم درخواست‌ها به‌صورت فایل ZIP

### درخواست

برخی درخواست‌ها (RFQ) ضمایم زیادی دارند و دانلود تکتک آن‌ها زمان‌بر است؛ امکان دانلود
**فایل ZIP همهٔ ضمایم** یک درخواست فراهم شود.

### پیاده‌سازی

#### ۱. endpoint سروری — `api/zip-attachments.php`

- ورودی JSON: `{ files: [{key,name},…], base: '…' }`.
- برای هر کلید، یک `GET` امضاشده (AWS SigV4) از فضای ابری آروان گرفته می‌شود و بایت‌ها
  داخل یک ZIP چیده و یک‌جا stream می‌شوند (کاربر لازم نیست تکتک دانلود کند).
- ساخت ZIP با `ZipArchive`؛ در نبود آن، فال‌بک بدون وابستگی با روش `STORE` و پرچم UTF-8
  (نام فارسی فایل‌ها سالم می‌ماند).
- یکتاسازی نام فایل‌های تکراری داخل ZIP با پسوند `(2)`, `(3)`, ….
- سقف‌ها: حداکثر ۶۰ فایل و ۲۰۰MB مجموع.

#### ۲. امنیت

- احراز توکن (`auth_verify_token`) + نقش مجاز (۸ نقش CRM).
- بررسی منشأ (Cross-origin blocked).
- فقط `POST`.
- **allowlist سخت‌گیرانهٔ پیشوند کلید**: فقط `rfqatt/` (ضمایم درخواست در CRM) و
  `site-rfq/` (ضمایم استعلام ثبت‌شده از سایت، پس از تایید وارد `files.oth` درخواست می‌شوند).
  سند مالی/پرونده/چک و… از این مسیر قابل دانلود نیست.
- گارد مسیر پیمایش (`..`) و طول کلید.

#### ۳. کلاینت — `crm/inqreader.js`

- `ptfRfqZipEntries(r)`: فهرست فایل‌های ابریِ قابل‌زیپ‌کردن (dedup بر اساس کلید).
- `ptfDownloadRfqZip(cd)`: ارسال فهرست به endpoint با هدر احراز، دریافت blob و دانلود
  فایل `RFQ-<کد>-ضمائم.zip` + toast وضعیت (ساخت/موفقیت/خطا).
- دکمهٔ **«⬇️ دانلود همه (ZIP)»** در بالای مودال «مدیریت پیوست‌های فنی استعلام».

#### ۴. allowlist سرویس — `api/.htaccess`

- `zip-attachments.php` به فهرست `Require all granted` اضافه شد (مثل `attachment-read.php`).

### نکته

- فقط فایل‌های **ابر وار** (کلید `rfqatt/` یا `site-rfq/`) در ZIP قرار می‌گیرند؛ ضمایم
  قدیمیِ هاست که کلید ابری ندارند (و در مودال هم «مرجع قدیمی» نمایش داده می‌شوند) در ZIP
  نیستند و همچنان فقط به‌صورت تکی (در صورت در دسترس بودن) باز می‌شوند.
- فایل‌های حذف‌شده از فضای ابری هنگام ساخت ZIP بی‌صدا رد می‌شوند؛ اگر هیچ فایلی قابل
  دانلود نبود، پیام واضح «هیچ فایل قابل دانلودی یافت نشد» نمایش داده می‌شود.

### فایل‌ها

- `api/zip-attachments.php` — endpoint جدید (احراز + allowlist + ZIP).
- `api/.htaccess` — فعال‌سازی `zip-attachments.php`.
- `crm/inqreader.js` — `ptfRfqZipEntries` + `ptfDownloadRfqZip` + دکمهٔ ZIP.
- `_tools/uat/tester479-v34.7.77-rfq-zip-download.js` — تستر جدید (ثبت در گیت CI).

### اعتبارسنجی

- `node _tools/arch/arch-guard.js` → `=== ARCH GUARD: PASS ===`
- `node _tools/uat/run-ci-gate.js` → `95 PASS / 0 FAIL`
