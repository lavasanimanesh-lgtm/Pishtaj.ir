# RELEASE NOTES — v34.14.0 (S4 «کیفیت و مقیاس»)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** تستر559 (۴۴ سنجه) · **گیت:** 175 PASS / 0 FAIL · **PHP-لینت:** php-parser (cms.php + llm.php) ✓

---

## ۱) 🕘 انتشار زمان‌بندی‌شده (بدون cron)

- فرم «📄 صفحهٔ جدید» دکمهٔ «🕘 زمان‌بندی انتشار» گرفت: صفحه **همان لحظه رندر** می‌شود و HTML نهایی با موعد در `crm/data/cms-sched.json` ذخیره می‌شود (سقف ۲۰ آیتم معلق، بازهٔ ۵دقیقه تا ۶۰ روز).
- **موتور lazy:** هر فراخوانی `api/cms.php` (حتی `sched_list`) ابتدا `cms_sched_due()` را اجرا می‌کند — آیتم‌های موعدرسیدهٔ تأییدشده با **بک‌آپ + ثبت نقشه + لاگ** منتشر و ۱۵ انتشارِ آخر نگهداری می‌شوند.
- تیکِ بازبینی انسانی برای زمان‌بندی هم الزامی است؛ `exists` همان جریان confirm→overwrite را دارد.

## ۲) 👥 جریان تأیید دومرحله‌ای (نویسنده/منتشرکننده)

- آیتم ثبت‌شده به دست نقش `commercial` با وضعیت «🟡 منتظر تأیید مدیر ارشد» می‌ماند تا `admin/chairman/ceo` تأیید کند.
- «🚀 هم‌اکنون» (انتشار فوری) فقط مدیر ارشد؛ «✖ لغو» توسط سازنده یا مدیر ارشد.
- رندر مشترک: `cms_render_public_page()` حالا منشأ یگانهٔ `page_create` و `sched_add` است.

## ۳) 🕰 تاریخچه و بازگشت (diff/rollback UI)

- تب جدید «🛠 کیفیت» → فهرست گروهی بک‌آپ‌های `crm/data/cms-backups` (۳ نسخهٔ آخر هر فایل).
- «🔍 مقایسه»: جدول title/description/H1 (قرمز/سبز) + **diff خطی** با حذف پیشوند/پسوند مشترک و سقف ۴۰۰ خط (بلوک بزرگ‌تر فقط شمارش می‌شود).
- «↩️ بازگردانی»: **از نسخهٔ فعلی هم بک‌آپ گرفته می‌شود** (بازگشتِ بازگشت ممکن) + `sitemap_add` + بی‌اعتبارسازی کش سئو + لاگ.
- امنیت: الگوی سخت stamp (`\d{8}-\d{6}`)، گیت‌های مسیر (`cms_skip_dir`)، ضد path-traversal.

## ۴) 💸 داشبورد هزینهٔ هوش مصنوعی

- **شمارنده در llm.php:** هر فراخوانیِ واقعی provider → توکن واقعی (Gemini `usageMetadata` / OpenAI `usage`؛ در نبود، تخمین حرفی) + زمان پاسخ در `crm/data/ai-usage.json` با تفکیک روز/اکشن/مدل (نگهداری ۱۲۰ روز). **cache-hitها هزینه ندارند و شمرده نمی‌شوند.**
- **اکشن `usage_stats`:** فقط نقش‌های ارشد — گیت نقش از **توکن تأییدشده** (نه هدر خام). خروجی: مجموع ۳۰ روز (درخواست/توکن ورودی/خروجی/هزینه) + جدول ۱۴ روز آخر + اکشن‌های پرکاربرد + مدل‌ها.
- قیمت‌ها per-model تخمینی (flash-lite 0.10/0.40 · flash 0.30/2.50 · pro 1.25/10 · gpt-4o-mini 0.15/0.60 · gpt-4o 2.50/10 دلار بر ۱M توکن) و با کلید `pricing` در `llm-config.php` دقیق‌شدنی.

## ۵) ⚡ PageSpeed Insights (صفحات پول‌ساز)

- فهرست تا ۱۰ مسیر قابل ویرایش (`psi-config.json` — فقط مسیرهای موجود روی دیسک پذیرفته می‌شوند).
- سنجش موبایل (performance + seo) با cURL و timeout ۲۵ثانیه؛ **هر مسیر حداکثر یک‌بار در ۶ ساعت** (کلید اختیاری `psi_key` در `api/gsc-config.php`).
- خروجی: امتیاز رنگی + LCP/CLS/TBT/FCP + امتیاز سئو + **روند ۱۰ سنجش آخر** هر مسیر؛ «⚡ اندازه‌گیری همه» ترتیبی با نمایش پیشرفت (هر سنجش ~۳۰ ثانیه).

## ۶) 🌐 hreflang خودکار با `en/`

- مولد صفحه اگر نسخهٔ انگلیسی هم‌مسیر (`en/<folder>/<slug>.html`) روی دیسک موجود باشد، سه‌گانهٔ `fa-IR` + `en` + `x-default` را به head تزریق می‌کند.
- بدون نسخهٔ انگلیسی **هیچ لینکی به ۴۰۴ ساخته نمی‌شود** (ویژگی خفته تا وقتی درخت en/ گسترش یابد).

## ۷) بهداشت

- A10 پابرجا: بدون `localStorage` مستقیم؛ حالت‌های UI از سرور/`ptfDevKv`.
- پنجرهٔ tester558 با جابه‌جایی رندر هم‌مسیر شد (رفتار همان، مکان جدید: `cms_render_public_page`).
- انکرهای تستر559 نسخه‌زدایی‌شده و مقاوم به جاروی bump.

## ۸) فایل‌های کلیدی

`api/cms.php` (رندر مشترک + ۹ case جدید + موتور lazy) · `api/llm.php` (شمارنده + usage_stats + llm_price) · `crm/cms.js` (تب «🛠 کیفیت» + cmsPageSchedule) · `_tools/uat/tester559-v34.14.0-seo-s4-quality.js`

## ۹) بعدی

ادامهٔ رودمپ: hreflang دوطرفه برای صفحات en/ موجود، ادیتور canonical گروهی، alt تصویر گروهی (vision) — یا هر اولویت دیگری که مالک تعیین کند.
