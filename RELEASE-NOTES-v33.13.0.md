# یادداشت‌های انتشار (Release Notes — Version 33.13.0) — 🚨 فوری

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (PR #11 باز)
**اولویت:** 🔴 فوری (مصوبهٔ کارفرما) — **پر شدن حافظهٔ مرورگر در بکاپ/بازگردانی + عدم اتصال به سرور**
**مرجع برنامه:** `crm/PLAN-BACKUP-STORAGE-URGENT-2026-08-02.md` (فاز ۰ در این ریلیز اجرا شد)

---

## 🚨 ریشه‌های شناسایی‌شده (تأیید در کد)

| # | ریشه | اثر |
|---|---|---|
| R1 | fallback آفلاین: کل payload بکاپ (چند MB) مستقیم در `localStorage` نوشته می‌شد وقتی IndexedDB در دسترس نبود → `QuotaExceededError` → **پر شدن حافظهٔ مرورگر** | 🔴 بحرانی |
| R2 | بازگردانی: همهٔ کلیدها با `localStorage.setItem` خام نوشته می‌شد؛ مجموع > ۵MB → **بازگردانی ناقص/نیمه‌کارهٔ بی‌صدا** | 🔴 بحرانی |
| R3 | payload سنگین: audit نامحدود + notifs + `ptf_storage_queue` (صف فایل‌ها) در هر بکاپ کامل | 🟠 زیاد |
| R4 | 401/needLogin در بکاپ به‌عنوان «سرور در دسترس نیست» تلقی می‌شد و توکن refresh نمی‌شد | 🟠 زیاد |
| R5 | fetchهای بکاپ بدون timeout → معلق ماندن و «عدم اتصال» کاذب | 🟡 متوسط |
| R6 | بکاپ ساعتی بی‌شرط (کل payload هر ساعت) | 🟡 متوسط |

## 🔧 فاز ۰ — HOTFIX (اجراشده در v33.13.0)

- **F0-1:** در آفلاین **هرگز دادهٔ حجیم در localStorage** — فقط IndexedDB؛ اگر IDB fail → فقط marker کوچک با پیام «حافظهٔ محلی برای نگهداری نسخهٔ اضطراری کافی نیست» → حافظهٔ مرورگر پر نمی‌شود.
- **F0-2:** بازگردانی با گارد ظرفیت — پاک‌سازی کلیدهای موقت پیش از جایگزینی + `ptfStorageSafeSetItem` برای هر کلید + **گزارش کلیدهای ناموفق** در پایان (دیگر نیمه‌کارهٔ بی‌صدا نیست).
- **F0-3:** تشخیص `needLogin/401` در بکاپ و `data_push` بازگردانی → **`ptfSyncRefreshAuth()`** و یک بار تلاش مجدد (مثل sync).
- **F0-4:** همهٔ fetchهای بکاپ (save/list/get/download/data_push) با **timeout ۲۰ ثانیه (AbortController)** + پیام «سرور پاسخ نداد».
- **F0-5:** `ptf_storage_queue` (صف موقت آپلود فایل) از بکاپ حذف شد — فایل‌ها در ابری/IndexedDB جدا هستند.
- **F0-6:** کاهش حجم payload — در بکاپ: audit → آخرین ۱۰۰۰، notifs → آخرین ۵۰۰ رکورد (دادهٔ کامل محلی محفوظ است).

## 📋 فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/backup.js` | F0-1..F0-6: backupFetch(timeout) + refresh توکن + fallback فقط IDB + restore گارددار + DATA_KEYS/PAYLOAD_CAPS |
| `crm/PLAN-BACKUP-STORAGE-URGENT-2026-08-02.md` | **جدید** — برنامهٔ فوری (ریشه‌ها + فاز ۰/۱/۲ + معیار پذیرش) |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.13.0 |
| `_tools/uat/tester293-backup-urgent.js` | **جدید** (۱۵/۰) |
| `RELEASE-NOTES-v33.13.0.md` / `REGRESSION-REPORT-v33.13.0.md` | اسناد انتشار |

## 🚦 بعدی
- **فاز ۱ (نشست بعد):** باکس «🔌 بررسی اتصال سرور» + بکاپ فقط در صورت تغییر (R6) + مهاجرت avatars به IDB + تست کامل.
- **فاز ۲:** بکاپ دلتا + ذخیرهٔ MySQL + بازگردانی تراکنشی (هم‌راستا با مهاجرت MySQL).
- راستی‌آزمایی استیجینگ: v33.13.0.
