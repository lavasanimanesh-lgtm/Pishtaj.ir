# Release Notes v34.7.6 — همسان‌سازی بازگشت پیشنهاد برنده

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## علت اختلاف Staging و Production

- فایل دکمه در هر دو محیط وجود داشت و مشکل از حذف‌شدن asset در استقرار Production نبود.
- دکمه در `backup.js` پس از render و فقط وقتی `curRole() === 'admin'` بود تزریق می‌شد.
- دامنه‌های Staging و Production نشست و user store جدا دارند؛ نشست Staging با نقش admin دکمه را نشان می‌داد، اما حساب Production با نقش رئیس هیئت‌مدیره از شرط عبور نمی‌کرد.
- نشست‌های قدیمی که فقط `role` داشتند نیز توسط `curRole()` به‌اشتباه `sales` تلقی می‌شدند و ابزار اصلاح بدون توضیح ناپدید می‌شد.

## اصلاح دائمی

- طبق تصمیم کارفرما، «بازگرداندن کنترل‌شده برد» برای نقش‌های `admin` و `chairman` مجاز شد.
- حذف قطعی پیشنهاد همچنان فقط برای `admin` مجاز است.
- دکمه بازگشت از hook دیرهنگام `backup.js` خارج و مستقیماً در رندر اصلی پیشنهاد قرار گرفت؛ desktop/mobile و Staging/Production اکنون قرارداد واحد دارند.
- `curRole()` نشست‌های قدیمی دارای `role` ولی فاقد `roleId` را صحیح نرمال می‌کند.
- action موبایل تابع جدید را می‌شناسد و دکمه در منوی عملیات گم نمی‌شود.

## کنترل داده و امنیت

- مسیر قدیمی `adminUnwin` دیگر `localStorage`، پیشنهاد و `ptf_crm_projects` را مستقیم تغییر نمی‌دهد.
- بازگشت فقط از فرمان اتمیک `sales-domain.php`، با دلیل اجباری، ثبت correction/audit و projection سرور انجام می‌شود.
- اگر پرونده یا فاکتور وابسته وجود داشته باشد، عملیات fail-closed متوقف و وابستگی‌ها به کاربر نمایش داده می‌شوند.
- برای یافته «پیشنهاد برنده بدون پرونده»، دکمه بازگرداندن کنترل‌شده مستقیماً کنار همان یافته دیده می‌شود.
- کاربران فاقد مجوز به‌جای ناپدیدشدن خاموش دکمه، توضیح نقش مجاز را در باکس یافته‌ها می‌بینند.

## کنترل کیفیت

- `tester411-v34.7.6-winner-repair-parity.js`: 14 PASS / 0 FAIL
- `tester403-v35-sales-to-cash.js`: PASS
- قرارداد اصلاح‌شده `tester63`: 38 PASS / 2 FAIL تاریخی و نامرتبط؛ assertion قفل برد با متن جدید کنترل‌شده PASS است.
- `tester8-sprint71.js`: 60 PASS / 1 FAIL تاریخی و نامرتبط `.htaccess`؛ همه قراردادهای جدید بازگشت برد PASS هستند.
- CI متمرکز ریلیز: 30 PASS / 0 FAIL
- Auditor روی 624 صفحه و هم‌ترازی VERSION/PWA: PASS
- رگرسیون کامل: 394 تستر، 337 فایل PASS، 57 فایل بدهی تاریخی FAIL، 5833 check PASS و 129 check تاریخی FAIL؛ شکست جدیدی نسبت به v34.7.5 باقی نماند.
- PHP CLI در محیط محلی موجود نبود و lint محلی PHP در CI به‌صورت صریح skip شد؛ syntax فایل‌های JS و `git diff --check` سبز هستند.
