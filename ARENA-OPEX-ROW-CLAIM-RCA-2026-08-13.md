# RCA — رگرسیون ادعای رویداد لینک‌شده هزینه جاری (OPEX-ROW-CLAIM) — v34.5.9

**تاریخ:** 2026-08-13
**شناسه:** `OPEX-ROW-CLAIM-REGRESSION`
**منشأ کشف:** تستر `tester340-v34.4.46-opex-row-identity-collapsed-docs.js` (تنها کرش باقی‌مانده پس از بازسازی سوئیت)
**نوع:** رگرسیون واقعی محصول (بی‌صدا، روی داده مالی) — معرفی‌شده در بازطراحی FIN-WF-P3 (v34.4.54+)

---

## ۱) علائم (در تستر)

ویرایش یک هزینهٔ جاری با کد تکراری (دو ردیف با `cd` یکسان ولی `_opexRowId` متفاوت):
- ردیفِ کلیک‌شده درست ویرایش می‌شد،
- اما رویداد `costEvents` پروندهٔ فروش **مبلغ جدید نمی‌گرفت** و در عوض رویدادِ «خواهر» (متعلق به ردیف دیگر) بازنویسی می‌شد → مبلغ پرونده و سود پروژه بی‌صدا خراب می‌شد.

## ۲) ریشه

1. **پس از v34.4.46:** همگام‌سازی رویداد پرونده داخل `opex.js` با `opexDealEvent()` انجام می‌شد که سه‌لایه ایمنی داشت:
   شناسهٔ ردیف → رویداد لگاسی یکتا → در حالت چندتایی فقط تطبیقِ مبلغِ یکتا؛ وگرنه **null** (هیچ رویدادی دست‌کاری نمی‌شد).
2. **بازطراحی FIN-WF-P3 (v34.4.54+):** مسیر به `finance-write-guard.js` → `window.ptfDealCostSync` منتقل شد. `findEv` داخلی آن از `window.ptfDealCostMatch` استفاده می‌کرد که برای `opex` این بود:
   ```js
   if (rec._opexRowId && ev.opexRowId === rec._opexRowId) return true;
   return !!(ev.fromOpex && !ev.opexRowId && ev.cd === rec.cd);   // ← بی‌ابهام‌نبودن!
   ```
   در کدهای تکراری، اگر ردیف هنوز رویداد `opexRowId`دار نداشت، **اولین رویداد لگاسی هم‌کد** (هر کدام بود — معمولاً رویداد ردیف دیگر) ربوده و با مبلغ/شناسهٔ ردیف جاری بازنویسی می‌شد. رویداد واقعی ردیف جاری هم دست‌نخورده می‌ماند.
3. **چرا بی‌صدا:** تستر v34.4.46 بعد از v34.4.54 خراب شده بود (هم پین نسخه و هم تغییر مسیر) → در چرخهٔ ریلیز سبز نمی‌ماند → رگرسیون تا این نشست پنهان بود. (مثال تازه از همان الگوی tester101 در گذشته.)

## ۳) اصلاح (v34.5.9)

در `crm/finance-write-guard.js` → `ptfDealCostSync`، تابع `findEv` برای `source === 'opex'` با همان قرارداد سه‌لایهٔ `opexDealEvent` بازنویسی شد:

1. تطبیق دقیق `opexRowId` (مقدم بر همه)
2. رویداد لگاسی فقط وقتی **یکتا** باشد (یک هم‌کد)
3. در حالت چندتایی: فقط تطبیقِ مبلغِ یکتا
4. وگرنه **null** → رویداد جدید ساخته می‌شود و هیچ رویداد موجودی دست‌کاری/ربوده نمی‌شود

هیچ تغییری در مسیر `petty` و سایر منابع داده نشد.

## ۴) راستی‌آزمایی

- `tester340` — PASS (قبلاً کرش؛ شامل سناریوی ویرایش ردیف تکراری و گارد «سرقت خواهر»)
- شبیه‌سازی مستقل باگ قبلی: ویرایش ردیف B دیگر رویداد A را نمی‌رباید ✅
- `tester396` (ci-gate-contracts) و سایر تسترهای مالی گیت — PASS
- **گیت CI (۱۷ تستر): ۱۷/۱۷ سبز**
- رگرسیون کامل: **۲۵۶ پاس / ۱۴۳ قرمز / ۰ کرش** (هیچ فایل جدیدی نسبت به اجرای قبل قرمز نشد)
- نسخه/کش هماهنگ: `VERSION.json`، `index.html` (۹۷ اسکریپت)، `sw.js`، `manifest.json`، `clear-cache.html`، `shell.js` → `v34.5.9`

## ۵) درس فرآیندی

- بازطراحی‌های مسیر مالی باید **تسترِ مالکیتِ سناریوی قدیمی را هم به‌روز کنند یا صریحاً بازنشسته‌اش کنند** — تستر سبزِ از-دست‌رفته دقیقاً جایی است که رگرسیون بی‌صدا پنهان می‌شود.
- `tester340` به گیت CI اضافه شد تا این کلاس برای همیشه قبل از FTP مهار شود.
