# 📦 ریلیزنوت v14.0 — اسپرینت ۱: سلامت داده + US-353 (ادمین محل ارجاع نیست)

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v13.9 | قدیمی‌ترین تعهدهای باز پروژه در این اسپرینت بسته شدند.

---

## ✅ US-260 — حذف سند → وضعیت درخواست یتیم نمی‌ماند
پس از هر «حذف ایمن با دلیل» (پیشنهاد/فاکتور/پرونده/درخواست)، وضعیت گردش کار درخواست‌های مرتبط **خودکار بازمحاسبه** می‌شود — مثلاً با حذف تنها CO یک درخواست، وضعیت از «پیش‌فاکتور» به مرحله درست قبلی برمی‌گردد و در تاریخچه گردش کار با ذکر سند حذف‌شده ثبت می‌شود. (ایزوله — اگر ماژول گردش کار لود نبود، حذف اصلی هیچ‌وقت نمی‌شکند.)

## ✅ US-263 — هرس خودکار صف‌ها
همراه چرخه بک‌آپ ساعتی (بدون هیچ polling جدید):
- **صف ارسال:** پیامک‌های ارسال‌شده قدیمی‌تر از ۳۰ روز حذف
- **تاریخچه گردش کار:** حداکثر ۵۰ رویداد اخیر برای هر درخواست
- سقف‌های قبلی (اعلان‌ها ۱۰۰۰ / audit ۲۰۰۰) پابرجا → رشد بی‌پایان حافظه متوقف شد

## ✅ US-264 — آرشیو حذف‌ها چنددستگاهه شد
`ptf_crm_deleted_archive` (سوابق حذف با دلیل) به هر سه لایه اضافه شد: **سینک بین دستگاه‌ها + بک‌آپ + whitelist سرور** + هرس ۵۰۰تایی. دیگر سوابق حذف فقط روی یک مرورگر نمی‌ماند.

## ✅ BUG-014 — حذف «مالی» قدیمی
پنل «مالی» با کلید داده‌ای legacy کار می‌کرد که **هیچ ماژول جدیدی به آن نمی‌نویسد** → عملاً همیشه خالی بود و کاربر را گیج می‌کرد. از منو حذف شد (حذف نرم). جایگزین‌های واقعی: فاکتورها، مطالبات، تنخواه، تحلیلگر و سود خالص.

## ✅ US-353 — ادمین محل ارجاع فرآیندهای شرکت نیست (یوزر استوری شما)
- ادمین از فهرست گیرندگان **ارجاع درخواست‌ها و فرآیندهای بازرگانی حذف شد**
- **استثنای کنترل‌شده:** تیک جداگانه «🔧 مسئله خاص فنی/پشتیبانی سیستم است» → فقط در این حالت گزینه «ادمین سیستم — فقط پشتیبانی فنی» فعال می‌شود؛ با برداشتن تیک، فهرست عادی برمی‌گردد
- ارجاع به خود همچنان ممنوع

## 🧪 کیفیت
تستر جدید `tester60` ۱۹/۱۹ ✅ | رگرسیون **۴۷ تستر** ✅ | audit صفر خطا ✅

## 📁 فایل‌های تغییریافته
`crm/guards.js` (US-260 + هرس آرشیو) · `crm/backup.js` (pruneQueues + کلید آرشیو) · `crm/sync.js` (کلید آرشیو) · `api/crm.php` (whitelist) · `crm/bridge.js` (US-353) · `crm/index.html`/`shell.js` (حذف fin + VER v14.0) · `crm/sw.js` · `_tools/uat/tester60` (جدید)

⚠️ `api/crm.php` تغییر کرده — جایگزین شود.

## 📌 اسپرینت بعدی طبق نقشه راه
اسپرینت ۲ (سود و مهلت‌ها): US-347 (حاشیه سود در CO) + US-348 (مهلت پاسخ درخواست) + US-352 (سقف اعتبار مشتری) — با اعلام شما آغاز می‌شود.
