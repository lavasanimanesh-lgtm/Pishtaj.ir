# RELEASE NOTES — v34.7.67 (۱۴۰۵/۰۵/۳۰ — 2026-08-21)

## چت هوشمند سایت — ارتقای بزرگ

ویجت چت از «منشی قاعده‌محور» به یک دستیار واقعی ارتقا یافت، **رایگان و با سهمیهٔ توکن جدا**.

### ۱) وضعیت درخواست و سفارش داخل چت (بدون مصرف توکن)

- کاربر کد رهگیری را می‌پرسد (مثلاً «وضعیت سفارش PTF-RFQ-XXXXX-XXXXX»):
  - وضعیت استعلام/تامین‌کننده از `track` خوانده می‌شود (مسیر قطعی و رایگان — LLM صدا زده نمی‌شود).
  - اگر سفارش ابلاغ شده باشد، **مراحل پیشرفت سفارش** با تیک نمایش داده می‌شود:
    ابلاغ ← حمل ← فاکتور ← تحویل و تسویه.
- **حذف محرمانگی:** فقط مرحلهٔ عمومی نمایش داده می‌شود؛ هیچ مبلغ، نام مشتری یا تاریخ مالی نشت نمی‌کند.

### ۲) سهمیهٔ توکن مجزا و رایگان

- چت عمومی از فایل **جداگانهٔ** `ptf_chat_public_quota.json` استفاده می‌کند:
  - سقف روزانهٔ هر بازدیدکننده (۲۵) + سقف سراسری روزانه (۳۰۰) برای محافظت از بودجه.
- **سهمیهٔ هوش مصنوعی کاربران CRM (`ai_quota.json`) دست‌نخورده می‌ماند** — کاملاً مستقل.

### ۳) پاسخ فنی با دانش مرکز دانش (RAG سبک)

- سرور ۳ مقالهٔ مرتبط را از `search-index.json` (۶۵۳ صفحه) بازیابی و به مدل تزریق می‌کند؛
  پاسخ فنی با **لینک مقالهٔ منبع** ارائه می‌شود (کاهش توهم).

### ۴) چندزبانه — فارسی / English / العربية

- تشخیص زبان سمت کلاینت و سرور؛ پاسخ به زبان کاربر.
- احوال‌پرسی/تشکر عربی هم در موتور محلی (بدون توکن) پشتیبانی شد.

### ۵) رفع باگ «ارسال به کارشناس»

- قبلاً فقط در localStorage می‌ماند و **هرگز به CRM نمی‌رسید**. حالا به‌صورت **لید واقعی**
  از اکشن عمومی جدید `chat_lead` در `ptf_crm_leads` ذخیره می‌شود (با rate limit).

### ۶) پشتیبانی Gemini + OpenAI

- پروکسی حالا هم Gemini (پیش‌فرض) و هم OpenAI-compatible را پشتیبانی می‌کند.

### فایل‌ها

`api/chat-llm.php` (بازنویسی) · `api/crm.php` (chat_lead + order در track) · `assets/js/ptf-chat.js`.

### تست

- تستر جدید `tester469-v34.7.67-smart-chat.js` (در گیت CI).
- **گیت CI: ۸۵ PASS / ۰ FAIL** · نگهبان معماری: PASS.

### پیش‌نیاز روی سرور

- `llm-config.php` خارج از webroot (provider=gemini توصیه می‌شود) — برای پاسخ فنی.
- وضعیت درخواست/سفارش **بدون LLM هم کار می‌کند** (فقط به `track` نیاز دارد).
