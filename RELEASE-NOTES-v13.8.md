# 📦 ریلیزنوت v13.8 — لوگوی بات + پوشش کامل اعلانات + ارسال پیام از طریق بات

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v13.7

---

## 🎨 لوگوی بات شرکت
لوگوی اختصاصی بات طراحی شد: ربات دوستانه + حباب گفتگو + زنگ اعلان، روی گرادیان نارنجی برند (ef4b1a→f79400) — مینیمال و مناسب آواتار دایره‌ای تلگرام/بله.
- فایل‌ها در بسته: `assets/images/bot-logo.png` (۱۰۲۴) و `bot-logo-640.png` (سبک — مناسب آپلود در BotFather)
- **نصب:** BotFather → `/setuserpic` (یا Edit Info → Edit Botpic در صفحه بات) → ارسال تصویر

## 🔔 US-339 — پوشش کامل اعلانات بات
قبلاً فقط ۳ نوع (چک/سیستم/ادمین) به گروه می‌رفت. حالا **همه اعلانات مهم + ارجاعات**:

| نوع | آیکون در گروه |
|---|---|
| ارجاع درخواست (جدید ✅) | 📨 |
| چک‌ها | 🏦 |
| پرداخت/وصولی | 💰 |
| قیمت خرید جدید | 🛒 |
| تغییر وضعیت | 📊 |
| یادآورها | ⏰ |
| سیستمی / ادمین | ⚙️ / 👑 |

کلید روشن/خاموش در تنظیمات همچنان حاکم است؛ سقف ایمنی ۳۰ پیام/ساعت پابرجا.

## ✍️ US-340 — نوشتن و ارسال پیام از طریق بات
- دکمه **«✍️ نوشتن پیام به گروه»** در تنظیمات (بخش بات) + دکمه دسترسی سریع در **کارتابل**
- مودال ساده: متن را بنویسید → «📤 ارسال به گروه» → پیام با **نام فرستنده** در گروه تلگرام/بله ظاهر می‌شود: «💬 پیام از حامد لاوسانی: …»
- وضعیت «در حال ارسال»، خطای شفاف در صورت مشکل کانفیگ، و ثبت در audit

## 🧪 کیفیت
تستر جدید `tester58` ۱۱/۱۱ ✅ | رگرسیون **۴۵ تستر** ✅ (tester55 با پوشش جدید سینک شد) | audit صفر خطا ✅

## 📁 فایل‌های تغییریافته
`crm/messengers.js` (KIND_ICON + گسترش CRITICAL + ptfBotCompose + دکمه کارتابل) · `assets/images/bot-logo.png` + `bot-logo-640.png` (جدید) · `crm/index.html`/`sw.js` (v13.8) · `_tools/uat/tester58` (جدید)

## 📌 یادآوری
برای فعال شدن این‌ها فقط کافی است `bot-config.php` (قالبی که قبلاً دادم) را روی هاست بگذارید و در تنظیمات تیک فعال‌سازی را بزنید.
