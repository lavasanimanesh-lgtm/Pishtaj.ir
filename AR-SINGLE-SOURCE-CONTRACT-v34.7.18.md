# قرارداد منبع واحد مطالبات (AR Single Source) — از v34.7.18

این قرارداد برای هر توسعه‌دهنده/ایجنتی است که در آینده روی «وصولی، مطالبات، حساب مشتری یا پروندهٔ فروش» کار می‌کند. هدف: تکرار نشدن باگی که در `ARENA-AR-RECEIPT-INTEGRATION-RCA-2026-08-16.md` ریشه‌یابی شد (پول پرداخت‌شده در یک نما دیده می‌شد و در نمای دیگر نه).

## ۱) منبع واحد

| موضوع | تنها مرجع مجاز |
|---|---|
| مانده/وصولی یک فاکتور | `PTF.ar.invoiceState(inv)` → `{billed, paid, open, allocated, legacyPaid, overPaid, stale, caseId, caseBinding}` |
| وضعیت مالی پرونده | `PTF.ar.caseState(caseRecord)` |
| وضعیت حساب مشتری | `PTF.ar.customerPosition(customerCd)` |
| «فاکتور فعال است؟» | `PTF.ar.activeInvoice(inv)` |
| سقف تخصیص فاکتور | `PTF.ar.invoiceCaps(inv)` — سمت سرور: `sd_invoice_caps()` |
| گزارش مغایرت | `PTF.ar.reconcile()` — UI: `ptfArReconcileOpen()` |

`PTF.invPaidSum` هنوز کار می‌کند و برای سازگاری نگه داشته شده، اما داخلاً به `PTF.ar` تفویض می‌کند. **هیچ ماژول جدیدی نباید فرمول مانده را دوباره بنویسد.**

## ۲) قواعد تغییرناپذیر

1. **سقف تخصیص هر فاکتور = `amount` قطعی سند** (نه `base+vat` خام). سهم VAT حداکثر تا `vat` ثبت‌شده.
2. **ارزش‌افزوده پس از صدور فاکتور قابل تخصیص است.** هیچ‌جا نباید مجوز VAT را از فیلد منجمد `timing` بخوانید؛ `timing` صرفاً گزارشی است.
3. **تخصیص فقط داخل پرونده انجام می‌شود.** فاکتور بدون `caseId` فقط با **تطبیق یکتای شمارهٔ پیشنهاد** به پرونده وصل می‌شود؛ تطبیق مبهم = عدم اتصال.
4. **هیچ رکورد پولی حذف فیزیکی نمی‌شود.** ابطال = علامت‌گذاری (`status:'void'` / `voided:true`) + دلیل + مهر زمان.
5. **پروجکشن نرسیده نباید پول را ناپدید کند.** اگر تخصیص محلی و ذخیره‌شده اختلاف داشتند، مقدار بزرگ‌تر ملاک نمایش و `stale=true` است؛ هیچ نوشتنی انجام نمی‌شود.
6. **اضافه‌پرداخت سقف‌گذاری نمی‌شود** تا اعتبار مشتری قابل محاسبه بماند؛ فقط `open` کف صفر دارد.
7. **رکورد `superseded` بدهی نمی‌سازد** — این تعریف در سرور، مطالبات و حساب مشتری یکسان است.
8. هر رویداد پولِ جدید باید `sourcePath` داشته باشد (`case_receipt` | `legacy_receivables` | `cheque_module`) تا در گزارش تسویه طبقه‌بندی شود.

## ۳) اتحادهایی که همیشه باید برقرار باشند

```
Σ دریافت‌های پرونده = Σ تخصیص + Σ بستانکاری
Σ ردیف‌های تخصیص فعال = Σ allocatedIRR رسیدها
invoice.amount = invoice.base + invoice.vat
مانده پنل مطالبات = مانده حساب مشتری = مانده پنجرهٔ پرونده
```
سه اتحاد اول را سرور در `sd_reconcile_case()` پس از هر بازسازی می‌سنجد و در `result.reconcile` برمی‌گرداند؛ اتحاد چهارم را `PTF.ar.reconcile()` و تستر `tester421` می‌سنجند.

## ۴) هنگام تغییر، این‌ها را اجرا کنید

```bash
node _tools/uat/tester421-v34.7.18-ar-receipt-integrity.js      # باید 0 FAIL بدهد
node _tools/audit/repro-receivables-payment-integration.js      # هر ۵ سناریو باید «هم‌خوان و درست» شود
node _tools/uat/run-ci-gate.js                                  # گیت CI
```

## ۵) کارهای عمداً انجام‌نشده (تصمیم آگاهانه)

- مسیرهای نوشتن میراثی (`savePay` در پنل مطالبات و وصول چک) **حذف نشدند**؛ حذف آن‌ها ریسک شکستن جریان‌های در حال استفاده را داشت. اثرشان اکنون از منبع واحد خوانده و در گزارش تسویه طبقه‌بندی می‌شود. اگر روزی حذف شدند، باید هم‌زمان مهاجرت داده و آموزش کاربر انجام شود.
- مهاجرت اجباری `payments[]` به رسید انجام نشد؛ مسیر موجود `migration_apply_safe` با تأیید مدیر باقی است.
