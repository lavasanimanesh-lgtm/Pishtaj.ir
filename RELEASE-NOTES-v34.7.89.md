# RELEASE NOTES — v34.7.89 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## ارزش افزوده فصلی در هاب مالی + نرخ مصوب سال مشترک (VAT-LEDGER-001)

### هدف
- یک تب جداگانه «ارزش افزوده» در هاب مالی برای محاسبه/نمایش بدهی و اعتبار ارزش افزوده
  هر فصل (جدا از سایر اطلاعات مالی).
- «نرخ مصوب ارزش افزوده» فقط یکجا در هاب مالی تعریف شود؛ فاکتور رسمی خرید و فروش
  از همان نرخ بخوانند (نه ورود مجدد در هر مودال).

### تغییرات
- `crm/vat-shared.js` (جدید):
  - `ptfVatRateOf(date)` → نرخ سال (پیش‌فرض ۱۰٪) از `ptf_crm_settings.vatRates[سال]`.
  - `ptfVatRateSet(year, pct, reason)` → ذخیره نرخ + تاریخچه.
- `crm/vat-quarterly.js` (جدید):
  - باکس «ارزش افزوده فصلی» در هاب مالی.
  - بدهی VAT = جمع `vat` فاکتورهای رسمی فروش.
  - اعتبار VAT = جمع `vatAmount` فاکتورهای رسمی خرید واقعی + `coverVatAmount` فاکتورهای پوششی.
  - **کارمزد فاکتورساز در VAT محاسبه نمی‌شود** (فقط هزینه جاری غیررسمی).
  - مانده = بدهی − اعتبار − اعتبار منتقل‌شده از فصل قبل.
  - اعتبار مازاد → به فصل بعد منتقل؛ بدهی مازاد → «تسویه» با **مدرک پرداخت** (فصل بعد از صفر).
- `crm/financehub.js`:
  - تب جدید «ارزش افزوده» + باکس `vatBox` + ترتیب/نمایش.
- `crm/official-invoice-v2.js`:
  - `vatDefault()` از `ptfVatRateOf`؛ دکمه «ذخیره پیش‌فرض سال» از مودال حذف شد
    (نرخ از هاب مالی خوانده می‌شود).
- `crm/supplier-finance.js`:
  - درصد اولیه فاکتور رسمی خرید از `ptfVatRateOf` خوانده می‌شود.

### رفتار
- درصد مصوب سال (پیش‌فرض ۱۰٪) در «هاب مالی → تب ارزش افزوده» قابل تغییر است.
- همه فاکتورهای رسمی کشور در همان سال از همین نرخ استفاده می‌کنند.
- غیررسمی در محاسبه VAT وارد نمی‌شود.

### تست
- `_tools/uat/tester492-v34.7.89-vat-quarterly.js` در گیت CI.
- گیت CI: **108 PASS / 0 FAIL**.

### توجه
- این باکس محاسبه/نمایش و ثبت «تسویه» است؛ داده‌های فاکتورها/هزینه‌ها را تغییر نمی‌دهد.
- کارمزد فاکتورساز به‌صورت «هزینه جاری غیررسمی» همان ماه ثبت/نمایش می‌شود (خارج از VAT).
