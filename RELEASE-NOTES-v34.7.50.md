# RELEASE NOTES — v34.7.50 (۱۴۰۵/۰۵/۲۹ — 2026-08-20)

> **یادداشت:** این سند پس از انتشار (در نشست v34.7.54) تکمیل شد؛ محتوا از commitهای
> `a102486` و `5ce26ca` و قراردادهای tester452 استخراج شده است.

## فرصت شغلی سایت و CRM (PDF، OTP، منوی پویا) + بازآرایی منوی سایت

### ۱) ماژول «فرصت شغلی» — سمت سایت

- صفحات جدید: `careers/index.html` (فارسی) و `en/careers.html` (انگلیسی) + ورودی‌های
  sitemap (`sitemap-misc.xml`).
- بک‌اند جدید `api/careers.php` (در allow-list `api/.htaccess`):
  - فرم عمومی فقط وقتی آگهی منتشرشده وجود دارد ساخته می‌شود.
  - **رزومه فقط PDF با سقف ۵ مگابایت**؛ آپلود ابری با پیشوند `careers-resume`.
  - ضدربات: **کپچا + OTP موبایل** در ثبت درخواست + فیلد honeypot (`website`).
  - **retention ۱۸۰ روز** برای دادهٔ متقاضی؛ PII جدا از سینک دادهٔ فروش نگه‌داری می‌شود.
  - بستن آگهی = `published:false` (حذف نمی‌شود).
- فرم متقاضی ساخت‌یافته است، نه متن آزاد: مدرک (۵ سطح استاندارد)، سابقه (۵ بازه)،
  حقوق درخواستی (بازه‌ای + «سایر» بدون متن آزاد).
- **منوی پویا:** دکمهٔ «فرصت شغلی» در منوی سایت فقط وقتی ظاهر می‌شود که
  `careers/status.json` آگهی منتشرشده گزارش کند (`ptf-discover.js`؛ مسیر API بر اساس
  عمق صفحه در `ptf-guard.js`).

### ۲) ماژول «فرصت شغلی» — سمت CRM

- پنل جدید `crm/careers.js` در سایدبار (GROUPS): ساخت/ویرایش آگهی، برداشتن/انتشار
  مجدد، مشاهدهٔ درخواست‌ها.
- **دسترسی فقط `admin` / `chairman` / `ceo`** — احراز سمت سرور با توکن
  (`X-CRM-Token`)، نه هدر نقش قابل‌جعل.
- آگهی دوزبانه FA+EN روی یک صفحه.

### ۳) بازآرایی منوی سایت (commit همراه، بدون بامپ نسخه)

- «محصولات» به نوار بالای سایت آمد؛ «اخبار» و «وبلاگ» زیر «درباره ما» منتقل شدند.
- URLهای `/blog/` و `/news/` بدون تغییر؛ لینک‌های داخلی زیرمنو و پاورقی حفظ شدند.

### تست

- تستر جدید `tester452-v34.7.50-careers-site-crm.js` (در گیت CI) — ۳۰+ قرارداد شامل
  سقف PDF، کپچا/OTP، honeypot، retention، نقش‌ها و منوی پویا.

### فایل‌های اصلی

`api/careers.php` · `crm/careers.js` · `careers/` · `en/careers.html` ·
`assets/js/ptf-careers-apply.js` · `assets/js/ptf-discover.js` · `assets/js/ptf-guard.js`

### نکتهٔ عملیاتی (برای فعال شدن واقعی)

انتشار اولین آگهی از CRM + پیکربندی SMS (برای OTP) روی سرور لازم است؛ تا آن زمان
`careers/status.json` صفر است و دکمهٔ منو ظاهر نمی‌شود (طراحی عمدی).
