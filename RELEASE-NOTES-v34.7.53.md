# RELEASE NOTES — v34.7.53 (۱۴۰۵/۰۵/۲۹ — 2026-08-20)

> **یادداشت:** این سند پس از انتشار (در نشست v34.7.54) تکمیل شد؛ محتوا از commit
> `d806916` و قراردادهای tester456 استخراج شده است.

## remap دستی گروه B کلید ابری — بدون حذف فایل

ادامهٔ v34.7.52: برای کلیدهای ردهٔ **B** (تطبیق یکتا در باکت) مسیر اصلاح امن اضافه شد.

### طراحی ایمنی (سه‌مرحله‌ای)

1. **`ptfPlanCloudKeyRemap`** — برنامهٔ remap را می‌سازد؛ فقط ردهٔ B؛ رد می‌کند:
   - فهرست ناقص S3 (`error: 'truncated'`) — دکمه هم تا فهرست کامل غیرفعال است،
   - کلیدهای `archives/`،
   - تطبیق‌های متعارض (یک کلید با دو مقصد).
2. **`ptfConfirmCloudKeyRemap`** — تأیید صریح **نقش ارشد**؛ بدون تأیید
   (`confirm_required`) هیچ چیزی اعمال نمی‌شود.
3. **`ptfApplyCloudKeyRemap`** — فقط **metadata محلی CRM** را به‌روز می‌کند
   (`PTF_CLOUD_REMAP_FIELDS` — شامل `key` و مشابه‌ها؛ **بدون** `archiveKey` و **بدون**
   `path` عمومی). **هیچ DELETE ابری زده نمی‌شود** (بدون `delete_batch` /
   `STORAGE_API` / `archive_zip` در مسیر apply).

- نتیجه در **audit log** ثبت می‌شود (تعداد کلید/فیلد/مخزن) و گزارش پس از اعمال
  بازساخته می‌شود.
- تشخیص `file_not_found` و پیام «کلید قدیمی/مهاجرت‌نشده» دست نخورد.

### تست

- تستر جدید `tester456-v34.7.53-cloud-key-remap-b.js` (در گیت CI) — شامل اجرای sandbox
  واقعی `plan`/`remap` با سناریوهای truncated، archives، تطبیق متعارض و عدم تغییر
  `archiveKey`.

### فایل‌های اصلی

`crm/storage.js` · نقاط رسمی نسخه

### باقی‌مانده (برای نشست‌های بعد)

- اجرای عملی گزارش + remap روی دادهٔ Production (با کاربر ارشد).
- تعیین تکلیف ردهٔ **C** (استخراج از zip بایگانی) و ردهٔ **E** (آپلود مجدد).
- RCA اینکه چرا کلیدها از ابتدا منحرف شدند.

### ⚠️ یادداشت پسینی

این نسخه با گیت CI قرمز (تخلف A2 از v34.7.52) merge و دیپلوی شد؛ در **v34.7.54**
اصلاح شد. این اتفاق ضرورت مورد باز H1 (اجرای گیت UAT در workflow پیش از FTP) را
دوباره نشان داد.
