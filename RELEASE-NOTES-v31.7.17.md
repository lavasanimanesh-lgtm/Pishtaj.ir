# Release Notes — v31.7.17

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-19)
**نسخه:** v31.7.17
**نوع:** 🟢 رفع فلش تم قدیمی (FOUC) + گرافیک جدید نوار پایین موبایل طبق مرجع بصری کارفرما (BUG-FOUC-001 + US-MNV-DELIGHT)

---

## 🎯 درخواست کارفرما

1. هنگام رفرش (موبایل و دسکتاپ)، ابتدا «تم قبلی» نمایان می‌شود و تم اصلی با تاخیر می‌آید (اسکرین‌شات پیوست کارفرما).
2. آیکون‌های نوار پایین موبایل با تغییر تب، گرافیکی مانند نمونه اپ مالی پیوست‌شده داشته باشند.
3. تشکیل جلسه با متخصصان نرم‌افزار و طراحان ارشد گرافیک موبایل برای ایده‌های جذاب‌سازی — و پیاده‌سازی موارد **بی‌خطر**.

## 🔍 علت ریشه‌ای FOUC (BUG-FOUC-001)

پوسته اصلی (فونت وزیرمتن، استایل سایدبار، مخفی‌سازی سایدبار در موبایل، رزرو جای نوار پایین) همگی از `theme.js` و `mobilenav.js` تزریق می‌شوند که **انتهای body** لود می‌شوند. مرورگر ابتدا صفحه را با CSS خام (همان «تم قبلی» در اسکرین‌شات: ستون ایموجی‌ها، فونت سیستم، کارت‌های بی‌استایل) رندر می‌کرد و بعد از parse شدن اسکریپت‌ها پوسته اصلی می‌نشست. (نکته: تمِ «تاریک/روشن» از قبل با اسکریپت head درست بود — مشکل، CSSِ پوسته بود نه متغیرهای رنگ.)

**رفع:** زیرمجموعه بحرانی پوسته به `<style id="ptfCriticalTheme">` در head منتقل شد: فونت، بدنه سایدبار/act، سایه پنل‌ها، `scrollbar-gutter`، و در موبایل مخفی‌سازی سایدبار + `padding-bottom` نوار پایین از **فریم اول**. theme.js/mobilenav.js همان CSS را دوباره تزریق می‌کنند (idempotent — منبع واحد رفتار نهایی تغییری نکرده).

## 🎨 صورت‌جلسه پنل متخصصان (سند کامل: `EXPERT-PANEL-MOBILE-UX-v31.7.17.md`)

پنل ۵نفره (طراح ارشد UI فینتک، متخصص Motion، مهندس PWA، وتوی QA، نماینده کاربر) مرجع بصری کارفرما را تحلیل کرد؛ ۶ ایده تصویب و اجرا شد، ۲ ایده رد قطعی (کتابخانه خارجی، صدا) و ۴ ایده تعویق با دلیل (pull-to-refresh، skeleton، بازطراحی تم تیره، ویجت داشبورد — نیازمند approval مستقل).

## ✅ تغییرات اجراشده

| # | مصوبه | تغییر | فایل |
|---|-------|--------|------|
| A1 | رفع FOUC | CSS بحرانی پوسته در head | `crm/index.html` |
| A2 | گرافیک تب فعال (الگوی مرجع) | آیکون تب فعال با spring ملایم بالا می‌آید و در pill گرادیانی برند می‌نشیند؛ لیبل bold؛ نقطه نشانگر زیر تب؛ فشردن لمسی scale | `crm/mobilenav.js` |
| A3 | FAB زنده | پالس سایه ۳.۲ثانیه‌ای بسیار ملایم + فشرده‌شدن هنگام لمس | `crm/mobilenav.js` |
| A4 | هپتیک ظریف | vibrate 8ms هنگام تعویض تب (فقط با پشتیبانی مرورگر، try/catch) | `crm/mobilenav.js` |
| A5 | ورود نرم پنل | fade+slide-up ۱۸۰ms هنگام تعویض ماژول در موبایل | `crm/mobilenav.js` |
| A6 | دسترس‌پذیری | `prefers-reduced-motion` تمام انیمیشن‌ها را خاموش می‌کند | هر دو |

## 🔒 قیدهای بی‌خطری (شرط کارفرما — تأیید QA پنل)

- فقط `transform`/`opacity` انیمیت می‌شود (GPU-composited، بدون reflow) — منطق داده/sync/auth/RBAC/goPanel **صفر تغییر**.
- بدون هیچ کتابخانه/CDN خارجی (منشور Self-Contained).
- کاملاً قابل‌بازگشت: حذف دو بلاک CSS + یک خط vibrate.

## 📊 نتایج گیت

- تستر جدید `tester192-mobile-theme-fouc`: **۱۷/۱۷ PASS** (CSS بحرانی در head، همه افکت‌ها، قیدهای QA)
- رگرسیون کامل: **170/170 فایل PASS — 3950/3950 چک PASS**
- `audit.py` (۱۰ بخش): PASS

## 📋 راستی‌آزمایی توسط کارفرما (۳ دقیقه)

1. **FOUC:** موبایل و دسکتاپ را چند بار هارد رفرش (Ctrl+F5) کنید → از همان لحظه اول پوسته اصلی (فونت وزیرمتن، بدون ستون ایموجی سایدبار در موبایل) دیده شود — بدون فلش تم خام.
2. **تب‌ها:** در موبایل بین داشبورد/کارتابل/دستیار جابه‌جا شوید → آیکون تب فعال بالا بیاید و در کپسول رنگی بنشیند + نقطه زیر آن + لرزش خیلی ظریف.
3. FAB «پیشنهاد» نفس بکشد و با لمس فشرده شود؛ پنل‌ها با اسلاید نرم وارد شوند.
4. (اختیاری) در تنظیمات گوشی Reduce Motion را فعال کنید → همه حرکت‌ها خاموش شوند.

---
*توسعه‌دهنده: PTF Development Team — 2026-07-19*
