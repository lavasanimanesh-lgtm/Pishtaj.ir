# 📦 ریلیزنوت v13.5 — اجرای مصوبات: پیام‌رسان‌ها (US-332) + بات اعلان (US-333) + بسته APK (US-334)

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v13.4

---

## 💬 US-332 — دکمه‌های پیام‌رسان (هر کاربر با اکانت خودش)

- دکمه **💬** روی هر ردیف مشتری و تامین‌کننده → پنجره ارسال پیام:
  - انتخاب **متن آماده** (از US-330 — نام مخاطب خودکار جایگذاری) یا نوشتن متن آزاد
  - ۵ دکمه: 🟢 واتساپ | 🔵 تلگرام | 🟩 بله | 🟧 ایتا | 🟣 روبیکا
  - واتساپ: چت با **متن آماده داخل باکس پیام** باز می‌شود (wa.me) — شماره ایران خودکار +98
  - بقیه پیام‌رسان‌ها: چت باز + متن خودکار **کپی** می‌شود (این‌ها پارامتر متن ندارند — فقط Paste کنید)
- بخش «⚙️ شناسه‌های پیام‌رسان» برای هر مخاطب: آیدی تلگرام/بله/ایتا/روبیکا یک بار ثبت و ذخیره می‌شود
- چون از deep-link استفاده می‌شود، **ارسال همیشه از اکانت خودِ کاربر لاگین‌شده در آن پیام‌رسان** انجام می‌شود — بدون سرور واسط، بدون ریسک مسدودی

## 🤖 US-333 — بات اعلان تلگرام + بله

- سرور جدید `api/notify-bot.php`: ارسال به بات تلگرام (API رسمی) و بله (tapi.bale.ai) — توکن‌ها در `bot-config.php` **خارج از public_html** (الگوی امن sms-config) + گارد دامنه + سقف ۳۰ پیام/ساعت
- اعلان‌های **حیاتی** (یادآور چک، رویداد سیستمی مثل مختومه شدن پرونده، اقدامات ادمین) خودکار به گروه شرکت می‌روند — با کلید روشن/خاموش در تنظیمات
- تنظیمات ← بخش جدید «🤖 بات اعلان»: **تست اتصال** + **ارسال پیام آزمایشی** + فعال‌سازی

### 📘 راهنمای گام‌به‌گام شما: `BOT-SETUP-GUIDE.md` (در ریشه بسته)
خلاصه ۴ بخش (~۱۵ دقیقه):
1. **بات تلگرام:** @BotFather → `/newbot` → توکن | گروه مدیران بسازید + بات را عضو کنید | آدرس getUpdates → Chat ID (عدد منفی)
2. **بات بله:** همان مراحل با @botfather بله و tapi.bale.ai
3. **فایل `bot-config.php`:** قالب آماده داخل راهنما — با File Manager **یک پوشه بالاتر از public_html** (کنار sms-config.php)
4. **CRM ← تنظیمات:** تست اتصال ✅ → پیام آزمایشی ✅ → تیک فعال‌سازی
⚠️ توکن‌ها را هرگز در چت نفرستید — فقط در فایل کانفیگ روی هاست. جدول عیب‌یابی هم داخل راهنماست.

## 📲 US-334 — بسته APK (TWA)

- `.well-known/assetlinks.json` آماده در بسته — با آپلود همین zip در ریشه هاست قرار می‌گیرد
- `_tools/apk/twa-manifest.json` — کانفیگ کامل Bubblewrap (پکیج ir.pishtaj.crm، رنگ برند، آدرس شروع CRM)
- `_tools/apk/APK-BUILD-GUIDE.md` — راهنمای ساخت: نصب Bubblewrap → `bubblewrap build` → خروجی APK امضاشده → جایگذاری اثر انگشت keystore در assetlinks
- ساخت APK به محیط Node+JDK نیاز دارد (~۱۵ دقیقه، هر کامپیوتری)؛ پس از ساخت، **آپدیت‌های CRM بدون نصب مجدد** به گوشی‌ها می‌رسند

## 🧪 کیفیت
تستر جدید `tester55` ۲۳/۲۳ ✅ | رگرسیون **۴۲ تستر** ✅ | audit صفر خطا ✅ | حضور `.well-known` در zip تایید شد ✅

## 📁 فایل‌های جدید/تغییریافته
`crm/messengers.js` (جدید) · `api/notify-bot.php` (جدید) · `api/.htaccess` (مجوز notify-bot) · `BOT-SETUP-GUIDE.md` (جدید — راهنمای شما) · `.well-known/assetlinks.json` + `_tools/apk/*` (جدید) · `crm/index.html`/`sw.js` (v13.5) · `_tools/uat/tester55` (جدید)

⚠️ در آپلود: `api/notify-bot.php` و `api/.htaccess` جدید هستند؛ پوشه `.well-known` هم باید در ریشه سایت قرار گیرد.
