# یادداشت انتشار — v34.37.3

**تاریخ:** ۲۰۲۶-۰۹-۰۵ (۱۴۰۵/۰۶/۱۴) · **پایه:** v34.37.2
**گیت CI:** ۲۱۵ PASS / ۰ FAIL · **نگهبان معماری:** PASS

دو خواستۀ کارفرما روی **پنل فاکتورهای رسمی**، هر دو در لایۀ نمایش/همگام‌سازی محلی:

> ① «وقتی برای یک فاکتور لغو ارجاع می‌زنیم باید بلافاصله از فهرست موجود در صفحه کلاً پاک
> شود… باید فاکتور ارجاع‌شده کلاً در ردیف‌های فاکتورهای ثبت‌شده یا ارجاع‌شده حذف گردد.»
> ② «همچنین یک بهم‌ریختگی در چینش ستون‌ها در این قسمت دیده می‌شود.»

---

## ① «لغو ارجاع» حالا همان لحظه ردیف را برمی‌دارد (INV-REF-UNDO-IMMEDIATE)

**چرا ردیف می‌ماند؟** پنل فاکتورها **ارجاع‌محور** است: `renderInvoices` ردیف‌ها را از
`ptf_crm_offers` با شرط `o.invRef` می‌سازد (`crm/official-invoice-v2.js:153`). فرمان
`revoke_invoice_ref` روی سرور همان `invRef` را پاک می‌کند، ولی نمایشِ کلاینت به دو چیز
واسته بود که هر دو به‌تعویق می‌افتادند:

1. رندر فقط در `onAck` صدا زده می‌شد — یعنی بعد از پایان round-trip؛ و
2._projectionِ پاسخ فرمان می‌توانست رد شود_: `applyProjection` برای **همۀ** کلیدها همان
   `rev` سراسری را می‌فرستد و `crm/sync.js:887` هر projection را که rev‌اش کوچک‌تر از
   `krevs` محلی همان کلید باشد بازمی‌گرداند (`return false`). اگر در فاصله‌ای دستگاه
   دیگری روی پیشنهادهای دیگر نوشته بود، `krevs['ptf_crm_offers']` جلوتر می‌رفت و
   projectionِ همین فرمان انداخته می‌شد؛ `invRef` در آینه زنده می‌ماند و رندر دوباره همان
   ردیف را می‌ساخت. پولِ دلتا هم درست در همین حالت چیزی برنمی‌گرداند، چون rev آن کلید
   عوض نشده است (`api/crm.php:2537`).

**اکنون** (`crm/sales-domain-v2.js` — `ptfRevokeInvoiceRef`):

| گام | رفتار |
|---|---|
| پیش از ارسال | تغییر محلیِ **revoke-only** روی همان کانال projection (`applyLocalProjection('ptf_crm_offers', nextArr)`: فقط `delete invRef` + مهرهای `invRefRevokedAt/By/Reason`) و **رندر فوری** — ردیف و هر دو کنتور «📤 ارجاع‌شده / ✅ ثبت‌شده» همان لحظه کم می‌شوند |
| در ACK | آرایۀ سرور از `d.data` به‌عنوان مرجع اعمال می‌شود (این‌بار **بدون rev**، پس گاردِ krevs دیگر نمی‌تواند projectionِ قطعیِ همین فرمان را بیندازد) و پرچم نشست پاک می‌شود |
| در رد (409 «فاکتور ثبت شده») | آرایۀ قبلی کامل بازگردانده می‌شود + رندر + همان alert تشریحی — هیچ «نیمه‌ثبت‌شدگی» روی صفحه نمی‌ماند |
| در نتیجهٔ نامشخص | بازگردانی نمایش + هشدار صادقانه؛ حقیقت را `ptf_crm_sales_commands` و همگام‌سازی بعدی قطعی می‌کند |

به‌طور مکمل، `renderInvoices` ردیف‌هایی که لغو ارجاعشان **در راه است** را با پرچم
نشست‌محور `window._ptfInvRefPendingRevoke` پنهان می‌کند، تا اگر نوشتن محلی به هر دلیلی
(کووتا/فاز B) نخورد، ردیفِ شبح‌مانده دیده نشود. پرچم در هر سه مسیر پاک می‌شود و هیچ‌جا
در `localStorage` ذخیره نمی‌گردد.

**هیچ نوشتن جدیدی به سرور افزوده نشد:** مسیر محلی `setData`/روتر/`ptfSilentWrite` را صدا
نمی‌زند؛ سرور خودش commit کرده و snapshot قبلی را در `ptf_crm_corrections` نگه داشته است.
گاردهای v34.37.0 هم دست‌نخورده‌اند: دلیل اجباری، تأیید صریح پیش از ثبت، و fail-closed بودن
روی «هر فاکتور فعال (رسمی یا غیررسمی)».

## ② تراز ستون‌های پنل (INV-PANEL-COLS)

علت بهم‌ریختگی: عرض ستون‌ها **دو بار دستی** نوشته شده بود (یک‌بار در سربرگ، یک‌بار در
ردیف) و ردیف `flex-wrap:wrap` داشت — پس نام بلندِ مشتری، مبلغ/مطالبه باز/اقدام را به خط
بعد می‌راند و ترازِ ستون‌ها بین دو ردیف تضمینی نداشت. اکنون:

- یک قالب ستونی مشترک `INV_COLS` (هفت ستون: فلش، سند مبنا، مشتری، وضعیت فاکتور، مبلغ،
  مطالبه باز، اقدام) که **هم سربرگ و هم ردیف** از آن می‌خوانند (`invCol(id)` /
  `invHeaderHtml()`) — هیچ عرض دستیِ تکراری در فایل نمانده؛
- ستون مشتری انعطاف‌پذیر است و خودش را با `text-overflow:ellipsis` کوتاه می‌کند (با `title`
  کامل برای hover) به‌جای این‌که بقیۀ ستون‌ها را جابه‌جا کند؛
- `flex-wrap` ردیف حذف شد؛ کشوی جزئیات همان‌جا و بی‌تغییر است.

---

## ③ تست‌ها

| تست | Coverage |
|---|---|
| `tester600-v34.37.3-invoice-panel-undo-immediate.js` | اجرای واقعی برشِ پنل در vm: ۳۵ سنجه — تراز سربرگ/ردیف روی `innerHTML` (۷ به ۷، همان عرض‌ها)، حذف فوری ردیف + کنتورها با پرچم pending، نشت‌نکردن به بخش یتیم‌ها، نشست‌محوربودن پرچم، قرارداد `ptfRevokeInvoiceRef` (اعمال محلی پیش از فرمان، revoke-only، مسیر projection، بازگردانی در رد/نامشخص، پاک‌شدن پرچم در هر سه مسیر، حفظ گاردهای v34.37.0) + **آزمون جهش** (با حذف فیلتر یا خراب‌کردن قالب، تست باید بشکند) |
| `tester596` | پنجرۀ متنیِ بند ۷.۵ باز شد (بلوک «اعمال محلیِ فوری» میان سرِ تابع و alert اضافه شده)؛ سایر سنجه‌ها بدون تغییر |
| `tester599` | همان سنجه‌های ۳۷.۲ روی پنل جدید سبز ماند (بخش یتیم‌ها، تطبیق نرمال‌شده، سربرگ تشخیصی) |

**زیرساخت نسخه:** بامپ `v34.37.3` در `VERSION.json`، `crm/index.html` (۱۱۱ بامپ `?v=`)،
`crm/sw.js` (نام کش)، `crm/manifest.json`، `crm/clear-cache.html`، `crm/shell.js`،
`crm/cms.js` و `api/sales-domain.php` (`SD_SERVICE_VERSION`).

## ④ استقرار و نکات بازبینی

- تغییر فقط در دو فایل UI/کلاینت است: `crm/official-invoice-v2.js`، `crm/sales-domain-v2.js`. **سرور، مدل داده و مجوزها دست‌نخورده‌اند.**
- اگر پیش از این نسخه، «لغو ارجاع» زده شده و ردیف روی صفحه مانده بود: یک بار «↻ بازخوانی از سرور» در همان سربرگ کافی است (یا رفرش صفحه). داده روی سرور از همان اول درست ثبت شده بود.
- **مستقل و باز:** دو سند ریشۀ دیگری همچنان بی‌پاسخ‌اند و این انتشار آن‌ها را پوشش نمی‌دهد — `ARENA-CRM-CUSTOMER-VANISH-RCA-2026-09-05.md` (حذف اشتباه رکورد تازه توسط روتر دیف‌محور) و `ARENA-CRM-OFFERS-RFQ-EMPTY-RCA-2026-09-05.md` (تلهٔ آب‌رسانی آینۀ IDB + فیلتر مشتریِ مُرده). تا پیش از پچِ آن دو، توصیهٔ «push نکردن از دستگاه‌های خالی‌دیده و گرفتن snapshot از `data/sync`» همچنان برجا است.
