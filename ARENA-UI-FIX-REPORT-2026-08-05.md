# 🔧 گزارش بازسازی رابط کاربری CRM — ۱۴ مرداد ۱۴۰۵

## 📋 خلاصه مشکل

تمام کلمات فارسی در دکمه‌های منوی پایین صفحه (Bottom Tab Bar) و دکمه‌های داخلی ماژول‌ها **حرف‌به‌حرف شکسته** نمایش داده می‌شدند. به جای «داشبورد» متن به صورت «د ا ش ب و ر د» نمایش داده می‌شد.

---

## 🔍 تشخیص ریشه‌ای (Root Cause Analysis)

### مشکل اصلی: قوانین CSS نادرست برای متن فارسی

سه قانون CSS در چندین نقطه اعمال شده بودند که برای زبان فارسی ویرانگر هستند:

| قانون CSS | تأثیر روی فارسی |
|-----------|----------------|
| `overflow-wrap: anywhere` | به مرورگر اجازه می‌دهد کلمه را در **هر نقطه‌ای** بشکند — حتی وسط یک کلمه فارسی |
| `word-break: break-word` | (Deprecated) مشابه بالا — شکستن کلمه در هر نقطه |
| `white-space: normal` | در ترکیب با بالا، اجازه wrap شدن در وسط کلمه |

### محل‌های آلوده:
1. **بلاک Sprint 108** (خطوط ۴۲-۶۵): قوانین موبایل دکمه‌ها
2. **بلاک تکراری Sprint 108** (خطوط ۳۳۰-۴۲۹): ۱۰۰ خط CSS تکراری!
3. **استایل پایه `.bt`** (دکمه اصلی): `overflow-wrap:anywhere`
4. **استایل پایه `.ba`** (دکمه عملیاتی): `overflow-wrap:anywhere`
5. **`.sc b`** (اعداد کارت آماری): `overflow-wrap:anywhere`
6. **`mobilenav.js`**: `overflow-wrap:anywhere` در trialBarWrap
7. **`theme-contrast.js`**: `overflow-wrap:anywhere` در KPI

---

## ✅ اصلاحات انجام‌شده

### ۱. اصلاح قوانین CSS شکننده‌ی فارسی (`crm/index.html`)

**قبل:**
```css
.bt, .bt-o, .btn, .btn-l, .ba, button {
    white-space: normal !important;
    overflow-wrap: anywhere !important;
    word-break: break-word !important;
}
```

**بعد:**
```css
.bt, .bt-o, .btn, .btn-l, .ba, button {
    white-space: nowrap !important;
    overflow: hidden !important;
    text-overflow: ellipsis !important;
}
/* دکمه‌های داخل مودال (فضای کافی دارند): */
.md .bt, .md .bt-o, ... {
    white-space: normal !important;
    overflow: visible !important;
}
```

### ۲. حذف بلاک تکراری Sprint 108
- **۱۰۰ خط CSS تکراری** حذف شد (خطوط ۳۳۰-۴۲۹)
- این بلاک دقیقاً کپی خطوط ۲۵-۱۳۰ بود و قوانین مخرب را دوبار اعمال می‌کرد

### ۳. کوتاه‌سازی برچسب دکمه‌ها (۱۵ فایل)

#### سایدبار (crm/index.html):
| قبل | بعد |
|-----|------|
| درخواست تامین | تامین |
| موجودی انبار | انبار |
| چاپ چک فیزیکی | چاپ چک |
| پرونده‌های فروش | پرونده‌ها |
| تحلیلگر هوشمند | تحلیلگر |
| سامانه پیامکی | پیامک |
| مدیریت سایت | سایت |

#### دکمه‌های ماژول‌ها:
| فایل | قبل | بعد |
|------|------|------|
| rfqsmart.js | + درخواست تامین جدید | + تامین جدید |
| bridge.js | + ثبت درخواست جدید | + جدید |
| bridge.js | 🧹 پیش‌نمایش و پاکسازی امن | 🧹 پاکسازی |
| buycompare.js | + استعلام قیمت جدید (انتخاب درخواست) | + استعلام جدید |
| buycompare.js | 🤖 استعلام جدید از تامین‌کننده | 🤖 استعلام |
| buycompare.js | + قیمت دور ۲ (پس از برد) | + قیمت دور ۲ |
| buycompare.js | 🛍 ثبت / مشاهده / اصلاح خرید | 🛍 خرید |
| ai-workbench.js | 📎 انتخاب کارت ویزیت | 📎 کارت ویزیت |
| ai-workbench.js | ✅ ثبت در فهرست انتخاب‌شده | ✅ ثبت |
| ai-workbench.js | 🤖 تولید پیش‌نویس رسمی | 🤖 پیش‌نویس |
| ai-workbench.js | ✅ تایید و ذخیره در مکاتبات | ✅ ذخیره |
| ai-workbench.js | 🚀 استخراج واقعی و جاگذاری خودکار اقلام | 🚀 استخراج اقلام |
| archive.js | تایید نهایی: حذف انتخابی‌ها + فشرده‌سازی بقیه | تایید و اجرا |
| backup.js | ✅ فعال‌سازی حالت سرور-محور | ✅ فعال‌سازی |
| cheque-panel.js | 📥 چک وارده — دریافت از مشتری / ثالث | 📥 چک وارده |
| cheque-panel.js | 🏢 چک صادره — پرداخت / ضمانت شرکت | 🏢 چک صادره |
| index.html | ✅ متوجه شدم، انتخاب و بارگذاری فایل اکسل | ✅ بارگذاری اکسل |
| *.js | ورود اکسل با راهنما | ورود اکسل |

---

## 📂 فایل‌های تغییریافته (۱۵ فایل)

1. `crm/index.html` — اصلاح CSS + حذف بلاک تکراری + کوتاه‌سازی سایدبار
2. `crm/mobilenav.js` — اصلاح overflow-wrap
3. `crm/theme-contrast.js` — اصلاح overflow-wrap
4. `crm/ai-workbench.js` — کوتاه‌سازی ۵ دکمه
5. `crm/archive.js` — کوتاه‌سازی دکمه تایید
6. `crm/backup.js` — کوتاه‌سازی دکمه فعال‌سازی
7. `crm/bridge.js` — کوتاه‌سازی ۲ دکمه
8. `crm/buycompare.js` — کوتاه‌سازی ۴ دکمه
9. `crm/cheque-panel.js` — کوتاه‌سازی ۲ دکمه
10. `crm/cheque-print.js` — کوتاه‌سازی دکمه چاپ
11. `crm/inqreader.js` — کوتاه‌سازی دکمه ورود اکسل
12. `crm/leads.js` — کوتاه‌سازی دکمه ورود اکسل
13. `crm/offers.js` — کوتاه‌سازی دکمه ورود اکسل
14. `crm/rfqsmart.js` — کوتاه‌سازی ۲ دکمه
15. `crm/salesfiles.js` — کوتاه‌سازی دکمه خرید

---

## ⚡ آمار تغییرات
- **۴۷ خط اضافه شد / ۱۴۷ خط حذف شد** (خالص: ۱۰۰ خط کاهش)
- **صفر** اینستنس `overflow-wrap: anywhere` باقی‌مانده
- **صفر** اینستنس `word-break: break-word` در CSS بحرانی

## 🛡️ ملاحظات امنیتی
- هیچ منطق کسب‌وکار (business logic) تغییر نکرده
- هیچ اندپوینت API تغییر نکرده
- فقط متن نمایشی دکمه‌ها و قوانین CSS اصلاح شدند
- عملکرد `onclick` تمام دکمه‌ها دست‌نخورده است
