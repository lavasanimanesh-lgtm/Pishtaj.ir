# RELEASE NOTES — v34.7.68 (۱۴۰۵/۰۵/۳۰ — 2026-08-21)

## چت هوشمند — فاز ۴ (خیلی پیشرفته)

تکمیل تجربهٔ چت با کارت‌های غنی، استریم پاسخ، هنداف انسانی و ثبت تحلیلی.

### ۱) کارت‌های غنی (Rich Cards)

- **کارت مقاله:** پاسخ فنی حالا همراه با کارت‌های «مقالهٔ مرتبط» (عنوان + خلاصه + لینک به
  مرکز دانش) نمایش داده می‌شود — از RAG (`search-index.json`).
- **کارت پیشرفت سفارش:** وقتی کاربر وضعیت سفارش را می‌پرسد، مراحل (ابلاغ ← حمل ← فاکتور ←
  تحویل و تسویه) به‌صورت **نوار پیشرفت بصری** با وضعیت انجام‌شده/جاری/آینده نمایش داده می‌شود.
- **CTA:** دکمهٔ «ثبت استعلام» روی پاسخ‌های LLM.

### ۲) استریم پاسخ (Streaming)

- پاسخ مدل حالا **توکن‌به‌توکن** نمایش داده می‌شود (SSE) — احساس پاسخ آنی.
- پشتیبانی Gemini (`streamGenerateContent`) و OpenAI (`stream:true`).
- اگر استریم در دسترس نباشد (هاست/مرورگر)، کلاینت خودکار به مسیر غیراستریم برمی‌گردد —
  تجربه هرگز نمی‌شکند.

### ۳) هنداف انسانی (واتس‌اپ / تلگرام)

- بعد از «ارسال گفتگو به کارشناس»، دکمه‌های **💬 واتس‌اپ**، **✈️ تلگرام** و **📞 تماس**
  نمایش داده می‌شوند تا کاربر مستقیم در ارتباط باشد.

### ۴) ثبت تحلیلی (Privacy-first)

- هر پرسش با «نیت» (status / kb / chat)، زبان و متن کوتاه (حداکثر ۱۲۰ حرف) در
  `ptf_chat_analytics.json` ثبت می‌شود (حداکثر ۲۰۰۰ ردیف) — برای کشف پرسش‌های پرتکرار و
  بهبود دانش‌نامه. بدون دادهٔ شخصی (IP/نام ذخیره نمی‌شود).

### حفظ محدودیت‌ها

- سهمیهٔ توکن مجزا (`ptf_chat_public_quota.json`) — **سهمیهٔ CRM دست‌نخورده**.
- وضعیت درخواست/سفارش همچنان **بدون توکن** (از `track`) و بدون نشت محرمانگی.

### فایل‌ها

`api/chat-llm.php` (استریم + کارت + تحلیل) · `assets/js/ptf-chat.js` (کارت + استریم + هنداف).

### تست

- تستر جدید `tester470-v34.7.68-smart-chat-phase4.js` (در گیت CI).
- **گیت CI: ۸۶ PASS / ۰ FAIL** · نگهبان معماری: PASS.

### UAT پیشنهادی

1. سؤال فنی بپرسید (مثلاً «تفاوت لوله A106 و API 5L») → پاسخ استریم‌شده + کارت مقاله.
2. وضعیت سفارش بپرسید → نوار پیشرفت بصری.
3. «ارسال به کارشناس» → دکمه‌های واتس‌اپ/تلگرام/تماس.
