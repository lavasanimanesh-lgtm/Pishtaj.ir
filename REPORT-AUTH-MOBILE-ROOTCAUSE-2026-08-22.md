# گزارش ریشه‌یابی باگ: ورود حسابدار از موبایل («کاربر وجود ندارد» و «توکن معتبر وجود ندارد»)

**تاریخ:** 2026-08-22 (۱۴۰۵/۰۵/۳۱)
**نوع گزارش:** تحلیل/ریشه‌یابی — **بدون تغییر کد** (صرفاً مستند)
**برنچ کار:** `arena/01a02872-pishtaj-ir` (در نسخه `v34.7.83`)
**هدف:** بررسی عمیق مشکل کاربر خاص (حسابدار) و تعمیم آن به مسیر ورود/نشست **همه کاربران**، و ارائه راهکار رفع دایمی.

---

## ۱. خلاصه تصمیم

این مشکل **دو باگ از خود نرمافزار** است، نه صرفاً مشکل شبکه کاربر:

| سناریو | پیام کاربر | باگ واقعی |
|---|---|---|
| ۱) اولین ورود حسابدار از موبایل | «کاربر وجود ندارد — همگام‌سازی کاربران را اجرا کنید» | کاربر فقط در `localStorage` مرورگر ادمین ساخته شده بود و هنوز روی سرور نرفته بود. |
| ۲) بعد از خروج/ورود مجدد | «توکن معتبر وجود ندارد؛ با رمز واقعی وارد شوید» | مسیر «ورود فقط-محلی» هنوز فعال است؛ با شکست سرور (شبکه/تلاش/رمز لحظه‌ای) مرورگر فقط **session بدون توکن** می‌سازد و بلافاصله `showCrm()` همان پیام را تولید می‌کند. |

پیام «توکن معتبر وجود ندارد» **بیشتر به‌خاطر نبودِ توکن در مسیر local-only است، نه ابطال توکن روی سرور.**

---

## ۲. نقشه داده و مسیر ورود

- ورود همه نقش‌ها از `doLogin()` در `crm/index.html` انجام می‌شود (نه فقط موبایل؛ موبایل همان SPA/PWA است).
- سرور اکشن `auth_login` رمز را با `password_hash` (bcrypt) یا `passhash` (SHA-256 قدیمی) مقایسه می‌کند و در صورت موفقیت یک **JWT-like token** تولید و در `crm/data/tokens.json` ذخیره می‌کند (`api/auth.php → auth_generate_token`).
- به‌جز `admin`، تمام کاربران از **منبع سروری** خوانده می‌شوند: `load_all_crm_users_sources()` = `users.json` + `crm_users.json` + `crm/data/sync/ptf_crm_users.json`.
- کلاینت هنگام باز شدن CRM با `showCrm()` چک می‌کند:
  - `session` داشته باشد (`ptf_crm_session`)
  - **توکن داشته باشد** (`ptf_crm_token`)
  - در غیر این صورت `ptfInvalidateSession(...)` با پیام دقیقِ گزارش‌شده اجرا می‌شود.

---

## ۳. ریشه‌یابی سناریو ۱: «کاربر وجود ندارد»

### شواهد
- `crm/rbac.js → saveUser2`: ابتدا `setData('ptf_crm_users', ...)` (فقط localStorage این مرورگر) انجام می‌شود.
- سپس دو عملیات سروری انجام می‌شود: `add_user` و سپس `usersSyncToServer()`.
- اگر `add_user`/`usersSyncToServer` به دلیل **توکن، نقش، یا شبکه** شکست بخورد، به کاربر پیام:
  > «کاربر در این مرورگر تعریف شد اما همگام‌سازی با سرور ناموفق بود… فقط از همین مرورگر قابل ورود است»
  ثبت می‌شود (`crm/rbac.js` سطر ~۴۶۷).
- اما در **UI پنل کاربران**، رکورد کاربر اضافه شده است و مدیر در همان دستگاه آن را «موجود» می‌بیند.
- از دستگاه دیگر/موبایل، `auth_login` و `users_get` سرور کاربر را پیدا نمی‌کنند → پیام «کاربری با نام … روی سرور پیدا نشد» (`crm/index.html` سطر ~۲۰۷۳).

### نتیجه‌گیری
ریشه این مورد **همگام‌سازی اولیه ناکامل/بی‌صدا** است: کاربر فقط محلی ساخته شده و به سرور نرفته. راه‌حل افقی فعلی («همگام‌سازی کاربران» از ادمین) همین داده را push می‌کند و به همین دلیل در آن لحظه جواب داده است. مشکل در **نبود شفافیت/تأییدِ موفقیتِ سروری هنگام ساخت کاربر** است.

---

## ۴. ریشه‌یابی سناریو ۲: پیام «توکن معتبر وجود ندارد؛ با رمز واقعی وارد شوید»

### شواهد (قطعی)
در `crm/index.html`، توالی زیر موجود است:

```js
// 1) ابتدا تلاش سروری
var _login = await ptfServerLogin(u, p);
if (_login && _login.ok && _login.token) {
  // ... session + token + showCrm()
  return;
}

// 2) اگر سرور پاسخ موفق نداد → مسیر فقط-محلی (هنوز فعال است برای همه غیر-admin)
var users = JSON.parse(localStorage.getItem('ptf_crm_users') || '[]');
for (...) {
  if (users[i].username === u && users[i].passhash && users[i].passhash === ph) {
    localStorage.setItem('ptf_crm_session', JSON.stringify({...}));   // ← فقط session
    showCrm();                                                         // ← NO token
    return;
  }
}
```

و داخل `showCrm()`:

```js
if (!s.user || !localStorage.getItem('ptf_crm_token')) {
  ptfInvalidateSession('نشست سرور یا توکن معتبر وجود ندارد؛ لطفاً با رمز واقعی دوباره وارد شوید.');
  return;
}
```

### این یعنی چه
اگر `_login` به هر دلیلی `ok/token` برنگرداند (خطای لحظه‌ای شبکه، `429`، `invalid_credentials` موقت، یا هر ۵۰۰/HTML از سرور)، و **همان مرورگر یک رکورد کاربری با `passhash` محلی داشته باشد** (که فی‌الواقع بعد از اولین موفقیت سروری نوشته می‌شود)، آنگاه:

1. مرورگر یک session می‌سازد **بدون** `ptf_crm_token`.
2. بلافاصله `showCrm()` می‌گوید «توکن معتبر وجود ندارد…».
3. کاربر پیام گمراه‌کننده می‌گیرد و حس می‌کند سرور/توکن مشکل دارد.

این دقیقاً همان وضعیت «بعد از ورود موفق قبلی، خروج، ورود مجدد» است. چرا؟
- بعد از اولین ورود موفق، کد در `crm/index.html` می‌نویسد: `localStorage['ptf_crm_users']` شامل `passhash=sha256(password)` برای همان کاربر.
- در ورود بعدی اگر `auth_login` موقتاً با شبکه/سرور مشکل بخورد، همان لحظه مسیر local-only فعال می‌شود.
- چون `session` بدون `token` است، `showCrm()` هیچ ورودی واقعی را رد نمی‌کند و همان پیام خطا را می‌دهد.

### چرا «همگام‌سازی کاربران» حل نمی‌کند
همگام‌سازی کاربران فقط **مسئله وجود کاربر روی سرور** را حل می‌کند؛ **مسئله local-only fallback** را حل نمی‌کند. به همین دلیل پس از خروج/ورود مجدد، همگام‌سازی هم کمکی نمی‌کند و مشکل تکرار می‌شود.

---

## ۵. عوامل تشدیدکننده (ثانویه)

1. **`ptfSafeReset()` (`رفع مشکل ورود`)** فقط `ptf_crm_session` و `ptf_login_lock` را پاک می‌کند؛ **`ptf_crm_token` را پاک نمی‌کند**.
   - نتیجه: توکن کهنه/نامعتبر در localStorage می‌ماند و ممکن است با توکن جدید ورود تداخل/گیجی بدهد.

2. **`idleCheck()` در `crm/inqreader.js`** پس از ۸ ساعت بیکاری فقط `ptf_crm_session` را حذف می‌کند؛ **توکن را حذف نمی‌کند**.
   - همین منجر به حالت ناهماهنگ «session نیست ولی token هست» می‌شود و ماژول‌هایی که فقط توکن را چک می‌کنند ممکن است رفتار عجیب نشان دهند.

3. **توکن ۷ روزه است و در `crm/data/tokens.json` نگهداری می‌شود** (`api/auth.php`).
   - اگر فایل `tokens.json` بازسازی/پاک شود یا server برای همان کاربر چند بار توکن بسازد، توکن قدیمی نامعتبر می‌شود.
   - `sync.js → refreshAuthToken` وقتی 401 می‌گیرد توکن را پاک و کاربر را به `index.html?reauth` می‌فرستد؛ اما در مسیر local-only این اتفاق نمی‌افتد چون اصلاً توکن نبوده.

4. **ساختار «دو منبع شبه‌موازی» کاربران**:
   - `ptf_crm_users` (localStorage) برای UI خود چند دستگاه.
   - `crm_users.json` / `users.json` / `sync/ptf_crm_users.json` برای سرور.
   - `users_get` عمداً `passhash` نمی‌دهد (نقطه امنیتی درست)، اما در merge های متعدد می‌تواند رکوردهای محلی بی‌هش به‌جای رکورد هش‌دار بنشیند و سپس users_sync دوباره آن را با هش سرور حفظ کند. در برخی مسیرها خطای کاربر «بی‌صدا» می‌شود.

5. **اعتماد به `users_get` برای پیش‌بررسی کاربر**:
   - در `doLogin` وقتی `_login` شکست می‌خورد، `users_get` عمومی را می‌زند. این اکشن بدون توکن هم `users` برمی‌گرداند و ممکن است `stale`/فیلتر-نقش باشد. هرچند برای `direct auth_login` fallback دارد، اما همین پیچیدگی منشأ پیام‌های متضاد است.

---

## ۶. نمودار جریان شکست (سناریوی تکراری)

```
[ورود مجدد با رمز درست]
        │
        ▼
ptfServerLogin(...) ── می‌تواند null/{ok:false} باشد
        │                       │
        │ ok+token              │ fail (network / 429 / server 500 / invalid_credentials)
        ▼                       ▼
 session+token+showCrm()   مسیر local-only (STILL ACTIVE for non-admin)
        │                       │
        │                       ▼
   موفق                     رکورد محلی passhash را دارد؟
                                  │ yes
                                  ▼
                        session ONLY (بدون token)
                                  │
                                  ▼
                        showCrm() → !token? ✅
                                  ▼
        پیام «توکن معتبر وجود ندارد؛ با رمز واقعی وارد شوید»
```

---

## ۷. راهکار رفع دایمی (پیشنهادی — بدون اجرا)

> این بخش فقط پیشنهاد است؛ طبق درخواست، هنوز کد تغییر نکرده است.

### A. از بین بردن مسیر local-only بدون توکن (اصلی‌ترین اصلاح)
- رفتار درست: غیر از `admin`، **هر ورود موفق باید از `auth_login` سرور توکن بگیرد**.
- مسیر `localUsers` فقط باید برای **آفلاینِ state-بعد از نشست** (وقتی session+token از قبل معتبرند) باشد، نه ورود جدید.
- اگر `_login` موفق نشود → **هرگز session بدون `token` نسازیم**؛ پیام دقیق (network / 429 / invalid_credentials / token-needed) نشان داده شود.
- در خطای `token`/نشست، از `ptfInvalidateSession` و `ptfSyncRefreshAuth` (که توکن را پاک و reauth می‌کند) استفاده شود، نه از fallback محلی.

### B. حذف/شفاف کردن ورود «فقط-سروی» و «توکن فقط از سرور»
- تابع `showCrm()` فعلاً token را الزامی می‌کند؛ بنابراین هر مسیری که session بدون token بسازد **باگ است**.
- نقاط ساخت session بدون token در `crm/index.html` باید شناسایی و حذف شوند (سطرهای ۱۹۷۲ و ۱۹۸۱).
- هیچ نقش غیر-ادمینی نباید با SHA-256 محلی بدون توکن وارد شود؛ چون هیچ اکشن سروری (data_pull/data_push/sync) با توکن ناموجود کار نمی‌کند.

### C. پاک‌سازی توکن هنگام logout / idle / safe-reset
- `doLogout()`: مطمئن شویم `ptf_crm_token`, `ptf_crm_token_role`, `ptf_crm_session` هر سه حذف شوند (فعلاً در `doLogout` حذف می‌شوند، ولی در `idleCheck` و `ptfSafeReset` نه).
- `idleCheck()` بعد از انقضای ۸ ساعت، **توکن را نیز حذف کند** (یا حداقل مطمئن شود ماژول‌ها با توکن بیکار ادامه ندهند).
- `ptfSafeReset()` علاوه بر session، `ptf_crm_token` و `ptf_crm_token_role` را هم پاک کند (به‌ویژه چون عنوانش «رفع مشکل ورود» است).

### D. می‌بایستی ساختکاربر (رفع سناریو ۱)
- هنگام ساخت کاربر، **نسخه سروری** باید معیار موفقیت باشد؛ پیام «تعریف شد» فقط پس از `users_sync`/`add_user` موفق نشان داده شود (در v34.7.73 این کار شده، اما هنوز sync ممکن است «بی‌صدا» شکست بخورد).
- در UI «کاربران» شاخص «روی سرور هست؟» اضافه شود و دکمه همگام‌سازی با خطا/موفقیت صریح (تاکنون به‌صورت `ptfUsersSyncManual` انجام شده).
- اگر `users_sync` کاربر دارای `passhash` مرجع صفر باشد، باید **دوباره تعریف رمز** خواسته شود و کاربر از فهرست سروری حذف نشود (قانون حفظ `password_hash` موجود، همین است).

### E. باثبات‌سازی `auth_login`
- یکپارچه‌سازی `users_get`/`auth_login` از `load_all_crm_users_sources` برای همه کاربران (نشانه‌ی درست).
- استانداردسازی «نسخه رمز» (حداقل ۶ کاراکتر، normalize username)، جلوگیری از `invalid_credentials` ناشی از نقش/space.
- فقط با `POST`, به‌جای GET — طبق `api/crm.php`. مطمئن شویم `ptfServerLogin` همیشه `application/x-www-form-urlencoded` ارسال می‌کند (فعلاً درست است).

### F. پیام‌های دقیق به‌جای پیام عمومی
- اگر `auth_login` شکست: `network`, `invalid_credentials`, `too_many_attempts`, `auth_not_configured`.
- اگر session بدون token: پیام واضح «برای ورود با سرور/توکن دوباره وارد شوید؛ حالت فقط-محلی برای این نقش مجاز نیست».
- اگر `users_sync` ناقص: «کاربر فقط روی این مرورگر تعریف شده؛ برای موبایل/دستگاه دیگر نیاز به همگام‌سازی موفق است».

---

## ۸. رفع امنیتی/مقابله با پیام پنهان

- در `verifyRoleFromServer` پس از ورود، نقش سرور با نقش محلی تطبیق می‌شود؛ اگر نقش متفاوت باشد session به‌روز می‌شود. این مسیر مناسبی است، اما فقط **بعد از داشتن token** انجام می‌شود؛ پس در مسیر local-only اصلاً اجرا نمی‌شود. با حذف مسیر local-only این مشکل هم بسته می‌شود.

---

## ۹. چک‌لیست تأیید قبل از اعمال هر تغییر

1. ورود accountant از دسکتاپ: موفق و `ptf_crm_token` ست می‌شود.
2. خروج کامل: هر دو کلید `session` و `token` حذف می‌شوند.
3. ورود مجدد accountant: `auth_login` موفق؛ پیام «توکن معتبر وجود ندارد» **هرگز** نمایش داده نشود (حتی اگر سرور موقتاً قطع شود، پیام `network` داده شود نه «توکن معتبر ندارد»).
4. سناریوی شبیه‌سازی `_login = null` (قطع fetch): بدون ایجاد false session; فقط پیام شبکه.
5. سناریوی `_login = {ok:false,error:'too_many_attempts'}`: پیام «تعداد تلاش» و نه fallback محلی.
6. سناریوی ۸ ساعت idle: session+token هر دو از بین بروند.
7. سناریوی safe-reset: session/token همه پاک شوند.
8. `users_sync` اولیه: اگر کاربر فقط محلی باشد و sync ناقص باشد، کاربر در UI سرور واضح علامت «نرسیده» بگیرد.
9. گیت CI سبز بماند (تست‌های `tester484`/existing auth tests).
10. تست واقعی از موبایل (یا PWA) بعد از استقرار: ثبت یک رکورد، خروج، ورود مجدد، و تأیید اینکه سینک سبز است.

---

## ۱۰. جمع‌بندی

| موضوع | وضعیت |
|---|---|
| «کاربر وجود ندارد» | باگ **همگام‌سازی اولیه** (کاربر فقط local) — راه‌حل: ساخت/سینک سروری قطعی و نشانگر «روی سرور هست». |
| «توکن معتبر وجود ندارد» | باگ **fallback فقط-محلی** — مسیر login برای کاربر عادی session بدون token می‌سازد و `showCrm` پیام گمراه‌کننده تولید می‌کند. |
| چرا تکرار می‌شود | هر بار که `auth_login` لحظه‌ای ناموفق باشد (مخصوصاً موبایل/شبکه) و `passhash` محلی موجود باشد، همین مسیر activated می‌شود. |
| آیا همگام‌سازی کاربران حل می‌کند؟ | تا زمانی که مشکل ۲ باشد **خیر**؛ همگام‌سازی فقط مشکل ۱ را حل می‌کند. |
| پیام نهایی | برای رفع دایمی: حذف ورود local-only بدون توکن + پاک‌سازی توکن در logout/idle/reset + صراحت نشان دادن status سروری ساخت کاربر. |

---

## ۱۱. پیوست: فایل/سطرهای کلیدی

- `crm/index.html`:
  - `ptfServerLogin` و `ptfInvalidateSession` (~۱۹۰۱–۱۹۰۸)
  - `doLogin`، مسیر فقط-محلی بدون توکن (~۱۹۶۳–۱۹۸۵)
  - `showCrm()` و شرط `!token` + پیام (~۲۰۹۲–۲۰۹۴)
  - `doLogout()` (~۲۱۳۳–۲۱۳۸)
  - `auto-login check` (~۲۱۵۰–۲۱۵۴)
- `crm/inqreader.js`:
  - `idleCheck` فقط حذف session (~۱۰–۲۲)
- `crm/rbac.js`:
  - `saveUser2` / `afterSync` (~۴۴۲–۴۸۳)
  - `usersSyncToServer` (~۵۱۹–۵۴۵)
  - `ptfUsersSyncManual` (~۵۵۰–۵۷۰)
  - `usersPullFromServer` (~۵۸۴–۶۲۹)
- `api/crm.php`:
  - `public_actions` و `verify_request()` (~۲۷–۶۰)
  - `users_sync` (~۱۷۰۸–۱۷۶۰)
  - `auth_login` (~۱۷۶۲–۱۸۶۰)
  - `users_get` (~۱۸۶۱–۱۸۹۰)
- `api/auth.php`:
  - `auth_generate_token` / `auth_verify_token` (~۴۸–۹۵)
