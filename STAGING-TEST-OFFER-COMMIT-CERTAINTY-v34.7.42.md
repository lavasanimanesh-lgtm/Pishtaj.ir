# تست استیجینگ v34.7.42 — قطعیت نتیجه ثبت پیشنهاد

> فقط وقتی هدر CRM نسخه `v34.7.42` را نشان می‌دهد اجرا شود. تا رفع مانع مجوز Workflow، استیجینگ روی v34.7.39 باقی می‌ماند.

## ۱) سناریوی اصلی گزارش‌شده

1. با یک کاربر فروش، درخواست دارای اقلام را باز کنید.
2. یک پیشنهاد مالی جدید ثبت کنید.
3. انتظار:
   - دکمه هنگام انتظار غیرفعال باشد؛
   - فقط پیام موفقیت «پیشنهاد و وضعیت درخواست در یک تراکنش سرور تأیید شد» دیده شود؛
   - پیام «سرور ثبت پیشنهاد را نپذیرفت» ظاهر نشود؛
   - پیشنهاد یک‌بار در فهرست باشد؛
   - RFQ همان لحظه وضعیت صحیح پیشنهاد مالی را نشان دهد.

این سناریو را حداقل سه بار روی موبایل و دسکتاپ و برای یک TO نیز تکرار کنید.

## ۲) پاسخ گم‌شده / شبکه ناپایدار

با DevTools یا شبکهٔ ناپایدار، پاسخ درخواست اول `register_offer` را قطع کنید ولی اجازه دهید request به سرور برسد.

انتظار:
- کلاینت همان operation ID را خودکار replay کند؛
- پیشنهاد تکراری ساخته نشود؛
- در صورت بازیابی پاسخ، پیام موفقیت با عبارت «بازیابی پاسخ» دیده شود؛
- هیچ پیام رد کاذبی نمایش داده نشود.

## ۳) دو شکست transport

اگر هر دو تلاش و pull شکست خوردند:

- پیام باید با «پاسخ قطعی ثبت از سرور دریافت نشد» شروع شود، نه «سرور نپذیرفت»؛
- RFQ محلی نباید به وضعیت صادرشده جلو برود؛
- draft باقی بماند؛
- پس از برگشت اینترنت، کلیک دوباره همان operation را reconcile کند.

## ۴) رد واقعی

یک conflict واقعی مانند شمارهٔ متعلق به رکورد دیگر را ایجاد کنید.

انتظار:
- فقط یک درخواست ثبت انجام شود و retry کور صورت نگیرد؛
- پیام «سرور ثبت پیشنهاد را نپذیرفت» همراه دلیل واقعی بیاید؛
- offer جدید و WF50 محلی rollback شوند؛
- رکورد قبلی overwrite نشود.

## ۵) خطای post-ACK

در محیط تست، یکی از render/side effectهای بعد از ACK را عمداً throw کنید.

انتظار:
- offer و RFQ قطعی سرور باقی بمانند؛
- پیام رد نمایش داده نشود؛
- warning غیرمسدودکننده ظاهر شود؛
- diagnostic با کلید `ptf_offer_post_ack_warning_<offerNo>` در localStorage موجود باشد.

## شواهد ثبت تست

برای هر سناریو شماره offer، شماره RFQ، operation ID، تعداد رکوردهای هم‌شماره و متن پیام UI را ثبت کنید. نتیجه فقط وقتی قبول است که offer count برابر ۱ و وضعیت RFQ با projection سرور هم‌خوان باشد.
