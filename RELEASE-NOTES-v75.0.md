# 🚀 ریلیز v75.0 — اسپرینت ۷۵: مدیریت سایت از داخل CRM (US-130 Mini-CMS)
**تاریخ:** تیر ۱۴۰۵ | ماژول جدید: «🎛 مدیریت سایت» در دسته سیستم — **فقط ادمین + رییس هیات مدیره**

از این نسخه برای به‌روزرسانی محتوای سایت دیگر نیازی به FTP و ویرایش دستی فایل نیست.

---

## 📰 ۱) مدیریت اخبار (AC1)
- فهرست اخبار فعلی سایت مستقیم از `news/index.html` خوانده می‌شود.
- **افزودن / ویرایش / حذف / جابجایی ترتیب** با فرم ساده (عنوان، تاریخ شمسی، دسته، متن).
- دکمه «🚀 انتشار تغییرات روی سایت» → درج نقطه‌ای و امن در فایل خبر سایت.
- ایمنی نوشتن: بررسی یکتایی نشانگر + سقف حجم خروجی + قفل فایل (LOCK_EX) — درس حادثه فایل ۴۶MB لحاظ شده.

## 📝 ۲) انتشار مقاله وبلاگ (AC2)
- فرم مقاله: عنوان، نامک انگلیسی (slug)، خلاصه متا، دسته، متن کامل.
- **نگارش ساده:** `##` → تیتر H2، `###` → H3، خط با `-` → لیست؛ پاراگراف‌بندی خودکار.
- با «🚀 انتشار»:
  - صفحه HTML کامل با **قالب استاندارد سایت** ساخته می‌شود (هدر/فوتر/استایل از مقالات موجود + title/description/canonical/OG/Schema Article)
  - به فهرست وبلاگ اضافه می‌شود
  - به **sitemap.xml** اضافه می‌شود
- **آرشیو مقاله (حذف با حفظ سئو):** نسخه اصلی در پوشه امن سرور بکاپ → آدرس قدیمی به وبلاگ ریدایرکت (+noindex) → حذف از فهرست و sitemap.

## 🔍 ۳) سئوی صفحات (AC3 — فاز سبک)
- فهرست ۱۵ صفحه اصلی سایت با title/description فعلی.
- ویرایش هر دو فیلد از پنل → اعمال مستقیم روی سایت.
- (ویرایش بلوک‌های متنی داخل صفحات data-editable در فاز بعدی در صورت نیاز)

## 🔐 امنیت (AC5)
- گارد سروری: فقط `admin` و `chairman` (پاسخ 403 برای بقیه) + دکمه برای سایر نقش‌ها مخفی.
- پاکسازی کامل ورودی‌ها: strip_tags با whitelist، حذف event handlerها و `javascript:`، گارد ضد path-traversal، محدودیت slug.
- لاگ کامل عملیات CMS در فایل محافظت‌شده.
- نشانگر وضعیت در بالای پنل: اتصال + **بررسی مجوز نوشتن** (اگر هاست write ندهد، پیام دقیق راهنما می‌دهد).

## ℹ️ درباره «آپدیت CRM با zip» (AC4)
این بخش نیازمند تایید فعال بودن `ZipArchive` در PHP هاست شماست. طبق رودمپ، فعلاً روال فعلی (دلتای کوچک + آپلود File Manager) که برایتان جا افتاده حفظ شده؛ اگر مایل بودید، از هاستینگ بپرسید «آیا ZipArchive فعال است؟» و با پاسخ مثبت، در اسپرینت بعدی بخش آپدیت خودکار را هم اضافه می‌کنم.

## 🧪 کیفیت
| مورد | نتیجه |
|---|:---:|
| tester12 جدید (CMS) | ✅ ۳۳/۳۳ |
| رگرسیون tester1–11 | ✅ ۴۹۶/۴۹۶ |
| ممیز ۸بخشی | ✅ PASS |
| **جمع** | **✅ ۵۲۹/۵۲۹** |

## 📁 دلتا (۸ فایل)
`api/cms.php` (**جدید**) • `crm/cms.js` (**جدید**) • `api/.htaccess` • `crm/index.html` • `crm/shell.js` • `crm/sw.js` • `ROADMAP-SPRINTS-52-70.md` • `RELEASE-NOTES-v75.0.md`

## ⚠️ تست بعد از آپلود
1. CRM → سیستم → «🎛 مدیریت سایت» → نوار وضعیت باید سبز شود («مجوز نوشتن تایید شد»). اگر قرمز بود → از هاستینگ write permission بخواهید.
2. تب اخبار → یک خبر آزمایشی اضافه → «🚀 انتشار» → صفحه `pishtaj.ir/news/` را چک کنید.
3. تب وبلاگ → مقاله کوتاه آزمایشی بسازید → آدرس جدید را باز کنید (قالب کامل سایت) → بعد «🗄 آرشیو» کنید.
4. با نقش غیرمجاز (مثلاً مدیرعامل) وارد شوید → دکمه «مدیریت سایت» نباید دیده شود.

## 🔭 بعدی: US-127/128 — ارتقای ۱۱۱ مقاله قرنطینه + محتوای جدید + تصاویر واقعی محصولات
