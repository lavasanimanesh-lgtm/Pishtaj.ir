# Release Notes — v31.7.41

**تاریخ:** ۱۴۰۵/۰۴/۳۰ (2026-07-21)  
**نسخه:** `v31.7.41`  
**نوع:** 🟠 ADV-TOOLS-RFQ-ACTIVATION-001 — نمایش واضح درخواست فعال‌سازی ابزار در RFQ هوشمند

---

## 🎯 مسئله گزارش‌شده

کاربر از صفحه ابزارها روی «درخواست فعال‌سازی» کلیک می‌کرد و وارد RFQ هوشمند می‌شد، اما داخل فرم RFQ گزینه یا نشانه واضحی برای «فعال‌سازی ابزار» نمی‌دید. فقط ممکن بود متن در توضیحات پر شود و کاربر فکر کند وارد فرم استعلام کالایی عادی شده است.

---

## ✅ اصلاح انجام‌شده

### 1) حالت Activation Mode در RFQ

در `rfq/index.html` وقتی URL شامل پارامترهایی مثل زیر باشد:

```text
activation=tools
tool=...
item=...
```

یا حتی متن item شامل `Advanced / License / Report / فعال‌سازی / لایسنس` باشد، فرم RFQ وارد حالت فعال‌سازی ابزار می‌شود.

رفتار جدید:

- بنر واضح نمایش داده می‌شود:

```text
درخواست فعال‌سازی ابزارهای پیشرفته
```

- توضیح بنر:

```text
این فرم برای صدور/فعال‌سازی کد لایسنس ابزارهای مهندسی PTF ثبت می‌شود.
```

- category مخفی به این مقدار تغییر می‌کند:

```text
فعال‌سازی ابزارهای پیشرفته مهندسی
```

- گزینه subject جدید ساخته و انتخاب می‌شود:

```text
درخواست فعال‌سازی ابزارهای پیشرفته PTF
```

- فیلدهای فنی و توضیحات با متن فعال‌سازی/لایسنس پر می‌شوند.

- دسته‌های کالایی قفل می‌شوند تا کاربر دنبال انتخاب پایپینگ/برق/ابزار دقیق نگردد.

---

### 2) لینک‌های فعال‌سازی ابزارها explicit شدند

در `tools/index.html` و `tools/tools-ui.js` لینک‌های فعال‌سازی اکنون explicit هستند:

```text
/rfq/?activation=tools&tool=all&item=Advanced Engineering Tools License
```

و برای paywall:

```text
/rfq/?activation=tools&tool=advanced_report&item=Advanced engineering report — ...
```

---

## 📁 فایل‌های تغییر یافته

```text
rfq/index.html
tools/index.html
tools/tools-ui.js
crm/index.html
crm/sw.js
crm/clear-cache.html
PTF-MASTER-HANDOVER.md
_tools/uat/tester218-rfq-tool-activation-visible.js
RELEASE-NOTES-v31.7.41.md
REGRESSION-REPORT-v31.7.41.md
```

---

## 🧪 تست اضافه‌شده

### `_tools/uat/tester218-rfq-tool-activation-visible.js`

پوشش:

- RFQ پارامترهای `activation`, `tool`, `item` را می‌خواند.
- بنر فعال‌سازی ابزار نمایش داده می‌شود.
- category مخفی به فعال‌سازی ابزار تغییر می‌کند.
- subject اختصاصی فعال‌سازی ساخته و انتخاب می‌شود.
- message/technical/vendor fields برای درخواست لایسنس پر می‌شوند.
- دسته‌های کالایی در activation mode قفل می‌شوند.
- لینک‌های tools دارای `activation=tools` هستند.

---

## ⚠️ Impact Analysis

| بخش | اثر | ریسک |
|---|---|---|
| RFQ هوشمند | فقط حالت URL-based activation اضافه شد | کم |
| RFQ عادی | دست‌نخورده مگر URL activation داشته باشد | کم |
| Tools activation links | explicit شدند | کم |
| API/CRM/Auth/Finance/Sync | دست‌نخورده | صفر |

---

## 📋 راستی‌آزمایی کارفرما

1. از صفحه `/tools/` روی «درخواست فعال‌سازی» کلیک کنید.
2. باید وارد `/rfq/` شوید.
3. در همان مرحله اول باید بنر واضح «درخواست فعال‌سازی ابزارهای پیشرفته» ببینید.
4. گزینه subject باید «درخواست فعال‌سازی ابزارهای پیشرفته PTF» باشد.
5. در مرحله مشخصات فنی، متن درخواست لایسنس/فعال‌سازی باید از قبل پر شده باشد.
6. ارسال فرم باید مثل RFQ معمولی کد رهگیری واقعی بدهد.

---

## 🚦 نتیجه

درخواست فعال‌سازی ابزارها اکنون از طریق RFQ هوشمند هم واقعی است و هم برای کاربر کاملاً قابل مشاهده و قابل تشخیص است.
