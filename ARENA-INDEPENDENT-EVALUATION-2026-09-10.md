# ارزیابی مستقل و جامع تغییرات مالی CRM — ۱۴۰۵/۰۶/۱۹

**نسخه:** v34.38.19
**بازهٔ بررسی:** همهٔ تغییرات از مبدأ `7d84f1b` (پیش از کار این نشست) تاکنون
**روش:** بازخوانی کامل diff هر فایل، تطبیق با قراردادهای قفل‌شدهٔ تسترها، ردیابی مسیرهای cross-cutting (sync / reconcile / backfill / merge / دفتر نقد)، و اجرای گیت CI کامل.

---

## ۱) دامنه و روش

هدف این سند این است که «هیچ چیز از قلم نیفتاده باشد». برای همین علاوه بر بازخوانی هر تغییر،
دو سؤال را برای تکتک موارد پرسیدیم:

1. آیا تغییر، **قرارداد گارد/تستر موجود** را نقض نمیکند؟ (بهخصوص `tester628`/`tester629`/`tester612`/`tester613`)
2. آیا تغییر در **تعامل با تغییرهای دیگر و با مسیرهای جانبی** (sync، بوت، سال قفل، چنددستگاهی، idempotency، replay) شکاف تازه باز نمیکند؟

خروجی این ارزیابی: **دو شکاف واقعی پیدا و در همین نشست رفع شد** (بخش ۴)؛ مابقی موارد سالم یا
با ریسکِ مستندِ کم باقی ماندند (بخش ۵). گیت CI پس از رفعها **۲۴۶ تستر سبز / ۰ شکست** است.

---

## ۲) فهرست کامل تغییرات (۱۸ فایل)

| فایل | تغییر | تستر قفل‌شده |
|---|---|---|
| `crm/fiscal.js` | B1/B2/B3: حقوقِ پرداخت‌شده (draw با paymentFor/salaryMonth) از ستون «مانده قابل تسویهٔ امسال» خارج و به خروجی نقدی (`salaryPaid` → `outflowsTotal`/`cashEnd`) اضافه شد؛ تفکیک پرداخت‌شده/پرداخت‌نشده در KPI/گزارش/CSV؛ **`netProfit` دست‌نخورده** | `tester628` |
| `crm/treasury.js` | برچسب خزانهٔ drawِ حقوق از «برداشت» به «پرداخت حقوق» (هم‌راستا با دفتر سهامداران) | `tester628` (ضمنی) |
| `crm/opex.js` | گاردهای write-time دوباره‌شماری: ساخت/ویرایش قالب در برابر ردیف دستی/تنخواه (گام ۳)، ثبت دستی در برابر تنخواه (گام ۱ تکمیلی)، تسویهٔ تک‌منبعی (گام ۴)، اعلان قالب ثبت‌نشده (گام ۵)، آستانهٔ قالب تکراری cat+amt | `tester629` |
| `crm/shareholders.js` | آشکارساز read-only ماه‌های غایب/تکراری حقوق (`ptfShareholderSalaryGaps`)؛ `eligibilitySince` فقط هنگام فعال‌شدن موظفی؛ دکمهٔ جبران حقوق (`ptfShareBackfillSalaries`) | `tester629`, `tester632` |
| `crm/data-quality.js` | نمایش یافتهٔ ماه غایب/تکراری حقوق + راهنما/اقدام؛ **fallback فقط‌خواندنی** نوع سند از پروفایل سهامدار برای ردیف حقوقِ بدون `isOfficial` | `tester629`, `tester633` |
| `crm/sync.js` | `sfComparableTs` — نرمال‌سازی زمان برندهٔ merge برای `ptf_crm_supplier_finance` (ارقام فارسی/عربی + شمسی→میلادی)؛ **در این ارزیابی سخت‌تر شد** (بخش ۴) | `tester630` |
| `crm/supplier-finance.js` | `manualAmountEdit` ماندگار روی ویرایش مبلغ فاکتور؛ `slImportRealPurchase` مبلغ دستی فاکتور/پرداخت را بازنویسی نمیکند | `tester630` |
| `api/sales-domain.php` | F-3 cross-check سروری (`possibleDuplicates`)؛ F-4 `backfill_shareholder_salaries` + `sd_jalali_months_between`؛ انتشار cross-month نوع سند حقوق؛ **ثبت backfill در allowlist‌های projection (در این ارزیابی)** | `tester631`, `tester632`, `tester633` |
| `_tools/uat/run-ci-gate.js` | ثبت تسترهای `628` تا `633` | — |
| ۶ تستر جدید + ۳ سند ARENA-* | تسترهای قفل‌شده و اسناد تحویلی | — |

---

## ۳) بررسی تک‌تک تغییرات

### ۳.۱ حقوق نقدی (B1/B2/B3) — `crm/fiscal.js`
- **صحیح:** `salaryPaid` فقط از draw با `paymentFor==='salary'` یا `salaryMonth` جمع میشود؛ علی‌الحساب عادی همچنان در `advYear` میماند (تصمیم محصول). دوباره‌شماری رخ نمیدهد چون ردیف تعهدی حقوق از `out.opex` بیرون است.
- **قرارداد حفظ شد:** `netProfit` (سود تعهدی) هیچ‌جا لمس نشده؛ `tester628` آن را قفل کرده است.
- **ریسکِ باقیماندهٔ کم (گزارشی، نه خطا):** `salaryUnpaid = max(0, opexSalaryTotal − salaryPaid)` فرض میکند پرداختِ حقوقِ سالِ جاری دقیقاً از محل تعهدِ همان سال است. اگر draw، حقوقِ سالِ قبل را بپردازد، ستون «پرداخت‌نشده» سالِ جاری میتواند کم‌نما شود. اثر فقط روی تفکیک نمایشی است؛ `cashEnd` و `netProfit` تحت تأثیر نیستند. **تصمیم:** تغییر ندادیم تا قرارداد `tester628` دستنخورده بماند.

### ۳.۲ گاردهای دوباره‌شماری — `crm/opex.js`
- همهٔ گاردها **فقط تأیید انسانی** هستند (بدون void/ادغام خودکار) — مطابق شرط کارفرما.
- متغیر `month` در onOk قالبِ ویرایش **قبل از استفاده** تعریف شد (رفع باگ احتمالی ترتیب)؛ در onOk ثبت قالب/دستی هم `month`/`amt` در scope هستند (تأیید شد).
- `oRows()`/`oAll()`/`opexRowActive()`/`fmtT()` همگی از قبل موجودند.

### ۳.۳ آشکارساز ماه حقوق — `crm/shareholders.js`
- read-only است؛ fallback مبدأ از `eligibilitySince` → اولین ادعای active → در غیر این صورت گزارش نمیشود.
- **تأیید ترتیب ماهها:** قالب `YYYY/MM` با ارقام لاتین است؛ sort لغوی برای این قالب صحیح است.

### ۳.۴ رفع بازگشت مبلغ فاکتور — `crm/sync.js` + `crm/supplier-finance.js`
- `manualAmountEdit` فقط روی تغییر واقعی مبلغ ست میشود؛ بازوارد فقط وقتی بازنویسی میکند که پرچم نباشد. متادیتا (قلم/تعداد/قیمت واحد/فایل) همیشه به‌روز میشود.
- void-wins (`ptfFinanceVoidWins`) کماکان مقدم بر مقایسهٔ زمان است.

### ۳.۵ F-3 (cross-check سروری) — `api/sales-domain.php`
- پویش ردیف دستیِ فعالِ هم‌دسته/هم‌مبلغ و تنخواهِ هم‌مبلغ/هم‌ماه، فقط هشدار در `possibleDuplicates`؛ ردیف خودِ قالب (tplId) و ردیف حقوق (shareholderSalary) مستثنا هستند.
- **یادداشت کم‌اهمیت:** اگر دو قالب هم‌دسته/هم‌مبلغ باشند، یک ردیف دستی ممکن است برای هر دو در `possibleDuplicates` تکرار شود — زائد ولی بی‌ضرر.

### ۳.۶ F-4 (eligibilitySince + backfill) — `api/sales-domain.php`
- `sd_jalali_months_between`: اعتبارسنجی دو سر بازه، گذر سال (اسفند→فروردین) صحیح، سقف ۳۰۰ سال.
- backfill فقط ساخت idempotent است: ردِ ماهِ سالِ قفل‌شده، ردِ هویت موجود (فعال یا سنگ‌قبر — `sd_salary_find_indexes`/`sd_recurring_find_indexes` بدون فیلتر active، سنگ‌قبر را هم میبینند ⇒ احیا نمیشود)، بدون void، ثبت در `corrections`.
- **یادداشت کم‌اهمیت:** اگر ماهی «نیمه‌موجود» باشد (فقط opex یا فقط sharetx)، backfill آن را میسازد نه تعمیر — تعمیرِ جفتِ ناقص عمداً کارِ reconcile است.

### ۳.۷ انتشار نوع سند حقوق — `api/sales-domain.php` + `crm/data-quality.js`
- فقط در reconcile صریح (`explicitEligibility`) و فقط ردیف‌های فعالِ حقوقِ سهامدارِ محدوده؛ ماهِ سالِ قفل‌شده رد میشود.
- fallback کلاینت فقط‌خواندنی است و حدس نمیزند (پروفایلِ بدون `salaryOfficial` → همچنان «نامشخص»).

---

## ۴) دو شکاف کشف‌شده در همین ارزیابی (و رفع آنها)

### 🔴 شکاف ۱ — ترتیب `sfComparableTs` در merge تأمین‌کننده (`crm/sync.js`)
**مشکل:** پیشوندهای قبلی `'G'`(ISO/شمسیِ تبدیل‌شده) < `'H'`(fallback غیرقابل‌تبدیل) < `'Z'`(بی‌زمان) در ترتیب لغوی بودند؛ یعنی:
- رکوردِ **بدون زمان** (`'Z'`) بالاترین رتبه را میگرفت و برندهٔ merge میشد؛
- تاریخِ **غیرقابل‌تبدیل** (`'H'`) از ISO واقعی (`'G'`) برنده میشد.

این دقیقاً همان ریشهٔ «ویرایش شد ولی برگشت» بود که فقط در حالتهای نادرتر (دادهٔ کهنهٔ بدون timestamp یا تاریخِ غیرشمسی) دوباره زنده میشد.

**رفع:** رتبهٔ سهلایه — `'2'` = ISO/شمسیِ تبدیل‌شده (قابل‌اعتمادترین) > `'1'` = fallback غیرقابل‌تبدیل > `'0'` = بی‌زمان (کهنه‌ترین، هرگز برنده نمیشود). حالتهای رایج (دو ISO، دو شمسی، ISO در برابر شمسی) ترتیب نسبی قبلی را حفظ میکنند.

**قفل:** `tester630` با سه assertion رفتاری جدید (بی‌زمان بازنده است؛ fallback بازندهٔ ISO است؛ بی‌زمان از شمسیِ تبدیل‌شده هم بازنده است).

### 🟠 شکاف ۲ — backfill در allowlist‌های projection ثبت نبود (`api/sales-domain.php`)
**مشکل:** `backfill_shareholder_salaries` در `sd_is_recurring_projection_action` و `sd_recurring_sharetx_projection_allowed` نبود؛ در نتیجه پاسخِ فرمان از مسیر عمومی `sd_result_data(['ptf_crm_opex'=>[]])` میرفت (یک envelope خالی) و ردیف‌های ساخته‌شده فقط بعد از pull دوم ظاهر میشدند؛ مدیران هم projection فوریِ sharetx نمیگرفتند. داده در نهایت درست مینشست، اما ضعیف‌تر و ناهمگن با بقیهٔ فرمان‌های recurring بود.

**رفع:** ثبت action در هر دو allowlist. حالا backfill برای مدیران، envelopeِ opex دلتا + projection فوری sharetx برمیگرداند (و برای accountant طبق قرارداد، فقط opex — مطابق رفتار reconcile).

**قفل:** `tester632` با دو assertion استاتیک جدید (ثبت در هر دو allowlist).

---

## ۵) ریسک‌های باقیمانده (مستند، بدون اقدام — با دلیل)

| # | شدت | شرح | چرا اقدام نکردیم |
|---|---|---|---|
| R1 | کم | تفکیک «حقوق پرداخت‌نشده» در صورت پرداختِ حقوقِ سالِ قبل میتواند کم‌نما شود | فقط نمایشی؛ تغییر آن قرارداد `tester628` را میشکند |
| R2 | کم | `possibleDuplicates` در F-3 میتواند برای چند قالب، یک ردیف را تکراری فهرست کند | زائد، نه خطا |
| R3 | کم | backfill ماهِ «نیمه‌موجود» (فقط opex یا فقط sharetx) را تعمیر نمیکند | عمداً create-only؛ تعمیر وظیفهٔ reconcile |
| R4 | اطلاعی | انتشار isOfficial روی ردیف‌های ماه‌های دیگر، `updatedAtISO` آن ردیف‌ها را تازه میکند ولی `$updated` را افزایش نمیدهد (شمارندهٔ جداگانه `salaryOfficialPropagated` دارد) | فقط متریک گزارش؛ داده در `$changes` کامل commit میشود |

هیچ‌کدام از اینها مسیر نوشتنِ مالی، قفل سال مالی، یا قرارداد «بدون void/ادغام خودکار» را نقض نمیکند.

---

## ۶) پوشش تست

- **گیت CI:** ۲۴۶ تستر سبز / ۰ شکست (پس از رفع‌های این ارزیابی).
- هر ۶ تغییر قفل‌شدهٔ مالی یک تستر رفتاریِ مستقل دارد (`628`–`633`) که نه‌فقط وجودِ رشته، بلکه مدل رفتار را میسنجد.
- `tester630` و `tester632` در همین ارزیابی برای قفلِ دو شکافِ رفع‌شده تقویت شدند.
- سینتکس: `node --check` روی فایل‌های JS سالم؛ `php-parser` روی `api/sales-domain.php` سالم (php-cli در محیط موجود نیست؛ fallback گیت استفاده شد).

---

## ۷) جمع‌بندی

- **همهٔ تغییراتِ مورد تأیید کارفرما (B2/B3، گاردهای RCA، رفع بازگشت فاکتور، F-3، F-4، رفع یافتهٔ رسمی/غیررسمی) پیاده و قفل شده‌اند.**
- در بازبینی مستقل، **دو شکاف واقعی** یافت شد که هر دو مربوط به حالت‌های مرزیِ مسیرهای فرعی بودند (merge بین‌دستگاهی و projection پاسخ فرمان) — هر دو رفع و قفل شدند.
- چهار ریسکِ کم/اطلاعی مستند باقی ماندند که عمداً دست‌نخورده‌اند تا قراردادهای قفل‌شدهٔ موجود نقض نشوند.
- هیچ موردی از «حذف بی‌صدا»، «ادغام خودکار ردیف دستی»، «نقض قفل سال مالی»، یا «تغییر `netProfit` بدون تستر» مشاهده نشد.

**وضعیت نهایی: آمادهٔ استقرار.**
