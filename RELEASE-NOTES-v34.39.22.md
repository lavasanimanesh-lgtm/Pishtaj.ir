# انتشار v34.39.22 — ۱۴۰۵/۰۷/۰۱ (CONTACT-ROOTS)

## ریشه‌یابی و ریشه‌کنی «شماره‌های تماس مشتریان ثبت‌شده توسط کاربران پاک می‌شود و قابل مشاهده نیست»

گزارش تکرارشوندهٔ کارفرما با اجرای کد واقعی (تستر676 — بوت کامل dedup→offers→phonefmt→sales-domain-v2
در vm + سرور واقعی‌نمای ۱:۱ با semantics دقیق `entity_upsert`) ریشه‌یابی شد. fixهای قبلی
(v34.37.7→v34.39.21) هر کدام یک لایه را بسته بودند؛ **۶ ریشهٔ بازمانده** اثبات و رفع شد:

| ریشه | نشانه | علت | رفع |
|---|---|---|---|
| **R1** پاک‌شدن واقعی | تبدیل حقوقی→حقیقی، همهٔ شماره‌های قبلی می‌پرید | `ptfPreserveLegalContactsAsPhones` کلید dedup را با `[^0-9]` می‌ساخت؛ ارقام **فارسی**ِ (ذخیرهٔ نرمال‌ساز) کلید خالی → `if (!key) return` همه را می‌ریخت؛ حتی `seen['']` ناشی از یک شمارهٔ فارسیِ فرم کل قدیمی‌ها را حذف می‌کرد | کلید script-fold (`ptfToEnDigits`) + fallback برای بدون-رقم + هرگز drop نکردن |
| **R2** پاک‌شدن واقعی (مالِ من سالم، مالِ او پاک) | ذخیرهٔ مودالِ بازمانده، شمارهٔ ثبت‌شدهٔ کاربر دیگر (پس از pull وسط مودال) را می‌پراند | `_ccBaseAt` از `oldRecPre` (کشِ لحظهٔ save) خوانده می‌شد نه «نسخهٔ لحظهٔ بازشدن مودال» → با pull وسط مودال، base با `updatedAt` سرور برابر می‌شد → `sd_contact_stale_merge` مسیر «fresh edit = LWW» → جایگزینی wholesale؛ شرط اتحاد کلاینتی هم (`liveAt !== oldAt`) هرگز فعال نمی‌شد | capture صریح `window._ptfContactFormBaseAt` در `showCustModal`/`showSupModal2` + استفاده در `_ccBaseAt` و شرط اتحاد (saveCust2/saveSup2) |
| **R3** تخریب/پاک‌شدن | «021 8800 داخلی 12» → شمارهٔ غلط به‌هم‌چسبیده؛ «تماس بگیرید» → '' → auto-_ccClear بی‌جهت | `ptfPhoneNorm` برای ورودی ساختارنیافته ارقام کلمات را به شماره می‌چسباند؛ بدون-رقم → '' | نگهبان ساختار (هر ورودی با حروف دست‌نخورده برمی‌گردد) + بدون-رقم → متن اصلی |
| **R4** نامرئی | کارت مشتری: «اشخاص رابط» کنار phones پنهان؛ تلفنخانه‌های دوم به‌بالا نامرئی | `showEntityCard` الگوی either/or + فقط `coTels[0]` | بخش‌های مستقل phones و people + همهٔ coTels |
| **R5** نامرئی/نیافتنی | جستجوی شماره مشتری را نمی‌یافت؛ کلیک روی شمارهٔ فارسی تماس نمی‌گرفت | `entityMatches` بدون coTels/phones/ph در haystack و بدون fold ارقام فارسی↔انگلیسی؛ `telHref` ارقام فارسی را می‌ریخت | haystack کامل + fold دوطرفه + `telHref` با `ptfToEnDigits` |
| **R6** پاک‌شدن در sync | push مسیر legacy (heal/migrate/import/fallback) با رکوردِ ناقص، شماره‌های سرور را می‌پراند | `data_push` کل blob را جایگزین می‌کرد و فقط «حذف ردیف» را می‌دید (MASS-DELETE-SHIELD) | `sync_contact_fields_fill` در api/crm.php — قرارداد v34.8.34 «فیلد غایب = تغییر نکرده»: کلیدِ تماسِ غایب با مقدارِ غیرتهیِ سرور پر می‌شود (customers/suppliers؛ مستثنی restore/allow_wipe) |

## قراردادهای حفظ‌شده
- حذف آگاهانهٔ شخص/شماره در مودالِ تازه (base = نسخهٔ سرور) همچنان محترم است (LWW).
- `_ccClear` صریح همچنان پاک‌سازی عمدی را از همهٔ گاردها مستثنا می‌کند.
- نرمال‌سازی شماره تغییر شکل نداده: fa = ملیِ فارسی، en/print = بین‌المللیِ خوانا.
- کلیدِ حاضرِ خالی در payload (نیت صریح فرم) در data_push پر نمی‌شود.

## آزمون
`node _tools/uat/tester676-v34.39.22-contact-roots.js` — ۳۵ تست (۶ ریشه با کد واقعی + قراردادهای ساختاری).
رگرسیون: tester604 (45/0)، tester611 (21/0)، tester614 (36/0)، tester615 (34/0)، tester660 (37/0)، tester674/675 سبز.
(شکست‌های pre-existing بدون تغییر: tester554 expectCreate و tester83 rfqsFinalize — هر دو با baseline در HEAD بدون تغییرات عیناً FAIL بودند.)
