# 🔎 بررسی جامع وضعیت پروژه Pishtaj.ir (وب‌سایت + CRM)
**تهیه‌کننده:** ایجنت Arena.ai | **تاریخ بررسی:** ۲۰۲۶-۰۷-۲۵ | **مبنا:** آخرین کامیت گیت‌هاب (`db35ac0`، شاخه `main`) + سند هندآور رسمی `PTF-MASTER-HANDOVER.md` (v32) و بیش از ۷۸۰ سند جانبی موجود در ریشه پروژه

> این سند از طریق کلون واقعی مخزن، اجرای واقعی ابزارهای داخلی خود پروژه (`audit.py` و رگرسیون ۲۶۶ تستری) و بازرسی دستی کد/داده تهیه شده — نه صرفاً خلاصه‌برداری از فایل‌های ادعایی هندآور.

---

## ۱. خلاصه اجرایی (۳۰ ثانیه‌ای)

| وضعیت | نتیجه |
|---|---|
| **آیا پروژه در «آماده تحویل نهایی» است؟** | ❌ **خیر.** سند هندآور رسمی چند نسخه عقب‌تر از کد واقعی مخزن است و پروژه هنوز درگیر یک Incident امنیتی حل‌نشده است. |
| **آیا ابزار ممیزی داخلی خودِ پروژه (`audit.py`) سبز است؟** | ❌ **خیر — ۴ خطای قرمز** از جمله عدم تطابق مستندات نسخه با نسخهٔ واقعی کد. |
| **آیا رگرسیون خودکار (۲۶۶ تستر) سبز است؟** | ❌ **خیر — ۶۳ فایل و ۹۳ چک FAIL** (بیشتر به دلیل عدم به‌روزرسانی نسخه، اما چند مورد باگ واقعی). |
| **بزرگ‌ترین ریسک؟** | 🔴 **افشای دادهٔ حساس Production در گیت‌هاب** — هش رمز کاربران واقعی، توکن‌های نشست فعال (تا ۱ مرداد معتبر)، IP کاربران و دادهٔ مشتریان واقعی همگی داخل ریپو کامیت شده‌اند. |
| **گیت زیرساختی حیاتی؟** | 🟡 محیط Staging مستقل هنوز پیاده نشده (`STAGE0-ENV-001` باز است) با وجود این‌که یک Incident جدی (قطعی کامل احراز هویت) دقیقاً به همین دلیل رخ داده بود. |

---

## ۲. عدم تطابق «سند هندآور» با واقعیت مخزن — مهم‌ترین یافته

سند اصلی هندآور که در ریشهٔ پروژه قرار دارد (`PTF-MASTER-HANDOVER.md`) عنوان می‌کند نسخهٔ فعلی **v32** و «آمادهٔ UAT» است. اما بازرسی مستقیم کد نشان می‌دهد:

- نسخهٔ واقعی فعال در `crm/index.html`, `crm/sw.js`, `crm/clear-cache.html`: **`v33.0.3`**
- آخرین اسناد رسمی همراه یک نسخه (`RELEASE-NOTES`, `REGRESSION-REPORT`) فقط تا **v33.0.1** موجودند — یعنی برای **v33.0.2 و v33.0.3** هیچ `RELEASE-NOTES` یا `REGRESSION-REPORT` ثبت نشده.
- ابزار خودِ پروژه (`_tools/audit.py`) دقیقاً برای همین موضوع یک گیت مکانیکی ساخته (به‌خاطر Incident قبلی مستندسازی)، و همین الان با اجرای واقعی **رد می‌شود**:
  ```
  ❌ release-docs: سند الزامی انتشار موجود نیست: RELEASE-NOTES-v33.0.3.md
  ❌ release-docs: سند الزامی انتشار موجود نیست: REGRESSION-REPORT-v33.0.3.md
  ```
- در کنار این، مخزن حاوی **بیش از ۷۸۰ فایل Markdown** با شماره‌گذاری نسخهٔ متناقض و غیرخطی است (نمونه: `v22.5`, `v31.9`, `v32`, `v33.0`, `v33.0.1`، سپس ناگهان جهش به `v90`، `v100`، `v121`، `v123.2`، بعد بازگشت به `v22.4→v22.5`، بعد دوباره `v32→v33`). این خودش نشانهٔ رسمی یک مشکل تاریخی تکرارشونده در پروژه است که حتی خودِ تیم/ایجنت‌های قبلی هم به آن اذعان کرده‌اند (`RELEASE-NOTES-v22.5.md`: *«کلیه ریلیزنوت‌های گیج‌کننده قدیمی با شماره‌های سه رقمی حذف شدند تا ایجنت‌های بعدی دچار اشتباه نشوند»*) — اما این وعده دوباره نقض شده و اکنون سری `v90` تا `v123.2` دوباره در کنار سری `v31.x/v32/v33` زنده است.

**نتیجه:** هیچ ایجنت یا انسان جدیدی نمی‌تواند صرفاً با خواندن `PTF-MASTER-HANDOVER.md` بفهمد نسخهٔ واقعی و تاریخچهٔ واقعی چیست؛ باید مثل این بررسی مستقیماً کد را بخواند.

---

## ۳. یافتهٔ حیاتی امنیتی 🔴 — دادهٔ Production داخل گیت‌هاب کامیت شده

مخزن گیت‌هاب (خصوصی، اما همچنان یک مخزن کد منبع، نه یک بکاپ امن) حاوی فایل‌های زیر است که **مستقیماً از سرور واقعی Production کپی شده‌اند**:

| فایل | محتوای حساس |
|---|---|
| `crm/data/crm_users.json` | نام، موبایل، ایمیل **واقعی** ۳ نفر از مدیران/کارکنان شرکت + `password_hash` بروکریپت واقعی آن‌ها |
| `crm/data/tokens.json` | **۱۱ توکن نشست فعال و منقضی‌نشده** با IP واقعی کاربران؛ جدیدترین توکن تا **۱ مرداد ۱۴۰۵ (2026-08-01)** معتبر است — یعنی هرکسی به این ریپو دسترسی داشته باشد می‌تواند همین الان جای این کاربران وارد CRM شود |
| `crm/data/api_log.txt` | لاگ کامل IP و اکشن‌های واقعی کاربران تا ساعات قبل از کامیت |
| `api/error_log` | ۵۰۶ خط لاگ خطای سرور واقعی با مسیر کامل هاست (`/home/cp46861/pishtaj.ir/...`) |
| `api/ptf_crm_data.json`, `crm/data/rfqs.json` | نام واقعی مشتریان B2B (پتروشیمی جم، پالایشگاه ستاره خلیج فارس و...)، شماره موبایل تماس |
| `crm/data/backups/*.json.gz` | ۶ فایل بکاپ فشردهٔ کامل دادهٔ CRM (روزانه/ساعتی/ماهانه) |

**چرا این حیاتی است:**
1. برخلاف تمام مستندات پروژه که مکرراً روی «امنیت» و «RBAC» و «JWT ۷روزه» تأکید دارند (اسناد هندآور، Incident Report و ده‌ها Release Note امنیتی)، **دقیقاً همان چیزی که این کنترل‌ها قرار است از آن محافظت کنند (توکن معتبر + پسورد هش‌شده)** به‌صورت متن ساده در گیت‌هاب نگه داشته می‌شود.
2. این با دستورالعمل رسمی خودِ پروژه در تناقض است: `STAGE-0-STAGING-SETUP-CHECKLIST-v31.7.md` صراحتاً می‌گوید *«اتصال staging به `ptf_crm_data.json` واقعی ممنوع»* و *«password/token واقعی در fixture ممنوع»* — اما این قاعده دربارهٔ خودِ ریپوی اصلی هم به همان اندازه صدق می‌کند و اینجا نقض شده.
3. `.gitignore` در ریشهٔ پروژه اصلاً وجود ندارد؛ هیچ مکانیزمی جلوی کامیت دوبارهٔ این فایل‌ها را نمی‌گیرد.

**اقدام فوری پیشنهادی (مستقل از باقی این گزارش):**
- فوراً تمام رمزهای کاربران فوق را در سرور واقعی تغییر دهید و تمام توکن‌های فعال را باطل (revoke) کنید.
- `crm/data/`, `crm/data-old/`, `api/error_log`, `api/ptf_crm_data.json`, بکاپ‌ها و لاگ‌ها را از تاریخچهٔ گیت حذف کرده و یک `.gitignore` مناسب اضافه کنید.
- در آینده هرگز پوشهٔ دادهٔ زندهٔ CRM را داخل ZIP ریلیز/ریپو قرار ندهید (این نکته خودش در چند سند پروژه به‌عنوان قاعده ذکر شده بود ولی رعایت نشده).

---

## ۴. نتیجهٔ اجرای واقعی ابزار ممیزی پروژه (`_tools/audit.py`)

اجرا شد (نه فرضی — واقعاً روی ۵۸۶ صفحهٔ سایت اجرا گردید):

```
❌ 4 خطا:
 • ۵۵۱ لینک شکسته (نمونه بارز: صفحات about/*.html که با ../../ به assets ارجاع می‌دهند
   درحالی‌که این فایل‌ها خودشان در سطح اول پوشه‌اند نه در زیرپوشهٔ دوم — مسیر نسبی اشتباه)
 • احتمال درز رمز/کلید متنی در crm/index.html (۳ مورد الگوی password=)
 • RELEASE-NOTES-v33.0.3.md موجود نیست
 • REGRESSION-REPORT-v33.0.3.md موجود نیست

⚠️ 4 هشدار:
 • ۱۷۵ صفحه خارج از sitemap.xml
 • ۸۳ صفحه بدون favicon
 • ۱۰۹ گروه عنوان (title) تکراری بین صفحات
 • ۹۳ مقاله در «مرکز دانش» منتشرشده با کمتر از ۸۰۰ کلمه (با وجود ادعای مکرر اسناد هندآور مبنی بر «۱۵۰۰+ کلمه در همهٔ مقالات»)
```

نکتهٔ مهم دربارهٔ ۵۵۱ لینک شکسته: این عدد به احتمال زیاد شامل **موارد کاذب (false positive)** ناشی از این است که پروژه هم‌زمان دو ساختار URL دارد — نسخهٔ تخت (`about/certificates.html`) و نسخهٔ پوشه‌ای (`about/certificates/index.html`) — و لینک‌های داخلی گاهی برای یکی نوشته شده‌اند ولی چک برای دیگری اجرا می‌شود. با این حال، **همین دوگانگی ساختار URL خودش یک بدهی فنی واقعی است** که باید یکی از دو الگو حذف/ریدایرکت شود، وگرنه خطر محتوای تکراری (duplicate content) در سئو (که با ۱۰۹ گروه عنوان تکراری همسو است) جدی‌تر می‌شود.

---

## ۵. نتیجهٔ اجرای واقعی رگرسیون CRM (۲۶۶ تستر خودکار)

رگرسیون کامل (`node _tools/run-full-regression.js`) به‌صورت واقعی اجرا شد:

```
files_pass: 203   files_fail: 63
checks_pass: 5046  checks_fail: 93
```

توزیع علت‌ها:

| دسته | تعداد فایل | توضیح |
|---|---:|---|
| **صرفاً عدم هم‌گام بودن نسخه** (تسترهای قدیمی انتظار `v31.9` یا نسخه‌های میانی دارند، کد به‌درستی روی `v33.0.3` جلو رفته) | ۳۰ | بی‌خطر — صرفاً باید تسترهای قدیمی حذف/به‌روزرسانی شوند تا سیگنال درست باقی بماند |
| **تسترهای Placeholder خالی عمداً گذاشته‌شده** (`tester282‑286-dummy.js`) | ۵ | نشانهٔ کار نیمه‌تمام/جانگه‌دار در پایگاه تست |
| **باگ منطقی واقعی و بازتولیدپذیر** | ۲۸ | نیاز به رفع دارند (نمونه‌های کلیدی زیر) |

### مهم‌ترین باگ‌های واقعی یافت‌شده در رگرسیون:

1. **`tester126-v23.js` کرش می‌کند** با خطای جاوااسکریپت `TypeError: m.countByOwnership is not a function` — یعنی یک تابع مورد انتظار در ماژول مربوطه اصلاً وجود ندارد یا export نشده (همان الگوی تکراری «Gap معماری TECH-DEBT-010» که در `AUDIT-SUMMARY-FA.md` قبلاً هم ثبت شده بود و ظاهراً دوباره تکرار شده).
2. **`tester245-mobile-login-user-merge.js`**: تابع کمکی `load_all_crm_users_sources()` نوشته شده و در `users_sync` استفاده می‌شود، اما در دو نقطهٔ حیاتی دیگر (`case 'auth_login'` و `case 'users_get'`) هنوز استفاده نشده — یعنی دقیقاً همان کلاس باگ ریشه‌ای Incident قبلی (`BUG-AUTH-MOBILE-USER-001`) که «منابع کاربر در سرور جدا از هم‌اند» **به‌طور کامل رفع نشده**؛ فقط نیمی از مسیرها اصلاح شده‌اند.
3. **`tester277-auth-token-session-stabilization.js`** (۴ از ۶ چک FAIL): مکانیزم‌های ادعاشدهٔ «single-flight refresh»، «حداکثر ۳ بار retry»، و «cache توکن در سطح PHP request» یا در کد پیاده نشده‌اند یا الگوی دقیق مورد انتظار را ندارند — این دقیقاً حوزهٔ همان Incident امنیتی SEV-1 قبلی (`BUG-AUTH-001`) است که مستندات ادعا می‌کنند «رفع و پایدار شده»، اما تست قراردادی (contract test) نشان می‌دهد پایداری کامل تأیید نیست.
4. **`tester184-auth-contract-e2e.js`**: تسترِ ساخته‌شده مستقیماً برای همان Incident، اکنون خودش FAIL می‌دهد چون `users_get` و `data_rev` در لیست `public_actions` مطابق انتظار نیستند — یعنی کد از زمان نوشتن آن تست دوباره تغییر کرده و کسی این تست حیاتی امنیتی را به‌روزرسانی/بازبینی نکرده.
5. **`tester208`, `tester209`, `tester211` (سئو/متریک)**: چند صفحهٔ عمومی توضیح متا (`description`) کوتاه‌تر از ۷۰ کاراکتر یا تکراری دارند؛ صفحات `knowledge-center/index.html` و `tools/index.html` فاقد عنوان/توضیح «استراتژیک» مورد انتظارند.
6. **`tester213`**: صفحهٔ ابزارها هنوز ادعای «PDF رایگان» می‌کند در حالی‌که این ویژگی طبق تصمیم قبلی حذف/محدود شده بود (paywall) — یعنی محتوای مارکتینگ با منطق واقعی محصول هم‌گام نیست.
7. **`tester55` / `tester8`**: قواعد امنیتی `.htaccess` مربوط به `api/` (اجازهٔ صریح `notify-bot`) و ریشهٔ سایت (مسدودسازی `json|log|txt`) با انتظار تسترهای قدیمی مطابقت ندارد — با توجه به یافتهٔ بخش ۳ (افشای `error_log` و JSONهای داده)، این هشدار باید **جدی گرفته شود**، چون `.htaccess` فعلی ریشه صراحتاً `json|log|zip|gz|bak|sql|md` را مسدود می‌کند ولی **در عمل فایل‌ها همچنان از طریق گیت قابل دسترس‌اند** (وب‌سرور فقط جلوی HTTP request را می‌گیرد، نه دسترسی از طریق ریپو).

---

## ۶. Incident باز و بدون تأیید نهایی — `BUG-AUTH-001`

سند رسمی `INCIDENT-REPORT-BUG-AUTH-001.md` (که خودِ پروژه تهیه کرده) یک قطعی کامل SEV-1 در احراز هویت را شرح می‌دهد (کل کاربران در بازهٔ ۱۸ تا ۱۹ تیر قادر به ورود نبودند، به دلیل ناهماهنگی قرارداد توکن بین فرانت و بک‌اند در حین یک اصلاح امنیتی). نکات مهم برای شما به‌عنوان کارفرما:

- **بند ۷ این گزارش («اقدامات باز») هنوز به‌صراحت بسته اعلام نشده:**
  - ⏳ اجرای تست ۴نقشه روی Production واقعی و ثبت نتیجه — باز
  - ⏳ اجرای اسکریپت سلامت داده روی سرور واقعی — باز
  - ⏳ بررسی لاگ دسترسی برای فعالیت مشکوک در بازهٔ Incident — باز
  - ⏳ بکاپ کامل خارج از سرور — باز
  - ⏳ **تصمیم رسمی کارفرما دربارهٔ راه‌اندازی محیط Staging مستقل (`STAGE0-ENV-001`)** — باز
- ریشهٔ فرآیندی اصلی Incident این بود که یک تغییر امنیتی مستقیماً روی Production دیپلوی شد چون محیط Staging مستقل هرگز ساخته نشده بود. **این بند هنوز باز است** — یعنی پروژه دقیقاً همان شرایطی را دارد که باعث Incident قبلی شد، و هیچ تضمین ساختاری‌ای وجود ندارد که تکرار نشود.
- در همین حین، رگرسیون واقعی (بخش ۵ بالا) نشان می‌دهد بخشی از منطق مرتبط با همان حوزه (auth/token/session) هنوز دقیقاً منطبق با ادعای «پایدارشده» نیست.

---

## ۷. بدهی فنی و کیفیت کد (مستقل از گزارش‌های قبلی، با شواهد تازه)

- **حجم بی‌سابقهٔ اسناد Markdown غیرفعال:** ۷۸۰ فایل `.md`/`.txt` در ریشهٔ پروژه (بک‌لاگ‌ها، ارزیابی‌ها، ریلیزنوت‌ها، مانیفست‌ها) بدون هیچ پوشه‌بندی یا آرشیو. این حجم عملاً پیمایش/نگهداری پروژه را برای هر انسان یا ایجنت جدید به‌شدت سخت می‌کند و خودش منبع خطای «کدام سند معتبر است؟» شده (مثال بند ۲).
- **فایل‌های ZIP/باینری اضافی داخل کد منبع:** `crm/data.zip` (2.6MB)، `crm/index.html.zip`، `crm/sync.js.zip`، `api/auth.php.zip`، `tools.zip` — نسخه‌های فشردهٔ همان فایل‌های موجود، ظاهراً باقی‌ماندهٔ فرآیند build/deploy که هرگز پاک نشده‌اند.
- **پوشهٔ `crm/__MACOSX/`** — آرتیفکت macOS از استخراج نادرست یک ZIP، باید حذف شود.
- **پوشهٔ `crm/data-old/`** موازی با `crm/data/` — داده‌های تکراری/قدیمی که نامشخص است چرا هنوز نگه داشته شده‌اند.
- **دو ساختار URL موازی** برای بخش‌های `about/`, `services/`, `industries/` (هم `xxx.html` مسطح و هم `xxx/index.html`) — منبع دوباره‌کاری، لینک شکسته و سئوی ضعیف (بند ۴).
- **لاگ خطای واقعی سرور (`api/error_log`, ۵۰۶ خط)** نشان می‌دهد حتی در روزهای اخیر (۲۳ تیر) خطای بحرانی `Cannot redeclare save_data()` به‌طور مکرر (۴۴۹ بار) در سرور واقعی رخ داده — یعنی نسخه‌ای از `api/crm.php` با تابع تکراری روی Production در حال اجرا بوده (به احتمال زیاد به دلیل آپلود دستی/ناقص فایل روی هاست، نه لزوماً باگ کد فعلی مخزن — چون در نسخهٔ فعلی مخزن این تابع فقط یک‌بار تعریف شده)، که نشان می‌دهد **فرآیند دیپلوی دستی و بدون کنترل نسخه است** و خطای انسانی مستقیماً به Production راه پیدا می‌کند.

---

## ۸. نکات مثبت (برای انصاف در گزارش)

- خودِ پروژه یک زیرساخت تست نسبتاً پخته دارد: ۲۶۶ تستر خودکار جاوااسکریپتی + `audit.py` (ممیزی استاتیک سایت) + چارچوب «تست شبیه‌سازی‌شدهٔ انسانی» (`_human_test/`) با قاعدهٔ مصوب کارفرما («بعد از هر ۵ اسپرینت اجباری است»). این سطح از self-testing برای یک پروژهٔ بدون تیم QA رسمی قابل‌توجه است.
- تیم/ایجنت‌های قبلی هرگاه Incident رخ داده، **گزارش ریشه‌یابی رسمی و شفاف (RCA)** نوشته‌اند (مثل `INCIDENT-REPORT-BUG-AUTH-001.md`) به‌جای پنهان‌کاری — این فرهنگ خوبی است که باید حفظ شود.
- محتوای وب‌سایت عمومی (۵۰۰ مقالهٔ مرکز دانش + ۳۲ مقالهٔ وبلاگ + صفحات خدمات/صنایع) از نظر حجم قابل‌توجه است و ساختار سئوی پایه (schema.org، sitemap، robots.txt) وجود دارد؛ نیاز اصلی، پاکسازی و یکپارچه‌سازی است نه بازسازی از صفر.
- معماری امنیتی CRM (JWT، RBAC نقش‌محور، سپر داده‌صفر برای جلوگیری از overwrite، role-scoped sync) از نظر طراحی مفهومی درست و پیشرفته است — مشکل اصلی در **پیاده‌سازی کامل و پایدارسازی نهایی** آن است، نه در طراحی.

---

## ۹. جمع‌بندی و توصیه‌های اولویت‌دار

| اولویت | اقدام |
|---|---|
| 🔴 فوری (امروز) | چرخش (rotate) تمام رمزهای عبور و ابطال تمام توکن‌های فعال کاربران CRM که هش/توکن‌شان در گیت‌هاب افشا شده؛ حذف `crm/data/`, `api/error_log`, `api/ptf_crm_data.json` و بکاپ‌ها از تاریخچهٔ گیت؛ افزودن `.gitignore` مناسب |
| 🔴 فوری | ساخت `RELEASE-NOTES-v33.0.3.md` و `REGRESSION-REPORT-v33.0.3.md` تا گیت `audit.py` سبز شود؛ یکسان‌سازی سند هندآور اصلی با نسخهٔ واقعی کد |
| 🟠 این هفته | تصمیم رسمی کارفرما دربارهٔ راه‌اندازی محیط Staging مستقل (`STAGE0-ENV-001`) — این تنها موردی است که به تصمیم شما نیاز دارد، نه کد |
| 🟠 این هفته | بستن رسمی بند ۷ گزارش Incident (اجرای تست روی Production واقعی + بررسی لاگ دسترسی مشکوک) |
| 🟡 این اسپرینت | رفع باگ‌های واقعی رگرسیون: تکمیل `load_all_crm_users_sources()` در `auth_login`/`users_get`، رفع کرش `tester126` (`countByOwnership`)، تکمیل مکانیزم single-flight/retry توکن |
| 🟡 این اسپرینت | یکی‌سازی ساختار URL (حذف یکی از دو الگوی html مسطح/پوشه‌ای)، رفع ۱۰۹ عنوان تکراری، تکمیل ۹۳ مقالهٔ زیر ۸۰۰ کلمه |
| 🟢 میان‌مدت | پاکسازی مخزن: حذف ZIPها و `__MACOSX`، آرشیو کردن ۷۸۰ فایل Markdown تاریخی در یک پوشهٔ `/archive` با یک ایندکس واحد و معتبر، حذف تسترهای قدیمی نامرتبط با نسخهٔ فعلی |

**جمع‌بندی یک‌خطی:** پروژه از نظر کارکردی و محتوایی حجیم و نسبتاً پیشرفته است، اما در حال حاضر **در وضعیت «سالم برای ادامهٔ کار روزمره» است، نه «آمادهٔ تحویل نهایی و بی‌نقص»** — مهم‌ترین مانع، افشای دادهٔ حساس Production در گیت‌هاب و مستندات هندآوری است که با کد واقعی هم‌گام نیست.
