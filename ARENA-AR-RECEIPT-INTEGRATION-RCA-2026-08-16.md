# ریشه‌یابی و طرح اصلاح فازبندی‌شده — یکپارچگی «وصولی» بین مطالبات، حساب مشتری و پرونده
**تاریخ:** ۲۰۲۶-۰۸-۱۶ | **نسخهٔ پایه:** v34.7.17
**گزارش کارفرما:** «مشتری مبلغی پرداخت کرده؛ رقم پرداخت در ردیف دیده می‌شود اما از مطالبات باز کسر نشده و کل فاکتور همچنان به‌عنوان مطالبات باز نمایش داده می‌شود.»
**ابزار بازتولید:** `_tools/audit/repro-receivables-payment-integration.js` (اجرای مستقل با Node؛ تابع تخصیص سرور پورت‌شده + خواننده‌های واقعی کلاینت)

---

## ۰) خلاصهٔ اجرایی

مشاهدهٔ کارفرما بازتولید شد و **یک باگ نیست؛ نتیجهٔ یک شکاف معماری است**: سیستم امروز **سه مسیر نوشتن پول** و **دو مدل حساب‌کردن مانده** دارد که هیچ‌کدام با دیگری تسویه (reconcile) نمی‌شوند.

- **مسیر نوشتن ۱ (مدل هدف v35):** «دریافت قطعی پرونده» → `post_receipt` روی سرور → تخصیص FIFO → `invoice.allocatedBase/allocatedVat`.
- **مسیر نوشتن ۲ (میراثی):** `savePay` در `crm/rbac.js:1010` → مستقیم `invoice.payments[]` روی مرورگر، بدون فرمان سرور، بدون رسید پرونده.
- **مسیر نوشتن ۳ (چک):** `ptfChequeApplyReceived` در `crm/cheque-module.js:333` → مستقیم `invoice.payments[]`، بدون رسید پرونده و بدون اثر در خزانه/پرونده.

و در سمت خواندن:

- **مدل A — تخصیصی:** مانده = `amount − allocatedBase − allocatedVat` (پنل مطالبات، حساب مشتری، پرونده).
- **مدل B — نقدی:** مانده = Σ(فاکتور بدهکار) − Σ(رسید بستانکار) (گردش حساب مشتری، `cfLedgerRows`).

**هر جا تخصیص انجام نشود، مدل B پول را می‌بیند و مدل A نمی‌بیند** — و کاربر دقیقاً همین را گزارش کرده: «رقم دیده می‌شود ولی کسر نشده».

خروجی بازتولید (پنج سناریوی واقعی، همگی با کد فعلی):

| سناریو | پول واقعی | مطالبات (نمای ۱) | حساب مشتری (نمای ۲) | گردش حساب (۲ب) | پرونده (نمای ۳) | مانده صحیح |
|---|---|---|---|---|---|---|
| ۱ — پرداخت کامل **پیش از** صدور فاکتور | ۱٬۰۹۰م | مانده **۹۰م** | بدهی ۹۰م + اعتبار ۹۰م | **۰** | باز ۹۰م | **۰** |
| ۲ — فاکتور بدون `caseId` | ۵۰۰م | مانده **۱٬۲۰۰م** (وصولی ۰) | بدهی ۱٬۲۰۰م + اعتبار ۵۰۰م | ۷۰۰م | باز **۰** | ۷۰۰م |
| ۳ — پرداخت میراثیِ مهاجرت‌شده، تخصیص نرسیده | ۴۰۰م | مانده **۱٬۱۰۰م** | بدهی ۱٬۱۰۰م، اعتبار ۰ | ۷۰۰م | باز ۱٬۱۰۰م | ۷۰۰م |
| ۴ — فاکتور غیررسمی با تخفیف (`base≠amount`) | ۹۰۰م | مانده ۰ ✔ | ۰ ✔ | ۰ ✔ | باز **۱۰۰م** | ۰ |
| ۵ — صورتحساب غیررسمیِ `superseded` | ۰ | ۱٬۰۹۰م ✔ | بدهی **۲٬۰۹۰م** | ۲٬۰۹۰م | ۱٬۰۹۰م ✔ | ۱٬۰۹۰م |

هیچ‌یک از پنج سناریو «هم‌خوان» نیست. سناریوی ۱ محتمل‌ترین انطباق با گزارش کارفرماست (پول کامل پرداخت شده، فقط بخش ارزش‌افزوده باز مانده)، و سناریوی ۲ محتمل‌ترین انطباق با «کل فاکتور هنوز باز است».

---

## ۱) نقشهٔ وضعیت فعلی

```
                      ┌──────────────── رویداد پول ────────────────┐
   پرونده (v35)              مطالبات (میراثی)            ماژول چک
   ptfReceiptOpen            showPayModal/savePay        ptfChequeApplyReceived
   crm/sales-domain-v2:214   crm/rbac.js:1010            crm/cheque-module.js:333
        │ فرمان سرور               │ فقط localStorage          │ فقط localStorage
        ▼                          ▼                           ▼
  ptf_crm_case_receipts     invoice.payments[]          invoice.payments[]
        │  sd_rebuild_allocations (api/sales-domain.php:179)
        ▼
  invoice.allocatedBase / allocatedVat / openAmountIRR
        │
        ├─► نمای مطالبات      PTF.invPaidSum = Σpayments(فعال، غیرمهاجرت) + allocated   (rbac.js:950)
        ├─► نمای حساب مشتری   bal() با همان فرمول                                       (customer-finance.js:26,77)
        ├─► نمای گردش حساب    فاکتور بدهکار − رسید بستانکار (مدل متفاوت!)                (customer-finance.js:330)
        └─► نمای پرونده       Σ openAmountIRR                                          (sales-domain-v2.js:167)
```

نکتهٔ کلیدی: `PTF.invPaidSum` (`crm/finance-helpers.js:99`) جمعِ **دو دنیای متفاوت** است — پرداخت‌های میراثی + تخصیص‌های v35 — و برای جلوگیری از دوباره‌شماری، پرداخت میراثیِ مهاجرت‌شده را حذف می‌کند. این فرمول فقط وقتی درست است که **تخصیص متناظر واقعاً ساخته شده و به دستگاه رسیده باشد**. هر شکست در تخصیص = پول ناپدید.

---

## ۲) علل ریشه‌ای

### R1 🔴 ارزش‌افزوده هرگز به رسیدهای «پیش از فاکتور» تخصیص نمی‌یابد
`api/sales-domain.php:215` → `$allowVat = (timing === 'post_invoice')`
`timing` هنگام **ثبت رسید** یک‌بار محاسبه می‌شود (`post_receipt`، خط ۸۷۷) و **در `register_invoice` بازمحاسبه نمی‌شود** (خط ۹۲۸ فقط `sd_rebuild_allocations` را صدا می‌زند). بنابراین پیش‌پرداختی که پیش از صدور فاکتور دریافت شده، برای همیشه `pre_invoice` می‌ماند و سقف تخصیصش فقط `base` است.
**اثر:** مشتری کل مبلغ (شامل VAT) را پرداخت می‌کند، اما دقیقاً به اندازهٔ VAT برای همیشه «مطالبهٔ باز» می‌ماند و هم‌زمان همان مبلغ «بستانکاری» نشان داده می‌شود (سناریو ۱).

### R2 🔴 دامنهٔ تخصیص فقط `invoice.caseId` است
`sd_rebuild_allocations` فقط فاکتورهایی را می‌بیند که `caseId` آن‌ها دقیقاً برابر پروندهٔ رسید باشد. اما:
- `crm/unofficial-invoice.js:655` تنها وقتی `caseId` می‌گذارد که پرونده‌ای با `wonOffer === o.no` پیدا شود؛ در غیر این صورت `caseId:''`.
- فاکتورهای میراثی پیش از v35 اگر در `migration_apply_safe` تطبیق یکتا نگرفته باشند، بدون `caseId` مانده‌اند.
**اثر:** رسید ثبت می‌شود، در پرونده و خزانه دیده می‌شود، ولی هیچ‌گاه از فاکتور کسر نمی‌شود (سناریو ۲) — «کل فاکتور همچنان مطالبات باز».

### R3 🔴 سقف تخصیص `base+vat` است ولی همهٔ نماها `amount` را ملاک می‌گیرند
`crm/unofficial-invoice.js:665-667`: `amount = پس از تخفیف/پیش‌پرداخت` ولی `base = مبلغ ناخالص`، `vat = 0`.
سرور روی `base` تخصیص می‌دهد و `openAmountIRR = base+vat−allocated` می‌سازد؛ کلاینت `amount−paid` را نشان می‌دهد.
**اثر دوطرفه:** فاکتور در پنل مطالبات «تسویه» و در پرونده «۱۰۰م باز» (سناریو ۴)؛ و در حالت عکس (اگر `base < amount`) پول تخصیص‌نیافته می‌ماند.

### R4 🔴 سه مسیر نوشتن موازی برای یک رویداد اقتصادی
- `savePay` (`rbac.js:1010`) پرداخت را فقط در `invoice.payments[]` می‌نویسد؛ **هیچ فرمان سروری صادر نمی‌شود**، رسید پرونده ساخته نمی‌شود، خزانه خبردار نمی‌شود. این مسیر هنوز برای هر فاکتور بدون `caseId` فعال است (`rbac.js:983`).
- `ptfChequeApplyReceived` (`cheque-module.js:333`) هم همین کار را می‌کند و علاوه بر آن `ptfChequeReverseReceived` (خط ۴۶۷) پرداخت را **حذف فیزیکی** می‌کند (به‌جای ثبت سند معکوس) → تاریخچه از بین می‌رود.
- `post_receipt` عمداً چک را رد می‌کند (`cheque_requires_collection`)، پس چک هیچ‌وقت وارد دفتر رسیدها نمی‌شود.
**اثر:** پول در بعضی نماها هست و در بعضی نیست؛ و اگر کاربر برای جبران، همان پول را یک‌بار هم در پرونده ثبت کند، **دوباره‌شماری** رخ می‌دهد.

### R5 🟠 دو مدل حسابداری در خواننده‌ها
`cfLedgerRows` (`customer-finance.js:330-375`) رسید پرونده را به‌عنوان بستانکار مستقل ردیف می‌کند و ماندهٔ تجمعی می‌سازد (مدل نقدی)، در حالی که عدد «بدهی/مطالبات باز» همان صفحه از `bal()` (مدل تخصیصی) می‌آید.
**اثر:** در سناریو ۱ ردیف‌های گردش حساب به ۰ می‌رسند ولی تیتر صفحه ۹۰م بدهی نشان می‌دهد — «رقم را می‌بینم ولی کسر نشده».

### R6 🟠 وابستگی نمایش به پروجکشن سرور، بدون تشخیص کهنگی
تخصیص فقط روی سرور محاسبه می‌شود؛ کلاینت آن را از `applyProjection`/`ptfSyncPullNow` می‌گیرد (`sales-domain-v2.js:34-60`). اگر پروجکشن یک کلید شکست بخورد یا دستگاه دیگری sync نشده باشد، `allocatedBase` محلی کهنه می‌ماند و **هیچ نشانه‌ای هم نمایش داده نمی‌شود** (سناریو ۳). هیچ محاسبهٔ محلی fallback و هیچ برچسب «داده کهنه» وجود ندارد.

### R7 🟠 بستانکاری بین فاکتورها/پرونده‌ها جابه‌جا نمی‌شود
`creditRemainIRR` در سطح پرونده می‌ماند؛ اگر فاکتور دیگری از همان مشتری باز باشد، تخصیص خودکار انجام نمی‌شود و کاربر ابزار دستیِ «تخصیص بستانکاری به فاکتور» هم ندارد. نتیجه: هم‌زمان بدهی و اعتبار برای یک مشتری.

### R8 🟠 تعریف «فاکتور فعال» در خواننده‌ها یکسان نیست
- `rbac.js:942` → `void` و **`superseded`** را حذف می‌کند.
- `customer-finance.js:9` (`active`) → فقط `void` را حذف می‌کند.
**اثر:** صورتحساب غیررسمی که با فاکتور رسمی جایگزین شده، در «حساب مشتریان» **دوباره** بدهی می‌سازد — در سناریو ۵ بدهی ۲٬۰۹۰م به‌جای ۱٬۰۹۰م (تقریباً دو برابر).

### R9 🟡 هیچ سنجهٔ تسویه‌ای (reconciliation) وجود ندارد
هیچ‌جا این سه اتحاد بررسی نمی‌شود:
1. `Σ رسیدهای پرونده = Σ تخصیص‌ها + Σ بستانکاری`
2. `invoice.amount = base + vat`
3. `مانده مطالبات (مدل A) = مانده گردش حساب (مدل B)`
به همین دلیل خطا سال‌ها بی‌صدا می‌ماند تا کاربر تصادفی ببیند.

### R10 🟡 حذف/ابطال ناهمگون
`ptfChequeReverseReceived` رکورد پرداخت را حذف می‌کند؛ `ptfInvoicePayVoidPrompt` سند معکوس می‌زند؛ `void_receipt` روی سرور سند ابطال می‌سازد. سه رفتار متفاوت برای یک مفهوم.

---

## ۳) پاسخ مستقیم به گزارش کارفرما

آنچه دیده‌اید ترکیبی از R1/R2 است:
- اگر مبلغ باقی‌مانده **دقیقاً برابر ارزش‌افزودهٔ فاکتور** است → علت R1 است (پیش‌پرداخت پیش از صدور فاکتور، VAT تخصیص نگرفته).
- اگر **کل مبلغ فاکتور** همچنان باز است ولی پول در پرونده/گردش حساب دیده می‌شود → علت R2 است (فاکتور `caseId` ندارد یا رسید روی پروندهٔ دیگری نشسته).
- اگر ردیف پرداخت در «مطالبات» اصلاً دیده نمی‌شود ولی در گردش حساب هست → مسیر نوشتن چک (R4).

هر سه با یک ابزار قابل تشخیص‌اند: **گزارش تسویهٔ فاکتور↔رسید** که در فاز ۰ ساخته می‌شود.

---

## ۴) مدل هدف (Target Model)

یک **دفتر واحد رویداد پول** و یک **موتور تخصیص قطعی**، با سه اصل:

1. **هر ریالِ دریافتی دقیقاً یک رکورد رسید دارد** (`ptf_crm_case_receipts`) — چه از پرونده، چه از پنل مطالبات، چه از وصول چک. `invoice.payments[]` فقط «تاریخچهٔ میراثی» می‌شود و برای هیچ محاسبه‌ای مبنا نیست.
2. **تخصیص، مشتری‌محور است نه پرونده‌محور**: اولویت (۱) همان پرونده، (۲) سایر فاکتورهای باز همان مشتری با تأیید کاربر، (۳) بستانکاری. سقف تخصیص = `amount` قطعی فاکتور (`base+vat` که در ثبت اعتبارسنجی می‌شود).
3. **یک خواننده برای همه**: `PTF.ar.invoiceState(inv)` / `PTF.ar.customerPosition(cd)` که هر سه پنل و همهٔ گزارش‌ها از آن تغذیه می‌شوند؛ خروجی شامل `open`, `paid`, `credit`, `unreconciled`, `stale` است تا هیچ نما فرمول خصوصی نداشته باشد.

---

## ۵) طرح فازبندی‌شدهٔ اصلاح

### فاز ۰ — تشخیص و شفافیت (بدون تغییر داده) — ۱ نسخه
هدف: قبل از هر تغییر، دقیقاً بدانیم چند رکورد و چه مبلغی درگیر است.
1. `PTF.ar.reconcile()` — موتور تسویهٔ فقط‌خواندنی که برای هر فاکتور/مشتری اختلاف مدل A و B را محاسبه می‌کند.
2. تب «تسویهٔ وصولی» در کیفیت داده با شش دستهٔ صریح: VAT تخصیص‌نیافته (R1)، فاکتور بدون پرونده (R2)، `amount≠base+vat` (R3)، پرداخت میراثی بدون رسید (R4)، تخصیص کهنه/نرسیده (R6)، فاکتور superseded شمرده‌شده (R8) — هر ردیف با لینک مستقیم به فاکتور/پرونده.
3. بنر «⚠ این فاکتور در تسویه اختلاف دارد» روی کارت مطالبات و حساب مشتری، با نمایش هر دو عدد.
**پذیرش:** گزارش تسویه روی دادهٔ واقعی تولید شود و پنج سناریوی بازتولید را با همان اعداد علامت بزند. هیچ نوشتنی انجام نشود.

### فاز ۱ — اصلاح موتور تخصیص (سرور) — ۱ نسخه
1. **R1:** `timing` در `sd_rebuild_allocations` **بازمحاسبه** شود (مقایسهٔ تاریخ رسید با تاریخ فاکتورهای فعال همان پرونده) به‌جای اتکا به مقدار منجمدِ زمان ثبت؛ یعنی VAT پس از صدور فاکتور قابل تخصیص شود.
2. **R3:** سقف تخصیص و `openAmountIRR` بر مبنای `amount` قطعی؛ اعتبارسنجی `amount == base + vat` در `register_invoice`/`register_unofficial_invoice` و اصلاح `unofficial-invoice.js` تا `base` را برابر مبلغ خالص بنویسد.
3. **R8:** تعریف واحد `sd_active_for_ar()` (خروج `void` و `superseded`) و هم‌ترازی `active()` در `customer-finance.js`.
4. **R9:** بازگرداندن سه اتحاد تسویه در پاسخ هر فرمان و ثبت `finding` در صورت نقض.
**پذیرش:** سناریوهای ۱، ۴ و ۵ سبز شوند؛ اجرای مجدد تخصیص روی دادهٔ موجود (idempotent) هیچ رقم درستی را خراب نکند.

### فاز ۲ — یکپارچه‌سازی مسیرهای نوشتن — ۲ نسخه
1. **R2:** «الزام پرونده» برای هر فاکتور: هنگام ثبت، اگر پرونده تشخیص داده نشد، ثبت متوقف و کاربر به انتخاب/ساخت پرونده هدایت شود؛ برای رکوردهای موجود، ابزار «اتصال فاکتور به پرونده» با پیشنهاد قطعی و تأیید انسانی.
2. **R4-الف:** `savePay` به فرمان `post_receipt` تبدیل شود (همان فرم، همان دکمه، پشت‌صحنه رسید سروری). مسیر مستقیم نوشتن `payments[]` بسته و پشت یک گارد «فقط بازخوانی» قرار گیرد.
3. **R4-ب:** وصول چک وارده → `post_receipt` با `method='cheque_collected'` و ارجاع `chequeCd`؛ برگشت چک → `void_receipt` (سند معکوس) به‌جای حذف فیزیکی (R10).
4. **R7:** ابزار «تخصیص بستانکاری» برای انتقال اعتبار به فاکتور باز دیگر همان مشتری، با ثبت سند.
**پذیرش:** هیچ مسیر UI باقی نماند که `invoice.payments[]` را مستقیم بنویسد (تست ایستا)؛ سناریو ۲ سبز شود؛ چک وصول‌شده در پرونده، خزانه و مطالبات هم‌زمان دیده شود.

### فاز ۳ — یکپارچه‌سازی خواننده‌ها + مهاجرت داده — ۱ نسخه
1. `PTF.ar.*` تنها منبع مانده؛ حذف فرمول‌های محلی از `rbac.js`، `customer-finance.js`، `sales-domain-v2.js`، `working-capital.js`، `fiscal.js`، `treasury.js` و `metrics-shared.js`.
2. **R5:** گردش حساب هم از همان موتور تغذیه شود: ردیف رسید + ردیف تخصیص، با ستون «تخصیص‌نیافته» تا مدل نقدی و تخصیصی همیشه به یک عدد برسند.
3. **R6:** محاسبهٔ محلی تخصیص به‌عنوان fallback + برچسب «کهنه/در انتظار همگام‌سازی» وقتی `rev` محلی از سرور عقب است.
4. مهاجرت کنترل‌شده: تبدیل باقی‌ماندهٔ `payments[]` واقعی به رسید (idempotent، با گزارش پیش‌نمایش و تأیید مدیر).
**پذیرش:** هر پنج سناریو سبز؛ اجرای `PTF.ar.reconcile()` روی کل دادهٔ واقعی صفر اختلاف بدهد.

### فاز ۴ — گارد و پایش دائمی — ۱ نسخه
1. گیت تسویه در CI (تستر اختصاصی روی سناریوهای بازتولید + دادهٔ نمونه).
2. اعلان خودکار به مالی وقتی `reconcile()` اختلاف جدیدی می‌سازد.
3. مستندسازی قرارداد در `PTF-MASTER-HANDOVER.md` و راهنمای کاربر.

---

## ۶) ریسک و ترتیب اجرا

- فازها **به همین ترتیب** اجرا شوند؛ اجرای فاز ۲ پیش از ۱ باعث می‌شود رسیدهای جدید هم گرفتار همان تخصیص معیوب شوند.
- تمام تغییرات نوشتن، **idempotent** و همراه سند اصلاحی (`ptf_crm_corrections`) باشند تا برگشت‌پذیر بمانند.
- هیچ رقمی «حدس» زده نمی‌شود: اتصال فاکتور به پرونده و مهاجرت پرداخت‌ها فقط با تطبیق یکتا یا تأیید انسانی.
- پیش از فاز ۱ روی داده واقعی، یک backup کامل + اجرای dry-run گزارش فاز ۰ الزامی است.

---

## ۷) خروجی بازتولید (برای پیوست جلسه)

```
node _tools/audit/repro-receivables-payment-integration.js
```
پنج سناریو با اعداد جدول بخش ۰ چاپ می‌شود؛ همین فایل در فازهای بعد به تست پذیرش تبدیل می‌شود.
