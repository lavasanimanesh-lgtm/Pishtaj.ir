# Release Notes — v31.7.42

**تاریخ:** ۱۴۰۵/۰۴/۳۰ (2026-07-21)  
**نسخه:** `v31.7.42`  
**نوع:** 🔴 Hotfix عمومی — BUG-SUP-OTP-001

---

## 🎯 مسئله گزارش‌شده

یک تامین‌کننده گزارش داد در فرم ثبت‌نام تامین‌کنندگان، در مرحله آخر که باید پیامک تایید شماره ارسال شود، پیامک دریافت نمی‌کند و ثبت‌نام عملاً متوقف می‌شود.

---

## 🔍 RCA — ریشه فنی

مسیر ثبت‌نام تامین‌کننده از OTP پیامکی استفاده می‌کند:

```text
supplier/ → ptfOtpMount → api/crm.php?action=otp_send → sms_send → otp_verify → add_supplier
```

مشکل اصلی این بود که اگر `sms-config.php` وجود داشته باشد و `sms_enabled()` true شود، سرور در `add_supplier` حتماً `otp_token` معتبر می‌خواهد. اما اگر ارسال واقعی پیامک به دلیل مشکل provider، خط ارسال، اعتبار پنل، خطای کاوه‌نگار یا اختلال موقت انجام نشود، کاربر هیچ راه عبوری نداشت.

یعنی حالت زیر بن‌بست می‌ساخت:

```text
sms_enabled = true
sms_send = false
کاربر OTP نمی‌گیرد
add_supplier همچنان OTP می‌خواهد
```

همچنین متن فیلد شماره تماس اجازه برداشت اشتباه می‌داد و کاربر می‌توانست شماره ثابت وارد کند، در حالی‌که OTP فقط برای موبایل 09 معتبر است.

---

## ✅ اصلاح انجام‌شده

### 1) Degraded OTP fallback سمت سرور

در `api/crm.php`، در مسیر `otp_send` اگر `sms_send` fail شود، سیستم دیگر کاربر را بن‌بست نمی‌کند.

رفتار جدید:

- کپچا و rate-limit همچنان الزامی است.
- اگر SMS ارسال نشد، سرور یک `otp_token` امضاشده برای همان شماره صادر می‌کند.
- پاسخ با `degraded: true` برمی‌گردد.
- ثبت‌نام ادامه می‌یابد، اما پیام به کاربر می‌گوید شماره توسط تیم بازرگانی تلفنی بررسی می‌شود.

این رفتار فقط زمانی رخ می‌دهد که ارسال واقعی SMS fail شود؛ در حالت عادی همچنان OTP واقعی ارسال و verify می‌شود.

---

### 2) پشتیبانی degraded در `ptf-guard.js`

در `assets/js/ptf-guard.js`، اگر پاسخ `otp_send` دارای `degraded: true` و `otp_token` باشد:

- OTP به‌صورت verified علامت می‌خورد.
- token ذخیره می‌شود.
- فرم تامین‌کننده می‌تواند همان token را به `add_supplier` ارسال کند.
- پیام کاربر از حالت خطا به حالت «ادامه با بررسی تلفنی» تبدیل می‌شود.

---

### 3) اصلاح UX شماره موبایل در فرم تامین‌کننده

در `supplier/index.html` متن فیلد اصلاح شد:

```text
شماره موبایل واتساپ برای تایید پیامکی
```

placeholder:

```text
09xxxxxxxxx
```

و راهنما اضافه شد:

```text
برای دریافت رمز تایید فقط شماره موبایل با 09 وارد کنید؛ تلفن ثابت را در توضیحات بنویسید.
```

---

## 📁 فایل‌های تغییر یافته

```text
api/crm.php
assets/js/ptf-guard.js
supplier/index.html
crm/index.html
crm/sw.js
crm/clear-cache.html
PTF-MASTER-HANDOVER.md
_tools/uat/tester219-supplier-otp-degraded.js
RELEASE-NOTES-v31.7.42.md
REGRESSION-REPORT-v31.7.42.md
```

---

## 🧪 تست اضافه‌شده

### `_tools/uat/tester219-supplier-otp-degraded.js`

پوشش:

- وجود `BUG-SUP-OTP-001` در API.
- در صورت failure ارسال پیامک، سرور `degraded + otp_token` می‌دهد.
- `add_supplier` همچنان توکن معتبر می‌خواهد.
- `ptf-guard` پاسخ degraded را verified می‌کند.
- token در submit تامین‌کننده ارسال می‌شود.
- فیلد تامین‌کننده صریحاً موبایل 09 می‌خواهد.

نتیجه مستقیم:

```text
tester219-supplier-otp-degraded: 9 PASS / 0 FAIL
```

---

## ⚠️ Impact Analysis

| بخش | اثر | ریسک |
|---|---|---|
| ثبت‌نام تامین‌کننده | دیگر در اختلال SMS بن‌بست نمی‌شود | کم/متوسط |
| امنیت public form | کپچا و rate-limit حفظ شده؛ OTP degraded فقط برای availability است | متوسط کنترل‌شده |
| SMS واقعی | در حالت سالم همچنان OTP واقعی کار می‌کند | کم |
| RFQ/CRM/Auth/Finance/Sync | دست‌نخورده | صفر |

---

## 📋 راستی‌آزمایی کارفرما

1. وارد صفحه `/supplier/` شوید.
2. شماره موبایل با فرمت `09xxxxxxxxx` وارد کنید.
3. کپچا را تکمیل کنید.
4. روی «ارسال رمز تایید» بزنید.
5. اگر پیامک سالم باشد، رمز باید بیاید و verify شود.
6. اگر سامانه SMS مشکل داشته باشد، فرم نباید بن‌بست شود و باید پیام ادامه ثبت‌نام با بررسی تلفنی نمایش داده شود.
7. پس از ثبت، کد `PTF-VEN-...` باید نمایش داده شود.

---

## نکته عملیاتی

اگر SMS به‌صورت واقعی ارسال نمی‌شود، ادمین سرور باید بعداً تنظیمات زیر را بررسی کند:

```text
sms-config.php
api_key
line / sender
اعتبار پنل
وضعیت تایید خط ارسال
دسترسی هاست به api.kavenegar.com
```

اما تا زمان رفع پنل پیامک، ثبت‌نام تامین‌کننده متوقف نمی‌ماند.
