# یادداشت انتشار CRM v34.8.18

تاریخ: ۱۴۰۵/۰۶/۰۵ (۲۰۲۶-۰۸-۲۷)

## رفع باگ کارتابل (گزارش کارفرما): «تکرار کارت قطع شد ولی هر چند ثانیه دینگ صدا می‌دهد و پیام «🔄 N بخش از دستگاه دیگر به‌روز شد» می‌آید»

### ریشه
پس از v34.8.17، وقتی state ضدتکرار (notifiedUsers) گم‌شده دیده می‌شد:
- سپر dkey کارت دوم نمی‌ساخت، ولی کد همچنان `added=true` برمی‌گرداند → **دینگ هر تیک poll**
- و دوباره `entity_upsert` می‌فرستاد → rev هر بار بالا می‌رفت → **toast «N بخش به‌روز شد» در هر pull**

### رفع — «حضور کارت زنده = خودِ state»
- قبل از هر تیک، کارت‌های زندهٔ یادآور (نه done) اسکن می‌شوند؛ اگر برای (یادآور، گیرنده) کارت هست، فقط `notifiedUsers` **ساکت** بازسازی می‌شود — بدون دینگ و بدون فرمان
- دینگ فقط وقتی کارت **واقعاً جدیدی** ساخته شود
- گارد in-flight: تا ACK فرمان قبلی، فرمان جدیدی نمی‌رود (پایان طوفان فرمان در تیک‌های فشرده)
- حالت چند-گیرنده: کارت هر گیرنده مستقل چک می‌شود (dkey بر اساس cd:user)

### کنترل
tester522 رفتاری (۴ سناریو: state گم + کارت موجود = سکوت کامل؛ یادآور تازه = یک دینگ؛ چند گیرنده؛ ۶ تیک فشرده با ACK کند ≤ ۲ فرمان) — ۲۲/۰. گیت کامل ۱۳۶/۰.
