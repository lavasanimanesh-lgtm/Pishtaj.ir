# یادداشت انتشار v34.39.5 — HOME-ICONMIN (حذف جستجو از شیت، کلید شب/روزِ فقط‌آیکون، فیکسِ بازگشتِ تمِ دستی)

**تاریخ:** 2026-09-17
**بازوی تغییر:** Arena Agent
**کانال:** سایت عمومی — صفحهٔ اول (لایهٔ نمایشی/رفتاری v34.39.5؛ محتوا/سئو/فرم‌ها بدون تغییر)
**قرارداد:** tester667 (۲۱ سنجه) + بازتنظیم پین‌ها با ابزار رسمی `bump-version-pins.js`

## گزارش کارفرما
> «جستجو از قسمت منو حذف شود. در نمای موبایل کادر دور نمای روز و شب حذف شود، فقط یک آیکون شود. وقتی نمای روز را در شب فعال می‌کنیم بعد از چند ثانیه دوباره برمی‌گردد به نمای شب. دکمه تماس و واتس‌اپ به شماره‌های واقعی لینک نیستند.» ← شماره‌ها با پرسشِ مستقیم تأیید شد: `021-46087679` و واتساپ `09925868479` همان canonical‌های کل سایت (هدر/داک/فوتر/JSON-LD) هستند؛ بی‌عملقوییِ apparent در پیش‌نمایش به‌خاطر بلاکِ پروتکل `tel:`/`wa.me` در iframe شنبلدی است و روی دامنه واقعی کار می‌کند.

## آنچه انجام شد
1. **حذف «جستجو» از منوی موبایل:** پیوندِ جستجو که `ptf-discover.js` به `#mainNav` اضافه می‌کند، در شیت موبایل با `#mainNav .nav-search{display:none!important}` حذف نمایشی شد — DOM و مسیر `search/` برای دسکتاپ دست‌نخورده؛ کادر جست‌وجوی تخصصیِ هیرو (Finder) که مرجع اصلی یافتن تجهیز است، سر جایش پرقدرت‌تر از قبل کار می‌کند.
2. **کلید شب/روزِ فقط‌آیکون در شیت:** قاب (border/پس‌زمینه/سایه) و برچسبِ متنیِ «نمای شب/روز» در موبایل حذف شد؛ آیکونِ ۴۲px گوشهٔ چپ‌بالای شیت نشست — با هایلایت لمسیِ کهربایی در هر دو حالت و بدون ترنزیشن در reduce-motion. در دسکتاپ همان قرصِ جمع‌وبرچسبِ برچسب‌دار انتهای نوبار باقی است.
3. **فیکسِ بازگشتِ تم (باگ واقعیِ شناخته‌شده):** بازبینیِ خودکارِ هر-دقیقه (ساعت غروب) تنها به کوکی `ptf_theme` نگاه می‌کرد؛ در محیط‌هایی که کوکیِ third-party مسدود است (iframe پیش‌نمایش با Safari/Chrome سخت‌گیر) `set()` بی‌سروصدا شکست می‌خورد و ۶۰ ثانیه بعد صفحه به شبِ خورشیدی برمی‌گشت. اکنون **اوررایدِ دستی در حافظه هم قفل می‌شود** (`memManual`): تا همان سشن، هیچ بازبینیِ خودکاری انتخابِ کاربر را له نمی‌کند؛ نوشتنِ کوکی هم best-effort ادامه دارد تا روی دامنهٔ واقعی، انتخاب بین رفرش‌ها هم بماند. (محدودیتِ آگاهانهٔ پیش‌نمایش: بعد از رفرشِ کامل در iframeِ بی‌کوکی، حالت خودکارِ خورشیدی حاکم می‌شود.)
4. **بودجهٔ سرعت:** افزودن‌ها فایل را از سقفِ ۴۰٬۰۰۰نویسه‌ایِ گارد tester662 رد کرد؛ با layout-minifyِ ایمن (الحاق خطوطِ ادامه + حروفِ اضافیِ انتهای قواعد، بدون حذف حتی یک قانون؛ مارکرهای هر شش لایه راستی‌آزمایی شد) به `home.css = ۳۹٬۷۹۵` و `ptf-motion.js = ۱۱٬۶۴۴` برگشت.

## فایل‌های تغییر‌یافته
`index.html` (cache-bust → v=34.39.5) · `assets/css/home.css` (لایهٔ v34.39.5 + فشرده‌سازیِ layout) · `assets/js/ptf-motion.js` (memManual) · `VERSION.json` و ۸ نقطهٔ رسمی نسخه · `RELEASE-NOTES-v34.39.5.md` · tester667.

## وضعیت دروازه
tester667: ۲۱/۲۱ ✓ · tester666: ۲۳/۲۳ ✓ · tester665: ۲۵/۲۵ ✓ · tester664: ۲۹/۲۹ ✓ · tester663: ۳۰/۳۰ ✓ · tester662: ۳۶/۳۶ ✓ · گیت کامل CI: **۲۷۹ PASS / ۱ FAIL** (شکستِ محیطیِ شناخته‌شدهٔ tester656) · arch-guard: PASS (A6 هم‌تراز، A10 بدون localStorage).
