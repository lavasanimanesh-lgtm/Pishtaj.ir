# یادداشت انتشار — v34.38.1

**تاریخ:** ۲۰۲۶-۰۹-۰۷ (۱۴۰۵/۰۶/۱۶) · **پایه:** v34.38.0
**گیت CI:** tester606 (قفل BUG-OFFER-TOCO-IDENTITY + COLD-BOOT-HYDRATE + LIST-RENDER-N1) + سوئیت کامل گیت

---

## ① رفع خطای «رکورد اصلی پیشنهاد پیدا نشد» در تبدیل TO→CO (BUG-OFFER-TOCO-IDENTITY)

- **ریشه:** کلون پیشنهاد فنی در `offerToCo` هویت ورود فرم سند مبدأ (`editMode` ماندگار `direct/revision`) را به ارث می‌برد؛ `ptfOfferResolveSaveIdentity` شمارهٔ CO تازه را به‌عنوان «رکورد ویرایش» می‌جست (`idx=-1`) و نگهبان `offerSave` ثبت را با «رکورد اصلی پیشنهاد پیدا نشد» متوقف می‌کرد. به همین دلیل تبدیل فقط وقتی می‌مرد که رکورد TO قبلاً ویرایش و ذخیره شده بود.
- **اصلاح (`crm/offers.js`):** ① `offerToCo` هویت را صریح `new` می‌کند و نشانگرهای رکورد-مبدأ را پاک می‌کند؛ ② `offerNew` هنگام بازیابی پیش‌نویس، هویت احتمالاً مسموم دورهٔ قبل را پاک می‌کند؛ ③ `offerSave` دیگر `editMode` را روی رکورد ذخیره‌شده نگه نمی‌دارد (هویت ورود فرم فقط متعلق به state است). همهٔ مسیرهای ورود به فرم آن را صریح ست می‌کنند، پس رفتار ویرایش مستقیم/نگارش جدید دست‌نخورده ماند.

## ② پایان دانلود کامل چندمگابایتی در هر رفرش (COLD-BOOT-HYDRATE)

- **ریشه:** کلیدهای کسب‌وکاری offloadشده (پیشنهادها/درخواست‌ها/پرونده‌ها/تأمین/…) پس از رفرش نه در `localStorage` بودند (حذف شده) نه در حافظهٔ نشست؛ پنل‌ها `[]` می‌دیدند و اولین pull مجبور بود کل دیتاست چندمگابایتی را کامل از سرور دانلود کند — در **هر** رفرش.
- **اصلاح:** ① `ptfBIdbPreload` در `crm/client-server.js` حالا همهٔ کلیدهای `bdata:` در IndexedDB را شمارش و آب‌رسانی می‌کند (نه فقط فهرست ثابت ۱۰تایی) و آب‌رسانی خالص IDB هم رندر قطعی `__cold_boot__` می‌گیرد؛ ② اولین pull هر بوت در `crm/sync.js` حداکثر ۲ ثانیه منتظر آب‌رسانی می‌ماند تا به‌جای دانلود کامل، پاسخ fresh/دلتا بگیرد. هر دو مسیر fail-open هستند (خرابی IDB هرگز pull/رندر را متوقف نمی‌کند) و گیت فقط یک‌بار در هر بوت اعمال می‌شود.

## ③ حذف parse تکراری مشتریان در رندر فهرست‌ها (LIST-RENDER-N1)

- **ریشه:** حل‌گرهای نام مشتری (`ptfCustNamePair` ← FaByCd/EnByCd/FaByEn) در هر ردیف فهرست‌ها صدا زده می‌شدند و هر کدام کل کالکشن مشتریان را از نو parse می‌کردند (۲ تا ۳ parse کامل به‌ازای هر ردیف).
- **اصلاح:** کش کوتاه‌مدت ۲ثانیه‌ای در `crm/index.html` که همان آرایه را در طول یک رندر همگام بازمی‌گرداند؛ هر نوشتن روی `ptf_crm_customers` — مسیر legacy، مسیر فاز B و اعمال pull — آن را باطل می‌کند. فقط «نام نمایشی» از این کش می‌آید؛ ردیف‌ها و شمارها همیشه از `getData` تازه خوانده می‌شوند.

---

## ④ فایل‌های تغییر یافته

- `crm/offers.js`: بهداشت هویت TO→CO + بازیابی پیش‌نویس + ذخیره (BUG-OFFER-TOCO-IDENTITY).
- `crm/client-server.js`: شمارش/آب‌رسانی همهٔ `bdata:` + سیگنال `ptfBWhenHydrated` + رندر قطعی + ابطال کش نام مشتری.
- `crm/sync.js`: گیت اولین pull بوت روی آب‌رسانی آینه + ابطال کش نام مشتری (wrapper و `wr`).
- `crm/index.html`: کش کوتاه‌مدت نام مشتری + ارتقای هماهنگ نسخه.
- `_tools/uat/tester606-v34.38.1-offer-toco-identity-and-cold-boot-hydrate.js`: تستر قفل (جدید) + ثبت در `run-ci-gate.js`.
- `VERSION.json`, `crm/sw.js`, `crm/manifest.json`, `crm/clear-cache.html`, `crm/shell.js`, `crm/cms.js`, `api/sales-domain.php`, `supplier/index.html`: ارتقای هماهنگ نسخه به `v34.38.1` (+ پین نسخهٔ تسترها).
