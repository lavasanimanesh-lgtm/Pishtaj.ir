# داوری فنی حادثهٔ ورود/اتصال CRM — مرزِ آنچه «بازگشت بوت» (v34.38.15) حل می‌کند و آنچه نمی‌کند

> تاریخ: ۲۰۲۶-۰۹-۰۹ · مبنا: کدِ همین مخزن (HEAD `916d8d8` = v34.38.15) · روش: بازبینی کدِ استاتیک، نه دسترسی به سرور/دادهٔ واقعی.
> نتیجهٔ صریح: با شواهد فعلی، **نه‌تنها نمی‌توانم بگویم اصلاح بوت همهٔ مشکلات را حل می‌کند؛ بلکه در کد یک مانع باقی‌مانده هست که حتی بعد از این بازگشت، «دریافت کامل اولیه» را می‌تواند متوقف نگه دارد.** در ادامه همین مانع و مسیر رفع آن آمده است. «اطمینان از سالم‌ماندن دادهٔ سرور» با شواهد موجود قابل اثبات نیست و فقط از مسیر فقط-خواندنی قابل تأیید است (بخش ۸).

---

## ۱) خلاصهٔ یک‌خطی برای تصمیم‌گیر

- **اولین تغییری که سبب زنجیرهٔ مشکلات ورود شد، v34.38.10 (ATOMIC-SNAPSHOT-PULL) است** — افزودن فراخوانیِ دریافت اسنپ‌شات اتمیک (`data_manifest` + `data_chunk`) به مسیر بوت/ورود که fail-closed بود.
- **بازگشت به حالتِ قبل، همین الان در مخزن وجود دارد:** v34.38.15 با خاموش‌کردن پرچم `PTF_CRM_SNAPSHOT_V2=false` بوت را به همان `data_pull` تک‌مرحله‌ایِ قبل از v34.38.10 برگردانده است. پس «کدِ بازگشت» را لازم نیست از صفر بنویسم — باید **روی هر دو محیط و هر دستگاه** مستقر و با رفرش اجباری (سپر نسخه) روی باندلِ واقعاً v34.38.15 بیاید.
- **اما بازگشتِ بوت به‌تنهایی کافی نیست.** در `sync.js` همان اعتبارسنج یکپارچگیِ قرارداد v2 (`ptfValidatePullPayload`) که در v34.38.10–11 ساخته شد، روی **مسیر legacy تک‌مرحله‌ای** هم اجرا می‌شود (سطر ۱۷۸۴). اگر سرور برای کلید `ptf_crm_commission_records` در response خود entry کاملِ integrity نداشته باشد، کلاینت دقیقاً همان خطای گزارش‌شدهٔ شما را می‌دهد:
  `ptf_crm_commission_records:integrity-manifest-missing`
  و **هیچ dataای اعمال نمی‌شود و cursor جلو نمی‌رود** → نشانگر سبز نمی‌شود و «دریافت کامل اولیه هنوز تکمیل نشده» باقی می‌ماند.
- نتیجه: **اصلاح بوت شرط لازم است، اما برای «همهٔ مشکلات» و «برگشت کامل به حالت قبلی» کافی نیست.** چند مشکل دیگر (بخش ۵) به آن گره نخورده‌اند.

---

## ۲) فهرستِ ~۱۵ تغییرِ اخیرِ CRM (بازسازی از مارکرهای داخل کد؛ چون تاریخچهٔ git تک‌کامیت squashed است)

توالی انتشار در برچسب v34.38.0 → v34.38.15 (مهم‌ترین‌ها):

| نسخه | عنوانِ داخل کد | محتوا / چرا مهم است |
|---|---|---|
| .0 | EVENT-DEDUP-TS | زمان‌بندی dedup رویدادها |
| .1 | LIST-RENDER-N1 / COLD-BOOT-HYDRATE / BUG-OFFER-TOCO-IDENTITY | کشِ کوتاه‌مدت نام مشتری برای رندر؛ هیدرات آینهٔ IDB قبل از اولین pull |
| .2 | COST-RESURRECTION / COST-SUM | بازیابی هزینهٔ پرونده‌ها هنگام pull |
| .3 | PRJ-COST-RESURRECTION | همان در گزارش مالی/پرونده‌ها |
| .4 | CONTACT-WIPE-EXT | (دامنهٔ پاک‌شدن فیلد تماس مشتری — جدا از بوت) |
| .5 | DATA-QUALITY SH-SALARY | کیفیت داده |
| .6 | CONTACT-WIPE R2/R3 · DEAL-DUE-SETTLED · OPEX-DUP-GUARD | ادامهٔ دامنهٔ تماس + دوگانگی opex |
| .7 | CONTACT-GHOST | (دامنهٔ تماس/شماره‌های مشتری) |
| .9 | BACKUP-BLIND-SPOT / CONTACT-RECOVERY | بازیابی خودکار تماس‌ها از بک‌آپ‌ها (فقط فیلد خالی) |
| **.10** | **ATOMIC-SNAPSHOT-PULL** + SYNC-SNAPSHOT-INTEGRITY | **← ریشهٔ زنجیرهٔ ورود.** pull اتمیک chunkی (`data_manifest`+`data_chunk`) به بوت اضافه شد؛ `snapshot-v2` + اعتبارسنج integrity با قوانین سخت‌گیرانه |fail-closed|. همچنین دکمهٔ «بازیابی تماس‌ها» به فهرست مشتری اضافه شد. |
| .11 | ATOMIC-PULL-COMPLETION · NO-DEAD-BOOTSTRAP · BOOT-NOTICE-SANITY | رفعِ «دریافت کامل انجام نشد» در مسیر اتمیک: حذف مقایسهٔ canonical hash؛ تحمل کلید ناخوانا؛ fallback بعد از ۲ شکست به همان pull دلتای معتبر |
| .12 | MASS-DELETE-SHIELD · PRE-BOOTSTRAP-PUSH | سپرِ حذف انبوه کلاینت/سرور (ریشهٔ حادثهٔ حذف ۶۸→۱ رکورد) |
| .13 | RECORD-RECOVERY | ابزار بازیابی رکورد |
| .14 | CASE-UNIQUENESS · ARCHIVE-ATOMIC · RESTORE | رفعِ یکتایی/بازیابی پروندهٔ بایگانی |
| **.15** | **BOOT-REVERT** | **بازگشت بوت به قبل از .10:** `PTF_CRM_SNAPSHOT_V2=false`؛ بوت از همان `data_pull` تک‌مرحله‌ای می‌آید. سپرهای .12/.13/.14 دست‌نخورده ماندند. |

> نکته: «آخرین ۱۰ تغییر» عملاً همین بازهٔ v34.38.x است که مستقیماً به حادثه گره خورده؛ بازه‌های پیشین (v34.37، v34.29 و…) تغییرات ناوبری/مالی/موبایل جداگانه‌اند.

---

## ۳) تغییرِ ریشه و این که «بازگشت» الان کجاست

**ریشه (در کد):**
- `crm/index.html:2285` → `window.PTF_CRM_SNAPSHOT_V2 = false` (در v34.38.10 این پرچم true و مسیر اتمیک فعال بود).
- تنها نقطهٔ فراخوانیِ مسیر اتمیک: `crm/sync.js:1692-1693` که شرطش `forceFull && window.PTF_CRM_SNAPSHOT_V2 === true && …` است.

**بازگشت (قبلاً انجام شده در همین HEAD):**
- کامیت `916d8d8` = «Merge PR #24 → v34.38.15 BOOT-REVERT». توضیح کامل در بلوک کامنت `crm/index.html:2286-2293`.
- یعنی «برگرداندن سیستم به حالت قبل از v34.38.10» **در کد این مخزن حاضر است و فقط نیاز به استقرار/رفرش دارد**، نه اینکه دوباره نوشته شود.

**پس از بازگشت، بوت چه می‌کند:** `sync.js:1692` از مسیر اتمیک رد می‌شود و به مسیر legacy می‌رود که در `sync.js:~1700-1710` با `?action=data_pull&since=0` (برای بوت/forceFull) اسنپ‌شات کامل می‌گیرد — دقیقاً همان رفتاری که قبل از v34.38.10 بود.

---

## ۴) یافتهٔ کلیدی: مانعِ باقی‌مانده که حتی بعد از بازگشتِ بوت pull را متوقف می‌کند

حتی وقتی پرچم اتمیک خاموش است و بوت از `data_pull` تک‌مرحله‌ای می‌آید، پاسخ با همین اعتبارسنج سخت‌گیرانهٔ v2 بررسی می‌شود:

- `crm/sync.js:1784` در مسیر legacy: `var pullIntegrity = ptfValidatePullPayload(d);` و اگر `!pullIntegrity.ok` → **هیچ projection نوشته نمی‌شود، cursor جلو نمی‌رود**، و `finishPull({ok:false, reason:'integrity'})` برمی‌گردد. (کاملاً منطبق با پیام «دریافت کامل اولیه هنوز تکمیل نشده».)
- داخل `ptfValidatePullPayload`، `crm/sync.js:1423`: اگر `d.contract === 'ptf-sync-v2'` باشد و برای کلیدِ `k` در `snapshot.keys[k]` فیلدِ `sha256/bytes/count` سالم نباشد ⇒ `k + ':integrity-manifest-missing'`.
- کلید گزارش‌شدهٔ شما (`ptf_crm_commission_records`) در لیست کلیدهای سینک سرور هست (`api/crm.php:362,373`).

**پس:** خطای `ptf_crm_commission_records:integrity-manifest-missing` **اثباتِ اجرای مسیر اتمیک نیست** (قبلاً در تحلیل قبلی این‌طور تفسیر شده بود). این خطا از اعتبارسنج مشترک می‌آید که روی **هر دو** مسیر (اتمیک و legacy) اجرا می‌شود. یعنی صرفِ بازگشت بوت به legacy، این توقف را برطرف نمی‌کند مگر اینکه سرور برای آن کلید، entry درستِ manifest بفرستد (یا کلاینت آن کلیدِ ناخوانا را به‌جای fail-کل-snapshot «unavailable» بگیرد و بقیه را اعمال کند — دقیقاً الگویی که v34.38.11 برای مسیر اتمیک در نظر گرفته بود ولی در اعتبارسنج مشترکِ مسیر legacy جا افتاده).

**نکتهٔ داده‌ای (طبق یافتهٔ قبلی و بدون حدس):** باید تأیید شود مقدار واقعیِ ذخیره‌شدهٔ سرور برای `ptf_crm_commission_records` آیا اسکالر/`null`/شکل غیر-قابل-شمارش است که سمت سرور entry درستِ stats نمی‌سازد، یا پاسخ در میانه بریده/ناقص است. این فقط با شواهد فقط-خواندنی از سرور تأیید می‌شود (بخش ۸).

---

## ۵) نگاشتِ هر علامت به ریشه و محلِ کد — کدام «با بازگشت بوت» حل می‌شود و کدام نه

| علامتِ گزارش‌شده | ریشهٔ محتمل در کد | حل با بازگشتِ بوت؟ |
|---|---|---|
| نشانگرِ هیچ کاربری سبز نمی‌شود / اتصال برقرار نمی‌شود | pull (بوت یا بعدی) fail می‌شود؛ bootstrap کامل نمی‌شود. پس از .15 هم اگر گیت v2 روی `commission_records` fail شود، باز هم fail می‌ماند | **جزئی** — لازم است ولی کافی نیست (بخش ۴) |
| «دریافت کامل اولیه هنوز تکمیل نشده (تلاش N)» | `sync.js:631-632`؛ همان حالت bootstrap ناتمام | جزئی (مثل بالا) |
| پنل‌های خالی: سرنخ، پرونده، تأمین‌کننده، موجودی انبار، بایگانی، مطالبات، هاب مالی | آینه/کشِ محلی هرگز از سرور آب‌رسانی (hydrate) کامل نشده چون bootstrap گیر کرده؛ و برای پرونده/فاکتور، تفکیک «پنل» در برابر «کالکشن» (ردیف‌های پنل فاکتور از پیشنهادها ساخته می‌شوند و سپس به پرونده/فاکتور/فایل وصل می‌شوند) | با رفع pull (نه فقط بازگشت) |
| «پرونده فروش یافت نشد» ولی شماره‌ها هست + فایل‌های ضمیمه‌شده‌ٔ فاکتور نیستند | این پیام یعنی شکستِ اتصالِ ردیفِ پنل به پرونده/فایل، **نه لزوماً حذفِ فایل از فضای ذخیره‌سازی**؛ و چون pull کامل نشده، رکوردها/کلیدهای ابری در کش نیستند | با رفع pull؛ تأییدِ «فایل حذف شده یا فقط نامرئی» نیاز به شواهد فقط-خواندنی دارد |
| دکمهٔ «ثبت پیشنهاد» فقط می‌گوید اول همگام‌سازی قبلی کامل شود | `sales-domain-v2.js:1014-1019`؛ مسیر جدید Sales-to-Cash فقط وقتی ذخیره می‌کند که در صفِ dirty کلیدِ بلوکه‌کننده نباشد. `OFFER_COMMAND_KEYS` شامل `ptf_crm_notifs` است (سطر ۸۸۳) و `ptfSyncPendingKeys` کلیدهای `state.dirty` را می‌دهد (`sync.js:246`). صفِ dirty شما (`ptf_crm_notifs` و…) دقیقاً همین گارد را فعال می‌کند | خیر — این گارد عمدی است و تا وقتی pull/ارسال گیر کرده، تکرارِ دکمه بی‌اثر است |
| 3 کلید در صف ارسال (dirty) و صف آفلاین فاز B | `ptf_crm_audit/notifs/customers` در حالت dirty/آفلاین گیر کرده‌اند و چون pull/ارسال موفق نمی‌شود هرگز flush نمی‌شوند | نه با بازگشت؛ با رفع pull/ارسال |
| پاسخ 403ِ ابزارها HTML است (نه JSON خطای برنامه) | لایهٔ وب‌سرور: `api/.htaccess:9` → `RewriteRule ^(?:tech-proposal-docx|tools)\.php$ - [F,L,NC]` (deny-by-default). یعنی خودِ وب‌سرور `tools.php` را 403 می‌کند؛ با کدِ احراز هویتِ برنامه ارتباطی ندارد و در .15/.16 هم تغییر نکرده است | خیر — این فایلِ واقعیِ هاست باید با این قانون تطبیق داده شود، نه کورکورانه برداشتنِ محدودیت API |
| یک کاربر «نام کاربری یا رمز نادرست» | در جریان login جدا از sync | خیر — باید جدا بررسی شود |
| حذفِ تماسِ مشتریِ تازه‌ثبت‌شده | دامنهٔ جداگانهٔ CONTACT-WIPE (v34.38.4/6/7) و بازیابی (v34.38.9/.10)؛ به بازگشتِ بوت گره نخورده | خیر — جدا |

---

## ۶) جمع‌بندیِ «آیا با این اصلاحات همه چیز برمی‌گردد به حالت قبلی؟»

**پاسخ صادقانه: خیر — نه با بازگشتِ بوت به‌تنهایی، و نه «باقطعیت» حتی با افزودنِ مقاوم‌سازی یکپارچگی.** سه دستهٔ مستقل داریم:

1. **دستهٔ گره‌خورده به بوت/دریافت** (نشانگر، پنل‌های خالی، پیام دریافت ناتمام): پیش‌نیازش استقرارِ v34.38.15 روی همهٔ محیط/دستگاه + رفعِ مانعِ باقی‌ماندهٔ `commission_records` (سرور: ارسالِ manifest درست؛ یا کلاینت: کلید ناخوانا → unavailable به‌جای fail-کل).
2. **دستهٔ گارد عمدیِ ذخیره/صف** (ثبت پیشنهاد، صف‌های dirty/آفلاین): بعد از سبزشدن pull/ارسال، خودبه‌خود آزاد می‌شود؛ با تکرارِ دکمه یا پاک‌کردن صف حل نمی‌شود.
3. **دسته‌های کاملاً مستقل**: لایهٔ وب‌سرورِ ابزارها (403)، یک کاربرِ «رمز/نام نادرست»، و دامنهٔ پاک‌شدن تماسِ مشتری.

هر ادعایی که «همهٔ مشکلات حل می‌شود» یا «دادهٔ حذف‌شده برمی‌گردد» بگوید، فراتر از شواهدِ فعلی است.

---

## ۷) سلامت داده روی سرور — چه چیزی هنوز قابل اثبات نیست

- «وجود توکن» اعتبارِ داده را اثبات نمی‌کند.
- «خطای ثبتِ محلی نداریم» هم نشانهٔ سلامتِ دادهٔ سرور نیست.
- پنل‌های خالی و «فایل نیست» با **دریافتِ ناقص + تفکیک پنل/کالکشن** توضیح داده می‌شوند و **الزاماً به‌معنای حذف در سرور نیستند**. اما این‌هم احتمالِ حذف را رد نمی‌کند؛ فقط می‌گوید هنوز دلیلی برای «حذف سروری» نداریم.
- «اطمینان از سالم‌ماندن داده» فقط با شواهد فقط-خواندنی از یک کپیِ مستقل/منجمد تأیید می‌شود.

---

## ۸) اقدام توصیه‌شده (به‌ترتیب اولویت، همه‌شان فقط-خواندنی/غیرمخرب)

**موارد ممنوع تا اطلاع ثانوی:** پاک‌کردن صف‌ها، Force Push، رفرشِ اجباری مکرر، و هر آزمایشِ ثبت/بازیابی/حذف روی تولید. تکرارِ دکمهٔ «ثبت پیشنهاد» علت را برطرف نمی‌کند.

1. **تأیید وضعیت استقرار:** روی هر دو محیط اطمینان حاصل کنید `manifest.json.version == 34.38.15` و باندلِ در حال اجرای مرورگر واقعاً v34.38.15 باشد (سپر نسخه باید رفرشِ اجباری کند). اگر دستگاهی هنوز باندلِ قدیمی (.10–.14) اجرا می‌کند، رفتارِ قدیمی همان «گیرکردنِ دریافت» را نشان می‌دهد.
2. **تأیید فقط-خواندنیِ گرهٔ واقعی pull:** در Console همان تبِ مشکل‌دار فقط این را اجرا کنید (می‌خواند، ارسال/پاک/تغییر صف نمی‌کند) و خلاصه را بفرستید:
```js
(() => {
  let e = null; try { e = JSON.parse(localStorage.getItem('ptf_sync_last_error') || 'null'); } catch (_) {}
  const flag = x => typeof x === 'boolean' ? x : null;
  console.log(JSON.stringify({
    site: location.origin,
    rel: (window.PTF_CRM_RELEASE || window.VER),
    snapshotV2: flag(window.PTF_CRM_SNAPSHOT_V2),
    bootstrapped: flag(window._ptfSyncBootstrapped),
    mirrorHydrated: flag(window.ptfBIdbHydrated),
    lastError: e && { time: e.t, scope: e.scope, status: e.status, reason: e.reason }
  }, null, 2));
})();
```
   اگر `lastError.reason` همان `ptf_crm_commission_records:integrity-manifest-missing` باشد، ریشهٔ باقی‌مانده تأیید می‌شود و تصمیم فقط بینِ «اصلاح سرور» یا «مقاوم‌سازی کلاینت» است.
3. **شاهد فقط-خواندنی برای کلید پورسانت (بدون دادهٔ مالی):** فقط مشخصاتِ کلید `ptf_crm_commission_records` (شکلِ مقدار ذخیره‌شده: آرایه/اسکالر/`null`) را از روی یک کپیِ منجمد بفرستید — نه خودِ رکورد/مبلغ.
4. **لایهٔ وب‌سرور ابزارها:** روی هاست واقعی تطبیقِ `api/.htaccess` را نسبت به قانونِ `tools.php` بررسی کنید (متنِ پاسخِ 403، نه فقط عدد).
5. **بستهٔ ممیزیِ مسئول هاست:** خروجیِ «وجود تماس/پیوست» و گزارش JSON کافی است؛ بک‌آپِ خام/فایلِ کاربران/تنظیمات محرمانه ارسال نشود.
6. **تصمیمِ رفعِ گیت یکپارچگی** پس از شواهد بند ۲/۳:
   - گزینهٔ سرورمحور: سرور برای آن کلید entry درستِ `{sha256,bytes,count}` بسازد (و اگر مقدار واقعاً نرمال‌نشدنی است، تصمیم صریح دربارهٔ نرمال‌سازی — که عمداً خودسرانه انجام ندادیم).
   - گزینهٔ مقاوم‌سازی کلاینت: در `ptfValidatePullPayload` کلیدِ دارای `integrity-manifest-missing` را مانند v34.38.11 برای مسیر اتمیک، «unavailable» بگیرد (نگه‌داشتن آخرین نسخهٔ سالم محلیِ همان کلید و اعمال بقیه) به‌جای fail-کلِ snapshot.

---

## ۹) وضعیتِ پیش‌نویسِ اصلاح کلاینت (v34.38.16 — PULL-RESILIENCE) — «درفت، منتشرنشده»

به‌درخواست شما پیش‌نویسِ مقاوم‌سازی کلاینت را روی همین مخزن اعمال کردم. **محتوا:**

- `crm/sync.js` — تابع `ptfValidatePullPayload` (اعتبارسنج یکپارچگیِ قرارداد v2):
  - کلیدی که سرور برایش entry کاملِ `manifest` (sha256/bytes/count) نمی‌فرستد → دیگر **fail کل snapshot نمی‌شود**؛ به فهرست `unavailable` می‌رود.
  - `integrity-mismatch`ِ واقعی (expected کامل اما count/bytes/shape درست نیست) → **همچنان fail-closed** می‌ماند.
- `crm/sync.js` — مسیر legacy تک‌مرحله‌ایِ `data_pull` (همان‌که بوتِ v34.38.15 از آن می‌آید): کلیدهای `unavailable` از `d.data` حذف می‌شوند (نسخهٔ سالم محلی حفظ می‌شود)، در `state.lastUnavailableKeys` ثبت می‌شوند و در نتیجهٔ موفق pull به `initialSync` می‌رسند تا همان پیامِ موجودِ «کلیدهای ناخوانا روی سرور (نسخهٔ محلی حفظ شد)» و برقراریِ bootstrap اجرا شود.
- `crm/client-server.js` — فاز B (آینهٔ IDB): همان کلیدهای `unavailable` روی آینه/محلی نوشته نمی‌شوند.
- `_tools/uat/tester624-v34.38.16-pull-resilience.js` — تستِ رگرسیونِ جدید (جدا از گیتِ CI؛ توسط run-all اجرا می‌شود).

**محدودهٔ تأثیرِ این اصلاح:** فقط «اگر سرور برای **یک** کلید entryِ کاملِ manifest ندارد، بقیهٔ کلیدها دیگر قفل نمی‌شوند». این **ریشهٔ باقی‌ماندهٔ** گیرکردنِ «دریافت کامل اولیه» روی `ptf_crm_commission_records` را هدف می‌گیرد. این اصلاح به‌تنهایی: صف‌ها را پاک نمی‌کند، Force push نمی‌کند، و دادهٔ سرور را دست نمی‌زند — دقیقاً طبق محدودیت‌های امنیتیِ بخش ۸.

**تست‌ها:** `node --check` روی هر دو فایل JS سالم؛ `tester617` (۸ ادعای قبلی) سبز؛ `tester624` جدید سبز.

**⚠️ مهم — این هنوز «مستقر» نیست و مارکرِ نسخه را بالا نبردم (هنوز v34.38.15):** برای rollout باید به‌صورت مکانیکی نسخه به `v34.38.16` ارتقا یابد (در غیر این صورت سپرِ نسخه/کش، باندلِ جدید را به مرورگرها نمی‌رساند) و روی استیجینگ اعتبارسنجی شود. چک‌لیستِ ارتقا:
1. `crm/index.html`: `window.PTF_CRM_RELEASE = 'v34.38.16'` (و هم‌جاهای `?v=34.38.15`)، `PTF_CRM_SNAPSHOT_V2` همچنان `false`.
2. `crm/manifest.json`: `"version": "34.38.16"`.
3. `crm/sw.js`: `var RELEASE = 'v34.38.16'`.
4. `VERSION.json`: `crm_version: "v34.38.16"` + یادداشت updated_by.
5. به‌روزرسانیِ تستِ هم‌ترازیِ نسخه (مانند `tester617`/`tester623` که نسخه را پین می‌کنند).
6. استقرار روی استیجینگ → اعتبارسنجیِ دریافت کامل → سپس تولید.

**باز هم یادآوری:** این فقط ریشهٔ «گیرکردنِ دریافت/بوت» را هدف می‌گیرد. مشکلاتِ مستقل (لایهٔ وب‌سرورِ ابزارها، ورودِ یک کاربر، دامنهٔ پاک‌شدن تماس مشتری) و «اثباتِ سالم‌ماندن/برنگشتنِ دادهٔ سرور» همچنان نیازمند شواهد فقط-خواندنیِ بخش ۸ هستند و با این پچ حل یا اثبات نمی‌شوند.

---

## ۹ب) به‌روزرسانی حیاتی — شاهدِ جدید (لاگِ کنسول دستگاه) تصویر را عوض می‌کند

شاهدِ جدیدی که از دستگاه ارسال شد (لاگِ کنسول) معلوم می‌کند **مقصرِ فعلی، «integrity» نیست؛ بلکه سرور پاسخ نمی‌دهد:**

```
bootstrapped: false
lastError: { scope: "pull", reason: "", status: "network" }
GET https://pishtaj.ir/api/crm.php?action=data_pull&...   net::ERR_TIMED_OUT
GET https://pishtaj.ir/api/crm.php?action=get_inbox&...   net::ERR_TIMED_OUT
```

نکاتِ تعیین‌کننده:
- `reason:""` + `status:"network"` ⇒ کلاینت به مرحلهٔ اعتبارسنجِ v2 (و خطای `commission_records:integrity-manifest-missing`) **نمی‌رسد**؛ چون اصلاً پاسخی از سرور دریافت نمی‌کند.
- **همهٔ** فراخوانی‌ها به `api/crm.php` وقت‌سوزی می‌شوند — حتی `get_inbox` که سبک است ⇒ این «پردازشِ سنگینِ data_pull» نیست؛ کلِ endpoint/سرور در دسترسِ این شبکه نیست یا workerهایش قفل‌اند.
- این دستگاه `since=26385` و krevهای بالاست (دیتای محلی زیاد) ولی `bootstrapped=false` ⇒ دریافتِ کاملِ اولیهٔ همین دستگاه هرگز تمام نشده.

**پیامد برای پچِ v34.38.16:** آن پچ، مشکلِ فعلی را حل نمی‌کند (کلاینت به اعتبارسنج نمی‌رسد). به‌عنوان محکم‌سازیِ بعدی می‌ماند؛ نباید با امیدِ رفعِ این علامت مستقر شود.

**فرضیهٔ فنیِ درخورِ بررسی (نیازمند لاگِ هاست، نه حدس):** نسخهٔ .15 بوت را به `data_pull` تک‌مرحله‌ایِ کامل برگرداند؛ با دیتاستِ بزرگ، چند دستگاهِ هم‌زمانِ bootstrapping می‌توانند workerهای PHP/MySQL را اشباع کنند و حتی فراخوانیِ سبک را هم timeout کنند («هیچ کاربری سبز نیست»).

**اقدامِ تعیین‌کنندهٔ بعدی (تفکیک «سرور» از «شبکهٔ کاربر»):**
1. از **دو شبکه/دستگاهِ متفاوت**: `robots.txt` (ایستا) و `api/crm.php` بدون پارامتر را باز کنید — اگر ایستا لود شد ولی API timeout شد ⇒ مشکلِ PHP/سرور.
2. از مسئول هاست: PHP error log + وضعیت workerها + `SHOW PROCESSLIST` + `memory_limit` و زمانِ یک `data_pull` کامل از CLI روی خودِ هاست.

---

## ۱۰) اگر بخواهید، قدم‌های بعدیِ ممکن از سمت من

من به سرور/دادهٔ واقعی دسترسی ندارم و فقط روی همین مخزن کار می‌کنم. می‌توانم یکی از این‌ها را برایتان آماده کنم (با تأیید شما، چون هرکدام ریسک/دامنهٔ متفاوت دارد):

- (الف) پیش‌نویسِ تغییرِ **مقاوم‌سازی کلاینتِ v34.38.16** در `ptfValidatePullPayload` (کلید ناخوانا → unavailable به‌جای fail-کل) + بازبینی‌گرِ محدودهٔ تأثیر.
- (ب) فهرستِ بازبینیِ استقرار/رفرش برای اطمینان از اینکه همهٔ دستگاه‌ها واقعاً v34.38.15 اجرا کنند.
- (ج) یک «بستهٔ تحویل به مسئول هاست» فقط-خواندنی (مثل پکیجِ ممیزی قبلی) برای گرفتن همان ۴ شاهدِ تعیین‌کننده، بدون ارسال دادهٔ محرمانه.

قبل از نوشتن هر کدِ اصلاحی روی تولید، منتظر شواهدِ بند ۲/۳ هستم تا «سرور» را از «کلاینت» جدا کنم و حدس نزنم.
