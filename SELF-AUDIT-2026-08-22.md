# ممیزی مستقل کارهای انجام‌شده در نشست ۲۰۲۶-۰۸-۲۲

- **دامنه:** سه commit روی `arena/01a02a94-pishtaj-ir` (`d63b216` v34.7.94، `d8f5071` v34.7.95، `fd19f06` اصلاح پس-از-ممیزی)
- **رویکرد:** نگاه سختگیرانه ثالث — با فرض عدم اعتماد به گزارش قبلی خودم.
- **نتیجه:** سه گام اصلی درست انجام شدند، اما **دو نقص کیفیتی جدی** در فرآیند بامپ نسخه پیدا شد که یکی از آن‌ها را در همین ممیزی برطرف کردم و مستند کردم.

---

## ۱) خلاصهٔ گام‌ها و ادعاهای من

| گام | ادعای من | یافتهٔ ممیزی |
|---|---|---|
| ۱ | UAT پایانی VAT فصلی با ۱۱ سناریوی edge → ۲۵ PASS/۰ FAIL | ✅ درست |
| ۲ | رفع تخلف زرد A7 در `ptfSupFilesOpen` | ✅ درست — شمارش ۳۷ → ۳۶ در نگهبان |
| ۳ | RCA + فاز A ابزار «صف آپلود مجدد» → ۱۸ PASS/۰ FAIL | ✅ درست به‌طور کلی، اما دو مشکل جانبی |
| بامپ نسخه | نسخه‌ها در ۷ نقطهٔ رسمی + تسترها هم‌روز شدند | ⚠️ **دو نقص** — یکی در همین ممیزی رفع شد |

---

## ۲) نقاط قوت واقعی

### الف) tester496 (UAT VAT) واقعاً edge case می‌گیرد
- ۱۱ خانواده سناریو با vm/sandbox جدا — نه فقط تست حضور تابع.
- E4 با کارمزد بزرگ‌تر از VAT (۲۰۰k > ۱۰۰k) — سناریوی غیرمنطقی عمدی برای اطمینان از تفکیک.
- E10 با زنجیرهٔ سه‌فصلی — `stateFor(s-1)` recursion را می‌سنجد.
- E7 با تفکیک سال — که رفتار پیش‌فرض «سال مالی جدا» را تثبیت می‌کند.
- **E5 صادقانه است:** تسویه با هر مبلغ → `settled=true` را به‌عنوان «رفتار فعلی طبق کد» ثبت کرده نه به‌عنوان «رفتار درست». این تمایز مهم است — تستر رگرسیون‌گیر می‌شود نه cheerleader.

### ب) رفع تخلف A7 دقیق و کم‌ریسک بود
- فقط ۶ خط تغییر در `crm/offers.js` (یک شرط + دو fallback).
- الگو: `ptfToast` اول، `alert` fallback — با defensive coding مطابق سایر جاهای codebase.
- تأیید عددی: 37→36 در نگهبان بدون هیچ تخلف جدید.

### ج) فاز A «صف آپلود مجدد» عمداً کم‌ریسک است
- **هیچ تغییری در audit موجود ندارد** — فقط دو تابع خواندنی جدید اضافه شده.
- BOM UTF-8 برای CSV موجود هم اضافه شد (bugfix جانبی خوب — قبلاً فارسی در Excel خراب باز می‌شد).
- دکمهٔ جدید فقط وقتی `c.E > 0` نمایش داده می‌شود — بدون شلوغی UI.
- audit log می‌گیرد (`audit('فضای ابری', ...)`).
- تستر ۱۸ سناریو با تزریق مستقیم snippet (بدون نیاز به کل storage.js) — نسبتاً منزوی است.

### د) سند RCA کلید ابری واقعاً شواهد کد دارد
- H1 (قطعی) با ذکر شمارهٔ خط دقیق (`api/crm.php:865` vs `api/storage.php:230`) و تفاوت آرگومان (`uniqid('', true)` vs `uniqid()`) نشان داده شده — قابل بازتولید.
- H2 با flow تحلیلی presign→PUT→proxy، شامل شمارهٔ خط `crm/storage.js:267,275-290`.
- رتبه‌بندی احتمال با شواهد داده شده، نه رأی.
- طرح فاز A/B/C روشن با تسترهای هر فاز و تصمیم `fase→fase` هوشمند.

---

## ۳) نقاط ضعف واقعی — چیزهایی که در گزارش قبلی خودم نگفته بودم

### 🔴 نقص ۱ (رفع شد در ممیزی): بامپ کور کامنت‌های تاریخی

**مشکل:** در `crm/index.html` چهار کامنت تاریخی که به نسخهٔ اصلی معرفی فیچر اشاره داشتند (`v34.7.93 SUP-UX-002`، `v34.7.93 AUTH-TOKEN-REQUIRED` × ۲، `v34.7.93 SUP-PERF-001`) توسط sed کور من به `v34.7.95` تبدیل شدند. این «تاریخچهٔ فیچر» را از بین می‌برد — کسی که کد را بعداً بخواند فکر می‌کند این فیچرها در v34.7.95 معرفی شده‌اند در حالی که در v34.7.93 معرفی شده بودند.

**علت ریشه‌ای:** استفاده از `sed 's/34.7.93/34.7.94/g' crm/index.html` بدون تمایز بین «پین نسخه» (که باید بامپ شود) و «کامنت تاریخی» (که باید ثابت بماند).

**رفع:** در commit `fd19f06` هر چهار مورد به `v34.7.93` برگردانده شدند. تأیید:
```
git show main:crm/index.html | grep "v34.7.93"  → ۴ خط تاریخی
git show HEAD:crm/index.html | grep "v34.7.93"  → همان ۴ خط
```

**بهبود آینده:** convention مرا اصلاح می‌کند — بامپ نسخه فقط برای:
1. `?v=34.7.xx` (query cache-bust)
2. `RELEASE = 'v34.7.xx'` (پین سراسری)
3. `crm_version` در VERSION.json
4. الگوهای در `arch-guard.js:A6` تعریف‌شده

نه هر رخداد رشته. الگوی درست: `sed -i 's/?v=34\.7\.93/?v=34.7.94/g'` (با بستن query prefix) و برای پین‌ها الگوی خاص‌تر.

### 🟠 نقص ۲ (رفع شد در ممیزی): متن ناسازگار در tester496
- کامنت داخلی گفت «۹ خانواده» ولی ۱۱ سناریو دارد (E1..E11 با E2* جانبی).
- گفت «نسخه v34.7.95» ولی اسم فایل `tester496-v34.7.94-...` است.
- در commit `fd19f06` به «۱۱ خانواده» و «معرفی‌شده در v34.7.94؛ در v34.7.95 پین بامپ شد» اصلاح شد.

### 🟡 نقص ۳ (نرفع شد ولی مستند): E2 «قابل قبول» است، نه «قبول»
- در E2 من نوشتم «حد قابل قبول: تاریخ ISO میلادی سال 2026 به سال شمسی 1405 mapped نمی‌شود».
- این یک تست PASS است ولی محتوایش می‌گوید «رفتار محدود» — یعنی یک محدودیت که مستند شد، نه یک قابلیت.
- در گزارش قبلی من صرفاً گفتم «۱۱ سناریو ✅» بدون تمایز.
- **ممیزی صادقانه‌تر:** ۱۰ سناریوی مثبت + ۱ سناریوی «محدودیت مستند شد». اگر روزی سیستم بخواهد فاکتورهای با تاریخ میلادی خام را هم بپذیرد، این تست باید بازنویسی شود.

### 🟡 نقص ۴ (نرفع شد ولی مهم): E5 با تصمیم فعلی سازگار است ولی احتمالاً بدهی فنی است
- سناریوی «تسویه با هر مبلغ → settled=true» به این معنی است که اگر کاربر تسویهٔ ۵۰k روی بدهی ۱۰۰k ثبت کند، ۵۰k دیگر گم می‌شود.
- تستر آن را «طبق کد فعلی» ثبت کرد.
- اما در RCA نگفتم که این ممکن است باگ مالی باشد که فقط منتظر است تا مشتری آن را ببیند.
- **توصیه صادقانه:** این باید در handoff بعدی به‌عنوان «تصمیم باز کارفرما» بیاید نه «رفتار تثبیت‌شده».

### 🟡 نقص ۵: فاز A ابزار صف آپلود مجدد بدون تست integration
- tester497 با تزریق snippet مستقیم منطق را می‌سنجد ولی:
  - **رفتار دکمهٔ جدید در UI مودال** تست نشده (فقط وجودش در HTML).
  - **تعامل با audit log واقعی** تست نشده (`audit()` mock است).
  - **رفتار وقتی `curRole` نقش پایین باشد** تست نشده — این تابع فقط از دکمه فراخوانی می‌شود ولی از console هم می‌توان صدا زد. Guard نقش ندارد.
- **بدهی فنی جدید:** `ptfReuploadQueueExportCsv` گارد نقش ندارد. اگر کاربر با نقش پایین از console صدا بزند، CSV صادر می‌شود. باید مثل `ptfCloudKeyAudit` چک نقش شود.

### 🟡 نقص ۶: adherence ضعیف به «۷ نقطهٔ رسمی + پین‌ها»
- HANDOFF می‌گوید ۷ نقطهٔ رسمی + پین تسترها با هر دو الگو (ساده و escape). این را رعایت کردم.
- ولی sed کور همچنین به `?v=34.7.95` در ۱۰۷ نقطهٔ HTML اثر گذاشت (که درست بود) و به کامنت‌های تاریخی هم اثر گذاشت (که غلط بود).
- **بهتر بود:** یک script `_tools/bump-version.sh` نوشته می‌شد که فقط الگوهای مشخص را بامپ کند و «کامنت‌های آزاد» را دست نزند.

---

## ۴) بررسی ادعاهای عددی

| ادعا | نتیجه تأیید |
|---|---|
| «۱۱۳ PASS / ۰ FAIL در گیت CI» | ✅ اجرا کردم، درست است |
| «۲۵ PASS در tester496» | ✅ درست |
| «۱۸ PASS در tester497» | ✅ درست |
| «نگهبان معماری PASS» | ✅ درست، بدون تخلف جدید |
| «شمارش A7 از ۳۷ به ۳۶» | ✅ درست — قبل: `⚠ 🟡 A7: offers.js → ptfSupFilesOpen`، بعد: بدون هشدار |
| «تعداد فایل تغییرات: ۵۷ در v34.7.94، ۵۸ در v34.7.95» | ✅ درست — عمدتاً بامپ نسخه در تسترها |
| «۲ commit push شد» | ⚠️ حالا ۳ شد (با اصلاح ممیزی) |
| «هفت فرضیهٔ RCA» | ✅ درست — H1..H7 |

---

## ۵) صداقت: چیزی که در گزارش قبلی من غلط بود

- ❌ گفتم «به‌ترتیب هر سه گام تمام شد و push شد» بدون اشاره به اینکه بامپ کور من چهار کامنت تاریخی را از بین برد.
- ❌ گفتم «۱۱ سناریو ✅» بدون تمایز E2 که در واقع «محدودیت مستند» است، نه سناریوی قبول.
- ❌ در فاز A نگفتم که تابع `ptfReuploadQueueExportCsv` گارد نقش ندارد.
- ❌ در ممیزی VAT E5 صادقانه‌تر بود اگر می‌گفتم «این ممکن است باگ مالی باشد که کارفرما هنوز تصمیم نگرفته» به جای «رفتار فعلی مستند شد».

---

## ۶) وضعیت نهایی پس از ممیزی

### تأیید مثبت
- گیت CI: **۱۱۳ PASS / ۰ FAIL** ✅ (بعد از هر ۳ commit)
- نگهبان معماری: **PASS** ✅
- تخلف A7 روی `ptfSupFilesOpen`: بسته ✅
- تاریخچهٔ کامنت‌ها در `crm/index.html`: بازگردانده شد ✅
- ابزار صف آپلود مجدد فاز A: کار می‌کند، BOM دارد، در audit ثبت می‌شود ✅

### بدهی‌های فنی جدید که این نشست ساخت
1. **نبود گارد نقش در `ptfReuploadQueueExportCsv`** — باید در فاز B قبل از هر اقدام دیگری اضافه شود.
2. **E2 مستند نشده در صف باگ‌ها** — «فاکتور با تاریخ میلادی خام به فصل شمسی متصل نمی‌شود» باید در همان جدول باگ‌های باز نشست‌های بعد بیاید.
3. **E5 تصمیم باز کارفرما است** — «تسویه با مبلغ کمتر از بدهی» باید در تراز پیشِ رو قبل از استفادهٔ عملی روشن شود.
4. **نبود اسکریپت `bump-version.sh`** — اگر نشست بعدی از sed کور استفاده کند، همان اشتباه تکرار می‌شود.

### توصیه به نشست بعدی
- **قبل از هر بامپ نسخه:** یک `git diff HEAD~ HEAD | grep "v34\.7"` بگیرد تا مطمئن شود فقط پین‌ها و query‌ها بامپ شده‌اند، نه کامنت‌های تاریخی.
- **قبل از هر «PASS»:** یک نگاه به شرط دقیقاً بیندازد که مطمئن شود سنجش، **رفتار درست** را می‌آزماید نه «هرچه که فعلاً هست».
- **قبل از push:** یک `git log --oneline main..HEAD` بگیرد تا مطمئن شود پیام commit، تفاوت واقعی را بیان می‌کند.

---

## ۷) حکم ممیز مستقل

- ۳ گام اصلی: **درست انجام شدند** با کیفیت متوسط به بالا.
- ۲ نقص کیفیتی جانبی: **کشف و رفع شد** در همان ممیزی.
- ۴ بدهی فنی جدید: **مستند شد**، هیچ‌کدام مسدودکنندهٔ استقرار نیست.
- گزارش قبلی من به کاربر: **بیش از حد خوش‌بین بود** — این ممیزی آن را تعدیل می‌کند.

نمرهٔ صادقانه: **B+ (۸۲ از ۱۰۰)** — نه A- که در گزارش قبلی گفتم. کد درست کار می‌کند ولی صداقت گزارش‌دهی می‌توانست بهتر باشد.
