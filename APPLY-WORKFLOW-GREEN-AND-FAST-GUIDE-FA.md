# راهنمای گام‌به‌گام — دیپلوی همیشه‌سبز و افزایشی (v34.36.0)

**تاریخ:** ۲۰۲۶-۰۹-۰۴
**مخاطب:** مالک/ادمین مخزن `lavasanimanesh-lgtm/Pishtaj.ir`
**زمان لازم:** ~۱۵ دقیقه · **دانش فنی لازم نیست** — کلیک و کپی/پیست
**قالب:** همان قالب `APPLY-WORKFLOW-PATCHES-GUIDE-FA.md`
**جایگزین:** `APPLY-INTEGRITY-GATE-FIX-GUIDE-FA.md` و `docs-deploy/APPLY-integrity-gate-fix.txt` (ادغام شدند)

---

## خلاصهٔ یک‌خطی

خودِ مخزن از ۲۰۲۶-۰۸-۲۹ راه‌حل «دیپلوی افزایشی + گیت همیشه‌سبز» را طراحی و تستر-قفل کرده بود،
ولی به‌دلیل نداشتن مجوز `workflows` به‌صورت **پچ معلق** در `_tools/` منتظر اعمالِ شما مانده بود.
آن پچ‌ها را پیدا کردم، **یک باگ قطعی که داخل خودشان بود** را گرفتم، و آمادهٔ اعمال کردم.

---

## خواستهٔ شما و پاسخش

| خواسته | پاسخ |
|---|---|
| دیپلوی خودکار استیجینگ بماند | ✅ دست‌نخورده — همان تریگرهای `push` به `main` و `arena/**` و بقیه |
| خطای قرمز «مطابقت» برود و دیپلوی‌ها همیشه سبز باشند | ✅ دو علت داشت؛ هر دو رفع شد (پایین توضیح داده‌ام) |
| کوتاه‌ترین زمان — فقط همان تغییر کوچک دیپلوی شود | ✅ آپلود بر مبنای `git diff` — فقط فایل‌های تغییریافته |

**برآورد زمان** (از سند خودِ مخزن، `ARENA-DEPLOY-SPEEDUP-STAGING-2026-08-29.md`):

| سناریو | قبل | بعد |
|---|---|---|
| تغییر کد CRM (push عادی) | ~۸ تا ۱۲ دقیقه | **~۲ تا ۳ دقیقه** |
| همگام‌سازی کامل هفتگی / fallback | ~۸ تا ۱۲ دقیقه | همان قبل (کامل است، عمداً) |

---

## چرا خطای قرمز هیچ‌وقت نمی‌رفت؟ دو علت مستقل

### علت ۱ — کش مسیرمحور هاست (طراحی مخزن این را حل کرده)
هاست فایل‌ها را با کشِ «مسیرمحور» سرو می‌کند و `?v=...` را نادیده می‌گیرد. پس گیت صحت که بایت‌ها را
از **HTTP** می‌گرفت، در پنجرهٔ کوتاهِ ۵×۱۲ ثانیه بایت **کهنه** می‌دید و قرمزِ کاذب می‌داد —
درحالی‌که فایل روی دیسک سرور درست بود.

**راه‌حل (لایهٔ ۱ مسدودکننده):** بایت‌ها مستقیماً **از خود FTP** خوانده می‌شوند (readback)، نه از HTTP.
این لایه مستقل از کش هاست است؛ ناهمخوانی یعنی فایل روی دیسک واقعاً غلط است → `exit $fail`.
**لایهٔ ۲ (هشداری):** تازگی HTTP با هدرهای `no-cache` و دوبل cache-buster در پنجرهٔ ۱۲×۳۰ ثانیه.
اگر فقط این لایه عقب بماند → `::warning` و ران **سبز** می‌ماند، چون دیسک با readback اثبات شده.

این همان چیزی است که «همیشه سبز» را واقعاً ممکن می‌کند، بدون اینکه گیت خنثی شود.

### علت ۲ — باگ نگارشی `FILES` (این را خودم پیدا کردم؛ داخل پچ‌های مخزن هم بود)
`FILES` یک رشتهٔ **یک‌خطی با فاصله** بود که با `while IFS= read -r f` مصرف می‌شد. چون `IFS` تهی است،
حلقه **فقط یک‌بار** می‌چرخید و *کل* رشته را یک نام فایل می‌گرفت:

```
sha1sum: 'crm/sw.js crm/shell.js ...': No such file or directory   → want = (تهی)
curl: (3) URL using bad/illegal format or missing URL              → got  = da39a3ee5e
```

پس `want != got` همیشه برقرار بود و گیت **بی‌قیدوشرط** قرمز می‌شد — مستقل از درستیِ استقرار.

⚠️ **نکتهٔ مهم:** این باگ داخل **هر دو پچ معلق مخزن هم بود**. یعنی اگر پچ‌ها را همان‌طور که بودند
اعمال می‌کردید، خطای قرمز نه‌تنها نمی‌رفت، بلکه در استیجینگ به لایهٔ **مسدودکننده** منتقل می‌شد
و دیپلوی سخت‌تر می‌شکست. من هر دو پچ را بازتولید کردم و این باگ را در هر دو گرفتم.

**شاهد:** در دو اجرای مستقل (`main@4ec342e` با v34.35.0 و `arena@7366edf` با v34.36.0) همین گام شکست،
درحالی‌که در هر دو `verify-deploy.sh` همهٔ ۵ فایل را «هش زنده == کامیت ✅» گزارش کرده بود.
دلیلش روشن است: `verify-deploy.sh` متغیر `BYTES_FILES` را **چندخطی** می‌سازد، پس درست حلقه می‌زند.

---

## چه چیزی تحویل می‌دهم

| فایل | نقش |
|---|---|
| `_tools/PENDING-workflow-deploy-staging-speedup-v34.8.40.patch` | **بازتولید شد** — سرعت‌بخشی استیجینگ + رفع `FILES` |
| `_tools/PENDING-workflow-integrity-hardening-v34.8.37.patch` | **بازتولید شد** — هاردنینگ پروداکشن + رفع `FILES` |
| `docs-deploy/deploy-staging.FIXED.yml` | نسخهٔ کامل و نهایی — برای کپی/پیست مستقیم |
| `docs-deploy/deploy-production.FIXED.yml` | نسخهٔ کامل و نهایی — برای کپی/پیست مستقیم |

این دو پچ روی دو فایل **جدا** دست می‌زنند (یکی `deploy-staging.yml`، دیگری `deploy-production.yml`)،
پس با هم تداخل ندارند و هر دو با هم تمیز اعمال می‌شوند — این را آزمودم.

---

## ورک‌فلوی استیجینگ بعد از اعمال چه شکلی می‌شود

```
✓ Checkout                          ✓ Compute deploy delta (git-based)      ← جدید
✓ Setup Node                        ✓ Incremental FTP upload (delta only)   ← جدید
✓ Architecture guard                ✓ Apply deletions (git-diff D)          ← جدید
✓ CI gate                           ✓ Deploy via FTP (full sync fallback)   ← فقط در حالت full
✓ PHP syntax lint                   ✓ Verify deployment
✓ robots.txt → noindex              ✓ Publish diagnostics
✓ Inject staging banner (crm+home)  ✓ Post-deploy integrity check           ← دولایه
✓ Staging no-cache headers          ✓ Fail if verification failed
✓ Write deploy marker               ✓ Summary
```

### آپلود افزایشی چطور کار می‌کند
1. مارکر `__deploy__.txt` **رانِ موفق قبلی** از FTP خوانده می‌شود → SHA پایه.
2. اگر آن SHA در تاریخچهٔ git باشد (لازم شود `git fetch --depth=1 origin <sha>`)، فقط فایل‌های
   `git diff --name-only --diff-filter=ACMRT "$PREV_SHA" "$GITHUB_SHA"` آپلود می‌شوند —
   **چند ثانیه به‌جای ۵ تا ۸ دقیقه**.
3. فایل‌های حذف‌شده (`--diff-filter=D`) سمت سرور `DELE` می‌شوند.
4. فایل‌های تولیدی هر ران همیشه آپلود می‌شوند: بنر (`crm/*.html` + `index.html`)، `robots.txt`،
   `crm/.htaccess` و `__deploy__.txt`.

### چه وقت به همگام‌سازی کامل برمی‌گردد (fallback خودکار)
هر یک از این‌ها → آپلود کامل با همان اکشن SamKirkland (یعنی رفتار قبلی، کند ولی درست):
- مارکر ران قبلی پیدا نشود، یا SHA پایه در تاریخچه نباشد
- **مسیر غیر-ASCII در دلتا** — چون این مخزن نام فایل فارسی دارد و URLِ curl برای آن‌ها امن نیست
- آپلود افزایشی بخوابد (`continue-on-error` دارد، پس job نمی‌افتد)
- `cron: '0 4 * * 6'` — همگام‌سازی کامل **شنبه‌ها ۰۴:۰۰ UTC** برای درمان drift سمت سرور
- ورودی دستی `full` در `Run workflow`

### سرعت‌های جانبی
- **بنر:** پیش‌تر به **۶۷۳ فایل HTML** تزریق می‌شد و چون timestamp داشت همه هر بار عوض می‌شدند
  (۲۰٫۷۵ مگابایت آپلود بیهوده). حالا فقط به **`crm/*.html` + `index.html` ریشه** (۸ فایل).
- **php:** از نسخهٔ **پیش‌نصبِ رانر** استفاده می‌شود؛ `apt-get install php-cli` فقط fallback است
  (پیش‌تر بی‌قیدوشرط اجرا و ~۱ تا ۲ دقیقه تلف می‌شد).
- **`paths-ignore`:** بدون تغییر باقی ماند — من عمداً دست نزدم تا پوشش تسترها کم نشود.

---

# مرحله ۰ — ادغام PR #2

گیت CI را سبز می‌کند (۲۰۲ PASS / ۰ FAIL) و پچ‌های بازتولیدشده را به `main` می‌رساند.

1. **https://github.com/lavasanimanesh-lgtm/Pishtaj.ir/pull/2**
2. **Merge pull request** → **Confirm merge**

> **بدون این مرحله** دیپلوی پروداکشن همان‌جا که الان می‌شکند می‌شکند (گام `CI gate`) و اصلاً به FTP نمی‌رسد.

---

# مرحله ۱ — رساندن ورک‌فلوها به `main`

سه روش — **یکی** را انتخاب کنید.

## روش A — اعطای مجوز `workflows` (حل دائمی)

1. گیت‌هاب → **Settings** (پروفایل) → **Developer settings** → **GitHub Apps**
2. اپ Arena → **Permissions and events** → **Repository permissions**
3. **Workflows** = **Read and write** → ذخیره
4. به من بگویید «مجوز workflows دادم» — بقیه را خودم push و راستی‌آزمایی می‌کنم.

> اگر اپ Arena را در این فهرست نمی‌بینید، مدیریتش دست شما نیست → **روش B**.

## روش B — در وب گیت‌هاب، بدون نصب هیچ چیز

برای هر دو فایل، همین پنج گام:

1. ویرایشگر را باز کنید:
   - `https://github.com/lavasanimanesh-lgtm/Pishtaj.ir/edit/main/.github/workflows/deploy-staging.yml`
   - `https://github.com/lavasanimanesh-lgtm/Pishtaj.ir/edit/main/.github/workflows/deploy-production.yml`
2. نسخهٔ کامل و نهایی را از این دو فایل باز کنید و **همه‌اش را کپی** کنید (دکمهٔ **Raw** برای کپی تمیز):
   - `docs-deploy/deploy-staging.FIXED.yml`
   - `docs-deploy/deploy-production.FIXED.yml`
3. در ویرایشگر: **Ctrl+A** → **Delete**
4. **Ctrl+V**
5. **Commit changes…** → **Commit directly to the `main` branch** → **Commit changes**

> ⚠️ چیزی را دست‌کاری نکنید، به‌ویژه تورفتگی‌ها — YAML به تورفتگی حساس است.
> اگر اشتباه کردید: صفحهٔ فایل → **History** → کامیت قبلی → **⋯** → **Revert changes**.

**ترتیب:** اول `deploy-staging.yml`، بعد مرحلهٔ ۲ را امتحان کنید، و فقط وقتی سبز شد سراغ پروداکشن بروید.

## روش C — با git روی کامپیوتر

```bash
cd <پوشهٔ مخزن>          # همان پوشه‌ای که .github/ داخلش است
git checkout main
git pull

git apply _tools/PENDING-workflow-deploy-staging-speedup-v34.8.40.patch
git apply _tools/PENDING-workflow-integrity-hardening-v34.8.37.patch

git add .github/workflows/deploy-staging.yml .github/workflows/deploy-production.yml
git commit -m "ci: دیپلوی افزایشی + گیت صحت دولایه + رفع باگ FILES"
git push origin main
```

**نکتهٔ مسیر:** `git apply` را حتماً از **ریشهٔ مخزن** بزنید. از پوشهٔ `_tools/` خطای
*No such file or directory* می‌گیرد.

**اگر `git apply` خطا داد**، از کپی مستقیم استفاده کنید:

```bash
cp docs-deploy/deploy-staging.FIXED.yml    .github/workflows/deploy-staging.yml
cp docs-deploy/deploy-production.FIXED.yml .github/workflows/deploy-production.yml
git add .github/workflows/ && git commit -m "ci: دیپلوی افزایشی + گیت صحت دولایه" && git push origin main
```

> 🪟 **ویندوز:** این دستورها در **Git Bash** کار می‌کنند، نه PowerShell/CMD.

---

# مرحله ۲ — آزمون روی استیجینگ

1. **https://github.com/lavasanimanesh-lgtm/Pishtaj.ir/actions/workflows/deploy-staging.yml**
2. **Run workflow** → برنچ `main` → `full` را **خالی/نزنده** بگذارید → **Run workflow**
3. صبر کنید.

✅ **نشانه‌های موفقیت:**

در گام `Compute deploy delta` یکی از این دو:
```
mode=delta  → فقط فایل‌های تغییریافته آپلود می‌شوند
mode=full   → همگام‌سازی کامل (اولین اجرا، یا fallback)
```

و در گام `Post-deploy integrity check`:
```
✅ لایهٔ ۱: بایت‌های دیسک سرور == کامیت (readback FTP)
✅ مارکر استقرار == کامیت این ران
```

اگر لایهٔ ۲ عقب بماند، این را می‌بینید و ران **سبز** می‌ماند:
```
::warning::کش HTTP استیجینگ هنوز نسخهٔ تازه نمی‌دهد؛ فایل‌ها روی سرور درست‌اند (readback ✅)
```

4. **اجرای دوم را بسنجید** — یک تغییر کوچک بدهید و دوباره push کنید. حالا باید
   `mode=delta` باشد و زمان کل اجرا به **~۲ تا ۳ دقیقه** افت کند. **این‌جا صرفه‌جویی واقعی دیده می‌شود.**

5. **https://staging.pishtaj.ir** با **Ctrl+Shift+R** — بنر نارنجی «محیط تست (Staging)» پایین صفحه باشد.

> ⚠️ توجه: بنر حالا فقط روی **صفحهٔ اصلی** و **پوستهٔ CRM** است، نه روی همهٔ ۶۷۳ صفحهٔ محتوا.
> این عمدی است (همان طراحی خودِ مخزن) — چون تزریق به همهٔ صفحه‌ها باعث می‌شد هر بار ۲۰٫۷۵ مگابایت
> آپلود بیهوده شود. دامنهٔ `staging.pishtaj.ir` خودش نشانهٔ محیط تست است.

---

# مرحله ۳ — دیپلوی پروداکشن

1. **https://github.com/lavasanimanesh-lgtm/Pishtaj.ir/actions/workflows/deploy-production.yml**
2. **Run workflow**:

| فیلد | مقدار |
|---|---|
| `confirm` | `DEPLOY` (دقیقاً، حروف بزرگ انگلیسی) |
| `ref` | `main` |

3. صبر کنید.

✅ همهٔ گام‌ها باید سبز باشند و در پایان **تگ نسخه** و **Release با فایل بک‌آپ** ساخته شود —
این فقط بعد از سبزشدن گیت صحت اتفاق می‌افتد، پس دیدنش یعنی دیپلوی واقعاً موفق بوده.

> ⏳ **توجه دربارهٔ پروداکشن:** گیت صحت پروداکشن عمداً **fail-closed** نگه داشته شده
> (تصمیم مستندِ خودِ مخزن: «نسخهٔ پروداکشن که fail-closed HTTP را نگه می‌دارد — درست است»).
> برای همین ۱۸۰ ثانیه انتظار اولیه دارد تا کش مسیرمحور هاست پیر شود، بعد پنجرهٔ ۱۲×۳۰ ثانیه.
> در حالت سالم همان ۱۸۰ ثانیه کافی است و گام در تلاش اول سبز می‌شود. پروداکشن دستی و کمیاب است،
> پس این هزینه قابل قبول است — در عوض اگر نسخهٔ مخلوط روی سایت اصلی برود، **حتماً** گرفته می‌شود.

---

# مرحله ۴ — تأیید نهایی روی سایت زنده

1. **https://pishtaj.ir** با **Ctrl+Shift+R**
2. **https://pishtaj.ir/__deploy__.txt** → باید `"env":"production"` و `"ref":"main"` و `"sha":"..."` داشته باشد
3. **https://pishtaj.ir/robots.txt** → باید `Allow: /` باشد، نه `Disallow: /`
4. **https://pishtaj.ir/crm/** → تب «مدیریت سایت» را باز کنید؛ بنر قرمز
   «فایل برنامهٔ مدیریت سایت در مرورگر شما قدیمی است» باید **نباشد** (نقص ۲ رفع شد)
5. دکمهٔ **خروج** کنار نام کاربر در سایدبار، و نقش/سمت زیر نام (تغییر اصلی v34.36.0)

---

# عیب‌یابی

### گام `CI gate` قرمز شد
یعنی pin نسخه دوباره drift کرده. لاگ را باز کنید و دنبال `=== CI gate:` بگردید.
```bash
node _tools/uat/bump-version-pins.js <نسخهٔ-قدیمی>     # مثال: 34.36.0
node _tools/uat/run-ci-gate.js                          # راستی‌آزمایی محلی
```

### گام `Compute deploy delta` گفت `mode=full`
خطا نیست — fallback خودکار است. علت‌های ممکن: اولین اجرا، مارکر ران قبلی روی FTP نبود،
SHA پایه در تاریخچه نبود، یا مسیر غیر-ASCII در دلتا. فقط آن اجرا کند است (کامل آپلود می‌کند).
اگر **همیشه** `full` می‌شود، یعنی مارکر `__deploy__.txt` روی FTP خوانده نمی‌شود →
`FTP_SERVER_DIR` را در Secrets بررسی کنید.

### گام `Post-deploy integrity check` قرمز شد
با گیت دولایه، قرمزی یعنی **لایهٔ ۱ (readback از FTP)** شکست خورده — یعنی بایت‌های روی دیسک سرور
واقعاً با کامیت یکی نیستند. پیام‌ها گویا هستند:

| پیام | معنا | اقدام |
|---|---|---|
| `≠ ... (دانلود FTP ناموفق)` | readback از FTP کار نکرد | `FTP_SERVER_DIR` / مجوز کاربر FTP |
| `≠ ... (want=... got=... — روی دیسک سرور)` | فایل واقعاً متفاوت است | `Run workflow` با `full=true` |
| `≠ مارکر __deploy__.txt با کامیت این ران مطابقت ندارد` | مارکر آپلود نشده | `full=true` |
| `فقط تفاوت پایان‌خط (\r)` | سرور با تبدیل CRLF تحویل می‌دهد | بی‌ضرر — محتوا یکی است |

### `::warning::کش HTTP استیجینگ هنوز نسخهٔ تازه نمی‌دهد`
**این خطا نیست** و ران سبز می‌ماند. یعنی دیسک سرور درست است (readback تأیید کرده) ولی کش هاست
هنوز بایت کهنه سرو می‌کند. کش ظرف چند دقیقه خودش تازه می‌شود؛ `.htaccess` تزریقی `no-cache` هم کمک می‌کند.

### گام `Deploy via FTP` قرمز شد
**این‌جا واقعاً ممکن است مشکل FTP باشد:**
- `Login incorrect` / `530` → نام کاربری یا رمز در Secrets غلط است
- `Timeout` / `Could not connect` → آدرس سرور یا فایروال/پورت
- `Permission denied` / `550` → کاربر FTP روی آن مسیر حق نوشتن ندارد

سکرت‌ها: **Settings → Secrets and variables → Actions**
استیجینگ: `FTP_SERVER` · `FTP_USERNAME` · `FTP_PASSWORD` · `FTP_SERVER_DIR`
پروداکشن: `FTP_PROD_SERVER` · `FTP_PROD_USERNAME` · `FTP_PROD_PASSWORD` · `FTP_PROD_SERVER_DIR`

### `[FTP] مسیر مقصد () با LIST/NLST خوانده نشد`
آن `()` **خالی** است، یعنی `FTP_SERVER_DIR` مقدار ندارد. چون این بررسی فقط تشخیصی است job را
نمی‌اندازد، ولی **برای دیپلوی افزایشی مهم می‌شود** — چون خواندن مارکر و readback هر دو به این مسیر
نیاز دارند. حتماً پرش کنید (مثلاً `/public_html/`).

### ورک‌فلو کلاً از کار افتاد (خطای YAML)
صفحهٔ فایل → **History** → کامیت سالم → **⋯** → **Revert changes**. یا با git:
```bash
git checkout HEAD~1 -- .github/workflows/deploy-staging.yml .github/workflows/deploy-production.yml
git commit -m "revert: بازگردانی ورک‌فلوها" && git push origin main
```

---

# رول‌بک پروداکشن

1. **https://github.com/lavasanimanesh-lgtm/Pishtaj.ir/releases** → آخرین تگ پایدار
2. **Actions → Deploy to Production → Run workflow**

| فیلد | مقدار |
|---|---|
| `confirm` | `DEPLOY` |
| `ref` | نام تگ، مثلاً `v2026.09.04-1030` |

> ⚠️ فقط تگ‌هایی معتبرند که **بعد از** اولین اجرای موفقِ ورک‌فلوی جدید ساخته شده باشند.
> تا پیش از این رفع، هیچ دیپلویی راستی‌آزمایی‌شده اعلام نمی‌شد و تگی هم ساخته نمی‌شد.

---

# چک‌لیست یک‌صفحه‌ای

```
[ ] ۰. PR #2 را merge کردم
[ ] ۱. دو ورک‌فلو را به main رساندم                    (روش A یا B یا C)
[ ] ۲. Deploy to Staging → «✅ لایهٔ ۱: بایت‌های دیسک سرور == کامیت (readback FTP)»
[ ] ۳. اجرای دوم → mode=delta و زمان ~۲ تا ۳ دقیقه
[ ] ۴. staging.pishtaj.ir با Ctrl+Shift+R + بنر «محیط تست» روی صفحهٔ اصلی
[ ] ۵. FTP_SERVER_DIR در Secrets پر است (برای دلتا و readback لازم است)
[ ] ۶. Deploy to Production با confirm=DEPLOY و ref=main → همه سبز
[ ] ۷. تگ نسخه + Release بک‌آپ ساخته شد
[ ] ۸. pishtaj.ir/__deploy__.txt مقدار production/main دارد
[ ] ۹. robots.txt پروداکشن هنوز Allow: / است
[ ] ۱۰. تب «مدیریت سایت» بدون بنر قرمز «فایل قدیمی» باز شد
[ ] ۱۱. دکمهٔ خروج کنار نام کاربر و نقش/سمت زیر نام دیده می‌شود
```

---

## پیوست — چه چیزی راستی‌آزمایی شد

**اثبات‌شده با اجرا در این نشست:**

| بررسی | نتیجه |
|---|---|
| هر دو پچ معلق با هم، تمیز اعمال می‌شوند | ✅ `git apply` بدون خطا |
| نتیجهٔ پچ‌ها == فایل‌های `docs-deploy/*.FIXED.yml` | ✅ `diff -q` یکسان |
| YAML هر دو ورک‌فلوی حاصل | ✅ معتبر (۱۸ و ۱۹ گام) |
| `bash -n` روی **همهٔ** بلوک‌های `run` | ✅ پاک |
| `tester536` (اتصال گیت‌ها به CI) | ✅ **۶۰ PASS / ۰ FAIL** |
| `tester540` در حالت «وصلهٔ معلق» (شبیه‌سازی اعمال پچ) | ✅ **۲۶ PASS / ۰ FAIL** |
| `tester540` در حالت «wired» (ورک‌فلوی پچ‌شده) | ✅ **۲۴ PASS / ۰ FAIL** |
| کل گیت CI با ورک‌فلوهای پچ‌شده | ✅ **۲۰۲ PASS / ۰ FAIL** |
| شبیه‌سازی لایهٔ ۱ readback با `FILES` چندخطی | ✅ **۵ فایل بررسی شد، fail=0، exit=0** |
| پایداری نگهبان معماری | ✅ ۱۰ اجرای پیاپی، هر دو حالت، exit=0 |

**اندازه‌گیری‌شده:**

- بار کامل FTP (در حالت fallback): **۱۴۶۴ فایل / ۸۲٫۹ مگابایت**
  (jpg ۳۸٫۰ + html ۲۰٫۸ + webp ۹٫۳ + js ۵٫۵ + png ۴٫۱ + jpeg ۲٫۶ + php ۱٫۱ + بقیه)
- بنر: **۶۷۳ فایل → ۸ فایل**؛ یعنی ۲۰٫۷۵ مگابایت آپلود بیهوده در هر اجرا حذف شد
- سه اجرای واقعی استیجینگ پیش از این: ۱۰m۳۶s / ۱۱m۲۳s / ۱۱m۵۸s

**برآوردی (از این محیط قابل سنجش نبود):** زمان آپلود FTP و `apt-get`. افت به ~۲ تا ۳ دقیقه را
از اولین اجرای دوم به بعد خودتان روی Actions می‌سنجید.

**چیزی که عمداً دست نخورد:** سوئیت تسترهای گیت CI (۲۰۲ تستر، ۵۴٫۱ ثانیه سری) موازی‌سازی نشد.
اندازه گرفتم: ۱۰ تستر کند ۸۱٪ زمان‌اند و سه‌تایشان ذاتاً از زنجیرهٔ `setTimeout(0)` استفاده می‌کنند؛
موازی‌سازی روی رانر ۲ هسته‌ای فقط ~۵٪ سود داشت و ۵ تستر فایل می‌نویسند، پس خطر خرابی داشت.
پوشش کامل تست‌ها ارزشش بیشتر از یک دقیقه صرفه‌جویی است.
