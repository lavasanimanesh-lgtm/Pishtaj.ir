# RELEASE NOTES — v34.7.33 (۱۴۰۵/۰۵/۲۶ — 2026-08-17)

مجوز کارفرما برای موارد بازِ نیازمند تصویب.

## H1 — گیت کیفیت در GitHub Actions

- `php.yml` دیگر `composer validate` روی مخزن بدون Composer نیست: `php -l` + `node --check` + `run-ci-gate.js`.
- `deploy-staging.yml` و `deploy-production.yml` **قبل از FTP** همان گیت را اجرا می‌کنند.
- `api/migrate.php` و `api/data-health-check.php` از پروداکشن exclude شدند.

> اعمال روی GitHub نیازمند push فایل‌های `.github/workflows` است.

## AN-04 — کالیبراسیون امتیاز سلامت (مصوب)

| پارامتر | قبل | بعد |
|---|---|---|
| پایه | ۵۰ | **۴۵** |
| سقف اثر برد | +۲۰ | **+۲۵** |
| وصول | حول ۵۰٪، −۲۵…+۲۰ | حول **۵۵٪**، −۲۰…+۲۰ |
| جریمه کنترل | −۲۰ | **−۲۲** |
| پوشش ناقص | −۵ | **−۸** |
| آستانه مناسب / پیگیری | ۷۰ / ۴۵ | **۷۲ / ۴۸** |

رتبه‌بندی همچنان کمکی است، نه قضاوت قطعی.

## ARCH-02 — گام اول یکپارچه‌سازی نوشتن پول

وصول **نقد / حواله / سایر** از پنل مطالبات، اگر فاکتور به پرونده وصل باشد (یا با شمارهٔ پیشنهاد به‌طور یکتا وصل شود)، دیگر `payments[]` محلی نمی‌نویسد؛ فرمان سروری `post_receipt` اجرا می‌شود (fail-closed).

**چک** عمداً در مسیر قبلی می‌ماند تا سند مستقل ماژول چک ساخته شود.

مسیرهای باقی‌ماندهٔ ARCH-02 (وصول چک پس از پاس شدن، `savePay` روی فاکتور بی‌پرونده): هنوز legacy.

## آزمون

`tester427` با وزن‌های جدید هم‌تراز شد. `tester436` برای H1/AN-04/ARCH-02 اضافه شد.
