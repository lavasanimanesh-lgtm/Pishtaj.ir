# 🔍 گزارش ارزیابی فنی ریلیز ۵۰ — pishtaj.ir
**تاریخ ارزیابی:** ۱۲ تیر ۱۴۰۵ (2026-07-03)
**ارزیاب:** AI Agent — Arena.ai (ایجنت جدید، ادامه‌دهنده مسیر)
**مبنا:** `pishtaj-release-final-v50.zip` (۶.۹MB — ۵۴۵ فایل — ۴۰۳ صفحه HTML)

---

## ۱. خلاصه وضعیت کلی

| معیار | وضعیت | توضیح |
|:------|:-----:|-------|
| ساختار پروژه | ✅ سالم | مطابق سند HANDOVER — استاتیک HTML5/CSS3/ES6 + PHP سبک |
| اسناد انتقال | ✅ کامل | HANDOVER-FINAL-v50 + ۳۰ فایل Release Notes موجود است |
| مرکز دانش | ⚠️ نیاز به اصلاح | ۳۶۱ مقاله؛ اما **۱۱۱ مقاله زیر ۸۰۰ کلمه** (خلاف استاندارد ۱۵۰۰+) |
| سئو / Sitemap | ⚠️ عقب‌افتاده | sitemap فقط ۲۷۹ URL دارد؛ **۱۲۵ صفحه در sitemap نیستند** |
| لینک‌های داخلی | ❌ خرابی | **۷۳ لینک شکسته (۲۶ مقصد یکتا)** شامل لوگو و فونت |
| امنیت CRM | ❌ ضعیف | رمز ادمین به‌صورت متن ساده در JS (`ADMIN_PASS='ptf2026'`) |
| نسخه انگلیسی | ⚠️ حداقلی | ۴ صفحه بسیار سبک (۵-۸KB) — نیاز به تکمیل |

**جمع‌بندی:** پروژه در وضعیت خوبی تحویل گرفته شد، اما اسپرینت‌های ۴۴ تا ۵۰ با عجله بسته شده‌اند و **بدهی فنی (Technical Debt)** قابل‌توجهی باقی مانده که باید قبل از پیشروی به اسپرینت ۵۱ پاکسازی شود.

---

## ۲. یافته‌های تفصیلی

### 🔴 بحرانی (باید فوراً حل شود)

1. **فایل‌های حیاتی گم‌شده — لینک به آنها در همه صفحات وجود دارد:**
   - `assets/images/ptf-logo.png` — لوگوی شرکت **در پکیج نیست** (۳۳+ صفحه به آن لینک داده‌اند)
   - `assets/fonts/Vazirmatn-Regular.woff2` و `Vazirmatn-Bold.woff2` — **پوشه fonts کلاً وجود ندارد** (فونت احتمالاً از CDN لود می‌شود، اما preload به فایل محلی اشاره دارد → خطای 404 و اتلاف Core Web Vitals)
   - `assets/images/favicon.png` — موجود نیست (۸ صفحه)
   - `assets/images/real/webp/real-hero-energy-plant.webp` — تصویر hero صفحه اصلی!
   - `site.webmanifest` — موجود نیست

2. **۲۹ مقاله «z-*.html» بی‌کیفیت در مرکز دانش:** فایل‌هایی با نام هش (مثل `z-85401d51.html`) با محتوای **۶۴ تا ۸۰ کلمه** — احتمالاً خروجی ناقص تولید انبوه اسپرینت ۴۴. این‌ها از نظر گوگل thin content هستند و به سئو آسیب می‌زنند. → یا تکمیل شوند یا حذف + ریدایرکت.

3. **صفحات لینک‌شده اما ناموجود (404 واقعی در سایت لایو):**
   - `about/logo-philosophy/`، `about/management-message/`، `about/organizational-chart/`
   - `services/piping-equipment/`، `electrical-equipment/`، `instrumentation-equipment/`، `pumps/`، `compressors/`، `boilers/`
   - باگ تمپلیت: `${a.u}` به‌صورت خام در href دو صفحه (blog/index و یک صفحه دیگر) → باگ JS رندر

### 🟠 مهم

4. **Sitemap عقب‌افتاده:** ۱۲۵ صفحه شامل ۱۱۲ مقاله KC، ۶ صفحه خدمات، ۴ صفحه EN، صفحه news و ۲ پست بلاگ در sitemap نیستند. خروجی اسپرینت‌های ۴۴/۴۷/۴۸/۴۹ ایندکس نمی‌شود.

5. **امنیت CRM:** رمز `ptf2026` هاردکد در JS + ذخیره رمز کاربران به‌صورت plain در localStorage. حداقل: هش SHA-256 سمت کلاینت + انتقال احراز هویت به `api/crm.php`.

6. **۱۱۱ مقاله KC زیر ۸۰۰ کلمه** (میانه کل: ۱۴۶۲ کلمه) — خلاف استاندارد اعلام‌شده «۱۵۰۰+ کلمه» در HANDOVER.

### 🟡 قابل بهبود

7. نسخه انگلیسی بسیار حداقلی است (میانگین ~۶KB در برابر ۷۸KB صفحه اصلی فارسی) و hreflang فقط در index.
8. `assets/downloads/` فقط دو فایل `.txt` دارد در حالی که به‌عنوان «کاتالوگ و هندبوک» معرفی شده — کاتالوگ PDF واقعی لازم است.
9. تصویر «رئیس هیئت مدیره» (اسپرینت ۵۰) در پکیج یافت نشد — باید صحت صفحه certificates بررسی شود.

---

## ۳. دارایی‌های سالم تأییدشده ✅

- ۴۳ لوگوی برند (SVG/PNG) در `assets/images/brands/` + نسخه WebP
- ۱۰ تصویر صنعتی + WebP در `assets/images/real/`
- ۱۱ مقاله وبلاگ کامل + ۶ صفحه خدمات اختصاصی + ۴ صفحه محصول
- CRM ۹ ماژوله (۳۷.۵KB) + API با HMAC در `api/crm.php` + htaccess امنیتی
- robots.txt، htaccess کش/فشرده‌سازی، Schema.org، Open Graph
- سامانه‌های rfq / tracking / supplier / assistant / news فعال

---

## ۴. پیشنهاد اسپرینت ۵۱ — «پاکسازی بدهی فنی و تثبیت» (Stabilization)

طبق اصل حرفه‌ای، قبل از فیچر جدید باید خرابی‌های ریلیز قبل بسته شود:

| # | کار | اولویت |
|:-:|-----|:------:|
| 1 | بازسازی لوگو، favicon، فونت‌های محلی، تصویر hero و webmanifest | 🔴 |
| 2 | رفع همه ۷۳ لینک شکسته + ساخت یا حذف ۹ صفحه ناموجود | 🔴 |
| 3 | تعیین تکلیف ۲۹ مقاله z-* (تکمیل به ۱۵۰۰ کلمه یا حذف) | 🔴 |
| 4 | بازتولید sitemap.xml کامل (~۴۰۰ URL) | 🟠 |
| 5 | رفع باگ تمپلیت `${a.u}` در بلاگ | 🟠 |
| 6 | سخت‌سازی لاگین CRM (هش رمز) | 🟠 |

سپس اسپرینت‌های ۵۲ تا ۷۰ طبق رودمپ مصوب ادامه می‌یابد.

---
*این گزارش مبنای شروع کار ایجنت جدید است و در ریشه پروژه نگهداری می‌شود.*
