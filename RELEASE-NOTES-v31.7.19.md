# Release Notes — v31.7.19

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.19
**نوع:** 🟡 سه مورد گزارش‌شده کارفرما — نام سنددار PDFها + آیکون مینیمال موجودی انبار + نظم نهایی هدر موبایل (US-PDF-NAME + BUG-ICON-STORE-001 + BUG-HDR-MOBILE-002)

---

## 🎯 مسائل (به گزارش کارفرما با اسکرین‌شات)

1. فایل‌های PDF دانلودی همه با نام «سامانه مدیریت پیشرو تجهیز فرتاک» ذخیره می‌شوند — هیچ مشخصه‌ای برای تمایز ندارند؛ باید شماره سند خودشان را داشته باشند.
2. آیکون «موجودی انبار» مینیمال نیست (ایموجی رنگی 🏬 در کنار آیکون‌های خطی SVG).
3. آیکون‌های هدر موبایل نامنظم‌تر شدند (بج نسخه بریده «.7.18»، بج زنگ ۶۷ نیمه‌بریده).

## 🔍 علت ریشه‌ای (RCA)

### US-PDF-NAME — نام PDF از کجا می‌آید؟
نام پیش‌فرض PDF مرورگر از `<title>` صفحه چاپ می‌آید. اسناد اصلی (TO/CO/TC، نامه، قرارداد، بسته‌بندی، درخواست تامین، اقلام استعلام) از قبل title سنددار داشتند. حفره‌ها: **چک صیادی** («چاپ چک صیادی» ثابت)، **گزارش تحلیلی** («گزارش تحلیلی» ثابت)، و مهم‌تر: چاپ‌هایی که با `window.print()` روی **صفحه اصلی** انجام می‌شوند (سال مالی، امتیازها، پیش‌نویس نامه دستیار) که با عنوان کلی «CRM | سامانه مدیریت | پیشرو تجهیز فرتاک» ذخیره می‌شدند — همان چیزی که کارفرما دید.

### BUG-ICON-STORE-001
موتور iconx (US-284) ایموجی‌ها را به SVG خطی تبدیل می‌کند اما `🏬` در جدول نگاشتش نبود → ایموجی رنگی سیستم‌عامل کنار آیکون‌های مینیمال می‌نشست.

### BUG-HDR-MOBILE-002 — اعتراف: عارضه اصلاح قبلی خودمان
`overflow:hidden` سراسری که در v31.7.18 برای مهار بیرون‌زدگی گذاشتیم، **خودش بج نسخه را از وسط می‌برید و بج قرمز زنگ (۶۷) را clip می‌کرد** — دقیقاً اسکرین‌شات کارفرما. درس: مهار سرریز باید در سطح فرزندان (ellipsis/min-width) باشد، نه clip والد.

## ✅ اصلاحات

| # | فایل | تغییر | شدت |
|---|------|--------|-----|
| 1 | `crm/ui-kit.js` | ✨ helper سراسری `ptfPrintWithTitle(docNo)`: عنوان سند را موقتاً st شماره سند می‌کند، پس از چاپ (afterprint + fallback) عنوان برنامه برمی‌گردد | 🟡 P1 |
| 2 | `crm/cheques.js` | چاپ چک: عنوان = `CHQ-{شماره صیادی/چک}` (تا ۳ چک در دسته) | 🟡 P1 |
| 3 | `crm/analyzer.js` | گزارش تحلیلی: `ANL-{تاریخ شمسی}` | 🟡 P1 |
| 4 | `crm/fiscal.js` | هر دو گزارش سال مالی: `FIS-{سال}` | 🟡 P1 |
| 5 | `crm/scoring.js`, `crm/ai-workbench.js` | `SCORE` و `LTR-DRAFT` | 🟢 P2 |
| 6 | `crm/iconx.js` | آیکون خطی جدید `store` (فروشگاه با سایه‌بان) + نگاشت `🏬` و `🧱` — موجودی انبار حالا هم‌خانواده بقیه آیکون‌های مینیمال | 🟡 P1 |
| 7 | `crm/mobilenav.js` | حذف `overflow:hidden` سراسری هدر؛ بج نسخه ellipsis با `max-width` و LTR (نسخه از سمت درست بریده نمی‌شود)؛ tbic با `overflow:visible` (بج زنگ سالم)؛ خلوت‌سازی هدر موبایل: بایگانی/ابر مخفی (از کشوی «سایر» در دسترس‌اند) | 🔴 P0 |
| 8 | ۴ نقطه نسخه | v31.7.19 | — |

## 📊 نتایج گیت

- تستر جدید `tester194`: **۱۶/۱۶ PASS** — شامل تست رفتاری `ptfPrintWithTitle` (عنوان حین چاپ = شماره سند، بازگشت پس از چاپ) و رگرسیون titleهای سنددار موجود
- رگرسیون کامل: **172/172 فایل PASS — 3981/3981 چک PASS**
- `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| اسناد اصلی (CO/TO/نامه/قرارداد…) | از قبل سنددار بودند — دست‌نخورده | صفر |
| چاپ صفحه اصلی | فقط تعویض موقت title؛ منطق چاپ همان است | ناچیز |
| هدر دسکتاپ | بدون تغییر (قواعد فقط @media موبایل) | صفر |
| بایگانی/ابر در موبایل | از هدر حذف، از کشوی «سایر» در دسترس | ناچیز |

## 📋 راستی‌آزمایی توسط کارفرما (۴ دقیقه)

1. **PDF:** یک پیشنهاد را چاپ/ذخیره PDF کنید → نام پیشنهادی فایل = شماره سند (مثل `CO-1405-77.pdf`)؛ همین را برای چک صیادی (`CHQ-…`) و گزارش سال مالی (`FIS-1405`) تکرار کنید.
2. **آیکون:** منو → «موجودی انبار» → آیکون خطی مینیمال هم‌خانواده بقیه (نه ایموجی رنگی).
3. **هدر موبایل:** بج «نسخه فعال: v31.7.19» کامل و تک‌خطی؛ بج قرمز زنگ کامل و گرد؛ هدر خلوت‌تر (جستجو/زنگ/تم)؛ هیچ بریدگی.

---
*توسعه‌دهنده: PTF Development Team — 2026-07-20*
