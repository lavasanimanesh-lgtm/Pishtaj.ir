# ارزیابی کار ایجنت قبلی — فاصلهٔ پروداکشن با استیجینگ و سه علامت گزارش‌شده

**تاریخ:** ۱۴۰۵/۰۵/۱۳ (۲۰۲۶-۰۸-۰۴)
**نویسنده ارزیابی:** Arena Agent (نشست فعلی)
**منبع داده:** بررسی کامل گیت (برنچ‌ها، PRها، تگ‌ها، ران‌های GitHub Actions)، کد دیپلوی‌شده در main، اسناد RELEASE-NOTES / BACKLOG / AUDIT، و گزارش‌های استیجینگ ثبت‌شده توسط ایجنت قبلی.

> **⚠️ به‌روزرسانی ۱۴۰۵/۰۵/۱۳ (~۱۰:۰۰):** پس از این ارزیابی، نشست بعدی PR #19 را مرج کرد (`ba2e9e9` — v34.0.2-alpha) و پروداکشن در ۰۹:۴۳ UTC دیپلوی شد (تگ `v2026.08.04-0943`). این PR هر سه شکافِ شناسایی‌شده در بخش ۳ را می‌بندد: (۱) بامپ نسخه + cache-busting کامل (همان `v34.0.1-alpha` که به main نرسیده بود، حالا با `v34.0.2-alpha` اعمال شد + نام کش SW)؛ (۲) رفع ردیف‌های تکراری گزارش تنخواه در `ptfPettyPeriodEvents`/`ptfPettyPeriodTotals` (حذف ردیف تراکنشِ متصل به هزینهٔ حاضر + جلوگیری از دوباره‌شماری پرداخت‌های مستقیم)؛ (۳) مهاجرت «حقوق سهامدار فاقد opex» از داخل اپ (`ptfMigrateShareholderOpex` + دکمهٔ «🛠 بازسازی حقوق سهامدار» در پنل هزینه‌های جاری). بازبینی این نشست: فیکس‌ها صحیح و کم‌ریسک‌اند؛ صرفاً یادآوری می‌ماند که **دکمهٔ بازسازی باید روی پروداکشن اجرا شود** و health-check بخش ۱۱ + وضعیت Imagick/S3 (ضمایم) همچنان باید راستی‌آزمایی شود.

---


## ۱) خلاصهٔ اجرایی

| پرسش | پاسخ مستند |
|---|---|
| آیا کد پروداکشن از کد استیجینگ **عقب‌تر** است؟ | **خیر — از نظر کد تقریباً یکسان است.** تنها تفاوت، رشتهٔ نسخه در `crm/index.html` است: پروداکشن `v34.0.0-alpha`، استیجینگ `v34.0.1-alpha` (۸۷ خط `?v=` باستر + رشتهٔ VER). همهٔ فایل‌های اجرایی (petty.js، shareholders.js، storage.js، sms.js، sw.js و…) بایت‌به‌بایت یکسان‌اند. |
| چه چیزی واقعاً دیپلوی شد؟ | امروز ۰۹:۰۲ UTC پروداکشن از `main@04ea070` (مرج PR #18) دیپلوی شد و تگ `v2026.08.04-0902` ساخته شد. پیش از آن، پروداکشن روی `bae63dc` (v33.22.3) بود (دیپلوی ۲۰۲۶-۰۸-۰۲ ۲۱:۵۶). |
| پس چرا «عقب‌تر» دیده می‌شود؟ | (۱) برچسب نسخه: ایجنت بامپ `v34.0.1-alpha` را **بعد از** مرج PR #18 (۰۸:۲۲ در برابر ۰۸:۲۰) روی برنچ push کرد؛ این بامپ هرگز در main نرفت ولی روی استیجینگ (پوش خودکار برنچ‌های arena/**) اعمال شد. (۲) کش PWA/مرورگر: بلافاصله پس از دیپلوی، مرورگر کاربر ممکن است هنوز نسخهٔ قبلی (v33.22.3 با همان سه باگ) را اجرا کند. (۳) تفاوت‌های **داده و محیط** پروداکشن در برابر فیکسچر استیجینگ — که ریشهٔ واقعی دو علامت از سه علامت است. |
| ریشهٔ هر علامت چیست؟ | ضمایم ← محیط/دادهٔ پروداکشن (Imagick/S3، ردیف‌های کهنه DB، کش)؛ ردیف‌های تکراری گزارش تنخواه ← **باگ واقعی کد** در `ptfPettyPeriodEvents`/`ptfPettyPeriodTotals` (هزینهٔ تسویه‌شده دو ردیف می‌شود)؛ حقوق سهامداران در هزینه‌های جاری ← **اسکریپت مهاجرت داده روی پروداکشن اجرا نشده** (فیکس کد فقط آینده را درست می‌کند). |

---

## ۲) رویدادنگاری دقیق (با شواهد)

| زمان (UTC) | رویداد | شواهد |
|---|---|---|
| ۰۸-۰۲ ۲۱:۵۶ | دیپلوی v33.22.3 روی پروداکشن (گارد تازگی `ptf_db_read_fresh` + health-check بخش ۱۱) | run `30768990393` → `bae63dc8` |
| ۰۸-۰۳ ۰۵:۱۲ | مرج PR #17 (v33.22.4 — استثنای fx-cache در health-check) — **بدون دیپلوی** | commit `6533a2a` |
| ۰۸-۰۴ ~۰۵:۰۰–۰۸:۰۰ | ایجنت قبلی روی برنچ `arena/019fc8c9-pishtaj-ir` کار v34.0.0-alpha (فاز ۴ مالی) را انجام داد: F4-2 (تقویم)، F4-4 (فشرده‌سازی ضمایم)، F4-5 (لینک تنخواه↔پرونده)، F4-6 (رفع دو باگ گزارش بازه‌ای تنخواه)، F4-7 (حقوق سهامدار در opex + اسکریپت مهاجرت)، F4-10 (بازهٔ ۳۰ روزه)، F4-12، F4-13 (رکوردهای میلادی)، F4-9+F4-11 (چیدمان PDF + لیست سفید ضمیمه) — هر push به‌صورت خودکار روی استیجینگ دیپلوی شد | ران‌های deploy-staging از `30880816058` تا `30891667595`؛ کامیت‌های `f0b512a`..`4e4d754` |
| ۰۸-۰۴ ۰۸:۲۰ | مرج PR #18 در main (squash → `04ea070`). هد PR فقط `4e4d754` بود؛ **بامپ `v34.0.1-alpha` (کامیت `5ea39cf`) در PR نبود** | mergeCommit=`04ea070`، headRefOid=`4e4d754` |
| ۰۸-۰۴ ۰۸:۲۲ | ایجنت بامپ `v34.0.1-alpha` را روی برنچ push کرد (بعد از مرج!) → استیجینگ `v34.0.1-alpha` شد، main `v34.0.0-alpha` ماند | commit `5ea39cf` (۰۸:۲۲:۱۳) + run استیج `30891667595` |
| ۰۸-۰۴ ۰۹:۰۲ | دیپلوی پروداکشن از `main@04ea070` → تگ `v2026.08.04-0902` | run `30894513797` (headSha=`04ea070…`) |

**نتیجهٔ خالص:** پروداکشن از نظر فایل‌های اجرایی = استیجینگ (v34.0.0-alpha ≡ v34.0.1-alpha به‌جز برچسب). پس سه علامتی که در پروداکشن می‌بینید **ناشی از کمبود کد نیست**؛ ناشی از (الف) داده/محیط پروداکشن، (ب) باگی که در استیجینگ هم هست ولی با دادهٔ فیکسچر دیده نشده، و (ج) کش مرورگر است.

---

## ۳) بررسی سه علامت

### علامت ۱: ضمایم در پروداکشن نمایش داده نمی‌شود (در استیجینگ نمایش داده می‌شود)

**وضعیت کد:** فیکس‌های مربوط به ضمایم (F4-6: باز شدن مودال «فیش‌های پیوست» — قبلاً `ReferenceError: arg` داشت؛ F4-9: چیدمان ۲-ستونه؛ F4-13: رکوردهای میلادی؛ و فیکس v2 که در خطای سرور، URL فایل حفظ می‌شود به‌جای خالی‌شدن) **همگی در main و پروداکشن هستند** (diff پروداکشن↔استیجینگ فقط index.html است).

**دلایل احتمالی باقی‌مانده (به ترتیب احتمال):**

1. **محیط سرور پروداکشن:** در گزارش استیجینگ خودِ ایجنت (F4-3، ثبت‌شده در `crm/BACKLOG-USER-2026-08-01.md`) آمده: «سرور هاست **Imagick ندارد** (یا مسیر فایل از S3/Aravan خوانده نمی‌شود) → `api/attachment-thumb.php` پاسخ خطا می‌دهد». فیکس v2 (حفظ URL + `<embed>` برای PDF و `<img>` برای HEIC) در همهٔ مرورگرها/پرینترها کار نمی‌کند و خود ایجنت F4-3 را **باز (🟠)** گذاشته. اگر هاست پروداکشن و استیجینگ متفاوت باشند، این دقیقاً تفاوت نمایش است.
2. **دادهٔ کهنه/گم‌شده در DB پروداکشن:** رخداد P0 هفتهٔ گذشته («ناپدید شدن ضمایم پس از سوییچ MySQL») در v33.22.3 با «گارد تازگی» درمان شد: اگر mtime فایل از `updated_at` ردیف DB جلوتر باشد، مقدار فایل سرو و DB خودترمیم می‌شود. ولی اگر ردیف DB **جدیدتر** از فایل باشد (مثلاً نوشتهٔ یک کلاینت کهنه با LWW)، گارد کاری نمی‌کند. ابزار تشخیص: `api/data-health-check.php` بخش ۱۱ (تطابق واقعی فایل↔DB) — باید **روی پروداکشن پس از دیپلوی امروز** اجرا شود (پروداکشن قبلاً v33.22.4 را نداشت؛ این نسخه خطای کاذب fx-cache را هم برطرف کرده).
3. **کش PWA/مرورگر:** پروداکشن تا امروز ۰۹:۰۲ روی v33.22.3 بود — دقیقاً نسخه‌ای که باگ P0 ضمایم را داشت. اگر مرورگر/تب کاربر هنوز JS قدیمی را اجرا می‌کند (Service Worker با skipWaiting/network-first معمولاً خودش را تازه می‌کند، ولی تب باز یا کش مرورگر می‌تواند بماند)، همان علائم قدیمی دیده می‌شود. **تست:** `Ctrl+Shift+R` یا باز کردن `crm/clear-cache.html` و چک «نسخهٔ فعال» در سایدبار (باید `v34.0.0-alpha` باشد).

**اقدام:** (۱) رفرش کامل؛ (۲) اجرای health-check بخش ۱۱ روی پروداکشن؛ (۳) بررسی `api/attachment-thumb.php` و وضعیت Imagick + کانفیگ آروان (`storage-config.php`) روی پروداکشن در برابر استیجینگ؛ (۴) در صورت مغایرت داده → اجرای مراحل ۳–۶ ویزارد `api/migrate.php` (انتقال دسته‌ای idempotent است).

---

### علامت ۲: گزارش دوره‌ای تنخواه — ردیف‌ها تکراری دیده می‌شوند ولی جمع مبالغ صحیح است

**این یک باگ واقعی کد است** که در `crm/petty.js` (نسخهٔ فعلی main = استیجینگ) وجود دارد و با دادهٔ واقعی (هزینه‌های تسویه‌شده/پرداخت مستقیم) ظاهر می‌شود؛ با فیکسچر کم‌دادهٔ استیجینگ ممکن است دیده نشود:

- **`ptfPettyPeriodEvents` (خط ۷۴۴):** ردیف‌های گزارش از دو منبع ساخته می‌شوند: تراکنش‌ها (`d.tx`) و رکوردهای هزینه (`d.petty`) — **بدون حذف همپوشانی**. اما هر هزینه‌ای که «تسویه» شود (`pettySettle`) **هم یک رکورد هزینه می‌سازد و هم یک تراکنش `settle`** (با `acctTx`)، و هر «پرداخت مستقیم» (`pettyDirectPay`) **هم رکورد هزینهٔ `payMode:'direct'` و هم تراکنش `direct`**. نتیجه: همان یک هزینه در گزارش **دو ردیف** می‌شود («هزینه — تسویه در …» + «تسویه از حساب»)، که کاملاً شبیه «ردیف تکراری» است.
- **چرا جمع صحیح است؟** `ptfPettyPeriodTotals` (خط ۷۸۷): `totalOut = pettyOut + directOut` که در آن تراکنش‌های `settle` **شمرده نمی‌شوند** — پس برای هزینه‌های تسویه‌شده، مبلغ فقط یک‌بار (از ردیف هزینه) در جمع می‌آید و جمع درست می‌ماند؛ دقیقاً مطابق گزارش شما.
- **نکتهٔ جانبی (باید چک شود):** برای «پرداخت مستقیم» همین الگو جمع را **دوبرابر** می‌کند (`pettyOut` + `directOut` هر دو شاملش هستند). اگر دوره‌ای پرداخت مستقیم داشته باشد، جمع فعلاً اشتباه است — این هم باید در همان فیکس اصلاح شود.
- **احتمال مکمل:** وجود رکوردهای واقعاً تکراری در دادهٔ پروداکشن (همان `cd`) ناشی از تاریخچهٔ sync/LWW. کد همین باگ را مستند کرده: `BUG-SYNC-DUP-COLLAPSE`/`BUG-DUP-GROW-001` در `sync.js` — و نکتهٔ مهم: کلیدهای `ptf_crm_petty`/`ptf_crm_petty_tx` در لیست «ادغام متعارف» (collaps) قرار ندارند و فقط رکوردهای **کاملاً یکسان** dedup می‌شوند (`ptfMergeNoCollapse`). باید روی پروداکشن اسکن شود: آیا `cd` تکراری در این دو کلید هست؟

**فیکس پیشنهادی (کد):** در `ptfPettyPeriodEvents`، وقتی رکورد هزینه `acctTx` دارد (یا تراکنشی با `ref === cd` موجود است)، ردیف تراکنشِ متناظر ادغام/حذف شود (یا به‌جای دو ردیف، یک ردیف با ستون «نحوهٔ پرداخت» کامل شود)؛ و در `ptfPettyPeriodTotals` پرداخت‌های مستقیم فقط یک‌بار شمارش شوند (مثلاً حذف `payMode==='direct'` از `pettyOut` چون `directOut` آن را دارد).

---

### علامت ۳: حقوق سهامداران در «هزینه‌های جاری» پروداکشن نمایش/محاسبه نمی‌شود (استیجینگ درست است)

**وضعیت کد:** فیکس F4-7 (کامیت `88fee24`) در main و پروداکشن است: `ensureSalaryTxForMonth` در `crm/shareholders.js` حالا در **همهٔ مسیرها** opex متناظر را ایجاد می‌کند (اگر مبلغ مساوی بود ولی opex نبود هم). اما این فقط **از این پس** از قلم‌افتادن جلوگیری می‌کند.

**دلیل قطعی عدم نمایش در پروداکشن:** ردیف‌های opexِ ازدست‌رفتهٔ **تاریخی** (که به دلیل باگ F4-7 در نسخه‌های قبلی ساخته نشده‌اند) باید با اسکریپت مهاجرت `crm/_tools/migrate_shareholder_opex.js` بازسازی شوند — این اسکریپت **دستی** است (در کنسول مرورگر paste می‌شود) و توسط PR #18 به ریپو اضافه شده، اما **روی پروداکشن اجرا نشده است** (روی استیجینگ اجرا شده — به همین دلیل استیجینگ درست نمایش می‌دهد). در پروداکشن، هیچ کس آن را اجرا نکرده، پس txهای حقوق ماه‌های قبل، opex ندارند → در هزینه‌های جاری دیده نمی‌شوند و در محاسبات سال مالی (fiscal) لحاظ نمی‌شوند.

**اقدام:** در پروداکشن، پنل CRM را باز کنید (F12 → Console) و محتوای `crm/_tools/migrate_shareholder_opex.js` را اجرا کنید؛ سپس `ptfOpexRender` و محاسبات سال مالی را راستی‌آزمایی کنید. (اسکریپت idempotent است؛ `created>0` باید گزارش دهد.)

---

## ۴) ارزیابی فرایند ایجنت قبلی

### نقاط قوت (منصفانه)
- **انضباط ریشه‌یابی:** هر رخداد با مستندسازی کامل (RELEASE-NOTES، BACKLOG با ستون ریشه/وضعیت، تسترهای UAT، audit) پیگیری شده؛ مثال عالی: سه‌لایه‌بودن ریشه‌کن P0 ضمایم (گارد تازگی + سوییچ مشروط + health-check بخش ۱۱).
- **جریان استیجینگ-اول:** همهٔ تغییرات روی برنچ arena/** که به‌صورت خودکار استیجینگ را به‌روز می‌کند؛ ۵ گزارش استیجینگ از کارفرما ثبت و هر کدام به کامیت/فیکس وصل شده.
- **ابزارهای تشخیص دائمی:** tester304 (۱۳ تست)، health-check بخش ۱۱، ویزارد مهاجرت idempotent — سرمایهٔ بلندمدت پروژه.

### نقاط ضعف و ریشهٔ مشکلات فعلی
1. **آشوب نسخه‌بندی (ریشهٔ اصلی سردرگمی «عقب‌تر بودن»):** یک برنچ هم‌زمان v33.22.5 → v33.22.6 → v33.22.7 → v33.23.x → v34.0.0-alpha را حمل کرد؛ PR #18 با عنوان «v33.22.6» در واقع یک ریلیز کامل v34.0.0-alpha (۲۵ فایل، ۲۳۴۲+ خط) را squash کرد؛ و بامپ `v34.0.1-alpha` **بعد از** مرج push شد و در main نرفت. نتیجه: برچسب پروداکشن (v34.0.0-alpha) ≠ استیجینگ (v34.0.1-alpha) با وجود کد یکسان، و نام کش Service Worker پروداکشن (`ptf-crm-v34.0.0-alpha`) با استیجینگ ناهم‌خوان است.
2. **نبود گام «مهاجرت دادهٔ پروداکشن» در برنامهٔ ریلیز:** اسکریپت دستی F4-7 به ریپو اضافه شد ولی هیچ‌جا به‌عنوان گام الزامیِ «پس از دیپلوی پروداکشن» اعلام نشد — چک‌لیست PR #18 فقط تست استیجینگ داشت. همین باعث علامت ۳ شد.
3. **شروع آیتم‌های باز در ریلیز:** F4-3 (تبدیل ضمایم — سرور Imagick ندارد) و F4-9/F4-11 در بک‌لاگ «🟠 باز» بودند اما همان‌ها در PR #18 مرج و دیپلوی شدند؛ محدودیت محیط پروداکشن (Imagick/S3) هرگز راستی‌آزمایی نشد — ارتباط مستقیم با علامت ۱.
4. **تست فقط روی فیکسچر:** شرایط دادهٔ واقعی (تاریخ‌های میلادی پس از مهاجرت، هزینه‌های تسویه‌شدهٔ فراوان، رکوردهای تکراری از sync) در استیجینگ دیده نشد؛ F4-13 («از ۲ تیر هزینه‌ها دیده نمی‌شود») و حالا «ردیف‌های تکراری» نمونه‌های مشابه‌اند — هر دو پس از شکایت کاربر پیدا شدند.
5. **کش PWA پس از دیپلوی:** هیچ راهنمای «پس از دیپلوی: رفرش/پاک‌سازی کش» برای کارفرما در PR نبود؛ با توجه به اینکه پروداکشن از v33.22.3 (نسخهٔ دارای باگ P0 ضمایم) مستقیماً به v34 جهش کرد، یک رفرش ساده می‌تواند نصف علائم ظاهری را حذف کند.

---

## ۵) اقدامات پیشنهادی (به ترتیب اولویت)

**فوری — روی پروداکشن (بدون تغییر کد):**
1. رفرش کامل CRM (`Ctrl+Shift+R` / `crm/clear-cache.html`) و راستی‌آزمایی «نسخهٔ فعال: v34.0.0-alpha» در سایدبار.
2. اجرای اسکریپت مهاجرت `crm/_tools/migrate_shareholder_opex.js` در کنسول پروداکشن → بازگشت حقوق سهامداران در هزینه‌های جاری + محاسبات سال مالی.
3. اجرای `api/data-health-check.php` (بخش ۱۱) روی پروداکشن؛ در صورت مغایرت فایل↔DB → مراحل ۳–۶ ویزارد `api/migrate.php` (idempotent).
4. بررسی `api/attachment-thumb.php` (وضعیت Imagick) و خواندن فایل از آروان (`presign_get`) روی پروداکشن در برابر استیجینگ.
5. اسکن رکورد تکراری (`cd` تکراری) در `ptf_crm_petty` و `ptf_crm_petty_tx` پروداکشن.

**کد (PR بعدی):**
6. فیکس باگ ردیف‌های تکراری گزارش دوره‌ای تنخواه (dedup بر اساس `acctTx`/`ref` در `ptfPettyPeriodEvents` + شمارش یک‌بارِ پرداخت‌های مستقیم در `ptfPettyPeriodTotals`).
7. مرج بامپ نسخه به main (یکسان‌سازی `v34.0.1-alpha` یا بالاتر) تا برچسب/کش‌باستینگ پروداکشن و استیجینگ هم‌خوان شوند.
8. بستن F4-3/F4-11 (نصب Imagick روی هاست یا لیست سفید فرمت‌ها) — ضمایم PDF/HEIC.
9. افزودن «چک‌لیست پسادپلوی پروداکشن» به workflow (health-check + راستی‌آزمایی نسخه + مهاجرت‌های داده) و الزام تطابق `VER` در هر PR با برنچ/استیجینگ.

---

## ۶) پیوست: شواهد کلیدی

- `git diff origin/main origin/arena/019fc8c9-pishtaj-ir --stat` → فقط `crm/index.html` (۸۷+/۸۷− = بامپ نسخه).
- PR #18: head=`4e4d754`، merge=`04ea070`، merge time ۰۸:۲۰ UTC؛ بامپ `5ea39cf` در ۰۸:۲۲ (بعد از مرج).
- ران دیپلوی پروداکشن `30894513797` (۰۹:۰۲:۰۱–۰۹:۰۲:۵۰) با headSha=`04ea070…`؛ تگ `v2026.08.04-0902`.
- `6533a2a..04ea070`: ۲۵ فایل، +۲۳۴۲/−۱۸۹ (شامل petty.js +۴۵۳، shareholders.js +۴۱، sms.js +۳۵، storage.js +۳۴، index.html، sw.js، date-kit/finance-core/finance-helpers جدید، `crm/_tools/migrate_shareholder_opex.js`، ۲ تستر).
- بک‌لاگ F4-3/F4-9/F4-11 در `crm/BACKLOG-USER-2026-08-01.md` → وضعیت «🟠 باز».
- `crm/petty.js`: `ptfPettyPeriodEvents` خط ۷۴۴ (دو منبع بدون dedup)، `ptfPettyPeriodTotals` خط ۷۸۷ (`settle` در جمع نمی‌آید؛ `direct` دوبار می‌آید).
- `crm/sync.js`: `ptfMergeNoCollapse` برای کلیدهای مالی (بدون collapse متعارف) + مستندات BUG-SYNC-DUP-COLLAPSE / BUG-DUP-GROW-001.
