# 📦 Release Notes — v58.0
**اسپرینت ۵۸: نقش‌ها و سطوح دسترسی (US-122) + مرکز اعلانات و کارتابل (US-123)**
**تاریخ:** ۱۳ تیر ۱۴۰۵ (2026-07-03)

---

## ✅ US-122 — RBAC هشت‌نقشه (فایل جدید `crm/rbac.js`)

### ماتریس اجراشده (مطابق مصوبه کارفرما):
| نقش | تعریف کاربر | پنل‌ها | قیمت خرید | قیمت فروش | مالی/سود |
|---|:---:|---|:---:|:---:|:---:|
| ادمین | ✅ | همه | ✅ | ✅ | ✅ |
| رییس هیات مدیره | ✅ | همه | ✅ | ✅ | ✅ |
| مدیرعامل | ❌ | همه | ✅ | ✅ | ✅ |
| مدیر بازرگانی | ❌ | همه به‌جز مالی | ✅ | ✅ | ❌ |
| کارشناس فروش | ❌ | فروش/لید/پیشنهاد | ❌ | ✅ | ❌ |
| کارشناس خرید | ❌ | تامین/کالا/قیمت خرید | ✅ ثبت | ❌ | ❌ |
| حسابدار | ❌ | فاکتورها + مطالبات | ❌ | فقط ارجاعی | ❌ |
| تحصیلدار | ❌ | فقط مطالبات | ❌ | ❌ | ❌ |

### قابلیت‌های کلیدی:
- **AC1:** فرم کاربر جدید با نقش‌های ۸گانه + **موبایل و ایمیل الزامی** + رمز هش SHA-256 + حذف کاربر (فقط ادمین/رییس).
- **AC2 — پنل «🛒 قیمت‌های خرید»:** کارشناس خرید قیمت‌های تامین‌کنندگان را ثبت می‌کند → اعلان خودکار به نقش‌های ارشد؛ کارشناس فروش این پنل را نمی‌بیند و کارشناس خرید پنل پیشنهادها (قیمت فروش) را نمی‌بیند.
- **AC3 — جریان حسابدار:** دکمه «🧾 فاکتور» روی هر CO (فقط نقش ارشد) → ارجاع → CO در پنل «فاکتورها»ی حسابدار ظاهر می‌شود → دانلود CO → ثبت شماره/مبلغ/فایل فاکتور صادره + **هشدار مغایرت درصدی مبلغ فاکتور با CO**.
- **AC4 — مطالبات:** نوار پیشرفت وصول هر فاکتور (۰ تا ۱۰۰٪)، ثبت وصولی مرحله‌ای (با سقف مانده)، جمع مطالبات باز؛ **اطلاعات تماس طرف‌حساب فقط با چرخه درخواست ← تایید نقش ارشد** نمایش داده می‌شود.
- **AC5 — رد پا:** `ptf_crm_audit` — هر اقدام با تاریخ شمسی + نام + نقش، غیرقابل‌ویرایش از UI.
- **AC6:** گارد دسترسی در `goPanel` (کلاینت) + **گارد نقش سمت سرور در `api/crm.php`** برای اکشن‌های مالی/کاربران (ACL) — احراز هویت session کامل طبق رودمپ در اسپرینت ۶۹.
- **AC7:** کاربران قدیمی بدون roleId → نگاشت پیش‌فرض؛ admin → نقش ادمین خودکار.

## ✅ US-123 — کارتابل و اعلانات

- **AC1 — پنل «🗂 کارتابل من»:** اعلان‌های شخصی بر اساس نقش/نام کاربری، خوانده/نخوانده، بج شمارنده قرمز کنار منو، دکمه «برو» به پنل مرتبط.
- **AC2:** اعلان‌کننده گیرنده (نقش‌ها) و کانال‌های اضافه (پیامک/ایمیل) را هنگام رویداد انتخاب می‌کند — پیش‌فرض همیشه کارتابل.
- **AC3:** «⚙️ ترجیحات اعلان» شخصی: به تفکیک ۴ نوع رویداد، فعال‌سازی پیامک/ایمیل (کارتابل همیشه فعال).
- **AC4 — استثنای فاکتور:** ارجاع CO برای فاکتور **فقط و خودکار به حسابدار** اعلان می‌شود ✔
- **AC5:** تایید دسترسی تماس → اعلان به حسابدار/تحصیلدار؛ تسویه کامل → اعلان به نقش‌های ارشد.
- **AC6:** صف ارسال `ptf_crm_sendqueue` برای پیامک/ایمیل — آماده اتصال به وب‌سرویس (کاوه‌نگار/ملی‌پیامک) به‌محض دریافت کلید API از کارفرما؛ تا آن زمان fallback کارتابل فعال است.
- **AC7:** لاگ کامل اعلانات (که/کی/کانال/توسط چه نقشی) در audit.

## 🧪 تست‌ها — ۱۲/۱۲ PASS
- ماتریس ۸ نقش (T1a-h): دسترسی‌های مالی/کاربران/قیمت خرید/فروش/پنل‌ها همه صحیح ✔
- تشخیص نقش ارشد ✔ | صف پیامک فقط برای کانال انتخابی ✔
- فیلتر کارتابل بر اساس نقش (حسابدار فقط اعلان خودش) ✔ | audit با نقش و کاربر ✔
- سینتکس ۴ فایل JS (main/offers/leads/rbac) با Node ✔ | ممیز کل سایت PASS ✔

## 🚀 دیپلوی
فایل‌ها: `crm/index.html`، `crm/offers.js`، `crm/rbac.js` (جدید)، `api/crm.php`

## ⚠️ نکات
1. **پیامک/ایمیل:** صف ارسال آماده است اما ارسال واقعی نیازمند کلید API پنل پیامک + SMTP است — بدون آن همه اعلان‌ها در کارتابل تحویل می‌شوند.
2. **امنیت:** چون CRM فعلاً localStorage-محور است، RBAC کلاینت + ACL سروری پایه پیاده شد؛ استقرار کامل session سروری در اسپرینت ۶۹ انجام می‌شود (در رودمپ ثبت است).

## ⏭️ اسپرینت بعدی (۵۹)
US-107 فاز ۱ — منشی هوشمند: ویجت چت سایت + پایگاه دانش شرکت + موتور پاسخ محلی.
