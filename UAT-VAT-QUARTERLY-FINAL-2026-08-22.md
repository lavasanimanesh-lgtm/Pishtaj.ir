# UAT پایانی «ارزش افزوده فصلی» (VAT-LEDGER-003) — v34.7.94

- **تاریخ:** ۱۴۰۵/۰۵/۳۱ — 2026-08-22
- **دامنه:** ماژول‌های `crm/vat-shared.js` + `crm/vat-quarterly.js` + مسیرهای مصرف‌کننده (`official-invoice-v2.js`, `supplier-finance.js`).
- **نوع کار:** UAT رفتاری با دادهٔ مصنوعی روی هستهٔ محاسبات — بدون تغییر منطق موجود.
- **گیت CI:** ۱۱۲ PASS / ۰ FAIL (پس از افزودن `tester496`).

---

## ۱) هدف

قبل از این نسخه، `tester493` هشت سناریوی اصلی VAT فصلی را می‌سنجید. `tester496` این پوشش را به یازده سناریوی edge گسترش می‌دهد که در محیط واقعی محتمل‌اند اما در تست قبلی نیامده بودند. هدف این است که پیش از راه‌اندازی عملیاتی روی یک فصل کامل، هیچ رفتار غیرمنتظره باقی نماند.

## ۲) سناریوهای پوشش‌داده‌شده در `tester496`

| کد | سناریو | نتیجه |
|---|---|---|
| **E1** | فاکتور با تاریخ شمسی خالص (`invDate: '1405/02/10'`, `dateFa: '1405/03/20'`) | ✅ به فصل درست می‌رود |
| **E2** | فاکتور با تاریخ ISO خالص با سال شمسی (`1405-02-10`) | ✅ به فصل درست می‌رود |
| E2* | فاکتور با تاریخ ISO میلادی خام (`2026-06-15`) | ⚠️ **محدودیت مستند:** به سال شمسی 1405 mapped نمی‌شود (نیاز است تاریخ ورودی همیشه شمسی یا ISO با سال ایرانی باشد؛ این قرارداد سراسری پروژه است). |
| **E3** | نرخ VAT سالانه غیر ۱۰٪ (1404=9٪، 1405=10٪، 1406=11٪) | ✅ `ptfVatRateOf` هر سال نرخ صحیح را برمی‌گرداند؛ سال ناشناخته → پیش‌فرض `vatDefaultPct` |
| **E4** | فاکتور پوششی با کارمزد بزرگ‌تر از VAT (200k کارمزد، 100k VAT) | ✅ کارمزد جدا؛ اعتبار = 100k فقط؛ مانده صفر |
| **E5** | ثبت تسویه با هر مبلغی (حتی جزئی) → فصل «تسویه» شمرده می‌شود | ✅ رفتار فعلی مستند شد (طبق تصمیم کارفرما: «تسویه = ثبت مدرک»، محاسبهٔ ریالی جزئی نمی‌کند) |
| **E6** | حذف رکورد تسویه → `payable` دوباره برمی‌گردد | ✅ مقدار درست بازیابی می‌شود |
| **E7** | اعتبار مازاد در زمستان ۱۴۰۴ نباید به بهار ۱۴۰۵ منتقل شود | ✅ `prevCarry=0` در فصل ۱ سال جدید (تفکیک درست سال مالی) |
| **E8** | فاکتور پوششی بدون `coverVatAmount` (مقدار نامعتبر) | ✅ به‌صورت صفر شمرده می‌شود، crash نمی‌کند |
| **E9** | فاکتور با تاریخ خالی/خراب (`''`, `'invalid'`, `null`) | ✅ در هیچ فصلی شمرده نمی‌شود، crash نمی‌کند |
| **E10** | Carry سه‌فصلی: بهار (اعتبار 30k) + تابستان (اعتبار 20k) → پاییز (بدهی 200k) | ✅ `prevCarry=50k`, `payable=150k` — زنجیرهٔ `stateFor(s-1)` درست کار می‌کند |
| **E11** | پس از تسویهٔ پاییز، زمستان با carry=0 شروع شود | ✅ `prevCarry=0`, `prevDue=0` |

## ۳) یافته‌های تحلیلی (نه باگ)

### الف) محدودیت «تاریخ میلادی خام»
`ptfVatSeason` ابتدا با regex `(13|14)\d{2}` سال شمسی را استخراج می‌کند و اگر پیدا نشد، از `yearOfJ` که رشتهٔ قبل از `/` را برمی‌گرداند استفاده می‌کند. یک ISO میلادی مثل `2026-06-15` هیچ‌کدام را نمی‌گیرد و در نتیجه به فصل هیچ سال شمسی متصل نمی‌شود. این طبق قرارداد پروژه است (همه فرم‌ها تاریخ شمسی می‌گیرند و در ذخیره‌سازی به ISO با سال شمسی تبدیل می‌شوند)، ولی توصیه می‌شود:
- در فرم فاکتور فروش (`ptfOfficialInvoiceCalc`) و خرید (`slInvoiceForm`) اگر کاربر تاریخ میلادی وارد کرد، به‌طور شفاف اخطار داده شود.
- در گزارش «ارزش افزوده فصلی» یک شمارندهٔ «فاکتورهای بدون تاریخ فصلی» اضافه شود تا رکورد یتیم از دید مالی گم نشود.

### ب) رفتار «تسویه = ثبت مدرک»
E5 نشان می‌دهد که اگر کاربر تسویه‌ای با مبلغ کمتر از بدهی ثبت کند (مثلاً بدهی 100k ولی پرداخت 50k)، فصل به‌طور کامل «تسویه» شمرده می‌شود و مانده باقی‌مانده نمایش داده نمی‌شود. این طبق تصمیم فعلی کارفرماست (که تسویه یک ورودی «ثبت مدرک» است، نه محاسبهٔ خودکار مانده). اگر کارفرما بخواهد تسویهٔ جزئی هم پشتیبانی شود، تغییرات لازم:
1. در `stateFor` مبلغ تسویه با `payable` مقایسه شود.
2. `settled` فقط وقتی true شود که `sum(settlements.amount) >= payable`.
3. مانده باقی‌مانده به فصل بعد به‌صورت `prevDue` منتقل شود.

### ج) عدم انتقال carry بین سال‌ها
E7 عمداً بررسی می‌کند که کد فعلی اعتبار زمستان ۱۴۰۴ را به بهار ۱۴۰۵ منتقل **نمی‌کند** — این هم‌راستا با اکثر رویه‌های حسابداری ایران است (سال مالی جداست). اگر کارفرما بخواهد این رفتار عوض شود، `stateFor` باید در محدودهٔ سال بمانَد ولی برای فصل ۱ به `stateFor(y-1, 4)` هم نگاه کند.

## ۴) پوشش عملی که هنوز نیاز به UAT انسانی دارد

این تستر رفتار **هسته** را روی داده مصنوعی می‌سنجد. سه سناریو باقی مانده که فقط با کاربر مالی روی داده واقعی/استیجینگ راستی‌آزمایی می‌شود:

1. **UI هاب مالی → تب «ارزش افزوده»** — نمایش رنگ‌ها، دکمهٔ تسویه، دیالوگ پیوست مدرک روی مرورگر واقعی.
2. **آپلود مدرک تسویه** — پوشهٔ `vat-settlements/YYYY-S/` روی فضای ابری (نیازمند `storage-config.php` سالم روی سرور).
3. **پیوست به گزارش سالانه** — پس از تسویهٔ چهار فصل، ماژول «📁 اظهارنامه‌ها» بتواند سند سالانه را با اشاره به این چهار تسویه چاپ کند (هنوز پیاده نشده — بدهی باز).

## ۵) توصیه‌های بعدی (اختیاری، بدون کد این نشست)

- افزودن یک دکمهٔ «🔍 فاکتورهای بدون فصل» در تب VAT برای شناسایی رکوردهایی که تاریخ‌شان قابل تفسیر نیست (E9).
- افزودن نمای «تجمعی سال» زیر جدول فصلی: مجموع بدهی − اعتبار سال، برای تطبیق سریع با اظهارنامهٔ عملکرد.
- نوشتن یک تستر رفتاری برای مسیر ذخیره‌سازی نرخ سال (`ptfVatRateSet`) که تاریخچه‌نویسی و اعتبار نقش را بسنجد.

## ۶) اعتبارسنجی

```
$ node _tools/uat/tester496-v34.7.94-vat-quarterly-uat.js
PASS: 25 | FAIL: 0

$ node _tools/uat/run-ci-gate.js
=== CI gate: 112 PASS / 0 FAIL ===
همهٔ تسترهای گیت سبز — اجازهٔ FTP
```

- `tester496` به `_tools/uat/run-ci-gate.js` اضافه شد.
- بامپ نسخه: `v34.7.93` → `v34.7.94` در `VERSION.json`, `crm/index.html`, `crm/sw.js`, `crm/manifest.json`, `crm/clear-cache.html`, `crm/shell.js`, `api/sales-domain.php` و کلیهٔ تسترها.

## ۷) حکم

منطق هستهٔ VAT فصلی **پایدار و درست** است. سه رفتار «قابل تفسیر» (محدودیت تاریخ میلادی خام، تسویهٔ جزئی، عدم انتقال بین سال‌ها) به‌صورت مستند تثبیت شدند تا اگر بعداً تغییر لازم شد، تصمیم آگاهانه گرفته شود نه رگرسیون خاموش. راه‌اندازی عملیاتی روی یک فصل کامل بلامانع است.
