# 🩹 ریلیز v89.1 — تشخیص دقیق مشکل AI + استعلام متنی (US-206)

**تاریخ:** ۱۶ تیر ۱۴۰۵

---

## 🔬 مشکل AI — حالا دقیق می‌فهمیم کجاست

**پاسخ سوال شما:** بله، دقیقاً همین احتمال وجود دارد! نکته مهم: **«سبز» در تنظیمات فقط یعنی فایل کانفیگ و کلید روی سرور پیدا شده** — تماسی با گوگل نمی‌گیرد. اگر IP خروجی هاست از دید گوگل در منطقه پشتیبانی‌نشده باشد (ایرانی باشد یا رنج تحریمی)، کانفیگ سبز است ولی هر درخواست واقعی شکست می‌خورد — دقیقاً علائمی که شما می‌بینید.

### ✅ چه اضافه شد:
در **تنظیمات → باکس 🤖** دکمه جدید: **«🧪 تست واقعی اتصال»** — یک فراخوانی کوچک واقعی به Gemini می‌زند و خطای دقیق را به فارسی می‌گوید:

| تشخیص | معنی | اقدام |
|---|---|---|
| 🌍 «گوگل موقعیت IP سرور را نمی‌پذیرد» | IP هاست ایرانی/تحریمی است (حدس شما درست بوده) | از پشتیبانی هاست بپرسید IP خروجی چیست؛ یا provider را به واسطه OpenAI-compatible تغییر دهید (راهنمای انتهای llm-config.sample.php) |
| 🔑 کلید نامعتبر | کلید در کانفیگ ناقص کپی شده | کلید را دوباره بگذارید |
| ⏳ سهمیه (429) | سهمیه رایگان لحظه‌ای پر شده | کمی بعد |
| 📦 مدل اشتباه (404) | نام مدل در کانفیگ غلط است | `gemini-2.0-flash` |
| 🔌 عدم دسترسی خروجی | فایروال هاست، HTTPS خروجی بسته | پشتیبانی هاست |

**لطفاً بعد از نصب، این دکمه را بزنید و متن قرمز را برایم بفرستید** — با آن دقیقاً می‌گویم راه‌حل چیست. اگر IP مشکل بود، سوییچ به Groq یا واسطه ریالی فقط ۳ خط تغییر کانفیگ است (بدون تغییر کد).

## 📝 US-206 — استعلام متنی

- **فرم ثبت درخواست**: فیلد جدید «📝 متن استعلام» — متن ایمیل/واتساپ/پیامک کارفرما را همان‌جا paste کنید؛ با درخواست ذخیره می‌شود.
- **فهرست استعلامات**: درخواست دارای متن → دکمه **«📝 متن»** (مشاهده در مودال با حفظ قالب متن) + دکمه «📋 کپی متن» + دکمه **«📖 استخراج اقلام از این متن»** (اتصال به ویزارد خواندن استعلام).
- درخواست‌های موجود بدون متن → دکمه کم‌رنگ **«📝+»** برای افزودن متن بعداً.
- **خلاصه هوشمند AI** هم از متن استعلام استفاده می‌کند (وقتی اتصال درست شود، خلاصه دقیق‌تری از استعلام‌های متنی می‌دهد).

## ✅ کیفیت
tester31 جدید: **14/14 PASS** | رگرسیون ۲۱ تستر همه PASS | ممیز ۸بخشی PASS | کش → `ptf-crm-v89.1`

## 📦 نصب (File Manager)
**`pishtaj-release-v89.1-delta.zip`**: `api/llm.php` (تست واقعی)، `crm/bridge.js`، `crm/offerlock.js`، `crm/inqreader.js`، `crm/index.html`، `crm/sw.js` + مستندات.

> 🧪 اقدام فوری بعد از نصب: تنظیمات → 🤖 → **«🧪 تست واقعی اتصال»** → متن نتیجه را برایم بفرستید.
