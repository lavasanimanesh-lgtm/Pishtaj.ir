# 📦 Release Notes — v65.0
**اسپرینت ۶۵: تحلیلگر هوشمند CRM (US-111) + ثبت US-130 در بک‌لاگ**
**تاریخ:** ۱۴ تیر ۱۴۰۵ (2026-07-03)

---

## 📊 US-111 — پنل «تحلیلگر هوشمند» (فایل جدید `crm/analyzer.js`)

### AC1 — داشبورد تحلیلی:
- **قیف پیشنهادهای CO** با نوارهای بصری: پیش‌نویس/ارسال‌شده/برنده/بازنده + **نرخ برد** + ارزش کل بردها
- **کارت‌های کلیدی:** نرخ برد CO، نرخ تبدیل لیدها، میانگین روزهای تبدیل، مطالبات باز
- **پیش‌بینی مالی:** فاکتورشده / وصول‌شده / پایپ‌لاین (COهای ارسالی) / **پیش‌بینی وزنی** (پایپ‌لاین × نرخ برد تاریخی)
- **عملکرد منابع لید** با نرخ تبدیل هر منبع + معرفی بهترین منبع

### AC2 — پیشنهادات عملیاتی خودکار (۷ قاعده):
لید بی‌پیگیری +۷ روز | CO ارسالی بی‌تکلیف +۱۰ روز | فاکتور وصول‌نشده با مبلغ | درخواست دارای اقلام بدون پیشنهاد | یادآورهای عقب‌افتاده | پرونده تحویل جزئی | نامه در انتظار امضا — هر مورد با دکمه «برو ↗» به پنل مربوطه

### AC3 — تحلیل مالی:
مطالبات باز، جمع وصولی، ارزش بردها — همسو با ماتریس RBAC: **پنل فقط برای نقش‌های دارای دسترسی مالی** (ادمین/رییس/مدیرعامل)

### AC4 — Lead Scoring (امتیاز ۰-۱۰۰):
ارزش برآوردی (تا ۳۰) + صنعت هدف (۱۵) + منبع (معرفی ۱۵ > نمایشگاه ۱۰ > وب‌سایت ۸) + تعداد پیگیری (تا ۲۰) + پیشرفت مرحله (تا ۲۰) → جدول ۸ لید برتر با رنگ‌بندی سبز/زرد/خاکستری

### AC5 — موتور کاملاً محلی (rule-based، بدون API) ✅ | AC6 — **گزارش PDF** جامع با جدول‌های قیف/لید/مالی + فهرست اقدامات

## 📋 ثبت در بک‌لاگ — US-130: سیستم مدیریت یکپارچه (درخواست کارفرما)
- **Mini-CMS از داخل CRM:** افزودن/ویرایش/حذف اخبار، مقالات وبلاگ (با تولید صفحه از قالب + sitemap + ریدایرکت 301 هنگام حذف)، ویرایش بلوک‌های متنی صفحات داخلی
- **آپدیت CRM با zip از پنل تنظیمات:** آپلود ریلیز → اعتبارسنجی → **بکاپ خودکار** → استقرار → **Rollback** — بدون نیاز به FTP/هاست
- امنیت: فقط ادمین/رییس + تایید رمز مجدد + audit + `api/cms.php` و `api/updater.php` سروری
- ⚠️ پیش‌نیاز: ZipArchive و مجوز نوشتن PHP روی هاست (در شروع اسپرینت ۶۹ بررسی می‌شود؛ در نبود مجوز، حالت «بسته آماده + راهنما»)
- **زمان‌بندی: اسپرینت ۶۹** (همراه امنیت سروری — سنخیت کامل)

## 🧪 تست‌ها — ۱۰/۱۰ PASS با داده واقعی‌نما
نرخ برد ۶۷٪ ✔ ارزش بردها ✔ نرخ تبدیل ۵۰٪ و میانگین ۲۵ روز ✔ بهترین منبع «معرفی» ✔ پیش‌بینی وزنی (۴M×۶۷٪) ✔ **امتیاز لید = ۷۲ دقیقاً طبق فرمول** ✔ هر ۴ قاعده پیشنهادات (درخواست بی‌پیشنهاد/فاکتور باز/نامه معطل/پرونده جزئی) ✔ + ممیز سایت PASS ✔

## 🚀 دیپلوی
جدید: `crm/analyzer.js` | تغییر: `crm/index.html`

## ⏭️ اسپرینت بعدی (۶۶)
US-119 (دستیار قرارداد rule-based فا/EN) + US-126 (ابزارهای مهندسی: سایزینگ ولو/PRV، وزن، تحمل فشار، ترک‌تیبل + PDF)
