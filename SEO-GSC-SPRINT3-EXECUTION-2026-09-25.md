# اجرای اسپرینت ۳ سئو — ۲۰۲۶-۰۹-۲۵ (گپ‌های محتوایی: تست PE، جدول استنلس، خوشهٔ واردات/ترخیص)

ادامهٔ `SEO-GSC-SPRINT1...` و `SEO-GSC-SPRINT2...`. هدف: پوشش کوئری‌هایی که ایمپرشن می‌گیرند ولی صفحهٔ متناسب ندارند (گپ محتوا).

## ✅ کارهای اجراشده

### ۱. مقالهٔ جدید: تست هیدرواستاتیک لوله پلی‌اتیلن
`knowledge-center/pe-pipe-hydrostatic-test-guide.html` — **اولین محتوای PE سایت** (کوئری «تست هیدرواستاتیک پلی اتیلن» تا امروز صفحهٔ متناسب نداشت).

- ۹ بخش اصلی + ۵ FAQ + ۲ جدول (کلاس‌های SDR/PN برای PE80 و PE100 + مقایسهٔ تست PE با فولادی)
- محتوای فنی: MRS/MRS=۸/۱۰، فرمول MOP از SDR، فشار تست ~۱٫۵×PN/MOP، مراجع ISO 4427-5 و ASTM F2164، ضریب تصحیح دما (ISO 13761)، رفتار ویسکوالاستیک و ارزیابی با آب جبرانی، تفاوت تست میدانی/آزمایشگاهی (ISO 1167/9080)، مرحله‌بندی اجرا + چک‌لیست
- ساختار کامل مطابق الگوی مقالات موفق: TOC، byline، breadcrumb، JSON-LD سه‌گانه (Breadcrumb+Article+FAQPage)، لینک‌باکس RFQ، CTA
- ثبت در **sitemap + search-index + هاب مرکز دانش** (دستهٔ پایپینگ)

### ۲. جدول گریدهای استنلس استیل (کوئری «جدول استنلس استیل»)
- در `stainless-steel-pipe-guide.html` (صفحهٔ راهنمای انتخاب گرید): جدول ۷ گرید A312 — TP304/304L/316/316L/321/347 + Duplex 2205 با ترکیب، ویژگی و سرویس معمول + لینک متقابل به مقایسهٔ 304/316 و صفحهٔ محصول
- تا قبل از این، هیچ صفحه‌ای جدول جامع گریدها نداشت.

### ۳. خوشهٔ واردات/ترخیص
- `kc-customs-clearance.html` (canonical) و `kc-customs-clearance-hs-code-tariff-guide.html`: لینک به صفحهٔ پول‌ساز `suppliers/industrial-import.html`
- `suppliers/industrial-import.html`: لینک به راهنمای canonical ترخیص (تکمیل دوطرفه)

### ۴. راستی‌آزمایی گپ‌های ظاهری
- «تامین‌کننده تجهیزات برق صنعتی»: صفحهٔ `suppliers/electrical-supplier.html` از قبل موجود، بهینه و با **۱۲ لینک درون‌متنی** از صفحات محصول → هیچ اقدامی لازم نبود.
- `en/about.html`: title/desc مناسب؛ pos 11 با 0 کلیک طبیعی است → اقدامی نشد.

## ⚠️ کشف سوم پوستهٔ ۳۰۱ (و قانون طلایی)

`industrial-customs-clearance-guide.html` هم پوستهٔ ادغام ۳۰۱ بود (→ `kc-customs-clearance.html`). ویرایش اولیهٔ من روی پوسته انجام شد که در همان جلسه کشف و **revert** شد و همهٔ لینک‌ها مستقیماً به canonical اصلاح شدند.

**قانون تثبیت‌شده:** قبل از هر ویرایش/لینک به هر صفحهٔ KC، سه چیز چک شود: `rel="canonical"`، 301های `.htaccess`، و وجود در sitemap.

**کشف بزرگ‌تر (اسکن کل سایت):** ۹۰ پوستهٔ ۳۰۱ در `.htaccess` وجود دارد که **۴۱ تای آن‌ها هنوز از صفحات داخلی لینک می‌گیرند** (مجموعاً ~۱۰۰+ لینک داخلی به URLهای ریدایرکت‌شده — از جمله `a106-gr-b` با ۹ فایل و `third-party-inspection-guide` با ۷ فایل). این لینک‌ها equity را از مسیر ۳۰۱ عبور می‌دهند.

## 🎯 اهداف KPI (۴–۶ هفته)

| کوئری/صفحه | هدف |
|---|---|
| «تست هیدرواستاتیک پلی اتیلن» | ایندکس و ورود به ۲۰ صفحهٔ اول |
| «جدول استنلس استیل» | ورود stainless-steel-pipe-guide به ۲۰ صفحهٔ اول |
| خوشهٔ واردات/ترخیص | افزایش ایمپرشن صفحات industrial-import و kc-customs-clearance |

## 📋 بک‌لاگ اسپرینت ۴

1. **پاکسازی لینک‌های داخلی به ۴۱ پوستهٔ ۳۰۱** — بازنویسی مستقیم به canonical (بزرگ‌ترین بدهی فنی لینک داخلی).
2. **Authority غیربرند طبق ARENA-SEO-P4** — اقدامات انسانی کاربر (دایرکتوری‌ها، GBP، لینکدین)؛ چک‌لیست آماده است و `_audit/SEO-OFFSITE-TRACKER.json` برای ثبت وجود دارد. **این آیتم فقط با دست کاربر جلو می‌رود.**
3. بازبینی query↔page digest بعد از ۴ هفته (پس از استقرار سرور api/gsc.php + api/llm.php که هنوز معلق است).
4. ارزیابی مقالهٔ «هیدرواستاتیک vs دیسپلیسر» بر اساس دادهٔ GSC.
5. محتوای EN برای صفحات با ایمپرشن انگلیسی.
