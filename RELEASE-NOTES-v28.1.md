# ریلیزنوت `v28.1` — Sprint 281: زیرساخت گزارش رسمی مالی

## هدف این اسپرینت
ایجاد یک گزارش شفاف و **فقط‌خواندنی** از وضعیت مالی ثبت‌شده، بدون انتقال خودکار یا دستکاری اطلاعات پیشین.

## آنچه اضافه شده است
- در «هاب مالی مدیریتی»، تب **گزارش رسمی مالی** جایگزین نمای سادهٔ سرمایه در گردش شده است.
- انتخاب بازهٔ سال مالی با تاریخ‌های شمسی و نمایش وضعیت «تا امروز/تا انتهای بازه».
- ثبت ماندهٔ افتتاحیهٔ مستند برای پنج سرفصل: مطالبات بدون سند، بدهی/اعتبار تأمین بدون سند، تعهد چک شرکتی بدون سند و وجه نقد/بانک افتتاحیه.
- نگهداری قابل رسیدگیِ افتتاحیه در `ptf_crm_fiscal_snapshots`؛ ابطال رکورد به‌جای حذف فیزیکی و جلوگیری از ابطال پس از قفل سال مالی.
- محاسبهٔ مستقیم و بدون دفتر موازی از داده‌های موجود:
  - فاکتور مشتری منهای وصولی‌ها؛
  - فاکتورهای زیر‌دفتر تأمین، تعهدهای legacyِ لینک‌نشده و اصلاحیات؛
  - اعتبار تخصیص‌نیافته نزد تأمین‌کننده؛
  - فقط چک‌های دارای `ownership=company` و `st=open`.
- چک شخصی، چک ثالث منتقل‌شده، چک ابطال‌شده و چک با مالکیت نامشخص در ارقام شرکت وارد نمی‌شوند. مورد مالکیت نامشخص به‌عنوان هشدار کیفیت داده نشان داده می‌شود.
- گردش ثبت‌شدهٔ سال، CSV و پیش‌نمایش/چاپ **داخل سامانه** افزوده شده است.
- خلاصهٔ وضعیت مالی فقط هنگام تهیهٔ گزارش/قفل سالِ هم‌سال به snapshot مالی متصل می‌شود؛ گزارش‌های سال‌های قبلی با دادهٔ امروز بازنویسی نمی‌شوند.

## حفاظت از داده
- هیچ migration، حذف، تغییر خودکارِ payable، فاکتور، پرداخت یا چک اجرا نمی‌شود.
- ماندهٔ افتتاحیه فقط برای رقمی وارد شود که در اسناد موجود CRM ثبت نشده است؛ در غیر این صورت دوباره‌شماری رخ می‌دهد.
- این گزارش «وضعیت مالی ثبت‌شده در CRM» است، نه جایگزین دفترکل قانونی یا گردش بانکی. گردش بانکی هنوز منبع عملیاتی مجزایی در CRM ندارد.

## کنترل‌های فنی انجام‌شده
- `node --check` برای ۶۷ اسکریپت لودشده از `crm/index.html`: موفق.
- `tester154-v281-finance-foundation.js`: **15 PASS / 0 FAIL**؛ شامل مانده مشتری، لینک legacy، اعتبار تأمین، ابطال پرداخت، چک شخصی/شرکتی/ابطال‌شده و افتتاحیه.
- `python3 _tools/audit.py`: بدون خطا؛ فقط هشدارهای قدیمی حجم ۵ تصویر.

> این نتایج، تست فنی/fixture هستند و جای UAT واقعی در مرورگر و داده‌های شما را نمی‌گیرند.

## راهنمای کوتاه UAT
1. با نقش `admin` یا `chairman` به **هاب مالی → گزارش رسمی مالی** بروید.
2. ابتدا بازهٔ سال مالی را کنترل کنید؛ صرفاً در صورت نیاز آن را با تاریخ شمسی تنظیم کنید.
3. قبل از ثبت افتتاحیه، بررسی کنید همان مانده در فاکتور/پرداخت/چک فعلی CRM وجود نداشته باشد.
4. یک افتتاحیهٔ آزمایشی با شرح مشخص ثبت و اثر آن را فقط در همان سرفصل مشاهده کنید؛ سپس با «ابطال» آن را برگردانید.
5. یک چک شخصی، یک چک انتقالی و یک چک ابطال‌شده را کنترل کنید: هیچ‌کدام نباید در «چک‌های شرکتی باز» باشند.
6. از CSV و «پیش‌نمایش/چاپ» استفاده کنید و تأیید کنید پیش‌نمایش در خود سامانه باز می‌شود.

## موارد خارج از این اسپرینت
- دفترکل قانونی/حسابداری دوبل، گردش خودکار بانک و تطبیق بانکی پیاده‌سازی نشده‌اند.
- مهاجرت یا طبقه‌بندی خودکار چک‌های قدیمیِ بی‌مالکیت انجام نشده است؛ سامانه آن‌ها را هشدار می‌دهد تا با تصمیم کاربر اصلاح شوند.
