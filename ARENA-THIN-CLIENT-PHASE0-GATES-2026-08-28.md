# فاز صفرِ نازک‌سازی — گیت‌های CI آمادهٔ اتصال به GitHub Actions (v34.8.35)

**تاریخ:** ۲۰۲۶-۰۸-۲۸ | **برنچ:** `arena/01a047df-pishtaj-ir` | **نسخه:** `v34.8.35`
**وضعیت تحویل:** وصلهٔ آمادهٔ اعمال (`_tools/PENDING-workflow-t0-gates-2026-08-28.patch`)
— pushِ مستقیمِ `.github/workflows/` از سوی اپلیکیشن گیت‌هابِ نشست به خاطر نبودِ
دسترسی `workflows` رد می‌شود (رَویهٔ PENDING-patch در این مخزن).
**پیوند:** ادامهٔ `ARENA-THIN-CLIENT-STATUS-AUDIT-2026-08-28.md` (گزارش روی
`arena/01a0479d-pishtaj-ir`)

---

## ۱) این نشست چه بست؟

اولویت ۱ ممیزیِ ۲۰۲۶-۰۸-۲۸ («گیت‌های CI را به هر دو workflow وصل کنید») آمادهٔ بسته
شدن است: سه بندِ T0 که «انجام‌نشده» بودند، در قالب یک وصلهٔ تست‌شده تحویل شده‌اند و
**توسط تستر قفل شده‌اند** (یعنی بعد از اعمال، برگشت‌پذیر نیستند مگر با شکستنِ گیت):

| بند | قبل | بعد از اعمالِ وصلهٔ v34.8.35 | قفلِ تستی |
|---|---|---|---|
| **T0-3** | هیچ workflowای تست اجرا نمی‌کرد | `run-ci-gate.js` + `arch-guard.js` **قبل از FTP** در استیجینگ و پروداکشن (با Setup Node ۲۰) | tester536 + tester431-G2 |
| **T0-4** | هیچ تایید پس از استقراری نبود | نسخهٔ زنده == `VERSION.json` + هشِ ۵–۶ فایل کلیدی زنده == کامیت، با تلاش مجدد و `exit 1` | tester536 |
| **T0-6** | هدر کش استیجینگ نبود | تزریق `no-cache` برای js/html در `crm/.htaccess` **فقط** استیجینگ (بدون نشت به پروداکشن) | tester536 |
| _(جنبی)_ | `php.yml` بی‌اثر (`composer validate` بدون composer.json) | `php -l` + `node --check` + همان گیت کانونی روی هر PR به main | tester536 |

افزون بر وصلهٔ معلقِ نشست قبل (`_tools/PENDING-workflow-t0-gates-2026-08-27.patch`،
که اعمال و سپس حذف شد)، دو چیز فراتر از آن وصله اضافه شد:

1. **چکِ «نسخهٔ زنده == VERSION.json»** — مستقیم‌ترین سنجشِ ریشهٔ حادثهٔ
   «نسخهٔ مخلوط» (۲۰۲۶-۰۸-۲۷). وصله فقط هش فایل را چک می‌کرد؛ اگر FTP فایل‌ها را
   درست می‌برد ولی `index.html` کهنه می‌ماند، هشِ `index.html` در پروداکشن
   آن را می‌گرفت — حالا حتی پیش از آن، نسخه خوانده می‌شود.
2. **Setup Node در پروداکشن** — وصله فقط برای استیجینگ افزوده بود؛ پروداکشن به
   نودِ پیش‌فرضِ runner متکی بود (تغییر نسخهٔ runner = گیتِ غیرقطعی).

---

## ۲) اصلاحِ پاسِ گمراه‌کننده (یافتهٔ فرعی F-1)

تستر `tester431` ادعا می‌کرد «G2 نگهبان به گیت CI وصل است» در حالی که فقط
می‌سنجید `run-ci-gate.js` **درونِ خودش** به `arch-guard.js` ارجاع دارد. این
یعنی «گیت وصل است» حتی وقتی هیچ workflowای آن را اجرا نمی‌کرد.

- assertion قدیمی نگه داشته شد (هنوز معتبر است)،
- یک assertion **واقعی** افزوده شد: هر دو `deploy-*.yml` باید واقعاً
  `node _tools/uat/run-ci-gate.js` و `node _tools/arch/arch-guard.js` را صدا بزنند،
- قرارداد کامل‌تر در **tester536** (۳۷ مورد) نشست: ترتیبِ گیت نسبت به FTP،
  وجود تلاش مجدد، `exit $fail`، وجودِ واقعیِ مسیرهای فهرست هش در مخزن،
  عدم نشتِ no-cache به پروداکشن، و اینکه **هیچ `deploy-*.yml`ای نتواند گیت را دور بزند**.

---

## ۲.۵) چرا وصله، و چه تضمینی دارد؟

تستر `tester536` دو حالت دارد:

- **اگر workflowها وصل باشند** → قراردادِ کامل را مستقیماً روی
  `.github/workflows/*.yml` می‌سنجد (۴۰ مورد).
- **اگر هنوز وصل نباشند** → وصلهٔ معلق را می‌یابد، `git apply --check` می‌کند،
  سپس وصله را در یک مسیر موقت با `patch -p1` **واقعاً اعمال** می‌کند و همان قرارداد
  را روی نتیجه اجرا می‌کند؛ اگر وصله نباشد، تداخل داشته باشد، یا ناقص باشد → **گیت
  قرمز می‌شود**.

یعنی در هر دو حالت دروغ گفته نمی‌شود: یا متصل است، یا «در صف و سالم». به محضِ اعمالِ
وصله، tester536 خودکار به حالتِ سخت‌گیرانه می‌رود و حذفِ فایل وصله هم در
`RELEASE-NOTES-v34.8.35.md` مستند شده است.

---

## ۳) وضعیت تست‌ها

```
node _tools/uat/run-ci-gate.js      → 151 PASS / 0 FAIL   (۱۵۰ قبلی + tester536)
node _tools/arch/arch-guard.js      → PASS
  A6  نسخه: ۰ تخلف (VERSION.json == index.html == sw.js == manifest == shell == clear-cache == sales-domain.php)
  A10 بایپس لایهٔ داده: ۲۶۳/۲۶۳ بدهیِ baseline — صفر تخلف جدید
  A11 ناهماهنگی رجیستری فرمان موجودیت: ۰/۰
```
سه workflow با `yaml.safe_load` و `bash -n` (تک‌تک گام‌ها) هم اعتبارسنجی شدند.

---

## ۴) هنوز تمام نشده (اولویت‌های بعدی)

فازِ نازک‌سازی از نظر DoD هنوز کامل نیست؛ ترتیب پیشنهادی همان ممیزی است:

1. **T6 / C5 — بازنشستگی موتور سینک legacy** (`pushDirty`/`pullCheck`): دو موتور هنوز
   زنده‌اند و اصلِ E7 («یک موتور همگام‌سازی») برقرار نیست. بزرگ‌ترین بندِ باقیمانده.
2. **T5-1 — حذف fallbackهای مردهٔ localStorage**:
   `crm/codegen.js` (`ptfDupAckSet` → `ptf_crm_settings`) و
   `crm/treasury.js` (`ptfTreasurySetCustodian` → `ptf_crm_shareholders`).
   در عمل غیرفعال‌اند ولی خلاف قراردادند و اگر روتر روزی خاموش شود، کسب‌وکار را
   مستقیم به LS می‌نویسند. نیازمند تسترِ اختصاصی قبل از حذف.
3. **T5-2 / T3-4 — کلیدهای DEV و کش** هنوز مستقیم در LS (`ptf_fx_live_cache`،
   `ptf_cloud_usage`، صف کدینگ، پیش‌نویس autodraft، `ptf_sales_command_*`).
4. **T4-1b** حذف توکن از LS (عمداً defer شده) و **T7** ابزارهای ریکاوری IDB-aware.

---

**ضمیمه — فایل‌های تغییرکرده در این نشست:**
`.github/workflows/deploy-staging.yml`، `deploy-production.yml`، `php.yml`،
`VERSION.json`، `crm/{index.html,sw.js,manifest.json,shell.js,clear-cache.html}`,
`api/sales-domain.php`، `_tools/uat/run-ci-gate.js`،
`_tools/uat/tester431-v34.7.28-architecture-guardrails.js`،
**جدید:** `_tools/uat/tester536-v34.8.35-ci-gate-wired.js`،
**جدید:** `RELEASE-NOTES-v34.8.35.md`،
**جدید:** `_tools/PENDING-workflow-t0-gates-2026-08-28.patch` (تغییرات workflow — نیازمند اعمال دستی)،
**حذف:** `_tools/PENDING-workflow-t0-gates-2026-08-27.patch` (با وصلهٔ جدید جایگزین شد).
