# بستهٔ تشخیصیِ فقط-خواندنی برای مسئول هاست — قطع اتصالِ سراسری CRM (تولید)

> تاریخ: ۲۰۲۶-۰۹-۰۹ · وضعیت: **اورژانسی** — هیچ کاربری و هیچ دستگاهی در پروداکشن سبز نیست؛ همهٔ درخواست‌ها به `api/crm.php` با `net::ERR_TIMED_OUT` (وقت‌سوزی) مواجه می‌شوند، حتی فراخوانیِ سبک `get_inbox`.
> هدف: جداکردن و پیدا کردنِ **ریشهٔ قطعیِ سمت سرور** و تصمیمِ رفع. این راهنما **فقط می‌خواند**؛ هیچ write/بازیابی/حذف/پاک‌کردن صف‌روی دادهٔ زنده انجام نمی‌دهد. رمز/توکن/فایل‌های کاربران/HAR ارسال نشود.
> کلیدِ این اورژانس این است که مشکل **همهٔ کاربران/دستگاه‌ها** را می‌گیرد ⇒ سمت سرور است، نه کد یا شبکهٔ یک کاربر.

---

## گام ۰ — خلاصهٔ یک‌خطی برای اینکه اول کجا را نگاه کنید

اگر «همه دستگاه‌ها» وقت‌سوزی می‌گیرند، محتمل‌ترین حالت‌ها به ترتیب:
1. PHP/سرور پایین یا در حال restart/کرش، یا حسابِ هاست (cPanel) به سقفِ منابع (CPU/memory/inode/bandwidth) رسیده.
2. workerهای PHP-FPM/Apache اشباع/قفل شده‌اند (همه پر از یک درخواستِ سنگین/گیرکرده).
3. یک کوئریِ MySQL طولانی/قفل، یا deadlock، یا صفِ طولانی.
4. قفل/گیرکردنِ خودِ `api/crm.php` در یک عملیات (مثلاً یک `data_pull` کاملِ سنگین).

---

## بخش الف — تست‌های سریع از مرورگر (در هر دستگاه/شبکه، چند ثانیه)

> این‌ها را باز کنید و فقط یادداشت کنید «لود شد» یا «timeout»:

| # | چه چیز را تست کنیم | اگر «لود شد» | اگر «timeout» |
|---|---|---|---|
| A1 | صفحهٔ اصلی `https://pishtaj.ir/` (فایلِ ایستا) | هاست/IP زنده است | احتمالاً کل هاست/دامنه/سرور پایین است |
| A2 | `https://pishtaj.ir/robots.txt` (فایلِ ایستا) | میزبانیِ فایلِ ایستا کار می‌کند | شبکه/هاست/HTTP پایین است |
| A3 | `https://pishtaj.ir/api/crm.php` (بدون پارامتر) | **PHP زنده است** (باید سریع یک خطای JSON مثل `{error:...}`/401 بدهد) | PHP/سرورِ برنامه پاسخ نمی‌دهد ⇒ مشکلِ اصلی این‌جاست |
| A4 | همان A3 روی استیجینگ | استیجینگ سالم است ⇒ مشکل فقط روی پروداکشن | مقایسه برای تشخیصِ «پروداکشن در برابر میزبانیِ مشترک» |

**خوانش تصمیم:** اگر A1/A2 لود شد ولی A3 timeout شد ⇒ PHP یا خودِ `api/crm.php` مشکل دارد. اگر A1 هم timeout شد ⇒ مشکلِ کلِ سرور/هاست/دامنه.

---

## بخش ب — چک‌های سمت سرور (مسئول هاست؛ فقط‌خواندنی)

از پنلِ هاست (cPanel/WHM) یا SSH:

1. **PHP error log** (معمولاً `~/logs/` یا `error_log` در `public_html/api/`): آیا خطای Fatal/memory/exhausted/وقفه هست؟ آخرین خطاها را بفرستید.
2. **وضعیت PHP-FPM/Apache workerها:** چند worker فعال/آماده‌اند؟ آیا همه «Busy»اند؟ (خروجی `apachectl status` یا پنل). اگر همه‌شان Busy/پراند ⇒ اشباعِ worker.
3. **MySQL process list:** `SHOW FULL PROCESSLIST;` — آیا کوئریِ طولانی/قفل (State: `Locked`/`Copying`/طولانی‌تر از چند ثانیه) هست؟
4. **سقف منابعِ حساب:** در پنل، CPU/memory/inode/bandwidthِ این ماه را ببینید — آیا به سقف خورده؟ (در cPanel رایج‌ترین علتِ timeoutِ سراسری همین است.)
5. **دسترسی از خودِ هاست (CLI):** روی سرور اجرا کنید:
   - `curl -sS -m 10 "https://pishtaj.ir/api/crm.php"` → اگر سریع پاسخ داد ولی از بیرون نه ⇒ محدودیت/فایروال/worker؛ اگر همین‌جا هم وقت‌سوزی/خطا کرد ⇒ خودِ PHP مشکل دارد.
   - `php -r 'echo memory_limit;'` → سقفِ حافظه را ببینید.
6. **تستِ پاسخِ یک pullِ کامل به‌صورت ایزوله:** (با احتیاط، بهتر است روی کپی) یک `data_pull since=0` چقدر طول می‌کشد / چه حافظه‌ای می‌خواهد؟ فرضیه: دیتاستِ بزرگ + چند دستگاهِ هم‌زمانِ bootstrapping، workerها را اشباع می‌کند.

> ⛔ این ۶ چک فقط برای **تشخیص** است؛ هیچ‌کدام داده را تغییر نمی‌دهد. اگر MySQL `SHOW PROCESSLIST` یک کوئریِ گیرکرده نشان داد، آن را **نکشید** مگر اینکه مطمئن باشید رکورد/فرمانی را ناقص نمی‌گذارد — بهتر است ابتدا snapshot/baseline بگیرید (بخش ج).

---

## بخش ج — شرطِ «صددرصد مطمئن‌شدن از سالم‌ماندن داده» (قبل از هر تغییری)

به‌دلیل تاریخچهٔ حادثهٔ حذفِ انبوه، **قبل از هر اقدامِ اصلاحی/راه‌اندازی مجددِ سنگین** یک baseline بگیرید (فقط‌خواندنی، روی خودِ سرور یا یک کپی):
- شمارشِ هر کالکشن (تعداد رکوردِ `rfqs`, `customers`, `suppliers`, `deals`, `projects`, `invoices`, `commission_records`, `notifs`, `audit`, …) و تاریخچه‌اش.
- این baseline را ذخیره کنید؛ پس از رفع و یک pullِ موفقِ هر دستگاه، دوباره بگیرید و مقایسه کنید. تساوی ⇒ اثبات می‌شود پنل‌های خالی «نمایش/دریافت» بوده، نه حذفِ سروری.

---

## بخش د — گزارشِ نهایی که برای تصمیم برگردانید

لطفاً فقط همین خلاصه را برگردانید (بدون دادهٔ محرمانه):
1. نتیجهٔ A1–A4 (لود شد / timeout).
2. یک خط از PHP error log (اگر خطای Fatal/memory هست).
3. وضعیت workerها (همه Busy یا نه؟).
4. خروجی کوتاهِ `SHOW FULL PROCESSLIST` (بدون بدنهٔ کوئری‌های محرمانه؛ فقط State/Time/Command).
5. آیا حساب به سقفِ منابع (CPU/memory/inode/bandwidth) رسیده؟ بله/خیر + کدام.
6. پاسخِ `curl` از خودِ هاست به `api/crm.php`.
7. (اختیاری) شمارشِ baseline از بخش ج.

با این ۷ مورد، ریشهٔ قطعی مشخص می‌شود و دیگر نه من حدس می‌زنم نه شما؛ سپس قدمِ رفعِ درست (نه‌تنها نمادِ سبز، بلکه تأییدِ سالم‌ماندن داده) تعیین می‌شود.
