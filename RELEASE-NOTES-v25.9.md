# ریلیزنوت v25.9 — خواندن ضمیمه‌های درخواست با AI از پنجره مشاهده

## نیاز کارفرما
در پنجرهٔ «مشاهده درخواست»، مسیر «خواندن فایل» باید بتواند ضمیمه‌های همان درخواست را با AI بخواند. اگر بیش از یک ضمیمه وجود دارد، کاربر باید انتخاب کند که کدام فایل یا فایل‌ها خوانده شوند.

## پیاده‌سازی

- در ویزارد «خواندن فایل استعلام» دکمهٔ جدید **«خواندن ضمیمه‌های درخواست با AI»** اضافه شد.
- همهٔ دسته‌های ضمیمهٔ درخواست بررسی می‌شوند: فایل اولیه، دیتاشیت، تصویر، نقشه، سایر مدارک و کاتالوگ قدیمی.
- اگر فقط یک فایل قابل خواندن وجود داشته باشد، همان فایل مستقیماً وارد جریان AI می‌شود.
- اگر چند فایل قابل خواندن وجود داشته باشد، یک dialog انتخاب چندگانه نمایش داده می‌شود؛ کاربر می‌تواند یک یا چند فایل را انتخاب کند.
- فایل‌های انتخاب‌شده به‌صورت **ترتیبی** پردازش می‌شوند تا درخواست‌های AI هم‌زمان و مصرف quota کنترل شود.
- برای هر فایل، نتیجه یا خطای مستقل نگه داشته می‌شود و در پایان خلاصهٔ تعداد فایل موفق/ناموفق و تعداد قلم استخراج‌شده نمایش داده می‌شود.
- اقلام استخراج‌شده فقط به جدول بازبینی ویزارد اضافه می‌شوند؛ ورود نهایی به اقلام درخواست/ماژول کالا همچنان نیازمند تأیید صریح کاربر است.

## فرمت‌های پشتیبانی‌شده

| نوع | مسیر خواندن |
|---|---|
| PDF، JPG، JPEG، PNG، WEBP | ارسال کنترل‌شده به OCR/vision AI |
| XLSX، XLS | دریافت کنترل‌شده، تبدیل ساختار sheet به متن و سپس استخراج AI |
| DOCX، CSV، TXT، MD | استخراج متن از سرور و سپس استخراج AI |

- فرمت‌های پشتیبانی‌نشده یا فایل‌های صف محلی بدون object key در انتخابگر با وضعیت مشخص نمایش داده می‌شوند و قابل انتخاب نیستند.
- برای PDF/تصویر/Excel، API سرور مسیر base64 کنترل‌شده با سقف raw file برابر **6MB** فراهم می‌کند؛ بنابراین flow به CORS مرورگر روی S3 وابسته نیست.

## تغییرات Backend

- `api/attachment-read.php`: حالت `base64` محدودشده به فایل‌های AI-readable و سقف 6MB اضافه شد.
- `api/llm.php`: اکشن `ocr_text` برای استخراج ساختاریافته از متن DOCX/XLSX/CSV/TXT اضافه شد.

## فایل‌های تغییرکرده

- `crm/inqreader.js`
- `api/attachment-read.php`
- `api/llm.php`
- `_tools/uat/tester140-v259-attachment-ai.js` — جدید
- `crm/index.html`، `crm/sw.js`، `INSTALL-GUIDE.md`، `PTF-MASTER-HANDOVER.md` — همگام‌سازی نسخه v25.9

## تست‌ها

- Syntax تمام 64 اسکریپت بارگذاری‌شدهٔ CRM: **0 failure**
- `tester138-v257.js`، dark theme: **21 PASS / 0 FAIL**
- `tester139-v258-llm.js`، transport/cache: **14 PASS / 0 FAIL**
- `tester140-v259-attachment-ai.js`: **15 PASS / 0 FAIL**
- `audit.py`: بدون error؛ تنها هشدار قدیمی تصاویر حجیم سایت باقی است.

## تست دستی کارفرما

1. یک درخواست دارای یک PDF یا تصویر cloud-stored باز کنید: مشاهده درخواست → خواندن فایل → خواندن ضمیمه‌های درخواست با AI. باید بدون انتخابگر، خواندن شروع شود.
2. یک درخواست دارای حداقل دو PDF/تصویر/Excel/DOCX cloud-stored باز کنید. انتخابگر باید نام، دسته، نوع و حجم فایل را نشان دهد.
3. یک فایل و سپس چند فایل را انتخاب کنید؛ پردازش باید ترتیبی باشد و اقلام به جدول بازبینی اضافه شوند.
4. یک فایل unsupported یا local queue را بررسی کنید؛ نباید به AI ارسال شود و باید وضعیت آن روشن باشد.
5. در پایان، پیش از «تایید نهایی و ورود به ماژول کالا»، اقلام را بازبینی و اصلاح کنید.
