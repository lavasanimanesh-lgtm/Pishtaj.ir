# 📦 ریلیزنوت v13.4 — چک‌های صادره، تغییرنام‌ها، متن‌های آماده، تب تامین‌کنندگان + دو گزارش کارشناسی

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v13.3 | ماژول جدید: `crm/cheques.js` | سند جدید: `ASSESSMENT-MESSAGING-APK-v134.md`

---

## 🏦 US-327 — چک‌های صادره (در ماژول شخصی)
- بخش جدید «چک‌های صادره» داخل پنل **یادآورها** (ماژول شخصی): ثبت شماره چک/صیاد، مبلغ، در وجه، بانک، سررسید، بابت
- **یادآوری روزانه از ۷ روز قبل از سررسید:** هر روز یک اعلان در کارتابل ثبت‌کننده (۷ روز مانده… سررسید امروز… سررسید گذشته) — هر روز فقط یک بار
- رنگ‌بندی فوریت (آبی/کهربایی/قرمز) + دکمه «پاس شد» و حذف + ثبت در audit
- داده در سینک بین‌دستگاهی، بک‌آپ و whitelist سرور اضافه شد

## 📋 US-328/329 — تغییرنام‌ها در کل نرم‌افزار
| قبل | بعد |
|---|---|
| استعلامات | **درخواست‌ها** (سایدبار، عنوان پنل، سربرگ، داشبورد، دسترسی‌ها، فیلتر/خروجی) |
| استعلام هوشمند تامین | **درخواست تامین** (سایدبار، پنل، ویزارد، دکمه، دسترسی‌ها) |

(کلیدهای داده و ثبت‌های audit قبلی برای سازگاری دست نخوردند — فقط نمایش)

## 💬 US-330 — متن‌های از پیش آماده با متغیر {نام}
- ۸ متن آماده در ۳ دسته: **مشتری** (پیگیری پیشنهاد/تشکر/اعلام آمادگی)، **تامین‌کننده** (استعلام قیمت/پیگیری/ابلاغ سفارش)، **سایر** (تبریک/هماهنگی جلسه)
- دکمه «💬 متن‌های آماده پیام» روی کارت مشتری و تامین‌کننده — نام رابط به‌صورت خودکار جایگذاری و متن نهایی با یک کلیک **کپی** می‌شود (آماده Paste در واتساپ/تلگرام/…)
- متن‌ها در کلید سینک‌شونده ذخیره می‌شوند (قابل توسعه در آینده)

## 🏭 US-331 — تامین‌کنندگان: تب داخلی / خارجی
- سه تب بالای فهرست: همه | 🇮🇷 داخلی | 🌍 خارجی
- فیلد «داخلی/خارجی» در فرم ثبت/ویرایش تامین‌کننده؛ رکوردهای قدیمی پیش‌فرض داخلی

## 📱 گزارش کارشناسی ۱ — اتصال پیام‌رسان‌ها (سند کامل در بسته)
جمع‌بندی: WhatsApp API رسمی برای شرکت ایرانی ممکن نیست، اما **راهکار سه‌فازی** ارائه شد:
- **فاز ۱ (US-332):** دکمه‌های پیام‌رسان با deep-link (wa.me/t.me/ble.ir/eitaa/rubika) + متن آماده — هر کاربر خودکار «با اکانت خودش» می‌فرستد؛ صفر ریسک و هزینه
- **فاز ۲ (US-333):** بات تلگرام + بله شرکت برای اعلان خودکار (چک/کارتابل/ارجاع) — نیازمند ساخت بات توسط شما و توکن در کانفیگ هاست
- **فاز ۳:** واتساپ رسمی از طریق BSP خارجی — توصیه تیم: فعلاً خیر
→ منتظر تایید US-332 و US-333

## 📲 گزارش کارشناسی ۲ — فایل نصبی APK (سند کامل در بسته)
**نتیجه: ممکن است و یوزر استوری ثبت شد — US-334:** بسته‌بندی PWA موجود در APK واقعی با **TWA/Bubblewrap** (ابزار رسمی و رایگان گوگل). پیش‌نیازها (منیفست/sw/HTTPS) همه از قبل آماده‌اند؛ APK قابل نصب مستقیم بدون Play Store و **آپدیت‌ها بدون نصب مجدد** از سایت می‌آیند. → منتظر تایید

## 🧪 کیفیت
تستر جدید `tester54` ۲۳/۲۳ ✅ | رگرسیون **۴۱ تستر** ✅ | audit صفر خطا ✅

## 📁 فایل‌های تغییریافته
`crm/cheques.js` (جدید) · `crm/index.html`/`bridge.js`/`rfqsmart.js`/`perms.js`/`listtools.js` (تغییرنام‌ها) · `crm/sync.js`/`backup.js`/`api/crm.php` (کلیدهای جدید) · `crm/sw.js` (v13.4) · `ASSESSMENT-MESSAGING-APK-v134.md` (جدید) · `_tools/uat/tester54` (جدید)

⚠️ `api/crm.php` تغییر کرده — در آپلود جایگزین شود.
