# Release Notes v34.7.3 — ماندگاری امضا و گزارش دوره‌ای خزانه

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## ماندگاری پروفایل امضا

- ریشه تکرار درخواست امضا مشخص شد: موتور `client-server` کلید سنگین `ptf_crm_sigprofiles` را از LocalStorage به IndexedDB منتقل می‌کرد، اما مکاتبات همچنان فقط `localStorage.getItem` را می‌خواند؛ بنابراین پس از migration یا refresh امضای موجود خالی دیده می‌شد.
- منبع واحد خواندن به `getData('ptf_crm_sigprofiles')` تغییر کرد تا Local/IDB/Server را پوشش دهد.
- کلید کاربر با alias و مقایسه بدون حساسیت به حروف حل می‌شود.
- یک mirror بازیابی نسخه‌دار برای همان کاربر نگه‌داری می‌شود؛ اگر map اصلی موقتاً خالی یا قدیمی باشد، امضای جدیدتر یک‌بار بازیابی می‌شود.
- ذخیره `false` دیگر پیام موفقیت نمی‌دهد و برای رکورد پروفایل رسید sync درخواست می‌شود.
- خطای خواندن تصویر مهر/امضا مدیریت می‌شود و نسخه قبلی به‌جای پاک شدن حفظ می‌گردد.
- Offers، Offers Pro، Contracts و اسناد رسمی نیز از همان accessor پایدار استفاده می‌کنند.
- دکمه «امضای من» با نشان `✓ ذخیره‌شده` وضعیت واقعی را نمایش می‌دهد.

## خروجی نامه با و بدون امضا

برای نامه امضاشده دو خروجی مستقل اضافه شد:

1. **با امضای دیجیتال**: از snapshot قطعی مهر/امضای زمان امضا استفاده می‌کند.
2. **بدون امضا / چاپ فیزیکی**: همان شماره، متن، نام و سمت را نگه می‌دارد، تصاویر مهر و امضا را حذف و محل مهر و امضای فیزیکی ایجاد می‌کند.

- هر دو خروجی در پنل مکاتبات و کشوی پرونده فروش در دسترس‌اند.
- نام فایل خروجی signed/unsigned متفاوت است تا با یکدیگر اشتباه نشوند.

## گزارش و خروجی دوره‌ای خزانه

- فیلتر تاریخ شروع/پایان با پشتیبانی تاریخ شمسی و میلادی اضافه شد.
- نوع گردش قابل انتخاب است: همه، فقط ورودی‌ها یا فقط خروجی‌ها.
- فیلتر منبع، مانند وصولی مشتری، هزینه جاری، پرداخت تامین، شارژ تنخواه و غیره اضافه شد.
- برای فیلتر فعال این موارد نمایش داده می‌شود:
  - جمع ورودی
  - جمع خروجی
  - خالص دوره
  - تعداد گردش
  - تعداد رکوردهای بدون تاریخ کنارگذاشته‌شده
- خروجی **Excel/CSV** با ردیف‌های گردش و جمع‌های پایانی ارائه می‌شود.
- خروجی **PDF/چاپ دوره** با همان فیلتر و KPIها ارائه می‌شود.
- اعتبارسنجی تاریخ نامعتبر و شروع بعد از پایان از خروجی اشتباه جلوگیری می‌کند.

## کنترل کیفیت

- `tester408-v34.7.3-signature-treasury-period.js`: 14 PASS / 0 FAIL
- CI متمرکز ریلیز: 27 PASS / 0 FAIL
- رگرسیون کامل: 391 تستر، 334 فایل PASS، 57 فایل بدهی تاریخی FAIL و بدون شکست جدید نسبت به v34.7.2.
- تست‌های قالب نامه فارسی/انگلیسی، تصویر نامه، sync امضا و رگرسیون خزانه اجرا شدند.
- Auditor و هم‌ترازی VERSION/PWA: PASS
- CI متمرکز شامل syntax تمام مصرف‌کنندگان پروفایل امضا و `treasury.js` است.
