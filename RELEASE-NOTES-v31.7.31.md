# Release Notes — v31.7.31

**تاریخ:** ۱۴۰۵/۰۴/۳۰ (2026-07-21)  
**نسخه:** `v31.7.31`  
**نوع:** 🟠 Bugfix ریشه‌ای + تثبیت رفتار پیشنها‌دها — `BUG-OFF-REV-001`

---

## 🎯 مسئله گزارش‌شده

در ماژول پیشنهادها مشاهده شد که کاربر یک پیشنهاد ثبت‌شده را باز می‌کند، دکمه «ویرایش» را می‌زند و تغییر کوچکی در فرم انجام می‌دهد؛ اما ذخیرهٔ ویرایش می‌تواند باعث افزایش `Rev` شود و در برخی مسیرهای stale/autodraft یا خطای هویت فرم، حتی خطر تغییر شماره پیشنهاد یا ثبت به‌عنوان سند جدید ایجاد کند.

قاعدهٔ صحیح کسب‌وکاری که در این ریلیز enforce شد:

1. **ویرایش مستقیم** فقط اصلاح همان سند است؛ نباید `Rev` جدید بسازد.
2. **نگارش جدید** فقط از دکمهٔ «📑 نگارش جدید» مجاز است.
3. نگارش جدید باید **همان شماره پیشنهاد** را نگه دارد و فقط `Rev` همان سند را افزایش دهد.
4. اگر فرم ویرایش stale باشد و رکورد اصلی پیدا نشود، ذخیره باید متوقف شود؛ نه اینکه پیشنهاد جدید یا شماره جدید بسازد.

---

## 🔍 RCA — تحلیل ریشه‌ای

ریشهٔ فنی در `crm/offers.js` بود:

- `offerEdit(no)` فرم را با `editMode = 'direct'` باز می‌کرد.
- اما در `offerSave()` منطق قبلی این بود که اگر وضعیت قبلی پیشنهاد در یکی از وضعیت‌های `sent / approved / rejected / revise / won / lost` باشد، حتی ویرایش مستقیم هم `madeRevision=true` می‌شد.
- یعنی سیستم به‌جای تشخیص «مسیر ورود فرم» از «وضعیت سند» نتیجه می‌گرفت که باید Rev بسازد.
- این رفتار با سیاست موردنظر کارفرما ناسازگار بود: وضعیت ارسال‌شده/تاییدشده بودن سند به‌تنهایی نباید ویرایش را تبدیل به نگارش جدید کند.
- علاوه بر آن، در صورت برگشت draft/stale یا خراب شدن شماره داخل state فرم، گارد هویت صریحی وجود نداشت که بگوید direct edit/revision فقط باید روی شماره اصلی ذخیره شود.

---

## ✅ اصلاح انجام‌شده

در `crm/offers.js` دو helper جدید اضافه شد:

- `ptfOfferPrepareEditState()`
- `ptfOfferResolveSaveIdentity()`

رفتار جدید:

| مسیر | نتیجه |
|---|---|
| دکمه ویرایش `offerEdit` | `editMode=direct` + ثبت شماره پایه؛ ذخیره روی همان رکورد؛ بدون Rev جدید |
| دکمه نگارش جدید `offerReviseClone` | `editMode=revision` + ثبت شماره پایه؛ افزایش Rev روی همان شماره پیشنهاد |
| فرم stale/autodraft بدون رکورد اصلی | ذخیره block می‌شود و سند جدید ساخته نمی‌شود |
| پیشنهاد جدید با شماره تکراری | گارد قبلی v31.7.24 حفظ شد: regen/block فقط برای `editMode='new'` |

منطق قبلی `afterSent` حذف شد. از این نسخه، افزایش Rev فقط از مسیر explicit revision انجام می‌شود.

---

## 📁 فایل‌های تغییر یافته

```text
crm/offers.js
crm/index.html
crm/sw.js
crm/clear-cache.html
PTF-MASTER-HANDOVER.md
_tools/uat/tester206-offer-edit-revision-identity.js
_tools/last-regression.json
RELEASE-NOTES-v31.7.31.md
REGRESSION-REPORT-v31.7.31.md
```

---

## 🧪 تست و Evidence

تستر اختصاصی جدید اضافه شد:

```text
_tools/uat/tester206-offer-edit-revision-identity.js
```

پوشش این تستر:

- ویرایش مستقیم `direct` حتی روی پیشنهاد `sent` نباید `explicitRevision` شود.
- اگر شماره داخل فرم خراب شود، helper آن را به شماره اصلی برمی‌گرداند.
- نگارش جدید `revision` همان رکورد را هدف می‌گیرد و فقط `explicitRevision=true` دارد.
- فرم stale بدون رکورد اصلی block می‌شود.
- گارد duplicate شماره برای پیشنهاد جدید همچنان برقرار است.

نتیجه اجرای مستقیم تستر:

```text
tester206-offer-edit-revision-identity: 13 PASS / 0 FAIL
```

گیت‌های کامل در `REGRESSION-REPORT-v31.7.31.md` ثبت شده‌اند.

---

## ⚠️ Impact Analysis

| بخش | اثر | ریسک |
|---|---|---|
| ویرایش پیشنهاد | اصلاح رفتار: دیگر Rev خودکار نمی‌خورد | کم |
| نگارش جدید | همان شماره حفظ می‌شود و فقط Rev افزایش می‌یابد | کم |
| پیشنهاد جدید | گارد duplicate قبلی حفظ شده | کم |
| TO→CO / پیشنهاد جایگزین | دست‌نخورده | بسیار کم |
| پیشنهاد برنده / پرونده فروش | read-only post-award دست‌نخورده | بسیار کم |
| sync / finance / RFQ tracking | هیچ تغییر مستقیم ندارد | صفر |

---

## 📋 مراحل راستی‌آزمایی توسط کارفرما

1. یک پیشنهاد مالی یا فنی موجود را باز کنید.
2. اگر وضعیت آن `sent` یا وضعیت‌های بعدی است، دکمه «✏️ ویرایش» را بزنید.
3. یک فیلد غیرشماره‌ای مثل توضیح، برند، قیمت، یا Terms را تغییر دهید.
4. ذخیره کنید.
5. انتظار صحیح:
   - شماره پیشنهاد تغییر نکند.
   - `Rev` افزایش پیدا نکند.
   - همان رکورد در فهرست اصلاح شود.
6. حالا از فهرست، دکمه «📑 نگارش جدید» را بزنید.
7. ذخیره کنید.
8. انتظار صحیح:
   - شماره پیشنهاد همان قبلی بماند.
   - فقط `Rev` یک واحد افزایش پیدا کند.

---

## 🚦 نتیجه

`BUG-OFF-REV-001` در سطح ریشه‌ای بسته شد. از این نسخه، Rev دیگر نتیجهٔ ویرایش معمولی نیست؛ Rev فقط با اقدام صریح «نگارش جدید» ایجاد می‌شود و شماره پیشنهاد immutable باقی می‌ماند.
