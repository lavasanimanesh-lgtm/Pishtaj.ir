# فاز ۱۸ (v34.0.21-alpha) — مدیریت اسناد تنخواه/فاکتور + رفع پنجرهٔ همپوشان حذف فاکتور

**برنچ:** `arena/019fcd1d-pishtaj-ir`
**گزارش کارفرما:**
۱) تنخواه مشکل ثابت دارد (فایلهای ضمیمه قابل مشاهده/حذف/ویرایش نیستند).
۲) بعضی درگاهها (مثل حساب تامینکنندگان) فایل ضمیمه دریافت میکنند ولی قابل مشاهده/حذف/ویرایش نیستند.
۳) ماژول حساب تامینکنندگان باگ دارد — هنگام حذف فاکتور یک پنجرهٔ (مودال) همپوشان/اشتباه باز میشود.

---

## اصلاحات

### ۱) تنخواه — مدیریت اسناد (`crm/petty.js`)
- دکمهٔ **«📎 اسناد»** به اکشنهای هر هزینهٔ تنخواه اضافه شد.
- `ptfPettyFilesUi(cd)`: مودال نمایش سندها (مشاهده با `openStoredFile`) + افزودن سند جدید (`attachUploadWidget`) + دکمهٔ «✕ حذف».
- `pettyRemoveFile(cd, key)`: حذف سند از رکورد و فضای ابری.

### ۲) حساب تامینکنندگان — مدیریت اسناد فاکتور (`crm/supplier-finance.js`)
- `slInvoiceEdit` حالا بخش **«📎 اسناد فاکتور»** دارد: نمایش سندهای موجود (مشاهده/حذف) + افزودن سند جدید.
- `slInvoiceRemoveFile` ارتقا یافت تا اگر از مودال ویرایش صدا زده شود، دوباره همان ویرایش را باز کند.

### ۳) رفع پنجرهٔ همپوشان حذف فاکتور
- **ریشه:** `slOpenLedger` مودال `slLedgerDlg` را با `insertAdjacentHTML` میساخت **بدون حذف مودال قبلی**. وقتی حذف/ابطال فاکتور انجام و `slOpenLedger` دوباره صدا زده میشد، یک **مودال دوم با همان id** ساخته میشد → دو پنجرهٔ همپوشان.
- **اصلاح:** `slOpenLedger` حالا قبل از باز کردن، مودال قبلی `slLedgerDlg` را حذف میکند.

---

## تست‌ها
- **tester316** جدید (۸/۰): تنخواه + فاکتور + رفع همپوشانی.
- رگرسیون کامل سبز (۲۶ تستر، ۰ شکست).
- بامپ نسخه → **v34.0.21-alpha**.
- تغییرات `.github/workflows/*` در PENDING-workflow-changes ثبت شد.

*تولیدشده توسط Arena Agent — مدیریت اسناد و رفع پنجرهٔ همپوشان.*
