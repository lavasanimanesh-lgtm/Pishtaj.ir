# 📦 Release Notes — v53.0
**اسپرینت ۵۳: یکپارچه‌سازی قالب — فوتر و هدر واحد در همه صفحات**
**تاریخ:** ۱۲ تیر ۱۴۰۵ (2026-07-03)

---

## ✅ کارهای انجام‌شده

### 1️⃣ US-105 — منوی هدر استاندارد در تمامی صفحات
**وضعیت قبل:** ۱۲+ نسخه متفاوت منو (برخی صفحات ۴ لینک، برخی ۱۲ لینک؛ ۲۵۴ مقاله KC حتی دکمه منوی موبایل نداشتند).

**اقدامات:**
- منوی مرجع تعریف و اعمال شد: **خانه · درباره ما · خدمات · صنایع · پروژه‌ها · مرکز دانش · وبلاگ · اخبار · استعلام · تماس + 🇬🇧 English (موبایل)**
- هدر مرجع (لوگو + منو + دکمه تماس + EN دسکتاپ + دکمه همبرگری) در **۳۱۳ صفحه فارسی** جایگزین شد؛ مسیرهای نسبی بر اساس عمق هر صفحه به‌صورت خودکار محاسبه شد.
- کلاس `active` بر اساس بخش جاری صفحه به‌صورت خودکار تنظیم می‌شود (AC3 ✔).
- **منوی موبایل در همه صفحات فعال شد:** اسکریپت سبک inline (توگل منو + افکت اسکرول هدر) به صفحاتی که `main.js` ندارند تزریق شد — قبلاً منوی همبرگری فقط در ۲۸ صفحه کار می‌کرد!
- نسخه انگلیسی: منوی ۴ صفحه یکدست شد (Home/Services/About/Projects/RFQ/Contact + سوییچ فارسی).

### 2️⃣ US-104 — فوتر واحد با فرمت صفحه اصلی
**وضعیت قبل:** ۸ نسخه متفاوت فوتر (۲۸۲+۱۶+۱۰+… صفحه).

**اقدامات:**
- فوتر مرجع = فوتر کامل صفحه اصلی (ستون‌های معرفی + دسترسی سریع پروژه‌ای + حوزه‌های تخصصی + استانداردها + کپی‌رایت + اینستاگرام).
- در **۳۰۹ صفحه** جایگزین و در **۴ صفحه محصول که اصلاً فوتر نداشتند** اضافه شد.
- مسیردهی نسبی خودکار بر اساس عمق صفحه + مستثنا بودن crm/api طبق AC4.

**نتیجه نهایی سرشماری:**
| قالب | قبل | بعد |
|---|:---:|:---:|
| نسخه‌های فوتر | ۸ | **۱ فارسی + ۱ انگلیسی** |
| نسخه‌های منو | ۱۲+ | **۱ فارسی + ۱ انگلیسی** |
| صفحات با منوی موبایل کارا | ۲۸ | **۳۱۸** |

### 3️⃣ تکمیل تصاویر اختصاصی (ادامه اسپرینت ۵۲)
- ۲ تصویر باقیمانده تولید شد: **تصفیه آب** (water) و **انبار تجهیزات صنعتی** (industrial-equipment) + WebP.
- اکنون **هر ۱۳ صفحه خدمات/صنایع تصویر hero منحصربه‌فرد** دارد — صفر تکرار.

## 🔍 گزارش ممیز خودکار (`_tools/audit.py`)
```
بررسی 319 صفحه — ✅ همه بررسی‌ها PASS (0 خطا، 0 هشدار)
لینک‌ها ✔ | سینتکس JS ✔ | XML/JSON-LD ✔ | sitemap ✔ | سئو پایه ✔ | امنیت ✔ | محتوا ✔ | تصاویر ✔
```

## 🚀 دیپلوی
تغییرات سراسری است (هدر/فوتر همه صفحات) → **دیپلوی کامل** پوشه توصیه می‌شود (به‌جز `_drafts/` و `_tools/`).

## ⏭️ اسپرینت بعدی (۵۴)
US-106 — سیگنال‌های قدمت و اعتبار: Trust Bar، بج‌های ISO، تایم‌لاین شرکت، Schema تکمیلی.
📌 **یادآوری به کارفرما:** اعداد رسمی (سال تاسیس، تعداد پروژه، پرسنل) و کد eNAMAD (در صورت وجود) را ارسال کنید.
