# Release Notes v34.7.17 — یکپارچگی سنجه‌های تصمیم‌یار مدیریت و تحلیلگر

تاریخ: ۱۴۰۵/۰۵/۲۵ (2026-08-16)

## زمینه

کارفرما گزارش داد: «برای یک شرکت ۱۱ پیشنهاد دادیم، یکی را بردیم، سیستم نرخ برد ۱۰۰٪ نشان می‌دهد.» بررسی عمیق (`ARENA-DECISION-SUPPORT-ANALYZER-DEEP-REVIEW-2026-08-15.md`) نشان داد این باگ نمایشی نیست؛ خطای تعریف سنجه در هستهٔ محاسبات است و پنج ایراد بحرانی هم‌زمان روی اعداد میز مدیریت اثر می‌گذاشتند. این نسخه **فاز ۰** آن گزارش را پیاده می‌کند (تصمیم کسب‌وکاری کارفرما: عدد اصلی داشبورد = نرخ برد از کل آفرها؛ پیشنهادهای منقضی فقط گزارش شوند و وضعیت رکورد تغییر نکند).

## اصلاح

### فایل جدید — `crm/metrics-shared.js` (لایهٔ سنجهٔ مشترک، بدون وابستگی DOM)
منبع واحد سنجه برای تحلیلگر و تصمیم‌یار؛ قابل اجرا و تست در Node. شامل `resolveCustomer`، `offerTotalIRR`، `winStats`، `pWin`، `invoiceBilledIRR/CollectedIRR`، `isExpired`.

### F-01 — مخرج نرخ برد (ریشهٔ گزارش کارفرما)
- به‌جای یک عدد، سه سنجهٔ صریح: `winRateAll = برد/کل صادرشده` (عدد اصلی)، `winRateDecided = برد/(برد+باخت)` (کمکی)، `coverage = پوشش تعیین تکلیف`.
- درصد با نمونهٔ کمتر از ۳ اصلاً نمایش داده نمی‌شود (`—`) و هر درصد با مخرجش («۱ از ۱۱») نشان داده می‌شود.
- پوشش زیر ۶۰٪ → بنر هشدار «نرخ برد قابل استناد نیست» در تحلیلگر، بینش هم‌معنا در تصمیم‌یار و یادداشت در PDF.
- سناریوی کارفرما: **۱۰۰٪ → ۹٫۱٪**.

### F-02 — نشت خطا به پیش‌بینی درآمد
`anlForecast` دیگر نرخ بردِ متورم را به‌عنوان احتمال به کار نمی‌برد؛ برآورد هموارشدهٔ بیزی `p=(won+k·prior)/(issued+k)` با `k=5`، `prior=0.30` و سقف ۰٫۷۰. در سناریوی کارفرما احتمال از **۱۰۰٪ → ۱۵٫۶٪** و پیش‌بینی وزنی از «کل پایپ‌لاین» به مقدار واقع‌بینانه اصلاح شد.

### F-03 — هویت واحد مشتری
پیشنهاد/درخواست/فاکتور همگی از `resolveCustomer` استفاده می‌کنند: کد صریح → نگاشت نام نرمال‌شده (نیم‌فاصله، «ي/ك» عربی، علائم) به کد مشتری → نام نرمال‌شده با پرچم حل‌نشده. RFQهای بدون `custCd` (مسیر دستیار/قدیمی) دیگر سطر دوم نمی‌سازند؛ ستون‌های RFQ و CO روی یک سطر می‌نشینند و قاعدهٔ «مشتری نیازمند کنترل هزینه فروش» واقعاً فعال می‌شود. مشتری بدون کد با نشان ⚠ و در «کیفیت داده» گزارش می‌شود.

### F-04 — نرمال‌سازی ارز
`offerTotalIRR` نرخ مرجع سند (`fxRateRef`/`fxConvert.rate`/`marginAtClose.fxRate`) را اعمال می‌کند. سند ارزی **بدون** نرخ مرجع دیگر خام با ریال جمع نمی‌شود؛ از جمع خارج و در `offersFxNoRate` شمرده و در UI/PDF اعلام می‌شود (همان الگوی `skipped` نمودار حاشیه).

### F-05 — وصول و فاکتور از منبع واحد مالی
تصمیم‌یار و تحلیلگر به `PTF.invPaidSum(inv,{useAmountIrr:true})` و فیلتر فاکتور فعال متصل شدند: فاکتور ابطالی از «فاکتورشده» خارج، پرداخت ابطالی/برگشتی و پرداخت مهاجرت‌شده به Receipt دیگر شمرده نمی‌شود و `allocatedBase/allocatedVat` لحاظ می‌گردد. «نرخ وصول» با کارت حساب مشتری و خزانه هم‌خوان شد.

### موارد جانبی همین فاز
- TC (فنی-مالی) در قیف مالی تحلیلگر لحاظ شد تا با تصمیم‌یار و نمودار حاشیه یک تعریف داشته باشند.
- پیشنهاد گذشته از `validUntil` به‌عنوان «منقضی» **فقط گزارش** می‌شود؛ وضعیت رکورد دست‌نخورده می‌ماند (تصمیم کارفرما).
- `snapshot` ارسالی به AI اکنون `portfolio` و `metricContract` (تعریف صریح مخرج/ارز/منبع وصول) دارد تا تفسیر AI روی همان اعداد ممیزی‌شده انجام شود.
- `healthScore` بر مبنای نرخ بردِ واقعی، با کسر کوچک برای پوشش پایین و برچسب «دادهٔ ناکافی» برای مشتری بدون سابقهٔ معنادار.
- `rfqWithoutOwner` فیلد `owner` را هم می‌بیند؛ شاخص‌های جدید کیفیت داده: `offersUndecided`، `offersExpiredUndecided`، `offersFxNoRate`، `customersUnresolved`.

## چرا این تغییر «بدون آسیب» است

- هیچ رکوردی نوشته/تغییر داده نمی‌شود؛ تمام تغییرات در لایهٔ محاسبه و نمایش است (منقضی فقط برچسب تحلیلی است).
- `anlOfferFunnel().winRate` برای مصرف‌کنندگان قدیمی حفظ شده (معادل `winRateDecided`) ولی مبنای هیچ تصمیمی نیست.
- اگر `metrics-shared.js` بارگذاری نشود، `analyzer.js` با fallback هم‌رفتار (`_anlWinStatsFallback`) همان تعاریف را می‌سازد.
- مسیر مالی دست‌نخورده است؛ فقط مصرف‌کنندهٔ گزارشی به منبع واحد موجود (`finance-helpers.js`) وصل شد.

## تست رگرسیون

- `_tools/uat/tester420-v34.7.17-decision-support-metrics.js`: **30 PASS / 0 FAIL** — اجرای واقعی توابع در sandbox، شامل بازتولید عین سناریوی کارفرما و آزمون ارز/منقضی/فاکتور ابطالی/پرداخت مهاجرت‌شده/سقف احتمال.
- `_tools/uat/tester320-v34.4.15-management-intelligence.js`: 12 PASS / 0 FAIL (بخش رفتاری از eval توابع داخلی به اجرای ماژول در sandbox ارتقا یافت).
- `_tools/uat/tester1-flow.js`: 17 PASS / 0 FAIL (انتظار قدیمیِ `winRate===100` که همان باگ را کدگذاری کرده بود، با قرارداد جدید جایگزین شد).
- `tester188` (نمودار حاشیه)، `tester322` (گزارش‌های مدیریتی)، و تسترهای قرارداد نسخه (`tester287/300/301/303`): بدون شکست.
- اجرای کامل سوئیت (۴۰۸ تستر) و مقایسه با baseline: هیچ شکست جدیدی ایجاد نشد.

## ابزار ممیزی

`_tools/audit/repro-decision-support-winrate.js` — بازتولید عددی سناریوی کارفرما (`SCENARIO=A node …`) برای مقایسهٔ قبل/بعد.

## کنترل کیفیت

- `node --check` روی همهٔ فایل‌های JS تغییریافته: بدون خطا.
- بهداشت نسخه: `index.html` (`PTF_CRM_RELEASE`/`?v=`)، `sw.js` (`RELEASE`/`ASSET_VERSION`/`CACHE` + precache فایل جدید)، `manifest.json`، `clear-cache.html`، `shell.js` و `VERSION.json` همگی روی `v34.7.17`.
