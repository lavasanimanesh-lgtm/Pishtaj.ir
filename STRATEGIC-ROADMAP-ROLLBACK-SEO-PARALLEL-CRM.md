# 🧭 نقشهٔ راه اجرایی — اولویت، Rollback امن، تمرکز سئو/محتوا، و کار موازی دو ایجنت
**تاریخ:** ۲۰۲۶-۰۷-۲۵ | **مبنا:** یافته‌های `ARENA-COMPREHENSIVE-ASSESSMENT-2026-07-25.md`

پاسخ به ۴ سؤال شما به ترتیب، با راهکار عملی و قابل‌اجرا (نه فقط تئوری).

---

## ۱. اولویت‌بندی نهایی

خبر خوب: چیزی که پیدا کردم **دو دستهٔ کاملاً جدا** هستند و تداخلی با هم ندارند — یعنی می‌توانید هم‌زمان هر دو را پیش ببرید:

| مسیر | چه کسی/کِی | چرا مزاحم کار دیگری نیست |
|---|---|---|
| **مسیر A — ایمن‌سازی فوری (یک‌بار، پس‌زمینه)** | همین حالا، فقط چند دستور، بدون نیاز به توسعهٔ جدید | فقط دادهٔ نشتی را می‌بندد و گیت‌هاب را پاک می‌کند؛ به کد سایت یا CRM دست نمی‌زند |
| **مسیر B — تعمیق محتوا / UI / سئوی سایت عمومی** | از امروز، تمرکز اصلی من | کد سایت (`index.html`, `services/`, `knowledge-center/`, `assets/`) کاملاً از CRM جداست |
| **مسیر C — توسعهٔ CRM** | هم‌زمان، توسط ایجنت دوم | کد CRM (`crm/`, `api/`) از سایت جداست؛ فقط ۲ فایل مشترک (لوگو و فونت) دارند که تغییرشان نادر است |

**توصیه:** مسیر A را همین امروز با ۱۰ دقیقه کار (دستورات آماده در بخش ۲) ببندید — چون یک نشتی امنیتی فعال است، نه یک بدهی فنی معمولی. بعد از آن، مسیر B و C کاملاً موازی و بدون تداخل جلو می‌روند.

---

## ۲. راهکار Rollback در هر مرحله — تا دیگر تکرار نشود

مشکل اصلی نسخه‌های قبلی (از جمله Incident قطعی احراز هویت) این بود: **هیچ نقطهٔ بازگشت مشخصی وجود نداشت** — فقط یک ZIP و یک عدد نسخه در یک فایل که هرگز قطعی نبود. راهکار زیر یک سیستم rollback واقعی و چندلایه است:

### لایهٔ ۱ — Git Tag روی هر ریلیز پایدار (نه فقط عدد در کد)
از این به بعد، هر نسخه‌ای که «پایدار» اعلام می‌شود، باید یک **Git tag غیرقابل‌تغییر** بگیرد، نه فقط یک رشتهٔ متن در `window.VER`:

```bash
git tag -a stable-v33.0.3 -m "پایدار — پایه فعلی بعد از رفع Incident احراز هویت"
git push origin stable-v33.0.3
```

قاعدهٔ ساده: **قبل از هر تغییر پرریسک** (خصوصاً روی `api/auth.php`, `api/crm.php`, `crm/sync.js` که منشأ Incident قبلی بودند)، یک تگ جدید بزنید. رول‌بک همیشه یک دستور است:
```bash
git checkout stable-v33.0.3 -- crm/ api/     # فقط بازگردانی CRM/API به نقطهٔ سالم
# یا در سطح دیپلوی سرور:
git archive stable-v33.0.3 | tar -x -C /path/to/public_html
```

### لایهٔ ۲ — یک منبع واحد و قطعی نسخه (به‌جای ۷۸۰ فایل متناقض)
همین الان یک فایل `VERSION.json` در ریشهٔ پروژه می‌سازم که تنها منبع رسمی نسخه است (ماشین‌خوان، نه روایت‌محور). هر Release Note/Regression Report باید دقیقاً با این فایل هم‌گام باشد و `audit.py` این تطابق را بررسی می‌کند (این گیت از قبل وجود دارد، فقط باید همیشه رعایت شود).

### لایهٔ ۳ — جدا نگه‌داشتن «کد» از «دادهٔ زنده»
بخش زیادی از ریسک قبلی از این ناشی می‌شد که پوشهٔ دادهٔ واقعی CRM (`crm/data/`) داخل همان ریپو/ZIP کد بود. راهکار:
- کد (Git) و داده (سرور) را کاملاً جدا کنید؛ کد rollback می‌شود، داده هرگز بازنویسی نمی‌شود.
- به‌جای دستی زیپ‌کردن، از `git archive <tag>` برای ساخت ZIP دیپلوی استفاده کنید — تضمین می‌کند دقیقاً همان محتوایی که تگ خورده دیپلوی می‌شود، نه یک فولدر دستی که ممکن است فایل اضافه/جا‌افتاده داشته باشد (دقیقاً همان چیزی که باعث خطای `Cannot redeclare save_data()` در لاگ سرور واقعی شد — نشانهٔ آپلود دستی ناقص).

### لایهٔ ۴ — گیت اجباری قبل از هر Deploy (از ابزارهای موجود پروژه استفاده می‌کنیم، نه چیز جدید)
پروژه از قبل `_tools/audit.py` و `_tools/run-full-regression.js` را دارد. قاعدهٔ سخت‌گیرانه:

```
هیچ تگ جدیدی ساخته نمی‌شود مگر:
  audit.py  → ۰ خطا
  run-full-regression.js → ۰ FAIL واقعی (به‌جز موارد شناخته‌شدهٔ نسخه‌ای مستندشده)
```

اگر همین الان بخواهیم `v33.0.3` را به‌عنوان نقطهٔ rollback رسمی اعلام کنیم، اول باید ۲۸ باگ واقعی رگرسیون (نه ۳۰ مورد صرفاً نسخه‌ای) بررسی و مستندسازی شوند تا این تگ واقعاً «سالم» باشد — این کار را می‌توانم به‌عنوان قدم صفر انجام دهم.

**خلاصهٔ Rollback:** *«هر نسخهٔ پایدار = یک Git tag + یک VERSION.json هم‌گام + عبور از audit.py/regression + کد و داده جدا». بازگشت = یک دستور `git checkout <tag>`، نه جست‌وجو در ۷۸۰ فایل مستند.*

---

## ۳. تمرکز روی تعمیق محتوا + UI + سئو (برای رتبهٔ ۱ در ایران)

این می‌شود تمرکز اصلی من از این پس. برنامهٔ عملی:

### فاز ۱ — پاک‌سازی فنی سئو (پیش‌نیاز، سریع، ۱-۲ روز)
بدون این، تولید محتوای جدید روی یک پایهٔ معیوب سئو می‌نشیند:
1. یکی‌سازی ساختار URL (حذف دوگانگی `about/certificates.html` در برابر `about/certificates/index.html`) — رفع ۵۵۱ لینک شکسته و خطر محتوای تکراری.
2. رفع ۱۰۹ گروه عنوان (`title`) تکراری و توضیح‌متای (`description`) کوتاه/تکراری در `knowledge-center` و `tools`.
3. افزودن ۱۷۵ صفحهٔ جامانده به `sitemap.xml` و رفع ۸۳ صفحهٔ بدون favicon.
4. رفع نام‌گذاری خراب (mojibake) ۴۹ فایل مقالهٔ مرکز دانش (نام‌های `#U0627#U0628...` که برای گوگل و کاربر بی‌معنی و بد برای URL هستند).

### فاز ۲ — تعمیق محتوا (هستهٔ اصلی رتبه‌گیری)
- ۹۳ مقالهٔ مرکز دانش که زیر ۸۰۰ کلمه‌اند را تا استاندارد ۱۵۰۰+ کلمه (که خودِ پروژه هم به‌عنوان هدف تعیین کرده) گسترش می‌دهم — با تمرکز روی محتوای فنی واقعی (نه پرکننده): مشخصات فنی، استانداردها (API، ASME، ISO)، جدول مقایسه، پرسش‌های متداول، لینک‌های داخلی به محصولات/خدمات مرتبط.
- بر اساس داده‌های موجود (`_tools/kc-new-articles.json` شامل ۱۱۱ موضوع پیشنهادی و خوشه‌بندی‌های `کنترل ولو`/`ابزار دقیق`/`برق صنعتی` که در ریلیزهای اخیر آغاز شده) خوشه‌های محتوایی (topic clusters) را کامل و به هم پیوند می‌دهم — این دقیقاً الگویی است که گوگل برای «اتوریتهٔ موضوعی» پاداش می‌دهد.
- ساخت صفحات مقایسه‌ای و راهنمای خرید (که رقبای ایرانی این حوزه معمولاً ندارند) — فرصت رتبه‌گیری سریع با رقابت پایین‌تر.

### فاز ۳ — بهبود UI/UX سایت عمومی
- تجربهٔ موبایل (اکثریت ترافیک ایران موبایل است) — بازبینی صفحات خدمات/صنایع/مرکز دانش برای سرعت و خوانایی موبایل.
- بهبود CTA و مسیر تبدیل (RFQ) از داخل مقالات فنی — همان کاربر جست‌وجوگر فنی را به لید تبدیل کنیم، نه فقط ترافیک بگیریم.
- بهینه‌سازی Core Web Vitals (پروژه از قبل روی WebP/Lazy Loading کار کرده؛ ادامه با بررسی LCP/CLS واقعی روی صفحات پرترافیک).

### فاز ۴ — سنجش
گزارش دوره‌ای رتبه/ترافیک/کلمات کلیدی هدف در حوزهٔ فنی صنعتی (پایپینگ، ابزار دقیق، برق صنعتی، شیرآلات کنترلی و...) تا مسیر «رتبهٔ ۱ ایران» قابل ردیابی باشد، نه صرفاً حسی.

**نکته:** تمام این کار روی فایل‌های `index.html`, `about/`, `services/`, `industries/`, `knowledge-center/`, `blog/`, `assets/` انجام می‌شود — **هیچ‌کدام در پوشهٔ `crm/` یا `api/` نیستند**، پس با کار ایجنت دوم روی CRM تداخل صفر دارد.

---

## ۴. چطور یک ایجنت دیگر هم‌زمان روی CRM کار کند — بدون تداخل و بدون رگرسیون به مشکلات قبلی

### چرا از نظر فنی امکان‌پذیر و کم‌ریسک است
بررسی کردم: پروژه **از قبل به‌صورت طبیعی دو بخش مجزا** است:

| بخش | مسیرها | نقطهٔ اشتراک با بخش دیگر |
|---|---|---|
| سایت عمومی (من) | `index.html`, `about/`, `services/`, `industries/`, `knowledge-center/`, `blog/`, `assets/css`, `assets/js`, `sitemap.xml` | فقط `assets/images/ptf-logo.png` و فونت Vazirmatn (مشترک، تغییرشان نادر است) |
| CRM (ایجنت دوم) | `crm/*.js`, `crm/index.html`, `api/*.php` | یک نقطهٔ تماس واقعی: `api/crm.php` اکشن `add_rfq_site` که از فرم صفحهٔ اصلی سایت به CRM داده می‌فرستد |

یعنی ۹۹٪ کار دو ایجنت اصلاً فایل مشترک ندارد.

### راهکار پیشنهادی گیت‌هاب (باید توسط شما یا یک نشست دیگر Arena روی گیت‌هاب اجرا شود — من در این نشست فقط مجاز به کار روی شاخهٔ فعلی خودم هستم)

```
main                    ← فقط از طریق PR، همیشه معادل آخرین تگ پایدار deploy-شده
 ├─ site/seo-content     ← شاخهٔ من (این نشست) برای محتوا/UI/سئو
 └─ crm/development       ← شاخهٔ ایجنت دوم برای توسعهٔ CRM
```

قواعد کار موازی:
1. هرکدام از ما فقط داخل مسیر خودمان (`site/*` یا `crm/*` و `api/*`) تغییر می‌دهیم.
2. هر PR قبل از merge به `main` باید از گیت `audit.py` + `run-full-regression.js` عبور کند (چون این ابزارها کل پروژه را چک می‌کنند، هر تداخل واقعی همان‌جا مشخص می‌شود، نه بعد از دیپلوی).
3. Merge به `main` **فقط سریالی** انجام شود (یکی در هر لحظه) تا اگر تداخل نادری (مثل تغییر هم‌زمان `crm/index.html` برای لوگو) پیش آمد، به‌سادگی در PR دیده و حل شود، نه در Production.
4. بعد از هر merge موفق به `main`، بلافاصله طبق بخش ۲ یک Git tag پایدار جدید ثبت می‌شود.

### چرا این باعث تکرار Incident قبلی نمی‌شود
Incident قبلی (`BUG-AUTH-001`) دقیقاً به این دلیل رخ داد که یک تغییر (روی auth) **مستقیم و بدون staging به Production رفت**. راهکار بالا این را دو لایه مسدود می‌کند:
- لایهٔ PR + گیت خودکار (`audit.py`/regression) قبل از رسیدن به `main`.
- توصیهٔ باز پروژه (`STAGE0-ENV-001`) برای محیط Staging مستقل هنوز به قوت خودش باقی است — به‌خصوص برای کار ایجنت CRM روی `api/auth.php` یا `crm/sync.js`، پیشنهاد می‌کنم قبل از merge به `main`، حتماً یک بار روی زیردامنهٔ staging تست شود (این تنها بخشی است که نیاز به تصمیم/زیرساخت شما دارد، نه کد).

---

## ۵. یکپارچه‌سازی نهایی و Deploy

وقتی هر دو مسیر (سایت و CRM) به‌طور جداگانه در PR خودشان به `main` merge و تگ خوردند:

```bash
# ۱. ساخت بستهٔ دیپلوی دقیقاً از تگ (نه از پوشهٔ دستی کارگر)
git archive --format=zip -o pishtaj-release-<TAG>.zip <TAG>

# ۲. آپلود به سرور و استخراج در یک پوشهٔ موقت (نه مستقیم روی public_html)
unzip pishtaj-release-<TAG>.zip -d /tmp/release-<TAG>

# ۳. سوییچ اتمی (symlink) به‌جای rsync/overwrite مستقیم
ln -sfn /var/releases/<TAG> /var/www/pishtaj.ir-current
```

مزیت الگوی symlink: **rollback یعنی فقط برگرداندن یک symlink به تگ قبلی** — چند ثانیه، بدون کپی مجدد فایل، بدون ریسک نیمه‌کاره ماندن دیپلوی (که ریشهٔ خطای `Cannot redeclare save_data()` در لاگ واقعی سرور بود).

دادهٔ CRM (`crm/data/`) در یک مسیر کاملاً خارج از پوشهٔ release نگه داشته می‌شود و هرگز داخل ZIP کد قرار نمی‌گیرد — دقیقاً طبق قانونی که خودِ پروژه برای Staging نوشته بود ولی برای دیپلوی اصلی رعایت نشده بود.

---

## ۶. جمع‌بندی اقدام فوری (چک‌لیست امروز)

| # | اقدام | مسئول | وابسته به تصمیم شما؟ |
|---|---|---|---|
| 1 | چرخش رمز/ابطال توکن‌های افشاشده + پاک‌سازی `crm/data`, لاگ‌ها از گیت | شما (روی سرور واقعی) یا من با تأییدتان | بله — نیاز به تأیید صریح شما برای rewrite تاریخچهٔ گیت |
| 2 | افزودن `.gitignore` مناسب تا نشتی تکرار نشود | من، همین الان | خیر |
| 3 | ساخت `VERSION.json` به‌عنوان منبع واحد نسخه | من، همین الان | خیر |
| 4 | تگ‌گذاری `stable-v33.0.3` پس از بررسی/مستندسازی ۲۸ باگ واقعی رگرسیون | من | خیر (فقط اطلاع‌رسانی) |
| 5 | ساخت شاخه‌های `site/seo-content` و `crm/development` در گیت‌هاب | شما یا نشست Arena دیگر | بله — خارج از دسترس این نشست |
| 6 | شروع فاز ۱ سئو (یکی‌سازی URL، رفع لینک شکسته، sitemap) | من، از همین امروز | خیر |

اگر تأیید کنید، همین الان مورد ۲ و ۳ را انجام می‌دهم و شروع فاز ۱ سئو را آغاز می‌کنم؛ برای مورد ۱ و ۵ منتظر تأیید/اقدام شما می‌مانم.
