# یادداشت انتشار CRM v34.8.17

تاریخ: ۱۴۰۵/۰۶/۰۵ (۲۰۲۶-۰۸-۲۷)

## رفع باگ کارتابل — گزارش کارفرما: «یادآور ثبت کردم؛ روی کارتابل هر چند ثانیه یک بار تکرار می‌شود»

### ریشه (سه عامل هم‌زمان)
1. `bridge.checkDueReminders` روی هر تیک poll برای یادآور سررسیدشده کارت کارتابل می‌سازد؛ state ضدتکرار (`notifiedUsers`) **داخل رکورد یادآور** است.
2. اعلان یادآور برخلاف rfq-due/deal-due **بدون `dkey`** ساخته می‌شد — سپر دومِ ضدتکرار نداشت.
3. `entity_upsert` (مسیر فرمان v34.8.13-16) در به‌روزرسانی، **کل رکورد را جایگزین** می‌کرد؛ upsert دیرهنگام/دوباره‌ارسالی `notifiedUsers` را پاک می‌کرد → تیک بعدی poll کارت تکراری می‌ساخت → حلقهٔ چندثانیه‌ای.

### رفع سه‌لایه
- **سرور:** به‌روزرسانیِ `entity_upsert` حالا merge است — فیلد غایب در payload یعنی «دست‌نخورده» نه «پاک»؛ `updatedAt/updatedBy` ثبت می‌شود.
- **کلاینت:** اعلان یادآور `dkey` پایدار گرفت (`rem-due-<cd>:<user>`)؛ state ضدتکرار از مسیر فرمان ذخیره می‌شود (نه پوش انبوه).
- **خودترمیم:** کارت‌های تکراریِ ساخته‌شده قبل از فیکس، یک‌بار در بوت صندوق جمع می‌شوند (`ptfSweepDuplicateReminderNotifs` — کارت‌های done و سایر انواع دست‌نخورده).

### کنترل
tester521 رفتاری (شبیه‌سازی سه تیک با revert وسط راه + مدل merge سرور + جارو) — گیت CI سبز.
