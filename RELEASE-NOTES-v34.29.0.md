# RELEASE NOTES — v34.29.0 (جستجوی محصولات CMS + عکس هوشمند + دو صفحهٔ استاندارد + سپر نسخهٔ کهنه)

**تاریخ:** ۲۰۲۶-۰۹-۰۲ · **کمیت:** tester575 (۱۸ سنجه) · **گیت:** 191 PASS / 0 FAIL

## گزارش‌های کاربر

«در استیجینگ هنوز آن دو تب کار نمی‌کند» + «در تب محصولات فهرست محصولات باید قابلیت جستجو داشته باشد تا از میان انبوهی از محصولات بتوان صفحه محصول مورد نظر را ساخت» + «دو صفحه جدید را هم اصلاح کن و عکس را قرار بده و فرمتشان را مانند سایر صفحات محصول قبلی کن».

## ① چرا تب‌ها در استیجینگ «هنوز» خراب به نظر می‌رسید — کد مبرا، عامل: کلاینت کهنه

استیجینگ **دقیقاً کامیت ff36d38 (v34.28.0)** را سرو می‌کند (پانوشت خود صفحه). فایل `cms.js` سرو‌شده هم TAB-DIRECT را دارد. بازتولید کامل با **کلیک واقعی DOM** (نه فراخوانی مستقیم): دکمهٔ سایدبار → کلیک تب «صفحهٔ جدید» → فرم کامل ✅، کلیک «کیفیت» → ✅. یعنی کدِ مستقر سالم است و مشکل از **مرورگر/تبِ بازِ قدیمی** است (JS نسخهٔ ۲۵ در حافظه؛ بنر تشخیص هم در آن نسخه وجود ندارد).

**رفع ریشه‌ای این کلاس خطا — VER-SHIELD (index.html):** در هر بارگذاری، `manifest.json` با `cache:'no-store'` (مسیر network-first سرویس‌ورکر) خوانده و با `PTF_CRM_RELEASE` مقایسه می‌شود؛ اگر مرورگر قدیمی بود، بنر تمام‌صفحهٔ «بارگذاری نسخهٔ جدید» می‌آید (یک‌بار برای هر نسخهٔ سرور). از این پس هیچ کاربری روی نسخهٔ کهنه گیر نمی‌کند.

## ② جستجوی زنده در تب محصولات (PROD-SEARCH)

- نوار جستجو با `oninput` زنده روی **نام / نام انگلیسی / برند / مدل / کد کالا**.
- شمارندهٔ نتایج («۸۳۳ نتیجه برای…»)، سقف کارایی: ۲۰۰ بدون جستجو / ۴۰۰ با جستجو، پیام «یافت نشد»، حفظ کوئری بعد از بازرندر.
- اثبات با بوت کامل jsdom + ۲۵۰۰ کالای واقعی‌شکل: پیش‌فرض ۲۰۰ ردیف؛ جستجوی «گیت» → ۸۳۳ نتیجه، ردیف‌های منطبق ✅.

## ③ عکس هوشمند (SMART-IMG)

- فرم «صفحهٔ محصول» دیگر عکس را با لوگوی پیش‌فرض پر نمی‌کند: از روی نام/برند/مدل کالا، عکس صنعتی متناسب از آرشیو `assets/images/products/generated/` (۷۳ عکس) حدس زده می‌شود (gate→گیت‌ولو API600، elbow/fitting→بات‌ولد، pump→API 610، ...).
- همان نقشه سمت سرور (`cms_prod_img_guess`): اگر عکس خالی/لوگو بماند، سرور هم عکس متناسب می‌گذارد — صفحات آینده هرگز بی‌عکس نمی‌مانند.

## ④ دو صفحهٔ محصول جدید — بازسازی در قالب استاندارد

ریشهٔ «فرمت متفاوت»: مولد قبلی اسکلت مرکز دانش را با کلاس‌های بی‌CSS (`article-hero`) کپی می‌کرد و لینک‌های نسبی هم‌پوشه (`astm-a312.html`) را به `/products/` می‌برد (۴۰۴). دو صفحه با قالب واقعی صفحات محصول (`services/products/*.html`) بازسازی شدند:

- **products/gate-valve-16-inch-cl600.html** — هیرو تیره با عکس `gate-valve-api600-realistic.jpg`، چیپ‌ها (API 600 / B16.5 / WC6 / Gear Operated)، جدول مشخصات، برند-استریپ CRANE/NEWAY، FAQ، اسکیمای Product+FAQ+Breadcrumb.
- **products/42-inch-wpb-wphy-elbow.html** — هیرو با عکس `butt-weld-fittings-realistic.jpg`، مشخصات WPB/WPHY/B16.9/MSS SP-75، FAQ و اسکیما.
- هر دو: بدون جدول/سؤال تکراری (DEDUP)، بدون مسیر `../../` شکسته، لینک «مطالب مرتبط» → `knowledge-center/` درست.
- مولد هم اصلاح شد (`cms_rel_links_fix`): لینک نسبی هم‌پوشه در cta/footer هنگام ساخت صفحهٔ محصول به `../knowledge-center/` بازنویسی می‌شود.

## ⑤ بستهٔ انتشار — تغییرات

`crm/cms.js` (PROD-SEARCH + SMART-IMG + VER)، `crm/index.html` (VER-SHIELD + ۱۰۹ کش‌باست `?v=34.29.0`)، `crm/sw.js` (CACHE جدید)، `crm/manifest.json`، `crm/clear-cache.html`، `crm/shell.js`، `api/sales-domain.php`، `api/cms.php` (SMART-IMG + REL-LINK)، `products/` (دو صفحهٔ جدید)، `VERSION.json`، tester574↔575 و گیت.

## ⑥ بعد از دیپلوی

1. فایل‌ها را آپلود کنید؛ برای پروداکشن حتماً **دو فایل `products/*.html`** را جداگانه FTP کنید.
2. CRM را باز کنید — اگر بنر «نسخهٔ جدیدی روی سرور است» آمد، دکمهٔ بارگذاری را بزنید (همین بنر از این پس مشکل «تب کهنه» را برای همیشه می‌بندد).
3. مدیریت سایت → تب محصولات: جعبهٔ جستجو بالا؛ نام کالا را بنویسید و «🌍 ساخت صفحه» بزنید — عکس پیش‌فرض هوشمند از قبل پر شده.
