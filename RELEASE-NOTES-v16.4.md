# 🚀 ریلیز v16.4 — اسپرینت «و»: تجربه کاربری و رهگیری کانبانی
**تاریخ:** ۱۴۰۵/۰۴/۱۹ | **نسخه قبلی:** v16.3 | **بسته:** `pishtaj-release-v16.4.zip` (تجمیعی از v14.5)

---

## 📦 محتوای این اسپرینت (۴ استوری مصوب)

| # | استوری | شرح | وضعیت |
|---|--------|------|-------|
| ۱ | **US-361** | بازطراحی تور آموزشی — هماهنگی کامل با منوی آکاردئونی | ✅ |
| ۲ | **US-375** | تور اختصاصی ۵ گامی نوار پایین موبایل | ✅ |
| ۳ | **US-372** | dialogx: دیالوگ‌های هم‌تم به‌جای پیام‌های «pishtaj.ir says» (فاز ۱) | ✅ |
| ۴ | **US-369 (نسخه نهایی شما)** | کانبان وضعیت برای «درخواست‌ها» و «پیشنهادها» — نه مشتری | ✅ |

---

## ۱) US-361 — تور آموزشی هماهنگ با آکاردئون 🎓

مشکل گزارش‌شده شما (دو سرشاخه روشن، کادر روی سرشاخه، زیرشاخه‌ها دیده نمی‌شدند) به‌طور ریشه‌ای حل شد:

- **منبع واحد:** `shell.js` گروه‌بندی منو را export می‌کند (`PTF_NAV_GROUPS`) و تور دقیقا از همان می‌خواند — هیچ فهرست موازی/وصله‌ای وجود ندارد.
- پیش از هر گام، **گروه والد ماژول باز و همه گروه‌های دیگر بسته** می‌شوند → در هر لحظه فقط یک گروه روشن است.
- اسپات‌لایت **دقیقا هم‌اندازه کادر آیتم ماژول** (نه هدر گروه، نه بزرگ‌تر) و بعد از پایان انیمیشن آکاردئون اندازه‌گیری می‌شود (بدون پرش).
- ترتیب گام‌ها **گروه‌به‌گروه**: همه ماژول‌های یک سرشاخه تمام می‌شوند، بعد سرشاخه بعدی باز و قبلی بسته می‌شود.
- پس از پایان یا «رد کردن»: **همه منوهای کشویی بسته** می‌شوند؛ ترجیح ذخیره‌شده کاربر (`ptf_nav_open`) دست نمی‌خورد و در ورود بعدی همان گروه همیشگی‌اش باز است.
- RBAC مثل قبل: ماژول غیرمجاز اصلا در تور نمی‌آید (این بار با چک `display` — نه `offsetParent` که آیتم‌های گروهِ بسته را به‌غلط حذف می‌کرد؛ **ریشه باگ قبلی همین بود**).
- توضیح ماژول‌های جدید (تنخواه، تحلیلگر، پیامک، مدیریت سایت، گزارش‌ها، کاربران) هم به تور اضافه شد.

## ۲) US-375 — تور نوار پایین موبایل 📱

- تور کوتاه ۵ گامی: **داشبورد ← کارتابل ← دکمه برجسته پیشنهاد (FAB) ← دستیار ← سایر**.
- گام «سایر»: کشوی تمام‌صفحه واقعا باز می‌شود و شبکه آیکون‌های رنگی + جستجوی سریع معرفی می‌شود؛ پایان تور کشو بسته می‌شود.
- اسپات‌لایت هم‌اندازه هر دکمه؛ کادر توضیح **بالای نوار** — هدف را نمی‌پوشاند.
- اجرای خودکار **یک‌باره برای هر کاربر** (کلید جدا `ptf_tour_mnv_done_<user>`) + دکمه اجرای دستی در تنظیمات.
- **تفکیک کامل از تور دسکتاپ:** در موبایل تور دسکتاپ اجرا نمی‌شود و بالعکس — هیچ تداخلی ندارند.

## ۳) US-372 — dialogx: پیام‌های هم‌تم نرم‌افزار 💬

ماژول جدید `crm/dialogx.js`:
- سه سرویس Promise-محور: `dialogx.alert / confirm / prompt` — RTL، هماهنگ با حالت روشن/تاریک (متغیرهای تم موجود)، آیکون و عنوان، دکمه‌های گرادیانی برند، پشتیبانی متن چندخطی.
- **ESC و کلیک بیرون = انصراف امن** (هرگز جریان را نمی‌شکند) + **focus trap** برای دسترس‌پذیری + موبایل: دکمه‌های تمام‌عرض.
- **فاز ۱ مهاجرت (طبق AC):**
  - پیام پرتکرار «ثبت تکراری» موتور dedup (US-174) → دیالوگ هم‌تم با آیکون ♻️
  - اعتبارسنجی فرم‌های اصلی («نام شرکت را وارد کنید» در مشتری/تامین‌کننده/درخواست — ۵ نقطه)
  - confirm حیاتی «حذف پیشنهاد» → دیالوگ قرمز خطر با تستر رفتاری
- **بدون ریسک رگرسیون:** هر نقطه fallback بومی دارد (اگر dialogx لود نشده باشد همان alert قبلی اجرا می‌شود)؛ «حذف با دلیل» و «مختومه‌سازی باخت» از قبل مودال اختصاصی هم‌تم داشتند و دست نخوردند.
- **باقیمانده برای مهاجرت تدریجی (AC6):** confirmهای زنجیره‌ای مختومه‌سازی/بایگانی (`salesfiles/archive`)، تاییدهای گردش کار TO/CO (به‌دلیل همزمانی sync رفتارشان sync است و مهاجرت async باید محتاطانه و تک‌به‌تک باشد)، پیام‌های راهنمای اکسل. در اسپرینت‌های بعد بی‌سروصدا مهاجرت می‌کنند.

## ۴) US-369 (نسخه نهایی شما) — کانبان درخواست‌ها و پیشنهادها 🗂

ماژول جدید `crm/kanban.js` — **فقط «نمای دوم» روی داده موجود؛ صفر منطق وضعیت جدید:**

**ماژول درخواست‌ها:**
- دکمه تعویض نما «📋 جدول / 🗂 کانبان» کنار جستجو؛ ستون‌ها = همان ۱۰ وضعیت موجود (دریافت اولیه … مختومه) از **همان منبع `RFQ_STATUSES`** (export شد — فهرست موازی ساخته نشد).
- **درگ کارت بین ستون‌ها = دقیقا همان مسیر مودال تغییر وضعیت:** هسته مشترک `ptfRfqSetStatus` از دل `saveRfqStatus` بیرون کشیده شد (refactor، نه کپی) — یعنی برچسب «منتظر صدور TO/CO»، اعلان کارتابل فروش، audit و **سینک وضعیت درخواست‌های سایت با سرور رهگیری**، همه مثل قبل و از یک نقطه اجرا می‌شوند.
- بج‌های موجود روی کارت: ⏰ مهلت پاسخ (US-348)، 📎 ضمایم (US-388)، ⏳ منتظر صدور، 🌐 از سایت.
- جستجوی بالای ماژول روی کارت‌های کانبان هم اثر می‌کند.

**ماژول پیشنهاد:**
- کانبان روی تب فنی (TO) و مالی (CO/TC) — ستون‌ها = همان وضعیت‌های موجود: TO شش‌گانه US-367 / CO پنج‌گانه؛ مهاجرت نرم داده قدیمی (won→approved) همان قاعده جدول.
- **درگ = همان `offerSetSt` با تمام گاردها:** تایید صریح «برنده» + قفل + انضمام مدارک به پرونده فروش + هدایت خرید واقعی (US-392)، confirm «عدم تایید» TO و بستن مسیر →CO — هیچ میان‌بری دور گاردها وجود ندارد.
- کارت CO برنده **قفل است و درگ نمی‌شود** (پیام همان قاعده جدول).
- بج‌ها: اعتبار CO (US-157)، →CO قفل/بسته، مبلغ کل.

**مشترک:**
- کلیک/تپ ساده روی کارت = باز کردن همان پنجره ویرایش موجود.
- **نمای انتخابی هر کاربر ذخیره و بین دستگاه‌ها سینک می‌شود** (`ptf_crm_settings.kanbanView` — همان الگوی dashOrder).
- موبایل: اسکرول افقی ستون‌ها + **درگ لمسی با نگه‌داشتن ۳۰۰ms** (همان موتور Pointer لانچر v12.9 — حرکت زودهنگام = اسکرول، درگ نمی‌شود).
- فیلتر صنعت مشتریان (بخش دوم US-369 قدیم) سر جای خود؛ به ماژول مشتریان هیچ دستی زده نشد.

---

## 🛡 اثبات «بدون وصله‌پینه، بدون باگ جدید، بدون تداخل با طرح توسعه»

| اصل | اثبات |
|-----|-------|
| بدون وصله‌پینه | کانبان از هسته مشترک واقعی استفاده می‌کند: `saveRfqStatus` به wrapper نازک + هسته `ptfRfqSetStatus` بازآرایی شد (یک منطق، دو نما). تور از `PTF_NAV_GROUPS` خودِ shell.js می‌خواند. dialogx یک ماژول واحد سراسری است نه چند کپی. |
| وابستگی‌ها | زنجیره hookهای renderRfq/renderOffers (workflow، backup، bridge، guards) سالم — kanban آخر زنجیره اضافه شد و قبلی‌ها را صدا می‌زند. hook تنظیمات تور مثل قبل. sw.js و cache-bust کامل. |
| بدون باگ جدید | tester82 جدید: **۶۵ چک** (کد + رفتاری: درگ→وضعیت→notify→سینک سایت→رفت‌وبرگشت با جدول طبق شرط شما) + **۲ اثبات DOM با jsdom**: کانبان (۱۰ چک — تعویض نما، رندر ستون، جابجایی کارت پس از تغییر وضعیت) و dialogx (۱۰ چک — OK/ESC/کلیک بیرون/prompt). رگرسیون کامل: **۶۹ تستر / ۱۹۲۳ چک — صفر خطا**. audit سایت PASS (فقط هشدار قدیمی تصاویر سنگین). |
| بدون تداخل با طرح توسعه | US-399 (تامین‌کننده تخصصی) و اسپرینت‌های ح/ط هیچ فایلی مشترک با این تغییرات ندارند جز offers.js که فقط confirm حذف آن refactor شد (منطق عینا در `ptfOfferDelDo`). مهاجرت تدریجی dialogx فهرست شد و اجباری یک‌جا ندارد. |

## 📋 راهنمای به‌روزرسانی (مثل همیشه)
1. `pishtaj-release-v16.4.zip` را روی هاست **روی فایل‌های موجود** اکسترکت کنید — پوشه `api` حذف نشود (بانک داده `api/ptf_crm_data.json` عمدا داخل بسته نیست).
2. همه کاربران یک‌بار صفحه CRM را رفرش کنند — نسخه در پایین سایدبار باید **v16.4** شود.
3. کانبان: داخل «درخواست‌ها» یا «پیشنهاد» دکمه «🗂 کانبان» کنار جستجو — انتخاب هر کاربر ذخیره می‌شود.
4. تور جدید: تنظیمات → «🎓 اجرای مجدد معرفی ماژول‌ها» (دسکتاپ) / «📱 تور نوار پایین موبایل».
