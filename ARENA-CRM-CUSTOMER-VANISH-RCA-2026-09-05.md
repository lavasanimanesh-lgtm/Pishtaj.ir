# ARENA · ریشه‌یابی P0 — «مشتری تازه‌ثبت‌شده ناپدید می‌شود؛ مجدد ثبت می‌کنم، دوباره پاک می‌شود»

**تاریخ:** 2026-09-05 · **مخزن:** `lavasanimanesh-lgtm/Pishtaj.ir` @ `1952dbe` (`main`, **v34.37.2**) · **شاخۀ این تحلیل:** `arena/01a0723f-pishtaj-ir`
**وضعیت:** **تحلیل و بازتولید — هیچ کد محصولی تغییر نکرده** (کارفرما فرمان اصلاح نداده است).

---

## ۰) نتیجه در یک بند

مشتری **واقعی** پاک می‌شود — نه پنهان، نه فیلتر. علت، **روتر «ذخیرۀ مبتنی بر دیف» است: `window.ptfEntitySaveCollection` در `crm/sales-domain-v2.js:577`.**
این روتر هر بار که مجموعه‌ای را «کامل و معتبر» تحویل می‌گیرد، فهرست را با **اسنپ‌شات حافظه‌ای خودِ همان کلاینت** مقایسه می‌کند و هر `cd` که در فهرست جدید نباشد را **حذف قطعی با سنگ‌قبر دائمی** می‌فرستد (`entity_delete`).
مشکل این‌جاست که در یک کلیکِ «ثبت مشتری»، **سه بار پشت سر هم** کل مجموعۀ مشتریان ذخیره می‌شود (ذخیرۀ خودِ فرم، سپس «درمان از استعلامات» داخل `renderCustomers2`، و سپس هوک نرمال‌سازی تلفن)، و **دو مورد آخر آرایه را از `getData()` تازه می‌خوانند**؛ `getData` لایۀ فاز B (`crm/client-server.js:1112`) است که یا **کش ۳۰ ثانیه‌ای کهنه** می‌دهد یا — وقتی نوشتن روی `localStorage` رد شده باشد — عمداً کش را می‌اندازد. پس آن خواندن، رکوردی را که چند میلی‌ثانیه پیش ساخته شده **ندارد**، در حالی که اسنپ‌شات حافظۀ روتر آن را دارد ⇒ «رکورد غایب» ⇒ **حذف**.
و چون گاردهای v34.37.0 (`DELETE_BURST_MAX=3`, `:648-660`) فقط **تعداد** را می‌شمارند و در این الگو هر بار دقیقاً **یک** رکورد غایب است، سپر هرگز فعال نمی‌شود. سنگ‌قبر هم دائمی است: `entity_delete` در سرور **بی‌استثنا** `kind:'archive_purge'` + `identities` می‌نویسد (`api/sales-domain.php:2600`) و سرور و کلاینت هر دو آن را روی همهٔ خواندها اعمال می‌کنند ⇒ کپیِ زندۀ دستگاه دیگر هم نجاتش نمی‌دهد. احیای سطل بازیافت سنگ‌قبر را خنثی می‌کند (`kind:'restored:…'` در `api/sales-domain.php:2663-2665`) ⇒ داده **قابل برگشت** است؛ اما تا علتِ کلاینت پچ نشود، ذخیرۀ خودکارِ بعدی دوباره همان کار را می‌کند.

**چرا پیشنهاد مانده؟** چون هوک تلفن و «درمان»، فقط روی `ptf_crm_customers` / `ptf_crm_suppliers` نشسته‌اند؛ `ptf_crm_offers` در این مسیر اصلاً ذخیرۀ مجدد نمی‌شود. نام مشتری هم در خودِ رکورد پیشنهاد (`buyerCo`) کپی شده ⇒ «پیشنهاد با نام او هست، ولی مشتری در فهرست نیست».

---

## ۱) زنجیرۀ علّی، خط‌به‌خط

| # | رخداد | شاهد (فایل:خط) |
|---|---|---|
| ۱ | کاربر «ثبت» می‌زند → `saveCust2(cd)`؛ برای رکورد نو `dedupStamp` می‌خورد و `window.ptfEntitySaveCollection('ptf_crm_customers', items, {reason:'offer-cust'})` صدا زده می‌شود | `crm/offers.js:3604`, `:3685-3687` · `crm/index.html:4265`, `:4275-4276` |
| ۲ | روتر: `base` (سمت «قبل») = **اسنپ‌شات حافظه‌ای** `_ptfEntityLastKnown[collection]`؛ `nextArr` (سمت «بعد») = همان آرایه‌ای که فراخوان داده | `crm/sales-domain-v2.js:620-626` · `:762-763` (`_ptfEntityLastKnown` = کپی عمیق آخرین ذخیرۀ همین کلاینت) |
| ۳ | دیف: `cd` که در `nextArr` نیست ⇒ `dels` ⇒ فرمان `entity_delete` **واقعی** (نه سنگ‌قبرِ بازیافت) | `crm/sales-domain-v2.js:641-646`, `:703-707` |
| ۴ | پیش از فرمان‌ها، `ptfSilentWrite(collection, JSON.stringify(nextArr))` — اگر `ptfStorageSafeSetItem` مقدار `false` بدهد (کووتا/حافظۀ پُر)، `ptfBApplyServerProjection` **کش را می‌اندازد و `false` برمی‌گرداند**؛ دیسک به‌روزرسانی نمی‌شود | `crm/sales-domain-v2.js:665-668` · `crm/client-server.js:1081`, `:1161` (`if (!stored) { delete cache[k]; return false; }`) |
| ۵ | **بلافاصله بعد از برگشت `saveCust2`، هوک نرمال‌ساز تلفن دوباره کل مجموعه را ذخیره می‌کند** — و آرایه را از `getData()` می‌خواند و `rec` را با `filter(cd)` یا `items[0]` پیدا می‌کند (اگر `cd` نباشد: **اولین سطر فهرست**) | `crm/phonefmt.js:89-103` (`hookCust` دور `window.saveCust2`)، بدنهٔ `:86-99` |
| ۶ | در همین کلیک، `renderCustomers2()` هم `ptfHealMissingCustomersFromRfqs()` را صدا می‌زند که در صورت ساختن رکوردِ نوپا، **باز هم کل مجموعه را ذخیره می‌کند** (`reason:'rfq-cust-heal'`؛ `stub` با `unshift` اضافه می‌شود) | `crm/offers.js:3858-3859`, `:3846`, `:3851` |
| ۷ | هر یک از ذخیره‌های ۵ و ۶ اگر `getData()`شان کهنه باشد (کش ۳۰ث، یا دیسکی که در گام ۴ نوشته نشد، یا `pull` در راهی که کش را با نسخۀ «بدون رکورد نو» پر کرده) ⇒ رکورد نو در `base` هست، در `nextArr` نیست ⇒ `dels=[همان رکورد]` | `crm/client-server.js:1118` (TTL ۳۰s) · `crm/sales-domain-v2.js:620-646` |
| ۸ | سپر حذف انبوه فقط «>۳ رکورد» یا «فهرست خالی» را می‌گیرد؛ **یک حذف، از نظر سپر یعنی «کاربر خود یک‌به‌یک حذف کرده»** ⇒ عبور | `crm/sales-domain-v2.js:648-660` (`DELETE_BURST_MAX = 3`) |
| ۹ | سرور `entity_delete` را اعمال می‌کند: `array_splice` روی سطر + درج **سنگ‌قبر دائمی** — **بدون هیچ استثنا با `kind:'archive_purge'`** و `identities:{coll:[id]}` و `reason` از متن فرمان (این‌جا `phonefmt` / `offer-cust` / `rfq-cust-heal`). کامنت خودِ کد: «دستگاه‌های stale رکورد را زنده نمی‌کنند» | `api/sales-domain.php:2599-2602` |
| ۹ب | کلاینت هم **همان لحظه** سطر بازیافت می‌نویسد (`kind:'recycle'` + `snapshot` کامل) — و آن را هم با **دیف کل مجموعه** ذخیره می‌کند؛ `ptf_crm_deleted_archive` در `DELETE_BURST_GUARDED` نیست ⇒ خودِ سطل بازیافت هم از همین مکانیزم در امان نیست | `crm/sales-domain-v2.js:462-471` · `DELETE_BURST_GUARDED` `:648-652` |
| ۱۰ | از این پس هر `pull` (سرور و قرینۀ کلاینت) همان `id` را سرکوب می‌کند؛ گارد تاریخ هم بی‌اثر است، چون رکوردِ هدف **قبل از** سنگ‌قبر متولد شده (`rowEpoch <= tombEpoch` ⇒ سنگ‌قبر برنده) ⇒ **کپیِ زنده روی دستگاه دیگر هم نجاتش نمی‌دهد** (شاهد: S5-۰) | `api/crm.php:782-786, 805-819, 830-845` · `crm/sync.js:2094-2112` (`ptfTombstoneEpoch`/`OutranksRow`), `:2129-2160` |
| ۱۱ | کاربر «مجدداً» ثبت می‌کند ⇒ `cd` تازه ⇒ `entity_upsert` موفق ⇒ در ذخیرۀ خودکار بعدی، همان خواندنِ کهنه ⇒ حذف دوباره ⇒ **چرخه بسته، بدون هیچ اقدامی از سوی کاربر** | بازتولید S3 در §۳ |

**نکتۀ کلیدی معماری:** `ptfEntitySaveCollection` معنایش «این، کلیتِ صحیح و کامل مجموعه است» (authoritative full-set). اما چهار مسیرِ خودکار در همان لحظۀ ثبت، دقیقاً با همین معنا **فهرست ناقص** می‌فرستند. روتر هم که می‌داند کدام `cd`ها تازه‌اند (`newCds` در `:635-639`)، از این دانش برای **مصون‌سازی** استفاده نمی‌کند؛ فقط برای ساختن `idempotencyKey` به کارش می‌آید.

---

## ۲) این باگ قبلاً یک‌بار گزارش و «اصلاح» شده بود — چرا زنده ماند؟

`api/crm.php:753` و `RELEASE-NOTES-v34.37.0.md:15` یک RCA **همان علامت** («مشتری تازه پاک می‌شود») را دارند و در v34.37.0 چهار درمان نشست: گارد تاریخ سنگ‌قبر، محدودکردن `aliases`، و «کد بازنشسته دوباره صادر نشود» (`crm/codegen.js:191-215`). تشخیصِ آن RCA این بود: *«سنگ‌قبر قدیمی بی‌تاریخ بود و مولد کد، cdِ آزادشده را دوباره صادر می‌کرد ⇒ مشتری نو در اولین سینک پاک می‌شد»*. **آن تحلیل، نیمۀ دومِ ماجرا بود: سازندۀ سنگ‌قبر را نشناخت.** سنگ‌قبر را خودِ کلاینت با دیفِ روتر می‌سازد — و سه حفرۀ زیر در درمانِ v34.37.0 باقی مانده:

| # | درمان v34.37.0 | چرا این سناریو را نمی‌گیرد | شاهد |
|---|---|---|---|
| ۱ | گارد تاریخ: سنگ‌قبر فقط رکوردی را می‌کُشد که **قدیمی‌تر از خودش** باشد | رکوردِ این باگ **همیشه قبل از** سنگ‌قبرش متولد شده (اول ساخته می‌شود، بعد در ذخیرۀ خودکاری که فهرست کهنه می‌خواند حذف می‌شود) ⇒ `rowEpoch <= tombEpoch` ⇒ سنگ‌قبر **حق** حذف دارد؛ گارد برای این حالت بی‌اثر است نه ضعیف | `api/crm.php:782-786` · `crm/sync.js:2112-2116` |
| ۲ | سپر حذف انبوه | «تعداد» را می‌شمارد نه «منشأ خواندن» را؛ این الگو در هر ذخیره **دقیقاً یک** رکورد غایب تولید می‌کند ⇒ همیشه زیر سقف. و بدتر: تستر `595` بند **۵.۴** عمداً پین می‌کند که «حذف تک‌رکوردی واقعی کاربر همچنان کار کند» ⇒ **نقطۀ کور، با تست قفل شده است** و اصلاح باید صریحاً همین assertion را بازتعریف کند | `crm/sales-domain-v2.js:648-660` · `_tools/uat/tester595-v34.37.0-tombstone-scope.js:224` |
| ۳ | کد بازنشسته دوباره صادر نشود (`retiredCodesForKey`) | به **بایگانی محلی** نگاه می‌کند؛ همان بایگانی هم از مسیر `ptfEntitySaveCollection('ptf_crm_deleted_archive', rcArch, {reason:'recycle-snapshot'})` نوشته می‌شود — یعنی **همان روترِ دیف‌محور**، بدون سپر (`ptf_crm_deleted_archive` در `DELETE_BURST_GUARDED` نیست). اگر خواندۀ بایگانی کهنه/ردشده باشد، خودِ سطرِ RC حذف می‌شود و حافظۀ «کد بازنشسته» پاک می‌شود ⇒ چرخۀ cd تکراری دوباره باز می‌شود | `crm/sales-domain-v2.js:462-471` · `crm/codegen.js:198-212` |

و سه تغییرِ همان نسخه که **شانس وقوع** را بالا برد (بر پایهٔ کامنت‌های درون‌کد؛ تاریخچۀ git فشرده است و قابل استعلام نیست):

| تغییر | اثر |
|---|---|
| `crm/client-server.js` — راه‌اندازی مجدد فاز B پس از `ptfBootSyncDonePromise` | توالی «خواندن/کش/پروجکشن» در دقایق اول سشن عوض شد ⇒ پنجرۀ «خواندِ کهنه» درازتر شد |
| `crm/sales-domain-v2.js:610-614` — `newCds` با کد یکپارچۀ جهانی، رکورد نو همان‌جا به `arr` اضافه می‌شود و `base` از حافظه برداشته می‌شود (`:624`) | مرزِ «رکورد نو فقط در حافظه است، نه در خواندِ دیسک» **پهن‌تر** شد — یعنی دقیقاً همان نااهنگیِ `base`↔`nextArr` که ماشین حذف است |
| `MAX_OPS=40` + تنزل به legacy در حالت مسدود | اگر دیف بزرگ شود، به `setData` تنزل می‌کند که خودش روی دیسکِ خراب بی‌صدا شکست می‌خورد ⇒ نشانه‌ای نمی‌ماند |

**ردّ فرضیه‌هایی که تست کردم و غلط بودند** (تا کارفرما دوباره بررسی نکند):
- «سنگ‌قبرِ `entity_delete` سمت سرور تاریخ ISO ندارد ⇒ رکورد نو بی‌دفاع حذف می‌شود» — **غلط**: `api/sales-domain.php:2600` از `sd_now()` استفاده می‌کند و `sd_now()` (`:364`) دقیقاً `gmdate('Y-m-d\TH:i:s\Z')` است.
- «فیلتر «مشتریان من» (`crm/my-customers-filter.js`) رکورد را پنهان می‌کند» — **نیست**؛ این سناریو فقط برای نقش‌های غیرارشد ممکن است، و آن‌وقت «ثبت مجدد» باید با «جلوگیری از ثبت تکراری» رد می‌شد (`crm/dedup.js` → `ptfDupBlock` روی `getData` کل مجموعه کار می‌کند، نه روی دید فیلترشده). **اینکه کارفرما توانسته با همان نام دوباره ثبت کند، خودش دلیلِ «غیوب واقعی رکورد» است.**
- «`maxFields: 120` سرور رکورد را کوتاه/رد می‌کند» — **غلط**؛ در `api/sales-domain.php` جایی مصرف نمی‌شود (`grep -n maxFields` فقط تعریف را نشان می‌دهد).
- «سنگ‌قبرِ غیرپرجویی که روتر می‌سازد بی‌اثر است و یک `data_push` از دستگاهِ دارای کپی، رکورد را برمی‌گرداند» — **غلط**؛ `entity_delete` در سرور **همیشه** `kind:'archive_purge'` + `identities` می‌نویسد (`api/sales-domain.php:2600`) و همان در `api/crm.php:813-817` و `crm/sync.js:2141-2147` اعمال می‌شود (شاهد: S5-۰).
- «`ptfEntityUpsert` ناموفق بوده» — **غلط**؛ پیام «ثبت روی سرور انجام شد» در `:3688` هم صادر می‌شود اگر… (بله، این پیام **بدون بررسی نتیجه** چاپ می‌شود — پس پیام، اثباتی نیست؛ اما در بازتولید، `entity_upsert` واقعاً ack می‌شود و حذف یک گام بعد اتفاق می‌افتد).

---

## ۳) بازتولید قطعی (هارنس، کد واقعی مخزن)

`/home/user/cust-eval/repro.js` (+ `slices/*.js` = برش‌های متنی از `saveCust2`، `ptfAllocCustCode`، `ptfEntitySaveCollection`، `hookCust`، `ptfApplyDeletionTombstones`).
فقط سه چیز که در مرورگر/سرور است شبیه‌سازی شده و همه با ارجاع به خط مرجع: لایۀ فاز B (`client-server.js:1112-1160`)، `entity_upsert/entity_delete` (`sales-domain.php:2550-2604`)، و `data_pull`.

```
═══ خلاصهٔ بازتولید: PASS 11 / ✘ 0 ═══
S0 کنترل — مسیر سالم: ثبت ⇒ یک entity_upsert ⇒ رکورد می‌ماند                     ✅
S1 نوشتن محلی ناموفق (کووتا) ⇒ پس از ثبت:
     • entity_upsert cd=CUST-1002 «شرکت آریا کنترل صنعت» [expectCreate]
     • entity_delete cd=CUST-1002  reason=phonefmt          ← حذف به‌دست هوک!
   سنگ‌قبر: archive_purge/CUST-1002 (reason=phonefmt) — روی سرور ❌، پس از pull ❌  ✅
S2 سه مشتری پشت سر هم + یک pull کهنۀ میانی (بدون هیچ خطای نوشتن):
   هر ثبت ⇒ یک حذف از قبلی‌ها؛ در پایان هیچ‌یک از سه مشتری در فهرست کاربر نیست    ✅
S3 «مجدداً ثبت کردم»: CUST-1002 ساخته شد و حذف شد؛ CUST-1003 هم ساخته شد و حذف شد ✅
S4-الف چهار ثبت با نوشتن خراب ⇒ ۴ حذف، هر بار یکی ⇒ سپر «>۳» هرگز فعال نمی‌شود    ✅
S4-ب همان روتر با ۴ رکورد غایب در یک ذخیره ⇒ «⛔ حذف 4 رکورد … مسدود شد»          ✅
S5-۰ کپی زندهٔ دستگاه دیگر هم سرکوب می‌شود (archive_purge دوطرفه)              ✅
S5-الف اما entity_restore سنگ‌قبر را خنثی می‌کند ⇒ احیا مؤثر است                 ✅
```

**S5 هم مهم است:** کپیِ زندۀ دستگاه دیگر هم فایده ندارد (سنگ‌قبر دوطرفه اعمال می‌شود)، ولی احیا از سطل بازیافت سنگ‌قبر را خنثی می‌کند ⇒ داده از دست رفته **قابل برگشت** است، مشروط بر این‌که کلاینت پچ شده باشد.

**S2 بالاترین ارزش تشخیصی را دارد.** در آن **هیچ** خطای نوشتن، کووتا، یا وضعیت خاص دستگاهی نیست — فقط یک `pull` که بین دو ذخیره می‌رسد. یعنی باگ روی هر دستگاهی، بدون نیاز به حافظۀ پُر، فعال است؛ کووتا فقط آن را **قطعی و همیشگی** می‌کند.

---

## ۴) تأیید میدانی روی دادهٔ واقعی (پیش از نوشتن پچ — ۳ بررسی، هر کدام ۱ دقیقه)

1. **اثر انگشتِ `reason`** — در «گزارش‌ها ← حذف‌های ردیابی‌شده» (`crm/reports.js:189-206` که ستون `reason` را رندر می‌کند) یا مستقیماً در `ptf_crm_deleted_archive` جست‌وجو کنید: سطرهای `kind:'archive_purge'` + `collection:'ptf_crm_customers'` که `reason`شان یکی از این‌هاست ⇒ **تأیید قطعی همین ریشه**: `phonefmt` · `offer-cust` · `rfq-cust-heal` · `ai-bizcard` · `ai-buyer` · `site-approve` · `migrate`. این رشته‌ها هرگز از حذف دستی تولید نمی‌شوند (حذف دستی، متن دلیلِ انتخابی کاربر را در `reason` می‌گذارد، `crm/guards.js:27+`). **همۀ `entity_delete`ها `kind:'archive_purge'` می‌نویسند، پس `kind` تشخیص‌دهنده نیست — تنها `reason` تشخیص می‌دهد.** (نکتۀ UI: `crm/reports.js:212` عمداً اجازه نمی‌دهد این سطرها دستی پاک شوند، پس در فهرست می‌مانند و قابل شمارش‌اند.)
2. **هم‌زمانی** — `deletedAt` همان سطرهای ۱ باید در فاصلۀ چند ثانیه تا چند دقیقه با `createdAtISO` رکوردِ حذف‌شده باشد (نه در یک روز دیگر).
3. **سلامت ذخیرۀ دستگاه** — «تنظیمات ← وضعیت دستگاه/پاک‌سازی داده‌ها»: اگر `ptfStorageSafeSetItem` خطای کووتا گزارش می‌کند یا سشن‌توکن/`ptf_sid` تهی است، دستگاه در حالت S1 است (نوشتن محلی رد می‌شود ⇒ هر ثبت، قبلی را می‌کُشد).

---

## ۵) اصلاح پیشنهادی (لایه‌ای — هنوز اجرا نشده)

| # | اصلاح | چرا | ریسک |
|---|---|---|---|
| **A (اصلی)** | **رکوردهای تازه/در‌راه‌ثبت را از حذف مصون دارید**: در روتر، `dels` را فیلتر کنید به `cd`هایی که (الف) در `newCds` بوده‌اند یا (ب) فقط در `base` حافظه‌ای‌اند و در هیچ «خواندنگاهِ» دیسک نبوده‌اند. ساده‌ترین شکل قابل‌اتکا: در `phonefmt`/`heal`/هم‌تایان، **به‌جای ذخیرۀ کل مجموعه، فقط همان یک رکورد را با `ptfEntityUpsert` بفرستید** (مسیر تک‌رکوردی که از ابتدا طراحی شده بود و در `:689` برای `records-without-cd` هم استفاده می‌شود) | حذف از «نتیجهٔ خواندن کهنه» ساخته نمی‌شود؛ دیف روی آرایۀ که خودِ فراخوان نساخته، بی‌معناست | پایین — رفتارسازیِ یک کلاس فراخواننده؛ **اما** باید با E باهم بیایند، وگرنه هوک همچنان فهرست کهنه را می‌فرستد و فقط دیفِ بزرگ‌تری می‌سازد |
| **B** | **منبعِ دیف را هم‌خانواده کنید**: `base` را از همان `getData()` بخوان (نه `_ptfEntityLastKnown`) و `_ptfEntityLastKnown` را **فقط برای ردیابی تغییر** نگه دارید؛ یا برعکس: `nextArr` را در هوک‌ها از `_ptfEntityLastKnown` بسازید. امروز ترکیب «base=حافظه، next=دیسک/کش» دقیقاً ماشینِ ساختِ حذف است | ناهم‌تجانیِ (asymmetry) علّت مستقیم است | متوسط — باید مطمئن شوید «ویرایش بیرون‌از-برنامه» (pull) به‌عنوان حذفِ انبوه تفسیر نشود؛ به همین دلیل گارد B باید با A همراه شود |
| **C** | **سپر را از «تعداد» به «منشأ» ببرید**: اگر `reason` از فهرست «ذخیره‌های خودکار/پس‌از‌رندر» است (`phonefmt`, `*-heal`, `*-clean`, `ai-*`, `site-*`, `migrate` ⇒ یک `AUTO_REASONS` ست) **هرگز `entity_delete` صادر نکن**؛ و در حالت `blocked` هم `console.warn` + `audit` بزن (امروز فقط toast است و اگر toast خورده نشود، بی‌صداست) | تشخیصِ «کسی عمدأ حذف کرده» از «کد این‌طور فرض کرد» | پایین |
| **D** | **شکست نوشتن محلی را علنی کنید**: در `ptfBApplyServerProjection`، وقتی `stored===false`، به‌جای `delete cache[k]`، کش را روی **مقدار تازه** بنشان و یک رویداد `ptf-storage-quota` صادر کن؛ در UI، هشدار «این دستگاه نمی‌تواند بنویسد» (`settings.js:1132` زیرساختش را دارد) | امروز «دیسک کهنه + کش خالی» = فاکتور تشدیدکنندۀ حذف، و کاربر هیچ نشانه‌ای نمی‌بیند | متوسط (لایۀ مشترک) |
| **E** | **تک‌رکوردی کردن هوک‌ها**: `crm/phonefmt.js:89-103` (و `hookSup` در `:105-139`) و `crm/offers.js:3846-3851` — به‌جای «خواندن `getData` و ذخیرۀ دوبارهٔ کل مجموعه»، فقط رکوردِ همان `cd` را نرمال و `ptfEntityUpsert` کن؛ اگر `cd` داده نشده، **هیچ کاری نکن** (نه `items[0]` — که در S1 همان رکوردِ تازه است و در حالت عادی اولین رکوردِ فهرست، یعنی بی‌ربط‌ترین رکورد ممکن) | هم حذفِ ریسک، هم رفع یک باگ مستقل: `items[0]` یعنی «نرمال‌سازی رکوردِ اشتباه» | پایین |
| **F** | **تست قفل‌شونده**: یک ورودی در `_tools/uat/run-ci-gate.js` (به سبک `tester597/598`) که ثابت کند: «ثبت مشتری + هوک تلفن + heal، روی دیسکِ کهنه و روی کشِ کهنه ⇒ `entity_delete` صادر **نشود** و رکورد در فهرست بماند» و «`reason` خودکار ⇒ هرگز حذف صادر نشود» | بدون این، هر بازگشتی دوباره بی‌صدا می‌آید. **نکته:** با این پچ، assertion بند **۵.۴** در `tester595` («حذف تک‌رکوردی کاربر همچنان کار می‌کند») باید بازتعریف شود — تفکیک «حذف با دلیلِ انسانی» از «حذف با `reason` خودکار»؛ این تنها راهی است که قفلِ تستیِ نقطۀ کور باز بماند و رگرسیون نیاید |

**بازیابی دادهٔ از‌دست‌رفته (خبر خوب):** سطر حذف‌شده در `snapshot` همان سطر بایگانی است و `entity_restore` (`api/sales-domain.php:2619-2671`) هم رکورد را برمی‌گرداند و هم سنگ‌قبر را با `kind:'restored:…'` + `restoredAt` **خنثی** می‌کند — و چون هر دو طرف فقط `kind === 'archive_purge'` را جدی می‌گیرند، سرکوب از همان لحظه برداشته می‌شود (شاهد S5-الف). **ترتیب الزامی:** ① کلاینت پچ شود (A+E+C) ② سپس بازیافت‌ها انجام شود — وگرنه اولین ذخیرۀ خودکار، دوباره حذفشان می‌کند ③ برای تعداد بالا، به‌جای کلیک‌تک‌مصرفِ سطل، یک اسکریپت یک‌بار‌مصرف روی همان بازۀ زمانی (که همان `entity_restore` را حلقه‌وار صدا می‌زند). **هشدار:** «push از دستگاهی که کپی دارد» جواب نمی‌دهد (سنگ‌قبر روی خواند اعمال می‌شود، نه نوشتن) و `data_push` کل مجموعۀ آن دستگاه را می‌نویسد ⇒ ریسک عقب‌گرد تغییر دیگران.

---

## ۶) دامنهٔ خطر (همین ریشه، کلکشن‌های دیگر)

- `crm/phonefmt.js:105-139` → **تامین‌کنندگان** (`hookSup`)، `:140-156` → کتابچۀ تلفن (`hookSmsBook`)، `:157+` → `migrateOnce` (مهاجرت یک‌بار‌مصرف که چند مجموعه را پشت هم ذخیرۀ مجدد می‌کند) — همان الگوی «خواندنِ `getData` و ذخیرۀ کل مجموعه».
- `crm/offers.js:3846-3851` → «درمان از استعلامات» روی مشتریان، از مسیر **رندر** (`renderCustomers2` در `:3858`) — یعنی فقط با باز کردن/تازه‌سازی فهرست مشتریان هم فعال می‌شود.
- `crm/leads.js:379/418/443/490`، `crm/ai-workbench.js:305-307` و مسیرهای `ai-bizcard`/`ai-buyer`/`ai-letterhead` → ذخیرۀ «کل آرایه‌ای که خودشان ساخته‌اند» با `reason` خودکار؛ هر جا آن آرایه از یک خواندِ ناقص/فیلترشده آمده باشد، همان ماشین حذف است (موارد `crm/bridge.js:1101-1103`، `crm/backup.js:793`، `crm/ai-workbench.js:720-733` **عمدی** هستند — فیلتر برای حذف ساخته شده‌اند و بهتر است از روتر خارج شوند، چون روی `pull`ِ کهنه می‌توانند حذف‌های دیگری را هم «تأیید» کنند).
- `crm/index.html:2464` نسخهٔ **میراثیِ** `getData` است (خواندن خامِ `localStorage`) که `crm/client-server.js:1105-1112` بعداً آن را wrap و جایگزین می‌کند؛ ترتیب `defer` (offers.js `:4428` → phonefmt.js `:4482` → client-server.js `:4510` → sales-domain-v2.js `:4519`) باعث می‌شود همه در زمان اجرا نسخۀ **بسته‌بندی‌شده** را بگیرند — پس «کش ۳۰ ثانیه‌ای» در همۀ این مسیرها حاکم است. نکتۀ ایمنی: هیچ `localStorage.getItem('ptf_crm_customers')` مستقیمی در `crm/*.js` وجود ندارد (بازبینی شد) ⇒ منبعِ کهنه فقط همین `getData` است.

---

## ۷-ب) علامت دوم (افزوده شد): «پیشنهادها و درخواست‌ها اصلاً نمایش داده نمی‌شوند»
منبعِ «خواندِ کهنه» در گام ۴ و ۷ بالا **قطعی** شد: نقص آب‌رسانی آینۀ IDB در بوت (`crm/client-server.js:160-164`). تحلیل کامل، راه‌حل بازکردن قفل روی دستگاه و یک تشخیص ۲۰ ثانیه‌ای در **`ARENA-CRM-OFFERS-RFQ-EMPTY-RCA-2026-09-05.md`** — همراه با یک علت مستقلِ UI (فیلتر مشتریِ مُرده در `crm/offers.js:454`/`:378`).

---

## ۷) بازبینی‌شدگان (برای اینکه کسی دوباره این مسیرها را نرود)

- گارد تاریخ **سمت سرور** و **سمت کلاینت** قرینه و سالم‌اند (`api/crm.php:765-855` ↔ `crm/sync.js:2037-2112`)؛ علت، بی‌تاریخیِ سنگ‌قبر نیست.
- «تعارض شناسه، سرکوب را ناقص می‌کند» — **نیست**: ترتیب شناسه‌سازی در دو طرف قرینه است (`api/crm.php:790-794` `sync_record_id_for_key` ↔ `crm/sync.js:2067-2071` `ptfRecordIdentityForKey`) و سطر مشتری `_id` ندارد ⇒ `cd` مبنای هر دو.
- `ptfDupBlock`/`ptfCheckDup` از ثبت تکراری جلوگیری می‌کند ⇒ اگر رکورد فقط «پنهان» بود، ثبت مجدد ممکن نبود.
- `_ptfEntityLastKnown` و `getData` کپی عمیق می‌دهند (`:749/:763` با `JSON.parse(JSON.stringify())`؛ `client-server.js` با `JSON.parse`) ⇒ دیف به‌دلیل «رفرنس مشترک» تهی **نمی‌شود** (این فرضیۀ اولیهٔ من بود و با خواندن رد شد؛ علت واقعی، **ناهم‌زمانی منبع** است نه اشتراک رفرنس).
- `crm/listclean.js` خودکار نیست (`btnListCleanApply` + `CLEAN_ROLES`) — در این پرونده دخیل نیست، ولی `reason:'list-clean'` هم به جمع «دلیلی‌های خودکارِ خطرناک» اضافه می‌شود.
