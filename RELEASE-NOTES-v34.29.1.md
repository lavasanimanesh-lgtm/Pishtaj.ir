# RELEASE NOTES — v34.29.1 (آزمون اتصال GSC — علتِ دقیقِ «ثبت نقشه ناموفق»)

**تاریخ:** ۲۰۲۶-۰۹-۰۲ · **کمیت:** tester576 (۱۱ سنجه) · **گیت:** 192 PASS / 0 FAIL

## گزارش کاربر

«به‌روزرسانی سایت‌مپ با خطا مواجه می‌شود: property_not_found — سرویس‌اکانت به هیچ پراپرتیِ pishtaj.ir دسترسی ندارد؛ پراپرتی‌های قابل‌دسترسی: (خالی) … در صورتی که من دسترسی Full داده‌ام.»

## ① معنی دقیق این خطا

احراز هویت سرویس‌اکانت **سالما**ست (توکن گرفته شده — وگرنه خطا 401/token_failed بود). گوگل صریح می‌گوید: این اکانت به **هیچ پراپرتی‌ای** دسترسی ندارد. یعنی افزودنِ «Full» در جایِ درستِ گوگل اعمال نشده است. چهار علت رایج:

1. **افزودن در Google Cloud/IAM** (نقش Owner/Editor در پروژه) به‌جای **خودِ Search Console** ← Settings ← Users and permissions ← Add user — این دو جای متفاوت‌اند و IAM معادلِ آن نیست.
2. **ایمیل ناقص/غلط تایپ شده** — باید عیناً `client_email` از JSON باشد (پایانش `…@…​.iam.gserviceaccount.com`، بدون فاصله).
3. **Full روی پراپرتیِ اشتباه** داده شده (مثلاً staging یا پراپرتیِ اکانت دوم) — باید روی خودِ پراپرتی `pishtaj.ir` باشد.
4. **افزودن فقط از مسیر Owners/Verification** (مالک تفویضی) — دام شناخته‌شده: فهرست سایت‌ها باز هم خالی برمی‌گردد؛ باید یک‌بار از مسیر Users and permissions به‌عنوان **User با Full** هم اضافه شود.

نکته: برای Search Console API و ثبت نقشه، **Full کافی است** (Owner لازم نیست). اعمال تغییرات معمولاً تا ۱–۲ دقیقه.

## ② چیزی که ساخته شد — 🧪 آزمون اتصال GSC

مسیر: CRM ← مدیریت سایت ← تب «سئوی صفحات» ← دکمهٔ **🧪 آزمون اتصال GSC** (همین دکمه در پیام خطای «📤 سایت‌مپ + سرچ کنسول» هم پیشنهاد می‌شود). مستقیم از گوگل می‌پرسد و نشان می‌دهد:

- **ایمیل سرویس‌اکانت** با دکمهٔ 📋 کپی — عیناً همان که باید در سرچ کنسول اضافه شود؛
- **فهرست زندهٔ پراپرتی‌های قابل‌دسترسی** با سطح هرکدام (Full/Owner سبز، Restricted کهربایی)؛
- **تشخیص علت**: اتصال سالم ✅ / بدون دسترسی ⛔ (با ۶ گام رفع) / سطح کم ⚠️ / کلید خراب / بی‌تنظیم — با راه‌حل گام‌به‌گام فارسی.

سمت سرور: اکشن `selftest` (api/gsc.php) + بازسازی `gsc_resolve_site` به `gsc_pick_site` بدونِ توقف؛ پیام خطای `property_not_found` حالا هر ۴ علت را صریح فهرست می‌کند. `gsc_token`/`gsc_api` حالت silent گرفتند تا خودتشخیص هیچ‌وقت روی خطا نیفتد.

## ③ مستندات

- `GSC-PANEL-SETUP-FA.md` — بخش «۴ علت رایجِ Full دادم ولی باز خالی است» (جدول علت/راه‌حل).
- `api/gsc-config.sample.php` — هشدارهای مکمل در گام ۵ (IAM ≠ سرچ کنسول، کپیِ دقیق ایمیل، پراپرتی درست، Owner-vs-User).

## ④ بعد از دیپلوی

۱) فایل‌های v34.29.1 را آپلود کنید (`api/gsc.php`، `crm/cms.js`، `crm/index.html` و بقیهٔ نقاط نسخه). ۲) در CRM اگر بنر «نسخهٔ جدید» آمد، بارگذاری مجدد کنید. ۳) تب سئو ← «🧪 آزمون اتصال GSC» — اگر ⛔ بود: ایمیل را با 📋 کپی کنید و در **Search Console ← Settings ← Users and permissions ← Add user** (روی پراپرتی pishtaj.ir، سطح Full) بچسبانید؛ ۱–۲ دقیله بعد دوباره آزمون را بزنید تا ✅ سبز شود. ۴) حالا «📤 سایت‌مپ + سرچ کنسول» باید ثبت شود.
