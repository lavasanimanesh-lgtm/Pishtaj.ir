# Release Notes v34.7.20 — فاز B نقشهٔ فازبندی: بازگرداندن قابلیت‌های مسدود

تاریخ: ۱۴۰۵/۰۵/۲۶ (2026-08-17)

## زمینه

طبق `PLAN-REMAINING-FIXES-PHASED-2026-08-17.md`، فاز B سه آیتم UI-01/UI-02/UI-03 را می‌بندد: یک قابلیتِ کاملاً مسدود، یک ذخیره‌سازی خاموش‌ناقص، و یک بدهی نگهداشت. هیچ منطق مالی‌ای تغییر نکرده است.

## UI-01 🔴 دیالوگ «صورتحساب غیررسمی» از کشوی پرونده باز نمی‌شد

**ریشه:** پروندهٔ فروش دو شناسه دارد — `_id` سروری و `cd` محلی. کشوی پرونده `r.cd` می‌فرستاد، اما گیرنده `String(x._id || x.cd) === dealCd` را می‌سنجید؛ برای هر پرونده‌ای که شناسهٔ سروری داشت (عملاً همهٔ پرونده‌های v35) `_id ≠ cd` بود و تطبیق همیشه شکست می‌خورد → پیام «⛔ پرونده فروش یافت نشد».

**اصلاح:** تطبیق متقارن — هر دو شناسه مستقل بررسی می‌شوند (`_id` یا `cd`) و فراخوان کشوی پرونده هم `r._id || r.cd` می‌فرستد. ورودی خالی هیچ تطبیق کاذبی نمی‌سازد. گاردهای نقش (ارشد/حسابدار) و مرحلهٔ ۷ (پس از تحویل) دست‌نخورده‌اند.

## UI-02 🟠 «صدور مجدد» مبلغ جدید را ذخیره نمی‌کرد

**ریشه:** شاخهٔ بازنویسی فقط `caseId`/`customerId` را می‌نوشت (و دو بار `setData` می‌زد)؛ مبلغ، تخفیف، snapshot اقلام و نرخ ارز دست‌نخورده می‌ماند اما پیام موفقیت نمایش داده می‌شد.

**اصلاح:** محتوای مالی واقعاً به‌روز می‌شود: `amount`، `base`، `discount/discountLabel/discountInput`، `linesSnapshot`، `offerCurrency/offerFxBasis/offerFxRateRef`، `bankAccount`، `sourceOfferNo` و مهر `reissuedAt/reissuedBy`.

**عمداً دست‌نخورده:**
- `cd` و `no` — هویت سند؛
- `invDate`/`t` — تغییرشان سال مالی و ترتیب FIFO را جابه‌جا می‌کرد؛
- `payments` — به‌جز ردیف پیش‌پرداخت که با مبلغ جدید **هم‌راستا** می‌شود (نه ردیف دوم).

**هم‌راستایی سروری:** همان فرمان `register_unofficial_invoice` با کلید idempotency اختصاصی (`UNOFFICIAL-REISSUE|cd|amount`) صدا زده می‌شود و سرور رکورد را با همان `cd` به‌روز می‌کند؛ در شکست، نسخهٔ قبلی از snapshot بازگردانده می‌شود و به کاربر اعلام می‌گردد. کش `PTF.ar` و رکورد audit هم به‌روز می‌شوند.

## UI-03 🟡 تعریف‌های تکراری در `crm/offers.js`

`offerPostAwardLocked` و `ptfGoSalesFileForOffer` هم در سطح ماژول و هم داخل `renderOffers` تعریف شده بودند و در هر رندر، نسخهٔ سراسری دوباره ست می‌شد. بدنه‌ها یکسان بودند (پس رفتار امروز درست بود)، ولی هر اصلاح آیندهٔ فقط‌یکی‌از‌آن‌ها به‌صورت خاموش بی‌اثر می‌شد. نسخهٔ داخلی حذف و تعریف واحد سطح ماژول باقی ماند؛ قاعدهٔ قفل پس از برد بدون کوچک‌ترین تغییر است.

## چرا این تغییر «بدون آسیب» است

- UI-01 فقط دامنهٔ تطبیق را **گسترده‌تر** می‌کند (هیچ پرونده‌ای که قبلاً پیدا می‌شد از دست نمی‌رود).
- UI-02 هویت سند، تاریخ صدور و وصولی‌های واقعی را دست نمی‌زند و در شکست سرور کاملاً برمی‌گردد.
- UI-03 حذف کد تکراری با بدنهٔ یکسان است؛ رفتار قفل با تست رفتاری (CO برنده/TO/ارسال‌شده) تأیید شد.

## تست رگرسیون

- `_tools/uat/tester423-v34.7.20-unofficial-issue-path.js`: **37 PASS / 0 FAIL** — شامل اجرای واقعی بلوک تطبیق پرونده روی چهار سناریو (v35 با `cd`، v35 با `_id`، legacy، شناسهٔ ناموجود/خالی)، اجرای واقعی ناحیهٔ انتساب‌های بازنویسی روی یک رکورد نمونه، و تست رفتاری قفل برد.
- `tester422` (فاز A): ۳۳/۰ — سنجهٔ نسخه از «عدد ثابت» به «هم‌راستایی» تغییر کرد تا در نسخه‌های بعد شکست کاذب ندهد.
- `tester421` ۳۸/۰، `tester420` ۳۰/۰، `tester1-flow` ۱۷/۰، `tester188` ۲۳/۰، هارنس تسویه ۵ از ۵.
- گیت CI: **۳۸ PASS / ۲ FAIL** — همان دو شکست پیش‌ازموجود (`tester403`, `tester415`).
- سوئیت کامل (۴۰۶ تستر): **۶۲ شکست، صفر شکست جدید** نسبت به baseline فاز A.
- بهداشت نسخه در ۶ نقطه روی `v34.7.20`.

## گام بعد

فاز C: هم‌ترازی چرخهٔ عمر `superseded` در پورسانت/سرمایه در گردش/سال مالی (LC-01) و — پس از تأیید کارفرما — احتساب مرجوعی فروش در چهار موتور مالی (LC-02).
