# Release Notes v34.7.22 — فاز D: گاردهای پیشگیرانه

تاریخ: ۱۴۰۵/۰۵/۲۶ (2026-08-17)

## زمینه

فاز D چهار گاردی است که جلوی «خطای خاموش کاربر» را می‌گیرد: سه مورد از گزارش تلفیقی و بررسی مستقل، و یک مورد بهداشت نسخه. هیچ منطق مالی‌ای بازنویسی نشده و هیچ مسیر موجودی مسدود نشده است.

## AW-01 🟠 متمم با مبلغ صفر یا منفی رد می‌شود

**ریشه:** `sd_num()` علامت منفی را نگه می‌دارد، `sd_offer_total()` جمع امضادار است و فرم پیشنهاد هم محدودیت علامت ندارد. بنابراین یک offer با جمع صفر/منفی می‌توانست به‌طور مکانیکی `contractAmount` پرونده را کم کند — **بدون نوع متمم، بدون دلیل/تأیید، بدون لغو خطوط قبلی و بدون هیچ ردی در گزارش‌ها**. گزارش تلفیقی این را «رفتار مکانیکی ناایمن» نامیده بود.

**اصلاح:** گارد سروری `invalid_amendment_amount` در هر دو نقطه — `mark_amendment` و شاخهٔ اتصال متمم در `win_offer` — **پیش از** افزوده‌شدن به `contractAmount`. قواعد قبلی متمم (تطابق مشتری/ارز، برنده‌بودن پیشنهاد پایه) دست‌نخورده‌اند.

> کاهش قراردادی همچنان باید در فاکتور/اصلاحیه منعکس شود؛ این مسیر عمداً بسته شد تا کاهش «قاچاقی» از راه متمم انجام نشود.

## AW-02 🟠 هشدار پروندهٔ موازی پیش از ساخت پروندهٔ دوم

**ریشه:** سرور فقط پرونده‌ای را «مرتبط» می‌داند که `rootOfferId`/`wonOffer`/`offerNo` آن با همان پیشنهاد بخورد. اگر کاربر یک CO جایگزین برای همان استعلام و همان مشتری بسازد و بدون علامت متمم برنده کند، `win_offer` بی‌صدا **پروندهٔ دوم مستقل** می‌ساخت و از آن پس مطالبات، وصولی و آمار روی دو پرونده پخش می‌شد. دیالوگ اتصال تا امروز فقط برای پیشنهادهای علامت‌خوردهٔ متمم ظاهر می‌شد.

**اصلاح:** پیش از فراخوان `win_offer`، اگر پروندهٔ فعالی با همان `inqNo` + همان مشتری + همان ارز وجود داشته باشد:
- **یک پرونده:** انتخاب صریح بین «اتصال به همان پرونده به‌عنوان متمم» یا «پروندهٔ مستقل» — و گزینهٔ دوم تأیید دوم می‌خواهد.
- **بیش از یک پرونده:** توقف و ارجاع به مسیر «پروندهٔ تکراری» (هیچ حدسی زده نمی‌شود).
- پروندهٔ خودِ همان پیشنهاد، پروندهٔ مشتری/استعلام/ارز دیگر و پروندهٔ ابطال‌شده هشدار نمی‌سازند.

## AW-03 🟠 پیش‌بررسی حذف، وابستگی‌های خارج از دامنه را هم نشان می‌دهد

`admin_delete_plan` فقط فاکتور/رسید/تخصیص/ضمیمه را کشف می‌کرد؛ ادمین بدون اطلاع از چک، خرید واقعی، تعهد خرید، مرجوعی، بارنامه و پروژه تصمیم می‌گرفت و نتیجه می‌توانست رکورد یتیم باشد.

اکنون هفت منبع دیگر اسکن و در `advisoryDependencies` برگردانده می‌شوند و کلاینت آن‌ها را با هشدار صریح «این اقلام با حذف پاک نمی‌شوند و ممکن است یتیم بمانند» نمایش می‌دهد. **رفتار commit تغییر نکرده**: cascade همچنان فقط همان چهار نوع قبلی است.

## OPS-01 🟡 نسخهٔ سرویس

عدد `34.6.0` در سه پاسخ سرور hardcode بود. حالا از ثابت واحد `SD_SERVICE_VERSION` خوانده می‌شود و تستر، هم‌راستایی آن با `PTF_CRM_RELEASE` را می‌سنجد (پس در نسخه‌های بعد نمی‌تواند بی‌صدا عقب بماند).

## تست رگرسیون

- `_tools/uat/tester425-v34.7.22-award-guards.js`: **33 PASS / 0 FAIL** — شامل **اجرای واقعی** منطق تشخیص پروندهٔ موازی روی شش سناریو (هم‌استعلام/مشتری دیگر/استعلام دیگر/ارز متفاوت/پروندهٔ ابطالی/پروندهٔ خود پیشنهاد) و بررسی جایگاه گارد متمم نسبت به محل افزایش `contractAmount`.
- تسترهای فازهای قبل: `tester424` ۳۱/۰، `tester423` ۳۷/۰، `tester422` ۳۳/۰، `tester421` ۳۸/۰، `tester420` ۳۰/۰.
- گیت CI: **۴۰ PASS / ۲ FAIL** — همان دو شکست پیش‌ازموجود (`tester403`, `tester415`).
- سوئیت کامل (۴۰۸ تستر): **۶۲ شکست، صفر شکست جدید** نسبت به baseline فاز C.
- بهداشت نسخه در ۷ نقطه (شامل نسخهٔ سرویس سرور) روی `v34.7.22`.

## گام بعد

فاز E: افزودن endpoint سروری `void_unofficial_invoice` و برداشتن گارد موقت فاز A — که سناریوی «صورتحساب غیررسمی موجود» را از حالت «متوقف» خارج می‌کند.
