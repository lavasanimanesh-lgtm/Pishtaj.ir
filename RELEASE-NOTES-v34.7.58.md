# RELEASE NOTES — v34.7.58 (۱۴۰۵/۰۵/۲۹ — 2026-08-20)

## خروجی چاپ / PDF رسمی بدون واترمارک PREVIEW (بازپیاده‌سازی PR #57 روی main)

### زمینه

تنها بخش باقی‌ماندهٔ PR #57 که هرگز به `main` نرسیده بود: کاربر برای گرفتن PDF قابل
ارسال مجبور بود از مسیر «پیش‌نمایش» عبور کند که واترمارک داخلی **PREVIEW** دارد.
شاخهٔ PR #57 با پیاده‌سازی موازی v34.7.47–49 در main واگرا شده و rebase آن پرهزینه بود؛
این نسخه همان فیچر را تمیز روی main فعلی بازپیاده‌سازی می‌کند.

### سه مسیر خروجی رسمی

| مسیر | تابع | رفتار |
|---|---|---|
| فرم باز پیشنهاد (مالی/فنی-مالی/رویژن) | `offerPrintCurrent()` — دکمهٔ «🖨 چاپ / PDF رسمی» در فوتر | سند از وضعیت فعلی فرم ساخته و با `isPreview=false` چاپ می‌شود — **بدون ذخیره، بدون رویژن، بدون واترمارک** |
| سند ذخیره‌شده در «اسناد پرونده» | `offerFormalPrint(no)` — دکمهٔ «🖨 PDF / چاپ» کنار هر پیشنهاد | آخرین نسخهٔ ذخیره‌شده با قالب انتخابی کاربر، بدون واترمارک |
| سند قطعی برد (snapshot) | `offerFormalPrintObj(snap)` | snapshot کپی می‌شود و **هرگز mutate نمی‌شود**؛ خروجی رسمی |

### جزئیات فنی

- `offMaterializeCurrentDocument()` از `offerPreview` استخراج شد — منبع واحد ساخت سند
  از فرم؛ حالا `validUntil` و `printAs` فرم را هم می‌خواند (پیش‌نمایش و خروجی رسمی
  هر دو دقیقاً وضعیت جاری فرم را منعکس می‌کنند). دو متغیر مردهٔ قدیمی حذف شد.
- **پیش‌نمایش داخلی عمداً دست‌نخورده ماند**: `offerQuickPreview` و دکمهٔ «👁️ پیش‌نمایش»
  همچنان واترمارک PREVIEW دارند — تمایز سند داخلی از سند قابل ارسال حفظ شد؛ در ردیف
  اسناد پرونده برچسب «👁 پیش‌نمایش» گرفت تا با «🖨 PDF / چاپ» اشتباه نشود.
- فوتر فرم: انصراف ← پیش‌نمایش ← **چاپ/PDF رسمی** ← ذخیره؛ قرارداد case-revision
  (دکمهٔ اول = انصراف، `#offSaveBtn` در گروه دکمه‌ها) حفظ شد — در فرم رویژن هم در دسترس است.

### تست

- تستر جدید `tester461-v34.7.58-offer-formal-output.js` (در گیت) — شامل sandbox واقعی:
  هر دو مسیر formal با `isPreview=false`، عدم mutate شدن snapshot، حل شمارهٔ سند.
- **گیت CI: ۷۷ PASS / ۰ FAIL** · نگهبان معماری: PASS.

### تعیین تکلیف PR #57

با این نسخه، همهٔ محتوای PR #57 یا قبلاً در main است (بارگذاری چند درخواست — v34.7.47/48)
یا با این نسخه رسید (خروجی رسمی). **PR #57 قابل بستن است** — تصمیم با کاربر.

### UAT پیشنهادی

1. فرم پیشنهاد را با چند قلم باز کنید → «🖨 چاپ / PDF رسمی» → سند نباید واترمارک PREVIEW داشته باشد و نباید ذخیره/رویژن بسازد.
2. «👁️ پیش‌نمایش» → همچنان واترمارک PREVIEW دارد.
3. پرونده فروش → اسناد → «🖨 PDF / چاپ» کنار پیشنهاد → خروجی رسمی؛ «👁 پیش‌نمایش» → با واترمارک.
4. سند قطعی برد → چاپ → بدون واترمارک؛ سپس دوباره باز کنید (snapshot نباید تغییر کرده باشد).
