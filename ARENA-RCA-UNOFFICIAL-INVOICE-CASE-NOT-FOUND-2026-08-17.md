# گزارش ریشه‌یابی باگ — «صدور فاکتور غیررسمی» در پرونده‌های فروش

- **تاریخ:** ۱۴۰۵/۰۵/۲۶ (۲۰۲۶-۰۸-۱۷)
- **برنچ:** `arena/01a00ec2-pishtaj-ir`
- **نسخهٔ کد بررسی‌شده:** `v34.7.16` (کامیت پایه `e178d90`، مرج PR #47)
- **وضعیت:** **فقط ارزیابی — هیچ خطی از کد تغییر نکرده است.**
- **علامت گزارش‌شده توسط کارفرما:** در «پرونده‌های فروش» با کلیک روی دکمهٔ **🧾 صورتحساب غیررسمی**، پیام خطای «پروندهٔ فروش …» ظاهر می‌شود و دیالوگ صدور باز نمی‌شود.

---

## ۰. خلاصهٔ مدیریتی (TL;DR)

باگ **یک اشکال یک‌خطی در تطبیق شناسهٔ پرونده** است، نه نقص در منطق مالی یا دسترسی.

| مورد | مقدار |
|---|---|
| **ریشهٔ اصلی** | `crm/unofficial-invoice.js` خط **۱۲۸۶** |
| **کد معیوب** | `String(x._id \|\| x.cd \|\| '') === String(dealCd \|\| '')` |
| **فراخوان** | `crm/salesfiles.js` خط **۱۱۸۶** — فقط `r.cd` را می‌فرستد |
| **نوع نقص** | عدم تقارن matcher (تطبیق «یا/الویت‌دار» به‌جای تطبیق «هر دو شناسه») |
| **شدت** | **P1 — قابلیت کاملاً از کار افتاده** برای هر پرونده‌ای که شناسهٔ سروری (`_id`) گرفته باشد |
| **دامنهٔ اثر** | در عمل **۱۰۰٪ پرونده‌های فعال** پس از فعال‌شدن معماری Sales-Domain v35 |
| **پیام دیده‌شده** | `⛔ پرونده فروش یافت نشد` (متن دقیق داخل کد) |
| **اثر داده‌ای** | ❌ هیچ — هیچ رکوردی نوشته یا خراب نمی‌شود؛ خطا قبل از هر write رخ می‌دهد |

**جملهٔ ریشه‌ای:** پرونده در سیستم **دو شناسه** دارد — `_id` (شناسهٔ اصیل سرور) و `cd` (کد قدیمی محلی). دکمهٔ پرونده `cd` را می‌فرستد، ولی تابع دریافت‌کننده **اول `_id` را می‌خواند و همان را با ورودی مقایسه می‌کند**؛ چون `_id ≠ cd` است، هرگز تطبیقی پیدا نمی‌شود.

---

## ۱. مسیر دقیق اجرا (Call Path)

```
پنل «پرونده‌ها» (goPanel('deals'))
  └─ renderDeals()                       crm/salesfiles.js:1518
      └─ sfDrawerHtml(r, d, hasInv)      crm/salesfiles.js:945
          └─ postAction('unofficial-invoice', …)      crm/salesfiles.js:1183-1187
              onclick = sfUnofficialInvoiceNew('<r.cd>')      ← ❶ فقط cd ارسال می‌شود
                └─ window.sfUnofficialInvoiceNew(dealCd)      crm/salesfiles.js:136
                    ├─ گارد ۱: بارگذاری ماژول            (خط ۱۳۷)
                    ├─ گارد ۲: نقش (senior یا accountant) (خط ۱۴۴)
                    └─ window.unofficialInvoiceBuilderOpen(dealCd)   crm/salesfiles.js:148
                        └─ crm/unofficial-invoice.js:1277
                            ├─ گارد ۳: نقش (تکرار مجدد)        (خط ۱۲۸۱)
                            ├─ گارد ۴: یافتن پرونده             (خط ۱۲۸۵-۱۲۹۱)  ← ❷ 💥 نقطهٔ شکست
                            ├─ گارد ۵: مرحلهٔ پرونده ≥ ۷        (خط ۱۲۹۲-۱۲۹۶)
                            └─ گارد ۶: وجود پیشنهاد دارای اقلام (خط ۱۲۹۷-۱۳۰۱)
```

---

## ۲. ریشهٔ باگ — تحلیل خط‌به‌خط

### ۲.۱ سمت فرستنده — `crm/salesfiles.js:1183-1187`

```js
postActions += postAction(
  'unofficial-invoice', '🧾', 'صورتحساب غیررسمی',
  'صدور صورتحساب پرداخت غیررسمی برای هر پیشنهاد متصل (تک یا تجمیعی). پیش‌فرض قیمت = CO.',
  'sfUnofficialInvoiceNew(\'' + ptfOnClickArg(r.cd) + '\')', { meta: 'پرونده' }
);                                        // ← فقط r.cd
```

**مقایسه با دکمهٔ سالمِ بغل‌دستی (خط ۱۱۷۹):**

```js
'ptfCaseFinanceOpen(\'' + ptfOnClickArg(r._id || r.cd) + '\')'   // ← r._id || r.cd  ✅
```

همین تفاوت در همان تابع و همان بلوک، خودِ نقص را ثابت می‌کند: قرارداد پروژه برای شناسهٔ پرونده `r._id || r.cd` است و دکمهٔ جدید (افزوده‌شده در PR #46، برچسب `ARENA-2026-08-17 / گام ۲`) از آن پیروی نکرده است. خط ۳۷۱ و ۳۷۲ همان فایل (`sfStageGuidance`) هم `r._id || r.cd` را رعایت کرده‌اند.

### ۲.۲ سمت گیرنده — `crm/unofficial-invoice.js:1285-1291`

```js
var _deal = (getData('ptf_crm_deals') || []).filter(function (x) {
  return x && String(x._id || x.cd || '') === String(dealCd || '');   // ← ❌
})[0];
if (!_deal) {
  if (typeof alert === 'function') alert('⛔ پرونده فروش یافت نشد');
  return;
}
```

عملگر `||` **یک انتخاب‌گر است، نه یک تطبیق‌گر**. وقتی رکورد هم `_id` و هم `cd` دارد، سمت چپ همیشه به `_id` تقلیل می‌یابد و مقایسه با `cd` ارسالی قطعاً `false` می‌شود.

### ۲.۳ فرم صحیح، همان‌طور که در بقیهٔ سیستم پیاده شده

| محل | پیاده‌سازی | نتیجه |
|---|---|---|
| `crm/sales-domain-v2.js:112` | `caseId(c) === String(id)` با `caseId = _id \|\| cd` | تطبیق تک‌شناسه‌ای — کار می‌کند چون فراخوان‌ها هم `_id \|\| cd` می‌فرستند |
| `api/sales-domain.php:145` (`sd_case_match`) | `($case['_id'] === $id) \|\| ($case['cd'] === $id)` | ✅ **تطبیق دوشناسه‌ای — صحیح** |
| `crm/unofficial-invoice.js:1286` | `($case['_id'] \|\| $case['cd']) === $id` | ❌ **معیوب** |
| `crm/unofficial-invoice.js:1758` | `(d.cd \|\| d._id) === ctx.dealCd` | ⚠️ همان نقص با ترتیب معکوس (بند ۶.۲) |
| `crm/unofficial-invoice.js:2068 / 2090` | `String(x._id \|\| x.cd) === String(_inv.caseId)` | ⚠️ همان الگو در مسیر ابطال (بند ۶.۳) |

منطق مرجع پروژه در PHP (`sd_case_match`) از روز اول درست بوده و کلاینت آن را بازتولید نکرده است.

---

## ۳. چرا `_id` و `cd` متفاوت‌اند؟ (زمینهٔ معماری)

پروندهٔ فروش (`ptf_crm_deals`) در دو مسیر کاملاً مستقل ساخته می‌شود:

| مسیر ساخت | فیلدها | نمونهٔ مقدار |
|---|---|---|
| **قدیمی/محلی** — `ptfSF_ensure` (`crm/salesfiles.js:42`) | فقط `cd` | `cd: genCode('DEAL')` → `DEAL-1042-D17` |
| **جدید/سروری** — `win_offer` (`api/sales-domain.php:745`) | هم `_id` هم `cd` | `_id: 'CASE-9f2a-…'` و `cd: 'DEAL-7c1d-…'` — **دو UUID مستقل** |

خط ۷۴۵ سرور:
```php
$case = ['_id'=>sd_uuid('CASE'), 'cd'=>sd_uuid('DEAL'), 'rootOfferId'=>$offerId, …];
```
`sd_uuid()` (خط ۵۲) از `random_bytes(16)` استفاده می‌کند، پس `_id` و `cd` **هرگز** برابر نمی‌شوند.

### ۳.۱ چرا در عمل «همیشه» خراب است، نه «گاهی»

`_id` فقط هنگام ساخت پرونده اضافه نمی‌شود؛ **هر فرمان سروری که به پرونده دست بزند، به آن `_id` می‌چسباند** (`sd_case_id()` که در نبود `_id` آن را می‌سازد):

| فرمان سرور | خط | اثر |
|---|---|---|
| `win_offer` | ۷۳۴، ۷۴۳، ۷۴۵ | ثبت برد / اتصال متمم |
| `post_receipt` | ۸۷۱ | ثبت هر دریافت قطعی |
| `register_unofficial_invoice` | ۸۹۶ | ثبت خود همین صورتحساب غیررسمی |
| `register_invoice` / `correct_invoice` | ۹۰۰ | ثبت فاکتور رسمی |
| `merge_duplicate_cases` | ۷۹۱ | ادغام پروندهٔ تکراری |
| **`migration_apply_safe`** | **۹۴۱** | **`foreach($cases as &$c) sd_case_id($c);` → به *همهٔ* پرونده‌ها یک‌جا `_id` می‌دهد** |

> **نتیجهٔ قطعی:** پس از یک‌بار اجرای مهاجرت v35 (یا حتی یک ثبت برد/دریافت روی هر پرونده)، آن پرونده `_id` دارد و دکمهٔ «صورتحساب غیررسمی» روی آن **۱۰۰٪ شکست می‌خورد**. تنها پرونده‌هایی که کار می‌کنند، پرونده‌های محلیِ دست‌نخورده‌ای هستند که هیچ‌گاه با سرور تبادل نشده‌اند — یعنی دقیقاً همان پرونده‌هایی که فاقد پیشنهاد برنده و مرحلهٔ ≥۷ هستند و به هر حال از گارد بعدی رد نمی‌شوند.

---

## ۴. بازتولید آزمایشگاهی (اثبات عینی)

ماژول `crm/unofficial-invoice.js` در یک VM ایزولهٔ Node با استاب‌های حداقلی بارگذاری شد و `unofficialInvoiceBuilderOpen` با سه سناریوی داده اجرا گردید. تمام گاردهای دیگر (نقش = `accountant`، `sfStageOf = 9`، یک CO دارای اقلام) عمداً «سبز» تنظیم شدند تا فقط گارد شناسه سنجیده شود:

| سناریو | رکورد پرونده | شناسهٔ ارسالی | نتیجه |
|---|---|---|---|
| **A** | `{cd:'DEAL-LOCAL-1'}` (بدون `_id`) | `DEAL-LOCAL-1` | ✅ از گارد عبور کرد و به رندر دیالوگ رسید |
| **B** | `{_id:'CASE-9f2a-…', cd:'DEAL-7c1d-…'}` | `DEAL-7c1d-…` ← **همان چیزی که دکمه می‌فرستد** | ❌ **`⛔ پرونده فروش یافت نشد`** |
| **C** | همان رکورد B | `CASE-9f2a-…` (یعنی `_id`) | ✅ از گارد عبور کرد |

ماتریس تطبیق matcher کلاینت در برابر matcher سرور:

| پرونده | شناسهٔ ارسالی | matcher کلاینت (خط ۱۲۸۶) | matcher سرور (`sd_case_match`) |
|---|---|---|---|
| محلی، بدون `_id` | `r.cd` (دکمهٔ فاکتور غیررسمی) | ✅ | ✅ |
| محلی، بدون `_id` | `r._id \|\| r.cd` (دکمهٔ حساب پرونده) | ✅ | ✅ |
| **سروری، `_id ≠ cd`** | **`r.cd` (دکمهٔ فاکتور غیررسمی)** | **❌** | ✅ |
| سروری، `_id ≠ cd` | `r._id \|\| r.cd` (دکمهٔ حساب پرونده) | ✅ | ✅ |

تنها سلول قرمز جدول، دقیقاً همان دکمه‌ای است که کارفرما گزارش کرده — و همان سلول در سرور سبز است.

---

## ۵. چرا تست‌های قبلی این را نگرفتند؟ (تحلیل شکست پوشش)

مستند `STEP4-TEST-RESULTS-2026-08-17.md` مدعی «۴۳/۴۳ تست قبول (۱۰۰٪)» است. علت مثبت کاذب:

1. **دادهٔ مصنوعی تست فقط `cd` داشت، نه `_id`.** یعنی تست‌ها دقیقاً سناریوی A (تنها حالت سالم) را پوشش داده‌اند و سناریوی B (حالت واقعیِ Production) اصلاً ساخته نشده بود.
2. **تست‌ها `unofficialInvoiceBuilderOpen` را از مسیر دکمهٔ واقعی صدا نزده‌اند.** ورودی مستقیماً به تابع داده شده، پس اختلاف بین «آنچه `salesfiles.js` می‌فرستد» و «آنچه `unofficial-invoice.js` انتظار دارد» — که *تمام* باگ است — هرگز دیده نشد. این یک **باگ رابط بین دو ماژول (integration seam)** است و تست واحدِ تک‌ماژولی ذاتاً آن را نمی‌بیند.
3. خودِ همان مستند در بخش «محدودیت‌ها» به این نکته اعتراف کرده است: *«تست تکی روی VM نه روی مرورگر واقعی — برای تأیید نهایی، یک تست دستی در CRM واقعی با پروندهٔ واقعی توصیه می‌شود.»*
4. PR #47 (`ARENA-FIX-UNOFFICIAL-INVOICE-BUTTON-2026-08-17.md`) فقط نقصِ **رشته‌سازی `onclick`** را اصلاح کرد (دکمه اصلاً کلیک نمی‌خورد). با آن اصلاح، دکمه شروع به کار کرد و باگِ لایهٔ بعدی — که پشت آن پنهان بود — تازه نمایان شد. یعنی گزارش فعلی کارفرما، **باگ دوم و مستقل** است، نه بازگشت باگ اول.

---

## ۶. یافته‌های جانبی (نقایص هم‌خانواده که هنوز فعال‌اند)

این موارد الان علامت اصلی را نمی‌سازند، ولی هم‌ریشه‌اند و پس از رفع باگ اصلی، بلافاصله به سطح می‌آیند.

### ۶.۱ 🔴 تکرار همان نقص با ترتیب معکوس — `unofficial-invoice.js:1658` و `:1758`

هنگام تأیید نهایی صدور:
```js
dealCd: _unInvState.deal.cd || _unInvState.deal._id || ''      // خط ۱۶۵۸ → cd-اول
…
var _salesCase = (getData('ptf_crm_deals') || []).filter(function (d) {
  return d && (d.wonOffer === _co.no || (_co._id && d.rootOfferId === _co._id)
            || (d.cd || d._id) === ctx.dealCd);                 // خط ۱۷۵۸ → cd-اول
})[0] || null;
```
سه قرارداد متفاوت برای یک مفهوم واحد در یک جریان کاری واحد: `salesfiles` می‌فرستد `cd`؛ `Open` می‌خواند `_id||cd`؛ `Submit` می‌فرستد `cd||_id` و می‌خواند `cd||_id`. این ناسازگاری تضمین می‌کند که هر رفع نقطه‌ایِ فقط-روی-خط-۱۲۸۶، باگ را به مرحلهٔ «تأیید و صدور» منتقل کند.

### ۶.۲ 🟠 اولویت نادرست در انتخاب پروندهٔ مقصد — `unofficial-invoice.js:1757-1759`

شرط `d.wonOffer === _co.no` **قبل از** `(d.cd || d._id) === ctx.dealCd` ارزیابی می‌شود. در سناریوی «متمم» (چند پرونده که به یک پیشنهاد اشاره دارند) یا «پیشنهاد مشترک»، ممکن است فاکتور به **پروندهٔ اشتباه** بچسبد، در حالی که کاربر پروندهٔ مشخصی را باز کرده بود. شناسهٔ صریحِ ارسالی باید مقدم باشد، نه استنتاج از شمارهٔ پیشنهاد.

### ۶.۳ 🟠 همان الگو در مسیر ابطال — `unofficial-invoice.js:2068` و `:2090`

```js
var _d0 = _deals0.filter(function (x) { return x && String(x._id || x.cd) === String(_inv.caseId); })[0];
```
اینجا چون `_inv.caseId` خودش با قاعدهٔ `_id || cd` ساخته شده (خط ۱۷۷۳)، فعلاً تطبیق می‌کند. اما این یک **همزیستی شکننده** است: هر فاکتور قدیمی/مهاجرت‌شده که `caseId` آن با `cd` پر شده باشد، در «ابطال ریشه‌کن» پرونده‌اش پیدا نمی‌شود و رویداد timeline و بازسازی مرحله بی‌صدا از قلم می‌افتد.

### ۶.۴ 🟠 شکست بی‌صدای رکورد موجود — `unofficial-invoice.js:1763-1769`

```js
if (existing && !ctx.isConsolidated) {
  _salesCase = _salesCase;                      // no-op
  existing.caseId = …; existing.customerId = …;
  setData('ptf_crm_invoices', invs);
  setData('ptf_crm_invoices', invs);            // ← نوشتن تکراری
}
```
در این شاخه: (الف) اقلام ویرایش‌شدهٔ کاربر (`linesSnapshot`)، مبلغ، تخفیف و شماره حساب **اصلاً ذخیره نمی‌شوند**؛ (ب) هیچ فراخوان سروری انجام نمی‌شود، پس مطالبات سرور با کلاینت واگرا می‌شود؛ (ج) با این حال toast موفقیت نمایش داده می‌شود. یعنی «صدور مجدد روی پیشنهادی که قبلاً فاکتور غیررسمی دارد» فقط چاپ می‌کند و **بی‌صدا هیچ‌چیز را به‌روز نمی‌کند**.

### ۶.۵ 🟡 تعارض RBAC: حسابدار به پنل پرونده‌ها دسترسی ندارد

- گارد نقش (`salesfiles.js:144` و `unofficial-invoice.js:1281`): مجاز = `isSenior()` یا `accountant`.
- ولی در `crm/rbac.js:25`: `accountant → panels: ['inv','recv','petty','chqprint','cart','ai']` — **`deals` در فهرست نیست.**

پس حسابدار هرگز نمی‌تواند پنل «پرونده‌ها» را باز کند و این دکمه را ببیند. عملاً قابلیت فقط در اختیار نقش‌های ارشد (`admin`/`chairman`/`ceo`/`commercial` با `panels:'*'`) است. اگر مقصود طراحی این بوده که حسابدار صادرکنندهٔ صورتحساب غیررسمی باشد، این یک **شکاف طراحی باز** است. متقابلاً نقش `sales` پنل `deals` را دارد ولی با پیام نقش رد می‌شود (رفتار درست، اما دکمه بی‌جهت به او نمایش داده می‌شود؛ در حالی که دکمهٔ «ارجاع فاکتور» در همان بلوک حالت `locked` بصری دارد).

### ۶.۶ 🟡 «کد مرده» و `new Function` بی‌اثر — `unofficial-invoice.js:1671-1678`

```js
if (typeof window.ptfUnofficialInvoiceVoid === 'function') { /* no direct … */ }
if (typeof window.cleanUpDoubleInvoices === 'function') { /*lint*/ }
try {
  var ev = new Function('try { cleanUpDoubleInvoices(); } catch(e) {}');   // ساخته می‌شود، هرگز صدا زده نمی‌شود
} catch (e){}
```
`cleanUpDoubleInvoices` داخل IIFE محبوس است و از این مسیر قابل دسترسی نیست. نتیجه: مسیر جدید (`unofficialInvoicePrintCases`) برخلاف مسیر قدیمی (`unofficialInvoicePrint`، خط ۵۲۹) **پاکسازی فاکتورهای دوتایی را انجام نمی‌دهد**؛ پس اگر برای همان پیشنهاد فاکتور رسمی صادر شده باشد، نسخهٔ غیررسمی به‌صورت خودکار باطل نمی‌شود و در مطالبات دوبار شمرده می‌شود. ضمناً `new Function` بلااستفاده یک بوی بد امنیتی/CSP نیز هست.

### ۶.۷ 🟡 نبود endpoint سروری برای ابطال — `unofficial-invoice.js:2139-2142`

```js
// TODO: در آینده اگر endpoint سروری void_unofficial_invoice اضافه شد، این‌جا صدا زده شود
```
تأیید شد که `api/sales-domain.php` هیچ اکشن `void_unofficial_invoice` ندارد. یعنی **صدور** سروری است ولی **ابطال** فقط محلی است → دستگاه دوم پس از sync، فاکتورِ باطل‌شده را دوباره فعال می‌بیند (بسته به نتیجهٔ merge). این یک ریسک واگرایی داده در محیط چنددستگاهی است.

---

## ۷. جدول پیام‌های خطای این جریان (برای تطبیق دقیق با آنچه دیده‌اید)

هیچ رشته‌ای با متن دقیق «پرونده فروش ثبت نشد» در کل مخزن وجود ندارد. نزدیک‌ترین و تنها پیام مرتبط با پروندهٔ فروش در این مسیر، مورد ردیف ۴ است:

| # | متن دقیق پیام | فایل:خط | معنی |
|---|---|---|---|
| ۱ | `⛔ ماژول صدور فاکتور غیررسمی بارگذاری نشده است (crm/unofficial-invoice.js).` | salesfiles.js:138 | فایل لود نشده / کش قدیمی |
| ۲ | `⛔ صدور صورتحساب غیررسمی فقط برای مدیران ارشد یا حسابدار مجاز است` | salesfiles.js:145 | نقش کاربر |
| ۳ | `⛔ صدور صورتحساب غیررسمی فقط برای مدیران ارشد یا حسابدار مجاز است` | unofficial-invoice.js:1282 | همان گارد، تکراری |
| **۴** | **`⛔ پرونده فروش یافت نشد`** | **unofficial-invoice.js:1289** | **🎯 باگ این گزارش** |
| ۵ | `🔒 صدور صورتحساب غیررسمی پس از تحویل کارفرما فعال می‌شود (مرحلهٔ فعلی: N از ۱۲).` | unofficial-invoice.js:1294 | گارد مرحله <۷ |
| ۶ | `⛔ هیچ پیشنهاد دارای اقلام به این پرونده متصل نیست.` | unofficial-invoice.js:1299 | پرونده بدون CO/TC دارای اقلام |
| ۷ | `⛔ ثبت سروری صورتحساب غیررسمی ناموفق بود و رکورد محلی بازگردانده شد: …` | unofficial-invoice.js:1830 | خطای سرور پس از صدور |

> اگر پیام دیده‌شده ردیف **۵** بوده (نه ۴)، ریشه متفاوت است: پرونده پیدا شده ولی مرحلهٔ آن زیر ۷ است — یعنی رویداد «تحویل کارفرما» (`shipEvents.type='delivered'`) یا وضعیت `st7` روی درخواست ثبت نشده. لطفاً متن دقیق را تأیید بفرمایید تا در صورت نیاز، شاخهٔ دوم را هم عمیق ریشه‌یابی کنم.

---

## ۸. ارزیابی اثر و ریسک

| بعد | ارزیابی |
|---|---|
| **یکپارچگی داده** | ✅ **سالم.** شکست *پیش از* هر `setData` رخ می‌دهد؛ هیچ فاکتور ناقص، تخصیص یتیم یا مطالبهٔ اشتباه تولید نمی‌شود. |
| **اثر مالی** | ✅ صفر — نه مطالبه‌ای ساخته می‌شود نه بستانکاری‌ای جابه‌جا. |
| **اثر عملیاتی** | 🔴 **بالا** — تنها مسیر صدور صورتحساب غیررسمی از داخل پرونده مسدود است. تنها راه دور زدن، مسیر قدیمی از ماژول «پیشنهادها» (`unofficialInvoicePrint`) است که فاقد ویرایش قلم‌به‌قلم و حالت تجمیعی (الزامات ۱ و ۳ کارفرما) است. |
| **اثر امنیتی** | ✅ خنثی — گاردهای نقش پیش از این نقطه اجرا می‌شوند و دست‌نخورده‌اند. |
| **بازگشت‌پذیری** | ✅ رفع، یک تغییر کاملاً موضعی و کم‌ریسک است. |

---

## ۹. توصیهٔ اصلاح (فقط طرح — پیاده‌سازی نشده)

### ۹.۱ اصل حاکم

> **شناسهٔ پرونده در سراسر کلاینت باید با همان قاعدهٔ `sd_case_match` سرور تطبیق داده شود: «تطبیق با _id **یا** cd»، نه «انتخاب _id **یا** cd و سپس تطبیق».**

### ۹.۲ سه لایهٔ اصلاح پیشنهادی (به‌ترتیب اولویت)

| گام | محل | ماهیت |
|---|---|---|
| **۱ (حداقلی/فوری)** | `unofficial-invoice.js:1286` | تبدیل matcher به تطبیق دوشناسه‌ای — همان الگوی `sd_case_match` |
| **۲ (هم‌راستاسازی)** | `salesfiles.js:1186` | ارسال `r._id \|\| r.cd` مطابق قرارداد خطوط ۳۷۱/۳۷۲/۱۱۷۹ همان فایل |
| **۳ (ریشه‌ای/پیشنهادی)** | یک helper واحد سراسری، مثلاً `window.ptfCaseMatch(rec, id)` و `window.ptfCaseId(rec)` | حذف هر ۵ نسخهٔ پراکندهٔ تطبیق (خطوط ۱۲۸۶، ۱۶۵۸، ۱۷۵۸، ۲۰۶۸، ۲۰۹۰) و استفاده از منبع واحد |

گام ۱ و ۲ **مکمل‌اند، نه جایگزین**: گام ۱ به‌تنهایی باگ باز شدن دیالوگ را رفع می‌کند ولی نقص خط ۱۷۵۸ در مرحلهٔ صدور باقی می‌ماند؛ گام ۲ به‌تنهایی جریانِ پرونده‌های محلیِ بدون `_id` را نمی‌شکند ولی مسیرهای دیگری که `cd` می‌فرستند (مثلاً فراخوان‌های آینده) همچنان آسیب‌پذیرند.

### ۹.۳ آزمون پذیرش پیشنهادی (برای جلوگیری از تکرار مثبت کاذب)

1. **مورد قرمز فعلی:** پرونده‌ای با `_id ≠ cd` بساز → دکمه را از خود کشوی پرونده کلیک کن → دیالوگ باید باز شود.
2. **عدم پسرفت:** پرونده‌ای فقط با `cd` (بدون `_id`) → باید همچنان باز شود.
3. **تست از مسیر رابط، نه فراخوان مستقیم:** رشتهٔ `onclick` تولیدشده توسط `sfDrawerHtml` استخراج و همان اجرا شود — تا شکاف بین دو ماژول واقعاً پوشش داده شود.
4. **صدور تا انتها:** پس از باز شدن دیالوگ، «تأیید و صدور» زده شود و بررسی گردد که `newInv.caseId === deal._id` است و `register_unofficial_invoice` با `case_not_found` رد نمی‌شود.
5. **دوباره‌صدور:** روی همان پیشنهاد بار دوم صادر شود (شاخهٔ `existing` — بند ۶.۴) و بررسی شود که مبلغ ویرایش‌شده واقعاً ذخیره می‌شود.

---

## ۱۰. جمع‌بندی

- **باگ گزارش‌شده:** یک نقص یک‌خطی و قطعی در `crm/unofficial-invoice.js:1286` — استفاده از `||` به‌عنوان تطبیق‌گر به‌جای تطبیق دوشناسه‌ای؛ در ترکیب با `crm/salesfiles.js:1186` که برخلاف بقیهٔ دکمه‌های همان بلوک، `_id` را در نظر نمی‌گیرد.
- **بازتولید:** به‌صورت آزمایشگاهی و قطعی اثبات شد (سناریوی B).
- **دامنه:** هر پرونده‌ای که `_id` سروری گرفته باشد — یعنی عملاً همهٔ پرونده‌های زندهٔ سیستم پس از v35.
- **داده:** هیچ آسیبی وارد نشده و نیازی به ترمیم داده نیست.
- **۷ یافتهٔ جانبی هم‌ریشه** شناسایی شد (بند ۶) که برای پایدارسازی کامل این قابلیت باید در همان تغییر دیده شوند.
- طبق دستور کارفرما، **هیچ کدی تغییر داده نشده است**؛ این سند صرفاً گزارش ارزیابی و ریشه‌یابی است.
