# RELEASE-NOTES — v33.21.0 (۱۴۰۵/۰۸/۱۱ — 2026-08-02)

## 🔴 هاتفیکس بحرانی — باید سریع دیپلوی شود

**BUG-SYNC-RD-SCOPE-001:** نسخهٔ v33.20.0 که امروز دیپلوی شده، یک خطای تولید پنهان دارد:
در `sync.js`، تابع کمکی `rd()` داخل IIFE تعریف شده ولی `ptfReadArchive()` (تombstoneها) در
اسکوپ سراسری فایل (بعد از پایان IIFE) آن را صدا می‌زد → **ReferenceError: rd is not defined**
در هر pull/pushِ دارای کلید کسب‌وکاری → نشان «🔴 آفلاین» کذب، عدم اعمال تغییرات دستگاه‌های
دیگر و شکست tombstone-فیلتر (خطر زنده‌شدن رکوردهای حذف‌شده در merge).
علت عبور تست‌ها در نشست قبل: تسترها `rd/wr` را در sandbox تزریق می‌کردند.
⇐ در این نسخه خواندن آرشیو با راهنمای خودکفا (`ptfReadDeletedArchiveStr`) در همان اسکوپ انجام می‌شود
و چک رفتاری دائمی در tester300 برای آن ثبت شده است.

## 🚀 PTF-SCALE-P0 — ارتقای فناوری سینک برای افزایش تعداد کاربران

پاسخ به درخواست کارفرما: «حداکثر تعداد کاربر با وضعیت فعلی چقدر است و چه کارهایی برای رشد؟»
تحلیل کامل ظرفیت در بک‌لاگ (UR-2026-08-02-18..21). سه ضایعهٔ اصلی بار سرور حذف شد:

| # | مشکل قبلی | رفع در v33.21.0 | اثر |
|---|---|---|---|
| ۱ | با هر تغییر، «اسنپ‌شات کامل (~۴MB)» برای **همهٔ کاربران** می‌رفت (rev سراسری تنها) | **دلتا-پول به‌ازای هرکلید**: کلاینت `krevs` (rev هرکلید) را می‌فرستد؛ سرور فقط کلیدهای جدیدتر را برمی‌گرداند + خواندن تنبَل آرشیو | ~۹۰٪+ کاهش ترافیک/CPU پول در ساعات ویرایش |
| ۲ | `getData` فاز B به‌ازای هر خواندن کلید منقضی‌شده، یک پول کامل مستقل می‌زد (نه single-flight نه حد نرخ) | **sharedPull**: تک‌پرواز + حداقل فاصلهٔ ۲ ثانیه + دلتا (rev مشترک با sync.js) + به‌روزرسانی مرکزی کش/آینهٔ همهٔ کلیدهای برگشتی | حذف بزرگ‌ترین هزینهٔ پنهان (ده‌ها پول کامل در دقیقه) |
| ۳ | پول ۲۰ثانیه‌ای در تب‌های مخفی/غیرمتمرکز هم ادامه داشت (کاربر چندتبه = ضرب‌دار) | تب مخفی: بدون پول؛ غیرمتمرکز: حداکثر هر ۱۲۰ ثانیه؛ **جبران فوری** روی focus/visibilitychange | حذف بار مردهٔ تب‌های پس‌زمینه |

**سازگاری با عقب ۱۰۰٪:** کلاینت قدیمی (بدون krevs) همان اسنپ‌شات کامل را می‌گیرد؛ سرور قدیمی krevs را نادیده می‌گیرد. مسیر بوت (forceFull، since=0) دست‌نخورده.

## فایل‌های تغییرکرده

- `api/crm.php` — data_pull: پارامتر `krevs` + فیلتر دلتا + فلگ `delta` در پاسخ + آرشیو تنبَل
- `crm/sync.js` — ارسال krevs در URL؛ گارد تب مخفی/غیرمتمرکز (۱۲۰ثانیه)؛ listenerهای focus/visibilitychange؛ **فیکس BUG-SYNC-RD-SCOPE-001**
- `crm/client-server.js` — serverPull سه‌ورودی (krevs)؛ **sharedPull**؛ اتصال getData
- بامپ نسخه: `crm/index.html` (۸۴ وهله) + `crm/sw.js` (`ptf-crm-v33.21.0`) + `crm/clear-cache.html`
- `_tools/uat/tester300-delta-poll.js` (**جدید — ۲۲/۰**)؛ بازنویسی مقاوم-به-indirection در `tester170` (الگوی مجرب نشست قبل)

## آزمون

- tester300 (جدید): **۲۲ PASS / ۰ FAIL** (سورس PHP/JS + runtime واقعی pullCheck/sharedPull در vm)
- رگرسیون کامل: **۲۹۵ تستر → ۲۳۹ PASS / ۵۶ FAIL فایل و ۵۳۸۸/۸۴ چک = دقیقاً baseline + tester300** (۵۶ FAIL، همان بدهی قدیمی؛ صفر شکست جدید)
- `python3 _tools/audit.py`: ✅ ۰ خطا
- sitemap: ۵۲۰ URL، بدون تغییر — `crm-update.zip`: ۵۵۸ ورودی (لیست یکسان با قبل) بازسازی شد

## گام‌های انتشار

مرج PR ← **دیپلوی فوری** (به‌خاطر هاتفیکس همگام‌سازی زندهٔ پروداکشن) ← به کاربران: یک‌بار رفرش.
پس از چند روز پایدارسازی: گام بعدی نقشهٔ راه مقیاس = P1 (اتصال واقعی به MySQL + رفع سقف ۱۰۰ کاربر).
