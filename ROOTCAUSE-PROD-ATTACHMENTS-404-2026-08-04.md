# 🔍 هندآف — ریشه‌یابی 404 ضمایم پروداکشن (برای نشست بعدی)

**تاریخ:** ۱۴۰۵/۰۵/۱۳ (2026-08-04)
**وضعیت:** 🟠 باز — به‌درخواست کارفرما، ادامهٔ ریشه‌یابی به نشست بعد موکول شد.
**علائم:** ضمایم قدیمی (تیر/اوایل مرداد) در پروداکشن 404 می‌دهند؛ ضمایم «امروز» سالم‌اند؛ کارفرما تأیید کرده همان فایل‌ها در استیجینگ قابل مشاهده‌اند و **هیچ فایلی از باکت حذف نشده**.

---

## ۱) واقعیت‌های تأییدشده (تا این لحظه)

| # | واقعیت | روش تأیید |
|---|---|---|
| ۱ | فایل‌ها در باکت آروان موجودند و حذف نشده‌اند | اعلام کارفرما |
| ۲ | `storage-config.php` استیجینگ و پروداکشن **یکسان** است | اعلام کارفرما |
| ۳ | کد API ذخیره‌سازی (`api/storage.php`, `storage-lib.php`) از کامیت اول تاکنون تغییری نکرده | git |
| ۴ | فایل‌های کلاینت (`petty.js`, `storage.js`, `inqreader.js`) در پروداکشن و استیجینگ بایت‌به‌بایت یکسان‌اند | git blob |
| ۵ | `ptfPurgeCloudOrphans` (پاک‌سازی زباله‌های ابری) وجود دارد و DELETE دائمی می‌زند — ولی **عامل این حادثه نیست** (فایل‌ها موجودند) | کد + تأیید کارفرما |
| ۶ | اسکن کامل localStorage **استیجینگ**: هیچ مرجعی به `LEVELSWITCH`/`6a6da4` یافت نشد (تعداد کلیدها = ۰) | کنسول استیجینگ |
| ۷ | لیست باکت (`action=list&prefix=rfqinq/`) از دید استیجینگ: کلید `6a6da4b91f71b-LEVELSWITCH…` **وجود ندارد**؛ کلیدهای هم‌دوره (`6a61e5bd…`, `6a60a8e9…`) موجودند | کنسول استیجینگ |
| ۸ | کنسول پروداکشن به **هیچ** دستوری پاسخ نمی‌دهد (حتی `console.log` ساده) — رفتار غیرعادی | کنسول پروداکشن |
| ۹ | مرجع `LEVELSWITCH` در localStorage هیچ‌کدام از دو محیط نیست (نه در rfqs/inqitems/deals و نه در اسکن کامل) | کنسول هر دو |

## ۲) نظریه‌های ردشده

- ❌ **حذف فایل‌ها از باکت** (پاک‌سازی ابری / lifecycle / پاک‌سازی امن): فایل‌ها موجودند.
- ❌ **ناهم‌خوانی region/endpoint بین دو محیط**: کانفیگ یکسان است.
- ❌ **باگ در کد مرجع**: کد دو محیط یکسان و از ابتدا بدون تغییر است.

## ۳) گره‌های باز (پارادوکس اصلی)

۱. اگر مرجع `LEVELSWITCH` در localStorage هیچ محیطی نیست و در باکت هم نیست — «مشاهده در استیجینگ» از کجا می‌آید؟
   - محتمل: از **MySQL** (کلاینت sync از DB می‌خواند) یا از **کش مرورگر** (تب قدیمی بدون رفرش).
   - اقدام: در استیجینگ، صفحهٔ همان پرونده را با **Ctrl+Shift+R** باز کنید و ببینید فایل باز می‌شود یا نه؛ اگر نشد = کش مرورگر بود.
2. بی‌پاسخی کامل کنسول پروداکشن — باید جدا بررسی شود (خطای JS اولیه؟ کنسول بسته؟ DevTools؟).
3. فایل‌های 404 پروداکشن (`pettyPTY-1022/2026-07/…` و `rfqinq/2026-08/…`) — باید دید رکوردِ پروداکشن (MySQL) به چه کلیدی ارجاع می‌دهد.

## ۴) ابزارهای آماده (در همین ریپو، `_tools/` — فقط‌خوان، از دیپلوی مستثنا)

| ابزار | کار | اجرا |
|---|---|---|
| `_tools/data-probe.php` | مقایسهٔ DB↔فایل برای کلیدهای مالی + جستجوی `LEVELSWITCH` در **همهٔ** کلیدهای DB و فایل (با rev/updated/mtime) | آپلود در public_html پروداکشن → باز کردن در مرورگر → ارسال خروجی |
| `_tools/storage-probe.php` | نمایش کانفیگ ماسک‌شده + تست GET امضاشدهٔ همان کلیدها با ۴ سناریوی region + لیست prefix ها | همین‌طور (اگر نظریهٔ «کانفیگ دوم» لازم شد) |

## ۵) گام‌های پیشنهادی نشست بعد (به ترتیب)

1. اجرای `_tools/data-probe.php` روی **پروداکشن** → خروجی را تحلیل کن:
   - اگر `LEVELSWITCH` فقط در **DB** هست → ریشه = ردیف کهنه/ناسازگار MySQL (کلاس P0 v33.22.3) → فیکس: اجرای مراحل ۳–۶ ویزارد `api/migrate.php` (فایل→DB) یا ریست mode.
   - اگر در هیچ منبعی نیست → «مشاهدهٔ استیجینگ» از کش مرورگر بوده → بررسی مجدد علائم با رفرش کامل.
2. بررسی «بی‌پاسخی کنسول پروداکشن» (باز کردن DevTools از نو، چک خطاهای سرخ ابتدای لود).
3. اگر ریشه = MySQL کهنه: بعد از اصلاح، همان فایل‌های خطادار را در پروداکشن تست کن.
4. بازطراحی `ptfPurgeCloudOrphans` به «قرنطینه» (کپی به `trash/` قبل از حذف) — ریسک امنیتی داده، مستقل از این حادثه.

## ۶) نکات جانبی ثبت‌شده

- در امضای URL خطای پروداکشن، region داخل Credential = `s3.ir-tbz-sh1.arvanstorage.ir` (یک هاست کامل) دیده شد؛ با اینکه کانفیگ دو محیط یکسان اعلام شده، اگر در بررسی‌های بعدی دوباره این مقدار دیده شد، یعنی پروداکشن یک `storage-config.php` **دوم** (مثلاً داخل public_html — fallback سوم در `load_cfg`) را load می‌کند — آن را بیابید.
- `ptfPurgeCloudOrphans` از کامیت اول وجود داشته و تا v33.22.2 دکمهٔ «☁️ پاک‌سازی زباله‌های ابری» داشت؛ در v33.22.2 از UI حذف شد ولی تابع باقی است. در پنجرهٔ بحران ۲–۳ مرداد در دسترس بود — اگر کسی آن را زده باشد، فایل‌های «یتیم‌نما» (که ارجاعشان در localStorage آن دستگاه نبود) حذف شده‌اند؛ ولی کارفرما تأیید کرده فایل‌ها موجودند، پس این مسیر فعلاً بسته است مگر اینکه شواهد جدیدی بیاید.
