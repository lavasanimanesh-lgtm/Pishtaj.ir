# 🚀 ریلیز v70.0 — اسپرینت ۷۰: جریان برنده → پرونده خودکار + گزارشات مدیریتی
**تاریخ:** تیر ۱۴۰۵ | **مبنا:** دور دوم بازخورد کارفرما | **استوری‌ها:** US-140 … US-144

---

## 🔍 نتیجه بررسی دو مورد «منجمد»

| مورد | یافته | اقدام |
|---|---|---|
| **ماژول درخواست‌ها (inqs)** | کارکرد مبهم — طبق دستور شما | ✅ **از منو حذف شد.** داده اقلام حفظ شده و دکمه «📥 بارگذاری اقلام درخواست» در فرم TO/CO همچنان کار می‌کند |
| **ماژول پرونده‌ها** | 🐞 باگ واقعی پیدا شد: پرونده‌های قدیمیِ بدون تایم‌لاین هنگام تغییر وضعیت **کرش می‌کردند** (Cannot read 'push') | ✅ رفع شد + تست رگرسیون اضافه شد |

---

## ✨ تغییرات اصلی

### 1) جریان «برنده» شدن پیش‌فاکتور (US-141) 🏆
- تغییر وضعیت CO به «برنده» → **دیالوگ تایید** با هشدار صریح: «دیگر امکان بازگشت به مرحله قبل وجود ندارد». بدون تایید، هیچ تغییری اعمال نمی‌شود.
- پس از تایید، وضعیت **قفل** می‌شود (بج «🏆 برنده 🔒» — بدون امکان ویرایش/حذف پیشنهاد).
- **بلافاصله و خودکار** یک پرونده در ماژول پرونده‌های پروژه ساخته می‌شود و منتقل می‌شوند:
  - اصل درخواست + ضمائم آن (از استعلام مرتبط) → پوشه جدید «**اصل درخواست و ضمائم**»
  - نسخه پیشنهاد مالی (CO) و پیشنهاد فنی (TO) مرتبط → پوشه اسناد مالی (PDF سیستمی با یک کلیک)
- مدارک سیستمی بعدی خودکار به پرونده می‌روند: **پکینگ لیست** (از قبل)، **فاکتور** (جدید — به‌محض ثبت در ماژول فاکتورها به پوشه مالی پرونده اضافه می‌شود). بارنامه، هزینه پیک، ریلیز نوت، گزارش بازرسی، نتایج آزمون و مکاتبات توسط کاربران در پوشه مربوطه آپلود می‌شوند (۹ پوشه).
- **قفل فاکتور:** دکمه «🧾 فاکتور» (ارجاع به حسابدار) فقط برای CO **برنده** فعال است؛ در غیر این‌صورت «🧾 فاکتور 🔒» + کنترل مضاعف در خود تابع ارجاع.

### 2) بهبودهای پیشنهاد فنی/مالی (US-142)
- **جستجو با شماره درخواست کارفرما:** Inquiry No در جدول ستون جدا دارد و در جستجو لحاظ می‌شود.
- **تب‌های جدا:** «🔧 پیشنهادهای فنی» / «💰 پیشنهادهای مالی» / «همه».
- **Contact Person قفل:** فیلد «اختصاری انگلیسی» (Mr. Lavasani) به فرم تعریف کاربر اضافه شد (اجباری)؛ هنگام صدور پیشنهاد، Contact Person = کاربر جاری و **غیرقابل تغییر** (readonly).
- **حذف Model از پیشنهاد فنی** (فرم، سند PDF و خروجی اکسل) — در CO حفظ شد. سقف ستون‌های جدید: **۴** (فونت پلکانی).
- **تبدیل TO→CO فقط یک بار:** پس از تبدیل، دکمه «→CO 🔒» قفل می‌شود؛ اگر CO حاصله حذف شود، دکمه دوباره فعال می‌گردد.
- **بندهای T&C قفل‌شونده:** هر بند انتخاب‌شده از کتابخانه در منوی کشویی «🔒» و غیرفعال می‌شود؛ با حذف بند از فهرست، دوباره آزاد می‌شود.
- **مهر و امضای صادرکننده:** تصویر امضا + مهر کاربر صادرکننده (از پروفایل «✍️ امضای من» سامانه مکاتبات) روی PDF پیشنهاد مالی/پیش‌فاکتور در جایگاه Authorized Signature & Stamp درج می‌شود.

### 3) ماژول کالاها (US-143)
- **کد کالا خودکار** (PTF-P-0001، PTF-P-0002، …) و فیلد آن در فرم **قفل**.
- شرح کالا فارسی **یا** انگلیسی — اما **واحد باید هم‌زبان شرح** باشد (شرح EN → No/PCS/Set/…؛ شرح فارسی → عدد/شاخه/…)؛ مغایرت با پیام روشن رد می‌شود.
- شرح و واحد برای ثبت **اجباری**.
- **جستجوی زنده در پیشنهاد:** دکمه «+ از ماژول کالا» حالا پنجره جستجو باز می‌کند — تایپ «زانو» → همه کالاهای منطبق فهرست می‌شوند → انتخاب با یک کلیک (به‌جای prompt قدیمی).

### 4) ماژول گزارشات (US-144) 📈
- **فقط ادمین و رییس هیات مدیره** — برای سایر نقش‌ها دکمه مخفی و ورود مسدود است.
- **عملکرد کاربران:** به تفکیک هر کاربر و بازه (امروز/۷/۳۰/۹۰ روز/همه): تعداد TO، CO، CO برنده، تامین‌کننده، مشتری، لید، فاکتور، نامه، کل اقدامات.
- **رصد کلیه تحرکات:** نمایشگر کامل رد پا (Audit Trail) با فیلتر کاربر + ماژول + بازه.

---

## 🧪 تضمین کیفیت
| مورد | نتیجه |
|---|:---:|
| ممیز ۸بخشی (۳۲۲ صفحه) | ✅ PASS |
| سینتکس JS همه ماژول‌ها | ✅ PASS |
| رگرسیون tester1–6 | ✅ ۲۴۳/۲۴۳ |
| tester7 جدید (اسپرینت ۷۰) | ✅ ۶۴/۶۴ |
| **جمع کل** | **✅ ۳۰۷/۳۰۷** |

## 📁 فایل‌های تغییریافته (دلتا)
`crm/offers.js` (جریان برنده/تب‌ها/قفل‌ها/امضا/جستجوی کالا) • `crm/reports.js` (**جدید**) • `crm/rbac.js` (قفل فاکتور + اختصاری EN + فاکتور→پرونده) • `crm/projects.js` (رفع کرش + پوشه اصل درخواست + PDF سیستمی) • `crm/index.html` (حذف inqs + گزارشات + کالای خودکار) • `crm/shell.js` (منو) • `crm/sw.js` (کش v70) • `ROADMAP-SPRINTS-52-70.md`

## ⚠️ نکات استقرار و تست پیشنهادی
1. فقط `pishtaj-release-v70-delta.zip` را آپلود کنید.
2. **سناریوی تست کامل:** یک CO بسازید → وضعیت را «برنده» کنید → هشدار را تایید کنید → در «پرونده‌های پروژه» پرونده خودکار با مدارک منتقل‌شده را ببینید → حالا دکمه «🧾 فاکتور» روی همان CO فعال شده → ارجاع به حسابدار → حسابدار فاکتور ثبت می‌کند → فاکتور خودکار در پوشه اسناد مالی پرونده ظاهر می‌شود.
3. برای درج مهر/امضا روی CO: هر کاربر یک بار در «مکاتبات → ✍️ امضای من» تصویر امضا و مهر خود را ثبت کند.
4. برای Contact Person: کاربران قدیمی فاقد «اختصاری انگلیسی» هستند — در صورت نیاز کاربر را حذف و دوباره تعریف کنید (یا موقتاً از نام پروفایل استفاده می‌شود).

## 🔭 باقیمانده رودمپ
- **۷۰.۵ (نهایی):** US-131 (پردازش هوشمند پیوست RFQ) + امنیت سروری + Mini-CMS + پیامک (منتظر کلید شما) + محتوای US-127/128 + HANDOVER نهایی
