# RELEASE NOTES — v34.29.6 (یادآور با یک کلیک تمام می‌شود + اعلان درخواست‌های سایت)

**تاریخ:** ۲۰۲۶-۰۹-۰۲ · **کمیت:** tester581 (۱۸ سنجه) + بازتولید رفتاری jsdom ۱۳/۱۳ · **گیت:** 197 PASS / 0 FAIL

## گزارش کارفرما

«در قسمت یادآورها یک یادآور ثبت کردم ولی هرچه انجام شد را می‌زنم بلافاصله دوباره می‌آید، دیوونه کرده، اصلاً پاک نمی‌شود. باید با یک کلیک تمام شود. همچنین درخواست‌هایی که از سایت ثبت می‌شوند اعلان نمی‌آید.»

## ① یادآور — چرا «انجام شد» برمی‌گشت؟

**ریشهٔ اول — نوشت محلی نبود:** از v34.8.13 عملیات یادآور به «فرمان اتمیک سروری» مهاجرت کرد ولی `remDone`/`remDel`/`remSnooze` در این مسیر **هیچ نوشت محلی انجام نمی‌دادند**؛ فقط فرمان می‌رفت و امید بود ACK سرور وضعیت را projection کند. یعنی تا رسیدن پاسخ سرور (~۱ ثانیه در بهترین حالت، و در کندی/خطا هرگز) ردیف «باز» می‌ماند — و چون `getData` در فاز B کش ۳۰ثانیه‌ای دارد، حتی رندرهای بعدی هم مقدار کهنه می‌دادند. حذف هم دقیقاً همین بود: «اصلاً پاک نمی‌شود.»

**ریشهٔ دوم — نوتیفایر ردیف را زنده می‌کرد:** `checkDueReminders` (سازندهٔ کارت اعلان سررسید) کل ردیف را که از نسخهٔ قبل از کلیک خوانده بود (st='open') دوباره upsert می‌کرد؛ اگر این فرمان دیرتر از «انجام شد» به سرور می‌رسید، سرور را به 'open' برمی‌گرداند و یادآور **برمی‌گشت**.

**اصلاح:**
- `remDone`/`remSnooze`/`remDel`/`addReminder`: نوشتن محلیِ بی‌درنگ از مسیر استاندارد `ptfEntitySaveCollection` (نوشتن بی‌صدا بدون dirty + فرمان اتمیک + سپر dirty در خطا) — **یک کلیک = همان لحظه پایان**؛ حذف هم فوری.
- `checkDueReminders`: پچ مینیمال `{cd, notifiedUsers, msgSent}` — merge سروری فقط فیلدهای حاضر در payload را بازنویسی می‌کند؛ این فرمان دیگر هرگز `st` را زنده نمی‌کند.

**اثبات (بوت کامل jsdom با فاز B فعال و سرور جعلیِ عین api/sales-domain.php — ۱۳/۱۳):** بلافاصله بعد از کلیک local.st=done؛ ACK سرور done؛ شبیه‌سازی upsert کهنهٔ کل‌ردیفی نشان داد مسیر قدیمی واقعاً سرور را برمی‌گرداند و مسیر جدید چنین payloadای نمی‌فرستد؛ 🗑️ = حذف محلی فوری + entity_delete؛ ۳۰ ثانیه پایدار بدون بازگشت.

## ② درخواست‌های سایت — حالا اعلان می‌آید

رویدادهای `supplier_site` (ثبت‌نام تامین‌کننده) و `rfq_site` (استعلام هوشمند) تا امروز فقط «همگام‌سازی بی‌صدا» بودند — هیچ کارتی در زنگ/کارتابل ساخته نمی‌شد. اکنون:

- **کارت اعلان `site_req`** با سپر dkey (هر رویداد فقط یک‌بار، بدون تکرار در تیک‌های بعدی) برای نقش مرتبط: ثبت‌نام تامین‌کننده → مدیران ارشد؛ استعلام → ارشد + تیم فروش.
- دکمهٔ «↗ برو» مستقیم به پنل درست (تامین‌کنندگان / درخواست‌ها).
- **با تعیین وضعیت (تایید/رد/حذف) کارت به‌طور خودکار برای همه بسته می‌شود** — استاندارد اعلانات v33.4.1 حفظ است: یک‌باره، بدون هرروزتکرار، خودپاک‌شونده.

## پیوند با زنجیرهٔ قبلی

این پنجمین لایهٔ پروندهٔ «تب صفحهٔ جدید» است: ① v34.28.0 کلیک تب؛ ② v34.29.3 برخورد کلاس‌های pn/tb با CSS سراسری و نامرئی‌شدن محتوا با display:none؛ ③ v34.29.4 پارسر خروجی هوش خارجی؛ ④ v34.29.5 برخورد شناسهٔ pgTitle؛ ⑤ v34.29.6 این نسخه — یادآور یک‌کلیکی + اعلان درخواست سایت.

## بعد از دیپلوی

فایل‌های v34.29.6 را آپلود کنید (به‌ویژه `crm/leads.js`، `crm/bridge.js`، `crm/rbac.js`، `crm/index.html`، `crm/sw.js`)، تب را ببندید/باز کنید. یادآور سررسیده را که «✔ انجام شد» بزنید همان لحظه می‌رود؛ ثبت‌نام/استعلام جدید سایت، زنگ اعلان با کارت «برو» می‌آورد.
