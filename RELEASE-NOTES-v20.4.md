# 💳 ریلیز v20.4 — US-445 «کارت ویزیت هوشمند با AI»

**موضوع:** استخراج کارت ویزیت و ثبت در مشتری/تامین‌کننده/سرنخ  
**نوع:** قابلیت جدید دستیار AI، هم‌خانواده با فهرست‌ها و تامین‌کننده تخصصی

---

## ✅ اجرا شد

### ۱) تب جدید در دستیار
در ماژول دستیار، تب جدید اضافه شد:

```text
💳 کارت ویزیت
```

کاربر می‌تواند تصویر یا PDF کارت ویزیت را بارگذاری کند.

---

### ۲) استخراج AI کارت ویزیت
اکشن جدید سمت سرور اضافه شد:

```php
bizcard
```

فایل:

```text
api/llm.php
```

فیلدهای استخراجی:

- نام شرکت فارسی
- نام شرکت انگلیسی
- نام شخص
- سمت
- موبایل
- تلفن ثابت
- ایمیل
- وب‌سایت
- آدرس
- شهر/کشور
- حوزه فعالیت
- برندها
- تجهیزات تخصصی

---

### ۳) پیش‌نمایش قابل ویرایش
پس از استخراج، همه فیلدها در فرم پیش‌نمایش نمایش داده می‌شوند و کاربر می‌تواند قبل از ثبت، اصلاح کند.

---

### ۴) ثبت در سه مقصد
کاربر می‌تواند مقصد ثبت را انتخاب کند:

```text
تامین‌کننده
مشتری
سرنخ
```

---

### ۵) ثبت تامین‌کننده
اگر مقصد تامین‌کننده باشد:

- رکورد در `ptf_crm_suppliers` ساخته می‌شود.
- اطلاعات رابط، تلفن، ایمیل، وب‌سایت و آدرس ثبت می‌شود.
- برندها در `spBrands` ثبت می‌شوند.
- تجهیزات تخصصی در `spEquip` ثبت می‌شوند.
- اگر فقط نام انگلیسی وجود داشته باشد، رکورد می‌تواند به‌عنوان خارجی/English-based ثبت شود.

---

### ۶) ثبت مشتری
اگر مقصد مشتری باشد:

- رکورد در `ptf_crm_customers` ساخته می‌شود.
- رابط و تماس‌ها به people/coTels متصل می‌شوند.
- صنعت/حوزه فعالیت از کارت پیشنهاد می‌شود.

---

### ۷) ثبت سرنخ
اگر مقصد سرنخ باشد:

- رکورد در `ptf_crm_leads` ساخته می‌شود.
- stage پیش‌فرض `new` است.
- منبع روی «کارت ویزیت» ثبت می‌شود.
- تاریخچه ثبت اولیه هم ساخته می‌شود.

---

### ۸) ضدتکرار
قبل از ثبت، مسیر موجود `ptfCheckDup` استفاده می‌شود. اگر رکورد مشابه پیدا شود، کاربر باید تایید کند که با وجود احتمال تکراری بودن، ثبت جدید انجام شود.

---

### ۹) ماندگاری نتیجه
نتیجه کارت ویزیت در history شخصی دستیار ذخیره می‌شود:

```text
ptf_ai_hist_<user>
```

یعنی با جابه‌جایی بین ماژول‌ها نتیجه از بین نمی‌رود.

---

## معماری

- کلید داده جدید ساخته نشد.
- از همان کلیدهای موجود استفاده شد:
  - `ptf_crm_suppliers`
  - `ptf_crm_customers`
  - `ptf_crm_leads`
- از زیرساخت موجود AI و history استفاده شد.
- از مسیر ضدتکرار موجود استفاده شد.
- پروفایل تخصصی تامین‌کننده US-399 با `spBrands/spEquip` تغذیه می‌شود.

---

## فایل‌های تغییر یافته

- `crm/ai-workbench.js`
- `api/llm.php`
- `crm/index.html`
- `crm/sw.js`
- `_tools/uat/tester120-v204.js`
- اصلاح تستر قبلی:
  - `_tools/uat/tester119-v203.js`
- `RELEASE-NOTES-v20.4.md`
- `PTF-MASTER-HANDOVER.md`

---

## 🔬 کیفیت

- تستر جدید: `tester120-v204.js` با ۱۴ چک.
- رگرسیون کامل: **۱۰۷ تستر / صفر خطا**.
- Audit سایت: PASS با همان هشدار قدیمی تصاویر سنگین.

---

## صف بعدی پیشنهادی

طبق صف v20.2:

1. US-405..408 فاز اسناد مستقل از پرونده
2. US-410/411
3. US-402/403
4. US-440 فاز ۲ — توالی تاریخ فاکتور/وصول
