# RELEASE-NOTES — v33.22.0 (۱۴۰۵/۰۸/۱۱ — 2026-08-02)

## 🗄 P1-MySQL-WIRE — مسیر سینک به MySQL همان هاست سیم شد (به درخواست کارفرما — بدون تغییر سرور)

زمینه: ذخیره‌سازی عمومی (load_data/save_data) از قبل dual/mysql-aware بود، ولی مسیر سینکِ
۵۲ کلید سنگین (data_push/data_pull — تقریباً همهٔ ترافیک) فقط فایل‌محور بود و مهاجرت
۶‌مرحله‌ای migrate.php بدون این سیم‌کشی عملاً فقط «کپی‌سازی» بود. حالا سیم‌کشی کامل شد.

## رفتار بر اساس mode (ساختهٔ migrate.php — در cPanel همان هاست)

| mode | خواندن | نوشتن | کاربرد |
|---|---|---|---|
| **off** (پیش‌فرض، بدون کانفیگ) | فایل | فایل | **دقیقاً رفتار امروز** — پخش به هر محیطی امن است |
| **dual** | فایل (منبع) | فایل + آینهٔ MySQL (خطای DB بی‌صدا) | دورهٔ مهاجرت — راستی‌آزمایی قبل از سوییچ |
| **mysql** | **MySQL (منبع حقیقت)** + fallback فایل | فایل همیشه (بکاپ گرم) + MySQL با rev | پس از سوییچ نهایی — نوشتن اتمیک، خواندن از بافر DB |

## جزئیات مهم

- **کلاینت اصلاً تغییر نکرده** — پروتکل یکسان است (همهٔ رفتارها سرور-ساید).
- شکست نوشتن DB در mode=mysql: کل پاسخ پوش `ok:false` + `needRetry` می‌شود → meta.json نوشته نمی‌شود، قفل آزاد می‌شود و کلاینت همان صف را **idempotent** دوباره می‌فرستد (بدون انحراف rev).
- **اتصال MySQL و کانفیگ request-scoped کش شدند** — بدون این بهینه‌سازی، پوش دسته‌ای ۲۰کلیدی ۲۰ اتصال تازه می‌گشود.
- دفتر rev (meta.json) در همهٔ حالت‌ها دست‌نخورده و سبک می‌ماند؛ فایل‌ها طبق وعدهٔ راهنمای مهاجرت هرگز حذف نمی‌شوند.
- نکتهٔ مهندسی: هلپرها top-level نگهداری می‌شوند (تعریف داخل switch در PHP «شرطی» است و در caseها اجرا نمی‌شود) — چک رگرسیون دائمی در tester301.

## مراحل فعال‌سازی (تست استیجینگ ← پروداکشن)

۱) استیجینگ را به v33.22.0 به‌روز کنید (CRM بدون دیتابیس هم مثل امروز کار می‌کند — mode=off).
۲) در cPanel **همان هاست** یک دیتابیس MySQL بسازید (راهنما: `crm/MIGRATION-MYSQL-CPANEL-GUIDE-FA.md`).
۳) `https://…/api/migrate.php` را باز کنید و ۶ گام را به ترتیب انجام دهید: بکاپ ← جدول ← انتقال ← تطابق(checksum) ← dual ← سوییچ نهایی.
۴) تست: ورود/اضافه/حذف/همگام دو دستگاه + در mode=dual صحت آینهٔ DB.
۵) تأیید → مرج (در نشست) ← دیپلوی دستی ← همان ویزارد روی پروداکشن.

## آزمون
- tester301 جدید: **۲۰/۰** (سیم‌کشی push/pull، needRetry idempotent، کش اتصال/کانفیگ، top-level بودن هلپرها، پایداری رشته‌های legacy تسترها)
- رگرسیون کامل: ۲۹۶ تستر → **۲۴۰/۵۶ فایل و ۵۴۱۱/۸۴ چک = baseline + tester300+tester301** (صفر شکست جدید)
- audit: ✅ ۰ خطا | zip: ۵۵۸ ورودی (db-lib.php/crm.php به‌روز در zip)
