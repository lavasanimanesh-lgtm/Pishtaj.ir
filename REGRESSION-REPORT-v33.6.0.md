# گزارش رگرسیون و ممیزی نسخه v33.6.0

**تاریخ ممیزی:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**نسخه هدف:** v33.6.0
**ابزارهای سنجش:** `_tools/audit.py` + `_tools/build_sitemap.py` + ۲۸۲ تستر UAT (رگرسیون A/B با درخت پایه)

---

## 📊 خلاصه نتایج ممیزی و رگرسیون فنی

| بخش سنجش | وضعیت | نتیجه |
|---|:---:|---|
| **audit.py (گیت انتشار)** | ✅ **۰ خطا** | همهٔ ۱۰ بررسی PASS |
| **build_sitemap.py** | ✅ هم‌گام | ۵۲۰ URL |
| **سینتکس JS** | ✅ سبز | `node --check` روی همهٔ crm/*.js + sw.js + تسترهای اصلاح‌شده |
| **تستر جدید CHQ-PRINT** | ✅ سبز | tester287 (۳۰/۰) |
| **تسترهای اصلاح‌شده** | ✅ سبز | tester215 (۴۳/۰)، tester54 (۲۴/۰)، tester193 (۱۵/۰)، tester124 (۱۰/۰)، tester145 (۱۱/۰) |
| **کل مجموعهٔ UAT (A/B)** | 🟡 ۲۳۰ سبز / ۵۲ باز | اجرای کامل ۲۸۲ تستر؛ **صفر شکست جدید** نسبت به درخت پایه (d23a7a5) |
| **بستهٔ نصب دستی** | ✅ بازسازی | crm-update.zip با ۵۵۴ فایل (شامل cheque-print.js) |

---

## 🔍 روش راستی‌آزمایی «بدون رگرسیون» (A/B)

کل مجموعهٔ ۲۸۲ تستر روی **درخت پایه (d23a7a5 — قبل از تغییرات این ریلیز)** و سپس روی **وضعیت جدید (v33.6.0)** اجرا شد:

- شکست‌های جدید ناشی از این ریلیز: **۰** (پس از به‌روزرسانی عمدی انتظارات ۵ تستر — موارد زیر)
- سبزهای جدید: **tester287** (۳۰ تست) + tester124 و tester145 که انتظارات‌شان به واقعیت جدید (حذف باکس شخصی) به‌روز شد

### به‌روزرسانی‌های عمدی انتظارات (نه رگرسیون — مصوبهٔ کارفرما)
| تستر | دلیل تغییر |
|---|---|
| `tester54` | باکس چک از پنل یادآورها حذف شد → تست «هوک شدن باکس» به «هوک نشدن» تبدیل شد + تست پنل جدید |
| `tester124` | دکمهٔ «🤖 دستیار چک» از باکس شخصی حذف → دستیار فقط در هاب مالی بررسی می‌شود |
| `tester145` | فیلتر «لیست فعال» (مربوط به باکس حذف‌شده) → گارد یادآور/نقدینگی ثالث همچنان سنجیده می‌شود |
| `tester193` | گروه کالا و اسناد حالا شامل chqprint است |
| `tester215` | چاپ برگه از هاب مالی حذف و به ماژول جدید منتقل شد → بررسی‌ها روی cheque-print.js |

---

## 🚦 وضعیت گیت‌ها جهت انتشار

- **گیت ممیزی فنی:** ✅ سبز (audit.py = ۰ خطا)
- **گیت انتشار و سوابق:** ✅ تراز (RELEASE-NOTES-v33.6.0.md + REGRESSION-REPORT-v33.6.0.md)
- **گیت تست:** 🟡 ۲۳۰/۲۸۲ سبز؛ ۵۲ شکست باقی‌مانده **همگی از قبل موجود** (بدهی v15–v22 + عصر auth — جزئیات در HANDOFF)
- **گیت PR:** PR #11 همچنان باز است و تا پایان مهاجرت MySQL در main مرج نمی‌شود (تصمیم کارفرما)
