# RELEASE NOTES — v34.8.3 (۱۴۰۵/۰۶/۰۲ — 2026-08-24)

## تایپوگرافی و امضای مکاتبات

- چینش پیش‌فرض متن نامه و مسیر «متن آماده روی سربرگ» به **تراز دوطرفه (Justify)** تغییر کرد؛ راست‌چین، چپ‌چین و وسط‌چین همچنان قابل انتخاب‌اند.
- فونت انتخاب‌شده برای کل نامه اکنون علاوه بر بدنه، روی خطاب/گیرنده، سمت گیرنده، موضوع و بلوک نام و سمت امضاکننده نیز اعمال می‌شود.
- اندازهٔ فونت نام امضاکننده به‌صورت مستقل قابل تنظیم است و سمت امضاکننده دو واحد کوچک‌تر، با همان خانوادهٔ فونت، نمایش داده می‌شود.
- نام امضاکننده همیشه همراه سمت او جمع‌آوری و اعتبارسنجی می‌شود؛ برای کاربران سازمانی، پروفایل امضا در اولویت است و مشخصات کاربری fallback معتبر باقی می‌ماند.
- گزینهٔ **«سایر — نام و سمت دلخواه»** به انتخاب امضاکننده افزوده شد. این مسیر نام و سمت را اجباری می‌گیرد، شمارهٔ قطعی نامه را صادر می‌کند و سند را بدون اعلان یا snapshot امضای دیجیتال برای مهر و امضای فیزیکی ثبت می‌کند.
- پنج فونت مصوب مکاتبات بدون تغییر حفظ شدند: بی‌یاقوت، بی‌نازنین، وزیرمتن، ایران‌یکان و ایران‌سنس.

## مبنای پورسانت فروش

- مبنای درصد پورسانت از مبلغ کل فاکتور به **مبلغ پایهٔ فاکتور بدون ارزش افزوده** تغییر کرد.
- اثر مرجوعی حفظ شد: چون مبلغ مرجوعی از مبلغ کل ثبت می‌شود، همان نسبت از مبلغ پایه کسر می‌شود تا VAT هیچ‌گاه وارد مبنای پورسانت نشود.
- شرط آزادسازی پورسانت تغییر نکرده است: تمام مبلغ کل فاکتور/پرونده، شامل VAT، باید واقعاً تسویه شده باشد؛ وصول صرف مبلغ پایه پورسانت را آزاد نمی‌کند.
- خروجی موتور برای حسابرسی، فرمول `invoice_base_ex_vat_after_returns` را ثبت می‌کند و متن راهنمای هاب مالی و تنظیمات پورسانت با همین قاعده هماهنگ شد.
- بخش مطالبات همچنان صرفاً ریالی است و هیچ مسیر وصول یا محاسبهٔ ارزی بازگردانده نشده است.

## آزمون و جلوگیری از رگرسیون

- سناریوی عددی VAT/مرجوعی با پایهٔ ۱٬۰۰۰٬۰۰۰٬۰۰۰ ریال، VAT برابر ۱۰۰٬۰۰۰٬۰۰۰ ریال و مرجوعی ۲۲۰٬۰۰۰٬۰۰۰ ریال آزموده شد؛ مبنای پورسانت ۸۰۰٬۰۰۰٬۰۰۰ ریال و پورسانت ۲٪ برابر ۱۶٬۰۰۰٬۰۰۰ ریال است.
- همان سناریو ثابت می‌کند وصول ۱٬۰۰۰٬۰۰۰٬۰۰۰ ریال از جمع ۱٬۱۰۰٬۰۰۰٬۰۰۰ ریال، به‌دلیل تسویه‌نشدن VAT، پورسانت را آزاد نمی‌کند.
- مسیرهای runtime ثبت امضاکنندهٔ دلخواه، عدم اعلان دیجیتال، fallback نام/سمت سازمانی، چاپ نامه و چاپ متن آماده روی سربرگ با DOM شبیه‌سازی‌شده آزموده شدند.
- تست‌های lifecycle/مرجوعی **۳۷ موفق و صفر شکست** و تست‌های یکپارچهٔ مکاتبات/اسناد/OPEX **۵۰ موفق و صفر شکست** دارند.
- گیت کامل CI شامل نگهبان معماری و **۱۱۹ مجموعهٔ آزمون** با نتیجهٔ **۱۱۹ موفق و صفر شکست** اجرا شد.

## نسخه و cache

- نسخهٔ رسمی CRM از `v34.8.2` به **`v34.8.3`** ارتقا یافت.
- `VERSION.json`، نسخهٔ رابط CRM، service worker، manifest، صفحهٔ پاک‌سازی کش، shell و نسخهٔ سرویس sales-domain هم‌راستا شدند.
- cache-bust همهٔ دارایی‌های CRM و نام cache سرویس‌ورکر به `34.8.3` تغییر کرد تا مرورگر و PWA فایل‌های جدید را دریافت کنند.
