# RELEASE NOTES — v34.20.0 (سه رفع شکایت ماژول مدیریت سایت)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** tester565 (۲۳ سنجه) · **گیت:** 181 PASS / 0 FAIL · **PHP-لینت:** php-parser (llm.php) ✓

## الف) گروه «مدیریت سایت» — روشن شد که کجا بود

- دسکتاپ: سه‌گانه (سایت / سرچ کنسول / فرصت شغلی) از v34.13.0 فقط زیر سرگروه «مدیریت سایت» است — چیزی خارج گروه وجود ندارد (تستر تأیید می‌کند goPanel('cms'/'gsc'/'jobs') هرکدام فقط یک‌بار در کل سایدبار است).
- **چرا شما آن را ندیده‌اید:** این گروه در کامیت v34.13.0 ساخته شد (بررسی git: در v34.12.0 وجود نداشت). محیطی که نگاه کرده‌اید هنوز نسخٔ قدیمی‌تر دارد — با دیپلوی نسخه‌های 13 به بعد، سرچ کنسولِ مستقل و سایت/فرصت شغلیِ زیر «سیستم» حذف می‌شوند.
- جدید در این نسخه: **کشوی «بیشتر» موبایل** هم سه‌گانه را با سرگروه «🌐 مدیریت سایت» (تمام-عرض، جداکنندهٔ خط‌چین) نشان می‌دهد — قبلاً فهرست موبایل مسطح بود.

## ب) فرم «📄 صفحهٔ جدید» — ابزارها اضافه شد

- **شمارندهٔ زنده:** عنوان (×/۶۰ با بازهٔ ۳۰–۶۵)، توضیح (×/۱۶۰ با بازهٔ ۷۰–۱۶۵)، بدنه (کلمه + حرف + گیت ۲۰۰) — رنگی، هم‌زمان با تایپ و بلافاصله پس از بازیابی پیش‌نویس.
- **نوار ابزار ۱۰ دکمه‌ای روی بدنه:** H2 · H3 · ¶ · B (با احترام به انتخاب کاربر و بازگشت focus) · لیست نقطه‌ای/فهرست شماره‌دار (تبدیل هر خط به <li>) · جدول ۳×۳ · لینک · تصویر (با escape نقل‌قول) · نقل‌قول.
- **👁 پیش‌نمایش:** مودال با slug + متا (title/desc و طولشان) + H1 + بدنهٔ رندرشده.
- **✍️ گسترش متن با AI:** `seo_expand` روی متن فعلی (گیت ≥۱۰۰ حرف + تأیید) — متن کامل‌تر (استانداردها/معیار انتخاب/چک‌لیست خرید)، فیلدهای خالی هم پر می‌شوند، فهرست افزوده‌ها نمایش داده می‌شود.
- هر ابزار بلافاصله پیش‌نویس را ذخیره می‌کند (A10).

## ج) RCA و رفع «خروجی AI ساختار JSON معتبر ندارد»

**ریشه:** پاسخ‌های بلند (مثل `seo_product` با سقف ۱۶۰۰ توکن) در سقف توکن **بریده** می‌شوند → JSON ناقص → تجزیه ناموفق. retry قبلی فقط وقتی متن کاملاً خالی بود فعال می‌شد.

**راه‌حل سه‌لایه در llm.php:**
1. **salvage:** تجزیهٔ مستقیم → استخراج `{..}` → حذف کاماهای انتهایی و تلاش دوباره.
2. **retry دومقطعه:** یک‌بار با **دو برابر توکن** + دور زدن کش + تلنگر «COMPLETE compact valid JSON».
3. فقط بعد از آن خطا (با پیام قبلی).
- مسیر جدید `llm_call_json` روی سه اکشن پرریسک: **seo_product · seo_article · seo_meta** (AI محصول، AI صفحه، متای گروهی).

## فایل‌های کلیدی

`api/llm.php` (llm_json_salvage + llm_call_json + out_json) · `crm/cms.js` (ابزارهای فرم صفحه) · `crm/mobilenav.js` (سرگروه موبایل) · `_tools/uat/tester565-v34.20.0-cms-complaints.js`
