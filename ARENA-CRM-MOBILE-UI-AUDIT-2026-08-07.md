# ممیزی عمیق رابط کاربری موبایل CRM

**تاریخ:** ۱۶ مرداد ۱۴۰۵ — ۷ اوت ۲۰۲۶
**دامنه:** `crm/` در وضعیت فعلی شاخه، با نسخهٔ اعلام‌شدهٔ برنامه `v34.1.0`
**نوع خروجی:** ارزیابی UX/UI، رفتار تعاملی، دسترس‌پذیری، پاسخ‌گویی، PWA و کارایی موبایل — **بدون اعمال اصلاح کد**

---

## خلاصهٔ مدیریتی

رابط موبایل نسبت به نسخه‌های قدیمی‌تر، چند پایهٔ درست دارد: viewport مناسب، فونت محلی، ورودی ۱۶px برای جلوگیری از zoom iOS، safe-area پایین، نوار ناوبری پایین، پشتیبانی از History API و PWA. با این حال، در وضعیت فعلی سه نقص بحرانی وجود دارد که مستقیماً عملیات CRM را در موبایل مخدوش می‌کند:

1. **کنترل‌های مودال به سه دکمهٔ رنگی بدون معنا تبدیل شده‌اند**؛ در آزمون واقعی هر کنترل ۹۸×۴۶px شد و عنوان مودال را پوشاند.
2. **تبدیل عمومی جدول به کارت، نام ستون‌ها را حذف کرده و برخی عملیات متنی را تقریباً نامرئی می‌کند**؛ برای داده‌های مالی/فروش خطر خطای عملیاتی دارد.
3. **ناوبری پایین وضعیت پنل فعال سایدبار را پاک می‌کند**؛ در نتیجه refresh ناشی از sync یا تغییر داده، روی پنل فعال موبایل انجام نمی‌شود و کاربر دادهٔ کهنه می‌بیند.

علاوه بر این، toast و بنر sync روی نوار پایین می‌افتند، حالت landscape گوشی به نسخهٔ شبه‌دسکتاپ ۵۶px برمی‌گردد، بار اول صفحه سنگین است، و بخش بزرگی از تعامل‌ها هنوز برای لمس، focus و دسترس‌پذیری طراحی نشده‌اند.

**شمار یافته‌ها:** ۳ مورد P0، ۹ مورد P1، ۸ مورد P2 و ۳ مورد P3؛ در مجموع **۲۳ یافتهٔ مستند**.

### امتیاز وضعیت فعلی

| محور | امتیاز | جمع‌بندی |
|---|---:|---|
| ناوبری و تداوم کار | 5/10 | Bottom bar خوب است، اما state پنل و deep-link ناقص است. |
| خوانایی و عملیات داده | 3/10 | کارت‌سازی جدول و icon-only شدن دکمه‌ها در چند مسیر معنای عملیات را از بین می‌برد. |
| مودال و فرم | 3/10 | فرم تک‌ستونه مفید است؛ کنترل‌های مودال و focus بحرانی‌اند. |
| لمس و پاسخ‌گویی | 5/10 | تب‌ها مناسب‌اند؛ کنترل‌های 34/36px و landscape مشکل‌دار است. |
| دسترس‌پذیری | 3/10 | بخشی از dialogx خوب است، اما الگوی غالب مودال و دکمه‌ها فاقد semantics/focus است. |
| کارایی شروع کار | 3/10 | FCP زود است، ولی ناوبری موبایل در تست شبکهٔ کند با تأخیر معنادار ظاهر شد. |
| PWA / آفلاین | 5/10 | SW و cache وجود دارد، اما پوشش cache و نسخه‌ها یکدست نیست. |

### اولویت فوری

| اولویت | کار | دلیل |
|---|---|---|
| P0-1 | جدا کردن استایل کنترل‌های `.mx-dot` از قانون عمومی `button` | مودال‌های همهٔ ماژول‌ها اکنون از نظر بصری و عملیاتی آسیب دیده‌اند. |
| P0-2 | بازطراحی card-table با label واقعی و حذف `::first-letter` | جلوگیری از اشتباه در داده/عملیات، به‌خصوص مالی و چک. |
| P0-3 | منبع واحد `activePanel` و اصلاح refresh بعد از sync | جلوگیری از نمایش دادهٔ کهنه در موبایل. |
| P1-1 | بردن toast / banner بالای bottom-nav | رفع مسدود شدن ناوبری در شروع و هنگام آفلاین. |
| P1-2 | اصلاح breakpoint landscape و bar/header | تلفن چرخیده اکنون به UI نامناسب دسکتاپ سقوط می‌کند. |
| P1-3 | lazy-load کردن ماژول‌ها و کوچک‌سازی اولین بار | نوار ناوبری در شبکهٔ موبایل دیر آماده می‌شود. |
| P1-4 | رفع clipping FAB پیشنهاد و استانداردسازی actionهای RFQ | دکمهٔ کلیدی پیشنهاد نیمه‌نمایان است و actionهای درخواست نشانهٔ قابل اتکا ندارند. |

---

## روش و دامنهٔ آزمون

### بررسی‌های انجام‌شده

- بازبینی کامل `crm/index.html`، `mobilenav.js`، `modalx.js`، `sync.js`، `tour.js`، `ui-kit.js`، `sw.js` و ماژول‌های UI مرتبط.
- اجرای واقعی CRM در مرورگر Chromium با viewport لمسی و RTL در اندازه‌های:
  - `320×568`، `360×800`، `375×667`، `390×844`، `412×915`؛
  - `844×390` (landscape گوشی).
- آزمایش ناوبری پایین، کشوی «سایر»، modal، table-card، History/Back، refresh داده و ۲۱ route اصلی با fixture دادهٔ نمونه.
- آزمون دسترس‌پذیری با axe (WCAG 2 A/AA و 2.1 A/AA) و ابزار contrast داخلی برنامه.
- آزمون بارگذاری تحت شبیه‌سازی شبکهٔ موبایل با latency=150ms و download=250KiB/s (حدود 2Mbps).

### نکتهٔ اعتبار نتایج

- موارد دارای برچسب **تأییدشده در اجرا** در مرورگر و روی کد فعلی بازتولید شده‌اند.
- موارد دارای برچسب **ریسکِ کد/نیازمند دستگاه واقعی** از منبع قطعی‌اند، اما برای iOS Safari، keyboard نرم‌افزاری، notch و شبکهٔ واقعی ایران باید UAT فیزیکی هم انجام شود.
- آزمون routeها با دادهٔ نمونه هیچ `pageerror` جدیدی در ۲۱ پنل اصلی نشان نداد؛ این **به معنی تأیید تمام workflowهای کسب‌وکاری یا دادهٔ حجیم نیست**.

### نکات مثبت تأییدشده

- در portraitهای 320 تا 412px، overflow افقی سراسری document مشاهده نشد.
- نوار پایین در portrait ارتفاع 74px و تب‌های اصلی قابل استفاده دارد.
- Back مرورگر بین پنل‌های بازشده کار می‌کند (`#off → #dash`).
- safe-area پایین، `viewport-fit=cover`، `prefers-reduced-motion` برای motion ناوبری و ورودی 16px iOS لحاظ شده‌اند.
- modalx لایهٔ مودال را بالاتر از bottom-nav می‌برد؛ بنابراین مشکل «نوار پایین روی backdrop مودال» در کد فعلی بازتولید نشد.

---

## فهرست کامل یافته‌ها

### P0 — بحرانی؛ پیش از توسعهٔ feature جدید باید رفع شوند

### MOB-001 — کنترل‌های مودال به نوارهای رنگی بی‌معنا تبدیل می‌شوند

**وضعیت:** تأییدشده در اجرا
**شدت:** P0
**محل ریشه:** `crm/index.html:28-57`، `crm/modalx.js:40-46` و `86-90`

**آنچه رخ می‌دهد**

قانون عمومی موبایل روی تمام `button`ها اعمال می‌شود (`min-height:46px !important`، `padding:10px 14px !important`) و همچنین هر `button` داخل `.md` را تمام‌عرض می‌کند. این دو قانون، طراحی 13px کنترل‌های مک‌استایل `.mx-dot` را می‌شکنند.

در اجرای 390×844 برای یک مودال استاندارد:

- هر سه کنترل close / minimize / fullscreen به **98×46px** تبدیل شد؛
- رنگ‌ها به سه کپسول بزرگ قرمز، زرد و سبز تبدیل شدند؛
- علامت‌های داخل آن‌ها فقط در `:hover` ظاهر می‌شوند؛ در touch عملاً معنایی ندارند؛
- کنترل‌ها تقریباً تمام عرض بالای مودال را گرفتند و عنوان مودال به چند حرف بریده شد.

**اثر کسب‌وکاری/UX**

کاربر نمی‌فهمد کدام دکمه بستن، کوچک‌سازی یا تمام‌صفحه است؛ احتمال بستن ناخواستهٔ فرم و از دست رفتن جریان کاری بالا می‌رود. این ایراد در هر مودال مجهز به modalx رخ می‌دهد، نه یک ماژول خاص.

**بازسازی**

1. در موبایل یک مودال ساده مانند «تأمین‌کننده جدید» باز کنید.
2. بالای مودال را ببینید: سه کنترل بزرگ رنگی، بدون نماد قابل‌دیدن، عنوان را پوشانده‌اند.

**اصلاح پیشنهادی**

- قانون عمومی `button` را حذف یا فقط به کلاس‌های عملیاتی مجاز scope کنید؛ هرگز bare `button` را در breakpoint موبایل target نکنید.
- `.mx-dot` را از `.md button { width:100% }` صریحاً مستثنا کنید.
- اگر touch target 44px لازم است، wrapper نامرئی 44px بسازید و دایرهٔ 13px را با `::before` در مرکز نگه دارید؛ نه اینکه خود دایره را به کپسول 98px تبدیل کنید.
- برای هر کنترل نام قابل لمس/نمایش بدهید (مثلاً منوی «بستن / کوچک‌سازی / تمام‌صفحه»)؛ صرف رنگ و hover راه‌حل موبایل نیست.

---

### MOB-002 — Card view جدول‌ها نام ستون را حذف می‌کند و عمل‌های متنی را نامرئی/مبهم می‌سازد

**وضعیت:** تأییدشده در اجرا و منبع
**شدت:** P0
**محل ریشه:** `crm/index.html:73-106` و `141-174`

**آنچه رخ می‌دهد**

برای همهٔ `.tb2`ها، `thead tr` در `top:-9999px` پنهان و همهٔ `td`ها به flex تبدیل می‌شوند. اما هیچ `data-label` یا `td::before { content: ... }` وجود ندارد.

- در اسکن منبع، **74** استفادهٔ table-card و **صفر** استفاده از `data-label` پیدا شد.
- در تست زنده، `thead` ارتفاع صفر داشت، `td::before` مقدار `none` و سلول فقط مقدار خام را نشان می‌داد.
- در نتیجه در کارت موبایل کاربر می‌بیند: `RFQ-001`، مبلغ، تاریخ یا وضعیت؛ اما نمی‌داند هر مقدار متعلق به کدام ستون است.

هم‌زمان `.tb2 .ba` به `font-size:0` و 36×36px تبدیل می‌شود و فقط `::first-letter` را نشان می‌دهد. بنابراین:

- `ابطال` به یک «ا» یا دکمهٔ ظاهراً خالی تبدیل می‌شود؛
- `باز کردن پیشنهادها`، `گردش حساب` و موارد مشابه قابل تشخیص نیستند؛
- دکمه‌های بدون emoji اولیه یا با `+` اولیه، آیکون معنی‌دار ندارند.

نمونه‌های متنی واقعی `.ba` در کد: `ai-workbench.js:638`، `cheque-panel.js:82,130`، `supplier-finance.js:273,275,736` و `working-capital.js:255`.

**اثر کسب‌وکاری/UX**

این مسئله در جدول‌های مالی، فاکتور، چک، پیشنهاد، گردش حساب و گزارش‌ها می‌تواند باعث انتخاب/حذف/ابطال اشتباه شود. این فقط مسئلهٔ زیبایی نیست؛ زمینهٔ داده و معنای action حذف می‌شود.

**اصلاح پیشنهادی**

1. table-card را فقط در componentهایی اعمال کنید که واقعاً schema مشخص دارند؛ selector سراسری `.tb2` خطرناک است.
2. هنگام ساخت هر `td`، `data-label="شماره درخواست"` یا HTML ساخت‌یافتهٔ label/value اضافه کنید.
3. در CSS: `td::before { content: attr(data-label); ... }` و برای ستون عملیات label صریح بگذارید.
4. icon-only را فقط با markup استاندارد `<span class="ic">…</span><span class="lb">…</span>` اجرا کنید؛ به `::first-letter` تکیه نکنید.
5. برای actionهای کارت، حداقل 44×44px و `aria-label`/label قابل دیدن لازم است.

---

### MOB-003 — پس از ناوبری پایین، refresh sync روی پنل فعال کار نمی‌کند و دادهٔ کهنه می‌ماند

**وضعیت:** تأییدشده در اجرا با دادهٔ واقعی fixture
**شدت:** P0
**محل ریشه:** `crm/mobilenav.js:91-96`، `crm/index.html:787-795`، `crm/sync.js:416-440`

**زنجیرهٔ خطا**

1. `ptfMnvGo(id)` در bottom-nav، `goPanel(id)` را **بدون** `btn` سایدبار صدا می‌زند.
2. `goPanel` همهٔ `.sb-i.act` را پاک می‌کند و فقط در صورت وجود `btn` یک مورد را فعال می‌کند.
3. `sync.js` برای refresh پنل فعلی فقط `document.querySelector('.sb-i.act')` را می‌خواند؛ اگر null باشد، فوراً `false` برمی‌گرداند.

**بازسازی تأییدشده**

- با bottom-nav وارد «مشتریان» شدم؛ sidebar active نداشت.
- دادهٔ مشتری در storage به «نام جدید مشتری» تغییر داده شد.
- پس از scheduler sync، پنل همچنان «نام قدیمی مشتری» را نشان داد.
- `ptfRefreshCurrentPanel()` مقدار `false` داد.
- فقط پس از ورود مجدد با `goPanelByName('cust')` دادهٔ جدید دیده شد.

**اثر کسب‌وکاری/UX**

به‌روزرسانی از دستگاه دیگر، sync، update پس از ذخیرهٔ بعضی workflowها و تغییرهای server-driven ممکن است تا زمان خروج/ورود مجدد به پنل دیده نشوند. برای CRM چندکاربره، نمایش وضعیت/مانده/فاکتور کهنه خطرناک است.

**اصلاح پیشنهادی**

- منبع حقیقت مستقل بسازید: `window.ptfActivePanel = id` در ابتدای همهٔ مسیرهای navigation.
- `refreshCurrentPanel()` باید از activePanel استفاده کند، نه از کلاس ظاهری سایدبار مخفی.
- در `goPanel`، اگر `btn` ارسال نشد، button متناظر را از روی `onclick` پیدا و active کنید؛ یا `ptfMnvGo` همان button را پاس بدهد.
- تست regression اجباری: تغییر داده در حالی که کاربر با bottom-nav روی `cust`, `off`, `petty`, `rfq` است باید بدون navigate مجدد render شود.

---

## P1 — زیاد؛ در اولین اسپرینت اصلاح شوند

### MOB-004 — Toast و بنر همگام‌سازی bottom-nav را می‌پوشانند

**وضعیت:** تأییدشده در اجرا و screenshot
**شدت:** P1
**محل ریشه:** `crm/ui-kit.js:44` و `crm/sync.js:457-486`

- toast با `bottom:20px; z-index:10000` ساخته می‌شود؛ bottom-nav در `z-index:1500` و پایین صفحه است.
- در اجرای 390×844، toast شروع کار با ارتفاع حدود **106px** روی چند تب نوار پایین افتاد و لمس را intercept کرد.
- بنر `#ptfUnsavedBanner` نیز `bottom:0; z-index:9999` دارد؛ هنگام آفلاین/warn می‌تواند نوار ناوبری را تا زمان sync بپوشاند.

**اصلاح پیشنهادی:** در موبایل toast و banner را در `bottom: calc(74px + env(safe-area-inset-bottom) + 12px)` قرار دهید؛ banner باید فضای bottom-nav را reserve کند یا به inline status تبدیل شود. پیام صرفاً اطلاع‌رسان باید `pointer-events:none` باشد.

---

### MOB-005 — چرخش landscape گوشی CRM را به layout نیمه‌دسکتاپ غیرقابل‌استفاده می‌برد

**وضعیت:** تأییدشده در اجرا و screenshot
**شدت:** P1
**محل ریشه:** breakpointهای `max-width:768px` در `index.html`/`mobilenav.js` در برابر `max-width:900px` در `index.html:321+` و `theme.js:49`

در viewport رایج گوشی landscape یعنی 844×390:

- `#mnvBar` پنهان شد؛
- سایدبار icon-only با عرض 56px فعال شد؛
- header به 99px رسید؛
- تعداد زیادی action عرض کمتر از 44px داشت و بخش مهمی از محتوا در ارتفاع 390px از دید خارج شد.

از آن‌جا که `manifest.json` orientation را `any` اعلام کرده، این حالت عملاً supported معرفی شده است.

**اصلاح پیشنهادی**

- breakpoint را با **عرض و ارتفاع** تعیین کنید: مثلاً `(max-width:900px) and (max-height:600px)` همچنان الگوی mobile bottom-nav یا landscape-specific UI را نگه دارد.
- برای landscape یک header 48px، bottom-nav فشرده و content padding مستقل تعریف کنید.
- قبل از انتشار روی Chrome Android و Safari iOS در landscape UAT شود.

---

### MOB-006 — دکمه‌های عملیاتی icon-only معنای action را از دست می‌دهند

**وضعیت:** تأییدشده در اجرای پنل پیشنهادها
**شدت:** P1
**محل ریشه:** `crm/index.html:141-174`

در پنل پیشنهادها، چهار action واقعی زیر با `font-size:0`، بدون `title` و بدون `aria-label` رندر شدند:

- `+ پیشنهاد فنی`
- `+ پیشنهاد مالی`
- `تطبیق سراسری خرید`
- `فیلتر و خروجی`

الگوی `::first-letter` برای دو action اول فقط «+» را نگه می‌دارد؛ کاربر نمی‌تواند Technical و Financial را از هم تفکیک کند. در action سوم به‌جای آیکون یک حرف فارسی نمایش داده می‌شود.

**اصلاح پیشنهادی**

- کلیدهای پرریسک (ایجاد، حذف، تأیید، مالی، خروجی) را icon-only نکنید؛ label کوتاه دوکلمه‌ای نگه دارید.
- اگر icon-only ضروری است، markup کنترل‌شده با SVG/emoji مشخص، `aria-label`، عنوان touch-friendly و action sheet/tooltip قابل لمس استفاده شود.
- استاندارد محتوا: هر action باید `icon`, `shortLabel`, `accessibleName` داشته باشد؛ متن آزاد HTML را با CSS حدس نزنید.

---

### MOB-007 — بیشتر مودال‌ها semantics، focus و Escape استاندارد ندارند

**وضعیت:** تأییدشده در اجرای modal پایه و اسکن منبع
**شدت:** P1 برای accessibility و کاربران keyboard/assistive tech
**محل ریشه:** الگوی `.md-b` در `crm/index.html:316-319` و دست‌کم 324 occurrence در 55 فایل

در modal پایهٔ اجراشده:

- `role="dialog"` و `aria-modal="true"` وجود نداشت؛
- focus روی `BODY` ماند، نه اولین input یا عنوان؛
- `Escape` مودال را نبست؛
- focus trap و بازگردانی focus به trigger وجود ندارد.

`dialogx.js` در بخشی از مسیرها focus trap دارد، اما الگوی غالب `.md-b` و `ui-kit` هنوز یک component دسترس‌پذیر واحد نیستند.

**اصلاح پیشنهادی:** یک `ModalManager` واحد بسازید: role/dialog، aria-labelledby، focus اولیه، focus trap، Escape، click-outside policy روشن، restore-focus، scroll lock و stack management. modalx فقط باید روی همین component سوار شود.

---

### MOB-008 — راه‌اندازی نوار ناوبری در شبکهٔ موبایل دیر است و صفحه loading state ندارد

**وضعیت:** تأییدشده در تست آزمایشگاهی
**شدت:** P1
**محل ریشه:** `crm/index.html:2514-2602`، `crm/mobilenav.js:230-250`

**اعداد فعلی**

- 88 تگ script: 87 `defer` و 1 `async`؛
- 89 فایل JS با حجم خام **3,532,096 bytes** (حدود 3.37MiB)؛
- `mobilenav.js` نزدیک انتهای لیست scriptهاست؛
- در شبیه‌سازی 150ms latency و 250KiB/s download، FCP حدود **344ms** بود اما `#mnvBar` پس از **13.4 ثانیه** آماده شد.

یعنی کاربر صفحه را می‌بیند ولی مسیر اصلی navigation هنوز آماده نیست. تست روی HTTP محلی و بدون فشرده‌سازی انجام شده، بنابراین زمان production دقیقاً همین عدد نیست؛ اما ترتیب وابستگی و نبود readiness/loading state قطعی است.

**اصلاح پیشنهادی**

1. shell موبایل، auth، navigation و dashboard را core bundle کوچک و زودهنگام کنید.
2. ماژول‌های نادر (چاپ چک، archive، fiscal، AI، report، backup و...) را on-demand load کنید.
3. تا آماده شدن navigation، skeleton یا bottom-nav disabled با پیام «در حال آماده‌سازی» نمایش دهید.
4. بودجهٔ عملکرد تعیین کنید: navigation قابل لمس زیر 3s در Fast 3G و زیر 5s در 2Mbps.

---

### MOB-009 — PWA cache، query version و نسخهٔ برنامه با هم همگام نیستند

**وضعیت:** تأییدشده در منبع
**شدت:** P1 (ریسک stale/offline UI)

| مورد | وضعیت فعلی |
|---|---|
| نسخهٔ runtime | `VER = v34.1.0` در `index.html:758` |
| query 87 script | `?v=34.0.21-alpha` |
| query یک script | `offer-rial-convert.js?v=34.2.4` |
| cache SW | `ptf-crm-v34.1.0` |
| فایل shell جاافتاده | `offer-rial-convert.js` در `sw.js` precache نیست |
| کلید cache | SW فایل‌های بدون query را precache می‌کند، اما صفحه URLهای queryدار درخواست می‌دهد |

اثر احتمالی: دانلود موازی/تکراری در نصب اول، اختلاف cache در refresh آفلاین، و دشواری ردیابی اینکه کاربر دقیقاً کدام UI را می‌بیند.

**اصلاح پیشنهادی:** یک manifest تولیدی واحد (build hash) برای HTML، SW و تمام assetها استفاده کنید؛ SW را از manifest بسازید؛ query دستی alpha/future را حذف کنید؛ تست offline cold/warm و upgrade را در CI اضافه کنید.

---

### MOB-010 — Deep link به پنل کار نمی‌کند؛ URL کاربر به داشبورد بازنویسی می‌شود

**وضعیت:** تأییدشده در اجرا
**شدت:** P1/P2 بسته به نیاز اشتراک‌گذاری workflow
**محل ریشه:** `crm/index.html:787-795` و bootstrap `loadAll()`

ورود مستقیم به `crm/index.html#off` پس از آماده شدن برنامه به `#dash` و عنوان «داشبورد» تبدیل شد. History Back بین پنل‌های داخل برنامه درست کار کرد، اما deep-link اولیه خوانده نمی‌شود.

**اثر:** لینک از پیامک، notification، bookmark یا ادامهٔ کار پس از reload، کاربر را به پنل مورد انتظار نمی‌برد.

**اصلاح پیشنهادی:** در bootstrap، hash معتبر را parse و با `history.replaceState` به‌عنوان state اولیه ثبت کنید؛ فقط اگر hash نامعتبر است dashboard را باز کنید.

---

### MOB-022 — FAB «پیشنهاد» نصفه دیده می‌شود؛ دایره توسط parent بریده می‌شود

**وضعیت:** گزارش کاربر + ریشهٔ CSS تأییدشده
**شدت:** P1
**محل ریشه:** `crm/index.html:44-57` و `crm/mobilenav.js:160-172`

FAB پیشنهاد به‌صورت یک دایرهٔ 54px با `top:-26px` طراحی شده است تا از بالای نوار پایین بیرون بزند. اما قانون عمومی موبایل روی تمام `button`ها `overflow:hidden !important` اعمال می‌کند. از آن‌جا که خود `.mnv-fabwrap` یک `button` است، قانون بعدی `.mnv-fabwrap{overflow:visible}` در `mobilenav.js` به‌دلیل نداشتن `!important` برنده نمی‌شود.

**نتیجه:** بخش بیرون‌زدهٔ FAB clip می‌شود و دکمهٔ «پیشنهاد» به شکل نیم‌دایره دیده می‌شود؛ دقیقاً همان مورد گزارش‌شده توسط کاربر. هرچند label پایین باقی می‌ماند، primary action بصری ناقص و غیرحرفه‌ای است.

**اصلاح پیشنهادی:**

```css
@media (max-width:768px) {
  #mnvBar .mnv-fabwrap { overflow: visible !important; }
  #mnvBar .mnv-fab { overflow: visible; }
}
```

راه‌حل پایدارتر، حذف selector عمومی `button` از قانون overflow است. پس از اصلاح، در 320/390px و safe-area آیفون باید تمام دایره، ring سفید و shadow بدون clip دیده شوند.

---

### MOB-023 — actionهای زیر ردیف درخواست، قرارداد آیکون یکدست و قابل اتکا ندارند

**وضعیت:** گزارش کاربر + ریشهٔ منبع تأییدشده
**شدت:** P1
**محل ریشه:** `crm/bridge.js:888-890`، `crm/inqreader.js:548-580` و `crm/index.html:162-174`

در ردیف درخواست دو الگوی ناسازگار هم‌زمان استفاده می‌شود:

- actionهای پایهٔ مشاهده/ویرایش/ارجاع با `.bt` و emoji داخل HTML ساخته می‌شوند؛
- actionهای افزوده‌شده زیر ردیف (مثلاً مشاهده پروژه، مدیریت پیوست و ویرایش) با `.ba` و متن آزاد ساخته می‌شوند.

قانون عمومی موبایل برای `.tb2 .ba`، `font-size:0` دارد و برای نمایش icon فقط به `::first-letter` متکی است. این روش برای emoji، نمادهایی مثل `✎`، متن فارسی و دکمه‌هایی که icon ابتدای متن ندارند قابل اعتماد نیست؛ بنابراین بعضی actionهای زیر ردیف بدون نشانهٔ قابل‌دیدن یا فقط با یک حرف مبهم دیده می‌شوند. همچنین actionهای پایه عرض inline 32px دارند که از target مناسب لمس کوچک‌تر است.

**اصلاح پیشنهادی:**

1. هر action درخواست را با component واحد بسازید: `icon`, `shortLabel`, `ariaLabel`, `handler`.
2. از markup صریح استفاده کنید، نه `::first-letter`:
   ```html
   <button class="row-action" aria-label="ویرایش درخواست">
     <span class="ic" aria-hidden="true">✏️</span><span class="lb">ویرایش</span>
   </button>
   ```
3. برای actionهای primary زیر ردیف، icon همیشه visible و hit-area حداقل 44×44px باشد؛ label می‌تواند در عرض کم به bottom-sheet یا tooltip قابل لمس منتقل شود.
4. جدول درخواست با داده‌های دارای/بدون پیشنهاد، برنده، پیوست و ارجاع در 320px regression تصویری شود.

---

## P2 — متوسط؛ در برنامهٔ اصلاح UX قرار گیرند

### MOB-011 — دو جستجوی هم‌شکل با رفتار متفاوت در header

**وضعیت:** تأییدشده در منبع/portrait
**محل:** `crm/index.html:412` و `crm/theme.js:110`

یک آیکون ذره‌بین command palette سراسری را باز می‌کند و ذره‌بین دیگر فقط نخستین text input پنل را focus می‌کند. در موبایل هر دو icon-only هستند؛ کاربر تفاوت را نمی‌فهمد و دومی ممکن است در پنلی بدون input هیچ کاری نکند.

**پیشنهاد:** فقط یک action «جستجوی سراسری» نگه دارید؛ جستجوی داخل پنل را داخل همان پنل و با placeholder روشن نشان دهید.

---

### MOB-012 — touch targetهای header و table کمتر از حد توصیه‌شده‌اند

**وضعیت:** تأییدشده در اجرا
**محل:** `mobilenav.js:184-190`، `index.html:163-174`

- command search و header iconها: عرض **34px**؛
- actionهای table: **36×36px**؛
- در landscape، سایدبار icon-only تا **39px** عرض دارد.

حد پیشنهادی عملی برای لمس 44×44 CSS px است. کوچک بودن width به‌ویژه برای کاربران دستکش‌پوش، سالمند یا حرکت در خودرو مشکل ایجاد می‌کند.

**پیشنهاد:** hit-area را 44×44 نگه دارید، حتی اگر glyph کوچک‌تر است؛ فاصلهٔ بین targetها حداقل 8px باشد.

---

### MOB-013 — auto-tour کل صفحه را قفل می‌کند و با toast شروع کار رقابت دارد

**وضعیت:** تأییدشده در اجرا
**محل:** `crm/tour.js:170-257`

حدود 2.5 ثانیه پس از ورود جدید، `#ptfTourMnvDim` با `z-index:5000` کل viewport را intercept می‌کند. در همان شروع ممکن است toast sync نیز روی bottom-nav باشد. تور برای کاربر جدید مفید است، اما در CRM عملیاتی نباید بدون انتخاب کاربر مانع استفاده شود؛ با پاک شدن storage یا دستگاه جدید نیز دوباره اجرا می‌شود.

**پیشنهاد:** تور را به banner غیرمسدودکننده یا CTA «معرفی سریع» تبدیل کنید؛ فقط پس از اولین تعامل یا با opt-in نشان دهید؛ toast را پیش از tour suppress کنید.

---

### MOB-014 — native alert/confirm/prompt هنوز مسیر غالب تعامل است

**وضعیت:** تأییدشده در اسکن منبع
**حجم مسئله:** 747 `alert`، 144 `confirm` و 34 `prompt` در منبع CRM

نتیجه: ظاهر/ترتیب/زبان/دسترسی confirmationها بین browserها یکدست نیست، متن بلند روی موبایل آزاردهنده است و user context فرم حفظ نمی‌شود. وجود `dialogx.js` و `ui-kit.js` کافی نیست زیرا مهاجرت کامل نشده است.

**پیشنهاد:** inventory actionهای destructive/financial تهیه کنید و به‌ترتیب P0 آن‌ها را به ModalManager جدید منتقل کنید؛ native dialog فقط fallback باشد.

---

### MOB-015 — contrast در چند جزء زیر حد WCAG گزارش شده است

**وضعیت:** تأییدشده با axe و diagnostic داخلی
**نتیجه axe در light:** 5 violation جدی color-contrast؛ از جمله `#fxTicker`، `#mydayBox`، `#ptfFunnelBox` و `summary`.

در dark mode، diagnostic داخلی برای labelهای tab «کارتابل»، «دستیار»، «سایر» نسبت 4.41 (زیر 4.5) گزارش کرد. گزارش 1.44 برای متن toast در diagnostic داخلی به علت محاسبهٔ background شفاف نیازمند تأیید دستی است؛ آن را defect قطعی محسوب نکرده‌ام.

**پیشنهاد:** tokenهای رنگی را با تست contrast خودکار در CI کنترل کنید؛ متن subtle را حداقل 4.5:1 و UI componentها را حداقل 3:1 نگه دارید.

---

### MOB-016 — معماری CSS موبایل شکننده و علت ریشه‌ای regressionهای مکرر است

**وضعیت:** تأییدشده در منبع

| شاخص | مقدار |
|---|---:|
| `!important` در CRM | 475 |
| فایل‌هایی که style runtime تزریق می‌کنند | 38 |
| inline `onclick` | 1,258 |
| الگوی `.md-b` | 324 occurrence در 55 فایل |
| gridهای چندستونه hard-coded | 21 مورد شناسایی‌شده |

قانون‌های global مانند `button`, `.md button`, `.tb2 td` و style injectionهایی که به ترتیب load وابسته‌اند، باعث شده‌اند اصلاح یک نقطه (مثل touch target) کنترل مودال، table و header را هم تخریب کند.

**پیشنهاد:** `crm/mobile.css` واحد با layerهای مشخص بسازید؛ CSS module/component token تعریف کنید؛ قوانین generic را حذف و قرارداد component (`Button`, `Modal`, `DataCard`, `Toolbar`) ایجاد کنید؛ `!important` را در هر PR کاهش دهید.

---

### MOB-017 — فرم‌های غیر `.fr` ممکن است در عرض کم دو/چهارستونه بمانند

**وضعیت:** ریسکِ کد؛ نیازمند UAT فیزیکی کامل

قانون responsive فقط `.md .fr` را تک‌ستونه می‌کند، ولی حداقل 21 grid چندستونه hard-coded در ماژول‌هایی چون AI Workbench، cheque-print، date-kit، offers، petty، projects و surplus وجود دارد. برخی inputها نیز `min-width` ثابت 100 تا 240px دارند و parent مودال `overflow-x:hidden` است؛ بنابراین خطر clip شدن بدون امکان scroll وجود دارد.

**پیشنهاد:** یک selector/utility رسمی responsive grid داشته باشید و تمام inline gridها را به آن migrate کنید؛ آزمون 320px با keyboard باز برای هر فرم الزامی باشد.

---

### MOB-018 — iOS safe-area بالا و keyboard viewport کامل پوشش داده نشده‌اند

**وضعیت:** ریسکِ کد؛ باید روی Safari iPhone واقعی تأیید شود

`viewport-fit=cover` و safe-area پایین خوب است، ولی header sticky با `top:0` و بدون `env(safe-area-inset-top)` است. همچنین layoutها عمدتاً از `100vh` و `max-height:88/90vh` استفاده می‌کنند، نه `dvh/svh` یا VisualViewport. در PWA standalone با `black-translucent` احتمال قرار گرفتن header زیر status bar و پنهان شدن input پایین پشت keyboard وجود دارد.

**پیشنهاد:** `padding-top: env(safe-area-inset-top)` برای shell، `100dvh` با fallback، و scroll-to-focused-input برای modal فرم اضافه شود.

---

## P3 — بهبود تجربه و کاهش اصطکاک

### MOB-019 — کشوی «سایر» گروه‌بندی سایدبار را از دست می‌دهد و تب‌های اصلی را تکرار می‌کند

**وضعیت:** تأییدشده در اجرا

در 390px، کشو 26 آیتم در یک grid چهارستونه دارد و «داشبورد»، «کارتابل من»، «پیشنهاد» و «دستیار» را با وجود bottom-nav دوباره نمایش می‌دهد. گروه‌بندی فروش/تأمین/مالی که در desktop وجود دارد از بین می‌رود؛ پیدا کردن ماژول برای کاربر جدید زمان‌بر می‌شود.

**پیشنهاد:** primary tabs را از More حذف کنید، گروه‌بندی/جستجوی واقعی اضافه کنید، و recent/favorite را بالای grid قرار دهید.

---

### MOB-020 — More sheet focus/inert policy ندارد

**وضعیت:** ریسک دسترس‌پذیری؛ منبع و ساختار DOM بررسی شد

کشوی More به body اضافه می‌شود، اما focus به آن منتقل و پس از بسته شدن بازگردانده نمی‌شود؛ background هم inert نمی‌شود و Escape ندارد. برای keyboard/screen-reader می‌تواند focus را از sheet خارج کند.

**پیشنهاد:** هنگام باز شدن focus روی search action، `aria-modal`/dialog semantics، focus trap، Escape و focus restoration را در ModalManager مشترک پیاده‌سازی کنید.

---

### MOB-021 — وضعیت sync در header تقریباً فقط یک emoji است

**وضعیت:** تأییدشده در منبع
**محل:** `crm/sync.js:473-485`

`#syncBadge` فقط emoji 11px و title دارد. title برای touch قابل اتکا نیست و کاربر موبایل ممکن است نداند رنگ/نقطه چه وضعیتی را نشان می‌دهد؛ در عین حال بنر هشدار به‌جای توضیح nonblocking روی nav می‌افتد.

**پیشنهاد:** یک chip کوتاه قابل لمس («آفلاین»، «3 تغییر در صف»، «همگام») بالای content یا در header با action مشاهدهٔ جزئیات بسازید.

---

## نقشهٔ اصلاح پیشنهادی

### اسپرینت 0 — توقف آسیب (1 تا 2 روز)

1. **MOB-001:** exception صریح برای `.mx-dot` و تست visual همهٔ modalها.
2. **MOB-003:** `ptfActivePanel` + refresh مستقل از sidebar.
3. **MOB-004:** جابه‌جایی toast/banner بالای bottom-nav.
4. **MOB-002 بخش action:** حذف `font-size:0` از `.tb2 .ba` تا زمان بازطراحی کامل.
5. تست 320/390 portrait + 844×390 landscape + dark mode.

### اسپرینت 1 — قرارداد component (3 تا 5 روز)

1. Button component با `icon/label/aria-label/variant/size`.
2. ModalManager واحد و migration مسیرهای مالی/حذفی.
3. DataCard schema با labelهای واقعی؛ migration 10 جدول پرتکرار اول: پیشنهاد، RFQ، چک، فاکتور، تنخواه، حساب تأمین‌کننده، مشتری، کالا، کارتابل و گزارش مالی.
4. رفع duplicate search و hit-targetهای کمتر از 44px.

### اسپرینت 2 — performance و PWA (2 تا 4 روز)

1. Core/mobile shell bundle و lazy load ماژول‌ها.
2. manifest تولیدی واحد برای cache/version/SW.
3. loading state برای navigation و cold start.
4. تست offline warm/cold، upgrade از نسخهٔ قبلی، و throttling 2Mbps/3G.

### اسپرینت 3 — کیفیت پایدار (مستمر)

1. screenshot regression برای 320/360/390/412 portrait و 844×390 landscape.
2. axe در CI و budget contrast/touch target.
3. lint برای ممنوعیت bare `button` در mobile CSS، inline modal جدید، و `alert/confirm/prompt` جدید.
4. کاهش تدریجی CSS injection و `!important`.

---

## معیار پذیرش بازآزمایی

- هیچ `.mx-dot` نباید بیش از hit-area طراحی‌شده باشد و عنوان مودال نباید زیر کنترل‌ها برود.
- هر value در mobile data card باید label واضح داشته باشد؛ هیچ action مهمی نباید فقط یک حرف/emoji مبهم باشد.
- بعد از navigation با bottom-nav، تغییر storage/sync باید بدون خروج و ورود دوباره render شود.
- toast/banner نباید حتی یک پیکسل از bottom-nav را بپوشاند.
- FAB پیشنهاد باید به شکل دایرهٔ کامل، ring و shadow کامل دیده شود؛ actionهای ردیف درخواست باید icon قابل‌دیدن و نام قابل‌فهم داشته باشند.
- در 320px portrait و 844×390 landscape، تمام actionهای اصلی 44×44 یا بزرگ‌تر و قابل تمایز باشند.
- `#off` و سایر hashهای معتبر باید بعد از reload همان پنل را باز کنند؛ Back باید ابتدا panel قبلی را بازگرداند.
- axe: صفر violation جدی contrast در dashboard و پنل‌های پرتکرار.
- در 2Mbps، navigation قابل لمس باید حداکثر در بودجهٔ توافق‌شده حاضر باشد؛ وضعیت loading روشن باشد.

---

## پیگیری پس از ممیزی

### MOB-001 — مرحلهٔ اول اصلاح کنترل‌های مودال

کنترل‌های close / minimize / fullscreen مودال در موبایل از کپسول‌های رنگی کشیده به دکمه‌های ثابت **36×36px** با نماد همیشگی `×`، `−` و `⤢` تبدیل شدند. عنوان مودال اکنون زیر ردیف کنترل‌ها قرار می‌گیرد؛ برای جلوگیری از هم‌پوشانی، padding بالای مودال 62px و title از فضای کنترل‌ها مستقل شد. برای هر کنترل نیز aria-label افزوده شد.

**تأیید فنی:** در 390×844، هر سه کنترل 36×36px بود و پایین‌ترین کنترل در y=173.5 قرار گرفت، در حالی که title از y=187.5 شروع شد؛ overlap صفر بود.

### MOB-022 — اصلاح آزمایشی انجام شد

در همین شاخه، selector مربوط به FAB از:

```css
.mnv-fabwrap{overflow:visible;position:relative}
```

به selector مشخص‌تر و دارای اولویت لازم تغییر داده شد:

```css
#mnvBar .mnv-fabwrap{overflow:visible!important;position:relative}
```

**تأیید فنی:** در Chromium لمسی 390×844، `overflow` واقعی FAB wrapper برابر `visible` شد و کادر دایرهٔ 54px از y=751 تا y=805، در حالی که parent از y=777 شروع می‌شود، به‌طور کامل قابل‌مشاهده بود. screenshot regression نیز دایرهٔ کامل، ring و shadow را نشان داد. این تغییر فقط clipping FAB را هدف می‌گیرد و هنوز نیازمند تأیید بصری کاربر روی دستگاه واقعی است.

### MOB-023 — اصلاح آزمایشی نمایش آیکون‌های action ردیف درخواست

برای actionهای `.ba` در `#rTb`، SVGهای تولیدشده توسط `icons.js` داخل `[data-ix]` قرار می‌گیرند. از آن‌جا که button والد `font-size:0` دارد، SVGهای مبتنی بر `em` صفر می‌شدند. یک override کاملاً محدود به جدول درخواست اضافه شد تا font-size و اندازهٔ SVG icon مستقل از متن پنهان‌شده باشد:

```css
#rTb .ba [data-ix] { font-size:18px!important; }
#rTb .ba [data-ix] svg { width:18px!important; height:18px!important; }
```

**تأیید فنی:** در سناریوی درخواست برنده در 390×844، دکمه‌های پیش‌تر خالیِ «مشاهده پروژه» و «افزودن متن استعلام» به‌ترتیب با آیکون پوشه و سند قابل‌دیدن شدند؛ آیکون‌های مشاهده، ویرایش و ارجاع نیز همچنان باقی ماندند.

### MOB-023 — یکسان‌سازی ظاهری actionهای ردیف درخواست

پس از بازخورد کاربر، تمام `button`های مستقیم در ستون عملیات `#rTb` نیز با قرارداد یکسان **36×36px**، `display:grid`، `place-items:center`، border و radius رندر شدند. برای جلوگیری از اثر text-node پنهان روی جای SVG، `[data-ix]` به‌صورت absolute در `top/left:50%` و `translate(-50%,-50%)` قرار گرفت. در آزمون هندسی 390×844، delta مرکز button و SVG برای آیکون‌های قابل اندازه‌گیری **0px در محور x و y** بود.

برای جذابیت و تشخیص سریع، رنگ actionها نیز معنایی شد: مشاهده آبی، ویرایش کهربایی، ارجاع آبی پررنگ، پیوست فیروزه‌ای، متن استعلام صورتی و پروژه بنفش. وضعیت زرد «منتقل‌شده به پروژه» عمداً تغییر نکرده است، زیرا chip وضعیت است و button عملیاتی نیست.

### MOB-012 — اصلاح آیکون‌های header موبایل

دکمه‌های ماه، زنگوله و جستجوی محلی از 34px به **42×42px** و SVGهای آن‌ها به **26×26px** ارتقا یافتند. دلیل کوچک ماندن SVGها فقط width نبود: قانون عمومی mobile برای همهٔ `button`ها padding 10×14px می‌داد و SVG را داخل flex box تا 14px کوچک می‌کرد. padding/gap برای `.tbic` صفر و container به grid وسط‌چین تبدیل شد؛ جستجوی سراسری نیز 42×42px با SVG 24px شد. ارتفاع header از 48 به 56px رسید تا این تغییر بدون clipping انجام شود.

**تأیید فنی:** در هر دو viewport 390×844 و 320×568، هر سه دکمه 42×42px، هر SVG 26×26px و بدون overflow افقی رندر شد.

### MOB-025 — تراکم‌زدایی و استانداردسازی actionهای کارت پیشنهاد

برای هر کارت پیشنهاد در موبایل، فقط actionهای اصلی (ویرایش/پرونده، پیش‌نمایش، چاپ، جریان مالی یا ریالی و «بیشتر») روی کارت باقی می‌مانند. actionهای کم‌تکرار مانند نگارش جدید، Excel، بهینه‌سازی سود، کنترل سلامت، حذف، صورتحساب و پاسخ/اصلاح به bottom sheet «عملیات بیشتر» منتقل شدند؛ هیچ action حذف نشده و با کلیک روی آیتم sheet، همان handler اصلی اجرا می‌شود.

در پیشنهاد ارزی دارای نسخهٔ ریالی، سه دکمهٔ کوچک پیش‌نمایش/چاپ/شرایط از باکس اطلاعاتی ردیف برداشته و به sheet اختصاصی `💱 عملیات نسخه ریالی` منتقل شدند. دکمهٔ بازگشت ادمین نیز اکنون به‌صورت آیکون قرمز `⏪` با عنوان و aria-label صریح نمایش داده می‌شود. همهٔ actionهای visible کارت 40×40px، وسط‌چین و دارای رنگ معنایی هستند. وضعیت فاکتور نیز از ستون عملیات خارج و به badge متنیِ قابل‌فهم در ستون وضعیت منتقل شد تا با دکمه اشتباه گرفته نشود.

**تأیید فنی:** در fixture شامل پیشنهاد ارزی + همراه ریالی + پیشنهاد برنده، کارت ارزی 5 action اصلی بدون overflow داشت؛ sheet عمومی 6 action ثانویه و sheet ریالی 3 action اختصاصی را با labelهای کامل نمایش داد. هیچ action مخفی حذف نشده است؛ sheet همان handler اصلی را اجرا می‌کند.

### MOB-034 — رفع هم‌پوشانی کنترل‌های «کارتابل من»

بازخورد کاربر نشان داد الگوی فشردگی کنترل‌های زیر فیلتر، علاوه بر کالاها، در «کارتابل من» نیز رخ می‌دهد. علت مشترک، قرار گرفتن checkbox و actionها در `.sb2`/`.ph` عمومی بود که در موبایل input را کش می‌داد و دکمه‌ها را به مربع‌های icon-only تبدیل می‌کرد.

کارتابل اکنون wrapper مستقل دارد: toggle تمام‌عرض «نمایش خوانده‌شده‌ها» با checkbox 20px و grid سه‌ستونهٔ actionهای برچسب‌دار «خواندم همه»، «ترجیحات اعلان» و «پیام گروه». دکمهٔ پیام گروه که پیش‌تر کنار عنوان inject می‌شد، در همان grid قرار می‌گیرد؛ تمام actionها title و aria-label صریح دارند.

**تأیید فنی:** در 320×568، toggle `272×48px` و سه action `85.3×64px` بودند؛ در 390×844، سه action `108.7×64px` بودند. overlap صفر، overflow افقی صفر و page/console error صفر بود. toggle و «خواندم همه» نیز با fixture اعلان واقعی و تغییر state آزموده شدند.

### MOB-035 — رفع فشردگی کنترل‌های «یادآورها»

کنترل‌های یادآورها نیز به‌دلیل استفاده از `.sb2/.ph` عمومی در موبایل دچار همان regression کارتابل بودند: checkbox کشیده و action «یادآور جدید» به آیکون مبهم تبدیل می‌شد. ساختار مستقل شامل دو select تمام‌عرض، toggle 58px با checkbox 20px و tile برچسب‌دار «یادآور جدید» جایگزین شد.

**تأیید فنی:** در 320×568، دو select `272×48px` و دو کنترل پایانی هرکدام `132×58px` بودند؛ overlap و overflow صفر بود. فعال‌سازی نمایش انجام‌شده‌ها و بازشدن مودال یادآور جدید نیز آزموده شد.

### MOB-036 — بازآرایی تب‌ها و محتوای هاب مالی

ده tab هاب مالی بر پایهٔ طول label در ردیف‌های نامتقارن می‌نشستند و بخش‌هایی از ابزارهای «هزینه جاری» و «چک‌ها» خارج از viewport قرار می‌گرفتند. افزون بر آن، rule عمومی `display:flex!important` مانع پنهان‌شدن toolbar تنخواه در تب‌های غیرتنخواه بود؛ بنابراین محتوای تب‌ها با فیلترهای تنخواه مخلوط دیده می‌شد.

tabها اکنون grid دو‌ستونه با label قابل‌خواندن، `title`، `aria-label` و `aria-pressed` هستند. state tab فعال روی parent نگه داشته می‌شود تا بازسازی dynamic محتوا، تب فعال را پنهان نکند. toolbar سال مالی، action تقسیم سود، هزینه جاری و ابزارهای چک نیز responsive grid/wrap گرفتند.

**تأیید فنی:** در loop تمام 10 tab در 320×568 و 390×844، `document.scrollWidth` دقیقاً با viewport برابر بود؛ هیچ دکمهٔ خارج از viewport یا clipped و هیچ page/console error دیده نشد. `fiscalBox`، `opexBox` و `chequeBox` نیز به‌ترتیب `clientWidth === scrollWidth` داشتند. click واقعی tab سال مالی و زیرتب چک وارده، state و visibility را پایدار نگه داشت.

### MOB-004 — رفع پوشاندن bottom-nav توسط toast و بنر همگام‌سازی

toast و banner تغییرات آفلاین/ذخیره‌نشده دیگر روی bottom navigation رندر نمی‌شوند. toast در موبایل با clearance نوار 74px، safe-area و فاصلهٔ 12px بالا می‌رود و برای عبور لمس به کنترل زیرین `pointer-events:none` دارد. banner پایدار نیز با حاشیهٔ 8px بالای nav قرار می‌گیرد.

وقتی هر دو پیام هم‌زمان حاضرند، ارتفاع واقعی banner با `ResizeObserver` در یک CSS variable ثبت می‌شود؛ toast با فاصلهٔ 16px بالاتر از آن stack می‌شود. به این ترتیب متن بلند banner نیز نه nav را می‌پوشاند و نه toast را.

**تأیید فنی:** در 320×568، banner تا y=486 در برابر شروع nav در y=494 و toast تا y=389 بود؛ در 390×844، banner تا y=762 در برابر nav y=770 و toast تا y=685 بود. هیچ تقاطعی دیده نشد؛ toast بدون banner هم دقیقاً 12px بالای nav قرار گرفت و لمس tab پایین به `#mnvBar` رسید.

### MOB-005 — رفع سقوط به layout نیمه‌دسکتاپ در landscape

در گوشی landscape با اندازهٔ رایج 844×390، شرط قدیمی `max-width:768px` فعال نمی‌شد؛ sidebar 56px برمی‌گشت، bottom-nav پنهان می‌شد و header با الگوی desktop رندر می‌شد. shell موبایل اکنون برای `(max-width:900px) and (max-height:600px) and (orientation:landscape)` نیز فعال است.

sidebar پنهان، main تمام‌عرض، header بدون wrap و 52px، و bottom-nav فشرده با ارتفاع 52px و tabهای 45px هستند. breakpoint toast/banner نیز هم‌راستا شد تا اعلان‌ها در landscape روی nav نیفتند.

**تأیید فنی:** در شروع مستقیم و در آزمون چرخش portrait→844×390، sidebar `display:none`، main `844px`، nav `844×52px` و header `52px` بود؛ پنج tab داخل viewport ماندند، `document.scrollWidth=844` و page/console error صفر بود. کلیک «کارتابل» نیز state صحیح `cart` را ثبت کرد.

### MOB-007 — استانداردسازی semantics، focus و Escape مودال‌ها

لایهٔ مرکزی `modalx.js` اکنون تمام modalهای متداول `.md-b` و `.ptfdlg-b` را هنگام ایجاد تجهیز می‌کند: `role="dialog"`، `aria-modal`، `aria-labelledby`، focus اولیه، Tab/Shift+Tab trap، Escape برای بالاترین modal و بازگردانی focus به trigger.

در modalهای تو‌در‌تو، Escape فقط پنجرهٔ رویی را می‌بندد. `ptfDialog` از مسیر Cancel خودش بسته می‌شود تا callback حفظ شود؛ `dialogx` نیز Promise/handler اختصاصی خود را نگه می‌دارد. dock کوچک‌سازی هم با keyboard قابل استفاده و قابل بازگردانی شد.

**تأیید فنی:** در 320×568 و 390×844، modal واقعی ثبت درخواست role/aria صحیح و focus اولیه داخل فرم داشت؛ Tab و Shift+Tab از modal خارج نشدند و Escape focus را به trigger بازگرداند. nested `ptfDialog`، `dialogx.confirm` و minimize/restore با Enter نیز بدون page/console error آزموده شدند.

### MOB-008 — مرحلهٔ اول سرعت ادراک‌شدهٔ cold start

`mobilenav.js` از انتهای صف 91 script به دومین script بعد از `ui-kit` منتقل شد. در نتیجه shell navigation بدون انتظار برای AI، چاپ چک، مالی، گزارش و سایر ماژول‌های سنگین ساخته می‌شود. status کوچک و قابل‌فهم `loading → nav → ready` نیز وضعیت را تا پایان بارگیری اعلام می‌کند.

اگر کاربر پیش از کامل‌شدن bundleها روی «کارتابل»، پیشنهاد، دستیار یا «سایر» لمس کند، مقصد queue می‌شود؛ dashboard خالی نمی‌شود و پس از آماده‌شدن builder همان پنل باز خواهد شد. hook نهایی `goPanel` نیز تا پایان defer bundleها صبر می‌کند تا state ناوبری حفظ شود.

**تأیید فنی:** در شبیه‌سازی 150ms latency / 250KiB/s در 390×844، nav در **2.47s** آماده شد، در حالی که آزمون اولیهٔ ممیزی آن را حدود 13.4s نشان داده بود. FCP حدود 0.54s و بارگیری کامل صف 91 script هنوز حدود 16.6s است؛ بنابراین lazy-load واقعی ماژول‌های سنگین همچنان مرحلهٔ بعدی performance/PWA خواهد بود. click زودهنگام کارتابل queue و پس از ready با state صحیح `cart` اجرا شد؛ تمام tabهای اصلی و More بدون error آزمایش شدند.

### MOB-009 — قرارداد یکپارچهٔ release، cache و Service Worker

release واحد **`v34.4.0`** اکنون در `VERSION.json`، runtime CRM، manifest، clear-cache، URL worker و cache namespace ثبت شده است. تمام 91 script صفحه از query واحد `?v=34.4.0` استفاده می‌کنند و SW نیز دقیقاً همان URLهای queryدار را precache می‌کند؛ اختلاف قبلی cache key بدون-query و درخواست queryدار حذف شد.

`offer-rial-convert.js` که از SHELL جا افتاده بود افزوده شد، registration worker با URL نسخه‌دار انجام می‌شود و purge cache به‌جای متغیر تعریف‌نشده، release فعال را استفاده می‌کند. navigation آفلاین دارای fallback به index precache شده است.

**تأیید فنی:** تطابق 91/91 index و SW به‌صورت برنامه‌ای بررسی شد. در اجرای واقعی، worker فعال `sw.js?v=v34.4.0`، cache فعال `ptf-crm-v34.4.0` و assetهای queryدار نمونه در cache وجود داشتند. reload در offline با controller فعال، nav قابل‌استفاده و بدون page/console error اجرا شد.

### MOB-037 — رفع بریدگی badge قرمز زنگولهٔ header

بازخورد کاربر نشان داد شمارندهٔ قرمز صندوق پیام روی زنگوله نصفه دیده می‌شود. علت، ترکیب offset منفی badge با `overflow:hidden!important` عمومی button بود. overflow زنگوله و wrapper آن به‌صورت محدود visible شد و badge به offset داخلی امن `1px` منتقل شد؛ بنابراین بدون بیرون‌زدن از header، کامل دیده می‌شود.

**تأیید فنی:** در 320×568 و 390×844 با شمارندهٔ `99+`، badge `29×18px` کاملاً داخل bell `42×42px` و header قرار داشت؛ overflow افقی و page/console error صفر بود.

## پیوست: شواهد عددی

| شاخص | نتیجه |
|---|---:|
| مسیرهای اصلی که با fixture بدون pageerror باز شدند | 21 پنل |
| اندازهٔ modal dot در اجرای واقعی | 98×46px برای هر کنترل، به‌جای طراحی 13px |
| modal پایه با role/aria-modal | ✅ MOB-007: `role=dialog`، `aria-modal` و `aria-labelledby` خودکار |
| Escape روی modal پایه | ✅ فقط بالاترین modal بسته و focus به trigger بازمی‌گردد |
| table-card label (`data-label`) | 0 occurrence |
| table action test «ابطال» | 36×36px، font-size=0، title/aria-label خالی |
| navigation در 2Mbps آزمایشگاهی | ✅ مرحلهٔ اول MOB-008: shell در 2.47s؛ صف کامل 91 script ~16.6s |
| First Contentful Paint در آزمون MOB-008 | ~0.54s |
| PWA release/cache | ✅ MOB-009: 91/91 URL queryدار index/SW، cache `ptf-crm-v34.4.0` و reload آفلاین موفق |
| toast + banner در 390×844 | ✅ MOB-004: banner با فاصلهٔ 8px و toast با stack 16.7px بالاتر از آن؛ بدون پوشاندن bottom-nav |
| More sheet در 390×844 | 26 آیتم، 4 ستون، primary tabهای تکراری |
| landscape 844×390 | ✅ MOB-005: sidebar پنهان، main تمام‌عرض، header/nav برابر 52px و پنج tab لمسی |

> این گزارش فقط فهرست مشکل نیست: موارد P0/P1 با مسیر بازتولید و پیشنهاد اصلاح قابل تبدیل به ticket نوشته شده‌اند. اصلاح‌های بصری MOB-001 تا MOB-005، semantics پایهٔ MOB-007، navigation زودهنگام MOB-008 و قرارداد cache/PWA در MOB-009 اکنون با regression screenshot قفل شده‌اند؛ موارد باز بعدی شامل lazy-load واقعی، deep-link، Kanban و focus/inert کشوی «سایر» هستند.
