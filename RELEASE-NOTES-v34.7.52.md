# RELEASE NOTES — v34.7.52 (۱۴۰۵/۰۵/۲۹ — 2026-08-20)

> **یادداشت:** این سند پس از انتشار (در نشست v34.7.54) تکمیل شد؛ محتوا از commit
> `b003f9f` و قراردادهای tester455 استخراج شده است.

## فشرده‌سازی WebP/JPEG و گزارش تشخیصی کلید ابری

### ۱) فشرده‌سازی هوشمند تصویر در آپلود

- `compressImage` در `crm/storage.js` هر دو خروجی **WebP و JPEG** را می‌سازد و
  **کوچک‌ترین خروجی معتبر** را انتخاب می‌کند (fallback خودکار وقتی WebP بزرگ‌تر یا
  نامعتبر است).
- `savedNote` فرمت انتخاب‌شده را به کاربر نشان می‌دهد.

### ۲) گزارش فقط‌خواندنی کلیدهای ابری (`ptfCloudKeyAudit`)

- کلیدهای پیوست ثبت‌شده در مخازن CRM (`ptfHarvestFileKeys`) با فهرست کامل باکت S3
  تطبیق داده می‌شوند و در پنج رده طبقه‌بندی می‌شوند:

| رده | معنا |
|---|---|
| **A** | موجود در ابر (سالم) |
| **B** | تطبیق یکتا با کلید دیگر (بدون اسلش / نام فایل یکتا) — نامزد remap |
| **C** | اصل فایل داخل zip بایگانی (`archives/`) |
| **E** | در باکت فعلی نیست — نیازمند آپلود مجدد |
| **F** | دادهٔ محلی (`data:`) |

- **کاملاً فقط‌خواندنی**: هیچ DELETE یا تغییری نمی‌زند؛ خروجی CSV دارد؛ دکمه در دیالوگ
  فضای ابری و فقط برای **نقش ارشد**.
- اگر فهرست S3 ناقص (truncated) برگردد، هشدار نمایش داده می‌شود (ردهٔ E ممکن است
  بیش‌برآورد باشد).
- `presign_get` در `api/storage.php` فقط **404** را `file_not_found` می‌شمارد (نه 403)؛
  پیام «کلید قدیمی/مهاجرت‌نشده — دوباره آپلود کنید» حفظ شد.

### تست

- تستر جدید `tester455-v34.7.52-cloud-key-audit-webp.js` (در گیت CI).

### فایل‌های اصلی

`crm/storage.js` · `api/storage.php` · نقاط رسمی نسخه

### ⚠️ یادداشت پسینی

کد `ptfHarvestFileKeys` این نسخه یک تخلف A2 نگهبان معماری (ترتیب `cd || _id`) ایجاد کرد
که گیت CI را قرمز کرد؛ در **v34.7.54** اصلاح و با tester457 قفل شد.
