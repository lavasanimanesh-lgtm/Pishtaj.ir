# چک‌لیست staging v34.7.46 — تعیین نتیجهٔ رویژن قبلی

ابتدا چک‌لیست کامل `STAGING-TEST-AWARD-REVISION-v34.7.45.md` را اجرا کنید.

برای operation ID گزارش‌شده `REV-mt0fqdf5-93hkmqvx`:

1. با همان کاربر روی staging وارد شوید و Hard Refresh کنید.
2. حداکثر چند ثانیه برای بررسی journal و pull صبر کنید.
3. دقیقاً یکی از دو نتیجه باید نمایش داده شود:
   - «نتیجه قطعی … بازیابی شد»؛ سپس Rev و محتوای پیشنهاد را بررسی کنید.
   - «رسید سرور تأیید کرد … ثبت نشده است»؛ سپس فرم کامل رویژن را دوباره باز و یک intent تازه ثبت کنید.
4. نتیجه نباید پس از پاسخ موفق `command_status` همچنان uncertain بماند.
5. در Network، boot فقط `command_status`/pull دارد و نباید `revise_award` را خودکار دوباره ارسال کند.

Production نباید deploy شود.
