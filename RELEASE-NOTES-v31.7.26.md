# Release Notes — v31.7.26

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.26
**نوع:** 🔴 رفع باگ داده — نوسان خودکار (رشد تکراری و سپس کاهش) تامین‌کنندگان/مشتریان/دفتر تلفن (BUG-SYNC-OSC-001)

---

## 🎯 گزارش کارفرما

«تعداد تامین‌کنندگان، مشتریان و… خودکار زیاد می‌شود، بعد خودکار کم می‌شود؛ تامین‌کنندگان قبلی دوباره و تکراری اضافه می‌شوند. دفتر تلفن هم مدام زیاد می‌شود و مخاطبین قبلی مجدد اضافه می‌شوند.»

## 🔍 علت ریشه‌ای (RCA) — دو مکانیزم نوسان، هر دو با شبیه‌سازی merge واقعی اثبات شد

### ❶ دفتر تلفن — شناسه تصادفی در هر بازسازی

بازسازی خودکار دفترچه (`smsBookSyncAll` — با هر ورود به پنل پیامک/ذخیره مشتری/تامین‌کننده اجرا می‌شود) رکوردهای auto را حذف و از نو می‌سازد و هر بار با `genCode('PB')` **کد تصادفی جدید** می‌داد. نتیجه در محیط چنددستگاهی:
- دستگاه A برای «علی ۰۹۱۲…» کد `PB-71001` می‌سازد، دستگاه B همان مخاطب را با `PB-84500`
- merge سینک با کلید `cd` هر دو را «دو رکورد متفاوت» می‌بیند → **مخاطب تکراری اضافه می‌شود (رشد)**
- بازسازی بعدی دفترچه، duplicates را جمع می‌کند → **کاهش** — و چرخه از نو → نوسان دائمی که می‌دیدید

### ❷ تامین‌کنندگان/مشتریان — حذف بی‌صدای رکوردهای بدون شناسه

در merge عمومی سینک، هر رکوردی که فیلد شناسه (`cd/no/id/code`) نداشت (رکوردهای legacy/واردشده با ساختار قدیمی) **بی‌صدا حذف می‌شد** → «کم شدن خودکار». اما دستگاه دیگر که هنوز آن رکوردها را داشت دوباره push می‌کرد → «برگشتن/زیاد شدن» → نوسان.

## ✅ اصلاحات (سه‌گانه — همگی در جهت هویت پایدار)

| # | فایل | تغییر | شدت |
|---|------|--------|-----|
| 1 | `crm/sync.js` | **merge اختصاصی دفتر تلفن با کلید هویت واقعی (شماره موبایل)** — یک شماره = یک رکورد در هر شرایطی؛ مخاطب دستی/اکسل همیشه بر auto مقدم؛ در تساوی، جدیدتر برنده. وضعیت آلوده فعلی (cdهای متفاوت دو دستگاه) هم با اولین sync پس از استقرار خودبه‌خود جارو می‌شود | 🔴 P0 |
| 2 | `crm/sms.js` | **کد پایدار `PB-{شماره موبایل}`** برای رکوردهای بازسازی خودکار — هر دستگاه و هر rebuild همان کد را می‌سازد (رکوردهای دستی/اکسل که یک‌باره ساخته می‌شوند مثل قبل) | 🔴 P0 |
| 3 | `crm/sync.js` | **رکوردهای بدون فیلد شناسه دیگر در merge حذف نمی‌شوند** — با امضای JSON یکتا حفظ می‌شوند (بدون تورم تکراری) | 🔴 P0 |

## 📊 نتایج گیت

- تستر جدید `tester201-sync-oscillation`: **۱۳/۱۳ PASS** — بازتولید هر دو مکانیزم نوسان و اثبات رفع: یک مخاطب = یک رکورد، idempotency merge (نوسان قطع)، تقدم دستی بر auto، بقای رکورد legacy بدون تورم، پایداری cd در دو rebuild متوالی
- رگرسیون کامل: **179/179 فایل PASS — 4089/4089 چک PASS** | `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| دفتر تلفن موجود (آلوده به تکراری) | با اولین sync پس از استقرار، duplicates بر اساس شماره موبایل خودکار ادغام می‌شوند — مخاطبین دستی حفظ | مثبت |
| تامین‌کنندگان/مشتریان | رکوردهای legacy بدون شناسه دیگر گم/برگشتی نمی‌شوند؛ رکوردهای عادی cd دار بدون تغییر رفتار | صفر |
| مکانیزم‌های قبلی merge (اعلان/آواتار/مالی/سال مالی) | دست‌نخورده — tester185/186 سبز | صفر |
| حجم sync | کاهش — چرخه رشد/جاروی دائمی حذف شد | مثبت |

## 📋 راستی‌آزمایی توسط کارفرما (۵ دقیقه — همان علائم گزارش)

1. استقرار روی هر دو دستگاه → رفرش هر دو → چند دقیقه صبر (چند چرخه sync ۲۰ثانیه‌ای).
2. **دفتر تلفن:** شمار مخاطبین باید تثبیت شود و دیگر بالا/پایین نرود؛ مخاطبین تکراری قبلی خودکار ادغام شده باشند.
3. **تامین‌کنندگان/مشتریان:** شمار داشبورد در چند رفرش متوالی ثابت بماند (نه رشد، نه کاهش).
4. یک مخاطب دستی در دفترچه اصلاح کنید → پس از sync دستگاه دوم، اصلاح شما حفظ شده باشد (نه بازنویسی با auto).

---
*توسعه‌دهنده: PTF Development Team — 2026-07-20*
