# 📦 Release Notes — v56.3 (تکمیلی اسپرینت ۵۶)
**ایمپورت مستقیم Excel (xlsx) + به‌روزرسانی بک‌لاگ طبق بازخورد کارفرما**
**تاریخ:** ۱۳ تیر ۱۴۰۵ (2026-07-03)

---

## ✅ ۱. ایمپورت مستقیم فایل اکسل (xlsx) — بدون نیاز به تبدیل CSV

کتابخانه **SheetJS (xlsx.mini v0.20.3)** به‌صورت محلی (`crm/xlsx.min.js` — ۲۷۳KB، بدون وابستگی CDN) اضافه شد:

### ماژول کالاها:
- دکمه «⬆️ ایمپورت» حالا `.xlsx` و `.xls` را مستقیم می‌خواند (علاوه بر CSV).
- **تشخیص خودکار ستون‌ها از روی هدر** فارسی/انگلیسی: کد، نام کالا، نام انگلیسی، دسته، استاندارد/گرید، برند، واحد، قیمت، توضیحات — ترتیب ستون‌ها مهم نیست.
- اعتبارسنجی کد + رد تکراری‌ها + گزارش ردیف‌های ردشده مثل قبل.
- ثبت تکی از قبل موجود بود (دکمه «+ کالای جدید») — حالا هر دو مسیر کامل است.

### فرم پیشنهاد TO/CO:
- ایمپورت اقلام هم مستقیم xlsx می‌خواند (پیام «تبدیل به CSV» حذف شد — زودتر از وعده فاز ۲ تحویل شد).
- بازسازی داخلی: منطق مشترک `offImportRows` برای هر دو فرمت.

### تست واقعی:
فایل xlsx آزمایشی با هدر فارسی («کد/نام کالا/قیمت...») ساخته و روندتریپ شد → نگاشت ستون‌ها صحیح، ۲ قلم با قیمت درست ایمپورت شد ✅

## ✅ ۲. به‌روزرسانی بک‌لاگ (پیش از اجرا — طبق بازخورد)

### US-118 (پرونده پروژه) — الحاقیه AC2b: گالری تصاویر
- آپلود چندتایی عکس پروژه و کالاها (drag & drop)
- Thumbnail خودکار + فشرده‌سازی WebP سمت کلاینت
- نمایش شبکه‌ای با لایت‌باکس + کپشن و برچسب (کالا/بازرسی/بارگیری/تحویل/سایت کارفرما)
- فیلتر بر اساس برچسب + اتصال عکس به قلم خاص از اقلام CO

### US-121 (اتوماسیون مکاتبات) — الحاقیه AC6b: خلاصه‌سازی هوشمند و جستجوی موضوعی
- سیستم متن هر نامه (صادره **و** وارده) را می‌خواند، **خلاصه موضوعات مهم + کلیدواژه‌ها** استخراج و به رکورد نامه ضمیمه می‌کند
- جستجو با هر یک از آن کلمات، نامه را پیدا می‌کند (علاوه بر شماره اندیکاتور و موضوع)
- صادره: استخراج محلی rule-based (شماره‌های ارجاع، مبالغ، تاریخ‌ها، اقلام) + خلاصه LLM در صورت وجود کلید API
- وارده: استخراج متن از PDF متنی؛ اسکن تصویری → OCR فا/EN از طریق LLM تصویری؛ بدون کلید API → ورود دستی ساختاریافته
- نرمال‌سازی جستجوی فارسی (ی/ي، ک/ك، ارقام فارسی/لاتین)

## 🧪 تست‌ها
- ✔ سینتکس `crm/index.html` و `crm/offers.js` (node --check) + بالانس آکولادها
- ✔ روندتریپ xlsx با SheetJS (ساخت → خواندن → نگاشت → ایمپورت)
- ✔ ممیز خودکار کل سایت: PASS

## 🚀 دیپلوی
فایل‌ها: `crm/index.html`، `crm/offers.js`، `crm/xlsx.min.js` (جدید)
