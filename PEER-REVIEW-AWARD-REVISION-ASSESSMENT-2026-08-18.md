# مرور تخصصی مستقل گزارش رویژن سند برد

**نقش این سند:** بازبینی ادعاهای  
`ASSESSMENT-AWARD-OFFER-REVISION-FINANCE-2026-08-18.md`  
و گزارش قبلی کشو/نشت، توسط زاویهٔ معماری سیستم، دامنهٔ فروش‌به‌نقد، هویت سند، مالیات، و همگام‌سازی کلاینت/سرور.

**روش:** هر حکم یا با مسیر فایل+خط تأیید شده، یا به‌عنوان اغراق / جاافتاده / تناقض علامت خورده. کد محصول در این مرور تغییر نکرده است.

**نسخه کد:** v34.7.33 — `api/sales-domain.php`, `crm/offers.js`, `crm/case-revision.js`, `crm/salesfiles.js`

---

## رأی هیئت (یک پاراگراف)

جهت محصول که کارفرما گفت — رویژن **همان پیشنهاد برنده** از داخل پرونده، قیمت زندهٔ CO برابر رویژن، ابطال فاکتور با تأیید، هم‌ترازی مالی — از نظر دامنه درست است و با قفل فعلی تب پیشنهاد (`offerPostAwardLocked` → «نگارش از پرونده») سازگار است.  
مدل پیاده‌شدهٔ P5 (`revise_award` → شمارهٔ `-Rn` + بدون void فاکتور) این جهت را برآورده نمی‌کند.  
اما گزارش قبلی **سختی اتصال فرم پیشنهاد**، **اثر متمم روی `contractAmount`**، **شکاف نقش WIN در برابر FIN**، **تعهد قانونی ابطال فاکتور رسمی**، و **دو‌نسخه‌ای بودن deal کلاینت/سرور** را کم‌وزن گرفته. «زیرساخت تقریباً هست» فقط برای لایهٔ مالی void+FIFO درست است، نه برای UI و هویت سند.

---

## الف) ادعاهای تأییدشده (نگه دارید)

| # | ادعا | شاهد |
|---|---|---|
| A1 | دو مدل رویژن ناسازگارند: پیشنهاد = همان `no` + `rev`؛ پرونده = `no-Rn` | `offerReviseClone` / `offerSave` در `offers.js`؛ `revise_award` حدود ۸۴۵–۹۵۰ در `sales-domain.php` |
| A2 | بعد از برد، رویژن از تب پیشنهاد عمداً بسته است و به پرونده حواله می‌شود | `offerPostAwardLocked` + پیام `offerReviseClone` |
| A3 | قفل با `d.inqNo === o.inqNo` زیاده‌رو است؛ CO غیربرندهٔ همان استعلام هم قفل می‌شود | `offers.js` بدنهٔ `offerPostAwardLocked` |
| A4 | UI بازنگری پرونده قلم جدید ندارد و حداقل یک قلم می‌خواهد | `ptfAwardReviseOpen` / `ptfAwardReviseSubmit` |
| A5 | فاکتور رسمی فعال کاهش مبلغ را ۴۰۹ می‌کند و void نمی‌کند | `official_invoice_blocks_decrease` |
| A6 | `awardDocs` پس از `revise_award` بازنویسی نمی‌شود | فرمان فقط `wonOffer`/`awardRevisions` را می‌نویسد |
| A7 | نشت TO از `inqNo` در `sfAwardEnsure` و `autoCreateProjectFromCO` واقعی است | هر دو: `x.inqNo === o.inqNo` بدون `srcToNo` |
| A8 | `void_invoice` / `void_unofficial_invoice` سند را پاک نمی‌کنند و `sd_rebuild_allocations` می‌زنند | حدود ۱۱۷۲–۱۱۹۶ |
| A9 | رسید حذف نمی‌شود؛ پس از void، تخصیص آزاد → `creditRemainIRR` | طراحی صریح INV-01 |
| A10 | `register_offer` روی `st=won` اگر JSON فرق کند `won_offer_locked` است | خط ۷۸۷ — پس `offerSave` کلاینتی راه رویژن برد نیست |

این‌ها ستون فقرات گزارش‌اند و در مرور رد نشدند.

---

## ب) اغراق یا ساده‌سازی خطرناک

### B1. «فرم پیشنهاد را داخل پرونده باز کنید» کار کوچکی نیست

`offerForm` مودال تمام‌صفحه است، به `_offState` سراسری وابسته است، `offerSave` قفل برد را می‌شکند یا به سرور `register_offer` می‌خورد. کشوی پرونده همزمان باز است.  
اتصال خام یعنی تداخل مودال، پیش‌نویس `localStorage`، SMS بعد از ذخیره، همگام کالا/نرخ مرجع، و دور زدن فرمان اتمی.  
**حکم:** یا فرم را با پرچم `postAwardRevision` و ذخیرهٔ فقط‌API بپیچید (هزینهٔ بالا)، یا جدول اقلام را از همان موتور `offRenderItems` **کپی کنترل‌شده** بیاورید بدون `offerSave`. گزارش این را کم دیده.

### B2. «زیرساخت تقریباً هست»

درست برای: void، FIFO، correction، قفل سال مالی، reconciliation.  
نادرست برای: هویت شمارهٔ ثابت روی سرور، به‌روز `awardDocs`، `lineId`، متمم، نسخهٔ ریالی، docsx، خرید واقعی.  
P5 حتی `lineId` / `sourceItemKey` را در `$newItems` کپی نمی‌کند؛ پوشش خطی فاکتور بعدی روی اندیس می‌شکند (`register_invoice` خط ۱۱۴۳).

### B3. «یک کلیک»

ابطال فاکتور رسمی در این محصول یعنی `legal_void` + مدرک سامانه مودیان در دنیای واقعی. دورهٔ قفل‌شده ۴۰۹ است. چک وصول‌شده به فاکتور در `void_invoice` cascade نمی‌شود (فقط در `admin_delete_plan` advisory است).  
جریان درست حداقل است: پیش‌نمایش دلتا → فهرست فاکتور/چک/خرید باز → تأیید ابطال → (در صورت قفل سال: توقف) → فرمان. «یک کلیک» را در UI به‌معنی «یک ورود از پرونده» نگه دارید نه یک `confirm()`.

### B4. عوض کردن تصمیم ۱۴۰۵/۰۵/۲۶ بدون ثبت تصمیم جدید

گارد کاهش فاکتور رسمی تصمیم مکتوب کارفرماست و تستر `tester434` همان را قفل کرده. مدل جدید آن را مشروط به void می‌کند. این **تغییر قرارداد محصول** است؛ تا در تصمیم‌نامه نیاید نباید P5 را بشکنید.

### B5. مهاجرت `-R` موجود ساده‌تر از واقعیت نوشته شده

اگر حتی یک پرونده با `wonOffer=CO-x-R1` و فاکتور روی `offerNo` همان `-R1` زنده باشد، برگرداندن هویت به `CO-x` فاکتور/تخصیص/چاپ را یتیم می‌کند. مهاجرت باید **اختیاری و موردی** باشد، پیش‌فرض: اسناد `-R` همان هویت بمانند؛ فقط **رویژن‌های بعدی** شمارهٔ ثابت بگیرند. یا سیاست صریح: از این به بعد همیشه `-R` ولی UI مثل Rev نشان بدهد — آن وقت «قیمت پیشنهاد برنده» باید alias به آخرین `-R` باشد نه mutate ریشه.

---

## ج) جاافتادگی‌های معماری (مهم‌تر از UX کشو)

### C1. متمم‌ها و `contractAmount` — باگ فعلی و دام مدل پیشنهادی

`win_offer` متمم را به `linkedOffers` اضافه و `contractAmount += sd_offer_total(amendment)` می‌کند.  
`revise_award` می‌نویسد `$case['contractAmount'] = $newTotal` **فقط از اقلام سند جایگزین** و مجموع متمم را دور می‌ریزد.

مدل «اقلام رویژن = کل قرارداد» با پروندهٔ دارای متمم مثبت **غلط** است مگر:

- رویژن فقط سند ریشه را عوض کند و `contractAmount = sum(root_rev) + sum(active amendments)`، یا  
- متمم‌ها در همان رویژن بازپخش/باطل شوند.

گزارش قبلی این را ذکر نکرد.

### C2. شکاف نقش

| عمل | UI | سرور |
|---|---|---|
| بازنگری برد | `canRevise`: ارشد / admin/chairman/ceo/commercial — **بدون sales** | `SD_WIN_ROLES` **با sales** |
| ابطال فاکتور | عملاً ارشد (`isSenior` در مسیر رسمی) | `SD_FIN_ROLES` شامل **accountant** نه sales |

فرمان ترکیبی رویژن+void یا باید اشتراک نقش‌ها باشد (`commercial/ceo/admin/chairman`) یا دو امضا (بازرگانی + مالی). Accountant امروز void می‌تواند، رویژن نه؛ sales روی سرور رویژن می‌تواند، در UI نه. گزارش «یک نقش WIN+FIN» را سرسری گفت.

### C3. دو منبع حقیقت پرونده

کلاینت `ptf_crm_deals` را با `sfSave`/`autoCreateProjectFromCO` می‌نویسد؛ سرور همان کلید را در فرمان‌ها commit می‌کند. اگر کلاینت بعد از `revise_award` snapshot نگیرد، کشو `wonOffer` کهنه و `awardDocs` محلی می‌ماند — همان پدیدهٔ «دو سند برد». هر طرح رویژن باید **فقط پس از `data` پاسخ سرور** رندر کند.

### C4. هویت فاکتور غیررسمی روی `offerNo`

اگر شماره CO ثابت بماند، lookupهایی مثل «غیررسمی فعال همین offerNo» بعد از void+صدور مجدد سالم‌اند. اگر `-R` بسازید، فاکتور کهنه روی `CO` و جدید روی `CO-R1` است و گزارش‌های `offerNo` دوپاره می‌شوند. این **دلیل فنی محکم** برای شمارهٔ ثابت است — از گزارش قوی‌تر از «شبیه پیشنهاد».

### C5. چک، مرجوعی فروش، پیش‌پرداخت زمان‌بندی VAT

`sd_rebuild_allocations` پس از void درست است. اما چک دریافتی با `sourceInvoiceCd`، مرجوعی فروش، و `timing` پیش‌فاکتور در فرمان ترکیبی دیده نشده. حداقل: اگر چک فعال به فاکتورِ در حال void وصل است، فرمان باید ۴۰۹ بدهد یا تعیین‌تکلیف بخواهد.

### C6. نسخهٔ ریالی

`ptfRialCompanionOf(no)` با `rialOf === no`. با شمارهٔ ثابت، همراه کهنه با اقلام/نرخ قدیم می‌ماند و چاپ ریالی دروغ می‌گوید. باید در همان فرمان `stale` شود یا rebuild اجباری.

### C7. هم‌زیستی سناریوی بازرسی P4

P5 برای «مردود بازرسی + قیمت توافقی باقی» طراحی شده و پیش‌پر از `inspections[]` است. رویژن کامل سفارش کارفرما سناریوی دیگری است. یک فرمان با دو *intent* (`inspection_adjust` در برابر `employer_order_change`) بهتر از دو API موازی است؛ UI بازرسی می‌تواند همان فرمان را با قلم‌های ازپیش‌پر صدا بزند.

---

## د) تناقض‌های داخلی دو گزارش ارزیابی

1. گزارش اول حذف docsx و UX سه‌زبانه را هم‌عرض رویژن گذاشت؛ کارفرمای بعدی رویژن+فاکتور را اصل کرد. اولویت درست: هویت+مالی اول؛ docsx/UX فاز بعد.  
2. گزارش دوم «دیگر `-R` نسازیم» گفت و همزمان «مهاجرت `-R` به history». بدون سیاست C/B5 این دو در دادهٔ زنده برخورد می‌کنند.  
3. «افزایش مبلغ بدون void مجاز» با پوشش خطی فاکتور در تضاد است اگر فاکتور `coverageMode=lines` باشد.

---

## هـ) مدل اصلاح‌شده پس از مرور (جایگزین فاز ۰)

سه بند کارفرما را نگه دارید، با قیود هیئت:

1. **هویت تجاری از این به بعد ثابت** (`no` + `rev`). اسناد `-R` موجود دست نخورند مگر ابزار موردی با پیش‌نمایش وابستگی.  
2. **ابطال فاکتور فعال داخل همان فرمان، فقط با فهرست و تأیید**؛ اگر فاکتور رسمی هست و کاربر رد کند، گارد کاهش ۱۴۰۵/۰۵/۲۶ بماند.  
3. **ذخیره فقط سرور**؛ UI اقلام کامل (افزودن/حذف) ولی نه `offerSave` خام.  
4. **متمم:** مبلغ مؤثر = ریشهٔ رویژن‌شده + متمم‌های فعال؛ یا متمم در همان wizard بازبینی شود.  
5. **نقش:** رویژن+void برای `admin/chairman/ceo/commercial`؛ accountant فقط void جدا؛ sales هیچ‌کدام.  
6. **توقف اجباری:** سال مالی قفل، چکِ متصل به فاکتور، قلم خرید/PL بدون disposition.

---

## و) آنچه در گزارش اول/دوم درست ماند و نباید گم شود

- نشت `awardDocs` / `inqNo` را قبل از mutate رویژن انبوه، تشخیص فقط‌خواندنی کنید.  
- جعبهٔ زرد باید سند جاری را نشان بدهد نه همهٔ آرایه.  
- docsx هنوز حذف/ابطال ندارد؛ برای «کارفرما پروژه را لغو کرد» لازم است ولی بعد از فرمان مالی.  
- کشوی شلوغ مشکل واقعی است اما حل آن را به رویژن گره نزنید.

---

## ز) جمع‌بندی هیئت

گزارش جهت را درست تشخیص داده و شواهد فایل برای شکاف P5 در برابر رویژن پیشنهاد کافی است.  
برای اجرا، سند را از «تقریباً آماده» به «چهار ریسک مسدودکننده» اصلاح کنید: متمم/`contractAmount`، نقش FIN در برابر WIN، ممنوعیت `offerSave`، سیاست `-R` موجود.  
بدون این چهار قید، پیاده‌سازی «مثل پیشنهاد» یا مبلغ قرارداد را خراب می‌کند یا فاکتور/نقش را دور می‌زند.

**توصیه:** قبل از کد، یک صفحهٔ تصمیم کارفرما با شش بند بخش هـ امضا شود. سپس فقط فرمان سرور + UI محدود، نه بازطراحی کشو.
