# یادداشت انتشار CRM v34.8.21

تاریخ: ۱۴۰۵/۰۶/۰۵ (۲۰۲۶-۰۸-۲۷)

## NOTIFS-UNION — پایان نوار زرد پایدار [notifs]

**گزارش کارفرما (تشخیصی v34.8.19 کار کرد):** «۱ تغییر هنوز به سرور نرسیده — [notifs] — آخرین خطا: pull»

### ریشه
`ptf_crm_notifs` (کارتابل) از مسیر base-merge معمولی می‌رفت؛ ولی merge سرور (append + dedupe خام)
با merge کلاینت (`ptfSmartMerge`: dedupe با cd + اتحاد readBy/done + مرتب‌سازی iso نزولی)
**هیچ‌وقت فرم برابر تولید نمی‌کرد** ⇒ `sameSyncJson` پاس نمی‌شد ⇒ dirty دائمی ⇒ نوار زرد پایدار.

### رفع
- `notifs` به کلیدهای union مشترک سرور اضافه شد (مثل audit/avatars — تعارض base بی‌معنا، ACK صادقانه)
- شاخهٔ merge کانونیکال سرور: دقیقاً قرارداد کلاینت — dedupe با cd، اتحاد readBy/done/repeat،
  dedupe ارجاع با dkey (بازمانده = رکورد سرور، به ترتیب درج — dedupe **قبل از** sort)،
  sort نزولی iso در انتها، سقف ۴۰۰۰
- نتیجه: خروجی سرور == خروجی کلاینت ⇒ dirty پاک می‌شود، پیام «N بخش به‌روز شد» تکرار نمی‌شود

### کنترل
tester524: همگرایی بایت‌به‌بایت مدل سرور با ptfSmartMerge واقعی کلاینت (۴ سناریو + نقطهٔ ثابت +
اثبات نابرابری مدل قدیمی) — ۱۹/۰. گیت کامل ۱۳۹/۰.
