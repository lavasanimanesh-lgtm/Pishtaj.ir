# بررسی منطق «حقوق سهامداران در هزینه‌های جاری» و اثر آن بر سود نهایی

**برنچ:** `arena/019fcd1d-pishtaj-ir` (v34.0.7-alpha)
**روش:** ردیابی کامل مسیر داده (shareholders.js ← opex.js ← fiscal.js ← ledger-report.js) + آزمایش عددی.

---

## ۱) معماری فعلی — حقوق سهامدار در دو دفتر ثبت می‌شود

هنگامی که حقوق «سهامدار موظف» ثبت می‌شود (`ptfShareApplySalary` / `ensureSalaryTxForMonth`)، **دو** رکورد هم‌زمان ساخته می‌شود (هر دو به یک رویداد اقتصادی اشاره دارند):

| دفتر | کلید | نوع | معنا |
|---|---|---|---|
| هزینه‌های جاری | `ptf_crm_opex` | `cat:'حقوق و دستمزد'`, `shareholderSalary:true`, `shareTx:<cd>` | **هزینهٔ واقعی** شرکت ← سود را کم می‌کند |
| دفتر سهامدار | `ptf_crm_sharetx` | `type:'salary'` | **مطالبه** (بستانکاری) سهامدار از شرکت — بدهی شرکت به او |

اتصال این دو با `shareTx` انجام می‌شود تا دوباره‌ساخته نشوند (هم‌راستا با F4-7 / مهاجرت `ptfMigrateShareholderOpex`).

**پرداخت حقوق** با `ptfShareDraw` ثبت می‌شود → یک رکورد `type:'draw'` در دفتر سهامدار (بدهکاری) که همان بستانکاری حقوق را خنثی می‌کند و **هیچ هزینهٔ دومی نمی‌سازد**.

---

## ۲) آیا حقوق در محاسبهٔ سود «دوبار» کم می‌شود؟ → خیر، دقیقاً یک‌بار کم می‌شود

در همهٔ نقاط محاسبهٔ سود، هزینهٔ حقوق سهامدار به‌عنوان opex فقط **یک‌بار** کسر می‌شود:

- **سود تعهدی** `ptfFiscalData`:
  `netProfit = projectProfit − opexTotal − petty + amend`
  که `opexTotal` = مجموع opexهای بدون لینک پرونده (شامل حقوق سهامدار).
- **سود نقدی** `ptfFiscalCashData`:
  `netCash = receipts − outflows` که `out.opex = opexTotal` (شامل حقوق).
- **تراز/گزارش تجمیعی** `ledger-report.js`:
  `profit = sales − opex − purchase` (همان یک‌بار کسر).

نکتهٔ کلیدی: رکورد `salary` در `ptf_crm_sharetx` **هزینه نیست** — یک بستانکاری (بدهی شرکت به سهامدار) است و در فرمول سود وارد نمی‌شود؛ فقط در ماندهٔ حساب سهامدار (کشف/بستانکاری) دیده می‌شود. پس **دوباره‌شماریِ هزینه رخ نمی‌دهد**. همچنین سود پروژه (`ptfProjectProfitIRR`) فقط هزینه‌های مستقیمِ همان پروژه را کسر می‌کند و حقوق سهامدار (opex عمومی) را در بر نمی‌گیرد → بین «سود پروژه» و «opex عمومی» هم تداخلی نیست.

**آزمایش عددی** (دو سهامدار ۵۰/۵۰؛ A موظف با حقوق ۱٬۲۰۰/سال که ماهانه گرفته؛ وصولی ۵٬۰۰۰؛ کف صفر؛ ۶۰٪ تقسیم):

- `opex=1200` ← `netCash=3800`، `cashEnd=3800`، `distributable=2280`، سهم هر یک `gross=1140`.
- دفتر A: بستانکاری حقوق `+1200` − برداشت/پرداخت `−1200` = صفر؛ پس از تقسیم `+1140`.
- جمع دریافتی A = ۱٬۲۰۰ (حقوق) + ۱٬۱۴۰ (سود)؛ B = ۱٬۱۴۰.
- خروجی = ۱٬۲۰۰ + ۲٬۲۸۰ = ۳٬۴۸۰ و ۱٬۵۲۰ به کف → **کاملاً متعادل؛ هیچ مبلغی دو بار داده نمی‌شود.**

---

## ۳) یافتهٔ اصلی — یک ناسازگاری مفهومی (نه خطای عددی)

ستون «ماندهٔ قابل تسویهٔ امسال» در جدول تقسیم سود (`fiscal.js:341-343`):

```js
settleYear = gross − adv   // adv = draw/advance/debit همان سال
```

چون **پرداخت حقوق هم با `type:'draw'` ثبت می‌شود** (همان تابع `ptfShareDraw` برای علی‌الحساب هم استفاده می‌شود)، پرداختِ حقوق در `adv` محاسبه و از سهم ناخالص کسر می‌شود:

- اگر حقوقِ گرفته‌شده از سهم سود بیشتر باشد، این ستون **منفی** بزرگ نشان می‌دهد و پیام می‌دهد «برداشت بیش از سهم = بدهکار».
- درحالی‌که حقوق یک **هزینهٔ جدا** است نه «علی‌الحسابِ سود»؛ پس نمایشِ منفیِ این ستون برای سهامدار موظف **گمراه‌کننده** است.

نکتهٔ مهم: `ptfFiscalDividendApply` در نهایت **`s.gross` را ثبت می‌کند، نه `settleYear` را** (`fiscal.js:370`)؛ یعنی این کسر در پرداخت واقعی اعمال نمی‌شود و فقط در **نمایش** دیده می‌شود. پس خطای عددی/دوباره‌پرداخت ندارد، ولی منطقِ «حقوق=هزینه» با منطقِ «برداشت=علی‌الحساب سود» در یک جا جمع شده و پیام متناقضی به کاربر می‌دهد.

---

## ۴) سه نگرانی واقعی برای تصمیم‌گیری

### ۴.۱ (سیاست/حسابداری) — آیا «حقوق سهامدار» واقعاً هزینه است یا تقسیم پنهان سود؟
این مهم‌ترین مسئله است و صرفاً با کد قابل حل نیست:
- اگر حقوق، **مزد بازار برای کار واقعی** باشد ← درست است که opex شود و سود را کم کند.
- اگر حقوق **بالاتر از مزد واقعی** باشد (استخراج سود به نام حقوق — رایج در شرکت‌های کوچک) ← سود واقعی را **پنهان/کم‌نمایش** می‌دهد، سهمِ قابلِ تقسیمِ همهٔ سهامداران را می‌کاهد، و به سهامدارِ موظفِ صاحبِ حقوق، درآمدِ تضمینیِ «پیش از سود» می‌دهد که سهامدار غیرموظف از آن بی‌بهره است (بی‌عدالتی سهامداری).

### ۴.۲ (منطق نقدی) — حقوقِ تعلّق‌گرفته ولی «پرداخت‌نشده» به‌عنوان خروجی نقد شمرده می‌شود
در `ptfFiscalCashData`، کلِ opex (شامل حقوق) بدون بررسی «آیا واقعاً پرداخت شده» به‌عنوان خروجی نقد کسر می‌شود. اگر حقوق ماهانه پرداخت شود ← درست است. اگر حقوق تعلّق بگیرد ولی نقد پرداخت نشود (شرکت کمبود نقد دارد) ← `cashEnd`/مبلغ قابل تقسیم **کم‌نمایش** می‌شود، درحالی‌که آن وجه هنوز در شرکت است و به‌صورت بستانکاری در حساب سهامدار مانده است.

### ۴.۳ (ورودی کاربر) — امکان ثبتِ دوبارهٔ هزینهٔ حقوق دستی
چیزی جلوی ثبتِ دستیِ یک opex «حقوق و دستمزد» در کنار رکورد خودکار را نمی‌گیرد (اتصال `shareTx` فقط جلوی تکرارِ خودکار را می‌گیرد). ورودی دستیِ تکراری می‌تواند هزینه را دو برابر کند.

---

## ۵) جمع‌بندی / پاسخ به سؤال

**پاسخ:** در کدِ فعلی، قراردادنِ حقوق سهامدار در هزینه‌های جاری **دوباره‌شماریِ عددی در محاسبهٔ سود ایجاد نمی‌کند** — حقوق دقیقاً یک‌بار (به‌عنوان opex) از سود کسر می‌شود، و دفتر سهامدار جداگانه بستانکاری/پرداخت را پیگیری می‌کند (با آزمایش عددی راستی‌آزمایی شد).

ولی **منطقِ حاکم** سه ابهام دارد که باید به‌عنوان تصمیم مدیریتی روشن شود:

1. **تعیین تکلیف ماهیت «حقوق سهامدار»:** اگر مزد واقعی است → درست است که opex شود. اگر استخراج سود است → سود را کم‌نمایش می‌دهد و بی‌عدالتی سهامداری ایجاد می‌کند. این **سیاست حساب‌داری** است و باید صریح شود.
2. **جداسازی «پرداخت حقوق» از «علی‌الحساب سود» در دفتر سهامدار:** پیشنهاد می‌شود پرداخت حقوق یک نوع جداگانه (مثلاً `salary_payment`) بگیرد تا در `adv` (ماندهٔ قابل تسویه) لحاظ نشود و ستون «ماندهٔ قابل تسویهٔ امسال» برای سهامدار موظف گمراه‌کننده نباشد.
3. **پرداخت‌نشدگی حقوق در منطق نقدی:** در صورتی که حقوق‌ها نقد پرداخت نمی‌شوند، باید یا فقط حقوقِ پرداخت‌شده در خروجی نقد لحاظ شود، یا این رفتار صریحاً مستند شود.

---

## ۶) تغییر پیشنهادی (اختیاری — منتظر تأیید ماهیت حقوق)

| اقدام | اثر |
|---|---|
| (الف) نوع `salary_payment` جدا برای پرداخت حقوق + حذف آن از `adv` در `ptfFiscalCashDistribution` | رفعِ پیامِ منفیِ گمراه‌کنندهٔ «ماندهٔ قابل تسویه»؛ بدون تغییر در مبلغ پرداختی واقعی |
| (ب) در صورت تأیید «حقوق = استخراج سود»: حذف حقوق سهامدار از opex و انتقال به کسر از سهم هر سهامدار | سود واقعی شفاف می‌شود؛ بی‌عدالتی سهامداری رفع می‌شود |
| (ج) هشدار/جلوگیری از ثبتِ دستیِ opex حقوقِ تکراری | جلوگیری از هزینهٔ دوباره |

*تولیدشده توسط Arena Agent — بازبینی فقط‌خواندنی؛ تغییری در کد اعمال نشده و منتظر تأیید ماهیت حقوق سهامدار است.*
