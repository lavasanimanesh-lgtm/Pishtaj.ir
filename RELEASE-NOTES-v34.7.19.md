# Release Notes v34.7.19 — فاز A نقشهٔ فازبندی: ایمنی دادهٔ مالی

تاریخ: ۱۴۰۵/۰۵/۲۶ (2026-08-17)

## زمینه

بررسی مستقلِ گزارش تلفیقی «تغییر سند قطعی برد» دو ریسک را آشکار کرد که هیچ‌کدام در آن گزارش نبودند و هر دو می‌توانستند **همین امروز دادهٔ مالی را خراب کنند**. طبق `PLAN-REMAINING-FIXES-PHASED-2026-08-17.md`، فاز A فقط و فقط این دو مورد را می‌بندد؛ هیچ قابلیتی اضافه یا حذف نشده است.

## AR-01 🔴 ابطال محلی صورتحساب غیررسمی، بستانکاری **همهٔ پرونده‌ها** را بازنویسی می‌کرد

**ریشه:** در معماری Sales-Domain v35 تخصیص‌ها روی سرور و با schema `{invoiceId, amountIRR}` ساخته می‌شوند، اما مرحلهٔ ۳ تابع `ptfUnofficialInvoiceVoid` با کلیدهای `{invoiceCd, amount}` می‌گشت. نتیجه: هیچ تخصیصی `reversed` نمی‌شد، `_stillAllocated` همیشه صفر می‌ماند، و حلقهٔ بازسازی `creditRemainIRR` — که **هیچ فیلتری روی `caseId` نداشت** — برای تمام رسیدهای `posted` کل سیستم مقدار بستانکاری را برابر کل مبلغ رسید می‌کرد. یعنی یک کلیک روی ابطال یک فاکتور، اعتبار همهٔ مشتریان را متورم می‌کرد.

**اصلاح (دو لایه):**
- **گارد ۰ (fail-closed):** در حالت `PTF_SALES_DOMAIN_V2` و تا زمانی که مسیر سروری (`void_unofficial_invoice` — فاز E) وجود ندارد، مسیر محلی اجرا نمی‌شود و به کاربر مسیر جایگزین امن اعلام می‌شود (تبدیل به فاکتور رسمی با انتخاب همین صورتحساب به‌عنوان سند مبدأ). تلاش مسدودشده در audit ثبت می‌شود.
- **دفاع در عمق:** حتی اگر آن بلوک اجرا شود، اکنون فقط رسیدهای **همان پرونده** بازنویسی می‌شوند و مبلغ تخصیص از هر دو schema خوانده می‌شود (`amountIRR` سروری و `amount` میراثی). کش `PTF.ar` هم پس از تغییر باطل می‌شود.
- **مسیر legacy (بدون v35) دست‌نخورده است.**

## AR-02 🔴 اصلاح فاکتور رسمی، فیلدهای سند را بی‌صدا حذف می‌کرد

**ریشه:** `correct_invoice` رکورد را از صفر و با فهرست ثابتی از کلیدها بازمی‌ساخت؛ هر فیلد خارج از آن فهرست در هر اصلاح پاک می‌شد:

| فیلد | مصرف‌کننده | اثر حذف |
|---|---|---|
| `pays` | ۶۷ نقطه در کلاینت (`PTF.invPaidSum`، حساب مشتری، خزانه) | پولِ ثبت‌شده از مانده ناپدید و مطالبات باز متورم می‌شد |
| `dueISO` / `dueFa` | پنل مطالبات | سررسید وصول و هشدار «سررسید گذشته» پاک می‌شد |
| `contactReq` / `contactApproved` | گردش تأیید دسترسی تماس تحصیلدار | تأیید باید از نو گرفته می‌شد |
| `offerCurrency` / `offerFxBasis` / `offerFxRateRef` | کنترل تطابق فاکتور ارزی در حساب مشتری | فرادادهٔ ارزی از بین می‌رفت |
| `advApplied` | گزارش/چاپ | سابقهٔ کسر پیش‌پرداخت گم می‌شد |

**اصلاح:** در شاخهٔ اصلاح، رکورد قبلی مبنا قرار می‌گیرد (`array_merge($oldInv,$record)`) و فقط فیلدهای محاسبه‌شدهٔ همین فرمان بازنویسی می‌شوند. `register_invoice` (ثبت جدید) دست‌نخورده است و `allocated*/open*` بلافاصله با `sd_rebuild_allocations` بازتولید می‌شوند.

**گارد مکمل:** اصلاح فقط روی **سند فعال** مجاز است (`invoice_not_active`). پیش از این، اصلاحِ یک فاکتور ابطال‌شده/جایگزین‌شده آن را بی‌صدا به `active` بازمی‌گرداند؛ UI هم دکمهٔ اصلاح را برای سند غیرفعال نشان نمی‌دهد، پس این گارد با رفتار موجود هم‌راستا و fail-closed است.

## چرا این تغییر «بدون آسیب» است

- هر دو اصلاح **مانع نوشتن غلط** هستند، نه تغییر منطق محاسبه؛ هیچ عددی در گزارش‌ها جابه‌جا نمی‌شود.
- مسیر legacy ابطال و مسیر ثبت فاکتور جدید دقیقاً مثل قبل کار می‌کنند.
- گارد AR-01 با یک شرط ساده برداشته می‌شود (وقتی فاز E مسیر سروری را اضافه کند)، و بلوک محلی آن‌گاه به‌کلی حذف خواهد شد.
- هیچ رکورد موجودی اصلاح یا مهاجرت داده انجام نشد.

## تست رگرسیون

- `_tools/uat/tester422-v34.7.19-ar-data-safety.js`: **33 PASS / 0 FAIL** — شامل: fail-closed بودن مسیر v35، محدودبودن دامنهٔ بازسازی به یک پرونده (رگرسیون مستقیم N1)، پشتیبانی از هر دو schema تخصیص، دست‌نخوردن مسیر legacy، حفظ ۸ فیلد در اصلاح فاکتور و بهداشت نسخه.
- تسترهای مرتبط بدون شکست: `tester421` (۳۸/۰)، `tester420` (۳۰/۰)، هارنس تسویه (۵ سناریو از ۵).
- گیت CI: ۳۷ PASS / ۲ FAIL — همان دو شکست پیش‌ازموجود (`tester403`, `tester415`).
- اجرای کامل سوئیت (۴۰۵ تستر) و مقایسهٔ خط‌به‌خط با baseline پیش از فاز: **۶۲ شکست، صفر شکست جدید**.
- بهداشت نسخه در ۶ نقطه روی `v34.7.19`.

## گام بعد

طبق نقشه، فاز B (بازگرداندن قابلیت‌های مسدود: باگ P1 دیالوگ صدور غیررسمی، شاخهٔ صدور مجدد، تعریف دوگانهٔ گارد قفل برد).
