# یادداشت انتشار v34.39.1 — HOME-ELECTRIC-3D (جریان برق روی کارت/دکمه، شیتِ منوی پایین موبایل، نمای روز/شب، سه‌بعدی‌سازی)

**تاریخ:** 2026-09-17
**بازوی تغییر:** Arena Agent
**کانال:** سایت عمومی — صفحهٔ اول (لایهٔ نمایشی v34.39.1؛ محتوا/سئو/فرم‌ها بدون تغییر)
**قرارداد:** tester663 (۳۰ سنجه) + هم‌ترازی پین‌ها با ابزار رسمی `bump-version-pins.js`

## دستور کارفرما
> «جریانی از برق بخواهم از کارت‌ها و دکمه‌ها عبور کند؛ با هاور رنگِ زیبایی بگیرند؛ منوی بالا در موبایل حذف و به نوار پایین منتقل شود؛ روی منوها انیمیشن؛ کلید روز/شب بالا؛ استعلام در داک کافی است و RFQ از نوار پایین حذف و دکمه به وسط منتقل شود با جلوهٔ سه‌بعدی؛ فلش‌ها مینیمال‌تر؛ اعداد «دپارتمان تامین/صنعت هدف» فارسی شوند.»

## آنچه انجام شد
1. **جریان برق (Current-Ring):** حلقهٔ نورانی گرادیانی (mask روی `padding-box/border-box` — سازگار با همهٔ مرورگرها) که روی لبهٔ `.btn-primary`، تماس هدر، CTA داک، چت داک و دکمهٔ فایندر **مداوم می‌چرخد** و روی کارت‌های خدمات/مسیر سریع/چرایی **هنگام هاور** جاری می‌شود؛ کاملاً زیر `prefers-reduced-motion: no-preference`؛ صفر JS اضافه.
2. **هاورِ رنگی:** کارت‌ها گرادیان کرمی + حاشیهٔ شعله‌ای + سایهٔ کهربایی و رنگی‌شدن تیتر؛ ghost-btn و تب‌ها هم تینت گرم؛ فیلتر Saturate روی CTA اصلی.
3. **موبایل:** دکمهٔ منوی بالا حذف شد؛ `#mainNav` به **شیت پایین** (بالای داک، `max-height: min(60dvh,520px)`، اسکرول داخلی، استگر ورودی پلکانی، بک‌دراپ/قفل اسکرول/ESC) تبدیل شد؛ مگا/آکاردئون موجودِ منو همان‌جا کار می‌کند.
4. **داک جدید منومحور:** خانه / تماس / **منو (وسط، برجسته، سه‌بعدی، همبرگر↔✕)** / واتس‌اپ / چت — RFQ حذف شد؛ مسیر استعلام در همان منو و هیرو باقی است («استعلام کافیه»); داک هنگام باز بودن شیت مخفی نمی‌شود؛ تعادل هدر موبایل (chip تماس در ≤۵۶px حذف می‌شود چون شماره در داک هست).
5. **نمای روز/شب:** کلید خورشید/ماه با مورف در هدر بالای صفحه؛ ماندگاری با کوکی `ptf_theme` (بدون localStorage — سازگار با نگهبان A10)؛ اسکریپت ضدFOUC در `<head>`؛ تشخیص `prefers-color-scheme` برای بار اول؛ هماهنگی تگ `theme-color`؛ **پالت کامل شبِ صفحهٔ اول**: بدنه/هدر/کارت‌ها/فرم تماس/داک/شیت/مارکی‌ها با سرمهٔ گرافیتی و متن‌های بازتنظیم‌شده (قاعدهٔ attribute-override برای استایل‌های درون‌خطی).
6. **انیمیشن منوها:** ورود پلکانی آیتم‌های منوی دسکتاپ + لیفت هاور (زیر no-preference) + استگر شیت موبایل + پرش ریز `:active`.
7. **سه‌بعدی:** `perspective` روی گرید خدمات + تیلت `rotateX/rotateY` دنبال‌کنندهٔ نشانگر (--rx/--ry با rAF) + `translateZ` روی بدنهٔ کارت‌ها + لیفت ۳بعدی دکمه‌های هیرو با فشرده‌شدن در `:active` + سایهٔ فیزیکی لایه‌ای (inset highlight) برای CTA داک + تیلت ملایم پنل هیرو.
8. **فلش‌ها:** هر `←/→` انتهایی در لینک‌ها با `<i class="arw">` درپوش می‌خورد (بدون JS همان متن؛ بدون افت SEO) و روی هاور ۵px فنری به جلو می‌لغزد و پررنگ می‌شود — مینیمال.
9. **اعداد فارسی:** پنل هیرو «۳ دپارتمان تامین / ۶+ صنعت هدف».

## QA
- `tester663`: **۳۰/۳۰** · `tester661/662` (پین cache-bust خودکار به 34.39.1): سبز · `tester210/216/318/622/623/627/660/578`: سبز
- `arch-guard`: PASS (A6 هم‌تراز؛ A10 بدون تخلف جدید؛ A9/A4 سبز)
- پارسر HTML سالم؛ `node --check` main.js/ptf-motion.js سبز؛ حجم‌ها: home.css ≈ ۳۷KB خام (gzip ≈ ۷KB)، ptf-motion ≈ ۱۰KB با defer

## فایل‌ها
`index.html` (کلید تم + بوت‌استرپ + داک جدید + اعداد + ورژن‌ها) · `assets/css/home.css` (لایهٔ v34.39.1) · `assets/js/ptf-motion.js` (شیت/تم/تیلت/فلش) · `assets/css/style.css` (ثابت) · نقاط نسخه + `RELEASE-NOTES-v34.39.1.md` + `tester663` + ثبت در گیت
