# 🤖 ریلیز v14.4 — اسپرینت ج «دستیار قابل‌اتکا»

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **اسپرینت:** ج از برنامه ۶ اسپرینتی مصوب | **تسترها:** ۵۱/۵۱ سبز ✅ | **Audit:** PASS ✅

این اسپرینت چهار مشکل اصلی دستیار هوش مصنوعی را ریشه‌ای حل کرد: از بین رفتن نتایج، کاراکترهای نامربوط، نام‌های انگلیسی خراب (Mjtm Snti…/Mrad) و نبود تشخیص رابط خرید.

---

## ✅ محتوای ریلیز (۴ استوری)

### US-378 — نتایج دستیار دیگر گم نمی‌شوند 🕓
- **ریشه مشکل:** نتیجه فقط در DOM پنل بود؛ با رفتن به هر ماژول دیگر، توکن مصرف‌شده و نتیجه هر دو از دست می‌رفت.
- حالا **آخرین نتیجه هر ۵ تب** (استخراج اسناد، مترجم، شناساگر، خلاصه‌ساز، نامه) به‌صورت شخصی برای هر کاربر ذخیره می‌شود و با برگشت به دستیار **خودکار بازیابی می‌شود** — بدون مصرف حتی یک توکن.
- دکمه جدید **«🕓 نتایج اخیر»** بالای دستیار: ۱۰ خروجی آخر با زمان، نوع و پیش‌نمایش + کپی 📋 + حذف 🗑 + پرش به تب مربوطه.
- هرس خودکار (۱۰ مورد + سقف حجمی) تا مرورگر سنگین نشود؛ تاریخچه شخصی است و در سینک بین‌کاربری قرار نمی‌گیرد.

### US-379 — پایان کاراکترهای نامربوط در متن نامه 🧹
- **ریشه مشکل:** تولید نامه از ترفند اکشن «خلاصه‌ساز» استفاده می‌کرد و خروجی خام LLM (Markdown، JSON، `\n`های خام) مستقیم نمایش می‌یافت.
- **اکشن اختصاصی `letter`** در سرور ساخته شد با قواعد سخت‌گیرانه: فقط متن خالص فارسی، بدون هیچ نماد Markdown.
- **پاک‌ساز سراسری `ptfAiClean`**: حذف بلوک کد، `**بولد**`، سرفصل‌ها، JSON دورریز، کاراکترهای کنترلی — روی نامه، خلاصه‌ساز و بررسی قرارداد اعمال شد.
- متن نامه حالا مستقیم قابل کپی و ذخیره در ماژول مکاتبات است.

### US-362 — نام انگلیسی هوشمند + تشخیص رابط خرید (هر ۸ AC) 👤
- **AI حالا هم‌زمان نام انگلیسی شرکت را برمی‌گرداند** و هنگام ثبت خودکار مشتری، فیلد `coEn` پر می‌شود — سند چاپی دیگر «Mjtm Snti Zoub Ahn Pasargad» نمی‌سازد.
- **تشخیص کارشناس خرید از هر نوع فایل** (عکس/PDF/…): AI فقط نامی که صراحتا نقش خرید/تدارکات دارد را برمی‌گرداند (نه امضاکنندگان مدیریتی). پیشنهاد با پیش‌نمایش کامل (نام فارسی + انگلیسی + سمت) و **تیک تایید شما** نمایش داده می‌شود — بدون تایید هیچ چیزی ثبت نمی‌شود؛ رابط هم‌نام تکراری نمی‌شود.
- با تایید: رابط با **نام انگلیسی** به اشخاص رابط مشتری اضافه می‌شود → فیلد Attention پیشنهادها از همان استفاده می‌کند.
- در چاپ اسناد، اولویت همیشه با نام انگلیسی ذخیره‌شده است؛ ترانویسی فقط پشتیبان است.
- دکمه جدید در فرم مشتری: **«🤖 تولید نام انگلیسی با AI»** برای مشتریان قدیمی بدون coEn (بدون AI هم با ترانویسی اصلاح‌شده کار می‌کند).

### US-360 — ترانویسی دقیق: مراد → Morad ✍️
- **ریشه مشکل:** موتور حرف‌به‌حرف واکه‌های کوتاه نانوشته فارسی را بازسازی نمی‌کرد (م‌ر‌ا‌د = Mrad).
- **فرهنگ ۱۵۰+ نام و واژه رایج** با املای استاندارد لاتین اضافه شد (مراد→Morad، محمد→Mohammad، حسین→Hossein، پورشاد→Pourshad، پتروشیمی→Petrochemical و…) — اولویت اول جستجو.
- برای نام‌های خارج از فرهنگ، **قاعده واکه‌گذاری** میان خوشه‌های صامت اضافه شد تا خروجی همیشه تلفظ‌پذیر باشد.
- **ترتیب انگلیسی نام سازمان‌ها** هم اصلاح شد: «مجتمع صنایع فولاد پاسارگاد» → `Pasargad Steel Industries Complex` (ساختار مضافی فارسی معکوس می‌شود).
- نام‌هایی که دستی لاتین وارد شده‌اند هرگز بازنویسی نمی‌شوند.

---

## 🔬 کیفیت

| سنجه | نتیجه |
|-------|--------|
| تستر جدید | tester64-v144 با **۵۷ چک** — شامل تست اجرایی واقعی ترانویسی (Morad/Mohammad/Pasargad Steel…) و پاک‌ساز (Markdown/JSON/بولت) ✅ |
| رگرسیون کامل | **۵۱/۵۱ تستر سبز** |
| audit.py | PASS (تنها هشدار قدیمی تصاویر سایت) |
| سینتکس | ai-workbench / workflow / offers / contracts / sw + توازن llm.php ✅ |

## 📦 نصب
`pishtaj-release-v14.4.zip` — آپلود با File Manager؛ کش PWA خودکار نو می‌شود (`ptf-crm-v14.4`).
**نکته:** فایل `api/llm.php` هم تغییر کرده (اکشن letter + فیلدهای جدید ocr) — حتما جایگزین شود.

## 🔜 بعدی طبق برنامه مصوب
**اسپرینت د «آماده بهره‌برداری واقعی»** = US-377 (🔴 پاک‌سازی داده‌های آزمایشی Go-Live) + US-368 (نقش‌های اصلی تک‌نمونه) + US-376 (ارسال مجدد پیامک ورود) + US-371 (قفل کامل افزودن فایل به پرونده مختومه بدون فاکتور) — با اعلام شما آغاز می‌شود.
