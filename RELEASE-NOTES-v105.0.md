# 🚀 یادداشت‌های انتشار اسپرینت ۱۰۵ (Release Notes - v105.0)

**تاریخ انتشار:** ۱۴۰۵/۴/۱۵ (July 2026)  
**هدف اسپرینت:** سیستم ارجاعات بدون قفل و موتور بایگانی حذفیات با ثبت دلیل (اولویت ۴ بک‌لاگ محصول)  
**تیم توسعه:** تیم ارشد بازرگانی و توسعه نرم‌افزار PTF  

---

## 📋 تغییرات اعمال‌شده در این نسخه

### ۱. 📨 سیستم ارجاعات بدون قفل و همگانی (Unrestricted Referral System)
* **فایل تغییر یافته:** `crm/bridge.js`
* **شرح تغییر:** محدودیت قبلی که امکان ارجاع کارها را فقط به نقش‌های ارشد (`isSenior`) و فقط برای کاربران با دسترسی فروش محدود می‌کرد، کاملاً برداشته شد. 
* اکنون در تیم چابک ۳ نفره PTF، هر یک از اعضا (مدیر، کارشناس فروش، کارشناس مهندسی بازرگانی و حتی حسابدار) می‌توانند هر استعلام، پیشنهاد یا پرونده‌ای را همراه با یادداشت و اقدام مورد نظر به عضو دیگر ارجاع دهند.
* **قانون سیستم:** منطبق بر صورت‌جلسه هدف، تنها شرط حفظ‌شده این است که *«هیچ‌کس نمی‌تواند کاری را به خودش ارجاع دهد»*.

### ۲. 🗑️ موتور حذف با ثبت دلیل (Reasoned Deletion Engine)
* **فایل‌های تغییر یافته:** `crm/guards.js`، `crm/workflow.js`
* **شرح تغییر:** قفل‌های ممانعت از حذف و همچنین پیغام‌های تأییدیه ساده (`confirm`) جایگزین یک پنجره هوشمند و حرفه‌ای شدند.
* هنگام کلیک روی دکمه حذف هر استعلام (RFQ)، مشتری، تأمین‌کننده یا کالا، پنجره **«🗑 بایگانی و حذف ایمن با ثبت دلیل»** باز می‌شود که از کاربر می‌خواهد دلیل حذف (مثل: انصراف کارفرما/لغو درخواست، ثبت تکراری/آزمایشی، اشتباه تایپی/ورود اطلاعات نادرست، عدم امکان تأمین فنی/مالی و...) را به همراه توضیح اختیاری مشخص کند.

### ۳. 📊 ماژول بررسی‌های آماری بایگانی حذفیات (Deletion Audit Trail)
* **فایل‌های تغییر یافته:** `crm/reports.js`، `crm/index.html`
* **شرح تغییر:** بخش جدیدی تحت عنوان **«🗑 بایگانی حذفیات و بررسی‌های آماری دلایل»** به ماژول گزارشات مدیریتی اضافه شد. تمام رکوردهایی که توسط اعضای تیم حذف می‌شوند، به صورت دائمی همراه با شناسه، عنوان، دلیل دقیق حذف، نام حذف‌کننده و تاریخ/ساعت در این جدول بایگانی می‌شوند تا مدیر سیستم بتواند دلایل توقف یا لغو استعلام‌ها را تحلیل کند.

---

## 🧪 وضعیت کنترل کیفیت (QA Verification)
* تمام فایل‌های جاوااسکریپت اصلاح‌شده بدون خطای سینتکس کامپایل شدند.
* تست ارجاع همگانی بین نقش‌های مختلف بدون قفل انجام شد.
* ثبت دلیل حذف و انتقال موفق به جدول گزارشات بایگانی تأیید شد.
