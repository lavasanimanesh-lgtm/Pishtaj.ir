# 🔐 ریلیز v21.3 — فیلتر پیشنهاد بر مشتری (US-450) + ریشه‌کنی دفترچه پیامکی (BUG-038)

**مبنا:** v21.2  
**تاریخ:** ۱۴۰۵/۰۴/۲۰

---

## یوزر استوری US-450 — فیلتر پیشنهادات صادره بر حسب مشتری

- در ماژول **پیشنهاد**: کشوی «🏢 همه مشتریان / انتخاب مشتری»
- با انتخاب یک مشتری، **همه پیشنهادهای فنی (TO) و مالی (CO/TC)** همان کارفرما در:
  - نمای **جدول**
  - نمای **کانبان**
  فیلتر می‌شوند (هم‌راستا با تب TO/CO/همه + جستجوی متنی)
- تطبیق: `buyerCd` دقیق + fallback نام (`buyerCo` / `coEn`) برای اسناد قدیمی
- ماندگاری انتخاب در `localStorage` (`ptf_off_cust_filter`)
- شمارنده راهنما: «فیلتر مشتری: X — N پیشنهاد»

## BUG-038 — دفترچه تلفن پیامکی: تب مشتریان خالی

### ریشه‌یابی عمیق
1. **علت اصلی:** از v13.7 (`phonefmt` / US-338) شماره‌های مشتریان با **ارقام فارسی** ذخیره می‌شوند (`۰۹۱۲…`).  
   تابع `normMob` در `sms.js` با `replace(/\D/g,'')` ارقام فارسی را «غیررقم» می‌دانست و **کل شماره را حذف** می‌کرد → سینک خودکار صفر مخاطب برای مشتریان.
2. **علت تشدید:** فقط `people[].mobs` و `phones[k=mob]` و در نبودشان `e.ph` خوانده می‌شد؛  
   **`coTels` اصلاً جمع نمی‌شد** و `tels` موبایل‌مانند / `phones` بدون `k` نادیده گرفته می‌شد.

### درمان
- لاتین‌سازی ارقام قبل از نرمال (`ptfToEnDigits` / fallback)
- جمع کامل: people.mobs + people.tels (اگر 09) + phones + **coTels** + ph/mob/tel/phone
- فقط موبایل `09xxxxxxxxx` وارد دفترچه می‌شود (ثابت عمداً نه)
- هوک پس از `saveCust2` / `saveSup2` برای تازه‌سازی خودکار دفترچه
- گاردهای BUG-018 (سپر داده‌صفر + bootstrapped) حفظ شد

### اقدام کاربری پس از آپدیت
1. سامانه پیامکی → **🔄 سینک از مشتریان/تامین‌کنندگان**
2. اگر مخاطب دستی قبلاً از دست رفته: **🩹 بازیابی دفترچه از بک‌آپ سرور**

---

## تست
- **tester129-v213:** 42 PASS / 0 FAIL (ساختاری + رفتاری شامل ارقام فارسی و collect واقعی)

## فایل‌ها
- `crm/sms.js`
- `crm/offers.js`
- `crm/kanban.js`
- `crm/index.html` / `crm/sw.js` → **v21.3**

## نصب
extract-overwrite + Ctrl+F5  
SW: `ptf-crm-v21.3`
