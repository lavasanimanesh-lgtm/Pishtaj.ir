# Release Notes — v31.7.30

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.30
**نوع:** 🟢 ممیزی درخواستی کارفرما + بهبود کم‌ریسک — زنجیره RFQ هوشمند سایت ↔ CRM ↔ رهگیری (US-RFQ-TRACK-AUDIT + US-RFQ-TRACK-POLISH)

---

## 🎯 سوال کارفرما

آیا RFQ هوشمند سایت کاملاً به سیستم داخلی لینک است؟ کد رهگیری واقعی است و مشتری می‌تواند هر لحظه وضعیت پیشنهادش را ببیند؟ اگر نه، زیرساختش هست؟ در این صورت بدون هیچ باگی انجام شود.

## ✅ نتیجه ممیزی: بله — زنجیره کامل از قبل ساخته شده و سالم است (با E2E سرور واقعی اثبات شد)

| حلقه زنجیره | وضعیت | جزئیات |
|-------------|--------|--------|
| ثبت از سایت (`rfq/`) | ✅ لینک واقعی | فرم به `add_rfq_site` پست می‌کند؛ کپچا اجباری؛ rate-limit ۱۰/ساعت؛ پیوست به فضای ابری |
| کد رهگیری | ✅ **واقعی و سرورساز** | `PTF-RFQ-{سال}-NNNN` با شمارنده اتمیک سرور (نه تصادفی کلاینت)؛ به کاربر نمایش و مستقیم به `tracking/?code=…` لینک می‌شود |
| ورود به CRM | ✅ | صندوق `get_inbox` → تایید ادمین (`rfqApprove`) → **با همان کد** وارد چرخه درخواست‌ها + ساخت خودکار مشتری (US-380) + انتقال پیوست سایت |
| بازتاب وضعیت | ✅ | هر تغییر وضعیت درخواستِ site از **هسته واحد** `ptfRfqSetStatus` می‌گذرد (فرم، کانبان، پرونده فروش، تامین buycompare — همه) → `set_status` سرور → رهگیری |
| رهگیری مشتری | ✅ | `track` عمومی با rate-limit ۶۰/ساعت؛ فقط فیلدهای امن (کد/شرکت/دسته/وضعیت/تاریخ — نه تلفن/ایمیل/متن)؛ صفحه `tracking/` آماده |
| **E2E اثبات** | ✅ | روی سرور PHP واقعی: ثبت → کد `PTF-RFQ-1405-0001` → رهگیری pending → تایید CRM → رهگیری approved → تغییر به stCO/st8/st7 → رهگیری هر بار وضعیت جدید؛ کد جعلی → notfound |

## ✨ تنها شکاف واقعی که پیدا و رفع شد (US-RFQ-TRACK-POLISH)

متن وضعیت‌های **داخلی و عملیاتی** CRM عیناً به مشتری نمایش داده می‌شد — مثلاً «🏭 در حال تامین توسط تامین‌کننده» (اطلاعات داخلی زنجیره تامین که جای نمایش به مشتری نیست).

**رفع:** نگاشت سمت سرور در `track` — هر وضعیت داخلی به پیام مشتری‌پسند ترجمه می‌شود:

| وضعیت داخلی | مشتری می‌بیند |
|--------------|----------------|
| در انتظار تایید مدیران | ثبت شد — در انتظار بررسی کارشناسان |
| stTO / stCO | در حال تهیه پیشنهاد فنی / مالی |
| st8 (تامین توسط تامین‌کننده) | سفارش در حال تامین است |
| st7 | تحویل شد — با سپاس از اعتماد شما |
| rejected | دلیل ثبت‌شده حفظ می‌شود |

رهگیری تامین‌کننده و داده‌های CRM کاملاً دست‌نخورده — تغییر فقط در پاسخ عمومی `track` برای type=rfq.

## 📁 فایل‌های تغییریافته

`api/crm.php` (نگاشت pubMap در track — تنها تغییر کد)، `crm/index.html` + `crm/sw.js` + `crm/clear-cache.html` (bump)، `_tools/uat/tester205` (جدید — سند ممیزی زنجیره)

## 📊 نتایج گیت

- **E2E سرور PHP واقعی:** چرخه کامل ثبت→رهگیری→تایید→۳ تغییر وضعیت→رهگیری + نگاشت مشتری‌پسند ✅
- تستر جدید `tester205`: **۱۸/۱۸ PASS** — ممیزی هر ۵ حلقه زنجیره + نگاشت polish
- رگرسیون کامل: **183/183 فایل PASS — 4147/4147 چک PASS** | `audit.py` (۱۰ بخش): PASS | `php -l`: سالم

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| زنجیره RFQ سایت | بدون تغییر رفتاری — فقط متن نمایشی رهگیری بهتر شد | صفر |
| رهگیری تامین‌کننده | دست‌نخورده (نگاشت فقط type=rfq) | صفر |
| داده/سینک/CRM | صفر تغییر | صفر |

## 📋 راستی‌آزمایی توسط کارفرما (۵ دقیقه)

1. از صفحه سایت `rfq/` یک استعلام آزمایشی ثبت کنید → کد `PTF-RFQ-…` بگیرید.
2. با کد در `tracking/` رهگیری کنید → «ثبت شد — در انتظار بررسی کارشناسان».
3. در CRM (صندوق درخواست‌ها) تایید کنید → رهگیری مجدد → «تایید شد — در حال بررسی فنی و تامین».
4. وضعیت را در کانبان به «در حال تامین» ببرید → رهگیری → «سفارش در حال تامین است» (نه متن داخلی).

---
*توسعه‌دهنده: PTF Development Team — 2026-07-20*
