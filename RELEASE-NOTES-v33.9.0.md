# یادداشت‌های انتشار (Release Notes — Version 33.9.0)

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (PR #11 باز تا پایان مهاجرت MySQL)
**تمرکز ریلیز:** ① تحلیل کامل منطق سود/تقسیم سهامداران (سند + باگ‌ها + پیشنهادات) ② مینیمال‌سازی دکمه‌های درخواست‌ها ③ پوشش سراسری فیلدهای مبلغ (جداکننده + به‌حروف + ارقام دوجهته).

---

## 🚀 دستاوردهای کلیدی نسخه ۳۳.۹.۰

### ۱. تحلیل منطق محاسبه سود و تقسیم بین سهامداران
سند `crm/ANALYSIS-PROFIT-DIVIDEND-2026-08-02.md` — پاسخ سه پرسش کارفرما + ۷ باگ مستند + ۶ پیشنهاد رفع:

**پاسخ پرسش‌ها:**
- **چک‌های در راه از درآمدها کسر نمی‌شوند**؛ اما در پروژه‌های ارزی، چک واردهٔ وصول‌نشده «به‌عنوان وصول» در درآمد وارد می‌شود (ناسازگار با ریالی).
- **بدهی‌های بازِ خریدهای ثبت‌شده کسر می‌شوند** (مبنای تعهدی)؛ اما فاکتورهای تأمین‌کنندهٔ خارج از buycmp (خدمات و…) از سود کسر نمی‌شوند.
- **مطالبات باز در پروژه‌های ریالی در درآمد لحاظ می‌شوند** (فاکتور کامل)؛ در ارزی فقط وصولی‌ها.

**باگ‌های کلیدی:** `BUG-P-2` (تقسیم بدون سقف نقدینگی — بحرانی)، `BUG-P-6` (تقسیم فقط کاربرگ است؛ هیچ ثبت واقعی در دفتر سهامداران نیست — بحرانی)، `BUG-P-1` (دو مبنای ناسازگار درآمد)، `BUG-P-3` (چک در راه در درآمد ارزی)، `BUG-P-4` (بدهی خدماتی غایب)، `BUG-P-5` (چک در راه بدون فاکتور گم می‌شود)، `BUG-P-7` (چک صادره در تقسیم لحاظ نمی‌شود).

**پیشنهادات:** ① سود تعهدی (گزارشی) + سود نقدی قابل تقسیم (با سقف نقدینگی) ② یکسان‌سازی مبنای درآمد (پیشنهاد: هر دو تعهدی) ③ اثر مالی چک وارده فقط هنگام وصول ④ کسر بدهی باز تأمین‌کننده از سود ⑤ دکمهٔ واقعی «ثبت تقسیم سود» در دفاتر سهامداران ⑥ نمایش چک در راه در ترازنامه. — **منتظر تصمیم کارفرما برای اجرا.**

### ۲. گزینه‌های مقابل درخواست‌ها مینیمال شد (مثل پیشنهادات)
ستون اکشن جدول «درخواست‌ها» (مشاهده / ویرایش-حذف / ارجاع) از دکمه‌های متنی بلند به **دکمه‌های آیکونی ۳۲×۳۲ با tooltip** (👁️ ✏️ 📨) تغییر کرد — هم‌الگو با جدول پیشنهادات.

### ۳. پوشش سراسری فیلدهای مبلغ (جداکننده + به‌حروف + ارقام دوجهته)
- **moneyx.js** (`ptfEnDigits` / `ptfFaDigits`): تبدیل دوجهتهٔ ارقام فارسی/عربی ↔ انگلیسی — برای قالب‌های انگلیسی، عدد فارسی → انگلیسی.
- **فرمت خودکار هنگام blur** برای فیلدهای «مبلغ‌مانند» که `data-money` ندارند: تشخیص بر اساس id/placeholder (مبلغ/قیمت/هزینه/حقوق/اعتبار/پرداخت/دریافت/مانده و amt/price/amount/salary…) با **خروج امن** (نرخ/درصد/تعداد/روز/تاریخ/نرخ مرجع) و opt-out با `data-nomoney="1"` → جداکنندهٔ هزارگان + «مبلغ به حروف» زیر فیلد.
- فیلدهای مبلغِ فرم‌های چک (تکی/چندتایی/ثبت/دستیار AI) از قبل `data-money` داشتند (تأیید شد).

---

## 📋 تغییرات فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/ANALYSIS-PROFIT-DIVIDEND-2026-08-02.md` | **جدید** — تحلیل کامل سود/تقسیم + ۷ باگ + ۶ پیشنهاد |
| `crm/bridge.js` | دکمه‌های درخواست‌ها → آیکونی ۳۲px با tooltip |
| `crm/moneyx.js` | ptfEnDigits/ptfFaDigits + moneyLike + focusout auto-format + opt-out |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.9.0 |
| `_tools/uat/tester289-ui-minimal-money.js` | **جدید** (۷/۰) |
| `_tools/uat/tester112-v196.js` | به‌روزرسانی انتظار ۳ لیسنر + انتخاب input listener |
| `RELEASE-NOTES-v33.9.0.md` / `REGRESSION-REPORT-v33.9.0.md` | اسناد انتشار |
| `_audit/HANDOFF-NEXT-SESSION-2026-08-01.md`، `crm/BACKLOG-USER-2026-08-01.md` | به‌روزرسانی |

---

## ⚠️ برای کاربر
1. `Ctrl+Shift+R` یا `crm/clear-cache.html` (نسخه باید **v33.9.0** باشد).
2. جدول درخواست‌ها → دکمه‌های آیکونی کوچک.
3. فیلدهای مبلغ (مثلاً در پنجره‌های ثبت) → بعد از ترک فیلد، جداکننده + مبلغ به حروف.
4. سند تحلیل سود: `crm/ANALYSIS-PROFIT-DIVIDEND-2026-08-02.md` — **پیشنهادات رفع باگ‌ها منتظر تصمیم شماست.**
