# یادداشت انتشار CRM v34.8.15

تاریخ: ۱۴۰۵/۰۶/۵ (۲۰۲۶-۰۸-۲۷)

## رفع باگ مسیر فرمان C2/C3 (گزارش کارفرما: صف همگام‌سازی بعد از یک ثبت، هر چند ثانیه تکرار می‌شد)

### ریشه
پاسخ فرمان `entity_upsert/delete`، دادهٔ projection را به‌صورت **آرایهٔ JSON** برمی‌گرداند (خروجی sd_result_data)، اما `entityApplyProjection` کلاینت فقط مقدار «رشته‌ای» را قبول می‌کرد و در نتیجه:
- projection سرور **هرگز محلی اعمال نمی‌شد** (رکورد جدید تا pull بعدی دیده نمی‌شد؛ کاربر حس می‌کرد ثبت نگرفته)
- watermark کلاینت برای همان کلید عقب می‌ماند و چرخهٔ push/pull بی‌ثبات می‌شد

### رفع
- `entityApplyProjection` حالا رشته و آرایه را می‌پذیرد و مستقیم به `ptfBApplyServerProjection` می‌دهد (که خودش هر دو نوع را پشتیبانی می‌کند) — projection بلافاصله اعمال، کش/آینه/‏krev هم‌زمان تازه، بدون dirty.
- fallback legacy هم برای آرایه اصلاح شد.
- بازبینی هم‌زمان: قفل meta در مسیر فرمان از قبل کامل است (دیسپچر زیر همان meta.json.lock قفل است) — تغییری لازم نبود.

### کنترل
گیت CI سبز + تست دستی روی استیجینگ: یک ثبت → ظاهرشدن فوری رکورد + صف همگام‌سازی خالی می‌ماند.
