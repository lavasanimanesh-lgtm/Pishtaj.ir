# RCA حادثه — حذف ۶۷ «درخواست تأمین» از سرور (ptf_crm_rfqsmart)

- تاریخ حادثه: بین ۲۰۲۶-۰۹-۰۷ ساعت ۲۱:۲۸ و ۲۰۲۶-۰۹-۰۸ ساعت ۲۳:۴۲ (شواهد بک‌آپ)
- تاریخ بررسی: ۲۰۲۶-۰۹-۰۹ | نسخهٔ اصلاح: **v34.38.12**
- شدت: از دست رفتن دادهٔ کسب‌وکاری روی پروداکشن (۶۸ → ۱ رکورد)، سرایت به همهٔ بک‌آپ‌های
  چرخشیِ پس از حادثه
- تستر: `_tools/uat/tester619-v34.38.12-mass-delete-shield.js`

---

## ۱) شواهد (اندازه‌گیری‌شده روی خود پروداکشن)

| منبع | زمان | `ptf_crm_rfqsmart` | `ptf_crm_rfqs` | مشتری |
|---|---|---|---|---|
| `monthly-latest` | 2026-09-01 00:54 | ۶۱ | ۱۲۰ | ۵۳ |
| `suspect-2026-09-06-155623` | 2026-09-06 15:56 | ۶۸ | ۱۳۷ | ۶۶ |
| `weekly-latest` | 2026-09-07 05:43 | ۶۸ | ۱۳۷ | ۶۷ |
| **`daily-2026-09-07`** | 2026-09-07 21:28 | **۶۸** | ۱۳۷ | ۶۸ |
| `daily-2026-09-08` | 2026-09-08 23:42 | **۱** | ۱۴۷ | ۷۱ |
| `hourly-latest` / `daily-2026-09-09` | 2026-09-09 00:18 | ۱ | ۱۴۷ | ۷۱ |
| فایل زندهٔ سرور (`collection_query`، بدون سنگ‌قبر) | 2026-09-09 | **۱** (`PTF-RFQS-1405-001`) | ۱۴۷ | — |

نکات کلیدی:

- `data_pull` (با اعمال سنگ‌قبر) و `collection_query` (خام) هر دو **۱** می‌دهند و در
  بایگانی حذف **هیچ سنگ‌قبری** از نوع `rfqsmart`/`supplyrfq` و هیچ `archive_purge` با
  `identities.ptf_crm_rfqsmart` وجود ندارد ⇒ حذف «ثبت‌شده» نبوده؛ رکوردها **بی‌صدا**
  رونویسی شده‌اند.
- `rev` این کلید (۲۵۹۰۹) نزدیک به rev سراسری (۲۵۹۲۷) است ⇒ نوشتنِ اخیر.
- سایر کلیدها در همان بازه **رشد** کرده‌اند (rfqs ۱۳۷→۱۴۷، مشتری ۶۸→۷۱) ⇒ خرابیِ
  کلی سرور یا بازیابی اشتباه در کار نبوده؛ فقط همین یک کلید رونویسی شده است.

## ۲) زنجیرهٔ علت

1. **نمای ناقص در مرورگر**: فاز B کلیدهای سنگین را از `localStorage` به IndexedDB منتقل
   می‌کند. `getData()` (crm/index.html) اول `localStorage` را می‌خواند و آینه را فقط
   وقتی جایگزین می‌کند که مقدار داشته باشد؛ پیش از آب‌رسانی آینه، نتیجه **`[]`** است.
2. **باگ v34.38.10 (pull اتمیک)**: bootstrap دائماً شکست می‌خورد
   (`bootstrapped:false` — همان پیام تکرارشوندهٔ «دریافت کامل CRM انجام نشد»)، پس
   نسخهٔ درست سرور هرگز روی آن مرورگر ننشست تا نمای ناقص را اصلاح کند.
3. **گیت bootstrap دور زده می‌شد**: قرارداد US-384 می‌گوید «تا سینک اولیه کامل نشده
   push ممنوع»، اما این گیت فقط در `pushDirty()` بود و مسیر فاز B (`pushViaPhaseB()`)
   از **بالای همان تابع، پیش از گیت** فراخوانی می‌شد. کاربر یک درخواست تأمین ثبت کرد،
   کل مجموعه (= یک ردیف) dirty شد و رفت.
4. **سپر سروری فقط «خالی» را می‌گرفت**: شرط US-382 در `data_push` صرفاً
   `count($newArr) === 0` را رد می‌کرد؛ «۶۸ → ۱» از آن عبور کرد.
5. **نقاط کور سپرها**: `GUARD_KEYS` کلاینت و فهرست دستی ۸ کلیدیِ قرنطینهٔ بک‌آپ
   (`ptf_rotate_backup`) هیچ‌کدام `ptf_crm_rfqsmart` را نداشتند؛ نه هشدار افت انبوه
   داده شد و نه بک‌آپ‌های بعدی قرنطینه شدند.
6. **مبنای سپر خودتخریب**: `ptfUpdateGuardCounts()` بعد از هر pull، شمارش را از همان
   نمای ناقص می‌گرفت (`rd()` برابر `null` ⇒ صفر) و مبنای سالم را پاک می‌کرد.

## ۳) اصلاحات v34.38.12

| # | لایه | اصلاح |
|---|---|---|
| ۱ | `api/crm.php` — `data_push` | ردیفِ موجود روی سرور که در payload نیست و **سنگ‌قبر ندارد** «گم‌شده» است؛ اگر گم‌شده‌ها >۳ ردیف **و** >۲۵٪ مجموعه باشند، ذخیره رد و کلید به‌عنوان `conflict` با نسخهٔ سرور برگردانده می‌شود (کلاینت merge و اجتماع را می‌فرستد) + ثبت در `sync/mass_deletion_blocked.log` + فیلد `massDeletionBlocked` در پاسخ |
| ۲ | `api/crm.php` — `ptf_rotate_backup` | پایش افت >۵۰٪ برای **همهٔ** مجموعه‌های موجود در بک‌آپ قبلی (نه فهرست ثابت ۸ کلیدی) |
| ۳ | `crm/sync.js` — `pushViaPhaseB` | گیت bootstrap برای مسیر فاز B هم اعمال شد؛ صف dirty پایدار می‌ماند و پس از bootstrap ارسال می‌شود |
| ۴ | `crm/sync.js` — `GUARD_KEYS` | از روی `SYNC_KEYS` ساخته می‌شود ⇒ پوشش خودکار همهٔ کلیدها (از جمله rfqsmart) |
| ۵ | `crm/sync.js` — `ptfUpdateGuardCounts`/`massDropCheck` | در «آینهٔ فعال ولی آب‌رسانی‌نشده» مبنا به‌روزرسانی نمی‌شود و هشدار کاذب داده نمی‌شود؛ خواندن `null` هرگز صفر تفسیر نمی‌شود |
| ۶ | `crm/sync.js` — سپر ارسال | افت شدید فقط وقتی سمت کلاینت مسدود می‌شود که نمای محلی اثباتاً ناقص باشد؛ حذف عمدیِ کاربر (که سنگ‌قبر می‌سازد) مسدود نمی‌شود |
| ۷ | `crm/sync.js` — پاسخ سپر سروری | توست + audit برای کاربر |

مرزهایی که عمداً دست‌نخورده ماندند: حذف تک‌رکوردی، حذف سنگ‌قبردار (هر تعداد)،
`restore`، `allow_wipe` («شروع بهره‌برداری واقعی»)، کلیدهای union (`audit`/`avatars`/
`notifs`) و پاک‌سازی گراف پروژه (`archive_purge` بدون `collection`).

## ۴) بازیابی دادهٔ از دست رفته

منبع سالم: **`daily-2026-09-07.json.gz`** (۶۸ رکورد `ptf_crm_rfqsmart`) و
`weekly-latest.json.gz` (همان ۶۸). نسخهٔ نجات روی دستگاه کارفرما دانلود شده است.

مسیر پیشنهادی بازیابی (افزایشی، بدون حذف):
1. رکوردهای بک‌آپ که `no` آن‌ها در فایل فعلی سرور نیست، به مجموعه اضافه شوند.
2. رکورد موجود (`PTF-RFQS-1405-001`) و هر رکورد جدیدِ پس از حادثه دست‌نخورده بماند.
3. ارسال با `by: 'rfqsmart-restore'` و ثبت audit؛ سپر جدید مانع نیست چون فقط **افزایش**
   رخ می‌دهد.

## ۵) درس‌ها

- «غیبت در snapshot مرورگر هرگز به‌معنای حذف نیست» — این قرارداد تا امروز فقط برای
  کلیدهای مالی نوشته شده بود؛ حالا برای همهٔ مجموعه‌ها (با آستانه) اجرا می‌شود.
- هر سپری که فهرست کلید **دستی** دارد، با اضافه‌شدن ماژول بعدی نقطهٔ کور می‌سازد؛ هر سه
  فهرست (سپر کلاینت، قرنطینهٔ بک‌آپ، تله‌متری) اکنون از منبع واحد مشتق می‌شوند.
- شکست bootstrap فقط «نمایش ناقص» نیست؛ تا وقتی نوشتن ممکن باشد، **خطر رونویسی** است.
