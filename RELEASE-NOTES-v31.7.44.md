# Release Notes — v31.7.44

**تاریخ:** ۱۴۰۵/۰۴/۳۰ (2026-07-21)  
**نسخه:** `v31.7.44`  
**نوع:** 🟠 CRM UX / RFQ Items — BUG-INQ-ITEMS-001

---

## 🎯 مسئله گزارش‌شده

در CRM یک درخواست با فایل ضمیمه ثبت شده بود، اما قلمی برای آن وارد نشده بود. کاربر بعداً وارد درخواست شد و «خواندن فایل» را زد؛ هوش مصنوعی نتوانست فایل ضمیمه را بخواند. بعد از آن مسیر واضحی برای افزودن اقلام درخواست به‌صورت دستی یا با فایل Excel وجود نداشت.

---

## 🔍 RCA

امکانات پراکنده‌ای برای خواندن/ویرایش اقلام وجود داشت، اما در UX درخواست‌ها به‌اندازه کافی واضح نبود:

1. در «مشاهده درخواست»، بخش اقلام فقط جدول نمایش می‌داد و مسیر «افزودن/ویرایش اقلام» در خود همان بخش برجسته نبود.
2. در حالت بدون قلم، کاربر پیام عملیاتی واضح نمی‌دید که اگر AI فایل را نخواند، هنوز می‌تواند اقلام را دستی یا با اکسل وارد کند.
3. ورود اکسل برای اقلام درخواست راهنمای اختصاصی نداشت.
4. mapping اکسل در ویرایش اقلام request کامل نبود و ستون‌های نوع/برند/مدل را به‌خوبی پشتیبانی نمی‌کرد.

---

## ✅ اصلاح انجام‌شده

### 1) دکمه افزودن/ویرایش اقلام در مشاهده درخواست

در `crm/inqreader.js` داخل modal مشاهده درخواست، در بخش «اقلام درخواست»، دکمه‌های زیر اضافه شد:

```text
افزودن/ویرایش اقلام
نمونه اکسل
```

اگر درخواست هیچ قلمی نداشته باشد، پیام راهنما نمایش داده می‌شود:

```text
اگر خواندن فایل با AI موفق نبود، از «افزودن/ویرایش اقلام» می‌توانید دستی یا با اکسل راهنمادار اقلام را وارد کنید.
```

---

### 2) راهنمای اختصاصی Excel برای اقلام درخواست

در `crm/index.html` config جدید برای Excel guide اضافه شد:

```text
INQ
```

ساختار فایل Excel / CSV اقلام درخواست:

| ستون | محتوا |
|---|---|
| A | شرح کالا / تجهیز |
| B | مشخصات فنی / استاندارد |
| C | تعداد |
| D | واحد |
| E | نوع / خانواده تجهیز |
| F | برند |
| G | مدل / پارت‌نامبر |

---

### 3) نمونه CSV قابل دانلود

تابع جدید:

```js
ptfDownloadInqItemsTemplate()
```

این تابع فایل نمونه `ptf-inquiry-items-template.csv` را برای کاربر دانلود می‌کند.

---

### 4) ویرایش دستی کامل‌تر

در full editor اقلام درخواست، ستون‌های زیر قابل ورود/ویرایش شدند:

```text
شرح کالا
مشخصات فنی
تعداد
واحد
نوع
برند
مدل
```

---

### 5) Import Excel بهتر شد

ورود اکسل اکنون از helper مشترک زیر استفاده می‌کند:

```js
irXlsRowToItem()
```

این helper ستون‌های A تا G را به فیلدهای درست تبدیل می‌کند و header ردیف اول را نادیده می‌گیرد.

---

### 6) مسیر «خواندن فایل» هم fallback دستی/Excel دارد

در modal «خواندن فایل»، پیام راهنما اضافه شد که اگر AI فایل را نخواند، کاربر می‌تواند از:

```text
+ ردیف
ورود اکسل با راهنما
دانلود نمونه CSV
```

استفاده کند.

---

## 📁 فایل‌های تغییر یافته

```text
crm/inqreader.js
crm/index.html
crm/sw.js
crm/clear-cache.html
PTF-MASTER-HANDOVER.md
_tools/uat/tester221-rfq-items-late-entry.js
RELEASE-NOTES-v31.7.44.md
REGRESSION-REPORT-v31.7.44.md
```

---

## 🧪 تست اضافه‌شده

### `_tools/uat/tester221-rfq-items-late-entry.js`

پوشش:

- دکمه افزودن/ویرایش اقلام در مشاهده درخواست وجود دارد.
- پیام راهنما در حالت بدون قلم وجود دارد.
- نمونه CSV قابل دانلود است.
- Excel guide برای `INQ` اضافه شده است.
- full editor و read-file modal هر دو ورود اکسل با راهنما دارند.
- Excel parser از helper مشترک استفاده می‌کند.
- ستون‌های type / brand / model پشتیبانی می‌شوند.
- ذخیره اقلام همچنان `ptf_crm_inqitems` و `r.items` را به‌روزرسانی می‌کند.
- snapshot قدیمی خوانش AI هنگام ذخیره پاک می‌شود.

نتیجه مستقیم:

```text
tester221-rfq-items-late-entry: 12 PASS / 0 FAIL
```

---

## ⚠️ Impact Analysis

| بخش | اثر | ریسک |
|---|---|---|
| مشاهده درخواست | مسیر افزودن/ویرایش اقلام واضح شد | کم |
| خواندن فایل | fallback دستی/Excel روشن شد | کم |
| Excel guide | نوع جدید `INQ` اضافه شد | کم |
| ذخیره اقلام | همان مسیر قبلی `ptf_crm_inqitems` حفظ شد | متوسط کنترل‌شده |
| AI OCR/LLM | تغییری نکرد | صفر |
| RFQ/Offer/Finance/Auth/Sync | منطق اصلی دست‌نخورده | صفر |

---

## 📋 راستی‌آزمایی کارفرما

1. وارد CRM شوید و یک درخواست بدون قلم را باز کنید.
2. روی «مشاهده» بزنید.
3. در بخش اقلام باید دکمه «افزودن/ویرایش اقلام» و «نمونه اکسل» دیده شود.
4. روی «افزودن/ویرایش اقلام» بزنید.
5. چند ردیف دستی اضافه کنید و ذخیره کنید.
6. دوباره درخواست را ببینید؛ اقلام باید نمایش داده شوند.
7. یک فایل CSV/Excel با قالب ستون‌های A تا G وارد کنید.
8. اقلام باید به جدول وارد شوند و با ذخیره در درخواست ثبت شوند.

---

## 🚦 نتیجه

از این نسخه، حتی اگر AI فایل ضمیمه را نخواند، کاربر همچنان مسیر روشن و عملیاتی برای تکمیل اقلام درخواست دارد: دستی، Excel با راهنما، یا نمونه CSV.
