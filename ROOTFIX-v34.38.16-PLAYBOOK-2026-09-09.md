# رفعِ ریشه‌ای — نسخهٔ v34.38.16 (گام‌به‌گام برای مدیرِ غیرفنی)

> ۲۰۲۶-۰۹-۰۹ · هدف: **هیچ ایرادی نماند.** سه فاز: اول سیستم را فوری سبز کنیم، بعد ریشه را برای *همهٔ* کلیدها ببینیم، و در آخر فقط با تأیید و با بکاپ، داده را نرمال کنیم.

## وضعیتِ الان (مهم)
v34.38.16 **هنوز روی پروداکشن نیست** — فقط در مخزن است. پس پیامِ «دریافت کامل CRM انجام نشد» تا وقتی نسخهٔ جدید را بفرستید و رفرش اجباری کنید، **طبیعی است که بیاید**. دست به پاک‌کردنِ localStorage نزنید (داده است).

پس این ترتیب را بروید؛ فاز ۱ بدون هیچ ریسکِ داده‌ای، خودِ مشکلِ قفل‌شدن را حل می‌کند.

---

## فاز ۱ — استقرار v34.38.16 و سبز شدن (بدون دست‌زدن به داده)
رفعِ این نسخه: کلیدی که سرور برایش manifest کامل نداده (مثل `commission_records` که مقدارش شیء/تهی است) دیگر کلِ همگام را رد نمی‌کند؛ «unavailable» می‌شود (نسخهٔ سالمِ محلی حفظ می‌شود) و بقیهٔ کلیدها اعمال می‌شوند ⇒ bootstrap تمام و بنر سبز می‌شود.

**قدم‌ها:**
1. این فایل‌های تغییرکرده را روی هاست (استیجینگ، بعد پروداکشن) آپلود کن:
   `crm/sync.js` · `crm/client-server.js` · `crm/index.html` · `crm/manifest.json` · `crm/sw.js` · `crm/shell.js` · `crm/cms.js` · `crm/clear-cache.html` · `crm/device-reconnect.html` · `api/sales-domain.php` · `VERSION.json`
2. روی هر دستگاهِ دارای این CRM یک بار **رفرش اجباری**: `Ctrl+Shift+R` (یا `Cmd+Shift+R`). سرویس‌ورکرِ نسخه‌دار کشِ قدیمی را خودش حذف می‌کند؛ فقط همین یک رفرش لازم است. **لازم نیست** کلیدهای `ptf_crm_*` را پاک کنی.
3. در Console روی همان دستگاهِ مشکل‌دار اجرا کن و مطمئن شو نسخه درست است:
   ```js
   JSON.stringify({ rel: window.PTF_CRM_RELEASE, v2: window.PTF_CRM_SNAPSHOT_V2 })
   ```
   باید `{"rel":"v34.38.16","v2":false}` بدهد.
4. صبر کن bootstrap تمام شود (تا ~۲۰ ثانیه)؛ نشانگر باید سبز شود، بنر زرد محو و مشتریِ `[customers]` که در صف بود خودکار push شود.
   ممکن است پیامِ کوتاه «نسخهٔ محلی حفظ شد» برای پورسانت ببینی — **طبیعی و درست** است (آن کلید unavailable است؛ اگر دادهٔ پورسانتِ واقعی روی سرور نباشد چیزی از دست نمی‌رود).

> ✅ اگر بعد از فاز ۱ سبز شد، یعنی سمت کلاینت حل است. برای این‌که «هیچ ایرادی نماند» به فاز ۲ برو تا ریشهٔ داده‌ای را برای همهٔ کلیدها ببینیم.

---

## فاز ۲ — تشخیص فقط‌خواندنی (ریشه را برای همهٔ کلیدها ببین) — بدون ترمینال
**نیازی به ترمینال یا دسترسی هاست نیست.** این کار را از داخل خودِ صفحهٔ CRM (که در آن لاگین‌ای) انجام می‌دهیم. سرور اکشنِ `data_manifest` دارد که فقط ساختار/آمار هر کلید را برمی‌گرداند (بدون محتوای رکورد)؛ ما فقط آن را می‌خوانیم و مظنون‌ها را جدا می‌کنیم.

**قدم‌ها:**
1. صفحهٔ CRM را باز کن و در آن لاگین باش (مثل همیشه).
2. F12 → برگهٔ Console → این را بچسبان و Enter بزن (خروجی فقط اسامی کلیدها + نوع/تعداد است، نه دادهٔ هیچ رکوردی):
   ```js
   (async()=>{const out={rel:window.PTF_CRM_RELEASE};
   try{const r=await fetch('../api/crm.php?action=data_manifest',{credentials:'include'});
   const j=await r.json();out.ok=j&&j.ok===true;
   if(!out.ok){out.raw=String((j&&j.error)||r.status);return out;}
   const ks=(j.snapshot&&j.snapshot.keys)||{};const unav=(j.snapshot&&j.snapshot.unavailable)||{};
   out.unavailable=Object.keys(unav).map(k=>({key:k,reason:unav[k].reason}));
   out.suspects=[];for(const k of Object.keys(ks)){const s=ks[k];
   if(s.kind!=='array'||s.available===false){out.suspects.push({key:k,kind:s.kind,count:s.count,bytes:s.bytes,available:s.available!==false});}}
   out.totalKeys=Object.keys(ks).length;out.revision=j.revision;}
   catch(e){out.error=String(e);}return out;})()
   ```
3. نتیجه را کامل برای من بفرست. `suspects` دقیقاً همان کلاسِ مشکلِ `commission_records` را در همهٔ کلیدها نشان می‌دهد (نوع `object`/`NULL` یعنی غیرلیست)، و `unavailable` کلیدهایی که اصلاً قابل خواندن نیستند.

> ⛔ در این فاز **هیچ چیز را تغییر نده**. منتظر نتیجه بمان. (فایل `_tools/host/diag_sync_shapes.php` فقط برای کسانی است که ترمینال دارند — برای تو لازم نیست.)

---

## فاز ۳ — نرمال‌سازیِ امن (فقط بعد از دیدنِ خروجیِ فاز ۲ و تأیید تو)
یک نسخهٔ پشتیبان می‌گیریم، سپس فقط کلیدهایی که **واقعاً باید لیست باشند ولی نیستند** (و تهی/بدونِ دادهٔ باارزش‌اند) به `[]` (آرایهٔ خالی) نرمال می‌کنیم و هم‌زمان یک گاردِ سروری اضافه می‌کنیم که دیگر هیچ کلیدِ مجموعه‌ای به‌شکل شیء/تهی ذخیره نشود.

> این فاز را **من الان اجرا نمی‌کنم** — چون اول باید ببینم آیا داخلِ آن کلیدها دادهٔ واقعی هست یا نه. اگر داده بود، نرمال‌سازی باید «آرایه‌ای شدنِ امن» باشد نه «خالی‌کردن». بر اساس خروجیِ فاز ۲، دقیق‌ترین و امن‌ترین اسکریپت (با بکاپ و تأیید دوگانه) را می‌دهم.

---

## سؤال‌های صادقانه دربارهٔ «هیچ مشکلی نماند»
- **آیا یک راه‌حلِ قطعیِ «یک بار برای همیشه» وجود دارد؟** دو لایه:
  1. سمت کلاینت (کد): سیستم باید حتی اگر سرور کلیدی را غیرلیست/ناقص داد، گیر نکند و کار کند — این را v34.38.16 داد.
  2. سمت داده/سرور: خودِ دادهٔ کلیدهای مجموعه‌ای باید همیشه لیست باشد و manifest درست بگوید — این فقط با دسترسی هاست و با همین سه فاز حل می‌شود.
- **چرا فقط فاز ۱ کافی نیست؟** چون اگر کلیدِ `commission_records` (و هر کلیدِ مشابه) همچنان به‌شکل `{}` روی سرور بماند، سرویس‌دهیِ پورسانت/مالی برای دستگاه‌های تازه بی‌سابقه (که نسخهٔ محلی ندارند) ناقص می‌ماند و دادهٔ مرجعِ روی سرور هم نامنظم است. فازهای ۲ و ۳ این را ریشه‌ای جمع می‌کنند.
- **آیا حینِ این سه فاز داده‌ای می‌سوزد؟** خیر؛ فاز ۱ فقط فایل‌های کد را جایگزین می‌کند، فاز ۲ فقط می‌خواند، و فاز ۳ تنها با بکاپ و تأییدِ صریح تو انجام می‌شود.
