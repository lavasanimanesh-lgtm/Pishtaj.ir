# Release Notes — v31.7.10

**تاریخ:** ۱۴۰۵/۰۴/۲۸ (2026-07-19)
**نسخه:** v31.7.10
**نوع:** 🟡 رفع دو نقیصه UX گزارش‌شده توسط کارفرما — بهداشت اعلان‌ها + هشدار تکراری «کد تکراری»

---

## 🎯 مسئله (به گزارش کارفرما)

1. **انبوه اعلان‌های تکراری:** اعلان‌ها تکراری می‌آیند؛ «خواندم» هم اثر ماندگار ندارد و اعلان‌ها برمی‌گردند. نتیجه: اعلان مهم در انبوه اعلان‌های عادی گم می‌شود و کاربر دیگر به کارتابل اعتنا نمی‌کند (notification fatigue).
2. **پیام «کد تکراری» با هر هارد رفرش:** پس از هر رفرش، هشدار «N کد تکراری شناسایی شد» دوباره می‌آید؛ درحالی‌که از تنظیمات هم کار خاصی نمی‌توان کرد (موارد ambiguous عمداً قابل repair خودکار نیستند).

## 🔍 علت ریشه‌ای (RCA)

### BUG-NTF-001 — تولید اعلان تکراری
`notify()` در `rbac.js` هیچ ضدتکراری نداشت؛ هر فراخوانی (از جمله فراخوانی‌های خودکارِ تکرارشونده مثل هشدار duplicate در هر sync/رفرش) یک رکورد جدید در `ptf_crm_notifs` می‌ساخت.

### BUG-NTF-002 — برگشتن اعلان‌های خوانده‌شده
`ntfRead()` فقط `readBy` را تغییر می‌دهد و **timestamp رکورد ثابت می‌ماند**. قاعده عمومی merge در `ptfSmartMerge` («newer-timestamp wins») هنگام sync، نسخه سرور (خوانده‌نشده) را هم‌تراز یا برنده می‌دید → وضعیت «خوانده» عملاً موقت بود و بعد از sync برمی‌گشت. (همان الگوی BUG-AUTH: تغییر یک سمت قرارداد بدون سمت دیگر — این‌بار قرارداد merge.)

### BUG-NTF-003 — درهم‌آمیختگی مهم و غیرمهم
کارتابل یک فهرست تخت بود؛ اعلان actionable/بحرانی هیچ تمایز بصری یا جایگاهی نداشت.

### BUG-DUP-NAG-001 — هشدار کد تکراری در هر رفرش
`ptfAutoRepairSafeDuplicates()` در startup و پس از هر full pull اجرا می‌شود (`codegen.js:93`, `sync.js:329`). موارد `referenced-ambiguous` **عمداً** auto-repair نمی‌شوند (تصمیم درست از v31.7.2)، اما تابع هر بار بدون حافظه، دوباره toast + notify می‌ساخت → هشدار جاودانهٔ بی‌عمل، و خودش تولیدکنندهٔ اعلان تکراری (تشدید مشکل ۱).

## ✅ اصلاحات

| # | فایل | تغییر | شدت |
|---|------|--------|-----|
| 1 | `crm/rbac.js` | `notify()` ضدتکرار شد: اعلان یکسان (عنوان+متن+گیرندگان) تا خوانده‌نشده باشد رکورد جدید نمی‌سازد؛ فقط شمارنده `×N تکرار` و زمان آخرین وقوع به‌روز می‌شود. پس از خوانده‌شدن، وقوع جدید اعلان جدید می‌سازد (اطلاع از دست نمی‌رود) | 🔴 P0 |
| 2 | `crm/sync.js` | merge اختصاصی `ptf_crm_notifs`: `readBy` اجتماع دو طرف (خوانده‌شده هرگز برنمی‌گردد)، `done` و `repeat` حداکثری | 🔴 P0 |
| 3 | `crm/rbac.js` | کارتابل دوبخشی: «🔴 اعلان‌های مهم» (actionable یا kind بحرانی) جدا و بالای «🔵 سایر اعلان‌ها» + نشان تکرار ×N | 🟡 P1 |
| 4 | `crm/rbac.js` | دکمه «✓✓ خواندم همه» برای تخلیه یک‌جای انباشت تاریخی | 🟡 P1 |
| 5 | `crm/codegen.js` | هشدار duplicate فقط وقتی **وضعیت تغییر کرده باشد** (fingerprint plan) + دکمه «✓ دیدم — دیگر هشدار نده» در دیالوگ بررسی کدهای تکراری (با ثبت audit)؛ حل شدن duplicateها ack را خودکار پاک می‌کند | 🟡 P1 |
| 6 | `crm/index.html`, `crm/sw.js`, `crm/clear-cache.html` | همگام‌سازی ۴نقطه‌ای نسخه به v31.7.10 | — |

**آنچه عمداً تغییر نکرد:** سیاست «repair فقط با کد server و عدم تغییر خودکار موارد ambiguous» دست‌نخورده ماند — مشکل کارفرما نحوهٔ اعلام بود، نه سیاست repair.

## 📊 نتایج گیت

- تستر جدید `tester185-notif-dup-hygiene`: **۱۹/۱۹ PASS** (رفتاری: ضدتکرار، بقای readBy در merge، خواندم‌همه، تفکیک مهم/عادی، خاموشی هشدار پس از ack)
- رگرسیون کامل: **163/163 فایل PASS — 3842/3842 چک PASS**
- `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| داده اعلان‌های موجود | دست‌نخورده؛ فیلدهای جدید (`dkey`,`repeat`) فقط به رکوردهای جدید اضافه می‌شود؛ رکوردهای قدیمی سازگارند | صفر |
| sync اعلان‌ها | merge جدید فقط union/max است — هیچ رکوردی حذف نمی‌شود | کم |
| سیاست duplicate repair | بدون تغییر — فقط ضدهشدار تکراری | صفر |
| سایر ماژول‌ها | `notify()` امضای قبلی را حفظ کرده؛ ۳۸ فراخوانی موجود بدون تغییر کار می‌کنند | صفر |

## 📋 راستی‌آزمایی توسط کارفرما (۳ دقیقه)

1. **اعلان تکراری:** دوبار پشت‌سرهم عملی که اعلان می‌سازد تکرار کنید → در کارتابل فقط ۱ اعلان با نشان «×۲ تکرار» ببینید.
2. **ماندگاری خواندم:** «خواندم» بزنید، هارد رفرش (Ctrl+F5) و صبر تا sync → اعلان خوانده‌شده نباید برگردد.
3. **خواندم همه:** کارتابل → «✓✓ خواندم همه» → badge صفر شود.
4. **کد تکراری:** هارد رفرش → پیام می‌آید (بار اول). تنظیمات → «بررسی کدهای تکراری» → «✓ دیدم — دیگر هشدار نده» → رفرش‌های بعدی بدون پیام. اگر duplicate جدیدی رخ دهد، هشدار دوباره می‌آید (خاموشی کور نیست).

---
*توسعه‌دهنده: PTF Development Team — 2026-07-19*
