# راهنمای پیشگیری معماری — PTF CRM

نسخه: **v34.7.40** | تاریخ: ۱۴۰۵/۰۵/۲۸ (2026-08-19)
پیش‌زمینه: `BRANCH-AUDIT-2026-08-17.md`، `ASSESSMENT-SALESFILE-3ISSUES-2026-08-17.md`

> **اصل حاکم:** معماری فعلی (اسکریپت‌های سراسری + لایه‌گذاری روی `window`) عوض نمی‌شود.
> کاری که می‌کنیم این است که **خانواده‌های باگی که واقعاً رخ دادند** دیگر نتوانند برگردند —
> با سه چیز: یک قرارداد، یک نگهبان خودکار، و یک چک‌لیست کوتاه.

---

## ۱) چرا باگ‌ها تکرار می‌شدند؟ (ریشهٔ مشترک، نه اتفاق)

سه باگ اخیر ظاهراً بی‌ربط بودند ولی یک ریشه داشتند:

| باگ | ظاهر | ریشه |
|-----|------|------|
| «ابطال کنترل‌شده» کار نمی‌کرد (v34.7.26) | دکمه هیچ کاری نمی‌کرد | فرستنده `cd \|\| _id`، گیرنده `_id \|\| cd` |
| پیش‌پرداخت مشتری دیگر در حساب (v34.7.26) | عدد اشتباه در دفتر | تطبیق با مقدار **تهی** (`'' === ''`) و با **نام** |
| پول در نماها دیده نمی‌شد (v34.7.27) | فاکتور «کاملاً باز» | رسید و فاکتور یک پرونده با دو شناسهٔ متفاوت ذخیره شده بودند |

یعنی ریشهٔ مشترک = **«هویت رکورد» قرارداد واحد نداشت** و **شکست‌ها بی‌صدا** بودند.
هر دو مورد با ابزار قابل‌کشف‌اند؛ پس باید ابزار داشته باشیم، نه صرفاً دقت انسانی.

---

## ۲) قرارداد ۱ — هویت رکورد (تنها قرارداد مجاز)

از v34.7.28 چهار تابع در `crm/finance-helpers.js` (که خیلی زود بارگذاری می‌شود) مرجع‌اند:

```js
PTF.id(x)               // شناسهٔ متعارف = _id || cd   (همیشه رشته؛ نبود ⇒ '')
PTF.aliases(x)          // همهٔ شناسه‌های همان رکورد: ['_id', 'cd']
PTF.sameEntity(a, b)    // آیا این دو (رکورد یا رشته) به یک رکورد اشاره می‌کنند؟
PTF.findById(list, id)  // یافتن رکورد با هر یک از شناسه‌هایش
```

**قاعدهٔ طلایی: «نوشتن با شناسهٔ متعارف، خواندن با همهٔ نام‌های مستعار.»**

| کار | ✅ درست | ❌ غلط |
|-----|---------|--------|
| ساخت آرگومان دکمه | `PTF.id(rec)` | `rec.cd \|\| rec._id` |
| پیدا کردن رکورد | `PTF.findById(list, id)` | `list.filter(x => x._id === id)[0]` |
| مقایسهٔ دو مرجع | `PTF.sameEntity(a, b)` | `String(a._id \|\| a.cd) === String(b)` |
| کلید نقشه/گروه‌بندی | `var k = PTF.id(x); if (k) map[k] = …` | `map[String(x._id \|\| x.cd \|\| '')] = true` |

سه قاعدهٔ مکمل که همان‌جا رعایت می‌شوند:
1. **شناسهٔ تهی هرگز تطبیق نمی‌کند** — `'' === ''` یعنی باگ، نه تطبیق.
2. **تطبیق با نام (شرکت/سند) فقط وقتی نام یکتاست**؛ در غیر این‌صورت نادیده گرفته شود.
3. **تطبیق مبهم = عدم انتساب + ثبت finding**، نه انتخاب اولین گزینه (الگوی `invoice_case_bind_ambiguous` در سرور).

---

## ۳) قرارداد ۲ — «اکشن کاربر هرگز بی‌صدا شکست نمی‌خورد»

هر تابعی که به دکمه وصل است (`*Void`, `*Save`, `*Open`, `*Delete`, `*Apply`, …) باید در هر مسیر خروج یکی از این‌ها را داشته باشد:
- انجام کار، یا
- `alert`/`ptfToast` با **علت دقیق** («فاکتور با شناسهٔ X یافت نشد»، «وضعیت: void»، «نقش مجاز نیست»).

`return;` خالی در اکشن کاربر ممنوع است. دلیل: کاربر تفاوت «دکمهٔ خراب» و «عمل غیرمجاز» را نمی‌فهمد و گزارش باگ به تأخیر می‌افتد (دقیقاً همان چیزی که سه هفته طول کشید).

---

## ۴) قرارداد ۳ — مالکیت اکشن در UI (یک اکشن = یک محل)

- هر اکشن دقیقاً **یک مالک** دارد؛ مالک ترجیحاً بلوک تخصصی همان حوزه است (اسناد رسمی ⇒ `docsx`، خرید ⇒ `buycompare`).
- ماژول تزریق‌کننده باید پرچم هوک بگذارد (`window._dxDealsHooked`, `window._rbDealsHooked`).
- نوار عملیات عمومی فقط **fallback** رندر می‌کند: `if (!window._xxHooked) …` تا اگر ماژول نبود، کاربر بدون مسیر نماند.

## ۵) قرارداد ۴ — بازنویسی توابع سراسری فقط به‌صورت زنجیره‌ای

معماری روی لایه‌گذاری بنا شده و این اشکال ندارد؛ اما **بازنویسی خاموش** ممنوع است:

```js
// ✅ زنجیره‌ای (رفتار قبلی حفظ می‌شود)
var _prev = window.renderDeals;
window.renderDeals = function () { _prev(); /* افزودهٔ من */ };

// ❌ خاموش (نسخهٔ قبلی برای همیشه گم می‌شود — منشأ باگ ابطال)
window.ptfInvoiceVoid = function (id) { /* ... */ };   // در حالی که ماژول دیگری هم آن را دارد
```

اگر واقعاً باید نسخهٔ قدیمی بازنشسته شود: نام قدیمی به `…Legacy` تغییر کند و در همان کامیت مستند شود (کاری که با `ptfInvoiceVoidLegacy` انجام شد).

---

## ۶) نگهبان خودکار — `_tools/arch/arch-guard.js`

قواعد بالا حالا **قابل اجرا** هستند، نه فقط توصیه:

```
node _tools/arch/arch-guard.js             # بررسی (کد خروج ۱ برای تخلف جدید)
node _tools/arch/arch-guard.js --baseline  # ثبت وضعیت فعلی به‌عنوان مبنا
```

| قاعده | چه چیزی را می‌گیرد | حالت |
|-------|---------------------|------|
| **A1** | بازنویسی خاموش تابع سراسری | مسدودکننده |
| **A2** | ترتیب شناسه برخلاف قرارداد (`cd \|\| _id`) | مسدودکننده |
| **A3** | تطبیق با مقدار تهی (کلید `''` در نقشه، مقایسهٔ دو مقدار احتمالاً تهی) | مسدودکننده |
| **A4** | دکمهٔ مرده (هندلر `onclick` بدون تعریف) | مسدودکننده |
| **A5** | خرابی انکدینگ فارسی (`U+FFFD`) | مسدودکننده |
| **A6** | ناهماهنگی نسخه در ۷ نقطهٔ رسمی | مسدودکننده |
| **A7** | شکست خاموش در اکشن کاربر | گزارشی (فعلاً) |
| **A8** | وصلهٔ زنجیره‌ای بدون انتقال `arguments` | مسدودکننده |
| **A9** | تعریف تکراری یک نام سراسری در همان فایل | مسدودکننده |

**مدل «مبنا» (baseline):** بدهی امروز مسدودکننده نیست، اما **افزایش آن مسدود است**.
مبنای ثبت‌شده: `A1=5، A2=0، A3=105، A4=0، A5=0، A6=0، A7=36، A8=0، A9=24`.
هر تخلف جدیدِ خارج از این multiset ⇒ `ARCH GUARD: FAIL` و شکست گیت CI.
امضای A2/A3 از v34.7.40 به شمارهٔ خط وابسته نیست و از hash محتوای نرمال‌شده ساخته می‌شود؛ در عین حال multiset باعث می‌شود افزودن یک نسخهٔ تکراری از همان finding نیز شکست بخورد.

وقتی بدهی‌ای برطرف شد، نگهبان تذکر می‌دهد. اجرای `--baseline` فقط پس از مقایسهٔ signatureهای مبنای قبلی با وضعیت جاری و بررسی موارد new/fixed مجاز است؛ برابر بودن صرف شمارها دلیل refresh نیست (debt swap قاعدهٔ A2 در v34.7.40 با همین بررسی کشف و رفع شد).

نگهبان به `_tools/uat/run-ci-gate.js` وصل شده و گیت کانونی محلی آن را اجرا می‌کند. اتصال اجباری همین گیت به workflow استقرار، به‌علت نداشتن مجوز نوشتن `.github/workflows` توسط GitHub App، جداگانه و پس از اعمال workflow محافظت‌شده فعال می‌شود.

> اعتبارسنجی عملی در `tester443`: جابه‌جایی خط یک finding موجود سبز می‌ماند، ولی افزودن دومین finding با signature کاملاً یکسان، A3 را از `105/105` به `106/105` می‌برد و گیت را قرمز می‌کند.

---

## ۷) چک‌لیست تغییر (سه دقیقه، پیش از هر کامیت)

1. هیچ‌جا `cd || _id` ننوشتم؛ از `PTF.id/sameEntity/findById` استفاده کردم.
2. هیچ مقایسه‌ای با مقدار تهی صادق نمی‌شود (شناسه، شمارهٔ پیشنهاد، نام).
3. هر اکشن کاربر در همهٔ مسیرهای خروج بازخورد می‌دهد.
4. اگر تابع سراسری را بازنویسی کردم، زنجیره‌ای است یا نسخهٔ قدیمی `Legacy` شد.
5. اکشن جدید فقط **یک** محل دارد (یا fallback مشروط به پرچم هوک).
6. `node _tools/arch/arch-guard.js` سبز است.
7. `node _tools/uat/run-ci-gate.js` سبز است و نسخه در ۷ نقطه بامپ شده.
8. برای هر رفع باگ، یک assertion «عدم رگرسیون» هم نوشتم (نه فقط assertion رفع).

## ۸) قواعد نوشتن تستر (درس ۵۹ تستر قرنطینه‌شده)

- نسخه، رشتهٔ دقیق UI و لیترال آرایه را **پین نکنید**؛ وجود شناسه‌ها را جدا جدا بررسی کنید و برای نسخه از `^v\d+\.\d+\.\d+$` استفاده کنید.
- کد خطای دقیق مسیرهای در حال مهاجرت را پین نکنید.
- assertion مبتنی بر فاصلهٔ کاراکتری در فایل فارسی‌دار غلط است (چندبایتی)؛ ترتیب نسبی دو نشانه را مقایسه کنید.
- ترجیحاً تستِ **رفتاری** در `vm` بنویسید، نه grep روی متن؛ اگر ناچار به grep شدید، الگو را به قرارداد ببندید نه به جمله.
- شمارش شکست‌ها فقط با `node _tools/uat/run-all.js` (پنج قالب خلاصه دارد؛ grep دستی روی `FAIL` غلط می‌شمارد).
- تستر قدیمی که بدنهٔ تابع را با regex برش می‌زند، به تابع کمکی بیرونی حساس است؛ در ماژول‌های پرمصرف (مثل `rbac.js`) گارد را **درون‌خطی** بنویسید.

## ۹) نقشهٔ کاهش بدهی (بدون توقف کار جاری)

| گام | کار | اثر روی مبنا |
|-----|-----|---------------|
| ۱ | حذف/`Legacy` کردن ۵ بازنویسی خاموش (`saveRfqEdit`, `delRfq`, `ptfDownloadStoredFile`, `ptfAutoRegisterSummaryProducts`, `onload`) | A1: ۵ ⇒ ۰ |
| ۲ ✅ | مسیر purge در `sync.js` اصلاح شد؛ debt swap بعدی در `customer-finance.js` نیز بدون ورود به baseline رفع شد | A2: ۱ ⇒ ۰ |
| ۳ | تبدیل تدریجی ۱۰۵ تطبیقِ بدون گارد به `PTF.findById`/گارد ناتهی — ماژول‌به‌ماژول همراه با تستر | A3 ⇒ کاهشی |
| ۴ | افزودن بازخورد به ۳۶ اکشن دارای خروج خاموش | A7 ⇒ ۰، سپس تبدیل A7 به مسدودکننده |
| ۵ | خروج تدریجی ۵۹ تستر از قرنطینه طبق دسته‌بندی `TEST-DEBT-TRIAGE-2026-08-17.md` | — |

هر گام مستقل، کوچک و با تستر خودش است؛ هیچ‌کدام نیازمند بازنویسی معماری نیست.

## ۱۰) وضعیت فعلی

- قرارداد هویت: پیاده و expose شده + `tester431` (**۲۰/۰**).
- نگهبان: signature پایدار و مقایسهٔ multiset دارد؛ self-test و regression سرتاسری `tester443` سبزند.
- A2 به صفر رسیده و duplicate واقعی A9 حذف شده است.
- گیت کانونی محلی v34.7.40: **۵۵ PASS / ۰ FAIL**؛ اتصال پیش از FTP تا انتشار مجاز workflowها باز است و staging موجود همچنان روی `arena/**` خودکار deploy می‌شود.
