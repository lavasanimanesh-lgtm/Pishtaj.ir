# یادداشت انتشار CRM v34.8.31

تاریخ: ۱۴۰۵/۰۶/۰۵ (۲۰۲۶-۰۸-۲۷)

## T3-1 — خواندن سرور-محور: `collection_query`
(ROADMAP-THIN-CLIENT فاز T3 — بزرگ‌ترین اثر تجربهٔ باقی‌مانده)

endpoint جدید در crm.php:
- **فیلتر تساوی** روی هر فیلد (پارامتر آزاد)، **جستجوی آزاد** case-insensitive (سقف اسکن ۳۰۰۰)
- **مرتب‌سازی** (sortBy/sortDir) و **صفحه‌بندی** (سقف صفحه ۱۰۰)
- **پروجکشن** CSV اختیاری (فقط فیلدهای لازم — ترافیک حداقلی)
- **role-guard کامل**: کلید خارج از ماتریس نقش = 403 collection_forbidden
- rev کلید در پاسخ → کش هوشمند کلاینت (فقط وقتی rev عوض شده دوباره بگیر)

کلاینت: `ptfCollectionQuery(collection, opts, cb)` در sync.js (فقط-خواندنی؛ GET با توکن).

### مصرف هدف
بوت دستگاه جدید (فقط صفحات موردنیاز به‌جای کل دیتاست) + پنل‌های فهرست‌محور.

### کنترل
tester533 (۱۸: مدل PHP با ۲۵۰ رکورد — فیلتر/مرتب/صفحه/جستجوی فارسی/پروجکشن + قراردادهای کلاینت).
