# RCA — انحراف کلیدهای ابری (Cloud Key Deviation)

- **تاریخ:** 2026-08-22 (۱۴۰۵/۰۵/۳۱)
- **نسخهٔ فعلی:** v34.7.94
- **دامنه:** کلیدهای ذخیره‌شده در باکت آروان (`site-rfq/…`, `site-ven/…`, `rfq/…`, `rfqatt/…`, `careers/…`, `contact/…`, `management-reports/…`) در مقایسه با کلیدهای ثبت‌شده در مخازن CRM (`ptf_crm_*`) — به‌ویژه ردهٔ **B** (کلید در CRM با کلید ابری دیگری فقط با یک اسلش/base اختلاف دارد) و ردهٔ **E** (کلید در CRM هست ولی هیچ بایتی در باکت نیست).
- **مبنای شواهد:** خواندن کد و رفتار نسخه‌های v34.7.52 → v34.7.94. اجرا روی داده Production لازم است — این سند فرضیه‌های ریشه‌ای را با شواهد کد رتبه‌بندی می‌کند.
- **نوع سند:** تحلیلی — بدون تغییر کد در این نشست.

---

## ۱) خلاصهٔ اجرایی

هفت مسیر احتمالی برای انحراف کلید کشف شد. **دو مسیر «قطعی»** (کد شاهد دارد) و **پنج مسیر «محتمل»** (نیاز به تأیید با داده). ریشهٔ اصلی احتمالاً یکی از این سه است، به‌ترتیب احتمال:

1. **دوگانگی الگوی نام‌گذاری کلید بین PHP و کلاینت** — قطعی. `save_attachment` در `api/crm.php` (مسیر فرم‌های عمومی سایت) از `ptf_storage_object_key()` استفاده می‌کند که `uniqid('', true)` تولید می‌کند؛ ولی `presign_put` در `api/storage.php` (مسیر CRM/فرم اصلی) از `uniqid()` (بدون `true`) استفاده می‌کند. **دو کلید متفاوت با ساختار متفاوت** برای همان فایل — این ریشهٔ مستقیم ردهٔ B است.
2. **فال‌بک `upload_proxy` کلید متفاوت از `presign_put` می‌سازد** — قطعی. مرورگر ابتدا `presign_put` می‌گیرد (کلید ۱)؛ اگر PUT مستقیم شکست خورد به `upload_proxy` می‌رود که **کلید جدید (کلید ۲)** می‌سازد. اگر UI کلید ۱ را ذخیره کرده باشد ولی سرور کلید ۲ را در باکت گذاشته باشد، کلید CRM با هیچ بایتی مطابقت ندارد → ردهٔ E.
3. **مهاجرت پیش از v34.7.54** — محتمل. تخلف A2 نگهبان معماری در v34.7.52 (ترتیب `cd || _id`) شواهد این را می‌دهد که تعدادی رکورد با شناسهٔ نامنتظره ساخته شده‌اند. اگر کلیدها به این شناسه پیوسته باشند، اصلاحیهٔ v34.7.54 کلید را در CRM جابجا کرده ولی فایل ابری قدیمی باقی مانده — ردهٔ B/E.

## ۲) هفت فرضیهٔ ریشه‌یابی — با شواهد و رتبه‌بندی

### 🔴 H1 — دو مسیر تولید کلید با الگوهای متفاوت (قطعی)

**شواهد:**
- `api/crm.php:865` (فرم عمومی سایت):
  ```php
  $key = ptf_storage_object_key('site-' . $prefix, $original);
  // ⇒ site-rfq/2026-08/68a1b2c3d4e5f.6789012-file.pdf   (uniqid با more_entropy=true)
  ```
- `api/storage.php:230` (پیش‌امضای CRM):
  ```php
  $key = $folder . '/' . date('Y-m') . '/' . uniqid() . '-' . $name;
  // ⇒ rfq/2026-08/68a1b2c3d4e5f-file.pdf                (uniqid بدون more_entropy)
  ```
- `api/storage.php:259` (upload_proxy) — همان الگوی بالا (بدون `more_entropy`).

**نتیجه:** دو تابع «مستقل» با ظاهر مشابه ولی الگوی طول شناسهٔ متفاوت (`13` کاراکتر vs `24` کاراکتر). این ناسازگاری در گزارش `ptfClassifyCloudKeys` به‌عنوان ردهٔ B (base یکسان، مسیر متفاوت) دیده می‌شود.

**اقدام:** یک تابع واحد `ptf_storage_object_key()` سراسری، هر دو نقطه از آن بخواند. بامپ pre‌شرط RCA-CLOUD-01.

### 🔴 H2 — فال‌بک `upload_proxy` کلید نو می‌سازد (قطعی)

**شواهد:**
- `crm/storage.js:267` — بعد از موفقیت `presign_put`: `finish({ key: d.key, ... })` — کلید مرحلهٔ ۱.
- `crm/storage.js:275-290` — در fallback: `presign_put` صدا نمی‌شود؛ به `upload_proxy` می‌رود که در `api/storage.php` **کلید کاملاً جدیدی** (`$folder . '/' . date('Y-m') . '/' . uniqid() . '-' . $name`) تولید و همان را در پاسخ (`pr.key`) برمی‌گرداند.
- `crm/storage.js:290` — `finish({ key: pr.key, ... })` — کلید مرحلهٔ ۲ ذخیره می‌شود.

خبر خوب این است که کلاینت **کلید نهایی سرور را برمی‌دارد** (`pr.key`)، پس تا وقتی کاربر صبر می‌کند تا `finish` صدا زده شود، رکورد CRM با فایل ابری sync می‌ماند.

**ریسک باقی‌مانده:** اگر شبکه در میانهٔ راه قطع شود:
- کلیدی که در ثانیهٔ اول به `d.key` (از `presign_put`) در متغیرهای موقت رفت،
- ولی فایل هرگز آپلود نشد (نه با PUT مستقیم، نه با proxy)،
- و همان `d.key` به رکورد ذخیره شد،
- → **ردهٔ E**: کلید در CRM هست، بایت در باکت نیست.

**اقدام:** `presign_put` نباید کلید را به کلاینت بازگرداند تا پیش از تأیید موفقیت `PUT` ذخیره‌شدنی باشد. راه بهتر: `presign_put` **کلید کاندید** بدهد ولی کلاینت فقط پس از دریافت 200 از S3 آن را «commit» کند. جایگزین سریع‌تر: کلاینت اگر `xhr.status != 2xx` بود، قبل از fallback، همان کلید کاندید را در یک لیست «pending» ذخیره کند تا در audit شناسایی شود.

### 🟠 H3 — تخلف A2 (ترتیب `cd || _id`) پیش از v34.7.54 (محتمل بالا)

**شواهد:**
- HANDOFF `2026-08-20` صراحتاً می‌گوید: «v34.7.54 تخلف A2 `ptfHarvestFileKeys` — ترتیب `_id || cd`) و سبز شدن گیت».
- `crm/storage.js:851` (تصحیح شده): `var id = recId || obj._id || obj.cd || obj.no || obj.id || '';`
- قبل از v34.7.54 ترتیب معکوس بوده. اگر یک رکورد هم `_id` هم `cd` داشته باشد و این دو متفاوت باشند، `ptfHarvestFileKeys` قبل از v34.7.54 کلید را به شناسهٔ **اشتباه** پیوند می‌داد. audit اجرا شده روی داده‌ای که هنوز کلیدها به `cd` (قبل از A2 fix) پیوسته‌اند، ردهٔ B گزارش می‌کند.

**اقدام:** یک migration یک‌بار روی داده تولید و report قبل/بعد بگیر (رکوردهایی که `_id != cd`).

### 🟠 H4 — تغییر مسیر فولدر بین نسخه‌ها (محتمل متوسط)

**شواهد:**
- v34.7.66 (تامین‌کننده سایت) از `add_supplier` استفاده کرد که با `prefix='ven'` می‌رود → `site-ven/…`.
- ولی در v34.7.69 (نمایش فایل تامین‌کنندهٔ تاییدشده) کد `ptfEntityFiles` انتظار دارد `files.cat` یا آرایه — ساختار پس از `supApprove` فایل را از `oth` به `cat` کپی می‌کند.
- اگر رکوردی از قبل از v34.7.66 مانده باشد، مسیر ابری `site-ven/…` است ولی رکورد CRM ممکن است هنوز به یک کلید قدیمی‌تر (که در همان الگو نبود) اشاره کند.

**اقدام:** بررسی audit log تغییرات فولدر بین v34.7.30-v34.7.66.

### 🟠 H5 — مهاجرت مسیرها: `rfq/` ↔ `rfqatt/` ↔ `site-rfq/` (محتمل)

**شواهد:**
- سه پیشوند برای یک منظور: `crm/inqreader.js:670` می‌گوید هر سه معتبرند.
- ولی allowlist سرور در `api/zip-attachments.php:179` هم هر سه را می‌پذیرد. یعنی رکوردهای CRM هم می‌توانند هر یک از این سه پیشوند را داشته باشند.
- ابزار audit `ptfClassifyCloudKeys` سه پیشوند را «مساوی» نمی‌بیند: اگر کلید در CRM `rfq/2026-08/xyz.pdf` باشد ولی در باکت `rfqatt/2026-08/xyz.pdf` باشد، این ردهٔ B/E می‌شود.

**اقدام:** یک normalize step در `ptfHarvestFileKeys` قبل از تطبیق — یا **بهتر**: تک‌کردن پیشوندها به یک نام (مثلاً همیشه `rfqatt/`) و remap تدریجی.

### 🟡 H6 — کلید Content-Type/Extension در باکت متفاوت (محتمل کم)

**شواهد:**
- `save_attachment` هرگز پسوند فایل را در نام کلید تغییر نمی‌دهد ولی هنگام آپلود از خود اسم اصلی استفاده می‌کند.
- `ptf_storage_safe_name` می‌تواند حروف فارسی را در URL encode کند (`preg_replace('/[^\\w\\-\\.\\x{0600}-\\x{06FF} ]/u', '_', ...)`).
- کلید در CRM ممکن است با کاراکترهای غیر‌ASCII باشد ولی در باکت آروان (که path Segmentation UTF-8 دارد) با pattern متفاوتی ذخیره شود.

**اقدام:** یک تست خاص روی فایل‌های با نام فارسی/عربی/ترکی.

### 🟡 H7 — بک‌آپ رستور جزئی (محتمل کم)

**شواهد:**
- HANDOFF می‌گوید بک‌آپ offsite هنوز آماده نیست. اگر روی هاست restore جزئی داده‌ی JSON انجام شده باشد (بدون restore کامل باکت)، رکورد CRM ممکن است به فایلی اشاره کند که پس از snapshot حذف شده — ردهٔ E.

**اقدام:** بررسی audit log ادمین برای هر عملیات import/restore پس از تاریخ اولین مشاهدهٔ ردهٔ E.

---

## ۳) اقدامات پیشنهادی — به ترتیب اولویت و ریسک

| # | اقدام | اثر | ریسک | نسخه پیشنهادی |
|---|---|---|---|---|
| **1** | یکسان‌سازی الگوی تولید کلید بین `api/crm.php` و `api/storage.php` (تنها یک تابع، `more_entropy=true`) | ریشهٔ H1 را می‌بندد؛ کلید جدید همه دقیقاً یک الگو | کم — API خارجی تغییر نمی‌کند | v34.7.95 |
| **2** | `presign_put` کلید را با پرچم «pending» به کلاینت بدهد؛ کلاینت بعد از تأیید 200 آن را «commit» کند | ریشهٔ H2 (کلید ذخیره‌شده بدون بایت در باکت) | کم-متوسط — نیازمند تغییر client/server | v34.7.96 |
| **3** | `ptfHarvestFileKeys` هنگام تطبیق پیشوندها را نرمالایز کند (`rfq/`, `rfqatt/`, `site-rfq/` → base یکسان) | H5 را می‌بندد و ردهٔ B را کاهش می‌دهد | کم | v34.7.95 |
| **4** | **ابزار «صف آپلود مجدد» ردهٔ E** (spec زیر) | H2/H4/H7 را قابل عمل می‌کند | کم — فقط تصمیم دستی کاربر | v34.7.95 |
| **5** | یک اسکن migration یک‌باره برای رکوردهایی که `_id != cd` و کلید مربوطه در باکت پیدا نمی‌شود | H3 را بسته | متوسط — نیاز به تأیید تک‌تک | v34.7.96 |

---

## ۴) طراحی ابزار «صف آپلود مجدد» ردهٔ E (RE-UPLOAD-QUEUE-001)

### هدف

پس از هر اجرای `ptfCloudKeyAudit`، ردیف‌های ردهٔ E (کلیدی که در CRM هست ولی بایتی در باکت نیست) به کاربر یک نمای عملیاتی می‌دهد که:

1. برای هر ردیف مشخص کند: **کدام ماژول / کدام رکورد / نام فایل تخمینی / تاریخ ثبت رکورد** (اگر داشته باشیم).
2. به کاربر «اقدام» بدهد:
   - **✅ آپلود مجدد** — انتخاب فایل جدید و آپلود در همان کلید (اگر ممکن است) یا کلید جدید + بروزرسانی رکورد.
   - **🗑 پاک‌کردن مرجع** — حذف کلید مرده از رکورد (فایل به هر حال گم است).
   - **📩 درخواست از منبع** — اگر رکورد ثبت‌نام سایت است (site-ven، site-rfq)، پیامک یا ایمیل به تامین‌کننده/متقاضی بفرست تا فایل را دوباره ارسال کند.
3. یک CSV export کند که به کاربر بیرون از CRM بدهد (برای مواردی که کاربر می‌خواهد از منبع خارج فایل جمع کند).

### ساختار داده

```json
// ptf_crm_reupload_queue
[
  {
    "cd": "REUP-1405053100001",
    "createdAt": "2026-08-22T10:00:00Z",
    "createdBy": "admin",
    "store": "ptf_crm_suppliers",
    "recId": "SUP-2025-0123",
    "recLabel": "شرکت الف",
    "key": "site-ven/2026-08/68a1b2c3-catalog.pdf",
    "name": "catalog.pdf",
    "moduleLabel": "تامین‌کننده (فایل کاتالوگ)",
    "status": "pending|resolved|dismissed|requested",
    "action": null,        // 'reuploaded' | 'cleared' | 'requested' | 'skipped'
    "actionAt": null,
    "actionBy": null,
    "newKey": null,
    "requestChannel": null,  // 'sms' | 'email' | 'manual'
    "requestSentAt": null,
    "notes": ""
  }
]
```

### توابع API کلاینت (`crm/storage.js` بعد از فاز 4)

- `ptfReuploadQueueBuild(rows)` — ورودی: خروجی `ptfCloudKeyAudit` (کلاسیفای‌شده)؛ خروجی: صفی از ردیف‌های `cls === 'E'` (بدون آنالوگ).
- `ptfReuploadQueueOpen()` — مودال مدیریت با فیلتر بر اساس ماژول/status.
- `ptfReuploadQueueAct(cd, action, payload)` — اعمال اقدام:
  - `'reupload'` — دیالوگ انتخاب فایل → آپلود از طریق `ptfStorageUpload` → به‌روزرسانی رکورد اصلی با کلید جدید (`ptfRemapCloudKeyFields` روی مسیر رکورد).
  - `'clear'` — حذف مرجع کلید مرده از رکورد (با confirm ارشد).
  - `'request'` — تولید پیام (`sms/email`) به مالک رکورد (تامین‌کننده/متقاضی).
- `ptfReuploadQueueExportCsv()` — خروجی CSV با ستون‌های: ماژول، کد رکورد، نام رکورد، نام فایل، کلید، status، اقدام، تاریخ اقدام.

### امنیت / محدودیت‌ها

- فقط نقش‌های ارشد (`admin`, `chairman`, `ceo`) — مثل خود گزارش audit.
- هر اقدام در audit log ثبت می‌شود.
- `'clear'` فقط پس از تأیید صریح (`confirm`) + دلیل متنی.
- `'reupload'` باید همان MIME/پسوند فایل قدیمی باشد (guard در UI؛ در server هم `ptf_storage_safe_name`).
- کلید جدید همیشه با تابع unified (پس از H1 fix) ساخته می‌شود تا در آینده انحراف نداشته باشد.
- `'request'` نرخ محدود دارد (حداکثر یک پیام در هر ۲۴ ساعت به هر مالک — از reuse همان مسیر پیامک `set_status` تامین‌کننده استفاده می‌شود).

### تسترها

- تستر جدید: `tester497-v34.7.95-reupload-queue.js` — با vm/sandbox:
  - `ptfReuploadQueueBuild` روی داده مصنوعی (mix از A/B/C/E) فقط E را می‌سازد.
  - `ptfReuploadQueueAct('reupload')` روی sandbox: کلید قدیمی از رکورد حذف و کلید جدید نشسته باشد.
  - `ptfReuploadQueueAct('clear')` بدون تأیید ارشد رد شود.
  - Rate limit یک‌روزه `request` ثابت شود.
  - Export CSV با UTF-8 BOM و فرمت درست.

### فازبندی پیشنهادی

- **فاز A (v34.7.95):** `ptfReuploadQueueBuild` + `ptfReuploadQueueExportCsv` + دکمه «⬇️ صف آپلود مجدد (CSV)» در مودال audit فعلی. فقط فقط‌خواندنی.
- **فاز B (v34.7.96):** مودال مدیریت با اقدام `reupload` و `clear`.
- **فاز C (v34.7.97):** اقدام `request` (SMS/Email با نرخ محدود).

## ۵) قدم بعدی

1. اجرای عملی `ptfCloudKeyAudit` روی Production توسط کاربر ارشد → گرفتن CSV اولیه.
2. اگر تعداد ردهٔ B > 20، H1 (unified key generator) اول پیاده شود تا رده B خودبه‌خود کاهش پیدا کند.
3. سپس فاز A از ابزار صف آپلود مجدد (فقط CSV) — که کاربر فایل‌های واقعی ردهٔ E را ببیند.
4. اگر تعداد ردهٔ E قابل مدیریت است (< 50)، فاز B پیاده شود.
5. اگر تعداد رده E زیاد است (> 100)، فاز C (تماس با مالک) در اولویت است.
