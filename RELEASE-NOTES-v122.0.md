# 🚀 ریلیز v122.0 — اسپرینت ۱۲۲: تسعیر ارز + رفع باگ TC + بهبود فرم پیشنهادها

**تاریخ:** ۱۶ تیر ۱۴۰۵ | **دامنه مصوب کارفرما:** US-266v2 + US-277 + US-278 + برگرداندن فونت‌ها

## 💱 US-266v2 — تسعیر ارز (طرح تیم متخصص — ورک‌فلو سبک، بدون دفترداری دوبل)
واقعیت کسب‌وکار شما مدل شد: **سند ارزی، تسویه ریالی با نرخ روز**:
1. فاکتورِ سند ارزی → دکمه ثبت دریافت، دیالوگ «💱 دریافت ریالی» باز می‌کند: مبلغ تومان + **نرخ تسعیر همان روز** → معادل ارزی تراکنش خودکار محاسبه و روی همان پرداخت ذخیره می‌شود (بدون کلید داده جدید).
2. **مطالبات**: جدول «اسناد ارزی» بالای پنل — مبلغ سند، دریافتی ریالی، معادل ارزی وصول‌شده، **مانده ارزی** (قرمز تا تسویه)، میانگین نرخ.
3. **سود خالص پروژه**: برای سند ارزی، فروش = **جمع دریافت‌های ریالی واقعی** (نه فرض نرخ) + نمایش وصول‌شده/مانده به ارز؛ اگر دریافتی ثبت نشده، صادقانه می‌گوید سود هنوز قابل محاسبه نیست. خرید ارزی هم با یادداشت تسعیر پشتیبانی می‌شود.

## 🤝 US-277 — رفع باگ TC (گزارش شما) + تب مستقل
- **ریشه باگ**: `offRenderItems` فقط `kind==='CO'` را «مالی» می‌دانست → فرم TC بدون ستون قیمت (شکل TO) باز می‌شد. رفع در هر دو رندر + اعتبارسنجی.
- **تب سوم «+ Techno-Commercial»** کنار TO/CO طبق پیشنهاد شما: فرم = همان مالی با قیمت؛ **برند به‌صورت پیش‌فرض هست ولی اختیاری** (خالی هم ثبت می‌شود)؛ همه قالب‌های چاپ سربرگ TECHNO-COMMERCIAL OFFER.
- **US-261 هیئت** هم بسته شد: TC حالا در ماشین وضعیت هم‌رده CO است (صدور/ارسال/برد TC وضعیت درخواست را می‌راند).

## ➕ US-278 — ردیف‌ها
دکمه «+ ردیف دستی» از همه مودال‌های پیشنهاد حذف شد → **یک ردیف پیش‌فرض** همیشه باز است + **دکمه سبز ＋ کنار ردیف اول** (هر فشردن = ردیف جدید).

## 🔤 برگرداندن فونت‌ها
فونت monospace اجباری که ایجنت قبلی به فرم‌ها و هر ۵ قالب چاپ تزریق کرده بود (۴۰+ نقطه) کامل حذف شد — فرم‌ها به فونت استاندارد پوسته و قالب‌ها به تایپوگرافی اصلی (Segoe UI/Georgia/Helvetica) برگشتند.

## کیفیت: tester35 جدید ۲۲/۲۲ PASS (شامل تست رفتاری تسعیر: سند €1000، دریافت ۶۰م با نرخ ۱۰۰هزار → وصول €600، مانده €400) | رگرسیون ۲۲/۲۲ تستر | ممیز ۰ خطا | کش → `ptf-crm-v122.0`
