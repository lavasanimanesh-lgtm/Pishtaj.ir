# گزارش ارزیابی — وضعیت main و آخرین نسخه‌های دیپلوی‌شده روی پروداکشن و استیجینگ

**تاریخ:** ۲۰۲۶-۰۸-۲۸ (ساعت ~۱۱:۴۵ UTC)
**نوع:** ارزیابی وضعیت + RCA خط لوله استقرار (بدون تغییر کد)
**برنچ بررسی:** `arena/01a0482b-pishtaj-ir` (از `main` در کامیت `58ce2f4`)

---

## ۱) خلاصهٔ مدیریتی — سه فاصلهٔ هم‌زمان

وضعیت فعلی **سه‌گانهٔ ناهمگام** است و مهم‌تر از آن، **خط لولهٔ استقرار «سبز ولی خراب»** است: ورک‌فلوهای FTP امروز هم روی پروداکشن و هم روی استیجینگ «success» ثبت کرده‌اند، در حالی که **هیچ‌کدام از فایل‌های امروز واقعاً روی سایت زنده قرار نگرفته‌اند.**

| مؤلفه | آنچه GitHub ادعا می‌کند | آنچه واقعاً زنده است | فاصله |
|---|---|---|---|
| **main** | `58ce2f4` (merge پرار #۴، ۱۱:۱۷Z) — `VERSION.json` = v34.8.35 | — | مبنا |
| **پروداکشن** pishtaj.ir | دیپلوی «موفق» ۰۸:۵۱Z از کامیت `687c43b` (v34.8.34) + تگ `v2026.08.28-0905` | **CRM ≈ v34.8.12 (مورخ ~۲۰۲۶-۰۸-۲۶)** — دو نسل عقب‌تر | فایل مارکر امروز ۴۰۴ است |
| **استیجینگ** staging.pishtaj.ir | دیپلوی «موفق» ۱۰:۵۰Z از `arena/01a047d8` @ `e156098b` (v34.8.35) | **CRM ≈ v34.8.33** از برنچ `arena/01a0425e` (دیپلوی از ریپوی قدیمی، پیش از مهاجرت) | بنر استیجینگ برنچِ جلسهٔ قدیمی را نشان می‌دهد |

**نتیجهٔ کلیدی:** حتی اگر همین الان main روی هر دو محیط «دیپلوی» شود، هیچ چیزی تغییر نمی‌کند — چون مقصد FTP با docroot واقعی سروکنندهٔ هر دو دامنه یکی نیست. **قدم اول، همگام‌سازی main و استیجینگ نیست؛ قدم اول ترمیم صحت خط لولهٔ استقرار است، بعد همگام‌سازی ۵ دقیقه‌ای است.**

---

## ۲) وضعیت دقیق main

- **HEAD:** `58ce2f4c` = ادغام PR #۴ از `arena/01a047df-pishtaj-ir` (۱۱:۱۷:۱۲Z امروز) با ۳ کامیت:
  - `a52ce805` — مستندات ممیزی نازک‌سازی (v34.8.34)
  - `8bd89bcc` — گیت‌های CI آماده (T0-3/4/6، v34.8.35)
  - `656898ac` — تکمیل فاز صفر + رفع باگ ASSET_VERSION (v34.8.35)
- `VERSION.json`: `crm_version: v34.8.35`، `last_stable_tag: v2026.08.16-0100` (**کهنه** — تگ واقعی `v2026.08.28-0905` است ولی همان تگ هم به دیپلویی اشاره دارد که هرگز زنده نشد؛ «آخرین نسخهٔ پایدارِ واقعاً زنده» عملاً v34.8.12 است).
- **فاصله با کامیت دیپلوی پروداکشن:** ۴ کامیت جلوتر از `687c43b`.
- **گیت CI در ورک‌فلوها هنوز وصل نیست:** تغییرات T0-3/T0-4 روی main به‌صورت پچ دستی `_tools/PENDING-workflow-t0-gates-2026-08-28.patch` تحویل شده و **اعمال نشده** (خود ممیزی `ARENA-THIN-CLIENT-STATUS-AUDIT-2026-08-28.md` همین را بندهای T0-3/T0-4 «❌ انجام‌نشده» ثبت کرده).
- **PRهای باز رقیب:** #۲ از `arena/01a047d8` (۴ کامیت جلو / ۴ کامیت عقبِ main — همین برنچ الان روی استیجینگ «دیپلوی» شده) و #۳ از `arena/01a047de` (۱ جلو / ۴ عقب). سه جلسهٔ موازی دقیقاً یک کار T0 را با سه پیاده‌سازی متفاوت انجام داده‌اند (پچ‌های PENDING سه‌گانهٔ `2026-08-27` / `v34.8.35` / `2026-08-28` و اسکریپت‌های اعمال‌شده روی برنچ d8).
- `php.yml` (PHP Composer): قالب پیش‌فرض گیت‌هاب است؛ `composer.json` در ریپو وجود ندارد → روی هر PR شکست می‌خورد. (الان manually disable شده، ولی سابقهٔ شکست‌ها آلوده است.)

## ۳) وضعیت پروداکشن (pishtaj.ir)

**ادعا:** دیپلوی ۰۸:۵۱:۰۱Z از `main`@`687c43b` → success، تگ `v2026.08.28-0905` + Release با بک‌آپ `site-backup.zip` ساخته شد.
(چهار تلاش قبلی امروز — ۰۵:۳۰، ۰۸:۱۷، ۰۸:۴۳، ۰۸:۴۸ — دقیقاً در گام **«Deploy via FTP»** شکست خورده‌اند؛ یعنی صبح FTP واقعاً قطع بوده و در تلاش پنجم «وصل» شده است.)

**واقعیت زنده (بررسی مستقیم HTTP):**

| پروب زنده | نتیجه | معنی |
|---|---|---|
| `/crm/sw.js` → `RELEASE` | **`v34.8.12`** | کامیت `687c43b` باید `v34.8.34` می‌داد |
| `/crm/key-registry.js` (فایلی که بین v34.8.12 و کامیت دیپلوی اضافه شد) | **۴۰۴ (صفحهٔ خطای اختصاصی سایت)** | فایل‌های کامیت دیپلوی‌شده روی docroot نیستند |

**نتیجه:** پروداکشن روی محتوای ~۲۰۲۶-۰۸-۲۵/۲۶ (نسل v34.8.12، آخرین دیپلای موفقِ ریپوی قدیمی) فروخورده است. تمام کارهای v34.8.13 تا v34.8.35 (فاز کامل نازک‌سازی C1–C4/T0–T5 و اصلاحات مالی) **هیچ‌گاه روی پروداکشن زنده نشده‌اند.**

## ۴) وضعیت استیجینگ (staging.pishtaj.ir)

**ادعا:** سه دیپلوی «موفق» امروز: ۰۹:۱۲ (`arena/01a0479d`)، ۱۰:۳۵ (`arena/01a047df`)، ۱۰:۵۰ (`arena/01a047d8` @ `e156098b`).

**واقعیت زنده:**

| پروب زنده | نتیجه | معنی |
|---|---|---|
| بنر پایین صفحه | «برنچ: **arena/01a0425e-pishtaj-ir**» | برنچ جلسه‌ای قدیمی از **ریپوی قبلی**؛ در این ریپو اصلاً رانِی از این برنچ وجود ندارد (کل تاریخ Actions این ریپو فقط ۱۷ ران از امروز است) |
| `/crm/sw.js` → `RELEASE` | **`v34.8.33`** | آخرین محتوای واقعاً زندهٔ استیجینگ از دیپلوی v34.8.33 است |
| `/api/deploy-probe.php` (در `e156098b` موجود + در `api/.htaccess` صریحاً مجاز) | **۴۰۴** | دیپلوی ۱۰:۵۰ هرگز به docroot نرسیده — پاسخ ۴۰۴ برای URL ای که تازه ساخته شده، قابل نسبت‌دادن به کش نیست |

**نتیجه:** استیجینگ هم روی محتوای دیپلویِ ریپوی قدیمی (جلسهٔ `01a0425e`، نسخهٔ v34.8.33) فروخورده است.

## ۵) ریشه‌یابی

1. **R1 — مقصد FTP با docroot یکی نیست (علت اصلیِ فروخوردگی هر دو محیط):** هر دو دامنه به `37.27.127.144` اشاره می‌کنند. در ریپوی قدیمی دیپلوی‌ها واقعاً می‌رسیدند (پروداکشن تا v34.8.12 در ~۲۶ اوت، استیجینگ تا v34.8.33 در ~۲۷ اوت). بعد از بحران بیلینگ هاست/Actions و مهاجرت ریپو به `shaykhsofla-hash`، FTP دوباره «وصل» شد و آپلود کامل انجام می‌شود (ران ۱۵ دقیقه‌ای = آپلود واقعی)، ولی فایل‌ها **جایی جز docroot سروکنندهٔ دامنه‌ها** می‌روند — محتمل‌ترین حالت: مقدار `FTP_PROD_SERVER_DIR` / `FTP_SERVER_DIR` در سکرت‌های ریپوی جدید به مسیر دیگری (پوشهٔ home، مسیر قدیمی، یا هاست دیگری) اشاره می‌کند.
2. **R2 — نبود گیت T0-4 (post-deploy hash check):** دقیقاً همان بندی که ممیزی ۲۰۲۶-۰۸-28 با برچسب 🔴 «انجام‌نشده» ثبت کرده بود. اگر این گیت وصل بود، هر سه دیپلوی «موفق» امروز باید fail می‌شدند و این گزارش ۱۲ ساعت زودتر نوشته می‌شد. **اسکریپت و پروب آماده است** (`_tools/ci/post-deploy-hash-check.sh` + `api/deploy-probe.php` روی برنچ PR#۲ و به‌صورت پچ روی main) — فقط باید اعمال شود.
3. **R3 — استیجینگ در تعریف فعلی هرگز با main همگام نمی‌شود:** تریگر `deploy-staging.yml` شامل `main` نیست (`development`، `site/**`، `crm/**`، `staging/**`، `arena/**`). ادغام PR #۴ در main اصلاً استیجینگ را تریگر نکرد و آخرین‌نویسندهٔ استیجینگ هر برنچِ جلسه‌ای است که آخرین push کرده باشد.
4. **R4 — کار موازی سه جلسه:** سه PR هم‌موضوع (T0/v34.8.35) با سه شکل تحویل (پچ ۲۷ اوت / اسکریپت اعمال‌شده / پچ ۲۸ اوت). فقط یکی merge شده؛ دو PR باز مانده‌اند که یکی‌شان (PR#۲) همین الان «روی استیجینگ» است.
5. **R5 — php.yml خراب:** قالب Composer بدون `composer.json` → CI همهٔ PRها قرمزِ بی‌معنی (فعلاً disable شده).

---

## ۶) برنامهٔ اقدام — «اول main و استیجینگ همگام شوند»

### مرحلهٔ ۰ — ترمیم صحت خط لوله (پیش‌نیاز؛ بدون این، همگام‌سازی صوری است)

1. **مقصد FTP را پیدا و اصلاح کنید (۱۰ دقیقه، در پنل هاست + سکرت‌های ریپو):**
   - در پنل هاست، docroot واقعی هر دو vhost (`pishtaj.ir` و `staging.pishtaj.ir`) را یادداشت کنید؛ سپس در فایل‌منیجر پوشه‌هایی را که فایل‌های امروز در آن‌ها نوشته شده‌اند پیدا کنید (فایل‌هایی با mtime ≈ ۰۹:۰۶–۱۱:۰۶ UTC امروز — مثلاً `crm/sw.js` تازه).
   - سکرت‌های `FTP_SERVER_DIR` و `FTP_PROD_SERVER_DIR` را به docrootهای واقعی اصلاح کنید (یا vhost را به مسیر مقصد فعلی هدایت کنید — کدام که با ساختار پنل سازگارتر است).
2. **تست مارکر (پیش از هر دیپلوی کامل):** یک فایل `zzz-sync-test.html` با محتوای زمان جاری به main اضافه و دیپلوی استیجینگ بزنید؛ فقط وقتی `https://staging.pishtaj.ir/zzz-sync-test.html` از مرورگر برگشت، سراغ بقیه بروید؛ سپس فایل را حذف کنید.
3. **گیت T0-4 را فعال کنید:** پچ `_tools/PENDING-workflow-t0-gates-2026-08-28.patch` موجود روی main را اعمال کنید (شامل `run-ci-gate.js` قبل از FTP و post-deploy hash check روی ۵–۷ فایل کلیدی در هر دو ورک‌فلو + بازنویسی `php.yml` به Quality Gate واقعی). از این به بعد «success» یعنی «فایل زنده == کامیت».
4. **تیر `main` را به تریگر استیجینگ اضافه کنید** (اصلاح R3): در `deploy-staging.yml`، `branches` را با `- main` تکمیل کنید تا هر merge به main، استیجینگ را بازنویسی کند و همیشه آخرین‌نویسندهٔ استیجینگ main باشد (نه برنچ جلسه‌ای).

### مرحلهٔ ۱ — همگام‌سازی main ↔ استیجینگ (بعد از سبزشدن مرحلهٔ ۰)

5. **بستن شاخه‌های موازی:** PR #۳ را ببندید (۱ کامیت؛ اگر نکتهٔ یکتایی دارد cherry-pick به main). برای PR #۲ (که اسکریپت‌های اعمال‌شده + probe دارد) دو راه دارید — **راه پیشنهادی:** محتوای مفیدش (فایل‌های `_tools/ci/*` و `api/deploy-probe.php` اعمال‌شده) را به‌جای پچ دستی، مستقیم روی main اعمال کنید و PR را ببندید؛ فقط یک شکل تحویل نگه دارید.
6. **دیپلوی استیجینگ از main:** بعد از اصلاح سکرت، `workflow_dispatch` روی `deploy-staging.yml` از `main`. پذیرش: بنر استیجینگ «برنچ: main» + `sw.js` = `v34.8.35` + پروب استقرار (در صورت اعمال T0-4) سبز. **این نقطه، نقطهٔ «main و استیجینگ همگام» است.**
7. یک UAT سریع روی استیجینگ (`_tools/uat/run-ci-gate.js` محلی + نگاه انسانی به CRM استیجینگ با Ctrl+Shift+R).

### مرحلهٔ ۲ — پروداکشن (فقط بعد از تثبیت استیجینگ)

8. دیپلوی پروداکشن از `main` با تایید `DEPLOY` (تگ و بک‌آپ خودکار ساخته می‌شود). پذیرش: `/crm/sw.js` = `v34.8.35` و `/crm/key-registry.js` = 200 و گیت post-deploy سبز.
9. `VERSION.json` را به‌روز کنید: `last_stable_tag` = تگ واقعیِ سبزشده، و سند RELASE-NOTES مطابق آن.
10. **مکانیزم دائمی:** قانون «merge به main → استیجینگ خودکار؛ پروداکشن فقط دستی بعد از گیت استقرار سبز» را در `ARCHITECTURE-GUARDRAILS.md` ثبت کنید تا این رخداد تکرارنشود.

### چیزهایی که این وسط عمداً نباید انجام شود
- دیپلوی پروداکشن از تگ `v2026.08.28-0905` برای «رول‌بک» — این تگ به دیپلویی اشاره دارد که هرگز زنده نشده؛ rollback واقعی همان وضعیت فعلی زنده (v34.8.12) است.
- آپلود دستی FTP برای «جبران» — مشکل مسیر را پنهان می‌کند و دوباره تکرار می‌شود.

---

## ۷) جمع‌بندی یک‌خطی

**main از هر دو محیط زنده جلوتر است (v34.8.35 در برابر v34.8.12 پرود / v34.8.33 استیج)، استیجینگ محتوای یک برنچ مردهٔ ریپوی قدیمی را سرو می‌کند، و علت اصلی یک مقصدِ اشتباه FTP است که با گیتِ از-پیش-طراحی‌شدهٔ T0-4 قابل کشف بود — اول سکرت/مسیر FTP اصلاح شود، بعد پچ گیت‌ها اعمال و `main` به تریگر استیجینگ اضافه شود، و آنگاه یک دیپلوی استیجینگ از main، همگامسازی را کامل می‌کند.**

*شواهد زندهٔ این گزارش در تاریخ ۲۰۲۶-۰۸-۲۸ ~۱۱:۳۰–۱۱:۴۵ UTC جمع‌آوری شده‌اند (پروب‌های HTTP مستقیم روی هر دو دامنه + تاریخچهٔ ۱۷ رانِ Actions ریپو + تاریخچهٔ git).*
