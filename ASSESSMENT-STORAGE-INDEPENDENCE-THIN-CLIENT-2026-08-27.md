# گزارش ارزیابی — قطع کامل وابستگی به localStorage و نازک‌کردن کلاینت CRM

**تاریخ:** ۲۰۲۶-۰۸-۲۷ | **نسخهٔ مبنا:** v34.8.12 | **نوع:** ارزیابی معماری + نقشهٔ راه (بدون تغییر کد)

---

## ۱) خلاصهٔ مدیریتی

CRM از نظر معماری داده **وسط راهِ گذار به «کلاینت نازک / سرور-محور» (فاز B)** است. ستون فقرات فاز B — هوک `getData/setData`، صف آفلاین، هم‌گرایی یک‌باره، آینهٔ IndexedDB و فعال‌سازی خودکار پس از هم‌گرایی — **ساخته و روی استیجینگ اعمال شده** (RCA `ARENA-STORAGE-INDEPENDENCE-RCA-2026-08-26.md` + tester514). اما:

1. **۵۴۶ ارجاع به localStorage در ۶۶ فایل** هنوز وجود دارد؛ از این میان **۵۹ نقطهٔ نوشتن مستقیم (`localStorage.setItem`) در ۳۰ فایل** از لایهٔ داده بیرون می‌زند. هر کدام یعنی: (الف) داده هرگز به سرور نمی‌رود یا (ب) هر بوت دوباره localStorage را پر می‌کند و خودترمیم فاز B را به کار مشغول نگه می‌دارد.
2. **دادهٔ کسب‌وکارِ «فقط-روی-دستگاه»** هنوز داریم (چک‌های شخصی per-user، صف کدینگ، پیش‌نویس‌ها، ریکاوری امضا، دفتر رویدادهای مالی گارد نوشتار) — ریسک گم‌شدن داده و نقض «سرور = منبع حقیقت».
3. **صف آفلاین (`ptf_b_queue`) خودش در localStorage است** — یعنی در دقیقهٔ ۱۰۰٪ پر بودن (همان سناریوی RCA)، ثبت تغییرِ کاربر شکست می‌خورد.
4. **گیت CI به workflowها وصل نیست** — arch-guard و تسترها فقط محلی اجرا می‌شوند؛ هر PR جدید می‌تواند دوباره localStorage بنویسد و کسی نفهمد.
5. سمت سرور همچنان **مدل «بلابِ کل کلید»** است (push/pull آرایهٔ کامل هر کلید). برای «کلاینت نازک واقعی» و مقیاس‌پذیری، موجودیت‌های داغ باید رکورد-محور شوند.

**جمع‌بندی:** حلقهٔ بحرانی (بن‌بست ۱۰۰٪) بسته شده، اما «قطع کامل وابستگی» یعنی حذف مسیرهای فرّار، انتقال صف به IndexedDB، امن‌کردن نشست، و رکورد-محورکردن سرور. ۶ فاز کار در بخش ۵ تعریف شده است.

---

## ۲) وضعیت فعلی معماری (As-Is)

| لایه | فایل | وضعیت |
|---|---|---|
| تعریف دادهٔ پایه | `crm/index.html` خط ۲۳۳۰ (`getData`) و ۲۳۴۰ (`setData`) | خواندن/نوشتن sync از localStorage + نگهبان سهمیه (`ptfStorageSafeSetItem`) + اعلان dirty به sync |
| هوک فاز B | `crm/client-server.js` (~۱۰۵۰ خط، ۶۶ ارجاع LS) | ✅ فعال: سرور منبع حقیقت، کش ۳۰ثانیه‌ای، پول دلتای مشترک (v33.21)، push دسته‌ای ≤۲۰ کلید، صف آفلاین، هم‌گرایی یک‌باره، فعال‌سازی خودکار پس از ACK (v34.8.9)، تخلیهٔ هر بوت (>۸KB به IDB)، گارد چندکاربره |
| آینهٔ سنگین | `client-server.js` + `storage-quota.js` (IDB در خط ۶۵۰) | کلیدهای IDB_KEYS + هر کلید >۱۲۰هزار کاراکتر → حافظهٔ نشست + IndexedDB با پیشوند `bdata:`؛ localStorage نسخه‌اش حذف می‌شود |
| همگام‌سازی | `crm/sync.js` | ~۷۰ کلید کسب‌وکار (`SYNC_KEYS`)، ۲۱ کلید فوری، دامنهٔ نقش‌محور (`SYNC_ROLE_KEYS` برای sales/buyer/accountant/collector)، rev هرکلید |
| نگهبان سهمیه | `crm/storage-quota.js` | ≥۸۵٪ → تخلیهٔ خودکار به IDB (نه فقط هشدار)؛ emergencyCompact فقط کلیدهای فرّار |
| سرور | `api/crm.php` | `data_push/data_pull/data_rev` (با `krevs`)، `users_sync`، `auth_login/auth_logout/role_verify`، `save_backup(_delta)`، MySQL منبع حقیقت |
| پیوست‌ها | `api/storage.php` + `crm/storage.js` | ✅ آپلود مستقیم مرورگر→آروان (S3)؛ رسانه‌ها از localStorage خارج شده‌اند |
| گاردها | `_tools/arch/arch-guard.js` (A1..A7) + تسترهای UAT (`tester514` و ~۱۸۰ تستر دیگر) | محلی؛ **در CI نیست** (فقط PHP-lint + FTP در workflowها) |

**نتيجه:** مسیر اصلی داده (کلیدهای `ptf_crm_*` در SYNC_KEYS) در حالت فاز B دیگر به localStorage وابسته نیست. وابستگیِ باقی‌مانده در **مسیرهای فرعی و مستقیم** است.

---

## ۳) اندازه‌گیری وضعیت (اعداد کلیدی)

| سنجه | مقدار |
|---|---|
| کل ارجاع‌های `localStorage` در `crm/` | **۵۴۶** در **۶۶ فایل** (از ۱۰۷ فایل JS) |
| نوشتن مستقیم `localStorage.setItem` خارج از لایهٔ داده | **۵۹ نقطه در ۳۰ فایل** |
| بدترین فایل‌ها | `index.html` (۲۰)، `codegen.js` (۱۰)، `bridge.js` (۷)، `sales-domain-v2.js` و `cheque-print.js` (هرکدام ۶) |
| ارجاع توکن نشست (`ptf_crm_token`) | **۴۹ نقطه** — ذخیره در localStorage (خواندنی توسط هر اسکریپت/XSS) |
| کلیدهای کسب‌وکار سینک‌شونده | ~۷۰ کلید (SYNC_KEYS) |
| حجم JS سمت کلاینت | ~۵MB در ۱۰۷ فایل + `index.html` ۳۲۶KB (مونولیت) |
| سقف بحرانی localStorage | ~۵MB (محل حادثهٔ «۱۰۰٪») |

---

## ۴) شکاف‌ها (GAP Analysis) — با شواهد دقیق

### GAP-1 — نوشتن مستقیم در کلیدهای کسب‌وکار (بایپس لایهٔ داده) 🔴 بحرانی
این نوشتن‌ها از `setData` عبور نمی‌کنند ⇒ نه وارد صف/پوش فاز B می‌شوند نه dirty اعلان می‌دهند؛ در فاز B با آینهٔ IDB هم تداخل دارند (نسخهٔ LS قدیمی می‌سازند که هر بوت دوباره تخلیه می‌شود — چرخهٔ خودترمیمِ بی‌پایان).

| محل | کلید | مشکل |
|---|---|---|
| `inqreader.js:1265-1266` | `ptf_crm_products` | نوشتن مستقیم برای رندر (hide→render→restore) — کلید سینک‌شونده |
| `finance-write-guard.js:237` | `ptf_crm_fin_events` | دفتر رویدادهای مالی با setItem مستقیم — کلید سینک‌شونده و حساس |
| `index.html:3670` | `ptf_crm_settings` | نوشتن مستقیم تنظیمات |
| `codegen.js:394` | `ptf_crm_settings` | مسیر fallback مستقیم |
| `treasury.js:472`، `treasury-call.js:25`، `surplus.js:10`، `case-revision.js:252`، `myday.js:23` | کلیدهای سینک‌شونده | الگوی «اگر setData نبود → localStorage» — fallbackهای مرده ولی خطرناک |
| `cheques.js:83,92` | `ptf_personal_cheques_<user>` | **دادهٔ کسب‌وکار فقط-روی-دستگاه**، کلید per-user، اصلاً در SYNC_KEYS نیست |
| `letters.js:193,263` | `sigRecovery_*` | کپی ریکاوری امضا در LS — می‌تواند بزرگ شود |
| `sales-domain-v2.js:41,124,376,404,514,595` | `ptf_sales_command_*`، `ptf_autodraft_offer_*`، `ptf_offer_post_ack_warning_*` | وضعیت عملیاتی فروش (ACK/پیش‌نویس/هشدار) در LS |
| `draftx.js:43,47`، `offers.js:868,2749` | پیش‌نویس‌ها | وضعیت فرم در LS |
| `codegen.js` (۱۰ نقطه) | `ptf_code_tmp_queue`، `ptf_code_duplicate_plan/ack` | صف کدینگ — دادهٔ عملیاتی |
| `my-customers-filter.js:39`، `messengers.js:448,459` | فیلتر مشتریان من، `ptf_bot_pairs` | تنظیمات کسب‌وکاری per-user |

### GAP-2 — صف آفلاین و نشانگرهای فاز B در localStorage 🔴 بحرانی
`ptf_b_queue` (خط ۴۴۳ client-server.js) + نشانگرهای `ptf_b_synced_/flushed_/offload_last` همگی در همان LS‌ای می‌نویسند که ممکن است پر باشد. در سناریوی هدف (دستگاه قدیمی)، دقیقاً وقتی باید تغییر در صف بماند، `queueAdd` شکست می‌خورد و نوشتن کاربر `false` برمی‌گردد. **صف باید به IndexedDB برود** (فقط حافظهٔ نشست + IDB)، یا حداقل صف با سقف سخت و first-class بودن تضمین شود.

### GAP-3 — ریسِ آب‌رسانی سردبوت (Cold-boot race) 🟠 مهم
`ptfBIdbPreload` ناهمگام است ولی `getData` همگام. روی بوت سردِ دستگاهی که کلیدهایش تخلیه شده‌اند، تا پایان preload، خواندن کلید سنگین `[]` برمی‌گرداند (فقط رندر بعدیِ پس از pull درست می‌شود). برای «قطع کامل» باید یکی از این‌ها تضمین شود: آب‌رسانی IDB پیش از اولین رندر پنل‌ها، یا re-render قطعی پس از `ptfBIdbPreload`/اولین pull، یا خواندن async استاندارد.

### GAP-4 — توکن و نشست در localStorage 🟠 مهم (امنیت + نازک‌سازی)
`ptf_crm_token` (۴۹ ارجاع)، `ptf_crm_session`، `ptf_crm_token_role` در LS هستند — خواندنی توسط هر اسکریپت (سطح حملهٔ XSS) و بقا پس از بستن مرورگر. مسیر درستِ کلاینت نازک: **کوکی HttpOnly/SameSite برای توکن** (سمت سرور `auth_login` صادر می‌کند) یا حداقل sessionStorage + الگوی BFF. این کار ده‌ها ارجاع خواندن توکن را هم داخل لایهٔ واحد متمرکز می‌کند.

### GAP-5 — رسانه/دادهٔ حجیم در LS 🟡 متوسط
`cheque-print.js:617` — تصویر پس‌زمینهٔ چک به‌صورت **dataURL/base64 در LS** (`ptf_chqprint_bg`). زیرساخت آروان (storage.php) موجود است؛ باید مثل بقیهٔ پیوست‌ها آپلود شود. همچنین پروفایل/لی‌اوت چاپ چک می‌تواند per-user روی سرور برود.

### GAP-6 — کش‌های سرور-محور که به‌صورت LS مدیریت می‌شوند 🟡 متوسط
`bridge.js:498-529,805` (`ptf_site_suppliers/rfqs/inbox_sig`)، `fx.js:269` (`ptf_fx_live_cache`)، آمار `ptf_cloud_usage` — این‌ها کشِ فقط-خواندنی سرورند؛ باید read-through با TTL و قابل-تخلیه باشند (یا به sessionStorage/IDB بروند) تا در سهمیهٔ LS حساب نشوند.

### GAP-7 — گیت معماری و CI 🟠 مهم
arch-guard قواعد A1..A7 دارد ولی **هیچ قاعده‌ای برای localStorage ندارد** و در هیچ workflow گیت‌هاب اجرا نمی‌شود (deploy-staging فقط PHP-lint + FTP است؛ چند پچ `PENDING-workflow-ci-gate-*.patch` هم اعمال نشده‌اند). بدون این گیت، هر ویژگی جدید دوباره setItem مستقیم اضافه می‌کند و کار فازها پس می‌گیرد.

### GAP-8 — مدل دادهٔ سرور (بلاب کل کلید) 🟠 مهم (بلندمدت، مقیاس)
`data_push/pull` آرایهٔ کامل هر کلید را جابه‌جا می‌کند (برخی کلیدها چند مگابایت؛ v33.21 با پول دلتا و بچ ≤۲۰ التیام داده ولی ریشه تغییر نکرده). برای تعداد کاربر بیشتر (هدف PTF-SCALE-P0) و کلاینت واقعاً نازک: موجودیت‌های داغ (invoices، cheques، offers، customers، fin_events) باید **API رکورد-محور** بگیرند (الگوی موجود `sales-domain.php` با operationId/ACK سرمشق است) و بلاب‌ها فقط برای دُم‌بلند داده بمانند.

### GAP-9 — ابزارهای ریکاوری/DIY و مونولیت 🟢 فرعی
`recover.html`، `force-restore.html`، `clear-cache.html`، `sync-diagnostics.html` مستقیم LS را می‌خوانند/می‌نویسند — باید IDB-aware شوند. مونولیت ۵MB جی‌اس و `index.html` ۳۲۶KB موضوع «نازک‌سازی تحویل» است؛ طبق `ARCHITECTURE-GUARDRAILS.md` معماری اسکریپت سراسری عمداً حفظ می‌شود، پس lazy-load تدریجی پنل‌ها پیشنهاد می‌شود نه بازنویسی.

---

## ۵) نقشهٔ راه پیشنهادی (۶ فاز)

> اصل راهنما: **«دادهٔ کسب‌وکار فقط از لایهٔ داده عبور می‌کند؛ localStorage فقط برای کش سبکِ قابل‌تخلیه و نشست.»** هر فاز مستقل و قابل انتشار است؛ ترتیب طوری است که هر فاز ریسک فاز بعد را کم می‌کند.

### فاز ۰ — بستن درِ آب (گیت) — کوچک، این هفته
1. قاعدهٔ **A8 در arch-guard**: منع `localStorage.setItem/getItem` خارج از فهرست سفید (client-server.js، sync.js، storage-quota.js، storage.js، rbac.js برای نشست، backup.js، ابزارهای *.html). baseline فعلی = بدهی مجاز؛ **افزایش = شکست CI**.
2. اتصال arch-guard + تسترهای مرتبط (tester514، tester227-229، tester294، tester298) به `deploy-staging.yml` (به‌جای پچ‌های PENDING معلق، گیت واقعی).
3. واحد نگهداری کلیدها: فهرست واحد `PTF_STORAGE_KEYS` (طبقه‌بندی: business / cache / ui / infra / ephemeral) که sync.js و client-server.js و گارت از همان بخوانند (رفع سه نسخهٔ موازی: SYNC_KEYS، bKeysFallback، IDB_KEYS).

### فاز ۱ — حذف بایپس‌های دادهٔ کسب‌وکار — متوسط، ۱-۲ هفته
4. تبدیل ۵۹ نقطهٔ مستقیم به لایهٔ داده؛ اولویت با GAP-1: `finance-write-guard` (fin_events)، `inqreader` (products)، `index.html:3670` و `codegen` (settings)، fallbackهای مردهٔ ۵ فایل (حذف کامل شاخهٔ else).
5. کلیدهای «فقط-دستگاه»ی واقعی یا به سرور می‌روند یا اعلام اکار می‌شوند:
   - `ptf_personal_cheques_<user>` → کلید مشترک سینک‌شونده (دادهٔ شرکت است، نه شخصی-دستگاهی) + مهاجرت یک‌بارهٔ رکوردها.
   - صف کدینگ (`ptf_code_tmp_queue`، `ptf_code_duplicate_plan`) → کلید سینک‌شونده یا سرور.
   - ریکاوری امضا (`letters`) و پیش‌نویس‌ها (`draftx/offers/sales-domain-v2`) → IDB (سنگین) یا کلید سینک‌شونده (اگر عملیاتی-حیاتی).
6. مهاجرت یک‌باره با همان الگوی امن هم‌گرایی (ACK سرور قبل از حذف محلی).

### فاز ۲ — زیرساخت فاز B روی IDB — متوسط، ۱ هفته
7. انتقال `ptf_b_queue` + نشانگرهای `ptf_b_*` به IndexedDB (نوشتن همگام در حافظهٔ نشست + فلاش async به IDB؛ crash-safe چون تا ACK سرور، رکورد صف حیاتی است).
8. رفع GAP-3: گارانتی رندر — پس از `ptfBIdbPreload` و پس از اولین pull موفق، re-render قطعی نماهای باز (یا مسدودکردن اولین رندر پنل داده‌ای تا پایان preload با اسکلتون).
9. تست: قطع برق/رفرش وسط تایپ، آفلاین طولانی، تعویض اکانت، دو تب همزمان.

### فاز ۳ — نشست و رسانه — کوچک-متوسط
10. توکن به **کوکی HttpOnly** (تغییر `auth_login/role_verify` و هدرخوان‌ها در یک لایهٔ `ptfAuth`)؛ حداقلِ قابل‌قبول: sessionStorage + تمرکز ۴۹ ارجاع در یک ماژول.
11. `ptf_chqprint_bg` و پروفایل‌های چاپ چک → آروان S3 (api/storage.php موجود است).
12. کش‌های bridge/fx → read-through با TTL و پاک‌سازی خودکار (خارج از سهمیهٔ LS).

### فاز ۴ — رکورد-محور شدن سرور (کلاینت نازک واقعی) — بزرگ، تدریجی
13. برای ۵ موجودیت داغ (invoices، cheques_issued/received، offers، customers، fin_events): API رکوردی (upsert/delete/by-query) با rev رکوردی، بر اساس الگوی `sales-domain.php`؛ بلاب‌ها فقط آرشیو/دُم‌بلند.
14. سمت کلاینت: لایهٔ repository روی همان `getData/setData` تا ماژول‌های UI دست‌نخورده بمانند (سازگار با قرارداد گاردها).
15. صف آفلاین رکوردی (idempotent با operationId — الگوی موجود).

### فاز ۵ — پاکسازی نهایی و انتشار — کوچک
16. جاروی یک‌بارهٔ کلیدهای legacy پس از rollout (گزارش «چه چیزی مانده» در sync-diagnostics)، به‌روزرسانی ۴ ابزار HTML ریکاوری.
17. انتشار پروداکشن v34.8.x (طبق RCA روی استیجینگ اعمال شده؛ انتشار منوط به تأیید کارفرما) + soak test ۲-۳ روزهٔ دستگاه قدیمی: سناریوی «تعویض اکانت → هم‌گرایی → تخلیهٔ خودکار → پاک‌سازی کش» باید بدون هیچ گام دستی کار کند.
18. معیار پذیرش نهایی (DoD): در یک هفتهٔ کاری واقعی، مجموع کلیدهای غیر-کش در localStorage < ~۲۰KB بماند؛ حادثهٔ «۱۰۰٪» ساختاراً ناممکن؛ تستر رفتاری شبیه‌ساز دستگاه قدیمی در CI سبز.

---

## ۶) ریسک‌ها و کنترل‌ها

| ریسک | کنترل |
|---|---|
| گم‌شدن داده هنگام مهاجرت کلیدهای فقط-دستگاه | الگوی موجود: ACK کامل سرور قبل از هر حذف محلی؛ صف غیرخالی = توقف مهاجرت؛ بکاپ پیش از مهاجرت (`save_backup`) |
| رگرسیون در نقش‌های محدود (sales/buyer/…) که دامنهٔ sync محدودی دارند | تست role-matrix موجود در sync.js + تسترهای UAT نقش‌محور |
| IDB در مرورگرهای خیلی قدیمی/حالت خصوصی نبود | گاردهای `idbUsable()` موجود باید در تمام مسیرهای جدید هم نگه داشته شود؛ fallback = وضعیت امروز |
| دو تب همزمان و صف IDB | singleton نوشتن صف (WebLock یا leader election سبک) + تست دو-تبی |
| کندی پول دلتا با رشد داده | پول رکوردی فاز ۴ + پایش `sync_stats` سرور |
| بازگشت عادت setItem مستقیم | قاعدهٔ A8 + CI (فاز ۰) — پیش از هر کار دیگر |

---

## ۷) ترتیب اجرای پیشنهادی (خلاصه یک‌نگاهی)

```
فاز ۰ (گیت CI + قاعدهٔ A8 + فهرست واحد کلیدها)   ← اول از همه، جلوی پس‌رفتگی
فاز ۱ (۵۹ بایپس → لایهٔ داده؛ کلیدهای فقط-دستگاه) ← حذف وابستگی واقعی
فاز ۲ (صف آفلاین→IDB؛ رفع ریس سردبوت)            ← استحکام فاز B
فاز ۳ (توکن HttpOnly؛ چک‌پرینت‌bg→S3؛ کش‌ها)      ← امنیت و سبک‌سازی
فاز ۴ (API رکوردی ۵ موجودیت داغ)                 ← مقیاس + کلاینت واقعاً نازک
فاز ۵ (جارو legacy + rollout پروداکشن + DoD)      ← پایان رسمی وابستگی
```

**ضمیمهٔ الف — پراکندگی نوشتن مستقیم (۶۲ نقطه با احتساب fallbackها):**
index.html=20، codegen=10، bridge=7، sales-domain-v2=6، cheque-print=6، rbac=3، offers=3، messengers=3، inqreader=3، cheques=3، ai-workbench=3، tour=2، letters=2، draftx=2، و ۱۶ فایل تک-نقطه‌ای (treasury، treasury-call، surplus، shell، phonefmt، perms، offers-pro، offerlock، myday، my-customers-filter، lossguard، ledger-report، golive، fx، finance-write-guard، case-revision).

**ضمیمهٔ ب — منابع درون‌ریپو:** `ARENA-STORAGE-INDEPENDENCE-RCA-2026-08-26.md`، `crm/DESIGN-PHASE-B-CLIENT-SERVER.md`، `ARCHITECTURE-GUARDRAILS.md`، `_tools/arch/arch-guard.js`، `_tools/uat/tester514-v34.8.9-storage-independence.js`، `crm/MIGRATION-MYSQL-SPEC-v1.md`.
