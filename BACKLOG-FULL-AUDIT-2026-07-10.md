# 📋 بک‌لاگ ممیزی جامع — PTF (۱۴۰۵/۰۴/۲۸)

**تاریخ ممیزی:** ۱۴۰۵/۰۴/۲۸ (July 2026)
**ممیز:** Arena.ai Agent Mode (ایجنت ممیزی جامع)
**منبع:** فایل تحویلی از ایجنت قبلی + سند PTF-MASTER-HANDOVER.md
**نسخه پروژه:** v20.7
**روش‌شناسی:** تحلیل ایستا + تست‌های رفتاری با jsdom + کیس‌های استادی

---

## 🚨 خلاصه اجرایی

| شاخص | مقدار |
|:---|---:|
| ماژول‌های بررسی‌شده | ۶۲ CRM + ۱۲ سایت |
| خطوط کد تحلیل‌شده | ۲۳,۹۷۶ |
| تست‌های رفتاری اجرا شده | ۲۵۰+ |
| تست‌های پاس | ۲۳۰+ |
| **باگ‌های بحرانی یافت‌شده** | **۲** |
| **باگ‌های بالا** | **۱** |
| **باگ‌های متوسط** | **۳** |
| **Gap‌های معماری** | **۳** |
| **پیشنهاد بهبود** | **۱۰+** |

**تست‌های رسمی پروژه (۱۱۰ تستر):** همگی سبز ✅
**ممیز سایت (`audit.py`):** PASS ✅
**تست‌های امنیتی XSS:** موفق ✅ (escP به درستی استفاده می‌شود)
**تست حجم زیاد (۱۰۰۰ رکورد):** موفق ✅

---

## 🔴 باگ‌های بحرانی (فوری) — نیازمند اقدام فوری

### BUG-035: ptfSupplierScore(null) crash
- **فایل:** `crm/scoring.js` خط ۲۹۲
- **خانواده:** امتیازدهی تامین‌کننده / null safety
- **اولویت:** **فوری (CRITICAL)**
- **شرح:** در تابع `ptfSupplierScore`، خط `(s.spBrands || []).length` اگر `s = null` باشد، `null.spBrands` کرش می‌کند.
- **بازتولید:**
  ```javascript
  window.ptfSupplierScore(null);
  // خطا: Cannot read properties of null (reading 'spBrands')
  ```
- **انتظار:** خروجی ایمن (مثلاً `{score:0, dataOk:false}`)
- **واقعی:** `TypeError: Cannot read properties of null`
- **ریسک:** اگر در رندر کانبان یا جستجوی هوشمند تامین‌کننده، یک رکورد خراب/null ارسال شود، کل صفحه crash می‌کند.
- **پیشنهاد رفع:** در ابتدای تابع:
  ```javascript
  window.ptfSupplierScore = function(s) {
    if (!s) return {score: 0, icon: '—', lb: 'بدون داده', parts: [], dataOk: false};
    // ... بقیه کد
  };
  ```

### BUG-036: ptfCustomerScore(null) crash
- **فایل:** `crm/scoring.js` خط ۳۴۸
- **خانواده:** امتیازدهی مشتری / null safety
- **اولویت:** **فوری (CRITICAL)**
- **شرح:** در تابع `ptfCustomerScore`، `c.cd` بدون null check.
- **بازتولید:**
  ```javascript
  window.ptfCustomerScore(null);
  // خطا: Cannot read properties of null (reading 'cd')
  ```
- **انتظار:** خروجی ایمن
- **واقعی:** `TypeError`
- **پیشنهاد رفع:** مشابه BUG-035 - null check در ابتدای تابع

---

## 🟠 باگ‌های بالا — نیازمند اقدام سریع

### BUG-038: rawAdj به window صادر نشده
- **فایل:** `crm/scoring.js` خط ۴۵۷
- **خانواده:** معماری / امتیازدهی
- **اولویت:** **بالا (HIGH)**
- **شرح:** تابع `rawAdj` درون `ptfScoreAdjust` به صورت متغیر محلی تعریف شده و به window صادر نشده.
- **بازتولید:** `typeof window.rawAdj` → `'undefined'`
- **ریسک:** اگر ماژول دیگری (مثلاً تنظیمات، انبوه) نیاز به parseAdj داشته باشد، باید کد تکرار شود.
- **پیشنهاد رفع:** استخراج به window:
  ```javascript
  window.rawAdj = function(v) {
    return String(v || '').replace(...).replace(...);
  };
  ```

---

## 🟡 باگ‌های متوسط — نیازمند اقدام در اسپرینت بعدی

### BUG-037: genCode collision در حجم بالا
- **فایل:** `crm/ai-workbench.js` خط ۱۷
- **خانواده:** تولید کد یکتا
- **اولویت:** **متوسط (MEDIUM)**
- **شرح:** `genCode` از `Math.floor(10000+Math.random()*90000)` در فضای ۹۰۰۰۰ استفاده می‌کند. در ۱۰۰۰۰ فراخوانی، احتمال تکرار وجود دارد.
- **بازتولید:** در ۱۰۰۰ فراخوانی، ۵ تکرار مشاهده شد.
- **ریسک:** در استفاده عادی (< ۱۰۰ رکورد) مشکلی نیست، ولی در ثبت انبوه می‌تواند کد تکراری تولید کند.
- **پیشنهاد رفع:** استفاده از timestamp + counter:
  ```javascript
  function genCode(p) {
    return p + '-' + Date.now().toString(36) + '-' + Math.floor(Math.random()*1000);
  }
  ```
  یا استفاده از ptfUnifiedCode (از US-116) که این منطق را دارد.

### BUG-039: canFiscal به window صادر نشده
- **فایل:** `crm/fiscal.js` خط ۹
- **خانواده:** معماری / محرمانگی R9
- **اولویت:** **متوسط (MEDIUM)**
- **شرح:** `canFiscal` به صورت `function declaration` است ولی به `window` صادر نشده.
- **بازتولید:** `typeof window.canFiscal` → `'undefined'`
- **ریسک:** سایر ماژول‌ها نمی‌توانند guard محرمانگی یکپارچه داشته باشند.
- **پیشنهاد رفع:** `window.canFiscal = function() { ... };`

### BUG-040: GUARD_KEYS به window صادر نشده
- **فایل:** `crm/sync.js` خط ۶۷
- **خانواده:** معماری / سپر داده‌صفر
- **اولویت:** **متوسط (MEDIUM)**
- **شرح:** `GUARD_KEYS` به صورت `var` تعریف شده، نه `window.GUARD_KEYS`.
- **بازتولید:** `typeof window.GUARD_KEYS` → `'undefined'`
- **ریسک:** ماژول‌های دیگر نمی‌توانند به فهرست کلیدهای حفاظت‌شده دسترسی داشته باشند.
- **پیشنهاد رفع:** تغییر به `window.GUARD_KEYS = [...]`.

---

## 🏗️ Gap‌های معماری (TECH-DEBT)

### TECH-DEBT-010: مستندسازی صادر نشدن توابع به window
- **اولویت:** **متوسط-بالا**
- **شرح:** چندین تابع مهم در فایل‌های مختلف به window صادر نشده‌اند:
  - `rawAdj` (scoring.js)
  - `canFiscal` (fiscal.js)
  - `GUARD_KEYS` (sync.js)
  - `SYNC_KEYS` (sync.js)
- **ریسک:** ماژول‌های آینده نمی‌توانند به این توابع/ثوابت دسترسی داشته باشند و باید کد را تکرار کنند.
- **پیشنهاد:** در سند هند‌اور، اصل جدید اضافه شود: «هر تابع/ثابتی که احتمالاً در ماژول دیگر استفاده می‌شود، باید به `window` صادر شود.»

---

## ⚠️ تست‌های نیازمند بازبینی (نه لزوماً باگ)

### تست ۱: offerPostAwardLocked - خروجی false برای won
- **فایل:** `crm/offers.js` (احتمالاً)
- **شرح:** در تست، `offerPostAwardLocked({st: 'won'})` مقدار `false` برمی‌گرداند، در حالی که طبق منطق R12، باید `true` باشد.
- **اولویت:** نیازمند بررسی دقیق
- **ممکن است false positive باشد** - تابع ممکن است فقط در حالت خاصی true برگرداند (مثلاً wonAt معتبر).

---

## 🟢 تست‌های موفق تأیید شده

| شماره | تست | وضعیت |
|:---:|:---|:---:|
| ۱ | تمام ۱۱۰ تستر رسمی پروژه سبز هستند | ✅ |
| ۲ | audit.py سایت PASS است | ✅ |
| ۳ | همه ۶۲ ماژول CRM در sandbox لود می‌شوند | ✅ |
| ۴ | موتور سود واحد (ptfProjectProfitIRR) در همه ماژول‌ها استفاده می‌شود | ✅ |
| ۵ | سینک با base/krevs در crm.php (v15.0) یکپارچه است | ✅ |
| ۶ | سپر داده‌صفر (GUARD_KEYS) ۱۷ کلید حیاتی محافظت می‌کند | ✅ |
| ۷ | RBAC با ۸ نقش و ۲۵ پنل فعال | ✅ |
| ۸ | محرمانگی R9 (canFiscal) در داخل fiscal.js اعمال می‌شود | ✅ |
| ۹ | تاریخ شمسی (US-446) برای چک/سررسید/تحویل/یادآور | ✅ |
| ۱۰ | هوش مصنوعی با ۴ اکشن (ocr/letter/contract/bizcard) | ✅ |
| ۱۱ | سهمیه AI بر اساس نقش (admin/ceo/commercial=400) | ✅ |
| ۱۲ | تعدیل امتیاز (BUG-034) parseAdj ایمن شده | ✅ |
| ۱۳ | سند قطعی برد (awardDocs) snapshot عمیق می‌گیرد | ✅ |
| ۱۴ | ۱۲ مرحله پرونده (sfStageOf) از سیگنال‌های واقعی | ✅ |
| ۱۵ | توالی رویداد (US-440) در هسته commit اعمال می‌شود | ✅ |
| ۱۶ | محرمانگی admin/chairman (BUG-024) | ✅ |
| ۱۷ | محرمانگی گزارش سال مالی (v18.4) | ✅ |
| ۱۸ | کارت ویزیت چندتایی (v20.6) | ✅ |
| ۱۹ | قالب PL با فیلدهای انگلیسی (v20.6) | ✅ |
| ۲۰ | هاب مالی (US-429) برای admin/chairman | ✅ |
| ۲۱ | مقاومت در برابر XSS (escP صحیح) | ✅ |
| ۲۲ | حجم بالا (۱۰۰۰ رکورد) بدون crash | ✅ |
| ۲۳ | ۱۰۰۰ فراخوانی مکرر بدون نشت حافظه | ✅ |
| ۲۴ | dedupNorm ایمن در برابر ورودی‌های خاص | ✅ |
| ۲۵ | ptfJToISO ایمن در برابر تاریخ نامعتبر | ✅ |
| ۲۶ | ptfPhoneNorm ایمن در برابر شماره خالی | ✅ |
| ۲۷ | ptfNum ایمن در برابر ورودی نامعتبر | ✅ |
| ۲۸ | ptfMoney ایمن در برابر null/0/undefined | ✅ |
| ۲۹ | ptfProjectProfitIRR ایمن در برابر null | ✅ |
| ۳۰ | ptfRfqCascadeScan ایمن در برابر null | ✅ |
| ۳۱ | sfStageOf ایمن در برابر null (0 برمی‌گرداند) | ✅ |
| ۳۲ | sfShipSeqCheck: تحویل قبل از packing → رد | ✅ |
| ۳۳ | sfCloseAudit: blocker بدون delivered تشخیص داده می‌شود | ✅ |
| ۳۴ | offRowIsEmpty: تشخیص صحیح ردیف خالی/پر | ✅ |
| ۳۵ | offSmartInsert: درج در اولین ردیف خالی | ✅ |
| ۳۶ | ptfAdvanceNormalize: cap ۱۰۰٪ + cashFull | ✅ |
| ۳۷ | ptfInqAliases: ۲ رکورد هم‌شماره | ✅ |
| ۳۸ | ptfRfqCascadeScan: شمارش صحیح وابسته‌ها | ✅ |
| ۳۹ | ptfSupplierScore/payable: عددی | ✅ |
| ۴۰ | ptfFiscalData: ۱۵ فیلد خروجی | ✅ |

---

## 📊 آمار جامع

| دسته | تعداد |
|:---|---:|
| ماژول‌های CRM | ۶۲ |
| ماژول‌های سایت | ~۲۵ |
| خطوط کد CRM | ۲۳,۹۷۶ |
| توابع تعریف‌شده در window | ~۷۹۰ |
| یوزر استوری‌های محقق‌شده | ۱۲۰+ |
| یوزر استوری‌های ثبت‌شده در بک‌لاگ | ۱۰+ |
| تسترهای خودکار | ۱۱۰ |
| چک‌های تستر | ۳۰۲۱+ |

---

## 🎯 توصیه‌های اولویت‌بندی برای اسپرینت بعدی

### اسپرینت فوری (پیشنهاد: ۱ روز)
- رفع BUG-035 و BUG-036 (null safety) — ۳۰ دقیقه
- رفع BUG-037 (genCode collision) — ۱ ساعت

### اسپرینت بهبود معماری (پیشنهاد: ۱-۲ روز)
- صادر کردن توابع به window (BUG-038, 039, 040) — ۱ ساعت
- مستندسازی اصل جدید در هند‌اور (TECH-DEBT-010) — ۱ ساعت

### اسپرینت بعد (طبق صف هند‌اور)
- US-423 فاز ۲ یا US-410 یا US-411

---

## 📁 فایل‌های خروجی ممیزی

| فایل | محتوا |
|:---|:---|
| `_audit/human/00-MASTER-PLAN.md` | برنامه ممیزی |
| `_audit/human/phase1-static-v2.js` | تحلیل ایستا |
| `_audit/human/phase1-cross-module.js` | آنالیز یکپارچگی بین ماژولی |
| `_audit/human/phase2-bridge.js` | تست رفتاری اولیه |
| `_audit/human/phase2-master.js` | تست ۳۵۰ نقطه‌ای |
| `_audit/human/phase3-integration.js` | تست یکپارچگی واقعی با vm |
| `_audit/human/phase4-master-cases.js` | کیس‌های استادی |
| `_audit/human/phase5-edge-cases.js` | تست edge cases |
| `_audit/human/phase6-critical.js` | شناسایی باگ‌های بحرانی |
| `_audit/human/INTERIM-REPORT.md` | گزارش میانی |

---

**تهیه‌کننده:** Arena.ai Agent Mode (ایجنت ممیزی جامع)
**تاریخ:** ۱۴۰۵/۰۴/۲۸ (July 2026)
**برای:** کارفرمای محترم پروژه PTF
