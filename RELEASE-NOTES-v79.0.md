# 🚀 ریلیز v79.0 — اسپرینت ۷۹: همگام‌سازی کل داده CRM بین دستگاه‌ها (US-151 فاز ۲)
**تاریخ:** تیر ۱۴۰۵ | بزرگ‌ترین ارتقای زیرساختی: CRM از «تک‌مرورگری» به **چنددستگاهه واقعی** تبدیل شد

---

## 🎯 چه مشکلی حل شد؟
تا دیروز: استعلامی که در کروم ثبت می‌کردید، در موبایل/فایرفاکس **وجود نداشت**. هر مرورگر جزیره جدا بود (همان یافته S1 ممیزی — ریشه باگ ورود هم همین بود).

از این نسخه: **همه ۲۶ نوع داده CRM** (استعلام‌ها، مشتریان، تامین‌کنندگان، پیشنهادها، فاکتورها، پرونده‌ها، نامه‌ها، لیدها، یادآورها و…) به‌صورت خودکار با سرور و بین همه دستگاه‌ها همگام می‌شوند.

## ⚙️ چطور کار می‌کند؟ (موتور sync.js)

| رویداد | رفتار |
|---|---|
| هر تغییری در CRM (ثبت/ویرایش/حذف) | ۴ ثانیه بعد خودکار به سرور push می‌شود (چند تغییر پشت‌سرهم = یک ارسال) |
| هر ۲۰ ثانیه | نسخه (rev) سرور چک می‌شود؛ اگر دستگاه دیگری چیزی تغییر داده → دریافت و اعمال + پیام «🔄 X بخش از دستگاه دیگر به‌روز شد» |
| بستن صفحه | تغییرات معلق با sendBeacon ارسال می‌شوند (چیزی گم نمی‌شود) |
| آفلاین | تغییرات محلی می‌مانند، نشانگر 🔴، با اولین اتصال ارسال می‌شوند |
| وسط کار بودن کاربر (مودال باز) | رندر مجدد به تعویق می‌افتد — کار شما قطع نمی‌شود |
| اولین اجرا پس از آپدیت | دستگاهی که داده دارد، سرور را seed می‌کند (خودکار) |

**نشانگر وضعیت** در نوار بالای CRM: 🟢 همگام / 🟡 در حال تلاش / 🔴 آفلاین

## 🔐 امنیت و ایمنی داده
- ذخیره‌سازی در `crm/data/sync/` — کاملاً مسدود از وب (htaccess)
- whitelist کلیدها (فقط داده‌های CRM)، سقف حجم per-key و کل، قفل نوشتن فایل
- تعارض همزمان: «آخرین نویسنده می‌برد» + ثبت نام نویسنده و زمان per-key
- تغییرات محلیِ ارسال‌نشده هرگز توسط pull له نمی‌شوند
- بک‌آپ ساعتی v71 همچنان جداگانه فعال است (دو لایه محافظت)

## 📋 بعد از آپلود (مهم)
1. `pishtaj-release-v79-delta.zip` را آپلود کنید.
2. **اول از دستگاه اصلی** (کروم که همه داده‌ها را دارد) وارد CRM شوید → در لاگ: «داده‌های این دستگاه به سرور منتقل شد (seed اولیه)» + نشانگر 🟢.
3. حالا از موبایل/فایرفاکس وارد شوید → چند ثانیه بعد همه استعلام‌ها/مشتریان/پیشنهادها ظاهر می‌شوند ✅
4. تست دوطرفه: در موبایل یک یادآور بسازید → حداکثر ۲۰ ثانیه بعد در کروم پیام «🔄 به‌روز شد» می‌آید.

⚠️ توجه: ترتیب مرحله ۲ مهم است — اولین دستگاهی که بعد از آپدیت وارد شود و داده داشته باشد، مرجع اولیه سرور می‌شود.

## 🧪 کیفیت
| مورد | نتیجه |
|---|:---:|
| tester16 جدید (شبیه‌سازی دو دستگاه: push/pull/fresh/LWW) | ✅ ۲۹/۲۹ |
| رگرسیون tester1–15 | ✅ همه PASS |
| ممیز ۸بخشی | ✅ PASS |

## 📁 دلتا (۴ فایل کوچک)
`api/crm.php` • `crm/sync.js` (**جدید**) • `crm/index.html` • `crm/sw.js`

## 🔭 باقیمانده US-151 (فاز ۳ — اسپرینت بعد در صورت تایید شما)
Session سروری واقعی (کوکی HttpOnly به‌جای هدر Role) — چون حالا داده روی سرور است، این لایه امنیتی اهمیت بیشتری پیدا کرده. طبق رودمپ بعدش: حلقه قیمت + جستجوی سراسری (اسپرینت ۸۰).
