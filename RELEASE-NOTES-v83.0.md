# 🚀 ریلیز v83.0 — اسپرینت ۸۳: عکس پروفایل + عرض ثابت فضای کاری + دسترسی ساده مجاز/غیرمجاز

**تاریخ:** ۱۳ تیر ۱۴۰۵ | **دامنه:** US-179 + US-180 + US-181 (دستور جدید کارفرما)

---

## 🖼 US-179 — عکس پروفایل

- در **تنظیمات** بخش «🖼 عکس پروفایل» اضافه شد + میان‌بر سریع: **کلیک روی دایره نام کاربر** در سایدبار (برای همکارانی که پنل تنظیمات ندارند).
- عکس خودکار به **۱۲۸×۱۲۸ فشرده** می‌شود (حجم ناچیز، بدون اشغال فضای ابری) و در دایره کاربر نمایش داده می‌شود.
- **سینک بین دستگاه‌ها**: کلید جدید `ptf_crm_avatars` به سینک سروری، بک‌آپ ساعتی و whitelist سرور اضافه شد — عکس در موبایل و کامپیوتر یکی است.
- امکان حذف عکس در هر لحظه.

## 📐 US-180 — رفع پرش فضای کاری (بازخورد شما)

ریشه مشکل دو چیز بود و هر دو رفع شد:

| علت پرش | راه‌حل |
|---|---|
| بعضی پنل‌ها `max-width` موضعی داشتند و عرض‌شان با هم فرق می‌کرد | همه پنل‌ها حالا **تا منتها الیه سمت چپ** کشیده می‌شوند (`width:100%; max-width:none`) |
| با کوتاه/بلند شدن محتوا، اسکرول‌بار عمودی ظاهر/محو می‌شد و کل صفحه چند پیکسل جابجا می‌شد | جای اسکرول‌بار **همیشه رزرو** است (`scrollbar-gutter:stable`) — عرض صفحه در هیچ جابجایی بین ماژول‌ها تغییر نمی‌کند |

نتیجه: با کلیک روی هر ماژول، **فقط محتوای فضای کاری عوض می‌شود** — هیچ پرش و تغییر عرضی رخ نمی‌دهد.

## 🎛 US-181 — دسترسی کاربران: فقط «مجاز / غیرمجاز»

مشکل شما: «مدیر نمی‌داند پیش‌فرض دسترسی کاربران چیست» — حل شد:

- ستون **«پیش‌فرض نقش» حذف شد**؛ مودال 🎛 دسترسی حالا فقط دو ستون دارد: **✅ مجاز | ⛔ غیرمجاز**.
- وضعیت پیش‌گزیده هر ردیف = **دسترسی واقعیِ همین الانِ همان کاربر** (چه از نقشش آمده باشد چه از تنظیم قبلی) — مدیر دقیقاً می‌بیند کاربر چه می‌بیند و با یک کلیک عوض می‌کند.
- ماژول‌هایی که قبلاً خارج از نقش تنظیم شده‌اند برچسب زرد **«شخصی»** دارند.
- دکمه‌های کمکی: **«✅ همه مجاز» | «⛔ همه غیرمجاز» | «↩️ بازگشت به پیش‌فرض نقش»**.
- 🔧 فنی: ذخیره هوشمند است — فقط تفاوت با پیش‌فرض نقش به‌عنوان override ثبت می‌شود؛ ساختار داده `ptf_crm_perms` و سینک قبلی **بدون تغییر** ماند (تنظیمات قبلی‌تان سالم است).

## ✅ کیفیت

| مورد | نتیجه |
|---|---|
| ممیز ۸بخشی (۴۳۴ صفحه) | ✅ PASS |
| `node --check` JSهای تغییرکرده | ✅ PASS |
| تستر جدید `tester20-sprint83` | ✅ **27/27 PASS** |
| رگرسیون ۱۹ تستر قبلی | ✅ همه PASS |
| نسخه کش | `ptf-crm-v83.0` (پاکسازی خودکار کش همکاران) |

## 📦 نصب (File Manager)

**`pishtaj-release-v83.0-delta.zip`** را روی هاست باز کنید. فایل‌ها: `crm/theme.js`، `crm/perms.js`، `crm/sync.js`، `crm/backup.js`، `crm/index.html`، `crm/sw.js`، `api/crm.php` + مستندات. **داده‌ای پاک نمی‌شود.**

> ℹ️ این دلتا شامل تغییرات v81 و v82 هم هست؛ اگر آن‌ها نصب نشده‌اند، دلتاهای v80.2 و v82.0 را هم باز کنید (فایل‌های `crm/dedup.js`، `crm/archive.js`، `crm/storage.js`، `crm/projects.js`، `crm/offers.js`، `crm/leads.js`، `crm/bridge.js`، `api/storage.php` و تصاویر products فقط در آن‌ها هستند).

## 🔔 یادآوری‌های باز
- 🧪 تست پیشنهادی: آپلود عکس پروفایل از تنظیمات + جابجایی بین ۵-۶ ماژول (نباید هیچ پرشی ببینید) + باز کردن 🎛 دسترسی یک کاربر (فقط دو ستون).
- ❓ استعلام از هاست: فعال بودن `ZipArchive` (بایگانی فشرده v82).
- 🌐 Search Console (۱۱۱ URL) و 📁 مدارک US-160 همچنان باز.
- ⏭ اسپرینت ۸۴: US-172 (ایمپورت هوشمند اکسل سراسری) + US-173 AC4 (ستون‌های TO/CO درگ + تصویر سلول).
