# ریلیزنوت `v28.3` — Sprint 283
## حاکمیت قفل سال مالی، جست‌وجوی حساب‌ها و ضمیمهٔ ابری اجباری

## ۱) بازکردن کنترل‌شدهٔ قفل سال مالی
### ریشهٔ مسئله
قفل سال در `ptf_crm_fiscal_snapshots` ذخیره و بین دستگاه‌ها sync می‌شود. بازگردانی بک‌آپ کامل، هم داده‌های جدید را در معرض حذف می‌گذارد و هم ممکن است پس از sync، نسخهٔ قفل‌شده را دوباره از سرور یا دستگاه دیگر دریافت کند.

### اصلاح
- فقط **رییس هیات‌مدیره** (`chairman`) در هاب مالی → سال مالی، کنار snapshot قفل‌شده دکمهٔ **«بازکردن قفل»** را می‌بیند.
- دلیل بازگشایی الزامی است؛ زمان، نام انجام‌دهنده، دلیل و شناسهٔ snapshot در audit ثبت می‌شود.
- snapshot حذف یا بازنویسی نمی‌شود؛ با برچسب «snapshot محفوظ پس از بازگشایی» قابل چاپ باقی می‌ماند.
- بازگشایی فقط اجازهٔ ثبت/اصلاح اسناد همان سال را برمی‌گرداند؛ هیچ سندی را خودکار تغییر نمی‌دهد.
- ادغام sync برای این کلید، بازگشایی جدیدتر را بر نسخهٔ قفل قدیمی مقدم می‌دارد تا پس از چند ثانیه قفل دوباره ظاهر نشود.

## ۲) حساب مشتریان و تأمین‌کنندگان
- جست‌وجوی نام یا کد در هر دو تب افزوده شد.
- تمام مشتریان و تأمین‌کنندگان نمایش داده می‌شوند.
- حساب‌های دارای ماندهٔ غیرصفر همیشه ابتدا و بر اساس بزرگی مانده مرتب می‌شوند؛ حساب‌های صفر پس از آن‌ها و بر اساس نام دیده می‌شوند.
- مانده‌ها فقط از منابع فعلی محاسبه می‌شوند؛ هیچ جدول یا ماندهٔ موازی جدیدی ایجاد نشده است.

## ۳) سیاست «فقط فضای ابری» برای ضمیمه‌ها
### یافتهٔ ممیزی
- ضمیمه‌های داخلی CRM از قبل با upload مستقیم مرورگر به فضای S3 آروان کار می‌کردند.
- اما ضمیمه‌های فرم عمومی RFQ، فرم ثبت‌نام تأمین‌کننده و فرم تماس، در دو مسیر قدیمی با `move_uploaded_file` روی هاست نگه‌داری می‌شدند.

### اصلاح
- مسیرهای مذکور اکنون فایل موقت request را مستقیم به S3 stream می‌کنند و فقط metadata شامل `key`، نام و حجم در سامانه نگه داشته می‌شود.
- در نبود یا خطای فضای ابری، درخواستِ دارای فایل با خطای روشن متوقف می‌شود؛ fallback دائمی روی هاست وجود ندارد.
- آپلودهای داخلی نیز در صورت خطای cloud، دیگر یک «پیوست صف محلی» بدون فایل واقعی ثبت نمی‌کنند.
- پیوست ابریِ استعلام سایت بعد از تایید، در فهرست عادی ضمیمه‌های همان درخواست نیز قابل مشاهده است.

### فایل‌های قدیمی روی هاست
هیچ فایل قدیمی خودکار حذف یا جابه‌جا نشده است. برای جلوگیری از حذف ناخواسته، UI آن‌ها را «پیوست قدیمیِ هاست» نشان می‌دهد. انتقال/حذف این فایل‌ها نیازمند inventory، گزارش اثر، بک‌آپ و تأیید صریح شماست.

## کنترل‌های فنی
- `tester156-v283-governance-accounts-storage.js`: **18 PASS / 0 FAIL**
  - بازگشایی توسط chairman، منع admin، حفظ snapshot، audit و دو سناریوی merge sync
  - ترتیب و جست‌وجوی مشتری/تأمین‌کننده
  - کنترل مسیر cloud-only در source
- `tester155-v282-to-save-scope.js`: **7 PASS / 0 FAIL**
- `tester154-v281-finance-foundation.js`: **15 PASS / 0 FAIL**
- parse موفق همهٔ 67 اسکریپت لودشده.
- `audit.py`: بدون خطا؛ فقط هشدار قدیمی حجم 5 تصویر.
- PHP CLI در محیط ساخت موجود نبود؛ بنابراین lint واقعی فایل‌های PHP باید روی هاست یا CI دارای PHP اجرا شود.

## UAT پیشنهادی
1. با نقش رییس هیات‌مدیره به **هاب مالی → سال مالی** بروید؛ کنار سال قفل‌شده «بازکردن قفل» را بزنید، دلیل وارد کنید و سپس پس از حدود ۳۰ ثانیه/رفرش کنترل کنید که قفل برنگشته باشد.
2. با نقش admin کنترل کنید که امکان بازگشایی قفل را ندارد.
3. در **حساب مشتریان** و **حساب تأمین‌کنندگان**، نام یا کد جست‌وجو کنید و ترتیب مانده‌های غیرصفر را کنترل کنید.
4. از یک فرم عمومی سایت با یک فایل کوچک آزمایشی استفاده کنید؛ در CRM جزئیات درخواست باید برچسب «پیوست در فضای ابری» و دکمهٔ مشاهده داشته باشد.
5. اگر پیکربندی S3 عمداً قطع شد، آپلود باید خطای روشن بدهد و نباید پیوست ساختگی یا فایل دائمی روی هاست ایجاد شود.

## خارج از این اسپرینت
- گردش بانک و صندوق هنوز شروع نشده است.
- migration یا حذف فایل‌های قدیمی هاست عمداً انجام نشده است.
