# RELEASE NOTES — v34.30.0 (ارجاع فاکتور پروندهٔ ارزی با نسخهٔ ریالی ثبت‌شده)

**تاریخ:** ۲۰۲۶-۰۹-۰۲ · **کمیت:** tester584 (۳۱ سنجه: ۲۵ رفتاری روی برش واقعی تابع‌های salesfiles.js در vm + ۶ قرارداد ایستا) · **گیت:** 200 PASS / 0 FAIL · **نکتهٔ بامپ:** ۸۹ پین نسخهٔ escapeشده در تسترها + نشانگرهای پوسته/سرویس‌ورکر/CMS هم‌نسخ شدند (همان روال جاروی بامپ)

## گزارش کارفرما

«در زمانیکه از یک پروندهٔ فروش که پیشنهاد مالی برندهٔ آن ارزی است می‌خواهم برای فاکتور ارجاع بدهم، اگر چنانچه پیشنهاد ریالی ثبت شده باشد باید در زمان ارجاع نسخهٔ ریالی هم برای صدور فاکتور به بخش فاکتورها ارجاع شود تا حسابدار با نرخ تسعیر درست فاکتور را که ریالی است بزند.»

## ریشه — تشخیص نسخهٔ ریالی فقط دو راه می‌شناخت

`sfInvoiceRefCommit` (INV-RIAL-BASIS از v34.7.76) برای پیشنهاد برندهٔ ارزی فقط این‌ها را
به‌عنوان مبنای ریالی می‌پذیرفت:

1. شمارهٔ صریح `rialBasisNo` — که فقط مسیر «ساخت ریالی و ارجاع» آن را می‌دهد؛
2. لینک `rialOf` — که فقط ابزار تبدیل 💱 (US-FX2RIAL) می‌سازد.

پیشنهاد ریالی مستقلی که کاربر برای همان درخواست/پرونده **ثبت کرده** هیچ‌کدام را ندارد →
پیدا نمی‌شد → سامانه ارجاع را به دیالوگ «نرخ تسعیر را وارد کنید تا نسخهٔ ریالی بسازیم»
می‌فرستاد. نتیجه: نسخهٔ ریالی واقعیِ داده‌شده به کارفرما نادیده گرفته می‌شد، یک سند
ریالی تکراری با نرخ تازه ساخته می‌شد و حسابدار مبنای درست برای فاکتور ریالی نداشت.
اثبات ریشه در تستر (سنجهٔ ROOT).

## اصلاحات

### الف) هستهٔ تشخیص مبنای ریالی (FX-RIAL-REF)
- **`sfInvoiceRialResolve`** (هستهٔ خالص، بدون نوشت/بدون UI): ترتیب تشخیص
  ① شمارهٔ صریح (انتخاب کاربر/مسیر ساخت) → ② نسخهٔ همراه `rialOf` (ابزار تبدیل) →
  ③ **پیشنهادهای ریالی مستقل ثبت‌شده در خود پرونده** (CO/TC، بدون ارز یا IRR، بدون لینک
  ریال‌اُف، غیر باخت، از نمای `ptfSalesFileOffers`). تک‌نماینده → خودکار همان مبنا؛
  چندنامزد → فهرست نامزدها برای انتخاب کاربر.
- **`sfInvoiceRialRateOf`**: نرخ = نرخ صریح `fxConvert` ابزار تبدیل؛ در نبود آن **نرخ
  برگرفته از جمع دو سند** (جمع ریالی ÷ جمع ارزی) با برچسب «برگرفته» — تا حسابدار همیشه
  نرخ تسعیر درست را ببیند حتی وقتی نسخهٔ ریالی دستی ثبت شده است.

### ب) مسیر ارجاع (`sfInvoiceRefCommit` / `sfInvoiceRef`)
- `invRef` دو فیلد جدید می‌گیرد: `rialBasisKind` (explicit/companion/registered) و
  `rialRateDerived` — برای برچسب صادقانه در پنل‌ها و اعلان.
- حالت جدید `pick_rial`: وقتی چند پیشنهاد ریالی در پرونده ثبت شده باشد، دیالوگ
  «💱 انتخاب پیشنهاد ریالی برای صدور فاکتور» (شماره/تاریخ/جمع/وضعیت هر نامزد) باز
  می‌شود و ارجاع با انتخاب کاربر کمییت می‌شود (`sfInvoiceRefPickRial/Do`).
- تایم‌لاین پرونده، audit و اعلان حسابدار حالا نوع مبنا (پیشنهاد ریالی ثبت‌شدهٔ پرونده/
  نسخهٔ همراه/انتخاب کاربر) و نرخ تسعیر (با برچسب برگرفته) را ثبت می‌کنند.
- مسیرهای قبلی دست‌نخورده‌اند: برندهٔ ریالی = خود سند؛ ارزی بدون هیچ نسخهٔ ریالی =
  همان دیالوگ نرخ‌گیری و ساخت (`need_rial`)؛ ارجاع تکراری بسته است.

### ج) نمایش در هر دو پنل فاکتورها
- **پنل فاکتورها (کارتابل — رندر اصلی):** کادر سبز «💱 مبنای ریالی صدور فاکتور» با
  شمارهٔ سند، برچسب نوع، نرخ تسعیر (فال‌بک به `invRef.rialRate` برای سندهای بدون
  `fxConvert`)، جمع ریالی و لینک «⬇️ PDF نسخه ریالی».
- **پنل فاکتور رسمی (official-invoice-v2):** نرخ تسعیر در مودال ثبت فاکتور دیگر برای
  نسخه‌های بدون `fxConvert` صفر نشان نمی‌دهد (فال‌بک `rbRateShown` + برچسب برگرفته)؛
  همان نرخ در `rialBasisRate` فرمان `register_invoice` هم جریان می‌یابد.

## سازگاری و دیپلوی

- تغییرات فقط کلاینت است؛ سرور تغییر لازم ندارد (`SD_SERVICE_VERSION` فقط بامپ هم‌نسخگی).
- ارجاع‌های قدیمی که با نسخهٔ همراه ساخته شده‌اند دقیقاً مثل قبل کار می‌کنند (اولویت
  `rialOf` حفظ شده). ارجاع‌های قدیمیِ بدون مبنا هم با فال‌بک‌های نمایشی سالم می‌مانند.

## سنجه‌ها (گزیده)

- ارجاع پروندهٔ ارزی با یک پیشنهاد ریالی ثبت‌شده: بدون دیالوگ ساخت، مبنا = همان سند،
  نرخ برگرفته = ۱٬۱۲۰٬۰۰۰ ریال (جمع ریالی ÷ جمع ارزی)، تایم‌لاین/اعلان با برچسب نوع.
- دو نامزد ریالی → `pick_rial` با جمع محاسبه‌شده؛ ارجاع با انتخاب کاربر = صریح.
- فیلترها: باخت/همراهِ پیشنهاد دیگر/فنی/ارزی نامزد نمی‌شوند.
- اولویت: صریح > همراه > ثبت‌شده؛ نرخ صریح ابزار تبدیل > برگرفته.
- بدون نسخهٔ ریالی → مسیر قبلی ساخت؛ برندهٔ ریالی → خود سند؛ ارجاع تکراری → بسته.

**تستر بعدی: 585** · نسخهٔ رسمی: `VERSION.json` = v34.30.0
