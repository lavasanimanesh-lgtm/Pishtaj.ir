# 🏦 ریلیز v20.8 — US-410 «دستیار هوشمند ورود چک»

## ✅ اجرا شد

### ۱) دکمه دستیار چک
در باکس چک‌های صادره، دکمه جدید اضافه شد:

```text
🤖 دستیار چک
```

### ۲) ورود از عکس/PDF یا متن
کاربر می‌تواند:
- عکس یا PDF چک را بارگذاری کند.
- یا متن مشخصات چک را وارد کند.

### ۳) استخراج AI
اکشن جدید سمت سرور اضافه شد:

```php
llm.php?action=cheque
```

فیلدهای استخراجی:
- شماره چک / صیاد
- مبلغ
- تاریخ سررسید شمسی
- در وجه
- بانک / شعبه
- بابت / یادداشت

### ۴) پیش‌نمایش قابل اصلاح
قبل از ثبت، فیلدها در UI نمایش داده می‌شوند و کاربر می‌تواند اصلاح کند.

### ۵) ثبت نهایی
با تایید کاربر، چک در `ptf_crm_cheques` ذخیره می‌شود:

```js
src: 'ai-cheque'
dueISO
dueFa
```

تاریخ شمسی به ISO تبدیل می‌شود تا یادآور ۷ روز قبل از سررسید درست کار کند.

## معماری
- کلید داده جدید ساخته نشد.
- از همان ماژول چک و همان کلید `ptf_crm_cheques` استفاده شد.
- ثبت نهایی فقط پس از تایید کاربر انجام می‌شود.
- fallback دستی قبلی کاملاً حفظ شد.

## فایل‌های تغییر یافته
- `crm/cheques.js`
- `api/llm.php`
- `crm/index.html`
- `crm/sw.js`
- `_tools/uat/tester124-v208.js`
- اصلاح تستر قبلی:
  - `_tools/uat/tester123-v207.js`
- `RELEASE-NOTES-v20.8.md`
- `PTF-MASTER-HANDOVER.md`

## 🔬 کیفیت
- تستر جدید: `tester124-v208.js` با ۱۰ چک.
- رگرسیون کامل: **۱۱۱ تستر / صفر خطا**.
- Audit: PASS با هشدار قدیمی تصاویر سنگین.

## صف بعدی پیشنهادی
- US-411 فاز ۱ — ثبت‌کننده درخواست + مالک مشتری
- سپس US-411 فاز ۲ و ۳ یا US-402/403 طبق اولویت کارفرما
