# یادداشت انتشار CRM v34.8.23

تاریخ: ۱۴۰۵/۰۶/۰۵ (۲۰۲۶-۰۸-۲۷)

## W1-iterate — تکمیل کامل مهاجرت فرمانی سه موجودیت
**صفر** نقطهٔ `setData` مستقیم برای مشتریان/تامین‌کنندگان/کالاها باقی ماند —
همه از روتر فرمانی (`ptfEntitySaveCollection`) یا fallback محافظت‌شده عبور می‌کنند.
۳۸ نقطهٔ فرعی در ۱۲ فایل اضافه بر ۹ مسیر اصلی W1، شامل:
- **حذف‌های خطرناک حالا فرمان tombstone بازیافت‌پذیرند:** حذف کالا (delProd)،
  حذف دسته‌جمعی، ادغام مشتری (custmerge)، ادغام کاتالوگ (data-quality)
- ثبت‌های سیستمی: کالاهای خودکار از استعلام/پیشنهاد/ابلود، قیمت مرجع rfqsmart،
  ثبت سریع offerlock، یادگیری تخصص supspec، اصلاح origin cheques،
  مهاجرت‌های بوت/phonefmt، کارت‌وویست AI، ایمپورت‌ها

## T3-3 — صف آفلاین (outbox) روی IndexedDB
صف `ptf_b_queue` — write-ahead record حیاتی — از localStorage (که ممکن است ۱۰۰٪
پر باشد) به IndexedDB منتقل شد: حافظهٔ نشست (همگام) + IDB (پایدار)، seed یک‌بارهٔ
LS، آب‌رسانی بوت از IDB، **سقف ۵۰۰ رکورد/۷۲ ساعت** (تنظیم تأییدشده S7).
پایان ریشهٔ حادثهٔ «در دقیقهٔ پر بودن localStorage، ثبت کاربر شکست خورد».

## کنترل
tester526: ۲۰/۰ (صفر بایپس + رفتار صف: seed/انتقال/سقف/preload).
tester506 به‌روزرسانی رفتاری صف IDB. گیت کامل **۱۴۲/۰**، arch-guard PASS.
