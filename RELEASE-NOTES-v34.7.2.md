# Release Notes v34.7.2 — چیدمان کارت تامین و اسناد زنده تنخواه

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## کارت‌های درخواست تامین

- ریشه جابه‌جایی دکمه‌ها، `flex-wrap` مشترک میان متن با عرض ذاتی و ستون عملیات بود؛ شرح/منبع طولانی عرض سطر را اشغال و کل عملیات را به پایین هل می‌داد.
- سربرگ هر کارت به Grid مستقل `minmax(0,1fr) + ستون عملیات` تبدیل شد.
- شرح طولانی حداکثر دو خط، دارای `overflow-wrap:anywhere` و عنوان کامل در tooltip است.
- عملیات در ستون مستقل ۴۳۰ تا ۵۲۰ پیکسل، از بالای کارت و با wrap داخلی کنترل‌شده قرار می‌گیرند.
- در عرض متوسط ستون عملیات کوچک‌تر و در موبایل عمداً به شبکه دو ستونه زیر متن تبدیل می‌شود؛ بنابراین شکست تصادفی layout رخ نمی‌دهد.

## سند هزینه تنخواه لینک‌شده به پرونده

- ریشه مشکل، snapshot شدن `files` داخل `deal.costEvents` در زمان لینک بود؛ افزودن بعدی سند فقط `ptf_crm_petty` را تغییر می‌داد.
- افزودن، حذف و حذف فوری فایل تازه‌آپلودشده اکنون projection پرونده را نیز به‌روز می‌کند.
- نمایش پرونده دیگر به snapshot متکی نیست و اسناد را هر بار از رکورد زنده تنخواه بر اساس `pettyCd` می‌خواند.
- برای هزینه لینک‌شده دکمه «اسناد (N)» داخل پرونده اضافه شد و همان مدیر اسناد تنخواه را باز می‌کند.
- رکوردهای قدیمی دارای `dealRef` که `costEvent` آن‌ها جا افتاده است به‌صورت projection زنده در پرونده نمایش داده می‌شوند.
- مبلغ، شرح، تاریخ و ثبت‌کننده هزینه لینک‌شده نیز از رکورد زنده تنخواه نمایش داده می‌شوند.

## اسناد قدیمی در گزارش تلفیقی تنخواه

- نرمال‌ساز واحد `ptfPettyRecordFiles` برای ساختارهای زیر اضافه شد:
  - `files[]`, `file`, `docs`, `documents`, `attachments`, `receipt`, `receiptFiles`, `images`, `bankFile`, `proofs`
  - فایل رشته‌ای یا JSON-string
  - `key`, `objectKey`, `storageKey`, `fileKey`, `docKey`, `receiptKey`, `path`
  - `url`, `src`, `dataUrl`, `thumbUrl`, Blob URL و Base64
  - نام‌های قدیمی `fileName`, `filename`, `originalName` و MIMEهای `contentType/mimeType`
- جمع‌آوری PDF تلفیقی، ZIP فیش‌ها، اسناد دوره و UI مدیریت اسناد همگی همین projection را مصرف می‌کنند.
- URL/Blob قدیمی پیش از چاپ به Data URL تبدیل می‌شود تا در پنجره گزارش از بین نرود.
- تصویر و PDF داخل گزارش رندر می‌شوند؛ Office/CSV و فرمت‌های غیرقابل‌رندر به‌جای حذف شدن، کارت نام‌دار و لینک بازکردن دارند.
- برای فایل‌های legacy بدون `petId` شناسه غیرتکراری «سند N» ساخته می‌شود و شناسه فایل جدید از بیشترین شناسه موجود ادامه پیدا می‌کند.

## کنترل کیفیت

- `tester407-v34.7.2-rfqs-petty-doc-projection.js`: 14 PASS / 0 FAIL
- CI متمرکز ریلیز: 26 PASS / 0 FAIL
- رگرسیون کامل: 390 تستر، 333 فایل PASS، 57 فایل بدهی تاریخی FAIL و بدون شکست جدید نسبت به v34.7.1.
- تست‌های متمرکز گزارش تلفیقی، بازه تنخواه، PDF/HEIC raster، تبدیل فایل و projection هزینه پرونده اجرا شدند.
- Auditor و هم‌ترازی VERSION/PWA: PASS
- CI متمرکز نسخه شامل syntax `petty.js`, `salesfiles.js`, `rfqsmart.js` و `finance-write-guard.js` است.
