# 🚀 ریلیزنوت v19.8 — اسپرینت ۱ از ۵ پیاپی: US-444 «پاکسازی زنجیره‌های یتیم»

**مبنا:** ابلاغ کارفرما «بله کل زنجیره باید پاک شود که چیزی یتیم نماند» + مجوز ۵ اسپرینت بدون توقف.

## ✅ US-444 — پاکسازی یک‌باره یتیم‌های موجود
- **دکمه «🧹 یتیم‌ها»** در پنل درخواست‌ها (فقط مدیران ارشد): اسکن همه رکوردهایی که به درخواست حذف‌شده اشاره می‌کنند (یادگار قبل از BUG-031).
- مودال بازبینی: هر زنجیره با شمارش دقیق (پیشنهاد/فاکتور/استعلام/جدول خرید/بستانکاری/پرونده/اقلام).
- «🧹 پاکسازی همه» با تایید برگشت‌ناپذیر ⟵ کل زنجیره‌های یتیم پاک؛ audit با فهرست کلیدها.
- **استثنای هم‌راستا با BUG-031:** زنجیره یتیمی که بایگانی مختومه دارد پاک نمی‌شود — فقط در بخش «🔒 محفوظ» گزارش می‌شود (سند نهایی شرکت).
- سد ارشد برنامه‌ای در هسته (نه فقط UI).

## 🧪 تست
tester114: ۱۹ چک — سناریوی سه‌زنجیره‌ای (سالم/یتیم/یتیم-محفوظ): پاکسازی دقیق ۷ رکورد، مصونیت داده سالم و زنجیره بایگانی‌دار، رد غیرارشد، idempotent (اجرای دوم صفر).

**فایل‌ها:** bridge.js (ptfOrphanScan/Purge/Review + دکمه) | index/sw v19.8 | tester114.
