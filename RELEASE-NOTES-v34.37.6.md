# یادداشت انتشار — v34.37.6

**تاریخ:** ۲۰۲۶-۰۹-۰۶ (۱۴۰۵/۰۶/۱۵) · **پایه:** v34.37.5
**گیت CI:** ۲۱۸ PASS / ۰ FAIL (شامل tester603 = ۲۴ سنجش رفتاری با سه آزمون جهش)

گزارش کارفرما:

> «یک بررسی روی ظاهر قسمت فاکتورها داشته باش. اعداد و نوشته‌ها و سرستون‌ها در هم
> می‌روند و خوانایی ندارند. مثلاً شماره سند مبنا با اسم مشتری همپوشانی شده؛
> سرستون مبلغ و نام مشتری و اقدام در ست در بالای مقادیر نیستند؛ مبلغ فاکتور
> با مطالبات باز درهم فرو رفته‌اند.»

---

## ① ریشۀ مشکل — سه نقص در همان «قرارداد ستون مشترک» (v34.37.3)

پنلِ زندهٔ فاکتورهای رسمی (`crm/official-invoice-v2.js`) سربرگ و ردیف‌ها را از یک
قالبِ ستونیِ مشترک (`INV_COLS`) می‌سازد — مکانیزم درست بود، ولی سه نقصِ ظاهری داشت:

**① ستون‌های ثابت `flex:0 0` — کوتاه‌نشدنی.**
مجموع عرضِ هفت ستون ≈ ۸۵۰px بود. در پنلِ باریک‌تر، آیتم‌های `flex-shrink:0`
جمع نمی‌شدند و از کارت بیرون می‌زدند؛ کارت `overflow:hidden` داشت، پس سمتِ
آخرِ ردیف (ستون اقدام) بریده می‌شد.

**② محتوای بلندتر از ستون هیچ clip نداشت — و RTL سرریز را «روی» همسایه می‌نویسد.**
در راستای RTL، متنِ سرریز از لبۀ چپِ جعبه ادامه می‌یابد؛ یعنی دقیقاً روی ستون
بعدی. سه جفتِ گزارش‌شدهٔ کارفرما همین بود:
- «شماره سند مبنا روی اسم مشتری» → شمارۀ `CO-…/…` + بَجِ 💱 در جعبۀ ۱۱۲px؛
- «مبلغ فاکتور با مطالبات باز درهم» → مبلغ ریالی ۱۰–۱۲ رقمی («… ریال») در جعبۀ ۱۱۶px؛
- «سرستون اقدام بالای مقادیر نیست» → دکمۀ ≈۱۷۰px «+ ثبت فاکتور رسمی صادرشده» در جعبۀ ۱۳۸px.
هیچ‌کدام `overflow:hidden` نداشتند؛ فقط `white-space:nowrap` — یعنی سرریزِ آزاد.

**③ هندسۀ سربرگ با کارتِ ردیف یکی نبود.**
سربرگ یک `div` بدون لبه بود و ردیف‌ها داخل کارتی با `border:1px` — کل ستون‌ها
۱–۲px نسبت به سرستون‌ها جابه‌جا (و این خودش «سرستون بالای مقدار نیست» را تشدید می‌کرد).

## ② اصلاح (فقط لایۀ نمایش — سرور/داده/مجوز دست‌نخورده)

در `crm/official-invoice-v2.js`:
1. **clip در خودِ قالب، برای همه:** هر شش ستونِ متنی حالا
   `overflow:hidden; text-overflow:ellipsis; white-space:nowrap` در همان `INV_COLS`
   دارند. بدترین حالت، سه‌نقطه است — هرگز همپوشانی. ستون سند و مبالغ `title`
   با مقدار کامل گذاشتند که با هاور، عدد/شمارۀ کامل خوانده شود.
2. **شrink با کف:** `flex:0 1 <basis>` + `min-width` برای هر ستون — پنل باریک،
   سربرگ و ردیف را **به یک اندازه** جمع می‌کند، پس تراز هیچ‌وقت نمی‌شکند.
3. **عرض‌های واقعی:** سند ۱۳۲، وضعیت ۱۲۸، مبلغ/مطالبه ۱۴۶ (با
   `font-variant-numeric:tabular-nums` تا رقم‌ها زیر هم تراز شوند)، اقدام ۱۷۶
   هم‌اندازۀ دکمه. برچسبِ ستون پولی در سربرگ و مقدار، هر دو `text-align:left`.
4. **برچسب‌های صریح:** «شماره سند مبنا»، «مبلغ فاکتور (ریال)»، «مطالبه باز (ریال)»
   (واحد از سرستون خوانده می‌شود، نه حدس).
5. **سربرگ هم‌هندسهٔ ردیف:** جعبۀ سربرگ همان `border:1px solid var(--brd)` و
   `border-radius:12px` کارتِ ردیف را گرفت (به‌علاوهٔ پس‌زمینۀ ملایم) — تراز، پیکسل‌به‌پیکسل.

`_tools/uat/tester600`: سنجشِ «سربرگ == INV_COLS» از لیستِ دستیِ استاتیک به
**بازتابِ زندهٔ خودِ قالب** تبدیل شد — دیگر هیچ ویرایشِ عرض، تستر را نمی‌شکند،
ولی شکستنِ مکانیزمِ مشترک، می‌شکند.

## ③ بازگشت‌پذیری / استقرار

فایل‌های تغییرکرده: `crm/official-invoice-v2.js` (قالب ستون + سربرگ + سه سلول ردیف)،
`_tools/uat/tester600` (EXPECT بازتاب‌محور، پین جهش)، `_tools/uat/tester599`
(لنگرِ عنوانِ title)، `VERSION.json` + بامپ ۷ نقطه و پین‌های تسترها
(148 فایل / 747 مورد)، `RELEASE-NOTES-v34.37.6.md` (این فایل).
دیپلوی معمولی FTP؛ برای کاربر فقط یک Reload کافی است (sw کشِ نسخه‌دار را می‌شکند).
اگر همان لحظه همپوشانی دیدید: Hard Reload (Ctrl+Shift+R) — کشِ قدیمیِ
`official-invoice-v2.js?v=…` متهم همیشگی است.

## ④ اثبات

`_tools/uat/tester603-v34.37.6-invoice-panel-clip.js` — ۲۴ سنجه:
- قرارداد منبع: clip در هر شش ستون، ممنوعیت `flex:0 0` برای ستون متنی،
  `basis ≥ min-width`، تک‌مبدأ بودن قرارداد (nowrap یدک حذف)، حذف عرض‌های
  ۱۱۲/۱۱۶/۱۲۶/۱۳۸، tabular-nums + چپ‌ترازی در قالب پولی، عرض اقدام ≥ ۱۷۰px؛
- رفتار روی رندرِ واقعیِ «بدترین حالت» (نام مشتری ۸۰+ کاراکتر، مبلغ ۱۱ رقمی،
  شمارۀ سندِ بلند): همهٔ سلول‌های سربرگ و ردیف clip دارند، هندسۀ سربرگ == کارتِ
  ردیف، سبکِ سلول‌ها پیشوندِ همان قالب است، مقدار کامل در DOM می‌ماند (clip فقط
  بصری است) و برچسب، پیش از ردیف در DOM می‌آید؛
- سه آزمون جهش: حذف clip ⇒ ۳.۱ قرمز · حذف لبهٔ سربرگ ⇒ ۳.۲ قرمز · بازگشت
  `flex:0 0` ⇒ ۱.۲ قرمز.

**گیت کامل: ۲۱۸ PASS / ۰ FAIL · ARCH GUARD: PASS**
