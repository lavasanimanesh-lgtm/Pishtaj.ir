# ARENA · RCA — «پروندهٔ بایگانی‌شده بعد از رفرش در پرونده‌های فروش هم بود» + قابلیت به جریان انداختن

**تاریخ:** ۲۰۲۶-۰۹-۰۹ (۱۴۰۵/۰۶/۱۹) · **انتشار:** CRM **v34.38.14** · **تستر:** `tester621-v34.38.14-archive-uniqueness-restore.js`
**دامنه:** `crm/salesfiles.js` · `crm/projects.js` · `crm/sales-domain-v2.js` (روتر) · `api/sales-domain.php` / `api/crm.php` (فقط خواندن)

---

## ۱) گزارش کارفرما

> پرونده‌ای را از پرونده‌های فروش بایگانی کردم → به بایگانی رفت → بعد از یک رفرش
> **در پرونده‌های فروش هم بود**. یک پرونده باید یکتاست: هم‌زمان هم در بایگانی هم در
> جریان نباشد. و تا پیش از «حذف قطعی» امکان **به جریان انداختن** پروندهٔ بایگانی‌شده
> (بازگشت به فروش) لازم است.

## ۲) ریشهٔ باگ (بازتولیدشده روی کد واقعی — شبیه‌سازی vm با روتر واقعی + سرور جعلی)

زنجیرهٔ دقیق تولد باگ وقتی رخ می‌دهد که پرونده‌ای که بایگانی می‌شود **«آخرین» پروندهٔ باز** باشد:

1. `sfArchive` رکورد `ARC-*` را در `ptf_crm_projects` می‌سازد و سپس پرونده را با
   `sfSave(sfAll().filter(...))` (دیفِ روتر مجموعه‌ای) از `ptf_crm_deals` حذف می‌کند.
2. دیفِ روتر (`ptfEntitySaveCollection`) یک حذف می‌بیند ولی «ته‌شدن کالکشن» را — مطابق سپر
   حذفِ انبوهِ v34.37.0 — امضای «خواندن کهنه/حادثه» تشخیص می‌دهد: **تنزل به legacy
   (setData محلی) + توست «حذف انبوه مسدود شد»**.
3. فرمان `entity_delete` **هرگز به سرور نمی‌رسد**؛ رکورد روی سرور زنده می‌ماند.
4. در pull/merge بعدی (canonical‑union بر `cd`) نسخهٔ سرور رکورد را به فهرست فروش برمی‌گرداند.
5. رکورد بایگانی (که به‌خاطر نبودِ `cd`، upsert آن نیز legacy بود) از push کل‌آرایه می‌رسد؛
   نتیجه‌ٔ نهایی: **یک پرونده هم‌زمان در هر دو محل** — نقض ماهیت یکتا.

> نکته: با بیش از یک پروندهٔ باز، `dels=1` و کالکشن ته نمی‌شد و حذف فرمانی سالم می‌گذشت — به همین دلیل باگ فقط در سناریوی «آخرین پرونده» قطعی بود.

## ۳) اصلاحات (v34.38.14)

| # | تغییر | اثر |
|---|---|---|
| ۱ | `sfRemoveDeal(r)` با `prevArr` صریح + `allowBulkDelete` برای همان یک حذف عمدی | `entity_delete` همیشه به سرور می‌رسد — حتی برای آخرین پرونده |
| ۲ | رکورد بایگانی با **`cd` پایدار** (`ARC-<cd>`) | upsert اتمیک؛ تلاش‌مجدد/دوبار بایگانی = همان رکورد (بدون رونوشت) |
| ۳ | **جاروی یکتایی** `ptfSalesfileUniquenessSweep` در رندر فروش (throttle) + بوت | «یک پرونده = یک محل»؛ دادهٔ آلودهٔ قبلی خودبه‌خود التیام می‌یابد |
| ۴ | `originTimeline`/`origSt` همراه آرشیو | تایم‌لاین با بایگانی از دست نمی‌رود؛ بازگشت با حافظهٔ کامل |
| ۵ | نمایش `restoredFrom` در فهرست فروش | مختومهٔ بدون فاکتورِ بازگردانده‌شده هم دیده می‌شود |

## ۴) به جریان انداختن (بازگشت از بایگانی)

- دکمهٔ **↩️ به جریان انداختن پرونده** در نمای پروندهٔ بایگانی‌شدهٔ ریشه‌فروش
  (`ptfArchiveCaseCanRestore`)، گارد نقش مثل تغییر اسناد بایگانی (admin/chairman/ceo/commercial).
- هستهٔ `ptfSalesfileRestoreFromArchive` سه حالت دارد و همه به «یک محل» ختم می‌شوند:
  - **restore**: حذف از بایگانی + ساخت رکورد فروش با **cd اصلی** (لینک تنخواه/چک/تسویه حفظ می‌شود) + بازگردانی اسناد/QC/ارسال/هزینه/زیان/سنگ‌قبرهای هزینه.
  - **prefer-live**: نسخهٔ فعال هم‌نام موجود است → نسخهٔ فعال می‌ماند، رونوشت بایگانی حذف می‌شود (یکتاسازی دادهٔ آلوده).
  - **prefer-archive** (برنامه‌ای): نسخه‌های فعال هم‌نام حذف می‌شوند و نسخهٔ بازگردانده‌شده می‌ماند.
- **خنثی‌سازی سنگ‌قبر حذف** (`kind → restored:<kind>` — قرینهٔ دقیق گام ② `entity_restore`
  سرور): بدون این گام، پروندهٔ بازگردانده‌شده در `sync_apply_tombstones` بعدی دوباره حذف می‌شد.
- قفل نهایی: جاروی یکتایی پس از هر بازگشت؛ idempotent (بازگشت دوم `not_found`).

## ۵) اثبات

- سناریوی دقیق باگ (بایگانیِ تنها پرونده → رفرش) **قبل از اصلاح** بازتولید شد:
  `upsert بایگانی به سرور رفت؟ 0 | حذف deal به سرور رفت؟ 0` و پرونده در هر دو محل.
- پس از اصلاح همان سناریو: `entity_delete` + `entity_upsert:ARC-*` هر دو به سرور می‌رسند،
  بعد از رفرش پرونده فقط در بایگانی است؛ هر دو مسیر بازگشت، یکتایی را حفظ می‌کنند.
- گیت CI کامل: **236 PASS / 0 FAIL** (`tester621` رفتاری + ساختاری + پین‌های نسخه).

## ۶) مرزهای شناخته‌شده

- برای رکوردهای بایگانیِ **قدیمیِ بدون cd** (پیش از این نسخه)، حذفِ رکورد بایگانی از مسیر
  legacy (push کل‌آرایه) انجام می‌شود؛ برای حذف تک‌رکوردی سپرهای سرور عبور می‌دهد و با
  جاروی یکتایی هم‌گرایی تضمین می‌شود. رکوردهای جدید cd‌دار کاملاً اتمیک‌اند.
- اگر سرور هنوز رکورد فروش را (به‌علت باگ قدیمی) داشته باشد و `expectCreate` به 409 بخورد،
  روتر `failDirty` می‌کند و push کل‌آرایهٔ بعدی هم‌گرایی را می‌بندد — رفتار fail-closed بدون
  بازنویسی بی‌صدا.
