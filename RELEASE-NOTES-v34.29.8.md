# RELEASE NOTES — v34.29.8 (پایداری حذف/ابطال در پروندهٔ فروش — هزینه‌ها و پکینگ‌لیست‌ها)

**تاریخ:** ۲۰۲۶-۰۹-۰۲ · **کمیت:** tester583 (۳۹ سنجه: ۲۰ رفتاری روی ptfSmartMerge واقعی در vm + ۱۹ ایستا/قراردادی) · **گیت معماری:** PASS

## گزارش کارفرما (یک پروندهٔ فروش، سه علامت)

1. «هزینه‌های مستقیم پروندهٔ فروش **چند باره محاسبه شده** و با پاک کردنشان **دوباره برمی‌گردند**».
2. «یک هزینه که از تنخواه به یک پروندهٔ دیگر لینک شده، **در این پرونده نیز درج شده** و با پاک کردن دوباره برگشت».
3. «پکینگ‌لیست‌ها را **ابطال می‌کنم ولی همچنان هستند** و delete کردنشان فایده ندارد».

## ریشهٔ مشترک — merge بین‌دستگاهی «اجتماع» بود، نه «آشتی»

`ptfSmartMerge` برای `ptf_crm_deals` از `ptfMergeByCodeCanonical` می‌رفت که فیلدهای آرایه‌ای را با
`ptfMergeArrayUnique` (امضای کامل-JSON) **اجتماع** می‌کرد:

- **بازگشت حذف:** اجتماع هیچ‌وقت عضو حذف‌شده را حذف نمی‌کند؛ هر نسخهٔ کهنه (سرور/دستگاه همکار)
  هزینهٔ حذف‌شده را در sync بعدی برمی‌گرداند. بدتر: `ptfPreferRecord` رکوردِ «کامل‌تر» را برنده
  می‌کرد و رکوردِ دارای costEvents بلندتر (با همان هزینهٔ حذف‌شده/تکراری) همیشه می‌برد.
- **تکرار:** هر تفاوت جزئی بین دو نسخه (پاک‌سازی سرور، به‌روزرسانی زمان/ویرایشگر) امضای متفاوت =
  یک نسخهٔ دوم از همان هزینه. اثبات در tester583 (ROOT).
- **هزینهٔ پروندهٔ دیگر:** رویداد یتیمِ تنخواه پس از انتقال لینک، با union برای همیشه می‌ماند.

پکینگ‌لیست هم در merge عمومی بود: برنده با ts رشته‌ای انتخاب می‌شد، ابطال **هیچ ts را عوض نمی‌کرد**
و ISO از تاریخ شمسیِ t همیشه lexicographically می‌باخت → نسخهٔ ابطال‌نشدهٔ دستگاه دیگر همیشه برنده.
ضمناً رکوردهای PL اصلاً **cd نداشتند** → مسیر فرمان/tombstone (رجیستری سرور id=cd) هرگز برایشان فعال نبود.

## اصلاحات

### الف) هزینه‌های پرونده (COST-EVENT-TOMB)
- **tombstone نقشه‌ای `_costTomb = {cd: iso}`** روی خود رکورد پرونده؛ همهٔ مسیرهای حذف آن را
  می‌نویسند: `ptfDealRemoveCost` (کشو پرونده)، `ptfProjectCostDel` (هزینهٔ مستقیم)،
  `ptfDealCostSync` (انتقال/حذف لینک OPEX/تنخواه)، `ptfPettyRemoveDealCostEvent` (حذف تنخواه).
- **کانونیکال‌سازی costEvents در merge** (`crm/sync.js`): اجتماع tombstoneها (سقف ۲۰۰ مدخل)،
  ددوب بر اساس cd (نسخهٔ ویرایش‌شده برنده)، حذف موارد tombstoneشده — رفPet-وبرگشت اثبات‌شده در تستر.
- **جاروب تعمیر یک‌باره** `ptfDealCostRepairSweep` (boot، یک‌بار در هر دستگاه، idempotent):
  ددوب هم‌کد، حذف رویدادهای یتیم تنخواه (منبع حذف/ابطال‌شده، لینک به پروندهٔ دیگر، لینک‌گسسته)
  و نوشتن tombstone برای هر حذف تا merge دیگر بازشان نگرداند. گارد در `ptf_app_flags` (از مسیر
  لایهٔ داده — A10).
- **پاک‌کردن لینک تنخواه در حذف هزینهٔ مستقیم** (`ptfProjectCostDel`): تا projection زندهٔ نمایش
  (petty.dealRef) رویداد را دوباره نسازد؛ هزینهٔ اصلی در تنخواه سالم می‌ماند.
- **ددوب نمایش/جمع**: projection کشو، نوار مالی (costSum) و `dealTotalCosts` (helpers و core)
  هر هزینه را بر cd یک‌بار می‌شمارند (fiscal از قبل cd-ددوب بود).

### ب) پکینگ‌لیست‌ها (PL-VOID-WINS + PL-DELETE)
- `ptf_crm_packinglists` به مسیر **canonical merge** رفت؛ قاعدهٔ **void-wins** (هم‌سنخ
  invoice/cheque): نسخهٔ ابطال‌شده همیشه برنده — ابطال در sync می‌ماند.
- **PL جدید cd می‌گیرد** (cd = no)؛ رکوردهای قدیمی در ابطال و در merge خودکار backfill می‌شوند →
  مسیر فرمان entity برای PL فعال شد.
- `plVoid`: `voidedISO` + `ts` ISO نوشته می‌شود.
- **`plDelete` (دکمهٔ 🗑 در فهرست PLها):** حذف قطعی بین‌دستگاهی با tombstone `archive_purge` +
  `identities` فقط برای همین کلید (**بدون aliases** — alias در فیلتر cross-collection به‌صورت
  substring است و شمارهٔ PL در timeline پروژه آمده؛ اثبات عدم آسیب در tester583)؛ برای رکوردهای
  cd-دار فرمان `entity_delete` سرور هم می‌رود (idempotent). ابطال = برگرداندن تعداد؛ حذف = پاک‌کردن سند.

## سازگاری و دیپلوی

- بدون تغییر سرور لازم (تغییرات فقط کلاینت؛ `api/sales-domain.php` فقط بامپ نسخه).
- دادهٔ قدیمی آسیب‌دیده با جاروب تعمیر در اولین باز شدن CRM هر دستگاه پاک می‌شود؛ tombstoneها
  از طریق خود رکورد پرونده sync می‌شوند → دستگاه‌های دیگر هم همگرا می‌شوند.
- **نکتهٔ دیپلوی:** این نسخه روی اجتماع خطِ v34.29.7 (شاخهٔ جلسه) و main (تا PR #23) ساخته شده؛
  PR آن باید روی main فعلی بیاید — درخت کاری اجتماعی از هر دو خط است و هیچ تغییری از main حذف نشده.

## سنجه‌ها (گزیده)

- حذف هزینه + sync رفت‌وبرگشت: C1 دیگر برنمی‌گردد؛ ددوب C2؛ نسخهٔ updatedT برنده.
- ابطال PL با وجود نسخهٔ شمسی‌تاریخِ ریموت برنده می‌شود؛ cd قدیمی‌ها backfill.
- حذف PL: رکورد tombstoneشده از merge زنده نمی‌شود؛ پروژهٔ دارندهٔ شمارهٔ PL در timeline سالم می‌ماند.
- جاروب: تکرار حذف، یتیم حذف + tombstone، اجرای دوم idempotent، تکرارِ هم‌کد tombstone نمی‌گیرد.
- رگرسیون: offers همچنان atomic-snapshot؛ A10 (localStorage) رعایت شد.

**تستر بعدی: 584** · نسخهٔ رسمی: `VERSION.json` = v34.29.8
