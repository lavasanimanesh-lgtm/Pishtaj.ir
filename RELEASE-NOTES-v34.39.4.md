# یادداشت انتشار v34.39.4 — HOME-ACCORD-NIGHTMEGA (آکاردئونِ «درباره ما» در شیت موبایل + خواناییِ کاملِ شبِ زیرمنوهای محصولات)

**تاریخ:** 2026-09-17
**بازوی تغییر:** Arena Agent
**کانال:** سایت عمومی — صفحهٔ اول (لایهٔ نمایشی/رفتاری v34.39.4؛ محتوا/سئو/فرم‌ها بدون تغییر)
**قرارداد:** tester666 (۲۳ سنجه) + بازتنظیم پین‌های 663/664/665 با ابزار رسمی `bump-version-pins.js`

## گزارش کارفرما
> «در نمای موبایل اخبار و وبلاگ زیر دربارهٔ‌ما هست؛ باید باز شوند. زیرمنوهای محصولات در نمای شب خوانایی ندارند.»

## ۱) آکاردئونِ «درباره ما» در شیتِ موبایل
- پیش از این، ردیفِ چیپ‌های اخبار/وبلاگ همیشه‌نمایش بود و هر تپ روی «درباره ما» بلافاصله شیت را می‌بست و صفحه عوض می‌شد (هندلرِ سراسری `main.js` روی همهٔ `.main-nav a`).
- **الان یک آکاردئونِ واقعی است:** ردیف «درباره ما ▾» با شِورونِ CSS که هنگامِ باز شدن ۱۸۰° می‌چرخد؛ تپِ اول = بازشدنِ نرمِ زیرآیتم‌ها (اخبار/وبلاگ) درست زیرِ «درباره ما» با استگرِ پله‌ای؛ تپِ دوم (وقتی باز است) = پیمایش به صفحهٔ دربارهٔ‌ما.
- **تکنیک:** هندلِرِ `click` در **فاز capture** روی خودِ `#mainNav` — اگر بسته باشد `preventDefault()` + `stopImmediatePropagation()` تا نه لینک برود نه شیت بسته شود؛ اگر باز باشد هیچ مداخله‌ای نمی‌کند و هندلرهای عادی کار می‌کنند. فقط زیرِ `max-width:790px` فعال است؛ دسکتاپ (هاو/فوکوسِ مگا) دست‌نخورده.
- تک‌باز: هم‌زمان فقط یک دراپ باز می‌ماند؛ بستنِ شیت (بک‌دراپ/✕/ESC/لینک) همهٔ دراپ‌ها را هم جمع می‌کند (در همان `sync` موجود).
- `prefers-reduced-motion`: ترنزیشنِ ارتفاع و انیمیشنِ آیتم‌ها خاموش.

## ۲) خواناییِ شبِ زیرمنوهای محصولات (مگا)
استایل‌های مگا توسط `ptf-discover.js` به‌صورت `<style id="ptf-mega-css">` **در runtime و بعد از home.css** تزریق می‌شوند؛ رنگ‌های تیره‌ی متنشان `!important` داشت و در شب روی پس‌زمینه‌های تیرهٔ ما ناخوانا می‌شد (متن سرمه‌ایِ تیره روی پنلِ تیره).
- فیکس با **بالا بردنِ ارجحیت، نه تغییر تزریق‌کننده:** کلِ پالتِ شبِ مگا با پیشوندِ `html.ptf-dark #mainNav` + `!important` در home.css — ارجحیتِ id حتی با تزریقِ بعدی هم برنده است؛ `ptf-discover.js` اصلاً دست‌نخورده مانده (سنجهٔ ۲.۶).
- پوشش: پنل/اینر `#0f1a2e`، ستون‌ها `#111d35` (باز: `#152342` با لبهٔ کهربایی)، هِدِ ستون و شمارنده‌ها (`#e8eef8`/کهربایی)، لینک‌های اقلام `#c9d6ea` با هاورِ کهربایی، تریگرِ شِورون تیره‌فریم، دکمهٔ «همهٔ محصولات» با فلرِ تیره‌تر + سایهٔ متن. نمای روز هیچ تغییری نکرد.

## ۳) بودجه‌های سرعت
افزودنِ دو بخشِ جدید، سقف‌ها را می‌شکست؛ با **فشرده‌سازیِ کامنت‌های تزئینی** (بدون حذف حتی یک قانون؛ مارکرِ هر پنج لایه حفظ و سنجه‌سازی شد) برگشت: `home.css = ۳۹٬۹۷۸` نویسه (سقف ۴۰Kِ گارد tester662) · `ptf-motion.js = ۱۱٬۴۶۵` (سقف ۱۲K). صفر کتابخانه، صفر درخواست شبکهٔ جدید، صفر localStorage (کوکی تنها راهِ ماندگاری).

## فایل‌های تغییر‌یافته
`index.html` (فقط cache-bust → v=34.39.4) · `assets/css/home.css` (لایهٔ v34.39.4 + جمع‌شدنِ دراپ + اوررایدهای شبِ مگا + فشرده‌سازیِ کامنت) · `assets/js/ptf-motion.js` (هندلِرِ capture + هم‌خوانیِ sync) · `VERSION.json` و ۸ نقطهٔ رسمی نسخه · `RELEASE-NOTES-v34.39.4.md` · tester666 و بازتنظیم پین ۳ تستر.

## وضعیت دروازه
tester666: ۲۳/۲۳ ✓ · tester665: ۲۵/۲۵ ✓ · tester664: ۲۹/۲۹ ✓ · tester663: ۳۰/۳۰ ✓ · tester662: ۳۶/۳۶ ✓ · گیت کامل CI: **۲۷۸ PASS / ۱ FAIL** (شکستِ محیطیِ شناخته‌شدهٔ tester656 — نبودِ php-cli؛ روی بیس هم قرمز است) · arch-guard: PASS.
