# 📦 ریلیزنوت v13.1 — سهمیه AI نقش‌محور + رفع اعلان موبایل + امضای نیابتی رییس هیات مدیره

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v13.0

---

## 🤖 US-320 — تخصیص توکن AI: پاسخ سوال شما + اصلاح

**وضعیت قبلی (یافته ممیزی):** عدد «۳۰» هیچ منطقی نداشت — یک شمارنده صرفاً نمایشی بود که ایجنت‌های قبلی هاردکد کرده بودند؛ نه بر اساس نقش بود، نه per-user واقعی (بین کاربران یک مرورگر مشترک بود!) و **هیچ‌جا واقعاً اجرا نمی‌شد** — فقط روی پیل نوشته می‌شد. سقف واقعی فقط ۶۰ درخواست/ساعت per IP سمت سرور بود.

**اصلاح طبق دستور شما:**

| نقش | سهمیه روزانه |
|---|---|
| ادمین، رییس هیات مدیره، مدیرعامل (۳ نقش اصلی) | **۴۰۰** |
| سایر نقش‌ها | **۵۰** |

- شمارنده حالا **per-user واقعی** است و **واقعاً اجرا می‌شود:** با پر شدن سقف، درخواست قبل از رسیدن به سرور متوقف و پیام شفاف داده می‌شود (تست اتصال سهمیه مصرف نمی‌کند)
- پیل سهمیه: «سهمیه امروز شما: ۳۹۶ / ۴۰۰» — با هشدار زرد بعد از ۸۰٪ مصرف
- عنوان پنل هم به «🤖 دستیار» یکدست شد (بقایای «AI Workbench»)

## 🐞 BUG-011 — اعلان موبایل کامل دیده نمی‌شد

**ریشه:** پنل صندوق پیام به گوشه نوار آیکون‌ها می‌چسبید (absolute) و در موبایل با عرض ۳۶۰px از فضای باقیمانده صفحه بیرون می‌زد → محتوای اعلان بریده می‌شد.
**رفع:** در موبایل پنل به‌صورت ثابت و تمام‌عرض زیر هدر باز می‌شود (۸px حاشیه از دو طرف) با ارتفاع محدود به صفحه و بالاتر از نوار پایین — دسکتاپ بدون تغییر.

## ✍️ US-321 — امضای نیابتی برای رییس هیات مدیره

در فرم پیشنهاد (TO/CO/TC)، وقتی **رییس هیات مدیره** (یا ادمین) وارد شده باشد، کنار گزینه مهر و امضا یک کشوی انتخاب ظاهر می‌شود:

- **امضای خودم** (پیش‌فرض)
- **به نیابت: عباس یوسفی**
- **به نیابت: شیوا کریمی**

امضا و مهر انتخاب‌شده روی هر ۵ قالب چاپ و پیش‌نمایش سند می‌نشیند و انتخاب روی سند ذخیره می‌شود (چاپ‌های بعدی همان امضا). برای سایر نقش‌ها این کشو اصلاً ظاهر نمی‌شود — رفتار قبلی‌شان دست‌نخورده.

## 🧪 کیفیت
تستر جدید `tester51` ۱۸/۱۸ ✅ | رگرسیون **۳۸ تستر** ✅ (tester8 با متن جدید چک‌باکس سینک شد) | audit صفر خطا ✅

## 📁 فایل‌های تغییریافته
`crm/ai-workbench.js` (سهمیه نقش‌محور + اجرا در llmPost + عنوان) · `crm/bridge.js` (CSS موبایل پنل صندوق) · `crm/offers.js` (ptfCanDelegateSig/ptfSignAsOptions/ofSignAs/ذخیره signAs) · `crm/offers-pro.js` (مصرف signAs در قالب‌ها) · `crm/index.html`/`sw.js` (v13.1) · `_tools/uat/tester51` (جدید)

## 📌 یادداشت
سهمیه سمت کلاینت اعمال می‌شود (کاربر فنی می‌تواند دور بزند)؛ سقف سروری per-user واقعی بخشی از US-275 (توکن سروری) است که در بک‌لاگ منتظر تایید است.
