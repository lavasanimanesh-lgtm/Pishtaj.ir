# تست استیجینگ v34.7.41 — رویژن هم‌زمان و پاسخ گم‌شده

> این سناریو پس از رسیدن نسخه فعال CRM به `v34.7.41` اجرا شود. نسخه فعلی استیجینگ تا زمان رفع مجوز Push همچنان v34.7.39 است.

## ۱) عدم رگرسیون عادی

1. یک پروندهٔ فعال با پیشنهاد برنده و بدون فاکتور باز کنید.
2. «رویژن پیشنهاد برنده» را باز و قیمت یک قلم را تغییر دهید.
3. ثبت کنید.
4. انتظار: همان شماره و `_id` باقی بماند، Rev دقیقاً یک واحد بالا رود و مبلغ پرونده/پیشنهاد یکسان شود.

## ۲) دو کاربر هم‌زمان

1. همان پرونده را روی دو مرورگر یا دو دستگاه A و B باز کنید.
2. روی هر دو، فرم رویژن را پیش از هر ذخیره باز نگه دارید.
3. روی A تغییر را ثبت کنید؛ باید موفق شود.
4. بدون refresh روی B تغییر دیگری را ثبت کنید.
5. انتظار: B با پیام «از زمان بازشدن فرم، پیشنهاد یا پرونده روی دستگاه دیگری تغییر کرده» متوقف شود؛ Rev و اقلام ثبت‌شدهٔ A دست‌نخورده بمانند.

## ۳) کلیک دوباره / پاسخ گم‌شده

1. فرم رویژن را باز و یک تغییر مشخص ثبت کنید.
2. هنگام انتظار، دکمه باید غیرفعال و متن آن «در انتظار تأیید سرور» باشد.
3. در شبیه‌سازی خطای ارتباط، همان پنجره را نبندید و دوباره ثبت کنید.
4. انتظار: همان operation ID replay شود؛ Rev در مجموع فقط یک واحد بالا رود و correction تکراری ساخته نشود.

## ۴) حفظ metadata و lineId

1. پیشنهادی انتخاب کنید که قلمش `lineId`، `sourceItemKey` و نرخ مرجع دارد.
2. فقط qty یا price همان قلم را تغییر دهید و یک قلم جدید اضافه کنید.
3. انتظار پس از ثبت:
   - metadata قلم قدیمی همچنان موجود باشد؛
   - lineId قلم قدیمی عوض نشود؛
   - قلم جدید یک lineId با پیشوند `LINE-` داشته باشد؛
   - lineIdها یکتا باشند.

## ۵) ارز

روی یک پیشنهاد ارزی فرم را باز کنید. مبلغ فعلی، جدید، دلتا و پیام تأیید باید کد همان ارز را نشان دهند، نه «ریال».

## ۶) گارد دادهٔ مبهم

اگر شماره پیشنهاد یا پروندهٔ فعال تکراری وجود داشته باشد، رویژن باید متوقف شود و هیچ فاکتور، پیشنهاد، پرونده یا correction تغییر نکند.
