# ارزیابی فرم‌های عمومی سایت و نمایش فایل‌های پیوست در CRM — ۲۰۲۶-۰۸-۲۱

**تاریخ:** ۱۴۰۵/۰۵/۳۰ — 2026-08-21 | **دامنه:** فرم‌های عمومی (استعلام هوشمند / استعلام سریع / ثبت‌نام تامین‌کننده) و نمایش پیوست‌ها در CRM

---

## ۰) خلاصهٔ اجرایی

| فرم | مقصد | فایل ذخیره می‌شود؟ | فایل در CRM قابل نمایش بود؟ |
|---|---|---|---|
| استعلام هوشمند `/rfq/` | `add_rfq_site` | ✅ در فضای ابری (Arvan/S3) | ✅ قابل نمایش بود (با یک پیش‌نیاز) |
| استعلام سریع صفحهٔ اصلی | `add_rfq_site` | ✅ همان مسیر | ✅ قابل نمایش بود (با یک پیش‌نیاز) |
| ثبت‌نام تامین‌کننده `/supplier/` | `add_supplier` | ✅ در فضای ابری | ❌ **هیچ‌جا قابل نمایش نبود — و پس از تایید هم حذف می‌شد** |

**نتیجه:** مشکل اصلی شما (ندیدن فایل) مربوط به **ثبت‌نام تامین‌کننده** است: فایل کاتالوگ
آپلود و در فضای ابری ذخیره می‌شد، اما CRM **هیچ رابطی برای دیدنش نداشت** و هنگام «تایید»
هم فایل و متن درخواست دور ریخته می‌شد. این دو مورد در v34.7.66 اصلاح شد (تب جداگانه +
جزئیات + نمایش فایل).

---

## ۱) زنجیرهٔ فنی (اندازه‌گیری‌شده از سورس)

### الف) هر سه فرم فایل را به «فضای ابری» می‌فرستند
- `api/crm.php` → `save_attachment()` → `ptf_storage_put_uploaded_file()` → آپلود مستقیم
  از temp فایل PHP به S3 (Arvan) با AWS Signature V4.
- کلید فایل: `site-rfq/YYYY-MM/<uniq>-<name>` و `site-ven/YYYY-MM/<uniq>-<name>`.
- **هیچ نسخهٔ محلی روی هاست نگه‌داشته نمی‌شود.**

### ب) پیش‌نیاز حیاتی (احتمالاً دلیل «ندیدن فایل»)
- `save_attachment` به `storage-config.php` روی سرور نیاز دارد (در مخزن فقط
  `storage-config.sample.php` هست).
- اگر این فایل نباشد/نادرست باشد:
  - **استعلام (RFQ):** کل ثبت رد می‌شود (HTTP 503 «attachment_cloud») → استعلام با فایل
    اصلاً ذخیره نمی‌شود.
  - **تامین‌کننده:** ثبت‌نام انجام می‌شود ولی پیوست ذخیره نمی‌شود (فقط warning).

> ⚠️ توصیه: روی هاست `storage-config.php` را بررسی/بازسازی کنید. اگر RFQهای دارای فایل
> در لیست شما اصلاً ظاهر نشده‌اند، احتمال زیاد همین است (کل درخواست رد شده).

---

## ۲) استعلام (هوشمند و سریع) — وضعیت فایل

| مرحله | نمایش فایل |
|---|---|
| در انتظار تایید (بنر «استعلام‌های سایت») | ✅ دکمهٔ «👁 جزئیات کامل» → بلوک «☁️ پیوست در فضای ابری» + دکمهٔ «مشاهده/دانلود» |
| پس از تایید (ورود به چرخه) | ✅ فایل به `files.oth` منتقل می‌شود و در بج «📎 N ضمیمه» ردیف درخواست دیده/باز می‌شود |

نتیجه: مسیر فایل استعلام سالم است؛ نمایش آن فقط وابسته به سالم‌بودن فضای ابری است.

---

## ۳) ثبت‌نام تامین‌کننده — باگ تأییدشده (قبل از v34.7.66)

1. ثبت‌نام در سایت با فایل کاتالوگ → ذخیره در `suppliers.json` با `attachment:{key,...}`.
2. در CRM، جدول «ثبت‌نام‌شده از سایت» فقط ستون‌های کد/شرکت/مسئول/تماس/حوزه/تاریخ/عملیات
   داشت — **بدون ستون پیوست، بدون جزئیات، بدون هیچ راهی برای دیدن فایل**.
3. هنگام «✅ تایید»، رکورد نهایی با این فیلدها ساخته می‌شد و **`attachment` و `message`
   حذف می‌شدند** → فایل برای همیشه از دید کاربر گم می‌شد.

---

## ۴) اصلاحات انجام‌شده (v34.7.66)

### ۱) تب جداگانهٔ «🌐 درخواست‌های سایت»
- در پنل تامین‌کنندگان، تب جدید کنار «همه / داخلی / خارجی» اضافه شد (با شمارندهٔ موارد در انتظار).
- این تب فقط فهرست ثبت‌نام‌های سایت را نشان می‌دهد (در انتظار / تاییدشده / ردشده) و
  فهرست تاییدشده را موقتاً پنهان می‌کند.

### ۲) خوانا شدن فایل‌های پیوست تامین‌کننده
- ستون «ضمیمه» با نشان 📎 (نام فایل) و دکمهٔ «👁 جزئیات» به هر ردیف اضافه شد.
- مودال «جزئیات ثبت‌نام تامین‌کننده» (`supSiteDetail`) اضافه شد: همهٔ فیلدها + شرح درخواست
  + بلوک «☁️ پیوست در فضای ابری» با دکمهٔ «مشاهده/دانلود».

### ۳) جلوگیری از گم‌شدن فایل پس از تایید
- `supApprove` حالا `attachment` (به‌صورت `files.oth`) و `message` را روی رکورد
  تاییدشده در `ptf_crm_suppliers` حفظ می‌کند.

---

## ۵) اقدامات لازم از شما (خارج از کد)

1. **بررسی `storage-config.php` روی هاست** — اگر نیست، از `storage-config.sample.php`
   بسازید (endpoint/bucket/access_key/secret_key/region). بدون آن، فایل‌ها بالا نمی‌روند
   و استعلام دارای فایل رد می‌شود.
2. پس از دیپلوی، یک **ثبت‌نام تامین‌کنندهٔ آزمایشی با فایل** بزنید و در CRM تب
   «درخواست‌های سایت» → «👁 جزئیات» → «مشاهده/دانلود» را امتحان کنید.
3. اگر باز هم فایل باز نشد، خطای `presign_get` را بررسی کنید (لاگ کنسول مرورگر: F12 → Network).

---

## ۶) محدودیت‌های این ارزیابی
- PHP در محیط بررسی نبود؛ صحت مسیر آپلود/دانلود با خواندن کد (storage-lib.php،
  attachment-read.php، storage.js `openStoredFile`) سنجیده شد.
- به سرور/باکت واقعی و دادهٔ تولید دسترسی نداشتم؛ «فایل هست/نیست» را قطعی نمی‌توانم
  بگویم — اما شکاف رابط کاربری (نبودِ هیچ راهی برای دیدن فایل تامین‌کننده) قطعی و رفع شد.
