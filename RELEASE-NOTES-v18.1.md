# 📊 ریلیز v18.1 — US-420 «داشبورد سال مالی + تقسیم سود» (تکمیل خانواده R9)

**اسپرینت:** R9 مالی — پس از US-418/422/419/421  
**استوری:** US-420 — داشبورد سال مالی، کاربرگ تقسیم سود، اندوخته شرکت و snapshot قفل‌شده پایان سال

## ✅ اجرا شد

### ۱) ماژول داشبورد سال مالی
- فایل جدید `crm/fiscal.js` اضافه شد.
- این داشبورد به‌صورت hook تمیز روی پنل R9/تنخواه اضافه شد و پنل موازی ساخته نشد.
- ترتیب لود خانواده R9 حفظ شد:
  - `petty.js`
  - `opex.js`
  - `shareholders.js`
  - `lossguard.js`
  - `fiscal.js`

### ۲) مصرف موتور واحد سود
- منطق سود پروژه تکرار نشد.
- داشبورد فقط از موتور واحد زیر استفاده می‌کند:

```js
ptfProjectProfitIRR
```

- پروژه‌هایی وارد سود سال می‌شوند که قابل اتکا باشند.
- پروژه‌های ناقص جداگانه فهرست می‌شوند و در سود سال لحاظ نمی‌شوند.

### ۳) محاسبه سال مالی
تابع جدید:

```js
ptfFiscalData(year)
```

خروجی شامل:
- پروژه‌های قابل اتکا
- پروژه‌های ناقص
- سود/زیان پروژه‌ها
- زیان-only پروژه‌ها
- هزینه‌های جاری سال از `ptfOpexSum(year)`
- مطالبات باز فاکتورها
- سود خالص مدیریتی سال

فرمول کلی:

```text
سود خالص مدیریتی = سود/زیان پروژه‌های قابل اتکا − هزینه‌های جاری سال
```

### ۴) پروژه‌های ناقص
پرونده‌های زیر وارد سود سال نمی‌شوند و به‌عنوان ناقص نمایش داده می‌شوند:
- فروش/CO دارد اما خرید واقعی ندارد.
- فروش ارزی دریافت/تسعیر کافی ندارد.
- خرید ارزی نرخ پرداخت ندارد.
- خروجی موتور سود `ok=false` یا `complete=false` است.

### ۵) کاربرگ تقسیم سود
تابع جدید:

```js
ptfFiscalDistribution(year, distPct)
```

موارد محاسبه‌شده:
- درصد تقسیم سود
- درصد اندوخته/صندوق شرکت
- مبلغ قابل تقسیم
- مبلغ اندوخته
- سهم هر سهامدار براساس درصد سهام
- مانده جاری سهامدار از US-419
- نتیجه نهایی هر سهامدار پس از تقسیم سود

فرمول سهم سهامدار:

```text
نتیجه سهامدار = سهم سود قابل تقسیم + مانده جاری سهامدار
```

اگر نتیجه مثبت باشد، سهامدار بستانکار است.  
اگر نتیجه منفی باشد، سهامدار به شرکت بدهکار است.

### ۶) قفل سال مالی / Snapshot پایان سال
کلید جدید:

```text
ptf_crm_fiscal_snapshots
```

امکان قفل سال اضافه شد:
- فقط admin/chairman
- snapshot شامل داده محاسبه‌شده سال، درصد تقسیم، اندوخته و سهم سهامداران است.
- اگر سال قبلاً قفل شده باشد، دوباره قفل نمی‌شود.
- اگر پروژه ناقص وجود داشته باشد، پیش از قفل هشدار گرفته می‌شود.

### ۷) گزارش چاپی/نمایش گزارش
- دکمه «چاپ گزارش» برای مشاهده خروجی سال مالی اضافه شد.
- خروجی شامل سود خالص، قابل تقسیم، اندوخته و داده محاسبه‌شده است.

## 🔐 داده و سینک
کلید جدید:

```text
ptf_crm_fiscal_snapshots
```

به این بخش‌ها اضافه شد:
- Sync
- Backup
- API whitelist
- Zero-Data Guard
- Go-Live Reset

در Go-Live، snapshotهای آزمایشی سال مالی پاک می‌شوند.

## معماری
- منطق سود دوباره‌نویسی نشد.
- از `ptfProjectProfitIRR` استفاده شد.
- از `ptfOpexSum(year)` برای هزینه‌های جاری استفاده شد.
- از `ptfShareholderBalance(cd)` برای مانده سهامداران استفاده شد.
- زیان‌های پروژه از wrapper قبلی US-421 داخل همان موتور سود لحاظ می‌شوند.
- داده رسمی پایان سال در یک snapshot قفل‌شده نگهداری می‌شود.

## فایل‌های تغییر یافته
- `crm/fiscal.js`
- `crm/sync.js`
- `crm/backup.js`
- `crm/golive.js`
- `crm/index.html`
- `crm/sw.js`
- `api/crm.php`
- `_tools/uat/tester99-v181.js`
- اصلاح تستر قبلی:
  - `_tools/uat/tester98-v180.js`
- `RELEASE-NOTES-v18.1.md`
- `PTF-MASTER-HANDOVER.md`

## 🔬 کیفیت
- تستر جدید: `tester99-v181.js` با ۲۴ چک.
- رگرسیون کامل: **۸۶ تستر / صفر خطا**.
- Audit سایت: PASS با همان هشدار قدیمی تصاویر سنگین.

## وضعیت خانواده R9
با این ریلیز، خانواده R9 طبق برنامه تکمیل شد:

1. US-418 — هزینه‌های جاری ✅
2. US-422 — حساب تنخواه کامل ✅
3. US-419 — سهامداران و علی‌الحساب ✅
4. US-421 — زیان پروژه و هشدار داده ناقص ✅
5. US-420 — داشبورد سال مالی و تقسیم سود ✅

## صف بعدی پیشنهادی
طبق بک‌لاگ، بعد از R9:

- US-410/US-411
- US-405..408
- US-402/US-403
- US-417 فاز ۲

اما یادآوری مهم: `BUG-023 + US-424` از R8 اثر مالی مستقیم دارد و پس از تکمیل R9، اولویت پیشنهادی بالایی دارد مگر اینکه کارفرما ترتیب دیگری ابلاغ کند.
