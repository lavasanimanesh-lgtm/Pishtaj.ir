# ARENA · RCA — «پیشنهاد متصل به پروندهٔ بایگانی در کادر بالای رکوردهای پیشنهاد به‌صورت یافته دیده می‌شود»

**تاریخ:** ۲۰۲۶-۰۹-۱۰ (۱۴۰۵/۰۶/۱۹) · **برچسب تغییر:** CRM **v34.38.18** (ORPHAN-ARCHIVED-DISPLAY) · **تستر:** `tester626-v34.38.18-orphan-archived-not-finding.js`
**دامنه:** `crm/sales-domain-v2.js` · `api/sales-domain.php` · `_tools/uat/tester626-…js` · `_tools/uat/tester625-…LEGACY.md` · `_tools/uat/run-ci-gate.js`

---

## ۱) گزارش کارفرما

> در قسمت پیشنهادات، در کادر بالای رکوردهای پیشنهاد، پیشنهادهایی که به پروندهٔ بایگانی‌شده
> متصل هستند به‌صورت یافته نمایش داده می‌شوند؛ درحالی‌که نباید نمایش داده شوند و یافته محسوب نمی‌شوند.

## ۲) ریشهٔ مشکل

- v34.38.17 (ORPHAN-ARCHIVED) برای جلوگیری از خطای بحرانیِ کاذب «پیشنهاد برنده بدون پرونده»،
  یک ردیف **یافتهٔ اطلاعی** `orphan_won_archived` ساخت و آن را در یک کادر آبی جدا،
  **بالای جدول پیشنهادها** (`#ptfOfferIntegrity` — هوک `hookOfferRender` روی `renderOffers`)
  و هم‌چنین در باکس «فروش تا وصول» صفحهٔ کیفیت داده نمایش می‌داد:
  «🗄 N پیشنهاد برنده به پروندهٔ بایگانی/مختومه متصل است (داده سالم — قابل باز کردن)».
- از دید کارفرما همین ردیفِ اطلاعی هم «یافته» است: پروندهٔ مختومه/بایگانی‌شدهٔ **سالم** نباید
  در هیچ فهرست یافته‌ای (به هر شدتی) ظاهر شود؛ سلامت داده نیازی به اعلام دائمی ندارد.

## ۳) اصلاح (v34.38.18)

| # | تغییر | اثر |
|---|---|---|
| ۱ | `ptfSalesIntegrityScan` (کلاینت): برای پیشنهاد برندهٔ متصل به بایگانی **هیچ یافته‌ای** صادر نمی‌شود؛ تشخیص `archivedCaseForOffer` فقط `orphan_won` بحرانیِ کاذب را سرکوب می‌کند | ردیف‌های آبی/نارنجی برای پروندهٔ سالمِ بسته از بین می‌روند |
| ۲ | `ptfSalesIntegrityHtml` + `hookOfferRender` + راهنمای یافته‌ها: باکس اطلاعیِ بایگانی، فیلتر `orphan_won_archived`، ورودی راهنما و دکمهٔ «بازکردن پروندهٔ بایگانی» از مسیر یافته برچیده شد | کادر بالای رکوردهای پیشنهاد دیگر هیچ ردیف بایگانی ندارد؛ افزودن این یافته به صفحهٔ کیفیت داده هم قطع است |
| ۳ | `sd_migration_report` (سرور): عبارت `orphan_won_archived` از issues حذف شد؛ `orphan_won` بحرانی فقط با شرط `sd_archived_case_for_offer(...)===null` صادر می‌شود (قانون A11 — هم‌راستایی کلاینت/سرور) | گزارش مهاجرت و رجیستری `fin_findings` نیز دیگر این موارد را ثبت نمی‌کنند |
| ۴ | `window.ptfOpenArchivedWonFile` حفظ شد (ابزار بازکردن پروندهٔ بایگانی از کنسول/بایگانی) — فقط از UI یافته‌ها جدا شد | دسترسی به پروندهٔ بایگانی از پنل «پروژه‌ها/بایگانی» همان‌طور که بود می‌ماند |

**دست‌نخورده (مهم):** تشخیص `orphan_won` **بحرانی** برای یتیمِ واقعی — یعنی پیشنهاد برنده‌ای که
نه پروندهٔ فعال دارد و نه بایگانیِ قابل‌اثبات (یا بایگانی با هویت متفاوت دارد) — عیناً پابرجاست
و دکمه‌های «بازگرداندن کنترل‌شده»/«لغو برد و حذف پیشنهاد» همچنان فقط برای آن رندر می‌شوند.

## ۴) تست و تأیید

- `tester626-v34.38.18-orphan-archived-not-finding.js` (ثبت‌شده در `run-ci-gate.js`):
  ساختارِ سرور/کلاینت + رفتار زمان‌اجرا با vm (۵ سناریو: بایگانی ⇒ **صفر یافته**؛ بدون بایگانی ⇒
  orphan_won بحرانی؛ هویت متفاوت ⇒ بحرانی؛ پیوند offerNos ⇒ صفر؛ پروندهٔ فعال ⇒ صفر).
- smoke واقعیِ `ptfSalesIntegrityHtml` روی ۳ سناریو: دیگر هیچ متنِ «بایگانی/مختومه» در HTML نیست؛
  برای یتیمِ واقعی، هشدار + revoke سر جایش.
- `tester625-v34.38.17-orphan-won-archived.js` بازنشسته شد → `…LEGACY.md` (قرارداد قدیمی به خواست کارفرما تغییر کرد).
- `node --check` روی فایل‌های JS سالم؛ تعداد آکولاد/پرانتز `api/sales-domain.php` با نسخهٔ پایه سازگار (php-cli در سندباکس نیست؛ `php -l` در گیت CI اجرا می‌شود).
- **ARCH GUARD: PASS** · **CI gate: 239 PASS / 0 FAIL**.

## ۵) نکتهٔ انتشار

- هیچ مهاجرت داده‌ای لازم نیست؛ تغییر کاملاً نمایشی/تشخیصی است و رکوردهای `fin_findings` قدیمیِ
  نوع `orphan_won_archived` (در صورت وجود روی سرور) صرفاً تاریخی‌اند و دیگر تولید نمی‌شوند.
