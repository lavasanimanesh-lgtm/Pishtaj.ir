# یادداشت‌های انتشار (Release Notes - Version 33.3.3 — ثبات‌سازی جریان‌های مرج‌نشده)

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۱ آگوست ۲۰۲۶ (۱۰ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (ادامهٔ `arena/019fb322-pishtaj-ir` — ۱۶۵ کامیت مرج‌نشده)
**تمرکز ریلیز:** بامپ سراسری نسخه/Cache-buster به `v33.3.3` برای فعال‌سازی ۷۱ کامیت جریان CRM مرج‌نشده، تکمیل SHELL سرویس‌ورکر، بازسازی بستهٔ نصب دستی، و اولین موج تسترهای UAT اختصاصی جریان‌های جدید.

---

## 🚀 دستاوردهای کلیدی نسخه ۳۳.۳.۰

### ۱. بامپ سراسری نسخه به v33.3.3
- `window.VER` در `crm/index.html` و همهٔ ۷۹ ارجاع `?v=` (شامل ۲۴ فایل JS تغییرکرده در برنچ ۰۱۹fb322) → `v33.3.3`.
- کش سرویس‌ورکر → `ptf-crm-v33.3.3`؛ `clear-cache.html` هم‌گام.
- **اثر:** مرورگر کاربران نسخهٔ جدید JS را دریافت می‌کنند؛ بدون این بامپ، ۷۱ کامیت CRM (برگشت فروش، کاتالوگ، لوت‌ها و…) هرگز فعال نمی‌شد.

### ۲. تکمیل SHELL سرویس‌ورکر
- `official-ledger.js` و `ledger-report.js` (از PR #9) به فهرست پیش‌کش اضافه شدند — از قبل جا مانده بودند.

### ۳. بازسازی `crm-update.zip`
- بستهٔ قبلی (PR #9) قدیمی بود: فایل‌های حذف‌شده داشت و فایل‌های جدید را نداشت. بازسازی با وضعیت فعلی: ۵۹۸ فایل (crm + ۴۹۸ مقالهٔ KC + sitemap + api/crm.php + .htaccess + projects/index.html) بدون فایل‌های md/bak.

### ۴. تسترهای UAT اختصاصی جریان‌های جدید (S2)
- **tester200** برگشت فروش/اعتبار مشتری (۱۵ تست)، **tester201** هویت کاتالوگ/ادغام (۲۰ تست)، **tester202** لوت‌های تفکیکی/برگشت تامین‌کننده/انتقال انبار (۲۴ تست) — همه سبز.
- اصلاح تسترهای منسوخ: tester168 (دامنهٔ read-only)، tester2-rbac (باگ واقعی دسترسی AI حسابدار — رفع شد)، ۴۰ تستر با ارجاع دقیق نسخهٔ v31.9 → نسخه‌آگاه، tester227/272 (ترتیب لود)، tester264/265/266 (اولویت sitemap و برچسب ایندکس).

### ۵. رفع باگ واقعی RBAC (BUG-2026-08-01-002)
- نقش «حسابدار» پنل `ai` نداشت در حالی که دستور US-383 «دستیار برای همه نقش‌ها» است → `panels: ['inv','recv','petty','ai']` — tester2-rbac از ۶۸/۱ به ۶۹/۰ سبز شد.

---

## 📋 تغییرات فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/index.html` | VER + ۷۹ cache-bust → v33.3.3 |
| `crm/sw.js` | CACHE v33.3.3 + SHELL +۲ فایل |
| `crm/clear-cache.html` | VER v33.3.3 |
| `crm/rbac.js` | پنل ai برای حسابدار |
| `crm-update.zip` | بازسازی کامل |
| `_tools/uat/tester200/201/202-*.js` | جدید |
| `_tools/uat/tester168,2-rbac,227,272,264,265,266,228-277…` | به‌روزرسانی نسخه/انتظارات |
| `_audit/HANDOFF-NEXT-SESSION-2026-08-01.md` | تحویل نشست |
