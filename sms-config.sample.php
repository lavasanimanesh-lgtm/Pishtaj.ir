<?php
/**
 * PTF — پیکربندی سامانه پیامکی (US-149 / US-132)
 *
 * 📌 نحوه فعال‌سازی:
 *  ۱. این فایل را به نام sms-config.php کپی کنید.
 *  ۲. مقادیر زیر را از پنل پیامکی خود (کاوه‌نگار یا ملی‌پیامک) بگیرید.
 *  ۳. فایل sms-config.php را «خارج از public_html» آپلود کنید
 *     (همان جایی که storage-config.php را گذاشته‌اید — یک پوشه بالاتر از webroot).
 *  ⚠️ هرگز کلید API را در چت/ایمیل نفرستید و فایل را داخل webroot نگذارید.
 *
 * تا وقتی این فایل نباشد، سیستم به‌طور خودکار در «حالت بدون پیامک» کار می‌کند:
 * فرم تامین‌کننده با کپچا (بدون تایید پیامکی) ثبت می‌شود.
 */
return [
    // 'kavenegar' یا 'melipayamak'
    'provider' => 'kavenegar',

    // کلید API (کاوه‌نگار: API Key | ملی‌پیامک: رمز عبور وب‌سرویس)
    'api_key'  => '',

    // فقط برای ملی‌پیامک: نام کاربری وب‌سرویس
    'username' => '',

    // شماره خط خدماتی (مثلاً 10004346 یا 3000xxxx) — برای ارسال OTP خط خدماتی الزامی است
    'line'     => '',
];
