# 🚀 ریلیزنوت v19.1 — اسپرینت ۲ از R12: «اسناد قطعی برد + QC داخل پرونده فروش»

**تاریخ:** ۱۴۰۵/۰۴/۲۱ | **شاخه:** واحد (پس از ادغام v19.0) | **مبنا:** BACKLOG-SALESFILE-SOURCE-OF-TRUTH-R12.md — اسپرینت ۲ پیشنهادی

---

## 📦 محتوای اسپرینت (طبق قاعده ۱-۲ استوری هم‌خانواده)

| آیتم | شرح | وضعیت |
|---|---|---|
| **US-432** | انضمام خودکار سند قطعی پیشنهاد مالی برنده + پیشنهاد فنی مرتبط به پرونده فروش (snapshot تغییرناپذیر) | ✅ کامل |
| **US-434 فاز ۱** | عملیات Post-Award داخل پرونده: کنترل کیفیت/بازرسی (نوت، آزمایش، گزارش، MTC/TPI) + برچسب‌گذاری عملیات پرونده | ✅ کامل |

هر دو از یک خانواده‌اند: «اسناد و عملیات داخل پرونده فروش» — گام دوم رودمپ R12 پس از US-431 فاز ۱ (v18.9).

---

## 🏆 US-432 — اسناد قطعی برد (Award Snapshot)

### چرا snapshot و نه PDF باینری؟
تولید PDF در مرورگر بدون کتابخانه سنگین ممکن نیست و AC4 بک‌لاگ صراحتا اجازه داده: «اگر PDF قابل تولید/ذخیره نبود، حداقل لینک چاپ/نسخه سند در پرونده ثبت شود.» راه‌حل انتخابی **قوی‌تر از لینک** است: **کپی عمیق (snapshot) کل سند** لحظه برد داخل رکورد پرونده ذخیره می‌شود و دکمه «🖨 PDF / چاپ سند برد» همان موتور چاپ رسمی (قالب‌های ۵گانه offers-pro) را روی snapshot اجرا می‌کند — خروجی PDF واقعی از مرورگر، همیشه با **نسخه لحظه ابلاغ**، حتی اگر پیشنهاد بعدا رویژن بخورد یا حذف شود.

### رفتار
1. **لحظه برد CO/TC** (`autoCreateProjectFromCO`): روی رکورد پرونده `awardDocs[]` ساخته می‌شود:
   - سند ۱: پیشنهاد مالی برنده (kind/no/rev + snapshot کامل).
   - سند ۲: آخرین پیشنهاد فنی مرتبط — اول از پیوند رسمی `srcToNo`، وگرنه از `coNo`/`inqNo` با بالاترین رویژن (AC2).
2. **کشوی پرونده فروش**: باکس «🏆 اسناد قطعی برد» با شماره/رویژن/تاریخ و دکمه چاپ (AC3).
3. **مهاجرت نرم** (`sfAwardEnsure`): پرونده‌های برنده قدیمی (قبل از v19.1) با اولین باز شدن کشو، یک‌بار snapshot از پیشنهاد زنده می‌سازند — با برچسب «مهاجرت نرم» و audit.
4. **چاپ** (`sfAwardPrint`): روی **کپیِ** snapshot اجرا می‌شود — سند قطعی هرگز mutate نمی‌شود؛ audit دارد.
5. **بایگانی**: `awardDocs` همراه پرونده به بایگانی منتقل می‌شود (سازگار با الگوی lossEvents در US-421).

### اثر معماری (پیش‌نیاز اسپرینت‌های بعد R12)
US-435/436 (ارجاع فاکتور و صدور فاکتور رسمی) طبق بک‌لاگ باید «PDF پیشنهاد مالی برنده» را ضمیمه کنند — از این نسخه، منبع آن `awardDocs[0]` است، نه پیشنهاد زندهٔ قابل‌تغییر.

---

## 🔬 US-434 فاز ۱ — کنترل کیفیت/بازرسی داخل پرونده فروش

مصداق مستقیم کیس واقعی کارفرما (R9): «کالا وارد شد، مطابقت نداشت، کل هزینه‌ها زیان شد.» از این نسخه نقطه ثبت رسمی انطباق/عدم انطباق داخل خود پرونده است و به موتور زیان متصل شد.

### رفتار
1. دکمه **«🔬 بازرسی / QC»** در کشوی پرونده‌های برنده (کنار زیان/تحویل تعهدی/مختومه).
2. فرم هم‌تم (`ptfDialog`): نوع رکورد (📝 نوت بازرسی / 🧪 نتیجه آزمایش / 📋 گزارش بازرسی / 📜 MTC-TPI-Release Note) + وضعیت انطباق (⏳ در انتظار / ✅ انطباق / ⛔ عدم انطباق) + شرح اجباری.
3. رکورد روی `r.qcEvents[]` + خط timeline پرونده + audit.
4. **عدم انطباق** ⟵ ① notify هشدار به مدیران (SENIOR_ROLES) ② پیشنهاد خودکار باز کردن «💥 ثبت زیان پروژه» (lossguard موجود — بدون منطق سود جدید، طبق قاعده «هر گزارش سود فقط از ptfProjectProfitIRR»).
5. پیوست مدرک: به رکورد QC **و** اسناد پرونده هر دو (الگوی رسید خرید واقعی v18.6).
6. سوابق QC در کشوی پرونده با رنگ قرمز برای عدم انطباق نمایش داده می‌شود.
7. `qcEvents` و `costEvents` (هزینه مستقیم v18.6) هنگام مختومه‌سازی همراه پرونده به بایگانی می‌روند — قبلا `costEvents` جا می‌ماند که همین‌جا اصلاح شد.
8. برچسب راهنمای «🧰 عملیات پرونده (Post-Award) — همه فقط از داخل همین پرونده» بالای ردیف دکمه‌ها (اصل US-434).

### فاز ۲ (بک‌لاگ — اسپرینت ۳ R12)
پکینگ لیست، بارنامه/اسناد ارسال و گذارهای وضعیت مرحله‌ای همراه US-433.

---

## 🛡 اثبات شروط دائمی کارفرما

| شرط | اثبات |
|---|---|
| بدون وصله‌پینه | snapshot در همان نقطه واحد ساخت پرونده (autoCreateProjectFromCO)؛ QC با هسته برنامه‌ای `sfQcCommit` جدا از UI؛ چاپ از همان موتور رسمی offers-pro — هیچ رندر/موتور موازی ساخته نشد |
| وابستگی‌ها | اتصال به lossguard (US-421)، ptfDialog (ui-kit)، attachUploadWidget، بایگانی (projects)، قالب‌های چاپ (offers-pro)؛ کلید داده جدید ساخته نشد — همه روی `ptf_crm_deals` موجود (سینک/GUARD/whitelist از قبل کامل) |
| بدون باگ جدید | ۹۴ تستر / **۲۶۶۸ چک — صفر خطا** + `node --check` ۵۹ فایل JS + ۵ بلوک inline + audit PASS |
| بدون تداخل با طرح توسعه | دقیقا اسپرینت ۲ پیشنهادی BACKLOG-R12؛ پیش‌نیاز US-435/436 آماده شد؛ US-433/434ف۲ دست‌نخورده در صف |

## 🧪 تست
- **tester107** (جدید): ۴۲ چک — ساختاری + رفتاری (snapshot دو-سندی، مصونیت از ویرایش بعدی، sfQcCommit با notify عدم انطباق) + رگرسیون BUG-027/US-421/US-431ف۱/US-404ف۲.
- tester102: چک cache-bust سالسفایل به الگوی «>=» اصلاح شد (قاعده BUG-026).
- کل مجموعه: **۹۴ تستر / ۲۶۶۸ چک سبز**.

## 📂 فایل‌های تغییر یافته
`crm/offers.js` (awardDocs لحظه برد) | `crm/salesfiles.js` (sfAwardEnsure/sfAwardPrint/باکس اسناد برد + QC کامل + انتقال بایگانی) | `crm/index.html` + `crm/sw.js` (v19.1) | `_tools/uat/tester107-v191.js` (جدید) | `_tools/uat/tester102-v185.js` (نسخه‌پذیر)

## 📤 آپلود
zip روی هاست روی نسخه موجود اکسترکت شود — **پوشه api حذف نشود** (بانک داده `api/ptf_crm_data.json` عمدا داخل بسته نیست). بعد از آپدیت، همه کاربران یک بار رفرش کنند (چک «CRM v19.1» در سایدبار).

---

## ⏭ صف بعدی (منتظر ابلاغ)
① US-433 + US-434ف۲ (وضعیت‌های ۱۲گانه Post-Award + پکینگ/بارنامه — اسپرینت ۳ R12) ② US-435/436 (قفل و صدور فاکتور رسمی — اسپرینت ۴ R12) ③ US-437 ④ Post-R9 (BUG-024/025 اجراشده در v18.4 — باقی: US-426ف۲..429) ⑤ US-423 (کارت ویزیت) ⑥ US-410/411 ⑦ US-405..408 ⑧ US-402/403

**شماره‌گذاری:** بعدی US-438 | باگ بعدی BUG-030
