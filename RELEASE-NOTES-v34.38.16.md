# CRM v34.38.16 — PULL-RESILIENCE + ORPHAN-WON-ARCHIVED

**تاریخ:** 2026-09-09
**بازوی تغییر:** Arena Agent
**کانال:** CRM (سینک/بوت + یکپارچگی فروش)

> این فایل همان «یادداشت انتشار نسخهٔ جاری» است که گیت CI (تستر۵۷۸، سطر ۷۹) وجودش را
> با کلید `VERSION.json → crm_version` کنترل می‌کند.

## ۱) مشکل (چرا)

پس از v34.38.15 (BOOT-REVERT) بوت از مسیر تک‌مرحله‌ای `data_pull` می‌آید، اما
اعتبارسنجِ نسخهٔ v2 روی همان مسیر هم فعال است. اگر سرور برای یک کلید entryِ کاملِ
manifest ندهد — نمونهٔ واقعی: `ptf_crm_commission_records` که مقدارش شیء/تهی است و
`count`/`kind` درست برنمی‌گردد — کلِ snapshot با دلیل
`commission_records:integrity-manifest-missing` fail-closed می‌شد. نتیجه:

- مکان‌نمای rev جلو نمی‌رفت و «دریافت کامل اولیه» هرگز تمام نمی‌شد؛
- bootloader سبز نمی‌شد و پیام تکرارشوندهٔ «دریافت کامل CRM انجام نشد» می‌ماند؛
- داده‌های سالمِ بقیهٔ کلیدها هم پشت همان یک کلیدِ بد بلوکه می‌شدند.

هم‌زمان، گزارش کارفرما: پیشنهادِ برنده‌ای که پروندهٔ فروشش مختومه/بایگانی شده
(رکورد از `ptf_crm_deals` به `ptf_crm_projects` با `state='archived'` و
`origin='salesfile'` رفته) به‌اشتباه `orphan_won` **بحرانی** گزارش می‌شد و دکمهٔ
«بازگرداندن برد / حذف پیشنهاد» نشان می‌داد.

## ۲) تغییر ۱ — PULL-RESILIENCE (رفع قفل دریافت کامل)

### `crm/sync.js`
- کلیدی که entryِ کاملِ manifest ندارد دیگر کلِ snapshot را fail نمی‌کند؛ در فهرست
  **`unavailable`** می‌رود (`return false` به‌جای `return true`) و از تصویرِ pull بیرون
  می‌ماند ⇒ **نسخهٔ سالمِ محلیِ همان کلید حفظ می‌شود** و بقیهٔ کلیدها اعمال می‌شوند.
- الگو، همان الگوی v34.38.11 برای «کلیدِ ناخوانا» در مسیر اتمیک است.
- `integrity-mismatch` **واقعی** (kind/count/sha256 ناسازگار) همچنان fail-closed
  می‌ماند — در هر دو مسیر اتمیک و legacy.

نکتهٔ دامنه: بوت همچنان از `data_pull` تک‌مرحله‌ایِ قبل از v34.38.10 می‌آید
(`window.PTF_CRM_SNAPSHOT_V2 = false` از v34.38.15 دست‌نخورده است) و سپرهای دادهٔ
v34.38.12/.13/.14 سرِ کارند. این نسخه فقط نقطهٔ باقی‌ماندهٔ همان زنجیره را می‌بندد.

## ۳) تغییر ۲ — ORPHAN-WON-ARCHIVED (یافتهٔ اطلاعی به‌جای بحرانی)

### `crm/sales-domain-v2.js` + `api/sales-domain.php`
- پیوندِ پیشنهاد به یک پروندهٔ بایگانیِ salesfile (از شمارهٔ پیشنهاد + سازگاری هویت)
  یک یافتهٔ **اطلاعی** `orphan_won_archived` می‌سازد که فقط «بازکردن پروندهٔ بایگانی»
  دارد — هرگز revoke / حذف برد.
- `orphan_won` بحرانی فقط وقتی می‌ماند که هیچ بایگانیِ قابل اثباتی نباشد؛ تطبیق، هویت
  را هم کنترل می‌کند تا پروندهٔ سالمِ بسته پنهان نشود و orphanِ واقعی بحرانی بماند.
- دکمهٔ راهنمای ردیف‌ها به تابع مشترک `guideBtn(x)` ریفکتور شد تا در هر دو جعبهٔ
  «یافته‌ها» و «پیشنهاد برنده به پروندهٔ بایگانی» یکسان رندر شود. **برچسب همان
  «🧭 راهنمای بررسی و رفع» قبلی ماند** (بخش ۵).

## ۴) سطح انتشار نسخه

`VERSION.json → v34.38.16`؛ `crm/index.html` (`PTF_CRM_RELEASE`/`VER` + cache-busterها)؛
`crm/sw.js`؛ `crm/manifest.json`؛ `crm/shell.js`؛ `crm/cms.js`؛ `crm/clear-cache.html`؛
`crm/device-reconnect.html`؛ `crm/client-server.js`؛ `api/sales-domain.php`
(`SD_SERVICE_VERSION = '34.38.16'`). پینِ نسخهٔ همهٔ تسترهای version-stamped به
34.38.16 مهاجرت کرد.

## ۵) یادداشت تکمیلی — رفع بلاکرِ دیپلویِ همین نسخه

اولین تلاشِ دیپلوی پروداکشنِ v34.38.16 (GitHub Actions run `34382007494`،
`workflow_dispatch` روی main @ `8d2ec72`، ۲۰۲۶-۰۹-۰۹ ساعت ۱۷:۱۷ UTC) در گام
**«CI gate»** با exit 1 شکست و همهٔ گام‌های بعدی (lint، آپلود FTP، راستی‌آزمایی،
تگ، release) skip شدند ⇒ چیزی روی پروداکشن نرفت و سایت روی v34.38.15 ماند.
علت، دو تستر بود:

1. **`tester412-v34.7.7-guided-case-dedup.js`** (۲ ادعا): در ریفکتورِ `guideBtn(x)`
   برچسب دکمه از «🧭 راهنمای بررسی و رفع» به «🧭 راهنمای بررسی» کوتاه شده بود؛ تستر
   وجودِ خودِ آن رشته در `crm/sales-domain-v2.js` و نیز در خروجیِ
   `ptfSalesIntegrityHtml()` را کنترل می‌کند. **برچسب به حالت کامل برگردانده شد.**
2. **`tester578-v34.29.7-cms-css-visibility.js`** (سطر ۷۹): چکِ متای
   «یادداشت انتشار نسخهٔ جاری موجود است» ⇒ فایل `RELEASE-NOTES-v34.38.16.md` ساخته
   نشده بود. **همین فایل.**

نتیجهٔ بازتولید محلی پیش از رفع: `=== CI gate: 236 PASS / 2 FAIL ===`.

## ۶) تست‌های قفل‌شده

- `_tools/uat/tester624-v34.38.16-pull-resilience.js` — رفتار کلیدِ با manifest ناقص
  (unavailable به‌جای fail-کل) + حفظ fail-closed برای mismatch واقعی.
- `_tools/uat/tester625-v34.38.17-orphan-won-archived.js` — یافتهٔ اطلاعی در برابر
  orphan_won بحرانی، روی کلاینت و سرور، با کنترل هویت.
- `_tools/uat/tester412-v34.7.7-guided-case-dedup.js` — قفلِ برچسب/دکمهٔ راهنمای ردیف.
- **گیت کامل:** `node _tools/uat/run-ci-gate.js` باید کامل سبز باشد.

## ۷) اقدام پس از دیپلوی

1. مرورگرهای گیرکرده روی نسخهٔ قبل، یک بار `crm/clear-cache.html` را باز کنند
   (یا Hard-Refresh) تا سرویس‌ورکر نسخهٔ جدید نصب شود.
2. اگر «دریافت کامل اولیه» هنوز تمام نشد، از «تنظیمات ← ابزار اتصال مجدد امن مرورگر»
   (v34.38.14) استفاده شود؛ مسیر تشخیصی `sync-diagnostics` علت را نشان می‌دهد.
3. یافته‌های `orphan_won_archived` اطلاعی‌اند؛ فقط «بازکردن پروندهٔ بایگانی» دارند و
   نباید با revoke اشتباه گرفته شوند.
