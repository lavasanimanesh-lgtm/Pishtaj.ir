# یادداشت انتشار CRM v34.8.11

تاریخ: ۱۴۰۵/۰۶/۵ (۲۰۲۶-۰۸-۲۷)

## VERBATIM-CONVERGENCE — بستن ریاضی حلقهٔ تعارض کلیدهای مالی

### RCA (گزارش کارفرما: مدیر بازرگانی و ادمین هنوز نوار زرد دارند؛ ptf_crm_opex همگرا نمی‌شود)
نجات v34.8.10 برای کلیدهای محافظت‌شده کار می‌کرد ولی می‌توانست حلقه بسازد:
۱) بعد از merge سه‌طرفه، پاسِ tombstone روی خروجی اعمال می‌شد و canonical سرور را تغییر می‌داد → امضای push بعدی متفاوت → تعارض مجدد؛
۲) merge سه‌طرفه اگر کوچک‌ترین تفاوت محلی را زنده نگه می‌داشت، همان تفاوت در هر چرخه دوباره تزریق می‌شد → تعارض ابدی.

### رفع
`ptfSyncResolveProtectedConflictFromServer` حالا دو مسیر دارد:
- **اگر مقدار محلی از لحظهٔ ارسال تغییر نکرده باشد:** canonical سرور **عیناً (verbatim)** پذیرفته می‌شود — بدون merge مجدد و بدون tombstone (سرور خودش tombstones را پیش از merge اعمال کرده). این تضمین می‌کند push بعدی امضای یکسان بدهد و ACK شود؛ همگرایی در حداکثر یک چرخهٔ نجات.
- **اگر در پرواز ویرایش جدیدی رسیده باشد:** همان merge سه‌طرفهٔ قبلی (ویرایش حفظ می‌شود، یک چرخهٔ اضافه).

### چرا این‌بار «قطعی» است
شرط پذیرش سرور: امضای incoming == امضای canonical. با پذیرش verbatim، incoming بایت‌به‌بایت همان canonical است → عدم تقارن (asymmetry) تزریق تفاوت حذف می‌شود؛ حلقه از نظر منطقی ممکن نیست.

### کنترل
tester515 با اطمینان‌های verbatim؛ گیت CI کامل سبز.
