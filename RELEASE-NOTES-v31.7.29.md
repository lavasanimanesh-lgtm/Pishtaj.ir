# Release Notes — v31.7.29

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.29
**نوع:** 🟡 رفع تکرار پیشنهاد/درخواست در «فرصت‌های فعال» پرونده‌های فروش (BUG-OPPO-DUP-001)

---

## 🎯 گزارش کارفرما (با اسکرین‌شات)

در پرونده‌های فروش → فرصت‌های فعال، یک درخواست و پیشنهاد مالی گاهی چند بار تکرار شده — در اسکرین‌شات: `PTF-CO-1405-0441` دو بار (یک‌بار sent و یک‌بار draft) زیر همان مشتری، و شمارنده «۷ پیشنهاد مالی» بیش از اسناد واقعی.

## 🔍 علت ریشه‌ای (RCA)

**رکوردهای هم‌شماره در `ptf_crm_offers` — میراث دوران BUG-CODE-DUP-003** (قبل از رفع v31.7.24، ترکیب شماره تکراری سرور + مسیرهای sync دو نسخه از یک سند با وضعیت‌های متفاوت باقی گذاشته بود؛ مثلاً نسخه sent و draft همان 0441). نمای فرصت‌ها (هر دو حالت «بر اساس مشتری» US-435 و «بر اساس درخواست») هیچ dedup ای نداشت و هر رکورد را یک ردیف مستقل نشان می‌داد. تولید تکراریِ جدید از v31.7.24 بسته شده؛ این ریلیز نمایش را در برابر داده آلوده باقیمانده مقاوم می‌کند.

## ✅ اصلاحات

| # | تغییر | جزئیات |
|---|--------|--------|
| 1 | **dedup نمایشی بر اساس شماره سند** در هر دو نمای فرصت‌ها | نماینده هر شماره = رکورد با **وضعیت پیشرفته‌تر** (sent بر draft، …) و در تساوی، تاریخ جدیدتر — دقیقاً نسخه‌ای که کاربر باید ببیند |
| 2 | **شمارنده‌های واقعی** | بج «N پیشنهاد مالی» حالا اسناد یکتا را می‌شمارد |
| 3 | **هشدار پاکسازی صادقانه** | اگر شماره‌های هم‌رکورد در داده باشند، نوار زرد بالای فهرست: «⚠️ N شماره دارای رکورد تکراری است (فهرست) — برای پاکسازی ریشه‌ای: تنظیمات → بررسی کدهای تکراری» — مشکل داده پنهان نمی‌شود |
| 4 | **گزینه‌های موازی (US-OFF-ALT) سالم** | پیشنهادهای واقعاً متفاوت (شماره‌های متفاوت برای یک درخواست — اروپایی/چینی) مثل قبل جدا نمایش داده می‌شوند |

## 📁 فایل‌های تغییریافته

`crm/oppo.js`، `crm/index.html` (bump)، `crm/sw.js`، `crm/clear-cache.html`، `_tools/uat/tester204` (جدید)

## 📊 نتایج گیت

- تستر جدید `tester204`: **۱۲/۱۲ PASS** — بازتولید دقیق اسکرین‌شات (0441 دوتایی → یکی با نسخه sent)، شمارنده واقعی، هشدار آلودگی، هر دو نما، و رگرسیون گزینه‌های موازی
- رگرسیون کامل: **182/182 فایل PASS — 4118/4118 چک PASS** | `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| داده offers | صفر تغییر — dedup فقط لایه نمایش فرصت‌ها | صفر |
| فهرست اصلی پیشنهادات | این ریلیز فقط فرصت‌ها را پوشش می‌دهد؛ رکوردهای هم‌شماره در فهرست اصلی با پاکسازی duplicates حل می‌شوند | — |
| win/loss و پرونده‌سازی خودکار | دست‌نخورده — dedup پس از محاسبه wonInq اعمال می‌شود | صفر |

## 📋 راستی‌آزمایی توسط کارفرما (۲ دقیقه)

1. پرونده‌های فروش → فرصت‌های فعال → گروه Foulad Mashiz → `0441` فقط یک بار (با وضعیت sent) و شمارنده بج واقعی.
2. اگر نوار زرد «رکورد تکراری» دیدید → تنظیمات → «بررسی کدهای تکراری» برای پاکسازی ریشه‌ای (پس از آن نوار محو می‌شود).

---
*توسعه‌دهنده: PTF Development Team — 2026-07-20*
