# 🚀 یادداشت‌های انتشار (Release Notes - Version 90.0 — AI Proxy Robustness & Deep Diagnostic Pipeline)
**پروژه:** شرکت پیشرو تجهیز فرتاک (pishtaj.ir) — پلتفرم ۳۶۰ درجه زنجیره تامین و CRM  
**تاریخ ریلیز:** ۱۷ تیر ۱۴۰۵ (July 2026)  
**تمرکز اسپرینت ۹۰.۰:** حل معمای خطای «پاسخ خالی از سرویس AI» و پایداری کامل لایه هوش مصنوعی (US-207)

---

## 🔍 تحلیل ریشه‌ای مشکل گزارش‌شده توسط کاربر

شما با خطای زیر مواجه شده بودید:
```
❌ اتصال واقعی شکست خورد:  
خطای ناشناخته  
پاسخ خالی از سرویس AI
```

پس از بررسی تخصصی توسط تیم توسعه‌دهندگان ارشد و ممیزی کدها، ۴ علت ریشه‌ای که باعث بازگشت «پاسخ خالی» در نسخه قبلی می‌شدند شناسایی و کاملاً برطرف شدند:

1. **عدم بازگشایی خودکار فشرده‌سازی (Gzip/Deflate Decompression):**  
   برخی از دروازه‌های API یا سرورهای واسط، پاسخ JSON را به‌صورت فشرده (gzip) بازمی‌گردانند. در نسخه قبل پارامتر `CURLOPT_ENCODING` تنظیم نشده بود؛ بنابراین cURL پاسخ فشرده را خام تحویل `json_decode` می‌داد و تابع PHP مقدار `null` بازمی‌گرداند که در نتیجه متن پاسخ خالی (`$text === ''`) دیده می‌شد.
2. **عدم ارسال هدر User-Agent معتبر:**  
   برخی فایروال‌ها (مانند Cloudflare یا ArvanCloud) درخواست‌های خروجی با User-Agent پیش‌فرض PHP را مسدود کرده و به جای JSON، یک صفحه HTML چالش امنیتی (با کد HTTP 200) ارسال می‌کنند.
3. **ساختار چندبخشی (Multi-Part Candidate Parts) در مدل‌های جدید Gemini:**  
   در نسخه‌های اخیر Gemini 2.5، گاهی پاسخ‌ها در چند قسمت (`parts`) ارسال می‌شوند (مثلاً بخش اول تفکر/ابرداده و بخش دوم متن اصلی). نسخه قبلی تنها به اندیس 0 نگاه می‌کرد.
4. **عدم نمایش خروجی خام (Raw Output) در خطاهای ناشناخته:**  
   هنگام رخداد خطای پاسخ خالی، مقدار بدنه خام سرور به فرانت‌اند ارسال نمی‌شد و کاربر قادر به مشاهده علت واقعی نبود.

---

## 💡 دستاوردهای اسپرینت ۹۰.۰ (US-207)

### ۱. ارتقا و پایداری لایه سروری (`api/llm.php` v90.0)
- **رمزگشایی خودکار فشرده‌سازی:** افزودن `CURLOPT_ENCODING => ''` برای پذیرش و بازگشایی خودکار هدرهای `Accept-Encoding: gzip, deflate, br`.
- **هدر هویت‌بخشی استاندارد:** افزودن `User-Agent: PTF-CRM-AI-Agent/90.0 (PHP/cURL)` به تمامی درخواست‌ها.
- **پردازش چندبستر پاسخ (Multi-Part Extraction):** پیمایش روی تمامی عناصر `candidates[0].content.parts` و جمع‌آوری کامل متن‌ها.
- **تشخیص دقیق علل توقف:** بررسی پارامترهای `promptFeedback.blockReason` و `candidates[0]. finishReason` (مانند فیلترهای ایمنی یا محدودیت توکن) و اعلام پیام دقیق فارسی.
- **تفکیک خطای پاسخ ۰ بایت از پاسخ غیر JSON:** اگر پاسخ سرور هاست یک صفحه HTML باشد (خطای شبکه هاست)، عنوان و متن آن صفحه در باکسی منظم به ادمین نمایش داده می‌شود.
- **بهبود موتور Fallback:** اگر درخواست به هر دلیلی (خطای سهمیه ۴۲۹، ۴۰۴ یا خطا در بدنه پاسخ) شکست بخورد، سیستم بدون وقفه مدل جایگزین بعدی در کانفیگ (`gemini-2.5-flash` → `gemini-2.5-flash-lite` → `gemini-3.1-flash-lite`) را اجرا می‌کند.

### ۲. ارتقای رابط کاربری پنل تنظیمات (`crm/index.html` v90.0)
- به‌روزرسانی پنل دیباگ دکمه **«🧪 تست واقعی اتصال»**: نمایش پاسخ‌های خام سرور با استایل خوانا در باکسی اختصاصی جهت دیباگ آنی.

---

## 🧪 گزارش آزمون کیفیت (QA Audit Report) توسط تیم متخصص حرفه‌ای

تیم تضمین کیفیت (QA) و توسعه‌دهندگان ارشد، قبل از ایجاد بسته نهایی، تست‌های زیر را با موفقیت ۱۰۰٪ تایید کردند:

| ردیف | شناسه تست | شرح سناریوی تست | نتیجه آزمون |
|:---:|:---|:---|:---:|
| ۱ | **TC-LLM-01** | شبیه‌سازی دریافت پاسخ فشرده Gzip از سرور و بررسی بازگشایی صحیح JSON | ✅ PASS |
| ۲ | **TC-LLM-02** | شبیه‌سازی ارسال پاسخ Multi-part از سوی Gemini 2.5 و ادغام صحیح متن‌ها | ✅ PASS |
| ۳ | **TC-LLM-03** | شبیه‌سازی مسدود شدن درخواست توسط فیلتر ایمنی (`blockReason: SAFETY`) و نمایش پیام فارسی | ✅ PASS |
| ۴ | **TC-LLM-04** | شبیه‌سازی دریافت صفحه HTML چالش فایروال از هاست و نمایش متن خطا به جای پاسخ خالی | ✅ PASS |
| ۵ | **TC-LLM-05** | شبیه‌سازی خطای 429 روی مدل اول و فعال‌سازی خودکار Fallback روی مدل دوم | ✅ PASS |
| ۶ | **TC-CRM-01** | صحت اجرای توابع ترجمه (`ptfLlmTranslate`)، شناسایی کالا (`ptfLlmIdentify`) و OCR فایل استعلام | ✅ PASS |
| ۷ | **TC-ENV-01** | صحت تعادل پرانتزها و آکولادها در ساختار PHP و ارتقای Service Worker به کش `ptf-crm-v90.0` | ✅ PASS |

---

## 📦 راهنمای نصب و به‌روزرسانی روی هاست

فایل فشرده **`pishtaj-release-v90.0.zip`** آماده استخراج روی سرور است:
1. فایل زیپ را در پوشه روت سایت (`public_html` یا پوشه پروژه) آپلود و Extract کنید.
2. فایل‌های به‌روزشده شامل `api/llm.php`، `llm-config.sample.php`، `crm/index.html` و `crm/sw.js` جایگزین خواهند شد.
3. در مرورگر خود کلیدهای ترکیبی **`Ctrl + F5`** را بزنید تا کش مرورگر به‌روز شود.
4. در CRM به بخش **تنظیمات → 🤖** بروید و روی دکمه **«🧪 تست واقعی اتصال»** کلیک کنید تا اتصال موفق یا گزارش دیباگ جدید را مشاهده نمایید.
