# فاز ۶ (v34.0.9-alpha) — هماهنگ‌سازی «تراز رسمی/غیررسمی» و «گزارش مالی تجمیعی»

**برنچ:** `arena/019fcd1d-pishtaj-ir`
**مبنای تصمیم:** تأیید کارفرما برای اصلاح مغایرت‌های شناسایی‌شده (فاکتور صوری/پوششی، legacy، وابستگی به «امروز»).

---

## مغایرت‌های اصلاح‌شده

### M-1 + M-2 — فاکتور صوری/پوششی در هر دو گزارش
- **گزارش A (تراز):** قبلاً `purchase.total` (مبلغ اسمی، شامل cover) از سود کم می‌شد → فاکتور صوری کامل هزینه می‌شد. حالا سود بر **خریدِ واقعی + منفعت پوششی** محاسبه می‌شود (`realPurchaseSplit` + `coverBenefitTotal`). مبلغ اسمی پوششی در سود کم نمی‌شود.
- **گزارش B (تجمیعی):** قبلاً فاکتور پوششی کامل به `supplierLiability` اضافه می‌شد. حالا فقط **کارمزد فاکتورساز** به بدهی می‌رود و **اعتبار ارزش‌افزوده (منفعت)** جدا (`coverCommission`/`coverVat`) گزارش می‌شود.

### M-3 — legacy از مبلغ بدهی حذف
- در گزارش B، `legacy.filter(...)` دیگر به `src.supplierLiability` اضافه نمی‌شود؛ به‌صورت `legacyUnlinked` جدا گزارش می‌شود (سازگار با فاز ۳ که در `balance()` legacy را از مبلغ حذف کرد). مبنای تعهد فقط فاکتور خرید است.

### M-B3 — حذف وابستگی خروجی به «امروز»
- `slSupplierOpenTotalsIRR` (منبع بازنویسی گزارش B وقتی `asOf===today`) از `balance()` می‌آید که حالا با فاز ۳ و cover هماهنگ است → دو مسیر (داخلی vs بازنویسی) هم‌ارز شدند؛ خروجی مستقل از «امروز بودن یا نبودن» است.

---

## نتیجهٔ شبیه‌سازی عددی (دادهٔ یکسان)
فروش ۵۰۰م + opex ۵۰م + خرید واقعی ۲۰۰م + فاکتور صوری ۱۰۰م (VAT ۱۰م، کارمزد ۱.۵م) + تعهد legacy ۷۰م:

| شاخص | قبل | بعد |
|---|---|---|
| سود تجمیعی (گزارش A) | ۱۵۰م (شامل ۱۰۰م پوششی کامل) | **۲۵۸.۵م** (خرید واقعی ۲۰۰م + منفعت ۸.۵م) |
| بدهی تأمین‌کننده (گزارش B) | ۳۰۰م (شامل ۱۰۰م پوششی) | **۲۰۱.۵م** (۲۰۰م واقعی + ۱.۵م کارمزد) |
| منفعت پوششی (VAT−کارمزد) | محاسبه نمی‌شد | ۸.۵م (جدا در هر دو گزارش) |
| تعهد legacy | در بدهی ۷۰م | از بدهی حذف، فقط گزارش |

**هماهنگی:** بدهیB = خریدواقعیA + کارمزد پوششی (۲۰۱.۵م) و منفعت پوششی در هر دو گزارش یکسان (۸.۵م).

---

## تست‌ها
- **tester305** جدید (۱۳/۰): پوشش هر دو گزارش + هماهنگی + legacy + `balance()`.
- رگرسیون کامل سبز (۱۴ تستر، ۰ شکست).
- بامپ نسخه → **v34.0.9-alpha** (VERSION.json + VER + باستر + کش SW + clear-cache).
- تغییرات `.github/workflows/*` در PENDING-workflow-changes ثبت شد (بدون اجازهٔ workflows).

*تولیدشده توسط Arena Agent — اصلاح فازبندی‌شدهٔ مغایرت‌های گزارش‌های مالی.*
