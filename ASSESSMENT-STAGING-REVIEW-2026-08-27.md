# گزارش بررسی نسخهٔ روی استیجینگ — چرا «بعضاً باگ و ناموفق» است

**تاریخ:** ۲۰۲۶-۰۸-۲۷ | **نوع:** ارزیابی زندهٔ استیجینگ + کدِ دلتا (بدون تغییر کد)
**مبنای مقایسه:** `main` = `5efcb49` (v34.8.12، PR #87) در برابر استیجینگ زندهٔ `staging.pishtaj.ir`

---

## ۰) حکم یک‌خطی

استیجینگ نسخهٔ **v34.8.15** (برنچ `arena/01a03ab4-pishtaj-ir`، ۳ کامیت جلوتر از مین) را **تبلیغ** می‌کند، اما زنده‌اش **نسخهٔ مخلوط (Frankenstein)** است: `index.html` جدید (34.8.15) است ولی `leads.js`، `sales-domain-v2.js` و `sw.js` هنوز **نسخهٔ قدیمی (34.8.12)** را سرو می‌کنند — یعنی فاز C2/C3 (فرمان‌های موجودیتی یادآور/سرنخ) عملاً روی استیجینگ «لایو» نیست، ولی رابط کاربری ادعای نسخهٔ جدید دارد. به‌علاوه یک **باگ امنیتی تأییدشده روی هر دو محیط** (نشت موبایل/ایمیل کاربران بدون لاگین — روی پروداکشن!) پیدا شد. ترکیب همین دو عامل، تجربهٔ «بعضاً باگ و ناموفق» را می‌سازد.

---

## ۱) استیجینگ دقیقاً چه نسخه‌ای دارد؟

| مورد | مقدار | منبع بررسی |
|---|---|---|
| برنچ دیپلوی‌شده | `arena/01a03ab4-pishtaj-ir` @ `6a4a47d6` | بنر زندهٔ استیجینگ |
| نسخهٔ اعلامی | v34.8.15 | داشبورد زنده («نسخهٔ فعال») |
| نسخهٔ مین | v34.8.12 (`5efcb49`) | VERSION.json |
| دلتا | ۳ کامیت: `d83603cb` (v34.8.13 فاز C2 — زیرساخت فرمان عمومی موجودیت + پایلوت یادآور)، `703380d4` (v34.8.14 فاز C3 — یادآور کامل + مهاجرت سرنخ‌ها)، `6a4a47d6` (v34.8.15 — فیکس projection فرمانی که اعمال نمی‌شد) | `git log main..برنچ` |
| فایل‌های تغییرکردهٔ دلتا | `api/sales-domain.php` (+۱۱۸)، `crm/index.html`، `crm/leads.js`، `crm/sales-domain-v2.js` (+۶۴)، `crm/sw.js`، `crm/shell.js`، `crm/manifest.json`، تسترهای 517/518 | diffstat: ۸۳ فایل، +836/−468 |

دلتای واقعی = «مسیر فرمان اتمیک سروری» برای یادآور و سرنخ (گام بعدیِ کلاینت‌نازک‌سازی، ادامۀ فاز B). خودِ کد دلتا **سالم** است:
- گیت CI رسمی ریپو روی همان برنچ: **۱۳۴ PASS / ۰ FAIL** ✓
- arch-guard: **PASS** ✓ (فقط بدهی قدیمی A3/A9، تخلف جدید ندارد)
- تسترهای جدید 517 (۲۶/۰) و 518 (۱۵/۰) ✓
- قرارداد نسخه در سورس (sw.js/manifest/shell/index) همه 34.8.15 ✓
- مخزن دادهٔ فرمان‌ها همان فروشگاه JSON + meta rev مشترک `data_pull` است (sd_publish_changes → همان `<key>.json` + `sd_meta_commit` + mirror دیتابیس) — **منبع حقیقت دوم به‌وجود نیامده** ✓

پس مشکل «باگ و ناموفق بودن» از کیفیت کد دلتا نیست؛ از **استقرار** و دو باگ مستقل است که پایین‌تر آمده.

---

## ۲) یافتهٔ ۱ — استیجینگ نسخهٔ مخلوط سرو می‌کند (ریشهٔ اصلی «ناموفق بودن‌ها») 🔴

بررسی زندهٔ فایل‌های سروشده از استیجینگ (درخواست مستقیم HTTP):

| فایل زنده | انتظار (v34.8.15) | واقعیت زنده | حکم |
|---|---|---|---|
| `crm/index.html` | ثابت‌های 34.8.15 | **34.8.15** ✓ (داشبورد «نسخهٔ فعال: v34.8.15») | جدید |
| `crm/client-server.js` | بدون تغییر در دلتا (≥34.8.9) | کد v34.8.9 موجود ✓ | جدید (به‌اندازهٔ کافی) |
| `crm/leads.js?v=34.8.15` | باید `ptfEntityUpsert` داشته باشد (v34.8.14) | **`setData` سادهٔ قدیمی — بدون هیچ کد فرمانی** | ❌ قدیمی (34.8.12) |
| `crm/sales-domain-v2.js?v=34.8.15` | باید `PTF_ENTITY_CMD_ENABLED`/`ptfEntityUpsert` داشته باشد (v34.8.13) | **بلاک فرمان عمومی اصلاً وجود ندارد** — بعد از `ptfSalesDomainCommand` مستقیم `ptfSalesCommandErrorIsAmbiguous` آمده | ❌ قدیمی (34.8.12) |
| `crm/sw.js` | `CACHE = ptf-crm-v34.8.15` | **`CACHE = ptf-crm-v34.8.12`** | ❌ قدیمی (34.8.12) |

**معنا:** نسخهٔ روی استیجینگ ادعای 34.8.15 می‌کند ولی رفتارِ لید/یادآور همان 34.8.12 است؛ قرارداد سرویس‌ورکر (که خودش در کامنتش الزامی می‌داند RELEASE با PTF_CRM_RELEASE و query اسکریپت‌ها یکی باشد) زیر پا است. «تست دستی روی استیجینگ» که در RELEASE-NOTES-v34.8.15 ثبت شده در این وضعیت معتبر نیست — ایجنت/کارفرما در واقع نسخهٔ قدیمی را تست کرده. هر تست بعدی هم بسته به اینکه مرورگر/کش چه ترکیبی بدهد، نتیجهٔ متفاوت می‌بیند = دقیقاً حس «بعضاً کار می‌کند بعضاً نه».

### علت محتمل (دو مظنون، هر دو باید روی هاست چک شوند)
1. **کش سرور/CDN که query string را نادیده می‌گیرد:** `.htaccess` (خط 247-249) برای همهٔ `.js` این را می‌فرستد: `Cache-Control: public, max-age=2592000` (۳۰ روز). اگر LiteSpeed/CDN هاست با کلید URI بدون query کش کند، `leads.js?v=34.8.15` (URL تازه!) هم همان نسخهٔ قدیمی کش‌شده را برمی‌گرداند. الگوی زنده دقیقاً با این فرضیه سازگار است: HTML تازه، JSهای تغییرکردهٔ دلتا قدیمی.
2. **FTP-Deploy ناقص:** state فایل `.ftp-deploy-sync-state-staging.json` ممکن است آپلود بعضی فایل‌ها را «انجام‌شده» فرض کرده باشد یا انتقال وسط راه قطع شده باشد (الگوی الفبایی: index.html آپلود شده، بقیه نه). workflow قبلاً هم یک‌بار فایل PHP ناقص روی استیجینگ گذاشته بود (RCA ۲۰۲۶-۰۸-۲۵).

### رفع فوری (پیشنهاد)
1. دیپلوی دوبارهٔ دستی برنچ (`workflow_dispatch`) + **پاک‌سازی کش LiteSpeed/CDN استیجینگ**.
2. برای استیجینگ، هدر کش JS را خاموش کنید (staging فقط `.htaccess` مخصوص با `Cache-Control: no-cache` برای js/html).
3. **گام post-deploy smoke در workflow:** بعد از FTP، چند فایل کلیدی (`sw.js`، `leads.js`، `sales-domain-v2.js`، `api/sales-domain.php`) را از استیجینگ بگیرید و SHA-1 (یا git hash-object) را با همان فایل کامیت مقایسه کنید؛ نابرابر = شکست دیپلوی با پیام واضح. این چک ساده همین امروز این اتفاق را می‌گرفت.

---

## ۳) یافتهٔ ۲ — باگ امنیتی تأییدشده: نشت PII کاربران بدون احراز هویت (روی پروداکشن هم هست!) 🔴

بررسی زنده، بدون هیچ توکن/کوکی:

- `GET staging.pishtaj.ir/api/crm.php?action=users_get` → موبایل و ایمیل کاربران را برمی‌گرداند.
- `GET pishtaj.ir/api/crm.php?action=users_get` → **۴ کاربر کامل با موبایل/ایمیل شخصی** (از جمله رییس هیات‌مدیره).

**ریشه در کد** (نه فقط استیجینگ؛ در مین هم هست): در `api/crm.php` خط ۲۹۵ `$client_role = 'anonymous';` مقداردهی اولیه می‌شود و بعد در `users_get` (خط ~2230) فیلتر D-02 چک می‌کند: `$authenticated = !empty($client_role);` — چون `'anonymous'` رشتهٔ غیرخالی است، این شرط **همیشه true** است و گارد «حداقل‌سازی فیلدها پیش از لاگین» (v34.0.6-alpha D-02) عملاً کد مرج است. تسترهای موجود هم این را نگرفته‌اند (مسیر واقعی بدون توکن را تست نمی‌کنند).

**رفع پیشنهادی (یک خط):** `$authenticated = !empty($client_role) && $client_role !== 'anonymous';`
+ یک تستر رگرسیون که واقعاً بدون هدر توکن صدا می‌زند و نبود کلیدهای `mobile/email` را ادعا می‌کند. اولویت: همین هفته، روی مین (چون پروداکشن را ترمیم می‌کند)، مستقل از استیجینگ.

---

## ۴) یافته‌های کیفیت در کد دلتا (v34.8.13-15) — برای مرج بعدی

### 4-1. Sanitizer سرور داده را «بی‌صدا» می‌بُرد و کلاینت همان را جایگزین کل مجموعه می‌کند 🟠
`sd_entity_sanitize_row`: فیلد با مقدار `null` کلاً حذف می‌شود (یادآورها `link: null` می‌سازند!)، رشته‌ها به ۲۰۰۰ کاراکتر بریده می‌شوند، لیست‌ها سقف ۶۰ آیتم و `hist` هر آیتم سقف ۵۰۰ کاراکتر دارند. کلاینت بعد از ACK، projection برگشتی را با `ptfBApplyServerProjection` **جایگزین کل کلید محلی** می‌کند. یعنی اولین ذخیره/ویرایش از مسیر فرمان، هر دادهٔ غنی‌تر از سقف‌ها را برای همیشه کوتاه می‌کند (یادداشت بلند سرنخ، تاریخچهٔ پر). پیشنهاد: عبور بدهی ساختارهای شناخته‌شده با سقف‌های بزرگ‌تر + هشدار/خطای صریح به‌جای برش خاموش، و شمارش «فیلد حذف‌شده» در پاسخ فرمان.

### 4-2. پیام خطای گمراه‌کننده در مسیر فرمان 🟡
`cb` فقط `state !== 'acked'` را می‌بیند: خطای ۴۰۳ نقش (مثلاً accountant/collector برای `ptf_crm_leads` که در رجیستری سرور مجاز نیستند) هم پیام «ذخیرهٔ سرنخ روی سرور قطعی نشد؛ **دوباره تلاش کنید**» می‌گیرد — در حالی که تلاش دوباره فایده ندارد و باید «دسترسی ندارید» بگوید. ضمناً مسیر فرمان از گیت سمت‌کلاینت `ptfSyncCanWriteKey` عبور می‌کند (کَش رفتار نقش فقط سمت سرور است — قابل قبول، ولی پیام باید دقیق باشد).

### 4-3. دو رجیستری موازی که باید با هم هم‌راستا بمانند 🟡
`PTF_ENTITY_CMD_ENABLED` (کلاینت) و `sd_entity_registry()` (سرور) دو فهرست دستی‌اند؛ نقش‌هایشان با `SYNC_ROLE_KEYS` هم یکی نیست. اگر یکی جلو بزند: کلاینت فرمان می‌فرستد و سرور `entity_collection_not_enabled` می‌دهد (دقیقاً سناریوی «ناموفق» فعلی که در بن‌بست توضیح RCA-of-RCA هم دیدیم). پیشنهاد: قاعدهٔ A10 در arch-guard — تطابق دو لیست را از سورس استخراج و مقایسه کند.

### 4-4. ناوگانی مختلط در دورهٔ گذار 🟡
تا وقتی مین در v34.8.12 است و دستگاه‌های پروداکشن push توده‌ای قدیمی (`data_push`) را برای `leads/reminders` انجام می‌دهند، در حالی که سرورِ به‌روزشده فرمان رکوردی هم می‌پذیرد، مسیر merge تعارض این دو باید برای این دو کلید هم پوشش داده شود (کار v34.8.11 «protected conflict» را برای این دو کلید تست کنید).

### 4-5. موارد درست (برای ثبت)
- فیکس v34.8.15 (پذیرش پاسخ آرایه‌ای در `entityApplyProjection`) درست و لازم بود — علامت کارفرما («صف همگام‌سازی بعد از هر ثبت، هر چند ثانیه تکرار می‌شد») را بسته است.
- WAL + idempotencyKey + tombstone آرشیو در مسیر حذف، از الگوی بالغ فروش استفاده می‌کند ✓
- بنر/داشبورد/قفل نسخه (sw/manifest/shell) در سورس هماهنگ است ✓ — مشکل فقط در لایهٔ استقرار است.

---

## ۵) ریشهٔ ساختاری: چرا استیجینگ همیشه «چند نسخه جلوتر و لرزان» است؟

1. **هر push به هر برنچ `arena/**` مستقیماً روی استیجینگ دیپلوی می‌شود** (الان ۴۳ برنچ arena روی ریموت هست). یعنی استیجینگ = «آخرین کار نیمه‌کارهٔ هر ایجنت»، نه «نامزد انتشار بررسی‌شده».
2. تنها گیتِ قبل از FTP، `php -l` است. گیت JS (run-ci-gate ۱۳۴ تستر) و arch-guard فقط به‌صورت اختیاری/محلی توسط ایجنت‌ها اجرا می‌شوند — در workflow نیستند.
3. **هیچ verification بعد از دیپلوی وجود ندارد** — هیچ‌کس چک نمی‌کند فایل زنده == فایل کامیت. (یافتهٔ ۲ همین گزارش نتیجهٔ مستقیم همین خلأ است.)
4. چرخهٔ «استیجینگ جلو، مین عقب» تکرار می‌شود: مرور ۰۸-۲۵ گفت ۷ کامیت F1-F7 مرج نشده؛ الان v34.8.9-12 مرج شده ولی v34.8.13-15 دوباره مرج نشده و روی استیجینگ «تست» شده (به‌صورت مخلوط!). نسخه‌های تست‌نشدهٔ واقعی روی محیط مشترک، منبع گزارش‌های متناقض کارفرماست.

### پیشنهادهای ساختاری (خلاصه)
- دیپلوی استیجینگ فقط از یک برنچ ناکام‌شدهٔ مشخص (مثلاً `staging/**` یا بعد از مرج PRها) + درگاه `workflow_dispatch` برای تست موردی.
- گیت قبل از FTP: `run-ci-gate.js` + arch-guard (همان که برای پروداکشن لازم است، اینجا هم کافی است).
- گیت بعد از FTP: مقایسهٔ هش ۴-۵ فایل کلیدی زنده با کامیت (بند ۲).
- قانون انتشار: هیچ نسخه‌ای روی استیجینگ بیش از X روز بدون PR/مرج نماند (الگوی فعلی RCAها همین را می‌گوید).

---

## ۶) جدول اقدامات پیشنهادی (به ترتیب اجرا)

| # | اقدام | محیط | حجم | اثر |
|---|---|---|---|---|
| 1 | فیکس یک‌خطی users_get (`!== 'anonymous'`) + تستر بدون-توکن | **مین → پروداکشن** | خیلی کوچک | بستن نشت PII زنده |
| 2 | پاک‌سازی کش سرور + redeploy برنچ 01a03ab4 | استیجینگ | کوچک | پایان نسخهٔ مخلوط |
| 3 | post-deploy hash-check در `deploy-staging.yml` (+ پروداکشن) | CI | کوچک | جلوگیری دائمی از «دیپلوی ناقص» |
| 4 | no-cache کردن js/html فقط روی استیجینگ | هاست | کوچک | تست قابل‌اتکا |
| 5 | run-ci-gate + arch-guard به‌عنوان گیت قبل از FTP | CI | متوسط | توقف کد شکسته قبل از استقرار |
| 6 | برش خاموش sanitizer → سقف بزرگ‌تر + خطای صریح + شمارش حذفیات در پاسخ | استیجینگ | متوسط | جلوگیری از افت دیتا در مسیر فرمان |
| 7 | پیام دقیق خطا در cb فرمان (تفکیک permission از network) + قاعدهٔ A10 تطابق دو رجیستری | استیجینگ | کوچک | UX + پیشگیری |
| 8 | مرج v34.8.13-15 به مین پس از تأیید زندهٔ واقعی (post-fix بند 2) | مین | فرآیندی | کوتاه‌کردن فاصلهٔ استیجینگ/مین |

---

## ضمیمه — شواهد زنده (خلاصه)
- `staging.pishtaj.ir/crm/` → داشبورد: «نسخه فعال: v34.8.15»، بنر: برنچ `arena/01a03ab4-pishtaj-ir` ✓
- `staging.pishtaj.ir/crm/sw.js` → `CACHE = 'ptf-crm-v34.8.12'` ❌
- `staging.pishtaj.ir/crm/leads.js?v=34.8.15` → `saveLead` با `setData` قدیمی، بدون `ptfEntityUpsert` ❌
- `staging.pishtaj.ir/crm/sales-domain-v2.js?v=34.8.15` → بدون بلاک `PTF_ENTITY_CMD_ENABLED` ❌
- `staging.pishtaj.ir/api/crm.php?action=users_get` → PII بدون توکن ❌ (و `pishtaj.ir` همان با ۴ کاربر ❌❌)
- `staging.pishtaj.ir/api/crm.php?action=sync_stats` → `needLogin` ✓ (گارد اکشن‌های حساس سالم است)
- `VERSION.json` روی هر دو محیط 403 → عمدی است (`.htaccess` خط 10 همهٔ *.json جز manifest را می‌بندد) — بی‌خطر.
