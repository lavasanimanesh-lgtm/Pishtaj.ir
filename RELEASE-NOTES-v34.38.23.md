# CRM v34.38.23 — LETTER-SIG-PAGINATION

**تاریخ:** 2026-09-16
**بازوی تغییر:** Arena Agent
**کانال:** CRM (مکاتبات — نامهٔ صادره / چاپ و PDF)

> این فایل همان «یادداشت انتشار نسخهٔ جاری» است که گیت CI (تستر۵۷۸، سطر ۷۹) وجودش را
> با کلید `VERSION.json → crm_version` کنترل می‌کند.

## ۱) درخواست کارفرما

> «در قسمت مکاتبات ثبت نامهٔ صادره، مهر امضا به صفحهٔ بعد منتقل می‌شود و گاهاً یک
> صفحهٔ خالی هم بین متن و امضا وجود دارد. بررسی و اصلاح کن.»

## ۲) ریشه‌ها (سه نقص مستقل در `letPrintObj` — چاپ نامهٔ صادره/وارده)

1. **بلوک امضا شکستنی بود.** `.sig`/`.sigbox` هیچ `break-inside:avoid` نداشتند؛ وقتی
   انتهای متن به کف صفحه می‌رسید، بلوک بین دو صفحه نصف می‌شد: «با تجدید احترام + نام +
   سمت» در صفحهٔ قبل می‌ماند و **مهر** (`img.st` با `position:absolute` نسبت به sigboxِ
   شکسته) و **تصویر امضا** به صفحهٔ بعد می‌افتاد. (مسیر «متن آماده روی سربرگ» از
   v34.7.60 همین قرارداد را با `.endsig{break-inside:avoid}` داشت — مسیر استاندارد نداشت.)
2. **جدول بلندتر از صفحه ⇒ صفحهٔ خالی.** `.body table{page-break-inside:avoid}` جدولی
   که در باقی‌ماندهٔ صفحه جا نمی‌شد (و به‌ویژه جدولی که از یک صفحه بلندتر است) را به
   صفحهٔ بعد هل می‌داد و در کروم می‌توانست **یک صفحهٔ کاملاً سفید** بین متن و ادامهٔ
   نامه/امضا بسازد (باگ شناخته‌شدهٔ avoid-break برای جعبهٔ بزرگ‌تر از صفحه). چاپ
   پیشنهادها (offers-pro) سال‌هاست الگوی درست را دارد: جدول بین سطرها شکستنی، سطر
   اتمیک، سرستون در هر صفحه تکرار.
3. **دمِ نامرئیِ کپی از Word.** متن نامه که از Word/Outlook کپی می‌شود معمولاً چند
   پاراگراف خالی انتهایی (`<p><br></p>`، `<p>&nbsp;</p>`، `<p><span>&nbsp;</span></p>`)
   و `<br>` اضافه دارد. در ادیتور دیده نمی‌شوند ولی در چاپ هر کدام ~۱۰mm ارتفاع
   می‌گیرند (line-height 2.1) — امضا را با اینکه «جا هست» به صفحهٔ بعد می‌رانند و اگر
   زیاد باشند، **یک صفحهٔ کاملاً سفید بین متن و امضا** می‌سازند.

## ۳) اصلاح

| # | تغییر | اثر |
|---|---|---|
| ۱ | `.sig` و `.sigbox` در `letPrintObj`: `break-inside:avoid;page-break-inside:avoid` | بلوک امضا (تعارف/نام/سمت/تصویر امضا/مهر/محل امضای فیزیکی) هرگز نصف نمی‌شود؛ اگر جا نشود **یکجا و کامل** به صفحهٔ بعد می‌رود |
| ۲ | `.body table` در **هر دو مسیر چاپ** (نامهٔ استاندارد + متن آماده روی سربرگ): `page-break-inside:auto` + `.body tr{page-break-inside:avoid}` + `.body thead{display:table-header-group}` | جدول بلند بین سطرها تقسیم می‌شود؛ صفحهٔ خالی ساخته نمی‌شود؛ سرستون در هر صفحه تکرار می‌شود — دقیقاً الگوی خانه در `offers-pro` |
| ۳ | تابع جدید `letTrimTrailingEmptyHtml` + `printBody` در `letPrintObj`: پیرایش **فقط انتهای** بدنه در **زمان چاپ** (پاراگراف/‌<br>/nbsp‌های خالیِ انتهایی، تو در تو هم) | فضای نامرئیِ انتهای متن دیگر امضا را به صفحهٔ بعد نمی‌راند؛ فاصله‌های عمدیِ **میانی** متن و هر محتوای مرئی (جدول/تصویر/hr) دست‌نخورده می‌ماند |

**دست‌نخورده (مهم):** مسیر «📄 متن آماده روی سربرگ» به قرارداد «متن فقط sanitize
می‌شود، نه بازنویسی» (تستر۴۶۳) وفادار ماند — پیرایشِ انتها فقط در مسیر نامهٔ استاندارد
اعمال می‌شود. `.content{…min-height:170mm}`، `.limgs`، `.endsig`، ترفند thead/tfoot
صفحه‌بندی سربرگ، `letSafeBodyHtml`/`letSafeStyle` و همهٔ قراردادهای فونت/چینش
(تسترهای ۱۹۷/۳۱۷/۴۶۳/۴۶۴/۴۶۵/۴۶۷/۵۰۲) بدون تغییرند.

## ۴) تست و تأیید

- **تستر۶۵۸** (`tester658-v34.38.23-letter-sig-pagination.js`، ثبت‌شده در گیت):
  `letPrint` واقعی در vm (الگوی هارنس تستر۵۰۲) + واحدِ تابع پیرایش — ۲۱ چک:
  بدنهٔ با دمِ خالی ⇒ چاپِ پیرایش‌شده؛ فاصلهٔ میانی ⇒ حفظ؛ متن ساده با `\n\n\n` ⇒
  پیرایش؛ جدول/تصویر/hr انتهاییِ معنادار ⇒ حفظ؛ CSS اتمیک‌بودن `.sig` و الگوی جدول
  در هر دو مسیر. **روی کد قبل از اصلاح: ۱۰ FAIL + خروجی سناریو دقیقاً دمِ خالیِ
  چاپ‌شده را نشان می‌داد** — روی اصلاح‌شده: ۲۱ PASS / 0 FAIL.
- تسترهای موجود مکاتبات سبز ماندند: ۴۶۳ (۳۳/۰)، ۴۶۴ (۳۲/۰)، ۴۶۵ (۱۰/۰)، ۴۶۷ (۳۰/۰)،
  ۵۰۲ (۵۰/۰). (تسترهای legacy ۱۹۷/۳۱۷ که در گیت ثبت نیستند، قبل و بعد از این تغییر
  رفتار یکسان دارند.)
- گیت کامل CI: **269 PASS / 2 FAIL** — هر دو FAIL عیناً بیس‌لاینِ پیش‌موجود
  (tester427 کارکردی، tester656 قالب خروجی) و نامرتبط با مکاتبات.

## ۵) سازگاری و نسخه

- تمام نقاط رسمی نسخه → v34.38.23 (VERSION.json، manifest، sw.js، index.html و
  کوئری‌های `?v=`، `SD_SERVICE_VERSION` در sales-domain.php، clear-cache.html،
  shell.js، cms.js، device-reconnect.html) + پین‌های تسترها با
  `_tools/uat/bump-version-pins.js 34.38.22`. همزمانی را arch-guard (A6) کنترل می‌کند.
- هیچ مهاجرت داده‌ای لازم نیست — تغییر فقط در رندر چاپ است؛ رکوردهای نامه
  (`ptf_crm_letters`) دست‌نخورده‌اند و متن ذخیره‌شدهٔ نامه‌ها عوض نمی‌شود (پیرایش فقط
  روی خروجی چاپ اعمال می‌شود، نه روی داده).
- RCA کامل: `ARENA-LETTER-SIG-PAGINATION-RCA-2026-09-16.md`.
