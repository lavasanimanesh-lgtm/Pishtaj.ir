# یادداشت‌های انتشار — v34.8.45 (R6-الف)

**موضوع:** ابزارهای ریکاوری IDB-aware (T7) + سقف نرخ فرمان‌های نوشتاری per-user.
**تاریخ:** ۲۰۲۶-۰۸-۲۹ · **تستر قفل‌کننده:** `tester546-v34.8.45-r6a-idb-tools-rate-limit.js` (۴۰ سنجه · سبز) · **گیت:** run-ci-gate + arch-guard

## چه چیزی عوض شد

### ۱) چهار ابزار ریکاوری، IDB-aware شدند (تاکنون: صفر ارجاع IndexedDB)
از v33.20 دادهٔ سنگین در IndexedDB (`ptf-crm-storage-v1`) آینه می‌شود، از v34.8.39 پیش‌نویس‌ها (devkv:) و از v34.8.41 کش‌ها (cache:) هم آنجا هستند — اما ابزارهای ریکاوری مستقل فقط localStorage را می‌دیدند.

| ابزار | ارتقا |
|---|---|
| `clear-cache.html` | آمار IDB + دکمهٔ **پاکسازی امن کش‌های منقضی IDB** — فقط ردیف‌های `cache:` منقضی > TTL+۷ روز (همان قرارداد `ptfCacheSweep`)؛ `bdata:`/`devkv:` هرگز لمس نمی‌شوند |
| `force-restore.html` | **سازگار با نشست v34.8.43**: توکن از sessionStorage (fallback LS مهاجرت‌نشده)؛ نبود توکن JS ≠ نبود نشست — کوکی HttpOnly خودش احراز می‌کند و هدر خالی ارسال نمی‌شود؛ گیت عملیات = `hasSession`؛ جدول وضعیت IDB |
| `recover.html` | پنل وضعیت IDB کنار اسکن LS (آینهٔ سنگین دیده می‌شود) |
| `sync-diagnostics.html` | کارت وضعیت IDB در «وضعیت کلی» |

- آمارکنندهٔ استاندالون `ptfIdbStats` (بدون وابستگی به crm/*.js): تعداد ردیف‌ها به تفکیک `bdata:`/`cache:`/`devkv:` + حجم تقریبی.

### ۲) سرور — سقف نرخ فرمان‌های نوشتاری (api/sales-domain.php)
- **۶۰ فرمان در ۶۰ ثانیه per-user** (پنجرهٔ لغزان؛ شناسه = user توکن) — هدف: کشف زودهنگام حلقهٔ خراب کلاینت/اسکریپت؛ کاربر انسانی به این سقف نمی‌رسد.
- رد = **429** با `retryAfter` + `limit/window`؛ چک **قبل از قفل اصلی** (ردِ ارزان)؛ فقط مسیر نوشتن — اکشن‌های readOnly و replayهای idempotent دست‌نخورده.
- شمارنده best-effort (شکست نوشتن فایل، درخواست را نمی‌شکند) + هرس دوره‌ای کاربران بی‌فعالیت.

## گیت‌ها
- `run-ci-gate`: ۱۶۲ تستر (tester546 جدید) — سبز؛ arch-guard بدون تخلف جدید.
- تغییرناپذیرها: منطق idempotency/WAL دست‌نخورده؛ backup.js؛ پروداکشن v34.8.12.

## تست دستی پیشنهادی روی استیجینگ
1. `crm/clear-cache.html` → بخش «کش‌های IndexedDB» شمارش ردیف‌ها را نشان می‌دهد؛ دکمهٔ پاکسازی فقط کش منقضی را حذف می‌کند و گزارش می‌دهد.
2. `crm/force-restore.html` در تب جدید (بدون توکن JS) → وضعیت «✅ نشست سروری (کوکی HttpOnly)» و pull/backupها بدون خطای 401 کار می‌کنند.
3. (اختیاری) بیش از ۶۰ فرمان در یک دقیقه → پاسخ 429 با `retryAfter`.

## قدم بعدی
**R6-ب (v34.8.46):** 2FA پیامکی نقش‌های مالی + bulk-revoke نشست‌ها (ادمین) → سپس **v34.9.0** (چک‌لیست دیپلوی پروداکشن + بستن باقی‌ماندهٔ A10).
