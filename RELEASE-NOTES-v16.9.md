# 🚀 ریلیز v16.9 — اسپرینت «تجربه ورود»: BUG-020 + BUG-021 + تشخیص سنا
**تاریخ:** ۱۴۰۵/۰۴/۲۳ | **نسخه قبلی:** v16.8 | **بسته:** `pishtaj-release-v16.9.zip` (تجمیعی از v14.5)

طبق ابلاغ شما («به ترتیب اولویت نهایی‌شدن و کاربردی‌شدن»)، اسپرینت «تجربه ورود» انتخاب شد — سه مورد هم‌خانواده که هر روز در اولین لحظه استفاده دیده می‌شوند (قاعده ۱-۲ استوری رعایت شد: دو باگ + یک تشخیص، همگی یک خانواده).

---

## 🐞 BUG-020 — پرش تم هنگام رفرش (رفع ریشه‌ای)

**ریشه:** بلوک ضدپرش قبلی در `<head>` فقط متغیرهای رنگ را ست می‌کرد؛ اما کلاس حالت شب (`ptf-dark`) که عمده استایل شب به آن زنجیر است، دیرتر (پس از لود کامل صفحه + تاخیر ۱۰۰ms) به body اضافه می‌شد → یک لحظه تم قبلی/روشن دیده می‌شد.

**رفع:** یک اسکریپت سبک در **همان اولین خط body** کلاس تم را همان لحظه می‌گذارد (شامل حالت «خودکار/هماهنگ با سیستم» US-354) → **صفر فریم تم غلط** در موبایل و دسکتاپ. سوییچ دستی روز/شب و حالت خودکار عینا مثل قبل.

## 🐞 BUG-021 — داشبورد از همان ابتدا کامل: «روز من» + نرخ ارز

**ریشه:** پس از ورود/رفرش، داشبورد فورا رندر می‌شد در حالی که ویجت‌ها (نوار ارز، روز من، کاشی‌های لانچر، قیف فروش) با تاخیرهای جداگانه به داشبورد وصل می‌شوند → رندر اول فقط «آمار خام» (تعداد مشتریان/درخواست‌ها) را نشان می‌داد و تا ورود بعدی به داشبورد ناقص می‌ماند — دقیقا گزارش شما.

**رفع:** ناظر سبک — به‌محض اتصال کامل زنجیره ویجت‌ها، اگر داشبورد هنوز ناقص است **یک‌بار خودکار تازه می‌شود**، با سه گارد ایمنی: فقط وقتی پنل فعال داشبورد است + ویجتی واقعا غایب است + کاربر هنوز هیچ تعاملی نکرده (هیچ پرش مزاحمی وسط کار). حالا از همان لحظه اول: **💱 نرخ لحظه‌ای ارز (بالاترین نقطه) → ☀️ روز من → کاشی‌ها → قیف**.

## 🔬 تشخیص سنا — پایان ابهام «حواله سنا نمایش داده نمی‌شود»

کد نرخ‌های سنا از v16.7 سالم و تست‌شده است؛ مشکل سمت منبع/هاست است. برای اینکه دیگر حدس نزنیم:
- اگر نرخ آزاد برسد ولی سنا صفر باشد، نوار به‌جای حذف بی‌صدا، برچسب می‌زند: **«سنا: منبع پاسخ نداد»**.
- کنار آن برای ادمین/رییس هیات مدیره دکمه **«🔬 تست منبع»**: پاسخ خام لحظه‌ای سرور نرخ (بدون کش) را نشان می‌دهد — اگر کلیدهای sana در پاسخ صفر بودند، قطعی می‌شود که **هاست به tgju.org دسترسی خروجی ندارد** و باید از پشتیبانی هاست باز شدن outbound به `*.tgju.org` خواسته شود.

📌 **لطفا پس از آپدیت:** یک‌بار نوار ارز را ببینید؛ اگر برچسب سنا ظاهر شد، «🔬 تست منبع» را بزنید و نتیجه را اعلام کنید — با همان یک اسکرین‌شات تکلیف نهایی روشن می‌شود.

## 🛡 اثبات کیفیت

| اصل | اثبات |
|-----|-------|
| بدون وصله‌پینه | رفع تم در ریشه (لحظه ساخت body) نه پچ‌های دیرهنگام؛ ناظر داشبورد از همان `goPanel` موجود استفاده می‌کند (نه رندر موازی)؛ تشخیص سنا داخل همان ویجت fx. |
| وابستگی‌ها | زنجیره hook داشبورد (launcher→leads→insights→fx→myday — TD-007) دست‌نخورده؛ perms.js (سوییچ تم) دست‌نخورده؛ چاپ (همیشه روشن) بی‌اثر. |
| بدون باگ جدید | tester87: **۲۶ چک** + **jsdom: ۸ چک رفتاری** (کلاس تم قبل از DOMContentLoaded با تم dark/light واقعی؛ ناظر داشبورد: دقیقا یک re-render پس از اتصال hookها، صفر re-render وقتی کاربر تعامل کرده). رگرسیون کامل: **۷۴ تستر / ۲۱۴۰ چک — صفر خطا**. audit PASS. |
| چابکی/سهولت | هر سه مورد «اولین تجربه روزانه» کاربر را روان می‌کنند؛ هیچ رفتار جدیدی برای یادگیری ندارند. |

## 📋 راهنمای به‌روزرسانی
1. `pishtaj-release-v16.9.zip` روی هاست روی موجود اکسترکت شود — پوشه `api` حذف نشود.
2. همه کاربران رفرش کنند — نسخه سایدبار **v16.9**.
3. تست سریع: رفرش با حالت شب فعال → هیچ لحظه‌ای تم روشن دیده نشود؛ ورود مجدد → داشبورد از ابتدا با نوار ارز و «روز من».
4. اگر برچسب «سنا: منبع پاسخ نداد» دیدید → دکمه 🔬 تست منبع (ادمین) → نتیجه را اعلام فرمایید.

## ⏭ صف بعدی (به ترتیب اولویت کاربردی‌شدن — پیشنهاد پنل)
① **BUG-019 + US-409** (فرم/چاپ پیشنهاد — کوچک، گزارش فعال کاربر) → ② US-404 فاز ۲ (انتقال خودکار اسناد به پرونده) → ③ US-410 (چک‌ها) → ④ US-411 فاز ۱ (ثبت‌کننده/مالکیت) → ⑤ US-405…408 (اسناد بازرگانی) → ⑥ US-402/403 (R5).
