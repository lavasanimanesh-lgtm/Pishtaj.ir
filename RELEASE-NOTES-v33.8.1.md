# یادداشت‌های انتشار (Release Notes — Version 33.8.1)

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (PR #11 باز تا پایان مهاجرت MySQL)
**تمرکز ریلیز:** حالت گرافیکی چیدمان چک در **صفحهٔ اصلی ماژول** با اندازهٔ واقعی + پس‌زمینهٔ اسکن برگه چک + پرسش «وارده/صادره» هنگام ثبت + **رفع باگ «پرونده باز مشتری دیده نمی‌شد»** + **ثبت و خرج چک ثالث**.

---

## 🚀 دستاوردهای کلیدی نسخه ۳۳.۸.۱

### ۱. حالت گرافیکی چیدمان — در صفحهٔ اصلی ماژول (بدون مودال)
- **سکشن «🖱 حالت گرافیکی چیدمان (روی برگهٔ واقعی چک)»** بالای صفحهٔ «چاپ چک فیزیکی» قرار گرفت — دیگر نیازی به باز کردن پنجرهٔ مودال نیست.
- **اندازه ≈ چک صیادی واقعی:** مقیاس ۶ پیکسل بر میلی‌متر → 169mm ≈ ۱۰۱۴px (با دکمه‌های ➖/➕ زوم ۰.۵ تا ۲ برابر برای صفحه‌های کوچک).
- هر نوشته (تاریخ/ذی‌نفع/کد ملی/مبلغ بالا/مبلغ پایین-چپ/مبلغ حروف/بابت) را با ماوس می‌کشید و رها می‌کنید؛ مختصات mm خودکار به‌روز و با «💾 ذخیره چیدمان» ذخیره می‌شود.

### ۲. اسکن برگهٔ چک واقعی به‌عنوان پس‌زمینه
- دکمهٔ «📎 اسکن برگه چک (پس‌زمینه)»: تصویر اسکن/عکس برگهٔ چک بانکی خودتان آپلود می‌شود، با canvas فشرده (عرض ≤۱۶۰۰، JPEG) و در localStorage ذخیره و **در پس‌زمینهٔ برگهٔ گرافیکی** قرار می‌گیرد.
- سپس هر فیلد را دقیقاً روی جای صحیح چک واقعی می‌کشید — دقیقاً همان کاری که خواستید («چک واقعی را اسکن کرد و در پس‌زمینه قرار داد و مقادیر را جانمایی کرد»).
- «✕ حذف پس‌زمینه» برای پاک کردن. مودال «تنظیمات چاپ» فقط مقادیر عددی را نگه داشت (جلوگیری از تداخل).

### ۳. ثبت چک: اول می‌پرسد «وارده است یا صادره»
- با زدن «+ چک جدید»، پنجرهٔ انتخاب باز می‌شود: **«📥 چک وارده — دریافت از مشتری/ثالث»** یا **«🏢 چک صادره — پرداخت/ضمانت شرکت»** و سپس فیلدها بر همان اساس ساخته می‌شوند.
- **دستیار هوشمند** هم سِلکت «نوع چک: وارده/صادره» در بالای فرم دارد — تغییر نوع، فرم را با حفظ اطلاعات استخراج‌شده بازسازی می‌کند.

### ۴. رفع باگ «برای چک مشتری هیچ پرونده بازی نشان نمی‌داد»
**ریشه:** رکوردهای پرونده فروش (`ptf_crm_deals`) فقط فیلد `buyerCo` (نام) دارند و `buyerCd` ندارند؛ اما فهرست مشتریان با `d.buyerCd` فیلتر می‌شد → همیشه خالی.
**رفع:** تطبیق دومرحله‌ای — اگر `buyerCd` بود با کد، وگرنه **تطبیق نام** `buyerCo` با نام مشتری (الگوی `ptfOfferMatchCust`) → حالا همهٔ مشتریان دارای پروندهٔ باز (برنده و غیربایگانی) در لیست می‌آیند.

### ۵. ثبت و خرج چک ثالث
- در ثبت چک وارده، نوع ذی‌نفع جدید: **«🪪 ثالث (چک شخص/شرکت دیگر)»** — نام ثالث وارد می‌شود و برچسب «🪪 ثالث» در جدول چک‌ها نمایش داده می‌شود (دستی + دستیار AI).
- **خرج کردن چک:** دکمهٔ «↪ انتقال» حالا پنجرهٔ «خرج کردن چک به تامین‌کننده» است با **لیست تامین‌کنندگان دارای مطالبه** + یادداشت؛ اگر چک مالی باشد، همان لحظه **payment در حساب تامین‌کننده** ساخته می‌شود (کسر بدهی ما) و چک endorsed می‌شود (`ptfChequeEndorse` با `supCd`).

---

## 📋 تغییرات فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/cheque-print.js` | سکشن گرافیکی inline در صفحهٔ ماژول + مقیاس واقعی (۶px/mm) + زوم + اسکن پس‌زمینه (chqBgUpload/Clear/Zoom) + انتقال گرافیک از مودال |
| `crm/cheque-module.js` | رفع باگ تطبیق نام پرونده باز مشتری + `ptfChequeEndorse` با supCd و اثر مالی خرج + دسته «ثالث» |
| `crm/cheque-panel.js` | انتخابگر «وارده/صادره» قبل از فرم ثبت + گزینه ثالث (دستی و AI) + سِلکت نوع در دستیار + پنجرهٔ «خرج کردن چک» با لیست تامین‌کننده + برچسب ثالث |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.8.0 |
| `_tools/uat/tester288-cheque-party-recurring.js` | ۳۵ تست (تطبیق نام / ثالث / خرج مالی / inline / اسکن) |
| `_tools/uat/tester287` | بامپ نسخه + inline |
| `RELEASE-NOTES-v33.8.1.md` / `REGRESSION-REPORT-v33.8.1.md` | اسناد انتشار |
| `_audit/HANDOFF-NEXT-SESSION-2026-08-01.md`، `crm/BACKLOG-USER-2026-08-01.md` | به‌روزرسانی |

---

## ⚠️ برای کاربر (روش اعمال)
1. `Ctrl+Shift+R` یا `crm/clear-cache.html` (نسخه باید **v33.8.0** باشد).
2. چاپ فیزیکی → بخش «🖱 حالت گرافیکی چیدمان» در همان صفحه: «📎 اسکن برگه چک (پس‌زمینه)» → فیلدها را بکشید → «💾 ذخیره چیدمان» → «🖨 چاپ آزمایشی».
3. ثبت چک → «+ چک جدید» → انتخاب «وارده/صادره» → ذی‌نفع (مشتری/ثالث/سایر).
4. چک وارده → «↪ خرج کردن چک به تامین‌کننده» → انتخاب تامین‌کننده → اثر مالی همان لحظه.

---

## 🔧 v33.8.1 — رفع باگ جهت درگ گرافیکی + جانمایی فیلدها (بازخورد کارفرما)

**بازخورد:** «درگ در جهت موس نیست؛ باید بری چپ تا نوشته بیاد راست و بالعکس. برخی مقادیر هم اصلا در کادر نیستند.»

**ریشه:** در `chqGvSync` موقعیت فیلدهای راست‌چسب (تاریخ/ذی‌نفع/کد ملی/بابت) با `(pageW - right) * scale` محاسبه می‌شد به‌جای `right * scale` → ① فیلدهای راست‌چسب به گوشهٔ چپ برگه پرتاب می‌شدند (روی هم و بعضاً خارج از کادر/زیر اسکرول) و ② چون مقدار right در حین درگ برعکس تفسیر می‌شد، جهت حرکت معکوس می‌شد.

**رفع (v33.8.1):**
- `chqGvSync`: فیلدهای راست‌چسب حالا `right: f.right * scale` (فاصله از لبهٔ راست — دقیقاً مثل چاپ واقعی).
- `chqGvMove`: جهت درگ هم‌جهت با موس (چپ‌چسب: left+dx؛ راست‌چسب: right−dx).
- `words` (مبلغ به حروف): در گرافیک عرضش بین left/right کشیده می‌شود و در درگ هر دو لبه با هم جابه‌جا می‌شوند (+ آپدیت input های wordsLeft/wordsRight).
- تستر tester288: +۳ تست جهت/جانمایی (۳۸/۰). رگرسیون = صفر شکست جدید.
