# RELEASE NOTES — v34.26.0 (ریشه‌کنی جریان محصول — بازخورد پروداکشن پس از دیپلوی)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** tester571 (۲۶ سنجه) · **گیت:** 187 PASS / 0 FAIL · **PHP-lint:** ✓
**تشخیص روی دادهٔ واقعی:** دو صفحهٔ محصول منتشرشدهٔ امروز مستقیماً از pishtaj.ir بررسی شد.

## پنج علامت → پنج ریشه

### ① عکس در پیش‌نمایش بود، در صفحهٔ منتشرشده نبود
قالب محصول تصویر را فقط به og:image/اسکیما می‌داد و **هیچ `<img>` مرئی در صفحه نمی‌ساخت**. اکنون تصویر بالای متن صفحه درج می‌شود (هم‌شکل پیش‌نمایش).

### ② پیش‌نمایش خوب، صفحهٔ منتشرشده ظاهر متفاوت
پیش‌نمایش قبلی تقریبیِ دست‌ساز بود. حالا **پیش‌نمایش از خود سرور** می‌آید (اکشن `product_preview` — همان کد رندرِ انتشار، بدون نوشتن) و در iframe با استایل واقعی سایت نمایش داده می‌شود + دکمهٔ 📱 موبایل / 🖥 دسکتاپ. آنچه می‌بینید همان است که منتشر می‌شود؛ نسخهٔ محلی به‌عنوان جایگزین آفلاین ماند.

### ③ عبارات اضافی بالای صفحه
صفحهٔ واقعی با یک خط تکراریِ نام محصول (برابر H1) شروع می‌شد. حالا پاراگراف/تیتر نخستِ عیناً برابر H1/عنوان در رندر حذف می‌شود + قاعدهٔ صریح به پرامپت: «متن را بدون خط تکراری نام شروع کن».

### ④ دوباره‌گویی + دامپ خام CRM
متن AI خودش جدول مشخصات و FAQ داشت و قالب دوباره از فیلدهای CRM جدول می‌ساخت — از جمله سطرِ «استاندارد» که متنِ کاملِ بلندِ RFQ-مانندِ رکورد را دمپ می‌کرد. رفع: اگر متن خودش جدول/FAQ دارد، جدول‌های قالب ساخته نمی‌شوند (اسکیمای FAQ هم فقط از FAQ رندرشده)؛ مقدار مشخصه به ۱۶۰ نویسه کپ؛ فرم کلاینت هم سطر استاندارد بلند (>۱۲۰) را پیش‌پر نمی‌کند.

### ⑤ محصول در «صفحهٔ محصولات» ساخته نمی‌شد
پوشهٔ products روی سایت **اصلاً صفحهٔ فهرستی نداشت** (403). حالا با هر انتشار، `products/index.html` بازسازی می‌شود: کارت هر صفحهٔ محصول (تصویر/عنوان/توضیح از متای همان صفحه)، جدیدترین اول، با هدر/فوتر همان قالب سایت و canonical.

### ⑥ محتوای «RFQ-مانند» — پرامپت راهنمای فنی
قابِ پرامپت (سرور و دستیار خارجی) به «**راهنمای فنی محصول**» تغییر کرد: معرفی → جدول مشخصات فنی → معیارهای انتخاب → نصب و نگهداری → اشتباهات رایج → مدارک قابل درخواست؛ با حذف صریح RFQ/کد پیگیری/شماره سفارش و پاک‌سازی این توکن‌ها از دادهٔ خام پیش از پرامپت (سرور: st/md کپ ۱۶۰، ds کپ ۴۰۰؛ کلاینت: cmsProdClean).

## فایل‌های کلیدی

`api/cms.php` (product_preview + PROD-TIDY/DEDUP/IMG-VIS + cms_products_index_rebuild) · `crm/cms.js` (پیش‌نمایش iframe + پاک‌سازی + قواعد پرامپت خارجی) · `api/llm.php` (TECHNICAL PRODUCT GUIDE) · `_tools/uat/tester571-v34.26.0-product-pipeline-fix.js`

**دیپلوی این نسخه لازم است** (api/cms.php + api/llm.php + crm/cms.js). برای صفحات محصولِ امروز: دوباره «انتشار» بزنید (بازنویسی با تیک overwrite) تا تصویر/تکرار/فهرست اصلاح شود.
