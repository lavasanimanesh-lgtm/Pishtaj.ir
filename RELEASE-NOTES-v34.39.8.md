# یادداشت انتشار v34.39.8 — HOME-MINBTN (حذفِ کلمه و کادر از کلید تم و زبان در دسکتاپ)

**تاریخ:** 2026-09-17
**کانال:** سایت عمومی — هدر دسکتاپ (فقط لایهٔ نمایشی؛ لینک‌ها، DOM‌منطق و محتوا دست‌نخورده)
**قرارداد:** tester669 (۱۵ سنجه) + بازتنظیم پین‌ها با ابزار رسمی `bump-version-pins.js`

## درخواست کارفرما
> «در حالت دسکتاپ کلمهٔ «نمای روز» و کادر دور آن حذف و فقط آیکون ماه و خورشید بماند. در خصوص زبان هم کلمه‌ای «EN» حذف و فقط آیکون پرچم مینیمال بماند و کادر دور آن هم حذف شود.»

## آنچه انجام شد
1. **کلید تم دسکتاپ:** قرصِ «ارتفاع ۳۴ + برچسب» به آیکونِ **۴۲px بدون قاب** تبدیل شد — `border:0!important`، پس‌زمینه و سایه شفاف، برچسب `.tt-txt` با `display:none` مخفی؛ آیکون خورشید/ماه همان‌جا در انتهای نوبار می‌درخشد (شب: `#cfe0f5`) و هاورِ کهرباییِ موجود (کهرباییِ ۱۲٪ از v34.39.2) بی‌کادر و نرم ماند. منطق JS (کلاس `ptf-dark`، کوکی، `memManual`، `aria-pressed`، `aria-label`) بدون تغییر است — فقط نمایش.
2. **سوئیچِ زبان:** اموجیِ 🇬🇧 (که روی ویندوز اصلاً رندر نمی‌شد) و متن «EN» و قرصِ طوسیِ `#e2e8f0` حذف؛ به‌جایش **پرچمِ SVGِ اینلاینِ مینیمال** (یونیون‌جک، ۲۱×۱۶، گوشهٔ ۴px) با `aria-label="English — نسخه انگلیسی"` و `href="en/"` عیناً حفظ‌شده (سیگنال hreflang بدون تغییر). هندلِرهای اینلاین `onmouseover/onmouseout` هم حذف و هاورِ ملایم (روز: تنِ شعله‌ای ۱۲٪ / شب: کهربایی ۱۳٪) به CSS منتقل شد.
3. **پاکسازیِ بودجه (قانونِ سختِ <۴۰K نویسه):** افزودنِ لایه، فایل را عبور می‌داد؛ به‌جای حذفِ قابلیت، **قاعدهٔ پایهٔ `.theme-toggle` از ۳۹.۱ به حداقلِ زنده تراکم یافت** (سایه/بدر/بک‌گراند/ابعادی که هر دو نسخه — شیت و دسکتاپ — بعداً override می‌کردند حذف)، قاعدهٔ مردهٔ `≤790px` نسخهٔ ۳۹.۱ و `justify-content` مردهٔ pill حذف شد → `home.css = ۳۹٬۹۷۱` با راستی‌آزماییِ **تک‌تکِ مارکرهای هشت لایه**.

## فایل‌های تغییر‌یافته
`index.html` (لینکِ زبان + cache-bust → v=34.39.8) · `assets/css/home.css` (لایهٔ v34.39.8 + فشرده‌سازیِ قوانینِ مرده) · `VERSION.json` و ۸ نقطهٔ رسمی نسخه · `RELEASE-NOTES-v34.39.8.md` · tester669.

## وضعیت دروازه
tester669: ۱۵/۱۵ ✓ · 662:۳۶ · 663:۳۰ · 664:۲۹ · 665:۲۵ · 666:۲۳ · 667:۲۱ · 668:۱۳ — همگی PASS · گیت کامل CI: **۲۸۱ PASS / ۱ FAIL** (شکستِ محیطیِ شناخته‌شدهٔ tester656) · arch-guard: PASS.
