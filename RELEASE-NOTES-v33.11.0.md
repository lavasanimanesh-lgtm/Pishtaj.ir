# یادداشت‌های انتشار (Release Notes — Version 33.11.0)

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (PR #11 باز تا پایان مهاجرت MySQL)
**تمرکز ریلیز:** ① حذف جدول/کارت‌های سود تعهدی قدیمی (اعداد ناسازگار) از داشبورد سال مالی — فقط منطق نقدی ② رفع به‌هم‌ریختگی هاب مالی: باکس «چک‌های در راه/جزئیات تامین‌کنندگان» فقط در تب خودش + نوار هاب بالای صفحه.

---

## 🚀 دستاوردهای کلیدی نسخه ۳۳.۱۱.۰

### ۱. حذف جدول تعهدی قدیمی از داشبورد سال مالی (بازخورد کارفرما)
- **حذف کامل** کارت‌های «سود/زیان پروژه‌های قابل اتکا»، «سود خالص مدیریتی پس از هزینه‌ها»، «اندوخته صندوق»، «مطالبات باز»، و **جدول «کاربرگ تقسیم سود» تعهدی** از داشبورد سال مالی — همان اعدادی که با منطق نقدی ناسازگار بودند.
- داشبورد حالا فقط شامل: **بلوک «💵 سود نقدی و تقسیم»** + هشدار موارد ناقص + سندهای اصلاحی + دکمه‌های قفل/گزارش/CSV/سند اصلاحی.
- **گزارش رسمی (PDF/چاپ) و CSV حسابدار هم نقدی شدند**: درآمد نقدی (وصولی‌ها)، چک وصول‌نشده، فاکتورهای خرید، پرداخت بدون تخصیص، چک صادرهٔ مستقل، هزینه جاری، تنخواه، سود نقدی، موجودی نقد پایان، کف نقدینگی، مازاد بر کف، قابل تقسیم، بازگشت به کف + کاربرگ تقسیم نقدی سهامداران.

### ۲. رفع به‌هم‌ریختگی هاب مالی (بازخورد کارفرما)
- **ریشه:** باکس «📊 تعهدات نقدینگی تأمین و چک‌های شرکت» (`slLiquidity`) در هیچ تبی مخفی نمی‌شد → «چک‌های در راه و جزئیات تامین‌کنندگان» در همهٔ تب‌ها دیده می‌شد. و باکس «هزینه‌های جاری» (`opexBox`) قبل از نوار هاب چیده می‌شد → «هاب مالی وسط صفحه».
- **رفع:**
  - `slLiquidity` حالا فقط در تب **«حساب تأمین‌کنندگان»** نمایش داده می‌شود.
  - تابع `finHubOrder`: ترتیب همهٔ باکس‌های هاب را بازمی‌چیند — **نوار هاب اول**، سپس باکس‌های تب‌ها (هزینه جاری → نقدینگی تامین → تنخواه → سهامداران → سال مالی → حساب‌ها → گزارش تجمیعی → تراز → کیفیت → چک‌ها).
  - `finHubApply` و `renderPetty` بعد از هر رندر، ترتیب را دوباره اعمال می‌کنند.

---

## 📋 تغییرات فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/fiscal.js` | حذف KPIهای تعهدی و کاربرگ قدیمی از داشبورد؛ گزارش رسمی/CSV → نقدی (`ptfFiscalCashReportHtml`) |
| `crm/financehub.js` | `slLiquidity` فقط در تب supacc + `finHubOrder` (نوار هاب اول) + فراخوانی در apply/render |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.11.0 |
| `_tools/uat/tester291-hub-clean.js` | **جدید** (۱۲/۰) |
| `RELEASE-NOTES-v33.11.0.md` / `REGRESSION-REPORT-v33.11.0.md` | اسناد انتشار |
| `_audit/HANDOFF-NEXT-SESSION-2026-08-01.md`، `crm/BACKLOG-USER-2026-08-01.md` | به‌روزرسانی |

---

## ⚠️ برای کاربر
1. `Ctrl+Shift+R` یا `crm/clear-cache.html` (نسخه باید **v33.11.0** باشد).
2. هاب مالی: نوار تب‌ها بالای صفحه است و هر باکس فقط در تب خودش دیده می‌شود (چک/نقدینگی تامین فقط در تب حساب تأمین‌کنندگان).
3. تب سال مالی: فقط بلوک «سود نقدی و تقسیم» — جدول تعهدی قدیمی حذف شد.
