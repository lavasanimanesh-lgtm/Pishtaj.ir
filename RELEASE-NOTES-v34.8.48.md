# یادداشت‌های انتشار — v34.8.48 (هات‌فیکس ۲)

**موضوع:** COOKIE-ONLY-MODE — ورود حتی وقتی مرورگر **هر دو** مخزن sessionStorage و localStorage را مسدود کرده است.
**تاریخ:** ۲۰۲۶-۰۸-۲۹ · **تستر قفل‌کننده:** `tester549-v34.8.48-cookie-only-mode.js` (۱۹ سنجه · سبز)

## زمینه
هات‌فیکس ۴۷ توفان `role_verify` را بست و پیام شفاف داد، ولی مرورگر واقعی کاربر هر دو مخزن را می‌بندد (وب‌ویو داخل پیام‌رسان/پنجرهٔ خصوصی/سهمیهٔ پر/تنظیم دادهٔ سایت) — یعنی حتی fallbackِ LS هم می‌انداخت و کاربر پیام «بازسازی نشست بی‌نتیجه بود…» می‌دید.

## fix (crm/rbac.js)
- **زنجیرهٔ کامل fallback برای نشست/توکن:** `sessionStorage → localStorage → حافظهٔ همین تب (PTF_SESS_MEM)`.
- در حالت فقط-کوکی: ورود با کوکی HttpOnly کامل می‌شود؛ تبِ تازه با همان کوکی و دقیقاً **یک** `role_verify` نشست را در حافظه بازمی‌سازد (قرارداد v34.8.43: «نبود توکن JS ≠ نبود نشست»).
- هشدار کاربر یک‌بارمصرف (toast) با توصیهٔ صریح: استفاده از مرورگر عادی.
- سقف ضدتوفانِ ۴۷ (حداکثر ۳ تلاش بازسازی) دست‌نخورده به‌عنوان آخرین سد باقی ماند.

## محدودیت شناخته‌شده (منتقل به کاربر)
در حالت فقط-کوکی، عمر نشست = عمر همان صفحه است و لایهٔ دادهٔ مرورگر (localStorage) هم در دسترس نیست — برای کار واقعی باید مرورگر عادی/مجاز استفاده شود. این حالت صرفاً «ورود و مشاهده» را نجات می‌دهد و گمراهی «توکن معتبر وجود ندارد» را حذف می‌کند.

## گیت‌ها
CI ۱۶۵ تستر (tester549 جدید) — سبز؛ arch PASS؛ هم‌نسخگی کامل v34.8.48.
