# CHQ-DOC-001 (v34.4.30) — رفع مشکل «عکس چک قابل مشاهده/حذف نیست»

**درخواست کارفرما (۱۴۰۵/۰۵/۱۹):**
«بعضی جاها مانند ثبت چک در بخش مالی می‌توان عکس چک را داد اما وقتی رجوع می‌کنیم دیگر جایی برای دیدنش وجود ندارد؛ در حساب تامین‌کنندگان هم چک شود.»

## نتیجهٔ بررسی
ادعا تایید شد و در واقع در چند نقطه (نه فقط یکی) صادق بود.

## ریشه‌یابی و اصلاحات

### ۱) پنل چک (بخش مالی → تب «چک‌ها») — `crm/cheque-panel.js`
- **قبل:** دکمه‌های ردیف چک صادره/وارده فقط شامل ✏️ویرایش، 🗑حذف، ✓وصول، ⛔ابطال بودند؛ تنها راه دیدن عکس، باز کردن فرم کامل ویرایش و اسکرول به پایین بود — چیزی که کاربر عادی پیدا نمی‌کرد.
- **اصلاح:** دکمهٔ مستقل «📎 سند» (با شمارندهٔ تعداد اسناد) به هر دو جدول اضافه شد که مودال سبک `ptfChequeFilesUi` را باز می‌کند: نمایش/مشاهدهٔ فایل‌های موجود + افزودن سند جدید + حذف — همه بدون نیاز به ورود به فرم ویرایش.
- **باگ جداگانه:** دکمهٔ «✕ حذف سند» داخل مودال ویرایش (`ptfChEditRemoveFile`) به `window._ptfChEditCd` وابسته بود که فقط داخل تابع «ذخیره» ست می‌شد، نه در لحظهٔ باز شدن مودال. یعنی اگر کاربر بلافاصله بعد از باز کردن ویرایش می‌خواست سندی را حذف کند، هیچ اتفاقی نمی‌افتاد. رفع شد با ست‌کردن این متغیر در همان ابتدای `ptfChequeEditUi`.
- یک نسخهٔ قدیمی/مرده از `ptfChequeEditUi`/`ptfChequeEditSave` (بدون پشتیبانی از سند) که همیشه توسط نسخهٔ جدیدتر بازنویسی می‌شد، حذف شد (کد مرده).

### ۲) حساب تأمین‌کنندگان — `crm/supplier-finance.js`
- **قبل:** فرم «ثبت پرداخت» با روش «چک شرکت» یا «چک ثالث منتقل‌شده» اصلاً فیلد آپلود عکس نداشت. اگر بعداً از دکمهٔ 📎 کنار پرداخت سندی اضافه می‌شد، **هیچ‌جا** (نه در جدول پرداخت‌ها، نه در ویرایش پرداخت) نمایش داده نمی‌شد — دقیقاً مصداق «می‌دهیم ولی جایی برای دیدنش نیست».
- **اصلاح:**
  - فیلد آپلود «📎 رسید / تصویر چک» به فرم ثبت پرداخت اضافه شد؛ فایل هم روی رکورد پرداخت و هم (در حالت چک) روی رکورد چکِ ساخته‌شده ذخیره می‌شود.
  - جدول «پرداخت‌ها و اعتبار» در گردش حساب تأمین‌کننده حالا ستون «فایل» با لینک مشاهده + دکمهٔ حذف دارد (مثل فاکتورهای خرید).
  - مودال «✏️ ویرایش پرداخت» حالا اسناد موجود را نشان می‌دهد و افزودن/حذف سند از همان‌جا ممکن است (هم‌تراز با ویرایش فاکتور خرید).

### ۳) حساب مشتریان — `crm/rbac.js` (برای هماهنگی)
- فرم «ثبت وصولی» با روش «چک» هم فیلد آپلود عکس چک نداشت؛ اضافه شد تا این چک وارده هم از لحظهٔ ثبت سند داشته باشد.

## تست
- **`tester178-v34.4.30-cheque-doc-view-fix.js`** (جدید، ۱۷ بررسی در ۳ بخش) — می‌سنجد:
  - مودال مستقل مشاهده/افزودن/حذف سند در ردیف چک به‌درستی کار می‌کند.
  - رفع باگ `_ptfChEditCd` (حذف سند بلافاصله بعد از باز کردن ویرایش، بدون نیاز به ذخیرهٔ قبلی).
  - وجود دقیقاً یک تعریف زندهٔ `ptfChequeEditUi` (حذف کد مرده).
  - مسیر پیوست عکس چک در فرم پرداخت تأمین‌کننده + نمایش/حذف آن در جدول و ویرایش پرداخت.
  - مسیر پیوست عکس چک در فرم وصولی مشتری.
- رگرسیون کامل ۲۴ تستر اجرا شد: ۷ شکست از قبل موجود بودند (روی حالت پایه/قبل از این تغییرات هم دقیقاً همان‌طور fail می‌شوند) و **هیچ ربطی به این تغییرات ندارند**؛ بقیه (از جمله تسترهای مرتبط با چک: ۱۶۶، ۱۶۷، ۱۷۳، ۱۷۴) سبزند.
- بامپ نسخه → **v34.4.30** (`VERSION.json` + `crm/index.html` cache-bust).

*تولیدشده توسط Arena Agent.*
