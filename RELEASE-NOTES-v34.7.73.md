# RELEASE NOTES — v34.7.73 (۱۴۰۵/۰۵/۳۰ — 2026-08-21)

## رفع باگ ورود کاربر جدید از موبایل + دکمهٔ واقعی «همگام‌سازی کاربران»

### مشکل (گزارش کاربر)

کاربرِ تازه‌تعریف‌شده از **دسکتاپ** وارد می‌شد اما از **موبایل** نه؛ پیام خطای موبایل می‌گفت
«از دسکتاپ یک‌بار همگام‌سازی کاربران را اجرا کنید» در حالی که چنین عملی در UI وجود نداشت.

### ریشهٔ علت

| یافته | توضیح |
|---|---|
| کاربر فقط در `localStorage` مرورگرِ سازنده ذخیره شده بود | سینک به سرور شکست خورده و کاربر هرگز روی سرور نرفته بود (`crm/rbac.js` → `usersSyncToServer`) |
| ورود دسکتاپ از روی کپی محلی موفق بود | `crm/index.html` در صورت شکست سرور به فهرست محلی برمی‌گشت؛ موبایل چنین کپی‌ای ندارد |
| پیام موفقیت پیش از پاسخ سرور | `saveUser2` بلافاصله «✅ تعریف شد» نشان می‌داد و شکست سینک در alert دومی که دیده نمی‌شد گم می‌شد |
| «همگام‌سازی کاربران» وجود خارجی نداشت | فقط در متن خطا (`index.html`) و toast (`rbac.js`) نام برده شده بود؛ دکمه‌ای نبود |

### تغییرات

1. **دکمهٔ واقعی «🔄 همگام‌سازی کاربران»** در پنل کاربران، کنار «+ کاربر جدید» (`crm/index.html` → `buildUsers`) که تابع جدید `ptfUsersSyncManual()` را صدا می‌زند (`crm/rbac.js`):
   - گارد نقش (`roleDef().users`)؛
   - نمایش تعداد کاربران همگام‌شده و لیست کاربران کنارگذاشته‌شده (بدون رمز)؛
   - پیام صریح موفقیت/شکست.
2. **تأیید سروری پس از تعریف کاربر** (`saveUser2`):
   - پیام موفقیت فقط **پس از** پاسخ `users_sync` نمایش داده می‌شود؛
   - یک retry کوتاه برای خطای لحظه‌ای شبکه/توکن؛
   - پیام شکست، کاربر را به دکمهٔ جدید ارجاع می‌دهد.
3. **اصلاح متن‌های خطای ورود** (`crm/index.html`): به‌جای ارجاع به عملی ناموجود، کاربر را به
   «خروج/ورود مجدد ادمین» یا «دکمهٔ همگام‌سازی کاربران» راهنمایی می‌کنند.

### نکتهٔ عملیاتی

- برای رفع فوری در محیط فعلی: از دسکتاپ با حساب **admin** وارد شوید، پنل «کاربران» را باز کنید
  و «🔄 همگام‌سازی کاربران» را بزنید؛ سپس ورود کاربر از موبایل را تست کنید.
- اگر باز هم کاربر روی سرور نبود، علت در توکن/نقش/شبکه است (توکن‌ها ۷ روزه‌اند؛ خروج/ورود مجدد
  ادمین توکن را تازه می‌کند).

### فایل‌ها

- `crm/rbac.js` — `ptfUsersSyncManual` + بازنویسی `saveUser2` (تأیید سروری + retry).
- `crm/index.html` — دکمهٔ همگام‌سازی در `buildUsers` + اصلاح دو پیام خطای ورود.
- `_tools/uat/tester475-v34.7.73-users-sync-button.js` — تستر جدید (ثبت در `run-ci-gate.js`).

### اعتبارسنجی

- `node _tools/arch/arch-guard.js` → `=== ARCH GUARD: PASS ===`
- `node _tools/uat/run-ci-gate.js` → `91 PASS / 0 FAIL`
