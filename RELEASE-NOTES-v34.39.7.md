# یادداشت انتشار v34.39.7 — HOME-WAVE2 (فیکس «موج ثابت»)

**درخواست:** «موج ثابته حرکت نمی‌کنه.»
**ریشه:** نبودِ `background-repeat` روی `.hero-panel::after` — مقدارِ پیش‌فرضِ `repeat` باعث می‌شد در دمِ سکوتِ انیمیشن، نسخه‌های تایل‌شدهٔ گرادیان داخلِ قاب ثابت بمانند؛ کاربر نوارِ فریزشده می‌دید نه عبورِ موج.
**فیکس:** `background-repeat:no-repeat` + `background-position:210% 0` صریح در حالتِ اولیه (بدون فلشِ نوار روی لود) + جبرانِ resetِ `background-size` در قانونِ شب (shorthandِ `background:` اندازه را بیرون‌می‌انداخت — شب هم الان tile درست دارد).
**قرارداد:** سنجهٔ رگرسیونِ ۱.۳b در tester668 (۱۳ سنجه ✓). بودجه: ۳۹٬۸۷۱ < ۴۰K ✓ · دروازه: 662:36 / 663:30 / 664:29 / 665:25 / 666:23 / 667:21 / 668:13 — همگی PASS.
