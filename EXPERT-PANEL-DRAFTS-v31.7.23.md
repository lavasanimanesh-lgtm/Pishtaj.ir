# 🧠 صورت‌جلسه پنل متخصصان — پیش‌نویس سراسری فرم‌ها (US-DRAFT-EVERYWHERE)

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**موضوع:** درخواست کارفرما: «در سراسر نرم‌افزار هیچ امکانی برای ذخیره پیش‌نویس وجود ندارد؛ با بستن پنجره کل نوشته‌ها از دست می‌رود» — طراحی راهکار اجرایی **بدون ایجاد باگ در سراسر نرم‌افزار**.
**ترکیب پنل:** معمار ارشد فرانت‌اند، متخصص UX فرم‌ها، مهندس داده/سینک (نگهبان ۴۸ کلید)، نماینده QA/ثبات (وتوی ریسک)، نماینده کاربر عملیاتی.

---

## تحلیل وضعیت موجود

- تنها پیش‌نویس موجود: `ptf_autodraft_offer_*` مخصوص فرم پیشنهاد (TO/CO/TC) — بقیه ~۵۰ مودال فرم (نامه، درخواست، کالا، چک، قرارداد، استعلام تامین و…) هیچ محافظتی ندارند.
- همه فرم‌ها الگوی واحد دارند: مودال `.md-b` + فیلدهای `input/textarea/select` دارای `id` — این یکنواختی، راهکار **عمومی بدون دست‌زدن به تک‌تک فرم‌ها** را ممکن می‌کند.

## سه گزینه بررسی‌شده

| گزینه | شرح | رأی پنل |
|-------|------|---------|
| A) دکمه «ذخیره پیش‌نویس» در تک‌تک فرم‌ها | تغییر ~۵۰ فرم در ~۳۰ فایل | ❌ رد — سطح تماس عظیم = ریسک باگ بالا؛ خلاف قید کارفرما |
| B) تبدیل همه فرم‌ها به draft-record در دیتابیس ماژول‌ها | پیش‌نویس به‌عنوان رکورد st:draft در هر کلید | ❌ رد — آلودگی داده‌های sync‌شده با رکوردهای ناقص؛ ریسک گزارش‌ها/شمارنده‌ها |
| **C) لایه سراسری غیرمداخله‌گر (draftx.js)** | event-delegation روی document: هر تایپی در هر مودالی خودکار در کلید محلی مستقل ذخیره؛ بازشدن دوباره همان فرم → نوار «بازیابی» اختیاری | ✅ **مصوب** — صفر تغییر در کد فرم‌ها |

## قیدهای بی‌خطری مصوب QA (همه اعمال شد)

1. **صفر تغییر در ماژول‌های موجود** — فقط یک فایل جدید `draftx.js` + ثبت در index/sw. هیچ خط کدی از فرم‌ها دست نخورد.
2. **کلید `ptf_draft_forms` عمداً خارج از SYNC_KEYS/backup** — پیش‌نویس ناقص هرگز sync نمی‌شود، در backup نمی‌رود، و merge/سپرهای داده را لمس نمی‌کند (نگهبان ۴۸ کلید: «تنها راه صفرکردن ریسک داده»).
3. **بدون setInterval** — رویدادمحور (input/change/click با debounce) + MutationObserver برای مودال‌های جدید (الگوی مصوب پروژه).
4. **بازیابی همیشه opt-in** — نوار زرد با دکمه «↩️ بازیابی» / «🗑 حذف»؛ هیچ auto-restore ای فرم را غافلگیر نمی‌کند؛ اگر فرم در حالت ویرایش رکورد موجود باز شود (محتوا دارد)، نوار اصلاً ظاهر نمی‌شود.
5. **حریم:** فیلدهای password/file/hidden هرگز ذخیره نمی‌شوند؛ سقف ۱۵ پیش‌نویس؛ انقضای خودکار ۷ روزه؛ محتوای کمتر از ۱۲ کاراکتر ذخیره نمی‌شود (نویز).
6. **پاکسازی پس از ثبت موفق:** کلیک روی دکمه ثبت/ذخیره + بسته‌شدن مودال ظرف ۱.۵ ثانیه = موفق → پیش‌نویس همان فرم حذف. انصراف/بستن ساده → پیش‌نویس می‌ماند (دقیقاً سناریوی کارفرما).
7. **هوک اختیاری برای وضعیت غیرفرمی:** `ptfDraftHooks` — فعلاً فقط تصاویر نامه (US-LTR-IMG)؛ ماژول‌های آینده بدون تغییر draftx می‌توانند اضافه شوند.
8. **هم‌زیستی با autodraft قدیمی پیشنهادها:** فرم پیشنهاد چون فیلدهایش id-محور نیست عمدتاً توسط مکانیزم اختصاصی خودش پوشش داده می‌شود — دو مکانیزم تداخلی ندارند (کلیدهای جدا).

## ردشده‌ها با دلیل

- ذخیره در sessionStorage: با بستن تب از دست می‌رود — نقض هدف.
- sync پیش‌نویس بین دستگاه‌ها: ریسک تداخل ویرایش همزمان و آلودگی کانال sync؛ در صورت نیاز واقعی، US مستقل با approval.
- beforeunload prompt: تجربه آزاردهنده و در موبایل نامطمئن؛ ذخیره خودکار بی‌صدا بهتر است.

## معیار پذیرش

تایپ در هر فرم → بستن بی‌ثبت → بازکردن دوباره همان فرم → نوار بازیابی → «↩️» → همه فیلدها (و تصاویر نامه) برگردند. ثبت موفق → پیش‌نویس پاک. ویرایش رکورد موجود → بدون نوار مزاحم.
