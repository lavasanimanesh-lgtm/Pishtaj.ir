# گزارش جلسه ممیزی چندتخصصی پس از تکمیل نظام سود و تقسیم سود — v18.3

**موضوع:** ممیزی R9 و تغییرات v17.7 تا v18.2  
**وضعیت:** فقط بررسی و ثبت بک‌لاگ — بدون تغییر کد اجرایی  
**تیم‌های فرضی/تخصصی حاضر:** مدیران ارشد بازرگانی، حسابرسان ارشد، حسابداران باسابقه، متخصصین ارشد نرم‌افزار

---

## ۱) نتیجه مدیریتی جلسه

نظام محاسبه سود و تقسیم سود از نظر فازهای اصلی پیاده‌سازی شده است، اما پیش از اتکای عملیاتی کامل، چند مورد مهم باید در بک‌لاگ اصلاحی قرار گیرد. مهم‌ترین یافته، **ریسک محرمانگی داشبورد سال مالی** در v18.1/v18.2 است: چون داشبورد سال مالی با hook روی پنل تنخواه اضافه شده و پنل تنخواه برای همه کاربران قابل دسترسی است، خروجی مالی سال ممکن است برای کاربران غیرمجاز نمایش داده شود.

**تصمیم جلسه:** قبل از توسعه قابلیت جدید، ابتدا اسپرینت اصلاحی «سخت‌سازی مالی و محرمانگی R9» اجرا شود.

---

## ۲) ارزیابی تیم مدیران ارشد بازرگانی

### نقاط مثبت
- زنجیره فروش → خرید واقعی → زیان پروژه → سود سالانه از نظر مفهومی کامل شده است.
- پیش‌پرداخت ساختاریافته، خطای تصمیم‌گیری مالی را کم می‌کند.
- داشبورد سال مالی برای تصمیم هیات مدیره مسیر درستی دارد.

### یافته‌ها
1. داشبورد سال مالی باید در یک فضای کاملاً محرمانه و فقط برای ارشدترین نقش‌ها نمایش داده شود.
2. خروجی داشبورد باید قابل ارائه در جلسه هیات مدیره باشد، نه صرفاً JSON/مودال فنی.
3. پروژه‌های ناقص باید علاوه بر هشدار، مسیر اقدام مستقیم داشته باشند.

---

## ۳) ارزیابی حسابرسان ارشد

### نقاط مثبت
- استفاده از موتور واحد `ptfProjectProfitIRR` تصمیم صحیح معماری/حسابرسی است.
- جدا کردن پروژه‌های ناقص از سود سال، جلوی سود غیرواقعی را می‌گیرد.
- snapshot قفل‌شده پایان سال جهت حسابرسی مدیریتی ضروری و درست است.

### یافته‌ها
1. باید امکان سند اصلاحی بعد از قفل سال وجود داشته باشد؛ صرفاً جلوگیری از قفل مجدد کافی نیست.
2. باید trail هر عدد داشبورد قابل ردیابی باشد: هر مبلغ باید به پروژه/فاکتور/هزینه/زیان/سهامدار منبع وصل شود.
3. فیلتر سال مالی برای بعضی داده‌ها، از جمله مطالبات باز، باید دقیق‌تر و قابل توضیح باشد.
4. رکوردهای بدون تاریخ روشن نباید به‌طور پیش‌فرض در هر سال مالی وارد شوند.

---

## ۴) ارزیابی حسابداران باسابقه

### نقاط مثبت
- تنخواه، شارژ، تسویه و گزارش دوره مسیر کاربردی پیدا کرده است.
- پیش‌پرداخت ۱۰۰٪ به‌عنوان پرداخت کامل مدل شده و این خطای مطالبات را کاهش می‌دهد.

### یافته‌ها
1. گزارش سال مالی باید خروجی چاپی/Excel قابل استفاده برای حسابدار و هیات مدیره داشته باشد.
2. گزارش تنخواه و سال مالی باید شماره سند/ارجاع حسابداری را در خروجی رسمی نشان دهند.
3. پیش‌پرداخت ساختاریافته خوب است، ولی UI درصد و مبلغ هنوز دوطرفه و زنده نیست؛ کاربر باید با تغییر یکی، دیگری را فوراً ببیند.

---

## ۵) ارزیابی متخصصین ارشد نرم‌افزار

### نقاط مثبت معماری
- R9 عموماً روی زیرساخت موجود سوار شده و کلیدهای بی‌مورد زیاد ساخته نشده است.
- `lossEvents` داخل deal/project ذخیره شده و کلید جداگانه زیان ساخته نشده؛ این تصمیم معماری مناسب است.
- `ptf_crm_fiscal_snapshots` برای snapshot قفل‌شده توجیه دارد.
- رگرسیون‌ها تا v18.2 سبز بوده‌اند.

### یافته‌های فنی/معماری
1. **BUG-024 بحرانی:** `fiscal.js` در `buildPetty` برای همه کاربران رندر می‌شود و `fiscalHtml()` guard محرمانگی ندارد.
2. `ptfFiscalPrint()` نیز guard ندارد و اگر تابع فراخوانی شود، داده خروجی می‌دهد.
3. قرار گرفتن همه اجزای مالی R9 در پنل «تنخواه» از نظر UX و محرمانگی سنگین شده است؛ بهتر است یک «هاب مالی مدیریتی» یا حداقل گارد داخلی واضح ایجاد شود.
4. تسترهای قبلی چند بار به نسخه ثابت حساس بودند و اصلاح شدند؛ باید از این پس تسترها نسبت به نسخه‌های بالاتر مقاوم نوشته شوند.
5. `ptfFiscalData` برای رکوردهای بدون تاریخ، امکان ورود ناخواسته به سال انتخابی دارد؛ باید سیاست تاریخ‌گذاری سخت‌تر شود.

---

## ۶) حکم نهایی جلسه

نظام R9 از نظر فازهای اجرایی تکمیل شده، اما **قبل از اتکای سازمانی و نصب در محیط واقعی با کاربران متعدد، اسپرینت اصلاحی محرمانگی و حسابرسی لازم است**.

اولویت فوری:
1. BUG-024 محرمانگی داشبورد سال مالی
2. BUG-025 فیلتر سال مالی / داده‌های بدون تاریخ / مطالبات باز
3. US-426 خروجی رسمی و audit trail محاسبات
4. US-427 سند اصلاحی پس از قفل سال
5. US-428 بهبود UX پیش‌پرداخت درصد/مبلغ زنده

