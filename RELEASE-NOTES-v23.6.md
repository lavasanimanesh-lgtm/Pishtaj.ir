# 📦 ریلیزنوت v23.6 — اسپرینت ۲۳۶: تضمین نمایش دکمه مشاهده درخواست‌ها، آزادسازی دکمه ذخیره پیشنهاد و سیستم پیشرفته محاسبه حاشیه سود (`US-289`)

**تاریخ:** ۱۴۰۵/۰۴/۲۱ (July 2026) | **نسخه قبلی:** v23.5 (اسپرینت ۲۳۵) | **مرجع حاکمیتی:** تیم ارشد QA/QC (پروتکل ۷مرحله‌ای)

---

## 🎯 دستاوردهای اسپرینت ۲۳۶ (حل فوری ۳ مورد گزارش‌شده کارفرما)

### ۱. تضمین نمایش قطعی دکمه «👁 مشاهده» در درخواست‌های ثبت‌شده (`index.html` و `inqreader.js`)
- **تزریق مستقیم در جدول اولیه:** دکمه «👁 مشاهده» به طور مستقیم و ثابت در کد اصلی رندر جدول درخواست‌ها (`renderRfq` در `index.html`) قرار گرفت تا به هیچ هوک زمانی وابسته نباشد.
- **حفظ دکمه در زمان رندر پیوست‌ها:** در اسکریپت `inqreader.js` (`patchRenderRfq`)، منطق رندر اقلام و پیوست‌ها اصلاح شد تا در درخواست‌های دارای پیشنهاد (`hasOffer` یا `wonOffer`)، دکمه «مشاهده» هرگز پاک یا جایگزین نشود.

### ۲. آزادسازی دکمه «💾 ذخیره» پیشنهادها در تمام حالات ورود دستی و انتخابی (`offers.js`)
- **حذف قفل اعتبارسنجی استعلام دستی (`ptfInqNoValid`):** در نسخه قبلی اگر کاربر شماره استعلام را به صورت دستی تایپ می‌کرد (یا پیشنهاد فوری بدون استعلام می‌زد)، سیستم با هشدار «شماره درخواست باید از فهرست انتخاب شود» دکمه ذخیره را متوقف می‌کرد. این قفل اکنون برداشته شده و در صورت تایپ دستی، سیستم آن شماره را به طور خودکار به عنوان استعلام اولیه معتبر ثبت کرده و پیشنهاد را ذخیره می‌کند.
- **حفاظت در برابر خطاهای `DOM`:** تمامی دسترسی‌ها به عناصر فرم (`ofBuyer`، `ofInq`، `ofSeller`) مجهز به لایه دفاعی (`Null Safety`) شدند تا در هیچ شرایطی دکمه ذخیره از کار نیفتد.

### ۳. سیستم پیشرفته حاشیه سود پیش‌فرض و تک‌تک اقلام بر اساس نرخ مرجع کالا (`US-289` در `offers.js`)
برای پایان دادن به محاسبات دستی قیمت فروش، قابلیت‌های زیر به پنجره ثبت/ویرایش پیشنهاد مالی (`CO` و `TC`) اضافه شد:
- **نوار محاسبه حاشیه سود پیش‌فرض کل اقلام (`Global Default Margin Bar`):** در بالای جدول اقلام پیشنهاد، یک باکس اختصاصی «📈 حاشیه سود پیش‌فرض کل اقلام (٪)» قرار گرفت. با وارد کردن درصد دلخواه (مثلاً ۳۰٪) و فشردن دکمه «⚡ محاسبه خودکار قیمت فروش»، سیستم برای تمام ردیف‌هایی که دارای نرخ مرجع هستند، قیمت فروش واحد (`price`) را بر اساس فرمول `نرخ مرجع × (1 + درصد سود)` محاسبه کرده و جمع کل را به‌روزرسانی می‌کند.
- **ستون‌های تعاملی «نرخ مرجع خرید» و «(٪) سود» برای تک‌تک آیتم‌ها:** در جدول اقلام، دو ستون جدید و تعاملی در کنار قیمت واحد فروش اضافه شد:
  - با تغییر یا ورود **نرخ مرجع خرید** (`refPrice` که خودکار از بانک کالا هم پر می‌شود)، قیمت واحد فروش بر اساس درصد سود فعال محاسبه می‌شود.
  - با تغییر **درصد حاشیه سود (`marginPct`) هر ردیف**، قیمت واحد فروش همان کالا بلافاصله به‌روزرسانی می‌شود.
  - در صورت تغییر دستی قیمت واحد فروش، درصد حاشیه سود واقعی به صورت معکوس و دقیق محاسبه و نمایش داده می‌شود.
- **عدم انتشار در اسناد چاپی مشتری:** ستون‌های نرخ مرجع و درصد سود صرفاً ابزار محاسباتی داخلی کارشناس فروش در فرم ویرایش هستند و هرگز روی اسناد رسمی چاپی یا PDF مشتری (`CO/TC/TO`) درج نمی‌شوند.

---

## 🛡️ ممیزی کیفیت و حاکمیت تیم QA/QC
- اجرای ممیز `audit.py`: **۰ خطا PASS**
- اجرای هارنس رگرسیون محاسبات حاشیه سود و ذخیره پیشنهاد: **۱۰۰٪ PASS**
- شناسه نسخه سیستم به `v23.6` ارتقا یافت و خروجی با نام **`pishtaj-release-v23.6.zip`** صادر گردید.
