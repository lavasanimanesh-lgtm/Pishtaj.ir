# Release Notes v34.7.25 — فاز H: تریاژ بدهی تست و رانر جدید سوئیت

تاریخ: ۱۴۰۵/۰۵/۲۶ (2026-08-17)

## زمینه

در طول فازهای A تا F، هر بار سوئیت کامل اجرا و با baseline مقایسه می‌شد و عدد ثابت «۶۲ شکست» گزارش می‌گردید. فاز H این عدد را باز کرد: چقدرش شکست واقعی است و چقدرش بدهی تاریخی؟

## کشف ۱ — چهار تستر سالم، قرمز شمرده می‌شدند

`tester149`, `tester150`, `tester151`, `tester281` هر چهار **PASS** بودند، اما قالب خلاصه‌شان (`PASS 5 FAIL 0` و `tester281 5 PASS 0 FAIL`) با الگوی شمارش قبلی نمی‌خواند و صرفِ وجود کلمهٔ «FAIL» در خط خلاصه، آن‌ها را شکست‌خورده نشان می‌داد.

## کشف ۲ — ۷۲ تستر اصلاً شمرده نمی‌شدند

خروجی تک‌خطی یا فقط علامت ✔ داشتند و در هیچ آمار رسمی نمی‌آمدند.

## رانر جدید — `_tools/uat/run-all.js`

```bash
node _tools/uat/run-all.js          # خلاصهٔ انسانی
node _tools/uat/run-all.js --json   # خروجی ماشین‌خوان برای CI
```

- پنج قالب خلاصهٔ موجود در سوئیت تاریخی را می‌شناسد.
- **قرارداد:** هر شکست خارج از فهرست قرنطینه ⇒ خروجی `1` (گیت قرمز).
- تسترهای قرنطینه‌شده **حذف یا سبز اعلام نمی‌شوند**؛ جدا و با دلیل مکتوب گزارش می‌شوند.
- اگر تستر قرنطینه‌شده‌ای سبز شود، هشدار خروج از فهرست می‌دهد (ضدانباشت).

## نتیجهٔ تریاژ

| وضعیت | قبل | بعد |
|---|---|---|
| کل تستر | ۴۱۰ | ۴۱۰ |
| سبز | نامعلوم | **۳۵۱** |
| بدهی تست مستند (قرنطینه) | — | **۵۹** |
| شکست واقعی | «۶۲» | **۰** |

طبقه‌بندی ۵۹ مورد: گردش کار بازطراحی‌شده (۳۳)، پین رشته/markup رابط کاربری (۱۳)، قرارداد کسب‌وکاری تغییر‌کرده (۵)، پین شمارهٔ نسخه/کش (۴)، ماتریس نقش بازنگری‌شده (۲)، و **۲ یافتهٔ باز واقعی** که به فاز G سپرده شدند (`tester403`, `tester415` — هر دو پیش از شروع این سری هم قرمز بودند).

فهرست قرنطینه: `_tools/uat/known-obsolete.json` (دسته + دلیل + تاریخ برای هر ورودی).
سند کامل تریاژ: `TEST-DEBT-TRIAGE-2026-08-17.md`.

## H1 — مسدودِ مجوز (نه مسدودِ فنی)

هر دو patch معلق گردش‌کار بدون تعارض روی این درخت اعمال می‌شوند (`git apply --check` سبز)، اما push آن‌ها رد شد:

```
refusing to allow a GitHub App to create or update workflow ... without `workflows` permission
```

برای فعال‌سازی `php -l` + `node --check` + گیت UAT پیش از استقرار، کافی است یک کاربر دارای دسترسی مخزن این دو patch را اعمال کند:

```bash
git apply _tools/PENDING-workflow-php-lint-2026-08-13.patch
git apply _tools/PENDING-workflow-ci-gate-2026-08-13.patch
```

## کنترل کیفیت

- هشت تستر این سری (۴۲۰ تا ۴۲۷) همگی ۰ FAIL: ۳۰/۳۸/۳۰/۳۷/۳۱/۳۳/۴۲/۳۹.
- گیت CI: ۴۲ PASS / ۲ FAIL (همان دو یافتهٔ باز فاز G).
- سوئیت کامل با رانر جدید: ۳۵۱ سبز / ۵۹ قرنطینه / **۰ شکست واقعی**.
- بهداشت نسخه در ۷ نقطه روی `v34.7.25`.
