# 🐞 ریلیز v15.3 — BUG-016 «دکمه متوجه شدم راهنمای ورود اکسل»

**تاریخ:** ۱۴۰۵/۰۴/۱۸ | **نوع:** رفع باگ گزارش‌شده کارفرما
**باگ:** در همه ماژول‌های دارای «ورود اکسل با راهنما»، با فشردن «✅ متوجه شدم» پنجره بسته می‌شد ولی پنجره انتخاب فایل هرگز باز نمی‌شد.

---

## 🔬 ریشه دقیق

یک خطای کلاسیک «حوزه متغیر» (Scope) در جاوااسکریپت:

- دکمه «متوجه شدم» در کدش نوشته بود: «فایل‌پیکرِ `fileInputId` را کلیک کن»؛
- ولی `fileInputId` یک **متغیر محلی داخل تابع سازنده مودال** بود، در حالی که کلیک دکمه در **حوزه سراسری صفحه** اجرا می‌شود که چنین متغیری نمی‌شناسد؛
- نتیجه: دستور اول (بستن مودال) اجرا می‌شد، دستور دوم (باز کردن فایل‌پیکر) با خطای `ReferenceError` بی‌صدا می‌مرد.

این باگ از v122.2 (US-280) وجود داشت و **هر ۵ ماژول** را که از این راهنمای مشترک استفاده می‌کنند گرفتار می‌کرد: کالاها، مشتریان، تامین‌کنندگان، سرنخ‌ها و اقلام پیشنهاد (TO/CO).

## ✅ رفع

- تابع سراسری جدید `ptfXlsGuideGo`: شناسه فایل‌پیکر حالا موقع ساخت دکمه **داخل خودش درج می‌شود** (نه ارجاع به متغیری که وجود ندارد).
- مقاوم‌سازی اضافه: اگر چند نمونه از فیلد با یک شناسه در صفحه باشد (مودال‌های مینیمایزشده — همان خانواده باگ US-364)، **آخرین نمونه** (پنل فعال) کلیک می‌شود.
- اگر فیلد پیدا نشود، به‌جای سکوت پیام شفاف می‌دهد.

با یک رفع، هر ۵ ماژول درست شدند (همه از همان مودال مشترک استفاده می‌کنند).

## 🔬 کیفیت

| مورد | نتیجه |
|-------|--------|
| tester73 جدید | ✅ ۱۴ چک — شامل شبیه‌سازی اجرایی کلیک (بسته شدن مودال + کلیک روی آخرین نمونه + پیام نبود فیلد) و چک پوشش هر ۵ ماژول |
| رگرسیون کامل | ✅ **۶۰ تستر / ۱۶۳۳ چک — صفر خطا** |
| `_tools/audit.py` | ✅ PASS |
| نسخه/کش | VER=v15.3، کش `ptf-crm-v15.3` |

## 📦 بسته تحویلی
`pishtaj-release-v15.3.zip` — تجمیعی **v14.5 تا v15.3**. مثل همیشه: آپلود + اکسترکت روی موجود + رفرش (سایدبار: v15.3).

**تست بعد از آپلود:** کالاها (یا مشتریان/تامین‌کنندگان/سرنخ‌ها) → «📥 ورود اکسل با راهنما» → «✅ متوجه شدم» → این‌بار پنجره انتخاب فایل باید باز شود.
