# RELEASE NOTES — v34.36.3 (پنل‌های بالای فهرست: پیش‌فرض بسته + جست‌وجو + «حساب مشتریان» جدید)

**تاریخ:** ۲۰۲۶-۰۹-۰۴ · **گیت:** سبز · **تستر جدید:** `tester591-v34.36.3-collapsible-account-panels.js` (۶۹ سنجهٔ رفتاری + منبعی)
**پرونده‌های تغییر:** `crm/supplier-finance.js` · `crm/scoring.js` · `crm/customer-finance.js` · `crm/theme-contrast.js` + نشانگرهای نسخه

---

## گزارش کارفرما

> «در قسمت تامین‌کنندگان پنجرهٔ بالای فهرست یعنی حساب تامین‌کنندگان به‌صورت پیش‌فرض باز است؛ باید به‌صورت پیش‌فرض بسته باشد و قابلیت جستجو داشته باشد. مشابه همین پنجره در قسمت مشتریان یعنی حساب مشتریان ایجاد شود.»

تصمیم‌های تأییدشدهٔ کارفرما (پرسشِ مستقیم، پیش از کد):

| پرسش | تصمیم |
|------|-------|
| در بخشِ مشتریان چه شود؟ (پیش از این هیچ پنلی بالای فهرست مشتریان نبود؛ «حساب مشتریان» فقط تبی در هابِ مالی بود) | **پنلِ مشابه ساخته شود** — بالای فهرست مشتریان، پیش‌فرض بسته، با جستجو، ماندهٔ هر مشتری و دکمهٔ «حساب و اسناد» |
| جعبهٔ «💳 بدهی غیرنقدی تأمین‌کنندگان» که آن هم بالای فهرست و همیشه باز است؟ | **هر دو جعبه بسته شوند** |
| وضعیتِ باز/بسته بعد از رفرش؟ | **همیشه بسته شروع شود** (ذخیره نمی‌شود) |

---

## تغییرات

### ۱) پنلِ «🧾 فاکتور، حساب و پرداخت تأمین‌کنندگان» (`crm/supplier-finance.js`)

- **پیش‌فرض بسته:** `<details id="slBox" open …>` ⇒ `<details id="slBox" …>`؛ فهرستِ اصلیِ تامین‌کنندگان بدونِ پیمایشِ اضافه دیده می‌شود.
- **جست‌وجو:** کادرِ `#slBoxQ` (نام، نامِ انگلیسی، نامِ رابط، کد) با نرمال‌سازیِ فارسیِ `dedupNorm` ⇒ «ي/ی» و «ك/ک» یکسان می‌گیرند. جست‌وجو **فقط `tbody`** را به‌روز می‌کند تا فوکوس و مکانِ نشانگرِ کادر حفظ شود (هم‌خانواده با قاعدهٔ `UR-2026-08-01-02` در جعبهٔ هاب مالی).
- **محاسبه تا نخستین بازکردن به تعویق افتاد:** پیش‌تر `box()` یک `setTimeout(30ms)` می‌گذاشت که **همیشه** — حتی وقتی کسی کاری با این جعبه نداشت — همهٔ تامین‌کنندگان را پیمایش و ماندهٔ هرکدام را حساب می‌کرد. اکنون `ontoggle` ⇒ `ptfSlBoxToggle` ⇒ `ptfSlBoxLazy(true)`؛ وقتی پنل بسته است همان فراخوانیِ `cheques.js` فقط نشانِ «در انتظار» (`_slBoxLazyPending`) می‌گذارد و برمی‌گردد.
- **ترتیب و سقفِ ردیف:** حساب‌های باز ابتدا، سپس بیشترین مانده، سپس الفباییِ فارسی (هم‌ترتیب با جعبهٔ «حساب تأمین‌کنندگان» در هاب مالی) و حداکثر ۶۰ ردیف + ردیفِ راهنمای «… و N حساب دیگر — نام یا کد را جست‌وجو کنید».
- **بهینه‌سازیِ همراه:** تشخیصِ «این تامین‌کننده فاکتور/پرداخت دارد» پیش‌تر برای **هر** تامین‌کننده با `.some()` روی کل فهرست فاکتور‌ها و پرداخت‌ها انجام می‌شد (`O(n·m)`)؛ اکنون یک‌بار مجموعهٔ شناسه‌ها ساخته می‌شود (`O(n+m)`) — همان معنا، بدونِ تغییرِ قاعده.

### ۲) جعبهٔ «💳 بدهی غیرنقدی تأمین‌کنندگان» (`crm/scoring.js`)

- از `<div id="payablesBox">` به `<details id="payablesBox">` **بسته** تبدیل شد؛ چون بسته است، **جمع و شمارِ قلم‌های باز روی عنوان (`summary`)** نوشته می‌شود تا وضعیتِ کلی بدونِ بازکردن دیده شود: «۲ تأمین‌کننده · ۷٬۰۰۰٬۰۰۰ ریال باز» یا «بدونِ بدهی باز ✅».
- **جست‌وجو:** کادرِ `#payablesQ` روی نامِ تامین‌کننده؛ فقط ناحیهٔ `#payablesRows` به‌روز می‌شود.
- سقفِ ۸ ردیف حفظ شد و وقتی موردِ بیشتری هست، راهنمای «… و N تأمین‌کنندهٔ دیگر — نام را جست‌وجو کنید یا «📋 بستانکاری‌ها» را بزنید» اضافه می‌شود.
- **رفرشِ برنامه‌ای وضعیت را حفظ می‌کند:** `refreshPayablesBox()` (پس از ثبتِ پرداخت) با `payablesBoxHtml(!!box.open)` همان حالتِ لحظه‌ای را نگه می‌دارد تا جعبه وسطِ کارِ کاربر بسته نشود؛ **رندرِ تازهٔ پنل همچنان بسته شروع می‌شود**.
- کلاس‌های CSS (`payables-head/summary/actions`, `payable-debt-row/info`, `payables-empty`) و handlerها (`ptfPayablesOpen`, `ptfScoreReport`) دست‌نخورده‌اند ⇒ هیچ برگهٔ CSS یا pin رفتاری نشکست.
- **نمای شب:** دو قاعدهٔ تازه در `crm/theme-contrast.js` برای خواناییِ `summary` و کادرِ جست‌وجو در دارک‌مود.

### ۳) جعبهٔ **جدیدِ** «📘 حساب مشتریان» بالای فهرست مشتریان (`crm/customer-finance.js`)

- `<details id="cfBox">` بسته + `ontoggle` + کادرِ جست‌وجوی `#cfBoxQ` + جدولِ «مشتری / مطالبات باز / اعتبار نزد مشتری / 📘 حساب و اسناد» (دکمه ⇒ `cfOpen(cd)`، همان گردشِ حسابِ موجود).
- **منبعِ عددها یکی است:** ردیف‌ها از `cfAccountRows(q)` و `accountIsOpen` ساخته می‌شوند، یعنی دقیقاً همان مسیری که جعبهٔ «حساب مشتریان» در هابِ مالی و قراردادِ canonical مطالبات (`ar-reconcile`) استفاده می‌کنند ⇒ دو نما هرگز عددِ متفاوتی نمی‌گویند (هیچ فرمولِ خصوصیِ تازه‌ای نوشته نشد — همان درسی که در `ARENA-AR-RECEIPT-INTEGRATION-RCA-2026-08-16.md` ثبت شده).
- **بی‌حساب‌ها ردیف نمی‌گیرند ولی پنهان هم نمی‌مانند:** در نمایِ اولیه فقط حساب‌های دارای مانده/اعتبار می‌آیند و زیرِ جدول نوشته می‌شود «N حساب با ماندهٔ صفر — برای دیدن هر مشتری، نام یا کدش را جست‌وجو کنید»؛ با جست‌وجو همان حسابِ صفر هم پیدا می‌شود. سقفِ ۶۰ ردیف + راهنمای «و N حساب دیگر».
- **RBAC:** نقشِ ارشد یا `ledgerScope` غیرِ `none` — کارشناسِ فروش این جعبه را نمی‌بیند؛ حسابدار (`official`) می‌بیند و خودِ `accountPosition` هم اسنادِ غیررسمی را بر اساسِ نقش فیلتر می‌کند.
- **دکمهٔ «📈 نمای کامل در هاب مالی»:** `goPanel('petty')` سپس `finHubSet('custacc')` با گاردِ نقش (برای نقش‌های غیرارشد پیامِ صریح).
- **hook با تلاشِ مجدد** روی `buildCustomers` (هم‌الگوی `scoring.js`) چون ترتیبِ بارگیریِ اسکریپت‌های `defer` تضمینِ قطعی نیست.

### ۴) قاعدهٔ سراسریِ این انتشار: برخوردِ `id` با نامِ تابع ممنوع

نامِ کادرهای جست‌وجو عمداً با نامِ تابع‌ها **یکی نیست**: `id="slBoxQ"`/`slBoxSearch()` · `id="payablesQ"`/`ptfPayablesSearch()` · `id="cfBoxQ"`/`cfBoxSearch()`. در مرورگر هر `id` یک متغیرِ سراسری هم می‌سازد و اگر با نامِ تابع یکی شود، رفتار به ترتیبِ رندر وابسته می‌شود — همان کلاسِ باگی که پیش‌تر در `pgTitle` ریشه‌کن شد و سنجهٔ «۱.۴/۲.۳/۳.۴» در تسترِ جدید جلویش را می‌گیرد. `cfSearch`/`_cfSearch` (متعلق به جعبهٔ هابِ مالی) کاملاً دست‌نخورده ماند.

---

## یک شکستِ همراه که در گیت لو رفت و ریشه‌کن شد

هوکِ `buildCustomers` در تلاشِ مجدد از `setInterval` استفاده می‌کند. چهار تسترِ قدیمیِ حوزهٔ مشتریان (`tester415`, `tester421`, `tester429`, `tester504`) فایل را در `vm` **بدونِ تایمر** بارگذاری می‌کنند ⇒ `ReferenceError: setInterval is not defined` و **سقوطِ کاملِ تستر** (نه فقط یک سنجه).

اصلاح: گاردِ `typeof setInterval === 'function'` + `try/catch` پیرامونِ هوک (و گاردِ `clearInterval`). یک ماژولِ UI هرگز نباید میزبانی را که تایمر ندارد بیندازد؛ در مرورگرِ واقعی مسیرِ تلاشِ مجدد مانند قبل کار می‌کند. سنجهٔ «۳.۳» در تسترِ جدید همین گارد را pin می‌کند.

---

## سازگاری (بدونِ تغییر)

- شناسه‌ها و تابع‌های pin‌شده سرِ جای خود: `slBox`, `slBoxBody`, `slBoxRows`, `ptfSlBoxLazy`, `_slBoxLazyTries < 8`, `balanceHtmlFrom(b, s.cd)`, `payablesBox`, `cfFinanceHubBox`, `cfSearch`, `cfFinanceSearch`, `cfOpen`, `slOpenLedger`.
- «تنظیمات ← سیستمِ امتیازدهی» و مودالِ «بستانکاری‌ها» بدونِ تغییر؛ جعبهٔ هابِ مالی (`supacc`/`custacc`) هم دست‌نخورده است — این انتشار فقط **بالای فهرستِ** خودِ تامین‌کنندگان/مشتریان را مرتب کرد.
- هیچ دادهٔ تازه‌ای ذخیره نمی‌شود (بدونِ کلیدِ localStorage جدید، بدونِ تغییرِ schema) ⇒ «همیشه بسته شروع شود» به‌صورت ساختاری تضمین است، نه تنظیمی که بتواند واگرا شود.
- گاردِ merge محافظت‌شده و مسیرِ بدونِ کدِ v34.36.1 و رسیدِ پیوستِ v34.36.2 بدونِ regression (سنجه‌های `tester588`/`tester589` سبز).

---

## آزمون

- **`tester591` (جدید، در گیت): ۶۹ PASS / ۰ FAIL** — سه ماژول در `vm` با DOMِ ساختگی **اجرا** می‌شوند و رفتار واقعاً سنجیده می‌شود، نه فقط متنِ منبع:
  - بسته بودنِ پیش‌فرض (و نبودِ `open` در HTMLِ تولیدیِ هر سه جعبه)؛
  - «تا بسته است محاسبه نمی‌شود» ⇒ بدنه دست‌نخورده و `_slBoxLazyPending`؛ نخستین بازکردن ⇒ ردیف‌ها؛ بستن ⇒ بدونِ محاسبه؛
  - جست‌وجو: نام، کد، بی‌نتیجه (پیامِ صریح)، پاک‌کردن (بازگشتِ فهرستِ کامل) و اینکه فقط `tbody`/`payablesRows` به‌روز می‌شود؛
  - سقفِ ردیف: ۶۵ تامین‌کننده ⇒ ۶۰ ردیف + راهنما با رقمِ فارسی، و پیدا شدنِ ردیفِ خارج از سقف با جست‌وجو؛ ۱۲ بدهی ⇒ ۸ ردیف + «و ۴ تأمین‌کنندهٔ دیگر»؛ ۷۰ مشتری ⇒ ۶۰ ردیف + «و ۱۰ حساب دیگر»؛
  - RBAC: `sales` هیچ‌کدام از سه جعبه را نمی‌بیند، `accountant` جعبهٔ مشتریان را می‌بیند؛
  - تفکیکِ جعبهٔ جدید از جعبهٔ هابِ مالی (idها و state جدا) و سلامتِ زنجیرهٔ هوک‌ها (پایهٔ پنل حفظ می‌شود).
- **pinهای به‌روز (سست‌تر نشدند):** `tester487` (۱۴/۰) — «`box()` محاسبه را به `setTimeout` می‌سپارد» به «به نخستین بازکردن می‌سپارد» و «`slBox` همچنان open است» به «تاشو و پیش‌فرض بسته + جستجو» تغییر کرد، به‌همراهِ pinِ پارامترِ `query` در `slBoxRows`؛ `tester149` (۵/۰، خارجِ گیت) هم‌راستا شد تا مخزن با خودش در تناقض نباشد.
- **گیت CI: ۲۰۶ PASS / ۰ FAIL** (۲۰۵ ورودیِ پیشین + `tester591`).
- `arch-guard`: PASS · بارگذاریِ هر سه ماژول در `vm` بدونِ خطا.

---

## اقدام لازم پس از استقرار

1. **رفرشِ سختِ CRM** تا `supplier-finance.js?v=34.36.3`، `scoring.js?v=34.36.3` و `customer-finance.js?v=34.36.3` گرفته شود (service worker کشِ نسخهٔ قبل را دارد).
2. بالای فهرستِ **تامین‌کنندگان** اکنون دو جعبهٔ بسته می‌بینید: «💳 بدهی غیرنقدی …» (با جمع و شمار روی عنوان) و «🧾 فاکتور، حساب و پرداخت …» — با کلیک باز می‌شوند و کادرِ جست‌وجو دارند.
3. بالای فهرستِ **مشتریان** جعبهٔ تازهٔ «📘 حساب مشتریان» اضافه شده؛ برای دیدنِ همهٔ مشتریان با سورتِ ستون‌ها، دکمهٔ «📈 نمای کامل در هاب مالی» همان تبِ قبلی را باز می‌کند.
