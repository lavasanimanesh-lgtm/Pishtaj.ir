# 📚 کیس استادی R8 — «خرید واقعی، تسعیر یکپارچه و چرخه وضعیت تامین»
**تاریخ:** ۱۴۰۵/۰۴/۲۴ | **وضعیت:** راهکار اجرایی نهایی‌شده پنل — ثبت بک‌لاگ، **بدون تغییر کد تا ابلاغ اسپرینت** (قاعده مصوب)
**تدوین:** پنل مشترک — متخصصان ارشد بازرگانی (فرایند تامین/تسعیر) + مهندسان ارشد نرم‌افزار | **توافق کامل پنل حاصل شد ✅**

---

## ۱) ممیزی فرایند فعلی روی کد (چه داریم، چه غلط است)

| جزء | وضعیت فعلی | حکم پنل |
|-----|-------------|---------|
| ثبت خرید واقعی (cmpBuy) | از بین آخرین قیمت هر تامین‌کننده انتخاب + قیمت قابل اصلاح + ارز/نرخ (v16.0) + نقدی/اعتباری (v16.6) | پایه درست — ولی **نرخ تسعیر ارزی اختیاری است** (فقط confirm هشدار) → باید در مسیر پرونده فروش الزامی شود |
| جدول مقایسه از پرونده فروش (ptfRealBuyOpen→cmpOpen) | همان cmpOpen عمومی باز می‌شود: دکمه‌های «+ قیمت دور ۱» و «+ قیمت دور ۲» فعال‌اند | ❌ **مغایر فرایند** — گزارش درست کارفرما: از داخل پرونده باید فقط‌خواندنی باشد؛ استعلام جدید فقط از سامانه استعلام تامین |
| قیمت استعلامی ارزی (r.quoteCur در rfqsmart) | ثبت می‌شود ولی هنگام «ثبت خرید» تبدیل ریالی خودکار پیشنهاد نمی‌شود | باید نرخ تسعیر گرفته و **معادل تومانی به‌عنوان خرید واقعی** ذخیره شود |
| تسعیر فروش (ptfFxPayDialog) | نرخ دستی روز پرداخت گرفته می‌شود (US-266v2) و در pays[].fx می‌نشیند؛ موتور سود از آن می‌خواند | پایه درست — کمبود: **انتخاب نوع نرخ (آزاد/سنا) با پیشنهاد نرخ زنده** + ورود درصدی (٪۳۰ پیش‌پرداخت) + نمایش باقیمانده در مطالبات (زیرساخت remainFx موجود) |
| وضعیت‌های درخواست | st1..st7 — «در حال تامین/تحویل تامین‌کننده» وجود ندارد | دو وضعیت جدید + گذار خودکار |
| رنگ ردیف درخواست | فقط پس‌زمینه مهلت (US-348) | سبز=برنده / قرمز=بازنده اضافه شود (با اولویت‌بندی نمایشی نسبت به رنگ مهلت) |

## ۲) راهکار جامع مصوب پنل (یکپارچه در سراسر نرم‌افزار)

**اصل مرکزی:** «قیمت استعلامی = فقط کشف قیمت، فقط از سامانه استعلام تامین، همیشه فقط‌خواندنی در پرونده» و «خرید واقعی = تنها مبنای سود، همیشه دارای معادل ریالی قطعی». موتور سود (ptfProjectProfitIRR) بدون تغییر می‌ماند — فقط ورودی‌هایش تمیزتر و اجباری‌تر می‌شوند.

### US-412 — خرید واقعی از پرونده فروش: انتخاب از استعلامی/دستی + تسعیر الزامی + قفل دورها (اسپرینت ۱ — هسته کیس)
1. **نمای قفل‌شده جدول مقایسه در مسیر پرونده فروش:** `cmpOpen(id, {realbuy:true})` — دکمه‌های «+ قیمت دور ۱/۲» حذف؛ قیمت‌های دور ۱/۲ فقط‌خواندنی با برچسب «📌 استعلامی — تغییر فقط از سامانه استعلام تامین»؛ به‌جای آن‌ها دکمه «🤖 استعلام جدید از تامین‌کننده» → مستقیم به rfqsmart همان درخواست (ایجاد/باز کردن کارت رهگیری) — رصد در پرونده از قبل هست (v17.1 گروه ③). مسیر مستقل ماژول «قیمت‌های خرید» (کشف قیمت قبل از برد) دست نمی‌خورد.
2. **پنجره «ثبت خرید واقعی» بازطراحی‌شده (cmpBuy در حالت پرونده):** منبع قیمت = ① انتخاب از قیمت‌های استعلامی موجود (آخرین قیمت هر تامین‌کننده — دور۱/دور۲/پاسخ‌های rfqsmart) ② **ورود دستی** (تامین‌کننده از فهرست + قیمت آزاد). 
3. **تسعیر الزامی ارزی:** اگر قیمت انتخابی/دستی ارزی است (quoteCur یا انتخاب کاربر)، فیلد «نرخ تسعیر (تومان)» **الزامی** (نه confirm) + پیشنهاد نرخ زنده آزاد/سنا از ویجت fx (فقط پیشنهاد — تصمیم با کاربر) → **معادل تومانی محاسبه و به‌عنوان قیمت خرید واقعی ذخیره** می‌شود (purchases: price=تومان، priceFx/cur/rate برای رهگیری) → کل محاسبات سود ریالی (سازگار کامل با موتور v16.0 — buyPendingFx عملا صفر می‌شود).
4. سازگاری: خریدهای قبلی دست نمی‌خورند؛ بستانکاری v16.6 (نقدی/اعتباری) در همان پنجره می‌ماند.

### US-413 — چرخه وضعیت تامین + رنگ برد/باخت در فهرست درخواست‌ها (اسپرینت ۲)
1. دو وضعیت جدید در منبع واحد RFQ_STATUSES: **st8 «🏭 در حال تامین توسط تامین‌کننده»** و **st9 «📦 تحویل تامین‌کننده»** (بین st5 و st6 در چرخه منطقی؛ کانبان v16.4 و مودال‌ها خودکار می‌گیرند چون از همان منبع می‌خوانند).
2. **گذار خودکار:** اولین «ثبت خرید واقعی» هر درخواست → st8 (از مسیر ptfRfqSetStatus موجود — با notify/audit/سینک سایت). تکمیل همه اقلام + رویداد «تحویل ok» بستانکاری (v16.6) → پیشنهاد گذار به st9 با تایید کاربر (خودکار بی‌صدا نه — تصمیم عملیاتی با کاربر).
3. **رنگ ردیف‌ها:** درخواستِ دارای CO برنده = پس‌زمینه سبز؛ بازنده (بایگانی lost) = قرمز؛ اولویت نمایشی: برد/باخت > هشدار مهلت (US-348) — در فهرست و کانبان.
4. myday/دیجست آینده از st8/st9 برای پیگیری تامین استفاده خواهند کرد (لینک آینده — بدون اجرا در این فاز).

### US-414 — تسعیر فروش در روز پرداخت: نرخ آزاد/سنا + پرداخت درصدی + اتصال مطالبات (اسپرینت ۳)
1. دیالوگ تسعیر دریافت (ptfFxPayDialog) ارتقا: انتخاب **نوع نرخ: آزاد / سنا / توافقی** با نمایش نرخ زنده هر دو (زیرساز fx-rates موجود) — انتخاب کاربر، نرخ قابل اصلاح (نرخ قطعی = تایید کاربر، مثل قاعده همیشگی).
2. **ورود درصدی:** فیلد «درصد از مبلغ سند» (مثلا ۳۰٪ از ۱۵۰۰$) → مبلغ ارزی سهم محاسبه → × نرخ انتخابی = مبلغ ریالی دریافت + ثبت fx (سازگار با pays[].fx موجود — موتور سود بدون تغییر).
3. **باقیمانده به مطالبات:** remainFx موجود در کارت مطالبات با برچسب شفاف «باقیمانده ارزی سند + معادل حدودی با نرخ روز (اطلاعی)» + بج درصد وصول‌شده.
4. ثبت نوع نرخ استفاده‌شده (free/sana/agreed) روی تراکنش برای گزارش‌های آتی.

## ۳) ماتریس یکپارچگی (لینک‌های بین‌ماژولی — الزام کارفرما)

| ماژول | اثر | مکانیزم |
|-------|-----|---------|
| پرونده فروش | دکمه خرید واقعی → نمای قفل‌شده؛ رصد استعلام‌ها (موجود v17.1) | hook موجود v16.3 + فلگ realbuy |
| استعلام تامین (rfqsmart) | تنها درگاه ایجاد/تغییر قیمت استعلامی (دور۱ قبل از برد، دور۲ بعد از برد) | دکمه میان‌بر از نمای قفل‌شده |
| موتور سود (fx) | بدون تغییر — ورودی purchases ریالی قطعی‌تر | سازگاری کامل v16.0 |
| بستانکاری (scoring v16.6) | همان ptfPayableUpsert با مبلغ ریالی تسعیرشده | بدون تغییر ساختار |
| وضعیت‌ها/کانبان (bridge/kanban) | st8/st9 از منبع واحد؛ گذار با ptfRfqSetStatus | صفر منطق موازی |
| مطالبات (recv) | باقیمانده ارزی + درصد وصول | ptfFxInvoiceSummary موجود |
| بایگانی/Win-Loss | رنگ قرمز بازنده از closeKind موجود | خواندنی |

## ۴) فازبندی و صف (قاعده ۱-۲ استوری هم‌خانواده)
**US-412 (هسته — متوسط) → US-413 (کوچک-متوسط) → US-414 (متوسط)** — سه اسپرینت متوالی هم‌خانواده «مالی-تامین». پنل توصیه می‌کند این خانواده **قبل از US-410/411** اجرا شود چون گزارش فرایندی فعال کارفرماست و مبنای سود را قطعی می‌کند؛ تصمیم نهایی با کارفرما.

**شماره‌گذاری بعدی: US-415** | این سند: مرجع اجرای هر سه اسپرینت.
