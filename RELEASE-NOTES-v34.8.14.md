# یادداشت انتشار CRM v34.8.14

تاریخ: ۱۴۰۵/۰۶/۵ (۲۰۲۶-۰۸-۲۷)

## فاز C3 — گام ۱: تکمیل یادآورها + مهاجرت سرنخ‌ها

- **یادآورها کامل شد:** مسیر «ثبت» (addReminder) هم به فرمان اتمیک سروری رفت؛ ماژول یادآور (ثبت/انجام‌شده/تعویق/حذف) صددرصد روی مسیر نازک است — دیگر هیچ push مجموعه‌ای برای این کلید وجود ندارد.
- **سرنخ‌ها (دومین ماژول):** `ptf_crm_leads` به رجیستری موجودیت اضافه شد؛ ذخیره (saveLead) و حذف (ptfLeadDelDo) با `entity_upsert`/`entity_delete` — با گاردهای dedup قبلی دست‌نخورده.
- **sanitizer سرور تقویت شد:** لیست اسکالر (shareUsers) و لیست نقشهٔ اسکالر (hist) عیناً و با سقف نگه داشته می‌شوند؛ ساختار لید/یادآور بی‌نقص عبور می‌کند.
- همهٔ مسیرها fallback امن legacy دارند (پرچم خاموش = رفتار قبلی).

### کنترل کیفیت
tester518 (۱۴ اطمینان + شبیه‌سازی sanitizer) — گیت CI سبز.
