# ریلیزنوت v24.6 — باگ حذف/ویرایش سرنخ (Lead)

## Root Cause
1. در فهرست/کانبان فقط دکمه 👁️ باز کردن کارت بود؛ ویرایش/حذف فقط داخل کارت بود و از دید کاربر «وجود ندارد».
2. `guards.js` به‌اشتباه `window.delLead` را wrap می‌کرد در حالی که API واقعی `leadDel` است.
3. توابع سرنخ روی `window` export صریح نداشتند (ریسک onclick در برخی مسیرها).

## Fix
- دکمه‌های ✏️ ویرایش و 🗑️ حذف مستقیم روی جدول و کانبان
- `leadDel` مقاوم + alias `delLead` + guard درست
- export سراسری API سرنخ‌ها

## Verify
1. سرنخ‌ها → جدول: دکمه‌های 👁️ ✏️ 🗑️
2. ویرایش → ذخیره
3. حذف → تأیید → از لیست حذف شود
