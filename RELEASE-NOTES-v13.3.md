# 📦 ریلیزنوت v13.3 — مجوز اسناد بایگانی + تسویه هنگام مختومه + پیوست‌ها و اقلام استعلام

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v13.2

---

## 🔐 US-323 — اسناد پرونده بایگانی‌شده: فقط ۴ نقش + رد تغییر

| قاعده | اجرا |
|---|---|
| چه کسانی؟ | فقط **ادمین، رییس هیات مدیره، مدیر بازرگانی، مدیرعامل** |
| افزودن سند | برای سایر نقش‌ها ناحیه آپلود قفل با پیام شفاف |
| حذف سند | برای سایر نقش‌ها پیام «مجاز نیست» |
| **رد تغییر** | هر افزودن/حذف روی پرونده بایگانی‌شده با **نام کاربر + نام کاربری + سند + تاریخ** ثبت می‌شود و در مودال پرونده، بخش «📋 رد تغییر اسناد (پس از بایگانی)» با رنگ سبز (افزودن) / قرمز (حذف) قابل مشاهده است + ثبت در audit سیستم |

## 💰 US-324 — مختومه با فاکتور = تسویه مطالبات (مگر نظر دیگر)

چون با ثبت فاکتور، مطالبات آن درخواست باز می‌شود، هنگام مختومه کردن پرونده‌ای که فاکتور دارد:
- اگر مانده وصول‌نشده باشد، سوال می‌شود: «**OK = تسویه‌شده ثبت شود (پیش‌فرض)** | Cancel = مانده باز بماند (نظر شما)»
- در حالت تسویه، پرداخت پایانی با برچسب «تسویه هنگام مختومه شدن پرونده» + نام ثبت‌کننده در مطالبات درج می‌شود (قابل ردیابی — autoSettle) + audit

## 📎 US-325 — پیوست‌های زمان ثبت استعلام حالا دیده می‌شوند

**ریشه باگ پیدا شد:** فرم ثبت استعلام فایل‌ها را در ۵ دسته ذخیره می‌کرد (استعلام/دیتاشیت/**عکس کالا**/نقشه/**سایر**) اما پنجره «مدیریت پیوست‌ها» ۴ دسته متفاوت را نشان می‌داد (…/**کاتالوگ**/…) → دسته‌های «عکس کالا» و «سایر مدارک» برای همیشه نامرئی بودند!
**رفع:** دسته‌های نمایش با فرم ثبت یکسان شد — همه پیوست‌های زمان ایجاد حالا با دکمه «📎 پیوست‌ها» روی ردیف درخواست قابل **مشاهده، دانلود، حذف و افزودن** هستند (حتی پس از صدور پیشنهاد).

## 📋 US-326 — اقلام درخواست: اختیاری هنگام ثبت + تایید ورود به کالاها

- **پس از ثبت استعلام** (فرم کامل): سوال «آیا همین حالا اقلام را وارد می‌کنید؟» — **الزامی نیست**؛ بعداً هم با دکمه «✏️ ویرایش استعلام و اقلام» روی ردیف ممکن است
- ویرایشگر اقلام با هر سه مسیر: دستی / اکسل / دستیار (چیپ 🤖) — اقلام ثبت‌شده برای هر کاربر مراجعه‌کننده قابل مشاهده و ویرایش است
- **ورود به فهرست کالاها فقط با تایید کاربر:** پس از ذخیره اقلام، سوال «کالاها به فهرست کالاها هم اضافه شوند؟» — با تایید، هر کالا **کد یکتای خود** را می‌گیرد (کدساز اصلاح‌شده v12.5) و **کالای مشابه قبلاً ثبت‌شده تکرار نمی‌شود** (ضدتکرار روی شرح/مشخصات)
- گزارش شفاف: «+ N کالای جدید با کد یکتا ثبت شد»

## 🧪 کیفیت
تستر جدید `tester53` ۲۰/۲۰ ✅ | رگرسیون **۴۰ تستر** ✅ | audit صفر خطا ✅

## 📁 فایل‌های تغییریافته
`crm/projects.js` (مجوز + رد تغییر + نمایش) · `crm/archive.js` (گارد حذف + رد تغییر) · `crm/salesfiles.js` (تسویه هنگام مختومه) · `crm/inqreader.js` (دسته‌های پیوست + تایید ورود کالا) · `crm/bridge.js` (پیشنهاد ورود اقلام پس از ثبت) · `crm/index.html`/`sw.js` (v13.3) · `_tools/uat/tester53` (جدید)
