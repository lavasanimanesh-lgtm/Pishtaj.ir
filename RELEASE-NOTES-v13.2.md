# 📦 ریلیزنوت v13.2 — US-322: بازطراحی کامل «پرونده‌های فروش» + تولد «بایگانی»

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v13.1 | ماژول جدید: `crm/salesfiles.js`

---

## 📁 نقش جدید پرونده‌های فروش — دقیقاً طبق فرآیند ابلاغی شما

### ① ایجاد خودکار
به محض **ثبت هر پیشنهاد** (فنی/مالی/فنی-مالی)، رکوردی با **همان شماره درخواست** در پرونده‌های فروش خودکار ساخته می‌شود — بدون هیچ اقدام دستی. (دکمه قدیمی «+ پرونده از CO برنده» حذف شد.)

### ② کشوی اسناد منضم
کلیک روی هر پرونده → منوی کشویی با **همه اسناد آن درخواست**:
- 📄 پیشنهادها — **همیشه آخرین رویژن** (اتصال زنده؛ هر تغییر/رویژن جدید خودکار منعکس می‌شود)
- ✉️ نامه‌ها — در فرم ثبت نامه، گزینه «📁 درخواست …» به کشوی لینک اضافه شد؛ نامه لینک‌شده در کشو ظاهر می‌شود
- 🧾 فاکتورهای ثبت‌شده حسابداری
- 📎 اسناد متفرقه — **افزودن از بیرون** با ویجت آپلود در همان کشو (+ حذف تکی با پاکسازی ابری)

### ③ مختومه‌سازی — دو مسیر

**مسیر الف: بدون فاکتور (عدم برنده شدن / هر دلیل):**
1. ثبت دلیل توسط کاربر
2. اگر سند ابری دارد → سوال: «آیا می‌خواهید قبل از حذف دانلود کنید؟» — بله = پنجره دانلود تک‌تک اسناد؛ خیر = ادامه
3. **هشدار نهایی** → اسناد متفرقه از فضای ابری پاک می‌شوند (متادیتای نام/تاریخ برای آمار می‌ماند)
4. رکورد از پرونده‌های فروش حذف و به **بایگانی** منتقل می‌شود — با آمار مدیریتی (تعداد پیشنهاد/نامه/فاکتور، ارزش CO، دلیل شکست، تاریخ و مسئول)

**مسیر ب: فاکتور ثبت‌شده توسط حسابدار:**
- مختومه = **پایان پروژه و تسویه کامل** → کل پرونده **با تمام ضمایم** به بایگانی منتقل می‌شود
- اعلان کارتابل به مدیران ارشد در هر دو مسیر

### ④ «بایگانی» (نام جدید پرونده‌های پروژه)
ماژول پرونده‌های پروژه در همه‌جا (سایدبار/پنل/دسترسی‌ها/گزارش‌ها) به **«بایگانی»** تغییر نام یافت — مقصد رکوردهای مختومه برای ثبت آماری و تحلیل‌های مدیریتی. کاربران مجاز مثل قبل می‌توانند اسناد منضم را از داخل پرونده بایگانی حذف/ویرایش کنند. پرونده‌های قدیمی این ماژول دست‌نخورده‌اند.

## 🧪 کیفیت
| سنجه | نتیجه |
|---|---|
| تست سناریو (شبیه‌ساز مرورگر) | ✅ ۱۱/۱۱ — ایجاد خودکار، ضدتکرار، کشو با رویژن، هر دو مسیر مختومه، مودال دانلود، purge ابری، آمار بایگانی |
| audit.py | ✅ ۰ خطا |
| تستر جدید `tester52` | ✅ ۲۲/۲۲ PASS |
| رگرسیون کامل | ✅ **۳۹ تستر** همه PASS |

## 📁 فایل‌های تغییریافته
`crm/salesfiles.js` (جدید — ۲۸۰ خط) · `crm/index.html` (نام بایگانی + ثبت اسکریپت + VER v13.2) · `crm/rbac.js` (روتینگ به نسخه جدید) · `crm/projects.js`/`perms.js`/`listtools.js`/`theme.js` (نام بایگانی) · `crm/sw.js` · `_tools/uat/tester52` (جدید)

## 📌 یادداشت
- رکوردهای قدیمی پرونده‌های فروش (ساخته‌شده از CO برنده) در فهرست جدید نمایش داده می‌شوند و قابل مختومه‌سازی‌اند
- «کاربران مجاز» برای ویرایش اسناد بایگانی فعلاً همان دسترسی ماژول prj در مدیریت دسترسی‌هاست؛ اگر محدودیت خاص‌تری خواستید (مثلا فقط ادمین/رییس) بفرمایید
