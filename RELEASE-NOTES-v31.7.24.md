# Release Notes — v31.7.24

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.24
**نوع:** 🔴 رفع باگ بحرانی داده — پیشنهاد مالی «ذخیره می‌شود» ولی در فهرست دیده نمی‌شود (BUG-CODE-DUP-003)

---

## 🎯 شکایت کاربر

«چند بار یک پیشنهاد مالی را ثبت و ذخیره کردم اما در پیشنهادات مالی نمی‌بینم — یعنی در سیستم ذخیره نمی‌شود.»

## 🔍 علت ریشه‌ای (RCA) — زنجیره ۴حلقه‌ای، با E2E اثبات شد

نکته کلیدی: پیشنهاد **واقعاً ذخیره می‌شد** — اما نه به‌عنوان رکورد جدید، بلکه **روی یک پیشنهاد قدیمی بازنویسی می‌شد**:

1. **سرور کدساز هنگام اسکن شماره‌های آفر (TO/CO/TC) فقط `crm/data/ptf_crm_offers.json` را می‌خواند — نه `crm/data/sync/`** که داده واقعی آنجاست. (رفع v31.7.20 فقط شامل درخواست/کالا/… بود؛ بلاک جداگانه آفرها از قلم افتاده بود — **مصداق چهارم شکاف data-dir**.)
2. → شمارنده CO از داده واقعی عقب می‌ماند و **شماره‌های قبلاً مصرف‌شده را دوباره رزرو می‌کرد**. بازتولید E2E: با وجود `PTF-CO-1405-0112` در sync، سرور `0100` می‌داد!
3. → در `offerSave`، شماره تکراری یعنی «این پیشنهاد از قبل هست» (idx>-1) → **به‌جای افزودن رکورد جدید، آفر قدیمیِ هم‌شماره overwrite می‌شد**.
4. → کاربر «ذخیره» می‌زد، toast موفقیت می‌دید، اما در فهرست رکورد جدیدی ظاهر نمی‌شد — فقط یک پیشنهاد قدیمی بی‌صدا عوض شده بود. تلاش‌های مکرر او هم هر بار همان آفر قدیمی را بازنویسی می‌کرد.

⚠️ این یعنی احتمالاً **یک یا چند پیشنهاد قدیمی هم در این ماجرا بازنویسی شده‌اند** (بخش بازیابی را ببینید).

## ✅ اصلاحات (دفاع دو لایه)

| # | فایل | تغییر | شدت |
|---|------|--------|-----|
| 1 | `api/codegen.php` | **رفع ریشه:** اسکن شماره آفرها اکنون هر دو مسیر `crm/data/` و `crm/data/sync/` را می‌بیند — E2E: با `0112` موجود در sync، رزرو جدید از **`0113`** شروع می‌شود (قبلاً `0100`) | 🔴 P0 |
| 2 | `crm/offers.js` (`offerSave`) | **گارد کلاینت:** پیشنهاد «جدید» با شماره‌ای که قبلاً وجود دارد، دیگر بی‌صدا overwrite نمی‌کند: تا ۵ بار شماره تازه از سرور می‌گیرد (با toast اطلاع‌رسانی شماره اصلاح‌شده)؛ اگر نشد، **ثبت با پیام صریح مسدود می‌شود** (متن فرم با draftx v31.7.23 محفوظ می‌ماند) + audit هر دو حالت | 🔴 P0 |
| 3 | مسیر ویرایش/رویژن واقعی | کاملاً دست‌نخورده — گارد فقط برای `editMode === 'new'` فعال است | — |

## 🛟 بازیابی پیشنهادهای بازنویسی‌شده (اقدام کارفرما)

1. از کاربر شاکی بپرسید حدوداً چه زمانی ثبت می‌کرد؛ در **بایگانی آماری/گزارشات → audit** ردپای «پیشنهاد … اصلاح شد» های مشکوک همان بازه را ببینید.
2. نسخه قبلی آفرهای بازنویسی‌شده در **بک‌آپ ساعتی/روزانه سرور** (تنظیمات → پشتیبان → کلید `ptf_crm_offers`) موجود است — daily مربوط به قبل از تاریخ شکایت را بازبینی کنید.
3. پس از استقرار v31.7.24، کاربر پیشنهادش را دوباره ثبت کند — این‌بار با شماره سالم در فهرست ظاهر می‌شود.

## 📊 نتایج گیت

- تستر جدید `tester199-offer-vanish`: **۱۳/۱۳ PASS** — بازتولید دقیق شکایت: شماره تکراری → قبلاً overwrite، حالا رکورد جدید با `0113` و آفر قدیمی دست‌نخورده؛ + سناریوی بدترین حالت (block بدون overwrite)
- **E2E سرور PHP واقعی:** قبل از fix `0100` (برخورد)، بعد از fix `0113` ✅
- رگرسیون کامل: **177/177 فایل PASS — 4064/4064 چک PASS** | `audit.py`: PASS | `php -l`: سالم

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| آفرهای موجود | هیچ migration — فقط جلوگیری از تکرار/overwrite جدید | صفر |
| ویرایش و رویژن (US-223) | دست‌نخورده (گارد فقط editMode=new) | صفر |
| سیاست CODEGEN-OFFER-SERVER (v31.6.27) | تقویت شد — رزرو سرور حالا واقعاً همه شماره‌های موجود را می‌بیند | مثبت |
| گاردهای v31.7.20 (درخواست/کالا) | سالم — tester195 سبز | صفر |

## 📋 راستی‌آزمایی توسط کارفرما (۴ دقیقه — همان شکایت)

1. دو پیشنهاد مالی پشت‌سرهم ثبت کنید → هر دو با شماره‌های متوالی جدید در فهرست ظاهر شوند.
2. با کاربر شاکی: پیشنهادش را دوباره ثبت کند → در فهرست ببیند؛ شماره‌اش با هیچ پیشنهاد قبلی یکی نباشد.
3. audit را چک کنید — اگر پیام «شماره پیشنهاد اصلاح شد» دیدید یعنی گارد یک برخورد واقعی را خنثی کرده (نشانه سلامت، نه خطا).

---
*توسعه‌دهنده: PTF Development Team — 2026-07-20*
