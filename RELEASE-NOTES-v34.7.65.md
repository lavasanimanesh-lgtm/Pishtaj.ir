# RELEASE NOTES — v34.7.65 (۱۴۰۵/۰۵/۳۰ — 2026-08-21)

## قالب‌بندی کامل «متن آماده روی سربرگ» — همان ابزار نامهٔ صادره

تمام تنظیمات متنی که برای «نامهٔ صادره» وجود دارد، حالا برای «📄 متن آماده روی سربرگ»
نیز در دسترس است.

### ۱) نوار ابزار غنی (قالب‌بندی بخشی از متن)

بالای کادر Paste، نوار ابزار کامل اضافه شد:

| گروه | ابزارها |
|---|---|
| فونت | یاقوت / وزیرمتن / تاهوما / آریال / تایمز |
| اندازه | ۹ تا ۲۴ (شامل کوچک‌تر از حد پیش‌فرض) |
| استایل | بولد، ایتالیک، زیرخط، خط‌خورده |
| رنگ | رنگ متن (پالت) + هایلایت زرد/سبز/حذف |
| چینش | راست‌چین / وسط‌چین / چپ‌چین / دوطرفه (برای هر بخش) |
| فاصلهٔ خطوط | ۱٫۲ تا ۳ (برای هر پاراگراف) |
| سایر | فهرست نقطه‌ای/شماره‌ای، تورفتگی، پاک‌سازی قالب، جدول، تصویر در متن |

### ۲) تنظیمات کل متن (🎛 تنظیمات دستی قالب)

- **اندازهٔ فونت** (از ۹ — کوچک‌تر از پیش‌فرض ۱۳)
- **فاصلهٔ خطوط** (پیش‌فرض ۲٫۱)
- **فونت متن** (پیش‌فرض یاقوت)
- **چینش** (پیش‌فرض بر اساس زبان)
- **بولد / ایتالیک** کل متن
- **حاشیه‌های صفحه** (بالا/راست/پایین/چپ به میلی‌متر)

همهٔ این تنظیمات روی خروجی چاپ/PDF اعمال می‌شوند و ادیتور به‌صورت زنده پیش‌نمایش
آن‌ها را نشان می‌دهد.

### فنی

- توابع هستهٔ ویرایشگر (`letEditorExec`، انتخاب/بازیابی، `wrapSel`، `setBlockStyle` و…)
  پارامتر `editorId` گرفتند تا یک هستهٔ مشترک برای هر دو ادیتور (نامه و سربرگ) کار کند.
- استایل‌های inline همچنان از `letSafeBodyHtml` (لیست سفید امن) عبور می‌کنند.
- paste/drop تصویر و جدول برای ادیتور سربرگ هم وصل شد.

### تست

- تستر جدید `tester467-v34.7.65-letterhead-formatting.js` (در گیت CI).
- **گیت CI: ۸۳ PASS / ۰ FAIL** · نگهبان معماری: PASS.

### UAT پیشنهادی

1. «متن آماده روی سربرگ» را باز کنید؛ متن را Paste و بخشی از آن را بولد/رنگ/فونت کنید.
2. یک پاراگراف را وسط‌چین و فاصلهٔ خطوطش را ۱٫۵ کنید.
3. در تنظیمات دستی، اندازهٔ ۱۰ و حاشیه‌ها را تغییر دهید → چاپ/PDF را ببینید که همه اعمال شده باشد.
