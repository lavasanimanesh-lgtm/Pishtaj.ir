# 📊 گزارش رگرسیون v31.7.14

**تاریخ:** ۱۴۰۵/۰۴/۲۸ (2026-07-19)
**نسخه:** v31.7.14
**ابزار:** `_tools/run-full-regression.js` + `python3 _tools/audit.py` (۱۰ بخش)

## ✅ نتیجه نهایی

| معیار | مقدار | وضعیت |
|--------|-------|--------|
| تعداد تسترها | **۱۶۷** (+۱: tester189-users-vanish) | — |
| فایل‌های PASS | **۱۶۷** | ✅ ۱۰۰٪ |
| چک‌های PASS | **۳,۹۱۰** (+۱۳) | ✅ ۱۰۰٪ |
| چک‌های FAIL | **۰** | ✅ صفر |
| audit.py (۱۰ بخش) | **PASS** | ✅ |
| `php -l api/crm.php` + `node --check rbac.js` | **PASS** | ✅ |

## 🆕 پوشش تست جدید (tester189 — شامل E2E سرور PHP واقعی)

- بازتولید دقیق باگ: users_sync با رکورد بی‌هش → قبل از fix کاربر حذف می‌شد؛ حالا هش از رکورد موجود سرور بازیابی می‌شود
- login موفق حسابدار پس از sync (سناریوی گزارش کارفرما)
- merge کلاینت: هش محلی زنده می‌ماند؛ هش سروری موجود بازنویسی نمی‌شود؛ کاربر فقط-محلی حفظ
- کاربر واقعاً جدید بی‌هش → `dropped` گزارش می‌شود (شفافیت به‌جای حذف بی‌صدا)

## 📁 فایل‌های تغییریافته

`api/crm.php`، `crm/index.html`، `crm/rbac.js`، `crm/sw.js`، `crm/clear-cache.html`، `_tools/uat/tester189-users-vanish.js` (جدید)

## 🚦 وضعیت گیت‌ها

- **گیت فنی: سبز** ✅
- **گیت انسانی: معوق** ⏳ — پس از استقرار: بازیابی کاربر حسابدار طبق بخش «بازیابی» RELEASE-NOTES + تست ورود او از دو مرورگر + بررسی data-health-check.
