# 🚀 ریلیز v14.5 — اسپرینت د «آماده بهره‌برداری واقعی»

**تاریخ:** ۱۴۰۵/۰۴/۱۷ | **اسپرینت:** د از برنامه ۶ اسپرینتی مصوب
**وضعیت کیفیت:** کد کامل + tester65 نوشته شده — ⚠️ اجرای رگرسیون نهایی و ساخت zip به دلیل قطعی موقت محیط اجرا به ابتدای پیام بعدی موکول شد (توضیح در انتهای سند)

---

## 📋 ابتدا: US-381 در بک‌لاگ ثبت شد (اجرا: اسپرینت هـ)

**بازخورد شما:** دستیار کالاهای تکراری را درست رد کرد (ضدتکرار سالم است ✅) ولی پیامی درباره تکراری بودن نداد.
**ریشه‌یابی:** مسیر «اجرای سه‌گانه» تکراری‌ها را بی‌صدا رد می‌کند و شمار ردشده‌ها گزارش نمی‌شود.
**AC ثبت‌شده:** پیش‌نمایش دو گروهِ «🆕 جدید» / «🔁 تکراری (با کد قبلی)» قبل از ثبت + **اخذ تایید کاربر** + پیام دقیق «n جدید ثبت شد + m تکراری رد شد» + پیام صریح وقتی همه تکراری‌اند + یکدست‌سازی سایر مسیرها. تخصیص: اسپرینت هـ (هم‌خانواده پیام‌های ضدتکرار US-363/380).

---

## ✅ محتوای ریلیز (۴ استوری)

### US-377 — 🔴 پاک‌سازی داده‌های آزمایشی (Go-Live Reset)
ماژول جدید `golive.js` — دکمه قرمز در تنظیمات، بخش «منطقه خطر» (فقط ادمین):
- **پاک می‌شود:** درخواست‌ها، مشتریان، سرنخ‌ها، اقلام، پیشنهادها، پرونده‌های فروش، بایگانی، درخواست‌های تامین، فاکتورها، تنخواه، چک‌ها، نامه‌ها، قراردادها، یادآورها، اعلان‌ها، صف‌ها، آرشیو حذف‌شده‌ها (۲۵ کلید داده تراکنشی).
- **حفظ می‌شود:** کاربران و نقش‌ها، مجوزها، تنظیمات (بات/تم/بک‌آپ)، امضاها، متن‌های آماده، ترجیحات ظاهری.
- **انتخابی با چک‌باکس ادمین:** کاتالوگ کالاها، تامین‌کنندگان، رد پای دوره آزمایشی.
- **تایید سه‌مرحله‌ای:** پیش‌نمایش با شمار رکوردها → تایپ کلمه «پاکسازی» → confirm نهایی.
- **بک‌آپ اجباری:** فایل دانلودی + نسخه سروری pre-golive — بدون بک‌آپ موفق اجرا نمی‌شود.
- پاک‌سازی با سینک سروری انجام می‌شود تا داده آزمایشی از سرور برنگردد + شمارنده‌های کدساز از نو + رویداد «🚀 شروع بهره‌برداری واقعی» در audit.

### US-368 — نقش‌های اصلی فقط یک‌بار قابل ایجاد 👥
- نقش‌های یکتا: **رییس هیات مدیره، مدیرعامل، مدیر بازرگانی** (نقش‌های چندنفره مثل کارشناس فروش آزاد).
- در فرم کاربر جدید، نقش اشغال‌شده **غیرفعال + نام دارنده فعلی** نمایش داده می‌شود.
- سد دوم در ذخیره (دور زدن دراپ‌داون ناممکن) با پیام شفاف؛ حذف دارنده → نقش آزاد می‌شود.

### US-376 — ارسال مجدد پیامک اطلاعات ورود 📱
- دکمه «📱 ارسال مجدد» روی هر کاربر در پنل کاربران (فقط ادمین/رییس هیات مدیره).
- چون رمز ذخیره‌شده hash است، **رمز موقت جدید** ساخته و پیامک می‌شود + **الزام تغییر رمز در اولین ورود** (با تغییر رمز، الزام خودکار برداشته می‌شود).
- امکان اصلاح شماره موبایل پیش از ارسال + ضداسپم ۳ ارسال در ساعت per کاربر + audit + سینک سروری.

### US-371 — قفل کامل پرونده مختومه بدون فاکتور 🔒
- پرونده‌های بایگانی با `مختومه بدون فاکتور (lost)`: ناحیه آپلود برای **همه نقش‌ها** (حتی مدیران) غیرفعال با پیام قرمز شفاف.
- **سد برنامه‌ای** در مسیر آپلود هم اضافه شد (نه فقط UI) — طبق AC2.
- پرونده‌های تسویه‌شده با فاکتور مشمول نیستند؛ قواعد US-323 (دانلود/حذف مدیران) دست نخورد.

---

## 🔬 کیفیت و وضعیت تحویل

| مورد | وضعیت |
|-------|--------|
| کد هر ۴ استوری | ✅ کامل اعمال شد (golive.js جدید + rbac/sms/projects/inqreader/index/sw) |
| tester65-v145 | ✅ نوشته شد (۳۸ چک شامل تست اجرایی ptfRoleHolder) |
| نسخه و کش | ✅ VER=v14.5، کش `ptf-crm-v14.5`، cache-bust ۵ فایل + golive.js در SHELL |
| رگرسیون ۵۲ تستر + audit + zip | ⚠️ **در انتظار** — محیط اجرای فرمان (ترمینال سندباکس) وسط اسپرینت از دسترس خارج شد و به هیچ فرمانی پاسخ نمی‌دهد؛ کد از مسیر ابزار فایل با بازبینی دستی اعمال و کنترل شد |

### ⚠️ شفاف‌سازی و برنامه جبران
طبق تعهد «ریشه‌یابی یا مسیر جایگزین»: مسیر جایگزین (ابزار فایل) استفاده شد و **هیچ کاری از دست نرفت** — همه تغییرات کد و تستر ذخیره شده‌اند. سه گام باقیمانده که فقط به ترمینال نیاز دارند:
1. اجرای tester65 + رگرسیون کامل ۵۲ تستر
2. python3 _tools/audit.py
3. حذف zip قدیمی + ساخت `pishtaj-release-v14.5.zip`

**در اولین پیام بعدی شما (حتی یک کلمه «ادامه»)، این سه گام بلافاصله اجرا و بسته نهایی تحویل می‌شود.** تا آن زمان لطفا نسخه v14.4 روی هاست بماند.

## 🔜 بعدی طبق برنامه مصوب
پس از تحویل نهایی v14.5: **نقشه راه قبلی «سود و مهلت‌ها»** = US-347 (حاشیه سود در CO) + US-348 (مهلت پاسخ درخواست) + US-352 (سقف اعتبار مشتری) — حالا با داده واقعی معنادار است.
