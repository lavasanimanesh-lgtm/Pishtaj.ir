# بررسی منطق «حقوق سهامداران موظف» و «حقوق پرسنل دیگر» در CRM

- تاریخ: ۱۴۰۵/۰۶/۱۹ (2026-09-10)
- شاخه: `arena/01a08c54-pishtaj-ir` — مبنا `7d84f1b`
- نسخه‌ی CRM در زمان بررسی: `v34.38.19` (VERSION.json)
- دامنه: بررسی، مقایسه با استاندارد، شناسایی باگ و امکان‌سنجی اصلاح — نه بازنویسی منطق.
- وضعیت کد: دو اصلاحِ امنِ نمایشی اعمال شد (بخش ۴). بقیه‌ی اصلاحات نیازمند تصمیم/تأیید کارفرماست (بخش ۵).

---

## ۰) خلاصه مدیریتی

حقوق «سهامدار موظف» در این CRM به‌صورت **دو سند جداشده** مدل شده است که از نظر حسابداری تعهدی درست است:

1. **مطالبه/هزینه (accrual)** — یک ردیف `sharetx` از نوع `salary` + یک ردیف `opex` هم‌جنس (دسته «حقوق و دستمزد») که با نشانه‌های `shareholderSalary / shareTx / recurringKey` به هم گره خورده‌اند. این ردیف **هزینهٔ سال** را می‌سازد ولی **خروج نقدی** ندارد.
2. **پرداخت واقعی (payment)** — با یک ردیف `draw` (نوع پرداخت = `paymentFor:'salary'` / `salaryMonth`) ثبت می‌شود و **خروج خزانه** دارد.

نقاط قوت سرور (idempotency، قفل سال مالی، سنگ‌قبر `tombstone`، ژورنال اصلاحات، کلیدهای پایدار `SHT-SAL/OPX-SAL/OPXR-SAL`) قوی و هم‌راستا با استاندارد است. اما در **لایهٔ نمایش و نقدی** چند شکاف وجود دارد که مهم‌ترین آن‌ها:

- **باگ اصلی (نقدی):** پرداخت حقوق (draw) در محاسبهٔ «موجودی نقد پایان سال» (`cashEnd`) به‌عنوان خروج نقدی لحاظ نمی‌شود؛ در نتیجه پس از پرداخت حقوق، `cashEnd` بیش‌ازحد واقعی گزارش می‌شود. (بخش ۳ — B2)
- **باگ نمایشی (اصلاح‌شده در این جلسه):** پرداخت حقوق به‌اشتباه در ستون «علی‌الحساب/مانده قابل تسویهٔ امسال» ظاهر می‌شد. (بخش ۴)
- «حقوق پرسنل دیگر» (غیرسهامدار) موجودیت درجه‌یک ندارد و فقط به‌صورت دستی در `opex` یا `petty` ثبت می‌شود که خطر **دوباره‌شماری** دارد (گارد فعلی صرفاً هشداری است). (B4، B7)

---

## ۱) وضعیت فعلی (منطق پیاده‌سازی‌شده)

### ۱.۱ چرخهٔ کامل حقوق سهامدار موظف

| مرحله | کد | رفتار |
|---|---|---|
| شرط واجد بودن | `api/sales-domain.php` (register)، `crm/shareholders.js:290,353` | سهامدار باید `active!==false` و `duty===true` و `salary>0` باشد. |
| ثبت حقوق (تک‌کارتی) | دکمه «📅 ثبت حقوق» → `ptfShareRegisterSalary` → `register_shareholder_salary` | یک `sharetx` از نوع `salary` + یک `opex` از دسته «حقوق و دستمزد» **به‌صورت اتمیک** می‌سازد. |
| تطبیق گروهی | `reconcile_shareholder_salaries` / `reconcile_recurring_opex` (نقش `SD_FIN_ROLES`) | برای ماه جاری همهٔ سهامداران واجد، سندها را می‌سازد/به‌روز/فعال می‌کند؛ سنگ‌قبر و حذف را محترم می‌شمارد و بدون restore صریح، ردیف terminal را زنده نمی‌کند. |
| پرداخت واقعی | `ptfSharePaySalary` → `register_shareholder_draw` با `salaryMonth` | draw با `paymentFor:'salary'` + `salaryMonth`؛ نوع قدیمی `salary_payment` دیگر رکورد جدید نمی‌سازد (فقط legacy). |
| مانده سهامدار | `ptfShareholderBalance` (`shareholders.js:174-194`) | `salary/credit/profit` = بستانکار؛ `draw/advance/debit/salary_payment` = بدهکار؛ فراخوان و صندوق جدا. |

**سند‌های تولیدشده (نمونه):**

```jsonc
// sharetx
{ "cd":"SHT-SAL-…", "type":"salary", "amt":…, "month":"1405/06",
  "recurringKey":"salary:SH1:1405/06", "status":"active",
  "serverReconciled":true, "t":"1405/06/01" }
// opex
{ "cd":"OPX-SAL-…", "_opexRowId":"OPXR-SAL-…", "cat":"حقوق و دستمزد", "amt":…,
  "month":"1405/06", "shareTx":"SHT-SAL-…", "shareholderSalary":true,
  "recurringKey":"salary:SH1:1405/06", "serverMaterialized":true,
  "serverReconciled":true, "isOfficial":true|false }  // isOfficial از پروفایل salaryOfficial (v34.38.5)
```

**Idempotency:** اگر هویت `recurringKey` یا `shareTx` موجود باشد، سرور `alreadyRegistered:true` برمی‌گرداند و چیزی دوباره نمی‌سازد (`api/sales-domain.php ~2095-2105`).

### ۱.۲ کلاس‌بندی هزینه در OPEX

`crm/opex.js`:

```js
// opex.js:179
function isShareholderSalaryOpex(x){
  return !!(x && (x.shareholderSalary===true || x.shareTx ||
          String(x.recurringKey||'').indexOf('salary:')===0));
}
```

نکتهٔ مهم و **عمدی** (کامنت `opex.js:177-181`): یک ردیف دستیِ معمولی که فقط دسته‌اش «حقوق و دستمزد» باشد، **حقوق سهامدار شمرده نمی‌شود**. این تصمیم برای جلوگیری از دوباره‌شماری است، اما نتیجه‌اش این است که حقوقِ دستیِ ثبت‌شده به‌عنوان «هزینهٔ نقدی» (نه تعهدی) رفتار می‌کند.

`ptfOpexSumFiscal` (`opex.js:187-201`) خروجی‌ها:

- `totalSalary` — فقط ردیف‌های نشان‌دارِ حقوق (تعهدی).
- `totalCash` — ردیف‌های بدون نشان و بدون `dealRef` (نقدی).
- `total = totalUnlinked` — **شامل حقوق هم هست** (پس حقوق در سود سال کسر می‌شود).

### ۱.۳ جایگاه در سود/نقد سال مالی

`crm/fiscal.js`:

```js
// fiscal.js:272 — سود خالص (تعهدی)
net = projectProfit − ox.total − pettyStandalone.total + amendTotal + cover.netBenefit;
// ox.total شامل totalSalary است ⇒ حقوقِ ثبت‌شده سود را کم می‌کند.

// fiscal.js:483-485 — خروجی نقدی
outflowsTotal = supplierInvoices + unallocatedPayments + independentCheques
              + opex(totalCash) + petty + coverCommission + chairRepay;
netCash = receipts + shareholderInject − outflowsTotal;
cashEnd = openingCash + netCash;
// نکته: draw/advance/salary_payment اینجا غایب‌اند (بخش ۳ — B2).

// fiscal.js:497-529 — توزیع سود
advRows = شaretx های draw/advance/debit همان سال؛
settleYear = gross − adv;  // (پس از اصلاح این جلسه، drawِ حقوق از advRows خارج شد)
```

---

## ۲) مقایسه با استاندارد

| اصل استاندارد (CRM/حسابداری) | وضعیت این سیستم | امتیاز |
|---|---|---|
| تفکیک **تعهد (هزینه)** از **پرداخت (نقد)** | ✅ دو سند جدا: salary-claim + draw | عالی |
| منبع واحد و سرور-authoritative برای ثبت مالی | ✅ دستورات دامنه‌ای؛ مرورگر نمی‌سازد/تعمیر نمی‌کند | عالی |
| Idempotency (ثبت تکراری = همان سند) | ✅ `recurringKey` + `alreadyRegistered` + کلید پایدار | عالی |
| عدم زنده‌سازی خودکار سند حذف‌شده | ✅ tombstone + restore صریح + ژورنال corrections | عالی |
| قفل دورهٔ مالی (عدم ثبت در سال قفل‌شده) | ✅ `sd_is_locked` (ثبت/پرداخت/تطبیق) | عالی |
| عدم دوباره‌شماری هزینه | ⚠️ برای حقوق سهامدار قوی (نشانه‌ها)، برای حقوق پرسنل دیگر فقط گارد هشداری | متوسط |
| **انعکاس پرداخت واقعی در جریان نقد** | ❌ پرداخت حقوق در `cashEnd` لحاظ نمی‌شود (B2) | ضعیف |
| برچسب/گزارش صحیح پس از پرداخت | ⚠️ «حقوق تعهدی بدون خروج نقدی» حتی پس از پرداخت کامل می‌ماند (B3) | متوسط |
| حقوق‌پردازی (payroll) برای پرسنل غیرسهامدار | ❌ موجودیت ندارد؛ فقط دستی | غایب |

**جمع‌بندی مقایسه:** معماری «مطالبه + پرداخت» در سطح سرور هم‌سطح استاندارد است؛ شکاف اصلی در **پای تعهدی/نقدی** (رابط میان پرداخت و جریان نقد) است، نه در ثبت سند.

---

## ۳) باگ‌ها و ریسک‌ها

### B2 — [بالا] پرداخت حقوق در جریان نقدی (`cashEnd`) لحاظ نمی‌شود
- **شواهد:** `fiscal.js:483-485` در `outflowsTotal` هیچ draw/advance/salary_payment ندارد؛ اما `treasury.js:313` همان draw را **به‌عنوان خروج خزانه** می‌شمارد. متن راهنمای داشبورد (`fiscal.js:611`) می‌گوید «حقوق ثبت‌شده **تا زمان draw** فقط هزینه/مطالبه است و خروج نقدی نیست» — یعنی پس از draw انتظار خروج نقدی می‌رود، ولی در کد پیاده نشده.
- **اثر:** حقوقِ پرداخت‌شده نه در `opexCash` (چون تعهدی است) و نه در `outflowsTotal` (چون draw شمرده نمی‌شود) می‌آید؛ پس `cashEnd` و `netCash` و در نتیجه «مازاد بر کف» و «قابل تقسیم» **بیش از واقع** گزارش می‌شوند. ناسازگاری بین خزانه (draw = خروج) و داشبورد نقدی.
- **شدت:** بر تصمیم تقسیم سود اثر می‌گذارد. باید اصلاح شود ولی چون فرمول «منطق نقدی» مصوب کارفرماست و بر `cashEnd/distributable` اثر دارد، نیازمند تأیید + تستر قفل‌شده در `run-ci-gate` است (بخش ۵ — F1).

### B3 — [متوسط] «حقوق تعهدی (بدون خروج نقدی)» حتی پس از پرداخت، کامل می‌ماند
- **شواهد:** `salaryClaims = +d.opexSalaryTotal` (`fiscal.js`، KPI و گزارش CSV/HTML)؛ ردیف opex حقوق پس از پرداخت هم `active` می‌ماند و draw سند جداست؛ هیچ‌جا «پرداخت‌شده/پرداخت‌نشده» تفکیک نمی‌شود.
- **اثر:** پس از پرداخت حقوق، کارت و گزارش هنوز کل مبلغ را «بدون خروج نقدی» نشان می‌دهند — گمراه‌کننده.
- **اصلاح:** مشتق «پرداخت‌شده» از drawهای `paymentFor:'salary'` همان سال و کم کردن از `salaryClaims` (یا نمایش دو ستون). (بخش ۵ — F2)

### B4 — [متوسط] دوباره‌شماری احتمالی حقوق پرسنل دیگر (تنخواه + هزینهٔ جاری)
- **شواهد:** `net` هم `ox.total` و هم `pettyStandalone.total` را کم می‌کند (`fiscal.js:272`)؛ تنخواه بدون تفکیک دسته جمع می‌شود (`fiscalPettyStandalone`)؛ گارد فقط `ptfOpexSuspectedDuplicates` (`opex.js:397`) با وضعیت `opex-petty-twin` و `manual-vs-recurring` است که در «کیفیت داده» **هشداری/فقط‌خواندنی** است، نه مانع سخت.
- **اثر:** اگر حقوق یک پرسنل یک‌بار در تنخواه و یک‌بار در opex ثبت شود، دو بار از سود کسر می‌شود.
- **اصلاح:** بستن نرم (confirm) هنگام ثبت opex دستیِ «حقوق و دستمزد» که هم‌ماه/هم‌دسته/هم‌مبلغ با تنخواه است — دقیقاً همان الگوی `clashRec` موجود (`opex.js:609-613`). (بخش ۵ — F3)

### B5 — [کم، اصلاح‌شده] برچسب خزانه برای پرداخت حقوق
- **شواهد (قبل):** `treasury.js:323-324` هر draw را «برداشت سهامدار» برچسب می‌زد؛ فقط `salary_payment` قدیمی «پرداخت حقوق» بود، در حالی که پرداخت امروز با draw است. **اصلاح شد** (بخش ۴).

### B1 — [کم، اصلاح‌شده] پرداخت حقوق در «مانده قابل تسویهٔ امسال» کسر می‌شد
- **شواهد (قبل):** `fiscal.js:499-501` همهٔ drawها را در `advRows` می‌آورد، در حالی که کامنت همان بلوک (فاز ۲) تصریح کرده بود حقوق نباید از سهم سود کسر شود. **اصلاح شد** (بخش ۴).

### B6 — [اطلاع] عدم محدودیت «ماه جاری» در ثبت تک‌کارتی حقوق
- `register_shareholder_salary` هر ماهِ قفل‌نشده‌ای را می‌پذیرد، در حالی که `reconcile_*` بدون `explicitEligibility` فقط ماه جاری را قبول می‌کند. ناسازگاری جزئی و بی‌خطر (سال قفل‌شده مسدود است)؛ صرفاً برای یکدستی قابل بررسی.

### B7 — [شکاف قابلیت] حقوق پرسنل غیرسهامدار فاقد «مطالبه» و سند تعهدی است
- فقط سهامدارانِ موظف (`duty:true`) مسیر تعهدی دارند. پرسنل دیگر یا opex دستی (نقدی) یا تنخواه‌اند؛ هیچ «مطالبه/تعهد» یا master پرسنلی ندارند. این یک شکاف قابلیتی در برابر استاندارد payroll است، نه باگ محاسباتی. افزودن آن پروژهٔ جداگانه است.

---

## ۴) اصلاحات اعمال‌شده در این جلسه (امن، نمایشی، بدون تغییر فرمول سود/نقد)

### ۴.۱ `crm/fiscal.js` — خروج پرداخت حقوق از «علی‌الحساب/مانده قابل تسویه»
```js
// advRows: draw/advance/debit همان سال، ولی drawِ حقوق (paymentFor:'salary' | salaryMonth) نه.
return x && !x.voided && x.status !== 'void' &&
  (x.type === 'draw' || x.type === 'advance' || x.type === 'debit') &&
  !(x.type === 'draw' && (x.paymentFor === 'salary' || !!x.salaryMonth)) &&
  fiscalYearOf(x.t || x.month || '') === String(year);
```
- اثر: فقط ستون‌های `advYear` / `settleYear` / `advYearTotal`؛ ثبت سود همچنان از `s.gross` است. هم‌راستا با قصد مستند فاز ۲ (که فقط نوع legacy `salary_payment` را کنار می‌گذاشت ولی مسیر draw جاری را جا انداخته بود).

### ۴.۲ `crm/treasury.js` — برچسب صحیح پرداخت حقوق در خزانه
```js
var isSalaryPay = t === 'salary_payment' || (t === 'draw' && (x.paymentFor === 'salary' || !!x.salaryMonth));
src:   isSalaryPay ? 'پرداخت حقوق سهامدار' : 'برداشت سهامدار',
label: (isSalaryPay ? 'پرداخت حقوق ' : 'برداشت ') + (x.shName || '')
```

### تأیید
- `node --check` هر دو فایل سالم.
- تسترهای مرتبط در گیت CI سبز ماندند: `tester301` (فقط خطای پیشینِ سلکتور سال که از قبل بوده و در گیت CI نیست)، `tester381`، `tester507`، `tester508`، `tester612`، `tester499`، `tester290`، `tester399`.

---

## ۵) اصلاحات (وضعیت به‌روز ۱۴۰۵/۰۶/۱۹)

### F1 — انعکاس پرداخت حقوق در خروج نقدی (رفع B2) — ✅ اعمال شد
در `ptfFiscalCashData`، drawهای حقوقِ همان سال (`paymentFor:'salary'`/`salaryMonth`) در `out.salaryPaid` جمع و به `outflowsTotal` اضافه شد (بدون تغییر `netProfit`). نتیجه: حقوق پرداخت‌شده فقط یک‌بار (روی draw) از `cashEnd`/«قابل تقسیم» کم می‌شود؛ تعهد opex همچنان فقط سود را کم می‌کند ⇒ بدون دوباره‌شماری. تستر قفل‌شده `tester628-v34.38.19-shareholder-salary-cash-outflow.js` در `run-ci-gate` ثبت شد.

### F2 — تفکیک «حقوق پرداخت‌نشده» از «پرداخت‌شده» (رفع B3) — ✅ اعمال شد
`salaryPaid`/`salaryUnpaid = max(0, opexSalaryTotal − salaryPaid)` در KPI، گزارش رسمی و CSV جدا نمایش داده می‌شوند.

### F3 — بستن نرم دوباره‌شماری تنخواه/حقوق (رفع B4)
- ریشه و راهکار دقیق در `ARENA-RCA-SALARY-MONTH-GAP-DOUBLE-OUTFLOW-2026-09-10.md` (بخش ۳.۲): بستن شکاف write-time دستی↔قالب، گارد تسویهٔ تک‌منبعی و cross-check سروری هشداردهنده.

### F4 — برچسب خزانه — ✅ اعمال شد (بخش ۴.۲).

### F5 — (اختیاری/بزرگ‌تر) ماژول حقوق پرسنل غیرسهامدار (رفع B7)
- افزودن master پرسنلی + تعهد/پرداخت، پروژهٔ مستقل است و خارج از دامنهٔ این بررسی.

---

## ۶) مواردی که به‌درستی محافظت شده‌اند (بدون نیاز به تغییر)

1. ثبت حقوق/پرداخت فقط از مسیر دستور سرور (`shareDomainCommand`)؛ مرورگر fallback محلی ندارد (`shareholders.js:44-96`).
2. Idempotency با `recurringKey` + `sd_stable_recurring_code` (کد پایدار `SHT-SAL/OPX-SAL/OPXR-SAL`) — retry رونوشت نمی‌سازد.
3. سنگ‌قبر و restore صریح: ردیف terminal بدون اجازهٔ صریح زنده نمی‌شود؛ ژورنال `corrections` ثبت می‌شود (`api/sales-domain.php`).
4. قفل سال مالی در ثبت/پرداخت/تطبیق (`sd_is_locked`، `shareYearLocked`).
5. گاردهای دوباره‌شماری: `ptfOpexSuspectedDuplicates` + `manual-vs-recurring` + `duplicate-template` + `opex-petty-twin` + confirm `clashRec` در `ptfOpexAdd` — و نمایش در «کیفیت داده».
6. کلاس‌بندی عمدیِ سخت‌گیرانهٔ `isShareholderSalaryOpex` (فقط با نشانهٔ سند، نه صرفاً دسته) — از دوباره‌شماری حقوق سهامدار با ردیف دستی جلوگیری می‌کند.
7. انتشار `isOfficial` (رسمی/غیررسمی) از پروفایل به ردیف حقوق (v34.38.5) و راهنمای کیفیت داده.

---

## ۷) پاسخ صریح به پرسش

- **آیا می‌شود باگ‌ها را اصلاح کرد؟** بله. دو باگ نمایشی/برچسبی (B1، B5) همین حالا امن و بدون تغییر فرمول اصلاح شد. باگ نقدی B2 و تفکیک B3 نیز قابل اصلاح‌اند، ولی چون خروجی «منطق نقدی» (cashEnd/قابل تقسیم) را تغییر می‌دهند، باید با تأیید کارفرما و افزودن تستر قفل‌شده به `run-ci-gate` انجام شوند (خط‌مشی موجود مخزن: فرمول سود/نقد بدون تستر قفل‌شده تغییر نکند). B4 با یک بستن نرم قابل رفع است. B7 نیازمند پروژهٔ جداگانه است.
- **هیچ حذف یا ادغام خودکاری انجام نشده** و همهٔ اصلاحات پیشنهادیِ مربوط به سندهای تکراری، فقط «تأیید انسانی» هستند — مطابق خط‌مشی مخزن.
