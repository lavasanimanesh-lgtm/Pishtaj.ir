# ARENA · ریشه‌یابی — «پیشنهادها و درخواست‌ها اصلاً نمایش داده نمی‌شوند» (بدون خطای کنسول)

**تاریخ:** 2026-09-05 · **مخزن:** `lavasanimanesh-lgtm/Pishtaj.ir` @ `1952dbe` (`main`, **v34.37.2**) · **مکملِ:** `ARENA-CRM-CUSTOMER-VANISH-RCA-2026-09-05.md`
**وضعیت:** تحلیل و بازتولیدِ ایستا — **هیچ کد محصولی تغییر نکرده**.

---

## ۰) نتیجه

دو علت مستقل در کد پیدا شد؛ هر دو **سمت کلاینت**، هر دو **دستگاه‌محور** («برای من»)، و هیچ‌کدام معلول تغییرات v34.37.x نیست:

| # | علت | نشانهٔ تشخیص |
|---|---|---|
| **الف** | **آب‌رسانی‌نشدن آینهٔ خالدار (IDB) در بوت** ⇒ `getData()` برای کلیدهای سنگین `[]` برمی‌گرداند در حالی که داده روی سرور و داخل IDB سالم است | `ptfBStatus()` سالم، کنسول بی‌خطا، `localStorage.getItem('ptf_crm_offers')` = **null**، شمارش سرور > ۰ |
| **ب** | **فیلتر «مشتری» مُردۀ صفحهٔ پیشنهاد** — مقدارش در `localStorage` مانده و بر «همه مشتریان» اولویت دارد؛ چون آن مشتری ردیفش حذف شده، همۀ رکوردها رد می‌شوند | فقط در یک دستگاه؛ `<select>` روی «🏢 همه مشتریان» است ولی فهرست خالی است |

علت «الف» همان چیزی است که در سند قبلی «خواندِ کهنه» نامیده شد — اینجا **منبعِ کهنه بودن، قطعی و قابل‌تکرار** شد، نه تصادفی.

---

## ۱) علت الف — نقص قطعیِ `ptfBIdbPreload` (زنجیره، خط‌به‌خط)

1. در هر بوت، `ptfBOffloadBusinessKeysToIdb({force:true})` اجرا می‌شود (`crm/client-server.js:1269`؛ بدنه `:1199-1221`) و **هر کلید کسب‌وکاری بزرگ‌تر از ۸KB** را به آینهٔ IDB می‌برد. `ptfBMirror` (`:93-101`) در پایان **نسخۀ `localStorage` را حذف می‌کند**.
2. «سنگین بودن» یک کلید در `idbKnown` نگه داشته می‌شود — **در حافظۀ همین نشست**؛ هیچ نشانگر پایداری وجود ندارد (`grep -rn "ptf_b_heavy" crm/*.js` خالی است). تنها `ptf_b_offload_last` نوشته می‌شود که فقط رسیدِ آزادسازی است.
3. در بوت **بعدی**، `ptfBIdbPreload` فهرست کلیدهایی که باید از IDB خوانده شوند را این‌طور می‌سازد (`:160-164`):
   ```js
   bKeys().forEach(function (k) { if (heavyList(k, localStorage.getItem(k))) list.push(k); });
   ```
   و `heavyList(k, str)` سنگینی را از سه چیز می‌فهمد:_membershipِ فهرست ثابت `IDB_KEYS` (`:71`)، `idbKnown[k]`، یا `str.length > 120000`_ (`:79-84`). برای کلیدی که کپی localStorage‌اش در گام ۱ حذف شده، `str === null` ⇒ هیچ‌کدام ⇒ **کلید به `list` راه نمی‌یابد ⇒ آب‌رسانی نمی‌شود**. (فقط `IDB_KEYS` ثابت — مثل `ptf_crm_inqitems`، `ptf_crm_deleted_archive` — استثنااند؛ پس «اقلام درخواست» هم می‌تواند خالی دیده شود اگر همان دورۀ رندر بخورد، ولی `ptf_crm_offers` / `ptf_crm_rfqs` / `ptf_crm_customers` **همیشه** در این تله‌اند.)
4. `window.getData` (`:1112-1131`) آینه را با شرط `idbKnown[k] && Object.prototype.hasOwnProperty.call(idbMem, k)` می‌خواند؛ چون `idbKnown[k]` تهی است، به `localGet(k)` می‌افتد که **null** است ⇒ `localArr = []` ⇒ **`getData('ptf_crm_offers')` === `[]`** — بدون خطا.
5. رندرها «بی‌دفاع» خالی را می‌پذیرند: `renderOffers()` (`crm/offers.js:448` — `if (!tb) return;` بعد `getData('ptf_crm_offers')`)، `renderRfq()` (`crm/index.html:3299`)، `renderInquiries()` (`crm/rbac.js:1424` که `ptf_crm_inqitems` و `ptf_crm_offers` را می‌خواند). نتیجه: **تب باز است، جدول خالی است، کنسول تمیز است.**
6. **خودش درست نمی‌شود**، چون سینک دلتاست: `data_pull` فقط کلیدهایی را برمی‌گرداند که rev‌شان روی سرور جلوتر رفته باشد (`api/crm.php:2537` + `krevs`؛ `fresh:true` هم در `:2534` کل پاسخ را کوتاه می‌کند) و کلاینت `krevs` را از `ptf_sync_krevs` می‌خواند (`crm/client-server.js:243`) ⇒ کلیدِ «بدون تغییر» هرگز دوباره دانلود نمی‌شود.
7. رندرِ اصلاحیِ پس از آب‌رسانی هم شرطی است و در این حالت رد می‌شود: `if (movedAny && ...) ptfScheduleDataRefresh('__cold_boot__')` (`:168-171`) — و `movedAny` فقط وقتی true است که کلیدی **از localStorage جابه‌جا شده باشد**، که دقیقاً اینجا نشده.
8. لایۀ sync همان خواندن را با `bReadValue` انجام می‌دهد که **fallback آسنکرون IDB دارد** (`:417-445`) ⇒ صف/پوش/تضادها داده را می‌بینند و «همگام» گزارش می‌کنند، در حالی که UI خالی است. **این عدم‌تقارن، دلیلی است که چرا هیچ هشداری ثبت نمی‌شود.**

**چرا «ناگهان» و «فقط برای من»:** شرط‌های فعال‌شدن = فاز B روشن + یک هم‌گرایی موفق (`isSynced()` به‌ازای هر نام کاربری: `syncedKey()` `:59-63`) + `indexedDB` قابل‌استفاده (`ptfBMirrorActive():87`) + این‌که offload حداقل یک بار روی همان دستگاه اجرا شده باشد. پس دستگاهی که IDB‌اش پر است ولی localStorage‌اش تخلیه شده، از دومین بوت به‌بعد در این حالت می‌ماند؛ دستگاه دیگر یا کاربر دیگر سالم است.

---

## ۲) علت ب — فیلتر مشتریِ مُرده روی همۀ صفحۀ پیشنهاد

```js
/* crm/offers.js:454 */  var custF = window._offCustFilter || ((document.getElementById('oFcust')||{}).value) || '';
/* crm/offers.js:392-396 */ اگر _offCustFilter تعریف‌نشده باشد ← localStorage.getItem('ptf_off_cust_filter')
/* crm/offers.js:364-368 */ offSetCustFilter(cd) ⇒ setItem/removeItem «ptf_off_cust_filter»
/* crm/offers.js:378 */  var c = getData('ptf_crm_customers').filter(x => x.cd === custCd)[0]; if (!c) return false;
```
* اگر کارفرما یک‌بار پیشنهادها را روی مشتری مشخصی فیلتر کرده باشد، آن کد در `localStorage` می‌ماند و **در هر بوت بازیابی و بر «همه مشتریان» تحمیل می‌شود** (چون `||` است: مقدارِ window بر ارزشِ خالیِ `select` می‌چربد).
* وقتی همان مشتری به‌علت باگ روتر (سند قبلی) حذف شود، `ptfOfferMatchCust` برای رکوردهای بی‌`buyerCd` (اسناد قدیمی) و برای همۀ رکوردهایی که `buyerCd`شان برابر نیست، `false` می‌دهد ⇒ **صفر ردیف**.
* UI هیچ سرنخی نمی‌دهد: `<select>` گزینهٔ «selected» ندارد چون کد در فهرست مشتریان نیست (`:288-291`) ⇒ کاربر «🏢 همه مشتریان» می‌بیند در حالی که فیلتر فعال است. **پس «فیلتر را بردارید» هم نمی‌تواند بزند** — یک بن‌بست ظاهری.
* `crm/kanban.js:152` همان `window._offCustFilter` را مصرف می‌کند ⇒ کارت‌ها هم تحت تأثیر همان فیلتر می‌مانند.

---

## ۳) تفکیک ۲۰ ثانیه‌ای (فقط‌خواندنی)

`/home/user/cust-eval/diag-offers-rfq.js` را در کنسولِ همان تب اجرا کنید؛ بخش B آن «محلی در برابر سرور» را کنار هم چاپ می‌کند و بخش C ردپای سپرها را. قضاوت:

| خروجی تشخیص | معنا | اقدام |
|---|---|---|
| `getData` محلی = ۰ و `localStorage` = null، ولی **`total` سرور > ۰** | علت **الف** (آب‌رسانی IDB) | ۴-الف؛ سپس پچ ۵ |
| `getData` محلی **غیرصفر** ولی فهرست خالی | علت **ب** (فیلتر مُرده) | ۴-ب؛ سپس پچ ۵ |
| `total` سرور هم = ۰ | **حذف واقعی** — همان روتر، اما این بار روی `ptf_crm_offers`/`ptf_crm_rfqs` | فوراً ۶ (توقف push + snapshot) و بازیابی از `sync/` یا بک‌آپ |
| `typeof renderOffers === 'undefined'` یا منابع ۴xx | **دیپلوی ناقص / کش سرویس‌ورکر** (استیجینگ) | `crm/clear-cache.html`، unregister، آپلود کامل |

---

## ۴) بازکردن قفل روی همان دستگاه (بی‌خطر، بدون نوشتن روی سرور)

```js
/* الف) اگر علت «ب» بود — فقط localStorage و UI محلی */
localStorage.removeItem('ptf_off_cust_filter'); window._offCustFilter = ''; renderOffers();

/* ب) اگر علت «الف» بود — شکستنِ مکانیزم دلتا تا کلیدهای گم‌شده کامل خوانده شوند */
localStorage.removeItem('ptf_sync_krevs'); localStorage.removeItem('ptf_sync_rev');
ptfSyncPullNow(function (d) { console.log('pull', d && d.ok); location.reload(); });
```
هر دو فقط خواندنی‌اند (پول = خواندن؛ هیچ `setData`/push ندارند). **پیش از اجرا صف خروجی را ببینید** — صف آفلاین از v34.8.23 روی IndexedDB است، پس با `localStorage` قابل‌مشاهده نیست: `ptfBStatus().queue` (کلاینت: `crm/client-server.js:1237`). اگر غیرصفر بود، **اول** §۶ را انجام دهید (آن صف همان خطر جایگزینی مجموعۀ سرور است).

---

## ۵) اصلاح پیشنهادی (هر دو علت، جمع‌وجور)

| # | اصلاح | محل | چرا |
|---|---|---|---|
| 1 | **آب‌رسانی بر مبنای «حضور در IDB»، نه اندازهٔ localStorage**: در `ptfBIdbPreload` وقتی `ptfBMirrorActive()` است برای **همۀ** `bKeys()` یک `ptfStorageIdbGet('bdata:'+k)` بزن (کلیدهای نبودن، null برمی‌گردانند و بی‌هزینه‌اند) | `crm/client-server.js:160-164` | تله را می‌بندد؛ منبعِ «خواندِ تهی» از بین می‌رود |
| 2 | یا کم‌هزینه‌تر: هنگام offload یک نشانگر پایدار `ptf_b_heavy_keys` (JSON فهرست) بنویس و در بوت از همان بخوان | `:1199-1221` + `:160` | همان اثر با یک کلید کوچک |
| 3 | **فول‌بک خواند**: در `getData` اگر `localGet(k)===null` و `mirrorActive` بود، یک‌بار در همان نشست `krevs[k]` را پاک کن و `sharedPull()` بزن تا سرور snapshot کامل آن کلید را برگرداند؛ تا رسیدن پاسخ، آرایۀ خالی را به‌عنوان «واقعیت» به رندر نده (علامت `window.__ptfBHydrating[k]=1`) | `:1112-1131` | UI دیگر «تهی» را با «نبودن داده» اشتباه نمی‌گیرد |
| 4 | `ptfScheduleDataRefresh('__cold_boot__')` را با `movedAny || hydratedAny` اجرا کن | `:168-171` | رندرِ پس از آب‌رسانی تضمین می‌شود |
| 5 | **فیلتر بی‌بازگشت را بشکن**: در `renderOffers` اگر `select` خالی است، `window._offCustFilter` را ریست (و `removeItem`) کن؛ و در `ptfOfferMatchCust` وقتی رکورد مشتری پیدا نشد، فقط `o.buyerCd === custCd` را ملاک بگیر (نه `return false`) | `crm/offers.js:454`, `:378` | هیچ فیلتری نباید «همه‌چیز» را پنهان کند |
| 6 | **سپر صفر را از «تهی» به «سقوطِ محسوس» گسترش بده**: `if (nextArr.length === 0 && base.length > 0)` در روتر (`:650`) و `arr.length === 0` در `crm/sync.js:1042-1058` و `api/crm.php:2456` هر سه **فقط صفر مطلق** را می‌بینند؛ یک افزودنِ شرط «کاهش > ۵۰٪ در یک ذخیره ⇒ رد فرمان و درخواست تأیید» ریسک اصلی این پرونده را می‌بندد | سه فایل | ریسک §۶ |

---

## ۶) هشدار پیش از دیپلوی (مهم‌ترین بند)

تا پیش از پچِ ۵، **این دستگاه‌ها نباید push بفرستند** و بهتر است یک snapshot تمیز از `data/sync/*.json` (یا DB در `mode=mysql`) گرفته شود. دلیل:
* روتر وقتی حذف انبوه را می‌بیند، آن را مسدود می‌کند و سپس **همان آرایۀ کوتاه را با `legacyFallback` → `setData(collection, nextArr)` می‌نویسد** (`crm/sales-domain-v2.js:631`، `:656`)؛
* سپر صفرِ کلاینت و سرور **فقط آرایۀ کاملاً خالی** را می‌گیرد (`crm/sync.js:1048` روی `GUARD_KEYS` و فقط وقتی `guardCounts()[k] >= 4`، `api/crm.php:2456`) ⇒ یک فهرست «ناقص ولی غیرخالی» پذیرفته و **جایگزین** می‌شود؛
* برای `ptf_crm_offers` همۀ بررسی‌های سرور مربوط به *تکراری‌سازی/ایجادِ غیرمجاز/پایۀ کهنه* است (`api/crm.php:2382-2392`, `:2401`)، نه مربوط به *حذف انبوه*؛
* و برخلاف مشتریان، اینجا **سنگ‌قبری هم ساخته نمی‌شود** ⇒ نه در سطل بازیافت دیده می‌شود، نه با `entity_restore` برمی‌گردد ⇒ بازیابی فقط از فایل/بک‌آپ سرور.

---

## ۷) ارتباط با دو پروندۀ باز

1. «علت الف» همان «خواندِ کهنه» در سند `ARENA-CRM-CUSTOMER-VANISH-RCA-2026-09-05.md` است — پس پچ‌های آن سند (A/E/C) بدون بستنِ این تله، فقط *فرکانس* را کم می‌کنند؛ اصلاح ۵-۱/۵-۲ علت را برمی‌دارد.
2. «علت ب» **معلولِ** آن باگ است: فیلتر پیشنهاد بعد از حذف شدن مشتری، قفل می‌شود و راه فرار هم ندارد — پس تا حذف‌های روتر بسته نشود، این علامت برمی‌گردد.
3. سند پنل فاکتور: گام ۲ (پنل فاکتور) و فاز ۱ همچنان در انتظار تأیید است؛ **دیپلوی را مشروط به §۶ انجام دهید** — تغییرات من در `63ce6e1` (`official-invoice-v2.js` + بامپ `?v=` + نام کش `sw.js`) در هیچ‌کدام از دو مسیر بالا دست ندارد.
