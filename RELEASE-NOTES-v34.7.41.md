# یادداشت انتشار v34.7.41 — ایمنی رویژن پیشنهاد برنده

**تاریخ:** 2026-08-19

## ارزیابی مستقل

پیاده‌سازی `revise_award` مستقیماً از روی `api/sales-domain.php` و `crm/case-revision.js` بررسی شد، نه بر اساس assertion تسترهای v34.7.34–36. پنج نقص قابل بازتولید در قرارداد کد دیده شد:

1. کلید idempotency در هر کلیک با `Date.now()` عوض می‌شد؛ پاسخ گم‌شده یا کلیک دوباره می‌توانست همان رویژن را یک Rev دیگر ثبت کند.
2. فرمان هیچ `expectedRev` نداشت؛ دو کاربر می‌توانستند فرم یک Rev را باز کنند و نفر دوم تغییر نفر اول را بی‌صدا overwrite کند.
3. lookup پیشنهاد با اولین شمارهٔ منطبق ادامه می‌داد؛ دادهٔ legacy با شماره تکراری می‌توانست پیشنهاد اشتباه را تغییر دهد و پروندهٔ تکراری نیز کنترل نمی‌شد.
4. اقلام رویژن با یک schema باریک جایگزین می‌شدند؛ metadata موجود مانند نرخ مرجع و منبع درخواست حذف و برای قلم تازه `lineId` ساخته نمی‌شد.
5. مبلغ رکورد root در `linkedOffers` پس از رویژن کهنه می‌ماند. همچنین تسترهای 437/438/439/441 با وجود merge شدن قابلیت‌ها به گیت کانونی متصل نشده بودند.

## اصلاحات

- operation ID هنگام بازشدن دیالوگ ساخته و تا بسته‌شدن همان پنجره ثابت می‌ماند.
- دکمه در زمان درخواست قفل است؛ retry خطای ارتباطی همان operation ID را استفاده می‌کند.
- کلاینت `expectedOfferId` و `expectedRev` می‌فرستد؛ سرور قبل از هر mutation هویت، Rev، وضعیت won و یکتایی پرونده/پیشنهاد را کنترل می‌کند.
- کلاینت قدیمیِ بدون precondition با HTTP 428 متوقف می‌شود و کاربر پیام refresh می‌گیرد.
- metadata خطوط موجود در سرور merge می‌شود، نه اینکه با schema باریک حذف شود.
- `lineId` قلم تازه در سرور ساخته می‌شود؛ lineId ناشناخته یا تکرار یک source line رد می‌شود.
- sequence با `max(revisionSeq, rev) + 1` جلو می‌رود و `fromRev` واقعی ثبت می‌شود.
- مبلغ و revision رکورد root در `linkedOffers` هم‌زمان به‌روزرسانی می‌شود.
- واحد پول UI از خود پیشنهاد خوانده می‌شود و پیشنهاد ارزی دیگر با برچسب «ریال» نمایش داده نمی‌شود.
- testerهای قابلیت‌های merge‌شده 437، 438، 439 و 441 همراه tester444 به `run-ci-gate.js` اضافه شدند.

## سیاست‌های بدون تغییر

- شماره و `_id` پیشنهاد برنده در رویژن ثابت می‌ماند.
- کاهش با فاکتور رسمی فعال بدون ابطال همچنان مسدود است.
- ابطال فاکتور نیازمند اشتراک نقش تجاری و مالی است.
- رسید حذف نمی‌شود و تخصیص‌ها در همان فرمان بازسازی می‌شوند.
- فاکتور تجمیعی چندپیشنهادی همچنان فرمان رویژن را متوقف می‌کند.

## آزمون

- `tester434`: **35 PASS / 0 FAIL**
- `tester437`: **14 PASS / 0 FAIL**
- `tester444`: **PASS**
- architecture guard: **PASS** (`A2=0/0`, `A9=24/24`)
- گیت کامل: **60 PASS / 0 FAIL**
- `tester442` ثبت اتمیک پیشنهاد/RFQ: **PASS**

اجرای native `php -l` پس از رفع مجوز Workflow در GitHub Actions انجام خواهد شد؛ parse ساختاری هر ۲۷ فایل PHP و syntax همهٔ ۱۰۳ فایل JS قابل‌اجرا در محیط عامل سبز است.
