# v34.9.2 — اصلاحات یکپارچگی داده + تجربهٔ کاربری (۲۰۲۶-۰۸-۳۱)

> دامنهٔ کامل A–H طبق رودمپ (b6dd7b3): دو RCA پروداکشن + شش درخواست جدید مالک + صف قبلی.
> گیت: **170 PASS / 0 FAIL** (tester554 جدید: 39 سنجه شامل ۴ رفتاری vm).

## A) اتحاد ضمائم شیءدسته‌ای — RCA «ضمائم گم‌شده» بسته شد
- `crm/sync.js` `ptfMergeAttachmentFields`: تا امروز فقط `files` **آرایه‌ای** union می‌شد؛ RFQها `files` **شیءدسته‌ای** (`{inq:[],ds:[]}`) دارند ⇒ رکورد برندهٔ merge کل ضمایم بازنده را بی‌صدا جایگزین می‌کرد.
- اکنون هر دو شکل: union per-category با کلید یکسان‌سازی `f.key||f.url||f.name`، اولویت محلی (b) سپس ریموت (a)، `_deletedFileKeys` محترم (فایل حذف‌شدهٔ عمدی زنده نمی‌شود).
- فایل‌های ابرِ «یتیم‌شده» سالم‌اند؛ **ابزار بازپیوست یتیم‌ها → v34.9.3** (merge-fix جلوی ضررِ بیشتر را گرفت).

## B) مشتریان — RCA «ثبت روی مشتری دیگر» بسته شد
- ریشه: `ptfUnifiedCode` با pool خالی ⇒ شمارهٔ محلی برخوردی ⇒ سرور `entity_upsert` رکورد موجود را **آپدیت** می‌کرد (نشست/جایگاه قدیمی ماند، فیلدهای payload آلوده شد).
- کلاینت: گارد برخورد در `saveCust` — کد تکراری ⇒ پسوند `-`+تصادفی، هرگز آپدیت هم‌کد؛ sort جدیدترین‌اول (`ptfCustSortNewest`).
- سرور + روتر: رکوردهای جدید با **`expectCreate`** فرستاده می‌شوند؛ برخورد ⇒ `409 entity_id_exists` + پیام فارسی «کد تولیدی تکراری بود…». بازنویسی بی‌صدای رکورد دیگر ناممکن شد.

## C) خزانه — تاریخ‌های ناهمگن یکدست و مرتب شدند
- `treasuryDateKey/Label`: کلید ISO از فارسی/ISO/ماه‌تنها؛ برچسب فارسی یکدست (+ساعت اگر هست؛ ماه‌تنها با نشانگر «(ماه)»)؛ جدول گردش نزولی زمانی.

## D) نام دوگانهٔ فارسی+انگلیسی مشتری
- فرم مشتری: فیلد `nCEn` (coEn ذخیره می‌شود؛ قبلاً فقط از اکسل پر می‌شد).
- نمایش: لیست مشتریان (`ptfCustDualName`)، فاکتورها، ردیف‌های پیشنهاد، مطالبات، اعلان سررسید پروندهٔ فروش. جستجوها coEn را هم پوشش می‌دهند.

## E) جستجوی فاکتورها
- ورودی زنده روی شماره/خریدار(فارسی+انگلیسی)/شماره استعلام/شماره فاکتور رسمی.

## F) فرم پیشنهاد — typeahead مشتری
- به‌جای select بلند: تایپ نام (فارسی/انگلیسی/کد) ⇒ فهرست فیلترشده (≤۴۰)؛ دکمهٔ «📋 لیست کامل» همان select رسمی را برمی‌گرداند؛ انتخاب همان `offerPickBuyer` رسمی ⇒ رفتار ثبت دست‌نخورده.

## G) مکاتبات
- **امضای نمایش‌داده‌نشدهٔ یک کاربر**: `letResolveSignerProfile` — نامهٔ امضاشده بدون snapshot به نام کاربری تکیه می‌کرد؛ تغییرنام/حذف کاربر ⇒ امضا بی‌صدا حذف. فالبک: تطبیق نام نمایشی نامه با nm/nmEn پروفایل‌ها.
- **«📋 کپی به پیش‌نویس»** برای نامه‌های امضاشده: متن/تنظیمات می‌ماند، امضا/شماره پاک، پیش‌نویس جدید قابل امضای مجدد.

## H) صف قبلی
- دکمهٔ «⬆️ انتقال/تکمیل انتقال» داخل بنر مهاجرت.
- محافظ `opexTpl` در `data_push`: incomingِ فاقد/خالی ⇒ حفظ نسخهٔ سرور (RCA حذف بی‌صدای قالب‌های تکرارشونده).
- سپر داده-صفر برای کلیدهای shared-union غیرفعال (RCA قفل ابدی avatars).
- عنوان ردیف: «🗄 بک‌آپ و بازگردانی + وضعیت دستگاه» (پین tester8 حفظ شد).

## نگهداری
- `bump-version-pins.js` 527 pin را برداشت اما ۸۹ فرم escape-شدهٔ باقی‌مانده (۵۱ تستر) با اصلاح دستی جمعی شد؛ گیت در همین ران سبز شد.
- قرارداد tester416 (سپر shared-union) و انکر برش tester546 به‌روز شدند.
- ۶+۵ نقطهٔ رسمی نسخه: VERSION.json، index.html (PTF_CRM_RELEASE/VER + 109 cache-buster)، sw.js (RELEASE/CACHE/ASSET_VERSION)، sales-domain.php، clear-cache.html، manifest.json، shell.js.

## بعدی
- v34.9.3: ابزار بازپیوست یتیم‌های ابری؛ حذف موتور legacy + A10 (شرط: ≥۷ روز سبز R4 و تصمیم مقایسهٔ فردایی).
