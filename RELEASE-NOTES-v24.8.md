# ریلیزنوت v24.8 — اسپرینت ثبات بک‌لاگ (Priority 2/3)

## سیاست
بدون ماژول جدید — فقط باگ‌های بک‌لاگ مالی/RBAC.

## Fixes
| ID | موضوع | فایل |
|----|--------|------|
| BUG-126-01 | وصول پیش‌پرداخت ارزی: پیش‌فرض how/نرخ/مانده | `crm/petty.js` |
| BUG-126-03 | حواله یوان + تبدیل‌ها با fallback واقعی | `api/fx-rates.php` |
| BUG-126-04 | تشخیص سنا: force + لایه مودال + راهنمای هاست | `crm/fx.js` |
| BUG-127-01 | ارشدها در فیلتر «من» همه مشتریان را می‌بینند | `crm/my-customers-filter.js` |

## قبلاً در همین فاز
- BUG-126-02 کسر پیش‌پرداخت از فاکتور = received
- Lead Finder / دستیار فنی پارک UI
- مودال stacking، سرنخ edit/delete، …

## Verify
1. پیش‌پرداخت ارزی → ثبت وصول فقط با نحوه حواله
2. صدور فاکتور بعد از وصول جزئی → کسر همان مبلغ وصول‌شده
3. فیلتر مشتریان با نقش chairman/ceo در حالت «من» → همه
4. دکمه تست سنا (admin) → مودال پاسخ خام
