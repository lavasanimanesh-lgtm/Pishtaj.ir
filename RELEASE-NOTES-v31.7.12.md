# Release Notes — v31.7.12

**تاریخ:** ۱۴۰۵/۰۴/۲۸ (2026-07-19)
**نسخه:** v31.7.12
**نوع:** 🟡 رفع باگ + قابلیت درخواستی کارفرما — نرخ مرجع در فرم پیشنهاد مالی (BUG-OFF-FOCUS-001 + US-OFF-REF)

---

## 🎯 درخواست کارفرما

1. در فرم پیشنهاد مالی، فیلد «درصد سود» و «نرخ مرجع» با تایپ هر عدد، فوکوس به بیرون می‌پرد.
2. اگر کالای فرم در ماژول کالا نرخ مرجع دارد، بتوان نرخ را داخل فرم تغییر داد و با «ذخیره»، روی نرخ مرجع همان کالا در ماژول کالا هم بنشیند.
3. در «ثبت سریع کالا» فیلد نرخ مرجع هم باشد.
4. کالای دستی (و اکسلی/هر مبدأ دیگر) در صورت غیرتکراری بودن، خودکار در ماژول کالا ثبت شود و مارک مخفی درخواست/پیشنهاد منبع را داشته باشد.

## 🔍 علت ریشه‌ای باگ فوکوس (BUG-OFF-FOCUS-001)

`offUpdRefPrice` و `offUpdMarginPct` در هر `oninput` تابع `offRenderItems()` را صدا می‌زدند که **کل جدول اقلام را با innerHTML بازسازی می‌کند** — فیلدِ در حال تایپ نابود و فوکوس گم می‌شد. جالب اینکه همین باگ برای qty/price در v17.0 (US-409) حل شده بود ولی دو فیلد ref/margin از قلم افتاده بودند.

## ✅ تغییرات

| # | فایل | تغییر | شدت |
|---|------|--------|-----|
| 1 | `crm/offers.js` | `offUpdRefPrice`/`offUpdMarginPct` (هر ۲ نسخه): به‌جای رندر کامل → helper جدید `offSyncRowInputs` که فقط input های همان ردیف (به‌جز فیلد فعال `activeElement`) و جمع‌ها را به‌روز می‌کند — **فوکوس دیگر نمی‌پرد** | 🔴 P0 |
| 2 | `crm/offers.js` + `crm/offerlock.js` | id ردیفی برای input های نرخ مرجع (`offRef{i}`)، درصد سود (`offMg{i}`) و قیمت (`off_price_{i}`) در هر دو رندر | — |
| 3 | `crm/offerlock.js` | نرخ مرجع ویرایش‌شده کاربر (`refPriceEdited`) بر نرخ کاتالوگ کالا در رندر مقدم است — تایپ کاربر دیگر با رندر بعدی بازنویسی نمی‌شود | 🔴 P0 |
| 4 | `crm/offers.js` (`offerSave`) | **write-back نرخ مرجع:** هنگام ذخیره، اگر نرخ مرجع قلمی در فرم ویرایش شده و کالای متناظر (pcode/resolver امن/نام نرمال) پیدا شود، `p.pr` کالا به‌روز می‌شود + ثبت `history` و `refPriceSrc='پیشنهاد {no}'` | 🟡 P1 |
| 5 | `crm/offers.js` (`offerSave`) | **ثبت خودکار کالای جدید:** هر قلم بدون کالای متناظر (دستی، اکسل، دستیار — هر مبدأ چون در نقطه ذخیره اجرا می‌شود) در صورت عدم تکرار (ptfCheckDup) با کد سرور (غیر TMP) در ماژول کالا ثبت می‌شود + **مارک مخفی `srcRef`** شامل شماره پیشنهاد، شماره درخواست، زمان و کاربر؛ ردیف فرم به کالای جدید قفل می‌شود | 🟡 P1 |
| 6 | `crm/offerlock.js` | دیالوگ «ثبت سریع کالا»: فیلد «نرخ مرجع خرید (اختیاری)» اضافه شد — با ثبت، `pr` و `refPriceAt/refPriceSrc` و `srcRef` روی کالا می‌نشیند و نرخ در همان ردیف فرم درج می‌شود | 🟡 P1 |
| 7 | ۴ نقطه نسخه | v31.7.12 | — |

## 📊 نتایج گیت

- تستر جدید `tester187-offer-refprice`: **۲۰/۲۰ PASS** — شامل تست رفتاری write-back (500,000→750,000 با history)، ثبت خودکار کالای دستی با srcRef، و idempotent بودن ذخیره مجدد
- رگرسیون کامل: **165/165 فایل PASS — 3877/3877 چک PASS**
- `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| نرخ مرجع ماژول کالا | فقط با ویرایش صریح کاربر در فرم (`refPriceEdited`) و فقط هنگام «ذخیره» تغییر می‌کند — مشاهده صرف، چیزی را عوض نمی‌کند؛ هر تغییر با history قابل ردیابی است | کم |
| ماژول کالا | کالای خودکار فقط در صورت عدم تکرار و کد معتبر سرور؛ srcRef فیلد اضافی است و UI موجود را نمی‌شکند | کم |
| فرم پیشنهاد | رفتار qty/price (v17.0) دست‌نخورده؛ فقط ref/margin به همان الگو پیوستند | صفر |
| سیاست codegen | کد کالای خودکار از `prodAutoCode`/سرور؛ TMP رد می‌شود (سازگار با CODEGEN-OFFER-SERVER) | صفر |

## 📋 راستی‌آزمایی توسط کارفرما (۵ دقیقه)

1. **فوکوس:** فرم CO → در فیلد «نرخ مرجع» یک عدد چندرقمی پیوسته تایپ کنید → فوکوس نباید بپرد؛ همین را برای «(٪) سود» تکرار کنید — قیمت واحد باید همزمان محاسبه شود.
2. **write-back:** کالایی با نرخ مرجع از ماژول کالا به فرم بیاورید → نرخ را در فرم عوض کنید → ذخیره → ماژول کالا → نرخ جدید + تاریخچه «به‌روزرسانی نرخ مرجع از پیشنهاد …» را ببینید.
3. **ثبت سریع:** «+ ثبت سریع» → فیلد «نرخ مرجع خرید» را پر کنید → کالا با نرخ در ماژول کالا ثبت و نرخ در ردیف فرم درج شود.
4. **کالای دستی:** یک ردیف با شرح جدید (بدون انتخاب از بانک کالا) بنویسید → ذخیره پیشنهاد → در ماژول کالا کالای جدید با توضیح «ثبت خودکار از پیشنهاد …» ایجاد شده باشد؛ ذخیره مجدد همان پیشنهاد نباید کالای تکراری بسازد.

---
*توسعه‌دهنده: PTF Development Team — 2026-07-19*
