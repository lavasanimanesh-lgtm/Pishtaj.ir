# پاک‌سازی ایندکس — فاز ۱ (اجرا‌شده) | ۲۰۲۶-۰۹-۱۹

**دامنه:** pishtaj.ir · **شاخه:** `arena/01a0ba7d-pishtaj-ir`
**مسیر انتخابی کاربر:** گزینهٔ (ب) — پاک‌سازی فنی ایندکس: رفع ۴۶ کانونیکالِ متناقض، مرتب‌کردن ۴۸ صفحهٔ noindex، کاهش «Not indexed» از ۲٬۰۶۰.

---

## ۰) خلاصهٔ یک‌خطی

۴۶ صفحه‌ای که فقط با `noindex` + متا-رفرش سمتِ کلاینت «بازنشسته» شده بودند، اکنون **۳۰۱ سروریِ تک‌مرحله‌ای** می‌گیرند؛ کلِ URLهای مردهٔ دو نسلِ قبلی سایت (ASP.NET و وردپرس) از سیاههٔ Wayback استخراج و به **۳۱ قاعدهٔ ۳۰۱ + ۱۴ قاعدهٔ ۴۱۰** تبدیل شد؛ و یک ابزار بازبینی خودکار (و گام CI) اضافه شد تا هیچ‌کدام از این ۳۰۳ قاعده دیگر بی‌آزمون منتشر نشود.

---

## ۱) مشکلِ واقعی (قبل از تغییر)

| یافته | عدد | منبع |
|---|---|---|
| نشانی در وضعیت «Not indexed» | **۲٬۰۶۰** | `SEO-FINAL-REPORT-2026-08-21.md` (GSC) |
| صفحات با `canonical ناهماهنگ` | ۴۶ | `_audit/SEO-AUDIT-DATA-2026-09-12.csv` |
| صفحات `noindex` | ۴۸ (۴۶ موردش استابِ ادغام + `search/` و `en/careers`) | همان |
| صفحات در sitemap نیستند | ۴۸ | همان |
| توزیع نشان‌ها در جایگاه ۳۰+ | ۴۵٪ | `_audit/GSC-ANALYSIS-2026-08-30.md` |

**ریشه‌یابی:** منبعِ اصلیِ ۲٬۰۶۰، URLهای دو نسلِ قبلی سایت است (ASP.NET ~۲۰۱۳ → وردپرس تا ۲۰۲۶-۰۱) که امروز **۴۰۴** می‌دهند. سه اشکالِ مشخص داشت:

1. **استاب‌های ادغام** (`services/*-equipment/`، `suppliers/valve-supplier.html`) فقط با `<meta http-equiv="refresh">` هدایت می‌شدند — سرور **۲۰۰** می‌داد و گوگل آن‌ها را «صفحهٔ ایندکس‌شده با canonical متفاوت» نگه می‌داشت. ۴ پاسخِ سروریِ ۳۰۱ از ۶ پاسخِ موردنیاز موجود بود.
2. **URLهای نسلِ قبلی** (`/aboutus`، `/logo/`، `/feed/`، `/?p=123`، `/Home/Page/10`، `/Weblog/…`، برگه‌های فارسیِ وردپرس) هیچ قاعده‌ای نداشتند و ۴۰۴ می‌دادند. ۴۰۴ برای محتوای حذف‌شده کندتر از ۴۱۰ پاک می‌شود و بودجهٔ خزش را می‌سوزاند.
3. **هیچ بازبینیِ خودکاری روی .htaccess نبود** — با ۲۶۰+ قاعدهٔ دستی، یک مقصدِ اشتباه هیچ‌جا گرفته نمی‌شد.

### شاهد زندهٔ قبل از تغییر (بررسی ۲۰۲۶-۰۹-۱۹)

| نشانی | وضعیت امروزِ سرور |
|---|---|
| `/services/piping-equipment/` | **۲۰۰ + متا-رفرش** (نه ۳۰۱) |
| `/aboutus` | **۴۰۴** |
| `/feed/` | **۴۰۴** |
| `/ساده-سازی-عملیات-مزایای-اجاره-ماشین-آل/` | **۴۰۴** |

---

## ۲) چه چیزی تغییر کرد

### الف) `.htaccess` — ۴۵ قاعدهٔ تازه (+۹۴ خط)

| بلوک | تعداد | کار |
|---|---|---|
| `SEO-2026-09-19 (P0-COMPLETE)` | ۴ قاعده | `services/electrical-equipment`، `instrumentation-equipment`، `piping-equipment` (هم `.html` و هم پوشه) و `suppliers/valve-supplier.html` → مستقیم به صفحهٔ پول‌سازِ `*-supply.html`. زنجیرهٔ دو مرحله‌ای (`.html` → پوشه → مقصد) هم حذف شد. |
| `INDEX-CLEANUP-01 (الف)` | ۲۷ قاعدهٔ ۳۰۱ | برگه‌های لاتینِ وردپرس (`about-us`, `contactus`, `our-services`, `faqs`)، برگه‌های ASP.NET (`Product`, `project`, `Recruitment`, `order`, `Service`, `Weblog` و مسیرهای شماره‌دارشان)، برگه‌های فارسی (`کالاهای-پایپینگ`, `2-ابزار-دقیق`, `پمپ-و-الکترو-موتور`, `چارت-سازمانی`, `قرار-ملاقات`) و ۹ برگهٔ `/en/<اسلاگِ-فارسی>/` |
| `INDEX-CLEANUP-01 (ب)` | ۱۴ قاعدهٔ ۴۱۰ | ماشین‌آلاتِ مردهٔ وردپرس/ASP: `/?p=ID` (با شرط `QUERY_STRING`)، هر مسیری که به `feed/` ختم شود، `xmlrpc.php`، `wp-*`، `author/`، `tag/`، `category/`، `Admin/`، `Areas/`، `Home/Page/N`، `css/`، `img/`، `js/` و پستِ بی‌ارتباطِ «اجارهٔ خودرو» |

**سه قاعدهٔ طراحی که رعایت شد:**
- **هیچ‌چیز به صفحهٔ اصلی ریدایرکت نشد** (آن الگو خودش soft-404 است و بدتر از ۴۰۴).
- **حالت حساس به بزرگی/کوچکی حروف دست‌نخورده ماند**: `^Service` هرگز `/services/` را نمی‌گیرد (بررسی‌شده).
- کتابخانهٔ مرکز دانش، استاب‌ها و فایل‌ها **حذف نشدند** — فقط سیگنالِ سرور اصلاح شد؛ بازگردانی = پاک‌کردن همان بلوک.

### ب) شکستنِ لینک داخلی — ۱ مورد (مهم‌ترین صفحهٔ ترافیک‌دار)

`knowledge-center/api-5l-x42.html` (کوئری «لوله x42»، ۲۳ نمایش) در CTA خود به `/services/products/api-5l-x42.html` لینک می‌داد که **وجود ندارد**. مقصد به `/services/products/api-5l-pipe.html` (صفحهٔ واقعی: «خرید و تامین لوله API 5L PSL1/PSL2») اصلاح شد.
اسکن: **۰ لینک شکسته** در ۶۷۶ صفحه / ۲۷٬۵۰۲ ارجاع.

### ج) ابزار و نگهبان (تا این دوباره خراب نشود)

| فایل | نقش |
|---|---|
| `_tools/seo_redirect_audit.py` | ۶ بررسی: مقصدِ ۳۰۱ موجود است؟ · زنجیره؟ · حلقه؟ · پوششِ سیاههٔ URLهای مرده · **هیچ صفحهٔ زنده‌ای ۳۰۱/۴۱۰ نگیرد** · لینک داخلی. خروج غیرصفر با `--strict`. |
| `_tools/legacy-url-inventory.json` | سیاههٔ ماشین‌خوانِ ۶۹ ورودی (منبع: Wayback CDX، دو نسلِ قبلی) با انتظارِ ۳۰۱/۴۱۰ برای هر URL |
| `_tools/seo_audit_local.py` | ستون `redirect_stub` اضافه شد؛ پرچم‌های `canonical-mismatch` / `noindex` / `not-in-sitemap` دیگر با استاب‌های عمدی قاطی نمی‌شوند |
| `.github/workflows/php.yml` | گام «SEO redirect audit» با `--strict` روی هر PR به main |

### د) نتیجهٔ بازبینی (خروجی واقعی ابزار)

```
قواعد: 303 کل | 287 ریدایرکت ۳۰۱ | 15 قاعدهٔ ۴۱۰
۱) مقصدِ ناموجود در مخزن: 0
۲) زنجیرهٔ ۳۰۱: 0
۳) حلقهٔ ۳۰۱: 0
۴) سیاههٔ URLهای مرده: 69 ورودی | بی‌پاسخ: 0 | ناهم‌خوان: 0
۵) لینک داخلی: 676 صفحه | 27502 ارجاع | شکسته: 0
۶) صفحات زنده: 676 | استابِ ادغام‌شده: 46 | صفحهٔ زندهٔ قربانی‌شده: 0
✅ سالم
```

### هـ) gateهای موجود، پس از تغییر
`node _tools/arch/arch-guard.js` → **PASS** · `node _tools/uat/run-ci-gate.js` → **286 PASS / 0 FAIL**

---

## ۳) شاخص‌ها: قبل → بعد

| شاخص | قبل | بعد | توضیح |
|---|---|---|---|
| `canonical ناهماهنگ` | ۴۶ | **۰ مؤثر** | هر ۴۶ مورد اکنون ۳۰۱ سروری دارد (نه فقط canonical/meta-refresh) |
| `noindex` | ۴۸ | **۲ مؤثر** | باقی‌مانده‌ها عمدی و درست‌اند: `search/` (صفحهٔ نتایج) و `en/careers.html` |
| خارج از sitemap | ۴۸ | **۲ مؤثر** | همان دو مورد بالا |
| هدفِ ۳۰۱ ناموجود | — | **۰** | بررسی خودکار روی همهٔ ۳۰۳ قاعده |
| زنجیره / حلقهٔ ۳۰۱ | — | **۰ / ۰** | بررسی خودکار |
| لینک داخلی شکسته | ۱ | **۰** | lینک صفحهٔ `api-5l-x42` اصلاح شد |
| URL مردهٔ پوشش‌داده‌شده | ۴ (فقط استاب‌ها) | **۶۹ خانواد**ه | ۳۱ قاعدهٔ ۳۰۱ + ۱۴ قاعدهٔ ۴۱۰ |

> شاخصِ سمتِ گوگل (Not indexed) فقط با دادهٔ سرچ کنسول اندازه‌گیری می‌شود؛ این تغییرات اثرشان را در چرخهٔ خزش بعدی (۲–۶ هفته) نشان می‌دهند.

---

## ۴) کاری که باید انجام دهید (کارفرما)

### الف) پیش از دیپلوی — تست روی staging
هیچ‌کدام از این قاعده‌ها روی سرور اجرا نشده‌اند (این محیط Apache ندارد). اول **staging**، بعد production:

```bash
# روی استیجینگ: باید برای هر خط، کد و مقصدِ درست را ببینید
for u in /aboutus /about-us/ /logo/ /feed/ /Home/Page/10 "/?p=630" \
         /services/piping-equipment/ /suppliers/valve-supplier.html \
         /Product /Weblog/9/x /js/main.js \
         "/%D8%B3%D8%A7%D8%AF%D9%87-%D8%B3%D8%A7%D8%B2%DB%8C-%D8%B9%D9%85%D9%84%DB%8C%D8%A7%D8%AA-%D9%85%D8%B2%D8%A7%DB%8C%D8%A7%DB%8C-%D8%A7%D8%AC%D8%A7%D8%B1%D9%87-%D9%85%D8%A7%D8%B4%DB%8C%D9%86-%D8%A2%D9%84/" ; do
  printf "%s  " "$(curl -o /dev/null -sS -w '%{http_code} → %{redirect_url}' "https://STAGING_HOST$u")"
  echo "$u"
done
```
انتظار: `301` برای ردیف‌های اول و `410` برای `/?p=630`، `/feed/`، `/Home/Page/10`، `/js/main.js` و نشانیِ «اجارهٔ خودرو».

### ب) بعد از دیپلوی — سرچ کنسول
1. **Sitemaps** → ثبت مجدد `sitemap-index.xml` (یک‌بار).
2. **Indexing → Pages** → فیلتر «Not found (404)»: شمارشِ هفتگی را یادداشت کنید؛ روندِ نزولی معیار موفقیت این فاز است.
3. **Removals** فقط اگر URL اسپم هنوز در نتایج دیده می‌شود (اسپم‌های کازینو ۴۱۰ شدند و خودشان می‌روند).
4. **Crawl Stats** هفتگی: تعداد درخواست‌ها به مسیرهای `feed|author|wp-` باید به صفر برسد → بودجهٔ خزش آزاد می‌شود برای صفحاتِ واقعی.
5. **URL Inspection** روی ۳ نمونه: `/aboutus` (۳۰۱)، `/services/piping-equipment/` (۳۰۱)، `/feed/` (۴۱۰) — تا وقتی گوگل خودش کشف کند.

### ج) هر بار تغییر .htaccess (از این پس)
```bash
python3 _tools/seo_redirect_audit.py --strict   # باید ✅ باشد
python3 _tools/seo_audit_local.py               # شاخص‌های صفحه‌ای
```
گام CI هم خودش این را روی هر PR اجرا می‌کند.

---

## ۵) محدودیت‌ها و هشدارهای صادقانه

- **اجرای واقعی روی Apache تأیید نشده.** این محیط Apache/`apachectl` ندارد و دسترسی شبکه به `pishtaj.ir` برای `curl` بسته است؛ صحتِ نحوی و منطقی با یک شبیه‌سازِ mod_rewrite (همین ابزار) و مقایسه با قاعده‌های موجودِ سالِ گذشته بررسی شد، نه با اجرای سرور. **تست staging الزامی است.**
- **۴۱۰ برگشت‌ناپذیر نیست ولی ناگهانی است**: اگر معلوم شد یکی از آن URLها محتوای واقعی داشته، باید فوراً ۳۰۱ شود (مسیرِ بازگشت: یک خط در همان بلوک).
- **سیاهه کاملِ ۲٬۰۶۰ نیست** — سیاههٔ Wayback فقط URLهایی را دارد که آرشیو دیده است. برای کامل‌کردن، خروجی «Not found (404)» از GSC لازم است (همان CSV سه‌ماهه)؛ با آن می‌شود بقیه را هم به همین دو دسته (۳۰۱/۴۱۰) برد.
- **این فاز ترافیک نمی‌آورد** — جلوی هدررفت را می‌گیرد. اهرمِ ترافیک همچنان اعتبار بیرونی + خوشهٔ تجاری ابزار دقیق است (`ARENA-TRAFFIC-DIAGNOSIS-2026-09-19.md`، گام‌های ۲ و ۳).

---

## ۶) فایل‌های تغییر‌یافته

| فایل | تغییر |
|---|---|
| `.htaccess` | +۹۴ خط: بلوک `P0-COMPLETE` و بلوک `INDEX-CLEANUP-01` (۳۱×۳۰۱ + ۱۴×۴۱۰) |
| `knowledge-center/api-5l-x42.html` | اصلاح لینک شکسته به `../services/products/api-5l-pipe.html` |
| `_tools/seo_redirect_audit.py` | جدید — بازبینِ ۶ بررسیه |
| `_tools/legacy-url-inventory.json` | جدید — سیاههٔ ۶۹ URL مرده با انتظارِ ۳۰۱/۴۱۰ |
| `_tools/seo_audit_local.py` | ستون `redirect_stub` + توضیح تفکیکِ پرچم‌ها |
| `.github/workflows/php.yml` | گام gating «SEO redirect audit (.htaccess)» |
| `_audit/SEO-AUDIT-DATA-2026-09-19.csv` | اسکن تازهٔ ۶۹۸ صفحه پس از تغییرات |
