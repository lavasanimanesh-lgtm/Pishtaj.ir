# ریلیزنوت `v28.7` — Hotfix ایمنی نگاشت خرید/استعلام با پیشنهاد مالی

## مسئلهٔ گزارش‌شده
در Post-Award Optimizer یک پیشنهاد مالی، قیمت استعلامی اقلام دیگر زیر ردیف نادرست نمایش داده می‌شد.

## تحلیل ریشه‌ای
منطق پیشین، قلم CO را با این فرض نادرست به خرید/استعلام متصل می‌کرد که شمارهٔ ردیف همهٔ آرایه‌ها یکسان است:

```text
purchase.idx === شماره ردیف CO
rfq.items[i]
```

با حذف، افزودن، جابه‌جایی یا تفاوت ترتیب اقلام بین CO، RFQ Smart و خرید واقعی، این فرض می‌تواند قیمت قلم دیگر را نمایش دهد.

## اصلاح
- ماژول جدید `procurement-link.js` اضافه شد.
- تطبیق با اولویت زیر انجام می‌شود:
  1. کد کالا / شناسهٔ پایدار
  2. نام + مدل + مشخصات + واحد به‌صورت نرمال‌شده
- fallback اندیسی در Optimizer و اعمال سود گروهی حذف شد.
- اگر تطبیق مبهم یا ناموجود باشد، سیستم هیچ قیمت خرید/استعلامی را به قلم CO نسبت نمی‌دهد و هشدار می‌دهد.
- خریدهای واقعی جدید، `sourcePcode` و `sourceItemKey` نگه می‌دارند.
- به‌روزرسانی قیمت مرجع کالا نیز دیگر «اولین نام مشابه» را انتخاب نمی‌کند.
- دکمهٔ **«تطبیق سراسری خرید»** برای نقش‌های دارای دسترسی خرید افزوده شد تا پیشنهادهای مشکوک را فقط‌خواندنی بررسی کنند.

## حفاظت از دادهٔ موجود
- هیچ migration یا اصلاح خودکار روی پیشنهادها، RFQها، خریدها یا قیمت‌های مرجع قدیمی انجام نشده است.
- در داده‌های legacy مبهم، عدد مشکوک پنهان نمی‌شود اما به قلم اشتباه هم منتسب نمی‌گردد؛ کاربر باید آن را در گزارش تطبیق بررسی کند.

## تست‌ها
- `tester161-v287-procurement-line-integrity.js`: **18 PASS / 0 FAIL**
- `tester162-v287-optimizer-safe-mapping.js`: **5 PASS / 0 FAIL**؛ سناریوی واقعی ترتیب معکوس CO/RFQ/CMP.
- `tester72-v152.js`: **30 PASS / 0 FAIL**
- parse همهٔ 68 اسکریپت لودشده: موفق.
- `audit.py`: بدون خطا؛ فقط یک هشدار قدیمی حجم 5 تصویر.

### شفافیت baseline
`tester62-v142.js`: **36 PASS / 3 FAIL**؛ موارد مربوط به تلفن/Terms چاپی قدیمی هستند و در این hotfix تغییر نکرده‌اند.

## UAT پیشنهادی
1. همان CO را باز کنید و **تطبیق سراسری خرید** را اجرا کنید.
2. پیشنهاد موردنظر را از گزارش باز کنید.
3. ماتریس سود را باز کنید؛ هر عدد استعلامی باید با نام/مشخصات همان قلم منطبق باشد.
4. اگر قلمی تطبیق قطعی ندارد، باید هشدار «تطبیق مبهم/بدون تطبیق» ببیند، نه مبلغ قلم دیگر.
5. یک CO با اقلام جابه‌جاشده یا مشابه را نیز آزمایش کنید.
