# Release Notes v34.7.8 — حذف قطعی پرونده آزمایشی بایگانی‌شده

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## دسترسی و رابط کاربری

- در جزئیات پرونده بایگانی‌شده، دکمه `🧹 حذف قطعی داده آزمایشی` اضافه شد.
- دکمه فقط برای نقش‌های `admin` و `chairman` و فقط برای رکورد با وضعیت `archived` نمایش داده می‌شود.
- پیش از هر حذف، plan فقط‌خواندنی سرور تعداد همه رکوردهای مرتبط، نمونه شناسه‌ها، تعداد فایل‌های ابری و سال‌های مالی متاثر را نمایش می‌دهد.
- کاربر باید هم‌زمان:
  1. آزمایشی/اشتباهی‌بودن پرونده را تایید کند؛
  2. غیرقابل‌بازیابی‌بودن عملیات را تایید کند؛
  3. دلیل حذف را بنویسد؛
  4. شماره پرونده را عیناً وارد کند.

## دامنه حذف کامل

حذف، فقط کارت بایگانی را پاک نمی‌کند؛ graph کامل پرونده را بررسی و حذف می‌کند:

- پرونده بایگانی و پرونده فروش مرتبط؛
- درخواست، پیشنهاد و اقلام درخواست؛
- فاکتور، دریافت، تخصیص و ضمیمه مالی؛
- اسناد، نامه، قرارداد و پکینگ‌لیست؛
- استعلام تامین، مقایسه قیمت و خرید واقعی؛
- تنخواه، هزینه جاری، چک، مرجوعی فروش و موجودی انبار مرتبط؛
- اگر موجودی به پیشنهاد واقعی دیگری رزرو/فروخته شده باشد، purge به‌صورت fail-closed متوقف می‌شود؛
- بدهی‌ها، قیمت‌های خرید، یادآورها، اعلان‌ها و لاگ‌های تفصیلی مرتبط؛
- فاکتور/پرداخت تامین‌کننده مرتبط، بدون حذف پرداخت مشترک واقعی؛
- فایل‌های ابری و zip بایگانی؛
- snapshot سال مالی سال‌های متاثر تا گزارش پاک دوباره تولید شود.

داده‌های master مشترک شامل مشتری، کالا و تامین‌کننده عمداً حذف نمی‌شوند، چون ممکن است توسط پرونده‌های واقعی دیگر استفاده شوند.

## ایمنی و عدم بازگشت داده

- planHash شامل hash محتوای رکوردهاست؛ تغییر همزمان داده باعث توقف می‌شود.
- کلاینت plan را درست قبل از حذف فایل‌ها دوباره می‌گیرد.
- metadata فقط پس از حذف موفق همه فایل‌های ابری و دریافت receipt معتبر commit می‌شود.
- اگر حتی یک فایل ابری حذف نشود، حذف رکوردها انجام نمی‌شود.
- همه projectionها در یک lock و commit سرور تغییر می‌کنند.
- snapshot کامل داده حذف‌شده در سیستم نگهداری نمی‌شود؛ فقط tombstone حداقلی شامل شناسه‌ها، دلیل، کاربر و hash باقی می‌ماند تا دستگاه قدیمی نتواند داده آزمایشی را دوباره زنده کند.
- tombstone حداقلی در گزارش سال مالی اثر ندارد و از رابط گزارش نیز قابل حذف نیست.
- شماره پیشنهادها و شناسه‌های تجاری حذف‌شده بازنشسته می‌مانند و دوباره استفاده نمی‌شوند.

## کنترل کیفیت

- `tester413-v34.7.8-archived-test-purge.js`: 22 PASS / 0 FAIL
- `tester71-v151.js`: 18 PASS / 0 FAIL
- `tester403-v35-sales-to-cash.js`: PASS
- CI متمرکز ریلیز: 32 PASS / 0 FAIL
- PHP parser مستقل روی `sales-domain.php`، `storage.php` و `crm.php`: PASS
- رگرسیون کامل نهایی: 396 تستر، 339 فایل PASS، 57 فایل بدهی تاریخی FAIL، 5875 check PASS و 129 check تاریخی FAIL؛ هیچ شکست جدیدی نسبت به v34.7.7 باقی نماند.
- Auditor روی 624 صفحه و هم‌ترازی VERSION/PWA با 100 مرجع cache-query: PASS
- PHP CLI در محیط محلی موجود نبود و lint داخلی CI به‌صورت صریح skip شد؛ syntax فایل‌های JS و `git diff --check` سبز هستند.
