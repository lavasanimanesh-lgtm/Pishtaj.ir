# RELEASE NOTES — v34.7.98 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## هاب مالی — بازخورد کارفرما: اعداد کامل ریالی + تب‌های افقی جمع‌وجور

نسخهٔ قبلی (v34.7.97) دو تصمیم داشت که پذیرفتنی نبود:
1. اعداد را با پسوند «میلیون/میلیارد» فشرده می‌کرد. کارفرما گفت **کماکان به ریال**.
2. تب‌های نوار هاب را به کارت‌های عمودی ۶۴px تبدیل کرده بود («خیلی دراز و نامناسب»).

این نسخه هر دو را اصلاح می‌کند بدون آنکه دستاورد اصلی v34.7.97 (اعداد در کارت هرگز نمی‌شکنند) از دست برود.

### ۱) اعداد کامل ریالی — نه فشرده

**`ptfMoneyCompact(n, unit)` بازنویسی شد:**
- خروجی همیشه عدد کامل با کاما فارسی (`۱٬۵۰۰٬۰۰۰٬۰۰۰ ریال`)
- بدون هیچ پسوند «میلیون/میلیارد»
- منفی/صفر/null/NaN همچنان safe

**`ptfMoneyCompactHtml(n, unit)` بازنویسی شد:**
- ساختار HTML شفاف: `<span title="عدد کامل"><span dir="ltr">۱٬۵۰۰٬۰۰۰٬۰۰۰</span><small class="unit">ریال</small></span>`
- عدد در `<span dir="ltr" style="unicode-bidi:embed">` — از راست به چپ نمایش نمی‌شود، به‌صورت طبیعی
- واحد در `<small class="unit">` جدا و کوچک‌تر (۶۲٪ فونت، خاکستری، وزن ۷۰۰)
- عدد کامل روی `title` (tooltip) باقی است

**رفتار در کارت‌های KPI:**
- در دسکتاپ (`minmax(180px, 1fr)`, `font-size: clamp(14, 1.3vw, 18px)`): عدد ۱۳ رقمی هم راحت جا می‌شود.
- در موبایل (`minmax(150px, 1fr)`, `font-size: 13.5px`): اعداد متوسط جا می‌شوند؛ اعداد خیلی بزرگ با `text-overflow: ellipsis` (`…`) کوتاه می‌شوند و عدد کامل روی `title` باقی است.
- **هرگز نمی‌شکند در دو خط.**

### ۲) نوار تب هاب مالی — chip افقی جمع‌وجور

نسخهٔ v34.7.97 در دسکتاپ کارت‌های عمودی ۶۴px با آیکون بالا و متن پایین می‌ساخت (بازخورد: «خیلی دراز و نامناسب»). بازنویسی:

| ویژگی | v34.7.97 (پس گرفته شد) | v34.7.98 (فعلی) |
|---|---|---|
| Layout | grid با `minmax(150px)` | flex-wrap افقی |
| min-height | ۶۴px | **۳۶px** (مثل چیپ استاندارد) |
| flex-direction | column (آیکون بالا/متن پایین) | **row** (آیکون کنار متن) |
| padding | `10px 8px` | `6px 12px` |
| gap | `8px` | `6px` |
| اندازه آیکون | ۲۲px | ۱۶px |
| فونت | ۱۲px با `line-height: 1.35` | ۱۲px با `line-height: 1.3` |
| نظم | یکنواخت (grid) ولی درشت | چیپ فشرده در ردیف، به تعداد نیاز |

**hover و تب فعال حفظ شدند:**
- hover: `border-color: var(--pri)` + `color: var(--pri)` + `background: #eff6ff` (بدون transform سنگین)
- active: گرادیانت `var(--pri) → var(--org)` + `box-shadow`
- dark mode support

**در موبایل:** CSS موبایل قبلی (grid دو ستونه) دست‌نخورد.

### ۳) تنظیمات کارت KPI متناسب با اعداد بلندتر

چون عدد کامل ریالی چاپ می‌شود (نه فشرده)، فضای بیشتری لازم است:

| CSS | v34.7.97 | v34.7.98 |
|---|---|---|
| `.sr` minmax (دسکتاپ) | 170px | **180px** (کمی بزرگ‌تر) |
| `.sc b` font (دسکتاپ) | `clamp(15, 1.4vw, 19)` | **`clamp(14, 1.3vw, 18)`** (کمی کمتر تا عدد جا شود) |
| `.sr` minmax (موبایل) | 140px | **150px** |
| `.sc b` font (موبایل) | 15px | **13.5px** |

### ۴) نقاط مصرف — بدون تغییر

پنج ماژول (`fiscal.js`, `working-capital.js`, `ledger-report.js`, `treasury.js`, `supplier-finance.js`) که در v34.7.97 به `ptfMoneyCompactHtml` migrate شدند، بدون تغییر باقی می‌مانند — چون خودِ تابع تغییر کرد، ۴۳ کارت خودکار عدد کامل ریالی نشان می‌دهند.

### تست

- `tester498` بروزرسانی شد با انتظارات جدید — **۴۰ سنجش** با vm/sandbox:
  - انتظارات ptfMoneyCompact از فشرده به کامل تغییر کرد.
  - انتظارات HTML به `dir="ltr"` روی عدد و `<small class="unit">ریال</small>` تغییر کرد.
  - انتظارات CSS دسکتاپ به chip افقی (`min-height:36px`, `flex-direction:row`, `flex-wrap`) تغییر کرد.
  - انتظارات CSS موبایل به `minmax(150px)` و `font-size:13.5px` تغییر کرد.

- گیت CI: **۱۱۴ PASS / ۰ FAIL** ✅
- نگهبان معماری: **PASS** ✅

### فایل‌ها

- تغییر: `crm/moneyx.js` (بازنویسی ۲ تابع)، `crm/index.html` (CSS `.sc/.sr` + `@media(min-width:900px)` + `@media(max-width:768px)`)، `_tools/uat/tester498-...` (بازنویسی سنجش‌ها)
- افزوده: `RELEASE-NOTES-v34.7.98.md`
- بامپ نسخه: ۷ نقطهٔ رسمی + ۵۰ تستر. کامنت‌های تاریخی `v34.7.97 (FINHUB-UX-A)` در ۷ فایل دست‌نخورده باقی ماندند.
