# RELEASE NOTES — v34.34.0 UX-R3 (انتخاب مشتری با جستجو در ثبت درخواست + جستجوی فهرست اتصال در درخواست تامین)

**تاریخ:** ۲۰۲۶-۰۹-۰۳ · **گیت:** در حال اجرا · **نکتهٔ بامپ:** جاروی استاندارد نسخه + فرم ترکیبی رجکس + نقاط رسمی

## گزارش کارفرما

۱) «در پنجرهٔ مودال ثبت درخواست، انتخاب مشتری از فهرست کشویی است؛ می‌خواهم مشابه پنجرهٔ مودال ثبت پیشنهاد باشد.»
۲) «در پنجرهٔ مودال ثبت درخواست تامین جدید، علاوه بر پنجرهٔ اتصال فعلی، امکان جستجو با نام مشتری یا شماره درخواست و… باید وجود داشته باشد.»

## راه‌حل

### ۱) مودال ثبت درخواست — انتخاب مشتری مثل فرم پیشنهاد (`crm/bridge.js`)
- هِلپر مشترک جدید `ptfCustAc*` (Html/Search/Pick/Toggle/Sync/Label): ورودی جستجوی مشتری با جعبهٔ نتایج زنده (نام فارسی/انگلیسی/کد)، دکمهٔ «📋 لیست کامل»، چیپ «🏢 مشتری انتخاب‌شده»، و بستن خودکار جعبه با کلیک بیرون — دقیقاً همان تجربهٔ فرم پیشنهاد.
- سلکت قدیمی `nR2Cust` برای سازگاری کامل منطق‌های موجود (پرشدن مخاطبین، ذخیره، به‌روزرسانی پس از ثبت مشتری جدید) حفظ و پنهان شده؛ انتخاب از نتایج جستجو رویداد `change` را آتش می‌زند تا همهٔ وابستگی‌ها مثل قبل کار کنند.
- پس از «+ ثبت مشتری جدید» هم ورودی جستجو با مشتری تازه‌ثبت‌شده همگام می‌شود.

### ۲) مودال درخواست تامین — جستجوی فهرست اتصال (`crm/rfqsmart.js`)
- جعبهٔ جستجوی جدید بالای فهرست «اتصال به درخواست مشتری»: فیلتر زنده با **نام مشتری، شماره درخواست، شماره کارفرما و موضوع**.
- مخزن جستجو تا ۴۰۰ درخواست (قبلاً فقط ۱۲۰ تای اول بدون جستجو دیده می‌شد)؛ نتیجهٔ فیلتر تا ۱۲۰ گزینه نمایش داده می‌شود.
- منطق‌های موجود دست نخورده‌اند: همگام‌سازی اتصال با فهرست اقلام (`rfqsSyncSrcInqUI`)، نشان «⚠️ تامین ثبت‌شده»، و حفظ انتخاب فعلی هنگام فیلتر.

## آزمون
- تستر جدید `tester586-v34.34.0-cust-ac-and-rqs-src-search.js` (۱۱ ادعا) به گیت اضافه شد.

## اثر عملیاتی
ثبت درخواست و درخواست تامین برای کاربرانی که تعداد مشتریان/درخواست‌هایشان زیاد است به‌طور محسوس سریع‌تر می‌شود؛ تجربهٔ انتخاب مشتری در هر دو فرم یکسان است.
