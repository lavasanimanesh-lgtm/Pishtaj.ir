# CRM v34.38.24 — NOTIF-FRESH

**تاریخ:** 2026-09-17
**بازوی تغییر:** Arena Agent
**کانال:** CRM (اعلانات / کارتابل / همگام‌سازی)

> این فایل همان «یادداشت انتشار نسخهٔ جاری» است که گیت CI (تستر۵۷۸، سطر ۷۹) وجودش را
> با کلید `VERSION.json → crm_version` کنترل می‌کند.

## ۱) درخواست کارفرما

> «یک بررسی هم روی اعلانات و کارتابل داشته باش. اعلانات و کارتابل اصلاً به‌روز نیست
> و کارهای بسیار قدیمی و اعلانات بسیار قدیمی در آن وارد می‌شود و زدن گزینهٔ خواندن هم
> موقت از بین می‌برد و پس یک هارد رفرش دوباره همان اعلانات قدیمی برمی‌گردد و این موضوع
> خیلی آزار دهنده است. باید سیستمی طراحی شود که اعلانات مهم و به‌روز دیده شوند و با
> گزینهٔ خواندم از بین بروند.»

## ۲) ریشه‌ها (چهار نقص مستقل — هر چهار تا با مکانیزم متفاوت احیا می‌ساختند)

1. **بازتولید کارتِ خوانده‌شده با شناسهٔ تازه (حلقهٔ سازنده‌ها).** `notify()` فقط وقتی
   کارت هم‌dkey را بازاستفاده می‌کرد که `readBy` خالی باشد (استثنا: referral پایدار).
   سازنده‌های دوره‌با dkey صریح — سررسید چک (`chq-due-<cd>`، **هر روز** با مهر
   `notified[today]`)، اخطار سهمیهٔ حافظه (`quota-*`)، گزارش مدیریتی (`dkey=دوره`)،
   مهلت RFQ/پرونده هنگام از دست رفتن مهر — پس از «خواندم» در شلیق بعدی **کارت تازه با
   cd تازه و readBy خالی و تاریخ امروز** می‌ساختند. همان اعلان، هر روز، تا ابد.
2. **گم شدن فرمان «خواندم» در هارد رفرش.** `ntfRead/ntfReadAll` از مسیر
   `ptfEntitySaveCollection` می‌روند: نوشتِ محلیِ بی‌صدا + فرمان `entity_upsert`
   ناهمگام — **بدون dirty و بدون صف پایدار**. اگر صفحه دقیقاً mid-flight رفرش/بسته
   شود، فرمان برای همیشه گم می‌شود: سرور هرگز readBy را نمی‌بیند و کلید dirty هم
   نیست. (مهر `notifiedUsers` یادآورها و بقیهٔ فرمان‌های اتمیک هم همین خطر را داشتند.)
3. **جایگزینی خامِ notifs در pull.** در اعمالِ `data_pull` (sync.js)، کلیدِ نه-dirty و
   نه-held با `pullWrite(k, newStr)` **خام جایگزین** می‌شد. `ptf_crm_notifs` با اینکه
   کلید اتحاد است (`isSharedUnionKey`) از این قاعده مستثنا نبود: readBy محلیِ
   commitنشده (ریشهٔ ۲) با نخستین pull که rev سرور بالا رفته بود پاک می‌شد و
   **همان اعلان قدیمی با همان تاریخ قدیمی برمی‌گشت** — دقیقاً «پس از هارد رفرش».
4. **بی‌سنی کارت‌های اقدام‌دار.** طبق v33.4.1 اعلان «مهم» هرگز با گذر زمان حذف
   نمی‌شد؛ `ptfPruneStaleNotifs` فقط خبرهای غیراقدام‌دار را پاک می‌کرد. نتیجه: انباشت
   کارت‌های اقدام‌دارِ ماه‌ها قدیمی (خوانده‌نشده = همیشه در نما؛ خوانده‌شده = همیشه در
   داده) — «کارها و اعلانات بسیار قدیمی».

نقص تکمیلی (چنددستگاهی): `entity_upsert` سرور رکورد را field-wise ادغام می‌کرد ولی
`readBy` حاضر در payload را **wholesale** جای سرور می‌گذاشت — snapshot کهنهٔ یک دستگاه
می‌توانست «خواندم» کاربر دیگر را پاک کند.

## ۳) اصلاح

| # | تغییر | فایل | اثر |
|---|---|---|---|
| ۱ | **dedupe چسبندهٔ dkey صریح**: کارتِ !done هم‌dkey — خوانده یا نخوانده — همیشه بازاستفاده می‌شود (repeat/lastT + تازه‌سازی محتوا)؛ کارت جدید فقط وقتی هیچ کارت زنده‌ای با آن هویت نیست. dkey خودکار (عنوان‌محور) همان قرارداد v31.7.10/تستر۱۸۵: وقوع تازه پس از خواندن = کارت تازه | `crm/rbac.js` (notify) | «خواندم» دیگر با شلیق روزانهٔ سازنده باطل نمی‌شود |
| ۲ | **suppress بازتولیدِ کارت بایگانیِ سنی**: کارت done+ageArchived با همان dkey صریح، شلیق دوباره را می‌بلعد (در `notify` و `addMsg`)؛ با حل/تعویق منبع، resolver کارت را سخت حذف و dkey را آزاد می‌کند | `crm/rbac.js`، `crm/bridge.js` | منبع کهنه هر تیک/هر روز کارت «بسیار قدیمی» تازه نمی‌سازد |
| ۳ | **سیاست سنی (جایگزین «هرگز منقضی نمی‌شود» برای کارت‌های کهنه)**: مهمِ خوانده‌نشدهٔ >۳۰ روز → `done+ageArchived` (خارج از نمای پیش‌فرض؛ زیر «نمایش انجام‌شده‌ها» با برچسب بایگانی دیده می‌شود) · مهمِ خوانده‌شدهٔ >۶۰ روز → سخت حذف · بایگانیِ >۹۰ روز → سخت حذف. همه تک‌رکوردی (`ntfCommitRemoval`، بدون شکاف MAX_OPS) + audit. منبع کسب‌وکار (چک/یادآور/پرونده) دست‌نخورده | `crm/rbac.js` (ptfPruneStaleNotifs) | کارتابل = «مهم و به‌روز»؛ قدیمی‌ها خودکار کنار می‌روند |
| ۴ | **pull برای notifs هرگز خام جایگزین نمی‌شود**: همیشه `ptfSmartMerge` (اتحاد readBy/done/repeat + dedupe ارجاع + حفظ ageArchived) + tombstone پس از merge (کارت سخت‌حذف‌شدهٔ سرور زنده نمی‌شود) + اگر نتیجه بیش از نسخهٔ سرور است → dirty تا push با merge کانونیک همگرا کند | `crm/sync.js` | readBy محلی هیچ‌وقت با pull نمی‌پرد |
| ۵ | **ژورنال فرمان‌های در پرواز (INFLIGHT-JOURNAL)**: هر فرمان اتمیک غیر read-only هنگام ارسال ثبت و روی `pagehide/beforeunload` در `localStorage.ptf_sd_inflight` پایدار می‌شود؛ پس از bootstrap نشست بعد با **همان idempotencyKey** بازپخش می‌شود (WAL سرور: commit‌شده → رسید، نشده → commit)؛ سقف ۴۸ ساعت/۴۰ فرمان؛ خطای قطعی = تسویه و حذف از ژورنال. مالکِ ذخیرهٔ کلید، لایهٔ دادهٔ sync است (`ptfSyncInflightJournal*` در sync.js — اصل A10/E2 نگهبان معماری: sales-domain-v2 نازک می‌ماند) | `crm/sales-domain-v2.js` + قلاب بوت در `crm/sync.js` | هارد رفرش mid-flight دیگر «خواندم» (و مهر یادآور/هر فرمان اتمیک) را گم نمی‌کند |
| ۶ | **STALE-BOOT-GUARD**: چهار سازندهٔ کارت (`checkOfferExpiry/checkRfqDue/checkDealDue/checkDueReminders`) تا `window._ptfSyncBootstrapped` اجرا نمی‌شوند (توصیهٔ §7 RCA 2026-09-08) + prune سیاست سنی در بوت و در `renderCartable` (با همان گارد) | `crm/bridge.js`، `crm/rbac.js` | بوت روی کش کهنهٔ فاز B برای منبعِ بسته‌شده در دستگاه دیگر کارت نمی‌سازد |
| ۷ | **سرور**: اتحاد `readBy` و OR شدن `done` در `entity_upsert` برای `ptf_crm_notifs` + اتحاد `ageArchived/ageArchivedAt` در merge کانونیک `data_push` (هر دو حلقه) | `api/sales-domain.php`، `api/crm.php` | snapshot کهنهٔ هیچ دستگاهی «خواندم» دیگران را پاک نمی‌کند؛ خروجی سرور == کلاینت (ACK صادقانه) |
| ۸ | `ptfResolveRfqReferral` هم به حذف تک‌رکوردی `ntfCommitRemoval` منتقل شد (شکاف MAX_OPSِ باقی‌مانده از v34.38.6) | `crm/rbac.js` | حذف انبوه ارجاع‌ها روی سرور واقعاً حذف می‌شود |

## ۴) سازگاری / قراردادها

- قرارداد تستر۱۸۵ (v31.7.10) دست‌نخورده: dkey **خودکار** پس از خواندن، وقوع تازه را
  کارت تازه می‌دهد؛ merge اتحاد readBy همان است.
- قرارداد v34.8.17/34.8.18 (DING-LOOP/ANTI-RESURRECT) و v34.38.6 (DEAL-DUE-SETTLED)
  حفظ شد؛ تستر۵۲۲ سبز.
- «انجام شد»/حل منبع = حذف سخت کارت (مثل قبل)؛ «خواندم» = per-user و پایدار.
- ageArchived یک وضعیت داده‌ای جدید است: `done:true` + پرچم + `ageArchivedAt` — در
  merge کلاینت/سرور اتحاد می‌شود تا زودهنگام حذف نشود.

## ۵) تست

- **tester659-v34.38.24-notif-fresh.js** (ثبت‌شده در گیت): ۴۳ چک ایستا+رفتاری —
  dedupe چسبنده، suppress بایگانی، سیاست سنی (۳۰/۶۰/۹۰)، merge اتحاد
  (readBy/done/ageArchived)، ژورنال in-flight (persist/replay/TTL/مسیر شکست).
  **روی کد قبل از اصلاح: ۷ PASS / ۲۲ FAIL** (شاهد قرمز-قبل-سبز)، روی اصلاح‌شده ۴۳/۰.
- گیت کامل CI: دیدن خلاصه در انتهای RCA.

## ۶) پرونده‌ها

- `ARENA-NOTIF-CARTABLE-FRESHNESS-RCA-2026-09-17.md` — RCA کامل (زنجیرهٔ احیا، نقشهٔ
  کد، چرایی هر تصمیم طراحی).
- `ARENA-NOTIFICATIONS-RESURRECTION-RCA-2026-09-08.md` — RCA پیشین (تحلیلی)؛ §7 آن
  (boot guard) در این نسخه پیاده شد و بندهای P1 آن در v34.38.6 پیاده شده بود.
