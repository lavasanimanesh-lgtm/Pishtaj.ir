# RELEASE NOTES — v34.7.57 (۱۴۰۵/۰۵/۲۹ — 2026-08-20)

## نظم بصری فرم پیشنهاد (فنی / مالی / فنی-مالی / رویژن)

### ارزیابی — چه چیزهایی نامرتب بود

| # | مشکل | اثر |
|---|---|---|
| ۱ | «قالب چاپ سند» و کارت بزرگ امضا **بالای** کارفرما/درخواست بودند | ترتیب پرکردن فرم غیرطبیعی؛ فیلد اصلی (کارفرما) وسط فرم گم می‌شد |
| ۲ | ردیف «اعتبار پیشنهاد» با یک **ستون خالی** رندر می‌شد | نیمهٔ چپ ردیف خالی و نامتوازن |
| ۳ | نوار ابزار اقلام: ۶ دکمه با ۵ رنگ/استایل ناهماهنگ، بدون قاب | شلوغ و بی‌سلسله‌مراتب |
| ۴ | غلط تایپی «⛭ ستون‌های تکمیلی **ستون‌ها**» (دکمه + عنوان پنجره) | غیرحرفه‌ای |
| ۵ | سرتیترهای بخش (`h4`) بدون تفکیک بصری | فرم بلند یکدست و بی‌مرز دیده می‌شد |
| ۶ | فوتر با `div` خالی برای فاصله‌گذاری و غیرچسبان | در فهرست‌های بلند (مثلاً ۲۶۷ قلم) دکمهٔ ذخیره ته اسکرول گم می‌شد |

### اصلاحات

1. **ترتیب طبیعی فیلدها:** کارفرما/رابط ← اعتبار مشتری ← درخواست/تاریخ ←
   (قالب چاپ + اعتبار پیشنهاد در **یک ردیف متوازن**) ← رابط فروشنده ← امضا ← اقلام.
   قرارداد MOB-041 حفظ شد: کارت امضا همچنان **قبل از اقلام** و در دید موبایل است.
2. **نوار ابزار اقلام** (`.offer-items-toolbar`): قاب یکدست با پس‌زمینه، دو اکشن اصلی
   (بارگذاری از درخواست / از درخواست دیگر) solid + جداکننده + سه اکشن ورودی
   (کالا / ورود اکسل / قیمت از اکسل) outline یکدست + «ستون‌های تکمیلی» جدا در انتها.
3. **سرتیترهای بخش** (`.offer-sec-title`): «📦 اقلام» و «📜 شرایط و ضوابط» با خط جداکننده.
4. **فوتر چسبان** (`.offer-form-footer`): انصراف / پیش‌نمایش / ذخیره همیشه در دید؛
   ریسپانسیو موبایل (دکمه‌ها تمام‌عرض).
5. رفع غلط تایپی؛ حالت تیره برای نوار ابزار و فوتر در `theme-contrast.js`.

### ایمنی قراردادها (بدون تغییر رفتار)

- همهٔ idها و هندلرها حفظ شد: `ofBuyer` `ofContact` `ofInq` `ofDateJ` `ofPrintAs`
  `ofValidJ` `ofSeller` `ofUseSig` `offInqBtn` `offOtherInqBtn` `offXls` `offSaveBtn` …
- قرارداد **case-revision** (فرم رویژن همین فرم است): `h3` عنوان، بنر بعد از h3،
  `#offSaveBtn` داخل گروه دکمه‌ها، والدِ والد = کانتینر اکشن‌ها، دکمهٔ اول = «انصراف» —
  فیلدهای رویژن مثل قبل بالای فوتر تزریق می‌شوند.
- tester117 (قالب چاپ)، tester448 (رویژن با فرم کامل)، tester449 (دکمه‌های بارگذاری) سبز.

### تست

- تستر جدید `tester460-v34.7.57-offer-form-layout.js` (در گیت) — ۲۶ قرارداد ترتیب/کلاس/فوتر/رگرسیون.
- **گیت CI: ۷۶ PASS / ۰ FAIL** · نگهبان معماری: PASS.

### UAT پیشنهادی

1. پیشنهاد مالی جدید: ترتیب فیلدها را ببینید (کارفرما اول، قالب چاپ+اعتبار یک ردیف، امضا قبل از اقلام).
2. با فهرست بلند اقلام اسکرول کنید — دکمهٔ ذخیره باید پایین پنجره چسبان بماند.
3. رویژن پرونده را باز کنید — بنر رویژن، فیلد دلیل و دکمهٔ «ثبت رویژن» باید مثل قبل کار کند.
4. حالت تیره و موبایل را چک کنید.
