# RELEASE-NOTES — v33.22.1 (۱۴۰۵/۰۸/۱۱ — 2026-08-02)

## 🔓 بازنشانی خودسرویس ویزارد + رفع قفل OPcache (به گزارش مستقیم کارفرما)

گزارش کارفرما: «عبارت mode را در ptf-db-config.php به off تغییر دادم ولی صفحهٔ migrate.php کماکان
پیام تکمیل مهاجرت را نشان می‌دهد.» راستی‌آزمایی زنده (هر دو دامنه) تأیید کرد سرور همچنان
`mode = mysql` می‌خواند.

### علت ریشه‌ای

`api/ptf-db-config.php` یک اسکریپت PHP است که با `require` خوانده می‌شود؛ خروجی آن (آرایهٔ کانفیگ)
توسط **OPcache** سرور کش می‌شود. روی هاست اشتراکی، ویرایش دستی فایل در سی‌پنل تا انقضای کش —
یا با `opcache.validate_timestamps=0` عملاً هرگز — در `require` دیده نمی‌شود. (سناریوی جایگزین:
ویرایش فایل اشتباه/مسیر اشتباه — که با نمایشگر زمان آخرین تغییر فایل تشخیص داده می‌شود.)

### تغییرات

| فایل | تغییر |
|---|---|
| `api/db-lib.php` | `ptf_db_config()`: قبل از `require`, `clearstatcache` + `opcache_invalidate` (محافظت‌شده با `function_exists`) → خواندن همیشه از دیسک. `ptf_db_save_config()`: بعد از نوشتن موفق، همان باطل‌سازی → اثر فوری روی درخواست‌های بعدی. |
| `api/migrate.php` | **گام جدید `reset_mode`** (قبل از گارد mode=mysql بررسی می‌شود — وگرنه در همان حالتی که بازنشانی لازم است قفل راه نمی‌داد): فقط وقتی mode=mysql، با `mig_token` + تایپ کلمهٔ «بازنشانی». فقط `mode` به `off` برمی‌گردد؛ نام/رمز دیتابیس و هیچ داده‌ای (فایل/DB) دست‌نخورده. سپس مراحل ۱–۶ دوباره قابل اجرا هستند. |
| `api/migrate.php` (صفحهٔ قفل) | **تشخیص‌گر وضعیت**: mode خوانده‌شده + مسیر فایل + زمان آخرین تغییر فایل کانفیگ (اگر ویرایش دستی دیده نشود، علت دقیق مشخص است) + فرم بازنشانی + تذکر حذف فایل. |

### امنیت

- بدون `mig_token` و کلمهٔ «بازنشانی» هیچ تغییری رخ نمی‌دهد (همان الگوی مراحل حساس فعلی).
- بازنشانی هیچ داده‌ای پاک نمی‌کند — فقط منبع خواندن به فایل برمی‌گردد (= رفتار امروز).
- گارد پس از سوییچ همچنان جلوی اجرای ناخواستهٔ مهاجرت را می‌گیرد (tester297 سبز: ۱۹/۰).

### نسخه‌گذاری

`window.VER = 'v33.22.1'` در crm/index.html (+۸۳ باستر)، crm/sw.js (`ptf-crm-v33.22.1`)، crm/clear-cache.html.

## تست
- tester302-migrate-reset-opcache (جدید): **۱۰/۰** — باطل‌سازی OPcache خواندن/نوشتن، ترتیب reset قبل از گارد، گارد token+کلمه، عدم تغییر بقیهٔ کانفیگ، فرم روی صفحهٔ قفل، تشخیص‌گر mtime.
- tester301 = ۲۰/۰ (گیت نسخه انعطاف‌یافته) · tester297 = ۱۹/۰.
- رگرسیون کامل: ۲۹۷ تستر → فایل **۲۴۱/۵۶** (= baseline، صفر شکست جدید)، چک **۵۴۲۱/۸۴** (= baseline + 301 + 302).
- audit: ۰ خطا · crm-update.zip بازسازی شد (۵۵۸ ورودی — api/db-lib.php و api/migrate.php به‌روز).

## گام بعدی (استیجینگ — توسط کارفرما)
۱) پوش این نسخه → دیپلوی خودکار staging.pishtaj.ir. ۲) باز کردن `https://staging.pishtaj.ir/api/migrate.php` با Ctrl+Shift+R. ۳) روی صفحهٔ «مهاجرت کامل شده»: تایپ «بازنشانی» ← دکمهٔ بازنشانی. ۴) اجرای مراحل ۱ تا ۶. ۵) تست CRM استیجینگ و گزارش نتیجه → سپس مرج PR #15 و همین روال روی پروداکشن.
