# ASSESSMENT — ارزیابی مقیاس بزرگ ماژول تأمین‌کنندگان و استارت سرد CRM

**تاریخ:** 2026-08-22 (۱۴۰۵/۰۵/۳۱)
**نسخه:** v34.7.86
**برنچ:** `arena/01a02872-pishtaj-ir`
**هدف:** بدون تغییر کدِ منطقی، سناریوی اندازه‌گیری «مقیاس بزرگ» و نقشه‌ی گام‌های بعدی (استارت سرد، MySQL، E2E موبایل) را مستند کنیم تا تصمیم‌گیری بر پایه‌ی داده باشد.

---

## ۱. وضعیت فعلی (خلاصه بهینه‌سازی‌های انجام‌شده)

| نسخه | بهینه‌سازی |
|---|---|
| v34.7.81 | gzip + امضای `since` در `get_inbox`؛ صفحه‌بندی ۵۰تایی رندر «درخواست‌های سایت»؛ رندر یک‌باره `renderSuppliers`؛ حذف محاسبه‌ی دوباره‌ی `balance` |
| v34.7.82 | خود-تشخیص همگام‌سازی + تست اتصال دقیق‌تر (data_rev / نشست) |
| v34.7.83 | صفحه‌بندی فهرست تاییدشده `renderSuppliers2` (۵۰ در گام) |
| v34.7.84 | ورود اجباری با توکن سرور + پاک‌سازی توکن در reset/idle |
| v34.7.85 | Lazy-load مالی تأمین‌کنندگان + صفحه‌بندی اختیاری `get_inbox` |
| v34.7.86 | فعال‌سازی صفحه‌بندی `get_inbox` در کلاینت + ادغام گام‌به‌گام |

---

## ۲. سناریوی اندازه‌گیری مقیاس بزرگ

### ۲.۱ داده‌ی پیشنهادی

| مجموعه | حجم پیشنهادی | هدف |
|---|---|---|
| `suppliers.json` (ثبت‌نام سایت) | ۱٬۰۰۰–۵٬۰۰۰ رکورد | بررسی real `get_inbox` + رندر «درخواست‌های سایت» |
| `ptf_crm_suppliers` (تاییدشده) | ۵۰۰–۲٬۰۰۰ رکورد | بررسی `renderSuppliers2` صفحه‌بندی |
| `ptf_crm_supplier_finance` | ۵۰۰–۲٬۰۰۰ فاکتور/پرداخت | بررسی lazy-load مالی (`slBoxRows`) |
| `ptf_crm_invoices` + `ptf_crm_payables` | مشابه بالا | اندازه‌ی اثر روی `balance()` |

> تولید داده‌ی مصنوعی فقط در محیط تست انجام شود؛ در محیط production هرگز ساخته نشود.

### ۲.۲ معیارهای اندازه‌گیری

1. **Network → `get_inbox`:**
   - قبل/بعد (نسخه‌ی قبلی اجرای بدون صفحه‌بندی)
   - زمان (TTFB + Transfer Size) برای پاسخ غنی در مقابل پاسخ `fresh`.
   - در بوت با ۵۰ رکورد، حجم مورد انتظار کوچک (خیلی کمتر از کل صندوق).
2. **Performance → `renderSuppliers2`:**
   - Duration + Layout/Paint برای ۵۰۰ / ۱٬۰۰۰ / ۲٬۰۰۰ رکورد.
   - مدت زمان دکمهٔ «نمایش بیشتر» و جستجو.
3. **Performance → باکس مالی:**
   - مدت زمان رندر اولیه‌ی پنل در مقابل مدت زمان `ptfSlBoxLazy`.
   - شمارش calls به `balance()` (از طریق Profiler) — باید یک‌بار به‌ازای هر تامین‌کننده باشد، نه چندبار.
4. **آخرین خطا/نشست:**
   - Settings → «🔎 تشخیص همگام‌سازی» بعد از ورود/خروج مجدد روی موبایل.
5. **Cold start:**
   - DevTools Network: زمان `DOMContentLoaded`، تعداد و حجم فایل‌های JS، و Total bytes منتقل‌شده.

### ۲.۳ جمع‌بندی موردانتظار

- `get_inbox` گام‌به‌گام: کاهش قابل‌انتظار در حجم دانلود بوت/پول دوره‌ای وقتی صندوق بزرگ می‌شود.
- `renderSuppliers2`: باز شدن پنل با رکوردهای زیاد باید در سطح «صفحه‌ی ۵۰» بماند.
- lazy مالی: باز شدن پنل نباید منتظر جمعِ کل گردش‌ها باشد؛ با باز شدن باکس جدول پر می‌شود.
- اگر پس از استقرار، این اعداد عالی نبودند، سراغ گام‌های زیر بروید.

---

## ۳. گام‌های بعدی (با ریسک)

| گام | شرح | ریسک | پیش‌نیاز |
|---|---|---|---|
| A | اندازه‌گیری واقعی بالا + ثبت در گزارش | کم | استقرار + محیط تست |
| B | `get_inbox` نقطه‌ای MySQL (`src='site'`, `status=pending`, LIMIT) | بالا (نیاز DB) | فعال‌سازی db-lib/mysql |
| C | استارت سرد SPA: جداسازی bundleهای سنگین (چک/مودیان/تحلیل/گزارش) | متوسط–بالا | بازبینی وابستگی‌ها + تست‌های لود |
| D | E2E موبایل/PWA | متوسط | دستگاه/مرورگر واقعی |
| E | پیکربندی مشترک "on-demand module" برای پنل‌های کم‌استفاده | متوسط–بالا | confirm رفتار آفلاین/سینک |

### ۳.۱ استارت سرد SPA (گام C) — نکات

- `crm/index.html` هم‌اکنون ~۱۰۹ تگ `<script>` با `defer/async` دارد.
- بهینه‌سازی ساده‌تر (کم‌ریسک) پیش از جداسازی کامل:
  1. اولویت‌بندی `mobilenav.js`/`ui-kit.js` (هم‌اکنون اول هستند).
  2. انتقال `xlsx.min.js` به `async` (هم‌اکنون async است).
  3. حذف `defer` از ماژول‌های استفاده‌شونده فقط در پنل‌های خاص؟ **خیر** — بسیاری از آن‌ها هوک‌هایی روی `window.buildSettings`/`goPanel` دارند و برداشتن `defer` معنی کارکردی ندارد؛ بهتر است به **لود تنبل پس از first paint** فکر کنیم، نه تمرکز بر تعداد تگ‌ها.
  4. اندازه‌گیری Total JS bytes ابتدا — اگر حجم جاوااسکریپت اصلی (بدون ماژول‌های سنگین) مثلاً <۱MB است، افزایش سرعت با کد تغییرات کم نیاید.
- **نتیجه‌گیری:** تغییر C را بدون عدد واقعی و بدون تست A معرفی نمی‌کنم؛ ابتدا A.

---

## ۴. پایش کاربر (به‌ویژه حسابدار بعد از v34.7.84)

سناریوی تأیید نهایی:
1. از دسکتاپ ادمین: تعریف کاربر + پیام «با سرور همگام شد»؛ در غیر این صورت «همگام‌سازی کاربران».
2. از موبایل: ورود → خروج → ورود مجدد. نباید پیام «توکن معتبر وجود ندارد» بیاید.
3. اگر پیام خطا آمد، در Settings → «🔎 تشخیص همگام‌سازی» وضعیت `needLogin/offline/forbidden` را بگیرید.
4. اگر `needLogin` است → نشست، نه مشکل شبکه. اگر `offline` است → شبکه/دستگاه.

---

## ۵. نتیجه

- گامهای A (اندازهگیری) کم‌ریسک و همین‌الان با هر تعداد رکورد قابل اجراست.
- گامهای B/C/D/E نیازمند محیط/داده‌ی real یا تغییر معماری‌اند؛ قبل از آن گزارش A ضروری است.
- تمام بهینه‌سازی‌های تأمین‌کنندگان تا v34.7.86 بدون تغییر در مدل داده انجام شده و گیت CI در
  `104 PASS / 0 FAIL` است.
