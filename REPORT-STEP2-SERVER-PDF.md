# مرحلهٔ ۲ — PDF باینری سمت سرور برای گزارش نهایی ADV-CV ✅

**تاریخ:** ۱۴۰۵/۰۶/۲۳ (2026-09-13)
**نسخهٔ انتشار:** v34.38.21
**شاخهٔ git:** `arena/2026-09-12-session-pishtaj-ir` (push شده)

---

## ۱) چه چیزی اضافه شد؟

قبلاً گزارش نهایی ابزار ADV-CV فقط یک فایل **HTML** بود؛ مشتری باید آن را باز می‌کرد و با
Print/Save-as-PDF مرورگر خود PDF می‌ساخت. حالا:

- **PDF باینری واقعی، روی سرور** ساخته می‌شود (با موتور wkhtmltopdf / نسخهٔ patched-Qt).
- در لحظهٔ **صدور** (Issue) گزارش، PDF همزمان ساخته می‌شود؛ اگر هاست هنوز wkhtmltopdf
  نداشته باشد، **صدور گزارش به‌هیچ‌وجه مختل نمی‌شود** — فقط PDF را بعداً می‌سازید.
- برای گزارش‌هایی که **قبل از این نسخه** صادر شده بودند هم می‌شود PDF ساخت: همان دکمه
  «📥 تولید PDF سروری» روی همان گزارش‌های قدیمی هم کار می‌کند (تولید idempotent).
- در **CRM → ابزارهای پیشرفته** و در **gate نهایی ADV-CV** نشان می‌دهد wkhtmltopdf
  روی هاست نصب است یا نه (با نسخه) — یعنی «چرا PDF نمی‌سازد» را همان‌جا می‌بینید.
- گزارش‌های قدیمی و جدید هر دو: دکمهٔ «📄 دانلود PDF سرور» (بعد از ساخت) +
  «🖨 دانلود HTML (Print/Save as PDF)» به‌عنوان جایگزین همیشگی.

## ۲) امنیت (مهم)

- PDFها در دایرکتوری خصوصی `crm/data/tool_report_pdfs` ذخیره می‌شوند که از وب **دست‌نیافتنی** است
  (`.htaccess: Deny from all`) — هیچ لینک مستقیمی به آن‌ها وجود ندارد؛ دانلود فقط از درون CRM و
  با توکن معتبر.
- هر بار پیش از ارسال، **فingerprint (sha256)** فایل با ثبتِ لحظهٔ ساخت مقایسه می‌شود؛
  هرگونه دستکاری = ارسال قطع (fail-closed).
- موتور رندر فقط به دایرکتوری موقتِ همان رندر دسترسی دارد (`--allow` محدود) — نه به بقیهٔ فایل‌های سرور.
- نام فایل‌ها sanitize می‌شود (امکان path-traversal بسته است — تست‌شده).
- خروجی با `Cache-Control: no-store` ارسال می‌شود (کش نگه‌داشته نمی‌شود).

## ۳) کیفیت و آزمون

- **CI gate: 269 تست — سبز ۲۶۹، شکست ۰** (شامل تست‌قرارداد جدید `tester656`).
- **رگرسیون کامل: ۶۴۱ تست — سبز ۵۲۴ + ۱۱۷ قرنطینهٔ مستند (قراردادهای منسوخ) + شکست واقعی ۰**.
- تست `tester656` زنجیرهٔ کامل را با **PHP واقعی** اجرا می‌کند: وضعیت → تولید → دانلود →
  دستکاری فایل → قطع ارسال (500) → توکن جعلی → 401.
- دو تست نگهبان قدیمی (`tester184` قرارداد احراز هویت / BUG-AUTH-001 و `tester189`
  ناپدیدشدن کاربر) که به شرط محیط وابسته بودند، **خودکفا** شدند و با قرارداد فعلی
  ورود (فیلد `password`) هم‌راستای شدند — یعنی حالا واقعاً محافظت می‌کنند نه اینکه
  از روی موقعیت سبز باشند.
- بامب نسخهٔ رسمی v34.38.20 → v34.38.21 با ابزار رسمی `bump-version-pins.js` انجام شد
  (۱۳۰ تستر، ۵۷۵ pin) + یادداشت انتشار `RELEASE-NOTES-v34.38.21.md`.

## ۴) وضعیت این لحظه

| کار | وضعیت |
|---|---|
| کد سمت سرور (api/tools.php) | ✅ push شده روی شاخهٔ arena |
| رابط کاربری CRM | ✅ push شده |
| bump نسخه + یادداشت انتشار | ✅ push شده |
| تست‌ها (gate + رگرسیون) | ✅ سبز |
| **ادغام به main + داپلی** | ⬜ اقدام بعدی (بخش ۵) |
| **نصب wkhtmltopdf روی هاست** | ⬜ بعد از داپلی (بخش ۶) |

---

## ۵) اقدامات روی GitHub (مرحله‌به‌مرحله)

> داپلی سایت از شاخهٔ **main** انجام می‌شود؛ کار ما روی شاخهٔ `arena/2026-09-12-session-pishtaj-ir`
> است. برای زنده‌شدن این نسخه روی سایت:

1. وارد GitHub شوید و به مخزن `Pihtaj.ir` بروید:
   `https://github.com/lavasanimanesh-lgtm/Pishtaj.ir`
2. بالای صفحه پیام سبز «**N new commits** … Compare & pull request» را می‌بینید؛
   روی دکمهٔ **Compare & pull request** کلیک کنید.
3. در صفحهٔ جدید:
   - Source branch: `arena/2026-09-12-session-pishtaj-ir` (انتخاب شده)
   - Base branch: `main`
   - عنوان: می‌توانید بنویسید: `v34.38.21 — PDF server-side ADV-CV + release bump`
   - روی **Create pull request** بزنید.
4. پایین صفحه روی **Merge pull request** و بعد **Confirm merge** کلیک کنید.
5. حالا به تب **Actions** (بالای مخزن) بروید → workflow **«Deploy to Production»** →
   دکمهٔ **Run workflow** → branch: `main` → در کادر تایید حتماً عبارت **DEPLOY** را
   دقیقاً تایپ کنید → **Run workflow**.
6. workflow چند دقیقه طول می‌کشد (گیت‌ها + انتقال FTP). در پایان باید سبز (✅) باشد.

> اگر در هر مرحله چیزی را ندیدید یا دکمه‌ای رد شدن خواستید، یک اسکرین‌شات بفرستید تا دقیق راهنمایی کنم.

---

## ۶) نصب wkhtmltopdf روی هاست (بعد از داپلی)

> فقط **یک دستور** در ترمینال هاست. (اگر ترمینال SSH ندارید، معمولاً در پنل هاست
> بخش «Terminal» / «SSH» وجود دارد؛ یا از اپ Termius روی موبایل.)

1. وارد ترمینال هاست شوید (SSH).
2. این یک‌خطی را کپی و اجرا کنید (اسکریپت را از خودِ GitHub می‌گیرد و نصب می‌کند):

```bash
curl -fsSL https://raw.githubusercontent.com/lavasanimanesh-lgtm/Pishtaj.ir/main/_tools/host/install-wkhtmltopdf.sh -o /tmp/install-wkhtmltopdf.sh && sudo bash /tmp/install-wkhtmltopdf.sh
```

3. اسکریپت idempotent است (اجرای دوباره بی‌خطر) و در پایان **پروندهٔ رندر** هم می‌زند:
   خروجی باید شامل این دو خط باشد:

```
Probe PDF rendered OK (… bytes).
=== Done ===
```

4. اگر پیام `wkhtmltopdf already installed` دیدید، یعنی از قبل نصب بوده و کاری لازم نیست.

## ۷) تأیید نهایی (بعد از داپلی + نصب)

1. وارد CRM شوید → **ابزارهای پیشرفته** → ببینید نشان «**PDF سروری: فعال**» سبز شده است.
2. یک گزارش ADV-CV آزمایشی: دادهٔ نمونه → **صدور گزارش نهایی**.
3. روی گزارش صادرشده دکمهٔ «📄 دانلود PDF سرور» را بزنید؛ فایل PDF باید مستقیم دانلود شود.
4. یک گزارش **قدیمی** (قبل از این نسخه) را هم باز کنید و با «📥 تولید PDF سروری» →
   «📄 دانلود PDF سرور» PDF بگیرید.

---

## ۸) اگر wkhtmltopdf نصب نشود چه؟

هیچ چیزی خراب نمی‌شود: صدور گزارش‌ها و همهٔ قابلیت‌های دیگر عادی کار می‌کنند؛ فقط
خروجی PDF موقتاً همان HTML + Print/Save-as-PDF مرورگر است. هر زمان که wkhtmltopdf
نصب شد، دکمهٔ PDF سروری (برای گزارش‌های جدید و قدیمی) بلافاصله فعال می‌شود.
