# Release Notes — v31.7.23

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.23
**نوع:** 🟢 قابلیت درخواستی کارفرما — پیش‌نویس سراسری فرم‌ها (US-DRAFT-EVERYWHERE)

---

## 🎯 درخواست کارفرما

«در سراسر نرم‌افزار هیچ امکانی برای ذخیره پیش‌نویس وجود ندارد. مثلاً وسط نوشتن نامه یادم می‌آید کاری را باید انجام دهم؛ با بستن پنجره کل نوشته‌ها از دست می‌رود. امکان ثبت پیش‌نویس باید همیشه وجود داشته باشد — با مشاوران و متخصصان ارشد گفتگو و راهکار اجرایی **بدون ایجاد باگ در سراسر نرم‌افزار** طراحی و پیاده‌سازی شود.»

## 🧠 صورت‌جلسه پنل متخصصان (سند کامل: `EXPERT-PANEL-DRAFTS-v31.7.23.md`)

سه گزینه بررسی شد؛ دو گزینه رد شد (دکمه دستی در ~۵۰ فرم = سطح تماس عظیم؛ رکورد draft در دیتابیس ماژول‌ها = آلودگی داده sync). گزینه مصوب: **لایه سراسری غیرمداخله‌گر `draftx.js`** — چون همه فرم‌های سیستم الگوی واحد دارند (مودال `.md-b` + فیلدهای id-دار)، یک لایه event-delegation می‌تواند همه را بدون دست‌زدن به حتی یک خط از کدشان پوشش دهد.

## ✅ قابلیت — چگونه کار می‌کند

| رفتار | جزئیات |
|--------|--------|
| **ذخیره خودکار بی‌صدا** | هر تایپی در هر فرم مودالی (نامه، درخواست، کالا، چک، قرارداد، استعلام، …) با debounce ذخیره می‌شود — نیازی به هیچ دکمه‌ای نیست |
| **بازیابی opt-in** | بازکردن دوباره همان فرم → نوار زرد «📝 پیش‌نویس ذخیره‌شده از {تاریخ} موجود است» با دو دکمه «↩️ بازیابی» و «🗑 حذف» — هیچ auto-restore غافلگیرکننده‌ای |
| **پاکسازی هوشمند** | ثبت موفق (دکمه ثبت/ذخیره + بسته‌شدن فرم) → پیش‌نویس همان فرم پاک؛ بستن/انصراف → پیش‌نویس می‌ماند (دقیقاً سناریوی کارفرما) |
| **پوشش تصاویر نامه** | هوک اختصاصی: تصاویر US-LTR-IMG هم در پیش‌نویس ذخیره/بازیابی می‌شوند |
| **عدم مزاحمت در ویرایش** | فرم ویرایش رکورد موجود (که محتوا دارد) → نوار بازیابی ظاهر نمی‌شود |

## 🔒 قیدهای بی‌خطری (شرط کارفرما — مصوب QA پنل، همه تست‌شده)

1. **صفر تغییر در فرم‌های موجود** — فقط یک فایل جدید + ۲ خط ثبت در index/sw؛ هیچ ماژولی دست نخورد.
2. **کلید `ptf_draft_forms` عمداً خارج از SYNC_KEYS/backup** — پیش‌نویس ناقص هرگز وارد sync/backup/merge نمی‌شود (تجربه BUG-NTF/AVATAR/USERS نشان داد این تنها راه صفرکردن ریسک داده است).
3. **بدون setInterval** — رویدادمحور + MutationObserver (الگوی مصوب پروژه، CPU idle صفر).
4. فیلدهای **password/file/hidden هرگز ذخیره نمی‌شوند**؛ سقف ۱۵ پیش‌نویس؛ انقضای خودکار ۷ روزه؛ محتوای ناچیز (<۱۲ کاراکتر) ذخیره نمی‌شود.
5. هم‌زیستی با autodraft قدیمی فرم پیشنهاد (کلیدهای جدا — بدون تداخل).

## 📁 فایل‌های تغییریافته

`crm/draftx.js` (**جدید** — کل قابلیت)، `crm/index.html` + `crm/sw.js` (ثبت + bump)، `crm/clear-cache.html` (bump)، `EXPERT-PANEL-DRAFTS-v31.7.23.md` + `_tools/uat/tester198` (جدید)

## 📊 نتایج گیت

- تستر جدید `tester198`: **۱۵/۱۵ PASS** — شامل چرخه رفتاری کامل سناریوی کارفرما: تایپ نامه → ذخیره خودکار → «بستن» → بازیابی همه فیلدها؛ + اثبات عدم ذخیره فیلد رمز و خارج‌بودن کلید از sync/backup
- رگرسیون کامل: **176/176 فایل PASS — 4051/4051 چک PASS** | `audit.py` (۱۰ بخش): PASS

## 📋 راستی‌آزمایی توسط کارفرما (۳ دقیقه — دقیقاً سناریوی خودتان)

1. مکاتبات → نامه جدید → گیرنده/موضوع/چند خط متن بنویسید (و یک تصویر اضافه کنید) → **بدون ثبت، پنجره را ببندید**.
2. دوباره «نامه جدید» را باز کنید → نوار زرد «پیش‌نویس ذخیره‌شده» → «↩️ بازیابی» → همه نوشته‌ها و تصویر برگردد.
3. نامه را «تایید و ثبت» کنید → بار بعد فرم خالی و بدون نوار باشد (پیش‌نویس مصرف شد).
4. همین را با فرم دیگری (مثلاً ثبت کالا یا چک) تکرار کنید — همه‌جا کار می‌کند.

---
*توسعه‌دهنده: PTF Development Team — 2026-07-20*
