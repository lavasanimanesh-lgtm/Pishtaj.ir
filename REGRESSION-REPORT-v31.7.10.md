# 📊 گزارش رگرسیون v31.7.10

**تاریخ:** ۱۴۰۵/۰۴/۲۸ (2026-07-19)
**نسخه:** v31.7.10
**ابزار:** `_tools/run-full-regression.js` + `python3 _tools/audit.py` (۱۰ بخش)

## ✅ نتیجه نهایی

| معیار | مقدار | وضعیت |
|--------|-------|--------|
| تعداد تسترها | **۱۶۳** (+۱: tester185-notif-dup-hygiene) | — |
| فایل‌های PASS | **۱۶۳** | ✅ ۱۰۰٪ |
| چک‌های PASS | **۳,۸۴۲** (+۱۹) | ✅ ۱۰۰٪ |
| چک‌های FAIL | **۰** | ✅ صفر |
| audit.py (۱۰ بخش) | **PASS** | ✅ |
| `node --check` سه فایل تغییریافته | **PASS** | ✅ |

## 🆕 پوشش تست جدید (tester185 — رفتاری، نه فقط pattern)

- سه `notify()` یکسان → ۱ رکورد با repeat=3؛ اعلان متفاوت → رکورد جدید؛ پس از خواندن، وقوع جدید → اعلان جدید
- merge واقعی `ptfSmartMerge('ptf_crm_notifs', ...)`: اجتماع readBy دو دستگاه (خوانده‌شده برنمی‌گردد)، repeat حداکثری، بدون حذف رکورد
- `ntfReadAll()` اجرای واقعی → صفر خوانده‌نشده
- تفکیک مهم/عادی (`ntfIsImportant`)
- fingerprint پایدار plan و تغییر آن با duplicate جدید

## 📁 فایل‌های تغییریافته

`crm/rbac.js`، `crm/sync.js`، `crm/codegen.js`، `crm/index.html` (bump)، `crm/sw.js`، `crm/clear-cache.html`، `_tools/uat/tester185-notif-dup-hygiene.js` (جدید)

## 🚦 وضعیت گیت‌ها

- **گیت فنی: سبز** ✅
- **گیت انسانی: معوق** ⏳ — سناریوهای ۴گانه «راستی‌آزمایی توسط کارفرما» در RELEASE-NOTES باید در مرورگر واقعی اجرا شود؛ به‌ویژه سناریوی ۲ (ماندگاری «خواندم» پس از sync دو دستگاه واقعی). طبق درس BUG-AUTH-001، سبز اعلام نمی‌کنیم تا evidence بیاید.
