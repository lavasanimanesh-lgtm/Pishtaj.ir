# ممیزی تضمین ذخیره: «کاربر فکر می‌کند ثبت شد، اما بعداً نیست»

**تاریخ:** ۲۰۲۶-۰۸-۱۳  
**نسخه بررسی‌شده در کد:** `v34.5.14`  
**دامنه:** ثبت پیشنهاد، درخواست، مشتری/تأمین‌کننده، لید، مالی، چک، تنخواه، پیوست و تمام مسیرهای مشترک `setData` / Sync / سرور.  
**روش:** بازبینی ایستا و اجرای تست‌های Sync/UAT موجود؛ بدون دسترسی به مرورگر، داده یا لاگ production.

> **تعریف این ممیزی:** «ثبت» سه مرحله دارد و نباید با هم اشتباه شوند:  
> ۱) ثبت در فرم/حافظهٔ تب، ۲) ثبت پایدار در حافظهٔ مرورگر یا IndexedDB، ۳) ثبت تأییدشده در سرور/MySQL.  
> فقط مرحله ۳ باید پیام «روی سرور ثبت شد» یا نشان سبز بدهد.

---

## ۱. حکم اجرایی

هسته Sync اکنون نسبت به گذشته ایمن‌تر است: صف dirty پایدار، retry، توکن، تعارض مبتنی بر نسخه، snapshot پیش از pull، سپر داده‌صفر و از `v34.5.14` **ACK کلیدبه‌کلید سرور** دارد. یعنی نشان سبز دیگر فقط با پاسخ کلی `ok` نباید روشن شود؛ سرور باید همان کلید را تأیید کند.

با این همه، در سطح کل محصول هنوز مسیرهایی وجود دارد که کاربر می‌تواند «ثبت شد» ببیند، در حالی که فقط ثبت محلی یا ثبت UI انجام شده است. خطر اصلی در فرم‌های اصلی نه پاک‌شدن آرام سرور، بلکه **اعلام موفقیت پیش از تأیید پایدار سرور** است.

### نتیجه ریسک

| سطح | تعداد سناریوی مهم | نتیجه |
|---|---:|---|
| P0 — امکان از دست‌رفتن کار تازه پس از refresh/تعویض دستگاه | ۳ | باید پیش از ادعای «ذخیره شد» در فرم‌های اصلی مهار شود |
| P1 — کاربر تغییر را بعداً گم‌شده/بازنویسی‌شده می‌بیند | ۶ | نیازمند UX شفاف، هم‌ترازی کلید و تست چنددستگاهی |
| P2 — خطای نمایشی، پیوست یا عملیات عمدی | ۶ | داده الزاماً گم نشده، اما برداشت کاربر از وضعیت اشتباه است |

### وضعیت بسیار مهم انتشار
اصلاحات `v34.5.13` و `v34.5.14` هنوز به GitHub/staging push نشده‌اند، چون GitHub برای به‌روزرسانی workflow مجوز `workflows` را رد کرده است. بنابراین تا رفع دسترسی GitHub و deploy، نباید رفتار production را برابر این گزارش بدانیم.

---

## ۲. مسیر واقعی ذخیره یک رکورد

```text
کلیک «ثبت» فرم
  → setData(key, data) / تغییر DOM و پیام UI
  → localStorage یا cache/IndexedDB (فاز B)
  → ثبت key در ptf_sync_dirty / صف آفلاین
  → push پس از debounce یا flush
  → data_push با token + base revision
  → فایل گرم + MySQL (در حالت mysql)
  → ACK همان key از سرور
  → حذف dirty و نشان سبز
```

نقطه شکست در هر پیکان می‌تواند باعث شود کاربر چیزی را روی همان صفحه ببیند ولی در دستگاه دیگر/پس از refresh نبیند.

---

## ۳. سناریوهای P0 — اولویت رفع

### P0-1: شکست ذخیره محلی به علت quota، اما فرم موفقیت نشان می‌دهد

**مکان:** `crm/storage-quota.js`، `crm/client-server.js` و فرم‌های فراوانی که بعد از `setData` فوراً toast/render می‌دهند.

- لایه quota در صورت پر بودن localStorage تلاش به پاک‌سازی می‌کند و در آخر `false` برمی‌گرداند.
- `setData` در فاز B نتیجه `localSet(k, s)` را کنترل نمی‌کند؛ حتی اگر `false` باشد، cache/queue را ادامه می‌دهد و `true` برمی‌گرداند.
- در حالت معمول نیز بسیاری از فرم‌ها مقدار بازگشتی `setData` را کنترل نمی‌کنند و بلافاصله «ثبت شد» نمایش می‌دهند.
- اگر همان لحظه مرورگر crash/refresh شود یا صف محلی هم به دلیل quota نوشته نشود، داده‌ای که فقط در حافظهٔ RAM بوده ممکن است از دست برود.

**نمونه‌های UI با success فوری:** ثبت مشتری/تأمین‌کننده/لید از AI، درخواست در `bridge.js`، بسیاری از فرم‌های پیشنهاد، مالی و چک.

**ریسک:** بالا؛ بیشترین احتمال در مرورگرهای قدیمی، داده حجیم، private mode یا دستگاه موبایل.  
**اصلاح پیشنهادی:** `setData` باید قرارداد نتیجه‌دار `{ok, durable, queued}` داشته باشد؛ اگر local persistence شکست خورد، فرم نباید success نشان دهد و باید دکمه را در حالت «ذخیره نشد؛ بک‌آپ/فضا را بررسی کنید» نگه دارد.

---

### P0-2: فاز B — شکست صف آفلاین یا cache محلی بدون توقف موفقیت فرم

**مکان:** `crm/client-server.js`

- `queueWrite` خطای localStorage را می‌بلعد.
- `localSet` می‌تواند false برگرداند، اما caller آن را نادیده می‌گیرد.
- داده ممکن است تا لحظه flush فقط در `cache` حافظهٔ همان تب باشد.
- بستن فوری تب/کرش قبل از push موفق، در ترکیب با شکست local persistence، مساوی از دست‌رفتن کار تازه است.

**ریسک:** بالا، اما مخصوص فاز B فعال و شرایط حافظه پر.  
**اصلاح پیشنهادی:** صف را در IndexedDB اتمیک بنویسید؛ فقط بعد از موفقیت queue write به فرم اجازه success بدهید. یک تست fault-injection برای `localStorage.setItem` و `indexedDB` لازم است.

---

### P0-3: تغییر با نقش فاقد مجوز Sync محلی می‌ماند و dirty آن حذف می‌شود

**مکان:** `crm/sync.js#pushDirty` و role allowlist.

- برای کلیدی که نقش فعلی اجازه Sync آن را ندارد، client آن key را از `state.dirty` حذف می‌کند و badge نارنجی می‌دهد.
- داده محلی می‌ماند، اما دیگر ضمانت push بعدی ندارد؛ با refresh/ورود روی دستگاه دیگر/pull ممکن است کاربر آن را گم‌شده ببیند.
- نمونه محتمل: کاربر با نقش محدود وارد فرم یا مسیری شود که UI اجازه ویرایشش را کامل نبسته، اما allowlist سرور آن کلید را نمی‌پذیرد.

**ریسک:** بالا؛ به‌خصوص برای داده مالی یا تنظیمات در نقش‌های محدود.  
**اصلاح پیشنهادی:** به‌جای حذف dirty، فرم باید **پیش از تغییر** با همان allowlist قفل شود. اگر به هر علت تغییر محلی رخ داد، key باید در «تغییرات نیازمند اقدام مدیر» نگه داشته شود و امکان export/انتقال امن داشته باشد، نه اینکه صرفاً حذف شود.

---

## ۴. سناریوهای P1 — ذخیره می‌شود اما کاربر بعداً آن را نمی‌بیند یا تغییرش از بین می‌رود

### P1-1: تعارض همزمان دو دستگاه/کاربر روی یک رکورد

سیستم base revision، conflict و `ptfSmartMerge` دارد و این یک نقطه قوت است. اما merge عمومی برای همهٔ ساختارهای nested کامل نیست. اگر دو نفر همزمان روی فیلدهای متفاوت یک فاکتور، پرونده یا object پیچیده کار کنند، بخشی از نسخه بازنده ممکن است با LWW/merge عمومی بازنویسی شود.

**نشانه کاربر:** «من اصلاح کردم، چند دقیقه بعد به حالت قبل برگشت.»  
**اصلاح:** merge سطح‌فیلد برای فاکتور، پیشنهاد، پرونده، پرداخت و پیوست؛ تاریخچه immutable رویدادهای مالی؛ سناریوی E2E دو مرورگر.

### P1-2: payload یا کلید قابل ارسال نیست

سرور برای هر key سقف ۸MB و برای کل request سقف ۴۰MB دارد. Sync اصلی می‌تواند چند key dirty را یکجا بفرستد. در نسخه جدید، اگر سرور key را ACK نکند banner می‌ماند؛ این از سبز کاذب جلوگیری می‌کند، اما هنوز داده به سرور نمی‌رسد تا کاربر اقدام کند.

**اصلاح:** batch کردن Sync اصلی همانند batch فاز B، نمایش نام فارسی کلیدهای pending و action «retry/export».

### P1-3: ناهماهنگی allowlist کلید بین client و server

بازبینی فعلی یک اختلاف واقعی یافت:

| فقط در client `SYNC_KEYS` | وضعیت server `sync_all_keys()` |
|---|---|
| `ptf_crm_treasury_calls` | وجود ندارد |

در نسخه `v34.5.14` این مورد نباید سبز کاذب بدهد، چون ACK ندارد و banner می‌ماند؛ اما خود داده تا هم‌ترازی allowlist به سرور نمی‌رسد.

**اصلاح:** یک منبع واحد برای allowlist یا تست CI که تفاوت client/server را fail کند؛ سپس تعیین کنید خزانه باید Sync شود یا از client list حذف شود.

### P1-4: توکن منقضی، 401، شبکه ضعیف، تب پس‌زمینه یا خاموش‌شدن دستگاه

Retry و refresh token وجود دارد. با این حال، قبل از ACK سرور هیچ تضمینی برای ارسال نیست. `beforeunload` از `sendBeacon` استفاده می‌کند، اما مرورگر نتیجه beacon را برنمی‌گرداند؛ پس beacon «آخرین شانس» است نه تأیید ذخیره.

**اصلاح:** همان rule عملیاتی: کاربر فقط پس از سبزشدن نشانگر تب را ببندد؛ همچنین UI ثبت باید وضعیت «روی این دستگاه / در صف / تأیید سرور» را کنار همان رکورد نمایش دهد.

### P1-5: کد موقت یا عدم رزرو کد رسمی

برای entityهای مالی، تولیدکننده کد هنگام نبود pool/server `TMP-*` می‌دهد و چند مسیر save را block می‌کند؛ این خوب است. اما entityهای غیرمالی می‌توانند fallback محلی داشته باشند. در آفلاین/چنددستگاهی، احتمال duplicate یا نیاز به repair وجود دارد؛ داده الزاماً ناپدید نمی‌شود ولی کاربر ممکن است رکوردش را با کد دیگری پیدا کند.

**اصلاح:** برای ثبت‌های حیاتی آفلاین، یک شناسه UUID پایدار مستقل از کد نمایشی و نمایش وضعیت «کد موقت» لازم است.

### P1-6: پاک‌سازی کش یا reset توسط کاربر/مرورگر

پاک‌سازی data/site storage، حالت private/incognito، policy سازمانی مرورگر، uninstall PWA یا browser crash می‌تواند local queue را حذف کند. اگر هنوز ACK سرور نگرفته باشد، داده از دست می‌رود.

**اصلاح:** هشدار صریح در UI، queue در IndexedDB، export اضطراری، و عدم اعتماد به localStorage به‌عنوان تنها محل نگهداری قبل از ACK.

---

## ۵. سناریوهای P2 — داده هست، ولی کاربر تصور می‌کند نیست

| سناریو | واقعیت محتمل | کنترل/اقدام |
|---|---|---|
| مودال/پنل stale | داده روی سرور یا دستگاه دیگر ثبت شده ولی UI باز render نشده | pull فوری و refresh مودال برای همهٔ فرم‌های دارای سند، نه فقط برخی ماژول‌ها |
| Sync نقش‌محور | نقش کاربر اجازه pull آن داده را ندارد | پیام صریح «دسترسی ندارید»، نه لیست خالی |
| ذخیره فقط در Draft فرم | کاربر قبل از زدن Save خارج شده | autosave draft با نشان «پیش‌نویس، هنوز ثبت نشده» و recovery پس از بازگشت |
| پیوست S3 آپلود شده ولی metadata فرم Save نشده | فایل در ابر یتیم است، رکورد اصلی ندارد | پس از upload، وضعیت «آپلود شد؛ برای اتصال به رکورد، فرم را ذخیره کنید» لازم است |
| metadata ذخیره شده ولی upload ناموفق | لینک مرده/`file_not_found` | PUT/proxy باید قبل از افزودن metadata تأیید شود؛ retry صریح |
| فایل/تصویر فشرده شده | فایل هست ولی اصل با کیفیت کامل ذخیره نشده | در UI باید اعلام شود تصویر به WebP/حداکثر ۱۶۰۰px تبدیل شد |

---

## ۶. عملیات عمدی اما پرخطر

این‌ها «ثبت نشدن ناخواسته» نیستند، اما برای کاربر نتیجهٔ مشابهِ گم‌شدن دارند:

1. Go-live wipe با تأیید صریح؛ می‌تواند داده‌های اصلی را خالی و به سرور push کند.
2. Restore بک‌آپ؛ می‌تواند داده جدیدتر را با snapshot جایگزین کند.
3. حذف رکورد با tombstone؛ حذف روی دستگاه‌های دیگر هم پخش می‌شود.
4. archive/purge پیوست؛ بدون versioning باکت، حذف S3 برگشت‌پذیر نیست.

برای این‌ها باید تأیید دو مرحله‌ای، نام کاربر/زمان، preview تعداد رکورد، export قبل از اجرا و rollback آزموده‌شده وجود داشته باشد.

---

## ۷. آنچه اکنون نسبتاً تضمین شده است

- `v34.5.14`: سبزشدن Sync نیازمند ACK کلیدبه‌کلید سرور است، نه پاسخ کلی موفق.
- داده dirty در `ptf_sync_dirty` حفظ می‌شود و پس از refresh قابل retry است، مگر سناریوهای quota/پاک‌شدن storage/role محدود بالا.
- توکن و 401 مسیر refresh/retry دارند.
- تعارض versioned است و برای بسیاری از dataها merge/tombstone وجود دارد.
- سپر داده‌صفر اجازه نمی‌دهد لیست خالی دستگاه، داده ناخالی سرور را پاک کند.
- فایل ضمیمه قبل از حذف metadata، حذف ابری را تأیید می‌کند.

---

## ۸. برنامه اصلاح پیشنهادی

### فاز A — فوری (P0)

1. **قرارداد durable save:** `setData` نتیجه‌دار شود؛ فرم بدون `durable/queued` موفقیت نشان ندهد.
2. **صف IndexedDB اتمیک:** در فاز B، اگر localStorage/queue شکست خورد، save متوقف و پیام قرمز actionable نمایش داده شود.
3. **Role preflight:** همان ACL سرور پیش از باز شدن دکمه ثبت اعمال شود؛ dirty نقش‌ممنوع حذف نشود.
4. **تست اجباری fault injection:** quota، شکست IndexedDB، قطع اینترنت بین local save و push، refresh فوری و token-expiry.

### فاز B — یک اسپرینت

5. source of truth واحد و تست CI برای برابری `SYNC_KEYS` client و `sync_all_keys()` server؛ اختلاف فعلی `ptf_crm_treasury_calls` را تعیین تکلیف کنید.
6. Sync اصلی را chunk کنید و برای هر key نام کسب‌وکاری/خطای سرور نمایش دهید.
7. status per-record اضافه کنید: `محلی`، `در صف`، `تأیید سرور`، `نیازمند اقدام`.
8. merge اختصاصی برای ساختارهای مالی nested و سناریوی دوکاربره.

### فاز C — تداوم کسب‌وکار

9. test restore واقعی DB + metadata + S3 و backup خارج از هاست.
10. versioning/version retention روی bucket پیوست فعال شود.
11. Telemetry بدون PII برای شمارش pending>۵دقیقه، failed local write و sync rejection؛ به مدیر هشدار داده شود.

---

## ۹. سناریوی پذیرش پیشنهادی برای هر فرم مهم

برای پیشنهاد، درخواست، فاکتور، پرداخت، چک و تنخواه باید حداقل این هشت تست اجرا شود:

1. ثبت آنلاین و دیدن همان داده در مرورگر دوم پس از نشان سبز.
2. ثبت آفلاین، بستن/بازکردن مرورگر، اتصال مجدد و مشاهده در مرورگر دوم.
3. قطع اینترنت دقیقاً پس از کلیک ثبت.
4. refresh فوری پیش از پایان ۴ ثانیه debounce.
5. token منقضی هنگام ثبت.
6. localStorage پر یا setItem fail.
7. ثبت همزمان تغییر متفاوت روی همان رکورد از دو مرورگر.
8. نقش فاقد مجوز برای کلید مقصد.

**معیار قبولی:** در هیچ حالت، پیام سبز «روی سرور ثبت شد» پیش از مشاهده ACK سرور نمایش داده نشود؛ و در failure، داده یا recoverable queue/export داشته باشد یا فرم صریحاً ثبت را ناموفق اعلام کند.

---

## نتیجه نهایی

در طراحی فعلی، بهترین گارد تازه `v34.5.14` مانع «سبز کاذب Sync» است. اما برای اینکه کاربر در تمام فرم‌ها واقعاً نتواند تصور اشتباه «ثبت شد» داشته باشد، باید موفقیت UI از `setData` محلی جدا و به **ثبت پایدار/صف قابل‌بازیابی/ACK سرور** وصل شود. P0های quota، queue فاز B و role-mismatch هنوز مهم‌تر از اضافه‌کردن قابلیت جدید هستند.
