# یادداشت انتشار v34.39.2 — HOME-DOCK-SUN (فیکس شیت منوی موبایل، داکِ استعلام‌محور، شب خودکار از غروب، ریتم آرامِ برق)

**تاریخ:** 2026-09-17
**بازوی تغییر:** Arena Agent
**کانال:** سایت عمومی — صفحهٔ اول (لایهٔ نمایشی/رفتاری v34.39.2؛ محتوا/سئو/فرم‌ها بدون تغییر)
**قرارداد:** tester664 (۲۹ سنجه) + بازتنظیم سنجه‌های برگشت‌خوردهٔ tester663 + هم‌ترازی پین‌ها با ابزار رسمی `bump-version-pins.js`

## دستور کارفرما
> «دکمه منو کار نمی‌کند. دکمه منو دکمه استعلام شود و دکمه خانه به منو تغییر کند. سرعت عبور برق و فرکانسش کاهش یابد؛ دیربه‌دیرتر و یواش‌تر. دکمه حالت شب و روز حذف شود اما سایت خودکار از ساعت غروب شب شود و دکمه به قسمت منو منتقل شود. ۳ دپارتمان تامین و ۶+ صنعت هدف با اعداد فارسی.»

## ریشهٔ باگ «دکمهٔ منو کار نمی‌کند»
`.site-header` روی موبایل `backdrop-filter` داشت؛ طبق استاندارد، این ویژگی برای نوادگانِ `position:fixed` **بلوکِ containing** می‌سازد. شیتِ منو (`#mainNav`) داخل هدر است و با `bottom:86px` به همان باکسِ ۸۶پیکسلیِ هدر لنگر می‌شد → عملاً **بالایِ بیرونِ ویوپورت** رندر می‌شد. کلیک کار می‌کرد (کلاس و مروف ✕ عوض می‌شد) ولی چیزی دیده نمی‌شد.
**فیکس:** در `max-width:790px` بلورِ هدر خاموش و پس‌زمینهٔ نیمه‌ماتِ جامد (`rgba .985`، روشن/تیره) جایگزین شد تا شیت دوباره به ویوپورت لنگر شود.

## آنچه انجام شد
1. **داک جدید (طبق درخواست):** ترتیب = **منو | تماس | استعلام | واتساپ | چت**. اسلات اول (خانهٔ قبلی) به دکمهٔ منو با آیکون همبرگر↔✕ تبدیل شد؛ دکمهٔ وسط دیگر «منو» نیست بلکه **لینک واقعی استعلام** (`<a href="rfq/">`) با همان قاب سه‌بعدی برجسته، هالهٔ شعله و حلقهٔ جریان است. خانه از طریق لوگو در دسترس است.
2. **شب/روز خودکار از غروب:** کلید تم از هدر حذف شد. اسکریپت ضدFOUC در `<head>` حالا با **فرمول خورشید (الگوریتم NOAA / معادلهٔ طلوع‌غروب)** تاریخِ امروز را محاسبه می‌کند (مختصات تهران ۳۵.۷°N / ۵۱.۴۴°E با دقت ±۵ دقیقه) و اگر لحظه بیرونِ بازهٔ طلوع تا غروب باشد، `.ptf-dark` از همان اولین پیکسل اعمال می‌شود — بدون فلاش، بدون کتابخانه، بدون کوکیِ اجباری. `ptf-motion.js` هر دقیقه (فقط تب فعال، فقط در حالت خودکار) بازبینی می‌کند؛ لحظهٔ غروب/طلوعِ واقعی صفحه را سوییچ می‌کند.
3. **دکمهٔ تم داخل منو:** به‌عنوان ردیف «نمای شب / نمای روز» در انتهای آیتم‌های `#mainNav` — در موبایل ردیف تمام‌عرض ۴۴px با آیکون ماه/خورشید و متنِ زنده، در دسکتاپ قرص کوچک انتهای نوبار. کلیک = **اوررایدِ دستی** با کوکی `ptf_theme` (ماندگار؛ نبودِ کوکی = حالت خودکار). بدون localStorage (سازگار با نگهبان A10).
4. **ریتم آرامِ برق:** کیفریم جدید `ptfCurS` — عبور حلقه در ۵۲٪ اول سیکل و **سکوتِ تاریک در ۴۸٪ دوم**: حلقهٔ دائم ۳.۴s → **۸s** (نصف‌سرعتِ عبور + فاصلهٔ بیشتر بین جرقه‌ها) و حلقهٔ هاور کارت‌ها ۲.۲s → **۵s**.
5. **دسترس‌پذیری/ریگریشن:** sync دوطرفهٔ دکمهٔ منو با MutationObserver همان‌جا ماند؛ `stopPropagation` روی هر دو کلیک (دکمهٔ داک و کلید تم داخل شیت تا لینک‌بندِ `main-nav a` اشتباهاً شیت نبندد/رفتار غیرقابل‌پیش‌بینی نداشته باشد)؛ reduce-motion: کیفریم جدید هم زیر kill-switch، ترنزیشن‌های ردیف تم در شیت خاموش؛ چاپ: بدون تغییر.
6. **اعداد فارسی پنل هیرو** (`۳ دپارتمان تامین / ۶+ صنعت هدف`) از دور قبل برجا بود و با سنجهٔ بازگشت‌ناپذیر در tester664 قفل شد؛ نمایش لاتینِ قبلی فقط کشِ مرورگر بود که با cache-bust `?v=34.39.2` برطرف می‌شود.
7. **قرارداد تست:** tester664 (۲۹ سنجه: فیکس بلور، ترتیب داک، فرمول خورشید + صحت عددی نیمروز/طول روز تهران، برچسبِ زنده، ptfCurS، حجم‌ها، بی‌تغییری محتوا)؛ tester663 در ۶ سنجه‌ای که قراردادشان با دستور جدید کارفرما برگشته بود (ترتیب داک، جای کلید تم، بوت غروب، ریتم ۸s/۵s، فیکس هدر) بازتنظیم شد؛ ۶۶۲ بدون تغییر سبز است.

## فایل‌های تغییر‌یافته
`index.html` (بوت غروب در head، حذف کلید هدر، ردیف تم داخل `#mainNav`، چیدمان جدید داک، cache-bust) · `assets/css/home.css` (لایهٔ v34.39.2 — فیکس بلور، استایل ردیف تم/داک، ptfCurS، بازتنظیم دو سرعت) · `assets/js/ptf-motion.js` (sunNight + بازبینی خودکار + برچسب زنده + stopPropagation) · `VERSION.json` و ۸ نقطهٔ رسمی نسخه · `RELEASE-NOTES-v34.39.2.md` · دو تستِ بالا.

## وضعیت دروازه
tester664: ۲۹/۲۹ ✓ · tester663: ۳۰/۳۰ ✓ · گیت کامل CI: **۲۷۶ PASS / ۱ FAIL** (شکست محیطی شناخته‌شدهٔ tester656 — نبودِ php-cli در سندباکس؛ روی بیس هم قرمز است) · arch-guard: PASS (A6 هم‌تراز، A10 بدون localStorage).
