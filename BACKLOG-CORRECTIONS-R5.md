# 📋 بک‌لاگ دور پنجم (R5) — گزارش کاربر + پیشنهاد اصلاح فرایند پرونده فروش
**تاریخ:** ۱۴۰۵/۰۴/۲۰ | **وضعیت:** US-402 + US-403 ✅ در v21.2 | BUG-018 قبلاً در v16.7 | US-404 قبلاً در v16.8/v17.1
**تدوین:** پنل مشترک متخصصان ارشد سیستم‌های بازرگانی + مهندسان ارشد نرم‌افزار (با ریشه‌یابی فنی روی کد موجود)

| # | استوری | نوع | اولویت پیشنهادی پنل | برآورد |
|---|--------|-----|---------------------|--------|
| US-402 ✅ v21.2 | انتخاب آسان تامین‌کننده در استعلام + فرم PDF مختص هر تامین‌کننده | بهبود UX | 🔴 بالا (مانع کار روزانه) | متوسط |
| US-403 ✅ v21.2 | پیش‌نمایش اسناد داخل نرم‌افزار (مودال) به‌جای تب جدید مرورگر | بهبود UX | 🔴 بالا | متوسط |
| BUG-018 | پاک شدن کامل دفترچه تلفن پیامکی (حتی مخاطبان دستی) | 🐞 باگ | 🔴 بحرانی (از دست رفتن داده) | کوچک-متوسط |
| US-404 | بازطراحی فرایند پرونده فروش: پرونده = ابلاغ سفارش (CO برنده) | اصلاح فرایند | 🟡 پس از تصمیم نهایی سناریو | بزرگ |

---

## US-402 — سهولت انتخاب تامین‌کننده در استعلام + فرم PDF مختص هر تامین‌کننده

**شرح (کارفرما):** درخواستی توسط هوش مصنوعی خوانده شد و با تیک «استعلام از تامین‌کننده» به بخش خودش رفت. کاربر در گام انتخاب تامین‌کنندگان با فهرست بلندبالایی روبرو شد که نه امکان فیلتر داشت و نه می‌شد PDF مختص استعلام هر تامین‌کننده را گرفت (فقط PDF عمومی ساخته می‌شود). بهترین سناریو با اصل سهولت کاربری توسط تیم متخصصان تعیین شود.

**ریشه‌یابی فنی (تایید شد):**
- گام ۳ ویزارد استعلام (`rfqsmart.js` → `rfqsToTargets/rfqsRenderTargets`): همه تامین‌کنندگان در یک فهرست عمودی چک‌باکسی رندر می‌شوند — **هیچ فیلد جستجو/فیلتری وجود ندارد**؛ فقط ۵ پیشنهاد برتر رنگی‌اند. با رشد بانک تامین‌کنندگان، فهرست غیرقابل استفاده می‌شود.
- فرم PDF (`rfqsPrint`): **یک فرم عمومی per استعلام** است، نه per تامین‌کننده — نام گیرنده روی فرم درج نمی‌شود و کاربر مجبور است یک PDF واحد را برای همه بفرستد (غیرحرفه‌ای و مبهم برای گیرنده).

**بررسی گزینه‌های اجرایی توسط پنل UX (مطابق دستور کارفرما):**
| گزینه | شرح | ارزیابی پنل |
|-------|------|-------------|
| الف | افزودن فقط فیلد جستجو به فهرست فعلی | حداقلی؛ مشکل PDF حل نمی‌شود — رد |
| ب | جستجوی زنده دوزبانه (موتور US-399: برند/تجهیز/زمینه) + گروه‌بندی «پیشنهاد هوشمند / سایرین» + چیپ انتخاب‌شده‌ها بالای فهرست | ✅ **منتخب پنل** — از زیرساخت موجود استفاده می‌کند |
| ج | جایگزینی کامل با جدول صفحه‌بندی‌شده | سنگین برای یک گام ویزارد؛ چابکی را می‌گیرد — رد |

**معیارهای پذیرش (سناریوی منتخب ب):**
1. بالای فهرست گام ۳: فیلد **جستجوی زنده دوزبانه** (نام/برند/تجهیز/زمینه — همان `entityMatches` گسترش‌یافته US-399؛ «زیمنس» = «Siemens»).
2. فهرست دو بخش: «⭐ پیشنهاد هوشمند» (مرتب بر اساس امتیاز با دلیل — موجود) و «سایر تامین‌کنندگان» (جمع‌شونده)؛ انتخاب‌شده‌ها به‌صورت **چیپ** بالای فهرست با ✕ حذف سریع.
3. در کارت ارسال و رهگیری (گام ۴): دکمه «🖨 فرم PDF» **کنار هر تامین‌کننده** = فرم استعلام با **نام و مشخصات همان گیرنده** روی سربرگ (To: شرکت X) + شماره استعلام یکسان؛ دکمه PDF عمومی فعلی حفظ می‌شود.
4. ارسال واتساپ/ایمیل هر تامین‌کننده به PDF مختص خودش اشاره کند (متن موجود فقط اصلاح ارجاع).
5. رفتار فعلی (۵ پیشنهاد تیک‌خورده خودکار + یادگیری US-399 + rfqsFinalize) عینا حفظ شود — فقط لایه نمایش/انتخاب بهبود می‌یابد.
6. موبایل: فیلد جستجو و چیپ‌ها تمام‌عرض؛ فهرست اسکرول داخلی.

---

## US-403 — پیش‌نمایش اسناد داخل نرم‌افزار (مودال) به‌جای تب جدید

**شرح (کارفرما):** برای دیدن هر پیشنهاد یا سند ضمیمه در فهرست درخواست‌ها یا پرونده فروش، یک تب مرورگر باز می‌شود و بعد از مدتی تب‌های زیادی جمع می‌شود. یک پنجره داخل خود نرم‌افزار (مودال) پیش‌نمایش را نشان دهد و بسته شود.

**ریشه‌یابی فنی (تایید شد):** `openStoredFile` در storage.js → `window.open(url, '_blank')` — همه ~۱۵ نقطه مصرف (ضمایم درخواست bridge.js، اسناد پرونده فروش salesfiles.js، بایگانی archive.js، فاکتورها و...) از همین یک تابع می‌گذرند → **نقطه اصلاح واحد و تمیز**.

**معیارهای پذیرش:**
1. ماژول `docviewer`: مودال تمام‌صفحه‌نما (هم‌الگوی md-b موجود + دکمه‌های modalx) با `<iframe>`/`<img>`/`<embed>` بر اساس پسوند: تصویر (jpg/png/webp)، PDF، متن؛ سایر فرمت‌ها (xlsx/docx/zip) → پیام «پیش‌نمایش مرورگری ندارد» + دکمه دانلود.
2. `openStoredFile` به‌جای تب جدید، مودال را باز کند — **fallback**: اگر مرورگر رندر iframe را بلاک کرد (هدر سرور فایل‌ها)، دکمه «باز کردن در تب جدید» داخل مودال باشد (هیچ سندی غیرقابل مشاهده نشود).
3. نوار ابزار مودال: نام فایل + ⬇️ دانلود (ptfDownloadStoredFile موجود) + 🗗 تب جدید + ✕ بستن (ESC هم ببندد).
4. سازگار با BUG-017: باز/بستن مودال پیش‌نمایش، مودال‌های مینیمایزشده را قربانی نکند (قاعده «آخرین visible»).
5. لینک‌های امن موقت storage مثل قبل (بدون تغییر سمت سرور).
6. موبایل: تمام‌صفحه با دکمه بستن بزرگ.

**ریسک فنی ثبت‌شده:** نمایش inline در iframe به هدر `Content-Disposition`/`X-Frame-Options` پاسخ فضای ابری بستگی دارد — در اسپرینت اجرا، اول روی هاست واقعی تست شود؛ معماری fallback (AC2) دقیقا برای همین است.

---

## 🐞 BUG-018 — پاک شدن کامل دفترچه تلفن پیامکی (حتی مخاطبان دستی)

**شرح (کارفرما):** با اینکه فهرست مشتریان و تامین‌کنندگان خالی نیست، دفترچه تلفن کاملا خالی است. ابتدا سینک انجام شد و افراد اضافه شدند ولی الان حتی مخاطبان دستی هم پاک شده‌اند.

**ریشه‌یابی فنی (فرضیه اصلی — روی کد تایید شد):**
1. **مقصر اصلی (احتمال بالا): حفره سپر داده‌صفر در سینک.** `ptf_crm_smsbook` عضو `SYNC_KEYS` سینک چنددستگاهی هست ولی عضو `GUARD_KEYS` سپر US-382 **نیست** → دستگاهی که دفترچه‌اش محلی خالی است (مرورگر تازه/پاک‌شده) می‌تواند آرایه خالی را push کند و **نسخه خالی روی سرور و سپس همه دستگاه‌ها بنشیند** — دقیقا الگوی علائم گزارش‌شده (اول پر بود، بعد همه‌جا خالی شد؛ Lost Update همان کیس استادی US-384).
2. **عامل تشدید:** `smsBookSyncAll` در بوت خودکار اجرا می‌شود و همه رکوردهای `src='auto'` را حذف و بازسازی می‌کند؛ اگر در لحظه اجرا فهرست مشتریان/تامین‌کنندگان هنوز pull نشده باشد (race با sync اولیه)، بازسازی روی داده خالی انجام و همان نتیجه خالی هم push می‌شود. (مخاطبان دستی از این مسیر حذف نمی‌شوند — پاک شدن دستی‌ها فقط با فرضیه ۱ توجیه می‌شود.)

**معیارهای پذیرش رفع:**
1. `ptf_crm_smsbook` به `GUARD_KEYS` سپر داده‌صفر اضافه شود (خالی محلی + ناخالی سرور = push ممنوع با پیام موجود).
2. `smsBookSyncAll` خودکارِ بوت فقط **پس از تکمیل pull اولیه** (`state.bootstrapped` موجود در sync.js) اجرا شود؛ و اگر هر دو فهرست منبع خالی بودند، بازسازی auto انجام نشود (رکوردهای قبلی دست‌نخورده بمانند).
3. بازیابی داده کاربر: مسیر بازگردانی از بک‌آپ‌های سروری موجود (hourly/داily) فقط برای کلید smsbook — بدون دست زدن به بقیه کلیدها (زیرساخت get_backup موجود).
4. تستر: سناریوی «دستگاه خالی + سرور پر» → push بلاک؛ سناریوی «sync دیرهنگام» → auto rebuild به تعویق.
5. در ریلیزنوت رفع، به کاربر اعلام شود مخاطبان دستی از بک‌آپ سرور قابل بازگشت‌اند.

---

## US-404 — بازطراحی فرایند پرونده فروش: «پرونده = ابلاغ سفارش (CO برنده)»

**شرح (کارفرما):** پرونده‌های فروش صرفا پرونده‌های ابلاغ سفارش‌شده (پیشنهادهای مالی برنده) را در بر بگیرند؛ هر ابلاغ سفارش = یک پرونده؛ کلیه اسناد قبلی (ضمایم درخواست، پیشنهادهای فنی/مالی با آخرین رویژن، استعلام از تامین‌کنندگان) پس از تشکیل پرونده به آن منتقل شوند و پس از مختومه شدن و تسویه نهایی به بایگانی برود. در حالت فعلی با صدور هر پیشنهاد پرونده تشکیل می‌شود که باعث شلوغی این بخش می‌شود. تیم متخصصان بازرگانی با مشورت مهندسان نرم‌افزار، با حفظ چابکی و سهولت، سناریو را انتخاب و همه کمبودها را در نظر بگیرند.

### 📊 تحلیل تطبیقی پنل (مزایا/معایب — مطابق دستور)

**سناریوی فعلی (پرونده با اولین پیشنهاد — US-322):**
| مزایا | معایب |
|-------|-------|
| همه مدارک از لحظه اولین پیشنهاد یکجا جمع می‌شود | شلوغی: هر TO/CO حتی شکست‌خورده = یک پرونده (گزارش کاربر) |
| مهلت‌های تعهدی (US-351) و پیگیری‌ها روی پرونده زنده‌اند | مفهوم «پرونده فروش» با عرف بازرگانی (پرونده = سفارش قطعی) نمی‌خواند |
| Win/Loss از مسیر مختومه‌سازی همین پرونده‌ها تغذیه می‌شود (US-349) | کاربر برای یافتن پروژه‌های واقعی باید بین ده‌ها پرونده باز بگردد |

**سناریوی پیشنهادی کارفرما (پرونده فقط با CO برنده/ابلاغ سفارش):**
| مزایا | معایب / کمبودهایی که باید پوشش داده شود |
|-------|------------------------------------------|
| پرونده = پروژه واقعی؛ بخش همیشه خلوت و معنادار | ① مهلت تعهدی/پیگیری/دلیل باخت که الان روی «پرونده» است، قبل از برد باید جای زیست داشته باشد |
| منطبق با عرف بازرگانی و ذهنیت مدیران | ② آمار Win/Loss (US-349) الان از مختومه‌سازی پرونده می‌آید — مسیر جایگزین برای «باخت قبل از تشکیل پرونده» لازم است |
| انتقال خودکار همه اسناد پیشین = پرونده کامل از روز اول | ③ مهاجرت داده‌های موجود (deals فعلی بدون wonOffer) باید بی‌خطا باشد |
| هم‌راستا با جریان «خرید واقعی» US-392 (که همین حالا به wonOffer قفل است) | ④ اسناد درخواست‌هایی که هرگز برنده نمی‌شوند، تعیین تکلیف می‌خواهند (سیاست پاکسازی بازنده archive.js موجود) |

### ✅ سناریوی منتخب پنل: پذیرش پیشنهاد کارفرما با معماری دو-لایه (پوشش همه کمبودها)
- **لایه ۱ — «کارتابل فرصت‌ها» (نمای سبک، نه پرونده):** درخواست‌های دارای پیشنهاد فعال با بج وضعیت/مهلت/پیگیری — جایگزین زیست‌گاه فعلی مهلت تعهدی و دلیل باخت قبل از برد؛ **روی داده موجود rfqs/offers، بدون موجودیت جدید**. باخت قبل از برد از همین‌جا با دلیل استاندارد ثبت → مستقیم به آمار Win/Loss (پوشش کمبود ①②).
- **لایه ۲ — «پرونده فروش» فقط با ثبت 🏆 برنده CO:** ساخت خودکار پرونده + **انتقال خودکار**: ضمایم درخواست + TO/CO با آخرین رویژن + استعلام‌های تامین (rfqsmart مرتبط) + جدول خرید واقعی (اتصال موجود v16.3) → مختومه/تسویه → بایگانی (مسیر موجود حفظ).
- **مهاجرت نرم:** deals موجودِ دارای wonOffer دست‌نخورده؛ deals بدون wonOffer به کارتابل فرصت‌ها منتقل می‌شوند (داده‌ای حذف نمی‌شود) (پوشش ③).
- **قواعد پابرجا:** قفل برنده، سد rejected→CO (US-367)، سیاست اسناد بازنده (archive)، موتور سود v16.0، کانبان v16.4 (پوشش ④).

**معیارهای پذیرش (خلاصه — AC تفصیلی هنگام تخصیص اسپرینت):**
1. ثبت برنده CO = تنها نقطه ساخت پرونده فروش؛ صدور TO/CO دیگر پرونده نمی‌سازد.
2. انتقال خودکار اسناد چهارگانه (ضمایم درخواست/TO/CO آخرین رویژن/استعلام تامین) هنگام تشکیل + هر سند جدید بعدی مستقیم به پرونده.
3. کارتابل فرصت‌ها: بج مهلت US-348/351، ثبت باخت با دلیل US-349، بدون CRUD موازی.
4. مهاجرت نرم بدون حذف داده + تستر رفت‌وبرگشتی کامل (برد→پرونده→مختومه→بایگانی و باخت‌قبل‌ازبرد→Win/Loss).
5. هیچ رگرسیونی در: خرید واقعی US-392، سود v16.0، کانبان US-369، دیجست/گزارش‌های موجود.

**پیش‌نیاز اجرا:** تایید نهایی کارفرما روی سناریوی منتخب پنل (به‌ویژه لایه «کارتابل فرصت‌ها»). | **برآورد:** بزرگ — یک اسپرینت کامل مستقل.

---

**شماره‌گذاری بعدی: US-405** | باگ بعدی: BUG-019
**یادآوری وضعیت:** طبق ابلاغ، هیچ‌کدام از موارد این سند اجرا نشده‌اند و منتظر انتخاب و ابلاغ کارفرما هستند.
