# 🚀 ریلیز v87.0 — اسپرینت ۸۷: حذف ایمن + سربرگ EN + جداول حرفه‌ای + خلاصه هوشمند درخواست

**تاریخ:** ۱۶ تیر ۱۴۰۵ | **دامنه:** US-197 + US-198 + US-199 + US-200 (چهار اصلاح درخواستی شما)

---

## 🛡 US-197 — حذف ایمن با حفظ وابستگی‌ها (ماژول جدید `guards.js`)

هیچ رکوردی که جای دیگر استفاده شده قابل حذف نیست — دقیقاً قواعد شما:

| رکورد | فقط وقتی حذف می‌شود که... |
|---|---|
| 📦 کالا | در هیچ TO/CO و اقلام درخواستی استفاده نشده باشد |
| 🤝 مشتری | هیچ درخواست، پیشنهاد، فاکتور، پرونده یا قراردادی به نامش نباشد |
| 🏭 تامین‌کننده | هیچ قیمت خرید، جدول مقایسه، خرید نهایی یا استعلام تامینی نداشته باشد |
| 📄 پیشنهاد فنی (TO) | از آن پیشنهاد مالی (CO) ساخته نشده باشد |
| 💰 پیشنهاد مالی (CO) | برنده نشده + فاکتور صادر نشده + ارجاع فاکتور نشده + پرونده/پکینگ لیست/پیش‌پرداخت نداشته باشد |
| 🎯 لید | به مشتری تبدیل نشده باشد |

- پیام خطا **وابستگی‌ها را با نام و شماره فهرست می‌کند** (تا ۸ مورد) تا بدانید اول چه چیزی را تعیین تکلیف کنید.
- دکمه حذف 🗑 برای مشتری و تامین‌کننده **اضافه شد** (قبلاً اصلاً نداشتند) — البته فقط برای رکوردهای بدون وابستگی.
- حتی حذف ادمین (CO) هم گارد وابستگی گرفت.

## 📄 US-198 — سربرگ نامه انگلیسی + اصلاحات فارسی

| مورد | اصلاح |
|---|---|
| نامه EN | **لوگوی انگلیسی** (بدون متن فارسی) + **نام و سمت امضاکننده به انگلیسی** (از nameEn کاربر/پروفایل امضا) + پیوست خالی = **None** |
| سربرگ فارسی | آیکون تلفن به **سمت چپ** شماره منتقل شد + **کل فونت سربرگ بی‌یاقوت** (B Yaghut با fallback وزیرمتن اگر فونت روی سیستم نبود) |

## 📊 US-199 — جداول پنل‌ها (ماژول جدید `tables.js`)

- **وسط‌چین یکدست**: سرستون و مقادیر هر ستون دقیقاً هم‌مرکز (نام مشتری زیر عنوان ستون)
- **تغییر عرض ستون**: لبه سرستون را بگیرید و بکشید
- **فریز ستون**: سنجاق 📌 روی هر سرستون → آن ستون هنگام اسکرول افقی ثابت می‌ماند (برای ردیف‌های عریض) — سازگار با حالت شب

## 🤖 US-200 — خلاصه هوشمند درخواست (بدون باگ محدودیت — دقیقاً طرح شما)

1. با ثبت درخواست جدید، سیستم **خودکار** تلاش می‌کند AI خلاصه بسازد: موضوع درخواست + حوزه فنی + نکات مفید تامین (استاندارد/متریال) — و متنی در رکورد ذخیره می‌شود.
2. **مدیریت محدودیت (خواسته صریح شما):** اگر AI آن لحظه در دسترس نبود (سهمیه/قطعی)، ثبت درخواست **بدون هیچ خطایی** انجام می‌شود و دکمه **🟡 زرد «خلاصه AI»** کنار درخواست می‌ماند؛ هر وقت محدودیت رفع شد با یک کلیک انجام و دکمه **🟢 سبز** می‌شود.
3. کلیک روی دکمه سبز → مودال خلاصه + حوزه + نکات تامین + تاریخ تولید + دکمه «🔄 تولید مجدد». دیسکلیمر: «ملاک، اصل مدارک است».
4. اکشن سروری جدید `summarize` — فقط متن درخواست/اقلام ارسال می‌شود، بدون قیمت و داده مالی.

## ✅ کیفیت
tester27 جدید: **31/31 PASS** (شامل ۶ تست رفتاری گارد حذف: مشتری با CO مسدود، کالای آزاد مجاز، TO با CO مسدود...) | رگرسیون ۲۰ تستر همه PASS | ممیز ۸بخشی PASS | کش → `ptf-crm-v87.0`

## 📦 نصب (File Manager)
**`pishtaj-release-v87.0-delta.zip`**: `crm/guards.js` و `crm/tables.js` (جدید)، `crm/letters.js`، `crm/inqreader.js`، `crm/index.html`، `crm/sw.js`، `api/llm.php` + مستندات.

> 🧪 تست: ① حذف مشتری دارای پیشنهاد → پیام وابستگی‌ها ② حذف کالای استفاده‌نشده → موفق ③ نامه EN → لوگو/امضا/None انگلیسی ④ جدول مشتریان → درگ لبه ستون + 📌 فریز ⑤ ثبت درخواست جدید → دکمه خلاصه AI (🟢 یا 🟡).
