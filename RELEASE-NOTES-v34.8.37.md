# یادداشت انتشار CRM v34.8.37

تاریخ: ۱۴۰۵/۰۶/۰۶ (۲۰۲۶-۰۸-۲۸) — نشست سوم بررسی نوار زرد personal_cheques

## ۱) PULL-EQUAL-ACK — آخرین لایهٔ خودترمیم نوار زرد

**علامت باقی‌مانده:** نوار زرد با «آخرین خطا: server — نوار وضعیت با پیام "سرور در
دسترس نیست" خودِ پول است…». تحلیل: پیام از مسیر پول (`sync.js:1263`) است و
`reason='server'` بدون فیلد error = امضای fallback آفلاین SW
(`{"ok":false,"offline":true}`) — خطای گذرای شبکه در پنجرهٔ پس از دیپلوی، نه مشکل
داده. تبِ درحال مشاهده هم کدِ قبل از دیپلوی را اجرا می‌کرد.

**تغییر:** در حلقهٔ اعمال pull، حالت برابری (`curStr === newStr`) که قبلاً زود-return
می‌کرد، حالا اگر کلید dirty و غیر-held باشد، پرچم «هنوز نرسیده» را پاک می‌کند —
مقدار سروری عیناً برابر محلی یعنی تغییر رسیده. رکوردهای واقعاً ارسال‌نشده (نابرابر)
dirty می‌مانند. + audit و به‌روزرسانی نشانگر.

## ۲) DEPLOY-GATE-WINDOW — رفع false-red گیت استقرار

**علامت:** دو ران پشت‌سرهم «Deploy to Staging» در گام Post-deploy integrity قرمز
شدند درحالی‌که پروب زندهٔ مستقل هر ۵ فایل را به‌روز نشان می‌داد (sw/shell/manifest =
v34.8.36، client-server شامل FORBIDDEN-DROP، sales-domain-v2 شامل ROUTER-CB).

**ریشه (نشست چهارم):** کشِ مسیرمحورِ نادیده‌گیرندهٔ query در لایهٔ هاست — URL یکتای
گیت (`?v=timestamp`) هم بی‌اثر است چون کلید کش = مسیر؛ verify (دقیقهٔ صفر) برای برخی
مسیرها entry تازه می‌گرفت و integrity در همان ۶۰ ثانیه برای مسیر دیگری بایت کهنه.
پروب مستقل پس از هر سه ران قرمز، محتوای زنده را دقیقاً برابر کامیت نشان داد
(آخرین: sw.js = v34.8.37) → false-red اثبات‌شده، نه آپلود ناقص.

**تغییر (پچ `_tools/PENDING-workflow-integrity-hardening-v34.8.37.patch` — اعمال توسط
مالک به‌دلیل مجوز workflows):** انتظار اولیهٔ ۱۸۰ ثانیه + پنجرهٔ ۱۲×۳۰ ثانیه + هدرهای
no-cache/Pragma + دوبل cache-buster، در هر دو workflow. معیار fail-closed بدون تغییر.

## ۳) تستر جدید

`tester538-v34.8.37-pull-equal-ack.js` — قفل‌کردن PULL-EQUAL-ACK (الگوهای منبع +
حفظ رفتار dirty واقعاً-متفاوت) و پنجرهٔ ۱۲×۳۰ در هر دو workflow.

## کنترل
گیت کامل **154 PASS / 0 FAIL** (۱۵۲ پایه + tester537 + tester538)، arch-guard PASS.

## پس از استقرار
یک بار `Ctrl+Shift+R` در CRM استیجینگ → حداکثر تا یک چرخهٔ poll (~۳۰ ثانیه) نوار
زرد personal_cheques باید سبز/محو شود. در غیر این صورت خروجی «تشخیص همگام‌سازی»
(تنظیمات) را برای نشست بعدی ذخیره کنید.
