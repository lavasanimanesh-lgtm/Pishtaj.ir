# 🚀 ریلیز v16.6 — US-400/US-401: نظام امتیازدهی مصوب + بستانکاری تامین‌کننده
**تاریخ:** ۱۴۰۵/۰۴/۲۰ | **نسخه قبلی:** v16.5 | **بسته:** `pishtaj-release-v16.6.zip` (تجمیعی از v14.5)

پاسخ‌های شما به ۵ سوال سند + ابلاغ تکمیلی، همان روز اجرایی شد. سند `PROPOSAL-SCORING-SYSTEM-v1.md` با مصوبات شما به وضعیت «✅ مصوب و اجراشده» به‌روزرسانی گردید.

---

## 📌 مصوبات شما و نحوه اجرا

| تصمیم شما | اجرا در v16.6 |
|-----------|----------------|
| ① وزن‌ها تایید | وزن‌های مصوب پیش‌فرض موتور شدند؛ در تنظیمات برای همه **قابل مشاهده** و فقط توسط مدیران ارشد **قابل تغییر با ثبت در audit** |
| ② معیار تحویل = یک کلیک اختیاری | در پنجره بستانکاری هر قلم: «✅ تحویل ok / ⚠️ با مشکل» — یک کلیک، اختیاری، بدون داده = خنثی |
| ③ هشدار بدحسابی نمایش داده شود | هنگام انتخاب کارفرما در فرم CO/TC، اگر وصولی مشتری زیر ۶۰٪ باشد، هشدار قرمز کنار باکس سقف اعتبار (US-352) ظاهر می‌شود: «این مشتری فقط X٪ فاکتورهایش را وصول کرده — شرایط پرداخت سفت‌تر» |
| ④ سطح‌بندی تایید | تامین‌کننده: 🥇 ممتاز / 🥈 تاییدشده / 🥉 قابل استفاده / ⚠️ نیازمند ارزیابی — مشتری: 💎 کلیدی / 🌟 ارزشمند / 🔵 عادی / ⚪ راکد/جدید |
| ⑤ گزارش فقط درخواستی | دکمه «📄 گزارش امتیازها (درخواستی)» — همیشه فعال نیست؛ فقط با درخواست مدیر ساخته و قابل چاپ است (+ ثبت در audit) |

## 🧾 ابلاغ تکمیلی شما — بستانکاری تامین‌کننده (اجرای کامل)

> «تامین‌کننده هر آیتم هنگام ورود قیمت مشخص شود... بستانکاری ثبت شود، نقدی همان لحظه صفر، غیرنقدی پرداخت مرحله‌ای... بدهی غیرنقدی امتیاز و ضریب بالاتر... مدیران در هر لحظه بدهی غیرنقدی به هر تامین‌کننده را ببینند.»

1. **تامین‌کننده per آیتم:** از قبل در جدول مقایسه/خرید واقعی مشخص بود (هر قیمت به نام تامین‌کننده ثبت می‌شود) — حالا در پنجره «🛍 ثبت خرید» فیلد جدید: **«نحوه پرداخت: 💵 نقدی / 🧾 غیرنقدی-اعتباری»**.
2. **ثبت خرید → بستانکاری خودکار** (`مبلغ × تعداد` با ارز/نرخ): نقدی = **همان لحظه صفر** (تسویه خودکار)؛ غیرنقدی = بستانکاری باز + اعلان به مدیران ارشد.
3. **پرداخت مرحله‌ای:** دکمه «💰 ثبت پرداخت» روی هر بستانکاری باز — هر مرحله با مبلغ/تاریخ/شرح/کاربر ثبت و مانده به‌روز می‌شود؛ صفر شدن = تسویه کامل ✅.
4. **رویت لحظه‌ای مدیران:** باکس دائمی «💳 بدهی غیرنقدی به تامین‌کنندگان» بالای ماژول تامین‌کنندگان — per تامین‌کننده با جمع ریالی + ارزی بی‌نرخ (شفاف، بدون ریال کاذب) + دسترسی مستقیم پرداخت/جزئیات. در گزارش درخواستی هم می‌آید.
5. **ضریب بالاتر برای اعتباردهنده:** «ضریب اعتباردهی (خرید غیرنقدی)» تا ۱۵+ امتیاز روی امتیاز وندور + بونوس در **تامین‌یاب هوشمند US-399** («🥇 امتیاز وندور ۸۵» در دلیل پیشنهاد).

## 📊 موتورهای امتیاز (لحظه‌ای، از داده موجود، با دلیل)

- **تامین‌کننده:** ۶ معیار مصوب — پاسخگویی استعلام (targets استعلام هوشمند)، رقابتی بودن قیمت (جایگاه در جداول مقایسه)، سابقه خرید واقعی، تخصص US-399، کیفیت تحویل (رویداد اختیاری)، اعتبار ثبتی + ضریب اعتباردهی.
- **مشتری:** ۵ معیار مصوب — خوش‌حسابی (وصولی فاکتورها)، حجم خرید برنده (نرمال لگاریتمی)، نرخ تبدیل درخواست→برد، وفاداری (رکود >۱۲ ماه = افت)، کیفیت تعامل.
- **بج امتیاز** کنار کد در فهرست تامین‌کنندگان/مشتریان (طبق RBAC سند: تامین‌کننده = بازرگانی+، مشتری = دارندگان finance) — کلیک = **کارت ریز معیارها** با نمودار.
- «داده ناکافی» ≠ امتیاز صفر (برچسب جدا). **تعدیل دستی ±۱۵** فقط ارشد، با دلیل اجباری + audit + علامت ⚖️.

## 🛡 اثبات «بدون وصله‌پینه، بدون باگ جدید، بدون تداخل»

| اصل | اثبات |
|-----|-------|
| بدون وصله‌پینه | یک ماژول واحد `scoring.js`؛ بستانکاری از همان لحظه ثبت خرید موجود (cmpBuy) تغذیه می‌شود نه فرم موازی؛ هشدار بدحسابی hook روی همان باکس اعتبار US-352؛ بونوس تامین‌یاب داخل همان امتیازدهنده rfqsmart/US-399. |
| وابستگی‌ها | `purchases` فقط فیلد `pay` اضافه گرفت — موتور سود v16.0 و خرید واقعی v16.3 عینا سالم (تست رگرسیون). کلید جدید `ptf_crm_payables` به سینک چنددستگاهی و بک‌آپ اضافه شد. زنجیره hook های buildSuppliers/renderSuppliers/renderCustomers/buildSettings سالم. |
| بدون باگ جدید | tester84: **۵۸ چک** (چرخه کامل نقدی/غیرنقدی/مرحله‌ای/تسویه/ارزی بی‌نرخ/ثبت مجدد، موتور امتیاز با وزن‌های مصوب، تعدیل، بدحساب، بونوس) + **jsdom (۸ چک)**: باکس بدهی، بج، کارت امتیاز، پرداخت، تحویل، گزارش. رگرسیون کامل: **۷۱ تستر / ۲۰۳۷ چک — صفر خطا**. audit PASS. |
| بدون تداخل | ساختار payables مستقل؛ اسپرینت‌های ح/ط (پیگیری، دیجست، تایم‌لاین) فایل مشترک ندارند و دیجست آینده می‌تواند از همین ptfSupplierDebts بخواند. |

## 📋 راهنمای به‌روزرسانی
1. `pishtaj-release-v16.6.zip` روی هاست روی موجود اکسترکت شود — پوشه `api` حذف نشود.
2. همه کاربران رفرش کنند — نسخه سایدبار **v16.6**.
3. از این پس هنگام «🛍 ثبت خرید» نحوه پرداخت را انتخاب کنید؛ بدهی‌های غیرنقدی بالای ماژول تامین‌کنندگان همیشه جلوی چشم مدیران است.
4. خریدهای قدیمی (قبل از v16.6) بستانکاری ندارند — در صورت نیاز، خرید قلم را یک‌بار مجدد ثبت کنید تا بستانکاری ساخته شود.
