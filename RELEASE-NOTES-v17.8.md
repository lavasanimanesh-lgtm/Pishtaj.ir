# 💵 ریلیز v17.8 — US-422 «حساب تنخواه کامل» (R9 فاز ۲)

**اسپرینت:** R9 مالی — پس از US-418 هزینه‌های جاری  
**استوری:** US-422 — موجودی حساب تنخواه، شارژ، کسر خودکار، تسویه مطالبات اشخاص، گزارش دوره و ارجاع حسابدار

## ✅ اجرا شد

### ۱) حساب واقعی تنخواه
- کلید جدید `ptf_crm_petty_tx` برای گردش حساب تنخواه اضافه شد.
- موجودی حساب تنخواه اکنون از فرمول زیر محاسبه می‌شود:
  - شارژ حساب `charge`
  - منهای پرداخت مستقیم `direct`
  - منهای تسویه مطالبات اشخاص `settle`
- سپر داده‌صفر، سینک، بک‌آپ، Go-Live و API برای کلیدهای جدید به‌روزرسانی شدند.

### ۲) شارژ حساب تنخواه
- تنخواه‌گردان می‌تواند شارژ حساب را با مبلغ و سند واریز ثبت کند.
- شارژ مستقیم روی موجودی حساب اثر می‌گذارد.
- همه عملیات audit می‌شوند.

### ۳) پرداخت مستقیم از حساب تنخواه
- تنخواه‌گردان می‌تواند پرداخت مستقیم ثبت کند.
- پرداخت مستقیم هم یک هزینه `settled` در `ptf_crm_petty` می‌سازد و هم یک تراکنش `direct` در گردش حساب ثبت می‌کند.
- اگر موجودی کافی نباشد، ثبت متوقف می‌شود.

### ۴) هزینه اشخاص = مطالبه از تنخواه
- هزینه‌ای که هر کاربر ثبت می‌کند، به‌صورت `open` و «در انتظار تسویه» می‌ماند.
- جمع مطالبات تسویه‌نشده per کاربر در پنل تنخواه نمایش داده می‌شود.

### ۵) تسویه مطالبات توسط تنخواه‌گردان
- تسویه هزینه کاربر از حساب تنخواه انجام می‌شود.
- هنگام تسویه، موجودی تنخواه به اندازه مبلغ هزینه کم می‌شود.
- به کاربر ثبت‌کننده، اعلان کارتابل ارسال می‌شود.

### ۶) گزارش دوره تنخواه و ارجاع حسابدار
- کلید جدید `ptf_crm_petty_periods` برای دوره‌های گزارش تنخواه اضافه شد.
- تنخواه‌گردان می‌تواند دوره ماهانه را ببندد/ارجاع دهد.
- گزارش دوره شامل موارد زیر است:
  - پرداخت‌های مستقیم
  - تسویه مطالبات اشخاص
  - مانده حساب
  - شناسه هزینه‌ها و تراکنش‌های دوره
  - پیوست‌های مرتبط
- امکان پیوست صورتحساب بانک برای دوره وجود دارد.
- دوره به کارتابل حسابدار ارجاع می‌شود.
- پیامک اطلاع‌رسانی به حسابدار، در صورت انتخاب کاربر و وجود شماره موبایل، ارسال می‌شود.

### ۷) ثبت حسابداری توسط حسابدار
- حسابدار می‌تواند دوره ارجاع‌شده را با شماره سند/شرح، به وضعیت «ثبت‌شده» ببرد.
- پس از ثبت، به مدیران ارشد اعلان داده می‌شود.

### ۸) تنظیم نقش تنخواه‌گردان
- نقش پیش‌فرض تنخواه‌گردان: `chairman`.
- ادمین/رییس هیات مدیره می‌تواند نقش تنخواه‌گردان را از پنل تنخواه تنظیم کند.

## 🔐 داده و سینک
- کلیدهای جدید:
  - `ptf_crm_petty_tx`
  - `ptf_crm_petty_periods`
- هر دو به Sync، Backup، API whitelist، Go-Live و Zero-Data Guard اضافه شدند.
- `ptf_crm_petty` نیز در Guard قرار گرفت تا مطالبات تنخواه با Push خالی از بین نرود.
- پاک‌سازی فایل‌های ابری، پیوست‌های دوره تنخواه را به‌عنوان فایل فعال می‌شناسد.

## 🔬 کیفیت
- tester جدید: `tester96-v178.js` با ۲۹ چک.
- رگرسیون کامل: **۸۳ تستر / بدون خطا**.
- Audit سایت: PASS با همان هشدار قدیمی تصاویر سنگین.

## فایل‌های تغییر یافته
- `crm/petty.js`
- `crm/sync.js`
- `crm/backup.js`
- `crm/golive.js`
- `crm/storage.js`
- `crm/index.html`
- `crm/sw.js`
- `api/crm.php`
- `_tools/uat/tester96-v178.js`

## صف بعدی طبق برنامه R9
1. **US-419** — سهامداران، حقوق موظف و علی‌الحساب
2. **US-421** — زیان پروژه + هشدار داده ناقص
3. **US-420** — داشبورد سال مالی و تقسیم سود
4. سپس طبق بک‌لاگ: US-410/411 → US-405..408 → US-402/403 → US-417 فاز ۲

> یادآوری: موارد R8 جدید ثبت‌شده‌اند؛ BUG-023/US-424 به‌دلیل اثر مالی بالا پیشنهاد می‌شوند بعد از فاز جاری R9 در اولویت قرار گیرند، مگر اینکه کارفرما دستور جلو انداختن بدهد.
