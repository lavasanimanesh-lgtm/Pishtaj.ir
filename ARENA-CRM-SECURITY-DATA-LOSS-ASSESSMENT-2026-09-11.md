# ارزیابی جامع امنیت داده و ریسک از دست رفتن داده — CRM «پیشرو تجهیز فرتاک»

- **نسخهٔ ارزیابی‌شده:** `v34.38.20` (کد زندهٔ فعلی، نه صرفاً ممیزی قدیمی v34.1.0)
- **تاریخ:** ۲۰ شهریور ۱۴۰۵ / ۲۰۲۶-۰۹-۱۱
- **روش:** بازبینی ایستا (static review) روی سورس `crm/*.js` و `api/*.php`؛ بدون اجرای مخرب و بدون هیچ تغییر کدی.
- **پیش‌زمینه:** ممیزی پیشین `SECURITY-AUDIT-2026-08-06.md` (نگاه نیمه‌حرفه‌ای مهاجم) و تصمیمات `SECURITY-RISK-ASSESSMENT-ACTIONS.md` (مرداد ۱۴۰۵ / v34.1.0) مبنای مقایسه‌اند.

> ⚠️ **دامنه:** این سند فقط «ارزیابی و گزارش» است. هیچ تغییر امنیتی اعمال نشده است. هر پیشنهادِ «توصیه» نیازمند تصمیم و اجرای جداگانهٔ کارفرماست.

---

## ۱. خلاصهٔ مدیریتی

CRM از نظر دفاع در عمق در وضعیت **خوب تا خیلی خوب** است؛ ممیزی v34.1.0 هیچ P0 نداشت و در v34.38.20 چند نقص آن زمان (CSP، ADMIN_HASH سرور، کوکی HttpOnly، ابطال نشست گروهی، سپر حذف انبوه، ابزارهای بازیابی) عمدتاً بسته شده است. **هیچ حفرهٔ اجرای کد از راه دور (RCE) یا دور زدن کامل احراز هویت پیدا نشد.**

اما برای پرسش محوری شما (دستگاه آلوده با نقش ارشد) دو واقعیت ساختاری باقی است که باید به‌صورت عملیاتی مدیریت شوند، نه فقط با کد:

1. **معماری PWA آفلاین** کل دادهٔ شرکت (شامل مالی، قیمت‌ها، فهرست کاربران و هش رمزها) را روی دستگاه هر نقش ارشد **کش می‌کند** و توکن نشست را در `sessionStorage`/`localStorage` نگه می‌دارد. یعنی دستگاه آلوده **بدون هیچ تماس با سرور** می‌تواند کل داده را بخواند/برون‌بَرد — هیچ لاگ، rate-limit یا کنترل سروری این مسیر را نمی‌بیند. (این بهای تصمیم عمدی «کار آفلاین» و «نه به AES-GCM» در ارزیابی قبلی است.)

2. **یک شکاف مجوزدهی سرور** پیدا شد که باید بسته شود: فلگ `allow_wipe` (بای‌پسِ «سپر ضد داده‌صفر») **سمت سرور به نقش مقید نیست**؛ هر نقشِ احرازشده با `sync_write` (هر ۸ نقش) می‌تواند با جعل این فلگ، مجموعه‌های مجازِ خودش را روی سرور خالی کند. برخلاف فلگ `restore` که صریحاً به `admin/chairman` محدود شده، `allow_wipe` فقط با یک `if (curRole()!=='admin')` سمت کلاینت محافظت می‌شود.

جزئیات، شواهد (فایل:خط)، و ماتریس اقدامات در ادامه آمده است.

---

## ۲. پرسش محوری: دیوایس آلوده با نقش ارشد

### ۲.۱ چه دارایی‌هایی روی دستگاه نقش ارشد نشسته است؟

| دارایی | محل | نتیجه برای دستگاه آلوده |
|---|---|---|
| کل دادهٔ همگام‌شده (برای ۴ نقش ارشد = **همهٔ کلیدها** شامل مالی، قیمت، کاربران، perms) | `localStorage` + IndexedDB (`ptf_crm_*`) | برون‌بَرد کامل بدون سرور؛ خاموش‌شدن سرور هم مانع نیست |
| توکن نشست (JWT امضاشده) | `sessionStorage` پیش‌فرض؛ `localStorage` اگر «مرا به خاطر بسپار» | بدافزار توکن را می‌دزدد؛ ۲۴ ساعت اعتبار (حسابدار ۸ ساعت) |
| هش SHA-256 رمز ادمین (باقیماندهٔ M1) | `ptf_crm_settings.adminHash` (سینک‌شونده + داخل بک‌آپ) | شکستن آفلاین هش اگر رمز ضعیف باشد |
| فهرست کاربران با `passhash`/`password_hash` | `ptf_crm_users` (سینک‌شونده + داخل بک‌آپ) | حملهٔ آفلاین به هش همهٔ کاربران |

> شواهد: `crm/rbac.js:135` (ذخیرهٔ توکن در localStorage با remember-me)، `crm/rbac.js:70-84` (`ptfAuthToken` از LS می‌خواند)، `crm/key-registry.js:24` (کلیدهای نشست)، `api/crm.php:364` (`sync_allowed_keys_for_role` برای ۴ نقش ارشد همهٔ کلیدها از جمله `ptf_crm_users`/`ptf_crm_perms`/`ptf_crm_settings`)، `crm/backup.js:105-111` (بک‌آپ شامل users/settings/perms).

### ۲.۲ توان نقش‌های ارشد (سرور-محور — منبع حقیقت)

| توانایی | admin | chairman | ceo | commercial | شاهد |
|---|---|---|---|---|---|
| خواندن همهٔ پنل‌ها + مالی + قیمت خرید/فروش | ✔ | ✔ | ✔ | ✔ | `crm/rbac.js` (SENIOR_ROLES) |
| `finance_read` / `finance_write` | ✔ | ✔ | ✔ | ✔ | `api/crm.php:284-285` |
| `users_write` → افزودن کاربر / `users_sync` (تغییر نقش‌ها) / `sessions_revoke` / `get_backup` / `save_settings` | ✔ | ✔ | ✔ | ✔ | `api/crm.php:290`, `3439`, `3085`, `2437`, `2466` |
| `approve_write` | ✔ | ✔ | ✔ | ✔ | `api/crm.php:291` |
| سینک همهٔ کلیدها (شامل users/perms/settings/finance) | ✔ | ✔ | ✔ | ✔ | `api/crm.php:367` |
| حذف دائمی فایل ابری (`delete`/`delete_batch`/`archive_zip`/`backup_prune`) | ✔ | ✔ | ✘ | ✘ | `api/storage.php:38-42` |
| بازگردانی کامل سرور (فلگ `restore`) | ✔ | ✔ | ✘ | ✘ | `api/crm.php:2509` |
| **جعل `allow_wipe` (بای‌پس سپر ضد داده‌صفر)** | **هر ۸ نقش** | | | | `api/crm.php:2507,2652` — بدون گارد نقش |

### ۲.۳ سناریوی حملهٔ گام‌به‌گام (دستگاه آلوده)

1. **مرحلهٔ ۰ — برون‌بَرد خاموش:** بدافزار `localStorage` را می‌خواند → کل دادهٔ شرکت (مشتریان، تأمین‌کنندگان، فاکتورها، مالی، قیمت‌ها) و توکن و هش رمزها را بدون هیچ ردی سروری خارج می‌کند. *(قابل مهار فقط با رمزنگاری محلّی که عمداً کنار گذاشته شده، یا با سیاست دستگاه مدیریت‌شده/MDM.)*
2. **مرحلهٔ ۱ — تصرف نشست:** با توکن دزدیده‌شده، تا ۲۴ ساعت به‌جای کاربر عمل می‌کند. چون گارد IP-binding عمداً حذف شده (مشکل NAT ایران)، هیچ وابستگی به IP مبدأ نیست.
3. **مرحلهٔ ۲ — تثبیت دسترسی:** چون `users_write` برای هر ۴ نقش ارشد است، از نقش `ceo`/`commercial` هم می‌تواند از طریق `users_sync` نقش یک کاربر خفته را به `admin` تغییر دهد یا کاربر جدیدی با رمز معلوم بسازد → **دسترسی دائمی فراتر از ۲۴ ساعت**.
4. **مرحلهٔ ۳ — تخریب:** 
   - هر نقش (حتی `sales`/`collector`) با `data_push` + جعل `allow_wipe:true` و آرایهٔ خالی، مجموعه‌های مجاز خودش را روی سرور خالی می‌کند (شکاف P1-1).
   - `admin`/`chairman` می‌تواند فایل‌های ابری را برای همیشه حذف کند یا `restore` بزند و دادهٔ سرور را بازنویسی کند.

### ۲.۴ عوامل تشدیدکننده (باید دانسته تصمیم گرفته شوند)

- **خروج فقط توکنِ ارائه‌شده را باطل می‌کند** (`api/auth.php` `auth_revoke_token`)؛ توکنِ سرقت‌شده در دستگاه دیگر تا انقضای ۲۴h یا ابطال دستی زنده می‌ماند. ابزار «خروج اجباری همهٔ دستگاه‌ها» وجود دارد (`api/crm.php:2435 sessions_revoke`, `crm/rbac.js:789`) اما **دستی و در دسترس خودِ ۴ نقش ارشد** است — مهاجمی که توکن ارشد دارد می‌تواند از همان ابزار برای «خروج اجباریِ همهٔ رقبا و تنها شدنِ خودش» استفاده کند.
- **۲FA اختیاری و fail-open** برای نقش‌های مالی (`api/crm.php` `auth_login`): اگر SMS پیکربندی نشده و `strict=false` باشد، بدون کد پیامکی ادامه می‌دهد.
- **حسابدار** (نقش مالی) فقط ۸ ساعت TTL دارد (خوب) اما `users_write` ندارد (خوب)؛ تمرکز اصلی شما روی ۴ نقش ارشد درست است.

---

## ۳. یافته‌ها (اولویت‌بندی‌شده)

### 🔴 P1-1 — فلگ `allow_wipe` سمت سرور به نقش مقید نیست (بای‌پس سپر ضد داده‌صفر)

- **شواهد:** `api/crm.php:2507` (`$allow_wipe = !empty($j['allow_wipe']);` — بدون گارد نقش) در برابر `api/crm.php:2509` (`$restore` که `in_array($client_role,['admin','chairman'])` دارد). بای‌پس سپر در `api/crm.php:2652` (`if (!$allow_wipe && !$restore && !$isSharedUnion)`). سمت کلاینت فقط `crm/golive.js:38` (`if (curRole() !== 'admin')`).
- **اثر:** هر نقشِ احرازشده با `sync_write` (هر ۸ نقش) می‌تواند با `data_push` و `allow_wipe:true` مجموعه‌های مجازِ نقش خودش (برای ۴ نقش ارشد = همهٔ کلیدها) را با آرایهٔ خالی روی سرور بازنویسی کند و «سپر حذف انبوه v34.38.12» را دور بزند. این تنها بای‌پسِ شناخته‌شدهٔ سپر ضد داده‌صفر است و مستقیم به «از بین رفتن داده» مربوط می‌شود.
- **توصیه:** دقیقاً مانند `$restore`، در همان نقطه گارد بگذارید:
  ```php
  if ($allow_wipe && !in_array($client_role, ['admin','chairman'], true)) {
      http_response_code(403);
      echo json_encode(['ok'=>false,'error'=>'wipe_permission_denied']);
      break;
  }
  ```
  (ترجیحاً فقط `admin`، هم‌تراز با `golive.js` که فقط admin را مجاز می‌داند.)

### 🔴 P1-2 — کش آفلاین کامل + توکن در LS: برون‌بَرد بی‌سر و صدا از دستگاه ارشد

- **شواهد:** `crm/rbac.js:135` (توکن در LS)، `crm/storage.js`/`client-server.js` (کش همهٔ کلیدها)، `api/crm.php:364` (نقش ارشد = همهٔ کلیدها).
- **اثر:** دستگاه آلوده کل داده را بدون لاگ/rate-limit سروری می‌خواند. این بزرگ‌ترین سطح محرمانگی است و با هیچ گارد سروری بسته نمی‌شود.
- **توصیه (عملیاتی، نه کد):** سیاست دستگاه برای ۴ نقش ارشد: الزام دستگاه مدیریت‌شده/ضدویروس، عدم استفاده از دستگاه شخصی، حذف «مرا به خاطر بسپار» برای نقش‌های ارشد (فقط `sessionStorage`)، خروج اجباری دوره‌ای. (تغییر کد اختیاری: عدم ارائهٔ گزینهٔ remember-me برای نقش‌های ارشد.)

### 🔴 P1-3 — بک‌آپ ابری فقط ۲ کلید ثابت و فقط با push مرورگر (بدون cron مستقل)

- **شواهد:** چرخش در `api/crm.php:1129` (`ptf_rotate_backup`)، آپلود ابری با کلید ثابت `crm-backup-latest` + `crm-backup-monthly` (`api/storage.php:326-345`)، هرس ابری که همهٔ چیز زیر `backups/` جز ۲ کلید را حذف می‌کند (`api/storage.php:337`)، زمان‌بندی ساعتی سمت کلاینت (`crm/backup.js:445-447`). تنها cron سروری موجود `api/management-report-cron.php` گزارش است، نه بک‌آپ.
- **اثر:** اگر هیچ مرورگری باز نباشد، بک‌آپ جدیدی ساخته نمی‌شود (RPO وابسته به آنلاین بودن یک کلاینت). نسخهٔ ابری فقط «آخرین + ماهانه» است؛ یک push خرابِ «غیرمشکوک» نسخهٔ خوب قبلی ابری را رونویسی می‌کند (سپر قرنطینه فقط افت >۵۰٪ را می‌گیرد، نه خرابی جزئی). روزانه فقط ۳ نسخه و فقط روی هاست نگه داشته می‌شود.
- **توصیه:** (۱) بک‌آپ سروری زمان‌بندی‌شده (cron) مستقل از مرورگر که JSON/MySQL را به Arvan بفرستد؛ (۲) نسخه‌بندی/حفظ ۳+ نسخهٔ ابری به‌جای ۲ کلید ثابت؛ (۳) اطمینان از بک‌آپ سطح هاست/دیتابیس توسط ارائه‌دهنده.

### 🟠 P2-1 — `users_write` برای هر ۴ نقش ارشد → تثبیت دسترسی از ceo/commercial

- **شواهد:** `api/crm.php:290` (`users_write => ['admin','chairman','ceo','commercial']`)، `users_sync` نقش‌ها را از بدنهٔ POST می‌نویسد (`api/crm.php:3083`)، `add_user` (`api/crm.php:3434`)، و `ptf_crm_users`/`ptf_crm_perms` از مسیر سینک هم قابل push هستند (`api/crm.php:367` + `load_all_crm_users_sources` خواندن `sync/ptf_crm_users.json`).
- **اثر:** دستگاه آلوده با نقش `ceo`/`commercial` می‌تواند کاربر ادمین جدید بسازد یا نقش کسی را به admin تغییر دهد → دسترسی پایدار.
- **توصیه:** `users_write` را به `admin` (حداکثر `admin+chairman`) محدود کنید؛ و/یا هر تغییر نقش/کاربر را مشمول ۲FA اجباری (`strict`) کنید. «ساخت کاربر admin» باید رویداد هشدار باشد.

### 🟠 P2-2 — باقیماندهٔ M1: هش SHA-256 ادمین همچنان در `ptf_crm_settings` سینک و بک‌آپ می‌شود

- **شواهد:** `crm/inqreader.js:55-57` (تغییر رمز ادمین `st.adminHash = newH` در `ptf_crm_settings`)، `crm/backup.js:110` (settings در بک‌آپ)، `api/crm.php:367` (settings در سینک نقش ارشد). مسیر لاگین سرور دیگر از این مقدار استفاده نمی‌کند (`api/crm.php:3201-3212` از `admin_password_hash` bcrypt / `admin_sha256` از secrets).
- **اثر:** یک نسخهٔ هش سریع (SHA-256) از رمز ادمین روی همهٔ دستگاه‌های ارشد و در بک‌آپ‌ها توزیع می‌شود؛ اگر رمز ضعیف باشد آفلاین شکسته می‌شود. (مقدار عملاً «بلااستفاده» برای لاگین سرور است → ریسک بدون فایده.)
- **توصیه:** حذف `adminHash` از settings و اتکا به مسیر سروری bcrypt؛ تغییر رمز ادمین هم از مسیر سروری (مانند `users_sync`) انجام شود نه نوشتن SHA-256 در کلاینت. توجه: مسیر تغییر رمز کاربران عادی نیز هنوز `passhash` SHA-256 می‌نویسد (`crm/inqreader.js` سطرهای بعدی) که مهاجرت به bcrypt را خنثی می‌کند.

### 🟠 P2-3 — گارد HMAC روی اکشن‌های حساس، از اکشن‌های «مرده» محافظت می‌کند

- **شواهد:** `api/crm.php:69` (`$SENSITIVE_ACTIONS_HMAC = ['delete_batch','purge_cloud','update_role','delete_user']`) اما در سوئیچ هیچ `case` ای برای این چهار اکشن وجود ندارد (grep کامل). مشابهاً `$SENSITIVE` در `api/crm.php:348` شامل `save_user`/`del_user` بدون handler است.
- **اثر:** لایهٔ دفاعیِ HMAC روی اکشن‌هایی نشسته که اجرا نمی‌شوند؛ اکشن‌های واقعاً خطرناک (`add_user`, `users_sync`, `delete_batch@storage.php`) فقط با نقش + Origin محافظت می‌شوند. ریسک: اگر بعداً handlerی اضافه شود، ممکن است گارد جا بماند.
- **توصیه:** یا handlerهای مربوطه را کامل کنید، یا این نام‌ها را از لیست پاک کنید و به‌جای آن گارد HMAC/۲FA را به اکشن‌های زندهٔ حساس (`users_sync`، `add_user`، `sessions_revoke`) ببرید. (سندنویسی واضح که چرا HMAC هست و secret کجاست.)

### 🟠 P2-4 — حذف دائمی ابری S3 بدون نسخه‌بندی/تأیید دوم

- **شواهد:** `api/storage.php:38-42` (`delete`/`delete_batch`/`archive_zip`/`backup_prune` فقط `admin/chairman`)، اجرای حذف دائمی S3 (`api/storage.php:451` `delete_batch`)، گارد «اول دانلود بعد حذف» فقط در کلاینت (`crm/archive.js`).
- **اثر:** اگر دستگاه admin آلوده باشد، حذف فایل‌های ابری برگشت‌ناپذیر است (مگر S3 نسخه‌بندی داشته باشد که باید با Arvan تأیید شود). گارد «دانلود اول» فقط UI است و بدافزار آن را دور می‌زند.
- **توصیه:** فعال‌سازی S3 Versioning/Replication روی باکت Arvan؛ محدودکردن `delete`/`archive_zip` به admin فقط؛ درخواست تأیید دوم (رمز) یا ۲FA برای هر عملیات مخرب ابری.

### 🟡 P3-1 — CSP با `unsafe-inline`/`unsafe-eval`

- **شواهد:** `.htaccess:28`. **اثر:** دفاع XSS ضعیف‌تر از حالت ایده‌آل (نیاز کد legacy). **توصیه:** مهاجرت تدریجی به nonce/hash و حذف `unsafe-eval` در فازهای بعدی؛ فعلاً ریسک پذیرفته‌شده.

### 🟡 P3-2 — بررسی هم‌دامنه Origin/Referer با curl دور زدنی است

- **شواهد:** `api/storage.php:87`. **اثر:** برای کلاینت غیرمرورگری Origin خالی است و عبور می‌کند؛ اما چون JWT معتبر لازم است، اثر عملی کم است (این لایه CSRF مرورگر را می‌بندد). **توصیه:** مستندسازی؛ در صورت امکان الزام `X-CRM-Token` هدر + غیرفعال‌کردن پذیرش token از `$_REQUEST` برای اکشن‌های مخرب.

### 🟡 P3-3 — rate-limit فقط روی ۴ اکشن عمومی + لاگین

- **شواهد:** `api/crm.php:89` (`$PUBLIC_LIMITED`)، `login_rate_allowed` در لاگین. **اثر:** اکشن‌های احرازشده (مثلاً `data_pull` سنگین) rate-limit ندارند. **توصیه:** در صورت نیاز، سقف فرکانس برای pullهای بزرگ.

---

## ۴. محافظ‌های موجود برای «از بین نرفتن داده» (دفاع در عمق)

این فهرست نشان می‌دهد کدام سناریوهای حذف/خرابی از قبل مهار شده‌اند (وضعیت خوب است):

| لایه | مکانیزم | شاهد |
|---|---|---|
| حذف نرم | tombstone → `ptf_crm_trash` / `ptf_crm_deleted_archive` (قابل بازیابی) | `crm/bridge.js:1114`, `crm/guards.js:208`, `crm/custmerge.js:129` |
| ضد حذف انبوه | `MASS-DELETE-SHIELD` v34.38.12 (ردِ push خالی روی دادهٔ ناخالی) | `api/crm.php:2652` |
| ضد افت ناگهانی بک‌آپ | قرنطینهٔ «suspect» وقتی شمارش کلید حیاتی >۵۰٪ افت کند | `api/crm.php:1136-1163` |
| چرخش بک‌آپ | hourly + daily(۳) + weekly + monthly + گام فوری | `api/crm.php:1164-1199` |
| بک‌آپ ابری | کلید ثابت latest+monthly در Arvan + هرس | `api/crm.php:1195-1207`, `api/storage.php:326-345` |
| ذخیرهٔ دوگانه | JSON هیچ‌وقت حذف نمی‌شود؛ شکست MySQL → fallback به JSON | `api/db-lib.php` |
| بازیابی تراکنشی | pre-restore snapshot + rollback تمام‌یا‌هیچ | `crm/backup.js:566-610` |
| بازیابی رکورد/تماس | `restore-collection.js` (فقط افزودن، admin/chairman، احترام به tombstone) و `restore-contacts.js` (فقط فیلد تماس) | `crm/restore-collection.js`, `crm/restore-contacts.js` |
| ضد race | `flock` روی `meta.json.lock` و `tokens.json.lock` | `api/crm.php:2515`, `api/auth.php` |
| ضد overwrite قدیمی | تشخیص تعارض base-revision + merge سروری | `api/crm.php:2597` |
| کلیدهای مالی محافظت‌شده | merge مالی‌ها فقط از مسیر محافظت‌شده | `api/crm.php:2621` |
| گارد ظرفیت LS | ۵MB soft limit + IndexedDB؛ حذف خودکار رکورد تجاری ندارد | `crm/storage-quota.js` |
| حذف ابری | «اول دانلود بعد حذف» | `crm/archive.js` |
| tokens خراب | فایل خراب به‌جای پاک‌شدن rename می‌شود (نشست‌ها یک‌جا نمی‌پرند) | `api/auth.php` `auth_load_tokens` |

---

## ۵. سناریوهای باقی‌ماندهٔ از دست دادن داده (residual)

1. **جعل `allow_wipe`** → خالی‌شدن مجموعه‌ها روی سرور (P1-1). **احتمال وقوع:** با دستگاه آلوده متوسط؛ **مهارکنندهٔ فعلی:** فقط بک‌آپ‌ها (که خودشان client-push هستند).
2. **حذف دائمی ابری توسط admin آلوده** (P2-4) → از دست رفتن پیوست‌ها بدون نسخه‌بندی.
3. **push خرابِ جزئی** که سپر قرنطینه نگیرد → رونویسی آخرین نسخهٔ خوب ابری (P1-3).
4. **رویداد چنددستگاهی هم‌زمان/سرد** → بازنویسی‌های متضاد (مهارشده با base-revision، ولی در سناریوهای پیچیده ممکن است به تصمیم دستی نیاز شود).
5. **از کار افتادن هاست** → JSON روی دیسک + Arvan (۲ کلید) تنها نسخه‌ها هستند؛ اگر بک‌آپ سطح هاست مستقل نباشد، RPO به آخرین push مرورگر می‌رسد.

---

## ۶. ماتریس اقدامات پیشنهادی (اولویت‌دار)

| # | اقدام | اولویت | مالک پیشنهادی | هزینه | ریسک تغییر |
|---|---|---|---|---|---|
| 1 | گارد سروری نقش برای `allow_wipe` (admin/chairman) + تستر قفل | **P1 — فوری** | Backend | خیلی کم | خیلی کم |
| 2 | بک‌آپ cron سروری مستقل + نسخه‌بندی ابری (۳+ نسخه) | **P1** | DevOps/هاست | متوسط | کم |
| 3 | سیاست دستگاه ارشد (MDM/ضدویروس، بدون remember-me، خروج دوره‌ای) | **P1** | مدیریت | کم | صفر (کد نیست) |
| 4 | `users_write` فقط admin (+۲FA اجباری برای تغییر نقش/کاربر) | P2 | Backend | کم | کم |
| 5 | حذف `adminHash` از settings؛ تغییر رمز از مسیر سروری bcrypt | P2 | Frontend+Backend | کم | متوسط (تستهای رمز) |
| 6 | S3 Versioning/Replication روی باکت Arvan | P2 | DevOps | کم | صفر |
| 7 | پاک‌سازی گاردهای HMAC/`$SENSITIVE` مرده و انتقال HMAC/۲FA به اکشن‌های زندهٔ حساس | P2 | Backend | کم | کم |
| 8 | حذف تدریجی `unsafe-eval` از CSP | P3 | Frontend | زیاد | زیاد |
| 9 | rate-limit برای pullهای سنگین احرازشده | P3 | Backend | کم | کم |

---

## ۷. محدوده و آنچه تغییر نکرد

- این ارزیابی **فقط خواندنی** بود؛ هیچ فایل تولیدی، گارد، یا تستی تغییر نکرده و درختی که ارزیابی شد همان `v34.38.20` شاخته‌شده است.
- اجرای PHP در محیط ارزیابی ممکن نبود؛ `api/*.php` به‌صورت ایستا بازبینی شد. صحت اجرایی ادعاها (مثلاً رفتار واقعی `allow_wipe`) پیش از تغییر، با یک تستر سروری/دستی باید تأیید شود.
- مواردی که باید **با ارائه‌دهنده/محیط واقعی** تأیید شود: وجود بک‌آپ سطح هاست/دیتابیس، نسخه‌بندی باکت Arvan، و مقدار واقعی `mode` در `api/ptf-db-config.php`.

---

## ضمیمه A — ماتریس دسترسی نقش‌ها (خلاصهٔ سروری)

| کلید/اکشن | admin | chairman | ceo | commercial | sales | buyer | accountant | collector |
|---|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|
| سینک همهٔ کلیدها (incl. finance/users/perms) | ✔ | ✔ | ✔ | ✔ | جزئی | جزئی | جزئی | جزئی |
| `finance_read/write` | ✔ | ✔ | ✔ | ✔ | ✘ | ✘ | ✘ | ✘ |
| `users_write` (add_user / users_sync / sessions_revoke / get_backup) | ✔ | ✔ | ✔ | ✔ | ✘ | ✘ | ✘ | ✘ |
| `approve_write` | ✔ | ✔ | ✔ | ✔ | ✘ | ✘ | ✘ | ✘ |
| قیمت خرید (`buyprice_read`) | ✔ | ✔ | ✔ | ✔ | ✘ | ✔ | ✘ | ✘ |
| قیمت فروش (`sellprice_read`) | ✔ | ✔ | ✔ | ✔ | ✔ | ✘ | ✔ | ✘ |
| storage مخرب (delete/delete_batch/archive_zip/backup_prune) | ✔ | ✔ | ✘ | ✘ | ✘ | ✘ | ✘ | ✘ |
| storage مالی (`delete_financial`) | ✔ | ✔ | ✔ | ✔ | ✘ | ✘ | ✔ | ✘ |
| storage رکورد (delete_case_document/delete_rfq_attachment) | ✔ | ✔ | ✔ | ✔ | ✔ | ✔ | ✔ | ✘ |
| `restore` (بازگردانی کامل) | ✔ | ✔ | ✘ | ✘ | ✘ | ✘ | ✘ | ✘ |
| `allow_wipe` (بای‌پس سپر) | **بدون گارد سروری — هر نقش احرازشده** | | | | | | | |

## ضمیمه B — رازها و محل نگهداری

| راز | محل | یادداشت |
|---|---|---|
| `auth_key` (امضای JWT) | `ptf-secrets.php` بیرون webroot | `api/secrets.php`, `api/auth.php` |
| `sensitive_action_key` | همان | گارد HMAC اکشن‌های حساس (فعلاً روی اکشن‌های مرده) |
| `captcha_key` | همان | کپچای سروری |
| `admin_password_hash` (bcrypt) / `admin_sha256` / `default_admin_hash` | همان | لاگین سروری ادمین |
| اطلاعات پیامک/۲FA | همان | `twofa_*`, `sms_*` |
| اعتبار دیتابیس | `api/ptf-db-config.php` (خارج git) | `api/db-lib.php` |
| اعتبار Arvan (S3) | `storage-config.php` بیرون webroot | `api/storage.php` |
