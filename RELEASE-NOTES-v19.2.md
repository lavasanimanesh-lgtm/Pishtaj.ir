# 🚀 ریلیزنوت v19.2 — اسپرینت ۳ از R12: «مراحل ۱۲گانه پرونده + ارسال و تحویل داخل پرونده»

**تاریخ:** ۱۴۰۵/۰۴/۲۱ | **شاخه:** واحد | **مبنا:** BACKLOG-SALESFILE-SOURCE-OF-TRUTH-R12.md — اسپرینت ۳ پیشنهادی

---

## 📦 محتوای اسپرینت (طبق قاعده ۱-۲ استوری هم‌خانواده)

| آیتم | شرح | وضعیت |
|---|---|---|
| **US-433** | وضعیت‌های مرحله‌ای ۱۲گانه پرونده فروش پس از برد — از «ابلاغ سفارش» تا «بایگانی‌شده» | ✅ کامل |
| **US-434 فاز ۲** | عملیات ارسال داخل پرونده: پکینگ لیست، بارنامه/بیجک، تحویل به کارفرما + گذار خودکار وضعیت | ✅ کامل |

هم‌خانواده: «Workflow پرونده فروش پس از برد» — گام سوم رودمپ R12 (پس از US-431ف۱ در v18.9 و US-432+434ف۱ در v19.1).

---

## 🧭 US-433 — مراحل ۱۲گانه پرونده فروش

### تصمیم معماری کلیدی: وضعیت «مشتق» است، نه فیلد دستی
مرحله پرونده هیچ‌جا به‌صورت فیلد آزاد ذخیره نمی‌شود؛ تابع واحد `sfStageOf(r)` آن را از **سیگنال‌های واقعی موجود** محاسبه می‌کند:

| مرحله | سیگنال منبع |
|---|---|
| ۱. ابلاغ سفارش / پرونده فعال | `wonOffer` (برد CO/TC) |
| ۲. استعلام تامین مرحله دوم | وجود استعلام rfqsmart متصل (sfDocsOf.supply) |
| ۳. در حال تامین توسط تامین‌کننده | وضعیت درخواست `st8` (گذار خودکار خرید واقعی — US-413/BUG-029) |
| ۴. تحویل تامین‌کننده | وضعیت درخواست `st9` (گذار ptfPayableDlv — US-413) |
| ۵. در حال آماده‌سازی ارسال | رویداد پکینگ لیست یا وضعیت `st6` |
| ۶. ارسال‌شده | رویداد بارنامه/سند ارسال |
| ۷. تحویل‌شده به کارفرما | رویداد تحویل کارفرما یا وضعیت `st7` |
| ۸. در حال صدور فاکتور | `invRef` پیشنهاد برنده (ارجاع حسابدار) |
| ۹. فاکتور صادر شد | فاکتور ثبت‌شده |
| ۱۰. در حال تسویه | فاکتور با مانده وصول‌نشده |
| ۱۱. تسویه‌شده / مختومه | وصول کامل همه فاکتورها |
| ۱۲. بایگانی‌شده | `st === 'archived'` |

**مزایا (پاسخ مستقیم به AC های بک‌لاگ):**
- **AC1/AC4** — پرونده، درخواست و کانبان همیشه هم‌راستا هستند چون مرحله از همان داده‌ها مشتق می‌شود؛ داده تکراری/واگرا ساخته نشد.
- **AC2** — هر رویداد مهم (خرید واقعی، تحویل تامین، پکینگ، بارنامه، تحویل کارفرما، ارجاع/صدور فاکتور، وصول) مرحله را جلو می‌برد.
- **AC3** — عقب‌گرد ساختاری ناممکن است: `sfStageOf` فقط max می‌گیرد و `sfRfqAlign` گذار درخواست را فقط رو به جلو (بر اساس ترتیب آرایه منبع واحد `PTF_RFQ_STATUSES`) و فقط از مسیر `ptfRfqSetStatus` انجام می‌دهد؛ درخواست لغوشده (stX) هرگز گذار نمی‌گیرد.

### UI
- **بج مرحله** در سطر فهرست پرونده‌ها: «🏭 در حال تامین توسط تامین‌کننده (3/12)».
- **استپر ۱۲خانه** بالای کشوی پرونده برنده — خانه فعلی پررنگ، طی‌شده‌ها روشن + توضیح «مرحله از رویدادهای واقعی محاسبه می‌شود؛ دستی و قابل عقب‌گرد نیست».

---

## 🚚 US-434 فاز ۲ — ارسال و تحویل داخل پرونده

سه دکمه جدید در ردیف «🧰 عملیات پرونده (Post-Award)» کشوی پرونده برنده:

1. **🧰 پکینگ لیست** — شماره + شرح بسته‌بندی/نگله ⟵ مرحله ۵ + گذار درخواست به «🟠 آماده‌سازی» (اگر عقب‌تر باشد).
2. **🚚 بارنامه/ارسال** — شماره بارنامه/بیجک (اجباری)، شرکت حمل (اجباری)، تاریخ ارسال ⟵ مرحله ۶.
3. **🤝 تحویل کارفرما** — نام تحویل‌گیرنده (اجباری)، تاریخ، شماره رسید ⟵ مرحله ۷ + گذار درخواست به «✅ تحویل شده» + **notify به مدیران: «گام بعد: ارجاع فاکتور از پرونده»** — زیرساخت مستقیم قفل فاکتور US-435 (اسپرینت بعد).

- رکوردها روی `r.shipEvents[]` + خط timeline + audit؛ هسته برنامه‌ای `sfShipCommit` جدا از UI (الگوی sfQcCommit v19.1 — قابل تست).
- پیوست اسکن سند: به رکورد ارسال **و** اسناد پرونده هر دو.
- سوابق در باکس «🚚 ارسال و تحویل» کشو نمایش داده می‌شوند و همراه پرونده به بایگانی می‌روند.
- ثبت تحویل مستقیم بدون پکینگ/بارنامه قبلی مجاز است اما در timeline با برچسب «ثبت مستقیم» شفاف می‌شود (واقعیت عملیاتی تحویل‌های حضوری کوچک).

---

## 🛡 اثبات شروط دائمی کارفرما

| شرط | اثبات |
|---|---|
| بدون وصله‌پینه | یک تابع مشتق واحد به‌جای فیلد وضعیت موازی؛ گذار درخواست فقط از `ptfRfqSetStatus` (قاعده v16.4/v17.3)؛ هسته‌های commit جدا از UI |
| وابستگی‌ها | سوار بر st8/st9 (US-413)، گذار خرید واقعی (BUG-029)، تحویل تامین (ptfPayableDlv)، invRef/فاکتورها (US-141/324)، بایگانی؛ کلید داده جدید ساخته نشد — همه روی `ptf_crm_deals` |
| بدون باگ جدید | **۹۵ تستر / ۲۷۱۰ چک — صفر خطا** + سینتکس ۵۹ فایل JS + ۵ بلوک inline + audit PASS |
| بدون تداخل با طرح توسعه | دقیقا اسپرینت ۳ پیشنهادی R12؛ مرحله ۷ و notify تحویل = پیش‌نیاز آماده US-435/436 (اسپرینت ۴)؛ US-437 (کنترل مختومه‌سازی) دست‌نخورده در صف |

## 🧪 تست
- **tester108** (جدید): ۴۲ چک — ساختاری + رفتاری کامل (پیمایش مرحله ۰→۱۲ با همه سیگنال‌ها، گذارهای st6/st7 از مسیر واحد، رد عقب‌گرد st7→st6، رد گذار درخواست لغوشده) + رگرسیون v19.1/BUG-027/US-413/BUG-029.
- کل مجموعه: **۹۵ تستر / ۲۷۱۰ چک سبز**.

## 📂 فایل‌های تغییر یافته
`crm/salesfiles.js` (موتور sfStageOf/sfStageLabel/sfRfqAlign + رویدادهای ارسال sfShipCommit/Open/Upload + استپر/بج/بایگانی) | `crm/index.html` + `crm/sw.js` (v19.2) | `_tools/uat/tester108-v192.js` (جدید)

## 📤 آپلود
zip روی هاست روی نسخه موجود اکسترکت شود — **پوشه api حذف نشود**. بعد از آپدیت همه کاربران رفرش کنند (سایدبار: CRM v19.2).

---

## ⏭ صف بعدی (منتظر ابلاغ)
① **US-435 + US-436 (اسپرینت ۴ R12):** قفل ارجاع فاکتور تا تحویل کارفرما + صدور فاکتور رسمی از کارتابل حسابدار — زیرساختش (مرحله ۷، notify تحویل، awardDocs) در v19.1/v19.2 آماده شد ② US-437 ③ US-426ف۲..429 ④ US-423 ⑤ US-410/411 ⑥ US-405..408 ⑦ US-402/403

**شماره‌گذاری:** بعدی US-438 | باگ بعدی BUG-030
