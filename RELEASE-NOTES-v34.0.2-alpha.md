# 📦 ریلیزنوت v34.0.2-alpha — هم‌ترازی پروداکشن با استیجینگ: مهاجرت حقوق سهامدار، رفع ردیف‌های تکراری گزارش تنخواه + cache-busting

**تاریخ:** ۱۴۰۵/۰۵/۱۳ (2026-08-04) | **نسخه قبلی:** v34.0.1-alpha (فقط استیجینگ)

---

## 🎯 هدف این ریلیز

پروداکشن (v34.0.0-alpha) از استیجینگ (v34.0.1-alpha) عقب مانده بود و سه مشکل در پروداکشن دیده می‌شد که در استیجینگ نبود:

1. **ضمایم** در گزارش/پرونده نمایش داده نمی‌شدند؛
2. **گزارش دوره‌ای تنخواه** ردیف‌های «تکراری» نشان می‌داد (جمع درست بود)؛
3. **حقوق سهامداران** در «هزینه‌های جاری» دیده/محاسبه نمی‌شد.

بررسی کامل (سند `EVALUATION-AGENT-PREVIOUS-v34-PROD-vs-STAGING-2026-08-04.md`) نشان داد کدِ رفع‌های F4-2…F4-13 از قبل در main/پروداکشن حاضر است؛ ریشهٔ واقعیِ هر سه مورد = **مهاجرت داده‌ای اجرانشده روی پروداکشن + رفتار گزارش‌گیر + محیط هاست (نبود Imagick)**. این ریلیز همان شکاف‌ها را می‌بندد.

---

## ✅ تغییرات این نسخه

### ۱) بامپ نسخه + cache-busting کامل (v34.0.2-alpha)
- `window.VER` در `crm/index.html` → **v34.0.2-alpha** و هر ۸۵ پارامتر `?v=` اسکریپت‌ها به‌روز شد (این همان کامیت `v34.0.1-alpha` بود که بعد از مرج روی برنچ استیجینگ مانده و به main نرسیده بود — حالا با یک نسخهٔ جدیدتر هر دو اعمال می‌شود).
- نام کش سرویس‌ورکر `crm/sw.js` → `ptf-crm-v34.0.2-alpha` (نکته‌ای که ایجنت قبلی جا انداخته بود).
- `VERSION.json` هم‌خوان شد (`crm_version: v34.0.2-alpha`).
- با بامپ نسخه، مکانیزم پاک‌سازی خودکار کش PWA در `index.html` فعال می‌شود؛ کاربران فقط یک بار صفحه را تازه کنند (یا `crm/clear-cache.html`).

### ۲) گزارش دوره‌ای تنخواه — رفع «ردیف‌های تکراری» (`crm/petty.js`)
- ریشه: هر هزینهٔ تنخواهِ تسویه‌شده/پرداخت‌مستقیم دو رکورد دارد: ردیف «هزینه» (ptf_crm_petty) + تراکنش «تسویه از حساب»/«پرداخت مستقیم» (ptf_crm_petty_tx با `ref` = شناسهٔ همان هزینه) — گزارش قبلاً هر دو را چاپ می‌کرد.
- حالا در `ptfPettyPeriodEvents` تراکنشی که «پرداختِ» یک هزینهٔ حاضر در همان دوره است، به‌عنوان ردیف مستقل حذف می‌شود؛ اطلاعات سند در ستون «نحوهٔ پرداخت/وضعیت» همان ردیف هزینه می‌آید (سند تسویه/پرداخت مستقیم هم اضافه شد).
- **رفع دوباره‌شماری جمع:** در `ptfPettyPeriodTotals` پرداخت‌های مستقیم قبلاً هم در `pettyOut` و هم در `directOut` می‌آمدند (دوبار) — حالا تراکنشِ وصل‌شده به هزینهٔ حاضر فقط یک بار شمرده می‌شود. جمع‌های قبلیِ سالم تغییری نمی‌کنند.
- گزارش، اکسل، چاپ/PDF تلفیقی همگی از همان `events` استفاده می‌کنند → در همه جا یکسان.

### ۳) مهاجرت «حقوق سهامدار فاقد opex» از داخل اپ (`crm/shareholders.js` + `crm/opex.js`)
- تابع `window.ptfMigrateShareholderOpex()` در `shareholders.js`: برای هر تراکنش حقوق موظف که رکورد هزینه (opex با `shareTx`) ندارد، ردیف «حقوق و دستمزد» می‌سازد (همان منطق اسکریپت کنسولی F4-7، حالا به‌صورت درون‌برنامه‌ای).
- دکمهٔ **«🛠 بازسازی حقوق سهامدار»** در هدر پنل «هزینه‌های جاری» (فقط نقش‌های مالی) → اجرا + گزارش تعداد + تازه‌سازی خودکار پنل‌ها. داده از طریق `setData` ذخیره و خودکار سینک می‌شود.
- `crm/_tools/migrate_shareholder_opex.js` به‌روزرسانی شد تا اگر تابع داخلی موجود بود از آن استفاده کند (یک منبع واحد).

### ۴) ضمایم — مستندسازی و راستی‌آزمایی
- کدِ رفع ضمایم (F4-4/F4-9/F4-11/F4-13 + گارد تازگی DB در v33.22.3) از قبل در پروداکشن است و با این ریلیز نسخهٔ آن هم‌تراز می‌شود.
- نکتهٔ باقی‌مانده (که ایجنت قبلی «باز» گذاشته بود): هاست اشتراکی **Imagick ندارد** و تبدیل PDF/HEIC به JPEG در `api/attachment-thumb.php` ممکن نیست. راه‌کار فعلی = لیست سفید فرمت برای آپلودهای جدید + لینک «↗ باز کردن فایل» برای اسناد قدیمی PDF/HEIC (fallback موجود در `ptfPettyReceiptHtml`).
- اقدام پیشنهادی جداگانه: فعال‌سازی Imagick روی هاست یا تبدیل سمت کلاینت — خارج از این ریلیز.

---

## 🧪 راستی‌آزمایی (محلی)
- `node --check` روی `crm/petty.js`، `crm/shareholders.js`، `crm/opex.js` ✅ (سینتکس سالم)
- منطق dedup گزارش با دادهٔ نمونه (هزینه + settle + direct) در یک تست مستقل ✅

## 📁 فایل‌های تغییریافته
`crm/index.html` (VER v34.0.2-alpha + ۸۵ پارامتر cache-bust) · `crm/sw.js` (نام کش) · `crm/petty.js` (dedup ردیف‌ها + جمع) · `crm/shareholders.js` (ptfMigrateShareholderOpex) · `crm/opex.js` (دکمهٔ بازسازی) · `crm/_tools/migrate_shareholder_opex.js` (یک‌منبع‌سازی) · `VERSION.json` · `RELEASE-NOTES-v34.0.2-alpha.md` (این فایل) · `EVALUATION-AGENT-PREVIOUS-v34-PROD-vs-STAGING-2026-08-04.md` (سند ارزیابی)

---

## 🚀 پس از استقرار روی پروداکشن (چک‌لیست)
1. **پروداکشن را از main استقرار دهید** (Actions → Deploy to Production → `confirm: DEPLOY`، `ref: main`).
2. **مهاجرت را یک بار اجرا کنید:** پنل تنخواه ← تب «هزینه‌های جاری» ← دکمهٔ «🛠 بازسازی حقوق سهامدار» ← تأیید. خروجی باید «✅ N رکورد اضافه شد» باشد (یا «موردی نبود»). سپس در ماژول سهامداران و محاسبات سال مالی راستی‌آزمایی کنید.
3. **کش:** یک بار Ctrl+Shift+R یا `crm/clear-cache.html` روی دستگاه‌های کاربران.
4. **ضمایم:** `crm/sync-diagnostics.html` بخش ۱۱ (تطابق فایل↔DB) + تست یک ضمیمهٔ JPG و یک PDF قدیمی.
