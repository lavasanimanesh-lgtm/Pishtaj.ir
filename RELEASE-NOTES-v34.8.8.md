# یادداشت انتشار CRM v34.8.8

تاریخ: ۱۴۰۵/۰۶/۴ (۲۰۲۶-۰۸-۲۶)

## ریشه‌یابی گیر دائمی «ptf_crm_avatars» در صف همگام‌سازی (پیوست v34.8.7)

### نشانهٔ گزارش‌شده توسط کارفرما
پس از v34.8.7 کلید `ptf_crm_audit` پاک شد اما `ptf_crm_avatars` برای مدیرعامل و مدیر بازرگانی همچنان dirty ماند؛ برای رئیس هیات مدیره همه‌چیز سبز است.

### علت‌های ریشه‌ای (دو مورد)
1. **رگرسیون v34.8.7 خودمان (سرور):** ادغام union آواتارها ورودی‌های غیررشته‌ای را پاک می‌کرد، در حالی که قرارداد آواتار (v31.7.11 BUG-AVATAR-001) رشتهٔ legacy یا شیء نسخه‌دار `{v, ts}` است (`v:null` = tombstone حذف). سرور مقدار پاک‌شده ذخیره می‌کرد، کلاینت مقدار محلی نسخه‌دار را در merge نگه می‌داشت و چرخهٔ dirty/overwrite ادامه داشت.
2. **بن‌بست ردیف صف phantom (کلاینت):** کلیدهای سنگین (از جمله آواتارها) در آینهٔ IndexedDB نگهداری می‌شوند؛ اگر ردیف صف موجود ولی مقدار در هیچ لایه‌ای خوانده نشود، flush با `local_payload_missing` شکست می‌خورد و کد قبلی همان کلید را `pending` می‌کرد — بدون هیچ مسیر پاک‌سازی. نتیجه: dirty ابدی بدون حتی یک درخواست push (هم‌راستا با «آخرین خطا: pull — ok» در تشخیص کاربر). نقش‌محور بودنش تصادفی بود: فقط دستگاه‌هایی که آواتارهایشان به آینهٔ IDB مهاجرت کرده بود.

### رفع‌ها
- **سرور (`api/crm.php`):** ادغام آواتار حالا نسخه‌دار و قرارداد-محور است — برندهٔ هر کاربر، ورودی با `ts` جدیدتر؛ نسخه‌دار بر رشتهٔ legacy می‌چربد؛ tombstone حذف با سقف ۳۰ روز (همان `ptfSmartMerge` کلاینت). هیچ مقدار معتبری پاک نمی‌شود.
- **کلاینت (`crm/client-server.js`):**
  - ردیف صف phantom: یک بازخوانی مجدد؛ اگر مقدار همچنان موجود نبود، ردیف صف و dirty همان کلید پاک می‌شود (با ثبت audit) و مقدار معتبر از سرور در pull بعدی می‌آید — به‌جای pending ابدی.
  - پول فاز B برای نقشه‌های نسخه‌دار (`ptf_crm_avatars` و `ptf_crm_sigprofiles`) merge می‌کند نه overwrite خام — مقدار محلی `{v,ts}` دیگر در پول از بین نمی‌رود.
- **نسخه:** bump واقعی v34.8.8 در تمام نقاط قرارداد.

### خودترمیمی
دادهٔ آواتار سرور در دورهٔ کوتاه v34.8.7 ممکن است پاک‌شده باشد؛ دستگاه‌هایی که مقدار محلی `{v,ts}` دارند در نخستین push آن را دوباره روی سرور می‌کارند. اگر آواتار کاربری جای دیگری هم پاک شده بود، یک بار بارگذاری مجدد عکس کافی است — این بار پایدار می‌ماند.

### کنترل کیفیت
- tester513 با اطمینان‌های جدید (merge نسخه‌دار، tombstone، پران phantom، merge نقشه‌ها در پول).
- گیت CI: ۱۲۹ PASS / 0 FAIL (شامل php-parse همهٔ api/*.php).
