# فاز ۵ (v34.0.8-alpha) — اصلاحات منطق سود / گردش مالی

**برنچ:** `arena/019fcd1d-pishtaj-ir`
**مبنای تصمیم:** تعریف کارفرما (این نشست) + تأیید موارد A/B/C/D از بازبینی قبل.

---

## فاز ۱ — فاکتور صوری/پوششی (cover invoice)
- **قبل:** مبلغ اسمی فاکتور پوششی به‌عنوان هزینهٔ کامل در `supplierInvoices` کسر می‌شد → سود را به‌شدت کم‌نمایش می‌کرد.
- **بعد:** فقط «درصد کارمزد فاکتورساز» (مثلاً ۱.۵/۲/۳٪) هزینه است و «اعتبار ارزش‌افزوده» (VAT) منفعت. اثر خالص بر سود = `VAT − کارمزد`.
  - مثال: فاکتور ۱۰۰م با ۱۰م VAT و کارمزد ۱.۵٪ → کارمزد ۱.۵م هزینه، اعتبار ۱۰م، خالص منفعت ۸.۵م.
- **پیاده‌سازی:** helper `fiscalCoverTotals(year)`؛ در `ptfFiscalData.netProfit` منفعت خالص پوششی اضافه می‌شود؛ در `ptfFiscalCashData` مبلغ اسمی پوششی از `supplierInvoices` حذف و فقط کارمزد (خروجی نقدی واقعی) لحاظ می‌شود؛ اعتبار ارزش‌افزوده نقد نیست و در گزارش به‌صورت جدا نمایش داده می‌شود.

## فاز ۲ — حقوق سهامدار = مطالبه (نه علی‌الحساب سود)
- حقوق سهامدار موظف هم «هزینهٔ جاری» (opex) است هم «مطالبهٔ آن سهامدار» (فارغ از درصد سهم).
- پرداخت حقوق با نوع جداگانهٔ `salary_payment` ثبت می‌شود (تابع `ptfSharePaySalary` + دکمهٔ «💳 پرداخت حقوق»).
- `salary_payment` در «علی‌الحساب سود» (`advRows` در `ptfFiscalCashDistribution`) لحاظ **نمی‌شود**، بنابراین ستون «ماندهٔ قابل تسویهٔ امسال» برای سهامدار موظف منفیِ گمراه‌کننده نمی‌شود.

## فاز ۳ (مورد B) — گردش حساب تأمین‌کننده فقط فاکتور خرید
- «تعهد خرید legacy» (که از قیمت خرید دستی می‌آمد و «خریدِ تعهدی» بود) از **مبلغ** گردش/بدهی حذف شد؛ فقط به‌عنوان شمارش اطلاع‌رسانی (`legacy++`) دیده می‌شود. مبنای تعهد = فاکتور خرید + ماندهٔ باقیماندهٔ همان.

## فاز ۴ (مورد C) — مغایرت فاکتور/پیش‌فاکتور غیربلوکه
- هشدار «مغایرت با پیش‌فاکتور ارزی» دیگر ثبت فاکتور را متوقف نمی‌کند (فقط اطلاع‌رسانی با `ptfToast`). اصل بر صحت فاکتور است؛ پیش‌نهاد فقط مبنای صدور فاکتور است.

## فاز ۵ (مورد A) — مبنای هزینهٔ پروژه = فاکتور خرید، نه قیمت دستی
- `ptfProjectProfitIRR` هزینه را از **فاکتورهای خریدِ لینک‌شده** به پرونده (از طریق `legacyPayableCds→payable.inqNo`، `itemLinks.offerNo`، یا `inqNo/offerNo` مستقیم) می‌خواند.
- «قیمت خریدِ دستی» اختیاری و فقط «کنترل صحت» است (مغایرت با فاکتور گزارش می‌شود، اما مانع محاسبه نیست).
- بدون فاکتور خریدِ لینک‌شده → سود قطعی اعلام نمی‌شود (هزینه مبنای فاکتور خرید است). نبودِ قیمت دستی دیگر سود را ناقص نمی‌کند.

## فاز ۶ (مورد D) — نرخ روزِ فاکتور
- نرخ تسعیر فقط برای محاسبهٔ درصد پیش‌پرداخت از کل پیشنهاد به‌کار می‌رود. نرخِ روزِ صدور فاکتور (`issueFxRate`) روی رکورد فاکتور ذخیره می‌شود تا بدهیِ باقی‌ماندهٔ ارزی با نرخ روز گزارش شود.
  - مثال: پیش‌پرداخت ۵۰۰$ (۱۰۰م با نرخ ۲۰۰هزار)؛ در فاکتور با نرخ ۳۰۰هزار → ۱۰۰۰$ باقی = ۳۰۰م و فاکتور کل ۴۰۰م.

---

## تست‌ها
- تستر جدید `tester304-v34.0.8-alpha-profit-cover-salary.js` (**۲۰/۰**): پوشش پوششی، حقوق، B، C، A، D + شبیه‌سازی عددی فاکتور پوششی.
- به‌روزرسانی `tester176` به مدل فاکتور-محور (**۱۰/۰**).
- رگرسیون کامل سبز (101/287/288/290/291/300/301/302/303/176/149/165).

*تولیدشده توسط Arena Agent — پیاده‌سازی فازبندی‌شدهٔ منطق سود طبق تأیید کارفرما.*
