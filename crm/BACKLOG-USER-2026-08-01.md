# بک‌لاگ رسیدگی — گزارش‌های کاربر ۱۴۰۵/۰۸/۱۰ (2026-08-01)

**برنچ:** `arena/019fbe19-pishtaj-ir`
**منبع:** گزارش مستقیم کارفرما در نشست جاری (۹ مورد — به ترتیب اولویت اجرا)
**وضعیت کلی:** هر مورد با شناسه، ریشهٔ فنی، و اقدام ردیابی می‌شود. موارد ۱ و ۵ همان نشست رفع شدند؛ بقیه به ترتیب.

---

| # | شناسه | نوع | عنوان | ریشه/ارجاع فنی | وضعیت |
|---|---|---|---|---|---|
| 1 | `UR-2026-08-01-01` | باگ | دکمه‌های «+ ثبت هزینه»، «پرداخت مستقیم»، «شارژ حساب»، «ارجاع دوره» در **همهٔ تب‌های هاب مالی** دیده می‌شوند؛ باید فقط در تب تنخواه باشند | تولبار `.sb2` در `petty.js#buildPetty` **بدون id** است؛ `financehub.js#finHubApply` فقط `ptAccount/ptPeriods/ptSummary/ptWrap` را مخفی می‌کند، تولبار را نه | ✅ **رفع شد** (id=ptToolbar + مخفی‌سازی در تب‌های غیر تنخواه) |
| 2 | `UR-2026-08-01-02` | باگ | جستجو/تایپ نام کامل تامین‌کننده و مشتری ممکن نیست — بعد از یک حرف تمرکز از کادر می‌پرد | الگوی `oninput="renderOffers()"` در `offers.js:297` (oFsrch) و موارد مشابه که کل کانتینر (شامل input) را بازرندر می‌کنند؛ سابقهٔ مشابه قبلاً در `offers.js:1945` رفع شده بود | ✅ **رفع شد** — بازرسی کامل: اکثر رندرها فقط ناحیهٔ داده را عوض می‌کنند (فوکوس سالم)؛ تنها مورد واقعی `cfFinanceSearch` بود که کل باکس را با `outerHTML` بازسازی می‌کرد → اکنون فقط `#cfFinanceTbl` را به‌روز می‌کند. tester204 (۱۱/۰). |
| 3 | `UR-2026-08-01-03` | باگ/نیاز | حساب مشتری (تب مشتریان هاب مالی) خروجی/گزارش گردش حساب نمی‌دهد | `customer-finance.js#cfOpen` فقط نمایش داخل مودال دارد؛ هیچ دکمهٔ چاپ/خروجی ندارد (الگوی آماده در `supplier-finance.js`: `slLedgerPrint` + `slCsv`) | ✅ **رفع شد** — با مورد ۸ (cfLedgerRows/Csv/Print + دکمه‌ها در مودال) — tester205 (۱۰/۰) |
| 4 | `UR-2026-08-01-04` | باگ | در ثبت قیمت خرید واقعی، عبارت «مجموع خرید» نمایش داده می‌شود؛ باید «قیمت واحد» درخواست شود | جمع‌بندی «مجموع خرید» در `buycompare.js` (خطوط 604/618 `cmpSplitSummary`) و «Σ n قلم = …» در `cmpBulkTotal` (خط 499+) که قیمت ورودی را در تعداد ضرب می‌کند و کاربر را به‌اشتباه می‌اندازد | ✅ **رفع شد** (طبق انتخاب کارفرما «هر دو جا»): در پنجرهٔ تقسیم خرید و ثبت گروهی، برچسب جمع به «جمع کل (اطلاعاتی)» + راهنمای «قیمت واحد هر قلم را وارد کنید — جمع فقط برای اطلاع است» تغییر کرد — tester202 (۲۵/۰) |
| 5 | `UR-2026-08-01-05` | باگ | تشخیص کالاهای مشابه (تب کیفیت داده): بعد از «تأیید ادغام» هیچ اثری دیده نمی‌شود و فقط همان پنجره دوباره باز می‌شود | دو علت: (الف) `data-quality.js#ptfCatalogMergeConfirm` در پایان `ptfCatalogSimilarAudit()` را صدا می‌زند که پنجره را دوباره باز می‌کند؛ (ب) `ptfCatalogSimilarAudit` کالاهای `hidden/merged` را فیلتر نمی‌کند، پس همان گروه دوباره پیشنهاد می‌شود | ✅ **رفع شد** (فیلتر کالاهای ادغام‌شده + بستن پنجره به‌جای بازکردن دوباره) |
| 6 | `UR-2026-08-01-06` | نیاز | در انتخاب تامین‌کنندهٔ هر قلم (پنجرهٔ استعلام) امکان **جستجو با نام/برند/تجهیزات** تامین‌کننده باشد | `supspec.js` از قبل جستجوی دوزبانهٔ برند-آگاه دارد (AC2) اما سلکت تامین‌کنندهٔ per-item در `buycompare.js` (سلکت سادهٔ `cmpSplitSuppliers`/فرم استعلام) به آن متصل نیست | ✅ **رفع شد** — `ptfSupSearch` (نام/برند/تجهیزات با امتیازدهی + مترادف دوزبانه) + پیکر `ptfSupPickerHtml/Filter/Pick/Key` در supspec.js؛ اتصال در ۳ محل buycompare: ثبت قیمت دور (cmpSup)، تقسیم خرید (هر ردیف)، ثبت خرید واقعی (cmpBuySupPick). tester206 (۱۳/۰) |
| 7 | `UR-2026-08-01-07` | نیاز | سورت‌بندی همهٔ فهرست‌ها (درخواست‌ها، پیشنهادات، تنخواه و…) بر اساس تاریخ/شرکت/… | نیازمند یک کامپوننت سورت مشترک (بررسی `listtools.js`) و افزودن به رندرهای اصلی: `offers.js#renderOffers`، `petty.js#renderPetty`، `supplier-finance.js`، `customer-finance.js` و… | ✅ **رفع شد** — کامپوننت مشترک `crm/sortable.js` (ptfSortVal/Rows/Sorted + هدر قابل کلیک + dropdown) + اتصال در ۵ لیست: پیشنهادات (شماره/خریدار/تاریخ/مبلغ/وضعیت)، درخواست‌ها RFQ (کد/مشتری/تاریخ/وضعیت)، تنخواه (dropdown: تاریخ/مبلغ/ثبت‌کننده)، حساب مشتریان (نام/مانده/اعتبار)، حساب تامین‌کنندگان (نام/مانده). در ضمن باگ فوکوس `slFinanceSearch` (outerHTML) هم رفع شد (هم‌خانوادهٔ مورد ۲). tester207 (۱۴/۰) |
| 8 | `UR-2026-08-01-08` | نیاز | خروجی **PDF/اکسل** گردش حساب مشتریان (تب مشتریان هاب مالی) | کپی الگوی تامین‌کننده (`slLedgerPrint` → PDF چاپی + `slCsv` → Excel/CSV) در `customer-finance.js` برای `cfOpen`/`cfAccountRows` | ✅ **رفع شد** — `cfLedgerRows` (ردیف‌های مرتب با ماندهٔ تجمعی) + `cfLedgerCsv` (CSV با BOM) + `cfLedgerPrint` (پیش‌نمایش چاپی/PDF) + دکمه‌های «🖨 چاپ/PDF» و «⬇ اکسل» در مودال حساب — tester205 (۱۰/۰) |
| 9 | `UR-2026-08-01-09` | نیاز | گزارش کامل دورهٔ تنخواه: ردیف، تاریخ، مبلغ، توسط، شرح هزینه + جمع زندهٔ «هزینه‌های دوره / موجودی دوره / شارژ دوره» | `petty.js#ptfPettyPeriodData(month)` داده‌ها را دارد (`petty`, `tx`, `totalOut`, `charges`, `balance`)؛ نیاز به مودال گزارش ردیفی + دکمه در کنار هر دوره (`renderPeriods`) | ✅ **رفع شد** — `ptfPettyPeriodEvents` (ردیف/تاریخ/نوع/شرح/توسط/مبلغ مرتب) + `ptfPettyPeriodTotals` (جمع زنده: هزینه‌های دوره/شارژ/موجودی — هزینه‌های ابطال‌شده حذف) + مودال گزارش + CSV + چاپ/PDF + دکمه‌های «📊 گزارش دوره» (کنار هر دوره) و «📊 گزارش دورهٔ جاری» (تولبار) — tester208 (۱۰/۰) |

---

## ترتیب اجرا (طبق درخواست «به ترتیب»)

1. ✅ مورد ۱ — تولبار تنخواه فقط در تب تنخواه
2. ✅ مورد ۲ — رفع ازدست‌رفتن فوکوس جستجو (تامین‌کننده/مشتری)
3. ✅ موارد ۳ + ۸ — خروجی گردش حساب مشتری (PDF/اکسل/چاپ)
4. 🔲 مورد ۵ — (قبلاً رفع شد — راستی‌آزمایی استیجینگ)
5. ✅ مورد ۴ — برچسب «قیمت واحد» در ثبت خرید واقعی (هر دو پنجره تقسیم/گروهی)
6. ✅ مورد ۶ — جستجوی تامین‌کننده با نام/برند در استعلام
7. ✅ مورد ۷ — سورت‌بندی فهرست‌ها (کامپوننت مشترک + ۵ لیست)
8. ✅ مورد ۹ — گزارش کامل دورهٔ تنخواه با جمع‌های زنده (تستر208)

> 🎉 **تمام ۹ مورد بک‌لاگ کاربر (UR-01..UR-09) بسته شد.** (۵ مورد قبل + موارد ۲/۳+۸/۴/۶/۷/۹ در این نشست)

## قواعد
- هر مورد = کامیت مستقل + تستر UAT (الگوی tester200-202) + به‌روزرسانی هند‌آف.
- بعد از هر مورد: `build_sitemap.py` + `audit.py` (۰ خطا) — فقط در صورت تغییر فایل‌های public.
- موارد ۳/۸ و ۹ نیازمند تأیید قالب خروجی توسط کارفرما هنگام ارائهٔ پیش‌نمایش.

---

## UR-10 — PDF تلفیقی دورهٔ تنخواه + الزام پیوست بانک (۱۴۰۵/۰۸/۱۰ — درخواست جدید کارفرما)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `UR-2026-08-01-10` | نیاز | هنگام ارجاع دوره به حسابدار، گزارش + ضمائم + رسیدهای پرداخت در **یک PDF** به‌هم‌مرتبط با **شناسهٔ «سند N»** هر رسید (مطابق ردیف گزارش)؛ چیدمان فشردهٔ **۳-۴ رسید در هر صفحه** (کمترین صفحه، بدون افت خوانایی)؛ و **الزام پیوست صورتحساب بانک/گردش حساب قبل از ارجاع** | ✅ **رفع شد** — `ptfPettyPeriodCombinedPdf` (گزارش + صفحهٔ رسیدها با `petId` + پیوست بانک در صفحهٔ جدا)، `ptfPettyReceiptsHtml` (چیدمان ۳۳٪ = ۳-در-صفحه)، `ptfPettyPeriodFiles` (جمع‌آوری فایل‌های دوره با شناسه)، ذخیرهٔ `petId` هنگام آپلود رسید (pettyAdd)، دکمهٔ «📎 PDF تلفیقی» در renderPeriods، الزام پیوست بانک در `pettyClosePeriod` (مسدودسازی ارجاع بدون پیوست). تستر tester209 (۱۳/۰). |

> **وضعیت کلی:** بک‌لاگ ۹ موردی + UR-10 همگی بسته شدند. (S3 تاریخچهٔ ادغام کاتالوگ و S4/S5 یکپارچگی/PR و کارهای سئو در هند‌آف باقی‌اند.)

---

## UR-11 و UR-12 — درخواست‌های جدید ۱۴۰۵/۰۸/۱۰ (ثبت + پیاده‌سازی)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `UR-2026-08-01-11` | نیاز | گزارش تنخواه **بازه‌ای** (از آخرین ارجاع تا تاریخ انتخابی)؛ ماندگاری گزارش+منضمات هر دوره؛ قالب «تنخواه‌گردان از تاریخ … تا …» با نام ثبت‌کننده و نحوهٔ پرداخت (مستقیم/تسویه/در انتظار)؛ PDF منضمات در هر زمان | ✅ **رفع شد (v33.3.4)** — طراحی در `crm/DESIGN-PETTY-PERIOD-RANGE-v1.md`؛ پیاده‌سازی در petty.js (SuggestedRange/PeriodData/Events/RangeLabel/Report/Csv/Print/CombinedPdf با بازه + renderPeriods + pettyClosePeriod با از/تا + الزام پیوست آخرین دوره) — tester212 (۱۳/۰) |
| `UR-2026-08-01-12` | نیاز | ساده‌سازی مختومهٔ پرونده فروش (پکینگ‌لیست اجباری نباشد، تسویه تامین‌کننده…) | ✅ **رفع شد (v33.3.4)** — پکینگ‌لیست/تسویه تامین‌کننده از قبل فقط هشدار بودند؛ تنها blocker «تحویل» با **مسیر تأیید صریح** (تیک + دلیل → closeOverride) قابل عبور شد و در audit ثبت می‌شود؛ گارد مالی commit حفظ شد — tester214 (۷/۰) + tester110 (۳۶/۰) |

---

## ماژول مستقل چک — درخواست نشست قبلی (ثبت + معماری)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `CHQ-MOD-001` | معماری/نیاز | جداسازی چک‌ها به **ماژول مستقل مالی**: چک‌های صادره (issued) و وارده (received) — خارج از «چک شخصی»؛ پنل مستقل در هاب مالی + گزارش + وضعیت‌ها | 🟡 **پیش‌نویس معماری آماده** — `crm/DESIGN-CHEQUE-MODULE-v1.md` (مدل داده/مهاجرت نرم/UI/گزارش/گام‌ها/۵ سؤال) — **منتظر تأیید کارفرما**؛ پیاده‌سازی پس از تأیید |

> یادداشت: کارفرما «نتیجهٔ تست اصلاحات قبلی (UR-11/12) را بعداً اعلام می‌کند» — تا آن زمان هیچ تغییری در آن مسیرها نمی‌کنیم مگر با گزارش.

> **CHQ-MOD-001 پیشرفت:** معماری تأیید شد؛ گام‌های ۱–۳، ۵–۸ (v33.4.0) اجرا شد (cheque-module.js + cheque-panel.js + تب هاب مالی + sync/backup/api + tester215 24/0). باقی: وصول مشتری با چک → وارده (گام ۴) + یکپارچه‌سازی کامل.

---

## UR-13 و BUG-ARCHIVE — بازخورد ۱۴۰۵/۰۸/۱۰ (رفع شد v33.4.1)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `UR-2026-08-01-13` | باگ/نیاز | گزارش/ارجاع تنخواه: تقویم شمسی در فیلدهای تاریخ + پذیرش فرمت‌های رایج (1405/04/01 یا 1405-4-1) + **افزودن فایل PDF گردش حساب بانک در خود پنجرهٔ ارجاع (الزامی)** | ✅ **رفع شد (v33.4.2)** — ptfDialog حالا `datePicker` (تقویم شمسی) و `type:'upload'` (ویجت آپلود داخل دیالوگ) را پشتیبانی می‌کند؛ `ptfPettyNormDate` فرمت‌ها را نرمال می‌کند؛ پنجرهٔ ارجاع فیلد «📎 فایل PDF گردش حساب بانک (الزامی)» دارد و بدون فایل ارجاع نمی‌شود؛ فایل‌ها روی رکورد دوره (files) ذخیره و در PDF تلفیقی می‌آیند — tester212 (۲۰/۰)، tester209 (۱۳/۰) |
| `BUG-ARCHIVE` | باگ | بایگانی پرونده فقط «سند متمم» را نشان می‌داد؛ باید **تمام اسناد** (پیشنهاد اصلی/متمم/فاکتورها/نامه‌ها/استعلام‌ها/متفرقه) در بایگانی باشند | ✅ **ریشه‌کنی شد** — ریشه: `ptfSalesFileOffers` فقط wonOffer+st==='won' را برمی‌گشت؛ و `sfArchive` فقط wonOffer را در offerNo ذخیره می‌کرد؛ و `openProject` فقط p.offerNo را نمایش می‌داد. رفع: (۱) ptfSalesFileOffers → همهٔ offers همان inqNo + زنجیره (altOf/srcToNo/coNo)؛ (۲) sfArchive → `docSnap` (فهرست کامل offers/letters/invoices/supply/misc) + `offerNos`؛ (۳) openProject → بخش «📎 اسناد کامل پرونده (بایگانی)» با همهٔ اسناد (دکمهٔ دانلود هر پیشنهاد + فهرست فاکتورها/نامه‌ها/متفرقه) — tester216 (۱۰/۰) |

---

## BUG-RANGE — ریشه‌کنی «دوره انتخاب می‌شود اما قالب تنخواه‌گردان خالی است» (v33.4.3)

**ریشه (تأیید با شبیه‌سازی):** فیلتر بازه فقط `t` شمسی (`1405/04/15 09:00`) را می‌خواند؛ رکوردهایی که (۱) فقط `month` داشتند (بی‌تاریخ) یا (۲) `t` میلادی (`2026-07-06`) داشتند، از بازه حذف می‌شدند → با دادهٔ واقعی، «هیچ اطلاعاتی در قالب تنخواه‌گردان».

**رفع (v33.4.3):**
- `recDate` مقاوم: شمسی → میلادی (تبدیل با ptfISOToJ) → fallback ماه (اول ماه).
- فیلتر بازه: رکورد دارای تاریخ → فقط اگر تاریخ در بازه؛ رکورد بی‌تاریخ → فقط اگر ماهش در محدودهٔ بازه (هیچ داده‌ای گم نمی‌شود).
- مقدار پیشنهادی تقویم (sg.from/to) حالا در فیلدهای datePicker نمایش داده می‌شود (قبلاً خالی بود).
- پیام راهنما وقتی بازه خالی است: «در بازهٔ انتخابی رکوردی نیست — N رکورد تنخواه در سیستم هست؛ بازه را گسترش دهید».

**تست:** tester212 (۲۲/۰) — سناریوی ۳ نوع رکورد (شمسی/میلادی/بی‌تاریخ) + خارج بازه؛ رگرسیون ۲۰۸/۲۰۹ سبز؛ audit PASS.

---

## BUG-DATE-NAV — خطای فرمت تاریخ + جابه‌جایی تقویم (v33.4.4)

- **خطای فرمت:** ارقام فارسی/عربی (`۱۴۰۵/۰۴/۱۵`) در تاریخ‌ها رد می‌شدند → `ptfPettyNormDate` حالا ارقام فارسی/عربی را به لاتین تبدیل می‌کند (در گزارش دلخواه و ارجاع).
- **جابه‌جایی تقویم:** ناوبری ماه/سال با `this.closest('[id$=_cal]')` در برخی محیط‌ها fail می‌شد → دکمه‌های ◀▶ حالا با `ptfCalNav(inputId, jy, jm)` (id مستقیم) کار می‌کنند؛ `ptfCalShow` هم مقدار فارسی input را نرمال می‌کند.
- تست: tester212 (۲۵/۰) — نرمال‌سازی فارسی/عربی + onOk بدون خطا با تاریخ فارسی؛ رگرسیون ۲۰۸/۲۰۹/۱۱۰/۲۱۴/۲۱۵/۲۱۶ سبز؛ audit PASS.

---

## BUG-PDF-ATTACH — گزارش تلفیقی ضمایم را نمایش نمی‌داد (v33.4.5)

**ریشه:** `ptfPettyReceiptsHtml` فقط `<img>` می‌ساخت و `f.url` هرگز از storage گرفته نمی‌شد (هیچ presign_get) → تصویر خالی → پیام «فرمت غیرمجاز» برای همهٔ ضمائم.

**رفع (v33.4.5):**
- `ptfPettyFileKind(name)`: تشخیص نوع فایل (عکس jpg/png/gif/webp / PDF / سایر).
- `ptfPettyResolveUrl(f)`: گرفتن URL واقعی هر فایل از storage (presign_get) — مثل openStoredFile؛ روی window هم قرار گرفت.
- `ptfPettyReceiptHtml(f)`: عکس → `<img>`؛ PDF → `<embed type=application/pdf>` (قابل مشاهده در PDF)؛ سایر → پیام «این فرمت در گزارش تلفیقی نمایش داده نمی‌شود».
- `ptfPettyPeriodCombinedPdf`: ابتدا URL همهٔ ضمائم (رسیدها + پیوست بانک) resolve می‌شود (Promise.all) سپس HTML ساخته و چاپ می‌شود.
- CSS: استایل `.rcpt embed`.

**تست:** tester209 (۱۷/۰) — تشخیص نوع + رندر img/embed + resolve؛ tester212 (۲۳/۰)؛ رگرسیون ۲۰۸ سبز؛ audit PASS؛ zip بازسازی.

---

## BUG-TOTALS + BUG-ATTACH-URL — ردیف‌های جمع + لودنشدن ۲ سند (v33.4.6)

- **ردیف‌های جمع در پایان جدول (گزارش مودال/PDF/CSV):** «مجموع هزینه‌های دوره»، «مجموع شارژ دوره»، «موجودی شروع دوره» (از تراکنش‌های قبل از بازه)، «موجودی پایان دوره» — `ptfPettyPeriodTotals` حالا `balanceStart/balanceEnd` دارد و `ptfPettyTotalsRowsHtml` در پایان تbody همهٔ خروجی‌ها اضافه شد.
- **لودنشدن ۲ سند (ریشه):** `ptfPettyPeriodFiles` فایل جدید با `url:''` هاردکد می‌ساخت — حتی فایل‌های دارای url هم در گزارش تلفیقی «لینک دریافت نشد» می‌شدند. رفع: `url: f.url || ''` (حفظ url قبلی).
- **UX مقاوم:** اگر url دریافت نشد → پیام واضح + دکمهٔ «↗ باز کردن فایل»؛ رندر PDF با `<embed>`.
- تست: tester208 (۱۲/۰) — ردیف‌های جمع + balanceStart/End؛ tester209 (۱۷/۰)؛ tester212 (۲۳/۰)؛ audit PASS؛ zip بازسازی.

---

## BUG-PDF-HEIC + چیدمان + تقویم (v33.4.7)

- **تبدیل PDF/HEIC → JPEG در گزارش تلفیقی:** endpoint جدید `api/attachment-thumb.php` (Imagick سمت سرور: خواندن از S3 → تبدیل PDF (تا ۸ صفحه) یا HEIC/HEIF به JPEG → آپلود thumbnail → لینک presign). کلاینت: `ptfPettyFileKind` HEIC را تشخیص می‌دهد؛ `ptfPettyToJpeg` در CombinedPdf قبل از رندر برای pdf/heic فراخوانی می‌شود؛ صفحه‌های بیشتر PDF → کارت‌های جدا (extraImages). اگر سرور Imagick نداشت → پیام واضح + «باز کردن فایل».
- **چیدمان فشرده:** CSS از flex به `grid-template-columns:repeat(3,1fr)` تغییر کرد (۳ کارت در هر ردیف ثابت) + ارتفاع کمتر (img max-height 300px، embed 200px، margin صفحه 10mm) — «قاعده فشردگی» برقرار.
- **تقویم:** دکمه‌های ◀▶ حالا `data-cal-nav` + **delegation** روی باکس تقویم (اگر onclick inline fail شود، کلیک همچنان کار می‌کند) + padding بیشتر برای کلیک راحت‌تر.
- تست: tester209 (۲۱/۰) — HEIC تشخیص + رندر بعد از تبدیل + extraImages + فراخوانی endpoint؛ رگرسیون ۲۰۸/۲۱۲/۱۱۰/۲۱۴/۲۱۵/۲۱۶ سبز؛ audit PASS؛ zip بازسازی.

---

## CHQ-MOD-001 تکمیلی — یکپارچه‌سازی یادآور/قفل سال مالی/data-quality (v33.4.8)

- **یادآور چک:** `chUpsertReminder` حالا چک‌های **وارده** (direction=received) را هم با عنوان «سررسید چک وارده» پوشش می‌دهد (قبلاً فقط صادره).
- **پنل چک:** بعد از ثبت چک (دستی) یادآور صدا زده می‌شود؛ عملیات وصول/انتقال چک گارد **قفل سال مالی** دارند (اگر سال سررسید قفل باشد مسدود).
- **data-quality:** چک‌ها از `ptfChequeAll` (هر دو کلید issued/received + legacy) خوانده می‌شوند — «چک بی‌مالکیت/بدون تاریخ» روی همهٔ چک‌ها اعمال می‌شود.
- تست: tester215 (۳۱/۰) — چک‌های یکپارچه‌سازی؛ رگرسیون tester145 (۱۱/۰)، tester2 (۶۹/۰)، tester208 (۱۲/۰) سبز؛ audit PASS؛ zip بازسازی.

---

## ⚠️ موارد باز برای ایجنت‌های بعدی (ثبت از بازخورد کارفرما ۱۴۰۵/۰۸/۱۰)

| شناسه | شدت | عنوان | وضعیت |
|---|---|---|---|
| `OPEN-CAL-NAV` | 🟠 زیاد | **تقویم همچنان کار نمی‌کند** (فلش‌های ◀▶ جابه‌جا نمی‌شوند و فقط تاریخ لاتین قبول می‌شود) — با وجود رفع قبلی (ptfCalNav + delegation + نرمال‌سازی فارسی). کارفرما تأیید کرده که «تقویم درست نشد». **نیازمند بررسی عمیق‌تر روی استیجینگ واقعی** (شاید مشکل از کش مرورگر کاربر، یا تداخل با دیالوگ/مولتی‌مودال، یا اجرا نشدن datex در بافت ptfDialog). اقدام بعدی: بازتولید مستقیم روی staging با ابزارهای مرورگر + بررسی اینکه آیا `ptfCalNav` واقعاً در دسترس است. | 🔴 باز |
| `OPEN-PDF-CONVERT` | 🟠 زیاد | **تبدیل PDF به عکس ممکن نشد** — با وجود `api/attachment-thumb.php` (Imagick). احتمالاً سرور/هاست **Imagick ندارد** یا مسیر فایل خوانده نمی‌شود. اقدام بعدی: بررسی روی هاست (php -m | grep imagick) یا استفاده از **Ghostscript/PDF.js سمت کلاینت** یا مسیر جایگزین (مثلاً تبدیل سمت کلاینت با کتابخانه‌ها). | 🔴 باز |

> این دو مورد باید قبل از «بستن کامل گزارش تلفیقی/تقویم» رسیدگی شوند. بقیهٔ موارد بسته‌اند.

---

## CHQ-MOD-001 — اتصال چک به گردش حساب (v33.4.9)

- **گردش حساب تامین‌کننده:** چک‌های صادرهٔ ماژول (issued) برای همان تامین‌کننده که در گردش‌اند (open/transferred) و payment لینک‌شده ندارند → ردیف «چک صادره (در گردش)» (بستانکار)؛ و `ptfChequeFind` برای چک payment‌های لینک‌شده استفاده می‌شود (هر دو کلید).
- **گردش حساب مشتری:** چک‌های وارده (received) از آن مشتری که در گردش‌اند (open/held/endorsed) → ردیف «چک وارده (در گردش)» (بدون اثر در مانده تا وصول/برگشتی).
- تست: tester215 (۳۳/۰) — اتصال به هر دو گردش؛ رگرسیون tester145/tester200/tester205/tester2 سبز؛ audit PASS؛ zip بازسازی.

---

## CHQ-V2 — دستیار هوشمند + اثر مالی + چاپ برگه + انتقال از ماژول شخصی (v33.5.0)

مصوب کارفرما: اثر مالی به محض ثبت؛ چاپ برگه چک فیزیکی؛ OCR با کلید AI موجود؛ چک صادره به حساب تامین‌کننده متصل.

- **دستیار هوشمند چک (🤖):** دکمه در پنل چک (زیرتب صادره/وارده) → `ptfChAiOpen` → آپلود عکس/PDF چک یا متن → `llm.php?action=cheque` (OCR با vision) → پیش‌نمایش قابل اصلاح (صیادی/مبلغ/تاریخ/بانک/ذینفع) → **انتخاب مشتری/تامین‌کننده از فهرست** (برای ذینفع ناقص) → فاکتور باز (برای وارده) → **کپی چک ضمیمه می‌شود (اختیاری)** → ثبت در ماژول جدید (issued/received).
- **اثر مالی (به محض ثبت):** چک وارده با فاکتور → payment روی فاکتور مشتری (کسر مطالبات)؛ چک صادره با تامین‌کننده → payment در supplier-finance (کسر بدهی)؛ **برگشتی وارده → حذف payment (بازگشت مطالبات)**؛ **ابطال صادره → void payment (بازگشت بدهی)**.
- **چاپ برگه چک:** `ptfChequePrint` — برگهٔ چک با صیادی/مبلغ به حروف (`ptfNumToFaWords`)/تاریخ/ذینفع/بانک/امضا؛ دکمه «🖨 چاپ برگه» برای چک‌های صادره.
- **انتقال از ماژول شخصی:** ثبت چک از پنل شخصی حذف شد (`chNew`/`chEdit` → پیام «ثبت/مدیریت فقط از هاب مالی → تب چک‌ها»)؛ پنل شخصی فقط **چاپ** (چاپ صیادی با کالیبره از قبل موجود) می‌ماند.
- تست: tester215 (۴۱/۰) — اثر مالی (invoice/supplier + معکوس)، دستیار (دکمه/فیلدها/OCR)، چاپ/مبلغ به حروف، حذف ثبت شخصی؛ رگرسیون tester145/200/205/2/110 سبز؛ audit PASS؛ zip بازسازی.

---

## CHQ-PRINT — ماژول مستقل «چاپ چک فیزیکی» + حذف کامل ثبت چک از شخصی (v33.6.0)

**درخواست کارفرما (۱۴۰۵/۰۸/۱۱):** «هنوز قسمت ثبت چک در بخش شخصی وجود دارد… یک ماژول مستقل برای چاپ فیزیکی چک در زیر شاخه کالا و اسناد ایجاد شود. ثبت تکی نداریم یا ثبت چندتای، فقط چاپ تکی و چاپ چندتایی. هیچ رکوردی نیاز به ذخیره‌سازی ندارد فقط مختص چاپ است. نیاز به ورود کد صیادی و چاپ آن روی چک نیست… تاریخ باید به حروف هم چاپ شود. نام ذینفع و شناسه ملی آن هم چاپ می‌شود و مبلغ هم به حروف و هم به عدد. سایز حروف و اعداد هر بخش قابل تنظیم + مختصات + فونت‌های چاپ فارسی (نستعلیق/بی‌نازنین/بی‌یاقوت). رنگ مبلغ بالای چک قرمز. تمام اصلاح با حذف کامل ثبت چک و انتقال به ماژول مستقل.»

**تصمیمات دوگانهٔ کارفرما (با ask_user):**
1. حذف فقط از **بخش شخصی**؛ هاب مالی (ثبت + اثر مالی + دستیار AI + لیست) دست‌نخورده بماند.
2. دکمهٔ «چاپ برگه» از هاب مالی حذف شود؛ چاپ فقط از ماژول جدید.

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `CHQ-PRINT` | نیاز | ماژول مستقل «🖨 چاپ چک فیزیکی» زیر «کالا و اسناد» — فقط چاپ (تکی/چندتایی)، بدون ذخیره‌سازی، بدون کد صیادی، تاریخ به حروف، ذی‌نفع + کد/شناسه ملی، مبلغ عدد (قرمز بالا) + حروف، فونت/اندازه/مختصات قابل تنظیم هر بخش | ✅ **رفع شد (v33.6.0)** — `crm/cheque-print.js` + پنل `chqprint` + tester287 (۳۰/۰) |
| `CHQ-PRINT-REMOVE` | باگ | باکس «چک‌های صادره» (ثبت تکی/دسته‌ای/چاپ/دستیار) هنوز در پنل شخصی (یادآورها) دیده می‌شود | ✅ **رفع شد (v33.6.0)** — `chBoxHtml`/`hookReminders`/`chqBox` حذف کامل؛ یادآورهای سررسید حفظ شد |
| `CHQ-PRINT-HUB` | نیاز | چاپ برگه از هاب مالی حذف شود (فقط ماژول جدید) | ✅ **رفع شد (v33.6.0)** — `ptfChequePrint` و دکمه «🖨 چاپ برگه» از cheque-panel.js حذف؛ `ptfNumToFaWords` به ماژول چاپ منتقل + رفع کرش مبالغ تریلیونی |
| `CHQ-PRINT-FIX` | باگ | `ptfNumToFaWords` برای مبالغ ≥ ۱۰۰۰ میلیارد کرش می‌کرد (گروه‌های نامحدود) | ✅ **رفع شد (v33.6.0)** — توسعهٔ گروه‌ها تا «هزار میلیارد/میلیون میلیارد» + سقف حلقه |

> نکته برای راستی‌آزمایی استیجینگ: اگر پنل جدید دیده نشد، کش مرورگر را با `Ctrl+Shift+R` یا `crm/clear-cache.html` پاک کنید (نسخه باید v33.6.0 باشد).

---

## v33.7.0 — ذی‌نفع شرطی چک + اثر مالی + ضمانت پرونده + چاپ گرافیکی + باگ حقوق سهامدار (۱۴۰۵/۰۸/۱۱)

**درخواست کارفرما:** «ذینفع بین مشتریان/تامین‌کنندگان/سایر انتخاب شود؛ تامین‌کننده = دارای مطالبات از ما؛ مشتری = دارای پرونده فروش باز؛ چک مالی بلافاصله روی گردش حساب اثر بگذارد؛ نوع چک: مالی/ضمانت پیش‌پرداخت/حسن انجام/...؛ ضمانت اثر مالی ندارد ولی در پرونده می‌نشیند و با پایان پروژه مسترد می‌شود؛ چاپ فیزیکی: حالت گرافیکی برای جابه‌جایی نوشته‌ها + مبلغ دوم به عدد پایین-چپ (هر چک دو مبلغ دارد)؛ **باگ: حقوق سهامداران ماهی ۱۳۰ میلیون ولی ۹۰ میلیون محاسبه شده**؛ حقوق/اجاره/تکرارشونده‌ها با شروع ماه جدید خودکار اعمال شوند.»

**تصمیمات کارفرما (ask_user):** Q1: فاکتور باز مشتری اختیاری (چک در گردش → با وصول اثر مالی). Q2: لینک پرونده برای ضمانت الزامی + عبور مختومه با تأیید صریح. Q3: حقوق سهامدار + قالب‌ها هر دو خودکار.

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `CHQ-PARTY` | نیاز | ذی‌نفع شرطی در ثبت چک (دستی + AI): تامین‌کننده دارای مطالبه (با مبلغ بدهی) / مشتری دارای پرونده باز / سایر | ✅ **رفع شد (v33.7.0)** — ptfChequeSupOptions/CustOptions/OpenInvoicesOf + فرم دستی + AI |
| `CHQ-FIN-ISSUED` | نیاز | چک مالی صادره → اثر همان لحظه روی بدهی تامین‌کننده | ✅ **رفع شد** — payment در supplier-finance (از قبل) + گارد ضمانت + باگ بازگشتی faDateL رفع |
| `CHQ-FIN-RECEIVED` | نیاز | چک مالی وارده با فاکتور → کسر مطالبات همان لحظه؛ بدون فاکتور → در گردش و با وصول اثر مالی | ✅ **رفع شد** — ptfChequeCollect اثر مالی می‌زند |
| `CHQ-GUAR` | نیاز | ضمانت: بدون اثر مالی + لینک پرونده الزامی + استرداد با پایان پروژه + گارد مختومه (با عبور صریح) | ✅ **رفع شد** — guarType ها + dealCd الزامی + دکمه «🏆 استرداد ضمانت» + sfCloseAudit blocker/override + نمایش در projects.js |
| `CHQ-PRINT-GV` | نیاز | حالت گرافیکی (درگ‌اند‌دراپ) در تنظیمات چیدمان چاپ چک | ✅ **رفع شد** — chqGvRender/Start/Move (کشیدن فیلدها روی برگه با خطوط راهنما) |
| `CHQ-PRINT-AMT2` | نیاز | مبلغ دوم به عدد در پایین-چپ برگه (مبلغ اصلی؛ قرمز بالا فقط محکم‌کاری) | ✅ **رفع شد** — amt2 با مختصات/اندازه/فونت/رنگ مستقل |
| `BUG-SH-SALARY` | باگ | حقوق سهامداران ۱۳۰ میلیون ولی ۹۰ محاسبه می‌شود | ✅ **ریشه‌کنی شد** — ریشه: `ptfShareEdit` برای سهامدار جدید syncSalaryTxForMonth را صدا نمی‌زد → حقوقش ثبت نمی‌شد؛ رفع با `ensureSalaryTxForMonth` (ایجاد/آپدیت/حذف) + فراخوانی برای جدید/تغییر/غیرموظف |
| `AUTO-RECURRING` | نیاز | حقوق + اجاره + تکرارشونده‌ها با شروع ماه جدید خودکار اعمال شوند (بدون دخالت کاربر) | ✅ **رفع شد (v33.7.0)** — ptfAutoApplyRecurring در بوت (یک‌بار در ماه + audit + toast) |

---

## v33.8.0 — بازخورد کارفرما: گرافیک inline + اسکن پس‌زمینه + انتخاب وارده/صادره + باگ پرونده باز + چک ثالث (۱۴۰۵/۰۸/۱۱)

**بازخورد:** ① کادر تنظیم گرافیکی کوچک/ناکارآمد است → باید اندازه واقعی چک باشد و در همان صفحهٔ اصلی ماژول (بدون مودال) تنظیم شود + امکان اسکن چک واقعی و قرار دادن در پس‌زمینه. ② در ثبت چک اول باید بپرسد وارده/صادره. ③ برای چک مشتری هیچ پرونده بازی نشان نمی‌دهد (با وجود چند پرونده باز). ④ باید چک ثالث هم خرج و ثبت شود.

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `CHQ-GV-INLINE` | نیاز | حالت گرافیکی چیدمان در صفحهٔ اصلی ماژول با اندازهٔ واقعی چک (بدون مودال) + زوم | ✅ **رفع شد (v33.8.0)** — سکشن «🖱 حالت گرافیکی چیدمان» بالای صفحهٔ چاپ چک فیزیکی؛ مقیاس ۶px/mm (≈۱۰۱۴px) + دکمه‌های ➖/➕ |
| `CHQ-GV-BG` | نیاز | اسکن برگهٔ چک واقعی → پس‌زمینه و جانمایی مقادیر روی آن | ✅ **رفع شد** — chqBgUpload (canvas فشرده + ذخیره) + chqBgClear؛ مودال فقط عددی شد |
| `CHQ-DIR-ASK` | نیاز | هنگام «ثبت چک» اول بپرسد وارده است یا صادره | ✅ **رفع شد** — دیالوگ انتخاب «📥 وارده / 🏢 صادره» قبل از فرم؛ دستیار AI هم سِلکت نوع دارد (با حفظ داده) |
| `CHQ-CUST-OPEN-BUG` | باگ | برای چک مشتری هیچ پرونده بازی نشان نمی‌دهد | ✅ **ریشه‌کنی شد** — ریشه: deals فقط buyerCo دارند (بدون buyerCd) و فیلتر با d.buyerCd همیشه خالی بود → تطبیق نام (الگوی ptfOfferMatchCust) |
| `CHQ-THIRD` | نیاز | ثبت و خرج چک ثالث | ✅ **رفع شد** — نوع ذی‌نفع «🪪 ثالث» (دستی + AI) + پنجرهٔ «خرج کردن چک به تامین‌کننده» با لیست دارای مطالبه و اثر مالی فوری (ptfChequeEndorse+supCd) |

---

## v33.8.1 — رفع جهت درگ گرافیکی + جانمایی فیلدها (بازخورد کارفرما)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `CHQ-GV-DIR` | باگ | درگ خلاف جهت موس است (چپ→راست و بالعکس) + برخی مقادیر در کادر گرافیکی دیده نمی‌شوند | ✅ **ریشه‌کنی شد (v33.8.1)** — ریشه: `chqGvSync` برای فیلدهای راست‌چسب `(pageW − right)` محاسبه می‌کرد → فیلدها به گوشهٔ چپ پرتاب و جهت درگ معکوس. رفع: `right = فاصله از لبهٔ راست` + درگ هم‌جهت (right−dx / left+dx) + words با عرض left→right و جابه‌جایی هر دو لبه. tester288 (۳۸/۰) |

---

## v33.9.0 — تحلیل سود/تقسیم + مینیمال درخواست‌ها + پوشش فیلدهای مبلغ (۱۴۰۵/۰۸/۱۱)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `PROFIT-REVIEW` | تحلیل | بررسی منطق محاسبه سود و تقسیم سهامداران: چک در راه / بدهی باز / مطالبات باز + استخراج باگ + پیشنهادات | ✅ **تحلیل کامل شد** — `crm/ANALYSIS-PROFIT-DIVIDEND-2026-08-02.md`: پاسخ ۳ پرسش + ۷ باگ (P-1..P-7) + ۶ پیشنهاد. **منتظر تصمیم کارفرما برای اجرا** |
| `UI-MINIMAL-RFQ` | نیاز | گزینه‌های مقابل درخواست‌ها مثل پیشنهادات مینیمال شوند | ✅ **رفع شد (v33.9.0)** — دکمه‌های آیکونی ۳۲px با tooltip (👁️ ✏️ 📨) در جدول درخواست‌ها |
| `MONEY-FIELDS` | نیاز | همهٔ فیلدهای مبلغ: جداکننده + مبلغ به حروف زیر فیلد + پذیرش ارقام فارسی/انگلیسی + تبدیل دوجهته در قالب انگلیسی | ✅ **رفع شد (v33.9.0)** — ptfEnDigits/ptfFaDigits + moneyLike + focusout auto-format (با خروج امن نرخ/درصد/تعداد و opt-out) + تأیید data-money در فرم‌های چک |

**باگ‌های سود استخراج‌شده (برای اجرا پس از تأیید):**
- `BUG-P-1` ناسازگاری مبنای درآمد (ریالی=تعهدی / ارزی=نقدی)
- `BUG-P-2` تقسیم بدون سقف نقدینگی (مطالبات باز در سود)
- `BUG-P-3` چک وارده در راه در درآمد ارزی به‌عنوان وصول
- `BUG-P-4` بدهی باز تأمین‌کننده خارج از buycmp کسر نمی‌شود
- `BUG-P-5` چک در راه بدون فاکتور در ترازنامه گم می‌شود
- `BUG-P-6` تقسیم سود فقط کاربرگ است (بدون ثبت واقعی در دفتر سهامداران)
- `BUG-P-7` چک صادره (تعهد) در تقسیم لحاظ نمی‌شود

---

## v33.10.0 — منطق نقدی سود و تقسیم (مصوب کارفرما) — CASH-PROFIT

**تصمیم کارفرما:** «فروش‌ها منبع درآمدند نه مبنای واقعی؛ درآمد واقعی = وصول مطالبات. هزینه‌ها از درآمد کسر شوند؛ خریدها نقدی و غیرنقدی (شامل چک‌ها — حتی چک بابت مسائل دیگر) کسر شوند. کف نقدینگی در گردش باید باشد؛ بیش از آن با درصد توافقی تقسیم و مابقی (۴۰٪) به کف اضافه شود.»

**تصمیمات تکمیلی (ask_user):** Q1: مبنای مازاد = موجودی نقد پایان − کف. Q2: چک وارده درآمد در لحظه وصول. Q3: بدون دوباره‌شماری. Q4: کف = عدد دستی.

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `CASH-PROFIT` | نیاز | موتور سود نقدی: درآمد = وصولی (نقد + چک وصول‌شده)؛ خروجی = فاکتور خرید + پرداخت بدون تخصیص + چک صادرهٔ مستقل + opex + تنخواه (بدون دوباره‌شماری)؛ موجودی نقد پایان = افتتاحیه + سود نقدی | ✅ **رفع شد (v33.10.0)** — `ptfFiscalCashData` در fiscal.js + تستر ۲۹۰ (۱۵/۰) |
| `CASH-FLOOR` | نیاز | کف نقدینگی در گردش (عدد دستی) + تقسیم فقط از مازاد بر کف + بازگشت باقی به کف (٪) + هشدار زیر کف | ✅ **رفع شد** — `ptfFiscalCashFloor(Set/Open)` + `ptfFiscalCashDistribution` + بلوک UI |
| `CASH-DIVIDEND` | نیاز | ثبت واقعی تقسیم در دفاتر سهامداران (type:profit) + قفل سال + یک‌بار در سال + audit | ✅ **رفع شد** — `ptfFiscalDividendApply` (BUG-P-6 بسته شد) |
| `CHQ-RECEIVED-CASH` | نیاز | چک وارده: اثر مالی فقط هنگام وصول (نه ثبت)؛ برگشتی پس از وصول مسدود | ✅ **رفع شد** — cheque-module.js (BUG-P-3 بسته شد) |
| `BUG-P-2` | باگ | تقسیم بدون سقف نقدینگی | ✅ **رفع شد** — سقف = کف نقدینگی |
| `BUG-P-7` | باگ | چک صادره (مسائل دیگر) در تقسیم لحاظ نمی‌شد | ✅ **رفع شد** — independentCheques در خروجی |
| `BUG-P-4/5` | باگ | بدهی خدماتی غایب / چک در راه گم‌شده | 🟡 **بخشی** — فاکتورهای تأمین همه لحاظ شدند؛ چک در راه کارت نمایش دارد |

---

## v33.11.0 — حذف جدول تعهدی قدیمی + رفع به‌هم‌ریختگی هاب مالی (بازخورد کارفرما)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `FISCAL-DASH-CLEAN` | باگ | جدول قبلی (اعداد تعهدی اشتباه) هنوز زیر جدول فعلی در داشبورد سال مالی بود | ✅ **رفع شد (v33.11.0)** — KPIهای تعهدی + «کاربرگ تقسیم سود» قدیمی حذف؛ فقط بلوک نقدی + هشدارها + سند اصلاحی؛ گزارش رسمی/CSV هم نقدی شدند (ptfFiscalCashReportHtml) |
| `HUB-MESS` | باگ | آشفتگی هاب مالی: «چک‌های در راه و جزئیات تامین‌کنندگان» در همهٔ تب‌ها؛ «هاب مالی وسط صفحه» | ✅ **ریشه‌کنی شد** — ریشه: باکس slLiquidity در هیچ تبی مخفی نمی‌شد + opexBox قبل از نوار هاب چیده می‌شد. رفع: slLiquidity فقط در تب supacc + finHubOrder (نوار هاب اول + ترتیب همه باکس‌ها) + فراخوانی در apply/render. tester291 (۱۲/۰) |

---

## v33.12.0 — ریشه‌کنی اعتبار مشتری + برنامه‌ریزی مالیات فقط در تب سال مالی + دارک‌مود (بازخورد کارفرما)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `BUG-CREDIT` | باگ | اعتبار مشتری با آپدیت‌های اخیر از بین رفته؛ در حالی که کالاهای برگشتی در موجودی هست | ✅ **ریشه‌کنی شد (v33.12.0)** — ریشه: تطبیق مرجوعی↔فاکتور فقط با `invoiceCd` بود؛ مرجوعی‌های قدیمی بدون invoiceCd (فقط offerNo/invoiceNo) هرگز پیدا نمی‌شدند → returnedAmount=0 → اعتبار=0. رفع: تطبیق مقاوم (invoiceCd → invoiceNo → offerNo یکتا، بدون دوباره‌شماری) + `cfRepairReturnLinks` (ترمیم خودکار در بوت + audit) + `cfCreditAudit` (گزارش اجزا). tester292 (۹/۰) |
| `HUB-TAXPLANNER` | باگ | «داشبورد برنامه‌ریزی فصلی مالیات» در تمام تب‌های هاب مالی دیده می‌شود | ✅ **رفع شد** — ptfTaxPlannerBox فقط در تب «سال مالی» + در finHubOrder |
| `DARK-SL` | باگ | «تعهدات نقدینگی تامین و چک‌های شرکت» پس‌زمینهٔ روشن دارد و در نمای شب دیده نمی‌شود | ✅ **رفع شد** — دارک‌مود body.ptf-dark برای slLiquidity و ptfTaxPlannerBox |

---

## v33.13.0 — 🚨 فوری: پر شدن حافظهٔ مرورگر در بکاپ/بازگردانی + عدم اتصال سرور

**مصوبهٔ کارفرما:** «مورد فوری پر شدن حافظه مرورگرها در قسمت بک آپ و بازگردانی و عدم اتصال آن ها به سرور — برنامه‌ریزی دقیق با قید فوریت.»

**برنامه:** `crm/PLAN-BACKUP-STORAGE-URGENT-2026-08-02.md` (ریشه R1..R6 + فاز ۰/۱/۲ + DoD).

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `BACKUP-URGENT-F0` | 🔴 فوری | فاز ۰ HOTFIX: fallback آفلاین فقط IDB (بدون localStorage حجیم)؛ restore با گارد ظرفیت + گزارش ناموفق؛ refresh توکن در 401؛ timeout ۲۰ثانیه؛ حذف storage_queue؛ محدودیت audit/notifs | ✅ **رفع شد (v33.13.0)** — backup.js + tester293 (۱۵/۰) |
| `BACKUP-URGENT-F1` | 🟠 فوری | فاز ۱: دکمهٔ «🔌 بررسی اتصال سرور» + بکاپ فقط در صورت تغییر + مهاجرت avatars به IDB | 🔲 **برنامه‌ریزی‌شده** (نشست بعد) |
| `BACKUP-URGENT-F2` | 🟡 | فاز ۲: بکاپ دلتا + ذخیرهٔ MySQL + بازگردانی تراکنشی | 🔲 برنامه‌ریزی‌شده (هم‌راستا با مهاجرت MySQL) |

---

## v33.14.0 — ریشهٔ «پاک‌سازی امن/مهاجرت حجم را کم نمی‌کند» + رفع (فوری)

**پاسخ ریشه‌یابی (پرسش کارفرما):**
- معماری داده روی localStorage (کلیدهای اصلی کسب‌وکار) + پاک‌سازی امن فقط کلیدهای حاشیه (notifs/sendqueue/audit/web_events/chat/ai) را هدف می‌گرفت؛ کلیدهای اصلی عمداً حذف نمی‌شوند → وقتی دادهٔ اصلی بزرگ است، آزادسازی ناچیز.
- مهاجرت به IDB هم فقط همان کلیدهای حاشیه را جابه‌جا می‌کرد (کلیدهای اصلی به دلیل وابستگی sync/backup به localStorage دست‌نخورده).
- بزرگ‌ترین عوامل پنهان: `ptf_crm_avatars` (عکس base64)، `ptf_storage_queue` (صف فایل — انجام‌شده‌ها هرگز پاک نمی‌شدند)، audit/notifs بدون هرس سنی، فایل‌های base64 داخل کلیدهای اصلی.

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `STORAGE-CLEAN-EFFECTIVE` | 🔴 فوری | پاک‌سازی امن/مهاجرت باید حجم واقعی آزاد کند | ✅ **رفع شد (v33.14.0)** — compactAvatars (۲۰ آخر + آرشیو IDB)، compactStorageQueue (انجام‌شده/۷روز)، هرس سنی audit(۱۸۰روز)/notifs(۹۰روز)/sendqueue(۳۰روز)، isVolatileArchiveCandidate گسترده + راهنمای صریح UI (علت + کلیدهای بزرگ). tester294 (۸/۰) |

---

## v33.15.0 — فاز ۱ بکاپ (انتخاب کارفرما: «بزن بریم گام بعد»)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `BACKUP-F1-CONN` | نیاز | دکمهٔ «🔌 بررسی اتصال سرور» + نمایش وضعیت (آنلاین/توکن منقضی/آفلاین) | ✅ **رفع شد (v33.15.0)** — ptfBackupServerStatus/ServerCheck + ناحیهٔ ptfBackupConn |
| `BACKUP-F1-SMART` | نیاز | بکاپ خودکار فقط در صورت تغییر (حذف ارسال ساعتی بی‌شرط) | ✅ **رفع شد** — backupSignature (hash محتوای کامل — رفع باگ «طول یکسان») + ptfBackupMaybeAuto |
| `BACKUP-F1-AUTO` | نیاز | فشرده‌سازی خودکار روزانه وقتی حافظه ≥ ۸۰٪ | ✅ **رفع شد** — ptfStorageAutoTame + flag روزانه |
| `BACKUP-F2` | 🟡 | بکاپ دلتا + ذخیرهٔ MySQL + بازگردانی تراکنشی | 🔲 برنامه‌ریزی‌شده (هم‌راستا با مهاجرت MySQL) |

---

## v33.16.0 — فاز ۲ بکاپ (بکاپ دلتا + بازگردانی تراکنشی)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `BACKUP-F2-DELTA` | نیاز | بکاپ دلتا: فقط کلیدهای تغییرکرده (امضای per-key) + سرور ادغام با آخرین بکاپ کامل + fallback needFull | ✅ **رفع شد (v33.16.0)** — client: hashString/deltaSigs/Collect/pushBackupDelta؛ سرور: `save_backup_delta` + `ptf_rotate_backup` مشترک. tester296 |
| `BACKUP-F2-TX` | نیاز | بازگردانی تراکنشی (تمام‌یا-هیچ) با rollback خودکار | ✅ **رفع شد** — doRestore: snapshot قبلی → در صورت هر کلید ناموفق، rollback کامل + audit RESTORE-FAIL + پیام (بدون reload) |
| `BACKUP-F2-MYSQL` | 🟡 | ذخیرهٔ بکاپ در MySQL | 🔲 پس از مهاجرت DB-MIG-001 (ساختار دلتا آماده است) |

---

## v33.17.0 — شروع مهاجرت MySQL (DB-MIG-001 فاز A) — آماده‌سازی کامل + راهنمای فارسی سی‌پنل

**تصمیم کارفرما:** مهاجرت آغاز شود با: کمترین کار توسط کارفرما، نهایت امنیت داده، راهنمای کامل فارسی برای هر مرحله در سی‌پنل (بدون ترمینال)، انتقال داده‌های پروداکشن، راهنمای دستگاه‌های کاربران، بدون باگ.

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `DB-MIG-A1/A2` | نیاز | لایهٔ MySQL + اسکریپت مهاجرت فارسی (بدون ترمینال) + dual-write + سوییچ | ✅ **آماده شد (v33.17.0)** — api/db-lib.php + api/migrate.php (۶ مرحلهٔ فارسی با تأیید کلمه) + wrapper در crm.php + الگوی کانفیگ امن |
| `DB-MIG-GUIDE` | نیاز | راهنمای کامل فارسی سی‌پنل (هر مرحله + عیب‌یابی + راهنمای کاربران) | ✅ **آماده شد** — crm/MIGRATION-MYSQL-CPANEL-GUIDE-FA.md |
| `DB-MIG-EXEC` | اقدام | اجرای ۵ مرحلهٔ سی‌پنل توسط کارفرما + تأیید بررسی تطابق + سوییچ نهایی | 🔲 **در انتظار اقدام کارفرما** (راهنما آماده است) |
| `DB-MIG-B` | 🟡 | فاز B (کلاینت نازک؛ حذف وابستگی به localStorage) | 🔲 پس از تکمیل فاز A |

---

## v33.18.0 — فاز B (کلاینت نازک) — DB-MIG-001

**تصمیم کارفرما:** تا پایان فاز B مرج انجام نمی‌شود. فاز B: آفلاین=صف+همگام‌سازی مجدد؛ هم‌گرایی=یک‌باره با تأیید؛ دامنه=همهٔ کلیدها.

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `DB-MIG-B` | نیاز | کلاینت نازک: سرور منبع حقیقت؛ localStorage فقط کش/آفلاین | ✅ **پیاده‌سازی شد (v33.18.0)** — crm/client-server.js (هوک getData/setData + صف ptf_b_queue + flush + هم‌گرایی یک‌باره با تأیید + فعال/غیرفعال‌سازی) + دکمه‌های تنظیمات + لود بعد از sync.js. tester298 (۸/۰). **در انتظار تست استیجینگ** |
| `DB-MIG-B-TEST` | تست | تست استیجینگ: فعال‌سازی → هم‌گرایی → کارکرد عادی ۲-۳ روز → آفلاین/صف/بازگشت | 🔲 **گام بعدی** (پس از استقرار v33.18.0 روی استیجینگ) |
| `DB-MIG-MERGE` | اقدام | مرج PR #11 به main + deploy پروداکشن | 🔲 پس از تکمیل تست فاز B (تصمیم کارفرما) |

---

## v33.19.0 — رفع باگ هم‌گرایی فاز B (گزارش کاربر) + ابزار آزادسازی حافظه — برنچ `arena/019fc272-pishtaj-ir`

**نکتهٔ نشست:** نشست قبلی با مرج PR #11 بسته شد و push کامیت اصلاحی محلی (`35faba6`) ممکن نشد؛ در این نشست (برنچ `arena/019fc272-pishtaj-ir`) اصلاحات از روی دستورالعمل هند‌آف بازسازی شدند.

**گزارش کاربر:** «هم‌گرایی داده می‌گوید اتصال برقرار نشد ولی تست اتصال سبز است؛ دکمهٔ اتصال بالای صفحه زرد مانده؛ استورج دستگاه‌ها از ۱۰۰٪ به ۷۰-۹۰٪ رسیده ولی هم‌گرایی fail می‌شود.»

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `UR-2026-08-02-14a` | 🔴 باگ | هم‌گرایی با توکن منقضی fail می‌شد ولی پیام «سرور در دسترس نیست» نشان می‌داد (تضاد با تست اتصال سبز = users_get عمومی) | ✅ **رفع شد (v33.19.0)** — serverPush: تشخیص needLogin/401 → `ptfSyncRefreshAuth` + یک بار retry (الگوی backup.js)؛ پیام دقیق «نشست شما منقضی شده است؛ دوباره وارد شوید» |
| `UR-2026-08-02-14b` | 🔴 باگ | هم‌گرایی همهٔ ~۵۲ کلید را یک‌جا می‌فرستاد → payload چند MB روی دستگاه‌های پرحافظه → رد/تایم‌اوت | ✅ **رفع شد** — `ptfBPushBatch` ارسال دسته‌ای (هر بار ≤۲۰ کلید) در هم‌گرایی و flush صف؛ شکست یک دسته فقط همان دسته را نگه می‌دارد؛ needLogin → توقف کامل |
| `UR-2026-08-02-14c` | نیاز | آزادسازی واقعی حافظهٔ مرورگر برای دستگاه‌هایی که مهاجرت کرده‌اند | ✅ **رفع شد** — `ptfBClearLocalCache`: پاک‌سازی امن کش محلی با گاردهای (فاز B فعال + نشانگر هم‌گرایی موفق `ptf_b_synced_<user>` + صف آفلاین خالی + تایپ «پاک») + دکمهٔ «🗑 پاک‌سازی کش محلی» در باکس فاز B |
| `UR-2026-08-02-14d` | باگ پنهان | boot interval و `ptfBEnable` هوک را دوباره اعمال می‌کردند (getData دولایه) | ✅ **رفع شد** — گارد `__ptfB` روی getData/setData |
| `UR-2026-08-02-15` | تست | تست واقعی فاز B: هم‌گرایی چنددستگاهی، آفلاین/صف، badge سبز، پاک‌سازی کش روی دستگاه پرحافظه | 🔲 **گام بعدی** (پس از deploy v33.19.0) |
| `DB-MIG-DEPLOY-3319` | اقدام | مرج PR جدید + Deploy to Production (confirm=DEPLOY, ref=main) | ✅ **انجام شد** — PR #12 مرج و Deploy دستی اجرا شد؛ پروداکشن v33.19.0 شد (بعداً با v33.20.0/PR #13 ارتقا یافت) |

**راهنمای کاربران دستگاه‌های پرحافظه:** ۱) رفرش (Ctrl+Shift+R) → ۲) اگر نشست منقضی: ورود مجدد → ۳) «تنظیمات → 🔄 هم‌گرایی دادهٔ محلی» → ۴) «🗑 پاک‌سازی کش محلی» + تایپ «پاک».

**تست:** tester298 از ۸/۰ به **۱۲/۰** رسید (batch، needLogin/retry، پیام دقیق، پاک‌سازی کش). رگرسیون = صفر شکست جدید؛ audit ۰ خطا.

**انتشار (۱۴۰۵/۰۸/۱۱):** ✅ PR #12 از برنچ `arena/019fc272-pishtaj-ir` به **main مرج شد** (`b6116a0`). ⏳ **Deploy to Production:** تریگر خودکار با توکن سندباکس ممکن نبود (HTTP 403 — فقدان مجوز Actions/workflow-dispatch)؛ باید دستی: Actions → Deploy to Production → `confirm=DEPLOY`، `ref=main`. پروداکشن هم‌اکنون v33.18.0 است.

---

## v33.21.0 — PTF-SCALE-P0 (مقیاس‌پذیری برای افزایش کاربران) + هاتفیکس همگام‌سازی

**درخواست کارفرما:** «بررسی کن چه کارهایی برای ارتقای تکنولوژی در این زمینه (افزایش تعداد کاربران) می‌شود؛ حداکثر کاربر با وضعیت فعلی؟»

**تحلیل ظرفیت (راستی‌آزمایی از کد):** معماری = فایل‌های JSON روی هاست اشتراکی (crm.php هنوز ارجاعی به MySQL/db-lib ندارد) + ۵۲ کلید = کل هر جدول به‌صورت یک رشتهٔ JSON (۸MB/کلید، ۴۰MB/درخواست). سقف سخت کد: **۱۰۰ کاربر** (users_sync). سقف واقعی همزمانِ فعال: **~۱۰ راحت، ~۲۵–۳۰ مرز مشکل** (پول کامل ۴MB به‌ازای هر تغییر برای همه + serverPull(0) در getData + پول تب‌های مخفی). gzip ✅ روی JSON فعال است.

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `UR-2026-08-02-18` | 🔴 بحرانی | **BUG-SYNC-RD-SCOPE-001** — v33.20.0 (دیپلوی امروز): `ptfReadArchive` در اسکوپ سراسری `rd()` را صدا می‌زد که داخل IIFE است → ReferenceError در هر pull/push دارای tombstone → نشان آفلاین کذب + عدم اعمال تغییرات (تسترها با rd تزریقی نقص را می‌پوشاندند) | ✅ **رفع شد** — راهنمای خودکفا `ptfReadDeletedArchiveStr` + چک رفتاری دائمی در tester300 |
| `UR-2026-08-02-19` | طراحی | پول = اسنپ‌شات کامل (~۴MB) برای همه مع هر تغییر (rev سراسری تنها) | ✅ **دلتا-پول به‌ازای هرکلید** — krevs کلاینت ↔ meta سرور؛ سازگار با عقب (بدون krevs → کامل)؛ آرشیو تنبَل |
| `UR-2026-08-02-20` | طراحی | getData فاز B: پول کامل مستقل به‌ازای هر خواندن (نه single-flight نه حد نرخ) | ✅ **sharedPull** — تک‌پرواز + حد ۲ثانیه + دلتا + تخلیهٔ تب مخفی + به‌روزرسانی مرکزی کش/آینه |
| `UR-2026-08-02-21` | طراحی | پول ۲۰ثانیه‌ای در تب‌های مخفی/غیرمتمرکز (ضرب‌دار چندتبی) | ✅ مخفی: بدون پول؛ غیرمتمرکز: هر ۱۲۰ثانیه؛ جبران فوری روی focus/visibilitychange |
| `UR-2026-08-02-22` | نقشهٔ راه | پس از P0 روی همین هاست ظرفیت ~۵۰ همزمان؛ گام‌های بعد: **P1** اتصال واقعی crm.php به MySQL (db-lib آماده ولی بدون مصرف‌کننده!) + رفع سقف ۱۰۰ کاربر + کلیدهای per-user برای دادهٔ شخصی — **P2** VPS (Hetzner DE ~€۵) + opcache + SSE/WebSocket + جدول‌های رکورد-محور (به‌جای بلاب JSON) + صفحه‌بندی سرور — **P3** میکروسرویس realtime/رپلیکا/CDN/مانیتورینگ (۲۰۰+) | 🔲 انتظار انتخاب کارفرما |
| `UR-2026-08-02-23` | اقدام | مرج PR v33.21.x + **دیپلوی فوری** (هاتفیکس همگام‌سازی زنده) | ✅ **مرج شد (۱۴۰۵/۰۸/۱۱)** — کارفرما پس از تست استیجینگ v33.21.1 تأیید کرد؛ PR #14 در همین نشست به main مرج شد (main=`3122e4d`) — ⏳ دیپلوی: دستی کارفرما (قاعدهٔ جدید: تأیید استیجینگ → مرج در نشست → دیپلوی کارفرما) |
| `UR-2026-08-02-24` | فیدبک استیجینگ | v33.21.0: «رکورد جدید در تب‌های دیگر تا قبل از جابه‌جایی ظاهر نمی‌شد» — کارفرما گزینهٔ **A (لحظه‌ای بین تب‌ها + پول آهسته)** را انتخاب کرد | ✅ **v33.21.1** — پینگ بین‌تبی (storage event → دلتا-پول فوری در تب‌های همان مرورگر حتی پنهان؛ مقایسهٔ rev مانع حلقه؛ حد نرخ ۵ثانیه) + مخفی=مسیر آهسته ۱۸۰ثانیه (v33.21.0: توقف کامل) + پینگ در پوش موفق. tester300=۲۵/۰؛ رگرسیون=۵۳۹۱/۸۴ = baseline+۲۵ |
| `UR-2026-08-02-25` | قاعده | محدودهٔ زیرساخت: **سرور تغییر نمی‌کند** — فقط فازهای قابل اجرا روی همین هاست cPanel (P2/P3 حذف: بدون VPS/WebSocket daemon/Redis) | ✅ ثبت شد — نقشه‌ٔ راه همین-هاست اجرا شد |
| `UR-2026-08-02-26` | P1 | **اتصال واقعی مسیر سینک به MySQL همان هاست** (۵۲ کلید سنگین — قبلاً فقط load_data/save_data سیم بود) | ✅ **v33.22.0** — sync_key_read/write با حالت‌های off/dual/mysql؛ rev-aware write؛ شکست DB→needRetry idempotent؛ کش request-scoped اتصال/کانفیگ؛ کلاینت دست‌نخورده. tester301=۲۰/۰؛ رگرسیون=۵۴۱۱/۸۴. **گام: تست استیجینگ (mode=off امن) + ویزارد migrate.php روی استیجینگ (ساخت DB در cPanel فعلی) → تأیید → مرج → دیپلوی** |
| `UR-2026-08-02-27` | تصمیم | رفع سقف ۱۰۰ کاربر **لازم نیست** (✅ تصریح کارفرما: «تا چندین سال آینده بیش از ۱۰۰ نه») — کلیدهای per-user و ETag/304 در انتظار نیاز بعدی | 🔲 بک‌لاگ کم‌اولویت |

**ظرفیت پس از P0 (همین هاست):** ~۵۰ کاربر همزمان راحت (برآورد مهندسی؛ تأیید قطعی با ابزار تست بار — گزینهٔ مطرح‌شده). tester300 جدید: **۲۲/۰**؛ رگرسیون: ۲۹۵ تستر → **۲۳۹/۵۶ فایل و ۵۳۸۸/۸۴ چک = baseline + tester300**؛ audit ۰ خطا.

---

## v33.20.0 — آینهٔ خالدار (پاسخ به گزارش «از ۴۱٪ به ۸۰٪ برگشت»)

**گزارش کاربر:** «نسخه را اجرا کردم؛ از ۴۱٪ localStorage نزدیک ۸۰٪ رسید؛ با این که پاک‌سازی کش زدم تغییری نکرد.»

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `UR-2026-08-02-16a` | 🔴 طراحی | کد sync.js در هر بارگذاری **آینهٔ کامل سرور** را در localStorage می‌نوشت (pullCheck forceFull + polling هر ۲۰ثانیه + هوک فاز B) → آینه بلافاصله برمی‌گشت؛ پاک‌سازی کش فقط پس‌مانده را حذف می‌کرد | ✅ **رفع شد (v33.20.0)** — آینهٔ خالدار: کلیدهای سنگین (فهرست + خودکار >۱۲۰هزار کاراکتر) فقط در حافظهٔ نشست + IndexedDB (`bdata:`) نگهداری می‌شوند؛ preload بوت نسخه‌های localStorage را migrate و آزاد می‌کند |
| `UR-2026-08-02-16b` | نیاز | توضیح برای کاربر که چرا پاک‌سازی کش برگشتی است | ✅ **مستند شد** — باکس فاز B در تنظیمات + RELEASE-NOTES-v33.20.0 + پیام موفقیت پاک‌سازی (کلیدهای سنگین دیگر localStorage را پر نمی‌کنند) |
| `UR-2026-08-02-16c` | ایمنی | خواندن‌های مستقیم (guardCounts/massDrop/beacon بدون حافظه → هشدار کاذب/دادهٔ گم) | ✅ **رفع شد** — راهنمای rd/wr در sync.js + اتصال ۱۶ سایت (شامل sendBeacon قبل از بستن صفحه و شمارنده‌های سپر داده) + theme.avatarsAll |
| `UR-2026-08-02-16d` | ایمنی | جابه‌جایی قبل از هم‌گرایی موفق خطرناک بود | ✅ گیت سه‌گانه: فاز فعال + نشانگر `ptf_b_synced_<user>` + IndexedDB موجود؛ غیرفعال‌سازی → ری‌هیدریت به localStorage |
| `UR-2026-08-02-17` | اقدام | مرج PR + Deploy پروداکشن + رفرش کاربران (بوت مهاجرت اجرا شود) | ✅ **کامل شد (۱۴۰۵/۰۸/۱۱)** — PR #13 مرج (main=`9c88a7b`) + Deploy دستی کارفرما اجرا شد + راستی‌آزمایی پروداکشن: «نسخه فعال: v33.20.0» در index، `client-server.js` با آینهٔ خالدار (ptfBMirror/ptfBIdbPreload)، `sw.js` = `ptf-crm-v33.20.0` — ⏳ باقی: رفرش کاربران تا بوت مهاجرت اجرا و درصد پایدار پایین بیاید (فیدبک چندروزه = UR-15) |

**تست:** tester299 (جدید، ۱۴/۰) + tester298 (۱۲/۰) + tester297 (۱۹/۰) + رگرسیون = صفر شکست جدید؛ audit ۰ خطا.
