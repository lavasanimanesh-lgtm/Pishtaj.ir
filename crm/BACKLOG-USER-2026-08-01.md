# بک‌لاگ رسیدگی — گزارش‌های کاربر ۱۴۰۵/۰۸/۱۰ (2026-08-01)

**برنچ:** `arena/019fbe19-pishtaj-ir`
**منبع:** گزارش مستقیم کارفرما در نشست جاری (۹ مورد — به ترتیب اولویت اجرا)
**وضعیت کلی:** هر مورد با شناسه، ریشهٔ فنی، و اقدام ردیابی می‌شود. موارد ۱ و ۵ همان نشست رفع شدند؛ بقیه به ترتیب.

---

| # | شناسه | نوع | عنوان | ریشه/ارجاع فنی | وضعیت |
|---|---|---|---|---|---|
| 1 | `UR-2026-08-01-01` | باگ | دکمه‌های «+ ثبت هزینه»، «پرداخت مستقیم»، «شارژ حساب»، «ارجاع دوره» در **همهٔ تب‌های هاب مالی** دیده می‌شوند؛ باید فقط در تب تنخواه باشند | تولبار `.sb2` در `petty.js#buildPetty` **بدون id** است؛ `financehub.js#finHubApply` فقط `ptAccount/ptPeriods/ptSummary/ptWrap` را مخفی می‌کند، تولبار را نه | ✅ **رفع شد** (id=ptToolbar + مخفی‌سازی در تب‌های غیر تنخواه) |
| 2 | `UR-2026-08-01-02` | باگ | جستجو/تایپ نام کامل تامین‌کننده و مشتری ممکن نیست — بعد از یک حرف تمرکز از کادر می‌پرد | الگوی `oninput="renderOffers()"` در `offers.js:297` (oFsrch) و موارد مشابه که کل کانتینر (شامل input) را بازرندر می‌کنند؛ سابقهٔ مشابه قبلاً در `offers.js:1945` رفع شده بود | ✅ **رفع شد** — بازرسی کامل: اکثر رندرها فقط ناحیهٔ داده را عوض می‌کنند (فوکوس سالم)؛ تنها مورد واقعی `cfFinanceSearch` بود که کل باکس را با `outerHTML` بازسازی می‌کرد → اکنون فقط `#cfFinanceTbl` را به‌روز می‌کند. tester204 (۱۱/۰). |
| 3 | `UR-2026-08-01-03` | باگ/نیاز | حساب مشتری (تب مشتریان هاب مالی) خروجی/گزارش گردش حساب نمی‌دهد | `customer-finance.js#cfOpen` فقط نمایش داخل مودال دارد؛ هیچ دکمهٔ چاپ/خروجی ندارد (الگوی آماده در `supplier-finance.js`: `slLedgerPrint` + `slCsv`) | ✅ **رفع شد** — با مورد ۸ (cfLedgerRows/Csv/Print + دکمه‌ها در مودال) — tester205 (۱۰/۰) |
| 4 | `UR-2026-08-01-04` | باگ | در ثبت قیمت خرید واقعی، عبارت «مجموع خرید» نمایش داده می‌شود؛ باید «قیمت واحد» درخواست شود | جمع‌بندی «مجموع خرید» در `buycompare.js` (خطوط 604/618 `cmpSplitSummary`) و «Σ n قلم = …» در `cmpBulkTotal` (خط 499+) که قیمت ورودی را در تعداد ضرب می‌کند و کاربر را به‌اشتباه می‌اندازد | ✅ **رفع شد** (طبق انتخاب کارفرما «هر دو جا»): در پنجرهٔ تقسیم خرید و ثبت گروهی، برچسب جمع به «جمع کل (اطلاعاتی)» + راهنمای «قیمت واحد هر قلم را وارد کنید — جمع فقط برای اطلاع است» تغییر کرد — tester202 (۲۵/۰) |
| 5 | `UR-2026-08-01-05` | باگ | تشخیص کالاهای مشابه (تب کیفیت داده): بعد از «تأیید ادغام» هیچ اثری دیده نمی‌شود و فقط همان پنجره دوباره باز می‌شود | دو علت: (الف) `data-quality.js#ptfCatalogMergeConfirm` در پایان `ptfCatalogSimilarAudit()` را صدا می‌زند که پنجره را دوباره باز می‌کند؛ (ب) `ptfCatalogSimilarAudit` کالاهای `hidden/merged` را فیلتر نمی‌کند، پس همان گروه دوباره پیشنهاد می‌شود | ✅ **رفع شد** (فیلتر کالاهای ادغام‌شده + بستن پنجره به‌جای بازکردن دوباره) |
| 6 | `UR-2026-08-01-06` | نیاز | در انتخاب تامین‌کنندهٔ هر قلم (پنجرهٔ استعلام) امکان **جستجو با نام/برند/تجهیزات** تامین‌کننده باشد | `supspec.js` از قبل جستجوی دوزبانهٔ برند-آگاه دارد (AC2) اما سلکت تامین‌کنندهٔ per-item در `buycompare.js` (سلکت سادهٔ `cmpSplitSuppliers`/فرم استعلام) به آن متصل نیست | ✅ **رفع شد** — `ptfSupSearch` (نام/برند/تجهیزات با امتیازدهی + مترادف دوزبانه) + پیکر `ptfSupPickerHtml/Filter/Pick/Key` در supspec.js؛ اتصال در ۳ محل buycompare: ثبت قیمت دور (cmpSup)، تقسیم خرید (هر ردیف)، ثبت خرید واقعی (cmpBuySupPick). tester206 (۱۳/۰) |
| 7 | `UR-2026-08-01-07` | نیاز | سورت‌بندی همهٔ فهرست‌ها (درخواست‌ها، پیشنهادات، تنخواه و…) بر اساس تاریخ/شرکت/… | نیازمند یک کامپوننت سورت مشترک (بررسی `listtools.js`) و افزودن به رندرهای اصلی: `offers.js#renderOffers`، `petty.js#renderPetty`، `supplier-finance.js`، `customer-finance.js` و… | ✅ **رفع شد** — کامپوننت مشترک `crm/sortable.js` (ptfSortVal/Rows/Sorted + هدر قابل کلیک + dropdown) + اتصال در ۵ لیست: پیشنهادات (شماره/خریدار/تاریخ/مبلغ/وضعیت)، درخواست‌ها RFQ (کد/مشتری/تاریخ/وضعیت)، تنخواه (dropdown: تاریخ/مبلغ/ثبت‌کننده)، حساب مشتریان (نام/مانده/اعتبار)، حساب تامین‌کنندگان (نام/مانده). در ضمن باگ فوکوس `slFinanceSearch` (outerHTML) هم رفع شد (هم‌خانوادهٔ مورد ۲). tester207 (۱۴/۰) |
| 8 | `UR-2026-08-01-08` | نیاز | خروجی **PDF/اکسل** گردش حساب مشتریان (تب مشتریان هاب مالی) | کپی الگوی تامین‌کننده (`slLedgerPrint` → PDF چاپی + `slCsv` → Excel/CSV) در `customer-finance.js` برای `cfOpen`/`cfAccountRows` | ✅ **رفع شد** — `cfLedgerRows` (ردیف‌های مرتب با ماندهٔ تجمعی) + `cfLedgerCsv` (CSV با BOM) + `cfLedgerPrint` (پیش‌نمایش چاپی/PDF) + دکمه‌های «🖨 چاپ/PDF» و «⬇ اکسل» در مودال حساب — tester205 (۱۰/۰) |
| 9 | `UR-2026-08-01-09` | نیاز | گزارش کامل دورهٔ تنخواه: ردیف، تاریخ، مبلغ، توسط، شرح هزینه + جمع زندهٔ «هزینه‌های دوره / موجودی دوره / شارژ دوره» | `petty.js#ptfPettyPeriodData(month)` داده‌ها را دارد (`petty`, `tx`, `totalOut`, `charges`, `balance`)؛ نیاز به مودال گزارش ردیفی + دکمه در کنار هر دوره (`renderPeriods`) | ✅ **رفع شد** — `ptfPettyPeriodEvents` (ردیف/تاریخ/نوع/شرح/توسط/مبلغ مرتب) + `ptfPettyPeriodTotals` (جمع زنده: هزینه‌های دوره/شارژ/موجودی — هزینه‌های ابطال‌شده حذف) + مودال گزارش + CSV + چاپ/PDF + دکمه‌های «📊 گزارش دوره» (کنار هر دوره) و «📊 گزارش دورهٔ جاری» (تولبار) — tester208 (۱۰/۰) |

---

## ترتیب اجرا (طبق درخواست «به ترتیب»)

1. ✅ مورد ۱ — تولبار تنخواه فقط در تب تنخواه
2. ✅ مورد ۲ — رفع ازدست‌رفتن فوکوس جستجو (تامین‌کننده/مشتری)
3. ✅ موارد ۳ + ۸ — خروجی گردش حساب مشتری (PDF/اکسل/چاپ)
4. 🔲 مورد ۵ — (قبلاً رفع شد — راستی‌آزمایی استیجینگ)
5. ✅ مورد ۴ — برچسب «قیمت واحد» در ثبت خرید واقعی (هر دو پنجره تقسیم/گروهی)
6. ✅ مورد ۶ — جستجوی تامین‌کننده با نام/برند در استعلام
7. ✅ مورد ۷ — سورت‌بندی فهرست‌ها (کامپوننت مشترک + ۵ لیست)
8. ✅ مورد ۹ — گزارش کامل دورهٔ تنخواه با جمع‌های زنده (تستر208)

> 🎉 **تمام ۹ مورد بک‌لاگ کاربر (UR-01..UR-09) بسته شد.** (۵ مورد قبل + موارد ۲/۳+۸/۴/۶/۷/۹ در این نشست)

## قواعد
- هر مورد = کامیت مستقل + تستر UAT (الگوی tester200-202) + به‌روزرسانی هند‌آف.
- بعد از هر مورد: `build_sitemap.py` + `audit.py` (۰ خطا) — فقط در صورت تغییر فایل‌های public.
- موارد ۳/۸ و ۹ نیازمند تأیید قالب خروجی توسط کارفرما هنگام ارائهٔ پیش‌نمایش.

---

## UR-10 — PDF تلفیقی دورهٔ تنخواه + الزام پیوست بانک (۱۴۰۵/۰۸/۱۰ — درخواست جدید کارفرما)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `UR-2026-08-01-10` | نیاز | هنگام ارجاع دوره به حسابدار، گزارش + ضمائم + رسیدهای پرداخت در **یک PDF** به‌هم‌مرتبط با **شناسهٔ «سند N»** هر رسید (مطابق ردیف گزارش)؛ چیدمان فشردهٔ **۳-۴ رسید در هر صفحه** (کمترین صفحه، بدون افت خوانایی)؛ و **الزام پیوست صورتحساب بانک/گردش حساب قبل از ارجاع** | ✅ **رفع شد** — `ptfPettyPeriodCombinedPdf` (گزارش + صفحهٔ رسیدها با `petId` + پیوست بانک در صفحهٔ جدا)، `ptfPettyReceiptsHtml` (چیدمان ۳۳٪ = ۳-در-صفحه)، `ptfPettyPeriodFiles` (جمع‌آوری فایل‌های دوره با شناسه)، ذخیرهٔ `petId` هنگام آپلود رسید (pettyAdd)، دکمهٔ «📎 PDF تلفیقی» در renderPeriods، الزام پیوست بانک در `pettyClosePeriod` (مسدودسازی ارجاع بدون پیوست). تستر tester209 (۱۳/۰). |

> **وضعیت کلی:** بک‌لاگ ۹ موردی + UR-10 همگی بسته شدند. (S3 تاریخچهٔ ادغام کاتالوگ و S4/S5 یکپارچگی/PR و کارهای سئو در هند‌آف باقی‌اند.)

---

## UR-11 و UR-12 — درخواست‌های جدید ۱۴۰۵/۰۸/۱۰ (ثبت + پیاده‌سازی)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `UR-2026-08-01-11` | نیاز | گزارش تنخواه **بازه‌ای** (از آخرین ارجاع تا تاریخ انتخابی)؛ ماندگاری گزارش+منضمات هر دوره؛ قالب «تنخواه‌گردان از تاریخ … تا …» با نام ثبت‌کننده و نحوهٔ پرداخت (مستقیم/تسویه/در انتظار)؛ PDF منضمات در هر زمان | ✅ **رفع شد (v33.3.4)** — طراحی در `crm/DESIGN-PETTY-PERIOD-RANGE-v1.md`؛ پیاده‌سازی در petty.js (SuggestedRange/PeriodData/Events/RangeLabel/Report/Csv/Print/CombinedPdf با بازه + renderPeriods + pettyClosePeriod با از/تا + الزام پیوست آخرین دوره) — tester212 (۱۳/۰) |
| `UR-2026-08-01-12` | نیاز | ساده‌سازی مختومهٔ پرونده فروش (پکینگ‌لیست اجباری نباشد، تسویه تامین‌کننده…) | ✅ **رفع شد (v33.3.4)** — پکینگ‌لیست/تسویه تامین‌کننده از قبل فقط هشدار بودند؛ تنها blocker «تحویل» با **مسیر تأیید صریح** (تیک + دلیل → closeOverride) قابل عبور شد و در audit ثبت می‌شود؛ گارد مالی commit حفظ شد — tester214 (۷/۰) + tester110 (۳۶/۰) |

---

## ماژول مستقل چک — درخواست نشست قبلی (ثبت + معماری)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `CHQ-MOD-001` | معماری/نیاز | جداسازی چک‌ها به **ماژول مستقل مالی**: چک‌های صادره (issued) و وارده (received) — خارج از «چک شخصی»؛ پنل مستقل در هاب مالی + گزارش + وضعیت‌ها | 🟡 **پیش‌نویس معماری آماده** — `crm/DESIGN-CHEQUE-MODULE-v1.md` (مدل داده/مهاجرت نرم/UI/گزارش/گام‌ها/۵ سؤال) — **منتظر تأیید کارفرما**؛ پیاده‌سازی پس از تأیید |

> یادداشت: کارفرما «نتیجهٔ تست اصلاحات قبلی (UR-11/12) را بعداً اعلام می‌کند» — تا آن زمان هیچ تغییری در آن مسیرها نمی‌کنیم مگر با گزارش.

> **CHQ-MOD-001 پیشرفت:** معماری تأیید شد؛ گام‌های ۱–۳، ۵–۸ (v33.4.0) اجرا شد (cheque-module.js + cheque-panel.js + تب هاب مالی + sync/backup/api + tester215 24/0). باقی: وصول مشتری با چک → وارده (گام ۴) + یکپارچه‌سازی کامل.

---

## UR-13 و BUG-ARCHIVE — بازخورد ۱۴۰۵/۰۸/۱۰ (رفع شد v33.4.1)

| شناسه | نوع | عنوان | وضعیت |
|---|---|---|---|
| `UR-2026-08-01-13` | باگ/نیاز | گزارش/ارجاع تنخواه: تقویم شمسی در فیلدهای تاریخ + پذیرش فرمت‌های رایج (1405/04/01 یا 1405-4-1) + **افزودن فایل PDF گردش حساب بانک در خود پنجرهٔ ارجاع (الزامی)** | ✅ **رفع شد (v33.4.2)** — ptfDialog حالا `datePicker` (تقویم شمسی) و `type:'upload'` (ویجت آپلود داخل دیالوگ) را پشتیبانی می‌کند؛ `ptfPettyNormDate` فرمت‌ها را نرمال می‌کند؛ پنجرهٔ ارجاع فیلد «📎 فایل PDF گردش حساب بانک (الزامی)» دارد و بدون فایل ارجاع نمی‌شود؛ فایل‌ها روی رکورد دوره (files) ذخیره و در PDF تلفیقی می‌آیند — tester212 (۲۰/۰)، tester209 (۱۳/۰) |
| `BUG-ARCHIVE` | باگ | بایگانی پرونده فقط «سند متمم» را نشان می‌داد؛ باید **تمام اسناد** (پیشنهاد اصلی/متمم/فاکتورها/نامه‌ها/استعلام‌ها/متفرقه) در بایگانی باشند | ✅ **ریشه‌کنی شد** — ریشه: `ptfSalesFileOffers` فقط wonOffer+st==='won' را برمی‌گشت؛ و `sfArchive` فقط wonOffer را در offerNo ذخیره می‌کرد؛ و `openProject` فقط p.offerNo را نمایش می‌داد. رفع: (۱) ptfSalesFileOffers → همهٔ offers همان inqNo + زنجیره (altOf/srcToNo/coNo)؛ (۲) sfArchive → `docSnap` (فهرست کامل offers/letters/invoices/supply/misc) + `offerNos`؛ (۳) openProject → بخش «📎 اسناد کامل پرونده (بایگانی)» با همهٔ اسناد (دکمهٔ دانلود هر پیشنهاد + فهرست فاکتورها/نامه‌ها/متفرقه) — tester216 (۱۰/۰) |
