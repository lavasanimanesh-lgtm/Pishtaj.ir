# بک‌لاگ رسیدگی — گزارش‌های کاربر ۱۴۰۵/۰۸/۱۰ (2026-08-01)

**برنچ:** `arena/019fbe19-pishtaj-ir`
**منبع:** گزارش مستقیم کارفرما در نشست جاری (۹ مورد — به ترتیب اولویت اجرا)
**وضعیت کلی:** هر مورد با شناسه، ریشهٔ فنی، و اقدام ردیابی می‌شود. موارد ۱ و ۵ همان نشست رفع شدند؛ بقیه به ترتیب.

---

| # | شناسه | نوع | عنوان | ریشه/ارجاع فنی | وضعیت |
|---|---|---|---|---|---|
| 1 | `UR-2026-08-01-01` | باگ | دکمه‌های «+ ثبت هزینه»، «پرداخت مستقیم»، «شارژ حساب»، «ارجاع دوره» در **همهٔ تب‌های هاب مالی** دیده می‌شوند؛ باید فقط در تب تنخواه باشند | تولبار `.sb2` در `petty.js#buildPetty` **بدون id** است؛ `financehub.js#finHubApply` فقط `ptAccount/ptPeriods/ptSummary/ptWrap` را مخفی می‌کند، تولبار را نه | ✅ **رفع شد** (id=ptToolbar + مخفی‌سازی در تب‌های غیر تنخواه) |
| 2 | `UR-2026-08-01-02` | باگ | جستجو/تایپ نام کامل تامین‌کننده و مشتری ممکن نیست — بعد از یک حرف تمرکز از کادر می‌پرد | الگوی `oninput="renderOffers()"` در `offers.js:297` (oFsrch) و موارد مشابه که کل کانتینر (شامل input) را بازرندر می‌کنند؛ سابقهٔ مشابه قبلاً در `offers.js:1945` رفع شده بود | 🔲 باز — بعدی |
| 3 | `UR-2026-08-01-03` | باگ/نیاز | حساب مشتری (تب مشتریان هاب مالی) خروجی/گزارش گردش حساب نمی‌دهد | `customer-finance.js#cfOpen` فقط نمایش داخل مودال دارد؛ هیچ دکمهٔ چاپ/خروجی ندارد (الگوی آماده در `supplier-finance.js`: `slLedgerPrint` + `slCsv`) | 🔲 باز — با مورد ۸ ادغام می‌شود |
| 4 | `UR-2026-08-01-04` | باگ | در ثبت قیمت خرید واقعی، عبارت «مجموع خرید» نمایش داده می‌شود؛ باید «قیمت واحد» درخواست شود | جمع‌بندی «مجموع خرید» در `buycompare.js` (خطوط 604/618 `cmpSplitSummary`) و «Σ n قلم = …» در `cmpBulkTotal` (خط 499+) که قیمت ورودی را در تعداد ضرب می‌کند و کاربر را به‌اشتباه می‌اندازد | 🔲 باز — نیاز به تأیید محل دقیق هنگام اجرا |
| 5 | `UR-2026-08-01-05` | باگ | تشخیص کالاهای مشابه (تب کیفیت داده): بعد از «تأیید ادغام» هیچ اثری دیده نمی‌شود و فقط همان پنجره دوباره باز می‌شود | دو علت: (الف) `data-quality.js#ptfCatalogMergeConfirm` در پایان `ptfCatalogSimilarAudit()` را صدا می‌زند که پنجره را دوباره باز می‌کند؛ (ب) `ptfCatalogSimilarAudit` کالاهای `hidden/merged` را فیلتر نمی‌کند، پس همان گروه دوباره پیشنهاد می‌شود | ✅ **رفع شد** (فیلتر کالاهای ادغام‌شده + بستن پنجره به‌جای بازکردن دوباره) |
| 6 | `UR-2026-08-01-06` | نیاز | در انتخاب تامین‌کنندهٔ هر قلم (پنجرهٔ استعلام) امکان **جستجو با نام/برند/تجهیزات** تامین‌کننده باشد | `supspec.js` از قبل جستجوی دوزبانهٔ برند-آگاه دارد (AC2) اما سلکت تامین‌کنندهٔ per-item در `buycompare.js` (سلکت سادهٔ `cmpSplitSuppliers`/فرم استعلام) به آن متصل نیست | 🔲 باز |
| 7 | `UR-2026-08-01-07` | نیاز | سورت‌بندی همهٔ فهرست‌ها (درخواست‌ها، پیشنهادات، تنخواه و…) بر اساس تاریخ/شرکت/… | نیازمند یک کامپوننت سورت مشترک (بررسی `listtools.js`) و افزودن به رندرهای اصلی: `offers.js#renderOffers`، `petty.js#renderPetty`، `supplier-finance.js`، `customer-finance.js` و… | 🔲 باز |
| 8 | `UR-2026-08-01-08` | نیاز | خروجی **PDF/اکسل** گردش حساب مشتریان (تب مشتریان هاب مالی) | کپی الگوی تامین‌کننده (`slLedgerPrint` → PDF چاپی + `slCsv` → Excel/CSV) در `customer-finance.js` برای `cfOpen`/`cfAccountRows` | 🔲 باز — با مورد ۳ |
| 9 | `UR-2026-08-01-09` | نیاز | گزارش کامل دورهٔ تنخواه: ردیف، تاریخ، مبلغ، توسط، شرح هزینه + جمع زندهٔ «هزینه‌های دوره / موجودی دوره / شارژ دوره» | `petty.js#ptfPettyPeriodData(month)` داده‌ها را دارد (`petty`, `tx`, `totalOut`, `charges`, `balance`)؛ نیاز به مودال گزارش ردیفی + دکمه در کنار هر دوره (`renderPeriods`) | 🔲 باز |

---

## ترتیب اجرا (طبق درخواست «به ترتیب»)

1. ✅ مورد ۱ — تولبار تنخواه فقط در تب تنخواه
2. 🔲 مورد ۲ — رفع ازدست‌رفتن فوکوس جستجو (تامین‌کننده/مشتری)
3. 🔲 موارد ۳ + ۸ — خروجی گردش حساب مشتری (PDF/اکسل/چاپ)
4. 🔲 مورد ۵ — (قبلاً رفع شد — راستی‌آزمایی استیجینگ)
5. 🔲 مورد ۴ — برچسب «قیمت واحد» در ثبت خرید واقعی (با تأیید محل دقیق)
6. 🔲 مورد ۶ — جستجوی تامین‌کننده با نام/برند در استعلام
7. 🔲 مورد ۷ — سورت‌بندی فهرست‌ها
8. 🔲 مورد ۹ — گزارش کامل دورهٔ تنخواه با جمع‌های زنده

## قواعد
- هر مورد = کامیت مستقل + تستر UAT (الگوی tester200-202) + به‌روزرسانی هند‌آف.
- بعد از هر مورد: `build_sitemap.py` + `audit.py` (۰ خطا) — فقط در صورت تغییر فایل‌های public.
- موارد ۳/۸ و ۹ نیازمند تأیید قالب خروجی توسط کارفرما هنگام ارائهٔ پیش‌نمایش.
