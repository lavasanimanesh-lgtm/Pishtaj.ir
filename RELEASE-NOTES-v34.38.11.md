# CRM v34.38.11

## رفع «دریافت کامل CRM انجام نشد» و بارگذاری‌نشدن درخواست‌های تأمین

گزارش کارفرما: پیام «⚠️ دریافت کامل CRM انجام نشد؛ آخرین snapshot سالم حفظ شد و تلاش مجدد
خودکار ادامه دارد» دائم می‌آمد و هر چند ثانیه تکرار می‌شد؛ داده‌ها — به‌ویژه درخواست‌های
تأمین — کامل بارگذاری نمی‌شدند. علت، سه اشکال در مسیر تازهٔ pull اتمیک (v34.38.10) بود.
شرح کامل در `ARENA-CRM-ATOMIC-PULL-INCOMPLETE-RCA-2026-09-08.md`.

- **chunk بایتیِ دقیق (`mode=bytes`)**: کلاینت دیگر ردیف‌ها را با `JSON.stringify` بازتولید
  نمی‌کند؛ تکه‌های دقیقِ همان رشتهٔ سرور را می‌گیرد، به‌هم می‌چسباند و `sha256` همان بایت‌ها
  را می‌سنجد. اختلاف طبیعی PHP/JS (`1.0` در برابر `1`، `{}` در برابر `[]`، خروجی
  `JSON_PRETTY_PRINT` فرمان‌های دامنه، فرار اسلش) دیگر نه pull سالم را رد می‌کند و نه شکل
  دادهٔ ذخیره‌شده را تغییر می‌دهد.
- **تحمل drift اسنپ‌شات**: chunk با pin شدن `keyRev`/`archiveRev` سرو می‌شود؛ بالا رفتن rev
  سراسری به‌خاطر نوشتن کاربر دیگر، دیگر همهٔ chunkها را 409 نمی‌کند. در تغییر واقعیِ همان
  collection، manifest تازه گرفته می‌شود و collectionهای بدون تغییر دوباره دانلود نمی‌شوند.
- **پایان «همه یا هیچ»**: کلید ناخوانا (فایل غایب یا JSON خراب) در `unavailable` گزارش
  می‌شود؛ بقیهٔ کلیدها کامل می‌آیند و آخرین نسخهٔ سالم محلیِ کلید معیوب حفظ و به کاربر
  اعلام می‌شود.
- **بن‌بست بوت برداشته شد**: پس از دو شکست پیاپی مسیر اتمیک، همان pull دلتای معتبر قبلی
  اجرا می‌شود تا CRM بارگذاری شود (و push تغییرات محلی از حالت انتظار خارج شود).
- **پیام کاربر**: علت واقعی در متن پیام می‌آید، همان علت حداکثر یک‌بار در دقیقه نمایش داده
  می‌شود و فاصلهٔ تلاش مجدد پلکانی است (۵ → ۱۰ → ۲۰ → ۳۰ ثانیه).
- `window.ptfSyncFullResync(cb)` برای درخواست «دریافت کامل مجدد» اضافه شد.

## امنیت و بهداشت

- `data_manifest` و `data_chunk` مانند `data_pull` زیر ACL نقش (`sync_read`) رفتند و از
  لاگ پرتکرار API خارج شدند.

## نگهداری

- تستر `tester618-v34.38.11-atomic-pull-completion.js` (شبیه‌سازی کامل کلاینت روی
  `crm/sync.js` واقعی + قرارداد سرور) به گیت CI اضافه شد.
- cache-buster و Service Worker به نسخهٔ `34.38.11` ارتقا یافتند.
