# ARENA · ریشه‌یابی — «جمع هزینه‌های مستقیم پرونده درست نیست» و «هزینه‌های قدیمی با حذف + رفرش برمی‌گردند»

**تاریخ:** ۲۰۲۶-۰۹-۰۷ (۱۴۰۵/۰۶/۱۶) · **پایه کد:** `arena/01a07be8-pishtaj-ir` روی `376b516` — `VERSION.json` = **CRM v34.38.1**
**نوع کار:** فقط ارزیابی — **هیچ فایل کدی تغییر نکرد**؛ فقط همین سند اضافه شده است.
**دامنه:** `crm/salesfiles.js` · `crm/buycompare.js` · `crm/finance-write-guard.js` · `crm/finance-core.js` · `crm/finance-helpers.js` · `crm/sync.js` · `crm/sales-domain-v2.js` · `crm/petty.js` · `api/sales-domain.php` · `api/crm.php`

---

## ۰) خلاصهٔ مدیریتی

| # | گزارش شما | تشخیص | شدت | ریشه در یک جمله |
|---|---|---|---|---|
| ۱ | «جمع کل هزینه‌ها درست نمایش داده نمی‌شود» | **تأیید می‌شود — سه منبع جمع ناهمسان + ناهماهنگی «جمع نوار» با «ردیف‌های فهرست»** | P1 | نوار مالی فقط `deal.costEvents` را جمع می‌زند، ولی فهرستِ همان پرونده ردیف‌های تنخواهِ «پروجکشن زنده» را هم نشان می‌دهد و مبلغِ زندهٔ تنخواه را جای اسنپ‌شات نشان می‌دهد؛ سه تابعِ متفاوتِ «جمع هزینه» هم اعداد متفاوت تولید می‌کنند |
| ۲ | «هزینه‌های قدیمی با حذف + رفرش برمی‌گردند؛ هزینه‌های غیرتنخواه باید کلاً حذف شوند و برنگردند» | **مکانیزم tombstone (v34.29.8) فقط در مسیر merge اثر دارد؛ مسیرهای render/overwrite آن را نمی‌بینند** | **P1** | `_costTomb` فقط در `ptfMergeBusinessRecord` (sync) اعمال می‌شود؛ پروجکشن زندهٔ تنخواه در کشوی پرونده و مسیر «جایگزینی مستقیم» در pull (‎`wr(k,newStr)`‎) هیچ‌کدام `_costTomb` را چک نمی‌کنند ⇒ رکورد حذف‌شده از هر مسیر غیر-merge دوباره دیده می‌شود |

---

## ۱) «جمع کل هزینه‌ها» — سه عدد متفاوت برای یک چیز

### ۱.۱ نوار مالی بالای کشو (چیزی که کاربر می‌بیند)

`sfFinancialStrip` در `crm/salesfiles.js:1250-1263`:

```js
var costSum = (function (evs) {
  var seen = {};
  return (evs || []).filter(function (x) {
    if (x && (x.fromAdvance || x.cat === 'advance' || /پیش.?پرداخت|prepay|advance/.test(...))) return false;
    var k = x && (x.cd || x.pettyCd) || x;
    if (k && seen[k]) return false;
    if (k) seen[k] = 1;
    return true;
  }).reduce(function (s, x) { return s + (+x.amt || 0); }, 0);
})(r.costEvents);
```

یعنی جمع فقط از `r.costEvents` (مبلغِ اسنپ‌شاتِ لحظهٔ لینک) ساخته می‌شود.

### ۱.۲ فهرست هزینه‌های همان پرونده (تب «عملیات»)

فهرست از `pjCostEvents` رندر می‌شود که **علاوه بر** `r.costEvents`، ردیف‌های تنخواهِ «پروجکشن زنده» را هم دارد — `crm/salesfiles.js:1425-1428`:

```js
pjPettyAll.forEach(function (p) {
  if (!p || p.st === 'void' || p.dealRef !== r.cd) return;
  if (pjCostEvents.some(function (ce) { return (ce.pettyCd || (ce.fromPetty && ce.cd)) === p.cd; })) return;
  pjCostEvents.push({ cd: p.cd, pettyCd: p.cd, fromPetty: true, amt: p.amt, ... });
});
```

و مبلغ هر ردیف تنخواه از **رکورد زندهٔ تنخواه** خوانده می‌شود، نه اسنپ‌شات — `crm/salesfiles.js:1454`:

```js
var viewAmt = +(livePetty ? livePetty.amt : ce.amt) || 0;
```

### ۱.۳ ناهماهنگی‌های قطعی بین «جمع» و «فهرست»

| حالت | فهرست (عملیات) | نوار مالی (costSum) | نتیجه |
|---|---|---|---|
| تنخواهِ لینک‌شدهٔ قدیمی که `costEvent`‌اش جا افتاده (رکورد میراثی) | ردیف **نمایش داده می‌شود** (پروجکشن زنده) | **شمرده نمی‌شود** (فقط `costEvents`) | جمع کمتر از واقع |
| تنخواهِ لینک‌شده که بعداً در ماژول تنخواه مبلغش عوض شده | مبلغ **زنده** (`livePetty.amt`) | مبلغ **اسنپ‌شات** (`ce.amt`) | دو عدد متفاوت |
| هزینهٔ مستقیمِ معمولی | ردیف با `ce.amt` | همان `ce.amt` | سازگار |

نتیجه: کاربر ردیف‌هایی را می‌بیند که در «جمع» وجود ندارند، یا مبلغ ردیف با جمع نمی‌خواند — «جمع کل درست نیست» دقیقاً از همین‌جا می‌آید.

### ۱.۴ سه تابعِ متفاوتِ «جمع هزینه» در سطح اپ

| تابع | محل | فرمول | ددوب/فیلتر |
|---|---|---|---|
| `costSum` (نوار مالی) | `crm/salesfiles.js:1250` | فقط `costEvents` | ددوب با `cd`, حذف advance |
| `dealTotalCosts` | `crm/finance-core.js:123` و `crm/finance-helpers.js:155` | `costEvents + projectCosts + postArchiveCosts` | ددوب با `cd` |
| هوک سود `hookProjectCostsProfit` | `crm/buycompare.js:1080-1100` | `Math.max(dealCosts, archCosts)` | **بدون ددوب cd** (خط ۱۰۹۳ فقط `reduce` خام) |

سه عددِ متفاوت از یک پرونده ⇒ هر پنلی که کاربر باز کند ممکن است جمع متفاوتی نشان دهد. (نمایش سود/حاشیهٔ پرونده از v34.5.4 عمداً حذف شده — `salesfiles.js:154` — ولی موتور سال مالی و سود پروژه همچنان از توابع بالا می‌خورند.)

---

## ۲) «حذف هزینه + رفرش = برگشت هزینه»

### ۲.۱ مکانیزم tombstone که (درست) ساخته شده

v34.29.8 (`RELEASE-NOTES-v34.29.8.md`) برای «هزینه حذف‌شده برمی‌گردد» این‌ها را گذاشت:

- **همهٔ مسیرهای حذف، tombstone می‌نویسند:** `ptfDealCostTomb` (`crm/finance-write-guard.js:83-90`) → `deal._costTomb[cd] = ISO`. فراخوانی‌ها:
  - `ptfDealRemoveCost` — `crm/salesfiles.js:2462`
  - `ptfProjectCostDel` — `crm/buycompare.js:416`
  - `ptfDealCostSync` (انتقال/حذف لینک OPEX/تنخواه) — `crm/finance-write-guard.js:176`
  - `ptfPettyRemoveDealCostEvent` — `crm/petty.js:294`
- **merge کانونیکال، tombstone را اعمال می‌کند:** `crm/sync.js:2383-2413` — اجتماع `_costTomb` دو طرف + ددوب بر `cd` + حذف موارد tombstoneشده. رفت‌وبرگشت در `tester583` اثبات شده.
- **سرور `_costTomb` را نگه می‌دارد:** `api/sales-domain.php` → `entity_upsert` → `sd_entity_sanitize_row` (شاخهٔ map تودرتو، خطوط ~۳۳۸-۳۵۲) نقشهٔ `{cd: iso}` را حفظ می‌کند. `data_push` در `api/crm.php` هم کل JSON را دست‌نخورده می‌نویسد.

پس در **مسیر سالم** (حذف → ذخیره → merge در sync) حذف ماندگار است و tester583 این را پوشش می‌دهد.

### ۲.۲ حفرهٔ الف — tombstone فقط «مدافع merge» است؛ render و overwrite آن را نمی‌بینند

`_costTomb` **فقط و فقط** داخل `ptfMergeBusinessRecord` (یعنی مسیر `ptfSmartMerge`/canonical) اعمال می‌شود. هیچ‌کدام از مسیرهای زیر آن را چک نمی‌کنند:

1. **پروجکشن زندهٔ تنخواه در کشوی پرونده** — `crm/salesfiles.js:1425-1428`. شرط بازساخت فقط «`dealRef === r.cd` و نبودِ `costEvent`» است؛ **`_costTomb` دیده نمی‌شود**. اگر پس از حذفِ لینک، پاک شدن `dealRef` به سرور/دستگاه دیگر نرسیده باشد (یا ویرایش هم‌زمان در تنخواه `ptfPettySyncLinkedDeal` دوباره لینک کرده باشد)، ردیف با رفرش دوباره ساخته می‌شود — حتی اگر `_costTomb[cd]` موجود باشد.

2. **جایگزینی مستقیم در pull** — `crm/sync.js:1476-1478`:

   ```js
   if (state.dirty[k]) return;
   wr(k, newStr);   // ← جایگزینی خامِ محلی با نسخهٔ سرور، بدون ptfSmartMerge
   ```

   برای کلیدی که نه dirty است نه held و نه در initialReconcile، نسخهٔ سرور **بدون merge و بدون اعمال `_costTomb`** روی محلی نوشته می‌شود. اگر نسخهٔ سرور کهنه باشد (حذف به سرور نرسیده باشد)، `costEvents`ِ کهنه عیناً برمی‌گردد و `_costTomb` محلی هم رونویسی می‌شود.

3. **جاروب تعمیر یک‌باره است** — `ptfDealCostRepairSweep` (`crm/salesfiles.js:2488-2535`) فقط یتیم/تکرار را می‌زداید و با فلگ `cost_repair_v1` در `ptf_app_flags` **فقط یک‌بار در هر دستگاه** اجرا می‌شود. هزینهٔ «بعداً» برگشته را هیچ پروسه‌ای دوباره جارو نمی‌کند.

### ۲.۳ حفرهٔ ب — هزینهٔ «غیرتنخواه» حذف می‌شود ولی رندر به `costEvents` اعتماد می‌کند

برای هزینهٔ مستقیم (غیرتنخواه)، پروجکشنِ ۲.۲(۱) وجود ندارد؛ پس برگشت آن فقط از ۲.۲(۲) یا از هر نویسندهٔ کهنهٔ دیگری می‌آید که آرایهٔ `costEvents` را با نسخهٔ قدیمی جایگزین کند. چون **render همیشه `costEvents` را بدون عبور از `_costTomb` می‌خواند** (`salesfiles.js:1411` و `1250`)، هر `costEvents`ِ کهنه‌ای که از هر مسیری بنشیند، ردیف را دوباره نشان می‌دهد — مگر اینکه merge رخ داده باشد.

### ۲.۴ تفکیک ۲۰ ثانیه‌ای برای تأیید روی دستگاه واقعی

چون به دادهٔ واقعی/سرور وصل نبودم، پیش از اصلاح، این‌ها را روی دستگاهِ گزارش‌دهنده چک کنید:

1. **کنسول (تب پرونده):** `getData('ptf_crm_deals').filter(d=>d.cd==='<cd>')[0]._costTomb` — اگر پس از حذف، کلیدِ هزینه در این نقشه هست ولی ردیف بازمی‌گردد ⇒ حفرهٔ render/projection (۲.۲) فعال است، نه نبودِ tombstone.
2. **وضعیت سینک:** تنظیمات ← «وضعیت دستگاه» — اگر `ptf_crm_deals` dirty است یا نوار زرد/خطای آخرین pull وجود دارد ⇒ حذف هنوز به سرور نرسیده و مسیر ۲.۲(۲) محتمل است.
3. **مورد تنخواه:** روی ردیف برگشته ببینید تگ «🔗 از تنخواه» دارد یا نه. دارد ⇒ حفرهٔ پروجکشن (۲.۲(۱))؛ ندارد ⇒ مسیر overwrite/merge (۲.۲(۲)).

---

## ۳) آنچه درست کار می‌کند (دوباره خراب نشود)

- tombstone در **merge بین‌دستگاهی** (sync) درست است و tester583 سبز.
- حذف از کشوی پرونده (`ptfDealRemoveCost`) و حذف هزینهٔ مستقیم (`ptfProjectCostDel`) و حذف/ابطال تنخواه لینک‌شده همگی tombstone می‌نویسند.
- سرور `_costTomb` و `costEvents` را در `entity_upsert` و `data_push` دست‌نخورده نگه می‌دارد.
- ددوب نمایشی/جمعی در نوار مالی و `dealTotalCosts` بر `cd` هست (فقط هوک سود buycompare بدون ددوب است).

---

## ۴) پیشنهاد اصلاح (برای گام بعد — **هنوز انجام نشده**)

1. **یک فیلتر مشترک `ptfDealVisibleCosts(deal)`** که `costEvents` را با `_costTomb` فیلتر کند و هم در نوار مالی (`costSum`) و هم در فهرست عملیات (`pjCostEvents`) و هم در پروجکشن تنخواه استفاده شود — تا render هرگز هزینهٔ tombstone‌شده را نشان ندهد، صرف‌نظر از اینکه داده از کدام مسیر آمده.
2. **پروجکشن زندهٔ تنخواه را به `_costTomb` گره بزنید** (`salesfiles.js:1425-1428`): ردیف تنخواهی که `_costTomb[cd]` دارد حتی اگر `dealRef` هنوز ست باشد بازسازی نشود.
3. **مسیر جایگزینی pull را برای `ptf_crm_deals` از «replace» به «merge» عبور دهید** (`crm/sync.js:1476-1478`) یا پیش از `wr`، `_costTomb` را اعمال کنید — تا نسخهٔ کهنهٔ سرور، هزینهٔ حذف‌شده را برنگرداند.
4. **جاروب `cost_repair` را از «یک‌بار» به «هر بوتِ ارزان» ارتقا دهید** (یا فلگ نسخه‌دار) تا هزینه‌های برگشتهٔ پیشین هم پاک شوند.
5. **یکسان‌سازی سه تابع جمع** (نوار مالی / `dealTotalCosts` / هوک سود) روی یک قرارداد واحد (مثلاً همان `ptfDealVisibleCosts` + ددوب `cd`) و حذف `Math.max(dealCosts, archCosts)` در هوک سود.
6. **تستر رفتاری قفل‌شدهٔ تازه** (الگوی tester583 با `vm`): «حذف هزینهٔ مستقیم → pull با نسخهٔ کهنهٔ سرور → ردیف برنگردد» و «حذف لینک تنخواه → رندر → ردیف بازسازی نشود با وجود `dealRef` کهنه». ثبت در `_tools/uat/run-ci-gate.js`.

---

## ۵) محدودیت این ارزیابی

- به پروداکشن/استیجینگ و دادهٔ واقعی وصل نبودم؛ سناریوی دقیقِ دستگاه گزارش‌دهنده (۲.۴) باید روی همان دستگاه تأیید شود.
- PHP در این محیط نصب نیست؛ صحت سرور از خواندن کد + قراردادها سنجیده شد.
- گیت CI فعلی **۲۲۳ PASS / ۰ FAIL** است؛ هیچ تستی برای دو سناریوی این سند (جمعِ ناهمسان و بازگشتِ غیر-merge) وجود ندارد — دقیقاً همان نقطهٔ کور.
