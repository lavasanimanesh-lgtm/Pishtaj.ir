# ارزیابی اصلاح پرونده پس از برد، نشت سند برد، حذف مدارک و UX کشو

**تاریخ:** ۱۴۰۵/۰۵/۲۷ (۲۰۲۶-۰۸-۱۸)  
**نسخه محصول مبنای بررسی:** v34.7.33  
**محدوده:** فقط ارزیابی و پیشنهاد — **هیچ کدی در این مرحله تغییر نکرده است.**  
**شاخه نشست:** `arena/01a01190-pishtaj-ir`

---

## ۱. درخواست کارفرما (خلاصه)

گاهی پیشنهاد برنده می‌شود و حتی فاکتور صادر می‌شود؛ سپس کارفرما پروژه را لغو می‌کند یا سفارش را به‌کلی عوض می‌کند. باید بتوان **در همان پرونده** با یک کلیک رویژن زد: افزودن/حذف قلم، تغییر قیمت، یا بستن پرونده. بازنگری فعلی چون قلم جدید ندارد سخت است. همهٔ مدارک پرونده (پکینگ، نوت بازرسی و غیره) باید قابل حذف باشند. علت «یک پیشنهاد برنده با دو سند برد که یکی مال پروندهٔ دیگر است» باید روشن شود. UX کشوی پرونده فروش هم باید سبک‌تر شود.

---

## ۲. وضعیت فعلی بازنگری سند برد (P5)

| لایه | محل | رفتار امروز |
|---|---|---|
| UI | `crm/case-revision.js` → `ptfAwardReviseOpen` | جدول فقط از اقلام **سند برد فعلی**. چک‌باکس «حفظ» + qty/price. **ردیف جدید وجود ندارد.** اگر همه برداشته شوند submit رد می‌شود («حداقل یک قلم»). |
| API | `api/sales-domain.php` action `revise_award` | سند قبلی `superseded`؛ سند جدید `<no>-R<n>` با `revisionOf` / `revisionSeq`. نقش `SD_WIN_ROLES` + دلیل اجباری. |
| قفل فاکتور رسمی | همان action + ترجمه در UI | اگر فاکتور رسمی فعال باشد **کاهش مبلغ مسدود** است (`official_invoice_blocks_decrease`) — تصمیم کارفرما ۱۴۰۵/۰۵/۲۶. مسیر درست: ابطال/اصلاحیه فاکتور سپس بازنگری. |
| قفل پیشنهاد زنده | `offers.js` `offerPostAwardLocked` + سرور `won_offer_locked` | ویرایش مستقیم برد عمداً قفل است؛ تنها مسیر مجاز `revise_award` است. |

نتیجه: «رویژن» از نظر حسابداری هست، اما از نظر کارفرما **رویژن سفارش** نیست (نمی‌توان قلم تازه آورد یا پرونده را با صفر قلم بست).

---

## ۳. چرا یک پیشنهاد برنده دو سند برد دارد / سند پروندهٔ دیگر دیده می‌شود

چند مسیر مستقل اثبات می‌شود؛ هر کدام به‌تنهایی کافی است.

### ۳.۱ نشت فنی از روی `inqNo` — قطعی‌ترین باگ

`sfAwardEnsure` در `crm/salesfiles.js` (حدود ۱۵۸–۱۷۷):

- اگر `awardDocs` خالی باشد، از `wonOffer` snapshot می‌سازد.
- پیشنهاد فنی را این‌طور پیدا می‌کند:
  1. `o.srcToNo` اگر باشد؛
  2. وگرنه **اولین TO** با `coNo === o.no` **یا** `(o.inqNo && x.inqNo === o.inqNo)` مرتب‌شده بر `rev` نزولی.

اگر دو پرونده فروش روی **یک استعلام** باشند (CO موازی / پروندهٔ دوم از `win_offer` وقتی `sd_case_offer_linked` برقرار نیست)، TO پروندهٔ الف روی سند برد پروندهٔ ب می‌نشیند.

`ptfSalesFileOffers` (۷۸–۱۰۶) همهٔ offerهای همان `inqNo` به‌علاوه زنجیره دوطرفه `altOf`/`srcToNo`/`coNo` را جمع می‌کند. این برای بایگانی عمدی است (BUG-ARCHIVE ۱۴۰۵/۰۸/۱۰) ولی در کشوی زنده پیشنهاد/سند پروندهٔ موازی را هم نشان می‌دهد.

### ۳.۲ دو منبع ساخت `awardDocs`

| منبع | چه می‌نویسد |
|---|---|
| `autoCreateProjectFromCO` (`offers.js` ~۷۷۷–۸۲۰) | snapshot کامل CO + TO مرتبط |
| `win_offer` سرور (`sales-domain.php` ~۸۴۰) | فقط `{kind:won_snapshot, offerNo, total}` بدون snap کامل و بدون TO |
| `sfAwardEnsure` | اگر آرایه خالی باشد مهاجرت نرم؛ اگر **غیرخالی** باشد **دست نمی‌زند** |

پروندهٔ ساخته‌شده از سرور یک `won_snapshot` بدون `snap` دارد → `sfAwardEnsure` دیگر TO را اضافه نمی‌کند؛ چاپ ممکن است بشکند. پروندهٔ قدیمی client-side ممکن است CO+TO کامل داشته باشد. اگر همان `wonOffer` روی دو رکورد deal باشد، هر دو جعبه «اسناد قطعی برد» را نشان می‌دهند.

### ۳.۳ پس از `revise_award` جعبه زرد به‌روز نمی‌شود

سرور `wonOffer` را به `-R1` عوض می‌کند ولی **`awardDocs` موجود را بازنویسی/فیلتر نمی‌کند**. UI همهٔ `awd` را چاپ می‌کند (مالی + فنی) بدون فیلتر روی `wonOffer` فعلی → «دو سند برد» روی یک پرونده.

### ۳.۴ تطبیق رکورد فقط با `cd`

`sfAwardEnsure` رکورد را با `x.cd === r.cd` پیدا می‌کند. اگر رکورد سروری `_id` دارد و `cd` فرق دارد، write روی رکورد غلط می‌رود یا fail می‌شود.

### ۳.۵ نمایش

باکس زرد «اسناد قطعی برد» (`sfDrawerHtml` ~۱۰۲۷) همهٔ عناصر `awardDocs` را ردیف می‌کند؛ نقش `won_snapshot` در مقابل `CO`/`TO` جدا فیلتر نمی‌شود.

**ابزار پاکسازی پیشنهادی (بعد از تأیید، نه در این نشست):** اسکریپت تشخیصی فقط‌خواندنی که برای هر deal، `awardDocs[].no` را با `wonOffer` و `inqNo` خود پرونده و `srcToNo` همان CO مقایسه کند و موارد نشتی را لیست کند — بدون mutate تا کارفرما تأیید کند.

---

## ۴. ماتریس حذف مدارک (وضعیت امروز)

| نوع مدرک | حذف؟ | محل | نکته |
|---|---|---|---|
| متفرقه `r.docs` | بله + دلیل + ابر | `sfDelMisc` | — |
| رویداد پکینگ / بارنامه / تحویل | بله + دلیل | `sfShipDelete` | مرحله `sfStageOf` دوباره محاسبه می‌شود |
| فایل روی رویداد ship/QC | بله | `sfEventFileDelete` | — |
| رویداد QC | بله | `sfQcDelete` | — |
| نامه | بله | `letDel` | — |
| استعلام تامین | بله | `rfqsDel` | — |
| هزینه پرونده | بله | `ptfDealRemoveCost` | — |
| **docsx رسمی PL / IN / IB / MOM** | **خیر** | `crm/docsx.js` ~۳۹۰ فقط 👁 و ✏️ | `ptfDocxDelete` وجود ندارد |
| سند برد `awardDocs` | عمداً غیرقابل حذف | فقط چاپ | تاریخچه حسابداری |
| فاکتور رسمی | ابطال / اصلاحیه نسخه‌دار | نه حذف فایل خام | — |
| فاکتور غیررسمی | ابطال ریشه‌کن | — | — |

صدور PL رسمی (`ptfDocxCommit`) هم‌زمان `sfShipCommit(..., 'packing')` می‌زند. حذف docsx بدون حذف/باطل کردن رویداد packing مرحله را ناسازگار می‌کند. نوت بازرسی رسمی نتیجه QC نیست؛ QC جداست (`SF_QC_TYPES`).

**پیشنهاد حذف docsx:** فرمان سروری با نقش + دلیل؛ سند `void`/`superseded` بماند نه پاک فیزیکی؛ اگر PL است رویداد packing متناظر هم باطل شود؛ پوشش `ptfDocxCoverage` دوباره حساب شود.

---

## ۵. زیرساخت لازم برای رویژن کامل سفارش (افزودن/حذف قلم + بستن)

باز کردن فقط UI کافی نیست. وابستگی‌ها:

### ۵.۱ مالی

- فاکتور رسمی فعال → کاهش مبلغ همچنان طبق تصمیم قبلی **مسدود** مگر ابطال/اصلاحیه اول.
- فاکتور غیررسمی: یا ابطال زنجیره یا صدور متمم/جایگزین هم‌تراز `correct_invoice` / `void_*`.
- دریافت قطعی `post_receipt` و تخصیص FIFO باید پس از تغییر مبلغ **بازسازی** شود (`sd_rebuild_allocations` همین حالا در `revise_award` صدا می‌شود — باید برای add-line و close-cancel هم بماند).
- مطالبات `PTF.ar` و بستانکاری مشتری روی `caseId` (نه شماره پیشنهاد خام).
- قفل سال مالی: پرونده سال بسته‌شده نباید مبلغ مؤثر را عوض کند مگر سند اصلاحی سال جاری.

### ۵.۲ خرید و پوشش اقلام

- خرید واقعی `buycmp` / پوشش اقلام نسبت به CO/TC.
- docsx coverage (`ptfDocxCoverage`) روی اقلام پرونده.
- QC قلم‌به‌قلم `deal.inspections[]` و `ptf_crm_purchase_returns` + تهاتر تامین.

افزودن قلم: پوشش خرید/PL/IN ناقص می‌شود — مرحله نباید به‌اشتباه «کامل» بماند.  
حذف قلمی که خرید یا PL دارد: یا disposition اجباری (انبار / عودت / اسقاط) یا مسدودی تا تعیین تکلیف.

### ۵.۳ گردش پرونده

- `sfStageOf` مشتق از شواهد است؛ حذف شاهد مرحله را عقب می‌کشد مگر شاهد بعدی تکمیل شده باشد.
- `contractAmount` / `effectiveContractAmount` باید منبع واحد بماند.
- پورسانت / سرمایه در گردش / سال مالی از **فاکتور فعال** تغذیه می‌شوند نه از `contractAmount` خام (تصمیم قبلی محصول).

### ۵.۴ بستن پرونده لغوشدهٔ کارفرما (حتی پس از فاکتور)

امروز: `sfClose` اگر فاکتور دارد مسیر تسویه است؛ اگر ندارد `sfCloseLost` با دلیل استاندارد (US-349).

برای «کارفرما پروژه را لغو کرد»:

1. نقش + دلیل اجباری (`employer_cancelled`).
2. اگر فاکتور رسمی فعال است: اول `void`/`correct` فاکتور (نه دور زدن گارد کاهش).
3. اگر دریافت قطعی هست: سند بستانکاری / عودت وجه — نه پاک کردن receipt.
4. سند برد: رویژن به مبلغ صفر **یا** وضعیت `cancelled` روی case بدون ساخت offer تهی (ترجیح: case `status=cancelled` + `wonOffer` superseded و `effectiveContractAmount=0`).
5. خرید باز: هشدار اقلام خریداری‌شده بدون disposition.
6. بایگانی با snapshot کامل (`awardDocs` همراه پرونده — الان در `sfArchive` هست).

---

## ۶. راهکار پیاده‌سازی پیشنهادی (پس از تأیید — فازبندی)

کد در این نشست زده نشود.

### فاز A — ایمنی و تشخیص نشت (بدون تغییر رفتار کسب‌وکار)

1. ابزار فقط‌خواندنی: لیست dealهایی که `awardDocs` شامل `no` خارج از زنجیره `wonOffer`/`srcToNo` همان پرونده است، یا دو deal با یک `wonOffer`.
2. فیلتر نمایش جعبه زرد: current commercial = `wonOffer` فعلی؛ فنی فقط اگر `srcToNo` همان CO یا `coNo` صریح همان CO باشد — **نه** «هر TO با همان inqNo».
3. `sfAwardEnsure`: اگر `awardDocs` فقط `won_snapshot` بدون snap است، مهاجرت کامل را کامل کند نه early-return؛ تطبیق رکورد با `_id || cd`.

### فاز B — گسترش `revise_award` (نه فرمان موازی مگر لازم شود)

گسترش همان action (اتمی بودن + correction + rebuild تخصیص حفظ شود):

- بدنه: `lines[]` می‌تواند شامل قلم جدید (`isNew: true` + pcode/name/qty/price) باشد.
- قلم حذف‌شده = نبودن در `lines` (مثل امروز) + اگر خرید/PL دارد `disposition` اجباری.
- سیاست فاکتور:
  - بدون فاکتور رسمی: آزاد (افزایش/کاهش/add).
  - با فاکتور رسمی و **کاهش یا حذف قلم فاکتورشده**: همان گارد فعلی مگر `voidOfficialFirst`.
  - با فاکتور رسمی و **فقط افزودن قلم / افزایش مبلغ**: مجاز + یادآوری صدور اصلاحیه/فاکتور متمم (صدور خودکار نکنیم مگر کارفرما بخواهد).
- پس از موفقیت: `awardDocs` را با snapshot سند جدید **جایگزین زنجیرهٔ فعال** کند؛ اسناد قدیمی در `awardRevisions[]` بمانند نه در جعبه زرد به‌عنوان «قطعی جاری».
- UI: دکمه «+ افزودن قلم» در `ptfAwardReviseOpen` + جستجوی کالا؛ حداقل صفر قلم فقط در مسیر «لغو کارفرما» نه در همین فرم عادی.

### فاز C — بستن لغو کارفرما

فرمان `cancel_case_by_employer` (جدا از `revise_award` تهی): orchestration فاکتور → دریافت → خرید باز → status. دکمه در کشو کنار مختومه‌سازی، نه جایگزین آن.

### فاز D — حذف مدارک رسمی

`ptfDocxVoid` با دلیل؛ PL → باطل packing مرتبط؛ IB/MOM بدون اثر مرحله ارسال. awardDocs و فاکتور از این مسیر حذف نشوند.

### فاز E — UX کشو (بدون از دست دادن قابلیت)

کشوی فعلی: استپر ۱۲تایی + چند باکس رنگی + ۱۰+ کاشی post-award + بلوک docsx + buycompare.

پیشنهاد ساختار سه زبانه داخل همان کشو:

1. **خلاصه و مرحله** — استپر + مبلغ مؤثر + وضعیت مالی یک خط + اقدام بعدی (`sfStageGuidance`).
2. **اسناد** — پیشنهادها (فقط زنجیرهٔ همین پرونده، نه همهٔ inqNo) / برد جاری / docsx / متفرقه / نامه؛ هر ردیف مشاهده / اصلاح / حذف‌یا‌ابطال طبق ماتریس.
3. **عملیات** — گروه‌بندی شده نه کاشی تخت:
   - مالی (دریافت، فاکتور غیررسمی، ارجاع فاکتور)
   - اجرا (خرید واقعی، QC، پکینگ رویداد، بارنامه، تحویل، بازرسی قلم)
   - قرارداد (بازنگری سند برد گسترش‌یافته، گزارش بازنگری، لغو کارفرما، مختومه)

کاشی‌های تکراری fallback (`!_dxDealsHooked` / `!_rbDealsHooked`) در UI عادی پنهان بمانند.

بازنگری: یک دکمهٔ واضح «رویژن سفارش» روی زبانه قرارداد؛ توضیح کوتاه که سند قبلی بایگانی می‌شود.

---

## ۷. ریسک‌ها اگر بدون فاز A سراغ UI برویم

- افزودن قلم روی پرونده‌ای که `awardDocs` نشتی دارد، TO غلط را مبنای پوشش می‌کند.
- دو deal با یک `wonOffer` هر دو رویژن می‌خورند یا قفل همدیگر را می‌شکنند.
- حذف PL بدون packing، استپر را دروغین جلو نگه می‌دارد.

---

## ۸. آنچه در این نشست انجام نشد (عمدی)

- هیچ پچ محصول، تستر یا workflow.
- هیچ mutate روی دادهٔ زنده.
- پیاده‌سازی فاز A–E منوط به تأیید کارفرماست.

## ۹. سؤال‌های تصمیم برای کارفرما

1. با فاکتور رسمی فعال، **افزایش مبلغ / قلم جدید** بدون اصلاحیه فاکتور مجاز باشد یا خیر؟
2. لغو کارفرما: پرونده `cancelled` بماند در لیست با فیلتر، یا مثل `sfCloseLost` به بایگانی برود؟
3. پاکسازی نشت موجود: فقط نمایش را درست کنیم یا `awardDocs` ذخیره‌شده را هم با ابزار تأییدشده اصلاح کنیم؟
