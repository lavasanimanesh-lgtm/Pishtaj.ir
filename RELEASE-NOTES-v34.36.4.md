# RELEASE NOTES — v34.36.4 (INTEGRITY-TRUTHFUL: گیتِ صحتِ پس از استقرار؛ پایانِ قرمزِ کاذب)

**تاریخ:** ۲۰۲۶-۰۹-۰۴ · **گیت:** سبز (۲۰۸ ورودی) · **تسترهای جدید:** `tester592-v34.36.4-deploy-integrity-truthful.js` (۸۴ سنجه) و `tester593-v34.36.4-deploy-delta-failsafe.js` (۲۷ سنجه) — هر دو **اجرای واقعیِ** بلوک‌های CI با `curl`/`sleep` ساختگی و بدونِ شبکه
**پرونده‌های تغییر:** `.github/workflows/deploy-staging.yml` · `.github/workflows/deploy-production.yml` · `_tools/ci/workflow-templates/*` (همسان با آن‌ها) · `_tools/uat/bump-version-pins.js` · `_tools/uat/tester538-…` · `_tools/uat/tester591-…` + نشانگرهای نسخه

---

## گزارش کارفرما و تصمیم‌ها

چکِ `deploy` روی PR #3 قرمز شد در حالی که `gate` سبز بود و سایتِ استیجینگ واقعاً همان کامیت را سرو می‌کرد. تصمیم‌های کارفرما (پرسشِ مستقیم، پیش از کد):

| پرسش | تصمیم |
|------|-------|
| چکِ قرمزِ `deploy` چطور مدیریت شود؟ | **رفعِ ریشه‌ایِ خودِ گیت در همین PR** (نه کامیتِ خالی، نه پذیرفتنِ قرمزی) |
| دیپلوی پروداکشن؟ | **فعلاً نه** — فقط استیجینگ؛ پروداکشن همچنان دستی است (`workflow_dispatch` + تایپِ `DEPLOY`) |

---

## ریشه‌یابی (با شواهدِ ران)

سه رانِ پیاپی، دقیقاً در یک گام قرمز شدند و در همه‌شان آپلودِ FTP، «Verify deployment (FTP-side + live HTTP)» و انتشارِ `__diag__.txt` سبز بود:

| ران | شاخه/کامیت | گامِ قرمز |
|-----|-----------|-----------|
| `33909238999` | PR #3 · `arena/01a06c26-pishtaj-ir` · `d5552c6` | Post-deploy integrity check |
| `33866519467` | `main` | Post-deploy integrity check |
| `33866324482` | PR #2 (مرج‌شده) | Post-deploy integrity check |

و در همان لحظه سایتِ زنده درست بود: `crm/sw.js` زنده `RELEASE = 'v34.36.3'` (نسخهٔ همان کامیت) و بنرِ استیجینگ «کامیت: `d5552c6`».

**علت، شبکه نبود — یک باگِ قطعی در خودِ اسکریپت بود:**

```bash
FILES="crm/sw.js crm/shell.js crm/client-server.js crm/key-registry.js crm/sales-domain-v2.js"   # ← یک خط، فاصله‌دار
while IFS= read -r f; do
  want="$(sha1sum "$f" | cut -d' ' -f1)"
  got="$(curl -fsSL "$BASE/$f?v=$(date +%s)" | sha1sum | cut -d' ' -f1)"
  …
done <<< "$FILES"
```

`IFS=` یعنی «بدونِ تقسیم بر فاصله»، پس `read` **کلِ رشته را یک «نام فایل»** می‌گیرد و حلقه فقط **یک‌بار** می‌زند:

1. `sha1sum "crm/sw.js crm/shell.js …"` → چنین فایلی نیست ⇒ خطا ⇒ `want` **تهی**؛
2. `curl` همان «نام» را می‌خواهد ⇒ ۴۰۴ و با `-f` بدنهٔ **تهی** ⇒ `got` = `da39a3ee5e6b4b0d3255bfef95601890afd80709` (= هشِ رشتهٔ خالی)؛
3. `want != got` ⇒ گیت **بی‌قیدوشرط قرمز** — برای هر استقرار، روی هر شاخه‌ای، حتی وقتی همه‌چیز درست است.

الگویِ درست از ابتدا در مخزن بود: `BYTES_FILES` **چندخطی** در `_tools/ci/verify-deploy.sh` (همان گامی که سبز می‌شد). دو وصلهٔ معوق هم برای همین نوشته شده بود و به‌دلیلِ نداشتنِ مجوزِ `workflows` اعمال نشده بود:
`_tools/PENDING-workflow-deploy-staging-speedup-v34.8.40.patch` و `_tools/PENDING-workflow-integrity-hardening-v34.8.37.patch` (و `_tools/ci/FIX-workflow-integrity.patch`).

یک علتِ دوم هم در کار بود: حتی با فهرستِ درست، «**پاسخ نگرفتیم**» (TLS/تایم‌اوت/۴۰۴ ⇒ بدنهٔ تهی) و «**محتوا فرق دارد**» هر دو یک پیامِ `≠` داشتند و هر دو به یک `exit 1` می‌رسیدند — پس نه ریشه‌یابی ممکن بود، نه تفکیکِ «کشِ کهنه» از «دیپلویِ ناقص».

---

## تغییرات

### ۱) اعمالِ دو وصلهٔ معوق (طراحیِ خودِ مخزن)

- **استیجینگ — `PENDING-workflow-deploy-staging-speedup-v34.8.40`** (اعمال و سپس از مخزن حذف شد؛ محتوا الآن در workflow است):
  - گیتِ صحت **دولایه** شد: **لایهٔ ۱** readback بایت‌به‌بایتِ ۵ فایلِ حساس + مارکرِ `__deploy__.txt` **مستقیم از FTP** (مسدودکننده، مستقل از کشِ مسیرمحورِ هاست) و **لایهٔ ۲** تازگیِ HTTP زنده با پنجرهٔ ۱۲×۳۰s و هدرهای `no-cache` (**فقط هشدار**).
  - نتیجهٔ عملی: دیپلویِ درست دیگر به‌خاطرِ کهنگیِ کشِ HTTP قرمز نمی‌شود، ولی دیپلویِ واقعاً ناقص همچنان **fail-closed** است (TRUTHFUL-GREEN).
  - همراهش **DEPLOY-SPEEDUP** هم آمد (چون لایهٔ ۱ به `steps.delta.outputs.proto` نیاز دارد): محاسبهٔ دلتا از مارکرِ رانِ قبل + `git diff`، آپلودِ افزایشی فقطِ فایل‌های تغییرکرده، حذفِ فایل‌های حذف‌شده با `DELE` سمتِ سرور، همگام‌سازیِ کاملِ هفتگی (`cron` شنبه)، ورودیِ دستیِ `full`، و **بازگشتِ خودکار به حالتِ full** اگر آپلودِ افزایشی شکست بخورد.
- **پروداکشن — `PENDING-workflow-integrity-hardening-v34.8.37`**: `FILES` چندخطی + انتظارِ اولیهٔ ۱۸۰s برای پیر شدنِ کشِ مسیرمحور + پنجرهٔ ۱۲×۳۰s + هدرهای `no-cache` و cache-buster دوبل. (این وصله در مخزن **نگه داشته شد** چون `tester538` برای شاخهٔ استیجینگ به آن ارجاع دارد.)
- `_tools/ci/FIX-workflow-integrity.patch` حذف شد: وصلهٔ حداقلیِ همین باگ بود و با اصلاحِ کاملِ بالا کنارگذاشته شد (نگه‌داشتنش تلهٔ «اعمالِ دوباره» بود).
- **قالب‌ها همسان شدند:** `_tools/ci/workflow-templates/deploy-{staging,production}.yml` بایت‌به‌بایت با `.github/workflows/` یکی‌اند. ریشهٔ واگراییِ تاریخی همین بود که یک منطق در دو کپی زندگی می‌کرد و وصلهٔ پروداکشن به استیجینگ نرسید؛ `tester592` این همسانی را پین می‌کند.

### ۲) سخت‌سازیِ اضافهٔ v34.36.4 (روی همان طراحی)

**لایهٔ ۱ استیجینگ (مسدودکننده):**
- اگر فایلی در چک‌اوت نباشد، پیامِ صریحِ «⛔ … مشکلِ داخلیِ گیت، نه استقرار» می‌آید (پیش‌تر `want` تهی می‌شد و به‌شکلِ «ناهمسانی» گزارش می‌شد).
- **تلاشِ مجددِ فایل‌به‌فایل** (۳ بار، ۵s): یک سکسکهٔ FTP نباید دیپلویِ درست را قرمز کند.
- **تفکیکِ علت:** «بدنهٔ تهی/انتقال ناموفق» در برابرِ «محتوا متفاوت است» — با اندازهٔ بایتِ کامیت و سرور و **اولین تفاوتِ محتوا** در لاگ.
- **fallback پایان‌خط:** اگر تفاوت فقط `\r` باشد (انتقالِ ASCII سمتِ سرور) **هشدار** است نه شکست — همان منطقِ `verdict` در `verify-deploy.sh` که آنجا فقط تشخیصی بود و اینجا تعیین‌کننده است.
- چکِ مارکرِ `__deploy__.txt` هم تلاشِ مجدد گرفت (پیش‌تر **تک‌تلاش** و مسدودکننده بود — یک بار بدنهٔ تهی = قرمز).

**لایهٔ ۲ استیجینگ (هشدار):**
- «پاسخ نداد / بدنهٔ تهی» از «HTTP هنوز کهنه» جدا شد و **شمارِ هر دسته** هم در `::warning::` و هم در خلاصهٔ گام می‌آید (`کهنه=N · بی‌پاسخ/تهی=N · فقط-پایان‌خط=N`).
- تفاوتِ فقط پایان‌خط، «کهنه» حساب نمی‌شود.

**پروداکشن (HTTP-محور، مسدودکننده):**
- همان تفکیکِ سه‌گانه + fallback پایان‌خط + گزارشِ اندازه‌ها و اولین تفاوت.
- پیامِ پایانیِ `::error::` حالا **دستهٔ علت** را می‌گوید: «ناهمسانی محتوا: N مورد · بدون پاسخ/تهی: N مورد» و اگر دستهٔ دوم غالب باشد راهنمایِ عملی دارد (دسترس‌پذیری/کشِ لبه، نه محتوا ⇒ یک‌بار اجرای دوباره).
- اگر تفاوت فقط پایان‌خط باشد: سبز + `::warning::` با توصیهٔ انتقالِ باینریِ FTP.

### ۳) ابزارِ bump و پین‌ها

- `_tools/uat/bump-version-pins.js` — **دو نقطهٔ کورِ واقعی** رفع شد (هر دو در همین انتشار گیت را قرمز کردند):
  1. شکلِ «دوبل-escape» (`34\\.36\\.3` در متنِ فایل، یعنی پین‌هایی که با `new RegExp('…')` ساخته می‌شوند) پوشش نداشت ⇒ پینِ کش‌باسترِ `tester591` روی نسخهٔ قبل می‌ماند؛
  2. **هویتِ فایل** از **پینِ نسخه** جدا نبود: نامِ تسترها (`tester591-v34.36.3-….js`) هم عوض می‌شد، پس ارجاعِ «در گیت ثبت شده است» به فایلِ واقعی اشاره نمی‌کرد. حالا نام‌ها محافظت می‌شوند و شمارشان گزارش می‌شود.
- `tester538`: پینِ «پنجرهٔ integrity گسترش یافت» برای استیجینگ حالا **ساختارِ دولایه** را هم می‌پذیرد (readback مسدودکننده + هشدارِ کش HTTP)، چون `sleep 180` در استیجینگ اتلافِ محض است (لایهٔ ۱ اصلاً از HTTP نمی‌گذرد). پین سست‌تر نشد: صریحاً هر دو لایه را می‌خواهد.
- `tester591`: پینِ کش‌باستر از نسخهٔ رسمیِ `VERSION.json` مشتق می‌شود (همان قراردادِ A6، بدونِ واگراییِ دستی) و پینِ هویتِ فایل به نامِ واقعی برگشت.

### ۴) باگی که **اولین اجرای واقعیِ** DEPLOY-SPEEDUP لو داد (run `33914510099`)

وصلهٔ v34.8.40 تا امروز هرگز اجرا نشده بود (معطلِ مجوزِ `workflows` بود). نخستین اجرای واقعی‌اش در همین PR، یک خطای **زمانِ اجرا** در گامِ «Compute deploy delta» را لو داد که هیچ پینِ ایستایی و حتی `bash -n` هم نمی‌گرفتند:

```bash
echo "آپلود: $(wc -l < /tmp/upload.txt) فایل | حذف: $(([ -f /tmp/delete.txt ] && wc -l < /tmp/delete.txt || echo 0)) فایل"
                                                    ^^ ارزیابیِ حسابی به‌جای جانشینیِ فرمان
```

`$(( … ))` یعنی «ارزیابیِ حسابی»، پس `[ -f … ]` درونش یک عبارتِ حسابیِ نامعتبر است و bash هنگامِ **ارزیابی** منفجر می‌شود — زیرِ `set -euo pipefail` گام همان‌جا مرد. از آن‌سو `mode=incremental` پیش‌تر در `GITHUB_OUTPUT` نوشته شده بود، پس:

- گامِ آپلودِ افزایشی **skip** شد (شرطِ ضمنیِ `success()`)، و
- همگام‌سازیِ کامل هم اجرا **نشد** (شرطش `mode == 'full'` بود)

⇒ **هیچ فایلی دیپلوی نشد** و ران قرمز ماند. این خطرناک‌ترین شکلِ شکست است: نه قرمزیِ کاذب، بلکه «قرمزِ بی‌دیپلوی».

**رفع‌ها (همه با `tester593` به‌صورت رفتاری قفل شدند):**

1. شکلِ درست: `$( … )` به‌جای `$(( … ))` (به‌همراهِ شمارشِ تمیز با `tr -d ' '`).
2. گامِ دلتا `continue-on-error: true` گرفت — **محاسبهٔ** دلتا هرگز نباید جلوی خودِ دیپلوی را بگیرد.
3. سپرِ «دلتایِ تهی»: اگر `/tmp/upload.txt` خالی یا ناپدید باشد، صریح به `mode=full` برمی‌گردیم (آخرین مقدارِ output در رانر می‌برد).
4. گامِ افزایشی فقط وقتی اجرا می‌شود که دلتا **کامل و موفق** باشد (`steps.delta.outcome == 'success'`).
5. شرطِ همگام‌سازیِ کامل از «فقط `mode == 'full'`» به **«هر چیزی جز مسیرِ افزایشیِ موفق»** تغییر کرد:
   `mode != 'incremental' || steps.delta.outcome == 'failure' || steps.inc.outcome == 'failure'`
   — عمداً **بدونِ** `always()`، پس گیتِ CI قرمز همچنان جلوی دیپلوی را می‌گیرد.

> درسِ عمومی‌تر: پینِ ایستایی (regex روی YAML) و `bash -n` هیچ‌کدام این را نمی‌گرفتند؛ فقط **اجرای واقعیِ بلوک** در یک ریپوی gitِ ایزوله گرفت. به همین دلیل `tester593` اضافه شد.

---

## جدولِ درستیِ گیت (آنچه `tester592` با اجرای واقعیِ بلوک‌ها اثبات می‌کند)

| وضعیتِ واقعی | استیجینگ | پروداکشن |
|--------------|----------|----------|
| دیسک/سایت == کامیت | ✅ سبز | ✅ سبز |
| دیسک درست، HTTP مرده یا کهنه (کشِ لبه) | ✅ **سبز** + `::warning::` با شمارشِ دسته‌ها | 🔴 قرمز (HTTP-محور است) با علتِ «بدون پاسخ/تهی: N» |
| دیسک/سایت != کامیت (نسخهٔ مخلوط، دیپلویِ ناقص) | 🔴 قرمزِ مسدودکننده + `want/got` + اولین تفاوت | 🔴 قرمز + «ناهمسانی محتوا: N» |
| تفاوتِ فقط پایان‌خط (`\r`) | ✅ سبز + هشدار | ✅ سبز + هشدارِ «انتقال را باینری کنید» |
| سکسکهٔ لحظه‌ایِ FTP/HTTP | ✅ سبز پس از تلاشِ مجدد | ✅ سبز در تلاشِ بعدیِ پنجرهٔ ۱۲×۳۰s |
| مارکرِ `__deploy__.txt` اشتباه | 🔴 قرمز (۳ تلاش) | — |
| چک‌اوتِ ناقص (فایلِ محلی نیست) | 🔴 قرمز با پیامِ «مشکلِ داخلیِ گیت» | 🔴 قرمز با همان پیام |

---

## سازگاری (بدونِ تغییر)

- **هیچ تغییرِ رفتاری در CRM نیست** — این انتشار فقط زیرساختِ استقرار و ابزارِ گیت را لمس کرد. تمامِ قراردادهای v34.36.1 تا v34.36.3 (همگراییِ «انتقال یک‌باره»، رسیدِ پیوستِ تامین‌کننده، تعمیرِ JSON هوش مصنوعی، پنل‌های تاشو + جست‌وجو) سرِ جای خود و بدونِ regression.
- گیت‌های پیشینِ استقرار دست‌نخورده‌اند: `arch-guard.js` و `run-ci-gate.js` **پیش از** FTP (مسدودکننده)، `verify-deploy.sh` (سمتِ FTP + HTTP)، انتشارِ `__diag__.txt`، و در پروداکشن ترتیبِ «تاییدِ صحت **پیش از** تگِ نسخه و بک‌آپ».
- استیجینگ همچنان `noindex` + بنرِ محیطِ تست + `crm/.htaccess` با `no-cache` (و هیچ‌کدام به پروداکشن نشت نمی‌کند).
- `api/migrate.php` و `api/data-health-check.php` همچنان از انتقالِ پروداکشن مستثنی‌اند (H1).
- **پروداکشن خودکار نشد:** فقط `workflow_dispatch` با تایپِ `DEPLOY`.

---

## آزمون

- **`tester592` (جدید، در گیت): ۸۴ PASS / ۰ FAIL** — بلوکِ واقعیِ `run:` از دلِ YAML بیرون کشیده می‌شود، عبارت‌های `${{ … }}` جایگزین می‌شوند و با `curl`/`sleep` ساختگی (بدونِ هیچ شبکه) در ۱۲ سناریو **اجرا** می‌شود؛ یعنی رفتارِ همان کدی سنجیده می‌شود که در CI اجرا خواهد شد، نه بازنویسیِ آن:
  - هر ۵ فایلِ استیجینگ **جداگانه** readback می‌شوند ⇒ اثباتِ رفعِ باگِ `FILES` تک‌خطی (همچنین ۷ فایلِ پروداکشن)؛
  - سناریوی «دیسک درست + HTTP مرده» ⇒ `exit 0` + `::warning::کش HTTP استیجینگ` و **بدونِ** هیچ `::error` — دقیقاً همان قرمزِ کاذبِ سه رانِ بالا؛
  - سناریوی «دیسک کهنه» ⇒ `exit 1` + `::error::فایل‌های روی سرور (FTP readback)` + `want/got` + اندازه‌ها + اولین تفاوت، و لایهٔ ۲ اجرا نمی‌شود؛
  - تفاوتِ فقط پایان‌خط ⇒ سبز (و در پروداکشن با هشدارِ عملیاتی)؛ سکسکهٔ FTP ⇒ سبز پس از تلاشِ مجدد؛ مارکرِ اشتباه ⇒ قرمز؛ چک‌اوتِ ناقص ⇒ قرمز با پیامِ «مشکلِ داخلیِ گیت»؛
  - هر دو بلوک زیرِ `bash -n` و سه سنجه هم زیرِ `bash -e` (پوستهٔ پیش‌فرضِ رانر) همان قضاوت را می‌دهند؛
  - پینِ ساختاری: نبودِ دوبارهٔ `FILES="a b c"` تک‌خطی و نبودِ الگوی شکنندهٔ `curl … | sha1sum`؛ همسانیِ بایت‌به‌بایتِ قالب‌ها با workflowها.
- **`tester593` (جدید، در گیت): ۲۷ PASS / ۰ FAIL** — گامِ «Compute deploy delta» را در **ریپوی gitِ ایزولهٔ موقت** (دو کامیت با دلتای معلوم: تغییر/افزودن/حذف + مسیرهای exclude‌شده) با `curl` ساختگی **اجرا** می‌کند:
  - مارکرِ معتبر ⇒ `mode=incremental` + فهرستِ آپلودِ درست (فایلِ تغییرکرده + فایل‌های تولیدیِ هر ران) + excludeهای `_tools`/`*.md`/`.github`/`tester*.js` + فهرستِ حذفِ سرور (`gone.txt` و بس)؛
  - مارکر با SHA ناشناخته ⇒ `full` · نبودِ مارکر ⇒ `full` · FTP مرده ⇒ `proto=none` + `::error::اتصال FTP برقرار نشد` و exit 1 (که با `continue-on-error` به مسیرِ `full` برمی‌گردد)؛
  - رویدادِ `schedule` ⇒ `full` حتی با مارکرِ معتبر · ورودیِ `full=true` ⇒ `full` · `full=false` ⇒ همان `incremental`؛
  - مسیرِ غیرِ ASCII در دلتا ⇒ هشدار + `full` (چون `curl` URL-Safe نیست)؛
  - و **رگرسیونِ همان باگِ `$(( … ))`**: گزارشِ «آپلود: N فایل | حذف: M فایل» باید واقعاً چاپ شود.
- **پین‌های پیشینِ workflow سبز ماندند (سست‌تر نشدند):** `tester536` ۶۰/۰ (T0-3/T0-4/T0-6) · `tester538` ۱۱/۰ · `tester540` **۲۹/۰** (SPEEDUP + TRUTHFUL-GREEN در حالتِ «اعمال‌شده» + ۵ پینِ تازهٔ fail-safe: شرطِ جدیدِ fallback، نبودِ `always()` در آن، `continue-on-error` گامِ دلتا، سپرِ «دلتایِ تهی»، و شکلِ درستِ گزارشِ شمارش).
- **گیت CI: ۲۰۸ PASS / ۰ FAIL** (۲۰۶ ورودیِ پیشین + `tester592` + `tester593`) · `arch-guard`: PASS (A6 = ۰ ناهماهنگی نسخه در نقاطِ رسمی) · هر ۲۶ بلوکِ `run:` هر دو workflow با `bash -n` سالم.
- نسخه در همهٔ نقاطِ رسمی ارتقا یافت: `VERSION.json` · `crm/index.html` (`PTF_CRM_RELEASE`/`VER` + ۱۰۹ کش‌باستر) · `crm/sw.js` (`RELEASE`/`ASSET_VERSION`/`CACHE`) · `crm/shell.js` · `crm/manifest.json` · `crm/clear-cache.html` · `crm/cms.js` (`PTF_CMS_JS_VER`) · `api/sales-domain.php` (`SD_SERVICE_VERSION`) + ۷۰۶ پینِ تسترها.

---

## اقدام لازم پس از استقرار

1. **هیچ اقدامی سمتِ CRM لازم نیست** (تغییرِ رابط/رفتار نداریم). فقط یک رفرشِ سختِ معمول اگر نسخهٔ `?v=34.36.4` را می‌خواهید.
2. **دیپلوی بعدیِ استیجینگ را تماشا کنید:** انتظار این است که گامِ «Post-deploy integrity check» با «✅ لایهٔ ۱: بایت‌های دیسک سرور == کامیت (readback FTP)» سبز شود؛ اگر کشِ HTTP دیر تازه شد، فقط `::warning::کش HTTP استیجینگ …` می‌بینید و ران سبز می‌ماند.
3. **اگر قرمز شد، حالا علت از خودِ پیام معلوم است:** «FTP readback» ⇒ دیپلویِ واقعاً ناقص (فایل‌ها روی سرور فرق دارند) · «بدون پاسخ/تهی» ⇒ دسترس‌پذیری/کشِ لبه · «ناهمسانی محتوا» ⇒ نسخهٔ مخلوط.
4. **پروداکشن** طبقِ تصمیمِ کارفرما **فعلاً دست‌نخورده** می‌ماند؛ هر وقت خواستید، از `Actions → Deploy to Production` با ورودیِ `DEPLOY` (و گیتِ صحتِ سخت‌شدهٔ همین نسخه) اجرا می‌شود.
5. **حالتِ استقرار را در خلاصهٔ ران ببینید:** `| حالت | incremental — آپلود دلتا (N فایل) |` یا `full — همگام‌سازی کامل`. هر چیزی جز «مسیرِ افزایشیِ موفق» به‌صورت خودکار `full` می‌شود، پس «دیپلویِ ناقصِ بی‌صدا» ممکن نیست؛ اگر روزی خواستید همگام‌سازیِ کاملِ دستی بزنید، `workflow_dispatch` ورودیِ `full` دارد (و شنبه‌ها `cron` خودش full می‌کند).
