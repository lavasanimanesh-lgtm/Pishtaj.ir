# بازبینی دقیق فرایند و ورک‌فلو مالی — شناسایی باگ‌ها

**برنچ:** `arena/019fcd1d-pishtaj-ir` (v34.0.7-alpha)
**دامنه:** مسیر کامل پول (فاکتور/وصول ← چک ← تنخواه ← هزینه‌جاری ← سهامدار ← سود سال مالی ← تقسیم)
**روش:** ردیابی کد ماژول‌های مالی (fiscal.js، opex.js، shareholders.js، cheque-module.js، cheque-panel.js، petty.js، supplier-finance.js، working-capital.js) + آزمایش عددی.

> ⚠️ این بازبینی **فقط‌خواندنی** است؛ تغییری در کد اعمال نشده. شدت هر باگ مشخص شده تا بعداً با تأیید شما اصلاح شود.

---

## خلاصهٔ باگ‌ها

| # | شدت | عنوان | فایل:خط |
|---|---|---|---|
| F-01 | 🔴 بالا | افتتاحیهٔ نقد سال مالیِ اشتباه (بدون گِیت سال) → سود نقدی/قابل تقسیم غلط | `fiscal.js` (بلاک `openingCash`) |
| F-02 | 🟠 متوسط | «حقوق سهامدار» به‌عنوان علی‌الحساب سود در ستون «ماندهٔ قابل تسویه» حساب می‌شود | `shareholders.js`+`fiscal.js:340` |
| F-03 | 🟠 متوسط | چک واردهٔ «انتقالی به تامین‌کننده» (endorsed) مطالبهٔ مشتری را تسویه نمی‌کند → درآمد سمت مشتری ثبت نمی‌شود | `cheque-module.js:73-86` |
| F-04 | 🟠 متوسط | چک صادرهٔ پنل چک `supplierPaymentCd` ندارد → با فاکتور تامین‌کننده در خروجی نقد دوباره‌شماری می‌شود | `cheque-panel.js`+`fiscal.js:321` |
| F-05 | 🟠 متوسط | حقوقِ تعلّق‌گرفته ولی پرداخت‌نشده به‌عنوان خروجی نقد کسر می‌شود → موجودی/قابل تقسیم کم‌نمایش | `fiscal.js` (`out.opex`) |
| F-06 | 🟡 پایین | هزینهٔ تنخواهِ لینک‌شده به پرونده در سود نقدی (خروجی) دیده نمی‌شود (فقط در تعهدی) → واگرایی تعهدی/نقدی | `fiscal.js:141-154` vs `fiscal.js:281` |
| F-07 | 🟡 پایین | دو عدد «سود نهایی» متفاوت: گزارش/قفل از تعهدی، داشبورد/تقسیم از نقدی | `fiscal.js` |

---

## F-01 (بالا) — افتتاحیهٔ نقدِ سال مالیِ اشتباه در سود نقدی

**شواهد:** در `ptfFiscalCashData(year)`:

```js
var openingCash = 0;
try {
  if (typeof window.ptfFinanceOfficialData === 'function') {
    var fp = window.ptfFinanceOfficialData();
    if (fp && fp.opening) openingCash = +fp.opening.cash_bank || 0;
  }
} catch (e) {}
```

- `ptfFinanceOfficialData()` (working-capital) از **یک** پیکربندی دستیِ سال مالی (`config()` در snaps) می‌آید.
- هیچ بررسی `cfg.fiscalYear === year` انجام نمی‌شود.
- در حالی که در **سود تعهدی** (`ptfFiscalData`) همین داده فقط وقتی وارد می‌شود که سال منطبق باشد (`financialPosition` با گارد `String(fp.cfg.fiscalYear) === String(year)`).

**اثر:** اگر داشبورد سال مالی سالِ ۱۴۰۴ را نشان دهد ولی پیکربندی working-capital برای ۱۴۰۵ باشد، `openingCash` مربوط به ۱۴۰۵ به محاسبهٔ ۱۴۰۴ اضافه می‌شود → `cashEnd`، «مازاد بر کف»، و **مبلغ قابل تقسیم** نادرست. ناسازگار با رفتار تعهدی (که گیت سال دارد).

**اصلاح:** `openingCash` را فقط وقتی بگیر که `String(fp.cfg.fiscalYear) === String(year)` (یا در نبود پیکربندیِ همان سال، صفر بگذار).

---

## F-02 (متوسط) — حقوق سهامدار به‌عنوان علی‌الحساب در «ماندهٔ قابل تسویهٔ امسال»

**شواهد:**
- پرداخت حقوق سهامدار با همان تابع برداشت/علی‌الحساب ثبت می‌شود: `ptfShareDraw` → `addTx('draw', ...)` (`shareholders.js:288`).
- در `ptfFiscalCashDistribution` ستونِ علی‌الحساب (`fiscal.js:340`):
  ```js
  advRows = sharetx.filter(x => (x.type==='draw'||x.type==='advance'||x.type==='debit') && fiscalYearOf(...)===year);
  ...
  settleYear = gross - adv
  ```

**اثر:** چون **پرداخت حقوق** یک `draw` است، از «سهم ناخالص» کسر و در ستون «ماندهٔ قابل تسویهٔ امسال» برای سهامدار موظف منفیِ گمراه‌کننده نشان می‌دهد. (ثبت نهایی تقسیم از `gross` استفاده می‌کند پس مبلغ پرداختی واقعی تغییر نمی‌کند؛ ولی نمایش/منطق «حقوق=هزینه» با «برداشت=علی‌الحساب سود» در هم آمیخته است.)

**اصلاح:** نوع جداگانهٔ `salary_payment` برای پرداخت حقوق، و حذف آن از فیلتر `advRows` در `ptfFiscalCashDistribution`.

---

## F-03 (متوسط) — چک واردهٔ «انتقالی» مطالبهٔ مشتری را تسویه نمی‌کند

**شواهد:** در `ptfChequeEndorse` (`cheque-module.js:73-86`):
- فقط `ptfChequeApplyIssued(c)` صدا می‌شود (ایجاد پرداخت به تامین‌کننده).
- هیچ `ptfChequeApplyReceived`/پرداخت روی فاکتور مشتری ساخته نمی‌شود.
- `c.st='endorsed'` (نه `cleared`).

**اثر:** وقتی چکِ مشتری را به تامین‌کننده منتقل می‌کنیم (پرداخت بدهی با چک وارده):
- سمت تامین‌کننده: پرداخت (SFPAY) ساخته می‌شود → بدهی تامین‌کننده کاهش می‌یابد.
- سمت مشتری: فاکتور مشتری **باز می‌ماند** و درآمد/وصول آن در سود نقدی ثبت نمی‌شود → **مطالبات مشتری بیش‌نمایش و سود نقدی کم‌نمایش**.

**اصلاح:** در `ptfChequeEndorse` هم اثر مالی سمت مشتری (تسویهٔ فاکتور/وصول با همان چک) اعمال شود؛ یا انتقال فقط از چک‌های بدون تعهد به فاکتور باشد و وصول پس از انتقال در سمت مشتری ثبت شود.

---

## F-04 (متوسط) — چک صادرهٔ پنل چک فاقد `supplierPaymentCd` → دوباره‌شماری خروجی

**شواهد:**
- چک صادرهٔ «مالی» از پنل چک با `supplierCd` ساخته می‌شود ولی `supplierPaymentCd` روی آن ست نمی‌شود (`cheque-panel.js:317-322`).
- `ptfChequeApplyIssued` هم SFPAY می‌سازد ولی `supplierPaymentCd` را روی چک برنمی‌گرداند (`cheque-module.js:224-233`).
- در `ptfFiscalCashData` (خروجی‌ها):
  ```js
  if (c.supplierPaymentCd) return;          // ← همیشه رد می‌شود چون ست نشده
  out.independentCheques += +c.amt || 0;    // ← چک به‌عنوان «مستقل» شمرده می‌شود
  ```
  و هم‌زمان فاکتور تامین‌کننده در `out.supplierInvoices` نیز کسر می‌شود.

**اثر:** اگر همان چک صادره بابت فاکتوری باشد که در supplier-finance ثبت شده، همان بدهی **دو بار** در خروجی نقد شمرده می‌شود (یک‌بار فاکتور + یک‌بار چک) → خروجی بیش‌نمایش → سود نقدی کم‌نمایش.

**اصلاح:** در `ptfChequeCreate('issued')` مقدار `supplierPaymentCd` (خروجی `ptfChequeApplyIssued`) را روی رکورد چک ست کن.

---

## F-05 (متوسط) — حقوقِ پرداخت‌نشده به‌عنوان خروجی نقد کسر می‌شود

**شواهد:** `out.opex = d.opexTotal` (کلِ opex بدون لینک پرونده، شامل حقوق سهامدار) بدون بررسی اینکه حقوق واقعاً **نقد پرداخت شده** از `receipts` کم می‌شود (`fiscal.js:281,325`).

**اثر:** اگر شرکت حقوق ماهانه را تعلّق بدهد ولی به‌دلیل کمبود نقد پرداخت نکند (بستانکاری در حساب سهامدار می‌ماند)، مبلغ در خروجی نقد شمرده و `cashEnd`/قابل تقسیم کم‌نمایش می‌شود.

**اصلاح:** یا فقط حقوقِ پرداخت‌شده در خروجی نقد لحاظ شود، یا این رفتار صریحاً مستند/معرفی شود.

---

## F-06 (پایین) — هزینهٔ تنخواهِ لینک‌شده به پرونده در خروجی نقد دیده نمی‌شود

**شواهد:**
- تنخواهِ با `dealRef` در `d.costEvents` می‌رود و در **سود تعهدی** پروژه کسر می‌شود (`petty.js:315-320`، `fiscal.js:141-154`).
- اما در سود **نقدی**، `out.petty` فقط «تنخواهِ مستقل» را می‌گیرد (`fiscalPettyStandalone` با `if (p.dealRef) return`) و هزینهٔ مستقیم پرونده (جز از طریق فاکتور تامین‌کننده) در خروجی نقد نیست (`fiscal.js:281`).

**اثر:** هزینهٔ نقدیِ تنخواهِ مربوط به پرونده (مثلاً حمل/اداری که نقد پرداخت شده) در محاسبهٔ سود نقدی/قابل تقسیم نادیده می‌شود → واگرایی تعهدی/نقدی و سود نقدی کمی بیش‌نمایش.

**اصلاح:** تصمیم بگیر که هزینه‌های مستقیم پرونده (کامل، نه فقط فاکتور تامین‌کننده) چگونه در خروجی نقد منعکس شوند.

---

## F-07 (پایین) — دو عدد «سود نهایی» متفاوت

**شواهد:**
- گزارش رسمی و پیام قفل سال از **تعهدی** استفاده می‌کنند: `netProfit = projectProfit − opex − petty + amend` (`fiscal.js:192,524`).
- داشبورد و تقسیم از **نقدی** استفاده می‌کنند: `netCash = receipts − outflows`، تقسیم از مازاد بر کف (`fiscal.js:325-352`).

**اثر:** «سود خالص» در گزارش رسمی/قفل با «سود نقدی/قابل تقسیم» داشبورد یکی نیست؛ اگر وصولی‌ها با درآمدِ تعهدی فاصله داشته باشد، دو عدد متفاوت گزارش می‌شود و ممکن است باعث سردرگمی یا تقسیمِ نادرست شود (با F-01..F-06 تشدید می‌شود).

---

## توصیهٔ کلی
این باگ‌ها بیشتر به «سود نقدی/تقسیم» (F-01..F-05) مربوط‌اند که **مبنای پرداخت واقعی سود است**، پس ریسک عملیاتی‌شان بالاتر است. پیشنهاد می‌شود اولویت اصلاح:
1. **F-01** (افتتاحیهٔ سال) — مستقیم و کم‌ریسک.
2. **F-04** (دوباره‌شماری چک صادره) و **F-03** (تسویهٔ مشتری در انتقال چک).
3. **F-02** (تفکیک پرداخت حقوق از علی‌الحساب).
4. **F-05/F-06/F-07** — نیازمند تصمیم مدیریتی/حسابداری.

بعد از تأیید شما، هر مورد را پیاده‌سازی و با تستر UAT پوشش می‌دهم.

*تولیدشده توسط Arena Agent — بازبینی فقط‌خواندنی؛ تغییر کد اعمال نشده.*
