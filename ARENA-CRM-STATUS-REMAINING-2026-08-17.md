# ارزیابی دقیق آخرین وضعیت CRM — باگ‌های باقی‌مانده

- **تاریخ:** ۱۴۰۵/۰۵/۲۶ (۲۰۲۶-۰۸-۱۷)
- **نسخهٔ رسمی:** `v34.7.32` (`VERSION.json`) — P7/S4/ARCH-03 در همین نشست بسته شد.
- **مبنای کد:** `arena/01a01190-pishtaj-ir` روی `ed822ff` (هم‌تراز `main` پس از PR #49)
- **نوع کار:** فقط ارزیابی. هیچ کدی در این نشست تغییر نکرد.

---

## ۰) حکم یک‌خطی

از نظر **باگ عملیاتی روزمرهٔ فروش/فاکتور/نشت حساب**، موج اخیر (v34.7.19 تا v34.7.31) تقریباً بسته شده است. آنچه «باز» مانده عمدتاً یکی از این چهار دسته است: **تصمیم کارفرما**، **پاکسازی دادهٔ واقعی**، **پروژهٔ معماری**، یا **بهداشت مهندسی** — نه دکمهٔ خرابِ قطعی مثل ابطال/صدور غیررسمی.

**نمرهٔ سلامت محصول (عملیات):** حدود **B+ / ۸۳ از ۱۰۰**  
**آمادگی انتشار خودکار بدون کنترل دستی:** هنوز **C+** (گیت CI روی GitHub اعمال نشده)

---

## ۱) چه چیزی همین حالا بسته است (تا دوباره کار نشود)

| کد | موضوع | نسخه |
|---|---|---|
| AR-01 | ابطال محلی غیررسمی، `creditRemainIRR` کل سیستم را بازنویسی می‌کرد | v34.7.19 |
| AR-02 | `correct_invoice` فیلدهای `pays`/سررسید/ارز را پاک می‌کرد | v34.7.19 |
| UI-01/02/03 | دیالوگ غیررسمی باز نمی‌شد؛ صدور مجدد مبلغ را ذخیره نمی‌کرد؛ تعریف دوگانهٔ قفل برد | v34.7.20 |
| LC-01/02/03 | `superseded` و مرجوعی در پورسانت/سرمایه در گردش/سال مالی | v34.7.21 |
| AW-01/02/03 + OPS-01 | متمم ≤۰، پروندهٔ دوم بی‌صدا، کشف وابستگی حذف، نسخهٔ health | v34.7.22 |
| INV-01 | ابطال سروری `void_unofficial_invoice` | v34.7.23 |
| AN-01…03, 05…08 | بازهٔ زمانی گزارش، نرخ برد واحد، واحد فرصت، خرید واقعی، گیت دفتر AI، تقویم شمسی | v34.7.24 |
| OPS-02 | تریاژ سوئیت: ۰ شکست واقعی در رانر جدید | v34.7.25 |
| S1/S3/S5 | ابطال/اصلاح از کشوی پرونده؛ ۴ مسیر نشت پیش‌پرداخت؛ تکرار دکمه | v34.7.26 |
| P1–P3 | بارگذاری اقلام درخواست + زنجیرهٔ نرخ مرجع | v34.7.30 |
| P4–P6 | بازرسی قلم‌به‌قلم، `revise_award`، گزارش بازنگری | v34.7.31 |

سوئیت کامل طبق یادداشت v34.7.31: **۴۱۷ تستر — ۳۵۸ سبز، ۵۹ قرنطینهٔ مستند، ۰ شکست واقعی**.  
گیت CI محلی: **۴۹ PASS / ۲ FAIL** که هر دو `known-open` فاز G هستند (`tester403`, `tester415`) — رگرسیون محصول نیستند.

---

## ۲) باگ / کار باز که واقعاً مانده

### الف) نیاز به شما (داده یا تصمیم) — بدون این‌ها کد درست کامل نمی‌شود

| # | مورد | شدت | چرا باز است | کار بعدی |
|---|---|---|---|---|
| **S4** | پاکسازی رکوردهایی که *قبلاً* از مسیر نشت پیش‌پرداخت آلوده شده‌اند | بحرانی **اگر در پروداکشن دادهٔ آلوده باشد** | ابزار تشخیص (`diagnose-advance-leak.js`) آماده است؛ هیچ رکورد مالی بدون تأیید بازنویسی نمی‌شود | خروجی S2 روی `crm/data` واقعی + تأیید فهرست ردیف‌ها |
| **AN-04** | `healthScore` فقط شفاف شده، وزن‌ها کالیبره نشده | پایین (گزارش مدیریتی) | نیازمند تصویب ضرایب | یک جدول وزن یا «چارک» |
| **P7** | تخصیص بستانکاری عودت به **فاکتور خرید مشخص** | متوسط | اثر پایه (بستانکاری تأمین‌کننده) در P4 هست | اگر لازم است: تخصیص به فاکتور خرید |
| **ARCH-01 باقی** | مدل کامل جایگزینی قرارداد (کاهش پس از فاکتور رسمی، متمم کاهشی تجاری) | بالا ولی پروژه‌ای | `revise_award` آمده؛ کاهش پس از فاکتور رسمی **عمداً مسدود** است | سند تصمیم تجاری، نه هات‌فیکس |
| **ARCH-02** | سه مسیر نوشتن پول: `post_receipt` / `savePay` / ماژول چک | متوسط-بالا | یکپارچه‌سازی عمداً عقب افتاده | تصویب + پوشش تست کامل |

### ب) نقص فنی باز (قابل کدنویسی، بدون تصمیم بزرگ)

| # | مورد | شدت | شاهد |
|---|---|---|---|
| **H1** | وصلهٔ workflow (`php -l` + گیت UAT پیش از FTP) روی GitHub اعمال نشده | عملیاتی | `refusing to allow a GitHub App to create or update workflow` — فایل‌ها در `_tools/PENDING-workflow-*.patch` آماده‌اند |
| **ARCH-03** | `awardDocs` در مسیر سروری برد تضمین نمی‌شود (lazy) | پایین | snapshot برد در `revise_award` هست؛ پرونده‌های قدیمی ممکن است lazy بمانند |
| **TODO مرده در مسیر legacy** | در انتهای `ptfUnofficialInvoiceVoid` هنوز TODO «اگر endpoint اضافه شد صدا بزن» مانده | پایین / نویز نگهداشت | مسیر V2 *الان* سرور را صدا می‌زند؛ این TODO فقط مسیر بدون V2 است و گمراه‌کننده است |
| **tester403 / tester415** | دو تستر `known-open` فاز G | پایین (تست، نه محصول) | ادعاهای معماری v35 که با گاردهای بعدی عوض شده؛ باید بازنویسی شوند نه «سبز اجباری» |

### ج) بدهی ساختاری (باگ روزمره نیست؛ ریسک میان‌مدت است)

| مورد | توضیح |
|---|---|
| بک‌آپ offsite تأییدنشده | MySQL + JSON روی همان هاست؛ INC-A4 هنوز باز |
| تراکنش چندکلیدی مالی | چک + رویداد + بدهی چند `ptf_kv` جدا بدون commit گروهی |
| CSP با `unsafe-inline` / `unsafe-eval` | عمق دفاع XSS ضعیف |
| ۵۹ تستر قرنطینه | سیگنال سوئیت تمیز است ولی پوشش تاریخی پینِ UI/نسخه است |
| بوت سنگین | ~۱۰۰ اسکریپت eager، `index.html` ~۳۲۰KB، بدون bundle |
| ریشهٔ مخزن | صدها Markdown نسخه‌ای؛ منبع حقیقت گم می‌شود |

---

## ۳) چه چیزی را «باگ باز» حساب نکنید

این‌ها قبلاً گزارش شده‌اند و **در کد فعلی بسته**اند. اگر روی پروداکشن هنوز دیده می‌شوند، یا کش PWA کهنه است (`v34.7.31` باید در `sw.js`/`index.html` باشد) یا دادهٔ آلودهٔ پیش از S3 است (→ S4):

- دکمهٔ ابطال/اصلاح فاکتور رسمی از کشوی پرونده بی‌اثر است
- دیالوگ صورتحساب غیررسمی «پرونده یافت نشد»
- پیش‌پرداخت مشتری الف در حساب مشتری ب *از مسیر کد جدید*
- قیمت پیش‌فاکتور از پیشنهاد فنی صفر می‌شود
- «بارگذاری اقلام درخواست» هیچ کاری نمی‌کند
- ابطال غیررسمی بستانکاری همهٔ پرونده‌ها را خراب می‌کند

---

## ۴) اولویت پیشنهادی برای رفع باقی‌مانده

1. **همین هفته، بدون کد محصول:** اجرای `node _tools/diagnose-advance-leak.js` روی دادهٔ پروداکشن → تصمیم S4.
2. **همین هفته، دسترسی گیت‌هاب:** اعمال دستی دو patch گردش‌کار (H1) تا استیجینگ بدون گیت نرود.
3. **اسپرینت بعد (اگر بخواهید):** P7 تخصیص عودت به فاکتور خرید + پاک کردن TODO گمراه‌کنندهٔ legacy.
4. **فقط پس از تصویب شما:** ARCH-02 (یک مسیر پول) و تکمیل مدل کاهش قرارداد.
5. **موازی کم‌ریسک:** بک‌آپ offsite + تمرین بازیابی؛ کاهش تدریجی قرنطینهٔ تست.

---

## ۵) محدودیت این ارزیابی

- به پروداکشن/استیجینگ و دادهٔ واقعی وصل نبودم؛ S4 را نمی‌توانم «داده آلوده است / نیست» قطعی بگویم.
- PHP در این محیط نصب نیست؛ صحت runtime سرور با خواندن کد و تسترهای قرارداد سنجیده شد.
- سوئیت کامل در این نشست دوباره اجرا نشد؛ اعداد از یادداشت انتشار v34.7.31 و اسناد هم‌روز است.

---

*اگر بخواهید، قدم بعدی منطقی S4 (تشخیص روی داده) یا H1 (اعمال workflow با دسترسی شما) است — نه بازنویسی معماری.*
