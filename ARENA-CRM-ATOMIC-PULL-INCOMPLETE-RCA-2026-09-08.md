# RCA — «دریافت کامل CRM انجام نشد» تکرارشونده و بارگذاری‌نشدن درخواست‌های تأمین

- تاریخ بررسی: ۲۰۲۶-۰۹-۰۸ (۱۴۰۵/۰۶/۱۷)
- نسخهٔ درگیر: **v34.38.10** (کامیت «Implement atomic CRM snapshot sync»)
- نسخهٔ اصلاح: **v34.38.11**
- فایل‌های درگیر: `crm/sync.js` (مسیر `ptfSyncPullAtomic`)، `api/crm.php`
  (`data_manifest` / `data_chunk`)
- تستر: `_tools/uat/tester618-v34.38.11-atomic-pull-completion.js` (در گیت CI ثبت شد)

---

## ۱) گزارش کارفرما

> «این پیغام دائم می‌آید و پس از چند ثانیه تکرار می‌شود؛ یعنی چه اتفاقی می‌افتد که کامل
> نمی‌شود دریافت اطلاعات، خصوصاً درخواست‌های تأمین کامل بارگذاری نمی‌شود:
> **دریافت کامل CRM انجام نشد؛ آخرین snapshot سالم حفظ شد و تلاش مجدد خودکار ادامه دارد.**»

## ۲) پاسخ کوتاه (چه اتفاقی می‌افتد)

این پیام دقیقاً یک جا تولید می‌شود: شکست **اولین pull کامل (bootstrap)** در
`initialSync()` داخل `crm/sync.js`. در نسخهٔ v34.38.10 مسیر bootstrap از «pull اتمیک»
تازه‌ای عبور داده شد (`data_manifest` + `data_chunk`) و این مسیر روی دادهٔ واقعی
شرکت **همیشه fail-closed** می‌شد. طبق طراحی، شکست یعنی «هیچ چیز نوشته نشود»؛ بنابراین:

1. هیچ کلیدی از سرور روی مرورگر اعمال نمی‌شد — کاربر فقط نسخهٔ محلی/کهنه (یا خالی، روی
   دستگاه تازه) می‌دید. درخواست‌های تأمین (`ptf_crm_rfqs`) چون بزرگ‌ترین و پرتغییرترین
   مجموعه‌اند، بیشترین نمود را داشتند.
2. `state.bootstrapped` هرگز `true` نمی‌شد و `initialSync()` هر ۵ ثانیه دوباره اجرا و
   همان توست را تکرار می‌کرد ⇒ «پیام دائم می‌آید و پس از چند ثانیه تکرار می‌شود».
3. عارضهٔ جانبی مهم: `pushDirty()` با شرط `if (!state.bootstrapped) { schedulePush(); return; }`
   منتظر می‌ماند؛ یعنی تا وقتی این حلقه ادامه دارد **هیچ تغییر محلی هم به سرور نمی‌رود**
   (نوار زرد «تغییر ذخیره‌نشده» و ریسک از دست رفتن کار روی همان دستگاه).

## ۳) ریشه‌های فنی (سه مورد، هر سه واقعی)

### ریشهٔ ①: صحت‌سنجی cross-language — hash‌ای که مرورگر هرگز نمی‌تواند بازتولید کند

`data_manifest` برای هر کلید `canonicalSha256` می‌داد:

```php
$canonical = json_encode(json_decode($payload, true), JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES);
'canonicalSha256' => hash('sha256', $canonical)
```

و کلاینت این را با بازتولید مرورگری مقایسه می‌کرد:

```js
var assembled = JSON.stringify(rows);              // آرایه‌ها
return ptfVerifyCanonicalPayload(assembled, spec, key);
...
return ptfVerifyCanonicalPayload(d.value, spec, key); // آبجکت‌ها: رشتهٔ خام سرور!
```

این مقایسه در دنیای واقعی شکست می‌خورد، چون خروجی `json_encode` پی‌اچ‌پی و
`JSON.stringify` مرورگر هم‌ارز نیستند:

| مورد | PHP | مرورگر | نتیجه |
|---|---|---|---|
| عدد اعشاری صحیح (`round()` مالی) | `1500000.0` | `1500000` | hash متفاوت |
| آبجکت خالی `{}` | `json_decode(assoc)` ⇒ `[]` ⇒ `[]` | `{}` | hash متفاوت + **تغییر شکل داده** |
| عدد صحیح خیلی بزرگ | float ⇒ `1.2345678901234568e+18` | `1234567890123456800` | hash متفاوت |
| کلید آبجکتی نوشته‌شده توسط فرمان دامنه | فایل با `JSON_PRETTY_PRINT` ذخیره شده | canonical فشرده | **همیشه** متفاوت |

مورد آخر قطعی و تکرارپذیر است: `api/sales-domain.php::sd_publish_changes()` همهٔ
projectionها را با `JSON_UNESCAPED_UNICODE | JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES`
می‌نویسد و `api/management-report-cron.php` هم همین کار را می‌کند. برای کلیدهای
**آبجکتی** (مثل `ptf_crm_supplier_finance`) کلاینت رشتهٔ خامِ pretty را با hashِ نسخهٔ
فشرده مقایسه می‌کرد ⇒ `snapshot_checksum_mismatch:ptf_crm_supplier_finance` در
**هر بار** بوت، برای نقش‌هایی که این کلید را دارند (admin/chairman/ceo/commercial/
accountant/buyer).

عارضهٔ دوم همین ریشه (حتی وقتی hash تصادفاً می‌خواند): مسیر ردیفی، داده را از سرور
decode و در مرورگر دوباره encode می‌کرد؛ یعنی `{}` داخل رکوردها به `[]` تبدیل و روی
حافظهٔ محلی نوشته می‌شد — دستکاری خاموش شکل داده.

### ریشهٔ ②: «همه یا هیچ» بودن manifest

سرور برای کلیدی که فایلش نبود یا JSON خراب داشت این را می‌فرستاد:

```php
$dm_keys[$dm_k] = ['available' => false, 'rev' => ...];   // بدون canonicalSha256
```

ولی کلاینت شرط می‌گذاشت «همهٔ کلیدهای فهرست باید hash معتبر داشته باشند»:

```js
keys.some(function (k) { return !specs[k] || !/^[a-f0-9]{64}$/i.test(String(specs[k].canonicalSha256 || '')); })
  ⇒ finish({ ok: false, reason: 'manifest_integrity_invalid' })
```

یعنی **یک** کلید معیوب کل دریافت ۶۵ کلید را برای همیشه می‌بست.

### ریشهٔ ③: livelock روی snapshot سراسری (چرا «چند ثانیه بعد باز تکرار»)

هر chunk به rev سراسری pin شده بود:

```php
if ($dc_snapshot !== '' && $dc_snapshot !== 'S-' . $dc_rev) { http_response_code(409); ... 'snapshot_changed' }
```

pull کامل یعنی ۱ درخواست manifest + ده‌ها درخواست chunk (۴ worker موازی). روی CRM
چندکاربره، rev سراسری با هر نوشتن — حتی یک لاگ `ptf_crm_audit` از کاربر دیگر — جلو
می‌رود. کافی بود در این چند ثانیه یک نوشتن رخ دهد تا همهٔ chunkها 409 بگیرند؛ و
`maxAttempts = 2` یعنی فقط یک تلاش دوباره، آن هم از صفر و بدون حفظ پیشرفت. روی دیتاست
واقعی این عملاً یعنی «هرگز کامل نمی‌شود».

### چرا هیچ‌کدام در تست دیده نشد

تستر نسخهٔ قبل (`tester617`) فقط **متن سورس** را می‌سنجید (وجود `case 'data_manifest'`،
وجود `maxAttempts`, …) و هیچ اجرای واقعی کلاینت/سرور با دادهٔ نمونه نداشت. هر سه ریشه
فقط با اجرای واقعی روی payload واقعی خود را نشان می‌دادند.

## ۴) اصلاحات انجام‌شده (v34.38.11)

### سرور — `api/crm.php`

1. **`data_chunk?mode=bytes`**: برش دقیق بایتیِ همان رشته‌ای که `data_pull` می‌فرستد،
   با هم‌ترازی مرز UTF-8 (هرگز وسط کاراکتر فارسی/ایموجی بریده نمی‌شود)، به‌همراه
   `bytesTotal`, `sha256`, `nextOffset`, `done`. مسیر ردیفی برای سازگاری عقب‌رو ماند.
2. **تحمل drift**: کلاینت `keyRev` و `archiveRev` را pin می‌کند؛ اگر rev سراسری عوض شده
   ولی rev همان collection و rev بایگانی سنگ‌قبر دست‌نخورده باشد، همان بایت‌ها با
   `drift: true` سرو می‌شوند. فقط تغییر واقعیِ collection/بایگانی 409 می‌گیرد (با فیلد
   `changed` برای تشخیص).
3. **manifest تاب‌آور**: کلید ناخوانا از `keyList` خارج و در `snapshot.unavailable`
   (با `reason`) گزارش می‌شود؛ `snapshot.degraded` علامت می‌خورد. برای کلیدهای سالم
   `payloadSha256` (hash خودِ payload) و `chunkBytes` منتشر می‌شود.
4. **ACL/بهداشت**: `data_manifest` و `data_chunk` مثل `data_pull` زیر `sync_read` رفتند و
   به `$SKIP_API_LOG` اضافه شدند (chunkها پرتکرارند و لاگ را می‌چرخاندند).

### کلاینت — `crm/sync.js`

1. `pullOneCollection` مسیر بایتی را ترجیح می‌دهد: الحاق تکه‌ها ⇒ بررسی `bytes` ⇒ `JSON.parse`
   برای بررسی `kind`/`count` ⇒ `sha256` روی همان بایت‌ها. **هیچ re-encode مرورگری در کار
   نیست**، پس مقدار ذخیره‌شده بایت‌به‌بایت همان چیزی است که سرور دارد.
   مقایسهٔ `canonicalSha256` حذف شد (قرارداد ذاتاً ناسالم بود).
2. کلیدهای `unavailable` گزارش می‌شوند و مانع دریافت بقیه نیستند؛ برای آن‌ها آخرین
   نسخهٔ سالم محلی حفظ و به کاربر اعلام می‌شود.
3. `maxAttempts = 4` با backoff، و **حفظ پیشرفت**: در تلاش دوباره، collectionهایی که
   `rev` خودشان و `rev` بایگانی عوض نشده دوباره دانلود نمی‌شوند (`reused`).
4. **بن‌بست بوت برداشته شد**: پس از دو شکست پیاپی مسیر اتمیک، `pullCheck` به همان مسیر
   دلتای معتبر قبلی (`data_pull` با صحت‌سنجی count/bytes/sha256) برمی‌گردد؛ CRM بارگذاری
   می‌شود، `bootstrapped` می‌شود و push‌های معلق آزاد می‌شوند. علت شکست اتمیک در
   `ptf_sync_last_error` ثبت می‌شود (قابل مشاهده در تشخیص همگام‌سازی تنظیمات).
5. **پیام کاربر**: علت واقعی داخل پیام می‌آید، همان علت حداکثر یک‌بار در دقیقه توست
   می‌شود و فاصلهٔ تلاش مجدد پلکانی است (۵ → ۱۰ → ۲۰ → ۳۰ ثانیه).
6. `window.ptfSyncFullResync(cb)` برای «دریافت کامل مجدد» به‌صورت عمومی اضافه شد.

## ۵) آنچه عمداً تغییر نکرد (قرارداد ایمنی داده)

- شکست صحت‌سنجی همچنان **fail-closed** است: payload دستکاری‌شده/ناقص هرگز نوشته نمی‌شود،
  cursor جلو نمی‌رود و آخرین snapshot سالم محلی می‌ماند (سناریوی ۶ تستر همین را می‌سنجد).
- commit همچنان staging دارد: تا وقتی همهٔ کلیدهای manifest دریافت و تأیید نشوند، هیچ
  projectionی نوشته نمی‌شود؛ در خطای نوشتن محلی rollback انجام می‌شود.
- مسیر دلتای روزمره (`data_pull` هر ۲۰ ثانیه)، merge هوشمند، سنگ‌قبرها و RBAC دست‌نخورده‌اند.
- نکتهٔ صریح: با «حفظ پیشرفت» ممکن است در یک تلاش، کلیدها از دو نسل manifest بیایند —
  این دقیقاً همان تضمین per-key است که مسیر دلتای موجود از ابتدا داشته و از «هرگز کامل
  نشدن» به‌مراتب ایمن‌تر است.

## ۶) شواهد آزمون

`node _tools/uat/tester618-v34.38.11-atomic-pull-completion.js` — کلاینت واقعی
(`crm/sync.js` داخل `vm`) روی یک سرور شبیه‌سازی‌شده:

| سناریو | سنجه |
|---|---|
| payload چندchunkی فارسی + `{}` + عدد `1.0` + اسلش | pull موفق؛ مقدار ذخیره‌شده بایت‌به‌بایت برابر سرور |
| کلید آبجکتی (`settings`) | `{}` دیگر به `[]` تبدیل نمی‌شود |
| یک کلید ناخوانا | بقیه کامل می‌آیند، نسخهٔ محلی کلید معیوب حفظ می‌شود، `degraded` گزارش می‌شود |
| نوشتن هم‌زمان کلید دیگر وسط pull | pull کامل می‌شود؛ collection بدون تغییر دوباره دانلود نمی‌شود |
| تغییر واقعی همان collection | manifest تازه + تلاش دوباره ⇒ نسخهٔ جدید سرور |
| دو شکست پیاپی مسیر اتمیک | عبور خودکار به pull دلتا و بارگذاری CRM |
| بایت دستکاری‌شده | رد کامل، هیچ نوشتنی، حفظ نسخهٔ سالم |
| برش بایتی | هر تکه UTF-8 معتبر و الحاق دقیقاً همان payload |

گیت‌های پروژه: `node _tools/arch/arch-guard.js` ⇒ PASS، `node _tools/uat/run-ci-gate.js`
⇒ ۲۳۳ PASS / ۰ FAIL (هم‌تراز مبنای پیش از تغییر: ۲۳۱ PASS / ۱ FAIL که همان FAIL هم
یادداشت انتشار نسخهٔ جدید بود و رفع شد).

## ۷) پس از استقرار چه باید دید

1. یک‌بار `crm/clear-cache.html` (یا Ctrl+F5) تا Service Worker نسخهٔ `v34.38.11` را
   بردارد؛ وگرنه مرورگر همان `sync.js` معیوب کش‌شده را اجرا می‌کند.
2. نشانگر همگام باید سبز شود و پیام «دریافت کامل CRM…» دیگر تکرار نشود.
3. اگر پیامی دیده شد، حالا **علت** داخل آن است (مثلاً `manifest_integrity_invalid` یا
   `snapshot_bytes_mismatch:ptf_crm_x`) و همان علت در تنظیمات ← تشخیص همگام‌سازی هم ثبت
   است؛ همان متن برای پیگیری کافی است.
4. اگر پیام «n بخش روی سرور قابل خواندن نبود» آمد، یعنی فایل sync آن کلید روی میزبان
   خراب/غایب است و باید از بک‌آپ چرخشی بازیابی شود — بقیهٔ سامانه سالم کار می‌کند.
