# یادداشت انتشار CRM v34.8.29

تاریخ: ۱۴۰۵/۰۶/۰۵ (۲۰۲۶-۰۸-۲۷)

## T5-2 — چک‌های شخصی دیگر «فقط-دستگاه» نیستند

**ریسک بسته‌شده:** چک‌های شخصی تا امروز در `ptf_personal_cheques_<user>` روی
همان یک مرورگر زندگی می‌کردند — گم‌شدن گوشی/پاک‌شدن مرورگر = گم‌شدن چک شخصی.

### تغییر
- کلید سینک‌شوندهٔ مشترک جدید: `ptf_crm_personal_cheques` (SYNC_KEYS + URGENT +
  ماتریس هر ۵ نقش سمت crm.php + رجیستری فرمانی هر دو طرف — A11)
- `chPersonalAll` حالا از کلید مشترک می‌خواند (رکوردهای کاربر فعلی) + دید legacyهای
  در انتظار مهاجرت
- `chSave` با merge در کلید مشترک می‌نویسد (فقط رکوردهای کاربر فعلی جایگزین؛ بقیه
  کاربران دست‌نخورده)
- **مهاجرت یک‌بارهٔ بوت:** همهٔ `ptf_personal_cheques_*`های LS ادغام → فرمان اتمیک →
  **پاک‌سازی legacy فقط بعد از ACK سرور** (الگوی ایمن استاندارد)
- key-registry: این کلید از DEV به REC منتقل شد (دادهٔ کسب‌وکار است، نه وضعیت دستگاه)

### کنترل
tester531 رفتاری (۲۱/۰): مهاجرت ۲ کاربر → ادغام → ACK → پاک‌سازی legacy.
گیت کامل **۱۴۷/۰**، arch-guard PASS (A10: cheques فقط برای مهاجرت legacy مجاز).
