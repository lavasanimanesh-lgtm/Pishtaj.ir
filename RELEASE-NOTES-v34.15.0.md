# RELEASE NOTES — v34.15.0 (سئوی بین‌المللی + بهداشت گروهی)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** تستر560 (۳۸ سنجه) · **گیت:** 176 PASS / 0 FAIL · **PHP-لینت:** php-parser (cms.php + llm.php) ✓

---

## ۱) 🌐 hreflang دوطرفه (fa ↔ en/)

- اکشن `hreflang_sync`: همهٔ صفحات `en/` با همتای فارسیِ هم‌مسیر جفت می‌شوند (۵ جفت آماده: index/about/services/projects/careers) و **سه‌گانهٔ hreflang** (fa-IR + en + x-default=فارسی) در **هر دو طرف** درج/یکسان‌سازی می‌شود.
- **idempotent:** بلوک‌های hreflang قبلی (هر دو ترتیب صفت) حذف و سه‌گانهٔ درست بعد از canonical (و در نبودش قبل از `</head>`) درج می‌شود؛ اگر نتیجه با فایل فعلی یکی باشد، هیچ نوشتن/بک‌آپی نیست.
- **مقاوم:** stubهای ریدایرکت (ptf-redirect) هرگز لمس نمی‌شوند؛ گیت جهش حجم؛ بک‌آپ پیش از نوشتن؛ `index.html` → آدرس پوشه‌ای با اسلش.
- UI: کارت «🌐 hreflang دوطرفه» + دکمهٔ «🌐 همگام‌سازی» در تب «🛠 کیفیت» — خروجی: تعداد جفت‌ها، فایل‌های تغییر یافته، صفحات انگلیسیِ بدون همتا.

## ۲) 🔧 خود-کانونیکال‌سازی گروهی

- دکمهٔ «🔧 canonical گروهی» در تولبار تب سئو → اکشن `canonical_bulk`:
  - فقط صفحات با ایراد **canonical-mismatch** یا **no-canonical** (از اسکن موجود) هدف می‌شوند.
  - canonical هر صفحه به **آدرس خودش** برمی‌گردد (جایگزینی لینک موجود یا درج پس از `</title>`؛ حالت پوشه‌ای برای index.html).
  - **stubهای ریدایرکت** (که canonicalشان عمداً به مقصد اشاره می‌کند) مستثنا؛ سقف ۶۰ فایل در هر اجرا؛ بک‌آپ هر فایل؛ کش اسکن نو + لاگ.

## ۳) 🖼 متن جایگزین تصاویر (alt) با بینایی AI

- **اسکن** (`alt_scan`): همهٔ صفحات عمومی؛ فقط تصاویرِ **بدون صفتِ alt** (alt خالیِ تزئینی طبق استاندارد درست است)؛ رد کردن `data:`/`javascript:`؛ نرمال‌سازی قطعه‌ای مسیر (رفع `../`)؛ فقط فایل‌های jpg/png/webp/gif تا 3MB؛ سقف ۶۰ ردیف.
- **بینایی گروهی** (`seo_alt` در llm.php): تا **۸ تصویر در یک فراخوانی**؛ تصویر **روی سرور از دیسک** خوانده و با realpath زیر ریشهٔ سایت اعتبارسنجی می‌شود (مسیر کلاینت قابل سوءاستفاده نیست)؛ زیرساخت چندتصویری به هر دو provider اضافه شد (Gemini `inline_data` چندتایی / OpenAI `image_url` چندتایی) و **تصاویر در کلید کش** می‌آیند تا پاسخ‌های تصادم نکنند.
- قواعد متن: فارسی، ۶–۱۴ کلمه، فقط توصیفِ دیدنی، بدون پیشوند «تصویر»، بدون ادعای بازاریابی، بدون اختراع برند/مدلِ ناخوانا.
- **UI (تب کیفیت):** جدول با چک‌باکس و فیلد قابل ویرایش → «🤖 تولید alt با بینایی AI» دسته‌های ۸تایی ترتیبی (فیلد پرشده را بازنویسی نمی‌کند) → **تیک بازبینی انسانی الزامی** → «✅ اعمال گروهی» (سقف ۴۰؛ پاکسازی متن از تگ/نقل‌قول/۱۶۰حرف؛ بک‌آپ یک‌بارهٔ هر صفحه؛ کش اسکن نو).

## ۴) بهداشت

- توابع جدید (hreflang/alt) **پیش از قلاب lazy زمان‌بند** نشستند — ترتیب «توابع ← قلاب ← switch» حفظ شد.
- انکر tester559 برای موتور مشترک page_create **نسخه‌زدایی** شد (مقاوم به جاروی bump).
- A10 پابرجا؛ بدون `localStorage` مستقیم.

## ۵) فایل‌های کلیدی

`api/cms.php` (cms_hreflang_url/apply + cms_alt_rows + ۴ case جدید) · `api/llm.php` (چندتصویری در llm_call_once + seo_alt) · `crm/cms.js` (دو کارت جدید + cmsCanonBulk) · `_tools/uat/tester560-v34.15.0-seo-s5-hygiene.js`

## ۶) بعدی

بخش‌های باز رودمپ: پوزیشن watchlist، مقایسهٔ صفحات AI-لمس‌شده با بقیه، ادیتور ریدایرکت‌های انگلیسی (en/)، یا اولویت دیگری که مالک تعیین کند.
