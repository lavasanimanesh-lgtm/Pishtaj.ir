# RELEASE NOTES — v34.8.1 (۱۴۰۵/۰۶/۰۲ — 2026-08-24)

## مکاتبات: ذخیره و ادامهٔ پیش‌نویس

- دکمهٔ مستقل **«ثبت پیش‌نویس»** پیش از مسیر امضا و ثبت نهایی افزوده شد.
- پیش‌نویس می‌تواند با محتوای ناقص ذخیره شود و هیچ شمارهٔ قطعی، امضا، اعلان امضا یا سند پرونده تولید نمی‌کند.
- نویسنده می‌تواند پیش‌نویس یا نامهٔ ردشده را دوباره باز کند، ویرایش و پیش‌نمایش بگیرد و سپس برای امضا/ثبت نهایی ارسال کند.
- هنگام ارسال مجدد، علت رد قبلی پاک می‌شود تا metadata وضعیت قبلی کنار وضعیت تازه باقی نماند.
- مجموعه فونت‌های مصوب مکاتبات بدون تغییر شامل بی‌یاقوت، بی‌نازنین، وزیرمتن، ایران‌یکان و ایران‌سنس باقی مانده است.

## یکپارچه‌سازی تاریخ شمسی

- کنترل روز در بخش‌های فارسی با تقویم شمسی نمایش داده می‌شود و قرارداد ذخیرهٔ ISO در مسیرهایی که به ISO نیاز دارند حفظ شده است.
- انتخاب دورهٔ ماهانه به‌صورت دو مرحله‌ای انجام می‌شود: ابتدا سال از جدول سال‌ها و سپس ماه از جدول ماه‌های فارسی.
- فیلدهای صرفاً سالانه به انتخابگر جدولی سال تبدیل شدند و دیگر به تایپ آزاد وابسته نیستند.
- کنترل‌های تاریخ و دوره در هاب مالی، مالیات، ارزش افزوده، سرمایه در گردش، OPEX، پورسانت، سهامداران، فاکتورهای تأمین‌کننده و سایر مسیرهای مرتبط مهاجرت شدند.
- ردیف‌های تکی و دسته‌ای چک، تقویم شمسی فشرده دارند و شناسه‌ها و کلاس‌های جمع‌آوری قبلی آن‌ها حفظ شده است.
- تاریخ‌های شمسی با فونت فارسی و تاریخ‌های صریحاً میلادی با فونت انگلیسی نمایش داده می‌شوند.

## اصلاحات عمیق DateKit

- تشخیص کبیسهٔ اسفند اصلاح شد؛ مقدار `leap === 0` اکنون به‌درستی سال کبیسه تلقی می‌شود.
- اعتبارسنجی روز بر اساس تعداد واقعی روزهای ماه انجام می‌شود؛ برای نمونه `1404/12/30` رد و `1403/12/30` پذیرفته می‌شود.
- تاریخ میلادی نامعتبر نیز پیش از تبدیل رد می‌شود و دیگر rollover بی‌صدای تاریخ رخ نمی‌دهد.
- جابه‌جایی ماه در مرز اسفند کبیسه و غیرکبیسه به‌درستی clamp می‌شود.
- بازهٔ آمادهٔ **«ماه قبل»** اکنون ابتدا و انتهای همان ماه را به ترتیب صحیح می‌سازد.

## مبدأ درخواست وب‌سایت

- زیر شمارهٔ RFQ درخواست‌های واردشده از سایت، عبارت صریح **«ثبت‌شده از وب‌سایت»** نمایش داده می‌شود.
- این درخواست‌ها دیگر به‌عنوان «ثبت‌کننده: نامشخص (قدیمی)» نمایش داده نمی‌شوند.
- metadata مبدأ و زمان ثبت سایت هنگام تأیید و انتقال درخواست به چرخهٔ CRM حفظ می‌شود.

## آزمون و جلوگیری از رگرسیون

- تستر اختصاصی `tester503-v34.8.0-jalali-draft-site-source.js` قراردادهای تقویم، انتخابگرهای روز/ماه/سال، پیش‌نویس مکاتبات و مبدأ RFQ سایت را پوشش می‌دهد.
- تمام **۷۳٬۰۴۹ روز معتبر شمسی از سال ۱۳۰۰ تا ۱۴۹۹** به‌صورت رفت‌وبرگشت شمسی/میلادی بررسی می‌شوند تا rollover یا تبدیل ناپایدار شناسایی شود.
- edge caseهای اسفند کبیسه و غیرکبیسه، تاریخ‌های نامعتبر و بازهٔ «ماه قبل» تست رگرسیون دارند.

## نسخه و cache

- نسخهٔ رسمی CRM از `v34.8.0` به **`v34.8.1`** ارتقا یافت.
- `VERSION.json`، نسخهٔ رابط CRM، service worker، manifest، صفحهٔ پاک‌سازی کش، shell و نسخهٔ سرویس sales-domain هم‌راستا شدند.
- cache-bust همهٔ دارایی‌های CRM و نام cache سرویس‌ورکر به `34.8.1` تغییر کرد تا مرورگر و PWA فایل‌های نسخهٔ جدید را دریافت کنند.
