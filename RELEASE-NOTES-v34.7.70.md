# RELEASE NOTES — v34.7.70 (۱۴۰۵/۰۵/۳۰ — 2026-08-21)

## جلوگیری از ثبت تکراری تامین‌کننده (سرور)

### ارزیابی (ریشه)

| مسیر ثبت | قبلاً ضدتکرار؟ |
|---|---|
| **سایت** (`add_supplier`) | ❌ هیچ — ثبت‌نام تکراری آزادانه (حتی در صف pending) ساخته می‌شد |
| **CRM** (`saveSup2`) | ⚠️ فقط سمت کلاینت (JS) و فقط در برابر فهرست تاییدشده — قابل دور زدن، و ثبت‌نام‌های pending سایت را نمی‌دید |
| **تایید** (`supApprove`) | ⚠️ گفتگوی ادغام می‌گرفت ولی «ثبت به‌عنوان رکورد جدید» تکراری می‌ساخت |

### راه‌حل — dedup سروری (منبع حقیقت، غیرقابل دور زدن)

در `api/crm.php → add_supplier`:

- نام شرکت و شماره تماس با نرمال‌سازی (هم‌ارز `dedupNorm/dedupNormPhone` سمت کلاینت:
  ارقام فارسی/عربی → EN، ی/ک عربی → فارسی، +98/0098 → 0) مقایسه می‌شوند.
- مقایسه در برابر **هر دو فهرست**:
  - `suppliers` (ثبت‌نام‌های سایت — در انتظار تایید) و
  - `ptf_crm_suppliers` (تامین‌کنندگان تاییدشده).
- در صورت تکرار → `ok:false, error:"duplicate"` با پیام شفاف («این تامین‌کننده قبلاً با
  نام/شماره ثبت شده — کد رکورد») و **رکورد جدید ساخته نمی‌شود**.
- فرم سایت از قبل `message` خطا را نمایش می‌دهد؛ پیام تکراری به کاربر می‌رسد.

### نتیجه

- تکرار از سایت: **مسدود** (سرور، نه فقط مرورگر).
- تکرار از CRM: همان‌طور که بود با `ptfDupBlock` مسدود است + حالا صف pending سایت هم در
  چرخهٔ سرور در نظر گرفته می‌شود.
- در تایید: گفتگوی ادغام موجود (merge/force) حفظ شده — برای موارد legacy که قبل از این
  اصلاح تکراری ثبت شده‌اند.

### فایل‌ها

`api/crm.php` — توابع `ptf_dedup_norm` / `ptf_dedup_phone` + چک تکراری در `add_supplier`.

### تست

- تستر جدید `tester472-v34.7.70-sup-dedup.js` (در گیت CI).
- **گیت CI: ۸۸ PASS / ۰ FAIL** · نگهبان معماری: PASS.

### UAT پیشنهادی

1. یک تامین‌کننده با همان نام/شمارهٔ قبلی از سایت ثبت کنید → باید پیام «قبلاً ثبت شده» بیاید.
2. در CRM، ثبت دستی با نام/شمارهٔ تکراری → پیام ضدتکرار موجود.
3. یک ثبت‌نام سایت تکراری (قبل از دیپلوی) را در تایید بررسی کنید → گفتگوی ادغام.
