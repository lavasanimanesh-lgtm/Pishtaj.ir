# 📊 گزارش رگرسیون v31.7.25

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.25
**ابزار:** `_tools/run-full-regression.js` + `python3 _tools/audit.py` (۱۰ بخش)

## ✅ نتیجه نهایی

| معیار | مقدار | وضعیت |
|--------|-------|--------|
| تعداد تسترها | **۱۷۸** (+۱: tester200-sms-auth-token) | — |
| فایل‌های PASS | **۱۷۸** | ✅ ۱۰۰٪ |
| چک‌های FAIL | **۰** | ✅ صفر |
| audit.py (۱۰ بخش) | **PASS** | ✅ |
| `node --check crm/ui-kit.js` | **PASS** | ✅ |

## 🆕 پوشش تست جدید (tester200 + E2E)

- **E2E سرور PHP واقعی:** `sms_bulk` با `X-CRM-Role` → همان «Authentication required» اسکرین‌شات کارفرما؛ با `X-CRM-Token` → عبور از auth (پاسخ `sms_off` یعنی به لایه پیامک رسید)
- ۶ سناریوی رفتاری wrapper: تزریق خودکار توکن (با/بدون init)، احترام به توکن صریح، عدم دستکاری URLهای خارجی، عدم جعل بدون login، ضد double-wrap، ترتیب لود ui-kit
- سند دامنه: sms.js عمداً بدون توکن دستی ماند — پوشش از wrapper است

## 📌 بسته‌شدن خانواده BUG-AUTH

مصادیق تاریخی: bridge (7.7)، users_get/login (7.8)، users_sync (7.14 غیرمستقیم)، اکنون sms/backup/golive/rbac (7.25). با wrapper سراسری، این خانواده از نظر مکانیکی بسته است — هیچ fetch به crm.php نمی‌تواند بدون توکن برود مگر توکنی وجود نداشته باشد.

## 📁 فایل‌های تغییریافته

`crm/ui-kit.js`، `crm/index.html` (bump)، `crm/sw.js`، `crm/clear-cache.html`، `_tools/uat/tester200` (جدید)

## 🚦 وضعیت گیت‌ها

- **گیت فنی: سبز** ✅
- **گیت انسانی: معوق** ⏳ — ارسال پیامک واقعی از پنل + بازکردن فهرست بک‌آپ‌های سرور روی سرور واقعی.
