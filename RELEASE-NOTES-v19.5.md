# 🚀 ریلیزنوت v19.5 — اسپرینت Post-R9: «سند اصلاحی سال قفل‌شده + خروجی حسابدار»

**تاریخ:** ۱۴۰۵/۰۴/۲۱ | **شاخه:** واحد | **مبنا:** BACKLOG-POST-R9-FINDINGS.md (پس از پایان کامل رودمپ R12 در v19.4)

---

## 📦 محتوای اسپرینت (طبق قاعده ۱-۲ استوری هم‌خانواده)

| آیتم | شرح | وضعیت |
|---|---|---|
| **US-427** | سند اصلاحی پس از قفل سال مالی — snapshot گذشته تغییرناپذیر، اصلاح در سال جاری | ✅ کامل |
| **US-426 فاز ۲** | خروجی CSV حسابدار + چاپ snapshot قفل‌شده از داده منجمد | ✅ کامل |

هم‌خانواده: «حسابرسی سال مالی» — ادامه طبیعی v18.4 (BUG-024/025 + US-426ف۱ که ایجنت دوم اجرا کرده بود).

---

## 🧾 US-427 — سند اصلاحی پس از قفل سال

### اصل حسابرسی
snapshot سال قفل‌شده **هرگز ویرایش نمی‌شود**. اگر بعد از قفل، اصلاح لازم شد (هزینه کشف‌شده، درآمد شناسایی‌نشده و...)، «سند اصلاحی» ثبت می‌شود که:

1. **فقط برای سال قفل‌شده** قابل ثبت است (برای سال باز، اصلاح مستقیم در ماژول مربوط انجام می‌شود — پیام راهنما دارد).
2. **اثر همیشه در سال جاری** می‌نشیند (AC3) — `netProfit سال جاری += مبلغ سند` (کاهنده یا افزاینده).
3. **ارجاع کامل** به snapshot مرجع (`refSnap`) + شرح اجباری + audit + فقط admin/chairman (AC4).
4. در داشبورد سال مالی با کارت «سند اصلاحی موثر بر سال»، فهرست بنفش با برچسب اصلاحی، و در گزارش رسمی با جدول جدا دیده می‌شود (AC3).

### تصمیم معماری
سند اصلاحی در **همان کلید** `ptf_crm_fiscal_snapshots` با `type:'amendment'` ذخیره می‌شود ⟵ هیچ کلید داده جدیدی ساخته نشد؛ سینک/سپر/بک‌آپ/whitelist سروری موجود بدون تغییر پوشش می‌دهند (قاعده چهارجای هندآور).

### UI
دکمه «🧾 سند اصلاحی» در نوار داشبورد سال مالی ⟵ فرم هم‌تم: سال قفل‌شده مرجع (فقط سال‌های قفل‌شده) + جهت اثر (کاهنده/افزاینده) + مبلغ + شرح اجباری.

---

## 📥 US-426 فاز ۲ — خروجی حسابدار و snapshot

1. **«📥 CSV حسابدار»** (`ptfFiscalCsv`): فایل اکسل‌خوان با BOM فارسی شامل — ردیف‌های پروژه (فروش/خرید/زیان/سود)، هزینه‌های جاری per دسته، سندهای اصلاحی، جمع‌های کنترلی (سود پروژه‌ها/هزینه/اصلاحی/سود خالص/قابل تقسیم/اندوخته/مطالبات سال و کل) و کاربرگ سهامداران — AC3 بک‌لاگ.
2. **«🖨 چاپ snapshot قفل‌شده»** (`ptfFiscalSnapPrint`): گزارش رسمی از **داده منجمد لحظه قفل** چاپ می‌شود، نه محاسبه مجدد — یعنی حتی اگر داده‌های عملیاتی بعدا تغییر کنند، سند قفل همان می‌ماند (AC4 بک‌لاگ + هم‌راستا با اصل US-427).

هر دو فقط admin/chairman (تداوم BUG-024).

---

## 🛡 اثبات شروط دائمی کارفرما

| شرط | اثبات |
|---|---|
| بدون وصله‌پینه | سند اصلاحی داخل موتور ptfFiscalData لحاظ شد (نه جمع موازی)؛ چاپ snapshot از همان ptfFiscalReportHtml؛ CSV از همان ptfFiscalDistribution |
| وابستگی‌ها | بدون کلید جدید (همان snapshots)؛ سازگار با قفل سال (v18.1)، محرمانگی (BUG-024)، تفکیک بی‌تاریخ (BUG-025)، گزارش رسمی (US-426ف۱)، opex/shareholders/lossguard |
| بدون باگ جدید | **۹۸ تستر / ۲۸۲۶ چک — صفر خطا** + سینتکس ۵۹ فایل JS + ۵ بلوک inline + audit PASS. tester101 به الگوی «>=» نسخه‌پذیر شد (قاعده BUG-026) |
| بدون تداخل با طرح توسعه | دقیقا صف Post-R9 پیشنهادی؛ US-428/429 + BUG-026 (lint) در صف بعد؛ R12 دست‌نخورده (رگرسیون سبز) |

## 🧪 تست
- **tester111** (جدید): ۳۸ چک — چرخه رفتاری کامل: سود ۱۴۰۴ ⟵ قفل ⟵ سند اصلاحی −۲۰۰هزار ⟵ snapshot ۱۴۰۴ immutable ماند و اثر فقط در ۱۴۰۵ ⟵ سند افزاینده +۵۰هزار ⟵ جمع −۱۵۰هزار ⟵ CSV با BOM ⟵ RBAC حسابدار رد ⟵ سود منفی = تقسیم صفر.
- کل مجموعه: **۹۸ تستر / ۲۸۲۶ چک سبز**.

## 📂 فایل‌های تغییر یافته
`crm/fiscal.js` (amendments در ptfFiscalData + ptfFiscalAmendCommit/Open + ptfFiscalCsv + ptfFiscalSnapPrint + UI داشبورد/گزارش) | `crm/index.html` + `crm/sw.js` (v19.5) | `_tools/uat/tester111-v195.js` (جدید) | `_tools/uat/tester101-v184.js` (نسخه‌پذیر)

## 📤 آپلود
zip روی هاست روی نسخه موجود اکسترکت شود — **پوشه api حذف نشود**. بعد از آپدیت همه کاربران رفرش کنند (سایدبار: CRM v19.5).

---

## ⏭ صف بعدی (منتظر ابلاغ)
① **US-428 + US-429 (ادامه Post-R9):** UI زنده درصد/مبلغ پیش‌پرداخت + هاب مالی مدیریتی (تفکیک تنخواه کاربران عادی از تب‌های مالی مدیران) ② BUG-026 (اسکریپت lint نسخه تسترها — عملا با اصلاح‌های >= انجام شده، فقط اسکریپت رسمی مانده) ③ US-423 (کارت ویزیت) ④ US-410/411 ⑤ US-405..408 ⑥ US-402/403

**شماره‌گذاری:** بعدی US-438 | باگ بعدی BUG-030
