# 📦 ریلیزنوت v14.1 — تکمیل اسپرینت ۱: حالت خودکار روز/شب + اعلان شخصی بات

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v14.0

---

## 🌓 US-354 — حالت خودکار روز و شب (موبایل و دسکتاپ)

- گزینه سوم در تنظیمات ← حالت نمایش: **«🌓 خودکار (هماهنگ با سیستم)»**
- در حالت خودکار، CRM تم **گوشی/کامپیوتر** شما را دنبال می‌کند — شب که سیستم تیره شد، CRM هم تیره می‌شود و بالعکس؛ **زنده و بدون رفرش** (گوش دادن به تغییر تم OS)
- دکمه ماه/خورشید نوار بالا حالا سه‌حالته می‌چرخد: روز ← شب ← خودکار
- بدون فلش سفید هنگام لود (بلوک ضد FOUC هم حالت خودکار را می‌شناسد)
- یک منطق مشترک برای موبایل و دسکتاپ — رفتار یکسان همه‌جا

## 🔐 US-355 — پاسخ سوال شما: بله، ممکن است — و پیاده شد!

**سوال:** «آیا اعلان مربوط به هر کاربر توسط ربات فقط به همان کاربر ارسال شود؟»

**پاسخ + اجرا:** بات حالا **دو کانال** دارد:

| نوع اعلان | مقصد |
|---|---|
| **شخصی** (چک شخصی، ارجاعی که منتظر اقدام اوست، یادآور فردی) — یعنی گیرنده مشخص دارد | فقط **چت خصوصی همان کاربر** — گروه هرگز نمی‌بیند |
| **نقشی/مدیریتی** (مختومه پرونده، رویداد سیستمی، اقدام ادمین) | گروه مدیران شرکت (مثل قبل) |

و درباره حسابدار: چون اعلان‌ها از همان موتور RBAC کارتابل عبور می‌کنند، حسابدار فقط اعلان‌های نقش خودش (ارجاع پیش‌فاکتور برای ثبت فاکتور) را می‌گیرد — در بات هم همین رعایت می‌شود.

### نحوه اتصال هر کاربر (یک بار، ۱ دقیقه):
1. تنظیمات ← بخش بات ← **«🔗 اتصال چت شخصی من به بات»**
2. کد یکتای نمایش‌داده‌شده (مثل `PTF-LAVASANI-4821`) را در تلگرام برای **@PTF_CRM_bot** بفرستد (اول Start)
3. دکمه «کد را فرستادم — بررسی کن» → اتصال برقرار ✅ (پیام تایید در چت خودش می‌آید)
- اتصال‌ها بین دستگاه‌های کاربر سینک می‌شوند؛ وضعیت اتصال در تنظیمات دیده می‌شود
- کاربری که متصل نشده باشد، اعلان شخصی‌اش فقط در کارتابل CRM می‌ماند (چیزی به گروه درز نمی‌کند)

## 🧪 کیفیت
تستر جدید `tester61` ۱۷/۱۷ ✅ | رگرسیون **۴۸ تستر** ✅ | audit صفر خطا ✅

## 📁 فایل‌های تغییریافته
`crm/perms.js` (تم سه‌حالته + شنونده OS) · `crm/theme.js` (چرخه نوار بالا) · `crm/index.html` (FOUC + VER v14.1) · `crm/messengers.js` (مسیریابی شخصی/گروهی + UI جفت‌سازی) · `api/notify-bot.php` (اکشن pair + ارسال شخصی) · `crm/sw.js` · `_tools/uat/tester61` (جدید)

⚠️ `api/notify-bot.php` تغییر کرده — جایگزین شود.

## 📌 بعدی
اسپرینت ۲ (سود و مهلت‌ها): US-347 + US-348 + US-352 — با اعلام شما.
