# 🚀 یادداشت‌های انتشار اسپرینت ۱۱۳ (Release Notes - v113.1)

**تاریخ انتشار:** ۱۴۰۵/۴/۱۵ (July 2026)  
**هدف اسپرینت:** ریشه‌کنی باگ‌های UI و سرور S3 توسط تیم‌های تخصصی، استقرار دستیار اسناد و حذف دسته‌جمعی کالاها (اولویت ۱۰ بک‌لاگ محصول)  
**تیم توسعه:** کارگروه‌های تخصصی ریشه‌یابی بک‌اند، فرانت‌اند و کنترل کیفیت PTF  

---

## 📋 تغییرات اعمال‌شده توسط تیم‌های تخصصی در این نسخه

### ۱. 🛡️ ریشه‌کنی خطای سروری حذف زباله‌های ابری (Backend Taskforce - BUG-005 Fix)
* **فایل تغییر یافته:** `api/storage.php` (خط ۴۲)
* **شرح ریشه‌یابی و حل مشکل:** تیم تخصصی بک‌اند دریافت علت خطای سرور در آپدیت قبل، عدم درج نام اکشن جدید `delete_batch` در آرایه مجاز `$SENSITIVE_ACTIONS` بود. این امر باعث می‌شد سرور به اشتباه تابع تعریف‌نشده `verify_request` را فراخوانی کرده و ارور Fatal 500 بدهد. با افزودن `delete_batch` به لیست مجاز سرور، این خطا ۱۰۰٪ ریشه‌کن شد و اکنون پاک‌سازی دسته‌جمعی زباله‌ها و بک‌آپ‌های قدیمی آروان‌کلود با سرعت بالا و بدون خطا انجام می‌شود.

### ۲. 🤖 استقرار دکمه‌های رابط کاربری دستیار هوشمند اسناد (Frontend Taskforce - US-115 UI)
* **فایل‌های تغییر یافته:** `crm/index.html`، `crm/offerlock.js`
* **شرح تغییر:** تیم فرانت‌اند مشکل عدم تطابق رشته‌های جستجو در آپدیت قبل را شناسایی و با روش جایگزینی مبتنی بر ایندکس، دکمه‌های دستیار هوشمند را با موفقیت در رابط کاربری تثبیت کرد:
  * در ماژول **📦 کالاها** (بالای جدول و کنار کادر جستجو)، دکمه بنفش‌رنگ **«🤖 دستیار هوشمند اسناد»** تعبیه شد.
  * در هر ردیف از جدول اقلام **📋 پیشنهادها (TO/CO)**، لینک **«🤖 دستیار هوشمند سند»** قرار گرفت. با کلیک روی آن‌ها، پنجره بارگذاری فایل‌های PDF، عکس، اکسل و ورد باز شده و اقلام استخراجی مستقیماً در جدول جاگذاری می‌شوند.

### ۳. ☑️ انتخاب چندگانه و حذف دسته‌جمعی کالاها (US-117 - Product Batch Deletion)
* **فایل تغییر یافته:** `crm/index.html`
* **شرح تغییر:** در سرستون جدول کالاها، چک‌باکس **«☑ انتخاب همه»** (`#pChkAll`) و در کنار هر ردیف کالا یک چک‌باکس مجزا (`.p-chk`) اضافه شد. دکمه قرمز رنگ **«🗑 حذف دسته‌جمعی انتخاب‌شده‌ها»** در بالای جدول کالاها قرار گرفت که با کلیک روی آن، تمام کالاهای تیک‌خورده به صورت یکجا و با ثبت دلیل در بایگانی مدیریت حذف می‌شوند.

### ۴. 📱 راهکار کلمات زیر آیکون در سایدبار موبایل (Mobile Sidebar Tiny Font)
* **فایل تغییر یافته:** `crm/shell.js`
* **شرح ریشه‌یابی و حل مشکل:** تیم فرانت‌اند دریافت که فایل `shell.js` در زمان اجرای آکاردئون، استایل‌های موبایل را بازنویسی می‌کرد. با اصلاح کدهای `shell.js`، استایل سرفصل‌ها (`.sb-g`) به صورت ستونی درآمد: آیکون در بالا و کلمه سرفصل (مثل «فروش» یا «تامین») با **فونت بسیار ریز ۸.۵ پیکسلی، پررنگ و وسط‌چین** در زیر آیکون قرار گرفت. سایدبار روی عرض ۵۶ پیکسل میخکوب است و هیچ اسکرول افقی ندارد.

---

## 🧪 وضعیت کنترل کیفیت (QA Verification)
* تست سرور S3: اکشن `delete_batch` با موفقیت لیست کلیدها را از آروان‌کلود حذف کرد.
* تست رندر کالاها: چک‌باکس‌ها و دکمه‌های دستیار هوشمند به درستی نمایش داده شدند.
