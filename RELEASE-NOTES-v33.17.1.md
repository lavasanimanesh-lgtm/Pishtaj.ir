# یادداشت‌های انتشار (Release Notes — Version 33.17.1) — آماده‌سازی مرج به main

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (آمادهٔ مرج به main → deploy پروداکشن)
**تمرکز:** گارد امنیتی مهاجرت + آماده‌سازی نهایی برای انتشار روی پروداکشن.

---

## 🔒 تغییرات v33.17.1

### ۱. گارد امنیتی `api/migrate.php` (بعد از سوییچ نهایی)
- اگر `mode=mysql` (مهاجرت کامل شده) → اسکریپت **هیچ عملیاتی اجرا نمی‌کند** و فقط پیام «مهاجرت کامل شده؛ فایل را حذف کنید» نشان می‌دهد.
- **چرا مهم:** workflow پروداکشن `api/migrate.php` را deploy می‌کند؛ این گارد تضمین می‌کند حتی بعد از مرج/دیپلوی، مهاجرت دوباره اجرا نمی‌شود.

### ۲. امنیت رمز دیتابیس
- `api/ptf-db-config.php` (فایل واقعی رمز) به `.gitignore` اضافه شد → رمز هرگز به گیت/مرج نمی‌رود.

### ۳. بامپ نسخه
- v33.17.1 سراسری (index/sw/clear-cache).

---

## ✅ وضعیت نهایی مهاجرت (فاز A)
- استیجینگ: مهاجرت کامل (سوییچ نهایی انجام‌شده یا آماده) — جدول `ptf_kv`.
- پروداکشن: مهاجرت کامل (مرحله ۵/۶) — جدول `ptf_kv_prod` (جدا از استیجینگ).
- dual-write فعال؛ فایل‌های JSON محفوظ.

## 🚀 آماده‌سازی مرج
- PR #11: MERGEABLE (باز).
- پس از مرج به main → GitHub Actions → **Deploy to Production** → `confirm=DEPLOY`، `ref=main`.
- پس از دیپلوی: نسخهٔ پروداکشن از v33.2.0 → **v33.17.1** (۱۵ نسخه جهش — همهٔ اصلاحات CRM/بکاپ/سود/چک/مهاجرت).
- کاربران: یک بار رفرش (Ctrl+Shift+R) + برای پرحافظه‌ها: پاک‌سازی امن فوری → مهاجرت به IndexedDB.

## 📋 فایل‌ها
| فایل | تغییر |
|---|---|
| `api/migrate.php` | گارد mode=mysql (قفل پس از سوییچ) |
| `.gitignore` | + api/ptf-db-config.php |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.17.1 |
| `_tools/uat/tester297` | +۳ تست گارد/gitignore (۱۹/۰) |
| `RELEASE-NOTES-v33.17.1.md` / `REGRESSION-REPORT-v33.17.1.md` | اسناد انتشار |
