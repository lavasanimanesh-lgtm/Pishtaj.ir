# چرا دیپلوی استیجینگ کند بود و چرا قرمز می‌شد — و چه تغییری خورد (v34.8.40)

**تاریخ:** ۲۰۲۶-۰۸-۲۹ | **فایل:** `.github/workflows/deploy-staging.yml` (+ پچ معلق پروداکشن)
**قفل تستر:** `tester540-v34.8.40-deploy-speedup-staging.js` (ثبت در run-ci-gate)

---

## ۱) کجا وقت می‌سوزاند؟ (پروفایل یک ران ۸-۱۲ دقیقه‌ای)

| گام | زمان قبل | علت |
|---|---|---|
| php lint | ~۱-۲ دقیقه | `apt-get install php-cli` در هر ران — در حالی که php روی ایمیج رانر **پیش‌نصب** است |
| Deploy via FTP | **~۵-۸ دقیقه** | همگام‌ساز درخت‌محور در هر ران کل درخت را LIST می‌کند (~۱۳۰۰+ فایل مسیرِ قابل‌انتشار) و بعد دلتا را می‌گذارد — حتی برای تغییر یک فایل |
| بنر استیجینگ | ~۱ دقیقه | `find` روی کل مخزن و تزریق به **۶۶۵ فایل HTML** — که همه باید آپلود شوند چون timestamp بنر عوض می‌شود |
| Post-deploy integrity | تا ~۱ دقیقه + قرمزی | کش مسیرمحور هاست (نادیده‌گیرندهٔ query) در پنجرهٔ ۵×۱۲s بایت کهنه می‌دهد → false-red |

## ۲) تغییرها (چهار اصل: سریع‌تر، سبزِ درست، fail-closed جایی که لازم است، fallback خودکار)

### ۲.۱ — آپلود افزایشی بر مبنای git-diff (ریشهٔ اصلی کندی)
- مارکر `__deploy__.txt` رانِ موفق قبلی از FTP خوانده می‌شود → SHA پایه.
- اگر SHA در تاریخچه باشد (در صورت نیاز `git fetch --depth=1 origin <sha>`) → فقط فایل‌های
  `git diff` + فایل‌های تولیدی ران (۸ بنر + robots + crm/.htaccess + مارکر) آپلود می‌شود —
  **چند ثانیه به‌جای چند دقیقه**.
- هر خطا (نبود مارکر / SHA ناشناس / مسیر غیر-ASCII / شکست PUT) → **fallback خودکار** به
  همگام‌سازی کامل SamKirkland (همان رفتار قبلی). صحت نهایی را گیت readback تضمین می‌کند.
- حذف‌ها: `diff-filter=D` → `DELE` سمت سرور.
- **full-sync هفتگی** (شنبه ۰۴:۰۰ UTC) + ورودی دستی `full` → درمان drift سمت سرور.

### ۲.۲ — گیت صحت دولایه (پایان false-red، بدون خنثی‌شدن گیت)
- **لایهٔ ۱ — readback از خود FTP (مسدودکننده):** ۵ فایل حساس مستقیماً از FTP دانلود و
  sha1 با کامیت مقایسه می‌شود + مارکر == SHA این ران. این لایه مستقل از کش HTTP هاست است؛
  ناهمخوانی = قرمزیِ واقعی (فایل روی دیسک سرور غلط است) → `exit $fail`.
- **لایهٔ ۲ — تازگی HTTP (هشداری):** همان چک‌های زندهٔ قبلی با هدرهای
  `Cache-Control/Pragma: no-cache` + دوبل cache-buster و پنجرهٔ ۱۲×۳۰s. اگر فقط این لایه
  عقب بماند → `::warning` + یادداشت Summary؛ ران سبز می‌ماند چون دیسک با readback اثبات شده.
  (کش هاست خودش ظرف چند دقیقه تازه می‌شود؛ .htaccess تزریقی no-cache هم کمک می‌کند.)

### ۲.۳ — سرعت‌های جانبی
- php lint از php پیش‌نصبِ رانر (apt فقط fallback).
- بنر فقط به `crm/*.html` (۷ فایل) + `index.html` ریشه — به‌جای ۶۶۵ فایل.

## ۳) انتظار زمانی بعد از تغییر

| سناریو | قبل | بعد |
|---|---|---|
| تغییر کد CRM (push عادی) | ~۸-۱۲ دقیقه | **~۲-۳ دقیقه** (گیت‌ها + آپلود دلتا + readback؛ لایهٔ HTTP معمولاً تلاش ۱ سبز می‌شود) |
| full-sync هفتگی / fallback | ~۸-۱۲ دقیقه | همان قبل (کامل است، عمداً) |

## ۴) پروداکشن
استیجینگ مستقیماً harden شد؛ نسخهٔ پروداکشن (که fail-closed HTTP را نگه می‌دارد — درست است)
با همان هاردنینگ پنجره (۱۸۰s اولیه + ۱۲×۳۰s + no-cache) در
`_tools/PENDING-workflow-integrity-hardening-v34.8.37.patch` (بازتولید ۲۰۲۶-۰۸-۲۹، فقط پروداکشن)
آمادهٔ اعمال مالک است — پیش از دیپلوی نهایی v34.9.0.

## ۵) پوش دسترسی
اگر پوشِ مستقیم `.github/workflows/` از سوی نشست رد شود (دسترسی `workflows`)، همان تغییر
در `_tools/PENDING-workflow-deploy-staging-speedup-v34.8.40.patch` تحویل می‌شود؛ tester540 هر دو
حالت را می‌سنجد (فایل وصل شود → قرارداد مستقیم؛ وگرنه → شبیه‌سازی اعمال وصله).
