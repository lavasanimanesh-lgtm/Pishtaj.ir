# RELEASE NOTES — v34.7.54 (۱۴۰۵/۰۵/۲۹ — 2026-08-20)

## نوع انتشار: هات‌فیکس بهداشت معماری (بدون تغییر رفتار محصول)

### زمینه

PR #58 (نسخه‌های v34.7.47 تا v34.7.53) با یک **تخلف مسدودکنندهٔ جدید A2** در نگهبان
معماری روی `main` ادغام و دیپلوی شد و گیت CI را قرمز کرد:

```
❌ A2: storage.js (cd-first) [aa75eb9f51e9]
   var id = recId || obj.cd || obj._id || obj.no || obj.id || '';
```

سه شکست زنجیره‌ای در گیت (arch-guard، tester443، tester431-G2) همه از همین یک خط بود.

### اصلاح

- **`crm/storage.js` — `ptfHarvestFileKeys`** (کد گزارش کلید ابری v34.7.52):
  ترتیب شناسه به قرارداد رسمی PTF.id برگشت: `recId || obj._id || obj.cd || obj.no || obj.id`.
- اثر عملیاتی: فقط برچسب/شناسهٔ رکورد در **گزارش فقط‌خواندنی** کلیدهای ابری؛ هیچ منطق
  مالی یا ذخیره‌سازی تغییر نکرده است. برای رکوردهایی که هم `_id` و هم `cd` دارند، از این
  پس شناسهٔ متعارف (`_id`) در گزارش و CSV نمایش داده می‌شود — همان قراردادی که بقیهٔ
  سیستم استفاده می‌کند.

### تست و قفل رگرسیون

- تستر جدید **`tester457-v34.7.54-arch-id-order.js`** (متصل به گیت CI):
  - ترتیب `_id || cd` در `ptfHarvestFileKeys` قفل شد؛ بازگشت الگوی `cd-first` شکست می‌دهد.
  - اسکن کل `crm/*.js` برای الگوی A2 (`.cd || x._id`) — باید صفر باشد.
  - اجرای واقعی `arch-guard` به‌عنوان زیرфرآیند و assert روی exit=0.
  - سلامت توابع v34.7.52/53 (`ptfCloudKeyAudit`, `ptfPlanCloudKeyRemap`, remap بدون DELETE).
- **گیت CI: ۷۳ PASS / ۰ FAIL** (پیش از این نسخه: ۷۰/۳).
- نگهبان معماری: **PASS** — بدون تخلف مسدودکنندهٔ جدید؛ `arch-baseline.json` دست نخورد
  (A2 در مبنا همچنان تهی است — بدهی جدیدی پذیرفته نشد).

### نقاط بامپ نسخه (۷ نقطهٔ رسمی + تسترها)

`VERSION.json` · `crm/index.html` (PTF_CRM_RELEASE + ۱۰۴ پارامتر `?v=`) · `crm/sw.js`
(RELEASE/ASSET_VERSION/CACHE) · `crm/manifest.json` · `crm/clear-cache.html` ·
`crm/shell.js` · `api/sales-domain.php` — و به‌روزرسانی پین نسخه در تسترهای گیت
(tester449 تا tester456).

### یادداشت فرآیندی

این نسخه شاهد دیگری بر ضرورت مورد باز **H1** است (اعمال وصله‌های workflow برای اجرای
گیت UAT پیش از FTP): v34.7.53 با گیت قرمز به Production رفت چون گیت هنوز فقط محلی
اجرا می‌شود. وصله‌ها در `_tools/PENDING-workflow-*.patch` آماده‌اند و باید توسط کاربر
(با دسترسی workflow) اعمال شوند.

### باقی‌ماندهٔ مرتبط (خارج از این نسخه)

- Release Notes نسخه‌های v34.7.47 تا v34.7.53 هنوز نوشته نشده‌اند (بدهی مستندسازی PR #58).
- اجرای عملی audit/remap کلید ابری روی دادهٔ Production + تعیین تکلیف گروه‌های C و E.
- تعیین تکلیف PR #57 (خروجی رسمی PDF بدون واترمارک) و بستن PR #50 (منسوخ — محتوایش در main است).
