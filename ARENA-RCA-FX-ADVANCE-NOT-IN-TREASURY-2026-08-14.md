# RCA — پیش‌پرداخت پرونده ارزی در «بانک و خزانه» نمی‌نشیند

تاریخ: ۱۴۰۵/۰۵/۲۳ (۲۰۲۶-۰۸-۱۴)
دامنه: ماژول خزانه/بانک (crm/treasury.js) + ماژول پیش‌پرداخت (crm/petty.js)

## خلاصه

پرونده‌های ارزی (و ریالی) که پیش‌پرداختِ آن‌ها به‌صورت **«پرداخت کامل/نقدی ۱۰۰٪»**
(cashFull) ثبت شده است، در «بانک و خزانه» به‌عنوان وصولی دیده نمی‌شوند؛ چون پولِ
واقعیِ وصول‌شده ثبت شده است ولی خزانه فقط آرایه‌ی `payments[]` را می‌خواند که در این
حالت خالی است.

## مسیر داده

1. پیش‌پرداخت روی **پیشنهاد** (`ptf_crm_offers`) و زیر `o.advance` ذخیره می‌شود.
   - پیش‌پرداخت عادی: وصول‌ها در `o.advance.payments[]` (هر قلم: `amt` = معادل ریالی،
     `docAmt` = مبلغ ارزی، `rate`، `how`) ثبت می‌شود (crm/petty.js → `advancePaid`).
   - پیش‌پرداخت کامل/نقدی (۱۰۰٪): در `ptfAdvanceOpen` با `mode:'full'` (یا `pct===100`
     یا `docAmt>=total`) ثبت می‌شود؛ در این حالت `o.advance.paid=true` و
     `o.advance.cashFull=true` ست می‌شود اما **هیچ رکوردی در `payments[]` نمی‌نشیند**.

2. خزانه/بانک (crm/treasury.js → `ptfTreasuryCrmMoves`) ورودیِ نقد را از دو منبع می‌سازد:
   - `o.advance.payments[]` → «وصولی پیش‌پرداخت»
   - `inv.payments/pays[]` → «وصولی مشتری» (با حذف رکوردهای `fromAdvance` برای جلوگیری از دوباره‌شماری)

3. در حالت cashFull، منبع اول خالی است و منبع دوم (فاکتور) با `fromAdvance:true` حذف
   می‌شود → **پولِ وصول‌شده هیچ‌جا در گردش نقدی خزانه نمی‌نشیند.**

## ریشه‌یابی

تنها جایی که پیش‌پرداخت را مستقیم از `o.advance.payments[]` (نه از `ptfAdvanceNormalize`)
می‌خواند، `treasury.js` است. بقیه‌ی ماژول‌ها (`rbac.js`، `unofficial-invoice.js`،
`salesfiles.js`، `buycompare.js`) از `ptfAdvanceNormalize(o).receivedAmt` استفاده می‌کنند
که برای cashFull مقدار `amt` را برمی‌گرداند؛ بنابراین فقط خزانه دچار این شکاف بود.

## رفع (اعمال‌شده)

در `crm/treasury.js` در حلقه‌ی `ptf_crm_offers`:

- اگر `o.advance.payments` خالی باشد و `o.advance.cashFull` (یا `paid`) برقرار باشد،
  یک حرکت وصول به مبلغ `o.advance.amt` (معادل ریالی وصول‌شده) ساخته می‌شود:
  `key: 'advfull:…'`، `src: 'وصولی پیش‌پرداخت'`، `dir: 'in'`.
- مسیر قبلی (وصول‌های عادی از `payments[]`) دست‌نخورده باقی مانده و دوباره‌شماری
  رخ نمی‌دهد (در حالت cashFull پرداختی در `payments[]` وجود ندارد).

## دلایل احتمالی دیگر (برای بررسی هنگام وقوع مجدد)

1. **پیش‌پرداخت فقط «تعریف» شده ولی «وصول» ثبت نشده** — در این حالت `payments[]` خالی
   است و خزانه درست عمل می‌کند (پول هنوز واقعاً نیامده). در پنل مطالبات دکمه‌ی
   «+ ثبت وصول» باید زده شود.
2. **وصول با چک ثبت شده** — `payIsCheque` عمداً وصولِ چکی را از خزانه حذف می‌کند و
   تا «وصول چک وارده» در ماژول چک، وارد نقد نمی‌شود (رفتار درست، ضد دوباره‌شماری).
3. **ثبت روی «نسخه ریالی» به‌جای پیشنهاد ارزی مبدأ** — کد جلوی آن را می‌گیرد
   (`o.rialOf` در `ptfAdvanceOpen`/`advancePaid` مسدود است).

## فایل‌های تغییرکرده

- `crm/treasury.js` (افزودن شاخه‌ی cashFull در `ptfTreasuryCrmMoves`)
- Bump نسخه به `v34.5.34` برای اعمال با یک hard-refresh روی همه‌ی دستگاه‌ها:
  - `crm/sw.js` (`RELEASE` + `ASSET_VERSION`)
  - `crm/shell.js` (fallback ورژن)
  - `crm/manifest.json` (`version`)
  - `crm/clear-cache.html` (عنوان + `window.VER`)
  - `crm/index.html` (`PTF_CRM_RELEASE` + `?v=` تمام اسکریپت‌ها + لینک manifest)
  - `VERSION.json` (`crm_version`)

## نحوه‌ی اعمال روی کاربران

با تغییر `RELEASE` در سرویس‌ورکر، کش PWA با نام `ptf-crm-v34.5.34` تازه ساخته و
کش‌های قدیمی در رویداد `activate` حذف می‌شوند؛ تغییر `?v=` نیز کش HTTP مرورگر را
می‌شکند. بنابراین **یک بار hard-refresh (Ctrl+Shift+R)** برای دیدن کد جدید کافی است.
