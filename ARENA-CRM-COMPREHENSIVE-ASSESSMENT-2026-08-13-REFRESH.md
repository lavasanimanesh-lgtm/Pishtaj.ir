# گزارش جامع وضعیت CRM پیشرو تجهیز فرتاک

**تاریخ ارزیابی:** ۱۳ اوت ۲۰۲۶  
**نسخه ارزیابی‌شده:** `v34.5.11`  
**مبنای کد:** شاخه `arena/019ffb5b-pishtaj-ir`، کامیت پایه `45a18b6`  
**نوع ارزیابی:** بازبینی کد و پیکربندی، اجرای تست‌های موجود و بررسی ایستای امنیت/کیفیت. این گزارش جایگزین تست پذیرش انسانی، تست نفوذ و آزمون بازیابی روی سرور واقعی نیست.

---

## ۱. جمع‌بندی مدیریتی

CRM از نظر دامنه کسب‌وکار، یک محصول بالغ و پرقابلیت است: فروش و CRM، تأمین/RFQ، پیشنهاد و پرونده فروش، حساب مشتری/تأمین‌کننده، چک، تنخواه، خزانه، سال مالی، سهامداران، اسناد/پیوست و ابزارهای AI در یک محصول واحد پیاده‌سازی شده‌اند. نسخه در چهار نقطه اصلی (`VERSION.json`، صفحه CRM، Service Worker و manifest) یکپارچه و صحیح است.

با این حال، **آمادگی انتشار قابل اتکا هنوز پایین‌تر از بلوغ عملکردی محصول است.** مشکل اصلی «نبود کد» نیست؛ مشکل، نبود یک دروازه کیفیت اجرایی پیش از استقرار، انباشت تست‌های تاریخیِ قرمز، و متمرکز بودن زیاد منطق در فایل‌ها و صفحه اصلی بزرگ است.

### حکم کلی

| شاخص | ارزیابی |
|---|---|
| سلامت محصول در سطح کد | **B- / حدود ۷۳ از ۱۰۰** |
| آمادگی انتشار بدون کنترل دستی | **C+** |
| ریسک داده‌های مالی و ضمیمه | **متوسط رو به بالا** تا زمان تأیید بک‌آپ خارج از هاست و بازیابی |
| ریسک امنیتی فعلی | **متوسط**؛ کنترل‌های پایه خوب است، CSP و سطح XSS نیازمند کار است |
| توصیه مدیریتی | انتشار فقط پس از عبور از گیت حداقلی، تریاژ شکست‌های مالی/دسترسی، و تأیید بازیابی داده |

> نکته مهم: ۵۶ تستر قرمز الزاماً به معنی ۵۶ نقص محصول نیستند. تعداد قابل توجهی از آن‌ها تست‌های نسخه/رابط تاریخی‌اند که با بازطراحی‌های بعدی همگام نشده‌اند. اما تا زمانی که هر مورد به «تست کهنه»، «تغییر عمدی قرارداد» یا «باگ واقعی» برچسب نخورد، وضعیت کیفیت قابل اتکا نیست.

---

## ۲. شواهد و روش ارزیابی

### اندازه و ساختار

| مورد | مقدار تأییدشده |
|---|---:|
| ماژول‌های JavaScript در `crm/` | ۹۸ فایل / **۵۱٬۰۷۰ خط** |
| PHP سمت سرور در `api/` | ۳۱ فایل / **۷٬۹۱۲ خط** |
| بزرگ‌ترین فایل‌های محصول | `crm/index.html` = ۴٬۰۲۳ خط و ۳۲۰KB؛ `offers.js` = ۳٬۱۴۵ خط؛ `api/tools.php` = ۱٬۹۳۵ خط؛ `petty.js` = ۱٬۸۳۶ خط |
| اسکریپت‌های صفحه CRM | ۱۰۱ تگ script، ۹۶ مورد async/defer؛ بارگذاری هنوز فایل‌محور و بدون bundling است |
| فایل‌های رهگیری‌شده مخزن | ۲٬۸۱۹ فایل، حدود ۸۷MB |
| مستندات Markdown در ریشه | ۸۵۶ فایل (و ۹۶۳ فایل MD در کل مخزن) |

### تست‌های اجراشده

| آزمون | نتیجه |
|---|---|
| `node --check` برای تمام فایل‌های JS در `crm/` و `api/` | **موفق**؛ خطای syntax مشاهده نشد |
| `node _tools/uat/run-ci-gate.js` | **۱۷ PASS / ۰ FAIL** |
| `node _tools/run-full-regression.js` | **۳۲۵ فایل PASS، ۵۶ فایل FAIL، ۵ فایل placeholder/soft** از ۳۸۱ تستر؛ ۵٬۷۳۰ check PASS و ۱۱۷ check FAIL |
| `php -l` | **اجرا نشد**؛ مفسر PHP در محیط ارزیابی نصب نبود |

سوئیت کامل حدود ۶ دقیقه زمان برد و hang گزارش نکرد. خروجی ماشین‌خوان در `_tools/last-regression.json` ثبت شده است.

---

## ۳. کارنامه حوزه‌ها

| حوزه | نمره از ۵ | وضعیت | شواهد و تفسیر |
|---|---:|---|---|
| پوشش فرایند کسب‌وکار | ۴٫۵ | قوی | فروش، تأمین، RFQ، پیشنهاد، پرونده، مالی، چک، تنخواه، خزانه، اسناد و AI همگی ماژول مستقل دارند. |
| معماری داده و همگام‌سازی | ۳٫۵ | قابل قبول با ریسک | MySQL سمت سرور، کش/صف آفلاین و `SYNC_KEYS` گسترده وجود دارد؛ ولی مدل key-value و LWW برای رخدادهای چندبخشی مالی مرز ریسک دارد. |
| امنیت و کنترل دسترسی | ۳٫۵ | متوسط رو به خوب | HMAC، `password_verify`، RBAC، rate-limit و headerهای امنیتی دیده شد؛ CSP فعلی هنوز `unsafe-inline` و `unsafe-eval` دارد. |
| تست و تضمین کیفیت | ۲٫۵ | نیازمند اصلاح فوری فرایند | گیت ۱۷تایی سبز است، ولی سوئیت کامل ۵۶ فایل قرمز دارد و workflow رسمی CI عملاً خراب است. |
| DevOps و انتشار | ۲٫۵ | ناکافی برای انتشار خودکار | انتشار production کنترل دستی و rollback/tag دارد؛ اما CI فعلی Composer را روی مخزنی بدون composer manifest اجرا می‌کند و staging قبل از تست deploy می‌شود. |
| عملکرد و نگهداشت‌پذیری | ۲٫۵ | ریسک رشد | ۱۰۱ script و HTML ۳۲۰KB، ماژول‌های ۱–۳ هزار خطی و ۳۲۲ استفاده از `localStorage`، هزینه تغییر و کارایی موبایل را بالا می‌برد. |
| UX/PWA و موبایل | ۳٫۵ | خوب، نیازمند تست واقعی | manifest، service worker، RTL و کارهای جدی موبایل وجود دارد؛ اما آزمون با دستگاه/مرورگر واقعی در این ارزیابی انجام نشد. |
| مستندسازی و مدیریت دانش | ۳ | غنی اما نامنظم | مستندات، RCA و runbook زیاد است؛ ۸۵۶ فایل MD در ریشه، پیدا کردن «منبع حقیقت» را سخت می‌کند. |
| بازیابی و تداوم کسب‌وکار | ۳ | نیازمند تأیید عملی | backup/snapshot و مسیرهای بازیابی وجود دارند؛ offsite backup و تمرین restore روی محیط واقعی از این مخزن قابل تأیید نیست. |

---

## ۴. یافته‌های اولویت‌دار

### P0 — قبل از هر انتشار مهم

#### ۱) CI رسمی معتبر نیست و قبل از استیجینگ اجرا نمی‌شود
- فایل `.github/workflows/php.yml` فقط `composer validate` و `composer install` دارد، در حالی که در مخزن `composer.json`/`composer.lock` وجود ندارد؛ بنابراین این workflow در GitHub شکست می‌خورد.
- گیت درست Node (`_tools/uat/run-ci-gate.js`) در مخزن وجود دارد و در اجرای محلی **۱۷/۱۷ سبز** شد، اما به workflow اصلی متصل نیست. patchهای معلقی نیز در `_tools/PENDING-workflow-*.patch` وجود دارند؛ patch معلق، کنترل فعال نیست.
- workflow استیجینگ روی push شاخه‌های `arena/**` deploy می‌کند، اما هیچ job تستی پیش از FTP ندارد.

**اقدام:** workflow PHP/Composer را با یک workflow کیفیت واقعی جایگزین کنید: Node syntax check + CI gate + PHP lint (روی runner دارای PHP) و سپس deploy staging با `needs: quality`. انتشار production نیز باید همین gate را الزام کند.

#### ۲) شکست‌های مالی و RBAC باید تریاژ دستی شوند
در خروجی کامل، چند شکست مستقیماً به حوزه‌های حساس اشاره دارند؛ از جمله سود/وصول (`tester35`، `tester100`)، نقش و محرمانگی سال مالی/سهامداران/تنخواه (`tester106`، `tester122`، `tester97`)، پرداخت و چک (`tester104`، `tester145`) و مسیرهای پرونده/فاکتور (`tester105`، `tester110`).

این موارد ممکن است از تغییر عمدی UI یا قرارداد تست ناشی شده باشند، اما به علت ماهیت مالی/دسترسی نباید صرفاً archive شوند.

**اقدام:** برای هر تستر یک نتیجه مستند ثبت کنید: `BUG`، `EXPECTED_CHANGE` یا `OBSOLETE_TEST`. موارد BUG در اولویت رفع، و موارد دیگر بازنویسی/آرشیو شوند.

### P1 — در اسپرینت بعدی

#### ۳) کاهش سطح حمله XSS
CSP در `.htaccess` از headerهای مهم مانند HSTS، `nosniff` و `frame-ancestors` استفاده می‌کند، اما `script-src` شامل `'unsafe-inline'` و `'unsafe-eval'` است. دست‌کم `new Function` در `crm/cms.js` و نیز نقاط فراوان `innerHTML` / `insertAdjacentHTML` وجود دارد. این به خودی خود اثبات آسیب‌پذیری نیست، ولی عمق دفاع CSP را کم می‌کند؛ مخصوصاً برای داده‌های برگشتی از AI، CMS و فرم‌ها.

**اقدام:** ابتدا `new Function` را حذف و parser امن جایگزین کنید؛ سپس event handlerهای inline را به listener منتقل، CSP را با nonce/hash سخت‌گیرانه کنید، و یک تست XSS بازگشتی برای هر surface ورودی اضافه کنید.

#### ۴) داده و بازیابی: تأیید عملی، نه صرفاً کد
همگام‌سازی شامل کلیدهای مالی، چک، تنخواه و `sales_returns` است و guard ضد نوشتن خالی دارد. با این وجود:
- رخدادهای مالی چندکلیدی (مثلاً چک + گردش مالی + بدهی) ذاتاً می‌توانند بین چند write پخش شوند؛ تراکنش اتمیک سراسری آن‌ها در سطح مرور کد تضمین نشد.
- سلامت offsite backup، versioning باکت و restore-test از مخزن قابل اثبات نیست.
- گزارش‌های پیشین مخزن نیز بر حساسیت purge/بایگانی ضمیمه تأکید دارند؛ باید وضعیت patchهای آن روی production با تست واقعی تأیید شود.

**اقدام:** برنامه ۳۰ روزه backup خارج از هاست، retention مشخص، restore drill مستند و reconciliation روزانه برای زنجیره‌های مالی تعریف شود.

#### ۵) عملکرد و مرزبندی ماژول‌ها
CRM در یک `index.html` ۳۲۰KB، با ۱۰۱ script و فایل‌های بزرگ چند هزارخطی اجرا می‌شود. async/defer از مسدود کردن parser می‌کاهد، اما تعداد درخواست‌ها، ترتیب وابستگی و cache invalidation همچنان ریسک دارد. استفاده پرتعداد از `localStorage` نیز در داده حجیم یا موبایل کم‌ظرفیت مسئله‌ساز می‌شود.

**اقدام:** ابتدا اندازه‌گیری واقعی با Lighthouse/Web Vitals روی CRM، سپس code-splitting ماژول‌های کم‌مصرف (AI/CMS/گزارش‌های سنگین)، استخراج vendor، و تعریف بودجه performance. تغییر را تدریجی و با smoke test انجام دهید.

### P2 — بهبود پایدار

#### ۶) بدهی تست و مستندسازی
۵۶ تستر قرمز و ۵ تست placeholder نشان می‌دهند سوئیت تاریخی به‌عنوان «معیار انتشار» قابل استفاده نیست. همچنین صدها Markdown ریشه باعث تکثیر نسخه‌های backlog، handover و assessment شده‌اند.

**اقدام:**
1. مالک و تاریخ انقضا برای هر تست تعیین شود.
2. تست‌های obsolete به `_tools/uat/archive/` منتقل شوند، نه اینکه در runner فعال بمانند.
3. شاخص کیفیت به جای «تعداد فایل تست» شامل: pass rate، تعداد BUG باز P0/P1، و زمان اجرای gate باشد.
4. مستندات قدیمی به `docs/archive/YYYY-MM/` منتقل و یک `docs/INDEX.md` به‌عنوان نقشه منابع رسمی ایجاد شود.

#### ۷) دسترس‌پذیری و تست موبایل واقعی
CRM PWA/RTL و دو تصویر دارای alt دارد، اما نشانه‌های ARIA در صفحه اصلی محدود است (۸ مورد از چهار نوع). بررسی کیبورد، focus trap مودال، کنتراست، screen-reader، شبکه ضعیف و Android/iOS در این بررسی انجام نشده است.

**اقدام:** یک سناریوی UAT کوتاه اما واقعی برای نقش‌های فروش، تأمین، حسابدار و مدیر روی موبایل و دسکتاپ تهیه و به gate انتشار وصل شود.

---

## ۵. نقاط قوت قابل اتکا

1. **پوشش عملیاتی وسیع:** CRM از lead تا RFQ، پیشنهاد، پرونده، وصول، تأمین، چک و گزارش مالی را پوشش می‌دهد؛ این یکپارچگی مزیت اصلی محصول است.
2. **امنیت پایه مناسب:** password hashing/verification، HMAC، نقش‌محوری سمت سرور، rate limiting در endpointها و headerهای امنیتی وجود دارد.
3. **کنترل نسخه یکپارچه:** نسخه `v34.5.11` میان `VERSION.json`، `crm/index.html`، `crm/sw.js` و `crm/manifest.json` هم‌خوان است؛ این برای جلوگیری از drift کش PWA مهم است.
4. **حداقل گیت رگرسیون قابل استفاده:** گیت ۱۷تایی در اجرای واقعی سبز شد و حوزه‌های اعلان، sync، مالی، چک، password rehash و قفل مهاجرت production را پوشش می‌دهد.
5. **انتشار production کنترل‌شده:** production فقط با تایید صریح `DEPLOY`، منبع `main` یا tag و ایجاد tag/release backup قابل اجراست؛ این از انتشار ناخواسته بهتر جلوگیری می‌کند.
6. **فرهنگ مستندسازی RCA:** برای رخدادها و تصمیم‌ها سندهای فراوان وجود دارد؛ ارزش آن با نظم‌دهی بیشتر بسیار بالاتر می‌رود.

---

## ۶. نقشه اقدام پیشنهادی

### ۰ تا ۷ روز — تثبیت انتشار
- [ ] workflow کیفیت واقعی را فعال و `deploy-staging` را وابسته به آن کنید.
- [ ] PHP را در CI lint کنید؛ تست محلی فعلی به علت نبود PHP این پوشش را نداشت.
- [ ] ۵۶ تستر قرمز را triage کنید؛ ابتدا ۱۲ مورد مالی/RBAC/چک/وصول.
- [ ] ۵ placeholder را حذف یا به تست معنادار تبدیل کنید.
- [ ] وضعیت آخرین restore و backup خارج از هاست را با مالک، زمان و نتیجه ثبت کنید.

### ۸ تا ۳۰ روز — کاهش ریسک داده و امنیت
- [ ] تست restore کامل (MySQL + metadata + پیوست‌های S3) را در محیط جداگانه تمرین کنید.
- [ ] reconciliation برای زنجیره‌های مالی چندکلیدی و گزارش مغایرت عملیاتی اضافه کنید.
- [ ] `new Function` را حذف و برنامه حذف `unsafe-eval`/inline CSP را اجرا کنید.
- [ ] تست‌های XSS، RBAC و عملیات مخرب attachment را به گیت اضافه کنید.

### ۳۱ تا ۹۰ روز — مقیاس‌پذیری و نگهداشت
- [ ] dashboard کیفیت با معیارهای release ایجاد کنید.
- [ ] مستندات ریشه را index/archive کنید.
- [ ] performance budget و code-splitting تدریجی اجرا کنید.
- [ ] UAT چنددستگاهی و accessibility baseline را به تعریف Done اضافه کنید.

---

## ۷. معیار تصمیم انتشار

### انتشار hotfix کم‌خطر
مجاز است اگر: گیت ۱۷تایی سبز، syntax JS/PHP سبز، تغییر خارج از مالی/RBAC/Sync باشد، و تست دستی همان مسیر ثبت شود.

### انتشار معمولی
مجاز است اگر: workflow کیفیت فعال و سبز، تسترهای متاثر از تغییر سبز، مورد P0 باز وجود نداشته باشد، و rollback/ref مشخص باشد.

### انتشار تغییر مالی، Sync، مجوز یا پیوست
علاوه بر موارد بالا: سناریوی end-to-end چندکاربره، بررسی نقش غیرمجاز، refresh/offline، و reconciliation پس از عملیات الزامی است. در تغییرهای storage، restore نمونه ضمیمه نیز لازم است.

---

## ۸. محدودیت‌های ارزیابی

- دسترسی به production/staging، پایگاه‌داده واقعی، لاگ‌ها، تنظیمات آروان/S3 و secretهای خارج از مخزن وجود نداشت.
- PHP در محیط اجرا موجود نبود؛ بنابراین lint PHP و تست endpoint واقعی انجام نشد.
- اسکن ایستای `innerHTML`/CSP به معنی اثبات یک exploit نیست؛ برای قطعیت به تست نفوذ کنترل‌شده نیاز است.
- نتایج full regression ترکیبی از تست محصول و تست‌های تاریخی/documentation هستند؛ به همین دلیل شکست‌ها باید تریاژ شوند، نه اینکه همگی باگ محصول تلقی گردند.

---

## نتیجه نهایی

پروژه از منظر قابلیت‌های CRM و بلوغ عملکردی، سرمایه نرم‌افزاری ارزشمندی دارد و پایه‌های امنیت، همگام‌سازی و PWA نیز وجود دارند. مانع اصلی برای رسیدن به سطح «انتشار مطمئن و قابل مقیاس» **نظم مهندسی کیفیت** است: CI فعال، سوئیت تست قابل اعتماد، تست بازیابی واقعی و کاهش تدریجی پیچیدگی فرانت‌اند. پیشنهاد این ارزیابی، توقف توسعه قابلیت‌های کم‌اولویت برای یک اسپرینت کوتاه تثبیت است؛ بازده این کار از افزودن قابلیت جدید بیشتر خواهد بود.
