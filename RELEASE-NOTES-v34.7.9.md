# Release Notes v34.7.9 — حفظ پرونده فعال هنگام حذف نسخه بایگانی

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## رفع خطر حذف پرونده فعال مشترک

- پیش‌بررسی حذف بایگانی اکنون قبل از ساخت graph حذف، وجود هر پرونده فعال با پیشنهاد، درخواست یا شناسه ریشه مشترک را تشخیص می‌دهد.
- اگر پرونده فعال مشترک وجود داشته باشد، عملیات خودکار به حالت `archive_shell_only` تغییر می‌کند.
- در این حالت فقط موارد زیر حذف می‌شوند:
  - کارت/snapshot بایگانی انتخاب‌شده؛
  - فایل‌های ابری منحصربه‌فرد همان snapshot که هیچ رکورد دیگری به آن‌ها ارجاع ندارد؛
  - snapshot سال مالی متاثر تا گزارش با پرونده فعال واقعی دوباره ساخته شود.
- موارد زیر صریحاً محفوظ می‌مانند:
  - پرونده فعال؛
  - پیشنهاد و RFQ مشترک؛
  - اسناد و فایل‌های مشترک؛
  - فاکتور، دریافت، تخصیص، خرید، هزینه، چک و سایر آثار پرونده فعال.
- tombstone در حالت shell-only فقط شناسه نسخه بایگانی را نگه می‌دارد و هیچ شناسه‌ای از پرونده فعال، پیشنهاد یا RFQ وارد دامنه عدم بازگشت نمی‌شود.

## شفافیت رابط کاربری

- ویزارد با کادر زرد اعلام می‌کند «پرونده فعال مشترک شناسایی شد — حذف محدود به نسخه بایگانی است».
- شناسه پرونده فعال محفوظ در همان کادر نمایش داده می‌شود.
- متن تایید و دکمه نهایی در این حالت به‌صراحت «حذف فقط نسخه بایگانی و حفظ پرونده فعال» است.
- پیام موفقیت تعداد پرونده‌های فعال حفظ‌شده را اعلام می‌کند.

## ادغام دو پرونده باز یکسان

مسیر ادغام کنترل‌شده نسخه v34.7.7 حفظ شده است:

1. در بالای بخش پیشنهادها، روی `🧭 راهنمای بررسی و رفع` کنار یافته «۲ پرونده برای پیشنهاد» کلیک کنید.
2. هر دو پرونده را با مرحله، مدارک و وابستگی‌ها مقایسه کنید.
3. پرونده‌ای که باید باقی بماند را در فیلد `پرونده‌ای که باقی می‌ماند` انتخاب کنید.
4. پرونده دیگر را در فیلد `پرونده‌ای که داخل پرونده اصلی ادغام می‌شود` انتخاب کنید.
5. دلیل را وارد و کلمه `ادغام` را تایپ کنید.
6. سرور مدارک، رویدادها و ارجاعات را منتقل، FIFO را بازسازی و snapshot پرونده ادغام‌شده را در حسابرسی نگهداری می‌کند.

## کنترل کیفیت

- `tester414-v34.7.9-shared-archive-preserve.js`: 12 PASS / 0 FAIL
- `tester413-v34.7.8-archived-test-purge.js`: 22 PASS / 0 FAIL
- CI متمرکز ریلیز: 33 PASS / 0 FAIL
- PHP parser و syntax فایل‌های تغییرکرده: PASS
- رگرسیون کامل: 397 تستر، 340 فایل PASS، 57 فایل بدهی تاریخی FAIL، 5887 check PASS و 129 check تاریخی FAIL؛ هیچ شکست جدیدی نسبت به v34.7.8 ایجاد نشد.
- Auditor روی 624 صفحه و هم‌ترازی VERSION/PWA با 100 مرجع cache-query: PASS
- PHP CLI در محیط محلی موجود نبود و lint داخلی CI به‌صورت صریح skip شد.
