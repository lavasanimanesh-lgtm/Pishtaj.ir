# 🚀 ریلیزنوت v20.0 — اسپرینت ۳ از ۵ پیاپی: BUG-033 «یکدستی کنترل‌های مودال + رفع تداخل دستیار»

**مبنا:** ابلاغ کارفرما: «برخی مودال‌ها کلیدهای کنترل متفاوت (بیضی بزرگ) دارند و دستیار AI روی دوتای آنها را می‌پوشاند.»

## ✅ ریشه و رفع
- **ریشه:** چیپ بیضی بنفش «🤖 دستیار» (US-300) به‌صورت عنصر جدا با `position:absolute;left:74px` روی برخی مودال‌ها اضافه می‌شد و بسته به عرض پنجره، روی نقاط کنترل مینیمایز/تمام‌صفحه می‌افتاد.
- **رفع (یکپارچه سراسری):** بیضی حذف شد؛ «دستیار» حالا **نقطه چهارم مک‌استایل** است — بنفش، هم‌اندازه و هم‌ردیف سه نقطه (بستن/مینیمایز/تمام‌صفحه) داخل همان نوار `mx-dots` ⟵ در **همه** پنجره‌ها (md-b و ptfdlg-b) یکسان و هیچ‌چیز را نمی‌پوشاند.
- رفتار قبلی حفظ شد: کلیک = مینیمایز با حفظ کامل اطلاعات فرم ⟵ رفتن به پنل دستیار.
- **پاکسازی دفاعی:** اگر مودالی از قبل بیضی قدیمی داشته باشد، هنگام تجهیز حذف می‌شود؛ CSS موبایل مخصوص بیضی از index حذف شد.
- ممیزی کل پنجره‌ها: هر دو خانواده مودال (md-b/ptfdlg-b) از یک مسیر equip می‌شوند + MutationObserver پنجره‌های آینده را هم پوشش می‌دهد ⟵ ضمانت یکدستی برای فرم‌های جدید.

## 🧪 تست
tester116: ۱۸ چک (حذف کامل بیضی، نقطه چهارم داخل bar، رفتار US-300، پاکسازی دفاعی، پوشش سراسری) + tester43/57 نسخه‌پذیر و هم‌راستا (قاعده BUG-026).

**فایل‌ها:** modalx.js (نقطه چهارم + پاکسازی) | index.html (حذف CSS بیضی + v20.0) | sw.js | tester116 + tester43/57 به‌روز.
