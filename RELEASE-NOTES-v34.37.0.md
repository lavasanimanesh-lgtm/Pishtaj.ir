# یادداشت انتشار — v34.37.0

**تاریخ:** ۲۰۲۶-۰۹-۰۵ (۱۴۰۵/۰۶/۱۴) · **پایه:** v34.36.4/v34.36.5
**سند ارزیابی مرتبط:** `ARENA-TRIAGE-3-ITEMS-2026-09-05.md`
**گیت CI:** ۲۱۲ PASS / ۰ FAIL (پیش از این انتشار: ۲۰۴ PASS / ۶ FAIL)

این انتشار سه گزارش کارفرما را **ریشه‌ای** می‌بندد. هر سه با تسترِ **رفتاری** (نه رشته‌ای) قفل شده‌اند، چون دلیل ماندگاری هر سه باگ همین بود که تسترهای قبلی فقط وجودِ یک رشته در فایل را می‌سنجیدند و با وجود باگِ زنده سبز می‌ماندند.

---

## ① P0 — «در CRM مشتریانی که جدیداً اضافه می‌شوند پاک می‌شوند»

### ریشه (بازتولید قطعی شد)

سنگ‌قبرِ حذف (`archive_purge` در `ptf_crm_deleted_archive`) **دائمی** بود و فقط با `cd` کار می‌کرد؛ از سوی دیگر مولد کد کلاینت (`legacyMaxNext`) کد بعدی را از «بیشینهٔ رکوردهای زنده + ۱» می‌ساخت. پس با حذفِ آخرین مشتری، بیشینه پایین می‌آمد و **همان کد دوباره صادر می‌شد** — و چون `sync_apply_tombstones` هم در `data_push` و هم در `data_pull` اعمال می‌شود، مشتری کاملاً جدید در اولین سینک پاک می‌شد. سناریوی کاربر: «یکی را اشتباه ثبت کردم، پاکش کردم، مشتری درست را ثبت کردم و آن هم پرید.»

### اصلاحات

| # | محل | تغییر |
|---|---|---|
| ۱ | `api/crm.php` — `sync_tombstone_epoch` / `sync_row_created_epoch` / `sync_tombstone_outranks_row` | سنگ‌قبر دیگر حق حذف رکوردی را ندارد که **اثبات‌پذیر بعد از آن** ساخته شده. اگر تاریخ سنگ‌قبر یا مهر ساخت رکورد ناخوانا باشد، رفتار **دقیقاً** مثل قبل است (سازگاری کامل عقب‌رو؛ این گارد فقط «نجات» می‌دهد، هرگز حذف اضافه نمی‌کند) |
| ۲ | `api/crm.php` — `sync_tombstone_mark` | نقشهٔ شناسه‌ها به‌جای `true` تازه‌ترین epoch را نگه می‌دارد؛ سنگ‌قبرِ بدون تاریخ همیشه می‌چربد |
| ۳ | `api/crm.php` — دامنهٔ `aliases` | تطبیق زیررشته‌ای فقط برای «پاک‌سازی گراف کل پروژه» (سنگ‌قبر **بدون** `collection`) می‌ماند. سنگ‌قبرِ تک‌رکوردی (`entity_delete`, حذف پکینگ‌لیست) دیگر alias وارد نمی‌کند ⇒ مشتریِ بی‌ربطی که فقط کدِ حذف‌شده را در توضیحاتش دارد پاک نمی‌شود |
| ۴ | `crm/sync.js` — `ptfTombstoneEpoch/ptfRowCreatedEpoch/ptfTombstoneOutranksRow/ptfTombstoneMark` | قرینهٔ دقیق سرور در کلاینت — دو موتور دیگر واگرا نیستند |
| ۵ | `crm/codegen.js` — `retiredCodesForKey` / `ptfCodeIsRetired` / `legacyMaxNext` | کدهای **بازنشسته** (دارای سنگ‌قبر) هم مصرف‌شده حساب می‌شوند ⇒ شمارنده هرگز عقب نمی‌رود. (استخر کد سرور از قبل سالم بود؛ باگ فقط در همین مسیر fallback بود) |
| ۶ | `crm/offers.js` — `ptfAllocCustCode` | «یکتا در فهرست زنده» به «یکتا در زنده **و** بازنشسته» ارتقا یافت |
| ۷ | `crm/dedup.js` — `dedupStamp` | مهر `createdAtISO` روی هر رکورد تازه — ورودیِ لازم برای گاردِ تاریخ. مهر موجود بازنویسی نمی‌شود |
| ۸ | `crm/custmerge.js` | سنگ‌قبر ادغام مشتری هم `iso` می‌گیرد |
| ۹ | `crm/sales-domain-v2.js` — سپر حذف انبوه | برای مجموعه‌های هویتیِ کسب‌وکار، «بیش از ۳ حذف در یک ذخیره» یا «فهرست تهی شد» به مسیر امن legacy تنزل می‌یابد و **هیچ فرمان مخربی صادر نمی‌شود**. حذف تک‌رکوردیِ واقعیِ کاربر دست‌نخورده است؛ ژورنال‌ها (audit/notifs) مشمول سپر نیستند تا هرس عادی نشکند |
| ۱۰ | `crm/sales-domain-v2.js` — `retryWithFreshCode` | پاسخ `409 entity_id_exists` سرور (که خودش `hint=regenerate_client_code` می‌فرستد) واقعاً مصرف می‌شود: یک نوبت با کد نو دوباره ارسال، اصلاح کد محلی، ثبت در audit و اطلاع به کاربر |

### آنچه عمداً تغییر نکرد
- حذف واقعی همچنان حذف است (تستر ۱.۲).
- سنگ‌قبرهای legacy بدون تاریخ و رکوردهای بدون مهر ساخت: رفتار قبلی (تسترهای ۱.۳ و ۱.۴).
- «پاک‌سازی گراف پروژهٔ بایگانی» بدون رگرسیون (تستر ۱.۶).

---

## ② P0 — «ارجاع فاکتور بدون تایید کاربر و بدون امکان بازگشت»

### ریشه
`sfInvoiceRef` مستقیم `commit` می‌کرد. تنها دیالوگِ روی مسیر، پرسشِ «پیامک فرستاده شود؟» بود که **بعد** از ثبتِ قطعی می‌آمد و کاربر آن را تاییدِ ارجاع می‌فهمید. `invRef` هم فقط نوشته می‌شد و هیچ‌جای CRM پاکش نمی‌کرد؛ تنها فرمانِ سروریِ توانا به پاک کردنش (`revoke_orphan_delete`) با وجود پروندهٔ فعال همیشه ۴۰۹ می‌داد — و چون ارجاع اصلاً فقط از داخل پرونده ممکن است، آن در **ساختاراً بسته** بود.

### اصلاحات

| # | محل | تغییر |
|---|---|---|
| ۱ | `crm/salesfiles.js` — `sfInvoiceRefPlan` | منطق حل‌وفصل از منطق نوشتن جدا شد؛ تابعی **خالص** که هیچ چیزی نمی‌نویسد و خلاصهٔ کامل برمی‌گرداند |
| ۲ | `crm/salesfiles.js` — `sfInvoiceRefConfirm` | مودال تایید با خلاصهٔ خواندنی: پرونده، خریدار، پیشنهاد برنده، مبلغ ارزی، **مبنای ریالی + نرخ تسعیر + جمع ریالی**، و هشدار صریح: «لغو این ارجاع فقط تا پیش از ثبت فاکتور و فقط توسط ادمین/رئیس هیئت‌مدیره ممکن است» |
| ۳ | `crm/salesfiles.js` — `sfInvoiceRefFinish(cd, sendSms)` | تصمیم پیامک از `confirm`ِ پس از ثبت به **تیکِ داخل مودالِ پیش از ثبت** منتقل شد ⇒ دیگر با تاییدِ ارجاع اشتباه گرفته نمی‌شود |
| ۴ | `crm/salesfiles.js` — مسیرهای ارزی | `sfInvoiceRefPickRialDo` و `sfInvoiceRefRialDo` هم از همان مودال تایید عبور می‌کنند (انتخاب سند/ورود نرخ ≠ تاییدِ ارجاع) |
| ۵ | `api/sales-domain.php` — فرمان تازهٔ `revoke_invoice_ref` | فقط `invRef` را برمی‌گرداند و به وضعیت «برنده» و خود پرونده دست نمی‌زند. گاردهای fail-closed: نقش ادمین/رئیس · دلیل اجباری · **هر فاکتور فعال (رسمی یا غیررسمی) ⇒ ۴۰۹** · snapshot کامل در `ptf_crm_corrections` · ردپای انسانی روی خط زمانی پرونده |
| ۶ | `crm/sales-domain-v2.js` — `ptfRevokeInvoiceRef` | خلاصهٔ ارجاعِ در حال لغو، دلیل اجباری، و پیام قابل‌اقدام برای خطای `invoice_exists` («ابتدا فاکتور را ابطال کنید») |
| ۷ | `crm/rbac.js` — پنل فاکتورها | دکمهٔ **«↩️ لغو ارجاع»** — دقیقاً همان خواستهٔ صریح کارفرما؛ فقط تا پیش از ثبت فاکتور و فقط برای نقش مجاز |
| ۸ | `crm/salesfiles.js` — `sfInvoiceRefUndoHtml` | همان دکمه روی کارت پرونده. اگر فاکتور ثبت شده باشد، به‌جای غیب شدنِ بی‌توضیح، **دلیل بسته بودن مسیر** نوشته می‌شود |
| ۹ | `crm/rbac.js` — `refToInvoice` | ~۲۰ خط کد مرده پس از `alert()+return` حذف شد. تنها نویسندهٔ `invRef` در کل CRM اکنون `salesfiles.js` است |

پس از لغو، چون `invRef` سیگنالِ `sfStageOf` است، مرحلهٔ پرونده خودبه‌خود از ۸ به ۷ برمی‌گردد و دکمهٔ «ارجاع فاکتور» دوباره فعال می‌شود — یعنی می‌توان با **مبنای ریالی/نرخ درست** دوباره ارجاع داد.

---

## ③ P1 — «در فرم ثبت‌نام تامین‌کنندگان فایل اضافه نمی‌شود»

اصلاحات v34.36.2 («موفقیتِ سبزِ بی‌صدا») دست‌نخورده باقی ماند؛ این انتشار سه نقصِ **باقی‌مانده** را می‌بندد که هنوز همان تجربه را تولید می‌کردند.

| # | محل | تغییر |
|---|---|---|
| ۱ | `api/crm.php` — `$supWillReject` | **پایان آبجکت یتیم:** `save_attachment` پیش از شاخهٔ `duplicate` اجرا می‌شد، پس فایل واقعاً روی فضای ابری نوشته و بعد دور ریخته می‌شد (هم هزینهٔ آروان، هم «فایلم رفت و هیچ‌جا نیست»). حالا پیش از هر آپلودی مشخص می‌شود که این درخواست اصلاً مقصدی دارد یا نه |
| ۲ | `api/crm.php` — پاسخ `duplicate` | اگر کاربر فایلی همراه کرده بود، صریح می‌گوید ذخیره نشد، **چرا**، و راه جبران (ایمیل/واتساپ با ذکر کد) |
| ۳ | `api/crm.php` — `$supPhoneMatchesDup` | گاردِ بازیابیِ پیوست فقط `phone` را می‌سنجید در حالی که تشخیصِ تکراری روی `phone`/`ph`/`mob` است ⇒ «ارسال دوبارهٔ فایل» در دو مسیر از سه مسیر همیشه `duplicate` می‌گرفت. حالا هر سه فیلد با همان `ptf_dedup_phone` سنجیده می‌شود |
| ۴ | `api/crm.php` — `$supMatches` | حلقهٔ dedup روی «اولین تطابق» می‌شکست؛ حالا همهٔ تطابق‌ها جمع و رکوردِ **pending/rejected و بدون پیوست** ترجیح داده می‌شود |
| ۵ | `supplier/index.html` | `accept` فیلد فایل با allowlist سرور و `ATTACH_EXT` **یکی** شد (تصویر از قبل مجاز بود ولی پنجرهٔ انتخاب فایل فیلترش می‌کرد) |
| ۶ | `api/crm.php` | `error_log` سروری برای شکست پیوست (فقط متادیتای محدودیت‌های میزبان، بدون هیچ داده حساس) — تا وقتی رکورد ساخته نمی‌شود هم ردّ پایدار بماند |

> **نکتهٔ عملیاتی:** مسیر پیوست **فقط فضای ابری** است و هیچ fallback محلی ندارد. اگر `storage-config.php` روی میزبان نباشد یا نامعتبر باشد، هیچ فایلی ذخیره نمی‌شود. پیش از هر تغییر کدی، در مودال همان رکورد در CRM مقدار `storageConfigReady` را ببینید.

---

## ④ بهداشت و امنیت

- **`.gitignore`:** `storage-config.php` / `llm-config.php` / `sms-config.php` اضافه شدند. فایل واقعی امروز در ریپو نیست، ولی اگر کسی آن را در ریشهٔ سایت می‌ساخت، **کلید و رمز فضای ابری وارد گیت‌هاب می‌شد**.
- **رفع بدهی نگهبان معماری:** تخلف `empty-map-key` در `crm/offers.js` (از v34.36.5) رفع شد؛ به‌تنهایی ۵ شکست گیت (`arch-guard`, `tester457`, `tester443`, `tester431`, `tester552`) را باز کرده بود.
- **رفع بایپس W1-iterate:** `else if (typeof setData === 'function')` در heal مشتری به فرم محافظت‌شدهٔ استاندارد برگشت (`tester526`).
- **هم‌ترازی نسخه:** `VERSION.json`, `crm/index.html` (شامل ۱۰۹ cache-buster), `crm/sw.js`, `crm/manifest.json`, `crm/cms.js`, `crm/shell.js`, `crm/clear-cache.html`, `SD_SERVICE_VERSION` و pinهای ۱۴۰ تستر.

---

## ⑤ تسترهای تازه (همه رفتاری، در گیت CI)

| تستر | پوشش | نتیجه |
|---|---|---|
| `tester595-v34.37.0-tombstone-scope.js` | اجرای واقعیِ برشِ سنگ‌قبر و مولد کد در `vm`: مشتری تازه نجات می‌یابد، حذف واقعی حذف می‌ماند، سازگاری عقب‌رو، عدم رگرسیون پاک‌سازی پروژه، سپر حذف انبوه، بازتولید کد | ۳۰/۰ |
| `tester596-v34.37.0-inv-ref-confirm-undo.js` | اجرای واقعیِ زنجیرهٔ ارجاع: **هیچ نوشتنی بدون تایید** (نه `invRef`، نه اعلان، نه پیامک)، محتوای مودال، ثبت پس از تایید، پیامک فقط با تیک، گاردهای قبلی، قرارداد کامل `revoke_invoice_ref`، رفتار دکمهٔ لغو | ۵۷/۰ |
| `tester597-v34.37.0-supplier-attach-order.js` | ترتیب واقعیِ سورس (dedup پیش از آپلود)، تطابق سه‌فیلدی شماره، انتخاب بهترین رکورد، هم‌ترازی `accept`↔allowlist↔`ATTACH_EXT`، لاگ بدون داده حساس، عدم رگرسیون v34.36.2 | ۳۱/۰ |

## ⑥ ریسک و بازگشت‌پذیری

- همهٔ گاردهای تازه **fail-closed به سمت رفتار قبلی** طراحی شده‌اند: نبودِ تاریخ ⇒ رفتار قبلی؛ سپر حذف انبوه ⇒ تنزل به `setData` (نه از دست رفتن داده)؛ `revoke_invoice_ref` ⇒ با هر ابهامی ۴۰۹.
- هیچ مهاجرت داده‌ای لازم نیست. رکوردهای موجود بدون `createdAtISO` دقیقاً مثل قبل رفتار می‌کنند و رکوردهای تازه از همین انتشار مهر می‌گیرند.
- **پیش از استقرار یک بک‌آپ کامل از `crm/data/` بگیرید.**
