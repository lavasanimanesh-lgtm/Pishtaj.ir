# RCA — سیستم اعلان‌ها / یادآورها: «خواندم» و «انجام شد» برمی‌گردد + هشدار تحویل برای پروندهٔ تسویه‌شده
## (بررسی عمیق — v34.38.4)

**تاریخ:** 2026-09-08
**وضعیت:** فقط تحلیل/خواندن کد — **هیچ کدی تغییر نکرده است.**
**محدوده:** `crm/bridge.js` (کارت‌سازها/اینباکس)، `crm/rbac.js` (کارتابل/خواندن/حل)، `crm/leads.js` (یادآورها)، `crm/salesfiles.js` (پرونده/مرحله/تعهد تحویل)، `crm/sync.js` (merge/pull)، `crm/sales-domain-v2.js` (روتر فرمانی).

---

## ۱. خلاصهٔ مدیریتی

| # | نشانه | ریشهٔ تاریخی | وضعیت در v34.38.4 | شکاف زنده |
|---|-------|--------------|-------------------|-----------|
| ۱ | «خواندم» با رفرش برمی‌گردد | BUG-NTF-002 (v31.7.10): merge «newer-wins» وضعیت readBy را پاک می‌کرد | **رفع‌شده** (merge اتحاد readBy در sync.js:2780 + کانونیکال سرور) | کارت‌های بازتولیدشونده از روی رکورد منبع + حذف انبوهِ >۴۰ که به union-key برمی‌خورد |
| ۲ | یادآور «انجام شد» برمی‌گردد | v34.29.6 ONE-CLICK + v34.8.18 ANTI-RESURRECT + CARTABLE/DING-LOOP | **عمدتاً رفع‌شده** (نوشت محلی بی‌درنگ + پچ مینیمال) | اجرای کارت‌ساز روی کش کهنه در بوت/تیک + soft-done (نه حذف) + چنددستگاهی |
| ۳ | پروندهٔ تسویه‌شده هشدار «نزدیک شدن تحویل تعهدی» دارد | **باگ زندهٔ قطعی** — گارد چرخهٔ عمر ندارد | ❌ **زنده** | `checkDealDue`/`ptfSfDueState` فقط `st==='archived'` را می‌گیرند؛ تسویه (مرحله ۱۱) را نه؛ و هیچ `ntfResolveByRef` هنگام تسویه صدا نمی‌شود |

**نتیجه:** نشانهٔ ۱ و ۲ ریشه‌های تاریخی‌شان رفع شده، اما چند لبهٔ بازتولیدِ زنده باقی مانده که همان علامت را بازتولید می‌کنند؛ نشانهٔ ۳ یک باگ زندهٔ قطعی و مستقل است (کمبود گارد چرخهٔ عمر، نه مشکل merge).

---

## ۲. معماری — «حالت» کجاست و چه کسی آن را می‌سازد

- **کارت اعلان** = `ptf_crm_notifs` (رکورد با `cd`). خواندن = `readBy[]` (per-user)؛ حل‌شدن = `done=true` یا حذف کامل با `ntfResolveByRef`/`ntfResolveRfqReferral`.
- **یادآور** = `ptf_crm_reminders` (`st`: `open`/`done`). «انجام شد» = soft (`st='done'`)؛ «حذف» = hard (فرمان tombstone).
- **کارت‌سازها** در هر تیک poll (چند ثانیه) و یک بار در بوت اجرا می‌شوند: `checkDueReminders`, `checkOfferExpiry`, `checkRfqDue`, `checkDealDue` (`bridge.js:549-552` و `:2307`).
- **ضدتکرار این کارت‌ها روی رکوردِ منبع است، نه روی کارت**: `deal.dueNotified`، `rfq.dueNotified`، `reminder.notifiedUsers/msgSent`. یعنی «فقط در لحظهٔ گذار یک بار بساز».
- `ptf_crm_notifs` هم **فرمان‌محور** (entity_upsert/delete) است و هم **union-key مشترک** در مسیر legacy (`sync.js:254` isSharedUnionKey).

---

## ۳. نشانهٔ ۱ — «خواندم» با رفرش برمی‌گردد

### ریشهٔ تاریخی (رفع‌شده)
`ntfRead()` فقط `readBy` را تغییر می‌داد و timestamp ثابت می‌ماند؛ merge عمومی «newer-timestamp wins» نسخهٔ سرور (خوانده‌نشده) را برنده می‌کرد و readBy عملاً موقت بود → بعد از هر sync برمی‌گشت.
- **فیکس v31.7.10 (BUG-NTF-002):** merge اختصاصی `ptf_crm_notifs` در `sync.js:2780` — `readBy` **اجتماع دو طرف** (خوانده‌شده هرگز برنمی‌گردد)، `done`/`repeat` حداکثری.
- **فیکس v34.8.34 (NOTIFS-UNION):** همین قرارداد به سرور (`api/crm.php`) هم منتقل شد تا خروجی دو طرف بایت‌به‌بایت برابر باشد (پایان نوار زرد دائمی).

### شکاف‌های زنده (که هنوز همین علامت را می‌سازند)
1. **کارت‌های بازتولیدشونده از رکورد منبع، readBy را دور می‌زنند.** ضدتکرار روی `dueNotified`/`notifiedUsers` در رکورد منبع است. اگر این مهر از بین برود (ویرایش تاریخ → `dueNotified=''` در `salesfiles.js:1674`؛ موکول/تعویق → `notifiedUsers={}` در `remSnooze`/`remPostponeCommit`؛ یا sync از دستگاه کهنه)، کارت **جدید با cd تازه** ساخته می‌شود و readBy خالی است — یعنی «خواندم» قبلی بی‌اثر می‌شود.
2. **کارت‌های `kind:'reminder'` بدون `remCd` هرگز حل نمی‌شوند.** `ntfNeedsAction` (`rbac.js:366`) هر `kind:'reminder'` را اقدامی می‌داند و `ptfPruneStaleNotifs` (`rbac.js:226`) آن را حذف نمی‌کند. پس کارت «مهلت درخواست» / «تحویل تعهدی» بعد از بسته‌شدن موضوع هم در کارتابل می‌ماند؛ «خواندم» فقط per-user است و برای سایر نقش‌ها/کاربران همچنان زنده می‌ماند.
3. **حذف انبوه (>۴۰) به union-key برمی‌خورد.** `ntfResolveByRef`/`ptfPruneStaleNotifs` با `reason:'w2'` کل مجموعه را به روتر می‌دهند؛ اگر بیش از `MAX_OPS=40` کارت حذف شود، روتر `legacyFallback('too-many-ops')` می‌کند → `setData` → push legacy؛ و چون notifs یک **union-key** است، legacy-push فقط union می‌کند و **هیچ چیزی را روی سرور حذف نمی‌کند** → با رفرش دوباره برمی‌گردند.

---

## ۴. نشانهٔ ۲ — یادآور «انجام شد» برمی‌گردد

### ریشهٔ تاریخی (رفع‌شده)
- **v34.8.17/18 (CARTABLE-LOOP / DING-LOOP):** کارت تکراری هر چند ثانیه؛ `checkDueReminders` کل ردیف کهنه را upsert می‌کرد.
- **v34.8.18 (ANTI-RESURRECT):** `checkDueReminders` حالا **پچ مینیمال `{cd, notifiedUsers, msgSent}`** می‌فرستد (`bridge.js:453`) — دیگر هرگز `st:'done'` تازه را با ردیف کهنهٔ `open` زنده نمی‌کند.
- **v34.29.6 (ONE-CLICK):** `remDone`/`remSnooze`/`remDel` نوشتن محلی بی‌درنگ (silent-write) + فرمان اتمیک گرفتند (`leads.js:738+`)؛ `remDone` همچنین `ntfResolveByRef(cd)` را صدا می‌زند تا کارت مرتبط برای همه حذف شود.

### شکاف‌های زنده
1. **کارت‌ساز در بوت/تیک روی کشِ کهنهٔ فاز B اجرا می‌شود.** `getData` فاز B کش ۳۰ثانیه‌ای دارد. اگر در لحظهٔ بوت، کش محلی هنوز `st:'open'` باشد (سرور `done` است ولی pull هنوز نرسیده)، `checkDueReminders` (`bridge.js:2307`) کارت تازه می‌سازد و پچ `notifiedUsers` می‌فرستد؛ pull بعدی `st` را درست می‌کند ولی **کارتِ ساخته‌شده می‌ماند** → کاربر «انجام شد برمی‌گردد» می‌بیند. سپر `haveCard`/`notifiedUsers` فقط تکرارِ همان ردیفِ باز را می‌گیرد، نه حالت «سرور done ولی کش open» را.
2. **soft-done، نه حذف.** «انجام شد» رکورد یادآور را حذف نمی‌کند؛ فقط `st='done'`. اگر هر مسیری (دستگاه کهنه با push legacy کل مجموعه) نسخهٔ `open` را بازفرستی کند، باز زنده می‌شود — reminders در فهرست union-key نیستند، پس merge عمومی «newer wins» است و ردیف کهنه می‌تواند برنده شود.
3. **لبهٔ MAX_OPS در حل کارت:** اگر برای یک یادآور تعداد کارت‌های >۴۰ داشته باشیم، `ntfResolveByRef` به legacy fallback می‌خورد و مثل نشانهٔ ۱ (بند ۳) روی union-key حذف نمی‌شود.

---

## ۵. نشانهٔ ۳ — پروندهٔ تسویه‌شده، هشدار «نزدیک شدن تحویل تعهدی» دارد (باگ زندهٔ قطعی)

### زنجیرهٔ ریشه‌ای
1. تعهد تحویل روی پرونده `dueISO` است (`salesfiles.js:1636`، اصلاح در `:1672`).
2. `ptfSfDueState(r)` (`salesfiles.js:31-37`) فقط می‌گوید: `if (!r.dueISO || r.st === 'archived') return null;` — **هیچ گاردی برای تسویه/مختومه ندارد.**
3. «تسویه‌شده» در مدل داده به‌صورت **مرحلهٔ ۱۱** از `sfStageOf` (`salesfiles.js:353`) مشتق می‌شود (فاکتور هست و مانده ≤ ۰.۵)، اما رکورد **هنوز در `ptf_crm_deals`** است و `st` آن `'archived'` نیست (بایگانی مرحلهٔ ۱۲ است و جدا انجام می‌شود).
4. `checkDealDue` (`bridge.js:352-385`) روی همان شرط `r.st === 'archived'` تصمیم می‌گیرد؛ پس برای پروندهٔ مرحلهٔ ۱۱ که `dueISO` آن گذشته/نزدیک است، `dueNotified` را ست و کارت می‌سازد (یا کارت قبلی را نگه می‌دارد).
5. کارت با `kind:'reminder'` و `dkey:'deal-due-<cd>'` ساخته می‌شود و **هیچ‌جا `ntfResolveByRef(deal.cd)` هنگام تسویه/بایگانی صدا نمی‌شود** (در `sfCloseSettledCommit`/`sfArchive` هیچ حل اعلانی نیست — grep نتیجه صفر). نتیجه: کارت برای همیشه می‌ماند.

> نکتهٔ منطقی گزارش کارفرما کاملاً درست است: «فاکتور پس از تحویل و تسویه پس از فاکتور» — یعنی اگر پرونده تسویه‌شده است، تحویل قطعاً اتفاق افتاده؛ هشدار «نزدیک شدن تحویل» در این حالت بی‌معناست و باید خاموش باشد.

---

## ۶. آیا به همان ریشهٔ CONTACT-WIPE برمی‌گردد؟

**خانواده‌ای مشترک، ولی نه همان باگ.** CONTACT-WIPE از «خواندنِ کهنهٔ getData → diff-router حذف واقعی صادر می‌کند + silent-write آرایهٔ ناقص» بود. اینجا الگوی خواهر است:

> **«حالتِ ضدتکرار روی رکوردِ منبع ذخیره می‌شود، کارت‌ساز خودکار هر تیک از getData (کهنه/کش‌دار) بازتولید می‌کند، و هیچ گاردِ تکمیل/تسویه + هیچ resolve هنگام بسته‌شدن وجود ندارد.»**

به‌عبارتی: هر دو خانواده از «بازسازی از روی خواندنِ کهنه + نبودِ گارد پایان چرخه» می‌آیند؛ اما سازوکار دقیق متفاوت است (این‌جا فرمان حذف مخرب نیست، بازتولید کارت/وضعیت است). نشانهٔ ۳ صرفاً یک گارد چرخهٔ عمرِ جاافتاده است و به merge/روتر اصلاً ربطی ندارد.

---

## ۷. پیشنهاد اصلاح (غیرمخرب — فقط در صورت صدور دستور اقدام)

1. **نشانهٔ ۳ (اولویت P1 — باگ قطعی):**
   - در `checkDealDue` (`bridge.js:352`) و/یا `ptfSfDueState` (`salesfiles.js:31`): پرش از پرونده‌های تسویه/مختومه — شرط `sfStageOf(r) >= 11` (یا وجود فاکتور تسویه‌شده) علاوه بر `st==='archived'`؛ و پاک‌کردن `r.dueNotified` برای این‌ها.
   - در `sfCloseSettledCommit`/`sfArchive` (`salesfiles.js:1970`/`~2340`): صدا زدن `ntfResolveByRef(r.cd)` تا کارت `deal-due-<cd>` برای همه حذف شود.
2. **نشانهٔ ۱ و ۲ (P2):**
   - کارت‌های `kind:'reminder'` بدون `remCd` و بدون گارد حل (rfq-due/deal-due) یا باید هنگام بسته‌شدن منبع resolve شوند (همان بالا) یا `ptfPruneStaleNotifs` باید آن‌ها را پس از گذشت سررسید به‌عنوان «اطلاعی» محو کند.
   - بوت/تیک `checkDueReminders`: پیش از ساخت کارت، ردیفِ بازِ سررسیده را با آخرین `st` تأییدشده (پس از pull، نه کش خام) بسنجد؛ یا ساخت کارت را تا تکمیل اولین pull به تأخیر بیندازد.
   - حذف انبوه notifs (resolve/prune) باید از مسیر فرمان با چند تکهٔ ≤۴۰ برود (نه fallback به legacy-union که حذف نمی‌کند).
3. **قرارداد پروژه:** هر اصلاح → بامپ نسخه + `RELEASE-NOTES` + تستر قفل جدید در `run-ci-gate.js` (سناریوی «تسویهٔ پرونده → هشدار تحویل خاموش + کارت resolve» و «خواندم/انجام‌شد پس از رفرش پایدار»).

_این سند صرفاً نتیجهٔ بررسی است؛ طبق رویهٔ جاری تا صدور دستور، هیچ فایلی تغییر نکرده است._
