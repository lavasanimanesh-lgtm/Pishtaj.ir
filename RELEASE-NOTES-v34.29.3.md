# RELEASE NOTES — v34.29.3 (ریشه‌کنی واقعی «تب‌های خالی» مدیریت سایت — برخورد نام کلاس CSS)

**تاریخ:** ۲۰۲۶-۰۹-۰۲ · **کمیت:** tester578 (۱۲ سنجهٔ ایستا + ۵ سنجهٔ رفتاری با jsdom) · **گیت:** 194 PASS / 0 FAIL

## گزارش کاربر

«تب‌های صفحهٔ جدید و کیفیت در مدیریت سایت کاملاً خالی و غیرقابل استفاده‌اند.» (تکرار پس از v34.26.1، v34.27.0، v34.28.0 و v34.29.0)

## ① RCA — این‌بار با سنجش «دیده‌شدن»، نه «طول HTML»

بوت کامل اپ در jsdom (index.html + ۱۰۸ اسکریپت، نشست ادمین، کلیک واقعی روی دکمهٔ سایدبار و دکمه‌های تب) و اندازه‌گیری `getComputedStyle(el).display` روی تک‌تک عناصر:

| تب | طول HTML | متن قابل‌دیدن | کنترل‌های پنهان | عامل |
|---|---|---|---|---|
| صفحهٔ جدید | ۱۰٬۳۹۷ | **۲۵ نویسه** | **۲۹/۲۹** | `DIV.pn` → `display:none` |
| کیفیت | ۹٬۵۱۰ | **۱۹ نویسه** | **۵/۵** | `DIV.pn` ×۶ → `display:none` |
| سئو / اخبار / محصولات | — | سالم | ۰ | — |

**ریشه:** `crm/index.html` یک قاعدهٔ سراسریِ قدیمی برای سوئیچ پنل دارد:

```css
.pn{background:var(--crd);border-radius:20px;border:1px solid var(--brd);padding:20px;display:none;animation:fi .2s ease}
.pn.act{display:block}
```

و `crm/cms.js` از v34.13.0 (صفحهٔ جدید) و v34.14.0 (کیفیت) همین نام کلاس را صرفاً به‌عنوان «قاب کارت» دور کل فرم‌ها گذاشته بود — بدون `act`. نتیجه: هر دو تب **از روز تولد** فقط پاراگراف معرفی‌شان را نشان می‌دادند. فرم جایگزین v34.26.1 هم داخل همان `.pn` بود؛ پس fallback هم نامرئی بود.

**برخورد دوم (هم‌خانواده):** `<table class="tb">` در ۸ جای cms.js و ۲ جای gsc.js با قاعدهٔ `.tb` نوار بالای چسبان برخورد داشت (`display:flex; position:sticky; z-index:40` و در موبایل `height:56px; overflow:hidden`) — جدول‌های صف زمان‌بندی/بک‌آپ/PageSpeed/alt/محصولات/GSC به‌جای جدول، یک نوار فلکسِ چسبان ۵۶ پیکسلی می‌شدند.

## ② چرا چهار رفع قبلی بی‌اثر بود

| نسخه | فرضیه | واقعیت |
|---|---|---|
| v34.26.1 TAB-GUARD | استثنای JS در رندر | استثنایی رخ نمی‌دهد؛ رندر سالم است |
| v34.27.0 بنر کش | cms.js کهنه در مرورگر | باگ در همهٔ نسخه‌ها هست |
| v34.28.0 TAB-DIRECT | `goPanelByName` دکمه را نمی‌یابد | رندر می‌شود؛ اثبات فقط `innerHTML.length` بود |
| v34.29.0 VER-SHIELD | تبِ بازِ قدیمی | همان |

هیچ تستری «دیده‌شدن» را نمی‌سنجید. tester578 این حفره را می‌بندد.

## ③ رفع

- `crm/cms.js`: ۸ قاب `class="pn"` → `class="cms-card"` با `background:var(--crd)` اینلاین (پس‌زمینه قبلاً از `.pn` می‌آمد؛ در تم تیره هم از متغیر می‌خواند). ۸ جدول `class="tb"` → `class="cms-tbl"` (استایل جدول از قواعد عمومی `table/th/td` می‌آید).
- `crm/gsc.js`: ۲ جدول `class="tb"` → `class="cms-tbl"`.
- قاعدهٔ سراسری `.pn` **دست نخورد** (پنل‌های دیگر ممکن است به آن تکیه کنند)؛ فقط دیگر به‌اشتباه به آن تکیه نمی‌کنیم.

**اثبات پس از رفع — همان هارنس:** صفحهٔ جدید: متن قابل‌دیدن ۲۵ → **۲۸۶**، کنترل‌های پنهان ۲۹/۲۹ → **۰/۲۹**. کیفیت: ۱۹ → **۶۸۵**، ۵/۵ → **۰/۸**؛ جدول صف `display:table` (نه flex/sticky). سایر تب‌ها بدون تغییر.

## ④ tester578 — نگهبان دائمی

- ایستا: هیچ `class="pn"` و هیچ `<table class="tb">` در cms/gsc/careers؛ و **به‌طور عمومی** هیچ کلاسِ خروجیِ این سه ماژول با قاعدهٔ تک‌کلاسیِ `display:none` در CSS سراسری برخورد نکند (مگر با `display` اینلاین صریح، مثل مودال‌های `md-b`).
- رفتاری (اگر jsdom نصب باشد): بوت کامل + کلیک واقعی + `getComputedStyle` روی همهٔ کنترل‌های دو تب. بدون jsdom با SKIP شفاف رد می‌شود (سنجه‌های ایستا کافی‌اند).

## ⑤ بستهٔ انتشار

`crm/cms.js`، `crm/gsc.js`، `crm/index.html` (۱۰۹ کش‌باست + `PTF_CRM_RELEASE`)، `crm/sw.js` (`CACHE='ptf-crm-v34.29.3'`)، `crm/shell.js`، `crm/manifest.json`، `crm/clear-cache.html`، `api/sales-domain.php`، `VERSION.json`، tester578 + جاروی انکرهای نسخهٔ تسترها (tester574↔577 هم‌نام نسخه شدند).

## ⑥ بعد از دیپلوی

1. پروداکشن هنوز **v34.25.0** است (manifest.json زنده بررسی شد) — v34.26.0 تا v34.29.2 هرگز مستقر نشده‌اند. این نسخه همهٔ آن‌ها را هم با خود می‌برد.
2. CRM را باز کنید؛ اگر بنر «نسخهٔ جدید» آمد، بارگذاری مجدد بزنید (یا `clear-cache.html`).
3. مدیریت سایت → «📄 صفحهٔ جدید»: فرم کامل (بخش مقصد، نامک، موضوع، دکمه‌های AI، عنوان/H1/توضیح، نوار ابزار، متن، تصویر، بازبینی، زمان‌بندی، انتشار) باید فوراً دیده شود. «🛠 کیفیت»: شش کارت (زمان‌بندی، تاریخچه/بازگشت، هزینهٔ AI، hreflang، alt تصاویر، PageSpeed).
