# گزارش ممیزی سئو و استراتژی رتبه یک گوگل
**پیشرو تجهیز فرتاک (pishtaj.ir)** | تاریخ: ۲۰۲۶-۰۸-۱۵ | تهیه: متخصص ارشد سئو

---

## بخش ۱ — خلاصه مدیریتی

وب‌سایت از نظر **زیرساخت فنی در وضعیت خوبی** بود (۶۱۷ صفحه، canonical کامل، بدون عنوان تکراری، schema فعال). اما یک **ایراد بنیادی** داشت که مستقیماً مانع رتبه گرفتن کلمات هدف فاز اول می‌شد:

> **کلمات کلیدی «تامین‌کننده…» در هیچ صفحه‌ای از سایت وجود نداشتند — صفر صفحه.**

سایت برای «تامین تجهیزات X» محتوا داشت، ولی برای «تامین‌کننده X» و «تامین کننده X» هیچ. این دو در فارسی از نظر گوگل **دو intent متفاوت** هستند: اولی خدمت‌محور، دومی **تامین‌کننده‌جو (کاربر دنبال شرکت است = نزدیک‌ترین کاربر به خرید)**. عملاً پرارزش‌ترین ترافیک تجاری پوشش داده نشده بود.

مورد دوم: **۴۵۰ مقاله مرکز دانش عملاً هیچ اعتباری به صفحات فروش منتقل نمی‌کردند** — فقط ۷ مقاله به صفحه پایپینگ و ۳ مقاله به صفحه ابزار دقیق لینک داشتند. یعنی بزرگ‌ترین دارایی محتوایی شرکت بلااستفاده مانده بود.

هر دو مورد در همین فاز **اصلاح و پیاده‌سازی شد**.

---

## بخش ۲ — ممیزی فنی: یافته‌ها

### ✅ نقاط قوت موجود
| مورد | وضعیت |
|---|---|
| تعداد صفحات ایندکس‌شدنی | ۶۱۷ |
| canonical / title / description | ۱۰۰٪ کامل، بدون تکرار |
| Schema.org | Organization، LocalBusiness، FAQPage، Service، Breadcrumb |
| HTTPS + HSTS + CSP | فعال |
| فشرده‌سازی و کش | mod_deflate + mod_expires فعال |
| lazy-load و ابعاد تصاویر | ۶۴ از ۷۱ تصویر صفحه اصلی |
| ساختار URL | تمیز و پوشه‌ای |

### ❌ ایرادهای شناسایی‌شده و رفع‌شده
| # | ایراد | اثر سئویی | وضعیت |
|---|---|---|---|
| ۱ | کلمات «تامین‌کننده…» در صفر صفحه | عدم امکان رتبه‌گیری کلمات فاز اول | **رفع شد** |
| ۲ | ۴۵۰ مقاله بدون لینک به صفحات فروش | هدررفت کامل اعتبار داخلی | **رفع شد** |
| ۳ | نبود صفحه ۴۰۴ سفارشی | هدررفت Crawl Budget + پرش کاربر | **رفع شد** |
| ۴ | ۴۲ لینک مرده `href="#"` در صفحات خدمات | سیگنال کیفیت پایین به گوگل | **رفع شد** |
| ۵ | نبود ریدایرکت `index.html` → `/` | ریسک محتوای تکراری | **رفع شد** |
| ۶ | robots.txt ناقص | عدم کنترل خزش مسیرهای داخلی | **رفع شد** |
| ۷ | H1 صفحه اصلی فاقد کلمه کلیدی | تضعیف صفحه اصلی | **رفع شد** |
| ۸ | lastmod قدیمی در sitemap | کندی بازخزش | **رفع شد** |

### ⚠️ ایراد باقی‌مانده (نیازمند اقدام شما — خارج از دسترس کد)
> **هیچ کد Google Analytics و هیچ تگ تایید Google Search Console روی سایت نصب نیست.**
>
> بدون Search Console **امکان سنجش، ثبت sitemap، و درخواست ایندکس وجود ندارد** و عملاً کور پرواز می‌کنید. این **فوری‌ترین اقدام** شماست (جزئیات در بخش ۵).

---

## بخش ۳ — تحلیل رقبا (کلمات فاز اول)

بررسی SERP فعلی نشان می‌دهد رقبا **ضعیف** هستند:

| رقیب | نقطه ضعف قابل بهره‌برداری |
|---|---|
| farscontrol.com | تمرکز منطقه‌ای (استان فارس)، محتوای فنی کم‌عمق |
| radocorp.com | صفحه دسته‌بندی ووکامرس، بدون محتوای مهندسی |
| petrosepid.com | سایت تک‌صفحه‌ای، بدون عمق محتوا |
| jahansanat.org | محتوای کوتاه، بدون schema غنی |
| sayalsanatkavan.ir | صرفاً معرفی شرکت |

**نتیجه‌گیری راهبردی:** هیچ‌کدام «اتوریته موضوعی» ندارند. شما با ۴۵۰ مقاله فنی + ۷۸ صفحه محصول + ۲۱ صفحه برند، **از نظر عمق محتوا از همه جلوترید**. تنها مشکل این بود که این عمق به صفحات فروش متصل نبود — که اکنون شد.

---

## بخش ۴ — اقدامات اجراشده در این فاز

### ۴.۱ صفحه فرود اختصاصی «تامین‌کننده» (جدید)
ایجاد `/tamin-tajhizat/` — صفحه هاب با هدف‌گیری مستقیم کلمات تامین‌کننده‌محور:
- ۳ جدول مقایسه‌ای (گروه کالایی / تامین‌کننده تخصصی در برابر عمومی / الزامات هر صنعت)
- ۶ سوال متداول با **FAQPage schema** (شانس Rich Snippet)
- Schema کامل: Organization + WebPage + Service + OfferCatalog + BreadcrumbList + FAQPage
- لینک‌دهی داخلی به تمام صفحات خدمات، صنایع و محصولات
- **لینک از فوتر تمام ۵۳۸ صفحه سایت** → انتقال حداکثری اعتبار

### ۴.۲ بهینه‌سازی صفحات پول‌ساز
| صفحه | تغییرات |
|---|---|
| `/services/piping-equipment/` | title/description/H1 بازنویسی با «تامین‌کننده پایپینگ» + بخش جدید ۲ پاراگرافی + جدول مقایسه + ۲ FAQ جدید در HTML و schema |
| `/services/instrumentation-equipment/` | همان الگو با «تامین‌کننده تجهیزات ابزار دقیق» + جدول معیارهای انتخاب |
| `/industries/petrochemical/` | همان الگو با «تامین‌کننده تجهیزات صنایع پتروشیمی» + جدول الزامات NACE/ATEX |
| `/` (صفحه اصلی) | H1، title، description و meta keywords بازنویسی شد |

تمام عنوان‌ها به **۴۵–۵۹ کاراکتر** و توضیحات به **۱۴۶–۱۴۹ کاراکتر** تنظیم شدند (بدون برش در نتایج گوگل).

### ۴.۳ معماری لینک داخلی (مهم‌ترین اقدام)
هر ۴۴۹ مقاله مرکز دانش بر اساس **موضوع واقعی خود** (تحلیل title/H1/H2) به خوشه مربوطه دسته‌بندی و یک ماژول لینک‌دهی هدفمند دریافت کرد:

| خوشه | تعداد مقاله | مقصد لینک |
|---|---|---|
| پایپینگ | ۹۸ | `/services/piping-equipment/` |
| شیرآلات | ۸۶ | `/services/valves-guide/` |
| تامین و کیفیت | ۱۱۲ | `/services/` |
| ابزار دقیق | ۶۹ | `/services/instrumentation-equipment/` |
| برق صنعتی | ۴۵ | `/services/electrical-equipment/` |
| پمپ و کمپرسور | ۴۰ | `/services/pumps/` |

نتیجه عددی:

| مسیر | لینک ورودی قبل | بعد |
|---|---|---|
| صفحه پایپینگ | ۷ | **۱۰۷** |
| صفحه ابزار دقیق | ۳ | **۱۴۵** |
| صفحه پتروشیمی | ~۰ | **۴۴۹** |
| صفحه هاب تامین‌کننده | — | **۵۳۹** |

هر ماژول شامل انکرتکست کلمه‌کلیدی + دکمه RFQ + شماره تماس است (هم سئو، هم نرخ تبدیل).

### ۴.۴ اصلاحات فنی
- `404.html` سفارشی + `ErrorDocument` در `.htaccess` (با `noindex, follow`)
- ریدایرکت ۳۰۱ برای `index.html` → مسیر پوشه‌ای
- بازنویسی کامل `robots.txt` (+ اجازه صریح به GPTBot/PerplexityBot/ClaudeBot برای دیده‌شدن برند در پاسخ‌های هوش مصنوعی)
- اتصال ۴۲ لینک مرده به صفحات محصول واقعی
- sitemap: افزودن صفحه جدید، به‌روزرسانی ۴۶۰ رکورد `lastmod`، افزایش `priority` صفحات هدف

### ۴.۵ صحت‌سنجی انجام‌شده
```
✅ ۶۲۰ بلوک JSON-LD  →  ۰ خطا
✅ sitemap.xml       →  XML معتبر، ۶۱۸ آدرس، همه دارای فایل
✅ لینک‌های شکسته    →  ۰ (بررسی سراسری)
✅ عنوان تکراری      →  ۰
✅ تست HTTP          →  همه صفحات کلیدی 200
```

### پوشش کلمات کلیدی: قبل و بعد
| کلمه کلیدی | قبل | بعد |
|---|---|---|
| تامین‌کننده پایپینگ | **۰** | ✅ |
| تامین‌کننده تجهیزات پایپینگ | **۰** | ✅ |
| تامین‌کننده تجهیزات ابزار دقیق | **۰** | ✅ |
| تامین‌کننده تجهیزات پتروشیمی | **۰** | ✅ |
| تامین‌کننده تجهیزات صنایع پتروشیمی | **۰** | ۴۵۱ |
| تامین‌کننده تجهیزات صنعتی | **۰** | ۵۴۰ |

---

## بخش ۵ — اقدامات فوری شما (بدون این‌ها نتیجه نمی‌گیرید)

این موارد **از داخل کد قابل انجام نیست** و باید توسط شما انجام شود. اولویت‌بندی‌شده:

### 🔴 اولویت ۱ — همین هفته
1. **ثبت Google Search Console** برای `pishtaj.ir` → ثبت `sitemap.xml` → درخواست ایندکس دستی برای ۴ آدرس:
   - `/tamin-tajhizat/`
   - `/services/piping-equipment/`
   - `/services/instrumentation-equipment/`
   - `/industries/petrochemical/`
   (تگ تایید را بدهید تا در `<head>` قرار دهم)
2. **نصب Google Analytics 4** (کد را بدهید تا نصب کنم — توجه: CSP فعلی سایت اسکریپت خارجی را می‌بندد و باید هم‌زمان اصلاح شود).
3. **ثبت و تکمیل Google Business Profile** با آدرس دقیق تهران، تلفن ۰۲۱-۴۶۰۸۷۶۷۹ و دسته «تامین‌کننده تجهیزات صنعتی» — برای جست‌وجوهای محلی و پنل دانش برند.

### 🟠 اولویت ۲ — ماه اول
4. **ثبت در دایرکتوری‌های صنعتی ایران** (سیگنال NAP و بک‌لینک معتبر): ایران‌صنعت، نیازمندی‌های صنعتی، ecvv، کalanet و اتاق بازرگانی تهران. **آدرس و تلفن باید دقیقاً یکسان با سایت باشد.**
5. **راه‌اندازی صفحه لینکدین شرکت** و انتشار هفتگی محتوای فنی با لینک به مرکز دانش.
6. **افزودن نمونه‌کار واقعی** به `/projects/` — گوگل برای E-E-A-T به «تجربه اثبات‌شده» وزن بالایی می‌دهد. حتی ۵ پروژه با نام کارفرما، اقلام تامین‌شده و تاریخ کافی است.

### 🟡 اولویت ۳ — ماه دوم و سوم
7. **دریافت بک‌لینک از سایت‌های صنعتی**: رپورتاژ در پایگاه‌های خبری صنعت نفت (شانا، نفت‌آنلاین، پترونت).
8. **افزودن نظرات و رضایت‌نامه مشتریان** با `Review`/`AggregateRating` schema.
9. **بهینه‌سازی Core Web Vitals**: صفحه اصلی ۹۴KB HTML با ۷۱ تصویر — پیشنهاد تبدیل کامل به WebP و کاهش CSS درون‌خطی.

---

## بخش ۶ — نقشه راه فازهای بعدی (کلمات کلیدی)

### فاز ۲ — کلمات محصول‌محور (ماه ۲ تا ۴)
۷۸ صفحه محصول موجود است ولی برای کلمات خرید بهینه نشده. هدف‌گیری:
- «خرید لوله مانیسمان» / «قیمت لوله A106» / «فروش فلنج ASME B16.5»
- «قیمت بال ولو API 6D» / «خرید گیت ولو»
- «قیمت ترانسمیتر فشار روزمونت» / «خرید فلومتر کوریولیس»

**اقدام:** افزودن `Product` schema با `Offer` و `priceRange`، بخش «قیمت و عوامل موثر» و FAQ خرید به هر صفحه محصول.

### فاز ۳ — کلمات برندمحور (ماه ۴ تا ۶)
۲۱ صفحه برند موجود. هدف‌گیری «نمایندگی روزمونت در ایران»، «فروش شیرآلات KITZ»، «تامین‌کننده Endress+Hauser».
این کلمات **رقابت کم و نرخ تبدیل بسیار بالا** دارند.

### فاز ۴ — کلمات صنعت و شهرمحور (ماه ۶ به بعد)
- «تامین‌کننده تجهیزات پالایشگاهی»، «تجهیزات نیروگاهی»، «تجهیزات فولاد»
- کلمات جغرافیایی: «تامین‌کننده تجهیزات صنعتی تهران / اصفهان / عسلویه / ماهشهر»

### فاز ۵ — کلمات فنی Long-tail
مرکز دانش را به RFQ متصل کرده‌ایم؛ گام بعد تولید محتوا برای پرسش‌های خرید-محور مانند «تفاوت A106 و A333 برای دمای پایین» با CTA مستقیم.

---

## بخش ۷ — انتظار واقع‌بینانه از زمان‌بندی

صادقانه بگویم: **«اول گوگل شدن در اسرع وقت» تضمین‌پذیر نیست** و هیچ متخصص سئویی نمی‌تواند تاریخ قطعی بدهد. سئو تابع اعتبار دامنه، سن دامنه و بک‌لینک است. برآورد واقع‌بینانه:

| بازه | انتظار |
|---|---|
| هفته ۱ تا ۳ | ایندکس صفحات جدید (منوط به ثبت Search Console) |
| ماه ۱ تا ۲ | ورود کلمات Long-tail به صفحه ۲ و ۳ |
| ماه ۳ تا ۴ | کلمات «تامین‌کننده…» در صفحه ۱ (با فرض اجرای بک‌لینک) |
| ماه ۵ تا ۸ | رقابت برای جایگاه ۱ تا ۳ کلمات اصلی |

**عامل تعیین‌کننده‌ای که در کد نیست:** بک‌لینک معتبر. محتوا و فنی سایت شما اکنون از رقبا قوی‌تر است؛ برنده نهایی را اعتبار خارجی دامنه مشخص می‌کند. اولویت ۲ بخش ۵ را جدی بگیرید.

---

## پیوست — فایل‌های تغییریافته
- **جدید:** `tamin-tajhizat/index.html`، `404.html`، این سند
- **بازنویسی:** `robots.txt`
- **اصلاح:** `index.html`، `sitemap.xml`، `.htaccess`، ۳ صفحه پول‌ساز، ۷ صفحه خدمات، ۴۴۹ صفحه مرکز دانش، فوتر ۵۳۸ صفحه
