# RELEASE NOTES — v34.7.90 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## تست رفتاری ارزش افزوده فصلی + اصلاح زنجیره اعتبار/بدهی (VAT-LEDGER-002)

### هدف
تستر استاتیک قبلی فقط وجود توابع را چک می‌کرد. این نسخه یک **تستر رفتاری** با دادهٔ مصنوعی
اضافه کرد و چند باگ محاسباتی را در طول تست کشف و اصلاح کرد.

### تست رفتاری (`_tools/uat/tester493-v34.7.90-vat-quarterly-behavior.js`)
در sandbox با `vm` اجرا و ۱۷ سناریو را می‌سنجد:

1. **فقط رسمی:** فاکتور فروش غیررسمی و ابطالی در بدهی VAT وارد نمی‌شوند.
2. **اعتبار خرید:** فاکتور رسمی واقعی + فاکتور پوششی (`coverVatAmount`).
3. **کارمزد فاکتورساز:** در VAT محاسبه نمی‌شود، تنها گزارش می‌شود.
4. **اعتبار مازاد:** منفی → به فصل بعد منتقل (carryToNext).
5. **زنجیره فصل:** فصل ۲ اعتبار منتقل‌شده از فصل ۱ را می‌گیرد.
6. **بدهی مازاد:** مثبت → پرداختنی؛ اگر از اعتبار+منتقل کافی باشد صفر.
7. **تسویه:** با مدرک → فصل تسویه و فصل بعد از صفر.
8. **فصل بدون داده:** صفر.

### باگ‌های اصلاح‌شده در این نسخه
- `activeSales` حالا فقط فاکتور فروش با `isOfficial === true` را می‌پذیرد؛
  قبلاً فقط `!isUnofficial` چک می‌کرد و فاکتور `isOfficial:false` اشتباهاً وارد می‌شد.
- `stateFor` حالا زنجیره‌ای است: اعتبار/بدهی فصل قبل از `stateFor(s-1)` گرفته می‌شود
  تا انتقال چندفصلی درست بماند.
- تست «مانده» از «خالص با منتقل‌شده» (`availableDebt − availableCredit`) استفاده می‌کند.

### تست
- `_tools/uat/tester493-v34.7.90-vat-quarterly-behavior.js` در گیت CI.
- گیت CI: **109 PASS / 0 FAIL**.

### نکته
این تغییر فقط محاسبه/export و رفتارهای internal بود؛ UI/تب ارزش افزوده همانند v34.7.89 است.
