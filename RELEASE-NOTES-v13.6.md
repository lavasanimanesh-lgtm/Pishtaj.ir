# 📦 ریلیزنوت v13.6 — تکمیل نقش بایگانی (گزارش کارفرما — کاملاً وارد بود)

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v13.5

---

## 🗄 مشکل گزارش‌شده و ریشه‌یابی

حق با شما بود — در v13.2 نام و مقصد مختومه‌سازی درست شد اما **دو مسیر قدیمی ساخت پرونده در بایگانی جا مانده بود:**

1. دکمه دستی «+ تشکیل پرونده از CO» بالای بایگانی
2. مسیر خودکار: با «برنده» شدن CO، پرونده‌ای مستقیم در بایگانی ساخته می‌شد (US-141 قدیم)

این با نقش مصوب (بایگانی = فقط مقصد پرونده‌های مختومه) تناقض داشت.

## ✅ اصلاحات

| مسیر | قبل | بعد |
|---|---|---|
| دکمه دستی | «+ تشکیل پرونده از CO» در بایگانی | **حذف شد** + بنر توضیح نقش: «بایگانی مقصد پرونده‌های مختومه است — پرونده در اینجا ساخته نمی‌شود» |
| CO برنده | پرونده جدید در بایگانی + انتقال مدارک به آنجا | **مدارک به «پرونده فروش» همان درخواست منضم می‌شود** (ضمائم RFQ + ثبت CO برنده با wonOffer/wonAt) — بایگانی دست نمی‌خورد |
| متن هشدار برنده | «پرونده در ماژول پرونده‌های پروژه ساخته می‌شود» | «مدارک به پرونده فروش همین درخواست منضم می‌شود» |
| اعلان مدیران | لینک به بایگانی | لینک به پرونده‌های فروش |

**جریان نهایی و یکپارچه:**
ثبت پیشنهاد → پرونده فروش خودکار → CO برنده → مدارک همان‌جا تکمیل → فاکتور/وصول → **مختومه** → انتقال به بایگانی (تنها راه ورود به بایگانی) ✅

- پرونده‌های موجود قدیمی بایگانی، پکینگ‌لیست‌ها و درصد تحویل دست‌نخورده‌اند
- ارجاع‌های قدیمی به فرم حذف‌شده، با پیام راهنما به مسیر درست هدایت می‌شوند

## 🧪 کیفیت
- تست رفتاری: CO برنده → پرونده فروش غنی شد، ضمیمه منضم شد، بایگانی صفر پرونده جدید ✅
- تستر جدید `tester56` ۱۳/۱۳ ✅ | تستر قدیمی ۷ (که رفتار قدیم را تثبیت کرده بود) با جریان جدید بازنویسی شد ✅
- رگرسیون کامل **۴۳ تستر** ✅ | audit صفر خطا ✅

## 📁 فایل‌های تغییریافته
`crm/projects.js` (حذف دکمه/فرم + بنر + استاب امن) · `crm/offers.js` (autoCreateProjectFromCO → غنی‌سازی پرونده فروش) · `crm/index.html`/`sw.js` (v13.6) · `_tools/uat/tester56` (جدید) + بازنویسی چک‌های tester7
