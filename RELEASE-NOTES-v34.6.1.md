# Release Notes v34.6.1 — گردش خرید نقدی تأمین‌کننده

تاریخ: 2026-08-15

## تغییر اصلی

ثبت خرید واقعی نقدی اکنون به‌صورت یکپارچه و ضدتکرار در زیر‌دفتر تأمین‌کننده ثبت می‌شود:

1. تأمین‌کننده با `supplierCd` یکتا شناسایی می‌شود؛ نام مفقود/مبهم، ثبت نقدی را متوقف می‌کند.
2. برای هر `purchaseCd` دقیقاً یک سند خرید ساخته می‌شود.
3. یک پرداخت نقدی هم‌مبلغ با تخصیص کامل به همان سند ساخته می‌شود.
4. مانده حساب صفر است، ولی فاکتور و پرداخت در گردش حساب باقی می‌مانند.
5. فهرست حساب‌ها تأمین‌کنندگان با مانده صفر را نیز برای مشاهده تاریخچه نگه می‌دارد.
6. ثبت تکراری همان `purchaseCd` رکورد مالی دوم نمی‌سازد.
7. اصلاح/جایگزینی خرید، Projection قبلی فاکتور و پرداخت را ابطال می‌کند.
8. رسید پرداخت خرید واقعی به خود پرداخت تأمین‌کننده نیز متصل می‌شود.
9. مسیرهای خرید تکی، گروهی و تقسیم lot همگی از قرارداد واحد استفاده می‌کنند.
10. نقش buyer فقط به زیر‌دفتر تأمین و تعهدهای خرید لازم دسترسی Sync گرفته است.

## کنترل کیفیت

- `tester404-v34.6.1-supplier-cash-ledger.js`: سناریوی رفتاری فاکتور + پرداخت + مانده صفر + idempotency + رسید + ابطال اصلاحی
- CI gate شامل تست جدید
- Syntax همه فایل‌های CRM و Parse همه PHPها سبز است
- CI gate: 23 PASS / 0 FAIL
- رگرسیون کامل تاریخی: 329 فایل PASS / 58 فایل FAIL (بدهی‌های تاریخی خارج از گیت)
