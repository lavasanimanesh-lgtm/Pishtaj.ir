# 👥 ریلیز v14.7 — اسپرینت هـ «مشتریان یکپارچه»

**تاریخ:** ۱۴۰۵/۰۴/۱۷ | **اسپرینت:** هـ از برنامه مصوب + US-382 بحرانی
**استوری‌ها:** US-382 (سپر داده) + US-363 (ادغام مشتریان) + US-380 (درخواست‌های سایت) + US-381 (اعلان تکراری‌ها) + US-370 (رابط در خروجی)

---

## ✅ محتوای ریلیز (۵ استوری)

### US-382 — 🛡 سپر ضد داده‌صفر (بحرانی — پاسخ مستقیم به حادثه پاک شدن مشتریان)
سه لایه دفاعی که دیگر اجازه تکرار آن حادثه را نمی‌دهد:
1. **سمت سرور (crm.php):** فهرست خالی روی داده ناخالی **رد می‌شود** — مگر با فلگ رسمی `allow_wipe` که فقط مسیر «شروع بهره‌برداری واقعی» (Go-Live) می‌فرستد. کلیدهای ردشده در پاسخ گزارش می‌شوند.
2. **سمت کلاینت (sync.js):** اگر دستگاهی فهرست خالی از کلیدهای حیاتی (مشتریان/درخواست‌ها/پیشنهادها/...) داشته باشد در حالی که سرور ناخالی است، **ارسال متوقف + هشدار شفاف** + ثبت در audit.
3. **آشکارساز افت انبوه:** کاهش >۵۰٪ رکوردهای هر کلید حیاتی → 🚨 رویداد در گزارشات + **اعلان فوری به ادمین و رییس هیات مدیره** با راهنمای بازگردانی — دقیقا «گزارشی که در حادثه نبود».

### US-363 — 🔀 ادغام مشتریان تکراری (ماژول جدید custmerge.js)
حل کامل مشکل «فولاد پارساگاد / مجتمع صنایع فولاد پاسارگاد»:
- **دو نقطه ورود:** ① داخل پیام «اطلاعات تکراری» گزینه ادغام پیشنهاد می‌شود ② دکمه 🔀 روی هر ردیف فهرست مشتریان.
- **ویزارد فیلدبه‌فیلد:** دو رکورد کنار هم؛ برای هر فیلد متفاوت (نام، شناسه ملی، آدرس، وندور، سقف اعتبار و…) انتخاب مقدار نهایی؛ **پیش‌فرض هوشمند = مقدار کامل‌تر** (مثل نام کامل به‌جای ناقص — دقیقا خواسته شما).
- **رابط‌ها و شماره‌ها جمع می‌شوند** (نه جایگزینی) — رابط هم‌نام تکرار نمی‌شود.
- **همه ارجاعات منتقل می‌شوند:** درخواست‌ها، پیشنهادها، پرونده‌های فروش، بایگانی، یادآورها، چک‌ها، نامه‌ها → کل اسناد در یک مسیر واحد.
- **ایمنی کامل:** فقط نقش‌های مدیریتی + confirm دومرحله‌ای + **snapshot هر دو رکورد قبل از ادغام** + حذف نرم رکورد دوم در آرشیو حذف‌شده‌ها + audit کامل + پیش‌نمایش شمار ارجاعات قبل از تایید.

### US-380 — 🌐 درخواست‌های سایت: کامل و خودکار
- **دکمه «👁 جزئیات کامل»** روی هر درخواست در انتظار: همه فیلدهای فرم سایت (نام، شرکت، تلفن، ایمیل، حوزه، موضوع، استاندارد فنی، برندها، شرح کامل) + **وضعیت پیوست** با مسیر امن سرور.
- **هنگام «✅ تایید و ورود»:** اگر مشتری با همان نام/تلفن موجود نباشد **خودکار ساخته می‌شود** (با رابط، موبایل و ایمیل فرم سایت)؛ اگر موجود باشد درخواست به همان متصل و رابط جدید با تایید شما تکمیل می‌شود.
- رکورد درخواست حالا **کامل** است: موضوع، شرح کامل (قابل مشاهده با «📝 متن استعلام»)، ایمیل، استاندارد، برندها و ارجاع پیوست.
- اعلان تایید، کد مشتری ساخته‌شده را هم اعلام می‌کند.

### US-381 — 📦 پایان سکوت کالاهای تکراری دستیار
- قبل از ثبت کالا در «اجرای سه‌گانه»: **پیش‌نمایش دو گروه** «🆕 جدید (ثبت می‌شوند)» و «🔁 تکراری — قبلا با کد X ثبت شده‌اند» + **اخذ تایید شما** برای ادامه.
- پیام نتیجه دقیق: «n کالای جدید ثبت شد + m تکراری رد شد»؛ اگر همه تکراری باشند پیام صریح «هیچ کالای جدیدی ثبت نشد — همه n قلم قبلا موجودند».

### US-370 — رابط اصلی در خروجی مشتریان
ستون «رابط اصلی (با سمت)» به «فیلتر و خروجی» مشتریان اضافه شد — پیش‌فرض تیک‌خورده، در PDF و Excel هر دو، خالی-امن.

---

## 🔬 وضعیت کیفیت

| مورد | وضعیت |
|-------|--------|
| کد ۵ استوری | ✅ کامل (custmerge.js جدید + sync.js/crm.php/bridge.js/ai-workbench.js/listtools.js/golive.js) |
| tester67-v147 | ✅ نوشته شد — ۵۰+ چک شامل **تست اجرایی کامل ادغام** (انتقال ارجاعات، جمع رابط‌ها، snapshot) و ساخت خودکار مشتری سایت |
| نسخه/کش | ✅ VER=v14.7، کش `ptf-crm-v14.7`، custmerge.js در SHELL |
| صحت‌سنجی | sync.js پس از پچ کامل بازخوانی و تایید شد؛ سایر پچ‌ها با تطبیق موفق edit اعمال شدند |
| اجرای تسترها + audit + zip | ⚠️ ترمینال سندباکس همچنان قطع — معوق مثل دو اسپرینت قبل (فهرست معوقات در هندآور) |

### 📌 وضعیت تحویل
- روی هاست: v14.4 بماند. بسته تجمیعی **v14.7 (شامل v14.5+v14.6+v14.7)** به محض بازگشت ترمینال: رگرسیون ۵۴ تستر → audit → zip.
- ⚠️ این ریلیز شامل تغییر `api/crm.php` است (سپر US-382) — هنگام آپلود پوشه api هم جایگزین شود (**اکسترکت روی موجود؛ حذف پوشه api ممنوع**).

## 🔜 بعدی طبق برنامه مصوب
**نقشه راه «تحلیل مدیریتی»** = US-349 (دلایل استاندارد باخت + گزارش Win/Loss) + US-350 (قیف تبدیل در داشبورد) + US-351 (تاریخ تحویل تعهدی) — سپس اسپرینت «و» (UX: تورها + دیالوگ هم‌تم + کانبان).
