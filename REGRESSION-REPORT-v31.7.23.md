# 📊 گزارش رگرسیون v31.7.23

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.23
**ابزار:** `_tools/run-full-regression.js` + `python3 _tools/audit.py` (۱۰ بخش)

## ✅ نتیجه نهایی

| معیار | مقدار | وضعیت |
|--------|-------|--------|
| تعداد تسترها | **۱۷۶** (+۱: tester198-draft-everywhere) | — |
| فایل‌های PASS | **۱۷۶** | ✅ ۱۰۰٪ |
| چک‌های FAIL | **۰** | ✅ صفر |
| audit.py (۱۰ بخش) | **PASS** | ✅ |
| `node --check crm/draftx.js` | **PASS** | ✅ |

## 🆕 پوشش تست جدید (tester198 — رفتاری با debounce واقعی)

- چرخه کامل سناریوی کارفرما: تایپ در فرم نامه → ذخیره خودکار (debounce ۸۰۰ms واقعی) → بازیابی همه فیلدها
- قیدهای پنل: کلید خارج از SYNC_KEYS/backup، بدون setInterval، password ذخیره نمی‌شود، سقف/TTL، opt-in restore، بدون نوار در ویرایش، پاکسازی پس از ثبت، هوک تصاویر نامه
- سند صورت‌جلسه پنل با گزینه‌های ردشده موجود

## 📌 نکته معماری

draftx کاملاً غیرمداخله‌گر است: هیچ ماژولی تغییر نکرد؛ حذف فایل = بازگشت کامل. کلید `ptf_draft_forms` تعمداً از هر ۷ نقطه whitelist داده (sync/role/backup/golive/API) بیرون است — این «نبودن» عمدی است و نباید در ممیزی‌های آینده به‌عنوان «کلید فراموش‌شده» اضافه شود (در سند پنل ثبت شد).

## 📁 فایل‌های تغییریافته

`crm/draftx.js` (جدید)، `crm/index.html`، `crm/sw.js`، `crm/clear-cache.html`، `EXPERT-PANEL-DRAFTS-v31.7.23.md` (جدید)، `_tools/uat/tester198` (جدید)

## 🚦 وضعیت گیت‌ها

- **گیت فنی: سبز** ✅
- **گیت انسانی: معوق** ⏳ — ۴ سناریوی RELEASE-NOTES در مرورگر واقعی روی چند فرم مختلف (نامه با تصویر، کالا، چک) + موبایل.
