# ارزیابی سطح‌به‌سطح: رویژن پیشنهاد برنده از پرونده فروش

**تاریخ:** ۱۴۰۵/۰۵/۲۷  
**کد:** v34.7.33  
**وضعیت:** فقط ارزیابی — بدون پچ.

هدف کارفرما (قطعی در این نشست):

- رویژن سند برد **داخل پرونده** مثل فرم پیشنهاد (افزودن/حذف/قیمت).
- پس از رویژن، **قیمت پیشنهاد برنده زنده = قیمت رویژن**.
- فاکتورهای قبلی **با تأیید کاربر ابطال**.
- اثرات مالی با پیشنهاد جدید هماهنگ.

این سند همهٔ رویه‌هایی را که این تغییر لمس می‌کند فهرست می‌کند تا چیزی از قلم نیفتد.

---

## ۰. نقشهٔ هویت امروز (باید قبل از هر طراحی در ذهن بماند)

```
استعلام (inqNo / rfq.cd)
    ├─ TO(ها)  srcToNo / coNo
    ├─ CO/TC موازی  altOf
    └─ CO برنده  st=won  no = ثابت تجاری
            ├─ wonRevisionSnapshot (اقلام قفل لحظه برد)
            ├─ revisionHistory[] (فقط مسیر کلاینت پیشنهاد، پیش از برد)
            ├─ invRef (ارجاع فاکتور → مرحله ۸)
            ├─ rial companion  rialOf = no
            └─ پرونده deal
                    wonOffer = no
                    rootOfferId = offer._id
                    contractAmount / effectiveContractAmount
                    linkedOffers[]  (root + amendments مثبت)
                    awardDocs[]     (اسنپ‌شات نمایشی؛ منبع واحد نیست)
                    awardRevisions[]
                    inspections[]
                    shipEvents / qcEvents / docsx
                    فاکتور.caseId + فاکتور.offerNo
                    رسید.caseId     (نه offerNo)
                    تخصیص FIFO روی caseId
```

دو منبع نوشتن پرونده: کلاینت (`sfSave`, `autoCreateProjectFromCO`) و سرور (`win_offer`, `revise_award`). هر رویژن که فقط یکی را عوض کند، «دو سند برد» می‌سازد.

---

## ۱. آنچه امروز هست (شکاف با هدف)

| خواسته | امروز |
|---|---|
| فرم کامل اقلام در پرونده | جدول حفظ/qty/price بدون ردیف جدید (`ptfAwardReviseOpen`) |
| قیمت CO برنده = رویژن | خیر؛ سند جدید `no-Rn` و ریشه `superseded` |
| ابطال فاکتور با تأیید | خیر؛ اگر رسمی فعال باشد کاهش ۴۰۹ |
| هم‌ترازی مالی | فقط `sd_rebuild_allocations` روی فاکتور **فعال کهنه** |
| وعده UI پیشنهاد | «نگارش از پرونده» — پرونده آن را تحویل نمی‌دهد |

تصمیم مکتوب ۱۴۰۵/۰۵/۲۶ (مسدود کاهش با فاکتور رسمی) هنوز در کد و `tester434` است. مدل جدید آن را **مشروط به void** می‌کند؛ این باید تصمیم جدید ثبت شود.

---

## ۲. ماتریس کامل وابستگی (هر ردیف یک کار اجباری در طراحی)

### ۲.۱ هویت پیشنهاد و پرونده

| جزء | اگر شماره ثابت + rev | اگر `-Rn` بماند | حداقل کار |
|---|---|---|---|
| `offers[].no` | همان | عوض | هدف کارفرما = ثابت |
| `offers[].rev` / `revisionHistory` | ++ و اسنپ | سند جدید جدا | سرور باید history بنویسد (الان فقط کلاینت پیش از برد) |
| `wonRevisionSnapshot` | بازنویسی | روی سند جدید | منبع اقلام UI (`awardLines` اول snapshot را می‌خواند) |
| `st` | بماند `won` | ریشه superseded، جدید won | تب پیشنهاد و insights روی `st=won` |
| `register_offer` / `won_offer_locked` | استثنا فقط فرمان پرونده | قفل ریشه درست است | `offerSave` هرگز |
| `case.wonOffer` | ثابت | عوض به `-R` | چاپ، docsx، buycmp، fiscal با همین فیلد |
| `case.rootOfferId` | بماند یا به همان `_id` | می‌شود `_id` جدید | `fiscalCaseMatchesOffer` اول `_id` را می‌سنجد |
| `linkedOffers` | ردیف root همان no | ریشه کهنه می‌ماند | پوشش فاکتور خطی از این مجموعه است |
| `contractAmount` | **ریشه + متمم فعال** | امروز فقط اقلام `-R` → **باگ متمم** | حتماً جمع دوباره |
| `awardDocs` | اسنپ جاری + rev | آرایه کهنه می‌ماند | فیلتر نمایش + بازنویسی سرور |
| `awardRevisions[]` | بماند برای گزارش | هست | — |
| اسناد `-R` قبلی | دست نخورند | هویت زنده‌اند | مهاجرت موردی با plan وابستگی |
| قفل `offerPostAwardLocked` | هنوز با inqNo همه را می‌بندد | همان | اصلاح جدا: فقط `wonOffer===no` |

### ۲.۲ فاکتور و مطالبات

| جزء | اثر رویژن | اگر void با تأیید | اگر void نشود |
|---|---|---|---|
| فاکتور رسمی فعال | گارد کاهش | `void_invoice` + correction `legal_void` | کاهش ممنوع؛ افزایش پوشش خطی ممکن است بشکند |
| فاکتور غیررسمی | lookup با `offerNo` | `void_unofficial_invoice` | صدور مجدد روی همان no با فعال قبلی تداخل (`existing` در unofficial-invoice.js) |
| `correct_invoice` | جایگزین void نیست | اصلاح مبلغ بدون ابطال هویت مالیاتی | برای تغییر اقلام کافی نیست |
| پوشش `lines` | سقف از `lineId\|sourceItemKey\|idx` | پس از void آزاد | اگر `lineId` در رویژن حذف شود over-coverage |
| `caseId` روی فاکتور | وصل به پرونده | void همان case | — |
| یتیم بدون caseId | `sd_bind_orphan` با wonOffer یکتا | اگر دو deal یک no → bind نمی‌شود | نشت award را اول درست کنید |
| مرجوعی فروش | اقلام از `offer.items[idx]` | فاکتور void → مرجوعی باید disposition داشته باشد | رویژن اقلام ایندکس مرجوعی را جابه‌جا می‌کند |
| AR / حساب مشتری | فقط سند فعال | بدهی فاکتور می‌رود | مبلغ کهنه می‌ماند |
| `invoiceOfOfferUnique` | یک فاکتور فعال per offerNo | پس از void صفر تا صدور جدید | دو فعال = bind انسانی |

### ۲.۳ خزانه و تخصیص

| جزء | قاعده غیرقابل نقض |
|---|---|
| رسید `posted` | حذف/mutate مبلغ ممنوع |
| تخصیص | همهٔ ردیف‌های case `replaced` سپس FIFO از نو |
| پس از void همهٔ فاکتورها | `creditRemainIRR` = کل وصول پرونده |
| اتحاد | `Σرسید = Σتخصیص + Σبستانکاری` (`sd_reconcile_case`) |
| ارز | رسید با fxRate؛ رویژن اقلام ارزی نرخ را عوض نمی‌کند مگر جدا |
| `timing` VAT | rebuild فعلی بر اساس صدور فاکتور است؛ بدون فاکتور VAT تخصیص نمی‌گیرد (درست) |
| اصلاح رسید / انتقال بستانکاری به پرونده دیگر | رویژن نباید caseId رسید را عوض کند |
| قفل سال مالی | تاریخ فاکتور/رسید؛ void در سال قفل = توقف کل فرمان |

### ۲.۴ اسناد قانونی و گردش پرونده

| جزء | وابستگی |
|---|---|
| `invRef` روی CO | سیگنال مرحله ۸؛ P5 آن را unset می‌کند؛ رویژن باید صریح باطل/بازنشانی کند وگرنه استپر دروغ می‌گوید |
| `sfStageOf` | از شواهد (تحویل، invRef، فاکتور). void فاکتور مرحله را عقب می‌کشد اگر شاهد بعدی نباشد |
| docsx PL/IN/IB/MOM | پوشش از اقلام `wonOffer`؛ بدون ابطال، PL روی قلم حذف‌شده می‌ماند. حذف docsx هنوز در UI نیست |
| `ptfDocxCommit` → `sfShipCommit(packing)` | ابطال PL باید packing را هم void کند |
| نامه | به `prjNo` پروژه نه offer؛ اثر کم |
| چاپ سند برد | `sfAwardPrint` از `awardDocs.snap`؛ اگر به‌روز نشود Rev قدیم چاپ می‌شود |

### ۲.۵ خرید، موجودی، بازرسی

| جزء | وابستگی |
|---|---|
| `buycmp.sourceOfferNo` + `item.sourceOfferNo` | با شماره ثابت سالم؛ با `-R` خرید از CO کهنه جدا می‌ماند |
| تطبیق قلم | `lineId` / `ptfProcLineKey` — P5 این‌ها را در items جدید نمی‌نویسد |
| خرید ثبت‌شده برای قلم حذف‌شده | disposition اجباری (انبار / عودت تأمین / اسقاط) — مسیر P4 هست |
| عودت تأمین | سند بستانکار تأمین + `ptf_crm_purchase_returns` |
| مازاد/رزرو `offerNo` | رزرو روی شماره؛ `-R` رزرو را رها نمی‌کند |
| QC رویداد | مستقل از اقلام؛ نوت رسمی جداست |
| `inspections[]` | پیش‌پر رویژن بازرسی؛ رویژن کامل سفارش باید inspections کهنه را stale کند |
| پیش‌فاکتور خرید / payable | بعضی با offerNo؛ اسکن advisory مثل `admin_delete_plan` |

### ۲.۶ گزارش‌های مالی جانبی

| موتور | کلید | پس از void+رویژن |
|---|---|---|
| پورسانت | فاکتور فعال تسویه‌شده، `dealOf` با wonOffer **یا inqNo** | void → از مبنا خارج. `inqNo` ممکن است پروندهٔ موازی را بردارد (باگ جدا) |
| سال مالی | deal با `fiscalCaseMatchesOffer` + فاکتور فعال در جمع سود | void درست کنار می‌رود؛ `rootOfferId` اگر عوض شود هویت سال عوض می‌شود |
| سرمایه در گردش | سند فعال | مانند سال مالی |
| FX / سود ارزی | لیست offerNos پرونده + wonOffer | شماره ثابت بهتر است |
| حساب مشتری | فاکتور فعال + مرجوعی | اقلام مرجوعی از offer.items زنده — خطر ایندکس |

### ۲.۷ نسخه ریالی و ارز

| جزء | کار |
|---|---|
| `rialOf === no` | شماره ثابت → همراه با اقلام کهنه؛ باید stale یا rebuild در فرمان |
| تبدیل جدید از برد قفل است | رویژن پرونده باید همراه را آگاه کند |
| حاشیه `legacyRialRate` | اگر اقلام عوض شود بدون fxConvertedCosts، حاشیه دروغ می‌شود |

### ۲.۸ همگام‌سازی و همزمانی

| جزء | کار |
|---|---|
| یک lock `meta.json.lock` | همهٔ offer+deal+invoice+alloc+correction در یک commit |
| idempotencyKey | اجباری؛ retry همان result |
| پاسخ `data` | کلاینت باید cache همان کلیدها را جایگزین کند قبل از رندر کشو |
| کلاینت دیگر | rev بالاتر؛ وگرنه overwrite محلی awardDocs |
| تست UAT `tester434` | انتظار `-R` و گارد کاهش؛ با مدل جدید باید بازنویسی شود نه دور زدن خاموش |

### ۲.۹ نقش و ممیزی

| نقش | رویژن اقلام | void فاکتور | توصیه فرمان ترکیبی |
|---|---|---|---|
| admin / chairman / ceo / commercial | UI بله | ارشد بله | بله |
| accountant | UI خیر | سرور بله | فقط void جدا |
| sales | سرور WIN بله / UI خیر | خیر | هیچ‌کدام |
| سایر | خیر | خیر | خیر |

هر فرمان: `reason` اجباری + `ptf_crm_corrections` + audit. ابطال رسمی در دنیای واقعی نیاز به سند مودیان دارد — نرم‌افزار جایگزین سامانه نیست؛ در UI هشدار بدهید.

### ۲.۱۰ چیزهایی که عمداً خارج از فرمان‌اند

- صدور خودکار فاکتور جدید
- حذف رسید / تغییر مبلغ خزانه
- پاک کردن docsx بدون void
- ادغام پرونده تکراری (فرمان جدا)
- باز کردن قفل سال مالی
- تغییر TO مگر کارفرما جدا بخواهد (رویژن برد = مالی)

---

## ۳. سناریوهای کسب‌وکار که باید در طراحی پوشش داده شوند

1. **تغییر سفارش کارفرما** (هدف اصلی): قلم +/− و قیمت؛ فاکتور با تأیید void؛ بستانکاری می‌ماند.  
2. **لغو کل پروژه:** همان فرمان با items خالی؟ امروز مبلغ ≤۰ رد است. بهتر فرمان `cancel_case` جدا: void فاکتور + `status=cancelled` + `effectiveContractAmount=0` بدون offer تهی.  
3. **مردود بازرسی (P4/P5):** زیرمجموعهٔ (۱) با پیش‌پر qty؛ disposition اقلام مردود اجباری.  
4. **فقط افزایش قیمت / قلم جدید، فاکتور رسمی مانده:** یا void+صدور مجدد، یا اصلاحیه/فاکتور متمم — پوشش خطی را چک کنید.  
5. **پرونده با متمم مثبت:** رویژن ریشه نباید متمم را صفر کند.  
6. **دو پرونده یک inq / یک wonOffer:** فرمان باید ۰ یا ۱ case قطعی پیدا کند وگرنه ۴۰۹ `duplicate_sales_cases`.  
7. **فاکتور در سال قفل:** کل رویژن نایستد یا بایستد؟ توصیه: بایستد تا بازگشایی صریح.  
8. **چک متصل به فاکتور:** توقف یا تعیین‌تکلیف قبل از void.  
9. **ارزی + نسخه ریالی.**  
10. **غیررسمی تجمیعی چند پیشنهاد:** void فقط سهم این پرونده/این offer؛ فاکتور تجمیعی را کامل void کردن ممکن است پروندهٔ دیگر را بزند — **باید در پیش‌نمایش جدا شود.**

---

## ۴. فرمان پیشنهادی پس از دیدن کل سطح

یک action سروری: `revise_won_offer`

```
ورودی:
  caseId, reason, items[] (کامل، با lineId/pcode/qty/price),
  voidActiveInvoices: bool,
  dispositions[]  (برای قلم حذف‌شدهٔ دارای خرید/PL),
  idempotencyKey

ترتیب lock:
  1. یافتن دقیقاً یک case فعال + offer با wonOffer (نه ساخت رکورد)
  2. گارد نقش ∩ (WIN بدون sales) 
  3. گارد duplicate case / fiscal lock / چک فاکتور / تجمیعی
  4. اگر voidActiveInvoices: void تک‌تک فاکتور فعال همین caseId
       (نه هر فاکتور با همین offerNo در پروندهٔ دیگر)
     وگرنه اگر newTotal < oldTotal و رسمی فعال: 409
  5. نوشتن items+rev+history+snapshot روی همان offer؛ st=won
  6. contractAmount = total(root items) + sum(active amendment linkedOffers)
     effectiveContractAmount = همان
  7. awardDocs جاری جایگزین؛ awardRevisions += 
  8. invRef پاک با ردپا
  9. rial companion → stale
 10. sd_rebuild_allocations
 11. correction یکپارچه
 12. خروجی: rev, amounts, voidedIds, credit, reconcile, blockers skipped
```

UI: موتور جدول اقلام پیشنهاد با فلگ `postAwardRevision`؛ **نه** `offerSave`. پیش‌نمایش چندمرحله‌ای: دلتا → فاکتورها/چک/خرید → تأیید void.

P4 بازرسی همین API را با intent `inspection_adjust` صدا می‌زند.

---

## ۵. ترتیب اجرا (بدون کد در این نشست)

0. امضای تصمیم: شماره ثابت؛ void مشروط؛ نقش؛ متمم؛ `-R` کهنه دست‌نخورده.  
1. ابزار فقط‌خواندنی نشت awardDocs / duplicate wonOffer.  
2. فرمان سرور + تستر جایگزین tester434.  
3. UI پرونده.  
4. stale ریالی، docsx void، disposition خرید.  
5. اصلاح قفل inqNo و نمایش جعبهٔ زرد.  
6. UX کشو — بعد از پایداری مالی.

---

## ۶. چیزهایی که «تمام جنبه‌ها» را ناقص می‌کرد اگر دوباره از قلم می‌افتاد

این مرور علاوه بر گزارش‌های قبل اضافه کرد:

- مرجوعی فروش بر **ایندکس قلم** زنده  
- فاکتور **تجمیعی غیررسمی** چند پیشنهاد  
- رزرو مازاد روی `offerNo`  
- `invRef` به‌عنوان شاهد مرحله  
- `fiscalCaseMatchesOffer` روی `rootOfferId`  
- پورسانت با fallback خطرناک `inqNo`  
- lookup غیررسمی «یک فعال per offerNo»  
- تستر و تصمیم ۱۴۰۵/۰۵/۲۶  
- دو نویسندهٔ `ptf_crm_deals`

اگر این‌ها در طراحی فرمان نباشند، رویژن «مثل پیشنهاد» از نظر بازرگانی درست و از نظر دفتر کل غلط از آب درمی‌آید.
