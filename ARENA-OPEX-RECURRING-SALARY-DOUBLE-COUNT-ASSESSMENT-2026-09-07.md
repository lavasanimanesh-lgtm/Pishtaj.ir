# ARENA · ارزیابی — «آیا حقوق (مثلاً حسابدار) ممکن است دوبار حساب شود؟» و «چطور جلوی تکرار هزینه‌های تکرارشونده را بگیریم؟»

**تاریخ:** ۲۰۲۶-۰۹-۰۷ (۱۴۰۵/۰۶/۱۶) · **پایه کد:** `arena/01a07be8-pishtaj-ir` — `VERSION.json` = **CRM v34.38.3**
**نوع کار:** فقط ارزیابی — **هیچ فایل کدی تغییر نکرد**؛ فقط همین سند اضافه شده است.
**دامنه:** `crm/opex.js` · `crm/shareholders.js` · `crm/fiscal.js` · `crm/finance-write-guard.js` · `crm/petty.js` · `api/sales-domain.php`

---

## ۰) خلاصهٔ مدیریتی (پاسخ کوتاه)

**آیا حقوق حسابدار می‌تواند دوبار حساب شود؟ — بله، در چند سناریوی مشخص و محتمل.**

سیستم امروز **برای «حقوق سهامدار» و «قالب‌های تکرارشوندهٔ سروری» ضد دوباره‌شماریِ خوبی دارد** (کلید هویت `recurringKey` + ماده‌سازی idempotent سرور + void دائمی). اما **هیچ سپری بین «ورودی دستی» و «قالب تکرارشونده»، بین «دو قالب مشابه»، و بین دو سیلوی هزینهٔ «جاری (opex)» و «تنخواه (petty)» وجود ندارد.** همین سه شکاف، مسیرهای واقعیِ دوبار شماردن یک حقوق (به‌ویژه حقوق حسابدار که یک کارمندِ غیرسهامدار است) هستند.

**شدت واقعی:** در مجموعِ سود سال (`netProfit`) اثر مستقیم است، چون فرمول سود دو سیلوی مستقل را از هم کم می‌کند:
`net = projectProfit − opexTotal − pettyStandaloneTotal + amendTotal + cover.netBenefit` (`crm/fiscal.js:272`).
اگر یک حقوق در هر دو سیلو نشسته باشد، دو بار کسر می‌شود.

| # | شکاف | سناریو | شدت |
|---|---|---|---|
| ۱ | ورودی دستی + قالب تکرارشوندهٔ همان هزینه | کاربر «حقوق حسابدار» را هم دستی ثبت می‌کند هم قالبش فعال است | **P1** (محتمل‌ترین) |
| ۲ | دو قالب تکراری برای یک هزینه | دو قالب با نام/دستهٔ مشابه ساخته شده | P1 |
| ۳ | یک حقوق در دو سیلو (opex + petty) | هزینه در «هزینهٔ جاری» ثبت شد و پرداخت نقدی همان مبلغ در «تنخواه» هم ثبت شد | P1 |
| ۴ | عدم تطابق لینک پرونده (dealRef ↔ costEvent) | هزینهٔ جاریِ لینک‌شده هم در opex نادیده گرفته می‌شود هم (در حالت ناهماهنگ) در پرونده | P2 |
| ۵ | حقوق سهامدارِ سروری + ورودی دستی «حقوق و دستمزد» برای همان نفر/ماه | ردیف دستی generic کنار ردیف سروری `salary:` | P2 |

---

## ۱) معماری فعلی — هزینهٔ حقوق از کجا و چگونه شمارده می‌شود؟

### ۱.۱ سه سیلوی مستقل «هزینه»

| کلید | سیلو | مصرف‌کنندهٔ جمع |
|---|---|---|
| `ptf_crm_opex` | هزینه‌های جاری (اجاره/حقوق/بیمه/…) ماهانهٔ شمسی | `ptfOpexSum` (پنل) و `ptfOpexSumFiscal` (سال مالی) |
| `ptf_crm_petty` | تنخواه گردان (خروج‌های نقدی خرد) | `fiscalPettyStandalone` (سال مالی) |
| `ptf_crm_sharetx` | دفتر سهامداران (مطالبه/برداشت/حقوق) | `ptfShareholderBalance` (تراز هر سهامدار) |

این سه کلید **هیچ ارجاع متقابلی به هم ندارند** (نه `opex.pettyCd`، نه `petty.opexRowId`). هر کدام در سود سال به‌طور مستقل اثر می‌کنند — این دقیقاً جایی است که دوباره‌شماری متولد می‌شود.

### ۱.۲ دو «مسیر ثبت» کاملاً متفاوت برای حقوق

**الف) حقوق سهامدار (سروری، هویت‌دار):**
- هویت = `salary:<shCd>:<month>` (`crm/shareholders.js:31`).
- ثبت فقط با فرمان سروری `register_shareholder_salary` / `reconcile_shareholder_salaries` → **اتمی** یک ردیف `sharetx` (مطالبه) + یک ردیف `opex` (هزینه) می‌سازد (`api/sales-domain.php:2067-2070`).
- ردیف opex نشانه‌های پایدار دارد: `shareholderSalary:true`, `shareTx:<txCd>`, `recurringKey:salary:...`.

**ب) هزینهٔ جاری عادی (حقوق حسابدار، غیرسهامدار):**
- یا **دستی** (بدون هویت تکرارشونده — فقط `cd`/`_opexRowId`) — `ptfOpexSave` در `crm/opex.js`.
- یا **قالب تکرارشونده** `settings.opexTpl` با هویت `opex-template:<tplId>:<month>` (`crm/opex.js:369`) که سرور پس از snapshot قطعی آن را idempotent ماده می‌کند (`reconcile_recurring_opex`).

نکتهٔ کلیدی: **این دو مسیر (ب) با هم لینک نمی‌شوند.** ردیف دستی `recurringKey` و `tplId` ندارد؛ پس وقتی قالب همان ماه ماده می‌شود، سرور ردیف دستی را «همان رخداد» نمی‌بیند و ردیف دوم می‌سازد.

---

## ۲) سپرهای ضد دوباره‌شماری که **الان وجود دارند** (و خوب کار می‌کنند)

این‌ها را نباید خراب کرد؛ مبنای راهکارِ بی‌خطرِ پیشنهادی همین‌ها هستند:

1. **هویت دامنه‌ای `recurringKey`** — `opex-template:<tplId>:<month>` و `salary:<shCd>:<month>`؛ سرور reconcile بر این مبنا `sd_recurring_find_indexes` می‌زند و ردیف موجود را به‌روزرسانی می‌کند، نه ساختهٔ تکراری (`api/sales-domain.php:995`).
2. **idempotency فرمان سروری** — `sd_idempotency` + `idempotencyKey`؛ اجرای دوبارهٔ همان فرمان (ریترای شبکه) نتیجهٔ قبلی را برمی‌گرداند (`api/sales-domain.php:1487-1503`).
3. **قرارداد «فعال» یگانه** — `opexRowActive` (`crm/opex.js:28`) هر ردیف void/deleted/superseded را از همهٔ جمع‌ها حذف می‌کند.
4. **پاک‌سازی تکراری در reconcile صریح** — `duplicate_recurring_void` ردیف‌های خواهرِ هم‌هویت را void می‌کند (`api/sales-domain.php` بلوک `$explicitEligibility`).
5. **حذف حسابرسی‌پذیر** — `ptfOpexDel` برای ردیف‌های تکرارشونده به‌جای splice فیزیکی، `void_recurring_opex` سروری صدا می‌زند (tombstone ماندگار)؛ `sd_recurring_void` (`api/sales-domain.php:940`).
6. **کارمزد فاکتور پوششی جدا است** — `isCoverOpex` در `ptfOpexSumFiscal` skip می‌شود و کارمزد از خود فاکتور شمارده می‌شود (`crm/opex.js:186`).
7. **هزینهٔ لینک‌شده به پرونده فقط یک‌بار** — `ptfOpexSumFiscal` ردیف‌های `dealRef`دار را از `total` بیرون می‌گذارد؛ هزینه در پرونده (costEvent) کسر می‌شود. لینک با `ptfDealCostSync`/`ptfDealCostMatch` (اول `_opexRowId`، بعد cd+مبلغِ بی‌ابهام) همگام می‌شود (`crm/finance-write-guard.js:254-360`).
8. **حقوق سهامدار = تعهدی، نه نقدی** — `ptfOpexSumFiscal` حقوق را در `totalSalary` جدا می‌کند و از `totalCash` بیرون می‌گذارد؛ در سود، فقط یک‌بار (داخل `opexTotal`) کسر می‌شود (`crm/fiscal.js:324-333`).

---

## ۳) شکاف‌ها — مسیرهای واقعیِ دوبار شماردن «حقوق حسابدار»

### شکاف ۱ — ورودی دستی + قالب تکرارشونده (محتمل‌ترین) — **P1**

- ردیف دستی: `{cd: OPX-…, cat:'حقوق و دستمزد', amt:30m, month:'1405/05', …}` — بدون `recurringKey`/`tplId`.
- قالب «حقوق حسابدار»: ردیف سروری `{recurringKey:'opex-template:TPL-9:1405/05', tplId:'TPL-9', …}`.
- `ptfOpexPendingTpls` (`crm/opex.js:374`) و reconcile سرور فقط `recurringKey` یا `(tplId, month)` را تطبیق می‌دهند ⇒ ردیف دستی «نامرئی» است ⇒ ردیف دوم ساخته می‌شود.
- `ptfOpexSumFiscal` هر دو را جمع می‌کند ⇒ **حقوق حسابدار دو بار از سود کسر می‌شود.**
- هیچ هشدار/تطبیقِ «هم‌دسته + هم‌ماه + هم‌مبلغ + هم‌شرح» در لحظهٔ ثبت دستی یا ماده‌سازی سرور وجود ندارد.

### شکاف ۲ — دو قالب تکراری برای یک هزینه — **P1**

- دو قالب `TPL-9` «حقوق حسابدار ۳۰م» و `TPL-14` «حقوق ۳۰م» ⇒ هر ماه دو ردیف `opex-template:…:1405/05` با `tplId` متفاوت ⇒ جمع دو برابر.
- `saveTpls` (`crm/opex.js:361`) هیچ اعتبارسنجی تکراری روی قالب‌ها ندارد (نه cat+desc+amt، نه نام).

### شکاف ۳ — یک حقوق در دو سیلو (opex + petty) — **P1**

- حقوق حسابدار: (الف) در «هزینهٔ جاری» ثبت شد؛ (ب) پرداخت نقدی همان مبلغ به‌عنوان خروج تنخواه (`pettyAdd` بدون `dealRef`) ثبت شد.
- فرمول سود (`crm/fiscal.js:272`) هر دو را کم می‌کند: `opexTotal` شامل حقوق است و `pettyStandaloneTotal` شامل پرداخت تنخواه.
- `fiscalPettyStandalone` (`crm/fiscal.js:179-195`) فقط `p.dealRef` و `p.st==='void'` را skip می‌کند؛ **هیچ ارجاعی به opex ندارد** ⇒ دوباره‌شماری قطعی.
- (در نقطهٔ مقابل، سمت نقدیِ حقوقِ سهامدار با draw است و draw در خروجی نقدی `ptfFiscalCashData` نیامده — یعنی سمت نقدِ سهامدار فعلاً «کم‌شمار» است، نه بیش‌شمار؛ این یک نگرانیِ جدا و عمدی است.)

### شکاف ۴ — ناهماهنگی لینک پرونده (dealRef ↔ costEvent) — **P2**

- `ptfOpexSumFiscal` صرفاً به `x.dealRef` نگاه می‌کند تا ردیف را از `total` بیرون بگذارد؛ در پرونده هم صرفاً `opexRowId`/`cd` روی costEvent.
- اگر `dealRef` پاک شود ولی costEvent بماند ⇒ هزینه هم در opex شمرده می‌شود هم در پرونده (دوبار). اگر برعکس costEvent حذف/tombstone شود ولی `dealRef` بماند ⇒ از هر دو جا می‌افتد (صفر بار). هیچ verifier آشتی‌دهنده‌ای وجود ندارد.

### شکاف ۵ — حقوق سهامدار + ردیف دستی generic — **P2**

- سهامدار X ردیف سروری `salary:X:1405/05` دارد؛ کاربر یک ردیف دستی `حقوق و دستمزد` هم برای X می‌زند (بدون `shareholderSalary`).
- `isShareholderSalaryOpex` (`crm/opex.js:179`) ردیف generic را «حقوق سهامدار» نمی‌شناسد ⇒ هر دو در `total` می‌آیند، و ردیف دستی به‌نادرست `totalCash` (خروج نقدی) هم می‌شود.

### شکاف ۶ — نکتهٔ UX (کم‌ریسک اما گمراه‌کننده) — **P3**

- `ptfAutoApplyRecurring` (`crm/opex.js:801`) فقط «خواندنی» است (missing را گزارش می‌کند، نمی‌سازد). کاربری که «missing» می‌بیند ممکن است دستی ثبت کند و هم‌زمان دکمهٔ «ثبت قالب» را بزند ⇒ تلاقی با شکاف ۱.

---

## ۴) راهکار عملیِ بدون ایجاد باگ — «آشکارسازی و یک‌بار-شماردن»، نه «حذف خودکار»

اصل ایمن: **هیچ مرحله‌ای ردیف موجود را حذف/ادغام/ویرایش خودکار نکند.** دوباره‌شماری با «جلوگیری در مبدأ + هشدار قابل‌حل توسط انسان + (در یک گام) شمارشِ یک‌بار بر مبنای هویت» بسته می‌شود، نه با پاک‌کردن داده.

### گام ۰ — آشکارساز خواندنی (صفر ریسک، فقط گزارش) — **پیشنهاد شروع**
یک تابع read-only (هم‌سنخ `ptfAutoApplyRecurring`) که برای هر ماه، ردیف‌های opex «مشکوک به تکرار» را فقط **لیست می‌کند** (بدون تغییر داده):
- ردیف‌های هم‌ماه که «هم‌دسته + هم‌مبلغ» و یکی `recurringKey` دارد و دیگری ندارد؛
- قالب‌های تکراری (هم‌cat + هم‌amt + هم‌desc)؛
- ردیف opex و ردیف petty هم‌مبلغِ هم‌دوره.
خروجی فقط به UI/گزارش می‌رود. هیچ جمعی را عوض نمی‌کند ⇒ **صفر ریسک رگرسیون**.

### گام ۱ — گارد نرم در لحظهٔ ثبت دستی (فقط هشدار، بدون بلوک داده)
در `ptfOpexSave` (مسیر دستی): اگر ردیفِ هم‌ماهِ «هم‌cat + هم‌amt» موجود است که `recurringKey`/`tplId` دارد، یک `confirm` بپرسد: «برای این ماه یک قالب تکرارشوندهٔ هم‌مبلغ وجود دارد؛ ادامه می‌دهید؟» — کاربر می‌تواند عمداً ادامه دهد (دو هزینهٔ واقعاً مجزا)، پس داده خراب نمی‌شود.

### گام ۲ — اعتبارسنجی قالب در `saveTpls`
هنگام ذخیرهٔ قالب جدید، اگر قالبِ فعالِ دیگری با همان `cat + amt + desc` (نرمال‌شده) هست، هشدار/تأیید بگیرد. باز هم غیرمخرب.

### گام ۳ — ارجاع متقابل سیلوها (تنها گامی که جمع را عوض می‌کند — با تستر قفل)
برای بستن شکاف ۳، یک ارجاعِ یک‌طرفه و اختیاری اضافه شود (مطابق الگوی موجود `pettyCd` روی costEvent):
- وقتی پرداختِ یک هزینهٔ جاری از تنخواه انجام می‌شود، روی ردیف opex فیلد `paidByPettyCd` (یا برعکس روی petty `opexRowId`) بنشیند؛
- `fiscalPettyStandalone` ردیف petty‌ای را که `opexRowId` دارد skip کند (هزینه یک‌بار از opex کسر می‌شود).
این دقیقاً همان الگوی «تنخواهِ لینک‌شده به پرونده از جمع مستقل حذف می‌شود» است که امروز با `p.dealRef` کار می‌کند — پس از نظر مفهومی با کدِ موجود هم‌خانواده است. **الزامی:** تستر قفل‌شده در `run-ci-gate` (رفتار: petty با `opexRowId` نباید در `fiscalPettyStandalone` بیاید؛ و `netProfit` فقط یک‌بار کم شود).

### گام ۴ — «یک‌بار-شماردن» بر مبنای هویت در خودِ جمع (اختیاری، پادزهر نهایی)
در `ptfOpexSumFiscal` به‌جای جمع خام، یک `seen` بر مبنای `recurringKey || _opexRowId || cd` (با همان احتیاطِ `opexFindRow` که روی cd مبهم حدس نمی‌زند). چون `_opexRowId` امروز برای همهٔ ردیف‌ها backfill می‌شود (`opexEnsureRowIds`)، ددوب‌سازی ایمن است. اما این گام را **آخر و فقط پس از گام ۰-۱** انجام دهید تا ردیف‌های تکراریِ واقعیِ تاریخی، پیش از حذفِ اثرشان، دیده و توسط انسان تعیین‌تکلیف شده باشند.

### ترتیب پیشنهادی اجرا (هر گام مستقل و قابل بازگشت)
```
گام ۰ (آشکارساز read-only)  →  گام ۱ (هشدار ثبت دستی)  →  گام ۲ (هشدار قالب تکراری)
        ↘  سپس، پس از پاکسازی دستی داده‌های تاریخی:
گام ۳ (ارجاع petty↔opex + تستر قفل)  →  (اختیاری) گام ۴ (ددوب در جمع)
```

---

## ۵) ریسک‌های راهکارهای «غلط» که نباید انتخاب شوند

1. **حذف/ادغام خودکار ردیف‌های هم‌مبلغ** — دو هزینهٔ واقعاً مجزا (دو فاکتور هم‌مبلغ در یک ماه) را یکی می‌کند و دادهٔ مالی را بی‌صدا خراب می‌کند. ممنوع.
2. **ددوب بر `cd` به‌تنهایی در جمع** — در دادهٔ legacy، `cd` می‌تواند تکراری باشد (به همین دلیل `opexFindRow` روی cd مبهم `null` برمی‌گرداند و `opexWarnDuplicate` هشدار می‌دهد). ددوب باید روی `_opexRowId`/`recurringKey` باشد، نه `cd` خام.
3. **تغییر فرمول سود (`net = …`) بدون تستر قفل** — این فرمول قلب گزارش سال مالی است؛ هر تغییرش باید با تستر قفل‌شده در گیت پوشش داده شود (همان قراردادی که در v34.38.2/3 رعایت شد).
4. **ماده‌سازی محلی قالب در مرورگر** — عمداً در معماری فعلی ممنوع است (فقط سرور پس از snapshot قطعی). هر راهکار نباید این مرز را بشکند.

---

## ۶) نتیجه و پیشنهاد

- پاسخ صریح: **بله، «حقوق حسابدار» امروز می‌تواند دوبار حساب شود** — نه از ضعف در یک نقطه، بلکه از **نبودن پل بین سه سیلوی مستقل هزینه و بین «ثبت دستی» و «ثبت قالب‌محور»**. شکاف‌های ۱ و ۳ محتمل‌ترین و پراثرترین‌اند (مستقیم `netProfit` را کم‌کنند).
- زیرساختِ ضد تکراری که از قبل ساخته شده (recurringKey + idempotency سرور + void دائمی) **درست و قابل اتکا** است؛ راهکار باید روی همین شالوده بنا شود و فقط شکاف‌های «ورودی دستی/قالب دوم/سیلوی دوم» را پر کند.
- کم‌ریسک‌ترین مسیر: **گام ۰ (آشکارساز read-only) + گام ۱/۲ (هشدارهای غیرمخرب)** — این سه بدون تغییر هیچ عددی، دوباره‌شماری‌های جدید را عملاً متوقف می‌کنند؛ سپس گام ۳ (ارجاع petty↔opex) با تستر قفل برای بستن قطعیِ شکاف سیلوها.
- هیچ‌کدام از این‌ها نیاز به «حذف خودکار» ندارد؛ تعیین‌تکلیف ردیف‌های تکراریِ تاریخی بهتر است دستی/نیمه‌خودکار و با تأیید انسان باشد تا هیچ دادهٔ مالی بی‌صدا از بین نرود.

**وضعیت:** فقط ارزیابی؛ کد تغییر نکرده است.
