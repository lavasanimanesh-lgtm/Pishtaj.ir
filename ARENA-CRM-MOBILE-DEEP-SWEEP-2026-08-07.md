# پیمایش عمیق سراسری UI موبایل CRM — موجودی دکمه‌ها، آیکون‌ها و overflow

**تاریخ:** ۱۶ مرداد ۱۴۰۵ / ۷ اوت ۲۰۲۶
**دامنه:** همهٔ پنل‌های CRM، دکمه‌های تولیدشدهٔ runtime، action rowها، toolbarها، Kanban و ماژول‌های چاپ
**وضعیت:** گزارش شناسایی؛ مبنای اجرای مرحله‌ای اصلاحات بعدی

---

## 1. روش آزمون

### اجرای واقعی

- Chromium لمسی، RTL، viewport **320×568**؛
- کاربر Admin با fixture داده برای مشتری، تأمین‌کننده، کالا، درخواست، پیشنهاد، لید و یادآور؛
- باز کردن **۲۶ پنل قابل دسترس** از navigation؛
- اسکن هر پنل برای:
  - دکمه با `font-size:0` و بدون SVG/`data-ix`؛
  - دکمهٔ icon-only بدون `title` و `aria-label`؛
  - کنترل کوچک‌تر از 36px؛
  - button خارج از عرض viewport یا container clipping؛
  - متن truncate شده؛
  - container با `scrollWidth > clientWidth`.

### اسکن منبع

| شاخص | مقدار |
|---|---:|
| فایل JS CRM | 89 |
| `<button>` در منبع | 1,022 |
| `onclick` inline | 1,258 |
| `title` attribute | 144 |
| `aria-label` attribute | 8 |
| دکمه با کلاس `.ba` | 60 |
| `.ba` بدون title/aria | 51 |
| `.ba` با متن آغازین فارسی/لاتین (ریسک icon مبهم) | 8 |
| استفاده از `.tb2` | 114 |
| modal pattern (`.md-b`) | 324 |
| `position:fixed` در منبع | 57 |

> نتیجهٔ مهم: به‌دلیل icon-only کردن CSS با `font-size:0`، صرف داشتن متن در HTML تضمین نمی‌کند که کاربر موبایل آیکون معنی‌دار ببیند. اگر icon اولیه SVG نشده یا متن با `+`/حرف فارسی شروع شود، دکمه خالی یا مبهم می‌شود.

---

## 2. نتیجهٔ پیمایش 26 پنل

| پنل | دکمهٔ مشکوک به icon نامشخص | action فشرده/بدون نام | خارج از عرض viewport | truncate | overflow داخلی | وضعیت |
|---|---:|---:|---:|---:|---:|---|
| داشبورد | 0 | 0 | 0 | 0 | 0 | سالم در fixture |
| کارتابل | 1 | 0 | 0 | 0 | 0 | `✓✓ خواندم همه` icon استاندارد ندارد |
| دستیار | 0 | 0 | 0 | 0 | 0 | سالم در نمای اولیه |
| سرنخ‌ها | 1 | 0 | 0 | 0 | 1 | Kanban با عرض 900px |
| مشتریان | 1 | 2 | 0 | 0 | 0 | action row کوچک/بی‌نام |
| درخواست‌ها | 1 | 0 | 0 | 0 | 0 | دکمهٔ «+ جدید» فقط + |
| پیشنهادها | 2 | 0 | 0 | 0 | 0 | toolbar ایجاد فنی/مالی فقط + |
| پرونده‌های فروش | 0 | 0 | 0 | 2 | 2 | تب‌های اصلی truncate |
| تأمین‌کنندگان | 1 | 2 | 1 | 3 | 5 | گزارش امتیازها از چپ بیرون می‌رود |
| درخواست تأمین | 1 | 0 | 0 | 0 | 0 | ایجاد تأمین فقط + |
| کالاها | 1 | 1 | 0 | 0 | 0 | action row کوچک/بی‌نام |
| انبار | 1 | 0 | 0 | 0 | 0 | ثبت موجودی فقط + |
| چاپ چک | 6 | 0 | 7 | 0 | 1 | بحرانی؛ toolbar و canvas controls |
| بایگانی | 0 | 0 | 0 | 0 | 0 | سالم در fixture |
| مکاتبات | 2 | 0 | 0 | 0 | 0 | ایجاد نامه‌ها فقط + |
| قراردادها | 1 | 0 | 0 | 0 | 0 | قرارداد جدید فقط + |
| فاکتورها | 0 | 0 | 0 | 0 | 0 | سالم در fixture |
| مطالبات | 0 | 0 | 0 | 0 | 0 | سالم در fixture |
| هاب مالی | 4 | 0 | 0 | 0 | 0 | عملیات مهم بدون icon صریح |
| تحلیلگر | 0 | 0 | 0 | 0 | 4 | overflow ریز KPI/منبع لید |
| یادآورها | 1 | 0 | 0 | 0 | 0 | ایجاد یادآور فقط + |
| پیامک | 2 | 0 | 0 | 0 | 0 | بازیابی و مخاطب جدید مبهم |
| مدیریت سایت | 1 | 0 | 0 | 0 | 0 | خبر جدید فقط + |
| گزارشات | 0 | 0 | 0 | 0 | 0 | سالم در fixture |
| کاربران | 1 | 0 | 0 | 0 | 0 | کاربر جدید فقط + |
| تنظیمات | 0 | 0 | 0 | 0 | 2 | overflow کوچک در زیرپنل‌ها |

**نتیجهٔ مثبت:** در fixture پایه، هیچ یک از 26 پنل `pageerror` تولید نکرد و `document.scrollWidth` از 320px فراتر نرفت. این به معنی نبودِ مشکل نیست؛ مشکلات زیر در containerهای داخلی یا کنترل‌های پنهان رخ می‌دهند.

---

## 3. یافته‌های تاییدشده و اولویت اصلاح

### MOB-026 — ماژول چاپ چک: کنترل‌های مهم پنهان/خارج از viewport

**شدت: P0**
**شواهد اجرا:**

- toolbar بالای ماژول شامل «چاپ تکی»، «چاپ چندتایی»، «تنظیمات چاپ» و «راهنما» در 320px به مربع‌های بدون نماد تبدیل شد.
- کنترل‌های حالت گرافیکی شامل فعال، اسکن برگه، حذف پس‌زمینه، zoom، ذخیره و چاپ آزمایشی در مختصات x منفی تا حدود `-763px` دیده شدند.
- wrapper اصلی ماژول `scrollWidth=1074px` و `clientWidth=272px` داشت.

**ریشه:** `crm/cheque-print.js:184-210`؛ ترکیب toolbarهای متنی داخل `.sb2` با CSS icon-only سراسری و canvas/preview عریض.

**اصلاح پیشنهادی مرحله‌ای:**

1. toolbar اصلی را به action grid یا bottom-sheet دارای icon ساخت‌یافته تبدیل کنید؛
2. کنترل‌های گرافیکی را در یک accordion مستقل زیر canvas قرار دهید، نه در flex ردیف canvas؛
3. canvas را در wrapper افقی واضح با indicator «برای مشاهده بکشید» نگه دارید؛
4. برای هر action `icon`, `aria-label`, `title`, `data-action` تعریف کنید.

---

### MOB-027 — کارت بدهی تأمین‌کنندگان: دکمه گزارش امتیازها از سمت چپ بریده می‌شود

**شدت: P1**
**شواهد اجرا:**

- دکمهٔ «گزارش امتیازها (درخواستی)» در 320px با `x=-80px` شروع شد؛ بخش مهمی از آن خارج از viewport است.
- `#payablesBox` عرض محتوایی 375px در فضای 272px دارد.

**ریشه:** `crm/scoring.js:117-133`؛ header اطلاعاتی + دکمهٔ بلند در flex بدون قرارداد mobile-safe.

**اصلاح پیشنهادی:** عنوان و action را در mobile به دو سطر تبدیل کنید؛ دکمه را full-width با label و icon مشخص یا action-card 44px قرار دهید؛ هیچ child نباید `min-content` بزرگ‌تر از parent داشته باشد.

---

### MOB-028 — toolbarهای icon-only فاقد icon ساخت‌یافته در چندین ماژول

**شدت: P1**

الگوی مشترک: `.ph .bt` و `.sb2 .bt` در موبایل `font-size:0` می‌گیرند و تنها `::first-letter` برای نمایش نشانه استفاده می‌شود. در نتیجه:

- `+ پیشنهاد فنی` و `+ پیشنهاد مالی` هر دو به + مبهم تبدیل می‌شوند؛
- در هاب مالی، «پرداخت مستقیم»، «شارژ حساب»، «ارجاع دوره» icon معنی‌دار ندارند؛
- در چاپ چک، emojiهای `🔹`، `🔸` و `❓` به icon سازگار تبدیل نمی‌شوند؛
- در مشتری، تأمین‌کننده، کالا، انبار، مکاتبات، قرارداد، یادآور، پیامک، CMS و کاربران، action ایجاد فقط + است.

**ماژول‌های با اولویت بالا:** پیشنهادها، هاب مالی، چاپ چک، پیامک، مشتری/تأمین‌کننده.

**اصلاح پیشنهادی:** Button component واحد با markup اجباری زیر:

```html
<button class="mobile-action" aria-label="پرداخت مستقیم">
  <span class="ic" aria-hidden="true">…SVG…</span>
  <span class="lb">پرداخت مستقیم</span>
</button>
```

در mobile فقط `.lb` مخفی شود، نه کل text دکمه و نه با `::first-letter`.

---

### MOB-029 — action rowهای مشتری، تأمین‌کننده و کالا کوچک یا بدون نام هستند

**شدت: P1**

| ماژول | شواهد runtime | ریشهٔ نمونه |
|---|---|---|
| مشتریان | 2 action بدون title/aria، 39×34px؛ merge فقط 30×37px | `crm/offers.js:2982-2983` + hookهای حذف/ادغام |
| تأمین‌کنندگان | 2 action بدون title/aria، 39×34px | `crm/offers.js:3004-3005` |
| کالاها | 1 action 39×34px بدون نام | `renderProducts` و hookهای محصول |

**اصلاح پیشنهادی:** الگوی موفق `#rTb` و `#oTb` را با selector/metadata اختصاصی برای `#cTb`, `#sTb`, `#pTb` تعمیم دهید: 40×40، SVG centered، tone معنی‌دار، title/aria، و sheet برای actionهای ثانویه.

---

### MOB-030 — تب‌های پرونده فروش truncate می‌شوند

**شدت: P2**

در 320px متن‌های `📁 پرونده‌ها (ابلاغ سفارش)` و `🎯 فرصت‌های فعال` بیش از عرض دکمه هستند. کاربر نام کامل تب را نمی‌بیند.

**ریشه:** `crm/salesfiles.js:473-490`؛ flex row بدون `flex-wrap` یا variant موبایل.

**اصلاح پیشنهادی:** در mobile، تب‌ها تمام‌عرض دو سطری یا segmented control با label کوتاه «پرونده‌ها / فرصت‌ها» و title کامل شوند.

---

### MOB-031 — Kanban سرنخ‌ها عرض 900px دارد، بدون affordance کافی برای حرکت افقی

**شدت: P2**

`#ldWrap` در mobile محتوای 900px در عرض 272px دارد. Grid کانبان برای desktop مناسب است، ولی کاربر موبایل cue روشن برای swipe/scroll ندارد.

**ریشه:** `crm/leads.js:59-79`.

**اصلاح پیشنهادی:** wrapper دارای `overflow-x:auto` واقعی، gradient edge، hint «برای دیدن مراحل بکشید» و گزینهٔ پیش‌فرض table/card در عرض کمتر از 480px.

---

### MOB-032 — overflow و truncate موضعی در تحلیلگر، تنظیمات و tabs تأمین‌کننده

**شدت: P2/P3**

- تحلیلگر: KPI و منبع لید در چند child تا 291px در فضای 272px؛
- تنظیمات: دو زیرپنل با 220px در container 210px؛
- تأمین‌کننده: تب‌های داخلی/خارجی/بازبینی 1 تا 2px truncate می‌شوند.

این موارد فوریت MOB-026/027 را ندارند، اما نشان‌دهندهٔ نبود design token برای min-width و tab behavior هستند.

---

### MOB-033 — `.ba`های پرریسک بدون نام قابل‌دسترسی و با متن آغازین غیرآیکونی

**شدت: P1 برای ماژول‌های مالی/چک، P2 برای بقیه**

از 60 `.ba` مستقیم، 51 مورد title/aria ندارند. 8 مورد با متن فارسی/لاتین شروع می‌شوند و در mobile icon-only به حرف/فضای مبهم تبدیل می‌شوند؛ نمونه‌ها:

- `crm/cheque-panel.js:82,130` — «ابطال»؛
- `crm/supplier-finance.js:273,275` — «ابطال»؛
- `crm/working-capital.js:255` — «ابطال»؛
- `crm/ai-workbench.js:638` — «باز کردن پیشنهادها»؛
- `crm/supplier-finance.js:736` — «گردش حساب»؛
- `crm/procurement-link.js:226` — تغییر/لینک فاکتور.

**اصلاح پیشنهادی:** ابتدا چک، حساب تأمین‌کننده، سرمایه در گردش و procurement-link؛ سپس باقی `.ba`ها. هر migration باید title/aria و icon صریح داشته باشد.

---

## 4. وضعیت یافته‌های پیشین

| شناسه | وضعیت کنونی |
|---|---|
| MOB-001 کنترل‌های مودال | ✅ مرحلهٔ visual/label اجرا شد؛ focus trap و Escape عمومی هنوز در MOB-007 باز است |
| MOB-002 label جدول‌ها | ⏳ باز؛ قبل از migration گسترده action rowها انجام شود |
| MOB-003 refresh پس از bottom-nav/sync | ⏳ باز، P0 |
| MOB-004 toast/banner روی nav | ⏳ باز، P1 |
| MOB-005 landscape | ⏳ باز، P1 |
| MOB-012 header icons | ✅ اجرا و آزمایش شد |
| MOB-022 FAB پیشنهاد | ✅ اجرا و آزمایش شد |
| MOB-023 action ردیف درخواست | ✅ اجرا و آزمایش شد |
| MOB-025 action پیشنهادها/ارزی | ✅ اجرا و آزمایش شد |
| MOB-026 تا MOB-033 | 🆕 در این پیمایش شناسایی شدند |

---

## 5. ترتیب پیشنهادی اجرای اصلاحات

### فاز 2A — جلوگیری از کنترل‌های ناپدیدشده

1. **MOB-026: چاپ چک** — اولین اقدام بعدی؛ چند action عملاً قابل دسترس نیستند.
2. **MOB-027: کارت تأمین‌کننده** — دکمه گزارش و overflow payables.
3. **MOB-029: action row مشتری/تأمین‌کننده/کالا** — migration به component 40×40 + metadata.

### فاز 2B — قرارداد آیکون toolbar

4. **MOB-028** — map آیکون صریح برای toolbarهای پرتکرار؛ اولویت: پیشنهاد، تنخواه، چاپ چک، پیامک، مشتری/تأمین‌کننده.
5. **MOB-033** — `.ba`های مالی و چک با title/aria/icon صریح.

### فاز 2C — layout و داده

6. **MOB-002** — table-card labels و action schema.
7. **MOB-003** — state مستقل active panel و refresh sync.
8. **MOB-004/005/030/031/032** — overlay، landscape، tabs، Kanban و overflowهای باقی‌مانده.

---

## 6. معیار پذیرش مشترک برای هر مرحله

- در 320px، هیچ action ضروری x منفی یا `right > viewport` نداشته باشد.
- هر icon-only action: SVG/آیکون قابل‌دیدن + `title` + `aria-label`.
- هر action row: حداقل 40×40px، مرکز آیکون با delta صفر، رنگ معنایی و بدون text truncation.
- actionهای ثانویه هرگز حذف نشوند؛ در sheet/menu با label کامل در دسترس باشند.
- هر module قبل از merge در 320×568، 390×844، dark mode و landscape 844×390 بررسی شود.
- هیچ `pageerror` جدیدی در پیمایش routeهای پایه ظاهر نشود.

---

## 7. پیگیری اجرایی — MOB-026 انجام شد

ماژول چاپ چک در همین شاخه اصلاح شد:

- toolbar اصلی به چهار tile برچسب‌دار «تکی، چندتایی، تنظیمات، راهنما» تبدیل شد؛
- کنترل‌های گرافیکی به grid چهارستونهٔ برچسب‌دار با iconهای رنگی تبدیل شدند؛
- parent کارت گرافیکی، section و viewport چک `width:100%` و `min-width:0` گرفتند تا canvas بزرگ، toolbar را از صفحه بیرون نزند؛
- خود canvas عمداً عریض باقی مانده، اما اکنون در viewport داخلی 240px با scroll لمسی و hint صریح حرکت افقی نمایش داده می‌شود.

**بازآزمایی 320×568:** grid کنترل‌ها `242px` عرض داشت، هیچ دکمه‌ای x منفی نبود، `document.scrollWidth=320`، و پیمایش دوبارهٔ 26 پنل همچنان بدون pageerror/console error بود.

### 8. پیگیری اجرایی — MOB-027 انجام شد

کارت بدهی غیرنقدی تأمین‌کنندگان بازطراحی شد:

- عنوان کوتاه و جمع بدهی در بخش summary مستقل قرار گرفت؛
- actionهای «بستانکاری‌ها» و «امتیازها» به دو tile رنگی برچسب‌دار تبدیل شدند؛
- هر ردیف بدهی، اطلاعات قابل wrap و دکمهٔ 54px «جزئیات» با نام کامل قابل‌دسترسی دارد؛
- دکمهٔ گزارش امتیازها دیگر از سمت چپ بریده نمی‌شود.

**بازآزمایی 320×568 با بدهی فعال:** `payablesBox` برابر 270px scrollWidth/clientWidth بود، `document.scrollWidth=320` ماند، دو action اصلی 119×58px و action جزئیات 54×54px بدون clipping نمایش داده شدند.

### 9. پیگیری اجرایی — MOB-029 انجام شد

action rowهای مشتری، تأمین‌کننده و کالا استانداردسازی شدند:

- مشاهده آبی، ویرایش کهربایی، حذف قرمز، پیام سبز و ادغام بنفش؛
- همهٔ actionها 40×40px، SVG 20×20px و وسط‌چین هستند؛
- برای base actionها و actionهای تزریق‌شدهٔ حذف/ادغام/پیام، `title` و `aria-label` صریح افزوده شد؛
- buttonهای کوچک 30/39×34px و بدون نام پیشین حذف شدند.

**بازآزمایی 320×568:** برای مشتری 5 action، تأمین‌کننده 4 action و کالا 2 action؛ همگی 40×40px با delta مرکز icon/button برابر صفر، `document.scrollWidth=320` و بدون clipping رندر شدند.

### 10. پیگیری اجرایی — MOB-028 و MOB-033 مرحلهٔ اول انجام شد

ماژول سراسری `mobile-actions.js` افزوده شد تا buttonهای toolbar که در mobile `font-size:0` می‌گیرند، با icon صریح، رنگ معنایی، `title` و `aria-label` دوباره قابل‌تشخیص شوند. این mapping، actionهای پرتکرار مانند پیشنهاد فنی/مالی، پرداخت مستقیم، شارژ، ارجاع، گزارش، ورود/خروجی Excel، مشاهده، ویرایش، حذف، فاکتور، حساب و اتصال را پوشش می‌دهد.

همچنین `.ba`های داخل جدول‌های باقی‌مانده به actionهای 40×40px با SVG وسط‌چین و رنگ معنایی ارتقا می‌یابند؛ متن اصلی برای desktop حفظ می‌شود و فقط در mobile پنهان می‌شود. مودال‌ها، action rowهای قبلاً مهاجرت‌شده، چاپ چک و actionهای پیشنهاد از این normalizer مستثنا هستند تا regression ایجاد نشود.

**بازآزمایی 320×568:** toolbar پیشنهاد فنی/مالی و 5 action هاب مالی همگی 44×44px با delta مرکز icon/button صفر داشتند؛ نمونه‌های «ابطال» و «گردش حساب» `.ba` نیز به‌ترتیب action قرمز و سبزآبی 40×40px با aria-label تبدیل شدند.

### 11. پیگیری اجرایی — MOB-002 مرحلهٔ اول انجام شد

ماژول `mobile-table-labels.js` اضافه شد تا header هر `.tb2 table` را خوانده و برای هر cell کارت موبایل `data-mobile-label` بسازد. labelهای ستون مانند «کد»، «شرکت»، «صنعت»، «رابط اصلی»، «تاریخ»، «وضعیت»، «قیمت مرجع» و… اکنون کنار مقدار واقعی نمایش داده می‌شوند. ستون عملیات و rowهای `colspan` عمداً label نمی‌گیرند تا action row شلوغ نشود.

فرم‌های داخل modal، جدول‌های چاپی و tableهای دارای `data-mobile-labels="off"` مستثنا هستند تا grid ورودی و چاپ آسیب نبیند.

**بازآزمایی 320×568:** جدول مشتری، درخواست و کالا labelهای صحیح دریافت کردند؛ action cell label خالی ماند، `document.scrollWidth=320` و overflow/clipping مشاهده نشد.
