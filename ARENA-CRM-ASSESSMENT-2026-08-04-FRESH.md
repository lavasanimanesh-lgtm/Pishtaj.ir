# ارزیابی جامع وضعیت CRM — ۱۴۰۵/۰۵/۱۳ (2026-08-04)

**برنچ:** `arena/019fcc74-pishtaj-ir` @ `2a361a7` (v34.0.3-alpha)
**روش:** بازبینی ایستای کامل کد (۸۷ اسکریپت لودشده + index.html + ۲۲ فایل PHP)، تحلیل امنیتی، اجرای مجموعهٔ تست UAT موجود، و راستی‌آزمایی هر یافته با شواهد `فایل:خط`.
**دامنه:** ریپو محض است؛ دسترسی به پروداکشن/سرور نداشتم — موارد محیطی به‌صورت «نیازمند راستی‌آزمایی روی سرور» علامت خورده‌اند.

---

## ۱) خلاصهٔ اجرایی

| شدت | تعداد | موضوع |
|---|---|---|
| 🔴 بحرانی | ۲ | XSS ذخیره‌شدهٔ سیستمیک (۳۲۳ نقطه تزریق) + ابزار تشخیص بدون احراز هویت |
| 🟠 بالا | ۷ | ۵ ویژگی مالی خراب (ReferenceError/رندر نشدن) + پیش‌نمایش پیوست تنخواه دوضرب خراب + کش‌باستینگ دو فایل |
| 🟡 متوسط | ۵ | کش PWA ناقص، PII کاربران بدون توکن، ~۱۷۰KB کد مرده، ۲۲MB زیپ عمومی در گیت، رگرسیون تست بی‌صدا |
| 🔵 بهبود | ۵ | بدهی فنی معماری (۸۹ اسکریپت سراسری، تکثیر `esc/norm/money`، …) |
| ✅ سالم | — | سنخون‌دهی، Auth سرور، ایندکس‌ها، نسخه‌بندی — بخش ۷ |

**مهم‌ترین پیام:** سه تابع کلیدی مالی در refactorهای v33.10/11 حذف شده‌اند (`ptfFiscalLock`، `ptfFiscalRender`، `ptfFiscalSnapshotOpen`) و دکمه‌هایشان هنوز در UI هست — **تستِ پروژه (tester101) این را می‌گیرد ولی کسی اجراش نکرده**؛ پس رگرسیون بی‌صدا به پروداکشن رفته.

---

## ۲) یافته‌های بحرانی 🔴

### SEC-01 — XSS ذخیره‌شدهٔ سیستمیک در ۳۲۳ نقطه (تأییدشده با PoC)

- `escP()` (تعریف در `crm/index.html:1851`) فقط `& < > "` را escape می‌کند — **تک‌کوتیشن `'` را نه.**
- اما در **۳۲۳ محل** خروجی `escP(...)` مستقیم داخل رشتهٔ تک‌کوتیشنِ هندلرهای inline ساخته می‌شود؛ الگو: `onclick="fn(\'' + escP(x) + '\')"`.
- **اثبات اجرایی (node):** سورس `crm/bridge.js:444` با ورودی `EVIL');alert(1);//` خروجی زیر را ساخت → اجرای کد دلخواه:
  ```html
  onclick="supApprove('EVIL');alert(1);//')"
  ```
- **نقاط بهره‌برداری‌پذیر با دادهٔ کاربر (فیلدهای آزاد):**
  - `crm/scoring.js:560` — `ptfScoreAdjust(..., escP(rec.co))` → **نام شرکت مشتری**
  - `crm/cheques.js:889` — `ptfTplCopy(i, escP(aud), escP(name))` → **نام مخاطب**
  - `crm/supspec.js:154` — نام/شرکت تامین‌کننده • `crm/backup.js:558`، `crm/bridge.js:558`، `crm/inqreader.js:623` — **نام فایل پیوست** • `crm/guards.js:197` — label
  - ~۳۱۶ نقطهٔ دیگر با کدها/شناسه‌ها — هر کدام که کاربرقابل‌ویرایش باشند (نام بانک، شرح، پیوست و…) همین وضعیت را دارند؛ علاوه بر تزریق، **هر آپستروف معمولی** (مثل نام شرکت با `'`) هندلر را می‌شکند.
- **مسیر ورود از بیرون:** اکشن عمومی `add_supplier` (بدون توکن) → رکورد تامین‌کننده → نمایش در پنل ادمین → اجرای کد در **نشست ادمین**. شدت واقعی: بالا.
- **تشدیدکننده:** لایهٔ دفاعی ادعاشده مرده است — `crm/xss-guard.js` و `crm/dompurify.min.js` در `index.html` **هرگز لود نمی‌شوند** («XSS Guard v32» صرفاً دکور در ریپوست).
- **رفع:** (الف) در همهٔ ۳۲۳ نقطه، ساخت آرگومان JS با الهام از `siteJsArg` (`bridge.js:59` — escape کردن `\` و `'`) یا `JSON.stringify` به‌جای `escP`؛ (ب) میان‌مدت: حذف هندلرهای inline و استفاده از `addEventListener`/data-attributes؛ (ج) یا لود واقعی DOMPurify + `ptfSafeSetHTML` برای مسیرهای HTML آزاد.

### SEC-02 — `api/data-health-check.php` بدون احراز هویت در بستهٔ دیپلوی

- این ابزار تشخیصی با صرف `?confirm=yes` ساختار داده/مسیرها را افشا می‌کند (`api/data-health-check.php:13-19`) و در خود فایل نوشته «پس از استفاده از سرور حذف شود» — ولی **در گیت committed است و هر ریلیز دیپلوی می‌شود**. امروز فقط `FilesMatch` در `api/.htaccess` جلوی HTTP را می‌گیرد (وابسته به پردازش .htaccess توسط هاست).
- **رفع:** حذف از بستهٔ دیپلوی (deploy-exclude) یا افزودن گارد توکن سرور مثل بقیهٔ endpointها.

---

## ۳) باگ‌های فعال (تأییدشده در کد فعلی) 🟠

| # | باگ | شواهد | اثر |
|---|---|---|---|
| B-01 | **«🔒 قفل سال» سال مالی خراب** — `ptfFiscalLock` صدا زده می‌شود ولی هیچ‌جا تعریف نشده؛ بدنهٔ تابع به‌صورت کد مرده **بعد از `return`** در `fiscalHtml()` مانده | دکمه: `crm/fiscal.js:443` • کد مرده: `fiscal.js:450-453` • تست شکست‌خورده: `_tools/uat/tester101-v184.js` | قفل کردن سال مالی از UI غیرممکن؛ ReferenceError در کنسول |
| B-02 | **`ptfFiscalRender` تعریف نشده** — ۶ فراخوانی زنده (تغییر سال، تغییر درصد تقسیم، ثبت تقسیم سود، سند اصلاحی، بازگشایی قفل) | `crm/fiscal.js:345,352,441,442,484,573` (+`working-capital.js:263` با گارد) | داشبورد سال مالی بعد از هر تعامل رفرش نمی‌شود؛ کاربر نتیجهٔ عملیات را نمی‌بیند |
| B-03 | **دکمهٔ «✔ وصول» چک صادرهٔ عادی در هاب مالی خراب** — `ptfChequeClearIssuedUi` تعریف نشده | `crm/cheque-panel.js:78` (تابع هسته `window.ptfChequeClearIssued` موجود است؛ wrapper از UI گم است) | وصول چک صادره از پنل مالی = ReferenceError (هم‌راستا با گام باز CHQ-MOD-001) |
| B-04 | **«🔒 اسنپ‌شات و ترازنامه سال مالی» بی‌صدا کاری نمی‌کند** — `ptfFiscalSnapshotOpen` تعریف نشده (با گارد typeof، پس فقط دیالوگ بسته می‌شود) | `crm/reports.js:241` | ویژگی مرده در مرکز فرماندهی گزارش |
| B-05 | **بدنهٔ «گزارش جامع مدیریتی» همیشه «گزارشی ثبت نشده»** — `ptfBuildReportHtml` تعریف نشده | `crm/reports.js:243` | گزارش جامع US-272 نمایش داده نمی‌شود |
| B-06 | **پیش‌نمایش PDF/HEIC رسید تنخواه دوضرب خراب:** (۱) مسیر نسبی غلط `fetch('api/attachment-thumb.php')` بدون `../` → از زیر `/crm/` → 404؛ (۲) حتی با مسیر درست، `api/.htaccess` (FilesMatch) آن را deny می‌کند (در لیست allow نیست) | `crm/petty.js:983` (بقیهٔ کد از `'../api/…'` استفاده می‌کند) • `api/.htaccess:11-17` | همان «علامت ۱: تامبنیل ضمایم در پروداکشن دیده نمی‌شود» که در ارزیابی قبلی باز ماند — fallback به `<embed>` باعث شده کمتر دیده شود |
| B-07 | **Cache-busting خراب برای دو فایل:** `official-ledger.js?v=1.0` و `ledger-report.js?v=1.0` (بقیهٔ ۸۵ اسکریپت `?v=34.0.3-alpha`) | `crm/index.html` (grep بر ?v=) | به‌روزرسانی این دو ماژول به کاربر نمی‌رسد (تا پاک شدن کش/SW) |

> یادداشت مثبت: باگ «ردیف‌های تکراری گزارش تنخواه» ارزیابی قبلی در v34.0.2 رفع شده و درست کار می‌کند (`crm/petty.js:744-790` بازبینی شد).

---

## ۴) مشکلات پیکربندی/دیپلوی 🟡

| # | مورد | شواهد |
|---|---|---|
| D-01 | **Precache سرویس‌ورکر ناقص:** ۷ اسکریپتِ لودشده در SHELL نیستند: `client-server`، `settings-accordion`، `storage-quota`، `tool-feedback`، `tool-licenses`، `tool-report-drafts`، `unofficial-invoice` → آفلاین/PWA ناقص | diff لیست `<script>` index.html با `crm/sw.js` |
| D-02 | **`users_get` عمومی = افشای PII:** در لیست اکشن‌های عمومی است (`api/crm.php:32`) و `name/mobile/email` همهٔ کاربران را بدون توکن می‌دهد (`api/crm.php:1454+`). لاگین قطره‌ای لازم است ولی mobile/email برایش لازم نیست | حداقل‌سازی فیلدهای خروجی پیش از لاگین |
| D-03 | **۴ فایل zip (~۲۲MB) ردیف‌شده در گیت** علی‌رغم `*.zip` در `.gitignore`: `crm-update.zip` (۴.۹MB — **پکیج کامل CRM عمومی دانلودشدنی است**)، `staging-update-human-photos.zip`، `services-photos.zip`، `ptf-all-photos.zip`. حجم pack ریپو ~۶۲MB | `git ls-files` |
| D-04 | **کد مردهٔ دیپلوی‌شده (~۱۷۰KB):** `ai-tech-assistant.js`، `lead-finder.js`، `eng-calc.js`، `tech-proposals.js`، `xss-guard.js`، `dompurify.min.js` (بدون ارجاع از index.html)، `datex.js` (طبق کامنت VER «حذف شده» ولی فایل مانده)، `crm/inqreader.js.bak` (۶۰KB سورس قدیمی قابل‌دریافت) | grep ارجاعات |
| D-05 | **رگرسیون تست بی‌صدا:** `tester101-v184` الان **۴ FAIL** دارد (از جمله گاردهای render/print/lock سال مالی) ولی در چرخهٔ ریلیز اجرا نمی‌شود → رگرسیون‌های B-01/B-02 بدون هشدار رفتند. ۱۰ تستر جدید (290-299) همه سبزند | اجرای واقعی در این نشست |

---

## ۵) بدهی فنی / بهبودها 🔵

1. **معماریِ ۸۹ اسکریپت با اسکوپ سراسری مشترک:** تعریف‌های تکراری هم‌نام — `esc` (۱۴ فایل)، `norm` (۱۲)، `money` (۹)، `add` (۹)، `getData/setData` (چند فایل) با **پیاده‌سازی‌های متفاوت** (مثلاً `esc` در `settings-accordion.js` تک‌کوتیشن را escape نمی‌کند). آخرین فایل لودشده برنده است → باگ‌های وابسته به ترتیب لود. مسیر درست: Strangler Fig که با `finance-core.js` شروع شده را ادامه دهید.
2. **پاک‌سازی knowledge-center در هر ریکوئست API:** `api/crm.php:2-8` روی هر call یک `glob+unlink` اجرا می‌کند — I/O بیهوده + منطق ناهم‌جا در endpoint. باید به اسکریپت نگهداری منتقل شود.
3. **فرمت‌های نسخه ناسازگار:** `v34.0.3-alpha` با regex قدیمی تسترها (`v([0-9.]+)'`) نمی‌خواند → تسترها failِ کاذب می‌دهند و فرسوده می‌شوند (همان‌طور که در D-05 دیدیم). VERSION.json/index.html/sw.js خودشان هم‌راستا هستند ✅.
4. **گام ۴ ماژول چک (وصول مشتری با چک → وارده) طبق بک‌لاگ رسمی هنوز پیاده نشده** (`crm/BACKLOG-USER-2026-08-01.md`, CHQ-MOD-001).
5. **اقدام عملیاتی معوق:** «🛠 بازسازی حقوق سهامدار» (`ptfMigrateShareholderOpex`) باید **یک‌بار روی پروداکشن** اجرا شود (یادآوری ارزیابی ۰۸-۰۴ — نیازمند تأیید اجرا روی سرور).

---

## ۶) گردش کار پیشنهادی رفع (اولویت)

| ترتیب | اقدام | تلاش |
|---|---|---|
| ۱ | SEC-01: یکپارچه‌سازی escape آرگومان JS در ۳۲۳ نقطه (استخراج `siteJsArg` به ui-kit و جایگزینی الگو) + تستر UAT | متوسط |
| ۲ | B-01…B-05: بازگرداندن `ptfFiscalRender`/`ptfFiscalLock`/`ptfFiscalSnapshotOpen`/`ptfBuildReportHtml`/`ptfChequeClearIssuedUi` (بازیابی از کد مرده/تستر به‌عنوان مرجع) + سبز کردن tester101 در CI | کم |
| ۳ | B-06: `../api/` + افزودن `attachment-thumb` به allow-list (با گارد توکن)؛ B-07: بامپ ?v دو فایل؛ D-01: تکمیل SHELL | کم |
| ۴ | SEC-02 + D-02 + D-03: حذف health-check از بسته، محدودسازی فیلدهای users_get، پاک‌سازی zipها از ردیابی گیت | کم |
| ۵ | D-04 حذف کد مرده؛ فعال‌سازی اجرای تسترها در CI (D-05) | کم |

---

## ۷) مواردی که سالم‌اند (منصفانه)

- **سینک سرور (data_push/pull):** قفل flock روی meta، rev-per-key، tombstone سمت سرور، سپر «دادهٔ صفر روی دادهٔ ناخالی»، رد payload با ردیف تکراری پیشنهاد، ناموفق‌سازی کامل پاسخ هنگام شکست DB (v33.22.0) — طراحی درست.
- **Auth سرور:** توکن HMAC امضاشده + کال‌بک سمت فایل tokens با انقضا، rate-limit لاگین، نقش «سرور-معتبر»، allow-list اکشن‌ها و فیلتر کلیدهای سینک بر اساس نقش.
- **همهٔ ۸۷ فایل JS لودشده بدون خطای نحوی** (node --check)؛ **بدون id تکراری در index.html**؛ بدون TODO/FIXME/console.log رها شده.
- نسخه‌بندی VER/`?v=`/نام کش SW در ۸۵+۲ اسکریپت هم‌راستا (به‌جز B-07).

---

*تولیدشده توسط Arena Agent — ارزیابی مستقل از ARENA-EVALUATION-PREV-AGENT-2026-08-04 (که موارد B-01/B-03 به بعد را پوشش نمی‌داد).*
