# RELEASE NOTES — v34.31.0 (رفع «ثبت نقشه از پنل سئو اجازه دسترسی نمی‌دهد»)

**تاریخ:** ۲۰۲۶-۰۹-۰۲ · **گیت:** در حال اجرا · **نکتهٔ بامپ:** جاروی استاندارد سه‌فرمی نسخه (۱۳۹ فایل) با استثنای همیشگی `run-ci-gate.js` و `fix-version-drift.js`

## گزارش کارفرما

«از داخل پنل سئو نمی‌شود نقشه ثبت کرد؛ ارور می‌دهد و اجازه دسترسی نمی‌دهد. تمام راهکارهای اشاره‌شده در راهنما را اجرا کردم ولی باز وصل نمی‌شود.»

## ریشه‌یابی — چرا راهنما کمک نمی‌کرد

زنجیرهٔ شکست: دکمهٔ «📤 ثبت نقشه» → `api/gsc.php?action=sitemap_submit` → اگر فایل `api/gsc-config.php`
روی سرور **خطای نحوی** داشته باشد، `require` با خطای مرگبار تمام می‌شود و پاسخ به‌جای JSON، صفحهٔ خطای
HTTP 500 است. فرانت فقط وقتی خطا را «می‌بیند» که پاسخ JSON باشد؛ پس به شاخهٔ `catch` می‌افتد و پیام عمومی
«عدم دسترسی به سرور / سرچ کنسول» نشان می‌دهد. کاربر همهٔ راهکارهای سمت گوگلِ راهنما (افزودن سرویس‌اکانت
با سطح Full و…) را انجام می‌دهد ولی حلقه هرگز حل نمی‌شود، چون مشکل در فایل تنظیماتِ سمت سرور است نه در گوگل.

شواهد تشخیصی جلسه: `api/gsc.php` و `api/careers.php` به‌درستی پاسخ JSON می‌دهند، ولی فایل‌های صرفاً
`return`کننده/کتابخانه‌ای با خروجی خالی در این هاست ۵۰۰ برمی‌گردانند — یعنی «بازکردن کانفیگ در مرورگر»
آزمون معتبری برای سالم‌بودن آن نیست؛ آزمون معتبر فقط `php -l api/gsc-config.php` است.

## تغییرات

1. `api/gsc.php`:
   - `gsc_cfg()` حالا خطای پارس را با `try/catch (Throwable)` + بافر خروجی شکار می‌کند و حالت‌های
     `missing / syntax / not_array / incomplete / ok` برمی‌گرداند.
   - قرنطینهٔ خودکار: فایل دارای خطای نحوی به `gsc-config.php.broken-<ts>` منتقل می‌شود تا حلقهٔ ۵۰۰ بشکند.
   - اکشن‌ها به‌جای `gsc_not_configured` خام، `gsc_cfg_or_jerr()` با پیام فارسیِ دقیق + راه‌حل می‌خوانند.
   - گیت‌های احراز (۴۰۱/۴۰۳) پیام فارسی قابل‌اقدام دارند: نشست منقضی → خروج/ورود؛ نقش غیرمجاز →
     فهرست نقش‌های مجاز و نقش فعلی کاربر.
   - `status` حالا `config_state` و `config_detail` برمی‌گرداند؛ `selftest` برای هر حالت خرابی
     (`no_config / config_broken / config_incomplete`) گام‌های رفع جداگانه می‌دهد.
2. `crm/cms.js`: پیام شاخهٔ خطای شبکه/۵۰۰ دقیق شد (اشاره به `php -l` و قرنطینه)؛ رندر «آزمون اتصال»
   حالت‌های `config_broken / config_incomplete` و پاسخ‌های `ok:false` را نشان می‌دهد.
3. `crm/gsc.js`: پیام شاخهٔ خطا در پنل سرچ کنسول دقیق شد.
4. `GSC-PANEL-SETUP-FA.md`: جدول عیب‌یابی با چهار ردیف جدید (خطای ۵۰۰/نحوی، `gsc_config_broken`،
   `permission_denied`، `authentication_required`).

## اقدام لازم کاربر روی سرور

`php -l api/gsc-config.php` — در صورت خطا، فایل را مطابق `api/gsc-config.sample.php` از نو بسازید
(کلید خصوصی عیناً با `\n`ها داخل `"` دوتایی). سپس تب سئو ← «🧪 آزمون اتصال».
