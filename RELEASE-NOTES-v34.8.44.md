# یادداشت‌های انتشار — v34.8.44 (R5-گام ۲)

**موضوع:** عکس پروفایل به فضای ابری (T4-3b) — آخرین رسانهٔ سنگینِ داخل نقشهٔ سینک.
**تاریخ:** ۲۰۲۶-۰۸-۲۹ · **تستر قفل‌کننده:** `tester545-v34.8.44-r5g2-avatars-s3.js` (۳۴ سنجه · سبز) · **گیت:** run-ci-gate + arch-guard

## چه چیزی عوض شد

| | قبل | بعد (v34.8.44) |
|---|---|---|
| تصویر کامل آواتار (۱۲۸px) | dataURL داخل `ptf_crm_avatars` — **با هر سینک به همهٔ دستگاه‌ها** (~۲۰KB per کاربر) | **آروان S3** (پوشهٔ `avatars/`، آپلود مستقیم مرورگر — الگوی چک‌پرینت v34.8.28) |
| نقشهٔ سینک‌شونده | `{user: {v: dataURL کامل, ts}}` | `{user: {k: کلید ابری, t: پیش‌نمایش ریز ۶۴px, ts}}` — **~۳KB per کاربر** |
| رندر | dataURL | پیش‌نمایش ریز فوری + **ارتقای نرم** به کیفیت کامل با presign (کش ۴۵دقیقه‌ای per-tab) |
| سهم LS + پهنای باند سینک | N کاربر × ~۲۰KB روی هر دستگاه | N × ~۳KB |

- **سازگاری سه‌شکلی:** رشتهٔ legacy، `{v,ts}` و `{k,t,ts}` همه خوانده می‌شوند؛ حذف همچنان tombstone `{v:null,ts}` — merge سینک per-user با `ts` برنده می‌ماند (قفل رگرسیون در تستر).
- **fallback بدون ابر:** اگر آپلود شکست بخورد، همان dataURL کامل ذخیره می‌شود (رفتار قبل) — آفلاین همیشه کار می‌کند.
- **مهاجرت نرم:** مقادیر قدیمی >۴KB با گارد ۶ساعته (ptfCache) به ابر منتقل و با نسخهٔ سبک جایگزین می‌شوند؛ خلاصه در audit.
- key-registry: MEDIA → `s3-with-local-pointer`.

## گیت‌ها
- `run-ci-gate`: ۱۶۱ تستر (tester545 جدید) — سبز؛ arch-guard بدون تخلف جدید.
- تغییرناپذیرها: sync.js merge (فقط قفل رگرسیون)، ۴ ابزار ریکاوری HTML، backup.js، پروداکشن v34.8.12.

## تست دستی پیشنهادی روی استیجینگ
1. تنظیمات → 🖼 عکس پروفایل → انتخاب عکس → پیام «در فضای ابری ذخیره شد»؛ DevTools → Network: درخواست `presign_put` (پوشهٔ avatars) و PUT به آروان.
2. Application → Local Storage → `ptf_crm_avatars` → مقدار جدید ~۳KB است (`k` + `t`).
3. رفرش → آواتار فوری از پیش‌نمایش؛ بعد از یک لحظه کیفیت کامل (یک `presign_get` در Network).
4. کاربر دیگر (دستگاه/تب دیگر) → آواتار همین کاربر بدون دانلود ۲۰KB سینک می‌شود.

## وضعیت نازک‌سازی پس از این نسخه
- ✅ R1..R3، R4-گام۱، R5 کامل (توکن/نشست خارج از LS + رسانهٔ نهایی به S3)
- ⏳ R6 (v34.8.45/46): 2FA نقش‌های مالی، bulk-revoke، rate-limit، ابزارهای ریکاوری IDB-aware → سپس **v34.9.0** برای پروداکشن
- 📊 R4-گام۲ (حذف legacy) و T3-5: پس از ≥۷ روز تلهمتری واقعی پروداکشن (win7 + صفر R4-GATE-BYPASS) → v34.9.1
