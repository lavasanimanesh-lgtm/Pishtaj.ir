# نقشهٔ کلیدواژه→صفحه v1 + baseline واقعی GSC — گام‌های A3 و B1

**تاریخ:** ۱۴۰۵/۰۵/۲۹ — 2026-08-20 | **فایل داده:** `SEO-KEYWORD-MAP-v1.csv` (۶۳۲ ردیف)
**مولد (idempotent):** `tools/seo_kw_map.py`

---

## ۱) گام A3 — baseline از دادهٔ واقعی GSC (اسکرین‌شات کاربر، بازهٔ ۳ ماهه)

| سنجه | مقدار | تفسیر |
|---|---|---|
| کل کوئری‌های دارای نمایش | **~۲۸** | سایت تقریباً در نتایج ظاهر نمی‌شود |
| کل نمایش (Impressions) | **~۲۸۳** | برای سایتی با ۶۵۹ صفحه، تقریباً صفر |
| کل کلیک | ۳۱ (که ۲۹ تای آن دو کوئری اول = برند) | ترافیک فعلاً فقط برندی است |
| **کوئری صفحهٔ اول (pos≤10)** | **~۴** (برند + ۲ کوئری تک‌نمایشی) | **baseline هدف ۵۰۰: از ~۴ شروع می‌کنیم** |
| کوئری جایگاه ۱۱–۳۰ (دم‌دستی) | ~۶ (بزرگ‌ترین: ۵۳ نمایش در جایگاه ~۱۹) | کم ولی وجود دارد |
| کوئری جایگاه >۳۰ | ~۱۸ با نمایش‌های تک‌رقمی | گوگل صفحات را می‌شناسد ولی بالا نمی‌آورد |

### حکم تشخیصی

مشکل، «جایگاه» نیست — **دیده‌شدن** است. محتوای سنگین سایت عمدتاً تیر–مرداد ۱۴۰۵ منتشر
شده و گوگل هنوز اعتماد/اعتبار کافی برای نمایش آن نساخته. سه اهرم اصلی به ترتیب اثر:

1. **پوشش ایندکس** — باید بدانیم از ۶۵۹ صفحه چندتا ایندکس شده (GSC → Indexing → Pages؛
   دو عدد Indexed / Not indexed را بفرستید).
2. **هدف‌گیری on-page سیستماتیک** (گام B2 بر اساس همین نقشهٔ v1) تا صفحات برای
   کوئری‌های درست «کاندیدای نمایش» شوند.
3. **سیگنال بیرونی** (فاز D: GBP، دایرکتوری‌ها، رپورتاژ) — بدون چند لینک معتبر، دامنهٔ
   جوان در فارسی B2B دیر بالا می‌آید. این مورد فقط از دست شما برمی‌آید.

> **دو خواستهٔ کوچک از شما:**
> ۱) فایل `Queries.csv` را مستقیم در چت پیوست کنید (متن کوئری‌ها در اکسل شما به‌هم
> ریخته چون Excel آن را UTF-8 باز نکرده؛ خود فایل سالم است — برای دیدنش در Excel:
> Data → Get Data → From Text/CSV → File Origin: **65001 UTF-8**).
> ۲) دو عدد Indexed / Not indexed از مسیر Indexing → Pages.

---

## ۲) گام B1 — نقشهٔ کلیدواژه→صفحه v1 (انجام شد ✅)

از تایتل واقعی هر صفحه + قواعد نیت جستجوی B2B فارسی، برای **۶۳۲ صفحه** نگاشت ساخته شد:

| خوشه | کلیدواژه | نمونه قاعده |
|---|---|---|
| دانش‌نامه (kc) | ۱٬۲۶۹ | موضوع + «قیمت …» + «خرید …» |
| محصولات (products) | ۲۳۱ | «خرید X» + «تامین X» + «قیمت X» |
| تامین‌کننده‌ها | ۶۲ | تایتل + «شرکت تامین X» + «لیست تامین‌کنندگان X» |
| برندها | ۶۱ | تایتل + «قیمت محصولات برند» + «خرید برند در ایران» |
| مقایسه‌ها (kc + هاب) | ۸۳ | «مقایسه A و B» + «تفاوت A و B» |
| خدمات/صنایع/بلاگ/هسته | ۷۱ | نیت‌محور |
| **جمع** | **۱٬۷۷۷** | هدف ≥۱٬۲۰۰ ✅ |

- **ستون‌های CSV:** `url, cluster, intent (info/commercial/transactional), primary, variants, kw_count, words`
- این v1 «پیش‌نویس نگاشت» است: منبع کار گام B2 (بازنویسی تایتل/H2/FAQ بر اساس کوئری
  هدف) و پس از رسیدن `Queries.csv` خوانا، با کوئری‌های واقعی کالیبره می‌شود.

### ریاضی هدف با این نقشه

۱٬۷۷۷ کلیدواژهٔ نگاشت‌شده × نرخ صفحه‌یک‌شدن ۳۰٪ (پس از B2 + ایندکس کامل + حداقل
off-page) ≈ **۵۳۰ کلمهٔ صفحه‌یک** — یعنی هدف ۵۰۰ با موجودی فعلی سایت قابل دستیابی است،
به شرط اجرای B2 و فاز D.

---

## ۳) گام بعدی

- **B2 (من):** بهینه‌سازی on-page سیستماتیک بر اساس نقشه — شروع از خوشهٔ products و
  suppliers (نیت تجاری، نزدیک‌ترین به درآمد)، سپس KC با FAQPage schema.
- **شما:** دو عدد Indexing + پیوست Queries.csv + شروع چک‌لیست فاز D (GBP در اولویت).
