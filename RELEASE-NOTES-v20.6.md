# 🧩 ریلیز v20.6 — کارت ویزیت چندتایی، هاب مالی، رفع حقوق سهامدار و PL انگلیسی

## ✅ پرسش کارت ویزیت‌های متعدد
از این نسخه، دستیار کارت ویزیت می‌تواند خروجی چندتایی `cards[]` را دریافت کند. اگر یک تصویر شامل چند کارت باشد، سیستم کارت‌ها را به‌صورت تب «کارت ۱، کارت ۲، ...» نمایش می‌دهد و امکان «ثبت همه کارت‌ها با مقصد انتخابی» دارد.

> دقت عملی وابسته به کیفیت عکس است: برای ۱۰ کارت یا بیشتر، تصویر باید واضح، بدون پرسپکتیو شدید و با رزولوشن کافی باشد.

## ✅ رفع باگ حقوق سهامدار
- فیلد «حقوق ماهانه موظف» از نوع number/dialog-money به text قابل‌ورود آزاد تغییر کرد.
- مقدار با parser امن `n(v.salary)` خوانده می‌شود.
- ریشه مشکل: فرمت/کنترل عددی عمومی روی برخی مرورگرها/موبایل می‌توانست ورود عدد طولانی را مختل کند.

## ✅ نام ماژول مالی
- نام «تنخواه گردان» در UI به «هاب مالی» تغییر کرد.
- دسترسی کامل هاب مالی فقط برای `admin/chairman` است.
- سایر کاربران همچنان امکان ثبت هزینه خود را دارند.

## ✅ پکینگ لیست
- قالب PL فیلدهای انگلیسی گیرنده را دارد: Consignee Name, Attention, Tel, Address.
- وزن خالص/ناخالص کلی و ردیفی قابل ورود است.
- صدور PL همچنان از داخل پرونده فروش و از موتور `docsx` انجام می‌شود.

## فایل‌های تغییر یافته
- `crm/ai-workbench.js`
- `api/llm.php`
- `crm/shareholders.js`
- `crm/petty.js`
- `crm/docsx.js`
- `crm/index.html`
- `crm/sw.js`
- `crm/perms.js`
- `crm/tour.js`
- `_tools/uat/tester122-v206.js`
- اصلاح تسترهای نسخه‌پذیر: tester118/119/120/121

## 🔬 کیفیت
- تستر جدید: `tester122-v206.js` با ۱۶ چک.
- رگرسیون کامل: **۱۰۹ تستر / صفر خطا**.
- Audit: PASS با هشدار قدیمی تصاویر سنگین.
