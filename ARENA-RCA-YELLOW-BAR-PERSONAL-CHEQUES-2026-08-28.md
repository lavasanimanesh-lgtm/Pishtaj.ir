# RCA — نوار زرد پایدار «۱ تغییر هنوز به سرور نرسیده [personal_cheques]» در استیجینگ

**تاریخ:** ۲۰۲۶-۰۸-۲۸ | **برنچ بررسی:** `arena/01a04968-pishtaj-ir` (= `origin/main@47e0c14`)
**نسخهٔ زندهٔ استیجینگ (احرازشده):** `v34.8.35` — بنر استیجینگ زنده: «برنچ: main — کامیت: 47e0c14 — 17:12 UTC»
**نوع:** تحلیل علت ریشه‌ای (بدون تغییر کد) — علت قطعی پیدا شد؛ **در main امروز هم موجود است.**

---

## ۱) علامت

بنر زرد پایین صفحهٔ CRM استیجینگ:

> 🟡 ۱ تغییر هنوز به سرور نرسیده — تب را نبندید تا نشانگر همگام سبز شود **[personal_cheques]**

- بدون بخش «آخرین خطا» در انتهای پیام.
- با رفرش و با وجود نسخهٔ ۳۵ روی استیجینگ، برنمی‌گردد به سبز — پایدار است.

---

## ۲) زنجیرهٔ فنی (کدِ واقعی)

### ۲.۱ — بنر از کجا می‌آید؟
`crm/sync.js` (~خط ۱۵۶۶): بنر = نمایش `state.dirty` است. پیام مذکور حالت پیش‌فرض (warn) است. `state.dirty` در **`localStorage['ptf_sync_dirty']`** ذخیره می‌شود (sync.js:178) → بین رفرش‌ها می‌ماند.

### ۲.۲ — انتقال امروز مسیر Phase B است
`pushDirty` اول چک می‌کند `ptfBPhaseActive()` → بله → push از `crm/client-server.js` با صف IDB و endpoint **`api/crm.php?action=data_push`** (cs_live:357-374) انجام می‌شود.

### ۲.۳ — چرا ACK نمی‌شود؟
سرور در `data_push` هر کلید را از دو صافی می‌گذراند (api/crm.php ~1931):

```php
if (!in_array($k, $allowed_keys, true))       { $skipped_keys[] = $k; continue; }
if (!in_array($k, $role_sync_keys, true))     { $forbidden_keys[] = $k; continue; }
```

کلیدِ `forbidden` **هرگز** وارد `savedKeys` نمی‌شود. سمت کلاینت (`ptfBFlushQueue`، cs_live ~690):

```js
var pending = failed.concat(result.rejected || [], result.skipped || [], result.forbidden || [], …);
window.ptfSyncMarkPendingKeys(pending);   // ← دوباره dirty می‌شود
```

یعنی: **forbidden سمت سرور = بن‌بست ابدی** — هر flush دوباره dirty می‌سازد، نوار هرگز سبز نمی‌شود. (مسیر legacy `pushDirty` چنین وضعیتی را با حذف dirty و نشانگر 🟠 مدیریت می‌کرد، ولی چون Phase B مالک انتقال است و صافی سمت کلاینت هم این کلید را «مجاز» می‌داند، هرگز اجرا نمی‌شود.)

---

## ۳) 🔴 علت ریشه‌ای: ناهماهنگی ماتریس RBAC سینک بین کلاینت و سرور

سهمیهٔ sync `ptf_crm_personal_cheques` در سه لایه، این‌طور است:

| لایه | فایل | sales | buyer | accountant | collector | admin/chairman/ceo/commercial |
|---|---|---|---|---|---|---|
| ماتریس سمت کلاینت | `crm/sync.js:31-34` | ✅ | ✅ | ✅ | ✅ | ✅ (SYNC_KEYS) |
| **ماتریس سمت سرور** | `api/crm.php:356-370` | ❌ (`$crm` ندارد) | ❌ (buyer = `$crm`+۳ کلید) | ✅ (`$accountant`) | ❌ (`$collector` ندارد) | ✅ (`sync_all_keys`) |
| رجیستری فرمانی سرور | `api/sales-domain.php:267` | ✅ هر ۸ نقش | ✅ | ✅ | ✅ | ✅ |

- **سه درخت بررسی‌شده و هر سه یکسان:** `main@47e0c14`، `e156098b` (استقرار ۱۱:۰۶ امروز)، `d949306d` (`arena/01a0425e` = محتوای زندهٔ قبل از ظهر) — یعنی **با استقرار امروز هم رفع نشده و روی main فعلی هم هست.**
- RELEASE-NOTES-v34.8.29 مدعی «ماتریس هر ۵ نقش سمت crm.php» است — دقیقاً همان ۵ نقشی که اضافه شده (admin/chairman/ceo/commercial/accountant)؛ **sales/buyer/collector جا مانده‌اند** درحالی‌که کلاینت و رجیستری فرمانی هر سه را مجاز دانسته‌اند.

**نتیجه:** هر کاربر با نقش sales/buyer/collector که کلید شخصی‌اش dirty شود (مهاجرت T5-2، ثبت/ویرایش چک شخصی، یا حتی merge پس از pull) در حلقهٔ بن‌بست ۲.۳ می‌افتد: flush → forbidden → pending دوباره → نوار زرد پایدار. نکتهٔ ظریف: خودِ داده احتمالاً از مسیر فرمانی (`entity_upsert` با رجیستری ۸-نقشی) **به سرور رسیده** — فقط ACK مسیر `data_push` هرگز صادر نمی‌شود؛ پس نوار، «نرسیدن» را گزارش می‌کند که دیگر واقعی نیست.

---

## ۴) یافته‌های فرعی (مؤثر در پایداری/پنهان‌ماندن)

- **F-A — `ptfEntitySaveCollection` هیچ‌وقت `opts.cb` را صدا نمی‌زند** (`crm/sales-domain-v2.js:462-505`): مهاجرت `cheques.js` پاک‌سازی کلیدهای legacy (`ptf_personal_cheques_<user>`) را فقط در cb با `state==='acked'` انجام می‌دهد؛ در مسیر فرمانی cb هرگز فراخوانی نمی‌شود → کلیدهای legacy تا بی‌نهایت روی دستگاه می‌مانند و مهاجرت هر بوتِ پنل چک‌ها دوباره اجرا می‌شود (بعد از همگرایی، diff=۰ و بی‌ضرر است، ولی نشانهٔ قراردادی ناقص است).
- **F-B — مسیر Phase B برای bucketهای rejected/forbidden/skipped هیچ `ptf_sync_last_error`‌ای نمی‌نویسد** → بنر بدون بخش «آخرین خطا» رندر می‌شود و علت برای کاربر/پشتیبانی نامرئی است (همان چیزی که در علامت دیدیم).
- **F-C — کور‌بُott A11:** گیت A11 فقط `PTF_ENTITY_CMD_ENABLED` (کلاینت) ↔ `sd_entity_registry()` (سرور) را مقایسه می‌کند — هر دو این کلید را با نقش‌های کامل دارند → گیت سبز. **هیچ گِیتی ماتریس RBAC سینک (`sync.js` ↔ `sync_allowed_keys_for_role`) را مقایسه نمی‌کند.** tester531 هم (مثل بوت واقعی) با نقش ممتاز اجرا شده و صحنهٔ sales/buyer/collector را ندیده.

---

## ۵) طرح اصلاح پیشنهادی

### ۵.۱ — اصلاح اصلی (یک خط منطقی، کم‌ریسک): هم‌ترازسازی ماتریس سرور
در `api/crm.php` → `sync_allowed_keys_for_role()`:
- افزودن `'ptf_crm_personal_cheques'` به لیست `$crm` (خط ۳۶۲) → sales و buyer (از طریق merge خط ۳۶۹) پوشش می‌یابند؛
- افزودن همان کلید به `$collector` (خط ۳۶۴).

مبنای درستی: «کلید مشترکِ سینک‌شونده» از v34.8.29 عمداً برای همهٔ نقش‌ها طراحی شده (کلاینت + رجیستری فرمانی + سند T5-2)؛ سرور عقب است نه کلاینت جلو.

### ۵.۲ — سخت‌سازی‌های مکمل (پیشنهادی، جدای ۵.۱)
1. در `ptfBFlushQueue`: برای bucketهای `forbidden/rejected/skipped`، یک ردیف `ptf_sync_last_error` با TTL ۱۵ دقیقه بنویسد تا بنر «چرا» را نشان دهد (هم‌تراز با مسیر legacy).
2. در `ptfEntitySaveCollection`: پس از اتمام Upsert/Deleteها، `opts.cb` را با جمع‌بندی `{state:'acked'|'rejected', ...}` صدا بزند (قرارداد مهاجرت cheques.js ترمیم می‌شود).
3. تستر گیت جدید: مقایسهٔ ماتریس نقشِ کلیدهای SYNC بین `crm/sync.js` و `sync_allowed_keys_for_role` (پر کردن کوربُott A11) — الگو: تستر A11 موجود.

### ۵.۳ — کار موقت کاربر تا استقرار اصلاح
داده از مسیر فرمانی رسیده؛ فقط پرچم stuck مانده. با یک اجرای کنسولی (`localStorage['ptf_sync_dirty']` حذف کلید `ptf_crm_personal_cheques` + حذف ورودش از صف IDB در sync-diagnostics) نوار خاموش می‌شود — ولی پس از هر dirtyشدن جدید دوباره برمی‌گردد؛ اصلاح ۵.۱ تنها راه قطعی است.

### ۵.۴ — راستی‌آزمایی پس از اصلاح
1. با کاربر نقش sales (یا buyer/collector) در استیجینگ: یک چک شخصی ثبت/ویرایش کن → حداکثر چند ثانیه بعد نشانگر 🟢 و نوار محو.
2. `data_push` تستر: پاسخ سرور نباید `forbidden` شامل `ptf_crm_personal_cheques` باشد.
3. تستر ماتریس جدید (۵.۲-۳) در run-ci-gate سبز.

---

## ۶) چرا «نسخهٔ ۳۵» موضوع را عوض نمی‌کند؟
بنر فقط وضعیت `state.dirty` دستگاه است و ربطی به شمارهٔ نسخه ندارد؛ چون ناهماهنگی ماتریس در **هر سه درخت امروز** (از جمله همان که ۱۷:۱۲ UTC روی استیجینگ نشسته) یکسان وجود دارد، هیچ استقرارِ امروزِ مانده‌ای (حتی با رفع کامل مشکل FTP) این نوار را سبز نمی‌کند — اصلاح کد لازم است.

---

---

## ۷) بررسی تکمیلی (نشست دوم — ۲۰۲۶-۰۸-۲۸) و رفع اعمال‌شده (v34.8.36)

بررسی عمیق‌تر سه مسیر دیگر را هم تأیید/کشف کرد:

- **تأیید مسیر ۲.۳ با دیفرنس دقیق:** مقایسهٔ ست‌به‌ست ماتریس کلاینت/سرور نشان داد
  **فقط همین یک کلید** در سه نقش (sales/buyer/collector) ناهماهنگ است — هیچ انحراف
  دیگری بین دو ماتریس نیست (admin-tier و accountant کاملاً برابرند).
- **F-D — صف IDB هم در حلقه بود:** کلید dirty از مسیر `failDirty` وارد صف فاز B
  (`ptf_b_queue`) هم شده بود؛ پاک‌سازی phantom فقط برای «مقدارِ غایب» است، پس ورود
  صفِ forbidden تا ابد push و re-dirty می‌کرد — حتی اگر entity-command بعداً موفق
  می‌شد. یعنی رفع صرفِ ماتریس RBAC بدون اصلاح فاز B، نوار را فقط برای داده‌های جدید
  سالم می‌کرد و پرچم‌های stuck قدیمی می‌ماندند.
- **F-E — tester531 پاسِ گمراه‌کننده:** چکِ «ماتریس کامل‌نقش‌ها» فقط انتهای آرایهٔ
  `$accountant` را می‌سنجید (همان الگوی F-1 ممیزی T0) — به همین دلیل شکاف از گیت رد
  شده بود.

### رفع (v34.8.36 — همین برنچ)
1. `api/crm.php`: افزودن کلید به `$crm` و `$collector` (هم‌ترازی کامل سه لایه).
2. `crm/sync.js` + `crm/client-server.js` (FORBIDDEN-DROP): کلید forbidden → حذف از
   صف IDB + پاک‌سازی dirty + ثبت `ptf_sync_last_error` + نشانگر 🟠 — پایان بن‌بست
   ابدی و پایان «نوار بی‌علت».
3. `crm/sales-domain-v2.js` (ROUTER-CB): `opts.cb` روتر حالا یک‌بار با جمع‌بندی
   ACK صدا زده می‌شود → پاک‌سازی legacy مهاجرت cheques.js عملاً اجرا می‌شود.
4. `tester537` (ثبت‌شده در run-ci-gate): برابری ست‌به‌ست ماتریس RBAC کلاینت↔سرور
   برای هر ۵ سطح نقش + دو تست رفتاری vm.

**نتیجهٔ گیت:** `run-ci-gate` ← **153 PASS / 0 FAIL**، `arch-guard` ← PASS،
نسخه ← `v34.8.36` در هر ۷ نقطهٔ رسمی. جزئیات: `RELEASE-NOTES-v34.8.36.md`.

*تهیه‌شده توسط Arena Agent — نشست `arena/01a04968-pishtaj-ir` — ۲۰۲۶-۰۸-۲۸ — به‌روزشده با بررسی تکمیلی و رفع v34.8.36 (بند ۷)*
