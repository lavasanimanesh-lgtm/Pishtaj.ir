# ارزیابی وضعیت CRM — مین در برابر استیجینگ

- **تاریخ:** ۱۴۰۵/۰۶/۳ (۲۰۲۶-۰۸-۲۵)
- **نسخهٔ رسمی هر دو محیط:** `v34.8.5` (طبق VERSION.json و sw.js هر دو)
- **مبنای بررسی:** کد `main` در `593ab72` (PR #80) و برنچ استیجینگ `arena/01a0396d-pishtaj-ir` در `0ddb4ad` (۷ کامیت، ۲۵ اوت) + بررسی زندهٔ pishtaj.ir و staging.pishtaj.ir
- **نوع کار:** فقط ارزیابی — هیچ کدی تغییر نکرد.

---

## ۰) حکم یک‌خطی

استیجینگ ۷ کامیت جدید (حدود **۱٬۹۴۰ خط افزوده / ۳۲۵ خط حذف در ۳۰ فایل**) دارد که زیر همان برچسب `v34.8.5` با پارامترهای cache-bust (f01…f08) استقرار یافته. این تغییرات **پیشگیری و تشخیص** را بهتر می‌کنند، اما **هیچ دادهٔ خراب تاریخی‌ای را ترمیم نمی‌کنند** و هنوز **در مین ادغام نشده‌اند**؛ بنابراین اگر مشکل اصلی شما اعداد/رکوردهای موجود است، احساس شما درست است — روی استیجینگ هم «مشکل» حل نشده، فقط «نقشهٔ مشکلات» ساخته شده.

---

## ۱) وضعیت زندهٔ دو محیط

| مورد | مین (pishtaj.ir) | استیجینگ (staging.pishtaj.ir) |
|---|---|---|
| آخرین استقرار | ۲۰۲۶-۰۸-۲۴ ۲۳:۰۶ UTC (پس از merge PR #80) | ۲۰۲۶-۰۸-۲۵ ۲۰:۴۷ UTC (کامیت `0ddb4ad`) |
| نسخهٔ نمایش‌داده‌شده در داشبورد | v34.8.5 | v34.8.5 |
| Service Worker | `CACHE=ptf-crm-v34.8.5` بدون پارامتر فاز | همان CACHE ولی با queryهای `&f0x=20260825` برای ۸ فایل تغییرکرده |
| بنر استیجینگ | — | برنچ: `arena/01a0396d-pishtaj-ir` ✓ |
| API بدون session | خطای auth برمی‌گرداند (درست) | خطای auth برمی‌گرداند (درست) |
| آخرین دیپلوی پروडاکشن | موفق | — |

هر دو محیط سالم بالا هستند و کد سرو‌شده با برنچ مربوط هم‌خوان است (برای استیجینگ، فایل `shareholders.js` زنده با معماری فرمان سروری جدید تأیید شد).

---

## ۲) ایجنت جدید چه تغییراتی داده؟ (برنچ `arena/01a0396d-pishtaj-ir`)

هفت کامیت در قالب «فازهای F1 تا F7»:

### F1 — `fix(sync): harden phase zero and one finance transport` (2beb9b4)
- `sync.js`/`client-server.js`: Phase B مالک واحد انتقال می‌شود؛ موتور push دوبله حذف؛ ACK فقط نسل همان snapshot را پاک می‌کند (`ptfSyncAcknowledgeKeys`)؛ watermark سرور فقط اگر جلوتر باشد اعمال می‌شود؛ snapshot تشخیصی فقط‌خواندنی (`ptfSyncDiagnosticsSnapshot`).
- `api/crm.php`: سخت‌سازی merge سمت سرور — مبنا snapshot سرور است (omission = حذف نیست)، evidence/پیوست‌ها append-only، تطبیق هویت فیزیکی (`_opexRowId`) قبل از fallback مهاجرت.

### F2 — `feat(finance): add server-owned salary and draw commands` (1c1211f)
- چهار فرمان سروری جدید: `register_shareholder_salary` (ثبت صریح حقوق یک سهامدار/ماه؛ اگر موجود باشد no-op برمی‌گرداند)، `register_shareholder_draw`، `register_chair_in`، `register_chair_out`.
- `shareholders.js` و `treasury.js`: **حذف کامل مسیر نوشتن محلی** برای برداشت، پرداخت حقوق، تزریق/تسویهٔ رییس — همه از این پس فقط از طریق فرمان سروری با idempotencyKey.
- UI: دکمهٔ گروهی «ثبت حقوق ماه» حذف شد (تابع قدیمی فقط alert می‌دهد)؛ به‌جایش دکمهٔ per-card «ثبت حقوق» (تعهدی) آمده و «پرداخت واقعی» = ثبت draw با `salaryMonth`.

### F3 — `fix(finance): separate salary accrual from cash flow` (2d3c139)
- `opex.js`/`fiscal.js`: تفکیک OPEX نقدی از حقوق تعهدی (`totalCash`/`totalSalary`). در داشبورد سال مالی، حقوق ثبت‌شده‌ای که draw ندارد دیگر جزء «خروجی‌های نقدی» نیست و KPI جدا «حقوق تعهدی (بدون خروج نقدی)» نمایش داده می‌شود. گزارش رسمی سال مالی هم همین تفکیک را می‌گیرد.

### F4 — `fix(crm): stabilize finance views and polling` (730c445)
- `bridge.js`/`fiscal.js`/`scoring.js`: رفرش پس‌زمینه دیگر فرم باز/فیلد در حال ویرایش را reset نمی‌کند؛ رندر فقط برای تب فعال؛ داشبورد سال مالی (`fiscalBox`) پیش‌فرض `display:none` و فقط در تب خودش نمایان می‌شود (جلوگیری از افشای نمای محرمانه در تب‌های دیگر هاب مالی).

### F5 — `feat(finance): add read-only repair manifest` (0be6d10)
- endpoint جدید `finance_repair_plan` (فقط نقش admin/chairman): فهرست تشخیصیِ موارد زیر با severity و evidence —
  - `salary_duplicate_active` (حقوق تکراری فعال برای یک سهامدار/ماه)
  - `salary_opex_missing` / `salary_opex_duplicate` (جفت ناقص یا تکراری هزینهٔ حقوق)
  - `salary_profile_amount_mismatch` (مبلغ رکورد ≠ حقوق پروفایل)
  - `salary_opex_orphan` / `salary_opex_unlinked`
  - `draw_month_mismatch` (برداشت در ماه اشتباه، در برابر expectedDraws)
  - `chair_in_missing` (تزریق رییس gم از دست رفته — «بدون سند خارجی قابل بازیابی نیست»)
  - `payable_delete_audit` (سابقهٔ حذف بدهی/بستانکاری)
- **نکتهٔ کلیدی:** همهٔ اقلام `readOnly: true` هستند؛ `automaticVoid/automaticCreate/automaticRestore/automaticMove` همگی `false`. یعنی manifest فقط «گزارش» است، نه ابزار ترمیم.

### F6 — `fix(finance): make repair diagnostics usable without console` (619ad93)
- دیالوگ UI برای manifest در ابزار کیفیت داده (فقط admin/chairman) + دکمهٔ «بررسی فرمان نامشخص» (command_status) با خطای ساخت‌یافته (F7).

### F7 — `fix(finance): return structured status errors` (0ddb4ad)
- خطاهای مسیر command_status ساخت‌یافته برمی‌گردند (`command_status_recovery_failed` با detail) به‌جای fatal خام.

**کنترل‌های مستقل من روی این برنچ:**
- اجرای مستقیم گیت CI (`_tools/uat/run-ci-gate.js`): **۱۲۷ PASS / ۰ FAIL** ✓
- همهٔ توابع سراسری جدید (`ptfSyncHoldCommandKeys`, `ptfBEnqueueKeys`, `finHubApply`, `ptfIsShareholderSalaryOpex`, …) تعریف‌شده‌اند و ارجاع بی‌تعریفی نداریم ✓
- قرارداد precache سرویس‌ورکر با queryهای فازِ index.html دقیقاً هم‌خوان است ✓
- تسترهای موجود (298، 396، 403، 442، 443، 483، 498، 502، 505) به‌روزرسانی شده‌اند و ۶ تستر جدید (506–511) اضافه شده‌اند.

---

## ۳) آیا مشکل حل شده؟ — ارزیابی صریح

### چرا احساس شما درست است

1. **در مین هیچ چیزی عوض نشده.** این ۷ کامیت فقط روی استیجینگ است؛ نه PR باز دارد، نه در main ادغام شده، نه نسخه‌ای گرفته. کاربر پروداکشن دقیقاً همان رفتار دیروز را می‌بیند.
2. **هیچ داده‌ای ترمیم نمی‌شود.** اگر مشکل واقعی، رکوردهای موجود است (حقوق تکراری، جفتِ ناقص هزینه/مطالبه، برداشت در ماه اشتباه، تزریق رییس گم‌شده)، ابزار جدید فقط آن‌ها را **فهرست** می‌کند و برای هر مورد صریحاً می‌گوید تصمیم انسانی/سند خارجی لازم است (`requires_canonical_selection`, `unrecoverable_without_external_evidence`, …). یعنی روی خود استیجینگ هم «عدد غلط» هنوز غلط است — فقط حالا می‌دانیم کجا.
3. **روتین ترمیم هنوز تعریف نشده.** مسیر عملی پس از دیدن manifest (چه کسی کدام canonical را انتخاب کند، با چه UIای void/restore صریح انجام شود، بازگردانی chair_in با سند خارجی چطور ثبت شود) در این برنچ ساخته نشده.

### آنچه واقعاً بهتر شده (منصفانه)

- **بستن ریشهٔ تکرار در آینده (F2):** در مین، «پرداخت حقوق/برداشت/تزریق رییس» هنوز نوشتِ آرایهٔ محلی مرورگر است (`addTx` → localStorage → data_push عمومی) — دقیقاً همان کلاس writer هم‌زمان مرورگر/سرور که RCA نسخهٔ v34.8.5 ریشهٔ تکرارها اعلام کرده بود. این برنچ آن مسیرها را کامل می‌بندد.
- **صحت منطق نقدی (F3):** در مین، حقوقِ تعهدیِ پرداخت‌نشده جزء خروجی نقدی سال شمرده می‌شود؛ «سود نقدی دوره» و «موجودی نقد پایان سال» را کم‌نمایش می‌دهد. این برنچ آن را درست تفکیک می‌کند.
- **پایداری UI مالی (F4)** و تشخیص‌پذیری (F5–F7) واقعی و بلامنازع است.
- کیفیت مهندسی خوب است (گیت CI سبز — من خودم اجرا کردم؛ هویت‌ها، idempotency و قفل دورهٔ مالی رعایت شده).

### ریسک‌ها و نقاط ضعف این برنچ

- **نسخه‌سازی نقض شده:** هفت تغییر رفتاری زیر همان `v34.8.5` با query فاز؛ بدون VERSION bump، بدون RELEASE-NOTES جدید. قرارداد خود ریپو («RELEASE باید با VERSION.json و query همهٔ scriptها یکی باشد») عملاً خم شده. برای پشتیبانی مبهم است: «کدام v34.8.5؟»
- `ptfShareApplySalary` به alert تبدیل شده — اگر کاربری عادت به دکمهٔ گروهی قبلی داشته باشد، تجربهٔ عقب‌رفتگی حس می‌کند (طراحی عمدی، ولی باید در یادداشت انتشار توضیح داده شود).
- نحوهٔ انتقال به پروداکشن (merge → deploy دستی) هنوز انجام نشده؛ اگر زیر همین نسخه منتشر شود، تشخیص نسخهٔ نصب‌شدهٔ کاربران سخت می‌شود.

---

## ۴) مشکلاتی که «مین» الان دارد

1. **نوشتن پول از مرورگر (بحرانی — عود ریشهٔ باگ):** در v34.8.5 تولیدی، `ptfSharePaySalary` و `ptfShareDraw` و تزریق/تسویهٔ رییس همچنان `addTx` محلی + sync عمومی‌اند. تا وقتی این برنچ merge نشود، امکان تکرار دوبارهٔ رکوردهای تکراری/ناسازگار sharetx و opex در پروداکشن باقی است.
2. **اختلاط تعهدی/نقدی در سال مالی:** خروجی‌های سال مالی شامل حقوق تعهدی پرداخت‌نشده است → «سود نقدی دوره»، «قابل تقسیم» و «موجودی نقد پایان سال» کمتر از واقع دیده می‌شود هر ماه که حقوق ثبت شده ولی draw نشده.
3. **بی‌ابزاری برای دادهٔ خراب تاریخی:** روی مین هیچ manifest/تشخیصی برای duplicateها و orphanها نیست؛ reconcile فعلی فقط حالت «OPEX هست ولی claim مفقود» را خودکار repair می‌کند. duplicate فعال نیاز به تصمیم و دستکاری دستی دارد و گزارشی هم برای دیدنشان وجود ندارد.
4. **PRهای باز مانده و منقضی:** PR #75 (v34.7.98) و PR #76 (revert آن) از ۲۳ اوت باز مانده‌اند و با مسیر v34.8.x منسوخ شده‌اند — باید بسته شوند تا سردرگمی نسخه‌ای ایجاد نکنند.
5. **دین تست:** tester403 و tester415 همچنان known-open (طبق TEST-DEBT-TRIAGE-2026-08-17) — بدهی تست، نه رگرسیون محصول.
6. **نکتهٔ جزئی قابل بررسی:** در fetch زندهٔ من، ویجت نرخ ارز داشبورد مین در «در حال دریافت…» ماند در حالی که استیجینگ نرخ‌ها را نشان داد؛ به‌احتمال زیاد مربوط به زمان‌بندی رندر بود نه باگ، ولی یک بازبینی دستی می‌ارزد.

---

## ۵) پیشنهاد مسیر بعدی (به ترتیب)

1. **تصمیم دربارهٔ برنچ استیجینگ:** محتوایش (F1–F7) درست و تست‌شده است؛ با یک **bump نسخهٔ واقعی (مثلاً v34.8.6/v34.9.0) + RELEASE-NOTES** merge شود، نه زیر v34.8.5. پس از merge، deploy دستی پروداکشن با ref=main.
2. **اجرای manifest روی دادهٔ واقعی پروداکشن** (بعد از استقرار): خروجی `finance_repair_plan` برای ماه‌های مشکل‌دار را بگیریم تا فهرست دقیق رکوردهای خراب مشخص شود.
3. **جلسهٔ تصمیم داده:** برای هر قلم manifest (canonical کدام است، کدام void شود، chair_in گم‌شده با چه سندی بازگردانده شود) تأیید کارفرما گرفته شود — سپس ابزار ترمیم صریح و auditدار ساخته شود (برنچ فعلی عمداً این را ننوشته).
4. بستن PR #75 و #76.

---

## ۶) پیوست — نگاشت فازها به فایل‌ها

| فاز | فایل‌های اصلی | query استیجینگ |
|---|---|---|
| F1 | sync.js, client-server.js, api/crm.php | `?v=34.8.5&f01=20260825` |
| F2 | shareholders.js, treasury.js, api/sales-domain.php | `&f02=20260825` |
| F3 | opex.js, fiscal.js | `&f03/f06` |
| F4 | bridge.js, fiscal.js, scoring.js, index.html | `&f04=20260825` |
| F5–F7 | api/sales-domain.php, sales-domain-v2.js, data-quality.js | `&f05…f08` |
