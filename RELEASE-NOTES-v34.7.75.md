# RELEASE NOTES — v34.7.75 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## ارجاع سند برد به حسابدار + افزودن سند فاکتور/مودیان پس از ثبت

### درخواست

۱. هنگام ارجاع فاکتور به حسابدار از پرونده، **سند برد (سند قطعی ابلاغ سفارش)** هم باید در
   دسترس حسابدار باشد تا بر اساس آن فاکتور صادر کند.
۲. پس از ثبت فاکتور، امکان **افزودن سند فاکتور حسابداری یا سند سامانه مودیان** در زمان‌های
   بعدی وجود داشته باشد.

### ریشهٔ وضعیت قبلی

- مسیر ارجاع فاکتور از پرونده (`sfInvoiceRefCommit` در `crm/salesfiles.js`) مقدار
  `o.invRef.fromFile` (شناسهٔ پرونده) و snapshot سند برد را می‌ساخت، اما پنل فاکتورها
  (رجیستری رسمی v35 در `crm/official-invoice-v2.js`) هیچ دکمه‌ای برای مشاهدهٔ سند برد نداشت؛
  فقط نسخهٔ قدیمی `rbac.js` آن را نشان می‌داد.
- پس از ثبت قطعی، فقط «جایگزینی» سند موجود ممکن بود (`replace_invoice_attachment` که
  `attachmentId` الزامی دارد)؛ راهی برای **افزودن** سند جدید (مثلاً وقتی ابتدا فقط سند
  حسابداری ثبت شده و بعداً سند مودیان آمده) وجود نداشت.

### تغییرات

#### ۱. سند برد در پنل فاکتورها (`crm/official-invoice-v2.js`)

- در کارت هر پیش‌فاکتور ارجاع‌شده، دکمهٔ **«🏆 سند برد (PDF)»** اضافه شد که وقتی ارجاع از
  پرونده آمده باشد (`o.invRef.fromFile`) نمایش داده می‌شود و `sfAwardPrint(پرونده, پیشنهاد)`
  را صدا می‌زند — نسخهٔ تغییرناپذیر لحظهٔ ابلاغ سفارش.
- متن اعلان ارجاع به حسابدار (`crm/salesfiles.js`) به‌روز شد و محل مشاهدهٔ سند برد
  (دکمهٔ «🏆 سند برد» در پنل فاکتورها) را اعلام می‌کند.

#### ۲. افزودن سند فاکتور/مودیان پس از ثبت

- **سرور** (`api/sales-domain.php`): فرمان جدید `invoice_attachment_add` —
  - گارد نقش `SD_FIN_ROLES` (ادمین/رییس/مدیرعامل/مدیر بازرگانی/حسابدار)؛
  - گارد فاکتور فعال + گارد دوره مالی قفل؛
  - اعتبارسنجی فایل (`sd_file_ok`) و allowlist دستهٔ سند
    (`accounting_official_invoice` / `modian_tax_invoice` / `supporting_document`)؛
  - append به `files` فاکتور + آینهٔ `ptf_crm_fin_attachments` + رکورد `correction` برای
    رد ممیزی (reason اختیاری — عمل افزودنی است، نه اصلاحی).
- **کلاینت** (`crm/official-invoice-v2.js`):
  - تابع `ptfOfficialAttachmentAdd(invId)` — دیالوگ با انتخاب نوع سند (فاکتور حسابداری /
    سند مودیان)، آپلود، OCR و تأیید انسانی، سپس فرمان `invoice_attachment_add`؛
  - دکمهٔ **«➕ افزودن سند جدید (فاکتور/مودیان)»** در دیالوگ «📎 اسناد» هر فاکتور رسمی
    (فقط برای نقش‌های مجاز و فاکتور فعال).

### نکته

- سند برد از snapshot تغییرناپذیر پرونده چاپ/دانلود می‌شود؛ حتی اگر پیشنهاد زنده بعداً
  ویرایش یا حذف شود، مبنای فاکتور دست‌نخورده می‌ماند.
- افزودن سند پس از ثبت، نسخه‌های قبلی را حذف نمی‌کند (append) و در تاریخچه نگهداری می‌شود.

### فایل‌ها

- `api/sales-domain.php` — فرمان `invoice_attachment_add`.
- `crm/official-invoice-v2.js` — دکمهٔ سند برد + `ptfOfficialAttachmentAdd` + دکمهٔ افزودن در دیالوگ اسناد.
- `crm/salesfiles.js` — به‌روزرسانی متن اعلان ارجاع.
- `_tools/uat/tester477-v34.7.75-invoice-award-ref-and-attach-later.js` — تستر جدید (ثبت در گیت CI).

### اعتبارسنجی

- `node _tools/arch/arch-guard.js` → `=== ARCH GUARD: PASS ===`
- `node _tools/uat/run-ci-gate.js` → `93 PASS / 0 FAIL`
