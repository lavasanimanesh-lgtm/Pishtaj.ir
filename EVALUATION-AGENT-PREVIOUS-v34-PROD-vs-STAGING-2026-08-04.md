# ارزیابی کار ایجنت قبلی — پروداکشن در برابر استیجینگ (v34.0.x)

**تاریخ ارزیابی:** ۱۴۰۵/۰۵/۱۳ (2026-08-04)
**دامنه:** CRM پیشرو تجهیز فرتاک — نسخهٔ deployed در پروداکشن (pishtaj.ir) در برابر استیجینگ (staging.pishtaj.ir)
**نتیجهٔ کلی:** ادعای «پروداکشن از استیجینگ عقب‌تر است» **به‌صورت تحت‌اللفظی درست است** (تفاوت یک کامیتِ نسخه)، اما **کدِ هر سه رفعِ موردنظر شما از قبل در پروداکشن حاضر است**؛ مشکلات مشاهده‌شده عمدتاً ریشه در **داده/مهاجرت/محیط** دارند، نه «نبودِ کد». جزئیات و شواهد در ادامه.

---

## ۱. خلاصهٔ مدیریتی

| محیط | نسخهٔ فعال (زنده) | منبع | وضعیت |
|---|---|---|---|
| **پروداکشن** `pishtaj.ir/crm` | **v34.0.0-alpha** | main (کامیت `04ea070`)، استقرار امروز ۰۹:۰۲ UTC — تگ `v2026.08.04-0902` | ✅ استقرار موفق |
| **استیجینگ** `staging.pishtaj.ir/crm` | **v34.0.1-alpha** | برنچ `arena/019fc8c9-pishtaj-ir` (کامیت `5ea39cf`) | ✅ استقرار خودکار آخرین پوش |

**تفاوت کد بین این دو محیط، فقط یک فایل است:** `crm/index.html` — یعنی فقط تغییر رشتهٔ نسخه از `v34.0.0-alpha` به `v34.0.1-alpha` (برای cache-busting کش PWA) + پارامترهای `?v=` اسکریپت‌ها. **هیچ تفاوت کدی در `petty.js`، `shareholders.js`، `storage.js`، `date-kit.js`، `finance-core.js` و… وجود ندارد** (تأیید با `git diff` و هم‌چنین با دریافت فایل‌های زنده از هر دو سرور).

نتیجهٔ مهم: **هر سه مشکلی که در پروداکشن می‌بینید، فیکسِ کدیِ آن‌ها در پروداکشن مستقر شده است** (کامیت‌های F4-6، F4-10، F4-13، F4-7، F4-9/F4-11 همگی داخل main هستند). بنابراین ریشهٔ هر سه مشکل باید در این سه دسته جست‌وجو شود:

1. **مهاجرت داده‌ای که فقط روی دادهٔ استیجینگ اجرا شده** (حقوق سهامدار → opex)؛
2. **رفتار گزارش تنخواه که ذاتاً «هزینه + پرداخت» را دو ردیف نشان می‌دهد** و با دادهٔ واقعیِ پروداکشن به‌صورت «ردیف تکراری» دیده می‌شود؛
3. **مشکل ضمایم PDF/HEIC که توسط خود ایجنت رسماً «هنوز باز» اعلام شده** (F4-13) و به محیط هاست (نبود Imagick) + وضعیت ردیف‌های MySQL وابسته است.

---

## ۲. متدولوژی و شواهد

برای این ارزیابی، موارد زیر بررسی شد:

- **تاریخچهٔ گیت:** تمام ۳۰۵ کامیت برنچ `arena/019fc8c9-pishtaj-ir` جلوتر از main، به‌همراه کامیت‌های مرج‌شده در main.
- **PRهای گیت‌هاب:** ۱۸ PR (PR #18 آخرین مرج به main است).
- **لاگ Actionهای استقرار:** runهای «Deploy to Production» و «Deploy to Staging».
- **تگ‌های نسخه:** `v2026.08.04-0902` (امروز) و تگ‌های قبلی.
- **سرورهای زنده:** دریافت `index.html`، `date-kit.js`، `shareholders.js`، `petty.js`، `api/crm.php` از هر دو محیط و مقایسه با درخت main.

**شواهد کلیدی:**

| # | شاهد | نتیجه |
|---|---|---|
| 1 | `git diff main 4e4d754` → **۰ فایل** متفاوت | درخت main == برنچ در کامیت `4e4d754` (v34.0.0-alpha F4-9+F4-11) |
| 2 | `git diff main origin/arena/019fc8c9-pishtaj-ir --stat` → فقط `crm/index.html` (۸۷+/۸۷-) | استیجینگ = main + فقط bump نسخه به v34.0.1-alpha |
| 3 | `git show v2026.08.04-0902` → اشاره به `04ea070` | استقرار پروداکشن امروز از main بوده است |
| 4 | دریافت زنده: پروداکشن «نسخه فعال: v34.0.0-alpha»، استیجینگ «نسخه فعال: v34.0.1-alpha» + بنر «برنچ: arena/019fc8c9-pishtaj-ir» روی استیجینگ | هر دو سرور با همان نسخه‌های بالا در حال سرویس‌دهی‌اند |
| 5 | دریافت زنده از پروداکشن: `date-kit.js` موجود و یکسان با استیجینگ؛ `shareholders.js` حاوی فیکس F4-7؛ `petty.js` حاوی کد F4-5 | فایل‌های جدید v34 (فایل‌هایی که در v33.22.x وجود نداشتند) روی پروداکشن **مستقر شده‌اند** |
| 6 | `api/crm.php` پروداکشن → پاسخ JSON معتبر (`{"ok":false,"error":"Invalid action"}`) | لایهٔ API روی پروداکشن فعال است |

---

## ۳. تیم‌لاین دقیق (چه اتفاقی افتاد)

| زمان (UTC, 2026-08-03/04) | رویداد |
|---|---|
| 08-03 ~۱۸:۵۴ | PR #18 (عنوان: v33.22.6) از برنچ `arena/019fc8c9` ساخته/به‌روزرسانی می‌شود |
| 08-03 ۱۹:۴۱ → 08-04 ۰۸:۲۲ | ایجنت روی همان برنچ **۲۲ کامیت دیگر** می‌زند: `v33.22.7` (P0 مالی) ← `v33.23.x` (refactor) ← `v34.0.0-alpha` (فاز ۴: بازنویسی بازهٔ تنخواه با DateKit + F4-2…F4-13) — همگی با push خودکار روی استیجینگ مستقر می‌شوند |
| 08-04 ۰۷:۵۷ | آخرین کامیتِ قبل از مرج: `4e4d754` (F4-9+F4-11) |
| 08-04 ۰۸:۲۰ | مرج (squash) PR #18 به main → کامیت `04ea070` — درخت main دقیقاً برابر `4e4d754` است |
| 08-04 ۰۸:۲۲ | ایجنت یک کامیت دیگر روی برنچ می‌زند: `5ea39cf` = «v34.0.1-alpha — VER bump برای PWA cache busting» → روی استیجینگ مستقر می‌شود ولی **به main نمی‌رسد** |
| 08-04 ۰۹:۰۲ | استقرار دستی پروداکشن از main → تگ `v2026.08.04-0902` → پروداکشن v34.0.0-alpha |

**نتیجه:** فاصلهٔ پروداکشن تا استیجینگ = **دقیقاً یک کامیت** (`5ea39cf`، فقط تغییر رشتهٔ نسخه). تمام اصلاحات عملکردیِ فاز v34 در main هستند.

---

## ۴. ارزیابی کار ایجنت قبلی

### ۴.۱ نقاط قوت (واقعاً خوب کار کرده)

1. **ریشه‌یابی درست هر سه ناحیه‌ای که شما گزارش می‌دهید:**
   - گزارش دوره‌ای تنخواه ← F4-6 (دو باگ بحرانی: دکمهٔ «فیش‌های پیوست» که ReferenceError می‌داد + حذف رکوردهایی با تاریخ میلادی/ناقص) و F4-10 (مقایسهٔ رشته‌ای تاریخ `1405/5/1` در برابر `1405/04/16` → مقایسهٔ عددی YYYYMMDD) و F4-13 (حذف رکوردهای میلادیِ بعد از مهاجرت MySQL).
   - حقوق سهامداران ← F4-7 (ثبت نشدن حقوق سهامدار در opex — دقیقاً همان «محاسبه نشدن در هزینه‌های جاری») + اسکریپت مهاجرت.
   - ضمایم ← F4-4 (فشرده‌سازی + تگ دوگانه در PDF تلفیقی)، F4-9 (چیدمان ۲-ستونه)، F4-11 (لیست سفید فرمت)، F4-13 (رفع بحرانی ضمایم PDF/HEIC).
2. **هر کامیت با پیام فارسی شفاف، مستند و با آزمایش روی استیجینگ** (۲۲ کامیت پشت‌سرهم با push خودکار به استیجینگ و ثبت بک‌لاگ بازخوردها — کامیت `c65d7d0`).
3. **کد تمیزتر شد:** refactor فاز ۳/۴ (حذف `datex.js`، لایهٔ واحد `DateKit`، `finance-core.js`/`finance-helpers.js`، حذف نقاط تکراری).

### ۴.۲ کاستی‌ها و ریسک‌های فرایندی (جایی که کار ناقص ماند)

1. **🔴 یک‌جا اسکواش‌کردن یک فاز کامل در PR با عنوان قدیمی:** کل فاز v34 (۲۲ کامیت) داخل همان PR #18 با عنوان «v33.22.6» اسکواش شد. عنوان/توضیح PR با محتوای واقعی (v34.0.0-alpha) هم‌خوانی نداشت؛ بازبینیِ «چه چیزی عوض شده» عملاً ناممکن شد و استقرار پروداکشن، یک تغییراتِ بزرگِ بی‌سروصدا را با خود برد.
2. **🔴 کامیت آخر (bump نسخه) بعد از مرج روی برنچ ماند:** کامیت `5ea39cf` (که خودِ ایجنت نوشت «برای PWA cache busting») هرگز به main نرسید؛ در نتیجه استیجینگ از لحاظ نسخه جلوتر ماند و بافرهای کش مرورگرِ کاربران پروداکشن با `?v=34.0.0-alpha` سرو شد. این دقیقاً همان «یک قدم عقب‌تر بودن پروداکشن» است.
3. **🔴 مهاجرت داده‌ای به‌صورت «اسکریپت دستی کنسول» رها شد:** اسکریپت `crm/_tools/migrate_shareholder_opex.js` (F4-7) فقط با copy-paste در کنسول مرورگر اجرا می‌شود، از هر دو استقرار (پروداکشن/استیجینگ) اکسکلود است (`_tools/**`) و **هیچ مدرکی از اجرای آن روی دادهٔ پروداکشن وجود ندارد**. کدِ F4-7 فقط از این به بعد جلوی ایجادِ «حقوق بدون opex» را می‌گیرد؛ رکوردهای تاریخیِ ازقبل‌خراب‌شده بدون مهاجرت، خراب می‌مانند.
4. **🟠 مسئلهٔ ضمایم PDF/HEIC رسماً «هنوز باز» اعلام شد:** پیام کامیت `9cdaad9` (F4-13) خودش می‌گوید «(هنوز باز)»؛ و بک‌لاگ `c65d7d0` ثبت کرده: «سرور Imagick ندارد (هاست اشتراکی)». راه‌حلِ اعمال‌شده = **محدودکردن فرمت‌های مجازِ آپلود جدید** (F4-11)، نه رفع نمایشِ ضمایمِ قدیمیِ PDF/HEIC. یعنی مشکلِ «ضمایم نمایش داده نمی‌شوند» برای اسنادِ موجودِ PDF/HEIC کماکان باز است.
5. **🟠 گزارش تنخواه «ردیف تکراری» اصلاح نشد:** مدل گزارش‌گیری ذاتاً هر هزینه را در دو ردیف نشان می‌دهد (ردیف «هزینه» از `ptf_crm_petty` + ردیف «تسویه از حساب»/«پرداخت مستقیم» از `ptf_crm_petty_tx`)، در حالی که جمع فقط یک‌بار شمرده می‌شود. ایجنت هیچ‌وقت به این dedup نپرداخت (فیکس F4-6 حتی فیلتر را بازتر کرد و ردیف‌های بیشتری نمایش داده می‌شوند).

### ۴.۳ امتیازدهی جمع‌بندی

| بُعد | امتیاز (از ۵) | توضیح |
|---|---|---|
| کیفیت فنیِ رفع‌ها | ۴ | ریشه‌یابی عمیق و دقیق؛ فیکس‌های واقعی |
| مستندسازی | ۴٫۵ | پیام کامیت‌ها و بک‌لاگ‌های فارسی عالی |
| انضباط Release/PR | ۱٫۵ | عنوان گمراه‌کنندهٔ PR، اسکواش یک‌جا، کامیتِ نسخه خارج از main |
| تحویل‌پذیری (مایگریشن/استقرار) | ۲ | مهاجرت داده دستی و اجرانشده روی پروداکشن؛ مسئلهٔ بازِ ضمایم |
| آزمون در محیط واقعی | ۲٫۵ | تست روی استیجینگ خوب، ولی بدون راستی‌آزمایی روی دادهٔ واقعی پروداکشن |

---

## ۵. تحلیل ریشه‌ای سه مشکل گزارش‌شده

### ۵.۱ ضمایم در پروداکشن نمایش داده نمی‌شوند (در استیجینگ سالم‌اند)

**وضعیت کد:** فیکس‌های F4-4/F4-9/F4-11/F4-13 + گارد تازگی MySQL (v33.22.3، تابع `ptf_db_read_fresh` در `api/db-lib.php`) همگی در پروداکشن مستقرند (تأیید زنده).

**ریشه‌های احتمالی (به ترتیب احتمال):**

1. **تبدیل PDF/HEIC به JPEG نیازمند Imagick سمت سرور است و هاست مشترک آن را ندارد** (مستند در بک‌لاگ F4-3: «سرور Imagick ندارد»). مسیر `ptfPettyToJpeg` → `api/attachment-thumb.php` شکست می‌خورد و fallback به `<embed>`/`<img>` در همهٔ مرورگرها/پرینترها کار نمی‌کند (طبق نظر خود ایجنت در کد). → **ضمیمه‌های PDF/HEICِ موجود در پروداکشن نمایش داده نمی‌شوند**؛ در استیجینگ (دادهٔ آزمایشی یا مرورگر متفاوت) دیده نمی‌شود.
2. **وضعیت ردیف‌های MySQL پس از مهاجرت:** اگر `updated_at` ردیف‌های MySQL جدیدتر از mtime فایل‌ها باشد (مثلاً به‌دلیل import مهاجرت)، گارد تازگی (`filemtime > updated_at + 3s`) فعال نمی‌شود و متادیتای کهنه/خالی از DB سرو می‌شود → لینک ضمیمه پیدا نمی‌شود.
3. **کش PWA/مرورگر کاربر پروداکشن:** تا وقتی مرورگرِ کاربر، نسخهٔ جدیدِ `index.html` را با رشتهٔ `v34.0.0-alpha` دریافت نکند (و logic پاک‌سازی کشِ نسخه‌ای اجرا نشود)، ممکن است همان نسخهٔ قدیمی (که ضمیمه را نمی‌خواند) را ببیند.

**چرا در استیجینگ دیده نمی‌شود؟** داده/آزمایش استیجینگ فاقد این حالت‌هاست (ضمایم آزمایشی معمولاً JPG/PNG)، مرورگرِ تست تمیز است، و/یا ردیف‌های DB در استیجینگ سالم‌اند.

**اقدام:** روی پروداکشن راستی‌آزمایی کنید: (الف) `crm/sync-diagnostics.html` → بخش ۱۱ (تطابق فایل↔DB)؛ (ب) تلاش برای نصب/فعال‌سازی Imagick روی هاست (یا جایگزینی fallback سمت کلاینت)؛ (ج) hard-refresh / پاک‌سازی کش PWA.

### ۵.۲ گزارش دوره‌ای تنخواه: ردیف‌ها تکراری به نظر می‌رسند ولی جمع صحیح است

**وضعیت کد:** فیکس‌های F4-6 (نمایش رکوردهای دارای تاریخ میلادی/ناقص)، F4-10 (مقایسهٔ عددی تاریخ) و F4-13 (ردیف‌های میلادی) در پروداکشن مستقرند.

**ریشه:** این **بگ کدی نیست — رفتارِ طراحی‌شدهٔ گزارش‌گیر است** (`ptfPettyPeriodEvents` در `crm/petty.js`):

- هر هزینهٔ تنخواهی که تسویه می‌شود، **دو رکورد** می‌سازد: رکورد «هزینه» در `ptf_crm_petty` + تراکنش «تسویه از حساب» در `ptf_crm_petty_tx` (تابع `pettySettle`).
- گزارش، **هر دو** را به‌عنوان ردیف مجزا فهرست می‌کند: یک ردیف «هزینه» و یک ردیف «تسویه از حساب» برای همان مبلغ → **ظاهرِ «ردیف تکراری»**.
- جمع‌ها (`ptfPettyPeriodTotals`) هزینه را فقط یک‌بار می‌شمارند (ردیف‌های petty؛ تراکنش‌های settle در جمع وارد نمی‌شوند) → **جمع صحیح است**.

**چرا در استیجینگ دیده نمی‌شود؟** وابسته به داده است: در پروداکشن (دادهٔ واقعی با هزینه‌های تسویه‌شدهٔ فراوان) این جفت‌ردیف‌ها زیاد دیده می‌شوند؛ در دادهٔ آزمایشی/تست استیجینگ هزینه‌های تسویه‌شدهٔ کمی وجود دارد. هم‌چنین فیکس F4-6 فیلتر تاریخ را بازتر کرد (رفع «فقط آخرین هزینه نمایش داده می‌شد») که طبیعتاً تعداد ردیف‌های نمایش‌داده‌شده — و در نتیجه جفت‌های «هزینه+تسویه» — را افزایش داد.

**اقدام:** افزودن dedup به `ptfPettyPeriodEvents`: اگر `p.acctTx` (سند تسویه) در txها موجود بود، ردیف «تسویه از حساب» را حذف یا به‌صورت ستونِ همان ردیف «هزینه» نشان دهید (ترجیحاً «نحوهٔ پرداخت: تسویه با سند X» در همان ردیف).

### ۵.۳ حقوق سهامداران در هزینه‌های جاری نمایش داده/محاسبه نمی‌شود (در استیجینگ درست است)

**وضعیت کد:** فیکس F4-7 (کامیت `88fee24`) در پروداکشن مستقر است (تأیید زنده روی `shareholders.js` پروداکشن): از این پس هر حقوق موظف سهامدار، رکورد opex متناظر (cat=«حقوق و دستمزد»، `shareTx=...`) را می‌سازد/بازسازی می‌کند.

**ریشه:** **داده‌ای، نه کدی.** قبل از F4-7، برای سهامدارانی که رکورد حقوقشان از opex حذف شده بود (یا هرگز ساخته نشده بود)، هیچ opexی وجود ندارد → در تب «هزینه‌های جاری» دیده نمی‌شود و در محاسبات سال مالی (جمع opex) لحاظ نمی‌شود. فیکسِ کدی فقط برای **ثبت‌های جدید** کار می‌کند. برای رکوردهای تاریخی باید **اسکریپت مهاجرت** اجرا شود:

- `crm/_tools/migrate_shareholder_opex.js` (کامیت `e136956`) — برای هر tx حقوقِ سهامدارِ بدون opex، یک رکورد opex می‌سازد.
- این اسکریپت **دستی** است (copy-paste در کنسول مرورگرِ دستگاهِ دارای دسترسی به داده) و از استقرارها اکسکلود است (`_tools/**`).

**چرا در استیجینگ درست است؟** مهاجرت روی دادهٔ استیجینگ اجرا شده (یا دادهٔ آزمایشیِ استیجینگ بعد از فیکس ساخته شده)؛ روی دادهٔ پروداکشن هیچ مدرکی از اجرای مهاجرت وجود ندارد.

**اقدام (اولویت ۰):** در یک دستگاهِ لاگین‌شده در پروداکشن، کنسول مرورگر (F12) را باز کنید، محتوای `crm/_tools/migrate_shareholder_opex.js` را اجرا کنید؛ خروجی `✅ N هزینهٔ حقوق فاقد opex ایجاد شد` باید بیاید؛ سپس Sync را بگذارید داده به سرور برسد و در سایر دستگاه‌ها Pull شود. برای اطمینان، همان را در ماژول «سهامداران» و تب «هزینه‌های جاری» راستی‌آزمایی کنید.

---

## ۶. اقدامات پیشنهادی (اولویت‌بندی‌شده)

### P0 — همین امروز
1. **اجرای مهاجرت F4-7 روی پروداکشن** (اسکریپت بالا) → رفع «حقوق سهامداران در هزینه‌های جاری».
2. **راستی‌آزمایی وضعیت ضمایم:** `crm/sync-diagnostics.html` (بخش ۱۱: تطابق فایل↔DB) + تست یک ضمیمهٔ PDF/HEIC و یک JPG؛ بررسی وجود Imagick روی هاست (`php -m | grep imagick` یا در cPanel).
3. **پاک‌سازی کش PWA/مرورگر** در دستگاه‌هایی که پروداکشن را می‌بینند (Ctrl+Shift+R / `crm/clear-cache.html`) تا نسخهٔ v34.0.0-alpha واقعاً لود شود.

### P1 — این هفته
4. **مرج کامیت `5ea39cf` (v34.0.1-alpha) به main و استقرار مجدد پروداکشن** → پروداکشن و استیجینگ از نظر نسخه یکسان می‌شوند و cache-busting موردنظر ایجنت اعمال می‌شود.
5. **رفع «ردیف‌های تکراری» گزارش تنخواه:** dedup هزینه+تسویه در `ptfPettyPeriodEvents` (با حفظ جمع‌های صحیح فعلی).
6. **تصمیم دربارهٔ ضمایم PDF/HEIC:** نصب Imagick روی هاست، یا fallback سمت کلاینت (رندر PDF با کتابخانهٔ JS / تبدیل HEIC با canvas) — مسئله‌ای که ایجنت قبلی «باز» گذاشته بود.

### P2 — فرایندی
7. **انضباط Release:** هر فاز با PR جداگانه و عنوان/توضیح هم‌خوان با محتوا؛ همیشه کامیتِ bump نسخه را قبل از مرج داخل PR بیاورید؛ چک‌لیست استقرار شامل «مهاجرت داده‌ها» و «راستی‌آزمایی روی دادهٔ واقعی پروداکشن».
8. **تبدیل اسکریپت‌های مهاجرت یک‌باره به دکمه/رویهٔ داخل اپ** (مثلاً در تنظیمات) تا دیگر «دستی در کنسول» گم نشوند.
9. **در نظر گرفتن dedup/یکپارچگی در سطح سینک (فایل↔MySQL)** برای جلوگیری از بازتولید رکوردهای تکراری در دورهٔ گذار dual-write.

---

## ۷. جمع‌بندی و داوری نهایی

- **ایجنت قبلی از نظر فنی کارِ درستی کرده و هر سه ناحیه‌ای که شما الان مشکل دارید را در استیجینگ ریشه‌یابی و رفع کرده است.** کدِ این رفع‌ها (به‌جز bump نسخه) به main و پروداکشن رسیده و حضورشان در سرور زنده تأیید شد.
- **مشکل اصلی «فرایند تحویل» بود، نه «کیفیت کد»:** فاز v34 در PR #18 با عنوان قدیمی اسکواش شد، کامیتِ آخر (نسخه) به main نرسید، و **مهاجرت دادهٔ F4-7 روی پروداکشن اجرا نشد**. دو مشکل دیگر (ردیف تکراریِ گزارش و ضمایم PDF/HEIC) ذاتاً «باز» هستند و در استیجینگ به‌دلیل داده/محیطِ متفاوت دیده نمی‌شوند.
- **پیام اصلی:** کافی است (۱) مهاجرت F4-7 روی پروداکشن اجرا شود، (۲) کامیت نسخه مرج و استقرار شود، (۳) کش مرورگرها پاک شود — آنگاه پروداکشن از نظر رفتاری با استیجینگ هم‌تراز می‌شود. رفع «ردیف تکراری» و «ضمایم PDF/HEIC» دو بهبود کدی/محیطیِ جداگانه هستند که پیشنهاد شده‌اند.

---

## پیوست: کامیت‌های کلیدی

| کامیت | موضوع | در main؟ |
|---|---|---|
| `88fee24` | F4-7 — حقوق سهامدار در opex ثبت نمی‌شد (فیکس کد) | ✅ |
| `e136956` | F4-7 — اسکریپت مهاجرت `migrate_shareholder_opex.js` | ✅ (اما در `_tools/` و از استقرار اکسکلود) |
| `4d169a5` | F4-6 — دو باگ بحرانی گزارش بازه‌ای تنخواه | ✅ |
| `0a8ddcf` | F4-10 — بازهٔ ۳۰ روزه تنخواه نتیجه نمی‌داد (مقایسهٔ تاریخ) | ✅ |
| `9cdaad9` | F4-13 — ضمایم PDF/HEIC (هنوز باز) + رکوردهای میلادی | ✅ |
| `4e4d754` | F4-9+F4-11 — چیدمان PDF تلفیقی + لیست سفید فرمت ضمیمه | ✅ (main دقیقاً برابر این کامیت) |
| `5ea39cf` | v34.0.1-alpha — VER bump برای PWA cache busting | ❌ **فقط روی استیجینگ** |
| `04ea070` | main (مرج PR #18) — مبنای استقرار پروداکشن امروز | — |
