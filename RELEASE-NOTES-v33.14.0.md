# یادداشت‌های انتشار (Release Notes — Version 33.14.0) — 🚨 فوری

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (PR #11 باز)
**تمرکز:** ریشه‌یابی «چرا مهاجرت و پاک‌سازی امن فوری حجم localStorage را کاهش نمی‌دهد» + رفع آن.

---

## 🔍 ریشه (تأیید در کد — پاسخ سؤال کارفرما)

**۱) معماری داده روی localStorage:** همهٔ داده‌های اصلی CRM (درخواست‌ها، پیشنهادها، فاکتورها، پرونده‌ها، موجودی، خریدها، تنخواه، حساب تامین/مشتری و…) در localStorage ذخیره می‌شوند (سقف مرورگر ~۵MB). با رشد واقعی داده، سقف خیلی زود پر می‌شود.

**۲) «پاک‌سازی امن» فقط کلیدهای حاشیه‌ای را هرس می‌کرد:** `ptf_crm_notifs`، `ptf_crm_sendqueue`، `ptf_crm_audit`، `ptf_web_events_v2`، `ptf_chat_history`، `ptf_draft_forms`، `ptf_ai_*` — این‌ها معمولاً فقط چند صد KB هستند. کلیدهای اصلی (که بزرگ‌ترین مصرف‌کننده‌اند) **عمداً** حذف نمی‌شوند (امنیت داده) → وقتی حجم اصلی از داده‌های کسب‌وکار باشد، آزادسازی ناچیز است.

**۳) «مهاجرت به IndexedDB» فقط همین کلیدهای حاشیه‌ای را جابه‌جا می‌کرد** (`isVolatileArchiveCandidate`) — کلیدهای اصلی به IDB نمی‌روند (زیرا sync/backup/ماژول‌ها از localStorage می‌خوانند) → برای دادهٔ اصلی بی‌اثر.

**۴) بزرگ‌ترین عوامل پنهان که هیچ‌کدام هدف نبودند:**
- `ptf_crm_avatars` — عکس‌های پروفایل base64 (هر کدام چند ده تا صدها KB؛ با چند ده کاربر = چند MB)
- `ptf_storage_queue` — صف فایل‌های آپلود (اقلام انجام‌شده هرگز پاک نمی‌شدند)
- `ptf_crm_audit`/`notifs` — رشد نامحدود (بدون هرس سنی)
- فایل‌های ضمیمهٔ base64 داخل کلیدهای اصلی (نامه‌ها/قراردادها/پیوست تنخواه)

## 🔧 رفع (v33.14.0)

- **compactAvatars:** آواتارها → فقط ۲۰ آواتار آخر در localStorage + آرشیو کامل در IndexedDB (بازیابی‌پذیر) → آزادسازی واقعی چند MB در مرورگرهای واقعی.
- **compactStorageQueue:** اقلام انجام‌شده/قدیمی‌تر از ۷ روز حذف.
- **هرس سنی:** audit قدیمی‌تر از ۱۸۰ روز (سقف ۱۰۰۰ حفظ شد) + notifs خوانده‌شدهٔ قدیمی‌تر از ۹۰ روز + sendqueue ارسال‌شدهٔ قدیمی‌تر از ۳۰ روز.
- **isVolatileArchiveCandidate** گسترده شد (avatars + storage_queue) → «مهاجرت» هم این‌ها را آرشیو می‌کند.
- **راهنمای صریح UI:** در باکس «حافظه محلی CRM» اگر پاک‌سازی کمکی نکرد، پیام علت + دکمهٔ «نمایش کلیدهای بزرگ» + اشاره به مهاجرت MySQL (DB-MIG-001). پس از پاک‌سازی، اگر آزادسازی < ۲۵۶KB بود، بزرگ‌ترین کلیدها و علت نمایش داده می‌شود.

---

## 📋 فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/storage-quota.js` | compactAvatars/StorageQueue/AuditByAge/NotifsByAge/SendQueueByAge + isVolatileArchiveCandidate گسترده + compactArrayKeyAge |
| `crm/backup.js` | راهنمای علت در Storage Meter + نمایش کلیدهای بزرگ پس از پاک‌سازی کم‌اثر |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.14.0 |
| `_tools/uat/tester294-storage-clean-effective.js` | **جدید** (۸/۰) |
| `RELEASE-NOTES-v33.14.0.md` / `REGRESSION-REPORT-v33.14.0.md` | اسناد انتشار |

---

## ⚠️ برای کاربر
1. `Ctrl+Shift+R` یا `crm/clear-cache.html` (نسخه باید **v33.14.0** باشد).
2. تنظیمات → «حافظه محلی CRM» → «پاک‌سازی امن فوری» — حالا آواتارهای قدیمی/صف فایل‌ها/لاگ‌های کهنه هم پاک می‌شوند.
3. اگر باز هم حجم کم نشد، پیام علت را ببینید (بزرگ‌ترین کلیدها) — راه‌حل نهایی: مهاجرت MySQL (DB-MIG-001).
