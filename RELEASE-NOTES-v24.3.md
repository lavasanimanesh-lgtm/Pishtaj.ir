# ریلیزنوت v24.3 — فاز ثبات: باگ نرم‌افزاری + گردش‌کار (بدون ماژول جدید)

## سیاست
از این نسخه: تمرکز روی رفع باگ و گردش‌کار؛ ماژول جدید اضافه نشده.

## BUG-C — scoring.js از کار افتاده بود
- **Root Cause:** رشته `confirm` چندخطی در `ptfPayableDel` باعث `SyntaxError` و عدم لود کل ماژول scoring می‌شد.
- **Fix:** تبدیل به یک رشته با `\n`.

## OPS-002 (جزئی) — لایه پیش‌نمایش/انتخاب قالب
- تعریف `window.ptfTopZIndex` برای لایه بالای مودال‌های باز
- z-index دیالوگ انتخاب قالب پیشنهاد: 1500 → 2800

## تست
- `node --check` روی تمام `crm/*.js` → صفر خطا
