# یادداشت انتشار CRM v34.8.33 (فوری)

## رفع: «کالاها نمایش داده نمی‌شوند»

**ریشه:** مسیر سروری جدید ماژول کالا در نبود توکن یا خطای پیاپی سرور در حلقهٔ
fetch می‌ماند — fallback محلی هرگز اجرا نمی‌شد.

**رفع:**
- نبود توکن → خطای محلی داخل DataLayer (بدون fetch) → fallback فوری
- حداکثر ۲ retry؛ شکست پیاپی = بقیهٔ نشست محلی
- renderProductsLocal تابع مستقل با رندر کامل ردیف‌ها
- فیلتر دسته روی ردیف‌های سروری؛ کلید بازگشت فوری حفظ شد
- مکانیک deploy: یک‌بار workflow به‌خاطر عدم هم‌گام‌سازی local/remote ری‌مج شد (git merge)

## کنترل
tester535 (۱۰/۰) + tester534/529/520 به‌روزرسانی قرارداد. گیت کامل ۱۵۰/۰، arch-guard PASS.
