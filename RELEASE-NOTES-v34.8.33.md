# یادداشت انتشار CRM v34.8.33 (فوری)

تاریخ: ۱۴۰۵/۰۶/۰۵ (۲۰۲۶-۰۸-۲۷)

## رفع فوری: «کالاها نمایش داده نمی‌شوند»

**ریشه:** مسیر سروری جدید ماژول کالا در نبود توکن (یا خطای پیاپی سرور) در حلقهٔ
fetch می‌ماند — fallback محلی هرگز اجرا نمی‌شد.

### رفع (v34.8.33)
- **نبود توکن → fallback فوری** به رندر محلی (بدون حتی یک fetch)
- **حداکثر ۲ retry** در هر ورود به ماژول؛ شکست پیاپی = بقیهٔ نشست محلی
- fallback محلی تابع مستقل `renderProductsLocal` (رندر کامل، نه فقط toast)
- فیلتر دسته (pFcat) روی ردیف‌های سروری هم اعمال می‌شود (سرور eq ندارد)
- کلید بازگشت: `ptfProdServerToggle(false)` + بازگشت خودکار پس از موفقیت

### کنترل
tester535 (۸ قرارداد). گیت کامل در ادامه.
