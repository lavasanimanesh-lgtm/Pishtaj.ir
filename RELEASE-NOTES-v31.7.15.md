# Release Notes — v31.7.15

**تاریخ:** ۱۴۰۵/۰۴/۲۸ (2026-07-19)
**نسخه:** v31.7.15
**نوع:** 🟡 رفع ریشه‌ای دو باگ گزارش‌شده کارفرما — هشدار «کد تکراری» در هر رفرش موبایل + اسپم همزمان بات تلگرام (BUG-DUP-NAG-002 + BUG-BOT-SPAM-001)

---

## 🎯 مسئله (به گزارش کارفرما)

هر بار رفرش با مرورگر موبایل: (۱) پیام «کدهای تکراری» دوباره ظاهر می‌شود و (۲) هم‌زمان بات تلگرام در گروه شرکت پیام می‌فرستد. رفع ریشه‌ای خواسته شده.

## 🔍 علت ریشه‌ای (RCA) — دو ریشه مستقل ولی هم‌زمان

### BUG-DUP-NAG-002 — ack تک‌دستگاهی بود
راه‌حل v31.7.10 (fingerprint + دکمه «دیدم») ack را در `localStorage.ptf_code_duplicate_ack` ذخیره می‌کرد — اما **localStorage تک‌مرورگری است و sync نمی‌شود**. ادمین در دسکتاپ «دیدم» می‌زد، ولی موبایل هرگز از این ack خبردار نمی‌شد → با هر رفرش موبایل (که full pull + auto-repair اجرا می‌کند) هشدار دوباره می‌آمد. این خودش مصداق پنجم الگوی «تصمیم یک‌دستگاهی برای وضعیت سراسری» بود.

### BUG-BOT-SPAM-001 — هوک بات از dedup بی‌خبر بود
هوک `window.notify` در `messengers.js` (US-333) **قبل از هر چیز پیام را به بات می‌فرستاد** — حتی وقتی `notify()` داخلی به‌دلیل تکراری بودن (dedup v31.7.10) فقط شمارنده را بالا می‌برد و رکورد جدیدی نمی‌ساخت. یعنی کارتابل تمیز شده بود ولی گروه تلگرام همچنان با هر رفرش اسپم می‌گرفت — دقیقاً همان چیزی که کارفرما دید.

## ✅ اصلاحات

| # | فایل | تغییر | شدت |
|---|------|--------|-----|
| 1 | `crm/codegen.js` | **ack سراسری sync‌شونده:** ack به `ptf_crm_settings.dupCodeAck` منتقل شد که با `setData` بین دستگاه‌های نقش‌های ارشد sync می‌شود — یک‌بار «دیدم» در هر دستگاه = سکوت در همه دستگاه‌ها. localStorage قدیمی به‌عنوان سازگاری عقب‌رو خوانده می‌شود؛ حل‌شدن duplicateها ack را در همه‌جا پاک می‌کند | 🔴 P0 |
| 2 | `crm/rbac.js` | `notify()` پرچم `_ptfNotifySuppressed` را ست می‌کند: وقتی اعلان به‌دلیل تکرار سرکوب شد، مصرف‌کننده‌های پایین‌دستی باخبر می‌شوند | 🔴 P0 |
| 3 | `crm/messengers.js` | هوک بات تلگرام/بله برای notify سرکوب‌شده **هیچ پیامی به گروه نمی‌فرستد**؛ رویداد واقعاً جدید همچنان می‌رود (سرکوب کور نیست) | 🔴 P0 |
| 4 | ۴ نقطه نسخه | v31.7.15 | — |

**نکته مهم:** سیاست‌های قبلی دست‌نخورده‌اند — اگر duplicate «جدیدی» رخ دهد، fingerprint تغییر می‌کند و هم هشدار داخلی هم پیام بات دوباره فعال می‌شوند. فقط «تکرارِ همان وضعیت قبلاً دیده‌شده» ساکت شده است.

## 📊 نتایج گیت

- تستر جدید `tester190-dup-nag-bot-spam`: **۱۶/۱۶ PASS** — شامل شبیه‌سازی دو دستگاه (ack دسکتاپ → سکوت موبایل)، سازگاری عقب‌رو، و اثبات این‌که بات برای ۳ notify تکراری فقط ۱ بار می‌فرستد ولی رویداد جدید را همچنان می‌فرستد
- رگرسیون کامل: **168/168 فایل PASS — 3926/3926 چک PASS**
- `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| ptf_crm_settings | فقط یک فیلد `dupCodeAck` اضافه می‌شود؛ saveSet از v31.7.8 merge می‌کند (adminHash امن) | کم |
| بات تلگرام | فقط پیام‌های «تکرار همان اعلان خوانده‌نشده» حذف شد؛ اعلان‌های واقعی چک/ارجاع/پرداخت مثل قبل | صفر |
| dedup اعلان‌ها (v31.7.10) | دست‌نخورده — tester185 سبز | صفر |
| rate-limit سرور بات (۳۰ پیام/ساعت) | فشار اسپم برداشته شد — ظرفیت برای پیام‌های واقعی آزاد می‌شود | مثبت |

## 📋 راستی‌آزمایی توسط کارفرما (۳ دقیقه — دقیقاً سناریوی گزارش‌شده)

1. در دسکتاپ: تنظیمات → «بررسی کدهای تکراری» → «✓ دیدم — دیگر هشدار نده».
2. چند ثانیه صبر (sync) → **موبایل را چند بار رفرش کنید → نه پیام کد تکراری بیاید، نه پیام بات در گروه تلگرام**.
3. کنترل مثبت: اگر duplicate جدیدی ساخته شود (مثلاً از دستگاه دیگر)، هشدار و بات دوباره فعال می‌شوند — یعنی سیستم کور نشده.

---
*توسعه‌دهنده: PTF Development Team — 2026-07-19*
