# v34.11.0 — فاز S2 سئو: مولد صفحات (۲۰۲۶-۰۸-۳۱)

> طبق `SEO-CMS-AI-ROADMAP-2026-08-31.md`. گیت: **172 PASS / 0 FAIL** (tester556: ۳۶ سنجه). PHP لینت با php-parser.

## ۱) 🛒 مولد صفحهٔ محصول — از دیتای CRM تا صفحهٔ سئوشده
- **تب جدید «محصولات»** در ماژول مدیریت سایت: فهرست کالاهای CRM (نام/برند/مدل/کد) + وضعیت انتشار هرکدام
- **اتصال دوطرفه**: صفحهٔ ساخته‌شده `meta ptf-product-cd` دارد؛ سرور با `product_list` برمی‌گرداند؛ پس از انتشار، رکورد CRM هم `siteSlug` می‌گیرد (از مسیر entity رسمی)
- **مولد AI** (`api/llm.php` اکشن `seo_product`): معرفی/ویژگی‌ها/کاربردها/سوالات متداول — فقط از دیتای واقعی کالا (نام/برند/مدل/استاندارد/واحد/توضیح)؛ قانون سخت: «هرگز قیمت/موجودی/ابعاد/گواهی که در ورودی نیست ساخته نشود»
- **دروازهٔ بازبینی انسانی** الزامی پیش از انتشار (همان الگوی مرکز دانش)؛ AI فقط فیلدهای خالی را پر می‌کند تا ویرایش دستی حفظ شود
- **صفحه**: `products/<slug>.html` با قالب هم‌شکل سایت (اسکلت مرکز دانش) + جدول مشخصات + FAQ + CTA استعلام + اسکیمای **Product / Offer (فقط اگر قیمت دادید) / BreadcrumbList سه‌سطحی / FAQPage** + `og:type=product`
- امنیت: sanitize همان لیست سفید KC، حداقل ۲۰۰ حرف، بک‌آپ پیش از بازنویسی، overwrite با تأیید

## ۲) 🗺 سایت‌مپ — یک باگ ساختاری هم بسته شد
- نگاشت `products/` → **`sitemap-products.xml`** اختصاصی
- **رفع باگ قدیمی**: `sitemap_add` وقتی زیرنقشهٔ جدید می‌ساخت، هرگز آن را به `sitemap-index.xml` اضافه نمی‌کرد (`sitemap_touch_index` فقط lastmod موجود را به‌روز می‌کرد) → تابع `sitemap_index_ensure`

## ۳) 🔗 ادیتور ریدایرکت (تب سئو)
- افزودن ریدایرکت: صفحهٔ مبدأ → stub امن (refresh + canonical به مقصد + noindex — همان الگوی اثبات‌شدهٔ blog_archive)، حذف از نقشه، بک‌آپ کامل، رجیستری `crm/data/cms-redirects.json`
- «↩️ بازگردانی»: بازیابی آخرین بک‌آپ سالم + بازگشت به نقشه
- محافظ‌ها: فقط مقصد داخلی؛ مبدأهای حساس (404/crm) رد؛ رکوردهای کهنه به‌طور خودکار از رجیستری می‌ریزند

## ۴) جزئیات
- پیش‌نویس ماندگار فرم محصول (همان موتور S1)؛ ثبت خودکار نقشه پس از انتشار محصول
- صفر `localStorage` مستقیم (A10)؛ PHP هر سه فایل با php-parser لینت
- نسخه: ۶ نقطهٔ رسمی + ۱۰۹ cache-buster + ۵۲۹+ پین تستر

## بعدی (S2-id ادامه + S3)
- قالب‌های نوع صفحه برای services/industries/comparisons (همان موتور، اسکیمای متفاوت)
- S3: اسنپ‌شات روزانهٔ GSC + خوشه‌بندی کلمه→محتوا + داشبورد KPI سئو
