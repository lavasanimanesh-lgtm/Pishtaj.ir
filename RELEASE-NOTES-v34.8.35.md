# یادداشت انتشار CRM v34.8.35 (T0 — اتصال واقعی گیت CI)

## خلاصه
بندهای **اولویت ۱** از گزارش ممیزی `ARENA-THIN-CLIENT-STATUS-AUDIT-2026-08-28.md`
(یافتهٔ F-1): گیت‌های CI فقط محلی/دستی اجرا می‌شدند و هیچ workflow گیت‌هابی آنها را
صدا نمی‌زد — هر PR می‌توانست بایپس localStorage یا تخلف معماری جدید را بدون مانع مرج کند.
این نسخه اتصال واقعی گیت‌ها + محافظ «دیپلوی مخلوط» را اضافه می‌کند (بدون تغییر رفتار CRM).

## ⚠️ تحویل تغییرات workflow — نیاز به اعمال دستی مالک
GitHub App ایجنت مجوز نوشتن پوشهٔ `.github/workflows/` را ندارد، بنابراین تغییرات سه
workflow به‌صورت پچ معلق تحویل می‌شود:
**`_tools/PENDING-workflow-t0346-ci-gates-2026-08-28.patch`**
(طبق قرارداد مخزن: `APPLY-WORKFLOW-PATCHES-GUIDE-FA.md` — روش A: اعطای `Workflows:
write` به App، یا روش B: اعمال دستی از طریق وب گیت‌هاب).
تا اعمال این پچ، tester431/436/536 متن پچ را به‌جای فایل اعمال‌نشده می‌سنجند (پاسِ سبز
مشروط) و **استقرار خودکار استیجینگِ برنچ‌های `arena/**` طبق روال گذشته کار می‌کند**.

محتوای پچ (T0-3 / T0-4 / T0-6):

### T0-3 — اتصال run-ci-gate + arch-guard به GitHub Actions
- `.github/workflows/deploy-staging.yml` و `deploy-production.yml`: مرحلهٔ
  **Setup Node** + **«Thin-client CI gate + arch guard»** قبل از FTP؛ سبز نشدن گیت
  ⇒ استقرار متوقف.
- `.github/workflows/php.yml`: از `composer validate` بی‌اثر (پروژه composer.json
  ندارد) به گیت واقعی بازنویسی شد: `php -l` روی `api`+`crm` و سپس
  `node _tools/uat/run-ci-gate.js` روی همان تریگرهای push/PR به main — مسیر مستقیم
  PR به main دیگر بدون گیت نیست.
- `tester431` (G2): ادعای گمراه‌کنندهٔ «نگهبان به گیت CI وصل است» (که فقط ارجاع فایل
  را چک می‌کرد) با ادعاهای **واقعیِ** اجرای رانر در استیجینگ/پروداکشن/php.yml جایگزین
  شد (اعمال‌شده یا پچ معلق).

### T0-4 — post-deploy hash check (گیت بعد از استقرار)
- ابزار جدید `_tools/uat/post-deploy-hash-check.js` (همین نسخه، در مخزن): بعد از FTP در
  هر دو workflow اجرا می‌شود و هش SHA-256 **۵ فایل کلیدیِ زنده** را با کامیت
  مستقرشده مقایسه می‌کند (index.html، sw.js، sales-domain-v2.js، leads.js + فایل PHP
  از طریق پروب). نابرابر = «نسخهٔ مخلوط/دیپلوی ناقص» ⇒ شکست استقرار (تا ~۲ دقیقه
  retry برای انتشار کامل FTP/CDN).
- `api/sales-domain.php` (همین نسخه، در مخزن): اکشن عمومی بدون احراز هویت
  **`deploy_probe`** که هش فایل زندهٔ خود endpoint و `SD_SERVICE_VERSION` را
  برمی‌گرداند (بدون هیچ دادهٔ کسب‌وکاری/نشست) — هش فایل PHP را نمی‌توان مستقیم از روی
  وب گرفت چون سرور آن را اجرا می‌کند؛ ثابت `SD_SERVICE_VERSION` به بالای فایل منتقل شد
  تا پروب قبل از گارد احراز هویت در دسترس باشد (همان ثابت واحد، بدون تکرار).

### T0-6 — هدر no-cache فقط استیجینگ
- در `deploy-staging.yml` (پچ معلق)، بلوک `<FilesMatch "\.(js|html)$">` با
  `Cache-Control: no-store, no-cache` به انتهای `.htaccess` تزریق می‌شود (فقط استیجینگ؛
  پروداکشن دست‌نخورده و همان بهینه‌سازی کش بلندمدت را دارد). ریشهٔ دیدن «نسخهٔ مخلوط»
  حین تست استیجینگ همین کش مرورگر/CDN بود.

### سایر (قرارداد H1/tester436 و tester398)
- `api/migrate.php` به فهرست exclude استقرار FTP **پروداکشن** در پچ اضافه شد
  (روی استیجینگ آزاد می‌ماند چون قفل مهاجرت جداگانه دارد).

## تست‌ها
- تستر جدید **tester536** (۱۸ قرارداد): اتصال هر سه workflow به گیت (اعمال‌شده یا پچ
  معلق)، وجود/اجرا/ترتیب post-deploy check، قرارداد deploy_probe (قبل از auth، هش فایل
  زنده، نسخه)، تزریق no-cache فقط استیجینگ، و هم‌نسخگی.
- `tester431` (G2: ۲۳ قرارداد) و `tester436` (H1: ۱۱ قرارداد) به‌روزرسانی شدند.
- پین‌های نسخهٔ تسترها از 34.8.34 به 34.8.35 (۸۰ فایل، فقط رشته‌های نسخه).
- گیت کامل محلی: **151 PASS / 0 FAIL** (۱۵۰ قبلی + tester536)؛ arch-guard PASS.

## نسخه
v34.8.34 → **v34.8.35** در هر ۷ نقطهٔ رسمی (VERSION.json، crm/index.html، crm/sw.js
شامل CACHE/ASSET_VERSION، crm/manifest.json، crm/clear-cache.html، crm/shell.js،
api/sales-domain.php).
