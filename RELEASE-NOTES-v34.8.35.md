# یادداشت انتشار CRM v34.8.35

**تاریخ:** ۲۰۲۶-۰۸-۲۸ | **مرجع:** `ARENA-THIN-CLIENT-STATUS-AUDIT-2026-08-28.md` (اولویت ۱ و بخشی از اولویت ۲)

این نسخه **اولویت شمارهٔ ۱ گزارش ممیزی** را می‌بندد: فاز T0 رودمپ نازک‌سازی
(«اول از همه، جلوی پس‌رفتگی را بگیر») که تا امروز روی کاغذ انجام‌شده اعلام شده بود
ولی در عمل **هیچ workflow گیت‌هابی گیت‌ها را اجرا نمی‌کرد**.

---

## 🟥 رفع F-1 — گیت CI حالا واقعاً در CI است (T0-3)

**مشکل:** `run-ci-gate.js` (۱۵۲ تستر قرارداد) و `arch-guard.js` (قواعد A1–A11 شامل
A10 «منع بایپس localStorage» و A11 «تطابق رجیستری فرمان») فقط **دستی و محلی** اجرا
می‌شدند. هر دو workflow استقرار فقط `php -l` و FTP داشتند. یعنی هر PR می‌توانست
دوباره `localStorage.setItem` کسب‌وکاری اضافه کند و بدون هیچ مانعی مرج شود.

> ### ⚠️ یک قدم دستی لازم است
>
> نوشتن روی `.github/workflows/` نیازمند دسترسی `workflows` است که اپلیکیشن گیت‌هابِ
> نشست ندارد (پوش مستقیم امتحان شد و گیت‌هاب رد کرد:
> `refusing to allow a GitHub App to ... without workflows permission`).
> پس تغییرات workflow به‌صورت **وصلهٔ تست‌شده** تحویل می‌شود:
>
> ```bash
> git apply _tools/PENDING-workflow-t0-gates-2026-08-28.patch
> git rm _tools/PENDING-workflow-t0-gates-2026-08-28.patch
> git commit -m "chore: اتصال گیت‌های T0 به GitHub Actions"
> ```
>
> **تا وقتی این اعمال نشود، گیت‌ها هنوز در CI اجرا نمی‌شوند.** برای اینکه این
> واقعیت پنهان نماند، tester536 در حالت «وصلهٔ معلق» وصله را در یک مسیر موقت
> **واقعاً اعمال می‌کند** و همان قرارداد را روی نتیجه می‌سنجد — یعنی اگر وصله گم
> شود یا ناکافی باشد، گیت قرمز می‌شود (تأیید شد: بدون وصله ۱ FAIL، با وصلهٔ
> تضعیف‌شده ۱۰ FAIL).

**رفع:** در هر سه workflow قدم اجرایی واقعی اضافه شد:

| workflow | قدم جدید | جایگاه |
|---|---|---|
| `deploy-staging.yml` | `arch-guard` + `run-ci-gate` | **قبل** از تزریق بنر و FTP |
| `deploy-production.yml` | `arch-guard` + `run-ci-gate` | بعد از php-lint، **قبل** از FTP |
| `php.yml` → **Quality Gate** | lint PHP/JS + arch-guard + گیت UAT | روی هر `push`/`pull_request` به main |

نکات پیاده‌سازی:
- گیت روی **سورس دست‌نخورده** اجرا می‌شود (قبل از تزریق بنر استیجینگ)، وگرنه قاعدهٔ
  A6 (هماهنگی نسخه) روی فایل‌های دستکاری‌شده سنجیده می‌شد.
- `arch-guard` **قبل** از سوئیت تسترها اجرا می‌شود (fail-fast: ۱ ثانیه به‌جای ۱۴ ثانیه).
- هیچ `continue-on-error` یا `|| true` روی این قدم‌ها نیست — واقعاً مسدودکننده‌اند.

**`php.yml` بازنویسی شد:** نسخهٔ قبلی `composer validate --strict` را صدا می‌زد در
حالی که این مخزن اصلاً `composer.json` ندارد — یک جاب کاملاً بی‌اثر. حالا واقعاً
lint می‌کند: `php -l` روی `api/` و `crm/`، `node --check` روی همهٔ `crm/*.js`،
سپس arch-guard و گیت UAT.

## 🟥 رفع T0-4 — هش‌چک بعد از استقرار (فایل زنده == کامیت)

ریشهٔ حادثهٔ «نسخهٔ مخلوط» (۲۰۲۶-۰۸-۲۷): `index.html` تازه روی سرور نشست ولی JSها
نسخهٔ قدیمی ماندند و ساعت‌ها وقت دیباگ هدر رفت. حالا بعد از FTP، هش SHA-1 فایل‌های
کلیدی زنده با کامیت مقایسه می‌شود (۵ بار retry با فاصلهٔ ۱۲ ثانیه برای کش سرور):

- **پروداکشن:** نابرابری = **شکست workflow**، و چون این قدم **قبل** از «Create version tag»
  است، نسخهٔ ناقص هرگز تگ «پایدار» و Release بک‌آپ نمی‌گیرد.
- **استیجینگ:** هشدار (نه شکست) — استیجینگ نباید به‌خاطر کش موقتاً قرمز شود؛ نتیجه در
  Summary گزارش می‌شود.

**اصلاح نسبت به پچ معلق قبلی:** پچ `PENDING-workflow-t0-gates-2026-08-27` فایل
`api/sales-domain.php` را در فهرست هش گذاشته بود — این فایل روی سرور **اجرا** می‌شود و
هرگز سورس برنمی‌گرداند، پس آن گیت **همیشه** شکست می‌خورد. فهرست به دارایی‌های استاتیک
محدود شد و `crm/index.html` از فهرست استیجینگ حذف شد (بنر عمداً به آن تزریق می‌شود).

## 🟥 رفع T0-6 — هدر no-cache فقط روی استیجینگ

workflow استیجینگ یک بلوک `Cache-Control: no-cache, must-revalidate` روی `js/html/json`
به `crm/.htaccess` اضافه می‌کند. **فقط در زمان استقرار استیجینگ** — نسخهٔ مخزن و
پروداکشن دست‌نخورده می‌مانند (تستر این را تضمین می‌کند).

## 🟠 رفع F-3 / T5-1 — حذف دو fallback مردهٔ localStorage

| فایل | کلید | قبل | بعد |
|---|---|---|---|
| `crm/codegen.js` | `ptf_crm_settings` | `else localStorage.setItem(...)` | `throw` (شکست آشکار به‌جای نوشتن غیرمجاز) |
| `crm/treasury.js` | `ptf_crm_shareholders` | `else localStorage.setItem(...)` | `alert` + `return false` |

هر دو کلید کسب‌وکاری/محافظت‌شدهٔ مالی‌اند؛ نوشتن مستقیم‌شان در localStorage خلاف اصل
E2 بود. بدهی A10 از **۲۶۳ به ۲۶۱** کاهش یافت و baseline به‌روزرسانی شد.

## 🐞 باگ واقعی که همین کار لو داد: `ASSET_VERSION` عقب‌مانده

هنگام ارتقای نسخه معلوم شد `crm/sw.js` **سه** نقطهٔ نسخه دارد ولی قاعدهٔ A6 نگهبان
معماری فقط یکی (`RELEASE`) را می‌سنجید:

```js
var RELEASE       = 'v34.8.35';   // ✅ سنجیده می‌شد
var ASSET_VERSION = '34.8.34';    // ❌ عقب مانده بود — سنجیده نمی‌شد
var CACHE         = 'ptf-crm-v34.8.35';
var ASSET_QUERY   = '?v=' + ASSET_VERSION;   // ← کل کش‌باستر service worker
```

یعنی service worker همهٔ اسکریپت‌ها را با `?v=34.8.34` واکشی می‌کرد در حالی که
`index.html` نسخهٔ ۳۵ را می‌خواست — **دقیقاً همان خانوادهٔ باگ «نسخهٔ مخلوط»** که این
فاز قرار بود ریشه‌کن کند. `ASSET_VERSION` اصلاح شد و **هر دو نقطه به قاعدهٔ A6 اضافه
شدند** تا دیگر بی‌صدا drift نکنند (با تست دستی تأیید شد که برگرداندن عدد، گیت را
می‌شکند).

## 🔧 ابزار جدید: `bump-version-pins.js` — پایان drift نسخه‌ای

۷۷ تستر شمارهٔ نسخه را لفظی pin کرده‌اند (`ver.crm_version === 'v34.8.34'` و
`/offers\.js\?v=34\.8\.34/`). این pinها **عمدی و مفیدند** — همان چیزی‌اند که نسخهٔ
مخلوط را می‌گیرند — ولی تا امروز در هر انتشار باید دستی در ده‌ها فایل عوض می‌شدند.

حالا که گیت واقعاً در CI اجرا می‌شود، جا ماندن حتی یکی از آنها **هر انتشار را مسدود
می‌کند**. ابزار جدید این کار را قطعی و کامل انجام می‌دهد (۵۷۰ مورد در ۷۷ فایل، با
بررسی نحوی هر فایل قبل از نوشتن):

```
node _tools/uat/bump-version-pins.js 34.8.34   # بعد از به‌روزرسانی VERSION.json
```

## 🔒 `api/migrate.php` دیگر روی پروداکشن منتشر نمی‌شود

tester436 مدت‌ها انتظار داشت `api/migrate.php` و `api/data-health-check.php` از FTP
پروداکشن exclude شوند (بند H1) ولی این هرگز اعمال نشده بود — چون خودِ آن تستر روی
شاخهٔ SKIP می‌افتاد. حالا اعمال شد و تستر سبز و مسدودکننده است.

## 🧪 کنترل کیفیت

- **tester536 (جدید، ۶۳ قرارداد):** اتصال واقعی گیت‌ها را می‌سنجد — وجود قدم در YAML،
  ترتیب صحیح نسبت به FTP، مسدودکننده بودن (بدون `continue-on-error`)، نبود PHP در فهرست
  هش، تمیز ماندن مخزن از هدر استیجینگ، exclude شدن `migrate.php`، هم‌نسخگی سه‌گانهٔ
  `sw.js`، و حذف fallbackهای مرده. در هر دو حالت «وصل» و «وصلهٔ معلق» کار می‌کند.
- **tester431 اصلاح شد:** ادعای «G2 نگهبان به گیت CI وصل است» فقط رانر محلی را می‌سنجید
  (پاسِ گمراه‌کننده — یافتهٔ F-1). حالا خودِ فایل‌های workflow بررسی می‌شوند.
- **tester436 اصلاح شد:** بررسی «php.yml دیگر composer validate نیست» با ذکر همان عبارت
  در توضیحات تاریخچه هم false-positive می‌داد؛ حالا فقط قدم اجرایی را می‌سنجد.
- **tester535** که ساخته شده بود ولی در سوئیت نبود، اضافه شد.
- **نتیجه:** گیت کامل **۱۵۲ PASS / ۰ FAIL** (از ۱۵۰)، arch-guard **PASS**،
  و هر سه فایل workflow با YAML parser اعتبارسنجی شدند.

## 📦 پاک‌سازی

پچ معلق قدیمی `PENDING-workflow-t0-gates-2026-08-27.patch` با نسخهٔ اصلاح‌شدهٔ
`...-2026-08-28.patch` جایگزین شد (اشکال `sales-domain.php` در فهرست هش رفع شد).

---

## آنچه هنوز باقی است (از گزارش ممیزی)

اولویت‌های ۲ تا ۴ همچنان باز‌ند: بازنشستگی موتور سینک legacy (T6/C5 — `pushDirty`/`pullCheck`)،
انتقال کلیدهای DEV/کش به IDB (T5-2 ناقص + T3-4)، حذف توکن از localStorage (T4-1b)،
آواتار→S3 (T4-3)، و ابزارهای ریکاوری IDB-aware (T7).
