# یادداشت انتشار CRM v34.8.35 — فاز صفرِ نازک‌سازی: گیت‌های CI بالاخره در CI اجرا می‌شوند

**تاریخ:** ۲۰۲۶-۰۸-۲۸ | **نسخهٔ قبلی:** v34.8.34 | **منبع رسمی نسخه:** `VERSION.json`

---

## 🎯 چرا این نسخه مهم است؟

ممیزی ۲۰۲۶-۰۸-۲۸ (`ARENA-THIN-CLIENT-STATUS-AUDIT-2026-08-28.md`) نشان داد بزرگ‌ترین
خلأ فاز نازک‌سازی این نبود که «تست کم داریم»، بلکه این بود که **تست‌ها هیچ‌جا اجرا
نمی‌شدند**:

- `run-ci-gate.js` و `arch-guard.js` فقط روی دستگاه ایجنت اجرا می‌شدند؛
- هیچ workflow گیت‌هابی آن‌ها را صدا نمی‌زد (PHP-lint + FTP بود و بس)؛
- تستر `tester431` با عنوان «نگهبان به گیت CI وصل است» فقط **ارجاعِ درونِ فایل گیت** را
  چک می‌کرد → **پاس گمراه‌کننده**. نتیجه: «۱۵۰ PASS / ۰ FAIL» در حالی که هیچ مانعی
  جلوی برگشتنِ `localStorage.setItem` کسب‌وکار در یک PR جدید نبود.

این نسخه دقیقاً همان بندِ «اول از همه و جلوی پس‌رفتگی» (T0-3 / T0-4 / T0-6) را می‌بندد.

---

## 🛠 چه تغییر کرد

### ۱) T0-3 — گیتِ قبل از FTP (هر دو مسیر استقرار)
`deploy-staging.yml` و `deploy-production.yml` حالا **پیش از FTP** اجرا می‌کنند:

```yaml
- name: Setup Node                       # نود صریح، متکی به نسخهٔ runner نیست
  uses: actions/setup-node@v4
  with: { node-version: '20' }
- name: JS CI gate + architecture guard (pre-deploy gate)
  run: |
    node _tools/uat/run-ci-gate.js
    node _tools/arch/arch-guard.js --quiet
```

یعنی اگر یک PR دوباره بایپسِ لایهٔ داده (قاعدهٔ A10) یا ناهماهنگی رجیستری (A11) یا
ناهماهنگی نسخه (A6) بیاورد، **استقرار اصلاً شروع نمی‌شود**.

### ۲) T0-4 — تاییدِ پس از استقرار (فایل زنده == کامیت)
بعد از FTP، برای هر دو محیط:

1. **نسخهٔ زنده == `VERSION.json`**: `crm/index.html` زنده خوانده می‌شود و رشتهٔ
   `PTF_CRM_RELEASE` با `crm_version` کامیت مقایسه می‌شود — مستقیم‌ترین سنجشِ همان
   حادثهٔ «نسخهٔ مخلوط» (index.html جدید + JSهای قدیمی، ۲۰۲۶-۰۸-۲۷).
2. **هشِ فایل‌های کلیدی زنده == کامیت** (`sha1sum` محلی در برابر `curl` زنده) برای
   `crm/sw.js`، `crm/sales-domain-v2.js`، `crm/leads.js`، `crm/client-server.js`،
   `api/sales-domain.php` — و در پروداکشن `crm/index.html` هم.
   استیجینگ `index.html` را در چکِ هش نمی‌آورد چون بنر استیجینگ عمداً به آن تزریق
   می‌شود (ولی رشتهٔ نسخه تحت‌تأثیر بنر نیست و چک می‌شود).
3. **تلاش مجدد** (۵ بار با فاصلهٔ ۱۲ ثانیه) برای دیپلوی/کشِ دیررس، و `exit 1` واقعی
   روی اختلاف — تا تگ نسخه و Release برای یک استقرار ناقص ساخته نشود.

### ۳) T0-6 — کش استیجینگ خاموش می‌شود
`deploy-staging.yml` هنگام استقرار، بلوک `Cache-Control: no-cache, must-revalidate`
را برای `*.js` و `*.html` به `crm/.htaccess` می‌افزاید (ریشهٔ «نسخهٔ مخلوط» در
استیجینگ: LiteSpeed/مرورگر JS را کش می‌کرد). این تزریق **فقط در استیجینگ** است و در
فایل کامیت‌شدهٔ `crm/.htaccess` نیست — یعنی به پروداکشن نشت نمی‌کند.

### ۴) گیت روی هر pull_request به main
`php.yml` قدیمی (که در این مخزنِ بدون `composer.json` عملاً بی‌اثر بود:
`composer validate --strict`) جایگزین شد با یک جاب واقعی:

- `php -l` روی همهٔ فایل‌های `api/` و `crm/`
- `node --check` روی همهٔ فایل‌های `crm/*.js`
- همان گیت کانونی: `node _tools/uat/run-ci-gate.js`

### ۵) تستر جدید + اصلاح پاسِ گمراه‌کننده
- **tester536** (۴۰ قرارداد): خودِ workflowها را می‌سنجد — گیت قبل از FTP باشد،
  تایید پس از استقرار با `sha1sum`/`curl`/تلاش‌مجدد/`exit $fail` باشد، مسیرهای فهرست
  در مخزن وجود داشته باشند، no-cache تزریق شود و به پروداکشن نشت نکند، و هر دو
  workflow استقرار گیت را اجرا کنند. (در حالتِ «وصله هنوز اعمال نشده» همین قرارداد را
  روی نتیجهٔ شبیه‌سازی‌شدهٔ اعمالِ وصله می‌سنجد — جزئیات در ادامه.)
- **tester431**: assertion قدیمی «نگهبان به گیت CI وصل است» گمراه‌کننده بود؛ یک
  assertion واقعی افزوده شد که اجرای `run-ci-gate.js` و `arch-guard.js` را در هر دو
  workflow استقرار چک می‌کند.

---

## 📊 وضعیت گیت

- `node _tools/uat/run-ci-gate.js` → **151 PASS / 0 FAIL** (۱۵۰ قبلی + tester536)
- `node _tools/arch/arch-guard.js --quiet` → **PASS** (A10: ۲۶۳/۲۶۳ بدهیِ baseline، صفر تخلف جدید؛ A6 نسخه: ۰/۰)
- هر سه workflow با `yaml.safe_load` و `bash -n` روی تک‌تک گام‌ها اعتبارسنجی شدند.

---

## ⚠️ نکتهٔ مهم برای استقرار — وصله باید دستی اعمال شود

اپلیکیشن گیت‌هابِ این نشست **دسترسی `workflows` ندارد**، برای همین pushِ مستقیمِ
تغییرات `.github/workflows/` از سوی ایجنت رد می‌شود. مطابق رَویهٔ خودِ این مخزن
(`_tools/PENDING-workflow-*.patch`)، تغییراتِ workflow به صورت یک وصلهٔ آماده و
تست‌شده تحویل شده است:

```bash
git apply _tools/PENDING-workflow-t0-gates-2026-08-28.patch
git rm     _tools/PENDING-workflow-t0-gates-2026-08-28.patch   # دیگر لازم نیست
git add -A && git commit -m "ci: اتصال گیت‌های CI به workflowهای استقرار"
```

وصلهٔ قدیمی‌تر `_tools/PENDING-workflow-t0-gates-2026-08-27.patch` (نشست قبل) با همین
وصله جایگزین شد و حذف گردید — وصلهٔ جدید همان کار را می‌کند به‌علاوهٔ چکِ «نسخهٔ
زنده == VERSION.json» و Setup Node در پروداکشن.

**تا وقتی وصله اعمال نشده**، گیت‌های کیفیت فقط روی دستگاه اجرا می‌شوند (همان خلأ
ممیزی). برای اینکه این وضعیت پنهان نماند، `tester536` در حالتِ «هنوز وصل نیست»
همان قرارداد را روی **نتیجهٔ شبیه‌سازی‌شدهٔ اعمالِ وصله** می‌سنجد و اگر وصله خراب،
ناکافی یا تداخل‌دار شود، گیت قرمز می‌شود. بلافاصله پس از اعمال، tester536 خودکار به
حالتِ سخت‌گیرانه (سنجشِ مستقیمِ workflowها) می‌رود.

## 🔜 باقیماندهٔ فاز (به ترتیب اولویت ممیزی)

1. بازنشستگی موتور سینک legacy (`pushDirty`/`pullCheck` — T6/C5) و حذف fallbackهای
   مردهٔ localStorage (T5-1: `codegen.js` → `ptf_crm_settings`, `treasury.js` → `ptf_crm_shareholders`).
2. انتقال کلیدهای DEV/کش به IDB (T5-2 ناقص + T3-4) و حذف توکن از LS (T4-1b).
3. 2FA پیامکی، bulk revoke نشست، ابزارهای ریکاوری IDB-aware (T7).
