# ارزیابی «تعهد لگاسی» (Legacy Commitment) — مفهوم، جمع‌شدن با فاکتور، و ضرورت

**برنچ:** `arena/019fcd1d-pishtaj-ir` (v34.0.11-alpha)
**وضعیت:** بازبینی + شبیه‌سازی عددی — **هیچ تغییری در کد اعمال نشد** تا تأیید کنید.
**دادهٔ کلیدی:** `ptf_crm_payables` با `pay === 'credit'`.

---

## ۱) «تعهد لگاسی» یعنی چه؟

- یک رکورد در `ptf_crm_payables` با `pay:'credit'` است — یعنی «بدهی اعتباریِ خرید از یک تأمین‌کننده» که **پیش از راه‌اندازی زیر‌دفتر فاکتور خرید** (supplier-finance) ثبت شده است.
- ویژگی‌های آن: `{ cd, sup/سپلایئر, amount, pay:'credit', inqNo, item, ... }`.
- **نکتهٔ مهم:** در کدِ فعلی **هیچ‌جا رکورد جدید با `pay:'credit'` ساخته نمی‌شود** (جستجو در کل ریپو صفر مورد ساخت). پس «تعهد لگاسی» فقط **دادهٔ قدیمی/مهاجرت‌شده** است که از نسخه‌های قبلی باقی مانده؛ جریان عادیِ جدید، تعهد نمی‌سازد (با فاز ۵: «خریدِ تعهدی بی‌معناست، مبنای تعهد فقط فاکتور خرید است» هماهنگ است).

---

## ۲) آیا «تعهد لگاسی» و «فاکتور خریدِ همان» با هم جمع می‌شوند؟

پاسخ **بستگی به «لینک» شدن** دارد، و در دو موتور متفاوت است:

### در گزارش مالی تجمیعی (working-capital) و گردش تأمین‌کننده (`balance`) — ✅ جمع نمی‌شوند
- فاز ۳ و ۶: تعهد لگاسی از **مبلغ** بدهی/سرمایه حذف شد؛ فقط به‌صورت `legacyUnlinked` (گزارش جدا) دیده می‌شود.
- شبیه‌سازی: تعهد ۱۰۰م + فاکتور ۱۰۰م → بدهی فقط **۱۰۰م** (فاکتور)، legacy جدا ۱۰۰م. **نه ۲۰۰م.**

### در موتور سود سال مالی (`fiscal.js`) — ⚠️ اگر فاکتور به تعهد **لینک نشود، دوباره‌شماری** می‌شود
- `fiscal.js` برای payables: `if (p.pay==='credit' && !p.sfInvoiceCd) → out.supplierInvoices += p.amount` (همان منطق قبل از فاز ۳).
- شبیه‌سازی (با تاریخ معتبر در بازه):
  - **فاکتور لینک‌نشده به تعهد:** خروجی = **۲۰۰م** (تعهد ۱۰۰ + فاکتور ۱۰۰) → **دوباره‌شماری/زیان مضاعف در سود نقدی**.
  - **فاکتور لینک‌شده (`sfInvoiceCd`):** خروجی = ۱۰۰م → درست.

### پس:
| حالت | گزارش تجمیعی | سود سال مالی |
|---|---|---|
| فاکتور به تعهد **لینک نشده** | جمع نمی‌شود (بدهی فقط فاکتور؛ legacy جدا) | **جمع می‌شود → دوباره‌شماری** |
| فاکتور به تعهد **لینک شده** (`sfInvoiceCd`) | جمع نمی‌شود | جمع نمی‌شود |

> **ریشهٔ ناسازگاری:** فاز ۳/۶ legacy را از `balance()` و `working-capital` حذف کرد ولی **از `fiscal.js` (سود سال مالی) حذف نکرد.** یعنی دو موتور، برخورد متفاوتی با تعهد لگاسی دارند → همان «مغایرت گزارش‌ها» که قبلاً بحث شد، این‌جا دوباره ظاهر می‌شود.

---

## ۳) ارزیابی ضرورت — آیا اگر تعهد لگاسی نباشد مشکلی ایجاد می‌کند؟

### کاربردهای فعلی تعهد لگاسی (جایی که خوانده می‌شود):
1. **فرم ثبت فاکتور خرید** (`slInvoiceForm`): به کاربر «تعهدهای بازِ لینک‌نشده» را نشان می‌دهد تا در صورت تمایل به فاکتور لینک کند (اختیاری).
2. **اختصاص پرداخت** (`slPaymentStart`): امکان تخصیص پرداخت مستقیم به یک تعهد لگاسی (بدون فاکتور).
3. **گردش/گزارش**: نمایش به‌عنوان «تعهد legacy بدون فاکتور» (اطلاع‌رسانی).
4. **موتور سود سال مالی**: به‌عنوان هزینه/خروجی (اینجا مشکل دوباره‌شماری است).

### اگر تعهد لگاسی نباشد:
- **دادهٔ تاریخی** (بدهی‌های بازِ ثبت‌شده قبل از زیر‌دفتر) از دید پنهان می‌شود — اطلاعاتِ «چه چیز به تأمین‌کننده بدهکاریم از قبل» از بین می‌رود مگر به فاکتور منتقل شود.
- پرداخت مستقیم به «تعهد بدون فاکتور» ممکن نمی‌شود (اما طبق فاز ۵ این نباید اتفاق بیفتد — مبنای تعهد فاکتور است).
- از نظر جریان **آینده**: ضرورتی ندارد، چون رکورد جدید ساخته نمی‌شود.

### جمع‌بندی ضرورت:
| بعد | ارزیابی |
|---|---|
| **دادهٔ تاریخی** | 🔵 مفید برای شفافیت (بدهی‌های قدیمی باز) — حذفش اطلاعات تاریخی را می‌پوشاند |
| **جریان جدید** | 🟢 غیرضروری — رکورد جدید ساخته نمی‌شود؛ مبنای تعهد فقط فاکتور است |
| **ریسک فعلی** | 🔴 **ناسازگاری**: اگر فاکتور به تعهد لینک نشود، در سود سال مالی دوباره‌شماری می‌شود (اما در گزارش تجمیعی نه) |

---

## ۴) توصیه

مشکلِ واقعی نه «وجودِ تعهد لگاسی» بلکه **ناسازگاری دو موتور** است:
1. **`fiscal.js` را با فاز ۳ هماهنگ کن:** همان‌طور که `balance()` و `working-capital` legacy را از مبلغ حذف کردند، `fiscal.js` هم باید تعهد لگاسیِ لینک‌نشده را **از خروجی نقدی سود حذف** کند (فقط به‌عنوان اطلاع‌رسانی/`legacyUnlinked`) — یا برعکس، اگر قرار است در سود بیاید، از `balance`/گزارش هم بیاید تا یک‌دست شود.
2. این کار هم «دوباره‌شماری» را برطرف می‌کند هم «مغایرت گزارش‌ها» را (که کاربر در گزارش تجمیعی دیده بود).

**نتیجهٔ ارزیابی:** تعهد لگاسی برای **داده‌های تاریخیِ موجود** مفید است و نباید حذفش کرد (اطلاعات بدهی قدیمی حفظ می‌شود)؛ ولی برای **جریان جدید ضرورتی ندارد**. تنها چیزِ اجباری، **هماهنگ‌سازی `fiscal.js`** است تا تعهد لینک‌نشده در سود دوباره‌شماری نشود.

*تولیدشده توسط Arena Agent — ارزیابی فقط‌خواندنی؛ منتظر تأیید برای اعمال اصلاح `fiscal.js`.*
