# یادداشت انتشار CRM v34.8.34 (فوری دوم)

## رفع نهایی: «کالاها نمایش داده نمی‌شوند»

### ریشهٔ واقعی (کشف جدید)
سرور `collection_query` پاسخ **موفق با rows خالی** برمی‌گرداند وقتی دادهٔ کالا هنوز
از این دستگاه push نشده (یا سرورِ داده را ندارد). مسیر سروری این «خالیِ موفق» را
جدول خالی رندر می‌کرد — در حالی که **دادهٔ کامل در کش محلی کاربر موجود است**.

### رفع (اصلی که باید می‌شد از اول)
**دادهٔ محلی تا sync شدن حاکم است**: اگر پاسخ سرور موفق ولی total=0 بود و کش محلی
رکورد دارد → رندر از محلی (prodServerRead این نشست خاموش تا سرور داده بگیرد).

+ گاردهای قبلی (نبود توکن→no_token بدون fetch، retry≤۲، fallback کامل).

### کنترل
tester535 +۱۱ قرارداد. گیت کامل **۱۵۰/۰**، arch-guard PASS.
