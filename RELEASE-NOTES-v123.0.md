# 📦 ریلیزنوت v123.0 — بسته ناوبری موبایل (US-286 + US-287 + US-290) — مصوب کارفرما

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v122.5 | **ماژول جدید:** `crm/mobilenav.js`
**مرجع:** پیشنهاد کلیدی هیئت ارزیابی موبایل (`ASSESSMENT-MOBILE-UX-v122.md`) — تایید کارفرما دریافت شد ✅

---

## 📱 US-286 — نوار ناوبری پایین (Bottom Tab Bar)

در موبایل، سایدبار ۵۶ پیکسلی حذف شد و **کل عرض صفحه آزاد شد**. به‌جای آن، نوار پایین ثابت با ۵ مقصد در دسترس شست:

| تب | مقصد | نکته |
|---|---|---|
| 🏠 داشبورد | پنل dash | تب پیش‌فرض |
| 🔔 کارتابل | پنل cart | **بج قرمز نخوانده‌ها** — از بج سایدبار به‌صورت زنده سینک می‌شود |
| 📈 فروش | سرنخ‌ها (leads) | ناوبری به مشتریان/استعلامات/پیشنهادها/معاملات هم همین تب را روشن نگه می‌دارد |
| 🛒 تامین | تامین‌کنندگان (sup) | شامل استعلام هوشمند و قیمت‌های خرید |
| ⋯ بیشتر | کشوی تمام‌صفحه | همه ماژول‌ها + جستجو + خروج |

**کشوی «بیشتر» (Bottom Sheet):**
- شبکه ۴ ستونه همه ماژول‌ها — **از سایدبار واقعی ساخته می‌شود، پس RBAC خودکار رعایت است** (کاربری که «تنظیمات» ندارد، در کشو هم نمی‌بیند)
- دستگیره + انیمیشن اسلاید + بستن با لمس پس‌زمینه
- دکمه «خروج از حساب» در انتهای کشو (جایگزین دکمه خروج سایدبار)

**جزئیات فنی:** آیکون‌های SVG خطی استروک 1.7 (هماهنگ iconx) · safe-area هوم‌بار iPhone · رزرو فضای پایین محتوا (هیچ دکمه‌ای زیر نوار نمی‌ماند) · حالت شب کامل · هایلایت تب با هوک سبک روی goPanel · دسکتاپ ذره‌ای تغییر نکرده (همه قواعد داخل `@media ≤768px`).

## 🎯 US-287 — هدر موبایل تک‌ردیفه
- ساعت/تاریخ و نشانگر سلامت در موبایل مخفی شدند (مصرف‌کننده اصلی ردیف دوم و سوم هدر)
- هدر فشرده: `flex-wrap:nowrap` + عنوان پنل با ellipsis — دیگر هیچ سرریزی رخ نمی‌دهد

## 🔍 US-290 — جستجوی موبایلی
- دکمه هدر در موبایل فقط آیکون ذره‌بین شد (متن «جستجوی سریع سرتاسری» و برچسب **Ctrl+K** که در موبایل بی‌معنی بود، مخفی)
- جستجوی سرتاسری با دکمه بزرگ در بالای کشوی «بیشتر» — در دسترس شست

## 🧪 تست عملکردی (شبیه‌ساز مرورگر jsdom، عرض 390px)
ساخت نوار ۵ تب ✅ · کشوی بیشتر با RBAC (ماژول مخفی در کشو هم مخفی) ✅ · جستجو و خروج در کشو ✅ · ناوبری واقعی goPanel ✅ · هایلایت تب گروهی (مشتریان → تب فروش) ✅ · سینک زنده بج کارتابل ✅

## 🧪 کیفیت
| سنجه | نتیجه |
|---|---|
| audit.py | ✅ ۰ خطا (۱ هشدار شناخته‌شده تصاویر) |
| تستر جدید `tester41-v1230.js` | ✅ ۲۴/۲۴ PASS |
| رگرسیون کامل | ✅ **۲۸ تستر** همه PASS |
| بهداشت تسترها | چک نسخه ۶ تستر اخیر یک‌بار برای همیشه به `v12` نسخه‌آزاد شد (دیگر با هیچ bump نسخه‌ای نمی‌شکنند) |

## 📁 فایل‌های تغییریافته
`crm/mobilenav.js` (جدید) · `crm/index.html` (ثبت اسکریپت + VER v123.0) · `crm/sw.js` (کش v123.0 + SHELL) · `_tools/uat/tester41` (جدید) + نسخه‌آزادسازی tester35..40

## 📌 پس از آپلود
PWA/مرورگر موبایل را یک بار کامل ببندید و باز کنید (کش sw قدیمی). سایدبار در موبایل دیگر دیده نمی‌شود — ناوبری کامل از نوار پایین و کشوی «بیشتر».

## 🔭 ادامه بک‌لاگ موبایل (منتظر تایید)
US-291 (برچسب ستون‌ها روی کارت‌ها — پیشنهاد بعدی هیئت)، US-288 (Bottom Sheet فرم‌ها)، US-289 (Swipe)، US-292 (باندل JS)، US-293 (بنر نصب PWA)، US-294 (landscape) — و اسپرینت CRM قبلی (US-260/263/264) نیز همچنان در صف.
