# RELEASE NOTES — v34.7.95 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## RCA کلید ابری + فاز A ابزار «صف آپلود مجدد» ردهٔ E

### ۱) سند RCA — انحراف کلیدهای ابری

- `RCA-CLOUD-KEYS-DEVIATION-2026-08-22.md` — هفت فرضیهٔ ریشه‌یابی رتبه‌بندی‌شده:
  - **H1 (قطعی):** دو الگوی نام‌گذاری کلید (`uniqid('', true)` در `api/crm.php` vs `uniqid()` در `api/storage.php`) — منبع اصلی ردهٔ B.
  - **H2 (قطعی):** فال‌بک `upload_proxy` کلید جدید می‌سازد — منبع ردهٔ E اگر شبکه در میان قطع شود.
  - **H3 (بالا):** تخلف A2 پیش از v34.7.54 (ترتیب `cd || _id`).
  - **H4/H5/H6/H7 (متوسط تا کم):** تغییر فولدر بین نسخه‌ها، سه پیشوند مترادف، نام فارسی، بک‌آپ restore جزئی.
- پنج اقدام پیشنهادی به ترتیب اولویت (unified key generator، commit پس از 200، normalize پیشوند، ابزار صف E، migration اسکن).

### ۲) فاز A ابزار «صف آپلود مجدد» ردهٔ E (RE-UPLOAD-QUEUE-001)

- توابع جدید در `crm/storage.js`:
  - `ptfReuploadQueueBuild(rows?)` — ورودی: خروجی audit؛ خروجی: فقط ردیف‌های `cls === 'E'` با فیلد `module` قابل خواندن (تامین‌کننده / کارفرما / پیشنهاد / …).
  - `ptfReuploadQueueExportCsv()` — دانلود CSV با BOM UTF-8 (باز شدن درست در Excel فارسی) شامل ماژول، کد رکورد، نام رکورد، نام فایل، کلید، وضعیت، دو ستون خالی «اقدام» و «تاریخ» برای پیگیری خارج CRM.
  - دکمهٔ **📤 صف آپلود مجدد (E)** در مودال «📋 گزارش کلیدهای ابری» — فقط وقتی `c.E > 0`.
- امنیت: هر دانلود در audit log ثبت می‌شود.
- تستر جدید `tester497-v34.7.95-reupload-queue.js` — با vm/sandbox:
  - build فقط E را برمی‌گرداند (۲ از ۶ ردیف mixed).
  - نام فایل از key استخراج، ماژول ناشناخته از prefix پاک می‌شود.
  - صف خالی → toast اطلاعی + بدون دانلود.
  - CSV با BOM دانلود، toast موفقیت.
  - ردیف‌های ناقص crash نمی‌کنند.

### فازهای بعدی (مستند شده، هنوز اجرا نشده)

- **فاز B (v34.7.96):** اقدام `reupload` (آپلود جایگزین برای همان کلید) و `clear` (حذف مرجع کلید مرده با تأیید ارشد).
- **فاز C (v34.7.97):** اقدام `request` (پیامک/ایمیل به مالک با نرخ محدود ۲۴ ساعته).

### تست

- گیت CI: **۱۱۳ PASS / ۰ FAIL**.
- نگهبان معماری: **PASS** — بدون تخلف جدید.

### فایل‌ها

- افزوده: `RCA-CLOUD-KEYS-DEVIATION-2026-08-22.md`, `_tools/uat/tester497-v34.7.95-reupload-queue.js`, `RELEASE-NOTES-v34.7.95.md`.
- تغییر: `crm/storage.js` (توابع + دکمه + BOM در ptfDownloadKeyAuditCsv)، `_tools/uat/run-ci-gate.js`، بامپ ۷ نقطهٔ رسمی + تسترها.
