# 👥 ریلیز v17.9 — US-419 «سهامداران، حقوق موظف و علی‌الحساب» (R9 فاز ۳)

**اسپرینت:** R9 مالی — پس از US-418 هزینه‌های جاری و US-422 حساب تنخواه  
**استوری:** US-419 — پروفایل سهامداران، حقوق موظف ماهانه، برداشت/علی‌الحساب و کارت مانده لحظه‌ای

## ✅ اجرا شد

### ۱) پروفایل سهامداران
- ماژول جدید `crm/shareholders.js` اضافه شد.
- اطلاعات هر سهامدار:
  - نام
  - درصد سهام
  - وضعیت فعال/غیرفعال
  - موظف/غیرموظف
  - حقوق ماهانه موظف
- جمع سهام فعال کنترل می‌شود و سیستم اجازه نمی‌دهد جمع درصد سهام فعال از ۱۰۰٪ عبور کند.
- اگر جمع سهام فعال دقیقاً ۱۰۰٪ نباشد، هشدار شفاف در پنل نمایش داده می‌شود.

### ۲) محرمانگی و RBAC
- این بخش فقط برای نقش‌های زیر نمایش/اجرا دارد:
  - `admin`
  - `chairman`
- مدیرعامل و مدیر بازرگانی با وجود دسترسی‌های گسترده قبلی، به این بخش فوق‌حساس سهامداران دسترسی پیش‌فرض ندارند؛ طبق سند R9، این داده در سطح محرمانگی بالاتر نگه داشته شد.

### ۳) حقوق موظف ماهانه
- دکمه «ثبت حقوق ماه» برای سهامداران موظف اضافه شد.
- با ثبت حقوق ماه:
  - یک تراکنش `salary` در `ptf_crm_sharetx` ثبت می‌شود.
  - همزمان یک هزینه در `ptf_crm_opex` با دسته «حقوق و دستمزد» ثبت می‌شود.
  - هزینه با `shareTx` و `shareholderSalary:true` علامت‌گذاری می‌شود تا در تحلیل‌های بعدی دوباره‌شماری نشود.
- ثبت دوباره حقوق همان سهامدار در همان ماه، duplicate نمی‌سازد.

### ۴) برداشت / علی‌الحساب سهامدار
- برای هر سهامدار امکان ثبت برداشت/علی‌الحساب اضافه شد.
- برداشت با نوع `draw` در `ptf_crm_sharetx` ثبت می‌شود.
- برداشت باعث کاهش مطالبه سهامدار یا ایجاد بدهی او به شرکت می‌شود.

### ۵) کارت مانده لحظه‌ای سهامدار
برای هر سهامدار، سیستم مانده را محاسبه می‌کند:

```text
مانده = حقوق/بستانکاری‌ها + مطالبات باز تنخواه − برداشت‌ها/علی‌الحساب‌ها
```

- اگر مانده مثبت باشد: سهامدار از شرکت بستانکار است.
- اگر مانده منفی باشد: سهامدار به شرکت بدهکار است.
- مطالبات باز تنخواه همان شخص، با تطبیق نام، در کارت سهامدار نمایش داده می‌شود.

### ۶) گردش سهامدار
- دکمه «گردش» برای هر سهامدار اضافه شد.
- گردش شامل salary/draw و شرح/تاریخ هر تراکنش است.

### ۷) یکپارچگی با پنل تنخواه و هزینه‌ها
- ماژول سهامداران به‌صورت hook تمیز بعد از `opex.js` روی پنل «تنخواه گردان» سوار شد.
- هیچ پنل موازی یا مسیر جداگانه ساخته نشد.
- ترتیب لود حفظ شد:
  - `petty.js`
  - `opex.js`
  - `shareholders.js`

## 🔐 داده و سینک
کلیدهای جدید:

- `ptf_crm_shareholders`
- `ptf_crm_sharetx`

به این بخش‌ها اضافه شدند:

- Sync
- Backup
- API whitelist
- Zero-Data Guard

در Go-Live:
- `ptf_crm_sharetx` به‌عنوان داده تراکنشی پاک می‌شود.
- `ptf_crm_shareholders` به‌عنوان پروفایل/setup حفظ می‌شود.

## 🔬 کیفیت
- تستر جدید: `tester97-v179.js` با ۲۷ چک.
- رگرسیون کامل: **۸۴ تستر / صفر خطا**.
- Audit سایت: PASS با همان هشدار قدیمی تصاویر سنگین.

## فایل‌های تغییر یافته
- `crm/shareholders.js`
- `crm/sync.js`
- `crm/backup.js`
- `crm/golive.js`
- `crm/index.html`
- `crm/sw.js`
- `api/crm.php`
- `_tools/uat/tester97-v179.js`
- اصلاح تسترهای قبلی برای نسخه‌های جلوتر:
  - `_tools/uat/tester96-v178.js`

## صف بعدی طبق برنامه R9
1. **US-421** — ثبت زیان پروژه + هشدار داده ناقص
2. **US-420** — داشبورد سال مالی و تقسیم سود
3. سپس طبق برنامه: US-410/411 → US-405..408 → US-402/403 → US-417 فاز ۲

> یادآوری: BUG-023 + US-424 از بک‌لاگ R8 به‌دلیل اثر مالی مستقیم، بعد از فاز جاری R9 اولویت پیشنهادی بالایی دارند؛ مگر اینکه کارفرما ترتیب دیگری ابلاغ کند.
