# یادداشت انتشار v34.38.7 — CONTACT-GHOST (ردیف-روحِ تماس)

**تاریخ:** 2026-09-08
**بازوی تغییر:** Arena Agent
**کانال:** CRM (دادهٔ مشتریان / سینک / اتصال سایت)

## گزارش کارفرما
> «در قسمت مشتریان اطلاعات تماس پاک میشه. فکر میکنم به این خاطره که شماره تلفن ها رو با اعداد انگلیسی مینویسم تا فقط هرکی که مشتریان شماره تلفن فارسی هستش ثبت شده سالمه. الان مدیرعامل روی سیستمش یک مشتری ثبت کرده و شماره تلفن هاشو سالم می‌بینم ولی روی سیستم خودم چه با مدیرعامل چه با رییس هیات مدیره تماس‌ها را نمی‌بینم (خود مشتری دیده می‌شود).»

## نتیجهٔ بررسی (RCA کامل در ریشهٔ ریپو: `ARENA-CONTACT-GHOST-EN-DIGITS-RCA-2026-09-08.md`)

**گمان ارقام انگلیسی رد شد.** شبیه‌سازی end-to-end با کد واقعی (فرم → هوک `phonefmt` → روتر فرمان → پورت دقیق `sd_entity_sanitize_*` و merge سرور → ماشین دوم) اثبات کرد شماره‌های لاتین توسط هوک فارسی‌سازی می‌شوند و تماس‌ها کامل و سالم به سرور و ماشین دوم می‌رسند (قفل: `tester614` بندهای ۱.۳–۱.۹). رد شده‌ها: گارد سمت‌سرور، ماسک نقش، فیلتر «مشتری‌های من»، merge سینک و رندر.

**امضای «یکی می‌بیند، یکی نمی‌بیند» یعنی رکوردی که کاربر دوم می‌بیند همان رکورد تماس‌دار نیست** — یا یک stub/تکراریِ بدون تماس است یا سرور نسخهٔ بدون تماس دارد. سه بردار واقعی در کد جاری پیدا و بسته شد:

### ① برخورد کد در heal/مهاجرت‌های سیستمی — مولد «مشتری روح» (اصلاح اصلی)
- **ریشه:** بازسازی خودکار مشتری از روی درخواست (`ptfHealMissingCustomersFromRfqs) و مهاجرت‌های دفترچه (`contact-mig`/`phonefmt-mig`) با `expectCreate` رکورد را upsert می‌کردند؛ اگر همان کد از قبل روی سرور با تماس کامل موجود بود، سرور `entity_id_exists (409)` برمی‌گرداند و مسیر `retryWithFreshCode` (که برای برخوردهای کاربرمحورِ tester595 طراحی شده بود) با **کد نو** دوباره می‌ساخت. محصول نهایی: دو ردیف هم‌نام — یکی معتبر و تماس‌دار، یکی stub سفید. کاربر دوم بعد از پول، سایهٔ stub را می‌بیند: «مشتری هست ولی تماس نیست».
- **اصلاح (`crm/sales-domain-v2.js`):** مجموعهٔ `NO_RECREATE_ON_CONFLICT_REASONS = {rfq-cust-heal, contact-mig, phonefmt-mig}`. reasonهای سیستمیِ بدونِ قصدِ ساخت، در 409 دیگر دوباره نمی‌سازند؛ تابع تازهٔ `dropLocalShadowStub` سایهٔ محلی را از فهرست می‌برد (تا پول بعدی رکورد معتبر سرور را بیاورد) و فرمان با `acked/skippedExisting` settle می‌شود. retry کاربرمحور (`offer-cust` و… با صدور کد نو + toast) عیناً حفظ شده — قرارداد tester595 بند ۵.۶ سبز.

### ② stub بازسازی دیگر کلیدهای تماس تهی نمی‌فرستد (دفاع در عمق)
- **ریشه:** همین stub خالیِ دفاعی با `people: []`, `con: ''`, `ph: ''` در payload می‌رفت؛ merge سرور «کلید حاضر را بازنویسی می‌کند» در هر مسیر `expectCreate:false` هم‌خانواده، کافی بود یک
  `people: []` ارسال شود تا اشخاص رکورد معتبرِ هم‌کد روی سرور **پاک** شوند — دقیقاً امضای «مدیرعامل هنوز می‌بیند (از کش خودش)، دیگران نه».
- **اصلاح (`crm/offers.js`):** stub فقط وقتی `con`/`ph` واقعی از روی درخواست هست آن‌ها را می‌سازد؛ در غیر این‌صورت کلیدهای تماس اصلاً در payload نیستند. قرارداد tester611 ۳.۳ (people وقتی con/ph هست) حفظ شده.

### ③ migrateContacts دیگر تماس خالی fabrication نمی‌کند
- **ریشه:** مهاجرت دفترچه برای هر رکوردِ فاقد `people` — حتی بدون `con`/`ph` قدیمی — یک `people: []` و `coTels: []` خالی می‌ساخت و کل دفتر را push می‌کرد؛ با merge سرور، همین آرایهٔ خالی می‌توانست اشخاص پرِ رکوردِ هم‌کدِ سرور را بشوید (بردار شستِ ماشین‌به‌ماشین).
- **اصلاح (`crm/offers.js`):** مهاجرت فقط وقتی واقعاً چیزی (`con`/`nm`/`ph`) برای مهاجرت هست اجرا می‌شود؛ رکورد بدون تماس legacy دست‌نخورده می‌ماند و دفتر push نمی‌شود. گارد truncation و reason `contact-mig` (v34.38.6) دست‌نخورده — tester613 سبز.

### ④ یافتهٔ فرعی — dedup-by-phone اتصال مشتری سایت ارقام فارسی را نمی‌فهمید
- **ریشه:** `crm/bridge.js` · نرمال‌سازِ شمارهٔ `rfqSiteEnsureCustomer` با `/\D/g` فقط ارقام لاتین را نگه می‌داشت؛ شماره‌های فارسی‌ارقام درخواسته‌ای سایت به رشتهٔ تهی می‌شدند → تطبیق ددوب بر شماره کار نمی‌کرد → برای همان شرکت **مشتری تکراریِ بدون تماس** ساخته می‌شد.
- **اصلاح:** `ptfToEnDigits` (با fallback اگر phonefmt هنوز لود نشده باشد) پیش از حذف غیررقم اعمال می‌شود؛ فارسی، لاتین و عربی-extended به یک ساختار می‌رسند.

## سطح انتشار نسخه (معمول پروژه)
`VERSION.json → v34.38.7`؛ `crm/index.html` (۱۱۱ نشانه)؛ `crm/sw.js`؛ `crm/manifest.json`؛ `crm/clear-cache.html`؛ `crm/shell.js`؛ `api/sales-domain.php` (`SD_SERVICE_VERSION = '34.38.7'`)؛ `crm/cms.js` (`PTF_CMS_JS_VER`).

## تست‌ها
- **قفل جدید:** `_tools/uat/tester614-v34.38.7-contact-ghost-cohorts.js` — ۳۶ سنجه (رفتاری با vm دربرگیرندهٔ کد واقعی + مارکرهای ایستا): ردِ گمان EN؛ نبودِ تکرار و retry در heal؛ حفظ تماس رکورد معتبر سرور؛ محو سایهٔ محلی؛ مهاجرت legacy پابرجا و بدون fabrication؛ ثبت tester614 در گیت؛ پین‌های نسخه.
- **گیت کامل:** `run-ci-gate.js` → **227 PASS / 0 FAIL** (داخل tester573/578 بعد از بامپ `PTF_CMS_JS_VER` سبز؛ tester329 که قبل از این نسخه در HEAD قرمز بود — خارج از دامنه — تغییری نکرد).

## اعتبارسنجی مجددِ محیط تولید
- برخورد کدِ سیستمی دیگر صفر عمل‌اش نیست: در audit رویداد «🛟 برخورد کدِ …» ثبت می‌شود تا اگر باز هم تکرار مشاهده شد، رد داشته باشیم (کد قدیم/جدید و reason دقیق).
