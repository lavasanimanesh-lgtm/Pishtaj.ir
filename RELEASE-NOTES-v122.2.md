# 📦 ریلیزنوت v122.2 — اسپرینت US-280: ادغام زنگوله، پاکسازی کنترل مودال‌ها، درگاه اکسل کامل + پرامپت AI

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v122.1 | **دامنه تغییرات:** CRM (theme.js، bridge.js، shell.js، index.html)

---

## 🔔 بخش ۱ — ادغام زنگوله وسط نوار بالا (بازخورد کارفرما)

زنگوله ایموجی 🔔 وسط نوار، «صندوق پیام» بود (اعلان‌ها + یادآورها + بج تعداد نخوانده) که ماژول bridge جداگانه تزریق می‌کرد — کارایی داشت، پس **حذف نشد بلکه ادغام شد**:

| قبل | بعد |
|---|---|
| ۲ زنگوله: ایموجی 🔔 وسط نوار + زنگوله خطی مینیمال | **۱ زنگوله خطی مینیمال** |
| زنگوله مینیمال فقط به کارتابل می‌رفت | کلیک = باز شدن **صندوق پیام** (همان پنل قبلی + دکمه «کارتابل ↗» داخلش) |
| بج قرمز تعداد نخوانده روی زنگوله ایموجی | بج قرمز روی **زنگوله مینیمال** سوار شد |

✅ AC1: در نوار بالا فقط یک زنگوله (خطی) وجود دارد؛ بج نخوانده و پنل صندوق پیام سالم؛ اگر ماژول صندوق هنوز لود نشده باشد کلیک به کارتابل می‌رود (fallback).

## 🔴🟡🟢 بخش ۲ — حذف دکمه‌های بستن قدیمی مودال‌ها (بازخورد کارفرما)

دکمه‌های مک‌استایل (v122.1) زیر دکمه‌های قدیمی رفته بودند. ریشه: دو نسل کنترل قدیمی هنوز فعال بود.

- ❌ حذف شد: تزریق دکمه ✕ شناور با `setInterval` هر ۴۰۰ms (shell.js — یک polling دائمی هم از سیستم کم شد → همسو با TECHDEBT-004)
- ❌ حذف شد: استایل sticky دکمه‌های بستن داخل h3 (index.html) — حالا با CSS به‌طور کامل مخفی‌اند
- ✅ تنها کنترل پنجره‌ها: سه دایره مک (قرمز=بستن، زرد=ارسال به دیسک پایین با حفظ اطلاعات، سبز=تمام‌صفحه)

✅ AC2: هیچ دکمه بستن دایره‌ای قدیمی روی هیچ مودالی دیده نمی‌شود؛ دکمه‌های داخل فرم («انصراف»، «بستن زونکن» و…) دست نخورده‌اند.

## 📥 بخش ۳ — US-280: درگاه‌های ورود اکسل کامل (یوزر استوری کارفرما)

> «فایل اکسل کامل‌تر هم باید بتوان به سیستم داد… تلفن از موبایل جدا شود… آدرس و شناسه ملی الزامی نباشد اما بتوان ثبت کرد… اگر مشتری دو رابط دارد یا یک رابط دو موبایل دارد راهکاری برای ثبتش از طریق اکسل وجود داشته باشد.»

### قواعد جدید مشترک (مشتریان / تأمین‌کنندگان / سرنخ‌ها)
1. **تفکیک موبایل از تلفن ثابت:** ستون جداگانه برای هرکدام؛ موبایل به `mobs[]` و ثابت به `tels[]` شخص رابط می‌نشیند (همان ساختار فرم ثبت دستی).
2. **چند شماره در یک سلول:** با `/` (یا `,` `؛` `|`) جدا می‌شوند → `0912xxxxxxx/0935xxxxxxx`.
3. **داخلی تلفن ثابت:** بعد از خط تیره → `02188000000-124`.
4. **چند رابط برای یک شرکت:** هر رابط = یک ردیف اکسل با همان نام شرکت؛ سیستم رکورد تکراری نمی‌سازد، رابط جدید را به همان مشتری/تأمین‌کننده **ادغام** می‌کند (و فیلدهای خالی رکورد موجود را از اکسل پر می‌کند بدون بازنویسی داده موجود).
5. **فقط نام شرکت الزامی است** — بقیه ستون‌ها اختیاری: سمت رابط، تلفنخانه، آدرس، شناسه ملی، وب/ایمیل، وضعیت و شماره وندور، نام انگلیسی (مشتری)، ایمیل/صنعت/برآورد ارزش (سرنخ).
6. **سازگاری عقب‌رو:** ۵ ستون اول دقیقاً همان چیدمان قبلی — فایل‌های قدیمی بدون هیچ تغییری کار می‌کنند.
7. کد یکتا با `genCode` + مهر ضدتکرار `dedupStamp` + وضعیت وندور فارسی یا انگلیسی (تاییدشده/approved، بررسی/pending).
8. خطای خواندن فایل دیگر بی‌صدا بلعیده نمی‌شود — پیام فارسی به کاربر.

### ساختار ستون‌ها (خلاصه)
| ماژول | A الزامی | B..E (مثل قبل) | ستون‌های جدید اختیاری |
|---|---|---|---|
| مشتریان (CUST) | نام شرکت | صنعت، رابط، موبایل، وضعیت وندور | F ثابت رابط، G سمت، H تلفنخانه، I آدرس، J شناسه ملی، K وب/ایمیل، L شماره وندور، M نام EN |
| تأمین‌کنندگان (SUP) | نام شرکت | دسته، رابط، موبایل، وب/ایمیل | F ثابت رابط، G سمت، H تلفنخانه، I آدرس، J شناسه ملی |
| سرنخ‌ها (LEADS) | نام شرکت | رابط، موبایل، منبع، نیاز | F ثابت، G سمت، H ایمیل، I صنعت، J برآورد ارزش |

جدول راهنمای داخل مودال «📥 ورود اکسل با راهنما» در هر سه ماژول با همین ساختار به‌روز شد.

## 🤖 بخش ۴ — پرامپت آماده هوش مصنوعی در راهنما (بخش دوم یوزر استوری)

در پایین هر مودال راهنما، جعبه جدید: **«🤖 فایل PDF یا به‌هم‌ریخته دارید؟ پرامپت آماده هوش مصنوعی»**

- پرامپت به‌صورت خودکار از **ساختار ستون‌های همان ماژول** ساخته می‌شود (کالا/پیشنهاد/مشتری/تأمین‌کننده/سرنخ — هرکدام پرامپت مخصوص خود).
- شامل همه قواعد: تفکیک موبایل/ثابت (موبایل با 09)، چند شماره با /، داخلی بعد از خط تیره، هر رابط یک ردیف، ارقام انگلیسی، فقط یک شیت با سرستون.
- دکمه **«📋 کپی پرامپت»** + دکمه میان‌بر **«🤖 باز کردن دستیار هوشمند سامانه»** (پنل AI داخلی).
- کاربر پرامپت + فایل PDF/عکس/اکسل به‌هم‌ریخته را به هر AI بدهد → خروجی، اکسل استاندارد قابل بارگذاری مستقیم در همان درگاه.

✅ AC3..AC5 در تستر ۳۷ پوشش داده شد.

---

## 🧪 کیفیت

| سنجه | نتیجه |
|---|---|
| ممیز `_tools/audit.py` | ✅ PASS — ۰ خطا (۱ هشدار شناخته‌شده تصاویر سنگین سئو موج۲) |
| `node --check` فایل‌های تغییریافته | ✅ |
| رگرسیون ۲۳ تستر قبلی | ✅ همه PASS (تستر ۳۵ به الگوی نسخه‌آزاد `v122x` پچ شد) |
| تستر جدید `tester37-v1222.js` | ✅ ۳۴/۳۴ PASS |
| مجموع تسترها | **۲۴ فایل** |

## 📁 فایل‌های تغییریافته
`crm/index.html` (VER v122.2، راهنماها، ۳ ایمپورتر بازنویسی، پرامپت AI، CSS) · `crm/theme.js` (tbBellBtn) · `crm/bridge.js` (ادغام صندوق پیام روی زنگوله مینیمال) · `crm/shell.js` (حذف تزریق دکمه قدیمی) · `crm/sw.js` (کش v122.2) · `_tools/uat/tester37-v1222.js` (جدید)

## 📌 وضعیت بک‌لاگ
اسپرینت ۱۲۳ (US-260 + US-263 + US-264) طبق برنامه در نوبت است — این ریلیز، بازخوردهای فوری کارفرما را مقدم کرد. پس از تایید شما، «بریم بعدی» → اسپرینت ۱۲۳.
