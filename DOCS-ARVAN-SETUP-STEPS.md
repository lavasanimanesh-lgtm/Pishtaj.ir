# ☁️ راهنمای گام‌به‌گام راه‌اندازی آروان‌کلود برای CRM پیشتاز
**مرحله فعلی شما: ثبت‌نام انجام شده ✅ — روی تب Storage هستید**
**زمان لازم: حدود ۱۰ دقیقه**

---

## گام ۱ — فعال‌سازی سرویس فضای ابری (Object Storage)

1. در پنل آروان (panel.arvancloud.ir)، از منوی محصولات، روی **«فضای ابری» (Object Storage)** کلیک کنید.
2. اگر اولین بار است، دکمه **«فعال‌سازی سرویس»** را می‌بینید — بزنید.
3. ممکن است انتخاب **بسته (پلن)** بخواهد: برای شروع همان **بسته پایه/رایگان** کافی است (بعداً از بخش «وضعیت بسته» در پیشخوان قابل ارتقاست).

## گام ۲ — ساخت صندوقچه (Bucket)

> آروان به باکت می‌گوید **«صندوقچه»**.

1. از منوی سمت راستِ بخش فضای ابری، روی **«صندوقچه‌ها»** کلیک کنید.
2. دکمه **«ایجاد صندوقچه»** (یا + صندوقچه جدید) را بزنید.
3. تنظیمات را این‌طور وارد کنید:

| فیلد | مقدار |
|---|---|
| نام صندوقچه | `ptf-crm-files` (حروف کوچک انگلیسی، بدون فاصله) |
| منطقه (Region) | همان پیش‌فرض تهران — معمولاً `ir-thr-at1` (شهریار) |
| سطح دسترسی | ⚠️ **خصوصی (Private)** — خیلی مهم! چون اسناد تجاری شرکت است |

4. **«ایجاد»** را بزنید. صندوقچه در لیست ظاهر می‌شود.

## گام ۳ — دریافت کلیدهای دسترسی (Access Key + Secret Key)

1. از منوی سمت راست پنل فضای ابری، روی **«مدیریت دسترسی»** کلیک کنید. (طبق مستندات رسمی آروان، کلیدها همین‌جاست)
2. اگر کلیدی وجود ندارد، دکمه **«ساخت کلید دسترسی» / «ایجاد کلید»** را بزنید.
3. دو مقدار به شما نمایش داده می‌شود:
   - **Access Key** — مثل نام کاربری فضای ابری
   - **Secret Key** — مثل رمز عبور ⚠️ **این مقدار فقط همین یک‌بار کامل نمایش داده می‌شود — همان لحظه کپی و در جای امن ذخیره کنید** (مثلاً فایل متنی رمزگذاری‌شده یا مدیر رمز عبور). اگر گم شد باید کلید جدید بسازید.
4. در همان صفحه **Endpoint URL** را هم می‌بینید — معمولاً:
   ```
   https://s3.ir-thr-at1.arvanstorage.ir
   ```
   (اگر منطقه دیگری انتخاب کرده باشید کمی فرق می‌کند — همان که پنل نشان می‌دهد را یادداشت کنید)

## گام ۴ — تنظیم CORS روی صندوقچه

> این تنظیم اجازه می‌دهد مرورگر کاربران CRM مستقیم به آروان فایل آپلود کند (بدون مصرف پهنای باند هاست شما).

1. در لیست صندوقچه‌ها، روی صندوقچه `ptf-crm-files` کلیک کنید (یا منوی سه‌نقطه کنارش) و بخش **«تنظیمات CORS»** را باز کنید.
2. یک قانون (Rule) جدید با این مقادیر بسازید:

| فیلد | مقدار |
|---|---|
| Allowed Origins (دامنه‌های مجاز) | `https://pishtaj.ir, https://www.pishtaj.ir, https://staging.pishtaj.ir` |
| Allowed Methods (متدهای مجاز) | `GET, PUT, POST, HEAD` |
| Allowed Headers (هدرهای مجاز) | `*` |
| Max Age | `3600` (پیش‌فرض) |

3. ذخیره کنید.

> ⚠️ **v34.4.33 — فیکس استیجینگ:** حتماً هر سه دامنه بالا را اضافه کنید. در غیر این صورت آپلود مستقیم از `staging.pishtaj.ir` با خطای CORS «اتصال به فضای ابری برقرار نشد» مسدود می‌شود (مرورگر PUT مستقیم به `*.arvanstorage.ir` را رد می‌کند). برای رفع فوری، هات‌فیکس `upload_proxy` در `api/storage.php` اضافه شد که در صورت خطای CORS، خودکار از طریق سرور آپلود می‌کند — اما برای کارایی بهتر، CORS باکت باید هر سه دامنه را مجاز کند. می‌توانید بعد از لاگین به CRM با نقش admin از `POST /api/fix-arvan-cors.php` برای تنظیم خودکار استفاده کنید.

> 💡 اگر بخش CORS را در پنل پیدا نکردید نگران نباشید — خودم می‌توانم بعداً با همان کلیدها از طریق API تنظیمش کنم (آروان این را پشتیبانی می‌کند). فقط بگویید «CORS را پیدا نکردم».

## گام ۵ — تحویل امن اطلاعات به من

حالا ۴ مقدار دارید:

```
Endpoint URL:  https://s3.ir-thr-at1.arvanstorage.ir   (یا آنچه پنل نشان داد)
Bucket Name:   ptf-crm-files
Access Key:    ................
Secret Key:    ................  ⚠️ محرمانه
```

**روش امن (توصیه‌شده):**
1. من در اسپرینت ۶۲ فایل قالبی به نام `storage-config.sample.php` تحویل می‌دهم.
2. شما آن را باز می‌کنید، ۴ مقدار را جایگذاری می‌کنید و با نام `storage-config.php` روی هاست، **یک پوشه بالاتر از public_html** (خارج از webroot) آپلود می‌کنید.
3. در چت فقط می‌گویید: **«کانفیگ آروان آماده است»** — همین!

⛔ **هرگز Secret Key را در چت، ایمیل عادی یا تیکت ننویسید.**

---

## ✅ چک‌لیست نهایی

- ☐ سرویس Object Storage فعال شد
- ☐ صندوقچه `ptf-crm-files` با دسترسی **خصوصی** ساخته شد
- ☐ از «مدیریت دسترسی»: Access Key + Secret Key گرفته و **در جای امن ذخیره شد**
- ☐ Endpoint URL یادداشت شد
- ☐ CORS برای `https://pishtaj.ir` تنظیم شد (یا: «پیدا نکردم» به من اطلاع داده شد)
- ☐ آماده جایگذاری در `storage-config.php` (بعد از تحویل قالب در اسپرینت ۶۲)

---
*منابع: مستندات رسمی آروان‌کلود — بخش‌های «پیشخان و مدیریت دسترسی» و «تنظیمات CORS» (docs.arvancloud.ir)*
