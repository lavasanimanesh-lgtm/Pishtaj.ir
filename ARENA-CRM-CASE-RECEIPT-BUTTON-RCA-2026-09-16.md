# ARENA · RCA — «دکمهٔ دریافت در پروندهٔ فروش کار نمی‌کند»

**تاریخ:** ۲۰۲۶-۰۹-۱۶ (۱۴۰۵/۰۶/۲۵) · **برچسب تغییر:** CRM **v34.38.22** (RECEIPT-BUTTON-FIX) · **تستر:** `tester657-v34.38.22-case-receipt-button-fix.js`
**دامنه:** `crm/sales-domain-v2.js` · `_tools/uat/tester657-…js` · `_tools/uat/run-ci-gate.js` · `VERSION.json` · cache-bust (`?v=34.38.22` در `crm/index.html`، `sw.js`، `manifest.json`، `clear-cache.html`، `device-reconnect.html`، `cms.js`، `shell.js`)

---

## ۱) گزارش کارفرما

> دکمهٔ «دریافت» در پروندهٔ فروش کار نمی‌کند. این دکمه قبلاً کار می‌کرد اما در تغییرات
> نسخه‌های اخیر از کار افتاده است.

نمونهٔ کارفرما در UI: دکمهٔ **«💳 دریافت و حساب پرونده»** در هاب عملیات پروندهٔ فروش
(و هم‌خانواده‌هایش: «💳 تکمیل تسویه» در گام ۱۰ چرخهٔ عمر، «+ دریافت از پرونده» در پنل
فاکتورها، ورودی حساب مشتری). کلیک ⇒ **هیچ اتفاقی نمی‌افتد**؛ بدون پیام، بدون پنجره.

## ۲) ریشهٔ مشکل (اثبات‌شده با اجرای واقعی کد)

- همهٔ ورودی‌های بالا به یک تابع می‌رسند: `window.ptfCaseFinanceOpen(id)` در
  `crm/sales-domain-v2.js` (خط ~۱۳۳۸).
- **v34.38.19 (INV-OPEN-CANONICAL)** برای یکسان‌سازی ستون «مطالبهٔ باز»، رندر ردیف
  فاکتورهای این پنجره را به منبع واحد AR تغییر داد (خط ۱۳۴۷):
  `var _open=(arCore&&typeof arCore.invoiceState==='function')?…`
- اما `arCore` **فقط داخل `caseTotals`** با `var arCore=(window.PTF||{}).ar;` (خط ۱۳۲۸)
  تعریف شده بود و در دامنهٔ `ptfCaseFinanceOpen` هیچ تعریفی نداشت؛ `arCore` سراسری هم
  در هیچ فایل دیگری وجود ندارد (grep کل مخزن).
- فایل در `'use strict'` است ⇒ نخستین ارزیابیِ شناسهٔ اعلام‌نشده، **`ReferenceError:
  arCore is not defined`** پرتاب می‌کند. چون دکمه‌ها `onclick` inline دارند، خطا فقط در
  console مرورگر می‌نشیند و از دید کاربر «دکمه مرده» است.
- **شرط بروز:** خطا داخل `t.invoices.map(…)` است ⇒ فقط وقتی پرونده **دست‌کم یک فاکتور
  فعال** دارد. پروندهٔ بدون فاکتور سالم باز می‌شد — به همین دلیل باگ هم «ناگهانی و
  بی‌دلیل» به‌نظر می‌رسید و هم در تست‌های دستیِ اولیه (روی پروندهٔ تازه/بدون فاکتور)
  دیده نشد. بدترین حالت ممکن: دقیقاً همان‌جا می‌شکست که کاربر فاکتور دارد و می‌خواهد
  دریافتِ همان فاکتور را ثبت کند.

### بازتولید (PoC با vm روی کد واقعیِ قبل از اصلاح)

```
--- پروندهٔ دارای فاکتور ⇒ کلیک «💳 دریافت و حساب پرونده» ---
RESULT: EXCEPTION → ReferenceError: arCore is not defined
        at sales-domain-v2.js:1347:55  at Array.map (<anonymous>)
dialog inserted? false
--- پروندهٔ بدون فاکتور (کنترل) ---
RESULT: dialog opened OK
```

## ۳) چرا گیت CI نگرفت؟

- تسترِ همان نسخه، `tester642-v34.38.19-invoice-open-canonical.js`، برای این ستون فقط
  **regex ایستا** دارد: `/arCore\.invoiceState\(i\)\.open/.test(sdv2)` — یعنی دقیقاً
  «وجودِ رشتهٔ شکسته» را پاس می‌کرد و هرگز `ptfCaseFinanceOpen` را **اجرا** نکرد.
- `tester436` و `tester421` هم برای همین تابع فقط بررسی رشته‌ای دارند.
- `node --check` (گیت سینتکس) هم ReferenceError را نمی‌گیرد چون سینتکس سالم است.
- درس: قفل‌کردنِ «الگوی متنی» بدون smoke رفتاریِ تابع رندرشونده، رگرسیونِ زمان‌اجرا را
  عبور می‌دهد. تستر۶۵۷ همین حفره را با اجرای واقعی تابع در vm می‌بندد.

## ۴) اصلاح (v34.38.22)

| # | تغییر | اثر |
|---|---|---|
| ۱ | `crm/sales-domain-v2.js` — داخل `ptfCaseFinanceOpen`: `var arCore=(window.PTF||{}).ar;` (دقیقاً همان الگوی `caseTotals`) | ReferenceError حذف؛ پنجرهٔ مالی پرونده برای پرونده‌های دارای فاکتور دوباره باز می‌شود و «+ ثبت دریافت قطعی» در دسترس است |
| ۲ | گارد `(window.PTF||{}).ar` + fallback قبلیِ ستون (`openAmountIRR`/فرمول قدیمی) دست‌نخورده | اگر `ar-reconcile.js` به هر دلیلی بارگذاری نشده باشد هم پنجره باز می‌شود (تاب‌آوری ترتیب اسکریپت) |
| ۳ | `_tools/uat/tester657-v34.38.22-case-receipt-button-fix.js` + ثبت در `run-ci-gate.js` | ۴ سناریوی رفتاری با موتور واقعی `PTF.ar`: ۱) پروندهٔ دارای فاکتور ⇒ بدون خطا + دکمهٔ ثبت دریافت + ردیف فاکتور؛ ۲) بدون فاکتور (کنترل)؛ ۳) بدون `PTF.ar` ⇒ fallback؛ ۴) ستون «مطالبهٔ باز» با وصولی میراثی ⇒ صفرِ منبع‌واحد (قرارداد ۶۴۲ این‌بار رفتاری). روی کدِ قبل از اصلاح: **۹ FAIL / exit 1** (بازتولید دقیق ReferenceError) — روی کد اصلاح‌شده: **۱۳ PASS / 0 FAIL** |
| ۴ | Bump نسخهٔ v34.38.22 در `VERSION.json` + همهٔ نشانگرهای زندهٔ cache-bust | طبق RCA بنچ‌های کهنگهٔ دسکتاپ (۲۰۲۶-۰۹-۱۰)، بدون bump پارامتر `?v=` کلاینت‌ها همان فایل شکستهٔ کش‌شده را نگه می‌داشتند |

**دست‌نخورده (مهم):** منطق مالی هیچ تغییر نکرد — نه `caseTotals`، نه `PTF.ar`، نه
`ptfReceiptOpen`/`command('post_receipt')`، نه `api/sales-domain.php`. کل اصلاح، یک
تعریف متغیر جاافتاده در دامنهٔ درست است. بقیهٔ زنجیرهٔ دریافت در بررسی سالم تأیید شد:
`ptfDialog` (ui-kit.js) موجود؛ `post_receipt/correct_receipt/void_receipt` در
`api/sales-domain.php` (خط ~۲۵۹۱) فعال؛ ترتیب اسکریپت‌ها در `index.html` درست
(`ar-reconcile.js` پیش از `sales-domain-v2.js`، هر دو `defer`).

## ۵) تست و تأیید

- `tester657`: روی کد اصلاح‌شده **۱۳ PASS / 0 FAIL**؛ روی کد HEAD (قبل از اصلاح)
  **۴ PASS / 9 FAIL با exit 1** ⇒ تستر واقعاً رگرسیون را می‌گیرد.
- `tester642` (قرارداد INV-OPEN-CANONICAL): همچنان **۷ PASS / 0 FAIL** — الگوی ستون
  منبع‌واحد حفظ شده، فقط حالا در دامنه‌ای که تعریف دارد اجرا می‌شود.
- `node --check` روی `crm/sales-domain-v2.js`، تستر و گیت: سالم.
- **گیت کامل CI پس از اصلاح: 268 PASS / 2 FAIL** — هر ۲ FAIL عیناً در بیس‌لاینِ
  HEADِ تمیز (پیش از این تغییر) هم قرمز بودند و به این اصلاح ربطی ندارند:
  ① `tester427` — دو سناریوی کارکردی AN-05 (تحلیل/ریسک پرونده) که در خود HEAD هم
  FAIL است؛ ② `tester656` — ناسازگاریِ قالب خروجی با harness گیت (exit=0 و بدون
  FAIL پارس‌شده، ولی `looksPassed` به خطوط `✔` جواب نمی‌دهد). arch-guard (A6 —
  هم‌راستایی نسخه در ۸ نقطه) سبز؛ tester425 (OPS-01) و tester578 (یادداشت انتشار)
  سبز. bump پین‌ها با ابزار رسمی `_tools/uat/bump-version-pins.js 34.38.21`
  (۵۸۱ مورد در ۱۳۲ فایل — شامل شکل‌های regex-literal که sed ساده آنها را نمی‌دید).

## ۶) نکتهٔ انتشار

- هیچ مهاجرت داده‌ای لازم نیست؛ تغییر کاملاً کلاینتی و نمایشی است.
- استقرار باید با همان روال همیشگی (FTP پس از گیت سبز) انجام شود؛ به‌لطف bump
  `?v=34.38.22` و `CACHE='ptf-crm-v34.38.22'`، مرورگرها/Service Worker فایل تازه را
  می‌گیرند. اگر کاربری همچنان دکمهٔ مرده دید: `crm/clear-cache.html`.
- تاریخچهٔ git این مخزن squash شده (تک‌کامیت)؛ برای همین «کدام نسخه شکست» از خود
  اسناد نسخه‌ای (تستر۶۴۲ + VERSION.json) استخراج شد: شکست از **v34.38.19**،
  کشف در **v34.38.21** (نسخهٔ فعلی پیش از این اصلاح).
