# یادداشت انتشار v34.39.0 — HOME-DYNAMIC-X (داینامیک‌ترین الگوی بنچ‌مارک‌های تامین تجهیزات + تم ۶۰/۳۰/۱۰ + موبایل اپ‌گونه + حداکثر سرعت)

**تاریخ:** 2026-09-17
**بازوی تغییر:** Arena Agent
**کانال:** سایت عمومی — صفحهٔ اول (نمایشی، بدون تغییر محتوا/سئو/فرم‌ها)
**سند کامل:** `ARENA-HOME-DYNAMIC-X-v34.39.0-IMPLEMENTATION.md`
**قرارداد:** tester662 (۳۶ سنجه) + بروزرسانی عمدهٔ pinهای نسخه با ابزار رسمی `bump-version-pins.js`

## گزارش/درخواست کارفرما
> «داینامیک‌ترین و زیباترین وب‌سایتی که یک شرکت تامین تجهیزات می‌تواند داشته باشد را در اینترنت پیدا و بررسی کن
> و مشابهش را روی سایت ما پیاده کن. تم رنگی فعلی را هم بررسی کن؛ با وفاداری نسبی به رنگ‌های لوگو، تمی که مشتری
> بیشتری جذب کند. سرعت در بالاترین حد باشد و رابط کاربری موبایلی بسیار حرفه‌ای.»

## پژوهش (بنچ‌مارک‌های برتر ۲۰۲۵/۲۰۲)
- **Grainger / Uline / Fastenal / Amazon Business** (رتبه‌های برتر B2B ecomm) — سرعت صفحه، مقوله‌محوری، مسیر استعلام شفاف
- **McMaster-Carr** — «جست‌وجو همه‌چیز است»؛ JS مینیمال، بدون تزئینات بلاک‌کننده، رندر سبک
- **SPT Labtech / Robin Radar / Devitech / Path Robotics** — ژست بصری برنده: تیره‌مایهٔ مهندسی + تایپوگرافی درشت + حرکت سینماییِ ارزان
- **روان‌شناسی رنگ B2B** — قانون ۶۰/۳۰/۱۰ و اثر انزوا (Von Restorff): CTA پیکرنگی روی زمینهٔ سرد بیشترین تبدیل را دارد؛ سرمه+نارنجی = «اعتماد + دقت» در صنعت

## تغییرات
1. **تم جدید صفحهٔ اول (لوگوفیلد):** ۶۰٪ سطوح روشن / ۳۰٪ گرافیت-سرمهٔ مهندسی (`--steel-900..950`، لینک استیل `#155a97`، یخی) / ۱۰٪ شعلهٔ برند فقط در CTAها. آیکون‌های تِالِ ناسازگار نسخهٔ ۳۲.۱ به تم برند یکپارچه شد؛ اورلی هیرو و باند سوابق سرمه‌ای؛ فوتر گرادیان گرافیت.
2. **Finder تجهیز در هیرو** (الگوی McMaster): جست‌وجوی Part Number با GET به `/search/?q=` — بدون JS هم کامل؛ چیپ‌های پرتکرار (A106، فلنج WN، Rosemount 3051، بال‌ولو، VFD، گسکت).
3. **موبایل حرفه‌ای:** داک پایین پنج‌تایی (خانه / **RFQ برجسته** / تماس / واتس‌اپ / چت) با `safe-area` واقعی، هدف لمسی ≥۵۴px، پنهان‌شدن در اسکرول نزولی و حذف سه دکمهٔ شناورِ هم‌پوشان؛ کشوی منو با بک‌دراپ + قفل اسکرول + همبرگر↔✕ + ESC؛ `viewport-fit=cover` و `theme-color` دوحالته.
4. **داینامیکِ سینمایی اما ارزان:** پرده‌گشایی کلمه‌ای H1 با گرادیان شعله، اسپات‌لایت نشانگر روی کارت‌ها (فقط دسکتاپ)، هدر جمع‌شونده با خط برند، شمارنده‌های فارسی‌رقم، لیفت ۳بعدی کارت خدمات — همگی `transform/opacity` با rAF؛ احترام کامل به `prefers-reduced-motion`؛ بدون JS = همان صفحه، سالم.
5. **سرعت:** اسکریپت جدید ۸KB صفروابستگی `ptf-motion.js` (defer)؛ `content-visibility` بخش‌های زیرِ صفحه؛ توقف خودکار اسلایدر در تب پنهان + **سوایپ لمسی هیرو**؛ صفر فونت/دارایی/CDN جدید؛ cache-bust `v=34.39.0`.

## QA
- `arch-guard`: PASS (از جمله A6 هماهنگی نسخهٔ ۸ نقطهٔ رسمی پس از bump)
- `tester662` جدید: ۳۶/۳۶ · `tester661` (بند ۵.۵ cache-bust عمداً به‌روز): ۲۸/۲۸ · `tester210/216/318`: سبز
- گیت کامل: تنها قرمزها موارد از پیش موجود/محیطی‌اند (بدون `php-cli` در محیط: tester656)
- پارسر HTML: بدون خطا؛ `node --check` main.js و ptf-motion.js سبز
