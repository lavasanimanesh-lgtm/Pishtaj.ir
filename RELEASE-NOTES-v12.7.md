# 📦 ریلیزنوت v12.7 — اقلام در ویرایش استعلام + حذف «خلاصه AI» توکن‌سوز + نام «پیشنهاد»

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v12.6

---

## 📋 US-314 — اقلام استعلام: ورود و مشاهده در ویرایش

مودال «✏️ ویرایش/حذف» درخواست، اقلام را نه نشان می‌داد نه راهی برای ورودشان داشت. حالا:

- **باکس «📋 اقلام استعلام»** وسط همان مودال: تا ۸ قلم اول با تعداد/واحد نمایش داده می‌شود (+ شمار مازاد)
- قلم ثبت شده → دکمه **«✏️ ویرایش اقلام»** | قلمی نیست → دکمه **«+ ورود اقلام (دستی / اکسل / دستیار)»** با پیام راهنما
- هر دو دکمه به ویرایشگر کامل اقلام می‌روند (همان سه مسیر: دستی، اکسل، چیپ دستیار)
- اقلام از هر سه منبع (بانک اقلام، خوانده‌های دستیار، خود رکورد) پیدا می‌شوند

## 🔥 US-313 — حذف «خلاصه AI» (تایید تیم: توکن‌سوز و ناکارآمد)

راستی‌آزمایی ادعای شما نتیجه بدتری هم داشت — این سیستم **از دو مسیر توکن اضافی می‌سوزاند**:
1. **هوک خودکار روی ثبت درخواست:** بعد از هر ثبت، بی‌سروصدا یک درخواست AI شلیک می‌شد (بدون اراده کاربر!)
2. polling دائمی هر ۱.۵ ثانیه برای تزریق دکمه‌های سبز/زرد (مصرف CPU)

**حذف شد:** هوک خودکار + polling + هر سه دکمه (🤖 خلاصه / 🟡 / 🟢). از این پس تحلیل اسناد فقط در **دستیار** و به اراده کاربر انجام می‌شود — این عملاً اجرای مصوبه هیئت (US-269: موتور واحد AI) هم هست.
- خلاصه‌های قدیمی ذخیره‌شده از بین نرفتند (در صورت ارجاع، به‌صورت آرشیو نمایش داده می‌شوند)
- یک setInterval دائمی هم از سیستم کم شد (همسو TECHDEBT-004)

## 📄 US-315 — نام ماژول: «پیشنهاد»

حق با شماست — در تغییرنام‌های قبلی (سرنخ‌ها) این مورد از قلم افتاده بود:
| نقطه | قبل | بعد |
|---|---|---|
| سایدبار | پیشنهادهای فنی و مالی (TO / CO) | **پیشنهاد** |
| عنوان پنل | پیشنهادها | **پیشنهاد** |
| سربرگ صفحه | پیشنهادهای فنی و مالی (TO / CO) | **پیشنهاد** |

دکمه‌های داخل پنل (+ Technical / + Commercial / + Techno-Commercial) و تمام منطق TO/CO/TC دست‌نخورده.

## 🧪 کیفیت
| سنجه | نتیجه |
|---|---|
| audit.py | ✅ ۰ خطا |
| تستر جدید `tester47-v127.js` | ✅ ۱۵/۱۵ PASS |
| رگرسیون کامل | ✅ **۳۴ تستر** همه PASS (چک tester34 با تصمیم جدید به‌روز شد) |

## 📁 فایل‌های تغییریافته
`crm/inqreader.js` (حذف بلوک خلاصه AI — ۷KB سبک‌تر) · `crm/bridge.js` (باکس اقلام editRfq) · `crm/offers.js` + `crm/index.html` (نام «پیشنهاد» + VER v12.7) · `crm/sw.js` · `_tools/uat/tester47` (جدید)
