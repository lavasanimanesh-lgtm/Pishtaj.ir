# ارزیابی عمیق: رویژن سند برد داخل پرونده = رویژن پیشنهاد + ابطال فاکتور + هم‌ترازی مالی

**تاریخ:** ۱۴۰۵/۰۵/۲۷  
**نسخه مبنای کد:** v34.7.33  
**محدوده:** فقط ارزیابی — هیچ کدی تغییر نکرده است.

کارفرما این بار مدل را صریح کرد:

> سند برد را بتوان **داخل پرونده** رویژن زد **مانند قسمت پیشنهاد**. با رویژن، **قیمت پیشنهاد برنده = قیمت رویژن پرونده** شود. **فاکتورهای قبلی با تأیید کاربر ابطال** شوند و **اثرات مالی با پیشنهاد جدید هماهنگ** شوند.

این با «بازنگری P5 فعلی» یکی نیست. زیر مدل هدف، فاصله با کد، و طرح فرمان واحد آمده است.

---

## ۱. دو مدل رویژن که امروز هم‌زیست‌اند و با هم نمی‌خوانند

### ۱.۱ رویژن ماژول پیشنهاد (مدل مطلوب کارفرما)

محل: `crm/offers.js` → `offerReviseClone` + `offerSave`

| قاعده | رفتار واقعی |
|---|---|
| شماره تجاری | **ثابت** می‌ماند (`o.no` immutable در `editMode=revision`) |
| نگارش | فقط `rev` یکی بالا می‌رود |
| اقلام | **همان فرم کامل پیشنهاد**: افزودن، حذف، قیمت، شرح، بارگذاری از درخواست، کالا، اکسل |
| تاریخچه | `revisionHistory[]` اسنپ‌شات نسخه قبلی را نگه می‌دارد |
| چاپ | همان شماره با `(Rev.NN)` |
| پس از برد | **قفل است.** `offerPostAwardLocked` دکمه 📑 را برمی‌دارد و به پرونده می‌فرستد: «نگارش بعدی باید از داخل پرونده فروش ثبت شود» |

یعنی محصول **از قبل وعده داده** رویژن برد از پرونده بیاید؛ ولی آنچه در پرونده هست همان فرم پیشنهاد نیست.

### ۱.۲ بازنگری سند برد P5 (مدل فعلی پرونده)

محل: `crm/case-revision.js` `ptfAwardReviseOpen` + `api/sales-domain.php` `revise_award`

| قاعده | رفتار واقعی |
|---|---|
| شماره | سند **جدید** `<wonOffer>-R<n>` با `_id` تازه |
| سند قبلی | `st=superseded` |
| پرونده | `wonOffer` و `rootOfferId` به شمارهٔ جدید می‌چسبد |
| اقلام UI | فقط جدول حفظ/qty/قیمت از اقلام موجود — **بدون افزودن ردیف، بدون فرم پیشنهاد** |
| مبلغ صفر | رد می‌شود (`invalid_revised_amount`) |
| فاکتور رسمی | اگر فعال باشد **کاهش مبلغ ۴۰۹** (`official_invoice_blocks_decrease`) — تصمیم ۱۴۰۵/۰۵/۲۶ |
| فاکتور | **ابطال نمی‌شود** |
| `awardDocs` | به‌روز نمی‌شود |
| `linkedOffers` | ردیف ریشه عوض نمی‌شود؛ متمم‌های قبلی روی شماره کهنه می‌مانند |

نتیجه برای کاربر: «بازنگری» یعنی سند موازی با شماره دیگر، نه رویژن همان CO. پیشنهاد زندهٔ برنده قفل است و قیمتش عوض نمی‌شود مگر کسی `-R1` را سند برد بداند.

### ۱.۳ قفل پیشنهاد حتی از روی `inqNo` گشاد است

```js
// offers.js offerPostAwardLocked
return deals.some(d => d.wonOffer === o.no || d.offerNo === o.no || (o.inqNo && d.inqNo === o.inqNo));
```

هر CO همان استعلام، حتی غیربرنده، پس از تشکیل هر پرونده روی آن inq قفل ویرایش/رویژن می‌شود. این با «رویژن فقط از پرونده» سازگار است فقط اگر پرونده همان فرم را بدهد — که نمی‌دهد.

---

## ۲. مدل هدف (قرارداد پیشنهادی محصول)

یک فرمان اتمی سروری، مثلاً `revise_won_offer` (گسترش کنترل‌شدهٔ `revise_award`، نه مسیر موازی کلاینت).

### ۲.۱ هویت سند — مثل پیشنهاد، نه مثل `-R`

- **شماره تجاری CO ثابت** بماند (همان چیزی که روی نامه/قرارداد کارفرماست).
- `rev += 1` و `revisionHistory` اسنپ‌شات قبلی.
- وضعیت همان `won` بماند؛ `wonRevisionSnapshot` با اقلام/مبلغ جدید بازنویسی شود.
- `case.wonOffer` **عوض نشود** (دیگر `-R1` ساخته نشود مگر کارفرما صریحاً سند جایگزین جدا بخواهد).
- `contractAmount` و `effectiveContractAmount` = جمع اقلام رویژن.
- `awardDocs` تجاری جاری = اسنپ‌شات همین شماره + `rev` جدید؛ نسخهٔ قبلی فقط در تاریخچه/چاپ «Rev قبلی».

این دقیقاً همان وعدهٔ قفل پیشنهاد است: «قیمت پیشنهاد برنده معادل قیمت رویژن پرونده».

اگر اسناد `-R` از P5 در داده هست، مهاجرت: آن‌ها را `revisionHistory` روی شمارهٔ ریشه کنند یا در گزارش به‌عنوان شاخهٔ منسوخ نشان دهند — نه سند زندهٔ دوم.

### ۲.۲ UI داخل پرونده = همان موتور فرم پیشنهاد

باز کردن `offerForm` روی کپی `wonOffer` با `editMode='revision'` و `postAward=true`:

- جدول کامل اقلام (افزودن از کالا/درخواست/اکسل، حذف ردیف، قیمت، واحد، شرح).
- پیش‌نمایش چاپ با Rev جدید.
- ذخیره **مستقیم `offerSave` ممنوع** (`won_offer_locked` / `offerPostAwardLocked`).
- ذخیره فقط از دکمهٔ پرونده که payload را به فرمان سرور می‌فرستد.

نباید جدول فقیر P5 را «کمی دکمهٔ +» کرد؛ کارفرما صریحاً «مانند قسمت پیشنهاد» خواسته.

### ۲.۳ فاکتورها — ابطال با تأیید، نه گارد کاهش خاموش

تصمیم قبلی (مسدود کردن کاهش در حضور فاکتور رسمی) با این درخواست **عوض می‌شود** اگر کارفرما همین پیام را قطعی بداند.

جریان پیشنهادی در همان تراکنش، پس از پیش‌نمایش:

1. فهرست فاکتورهای **فعال** پرونده (رسمی + غیررسمی) با مبلغ، تاریخ، وصول تخصیص‌یافته.
2. چک‌باکس اجباری: «فاکتورهای بالا باطل شوند و مطالبه از روی پیشنهاد رویژن‌شده بازسازی شود.»
3. دلیل واحد (رویژن سفارش کارفرما + ابطال فاکتور).
4. اگر دوره مالی قفل است: توقف با پیام `fiscal_period_locked` — همان گارد `void_invoice`.
5. برای هر فاکتور رسمی فعال: همان بدنهٔ `void_invoice` (سند می‌ماند، `status=void`، correction `legal_void`).
6. برای هر غیررسمی فعال: همان `void_unofficial_invoice`.
7. **رسید خزانه حذف نمی‌شود.** تخصیص‌ها `replaced` و `sd_rebuild_allocations` دوباره روی فاکتورهای فعال (که دیگر نیستند) → کل دریافت می‌شود `creditRemainIRR` بستانکاری همان پرونده.
8. صدور فاکتور جدید **خودکار نشود**؛ بعد از رویژن کاربر از مسیر موجود ثبت کند تا پوشش اقلام با CO جدید جور باشد.

اگر کاربر ابطال را تأیید نکند: فرمان رویژن **نباید** مبلغ را زیر جمع فاکتور فعال ببرد (گارد فعلی بماند). یعنی دو حالت شفاف:

| تأیید ابطال فاکتور | کاهش/حذف قلم / مبلغ کمتر | افزایش / قلم جدید |
|---|---|---|
| خیر | مسدود (همان ۴۰۹) | مجاز؛ فاکتور کهنه کمتر از قرارداد می‌ماند تا اصلاحیه جدا |
| بله | مجاز؛ همهٔ فاکتورهای فعال void | مجاز؛ فاکتورها void تا صدور مجدد روی اقلام جدید |

پیشنهاد محصول: حتی در افزایش هم اگر پوشش خطی فاکتور با اقلام جدید نمی‌خواند، همان تأیید ابطال را اجباری کنید — وگرنه فاکتور خطی روی قلم حذف‌شده یتیم می‌ماند.

### ۲.۴ هم‌ترازی مالی پس از رویژن (چک‌لیست اتمی)

همه در **یک lock** `sales-domain` مثل بقیهٔ فرمان‌ها:

| لایه | کار لازم | وضعیت امروز |
|---|---|---|
| پیشنهاد برنده | items + rev + snapshot + total | P5 سند موازی می‌سازد؛ پیشنهاد ریشه superseded |
| پرونده | effectiveContractAmount، awardRevisions، awardDocs جاری | مبلغ عوض می‌شود؛ awardDocs نه |
| فاکتور | void با تأیید | انجام نمی‌شود؛ کاهش مسدود |
| تخصیص FIFO | rebuild | الان هم صدا می‌شود ولی روی فاکتور فعال کهنه |
| مطالبات AR / حساب مشتری | از فاکتور فعال؛ void → بدهی می‌رود، بستانکاری می‌ماند | درست است **اگر** void رخ دهد |
| رسید | دست‌نخورده | درست |
| پورسانت / سرمایه در گردش / سال مالی | فقط سند فعال (LC-01) | پس از void خودبه‌خود کنار می‌روند |
| نسخه ریالی همراه | `ptfRialCompanionOf` باید Rev همگام یا باطل+بازسازی شود | امروز دست نمی‌خورد |
| `invRef` روی offer | P5 `unset` می‌کند | اگر شماره ثابت بماند باید صریح باطل/بازنشانی شود |
| پوشش خطی فاکتور بعدی | `maxByLine` از items جدید | تا items روی همان offer عوض نشود، سقف کهنه است |
| خرید واقعی / buycmp | تطبیق با `lineId`/`pcode`؛ قلم جدید پوشش ناقص؛ قلم حذف‌شده disposition | وابسته نیست به revise_award |
| docsx PL/IN | coverage روی اقلام CO | باید بعد از رویژن دوباره حساب شود |
| بازرسی / مرجوعی خرید | inspections روی کلید قلم | قلم حذف‌شده نباید بی‌سرنوشت بماند |
| قفل سال مالی | تاریخ فاکتور/رسید | void در سال قفل ۴۰۹ است |
| `register_offer` | هنوز `won_offer_locked` اگر JSON فرق کند | رویژن باید **مستثنی** از این قفل باشد و فقط از فرمان پرونده بیاید |

اتحاد تسویه که باید بعد از فرمان برقرار باشد (الان `sd_reconcile_case`):

`Σ رسید فعال = Σ تخصیص فعال + Σ بستانکاری`

و هیچ فاکتور `active` با مبلغ ≠ قرارداد جدید، مگر کارفرما صدور مجدد را عمداً عقب انداخته باشد (آن‌وقت AR باز = ۰ چون فاکتور نیست؛ بستانکاری = کل وصول).

---

## ۳. چرا «دو سند برد» با این مدل بدتر دیده می‌شود

اگر هدف «یک CO با Rev» است، P5 فعلی دقیقاً ضد آن است: `CO-123` و `CO-123-R1` هر دو در لیست و در جعبهٔ زرد.

علل نشت قبلی سر جایشان است:

1. `sfAwardEnsure` TO را با `inqNo` مشترک می‌چسباند.
2. `win_offer` فقط `won_snapshot` بدون snap می‌نویسد و مهاجرت نرم early-return می‌کند.
3. جعبهٔ زرد همهٔ `awardDocs` را بدون فیلتر `wonOffer`/`rev` جاری چاپ می‌کند.
4. `ptfSalesFileOffers` همهٔ پیشنهاد همان استعلام را می‌آورد.

با مدل شمارهٔ ثابت، نمایش باید بشود: **یک ردیف تجاری جاری (Rev فعلی) + تاریخچه Rev**. سند فنی فقط با `srcToNo` یا `coNo` صریح.

---

## ۴. طرح پیاده‌سازی (پس از تأیید — هنوز کد نزنید)

### فاز ۰ — قرارداد محصول (این سند)

کارفرما این سه خط را امضا کند:

1. رویژن برد = **همان شماره + Rev** (نه `-Rn`).
2. فاکتورهای فعال **با تأیید صریح در همان جریان** void شوند؛ رسید نماندِ حذف.
3. فرم = **همان فرم پیشنهاد** داخل پرونده؛ ذخیره فقط سرور.

اگر شمارهٔ جایگزین `-R` برای نامه به کارفرما لازم است، آن را فیلد چاپی/`altNo` کنید نه هویت سیستم.

### فاز ۱ — فرمان سرور `revise_won_offer`

ورودی: `caseId`, `reason`, `items[]` (کامل، مثل offer), `voidInvoices: true|false`, `idempotencyKey`.

ترتیب داخل lock:

1. بارگذاری case + offer با `wonOffer` (نه ساخت رکورد جدید).
2. اگر `voidInvoices`: برای هر فاکتور فعال همان منطق void موجود + fiscal lock.
3. نوشتن items/rev/history/snapshot روی **همان** offer؛ `st` همان `won`.
4. به‌روز کردن مبلغ پرونده + `awardRevisions` + `awardDocs` جاری.
5. `sd_rebuild_allocations`.
6. correction یکپارچه `kind: revise_won_offer`.
7. خروجی: `rev`, `newAmount`, `voidedInvoiceIds[]`, `caseCreditIRR`, `reconcile`.

گاردها: نقش WIN (+ اگر void، نقش FIN در همان کاربر یا تأیید دوم حسابدار — تصمیم محصول). پیشنهاد: commercial/ceo بتواند رویژن+void بزند چون کارفرما «یک کلیک در پرونده» خواسته؛ accountant در لاگ دیده شود.

`register_offer` کلاینت را برای این رکورد همچنان قفل نگه دارید تا کسی از تب پیشنهاد دور نزند.

### فاز ۲ — UI پرونده

- دکمه فعلی «بازنگری سند برد» → «رویژن پیشنهاد برنده».
- باز کردن فرم پیشنهاد در حالت `postAwardRevision`.
- قبل از commit: پیش‌نمایش دلتا مبلغ + جدول فاکتورهای در معرض ابطال + confirm دو مرحله‌ای.
- پس از موفقیت: toast مبلغ و بستانکاری؛ رفرش کشو و پیشنهادها.
- چاپ از پرونده = `offerPrintObj` روی اسنپ‌شات جاری (نه فقط `sfAwardPrint` کهنه).

### فاز ۳ — هم‌ترازی جانبی

- نسخه ریالی: regenerate یا mark stale.
- docsx coverage و استپر.
- disposition اجباری اگر قلم خرید/PL شده حذف شود.
- ابزار تشخیص نشت awardDocs (فقط‌خواندنی) قبل از mutate دادهٔ کهنه.

### فاز ۴ — UX کشو (سبک)

زبانهٔ «قرارداد»: رویژن + تاریخچه Rev + مبلغ مؤثر + بستانکاری پس از ابطال. عملیات مالی جدا.

---

## ۵. ریسک‌ها اگر فقط UI فرم پیشنهاد را به پرونده وصل کنیم

- `offerSave` کلاینتی از قفل می‌گذرد یا `register_offer` با `won_offer_locked` می‌ترکد.
- فاکتور فعال می‌ماند و AR/پورسانت روی مبلغ کهنه می‌ماند.
- `-R`های قبلی + Rev روی همان شماره = سه هویت برای یک برد.
- void بدون rebuild = بستانکاری غلط (قبلاً در غیررسمی محلی پیش آمده؛ INV-01 برای همین سروری شد).

---

## ۶. جمع‌بندی یک خطی

امروز پرونده **سند جایگزین موازی** می‌سازد و فاکتور را دست نمی‌زند. کارفرما **رویژن همان پیشنهاد برنده** می‌خواهد تا قیمت CO زنده عوض شود و با تأیید، فاکتورها باطل و مطالبه/تخصیص روی قرارداد جدید بنشیند. زیرساخت تقریباً هست (`void_invoice`, `sd_rebuild_allocations`, `revisionHistory` پیشنهاد)؛ آنچه نیست **یک فرمان اتمی با فرم کامل و هویت شمارهٔ ثابت** است.

منتظر تأیید سه بند فاز ۰ قبل از هر پچ.
