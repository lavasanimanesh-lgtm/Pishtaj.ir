# 🐞 ریلیز v15.1 — BUG-015 «خطای پاک‌سازی زباله‌های ابری»

**تاریخ:** ۱۴۰۵/۰۴/۱۸ | **نوع:** رفع باگ گزارش‌شده با اسکرین‌شات کارفرما
**باگ:** دکمه «☁️ پاک‌سازی زباله‌های ابری» → «❌ خطا در ارسال دستور حذف دسته‌جمعی به سرور»

---

## 🔬 ریشه دقیق باگ

خطا اصلا از اتصال یا آروان‌کلود نبود — یک **خطای مرگبار PHP روی سرور خودمان** بود:

- اکشن `delete_batch` در فایل `api/storage.php` تابع `clean()` را صدا می‌زد؛
- ولی `clean()` فقط در `api/crm.php` تعریف شده و **در storage.php اصلا وجود ندارد**؛
- نتیجه: PHP در همان اولین فایل، Fatal Error می‌داد → پاسخ سرور به‌جای JSON یک صفحه خطای HTML بود → کلاینت در پارس JSON شکست می‌خورد → پیام گنگ «خطا در ارسال دستور...».

نکته: این باگ **از نسخه v112 (US-108) خفته بود** و فقط وقتی ظاهر می‌شد که واقعا فایل یتیمی برای حذف پیدا شود — به همین دلیل تسترهای قبلی (که وجود اکشن را چک می‌کردند نه اجرایش را) آن را نگرفته بودند. یک فراخوانی مرده `verify_request()` (آن هم تعریف‌نشده در این فایل) هم در همان بلوک بود که پاک شد.

## ✅ رفع

| فایل | تغییر |
|---|---|
| `api/storage.php` | پاکسازی کلید حالا محلی و امن انجام می‌شود (trim + سقف طول + ضد `..` + سقف ۲۰۰۰ کلید)؛ فراخوانی‌های تعریف‌نشده حذف؛ گزارش `failed` (فایل‌هایی که آروان حذفشان را رد کرد) به پاسخ اضافه شد |
| `crm/storage.js` | پاسخ سرور دیگر کورکورانه JSON پارس نمی‌شود — اگر خطای PHP برگردد، **متن واقعی خطا + کد HTTP** به شما نمایش داده می‌شود (پایان پیام‌های گنگ)؛ گزارش فایل‌های حذف‌نشده هم نمایش داده می‌شود |
| محافظ‌ها | دست‌نخورده: `archives/` و `backups/` هرگز زباله محسوب نمی‌شوند؛ جمع‌آوری بازگشتی کلیدهای فعال (v121.2) پابرجا |

## 🔬 کیفیت

| مورد | نتیجه |
|-------|--------|
| tester71 جدید | ✅ ۱۸ چک — شامل چک ساختاری «هیچ تابع مختص crm.php در storage.php صدا زده نمی‌شود» (ضد بازگشت این خانواده باگ) + تست اجرایی پارس پاسخ سالم/خراب |
| رگرسیون کامل | ✅ **۵۸ تستر / ۱۵۸۹ چک — صفر خطا** |
| `_tools/audit.py` | ✅ PASS |
| نسخه/کش | VER=v15.1، کش `ptf-crm-v15.1`، cache-bust storage.js |

## 📦 بسته تحویلی
`pishtaj-release-v15.1.zip` — تجمیعی **v14.5 تا v15.1**. مثل همیشه: آپلود + اکسترکت روی موجود (پوشه api داخل بسته است و باید بازنویسی شود — حذف دستی ممنوع). بعد از آپلود یک رفرش و دوباره دکمه پاک‌سازی را امتحان کنید.

💡 اگر این‌بار خطایی دیدید، پیام جدید **علت واقعی** را نشان می‌دهد (مثلا مشکل دسترسی آروان) — همان را برای من بفرستید.
