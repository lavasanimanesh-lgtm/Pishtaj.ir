# RELEASE NOTES — v34.7.101 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## هاب مالی — کاشی‌های تراز و گزارش تجمیعی: اعداد خیلی درشت‌تر

بازخورد کارفرما پس از v34.7.100:  
> «هنوز کاشی ها مثل تراز رسمی و غیر رسمی، گزارش تجمیعی مالی و... اعداد کوچکتر از حد استاندارد اند»

### تحلیل چرایی

کاشی‌های `block()` در `ledger-report.js` و `card()` در `working-capital.js` روی `<b style="color:XXX">` می‌سازند. رنگ inline اعمال می‌شود ولی `.sc b` سراسری من سقف را فقط 28px گذاشته بود — که در دسکتاپ رایج (1024-1440px) به 20-27px می‌رسید. کاربر همچنان «کوچک» می‌دید چون کاشی‌های ledger پرتراکم‌اند (۱۵+ کاشی در یک صفحه) و کاشی‌های working-capital ۸ عدد در یک ردیف.

### تغییرات (فقط CSS دسکتاپ — موبایل دست‌نخورد)

| ویژگی | v34.7.100 | v34.7.101 | تغییر |
|---|---|---|---|
| کف فونت عدد | `20px` | **`24px`** | +20% |
| سقف فونت عدد | `28px` | **`34px`** | +21% |
| شیب vw | `1.9vw` | **`2.4vw`** | سریع‌تر به سقف می‌رسد |
| padding کارت | `18px 15px` | **`22px 18px`** | تنفس ماژور |
| minmax گرید | `200px` | **`220px`** | فضای بیشتر برای فونت 34px |
| gap گرید | `12px` | **`14px`** | نفس بین کارت‌ها |
| `.unit` واحد | `55%` | **`52%`** | نسبی کوچک‌تر (چون عدد اصلی خیلی بزرگ‌تر شد) |
| توضیح (span) | `12px` + margin-top 4 | **`12.5px`** + margin-top 6 | خوانا |

### رفتار در سه اندازه دسکتاپ

- **1024px** (لپ‌تاپ کوچک): `clamp(24, 2.4vw × 1024 = 24.6, 34)` → **24.6px** (+20% نسبت به قبل)
- **1280px** (لپ‌تاپ متوسط): `clamp(24, 30.7, 34)` → **30.7px**
- **1440px** (لپ‌تاپ بزرگ): `clamp(24, 34.6, 34)` → **34px** (سقف)
- **1920px** (Full HD): **34px** (سقف)

### چرا این اندازه‌ها ایمن‌اند

- **باریک‌ترین کارت دسکتاپ** با `minmax(220px, 1fr)` واقعاً حدود 220-260px می‌شود.
- عدد میلیاردی رایج (`۱٬۵۰۰٬۰۰۰٬۰۰۰` = 11 کاراکتر) با فونت 34px + letter-spacing کمی منفی حدود 200-215px عرض می‌گیرد. **راحت در 220px جا می‌شود.**
- عدد ۱۳ رقمی (`۹۹۹٬۹۹۹٬۹۹۹٬۹۹۹`) حدود 260-275px عرض. در کارت 220-240px `…` می‌شود و tooltip فعال است (اعداد این ابعاد نادرند).
- در صفحات بزرگ‌تر (1400px+) کارت‌ها هم بزرگ‌تر می‌شوند و همه اعداد راحت جا می‌شوند.

### اثر روی همه ۴۳ کاشی

چون `.sc b` سراسری تغییر کرد، **همه کاشی‌های KPI هاب مالی** بدون استثنا:
- ۱۱ کاشی «سود نقدی» (fiscal.js)
- ۸ کاشی «سرمایه در گردش» (working-capital.js)
- ۱۵+ کاشی «تراز رسمی/غیررسمی/تجمیعی» (ledger-report.js)
- ۴+۳ کاشی «خزانه» (treasury.js)
- ۵ کاشی «نقدینگی تأمین» (supplier-finance.js)

### موبایل بدون تغییر

CSS موبایل (`@media max-width:768px`) از v34.7.99 دست‌نخورده باقی ماند:
- فونت عدد `16px`، padding `12px 10px`، minmax `160px`، unit `62%`

### تست

- `tester498` بروزرسانی شد — **۴۵ سنجش** با انتظارات جدید:
  - سقف 34px، شیب 2.4vw، padding 22px 18px، minmax 220px، unit 52%
- گیت CI: **۱۱۴ PASS / ۰ FAIL** ✅
- نگهبان معماری: **PASS** ✅

### فایل‌ها

- تغییر: `crm/index.html` (فقط ۴ خط CSS `.sr/.sc/.sc b/.sc .unit/.sc span` سراسری)
- تغییر: `_tools/uat/tester498-...` (انتظارات فونت جدید)
- افزوده: `RELEASE-NOTES-v34.7.101.md`
- بامپ نسخه: ۷ نقطهٔ رسمی + ۵۰ تستر. کامنت‌های تاریخی v34.7.93/95/96/97 دست‌نخورده.

### گام بعد

اگر همچنان کوچک باشد، محدودیت واقعی این است که در `minmax(220px)`، هر افزایش فونت باعث `…` روی اعداد ۱۳ رقمی می‌شود. اگر کاربر تمایل به این ترد-آف داشته باشد (کاشی‌های کم‌تعدادتر، درشت‌تر) می‌توانم به `minmax(280px)` بروم که کاشی‌ها بزرگ‌تر و کم‌تعدادتر شوند در ردیف.
