# تریاژ مرحله دوم: شکست‌های UAT مالی و RBAC

**تاریخ:** ۲۰۲۶-۰۸-۱۳  
**نسخه بررسی:** `v34.5.11`  
**دامنه:** شکست‌های دارای اثر بالقوه بر وصول، سود، دسترسی مالی، چک و پرونده فروش در رگرسیون کامل

## نتیجه اجرایی

از ۱۳ تستر اولویت‌دار بررسی‌شده:

| نتیجه | تعداد | توضیح |
|---|---:|---|
| **BUG واقعی رفع‌شده** | ۱ | `tester100`: وصول کامل پیش‌پرداخت ارزی با نرخ پیش‌فرض به اشتباه رد می‌شد. |
| **تست منقضی/قرارداد عمداً تغییرکرده** | ۱۰ | نقش‌های مدیریتی، سیاست سود، ساختار هاب مالی یا قابلیت‌های بعدی محصول تغییر کرده‌اند ولی assertion قدیمی مانده است. |
| **نیازمند تصمیم محصول و UAT انسانی** | ۲ | گزارش روزانه چک منتقل‌شده و قواعد مختومه‌سازی با مطالبات باز. |

بنابراین، «قرمز بودن» این گروه دلیل کافی برای rollback نیست. با این حال، تا زمانی که تست‌های منقضی بازنویسی/آرشیو نشوند، نباید این‌ها را به‌عنوان پوشش معتبر regression حساب کرد.

---

## ۱. باگ واقعی رفع‌شده — UAT-ADV-001

### مورد
`tester100-v182.js` — «وصول پیش‌پرداخت paid می‌شود».

### علت ریشه‌ای
در `advancePaid` برای پیش‌پرداخت ارزی، وقتی کاربر در دیالوگ وصول نرخ جدید وارد نمی‌کرد:

1. نرخ زنده بر نرخ ثبت‌شده داخل پیش‌پرداخت تقدم داشت؛ پس با نوسان نرخ، مبلغ محاسبه‌شده از مانده قراردادی بیشتر می‌شد.
2. در رکوردهای ساختاریافته قدیمی که `remainAmt` هنوز materialize نشده بود، سقف وصول به اشتباه صفر در نظر گرفته می‌شد.

نتیجه: ثبت وصول کامل یک پیش‌پرداخت ارزی معتبر با پیام «مبلغ از مانده پیش‌پرداخت بیشتر است» رد می‌شد.

### اصلاح
در `crm/petty.js`:
- نرخ صریح کاربر ← نرخ ثبت‌شده در پیش‌پرداخت ← نرخ زنده، به ترتیب اولویت شد.
- سقف وصول از `remainAmt` یا، در نبود آن، از `amt` خوانده می‌شود.

### تأیید
`node _tools/uat/tester100-v182.js` → **۲۵ PASS / ۰ FAIL**  
`node --check crm/petty.js` → **PASS**

---

## ۲. طبقه‌بندی تسترها

| تستر | نتیجه تریاژ | شواهد / اقدام لازم |
|---|---|---|
| `tester35-sprint122` | **منقضی؛ سیاست سود تغییر کرده** | تست سود را «فقط وصول واقعی» می‌خواهد. موتور فعلی `fx.js` صراحتاً سود را از مبلغ خالص فاکتور فروش می‌گیرد و وصول‌نشده را اطلاع‌رسانی می‌داند. این یک تغییر سیاست کسب‌وکار است، نه نقص syntax. تست باید با سیاست مصوب سود بازنویسی شود. |
| `tester97-v179` | **منقضی؛ RBAC و مدل حقوق تغییر کرده** | نقش‌های `ceo` و `commercial` با تصمیم AUD-11 به دسترسی کامل سهامداران/مالی افزوده شده‌اند. حقوق اکنون مطالبه (`salary`) و پرداخت آن (`salary_payment`) مدل شده است؛ فرمول کارت تاریخی tester دیگر قرارداد فعلی نیست. تست را بازنویسی کنید، نه اینکه نقش‌ها را محدود کنید. |
| `tester100-v182` | **BUG واقعی، رفع شد** | جزئیات بخش ۱. |
| `tester104-v187` | **تست ساختاری منقضی** | رفتار `ptfPayableUpsert` برای `dueISO/dueNote` در همان tester پاس است؛ شکست فقط دنبال یک عبارت قدیمی در `buycompare.js` می‌گردد. assertion را به قرارداد داده/رفتار منتقل کنید. |
| `tester105-v189` | **تست UI/string منقضی** | قفل post-award و مسیر پرونده فروش در محصول باقی است، اما متن/دکمه‌های literal پس از بازطراحی تغییر کرده‌اند. UAT انسانیِ «پیشنهاد برنده قابل ویرایش نیست و مسیر فاکتور فقط پرونده فروش است» جایگزین assertion متنی شود. |
| `tester106-v190` | **منقضی؛ تغییر عمدی RBAC** | `fiscal.js` اکنون چهار نقش `admin/chairman/ceo/commercial` را طبق AUD-11 مجاز می‌داند. tester با سیاست تاریخی «فقط دو نقش» ناسازگار است. |
| `tester110-v194` | **نیازمند تصمیم محصول/UAT** | کد فعلی مطالبات باز را هشدار نرم می‌داند و امکان تسویه خودکار هنگام مختومه‌سازی دارد. tester قدیمی آن را blocker سخت می‌خواهد. قبل از تغییر، باید مالک مالی سیاست را تأیید کند؛ این یک تصمیم حسابداری است. |
| `tester111-v195` | **منقضی؛ بازنویسی گسترده لازم** | assertionهای CSV، متن قفل و RBAC به ساختار و سیاست v19.5 متکی هستند. نقش‌های مجاز بعداً تغییر کرده و خروجی/پیام‌ها توسعه یافته‌اند. سناریوی رفتاری قفل→اصلاحی→CSV باید با API/خروجی فعلی بازنویسی شود. |
| `tester114-v198` | **تست audit منقضی** | اسکن/پاکسازی یتیم و بخش عمده رفتاری پاس است؛ دو assertion به شکل قدیمی audit وابسته‌اند. log قرارداد فعلی باید بررسی و test با event/شناسه جدید سازگار شود. |
| `tester119-v203` | **منقضی؛ هاب مالی توسعه یافته** | هاب از چهار تب به تب‌های بیشتر (خزانه، کیفیت، چک، تراز و …) گسترش یافته و روش پنهان‌سازی برای موبایل از `style.display` ساده به `setProperty(..., 'important')` تغییر کرده است. تست DOM را با رفتار واقعی بازنویسی کنید. |
| `tester122-v206` | **منقضی؛ RBAC عمدی** | `petty.js` نقش‌های مدیریتی را به `admin/chairman/ceo/commercial` توسعه داده است؛ متن/شرط tester قدیمی است. باید سناریوهای مجاز و غیرمجاز فعلی (از جمله treasurer و ثبت هزینه شخصی) تست شوند. |
| `tester143-v262-supplier-finance` | **منقضی؛ قابلیت بعداً اضافه شده** | جمله «پرداخت و چک در Sprint 263 و 264» در tester، عمداً پس از تکمیل sprintهای 263/264 دیگر برقرار نیست. این assertion باید حذف و پوشش پرداخت/چک به تسترهای تخصصی منتقل شود. |
| `tester145-v264-cheque-transfer` | **نیازمند تصمیم محصول/UAT** | موتور یادآور چک در `cheques.js` انتقال/مالکیت ثالث را حذف می‌کند؛ اما `myday.js` کنونی دیگر اصلاً آیتم چک تولید نمی‌کند. باید مشخص شود آیا «روز من» باید دوباره یادآور چک داشته باشد یا حذف آن تصمیم محصول بوده است. تا تأیید، این تست نه BUG قطعی است و نه پوشش معتبر. |

---

## ۳. تصمیم‌های مورد نیاز از مالک محصول/مالی

1. **سیاست سود:** مبنا مبلغ خالص فاکتور است یا فقط دریافت نقدی؟ کد فعلی اولی را پیاده می‌کند.
2. **مختومه‌سازی با مطالبات باز:** هشدار نرم + تسویه خودکار (رفتار فعلی) یا blocker سخت (تستر تاریخی)؟
3. **روز من و چک‌ها:** آیا چک‌های سررسیدشونده باید دوباره در Dashboard دیده شوند؟ در صورت بله، تست نقش‌محور برای چک شرکت/ثالث/منتقل‌شده لازم است.

تا زمان پاسخ، این سه مورد نباید با حدس فنی تغییر کنند.

---

## ۴. اقدام بعدی پیشنهادی

1. `tester100` را در یک اجرای کامل رگرسیون دوباره تأیید کنید.
2. ۱۰ تستر منقضی را با قراردادهای فعلی بازنویسی یا به `archive/` منتقل کنید؛ هر دو حالت باید در changelog تریاژ ثبت شود.
3. برای سه مورد تصمیم محصول، سناریوی دستی کوتاه با امضای مالک مالی ثبت کنید.
4. بعد از تریاژ، فقط تست‌های معتبرِ حساس به مالی/RBAC را به گیت CI متمرکز اضافه کنید.
