# RELEASE NOTES — v34.27.0 (کش‌سنجی + پل سرچ کنسول در CMS)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** tester573 (۱۹ سنجه) · **گیت:** 189 PASS / 0 FAIL

## گزارش‌ها و درخواست‌ها

«تب صفحهٔ جدید هنوز خالی است، همچنین تب کیفیت» + «بعد از انتشار باید بتوان سایت‌مپ را آپدیت و در سرچ کنسول بارگذاری کرد» + «درخواست ایندکس صفحات ایندکس‌نشده با یک کلیک».

## ① تب‌های خالی — لایهٔ سوم تشخیص: کش سرویس‌ورکر

کد رندر هر دو تب سالم است و گاردهای v34.26.1 (پیام خطا/فرم جایگزین) هم اگر اجرا می‌شدند، تب خالی نمی‌گذاشتند. قوی‌ترین فرضیهٔ باقی‌مانده: **مرورگر cms.js قدیمیِ کش‌شده (استراتژی SWR سرویس‌ورکر) را اجرا می‌کند** — نسخه‌ای پیش از گاردها. v34.27.0:

- هر فایل cms.js نسخهٔ خودش را ثبت می‌کند (`PTF_CMS_JS_VER`) و با نسخهٔ پوسته مقایسه می‌شود؛ در ناهماهنگی، **بنر قرمز بالای صفحه** با نسخهٔ کش و سرور + دکمهٔ «🧹 پاک‌سازی کش و بارگذاری مجدد» (حذف ثبت SW + حذف caches + ریلود).
- گارد سوم: خود مسیر `goPanel('cms')` (ساخت پنل + رندر) هم try/catch شد — هیچ خطایی دیگر پنل را بی‌پیام خالی نمی‌گذارد.

**بعد از دیپلوی:** اگر بنر قرمز آمد → همان دکمهٔ پاک‌سازی را بزنید؛ اگر تب باز هم خالی و بنر هم نیست → F12←Console را بفرستید.

## ② 📤 سایت‌مپ + سرچ کنسول — بله، و حالا یک‌کلیکی در خود CMS

- ثبت خودکار پس از هر انتشار (صفحه/محصول/مقاله) از قبل موجود است (اگر «ثبت خودکار نقشه» روشن باشد) و بررسی زندهٔ امروز نشان داد sitemap-index سرور، زیرنقشهٔ محصولات را درست شامل می‌شود.
- **جدید:** دکمهٔ «📤 سایت‌مپ + سرچ کنسول» در نوار ابزار تب سئو — sitemap-index (با همهٔ زیرنقشه‌ها) را در سرچ کنسول ثبت/به‌روزرسانی می‌کند و وضعیت/خطا/آخرین زمان دانلود گوگل را گزارش می‌دهد.

## ③ 🚀 ایندکس‌یاب — بررسی و درخواست ایندکس صفحات

دکمهٔ «🚀 ایندکس‌یاب» در تب سئو:
- فهرست صفحات سایت را می‌گیرد (چک‌باکس، پیش‌فرض ۱۰ مورد اول) و با **API رسمی URL Inspection گوگل** وضعیت ایندکس هر صفحه را می‌خواند (متوالی، سقف ۲۵ صفحه در هر اجرا برای حفظ سهمیهٔ روزانه ۲۰۰۰؛ هر بررسی در تاریخچهٔ پایش ثبت می‌شود).
- ایندکس‌شده‌ها ✅ سبز؛ ایندکس‌نشده‌ها ⛔ با علت (verdict/coverage) + دکمهٔ «درخواست ایندکس ↗» که مستقیم صفحهٔ رسمی Request Indexing همان URL را باز می‌کند.
- صداقت فنی: گوگل برای «Request Indexing» **API عمومی ندارد** (فقط JobPosting/BroadcastEvent)؛ بنابراین نزدیک‌ترین اتوماسیون ممکن = همین مسیر (بازرسی رسمی + پیوند مستقیم + بازارسال سایت‌مپ که داخل ویزارد هم هست).

## فایل‌های کلیدی

`crm/cms.js` (PTF_CMS_JS_VER + cmsCachePurge + گارد goPanel + cmsGsc/cmsSeoSitemapPush/cmsIndexWizard) · `_tools/uat/tester573-v34.27.0-gsc-bridge-cache.js`
