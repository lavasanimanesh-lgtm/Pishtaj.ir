# 🚀 ریلیز v78.0 — اسپرینت ۷۸: کاربران سروری (US-151 فاز ۱)
**تاریخ:** تیر ۱۴۰۵ | 🐛 رفع باگ گزارش‌شده شما: «کاربر تعریف‌شده در کروم، از فایرفاکس/موبایل خطای یوزر اشتباه می‌گیرد»

---

## 🔍 ریشه باگ
تشخیص خودتان کاملاً درست بود — یوزر/رمز مشکلی نداشت. کاربران CRM تا این نسخه فقط در **localStorage همان مرورگری** ذخیره می‌شدند که ادمین آنها را تعریف کرده بود. فایرفاکس یا موبایل، حافظه جدا دارند و اصلاً از وجود آن کاربر خبر نداشتند. این دقیقاً همان یافته S1 ممیزی تیم بود که طبق اولویت‌بندی شما جلو کشیده شد.

## ✅ راه‌حل — کاربران روی سرور (فاز ۱ از US-151)

| جریان | رفتار جدید |
|---|---|
| **ثبت کاربر توسط ادمین** | بلافاصله به سرور هم ارسال می‌شود (`crm/data/crm_users.json` — پوشه محافظت‌شده) |
| **حذف کاربر** | از سرور هم حذف می‌شود — در همه دستگاه‌ها بی‌اثر می‌شود |
| **ورود از مرورگر/دستگاه جدید** | اگر کاربر محلی پیدا نشد، پیام «⏳ در حال بررسی کاربران سرور...» → دریافت از سرور → ورود موفق ✅ |
| **اولین ورود ادمین پس از این آپدیت** | کاربران موجودِ مرورگر فعلی **خودکار** به سرور seed می‌شوند (یک بار در هر session) |

### نکات امنیتی
- فقط **هش SHA-256 رمز** به سرور می‌رود و برمی‌گردد — رمز خام هرگز جایی ذخیره یا منتقل نمی‌شود.
- ذخیره‌سازی در پوشه `crm/data/` که با htaccess به‌طور کامل از وب مسدود است.
- آپلود لیست کاربران فقط با نقش ادمین/رییس (گارد سروری users_write).
- کاربرانِ فقط-محلی هنگام ادغام حذف نمی‌شوند (بدون از دست رفتن داده).

## 📋 دستورالعمل شما بعد از آپلود (مهم — ۱ دقیقه)
1. `pishtaj-release-v78-delta.zip` را آپلود کنید.
2. **با کروم** (همان مرورگری که کاربران را ساخته‌اید) یک بار وارد CRM شوید — همین ورود، کاربران را به سرور منتقل می‌کند (در لاگ داشبورد: «کاربران با سرور همگام شدند»).
3. حالا از **فایرفاکس یا موبایل** با همان یوزر/رمز کاربر وارد شوید → باید بدون خطا وارد شود ✅

## 🔭 فاز ۲ (اسپرینت ۷۹ — در حال اجرا)
این فاز فقط «کاربران» را سروری کرد. فاز بعدی همین US-151، **کل داده‌های CRM** (استعلام‌ها، پیشنهادها، مشتریان و…) را سروری و بین دستگاه‌ها همگام می‌کند + session واقعی سمت سرور.

## 🧪 کیفیت
| مورد | نتیجه |
|---|:---:|
| tester15 جدید (شبیه‌سازی کروم→فایرفاکس) | ✅ ۲۳/۲۳ |
| رگرسیون tester1–14 | ✅ همه PASS |
| ممیز ۸بخشی | ✅ PASS |

## 📁 دلتا (کوچک)
`api/crm.php` • `crm/rbac.js` • `crm/index.html` • `crm/sw.js` + ۴ تصویر محصول و ۴ صفحه محصول (تثبیت مجدد v76)
