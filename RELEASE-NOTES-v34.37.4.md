# یادداشت انتشار — v34.37.4

**تاریخ:** ۲۰۲۶-۰۹-۰۵ (۱۴۰۵/۰۶/۱۴) · **پایه:** v34.37.3
**گیت CI:** ۲۱۶ PASS / ۰ FAIL (شامل tester601 = ۲۱ سنجش، دو آزمون جهش) · **نگهبان معماری:** PASS
**ارجاع اسناد:** `ARENA-CRM-INVOICE-PANEL-ASSESSMENT-2026-09-05.md` (گام ۲ِ همان خانواده — این‌بار نه «داده نمی‌رسد» که «کدِ اشتباه اجرا می‌شود»)

گزارش کارفرما:

> «الان با ادمین که وارد می‌شوم فاکتورها را در بخش فاکتورها می‌بینم اما وقتی با اکانت رییس هیات مدیره
> وارد می‌شوم فهرست را نمی‌بینم.» — همراه با پنج‌تکرارِ این خطا در کنسول:
> `rbac.js:954 Uncaught TypeError: Cannot read properties of undefined (reading 'offerCurrency') at renderInvoices … at window._ptfPanelBuilders.inv (perms.js:69) … at window.goPanel (perms.js:56)`

---

## ① ریشهٔ علت — سه حلقه، هر سه لازم برای وقوع باگ

| # | حلقه | کجا | نقش |
|---|---|---|---|
| ۱ | **میان‌برِ override** | `crm/perms.js` — wrapper روی `goPanel`: اگر کاربر برای پنلی override `'allow'` داشته باشد، مسیر عادی **دور زده** می‌شود و `window._ptfPanelBuilders[id](btn)` اجرا می‌شود | اکانت رییس «شخصی» شده بود (برچسب «شخصی» در مودال 🎛 دسترسی)؛ ادمین override ندارد ⇒ او هرگز وارد این مسیر نمی‌شد — همان تفاوتِ «ادمین می‌بیند، رییس نمی‌بیند» |
| ۲ | **capture زودهنگام** | همان فایل: بیلدرها در `setTimeout(1200ms)` با **ارجاعِ** `window.buildInvoices` / `window.renderInvoices` ثبت می‌شدند. پنل زنده (`official-invoice-v2.js`) در `index.html` حدود ۶۵ اسکریپت **بعد** از `perms.js` با `defer` می‌آید؛ در لودِ سرد/کند (کش‌باستر `?v=` بعد از هر انتشار = لود سرد؛ موبایل/ADSL = کند) تایمر ۱۲۰۰ms **پیش از** اجرای v2 می‌سوخت ⇒ بیلدر برای کل آن نشست به پنلِ **بازنشستۀ `rbac.js`** میخکوب می‌شد | کامنت «این پنل اجرا نمی‌شود» در rbac.js دقیقاً همین‌جا **نقض** شد — همان‌طور که خود کامنت هشدار داده بود («یک‌بار واقعاً گاز گرفت») |
| ۳ | **فرضِ شکسته در کد بازنشسته** | `crm/rbac.js:954` — `renderInvoices` بازنشسته برای هر «پیش‌فاکتور ارجاع‌شده» فرض می‌کرد فاکتورِ مطابقت‌یافته حتماً وجود دارد و `inv.offerCurrency` را بی‌گارد می‌خواند؛ برای ارجاعِ بی‌فاکتور (همهٔ فاکتورها ابطال‌شده / هنوز به این دستگاه سینک‌نشده / `offerNo` کج — همان خانواده‌ای که v34.37.2 برای **v2** با «⏳ بدون فاکتور» و بخش یتیم‌ها تحمل‌پذیر کرد) `inv === undefined` ⇒ `TypeError` ⇒ `innerHTML` هرگز نوشته نمی‌شود ⇒ **فهرست خالی** | خطا تکرار می‌شد چون هر تلاشِ رندر (کلیک، بازتلاش خودکار، رفرش پنل) از همان بیلدرِ میخکوب شروع می‌شد |

هیچ‌کدام از سه حلقه به‌تنهایی باگ را نمی‌ساخت؛ به همین دلیل تسترهای رفتاری پنل (که همه از مسیر عادی/پنل v2 می‌گذشتند) سبز بودند و باگ فقط روی دستگاهِ «کاربرِ دارای override + لود سرد» ظاهر می‌شد.

## ② آنچه عوض شد (فقط لایۀ اتصال UI — سرور/داده/مجوز دست‌نخورده)

**۱) `crm/perms.js` — دیرهنگام‌بندی (PERM-ALLOW-LATE-BIND).**
`reg()` دیگر *نام* توابع را می‌گیرد (`buildInvoices`/`renderInvoices`/…) و **در لحظۀ کلیک** از `window` می‌خواند؛ ترتیب و زمان بارگذاری ماژول‌ها بی‌اثر می‌شود. ثبت بیلدرها از تایمر ۱۲۰۰ms به **فوری** منتقل شد (تایمر به‌عنوان re-register بی‌ضرر/idempotent ماند) تا «کلیک زیر ۱۲۰۰ms» هم مسیر allow را داشته باشد؛ اگر ماژول پنل اصلاً بارگذاری نشده باشد، کاربر پیام صادقانه می‌بیند («ماژول این پنل هنوز بارگذاری نشده است») — طبق قرارداد ۲ نگهبان معمولا، نه رندر نیمه و نه سکوت. این اصلاح **هرشت پنل‌های گارددار** را یک‌جا از خانوادهٔ باگِ capture نجات می‌دهد (let/cms/sms/rep/**inv**/recv/buyq/taxret).

**۲) `crm/rbac.js` — گارد تهی در رندر بازنشسته (INV-LEGACY-NULLGUARD).**
سه خوانشِ `o.currency || inv.offerCurrency` در سطر «مبلغ CO» به `(o.currency || (inv && inv.offerCurrency))` تبدیل شد. این کد باید بی‌اجرا بماند، اما وقتی **فقط** در حالت نبودِ v2 (خطای شبکه/404/مسدودکنندهٔ افزونه) اجرا می‌شود، نباید «ارجاع بی‌فاکتور» کل پنل را با throw بخورد؛ اکنون همان ردیف با دکمۀ «+ ثبت فاکتور صادره» دیده می‌شود. هدرِ بلاکِ بازنشسته به‌روز شد تا داستانِ واقعیِ «چرا یک‌بار اجرا شد» گم نشود.

**۳) نسخه در هر هفت نقطۀ رسمی بامپ شد** (`?v=` در `crm/index.html` ×۱۰۹ + `PTF_CRM_RELEASE` + `sw.js` سه‌گانه + `manifest.json` + `clear-cache.html` + `shell.js` + `SD_SERVICE_VERSION`) و پینِ تسترها با `bump-version-pins.js` (۱۴۶ فایل / ۷۴۲ مورد).

## ③ آنچه **عمداً** عوض نشد

- مسیر عادی `goPanel` (rbac.js:1600) همان‌طور که بود — چون تابع را **به نام** در لحظه صدا می‌زند، هیچ‌وقت مصونۀ capture نبود؛ به همین دلیل ادمین سالم می‌دید.
- `official-invoice-v2.js` (پنل زنده) — رفتار v2 در این گزارش درست بود.
- `invRef`/`offerNo`، مدل داده، گاردهای سروری، مجوزها، ساختار `ptf_crm_perms`.

## ④ عیب‌یابیِ سریع روی میزبان (برای اطمینان از همان ریشه در دستگاه‌های دیگر)

| نشانه | معنی |
|---|---|
| کنسول: `TypeError ... 'offerCurrency'` با استک `perms.js` → `rbac.js` | همین باگ؛ تازه‌سازی (Reload) معمولاً یک‌بار کافی است چون این‌بار v2 از کش زودتر اجرا می‌شود — ولی با v34.37.4 دیگر لازم نیست |
| کاربر در «🎛 دسترسی» برای فاکتورها برچسب «شخصی» دارد | همان میان‌بر allow؛ «↩️ بازگشت به پیش‌فرض نقش» راه‌حلِ بی‌کدِ موقت است |
| ریسید‌نِ ردیف‌ها در نسخهٔ کنونیِ دیپلوی‌شده با خطای بالا | نسخهٔ میزبان ≤ 34.37.3 ⇒ استقرار v34.37.4 |

## ⑤ تست‌ها

`_tools/uat/tester601-v34.37.4-perm-allow-late-bind.js` — **رفتاری، روی کد واقعی** در `vm` (قرارداد tester598/599/600):

| گروه | سنجش‌ها |
|---|---|
| ۱. late-bind | allow-override → با بازنویسیِ دیرهنگامِ v2، **رندرِ جاری** اجرا می‌شود؛ بیلدرها پیش از سوختن تایمر ثبت‌اند؛ مسیر allow پایه را دور می‌زند |
| ۲. بدون رگرسیون | deny → alert و صفر رندر؛ بدون override → مسیر عادی؛ هر ۸ پنل ثبت‌شده؛ ماژول غایب → پیام صادقانه به‌جای crash |
| ۳. آزمون جهش | منبع با منطقِ **ثبت‌زودهنگام** (capture) جایگزین شد ⇒ همان تست ۱ **قرمز** می‌شود (قفل روی پنل بازنشسته بازتولید می‌گردد) — بارِ تشخیص اثبات‌شده |
| ۴. گارد تهی | «ارجاع بی‌فاکتور» در rbac.js بدون throw رندر می‌شود؛ سناریوی EURِ سالم رگرسیون ندارد؛ حذف گارد ⇒ دقیقاً همان `TypeError ... offerCurrency` برمی‌گردد |
| ۵. قراردادها | امضای regِ نام‌محور، نبودِ capture در فایل، توالی perms→v2 در index.html، سازوکار بازنویسی v2، ارجاع مستندِ درخت |

**گیت CI:** `=== CI gate: 216 PASS / 0 FAIL ===` و `=== ARCH GUARD: PASS ===` (اجرای ۲۰۲۶-۰۹-۰۵، پس از بامپ نسخه و اصلاح دو پین: tester482 → «قراردادِ نام‌محور» به‌جای شکلِ `reg('taxret', …)`؛ نشانگر سپرِ کهنگی `PTF_CMS_JS_VER` در `crm/cms.js` هم‌راستا با VER پوسته شد — همان چیزی که tester573/578 می‌سنجند).

## ⑥ یادداشتِ عملیاتی برای تیم

- پس از استقرار، **یک‌بار Reload** روی دستگاه رییس کافی است تا کش سرویس‌ورکر با `v34.37.4` عوض شود؛ حتی پیش از آن هم با این نسخه «ترتیبِ لود» دیگر سرنوشت‌ساز نیست.
- اگر خواستند override شخصیِ آن کاربر را پاک کنند: «👥 کاربران ← 🎛 دسترسی ← ↩️ بازگشت به پیش‌فرض نقش» — ولی دیگر برای درست‌دیدنِ پنل لازم نیست.
- الگوی کلی: هر بیلدرِ پنلی که به توابع روی `window` بسته است باید **به نام** و در لحظۀ اجرا گشوده شود؛ capture در تایمِر = خانوادهٔ باگِ میخکوب‌شدن (سه بار در این پروژه گاز گرفته شد: دکمهٔ لغوِ ارجاع v34.37.0، رندرِ بازنشستۀ امروز، و «rep» که insights/reports همدیگر را بازنویسی می‌کنند).
