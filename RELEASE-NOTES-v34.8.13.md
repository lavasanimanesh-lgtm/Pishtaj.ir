# یادداشت انتشار CRM v34.8.13

تاریخ: ۱۴۰۵/۰۶/۵ (۲۰۲۶-۰۸-۲۷)

## فاز C2 — زیرساخت فرمان عمومی موجودیت (مسیر نازک نوشتن)

تعمیم الگوی موفق مالی (v34.8.6) به کل CRM:

### سرور (`api/sales-domain.php`)
- `sd_entity_registry()`: رجیستری موجودیت‌های فرمان‌محور — نقش‌های مجاز + فیلد هویت per-collection؛ فعال‌سازی تدریجی، غیرفعال = مسیر legacy بدون تغییر
- فرمان‌های `entity_upsert` / `entity_delete`: از journal/idempotency/WAL موجود عبور می‌کنند؛ sanitizer ردیف (سقف ۴۰ کلید/۲۰۰۰ کاراکتر/یک سطح تودرتو)؛ createdAt/createdBy سروری
- حذف = tombstone عمومی (`archive_purge` با `identities`) روی همان مکانیزم موجود client/server — دستگاه‌های stale رکورد حذف‌شده را زنده نمی‌کنند؛ حذف idempotent است (`alreadyDeleted`)
- دفاع عمیق: مجموعهٔ غیرفعال → 404؛ نقش غیرمجاز → 403

### کلاینت (`crm/sales-domain-v2.js` + پایلوت `crm/leads.js`)
- `ptfEntityUpsert` / `ptfEntityDelete` + پرچم `PTF_ENTITY_CMD_ENABLED` per-collection
- اعمال پاسخ با `ptfBApplyServerProjection` — **بدون dirty/صف/push** = مسیر نازک واقعی؛ fallback امن به legacy وقتی فاز B خاموش است
- پایلوت: «انجام‌شده/تعویق/حذف» یادآورها حالا فرمان اتمیک سروری است (اولین ماژول خارج از مالی)

### گام بعدی (C3)
مهاجرت ماژول‌به‌ماژول بر اساس دادهٔ تله‌متری C1 (فروش/فاکتور/درخواست…) — هر ماژول همان روز از تعارض و پوش مجموعه‌ای خلاص می‌شود.

### کنترل کیفیت
tester517 (سرور + کلاینت + پایلوت + شبیه‌سازی رفتاری) — گیت CI سبز؛ نگهبان معماری سبز.
