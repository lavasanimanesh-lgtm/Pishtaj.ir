# فاز ۱۵ (v34.0.18-alpha) — رفع NoSuchKey در فایل‌های ضمیمه

**برنچ:** `arena/019fcd1d-pishtaj-ir`
**گزارش کارفرما (پروداکشن):** فایل‌های ضمیمه قابل خواندن نیستند؛ خطای `NoSuchKey` با کلیدهایی مثل `ptf-crm-filestx0000026ae0e5cbfaa2735-006a727265-b62a192f-ir-thr-at1...` داده می‌شود.

---

## ریشه
- کلیدِ خطا مربوط به یک **ساختار ذخیره‌سازی قدیمی/مهاجرت‌نشده** (احتمالاً MySQL BLOB با prefix `filestx`) است که در باکت آروان `ptf-crm-files` **وجود ندارد**.
- `presign_get` بدون بررسی، کلید را امضا و URL برمی‌گرداند؛ وقتی مرورگر روی URL کلیک میکند، S3 با **`NoSuchKey`** جواب میدهد (کلید در باکت نیست).
- `openStoredFile` پیام عمومی «خطا در دریافت لینک» می‌داد، نه توضیحی که به کاربر بگوید مشکل چیست.

---

## اصلاحات

### `api/storage.php`
- `presign_get` حالا **با HEAD** وجود فایل را در باکت بررسی می‌کند:
  - `404/403` → `{ ok:false, error:'file_not_found', detail:'کلید قدیمی/مهاجرت‌نشده' }`.
  - اگر HEAD پشتیبانی نشد (۵xx)، کلید امضا و برمی‌گردد (همان رفتار قبلی).
- `s3_request` پشتیبانی HEAD (`CURLOPT_NOBODY`).

### `crm/storage.js`
- `openStoredFile` حالا `file_not_found` را تشخیص میدهد و پیام واضح «فایل در فضای ابری یافت نشد (کلید قدیمی/مهاجرت‌نشده) — لطفاً سند را دوباره آپلود کنید» نشان میدهد.

---

## نکتهٔ مهم
این اصلاح، **نمایش خطای معنادار** را فراهم می‌کند (به‌جای NoSuchKeyِ خام). اما برای **بازیابیِ فایل‌های قدیمی** که در آروان نیستند، باید:
1. فایل‌های قدیمی را از مبدأ قدیمی (MySQL/هاست) به باکت آروان منتقل کنید (مهاجرت), **یا**
2. برای رکوردهای قدیمی، سند را دوباره آپلود کنید.

بعد از این اصلاح، کاربر پیام واضح «دوباره آپلود کنید» می‌بیند به‌جای خطای مبهم.

---

## تست‌ها
- **tester314** جدید (۶/۰): HEAD check + file_not_found + پیام معنادار.
- رگرسیون کامل سبز (۲۴ تستر، ۰ شکست).
- بامپ نسخه → **v34.0.18-alpha**.
- تغییرات `.github/workflows/*` در PENDING-workflow-changes ثبت شد.

*تولیدشده توسط Arena Agent — رفع NoSuchKey با بررسی وجود فایل و پیام معنادار.*
