# 📦 ریلیزنوت v123.2 — US-297..300: ورژن واقعی، انتقال تغییر رمز، «دستیار» زیر کارتابل + دسترسی سریع از مودال‌ها

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v123.1

---

## 🔢 US-297 — ورژن واقعی در سایدبار
«CRM v6.0» هاردکد زیر نام شرکت حذف شد؛ حالا از `window.VER` واقعی پر می‌شود (این نسخه: **CRM v123.2**) و از این پس با هر ریلیز خودکار به‌روز است — دیگر هیچ‌وقت ورژن دروغ نمی‌گوید.

## 🔑 US-299 — تغییر رمز عبور → منوی تنظیمات
- دکمه «تغییر رمز عبور» از پایین سایدبار **حذف** شد (سایدبار خلوت‌تر؛ فقط «خروج» ماند)
- در **تنظیمات**، باکس جدید «🔐 امنیت حساب» با دکمه تغییر رمز اضافه شد
- منطق تغییر رمز (کاربر عادی + ادمین + سینک بین دستگاه‌ها) بدون هیچ تغییری

## 🤖 US-298 — «دستیار» زیر کارتابل
- «ابزار هوش مصنوعی» → **«دستیار»** (سایدبار + عنوان پنل) با آیکون 🤖
- جایگاه: در بالای منو، **دقیقاً زیر «کارتابل من»** (گروه داشبورد) — قبلاً وسط لیست فروش گم بود
- استایل متمایز بنفش و بج AI حفظ شد

## ⚡ US-300 — دسترسی سریع دستیار از مودال‌های مرتبط
روی مودال‌های مرتبط (ثبت/ویرایش **کالا**، **پیشنهاد فنی TO / مالی CO / TC**، **استعلام**، **نامه**، **قرارداد**، **فاکتور**) چیپ بنفش **«🤖 دستیار»** کنار دکمه‌های مک ظاهر می‌شود:

1. کلیک → پنجره فعلی **با تمام اطلاعات واردشده** به دیسک پایین صفحه می‌رود (همان مکانیزم دایره زرد)
2. بلافاصله پنل دستیار باز می‌شود
3. بعد از کار با دستیار، با یک کلیک روی دیسک، پنجره با همه داده‌ها برمی‌گردد

**نکته فنی مهم:** مودال هنگام مینیمایز به `body` منتقل می‌شود تا ناوبری بین پنل‌ها (که محتوای panels را بازنویسی می‌کند) آن را از بین نبرد — بدون این، اطلاعات فرم با رفتن به دستیار نابود می‌شد. مودال‌های نامرتبط (مثل تغییر رمز) چیپ نمی‌گیرند.

## ✅ راستی‌آزمایی موارد تکراری پیام شما (v123.1)
- «مشتریان بالقوه → سرنخ‌ها»: از v123.1 انجام شده و برقرار است ✅
- فیلد «سمت گیرنده» زیر نام در نامه: از v123.1 انجام شده و برقرار است ✅
(چک دائمی هر دو به تستر ۴۳ اضافه شد تا هرگز رگرس نشوند.)

## 🧪 تست عملکردی (شبیه‌ساز مرورگر)
چیپ روی مودال کالا/پیشنهاد مالی/نامه ✅ · مودال رمز عبور بدون چیپ ✅ · کلیک چیپ: ناوبری به دستیار + انتقال مودال به body + داده فرم سالم + دیسک در داک ✅ · برگشت با کلیک دیسک ✅

## 🧪 کیفیت
audit ✅ ۰ خطا | تستر جدید `tester43` ۱۹/۱۹ ✅ | رگرسیون **۳۰ تستر** ✅ (tester6 با ساختار جدید گروه داشبورد به‌روز شد)

## 📁 فایل‌های تغییریافته
`crm/index.html` (sbVer + دستیار + VER v123.2) · `crm/shell.js` (گروه g-dash) · `crm/inqreader.js` (انتقال تغییر رمز) · `crm/modalx.js` (چیپ mx-ai + mxMinimize مشترک + بقای body) · `crm/sw.js` · `_tools/uat/tester43` (جدید)
