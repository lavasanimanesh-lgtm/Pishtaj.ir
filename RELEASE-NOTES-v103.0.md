# 🚀 یادداشت‌های انتشار اسپرینت ۱۰۳ (Release Notes - v103.0)

**تاریخ انتشار:** ۱۴۰۵/۴/۱۵ (July 2026)  
**هدف اسپرینت:** آزادسازی سیستم و حذف قفل‌های ضدچابکی (اولویت ۱ بک‌لاگ محصول)  
**تیم توسعه:** تیم ارشد مهندسی و تجربه کاربری PTF  

---

## 📋 تغییرات اعمال‌شده در این نسخه

### ۱. 🔓 حذف گارد مرکزی ممانعت از حذف (`ptfDeleteGuard`)
* **فایل تغییر یافته:** `crm/guards.js`
* **شرح تغییر:** کدهای ضدچابکی که در صورت وجود وابستگی (مانند وجود پیشنهاد برای استعلام یا فاکتور برای مشتری)، مانع حذف رکورد توسط اعضای تیم یا ادمین می‌شدند، به‌طور کامل برداشته شد. تیم اکنون می‌تواند بدون برخورد با خطاهای بن‌بست‌ساز، رکوردهای اضافه، تکراری یا منسوخ را حذف و مدیریت کند.

### ۲. ✏️ آزادسازی ویرایش و اضافه شدن قابلیت حذف استعلامات (RFQ)
* **فایل تغییر یافته:** `crm/workflow.js`
* **شرح تغییر:** اورراید قبلی تابع `editRfq` که دکمه «📄 تغییر» را به یک صفحه فقط‌خواندنی تبدیل می‌کرد و پیام «قابل تغییر دستی نیست» را نمایش می‌داد، حذف شد. 
* اکنون کلیک روی دکمه ویرایش در فهرست استعلامات، فرم کامل ویرایش شامل **نام شرکت/مشتری**، **مسئول/رابط**، **حوزه کاری**، **موضوع/شرح** و **وضعیت دستی (از دریافت اولیه تا تحویل شده)** را به همراه دکمه‌های **«💾 ذخیره تغییرات»** و **«🗑 حذف درخواست»** در اختیار کاربر قرار می‌دهد.

### ۳. 📦 حذف قفل فیلدهای شرح و واحد کالا در پیشنهادهای فنی (TO) و مالی (CO)
* **فایل تغییر یافته:** `crm/offerlock.js` (فیچر US-201 و US-202)
* **شرح تغییر:** خاصیت `readonly` از فیلدهای «شرح کالا» و «واحد کالا» هنگام درج کالا از کاتالوگ برداشته شد. کارشناسان مهندسی بازرگانی و فروش اکنون می‌توانند پس از انتخاب کد کالا، متن شرح و واحد را به تناسب نیاز مشتری و پروژه آزادانه ویرایش و سفارشی‌سازی کنند.

### ۴. 👥 آزادسازی انتخاب مشتری در پیشنهادها
* **فایل تغییر یافته:** `crm/offers.js`
* **شرح تغییر:** قفل غیرفعال‌سازی لیست کشویی انتخاب مشتری (`buyerSel.disabled = true`) در زمان صدور پیشنهاد برداشته شد.

---

## 🧪 وضعیت کنترل کیفیت (QA Verification)
* کلیه فایل‌های جاوااسکریپت اصلاح‌شده از نظر سینتکس و یکپارچگی ارزیابی شدند (Zero Syntax Error).
* تست‌های رگرسیون (Regression Tests) برای اطمینان از صحت عملکرد سایر ماژول‌ها انجام گرفت.
