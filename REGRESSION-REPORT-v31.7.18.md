# 📊 گزارش رگرسیون v31.7.18

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.18
**ابزار:** `_tools/run-full-regression.js` + `python3 _tools/audit.py` (۱۰ بخش)

## ✅ نتیجه نهایی

| معیار | مقدار | وضعیت |
|--------|-------|--------|
| تعداد تسترها | **۱۷۱** (+۱: tester193-nav-orphan-header) | — |
| فایل‌های PASS | **۱۷۱** | ✅ ۱۰۰٪ |
| چک‌های FAIL | **۰** | ✅ صفر |
| audit.py (۱۰ بخش) | **PASS** | ✅ |
| `node --check` (`shell.js`, `mobilenav.js`, `theme.js`) | **PASS** | ✅ |

## 🆕 پوشش تست جدید (tester193)

- surplus در GROUPS + گارد ریشه‌ای «orphan به انتهای منو» + **تست رفتاری**: مقایسه همه `goPanel(...)`های index.html با GROUPS → صفر orphan
- هدر موبایل: بج نسخه nowrap، `min-width:0`، `overflow:hidden`، آیکون ۳۶px با svg وسط، مهار trial bar، عنوان 40vw
- وسط‌چین سراسری tbic (دسکتاپ)
- رگرسیون: آکاردئون (باز-ماندن گروه در localStorage)، انیمیشن‌های v31.7.17، CSS بحرانی FOUC

## 📌 درس ثبت‌شده برای گیت

شکاف «HTML خام vs DOM بازسازی‌شده»: تسترهای قبلی وجود دکمه در index.html را چک می‌کردند اما آکاردئون آن را حذف می‌کرد. tester193 اکنون هم‌پوشانی GROUPS با کل پنل‌ها را چک می‌کند و گارد کد، حذف بی‌صدا را ناممکن کرده است.

## 📁 فایل‌های تغییریافته

`crm/shell.js`، `crm/mobilenav.js`، `crm/theme.js`، `crm/index.html` (bump)، `crm/sw.js`، `crm/clear-cache.html`، `_tools/uat/tester193` (جدید)

## 🚦 وضعیت گیت‌ها

- **گیت فنی: سبز** ✅
- **گیت انسانی: معوق** ⏳ — تأیید بصری کارفرما روی همان دو صحنه اسکرین‌شات (کشوی «سایر» + هدر) پس از استقرار.
