# RELEASE NOTES — v34.8.0 (۱۴۰۵/۰۶/۰۲ — 2026-08-24)

## مطالبات و وصولی‌های صرفاً ریالی

### قاعدهٔ مالی

پیشنهاد یا قرارداد می‌تواند ارزی باشد، اما فاکتور فروش رسمی به ریال صادر می‌شود. از این نسخه، مبلغ فاکتور، وصول، مانده و درآمد فروش در محاسبهٔ سود فقط با مبالغ ریالی قطعی محاسبه می‌شوند. ارز پیشنهاد و منطق ارزی خرید/تأمین‌کننده تغییری نکرده‌اند.

### اصلاحات رابط کاربری

- جدول و خلاصهٔ «اسناد ارزی» از پنل مطالبات حذف شد.
- معادل ارزی وصول‌شده، ماندهٔ ارزی، نرخ میانگین و نرخ تسعیر فروش دیگر نمایش داده نمی‌شوند.
- نمایشگر درصد وصول حذف شد؛ هر کارت مبلغ فاکتور، مبلغ وصولی و ماندهٔ ریالی را نشان می‌دهد.
- در تسویهٔ کامل، ماندهٔ صفر و نشان «تسویه کامل» نمایش داده می‌شود و سررسید دیگر نشان داده نمی‌شود.
- فرم ثبت دریافت پرونده و fallback نسخه‌های قدیمی فقط ورودی مبلغ ریالی دارند؛ در معماری v2 ثبت مستقیم روی فاکتور مسدود و کاربر به «حساب پرونده» هدایت می‌شود.
- نسخهٔ ریالی همراهِ پیشنهاد ارزی دیگر مبلغ یا درصد وصول ارزی را نمایش یا snapshot نمی‌کند؛ تبدیل ارزِ خود پیشنهاد و شرایط تجاری آن محفوظ است.
- صورتحساب غیررسمی فقط جمع اقلام، تخفیف و مبلغ نهایی ریالی را نمایش می‌دهد و هیچ معادل ارزی، نرخ وصول یا ماندهٔ ارزی ندارد.

### قرارداد داده و API

- payloadهای `post_receipt` و `correct_receipt` فقط `amountIRR` و اطلاعات عملیاتی دریافت را ارسال می‌کنند.
- API برای پرونده‌های دارای پیشنهاد ارزی نیز نرخ یا منبع نرخ مطالبه نمی‌کند و Receipt تازه را بدون `currency`، `fxRate`، `fxRateSource` و `coveredFxAmount` می‌سازد.
- اصلاح رسمی Receipt قدیمی، رکورد اصلی را برای حسابرسی ابطال می‌کند و نسخهٔ جایگزین صرفاً ریالی می‌سازد.
- مهاجرت پرداخت‌های واقعی legacy نیز Receipt صرفاً ریالی تولید می‌کند؛ مبلغ ریالی رکوردهای قدیمی از بین نمی‌رود.
- شرط پیش‌پرداخت پیشنهاد صرفاً یک شرط تجاری است و دیگر `paid`، `cashFull`، Receipt، payment فاکتور، مانده یا وضعیت تسویه تولید نمی‌کند.
- مبلغ صورتحساب غیررسمی برابر معادل ریالی کل اقلام منهای تخفیف ریالی است و از پیش‌پرداخت پیشنهاد مستقل می‌ماند.
- صدور و بازصدور صورتحساب غیررسمی `RP-ADV-*`، `fromAdvance` یا `advApplied` نمی‌سازد؛ بازصدور artifact مصنوعی نسخه‌های قدیمی را حذف و ردپای دریافت واقعی legacy را حفظ می‌کند. API نیز artifact مصنوعی ورودی را فیلتر می‌کند.

### موتور سود پروژه

- درآمد فروش فقط از مبلغ خالص ریالی فاکتورهای صادرشده به‌دست می‌آید و به دریافت یا metadata تسعیر وابسته نیست.
- خروجی‌های `sellFxTotal`، `sellFxPaid`، `sellFxRemain` و `sellAvgRate` حذف شدند.
- خرید ارزی همچنان با ارز و نرخ ثبت‌شدهٔ فاکتور تأمین‌کننده به هزینهٔ ریالی تبدیل می‌شود.

## فونت مکاتبات و خروجی چاپ

- انتخابگرهای متن نامه و متن آمادهٔ سربرگ دقیقاً شامل **بی‌یاقوت، بی‌نازنین، وزیرمتن، ایران‌یکان و ایران‌سنس** هستند؛ نگاشت گزینه‌های قدیمی فقط برای نمایش داده‌های legacy حفظ شده است.
- فونت روی caret، انتخاب تک‌پاراگرافی و انتخاب چندپاراگرافی واقعاً در `contenteditable` اعمال می‌شود.
- انتخاب «فونت کل متن» روی ادیتور زنده اعمال می‌شود و فونت‌های inline چسبیده از Word را override می‌کند.
- CSS فونت در هر دو خروجی چاپ نامه و متن روی سربرگ تزریق می‌شود؛ وزن‌های وزیرمتن از asset محلی بارگذاری می‌شوند.

## اسناد پروندهٔ برنده

- در عملیات پس از برد، دو اکشن مستقل **سند ابلاغ سفارش** و **قرارداد** افزوده شد.
- فایل‌ها با `docType` ساختاریافته در همان `r.docs[]` پرونده ذخیره می‌شوند و مخزن موازی نمی‌سازند.
- مشاهده، اصلاح مشخصات، جایگزینی نسخه‌دار، حذف کنترل‌شده، فایل ابری، timeline و `documentAudit` موجود برای این دو نوع سند نیز فعال است.
- ثبت این اسناد برای پروندهٔ فاقد پیشنهاد برنده fail-closed است.

## هزینه‌های تکرارشونده و خزانه

- هزینه‌های دارای `tplId` مانند کارمزدها و اجاره‌های ماهانه، همانند کارمزد فاکتور پوششی، تا پیش از تسویه در وضعیت «در انتظار تسویه» می‌مانند.
- ردیف باز هم‌زمان اکشن **سند** و **تسویه** دارد؛ تسویه بدون شماره/شرح سند پرداخت رد می‌شود و امکان پیوست رسید نیز دارد.
- تسویه، کاربر، تاریخ شمسی/ISO، مدرک و فایل‌ها را ثبت می‌کند و دقیقاً یک خروج نقد با مبلغ و تاریخ تسویه در خزانه می‌سازد.
- هزینهٔ تکرارشوندهٔ دارای `chequeCd` خروج OPEX جداگانه تولید نمی‌کند؛ مسیر چک تنها منبع خروج نقد آن است.
- ثبت تسویه هزینه را در محاسبهٔ OPEX/سود دوباره‌شماری نمی‌کند.

## reconcile حقوق و پیام refresh

- گردش حقوق سهامدار و OPEX متناظر فقط هنگام تغییر واقعی داده، `updatedT`، `updatedBy` و شمارندهٔ `updated` را تغییر می‌دهند.
- replay پاسخ همان کلید idempotency دیگر اعداد اجرای نخست را تغییر تازه تلقی نمی‌کند؛ بنابراین پیام «هزینه‌های تکرارشوندهٔ ماه تطبیق شد» در هر refresh تکرار نمی‌شود.

### تست رگرسیون

`_tools/uat/tester501-v34.8.0-ar-rial-only.js` با اجرای واقعی توابع UI و موتور سود و بررسی قراردادهای کلاینت/API، موارد زیر را پوشش می‌دهد:

- رندر فاکتور باز و تسویه‌شده با اعداد ریالی و بدون درصد یا اطلاعات ارزی؛
- هدایت ثبت دریافت v2 به حساب پرونده و fail-closed بودن `savePay` مستقیم؛
- payload ثبت و اصلاح Receipt پرونده؛
- سازنده‌های Receipt در API و مسیرهای migration؛
- حذف مسیرهای تولید وصول پیش‌پرداخت در `petty.js`، `rbac.js` و `unofficial-invoice.js`؛
- محاسبه و نمایش صرفاً ریالی صورتحساب غیررسمی و پاک‌سازی artifactهای قدیمی هنگام بازصدور؛
- محاسبهٔ درآمد از فاکتور ریالی و حفظ تسعیر خرید ارزی.

نتیجهٔ تست اختصاصی مطالبات: **38 PASS / 0 FAIL**.

`_tools/uat/tester502-v34.8.0-letters-award-opex.js` نیز قراردادهای نسخه، پنج فونت مصوب، اعمال ادیتور و چاپ، ثبت نوع‌دار دو سند برنده، تسویهٔ recurring، خروج یکتای خزانه، guard چک، عدم دوباره‌شماری و ضدتکرار پیام reconcile را به‌صورت runtime/source-contract پوشش می‌دهد.

نتیجهٔ تست اختصاصی مکاتبات/پرونده/OPEX: **33 PASS / 0 FAIL**.

گیت کامل CI: **118 PASS / 0 FAIL**. بخش `php -l` به‌دلیل نصب نبودن PHP در محیط اجرا skip شد؛ بررسی syntax همهٔ ۴۹ فایل JavaScript و نگهبان معماری موفق بود.

### نسخه و cache

نسخهٔ رسمی CRM، service worker، manifest، API و cache-bust تمام دارایی‌های CRM روی `v34.8.0` قرار گرفت.
