# 🛒 ریلیز v18.6 — اصلاح سناریوی Post-Award خرید واقعی، هزینه‌ها و ماتریس سود

**گزارش کارفرما:** بعد از ثبت قیمت خرید واقعی، امکان استعلام مجدد از تامین‌کننده از بین می‌رفت و پنجره‌ها بسته می‌شدند. همچنین ماتریس سود، قیمت‌های دلاری را با برچسب IRR نمایش می‌داد. رسید پرداخت خرید واقعی و هزینه‌های مستقیم پرونده نیز باید در پرونده فروش ثبت شوند و مبلغ پیش‌پرداخت دیده شود.

## ✅ اصلاحات انجام‌شده

### ۱) عدم بسته‌شدن پرونده فروش پس از ثبت خرید واقعی
ریشه مشکل، پاک‌سازی گروهی همه `.md-b`ها در `cmpBuy` بود. این کار پنجره‌های مرتبط مانند پرونده فروش/استعلام تامین را هم می‌بست.

رفع:
- ماتریس خرید واقعی شناسه‌دار شد: `cmpModal_<id>`
- پس از ثبت خرید، فقط همان ماتریس بازسازی می‌شود.
- پرونده فروش و سایر پنجره‌ها بسته نمی‌شوند.

### ۲) استعلام مجدد پس از خرید واقعی
- دکمه «استعلام مجدد» در باکس خرید واقعی پرونده فروش اضافه/حفظ شد.
- `ptfRealBuyNewInquiry` دیگر همه مودال‌ها را نمی‌بندد؛ فقط ماتریس خرید را می‌بندد و به مسیر استعلام تامین می‌رود.

### ۳) رسید پرداخت خرید واقعی
- هر خرید واقعی اکنون `cd` و `files` دارد.
- پس از ثبت خرید واقعی، سیستم پیشنهاد می‌دهد رسید پرداخت پیوست شود.
- رسید هم داخل purchase ذخیره می‌شود و هم به اسناد مالی پرونده فروش افزوده می‌شود.

### ۴) ثبت هزینه مستقیم پرونده پس از خرید واقعی
توابع جدید:

```js
ptfProjectCostOpen(inqNo)
ptfProjectCostUpload(dealCd, costCd)
```

هزینه‌های قابل ثبت:
- حمل/باربری
- گمرک/ترخیص
- بازرسی/تست
- کارمزد بانکی
- سایر

هزینه‌ها در `ptf_crm_deals[].costEvents` ثبت می‌شوند و پیوست آن‌ها به پرونده فروش افزوده می‌شود.

### ۵) اثر هزینه‌های مستقیم در سود پروژه
- بدون تکرار موتور سود، wrapper روی `ptfProjectProfitIRR` اضافه شد.
- هزینه‌های مستقیم پرونده از سود پروژه کسر می‌شوند.
- خروجی موتور سود فیلد `projectCostIrr` می‌گیرد.

### ۶) اصلاح ماتریس سود ارزآگاه
- ماتریس سود دیگر قیمت فروش دلاری/ارزی را با برچسب IRR نمایش نمی‌دهد.
- فروش با ارز سند نمایش داده می‌شود.
- خرید واقعی همیشه با معادل ریالی/تومانی پرداخت‌شده نمایش داده می‌شود.
- اگر فروش ارزی باشد، سود ریالی نهایی به گزارش سود پس از وصول/تسعیر فروش ارجاع داده می‌شود و ارزها مخلوط نمی‌شوند.

### ۷) نمایش پیش‌پرداخت
- مبلغ/وضعیت پیش‌پرداخت ساختاریافته در باکس پرونده فروش و ماتریس سود نمایش داده می‌شود.

## فایل‌های تغییر یافته
- `crm/buycompare.js`
- `crm/offers.js`
- `crm/index.html`
- `crm/sw.js`
- `_tools/uat/tester103-v186.js`
- اصلاح تسترهای قبلی:
  - `tester80-v162.js`
  - `tester90-v172.js`
  - `tester100-v182.js`
  - `tester102-v185.js`
- `CASESTUDY-POSTAWARD-COSTS-R10.md`
- `RELEASE-NOTES-v18.6.md`
- `PTF-MASTER-HANDOVER.md`

## 🔬 کیفیت
- تستر جدید: `tester103-v186.js` با ۱۹ چک.
- رگرسیون کامل: **۹۰ تستر / صفر خطا**.
- Audit سایت: PASS با هشدار قدیمی تصاویر سنگین.
