# ریلیزنوت v30.6.1 — هات‌فیکس ماتریس خرید + گردش حساب با نام قلم

## باگ گزارش شده
- ماتریس خرید کلا حذف شد از پرونده‌های فروش
- گردش حساب تغییری نکرده (نام قلم نمایش داده نمی‌شود)
- خرید گروهی دیگر وجود ندارد
- اسکوپ 20% محقق شده

## ریشه
- در v30.6 یک `}).join('');` اضافی در buycompare.js باعث SyntaxError و لود نشدن کل ماژول buycompare → ماتریس خرید ناپدید شد
- گردش حساب legacy: فقط RFQ-99038 نشان می‌داد نه نام قلم - چون e.ref و e.note هر دو وجود داشت ولی فقط ref نمایش داده می‌شد

## فیکس
| فایل | تغییر |
|---|---|
| crm/buycompare.js | حذف `}).join('');` اضافی + کشویی تامین‌کننده از لیست + اعمال روی همه |
| crm/supplier-finance.js | گردش حساب: قبل از لینک به فاکتور نام قلم نمایش داده شود، بعد از لینک فقط شماره و تاریخ فاکتور. برای legacy: e.note (نام قلم) + e.ref (درخواست) هر دو نمایش |
| crm/rfqsmart.js | زمان تحویل (روز) در جدول مقایسه - بدون تغییر |
| crm/index.html | VER v30.6.1 |

## تست
1. پرونده فروش → باید دکمه 🛍 ثبت/مشاهده/اصلاح خرید باشد (ماتریس خرید)
2. ماتریس خرید → دکمه 🛒 ثبت گروهی خرید → باید کشویی تامین‌کننده باشد نه input متنی
3. گردش حساب امیر طاهری → قبل از لینک: باید نام قلم (مثلا شیر توپی) + RFQ-99038 ببینی - بعد از لینک به فاکتور با تیک، فقط شماره فاکتور بیاید

## نسخه
- VER v30.6.1
