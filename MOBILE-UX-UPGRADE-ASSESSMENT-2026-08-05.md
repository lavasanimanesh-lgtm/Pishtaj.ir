# 📱 ارزیابی عمیق و نقشه‌راه ارتقای رابط‌ کاربری موبایل CRM
### تاریخ: ۱۴ مرداد ۱۴۰۵ | نسخه: v34.0.21-alpha

---

## فهرست
1. [وضعیت فعلی — آمار و تحلیل فنی](#1-وضعیت-فعلی)
2. [مشکلات بحرانی شناسایی‌شده](#2-مشکلات-بحرانی)
3. [پیشنهادات اولویت‌بندی‌شده](#3-پیشنهادات)
4. [نقشه اجرا](#4-نقشه-اجرا)

---

## 1. وضعیت فعلی

### 📊 آمار کلیدی

| شاخص | مقدار | ارزیابی |
|-------|--------|---------|
| حجم کل JS | **۴.۲ مگابایت** (۸۸ فایل) | 🔴 بحرانی — ۴ برابر حد مطلوب |
| حجم index.html | **۱۷۵ کیلوبایت** | 🟡 سنگین |
| تعداد `<script src>` | **۸۷ فایل** — همه synchronous | 🔴 بحرانی |
| قوانین `!important` | **۱۷۴ عدد** | 🔴 نشانه جنگ CSS |
| بلاک‌های `@media` موبایل | **۵ بلاک** (فقط index.html) + ۳ از mobilenav.js | 🔴 تداخل |
| فایل‌هایی که CSS تزریق می‌کنند | **۱۵ فایل JS** — هرکدام `<style>` جدا | 🔴 بی‌نظمی |
| پشتیبانی `aria-*` / قابلیت دسترسی | **۱ مورد** — عملاً صفر | 🔴 بحرانی |
| مدیریت focus | **صفر** | 🔴 |
| Pull-to-refresh | ❌ ندارد | 🟡 |
| Skeleton/Loading UI | ❌ در ماژول‌های اصلی ندارد | 🟡 |
| مدیریت history/back | ❌ ندارد — دکمه Back کاربر را از CRM خارج می‌کند | 🔴 بحرانی |
| ژست لمسی (Swipe/Gesture) | ❌ فقط در Kanban — سایر ماژول‌ها ندارند | 🟡 |
| اندازه لمسی دکمه‌ها (Touch Target) | ۴۶px ✅ | 🟢 خوب |
| PWA/ServiceWorker | ✅ دارد — Network-First | 🟢 خوب |
| حالت شب موبایل | ✅ دارد — ولی ناقص | 🟡 |
| Safe Area (iPhone) | ✅ ۵ مورد | 🟢 خوب |
| بازخورد لمسی (Haptic) | ✅ در ۳ ماژول | 🟢 خوب |

### 🏗 معماری فعلی موبایل

```
index.html
  ├── <style> #1 (Critical CSS — FOUC fix)
  │     ├── @media 768px: سایدبار مخفی ← display:none
  │     ├── @media 768px: Sprint 108 — دکمه‌ها/جدول‌ها
  │     ├── @media 768px: Sprint 111 — سایدبار ۵۶px ← تناقض!
  │     └── Sprint 118: کنترل مودال
  ├── <style> #2 (Main CSS)
  │     └── @media 900px: سایدبار فشرده
  ├── 87× <script src="..."> — همه sync
  │     └── mobilenav.js: نوار پایین + @media 768px ← سایدبار مخفی (سومین بار!)
  └── ۱۵ فایل JS هرکدام CSS جدا تزریق می‌کنند
```

**مشکل ساختاری اصلی:** سایدبار موبایل ۳ بار با قوانین متناقض مدیریت شده:
1. Critical CSS: `display:none` (مخفی)
2. Sprint 111: عرض ۵۶px (نمایش!)
3. mobilenav.js: `display:none` (مخفی)

چون هر سه `!important` دارند، **ترتیب بارگذاری** تعیین‌کننده است — نه منطق.

---

## 2. مشکلات بحرانی شناسایی‌شده

### 🔴 P0 — دکمه Back مرورگر = خروج از CRM

**مشکل:** هیچ `history.pushState` یا `popstate` listener وجود ندارد. وقتی کاربر از ماژول «درخواست‌ها» به «پیشنهاد» می‌رود و Back می‌زند، **از CRM خارج می‌شود** — نه برگشت به ماژول قبلی.

**اثر:** در موبایل این فاجعه‌بار است چون ژست Back (swipe از لبه یا دکمه ناوبری) رایج‌ترین عمل کاربر است.

**پیشنهاد:**
```
هربار goPanel() صدا زده شود → history.pushState({panel: id}, '')
window.addEventListener('popstate') → goPanel(event.state.panel)
```
پیچیدگی: **کم** | تأثیر: **فوق‌العاده بالا** | زمان: **۲-۳ ساعت**

---

### 🔴 P0 — بارگذاری ۴.۲ مگابایت JS به‌صورت synchronous

**مشکل:** ۸۷ تگ `<script src>` بدون `async` یا `defer` در انتهای `<body>` بارگذاری می‌شوند. روی 3G/4G ایران (متوسط ۲-۵ مگابیت) این یعنی:

| سرعت | زمان بارگذاری JS | زمان تا تعامل‌پذیری (TTI) |
|-------|-------------------|--------------------------|
| 4G خوب (5Mbps) | ~۷ ثانیه | ~۱۰ ثانیه |
| 4G معمولی (2Mbps) | ~۱۷ ثانیه | ~۲۲ ثانیه |
| 3G (800Kbps) | ~۴۲ ثانیه | ~۵۰ ثانیه |

**پیشنهاد (سه‌فازی):**

**فاز A — فوری (بدون تغییر ساختار):**
- `defer` روی همه اسکریپت‌ها → مرورگر صفحه را نمایش می‌دهد در حالی که JS در پس‌زمینه لود می‌شود
- `xlsx.min.js` (۲۸۰KB) را `async` کنید — فقط هنگام import/export لازم است

**فاز B — Code Splitting ساده:**
- ماژول‌هایی مثل `cheque-print.js` (۵۱KB)، `archive.js` (۲۴KB)، `fiscal.js` (۸۴KB) فقط وقتی کاربر آن پنل را باز کند لود شوند (dynamic `<script>` injection)
- ۲۰ ماژول نادر-استفاده = ~۶۰۰KB صرفه‌جویی در بار اول

**فاز C — Bundling:**
- ماژول‌های هسته‌ای (bridge, offers, storage, rbac) → یک bundle فشرده با gzip
- ۴.۲MB raw → ~۸۰۰KB gzipped (JS خوب فشرده می‌شود)

پیچیدگی: A=کم, B=متوسط, C=بالا | تأثیر: **بحرانی** | زمان: A=۱ ساعت, B=۱ روز, C=۳ روز

---

### 🔴 P0 — جنگ CSS: ۱۵ فایل JS + ۵ بلاک @media + ۱۷۴ !important

**مشکل:** ۱۵ فایل JS هرکدام یک `<style>` مستقل به `<head>` تزریق می‌کنند. هیچ نظم مشخصی ندارند. قوانین **متناقض** هستند (سایدبار ۳ بار مدیریت شده). `!important` ابزار حل تناقض شده — نه ابزار طراحی.

**نشانه‌ها:**
- Sprint 111 سایدبار ۵۶px می‌سازد ← mobilenav.js آن را `display:none` می‌کند
- `.mn` (محتوای اصلی) سه padding-bottom متفاوت می‌گیرد
- دکمه‌ها ۴ بار قوانین متناقض `white-space` می‌گیرند

**پیشنهاد:**
1. **یکپارچه‌سازی CSS موبایل:** یک فایل `crm/mobile.css` بسازید که تنها مرجع قوانین موبایل باشد
2. **حذف Sprint 111 sidebar 56px:** چون mobilenav.js نوار پایین را جایگزین کرده، این بلاک مرده است
3. **کاهش !important:** با ترتیب صحیح CSS نیازی به !important نیست

پیچیدگی: **متوسط** | تأثیر: **بالا** — رفع باگ‌های آینده ۵ برابر آسان‌تر می‌شود | زمان: **۱ روز**

---

### 🔴 P0 — دکمه‌ها با متن بلند: خوانایی صفر در صفحه کوچک موبایل

**مشکل:** در عرض ۳۶۰-۴۰۰ پیکسل موبایل، دکمه‌هایی مثل «📥 ورود اکسل»، «+ کالای جدید»، «📊 گزارش دورهٔ دلخواه»، «🗑 حذف دسته‌جمعی انتخاب‌شده‌ها» یا **خوانده نمی‌شوند** (ellipsis/بریده) یا **فضای زیادی اشغال** می‌کنند و المان‌های دیگر را له می‌کنند.

**آمار بررسی‌شده:** **۵۶۰+ دکمه با متن بلندتر از ۱۳ کاراکتر** در ۶۴ فایل JS وجود دارد. مهم‌ترین‌ها:

| ماژول | تعداد دکمه بلند | نمونه بحرانی |
|-------|-----------------|-------------|
| offers.js | ۳۳ | «صدور پیشنهاد مالی»، «ذخیره تغییرات و بروزرسانی» |
| rfqsmart.js | ۲۹ | «📥 اقلام»، «+ تامین جدید»، «🤖 دستیار تامین» |
| salesfiles.js | ۲۹ | «🛍 خرید»، «مشاهده اسناد پرونده» |
| bridge.js | ۲۵ | «🧹 یتیم‌ها»، «✅ تایید و ورود» |
| buycompare.js | ۲۵ | «🛒 ثبت گروهی خرید»، «🤖 استعلام» |
| cheque-panel.js | ۲۳ | «📥 چک وارده»، «🏢 چک صادره» |
| petty.js | ۲۲ | «📊 گزارش دورهٔ دلخواه»، «پرداخت مستقیم» |

**پیشنهاد — استراتژی Icon-Only برای موبایل:**

دکمه‌ها در موبایل باید **فقط آیکون** نمایش بدهند و متن در `title` (tooltip) نگه‌داشته شود. با یک CSS media query ساده:

```css
@media (max-width: 768px) {
  /* دکمه‌های عملیاتی در هدر ماژول‌ها: فقط آیکون */
  .ph .bt, .ph .bt-o, .ph .ba {
    font-size: 0 !important;       /* متن مخفی */
    padding: 10px 12px !important;
    min-width: 44px;
    min-height: 44px;
  }
  /* ایموجی/SVG آیکون آغازی دکمه → نمایش بزرگ */
  .ph .bt::first-letter, .ph .bt-o::first-letter {
    font-size: 18px !important;
  }

  /* دکمه‌های عملیاتی روی ردیف‌ها/کارت‌ها: فقط آیکون */
  .tb2 .ba, .tb2 button:not(.mx-dot) {
    font-size: 0 !important;
    padding: 8px !important;
    min-width: 36px;
  }
  .tb2 .ba svg, .tb2 button svg { width: 18px; height: 18px; }
}
```

**الگوی بازنویسی دکمه (بدون شکستن دسکتاپ):**
```html
<!-- قبل (مشکل‌دار): -->
<button class="bt" onclick="...">📥 ورود اکسل با راهنما</button>

<!-- بعد (آیکون اول + متن قابل‌مخفی): -->
<button class="bt" onclick="..." title="ورود اکسل">
  <span class="ic">📥</span><span class="lb">ورود اکسل</span>
</button>
```
```css
@media (max-width: 768px) {
  .bt .lb, .ba .lb { display: none; }
  .bt .ic, .ba .ic { font-size: 20px; }
}
```

**مزیت:** دسکتاپ بدون تغییر می‌ماند. در موبایل دکمه‌ها کوچک، قابل لمس و مرتب می‌شوند. کاربر با نگه‌داشتن انگشت (long-press) tooltip را می‌بیند.

پیچیدگی: **بالا** (۵۶۰+ دکمه نیاز به بازنویسی HTML دارند) | تأثیر: **فوق‌العاده بالا** | زمان: **۳-۴ روز**

**راه‌حل فوری (بدون تغییر HTML):** با CSS خالص متن دکمه‌ها را مخفی و `::first-letter` ایموجی را نمایش بدهید — ۸۰٪ مشکل حل می‌شود:

```css
@media (max-width: 768px) {
  .ph .sb2 .bt, .ph .sb2 .bt-o {
    font-size: 0 !important;
    gap: 0 !important;
  }
  .ph .sb2 .bt::first-letter, .ph .sb2 .bt-o::first-letter {
    font-size: 16px !important;
  }
}
```
زمان راه‌حل فوری: **۲ ساعت**

---

### 🔴 P0 — هدر موبایل: به‌هم‌ریختگی آیکون‌ها و عناصر

**مشکل:** هدر (نوار `.tb`) در موبایل از **تناقض CSS** رنج می‌برد:

```
index.html:  .tb { flex-direction: COLUMN !important; }  → عمودی
mobilenav.js: .tb { flex-wrap: NOWRAP !important; }      → تک‌خط

column + nowrap = هر فرزند تمام‌عرض می‌شود
→ عنوان یک ردیف، آیکون‌ها ردیف بعد، topVerPill ردیف بعد
→ هدر ۳ ردیفه شلوغ → یا المان‌ها روی هم می‌افتند
```

**ساختار فعلی هدر:**
```
┌─────────────────────────────────┐
│ 📊 داشبورد    🔎 جستجوی سریع... │  ← ردیف ۱ (flex-direction:column → تمام‌عرض)
├─────────────────────────────────┤
│ v34.0.21  🏥 سالم  🕐 14:32     │  ← ردیف ۲ (topVerPill + healthPill + clock)
├─────────────────────────────────┤
│ 🌙  ☁️  📁  🔔  🔍               │  ← ردیف ۳ (tbic icons از theme.js)
└─────────────────────────────────┘
```

**مشکلات مشاهده‌شده:**
1. **هدر ۳ ردیفه** — ۷۰-۸۰ پیکسل ارتفاع اشغال می‌کند (از فضای ارزشمند موبایل)
2. **دکمه جستجو** با متن «جستجوی سریع سرتاسری» تمام ردیف اول را پر می‌کند (mobilenav.js آن را icon-only کرده ولی بعد از بارگذاری)
3. **آیکون‌های `tbic`** (تم/ابر/بایگانی/زنگ/جستجو) — ابر و بایگانی مخفی شده‌اند ولی ۳ تای باقی‌مانده جای مشخصی ندارند
4. **`topVerPill`** (شماره نسخه) در موبایل غیرضروری است ولی ۹۶ پیکسل اشغال می‌کند
5. **قوانین CSS متناقض:** `flex-direction: column` (index.html) vs `flex-wrap: nowrap` (mobilenav.js) — نتیجه‌اش غیرقابل‌پیش‌بینی است

**پیشنهاد — هدر تک‌ردیفه مینیمال:**

```
┌────────────────────────────────────┐
│  📊 داشبورد          🔍  🔔  🌙   │  ← فقط ۱ ردیف ← ۴۴px ارتفاع
└────────────────────────────────────┘
```

اجرا:
```css
@media (max-width: 768px) {
  .tb {
    flex-direction: row !important;    /* نه column! */
    flex-wrap: nowrap !important;
    align-items: center !important;
    justify-content: space-between !important;
    padding: 8px 12px !important;
    gap: 8px !important;
    height: 48px !important;
    overflow: hidden !important;
  }
  /* عنوان صفحه: فشرده */
  .tb h2 {
    font-size: 14px !important;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1;
    min-width: 0;
  }
  /* دکمه جستجو: فقط آیکون */
  .tb button[onclick*="ptfOpenCommandPalette"] {
    font-size: 0 !important;
    padding: 8px !important;
    min-width: 36px !important;
    width: 36px !important;
  }
  /* مخفی‌سازی عناصر غیرضروری */
  #topVerPill, #liveHealthPill, #clockD,
  .tb > div:nth-child(2) { display: none !important; }
  /* آیکون‌های هدر: فشرده‌تر */
  .tb .tbic { width: 34px !important; height: 34px !important; }
}
```

پیچیدگی: **کم** | تأثیر: **فوق‌العاده بالا** — ۳۰ پیکسل فضای عمودی آزاد می‌شود + ظاهر مرتب | زمان: **۲-۳ ساعت**

---

### 🟡 P1 — مودال‌ها در موبایل: فرم‌های طولانی در جعبه کوچک

**مشکل:** مودال‌ها (`max-height: 88vh`) فرم‌های طولانی (ثبت درخواست: ۱۵+ فیلد) را در جعبه‌ای محدود می‌اندازند. کاربر باید اسکرول کند — ولی اسکرول داخل مودال + اسکرول صفحه = **سردرگمی لمسی**.

**پیشنهاد:**
- مودال‌های با بیش از ۵ فیلد → **تمام‌صفحه شوند** (fullscreen modal) با انیمیشن slide-up
- Header ثابت (عنوان + دکمه بستن) + Footer ثابت (دکمه ذخیره)
- بدنه فرم = تنها ناحیه‌ اسکرول‌شونده

```css
@media (max-width: 768px) {
  .md.md-full {
    width: 100vw !important;
    height: 100vh !important;
    max-width: 100vw !important;
    max-height: 100vh !important;
    border-radius: 0 !important;
    margin: 0 !important;
    display: flex;
    flex-direction: column;
  }
  .md.md-full h3 { flex: 0 0 auto; position: sticky; top: 0; }
  .md.md-full .fr { flex: 1; overflow-y: auto; }
  .md.md-full > div:last-child { flex: 0 0 auto; position: sticky; bottom: 0; }
}
```

پیچیدگی: **متوسط** | تأثیر: **بالا** | زمان: **۴-۶ ساعت**

---

### 🟡 P1 — هیچ حالت بارگذاری/اسکلت وجود ندارد

**مشکل:** وقتی کاربر ماژولی را انتخاب می‌کند، محتوا فوراً ساخته می‌شود (کل HTML در JS) — ولی اگر داده زیاد باشد (۵۰۰ درخواست، ۱۰۰۰ کالا) مرورگر برای چند ثانیه فریز می‌شود بدون هیچ بازخورد بصری.

**پیشنهاد:**
- یک **Skeleton Screen** جنریک بسازید: ۳-۵ بلاک خاکستری چشمک‌زن
- قبل از render هر ماژول نمایش بدهید
- با `requestAnimationFrame` یا `setTimeout(render, 16)` مرورگر را آزاد کنید تا skeleton نمایش داده شود

```javascript
function goPanel(id) {
  showSkeleton(); // نمایش فوری
  requestAnimationFrame(function() {
    var html = buildModule(id); // ساخت HTML
    document.getElementById('ca').innerHTML = html;
    hideSkeleton();
  });
}
```

پیچیدگی: **کم** | تأثیر: **بالا** — حس سرعت ۳ برابر شود | زمان: **۲-۳ ساعت**

---

### 🟡 P1 — عدم پشتیبانی از ژست‌های Swipe

**مشکل:** در ماژول‌هایی مثل «درخواست‌ها»، «مشتریان»، «پیشنهادها» — کاربر موبایل فقط می‌تواند دکمه بزند. هیچ Swipe-to-action وجود ندارد.

**پیشنهاد:**
- **Swipe چپ** روی کارت‌ها (ردیف‌های جدول → کارت در موبایل) → دکمه‌های عمل سریع (ویرایش/حذف/تغییر وضعیت)
- **Swipe راست** → بازگشت به ماژول قبلی (هماهنگ با history)
- از Pointer Events API استفاده کنید (نه Touch Events) — سازگاری بهتر

پیچیدگی: **متوسط** | تأثیر: **متوسط** — حس اپ‌بودن | زمان: **۱ روز**

---

### 🟡 P1 — قابلیت دسترسی (Accessibility) = صفر

**مشکل:**
- فقط **۱ مورد** `aria-*` در کل پروژه
- هیچ `tabindex` یا `focus management`
- هیچ `role="dialog"` روی مودال‌ها
- هیچ `aria-label` روی آیکون‌بادکمه‌ها (فقط ایموجی)
- خواننده صفحه (Screen Reader) عملاً کور است

**پیشنهاد (حداقلی):**
```html
<!-- مودال‌ها -->
<div class="md-b" role="dialog" aria-modal="true" aria-labelledby="mdTitle">

<!-- دکمه‌های آیکونی -->
<button aria-label="ویرایش">✎</button>

<!-- نوار پایین -->
<nav id="mnvBar" role="tablist" aria-label="ناوبری اصلی">
```

پیچیدگی: **کم** | تأثیر: **متوسط** — رعایت WCAG + بهبود SEO | زمان: **۴ ساعت**

---

### 🟢 P2 — جستجوی سرتاسری: ارتقا به Spotlight-style

**وضعیت فعلی:** دکمه جستجو در هدر وجود دارد + Command Palette (Ctrl+K) — ولی در موبایل کوچک و دشوار.

**پیشنهاد:**
- جستجو از کشوی «سایر» قابل دسترسی است ← خوب
- ولی اگر pull-down از بالای صفحه ← جستجو باز شود → دسترسی ۱ حرکتی

پیچیدگی: **کم** | تأثیر: **کم-متوسط** | زمان: **۳ ساعت**

---

### 🟢 P2 — Offline-First UX Indicators

**وضعیت فعلی:** Service Worker وجود دارد ولی کاربر هیچ نشانه‌ای از آفلاین/آنلاین بودن نمی‌بیند.

**پیشنهاد:**
- نوار رنگی بالای صفحه هنگام آفلاین: «📴 اینترنت قطع — تغییرات ذخیره محلی شد»
- هنگام برگشت آنلاین: «✅ اتصال برقرار — همگام‌سازی...» (با پیشرفت)

پیچیدگی: **کم** | تأثیر: **متوسط** | زمان: **۲ ساعت**

---

### 🟢 P2 — بهبود کارت‌های جدول (Card View)

**وضعیت فعلی:** جدول‌ها در موبایل به کارت تبدیل می‌شوند ← خوب، ولی:
- هیچ **header** روی هر فیلد نیست (عنوان ستون مخفی شده)
- کاربر نمی‌داند هر عدد چیست

**پیشنهاد:** از `data-label` روی `<td>` استفاده و با CSS `::before` عنوان ستون نشان بدهید:
```css
.tb2 td::before {
  content: attr(data-label);
  font-weight: 900;
  font-size: 10px;
  color: #94a3b8;
}
```

پیچیدگی: **متوسط** (نیاز به تغییر JS تمام ماژول‌ها) | تأثیر: **بالا** | زمان: **۱ روز**

---

### 🟢 P2 — انیمیشن‌های حرفه‌ای‌تر

**وضعیت فعلی:** fade-in ساده (۰.۲ ثانیه) + FAB breathing → خوب ولی کافی نیست.

**پیشنهاد:**
- تعویض ماژول → **Shared Element Transition** (عنوان ماژول از نوار پایین به هدر animate شود)
- باز شدن مودال → **slide-up** از پایین (مثل iOS Sheet)
- بسته شدن مودال → **slide-down** + backdrop fade

پیچیدگی: **کم** | تأثیر: **متوسط** — حس حرفه‌ای | زمان: **۴ ساعت**

---

## 3. جدول اولویت‌بندی نهایی

| # | پیشنهاد | اولویت | پیچیدگی | تأثیر UX | زمان |
|---|---------|--------|---------|----------|------|
| 1 | **History API / Back Button** | 🔴 P0 | کم | فوق‌العاده | ۳ ساعت |
| 2 | **defer/async اسکریپت‌ها** | 🔴 P0 | کم | بحرانی | ۱ ساعت |
| 3 | **حذف CSS تکراری/متناقض** | 🔴 P0 | متوسط | بالا | ۱ روز |
| 4 | **🆕 دکمه‌ها → Icon-Only در موبایل** | 🔴 P0 | کم (CSS فوری) / بالا (HTML) | فوق‌العاده | ۲ ساعت فوری / ۳ روز کامل |
| 5 | **🆕 هدر تک‌ردیفه مرتب** | 🔴 P0 | کم | فوق‌العاده | ۲-۳ ساعت |
| 6 | **مودال تمام‌صفحه** | 🟡 P1 | متوسط | بالا | ۶ ساعت |
| 7 | **Skeleton Loading** | 🟡 P1 | کم | بالا | ۳ ساعت |
| 8 | **Swipe-to-Action** | 🟡 P1 | متوسط | متوسط | ۱ روز |
| 9 | **Accessibility حداقلی** | 🟡 P1 | کم | متوسط | ۴ ساعت |
| 10 | **Card View headers** | 🟢 P2 | متوسط | بالا | ۱ روز |
| 11 | **Offline Indicator** | 🟢 P2 | کم | متوسط | ۲ ساعت |
| 12 | **انیمیشن‌های Sheet-style** | 🟢 P2 | کم | متوسط | ۴ ساعت |
| 13 | **Code Splitting** | 🟢 P2 | بالا | بالا | ۳ روز |
| 14 | **Pull-down جستجو** | 🟢 P2 | کم | کم | ۳ ساعت |

---

## 4. نقشه اجرا (پیشنهادی)

### اسپرینت ۱ — «ظاهر و ثبات فوری» (۱-۲ روز)
- [x] رفع شکسته شدن متن فارسی ← ✅ انجام شد
- [ ] #5: 🆕 هدر تک‌ردیفه مرتب (رفع به‌هم‌ریختگی آیکون‌ها)
- [ ] #4: 🆕 دکمه‌ها Icon-Only (راه‌حل فوری CSS — بدون تغییر HTML)
- [ ] #3: حذف CSS متناقض Sprint 111
- [ ] #2: defer/async اسکریپت‌ها

### اسپرینت ۲ — «ناوبری حرفه‌ای» (۱-۲ روز)
- [ ] #1: History API + Back Button
- [ ] #7: Skeleton Loading
- [ ] #12: انیمیشن‌های Sheet-style

### اسپرینت ۳ — «حس اپ‌بودن» (۲-۳ روز)
- [ ] #6: مودال تمام‌صفحه موبایل
- [ ] #4 (فاز ۲): بازنویسی HTML دکمه‌ها به ساختار `<span class="ic">` + `<span class="lb">`
- [ ] #11: Offline Indicator
- [ ] #8: Swipe-to-Action

### اسپرینت ۴ — «کیفیت و عملکرد» (۳-۵ روز)
- [ ] #10: Card View headers
- [ ] #9: Accessibility
- [ ] #13: Code Splitting
- [ ] #14: Pull-down جستجو

---

## 📎 نتیجه‌گیری

CRM پیشتاج از نظر **عملکرد و ویژگی‌ها** بسیار غنی است (۸۸ ماژول JS، PWA، حالت شب، بج زنده، FAB، هپتیک). ولی تجربه موبایل از **پنج مشکل ساختاری** رنج می‌برد:

1. **👁 خوانایی دکمه‌ها** — ۵۶۰+ دکمه با متن بلند در ۳۶۰px جا نمی‌شوند → باید Icon-Only شوند
2. **📐 به‌هم‌ریختگی هدر** — آیکون‌ها/عنوان/نسخه روی هم می‌افتند (CSS متناقض: column vs nowrap)
3. **⚡ سرعت بارگذاری** — ۴.۲MB JS synchronous
4. **🎨 ثبات CSS** — ۱۵ منبع متناقض + ۱۷۴ !important
5. **🔙 رفتار ناوبری** — Back = خروج از CRM

با اجرای ۵ آیتم P0 (هدر + دکمه‌ها + CSS cleanup + defer + History) در **۲-۳ روز**، تجربه موبایل **به‌طور چشمگیری** بهبود می‌یابد بدون ریسک شکستن منطق کسب‌وکار.
