# تشخیص مغایرت‌های «تراز رسمی/غیررسمی» و «گزارش مالی تجمیعی»

**برنچ:** `arena/019fcd1d-pishtaj-ir` (v34.0.9-alpha)
**وضعیت:** بازبینی + بازتولید عددی — **هیچ تغییری در کد اعمال نشد** تا تأیید کنید.

---

## مغایرت ۱ — «رسمی + غیررسمی ≠ کل» در تراز رسمی/غیررسمی

### اعداد شما (و بازتولید من):
| | رسمی | غیررسمی | **نامشخص** | کل |
|---|---|---|---|---|
| خرید تامین | ۰ | ۱۸۱م | **۱۵م** | **۱۹۶م** |
| هزینه جاری | ۹۶م | ۲۹۰م | **۴۰م** | **۴۲۶م** |

### ریشه:
فارق بین «کل» و «رسمی+غیررسمی»، **باکت «نامشخص (unclassified)»** است — یعنی رکوردهایی که فیلد طبقه‌بندی رسمی/غیررسمی‌شان **ست نشده** (`isOfficial` نه `true` نه `false`؛ برای فاکتور فروش `isUnofficial` ست نشده).

- تابع طبقه‌بندی (`ptfLedgerOfOpex` / `ptfLedgerOfSupplierInvoice`) فقط وقتی «رسمی/غیررسمی» می‌گوید که فیلد صریحاً `true/false` باشد؛ **در غیر این صورت عمداً «نامشخص» می‌زند** (حدس نمی‌زند — طبق تصمیم طراحی).
- `کل` = رسمی + غیررسمی + نامشخص. پس `کل` همواره از جمع دو تا بیشتر است اگر رکوردِ بدون‌طبقه‌بندی وجود داشته باشد.

**پس این یک خطای محاسبه نیست؛ یک «خلأ داده» است** — رکوردهای قدیمی/ناقص که در زمان ثبت، رسمی/غیررسمی‌شان مشخص نشده. گزارش یک هشدار («موردی نوع رسمی/غیررسمی‌شان مشخص نیست») دارد، ولی عددِ نامشخص در پاورقی کوچک است و چشم کاربر آن را با «کل» جمع نمی‌بیند.

> نکته: برای **فروش**، نبودِ `isUnofficial` به‌معنای «رسمی» است (پیش‌فرض)، پس فروش باکتی «نامشخص» ندارد و `رسمی+غیررسمی=کل` برقرار است. مشکل فقط در **خرید و هزینه‌جاری** است.

---

## مغایرت ۲ — «بدهی باز ۶۳۴م» در برابر «فاکتور تامین سال ۱۹۶م» و «پرداخت سال ۳۱م»

### ریشه (بازتولید شده):
این سه عدد، **سه سنجهٔ متفاوت** هستند و نباید مستقیم مقایسه شوند:

**بدهی باز تامین‌کننده = افتتاحیه + ماندهٔ تمام فاکتورهای تامین (امسال و سال‌های قبل) + اصلاحیات − اعتبار**
- «افتتاحیه» = ماندهٔ دستیِ ثبت‌شده برای بدهی‌های قبل از CRM (`opening_balance`).
- «ماندهٔ هر فاکتور» = مبلغ فاکتور − مبلغ پرداخت/تخصیصِ آن (برای **همهٔ** فاکتورهای باز، بدون محدودیت به سال جاری).
- پس بدهیِ باز، یک **ماندهٔ تجمعی** است.

**فاکتور تامین ثبت‌شده در سال (۱۹۶م) = فقط فاکتورهایی که تاریخ صدورشان در سال جاری است** (مبلغ کامل).
**پرداخت تامین در سال (۳۱م) = فقط پرداخت‌هایی که تاریخشان در سال جاری است.**

### چرا ۶۳۴ ≠ ۱۹۶؟
زیرا ۶۳۴م شامل: افتتاحیه + فاکتورهای سال‌های قبل + ماندهٔ پرداخت‌نشدهٔ فاکتور امسال است؛ در حالی که ۱۹۶م فقط «مبلغ اسمی فاکتورهای صادرهٔ امسال» است. مقایسهٔ مستقیمشان درست نیست.

**بازتولید (دادهٔ نمونه):** فاکتور امسال ۱۹۶م، پرداخت امسال ۳۱م، فاکتور سال قبل ۴۰۰م، افتتاحیه ۴۵۰م →
- بدهی باز = (۱۹۶ − ۳۱) + ۴۰۰ + ۴۵۰ = **۱۰۱۵م** (در پروداکشن شما با ارقام واقعی = ۶۳۴.۲م).
- فاکتور سال = ۱۹۶م، پرداخت سال = ۳۱م. ✅

**پس این هم خطای محاسبه نیست**، ولی گزارش این رابطه را **به‌صراحت توضیح نمی‌دهد** — سه عددِ هم‌ردیفِ «گردش» و «مانده» کنار هم نشان داده می‌شوند بدون reconciliation → درست همان سردرگمی که شما می‌گویید.

---

## جمع‌بندی — آیا باگ واقعی هست؟

| مغایرت | باگ عددی؟ | مشکل واقعی |
|---|---|---|
| رسمی+غیررسمی ≠ کل (خرید/هزینه) | ❌ خیر | باکت «نامشخص» به‌دلیل رکوردهای بدون طبقه‌بندی؛ نمایشش در UI کمرنگ است |
| بدهی باز ≠ فاکتور سال | ❌ خیر | ماندهٔ تجمعی در برابر جریانِ سال؛ رابطه بین آن‌ها در گزارش توضیح داده نشده |

**دو مشکلِ واقعی (نه خطای عددی):**
1. **شفافیت باکت «نامشخص»**: باید بزرگ/برجسته شود و بهتر است کاربر بتواند آن‌ها را طبقه‌بندی کند (کیفیت داده).
2. **reconciliation گزارش تجمیعی**: باید نشان دهد «بدهی = افتتاحیه + فاکتورهای (امسال+قبل) − پرداخت‌ها + اصلاحیات» تا عددها قابل فهم شوند.

---

## اصلاح پیشنهادی (پس از تأیید شما)
- **گزارش A:** ردیف «نامشخص» را به‌عنوان یک خط صریح در جدول (نه فقط پاورقی) نمایش بده و در صورت وجود، پیام «برای صحت، این رکوردها را طبقه‌بندی کنید» با دکمهٔ انتقال به «کیفیت داده» بده.
- **گزارش B:** یک بلوک «بازسازی بدهی تامین‌کننده» اضافه کن: `افتتاحیه + فاکتورهای امسال − پرداخت امسال + فاکتورهای سال قبل − پرداخت سال قبل + اصلاحیات = بدهی باز`، تا رابطهٔ ۶۳۴ و ۱۹۶ و ۳۱ شفاف شود.
- **کیفیت داده:** اگر موافقید، می‌توانم یک عملِ «طبقه‌بندی انبوهِ رکوردهای نامشخص» بر اساس قانون/فرض پیشنهادی (مثلاً به‌عنوان رسمی) اضافه کنم تا باکت نامشخص خالی شود.

*تولیدشده توسط Arena Agent — تشخیص فقط‌خواندنی؛ منتظر تأیید برای اعمال اصلاحات.*
