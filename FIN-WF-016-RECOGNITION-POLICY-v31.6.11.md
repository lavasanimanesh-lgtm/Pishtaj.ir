# سیاست recognition و تطبیق سود پروژه با بدهی تأمین

**شناسه:** FIN-WF-016  
**نسخه:** v31.6.11  
**وضعیت:** سند سیاست read-only؛ این سند هیچ داده‌ای را تغییر نمی‌دهد.

## ۱. تفکیک منابع حقیقت

### سود/زیان پروژه

مبنای سود پروژه فقط این زنجیره است:

- فروش ثبت‌شدهٔ پروژه؛
- خرید واقعی ثبت‌شده در `ptf_crm_buycmp.purchases`؛
- هزینهٔ مستقیم ثبت‌شده در `deal.costEvents`؛
- زیان ثبت‌شده در `lossEvents`؛
- تسعیر فقط با نرخ ذخیره‌شده و قابل ردیابی.

### بدهی تأمین‌کننده

مبنای بدهی تأمین‌کننده این منابع است:

- فاکتورهای `ptf_crm_supplier_finance.invoices`؛
- پرداخت‌ها و تخصیص‌ها؛
- تعهدهای legacy در `ptf_crm_payables`؛
- اعتبار و adjustmentهای تأمین‌کننده؛
- چک شرکتی مرتبط، طبق ownership و status.

این دو منبع حقیقت یکی نیستند و نباید یکی به‌جای دیگری استفاده شود.

## ۲. قواعد recognition

1. فاکتور یا payable تأمین‌کننده به‌تنهایی هزینهٔ قطعی پروژه محسوب نمی‌شود.
2. خرید واقعی و provenance قلم مبنای بهای خرید پروژه است.
3. بدهی تأمین‌کننده نباید دوباره از سود پروژه کم شود.
4. هزینهٔ linked به پرونده فقط در سود پروژه اثر می‌گذارد؛ هزینهٔ unlinked فقط در گزارش سال مالی اثر می‌گذارد.
5. یک رویداد نباید هم در `costEvents` و هم در `opexTotal` دوباره شمرده شود.
6. دریافت مشتری، receipt و بدهی تأمین‌کننده سه رویداد جدا هستند.
7. auto-settlement فقط receipt است و نباید به‌عنوان خرید یا هزینهٔ پروژه ثبت شود.
8. reversal/void رویداد معکوس است، نه حذف تاریخچه.

## ۳. قواعد ارز

برای هر رقم ارزی گزارش باید این اطلاعات مشخص باشد:

- ارز سند؛
- مبلغ اصلی؛
- نرخ تسعیر؛
- تاریخ/منبع نرخ؛
- معادل ریالی.

اگر نرخ ارزی وجود نداشته باشد، رقم در سود رسمی وارد نمی‌شود و به‌عنوان مورد کیفیت داده گزارش می‌شود.

## ۴. خروجی reconciliation

گزارش تطبیق باید برای هر پروژه/تأمین‌کننده حداقل این ستون‌ها را داشته باشد:

| ستون | توضیح |
|---|---|
| Project/Deal | کد پرونده و درخواست |
| Sales source | فاکتور/پیشنهاد فروش مبنا |
| Real-buy cost | خرید واقعی provenanceدار |
| Direct cost | هزینهٔ مستقیم linked |
| Loss | زیان ثبت‌شده |
| Supplier invoices | فاکتورهای تأمین |
| Supplier payments | پرداخت‌های تأمین |
| Open payable | بدهی باز تأمین |
| Opening/adjustment | مانده افتتاحیه و سند اصلاحی |
| Currency/rate | ارز و نرخ هر رقم ارزی |
| Difference | اختلاف قابل بررسی، نه اصلاح خودکار |
| Classification | `matched`, `unlinked`, `undated`, `ambiguous`, `missing-rate` |

## ۵. رفتار مورد انتظار اختلاف

- `matched`: رابطه و مبلغ قابل ردیابی است.
- `unlinked`: سند وجود دارد اما رابطهٔ قطعی ندارد.
- `undated`: سال مالی از تاریخ استخراج نمی‌شود.
- `ambiguous`: بیش از یک تطبیق معتبر وجود دارد.
- `missing-rate`: سند ارزی نرخ معتبر ندارد.

هیچ‌کدام از این وضعیت‌ها نباید بدون dry-run، backup و تأیید کارفرما خودکار اصلاح شوند.

## ۶. گیت پذیرش

- گزارش فقط‌خواندنی باشد؛
- formula سود پروژه با بدهی تأمین قاطی نشود؛
- یک هزینهٔ linked دقیقاً یک‌بار در سود سال اثر کند؛
- اختلاف‌ها با شناسه و منبع نشان داده شوند؛
- دادهٔ legacy بدون inference خودکار باقی بماند؛
- تست ارز، نرخ ناقص، افتتاحیه، فاکتور linked و تعهد legacy اجرا شود.
