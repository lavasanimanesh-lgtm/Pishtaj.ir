# انتشار v34.39.23 — ۱۴۰۵/۰۷/۰۱

سه اصلاحِ بررسی‌شدهٔ این نشست، به ترتیب، با آزمون اجرایی اعمال شد.

## ۱) تماس‌ها (CONTACT-ROOTS، نشانگر v34.39.22)
شش ریشهٔ پاک‌شدن یا نامرئی‌شدن شمارهٔ تماس در کد همین درخت است و با tester676 قفل شده. حذف آگاهانه در مودالِ تازه همچنان محترم است.

## ۲) اجاره تا پرداخت نهایی (RENT-CASH-UNTIL-PAID)
خروج نقدیِ تکرارشونده و ردیفِ متصل به چک تا تسویهٔ بانکی یا سررسید چک صفر است. تعهدی (`total`) صفر نمی‌شود. هزینهٔ یک‌بارهٔ بدون چک و چکِ مستقل دست نخورده‌اند. حقوق تا draw خروج نیست. مقایسهٔ سررسید فقط با `ptfTodayISO` است و تاریخ شمسیِ آینده در برابر امروزِ میلادی سررسیدشده حساب نمی‌شود. نتیجه: اجارهٔ ۶۴۰میلیونی با چک وصل‌نشده دیگر ۱۲۸۰ میلیون خروج نمی‌سازد.

## ۳) نرخ تسعیر در ویرایش تأمین‌کننده (SUP-FX-RATE-EDIT)
ویرایش فاکتور و ویرایش پرداختِ ارزی فیلد نرخ دارند. ذخیره، `rate` و `amountIrr` را از نرخ جدید می‌سازد. ارز سند عوض نمی‌شود. نرخ صفر رد می‌شود. فاکتور ریالی فیلد نرخ ندارد. سال قفل، پیوست و `manualAmountEdit` (فقط تغییر مبلغ) سر جایشان هستند.

## آزمون
- `node _tools/uat/tester676-v34.39.22-contact-roots.js`
- `node _tools/uat/tester677-v34.39.23-rent-cash-until-paid.js`
- `node _tools/uat/tester678-v34.39.23-supplier-fx-rate-edit.js`
- رگرسیون قفل: tester384، tester290، tester502، tester508، tester628، tester630
