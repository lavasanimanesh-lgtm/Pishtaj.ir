# RELEASE NOTES — v34.7.60 (۱۴۰۵/۰۵/۲۹ — 2026-08-20)

## «متن آماده روی سربرگ» — سند چندصفحه‌ای Word با مهر و امضا در همهٔ صفحات

### نیاز کارفرما

«متن آماده‌ای از Word دارم؛ می‌خواهم تمام صفحاتش روی سربرگ شرکت برود و مهر و امضا شود،
**بدون اینکه متن تغییری کند**.»

### راه‌حل — دکمهٔ جدید در ماژول مکاتبات

**مکاتبات → «📄 متن آماده روی سربرگ»**

1. متن را در Word با Ctrl+A کپی و در کادر Paste کنید — قالب‌بندی پایه (بولد، لیست،
   جدول، تصویر) حفظ می‌شود؛ متن فقط از نظر امنیتی پاک‌سازی می‌شود (`letSafeBodyHtml`)
   و **هیچ بازنویسی/افزوده‌ای ندارد** (بسمه تعالی/گیرنده/موضوع درج نمی‌شود).
2. گزینه‌ها:
   - **زبان:** فارسی (RTL، فونت نامه‌های رسمی) / English (LTR)
   - **محل مهر و امضا:** پایین **همهٔ صفحات** (پیش‌فرض) / فقط انتهای متن / بدون
   - **تاریخ سربرگ:** اختیاری (پیش‌فرض خاموش — سند دست‌نخورده)
3. «🖨 چاپ / PDF» → از همان مسیر استاندارد چاپ (`ptfPreviewPrintableDoc`) خروجی
   می‌گیرید؛ در پنجرهٔ چاپ Margins=None و Headers خاموش (مثل نامه‌ها).

### فنی — تکرار سربرگ/مهر روی همهٔ صفحات

- سربرگ (نوارهای گرادیان + لوگو)، فوتر رسمی و بلوک مهر/امضا با `position:fixed`
  ساخته می‌شوند — در چاپ مرورگر روی **هر صفحه** تکرار می‌شوند.
- فضای بالا/پایین هر صفحه با ترفند `thead/tfoot` جدول رزرو می‌شود تا متن هرگز زیر
  سربرگ یا روی مهر نرود (thead/tfoot در چاپ در هر صفحه تکرار می‌شوند).
- تصاویر مهر/امضا از **پروفایل امضای خود کاربر** (مکاتبات → ✍️ امضای من) خوانده
  می‌شود؛ اگر ثبت نشده باشد هشدار می‌دهد و می‌توان بدون تصویر ادامه داد.
- سند رسمی است — بدون واترمارک PREVIEW؛ چاپ در audit log ثبت می‌شود.

### محدودهٔ نسخهٔ ۱

- ورودی از مسیر **Paste** است (کپی از Word). آپلود مستقیم فایل .docx در این نسخه
  پشتیبانی نمی‌شود (در صورت نیاز، فاز بعدی با پارس DOCX سمت مرورگر).
- تصاویر داخل متن Word فقط اگر هنگام Paste توسط مرورگر به data-URL تبدیل شوند منتقل
  می‌شوند (رفتار مرورگرها متفاوت است)؛ در غیر این صورت تصویر را جداگانه در متن بچسبانید.

### تست

- تستر جدید `tester463-v34.7.60-letterhead-paste.js` (در گیت) — ۲۴ قرارداد شامل
  «متن فقط sanitize می‌شود نه بازنویسی»، fixed بودن سربرگ/فوتر/مهر، رزرو thead/tfoot،
  سه حالت مهر، و نبود واترمارک.
- **گیت CI: ۷۹ PASS / ۰ FAIL** · نگهبان معماری: PASS.

### UAT پیشنهادی

1. یک متن ۳–۴ صفحه‌ای از Word کپی و Paste کنید → چاپ → هر صفحه باید سربرگ، فوتر و
   مهر/امضا داشته باشد و متن هیچ تغییری نکرده باشد.
2. حالت «فقط انتهای متن» → مهر فقط بعد از آخرین پاراگراف.
3. یک بار هم با کاربری بدون پروفایل امضا تست کنید → هشدار و ادامهٔ اختیاری.
