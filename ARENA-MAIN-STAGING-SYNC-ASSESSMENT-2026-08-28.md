# ارزیابی وضعیت main / پروداکشن / استیجینگ — ۲۰۲۶-۰۸-۲۸ (نسخهٔ ۲ — اصلاح‌شده)

**نوع سند:** ارزیابی عملیاتی + طرح همگام‌سازی مرحلهٔ اول
**دامنه:** برنچ `main`، محیط پروداکشن `pishtaj.ir`، محیط استیجینگ `staging.pishtaj.ir`
**روش:** بازرسی مستقیم گیت (کامیت/تگ/دیفرنس درخت‌ها)، لاگ GitHub Actions، دیفرنس فایل‌های قابل‌استقرار، اجرای محلی گیت‌های کیفیت روی main، و پروب‌های HTTP زنده
**تاریخ تهیه:** ۲۰۲۶-۰۸-۲۸ ساعت ~۱۲:۱۰ UTC — *نسخهٔ ۲: پس از کشف و تأیید مستقل این نکته که استقرارهای امروز «سبز ولی زنده‌نشده» هستند (جزئیات در بند ۱.۵)*
**سند مکمل:** `ARENA-MAIN-PROD-STAGING-SYNC-ASSESSMENT-2026-08-28.md` (RCA هم‌زمان روی همین برنچ — تحلیل علت ریشه‌ای مقصد FTP)

---

## ۱. خلاصه مدیریتی (یک نگاه)

| بستر | آخرین استقرار ثبت‌شده در Actions | محتوای **واقعاً زنده** روی سرور | همگام با main؟ |
|---|---|---|---|
| **main** | — (مرج PR #4 در ۱۱:۲۸ UTC) | — | مرجع |
| **پروداکشن** pishtaj.ir | `58ce2f4c` (تگ `v2026.08.28-1147`، ۱۱:۴۷ UTC) «success» | ⚠️ **نه!** درخت کهنهٔ حدود v34.8.12 (۲۵/۲۶ اوت) | ❌ |
| **استیجینگ** staging.pishtaj.ir | `e156098b` (برنچ `arena/01a047d8`، ۱۱:۰۶ UTC) «success» | ⚠️ **نه!** محتوای برنچ کهنهٔ `arena/01a0425e` (~v34.8.33) | ❌ |

سه نکتهٔ کلیدی:

1. **خط لولهٔ استقرار «سبز ولی خراب» است.** ورک‌فلوهای FTP امروز روی هر دو محیط success ثبت کرده‌اند ولی فایل‌ها **به docroot سروکنندهٔ دامنه‌ها نرسیده‌اند**. قوی‌ترین شوآن: `api/deploy-probe.php` که در استقرار ۱۰:۵۰ «موفق» آپلود شده، روی استیجینگ زنده **۴۰۴** است (۴۰۴ را نمی‌شود به کش نسبت داد)؛ و `crm/key-registry.js` که از ۲۷ اوت در مخزن هست، روی پروداکشن زنده **۴۰۴** است.
2. **main یک قدم جلوتر از هر دو محیط زنده است** (v34.8.35 در برابر ~v34.8.12 پرود و ~v34.8.33 استیج) و محتوای استیجینگِ زنده اصلاً متعلق به برنچ این ریپو نیست (بنر استیجینگ: `arena/01a0425e`).
3. ریشه‌های مشکل دو چیز است: **(الف)** مسیر/مقصد اشتباه FTP در سکرت‌های ریپوی جدید (بند ۱.۵) و **(ب)** گریپ ساختاری: workflow استیجینگ روی پوش/مرج به `main` تریگر نمی‌شود.

### ۱.۵ علت ریشه‌ای «سبز ولی زنده‌نشده» (تأیید مستقل این نشست)

- هر دو دامنه به یک سرور اشاره می‌کنند؛ در ریپوی قدیمی (`lavasanimanesh-lgtm`) دیپلوی‌ها واقعاً می‌رسیدند (پرود تا v34.8.12 در ~۲۶ اوت، استیج تا v34.8.33 در ~۲۷ اوت). بعد از بحران بیلینگ و مهاجرت به ریپوی `shaykhsofla-hash`، FTP دوباره «وصل» است و آپلود کامل انجام می‌شود (ران‌های ۱۵–۲۰ دقیقه‌ای = آپلود واقعی)، ولی خروجی جایی جز docroot سروکننده می‌رود.
- محتمل‌ترین علت: مقدار `FTP_SERVER_DIR` / `FTP_PROD_SERVER_DIR` در سکرت‌های ریپوی جدید به مسیر دیگری (پوشهٔ home، مسیر قدیمی، یا اکانت دیگری) اشاره می‌کند.
- پروب‌های مستقل این نشست (تازه):
  - `https://staging.pishtaj.ir/api/deploy-probe.php` → **۴۰۴** (فایل موجود در کامیتِ «موفق» ۱۰:۵۰)
  - `https://pishtaj.ir/crm/key-registry.js` → **۴۰۴** (فایل موجود در مخزن از ۲۷ اوت)
  - بنر صفحهٔ ۴۰۴ استیجینگ: «برنچ: `arena/01a0425e-pishtaj-ir`» = آخرین دیپلویِ واقعاً رسیده به docroot
- چرا کسی متوجه نشد؟ چون **گیت T0-4 (بررسی هش بعد از استقرار) هنوز اعمال نشده** — دقیقاً همان بندی که ممیزی ۲۰۲۶-۰۸-۲۸ 🔴 ثبت کرده بود. اگر فعال بود، هر سه دیپلوی «موفق» امروز باید fail می‌شدند.

---

## ۲. زمینهٔ مخزن (یک پاراگراف)

مخزن `shaykhsofla-hash/pishtaj.ir` در عصر ۲۰۲۶-۰۸-۲۷ (ساعت ۲۰:۴۶ UTC) ساخته شد و جایگزین مخزن قبلی `lavasanimanesh-lgtm/pishtaj.ir` است (مخزن قبلی حذف شده؛ تاریخچهٔ ۸۹۱ کامیت و PRهای #۱ تا #۸۹ آن از طریق تگ‌ها و برنچ‌های arena منتقل شده). استقرار از طریق GitHub Actions و FTP انجام می‌شود: استیجینگ خودکار (روی برنچ‌های کاری `arena/**` و…، **نه main**)، پروداکشن دستی (`workflow_dispatch` با تایید `DEPLOY`، فقط از `main` یا تگ — بعد از هر استقرار «موفق»، تگ نسخه + Release بک‌آپ ساخته می‌شود؛ همین مکانیزم امروز دو تگِ مربوط به استقرارهای زنده‌نشده ساخت).

---

## ۳. خط زمانی رویدادهای امروز (۲۰۲۶-۰۸-۲۸، همهٔ ساعت‌ها UTC)

| ساعت | رویداد | نتیجه در Actions | روی سرور زنده شد؟ |
|---|---|---|---|
| 08:17 / 08:43 / 08:48 | سه اجرای «Deploy to Production» از `main@687c43b1` | ❌ شکست در قدم FTP | — |
| **08:51–09:06** | پروداکشن از `main@687c43b1` (v34.8.34) → تگ `v2026.08.28-0905` | ✅ success | ❌ **نه** |
| 09:12 | استیجینگ ← `arena/01a0479d@b79dc642` (docs، v34.8.34) | ✅ | ❌ نه |
| 10:35 | استیجینگ ← `arena/01a047df@8bd89bcc` (T0-3/4/6، v34.8.35) | ✅ | ❌ نه |
| 10:43 / 10:44 / 10:48 | استیجینگ ← برنچ‌های `01a047df@656898ac`، `01a047d8@4ab8e426`، `01a047de@cb59ddce` | ⊘ کنسل | — |
| **10:50–11:06** | استیجینگ ← `arena/01a047d8@e156098b` | ✅ | ❌ **نه** (پروب ۴۰۴) |
| **11:28** | مرج PR #4 ← `main = 58ce2f4c` (v34.8.35) | ✅ | — |
| **11:46–11:47** | پروداکشن از `main@58ce2f4c` → تگ `v2026.08.28-1147` | ✅ | ❌ **نه** (key-registry ۴۰۴) |

**چه اتفاقی افتاد:** دو نشست موازی Arena (`arena/01a047d8` و `arena/01a047df`) فاز T0 را از پایهٔ مشترک `687c43b1` اجرا کردند؛ خروجی دومی با PR #4 به main رسید و (به‌ظاهر) پروداکشن؛ ولی به‌دلیل نقص مقصد FTP هیچ‌کدام زنده نشدند. آخرین محتوای واقعاً زنده: پرود = ~v34.8.12 (۲۵/۲۶ اوت)، استیج = برنچ `arena/01a0425e` (~v34.8.33).

---

## ۴. اختلاف فایل‌های قابل‌استقرار (main ↔ آخرین استقرار ثبت‌شدهٔ استیجینگ `e156098b`)

این دیفرنس برای مرحلهٔ بعدی (بعد از ترمیم FTP) لازم است — یعنی «چه چیزهایی باید در درخت مرج باشد تا یک دیپلوی سالم، هر دو محیط را همگام کند». ۱۲ فایل قابل‌استقرار اختلاف دارد:

### ۴.۱ چیزهایی که main دارد و استیجینگ ندارد

| فایل | محتوا در main |
|---|---|
| `crm/index.html`، `crm/manifest.json`، `crm/shell.js`، `crm/sw.js`، `crm/clear-cache.html` | رفع باگ ASSET_VERSION + نشانگر `PTF_CRM_RELEASE = v34.8.35` |
| `api/sales-domain.php` | تغییرات v34.8.35 نشست PR #4 |
| `crm/codegen.js`، `crm/treasury.js` | واریانت T5-1 نشست PR #4 (با گارد «لایهٔ داده در دسترس نیست») |
| `VERSION.json` | متن updated_by متفاوت |

### ۴.۲ چیزهایی که استیجینگ دارد و main ندارد (باید با اصلاح به main برسد)

| فایل | محتوا در استیجینگ | ارزیابی |
|---|---|---|
| `.htaccess` و `crm/.htaccess` | بلوک **no-cache شرطی استیجینگ (T0-6)** — کش js/html/css فقط وقتی هاست با `staging.`/`test.` شروع شود خاموش می‌شود؛ ریشهٔ حادثهٔ «نسخهٔ مخلوط» | ✅ ارزشمند — پورت شود (اثر صفر روی پروداکشن) |
| `api/deploy-probe.php` | **پروب صحت استقرار (T0-4)** — هش/اندازهٔ فایل‌های نسخه‌ای | ✅ ارزشمند — به main برسد |
| `api/.htaccess` | خط allowlist برای `deploy-probe.php` | ⚠️ همراه **باگ** (بند ۵.۳) — پورت با اصلاح |
| `crm/surplus.js`، `crm/finance-write-guard.js`، `crm/case-revision.js`، `crm/treasury-call.js` | واریانت T5-1 **تمیزتر**: هیچ `localStorage.setItem` مستقیمی ندارند؛ در main هنوز دارند (بند ۵.۲) | ✅ نسخهٔ استیجینگ درست‌تر است — جایگزین main شود |

---

## ۵. یافته‌های ریسک (به ترتیب اولویت)

### ۵.۱ 🔴 R1 — مقصد FTP با docroot یکی نیست (بلاکر اصلی)
تا وقتی `FTP_SERVER_DIR` / `FTP_PROD_SERVER_DIR` در سکرت‌های ریپو درست نشود، **هر دیپلوی، هر تگی و هر همگام‌سازی‌ای بی‌اثر است** و فقط تگ/Release «پایدار» های توخالی می‌سازد (دو تگ امروز: `v2026.08.28-0905` و `v2026.08.28-1147` هر دو به استقرارهای زنده‌نشده اشاره می‌کنند — برای رول‌بک واقعی معتبر نیستند).

### ۵.۲ 🔴 R2 — main هنوز بای‌پس localStorage در ۴ فایل کسب‌وکاری دارد
`crm/surplus.js:10`، `crm/finance-write-guard.js:237`، `crm/case-revision.js:252`، `crm/treasury-call.js:25` — همه `else localStorage.setItem(...)` زنده دارند؛ برنچ استیجینگ (`e156098b`) همین چهار فایل را تمیز کرده. گیت `arch-guard` سبز است چون این نمونه‌ها در baseline (سقف ۲۶۱تایی A10) لحاظ شده‌اند — گیت نمی‌گیرد، ولی هدف اعلام‌شدهٔ نسخه ناقص اجرا شده است.

### ۵.۳ 🟠 R3 — باگ syntax در `api/.htaccess` نسخهٔ استیجینگ
خط `<!-- v34.8.35 (T0-4): ... -->` کامنت معتبر آپاچی/لایت‌اسپید نیست (کامنت با `#`)؛ می‌تواند `/api/*` استیجینگ را ۵۰۰ کند. هنگام پورت با `#` نوشته شود.

### ۵.۴ 🟠 R4 — هیچ گیت واقعی در CI اجرا نمی‌شود + php.yml قرمزِ دائمی
وصلهٔ T0-3 (`_tools/PENDING-workflow-t0-gates-2026-08-28.patch`) اعمال نشده؛ `php.yml` قالب پیش‌فرض Composer است و چون `composer.json` نیست **هر PR قرمز می‌شود** — عادت «نادیده‌گرفتن CI قرمز» دقیقاً همان چیزی است که اجازه داد R1 تا شب مخفی بماند.

### ۵.۵ 🟠 R5 — تریگر استیجینگ شامل main نیست
`deploy-staging.yml` روی `development`, `site/**`, `crm/**`, `staging/**`, `arena/**` تریگر می‌شود نه `main` — حتی با FTP سالم، هر مرج به main دوباره drift می‌سازد.

### ۵.۶ 🟡 R6 — PRهای باز و تعارض‌دار + برخورد برچسب نسخه
PR #2 (`arena/01a047d8` = محتوای استقراری استیجینگ) و PR #3 (`arena/01a047de`) باز و CONFLICTING هستند (باید بعد از پورت بسته شوند؛ خودِ برنچ `arena/01a047d8` تا پایان کار آرشیو شود نه حذف). ضمناً main و استیجینگِ ثبت‌شده هر دو «v34.8.35»اند با درخت متفاوت — در همگام‌سازی به **v34.8.36** برسد.

### ۵.۷ 🟡 R7 — ملاحظات راستی‌آزمایی
`VERSION.json` روی وب 403 است؛ صحت استقرار باید از `PTF_CRM_RELEASE` در `crm/index.html`، پروب هش و بنر استیجینگ چک شود. کش عمومی هم کهنه سرو می‌کند (رندرهای fetch قدیمی) — برای مشاهدهٔ دستی `Ctrl+Shift+R`.

### ۵.۸ ✅ خبر خوب — پایهٔ main سالم است
روی درخت فعلی main به‌صورت محلی: `arch-guard` → **PASS** و `run-ci-gate.js` → **152 PASS / 0 FAIL**. یعنی بعد از ترمیم FTP، همگام‌سازی از پایهٔ main کم‌ریسک است.

---

## ۶. طرح مرحلهٔ اول — رساندن main و استیجینگ به همگامی واقعی

**هدف:** (۱) خط لوله سالم شود، (۲) استیجینگ دقیقاً برابر main زنده شود، (۳) پروداکشن دست‌نخورده بماند تا تایید استیجینگ.
**اصول:** main مرجع است؛ چیز مفید برنچ استیجینگ به main پورت می‌شود؛ هیچ «تگ پایدار»ی قبل از سبزشدن گیت T0-4 ساخته نشود.

### گام ۰ — 🔴 ترمیم مقصد FTP (بلاکر؛ بدون این، بقیه بی‌معنی است)
1. در پنل هاست، docroot واقعی هر دو دامنه را پیدا کنید (مثلاً `/home/USER/public_html` و `.../staging.pishtaj.ir` یا subsomain جدا).
2. سکرت‌های ریپو را مطابقت/اصلاح کنید: `FTP_SERVER_DIR` (استیجینگ) و `FTP_PROD_SERVER_DIR` (پروداکشن) — Settings → Secrets and variables → Actions.
3. تست کناری: یک فایل کناری (مثل `_deploy-canary.txt` با timestamp) از طریق یک ران دستی آپلود و در مرورگر باز کنید — یا مستقیم میزبانی فایل را از پنل چک کنید که آپلودهای امروز (`VERSION.json` با تاریخ ۲۰۲۶-۰۸-۲۸) کجا رسیده‌اند.
4. ⚠️ دو تگ امروز (`v2026.08.28-0905`, `v2026.08.28-1147`) را در ذهنیت «رول‌بک» باطل حساب کنید — به استقرار زنده‌نشده اشاره دارند؛ وضعیت واقعی پرود = همان درخت کهنهٔ زنده.

### گام ۱ — برنچ همگام‌سازی از main
```bash
git checkout main && git pull
git checkout -b sync/staging-v34.8.36
```

### گام ۲ — پورت چهار فایل T5-1 تمیز استیجینگ (پایه‌شان با main یکی است — بدون تعارض)
```bash
git checkout e156098b -- crm/surplus.js crm/finance-write-guard.js crm/case-revision.js crm/treasury-call.js
```

### گام ۳ — پورت T0-6 و پروب T0-4
```bash
git checkout e156098b -- .htaccess crm/.htaccess api/deploy-probe.php
```
`api/.htaccess` را **دستی** اصلاح کنید: خط `deploy-probe` به allowlist اضافه شود ولی کامنت با `#` نوشته شود (باگ ۵.۳ — کپی نکنید).

### گام ۴ — ارتقای نسخه به v34.8.36 (رفع برخورد برچسب ۵.۶)
`VERSION.json` → `v34.8.36` + ۶ نقطهٔ رسمی نسخه (ازجمله `PTF_CRM_RELEASE` در `crm/index.html`)، سپس:
```bash
node _tools/uat/bump-version-pins.js 34.8.35
```

### گام ۵ — گیت‌های محلی (باید سبز باشند)
```bash
node _tools/arch/arch-guard.js
node _tools/uat/run-ci-gate.js    # انتظار: 152 PASS / 0 FAIL
```

### گام ۶ — اعمال وصلهٔ workflowها + ترمیم تریگر (R4 و R5)
```bash
git apply _tools/PENDING-workflow-t0-gates-2026-08-28.patch
git rm _tools/PENDING-workflow-t0-gates-2026-08-28.patch
```
و در `deploy-staging.yml` به برنچ‌های `push`، `main` اضافه شود (استیجینگ همیشه دنبال‌کنندهٔ main). با فعال‌بودن گیت T0-4، هیچ دیپلویِ «سبز ولی زنده‌نشده»ای دیگر می‌گذرد.
⚠️ پوش تغییرات `.github/workflows/` توکن با دسترسی `workflows` می‌خواهد؛ اگر پوش ایجنت رد شد، فقط همین کامیت را مالک مخزن پوش کند.

### گام ۷ — مرج به main و استقرار روی استیجینگ
- PR `sync/staging-v34.8.36` → main (بعد از سبزشدن گیت‌های T0-3).
- استقرار استیجینگ: خودکار (اگر تریگر main فعال شد) یا دستی: Actions → **Deploy to Staging** → Run workflow → `main`.

### گام ۸ — چک‌لیست پذیرش (Definition of Done مرحلهٔ اول)
- [ ] **گیت post-deploy (T0-4) در ران سبز است** — هش فایل‌های زنده == کامیت (مهم‌ترین قلم!)
- [ ] بنر استیجینگ: برنچ `main` (نه `arena/01a0425e` و نه برنچ کاری قدیمی)
- [ ] `PTF_CRM_RELEASE` زنده == `v34.8.36` == `VERSION.json`
- [ ] `api/deploy-probe.php` زنده = 200 و هش‌ها مطابق کامیت
- [ ] هیچ ۵۰۰ روی `api/*.php` استیجینگ (اثبات اصلاح ۵.۳)
- [ ] `Cache-Control: no-store` روی js استیجینگ (T0-6 فعال)
- [ ] لاگین دود-تست CRM + یک ذخیرهٔ خزانه/مالی
- [ ] PR #2 و PR #3 بسته شوند؛ برنچ `arena/01a047d8` آرشیو شود
- [ ] php.yml تبدیل به Quality Gate سبز شده باشد

### گام ۹ — مرحلهٔ دوم (بعد از چند ساعت soak موفق استیجینگ)
Actions → **Deploy to Production** → `ref=main`، `confirm=DEPLOY`؛ پذیرش: همان چک‌لیست گام ۸ روی `pishtaj.ir` (نسخهٔ زنده = v34.8.36، key-registry.js = 200، گیت T0-4 سبز). این‌بار تگِ ساخته‌شده واقعاً «پایدار» خواهد بود.

---

## ۷. جمع‌بندی

- **main سالم و جلوتر از هر دو محیط زنده است** (گیت‌های محلی 152/152 سبز) — ولی امروز سه استقرار «موفق» هیچ کدام زنده نشدند؛ پروداکشن واقعاً ~v34.8.12 و استیجینگ واقعاً محتوای برنچ کهنهٔ `arena/01a0425e` را سرو می‌کند.
- **بلاکر اصلی مقصد FTP است نه کد** — تا سکرت مسیر درست نشود، هیچ همگام‌سازی ممکن نیست (گام ۰).
- بعد از ترمیم FTP، یک PR کوچک (`sync/staging-v34.8.36`: چهار فایل T5-1 تمیز + T0-6 + پروب + bump نسخه + وصلهٔ گیت‌ها + تریگر main) استیجینگ را دقیقاً برابر main می‌کند و ساختار (تریگر main→staging و گیت T0-4) جلوی تکرار هم drift و هم «سبزِ توخالی» را می‌گیرد.

*تهیه‌شده توسط Arena Agent — نشست `arena/01a0482b` — ۲۰۲۶-۰۸-۲۸ — نسخهٔ ۲ با تأیید مستقل پروب‌های زنده (deploy-probe ۴۰۴ / key-registry ۴۰۴ / بنر arena/01a0425e)*
