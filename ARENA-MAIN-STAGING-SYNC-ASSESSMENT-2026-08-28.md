# ارزیابی وضعیت main / پروداکشن / استیجینگ — ۲۰۲۶-۰۸-۲۸

**نوع سند:** ارزیابی عملیاتی + طرح همگام‌سازی مرحلهٔ اول
**دامنه:** برنچ `main`، محیط پروداکشن `pishtaj.ir`، محیط استیجینگ `staging.pishtaj.ir`
**روش:** بازرسی مستقیم گیت (کامیت/تگ/دیفرنس درخت‌ها)، لاگ GitHub Actions، دیفرنس فایل‌های قابل‌استقرار، و اجرای محلی گیت‌های کیفیت روی main
**تاریخ تهیه:** ۲۰۲۶-۰۸-۲۸ ساعت ~۱۱:۵۰ UTC

---

## ۱. خلاصه مدیریتی (یک نگاه)

| بستر | کامیت | نسخهٔ CRM | همگام با main؟ |
|---|---|---|---|
| **main** | `58ce2f4c` (مرج PR #4) | v34.8.35 | — (مرجع) |
| **پروداکشن** pishtaj.ir | `58ce2f4c` ← تگ `v2026.08.28-1147` | v34.8.35 | ✅ **بله** (درخت یکسان — استقرار ۱۱:۴۶ UTC امروز) |
| **استیجینگ** staging.pishtaj.ir | `e156098b` (برنچ `arena/01a047d8`) | v34.8.35 (برچسب؛ درخت متفاوت!) | ❌ **خیر — ۱۲ فایل قابل‌استقرار اختلاف** |

سه نکتهٔ کلیدی:

1. **پروداکشن و main هم‌اکنون کاملاً همگام‌اند.** دقیقاً حین همین ارزیابی (۱۱:۴۶:۵۵ UTC) یک استقرار موفق از `main@58ce2f4c` روی پروداکشن انجام و تگ `v2026.08.28-1147` + Release بک‌آپ ساخته شد.
2. **استیجینگ تنها محیط منحرف است** — و نکتهٔ خطرناک: هم `main` و هم استیجینگ برچسب «v34.8.35» دارند ولی **کد متفاوتی** اجرا می‌کنند. این بدترین حالت برای دیباگ است (شمارهٔ نسخه دیگر معنای «یک درخت مشخص» را نمی‌دهد).
3. ریشهٔ ساختاری انحراف: workflow استیجینگ روی پوش به `arena/**`، `site/**`، `crm/**`، `staging/**` و `development` تریگر می‌شود ولی **روی پوش/مرج به `main` خیر** — یعنی هر مرج به main به‌طور خودکار هرگز استیجینگ را به‌روز نمی‌کند.

---

## ۲. زمینهٔ مخزن (یک پاراگراف)

مخزن `shaykhsofla-hash/pishtaj.ir` در عصر ۲۰۲۶-۰۸-۲۷ (ساعت ۲۰:۴۶ UTC) ساخته شده و جایگزین مخزن قبلی `lavasanimanesh-lgtm/pishtaj.ir` شده است (مخزن قبلی دیگر وجود ندارد؛ تاریخچهٔ ۸۹۱ کامیت و PRهای #۱ تا #۸۹ آن از طریق تگ‌ها و برنچ‌های arena منتقل شده است). استقرار از طریق GitHub Actions و FTP انجام می‌شود: استیجینگ خودکار (روی برنچ‌های کاری)، پروداکشن دستی (`workflow_dispatch` با تایید `DEPLOY`، فقط از `main` یا تگ — بعد از هر استقرار موفق، تگ نسخه + Release بک‌آپ ساخته می‌شود).

---

## ۳. خط زمانی رویدادهای امروز (۲۰۲۶-۰۸-۲۸، همهٔ ساعت‌ها UTC)

| ساعت | رویداد | نتیجه |
|---|---|---|
| 08:17 / 08:43 / 08:48 | سه اجرای «Deploy to Production» از `main@687c43b1` | ❌ شکست در قدم FTP |
| **08:51–09:06** | استقرار پروداکشن از `main@687c43b1` (v34.8.34) | ✅ تگ `v2026.08.28-0905` + Release |
| 09:12 | استیجینگ ← `arena/01a0479d@b79dc642` (docs، v34.8.34) | ✅ |
| 10:35 | استیجینگ ← `arena/01a047df@8bd89bcc` (T0-3/4/6، v34.8.35) | ✅ |
| 10:43 | استیجینگ ← `arena/01a047df@656898ac` (نسخهٔ نهایی همان نشست) | ⊘ کنسل شد |
| 10:44 / 10:48 | استیجینگ ← `arena/01a047d8@4ab8e426` و `arena/01a047de@cb59ddce` | ⊘ کنسل شد |
| **10:50–11:06** | استیجینگ ← `arena/01a047d8@e156098b` — **آخرین محتوای زندهٔ استیجینگ** | ✅ |
| **11:28** | مرج PR #4 ← `main = 58ce2f4c` (v34.8.35) | ✅ |
| **11:46–11:47** | استقرار پروداکشن از `main@58ce2f4c` → تگ `v2026.08.28-1147` + Release | ✅ |

**چه اتفاقی افتاد (تحلیل):** دو نشست موازی Arena (برنچ‌های `arena/01a047d8` و `arena/01a047df`) هر دو فاز T0 رودمپ «نازک‌سازی کلاینت» را از یک پایهٔ مشترک (`687c43b1` = همان چیزی که صبح روی پروداکشن رفت) اجرا کردند. خروجی نشست دوم از طریق PR #4 به main رسید؛ ولی آخرین استقرار استیجینگ از برنچ نشستِ اول انجام شد. نتیجه: استیجینگ و main دو «واریانت موازی» از v34.8.35 هستند که هیچ‌کدام شامل کل کار دیگری نیست.

---

## ۴. اختلاف دقیق فایل‌های قابل‌استقرار (main ↔ استیجینگ)

۱۲ فایل قابل‌استقرار (غیر از مستندات/_tools که اصلاً deploy نمی‌شوند) بین `main(58ce2f4c)` و محتوای زندهٔ استیجینگ `e156098b` اختلاف دارند:

### ۴.۱ چیزهایی که main دارد و استیجینگ **ندارد** (استیجینگ از پرود هم عقب است!)

| فایل | محتوا در main |
|---|---|
| `crm/index.html`، `crm/manifest.json`، `crm/shell.js`، `crm/sw.js`، `crm/clear-cache.html` | رفع باگ ASSET_VERSION + نشانگر `PTF_CRM_RELEASE = v34.8.35` (همین‌ها الان روی **پروداکشن** زنده‌اند ولی روی استیجینگ نیستند) |
| `api/sales-domain.php` | تغییرات v34.8.35 نشست PR #4 |
| `crm/codegen.js`، `crm/treasury.js` | واریانت T5-1 نشست PR #4 (با گارد «لایهٔ داده در دسترس نیست») |
| `VERSION.json` | متن updated_by متفاوت |

### ۴.۲ چیزهایی که استیجینگ دارد و main **ندارد** (باید با اصلاح به main برسد)

| فایل | محتوا در استیجینگ | ارزیابی |
|---|---|---|
| `.htaccess` و `crm/.htaccess` | بلوک **no-cache شرطی استیجینگ (T0-6)** — کش js/html/css فقط وقتی هاست با `staging.`/`test.` شروع شود خاموش می‌شود؛ ریشهٔ حادثهٔ «نسخهٔ مخلوط» | ✅ ارزشمند — باید به main پورت شود (اثر صفر روی پروداکشن) |
| `api/deploy-probe.php` | **پروب صحت استقرار (T0-4)** — هش/اندازهٔ فایل‌های نسخه‌ای را برمی‌گرداند | ✅ ارزشمند — باید به main برسد |
| `api/.htaccess` | خط allowlist برای `deploy-probe.php` | ⚠️ همراه با **باگ** (بند ۵.۳) — پورت با اصلاح |
| `crm/surplus.js`، `crm/finance-write-guard.js`، `crm/case-revision.js`، `crm/treasury-call.js` | واریانت T5-1 **تمیزتر**: هر ۴ فایل در استیجینگ هیچ `localStorage.setItem` مستقیمی ندارند؛ در main هنوز دارند (بند ۵.۲) | ✅ نسخهٔ استیجینگ درست‌تر است — باید جایگزین main شود |

---

## ۵. یافته‌های ریسک

### ۵.۱ تریگر استیجینگ شامل main نیست (ریشهٔ ساختاری drift)
`deploy-staging.yml` فقط روی `development`, `site/**`, `crm/**`, `staging/**`, `arena/**` تریگر می‌شود. تا این اضافه نشود، **هر مرج به main دوباره همان انحراف امروز را می‌سازد**.

### ۵.۲ main هنوز بای‌پس localStorage در ۴ فایل کسب‌وکاری دارد
- `crm/surplus.js:10` → `else localStorage.setItem(...)`
- `crm/finance-write-guard.js:237` → `else try { localStorage.setItem(FIN_EV_KEY, ...) }`
- `crm/case-revision.js:252` → `else localStorage.setItem(KEY, ...)`
- `crm/treasury-call.js:25` → `else localStorage.setItem(KEY, ...)`

نسخهٔ برنچ استیجینگ (`e156098b`) همین ۴ فایل را طبق اصل E2/T5-1 تمیز کرده است. توجه: گیت `arch-guard` روی main **سبز** است چون این نمونه‌ها در baseline (تعداد مجاز ۲۶۱تایی A10) لحاظ شده‌اند — یعنی گیت ما را نمی‌گیرد ولی هدف اعلام‌شدهٔ نسخه («حذف fallbackهای مرده») ناقص اجرا شده است.

### ۵.۳ باگ احتمالی روی api استیجینگ: کامنت نامعتبر در `api/.htaccess`
نسخهٔ استقراریافتهٔ استیجینگ شامل این خط است:
```
<!-- v34.8.35 (T0-4): deploy-probe.php — ... -->
```
`<!-- -->` در syntax آپاچی/لایت‌اسپید **کامنت معتبر نیست** (کامنت با `#` است) و به‌عنوان دستور ناشناخته parse می‌شود که می‌تواند همهٔ درخواست‌های `/api/*` استیجینگ را ۵۰۰ کند. هنگام پورت، این خط باید با `#` نوشته شود و بلافاصله صحت `/api/*.php` استیجینگ بررسی شود.

### ۵.۴ هیچ گیت واقعی در CI اجرا نمی‌شود + php.yml قرمزِ دائمی
- وصلهٔ اتصال گیت‌ها به Actions (T0-3) به‌صورت `_tools/PENDING-workflow-t0-gates-2026-08-28.patch` تحویل شده ولی **هنوز اعمال نشده** (نوشتن روی `.github/workflows/` توکن با دسترسی `workflows` می‌خواهد که اپلیکیشن نشست نداشت).
- `php.yml` هنوز قالب پیش‌فرض «PHP Composer» است؛ چون `composer.json` در مخزن نیست، **هر PR قرمز می‌شود** (شکست در ۱۱–۱۵ ثانیه). همین باعث شده عادت شود که CI قرمز نادیده گرفته شود — خطرناک.

### ۵.۵ PRهای باز و تعارض‌دار
PR #2 (برنچ `arena/01a047d8` — همان محتوای استیجینگ) و PR #3 (برنچ `arena/01a047de`) هر دو **باز و CONFLICTING** هستند. محتوای مفیدشان (بند ۴.۲) باید پورت شود و بعد بسته شوند؛ برنچ `arena/01a047d8` تا پایان مرحلهٔ اول **نباید حذف شود** (تنها رکورد محتوای فعلی استیجینگ است).

### ۵.۶ برخورد برچسب نسخه
هر دو درخت `main` و استیجینگ `v34.8.35` هستند ولی کدشان متفاوت است. در همگام‌سازی، نسخه باید به **v34.8.36** برسد تا برچسب نسخه دوباره «یک به یک» با درخت کد معنا پیدا کند (عنوان PR #4 هم همین را پیش‌بینی کرده بود: «v34.8.35→36»).

### ۵.۷ نکات راستی‌آزمایی زنده
- `VERSION.json` روی وب 403 است (بیرون از دسترس) — صحت استقرار باید از `PTF_CRM_RELEASE` در `crm/index.html` و پروب هش چک شود.
- رندر کش‌شدهٔ fetch از هر دو سایت، اسنپ‌شاتِ **قبل از ۰۸:۰۰ UTC** را نشان می‌داد (بنر استیجینگِ برنچ قدیمی `arena/01a0425e` و نسخه‌های کهنه در داشبورد) — یعنی کش عمومی/CDN هنوز محتوا کهنه سرو می‌کند؛ برای مشاهدهٔ واقعی حتماً `Ctrl+Shift+R` یا پروب هش. (موتور T0-6 دقیقاً برای همین ساخته شده.)

### ۵.۸ خبر خوب — پایهٔ main سالم است
روی درخت فعلی main به‌صورت محلی اجرا شد:
- `node _tools/arch/arch-guard.js` → **PASS** (همهٔ قواعد A1–A11)
- `node _tools/uat/run-ci-gate.js` → **152 PASS / 0 FAIL**

یعنی همگام‌سازی از پایهٔ main کم‌ریسک است و گیت‌ها جلوی پس‌رفت را می‌گیرند.

---

## ۶. طرح مرحلهٔ اول — همگام‌سازی main و استیجینگ

**هدف:** استیجینگ دقیقاً برابر main شود (به‌علاوهٔ موارد ارزشمند استیجینگ که به main برمی‌گردد)، بدون هیچ تغییری روی پروداکشن.
**اصول:** main مرجع است؛ چیز مفید استیجینگ به main پورت می‌شود نه برعکس؛ پروداکشن در این مرحله دست نمی‌خورد (الان == main است).

### گام ۱ — برنچ همگام‌سازی از main
```bash
git checkout main && git pull
git checkout -b sync/staging-v34.8.36
```

### گام ۲ — پورت چهار فایل T5-1 تمیز استیجینگ (بدون تعارض — پایه‌شان با main یکی است)
```bash
git checkout e156098b -- crm/surplus.js crm/finance-write-guard.js crm/case-revision.js crm/treasury-call.js
```

### گام ۳ — پورت T0-6 (no-cache استیجینگ) و پروب T0-4
```bash
git checkout e156098b -- .htaccess crm/.htaccess api/deploy-probe.php
```
سپس `api/.htaccess` را **دستی** اصلاح کنید: خط `deploy-probe` را به allowlist اضافه کنید ولی کامنتش را با `#` بنویسید (باگ ۵.۳ — از فایل استیجینگ کپی نکنید).

### گام ۴ — ارتقای نسخه به v34.8.36 (رفع برخورد برچسب ۵.۶)
- `VERSION.json` → `crm_version: v34.8.36` + `last_updated`
- ۶ نقطهٔ رسمی نسخه (طبق RELEASE-NOTES) ازجمله `PTF_CRM_RELEASE` در `crm/index.html`
- سپس: `node _tools/uat/bump-version-pins.js 34.8.35` (به‌روزرسانی خودکار pinهای تسترها)

### گام ۵ — گیت‌های محلی (باید سبز باشند)
```bash
node _tools/arch/arch-guard.js
node _tools/uat/run-ci-gate.js    # انتظار: 152 PASS / 0 FAIL
```

### گام ۶ — اعمال وصلهٔ workflowها + ترمیم تریگر (بند ۵.۱ و ۵.۴)
```bash
git apply _tools/PENDING-workflow-t0-gates-2026-08-28.patch
git rm _tools/PENDING-workflow-t0-gates-2026-08-28.patch
```
و در `.github/workflows/deploy-staging.yml` به لیست برنچ‌های `push`، `main` را اضافه کنید:
```yaml
  push:
    branches:
      - main              # ← استیجینگ همیشه دنبال‌کنندهٔ main
      - development
      - 'site/**'
      - 'crm/**'
      - 'staging/**'
      - 'arena/**'
```
⚠️ **نکتهٔ مجوز:** پوش کامیت‌های شامل `.github/workflows/` توکن با دسترسی `workflows` لازم دارد. اگر پوش از محیط ایجنت رد شد، فقط همین یک کامیت را مالک مخزن باید پوش کند (بقیهٔ کار مستقل از این کامیت پیش می‌رود).

### گام ۷ — مرج به main و استقرار روی استیجینگ
- PR `sync/staging-v34.8.36` → main (مرج پس از سبزشدن گیت‌های T0-3).
- اگر تریگر main (گام ۶) فعال شده باشد، پوش به main خودکار استیجینگ را به‌روز می‌کند؛ در غیر این صورت دستی:
  GitHub → Actions → **Deploy to Staging** → Run workflow → برنچ `main`.

### گام ۸ — چک‌لیست پذیرش استیجینگ (Definition of Done مرحلهٔ اول)
- [ ] بنر استیجینگ نشان دهد: برنچ `main` (یا برنچ همگام‌سازی)
- [ ] `PTF_CRM_RELEASE` در `crm/index.html` زنده == `v34.8.36` == `VERSION.json` مخزن
- [ ] هش SHA-1 فایل‌های کلیدی زنده (`crm/index.html`, `crm/sw.js`, `crm/shell.js`, `crm/sales-domain-v2.js`, …) == کامیت مرج‌شده (از طریق `api/deploy-probe.php` یا مقایسهٔ دستی)
- [ ] هیچ ۵۰۰ روی `api/*.php` استیجینگ (اثبات اصلاح باگ ۵.۳)
- [ ] روی فایل‌های js استیجینگ هدر `Cache-Control: no-store` برمی‌گردد (اثبات فعال‌بودن T0-6)
- [ ] لاگین دود-تست CRM + یک ذخیره در خزانه/مالی (مسیر T5-1 سالم)
- [ ] `git diff main` نسبت به درخت استقرار == فقط `robots.txt` و بنر تزریقی
- [ ] PR #2 و PR #3 بسته شوند (با ارجاع به همین سند)؛ برنچ `arena/01a047d8` آرشیو شود
- [ ] بعد از اعمال وصله: هر دو workflow در Actions گیت‌ها را واقعاً اجرا کنند و php.yml تبدیل به Quality Gate سبز شود

### گام ۹ — مرحلهٔ دوم (بعد از تأیید استیجینگ؛ خارج از scope همین سند)
پس از چند ساعت soak-test موفق استیجینگ: Actions → **Deploy to Production** → `ref=main`، `confirm=DEPLOY` → تگ نسخه و Release بک‌آپ به‌صورت خودکار ساخته می‌شود؛ رول‌بک در دسترس با تگ `v2026.08.28-1147` (وضعیت فعلیِ سالم پروداکشن).

---

## ۷. جمع‌بندی

- **پروداکشن و main همگام‌اند** (تگ `v2026.08.28-1147`) — این نقطهٔ امن امروز است و به‌عنوان مرجع و نقطهٔ رول‌بک ثبت شد.
- **استیجینگ** روی یک برنچ کاری قدیمی‌تر از مرج گیر کرده و هم از main عقب است و هم دو مورد اصلاح ارزشمند (T0-6 و پروب T0-4 + چهار فایل تمیز T5-1) را دارد که در main نیامده.
- با اجرای گام‌های ۱ تا ۸ بالا، در یک PR کوچک و کم‌تعارض: استیجینگ == main == v34.8.36، گیت‌های CI واقعاً متصل، و تریگر «main → استیجینگ» برای همیشه جلوی تکرار این drift را می‌گیرد.

*تهیه‌شده توسط Arena Agent — نشست `arena/01a0482b` — ۲۰۲۶-۰۸-۲۸*
