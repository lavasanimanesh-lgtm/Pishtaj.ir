# Release Notes v34.7.5 — کد رهگیری تصادفی عمومی

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## کد رهگیری امن و غیرترتیبی

- کدهای جدید استعلام سایت با قالب `PTF-RFQ-ABCDE-23456` صادر می‌شوند.
- کدهای جدید ثبت‌نام تامین‌کننده با قالب `PTF-VEN-ABCDE-23456` صادر می‌شوند.
- token ده‌نویسه‌ای در سرور با `random_int` و الفبای ۳۲ نویسه‌ای `ABCDEFGHJKLMNPQRSTUVWXYZ23456789` ساخته می‌شود؛ نویسه‌های مبهم `0/O/1/I` حذف شده‌اند.
- کد دیگر سال، sequence یا تعداد ثبت‌ها را افشا نمی‌کند و تقریباً ۵۰ بیت فضای تصادفی دارد.
- مولد مشترک، collision را در هر دو مجموعه استعلام‌ها و تامین‌کنندگان بررسی می‌کند و پس از ۲۰ تلاش ناموفق به‌صورت fail-closed پاسخ `503` می‌دهد.
- هر سه مسیر ثبت `add_rfq_site`، مسیر قدیمی `add_rfq` و `add_supplier` فقط کد سرورساز دریافت می‌کنند؛ ورودی code مرورگر و `rand()` قدیمی حذف شد.

## سازگاری با کدهای قدیمی

- هیچ داده‌ای مهاجرت یا بازشماره‌گذاری نشد.
- action رهگیری همچنان تطبیق دقیق code را بدون اجبار قالب انجام می‌دهد؛ بنابراین کدهای ترتیبی قدیمی `PTF-RFQ-{سال}-{شماره}` و `PTF-VEN-{سال}-{شماره}` در کنار کدهای تصادفی جدید قابل جستجو هستند.
- fixtureهای ترتیبی قدیمی عمداً در UAT حفظ شدند تا سازگاری عقب‌رو پوشش داده شود.

## اصلاح رابط کاربری

- عبارت انگلیسی `(Vendor Portal v22.0)` از عنوان پورتال تامین‌کنندگان حذف شد.
- نمونه‌های کد در صفحات استعلام، تامین‌کننده، رهگیری و placeholderهای مرتبط با قالب جدید هم‌تراز شدند.

## کنترل کیفیت

- `tester410-v34.7.5-random-public-tracking.js`: 14 PASS / 0 FAIL
- تست زنجیره استعلام سایت `tester205`: 18 PASS / 0 FAIL
- تست زنجیره فرم صفحه اصلی `tester207`: 12 PASS / 0 FAIL
- تست bridge و سازگاری fixtureهای قدیمی `tester6`: 65 PASS / 0 FAIL
- CI متمرکز ریلیز: 29 PASS / 0 FAIL
- رگرسیون کامل: 393 تستر، 336 فایل PASS، 57 فایل بدهی تاریخی FAIL، 5819 check PASS و 129 check تاریخی FAIL؛ هیچ شکست جدیدی نسبت به v34.7.4 ایجاد نشد.
- PHP CLI در محیط محلی موجود نبود و lint محلی PHP در CI به‌صورت صریح skip شد؛ سایر syntax checkهای CI سبز هستند.
- Auditor روی 624 صفحه و هم‌ترازی VERSION/PWA: PASS.
