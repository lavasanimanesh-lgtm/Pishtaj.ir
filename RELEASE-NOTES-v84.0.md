# 🚀 ریلیز v84.0 — اسپرینت ۸۴: پیشنهاد ارزی + ستون‌های آزاد + قالب‌های PDF حرفه‌ای + چرخه مقایسه قیمت خرید + ادغام تامین‌کننده

**تاریخ:** ۱۴ تیر ۱۴۰۵ | **دامنه:** US-182 تا US-187 (شش استوری جدید کارفرما)

---

## 💶 US-182 — پیشنهاد مالی ارزی (EUR / USD)
- فرم CO کشویی **«ارز پیشنهاد»** گرفت: ریال ایران / یورو / دلار آمریکا.
- قیمت‌های ارزی با **۲ رقم اعشار** (€1,234.50)، ریالی بدون اعشار.
- سند چاپی: علامت ارز در سرستون‌ها (`Unit Price (€)`)، جمع کل با کد ارز و **مبلغ به حروف** با نام ارز (`One Thousand ... Euros`).

## ↔️ US-183 — مدیریت کامل ستون‌های TO/CO
- **جابجایی ستون‌ها با درگ** سرستون (⠿) به چپ/راست — ستون‌های اصلی و اضافی هر دو.
- **حذف هر ستونی** (حتی Model/Brand) با ✕ + بازگرداندن از نوار «ستون‌های حذف‌شده».
- **افزودن ستون بدون محدودیت** (سقف ۴تایی قبلی برداشته شد؛ فونت سند پلکانی تا 7.5px کوچک می‌شود).
- **«Sr. No.» → «No.»** در همه اسناد چاپی.
- ترتیب و حذف ستون‌ها **در PDF هم دقیقاً رعایت می‌شود**.

## 📄 US-184 — اصلاح سربرگ مکاتبات
تفاوت با سربرگ تایید‌شده رفع شد: **سه خط مورب سفید** (زاویه ۳۵-درجه) روی نوار گرادیان بالا و پایین اضافه شد — دقیقاً همان موقعیت‌های فایل مرجع (نیازی به ارسال مجدد تصویر نیست؛ فایل `letterhead-template.html` تاییدشده شما مبنا بود).

## 🎨 US-185 — قالب‌های PDF حرفه‌ای با انتخاب کاربر
دکمه چاپ TO/CO حالا **دیالوگ انتخاب قالب** باز می‌کند — با پیش‌نمایش (واترمارک PREVIEW) قبل از انتخاب نهایی:

| قالب | مشخصات |
|---|---|
| 🏢 **سربرگ رسمی** (پیشنهادی) | A4 عمودی — نوار گرادیان نارنجی + خطوط مورب سفید، هم‌خانواده کامل سربرگ مکاتبات؛ سرستون گرادیان، جدول کرم-طلایی |
| 📊 **کلاسیک افقی** | A4 افقی — برای اقلام زیاد یا ستون‌های اضافی |
| ⬜ **مینیمال بین‌المللی** | A4 عمودی — تک‌رنگ مشکی/طلایی، فضای سفید زیاد، تایپوگرافی Helvetica، سبک شرکت‌های اروپایی |

انتخاب هر کاربر ذخیره می‌شود و پیش‌نمایش داخل فرم هم با همان قالب است. تایپوگرافی: Georgia برای عناوین، اعداد tabular، فونت پلکانی هوشمند بر اساس تعداد ستون.

## 📊 US-186 — چرخه کامل استعلام قیمت خرید (طراحی متخصص ارشد)
ماژول «قیمت‌های خرید» **از نو ساخته شد** (`crm/buycompare.js`):

1. **انتخاب درخواست از کشویی** (اقلام خودکار از اقلام ایمپورت‌شده/CO می‌آیند)
2. **قیمت دور ۱** (قبل از صدور پیشنهاد مالی): per آیتم per تامین‌کننده
3. بعد از **برنده شدن CO** → دکمه بنفش **«قیمت دور ۲»** فعال می‌شود (تامین‌کنندگان جدید هم قابل افزودن)
4. **جدول مقایسه**: هر آیتم یک ردیف، هر تامین‌کننده یک ستون؛ سیستم **کمترین قیمت را ✅ هایلایت** و تامین‌کننده ارزان‌تر را اعلام می‌کند (دور ۲ بر دور ۱ همان تامین‌کننده اولویت دارد)
5. **🛍 ثبت خرید نهایی per آیتم** توسط کاربر مجاز (نقش‌های دارای buyPrice): پیش‌فرض = کمترین قیمت، قابل تغییر با صلاحدید → ثبت در purchases + اعلان به مدیران ارشد
- داده‌ها در کلید جدید `ptf_crm_buycmp` (سینک/بک‌آپ/whitelist سرور اضافه شد). رکوردهای قدیمی از دست نمی‌روند (بخش «قیمت‌های قدیمی»).

## 🔗 US-187 — ادغام تامین‌کننده سایت با رکورد موجود
وقتی تامین‌کننده‌ای که قبلاً توسط کاربر ثبت شده، خودش از سایت هم ثبت‌نام کند:
- هنگام تایید، سیستم تکرار را تشخیص می‌دهد و به‌جای رکورد تکراری، دیالوگ **«🔗 ادغام و تایید / ثبت جدید / انصراف»** با نمایش رکورد قبلی (کد، تاریخ، ثبت‌کننده) باز می‌کند.
- **ادغام**: فقط اطلاعات غیرتکراری تکمیل می‌شود (ایمیل، برندها، دسته، تلفن جدید با نرمال‌سازی +98، نام رابط) + مهر mergedAt/mergedBy.
- به ثبت‌کننده سایت پیام «تایید شد — اطلاعات شما با پروفایل موجودتان ادغام گردید» می‌رود + اعلان ارشد + ثبت ممیزی.

## ✅ کیفیت

| مورد | نتیجه |
|---|---|
| ممیز ۸بخشی (۴۳۴ صفحه) | ✅ PASS |
| `node --check` همه ماژول‌های جدید/تغییرکرده | ✅ PASS |
| تستر جدید `tester21-sprint84` | ✅ **39/39 PASS** |
| رگرسیون ۲۰ تستر قبلی | ✅ همه PASS (چک قدیمی «سقف ۴ ستون» tester7 مطابق US-183 به‌روزرسانی شد) |
| نسخه کش | `ptf-crm-v84.0` |

## 📦 نصب (File Manager)
**`pishtaj-release-v84.0-delta.zip`**: `crm/offers-pro.js` و `crm/buycompare.js` (جدید)، `crm/offers.js`، `crm/letters.js`، `crm/ui-kit.js`، `crm/sync.js`، `crm/backup.js`، `crm/index.html`، `crm/sw.js`، `api/crm.php` + مستندات. **داده‌ای پاک نمی‌شود.**

## 🔔 یادآوری‌ها
- 🧪 تست پیشنهادی: ① CO یورویی بسازید و PDF قالب «سربرگ رسمی» بگیرید ② سرستون Brand را درگ کنید ③ یک نامه چاپ کنید (خطوط مورب سفید) ④ در «قیمت‌های خرید» یک درخواست انتخاب و دو تامین‌کننده قیمت دهید — کمترین ✅ می‌شود ⑤ تامین‌کننده تکراری از سایت تایید کنید (دیالوگ ادغام).
- ⏭ اسپرینت ۸۵: US-172 (ایمپورت هوشمند اکسل سراسری) + سئو موج۲. یادآوری: ZipArchive هاست، Search Console، مدارک US-160.
