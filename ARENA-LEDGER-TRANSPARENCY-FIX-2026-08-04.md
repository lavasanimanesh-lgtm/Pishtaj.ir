# فاز ۷ (v34.0.10-alpha) — شفافیت تراز رسمی/غیررسمی + reconciliation گزارش تجمیعی

**برنچ:** `arena/019fcd1d-pishtaj-ir`
**مبنای تصمیم:** تأیید کارفرما برای اصلاح شفافیت مغایرت‌های گزارش‌های مالی (مستند در ARENA-LEDGER-DIAGNOSTIC).

---

## اصلاح ۱ — باکت «نامشخص» در تراز رسمی/غیررسمی صریح شد

**مسئله:** اختلاف «رسمی+غیررسمی» با «کل» ناشی از باکت «نامشخص» (رکوردهای بدون طبقه‌بندی رسمی/غیررسمی) بود، ولی در UI فقط یک پاورقی کوچک داشت.

**اصلاح (`crm/ledger-report.js`):**
- کارت‌های «نامشخص» جدا برای خرید و هزینه‌جاری در بخش تجمیعی اضافه شد (با تعداد و مبلغ).
- بنر هشدار با توضیح «چون رسمی/غیررسمی‌شان مشخص نیست، در سود لحاظ نشده‌اند» + دکمهٔ «🔍 باز در کیفیت داده».
- تابع `ptfLedgerGoQuality()` که به تب «کیفیت داده» (توسط `finHubSet('quality')`) میرود و اسکرول میکند.

## اصلاح ۲ — بازسازی بدهی تأمین‌کننده در گزارش تجمیعی

**مسئله:** «بدهی باز ۶۳۴م» در برابر «فاکتور سال ۱۹۶م» و «پرداخت سال ۳۱م» گیجکننده بود؛ کاربر نمیفهمید چرا با هم برابر نیستند.

**اصلاح (`crm/working-capital.js`):**
- تفکیک در `recSup`: افتتاحیه + فاکتورهای امسال + فاکتورهای سالهای قبل + اصلاحیات − پرداختهای امسال − پرداختهای سال قبل.
- بلوک «🧮 بازسازی بدهی باز تأمین‌کنندگان» در UI گزارش تجمیعی، که این معادله را به‌صراحت نشان میدهد و توضیح میدهد «بدهی باز یک ماندهٔ تجمعی است و با فاکتورهای همین سال قابل مقایسه مستقیم نیست».

**تأیید عددی:** ۴۵۰ (افتتاحیه) + ۱۹۶ (امسال) + ۴۰۰ (سال قبل) − ۳۱ (پرداخت امسال) = **۱۰۱۵م = بدهی گزارش** ✅

---

## تست‌ها
- **tester306** جدید (۱۴/۰): باکت نامشخص، دکمهٔ کیفیت داده، recSup و بازسازی بدهی، cover/legacy.
- رگرسیون کامل سبز (۱۵ تستر، ۰ شکست).
- بامپ نسخه → **v34.0.10-alpha** (VERSION.json + VER + باستر + کش SW + clear-cache).
- تغییرات `.github/workflows/*` در PENDING-workflow-changes ثبت شد (بدون اجازهٔ workflows).

*تولیدشده توسط Arena Agent — اصلاح شفافیت و reconciliation گزارش‌های مالی.*
