# 🚀 یادداشت‌های انتشار اسپرینت ۱۰۷ (Release Notes - v107.0)

**تاریخ انتشار:** ۱۴۰۵/۴/۱۵ (July 2026)  
**هدف اسپرینت:** بهینه‌سازی حداکثری نسخه موبایل، هرس هوشمند بک‌آپ‌های ابری و رفع نمایش حذفیات (اولویت ۶ بک‌لاگ محصول)  
**تیم توسعه:** تیم ارشد تجربه کاربری و زیرساخت PTF  

---

## 📋 تغییرات اعمال‌شده در این نسخه

### ۱. 📱 بهینه‌سازی حداکثری نسخه موبایل و سهولت کار (US-112 - Mobile PWA Excellence)
* **فایل تغییر یافته:** `crm/index.html` (استایل‌های CSS)
* **شرح تغییر:** برای سهولت فوق‌العاده کار با سیستم روی تلفن همراه (تیم چابکی که در جلسات یا کارگاه نیاز به کار با موبایل دارد)، استایل‌های اختصاصی موبایل (`@media max-width: 768px`) به هسته سیستم اضافه شد:
  * حداقل ارتفاع تمام دکمه‌ها، فیلدهای ورودی و منوهای کشویی به **۴۴ پیکسل (Touch-friendly Targets)** افزایش یافت تا لمس آن‌ها با انگشت بسیار راحت و بدون خطا باشد.
  * تمام جداول سیستم (استعلام‌ها، پیشنهادها، مشتریان) به قابلیت اسکرول نرم افقی لمسی (`-webkit-overflow-scrolling: touch`) مجهز شدند تا صفحه در موبایل به هم نریزد.
  * عرض پنجره‌های مودال روی **۹۶٪ عرض موبایل** با حداکثر ارتفاع ۹۰٪ تنظیم شد تا در صفحه‌نمایش‌های کوچک، هیچ فیلدی از کادر بیرون نزند.
  * چیدمان فرم‌ها در موبایل از افقی به ستونی (Vertical Flex) تغییر کرد تا تایپ و ویرایش اطلاعات با یک دست امکان‌پذیر باشد.

### ۲. ☁️ هرس‌کننده هوشمند بک‌آپ‌های ابری (Cloud Backup Pruner — حل مشکل ۱۴۲ فایل ابری)
* **فایل تغییر یافته:** `crm/storage.js`
* **شرح تغییر:** در پاسخ به سؤال شما در خصوص ۱۴۲ فایل ابری که دائم در حال افزایش بود: این فایل‌ها بک‌آپ‌های ساعتی و روزانه تاریخی سیستم بودند که در باکت آروان‌کلود روی هم انباشته شده بودند.
* **سازوکار جدید:** تابع پاک‌ساز زباله‌های ابری (`ptfPurgeCloudOrphans`) ارتقا یافت. اکنون سیستم علاوه بر فایل‌های یتیم، تمام بک‌آپ‌های موجود در باکت S3 را بر اساس تاریخ مرتب کرده، **صرفاً ۳ نسخه آخر (بک‌آپ ساعتی آخر + ۲ روز اخیر)** را حفظ می‌کند و تمام بک‌آپ‌های قدیمی‌تر (که بالغ بر ده‌ها فایل اضافی هستند) را به عنوان زباله ابری شناسایی و به طور دائم از آروان‌کلود حذف می‌کند! با کلیک روی دکمه «☁️ پاک‌سازی زباله‌های ابری»، تعداد فایل‌های کلود شما فوراً به حداقل (۳-۵ فایل ضروری) کاهش می‌یابد.

### ۳. 📊 رفع باگ عدم نمایش جدول بایگانی حذفیات در گزارشات
* **فایل تغییر یافته:** `crm/reports.js`
* **شرح تغییر:** اتصال تابع `renderDeletedArchive` به توابع سازنده و رندرکننده ماژول گزارشات (`window.buildReports` و `window.renderReports`) تصحیح شد. اکنون با باز کردن پنل گزارشات، جدول **«🗑 بایگانی حذفیات و بررسی‌های آماری دلایل»** با استایل واضح در قسمت فوقانی بخش رصد تحرکات (Audit Trail) به درستی و بدون پرش نمایش داده می‌شود.

### ۴. 📘 به‌روزرسانی سند جامع انتقال معماری (Master Handover v107.0)
* فایل **`PTF-MASTER-HANDOVER.md`** در ریشه فایل زیپ با آخرین تغییرات نسخه `v107.0`، وضعیت تحقق یوزر استوری‌های ۱۲گانه و حفظ باگ‌های حل‌نشده در بک‌لاگ فنی به‌روزرسانی شد.

---

## 🧪 وضعیت کنترل کیفیت (QA Verification)
* تست ریسپانسیو روی رزولوشن‌های مختلف موبایل (360px تا 428px) بدون به هم ریختگی کادرها پاس شد.
* اسکنر هرس کلود تست شد؛ سیستم با موفقیت بک‌آپ‌های قدیمی را در لیست حذف قرار می‌دهد و ۳ بک‌آپ جدید را حفظ می‌کند.
