# RELEASE NOTES — v34.7.66 (۱۴۰۵/۰۵/۳۰ — 2026-08-21)

## تب جداگانهٔ «درخواست‌های سایت» + نمایش فایل پیوست ثبت‌نام تامین‌کننده

### مشکل
ثبت‌نام تامین‌کنندگان از سایت (فرم `/supplier/`) با فایل کاتالوگ به فضای ابری ذخیره
می‌شد، اما در CRM **هیچ راهی برای دیدن آن فایل وجود نداشت** و هنگام «تایید» هم فایل و
متن درخواست دور ریخته می‌شد.

### ۱) تب جداگانهٔ «🌐 درخواست‌های سایت»
- در پنل تامین‌کنندگان، کنار تب‌های «همه / داخلی / خارجی»، تب جدید اضافه شد.
- شمارندهٔ موارد «در انتظار بررسی» روی خود تب نمایش داده می‌شود.
- این تب فهرست کامل ثبت‌نام‌های سایت (در انتظار / تاییدشده / ردشده) را نشان می‌دهد و
  فهرست تاییدشده را موقتاً پنهان می‌کند.

### ۲) خوانا شدن فایل‌های پیوست
- ستون «ضمیمه» (📎 با نام فایل) + دکمهٔ «👁 جزئیات» به هر ردیف اضافه شد.
- مودال «جزئیات ثبت‌نام تامین‌کننده» (`supSiteDetail`) همهٔ فیلدها (شرکت، مسئول، تماس،
  ایمیل، حوزه، نوع، برندها، شرح درخواست) + بلوک «☁️ پیوست در فضای ابری» با دکمهٔ
  «مشاهده / دانلود» را نمایش می‌دهد.

### ۳) جلوگیری از گم‌شدن فایل پس از تایید
- `supApprove` حالا پیوست ابری (به‌صورت `files.oth`) و متن درخواست (`message`) را روی
  رکورد تاییدشده در `ptf_crm_suppliers` حفظ می‌کند.

### فنی
- `crm/bridge.js` — بازنویسی `renderSupPending`، افزودن `supSiteDetail`، حفظ پیوست در
  `supApprove`.
- `crm/cheques.js` — افزودن تب و toggle نمایش در `ptfSupTab`.

### تست
- تستر جدید `tester468-v34.7.66-sup-site-attachment.js` (در گیت CI).
- **گیت CI: ۸۴ PASS / ۰ FAIL** · نگهبان معماری: PASS.

### UAT پیشنهادی
1. یک ثبت‌نام تامین‌کنندهٔ آزمایشی با فایل از سایت بزنید.
2. در CRM → تامین‌کنندگان → تب «🌐 درخواست‌های سایت» → ردیف را ببینید.
3. «👁 جزئیات» → «مشاهده / دانلود از فضای ابری» → فایل باید باز شود.
4. «✅ تایید» بزنید و بررسی کنید فایل/متن روی رکورد تاییدشده هم مانده است.
