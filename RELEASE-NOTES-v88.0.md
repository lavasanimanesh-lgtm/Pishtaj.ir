# 🚨 ریلیز v88.0 — رسیدگی فوری + قفل کالای پیشنهاد + فرم عریض

**تاریخ:** ۱۶ تیر ۱۴۰۵ | **دامنه:** US-201 + US-202 + US-203 (گزارش فوری شما)

---

## 🐞 US-203 — سه رفع فوری

### ۱. باگ «خلاصه AI زرد می‌ماند» — ریشه پیدا شد ✅
با اینکه تنظیمات سبز بود، **پارامتر خروجی JSON در فراخوانی Gemini با نام قدیمی** (`response_mime_type`) ارسال می‌شد که Gemini آن را نمی‌پذیرد و درخواستِ خلاصه خطا می‌داد (تست اتصال ساده بود و رد می‌شد — برای همین سبز بودید ولی خلاصه شکست می‌خورد). اصلاح شد به `responseMimeType` + پارس مقاوم پاسخ (اگر متن اضافه دور JSON بود) + کش وضعیت کوتاه شد تا فعال شدن AI سریع در دکمه‌ها اثر کند. حالا خلاصه هر سه درخواست شما (با کلیک روی دکمه زرد) باید سبز شود.

### ۲. مشاهده پیوست‌های درخواست ✅
دکمه جدید **«📎 N»** در فهرست استعلامات (فقط درخواست‌های دارای فایل) → مودال همه پیوست‌ها به تفکیک گروه (فایل استعلام/دیتاشیت/عکس/نقشه/سایر) با دکمه **«مشاهده / دانلود»** (لینک امن موقت آروان). اگر فایلی هنوز در صف آپلود ابری باشد، پیام شفاف می‌دهد.

### ۳. آیکون‌های مینیمال دکمه‌های داخل پنل ✅
ایموجی ابتدای دکمه‌های عملیاتی (🤖 خلاصه، 📖 خواندن استعلام، 📎 پیوست‌ها، 📨 ارجاع، 🗑 حذف، 🧰 فیلتر...) هم با آیکون خطی SVG هم‌خانواده جایگزین می‌شود — خودکار روی هر دکمه‌ای که رندر شود.

## 🔒 US-201 — کالای پیشنهاد فقط از ماژول کالا (ماژول جدید `offerlock.js`)

دقیقاً جریان درخواستی شما:
- ستون جدید **«کد کالا 🔒»** ابتدای هر ردیف: کد کامل را تایپ کنید و **Enter** → شرح و واحد فوراً در ردیف می‌نشیند. بخشی از کد یا شرح را تایپ کنید → **کشویی همه کالاهای منطبق** → انتخاب با کلیک.
- بعد از انتخاب، **شرح و واحد قفل (readonly)** می‌شوند — کاربر نمی‌تواند تغییرشان دهد؛ فقط با 🔁 می‌تواند کالای دیگری انتخاب کند.
- **کد کالا در فرم دیده می‌شود ولی در خروجی PDF درج نمی‌شود** (طبق خواسته شما).
- کالای جدید؟ **«+ ثبت سریع کالا»** همان‌جا: شرح+واحد → ثبت در ماژول کالا با **کد یکتای خودکار سیستم** (با ضد تکرار US-174) → درج در ردیف.

## 📐 US-202 — فرم پیشنهاد عریض + اعتبارسنجی

- پنجره ثبت پیشنهاد **~۲ برابر عریض‌تر** شد (تا 1700px / 96vw).
- عرض پیش‌فرض ستون‌ها بازچینی شد: **شرح بلند (300px)، تعداد/واحد کوچک (52px)** + **تغییر عرض هر ستون با درگ لبه سرستون** (ذخیره می‌شود و دفعه بعد همان می‌ماند).
- **اعتبارسنجی ثبت:** هیچ ستونی در هیچ ردیفی نباید خالی باشد — پیام دقیق: «ردیف ۳: ستون Brand خالی است — مقدار بدهید یا خط تیره (-) بگذارید». تعداد هم باید > صفر باشد.

## ✅ کیفیت
tester29 جدید: **24/24 PASS** (شامل تست رفتاری اعتبارسنجی: برند خالی خطا، با «-» مجاز، تعداد صفر خطا) | رگرسیون ۲۱ تستر همه PASS | ممیز ۸بخشی PASS | کش → `ptf-crm-v88.0`

## 📦 نصب (File Manager)
**`pishtaj-release-v88.0-delta.zip`**: `crm/offerlock.js` (جدید)، `crm/inqreader.js`، `crm/icons.js`، `crm/offers.js`، `crm/index.html`، `crm/sw.js`، **`api/llm.php`** (رفع باگ خلاصه — حتماً جایگزین شود) + مستندات.

> 🧪 تست فوری: ① روی دکمه 🟡 زرد هر سه درخواست کلیک کنید → باید سبز شوند ② 📎 کنار درخواستِ دو عکسی → مشاهده/دانلود ③ TO جدید → «+ ردیف دستی» → تایپ بخشی از شرح کالا → کشویی → انتخاب → شرح/واحد قفل ④ یک ستون را خالی بگذارید و ثبت بزنید → خطای ردیف/ستون.
