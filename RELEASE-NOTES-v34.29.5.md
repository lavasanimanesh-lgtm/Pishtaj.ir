# RELEASE NOTES — v34.29.5 (برخورد شناسهٔ pgTitle + شمارنده‌های زندهٔ عنوان/نامک/محصول)

**تاریخ:** ۲۰۲۶-۰۹-۰۲ · **کمیت:** tester580 (۹ سنجه) · **گیت:** 196 PASS / 0 FAIL

## گزارش کاربر

«فیلد عنوان کاراکترها را نمی‌شمرد، همینطور فیلد اسلاگ فکر می‌کنم؛ و آیا دویست حرف برای این‌ها زیاد نیست؟»

## ① ریشه — برخورد شناسه (ID collision)، بدتر از یک شمارندهٔ خراب

`pgTitle` از سال‌ها پیش **شناسهٔ رسمی «عنوان پنل» در هدر CRM** است و ده‌ها ماژول (`analyzer.js`, `careers.js`, `contracts.js`, `gsc.js`, `letters.js`, `perms.js`, `petty.js`, `projects.js`, `rbac.js`, …) با `getElementById('pgTitle').textContent = …` عنوان پنل را می‌نویسند. فرم «صفحهٔ جدید» هم input خودش را همین نام کرده بود. نتیجه: `getElementById` همیشه **span هدر** (بالاتر در سند، بدون `value`) را برمی‌گرداند، پس:

- شمارندهٔ عنوان همیشه «0/۶۰» می‌ماند (مقدار span هدر را می‌خواند) — همان گزارش کاربر؛
- «تولید با AI» و «اعمال خروجی خارجی» **عنوان را در فرم پر نمی‌کردند** (روی span هدر می‌نوشتند)؛
- خواندن عنوان در انتشار هم به خالی می‌افتید.

**رفع:** فیلد فرم به شناسهٔ یکتای `cmsPgTitle` تغییر نام یافت (در کل: رندر فرم کامل و ساده، PAGE_FIELDS، cmsExtFill، خواندن انتشار/پیش‌نمایش/زمان‌بندی). خطِ هدر (`textContent`) دست‌نخورده ماند. اثبات با بوت کامل و تایپ واقعی: شمارنده «48/۶۰» زنده است و پس از اعمال خارجی، عنوان در فرم ظاهر و شمارنده به‌روز می‌شود.

## ② نامک (slug) — شمارنده داشتیم؟ نه — الان دارد

هیچ بازخوردی نداشت. اکنون `cmsSlugFb` در هر سه فرم (صفحهٔ کامل، فرم جایگزین، محصول) زنده است: طول + اعتبار («فقط a-z، عدد و خط تیره») + **پیشنهادِ نسخهٔ تمیز**. مثال واقعی: `Valve Repair!` → «⛔ 2 نویسهٔ نامعتبر · پیشنهاد: valve-repair». با پر کردن برنامه‌ای (اعمال خارجی/پیش‌نویس) هم به‌روز می‌شود.

## ③ فرم محصول — شمارنده‌های مرده زنده شدند

`prTitleLen`/`prDescLen` فقط span بودند و هیچ تابعی به‌روزشان نمی‌کرد. اکنون `cmsPrCount` + `oninput` روی عنوان/توضیح/متن + span جدید `prBodyLen` (کلمه/حرف + کف ۲۰۰) + بازخورد نامک؛ پس از بازیابی پیش‌نویس و اعمال خارجی هم صدا زده می‌شود. اثبات: عنوان «52/۶۰»، توضیح «78/۱۶۰»، متن «19 کلمه / 109 حرف — کفِ متن ۲۰۰ حرف».

## ④ پاسخ به «۲۰۰ حرف زیاد نیست؟»

حداقل ۲۰۰ حرف **فقط برای فیلد «متن صفحه»** است — کف است نه سقف، و برای سئو واقعاً کم است (پرامپت ما ۱۲۰۰ کلمه می‌خواهد؛ گوگل صفحات نازک را کم‌ارزش می‌داند). عنوان و نامک **هیچ حداقل ۲۰۰ی ندارند** — هدف عنوان ۳۰–۶۵ و توضیح ۷۰–۱۶۵ کاراکتر است (همان‌که شمارنده‌ها نشان می‌دهند). برچسب‌ها شفاف شدند: «کف ۲۰۰ حرف، فقط همین فیلد».

## ⑤ پیوند با زنجیرهٔ قبلی

این چهارمین لایهٔ پروندهٔ «تب صفحهٔ جدید» بود: ① v34.28.0 کلیک تب رندر را اجرا نمی‌کرد؛ ② v34.29.3 کلاس‌های pn/tb با CSS سراسری برخورد می‌کردند و محتوا با display:none نامرئی می‌شد؛ ③ v34.29.4 پارسر خروجی هوش خارجی نشانگرها را نمی‌شناخت؛ ④ v34.29.5 شناسهٔ فیلد عنوان با هدر برخورد می‌کرد (شمارنده/AI/اعمال/انتشار). حالا کل مسیر «تایپ ← شمارنده ← پر کردن ← انتشار» سبز است.

## ⑥ بعد از دیپلوی

فایل‌های v34.29.5 را آپلود کنید (به‌ویژه `crm/cms.js`، `crm/index.html`، `crm/sw.js`). تب را ببندید/باز کنید. شمارندهٔ عنوان از اولین حرف می‌شمارد، کنار نامک بازخورد زنده می‌آید و در فرم محصول هم همهٔ شمارنده‌ها کار می‌کنند.
