# RELEASE NOTES — v34.24.0 (اصلاح هوشمند گروهی سئو — یک کلیک 🤖)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** tester569 (۲۵ سنجه) · **گیت:** 185 PASS / 0 FAIL · **PHP-lint:** ✓

## درخواست مالک

«در قسمت سئو صفحات ایرادهایی از هر صفحه زیرش نمایش داده می‌شود؛ باید دکمه‌ای باشد که هوش مصنوعی کامل آنالیز و طبق الگو و در چهارچوب قوانین سئو و سرچ کنسول گوگل، با یک کلیک اصلاح کند.»

پیش از این رفع ایراد با AI فقط **داخل مودال تک‌صفحه** و پله‌پله‌ای بود (باز کردن صفحه ← دکمه ← جای‌گذاری ← تیک بازبینی ← ذخیره). حالا **از خود فهرست سئو، یک دکمه، همهٔ صفحاتِ دارای ایراد.**

## جریان جدید — دکمهٔ «🤖 اصلاح هوشمند گروهی» در نوار ابزار سئو

۱. **یک کلیک «▶️ آنالیز و ساخت پیشنهادها»** — برای همهٔ صفحاتِ نمای فعلی که ایراد قابل اصلاح خودکار دارند (۹ نوع: عنوان/توضیح/H1 — وجود، بلندی، کوتاهی)، پشت‌سرهم: متن واقعی صفحه خوانده می‌شود → `seo_fix` در چهارچوب قوانین → پیشنهاد عنوان/توضیح/H1.
   - سقف هر اجرا: ۳۰ صفحه (با هشدار ادامه برای بقیه) · فاصلهٔ بین فراخوانی‌ها (محدودیت نرخ) · دکمهٔ «✖ توقف» · نوار وضعیت صفحه‌به‌صفحه.
۲. **اعتبارسنجی خودکار:** پیشنهاد خارج از چهارچوب (عنوان ۳۰–۶۵ / توضیح ۷۰–۱۶۵ / H1 خالی) رد می‌شود و برای ویرایش دستی علامت می‌خورد — هیچ مقدار نامعتبری روی سایت نمی‌نشیند.
۳. **بازبینی تجمیعی یک‌صفحه‌ای:** هر صفحه با چک‌باکس + جدول «اکنون (n) ↔ پیشنهاد (n)» با رنگ سبز/قرمز طول.
۴. **یک کلیک «✅ تأیید و اعمال انتخاب‌شده‌ها»** — همان دروازهٔ بازبینی انسانیِ اجباری سیستم، اما به‌جای تک‌تک، یک‌جا؛ نسخهٔ پشتیبان هر فایل در crm/data/cms-backups. **canonical و robots هر صفحه دست نمی‌خورد** (ابزار گروهی خودشان را دارند). نتیجهٔ هر صفحهً جدا گزارش و فهرست دوباره اسکن می‌شود + یادآوری Request Indexing در سرچ کنسول.

## سرور — چهارچوب Google Search Central در $SEO_RULES

به قواعد_llm سئو اضافه شد: یکتایی عنوان/توضیح در هر صفحه (بدون قالب تکراری)، تطابق صادقانه با محتوا، بدون کلیک‌بیت/ادعای اغراق‌آمیز، و «خروجی اصلاحی کامل و آمادهٔ ذخیره — نه دستورالعمل».

## فایل‌های کلیدی

`crm/cms.js` (cmsSeoAiBatch/Run/Apply + cmsSeoFixablePages) · `api/llm.php` (GOOGLE SEARCH CENTRAL FRAMEWORK) · `_tools/uat/tester569-v34.24.0-seo-batch-ai.js`
