# اجرای اسپرینت ۲ سئو — ۲۰۲۶-۰۹-۲۵ (خوشهٔ NACE + رفع هم‌خواری اینکوترمز)

ادامهٔ `SEO-GSC-SPRINT1-EXECUTION-2026-09-25.md`. هدف اسپرینت: شروع رفع مشکل authority خوشهٔ لوله (کوئری‌های باند ۳۱–۱۰۰) از مسیر NACE + رفع هم‌خواری سه صفحهٔ اینکوترمز.

## 🔄 چرخش مهم برنامه (کشف حین اجرا)

طبق برنامه، قرار بود «مقالهٔ لوله NACE» تولید شود. بررسی showed **دو مقالهٔ کامل NACE از قبل وجود دارد**:

- `nace-mr0175-sour-service-guide.html` — راهنمای استاندارد (۹ H2، self-canonical، در sitemap)
- `kc-api-5l-psl2-sour-service-nace-requirements.html` — الزامات سرویس ترش PSL2 (۲۱ H2)

مشکل این خوشه «نبودِ محتوا» نبود؛ **نبودِ لینک داخلی و سیگنال authority** بود:
- مقالهٔ اصلی NACE **صفر لینک درون‌متنی به صفحات محصول** داشت (فقط منوی سراسری).
- هیچ‌کدام از صفحات محصول لوله (a333/a335/api-5l) به مقالات NACE لینک نمی‌دادند.
- H1 مقالهٔ PSL2 ۷۲ کاراکتر بود (فراتر از حد ۷۰ قواعد سایت).

➡️ بنابراین به‌جای تولید محتوای تکراری (که هم‌خواری جدید می‌ساخت)، **بهینه‌سازی و لینک‌سازی خوشهٔ موجود** انجام شد.

## ✅ تغییرات اجراشده

| # | فایل | تغییر |
|---|---|---|
| 1 | `kc/nace-mr0175-sour-service-guide.html` | افزودن ۳ لینک صفحهٔ محصول به بخش مطالب مرتبط: A333 (دمای پایین)، A335 (آلیاژی)، API 5L — لینک‌سازی عمودی خوشهٔ لوله |
| 2 | `kc/kc-api-5l-psl2-sour-service-nace-requirements.html` | H1 72→53 («الزامات سرویس ترش (NACE MR0175) برای لولهٔ API 5L PSL2») + کارت لینک به صفحهٔ محصول لوله API 5L |
| 3 | `services/products/a333-low-temperature-pipe.html` | title 43→51 مجزا از H1 («لوله دمای پایین ASTM A333 Gr.6؛ خرید و استعلام قیمت») + **جدول گریدهای A333** (Gr.1/3/6/7/8/9 با ترکیب و دمای طراحی) + لینک NACE در بولت استانداردها و بخش مطالب مرتبط |
| 4 | `services/products/alloy-steel-pipe-a335.html` | پاراگراف سرویس ترش بعد از جدول گریدها: الزام NACE MR0175/ISO 15156 + سقف سختی 22 HRC + لینک به مقالهٔ NACE + لینک در مطالب مرتبط |
| 5 | `kc/index.html` (هاب) | افزودن «NACE MR0175 برای لوله و فلنج» به دستهٔ «لوله و اتصالات پایپینگ» (قبلاً فقط در دستهٔ کیفیت بود) |
| 6 | `kc/incoterms-industrial-trade-guide.html` | **رفع هم‌خواری با صفحهٔ canonical**: title → «انتخاب ترم Incoterms برای واردات صنعتی؛ از EXW تا DDP» + H1 → «انتخاب ترم اینکوترمز برای واردات صنعتی» + desc با تمرکز واردات (قبلاً title==H1 و تقریباً هم‌عنوان با kc-incoterms-2024 بود) |
| 7 | `assets/data/search-index.json` | به‌روزرسانی عنوان ۳ صفحه در جستجوی داخلی سایت |
| 8 | sitemap-knowledge-center / sitemap-services | `lastmod` → 2026-09-25 برای ۵ URL |

**اعتبارسنجی:** هر ۵ صفحهٔ محتوایی از قواعد سئو سایت عبور کردند (title 30–65، desc 70–165، H1 20–70، title≠H1، تگ‌های متوازن) → ۰ خطا.

## 🎯 اهداف KPI اسپرینت ۲ (۴–۶ هفته)

| کوئری/صفحه | مبنا | هدف |
|---|---|---|
| کوئری‌های NACE («لوله nace»، «nace mr0175») | باند ۳۱–۱۰۰ | ورود به باند ۱۱–۳۰ |
| a333 product | ایمپرشن محدود | +۳۰٪ ایمپرشن (جدول گریدها + title جدید) |
| «اینکوترمز 2024» | هدف اسپرینت ۱: ≤5 | تثبیت ≤5 با حذف رقیب هم‌عنوان؛ صفحهٔ واردات باید ایمپرشن کوئری‌های «incoterms واردات/EXW/FOB» بگیرد |

## 📋 بک‌لاگ اسپرینت ۳ (به‌روز)

1. گپ‌های محتوایی: جدول استنلس استیل، تست هیدرواستاتیک پلی‌اتیلن، خوشهٔ واردات/ترخیص، تامین‌کننده تجهیزات برق صنعتی.
2. بازبینی query↔page digest بعد از ۴ هفته — تاثیر لینک‌سازی NACE و اینکوترمز را ببینیم.
3. **Authority غیربرند** طبق `ARENA-SEO-P4` — تنها راه خروج ۷۴٪ کوئری‌های غیربرند از باند ۳۱–۱۰۰.
4. محتوای EN برای صفحات با ایمپرشن انگلیسی.
5. ارزیابی مقالهٔ مستقل «هیدرواستاتیک vs دیسپلیسر» بر اساس واکنش H2 درج‌شده در اسپرینت ۱.
