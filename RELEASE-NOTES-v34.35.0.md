# RELEASE NOTES — v34.35.0 UX-R4 (راهنمای سئو پیش‌فرض بسته + هدایت واقعی دکمهٔ بهینه‌سازی)

**تاریخ:** ۲۰۲۶-۰۹-۰۳ · **گیت:** در حال اجرا · **نکتهٔ بامپ:** جاروی استاندارد نسخه + فرم ترکیبی رجکس + نقاط رسمی

## گزارش کارفرما

۱) «در سئو، صفحات راهنما باید به‌صورت پیش‌فرض بسته باشد اما باز است و بسته هم نمی‌شود.»
۲) «در قسمت سرچ کنسول دکمهٔ بهینه‌سازی برای صفحات وجود دارد اما نمی‌دانم چطور می‌شود بهینه کرد؛ با کلیک روی آن به صفحهٔ مربوط هدایت نمی‌شوم و فقط به مدیریت سایت منتقل می‌شوم.»

## راه‌حل

### ۱) راهنمای سئو (`crm/cms.js`)
- **پیش‌فرض بسته:** راهنما فقط وقتی باز می‌شود که کاربر صریحاً بازش کرده باشد (`'1'` ذخیره شده باشد) — تا قبل از این هر نشست با راهنمای باز شروع می‌شد.
- **بسته‌شدن ماندگار و قابل‌اتکا:** حالت در `localStorage` ذخیره می‌شود (با بازگشت به `sessionStorage`)؛ اگر storage مرورگر مسدود باشد (برخی مرورگرها/حالت خصوصی — علت «بسته نمی‌شود» قبلی که خطا بی‌صدا قورت داده می‌شد) متغیر حافظه‌ای جایگزین می‌شود تا دکمهٔ «پنهان کردن راهنما» در هر شرایطی کار کند.

### ۲) دکمهٔ «بهینه‌سازی» سرچ کنسول (`crm/gsc.js`)
- **ریشهٔ مشکل:** ناوبری با دو `setTimeout` ثابت (۱۵۰ و ۳۰۰ میلی‌ثانیه) انجام می‌شد؛ رندر تب سئو ناهمگام است (دو فراخوانی سرور پشت سر هم) و در این بازه آماده نمی‌شد، پس فیلتر هرگز اعمال نمی‌شد و کاربر در همان «مدیریت سایت» رها می‌شد.
- **راه‌حل:** ناوبری با **پولینگ تا ۱۰ ثانیه** — سوییچ به تب سئو فقط پس از آماده‌بودن پنل، و اعمال فیلتر بلافاصله پس از ظاهرشدن جعبهٔ جستجو؛ با پیام وضعیت (توست) در شروع و در صورت خطا.
- **«چطور بهینه کنم؟»:** بنر گام‌به‌گام بالای فهرست نتایج تب سئو — اصلاح عنوان/توضیح با ✏️، به‌کاربردن طبیعی عبارت در متن، لینک‌سازی داخلی با 💡، و درخواست ایندکس با 🚀 ایندکس‌یاب — با دکمهٔ «✕ متوجه شدم».

## آزمون
تستر جدید `tester587-v34.35.0-seo-guide-gsc-optimize.js` (۹ ادعا) به گیت اضافه شد.
