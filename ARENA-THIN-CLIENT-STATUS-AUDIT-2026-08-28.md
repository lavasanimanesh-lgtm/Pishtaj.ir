# گزارش ارزیابی و ممیزی — فاز «نازک‌سازی سمت کلاینت» (کلاینت نازک) CRM

**تاریخ:** ۲۰۲۶-۰۸-۲۸ | **برنچ/اسنپ‌شات:** `arena/01a0479d-pishtaj-ir` = `origin/main` | **نسخهٔ نهایی فعلی:** `v34.8.34`
**نوع:** ارزیابی وضعیت + ممیزی کد/مستندات (بدون تغییر کد)

---

## ۱) خلاصهٔ مدیریتی

فاز «نازک‌سازی سمت کلاینت» CRM در نسخه‌های **v34.8.12 → v34.8.34** (همه مورخ ۲۰۲۶-۰۸-۲۷) اجرا شده و **وضعیت «نیمه‌کاره ولی پویا»** دارد:

- **موتور فرمان اتمیک سروری و مهاجرت همهٔ موجودیت‌ها (موج‌های W1–W4) انجام شده** — یعنی مسیر *نوشتن* کسب‌وکار از «پوش کل مجموعه» به «فرمان اتمیک + projection» منتقل شده و عملاً هیچ کلید کسب‌وکاری مسیر مستقیم فعال ندارد.
- **زیرساخت‌های T0 (قاعده‌های A10/A11 + رجیستری کلیدها) و T3/T4 تا حد خوبی جلو رفته** (collection_query، صف آفلاین روی IDB، کوکی HttpOnly، TTL نشست، چک‌پرینت→S3).
- اما **چندین فاز کلیدی هنوز کامل نشده** که مهم‌ترین‌شان **اتصال گیت CI به GitHub Actions است** — دقیقاً همان بندی که هر دو رودمپ «اول از همه و جلوی پس‌رفتگی» معرفی کرده بودند و **انجام نشده است**.

**جمع‌بندی:** کل تسترهای محلی سبزند (CI gate: 150 PASS / 0 FAIL) و کد منسجم و هم‌نسخه است، ولی (الف) این گیت‌ها **در هیچ workflow گیت‌هاب اجرا نمی‌شوند**، (ب) بازنشستگی موتور سینک legacy (فاز C5/T6) نشده، (ج) کلیدهای DEV/کش هنوز مستقیم در localStorage می‌نویسند، و (د) حذف توکن از localStorage کلاینت عمداً به بعد موکول شده است. به همین دلیل اصول «نازک حداکثری» (E1–E7) **هنوز همگی اثبات‌پذیر نیستند**.

---

## ۲) آخرین وضعیت

- **شماره نسخهٔ نهایی:** `v34.8.34` — منبع رسمی `VERSION.json` (و یکدست در UI `index.html`، `sw.js`، `manifest.json`).
- **برنچ:** این اسنپ‌شات دقیقاً با `origin/main` همگام است (diff صفر)؛ یعنی همین نسخه مبنای استقرار فعلی است.
- **وضعیت تست‌ها:** اجرای محلی `_tools/uat/run-ci-gate.js` → **150 PASS / 0 FAIL**؛ `_tools/arch/arch-guard.js` → **PASS** (همهٔ قواعد در چارچوب baseline).

---

## ۳) چه کارهایی انجام شده (نگاشت به رودمپ T0–T7)

دو رودمپ مرجع: `PHASE-C-THIN-CLIENT-ROADMAP.md` (C1–C6 «عین بانک») و `ROADMAP-THIN-CLIENT-MAXIMAL-2026-08-27.md` (T0–T7 = ابرمجموعه). نگاشت کارهای انجام‌شده:

| فاز | کار | نسخه | وضعیت |
|---|---|---|---|
| C1 / T3-1 | تله‌متری push سمت سرور (`sync_stats`) + سند مجموعه‌های داغ | v34.8.12 | ✅ |
| C2 | زیرساخت فرمان (`entity_upsert/delete` + journal/idempotency/tombstone + sanitizer) | v34.8.13 | ✅ |
| C3 | مهاجرت یادآورها + سرنخ‌ها | v34.8.14–15 | ✅ |
| — | رفع باگ کارتابل/اعلان (تکرار کارت، دینگ، merge سرور) | v34.8.17–18 | ✅ |
| — | بنر زرد تشخیصی + رفع پوشیده‌شدن در استیجینگ | v34.8.19–20 | ✅ |
| — | اتحاد notifs (پایان نوار زرد پایدار) | v34.8.21 | ✅ |
| T2/W1 | روتر فرمان `ptfEntitySaveCollection`؛ مشتریان/تامین‌کنندگان/کالاها | v34.8.22–23 | ✅ |
| T3-3 | صف آفلاین `ptf_b_queue` → IndexedDB | v34.8.23 | ✅ |
| T2/W2 | rfqs/deals/projects/notifs/inqitems/packinglists | v34.8.24 | ✅ |
| T2/W3 | invoices/case_receipts/receipt_allocations/corrections/fin_findings/sales_returns | v34.8.26 | ✅ |
| T2/W4 | ~۴۱ کلید پایانی (offers، settings، audit، users، cheques، …) | v34.8.27 | ✅ |
| T4-1a/4-2/4-3, T3-2 | کوکی HttpOnly + TTL نشست + چک‌پرینت→S3 + رندر سردبوت | v34.8.28 | ✅/🟡 |
| T5-2 | چک‌های شخصی → کلید سینک‌شونده مشترک + مهاجرت ایمن | v34.8.29 | ✅ |
| — | رفع حلقهٔ 403 پنل ابزارها (stale-token-heal) | v34.8.30 | ✅ |
| T3-1 | `collection_query` (خواندن سرور-محور) | v34.8.31 | ✅ |
| T3-1 مصرف | ماژول کالا از خواندن سرور-محور (+ ۲ فیکس فوری) | v34.8.32–34 | ✅ |
| T0-1/2/5 | قاعدهٔ A10 (بایپس LS) + A11 (تطابق رجیستری) + رجیستری واحد کلیدها | v34.8.34 | ✅ |
| T0-3/4 | **اتصال گیت‌ها به workflow گیت‌هاب (قبل و بعد FTP)** | — | ❌ **انجام‌نشده** |

---

## ۴) چه کارهایی هنوز باقی مانده

| بند رودمپ | کار | وضعیت/ریسک |
|---|---|---|
| **T0-3** | اجرای `run-ci-gate.js` + arch-guard در `deploy-staging.yml` و `deploy-production.yml` | 🔴 **انجام‌نشده.** هر دو workflow فقط PHP-lint + FTP دارند؛ هیچ تستر/arch-guard/CI gate اجرا نمی‌شود. |
| **T0-4** | **post-deploy hash check** (مقایسهٔ هش ۵ فایل کلیدی زنده با کامیت) | 🔴 انجام‌نشده — همین باگ «نسخهٔ مخلوط» را می‌گرفت که در سند ذکر شده. |
| **T0-6** | هدر کش استیجینگ `no-cache` (`.htaccess` شرطی) | 🔴 انجام‌نشده (موردی در `.htaccess` نیست). |
| **T4-1b** | حذف توکن `ptf_crm_token` از localStorage کلاینت (پس از چرخش دستگاه‌ها) | 🟠 عمداً defer شده؛ توکن هنوز از LS در ~ده‌ها نقطه خوانده می‌شود. |
| **T4-3** | `ptf_crm_avatars` → S3 | 🟠 انجام‌نشده (فقط چک‌پرینت bg منتقل شد). |
| **T4-4 / T7** | 2FA پیامکی + bulk revoke نشست + rate-limit فرمان‌ها | 🟠 انجام‌نشده (اختیاری/C6). |
| **T3-4** | کش‌های read-through با TTL (`ptf_site_*`, `ptf_fx_live_cache`, `ptf_cloud_usage`) | 🟠 انجام‌نشده — هنوز مستقیم در LS. |
| **T5-1** | حذف کامل شاخهٔ fallback های مردهٔ localStorage | 🟠 ناقص — شاخه‌ها باقی‌اند (غیرفعال ولی خلاف قرارداد). |
| **T5-2** | کلیدهای DEV → IDB/سینک (صف کدینگ، پیش‌نویس autodraft، sales_command) | 🟠 ناقص — فقط personal_cheques منتقل شد؛ بقیه در LS. |
| **T6 / C5** | بازنشستگی موتور سینک legacy (`pushDirty`/`pullCheck`) — «یک موتور» | 🔴 انجام‌نشده — دو موتور هنوز زنده. |
| **T7** | ابزارهای ریکاوری HTML (clear-cache/force-restore/recover/sync-diagnostics) IDB-aware | 🟠 انجام‌نشده (صفر ارجاع IndexedDB). |

---

## ۵) ممیزی — یافته‌ها

### ۵.۱ — باگ/نقص واقعی پیدا شده (درجه‌بندی شده)

**🟥 F-1 — گیت CI اصلاً در CI نیست (گمراه‌کننده + پرهزینه).**
- `run-ci-gate.js` و arch-guard و تمام testerهای نازک‌سازی **فقط به‌صورت دستی/محلی** اجرا می‌شوند؛ در `.github/workflows/deploy-*.yml` هیچ گامی آنها را صدا نمی‌زند.
- این یعنی هر PR/feature جدید می‌تواند دوباره `localStorage.setItem` کسب‌وکار اضافه کند (تخلف A10) و بدون هیچ مانعی در workflow مرج شود — **دقیقاً همان ریسکی که فاز ۰ هر دو رودمپ وعده داده بود «اول از همه ببندیم» بسته نشده است.**
- **یافتهٔ فرعی:** تستر `tester431` با اعلام «G2 نگهبان به گیت CI وصل است» فقط بررسی می‌کند `run-ci-gate.js` به `arch-guard.js` ارجاع دارد، نه اینکه workflow واقعاً آن را اجرا کند → **پاسِ گمراه‌کننده**؛ اتصال واقعی به GitHub Actions برقرار نیست.

**🟥 F-2 — اصل E7 («یک موتور همگام‌سازی») برقرار نیست.**
- `sync.js` همچنان شامل `pushDirty`/`pullCheck` و موتور legacy است؛ مسیر فرمان و مسیر legacy به‌طور موازی زنده‌اند. بازنشستگی legacy (فاز C5/T6) انجام نشده. ریسک: دوبلت/تعارض بین دو مسیر و نگهداری ~۲۰۰۰ خط منطق تعارض.

**🟠 F-3 — کلیدهای کسب‌وکار با fallback مرده به localStorage.** دو نقطهٔ نوشتن مستقیم کسب‌وکار:
- `codegen.js:394` → `ptf_crm_settings`
- `treasury.js:474` → `ptf_crm_shareholders`
هر دو شاخهٔ `else localStorage.setItem(...)` پس از `ptfEntitySaveCollection`/`setData` هستند. در عمل (زمانی که روتر و setData موجودند) **غیرفعال**اند، ولی (الف) خلاف T5-1 (حذف fallbackهای مرده)، (ب) اگر در آینده روتر خاموش شود یا بارگذاری ناقص باشد، کسب‌وکار را مستقیم به LS می‌نویسند. باید حذف شوند.

**🟠 F-4 — کلیدهای DEV هنوز در localStorage (T5-2 ناقص).** ۲۷ کلید `ptf_*` همچنان مستقیم نوشته می‌شوند؛ از جمله خودِ `sales-domain-v2.js` ۷ نقطه (صف کدینگ، `ptf_sales_command_*`, `ptf_offer_post_ack_warning_`, `ptf_autodraft_offer_`). این‌ها دادهٔ عملیاتی/تشخیصی‌اند که طبق رودمپ باید به IDB بروند. این یعنی **اصل E3 (LS سبک <۲۰KB) هنوز قابل اثبات نیست** و **E6 (آفلاین = outbox) کامل نیست**.

**🟠 F-5 — کش‌های سرور هنوز مستقیم در LS.** `ptf_site_suppliers/rfqs/inbox_sig` (bridge)، `ptf_fx_live_cache` (fx)، `ptf_cloud_usage` — طبق T3-4 باید read-through با TTL و خارج از سهمیهٔ LS شوند؛ نشده‌اند. در سهمیهٔ ۵MB حساب می‌شوند (ریسک مرتبط با حادثهٔ «۱۰۰٪» در مقیاس).

**🟡 F-6 — ابزارهای ریکاوری IDB-blind.** `clear-cache.html`, `force-restore.html`, `recover.html`, `sync-diagnostics.html` — صفر ارجاع به IndexedDB؛ چون دادهٔ سنگین حالا در IDB است، این ابزارها درک درستی از محل واقعی داده ندارند.

**🟡 F-7 — آمار موجود در arch-guard (بدهی پذیرفته‌شده):** خارج از فهرست‌سفید، ۲۰۱ عملیات مستقیم localStorage در `crm/*.js` (۵۴ setItem / ۱۲۶ getItem / ۲۱ removeItem) به‌عنوان baseline ثبت شده‌اند. هیچ تخلف **جدیدی** وجود ندارد (گیت PASS است) ولی **بدهی موجود صفر نشده** — یعنی «قطع کامل وابستگی» هنوز در مقیاس محقق نیست.

### ۵.۲ — چیزهایی که سالم‌اند (تا آنجا که تست/کد نشان می‌دهد)
- **هم‌نسخگی کامل:** `VERSION.json` = UI = sw.js = manifest = v34.8.34 (تست‌رها تأیید می‌کنند).
- **تطابق رجیستری A11 صفر تخلف** — کلاینت `PTF_ENTITY_CMD_ENABLED` با سرور `sd_entity_registry()` یکسان است (۴۳ کلید در W4).
- **تست‌رهای نازک‌سازی همگی سبز** (tester520/525/526/527/528/529/530/531/532/533/534/535): صفر بایپس فعال، صف IDB، مهاجرت چک‌شخصی، collection_query، read-hardening (v34.8.34: «دادهٔ محلی تا sync حاکم است»).
- **ممیزی A10:** ۲۷ کلید مستقیم، عمدتاً UI/CACHE/DEV هستند؛ هیچ کلید کسب‌وکاری مسیر فعالِ مستقیم ندارد (فقط ۲ fallback مرده در F-3).
- **اصل E1 (سرور منبع حقیقتِ نوشتن) تا حد زیادی برقرار** — نوشتن از روتر/فرمان می‌گذرد.

---

## ۶) نتیجه‌گیری و پیشنهاد

**نسخهٔ نهایی فعلی:** `v34.8.34`.

**آیا کار ناقص است؟ بله.** نازک‌سازی «تا» موج W4 و collection_query پیش رفته، اما فاز از نظر DoD **تمام نیست**:
1. **اولویت ۱:** گیت‌های CI (run-ci-gate + arch-guard + post-deploy hash) را به هر دو workflow وصل کنید — این تنها چیزی است که جلوی بازگشت بایپس‌ها را می‌گیرد و هم‌نسخگی استقرار را تضمین می‌کند.
2. **اولویت ۲:** بازنشستگی موتور legacy (T6/C5) و حذف fallbackهای مرده (T5-1) — شرط «یک موتور» و پاک‌سازی کد.
3. **اولویت ۳:** انتقال کلیدهای DEV/کش (T5-2 ناقص + T3-4) و حذف توکن از LS (T4-1b).
4. **اولویت ۴ (تأیید کارفرما):** 2FA، bulk revoke، ابزارهای ریکاوری IDB-aware (T7).

تا انجام موارد بالا، نمی‌توان مدعی شد «کلاینت حداکثری نازک» (اصول E1–E7) محقق شده است؛ وضعیت فعلی «موتور فرمان نوشتن + آغاز خواندن سرور-محور» است که همچنان دو موتور سینک و بدهی localStorage دارد.

---

**ضمیمه — منابع بررسی‌شده:** `VERSION.json`، `ROADMAP-THIN-CLIENT-MAXIMAL-2026-08-27.md`، `PHASE-C-THIN-CLIENT-ROADMAP.md`، `PHASE-C1-HOT-COLLECTIONS.md`، `ARENA-STORAGE-INDEPENDENCE-RCA-2026-08-26.md`، `ASSESSMENT-STORAGE-INDEPENDENCE-THIN-CLIENT-2026-08-27.md`، RELEASE-NOTES v34.8.12–34، `_tools/arch/arch-guard.js`، `_tools/uat/run-ci-gate.js` + tester520/525–535، `crm/key-registry.js`، `.github/workflows/deploy-*.yml`، و اسکن زندهٔ `crm/*.js` از نظر `localStorage.*`.
