# یادداشت‌های انتشار — v34.9.0 (نهایی نازک‌سازی — آمادهٔ پروداکشن)

**موضوع:** نسخهٔ نهاییِ نازک‌سازی کلاینت: بنر مهاجرت خودکار + بستهٔ اثبات E1..E7 + چک‌لیست دیپلوی پروداکشن.
**تاریخ:** ۲۰۲۶-۰۸-۲۹ · **تستر قفل‌کننده:** `tester552-v34.9.0-final-e-proofs.js` (۲۵ سنجه) · **گیت:** ۱۶۸ تستر + arch

## نوینی این نسخه
1. **MIGRATION-BANNER:** دستگاه منتقل‌نشده یک نوار کهربایی ملایم پایین صفحه می‌بیند: «تنظیمات ← وضعیت دستگاه ← انتقال یک‌باره» + دکمهٔ «رفتن به تنظیمات» و «بعداً» (ساکت فقط تا رفرش). دستگاه سبز/تازه هرگز نمی‌بیند؛ خارج از CRM خودش حذف می‌شود؛ شمار صف آفلاین را شفاف می‌گوید.
2. **بستهٔ اثبات E1..E7 (tester552):** E1/E2/E5/E6 سبز اجراشده؛ E3 هارنس+سنجهٔ آماده (عدد نهایی = هفتهٔ اول پروداکشن)؛ E4 قرارداد کامل + اثبات دستی در چک‌لیست؛ E7 پرچم‌دار (پیش‌فرض خاموش، نهایی در v34.9.1 با ≥۷ روز تله‌متری).
3. **`PRODUCTION-DEPLOY-CHECKLIST-v34.9.0.md`:** پنج فاز (پیش/دیپلوی/کاربران/هفتهٔ اول/v34.9.1) + طرح بازگشت + جدول وضعیت E.

## انباشتهٔ این شاخه از v34.8.45 (خلاصه)
R6-الف (ابزار ریکاوری IDB-aware + سقف نرخ) · R6-ب (2FA پیامکی — پیش‌فرض خاموش به درخواست مالک + پنل نشست‌های فعال با ابطال گروهی) · چهار هات‌فیکس نشست (توفان role_verify، حالت فقط-کوکی، حافظهٔ فانتوم) · جعبهٔ «وضعیت دستگاه» در تنظیمات + هماهنگی کامل پیام‌ها · بنر مهاجرت.

## برای دیپلوی
مطابق `PRODUCTION-DEPLOY-CHECKLIST-v34.9.0.md` — دیپلوی با مالک؛ کاربران: سه قدم (Ctrl+F5 → ورود → انتقال یک‌باره).

## گیت‌ها
CI ۱۶۸ تستر / ۰ خطا · arch-guard PASS (A6/A8/A10-new/A11 = صفر) · هم‌نسخگی کامل v34.9.0.
