# ARENA-HOME-DYNAMIC-X — v34.39.0 (۱۴۵/۰۶/۲۶)

## ۱) دستور کارفرما
«داینامیک‌ترین و زیباترین وب‌سایتی که یک شرکت تامین تجهیزات می‌تواند داشته باشد را در اینترنت بیاب، بررسی و کدش را بیاموز و مشابهش را روی سایت ما پیاده کن. تم رنگی فعلی را بررسی کن؛ چه تمی مشتری بیشتر با وفاداری نسبی به رنگ‌های لوگو جذب می‌کند؟ سرعت در بالاترین حد؛ UI موبایلی بسیار حرفه‌ای.»

## ۲) پژوهش — بنچ‌مارک‌های انتخابی و درس‌های استخراج‌شده
| بنچ‌مارک | چرا مرجع است | چه چیزی وام گرفته شد |
|---|---|---|
| **Grainger** (رتبه ۱ ecomm.design — «بهترین کلی B2B صنعتی») | صفحه اول سریع، مقوله‌محور، دسترسی آنی به دسته‌ها | سلسله‌مراتب «دسته/ابزار در همان نگاه اول» + مینی‌مال‌کردن تزئینات سنگین |
| **McMaster-Carr** («افسانه‌ای‌ترین جست‌وجوی صنعتی») | جست‌وجو محورِ تمام‌عیار، JS اندک، رندر سروری، بدون پاپ‌آپ/کاروسل تزئینی | **نوار جست‌وجوی Part Number در هیرو** (با فرم GET خالص، بدون JS هم کار می‌کند) |
| **Fastenal / Uline** | تعادل enterprise/SMB، مسیر شفاف استعلام | CTA دومِ «ثبت استعلام» در وضعیت برجسته + چیپ‌های میان‌بر |
| **SPT Labtech / Robin Radar / Devitech / Path Robotics** (برندگان ۲۰۲۵) | مدرن‌ترین ظاهر صنعتی: تیره+accent، تایپوگرافی درشت، حرکت سینمایی | اورلی تیره، تایپ‌کینتیک، باند تیره اعتماد، موشن‌های transform-only |
| روان‌شناسی رنگ B2B (usevisuals/wisecoda ۲۰۲۵) | ۶۰/۳۰/۱۰ + اثر انزوا (Von Restorff) | ساختار تم زیر؛ **CTA نارنجی روی زمینه‌های سرد = بالاترین تبدیل** |

**تحلیل تم فعلی (قبل):** `--red #ef4b1a` + `--orange #f79400` + ذغالی، اما (الف) لایه v32.1 آیکون‌ها را **تِال `#0e7490`** کرده بود که با لوگو ناسازگار است، (ب) هیرو/فوتر صرفاً خاکستری-سیاه بودند (بی‌هوویت)، (ج) در موبایل سه دکمه شناور روی هم می‌افتادند.

## ۳) تم رنگی جدید (وفادار به لوگو: چرخ‌دنده گرافیتی + شعله)
- **۶۰٪ سطوح روشن** (`#f7f8fa/سفید`) برای خوانایی و حس مهندسی؛
- **۳۰٪ گرافیت فولادی/سرمه مهندسی** (`--steel-900..950 #0b1220…`، یخی `--ice #eef3f9`، لینک استیل `--link-steel #155a97`) → منطقه اعتماد: اورلی هیرو، باند سوابق (#projects)، فوتر، گرید مهندسی ظریف؛ سرمه همان بازه خاکستریِ لوگو است که به عمق آبی-فولادی رفته — «اعتماد صنعتی» بدون خروج از هویت؛
- **۱۰٪ شعله برند** (`--red/--orange`) فقط برای CTA/تبدیل (اثر انزوا؛ روی زمینه‌های سرد بیشترین کنتراست را دارد).
- آیکون‌های تِالِ ناسازگار روی صفحه اول به «استیل + هاله شعله» یکپارچه شد؛ `theme-color` دوحالته (روشن/تیره) + `viewport-fit=cover`.
- توکن‌ها در `style.css` سراسری و در `home.css` لایه‌محور تعریف شدند؛ بقیه صفحات با افزودن مصرف‌کننده، بدون شکستن چیزی، می‌توانند ریزش کنند.

## ۴) آنچه پیاده شد (فقط لایه نمایشی؛ قرارداد HOME-MODERN حفظ)
1. **Finder تجهیز در هیرو** — `form.ptf-finder` با GET به `/search/?q=`، ورودی ۱۶px (ضد زوم iOS)، چیپ‌های پرتکرار (A106/فلنج WN/Rosemount 3051/بال‌ولو/VFD/گسکت) → الگوی McMaster؛ بدون JS کامل کار می‌کند.
2. **پرده‌گشایی تایپوگرافیک H1** — شکافت کلمه‌ای با `ptf-motion.js` (mask-reveal + گرادیان شعله روی strong)؛ با JS خاموش/کاهش‌حرکت، DOM محتوا دست‌نخورده (SEO امن؛ h1 exact-match با tester662 قفل شد).
3. **داک پایین موبایل** (≤۷۹۰px، هم‌بریک‌پوینت کشو): خانه / **RFQ برجسته** / تماس / واتس‌اپ / چت هوشمند؛ ۵ هدف لمسی ۵۴px+، `env(safe-area-inset-bottom)`، مخفی‌سازی خودکار در اسکرول نزولی، همان‌جا دکمه‌های شناور قدیمی خاموش می‌شوند (پایان هم‌پوشانی)؛ `body` padding ضدکلایز.
4. **کشوی حرفه‌ای**: بک‌دراپ بلور + قفل اسکرول + همبرگر↔✕ انیمیشنی + ESC و فوکوس‌بک — sync با MutationObserver بدون رقابت با toggle موجود main.js.
5. **میکرو-اینترکشن‌ها**: اسپات‌لایت نشانگر روی کارت‌ها (فقط `hover:hover+pointer:fine`)، لیفت ۳بعدی کارت خدمات، هدر جمع‌شونده با خط برند، شمارنده‌های فارسی‌رقم (متن اصلی دقیق بازگردانده می‌شود).
6. **باندهای تم**: اورلی سرمه+هاله شعله، باند سوابق سرمه با گرید مهندسی ماسک‌خورده، فوتر گرادیان گرافیت-سرمه، journey یخی.

## ۵) سرعت (بدون کتابخانه جدید؛ صفر دارایی جدید)
- `ptf-motion.js` ≈ ۸KB خام با defer + cache-bust؛ listenerها همه `passive`، همه نوشتن در rAF batch؛
- فقط `transform/opacity` (هیچ reflow انیمیشنی)، `will-change` فقط در لایه قبلی؛
- `content-visibility:auto + contain-intrinsic-size` برای ۵ بخش زیرِ صفحه (فقط ≥۷۹۱px؛ LCP دست‌نخورده)؛
- اسلایدر هیرو: **توقف خودکار در تب پنهان** (باتری/CPU) + سوایپ لمسی؛ `100svh` با fallback؛
- preload‌های حیاتی قبلی حفظ (تست ۵.۴)؛ کش ۳۰ روزه `.htaccess` بدون تغییر؛ حجم home.css ≈ ۲۳KB و style.css ≈ ۳۶KB خام (gzip ≈ ۵-۸KB).

## ۶) QA / گیت‌ها
- `arch-guard`: PASS (A1..A11 سبز) — پس از تمام تغییرات.
- `tester662` (قرارداد جدید، **۳۶/۳۶ سبز**): تم/Finder/Dock/Motion/Speed/نگهبان بی‌تغییری محتوا.
- `tester661`: بند ۵.۵ cache-bust عمداً به `v=34.39.0` به‌روز شد → **۲۸/۲۸**.
- `tester210/216/220/318`: سبز به‌جز یک FAIL از پیش موجودِ tester220 (پیوست ثبت‌نام تأمین‌کننده — روی base هم قرمز است، بی‌ربط).
- گیت کامل: `274 PASS / 1 FAIL`؛ FAIL باقی‌مانده = tester656 (پریدن ران تایم PHP در محیط بدون php؛ روی base هم رفتار مشابه).
- HTML با پارسر: بدون خطای تگ؛ `node --check` روی main.js و ptf-motion.js سبز.

## ۷) فایل‌های تغییر‌یافته
`index.html` (finder + dock + metaها + cache-bust + اسکریپت) · `assets/css/home.css` (لایه HOME-DYNAMIC-X) · `assets/css/style.css` (توکن‌های سراسری + قفل کشو + ✕ همبرگر) · `assets/js/main.js` (اسلایدر: swipe/pause) · `assets/js/ptf-motion.js` (جدید) · `_tools/uat/tester662-…js` (جدید) · `_tools/uat/tester661-…js` (بند ۵.۵) · `_tools/uat/run-ci-gate.js` (ثبت ۶۶۲) · `VERSION.json`.

## ۸) مرحله بعد (پیشنهادی، در این نسخه انجام نشد)
تسری تدریجی تم به صفحات خدمات/محصولات (مصرف همان توکن‌ها)، تبدیل `search/` به autocomplete زنده با `search-index.json`، و A/B رنگ CTA (نارنجی فعلی در برابر کهربایی) با متریک `ptf-metrics`.

---

## ۹) افزونهٔ v34.39.1 — HOME-ELECTRIC-3D (دور دوم دستور کارفرما)
درخواست: «جریان برق روی کارت/دکمه‌ها، هاور رنگی، منوی موبایل به نوار پایین، انیمیشن منوها، روز/شب، داک منومحور بدون RFQ با وسط سه‌بعدی، فلش‌های مینیمال، اعداد فارسی».
- **Current-Ring:** حلقهٔ گرادیانی با `mask: content-box/xor` + انیمیشن `background-position` (بدون @property؛ سازگار کامل). دائم روی `.btn-primary/.header-call/#ptfDock .dock-cta/#ptfDockChat/.ptf-finder button`، هاور روی سه خانوادهٔ کارت؛ زیر `no-preference`.
- **موبایل:** `.menu-toggle` حذف؛ `#mainNav` به شیت پایین (بالای داک) با استگر `ptfSheetItem` ارتقا یافت؛ مگا/آکاردئون تزریقی discover.js همان‌جا سالم کار می‌کند (اوراید با #id + !important)؛ دکمهٔ منو وسط داک با مروف همبرگر↔✕؛ داک هنگام بازبودن شیت قفلِ مخفی‌سازی دارد.
- **تم روز/شب:** کوکی `ptf_theme` (A10-safe) + بوت‌استرپ ضدFOUC در head + `prefers-color-scheme` بار اول + sync `theme-color`؛ پالت `html.ptf-dark` با attribute-override برای رنگ‌های درون‌خطیِ محتوای قدیمی (کارت‌ها/متن‌ها/فرم/داک/شیت/مارکی) و حفظ پس‌زمینهٔ روشن برای لوگوی برندها.
- **سه‌بعدی:** perspective + `--rx/--ry` از pointermove (rAF) + `translateZ` روی بدنهٔ کارت + لیفت/فشار دکمه‌ها + تیلت پنل هیرو.
- **فلش‌ها:** درپوش `<i class="arw">` با JS (متن اصلی دست‌نخورده؛ بدون JS = همان فلش‌های فعلی) و اسلاید فنری هاور.
- **اعداد:** ۳ / ۶+ در پنل هیرو.
- **QA:** tester663 ۳۰/۳۰ · arch-guard PASS · پین‌های سراسری با ابزار رسمی bump شد (۵۹۸ مورد/۱۳۹ فایل) · `RELEASE-NOTES-v34.39.1.md` الزامیِ متاتست ۵۷۸ تولید شد.

---

## ۱۰. الحاقیه v34.39.2 — HOME-DOCK-SUN (فیکس شیت + داک استعلام‌محور + شب خودکار از غروب)
- **باگ گزارش‌شدهٔ «دکمهٔ منو کار نمی‌کند»:** `backdrop-filter` روی `.site-header` برای نوادگان `position:fixed` بلوکِ containing می‌سازد؛ شیتِ داخلِ هدر با `bottom:86px` به باکس ۸۶پیکسلیِ هدر لنگر می‌شد و بیرونِ ویوپورت رندر می‌گشت (کلاس و مروف عوض می‌شد ولی دیده نمی‌شد). فیکس: خاموشی بلور + پس‌زمینهٔ جامدِ نیمه‌مات در ≤۷۹۰.
- **داک:** منو (اسلات اول، همبرگر↔✕) | تماس | **استعلامِ واقعی `a[href="rfq/"]` وسط با قاب CTA** | واتساپ | چت. خانه از لوگو در دسترس.
- **تم:** حذف کلید از هدر؛ بوت `<head>` با فرمول خورشید NOAA (تهران ۳۵.۷/۵۱.۴۴، دقت ±۵ دقیقه، صفر بایت شبکه) — نبودِ کوکی = خودکار؛ `setInterval` یک‌دقیقه‌ای در تب فعال؛ کلید دستی «نمای شب/روز» منتقل‌شده به انتهای آیتم‌های `#mainNav` (ردیف تمام‌عرض در شیت / قرص در نوبار) با ماندگاری کوکی `ptf_theme`؛ stopPropagation تا هندلر `.main-nav a` مزاحم نشود.
- **برق:** `ptfCurS` — عبور ۵۲٪ + سکوت ۴۸٪؛ دائم ۸s (از ۳.۴s)، هاور ۵s (از ۲.۲s)؛ زیر همان kill-switch کاهش‌حرکت.
- **تست:** tester664 (۲۹/۲۹) شامل صحت عددی نیمروز خورشیدی و بازتنظیم ۶ سنجهٔ برگشت‌خوردهٔ tester663؛ گیت ۲۷۶/۱ (۶۵۶ محیطی)؛ arch سبز.

---

## ۱۱. الحاقیه v34.39.3 — HOME-NIGHT-READ
- فیکسِ قطعیِ «منوی موبایل باز نمی‌شود»: جابه‌جاییِ گرهٔ `#mainNav` به `body` در ≤۷۹۰ (فرارِ کامل از هر containing-blockِ اجدادِ هدر) + بازگشت در دسکتاپ + `z-index:9535`.
- حذف `#ptfDockChat` از حلقهٔ جریان؛ نرم‌سازیِ هستهٔ سفید `.95→.7`.
- شب: هاورِ گرمِ کارت‌ها (گرانیتِ آبی‌نفتی، تیتر کهربایی)، روشن‌شدنِ summary/پاسخ‌های FAQ، پوششِ خاکستری‌های اسلیت درون‌خطی (#475569/#60646d/#4b4d55)، سفیدِ خالص + text-shadow روی CTA استعلامِ داک (شب با قاعدهٔ ارجحیتِ تیره‌تر از list رنگ خنثی می‌گرفت).
- فشرده‌سازیِ کامنت‌های تزئینیِ home.css برای ماندن زیرِ بودجهٔ ۴۰K نویسه، با حفظِ مارکرهای لایه‌ها.

---

## ۱۲. الحاقیه v34.39.4 — HOME-ACCORD-NIGHTMEGA
- آکاردئونِ «درباره ما» در شیت موبایل: `max-height/visibility` روی `.nav-drop-menu`، شِورونِ CSS با چرخش، هندلِرِ capture روی `#mainNav` (تپ اول = باز، تپ دوم = پیمایش) برای دورزدنِ بستنِ خودکارِ شیت توسط `main.js`؛ تک‌باز بودن و پاک‌شدنِ `.open` در `sync` (بستن شیت)؛ reduce-motion kill.
- خواناییِ شبِ مگای محصولات: اوررایدهای `html.ptf-dark #mainNav .nav-mega*` با `!important` (ارجحیتِ id بر MEGA_CSSِ تزریقِ runtime غلبه می‌کند)؛ `ptf-discover.js` دست‌نخورده.
- بازگشتِ home.css/ptf-motion به زیرِ بودجه‌ها (۳۹٬۹۷۸ / ۱۱٬۴۶۵) با فشرده‌سازیِ کامنت‌ها و حفظِ مارکرهای هر پنج لایه؛ tester666 (۲۳ سنجه).

---

## ۱۳. الحاقیه v34.39.5 — HOME-ICONMIN
- حذف نمایشیِ «جستجو» از شیت موبایل (`#mainNav .nav-search{display:none!important}` ≤۷۹۰)؛ دسکتاپ و Finder هیرو سالم.
- کلید تم در شیت = آیکونِ ۴۲px بی‌قابِ گوشهٔ بالا (border/bg/label حذفی؛ هاورِ کهربایی؛ reduce kill)؛ دسکتاپ قرصِ برچسب‌دار ماند.
- فیکسِ بازگشتِ تم: `memManual` در حافظه + کوکی best-effort — در iframeهای بدون کوکی، بازبینیِ ۶۰ثانیه‌ی خورشید دیگر انتخابِ دستی را واژگون نمی‌کند.
- شماره‌های تماس/واتساپ پس از پرسشِ کارفرما تأیید و به‌عنوان canonical قفل شدیدی (سنجه‌های ۴.۱–۴.۳)؛ بی‌عملکردیِ ظاهری فقط محدودیتِ پروتکلِ iframe پیش‌نمایش است.
- ماندن زیرِ بودجه‌ها با layout-minifyِ ایمنِ home.css (۳۹٬۷۹۵)؛ tester667 (۲۱ سنجه).

---

## ۱۴. الحاقیه v34.39.6 — HOME-WAVE
- درخواست: «موج مورب رنگی روی کادرِ دپارتمان/صنایع هر چند ثانیه عبور کند.»
- `.hero-panel::after` با گرادیان ۱۱۵degِ برند + `mix-blend:screen`؛ `ptfWave` ۸s (۵۲٪ عبور، ۴۸٪ سکوت)؛ شب پررنگ‌تر؛ reduce kill؛ صفر JS/المان/شبکه؛ tester668 (۱۲).

## ۱۵. الحاقیه v34.39.7 — HOME-WAVE2
فیکسِ «موج ثابت»: افزودنِ `background-repeat:no-repeat` + موقعیتِ اولیهٔ بیرونِ قاب و جبرانِ resetِ shorthand در قانونِ شب؛ رگرسیون در 668 (۱.۳b).
