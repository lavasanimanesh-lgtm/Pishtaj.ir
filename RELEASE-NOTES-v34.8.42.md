# یادداشت‌های انتشار — v34.8.42 (R4-گام۱)

**موضوع:** بازنشستگی موتور legacy سینک — گام ۱: تله‌متری پنجره‌ای + پرچم `PTF_LEGACY_PUSH_OFF` + داشبورد تصمیم (پیش‌فرض = رفتار امروز، بدون تغییر رفتار).
**تاریخ:** ۲۰۲۶-۰۸-۲۹ · **تستر قفل‌کننده:** `tester543-v34.8.42-r4g1-legacy-push-off.js` (۴۴ سنجه · سبز) · **گیت:** run-ci-gate + arch-guard + node --check

## پس‌زمینه
دو موتور سینک زنده‌اند: فاز B (فرمان‌های entity، مالک دستگاه‌های همگراشده) و موتور legacy (`pushDirty`/`data_push` توده‌ای + `pullCheck`/`data_pull` — شبکهٔ امنیتی دستگاه‌های همگرانشده، ~۲۰۰۰ خط). حذف legacy (گام ۲، v34.8.43) فقط با **شواهد پنجره‌ای** ممکن است: ≥۷ روز با push توده‌ای ≈ صفر برای هر کلید.

## چه چیزی عوض شد

### ۱) سرور (api/crm.php)
- **تله‌متری پنجره‌ای:** تله‌متری push (زیر همان flock دیتا_پوش) سطل روزانه `d[YYYY-MM-DD]` می‌گیرد و به ۱۴ روز آخر هرس می‌شود.
- **`sync_stats`** حالا per-key `win7` (مجموع push توده‌ای ۷ روز آخر)، `win7Days` و مبدأ `win7From` را برمی‌گرداند؛ ردیف بدون سطل (تله‌متری قدیمی) → `win7=null` = شواهد ناکافی.
- **اکشن‌های جدید** (هر دو `users_write`): `sync_engine_flags` (خواندن) و `sync_engine_flag_set` (نوشتن با **گیت شواهد سمت سرور**: قطع push توده‌ای فقط با win7 ≤ 3 یا force صریح + دلیل ≥۵ نویسه؛ هر تصمیم با کاربر/زمان/دلیل/win7 لحظهٔ تصمیم در `sync/engine_flags.json` ثبت و کاملاً بازگشت‌پذیر است؛ نوشتن اتمیک).

### ۲) کلاینت (crm/sync.js)
- **بارگذاری پرچم‌ها:** `ptfEngineFlagsLoad` — read-through از لایهٔ ptfCache (کلید `ptf_engine_flags`، TTL ۱ ساعت؛ ساختهٔ R3) با refresh از سرور؛ در بوت غیرمسدودکننده.
- **گیت fail-open در pushDirty:** `ptfLegacyPushGateDecision(keys)` — کلید پرچم‌شده روی دستگاه فاز B → از push توده‌ای حذف (gated؛ مالکش فرمان‌های entity است) + ثبت R4-GATE؛ روی دستگاه همگرانشده → **عبور** (bypass؛ تنها کانال نوشتن همین است) + ثبت یک‌باره به‌ازای نشست R4-GATE-BYPASS در audit — این خطوط تله‌متری دقیقاً نشان می‌دهند کدام دستگاه‌ها مانع گام ۲ هستند.
- **شبکهٔ امنیتی legacy:** اگر فاز B فعال باشد ولی صف آن در دسترس نباشد (استقرار ناقص)، قبلاً نوشته‌ها بی‌صدا گیر می‌کردند؛ حالا (الگوی خود `ptfSyncFlushNow`) به legacy برمی‌گردد و گیت فقط کلیدهای پرچم‌شده را حذف می‌کند.

### ۳) داشبورد تصمیم (تنظیمات → 🚦 موتور همگام‌سازی)
- جدول per-key: push کل · **پنجرهٔ ۷روزه** · تداخل · وضعیت (🟢 آمادهٔ قطع / 🟡 شواهد ناکافی / 🔴 هنوز فعال) · پرچم.
- دکمهٔ قطع/بازگردانی فقط برای نقش ارشد؛ دلیل اجباری → ثبت در audit + سرور.

## تصمیم دامنه
- **T3-5 (بوت صفحه‌ای)** همچنان در پنجرهٔ R4 (گام ۲) — پروتکل pull مرکزی، نیازمند تست دستگاه واقعی.
- تغییری در `pullCheck`/`data_pull` داده نشد — حذف pull در گام ۲ است.

## تست دستی پیشنهادی روی استیجینگ
1. تنظیمات → 🚦 موتور همگام‌سازی → جدول با ستون «پنجرهٔ ۷روزه» (روز اول اکثراً 🟡 «شواهد ناکافی» — طبیعی است؛ سطل‌ها از همین نسخه جمع می‌شوند).
2. نقش ارشد: قطع یک کلید با win7 ≤ 3 → پرچم ⛔ + ردیف audit R4-GATE؛ بازگردانی → ✅.
3. تلاش قطع کلید فعال بدون force → خطای گیت شواهد سمت سرور.

## گیت‌ها
- `run-ci-gate`: ۱۵۸ تستر (tester543 جدید) — همه سبز.
- `arch-guard`: بدون تخلف جدید (پرچم‌ها در ptfCache، نه LS).
- تغییرناپذیرها: ۴ ابزار ریکاوری HTML (R6)، backup.js، پروداکشن v34.8.12.
