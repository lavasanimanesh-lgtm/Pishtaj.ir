# RELEASE NOTES — v34.17.0 (صفحات AI-لمس‌شده در برابر بقیه — تکمیل داشبورد KPI سئو)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** tester562 (۲۷ سنجه) · **گیت:** 178 PASS / 0 FAIL · **PHP-لینت:** php-parser (cms.php + gsc.php) ✓

## ۱) رجیستری صفحات AI-لمس‌شده (`crm/data/ai-touched.json`)

- «AI-لمس‌شده» = صفحه‌ای که از مسیرهای محتوای هوشمند ساخته/ویرایش شده. **۷ نقطهٔ ثبت:** مولد صفحه (`page_create`)، زمان‌بند (`sched_publish`)، وبلاگ، مرکز دانش، محصول، **متای AI** (اعمال از صف سئو)، **بینایی** (alt گروهی).
- ساختار `{path:{last,k:{kind:ن‌بار}}}` با سقف ۱۰۰۰ مسیر (حذف قدیمی‌ترین) و گیت ضد path-traversal.
- **بذر یک‌باره از `cms_log.txt`:** اولین فراخوانی `ai_list` تاریخچهٔ انتشارهای گذشته (تا ۲۰هزار خط آخر لاگ) را با نقشهٔ اکشن→پوشه (blog/kc/products/…) بازسازی می‌کند — گذشته هم از دست نمی‌رود.
- `ai_list`: فهرست ۲۰۰ تازه‌ترین با وضعیت live و نشان kind.

## ۲) مقایسه (`ai_pages` در gsc.php)

- هر اسنپ‌شات روزانه (topPages) به دو گروه تفکیک می‌شود: AI (کلید نرمال‌شدهٔ رجیستری) و بقیه.
- نرمال‌ساز مسیر مشترک: حذف دامنه + اسلش انتهایی→`index.html` + ریشهٔ سایت.
- خروجی روزانه: کلیک/نمایش/جایگاه (میانگین وزن‌دار با نمایش) هر دو گروه + سقف ۶۰ روز.
- جمع‌بندی: **سهم کلیک AI** + سهم اولین روز در برابر آخرین روز (روند سهم) + صفحات AI در آخرین اسنپ‌شات (≤۱۵).
- **صداقت آماری:** یادداشت ثابت — مقایسه فقط در دامنهٔ ۳۰ صفحهٔ برترِ هر روزِ اسنپ‌شات معنا دارد؛ بدون هیچ فراخوانی/دادهٔ جدید.

## ۳) UI (پنل سرچ کنسول)

- کارت «🤖 صفحات هوش مصنوعی (N صفحه)»: سهم از کلیک‌ها + روند سهم (▲سبز/▼قرمز با مقدار)، **میانگین جایگاه AI در برابر بقیه** (رنگی: ≤۳/≤۱۰/بدتر)، میله‌های سهمِ ۳۰ روز آخر با tooltip دوبعدی، جدول صفحات هوشمندِ آخرین اسنپ‌شات.
- حالت خالی: پیام راهنما تا اولین انتشار هوشمند.

## بهداشت

- انکرهای پنجرهٔ تستر بدون نسخه (مقاوم به جاروی bump — تلهٔ شناخته‌شده این‌بار در همان تستر جدید ضبط و رفع شد).
- A10 پابرجا؛ `ai_pages` فقط می‌خواند (بدون نوشتن).

## فایل‌های کلیدی

`api/cms.php` (cms_ai_* + ۷ لمس + ai_list) · `api/gsc.php` (gsc_page_norm + ai_pages) · `crm/gsc.js` (کارت gscAi) · `_tools/uat/tester562-v34.17.0-seo-ai-impact.js`
