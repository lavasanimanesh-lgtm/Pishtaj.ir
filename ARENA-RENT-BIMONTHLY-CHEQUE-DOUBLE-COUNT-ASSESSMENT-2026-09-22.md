# ARENA · ارزیابی — «دوباره‌شماری هزینهٔ اجاره‌بها در پرداخت ۲ماهه با چک»

**تاریخ:** ۲۰۲۶-۰۹-۲۲ (۱۴۰۵/۰۷/۰۱) · **پایه کد:** `arena/01a0cac2-pishtaj-ir` — `VERSION.json` = **CRM v34.39.23**
**نوع کار:** فقط ارزیابی — **هیچ فایل کدی تغییر نکرد**؛ فقط همین سند اضافه شده است.
**دامنه:** `crm/opex.js` · `crm/treasury.js` · `crm/fiscal.js` · `crm/cheque-panel.js` · `crm/cheque-module.js` · `crm/data-quality.js` · `crm/finance-write-guard.js`

---

## ۰) خلاصهٔ مدیریتی — پاسخ کوتاه به چهار سؤال

| سؤال | پاسخ |
|---|---|
| **۱. وضعیت فعلی کد چگونه است؟** | طراحی فعلی «اجارهٔ ماهانه در opex + پرداخت ۲ماهه با چک» **فقط در صورتی درست است که چک به ردیف‌های هزینه وصل شود**. در این حالت هر عدد دقیقاً یک‌بار شمرده می‌شود. اگر چک بدون وصل‌شدن ثبت شود (یا ردیف‌ها به‌اشتباه «تسویه» شوند)، خزانه و سود نقدی **دو بار** کم می‌شود. |
| **۲. آیا «ثبت تسویه» و «وصول چک» دوباره‌شماری می‌سازد؟** | **با چکِ وصل‌شده: خیر** (دکمهٔ تسویه اصلاً ظاهر نمی‌شود و وصول فقط وضعیت را عوض می‌کند، خروج جدید نمی‌سازد). **بدون اتصال: بله** — تسویهٔ هر ردیف یک خروج ثبت می‌کند و سررسید/وصولِ همان چک یک خروج دیگر؛ جمعاً دو برابر. |
| **۳. اصلاح دوباره‌شماری گذشته بدون تغییر کد؟** | **بله** — با تب «کیفیت دادهٔ مالی» (آشکارساز read-only موجود) + حذف ردیف تکراری با tombstone + ابطال چک اضافی. دستور کار کامل در بخش ۴. |
| **۴. اگر خروجی خزانه قابل ویرایش شود مشکل حل می‌شود؟** | **نه** — دفتر خزانه هیچ رکورد ذخیره‌شده‌ای ندارد؛ هر بار از روی رکوردهای منبع (opex/چک/تنخواه) **بازمحاسبه** می‌شود. ویرایش مستقیم آن یا فوراً با رندر بعدی از بین می‌رود یا سود نقدی سال مالی را که از مسیر دیگری محاسبه می‌شود اصلاح نمی‌کند. اصلاح باید روی **رکورد منبع** انجام شود که همین امروز هم از UI ممکن است. |
| **۵. بهترین راهکار فعلاً (بدون تغییر کد)** | پاکسازی گذشته با دستورالعمل بخش ۴ + تغییر عادت کاری به جریان «چکِ وصل‌شده به ماه‌های هزینه» (بخش ۵). |

---

## ۱) وضعیت فعلی کد — پولِ اجاره از کجا و چگونه شمرده می‌شود؟

### ۱.۱ دو دفترِ مستقل که اجاره در آن‌ها ظاهر می‌شود

| دفتر | فرمول/منبع | چه چیزی از اجاره شمرده می‌شود |
|---|---|---|
| **سود خالص سال (تعهدی)** | `net = projectProfit − opexTotal − petty + amendTotal + cover` (`crm/fiscal.js:272`) | همهٔ ردیف‌های فعالِ بدون `dealRef` در `ptfOpexSumFiscal` (`crm/opex.js:253`) — هر ردیف اجاره یک‌بار، صرف‌نظر از پرداخت‌شدن |
| **خزانه / سود نقدی** | `treasuryCrmMoves` (`crm/treasury.js:126`) و `ptfFiscalCashData` (`crm/fiscal.js:378`) | فقط حرکت واقعی وجه — قواعد در ۱.۳ |

### ۱.۲ سه نوع ردیف اجاره در «هزینه‌های جاری» و قاعدهٔ نقدی‌شدن

`opexCashReady` (`crm/opex.js:240`) + `ptfOpexSumFiscal`:

| نوع ردیف | علامت | شمارش در سود تعهدی | شمارش در خزانه/نقدی |
|---|---|---|---|
| دستی یک‌باره | بدون `tplId`/`chequeCd` | ✔ بلافاصله | ✔ **بلافاصله** (فرض: بدون چک یعنی نقد) — `treasury.js:197-222` |
| تکرارشونده (قالب) | `tplId`/`recurringKey` | ✔ | ✘ تا `st==='settled'` (تسویه) |
| متصل به چک | `chequeCd` یا `opexRowIds` روی چک | ✔ | ✘ (نگه‌داشته می‌شود) — چک خودش در سررسید شمرده می‌شود |

قاعدهٔ نقدیِ چک‌متصل همین نسخه (v34.39.23 — RENT-CASH-UNTIL-PAID، ثبت در `VERSION.json`) تازه سفت‌شده است: «تکرارشونده و اجارهٔ متصل به چک تا تسویه/سررسید خروج نقدی نیست».

### ۱.۳ سمت خزانه — دقیقاً کدام رکوردها «خروج» می‌شوند؟

در `treasuryCrmMoves`:
- ردیف opexِ **متصل به چک** حذف می‌شود: `opexHeldForCheque` (`crm/treasury.js:96`).
- ردیف opexِ تکرارشونده فقط با `st==='settled'` خروج می‌شود (`crm/treasury.js:210-211`).
- ردیف opexِ **دستیِ بدون چک همان لحظه** خروج است.
- چک صادرهٔ شرکت: در **سررسید** (حتی قبل از وصول) یک‌بار با کلید یکتای `chqout:<cd>` خروج می‌شود — `chequeMatured` (`crm/treasury.js:88`)؛ وصول (`ptfChequeClearIssued`، `crm/cheque-module.js:166`) فقط `st='cleared'` می‌گذارد و **خروج جدیدی نمی‌سازد** (همان کلید قبلی دوباره جمع نمی‌شود).
- در سود نقدی هم چکِ دارای `opexRowIds` از «چک‌های مستقل» حذف می‌شود (`crm/fiscal.js:444`).

### ۱.۴ مسیر طراحی‌شدهٔ درست (single-count)

ثبت چک مالی از پنل چک با تیک‌زدن ماه‌های اجاره در بخش «بابت کدام ماه‌های هزینه جاری؟» (`crm/cheque-panel.js:320-366`):
1. ردیف‌های ثبت‌شدهٔ بدون چک با `ptfOpexLinkCheque` (`crm/opex.js:1220`) `chequeCd` می‌گیرند؛
2. ماه‌های آیندهٔ قالب با فرمان سروری `schedule_recurring_opex_cheque` ساخته و وصل می‌شوند (`crm/opex.js:1186`)؛
3. نتیجه: **سود تعهدی = ردیف‌های ماهانه (هر ماه یک‌بار)**، **خزانه = فقط چک (یک‌بار در سررسید)**. بدون هیچ دوباره‌شماری.

---

## ۲) کجا دوباره‌شماری متولد می‌شود؟ (سه سناریوی واقعی)

### سناریو A — چک ثبت شده ولی به ردیف‌ها وصل نشده + ردیف‌های اجاره «دستی» — **محتمل‌ترین**
- ردیف دستی بدون چک = خروج نقدی همان لحظه در خزانه (هر ماه) + چک ۲ماهه در سررسید = خروج دوم ⇒ **خزانه و سود نقدی: ۲ برابر**. سود تعهدی درست (یک‌بار).
- گارد تسویه (v34.38.19، `crm/opex.js:374-393`) فقط «خواهر opex» و «تنخواه هم‌مبلغ» را می‌بیند؛ **چکِ بدون اتصال را نمی‌بیند** ⇒ هیچ هشداری هم نمی‌آید.

### سناریو B — کاربر برای «قطعی‌شدن پرداخت» روی ردیف‌های قالبی «✔ تسویه» زده + چک هم بدون اتصال سررسید شده
- تسویه هر ردیف قالبی = خروج خزانه در تاریخ تسویه (`crm/opex.js:349-354`) + چک در سررسید = خروج دوم ⇒ **۲ برابر در خزانه/سود نقدی**. سود تعهدی درست.
- بعد از تسویه هیچ «لغو تسویه»ای وجود ندارد (`st='settled'` برگشت‌پذیر نیست؛ فقط ابطال کل ردیف).

### سناریو C — دو ردیف هزینه برای یک ماه (دستی + قالبی، هم‌دسته/هم‌مبلغ)
- سود تعهدی هم ۲ برابر می‌شود (`ptfOpexSumFiscal` هر دو فعال را جمع می‌کند). این همان شکاف شناخته‌شدهٔ سند `ARENA-OPEX-RECURRING-SALARY-DOUBLE-COUNT-ASSESSMENT-2026-09-07.md` (شکاف ۱) است و آشکارسازش از v34.38.6 موجود است.

### سناریوهایی که دوباره‌شماری **نمی‌سازند** (برای اطمینان)
- چک وصل‌شده → تسویه غیرممکن است (ردیفِ دارای `chequeCd` «تسویه‌شده» شمرده می‌شود و دکمهٔ تسویه ظاهر نمی‌شود — `crm/opex.js:334,1286`)؛
- وصول چک (قبل یا بعد از سررسید) → همان یک خروج قبلی می‌ماند، خروج دوم ساخته نمی‌شود؛
- ابطال چک → خروج چک از خزانه حذف می‌شود (`active()` در `crm/treasury.js:26-30`).

---

## ۳) پاسخ تفصیلی به «تسویه + وصول»

| ترکیب | نتیجه |
|---|---|
| چک **وصل** به ردیف‌ها → وصول چک | یک‌بار شمارش (در سررسید). ✅ |
| چک **وصل** → تسویهٔ ردیف‌ها | دکمهٔ تسویه وجود ندارد؛ ردیف‌ها خودبه‌خود «settled» محسوب می‌شوند ولی خروج جداگانه نمی‌سازند. ✅ |
| چک **بدون اتصال** → تسویهٔ ردیف‌ها + سررسید/وصول چک | **دو خروج برای یک پول.** ❌ (سناریو B) |
| ردیف‌های دستی بدون چک + چک بدون اتصال | **دو خروج.** ❌ (سناریو A) |

نکتهٔ مهم: خزانه چک را از **تاریخ سررسید** می‌شمارد نه از وصول؛ پس «وصول» هرگز عدد جدید اضافه نمی‌کند. عامل دوباره‌شماری فقط «تسویه/ردیف دستیِ بدون چکِ متصل» است.

---

## ۴) اصلاح گذشته — بدون هیچ تغییر کد

### ۴.۰ قبل از هر چیز: تشخیص (فقط‌خواندنی)
1. **تب «کیفیت دادهٔ مالی»** → «هزینه جاری مشکوک به دوباره‌شماری» (v34.38.6، `ptfOpexSuspectedDuplicates` در `crm/opex.js:513`؛ نمایش در `crm/data-quality.js:95`) — موردِ سناریو C را می‌گیرد.
2. **گزارش دوره‌ای خزانه** (`ptfTreasuryPeriodReportHtml`) با فیلتر منبع = «هزینه جاری» + «چک سررسیدشده» و مقایسهٔ ماه‌ها با چک‌های اجاره — سناریوهای A/B را عددی نشان می‌دهد.
3. جمع هر ماه «اجاره‌بها» را در سال مالی با (مبلغ ماهانه × تعداد ماه) و چک‌های صادرهٔ اجاره تطبیق دهید: **هر مبلغ باید فقط از یک مسیر در خزانه دیده شود.**

### ۴.۱ دستور اصلاح برای هر ماهِ دوباره‌شماری‌شده
اصل: **برای هر ماه، یک ردیف هزینه و یک منبع خروج نقدی نگه دارید؛ بقیه را با سند ابطال/حذف tombstone بردارید.**

- **دو ردیف opex برای یک ماه (دستی + قالبی):** ردیف **دستی** را 🗑 حذف کنید (`ptfOpexDel` — حذف فیزیکی نیست؛ tombstone حسابرسی‌پذیر با ثبت دلیل، `crm/opex.js:757`). ردیف قالبی بماند.
- **ردیف‌های قالبیِ «تسویه‌شده» + چکِ بدون اتصال (سناریو B):** چکِ بدون اتصال را از پنل چک **⛔ ابطال** کنید (`ptfChequeVoidIssued`) — خروج چک از خزانه حذف می‌شود و ردیف‌های تسویه‌شده فقط یک‌بار می‌مانند. اگر می‌خواهید چک منبع نقدی بماند: ردیف را ابطال کنید (`void_recurring_opex` سروری) و از «ثبت قالب» همان ماه را دوباره ماده کنید (`materializeTemplateOnServer` با `restore:true`) تا ردیفِ تازهٔ «باز» جایگزین شود — این‌بار آن را تسویه نکنید.
- **ردیف‌های دستیِ ماهانه + چک بدون اتصال (سناریو A):** چک را ابطال کنید (خزانه از ردیف‌های دستی یک‌بار کم می‌کند) یا — اگر چک باید بماند — ردیف‌های دستی را حذف و ماه‌ها را با قالب/چک بازسازی کنید.
- **ردیف دستیِ «پرداخت چک اجاره ۲ماهه» کنار ردیف‌های ماهانه:** آن ردیف تجمیعی را 🗑 حذف کنید؛ چکِ ۲ماهه خودش در خزانه شمرده می‌شود.
- **سال مالی قفل‌شده:** حذف/ویرایش ردیف با گارد بسته می‌شود (`ptfFinanceAssertWritable`، `crm/finance-write-guard.js:17`) — مسیر مجاز همان «سند اصلاحی» (amendment) در گزارش سال مالی است که تغییر سال قفل‌شده را در سال جاری اعمال می‌کند (`crm/fiscal.js` — amendments).

### ۴.۲ پس از اصلاح، صحت‌سنجی
- «سود سال مالی» (تعهدی و نقدی) قبل/بعد؛ «گزارش گردش خزانه» همان بازه؛ جمع «اجاره‌بها» در `opexByCat`.
- مورد باقی‌مانده در تب کیفیت داده باید صفر شود.

---

## ۵) پاسخ به «خروجی خزانه قابل ویرایش شود؟»

- دفتر خزانه (`treasuryCrmMoves`) **کاملاً مشتق‌شده** است: هیچ رکوردی در `ptf_crm_bank_recon` یا جایی دیگر ذخیره نمی‌شود؛ آن کلید فقط در لیست‌های sync/API/بکاپ است و هیچ UI/منطقی پشتش نیست.
- بنابراین «ویرایش خروجی خزانه» امروز **وجود ندارد** و اضافه‌کردنش (۱) با رندر بعدی بازنویسی می‌شود، (۲) با merge سروری تعارض می‌گیرد، و (۳) مشکل را در «سود نقدی سال مالی» که جداگانه از opex/چک محاسبه می‌شود (`crm/fiscal.js:390`) حل نمی‌کند — یعنی حتی با این تغییر هم دوباره‌شماری فقط از یک دفتر حذف می‌شد.
- **نتیجه:** اصلاح از رکورد منبع (بخش ۴.۱) هم خزانه را درست می‌کند هم سود تعهدی/نقدی را — بدون تغییر کد. ویرایش مستقیم خروجی خزانه گزینهٔ درستی نیست و توصیه نمی‌شود.

---

## ۶) بهترین راهکار — «فعلاً بدون تغییر کد»

### الف) یک‌بار: پاکسازی گذشته
دستورالعمل ۴.۰ + ۴.۱ را ماه‌به‌ماه اجرا کنید. ترتیب پیشنهادی: اول تشخیص (کیفیت داده + گزارش خزانه)، بعد برای هر ماه «یک ردیف + یک منبع نقد» نگه دارید، آخر صحت‌سنجی.

### ب) از این به بعد: جریان «چکِ وصل‌شده» (قاعدهٔ طلایی)
1. اجاره را **فقط با قالب تکرارشونده** نگه دارید؛ هرگز ردیف دستی اجاره نزنید (اگر قالب همان ماه را «ثبت‌نشده» نشان داد، از دکمهٔ ثبت قالب همان پنل استفاده کنید، نه فرم دستی).
2. هر چک ۲ماهه را از **پنل چک صادره (نوع مالی)** ثبت کنید و در بخش «بابت کدام ماه‌های هزینه جاری؟» **دقیقاً همان ۲ ماه را تیک بزنید** (ماه‌های آیندهٔ قالب هم همان‌جا ساخته و وصل می‌شوند؛ جمع تیک‌ها باید با مبلغ چک برابر باشد — خود سیستم کنترل می‌کند، `crm/cheque-panel.js:433`).
3. **هرگز روی ردیف‌های اجارهٔ متصل به چک «تسویه» نزنید** — اصلاً دکمه‌ای نمی‌بینید؛ این نشانهٔ درستی است.
4. اگر چکی را بدون اتصال ثبت کردید، قبل از سررسید آن را ابطال و با اتصال درست دوباره ثبت کنید.
5. هزینهٔ اجاره را در تنخواه ثبت نکنید (خروج تنخواهِ هم‌مبلغ دوباره کم می‌شود).

### ج) حین کار: پایش ماهانه
هر ماه اول، تب «کیفیت دادهٔ مالی» و گزارش گردش خزانهٔ ماه قبل را یک نگاه بیندازید؛ هر مورد «مشکوک به دوباره‌شماری» را همان‌جا تعیین‌تکلیف کنید.

### د) بعداً (فقط پس از تصمیم شما — این سند هیچ کدی عوض نکرد)
شکاف‌های باقی‌مانده که کد امروز نمی‌بندد و در آینده با تستر قفل قابل افزودن‌اند:
1. گارد تسویه در برابر «چکِ بدون اتصالِ هم‌مبلغ» (امروز فقط خواهر opex و تنخواه را می‌بیند)؛
2. امکان اتصالِ بعدیِ چکِ ثبت‌شده به ماه‌های هزینه (ویرایش چک امروز فیلد اتصال ندارد — `crm/cheque-panel.js:747`)؛
3. آشکارساز «چک ۲ماهه ↔ دو ردیف ماهانهٔ بدون اتصال» در تب کیفیت داده.

---

## ۷) پیوست (۲۰۲۶-۰۹-۲۳ — v34.39.24): گپ UI اتصال بسته شد

پس از این ارزیابی مشخص شد «ویرایش چک» هیچ راهی برای اتصال به ماه‌های هزینه نداشت و فرم ثبت هم بخش اتصال را در سه حالت بی‌توضیح پنهان می‌کرد (ذی‌نفع تامین‌کننده / نوع ضمانت / نبود ردیف بدون چک)؛ ثبت مجدد چک با همان صیادی هم به قفل دائمی صیاد می‌خورد. در **v34.39.24 (CHQ-OPEX-EDIT-LINK)** این گپ بسته شد: بخش «بابت کدام ماه‌های هزینه جاری؟» به مودال ویرایش چک صادره اضافه شد (اتصال/جدا کردن با تأیید انسانی، بدون ربودن ردیف چک دیگر) و حالت‌های پنهان فرم ثبت به راهنمای درجا تبدیل شدند. جزئیات: `RELEASE-NOTES-v34.39.24.md` و tester679 در گیت CI. بنابراین اصلاح چک‌های گذشتهٔ بدون اتصال حالا عمدتاً با «ویرایش همان چک و تیک‌زدن ماه‌ها» انجام می‌شود (بخش ۴.۱ این سند برای حالت‌های خاص معتبر می‌ماند).

**وضعیت اولیه:** فقط ارزیابی؛ کد تغییر نکرده بود. **به‌روزرسانی v34.39.24:** کد تغییر کرد (فقط گپ UI اتصال؛ هیچ جمع/فرمولی عوض نشد).
