# Release Notes — v31.7.39

**تاریخ:** ۱۴۰۵/۰۴/۳۰ (2026-07-21)  
**نسخه:** `v31.7.39`  
**نوع:** 🔴 Hotfix UX — Fix My Day dismiss click + Minimal Icon Policy

---

## 🎯 مسئله گزارش‌شده

پس از v31.7.38 دکمه `×` کنار آیتم‌های «روز من» دیده می‌شد، اما در مرورگر واقعی حذف/پنهان‌سازی انجام نمی‌شد. همچنین تاکید شد که آیکون‌های اضافه‌شده در سایت/CRM باید مینیمال باشند؛ در بخش مسیر سریع سایت از emoji استفاده شده بود که با سیاست بصری مطلوب هم‌راستا نبود.

---

## 🔍 RCA

1. دکمه حذف «روز من» داخل یک ردیف clickable قرار داشت که خودش `onclick` برای رفتن به پنل مقصد داشت.
2. inline handler قبلی مستقیماً به `event.stopPropagation()` متکی بود. در برخی شرایط مرورگر/DOM، این الگو قابل اتکا نیست و parent click می‌تواند قبل/به‌جای dismiss رفتار کند یا خطا بدهد.
3. بعد از ذخیره dismiss، refresh مستقیم خود box هم مستقل و قطعی نبود و بیشتر به navigation داشبورد متکی بود.
4. در بخش Home Journey، آیکون‌های emoji مثل 🧾 / ⚙️ / 🔎 / 🏭 استفاده شده بود که مینیمال نبودند.

---

## ✅ اصلاح انجام‌شده

### 1) دکمه حذف «روز من» robust شد

در `crm/myday.js` اضافه شد:

```js
ptfMyDayDismissClick(event, fp)
mdRefresh()
```

اکنون:

- click روی `×` propagation را به‌صورت کنترل‌شده متوقف می‌کند.
- dismiss در `ptf_crm_settings.mydayDismissed` ذخیره می‌شود.
- همان box «روز من» بلافاصله rerender می‌شود و آیتم ناپدید می‌شود.
- دیگر صرفاً به `goPanelByName('dash')` وابسته نیست.

### 2) آیکون‌های Home Journey مینیمال شدند

در `index.html` emojiهای مسیر سریع حذف شدند و با badgeهای مینیمال عددی جایگزین شدند:

```text
01 خریدار / EPC هستم
02 مهندس پروژه هستم
03 پیگیر استعلام هستم
04 تامین‌کننده هستم
```

### 3) آیکون‌های اضافه‌شده در Tools/MyDay مینیمال‌تر شدند

- دکمه PDF ابزارها از `گزارش PDF کامل 🔒` به `گزارش PDF کامل (فعال‌سازی)` تغییر کرد.
- عنوان paywall دیگر از emoji lock استفاده نمی‌کند.
- پیام فعال‌سازی از علامت‌های emoji غیرمینیمال پاک شد.
- دکمه «پاکسازی تکراری‌ها» در MyDay بدون emoji نمایش داده می‌شود.

---

## 📁 فایل‌های تغییر یافته

```text
crm/myday.js
index.html
tools/tools-ui.js
_tools/uat/tester213-tools-free-pdf-paywall.js
_tools/uat/tester215-duplicate-growth-myday-dismiss.js
crm/index.html
crm/sw.js
crm/clear-cache.html
PTF-MASTER-HANDOVER.md
RELEASE-NOTES-v31.7.39.md
REGRESSION-REPORT-v31.7.39.md
```

---

## 🧪 تست

تسترهای مرتبط:

```text
tester215-duplicate-growth-myday-dismiss.js
tester216-minimal-icons-policy.js
tester213-tools-free-pdf-paywall.js
tester210-home-journey-cro.js
```

گیت کامل در `REGRESSION-REPORT-v31.7.39.md` ثبت شده است.

---

## 📋 راستی‌آزمایی کارفرما

1. در CRM، داشبورد / «روز من» را باز کنید.
2. روی `×` کنار یک آیتم کلیک کنید.
3. انتظار صحیح: همان آیتم فوراً از لیست حذف/پنهان شود و سند اصلی حذف نشود.
4. روی «نمایش حذف‌شده‌ها» کلیک کنید.
5. انتظار صحیح: آیتم‌های پنهان‌شده دوباره ظاهر شوند.
6. صفحه اصلی سایت را باز کنید و بخش «مسیر سریع بر اساس نقش شما» را ببینید.
7. انتظار صحیح: دیگر emoji بزرگ وجود ندارد؛ badgeهای عددی مینیمال 01 تا 04 دیده شوند.

---

## سیاست از این نسخه به بعد

هر آیکون جدید در سایت یا CRM باید مینیمال، ساده، هم‌خوان با UI و در صورت امکان SVG خطی یا badge متنی باشد. استفاده از emojiهای بزرگ برای CTAهای اصلی ممنوع است مگر با تایید صریح.
