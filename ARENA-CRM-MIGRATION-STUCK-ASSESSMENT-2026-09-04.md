# ارزیابی «انتقال یک‌بارهٔ ناقص / بن‌بست کهربایی» — CRM

- **تاریخ:** ۲۰۲۶-۰۹-۰۴ (۱۴۰۵/۰۶/۱۳)
- **نسخهٔ بررسی‌شده:** `v34.36.0` (شاخهٔ `arena/01a06c26-pishtaj-ir`، کامیت `95177f2`)
- **دامنه:** جعبهٔ «وضعیت دستگاه» در تنظیمات ← بک‌آپ و بازگردانی، بنر مهاجرت، مسیر همگرایی فاز B، صف ارسال، تشخیص همگام‌سازی
- **نوع کار:** **فقط ارزیابی — هیچ فایل کدی تغییر نکرد.** (تنها همین سند اضافه شده است.)
- **روش:** خواندن کامل زنجیرهٔ کد (کلاینت + سرور) + **شبیه‌سازی اجرایی** با هارنس خارج از ریپو که فایل واقعی `crm/client-server.js` را روی پورتِ وفادار `api/crm.php:data_push` اجرا می‌کند (خروجی‌ها در پیوست الف).

---

## ۱) خلاصهٔ مدیریتی (یک بند)

دستگاه شما در حالت «سبزِ ناتمام» (`enabled && !synced`) گیر کرده است و این حالت **با کلیک روی «تکمیل انتقال یک‌باره» قابل خروج نیست**، چون تنها کدی که نشانگر پایان انتقال (`ptf_b_synced_<user>`) را می‌نویسد یک مسیر **همه-یا-هیچ، تک‌تلاش، بدون بازتلاشی، بدون حل تعارض و بدون ثبت خطا** است (`ptfBFinalize` در `crm/client-server.js:762-812`). کافی است **یک** کلید از ~۶۴ کلید در همان لحظه `conflict / rejected / forbidden / skipped / failed` شود — که برای `ptf_crm_opex` (کلید مالی محافظت‌شده) تقریباً **همیشه** اتفاق می‌افتد — تا کل انتقال ناتمام بماند. در مقابل، مسیری که واقعاً می‌تواند `opex` را همگرا کند (`ptfBFlushQueue` با «نجات تعارض محافظت‌شده»، `crm/client-server.js:700-725`) **هرگز نشانگر پایان انتقال را نمی‌نویسد**؛ بنابراین حتی موفق‌شدنِ آن هم بنر/جعبه را سبز نمی‌کند. نتیجه: پیام برای همیشه می‌ماند، «پاک‌سازی کش محلی» قفل می‌ماند، آینهٔ IndexedDB خاموش می‌ماند و دادهٔ سنگین در localStorage می‌ماند و مرورگر پر می‌شود — دقیقاً همان چیزی که گزارش کرده‌اید. «رها کردن امن» هم فقط برای `audit/notifs/avatars` تعریف شده (`crm/backup.js:712`، `crm/sync.js:244-249`) و برای `opex` عمداً هیچ درِ رهایشی در UI وجود ندارد.

**شدت:** P0 (بن‌بست کامل مهاجرت پروداکشن + فشار حافظه + فقدان هرگونه تشخیص/رهاسازی).
**ریسک داده:** دادهٔ کسب‌وکار در این وضعیت **گم نمی‌شود** (هیچ مسیر مخربی اجرا نمی‌شود)؛ اما یک کلیک ناموفق می‌تواند دستگاهِ **سالم (سبز)** را به کهربایی تنزل دهد (یافتهٔ F1) و کلیک‌های پیاپی وضعیت را **بدتر** می‌کند (یافتهٔ F2 — تعارض‌های جدید روی کلیدهای سالم).

---

## ۲) آنچه کاربر می‌بیند در برابر آنچه کد انجام می‌دهد

### ۲.۱ زنجیرهٔ یک کلیک روی «⬆️ تکمیل انتقال یک‌باره»

| # | رخداد | مرجع کد |
|---|---|---|
| 1 | جعبهٔ وضعیت دستگاه سه‌حالته رندر می‌شود؛ حالت شما `on && !synced` = «فعال — در انتظار تکمیل انتقال» | `crm/backup.js:687-706` |
| 2 | دکمه → `ptfBConfirmFlush()` | `crm/backup.js:705` |
| 3 | **اول هر دو نشانگر پاک می‌شوند** (`ptf_b_flushed_<user>` و `ptf_b_synced_<user>`) — قبل از هر کاری | `crm/client-server.js:814-817` |
| 4 | `ptfBFinalize()` → `flushRequired()` درست می‌شود → `readQueuePayload(bKeys())` یعنی **همهٔ کلیدهای موجود محلی** (نه فقط کلید کثیف) خوانده می‌شود | `crm/client-server.js:753-790` |
| 5 | دیالوگ «🌐 هم‌گرایی داده با سرور … ادامه می‌دهید؟» | `crm/client-server.js:791` |
| 6 | `ptfBPushBatch(payload)` → دسته‌های ۲۰تایی، تایم‌اوت ۲۰ ثانیه، `base = ptf_sync_krevs` | `crm/client-server.js:529-560` |
| 7 | سرور برای `ptf_crm_opex` «merge محافظت‌شده» می‌سازد و چون امضای snapshot خام مرورگر با آن یکی نیست → `conflicts[] + protectedConflicts[] + serverData{merge}` | `api/crm.php:2119-2157`, `681-747` |
| 8 | `ok = !failed && !rejected && !skipped && !forbidden && !conflicts` → **false** | `crm/client-server.js:548` |
| 9 | در `ptfBFinalize` فقط سه شاخه هست: موفق / `needLogin` / «بقیه» → **هیچ حل تعارضی، هیچ بازتلاشی، هیچ به‌روزرسانی watermark، هیچ ثبت خطایی** | `crm/client-server.js:793-811` |
| 10 | هشدار: «⚠️ فقط ۳ از ۴ کلید هم‌گرایی شد (**network**)…» و چون `markSynced()` صدا نشده، جعبه و بنر سر جایشان می‌مانند | `crm/client-server.js:810`, `crm/backup.js:693`, `crm/index.html:2261-2271` |

> متن داخل پرانتز در هشدار، **کلاس خطای سرور است نه وضعیت شبکه**. برای `conflict/rejected/forbidden/skipped` رشتهٔ `error` خالی است و کد `|| 'network'` می‌گذارد (`crm/client-server.js:810`) — یعنی کاربر به‌سمت «اینترنت» فرستاده می‌شود درحالی‌که علت، تعارض داده است (یافتهٔ F3).

### ۲.۲ چرا `opex` به‌طور خاص گیر می‌کند

`ptf_crm_opex` یکی از سه کلید مالی محافظت‌شده است (`api/crm.php:2119`). سرور برای این کلیدها:

1. اگر `base[k] < rev` سرور → `conflict` + برگرداندن merge محافظت‌شده (`api/crm.php:2121-2140`).
2. حتی اگر `base` تازه باشد، merge محافظت‌شده را می‌سازد و **امضای sha256** snapshot ارسالی را با امضای merge مقایسه می‌کند؛ اگر متفاوت بود → `conflict` (`api/crm.php:2143-2157`, `741-747`).
3. merge محافظت‌شده از ردیف‌های **سرور** شروع می‌شود و فیلدهای «سرورمالک» (`_opexRowId, serverOwnedIdentity, serverMaterialized, st=settled, chequeCd, payHow, …`) را از snapshot مرورگر **پس می‌زند** (`api/crm.php:624-680`).

پس تا وقتی مرورگر یک کپی «خام/قدیمی» از opex دارد (که تعریف دقیق دستگاهِ منتقل‌نشده است)، امضا **هرگز** یکی نمی‌شود ⇒ تعارض دائمی. خودِ کد هم این را می‌داند و در `crm/sync.js:139-142` نوشته: *«امضای آن تقریباً هیچ‌وقت با snapshot خام مرورگر یکی نیست؛ بدون اعمال این merge، push بعدی باز conflict می‌دهد (بن‌بست گزارش‌شده برای مدیر بازرگانی)»*. درمانش هم فقط در مسیر صف ساخته شده (`ptfSyncResolveProtectedConflictFromServer` با شاخهٔ VERBATIM-CONVERGENCE، `crm/sync.js:143-165`) — و `ptfBFinalize` از آن استفاده **نمی‌کند**.

نکتهٔ تکمیلی و مهم: چون watermark کلید `opex` در کلاینت برابر rev سرور است، **pull دلتا هم هرگز آن کلید را برای اصلاح نمی‌فرستد** (`api/crm.php:data_pull` فقط کلیدهایی با rev بزرگ‌تر را می‌دهد). یعنی نه push می‌پذیرد، نه pull ترمیم می‌کند ⇒ بن‌بست بسته.

---

## ۳) ریشه‌ها (RCA)

### R1 — نشانگر پایان انتقال «تک‌نویسنده» و «همه-یا-هیچ» است
`markSynced()` فقط در دو خط صدا زده می‌شود و هر دو داخل `ptfBFinalize` هستند:
- `crm/client-server.js:779` (مسیر دستگاه بدون داده — payload خالی)
- `crm/client-server.js:795` (مسیر موفقیت کامل)

و موفقیت کامل یعنی `d.ok` که در `crm/client-server.js:548` به معنای **صفر** کلید در هر پنج کلاس شکست است. هیچ مسیر جایگزینی (صف، بوت، بازیابی، ابزار تعمیر، `clear-cache.html`، `recover.html`، `force-restore.html`، `sync-diagnostics.html`) این نشانگر را نمی‌نویسد. ⇒ **یک کلید لجباز = قفل دائمی کل مهاجرت دستگاه.**

### R2 — مسیر مهاجرت، موتور حل تعارض مسیر صف را ندارد
`ptfBFlushQueue` (مسیر «تلاش مجدد ارسال» و debounce چهار ثانیه‌ای) برای تعارض‌ها نجات سه‌مرحله‌ای دارد: merge محلی، تازه‌سازی watermark، و تا ۳ نوبت flush مجدد (`crm/client-server.js:700-725`) + حذف کلیدهای `forbidden` از صف (`686-691`). `ptfBFinalize` هیچ‌کدام را ندارد؛ حتی `d.serverData` و `d.krevs` پاسخ را **دور می‌ریزد**. ⇒ دو موتور همگام‌سازی با دو رفتار ناسازگار.

### R3 — مسیر موفقِ فاز B هیچ‌گاه watermark را جلو نمی‌برد
`bSaveRevsFromMeta` فقط در دو جا صدا زده می‌شود: بعد از pull موفق (`crm/client-server.js:341`) و **فقط داخل شاخهٔ نجات تعارض** (`crm/client-server.js:715`). در نتیجه وقتی `opex` با موفقیت push می‌شود، rev سرور بالا می‌رود ولی `ptf_sync_krevs` کلاینت عقب می‌ماند ⇒ push بعدی (مثلاً کلیک بعدی روی «تکمیل انتقال») با `base` کهنه می‌رود ⇒ **تعارض کهنه‌نویسی برای همان کلیدی که یک لحظه پیش موفق شده بود**. مسیر قدیمی sync.js این کار را درست انجام می‌دهد (`applyKrevs(d.krevs)` در `crm/sync.js:1053`)؛ فاز B آن را از دست داده است.

### R4 — «رها سازی» وجود ندارد، و راهنمای UI هم بن‌بست را باز نمی‌کند
- «رها کردن امن» فقط برای `ptf_crm_audit / notifs / avatars` (مرجع سروری union) — `crm/backup.js:712`, `crm/sync.js:244-249`. برای opex عمداً لینک رهایش نیست (درست از نظر سیاست داده).
- راهنمای جعبه می‌گوید «⬆ تلاش مجدد ارسال» بزنید (`crm/backup.js:722`)؛ آن دکمه (`crm/sync.js:528`) واقعاً opex را همگرا می‌کند **اما نشانگر انتقال را نمی‌نویسد** ⇒ بنر و جعبه همان‌جا می‌مانند و کاربر نتیجهٔ کار درست را نمی‌بیند.
- شکست مسیر مهاجرت **هیچ جا ثبت نمی‌شود**: `client-server.js` هیچ فراخوانی `noteSyncError` ندارد ⇒ «آخرین خطا» در تشخیص همگام‌سازی هرگز علت واقعی را نشان نمی‌دهد (دقیقاً همان چیزی که در گزارش شما دیده می‌شود).

---

## ۴) کلاس‌های شکستی که هرکدام به‌تنهایی انتقال را برای همیشه قفل می‌کنند

| # | کلاس | کجا تولید می‌شود | در مسیر صف (`ptfBFlushQueue`) درمان دارد؟ | در مسیر مهاجرت (`ptfBFinalize`) |
|---|---|---|---|---|
| 1 | `conflicts + protectedConflicts` روی `opex/sharetx/shareholders` | `api/crm.php:2119-2157` | ✅ نجات merge + ۳ نوبت بازتلاش | ❌ بن‌بست دائمی |
| 2 | `conflicts` کهنه‌نویسی (دستگاه دیگر نوشته / R3) | `api/crm.php:2121-2140` | ✅ | ❌ |
| 3 | `rejected` — سپر داده‌صفر (محلی `[]`، سرور ناخالی) | `api/crm.php:2176-2185` | ❌ (dirty می‌ماند؛ نوار زرد دائمی) | ❌ |
| 4 | `rejected + conflicts` — یکپارچگی offers (ردیف تکراری/ثبت‌نشده) | `api/crm.php:2100-2115` | بخشی (merge عمومی) | ❌ |
| 5 | `forbidden` — کلید خارج از allowlist نقش (مثلاً `opex` برای sales/buyer/collector) | `api/crm.php:358-375`, `2084` | ✅ حذف از صف (v34.8.36) | ❌ تا وقتی آن کلید محلی وجود دارد، **هرگز** کامل نمی‌شود |
| 6 | `skipped` — مقدار >۸MB یا غیررشته | `api/crm.php:2083-2085` | ❌ | ❌ |
| 7 | `failed` — تایم‌اوت ۲۰ث/دستهٔ ۲۰ کلیدی، `ساختار نامعتبر` (post_max_size هاست)، `حجم بیش از حد` (>40MB)، `sync_lock_unavailable`، `pending_sales_transaction_recovery` | `crm/client-server.js:357-398` (تایم‌اوت ۲۰ث در `369`)، `585`؛ `api/crm.php:2028, 2055-2078` | بازتلاشی خودکار | ❌ بدون backoff/بازتلاش |

هر یک از این هفت کلاس به‌تنهایی برای ماندگاری ابدی پیام کافی است. در شبیه‌سازی، کلاس‌های ۱، ۳، ۵ و ترکیب ۱+۲ بازتولید شدند (پیوست الف).

---

## ۵) یافته‌های تشدیدکننده (نقص‌های جانبیِ همان ناحیه)

| ID | یافته | شواهد | شدت |
|---|---|---|---|
| **F1** | `ptfBConfirmFlush` نشانگرها را **پیش از تلاش** پاک می‌کند و در شکست هرگز بازنمی‌گرداند ⇒ دستگاهِ **سبز** با یک کلیک ناموفق به کهربایی تنزل می‌کند، `ptfBMirrorActive()` خاموش می‌شود، تخلیه به IndexedDB متوقف می‌شود و pullهای بعدی دوباره در localStorage می‌نویسند (همان «پمپ snapshot» که v34.9.1 درمانش کرده بود) | `crm/client-server.js:814-817`, `87`, `1001-1010`؛ شبیه‌سازی S3 | **P0** |
| **F2** | کلیک‌های پیاپی وضعیت را بدتر می‌کند: تلاش اول تعدادی کلید را ذخیره می‌کند، watermark جلو نمی‌رود (R3)، تلاش بعدی همان کلیدها را `conflict` می‌گیرد ⇒ «۳ از ۴» به «۰ از ۴» می‌رسد | شبیه‌سازی S1 (کلیک ۱→۲→۳) | **P1** |
| **F3** | فقدان کامل تشخیص: علت واقعی نه در «آخرین خطا»، نه در audit، نه در `lastPushResult` ثبت می‌شود؛ متن هشدار هم `(network)` می‌گوید و **نام کلیدهای ناموفق را نمی‌دهد** | `crm/client-server.js:806-811`؛ نبودِ `noteSyncError` در کل فایل | **P0** (بدون این، پشتیبانی نمی‌تواند تیکت را تشخیص دهد) |
| **F4** | «آخرین خطا: دریافت (pull) — ok» که در گزارش شماست در واقع **نتیجهٔ موفقِ تست اتصال** است که به‌عنوان خطا ذخیره شده و چون نگاشت نمایش فقط `push` را می‌شناسد، هر scope دیگری «دریافت (pull)» چاپ می‌شود | `crm/sync.js:563` + `crm/sync.js:537` | P2 (گمراه‌کننده) |
| **F5** | «🔵 صف آفلاین فاز B خالی است» در تشخیص همگام‌سازی **همیشه در فاز B غلط** است: صف از v34.8.23 در IndexedDB (`q:ptf_b_queue`) است و کپی localStorage حذف می‌شود، ولی `diagCounts` فقط localStorage را می‌خواند. جعبهٔ وضعیت دستگاه (`ptfBStatus().queue`) درست می‌گوید ⇒ دو عدد متناقض در یک صفحه | `crm/sync.js:512-520` در برابر `crm/client-server.js:466-500`, `1034-1046` | **P1** |
| **F6** | `bKeys()` عملاً همیشه از فهرست سخت‌کدشدهٔ ۶۴ کلیدی استفاده می‌کند چون `window._ptfSyncKeys` **هیچ‌جا تعریف نشده**؛ آن فهرست `ptf_crm_fin_events` و `ptf_crm_personal_cheques` را ندارد (سرور و `SYNC_KEYS` هر دو ۶۶ کلید دارند). پیامد: این دو کلید در فاز B نه در صف می‌روند (`ptfBEnqueueKeys` بی‌صدا حذفشان می‌کند)، نه در انتقال یک‌باره، نه در تخلیه به IDB، نه در پاک‌سازی کش ⇒ ریسک «ثبت شد ولی هرگز ارسال نشد» برای ژورنال مالی و چک شخصی | `crm/client-server.js:197-215`, `511-518`؛ `api/crm.php:355-357`؛ `crm/sync.js:13-21`؛ شبیه‌سازی S6 | **P0** (بی‌صدا) |
| **F7** | سن کلید کثیف با هر نوبت نجات/تلاش **بازنشانی** می‌شود (`state.dirty[k] = Date.now()`) ⇒ کاربر همیشه «از چند لحظه پیش» می‌بیند، حتی اگر کلید سه روز گیر کرده باشد؛ دقیقاً همان چیزی که باعث شد این بن‌بست دیر دیده شود | `crm/sync.js:133`, `157`, `250-259`؛ متن جعبه `crm/backup.js:714` | P2 |
| **F8** | مسیر موفقِ مهاجرت صف آفلاین را پاک نمی‌کند (فقط `dirty` را ACK می‌کند) و بعد `location.reload()` می‌زند؛ صف در IDB پایدار است ⇒ بلافاصله پس از «✅ هم‌گرایی انجام شد»، «پاک‌سازی کش محلی» ممکن است با «N تغییر هنوز به سرور نرسیده» رد شود تا flush بعدی بوت | `crm/client-server.js:793-804` در برابر `880-887` | P2 |
| **F9** | راهنمای منسوخ در هر سه پیام مسیر مهاجرت: «تنظیمات → **هم‌گرایی داده**» — این منو از v34.8.51 به «وضعیت دستگاه» تغییر نام داده | `crm/client-server.js:791, 806, 808, 810` در برابر `crm/backup.js:681-706` | P2 |
| **F10** | `hasLocalBusinessPayload` فقط localStorage را می‌بیند نه آینهٔ IDB ⇒ بنر مهاجرت می‌تواند غیب شود درحالی‌که جعبه هنوز کهربایی است (ناهمسانی بنر/جعبه) | `crm/client-server.js:822-835`؛ `crm/index.html:2261` | P2 |
| **F11** | چرخهٔ معیوب «حافظهٔ پر»: تا `synced` نباشد آینه/تخلیه خاموش است، تا آینه خاموش است هر pull در localStorage می‌نویسد، تا حافظه پر است نوشتن‌ها شکست می‌خورد، و `sharedPull` حتی وقتی `localSet` شکست خورده **watermark را جلو می‌برد** ⇒ واگرایی دائمی (کلید محلی کهنه/خالی ولی «به‌روز» فرض می‌شود → `rejected`/`conflict` همیشگی) | `crm/client-server.js:337-341`, `87`, `1001-1010`؛ `crm/storage-quota.js:717-752` | **P1** |
| **F12** | پوشش تست: تسترهای داخل گیت (551/552/553/554) فقط **رشته/الگوی منبع** را چک می‌کنند («دکمهٔ «تکمیل انتقال یک‌باره» وجود دارد») و سبز می‌شوند؛ تنها تستر رفتاری فاز B (`tester298-phase-b.js`) امروز **۹ PASS / ۳ FAIL** است و در گیت نیست ⇒ هیچ سدی این بن‌بست را نمی‌گرفت | `_tools/uat/run-ci-gate.js:213-216`؛ اجرای `tester553` (۳۱/۰) و `tester298` (۹/۳) در همین نشست | **P1** (فرآیندی) |
| **F13** | نکات سلامت کد در همان ناحیه: `emergencyCompact` دو بار در یک scope تعریف شده (`crm/storage-quota.js:210` مرده، `599` فعال)؛ صفحهٔ مستقل `crm/sync-diagnostics.html` کهنه است — «Force Pull» فقط `runTest()` را صدا می‌کند (pull واقعی/نوشتن ندارد) و «Force Push» فقط ۵ کلید غیرمالی را می‌فرستد | `crm/storage-quota.js:210,599`؛ `crm/sync-diagnostics.html:208-235` | P2 |

---

## ۶) راه‌حل فوری روی دستگاه گیرکرده — **بدون تغییر کد** (آزمایش‌شده در شبیه‌سازی)

ترتیب زیر در هارنس روی سناریوی S2c نتیجهٔ قطعی داد (`synced=1`، آینهٔ IDB فعال، «پاک‌سازی کش محلی» باز شد):

1. **ابتدا کلید کثیف را همگرا کنید، بعد انتقال را کامل کنید.**
   تنظیمات ← تشخیص همگام‌سازی ← **«⬆ تلاش مجدد ارسال»**. این دکمه از موتورِ دارای نجاتِ تعارض (`ptfBFlushQueue`) عبور می‌کند و `opex` را با نسخهٔ کانونیک سرور همگرا می‌کند.
   - ملاک موفقیت: نوار زرد پایین صفحه خاموش شود و «⏳ در انتظار ارسال به سرور» از جعبهٔ وضعیت دستگاه غیب شود.
   - اگر بعد از ۳ نوبت هنوز زرد است، یک بار صفحه را رفرش کنید و دوباره همان دکمه را بزنید.
2. **بگذارید یک pull دلتا انجام شود (حیاتی).** حداقل ۲۰–۳۰ ثانیه تب **در پیش‌زمینه** باز بماند (یا یک بار صفحه را رفرش کنید). دلیل: به‌خاطر R3، بعد از push موفق، watermark کلاینت عقب است؛ بدون یک pull، کلیک بعدی روی «تکمیل انتقال» با `base` کهنه می‌رود و **همان کلیدِ نجات‌یافته دوباره `conflict` می‌شود** (در شبیه‌سازی: بدون pull → شکست؛ با pull → موفق).
3. **حالا** تنظیمات ← بک‌آپ و بازگردانی ← وضعیت دستگاه ← **«⬆️ تکمیل انتقال یک‌باره»** → دیالوگ را تأیید کنید.
   انتظار: «✅ هم‌گرایی انجام شد…» و رفرش خودکار؛ سپس جعبه سبز («🖥 وضعیت دستگاه: سرور-محور فعال») و دکمهٔ **«🗑 پاک‌سازی کش محلی»** ظاهر می‌شود (با تایپ «پاک»).
4. **متن داخل پرانتزِ هشدار شکست را یادداشت کنید** (مثلاً `network`, `ساختار نامعتبر`, `حجم بیش از حد`, `sync_lock_unavailable`, `timeout`, `pending_sales_transaction_recovery`, `missing_saved_keys`). این تنها سرنخ تشخیصی موجود است و کلاس شکست را از جدول بخش ۴ مشخص می‌کند.
5. **دکمه را پشت‌سرهم نکوبید.** هر کلیک ناموفق، کلیدهای سالمِ ذخیره‌شده در کلیک قبلی را هم به تعارض می‌اندازد (F2). بین تلاش‌ها یک رفرش + ۳۰ ثانیه صبر.

### تشخیص سریع در کنسول مرورگر (فقط‌خواندنی، بدون ریسک)

```js
ptfBStatus()                       // {enabled, synced, queue, localPayload}
ptfSyncDiagnosticsSnapshot()       // lastPushResult → conflicts / rejected / forbidden / skipped / savedKeys
ptfSyncDirtyInfo()                 // کلیدهای کثیف + سن
Object.keys(localStorage).filter(k => k.indexOf('ptf_b_') === 0)   // نشانگرها
JSON.parse(localStorage.getItem('ptf_sync_krevs'))                 // watermark هر کلید
```
تفسیر: `conflicts=[ptf_crm_opex]` ⇒ کلاس ۱/۲ (همین راهنمای بالا)؛ `forbidden=[…]` ⇒ کلاس ۵ — انتقال باید با نقشی انجام شود که آن کلید در allowlist اوست (`admin/chairman/ceo/commercial` برای همهٔ کلیدها؛ `accountant` شامل `opex`؛ `sales/buyer/collector` بدون کلیدهای مالی — `api/crm.php:358-375`)؛ `rejected=[…]` ⇒ کلاس ۳ (محلی خالی، سرور ناخالی) — امروز بدون تغییر کد درمان قطعی ندارد.

### چه کارهایی **نکنید**
- ❌ پاک کردن دادهٔ مرورگر / `crm/clear-cache.html` / «پاک‌سازی امن فوری» پیاپی: مشکل را حل نمی‌کند و تنها کپی محلیِ دادهٔ همگرا‌نشده را در معرض خطر می‌گذارد.
- ❌ ست‌کردن دستی `localStorage.setItem('ptf_b_synced_<user>','1')` در کنسول: قفل UI را باز می‌کند ولی «پاک‌سازی کش محلی» را روی داده‌ای که **هرگز به سرور نرسیده** مجاز می‌کند ⇒ ریسک واقعی از دست رفتن رکورد. (تنها اگر مطمئنید همین دستگاه قبلاً با کاربر دیگری همگرا شده، خودِ `ptfBClearLocalCache` این حالت را می‌شناسد — `crm/client-server.js:866-878`.)
- ❌ کلیک روی «تکمیل انتقال یک‌باره» وقتی نوار زردِ کلید کثیف هنوز روشن است (تقریباً حتماً شکست می‌خورد و F1/F2 را فعال می‌کند).

---

## ۷) برنامهٔ اصلاح پیشنهادی (فقط پیشنهاد — پیاده‌سازی نشد)

**P0-1 — یکسان‌سازی دو موتور (ریشهٔ R1/R2).**
مهاجرت یک‌باره را روی همان موتور صف سوار کنید: `ptfBFinalize` → `ptfBEnqueueKeys(bKeys())` → `ptfBFlushQueue` (با نجات تعارض، حذف forbidden، watermark، بازتلاش) و سپس `markSynced()` وقتی «هیچ کلید قابل‌ارسالی در صف/dirty نماند» — نه «همهٔ کلیدها در یک درخواست ACK شدند». معیار پذیرش: دستگاه با `opex` واگرا در ≤۳ نوبت سبز شود.

**P0-2 — ترمیم شکست‌ناپذیری نشانگرها (ریشهٔ F1).**
`ptfBConfirmFlush` نشانگرها را **پس از** موفقیت بنویسد/پاک کند؛ در شکست، وضعیت قبلی (از جمله `synced` موجود و آینهٔ IDB) دست‌نخورده بماند. معیار پذیرش: دستگاه سبز با یک کلیک ناموفق، سبز بماند (`ptfBMirrorActive()` بدون تغییر).

**P0-3 — ثبت علت واقعی (ریشهٔ F3/F4).**
در مسیر مهاجرت: `noteSyncError('migration', …, {conflicts, rejected, forbidden, skipped, failed})` + `audit` + نمایش **نام کلیدها** و کلاس هرکدام در هشدار؛ حذف برچسب گمراه‌کنندهٔ `(network)`؛ اصلاح نگاشت «آخرین خطا» و توقف ذخیرهٔ نتیجهٔ موفقِ تست اتصال به‌عنوان خطا.

**P1-1 — R3: جلو بردن watermark پس از هر push موفقِ فاز B** (`bSaveRevsFromMeta(result.krevs, result.rev)` در `ptfBFlushQueue`، مشابه `applyKrevs` در `crm/sync.js:1053`) ⇒ حذف تعارض‌های خودساخته و حذف re-download بی‌مورد.

**P1-2 — F5: خواندن صف از منبع درست** (`ptfBPendingKeys()` در `diagCounts`) + نمایش یک عدد واحد در کل صفحه.

**P1-3 — F6: حذف فهرست موازی.** `window._ptfSyncKeys = SYNC_KEYS` از sync.js (یا بهتر: `window.ptfBusinessKeys()` از `crm/key-registry.js`) به‌عنوان تنها منبع `bKeys()`؛ افزودن قاعدهٔ arch-guard که «هیچ کلیدی در `SYNC_KEYS`/`sync_all_keys()` نباشد که در `bKeys()` غایب باشد». (خودِ `crm/key-registry.js:1-16` همین تعهد را اعلام کرده ولی تستر ۵۲۰ فقط پوشش رجیستری را چک می‌کند، نه `bKeysFallback` را.)

**P1-4 — F11: واگرایی پایدار.** اگر `localSet` در `sharedPull` شکست خورد، watermark همان کلید جلو نرود + کلید «نیازمند ترمیم» علامت بخورد؛ و مسیر «همگرایی روی حافظهٔ پر» (تخلیهٔ اضطراری به IDB **پیش از** همگرایی، بدون نیاز به `synced`).

**P2 — رهاسازی کنترل‌شدهٔ کلید کسب‌وکار.** یک درِ «انتقالِ بدون این کلید» با تأیید صریح + audit + ثبت اینکه آن کلید عمداً از همگرایی کنار گذاشته شد (نه drop بی‌صدا)؛ و اصلاح متن‌های منسوخ (F9) + سنِ «اولین کثیف‌شدن» به‌جای آخرین تلاش (F7) + پوشش تست **رفتاری** (هارنس پیوست الف می‌تواند به `tester5xx-migration-deadlock.js` تبدیل و به گیت اضافه شود).

---

## پیوست الف — روش و خروجی شبیه‌سازی (شواهد اجرایی)

هارنس خارج از ریپو (`/tmp/mig/{server.js,run.js,scenarios.js,s2c.js}` در همین نشست) سه کار می‌کند:
1. **فایل واقعی** `crm/client-server.js` را در یک VM با `localStorage/IDB/fetch/alert/confirm` ماک اجرا می‌کند (هیچ کپی/تغییری در ریپو نیست).
2. یک ماک از `api/crm.php:data_push` می‌سازد که **پورت وفادار** این توابع است: `sync_all_keys` (۶۶ کلید)، `sync_allowed_keys_for_role`، `sync_merge_protected_finance_snapshot`، `sync_merge_server_owned_recurring_row`، `sync_finance_snapshot_signature`، سپر داده‌صفر، تعارض `base`.
3. سناریوها را با دادهٔ واقع‌گرایانه اجرا می‌کند: سرور ۲ ردیف opex (یکی تسویه‌شده/سرورمالک)، کلاینت ۳ ردیف خام (همان ردیف‌ها بدون فیلدهای سرورمالک + یک ردیف محلی).

**S1 — سه کلیک روی «تکمیل انتقال یک‌باره» (نقش commercial):**
```
before: ptfBStatus = {"enabled":true,"synced":false,"queue":0,"localPayload":true}  mirrorActive = false
click #1: saved=[customers,offers,invoices] conflicts=[opex] protectedConflicts=[opex]
          markers={"flushed":null,"synced":null}
          alert: "⚠️ فقط 3 از 4 کلید هم‌گرایی شد (network). …"
click #2: saved=[customers,offers,invoices] conflicts=[opex]      markers بدون تغییر
click #3: saved=[] conflicts=[customers,offers,invoices,opex]     ← تشدید (F2)
RESULT: synced marker after 3 clicks = null
clicking «پاک‌سازی کش محلی» -> "⚠️ انتقال یک‌بارهٔ داده‌ها هنوز کامل نشده است."
```

**S2 — «تلاش مجدد ارسال» opex را همگرا می‌کند ولی نشانگر را نمی‌نویسد:**
```
queue before = ["ptf_crm_opex"]
  push#1: saved=[] conflicts=[ptf_crm_opex] protected=[ptf_crm_opex]
  push#2: saved=[ptf_crm_opex] conflicts=[]          ← نجات تعارض کار کرد
after: dirty=[] queue=[]  local opex == server canonical? true
markers = {"flushed":null,"synced":null}   mirrorActive = false
=> banner/box condition (enabled && !synced) still true? true
=> «پاک‌سازی کش محلی» still blocked? ⚠️ انتقال یک‌بارهٔ داده‌ها هنوز کامل نشده است.
S2b (کلیک بلافاصله پس از همگرایی): saved=[customers,offers,invoices] conflicts=[opex]  ← R3 (base کهنه)
```

**S2c — ترتیب درستِ راهنمای بخش ۶:**
```
step1: server opex rev=13 / client krevs opex=12   => watermark NOT advanced by the successful push
step2 (بدون pull): saved=[customers,offers,invoices] conflicts=[opex]  markers={"synced":null}
step3 (پس از pull دلتا): krevs → opex:13، بقیه:14
       saved=[customers,offers,invoices,opex] conflicts=[] rejected=[]
       markers = {"flushed":"1","synced":"1"}  ptfBStatus={"enabled":true,"synced":true,...}  mirrorActive=true
       alert = "✅ هم‌گرایی انجام شد."   logs = ["location.reload()"]
```

**S3 — تنزل دستگاهِ سبز (F1):** نقش `sales`، نشانگر `synced=1` موجود، کلیک روی دکمه:
```
before: {"enabled":true,"synced":true} mirrorActive=true
  push: saved=[customers,offers] forbidden=[invoices,opex]
after:  {"enabled":true,"synced":false} markers={"flushed":null,"synced":null} mirrorActive=false
```

**S4 — نقش بدون کلید مالی (کلاس ۵):** حتی پس از drop شدن forbidden از صف، کلیک مهاجرت دوباره `forbidden=[invoices,opex]` می‌گیرد ⇒ «همیشه کهربایی».

**S5 — سپر داده‌صفر (کلاس ۳):** `invoices=[]` محلی در برابر سرور ناخالی ⇒ `rejected` در هر دو مسیر؛ dirty برای همیشه می‌ماند («هیچ مسیر نجاتی برای rejected وجود ندارد»).

**S6 — فهرست کلیدهای موازی (F6):**
```
window._ptfSyncKeys defined? undefined
ptfBEnqueueKeys([fin_events, personal_cheques, opex]) -> true
ptfBPendingKeys() = ["ptf_crm_opex"]        ← دو کلید بی‌صدا حذف شدند
migration payload = 4 keys (بدون fin_events/personal_cheques)؛ آن‌ها در localStorage می‌مانند
after «پاک‌سازی کش محلی» هم زنده‌اند: fin_events=true personal_cheques=true
```

**اجرای تسترهای موجود (بدون تغییر):**
```
_tools/uat/tester520-v34.8.34-thin-client-t0t1.js : PASS 32 | FAIL 0
_tools/uat/tester553-v34.9.1-trap-snap-hotfix.js  : 31 PASS / 0 FAIL
_tools/uat/tester298-phase-b.js                   : 9 PASS / 3 FAIL  (در گیت CI نیست)
```

---

## پیوست ب — فهرست مرجع‌های کد

- `crm/backup.js:681-724` جعبهٔ «وضعیت دستگاه» (سه حالت)، `705` دکمهٔ انتقال، `709-723` فهرست کلیدهای در انتظار + `SAFE_DROP` + راهنمای «تلاش مجدد ارسال»
- `crm/index.html:2245-2281` بنر مهاجرت (v34.9.1)، `2261` شرط غیب‌شدن، `2268-2271` متن/دکمه
- `crm/client-server.js:53-63` کلیدها/نشانگرها، `87` گارد آینه، `197-215` `bKeys/bKeysFallback`، `240-265` و `337-341` watermark، `470-518` صف IDB و `ptfBEnqueueKeys`، `529-560` `ptfBPushBatch` (معیار `ok` در `548`)، `657-745` `ptfBFlushQueue` (نجات تعارض `700-725`، forbidden-drop `686-691`)، `753-817` `flushRequired/ptfBFinalize/ptfBConfirmFlush`، `822-835` `hasLocalBusinessPayload`، `860-905` گاردهای پاک‌سازی کش، `1001-1046` تخلیه/فعال‌سازی/`ptfBStatus`
- `crm/sync.js:130-165` حل‌کننده‌های تعارض (محافظت‌شده `143-165`)، `218-275` ACK/dirty/رهاسازی‌ها، `475-495` و `537` و `563` ثبت و نمایش «آخرین خطا»، `512-520` `diagCounts`، `528` دکمهٔ تلاش مجدد، `941-968` `pushViaPhaseB`، `969-1140` `pushDirty` (`applyKrevs` در `1053`)، `1141-1155` `ptfSyncFlushNow`، `1325-1350` SNAP-PUMP
- `api/crm.php:355-375` allowlist کلیدها/نقش‌ها، `540` یکپارچگی offers، `593-600` و `624-680` ادغام سرورمالک، `681-747` merge محافظت‌شده + امضا، `2021-2237` `data_push` (سقف‌ها `2028`، قفل `2055-2078`، offers `2100-2115`، تعارض/محافظت‌شده `2119-2157`، سپر داده‌صفر `2176-2185`، پاسخ `2235`)
- `crm/storage-quota.js:717-752` `safeSetItem`، `210` و `599` تعریف دوبارهٔ `emergencyCompact`
- `crm/key-registry.js:1-30` تعهد «پایان سه نسخهٔ موازی کلیدها»
- `_tools/uat/run-ci-gate.js:176,213-216` ترکیب گیت؛ `_tools/uat/tester298-phase-b.js` تنها تستر رفتاری فاز B

---

## پیوست ج — ران‌بوک بازیابی «۴۸ از ۴۹ کلید» بدون تغییر کد (۲۰۲۶-۰۹-۰۴)

زمینهٔ گزارش دوم کاربر: هشدار «فقط **۴۸ از ۴۹** کلید هم‌گرایی شد» + زدن «⬆ تلاش مجدد ارسال» ⇒ توست «هنوز تغییرات ارسال‌نشده در صف است؛ نوار وضعیت را ببینید». نقش کاربر: کامل (admin/chairman/ceo/commercial) ⇒ کلاس `forbidden` منتفی است.

**تفسیر:** ۴۸ کلید **هم‌اکنون روی سرور ذخیره شده‌اند** (`savedKeys`)؛ داده از دست نرفته. معیار موفقیتِ `crm/client-server.js:548` «صفر کلید ناموفق» است، پس همان **یک** کلید، `markSynced()` را برای همیشه قفل می‌کند. توست «هنوز تغییرات ارسال‌نشده…» هم فقط معنای `ok=false` دارد (`crm/sync.js:528`)، نه قطعی شبکه. چون توستِ «سبزِ دروغین» دیده نمی‌شود، کلیدِ گیرکرده حتماً داخل `bKeys()` است و واقعاً push می‌شود (کلاس F6 نیست).

### گام ۰ — آماده‌سازی
همهٔ تب‌های دیگر CRM بسته شوند (تب باز = نوشتن همزمان = تعارض/hold)؛ Chrome/Edge دسکتاپ؛ اینترنت پایدار.

### گام ۱ — شناسایی با یک دستور فقط‌خواندنی (کنسول)
```js
(function(){
  var d=(window.ptfSyncDirtyInfo&&ptfSyncDirtyInfo())||[];
  if(!d.length) return 'هیچ کلید گیرکرده‌ای در صف نیست';
  return d.map(function(x){
    var k=x.k, v=localStorage.getItem(k), n='?', kb=Math.round((v||'').length/1024);
    try{ var p=JSON.parse(v||'null'); n = Array.isArray(p)? p.length : (p&&typeof p==='object'? 'object':'empty'); }catch(e){ n='parse-error'; }
    if(v===null) n='missing';
    return k+' | ردیف محلی='+n+' | اندازه='+kb+'KB | سن='+x.ageSec+'s';
  }).join('\n');
})()
```
خروجی سه عدد می‌دهد: نام کلید، تعداد ردیف محلی، اندازهٔ KB. مسیر بعدی فقط به این دو عدد بستگی دارد.

### گام ۲ — یکی از سه مسیر

**مسیر ۱ — «ردیف محلی = ۰» ⇒ کلاس `rejected` (سپر داده‌صفر، `api/crm.php:2176-2185`).**
کپی محلی خالی است و سرور داده دارد ⇒ حذف کپی محلی **هیچ ریسکی ندارد** و pull بعدی آن را از سرور پر می‌کند. اسنیپت زیر **خودنگهدار** است: فقط وقتی عمل می‌کند که مقدار محلی واقعاً آرایهٔ خالی (یا غایب) باشد.
```js
(function(){
  var d=(window.ptfSyncDirtyInfo&&ptfSyncDirtyInfo())||[];
  if(!d.length) return 'چیزی در صف نیست';
  var out=[];
  d.forEach(function(x){
    var k=x.k, v=localStorage.getItem(k), n=-1;
    try{ var p=JSON.parse(v||'null'); n = Array.isArray(p)? p.length : -1; }catch(e){ n=-1; }
    if(v===null) n=0;
    if(n!==0){ out.push('اقدام نکرد: '+k+' (محلی '+(n<0?'غیرآرایه/نامشخص':n+' ردیف')+') — فقط مسیر «تلاش مجدد ارسال» امن است'); return; }
    localStorage.removeItem(k);
    try{ var m=JSON.parse(localStorage.getItem('ptf_sync_krevs')||'{}'); delete m[k]; localStorage.setItem('ptf_sync_krevs',JSON.stringify(m)); }catch(e){}
    try{ var g=JSON.parse(localStorage.getItem('ptf_sync_dirty')||'{}'); delete g[k]; localStorage.setItem('ptf_sync_dirty',JSON.stringify(g)); }catch(e){}
    try{ if(window.ptfStorageIdbDelete) ptfStorageIdbDelete('bdata:'+k,function(){}); }catch(e){}
    out.push('انجام شد: کپیِ خالیِ '+k+' حذف شد — رفرش کنید تا از سرور پر شود');
  });
  return out.join('\n');
})()
```
سپس: رفرش کامل (Ctrl+Shift+R) → ۳۰ ثانیه صبر تا pull دلتا کلید را پر کند → «⬆️ تکمیل انتقال یک‌باره».

**مسیر ۲ — «ردیف محلی > ۰» و «اندازه < ۸۰۰۰KB» ⇒ کلاس `conflicts` / `failed`.**
1. خروج و ورود مجدد (یا رفرش سخت) — hold های فرمانِ در حافظه و شمارندهٔ نجات (`flushRescueRound`) پاک می‌شوند.
2. **بدون بازکردن پنل‌های مالی** (OPEX/چک/تنخواه/سهامداران) مستقیم به تنظیمات ← تشخیص همگام‌سازی ← «⬆ تلاش مجدد ارسال»؛ تا ۳ بار با فاصلهٔ ~۱۰ ثانیه.
3. ملاک موفقیت: خاموش‌شدن نوار زرد پایین صفحه **و** غیب‌شدن خط «⏳ در انتظار ارسال به سرور» از جعبهٔ وضعیت دستگاه.
4. **اجباری:** ۳۰ ثانیه همان‌جا بمانید (تب در پیش‌زمینه) یا یک رفرش کنید — بدون pull دلتا، watermark عقب می‌ماند (R3) و کلیک بعدیِ مهاجرت همان کلیدِ نجات‌یافته را دوباره `conflict` می‌گیرد (در شبیه‌سازی اثبات شد).
5. سپس «⬆️ تکمیل انتقال یک‌باره» → تأیید → «✅ هم‌گرایی انجام شد» → رفرش خودکار → جعبه سبز + «🗑 پاک‌سازی کش محلی» (تایپ «پاک»).
6. اگر پس از ۲ ورود تازه هم گام ۳ رخ نداد ⇒ خروجِ بدون‌کد برای این کلاس تمام است (چیزی همان کلید را مدام بازنویسی می‌کند یا merge همگرا نمی‌شود) ⇒ فقط اصلاح P0. **دکمهٔ مهاجرت را پشت‌سرهم نزنید** (F2: هر کلیک ناموفق، کلیدهای سالم را هم به تعارض می‌اندازد).

**مسیر ۳ — «اندازه ≥ ۸۰۰۰KB» (کلاس `skipped`، سقف ۸MB هر کلید در `api/crm.php:2085`) یا خطای `ساختار نامعتبر` / `حجم بیش از حد`.**
خروجی درون‌برنامه‌ای ندارد. دو گزینه: (الف) تنظیمات PHP هاست (`post_max_size`, `upload_max_file_size`, `memory_limit`) — تغییر **پیکربندی سرور**، نه کد برنامه؛ برای `ساختار نامعتبر` معمولاً همین کافی است. (ب) اصلاح P0 (مهاجرت تکه‌ای/کلید‌به‌کلید).

### گام ۳ — اگر هدف فقط خالی‌کردن حافظه است (بدون تکمیل انتقال)
- «پاک‌سازی امن فوری» در همان بخش «حافظه محلی CRM»: اول بک‌آپ سروری می‌گیرد، بعد فقط کلیدهای فرار (اعلان/لاگ/صف ارسال/پیش‌نویس/کش AI/آواتار) را هرس می‌کند؛ رکورد کسب‌وکار دست‌نخورده (`crm/backup.js:730-760`, `crm/storage-quota.js:599-620`).
- ⚠️ «پاک‌سازی کش محلی» را تا همگراییِ آن یک کلید **با دور زدن گارد از کنسول اجرا نکنید**: کشِ هر ۴۹ کلید را پاک می‌کند و اگر آن کلید هنوز روی سرور نباشد، تنها کپیِ محلی‌اش از دست می‌رود.
- برای دیدن فشار حافظه: `ptfStorageHealthSync()` و `ptfStorageTopKeys(6)`.

### اگر هیچ مسیری جواب نداد — این دو خروجی را بفرستید
```js
(function(){var r=(ptfSyncDiagnosticsSnapshot()||{}).lastPushResult||{};return JSON.stringify({ok:r.ok,error:r.error,saved:(r.savedKeys||[]).length,failed:r.failed,rejected:r.rejected,skipped:r.skipped,forbidden:r.forbidden,conflicts:r.conflicts,protectedConflicts:r.protectedConflicts,blocked:r.blocked,pending:r.pending});})()
```
به‌همراه خروجی گام ۱. این دو، کلاس شکست را قطعی می‌کنند (بدون dump شدن دادهٔ کسب‌وکار در کنسول).

### راستی‌آزمایی اسنیپت‌ها (همین نشست، بدون تماس با ریپو)
| سناریو | انتظار | نتیجه |
|---|---|---|
| کلید متعارض با ۳ ردیف محلی | فقط تشخیص، بدون اقدام | `اقدام نکرد: ptf_crm_opex (محلی 3 ردیف)` — داده دست‌نخورده، `krevs` بدون تغییر |
| کلاس `rejected` (کپی محلی `[]`) | حذف کپی خالی + watermark + dirty + کپی IDB | `انجام شد…` — `krevs` فقط همان کلید را از دست داد، `dirty={}` |
| کلید غیرآرایه‌ای (`settings`) | عدم اقدام | `اقدام نکرد: ptf_crm_settings (محلی غیرآرایه/نامشخص)` |
| کلید ۱۱٫۷MB | گزارش اندازه برای شناسایی کلاس `skipped` | `ردیف محلی=3000001 | اندازه=11719KB` |
| صف خالی | پیام بی‌خطر | `هیچ کلید گیرکرده‌ای در صف نیست` |

---

## پیوست د — RCA قطعی پس از دریافت خروجی کنسول کاربر (۲۰۲۶-۰۹-۰۴)

### د.۱ خروجی واقعی دستگاه
```json
{"ok":false,"pushed":0,"error":"","saved":0,"failed":[],"rejected":[],"skipped":[],
 "forbidden":[],"conflicts":["ptf_crm_opex"],"blocked":[],"pending":[]}
```
`ptfSyncDirtyInfo()` → `[{"k":"ptf_crm_opex","since":1788525184954,"ageSec":24}]`

دو نشانهٔ کلیدی: (۱) کلاس شکست فقط `conflicts` است (نه rejected/skipped/forbidden/failed)؛ (۲) فیلد `protectedConflicts` در `lastPushResult` **اصلاً وجود ندارد** (JSON.stringify مقدار `undefined` را حذف می‌کند).

### د.۲ ریشهٔ قطعی (R5) — نجات تعارضِ محافظت‌شده در فاز B «کد مرده» است
- سرور برای `ptf_crm_opex` هم `conflicts` و هم `protectedConflicts` و هم `serverData` (= merge کانونیک محافظت‌شده) را می‌فرستد (`api/crm.php:2152`, `2237`).
- `ptfBPushBatch` در کلاینت فقط `d.rejected / d.skipped / d.forbidden / d.conflicts` را می‌خواند و **هرگز `d.protectedConflicts` را نمی‌خواند**؛ `finish()` هم آن را در شیء نتیجه قرار نمی‌دهد (`crm/client-server.js:541`, `546-560`, `566-573`).
- در نتیجه در `ptfBFlushQueue` عبارت `var protectedKeys = (result.protectedConflicts || [])` **همیشه `[]`** است (`crm/client-server.js:700`) ⇒ شاخهٔ نجاتِ محافظت‌شده (`708-710`) هرگز اجرا نمی‌شود و `ptf_crm_opex` همیشه به شاخهٔ عمومی می‌افتد: `ptfSyncResolveConflictFromServer` → `ptfSmartMerge` → `ptfOpexMergeSnapshots(local, remote, preferRemote=false)` (`crm/sync.js:130-138`, `2486`) که **محلی-برتر** است و نمی‌تواند نسخهٔ کانونیک سرور (با فیلدهای سرورمالک `st=settled / serverMaterialized / serverOwnedIdentity / chequeCd / …`) را بازتولید کند.
- پس هر push بعدی دوباره `conflict` می‌دهد ⇒ حلقهٔ بی‌پایان (۳ نوبت در هر چرخه، `crm/client-server.js:714-718`) و `state.dirty[ptf_crm_opex] = Date.now()` در هر نوبت ⇒ سن کلید همیشه «چند لحظه پیش» (همان `ageSec:24` گزارش‌شده؛ یافتهٔ F7).
- مسیر قدیمی `pushDirty` این فیلد را درست می‌خواند (`crm/sync.js:1083`) ولی در فاز B هرگز اجرا نمی‌شود (`crm/sync.js:969-975`, `1141-1150`).

**نتیجه:** اصلاح v34.8.10 (PROTECTED-CONFLICT-RESCUE) که دقیقاً برای همین بن‌بستِ «مدیر بازرگانی» نوشته شده بود، در معماری فاز B **غیرفعال** است. این حلقه، هم «تکمیل انتقال یک‌باره» را قفل می‌کند (R1) و هم نوار زرد را دائمی نگه می‌دارد.

### د.۳ اصلاح حداقلی (پیشنهاد — پیاده‌سازی نشد)
در `ptfBPushBatch`: تجمع `d.protectedConflicts` (مشابه `conflicts`) و افزودن آن به خروجی `finish()` و به پاسخِ مسیر `needLogin`. با همین ~۴ خط، شاخهٔ `crm/client-server.js:708-710` زنده می‌شود و `ptfSyncResolveProtectedConflictFromServer` با شاخهٔ VERBATIM-CONVERGENCE (`crm/sync.js:151-155`) مقدار کانونیک سرور را **عیناً** می‌پذیرد ⇒ همگرایی در ≤۲ نوبت. (ترجیحاً همراه P0-1 تا مسیر مهاجرت هم از همان موتور عبور کند.)

### د.۴ بازیابی بدون تغییر کد روی دستگاهِ گیرکرده (راستی‌آزمایی‌شده)
سرور همان merge کانونیک را در `lastPushResult.serverData['ptf_crm_opex']` به کلاینت **داده** است؛ نوشتنِ عینِ آن در کپی محلی، دقیقاً همان کاری است که شاخهٔ مردهٔ VERBATIM-CONVERGENCE باید انجام می‌داد. اسنیپت زیر **خودنگهدار** است: اگر حتی یک ردیف محلی در نسخهٔ کانونیک نباشد، اقدام نمی‌کند و فهرستشان را چاپ می‌کند.

```js
(function(){
  var r=(window.ptfSyncDiagnosticsSnapshot&&ptfSyncDiagnosticsSnapshot()||{}).lastPushResult||{};
  var srv=(r.serverData||{})['ptf_crm_opex'];
  if(typeof srv!=='string') return 'serverData موجود نیست — یک بار «⬆ تلاش مجدد ارسال» بزنید و بلافاصله دوباره این را اجرا کنید';
  var loc=localStorage.getItem('ptf_crm_opex'), A=[], B=[];
  try{A=JSON.parse(loc||'[]')}catch(e){}
  try{B=JSON.parse(srv||'[]')}catch(e){return 'پاسخ سرور قابل خواندن نیست';}
  if(!Array.isArray(A)||!Array.isArray(B)) return 'ساختار غیرآرایه — اقدام نکرد';
  var id=function(x){return String(x&&(x._opexRowId||x.cd||x.recurringKey||''))};
  var bs={}; B.forEach(function(x){bs[id(x)]=1});
  var lost=A.filter(function(x){return !bs[id(x)]});
  if(lost.length) return 'اقدام نکرد: '+lost.length+' ردیف محلی در نسخهٔ کانونیک سرور نیست ⇒ ابتدا «🗄 بک‌آپ فوری» بگیرید و این فهرست را بفرستید: '+lost.map(id).join('، ');
  localStorage.setItem('ptf_crm_opex', srv);
  return 'انجام شد: نسخهٔ کانونیک سرور ('+B.length+' ردیف) جایگزین کپی محلی ('+A.length+' ردیف) شد — حالا بدون بازکردن پنل مالی «⬆ تلاش مجدد ارسال» را بزنید';
})()
```

ترتیب اجرا: ۱) «🗄 بک‌آپ فوری» → ۲) یک بار «⬆ تلاش مجدد ارسال» (تا `serverData` در `lastPushResult` بنشیند) → ۳) اسنیپت بالا → ۴) «⬆ تلاش مجدد ارسال» (بدون بازکردن پنل‌های مالی) تا نوار زرد خاموش شود → ۵) ۳۰ ثانیه در پیش‌زمینه بمانید یا یک رفرش (برای جلو رفتن watermark — R3) → ۶) «⬆️ تکمیل انتقال یک‌باره» → ۷) «🗑 پاک‌سازی کش محلی».

| سناریو (راستی‌آزمایی در همین نشست) | انتظار | نتیجه |
|---|---|---|
| همهٔ ردیف‌های محلی در کانونیک هستند | پذیرش کانونیک | `انجام شد: نسخهٔ کانونیک سرور (3 ردیف)…` + مقدار محلی == کانونیک |
| یک ردیف محلی در کانونیک نیست | رد کردن + فهرست | `اقدام نکرد: 1 ردیف محلی … EXP-7777` + دادهٔ محلی دست‌نخورده (۴ ردیف) |
| `serverData` موجود نیست | راهنمای بی‌خطر | `serverData موجود نیست — یک بار «تلاش مجدد ارسال»…` |
| `lastPushResult` خالی | راهنمای بی‌خطر | همان پیام بالا |

---

## پیوست هـ — اجرای اصلاح ریشه‌ای (v34.36.1، ۲۰۲۶-۰۹-۰۴)

> به دستور کارفرما («اصلاح ریشه‌ای را انجام بده») تمام مواردِ بخش‌های ۳ و ۵ و ۷ پیاده‌سازی شد.
> گیت: **`arch-guard` PASS** و **CI gate = ۲۰۳ PASS / ۰ FAIL** (تستر جدید `tester588`: ۹۰ سنجه).
> جزئیات کامل: `RELEASE-NOTES-v34.36.1.md`.

### چه چیزی تغییر کرد (بر پایهٔ کدهای همین گزارش)

| کد | فایل | تغییر |
|---|---|---|
| **R5** | `crm/client-server.js` | `ptfBPushBatch` حالا `protectedConflicts` را جمع می‌کند و در `finish()` و **هر دو** بازگشت زودهنگام منتشر می‌کند؛ `savedKeys` هم کلیدِ محافظت‌شدهٔ در تعارض را کنار می‌گذارد (fail-closed). شاخهٔ نجاتِ v34.8.10 از «کد مرده» به مسیر زنده تبدیل شد. |
| **R3** | `crm/client-server.js` | helper مشترک `bAdvanceWatermarksFromPush(krevs, rev, ackedKeys)` — هر پاسخ push موفق watermark را جلو می‌برد، ولی **فقط برای کلیدهایی که واقعاً ACK شده‌اند** و **هرگز برای کلیدهای union** (`audit`/`avatars`/`notifs`). `rev` سراسری سرور هم از پاسخ جمع و منتشر می‌شود. |
| **P0-1** | `crm/client-server.js` | `convergeMigration` با موتور مشترک `rescueConflictedKeys` + تا ۳ نوبت تلاش مجدد (۹۰۰ms)؛ معیارِ پایان = تأییدِ تک‌تک کلیدها روی مبنای تجمعی `submitted`؛ کلیدهای ACK‌شده دوباره ارسال نمی‌شوند؛ «ردِ بی‌ضرر» (کپی محلیِ آرایهٔ خالی) مانعِ پایان نیست ولی هر مقدار ناخالی fail-closed می‌ماند. |
| **P0-2** | `crm/client-server.js` | `ptfBConfirmFlush` دیگر `ptf_b_flushed`/`ptf_b_synced` را پیش از تلاش پاک نمی‌کند؛ به‌جایش `ptfBFinalize({force:true})`. نشانگر فقط پس از ACK کامل ثبت می‌شود ⇒ تلهٔ «سبز→کهربایی» و پمپِ مجدد localStorage بسته شد. |
| **P0-3** | `crm/client-server.js` + `crm/sync.js` + `crm/backup.js` | گزارشِ تفکیکی بر اساس کلاسِ علت با نام کلیدها و راهنمای عملی هر کلاس؛ ثبت دائمی `ptfSyncNoteError('migration', …)` (صدور جدید)؛ برچسب دامنهٔ «انتقال یک‌باره» در جعبهٔ تشخیص؛ نمایش علتِ آخرین شکست در **جعبهٔ وضعیت دستگاه** (بدون کنسول، بدون افشای payload)؛ اصلاح ارجاع منسوخ «تنظیمات → هم‌گرایی داده» به «تنظیمات ← بک‌آپ و بازگردانی ← وضعیت دستگاه». |
| **F5** | `crm/sync.js` | `diagCounts()` صفِ زندهٔ فاز B (`ptfBPendingKeys`) را با LS union می‌کند ⇒ «صف آفلاین خالی» دیگر دروغ نمی‌گوید (همان نشانهٔ گمراه‌کنندهٔ این پرونده). |
| **F6** | `crm/sync.js` + `crm/client-server.js` | `window._ptfSyncKeys` صادر شد (فهرست مرجعِ ۶۶ کلیدی)؛ فهرست fallback کامل شد (`ptf_crm_fin_events`, `ptf_crm_personal_cheques`) و **cache نمی‌شود** ⇒ «۴۸ از ۴۹» دائمی ریشه‌کن شد. |
| **F7** | `crm/sync.js` | `ptfSyncMarkPendingKeys` نخستین مهرِ زمانیِ dirty را حفظ می‌کند ⇒ سنِ واقعیِ کلیدِ گیرکرده در UI دیده می‌شود. |

### نکتهٔ ایمنی که حین پیاده‌سازی کشف و بسته شد

پیش‌رفتن watermark برای کلیدِ **در تعارضِ نجات‌ناپذیر** (سرور `serverData` نفرستاده) یا **رد شده**، خطرِ بازنویسی کورکورانهٔ نسخهٔ معتبر سرور را داشت: push بعدی با `base` برابرِ سرور دیگر conflict نمی‌داد. به همین دلیل watermark فقط با فهرست سفیدِ `savedKeys` جلو می‌رود و کلیدهای در تعارض/ردشده کهنه می‌مانند تا pull نسخهٔ سرور را بیاورد (سنجهٔ رفتاری در `tester588`).

### وضعیت دستگاهِ گیرکردهٔ فعلی پس از استقرار

مسیرِ بدون کدِ پیوست ج/د همچنان معتبر است، ولی دیگر لازم نیست: پس از ورود مجدد با نسخهٔ v34.36.1، «⬆️ تکمیل انتقال یک‌باره» همان کار را خودکار انجام می‌دهد —
`opex` در نوبت اول conflict می‌دهد ⇒ canonical سرور VERBATIM محلی می‌شود ⇒ نوبت دوم فقط همان کلید را می‌فرستد و ACK می‌گیرد ⇒ نشانگرها ثبت، فاز B فعال، پیام موفقیت و رفرش.
اگر کلیدی واقعاً ناتمام بماند، نام کلیدها و علتِ تفکیکی هم در پیام و هم در جعبهٔ وضعیت دستگاه نوشته می‌شود (و نشانگرها دست‌نخورده می‌مانند تا تلاشِ دوباره ممکن باشد).
