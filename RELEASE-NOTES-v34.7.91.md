# RELEASE NOTES — v34.7.91 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## رفع باگ مودال ثبت فاکتور تامین‌کننده + نمایش بدهی غیرنقدی (SUP-FIX-001/002)

### مشکل گزارش‌شده
- ثبت فاکتور از حساب تامین‌کننده: پنجره بسته می‌شود.
- در منوی تامین‌کنندگان، بدهی غیرنقدی تامین‌کنندگان نمایش داده نمی‌شود؛ «در حال محاسبه» می‌ماند
  ولی چیزی لود نمی‌شود.

### ریشه
1. **ReferenceError در `slInvoiceForm`:** هنگام ساختن HTML، `window.ptfVatRateOf(prefill.date || date || '')`
   از متغیر `date` استفاده می‌کرد که در آن scope تعریف نشده بود → مودال ساخته نشده/بسته می‌شد.
2. **lazy-load اشتباه:** `box()` با `setTimeout(30ms)` قبل از این‌که `slBoxBody` در DOM قرار گیرد
   اجرا می‌شد، `body` null می‌شد و `_slBoxLazyDone=true`; بنابراین رندر مجدد هم جدول را نمی‌ساخت
   و «در حال محاسبه» می‌ماند.

### اصلاحات
- **`crm/supplier-finance.js`:**
  - `_vatDate` قبل از ساخت HTML تعریف شد و درصد VAT از `ptfVatRateOf(_vatDate)` خوانده می‌شود.
  - `ptfSlBoxLazy` تا ۸ بار با تأخیر کوتاه تلاش می‌کند تا `slBoxBody` در DOM ساخته شود؛
    باعث رفع «در حال محاسبه ولی چیزی لود نمی‌شود».
  - `slNewInvoice` به‌جای `ptfDialog` از مودال ساده خودساخته استفاده می‌کند تا پنجره باز بماند.
- **`crm/cheques.js`:** بعد از `renderSuppliers`، `ptfSlBoxLazy()` را صدا می‌زند تا جدول
  فاکتور/حساب/پرداخت پس از رندر DOM پر شود.

### تست
- `_tools/uat/tester494-v34.7.91-supplier-modal-debt.js`: بررسی:
  - `_vatDate` استفاده می‌شود (عدم ReferenceError)
  - lazy چندبار تلاش می‌کند
  - renderSuppliers بعد از رندر، lazy را صدا می‌زند
  - بدهیِ فاکتور بدون پرداخت (غیرنقدی) در `slSupplierOpenTotalsIRR` دیده می‌شود
- گیت CI: **110 PASS / 0 FAIL**.

### نکته
- بدهی غیرنقدی (فاکتور خرید بدون پرداخت) در `balance`/`slSupplierOpenTotalsIRR` لحاظ می‌شود
  و در کادر «فاکتور، حساب و پرداخت» با «در حال محاسبه…» -> جدول ردیف‌ها نمایش داده می‌شود.
- تعهد خرید legacy بدون فاکتور (طبق سیاست قبلی) همچنان فقط به‌عنوان شمارش در بدهی لحاظ نمی‌شود
  و فقط در کادر با `legacy count` نمایش داده می‌شود.
