# یادداشت انتشار CRM v34.8.6

تاریخ: ۱۴۰۵/۰۶/۴ (۲۰۲۶-۰۸-۲۶)

## ریشه‌یابی و رفع دائمی حلقهٔ «توکن معتبر وجود ندارد»

### نشانهٔ گزارش‌شده توسط کارفرما
روی موبایل، ورود با اکانت شخصی → سپس ورود با اکانت دیگر روی همان مرورگر → پس از آن ورود مجدد با اکانت خودِ کاربر به‌طور مکرر با پیام «نشست سرور یا توکن معتبر وجود ندارد» شکست می‌خورد.

### علت‌های ریشه‌ای (هردو سمت)

**۱) سرور — AUTH-TOKEN-RACE (`api/auth.php`):** صدور هر توکن یک چرخهٔ «خواندن tokens.json → افزودن → بازنویسی کامل فایل» **بدون قفل** بود. دو ورود هم‌زمان (چند کاربر/دستگاه در دفتر) باعث lost-update می‌شد: ذخیرهٔ دوم، snapshotِ قبل از توکنِ اول را می‌نوشت و توکن تازه‌صادرشده **بی‌صدا حذف** می‌شد. نتیجه: 401 → کاربر دوباره وارد می‌شد → ورودهای بیشتر → raceهای بیشتر؛ یعنی خرابی **خودتقویت‌شوندهٔ** سازمانی. سنارییوی «تعویض اکانت روی یک گوشی» فقط یکی از راه‌های رسیدن به همین race بود.
- رفع: کل چرخهٔ صدور/ابطال توکن زیر `flock` اختصاصی `tokens.json.lock` (همان الگوی `meta.json.lock`) اجرا می‌شود.
- فایل tokens.json خراب/ناقص دیگر همهٔ نشست‌ها را نمی‌سوزاند: به `tokens.json.corrupt.<زمان>` منتقل و از فروش تمیز ادامه داده می‌شود.

**۲) مرورگر — AUTH-401-HYGIENE (`client-server.js` + `sync.js`):**
- `isNeedLogin` با regex عام `/token|unauthorized|401/i` هر خطای حاوی کلمهٔ «token» (مثل `token_issue_failed`) را «نشست منقضی» تفسیر و نشست سالم را پاک می‌کرد. اکنون فقط نشانه‌های قطعی (`status 401`، `needLogin`، `authentication_required`، `Invalid or expired token`) مسیر لاگین‌مجدد را فعال می‌کنند.
- `retryPullAfterAuth` خودش توکن را نامتقارن پاک می‌کرد و وضعیت زامبی «session هست، token نیست» می‌ساخت — منبع همان پیام گمراه‌کننده روی رفرش. اکنون invalidate فقط در یک نقطه (`refreshAuthToken`) و **هم‌زمان برای هر سه کلید** انجام می‌شود.

**۳) خروج — AUTH-LOGOUT-REVOKE:** دکمهٔ خروج تا این نسخه توکن را تا ۷ روز روی سرور زنده نگه می‌داشت؛ روی دستگاه‌های مشترک یعنی توکن اکانت قبلی معتبر می‌ماند. اکنون:
- endpoint جدید `auth_logout` (POST با هدر `X-CRM-Token`) فقط **همان توکن ارائه‌شده** را باطل می‌کند — دستگاه‌های دیگرِ همان کاربر دست‌نخورده می‌مانند.
- `doLogout` مرورگر این فراخوانی را best-effort (keepalive) انجام می‌دهد و بعد نشست محلی را پاک می‌کند.

### بهداشت انتشار — بامپ واقعی نسخه
- `VERSION.json`، `window.PTF_CRM_RELEASE`، Service Worker (`RELEASE`/`CACHE`) و query همهٔ ۱۰۷ اسکریپت‌ها به‌صورت یکدست `v34.8.6` شدند؛ `manifest.json` و `clear-cache.html` و `SD_SERVICE_VERSION` هم هم‌تراز شدند.
- phase-queryهای موقتی (`&f01…f08=20260825`) که در v34.8.5 برای دورزدن کش بدون تغییر نسخه اضافه شده بودند حذف شدند — نقد گزارش ارزیابی ۲۰۲۶-۰۸-۲۵ (بند ریسک نسخه‌سازی) بسته شد.
- کلاینت‌های نصب‌شده (PWA) با منطق موجودِ `ptf_app_ver` کش قدیمی را خودکار پاک و بارگیری مجدد می‌کنند.

### اصلاحات همراه این شاخه (وارثهٔ استیجینگ ۲۰۲۶-۰۸-۲۵)
- رفع خطای سینتکس PHP در `api/sales-domain.php` (پرانتز جاافتاده در `sd_finance_repair_plan`) که کل API مالی استیجینگ را ۵۰۰ کرده بود — شامل RCA و گیت `php -l` در `run-ci-gate.js`.
- `autocomplete="username"` / `"current-password"` روی فرم ورود.
- کد F1–F7 ایجنت قبلی (فرمان‌های سروری حقوق/برداشت/تزریق رییس، تفکیک تعهدی/نقدی، manifest اصلاحات فقط‌خواندنی).

### کنترل کیفیت
- tester جدید `tester512-v34.8.6-auth-token-race.js` — شامل شبیه‌سازی رفتاری lost-update (اثبات RCA) و منطق جدید `isNeedLogin`.
- پین نسخه همهٔ تسترهای گیت به v34.8.6 به‌روزرسانی شد.
- گیت CI: ۱۲۸ PASS / 0 FAIL (شامل php-parse همهٔ api/*.php).
