# 📊 پیش‌نویس نظام امتیازدهی تامین‌کنندگان و مشتریان — نسخه ۱ (برای اخذ نظر کارفرما)
**تاریخ:** ۱۴۰۵/۰۴/۱۹ | **وضعیت:** ✅ **مصوب کارفرما (۱۴۰۵/۰۴/۱۹) — اجرا شده در v16.6 (US-400/401)**

## 📌 مصوبات کارفرما (پاسخ به ۵ سوال + ابلاغ تکمیلی)
| سوال | تصمیم |
|------|-------|
| ① وزن‌ها | ✅ تایید — بدون تغییر |
| ② معیار تحویل | ✅ فقط یک کلیک اختیاری هنگام ثبت خرید واقعی؛ در صورت حذف بین معیار ۱و۲ تقسیم می‌شد — حفظ شد |
| ③ هشدار بدحسابی هنگام CO | ✅ نمایش داده شود |
| ④ سطح‌بندی و برچسب‌ها | ✅ تایید |
| ⑤ حضور در گزارش‌ها | فقط **خروجی درخواستی مدیر** — همیشه فعال نیست |

**ابلاغ تکمیلی کارفرما (عینا):** «تامین کننده هر آیتم در هنگام ورود قیمت مشخص شود. زمانیکه قیمت خرید ثبت شد بستانکاری تامین کننده در سیستم ثبت می شود، اگر خرید نقدی باشد در همان لحظه صفر می شود و اگر نه میزان پرداختی در هر مرحله باید ثبت شود. میزان بدهی به هر تامین کننده غیر نقد برای او امتیاز و ضریب بالاتری دارد. در عمل مدیران باید بتوانند در هر لحظه میزان بدهی شرکت به هر تامین کننده غیر نقدی را ببینند.» → اجرا شد: `ptf_crm_payables` + نحوه پرداخت نقدی/غیرنقدی در ثبت خرید + پرداخت مرحله‌ای + باکس لحظه‌ای بدهی مدیران + ضریب اعتباردهی در امتیاز و تامین‌یاب.
**تدوین:** پنل مشترک — مشاوران ارشد بازرگانی (فرآیند تامین تجهیزات نفت/گاز/پتروشیمی/فولاد)، مدیران ارشد تامین (ارزیابی وندور به سبک AVL کارفرمایان بزرگ)، کارشناس مقررات سازمان‌های بازرگانی (آیین‌نامه‌های ارزیابی تامین‌کنندگان و رتبه‌بندی اعتباری مشتریان)، و متخصص نرم‌افزار (مسلط به داده‌های موجود CRM)

---

## اصول حاکم (مصوب پنل)

1. **هیچ داده جدیدی از کاربر خواسته نمی‌شود** — امتیاز ۱۰۰٪ از داده‌هایی که همین حالا در سیستم ثبت می‌شود محاسبه می‌گردد (خرید واقعی، استعلام‌ها، فاکتور/وصولی، چک، Win/Loss، تخصص‌ها US-399). کاربری که کارش را درست ثبت کند، امتیازها خودکار ساخته می‌شوند.
2. **امتیاز = ابزار تصمیم، نه حکم قطعی** — همیشه کنار امتیاز، «دلیل» به تفکیک معیار نمایش داده می‌شود و کاربر ارشد می‌تواند تعدیل دستی ثبت کند (با دلیل و audit).
3. **شفافیت وزن‌ها** — وزن‌ها در تنظیمات قابل مشاهده‌اند و تغییرشان فقط توسط مدیران ارشد (چهار نقش) با ثبت در audit.
4. **بدون رتبه‌بندی تنبیهی علنی** — امتیاز پایین فقط به نقش‌های مدیریتی و بازرگانی نمایش داده می‌شود؛ در اسناد چاپی/خروجی هیچ امتیازی درج نمی‌شود.
5. **هم‌خانوادگی با US-399:** امتیاز تامین‌کننده روی همان پروفایل تخصصی (برند/تجهیز) سوار می‌شود و مستقیماً پیشنهادگر «درخواست تامین» را تقویت می‌کند.

---

## بخش ۱ — نظام امتیازدهی تامین‌کنندگان (Vendor Rating) — ۱۰۰ امتیاز

| # | معیار | وزن | منبع داده موجود در سیستم | نحوه محاسبه |
|---|-------|-----|--------------------------|--------------|
| ۱ | **پاسخگویی به استعلام** | ۲۰ | rfqsmart: targets (پاسخ داد/رد کرد/بی‌پاسخ) + زمان پاسخ نسبت به مهلت | نرخ پاسخ × سرعت پاسخ (پاسخ در مهلت = کامل؛ بی‌پاسخ مکرر = کسر) |
| ۲ | **رقابتی بودن قیمت** | ۲۰ | جدول مقایسه قیمت (buycmp/quotes): جایگاه قیمت او بین استعلام‌شوندگان هر قلم | میانگین جایگاه (ارزان‌ترین=۱۰۰٪، گران‌ترین=۰٪) در ۱۲ ماه اخیر |
| ۳ | **سابقه خرید موفق** | ۲۰ | purchases (خرید واقعی v16.0/16.3) | تعداد و تداوم خریدهای قطعی؛ خرید در ۶ ماه اخیر وزن بیشتر |
| ۴ | **تخصص و پوشش کالایی** | ۱۵ | پروفایل US-399: spBrands/spEquip + زمینه کاری | کامل بودن پروفایل + تنوع برند/تجهیز (تامین‌کننده شفاف = قابل اتکاتر) |
| ۵ | **کیفیت تحویل/تعهد** | ۱۵ | ثبت رویداد ساده «تحویل بدون مشکل / با مشکل» هنگام ثبت خرید واقعی (تنها افزودنی UI — یک کلیک، اختیاری) | نرخ تحویل بدون مشکل؛ بدون داده = خنثی (نه مثبت نه منفی) |
| ۶ | **اعتبار و شفافیت ثبتی** | ۱۰ | شناسه ملی، اطلاعات تماس کامل، تاییدشده از سایت، داخلی/خارجی | چک‌لیست تکمیل پروفایل |

**سطح‌بندی خروجی:** 🥇 ممتاز (۸۰+) / 🥈 تاییدشده (۶۰–۷۹) / 🥉 قابل استفاده (۴۰–۵۹) / ⚠️ نیازمند ارزیابی (<۴۰ یا داده ناکافی)
**محل نمایش پیشنهادی:** بج کنار نام در فهرست تامین‌کنندگان + کارت تامین‌کننده + **ضریب در پیشنهادگر درخواست تامین** (امتیاز بالا = بونوس در رتبه‌بندی US-399)

## بخش ۲ — نظام امتیازدهی مشتریان (Customer Rating) — ۱۰۰ امتیاز

| # | معیار | وزن | منبع داده موجود | نحوه محاسبه |
|---|-------|-----|-----------------|--------------|
| ۱ | **خوش‌حسابی (وصول مطالبات)** | ۳۰ | فاکتورها + payments + چک‌های وصولی/برگشتی | میانگین تاخیر وصول نسبت به سررسید؛ چک برگشتی = کسر سنگین |
| ۲ | **حجم و ارزش خرید** | ۲۵ | CO های برنده + فاکتورها (۱۲ ماه اخیر، تسعیر با موتور v16.0) | سهم از فروش کل (نرمال‌شده لگاریتمی — مشتری بزرگ منصفانه جلوتر، نه له‌کننده) |
| ۳ | **نرخ تبدیل درخواست به خرید** | ۲۰ | درخواست‌ها ↔ CO برنده (Win/Loss US-349) | درصد درخواست‌هایی که به برد ختم شد؛ مشتری «فقط استعلام‌گیر» امتیاز کمتر |
| ۴ | **تداوم و وفاداری** | ۱۵ | تاریخ اولین/آخرین معامله، فاصله سفارش‌ها | مشتری فعال تکرارشونده > مشتری یک‌باره؛ رکود >۱۲ ماه = افت تدریجی |
| ۵ | **کیفیت تعامل** | ۱۰ | تکمیل پروفایل (رابط‌ها/شماره‌ها)، وضعیت وندورلیست، سابقه لغو/انصراف (دلایل حذف ثبت‌شده) | چک‌لیست + کسر بابت لغوهای مکرر |

**سطح‌بندی خروجی:** 💎 کلیدی (۸۰+) / 🌟 ارزشمند (۶۰–۷۹) / 🔵 عادی (۴۰–۵۹) / ⚪ راکد/جدید (<۴۰ یا داده ناکافی)
**محل نمایش پیشنهادی:** بج روی کارت مشتری + فهرست مشتریان + هشدار هوشمند در لحظه صدور CO برای مشتری با سابقه بدحسابی («این مشتری میانگین تاخیر وصول ۴۵ روز دارد — شرایط پرداخت را سفت‌تر ببندید»)

## بخش ۳ — قواعد مشترک

- **بازه محاسبه:** غلتان ۱۲ ماهه؛ به‌روزرسانی خودکار شبانه/در لحظه باز شدن کارت (بدون فشار به سرور — محاسبه سمت کلاینت از داده‌های سینک‌شده).
- **داده ناکافی ≠ امتیاز صفر:** رکورد با کمتر از حد نصاب تراکنش، برچسب «داده ناکافی» می‌گیرد نه امتیاز پایین.
- **تعدیل دستی:** مدیر ارشد می‌تواند ±۱۵ امتیاز با دلیل ثبت کند (در audit + نمایش «تعدیل‌شده»).
- **RBAC:** مشاهده جزئیات امتیاز مشتری (مالی) فقط نقش‌های دارای finance؛ امتیاز تامین‌کننده برای بازرگانی و بالاتر.
- **برآورد اجرا:** ۲ اسپرینت (اسپرینت اول: موتور محاسبه + بج‌ها؛ اسپرینت دوم: تعدیل دستی + هشدار هوشمند CO + اتصال بونوس به تامین‌یاب US-399). شماره پیشنهادی: **US-400 (تامین‌کننده) + US-401 (مشتری)**.

## ❓ سوالات باز برای تصمیم شما (پیش از نهایی شدن)

1. وزن‌های پیشنهادی دو جدول را تایید می‌کنید یا تغییری مد نظر است؟
2. معیار ۵ تامین‌کننده («تحویل بدون مشکل/با مشکل» — تنها فیلد جدید، یک کلیک اختیاری هنگام ثبت خرید واقعی) اضافه شود یا فعلا حذف و وزنش بین ۱ و ۲ تقسیم شود؟
3. هشدار بدحسابی هنگام صدور CO فعال باشد یا فقط نمایش بج کافی است؟
4. سطح‌بندی چهارگانه و برچسب‌های فارسی پیشنهادی مورد تایید است؟
5. آیا امتیاز در گزارش‌های مدیریتی (ماژول گزارش‌ها) هم بیاید؟
