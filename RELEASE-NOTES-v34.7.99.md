# RELEASE NOTES — v34.7.99 (۱۴۰۵/۰۶/۰۱ — 2026-08-23)

## بازگردانی پایدار تاریخ اعتبار پیشنهاد

### مشکل

در فرم پیشنهاد مالی/فنی‌مالی، فیلد «اعتبار پیشنهاد تا» ابتدا به‌درستی توسط `offers.js` ساخته می‌شد، اما افزونهٔ ارز در `offers-pro.js` حدود ۵۰ میلی‌ثانیه بعد آخرین `.fld` ردیف «قالب چاپ / اعتبار» را به خیال ستون خالی با `innerHTML` بازنویسی می‌کرد. در نتیجه `#ofValidJ`، مقدار انتخاب‌شده و date picker از DOM حذف و فیلد ارز جایگزین آن می‌شد.

### اصلاح

- تزریق ارز دیگر هیچ فیلد موجودی را بازنویسی نمی‌کند.
- فیلد ارز در ردیف مستقل `offer-currency-row` و پس از ردیف اعتبار درج می‌شود.
- مسیر fallback فرم‌های قدیمی نیز از همان ردیف مستقل و غیرمخرب استفاده می‌کند.
- تزریق همچنان idempotent است و با بازشدن/wrapperهای متعدد، فیلد ارز تکراری نمی‌سازد.
- قراردادهای قبلی حفظ شده‌اند: تاریخ پیش‌فرض هفت روز بعد از تاریخ سند، تبدیل شمسی به `validUntil` در materialize و ذخیره نهایی، و پردازش هشدار سه روز مانده به انقضا.

### تست رگرسیون

`_tools/uat/tester500-v34.7.99-offer-validity-visible.js` سناریوی واقعی DOM را با اجرای خود `injectCurrencyField` پوشش می‌دهد و ثابت می‌کند:

- `#ofValidJ` و مقدار آن پس از تزریق ارز باقی می‌مانند؛
- ارز و مبنای نرخ در ردیف مستقل اضافه می‌شوند؛
- مسیر fallback بی‌دلیل اجرا نمی‌شود؛
- اجرای دوم تزریق no-op است؛
- `validUntil` و یادآور انقضا دچار رگرسیون نشده‌اند.

### نسخه و cache

- نسخه رسمی CRM و service worker روی `v34.7.99` قرار گرفت.
- cache-bust دارایی‌های CRM، به‌ویژه `offers.js` و `offers-pro.js`، به `34.7.99` ارتقا یافت تا مرورگر نسخهٔ معیوب قبلی را نگه ندارد.
