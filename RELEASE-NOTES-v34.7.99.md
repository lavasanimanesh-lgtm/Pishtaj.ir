# RELEASE NOTES — v34.7.99 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## هاب مالی — اعداد کارت‌های KPI درشت‌تر برای خوانایی

بازخورد کارفرما پس از v34.7.98:  
> «اعداد کامل نمایش داده می شوند اما باید کمی درشت‌تر باشند تا خواناتر باشند»

### تغییرات

فقط اندازه‌ی فونت و اسپیس‌بندی — بدون تغییر منطق نمایش (کماکان عدد کامل ریالی با کاما فارسی).

#### دسکتاپ (`.sc` سراسری)

| ویژگی | v34.7.98 | v34.7.99 |
|---|---|---|
| فونت عدد | `clamp(14px, 1.3vw, 18px)` | **`clamp(16px, 1.5vw, 22px)`** (+4px سقف، +2px کف) |
| padding کارت | `14px 12px` | **`15px 13px`** (تنفس بیشتر) |
| `.unit` واحد | `62%` | **`65%`** (متناسب با عدد درشت‌تر خوانا می‌ماند) |
| minmax | `180px` | بدون تغییر |

#### موبایل (`@media max-width: 768px`)

| ویژگی | v34.7.98 | v34.7.99 |
|---|---|---|
| فونت عدد | `13.5px` | **`16px`** (+2.5px قابل توجه) |
| padding کارت | `10px 9px` | **`12px 10px`** |
| minmax | `150px` | **`160px`** (کمی بزرگ‌تر برای عدد درشت‌تر) |
| `.unit` واحد | `58%` | **`62%`** |
| span توضیح | `10.5px` | **`11px`** (+ margin-top 2px) |

### چرا این اندازه‌ها؟

- **دسکتاپ سقف 22px** (~150% اندازهٔ متن بدنه): درشت‌تر از قبل بدون اینکه در باریک‌ترین کارت (180px) اعداد میلیاردی رایج (11 کاراکتر مثل `۱٬۵۰۰٬۰۰۰٬۰۰۰`) شکسته یا `…` شوند.
- **موبایل 16px**: مطابق استاندارد iOS/Android برای «متن اصلی خوانا». پایین‌تر از این، در گوشی‌های Retina خفه‌کننده است.
- **حفظ `nowrap + ellipsis + title`**: اعداد ۱۳ رقمی نامعمول (`۹۹۹٬۹۹۹٬۹۹۹٬۹۹۹`) در موبایل با فونت 16px ممکن است `…` شوند ولی عدد کامل روی tooltip قابل مشاهده است.

### اثر جانبی

هیچ. فقط CSS بازنویسی شد. تابع‌های `ptfMoneyCompact` / `ptfMoneyCompactHtml` و پنج ماژول مصرف‌کننده (`fiscal.js`, `working-capital.js`, `ledger-report.js`, `treasury.js`, `supplier-finance.js`) دست‌نخورده باقی ماندند.

### تست

- `tester498` بروزرسانی شد — **۴۳ سنجش** (از ۴۰ به ۴۳)، انتظارات فونت جدید ثابت شد.
- گیت CI: **۱۱۴ PASS / ۰ FAIL** ✅
- نگهبان معماری: **PASS** ✅

### فایل‌ها

- تغییر: `crm/index.html` (فقط ۲ بلوک CSS `.sc/.sr` سراسری + `@media(max-width:768px)`)
- تغییر: `_tools/uat/tester498-...` (انتظارات فونت جدید)
- افزوده: `RELEASE-NOTES-v34.7.99.md`
- بامپ نسخه: ۷ نقطهٔ رسمی + ۵۰ تستر. کامنت‌های تاریخی v34.7.93/95/96/97 در همه فایل‌ها دست‌نخورده ماندند.
