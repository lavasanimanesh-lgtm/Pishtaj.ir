# یادداشت‌های انتشار (Release Notes — Version 33.6.0)

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (ادامهٔ جریان ۱۶۵+ کامیت مرج‌نشده — PR #11 باز تا پایان مهاجرت MySQL)
**تمرکز ریلیز:** ماژول مستقل **«🖨 چاپ چک فیزیکی»** زیر گروه «📦 کالا و اسناد» (CHQ-PRINT) + حذف کامل ثبت/چاپ چک از ماژول شخصی + انتقال چاپ از هاب مالی.

---

## 🚀 دستاوردهای کلیدی نسخه ۳۳.۶.۰

### ۱. ماژول مستقل «چاپ چک فیزیکی» (CHQ-PRINT — مصوب کارفرما)
پنل جدید `chqprint` در گروه **«کالا و اسناد»** (فایل `crm/cheque-print.js`):
- **فقط چاپ** — چاپ تکی و چاپ چندتایی؛ **هیچ رکوردی ذخیره نمی‌شود**.
- **بدون کد صیادی** — ورود و چاپ صیادی کاملاً حذف شد.
- محتوای هر برگه: **تاریخ (عدد + به حروف)**، **در وجه (ذی‌نفع)**، **کد ملی (۱۰ رقم) / شناسه ملی (۱۱ رقم)**، **مبلغ (به عدد — قرمز در بالای برگه — و به حروف)**، بابت (اختیاری).
- **تنظیمات قابل کالیبره برای هر بخش:** مختصات (top/right/left به میلی‌متر)، اندازهٔ حروف (pt)، فونت چاپی فارسی (نستعلیق IranNastaliq / بی‌نازنین / بی‌یاقوت / بی‌لوتوس / ایران‌سنس / وزیرمتن / تاهوما) و رنگ.
- **مبلغ بالای چک قرمز چاپ می‌شود** (پیش‌فرض `#b91c1c` — قابل تغییر از تنظیمات).
- ابعاد برگه قابل تنظیم (پیش‌فرض 169×78mm رایج چک ایران) + آفست ox/oy + خطوط راهنما + چاپ آزمایشی.
- تاریخ به حروف: تابع `ptfJDateWords` (مثال: «بیست و یکم تیر ماه هزار و چهارصد و پنج»).
- `ptfNumToFaWords` (مبلغ به حروف با «و») از هاب مالی به این ماژول منتقل و **رفع باگ**: مبالغ ≥ ۱۰۰۰ میلیارد (تریلیون) دیگر کرش نمی‌کنند (توسعهٔ گروه‌ها تا «هزار میلیارد/میلیون میلیارد»).

### ۲. حذف کامل ثبت/چاپ چک از ماژول شخصی
- باکس «چک‌های صادره» (دکمه‌های ثبت تکی / ثبت دسته‌ای + چاپ / چاپ چندتایی / کالیبره / دستیار چک) از پنل یادآورها **کاملاً حذف شد** (`chBoxHtml`/`hookReminders`/`chqBox` دیگر وجود ندارند).
- یادآورهای سررسید چک (مالی/وارده/صادره) طبق قبل کار می‌کنند (موتور یادآور حفظ شد).

### ۳. انتقال چاپ از هاب مالی به ماژول مستقل
- دکمهٔ «🖨 چاپ برگه» و تابع `ptfChequePrint` از هاب مالی (پنل چک‌ها) **حذف شد**؛ چاپ فقط از ماژول «چاپ چک فیزیکی» انجام می‌شود.
- ثبت/مدیریت چک (صادره/وارده) + اثر مالی + دستیار هوشمند (OCR) در هاب مالی **دست‌نخورده** باقی مانده‌اند (تصمیم کارفرما).

### ۴. دسترسی‌ها
- نقش «حسابدار» + نقش‌های ارشد به `chqprint` دسترسی دارند؛ گارد `ptfCanAccess` + پنل مدیریت دسترسی (ALL_PANELS) برای override شخصی هر کاربر اضافه شد.

### ۵. تست و انتشار
- تستر جدید **tester287-chqprint-module (۳۰/۰)** + به‌روزرسانی tester215 (۴۳/۰)، tester54 (۲۴/۰)، tester193 (۱۵/۰)، tester124 (۱۰/۰)، tester145 (۱۱/۰).
- بامپ سراسری نسخه به **v33.6.0** (index.html + sw.js + clear-cache.html) و بازسازی `crm-update.zip` (۵۵۴ فایل).
- رگرسیون A/B روی درخت پایه: **صفر شکست جدید** (فقط ۵۲ شکست از قبل موجود — بدهی قدیمی).

---

## 📋 تغییرات فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/cheque-print.js` | **جدید** — ماژول چاپ چک فیزیکی (پنل + چاپ + تنظیمات) |
| `crm/cheques.js` | حذف باکس چک شخصی (chBoxHtml/hookReminders) + refreshBox بی‌اثر |
| `crm/cheque-panel.js` | حذف ptfChequePrint و دکمه «چاپ برگه» + انتقال ptfNumToFaWords |
| `crm/index.html` | دکمه/عنوان/پنل chqprint + اسکریپت جدید + بامپ v33.6.0 |
| `crm/shell.js` | chqprint به گروه «کالا و اسناد» |
| `crm/rbac.js` | دسترسی حسابدار به chqprint |
| `crm/perms.js` | chqprint در ALL_PANELS (+ surplus که جا مانده بود) |
| `crm/sw.js` | کش v33.6.0 + پیش‌کش cheque-print.js |
| `crm/clear-cache.html` | بامپ v33.6.0 |
| `crm-update.zip` | بازسازی کامل (۵۵۴ فایل) |
| `_tools/uat/tester287-chqprint-module.js` | **جدید** (۳۰/۰) |
| `_tools/uat/tester215,54,193,124,145` | به‌روزرسانی انتظارات (حذف باکس شخصی / انتقال چاپ) |
| `RELEASE-NOTES-v33.6.0.md` / `REGRESSION-REPORT-v33.6.0.md` | اسناد انتشار این ریلیز |
| `_audit/HANDOFF-NEXT-SESSION-2026-08-01.md` | تحویل نشست |

---

## ⚠️ برای کاربر (روش اعمال)
1. `Ctrl+Shift+R` روی صفحهٔ CRM یا `crm/clear-cache.html` (کش قدیمی v33.5.0 ممکن است ماژول جدید را نشان ندهد).
2. از منوی «📦 کالا و اسناد» → «🖨 چاپ چک فیزیکی».
3. ابتدا «📐 تنظیمات چاپ → 🖨 چاپ آزمایشی (با راهنما)» برای هم‌راستایی با برگهٔ چک بانکی خودتان؛ سپس ذخیرهٔ چیدمان.
4. در دیالوگ پرینت: **Margins = None** و **Scale = 100%**.
