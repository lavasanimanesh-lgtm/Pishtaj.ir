# فاز ۱۳ (v34.0.16-alpha) — دسته چک + قفل صیادی + مشاهده/حذف سند + ویرایش کامل چک

**برنچ:** `arena/019fcd1d-pishtaj-ir`
**درخواست کارفرما (چندبخشی):**
۱) در بسیاری جاها (مثل ثبت فاکتور خرید) سند ضمیمهشده قابل مشاهده/حذف نیست — باید سرتاسری قابل مشاهده/حذف در همان نقطه باشد.
۲) قابلیت اصلاح چک هنوز کامل نیست؛ باید اصلاح + افزودن سند/کپی چک بعداً ممکن باشد.
۳) امکان ثبت دسته چک (بانک/شماره حساب/مالک/شعبه/سری/شماره از..تا) و انتخاب کشویی هنگام ثبت چک.
۴) اگر چک از دستیار ثبت شود، شماره صیاد باید قفل شود تا ثبت مجدد ممکن نباشد.

---

## اصلاحات

### ۱) مشاهده/حذف سند در همان نقطه (سرتاسری) — `crm/storage.js`
- `attachUploadWidget` (که همهٔ فرمها از آن استفاده میکنند) حالا بعد از آپلود هر فایل، **«👁 مشاهده»** و **«✕ حذف»** را در همان نقطه نشان میدهد.
- تابع `ptfRemoveJustUploaded` فایل را از فضای ابری حذف و ردیف UI را پاک میکند.
- **فاکتور خرید:** در گردش حساب، فایلِ هر فاکتور با «📎 مشاهده» + «✕ حذف» نمایش داده میشود (`slInvoiceRemoveFile`).

### ۲) ویرایش کامل چک — `crm/cheque-panel.js`
- مودال ویرایش چک (`ptfChequeEditUi`) حالا علاوه بر مبلغ/تاریخ/ذینفع، فیلدهای **بانک/شعبه/سری/مالک/شماره حساب** و بخش **«📎 اسناد / کپی چک»** (افزودن + حذف) دارد.
- `ptfChEditRemoveFile` حذف سند از چک.

### ۳) دسته چک (Cheque Book) — `crm/cheque-module.js` + `crm/cheque-panel.js`
- کلید داده `ptf_crm_cheque_books` + توابع `ptfChequeBooks/ptfChequeBookSave/ptfChequeBookDelete/ptfChequeBookCoversNo`.
- دکمهٔ «📒 دسته چک» در پنل چک → مودال مدیریت (لیست/ثبت/حذف).
- فرم دسته: نام/بانک/شعبه/سری/شماره حساب/مالک/شماره چک از..تا.
- در فرم ثبت چک صادره، فیلد «📒 دسته چک» (کشویی) → با انتخاب، بانک/شعبه/سری/مالک/حساب به چک منتقل میشود.

### ۴) قفل شماره صیادی — `crm/cheque-module.js`
- `ptfChequeReserveSayad`/`ptfChequeSayadReserved`: صیادِ ثبتشده (مخصوصاً از دستیار) رزرو/قفل میشود.
- در `ptfChequeCreate`: اگر صیاد رزرو شده باشد، ثبت مجدد **ممنوع** است (`sayad_locked`) — حتی اگر چک قبلی موجود باشد.

---

## تست‌ها
- **tester312** جدید (۱۶/۰): دسته چک + قفل صیاد + مشاهده/حذف سند + ویرایش کامل چک.
- رگرسیون کامل سبز (**۲۲ تستر، ۰ شکست**).
- بامپ نسخه → **v34.0.16-alpha**.
- تغییرات `.github/workflows/*` در PENDING-workflow-changes ثبت شد.

*تولیدشده توسط Arena Agent — دسته چک، قفل صیادی، و مشاهده/حذف سند سرتاسری.*
