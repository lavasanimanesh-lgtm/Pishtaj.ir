# 📊 گزارش رگرسیون v31.7.20

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.20
**ابزار:** `_tools/run-full-regression.js` + `python3 _tools/audit.py` (۱۰ بخش)

## ✅ نتیجه نهایی

| معیار | مقدار | وضعیت |
|--------|-------|--------|
| تعداد تسترها | **۱۷۳** (+۱: tester195-code-dup-model) | — |
| فایل‌های PASS | **۱۷۳** | ✅ ۱۰۰٪ |
| چک‌های FAIL | **۰** | ✅ صفر |
| audit.py (۱۰ بخش) | **PASS** | ✅ |
| `php -l api/codegen.php` + `node --check crm/rfqsmart.js` | **PASS** | ✅ |

## 🆕 پوشش تست جدید (tester195 + E2E)

- **E2E سرور PHP واقعی:** با `RFQ-1361` فقط در `crm/data/sync/`، رزرو جدید `RFQ-1362,1363,1364` برگرداند — قبل از fix شمارنده sync را نمی‌دید
- شبیه‌سازی سناریوی کارفرما: کد تکراری → ۲ regen → کد سالم `RFQ-1362`؛ همه-تکراری → ثبت مسدود (نه ادغام بی‌صدا/نه حذف آبشاری مشترک)
- گارد saveProd با حلقه ۵تایی (گارد قبلی تک‌تلاشی بود)
- PDF استعلام تامین: با مدل → ستون Model + مقدار؛ بی‌مدل → بدون ستون اضافه (رندر رفتاری هر دو حالت)
- mapping `x.md → model` از ماژول کالا سالم

## 📌 درس ثبت‌شده

مصداق دیگر شکاف `crm/data/` و `crm/data/sync/` — قبلاً auth (v31.7.7) و users (v31.7.14) همین شکاف را داشتند؛ اکنون codegen هم. آیتم بک‌لاگ `TECHDEBT-DATA-DIR-AUDIT` ثبت شد: ممیزی سیستماتیک همه خواندن‌های سروری از `crm/data/` که باید sync را هم ببینند.

## 📁 فایل‌های تغییریافته

`api/codegen.php`، `crm/index.html` (گاردها + bump)، `crm/rfqsmart.js`، `crm/sw.js`، `crm/clear-cache.html`، `_tools/uat/tester195` (جدید)

## 🚦 وضعیت گیت‌ها

- **گیت فنی: سبز** ✅
- **گیت انسانی: معوق** ⏳ — ۴ سناریوی RELEASE-NOTES روی سرور واقعی (دقیقاً بازتولید مشاهدات کارفرما) + بررسی duplicateهای موجود از تنظیمات.
