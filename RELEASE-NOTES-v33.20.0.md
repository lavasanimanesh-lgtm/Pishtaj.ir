# یادداشت‌های انتشار (Release Notes — Version 33.20.0) — آینهٔ خالدار (کلیدهای سنگین به IndexedDB)

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fc272-pishtaj-ir`
**پاسخ به گزارش کاربر:** «بعد از v33.19.0 استورج از ۴۱٪ به نزدیک ۸۰٪ برگشت و پاک‌سازی کش اثر نداشت.»

---

## 🔍 ریشه (تأییدشده از کد)

پاک‌سازی کش درست کار کرده بود (۴۱٪)، اما localStorage در پیاده‌سازی فعلی «**آینهٔ کامل دادهٔ سرور**» است:

1. `sync.js` در هر بارگذاری صفحه `pullCheck(forceFull)` می‌زند و **همهٔ کلیدهای سرور** را با `localStorage.setItem` دوباره می‌نویسد → آینه با اولین رفرش برمی‌گردد.
2. هر ۲۰ ثانیه pull مؤخره‌ای هم کلیدهای تغییرکرده را می‌نویسد.
3. هوک فاز B هم هر خواندن/نوشتن را محلی منعکس می‌کند (طرحی برای آفلاین/رندر فوری).

یعنی ۸۰٪ ≈ حجم **واقعی** دادهٔ همگام بود و هیچ پاک‌سازی کشی نمی‌توانست آن را پایین نگه دارد.

---

## ✅ راه‌حل (v33.20.0) — آینهٔ خالدار

**کلیدهای سبک** همچنان در localStorage آینه می‌شوند (آفلاین + رندر فوری حفظ می‌ماند)، اما
**کلیدهای سنگین** دیگر در localStorage نمی‌نشینند؛ فقط در «حافظهٔ نشست + IndexedDB» نگهداری می‌شوند — سقف IDB صدها برابر بزرگ‌تر از سقف ~۵MB مرورگر است.

### `crm/client-server.js`
- **فهرست سنگین `IDB_KEYS`** (آواتارها، بایگانی حذف‌شده‌ها، پکینگ‌لیست‌ها، نامه‌ها، قراردادها، پیشنهادهای فنی، اجراهای محاسبه، اسنپ‌شات‌های سال مالی، خوانش‌های استعلام، اقلام استعلام) **+ قاعدهٔ خودکار:** هر کلید با متن >۱۲۰هزار کاراکتر (~۲۴۰KB) خودکار سنگین می‌شود.
- **`window.ptfBMirrorActive()`** — فعال‌سازی فقط وقتی: فاز B فعال + نشانگر هم‌گرایی موفق `ptf_b_synced_<user>` + IndexedDB موجود (سه‌گانهٔ ایمنی؛ داده هیچ‌وقت قبل از سینک قطعی جابه‌جا نمی‌شود).
- **`ptfBMirror(k, str)` / `ptfBRead(k)`** — رابط رشته‌ای جایگزین localStorage برای مرورهای همگام (`sync.js`)؛ سنگین‌ها در `idbMem` با پیشوند `bdata:` در IDB، و هر نسخهٔ محلی باقی‌مانده بلافاصله حذف می‌شود.
- **`ptfBIdbPreload`** — هنگام بوت: نسخه‌های localStorage کلیدهای سنگین به حافظه/IDB **مigrate** و آزاد می‌شوند (آن ۸۰٪ واقعاً پایین می‌آید)؛ نسخه‌های IDB به حافظه برمی‌گردند (آفلاین حفظ).
- **هوک‌ها:** `getData` سنگین را از حافظه می‌دهد و تازه‌سازی‌اش به آینهٔ IDB می‌فرستد (نه localStorage)؛ `setData` سنگین را به حافظه+IDB می‌نویسد + صف ارسال به سرور مثل قبل.
- **`ptfBDisable`** کلیدهای سنگین را امن به localStorage برمی‌گرداند (سوییچ‌بک کامل).

### `crm/sync.js`
- راهنمای داخلی `rd(k)/wr(k,s)`: خواندن رشتهٔ کلید از حافظه (با fallback به localStorage — دقیقاً معنای قدیمی).
- ۱۶ سایت وصل شد: آینهٔ pull (۳ مسیر merge)، مونتاژ push معمولی، **sendBeacon beforeunload** (بحرانی — چک سنگین کثیف سر بستن صفحه گم نمی‌شود)، merge تعارضی، اسنپ‌شات پیش از pull، شمارنده‌های سپر داده (`guardCounts`/massDropCheck — بدون گارد، کلیدهای سنگینِ منتقل‌شده هشدار «فت انبوه داده» کاذب می‌دادند)، بوت‌سترپ seed و خواندن `deleted_archive`/dup-collapse.

### `crm/theme.js`
- `avatarsAll()` در حالت فاز B از `getData` می‌خواند (تنها خوانندهٔ مستقیم localStorage در کل codebase غیر از sync.js بود).

### امنیت داده
- هیچ مسیر push/pushBeacon/backup/restore حذف یا کم‌جان نشده؛ خواندن‌ها همیشه fallback دارند.
- گیت «فاز + synced + IDB» یعنی روی کاربران سینک‌نکرده/بدون IDB رفتار دقیقاً مثل v33.19.0 می‌ماند.
- داده‌ی پاک‌کنندهٔ کش عمل می‌کند، Migration های قدیمی‌تر (archive آواتارها) دست‌نخورده.

---

## 🧪 کیفیت
- **tester299-idb-mirror.js (جدید): ۱۴/۰** — گیت فعال‌سازی، مهاجرت بوت، فهرست+خودکار، mirror/read، get/setData سنگین، یکپارچگی sync.js/theme.js، برگرداندن امن در غیرفعال‌سازی.
- **tester298: ۱۲/۰** و **tester297: ۱۹/۰** بدون تغییر؛ رگرسیون کامل = صفر شکست جدید (گزارش: REGRESSION-REPORT-v33.20.0.md).
- audit.py = ۰ خطا؛ سینتکس JS سبز؛ crm-update.zip بازسازی.

## 📋 فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/client-server.js` | آینهٔ خالدار: IDB_KEYS/خودکار، ptfBMirror/ptfBRead، preload+مهاجرت، هوک‌ها، ری‌هیدریت در غیرفعال‌سازی |
| `crm/sync.js` | rd/wr + اتصال ۱۶ سایت (شامل beforeunload beacon و سپر داده) |
| `crm/theme.js` | avatarsAll از مسیر فاز‌B‌آگاه |
| `crm/backup.js` | توضیح v33.20.0 در باکس فاز B |
| `crm/index.html` / `sw.js` / `clear-cache.html` | بامپ v33.20.0 (۸۴+۱+۱) |
| `_tools/uat/tester299-idb-mirror.js` | تستر دائمی جدید (۱۴/۰) |

## ⚠️ گام بعدی
مرج → Deploy to Production (confirm=DEPLOY, ref=main) → کاربران یک‌بار رفرش کنند (یا clear-cache) تا preload مهاجرت اجرا و درصد localStorage پایدار پایین بیاید.
