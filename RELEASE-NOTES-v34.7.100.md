# RELEASE NOTES — v34.7.100 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## هاب مالی — اعداد کارت‌های KPI در دسکتاپ خیلی درشت‌تر

بازخورد کارفرما پس از v34.7.99:  
> «هنوز اعداد خوانا نیستند و کوچکند در دسکتاپ. تغییر بده سایزشونو بزرگتر و خواناتر شوند»

### تغییرات (فقط دسکتاپ — موبایل دست‌نخورد)

| ویژگی | v34.7.99 | v34.7.100 | تغییر |
|---|---|---|---|
| کف فونت عدد | `16px` | **`20px`** | +25% |
| سقف فونت عدد | `22px` | **`28px`** | +27% |
| شیب vw | `1.5vw` | **`1.9vw`** | سریع‌تر به سقف می‌رسد |
| font-weight | پیش‌فرض (700) | **`900`** | ضخامت واقعی |
| letter-spacing | — | **`-0.01em`** | اعداد فارسی فشرده‌تر |
| padding کارت | `15px 13px` | **`18px 15px`** | تنفس بیشتر برای عدد درشت |
| minmax گرید | `180px` | **`200px`** | جای بیشتر برای فونت 28px |
| `.unit` واحد | `65%` | **`55%`** | نسبی کوچک‌تر (چون عدد اصلی خیلی بزرگ‌تر شد) |
| توضیح (span) | `11.5px` | **`12px`** | خوانا |

### نمونهٔ رفتار در سه اندازه دسکتاپ

- **1024px:** `clamp(20, 1.9vw × 1024 = 19.5, 28)` → **20px** (کف اعمال می‌شود)
- **1440px:** `clamp(20, 1.9vw × 1440 = 27.4, 28)` → **27.4px**
- **1920px:** `clamp(20, 1.9vw × 1920 = 36.5, 28)` → **28px** (سقف)
- **1600px:** ~**30.4px → 28px کلمپ می‌شود**

### چرا این اندازه‌ها ایمن‌اند

- **باریک‌ترین کارت دسکتاپ** با `minmax(200px, 1fr)` واقعاً حدود 200-220px می‌شود.
- عدد میلیاردی رایج (`۱٬۵۰۰٬۰۰۰٬۰۰۰` = 11 کاراکتر) با فونت 28px و letter-spacing کمی منفی حدود 170-180px عرض می‌گیرد. **راحت جا می‌شود.**
- عدد ۱۳ رقمی (`۹۹۹٬۹۹۹٬۹۹۹٬۹۹۹`) حدود 220-230px عرض می‌گیرد. در کارت 200px `…` می‌شود و tooltip فعال است (که به‌ندرت لازم می‌شود).
- در صفحات بزرگ‌تر (`1400px+`) کارت‌ها هم بزرگ‌تر می‌شوند (به دلیل `1fr`) و همه اعداد راحت جا می‌شوند.

### موبایل بدون تغییر

طبق درخواست کاربر («در دسکتاپ»)، اندازه‌های موبایل از v34.7.99 دست‌نخورده باقی ماند:
- فونت عدد `16px`، padding `12px 10px`، minmax `160px`، unit `62%`

### تست

- `tester498` بروزرسانی شد — **۴۵ سنجش** (از ۴۳ به ۴۵) با انتظارات جدید:
  - سقف 28px، font-weight 900، letter-spacing منفی، padding 18px 15px، minmax 200px، unit 55%
- گیت CI: **۱۱۴ PASS / ۰ FAIL** ✅
- نگهبان معماری: **PASS** ✅

### فایل‌ها

- تغییر: `crm/index.html` (فقط ۴ خط CSS `.sr/.sc/.sc b/.sc .unit/.sc span` سراسری)
- تغییر: `_tools/uat/tester498-...` (انتظارات فونت جدید)
- افزوده: `RELEASE-NOTES-v34.7.100.md`
- بامپ نسخه: ۷ نقطهٔ رسمی + ۵۰ تستر. کامنت‌های تاریخی v34.7.93/95/96/97 دست‌نخورده.

### گام بعد

اگر همچنان کوچک به نظر می‌رسد بگویید — می‌توانیم به سقف 32-34px هم برویم (البته با آن، اعداد ۱۳ رقمی بیشتر مواقع ellipsis می‌شوند و کاربر باید روی کارت hover کند برای عدد کامل). یا اگر مورد جدیدی دیدید (رنگ، فاصله، …) بگویید.
