# 📋 ریلیز v15.7 + v15.8 — US-388 «ثبت درخواست کامل: اقلام داخل پنجره + دیدن ضمایم»

**تاریخ:** ۱۴۰۵/۰۴/۱۸ | **نوع:** رفع باگ اساسی گزارش‌شده کارفرما (سناریوی ثبت درخواست)

---

## 🔬 دو مشکل و ریشه هر کدام

### ① اقلام درخواست در پنجره ثبت قابل ورود نبود
پنجره «➕ ثبت درخواست جدید» فقط مشخصات کلی و پیوست‌ها را می‌گرفت؛ ورود اقلام به یک **مرحله دوم جدا** (پنجره‌ای که بعد از ثبت باز می‌شد) موکول شده بود — اگر کاربر آن سوال را رد می‌کرد یا متوجهش نمی‌شد، درخواست بدون قلم می‌ماند.

### ② ضمایم بعد از ثبت دیده نمی‌شدند
فایل‌ها درست ذخیره می‌شدند (از v13.3/US-325) ولی **هیچ نشانه‌ای در فهرست درخواست‌ها** نبود که این درخواست ضمیمه دارد؛ تنها راه دیدنشان دکمه کوچک «📎 پیوست‌ها» بود که کاربران پیدایش نمی‌کردند — در حالی که فلسفه پیوست‌ها دقیقا «رفرنس کاربران» است.

## ✅ رفع

### 📋 اقلام — داخل خود پنجره ثبت
- بخش جدید «**📋 اقلام درخواست**» در همان پنجره ثبت: دکمه «＋ افزودن قلم» → ردیف‌های سریع (شرح* / تعداد / واحد / استاندارد) با حذف ✕
- با زدن «ثبت استعلام»، اقلام همراه درخواست ذخیره می‌شوند (همان ساختار استاندارد — «از درخواست» در TO/CO کار می‌کند)
- اگر قلمی وارد کرده باشید، دیگر سوال تکراری «الان اقلام را وارد کنید؟» نمی‌آید
- مسیرهای قبلی (اکسل/دستیار/ویرایش بعدی) کاملا پابرجا — این یک راه سومِ سریع‌تر است

### 📎 ضمایم — همیشه جلوی چشم
- **بج بنفش «📎 n ضمیمه»** روی ردیف هر درخواست در فهرست — یک کلیک = پنجره مشاهده/دانلود همه ضمایم
- داخل مودال **✏️ ویرایش درخواست** هم بخش «📎 ضمایم درخواست» اضافه شد: فهرست همه فایل‌ها (فایل استعلام/دیتاشیت/عکس/نقشه/سایر) با دکمه **👁 مشاهده** و **⬇️ دانلود** مستقیم + دکمه افزودن

## 🔬 کیفیت

| مورد | نتیجه |
|-------|--------|
| tester76 جدید | ✅ ۲۱ چک — رفتاری: ذخیره ۲ قلم معتبر + رد ردیف بی‌شرح + پیش‌فرض تعداد ۱ + شمار بج ضمایم |
| رگرسیون کامل | ✅ **۶۳ تستر / ۱۷۱۲ چک — صفر خطا** |
| `_tools/audit.py` | ✅ PASS |
| نسخه/کش | VER=v15.8، کش `ptf-crm-v15.8`، cache-bust bridge.js |

## 🔄 الحاقیه v15.8 — پاسخ به دو سوال کارفرما (اتصال کامل به ماژول کالا)
**سوال ۱: آیا کالاهای واردشده در پنجره ثبت درخواست به ماژول کالا هم می‌روند؟**
حالا بله — بعد از «ثبت استعلام»، اگر قلمی وارد کرده باشید می‌پرسد: «📦 این کالاها به ماژول کالا هم اضافه شوند؟» → با تایید، هر کالا **کد یکتا** می‌گیرد و ثبت می‌شود؛ کالاهای قبلا موجود **تکرار نمی‌شوند** (همان قاعده ضدتکرار سراسری).

**سوال ۲: آیا مارک شماره درخواست می‌گیرند تا «بارگذاری از درخواست» خودکار بنشیند؟**
بله — دو مارک جدا و مستقل:
- روی خود **اقلام درخواست** شماره درخواست ثبت می‌شود → دکمه بنفش «🗂 بارگذاری از درخواست» در فرم TO/CO دقیقا از همین می‌خواند و اقلام را با تعداد/واحد در ردیف‌ها می‌نشاند (این از قبل کار می‌کرد و اقلام پنجره ثبت هم اکنون همان مسیر را دارند)
- روی رکورد **کالا در ماژول کالا** هم مارک `srcInq` (شماره درخواست مبدا) به‌صورت مخفی درج می‌شود

نکته درباره «سوال دومی که هرگز پرسیده نمی‌شد»: آن سوال قدیمی بعد از بسته شدن پنجره با تاخیر اجرا می‌شد و در عمل دیده نمی‌شد — با طراحی جدید (اقلام داخل خود پنجره) عملا بلااستفاده شده و فقط برای حالتی مانده که هیچ قلمی وارد نکرده باشید.

## 📦 بسته تحویلی
`pishtaj-release-v15.8.zip` — تجمیعی **v14.5 تا v15.8**. آپلود + اکسترکت روی موجود + رفرش (سایدبار: v15.8).

### تست با همان سناریوی خودتان
1. «➕ ثبت درخواست جدید» → پایین فرم، «＋ افزودن قلم» → چند قلم وارد کنید + چند فایل پیوست → «ثبت استعلام»
2. در فهرست درخواست‌ها: بج بنفش «📎 n ضمیمه» روی ردیف → کلیک → مشاهده/دانلود
3. «✏️ ویرایش/حذف» همان درخواست → بخش «📎 ضمایم درخواست» با دکمه مشاهده و دانلود هر فایل
