# ممیزی کامل شاخهٔ `arena/01a0077c-pishtaj-ir` — ۱۴۰۵/۰۵/۲۶ (2026-08-17)

دامنه: **همهٔ تغییرات این شاخه** (از پایهٔ `7374168f` تا `HEAD`) **به‌علاوهٔ هر چیزی که از `main` گرفته شده**.
نسخهٔ خروجی این ممیزی: **v34.7.27**.

---

## ۰) وضعیت شاخه نسبت به `main`

| مورد | نتیجه |
|------|--------|
| `origin/main` سر شاخه | `ef479082` (Merge PR #48) |
| آیا در این شاخه ادغام شده؟ | **بله** — کامیت `42c0a689`؛ PRهای #46، #47 و #48 همگی داخل‌اند |
| کامیت جدید در main پس از آن | **هیچ** — واگرایی صفر، چیزی برای pull نمانده |
| کامیت‌های کاری این شاخه | ۱۲ کامیت (`a44a4050` تا `e66e5192`) + کامیت ممیزی حاضر |
| دامنهٔ کد تغییر‌یافته نسبت به main | ۲۵ فایل کد (`crm/*`, `api/*`) — ۱٬۹۱۲+ / ۳۱۶− خط، به‌علاوهٔ ۱۸ سند و ۱۶ تستر |

آنچه از main آمده بود (بازنویسی صدور صورتحساب غیررسمی، تفکیک مسیر ابطال رسمی/غیررسمی، اصلاح رشتهٔ onclick دکمهٔ غیررسمی) دست‌نخورده باقی مانده و فقط روی همان پایه اصلاح‌های فازهای A–H و S1–S5 سوار شده است.

## ۱) روش ممیزی

۱. بازخوانی diff کامل تولیدی (`crm/*.js`, `api/sales-domain.php`) هانک‌به‌هانک.
۲. اسکن‌های خودکار سراسری (اسکریپت‌های یک‌بارمصرف):
   - صحت نحوی: `node --check` روی همهٔ فایل‌های JS تغییر‌یافته + اعتبارسنجی JSONها + توازن ساختاری PHP.
   - **سلامت انکدینگ**: جست‌وجوی `U+FFFD` در همهٔ فایل‌های فارسی‌دار.
   - **دکمهٔ مرده**: استخراج ۸۱۷ نام تابع صداشده در `onclick/onchange/oninput` و تطبیق با تعاریف پروژه.
   - **تعارض نام سراسری**: نگاشت ترتیب بارگذاری `crm/index.html` و تشخیص «بازنویسی زنجیره‌ای» در برابر «بازنویسی خاموش».
   - بازمانده‌های الگوی باگ‌های همین دوره: تطبیق `''===''`، کلید تهی در نقشه‌ها، `console.log/debugger/TODO` جامانده.
۳. اجرای گیت CI و سوئیت کامل UAT.

## ۲) یافته‌های جدید — همگی در همین ممیزی رفع شدند (v34.7.27)

### AUDIT-1 🔴 «شناسهٔ متعارف پرونده» — پول در نماها دیده نمی‌شد (هم‌خانوادهٔ باگ ابطال)
رکوردهای قدیمی گاهی `caseId` را با **`cd`** پرونده ذخیره کرده‌اند، در حالی که کلید گروه‌بندی همه‌جا `_id || cd` است. نتیجه: رسید و فاکتورِ **یک** پرونده در دو سطل جدا می‌افتادند ⇒ تخصیص FIFO هرگز انجام نمی‌شد ⇒ فاکتور «کاملاً باز» و پول «ناپدید».
- `crm/ar-reconcile.js`: نگاشت `caseAliasMap` (فقط نام‌های مستعارِ خودِ رکورد پرونده) در `computeAllocations` و `caseState`.
- `crm/sales-domain-v2.js`: `caseReceipts/caseInvoices/activeAllocations` با مجموعهٔ نام مستعار.
- ابزار تشخیص: بخش **E** جدید در `_tools/diagnose-advance-leak.js` (فهرست رکوردهای دارای نام مستعار).
- تستر: `tester430-v34.7.27-case-id-alias.js` (۱۰/۰) شامل عدم‌رگرسیون «دو پروندهٔ مستقل ادغام نمی‌شوند» و «دادهٔ مدرن بدون تغییر».

### AUDIT-2 🟠 کش مطالبات پس از همگام‌سازی سرور باطل نمی‌شد
`PTF.ar` یک کش ۱٫۵ ثانیه‌ای دارد. مسیر «فرمان ⇒ projection» آن را باطل می‌کرد، ولی **pull عادی سرور** نه؛ تا ۱٫۵ ثانیه پس از همگام‌سازی، عدد قبلی نمایش داده می‌شد (خودترمیم اما گیج‌کننده).
⇒ در `crm/sync.js: ptfSyncApplyServerProjection` برای کلیدهای مالی `PTF.ar.invalidate()` اضافه شد.

### AUDIT-3 🟠 گارد F2-A بیش‌ازحد سخت‌گیر بود
در وصلهٔ دیروز (S3)، یکتایی تطبیق فاکتور↔پرونده روی **همهٔ** پرونده‌ها (حتی ابطال‌شده‌ها) سنجیده می‌شد؛ یک پروندهٔ حذف‌شدهٔ قدیمی می‌توانست انتساب درست را مسدود کند.
⇒ اکنون فقط پرونده‌های فعال (`sd_active`) شمرده می‌شوند — دقیقاً هم‌راستا با `sd_bind_orphan_invoices`.

### AUDIT-4 🟡 هزینهٔ اجرایی گارد نام یکتا
`cfNameIsUnique` به‌ازای هر نام کل فهرست مشتریان را می‌خواند (در فهرست حساب‌ها ⇒ رفتار درجه‌دو).
⇒ جای آن `cfNameOwners()` یک‌بار در هر فراخوانی `invs()` ساخته می‌شود.

## ۳) آنچه بررسی شد و سالم بود

| بررسی | نتیجه |
|-------|--------|
| نحو JS همهٔ فایل‌های تغییر‌یافته | ✅ ۰ خطا |
| JSONها (`VERSION.json`, `manifest.json`, `known-obsolete.json`) | ✅ معتبر |
| توازن ساختاری `api/sales-domain.php` | ✅ OK (PHP CLI در محیط نیست؛ بررسی ساختاری + گیت `php -l` در CI مستندشده) |
| انکدینگ فارسی (`U+FFFD`) | ✅ صفر مورد در کل `crm/`, `api/` |
| دکمه‌های مرده در `onclick` | ✅ صفر (۸۱۷ نام بررسی شد؛ دو مورد مشکوک، تعریف‌شده با `W.` بودند) |
| `console.log` / `debugger` / `TODO` جامانده | ✅ صفر |
| باقی‌ماندهٔ الگوی «کلید تهی» و `x.no === offerNo` | ✅ در مسیرهای پول باقی نمانده |
| رفتار سرور در `void_invoice` با هر دو شناسه | ✅ از قبل درست بود؛ فقط کلاینت اصلاح شد |

### تعارض نام‌های سراسری (بررسی معماری)
۴۰ نام در بیش از یک ماژول تعریف می‌شوند. تفکیک:
- **۳۵ مورد «بازنویسی زنجیره‌ای»** (نسخهٔ قبلی گرفته و صدا زده می‌شود) — الگوی عمدی و سالم این معماری (`renderDeals`, `goPanel`, `buildSettings`, …).
- **۵ مورد «بازنویسی کامل و خاموش»** (نسخهٔ قبلی دور ریخته می‌شود) — همگی **قدیمی و خارج از تغییرات این شاخه**:
  `ptfAutoRegisterSummaryProducts` (offers.js→storage.js، ظاهراً کپی یکسان)، `saveRfqEdit` و `delRfq` (bridge.js→workflow.js، بازطراحی عمدی)، `ptfDownloadStoredFile` (storage.js→inqreader.js)، `onload`.
  ⚠ این دقیقاً همان الگویی است که باگ «ابطال کنترل‌شده» را ساخت. **پیشنهاد نگهداری** (بدون فوریت): تعریف‌های کهنه حذف یا `…Legacy` شوند. عمداً در این ممیزی دست نخوردند تا تغییر بی‌آزمون وارد نشود.

## ۴) موارد باز — تصمیم/داده لازم دارند (تغییری داده نشد)

| # | مورد | وابسته به |
|---|------|-----------|
| ۱ | S4 — پاکسازی رکوردهای آلودهٔ نشت پیش‌پرداخت (و اختیاراً یکسان‌سازی نام‌های مستعار بند AUDIT-1) | خروجی `_tools/diagnose-advance-leak.js` روی دادهٔ واقعی + تأیید شما |
| ۲ | فاز G (`replace_award`، یکپارچه‌سازی مسیرهای نوشتن پول، snapshot سند برد) و دو تستر `known-open` (tester403، tester415) | تصویب مدل تجاری |
| ۳ | AN-04 کالیبراسیون وزن‌های امتیاز سلامت مشتری | تأیید شما |
| ۴ | H1 اعمال دستی دو وصلهٔ `.github/workflows` (`_tools/PENDING-workflow-*.patch`) | دسترسی GitHub (App مجاز به push روی workflows نیست) |
| ۵ | ۵۹ تستر قرنطینه‌شده (`known-obsolete.json`) و ۵ بازنویسی خاموش بند ۳ | برنامهٔ نگهداری |

## ۵) ریسک‌های پذیرفته‌شده (مستند، بدون تغییر)

- **ترتیب FIFO تخصیص** با مقایسهٔ رشته‌ای تاریخ انجام می‌شود؛ اگر در یک پرونده تاریخ‌ها با دو قالب متفاوت (شمسی و ISO) ذخیره شده باشند ترتیب دقیق نیست. سرور از `sd_date_key` استفاده می‌کند و اثر مالی نهایی یکسان است (فقط ترتیب تخصیص).
- **گارد `offerNo` در `rbac.js` درون‌خطی نوشته شد** (نه با تابع کمکی) چون چند تستر قدیمی بدنهٔ توابع را با regex برش می‌زنند و ارجاع بیرونی در آن‌ها `ReferenceError` می‌ساخت. این محدودیت آگاهانه است و در release notes ثبت شده.
- **کاشی‌های fallback S5** به پرچم `_rbDealsHooked` / `_dxDealsHooked` تکیه دارند؛ اگر ماژول تخصصی دیرتر از اولین رندر هوک شود، در همان رندر اول کاشی هم دیده می‌شود (تکرار موقت، نه از دست رفتن قابلیت).

## ۶) نتیجهٔ آزمون‌ها پس از ممیزی

| آزمون | نتیجه |
|-------|--------|
| سوئیت کامل `node _tools/uat/run-all.js` | **۴۱۳ تستر — ۳۵۴ سبز، ۵۹ قرنطینهٔ مستند، ۰ شکست واقعی** |
| گیت CI `node _tools/uat/run-ci-gate.js` | **۴۵ PASS / ۲ FAIL** (فقط دو `known-open` فاز G؛ سه تستر جدید ۴۲۸/۴۲۹/۴۳۰ به گیت اضافه شدند) |
| تسترهای اختصاصی این دوره | ۴۲۸: ۱۹/۰ — ۴۲۹: ۱۸/۰ — ۴۳۰: ۱۰/۰ |
| بهداشت نسخه | v34.7.27 در ۷ نقطه |

**جمع‌بندی:** در بازبینی سراسری، سه نقص واقعی جدید (AUDIT-1 بحرانی، AUDIT-2 و AUDIT-3 متوسط) و یک مسئلهٔ کارایی پیدا و همان‌جا با آزمون بسته شد؛ هیچ رگرسیونی از تغییرات این شاخه در سوئیت مشاهده نشد و شاخه با `main` هم‌تراز است.
