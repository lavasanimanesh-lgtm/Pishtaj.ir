# 🎨 صورت‌جلسه پنل متخصصان — جذاب‌سازی تجربه موبایل CRM

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-19)
**موضوع:** ایده‌پردازی برای جذاب‌تر کردن فضای موبایل + الگوبرداری از مرجع بصری کارفرما (اپ مالی طلاسی — نوار تب پایین با FAB مرکزی و تب فعال متمایز)
**ترکیب پنل (نقش‌های شبیه‌سازی‌شده):** طراح ارشد UI موبایل (اپ‌های فینتک)، متخصص Interaction/Motion، مهندس ارشد فرانت‌اند PWA، نماینده QA/ثبات (وتوی ریسک)، نماینده کاربر عملیاتی (فروش/موبایل)
**قاعده حاکم جلسه:** فقط ایده‌های «بی‌خطر برای نرم‌افزار» تصویب اجرا می‌گیرند — معیار بی‌خطری: بدون تغییر منطق داده/sync/auth، بدون کتابخانه خارجی (منشور Self-Contained)، فقط CSS/DOM سبک، قابل‌بازگشت، سازگار با `prefers-reduced-motion`.

---

## تحلیل مرجع بصری کارفرما (اسکرین‌شات‌های اپ طلاسی)

آنچه آن نوار پایین را «خوب» می‌کند: ۱) FAB مرکزی برجسته با سایه رنگی، ۲) تب فعال با تغییر رنگ + لیبل bold، ۳) آیکون‌های خطی یکدست، ۴) حس عمق با سایه ملایم. CRM ما FAB و آیکون خطی SVG را از v13.0 داشت؛ شکاف اصلی: **بی‌حرکتی تب‌ها هنگام تغییر** و FOUC تم.

## مصوبات — اجرا در همین ریلیز (ریسک: ناچیز)

| # | ایده | تصویب‌کننده | پیاده‌سازی |
|---|------|-------------|-------------|
| A1 | **رفع FOUC تم** — CSS بحرانی پوسته (فونت، سایدبار، مخفی‌سازی سایدبار موبایل، جای نوار پایین) به `<style id="ptfCriticalTheme">` در head منتقل شود تا تم قدیمی حتی یک فریم هم دیده نشود | مهندس PWA + QA | ✅ `crm/index.html` |
| A2 | **انیمیشن تب فعال** (الگوی مرجع کارفرما): آیکون تب فعال با spring ملایم بالا می‌آید و در pill گرادیانی برند می‌نشیند؛ لیبل bold؛ نقطه نشانگر زیر تب؛ فشردن لمسی scale-down | طراح UI + Motion | ✅ `crm/mobilenav.js` |
| A3 | **FAB زنده**: پالس سایه ۳.۲ثانیه‌ای بسیار ملایم + فشرده‌شدن هنگام لمس | Motion (با قید «غیرمزاحم» از QA) | ✅ `crm/mobilenav.js` |
| A4 | **بازخورد هپتیک ظریف** (vibrate 8ms) هنگام تعویض تب — فقط اگر مرورگر پشتیبانی کند، داخل try/catch | نماینده کاربر | ✅ `crm/mobilenav.js` |
| A5 | **ورود نرم پنل‌ها در موبایل**: fade+slide-up بسیار کوتاه (۱۸۰ms) هنگام تعویض ماژول — فقط transform/opacity | Motion + QA | ✅ `crm/mobilenav.js` |
| A6 | **احترام به `prefers-reduced-motion`**: تمام انیمیشن‌ها برای کاربرانی که حرکت کم می‌خواهند خاموش | QA (شرط تصویب A2/A3/A5) | ✅ |

## بررسی‌شده و رد/تعویق (با دلیل)

| ایده | رأی پنل | دلیل |
|------|---------|------|
| کتابخانه انیمیشن (Lottie/Framer) | ❌ رد قطعی | نقض منشور Self-Contained؛ وابستگی خارجی؛ وزن صفحه |
| Pull-to-refresh سفارشی | ⏸ تعویق | دستکاری touch event روی کل صفحه با جدول‌های اسکرولی ریسک تداخل دارد؛ نیازمند UAT جدا |
| Skeleton loading برای پنل‌ها | ⏸ تعویق | نیازمند تغییر چرخه render همه ماژول‌ها — خارج از تعریف «بی‌خطر» |
| تم تیره خودکار پیشرفته موبایل | ⏸ تعویق | حالت شب موجود کار می‌کند؛ بازطراحی آن scope مستقل است |
| بازآرایی داشبورد موبایل با ویجت | ⏸ تعویق | تغییر ساختاری؛ پیشنهاد شد بعد از UAT این ریلیز به‌عنوان US مستقل تعریف شود |
| صدا هنگام اعلان | ❌ رد | مزاحم در محیط کاری؛ درخواست کارفرما نبوده |

## قید فنی مصوب QA (اعمال‌شده)

فقط `transform` و `opacity` انیمیت می‌شوند (GPU-composited، بدون reflow/jank)؛ هیچ `setInterval`؛ هیچ تغییری در `goPanel`/RBAC/sync؛ همه تغییرات با حذف دو بلاک CSS و یک خط vibrate قابل‌بازگشت کامل است.

## اقدام بعدی پیشنهادی پنل

پس از بازخورد کارفرما از این ریلیز: تصمیم درباره دو آیتم تعویقی «Skeleton» و «ویجت داشبورد» به‌عنوان US مستقل با approval.
