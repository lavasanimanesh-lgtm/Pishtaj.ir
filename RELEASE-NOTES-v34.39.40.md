# v34.39.40 — CONTACT-SYNC-DIAG-2: اصلاح ریشه‌ای «تشخیص سینک تماس / ثبت روی سرور» + زنده‌کردن زنجیرهٔ فرمان‌های entity سرور (۲۰۲۶-۰۹-۲۵)

> این نسخه پاسخ مستقیم به گزارش کارفرماست: «حتی تشخیص سینک تماس را زدم، مشتری را انتخاب کردم و شماره را زدم و ثبت روی سرور را هم زدم، ولی با زدن دوبارهٔ دکمهٔ تشخیص همان پیام مبنی بر اینکه روی سرور وجود ندارد آمد.» گزارش ریشه‌یابی کامل: `REPORT-CONTACT-SYNC-ROOT-CAUSE-2-2026-09-25.md`.
>
> **یادداشت شماره‌گذاری:** این اصلاح ابتدا v34.39.33 شماره خورد؛ چون شماره‌های v34.39.33 تا v34.39.39 قبلاً توسط اسپرینت‌های سئو مصرف شده بودند (RELEASE-NOTES-v34.39.32)، کل مجموعه به **v34.39.40** بازشماره شد.

## چه چیزی وارد شد

### ۱) باگ سروری بحرانی — زنجیرهٔ فرمان‌های entity از v34.19.0 مرده بود (R5)

در `api/sales-domain.php`، گارد `if (!function_exists('sd_tombstone_kinds')) { function sd_tombstone_kinds(…) { … } }` (وارد‌شده با v34.19.0 / کامیت `16e2b5d0` — سطل بازیافت) **به‌اشتباه وسط زنجیرهٔ `if/elseif` دیسپچر** بین شاخهٔ `attachment_*` و `entity_upsert` قرار داشت. چون این تابع پیش از آن نقطه هیچ‌جا تعریف نمی‌شد، شرط در هر درخواست true می‌شد، زنجیره همان‌جا تمام می‌شد و:

- شرط هیچ‌یک از شاخه‌های `entity_upsert` / `entity_delete` / `entity_restore` / `revoke_invoice_ref` / `entity_tombstones_neutralize` **هرگز ارزیابی نمی‌شد** (و `else` unknown_action هم به همان زنجیرهٔ مرده بند بود)؛
- هر فرمان entity پاسخ خالی `{"ok":true,"rev":N+1,"result":{"keys":[]},"data":[]}` می‌داد — **«موفقیت کاذب» بدون نوشتن یک بایت**؛ دقیقاً «ثبت روی سرور را زدم ولی روی سرور نیست».

**اصلاح:** گارد به‌طور کامل به پیش از دیسپچر منتقل شد (قبل از `$readOnly = …`) و زنجیرهٔ elseif دوباره پیوسته است.

**اثبات (E2E):** سناریوی کامل کارفرما روی **خودِ کد سرور** با PHP واقعی (php-wasm؛ فایل‌های `api/*.php` بدون تغییر) اجرا شد: seed سه مشتری → توکن admin → `data_pull` (شماره نیست) → `entity_upsert` با `_ccEdit/_ccBaseAt` و شمارهٔ فارسی «۰۹۱۲۱۲۳۴۵۶۷» → بازخوانی cache-busted (شماره دیده می‌شود، شماره‌های قبلی سالم، مهرهای _cc روی سرور نمانده‌اند) → `data_push` مجموعهٔ کهنهٔ دستگاهِ دیگر (بدون reject/conflict/massDeletion) → بازخوانی نهایی (شماره زنده ماند — اتحاد R7). **۲۴ سنجه، همه PASS** — پیش از اصلاح، همان سناریو روی همان کد، پاسخ خالیِ `ok:true` بدون نوشتن می‌گرفت.

### ۲) بازنویسی ماژول تشخیص سینک تماس (`crm/contact-sync-diag.js`) — R1–R4

- **یک مسیر نوشتن:** فقط `ptfEntityUpsert` (با پيش‌بررسی `window.ptfEntityUpsert` و `PTF_ENTITY_CMD_ENABLED['ptf_crm_customers']`)؛ در دسترس نبود ⇒ پیام صادقانه «سرویس ثبت فرمان روی این صفحه بارگذاری نشده» با **صفر نوشتن** — مسیرهای جایگزین `ptfEntitySaveCollection`/`setData` محلی (موجب موفقیت کاذب) حذف شدند.
- **ثبت = فرمان + بازخوانی تأیید:** پس از ACK، همان لحظه `data_pull` تازه (پارامتر buster `_csd=` + `cache:'no-store'`)؛ «✅ ثبت و تأیید شد» فقط وقتی شماره در پاسخ سرور دیده شود؛ سپس بازچشم‌انداز خودکار جداول + toast. «⛔ تأیید نشد» با راهنمای علت (استقرار ناهمگن/نقش/نشست).
- **نرمال‌سازی شماره:** ارقام فارسی/عربی، پیشوندهای `0098/98/0` و `+` — «۰۹۱۲…»، «۹۸۹۱۲…» و «+۹۸۹۱۲…» هم‌ارز شناخته می‌شوند.
- **خواندن غیرقابل‌کش:** هر دو API هدر `Cache-Control: no-store, no-cache, must-revalidate, private` + `Pragma: no-cache` می‌فرستند؛ پاسخ فرمان/رسید هرگز کش نمی‌شود.

### ۳) بامپ نسخه و رگرسیون

- `SD_SERVICE_VERSION`، `PTF_CRM_RELEASE`، `crm/manifest.json`، `crm/sw.js` (RELEASE/ASSET_VERSION/CACHE)، `VERSION.json` و ۱۱۴ پین `?v=` در `crm/index.html` ⇒ **v34.39.40**.
- **تکمیل نقاط رسمی نسخه که در بامپ اولیه جا مانده بودند:** `crm/clear-cache.html` (تیتر + `window.VER`)، `crm/device-reconnect.html` (تیتر + دکمهٔ ورود + `VER`)، `crm/shell.js` (fallback release + `PTF_CMS_JS_VER`)، `crm/cms.js` (`PTF_CMS_JS_VER` — بنر کهنگی کش اکنون درست تراز می‌خواند).
- `crm/index.html` و `crm/sw.js` از مسیر «سرویس‌ورکر کش قدیمی» پاکسازی شدند (کش قدیمی دور ریخته می‌شود).
- ** tester680-v34.39.40-contact-sync-verified.js: 53 PASS / 0 FAIL** (قرارداد تک‌مسیری نوشتن، بازخوانی تأیید، نرمال‌سازی شماره، نسخه‌ها، + بهداشت A10: هیچ LS مستقیم در سورس ماژول).
- **پین ۱۵۸ تستر (۶۷۱ مورد) با ابزار رسمی `bump-version-pins.js` از 34.39.24 به 34.39.40 به‌روز شد** (هویت نام‌فایل‌های تاریخی تسترها محفوظ ماند) — گیت CI از **۱۶۲ PASS / ۱۳۲ FAIL** (وضعیت HEAD پیش از کار) به **۲۷۹ PASS / ۱۵ FAIL** رسید؛ **صفر رگرسیون** — هر ۱۳ تسترِ قرمزِ باقی‌مانده در HEAD هم قرمز بودند (بدهی تاریخی، فهرست در گزارش RCA).
- گزارش کامل: `REPORT-CONTACT-SYNC-ROOT-CAUSE-2-2026-09-25.md` (R1–R5 با شواهد کد و خروجی E2E).

### ۴) بهداشت معماری (A10)

گارد معماری (`_tools/arch/arch-guard.js`) دسترسی مستقیم `localStorage` در فایل‌های UI را مسدود می‌کند. نسخهٔ اول ماژول برای خواندن نقشهٔ krevs به LS مستقیم برمی‌گشت (تخلف A10 جدید). اصلاح: لایهٔ داده مالک ماند — `crm/sync.js` تابع موجود `krevs()` را با `window.ptfSyncKrevs` عمومی کرد (یک خط، فقط‌خواندنی) و `contact-sync-diag.js` **فقط** از همین accessor می‌خواند؛ نبودِ accessor یعنی نقشهٔ خالی ⇒ سرور کل مجموعه را تازه می‌فرستد (برای تشخیص، درست و کافی). گارد A10 اکنون روی کل مخزن سبز است.

## فایل‌های تغییر‌یافته

**کد (۱۲ فایل — مجموعهٔ استقرار):** `api/sales-domain.php`، `api/crm.php`، `crm/contact-sync-diag.js`، `crm/sync.js`، `crm/shell.js`، `crm/cms.js`، `crm/index.html`، `crm/sw.js`، `crm/manifest.json`، `crm/clear-cache.html`، `crm/device-reconnect.html`، `VERSION.json`.
**تست/ابزار/سند:** `_tools/uat/tester680-v34.39.40-contact-sync-verified.js`، `_tools/uat/e2e-contact-sync-v34.39.40.js` (E2E روی PHP واقعی)، ۱۵۸ تستر (فقط پین نسخه)، `REPORT-CONTACT-SYNC-ROOT-CAUSE-2-2026-09-25.md`، `RELEASE-NOTES-v34.39.40.md`.

## چک‌لیست استقرار (cPanel — دستی، آپلود هم‌زمان کل مجموعه)

1. آپلود هم‌زمان هر ۱۲ فایل کد (فهرست بالا) — مهم‌تر از همه: `api/sales-domain.php` (اصلاح R5) باید با `crm/contact-sync-diag.js` و `crm/sync.js` هم‌زمان برود.
2. Ctrl+F5 (دو بار اگر سرویس‌ورکر فعال است) → در کنسول `window.PTF_CRM_RELEASE` باید `v34.39.40` باشد.
3. ابزار تشخیص سینک تماس → انتخاب مشتری → واردکردن شماره → «ثبت روی سرور» → باید «**✅ ثبت و تأیید شد**» بیاید و جدول خودکار تازه شود.
4. اگر «⛔ تأیید نشد» آمد: یعنی استقرار ناهمگن است (فایلی جا مانده) — کل مجموعه دوباره آپلود شود.

## خرابی‌های تاریخیِ قرمز (خارج از این اسپرینت — در HEAD هم قرمز بودند)

۱۳ تستر: arch-guard A3 (offer-fx-convert.js — empty-equality، از کامیت تاریخی)، tester676 (CONTACT-ROOTS v34.39.22 — مکانیزم `sync_contact_fields_fill` در v34.39.30 عمداً با R7 جایگزین شد و تستر قدیمی به‌روز نشده)، tester675 (UI-STABILITY)، tester648/608 (lastmod/ساختار knowledge-center)، tester431/443/457/552/565/575/590/615/616 (متنوع و تاریخی). فهرست کامل و دلایل در `REPORT-CONTACT-SYNC-ROOT-CAUSE-2-2026-09-25.md`.
