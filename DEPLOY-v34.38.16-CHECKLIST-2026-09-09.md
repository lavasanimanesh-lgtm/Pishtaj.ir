# استقرار v34.38.16 — رفعِ گیرکردنِ «دریافت کامل اولیه» (PULL-RESILIENCE)

> تاریخ ۲۰۲۶-۰۹-۰۹ · این نسخه روی مخزن آماده است و تمام تسترهای sync/integrity سبزند.
> نسخه در `index.html`، `manifest.json`، `sw.js`، `api/sales-domain.php` و `VERSION.json` = **v34.38.16**؛ پرچم `PTF_CRM_SNAPSHOT_V2=false` (بوت از `data_pull` تک‌مرحله‌ای، همان BOOT-REVERTِ .15).

## این نسخه چه می‌کند (و چه نمی‌کند)
- **می‌کند:** وقتی سرور برای **یک** کلید entryِ کاملِ manifest نمی‌فرستد (مثل `ptf_crm_commission_records` که مقدارش شیء/تهی است و `count/kind` درست نمی‌دهد)، دیگر کلِ snapshot رد نمی‌شود؛ آن کلید «unavailable» می‌شود (نسخهٔ سالم محلی حفظ می‌شود) و **بقیهٔ کلیدها اعمال می‌شوند** ⇒ دریافت کامل اولیه تمام و نشانگر سبز می‌شود. `integrity-mismatch` واقعی همچنان fail-closed است.
- **نمی‌کند / باید جدا پیگیری شود:** این مقدارِ شیءِ `{}` برای `commission_records` روی سرور (به‌جای آرایه) یک ناهنجاریِ سمتِ سرور است. کلاینتِ .16 گیرکردن را برطرف می‌کند، ولی بهتر است بعداً سرور آن را به آرایه (`[]`) نرمال کند تا رکوردها/گزارش‌های پورسانت هم‌شکل باشند (با access به هاست).

---

## گام ۱ — استقرار فایل‌ها
این فایل‌های تغییرکرده را روی استیجینگ و سپس پروداکشن آپلود کن (کل پوشهٔ `crm/` و `api/`):
- `crm/sync.js` ، `crm/client-server.js` (خودِ اصلاح)
- `crm/index.html` ، `crm/manifest.json` ، `crm/sw.js` ، `crm/shell.js` ، `crm/cms.js` ، `crm/clear-cache.html` ، `crm/device-reconnect.html` (bump نسخه + کش‌باسترها)
- `api/sales-domain.php` ، `VERSION.json`
- فایل‌های `_tools/uat/*` فقط مخصوص CI/تست‌اند و روی هاست لازم نیستند (به‌جز اگر deploy کل مخزن می‌کنی، ضرری ندارند).

## گام ۲ — اجباری‌کردن رفرش روی هر دستگاه
بعد از آپلود، به‌خاطر سرویس‌ورکرِ نسخه‌دار، هر دستگاه باید باندلِ جدید را بگیرد:
1. هر مرورگرِ باز را ببند (یا یک بار hard-refresh: `Ctrl+Shift+R` / `Cmd+Shift+R`).
2. نسخهٔ خودش (سپرِ نسخه) یک‌بار رفرش اجباری می‌دهد؛ چند دقیقه صبر کن و صفحه را یک‌بار تازه کن.
3. تأیید روی هر دستگاه در Console:
   ```js
   JSON.stringify({ rel: window.PTF_CRM_RELEASE, snapshotV2: window.PTF_CRM_SNAPSHOT_V2 })
   ```
   باید `{"rel":"v34.38.16","snapshotV2":false}` بدهد. اگر `rel` هنوز `v34.38.15` بود ⇒ کش هنوز قدیمی است (رفرش اجباری).

## گام ۳ — تأییدِ رفع روی یک دستگاهِ قبلاً مشکل‌دار
در همان دستگاهی که بنر زرد و «دریافت ناتمام» و خطای `commission_records:integrity-manifest-missing` را داشت، بعد از رفرش:
1. صبر کن bootstrap تمام شود (تا ۲۰ ثانیه).
2. Console:
   ```js
   JSON.stringify({ bootstrapped: !!window._ptfSyncBootstrapped,
     err: (()=>{let e=null;try{e=JSON.parse(localStorage.getItem('ptf_sync_last_error')||'null')}catch(_){}
        return e&&{scope:e.scope,reason:e.reason,status:e.status}})() })
   ```
3. **موفق:** `bootstrapped: true` و `err` یا null است یا یک خطای pull/مربوط به کلیدِ non-commission نیست. نشانگر سبز شود، بنر زرد محو شود، و مشتریِ [customers] که در صف بود خودکار push شود.
4. انتظار داریم یک پیام هشدارِ «نسخهٔ محلی حفظ شد» برای `commission_records` دیده شود — این **طبیعی و درست** است (آن کلید unavailable است؛ دادهٔ پورسانت اگر قبلاً محلی نبوده، بعد از نرمال‌سازی سرور می‌آید).

## گام ۴ — چک نهاییِ کلیدی
- پنل‌های قبلاً خالی (پرونده، تامین، موجودی، مطالبات، هاب مالی…) پُر شوند.
- ثبتِ یک پیشنهاد/مشتری دیگر پیامِ «ابتدا همگام‌سازی قبلی» ندهد.
- روی چند دستگاه/نقش تست کن — نه فقط یک مرورگر.

---

## پاسخ به «جلوگیری از اختلالِ دادهٔ ذخیره‌شدهٔ نسخه‌های بعدی در مرورگرها»
- **JS/کش نسخه:** سرویس‌ورکر فقط کشِ `ptf-crm-v34.38.16` را نگه می‌دارد و کشِ نسخه‌های قبلی را هنگام فعال‌شدن حذف می‌کند (`sw.js`). کافی است همه‌چیز هم‌نسخه شود و به دستگاه‌ها یک رفرش فرصت بدهی.
- **داده (localStorage `ptf_crm_*`):** ⛔ پاک نکن. این‌ها رکوردهای واقعی‌اند؛ پاک‌کردنشان = حذف داده. بعد از وصل‌شدن، گردش bootstrap/pull سرور را مرجع می‌کند و حالت‌های صفِ به‌جامانده از نسخه‌های نامرتب خودبه‌خود هم‌سو می‌شوند.
- اگر دستگاهی بعد از این‌ها هنوز sync عجیب داشت، فقط از ابزارِ خودِ اپ استفاده کن (`crm/clear-cache.html` یا تنظیمات → تشخیص/پاک‌سازی کش محلی) که فقط کش/حالت موقت را تمیز می‌کند و به داده دست نمی‌زند.
