# فاز ۱۱ (v34.0.14-alpha) — ویرایش/حذف چک + اصلاح/حذف آنی اثر مالی + لینک کیفیت داده

**برنچ:** `arena/019fcd1d-pishtaj-ir`
**درخواست کارفرما:** امکان ویرایش/حذف چک نبود؛ تب کیفیت داده در مورد چکها باید به چک مربوطه و مودال اصلاح چک لینک شود؛ در ویرایش مبلغ یا حذف چکِ مالی، اثر مالی روی حساب تأمینکننده آنی اعمال و در گردش حساب اصلاح شود.

---

## تحلیل وضعیت قبلی
- چکها فقط «ابطال» (void) داشتند؛ **ویرایش** به شکل مودال مجزا برای یک چک مشخص نبود (`chEdit` فقط به تب چک میرفت).
- **حذف واقعی** برای چکهای مالیِ شرکت که به تأمینکننده وصلاند، فقط `slPaymentVoid` میکرد (رکوردِ payment میماند؛ گردش حساب کامل حذف نمیشد).
- تب **کیفیت داده** برای چکها به `chEdit` لینک بود که فقط به تب میرفت، نه به مودالِ اصلاحِ همان چک.

---

## اصلاحات

### هسته (`crm/cheque-module.js`) — بخش خارج از IIFE (پس از `ptfChequeReverseIssued`)
- `ptfChequeUpdate(cd, patch)`: بهروزرسانی چک در کلید درست (issued/received/legacy).
- `ptfChequeApplyFinancialAmount(cd, newAmt)`: **اصلاح آنی مبلغ** paymentِ تأمینکنندهٔ مرتبط با چک مالیِ صادره → گردش حساب همان لحظه اصلاح میشود.
- `ptfChequeDelete(cd)`: **حذف کامل چک** + حذف کامل payment (گردش حساب) از supplier-finance (نه void).

### UI (`crm/cheque-panel.js`)
- دکمههای **«✏️ ویرایش»** و **«🗑 حذف»** در هر ردیف چک (صادره و وارده) در پنل چک هاب مالی.
- `ptfChequeEditUi(cd)`: مودال ویرایش چک (شماره/صیادی/مبلغ/تاریخ/ذینفع/بانک/یادداشت) + هشدار «این چک اثر مالی روی حساب تأمینکننده دارد» در صورت مالی بودن.
- `ptfChequeEditSave(cd)`: ذخیرهٔ ویرایش؛ اگر مبلغ چک مالیِ صادره تغییر کرد، `ptfChequeApplyFinancialAmount` را صدا میزند تا اثر مالی آنی اصلاح شود.
- `ptfChequeDeleteUi(cd)`: تأیید + `ptfChequeDelete` که اثر مالی/گردش را هم کامل حذف میکند.
- `ptfChequeEditFromQuality(cd)`: به تب چک میرود و مودال اصلاحِ همان چک را باز میکند.

### لینک کیفیت داده (`crm/data-quality.js`)
- چکهای مشکلدار (مالکیت/تاریخ نامشخص) حالا با `ptfChequeEditFromQuality` به **مودال اصلاحِ همان چک** لینک میشوند (بهجای `chEdit` که فقط به تب میرفت).

---

## تست‌ها
- **tester310** جدید (۱۴/۰): هستهٔ ویرایش/حذف + اصلاح آنی مبلغ اثر مالی + لینک کیفیت داده.
- بهروزرسانی tester307 (۸/۰) برای لینک جدید چک.
- رگرسیون کامل سبز (۲۰ تستر، ۰ شکست).
- بامپ نسخه → **v34.0.14-alpha**.
- تغییرات `.github/workflows/*` در PENDING-workflow-changes ثبت شد.

*تولیدشده توسط Arena Agent — ویرایش/حذف چک و اصلاح/حذف آنی اثر مالی.*
