# ارزیابی پنل «🧾 فاکتورها» — نمایش داده نشدن فاکتورهای ثبت‌شده و ارجاع‌شده

> **وضعیت (همان روز): گام ۱ اجرا و منتشر شد — v34.37.2.**
> لایهٔ نمایش اصلاح شد (بخش «فاکتورهای ثبت‌شده بدون ردیف ارجاع» + تطبیق نرمال‌شده + سربرگ تشخیصی و بازخوانی از سرور + بازگشت جستجو).
> جزئیات و جدول «آزمون جهش» در `RELEASE-NOTES-v34.37.2.md`. یافته‌های **F2 · F7** (گارد `revise_award` و فرمان `reconcile_invoice_refs`) و **F4/F1** در سطح داده، **گام ۲** هستند و هنوز انجام نشده‌اند — متن زیر بدون تغییر و به‌عنوان تحلیل مرجع مانده است.


- **تاریخ:** ۲۰۲۶-۰۹-۰۵ (۱۴۰۵-۰۶-۱۴)
- **پایه:** شاخهٔ `arena/01a0723f-pishtaj-ir` روی کامیت `1952dbe` — `VERSION.json` = **CRM v34.37.1**
- **گزارش کارفرما:** «در قسمت فاکتورها، فاکتورهای ثبت‌شده و ارجاع‌شده نمایش داده نمی‌شوند.»
- **نوع کار:** **فقط ارزیابی — هیچ فایل کدی تغییر نکرد.** تنها همین سند اضافه شده است.
- **روش:** خواندن کامل زنجیرهٔ نمایش (پنل ← منبع داده ← نوشتن ارجاع ← فرمان سروری ← همگام‌سازی) + **اجرای کدِ واقعی پنل در یک هارنس رفتاری** (`vm`) با پنج شکل داده‌ای واقع‌نما (خروجی در پیوست الف).

---

## ۰) خلاصهٔ مدیریتی

**پنل فاکتورها یک «فهرست فاکتور» نیست؛ یک «فهرست ارجاع» است.** تنها ورودی ردیف‌ها، سوابق `ptf_crm_offers` هستند که فیلد `invRef` دارند (`crm/official-invoice-v2.js:76`). فاکتورهای ثبت‌شده فقط **داخل** همان ردیف‌ها (به‌عنوان کارت، با تطبیق `invoice.offerNo === offer.no` در خط `:91`) رندر می‌شوند. پس:

> اگر `invRef` روی پیشنهادها در دسترسِ آن دستگاه نباشد، **هر دو** دستهٔ گزارش‌شده (هم ارجاع‌شده‌ها و هم فاکتورهای ثبت‌شده) **هم‌زمان و کامل** ناپدید می‌شوند و تنها پیام «ارجاع آماده ثبت فاکتور رسمی وجود ندارد.» می‌ماند — دقیقاً همان چیزی که گزارش شده است. این یک باگ رندر نیست؛ یک **وابستگی تک‌منبعیِ معماری** است.

| # | یافته | شدت | وضعیت |
|---|---|---|---|
| **F1** | پنل فقط از `offer.invRef` تغذیه می‌شود؛ فاکتور ثبت‌شدهٔ یتیم (بدون ارجاعِ فعال روی پیشنهاد) **هیچ‌جا** در این پنل دیده نمی‌شود | **P0** (علت مستقیم گزارش) | تأییدشده با هارنس |
| **F2** | «رویژن ابلاغ» (`revise_award`) بدون هیچ گاردی `invRef` را پاک می‌کند؛ اگر تیک «ابطال فاکتورها» نخورده باشد، فاکتور فعال می‌ماند و **ردیفش برای همیشه از پنل می‌رود** | **P0** (ریسک مالی/ممیزی) | تأییدشده در کد |
| **F3** | پنل «یک‌بار» رندر می‌شود و پس از رسیدن دادهٔ سرور **هیچ بازرندر خودکاری ندارد** — برای نقش حسابدار، صفحهٔ فرود پس از ورود *همین پنل* است (`rbac.js:1584` + `rbac.js:25`) در حالی که `getData` آینهٔ محلی را بی‌درنگ برمی‌گرداند و `sharedPull` ناهمزمان است | **P1** | تأییدشده در کد |
| **F4** | در حالت «مهاجرت گیرکرده» (سند `ARENA-CRM-MIGRATION-STUCK-ASSESSMENT-2026-09-04.md`) اگر نوشتن projection در `localStorage` پر شکست بخورد، `ptfBApplyServerProjection` **بی‌صدا `false` برمی‌گرداند** و آینهٔ کهنه باقی می‌ماند ⇒ `ptf_crm_offers` خالی/کهنه ⇒ F1 فعال می‌شود | **P0** (محتمل‌ترین علتِ محیطی روی دستگاه کارفرما) | زنجیره تأییدشده؛ نیازمند یک آزمون ۲ دقیقه‌ای روی میزبان |
| **F5** | جستجوی فاکتورها (v34.9.2) با بازنویسی پنل در v2 **حذف شده** — `invSrch` فقط در پنل بازنشستهٔ `rbac.js:895` وجود دارد | P2 (رگرسیون قابلیت) | تأییدشده |
| **F6** | مقایسهٔ `i.offerNo === o.no` سخت است؛ هر اختلاف (فاکتور مهاجرت‌شدهٔ بدون `offerNo`، شمارهٔ جابه‌جاشده، فاصلهٔ تهی) ⇒ «⏳ بدون فاکتور» **و** فعال شدن دوبارهٔ دکمهٔ «+ ثبت فاکتور رسمی» = ریسک سند مالیاتی تکراری | **P1** | تأییدشده با هارنس |
| **F7** | چهار نویسنده/پاک‌کنندهٔ `invRef` با گاردهای ناهمسان؛ گارد «جلوگیری از ثبت دوباره» (`ptfActiveInvoiceOfOffer`) هم دقیقاً همان تطبیق شکنندهٔ F6 را استفاده می‌کند ⇒ حصار هم با یک دادهٔ کج از کار می‌افتد | P1 | تأییدشده در کد |
| **F8** | پوشش تست: ۵ تستر مربوطه سبزند (شامل `tester598` رفتاری، ۵۰/۰) ولی **هیچ‌کدام حالت «فاکتور ثبت‌شده + نبودِ ارجاع» را نمی‌سنجد**؛ پیام خالیِ پنل هم هیچ عیب‌یابی/شمارشی ندارد | P2 (فرآیند) | تأییدشده |

**جمع‌بندی یک‌جمله‌ای:** کدِ رندرِ پنل برای «مسیر سالم» درست کار می‌کند؛ آنچه می‌شکند **قرارداد داده** است — یک کلید تک‌نویسنده (`offer.invRef`) تصمیم می‌گیرد که آیا یک فاکتورِ ثبت‌شده اصلاً دیده شود یا نه، و هیچ مسیر جایگزین، شمارش، جستجو، عیب‌یابی یا بازتلاشی در پنل وجود ندارد.

---

## ۱) پنل واقعاً چه می‌کند (زنجیرهٔ داده)

```
پروندهٔ فروش (sfInvoiceRef)                                  ← تنها نویسندهٔ invRef
  crm/salesfiles.js:796  o.invRef = { by, role, t, fromFile, awardDoc,
                                      rialBasis, rialRate, rialRateDerived,
                                      rialBasisKind, rialTotal, fxNo, fxCurrency }
  crm/salesfiles.js:797  ptfEntitySaveCollection('ptf_crm_offers', offers)   ← فرمان entity_upsert
        ↓
ذخیرهٔ فاکتور (پنل فاکتورها → مودال → سرور)
  api/sales-domain.php:2347  register_invoice / correct_invoice
  api/sales-domain.php:2368  … 'offerNo' => $offerNo (از بدنه یا case['wonOffer']) …
  'status'=>'active', 'isOfficial'=>true
        ↓
رندر (crm/official-invoice-v2.js)
  :76  offers = data('ptf_crm_offers').filter(o => o.invRef && !o.rialOf)   ← ← ← گلوگاه
  :91  list   = invs.filter(i => i.offerNo === o.no)                          ← تطبیق سخت
  :105 وضعیت: ✅ فاکتور … / 🚫 ابطال‌شده / ⏳ بدون فاکتور
  :170 اگر ردیفی نباشد → «ارجاع آماده ثبت فاکتور رسمی وجود ندارد.» و پایان
```

سه نکتهٔ ساختاری که همهٔ یافته‌های بالا از آن‌ها زاده می‌شوند:

1. **حلقهٔ واگرا:** وجود ردیف ⇐ `invRef`؛ محتوای ردیف ⇐ `offerNo`. هیچ‌گاه «فهرست فاکتورهای ثبت‌شده» به‌عنوان مبدأ خوانده نمی‌شود.
2. **تک‌نویسنده‌بودنِ حیاتی:** `invRef` فقط در `salesfiles.js:796` نوشته می‌شود و در **سه** مسیر سروری پاک می‌شود: `revise_award` (`:1839`)، `revoke_invoice_ref` (`:2713`)، `revoke_orphan_delete` (بازگردانی برد یتیم، `:1916`). هیچ بازسازی/هم‌ترازی معکوسی (از روی فاکتورهای فعال) وجود ندارد.
3. **نبود هرگونه «دادهٔ کافی نیست» signaling:** پیام خالی بدون شمارنده است — نه تعداد `invs`، نه تعداد ارجاع‌ها، نه وضعیت همگام‌سازی، نه دکمهٔ بازخوانی. پشتیبانی نمی‌تواند از روی صفحه بگوید «داده نیامده» یا «داده هست و نمایش نداریم».

---

## ۲) شواهد اجرایی (هارنس رفتاری روی کد واقعی)

اسکریپت: `node /home/user/inv-eval/panel-eval.js` — برشِ زندهٔ `renderInvoices` از `crm/official-invoice-v2.js` (همان تکنیک `tester598`) در `vm`، با `getData` واقع‌نما. نتایج:

| سناریو | ردیف‌های رندرشده | فاکتور ثبت‌شده دیده می‌شود؟ | پیام/رفتار پنل |
|---|---|---|---|
| S۱ `invRef` + فاکتور (مسیر سالم) | ۱ | ✔ «✅ فاکتور F-1001» + کارت کامل | سالم |
| **S۲ دو فاکتور ثبت‌شده، هیچ `invRef`** | **۰** | **✘** | فقط «ارجاع آماده ثبت فاکتور رسمی وجود ندارد.» (HTML = ۱۰۳ بایت) |
| **S۳ `invRef` هست، `invoice.offerNo` خالی** | ۱ | ✘ | «⏳ بدون فاکتور» **+ دکمهٔ «+ ثبت فاکتور رسمی صادرشده» فعال** |
| S۴ `invRef` روی نسخهٔ ریالی (`rialOf`) | **۰** | ✘ | فیلتر `!o.rialOf` کل ردیف را می‌اندازد |
| S۵ `offerNo` با یک فاصلهٔ تهی (`"CO-500 "`) | ۱ | ✘ | مثل S۳ — تطبیق سخت می‌بازد |
| S۶ پروندهٔ فروش `void` (ارجاع و فاکتور سالم) | ۱ | ✔ | «⛔ پرونده فروش یافت نشد» ولی فاکتور دیده می‌شود |

S۲ بازتولید کاملِ گزارش کارفرماست: **دو فاکتورِ ثبت‌شده در `ptf_crm_invoices` وجود دارند و پنل هیچ چیزی نشان نمی‌دهد.**

---

## ۳) عیب‌یابی تفکیکی روی میزبان (قبل از هر تغییر کد — ۳ تا ۵ دقیقه)

این چهار آزمون، «باگ نمایش» را از «داده نرسیده» جدا می‌کنند. تصمیمِ اصلاح به جواب همین‌ها وابسته است:

| # | آزمون | اگر … ⇒ نتیجه |
|---|---|---|
| **T1** | پنل **«💰 مطالبات»** را باز کنید (این پنل فاکتور-محور است: `rbac.js:1176-1179` از `ptf_crm_invoices` می‌خواند) | فاکتورها آنجا **هستند** ⇒ داده سالم است، مشکل **F1/F2/F7** (پیوند ارجاع) است. آنجا هم **نیستند** ⇒ مشکل **F3/F4** (آینهٔ خالی/کهنه) است |
| **T2** | در «پیشنهادها»، روی همان CO ببینید نشانگر **«🧾 ارجاع فاکتور»** هست یا نه (`offers.js:562`) | نشانگر **هست** ⇒ `invRef` در داده هست و فیلتر دیگری (S۴/S۶ یا `can()`) ردیف را حذف کرده. نشانگر **نیست** ⇒ `invRef` پاک/گم شده (F2 یا F4) |
| **T3** | تنظیمات ← «وضعیت دستگاه» و «تشخیص همگام‌سازی»: `enabled && !synced`؟ نوار زرد؟ «آخرین خطا»؟ | همان حالت **گیرکردگی مهاجرت** (سند ۲۰۲۶-۰۹-۰۴) ⇒ `ptfBMirrorActive()` = `getFlag() && isSynced() && idbUsable()` (`client-server.js:87`) **خاموش** ⇒ سنگینیِ آینه در `localStorage`؛ اگر سقف پر شود، `localSet` رد و `ptfBApplyServerProjection` بی‌صدا `false` می‌دهد (`client-server.js:130-131`) ⇒ F4 |
| **T4** | **بستن و بازکردن دوبارهٔ همان پنل** (رفتن به بخش دیگر و برگشتن) | اگر دفعهٔ دوم ردیف‌ها آمد ⇒ **F3** (رندر قبل از هیدریشن، بدون بازرندر). اگر هیچ‌وقت نیامد ⇒ F1/F2/F4 |
| T5 (کمکی) | جستجوی `audit` برای عبارت **«ارجاع فاکتور رسمی»** (`salesfiles.js:803`) | ردیفِ audit وجود دارد ولی `invRef` نیست ⇒ ارجاع **ثبت و بعداً پاک شده** (F2) — نه «هرگز ثبت نشده» |

**نکتهٔ حیاتی برای T1:** فاکتورها در این وضعیت‌ها **گم نمی‌شوند**؛ فقط از دیدِ پنلِ فاکتورها پنهان‌اند. `ptf_crm_invoices` و رسیدها/تخصیص‌ها (`sd_rebuild_allocations`) دست‌نخورده‌اند. هیچ مسیر اصلاحی نباید از «حذف و ثبت دوباره» شروع کند — این `duplicate_invoice_identity` (۴۰۹) و دوباره‌کاری مالیاتی می‌سازد.

---

## ۴) یافته‌های کد (F1…F8) با مرجع و تحلیل

### F1 — پنل ارجاع‌محور است، نه فاکتور-محور (P0)
`official-invoice-v2.js:76` مبدأ ردیف‌ها را `ptf_crm_offers` می‌گذارد. نتیجه: فاکتوری که (الف) از مسیر مهاجرت/ابزار/ادغامِ رکورد، `offerNo` ندارد، (ب) به رویژنی با شمارهٔ دیگر پیوند داشته، یا (ج) سوابقش از `invRef` پاک شده — **در این پنل هیچ‌جا ظاهر نمی‌شود**، حتی وقتی `status=active` و مطالبهٔ باز دارد.
پیامد دوم: با نبودِ ردیف، دسترسی‌های داخل کشو («اصلاح اطلاعات»، «ابطال»، «📎 اسناد») هم از دست می‌روند؛ تنها راهِ رسیدن به سند، پنل مطالبات یا حذف قطعی ادمین است.

### F2 — «رویژن ابلاغ» بی‌گارد، `invRef` را می‌اندازد (P0 — ریسک مالی)
`api/sales-domain.php:1648` (`revise_award`) در انتهای کار، خط `:1839`:

```php
unset($parent['invRef']);
```

تنها گارد فاکتوریِ این فرمان در `:1804` است:

```php
if (!$voidInvoices && $officialCount > 0 && $newTotal < $oldTotal) → 409 official_invoice_blocks_decrease
```

یعنی «ابطال اجباری فاکتورها» فقط وقتی **کاهش مبلغ** باشد فعال است. در سناریوی کاملاً مجازِ «مبلغ افزایش یابد یا ثابت بماند + تیک «ابطال فاکتورها» نخورده» (`crm/case-revision.js:373` — چک‌باکس پیش‌فرض **خاموش** است):
① فاکتور رسمی فعال روی پرونده باقی می‌ماند، ② `invRef` پاک می‌شود، ③ ردیفِ پنل فاکتورها ناپدید می‌شود (F1)، ④ با `offer.invRef` رفتن، `sfStageOf` هم سیگنال مرحلهٔ ۸ را از دست می‌دهد (`salesfiles.js:372`؛ فقط `(docs.invoices||[]).length` در `:475` مانع پس‌رفتن کامل مرحله می‌شود). ⑤ برای دیدن دوبارهٔ همان فاکتور باید **مجدداً ارجاع** ثبت کرد.
این «غيبِ هم‌ترازی معکوس» است: سرور می‌تواند از روی فاکتورهای فعالِ همان پرونده، `invRef` را بازسازی یا دست‌کم یک هشدار بلوک‌کننده بدهد — هیچ‌کدام نیست.

### F3 — رندر یک‌باره بدون بازگام‌سازی (P1)
`rbac.js:1607-1610` هنگام ورود به پنل، `buildInvoices()` و سپس `renderInvoices()` را **همزمان** صدا می‌زند. در فاز B، `getData` (`client-server.js:1112-1134`) ارزشِ آینهٔ محلی را فوراً برمی‌گرداند و `sharedPull(...)` را **ناهمزمان** می‌فرستد (با `PULL_MIN_GAP = 2000ms` و کش `CACHE_TTL = 30000ms`). هیچ جایی `renderInvoices` را «پس از رسیدن projection» صدا نمی‌زند — تنها شنوندهٔ رویداد `ptf:sync-ready` ماژول `opex.js:921` است (grep سراسری). و بدتر: برای نقش **حسابدار** (`rbac.js:25` → `panels: ['inv', …]` بدون `dash`)، `rbac.js:1583-1584` در ۲۰۰ میلی‌ثانیه‌ایِ پس از ورود `goPanelByName(r.panels[0])` را می‌زند؛ یعنی **اولین چیزی که حسابدار بعد از لاگین می‌بیند، همین پنل است، روی دادهای که هنوز تازه نشده.** اگر دستگاه سرد باشد (لاگین تازه، کش پاک‌شده، تبی که تازه باز شده) پیام «ارجاع آماده … وجود ندارد» نمایش داده می‌شود و تا تغییر پنل، همان‌جا می‌ماند.

### F4 — زنجیرهٔ «مهاجرت گیرکرده ⇒ آینهٔ کهنه ⇒ پنل خالی» (P0 محیطی)
سند `ARENA-CRM-MIGRATION-STUCK-ASSESSMENT-2026-09-04.md` (دیروز) با شدت P0 تأیید می‌کند که دستگاه در حالت `enabled && !synced` گیر کرده و «دادهٔ سنگین در localStorage می‌ماند و مرورگر پر می‌شود». دو حلقهٔ بسته در همین مسیر، مستقیماً پنل فاکتورها را می‌کُشد:
- `ptfBMirrorActive()` (`client-server.js:87`) ⇒ `isSynced()` کاذب ⇒ آینهٔ IDB خاموش ⇒ `ptf_crm_offers` (پرحجم‌ترین کلید CRM) در `localStorage` می‌ماند؛
- `ptfBApplyServerProjection` (`client-server.js:124-131`) اگر `localSet` به‌دلیل سقف quotas رد شود، `delete cache[k]; return false` — **بدون هیچ پیام/توست/audit**. `sync.js:912` هم فقط `return false` می‌کند و watermark جلو نمی‌رود.
نتیجه: خواندن‌های بعدی همان آرایهٔ کهنه/خالی را می‌بینند و پنل خالی است، در حالی که سرور داده را دارد. **این تنها یافته‌ای است که با «کدِ سالم» هم رخ می‌دهد؛ به همین دلیل T3 باید اول از همه انجام شود.**

### F5 — رگرسیون جستجو (P2)
`rbac.js:891-895` (پنل بازنشسته) هنوز `<input id="invSrch">` را می‌سازد و `:921` روی آن فیلتر می‌کند؛ `official-invoice-v2.js:45` (پنل زنده) آن را نساخته و `renderInvoices` زنده هیچ فیلتر جستجویی ندارد. هر چیزی که روی `invSrch` اتکا کند مرده است. این همان الگوی «پنل دو‌نسخه‌ای» است که در v34.37.1 باعث شد دکمهٔ «لغو ارجاع» در کد مرده افتاده باشد (RELEASE-NOTES-v34.37.1 §۳الف) — یعنی خطر تکراری، نه یک نقص یک‌باره.

### F6 — تطبیقِ یک‌مقداریِ `offerNo` (P1)
`:91` و `:182` (`ptfActiveInvoiceOfOffer`) هر دو `i.offerNo === o.no` را بدون نرمال‌سازی (trim / یکسان‌سازی ارقام فارسی / fallback به `caseId`) تطبیق می‌دهند. در S۳ و S۵ هارنس: فاکتور فعال وجود دارد، ولی پنل «بدون فاکتور» می‌گوید **و دکمهٔ ثبت را دوباره فعال می‌گذارد**. لایهٔ دوم قفل هم چون از همان تطبیق استفاده می‌کند، جلویش را نمی‌گیرد. سرور در `register_invoice` تنها `duplicate_invoice_identity` (شماره/شناسه مودیان تکراری) را می‌گیرد — پس اگر شمارهٔ فاکتورِ دوم فرق کند، **سند مالیاتی دوم روی همان پرونده ثبت می‌شود**.

### F7 — حصارِ ناهمگون روی `invRef` (P1)
نویسنده: `salesfiles.js:796` (با گارد `already` در `:748/:787`). پاک‌کننده‌ها با گاردهای ناهمسان: `revoke_invoice_ref` فاکتور فعالِ هم‌`offerNo` را می‌بیند و ۴۰۹ می‌دهد (`api/sales-domain.php:2704-2708` — خوب)، ولی `revise_award` (F2) و `revoke_orphan_delete` (خط `:1916` که برد یتیم را برمی‌گرداند) همان دقت را ندارند. هیچ «بازسازی `invRef` از روی فاکتورهای فعال» در `migration_apply_safe` هم وجود ندارد (درست همان‌جا که `caseId`/`offerNo` یتیم را وصل می‌کند).

### F8 — شکاف تست و نبودِ عیب‌یابی (P2)
`tester598` رفتاری و سبز است (۵۰/۰ — اجرا شد) ولی فقط «مسیر سالم» و «سه حالت وضعیت» را می‌سنجد؛ S۲/S۳/S۵ (پیوند شکسته) هیچ‌جا پوشش ندارند. `tester596` هم برای گاردها **پینِ رشته‌ای/regex** روی سورس است (`grep` الگوهای خاص) — همان بیماری که در `ARENA-TRIAGE-3-ITEMS-2026-09-05.md` §۰ به آن اشاره شده («تسترها رشته‌ای‌اند نه رفتاری؛ گیت سبز و باگ زنده»). در پنل هم هیچ شمارش/نسخهٔ داده/دکمهٔ بازخوانی نیست، در حالی که `ledger-report.js:131` الگوی «↻ بازخوانی» را دارد.

---

## ۵) راهکارهای پیشنهادی (بدون اجرا — برای تأیید کارفرما)

### گام ۰ — بدون کد، امروز
1. **T1…T4** (§۳) روی همان دستگاه. خروجی T1 و T3 تعیین می‌کند که کار «لایهٔ داده» است یا «لایهٔ نمایش».
2. اگر T3 حالت گیرکرده داد: **اول** خروج از بن‌بست مهاجرت (طبق §۵ سند ۲۰۲۶-۰۹-۰۴: «⬆ تلاش مجدد ارسال» و سپس آزادسازی فشرده‌سازی؛ یا فعال‌کردن آینهٔ IDB) — وگرنه هر اصلاح پنل روی آینهٔ کهنه بی‌اثر به‌نظر می‌رسد و «باگ برگشت» گزارش می‌شود.
3. از صفحهٔ تنظیمات، **`پاک‌سازی کش محلی`** فقط پس از سبز شدن وضعیت دستگاه (و خالی‌بودن صف) — در حالت فعلی طبق `client-server.js:1063-1067` قفل است و بازکردنش با کد اشتباه، دادهٔ مهاجرت‌نکرده را پاک می‌کند.

### گام ۱ — اصلاحِ کم‌ریسکِ لایهٔ نمایش (P0، بدون دست‌زدن به مدل داده)
| اقدام | محل | چرا | ریسک |
|---|---|---|---|
| افزودن «بخش فاکتورهای یتیم» به انتهای پنل: هر `invoice` فعال که `offerNo`اش به هیچ ردیفِ رندرشده نخورد، با برچسب «⚠️ پیوند ارجاع ندارد» + دکمهٔ «وصل کردن به پرونده/اقدام» | `official-invoice-v2.js:76-170` | F1 را از «ناپدید» به «دیده‌شده + قابل اقدام» تبدیل می‌کند؛ بدون تغییر سرور | کم — فقط افزودن |
| نرمال‌سازی تطبیق: `trim` + ارقام فارسی→لاتین، و fallback به `i.caseId === cid(c)` و `i.offerNo === (o.invRef.rialBasis)` | `:91`, `:182` | F6 + بستن حفرهٔ «ثبت سند دوم» | کم |
| سرِ ستون‌ها شمارش: «ارجاع‌ها: n · ثبت‌شده: k · یتیم: m · جمع باز: X» + دکمهٔ «↻ بازخوانی» (با `ptfSyncPullNow` سپس `renderInvoices`) و پیام خالیِ تشخیصی («۳ فاکتور در حافظه هست ولی ارجاع فعالی نیست — با T2 بررسی شود») | `:45`, `:170` | F3/F8: تشخیص از روی صفحه ممکن می‌شود؛ کاربر گیر نمی‌کند | ناچیز |
| برگرداندن جستجو (`invSrch`) به پنل زنده و حذف `invSrch` از پنل بازنشسته (یا حذف کامل سه تابع بازنشسته) | `:45` / `rbac.js:891-995` | F5 + ریشهٔ «ویرایش در کد مرده» | ناچیز |
| بازرندر خودکار یک‌بار پس از `ptf:sync-ready` (یا dispatch رویداد پس از اعمال projectionِ `ptf_crm_offers`/`ptf_crm_invoices` و صدا زدن `renderInvoices` در صورت باز بودن `#invWrap`) | `sync.js:882-928` / پنل | F3 | کم — باید ضدتکرار باشد (حلقهٔ رندر نشود) |

### گام ۲ — اصلاحِ قرارداد داده (P0، نیازمند تأیید کارفرما + تستر رفتاری)
1. **گارد F2:** در `revise_award`، اگر `voidInvoices=false` و فاکتور رسمی فعال روی پرونده هست، یا (الف) رویژن رد شود با `invoice_exists_for_award_revision`، یا (ب) `invRef` **بازسازی** شود (انتقال به نسخهٔ جدید با همان `rialBasis`/`rialRate` + یک `corrections` برای ممیزی). پیشنهاد (ب) است چون رویژن نباید مسیر اسناد مالی را قطع کند — ولی تصمیم با کارفرماست.
2. **هم‌ترازی بازسازی:** یک `reconcile_invoice_refs` ادمین (در «کیفیت داده») که برای هر فاکتور فعال بدون `offer.invRef`، یا `invRef` بدون فاکتور، **فقط گزارش** بدهد و با تأیید کاربر «وصل کن» انجام دهد — قرینهٔ `migration_apply_safe:2460+` که همین کار را برای `caseId` می‌کند.
3. **منبع‌واحدِ واقعی:** بلندمدت، پنل باید از `PTF.ar`/`ptf_crm_invoices` مبدأ بگیرد و `invRef` فقط «وضعیت فرایند» باشد، نه شرط دیده‌شدن سند. (بدون این، هر پاک‌شدن `invRef` یک سند مالی را از دید کاربر حذف می‌کند — الگویی که در `ARENA-AR-RECEIPT-INTEGRATION-RCA-2026-08-16.md` هم تکرار شده است.)

### گام ۳ — ضدبازگشت
تستر رفتاری تازه (الگوی `tester598`) با دقیقاً همین شش سناریوی §۲ (اجرای واقعی `renderInvoices` در `vm`، نه پین رشته‌ای): «S۲ باید فاکتور یتیم را نشان دهد»، «S۳ نباید دکمهٔ ثبت دوباره را فعال بگذارد»، «S۵ نباید با یک فاصله ببازد». به‌علاوه یک تست رفتاری سروری برای F2 (رویژن با فاکتور فعال ⇒ `invRef` بازسازی/بلوک می‌شود) و افزودنشان به گیت `run-ci-gate.js`.

---

## ۶) پیوست الف — خروجی هارنس

```
■ S۱) invRef + invoice (مسیر سالم)            (ردیف‌ها: 1، طول HTML: 3369)   ✔✔✔
■ S۲) invoice ثبت‌شده هست، هیچ invRef نیست    (ردیف‌ها: 0، طول HTML: 103)    ✔✔✔  ← بازتولید گزارش
■ S۳) invRef هست، فاکتور offerNo خالی دارد    (ردیف‌ها: 1، طول HTML: 2449)   ✔✔✔✔ ← «بدون فاکتور» + ثبت تکراری
■ S۴) invRef روی نسخهٔ ریالی (rialOf)          (ردیف‌ها: 0، طول HTML: 103)   ✔✔
■ S۵) offerNo با فاصلهٔ تهی                    (ردیف‌ها: 1)                  ✔
■ S۶) پروندهٔ فروش void، ارجاع/فاکتور سالم    (ردیف‌ها: 1)                  ✔✔✔
بررسی‌های ناموفق: 0  (یعنی کد دقیقاً همین رفتار را دارد)
```

هارنس: `/home/user/inv-eval/panel-eval.js` (خارج از ریپو؛ هیچ فایلی در `crm/` یا `api/` دست نخورد).

## ۷) پیوست ب — وضعیت فعلی گیت و نسخه

- `node _tools/uat/tester598-v34.37.1-invoice-panel-rows.js` → **PASS 50 / FAIL 0** (امروز، روی همین کامیت).
- `git log` این مخزن تنها یک کامیت merge دارد؛ بنابراین «چه چیزی بین v34.37.0 و v34.37.1 جابه‌جا شد» فقط از `RELEASE-NOTES-v34.37.1.md` قابل پیگیری است — و آن سند صریحاً می‌گوید §۶: «هیچ تغییری در سرور، مدل داده، مجوزها یا جریان ثبت/اصلاح/ابطال فاکتور انجام نشده است»؛ یعنی **پنل فاکتورها تنها چیزی است که در آخرین انتشار دست خورده** — همسو با گزارش «از مدتی است/بعد از آخرین آپدیت نمایش نمی‌شود».
- `php -l` روی `api/*.php` در این سندباکس قابل اجرا نبود (باینری `php` نصب نیست)؛ توصیه می‌شود پیش از هر انتشار، گیت `run-ci-gate.js` روی میزبان دارای PHP اجرا شود.

---

## ۹. افزودهٔ پس از اصلاح (v34.37.2) — چه چیزی از این تحلیل رفع شد و چه چیزی نه

| یافته | وضعیت پس از v34.37.2 | توضیح |
|---|---|---|
| **F1** — فاکتور یتیم نامرئی (P0) | ✅ رفع در لایهٔ نمایش | بخش «🧾 فاکتورهای ثبت‌شده بدون ردیف ارجاع» + پرش به پرونده مبدأ. علتِ تولیدِ یتیم (F2) همچنان باقی است |
| **F2** — `unset invRef` بی‌گارد در `revise_award` (`api/sales-domain.php:1839`) | ⛔ دست‌نخورده | گام ۲ — تغییر رفتار سرور/جریان ابلاغ، نیازمند تصمیم کارفرما |
| **F3** — رندر یک‌باره + فرود حسابدار | ✅ تسکین | «↻ بازخوانی از سرور» + یک بازتلاش خودکارِ یک‌باره (سقف ۶۰ ثانیه) روی رندر خالی. ریشه (بسته بودن رویداد sync) در گام ۲ است |
| **F4** — `ptfBApplyServerProjection` بی‌صدا `false` | ⚠️ فقط قابل‌مشاهده شد | کنتور «دادهٔ این دستگاه: n فاکتور · m پیشنهاد» همان تشخیص را به کاربر می‌گوید؛ منطق فاز B تغییر نکرد |
| **F5** — جستجوی گم‌شده | ✅ رفع | `#invSrch` به پنل زنده برگشت و روی هر دو بخش فیلتر می‌کند |
| **F6** — تطبیق سخت `offerNo` | ✅ رفع | `normRef` (trim + ارقام فارسی/عربی) + نام‌های مستعار (نسخهٔ ریالی/مبنای ریالی) — هم در ردیف‌ها، هم در قفل «ثبت دوباره» |
| **F7** — یک نویسنده و سه پاک‌کننده، بدون reconcile | ⛔ دست‌نخورده | گام ۲ |
| **F8** — تسترها رشته‌ای بودند | ✅ رفع | `tester599` رفتارِ رندر را در `vm` می‌سنجد و با آزمون جهش روی پنج نسخهٔ دست‌کاری‌شده راستی‌آزمایی شد (حذفِ رفع ⇒ ۲۶ سنجش قرمز) |

**پیشنهاد برای گام ۲ (پس از تأیید کارفرما):** ① گارد `revise_award`: هر پاک‌سازی `invRef` باید یا مانورِ بازسازی پیوند داشته باشد یا صریحاً به «یتیم شدن» اخطار دهد؛ ② فرمان `reconcile_invoice_refs` برای وصل‌کردن مجدد فاکتورهای یتیم روی سرور (نه در مرورگر)؛ ③ تغییر مبدأ رندر پنل از `ptf_crm_offers.invRef` به `ptf_crm_invoices` (پنل فاکتور‌محور می‌شود و بخش یتیم‌ها به «برچسبِ بی‌ارجاع» تبدیل می‌شود)؛ ④ یک رویداد `invoices-changed` پس از هر pull تا رندرِ دوبارهٔ دستی لازم نشود.

