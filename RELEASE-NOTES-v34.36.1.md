# RELEASE NOTES — v34.36.1 SYNC (رفع ریشه‌ای بن‌بست «انتقال یک‌باره» / همگرایی مهاجرت)

**تاریخ:** ۲۰۲۶-۰۹-۰۴ · **گیت:** سبز · **تستر جدید:** `tester588-v34.36.1-migration-convergence.js` (۷۷ سنجهٔ رفتاری + منبعی)
**نکتهٔ بامپ:** جاروی استاندارد نسخه (۸ نقطهٔ رسمی + ۱۲۰ ارجاع `index.html` + ۶۷۳ pin در ۱۳۲ تستر)

---

## گزارش کارفرما

> «انتقال یک‌باره هیچ چیزی را منتقل نمی‌کند؛ بنر سرِ جایش می‌ماند و هیچ گزینهٔ رهایشی هم نیست.»

نشانه‌های دستگاهِ نمونه (مدیر، دسترسی کامل):
`enabled=true` · `synced=false` · dirty = `ptf_crm_opex` · **صف فاز B: خالی** · آخرین pull: موفق · بنر «۴۸ از ۴۹ کلید».

---

## ریشه‌یابی (RCA) — زنجیرهٔ پنج حلقه‌ای

| کد | حلقه | چه می‌شد |
|----|------|----------|
| **R5** | `ptfBPushBatch` فیلد `protectedConflicts` را از پاسخ سرور نمی‌خواند | عبارت `(result.protectedConflicts \|\| [])` در `ptfBFlushQueue` همیشه `[]` بود ⇒ شاخهٔ نجاتِ کلید مالی محافظت‌شده (v34.8.10) **کدِ مرده** بود ⇒ `opex` همیشه به merge عمومی «محلی-برتر» می‌افتاد و هر push دوباره `conflict` می‌داد (حلقهٔ بی‌پایان). |
| **R3** | watermark فقط در شاخهٔ نجات تعارض جلو می‌رفت | کلیدی که همین لحظه ACK شده بود، در push بعدی با `base` کهنه می‌رفت ⇒ سرور دوباره conflict می‌داد. علتِ شکستِ «تکمیل انتقال یک‌باره» **بلافاصله پس از یک ارسال موفق**. |
| **P0-1** | مسیر مهاجرت تک‌تیر و همه-یا-هیچ بود | `d.serverData`/`d.krevs`/`d.protectedConflicts` دور ریخته می‌شد؛ هیچ نجات/تلاش‌مجددی در کار نبود؛ یک کلیدِ در تعارض، کلِ ۴۹ کلید را برای همیشه می‌بست. |
| **P0-2** | `ptfBConfirmFlush` نشانگرها را **پیش از** تلاش پاک می‌کرد | هر کلیکِ ناموفق، دستگاهِ همگراشدهٔ سبز را کهربایی می‌کرد، آینهٔ IndexedDB را خاموش می‌کرد و pullهای بعدی دوباره localStorage را پمپ می‌کردند (تلهٔ «سبز→کهربایی»). |
| **P0-3** | شکستِ مهاجرت هیچ ردِ پایداری به جا نمی‌گذاشت | کاربر فقط یک alert مبهم «network» می‌دید و ارجاعِ منسوخِ «تنظیمات → هم‌گرایی داده»؛ هیچ فارِنزیکی برای تشخیص وجود نداشت. |

یافته‌های همراه: **F5** صفِ IDB-محورِ فاز B در باکس «تشخیص همگام‌سازی» خالی نشان داده می‌شد (دقیقاً همان نشانهٔ گمراه‌کنندهٔ این پرونده) · **F6** فهرست fallback کلیدها دو کلید مالی (`ptf_crm_fin_events`, `ptf_crm_personal_cheques`) را نداشت و چون `window._ptfSyncKeys` هرگز صادر نمی‌شد، همان فهرست ناقص cache می‌ماند («۴۸ از ۴۹» دائمی) · **F7** هر تلاشِ ناموفق مهرِ زمانیِ dirty را بازنشانی می‌کرد و کلیدِ گیرکرده همیشه «همین الان» دیده می‌شد.

---

## تغییرات

### ۱) R5 — `protectedConflicts` حالا عبور می‌کند (`crm/client-server.js`)
- انباشتگر `protectedConflicts` در `ptfBPushBatch` (مشابه `conflicts`)، انتشار در `finish()` و در **هر دو** بازگشت زودهنگام (needLogin / همه-blocked) تا قرارداد پاسخ یکنواخت بماند.
- فیلتر `savedKeys` هم کلیدِ محافظت‌شدهٔ در تعارض را کنار می‌گذارد (**fail-closed**): کلیدی که سرور آن را در تعارض دیده، هرگز «تأییدشده» حساب نمی‌شود و صف/dirty‌اش پاک نمی‌شود.

### ۲) R3 — watermark پس از ACK (`bAdvanceWatermarksFromPush`)
- هر پاسخ push موفق، watermark کلیدهای ACK‌شده را از `krevs` همان پاسخ جلو می‌برد (هم‌رفتار با مسیر legacy که `applyKrevs(d.krevs)` می‌کرد).
- **فقط کلیدهایی که سرور واقعاً ذخیره کرد** (`savedKeys`) جلو می‌روند. سرور برای کلیدِ «در تعارض» یا «رد شده» هم `krevs` می‌فرستد؛ اگر watermark آن‌ها جلو می‌رفت، push بعدی با `base` برابرِ سرور دیگر conflict نمی‌داد و کپی محلیِ همگرانشده **نسخهٔ معتبر سرور را کورکورانه بازنویسی** می‌کرد. کلیدِ در تعارض/ردشده کهنه می‌ماند تا pull نسخهٔ سرور را بیاورد (سنجهٔ رفتاری در tester588).
- `rev` سراسری سرور هم از پاسخ جمع و در `finish()` منتشر می‌شود تا ثبت watermark **دقیق** باشد نه max-only.
- **استثنای عمدی:** کلیدهای مشترکِ union (`ptf_crm_audit`, `ptf_crm_avatars`, `ptf_crm_notifs`) جلو نمی‌روند — مقدار ذخیره‌شدهٔ سرور unionِ چند دستگاه است نه عینِ payload ما؛ پیش‌رفتن watermark یعنی از دست رفتن ردیف سایر دستگاه‌ها. پیش‌بینی مشترک `window.ptfSyncIsSharedUnionKey` در `sync.js` صادر شد و با `sync_shared_union_key()` سرور هم‌تراز است (سنجهٔ برابری در tester588).

### ۳) P0-1 — همگراییِ چندنوبتی با موتور مشترک (`convergeMigration`)
- منطق نجات تعارض به helper مشترک `rescueConflictedKeys(result, payload)` منتقل شد؛ **هم** صف (با سقف ۳ نوبتِ خودش) و **هم** مهاجرت از آن استفاده می‌کنند. کلید مالی محافظت‌شده → `ptfSyncResolveProtectedConflictFromServer` (VERBATIM-CONVERGENCE)، کلید عادی → `ptfSyncResolveConflictFromServer`.
- معیارِ پایان، **تأییدِ تک‌تک کلیدها روی سرور** است (مبنای تجمعی `submitted`) نه موفقیتِ یک درخواست: تا ۳ نوبت (۹۰۰ms) فقط با کلیدهای **قابل‌تلاش** و با مقدارِ *الانِ* محلی. کلیدهای تأییدشده هرگز دوباره ارسال نمی‌شوند.
- «قابل‌تلاش» دقیقاً سه حالت است: کلیدِ **نجات‌یافته** (کپی محلی با نسخهٔ سرور جایگزین شد ⇒ push بعدی مطابقت می‌کند)، **شکست ارسال/تایم‌اوت** و **در انتظار فرمان دامنه**. تعارضی که نجات نیافت (سرور `serverData` نفرستاد) با همان payload و همان base دوباره همان تعارض را می‌دهد ⇒ بدون تلاشِ بیهوده، فوراً گزارش دقیق می‌شود. `forbidden`/`skipped`/`rejected` هم قطعی‌اند.
- **«ردِ بی‌ضرر»:** اگر کپی محلیِ یک کلید آرایهٔ خالی باشد و سرور به‌خاطر سپر ضد داده‌صفر آن را رد کند، مانعِ پایانِ انتقال نیست (چیزی روی این دستگاه نیست که از دست برود و pull بعدی همان کلید را از سرور پر می‌کند) و این شفاف در audit گزارش می‌شود. هر مقدار **ناخالی** همچنان fail-closed می‌ماند.
- `forbidden` (خارج از allowlist نقش) و `skipped` (>۸MB) عمداً همگرایی را می‌بندند: تنها کپیِ محلیِ آن کلید هنوز روی سرور نیست و «پاک‌سازی کش محلی» نباید مجاز شود.
- پس از موفقیت: ثبت نشانگرها → ACK کلیدها → تمیزکردن صف (با حفظ نسلِ تازه‌تر) → فعال‌سازی فاز B → پیام موفقیت → رفرش.

### ۴) P0-2 — نشانگرها دست‌نخورده (`ptfBConfirmFlush`)
- حذف `localStorage.removeItem(flushKey()/syncedKey())` پیش از تلاش؛ به‌جایش `ptfBFinalize({ force: true })` = «اجرای دوبارهٔ مهاجرت» بدونِ لمسِ نشانگرها. نشانگر فقط پس از ACK کامل در `convergeMigration` ثبت می‌شود.

### ۵) P0-3 — گزارش دقیق + فارِنزیک روی صفحه
- پیام شکست، شمارِ واقعی «x از y کلید تأیید شد» و **نام کلیدها به تفکیک کلاسِ علت** را می‌گوید: تعارض داده / خارج از allowlist نقش / ردِ سپر داده / بزرگ‌تر از ۸MB / شکست ارسال / در انتظار فرمان دامنه — همراه با راهنمای عملیِ هر کلاس.
- ثبت دائمی با `window.ptfSyncNoteError('migration', code, reason, detail)` (صدورِ جدید از `sync.js`) ⇒ در «تشخیص همگام‌سازی» و در **جعبهٔ وضعیت دستگاه** دیده می‌شود.
- برچسب دامنهٔ `migration` در جعبهٔ تشخیص اضافه شد (پیش از این هر دامنهٔ غیر push «دریافت (pull)» نشان داده می‌شد).
- ارجاع‌های منسوخِ «تنظیمات → هم‌گرایی داده» به مسیرِ واقعیِ UI اصلاح شد: «تنظیمات ← بک‌آپ و بازگردانی ← وضعیت دستگاه».
- `crm/backup.js`: جعبهٔ کهرباییِ وضعیت دستگاه، علتِ آخرین شکستِ انتقال + کلیدهای درگیر + زمان را نشان می‌دهد (بدون کنسول، بدون افشای payload).

### ۶) F5 / F6 / F7 — تشخیص و برابری
- **F5:** `diagCounts()` صفِ زندهٔ فاز B (`ptfBPendingKeys`) را با LS union می‌کند؛ دیگر «صف آفلاین فاز B خالی است» دروغ نمی‌گوید (هم‌تراز با `ptfSyncDiagnosticsSnapshot`).
- **F6:** `window._ptfSyncKeys` صادر شد تا `bKeys()` همیشه فهرست مرجعِ ۶۶ کلیدی `sync.js` را بگیرد؛ فهرست fallback هم کامل شد (دو کلید مالی جامانده) و **cache نمی‌شود** تا ترتیب بارگیری نتواند فهرست ناقص را ماندگار کند.
- **F7:** `ptfSyncMarkPendingKeys` نخستین مهرِ زمانیِ dirty را حفظ می‌کند؛ سنِ واقعیِ کلیدِ گیرکرده در UI دیده می‌شود.

---

## سازگاری و ایمنی (بدون تغییر)

- مسیر **بدون کد** (ADOPT از `lastPushResult.serverData`) که پیش‌تر برای دستگاهِ گیرکردهٔ زنده داده شد، همچنان معتبر است؛ این انتشار همان کار را خودکار و ایمن انجام می‌دهد.
- `merge` محافظت‌شدهٔ سرور (`sync_merge_protected_finance_snapshot` — ردیف‌های recurring-owned هرگز بی‌صدا حذف نمی‌شوند) دست‌نخورده است.
- بوتِ خودکار همچنان fail-closed است: دادهٔ محلیِ موجود هرگز بدون تأیید انسان push/merge/پاک نمی‌شود (`local_data_requires_review` / `existing_local_data`).
- «رها کردن امن» فقط برای whitelist لاگ/اعلان (`audit`/`notifs`/`avatars`) باقی ماند.
- سقف ۸MB، allowlist نقش‌ها، سپر ضد داده‌صفر و تک‌transport بودنِ push بدون تغییر.

---

## آزمون

- **`tester588` (جدید، در گیت CI ثبت شد):** ۹۰ سنجه — بازسازی کامل سناریوی پروداکشن در VM با سرورِ ساختگی: عبور `protectedConflicts` و fail-closed بودن `savedKeys`؛ جلو رفتن watermark و استثنای union؛ همگرایی دو-نوبتی مهاجرت با اثبات VERBATIM-CONVERGENCE (نوبت دوم **فقط** کلید نجات‌یافته و **عینِ** نسخهٔ سرور)؛ گزارش دقیق با کلاس/نام کلید؛ باقی ماندن نشانگرها پس از شکست؛ «ردِ بی‌ضرر» در برابر ردِ fail-closed؛ needLogin؛ انصراف کاربر؛ مسیر خالی دستگاه تازه؛ F5/F6/F7.
- **pinهای به‌روز:** `tester513` و `tester515` برای ساختار helper مشترک به‌روز شدند (سست‌تر نشدند — pinهای R5/R3 و فراخوانی helper در مسیر مهاجرت **اضافه** شد).
- `arch-guard`: **PASS** (A6 نسخه ۰/۰؛ A10 بدهی بدون افزایش).
- گیت CI: **۲۰۳ PASS / ۰ FAIL** (همهٔ تسترها، از جمله ۵۲۰/۵۱۴/۵۵۱/۵۵۲/۵۵۳/۵۵۴/۴۸۴).
- رندرِ واقعیِ «جعبهٔ وضعیت دستگاه» هم در VM آزموده شد (برش بلوک از `backup.js` با stubها): کهربایی ⇒ علت + نام کلیدهای تمیز؛ سبز ⇒ بدونِ بخش علت؛ خطای غیرمهاجرتی ⇒ نمایش داده نمی‌شود.

## اقدام لازم برای دستگاهِ گیرکردهٔ فعلی

پس از استقرار این نسخه: ورود مجدد (برای گرفتن `client-server.js?v=34.36.1`) → «تنظیمات ← بک‌آپ و بازگردانی ← وضعیت دستگاه» → «⬆️ تکمیل انتقال یک‌باره». اگر کلیدی ماند، علت و نام کلیدها هم در پیام و هم در جعبهٔ دستگاه نوشته می‌شود.
