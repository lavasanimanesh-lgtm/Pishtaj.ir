# ارزیابی منطق ارزش افزوده — فاکتور پوششی/رسمی و تسویه فصلی (بدون تغییر کد)

**تاریخ:** 2026-08-22 (۱۴۰۵/۰۵/۳۱)  
**نسخه:** v34.7.88  
**برنچ:** `arena/01a02872-pishtaj-ir`  
**وضعیت:** فقط تحلیل/مستند — هیچ کدی تغییر نکرد.

---

## ۱. سناریوی کارفرما (برای بررسی)

1. فاکتور **رسمی پوششی/صوری** برای ایجاد گپ «خرید غیررسمی ↔ فروش رسمی» ثبت میشود.
2. ارزش افزودهی آن فاکتور = **اعتبار ارزش افزوده** شرکت.
3. **درصد فاکتورساز** = هزینهی واقعی (در هزینههای جاری ثبت میشود).
4. ارزش افزودهی **فاکتورهای فروش** = **بدهی مالیاتی** ارزش افزوده.
5. ارزش افزودهی **فاکتورهای رسمی خرید** (واقعی یا صوری) = **اعتبار ارزش افزوده**.
6. در پایان فصل: **پرداختی = بدهی فروش − اعتبار خرید**. اگر اعتبار > بدهی → پرداختی صفر و **اعتبار باقیمانده به فصل بعد منتقل میشود**. اگر بدهی > اعتبار → مانده باید پرداخت شود.
7. هدف: «محاسبه دقیق لازم نیست، ولی منطق درست حاکم باشد.»

---

## ۲. وضعیت فعلی کد (مستند)

### ۲.۱ فاکتور فروش رسمی
- فایل: `crm/official-invoice-v2.js` + سرور `api/sales-domain.php → register_invoice`.
- مبلغ پایه (`base`/`baseAmountIRR`) و درصد (`vatPercent`) ذخیره میشود؛
  `vat = base * vatPercent / 100` و `amount = base + vat`.
- **این `vat` در فاکتور فروش دقیقاً «بدهی مالیاتی بالقوه» است** ولی فقط `vat` روی همان فاکتور
  فروش نگهداری میشود؛ **جایی جمعشده/ماندهدار نمیشود.**

### ۲.۲ فاکتور خرید رسمی (واقعی یا پوششی)
- فایل: `crm/supplier-finance.js → slInvoiceSave`.
- فاکتور واقعی: `vatPct` و `vatAmount` روی رکورد ذخیره میشود (`inv.vatAmount`).
- فاکتور پوششی: `coverVatPct`, `coverVatAmount`, `coverCommissionPct`, `coverCommissionAmount`,
  `coverNetBenefit` ذخیره میشود که `coverNetBenefit = coverVatAmount − coverCommissionAmount`.
- **این `vatAmount`/`coverVatAmount` فقط روی فاکتور خرید است؛ جمع/ماندهی «اعتبار» جدا ندارد.**

### ۲.۳ گردش/مانده تأمینکننده (`balance`)
- فاکتور واقعی → `invRemain` (بدهی تأمینکننده).
- فاکتور پوششی → `بدهی ناشی از پوششی = کارمزد − اعتبار VAT` (v34.7.88).
- یعنی **در حساب تأمینکننده، منفعت خالص (VAT − کارمزد) دیده میشود**، اما این یک «اعتبار
  مالیاتی» عمومی نیست؛ فقط گردش همان فاکتور پوششی است.

### ۲.۴ گزارشهای مالی/سود
- `fiscal.js`, `working-capital.js`, `ledger-report.js`, `official-ledger.js`:
  - `coverNetBenefit = coverVat − coverCommission` بهعنوان **منفعت** گزارش میشود.
  - `coverCommission` بهعنوان **خروجی/هزینه واقعی**.
- اینها برای «سود/وضعیت مالی» درست هستند.

### ۲.۵ بخش «اظهارنامهها» (`tax-returns.js`)
- فقط **بایگانی فایل** (PDF/تصویر) برای «عملکرد سالانه» و «ارزش افزوده فصلی» است.
- **هیچ رکورد عددی / محاسبه / مانده / carryforward ندارد.**
- مدل: `{kind:'performance'|'vat', year, season, type, file, t, by, status}`.

### ۲.۶ جستجوی کلیدواژه
- `vatDebt`, `vatCredit`, `vatLiability`, `taxDebt`, `taxCredit`, `payableVat`,
  `vatCarryForward`, `مالیات ارزش افزوده/بدهی/اعتبار/مانده` در `api` و `crm/*.js`:
  **موردی پیدا نشد.**

---

## ۳. نتیجه ارزیابی

### الف. **دادههای پایه درست ذخیره میشوند.**
- بدهی VAT فروش = `vat` روی فاکتورهای رسمی فروش.
- اعتبار VAT خرید = `vatAmount` روی فاکتورهای رسمی خرید (و `coverVatAmount` روی پوششی).

### ب. **چرخهی «تسویه فصلی / باقیمانده» وجود ندارد.**
- هیچ ماژول/نمایی **جمع بدهی VAT فروش**، **جمع اعتبار VAT خرید**، **تفاضل**،
  **پرداختی/مانده**، و **انتقال به فصل بعد** را محاسبه/نمایش نمیدهد.
- بخش «ارزش افزوده فصلی» فقط آرشیو فایل است، نه موتور محاسبه.

### ج. **منطق «منفعت» فاکتور پوششی درست است، ولی bukan «اعتبار مالیاتی» نیست.**
- `coverNetBenefit = VAT − کارمزد` بهعنوان منفعت/سود در گزارشهای مالی درست است.
- اما کاربردِ این اعتبار در «مالیات ارزش افزوده فصل» (کاهش بدهی VAT) **پیاده نشده**.
- یعنی: فاکتور پوششی منفعت میسازد، ولی این اعتبار به «حساب VAT فصلی» منتقل نمیشود.

### د. **درصد فاکتورساز به «هزینههای جاری» وصل نمیشود.**
- `coverCommissionAmount` در گردش/سود بهعنوان خروجی دیده میشود، اما ممکن است به
  صراحت **در هزینههای جاری (opex)** ثبت نشود. (در گزارشها نمایش داده میشود؛ در
  هزینههای جاری بهصورت یک ردیف اپکس جداگانه شاید معلوم نباشد.)

---

## ۴. جدول وضعیت (نُه بخش منطق)

| # | منطق موردنیاز | وضعیت فعلی | نیاز |
|---|---|---|---|
| 1 | ثبت فاکتور رسمی فروش + VAT | ✅ `vat`/`vatPercent` روی فاکتور ذخیره میشود | — |
| 2 | ثبت فاکتور رسمی خرید + VAT | ✅ `vatAmount`/`vatPct` ذخیره میشود | — |
| 3 | فاکتور پوششی = اعتبار VAT + کارمزد هزینه | ✅ هر دو ذخیره میشود | — |
| 4 | `منفعت = VAT − کارمزد` | ✅ در گزارشهای مالی/sodu | — |
| 5 | `کارمزد` در هزینههای جاری | ⚠️ در گزارش مالی دیده میشود، در opex صریح نیست | بررسی/وصل |
| 6 | جمع بدهی VAT فروش (فصلی) | ❌ | باید جمع شود |
| 7 | جمع اعتبار VAT خرید (فصلی) | ❌ | باید جمع شود |
| 8 | تفاضل + پرداخت / انتقال به فصل بعد | ❌ | باید محاسبه/ذخیره شود |
| 9 | نمایش «مانده VAT پایان فصل / فصل بعد» | ❌ | باید UI/گزارش داشته باشد |

---

## ۵. سناریوی عددی پیشنهادی (برای آزمون بعدی)

فرض:
- فصل ۱: فروش رسمی: پایه ۱٬۰۰۰٬۰۰۰، VAT 10% = **۱۰۰٬۰۰۰** (بدهی).
- فصل ۱: خرید رسمی واقعی: پایه ۸۰۰٬۰۰۰، VAT = **۸۰٬۰۰۰** (اعتبار).
- فصل ۱: خرید رسمی پوششی: پایه ۲٬۰۰۰٬۰۰۰، VAT = **۲۰۰٬۰۰۰** (اعتبار)، کارمزد ۲% = **۴۰٬۰۰۰** (هزینه).
- جمع بدهی VAT = ۱۰۰٬۰۰۰
- جمع اعتبار VAT = ۸۰٬۰۰۰ + ۲۰۰٬۰۰۰ = ۲۸۰٬۰۰۰
- تفاضل = **−۱۸۰٬۰۰۰** → پرداختی صفر، **اعتبار باقیمانده ۱۸۰٬۰۰۰ به فصل ۲**.
- **این محاسبه در کد فعلی وجود ندارد؛ فقط تکثبتها داریم.**

---

## ۶. راههای پیادهسازی (پیشنهادی برای آینده — بدون اجرا)

### گزینه A — موتور محاسبهی فقطخواندنی (کمریسک)
- یک ماژول `vat-ledger.js` که روی `ptf_crm_invoices` (فروش) و `ptf_crm_supplier_finance.invoices` (خرید)
  در بازهی فصل جمع میزند:
  - `salesVat = Σ vat` فاکتورهای رسمی فروش (بر اساس `invDate`/فصل)
  - `purchaseVat = Σ vatAmount` فاکتورهای رسمی خرید + `Σ coverVatAmount` پوششی
  - `net = salesVat − purchaseVat`؛ اگر منفی بود `carryForward` به فصل بعد.
- نمایش در پنل «اظهارنامهها › ارزش افزوده فصلی» بههمراه «مانده» و «انتقال».
- این رفتار **داده را تغییر نمیدهد** و منطق کارفرما را تأمین میکند (بدون نیاز به دقت
  مالیاتی/دفتر).

### گزینه B — ثبت «مانده فصل» جدا
- در `ptf_crm_tax_returns` رکورد numeric `{kind:'vat_balance', year, season, salesVat, purchaseVat, net, carryForward}`.
- بر اساس گزینه A محاسبه و فقطگزارشی ذخیره میشود.

### گزینه C — اتصال کارمزد به opex
- هنگام ثبت فاکتور پوششی، یک ردیف هزینهی جاری (opex) برای `coverCommissionAmount` با
  برچسب «کارمزد فاکتورساز پوششی» ثبت شود تا در هزینههای جاری واقعاً دیده شود.

---

## ۷. تشخیص پایه پس از ارزیابی

- منطق «صوری پوششی → فروش رسمی → گپ» در سطح **ثبت** درست است.
- منطق «VAT فروش = بدهی / VAT خرید = اعتبار» در سطح **ذخیره** درست است.
- **منطق کلیدیِ «تسویه فصلی + انتقال باقیمانده» ناقص است.**
- اگر هدف صرفاً «منطق درست حاکم باشد» و «محاسبه دقیق لازم نیست»، گزینه A (موتور
  فقطخواندنی) منطقیترین و کمریسکترین است.

---

## ۸. خلاصه

| جنبه | وضعیت |
|---|---|
| ثبت فروش با VAT | ✅ |
| ثبت خرید رسمی با VAT | ✅ |
| فاکتور پوششی: اعتبار VAT + کارمزد | ✅ |
| منفعت/سود پوششی (VAT−کارمزد) | ✅ |
| کارمزد در هزینههای جاری | ⚠️ |
| جمع بدهی VAT فصلی | ❌ |
| جمع اعتبار VAT فصلی | ❌ |
| تفاضل/پرداختی/انتقال به فصل بعد | ❌ |

> **نتیجه:** سناریوی کارفرما در «ثبتها» پشتیبانی میشود، ولی «تسویه/ماندهی فصلی VAT» هنوز
> پیاده نشده است. برای حاکم شدن منطق درست بدون نیاز به دقت دفتری، یک ماژول محاسبهی
> فقطخواندنی (گزینه A) پیشنهاد میشود.
