# CRM v34.38.19 — SILENT-LOGIN + AUTO-MIGRATE-001 + LIQUID-RING

**تاریخ:** 2026-09-10
**بازوی تغییر:** Arena Agent
**کانال:** CRM (بوت/ورود + انتقال خودکار + گرافیک پردهٔ بوت)

> این فایل همان «یادداشت انتشار نسخهٔ جاری» است که گیت CI (تستر۵۷۸، سطر ۷۹) وجودش را
> با کلید `VERSION.json → crm_version` کنترل می‌کند.

## ۱) درخواست کارفرما

1. «بعد از رفتن پردهٔ بوت، هنوز هشدارها در نوار زرد و سفید پایین باقی است و با هر
   رفرش یا هارد‌رفرش در حالیکه داخل سیستم هستیم دوباره هشدارها می‌آیند.»
2. «هشدارها در زمان ورود دیگر نمایش داده نشوند و انتقال یک‌باره و همگرایی با سرور
   بدون زدن دکمه‌ای اتوماتیک انجام شود و در زمان هارد‌رفرش هم یک پردهٔ بوت بیاید.»
3. «گرافیک چرخش دور لوگو بهتر شود: کل لوگو داخل دایره (به‌جز عبارت پی‌تی‌اف زیر
   لوگو) و چرخش به‌صورت مایعِ جاذبه‌دار در حلقه‌ای باریک دور لوگو — هنگام فرود
   سریع‌تر و بازتر، هنگام صعود کُندتر و کم‌حجم‌تر.»

## ۲) چه عوض شد

### سکوت کامل ورود (SILENT-LOGIN)
- **بنر زرد `#ptfUnsavedBanner`** (sync.js): حین سکوت بوت هرگز باز نمی‌شود؛ پس از
  رویداد `ptf:boot-quiet-end` فقط اگر تغییرِ واقعیِ معلق باشد رندر می‌شود.
- **نوار سفید `#ptfMigBar`** (index.html): فقط برای موردِ نیازمند-انسان (تعارض/
  نقش/نشست) یا ناموفقِ ماندگار و پس از پایان سکوت بوت می‌آید؛ تا ۳ تلاش خودکار
  در هر نشست صفحه.

### انتقال یک‌بارهٔ خودکار (AUTO-MIGRATE-001)
- APIهای جدید در client-server.js: `ptfBAutoMigrate` / `ptfBMigrationNeeded` /
  `ptfBAutoMigState`. اجرای بی‌صدا (بدون confirm/alert/reload) با **همان همهٔ
  گاردهای داده‌ای مهاجرت**: ACK تک‌کلید پیش از ثبت نشانگر، نجاتِ تعارض با نسخهٔ
  سرور، سپر ضد داده‌صفر، سقف ۸MB و allowlist نقش.
- sync.js پس از snapshot سالم، آماده‌شدن ورود را تا پایان مهاجرت نگه می‌دارد و
  پردهٔ بوت مرحلهٔ «migrate» را نشان می‌دهد (**failsafe ۴۰ثانیه — fail-open**).

### حلقهٔ مایع جاذبه‌دار (LIQUID-RING)
- Canvas: مایع در کانال حلقه‌ای با `v(θ)=v₀√(max(0.16,۱+0.82·cosθ))` — پایین
  ≈۳.۲× سریع‌تر از بالا؛ طول قوس (∝ سرعت^1.5) و ضخامت (∝ سرعت^1.4) متغیر:
  فرود کشیده/باز، صعود فشرده/کم‌حجم؛ درخشش لبهٔ پیشرو + قطرهٔ دنباله.
- `prefers-reduced-motion` → فریم ایستا؛ نبود Canvas → حلقهٔ CSS قبلی (fallback).
- لوگو: `assets/images/ptf-logo-mark.png` (برش نشان بدون متن PTF) با زنجیرهٔ
  fallback به نسخهٔ کامل و سپس 🏢.

## ۳) سازگاری و نسخه
- مسیر بوتِ `data_pull` تک‌مرحله‌ایِ v34.38.15/.16 و سپرهای داده‌ای .12/.13/.14
  دست‌نخورده؛ مسیر دستیِ مهاجرت (تنظیمات ← وضعیت دستگاه) با همان confirm/alert
  کار می‌کند.
- تمام نقاط رسمی نسخه → v34.38.19 (VERSION.json، manifest، sw.js، index.html و
  ۱۱۶ کوئری `?v=`، sales-domain.php، clear-cache.html، shell.js، cms.js،
  device-reconnect.html) — همزمانی آن را A6 از arch-guard کنترل می‌کند.

## ۴) تست و تأیید
- تستر جدید: `_tools/uat/tester627-v34.38.19-silent-login-automigrate-liquid-ring.js`
  (۲۸ پین + ارزیابی واقعی فرمول سرعت + بررسی ابعاد دارایی برش لوگو).
- پین‌های نسخهٔ جاری تسترهای قدیمی مطابق روال هر ریلیز به v34.38.19 بامپ شدند؛
  قرارداد بنر مهاجرت (tester552/tester553) به قرارداد «خودکار-اول» بازنگری شد.
- گیت CI کامل و arch-guard سبز. سند کامل:
  `ARENA-CRM-BOOT-SILENT-LOGIN-AUTO-MIGRATE-LIQUID-RING-2026-09-10.md`
