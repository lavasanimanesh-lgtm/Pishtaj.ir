# فاز ۱۴ (v34.0.17-alpha) — ارزیابی و اصلاح جامع UI موبایل

**برنچ:** `arena/019fcd1d-pishtaj-ir`
**درخواست کارفرما:** بررسی وضعیت دکمههای رابط کاربری موبایل در همهٔ ماژولها/تبها/مودالها — دکمهها در بسیاری جاها غیرهماندازهاند، متنها خوانده نمیشوند (مثل «نسخه فعال»)، آیکونها وسط دکمه نیستند، و بسیاری از دکمهها از صفحه خارج شدهاند.

---

## ارزیابی (ریشه)
- دکمههای `.bt`/`.ba` فقط `padding` داشتند و `display:inline-block` بودند → **آیکون (ایموجی) و متن وسطِ دکمه قرار نمیگرفتند**، و متن بلند میتوانست دکمه را از صفحه بیرون ببرد.
- در موبایل، دکمههای داخل مودال تمامعرض نبودند → غیرهماندازه و خارج از صفحه.
- «نسخه فعال» (`#topVerPill`) با `font-size:11.5px` و متن بلند «نسخه فعال: v34...» در عرض محدود جمع/بریده میشد → خوانده نمیشد.

---

## اصلاحات (`crm/index.html`)

### دکمهها (پایه — desktop و موبایل)
- `.bt` و `.ba` حالا `display:inline-flex; align-items:center; justify-content:center; gap` → **آیکون و متن وسط دکمه قرار میگیرند**.
- `white-space:normal; overflow-wrap:anywhere; word-break:break-word` → **متن بلند شکسته میشود و دکمه از صفحه بیرون نمیرود**.
- `min-width:0; max-width:100%` → **دکمهها از عرض صفحه تجاوز نمیکنند**.

### موبایل (`@media max-width:768px`)
- دکمههای داخل مودال (`.md .bt` و...) → **تمامعرض و یکنواخت** (چیدمان عمودی با gap).
- دکمههای کوچک جدول/عملگری (`.ba`) → `min-height:34px` برای لمسی بودن.
- متن توضیحات/برچسبها (`small`/`label`/`td`) → شکستن کلمه برای خوانایی.

### «نسخه فعال»
- `#topVerPill`/`#liveHealthPill` → `font-size:12.5px; white-space:normal; overflow-wrap:anywhere` → **خوانا در موبایل**.

---

## تست‌ها
- **tester313** جدید (۸/۰): flex وسطچین، شکستن متن، max-width، دکمههای مودال، نسخه فعال.
- رگرسیون کامل سبز (۲۳ تستر، ۰ شکست).
- بامپ نسخه → **v34.0.17-alpha**.
- تغییرات `.github/workflows/*` در PENDING-workflow-changes ثبت شد.

## مرج به main
پس از این فاز، تمام ۱۵+ کامیت (فاز ۱ تا ۱۴) به شاخهٔ `main` مرج شد (fast-forward یا merge).

*تولیدشده توسط Arena Agent — ارزیابی و اصلاح جامع UI موبایل.*
