# CRM v34.38.15 — BOOT-REVERT (بازگشت مسیر ورود به قبل از v34.38.10)

**تاریخ:** 2026-09-09
**بازوی تغییر:** Arena Agent (به درخواست کارفرما)
**کانال:** CRM (سینک / ورود و بوت)

## دستور کارفرما

> «می‌خواهم بررسی کنی ببینی آخرین ۱۰ تغییر CRM چه چیزهایی بوده. الان مشکلات زیادی برای
> ورود به سیستم در چند تغییر اخیر ایجاد شده. اولین تغییری که سبب این موضوع شده — یعنی از
> جایی که فراخوانی از سرور اضافه شده — را پیدا کن و سیستم را به حالت قبل از آن برگردان.»

## نتیجهٔ بررسی (خلاصهٔ زنجیرهٔ علت)

از میان ۱۰ تغییر آخر (v34.38.5 تا v34.38.14)، نقطهٔ ورود مشکل **v34.38.10 — کامیت
«Implement atomic CRM snapshot sync»** بود: همان‌جا که «فراخوانی از سرور» به مسیر ورود
اضافه شد. پیش از آن، بوت/ورود پس از لاگین فقط یک `data_pull` تک‌مرحله‌ای (با `krevs`)
اجرا می‌کرد. v34.38.10 پرچم `PTF_CRM_SNAPSHOT_V2` را روشن کرد و دریافت کامل اولیه
(bootstrap) را به مسیر تازهٔ دو-مرحله‌ای `data_manifest` + `data_chunk` با قرارداد
checksum/اسنپ‌شات اتمیک برد.

طبق اسناد RCA خود پروژه (`ARENA-CRM-ATOMIC-PULL-INCOMPLETE-RCA-2026-09-08.md` و
`ARENA-CRM-RFQSMART-MASS-DELETION-RCA-2026-09-09.md`)، همین مسیر روی دادهٔ واقعی شرکت
همیشه fail-closed می‌شد و زنجیرهٔ مشکلات ورود را ساخت:

1. `bootstrapped` هرگز `true` نمی‌شد ⇒ پیام تکرارشوندهٔ «دریافت کامل CRM انجام نشد»؛
   داده‌ها (به‌ویژه درخواست‌های تأمین) کامل بارگذاری نمی‌شدند.
2. تا bootstrap کامل نشود، `pushDirty` هم ارسال نمی‌کرد ⇒ تغییرات محلی معلق می‌ماند.
3. مسیر فاز B (`pushViaPhaseB`) از بالای گیت bootstrap عبور می‌کرد ⇒ همان مرورگرِ با
   نمای ناقص، کل مجموعه را با یک ردیف روی سرور رونویسی کرد (حادثهٔ ۶۸ → ۱ رکورد
   `ptf_crm_rfqsmart`).

نسخه‌های v34.38.11 تا v34.38.14 عوارض همین تغییر را درمان می‌کردند (رفع checksum،
سپر حذف انبوه، ابزار بازیابی) — ریشه اما سر جایش بود.

## تغییر این نسخه (جراحی‌وار — حداقلی)

### `crm/index.html`
- **`window.PTF_CRM_SNAPSHOT_V2 = false`** — تنها کلید تغییر. با خاموشی این پرچم،
  بوت/ورود و «دریافت کامل مجدد» (`ptfSyncFullResync`) دقیقاً مثل قبل از v34.38.10 از
  همان `data_pull` تک‌مرحله‌ای (since=0 + krevs) عبور می‌کنند؛ دیگر هیچ
  `data_manifest`/`data_chunk` از مرورگر فراخوانی نمی‌شود.
- کد مسیر اتمیک (`ptfSyncPullAtomic` در `crm/sync.js` و endpointهای سرور در
  `api/crm.php`) **عمداً حذف نشد** تا پس از اعتبارسنجی کافی روی دادهٔ واقعی، با
  `true` کردن صریح همین پرچم دوباره فعال شود.

### آنچه دست‌نخورده ماند (به انتخاب کارفرما — گزینهٔ ب)
- سپر حذف انبوه سرور/کلاینت (v34.38.12) — جلوی تکرار حادثهٔ رونویسی را می‌گیرد.
- ابزار «🛟 بازیابی رکوردهای گم‌شده» (v34.38.13) — برای برگرداندن ۶۷ درخواست تأمین.
- رفع یکتایی بایگانی پرونده + «به جریان انداختن» (v34.38.14) و ابزار
  «اتصال مجدد امن مرورگر».
- بهبودهای پیام بوت (علت‌دار، بدون تکرار، backoff پلکانی) — با مسیر قدیمی هم
  هم‌خوان‌اند.

## چرا این روش و نه حذف کد؟

- رفتار ورود **بایت‌به‌بایت** به حالت v34.38.9 برمی‌گردد (همان مسیری که ماه‌ها پایدار
  بود) با یک تغییر یک‌خطی قابل‌رجوع؛
- تسترهای قفل‌شدهٔ tester617/618 (که خودشان پرچم را در vm مستقل از index.html ست
  می‌کنند) سبز می‌مانند و قرارداد صحت‌سنجی برای فعال‌سازی آینده محفوظ است؛
- سپرهای حذف انبوه — که دقیقاً برای بدترین عارضهٔ همین ریشه ساخته شدند — سرِ کار
  می‌مانند.

## تست قفل‌شده

- **قفل جدید:** `_tools/uat/tester623-v34.38.15-boot-legacy-pull.js` —
  ۱) قرارداد انتشار (پرچم `false` در index.html + هم‌ترازی VERSION/sw/manifest/SD)؛
  ۲) رفتار واقعی: بوت با پرچم خاموش فقط `data_pull` می‌زند و هیچ `data_manifest`/
  `data_chunk` صدا نمی‌شود (شبیه‌سازی کامل sync.js در vm)؛
  ۳) مسیر اتمیک با پرچم روشن (برای آینده) همچنان کار می‌کند — یعنی برگشت، برگشتِ
  قابل-اطمینانی است.
- **گیت کامل:** `run-ci-gate.js` — باید کامل سبز باشد.

## سطح انتشار نسخه

`VERSION.json → v34.38.15`؛ `crm/index.html` (PTF_CRM_RELEASE + پرچم + cache-busterهای
?V)؛ `crm/sw.js`؛ `crm/manifest.json`؛ `crm/shell.js`؛ `crm/cms.js`؛
`crm/clear-cache.html`؛ `crm/device-reconnect.html`؛ `api/sales-domain.php`
(`SD_SERVICE_VERSION = '34.38.15'`). پین‌بندی‌های تستری (فرم ساده و Regex) به 34.38.15
هم‌مهاجرت شدند؛ نام فایل تسترهای متولدبرجسته دست‌نخورده ماند.

## اقدام پس از دیپلوی

1. هر مرورگری که روی نسخهٔ قبلی گیر کرده، یک بار **crm/clear-cache.html** را باز کند
   (یا صفحه را Hard-Refresh بزند) تا سرویس‌ورکر نسخهٔ جدید نصب شود.
2. اگر مرورگری هنوز نشانگر همگام‌سازی قرمز داشت، از «تنظیمات ← ابزار اتصال مجدد امن
   مرورگر» (v34.38.14) استفاده شود — این ابزار مستقل از سلامت برنامهٔ اصلی است.
3. بازیابی ۶۷ درخواست تأمین حادثهٔ ۱۴۰۵/۰۶/۱۷ با ابزار v34.38.13 (تنظیمات ← بک‌آپ و
   بازگردانی ← 🛟 بازیابی رکوردهای گم‌شده) همچنان قابل اجراست.
