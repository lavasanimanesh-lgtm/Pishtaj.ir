# CHQ-DOC-002 (v34.4.31) — چرا مودال «سند/ضمیمه» گاهی خالی است و باید تب را عوض کرد

**سوال کارفرما (۱۴۰۵/۰۵/۱۹):**
«مشکل عدم نمایش ضمیمه‌ها در پنجرهٔ مودال باز شده چیه و چرا بعضا باید در تب تجدید مشاهده کرد ضمیمه‌ها رو، مثلاً ضمیمهٔ تنخواه را نمایش نمی‌دهد؟»

## ریشهٔ واقعی مشکل
این مورد یک باگ سطحی در یک فایل نبود؛ یک **تناقض معماری در لایهٔ همگام‌سازی** (`crm/sync.js`) بود که روی همهٔ مودال‌های «مشاهدهٔ سند» اثر می‌گذاشت:

1. `sync.js` هر **۲۰ ثانیه** (و کندتر در تب پس‌زمینه/غیرفعال — تا ۱۲۰ تا ۱۸۰ ثانیه) با سرور «pull» می‌زند و داده‌های تازه‌تر (مثلاً سندی که از دستگاه/کاربر دیگر آپلود شده) را در `localStorage` مرورگر ادغام می‌کند.
2. اما تابع `refreshCurrentPanel()` در همان فایل **عمداً** طراحی شده تا وقتی هر مودالی باز است (`class="md-b"`)، پنل را دوباره رندر نکند — تا وسط کار کاربر (مثلاً وسط پر کردن یک فرم) پرش/رفرش ناخواسته پیش نیاید. این تصمیم برای فرم‌های ویرایش درست است، اما یک اثر جانبی داشت: **مودال‌های صرفاً «نمایشی» سند هم مشمول همین قانون می‌شدند**، در حالی که آن‌ها فقط یک‌بار، در لحظهٔ باز شدن، از `localStorage` می‌خواندند و بعد از آن هیچ‌وقت خودشان را به‌روز نمی‌کردند.
3. نتیجه: اگر سندی همین الان (از یک کاربر یا دستگاه دیگر) ثبت شده بود ولی pull دوره‌ای هنوز آن را به این مرورگر نرسانده بود، مودالِ باز شده آن را نشان نمی‌داد. تنها راهی که به‌طور تصادفی مشکل را «حل» می‌کرد این بود که کاربر مودال را ببندد و به تب دیگری برود و برگردد — چون این کار باعث فراخوانی مجدد `goPanel()`/رندر پنل از `localStorage` می‌شد که تا آن لحظه توسط pull دوره‌ای به‌روز شده بود.
4. یک باگ کوچک‌تر هم کنار همین: در تنخواه، اگر رکورد اصلاً هنوز در `localStorage` محلی نبود (لحظاتی قبل از دستگاه دیگر ثبت شده)، `ptfPettyFilesUi` **کاملاً بی‌صدا** هیچ پنجره‌ای باز نمی‌کرد.

## اصلاح
- **`crm/sync.js`**: تابع جدید `window.ptfSyncPullNow(cb)` — یک pull فوری و به‌درخواست، بدون منتظرماندن برای تایمر ۲۰ثانیه‌ای یا محدودیت تب پس‌زمینه.
- **`crm/storage.js`**: هلپر مشترک `window.ptfAttachRefreshOnOpen(dlgId, getSignature, reopenFn)` — بلافاصله بعد از باز شدن هر مودال «سند»، یک pull فوری می‌زند؛ اگر امضای اسناد (کلیدهای فایل) واقعاً تغییر کرده باشد، **فقط همان مودال** (نه کل پنل) را با داده‌ی تازه دوباره می‌سازد — بدون این‌که کار دیگر کاربر (فیلترها، فرم‌های باز دیگر) دست‌کاری شود.
- وصل شد به سه مودال «مشاهدهٔ سند»ی که در تعامل قبلی ساختیم/بررسی کردیم:
  - **تنخواه** — `ptfPettyFilesUi` (crm/petty.js): هم auto-refresh گرفت، هم اگر رکورد هنوز محلی نبود، به‌جای سکوت، یک pull+تلاش مجدد می‌زند.
  - **چک** — `ptfChequeFilesUi` (crm/cheque-panel.js).
  - **گردش حساب تأمین‌کننده** — `slOpenLedger` (crm/supplier-finance.js) که فاکتور/پرداخت‌ها را با سند نشان می‌دهد.

## نکتهٔ مهم برای آینده
این الگو (`ptfAttachRefreshOnOpen`) عمومی و قابل استفاده در هر مودال «نمایشیِ» مشابه دیگر است (مثلاً اگر بعداً محل مشابهی پیدا شد). لازم نیست هرجا از صفر پیاده‌سازی شود.

## تست
- **`tester179-v34.4.31-attachment-modal-stale-sync-fix.js`** (جدید، ۱۱ بررسی در ۳ بخش):
  - Part 1 (functional/vm): `ptfSyncPullNow` واقعاً یک pull فوری (fetch) می‌زند و بعد از اتمام callback را صدا می‌زند.
  - Part 2 (functional/vm): `ptfAttachRefreshOnOpen` وقتی امضای اسناد تغییر نکرده کاری نمی‌کند، و وقتی تغییر کرده مودال را حذف و دوباره می‌سازد.
  - Part 3 (structural): اتصال صحیح در `petty.js` / `cheque-panel.js` / `supplier-finance.js` (شامل اینکه wrap روی نسخهٔ *نهاییِ فعال* `slOpenLedger` انجام شده، نه یک نسخهٔ میانی).
- رگرسیون کامل ۲۵ تستر اجرا شد: همان ۷ شکست از قبل (پیش از این تغییرات هم دقیقاً همان‌طور fail می‌شوند، بی‌ربط به این کار)؛ بقیه از جمله تسترهای مرتبط با sync/storage (۱۵۶، ۱۶۳، ۱۶۴) و چک/تنخواه سبزند.
- بامپ نسخه → **v34.4.31**.

*تولیدشده توسط Arena Agent.*
