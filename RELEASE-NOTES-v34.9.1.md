# v34.9.1 — هات‌فیکس «تلهٔ سبزِ ناتمام + پمپ snapshot» (2026-08-31)

**نوع:** هات‌فیکس جراحی‌شده پس از سه برخورد پروداکشن در ۲ روز (RCA کامل در رودمپ).
**سازگاری:** بدون تغییر قرارداد سرور؛ مترهای پنجرهٔ R4 (win7/بایپس) دست‌نخورده.

## RCA یک‌خطی
کلید dirty گیرکرده (همیشگی: `audit`) ⇒ هر pull یک snapshot کامل در LS می‌نویست
(bypass آینه/لایهٔ داده، بی‌سقف، بی‌انقضا، ×۳) ⇒ QuotaExceeded در هر نوشتن ⇒
«مرتب پر می‌شود» و درمانِ پاک‌کردن فقط موقتی بود؛ هم‌زمان دستگاه‌های «سبزِ ناتمام»
(enabled && !synced از دکمهٔ قدیمی) هر سه درِ نجات UI را بسته داشتند.

## تغییرات
1. **SNAP-PUMP (sync.js):** snapshotهای پیش-pull — آینهٔ IDB فعال ⇒ فقط نشانگر سبک
   با کلید ثابت در IDB؛ آینه خاموش ⇒ LS با سقف ۲۵۶KB، حداکثر ۳ نسخه، انقضای ۲۴س.
2. **TRAP-FIX (backup.js):** جعبهٔ وضعیت دستگاه سه‌حالته شد — سبز کامل (پاک‌سازی کش)،
   **سبزِ ناتمام (دکمهٔ «تکمیل انتقال یک‌باره»)**، کهربایی (انتقال یک‌باره). بنر مهاجرت
   هم حالت ناتمام را می‌پوشاند + مسیر اصلاح‌شده «تنظیمات ← بک‌آپ و بازگردانی».
3. **TRANSPARENCY:** `ptfSyncDirtyInfo()` (سن هر کلید در انتظار ارسال) در جعبه +
   «رها کردن امن» فقط برای audit/notifs/avatars (مرجع سروری union)؛
   `ptfSyncDropDirtyKey` برای کلید کسب‌وکار همیشه false.
4. **ERR-WHY:** بنر قرمز علت واقعی را از failedWrites می‌گوید — Quota ⇒ «پاک‌سازی کش /
   تکمیل انتقال»؛ Security/InvalidState ⇒ «از Chrome/Safari اصلی باز کنید».
5. dirty ها timestamp گرفتند (چهار محل) و loadPersistedDirty سن را حفظ می‌کند.

## گیت
tester553 (۳۱ سنجه) + به‌روزرسانی قرارداد tester551/552 → **۱۶۹ تستر / ۰ FAIL**؛
arch-guard PASS (A10 همان baseline ۱۹۵ — این تغییر یکی از موارد آیندهٔ A10 را عملاً بست).

## بعد از دیپلوی
- کاربران گرفتار: تنظیمات ← وضعیت دستگاه ← «تکمیل انتقال یک‌باره» (بدون پاک‌کردن دستی).
- حذف موتور legacy + A10 + T3-5 → **v34.9.2** با شرط ≥۷ روز سبز R4.
