# 📋 بک‌لاگ دور هفتم (R7) — ابلاغ ۱۴۰۵/۰۴/۲۲ (عصر)
**وضعیت:** فقط ثبت بک‌لاگ — بدون تغییر کد (قاعده مصوب: هر مورد اول بک‌لاگ، سپس اسپرینت مستقل با هم‌خانواده‌ها)
**تدوین:** پنل متخصصان ارشد بازرگانی + مهندسان ارشد نرم‌افزار — با ریشه‌یابی فنی روی کد موجود

| # | مورد | نوع | خانواده | برآورد |
|---|------|-----|---------|--------|
| US-409 | جمع ردیف لحظه‌ای CO/TC + وسط‌چین قیمت چاپ | باگ/بهبود | فرم/چاپ پیشنهاد (با BUG-019) | کوچک — قبلا در R6 ثبت شده ✅ |
| US-410 | تاریخ شمسی چک + دستیار هوشمند ورود چک‌ها | بهبود | چک‌ها | کوچک-متوسط |
| US-411 | ثبت‌کننده درخواست + مالکیت مشتری کارشناس + پورسانت + حریم اطلاعات حیاتی | زیرساخت | فروش/RBAC (بزرگ → ۳ فاز) | بزرگ |
| BUG-020 | پرش تم هنگام رفرش (تم قبلی → تم جدید) | 🐞 باگ | تجربه ورود/داشبورد | کوچک |
| BUG-021 | داشبورد اول: فقط آمار — «روز من» و نرخ ارز دیر می‌آیند | 🐞 باگ | تجربه ورود/داشبورد | کوچک-متوسط |
| پیگیری | حواله/نرخ سنا همچنان نمایش داده نمی‌شود | تشخیص | تجربه ورود/داشبورد (fx) | کوچک |

---

## US-410 — چک‌ها: تاریخ سررسید شمسی + دستیار هوشمند ورود

**شرح (کارفرما):** تاریخ ثبت چک شمسی شود و امکان دستیار هوش مصنوعی برای ورود چک‌ها فعال شود.

**ریشه‌یابی:** فرم ثبت چک (cheques.js — chNew) فیلد `<input type="date">` میلادی دارد (برچسب هم صراحتا «میلادی» است)؛ یادآور سررسید بر پایه ISO میلادی محاسبه می‌شود (chDaysTo).

**معیارهای پذیرش:**
1. ورود تاریخ سررسید **شمسی** (تایپ ساده 1405/06/15 یا انتخاب‌گر شمسی سبک — بدون کتابخانه سنگین جدید؛ تبدیل داخلی به ISO برای یادآورها تا chDaysTo و اعلان ۷روزه موجود عینا کار کند). نمایش همه‌جا شمسی.
2. چک‌های قبلی (ISO) مهاجرت نرم — نمایش شمسی، داده دست نخورد.
3. **دستیار ورود چک:** عکس/تصویر چک یا متن → استخراج شماره صیاد/مبلغ/تاریخ/در وجه/بانک با llm.php موجود (الگوی inqreader/دستیار) → پیش‌نمایش قابل اصلاح کاربر → ثبت. اعتبارسنجی مبلغ/تاریخ قبل از ثبت؛ هیچ ثبت خودکار بدون تایید کاربر.
4. سهمیه/خطای هوش مصنوعی → مسیر دستی مثل قبل (fallback کامل).
5. تستر: تبدیل شمسی↔ISO رفت‌وبرگشتی + یادآور ۷ روزه با تاریخ شمسی.

## US-411 — شفافیت فروش: ثبت‌کننده درخواست، مالکیت مشتری، پورسانت، حریم اطلاعات (بزرگ → ۳ فاز)

**شرح (کارفرما):** کاربر ثبت‌کننده درخواست زیر درخواست مشخص باشد. زیرساخت مشخص بودن مشتریانِ هر کارشناس فروش و محاسبه درصد و مبلغ پورسانت فروش هر کارشناس فراهم شود. کارشناس فروش نباید به اطلاعات حیاتی شرکت دسترسی داشته باشد؛ فهرست اطلاعات حیاتی توسط تیم تخصصی بازرگانی تعیین شود.

**ریشه‌یابی/زمینه:** رکورد درخواست (saveRfq در bridge) ثبت‌کننده را ذخیره نمی‌کند (dedupStamp فقط crAt/crBy روی برخی موجودیت‌هاست و در فهرست درخواست‌ها نمایش داده نمی‌شود). مشتری فیلد مالک (owner) ندارد. RBAC نقش sales الان sellPrice دارد و buyPrice/finance ندارد (rbac.js) — پایه خوب است ولی «فهرست اطلاعات حیاتی» مدون نیست.

**فاز ۱ — ثبت‌کننده و مالکیت (اسپرینت مستقل):**
1. هر درخواست جدید: `crBy/crAt` + نمایش «ثبت: نام کاربر — تاریخ» زیر ردیف درخواست (فهرست + کانبان + مودال ویرایش). رکوردهای قدیمی: «نامشخص (قدیمی)».
2. مشتری: فیلد «کارشناس فروش مسئول» (owner) — پیش‌فرض = سازنده مشتری؛ تغییر فقط مدیران ارشد (با audit). نمایش در کارت/فهرست مشتری.
3. فیلتر «مشتریان من» برای کارشناس فروش.

**فاز ۲ — پورسانت (اسپرینت مستقل):**
1. تنظیمات پورسانت (فقط ارشد): درصد پیش‌فرض + درصد اختصاصی per کارشناس؛ مبنا = انتخاب ارشد: مبلغ CO برنده یا وصولی واقعی (پیشنهاد پنل بازرگانی: **وصولی واقعی** — پورسانت روی پول نقدشده، انگیزه پیگیری وصول).
2. گزارش پورسانت per کارشناس per دوره (از داده موجود: CO برنده‌های مشتریانِ تحت مالکیت + payments) — فقط برای ارشد + خود کارشناس (فقط سهم خودش).
3. اتصال به نشان‌های ماهانه آینده (US-397) بدون دوباره‌کاری.

**فاز ۳ — حریم اطلاعات حیاتی (اسپرینت مستقل، پس از مصوبه فهرست):**
1. **فهرست پیشنهادی پنل بازرگانی برای تایید کارفرما:** قیمت/تامین‌کننده خرید (دارد — buyPrice)، سود و حاشیه سود، مالی/وصولی‌ها و چک‌ها (دارد — finance)، بستانکاری تامین‌کنندگان، امتیاز مشتریان (دارد — finance)، **مشتریان سایر کارشناسان (جدید)**، گزارش‌های مدیریتی، بک‌آپ/بازیابی، دفترچه کامل پیامک.
2. اجرا: گسترش همان ماتریس perms موجود (perms.js) — بدون سیستم موازی؛ کارشناس فقط مشتریان خودش + درخواست‌ها/پیشنهادهای مرتبط.
3. تستر نفوذ ساده: ورود با نقش sales → عدم دسترسی به هر قلم فهرست مصوب.

**پیش‌نیاز فاز ۳:** تایید فهرست اطلاعات حیاتی توسط کارفرما (بند ۱ به‌عنوان پیشنهاد پنل ارائه شد).

## BUG-020 — پرش تم هنگام رفرش (FOUC ناقص)

**شرح (کارفرما):** هنگام رفرش (موبایل و دسکتاپ) ابتدا تم قبلی می‌آید بعد تم جدید بارگذاری می‌شود؛ کمی کند.

**ریشه‌یابی (تایید شد):** بلاک ضد FOUC در `<head>` فقط **متغیرهای CSS ریشه** را ست می‌کند؛ ولی کلاس `body.ptf-dark` (که عمده استایل شب به آن زنجیر است — perms.js) دیرتر اضافه می‌شود: در DOMContentLoaded + یک `setTimeout(...,100)` در perms.js → یک فریم+ تم روشن دیده می‌شود. به‌علاوه کل CSS تم (theme.js/perms.js) با JS تزریق می‌شود که پس از پارس اسکریپت‌هاست → تاخیر محسوس در موبایل.

**معیارهای پذیرش:**
1. بلاک هد علاوه بر متغیرها، **همان لحظه کلاس تم را روی `<html>` بگذارد** و سلکتورهای شب به `html.ptf-dark body...` هم پاسخ دهند (یا کلاس روی body در اولین خط بعد از باز شدن body ست شود) — صفر فریم تم غلط.
2. حالت auto (تم سیستم — US-354) در همان بلاک هد پوشش داده شود (الان هست، حفظ شود).
3. بدون تغییر رفتار سوییچ دستی/خودکار روز-شب و بدون رگرسیون در چاپ (قالب چاپ همیشه روشن).
4. سنجش: در تستر jsdom کلاس تم قبل از DOMContentLoaded حاضر باشد.

## BUG-021 — داشبورد ورود: «روز من» و نرخ ارز باید از همان ابتدا دیده شوند

**شرح (کارفرما):** بعد از باز شدن صفحه اول فقط تعداد مشتریان/درخواست‌ها و... نمایش داده می‌شود؛ می‌خواهم همان ابتدا وارد داشبورد شود و «روز من» و نرخ لحظه‌ای ارز را ببیند.

**ریشه‌یابی (تایید شد):** `loadAll` بلافاصله `goPanel('dash')` را اجرا می‌کند در حالی که ویجت‌های داشبورد (لانچر/قیف/نوار ارز/روز من) با **زنجیره hook روی buildDashboard** و از طریق `setInterval` های ۲۵۰-۴۰۰ms جدا وصل می‌شوند → در اولین رندر ممکن است هیچ‌کدام (یا فقط بخشی) hook نشده باشند و داشبورد «خام» (فقط آمار قدیمی) رندر شود؛ تا ورود بعدی به داشبورد درست نمی‌شود.

**معیارهای پذیرش:**
1. رندر اول داشبورد پس از login/رفرش = نسخه کامل (لانچر + **نوار ارز بالاترین نقطه** + **روز من** + قیف) — مکانیزم پیشنهادی: پرچم آمادگی hook ها یا یک re-render سبک خودکار داشبورد وقتی آخرین hook زنجیره وصل شد (فقط اگر پنل فعال dash باشد و کاربر هنوز تعامل نکرده) — بدون فلیکر محسوس.
2. ترتیب زنجیره hook (launcher→leads→insights→fx→myday) دست نخورد (TD-007 آگاه).
3. موبایل هم پوشش داده شود (همین رفتار در mobilenav).
4. تستر: شبیه‌سازی لود دیرهنگام hook → داشبورد نهایی شامل fxTicker و بلوک روز من باشد.

## پیگیری — «حواله سنا» همچنان نمایش داده نمی‌شود

**تحلیل:** کد v16.7 نرخ‌های سنا (خرید/فروش دلار/یورو) را به نوار برگردانده و tester85 سبز است؛ سلول سنا فقط وقتی حذف می‌شود که **منبع مقدار صفر بدهد**. دو احتمال: ① بسته v16.7/16.8 هنوز روی هاست نرفته یا کاربران رفرش نکرده‌اند (چک: نسخه سایدبار باید v16.8 باشد) ② هاست به کلیدهای سنا TGJU دسترسی ندارد یا منبع این کلیدها را خالی می‌دهد.
**اقدام پیشنهادی (داخل اسپرینت خانواده داشبورد):** حالت تشخیصی در نوار ارز — اگر دلار آزاد آمد ولی سنا صفر بود، به‌جای حذف بی‌صدا، برچسب «سنا: منبع پاسخ نداد» + دکمه «🔬 تست منبع» برای ادمین (نمایش پاسخ خام fx-rates.php) تا بین «مشکل کد» و «مشکل منبع/هاست» فوری تفکیک شود. + مستند چک‌لیست هاست (outbound به tgju.org).

---

## 👨‍👩‍👧 هم‌خانواده‌بندی و صف پیشنهادی (قاعده ۱-۲ استوری)

| اسپرینت | محتوا | اندازه |
|---------|-------|--------|
| در صف قبلی | US-404 فاز ۲ (انتقال خودکار اسناد) | متوسط |
| جدید — «تجربه ورود» | **BUG-020 + BUG-021** (+ تشخیص سنا — همان خانواده داشبورد/fx) | کوچک-متوسط |
| جدید — «فرم/چاپ پیشنهاد» | BUG-019 + US-409 | کوچک |
| جدید — «چک‌ها» | US-410 | کوچک-متوسط |
| جدید — «فروش/RBAC» | US-411 فاز ۱ → فاز ۲ → فاز ۳ (سه اسپرینت) | بزرگ تقسیم‌شده |
| در صف قبلی | US-405..408 (اسناد بازرگانی) | ۴ اسپرینت |

**پیشنهاد پنل برای اسپرینت بعدی:** «تجربه ورود» (BUG-020 + BUG-021 + تشخیص سنا) — چون گزارش فعال کاربر است و هر روز دیده می‌شود؛ یا طبق صف قبلی US-404 فاز ۲. **انتخاب با کارفرما.**

**شماره‌گذاری بعدی: US-412** | باگ بعدی: BUG-022


---

# 📋 الحاقیه R7 — ابلاغ ۱۴۰۵/۰۴/۲۵ (سه استوری جدید — ثبت با ریشه‌یابی، بدون تغییر کد)

## US-415 — تامین‌کننده خارجی: نام انگلیسی به‌جای نام فارسی اجباری
**شرح (کارفرما):** در ثبت تامین‌کننده خارجی نام فارسی اجباری است در حالی که شرکت‌های خارجی نام فارسی ندارند و باید نام انگلیسی وارد شود.
**ریشه‌یابی:** saveSup2 (offers.js) فیلد nS2Comp (فارسی) را برای همه اجباری می‌کند؛ فیلد «داخلی/خارجی» (nS2Origin — cheques.js v13.4) هنگام اعتبارسنجی خوانده نمی‌شود.
**AC:** ① اگر origin=خارجی: نام انگلیسی (nS2CoEn) اجباری و نام فارسی اختیاری (برچسب‌ها پویا) ② co رکورد خارجی = نام EN (نمایش/جستجو/dedup سازگار — dedupNorm دوزبانه موجود) ③ داخلی مثل قبل ④ رکوردهای قدیمی دست نخورند. | **برآورد:** کوچک | **خانواده:** فرم تامین‌کننده

## US-416 — شفافیت ارز در سراسر نرم‌افزار (حذف «ت» اشتباه روی مبالغ ارزی)
**شرح (کارفرما):** هر نرخی نمایش داده می‌شود معلوم باشد چه ارزی است — بعضی جاها دلار/یورو اشتباها با «ت» نمایش داده می‌شوند.
**ریشه‌یابی اولیه:** نقاط toLocaleString+«ت» hardcode: offers.js (renderOffers مبلغ کل با «ت» حتی برای CO ارزی!)، salesfiles/buycompare/oppo/index — ممیزی کامل نقاط در اسپرینت اجرا.
**AC:** ① ممیزی همه نقاط نمایش مبلغ ② تابع کمکی واحد نمایش «مبلغ + ارز» (بازمصرف offerFmtMoney/CURRENCIES) ③ اسناد ارزی همه‌جا با کد ارز (EUR/USD/¥)، ریالی با «ت» ④ تستر: CO ارزی در فهرست پیشنهادها/پرونده/بایگانی/مطالبات با ارز درست. | **برآورد:** کوچک-متوسط | **خانواده:** نمایش مالی (نزدیک US-414 تسعیر فروش — پیشنهاد: هم‌اسپرینت با US-414)

## US-417 — ویراستار هوشمند فهرست‌ها: تلفیق تکراری‌ها + پاکسازی رکوردهای بی‌مصرف (بزرگ → ۲ فاز)
**شرح (کارفرما):** با رشد داده، مشتری/سرنخ/تامین‌کننده تکراری توسط کاربران مختلف وارد می‌شود. هوش مصنوعی فیلدها را ویراستاری و مشابه‌ها را تلفیق کند و همه لینک‌ها (پیشنهادات و...) با رکورد تلفیقی هماهنگ شوند. رکوردهای بی‌مصرف (مثلا بدون شماره تماس) اول تکمیل و در صورت شکست حذف شوند.
**زیرساخت موجود:** موتور dedup (ثبت‌گاه — US-174) + custmerge v14.7 (ادغام دستی مشتری با به‌روزرسانی لینک‌ها) + llm.php — پس فاز۱ بدون AI هم ارزش کامل دارد.
**فاز ۱ — اسکنر تکراری/بی‌مصرف + تلفیق نیمه‌خودکار:** ① دکمه «🧹 ویراستار فهرست» (مشتری/تامین‌کننده/سرنخ — erdBy ارشد): اسکن شباهت با موتور dedup موجود (نام نرمال/شناسه/تلفن مشترک) → فهرست زوج‌های مشکوک با امتیاز شباهت ② پیش‌نمایش تلفیق (فیلد به فیلد — کامل‌ترین مقدار پیش‌فرض) → اجرای ادغام با **موتور custmerge موجود** (توسعه به تامین‌کننده/سرنخ) — همه لینک‌ها (offers.buyerCd، rfqs، deals، payables، smsbook، امتیازها) به رکورد نهایی ③ رکوردهای بی‌مصرف (بدون هیچ راه تماس + بدون هیچ تراکنش) → فهرست «تعیین تکلیف»: تکمیل دستی / حذف با دلیل (ptfReasonedDelete موجود) — **هیچ حذف خودکاری بدون تایید کاربر**.
**فاز ۲ — لایه هوش مصنوعی:** ① llm.php برای موارد خاکستری (شباهت متوسط — «شرکت آریا کنترل» vs «آریاکنترل صنعت») با توضیح دلیل ② پیشنهاد تکمیل خودکار رکورد ناقص از سایر فیلدها/سوابق ③ سهمیه/fallback مثل سایر مصارف AI.
**قاعده ایمنی پنل:** ادغام همیشه با تایید انسانی + بک‌آپ خودکار قبل از هر ادغام گروهی + audit کامل + قابلیت undo از ptf_backup_prerestore. | **برآورد:** بزرگ (۲ اسپرینت) | **خانواده:** کیفیت داده

**صف پیشنهادی به‌روزشده:** US-413 (کیس R8) → US-414 + US-416 (هم‌خانواده نمایش/تسعیر ارز) → US-415 (کوچک — قابل چسباندن به US-413) → US-417 فاز ۱ → فاز ۲ → سپس US-410/411/405..408/402/403.
**شماره بعدی: US-418**
