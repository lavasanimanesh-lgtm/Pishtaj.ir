# RELEASE NOTES — v34.7.29 (۱۴۰۵/۰۵/۲۶ — 2026-08-17)

## خواستهٔ کارفرما
> «در پرونده‌های فروش، در زمان صدور پیش‌فاکتور، قیمت اقلام به‌صورت پیش‌فرض باید معادل قیمت‌های مندرج در پیشنهاد یا پیشنهادهای مالی پرونده باشد.»

## ارزیابی — سه نقص واقعی پیدا شد (بازتولیدشده در هارنس)

قیمت هر ردیف فقط از **همان پیشنهادی که ردیف از آن آمده بود** خوانده می‌شد
(`unofficialInvoiceSnapshotLines` / `unofficialInvoiceConsolidateLines`)، در حالی که سیاست مکتوب خودِ ماژول «پیش‌فرض قیمت = CO» بود. نتیجه:

| # | نشانه | بازتولید پیش از وصله |
|---|--------|----------------------|
| ۱ | انتخاب پیشنهاد فنی (TO) در حالت تک‌پیشنهاد ⇒ **همهٔ قیمت‌ها صفر** | `شیر توپی → 0 \| فلنج → 0` |
| ۲ | حالت تجمیعی **وابسته به ترتیب** پیشنهادها بود؛ ادغام «اولین ردیف برنده» بود، پس اگر TO زودتر می‌آمد قیمت CO دور ریخته می‌شد | `[TO, CO] → 0 \| 0` در برابر `[CO, TO] → 5,000,000 \| 250,000` |
| ۳ | ردیف‌های صفر هنگام صدور **بی‌صدا حذف** می‌شدند (فیلتر `price > 0`) | پیش‌فاکتور بدون اطلاع کاربر ناقص صادر می‌شد |

## راهکار — «دفتر قیمت پرونده»

- **`unofficialInvoicePriceBook(offers, preferredNo)`** — نگاشت قلم ⇒ قیمت، فقط از **پیشنهادهای مالی** پرونده.
  اولویت مرجع: **پیشنهاد برندهٔ پرونده ← CO برنده ← سایر CO ← TC**. پیشنهاد فنی (TO) هرگز مرجع قیمت نیست.
- **`unofficialInvoiceItemKey(it)`** — تطبیق قلم با کد کالا، سپس نام/شرح نرمال‌شده (نیم‌فاصله و فاصله خنثی می‌شوند).
- **پرکردن فقط جای خالی**: اگر خود ردیف قیمت واقعی دارد، هرگز بازنویسی نمی‌شود؛ کاربر هم مثل قبل می‌تواند هر ردیف را دستی ویرایش کند.
- **ادغام تجمیعی مستقل از ترتیب**: قلم تکراری با قیمت واقعی برندهٔ ادغام می‌شود (نه اولین ردیف)، و پس از ادغام، جای‌های خالی از دفتر قیمت پر می‌شوند.
- **ایمنی ارز**: قیمتی که ارزش با ارز سند نمی‌خواند **کپی نمی‌شود**؛ ردیف با پیام «ارز پیشنهاد مالی متفاوت است — قیمت را دستی وارد کنید» علامت می‌خورد.
- **شفافیت منشأ**: در ستون «پیشنهاد» هر ردیف، اگر قیمت پیش‌فرض از پیشنهاد دیگری آمده باشد، «قیمت از PTF-CO-…» نمایش داده می‌شود.
- **پایان حذف خاموش**: اگر قلمی در هیچ پیشنهاد مالی قیمت نداشته باشد، هنگام صدور فهرست آن اقلام نشان داده و **تأیید صریح** گرفته می‌شود.
- **سازگاری عقب‌رو**: امضای هر دو تابع با آرگومان اختیاری گسترش یافت؛ فراخوان‌های قدیمی (بدون دفتر قیمت) خودشان دفتر را می‌سازند و همان نتیجه را می‌دهند.

## آزمون

- تستر جدید `_tools/uat/tester432-v34.7.29-proforma-price-defaults.js` — **۲۳ PASS / ۰ FAIL** (اجرای رفتاری در `vm`، نه grep)، شامل:
  الزام اصلی، استقلال از ترتیب، اولویت پیشنهاد برنده، ایمنی ارز، کلید تطبیق قلم، و چهار assertion **عدم رگرسیون**
  (قیمت واقعی بازنویسی نشود، چند CO مستقل قیمت خودشان را نگه دارند، قلم بی‌قیمت واقعی صفر بماند، فراخوان قدیمی کار کند).
- گیت CI: **۴۷ PASS / ۲ FAIL** (همان دو `known-open` فاز G) — تستر جدید به گیت اضافه شد.
- سوئیت کامل: **۴۱۵ تستر — ۳۵۶ سبز، ۵۹ قرنطینهٔ مستند، ۰ شکست واقعی**.
- نگهبان معماری: **PASS** (بدون تخلف جدید).
- بامپ نسخه در ۷ نقطه ⇒ **v34.7.29**.
