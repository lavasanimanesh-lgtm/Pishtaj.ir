# v34.39.30 — CONTACT-ROOTS R7 + US-IRR2FX + CONTACT-SYNC-DIAG (ادغام در شاخهٔ Arena، ۲۰۲۶-۰۹-۲۵)

> زمینه: شاخهٔ `arena/01a0cb49-pishtaj-ir` پس از v34.39.27 سه نسخهٔ دیگر (v34.39.28 تا v34.39.30) داشت، ولی هیچ‌کدام به استیجینگ نرسید. اجراهای «Deploy to Staging» هر سه کامیت (`879b54d`، `e33cc01`، `1112468`) و کامیت پایه همان ابتدا در گام «نگهبان معماری» متوقف شدند: A6 (نسخه) و در کد جدید A3 و A10.
> پایهٔ مشترک با `main`: `ad7a21a` (v34.39.27). `main` بعد از آن چهار رفع در PR #51 داشت (`20f574f`، `5ec339b`، `422632d`، `145cb93`) که در شاخهٔ مبدأ نبودند؛ این ادغام هر دو طرف را نگه می‌دارد.

## v34.39.30 — CONTACT-ROOTS R7: اتحاد تماس رکورد به رکورد در `data_push`

- **ریشه**: flush عادی فاز B یک `data_push` کامل (blob) است، نه `entity_upsert`. گارد R6 (`sync_contact_fields_fill`) فقط کلیدهای تماسِ **غایب** را از سرور پر می‌کرد. برای همین رکوردِ کهنهٔ یک دستگاه، کلیدِ «حاضر اما کهنه/خالی» را کامل بازنویسی می‌کرد و شمارهٔ تازهٔ سرور بی‌صدا پاک می‌شد (گزارش: «شمارهٔ دوباره ثبت‌شده نمایش داده نمی‌شود»).
- **`api/contact-merge-lib.php`** (جدید): منبع مشترک قواعد اتحاد تماس. `sync_contact_records_merge` در `api/crm.php` جای R6 را گرفت. قواعد:
  1. کلیدِ غایب از سرور پر می‌شود (همان R6).
  2. رکوردِ «تازه»، یعنی مبنای رکورد برابر نسخهٔ فعلی سرور و ویرایشِ جدیدتر: آخرین نوشتن برنده است (LWW) و پاک‌سازیِ آگاهانه هم پذیرفته می‌شود.
  3. رکورد با مبنای کهنه یا نامشخص: کانال‌ها اجتماع (UNION) می‌شوند و کانالی که فقط روی سرور هست هرگز حذف نمی‌شود.
  4. `_ccClear` روی مبنای کهنه نادیده گرفته می‌شود و هر دو مُهر (`_ccClear`/`_ccBaseAt`) پیش از ذخیره برداشته می‌شوند.
- توابع پایهٔ مسیر `entity_upsert` در `api/sales-domain.php` (`sd_contact_*`) به همان کتابخانه alias شدند. شش تابع منتقل‌شده، صرف‌نظر از توضیحات، با نسخهٔ قبلی یکسان‌اند؛ پس رفتار مسیر فرمان فروش عوض نمی‌شود.
- ستون تماس در فهرست مشتری/تأمین‌کننده حالا همهٔ کانال‌ها را به ترتیب اولویت و با شمارندهٔ «+N» نشان می‌دهد (`ptfCustContactChannels`/`Cell`).
- مبنای مُهر خودکار `_ccClear` از «کش لحظهٔ ذخیره» به «snapshot لحظهٔ بازشدن مودال» (`window._ptfContactFormRec`) منتقل شد.
- **هات‌فیکس**: در خط جدول پیشنهادها در `offers.js` (از v34.39.28) سه پرانتز اضافه بود و خطای syntax می‌داد؛ این خطا کل ماژول offers را از کار انداخته بود.

## v34.39.29 — CONTACT-SYNC-DIAG: تشخیص سینک تماس مشتری

- **`crm/contact-sync-diag.js`** (جدید): دکمهٔ «تشخیص سینک تماس» کنار «🛟 بازیابی تماس‌ها». تازه‌ترین رکورد را مستقیم از سرور می‌خواند (`data_pull` فقط برای همین کلید، بی‌کش) و آن را با کش و آینهٔ همین دستگاه مقایسه می‌کند.
- نتیجه یکی از این حکم‌هاست: `stale-local` (با رفرش حل می‌شود)، `local-pending` (در صف آفلاین است)، `never-synced`، `record-missing` یا `ok`. کنار حکم، وضعیت دستگاه هم نمایش داده می‌شود: فاز B، صف آفلاین، نوشتن‌های ناموفق و آخرین خطای سینک.
- **ثبت سریع** فقط برای admin و رئیس هیئت‌مدیره است، با confirm صریح و audit: یک `ptfEntityUpsert` روی آخرین نسخهٔ سرور با مُهر `_ccBaseAt`. اگر شماره از قبل در یکی از بخش‌های رکورد سرور باشد، ثبت رد می‌شود.

## v34.39.28 — US-IRR2FX: تبدیل پیشنهاد ریالی به ارزی

- **`crm/offer-fx-convert.js`** (جدید، قرینهٔ `offer-rial-convert.js`): از پیشنهاد ریالی یک نسخهٔ ارزیِ همراه و مستقل ساخته می‌شود (پیوند `fxOf`). انتخاب ارز مقصد (USD/EUR/CNY/AED/GBP) و نرخ، تبدیل اقلام و شرایط، و پیش‌نمایش سند هم در آن هست.
- **بدون دوباره‌شماری**: نسخهٔ ارزی هیچ‌وقت won/lost نمی‌گیرد و پیشنهاد اصلی دست‌نخورده می‌ماند.
- رابط کاربری در فهرست پیشنهادها (نسخه‌های ارزی داخل همان ردیف)، کانبان (نشان و دکمه) و نوار ابزار پروندهٔ فروش اضافه شد.

## ادغام و هم‌راستاسازی (کارهای این شاخه)

- **تعارض‌ها**: فقط در رشتهٔ نسخه بود (`VERSION.json`، `PTF_CRM_RELEASE` در `crm/index.html`) و به نفع v34.39.30 حل شد. فیلدهای اضافهٔ `version`/`release` در `VERSION.json` که روی 34.39.28 مانده بودند و هیچ مصرف‌کننده‌ای نداشتند حذف شدند.
- **A6، ناهماهنگی نسخه**: شاخهٔ مبدأ `sales-domain.php`، `manifest.json`، `clear-cache.html`، `shell.js`، `cms.js` و `device-reconnect.html` را عقب گذاشته بود. همهٔ نقاط رسمی روی v34.39.30 یکدست شدند و pin تسترها با `bump-version-pins.js 34.39.27` به‌روز شد (۱۵۸ فایل، ۶۷۲ مورد).
- **تخلف‌های جدید نگهبان معماری در کد شاخهٔ مبدأ**، با کمترین تغییر و بدون تغییر رفتار، رفع شدند:
  - A3 در `offer-fx-convert.js`: بررسی یکتایی شماره به `x && x.no === newNo` تغییر کرد (همان الگوی `salesfiles.js`؛ خالی‌نبودن `newNo` از قبل تضمین شده بود).
  - A10 در `contact-sync-diag.js`: `localStorage.getItem('ptf_sync_krevs')` حذف شد و مقدار از API فقط‌خواندنیِ لایهٔ داده خوانده می‌شود (`ptfSyncDiagnosticsSnapshot().localKeyRevisions` در `sync.js`). اگر در دسترس نباشد `{}` برمی‌گردد، یعنی pull کامل: کندتر، ولی درست.
- **tester676**: این تستر هنوز قرارداد حذف‌شدهٔ R6 را pin کرده بود (`sync_contact_fields_fill`). حالا قرارداد R7 را می‌سنجد:
  - پورت ۱:۱ از `sync_contact_records_merge` و `contact-merge-lib`.
  - سناریوی S1 گزارش، LWW برای رکورد تازه، نادیده‌گرفتن `_ccClear` کهنه و UNION با ارقام فارسی/انگلیسی.
  - مقایسهٔ خروجی پورت JS با **PHP واقعی**، هر جا php با mbstring در دسترس باشد (روی CI). اگر در دسترس نباشد این بخش skip می‌شود.
  - تنها قراردادی که عمداً عوض شد ۶.۲ است: «کلیدِ حاضرِ خالی» روی رکوردِ کهنه دیگر شمارهٔ سرور را پاک نمی‌کند.

## نگهداری

- `VERSION.json` روی v34.39.30. نقاط رسمی نسخه: `index.html` (`PTF_CRM_RELEASE`)، `sw.js` (`RELEASE`، `ASSET_VERSION`، `CACHE`)، `manifest.json`، `cms.js` (`PTF_CMS_JS_VER`)، `shell.js`، `clear-cache`، `device-reconnect` و `SD_SERVICE_VERSION`.
- گیت‌های محلی:
  - `node _tools/arch/arch-guard.js`: PASS. A7 فقط گزارشی است: `sfFxTermsSave`، قرینهٔ `sfRcTermsSave` در مبنا.
  - `node _tools/uat/run-ci-gate.js`: همهٔ تسترها سبز.
  - `node --check` روی crm: سالم.
  - `php -l` روی ۳۲ فایل api و crm با PHP 8.3 (WASM): سالم.
  - `seo_redirect_audit.py --strict`: سالم.
- یک پیگیری باز: v34.39.28 و v34.39.29 هنوز تستر اختصاصی در گیت CI ندارند. سناریوهایی که نویسندهٔ شاخهٔ مبدأ اجرا کرده بود در مخزن ثبت نشده‌اند.
