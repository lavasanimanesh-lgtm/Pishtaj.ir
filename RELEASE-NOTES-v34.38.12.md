# CRM v34.38.12

## سپر «حذف انبوه» — جلوگیری از تکرار حذف ناخواستهٔ داده

پیشامد واقعی روی پروداکشن (۰۸ سپتامبر ۲۰۲۶): کلید `ptf_crm_rfqsmart` («درخواست تأمین»)
از **۶۸ رکورد به ۱ رکورد** رسید، بدون هیچ سنگ‌قبری در بایگانی حذف؛ و چون سپر قرنطینهٔ
بک‌آپ فقط ۸ کلید ثابت را می‌پایید، همهٔ بک‌آپ‌های چرخشیِ بعدی هم همان نسخهٔ ناقص را ثبت
کردند. شرح کامل در `ARENA-CRM-RFQSMART-MASS-DELETION-RCA-2026-09-09.md`.

- **سرور — `data_push`**: ردیفی که روی سرور هست، در payload نیست و **سنگ‌قبر ندارد**،
  «گم‌شده» شمرده می‌شود. اگر گم‌شده‌ها هم‌زمان بیش از ۳ ردیف و بیش از ۲۵٪ مجموعه باشند،
  ذخیره رد و کلید به‌عنوان `conflict` با نسخهٔ سرور برگردانده می‌شود (کلاینت merge و
  اجتماع را دوباره ارسال می‌کند). رویداد در `sync/mass_deletion_blocked.log` ثبت می‌شود.
  حذف‌های عادی، حذف‌های سنگ‌قبردار، `restore`، `allow_wipe` و کلیدهای union بدون تغییرند.
- **سرور — قرنطینهٔ بک‌آپ**: به‌جای فهرست دستی ۸ کلیدی، هر مجموعه‌ای که در بک‌آپ قبلی
  شمارش دارد پایش می‌شود؛ افت >۵۰٪ باعث قرنطینهٔ نسخهٔ جدید می‌شود.
- **کلاینت — گیت bootstrap برای مسیر فاز B**: `pushViaPhaseB()` تا پیش از تکمیل اولین
  دریافت کامل، هیچ آرایهٔ کاملی نمی‌فرستد (صف dirty پایدار می‌ماند و بعداً ارسال می‌شود).
- **کلاینت — پوشش کامل سپرها**: `GUARD_KEYS` از روی `SYNC_KEYS` ساخته می‌شود؛ نقطهٔ کور
  `ptf_crm_rfqsmart` و هر کلید جدیدی خودکار پوشش می‌گیرد.
- **کلاینت — مصون‌سازی مبنا**: وقتی آینهٔ فاز B فعال ولی آب‌رسانی‌نشده است، مبنای
  `ptf_guard_counts` به‌روزرسانی نمی‌شود (پیش از این، نمای ناقص مبنا را صفر می‌کرد و سپر
  خودش خلع‌سلاح می‌شد) و آشکارساز افت انبوه هشدار کاذب نمی‌دهد.
- گزارش سپر سروری به‌صورت توست و audit به کاربر نشان داده می‌شود.

## نگهداری

- تستر `tester619-v34.38.12-mass-delete-shield.js` (بازتولید عددیِ همان حادثه + مرزهای
  مجاز) به گیت CI اضافه شد.
- cache-buster و Service Worker به نسخهٔ `34.38.12` ارتقا یافتند.
