# 🎛 ROADMAP — سیستم جامع مدیریت محتوا و سئو با هوش مصنوعی در CRM
**تاریخ: ۲۰۲۶-۰۸-۳۱ · مبنا: کد ادغام‌شدهٔ شاخهٔ سئو (15d12a4) + ممیزی‌های `_audit/`**

> چشم‌انداز مالک: تولید صفحات جدید (مرکز دانش، محصولات، …)، رفع ایراد سئوی صفحات، درخواست ایندکس و به‌روزرسانی سایت‌مپ در GSC — همه از داخل CRM با هوش مصنوعی.
> این سند: (۱) موجودی دقیق امروز، (۲) شکاف‌ها، (۳) معماری هدف، (۴) فازبندی اجرایی با سنجهٔ پذیرش.

---

## ۱) موجودی امروز — چه چیزهایی «هست» و چقدر کامل‌اند

### ۱-۱) لایهٔ هوش مصنوعی — `api/llm.php` (بلوغ: خوب)
- **پروایدر**: Gemini پیش‌فرض یا هر سرویس OpenAI-compatible؛ کلید فقط سمت سرور (`llm-config.php` خارج از webroot) — هرگز به مرورگر نمی‌رود.
- **زنجیرهٔ fallback مدل** (مثلاً flash → flash-lite)، **کش SHA-256 پرامپت** (هزینهٔ تکرار = صفر)، **سقف نرخ per-IP**، پیام خطای فارسی با علت واقعی (IP، مدل، کلید).
- **اکشن‌های سئو آماده**: `seo_article` (تولید مقاله)، `seo_expand` (گسترش)، `seo_meta` (تایتل/دیسکریپشن)، `seo_fix` (اصلاح)، `seo_review` (بازبینی پیش از انتشار).
- پشتیبانی ورودی تصویر (vision) — برای OCR استفاده می‌شود؛ **برای alt-text تصاویر هنوز استفاده نشده**.

### ۱-۲) ماژول «مدیریت سایت» — `crm/cms.js` (۹۲۴ خط) + `api/cms.php` (۷۸۷ خط)
| قابلیت | وضعیت | جزئیات |
|---|---|---|
| اخبار | ✅ | افزودن/ویرایش/جابه‌جایی/حذف + انتشار |
| مرکز دانش | ✅ | فرم ساخت + **AI بنویس** (`seo_article`) + **AI گسترش** + **بازبینی AI اجباری پیش از انتشار** (تأیید انسانی) + انتشار |
| بلاگ | ✅ | ساخت + AI مقاله + Markdown→HTML + انتشار + بایگانی |
| انتشار امن | ✅ | sanitize سمت سرور (لیست سفید تگ، حذف `on*=` و `javascript:`)، حداقل ۲۰۰ کاراکتر، **بک‌آپ قبل از هر نوشتن** (`crm/data/cms-backups`)، قالب از صفحهٔ واقعی سایت |
| سایت‌مپ خودکار | ✅ | `sitemap_add/remove/touch_index` روی هر انتشار؛ نقشهٔ تکه‌تکه + ایندکس با lastmod واقعی |
| پس از انتشار | ✅ | پرسش «نقشه در سرچ کنسول ثبت شود؟» → `gscSubmitSitemap()` واقعی |

### ۱-۳) پویشگر سئو — `cms_seo_scan` + UI
- **۶۵۰ صفحهٔ عمومی** پویش می‌شود: h1، طول عنوان/توضیح، canonical (+ خودارجاع + **مشترک بین صفحات**)، hreflang، og، twitter، **JSON-LD**، **لینک ورودی**، **متن یکتای واقعی** (جداسازی قالب مشترک — متدولوژی `_audit/KC-DIAGNOSIS`).
- UI: فیلتر ایراد، فیلتر پوشه، جستجو، صفحه‌بندی، خروجی CSV.
- هر صفحه: **AI متا** (با متن واقعی صفحه)، **AI فیکس**، ویرایش دستی متا + ذخیرهٔ سروری روی فایل (`page_meta_save` با بک‌آپ).

### ۱-۴) پنل سرچ کنسول — `crm/gsc.js` + `api/gsc.php`
- **احراز واقعی**: سرویس‌اکانت JWT با اسکوپ `webmasters` (نه کشوندن UI؛ API واقعی).
- نمای کلی (کلیک/نمایش/CTR/جایگاه + روند)، کلمات، صفحات، **فرصت‌ها** (winها)، بدون‌کلیک، فیلتر برند.
- **`inspect`**: URL Inspection API واقعی (verdict/coverageState/lastCrawlTime/robots/pageFetch) + لینک مستقیم «درخواست ایندکس» — *با دلیل فنی درست: Indexing API خودِ گوگل فقط برای JobPosting/BroadcastEvent است و برای مقاله/محصول نادیده گرفته می‌شود؛ چاره‌ای جز UI گوگل نیست.*
- **`sitemap_submit`**: فراخوانی PUT واقعی `webmasters/sitemaps`.
- **`coverage`**: مقایسهٔ URLهای نقشه با دادهٔ GSC → فهرست صفحات بدون داده (کاندیدای ایندکس‌نشدن) + لینک inspect، کش ۱ ساعته.
- **گیت امنیتی قوی**: `auth.php` + توکن (برخلاف cms.php — ببینید شکاف ۱).

### ۱-۵) ممیزی‌های موجود (`_audit/`)
- `SEO-HEALTH-SCAN-2026-08-31.csv` — ۶۵۰ صفحه؛ مسائل فعلی: ~۳۰ canonical مشترک، صفحات با متن یکتای کم، برخی بدون JSON-LD.
- `KC-REWRITE-BACKLOG-2026-08-31.csv` — **۱۷۴ ردیف** صفحات مرکز دانش با متن یکتای کم، اولویت‌بندی‌شده (اولویت ۱ شروع شده).
- `KC-CONSOLIDATION-*` — ادغام ۴۰ صفحهٔ تکراری انجام‌شده.

### ۱-۶) RBAC
- کلاینت (`perms.js:40`): admin/chairman/ceo/commercial.
- سرور `api/cms.php`: همان چهار نقش — **ولی از هدر `X-CRM-Role` کلاینت** (شکاف ۱).
- سرور `api/gsc.php`: توکن واقعی (`auth_verify_token`) — الگوی درست همین است.

---

## ۲) شکاف‌ها نسبت به چشم‌انداز (اولویت‌بندی‌شده)

| # | شکاف | اهمیت | وضعیت امروز |
|---|---|---|---|
| ~~۱~~ | ~~امنیت cms.php~~ **بسته بود** — بازبینی کد پس از ادغام: از قبل `auth_verify_token` + نقش از identity (اصلاح v34.9.1 نشست سئو) | ✅ | — |
| **۲** | **مولد صفحهٔ محصول وجود ندارد**: CRM دیتای محصولات را دارد (`ptf_crm_products`) ولی هیچ مسیری از دیتا → صفحهٔ سئوشدهٔ سایت نمی‌سازد؛ `catalog/` فقط ۱ صفحه | 🔴 بالا | خواستهٔ مستقیم مالک |
| **۳** | **عملیات گروهی AI نیست**: متا/فیکس فقط تک‌صفحه با کلیک | 🔴 بالا | ۶۵۰ صفحه × ۱ کلیک غیرعملی |
| **۴** | **حلقهٔ coverage → اقدام ناقص**: صفحات بدون داده فقط لینک inspect دارند؛ نه صف، نه پیگیری، نه تاریخچهٔ وضعیت ایندکس | 🟠 متوسط | نیمه‌حلقه |
| **۵** | **پیش‌نویس ماندگار نیست**: فرم‌ها مودال‌اند؛ بستن = از دست رفتن متن AI | 🟠 متوسط | — |
| **۶** | **تاریخچه/rollback در UI نیست**: بک‌آپ سروری گرفته می‌شود ولی هیچ UI برای diff/بازگردانی نیست | 🟠 متوسط | بک‌آپ کور |
| **۷** | **لینک‌سازی داخلی ابزار ندارد**: اسکریپت‌های یک‌بار مصرف python؛ اسکن «لینک ورودی» را دارد ولی گزارش صفحهٔ یتیم + پیشنهاد AI لینک در UI نیست | 🟠 متوسط | — |
| **۸** | **حلقهٔ کلمه‌کلید → محتوا نیست**: کلمات/فرصت‌های GSC دیده می‌شوند ولی «خوشه‌بندی کلمات → پیشنهاد صفحهٔ جدید/اصلاح صفحهٔ موجود» وجود ندارد | 🟠 متوسط | — |
| **۹** | **مدیریت canonical مشترک/ادغام**: ~۳۰ مورد در اسکن؛ ادیتور ریدایرکت/کانونیکال مرکزی نیست | 🟠 متوسط | دستی |
| **10** | **alt تصویر با AI**: vision آماده است ولی استفاده نمی‌شود | 🟢 کم | — |
| **11** | **اندازه‌گیری اثر**: «صفحات دست‌کاری‌شدهٔ AI در برابر بقیه» مقایسه نمی‌شود؛ اسنپ‌شات تاریخی GSC ذخیره نمی‌شود | 🟢 کم | — |
| **12** | انتشار زمان‌بندی‌شده / جریان تأیید دومرحله‌ای / هزینهٔ AI داشبورد | 🟢 کم | — |

---

## ۳) معماری هدف — «حلقهٔ بستهٔ سئو»

```
┌─────────────────┤ GSC API (JWT سرویس‌اکانت) ├─────────────────┐
│   داده: کلمات/فرصت‌ها/coverage/inspect          اسنپ‌شات روزانه │
▼                                                                │
│ بینش ──► AI (llm.php) ──► پیش‌نویس/اصلاح ──► تأیید انسانی ──► انتشار
│  ▲          متا/متن/لینک/alt                        (گیت اجباری)   │
│  │                                                                  ▼
│ شکاف‌ها/ایتام‌ها ◄──── پویشگر ۶۵۰ صفحه ◄──────────── سایت‌مپ خودکار + submit
│                                                                      │
└────────────── اندازه‌گیری: قبل/بعد، AI-دست‌نخورده ◄──────────────────┘
```
اصل معماری: **همهٔ نوشتن‌ها از دو مسیر موجود** (`api/cms.php` + بک‌آپ/سنیتایز، `api/llm.php` + کش/سقف) — هیچ مسیر سومی اضافه نمی‌شود؛ فقط حلقه‌ها بسته می‌شوند.

---

## ۴) فازبندی اجرایی (به سبک R-فازهای مخزن، هر فاز با تستر + گیت ۱۷۰/۰)

### S1 — «بستن حلقه‌ها» (پیشنهاد شروع؛ بدون قابلیت جدیدِ بزرگ، فقط اتصال قطعه‌های موجود)
1. **امنیت**: `api/cms.php` → توکن `auth.php` (همان الگوی gsc.php) — حذف اعتماد به هدر نقش. *(شکاف ۱؛ پیش‌نیاز همه‌چیز)*
2. **صف گروهی AI**: انتخاب N صفحه از اسکن (فیلتر ایراد) → صف سمت سرور (فایل صف + اجرای تدریجی با سقف n/min) → نتیجه در «صف تأیید» با diff متا → تأیید گروهی/تکی. *هزینه با کش موجود llm.php کنترل می‌شود.*
3. **پایپ‌لاین ایندکس**: تب «ایندکس‌نشده‌ها» = coverage موجود + دکمهٔ inspect برای هر ردیف + ثبت نتیجه در تاریخچه (`cms/data/gsc-history/*.json`) + ستون «آخرین وضعیت/تغییر از دفعهٔ قبل».
4. **سایت‌مپ**: سوییچ «ثبت خودکار بعد از هر انتشار» (پیش‌فرض روشن؛ حذف confirm فعلی) + گزارش انحراف (URLهای نقشه بدون فایل و بالعکس).
5. **پیش‌نویس ماندگار**: ذخیرهٔ خودکار فرم‌ها در IDB (الگوی devkv موجود) — بستن تب متن AI را نمی‌پراند.
6. **صفحهٔ یتیم**: گزارش «لینک ورودی = ۰» از اسکن موجود + پیشنهاد AI: «از کدام ۳ صفحه به آن لینک شود» (دکمهٔ درج در بازبینی).

**سنجهٔ پذیرش S1**: تستر جدید (اغلب استاتیک + رفتاری vm)؛ گیت ۱۷۰/۰؛ یک چرخهٔ واقعی: ۲۰ صفحه متای AI → تأیید → انتشار → submit نقشه.

### ✅ S1 — «بستن حلقه‌ها» — ساخته‌شده (۲۰۲۶-۰۸-۳۱، v34.10.0)

- **صف متای AI** (سرور `seo_queue_*` پنج اکشن + کلاینت): افزودن تا ۶۰ صفحهٔ فیلترشده → تولید متا گروهی از متن واقعی صفحه (فاصلهٔ ۱.۲ث، دکمهٔ توقف) → **دیف قدیم→جدید با تیک تأیید انسانی** → اعمال با همان موتور page_meta_save (بک‌آپ/محافظ جهش حجم/ابطال کش اسکن) → گزینهٔ ثبت نقشه پس از اعمال.
- **حلقهٔ ایندکس**: هر inspect در تاریخچهٔ سروری ثبت می‌شود (`gsc-inspect-history.json`، ۵ ورودی/URL) و پنجرهٔ بررسی «وضعیت قبلی + تغییر کرده/بدون تغییر» را نشان می‌دهد؛ اکشن `inspect_log`.
- **نقشهٔ خودکار**: سوییچ «🔄 ثبت خودکار نقشه» در تول‌بار سئو (پیش‌فرض روشن، از `ptfDevKv`) + ثبت بی‌صدا (`gscSubmitSitemapQuiet`) پس از هر انتشار KC/بلاگ و پس از اعمال صف + **🧭 گزارش انحراف نقشه** (URLهای روح / فایل‌های خارج از نقشه).
- **پیش‌نویس ماندگار** فرم‌های مرکز دانش و بلاگ (ذخیرهٔ خودکار ۷۰۰ms در `ptfDevKv` — اصل A10؛ بستن مودال متن AI را نمی‌پراند؛ پاک‌شدن خودکار پس از انتشار).
- **یتیم‌ها**: اسکن حالا لینک ورودی هر صفحه را می‌شمارد (نرمال‌سازی مسیر، خودلینک/خروجی مستثنا) → chip «یتیم» + دکمهٔ «💡 لینک‌سازی» → اکشن جدید `seo_intlinks` در llm.php (۳ منبع + انکر فارسی + محل درج، از کاندیداهای هم‌پوشه).
- تستر555 (۴۱ سنجه)؛ گیت **171 PASS / 0 FAIL**؛ PHP لینت با php-parser؛ همهٔ قواعد معماری (A10) پابرجا.

### ✅ S2 — «مولد صفحات» — ساخته‌شده (۲۰۲۶-۰۸-۳۱، v34.11.0)

- **مولد صفحهٔ محصول** (تب «🛒 محصولات» در مدیریت سایت): فهرست کالاهای CRM با وضعیت انتشار (اتصال دوطرفه با `meta ptf-product-cd` ↔ `siteSlug` روی رکورد) → مودال مولد: «🤖 تولید با AI» (اکشن `seo_product`: intro/features/applications/FAQ فقط از دیتای واقعی کالا؛ قانون سخت «عدم اختراع قیمت/موجودی/ابعاد») → ویرایش + **دروازهٔ بازبینی انسانی** → انتشار در `products/<slug>.html` با قالب هم‌شکل سایت، جدول مشخصات، FAQ، و اسکیمای **Product/Offer(درصورت قیمت)/BreadcrumbList/FAQPage**.
- **سایت‌مپ**: نگاشت `products/` → `sitemap-products.xml` + رفع یک باگ ساختاری: زیرنقشهٔ تازه‌ساز تا امروز هرگز در `sitemap-index.xml` ثبت نمی‌شد (`sitemap_index_ensure`).
- **ادیتور ریدایرکت** (تب سئو): تبدیل صفحه به stub امن (refresh+canonical به مقصد+noindex، همان الگوی blog_archive) + حذف از نقشه + رجیستری زنده + «↩️ بازگردانی» از بک‌آپ؛ فقط مقصد داخلی مجاز.
- پیش‌نویس ماندگار فرم محصول؛ ثبت خودکار نقشه پس از انتشار؛ تستر556 (۳۶ سنجه)؛ گیت **172 PASS / 0 FAIL**؛ PHP لینت.
- **باقی‌ماندهٔ S2 → بعدی**: ~~قالب‌های نوع صفحه برای services/industries/comparisons~~ (✅ S2-id در v34.13.0)؛ ادیتور canonicalِ گروهی → S4.

### ✅ S2-id — «مولد صفحهٔ عمومی» — ساخته‌شده (۲۰۲۶-۰۹-۰۱، v34.13.0)

- **`page_create` در api/cms.php**: انتشار صفحات `services/ | industries/ | comparisons/<slug>.html` از اسکلت مرکز دانش (astm-a105) با اسکیمای **Service** (services: provider/areaServed=IR) یا **Article** + BreadcrumbListِ والدِ بخش، OG/canonical، sanitize لیست‌سفید تگ‌ها + پاکسازی onclick/js:، بک‌آپ پیش از بازنویسی، `sitemap_add` با نگاشت خودکار به sitemap-services/industries/misc، خطاهای فارسی (exists/پوشهٔ نامعتبر/حداقل ۲۰۰ حرف).
- **UI تب «📄 صفحهٔ جدید»** (مدیریت سایت): فرم ۶فیلدی با پیش‌نویس ماندگار (A10)، «🤖 تولید با AI» (`seo_article` — فقط فیلدهای خالی پر می‌شوند)، دروازهٔ تیکِ بازبینی انسانی، exists→confirm→overwrite، ثبت خودکار نقشه پس از انتشار.
- **فیکس‌های توسعه‌ای سئو**: ① `cmsSeoLinkSuggest(i, ev)` با event صریح (قبلاً global ضمنی — در ماژول‌های strict می‌شکست) ② ماندگاری وضعیت باز/بستهٔ زیرمنوی «مدیریت سایت» در `ptfDevKv` (قبلاً با هر رفرش می‌بست؛ موبایل همیشه‌باز محفوظ) ③ هایلایت نارنجی والد وقتی فرزند فعال + بازشدن یک‌بارهٔ گروه (delegation بدون interval).
- تستر558 (۲۸ سنجه)؛ گیت **174 PASS / 0 FAIL**؛ PHP لینت (php-parser)؛ node --check روی cms/gsc/careers/sw/shell.


### S2 — «مولد صفحات» (خواستهٔ مستقیم مالک)
1. **مولد صفحهٔ محصول**: از رکورد `ptf_crm_products` (نام/برند/مشخصات/عکس‌ها) + AI:
   - متن یگانه (نه قالب تکراری — درس KC-DIAGNOSIS)، جدول مشخصات، FAQ + `Product`/`Offer`/`BreadcrumbList` JSON-LD، alt تصویر با vision.
   - انتشار → `sitemap_add` + submit خودکار؛ حذف/منطقهٔ محصول → 401/301 → **ادیتور ریدایرکت** (شکاف ۹).
   - سنکرون: نشانهٔ «منتشرشده در سایت» روی رکورد محصول CRM (رفت/برگشت).
2. **قالب‌های نوع صفحه** برای services/industries/comparisons (همان موتور KC، با اسکیمای متفاوت).
3. **ادیتور canonical/ادغام**: از فیلتر «canonical مشترک» اسکن → اقدام: ادغام (ریدایرکت + انتقال لینک) یا اصلاح canonical — با بک‌آپ.

**سنجهٔ پذیرش S2**: ۱۰ محصول واقعی منتشرشده با اسکیمای معتبر (تست Rich-Results گوگل دستی)؛ صفر شدن canonical مشترک؛ تستر + گیت سبز.

### ✅ S3 — «رشد داده‌محور» — ساخته‌شده (۲۰۲۶-۰۹-۰۱، v34.12.0)

- **فیکس «ثبت نقشه ناموفق»** (گزارش مالک): ریشه = PUT فقط با سطح Full مجاز است + احتمال عدم تطابق site_url کانفیگ با نوع پراپرتی واقعی. حالا `gsc_resolve_site` فهرست سایت‌های قابل‌دسترسی را می‌خواند، بهترین تطبیق (دقیق → sc-domain → prefix) را برمی‌گزیند و اگر سطح Restricted/Unverified باشد **خطای فارسی دقیق با ایمیل سرویس‌اکانت و مسیر ارتقا** برمی‌گرداند؛ پیام کلاینت هم کامل شد.
- **اسنپ‌شات روزانهٔ lazy** (بدون cron): هر بازکردن پنل GSC اگر ≥۲۰ ساعت از آخرین اسنپ‌شات گذشته، جمع + ۳۰ کوئری + ۳۰ صفحه در `crm/data/gsc-snaps/<date>.json` (نگهداری ۱۸۰ روز).
- **KPI روند**: نوار اسپارک‌لاین ۳۰ نقطهٔ آخر + دلتای درصدی کلیک/نمایش بین دو اسنپ‌شات آخر (رنگی).
- **🧠 برنامهٔ محتوا**: دکمهٔ تحلیل → اکشن `seo_clusters` در llm.php — خوشه‌بندی فرصت‌ها (quickwins + بدون‌کلیک) به ۳–۶ خوشه با تصمیم «صفحهٔ جدید / بهینه‌سازی صفحهٔ موجود» (قانون: جایگاه ≤۵ ⇒ هرگز صفحهٔ جدید نگو) → هر خوشه یک‌کلیک به فرم مقاله (پرفیل موضوع) یا تب سئو (جست‌وجوی همان کلمه).
- تستر557 (۲۷ سنجه)؛ گیت **173 PASS / 0 FAIL**؛ انکرهای تستر نسخه‌زدایی شدند (مصون از bump).

### S3 — «رشد داده‌محور»
1. **اسنپ‌شات روزانهٔ GSC** (lazy: باز کردن پنل + گذر ۲۰ ساعت → ذخیرهٔ `overview/queries/pages` با تاریخ) — بدون نیاز به cron هاست.
2. **پایپ‌لاین کلمه → محتوا**: خوشه‌بندی AI کلمات بدون‌کلیک/فرصت → تطبیق با صفحهٔ موجود (پیشنهاد اصلاح) یا مقالهٔ جدید (پیش‌نویس با کلمه در H1/متا) — یک کلیک تا فرم KC.
3. **داشبورد KPI سئو**: روند کلیک/نمایش، **مقایسهٔ صفحات AI-لمس‌شده در برابر بقیه** (متادیتای «آخرین ویرایش AI» روی فایل‌ها)، پوزیشن watchlist — ✅ watchlist در v34.16.0: ⭐ کنار هر کوئری + کارت «⭐ واچ‌لیست جایگاه» (آخرین جایگاه رنگی، دلتا ▲بهبود/▼افت، میلهٔ روند ۱۶ نقطهٔ آخر) از روی اسنپ‌شات‌های روزانهٔ موجود — بدون دادهٔ جدید و بدون فراخوانی اضافهٔ GSC (سرور: watch_toggle/watch_list در gsc.php، سقف ۳۰ کلمه، سری ≤۶۰). تستر561 (۱۸ سنجه).
4. **alt تصویر گروهی** برای تصاویر مقالات موجود (vision).

### ✅ S4 — «کیفیت و مقیاس» — ساخته‌شده (۲۰۲۶-۰۹-۰۱، v34.14.0)

- **انتشار زمان‌بندی‌شده (lazy، بدون cron)**: دکمهٔ «🕘 زمان‌بندی انتشار» در فرم صفحهٔ جدید → صفحه **همان لحظه رندر** و html نهایی در `crm/data/cms-sched.json` ذخیره می‌شود؛ هر فراخوانی api/cms.php (حتی sched_list) آیتم‌های موعدرسیدهٔ تأییدشده را با بک‌آپ + نقشه + لاگ منتشر می‌کند. سقف ۲۰ معلق، بازهٔ ۵دقیقه تا ۶۰ روز، نگهداری ۱۵ انتشارِ آخر.
- **جریان دومرحله‌ای نویسنده/منتشرکننده**: آیتم ثبت‌شده به دست `commercial` با st=pending می‌ماند تا admin/chairman/ceo تأیید کند (sched_approve)؛ «🚀 هم‌اکنون» و «↩️ لغو» هم با همان گیت نقش/مالکیت.
- **diff/rollback UI** (تب جدید «🛠 کیفیت»): فهرست گروهی بک‌آپ‌های `cms-backups` → مقایسهٔ title/desc/H1 + diff خطی (پیشوند/پسوند مشترک، سقف ۴۰۰ خط) → بازگردانی با **بک‌آپ از نسخهٔ فعلی پیش از بازنویسی** (بازگشتِ بازگشت ممکن) + به‌روزرسانی نقشه + بی‌اعتبارسازی کش سئو. ضد path-traversal (الگوی stamp سخت + cms_skip_dir).
- **داشبورد هزینهٔ AI**: llm.php توکن واقعی (Gemini usageMetadata / OpenAI usage، در نبود تخمین حرفی) + زمان پاسخ هر فراخوانیِ واقعی را در `crm/data/ai-usage.json` (تفکیک روز/اکشن/مدل، ۱۲۰ روز) می‌شمارد؛ cache-hitها هزینه ندارند و شمرده نمی‌شوند. اکشن `usage_stats` (گیت نقش از توکن تأییدشده) → کارت‌های مجموع ۳۰روز + جدول روزبه‌روز + اکشن‌های پرکاربرد؛ قیمت تخمینی per-model با بازنویسی `pricing` در llm-config.php.
- **PageSpeed (PSI)**: ۱۰ مسیر پول‌ساز قابل ویرایش (`psi-config.json`، فقط مسیرهای موجود) → سنجش موبایل هر مسیر حداکثر ۱بار/۶h (کلید اختیاری `psi_key` در gsc-config.php) → score/LCP/CLS/TBT/سئو + روند ۱۰ سنجش آخر؛ اجرای گروهی ترتیبی با نمایش پیشرفت.
- **hreflang خودکار**: مولد صفحه اگر `en/<هم‌مسیر>` روی دیسک موجود باشد سه‌گانهٔ fa-IR + en + x-default را تزریق می‌کند — بدون نسخهٔ انگلیسی هیچ لینکی به ۴۰۴ ساخته نمی‌شود.
- تستر559 (۴۴ سنجه)؛ گیت **175 PASS / 0 FAIL**؛ PHP لینت (cms.php + llm.php)؛ پنجرهٔ tester558 هم‌مسیر جابه‌جایی رندر شد.

### ✅ S5 — «سئوی بین‌المللی + بهداشت گروهی» — ساخته‌شده (۲۰۲۶-۰۹-۰۱، v34.15.0)

- **hreflang دوطرفه (fa ↔ en/)**: اکشن `hreflang_sync` جفت‌های هم‌مسیر را می‌یابد (فقط هر دو فایل موجود) و سه‌گانهٔ fa-IR + en + x-default=فارسی را در هر دو طرف یکسان می‌کند — idempotent (بلوک قبلی حذف، بدون تغییر = بدون نوشتن)، stubهای ریدایرکت مستثنا، بک‌آپ پیش از هر نوشتن. `index.html` به آدرس پوشه‌ای نگاشت می‌شود. کارت + دکمه در تب «🛠 کیفیت».
- **خود-کانونیکال‌سازی گروهی**: دکمهٔ «🔧 canonical گروهی» در تولبار سئو → اکشن `canonical_bulk` فقط صفحات با ایراد mismatch/no-canonical را به آدرس خودشان برمی‌گرداند (stubهای ریدایرکت با canonical عمدی مستثنا؛ سقف ۶۰ در هر اجرا؛ بک‌آپ + کشِ اسکن نو).
- **alt تصویر گروهی با بینایی AI**: `alt_scan` (فقط نبودِ صفتٔ alt؛ alt تزئینیِ خالی درست است؛ نرمال‌سازی قطعه‌ای مسیر؛ گیت ≤3MB) → `seo_alt` در llm.php (تا ۸ تصویر در یک فراخوانی؛ خواندن از دیسک سرور با realpath زیر ریشه؛ چندتصویری در هر دو provider Gemini/OpenAI؛ تصاویر در کلید کش) → جدول قابل ویرایش + **تیک بازبینی انسانی** → `alt_apply` (سقف ۴۰؛ پاکسازی متن؛ بک‌آپ یک‌بارهٔ هر صفحه).
- تستر560 (۳۸ سنجه)؛ گیت **176 PASS / 0 FAIL**؛ PHP لینت (cms.php + llm.php).

### S4 — «کیفیت و مقیاس»
انتشار زمان‌بندی‌شده، diff/rollback UI روی بک‌آپ‌های موجود، جریان تأیید دومرحله‌ای (نویسنده/منتشرکننده)، داشبورد هزینهٔ AI (شمارنده روی llm.php)، PageSpeed API برای ۱۰ صفحهٔ پول‌ساز، خودکارسازی hreflang با `en/`.

---

## ۵) شروع سریع (هفتهٔ اول — بدون فاز جدید)
1. تستر نسخهٔ سئو: بستن گیت (فعلاً ۱۷۰/۰ شامل cms/gsc نمی‌شود — تستر555 برای S1).
2. ۱۰ ردیف اول `KC-REWRITE-BACKLOG` (اولویت ۱) با فرم AI موجود بازنویسی — خود ابزار امروز آماده است؛ فقط نیروی انسانی/تأیید لازم دارد.
3. اصلاح ~۳۰ canonical مشترک از UI فعلی (فیلتر «canonical مشترک»).

## ۶) ریسک‌ها و واقعیت‌ها
- **Indexing API گوگل** برای محتوا کار نمی‌کند (فقط JobPosting/BroadcastEvent) — مسیر درست همان است که هست: inspect API + لینک UI. ادعای بیشتر = فروش مار؛ انجام‌پذیر نیست.
- **هزینهٔ AI**: کش + سقف موجود؛ صف گروهی باید سقف روزانه داشته باشد (پارامتر در llm-config).
- **هزینهٔ گوگل**: sitemap_submit زیاد لازم نیست — گوگل بعد از submit اول، کشیدن خودکار را یاد می‌گیرد؛ حالت خودکار فقط بعد از «تغییر واقعی».
- **درسِ قالب مشترک**: قالب ۸۰۰۰ کاراکتری ۸۰۰۰ کاراکتری درسِ مرکز دانش — هر مولد جدید باید متن یگانه تولید کند (سنجهٔ «متن یکتا» در اسکن، نگهبان خودکار همین است).
