# RELEASE NOTES — v34.7.76 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## مبنای ریالی ارجاع فاکتور به حسابدار (پیشنهادهای ارزی)

### تصمیم کارفرما

سندی که به حسابدار ارجاع می‌شود باید **ریالی** باشد:

1. پیشنهاد ارزی که **نسخهٔ ریالی دارد** → حسابدار **فقط نسخهٔ ریالی** را می‌بیند.
2. پیشنهاد ارزی که **نسخهٔ ریالی ندارد** → هنگام ارجاع، **نرخ تسعیر** از کاربر پرسیده
   می‌شود؛ سیستم ابتدا **نسخهٔ ریالی را می‌سازد** و سپس به حسابدار ارجاع می‌دهد.

### وضعیت قبلی (ریشهٔ مشکل)

ارجاع فاکتور (`sfInvoiceRefCommit`) فقط روی پیشنهاد ارزیِ برنده (`wonOffer`) لنگر بود؛
سند برد هم snapshot ارزی است و پنل فاکتورها (`official-invoice-v2.js`) صراحتاً نسخهٔ ریالی را
با `!o.rialOf` فیلتر می‌کرد. نتیجه: حسابدار مبلغ را از سند ارزی می‌دید و خودش دستی به ریال
تبدیل/تایپ می‌کرد — ریسک مغایرت با پیشنهاد ریالیِ مورد قبول کارفرما.

### تغییرات

#### ۱. هستهٔ ارجاع (`crm/salesfiles.js`)

- `sfInvoiceRefCommit(cd, rialBasisNo)`:
  - پیشنهاد **ریالی (IRR)** → مبنای ارجاع = خود سند؛
  - پیشنهاد **ارزی دارای نسخهٔ ریالی** → مبنای ارجاع = نسخهٔ ریالی؛
  - پیشنهاد **ارزی بدون نسخهٔ ریالی** → برمی‌گرداند `why:'need_rial'` (مسدودکننده، بدون ثبت).
  - `o.invRef` حالا فیلدهای `rialBasis` / `rialRate` / `rialTotal` / `fxNo` / `fxCurrency`
    را هم دارد؛ `awardDoc` (snapshot برد) و `fromFile` دست‌نخورده می‌مانند.
- `sfInvoiceRef(cd)`: اگر `need_rial` بود، دیالوگ نرخ تسعیر باز می‌شود.
- `sfInvoiceRefRialPrompt` + `sfInvoiceRefRialDo`: نرخ تسعیر و تاریخ را می‌گیرد،
  `ptfOfferRialConvertCommit` را صدا می‌زند (ساخت نسخهٔ ریالی)، سپس
  `sfInvoiceRefCommit(cd, res.no)` + `sfInvoiceRefFinish` اجرا می‌شود.
- `sfInvoiceRefFinish(cd)`: پیامک/توست/render موفقیت (مشترک بین مسیر عادی و مسیر ساخت ریالی).

#### ۲. پنل حسابدار (`crm/official-invoice-v2.js`)

- `renderInvoices`: برای پیشنهاد ارزیِ ارجاع‌شده، اگر نسخهٔ ریالی داشته باشد (از `invRef.rialBasis`
  یا تشخیص خودکار `ptfRialCompanionOf`) **فقط نسخهٔ ریالی** به‌عنوان مبنای فاکتور نمایش داده
  می‌شود: شمارهٔ ریالی + جمع ریالی + نرخ تسعیر + پیشنهاد ارزی مبدأ (به‌صورت متن خاکستری).
  دکمهٔ **«💱 مبنای ریالی (PDF)»** (چاپ نسخهٔ ریالی) و «👁» پیش‌نمایش؛ دکمهٔ سند برد ارزی فقط
  برای پیشنهادهای ریالیِ بدون companion باقی است.
- `showInvModal`: کادر برجستهٔ **«مبنای ریالی»** (شماره + جمع ریالی + نرخ + پیشنهاد ارزی مبدأ) و
  **پیش‌فرض مبلغ پایه = جمع ریالی** (قابل ویرایش، غیرمسدودکننده).
- `saveInv`: `rialBasisNo` / `rialBasisRate` / `rialBasisTotal` به payload ثبت فاکتور اضافه شد.

#### ۳. سرور (`api/sales-domain.php`)

- رکورد `register_invoice` فیلدهای `rialBasisNo` / `rialBasisRate` / `rialBasisTotal` را
  برای رد ممیزی ذخیره می‌کند (هم در ثبت و هم در اصلاحیه).

### نکته

- `wonOffer` (منبع حقیقت برد) و `awardDocs` (snapshot تغییرناپذیر) دست‌نخورده می‌مانند؛
  نسخهٔ ریالی فقط «مبنای ارائه/فاکتور» است، نه جایگزین سند برد قانونی.
- برای ارجاع‌های قدیمیِ از قبل ارجاع‌شده، در صورت وجود companion، پنل به‌صورت خودکار همان را
  مبنای ریالی نشان می‌دهد (مهاجرت نرم بدون دست‌کاری داده).

### فایل‌ها

- `crm/salesfiles.js` — هستهٔ ارجاع + دیالوگ نرخ تسعیر + ساخت خودکار نسخهٔ ریالی.
- `crm/official-invoice-v2.js` — نمایش مبنای ریالی + پیش‌فرض مبلغ + payload.
- `api/sales-domain.php` — فیلدهای rialBasis در رکورد فاکتور.
- `_tools/uat/tester478-v34.7.76-invoice-rial-basis.js` — تستر جدید (ثبت در گیت CI).

### اعتبارسنجی

- `node _tools/arch/arch-guard.js` → `=== ARCH GUARD: PASS ===`
- `node _tools/uat/run-ci-gate.js` → `94 PASS / 0 FAIL`
