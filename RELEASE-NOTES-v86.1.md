# 🤖 ریلیز v86.1 — فعال‌سازی هوش مصنوعی (US-196 فاز ۲ + OCR)

**تاریخ:** ۱۵ تیر ۱۴۰۵ | **پیش‌نیاز:** کلید Gemini که گرفتید ✅

---

## 🚀 چه چیزهایی اضافه شد؟

### ۱. زیرساخت سروری — `api/llm.php` (جدید)
- کلید فقط سمت سرور (فایل `llm-config.php` **خارج از public_html** — مثل کاوه‌نگار/آروان)
- درخواست‌ها از IP هاست خارجی به گوگل می‌رود → همکاران از داخل ایران **بدون VPN** استفاده می‌کنند
- گارد same-origin + سقف ۶۰ درخواست/ساعت + فقط شرح کالا/تصویر استعلام ارسال می‌شود (**هیچ نام کارفرما/قیمتی نه**)
- ⚡ **قابل تعویض**: اگر روزی Gemini محدود شد، با تغییر ۳ خط کانفیگ روی Groq/OpenRouter/واسطه ریالی سوییچ می‌شود (OpenAI-compatible) — بدون تغییر کد

### ۲. 🤖 OCR فایل استعلام (تکمیل US-195)
در ویزارد «📖 خواندن فایل استعلام»: دکمه جدید **«🤖 خواندن خودکار PDF/عکس»** (فقط وقتی LLM فعال باشد ظاهر می‌شود) — فایل تا ~۶MB → همه اقلام با تایپ/شرح/مشخصات/تعداد/واحد استخراج می‌شوند → **بازبینی و تایید نهایی همچنان با شماست** (هشدار صریح نمایش داده می‌شود). اطلاعات سربرگ/کارفرما/قیمت در استخراج نادیده گرفته می‌شود.

### ۳. 🤖 ترجمه شرح کالا فا ↔ EN (سوال ۱ شما)
در فرم TO/CO دو دکمه بالای جدول اقلام (وقتی LLM فعال): **«فارسی → English»** و **«English → فارسی»** — ترجمه فنی تخصصی (استانداردها/سایزها/رده‌ها دست نمی‌خورند)، جایگزینی با اطلاع و قابل بازبینی قبل از ذخیره.

### ۴. 🤖 تشخیص برند از کد/مدل (سوال ۲ شما — طبق سیاست مصوب)
دکمه **«تشخیص برند از مدل (پیشنهادی)»**: فقط ردیف‌های بدون برند بررسی می‌شوند؛ برند فقط وقتی درج می‌شود که کد مدل **قطعاً** متعلق به سازنده مشخص باشد (3051→Rosemount، EJA→Yokogawa...) و با اطمینان ≥۷۰٪ — با پسوند **«(AI?)»** تا تایید/اصلاح کنید. هرگز از شرح جنرال برند حدس زده نمی‌شود.

### ۵. نشانگر وضعیت
تنظیمات → باکس «🤖 هوش مصنوعی»: سبز = فعال (با نام سرویس و مدل) / زرد = کانفیگ نشده.

## 📥 کاری که شما باید بکنید (۳ قدم — ۲ دقیقه)

1. فایل **`llm-config.sample.php`** (در ریشه بسته) را باز کنید، کلیدتان را در `'key'` بگذارید
2. نام فایل را به **`llm-config.php`** تغییر دهید
3. با File Manager آن را **یک پوشه بالاتر از public_html** آپلود کنید (کنار `sms-config.php` و `storage-config.php`)

سپس در CRM → تنظیمات → باکس 🤖 باید **سبز** شود. راهنمای کامل: `DOCS-LLM-SETUP.md`
⚠️ طبق قانون همیشگی: کلید را در چت/ایمیل نفرستید.

## ✅ کیفیت
tester26 جدید: **20/20 PASS** (شامل چک عدم نشت کلید در خروجی) | رگرسیون ۲۱ تستر همه PASS | ممیز ۸بخشی PASS | کش → `ptf-crm-v86.1`

## 📦 نصب
**`pishtaj-release-v86.1-delta.zip`**: `api/llm.php` و `llm-config.sample.php` و `DOCS-LLM-SETUP.md` (جدید)، `crm/inqreader.js`، `crm/index.html`، `crm/sw.js`، `api/.htaccess` + مستندات.

> 🧪 تست بعد از گذاشتن کانفیگ: ① تنظیمات → باکس 🤖 سبز ② درخواستی با عکس استعلام → 📖 → «🤖 خواندن خودکار» ③ فرم TO با شرح فارسی → «🤖 فارسی → English» ④ ردیفی با مدل 3051 → «🤖 تشخیص برند» → Rosemount (AI?)
