# ارزیابی عمیق و راهکار — سه موضوع کارفرما (۱۴۰۵/۰۵/۲۶ / 2026-08-17)

پایه: کامیت `6aebe34` (v34.7.29) | وضعیت: **فقط ارزیابی — هیچ کدی تغییر نکرده است؛ اجرا منوط به تأیید شماست.**

سه موضوع:
1. **الف)** تغییر سند برد از داخل پروندهٔ فروش (اقلام مردود در بازرسی، قیمت جدید، سرنوشت اقلام: انبار یا عودت به فروشنده) + گزارش آن.
2. **ب)** در فرم پیشنهاد، «بارگذاری اقلام درخواست» فهرست کشویی باز می‌کند و با انتخاب درخواست هیچ اتفاقی نمی‌افتد.
3. **ج)** قیمت واردشده در درخواست تامین یا ماژول کالا به‌عنوان «نرخ مرجع» در پیشنهاد دیده نمی‌شود؛ و درخواست شما: نرخ مرجع روی اقلام درخواست وجود داشته باشد و با تغییر در پیشنهاد، به‌روز شود.

خلاصهٔ حکم: **مورد ب و ج باگ قطعی‌اند** (ریشه‌یابی شد، همگی از تغییرات بعدی آمده‌اند — تأیید حرف شما که «قبلاً کار می‌کرد»). **مورد الف باگ نیست؛ قابلیت غایب است** و همان چیزی است که در نقشهٔ فازبندی به‌عنوان «فاز G — بازنگری سند برد» منتظر تصویب شما مانده بود.

---

# بخش ۱ — مورد «ب»: بارگذاری اقلام درخواست کار نمی‌کند

## ۱-۱) سه ریشهٔ مستقل (هر سه اثبات‌شده روی کد)

### B1 🔴 وصلهٔ روی تابع، آرگومان را دور می‌ریزد (علت اصلی «هیچ اتفاقی نمی‌افتد»)

`crm/offers.js:1545` تابع اصلی را با پارامتر تعریف می‌کند:
```js
function offLoadInqItems(pickedInq) { ... }
```
و دیالوگ انتخاب، درخواست را با آرگومان صدا می‌زند (`crm/offers.js:1566`):
```js
onclick="...; offLoadInqItems('RFQ-1405-021')"
```
اما `crm/inqreader.js:1238-1246` همین تابع را بازنویسی می‌کند:
```js
_offLoadOld = window.offLoadInqItems;
window.offLoadInqItems = function () {   // ← بدون پارامتر
  var before = _offState.items.length;
  _offLoadOld();                          // ← بدون آرگومان صدا زده می‌شود
  ...
};
```
یعنی هر بار که از دیالوگ یک درخواست انتخاب می‌کنید، شمارهٔ انتخابی **دور ریخته می‌شود**؛ تابع اصلی دوباره بدون ورودی اجرا می‌شود، می‌بیند شماره‌ای ندارد و **همان فهرست را دوباره باز می‌کند**. دقیقاً همان چیزی که دیدید: «لیست باز می‌شود، انتخاب می‌کنم، هیچ اتفاقی نمی‌افتد».

این وصله برای US-195 (ستون Type) اضافه شده بود؛ نگهبان معماری فعلی «بازنویسی خاموش» را می‌گیرد ولی «وصلهٔ زنجیره‌ای که آرگومان را منتقل نمی‌کند» را نمی‌گیرد ⇒ **قاعدهٔ جدید A8 لازم است**.

### B2 🔴 ناسازگاری کلید بین کشویی و محل ذخیرهٔ اقلام

کشویی «شماره درخواست» مقدارش **کد سیستمی RFQ** است (`crm/dedup.js:211-216`: `add(sysCd, ...)` با `sysCd = r.cd`)، ولی اقلام درخواست در `ptf_crm_inqitems` با **شمارهٔ درخواست کارفرما** (`inqNo`) ذخیره می‌شوند (`crm/bridge.js:1628`).
در لودر (`crm/offers.js:1573`) تطبیق چنین است:
```js
getData('ptf_crm_inqitems').filter(r => r.inqNo === inq || r.cd === inq)
```
که `r.cd` **کد خود قلم** (`IQI-…`) است، نه کد درخواست. پس وقتی کشویی کد سیستمی می‌دهد، هیچ قلمی پیدا نمی‌شود و پیام «اقلامی برای درخواست … یافت نشد» می‌آید — یا در بهترین حالت از مسیر جایگزین (`rfqs.items`) نجات پیدا می‌کند.
این همان خانوادهٔ «هویت رکورد» است که در `ARCHITECTURE-GUARDRAILS.md` قرارداد شد: باید نام‌های مستعار درخواست (`cd`, `inqNo`) با هم دیده شوند.

### B3 🟠 رفتار مطلوب شما اصلاً پیاده نشده: بارگذاری خودکار پس از انتخاب درخواست

`offerPickInq(inqNo)` (`crm/offers.js:1270`) فقط `_offState.inqNo` و مشتری را ست می‌کند و **اقلام را بارگذاری نمی‌کند**. بنابراین کاربر مجبور است دکمهٔ جداگانه بزند و آن‌جا با B1/B2 روبه‌رو شود.
خواستهٔ شما: «لیست کشویی نباید باز شود و فقط اقلام همان درخواست بارگذاری شوند.»

## ۱-۲) راهکار مورد ب

| کد | اصلاح | فایل |
|----|--------|------|
| **FB-1** | وصله باید آرگومان‌ها را عبور دهد: `_offLoadOld.apply(this, arguments)` و امضای وصله هم `function (pickedInq)` | `crm/inqreader.js:1240` |
| **FB-2** | تابع «حل درخواست» واحد: `ptfResolveInqRequest(key)` که رکورد RFQ را با هر یک از نام‌های مستعار (`cd`/`inqNo`) پیدا می‌کند و **همهٔ** منابع قلم (`ptf_crm_inqitems` با هر دو کلید، `ptf_crm_inqreads`، `rfq.items`) را برمی‌گرداند — بر پایهٔ `PTF.sameEntity` (قرارداد v34.7.28) | `crm/offers.js` |
| **FB-3** | `offerPickInq` پس از انتخاب، **خودکار** اقلام همان درخواست را بارگذاری می‌کند (با پیام «۷ قلم بارگذاری شد») و دیالوگ انتخاب فقط زمانی باز می‌شود که هیچ درخواستی انتخاب نشده باشد | `crm/offers.js` |
| **FB-4** | اگر درخواست انتخاب‌شده هیچ قلمی ندارد، پیام دقیق با راه‌حل («اقلام را در استعلامات/درخواست تامین ثبت کنید») — نه سکوت و نه باز شدن دوبارهٔ فهرست | `crm/offers.js` |
| **FB-5** | قاعدهٔ **A8** در نگهبان معماری: هر وصلهٔ زنجیره‌ای روی تابع سراسری باید `apply(this, arguments)` کند؛ در غیر این‌صورت گیت CI قرمز می‌شود | `_tools/arch/arch-guard.js` |

---

# بخش ۲ — مورد «ج»: نرخ مرجع در پیشنهاد دیده نمی‌شود

## ۲-۱) پنج ریشه (به‌ترتیب اثر)

### C1 🔴 «اقلام درخواست» اصلاً فیلد قیمت ندارند
فرم ثبت قلم درخواست (`crm/bridge.js:1622-1640`) فقط این‌ها را ذخیره می‌کند:
`inqNo, cd, nm, en, qty, un, st, t` — **هیچ فیلد قیمت/نرخ مرجعی وجود ندارد**.
پس آنچه شما در «درخواست تامین» وارد می‌کنید یا در جای دیگری (استعلام تامین) ذخیره می‌شود یا اصلاً ذخیره نمی‌شود. این دقیقاً همان چیزی است که خواسته‌اید اضافه شود.

### C2 🔴 لودر، قیمت را صفر می‌گذارد و کد کالا را هم منتقل نمی‌کند
`crm/offers.js:1585-1595`:
```js
var item = { name:…, desc:…, model:…, qty:…, unit:…, brand:…, dlv:'', price: 0, sourceInq: inq };
```
- `price: 0` ثابت است (هیچ نرخی از منبع خوانده نمی‌شود)،
- **`pcode` منتقل نمی‌شود** — درحالی‌که همان‌جا `ptfAutoRegisterSummaryProducts` کالاها را در بانک کالا می‌سازد. یعنی قلم پیشنهاد هیچ‌وقت کد کالا ندارد.

### C3 🔴 بدون کد کالا، تطبیق مرجع به «نام+واحد» می‌افتد و واحدها ناسازگارند
`crm/procurement-link.js:41-47`: اگر کد نباشد، امتیاز از نام شروع می‌شود و
```js
if ((targetModel && sourceModel && targetModel !== sourceModel) || (targetUnit && sourceUnit && targetUnit !== sourceUnit)) score = 0;
```
لودر واحد را `r.un || 'NO'` می‌گذارد (`offers.js:1590`) ولی اقلام درخواست/استعلام واحد فارسی («عدد») دارند (`bridge.js:1617`, `rfqsmart.js:966`).
⇒ `NO ≠ عدد` ⇒ **امتیاز صفر ⇒ هیچ مرجعی پیدا نمی‌شود** (بی‌صدا).

### C4 🟠 کالاهای خودکار با `pr: 0` ساخته می‌شوند
`crm/storage.js:905`: کالاهای ساخته‌شده از خلاصهٔ استعلام همیشه `pr: 0` می‌گیرند. حتی اگر تطبیق درست انجام شود، مرجع صفر است مگر کسی دستی در ماژول کالا قیمت بگذارد.

### C5 🟠 پل «استعلام تامین → پیشنهاد» به یک کلید شکننده بسته است
`crm/offers.js:1694-1704`:
```js
var rfqs = getData('ptf_crm_rfqsmart').filter(r => r.srcRfq === st.inqNo || r.no === st.inqNo);
```
اگر استعلام تامین با کد سیستمی RFQ ساخته شده باشد و پیشنهاد `inqNo` کارفرما را داشته باشد (یا برعکس — همان B2)، این فیلتر خالی می‌شود و `bestBuyPrice` هرگز خوانده نمی‌شود.

### C6 🟠 مسیر بازگشتی (از پیشنهاد به مرجع) وجود ندارد
`refPriceEdited` فقط روی خود قلم پیشنهاد می‌ماند (`crm/offerlock.js:231-241`). هیچ نوشتنی به قلم درخواست یا کالا انجام نمی‌شود ⇒ خواستهٔ شما («نرخ مرجع قبلی با مقدار جدید پیشنهاد به‌روز شود») امروز اصلاً پیاده نیست.

## ۲-۲) راهکار مورد ج — «زنجیرهٔ نرخ مرجع»

**مدل داده (افزودنی و سازگار با گذشته):**
روی هر قلم درخواست (`ptf_crm_inqitems` و `rfq.items`):
```
refPrice   نرخ مرجع (عدد)
refCur     ارز نرخ (پیش‌فرض IRR)
refSrc     منبع: 'manual' | 'catalog' | 'rfqsmart' | 'offer'
refAt      تاریخ ثبت/به‌روزرسانی (شمسی)
refBy      کاربر
refHistory [{price, cur, src, at, by, offerNo}]   ← تاریخچه، بدون بازنویسی کور
```

**ترتیب قطعی «نرخ مرجع مؤثر» برای هر قلم پیشنهاد (منبع واحد، تابع `ptfItemRefPrice`):**
1. نرخ دستی همان قلم در پیشنهاد (`refPriceEdited`) — دست کاربر همیشه برنده.
2. `refPrice` قلم درخواست (چیزی که شما در درخواست تامین وارد می‌کنید).
3. `bestBuyPrice` از استعلام تامین (`ptf_crm_rfqsmart`) با تطبیق یکتا.
4. `pr` کالا در بانک کالا (با تطبیق یکتای کد).
در هر حالت **منبع و تاریخ** کنار عدد نمایش داده می‌شود تا کاربر بداند از کجا آمده.

| کد | اصلاح | اثر |
|----|--------|-----|
| **FC-1** | افزودن فیلد «نرخ مرجع + ارز» به فرم ثبت/ویرایش قلم درخواست و به جدول اقلام درخواست تامین | خواستهٔ صریح شما |
| **FC-2** | لودر اقلام: انتقال `pcode` (از تطبیق/ثبت خودکار کالا)، `sourceItemKey`، و مقداردهی `refPrice` طبق زنجیرهٔ بالا (به‌جای `price: 0`) | مرجع در پیشنهاد دیده می‌شود |
| **FC-3** | نرمال‌سازی واحد (`NO ⇄ عدد`, `PCS`, `SET`, …) در `procurement-link` تا تطبیق نام‌محور بی‌صدا صفر نشود | رفع تطبیق‌های ازدست‌رفته |
| **FC-4** | `ptfAutoRegisterSummaryProducts` هنگام ساخت کالا، `pr` را از نرخ مرجع قلم درخواست پر کند (اگر باشد) و کد کالای ساخته‌شده را به قلم برگرداند | مرجع پایدار می‌شود |
| **FC-5** | پل استعلام تامین با نام‌های مستعار درخواست (`PTF.sameEntity`) به‌جای تطبیق تک‌کلیدی | `bestBuyPrice` دوباره دیده می‌شود |
| **FC-6** | **نوشتن بازگشتی**: هنگام ذخیرهٔ پیشنهاد، اگر نرخ مرجع قلم تغییر کرده باشد، `refPrice` قلم درخواست با مقدار جدید به‌روز و رکورد قبلی در `refHistory` نگه داشته می‌شود (با ثبت شمارهٔ پیشنهاد و کاربر) | خواستهٔ صریح شما |
| **FC-7** | به‌روزرسانی `pr` بانک کالا **فقط با تیک صریح** «این نرخ را به‌عنوان نرخ مرجع کالا هم ثبت کن» | جلوگیری از آلوده‌شدن کاتالوگ (نیازمند تصمیم شما — بند پرسش‌ها) |

---

# بخش ۳ — مورد «الف»: بازنگری سند برد از داخل پروندهٔ فروش

## ۳-۱) وضعیت امروز (چرا سناریوی شما اجرا نمی‌شود)

| نیاز سناریو | وضعیت فعلی | شاهد |
|---|---|---|
| تغییر اقلام/قیمت سند برد پس از برد | ❌ ندارد. سند برد قفل است و «snapshot» می‌شود | `wonRevisionSnapshot` در `api/sales-domain.php:822`؛ پیام UI: «سند قطعی برد تغییرناپذیر است» (`crm/salesfiles.js:967`) |
| کاهش مبلغ قرارداد (حذف اقلام مردود) | ❌ ممنوع است. متمم فقط دلتای **مثبت** می‌پذیرد | `mark_amendment` ⇒ `invalid_amendment_amount` اگر جمع ≤ ۰ (`api/sales-domain.php:803`) |
| ثبت رد شدن **قلم‌به‌قلم** در بازرسی | ❌ ندارد. QC فقط رویداد متنی با وضعیت انطباق است | `sfQcOpen` / `qcEvents` (`crm/salesfiles.js:212`) — فیلدها: نوع، وضعیت، شرح |
| سرنوشت قلم مردود: ورود به انبار | 🟡 ماژول «موجودی انبار» هست ولی به بازرسی وصل نیست | `crm/surplus.js` (`ptf_crm_surplus`) |
| سرنوشت قلم مردود: عودت به فروشنده | ❌ ندارد. مرجوعی فقط سمت **مشتری** است | `ptf_crm_sales_returns` (LC-02) — هیچ `purchase_return` وجود ندارد |
| گزارش تجمیعی این تغییرات | ❌ ندارد | — |

نتیجه: امروز تنها راه‌های موجود، **دور زدن سیستم** است (ابطال فاکتور، متمم مثبت ساختگی، یا ویرایش دستی) که هرکدام ردپای مالی غلط می‌سازد.

## ۳-۲) طراحی پیشنهادی (همان «فاز G» با دامنهٔ دقیق‌شده به سناریوی شما)

**۱) بازرسی قلم‌به‌قلم (`caseInspection`)** — روی همان پروندهٔ فروش:
```
{ cd, at, by, offerNo, lines: [
    { itemKey, name, qtyOffered, qtyAccepted, qtyRejected,
      disposition: 'stock' | 'supplier_return' | 'rework' | 'scrap',
      dispositionQty, unitCost, note, files[] } ] }
```
هر ردیف باید جمع بخورد: `qtyAccepted + qtyRejected = qtyOffered` (گارد سروری).

**۲) فرمان سروری `revise_award`** (اتمیک، idempotent، نقش‌محور، با `correction`):
- ورودی: `caseId`, `reason` (اجباری), `lines[]` (اقلام باقی‌مانده با تعداد/قیمت جدید), `mode: 'replacement'`.
- خروجی: یک **سند برد جایگزین** (پیشنهاد CO جدید با `revisionOf`, `supersedesOfferId`) + `supersededByOfferId` روی سند قبلی + `effectiveContractAmount` جدید روی پرونده + بازسازی تخصیص‌ها (`sd_rebuild_allocations`) + رکورد `corrections`.
- هیچ رکورد مالی حذف نمی‌شود؛ سند قبلی بایگانی‌شده و قابل استناد می‌ماند.
- گارد: اگر فاکتور رسمی صادر و وصول شده باشد، کاهش زیر مبلغ وصول‌شده ممنوع (خطای صریح، نه تغییر بی‌صدا).

**۳) اتصال سرنوشت اقلام مردود:**
- `disposition = stock` ⇒ ثبت خودکار در «موجودی انبار» (`ptf_crm_surplus`) با منبع پرونده/سند برد و بهای تمام‌شده.
- `disposition = supplier_return` ⇒ رکورد جدید `ptf_crm_purchase_returns` با لینک به خرید واقعی/تعهد پرداخت؛ **اثر مالی فقط پس از تأیید مالی** (بستانکاری تامین‌کننده) — تا مدل تجاری‌اش را تأیید نکنید، صرفاً ثبت و گزارش می‌شود.
- `rework/scrap` ⇒ ثبت در پرونده (و در صورت زیان، مسیر موجود `ptfLossOpen`).

**۴) پیش‌فاکتور با قیمت جدید:** چون از v34.7.29 قیمت پیش‌فرض پیش‌فاکتور از «پیشنهاد مالی پرونده» خوانده می‌شود، به‌محض ثبت سند برد جدید، پیش‌فاکتور **خودکار** قیمت‌های جدید را می‌گیرد — بدون کار اضافه.

**۵) گزارش «بازنگری سند برد و سرنوشت اقلام»** (خواستهٔ صریح شما) — یک صفحهٔ چاپی + CSV:
| بخش | محتوا |
|-----|--------|
| سرصفحه | پرونده، مشتری، سند برد اولیه و جایگزین، تاریخ و کاربر بازنگری، دلیل |
| مالی | مبلغ اولیه، مبلغ مؤثر جدید، دلتا (کاهش/افزایش)، مبلغ فاکتورشده، مبلغ وصول‌شده، مانده |
| اقلام | برای هر قلم: تعداد اولیه/پذیرفته/مردود، قیمت قبلی، قیمت جدید، دلیل |
| سرنوشت مردودها | تفکیک انبار / عودت به فروشنده / دوباره‌کاری / اسقاط با تعداد و ارزش |
| اثر پایین‌دستی | تغییر مبنای پورسانت، سود پروژه، و اسناد صادرشدهٔ متأثر (فاکتور/پیش‌فاکتور) |
| ردپا | شناسهٔ فرمان سروری، `correction`، و شمارهٔ نسخهٔ سند |

## ۳-۳) چرا این کار «پروژه‌ای» است و باید فازبندی شود
سه چیز هم‌زمان لمس می‌شود: هویت سند برد، مبلغ مؤثر قرارداد، و اثر مالی پایین‌دست (فاکتور، پورسانت، سود، انبار، حساب تامین‌کننده). به همین دلیل در نقشهٔ قبلی به‌عنوان فاز G با «دروازهٔ تصویب» گذاشته شده بود. پیشنهاد من اجرای آن در **چهار گام کوچک و مستقل** است (جدول بخش ۴).

---

# بخش ۴ — برنامهٔ اجرایی پیشنهادی

| گام | محتوا | وابستگی | اندازه | ریسک |
|-----|--------|---------|--------|------|
| **P1** | مورد ب کامل (FB-1..FB-5) + تستر | ندارد | کوچک | خیلی کم |
| **P2** | مورد ج، فاز خواندن (FC-1..FC-5): نرخ مرجع روی قلم درخواست + نمایش در پیشنهاد | P1 | متوسط | کم |
| **P3** | مورد ج، فاز نوشتن بازگشتی (FC-6/FC-7) با تاریخچه و ردپا | P2 | کوچک | کم (نیازمند تصمیم شما در بند ۵) |
| **P4** | بازرسی قلم‌به‌قلم + سرنوشت اقلام (بدون اثر مالی خودکار) | ندارد | متوسط | کم |
| **P5** | فرمان `revise_award` + سند برد جایگزین + بازسازی تخصیص | P4 | بزرگ | متوسط — نیازمند گاردهای مالی |
| **P6** | گزارش «بازنگری سند برد و سرنوشت اقلام» (چاپ + CSV) | P5 | متوسط | کم |
| **P7** | اثر مالی عودت به فروشنده (بستانکاری تامین‌کننده) | P5 + تأیید مدل مالی | متوسط | متوسط |

گیت خروج هر گام (بدون تغییر): تستر اختصاصی با ۰ شکست + `run-all.js` با ۰ شکست واقعی + نگهبان معماری سبز + `node --check` + توازن PHP + بامپ نسخه در ۷ نقطه + release notes.

پیشنهاد من: **P1→P3 را یک‌جا اجرا کنیم** (هر سه کوچک و بدون ریسک مالی‌اند و همین هفته گره کاری شما را باز می‌کنند)، سپس **P4→P6** به‌عنوان بستهٔ «بازنگری سند برد» و **P7** فقط پس از تأیید مدل مالی.

---

# بخش ۵ — تصمیم‌هایی که فقط شما می‌توانید بگیرید

1. **به‌روزرسانی بانک کالا:** وقتی نرخ مرجع در پیشنهاد تغییر می‌کند، «نرخ مرجع قلم درخواست» قطعاً به‌روز می‌شود (خواستهٔ شما). آیا `pr` کالا در بانک کالا هم به‌روز شود؟ پیشنهاد من: **فقط با تیک صریح کاربر**.
2. **کاهش قرارداد پس از فاکتور:** اگر برای پرونده فاکتور رسمی صادر شده باشد، بازنگری کاهشی چه کند؟ پیشنهاد من: **مسدود** با پیام صریح و هدایت به «اصلاحیه/ابطال فاکتور» — چون تغییر بی‌صدای مبلغ زیر فاکتور صادرشده، مغایرت مالیاتی می‌سازد.
3. **عودت به فروشنده:** فعلاً فقط ثبت و گزارش شود، یا از همان ابتدا بستانکاری تامین‌کننده هم بخورد؟ پیشنهاد من: **گام اول فقط ثبت/گزارش**، اثر مالی در P7.
4. **قیمت‌گذاری مجدد:** آیا قیمت جدید اقلام باید در «سند برد جایگزین» بنشیند (پیشنهاد من) یا فقط در پیش‌فاکتور اعمال شود و سند برد دست‌نخورده بماند؟ گزینهٔ دوم ساده‌تر است اما گزارش سود/پورسانت را از واقعیت جدا می‌کند.

---

## پیوست — تصمیم‌های کارفرما (۱۴۰۵/۰۵/۲۶) و وضعیت اجرا

| موضوع | تصمیم | وضعیت |
|--------|--------|--------|
| ترتیب اجرا | ابتدا **P1–P3** | ✅ اجرا شد — `RELEASE-NOTES-v34.7.30.md` |
| ثبت نرخ مرجع در بانک کالا | **فقط با تیک صریح** کاربر | ✅ پیاده شد (`#ofRefToCatalog`) |
| بازنگری کاهشی پس از صدور فاکتور رسمی | **مسدود** با پیام صریح | ⏳ در P5 اعمال می‌شود |
| عودت اقلام مردود به فروشنده | **اثر مالی فوری** (بستانکاری تامین‌کننده) | ⏳ در P5/P7 اعمال می‌شود |
