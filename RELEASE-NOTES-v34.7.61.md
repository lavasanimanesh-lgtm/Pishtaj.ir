# RELEASE NOTES — v34.7.61 (۱۴۰۵/۰۵/۳۰ — 2026-08-21)

## «متن آماده روی سربرگ» — شماره، پیوست و انتخاب محل مهر و امضا

ادامهٔ v34.7.60 (مسیر Paste از Word) با سه درخواست جدید کارفرما.

### ۱) شمارهٔ نامه (اختیاری)

- در مودال «📄 متن آماده روی سربرگ» فیلد **«شماره نامه»** اضافه شد.
- دکمهٔ **«↻ شمارهٔ بعدی»** شمارهٔ صادرهٔ بعدی را می‌نویسد:
  - فارسی: ارقام فارسی، قالب `۱۴۰۵/پ/ص/۰۰۰۹` (ادامهٔ سری همان سال، بدون گم‌شدن شماره).
  - English: سری میلادی `PTF-OUT-2026-XXXX`.
- شماره در سربرگ، کنار لوگو (همان جای «شماره» در نامهٔ عادی) درج می‌شود و با
  `letFaDigits` به ارقام فارسی نمایش داده می‌شود.

### ۲) پیوست (اختیاری)

- فیلد **«پیوست»** اضافه شد (مثلاً «۱ برگ شرح فنی») و در سربرگ کنار تاریخ/شماره درج می‌شود.
- در حالت English با برچسب `Encl.` نمایش داده می‌شود.

### ۳) محل مهر و امضا: تمام صفحات یا فقط صفحهٔ آخر

- گزینه‌ها بازنویسی شدند:
  - **تمام صفحات** — مهر و امضا با `position:fixed` روی همهٔ صفحات چاپی تکرار می‌شود.
  - **فقط صفحهٔ آخر** — بلوک امضا پس از پایان متن قرار می‌گیرد و با
    `break-inside:avoid` از شکستن وسط بلوک (افتادن روی صفحهٔ بعدی) جلوگیری می‌شود.
  - **بدون مهر و امضا (فقط سربرگ)**.
- مهر/امضا همچنان از پروفایل کاربر (`✍️ امضای من`) خوانده می‌شود.

### فنی

- فیلدهای سربرگ (تاریخ/شماره/پیوست) همه اختیاری‌اند؛ اگر هیچ‌کدام نباشد، سربرگ فقط
  لوگو دارد (مثل قبل — متن دست‌نخورده).
- فضای رزرو بالای صفحه (`headSpace`) وقتی شماره یا پیوست هم باشد بزرگ‌تر می‌شود تا متن
  زیر سربرگ نرود.
- متن ورودی همچنان فقط `letSafeBodyHtml` می‌شود و بازنویسی نمی‌شود.

### تست

- `tester463` بهروزرسانی شد (نسخهٔ v34.7.61 + قراردادهای شماره/پیوست/حالت صفحهٔ آخر).
- **گیت CI: ۷۹ PASS / ۰ FAIL** · نگهبان معماری: PASS.

### UAT پیشنهادی

1. متن چندصفحه‌ای را Paste کنید؛ شماره و پیوست را پر کنید → چاپ: هر صفحه سربرگ/فوتر
   داشته باشد و شماره/پیوست/تاریخ در سربرگ درست باشد.
2. دکمهٔ «↻ شمارهٔ بعدی» → شمارهٔ بعد از آخرین نامهٔ صادره درج شود.
3. حالت «فقط صفحهٔ آخر» → مهر فقط بعد از پایان متن (صفحهٔ آخر) باشد.
4. حالت «تمام صفحات» → مهر روی همهٔ صفحات تکرار شود.
