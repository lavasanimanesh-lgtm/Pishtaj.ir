# یادداشت انتشار v34.38.8 — CONTACT-RECOVERY-ONECLICK (بازیابی یک‌کلیکی تماس‌ها)

**تاریخ:** 2026-09-08
**بازوی تغییر:** Arena Agent
**کانال:** CRM (دادهٔ مشتریان / بک‌آپ سرور)

## درخواست کارفرما
> «مگر اطلاعات روی سرور موجود نیست؟ نمی‌شود خودکار برگردد؟ یا ابزاری بسازی که با یک کلیک برگردد و نخواهیم این همه کار انجام بدهیم — چون نمی‌دانم از کی اطلاعات پاک شده؛ برخی قدیمی‌تر و برخی جدیدتر هستند.»

## پاسخ فنی (به سؤال «مگر روی سرور نیست؟»)
وضعیت **فعلی** سرور برای رکوردهای شسته‌شده، تماس ندارد (فیلد روی‌جا بازنویسی شده و سرور history نگه نمی‌دارد)؛ **اما** بک‌آپ‌های چرخشی همان سرور (`hourly-latest` / `daily-*` سه روز اخیر / `weekly-latest` / `monthly-latest` / `suspect-*`) نسخه‌های تاریخی را در خود دارند و اکشن‌های `list_backups` و `get_backup` (با سپر gzip شفاف و نقش-محور) برای خواندن‌شان موجود بود. یعنی «داده روی سرور هست، ولی در بک‌آپ‌ها، نه در وضعیت فعلی» — این نسخه همان را با یک کلیک مصرف می‌کند، بدون این‌که «بازگردانی کامل دیتابیس» به ساعتِ گذشته را تحمیل کند.

## چه ساخته شد
- **ماژول `crm/restore-contacts.js`** + دکمهٔ **«🛟 بازیابی تماس‌ها»** در پنل مشتریان (کنار «ثبت» و «ورود اکسل»).
- جریان یک‌کلیکی: «شروع اسکن» → خواندن ترتیبی بک‌آپ‌های سرور از `list_backups`/`get_backup` → ساخت plan → پیش‌نمایش با چک‌باکس به‌ازای هر مشتری (فیلدهای بازیابی‌شده + منبع هر فیلد) → «تایید و بازگردانی» → ذخیره از روتر استاندارد → audit → رفرش جدول.
- **چندمنبعی (پاسخ مستقیم به «بعضی قدیمی‌تر بعضی جدیدتر»):** هر فیلد از جدیدترین بک‌آپی که خودِ آن فیلد را دارد می‌آید؛ توقف زودهنگام بهازای اولین برخورد وجود ندارد — یک رکورد می‌تواند `people` از hourly و `coTels` از daily را هم‌زمان دوباره بگیرد.

## چهار سپر امنیتی (قرارداد قفل‌شده با تست)
- **S1** فقط فیلدهای تماس نوشته می‌شود: `people, coTels, con, ph, coMail, coWeb, coAddr` — هر فیلد دیگری تخلف است.
- **S2** مقدار غیرخالیِ فعلی هرگز بازنویسی نمی‌شود (merge فقط روی خالی‌ها، هم در plan و هم در apply).
- **S3** هیچ رکوردی حذف/ایجاد نمی‌شود؛ تعداد رکوردها ثابت، تطبیق فقط با `cd` (به‌خاطر خطرِ «روح»ها، تطبیق اسمی عمداً نکرد).
- **S4/S5** نقش مجاز فقط `admin/chairman` (هم‌پایهٔ `role_guard('users_write')` روی سرور برای get_backup)؛ ذخیره با `reason: 'offer-cust'` از مسیر merge-safe استاندارد عبور می‌کند و رویداد audit ثبت می‌شود.

## همچنین در همین مسیر
- ابزار CLI `_tools/contact-recover.js` (نسخهٔ خارج‌برنامه‌ای برای فایل بک‌آپ دستی — بدون تغییر رفتاری CRM) حاضر و تست‌شده است.
- `crm/sw.js`: `./restore-contacts.js` در پیش‌کش آفلاین افزوده شد.

## سطح انتشار نسخه
`VERSION.json → v34.38.8`؛ `crm/index.html` (شامل تگ `restore-contacts.js?v=34.38.8`)؛ `crm/sw.js`؛ `crm/manifest.json`؛ `crm/clear-cache.html`؛ `crm/shell.js`؛ `crm/cms.js`؛ `api/sales-domain.php` (`SD_SERVICE_VERSION = '34.38.8'`). تمام پین‌بندی‌های تستری (فرم ساده و فرم Regex) به 34.38.8 هم‌مهاجرت شدند؛ نام فایلِ تسترهای تولدبرجسته (tester613/tester614) دست‌نخورده.

## تست‌ها
- **قفل جدید:** `_tools/uat/tester615-v34.38.8-contact-recovery-oneclick.js` — ۳۴ سنجه (منطق خالص vm از ماژول واقعی، اسکن شبکه با mock سرور، گارد نقش، مارکرهای UI/سیم‌کشی/امنیت، پین‌ها).
- **گیت کامل:** `run-ci-gate.js` → **230 PASS / 0 FAIL** (tester329 از قبل در HEAD قرمز پیش‌تاریخی بود — خارج از دامنه، بدون تغییر).

## راهنمای استفاده (یک خط)
پنل مشتریان → «🛟 بازیابی تماس‌ها» → «شروع اسکن» → مرور پیش‌نمایش → «تایید و بازگردانی» → صبر برای سینک → رفرش. اگر برای رکوردی «در هیچ بک‌آپی تماس یافت نشد» گزارش شد، آن اطلاعات قبل از شروع چرخهٔ بک‌آپ پاک شده و راهِ ماشینی ندارد.
