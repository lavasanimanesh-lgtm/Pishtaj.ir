# RCA دائمی — پایان وابستگی CRM به localStorage (v34.8.9)

تاریخ: ۱۴۰۵/۰۶/۴ (۲۰۲۶-۰۸-۲۶) | نوع: ریشه‌یابی معماری + رفع دائمی

## ۱) نشانهٔ کارفرما
پس از تعویض اکانت (رئیس هیات مدیره → مدیر بازرگانی → ادمین)، پیغام **«۱۰۰٪ پر بودن localStorage»**؛ اجرای «همگرایی دادهٔ محلی»، «پاک‌سازی امن فوری» و «پاک‌سازی کش محلی» هیچ فضایی آزاد نکرد. این چرخه در ماه‌های اخیر بارها توسط ایجنت‌های مختلف «رفع» شده و بازگشته است.

## ۲) زنجیرهٔ ریشه‌ای (چرا هیچ ابزاری کار نمی‌کرد)
معماری فعلی: دادهٔ کسب‌وکار به‌صورت cache در localStorage نگه داشته می‌شود (سقف مرورگر ≈۵MB). طرح نجات از قبل موجود بود — «حالت سرور-محور» (فاز B) که داده را به سرور/IndexedDB می‌برد و localStorage را فقط کش سبک می‌کند — اما هرگز روی دستگاه‌های قدیمی فعال نمی‌شد:

```
دستگاه قدیمی دادهٔ محلی دارد
   └─► ptfBAutoBootstrap: «existing_local_data» → فاز B روشن نمی‌شود (محافظت در برابر merge خودکار)
         └─► آینهٔ IndexedDB غیرفعال می‌ماند (ptfBMirrorActive = flag && synced && idb)
               └─► کل دیتاست شرکت (~۶۰ کلید، چند MB) برای همیشه در localStorage می‌ماند → ۱۰۰٪
کاربر «همگرایی» را می‌زند (ptfBFinalize با confirm)
   └─► داده به سرور push می‌شود و ✅ می‌گیرد — اما پرچم فاز B هنوز روشن نشده!
         └─► «پاک‌سازی کش محلی» → رد: «حالت سرور-محور فعال نیست» → ۰ بایت آزاد
کاربر «پاک‌سازی امن فوری» را می‌زنیم (emergencyCompact)
   └─► فقط کلیدهای فرّار (notifs/sendqueue/audit/چت…) را هرس می‌کند؛ کلیدهای کسب‌وکار را
       «عمداً» دست نمی‌زند → چند KB آزاد می‌شود در برابر MBها → بی‌اثر
تعویض اکانت وضعیت را بدتر می‌کند
   └─► نشانگر هم‌گرایی per-user است (ptf_b_synced_<user>)؛ کاربر تازه دوباره باید
       هم‌گرایی می‌کرد و گاردها دوباره رد می‌کردند → بن‌بست کامل برای همه
```

## ۳) راه‌حل دائمی (v34.8.9 — STORAGE-INDEPENDENCE)
اصل: **«هم‌گرایی موفق = پایان وابستگی»** — دیگر هیچ گام دستی بعدی لازم نیست:

1. **فعال‌سازی خودکار پس از همگرایی** (`ptfBEnableAfterConvergence`): لحظه‌ای که هم‌گرایی ACK کامل گرفت، پرچم فاز B روشن، هوک getData/setData نصب و کلیدهای کسب‌وکار (>۲۴KB) از localStorage به IndexedDB تخلیه می‌شوند (mirror خودش نسخهٔ localStorage را حذف می‌کند).
2. **تخلیهٔ هر بوت** (`ptfBOffloadBusinessKeysToIdb` در بوت): هر چیزی که مسیرهای قدیمی مستقیم در localStorage نوشته باشند، دوباره به IDB برمی‌گردد — خودترمیم مداوم.
3. **نگهبان سهمیه فعال** (storage-quota): در ≥۸۵٪ به‌جای هشدار منفعل، همان لحظه تخلیهٔ خودکار به IDB اجرا می‌شود (اگر فاز فعال باشد) و اگر نباشد، مسیر درستِ یک‌گامی به کاربر گفته می‌شود.
4. **رفع گارد چندکاربره**: پاک‌سازی کش دیگر به نشانگر کاربرِ فعلی وابسته نیست؛ اگر همین دستگاه با هر کاربری هم‌گرایی موفق داشته باشد کافی است (دادهٔ شرکت مشترک است).
5. **گاردهای داده دست‌نخورده**: هیچ داده‌ای بدون ACK کامل سرور حذف نمی‌شود؛ صف آفلاین غیرخالی همیشه پاک‌سازی را متوقف می‌کند.

نتیجه: localStorage از این پس فقط session/تنظیمات سبک را نگه می‌دارد؛ داده در MySQL/سرور معتبر، کش در IndexedDB (سقف صدها MB)، و پیغام «۱۰۰٪» ساختاراً دیگر رخدادنی نیست.

## ۴) چرا تلاش‌های قبلی بی‌حاصل بود
هر بار یا (الف) فقط emergencyCompact قوی‌تر شده بود (کلیدهای کسب‌وکار را هدف نمی‌گرفت)، یا (ب) حالت سرور-محور ساخته شده بود اما فعال‌سازی‌اش به کاربر و دو گام دستی وابسته بود که یکی‌اش (پرچم) در جریان هم‌گرایی خودکار زده نمی‌شد، یا (ج) نشانگرهای per-user بعد از تعویض اکانت ریست می‌شدند. این نسخه حلقه را در نقطهٔ ACK سرور می‌بندد، نه در رفتار کاربر.

## ۵) کنترل
tester514 + گیت CI کامل + arch guard (بعد از بامپ). اعمال روی استیجینگ؛ پیشنهاد انتشار پروداکشن پس از تأیید کارفرما.
