# RCA — ناپدیدشدن اطلاعات تماس مشتریان پس از ثبت
## (تلفن، اشخاص رابط، کانال تماس) — نسخهٔ v34.38.3

**تاریخ:** 2026-09-07
**وضعیت:** فاز تحلیل تکمیل شد و پس از گزارش جدید کارفرما («حوزهٔ کاری مشتری هم پاک می‌شود») **فیکس در v34.38.4 اعمال و با tester611 قفل شد** — گیت **226 PASS / 0 FAIL**. جزئیات فیکس در `RELEASE-NOTES-v34.38.4.md`.
**نتیجهٔ کلی:** زنجیرهٔ ریشه‌ای اصلی این باگ در v34.37.7 / v34.38.x **رفع و با تستر قفل (tester604، ۴۴ PASS / ۰ FAIL) مهار شده است**؛ مسیرهای اصلی ثبت/ویرایش/ایمپورت/heal/فرم سایت دیگر اطلاعات تماس را پاک نمی‌کنند. شکاف‌های باقی‌مانده (R1–R3) که همان امضای پاک‌شدن را داشتند ولی زیر سپر `AUTO_NO_DELETE` نبودند، در v34.38.4 بسته شدند.

---

## ۱. خلاصهٔ مدیریتی

| # | یافته | شدت | وضعیت در v34.38.3 |
|---|-------|------|-------------------|
| A | ۵ حلقهٔ ریشه‌ای پاک‌شدن تماس (روتر diff، migrateOnce، هوک items[0]، cbCollect، phones=[]) | بحرانی | **رفع‌شده + قفل تست** |
| B | سرور `entity_upsert` = merge (فیلد غایب ≠ پاک) | بحرانی | **رفع‌شده** (v34.8.34) |
| C | پاک‌ساز سرور آرایه‌های تودرتو (people[].tels/mobs/mails) | بحرانی | **رفع‌شده** (v34.29.7) |
| R1 | نویسندگانِ کل‌دفتر با `reason` خارج از `AUTO_NO_DELETE` (`lead-convert`, `ai-bizcard`, `ai-letterhead`, `ai-buyer`, `coen-fill`) | **P1** | **زنده** — مسیر حذف واقعیِ تک‌رکوردی هنوز باز است |
| R2 | `migrateContacts()` با `setData` خام (بیرون از روتر فرمانی) | P3 | زنده (فقط مسیر legacy، کم‌رخداد) |
| R3 | تغییر نوع حقوقی→حقیقی، `people`/`coTels` را عمداً صفر می‌کند و به `phones` منتقل نمی‌کند | P3 | زنده (طراحی v80.2) |
| R4 | fallback هوک phonefmt به `setData(کل مجموعه)` وقتی فرمان غیرفعال است | P3 | زنده (کم‌ریسک، fresh-read) |

---

## ۲. زنجیرهٔ ریشه‌ای (مستند tester604 — هر حلقه رفتاری است، نه فقط وجود رشته)

این باگ مستقیماً همان تستر قفل موجود `_tools/uat/tester604-v34.37.7-contact-wipe.js` است. زنجیرهٔ ریشه‌ایِ پنج‌گانه:

1. **روتر collection-diff** (`ptfEntitySaveCollection` در `crm/sales-domain-v2.js`): برای هر `cd` غایب از `nextArr` نسبت به `base` یک `entity_delete` واقعی صادر می‌کرد. در ثبت یک‌رکوردی / قالب‌بندی شماره / heal، `nextArr` از یک خواندنِ کهنه/ناقص ساخته می‌شد → تا ۴۰ حذف واقعی؛ حتی با سپر سقفِ ۳، ۱–۳ مشتری واقعاً پاک می‌شد و `ptfSilentWrite` همان آرایهٔ ناقص را روی کش می‌نشاند.
2. **`phonefmt.migrateOnce`**: کل دفتر را از `getData` کهنه با `setData`/SaveCollection بازنویسی می‌کرد؛ اگر `getData` خالی/ناقص بود، دفتر کامل با مجموعهٔ ناقص جایگزین می‌شد.
3. **هوک phonefmt**: بعد از `saveCust2/saveSup2` کل مجموعه را از `items[0]` قالب‌بندی می‌کرد؛ مشتریِ تازه بدون `cd`، رکوردِ *دیگری* را قالب‌بندی/حذف می‌کرد.
4. **`cbCollect`**: شخصِ بی‌نامِ دارای شماره/ایمیل را دور می‌ریخت (فقط «نام‌دار» نگه داشته می‌شد).
5. **`saveCust2/saveSup2`**: برای حقوقی `rec.phones = []` می‌فرستاد و `coTels` به فیلد تک‌خطی فرم تقلیل می‌یافت؛ چون سرور merge است، کلیدِ حاضر (`phones`, `coTels`) با مقدار خالی/ناقص **بازنویسی** می‌شد → شماره‌های وارداتی/قدیمی پاک می‌شد.

---

## ۳. آناتومی فعلی — کدام سپرها جلوی هر حلقه را می‌گیرند

### ۳.۱ روتر: `AUTO_NO_DELETE` + `mergedKeep` + سپر انبوه (حلقهٔ ①)
`crm/sales-domain-v2.js` (~۶۴۰–۶۸۵):
- اگر `dels` غیرخالی باشد و `opts.reason` در `AUTO_NO_DELETE_REASONS` باشد و `allowDeletes` نبود، رکوردهای غایب از `base` با `mergedKeep` **به آرایه برمی‌گردند** (نه حذف) و فقط upsert صادر می‌شود. `nextArr = mergedKeep; dels = [];`
- فهرست فعلی: `phonefmt, phonefmt-mig, rfq-cust-heal, offer-cust, offer-sup, excel-import, excel-std, vendorlist, site-rfq, saveCust, saveSup`.
- سپر انبوه (`DELETE_BURST_MAX=3` و حالت «فهرست تهی شد») برای مجموعه‌های هویتی، حذفِ انبوه را به مسیر legacy تنزل می‌دهد.
- تست قفل: بند ۱.۲/۱.۳/۱.۴ (صفر حذف + silent-write کامل)؛ ۱.۷ (خروج اضطراری `allowDeletes`)؛ ۱.۸ (reason نامرتبط مثل `w4` همچنان حذف می‌کند)؛ ۱.۹/۱.۱۰ (جهش = سنجه بار تشخیص).

### ۳.۲ سرور: `entity_upsert` = merge، نه replace (حلقهٔ ⑤ — ریشهٔ قطعی)
`api/sales-domain.php` (~۲۵۸۵–۲۶۰۰) — کامنت v34.8.34 (CARTABLE-LOOP) صریح است:
> «merge semantics — فیلدی که در payload نیست یعنی «تغییری نکرده»، نه «پاک».»

کد: برای هر کلیدِ رکورد قبلی که در payload نباشد (`!array_key_exists($pk, $row)`)، مقدار قبلی حفظ می‌شود. بنابراین دیگر **هیچ‌چیز به‌صرفِ غیبت از payload پاک نمی‌شود**. تنها راه پاک‌کردنِ عمدی یک کلید، ارسال صریحِ آن کلید با مقدار خالی است — و این دقیقاً همان چیزی است که v34.37.7 در کلاینت (`saveCust2/saveSup2`) حذفش کرد.

### ۳.۳ پاک‌ساز سرور: آرایه‌های تودرتو حفظ می‌شوند
`api/sales-domain.php` — `sd_entity_sanitize_row` / `sd_entity_sanitize_list` (v34.29.7 SITE-PARITY):
- لیست اسکالر/لیستِ map با سقف ۶۰ قلم و ۲۰ فیلد نگه داشته می‌شود.
- مقدار لیستی داخل map داخل لیست — مثل `people[].tels/mobs/mails` — با عمق (پیش‌فرض ۲) بازگشتی نگه داشته می‌شود. قبلاً همین‌جا بی‌صدا حذف می‌شد؛ حالا `{n, ext, lb}` شخص رابط پس از هر `entity_upsert`/همگام‌سازی باقی می‌ماند.
- `null` حفظ می‌شود؛ برش/حذف به‌صورت ساخت‌یافته در `sanitize` پاسخ برمی‌گردد (حذف بی‌صدا از بین رفت).

### ۳.۴ `phonefmt` (حلقه‌های ② و ③)
`crm/phonefmt.js`:
- `migrateOnce` با `skipEmptyOrTruncated` — اگر `getData` خالی یا کوتاه‌تر از `window._ptfEntityLastKnown[coll]` باشد، **نمی‌نویسد**؛ وگرنه با `reason: 'phonefmt-mig'` (در فهرست AUTO) می‌نویسد.
- `hookCust`/`hookSup`: پس از `saveCust2/saveSup2` فقط **همان رکورد** را با `ptfEntityUpsert` می‌فرستد؛ `cd` از آرگومان یا `_ptfLastSavedCustCd`/`_ptfLastSavedSupCd` گرفته می‌شود؛ هیچ ارجاعی به `items[0]` و هیچ SaveCollectionای نیست.

### ۳.۵ دفترچهٔ تماس (حلقهٔ ④)
`crm/offers.js`:
- `cbEnsurePerson` آرایه‌های غایب `tels/mobs/mails` را می‌سازد.
- `cbPersonHasContact`: شخص با نام **یا** هر شماره/ایمیلِ غیرخالی، دارای تماس است.
- `cbCollect` = `people.filter(cbPersonHasContact)`؛ شخص بی‌نامِ دارای شماره/ایمیل دیگر دور ریخته نمی‌شود.

### ۳.۶ `saveCust2`/`saveSup2` (حلقهٔ ⑤)
`crm/offers.js` (۳۶۶۴+ و ۳۸۰۳+):
- شاخهٔ حقوقی: **`rec.phones = []` وجود ندارد**؛ `con`/`ph`/`nm` از `primaryPerson` مشتق می‌شوند.
- `coTels = ptfMergeExtraCoTels(oldRecPre, tel)`: شمارهٔ اول فرم جایگزین، شماره‌های اضافه (ایندکس ۱ به بعد، مثل فکس/خط دوم) **حفظ** می‌شوند.
- در ویرایش، فیلدهایی که فرم مدیریت نمی‌کند (files/src/srcSite/ds/مهرهای dedup/…) از رکورد قبلی کپی می‌شوند (merge مثل سرور).
- ذخیره با `reason: 'offer-cust'` / `'offer-sup'` (در فهرست AUTO) و فلگ `_ptfLastSavedCustCd`/`_ptfLastSavedSupCd` ست می‌شود.

### ۳.۷ سایر مسیرهای مشتری‌ساز — همه در سپر
- `ptfHealMissingCustomersFromRfqs` (offers.js ۳۸۷۵+): `reason: 'rfq-cust-heal'` + `allowDelete: false`؛ کلید تهی وارد نقشهٔ بازیافت نمی‌شود (v34.37.0).
- `rfqSiteEnsureCustomer` (bridge.js ۱۶۱۲+): `reason: 'site-rfq'`؛ کانال تماس فرم سایت طبق موازین CRM طبقه‌بندی می‌شود (موبایل→mobs، ثابت→tels)؛ `phones: []` فقط روی رکورد **نو** است (چیزی برای پاک‌شدن ندارد).
- `saveCust2` از پیشنهاد (bridge ۲۰۱۸+): wrapper فقط دراپ‌داون را تازه می‌کند و به تابع اصلی برمی‌گردد — دفتر را بازنویسی نمی‌کند. wrapperهای `sms.js`/`cheques.js`/`supspec.js` نیز فقط اثر جانبی پس از ذخیره دارند.

---

## ۴. مسیرهای زندهٔ پاک‌شدن در v34.38.3 (یافته‌های جدید — قابل‌اقدام)

### R1 (P1) — نویسندگانِ کل‌دفتر که `reason` آن‌ها در `AUTO_NO_DELETE` نیست
شمارش کامل صدا‌زنندگان `ptfEntitySaveCollection('ptf_crm_customers', کل‌دفتر, …)`:

| محل | reason | در AUTO؟ | خطر حذف واقعی در صورت خواندن کهنه |
|-----|--------|-----------|-----------------------------------|
| offers.js:3752 (saveCust2) | `offer-cust` | ✅ | ندارد |
| offers.js:3923 (heal) | `rfq-cust-heal` | ✅ | ندارد |
| offers.js:4158 (لیست وندور) | `vendorlist` | ✅ | ندارد |
| bridge.js:1640/1660 (سایت) | `site-rfq` | ✅ | ندارد |
| phonefmt.js:185 (migrateOnce) | `phonefmt-mig` | ✅ | ندارد |
| **leads.js:480** (تبدیل لید) | `lead-convert` | ❌ | **دارد** |
| **ai-workbench.js:305** | `ai-bizcard` | ❌ | **دارد** |
| **ai-workbench.js:597** | `ai-letterhead` | ❌ | **دارد** |
| **ai-workbench.js:614** | `ai-buyer` | ❌ | **دارد** |
| **workflow.js:455** (ترجمهٔ LLM) | `coen-fill` | ❌ | **دارد** |
| custmerge.js:215 (ادغام) | `custmerge` | ❌ | عمدی (حذف تک‌رکوردیِ مقصد ادغام) |

**چرا این مهم است:** سپر انبوه فقط «حذفِ >۳» و «فهرستِ تهی‌شده» را می‌گیرد؛ یک حذف تک‌رکوردی (دقیقاً امضای «یک cd غایب از خواندن کهنه») از زیر هر دو سپر عبور می‌کند و به `entity_delete` واقعی + سنگ‌قبر دائمی می‌شود. `AUTO_NO_DELETE` برای همین ساخته شده و این پنج reason جا مانده‌اند — به‌ویژه `coen-fill` که در **callback ناهمگام ترجمهٔ AI** صدا می‌شود (فاصلهٔ بین خواندن و نوشتن بیشترین فرصت را برای کهنه‌شدن دارد) و `lead-convert`/`ai-*` که مسیر ثبت مشتریِ نو از منبع دیگری هستند.

**پیشنهاد (غیرمخرب، یک‌خطی):** افزودن `lead-convert`, `ai-bizcard`, `ai-letterhead`, `ai-buyer`, `coen-fill` به `AUTO_NO_DELETE_REASONS` در `sales-domain-v2.js`. برای `custmerge` حذف عمدی است؛ بهتر است صریح‌سازی شود (`allowDelete: true`) تا نیت ثبت شود و در آینده تصادفاً در فهرست AUTO نیاید.

### R2 (P3) — `migrateContacts()` با `setData` خام
`crm/offers.js:10–28`: مسیر مهاجرت legacy (`con/ph` → اولین شخص رابط) فقط وقتی اجرا می‌شود که رکوردی `people` نداشته باشد، و خروجی را با `setData(key, items)` (کل مجموعه، خارج از روتر فرمانی) می‌نویسد. اگر در همان لحظه `getData` کهنه/ناقص باشد، می‌تواند دفتر را با مجموعهٔ ناقص روی سرور push کند. چون سرور از آن پس merge می‌کند و همهٔ رکوردهای جدید `people` دارند، رخداد آن بسیار نادر است؛ ولی هم‌ردیفِ حلقهٔ ② است و در صورت لمس، باید مثل `skipEmptyOrTruncated` از `_ptfEntityLastKnown` چک کند یا از روتر عبور کند.

### R3 (P3) — تغییر نوع حقوقی→حقیقی
`saveCust2` (۳۷۱۴–۳۷۱۵) و `saveSup2` (۳۸۳۴–۳۸۳۵): شاخهٔ حقیقی عمداً `people=[]` و `coTels=[]` می‌کند و فقط `phones` را از `indivPhonesCollect()` (که از `c.phones` آغاز می‌شود) می‌گیرد. اگر کاربر یک مشتری حقوقیِ دارای `coTels`/اشخاص را به «حقیقی» تغییر دهد، آن‌ها **به `phones` منتقل نمی‌شوند** و در ذخیره پاک می‌شوند. این رفتار طراحی v80.2 است، اما یک گوشهٔ ازدست‌دادن تماس است که در گزارش‌های میدانی می‌تواند همان علامت «تماس پس از ثبت ناپدید شد» را بدهد. پیشنهاد: هنگام toggle نوع در مودال، در صورت وجود تماس‌ها، هشدار/پیش‌نمایش بدهد (یا coTels را به phones منتقل کند).

### R4 (P3) — fallback هوک phonefmt
`phonefmt.js` hookCust/hookSup: اگر `ptfEntityUpsert` نبود (فرمان غیرفعال)، به `setData('ptf_crm_customers', items)` تنزل می‌کند — `items` اینجا fresh-read همان لحظه است، پس به‌خودی‌خود ناقص نیست؛ فقط یک push کل‌دفتر است. کم‌ریسک.

---

## ۵. تأیید با آزمون (وضعیت فعلی)

- `node _tools/uat/tester604-v34.37.7-contact-wipe.js` → **PASS: 44 | FAIL: 0** (این‌جلسه اجرا شد).
- قرارداد ضد رگرسیون سبز: بند ۱.۵/۱.۶/۱.۸ (حذف تک‌رکوردی بدون reason) و ۱.۷ (allowDeletes) — یعنی سپر فقط در مسیرهای AUTO فعال است و حذف واقعیِ کاربر سالم مانده.
- گیت کامل v34.38.3: **225 PASS / 0 FAIL** (از فاز قبل).

---

## ۶. جمع‌بندی و مسیر پیشنهادی

1. **باگ گزارش‌شده در مسیرهای اصلی رفع است** و با تستر 604 قفل شده؛ ریشهٔ قطعی (merge سرور + حذف `phones=[]` + سپر AUTO) باقی است.
2. **گام بعدیِ غیرمخرب (پیشنهاد برای فاز فیکس):**
   - **R1:** افزودن ۵ reason به `AUTO_NO_DELETE_REASONS` + صریح‌سازی `custmerge` (`allowDelete:true`).
   - **R2:** عبور `migrateContacts` از چک `_ptfEntityLastKnown` (مثل `skipEmptyOrTruncated`).
   - **R3:** هشدار در toggle نوع حقوقی→حقیقی هنگام وجود تماس.
   - **R4:** (اختیاری) حذف fallback `setData` کل‌دفتر در هوک.
3. **قرارداد پروژه پابرجاست:** هر تغییر → بامپ نسخه + `RELEASE-NOTES` + تستر قفل جدید/تمدید‌شده در `run-ci-gate.js` (بهتر است تستر 604 با بندهای R1–R3 تمدید شود تا این شکاف‌ها هم قفل شوند).

_این سند تحلیل ریشه است؛ فیکس اعمال‌شده در v34.38.4 در `RELEASE-NOTES-v34.38.4.md` و قفل آن در `_tools/uat/tester611-v34.38.4-contact-wipe-ind-heal.js` مستند است._
