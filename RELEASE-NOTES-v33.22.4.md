# RELEASE-NOTES — v33.22.4 (FX-CACHE-FALSE-MISMATCH)

**تاریخ:** ۲۰۲۶-۰۸-۰۳
**نوع:** ریشه‌کن خطای مانیتورینگ — «مغایرت کاذب fx-cache در health-check بخش ۱۱»
**وضعیت:** روی استیجینگ — در انتظار تأیید تست کارفرما برای مرج
**اولویت:** P1 (مانیتورینگ) — **هیچ داده‌ای آسیب ندیده؛ ۵۳ کلید دیگر بخش ۱۱ ✅ یکسان بودند**

---

## ۱) علائم (گزارش health-check پروداکشن)

پس از دیپلوی موفق v33.22.3، «گزارش سلامت داده‌ها» در بخش ۱۱ (تطابق فایل ↔ دیتابیس) اعلام کرد:

> **۱ کلید مغایرت از ۵۴ کلید: `fx-cache`**

## ۲) ریشه‌یابی (مستند، از کد)

`fx-cache` **کش ۱۰‌دقیقه‌ای نرخ ارز** است (`api/fx-rates.php` → `crm/data/fx-cache.json`، `TTL = 600`):

- فقط و **مستقیم** در فایل بازنویسی می‌شود (`file_put_contents($cache_file, …, LOCK_EX)`)؛
- **نه خوانندهٔ DB دارد و نه نویسندهٔ DB** — `fx-rates.php` هیچ ارجاعی به `db-lib`/`ptf_db_*` ندارد و هیچ مسیر سینکی هم این کلید را به DB نمی‌برد/از DB نمی‌خواند؛
- تنها راه ورودش به DB، «گام انتقالِ» ویزارد مهاجرت بود که یک‌بار آن را کپی کرد — و از همان لحظه ردیف DB یقیناً کهنه است (نرخ ارز هر ۱۰ دقیقه تازه می‌شود).

پس **هر تازه‌سازی نرخ ارز = یک «مغایرت» در مقایسهٔ چک‌سام فایل↔DB** — دقیقاً همان کلاس کلیدهای فرّارِ عمداً فایل‌محور (`meta` = دفتر rev سینک، `tokens` = نشست‌های ورود) که در v33.22.3 مستثنا شدند؛ `fx-cache` از قلم افتاده بود.

**اهمیت:** خطا صرفاً مانیتورینگ بود — نوار نرخ ارز و همهٔ داده‌ها سالم‌اند؛ ولی ماندن آن، قرمزِ دائمیِ بخش ۱۱ را «عادی» می‌کرد و درست همان تله‌ای بود که v33.22.3 برای meta/tokens بست (مغایرتِ واقعیِ آینده دیده نمی‌شود).

## ۳) ریشه‌کن (این نسخه — استثنا از مقایسه‌ها؛ بدون تغییر کلاینت/رفتار mode)

1. **`api/migrate.php`** — `mig_compare_excluded()` = `['meta', 'tokens', 'fx-cache']` + کامنت مستندساز. در مرحلهٔ ۴ ویزارد (بررسی تطابق) و گارد سوییچ نهایی، fx-cache دیگر مقایسه نمی‌شود و در جدول با برچسب **«ℹ️ فایل‌محورِ دائمی»** نمایش داده می‌شود (همان رفتار meta/tokens).
2. **`api/data-health-check.php`** — فهرست استثنای بخش ۱۱: `['otp', 'ratelimit', 'meta', 'tokens', 'fx-cache']` + کامنت؛ نسخهٔ ابزار: **v33.22.4**.
3. هیچ تغییری در `fx-rates.php`، رفتار کش، کلاینت CRM یا منطق mode (off/dual/mysql) **نشد**.

## ۴) پرونده‌های تغییرکرده

| فایل | تغییر |
|---|---|
| `api/migrate.php` | افزودن `fx-cache` به `mig_compare_excluded()` + کامنت‌های مستندساز |
| `api/data-health-check.php` | استثنای fx-cache در بخش ۱۱ + نسخه ابزار v33.22.4 + کامنت |
| `_tools/uat/tester304-p1-attach-stale-db.js` | ۱۲ → **۱۳ تست** (پین استثناهای جدید + تست ریشه: کش ۱۰‌دقیقه‌ای فایل‌محور بدون هیچ تماس DB) |
| `crm/index.html` | `VER = v33.22.4` + ۸۳ کش‌باستر `v=33.22.4` |
| `crm/sw.js` | `CACHE = ptf-crm-v33.22.4` |
| `crm/clear-cache.html` | `window.VER = v33.22.4` |
| `crm-update.zip` | به‌روزرسانی ۴ ورودی (۵۵۸ ورودی — بدون تغییر تعداد) |
| `RELEASE-NOTES-v33.22.4.md` / `REGRESSION-REPORT-v33.22.4.md` | اسناد انتشار (گیت audit) |
| `crm/BACKLOG-USER-2026-08-01.md` | ردیف `UR-2026-08-03-31` |
| `_audit/HANDOFF-NEXT-SESSION-2026-08-01.md` | بخش v33.22.4 (بالای فایل) |

## ۵) شواهد کیفیت

- **tester304:** ۱۳ ✅ / ۰ ❌ (گیت نهایی سبز)
- **رگرسیون کامل (`node _tools/run-full-regression.js`):** ۲۹۹ تستر — فایل‌ها **۲۴۳ ✅ / ۵۶ ❌**، چک‌ها **۵۴۴۵ ✅ / ۸۴ ❌**، `prebroken=[]`، `hung=[]` ⇒ **دقیقاً baseline v33.22.3 (۵۴۴۴/۸۴) + ۱ چک جدید tester304 — صفر شکست جدید** (۵۶ فایل FAIL همان بدهی قدیمیِ شناخته‌شده است)
- **`audit.py`:** PASS (۰ خطا)
- **zip:** ۵۵۸ ورودی

> یادداشت نشست: دلتای اولیهٔ v33.22.4 (ساخته‌شده در نشست قبلی) با ریست سندباکس از دست رفت و این نسخه **از روی همان مشخصات بازسازی** شد؛ اعداد رگرسیون بازسازی (۵۴۴۵/۸۴، ۲۴۳/۵۶) با شواهد دلتای اولیه **دقیقاً منطبق‌اند**.

## ۶) چک‌لیست تست استیجینگ (کارفرما)

1. `https://staging.pishtaj.ir/api/data-health-check.php?confirm=yes` → بخش ۱۱: **«۵۴ کلید داده — همه یکسان ✅»**؛ `fx-cache` دیگر در فهرست مغایرت نیست؛ نسخه ابزار: **v33.22.4**
2. (اختیاری) `migrate.php` مرحلهٔ ۴ → ردیف `fx-cache` با برچسب **«ℹ️ فایل‌محورِ دائمی»**
3. نوار نرخ ارز CRM استیجینگ سالم

## ۷) پس از تأیید

مرج PR به `main` ← **دیپلوی دستی پروداکشن توسط کارفرما** (Actions → Deploy to Production → confirm=`DEPLOY`، ref=`main`) ← راستی‌آزمایی زنده: health-check پروداکشن «نسخه ابزار: **v33.22.4**» + بخش ۱۱ سبز.
