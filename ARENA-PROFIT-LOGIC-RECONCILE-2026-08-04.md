# منطق سود: راستی‌آزمایی پیاده‌سازی فعلی در برابر تعریف کارفرما

**برنچ:** `arena/019fcd1d-pishtaj-ir` (v34.0.7-alpha)
**تعریف کارفرما (این نشست):**
- **درآمد** = فاکتورهای فروش رسمی و غیررسمی.
- **هزینه** = هزینه‌های جاری + فاکتورهای خرید (به‌جز فاکتورهای **صوری پوشش**) + تنخواه + سایر هزینه‌ها.
- **حقوق سهامدار موظف** = هم «جزء هزینه‌ها» هم «مطالبهٔ آن سهامدار» باشد.
- **مازاد سود** تقسیم شود.

---

## ۱) چه چیزهایی درست و هم‌خوان است ✅

| مورد | وضعیت در کد |
|---|---|
| حقوق سهامدار هم هزینه هم مطالبه است | ✅ حقوق → `ptf_crm_opex` (دسته «حقوق و دستمزد») = هزینه؛ و هم‌زمان `ptf_crm_sharetx` نوع `salary` = بستانکاری/مطالبهٔ سهامدار (`shareholders.js:57,178`). دقیقاً خواستهٔ شماست. |
| تنخواه جز هزینه است | ✅ در سود نقدی `out.petty` و در تعهدی `pettyStandaloneTotal` کم می‌شود. |
| سایر هزینه‌ها (بیمه/مالیات/…) جز هزینه است | ✅ همهٔ دسته‌های opex در `ptfOpexSum` جمع و در `opexTotal` کسر می‌شود. |
| مازاد سود تقسیم می‌شود | ✅ تقسیم فقط از مازاد بر «کف نقدینگی» با درصد توافقی؛ باقی به کف برمی‌گردد. |

---

## ۲) ناسازگاری‌ها با تعریف کارفرما (باگ‌ها) 🔴🟠

### B-1 🔴 (بالا) — فاکتورهای «صوری پوشش» در موتور سود/تقسیم، هزینهٔ کامل شمرده می‌شوند
- مفهوم `isCover` (فاکتور صوری پوشش) فقط در ماژول‌های **گزارش** (`official-ledger.js`، `ledger-report.js`) هست.
- اما **موتور سود سال مالی** (`fiscal.js` — همان که تقسیم سود را محاسبه می‌کند) **هیچ ارجاعی به `isCover` ندارد** (شمارش = ۰).
- در `ptfFiscalCashData` همهٔ فاکتورهای خرید بدون استثنا در `out.supplierInvoices` کم می‌شوند:
  ```js
  (sf.invoices || []).forEach(i => { ...; out.supplierInvoices += +i.amount || 0; });
  ```
- **اثر:** یک فاکتور صوری پوشش مثلاً ۱۰۰ میلیون، سود را ۱۰۰ میلیون کاهش می‌دهد، درحالی‌که طبق تعریف شما «خرید واقعی کالا نیست» و فقط **سود/زیان خالصش** (اعتبار ارزش‌افزوده − کارمزد فاکتورساز = `ptfLedgerCoverNetBenefit`) باید اثر داشته باشد. → **سود/سهم قابل تقسیم به‌درستی کم‌نمایش می‌شود.**
- نکته: گزارش «تراز رسمی/غیررسمی» این تفکیک را درست دارد ولی صراحتاً فقط نمایشی است و می‌گوید «فرمول تقسیم سود در سال مالی هنوز تغییر نکرده» (`ledger-report.js:139`).

**رفع پیشنهادی:** در `ptfFiscalCashData` (و جمع تعهدی)، فاکتورهای `isCover===true` را از `supplierInvoices` حذف و به‌جای آن «سود/زیان خالص» آن‌ها (سودآور → افزاینده سود؛ زیان‌ده → کاهنده) اعمال کنید (الگوی `ptfLedgerRealPurchaseAmount`/`ptfLedgerCoverNetBenefit`).

### B-2 🟠 (متوسط) — پرداخت حقوق به‌عنوان «علی‌الحساب سود» کسر می‌شود (ماندهٔ قابل تسویه غلط)
- حقوق **مطالبه** است (نه علی‌الحسابِ سود). اما پرداختِ حقوق با همان تابع برداشت ثبت می‌شود: `ptfShareDraw` → `type:'draw'` (`shareholders.js:288`).
- در توزیع، ستون «ماندهٔ قابل تسویهٔ امسال» = `سهم ناخالص − draw/advance/debit` (`fiscal.js:340`) و چون `draw` شامل پرداخت حقوق هم هست، حقوقِ سهامدار موظف **دوبار اثر منفی** دارد (یک‌بار به‌عنوان هزینه در سود + یک‌بار به‌عنوان علی‌الحساب از سهمش) → این ستون برای سهامدار موظف منفیِ گمراه‌کننده می‌شود.

**رفع پیشنهادی:** پرداخت حقوق با نوع جداگانهٔ `salary_payment` ثبت شود و از فیلتر `advRows` در `ptfFiscalCashDistribution` حذف شود (فقط draw/advance/debitِ واقعی در علی‌الحساب بماند). این با تعریف شما («حقوق مطالبه است نه علی‌الحساب») سازگار است.

### B-3 🟠 (نیازمند تأیید) — مبنای درآمد: «فاکتور فروش» در برابر «وصولی» 
- تعریف شما: **فاکتور فروش (رسمی+غیررسمی) منبع درآمد است** (مبنای تعهدی/فاکتوری).
- اما موتور سود/تقسیم فعلی از **منطق نقدی** استفاده می‌کند: درآمد = «وصولی‌ها» (نقد + چک وصول‌شده) (`ptfFiscalCashData`)، که در نسخه‌های قبل «مصوب کارفرما» اعلام شده بود.
- گزارش «تراز رسمی/غیررسمی» از مبنای فاکتور (تعهدی) استفاده می‌کند ولی فقط نمایشی است.
- **نتیجه:** بسته به اینکه کدام مبنا تصمیم نهایی باشد، یا باید موتور تقسیم به مبنای فاکتور/تعهدی برگردد، یا دو عددِ سود نهایی (تعریف شما vs فعلی) همچنان متفاوت می‌مانند (باگ F-07 قبلی).

### (باگ‌های ارجاع‌شده از بازبینی قبلی که همچنان پابرجاست)
- **F-01 🔴** افتتاحیهٔ نقدِ سال مالیِ اشتباه بدون گِیت سال.
- **F-03 🟠** چک واردهٔ انتقالی به تامین‌کننده، مطالبهٔ مشتری را تسویه نمی‌کند.
- **F-04 🟠** چک صادرهٔ پنل چک بدون `supplierPaymentCd` → دوباره‌شماری خروجی.
- **F-05 🟠** حقوقِ تعلّق‌گرفته ولی پرداخت‌نشده به‌عنوان خروجی نقد کسر می‌شود.

---

## ۳) جمع‌بندی و تصمیم موردنیاز

بیشترین انحراف از تعریف شما **B-1** است (فاکتور صوری پوشش در موتور سود به‌عنوان هزینهٔ کامل کم می‌شود). **B-2** با تعریف «حقوق=مطالبه» در تضاد است. **B-3** به یک تصمیم مدیریتی نیاز دارد (مبنای درآمد: فاکتوری/تعهدی یا نقدی).

برای پیش بردن، لطفاً این سه را روشن کنید:

| | تصمیم | گزینه‌ها |
|---|---|---|
| **1** | آیا فاکتور صوری پوشش را از هزینهٔ خرید موتور سود حذف و فقط سود/زیان خالصش را اعمال کنم؟ | بله / خیر |
| **2** | آیا پرداخت حقوق را به نوع جداگانهٔ `salary_payment` منتقل کنم تا در «ماندهٔ قابل تسویه» کسر نشود؟ | بله / خیر |
| **3** | مبنای درآمد برای تقسیم سود کدام است؟ | الف) فاکتور فروش (تعهدی/فاکتوری) / ب) وصولی‌ها (نقدی — فعلی) / ج) ترکیبی |

*تولیدشده توسط Arena Agent — بازبینی فقط‌خواندنی؛ هیچ تغییری در کد اعمال نشده تا تصمیم‌های بالا مشخص شود.*
