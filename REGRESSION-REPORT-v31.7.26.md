# 📊 گزارش رگرسیون v31.7.26

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.26
**ابزار:** `_tools/run-full-regression.js` + `python3 _tools/audit.py` (۱۰ بخش)

## ✅ نتیجه نهایی

| معیار | مقدار | وضعیت |
|--------|-------|--------|
| تعداد تسترها | **۱۷۹** (+۱: tester201-sync-oscillation) | — |
| فایل‌های PASS | **۱۷۹** | ✅ ۱۰۰٪ |
| چک‌های FAIL | **۰** | ✅ صفر |
| audit.py (۱۰ بخش) | **PASS** | ✅ |
| `node --check` (`sync.js`, `sms.js`) | **PASS** | ✅ |

## 🆕 پوشش تست جدید (tester201 — بازتولید نوسان با merge واقعی)

- ❶ دفترچه: cdهای متفاوت دو دستگاه برای یک مخاطب → پس از رفع: ۱ رکورد؛ merge تکراری idempotent (نوسان قطع)
- ❶ب تقدم دستی/اکسل بر auto؛ ❶ج بقای مخاطبین متفاوت؛ rebuild متوالی همان `PB-{mob}` را می‌سازد
- ❷ رکورد بدون فیلد شناسه: حفظ (پایان کاهش خودکار) + بدون تورم (امضای یکتا)؛ مسیر عادی cd دار newer-wins سالم
- گاردهای قبلی rebuild (BUG-018: عدم rebuild قبل از bootstrap و با منابع خالی) سالم

## 📌 جایگاه در خانواده باگ‌های merge

هفتمین مصداق خانواده «قرارداد merge ناقص» (پس از NTF/AVATAR/USERS/…): این‌بار «هویت ناپایدار رکورد تولیدشده» + «سقوط رکورد بی‌شناسه». قاعده جدید در Master Handover: **هر رکورد تولیدشونده خودکار باید شناسه determinstic مشتق از هویت واقعی داشته باشد، نه کد تصادفی.**

## 📁 فایل‌های تغییریافته

`crm/sync.js`، `crm/sms.js`، `crm/index.html` (bump)، `crm/sw.js`، `crm/clear-cache.html`، `_tools/uat/tester201` (جدید)

## 🚦 وضعیت گیت‌ها

- **گیت فنی: سبز** ✅
- **گیت انسانی: معوق** ⏳ — مشاهده ۲۴ساعته شمارنده‌های داشبورد و دفتر تلفن روی دو دستگاه واقعی (سناریوهای RELEASE-NOTES).
