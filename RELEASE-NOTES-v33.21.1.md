# RELEASE-NOTES — v33.21.1 (۱۴۰۵/۰۸/۱۱ — 2026-08-02)

پالایش گزینهٔ A کارفرما (تست استیجینگ v33.21.0): «اضافه/حذف بین دو مرورگر درست نشست ولی رکورد جدید در تب‌های دیگر تا قبل از جابه‌جایی ظاهر نمی‌شد».

## 🔄 همگام‌سازی لحظه‌ای بین تب‌ها + مسیر آهستهٔ پس‌زمینه

| تغییر | توضیح |
|---|---|
| **پینگ بین‌تبی** | هر تب که دادهٔ تازه اعمال کرد یا پوش موفق داشت، نشانگر کوچک `ptf_sync_ping` را می‌نویسد؛ رویداد `storage` در بقیهٔ تب‌های **همان مرورگر** (حتی پنهان) فوراً می‌شلیکد و یک دلتا-پولِ فوری می‌دهند → تب‌های یک مرورگر **لحظه‌ای** تازه می‌شوند، **بدون هیچ رکوئست دوره‌ایِ اضافه** (پینگ فقط با تغییر واقعی می‌رود؛ مقایسهٔ rev مانع حلقه است؛ حد نرخ ۵ثانیه برای طوفان). |
| **مسیر آهستهٔ تب مخفی** | مخفی: پول هر **۱۸۰ ثانیه** (v33.21.0: توقف کامل ← ریشهٔ گزارش استیجینگ) — پاسخ «بدون تغییر» فقط ~۶۰ بایت؛ یعنی مرورگر/دستگاه دوم هم حداکثر تا ۳ دقیقه همگرا می‌شود. |
| غیرمتمرکزِ دیده‌شده | بدون تغییر: هر ۱۲۰ ثانیه؛ و جبران فوری با focus/visibilitychange همان‌طور که بود. |

## آزمون
- tester300: **۲۵/۰** (پینگ runtime: اعمال پول → نوشتن پینگ با rev جدید؛ instant حتی در تب مخفی فوراً پول می‌دهد)
- رگرسیون: ۲۹۵ تستر → **۲۳۹/۵۶ فایل و ۵۳۹۱/۸۴ چک = baseline + tester300 (۲۵)** — صفر شکست جدید
- audit: ✅ ۰ خطا | zip: ۵۵۸ ورودی | تستر source-pinned `tester178` به regex مقاوم بازنویسی شد

## گام‌ها
تست مجدد استیجینگ (تب/مرورگر دوم باید بدون لمس، لحظه‌ای/حداکثر ۳ دقیقه تازه شود) → تأیید کارفرما → مرج PR #14 در همین نشست → دیپلوی دستی کارفرما.
