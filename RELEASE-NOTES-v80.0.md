# 🚀 ریلیز v80.0 — اسپرینت ۸۰: موج اول نیازمندی‌های جدید شما (US-166 تا US-171)
**تاریخ:** تیر ۱۴۰۵ | ۶ استوری از ۸ استوری موج سوم شما در این ریلیز — ۲ استوری بزرگ باقیمانده در اسپرینت ۸۱-۸۲

---

## ✅ تحویل‌شده در این ریلیز

### 🎛 ۱) دسترسی شخصی‌سازی‌شده per کاربر (US-166)
دقیقاً سناریوهای شما:
- در ماژول کاربران، کنار هر کاربر دکمه **«🎛 دسترسی»** (فقط برای ادمین و رییس هیات مدیره).
- برای هر ماژول (۲۲ ماژول) سه حالت: **پیش‌فرض نقش / ✅ مجاز / ⛔ مسدود**.
- مثال ۱: به حسابدار «مکاتبات» را مجاز کنید → نامه می‌زند، برای امضا می‌فرستد؛ هر وقت خواستید پس بگیرید.
- مثال ۲: رییس به مدیر بازرگانی «مدیریت سایت» می‌دهد برای ثبت مقاله/خبر، بعداً می‌گیرد.
- اعمال آنی + **سینک بین همه دستگاه‌ها** (از موتور v79) + ثبت در audit.

### 🌗 ۲) حالت شب/روز (US-167)
تنظیمات → «حالت نمایش» → ☀️ روز / 🌙 شب — برای همه کاربران، ترجیح ذخیره می‌شود، کل CRM (کارت‌ها، جدول‌ها، مودال‌ها، ورودی‌ها) پوشش داده شده.

### 🏢 ۳) فرم مشتری و تامین‌کننده حقیقی/حقوقی داینامیک (US-168 + v80.1)
- **هر دو فرم مشتری و تامین‌کننده** (طبق درخواست تکمیلی شما):
- انتخاب **حقوقی** → فیلد «شناسه ملی» + تلفنخانه + اشخاص رابط.
- انتخاب **حقیقی** → فیلد «کد ملی» باز می‌شود و شناسه ملی/تلفنخانه/اشخاص رابط حذف می‌شوند.
- برچسب حقیقی/حقوقی در فهرست هر دو ماژول نمایش داده می‌شود.

### ✉️ ۴) لینک پرونده در نامه‌ها = کشویی (US-169)
در نامه صادره و وارده، «لینک به پرونده» دیگر تایپی نیست — فقط از فهرست پرونده‌های موجود (با نام کارفرما) انتخاب می‌شود.

### 💰 ۵) پیش‌پرداخت CO برنده (US-170 AC2)
- به‌محض برنده شدن CO، سیستم می‌پرسد: «پیش‌پرداخت دارد؟» → **درصدی یا مبلغ مشخص**.
- مبلغ به‌عنوان **«مطالبات پیش‌پرداخت»** در بالای ماژول مطالبات می‌نشیند (با جمع کل) تا با دکمه «✔ وصول شد» تسویه شود + اعلان به مدیران و حسابدار.
- (ثبت فاکتور با پرداخت‌های تدریجی تا تسویه از v58 موجود بود و به این چرخه متصل است — مانده per فاکتور با نام بدهکار + جمع کل مطالبات در همان صفحه نمایش داده می‌شود.)

### 💵 ۶) تنخواه گردان (US-171)
ماژول جدید در دسته مالی:
- **همه کاربران:** ثبت هزینه (مبلغ، نوع، توضیح، اتصال به درخواست) + **پیوست رسید/فاکتور** — و فقط هزینه‌های خودشان را می‌بینند + جمع مطالبات تنخواه خودشان.
- **مدیرعامل/مدیر بازرگانی/رییس/ادمین:** دیدن همه هزینه‌ها، فیلتر per کاربر، جمع مطالبات تنخواه هر کاربر (چیپ‌های بالای صفحه)، و دکمه **«✔ تسویه شد»** با ثبت شماره سند پرداخت → اعلان خودکار به کاربر.

## ⏭ ثبت‌شده برای اسپرینت ۸۱-۸۲ (استوری‌های بزرگ)
| US | چی | چرا اسپرینت بعد |
|---|---|---|
| **US-172** | ایمپورت هوشمند اکسل سراسری (تشخیص سرستون فا/EN + استنتاج از محتوا + پیش‌نمایش تایید/اصلاح کاربر) | موتور مشترک برای ۵ نقطه ورودی — نیازمند طراحی دقیق |
| **US-173** | قیمت خرید پروژه‌محور (فقط RFQ برنده، چند تامین‌کننده per درخواست، هزینه‌های جانبی، سود ناخالص/خالص) + چارت‌های مدیریتی + ستون‌های TO/CO با درگ + تصویر در سلول | بزرگ‌ترین بازطراحی مالی — با داشبورد نموداری هم‌زمان می‌شود |

## 🧪 کیفیت
tester17 جدید: **۳۷/۳۷** (شامل تست عملی سناریوهای دسترسی شما) + رگرسیون کامل سبز + ممیز ۸بخشی PASS

## 📁 دلتا (۱۱ فایل — ۱۱۸KB)
`crm/perms.js` (**جدید**) • `crm/petty.js` (**جدید**) • `crm/offers.js` • `crm/letters.js` • `crm/index.html` • `crm/shell.js` • `crm/sync.js` • `crm/backup.js` • `crm/sw.js` • `api/crm.php` • `ROADMAP-SPRINTS-52-70.md`

## ⚠️ تست پیشنهادی
1. کاربران → کنار حسابدار «🎛 دسترسی» → مکاتبات را «مجاز» کنید → با حسابدار وارد شوید → مکاتبات را ببیند → برگردید و «مسدود» کنید → دیگر نبیند.
2. تنظیمات → 🌙 شب.
3. مشتری جدید → نوع را حقیقی/حقوقی عوض کنید → فیلدها جابجا شوند.
4. یک CO را برنده کنید → سوال پیش‌پرداخت → ۳۰٪ → در مطالبات ظاهر شود.
5. با یک کاربر عادی هزینه تنخواه ثبت کنید (با عکس رسید) → با مدیر وارد شوید → ببینید و تسویه کنید.
