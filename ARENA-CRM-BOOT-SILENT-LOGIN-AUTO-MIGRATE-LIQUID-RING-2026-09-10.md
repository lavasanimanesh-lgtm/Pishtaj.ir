# ARENA · v34.38.19 — SILENT-LOGIN + AUTO-MIGRATE-001 + LIQUID-RING

**تاریخ:** ۲۰۲۶-۰۹-۱۰ (۱۴۰۵/۰۶/۱۹) · **دامنه:** `crm/boot-splash.js` · `crm/sync.js` · `crm/client-server.js` · `crm/index.html` · `crm/sw.js` · `crm/manifest.json` · `VERSION.json` · `api/sales-domain.php` · `crm/clear-cache.html` · `crm/shell.js` (همگام‌سازی A6 نسخه) · `assets/images/ptf-logo-mark.png` (جدید) · `_tools/uat/tester627-…js` (جدید) · `_tools/uat/run-ci-gate.js` · `_tools/uat/tester552-…js` (پین به‌روزرسانی‌شده)

---

## ۱) گزارش کارفرما

> بعد از رفتن پردهٔ بوت، هنوز هشدارها در نوار زرد و سفید پایین باقی است و با هر رفرش
> یا هارد‌رفرش دوباره می‌آیند. باید هشدارها در زمان ورود نمایش داده نشوند و «انتقال
> یک‌باره و همگرایی با سرور» بدون زدن دکمه‌ای اتوماتیک انجام شود و در هارد‌رفرش هم
> پردهٔ بوت بیاید. همچنین گرافیک چرخش دور لوگو: کل لوگو داخل دایره (به‌جز متن
> پی‌تی‌اف زیر لوگو) و چرخش به‌صورت مایعِ جاذبه‌دار در حلقه‌ای باریک دور لوگو —
> فرود: سریع‌تر + بازتر، صعود: کُندتر + کم‌حجم‌تر.

## ۲) دو نوار هشدار کدام‌اند؟

| نوار | منبع | چرا برمی‌گشت؟ |
|---|---|---|
| **زرد** `#ptfUnsavedBanner` | `sync.js←setSyncBadge` | با هر کلیدِ `dirty` ماندگار (تغییر ارسال‌نشده) در بوت باز می‌شد و کاربر فکر می‌کرد سامانه خراب است. |
| **سفید/کرمی** `#ptfMigBar` | `index.html←ptfMigBannerTick` (هر ۱۵ث + ۲.۵ث پس از ورود) | تا وقتی کاربر دکمهٔ «انتقال یک‌باره» را در تنظیمات نمی‌زد، با هر رفرش برمی‌گشت. |

## ۳) اصلاح رفتاری (SILENT-LOGIN)

### ۳-الف) بنر زرد — `sync.js` (BOOT-BAR-QUIET)
- در «سکوت بوت» (`ptfBootQuiet`) بنر هرگز باز نمی‌شود (نشانگر 🟢/🟡 بالای صفحه همچنان به‌روز).
- پایان سکوت رویداد `ptf:boot-quiet-end` می‌فرستد؛ sync.js همان لحظه بنر را با وضعیت **واقعی** رندر می‌کند — اگر پس از push خودکار همه‌چیز رفت، هیچ نواری نمی‌آید؛ اگر معلقِ واقعی ماند، همان هشدار محافظِ داده می‌آید.

### ۳-ب) انتقال یک‌بارهٔ خودکار — `client-server.js` (AUTO-MIGRATE-001)
- API جدید: `ptfBAutoMigrate(opts)` / `ptfBMigrationNeeded()` / `ptfBAutoMigState()`.
- از مسیر ورود (sync.js پس از snapshot سالم) و تیکِ نوار، **بدون هیچ confirm/alert/reload** همان موتور مهاجرت اجرا می‌شود.
- گاردهای داده‌ای هیچ‌کدام حذف نشده‌اند: ACK تک‌کلید پیش از ثبت نشانگر، نجاتِ تعارض با نسخهٔ معتبر سرور، سپر ضد داده‌صفر، سقف ۸MB، allowlist نقش، audits.
- نتیجه برای UI در `ptf_b_automig_state_<user>` ثبت می‌شود (ok/running/human/reason).
- جریان ورود: پردهٔ بوت مرحلهٔ «migrate» را با پیام «در حال انتقال و همگرایی…» نشان می‌دهد و آماده‌شدن ورود تا پایان مهاجرت نگه داشته می‌شود (**failsafe ۴۰ثانیه — ورود هرگز گیر نمی‌کند**).

### ۳-ج) نوار سفید — `index.html`
- ابتدا انتقالِ خودکار راه می‌افتد (تا ۳ تلاش در هر نشستِ صفحه — خطاهای گذرای شبکه پوشش داده می‌شوند).
- نوار فقط اگر ناموفقِ ماندگار یا نیازمند-انسان (تعارض/نقش/نشست منقضی) تشخیص داده شد **و** سکوت بوت هم تمام شده باشد، می‌آید — با دکمهٔ دستی [تکمیل انتقال].
- در حالت عادی (موارد ۹۹٪) نوار هرگز در زمان ورود دیده نمی‌شود.

### ۳-د) هارد‌رفرش → پردهٔ بوت
- مسیر نمایش پرده در `showCrm` به قرارداد v34.38.17 دست‌نخورده فعال است؛ با کش no-cache که در v34.38.18-02 کامیت شد، هارد‌رفرش هر بار پردهٔ بوت + سکوت کامل را می‌آورد و انتقال خودکار هم در همان پرده انجام می‌شود.

## ۴) اصلاح گرافیکی (LIQUID-RING) — `boot-splash.js`

- **حلقهٔ مایع جاذبه‌دار (Canvas):** تودهٔ مایع در کانال باریک حلقه‌ای دور لوگو با مدل انرژی:
  - سرعت: `v(θ) = v₀·√(max(0.16, ۱ + 0.82·cosθ))` ⇒ **پایین √1.82 (۱.۳۵× پایه)، بالا √0.18 (۰.۴۲× پایه)** — نسبت ≈ ۳.۲ برابر شتاب قابل‌مشاهده.
  - طول قوس مایع: ∝ `سرعت^1.5` (۰.۱۵ ← ۱.۱۵ رادیان)؛ ضخامت: ∝ `سرعت^1.4` ⇒ فرود **کشیده و باز**، صعود **فشرده و کم‌حجم**.
  - رندر دو‌لایه (نارنجی #ef4b1a + هستهٔ کهربایی #f79400) با پروفیل پهنای نوک‌باریک
    (توان ۱.۵)، درخشش لبهٔ پیشرو و قطرهٔ دنباله ⇒ حسِ مایعِ واقعی.
  - توقف در تبِ مخفی/پایان پرده؛ `prefers-reduced-motion` → یک فریم ایستا؛ عدم پشتیبانی Canvas → همان حلقهٔ CSS قبلی (fallback کامل).
- **لوگو:** داراییِ جدید `assets/images/ptf-logo-mark.png` — برش دقیق نشان (چرخ‌دنده+شعله) از سند اصلی **بدون متن «PTF»** (ردیف‌های ۸۰–۱۳۷۰ از ارتفاع ۱۷۱۵ کاراکتر). داخل دایره با `object-fit:contain` نمایش داده می‌شود؛ زنجیرهٔ fallback: نسخهٔ کامل → ایموجی 🏢.
- تشخیص سلامت موتور: `ptfBootSplashState().liquid`.

## ۵) نسخه و سازگاری
- `VERSION.json`، `crm/manifest.json`، `crm/sw.js` (RELEASE/ASSET_VERSION/CACHE)، `index.html` (PTF_CRM_RELEASE + هر ۱۱۶ کوئری `?v=`) و نقاط رسمی A6 (`api/sales-domain.php`، `crm/clear-cache.html`، `crm/shell.js`) → **v34.38.19**.
- سازگاری: مسیر بوتِ `data_pull` تک‌مرحله‌ایِ v34.38.15/.16 و سپرهای داده‌ای .12/.13/.14 دست‌نخورده؛ مسیر دستیِ مهاجرت از «تنظیمات ← وضعیت دستگاه» عیناً با confirm/alert کار می‌کند.

## ۶) تأیید
- `tester627-v34.38.19-silent-login-automigrate-liquid-ring.js` — ۲۸ پین قرارداد + ارزیابی واقعی فرمول سرعت + ابعاد PNG برش.
- `tester552` (بنر مهاجرت) با پین قراردادِ به‌روزرسانی‌شده ۲۵/۰ سبز؛ گیت کامل CI و arch-guard سبز.
- پیش‌نمایش رستری هندسهٔ مایع در چهار نقطهٔ حلقه (بالا/فرود/پایین/صعود) از روی خودِ فرمول‌های نهایی بررسی شد.
