# RELEASE NOTES — v34.8.2 (۱۴۰۵/۰۶/۰۲ — 2026-08-24)

## پنل‌های شناور تقویم و دوره

- پنل تقویم روز شمسی و انتخابگرهای جدولی ماه/سال از جریان چیدمان صفحه خارج شدند و به‌صورت overlay ثابت روی viewport باز می‌شوند.
- بازشدن تقویم دیگر ارتفاع فرم، ردیف جدول یا دیالوگ را تغییر نمی‌دهد و باعث جابه‌جایی ساختار صفحه نمی‌شود.
- موقعیت پنل بر اساس محل دکمهٔ انتخاب و فضای قابل‌استفادهٔ viewport محاسبه می‌شود؛ اگر پایین کنترل فضای کافی نباشد، پنل به‌صورت خودکار رو به بالا باز می‌شود.
- عرض پنل در نمایشگرهای کوچک داخل حاشیهٔ امن صفحه محدود می‌شود و محتوای بلند با ارتفاع کنترل‌شده و اسکرول داخلی در دسترس می‌ماند.
- هنگام scroll یا تغییر اندازهٔ پنجره، موقعیت پنل باز دوباره محاسبه می‌شود.
- در هر لحظه فقط پنل مرتبط باز می‌ماند؛ کلیک دوباره روی trigger یا کلیک بیرون، پنل را می‌بندد.
- ویژگی‌های دسترس‌پذیری `role="dialog"`، `aria-haspopup` و وضعیت `aria-expanded` برای triggerهای روز، ماه و سال هماهنگ شدند.

## آزمون و جلوگیری از رگرسیون

- قرارداد ثابت هر سه پنل روز، ماه و سال، جای‌گذاری مبتنی بر `getBoundingClientRect`، بازشدن رو به بالا و reposition هنگام scroll/resize آزموده می‌شود.
- تست runtime با DOM شبیه‌سازی‌شده، trigger نزدیک پایین viewport را برای تقویم روز و پنل دوره بررسی می‌کند و در محدوده‌ماندن overlay، وضعیت دسترس‌پذیری و toggle بسته‌شدن را می‌سنجد.
- تستر اختصاصی DateKit پس از این تغییرات **۴۸ آزمون موفق و صفر شکست** دارد.
- آزمون رفت‌وبرگشت تمام **۷۳٬۰۴۹ روز معتبر شمسی از سال ۱۳۰۰ تا ۱۴۹۹** همچنان بدون rollover اجرا می‌شود.
- گیت کامل CI شامل نگهبان معماری و **۱۱۹ مجموعهٔ آزمون** با نتیجهٔ **۱۱۹ موفق و صفر شکست** اجرا شد.

## نسخه و cache

- نسخهٔ رسمی CRM از `v34.8.1` به **`v34.8.2`** ارتقا یافت.
- `VERSION.json`، نسخهٔ رابط CRM، service worker، manifest، صفحهٔ پاک‌سازی کش، shell و نسخهٔ سرویس sales-domain هم‌راستا شدند.
- cache-bust همهٔ دارایی‌های CRM و نام cache سرویس‌ورکر به `34.8.2` تغییر کرد تا مرورگر و PWA فایل‌های جدید را دریافت کنند.
