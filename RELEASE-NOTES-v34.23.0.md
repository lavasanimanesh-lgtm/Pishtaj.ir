# RELEASE NOTES — v34.23.0 (سه‌گانهٔ درخواست‌ها — RCA و ریشه‌کنی)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** tester568 (۲۵ سنجه) · **گیت:** 184 PASS / 0 FAIL · **PHP-lint:** php-parser ✓

## شکایت مالک (سه علامت)

درخواستی از سایت ثبت و تایید شد؛ ① به‌جای بالای فهرست در انبوه گم شد ② سورت تاریخ هم ۱۰ شهریور را بالا نیاورد (فقط با سرچ نام شرکت پیدا شد) ③ پیوستِ آپلودشده از سایت پس از تایید در ضمایم وجود نداشت.

## RCA ① — گم شدن در فهرست + RCA ② — سورت دروغگو

- فهرست درخواست‌ها **هیچ پیش‌فرض سوتی نداشت**؛ ترتیب = ترتیب آرایه که پس از همگام‌سازی چنددستگاهه معتبر نیست (هم‌خانوادهٔ باگ مکاتبات v34.22.0).
- موتور سورت مشترک (`ptfSortVal`) تاریخِ خروجی `toLocaleDateString('fa-IR')` را — که **ارقام فارسی و بدون صفر پیش‌رو** است («۱۴۰۵/۶/۱۰») — به‌صورت رشته مقایسه می‌کرد: «۶» از «۱۰» بزرگ‌تر و «۹» از «۱۰» بزرگ‌تر می‌شد؛ سورت تاریخ عملاً بی‌معنا بود.

### رفع

- **پیش‌فرض فهرست = تاریخ نزولی** — درخواست تازه (تایید سایت یا ثبت داخلی) همیشه بالا.
- `ptfSortVal` بازنویسی شد: نرمال‌سازی ارقام فارسی/عربی → تشخیص تاریخ با جداکنندهٔ یکسان و مرز رقم → **اعتبارسنجی (پنجرهٔ سال شمسی ۱۲۰۰–۱۶۰۰ / میلادی ۱۹۰۰–۲۱۰۰، ماه ≤۱۳/۱۲، روز ≤۳۲)** → عدد قابل مقایسه؛ شمسی +۶۲۱ برای هم‌مرتبه‌شدن دو تقویم؛ ساعت اختیاری به دقیقه. مسیر مبالغ/اعداد و fallback رشته‌ای دست‌نخورده. **همهٔ فهرست‌های استفاده‌کننده از sortable** (نه فقط درخواست‌ها) بهره می‌برند. تست رفتاری: ۱۰ شهریور > ۹ شهریور، ماه ۱۰ > ماه ۶ ✓.

## RCA ③ — پیوست گم‌شده (مهم‌ترین)

زنجیرهٔ دقیق حذف:

1. تایید درخواست سایت درست کار می‌کند و پیوست ابری را در `files.oth` رکورد می‌کند.
2. ذخیره از مسیر فرمان اتمیک (`entity_upsert`) می‌گذرد و **پاک‌ساز رکورد سرور (`sd_entity_sanitize_row`) مقدارِ لیستیِ داخل map را بی‌صدا حذف می‌کرد** — `files = {oth:[…]}` → `files = []` روی سرور.
3. پس از ACK، **projection سرور جایگزین کپی محلی می‌شود** → ضمیمه بلافاصله پس از تایید «وجود نداشت». خود فایل در فضای ابری سالم است؛ فقط متادیتای رکورد ریخته بود.

### ریشه‌کنی (سرور + خودترمیمی)

- **سرور:** بدنهٔ لیست به هلپر مشترک `sd_entity_sanitize_list` منتقل شد و شاخهٔ map حالا مقدار لیستی را با همان سقف‌های امن (۶۰ قلم / ۲۰ فیلد / ۲۰۰۰ نویسه) نگه می‌دارد. این رفع کلاسِ باگ است، نه فقط این نمونه.
- **خودترمیمی (`ptfRfqHealSiteFiles`):** رکوردهای قبلاً آسیب‌دیده `siteAttachment` (map اسکالر — زنده مانده) دارند؛ در اولین رندر/تایید، `files.oth` از همان کلید ابری **بازسازی** می‌شود — یعنی پیوستِ همین پروندهٔ گم‌شده برمی‌گردد. گارد «یک‌بار در جلسه» جلوی حلقه با سرورِ هنوز به‌روز-نشده را می‌گیرد؛ رکورد درمان‌شده با برچسب `healedAt` ثبت می‌شود.

## ③‌ جدید — فیلتر منبع

فیلتر «همهٔ منابع / 🌐 از سایت / 🏢 داخلی» در نوار ابزار درخواست‌ها (پیشنهاد خودِ مالک) + بج 🌐 و خط «ثبت‌شده از وب‌سایت» که از قبل روی ردیف‌ها بود.

## فایل‌های کلیدی

`crm/sortable.js` (ptfSortVal) · `crm/bridge.js` (پیش‌فرض سورت + فیلتر منبع + ptfRfqHealSiteFiles) · `api/sales-domain.php` (sd_entity_sanitize_list + شاخهٔ map) · `_tools/uat/tester568-v34.23.0-rfq-site-fix.js`
