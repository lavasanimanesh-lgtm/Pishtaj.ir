# RCA ۱۴۰۵/۰۷/۰۴ — چرا اطلاعات تماس مشتری روی سرور نمی‌ماند + چرا بعضی کلیدها گیر می‌کند

> **ریشهٔ جدید (اثبات‌شده با اجرای کد واقعیِ سرور): عقب‌گردِ مهرِ نسخهٔ رکورد (`updatedAt`) روی مسیر `data_push`.**
> سرور با این مهر تصمیم می‌گیرد «ویرایشِ رسیده تازه است یا کهنه». این مهر **توسط کلاینت قابل‌نوشتن است**: یک
> `data_push` عمده‌فروشی از یک دستگاهِ کهنه، مهرِ سرور را به مقدار قدیمی‌تر برمی‌گرداند. از آن لحظه، اولین
> ویرایشِ هر دستگاهی که مهر محلی‌اش با آن مقدار برابر باشد، «ویرایشِ تازه» تشخیص داده می‌شود ⇒ قانون
> **LWW (آخرین‌نویسنده برنده)** اعمال می‌شود ⇒ شماره‌هایی که فقط روی سرور بودند **بی‌صدا حذف می‌شوند**.
>
> برای کاربر این دو شکل دیده می‌شود: «من شماره را ثبت کردم ولی روی سرور نیست» و «مالِ من سالم، مالِ او پاک».

---

## ۰) وضعیت زنده — آن‌چه قبلاً گفته شده، امروز دیگر برقرار نیست

| ادعای گزارش‌های قبلی | وضعیت امروز (۱۴۰۵/۰۷/۰۴) |
|---|---|
| دیپلوی پروداکشن پشتِ `552 Disk full` مانده و روی `2c6135a` قفل است | ❌ منقضی. `__diag__.txt` زنده: `expect: sha=65440a8… sw=v34.39.40` و هم مارکر HTTP و هم FTP **تأیید شده** (ران ۲۰۲۶-۰۹-۲۶T09:48:48Z) |
| R5 — تعریف `sd_tombstone_kinds` وسط زنجیرهٔ `elseif` همهٔ فرمان‌های `entity_*` را بی‌نوشت ack می‌کرد | ❌ در کد فعلی **رفع شده**: گارد به پیش از دیسپچر منتقل شده (`api/sales-domain.php:1651`) و زنده است |
| «کلیدهای تماس پیش از ارسال strip می‌شوند، برای همین ثبت نمی‌شود» | ❌ برای **فرم اصلی مشتری** صادق نیست: `saveCust2` با `reason='offer-cust'` صدا می‌زند که در `CONTACT_TOUCH_REASONS` هست (`crm/sales-domain-v2.js:743`) |

**نتیجه:** رفع‌های قبلی زنده‌اند و سناریویِ پایه درست کار می‌کند (تست A در پیوست: شماره ثبت و روی سرور ماندگار شد).
پس خرابیِ جاری یک «رفعِ مستقرنشده» نیست؛ **یک کلاسِ خطای بازمانده است که هیچ‌یک از سپرهای قبلی آن را نمی‌بندد.**

---

## ۱) بازتولیدِ آزمایشگاهی روی خودِ کد سرور

هارنسِ پیوست (`_tools/uat/e2e-contact-watermark-rewind-v34.39.42.js`) **`api/crm.php` و `api/sales-domain.php`
واقعی را با PHP واقعی (php-wasm)** اجرا می‌کند — نه شبیه‌سازی. هر درخواست یک نمونهٔ تازهٔ PHP است و state
بین درخواست‌ها حمل می‌شود (مثل HTTP). سناریوها، مسیرِ **فرمِ ویرایش مشتری (`saveCust2`)** را بازتولید می‌کنند،
نه مسیرِ ابزار تشخیص را.

**نتیجه: ۲۴ PASS / ۲ FAIL — و هر دو شکست، دقیقاً همان خرابیِ گزارش‌شده هستند.**

| # | سناریو | نتیجه |
|---|---|---|
| A | ویرایش تک‌دستگاهه: افزودن موبایل با پایهٔ تازه | ✔ شماره ثبت شد، `rev` جلو رفت |
| B | همان ویرایش + فرمان دومِ هوکِ `phonefmt` | ✔ شماره ماند |
| C | دستگاهِ کهنه (محتوای کهنه، بدون `updatedAtISO`) کل مجموعه را `data_push` کرد | ✔ شماره ماند (union درست کار کرد) |
| **D** | **پایهٔ تازه + محتوای کهنه** (`updatedAt` محلی == سرور، ولی شماره را ندارد) | ✘ **شماره حذف شد** |
| E | مشتری حقیقی (شماره فقط در `phones[]`) | ✔ ماند |
| F | دو دستگاه هم‌زمان (یکی پایهٔ تازه، یکی کهنه) | ✔ هر دو شماره ماندند |
| G | نویسندهٔ غیرتماسی (کلیدهای تماس strip شده) | ✔ ماند (قانون «غایب = تغییرنکرده») |
| H | پاک‌سازی آگاهانه (`_ccClear`) سپس افزودنِ دوباره | ✔ درست |
| **I** | **آیا `data_push` مهرِ سرور را عقب می‌بَرد؟** | ✘ **بله: `T1 → T2 → T1`** |
| **J** | **زنجیرهٔ کامل: push کهنه ⇒ عقب‌گردِ مهر ⇒ ویرایشِ بعدی شماره را می‌شوید** | ✘ **شماره حذف شد؛ ویرایشِ جدید ثبت شد** |

خروجیِ واقعیِ J:

```
✔ J1 شماره روی سرور ثبت شد
✔ J2 مهر سرور به مهرِ دستگاه کهنه برگشت
✔ J3 ویرایش دستگاه کهنه ok
✘ J4 شمارهٔ دستگاه A پس از این ویرایش روی سرور ماند؟ → people=[{"nm":"آقای رابط","tels":[{"n":"۰۲۱۸۸۰۰۱۲۳۴"}],"mobs":[],"mails":[]}]
✔ J5 آدرس جدید ثبت شد (یعنی ویرایش واقعاً اعمال شده)
```

یعنی: **ویرایش ثبت می‌شود، شماره نه.** دقیقاً همان «ثبت شد ولی روی سرور نیست» — با این تفاوت که شماره ابتدا
ثبت شده و سپس توسط نوبتِ بعدی پاک می‌شود.

---

## ۲) مکانیکِ دقیق — کجا تصمیمِ اشتباه گرفته می‌شود

سه نقطه زنجیره را می‌سازند:

**① مهرِ نسخه از کلاینت پذیرفته می‌شود (ریشهٔ اصلی)**
`data_push` آرایهٔ ارسالیِ دستگاه را — به‌جز فیلدهای تماس که merge می‌شوند — **عیناً** ذخیره می‌کند.
پس `updatedAt`/`updatedAtISO` یِ رکوردِ سرور همان چیزی می‌شود که دستگاه فرستاده است؛ حتی اگر قدیمی‌تر باشد.
هیچ جا «عدمِ عقب‌گرد» کنترل نمی‌شود.

**② تصمیمِ «تازه/کهنه» با مقایسهٔ همین مهر گرفته می‌شود**
```php
// api/crm.php:794-796
$inAt  = trim((string)($r['updatedAtISO'] ?? $r['updatedAt'] ?? ''));
$srvAt = trim((string)($srvRec['updatedAt'] ?? $srvRec['updatedAtISO'] ?? ''));
$fresh = ($inAt !== '' && $srvAt !== '' && strcmp($inAt, $srvAt) > 0);
```
```php
// api/contact-merge-lib.php — cm_contact_record_base_is_current()
$inBase = trim((string)($inRec['_ccBaseAt'] ?? $inRec['_ccBaseAtISO']
              ?? $inRec['updatedAt'] ?? $inRec['updatedAtISO'] ?? ''));
return $inBase === $srvAt;
```
```php
// api/sales-domain.php — sd_contact_stale_merge()
if ($clientBase !== '' && $serverAt !== '' && $clientBase === $serverAt) {
    $stats['contactsFreshEdit'] = …; return $row;   /* «ویرایش تازه» ⇒ LWW */
}
```

**③ وقتی «تازه» تشخیص داده شد، اتحاد (union) دیگر اجرا نمی‌شود**
`cm_contact_merge_record()` و `sd_contact_stale_merge()` هر دو در حالتِ تازه **زود بازمی‌گردند** و محتوای
ورودی را جایگزین می‌کنند. هر کانالِ تماسی که فقط روی سرور بوده و در payload نیست ⇒ حذف.

**زنجیرهٔ کامل در عمل:**

```
۱) دستگاه A شمارهٔ X را ثبت می‌کند (entity_upsert)  ⇒ سرور: محتوا شامل X، updatedAt = T2
۲) دستگاه B (کهنه، X را ندارد) کل مجموعه را push می‌کند
      ⇒ inBase(T1) ≠ srvAt(T2) ⇒ union ⇒ محتوا درست می‌ماند
      ⇒ اما آرایهٔ B ذخیره می‌شود و updatedAtِ سرور به T1 برمی‌گردد      ← عقب‌گرد (تست I)
۳) دستگاه B همان مشتری را ویرایش می‌کند (مثلاً آدرس)
      ⇒ _ccBaseAt(T1) == srvAt(T1) ⇒ «ویرایش تازه» ⇒ LWW
      ⇒ شمارهٔ X که فقط روی سرور بود حذف می‌شود                        ← تست J
```

**چرا «بعضی اوقات»؟** چون به ترتیبِ رسیدنِ یک pushِ کهنه و یک ویرایشِ بعدی بستگی دارد؛ و چون نویسنده‌های
خودکار (`phonefmt`, `coen-fill`, `vendorlist`, healهای دوره‌ای) تعدادِ این نوبت‌ها را بالا می‌برند.

**چرا نشانه‌ها متناقض‌اند؟** چون `rev` و مهر با هم جلو نمی‌روند: `data_push` مهر را عقب می‌بَرد ولی `rev` را
جلو می‌برد. به همین دلیل ابزار تشخیص چیزی شبیه «rev محلی == سرور، ولی نسخهٔ سرور شماره ندارد» نشان می‌دهد —
همان تناقضی که در گزارش ۱۴۰۵/۰۷/۰۳ ثبت شده و تا امروز بی‌پاسخ مانده بود.

---

## ۳) چرا «بعضی کلیدها گیر می‌کند»

این علامت از همان نویسنده‌های مکرر می‌آید و با ریشهٔ بالا هم‌خانواده است:

1. **باطل‌شدنِ ممتدِ صف (kept).** در `ptfBFlushQueue` (`crm/client-server.js:790`) اگر حینِ ارسال، مقدارِ محلیِ
   کلید عوض شود، ردیفِ صف پاک نمی‌شود (`queueClearMatching → kept`) و کلید دوباره dirty می‌ماند. چون روی
   `ptf_crm_customers` نویسنده‌های خودکارِ زیادی کار می‌کنند، هر flush می‌تواند بلافاصله باطل شود ⇒ نوار زردِ ماندگار.
2. **فرمانِ رها‌شده‌ی بدون callback.** هوکِ `phonefmt` روی هر ذخیرهٔ مشتری یک `ptfEntityUpsert` یِ **بدون `cb`**
   می‌فرستد (`crm/phonefmt.js:126`). نه پیام خطا می‌دهد، نه `failDirty()` را صدا می‌زند، نه در شمارشِ
   `settleCb` می‌آید ⇒ شکستِ آن کاملاً نامرئی است و کلید در وضعیتِ نامعلوم می‌ماند.
3. **تعارضِ بی‌همگرایی.** کلیدهای `rejected/skipped/conflicts` تا سه نوبت نجات (`rescueConflictedKeys`) تلاش
   می‌کنند؛ اگر فرمِ کانونیکِ کلاینت با فرمِ سرور یکی نشود، کلید در همان‌جا می‌ماند.

**پیوندِ دو علامت:** نویسنده‌های مکرر هم کلید را dirty نگه می‌دارند («گیر کردن») و هم تعدادِ نوبت‌هایی را که
می‌توانند به حفرهٔ LWW بخورند زیاد می‌کنند («ثبت نمی‌شود»). یک ریشه، دو علامت.

---

## ۴) چرا ده‌ها ایجنت نتوانستند حل کنند

همهٔ لایه‌های قبلی روی **مسیرِ نوشتن** سپر گذاشتند: stripِ کلیدهای تماس (v34.39.12)، `sd_contact_wipe_guard`
(v34.38.25)، `sd_contact_stale_merge` (v34.39.12)، مهرهای `_ccClear/_ccEdit/_ccBaseAt`، `AUTO_NO_DELETE_REASONS`،
سپرِ حذفِ انبوه… اما **مبنای تصمیم** — یعنی مهرِ `updatedAt` — همچنان در اختیارِ کلاینت است.

نتیجه: سپرها فقط وقتی کار می‌کنند که مهر درست باشد. با یک عقب‌گردِ مهر، سپرها نه‌تنها بی‌اثر می‌شوند، بلکه
**همان مسیرِ «محافظت‌شده» به مسیرِ حذف تبدیل می‌شود**. هر سپری که اضافه شده احتمال را کم کرده، ولی کلاسِ خطا را
نبسته است — برای همین هر بار «تا چند روز درست بود، دوباره برگشت».

---

## ۵) رفعِ پیشنهادی (سه لایه)

**L1 — سرور مالکِ مهر باشد (کمینه، امن، فوری).**
در مسیر `data_push` و هر مسیرِ عمده‌فروشی، مهرهای `updatedAt`/`updatedAtISO` یِ ارسالیِ کلاینت پذیرفته نشوند؛
مهر سمتِ سرور زده شود و **هرگز به عقب برنگردد** (`max(مهر فعلی، مهر جدید)`). این یک خطِ دفاع است و رفتارِ
خوش‌بینانه را عوض نمی‌کند.

**L2 — تصمیمِ «تازه» را از مهرِ قابل‌نوشتن جدا کن.**
مبنای مقایسه یا `rev` ِ سرور باشد (که کلاینت نمی‌تواند عقب ببرد) یا امضای محتوای فیلدهای تماس
(`hash(people/coTels/phones/ph)`) که کلاینت همراهِ فرم بفرستد و سرور با مقدارِ فعلی‌اش بسنجد.

**L3 — روی مسیرِ «تازه» هم حذفِ ضمنی ممنوع.**
حتی وقتی ویرایش تازه تشخیص داده شد، حذف فقط با نشانهٔ صریح مجاز باشد (`_ccClear`، یا نشانهٔ حذفِ تک‌کانال).
افزوده‌ها همیشه union. این لایه کلاسِ خطا را می‌بندد، نه فقط نمونه‌اش را.

**ابزارِ کمکی (برای قطعی کردن روی سیستم زنده).** افزودنِ دو ستون به «تشخیص سینک تماس»:
«مهرِ سرور در برابر مهرِ دستگاه» و «تصمیمِ سرور (تازه/کهنه/اتحاد)» از `sanitize`/`stats` یِ پاسخ فرمان.
با این دو ستون می‌توان در یک نگاه دید کدام دستگاه مهر را عقب برده است.

---

## ۶) آن‌چه برای قطعی کردن روی سیستم زنده لازم است

1. نام/کدِ یک مشتری که همین امروز روی آن مشکل دیده شده (مثلاً همان `عباس رجایی‌زاده`).
2. خروجیِ «🩺 تشخیص سینک تماس» برای همان مشتری، روی **دو دستگاه** (دستگاهی که شماره را ثبت کرده و دستگاهی که نمی‌بیند).
3. در صورت امکان: «تنظیمات ← وضعیت دستگاه» روی هر دو (مقادیرِ `rev` و کلیدهای dirty).

با این سه مورد می‌توانم تأیید کنم که آیا سیستم زنده دقیقاً در همین حالت است یا یک عاملِ فرعی هم دارد —
و بلافاصله L1 را با همین هارنس verify کنم (هدف: D و J از FAIL به PASS بروند، بدون افتِ ۲۴ تای دیگر).

---

*این گزارش با اجرای کد واقعیِ سرور (`php-wasm`) روی شاخهٔ `arena/01a0df02-pishtaj-ir` تولید شد.
هارنسِ بازتولید: `_tools/uat/e2e-contact-watermark-rewind-v34.39.42.js`
(`npm i php-wasm` در کنار فایل، سپس `node e2e-contact-watermark-rewind-v34.39.42.js`).*
