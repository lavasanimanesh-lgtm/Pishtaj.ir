# ممیزی فنی سئو و نقشه راه رتبه‌۱ گوگل — پیشرو تجهیز فرتاک (Pishtaj.ir)

**تهیه‌کننده:** متخصص ارشد سئو | **تاریخ:** ۲۰۲۶-۰۸-۱۵ | **دامنه هدف:** pishtaj.ir

---

## ۰) خلاصه مدیریتی (Executive Summary)

بررسی عمیق سورس وب‌سایت نشان می‌دهد زیرساخت **فنی و محتوایی** سایت در مجموع **بالاتر از میانگین رقبای B2B ایرانی** است، اما بین «وضعیت فعلی» و «رتبه‌۱ شدن برای کلمات تجاری هدف» یک **شکاف استراتژیک** وجود دارد که دقیقاً همان‌جاست که باید کار کنیم:

- ✅ **نقاط قوت:** HTTPS و ریدایرکت‌های تمیز، نقشه سایت، داده ساختاریافته (Schema)، پایگاه دانش ~۴۵۰ مقاله تخصصی، ساختار سلسله‌مراتبی H1/H2/H3، فشرده‌سازی و کش.
- ⚠️ **نقاط ضعف حیاتی:** ۶ کلمه کلیدی فاز اول شما از خانواده «**تامین‌کننده**» هستند، در حالی که صفحات فعلی فقط روی «**تامین تجهیزات**» هدف‌گذاری شده‌اند. یعنی برای نیمی از کلمات هدف، **هنوز صفحه هدفمند (Landing Page) وجود ندارد**.
- 🐞 **باگ‌های فنی واقعی:** `og:url` تمام صفحات خدمات/صنایع به آدرس‌های شکسته و ناقص اشاره می‌کند؛ Schema سازمان در صفحه اصلی **دوبار و متناقض** تکرار شده؛ برند در تایتل‌ها به‌طور ناهماهنگ نوشته شده.

**نتیجه:** رسیدن به رتبه‌۱ برای کلمات فاز اول **کاملاً دست‌یافتنی است**، مشروط به اجرای یک برنامه سه‌محوره: (۱) رفع باگ‌های فنی، (۲) ساخت/بهینه‌سازی صفحات فرود تجاری، (۳) تقویت سیگنال‌های اعتبار (لینک داخلی + اتوریته برند).

---

## ۱) بخش اول — ارزیابی وضعیت فعلی سئو (SEO Audit)

### ۱.۱ سئوی فنی (Technical SEO)

| آیتم | وضعیت | جزئیات |
|---|---|---|
| HTTPS + ریدایرکت www→non-www | ✅ مطلوب | در `.htaccess` با 301 پیاده‌سازی شده |
| ریدایرکت URLهای قدیمی `.html` → آدرس تمیز | ✅ مطلوب | حدود ۵۰+ رول 301 برای `/about`, `/services`, `/blog`, `/knowledge-center` |
| `robots.txt` | ✅ مطلوب | بلاک صحیح `/crm/` و `/api/`، اتصال صحیح sitemap |
| `sitemap.xml` | ⚠️ قابل بهبود | ۶۱۷ URL (شامل ۴۵۰ مقاله پایگاه دانش). اما `lastmod` اکثراً ثابت 2026-07-21 است و **همه در یک فایل واحد** (بدون ایندکس sitemap) |
| Canonical | ✅ مطلوب | روی صفحات اصلی و خدماتی حاضر است |
| Hreflang (fa-IR / x-default / en) | ✅ مطلوب | روی صفحه اصلی و صفحات خدماتی |
| Open Graph / Twitter Cards | ⚠️ دارای باگ | `og:title/description/image` حاضرند، اما **`og:url` خراب است** (جزئیات در بخش ۲) |
| Schema (JSON-LD) | ⚠️ دارای باگ | Organization + FAQPage + OfferCatalog حاضر، اما **سازمان دوبار تعریف شده** |
| فشرده‌سازی (gzip/deflate) و کش (expires) | ✅ مطلوب | در `.htaccess` فعال |
| پیش‌بارگذاری فونت/تصویر (preload) | ✅ مطلوب | Vazirmatn و تصویر hero به‌صورت webp |
| Core Web Vitals | ⚠️ نیاز به اندازه‌گیری | ساختار خوب است اما باید با Lighthouse/PSI عددی اندازه‌گیری و رفع شود |

### ۱.۲ سئوی درون‌صفحه (On-Page SEO)

**نقاط قوت:**
- ساختار سلسله‌مراتبی صحیح: هر صفحه خدماتی دقیقاً **یک H1** + چند H2 + H3 دارد.
- متن جایگزین (alt) تصاویر در صفحه اصلی کامل است (۰ تصویر بدون alt).
- تایتل و متا-دسکریپشن برای تمام صفحات خدماتی/صنایع تعریف شده.

**نقاط ضعف (بحرانی برای هدف فاز اول):**

1. **عدم پوشش خانواده کلمات «تامین‌کننده»:**
   - کلمات هدف شما: «تامین‌کننده پایپینگ»، «تامین‌کننده تجهیزات ابزار دقیق»، «تامین‌کننده تجهیزات پتروشیمی»، «تامین‌کننده تجهیزات صنایع پتروشیمی».
   - صفحات فعلی فقط «**تامین تجهیزات** …» را هدف گرفته‌اند (مثلاً `services/piping-supply.html` با تایتل «تامین تجهیزات پایپینگ»).
   - گوگل این دو خانواده را **دو intent متفاوت** می‌بیند: «تامین تجهیزات X» (خدمت) در برابر «تامین‌کننده X» (انتخاب یک شرکت/فروشنده). برای رتبه گرفتن در «تامین‌کننده…» باید صفحاتی با همان فیلولوژی و سیگنال تجاری قوی ساخته شوند.

2. **ناهماهنگی برند در تایتل‌ها:** در صفحات مختلف از «پیشرو تجهیز»، «پیشرو تجهیز فرتاک»، «Pishro Tajhiz Fartak (PTF)» استفاده شده. این ناهماهنگی سیگنال برند را تضعیف می‌کند (به‌ویژه برای جست‌وجوهای برندمحور و پنل دانش گوگل).

3. **تایتل صفحه اصلی** «تامین تجهیزات صنعتی نفت، گاز و پتروشیمی | پیشرو تجهیز» است که بر «صنعتی» تمرکز دارد و از کلمات پرتکرار فاز اول («تامین‌کننده پایپینگ»، «تامین تجهیزات ابزار دقیق») غافل است.

4. **نبود BreadcrumbList و Service Schema در صفحات خدماتی:** فقط صفحه صنعت پتروشیمی Breadcrumb دارد؛ صفحات خدمات فاقد `Service`/`BreadcrumbList` هستند که باعث از دست رفتن ریچ‌اسنیپت می‌شود.

### ۱.۳ سئوی محتوا و اتوریته موضوعی (Content & Topical Authority)

- پایگاه دانش شامل **۴۵۰ مقاله HTML** (نه «۲٬۰۰۰ مقاله» که در سند قبلی ادعا شده — عدد قبلی نادقیق است) با پوشش خوب لوله، فلنج، شیرآلات، ابزار دقیق، پمپ، کمپرسور.
- وبلاگ + خدمات + صنایع ساختار خوشه‌ای (Cluster) خوبی ساخته‌اند.
- **شکاف محتوایی اصلی:** نبود «صفحات فرود تجاری/مقایسه‌ای» برای جست‌وجوهای با نیت خرید (Buyer Intent) مثل «تامین‌کننده تجهیزات پتروشیمی»، «لیست تامین‌کنندگان پایپینگ»، «بهترین تامین‌کننده ابزار دقیق».

### ۱.۴ سئوی خارجی (Off-Page / Authority)

- داده لینک خارجی در دسترس نیست، اما برای رقابت در کلمات تجاری صنعتی فارسی، **لینک‌سازی و برند سیگنال (Google Business Profile، ثبت در دایرکتوری‌های صنعتی، نبض و اکسپوها)** تعیین‌کننده است. این بخش باید در فاز موازی اجرا شود.

---

## ۲) باگ‌ها و ناهمسانی‌های فنی که باید فوراً اصلاح شوند

### 🐞 باگ ۱ — `og:url` خراب در تمام صفحات خدمات/صنایع (Critical)
مقدار `og:url` به آدرس‌های ناقص و شکسته‌ای اشاره می‌کند که عملاً 404 هستند:
```
services/piping-supply.html          → og:url = https://pishtaj.ir/services/تامین-تجهیزات-پایپینگ-لوله،-فلنج،-فیتین.html
services/instrumentation-supply.html → og:url = https://pishtaj.ir/services/تامین-تجهیزات-ابزار-دقیق-ترانسمیتر،-فلو.html
services/valves-supply.html          → og:url = https://pishtaj.ir/services/تامین-شیرآلات-صنعتی-انواع-ولو،-gate,-gl.html
... (همه ۷ صفحه خدمات + برخی صنایع)
```
**اثر:** هر اشتراک‌گذاری در واتس‌اپ/تلگرام/لینکدین به صفحه 404 می‌رود → از دست رفتن سیگنال‌های اجتماعی و کلیک ارگانیک.
**اصلاح:** `og:url` باید **دقیقاً برابر canonical** هر صفحه باشد.

### 🐞 باگ ۲ — تعریف دوباره و متناقض Organization Schema (High)
در `index.html` دو بلوک JSON-LD مجزا (خط ۱۲۹ و ۳۱۱) هر دو `"@id": "#organization"` دارند؛ یکی نسخه کامل با OfferCatalog و یکی نسخه ساده‌تر. این برای گوگل پیام متناقض می‌فرستد.
**اصلاح:** ادغام در **یک** بلوک `@graph` واحد و حذف تکرار.

### 🐞 باگ ۳ — ناهماهنگی برند در تایتل‌ها (Medium)
**اصلاح:** انتخاب یک فرم ثابت — پیشنهاد «**پیشرو تجهیز فرتاک**» (و «Pishro Tajhiz Fartak» در محتوای لاتین) — و اعمال یکنواخت در همه تایتل‌ها، لوگو و Schema.

### ⚠️ مورد ۴ — ساختار sitemap
**اصلاح:** ساخت `sitemap-index.xml` با چند فایل بخش‌بندی‌شده (sitemap-core.xml، sitemap-services.xml، sitemap-knowledge-center.xml، ...) + به‌روزرسانی خودکار `lastmod`.

---

## ۳) تحلیل و نگاشت کلمات کلیدی (Keyword Mapping)

### ۳.۱ فاز اول — کلمات تجاری اولویت‌دار (متن کاربر)

| # | کلمه کلیدی | intent | صفحه هدف فعلی | وضعیت | اقدام |
|---|---|---|---|---|---|
| 1 | تامین تجهیزات پایپینگ | تجاری/خدمتی | `services/piping-supply.html` | ✅ موجود | بهینه‌سازی عمقی |
| 2 | تامین‌کننده پایپینگ | خرید/انتخاب شرکت | ❌ وجود ندارد | ❌ | **ساخت LP جدید** |
| 3 | تامین تجهیزات ابزار دقیق | تجاری/خدمتی | `services/instrumentation-supply.html` | ✅ موجود | بهینه‌سازی عمقی |
| 4 | تامین‌کننده تجهیزات ابزار دقیق | خرید/انتخاب شرکت | ❌ وجود ندارد | ❌ | **ساخت LP جدید** |
| 5 | تامین‌کننده تجهیزات پتروشیمی | خرید/انتخاب شرکت | ❌ (صنعت پتروشیمی «تامین تجهیزات صنعت پتروشیمی» است) | ❌ | **ساخت LP جدید** |
| 6 | تامین‌کننده تجهیزات صنایع پتروشیمی | خرید/انتخاب شرکت | ❌ | ❌ | **ساخت LP جدید** |

> نکته: «تامین تجهیزات ابزار دقیق» در پیام شما دوبار آمده (تکرار) — در جدول یکی لحاظ شد.

### ۳.۲ فاز دوم — بسط تجاری (Commercial Expansion)
- تامین شیرآلات صنعتی (✅ موجود: `valves-supply.html`)
- تامین فلنج و اتصالات / تامین لوله مانیسمان
- تامین تجهیزات برق صنعتی (✅ موجود: `electrical-supply.html`)
- تامین تجهیزات نفت و گاز (✅ موجود: `industries/oil-gas`)
- تامین تجهیزات نیروگاهی / فولاد / سیمان (✅ ساختار صنایع موجود)
- تامین پمپ صنعتی / تامین کمپرسور صنعتی
- تامین کالای پروژه EPC / بازرگانی تجهیزات صنعتی
- واردات تجهیزات صنعتی / ترخیص کالای صنعتی

### ۳.۳ فاز سوم — Long-tail اطلاعاتی (از پایگاه دانش تغذیه می‌شود)
- قیمت لوله مانیسمان A106 گرید B
- خرید فلنج گلودار ASME B16.5
- راهنمای انتخاب بال‌ولو API 6D
- کالیبراسیون ترانسمیتر فشار Rosemount 3051
- گواهی MTC EN 10204 3.1
- تفاوت لوله A106 و A333
- (ده‌ها نمونه دیگر که قبلاً در `knowledge-center/` و `blog/` تولید شده‌اند)

> پیشنهاد: پیش از اجرا، حجم جست‌وجوی دقیق هر کلمه را با Keyword Planner / Ahrefs (نسخه فارسی) یا ابزارهای ایرانی اعتبارسنجی کنید. اولویت را بر اساس **نیت خرید + حجم** بچینید، نه صرفاً حجم.

---

## ۴) راهکار پیشنهادی برای رتبه‌۱ فاز اول (در اسرع وقت)

رسیدن سریع به رتبه‌۱ در کلمات تجاری رقابتی، ترکیبی از «سرعت اجرا» و «کیفیت سیگنال» است. برنامه زیر به‌ترتیب اولویت اجرا می‌شود:

### گام ۰ — اصلاحات فنی (هفته اول، بلوکه نمی‌شود)
1. رفع `og:url` خراب در ۷ صفحه خدمات + صنایع.
2. ادغام Schema سازمان در یک `@graph`.
3. یکسان‌سازی برند در تایتل‌ها.
4. بازسازی sitemap به‌صورت index + بخش‌بندی + به‌روزرسانی lastmod.
5. افزودن `Service` + `BreadcrumbList` Schema به صفحات خدمات.

### گام ۱ — ساخت ۴ صفحه فرود (Landing Page) تجاری فاز اول (هفته ۱–۲)
برای کلمات «تامین‌کننده …» چهار LP اختصاصی بسازید (هر صفحه یک intent، بدون cannibalization):
1. `/supplier/piping-supplier.html` → «تامین‌کننده پایپینگ»
2. `/supplier/instrumentation-supplier.html` → «تامین‌کننده تجهیزات ابزار دقیق»
3. `/supplier/petrochemical-equipment-supplier.html` → «تامین‌کننده تجهیزات پتروشیمی»
4. `/supplier/petrochemical-industries-supplier.html` → «تامین‌کننده تجهیزات صنایع پتروشیمی»

**الزامات هر LP:**
- تایتل با کلمه کلیدی دقیق در ابتدا + برند: `تامین‌کننده پایپینگ | پیشرو تجهیز فرتاک`
- H1 شامل کلمه کلیدی دقیق؛ متا-دسکریپشن با CTA (ثبت RFQ / دریافت پیش‌فاکتور).
- بلوک «چرا ما» با اعتبارسنجی (۱۲+ سال، ISO 9001/10002/10004، برندهای قابل تامین، گواهی MTC).
- جدول برندها + لیست محصولات + بخش سوالات متداول (FAQ با Schema).
- فرم RFQ بالا در دید (بالای تاخوردگی) + شماره تماس + CTA.
- گالری/مستندات + نشان مشتریان (لوگوهای پتروپارس، مپنا و…).

### گام ۲ — بهینه‌سازی عمقی ۲ صفحه موجود (هفته ۲)
- `piping-supply.html` و `instrumentation-supply.html`: تقویت تراکم معنایی کلمه هدف، افزودن FAQ Schema، بهبود تایتل (با حفظ کلمه «تامین تجهیزات …»)، افزودن Breadcrumb و Service Schema، و افزودن جدول مقایسه‌ای + CTA استعلام.

### گام ۳ — لینک‌سازی داخلی (Internal Linking) (هفته ۲–۳، مداوم)
- از ۴۵۰ مقاله پایگاه دانش + وبلاگ، با **انکر تکست متنوع و طبیعی** به LPهای فاز اول لینک بدهید (مثلاً مقاله «راهنمای لوله مانیسمان» → «تامین‌کننده پایپینگ»).
- از صفحه اصلی و صفحات صنایع (پتروشیمی/نفت‌وگاز) به LPها لینک بدهید (صفحه اصلی مهم‌ترین منبع لینک داخلی است).
- **قانون:** هر صفحه هدف باید حداقل ۱۵–۳۰ لینک داخلی مرتبط دریافت کند.

### گام ۴ — سیگنال‌های خارجی و برند (هفته ۲–۶، موازی)
- تکمیل و بهینه‌سازی **Google Business Profile** با همان NAP (نام/آدرس/تلفن) یکنواخت.
- ثبت در دایرکتوری‌های صنعتی B2B ایرانی (نبض، صنعت‌یار، اکسپوها، اتاق بازرگانی).
- دریافت بک‌لینک از رسانه‌های صنعت (خبر + رپورتاژ با کیفیت، نه اسپم).
- انتشار منظم در لینکدین با لینک به LPها (سیگنال برند و ترافیک ارجاعی).

### گام ۵ — اندازه‌گیری و بهینه‌سازی مداوم (از هفته ۱)
- اتصال کامل **Google Search Console** + **GA4** + ابزار رتبه‌سنجی.
- رصد روزانه وضعیت ایندکس و رتبه کلمات فاز اول.
- هر ۲ هفته یک‌بار: تحلیل CTR/نرخ پرش و اصلاح تایتل/دسکریپشن صفحاتی که ایندکس شده ولی کلیک نمی‌گیرند.

---

## ۵) نقشه راه فازهای بعدی

### فاز دوم (ماه ۲–۳) — بسط کلمات تجاری
- ساخت LP برای «تامین شیرآلات صنعتی»، «تامین فلنج و اتصالات»، «تامین پمپ صنعتی»، «تامین کمپرسور»، «تامین تجهیزات نیروگاهی».
- اعمال همان الگوی گام ۱ (LP تجاری + لینک داخلی + Schema).

### فاز سوم (ماه ۳–۶) — تسلط بر Long-tail اطلاعاتی
- ارتقای مقالات برتر پایگاه دانش به «پیلار + کلاستر» و لینک دادن همه مقالات مرتبط به یکدیگر و به صفحات تجاری.
- به‌روزرسانی فصلی مقالات (تازه‌نگه‌داشتن lastmod و سیگنال freshness).

### فاز چهارم (مداوم) — اتوریته برند
- تولید منظم محتوای صنعتی (لینکدین + وبلاگ + خبر)، دریافت بک‌لینک کیفی، مدیریت ریپورتاژ، و تقویت NAP و حضور در اکسپوها.

---

## ۶) KPIها و معیارهای موفقیت

| معیار | هدف (۳ ماه) | ابزار |
|---|---|---|
| رتبه کلمات فاز اول | ورود به صفحه ۱ → سپس ۳ جایگاه برتر | GSC + رتبه‌سنج |
| صفحات ایندکس‌شده | ۱۰۰٪ URLهای sitemap | GSC |
| CTR ارگانیک | بهبود ۳۰٪ | GSC |
| کلیک ارگانیک | رشد ماهانه ۲۵٪ | GSC/GA4 |
| لید (RFQ) از ارگانیک | رشد ۲۰٪ | CRM |
| Core Web Vitals | LCP < 2.5s، CLS < 0.1، INP < 200ms | PSI/Lighthouse |

---

## ۷) چک‌لیست اجرایی اولویت‌بندی‌شده (نسخه کوتاه)

**فوری (هفته ۱):**
- [ ] رفع `og:url` خراب (۷ صفحه خدمات + صنایع)
- [ ] ادغام Schema سازمان در `index.html`
- [ ] یکسان‌سازی برند در تایتل‌ها
- [ ] بازسازی sitemap (index + بخش‌بندی + lastmod)

**هفته ۱–۲:**
- [ ] ساخت ۴ صفحه فرود «تامین‌کننده …» (پایپینگ، ابزار دقیق، پتروشیمی، صنایع پتروشیمی)
- [ ] افزودن Service + Breadcrumb + FAQ Schema به صفحات خدمات

**هفته ۲–۳:**
- [ ] بهینه‌سازی عمقی `piping-supply.html` و `instrumentation-supply.html`
- [ ] لینک‌سازی داخلی از پایگاه دانش/وبلاگ به LPها (انکر متنوع)

**هفته ۲–۶ (موازی):**
- [ ] تکمیل Google Business Profile + دایرکتوری‌های صنعتی
- [ ] بک‌لینک کیفی (رسانه صنعت + رپورتاژ معتبر)
- [ ] انتشار لینکدین

**مداوم:**
- [ ] رصد GSC + رتبه‌سنجی + اصلاح CTR
- [ ] به‌روزرسانی فصلی محتوا

---

> **پیشنهاد قدم بعدی:** اگر مایل باشید، در نوبت بعد **خودم باگ‌های فنی (og:url، Schema تکراری، برند، sitemap) را روی سورس اصلاح می‌کنم** و سپس **۴ صفحه فرود فاز اول را با رعایت اصول سئو می‌سازم**. کافی است تأیید کنید تا اجرا را شروع کنم.
