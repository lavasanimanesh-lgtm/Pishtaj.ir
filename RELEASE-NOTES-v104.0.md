# 🚀 یادداشت‌های انتشار اسپرینت ۱۰۴ (Release Notes - v104.0)

**تاریخ انتشار:** ۱۴۰۵/۴/۱۵ (July 2026)  
**هدف اسپرینت:** یکپارچه‌سازی معماری، حذف تایمرهای وصله‌پینه‌ای DOM و رفع باگ‌های بحرانی (اولویت ۲ بک‌لاگ محصول)  
**تیم توسعه:** تیم ارشد معماری و کنترل کیفیت PTF  

---

## 📋 تغییرات اعمال‌شده در این نسخه

### ۱. ⚡ جایگزینی تایمرهای مخرب DOM با Event Hooks اصولی (Refactoring)
* **فایل‌های تغییر یافته:** `crm/workflow.js`، `crm/offerlock.js`، `crm/guards.js`
* **شرح تغییر:** تایمرهای سنگین و مزاحم (`setInterval`) که هر ۱.۵ ثانیه یک‌بار به طور مداوم کل ساختار صفحه (DOM) را برای تزریق دکمه‌های «حذف»، «پیوست» و «متن استعلام» یا اورراید کردن توابع اسکن می‌کردند، کاملاً حذف شدند.
* **معماری جدید:** به جای اسکن مداوم پس‌زمینه، الگوریتم به صورت Event-driven و از طریق Function Hooking پیاده‌سازی شد. اکنون دکمه‌ها دقیقاً و صرفاً یک بار، همزمان با فراخوانی توابع رندر اصلی (`renderRfq`, `renderOffers`, `renderCustomers`) به جدول اضافه می‌شوند. نتیجه این تغییر، صفر شدن مصرف پردازنده (CPU Idle = 0%) در زمان سکون و افزایش محسوس سرعت مرورگر کاربر است.

### ۲. 🖱️ رفع باگ کشیدگی ستون‌های جدول پیشنهادها (Drag Memory Leak Fix)
* **فایل تغییر یافته:** `crm/offerlock.js`
* **شرح تغییر:** باگ رها شدن رویداد `mousemove` هنگام تغییر عرض ستون‌های جدول (زمانی که کاربر ماوس را سریع از محدوده مرورگر خارج می‌کرد یا تب را عوض می‌کرد) برطرف شد. با افزودن شنودگرهای رویداد `blur` و `mouseleave` به همراه استفاده از `setPointerCapture`، اکنون در هر شرایطی رویداد تغییر عرض به شکلی ایمن و تمیز خاتمه می‌یابد و دیگر ستون‌ها به دنبال ماوس کشیده نمی‌شوند.

### ۳. 🛡️ جلوگیری از رونویسی دیتابیس محلی در کار همزمان (Smart Concurrency Control)
* **فایل تغییر یافته:** `crm/sync.js`
* **شرح تغییر:** مکانیزم ادغام هوشمند آرایه‌ها (`window.ptfSmartMerge`) برای مدیریت کار همزمان اعضای تیم (مثلاً مدیر و کارشناس فروش) پیاده‌سازی شد.
* **سازوکار جدید:** پیش از این اگر دو کاربر همزمان روی استعلام‌ها یا مشتریان کار می‌کردند، ارسال داده توسط نفر دوم کل تغییرات نفر اول را رونویسی و حذف می‌کرد. در نسخه جدید، سیستم بر اساس شناسه‌های یکتا (`cd` / `no` / `id`) و برچسب‌های زمانی (`ts` / `date`)، رکوردهای جدید هر دو دستگاه را به طور هوشمند ادغام (Merge) می‌کند تا هیچ رکوردی در کار تیمی از دست نرود.

---

## 🧪 وضعیت کنترل کیفیت (QA Verification)
* تست بار (Stress Test) برای بررسی عدم افت فریم در مرورگر پس از باز بودن طولانی‌مدت صفحه انجام شد (موفقیت‌آمیز).
* تست‌های همزمانی برای ادغام داده‌های تضاددار در `sync.js` با موفقیت ۱۰۰٪ پاس شد.
