# Release Notes — v31.7.97

## عنوان ریلیز
**BUG-FISCAL-PROFIT-ICON-UX-001**

## RCA / دلیل دقیق
کارفرما گزارش داد که در هاب مالی، سود مدیریتی عملاً اختلاف خرید و فروش را محاسبه و فقط هزینه‌های جاری را کم می‌کند، در حالی‌که هزینه‌های مستقیم پروژه و هزینه‌های تنخواه نیز باید در سود قابل اتکا لحاظ شوند. همچنین بخش‌هایی با بک‌گراند روشن در گزارش رسمی مالی در حالت شب ناخوانا بودند و چند آیکون در وب‌سایت/CRM باید به فرم مینیمال و همخوان با دید شب اصلاح می‌شدند.

## اصلاحات سود مالی

### 1) هزینه‌های مستقیم پروژه
در `crm/fiscal.js` تابع‌های کمکی اضافه شدند:

```js
fiscalDirectProjectCosts(p)
findDealForFiscal(p)
```

اگر موتور سود اصلی به هر دلیل `projectCostIrr` را لحاظ نکرده باشد، fiscal خودش هزینه‌های مستقیم `costEvents` و `postArchiveCosts` را پیدا و از سود پروژه کسر می‌کند.

### 2) هزینه‌های تنخواه مستقل
تابع جدید:

```js
fiscalPettyStandalone(year)
```

هزینه‌های تنخواهی که به پرونده فروش لینک نشده‌اند، در سود خالص مدیریتی سال کسر می‌شوند.

### 3) فرمول سود خالص مدیریتی
فرمول اصلاح شد:

```text
سود خالص مدیریتی = سود پروژه‌های قابل اتکا - هزینه‌های جاری مستقل - هزینه‌های تنخواه مستقل + اسناد اصلاحی
```

### 4) نمایش و گزارش
- کارت «هزینه‌های تنخواه مستقل سال» اضافه شد.
- label سود به «سود خالص مدیریتی پس از هزینه‌ها» تغییر کرد.
- CSV و گزارش رسمی سال مالی اکنون هزینه مستقیم و تنخواه مستقل را نشان می‌دهند.

## اصلاحات Dark Mode / UI

### 1) گزارش رسمی مالی
باکس‌های روش محاسبه و کنترل کیفیت داده در `crm/working-capital.js` از رنگ‌های ثابت روشن به متغیرهای تم تبدیل شدند:

```text
var(--crd)
var(--tx)
var(--brd)
```

### 2) کارت‌های هاب مالی
اعداد بزرگ در کارت‌های `.sc` با `clamp`, `overflow-wrap:anywhere` و `word-break` بهتر نمایش داده می‌شوند.

## اصلاح آیکون‌ها
فقط آیکون‌ها اصلاح شدند:

- آیکون‌های هاب مالی به badge عددی مینیمال تبدیل شدند.
- آیکون حساب مشتریان در هاب مالی از emoji به `06 حساب مشتریان` تغییر کرد.
- آیکون مرکز دانش در صفحه مرکز دانش به badge مینیمال `KC` تبدیل شد.
- آیکون‌های cluster مرکز دانش عددی شدند.
- آیکون‌های بخش «دسترسی سریع پروژه‌ای» در فوتر وب‌سایت به badgeهای عددی مینیمال تبدیل شدند.
- آیکون ورود همکاران از emoji قفل به badge مینیمال `CRM` تبدیل شد.

## فایل‌های تغییرکرده

```text
crm/fiscal.js
crm/working-capital.js
crm/financehub.js
crm/index.html
crm/settings-accordion.js
knowledge-center/index.html
index.html
api/tools.php
tools/advanced-tools-ui.js
_tools/uat/tester275-fiscal-profit-and-minimal-icons.js
PTF-MASTER-HANDOVER.md
```

## تست
- تست جدید `tester275-fiscal-profit-and-minimal-icons.js` اضافه شد.
- تست runtime بررسی می‌کند که سود پروژه پس از هزینه مستقیم، هزینه جاری و تنخواه مستقل درست محاسبه شود.
- تست dark mode و آیکون‌های مینیمال نیز اضافه شد.
