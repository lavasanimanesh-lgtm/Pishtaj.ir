# یادداشت انتشار CRM v34.8.32

تاریخ: ۱۴۰۵/۰۶/۰۵ (۲۰۲۶-۰۸-۲۷)

## T3-1 مصرف عملیاتی — ماژول کالا از خواندن سرور-محور
اولین پنل فهرست‌محور روی `collection_query`:
- صفحهٔ سروری ۱۰۰تایی + جستجوی/مرتب‌سازی سروری + ناوبری صفحه
- **fallback کامل محلی**: هر خطای شبکه → toast + رندر محلی قدیمی
- **کلید بازگشت فوری**: `ptfProdServerToggle(false)` (فلگ per-settings، بدون ری‌دپلوی)
- پیش‌فرض روشن — مصرف LS صفحه‌به‌صفحه به‌جای کل دیتاست

### کنترل
tester534 (۹ قرارداد). گیت کامل در ادامه.
