# v34.39.27 — REALBUY-OPTIONAL: خرید واقعی پیش‌فرض فقط اطلاع‌رسانی (ضد دوباره‌شماری در حساب تأمین‌کننده) (۲۰۲۶-۰۹-۲۳)

> زمینه: خرید واقعیِ ثبت‌شده در مقایسهٔ قیمت (`buycompare`) به‌صورت خودکار فاکتور `PUR-` در حساب تأمین‌کننده می‌ساخت؛ وقتی کاربر فاکتور واقعی تأمین‌کننده را هم ثبت می‌کرد، **یک خرید دو بار** در گردش حساب تأمین‌کننده شمرده می‌شد و حذف `PUR-` توسط کاربر هم با ترمیم خودکار بازمی‌گشت.
> شاخهٔ مبدأ: `arena/01a0cb49-pishtaj-ir` (fast-forward روی `main`) + دو کامیت تکمیلی در PR #51.

## تغییر رفتار — خرید واقعی «اختیاری» در حساب تأمین‌کننده

- **پیش‌فرض جدید: `financeLinked=false`** — ثبت خرید واقعی (تکی و گروهی/bulk) فقط **اطلاع‌رسانی** است: قیمت قلم + تأمین‌کننده روی پروندهٔ فروش ثبت می‌شود و **هیچ اثری در حساب تأمین‌کننده ندارد**. فاکتور واقعی را کاربر خودش در حساب تأمین‌کننده ثبت می‌کند ⇒ منبع واحد، بدون دوباره‌شماری.
- گزارش‌های **gap / status / audit** خریدهای `financeLinked=false` را از شمول خارج می‌کنند (نه مغایرت، نه هشدار).
- **اتصال به فاکتور واقعی** (`ptfRealBuyLinkToRealInvoice` / `…Confirm` در `supplier-finance.js`): از دل مغایرت `realbuy-finance` می‌توان خرید واقعی را به فاکتور واقعیِ (غیر `PUR-`) همان تأمین‌کننده لینک کرد؛ `PUR-` خودکار (در صورت وجود) void می‌شود و `financeLinked=true` می‌گردد.
- **ترمیم خودکار محتاط‌تر**: `PUR-` که کاربر void/حذف کرده «عمدی» تلقی می‌شود و بازسازی نمی‌شود؛ خریدِ لینک‌شده به فاکتور واقعیِ فعال «جایگزین‌شده» است؛ پیش‌نمایش مودال به‌جای `confirm` ساده و گزارش کامل وقتی چیزی برای ترمیم نیست.
- **بستن دستی مغایرت** در گزارش کیفیت داده (`data-quality.js`): دکمهٔ «🙈 بستن دستی — مغایرت واقعی نیست» با ثبت دلیل (`ptfFinFindingDismiss`) + نوار شمارش موارد بسته‌شده + مودال مدیریت/بازگردانی (`ptfFinFindingDismissedOpen`). برای نوع `realbuy-finance` دکمهٔ «🔗 اتصال به فاکتور واقعی» هم کنار آن است.
- **بایگانی**: blocker بایگانی پرونده در `restore-contacts.js` رفع شد.

## رفع‌های تکمیلی (PR #51)

- **`crm/data-quality.js` — SyntaxError** در رشتهٔ `onclick` دکمهٔ بستن دستی: `\\'` (بک‌اسلش escape‌شده + پایان رشته) به‌جای `\'` نوشته شده بود و رشتهٔ JS زودتر بسته می‌شد ⇒ چون فایل با `<script defer>` بار می‌شود، **کل ماژول data-quality در مرورگر اجرا نمی‌شد**. یک بک‌اسلش اضافه حذف شد؛ خروجی نهایی `prompt('دلیل بستن این مغایرت:', 'مغایرت واقعی نیست — بررسی شد')` است.
- **ناهماهنگی نسخه (A6 نگهبان معماری)**: کامیت مبدأ فقط `sw.js`/`shell.js` و کش‌باسترهای `index.html` را به `34.39.27` برده بود و Quality Gate / Deploy قرمز می‌شد. همهٔ نقاط رسمی یکدست شدند (بخش «نگهداری»).

## نگهداری
- `VERSION.json` → v34.39.27 + همگام‌سازی همهٔ pinها (index.html `PTF_CRM_RELEASE`/sw.js `RELEASE`·`ASSET_VERSION`·`CACHE`/manifest.json/cms.js `PTF_CMS_JS_VER`/shell.js/clear-cache/device-reconnect)، `SD_SERVICE_VERSION` در `api/sales-domain.php`، و pin تسترها با `bump-version-pins.js` (۱۵۸ فایل، ۶۷۱ مورد؛ هویت `tester679-v34.39.24-…` محافظت‌شده).
- گیت‌ها: `node _tools/arch/arch-guard.js` → PASS؛ `node _tools/uat/run-ci-gate.js` → سبز (به‌جز تسترهای از-قبل‌قرمزِ وابسته به تاریخ/زمان که در یادداشت v34.39.24 هم ذکر شده‌اند).
