# بررسی ریسک از دست رفتن داده و فایل ضمیمه — CRM پیشرو تجهیز

تاریخ: ۲۰۲۶-۰۸-۱۲  
محدوده: `crm/` + `api/storage.php` + `api/crm.php` (مدل همگام‌سازی)  
روش: بازبینی ایستای مسیر آپلود، حذف، همگام‌سازی، بایگانی و پاک‌سازی ابری

---

## مدل فعلی (خلاصه)

CRM **فایل باینری را روی هاست نگه نمی‌دارد**. مسیر رسمی این است:

1. آپلود مستقیم مرورگر → آروان S3 (یا fallback `upload_proxy` از PHP به S3؛ فایل موقت PHP پاک می‌شود).
2. فقط **metadata** (`key`, `name`, `size`, `t`) داخل JSON رکورد در `localStorage` / سرور CRM ذخیره می‌شود.
3. همگام‌سازی دستگاه‌ها با `sync.js` روی همان JSON است، نه روی باکت.

بنابراین دو نوع «از دست رفتن» وجود دارد که نباید قاطی شوند:

| نوع | نشانه در UI | واقعیت روی دیسک/ابر |
|-----|-------------|---------------------|
| گم‌شدن **لینک/متادیتا** | ضمیمه در مودال نیست | فایل ممکن است هنوز در S3 باشد (یتیم) |
| گم‌شدن **خود فایل** | مشاهده → `file_not_found` / NoSuchKey | object از باکت حذف شده یا هرگز آپلود نشده |
| فایل هست ولی دیده نمی‌شود | مودال خالی / Save-As | معمولاً sync کهنه یا MIME (قبلاً اصلاح شده) |

---

## سطح ریسک

### بحرانی — امکان حذف دائمی فایل قانونی

**۱) بایگانی ZIP (`archive_zip` در `api/storage.php`)**  
بعد از آپلود zip، **همهٔ کلیدهای درخواست** از S3 حذف می‌شوند، حتی اگر بعضی در zip نیامده باشند (timeout دانلود، HTTP غیر ۲۰۰). شرط فعلی فقط این است که *حداقل یک* فایل به zip اضافه شده و خود zip آپلود شده باشد.  
نتیجه محتمل: بخشی از اسناد پرونده برای همیشه از بین می‌رود و zip ناقص است.

**۲) پاک‌سازی یتیم (`ptfPurgeCloudOrphans` در `crm/storage.js`)**  
کلیدهای فعال فقط از این storeها harvest می‌شوند:

`rfqs, projects, cms, letters, contracts, petty, petty_periods, offers, invoices, rfqsmart, deals, packinglists, inqreads, supplier_finance, sigprofiles`

**غایب‌های مهم** (ضمیمه واقعی دارند):

- `ptf_crm_cheques_issued` / `ptf_crm_cheques_received` / `ptf_crm_cheque_books`
- `ptf_crm_opex` و رویدادهای هزینه پرونده
- `ptf_crm_payables` (در صورت داشتن فایل)
- `ptf_crm_petty_tx`
- `ptf_crm_sales_returns` / فاکتور غیررسمی اگر جدا ذخیره شود

اگر ادمین «پاک‌سازی زباله ابری» را تأیید کند، اسناد چک/تنخواه-تراکنش/هزینه می‌توانند به‌عنوان یتیم **حذف دائم** شوند.

علاوه بر این، `list` سرور `max-keys=1000` است و pagination ندارد. اسکن ناقص است؛ هم یتیم واقعی جا می‌ماند، هم اعتماد کاذب به «فضا تمیز است» ایجاد می‌شود. هرس بک‌آپ داخل همین تابع فقط **۳ نسخهٔ آخر** را نگه می‌دارد.

**۳) `backup_prune`**  
هر چیزی زیر `backups/` جز دو کلید ثابت (`latest` / `monthly`) حذف می‌شود. تاریخچهٔ بک‌آپ ابری عملاً دو اسنپ‌شات است.

---

### بالا — دادهٔ کسب‌وکار بدون فایل، یا لینک شکسته

**۴) همگام‌سازی LWW + merge ناقص**  
`sync.js` برای تعارض «آخرین نویسنده» است، با لایهٔ `ptfSmartMerge`. موارد خوب:

- سپر push خالی روی کلیدهای حیاتی (US-382)
- tombstone حذف رکورد (`ptf_crm_deleted_archive`)
- union فایل‌ها با `_deletedFileKeys` (v34.4.34) برای `files[]`
- مسیر ویژه برای `supplier_finance` (object نه آرایه)، avatars، امضا، اعلان، fiscal

باقی‌ماندهٔ خطر:

- کلیدهایی که merge اختصاصی ندارند: اگر timestamp قدیمی‌تر باشد، **کل رکورد** جایگزین می‌شود؛ فیلدهای تودرتو غیر از `files` (مثلاً `docs` پروژه، پرداخت تو‌در‌توی فاکتور در مسیر غیرcanonical) ممکن است از نسخهٔ بازنده حذف شوند.
- `ptf_crm_sales_returns` در allowlist حسابدار هست ولی **در `SYNC_KEYS` نیست** → push/pull نمی‌شود؛ داده فقط محلی است و با تعویض دستگاه یا رفرش از سرور گم می‌شود.
- نقش `collector` چک‌بوک، تنخواه و تأمین‌کننده را sync نمی‌کند؛ روی دستگاه تحصیلدار این بخش‌ها کهنه یا خالی می‌ماند (از دست رفتن ظاهری، نه لزوماً سروری).

**۵) آپلود موفق S3 + ذخیرهٔ ناموفق metadata**  
اگر بعد از PUT، `setData` به‌خاطر سقف localStorage شکست بخورد (`storage-quota.js` write را drop می‌کند)، فایل در ابر هست ولی در CRM نیست. بعداً purge آن را یتیم می‌داند و پاک می‌کند.

برعکس: metadata ذخیره شود و PUT/proxy شکست بخورد → لینک مرده (`file_not_found`). کاربر باید دوباره آپلود کند؛ فایل از دست نرفته چون هرگز نرفته.

**۶) بستن فرم قبل از ذخیره**  
ویجت آپلود بلافاصله به S3 می‌نویسد؛ خیلی از فرم‌ها `files` را فقط موقع Save به رکورد می‌چسبانند. انصراف = فایل یتیم در ابر (فضا)، نه از دست رفتن سند ثبت‌شده. مسیر `ptfRemoveJustUploaded` این را بهتر کرده، به شرطی که کاربر صریحاً حذف بزند.

**۷) تبدیل به نسخه عملیاتی (`ptfConvertToProduction`)**  
با تأیید کلمهٔ «تایید»، کلیدهای اصلی را محلی `[]` می‌کند. اگر `_ptfGoLiveWipe` فعال باشد سپر داده‌صفر دور زده می‌شود و این خالی‌سازی به سرور هم می‌رود. عملیات عمدی است ولی باج‌افزار انسانی است: یک کلیک اشتباه روی محیط واقعی = پاک‌سازی مشتریان/استعلام/پیشنهاد.

---

### متوسط — از دست رفتن ظاهری یا کیفیت

**۸) مودال سند کهنه (رفع‌شده نسبی)**  
علت تاریخی: pull هر ۲۰ ثانیه + عدم رفرش پنل وقتی مودال باز است. `ptfSyncPullNow` / `ptfAttachRefreshOnOpen` برای تنخواه، چک و دفتر تأمین وصل شده. مودال‌های دیگر (پرونده فروش، نامه، قرارداد، opex) اگر همان الگو را نداشته باشند هنوز سند تازه‌ثبت‌شدهٔ دستگاه دیگر را نشان نمی‌دهند تا تب عوض شود. **فایل روی سرور هست.**

**۹) MIME / viewer (رفع‌شده در v34.4.36)**  
فایل‌های قدیمی بدون Content-Type در viewer باز نمی‌شدند. الان response override + `attachment-read.php` inline این را پوشش می‌دهد. از دست رفتن داده نبود.

**۱۰) فشرده‌سازی تصویر**  
JPG/PNG بزرگ به WebP حداکثر ۱۶۰۰px تبدیل می‌شود. اصل به S3 نمی‌رود. کیفیت/جزئیات کم می‌شود؛ برای اسکن مهر ریز مهم است.

**۱۱) سقف حجم و فرمت**  
۲۵MB کلی، PDF حداکثر ۵MB، فرمت غیر از تصویر/PDF رد می‌شود. آپلود fail می‌شود؛ داده قبلی دست نمی‌خورد.

**۱۲) list/usage ناقص و CORS**  
عدم pagination روی list یعنی ابزارهای ادمین تصویر کامل باکت ندارند.

**۱۳) ظرفیت مرورگر**  
سقف نرم ۵MB. compact خودکار audit/اعلان/آواتار را کوتاه می‌کند، نه مشتریان/فاکتور. اگر compact کافی نباشد، **write جدید ذخیره نمی‌شود** (دادهٔ تازه از دست می‌رود تا کاربر بک‌آپ/پاک‌سازی کند). سرور در این لحظه نسخهٔ قبلی را دارد اگر قبلاً push شده باشد.

---

## آنچه نسبتاً امن است

- حذف ابری قبل از پاک کردن metadata (`ptfDeleteStoredFile`) — دیگر UI موفقیت دروغین نمی‌دهد و فایل یتیم جعلی کمتر می‌سازد.
- عدم ذخیرهٔ دائم پیوست روی هاست (سیاست cloud-required).
- HEAD قبل از presign_get: 404 قطعی = `file_not_found`؛ 403 باعث رد اشتباه فایل موجود نمی‌شود.
- snapshot سه نسخه قبل از pull وقتی dirty هست (`ptf_pre_pull_snap_*`).
- بک‌آپ با کلید ثابت overwrite می‌شود (انباشت ۱۴۲ فایل بک‌آپ دیگر رخ نمی‌دهد) — در عوض تاریخچه کوتاه است.
- tombstone حذف رکورد جلوی زنده‌شدن رکورد پاک‌شده را می‌گیرد (ریسک معکوس: حذف اشتباه روی همهٔ دستگاه‌ها پخش می‌شود).

---

## احتمال عملی (تخمین مهندسی)

| سناریو | احتمال در بهره‌برداری عادی | شدت |
|--------|---------------------------|------|
| ادمین purge یتیم بزند و سند چک/opex پاک شود | متوسط اگر دکمه استفاده شود | فاجعه برای بایگانی مالی |
| archive_zip ناقص + حذف اصل | کم‌تکرار، شدت بالا | فاجعه پرونده |
| تعارض همزمان دو کاربر روی یک فاکتور/پرونده بدون merge تودرتو | متوسط در تیم چندنفره | از دست رفتن یک وصولی/رویداد |
| sales_returns فقط روی یک مرورگر | بالا اگر ماژول استفاده شود | دادهٔ محلی |
| مودال خالی به‌خاطر sync تأخیری | متوسط روی موبایل/تب پس‌زمینه | ظاهری |
| Quota → ذخیره نشدن تغییر تازه | متوسط وقتی CRM بزرگ شود | از دست رفتن کار ذخیره‌نشده |
| فایل واقعاً از S3 بدون اقدام ادمین «خودبه‌خود» پاک شود | پایین (باکت نسخه ندارد مگر در پنل آروان) | — |

نسخه‌بندی Object Lock / versioning روی باکت در کد فعال نیست؛ حذف S3 قطعی است مگر در خود آروان versioning روشن باشد (خارج از ریپو).

---

## سیاست کم‌حجم (v34.4.47، اعمال‌شده)

اصل: **کپی اضافه روی هاست/ابر نسازیم**؛ مسیر حذف اشتباه و sync ناقص را ببندیم.

- zip ناقص اصل را پاک نمی‌کند (فقط اگر همهٔ کلیدها داخل zip باشند حذف می‌شود).
- پاک‌سازی یتیم همهٔ `ptf_crm_*` را harvest می‌کند؛ اگر list ناقص باشد متوقف می‌شود.
- `list` صفحه‌بندی شد (بدون ذخیرهٔ فایل روی هاست).
- `ptf_crm_sales_returns` وارد SYNC و سپر داده‌صفر شد.
- بک‌آپ ابری همان ۲ کلید ثابت overwrite می‌ماند؛ versioning باکت روشن نشد.

## توصیهٔ اولویت‌دار (بدون اجرا در این بررسی)

1. **فوری:** در `archive_zip` فقط کلیدهایی حذف شوند که واقعاً داخل zip آمده‌اند؛ اگر `added < count(keys)` عملیات حذف اصل متوقف شود.
2. **فوری:** harvest یتیم را به همهٔ storeهای دارای `key`/`files`/`docs` گسترش دهید؛ حداقل چک‌ها، opex، petty_tx، payables، sales_returns. بهتر: اسکن همهٔ کلیدهای `ptf_crm_*` مثل خود harvest بازگشتی.
3. list را با continuation-token مثل `usage` صفحه‌بندی کنید.
4. `ptf_crm_sales_returns` را به `SYNC_KEYS` و سپر صفر اضافه کنید.
5. قبل از purge، گزارش «کلید یتیم پیشنهادی + از کدام prefix» را نشان دهید و حذف را دو مرحله‌ای کنید.
6. versioning باکت آروان را روشن کنید (۳۰ روز) تا purge/zip قابل برگشت باشد.
7. الگوی `ptfAttachRefreshOnOpen` را به پرونده فروش، نامه، قرارداد و opex وصل کنید.
8. دکمهٔ «تبدیل به عملیاتی» را روی محیط live قفل یا به wipe فقط محلی محدود کنید.

---

## جمع‌بندی یک‌خطی

خودِ آپلود به آروان نسبتاً مقاوم است و بسیاری از باگ‌های نمایش/حذف کاذب قبلاً وصله شده‌اند. **بزرگ‌ترین خطر واقعی از دست رفتن فایل، ابزارهای ادمین است** (پاک‌سازی یتیم با harvest ناقص، و حذف اصل بعد از zip ناقص)، نه خود فرم ثبت روزمره. **بزرگ‌ترین خطر از دست رفتن رکورد، sync نقش‌محور + کلیدهای خارج از `SYNC_KEYS` و تعارض LWW روی ساختارهای تودرتو است.**
