# Release Notes v34.7.24 — فاز F: تکمیل تصمیم‌یار و تحلیلگر

تاریخ: ۱۴۰۵/۰۵/۲۶ (2026-08-17)

## زمینه

هشت آیتم باقی‌ماندهٔ گزارش «بررسی عمیق تصمیم‌یار مدیریت» (نسخهٔ v34.7.17 فقط پنج مورد بحرانی آن را بسته بود) در این فاز تکمیل شد.

## AN-01 + AN-08 🟠 گزارش دوره‌ای واقعاً دوره‌ای شد

**ریشه:** `ptfManagementIntelligence()` هیچ فیلتر تاریخی نداشت؛ «گزارش هفتگی» و «گزارش ماهانه» هر دو یک snapshot مادام‌العمر چاپ می‌کردند و برچسب دوره هم میلادی بود.

**اصلاح:**
- امضای جدید `ptfManagementIntelligence({fromISO, toISO, basis})` — `basis` می‌تواند `issue` (تاریخ صدور سند) یا `close` (تاریخ برد/باخت) باشد. **بدون آرگومان، رفتار قبلی (کل تاریخچه) دقیقاً حفظ می‌شود** تا هیچ مصرف‌کنندهٔ فعلی نشکند.
- بازهٔ دوره‌ها شمسی است: هفته از **شنبه**، ماه از **اول ماه شمسی**.
- گزارش هفتگی/ماهانه با همان بازه ساخته و `periodFromISO/periodToISO/calendar` در رکورد گزارش ذخیره می‌شود؛ PDF هم بازه و مبنای تاریخ را چاپ می‌کند.
- انتخاب بازه در خود پنجرهٔ تصمیم‌یار: «کل تاریخچه | هفتهٔ جاری | ماه جاری».
- رکورد بدون تاریخ در گزارش دوره‌ای شمرده نمی‌شود (به‌جای اینکه بی‌صدا در همهٔ دوره‌ها بیفتد).

## AN-03 🟠 سنجهٔ سطح «فرصت»

واحد تحلیل تا امروز «سند» بود؛ چند پیشنهاد موازی برای یک استعلام مخرج را متورم می‌کرد. اکنون `PTF.metrics.opportunityStats` و کارت جدید در تحلیلگر: هر `inqNo` یک فرصت، برنده اگر حداقل یک سند برنده داشته باشد. سنجهٔ سند برای مقایسه و سازگاری حفظ شد — اختلاف این دو عدد یعنی برای بعضی استعلام‌ها بیش از یک پیشنهاد صادر شده است.

> در سناریوی آزمون: نرخ برد سند ۴۰٪ در برابر نرخ برد فرصت ۵۰٪.

## AN-02 🟠 هم‌ترازی تعریف نرخ برد در `insights.js`

این گزارش «پرونده‌های مختومهٔ بایگانی» را می‌شمارد، پس مخرجش ذاتاً «تعیین‌تکلیف‌شده‌ها» است — ولی بدون هیچ برچسبی «نرخ برد» نامیده می‌شد و کنار نرخ برد تحلیلگر دو عدد متفاوت با یک نام دیده می‌شد. اکنون: نام صریح («نرخ برد پرونده‌های مختومه»)، نمایش حجم نمونه، قاعدهٔ «زیر ۳ نمونه درصد نده»، و نمایش **نرخ برد سازمان** از لایهٔ مشترک در کنارش. فیلد `winRate` قدیمی برای سازگاری باقی است.

## AN-05 🟡 بازتعریف «پروندهٔ نیازمند بررسی»

پیش از این هر پروندهٔ بدون پیشنهاد برنده ریسک شمرده می‌شد و کارت قرمز عملاً شمارندهٔ پرونده‌های باز بود. اکنون سه دستهٔ معنادار با شمارش تفکیکی: **تأخیر تحویل | عدم انطباق QC | رکود (بدون رویداد در ۳۰ روز)**.

## AN-06 🟡 منبع «خرید واقعی»

به‌جای `Regex` روی متن آزاد یادداشت، از منبع ساختاریافتهٔ `ptf_crm_buycmp → purchases[]` خوانده می‌شود. یادداشت متنی فقط به‌عنوان fallback رکوردهای قدیمی باقی ماند و منبع شمارش (`purchaseSource`) در خروجی گزارش می‌شود.

## AN-07 🟡 گیت دفتر رسمی/غیررسمی روی snapshot AI

نقشی که اجازهٔ دفتر غیررسمی ندارد، دیگر ارقام غیررسمی را در تجمیع نمی‌بیند (همان قاعده‌ای که حساب مشتری از v33 اعمال می‌کرد). دامنهٔ دفتر (`ledgerScope`) در snapshot ارسالی به AI و در رکورد audit ثبت می‌شود.

## AN-04 🔷 شفاف‌سازی امتیاز سلامت (بدون تغییر وزن‌ها)

فرمول در خروجی (`healthModel`) و snapshot صریح گزارش می‌شود: پایهٔ ۵۰، وزن نرخ برد، وزن نرخ وصول، جریمهٔ کنترل و جریمهٔ پوشش پایین. **وزن‌ها عمداً تغییر نکردند** — کالیبراسیون واقعی نیازمند تصویب کارفرماست و در نقشه باز مانده است.

## تست رگرسیون

- `_tools/uat/tester427-v34.7.24-analytics-completion.js`: **39 PASS / 0 FAIL** — با اجرای واقعی موتور روی دادهٔ نمونه: فیلتر بازه (issue و close)، تفاوت نرخ برد سند/فرصت، سه‌گانهٔ ریسک، منبع خرید واقعی، و اثر گیت دفتر روی ارقام تجمیعی.
- تسترهای فازهای قبل: `tester426` ۴۲/۰، `tester424` ۳۱/۰، `tester420` ۳۰/۰ (بدون تغییر).
- گیت CI: **۴۲ PASS / ۲ FAIL** — همان دو شکست پیش‌ازموجود.
- سوئیت کامل (۴۱۰ تستر): **۶۲ شکست، صفر شکست جدید** نسبت به baseline فاز E.
- بهداشت نسخه در ۷ نقطه روی `v34.7.24`.

## گام بعد

فاز H (بدهی تست و patchهای CI) و سپس فاز G که **نیازمند تصویب مدل تجاری** است (فرمان `replace_award`، یکپارچه‌سازی مسیرهای نوشتن پول، تضمین snapshot سند برد).
