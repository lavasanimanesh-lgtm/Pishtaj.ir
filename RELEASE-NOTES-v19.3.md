# 🚀 ریلیزنوت v19.3 — اسپرینت ۴ از R12: «قفل ارجاع فاکتور + صدور فاکتور رسمی حسابدار»

**تاریخ:** ۱۴۰۵/۰۴/۲۱ | **شاخه:** واحد | **مبنا:** BACKLOG-SALESFILE-SOURCE-OF-TRUTH-R12.md — اسپرینت ۴ پیشنهادی

---

## 📦 محتوای اسپرینت (طبق قاعده ۱-۲ استوری هم‌خانواده)

| آیتم | شرح | وضعیت |
|---|---|---|
| **US-435** | قفل ارجاع فاکتور تا پس از «تحویل‌شده به کارفرما» — ارجاع فقط از داخل پرونده فروش | ✅ کامل |
| **US-436** | صدور فاکتور رسمی از کارتابل حسابدار (مبلغ + ارزش افزوده + شماره + تاریخ + PDF) و نشستن مستقیم در پرونده فروش + رفتار پیش‌پرداخت | ✅ کامل |

هم‌خانواده: «فاکتور رسمی و مطالبات» — گام چهارم رودمپ R12. زیرساخت هر دو در اسپرینت‌های قبل آماده شده بود (مرحله ۷ مشتق v19.2 + سند قطعی برد v19.1).

---

## 🔒 US-435 — قفل ارجاع فاکتور تا تحویل کارفرما

### رفتار
دکمه جدید در ردیف عملیات کشوی پرونده برنده:
- **قبل از تحویل کارفرما:** «🧾 ارجاع فاکتور 🔒» — کلیک = پیام شفاف با مرحله فعلی پرونده: «تا قبل از ثبت 🤝 تحویل کارفرما، ارجاع به حسابدار مجاز نیست».
- **پس از تحویل (مرحله ≥ ۷):** دکمه بنفش فعال «🧾 ارجاع فاکتور به حسابدار».
- **پس از ارجاع:** بج «🧾 ارجاع شد — در حال صدور فاکتور» و مرحله پرونده خودکار ۸ می‌شود (invRef سیگنال موتور مشتق US-433 است — بدون فیلد وضعیت جدید).
- **پس از ثبت فاکتور:** دکمه/بج حذف و چرخه مطالبات ادامه می‌یابد (مرحله ۹/۱۰/۱۱).

### قفل چندلایه (هسته قابل تست `sfInvoiceRefCommit` با خروجی {ok, why})
① فقط نقش ارشد ② فقط پرونده برنده ③ فقط مرحله ≥ ۷ ④ ضد تکرار (ارجاع مجدد رد) — مسیر قدیمی ارجاع از ماژول پیشنهادها همچنان مسدود است (US-431ف۱).

### AC3 — سند ضمیمه حسابدار
هنگام ارجاع، `sfAwardEnsure` صدا زده می‌شود و `invRef` با `fromFile` (کد پرونده) و `awardDoc` ثبت می‌شود ⟵ حسابدار **snapshot قطعی برد** را می‌بیند، نه پیشنهاد زندهٔ قابل‌تغییر.

اعلان فقط به حسابدار (الگوی محرمانگی US-141) + پیامک اختیاری (الگوی US-150) + timeline و audit در پرونده.

---

## 🧾 US-436 — صدور فاکتور رسمی از کارتابل حسابدار

### پنجره ثبت فاکتور (AC2 — ارتقای مودال موجود، نه ماژول موازی)
- شماره فاکتور حسابداری + **تاریخ فاکتور** + مبلغ بدون ارزش افزوده + **ارزش افزوده** (پیش‌فرض هوشمند ۱۰٪ — قابل اصلاح حسابدار) + جمع کل زنده + PDF فاکتور.
- اگر پیشنهاد پیش‌پرداخت ساختاریافته دارد، باکس راهنما رفتار مالی را از قبل به حسابدار می‌گوید.
- رکورد فاکتور حالا `base` / `vat` / `invDate` تفکیکی دارد؛ `amount` = جمع کل (سازگار کامل با مطالبات/سود/سال مالی موجود).

### نشستن در پرونده فروش (AC3/AC4)
PDF فاکتور و مشخصات آن مستقیم به `docs` پرونده فروش + خط timeline می‌رود؛ مرحله پرونده خودکار «۹ فاکتور صادر شد» می‌شود (سیگنال وجود فاکتور در sfStageOf — بدون کد وضعیت جدید).

### رفتار پیش‌پرداخت (AC5/AC6 — دقیقا متن بک‌لاگ)
| حالت پیش‌پرداخت (advance ساختاریافته v18.2) | نتیجه هنگام صدور فاکتور |
|---|---|
| پرداخت کامل/نقدی (cashFull) | فاکتور **تسویه‌شده کامل** ثبت می‌شود ⟵ مرحله ۱۱ (AC5) |
| درصدی/مبلغی **وصول‌شده** | مبلغ وصول‌شده خودکار به‌عنوان payment با برچسب «کسر پیش‌پرداخت» ثبت و **مانده به مطالبات** می‌رود ⟵ مرحله ۱۰ «در حال تسویه» (AC6) |
| بدون پیش‌پرداخت / وصول‌نشده | کل مبلغ به مطالبات باز می‌رود |

**تست عددی (کیس R8):** فاکتور ۱,۰۰۰,۰۰۰ + ارزش افزوده ۱۰۰,۰۰۰ = ۱,۱۰۰,۰۰۰ ت؛ پیش‌پرداخت وصول‌شده ۳۳۰,۰۰۰ ⟵ payment خودکار ۳۳۰,۰۰۰ و مانده مطالبات ۷۷۰,۰۰۰ ✅ (در تستر رفتاری اثبات شده).

### کارتابل حسابدار (AC1)
اگر ارجاع از پرونده آمده باشد، به‌جای «دانلود CO» دکمه **«🏆 سند قطعی برد (PDF)»** نمایش داده می‌شود — چاپ از snapshot تغییرناپذیر US-432.

---

## 🛡 اثبات شروط دائمی کارفرما

| شرط | اثبات |
|---|---|
| بدون وصله‌پینه | کارتابل/مطالبات موجود ارتقا یافت (ماژول موازی ساخته نشد)؛ مرحله ۸ و ۹ از همان سیگنال‌های مشتق US-433؛ هسته ارجاع جدا از UI |
| وابستگی‌ها | اتصال کامل به: مرحله ۷ (v19.2)، سند قطعی برد (v19.1)، پیش‌پرداخت ساختاریافته (v18.2 — ptfAdvanceNormalize)، مطالبات/savePay موجود، US-431ف۱ (مسیر قدیمی مسدود)، سال مالی (amount سازگار) |
| بدون باگ جدید | **۹۶ تستر / ۲۷۵۲ چک — صفر خطا** + سینتکس ۵۹ فایل JS + ۵ بلوک inline + audit PASS |
| بدون تداخل با طرح توسعه | دقیقا اسپرینت ۴ پیشنهادی R12؛ فقط US-437 از R12 مانده (اسپرینت ۵)؛ فیلدهای base/vat آماده گزارش رسمی سال مالی (US-426) |

## 🧪 تست
- **tester109** (جدید): ۴۲ چک — قفل سه‌لایه رفتاری (رد قبل از تحویل/نقش/تکرار، ارجاع موفق پس از تحویل، مرحله ۸ خودکار، notify فقط حسابدار) + سه سناریوی مالی صدور فاکتور (پیش‌پرداخت وصول‌شده/نقدی کامل/بدون پیش‌پرداخت) + رگرسیون v19.1/v19.2/مطالبات.
- کل مجموعه: **۹۶ تستر / ۲۷۵۲ چک سبز**.

## 📂 فایل‌های تغییر یافته
`crm/salesfiles.js` (sfInvoiceRefCommit/sfInvoiceRef + دکمه سه‌حالته کشو) | `crm/rbac.js` (مودال کامل فاکتور + invVatCalc + saveInv با VAT/تاریخ/پرونده/پیش‌پرداخت + دکمه سند قطعی برد کارتابل) | `crm/index.html` + `crm/sw.js` (v19.3) | `_tools/uat/tester109-v193.js` (جدید)

## 📤 آپلود
zip روی هاست روی نسخه موجود اکسترکت شود — **پوشه api حذف نشود**. بعد از آپدیت همه کاربران رفرش کنند (سایدبار: CRM v19.3).

---

## ⏭ صف بعدی (منتظر ابلاغ)
① **US-437 (اسپرینت ۵ R12 — پایان رودمپ):** مختومه‌سازی فقط پس از تحویل موفق + تسویه کامل + کنترل اسناد قبل از بایگانی ② US-426ف۲..429 (Post-R9) ③ US-423 (کارت ویزیت) ④ US-410/411 ⑤ US-405..408 ⑥ US-402/403

**شماره‌گذاری:** بعدی US-438 | باگ بعدی BUG-030
