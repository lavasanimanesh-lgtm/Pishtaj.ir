# ریلیزنوت v25.2 — کد ملی / شناسه ملی در ثبت دسته‌ای چک

## خواسته
امکان ورود کد ملی (اشخاص) یا شناسه ملی (شرکت‌ها) هنگام ثبت/چاپ دسته‌ای چک.

## Fix
- ستون **کد/شناسه ملی** در جدول «ثبت دسته‌ای + چاپ»
- ذخیره در `beneficiaryId` و چاپ روی برگه چک (همان فیلد قبلی فرم تکی)
- اعتبارسنجی نرم: ۱۰ یا ۱۱ رقم (در صورت پر بودن)
- نرمال‌سازی ارقام فارسی → انگلیسی

## یادآوری
- فرم ثبت تکی از قبل فیلد «کد ملی / شناسه ملی ذی‌نفع» داشت
- چاپ کاغذی از مختصات `nidTop/nidRight` در کالیبره استفاده می‌کند
