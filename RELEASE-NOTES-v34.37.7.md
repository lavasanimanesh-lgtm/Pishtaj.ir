# v34.37.7 — ایمنی دادهٔ مشتری و راه‌های تماس

تاریخ: 2026-09-06

## اصلاحات

- برای مجموعه‌های `ptf_crm_customers` و `ptf_crm_suppliers`، نبودن ردیف در snapshot خوانده‌شده به‌تنهایی دیگر `entity_delete` یا سنگ‌قبر نمی‌سازد؛ حذف فقط با `allowDelete: true` یا `userInitiated: true` مجاز است.
- مسیر ادغام عمدی مشتری (`custmerge`) با قصد حذف صریح علامت‌گذاری شد.
- fallbackهای legacy نیز از candidate محافظت‌شده استفاده می‌کنند تا snapshot ناقص در حالت فرمان خاموش، حذف محلی ایجاد نکند.
- ثبت/ویرایش مشتری و تامین‌کننده، نرمال‌سازی شماره را پیش از فرمان اصلی انجام می‌دهد و همان رکورد ذخیره‌شده را به hook برمی‌گرداند؛ حدس `items[0]` و فرمان رقابتی دوم حذف شد.
- migration قالب شماره‌ها و RFQ-heal به upsert رکوردی محدود شدند؛ projection محلی و همگام‌سازی، `people`، `mobs`، `tels` و شمارهٔ اصلی را حفظ می‌کنند.
- شکست quota یا صف پایدار، مقدار تازه را به snapshot تهی/کهن برنمی‌گرداند؛ مقدار volatile در cache نگه داشته و خطا به UI/sync اعلام می‌شود.

## آزمون

- `tester604-v34.37.7-customer-contact-safety.js`: ۱۴ سنجهٔ رفتاری برای ثبت، ویرایش، migration، رندر مجدد، pull و شکست quota ذخیرهٔ محلی.
- تست‌های regression مربوط به router، سایت/CRM مشتری و tombstone نیز سبز هستند.
- lint PHP در این محیط اجرا نشد چون `php-cli` نصب نیست؛ CI دارای گیت اجباری `php -l` است.
