# یادداشت انتشار — v34.37.7

**تاریخ:** ۲۰۲۶-۰۹-۰۶ (۱۴۰۵/۰۶/۱۵) · **پایه:** v34.37.6
**گیت CI:** tester604 (CONTACT-WIPE) + tester595 بند ۵.۴ + tester525 سناریو ۱

گزارش کارفرما:

> بررسی ماژول مشتریان CRM؛ باگ پنهانی که اطلاعات مشتری (تلفن‌ها، اشخاص رابط،
> کانال‌های تماس) را پاک می‌کند.

---

## ① زنجیرهٔ پاک‌شدن

چهار مسیر جدا، یک نتیجه: دفتر تماس مشتری پس از ذخیره یا بارگذاری دوباره خالی می‌شد.

**① روتر `ptfEntitySaveCollection`.** ذخیرهٔ یک‌رکوردی (قالب شماره، heal از درخواست،
ثبت از فرم پیشنهاد) کل آرایه را diff می‌کرد. اگر `getData` کهنه/ناقص بود، هر `cd`
غایب `entity_delete` واقعی می‌شد. سپر حذف انبوه فقط بالای ۳ رکورد را مسدود می‌کرد؛
۱ تا ۳ مشتری واقعاً از سرور پاک می‌شد و `ptfSilentWrite` همان آرایهٔ ناقص را روی کش
می‌نشاند.

**② مهاجرت یک‌بارهٔ `phonefmt.js`.** `migrateOnce` کل دفتر را از `getData` می‌خواند
و بازنویس می‌کرد. آرایهٔ خالی یا کوتاه‌تر از `_ptfEntityLastKnown` کل فهرست را با
مجموعهٔ ناقص جایگزین می‌کرد.

**③ هوک ذخیرهٔ `phonefmt`.** پس از `saveCust2`/`saveSup2` کل مجموعه قالب‌بندی می‌شد
و در مسیر کهنه به `items[0]` برمی‌گشت — مشتری تازه‌ثبت‌شده بدون `cd`، رکورد دیگری را
لمس می‌کرد.

**④ فرم دفترچه.** `cbCollect` فقط اشخاص نام‌دار را نگه می‌داشت؛ شخص بی‌نام با شماره
دور ریخته می‌شد. برای حقوقی، `saveCust2`/`saveSup2` کلید `phones: []` می‌فرستاد
(merge سرور کلید حاضر را بازنویسی می‌کند) و `coTels` را به تک‌فیلد فرم تقلیل می‌داد.

## ② اصلاح

1. **روتر — `AUTO_NO_DELETE_REASONS`:** برای `phonefmt` / `phonefmt-mig` /
   `rfq-cust-heal` / `offer-cust` / `offer-sup` / `saveCust` / `saveSup` /
   `site-rfq` / `excel-import` / `excel-std` / `vendorlist` رکورد غایب از `base`
   برمی‌گردد و `entity_delete` صادر نمی‌شود. حذف تک‌رکوردیِ واقعیِ کاربر (بدون این
   reasonها) دست‌نخورده است — قرارداد tester595 بند ۵.۴ و tester525 سناریو ۱.
2. **`phonefmt.migrateOnce`:** اگر آرایه تهی باشد یا کوتاه‌تر از lastKnown، مهاجرت
   آن کلید را نمی‌نویسد. هوک ذخیره فقط همان رکورد را با `ptfEntityUpsert` قالب
   می‌کند (`_ptfLastSavedCustCd` / `_ptfLastSavedSupCd`).
3. **`cbCollect`:** شخص با نام *یا* تلفن/موبایل/ایمیل حفظ می‌شود. `saveCust2` و
   `saveSup2` برای حقوقی `phones[]` خالی نمی‌فرستند و شماره‌های اضافهٔ `coTels`
   با `ptfMergeExtraCoTels` می‌مانند.

هیچ تغییری در سرور/مجوز/مدل داده نیست؛ merge سرور همان است — کلاینت دیگر کلید
تهی و حذف اشتباه نمی‌فرستد.

## ③ بازگشت‌پذیری / استقرار

فایل‌های تغییرکرده: `crm/sales-domain-v2.js`، `crm/phonefmt.js`، `crm/offers.js`،
`_tools/uat/tester604-v34.37.7-contact-wipe.js`، `run-ci-gate.js` (SUITE + SYNTAX
برای `phonefmt.js`)، `VERSION.json` + بامپ ۷ نقطه و پین‌های تسترها،
`RELEASE-NOTES-v34.37.7.md` (این فایل).

دیپلوی معمولی FTP؛ برای کاربر یک Reload کافی است (sw کشِ نسخه‌دار را می‌شکند).
اگر همان لحظه فهرست کهنه دیدید: Hard Reload (Ctrl+Shift+R).

## ④ اثبات

`_tools/uat/tester604-v34.37.7-contact-wipe.js` — سنجه‌های رفتاری با آزمون جهش:
- روتر: `reason=phonefmt` و cd غایب → صفر حذف + silent-write کامل؛ تهی‌شدن با
  `phonefmt-mig` حذف نمی‌سازد؛ بدون reason همان ۱ حذف tester595 ۵.۴ و سناریو
  tester525-۱ سبز می‌ماند؛ جهشِ برداشتن بلوک AUTO سنجه را قرمز می‌کند.
- phonefmt: مهاجرت روی آرایهٔ تهی/ناقص نوشته نمی‌شود؛ هوک فقط همان `cd` را upsert
  می‌کند نه `items[0]`.
- دفترچه: شخص بی‌نام با موبایل/تلفن/ایمیل حفظ می‌شود؛ `coTels` اضافه می‌ماند؛
  حقوقی `phones[]` خالی ندارد؛ جهشِ فیلتر نام‌محور سنجه را قرمز می‌کند.

**گیت کامل پس از بامپ پین‌ها اجرا می‌شود.**
