# 📦 ریلیزنوت v122.3 — اقدام فوری: ریشه‌کنی دکمه‌های قدیمی + بک‌آپ چرخشی کم‌حجم + یادآور ماهانه بک‌آپ

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v122.2 | **دامنه:** CRM (storage.js، modalx.js، backup.js، index.html) + API (crm.php، storage.php)

---

## 🔴🟡🟢 US-281 — دکمه‌های قدیمی مودال: این بار ریشه‌کن شد (عذرخواهی بابت v122.2)

در v122.2 دو نسل از دکمه‌های قدیمی حذف شد اما **نسل سوم** از قلم افتاده بود: یک `MutationObserver` در `storage.js` که روی هر پنجره، نوار «🗖 بزرگ‌نمایی / 💾✕ بستن با ذخیره / ✕ بستن» تزریق می‌کرد — همان دکمه‌هایی که در اسکرین‌شات شما بالای دکمه‌های مک دیده می‌شوند.

| اقدام | جزئیات |
|---|---|
| ❌ حذف تزریق نسل سوم | تزریق `🗖💾✕` از storage.js حذف شد؛ observer حالا اگر بقایای قدیمی ببیند **حذفشان می‌کند** |
| 🛡 دفاع سه‌لایه | modalx.js هم قبل از نصب دکمه‌های مک، هر `.ptf-win-ctrls`/`.md-ctrls` باقی‌مانده را پاک می‌کند + CSS آنها را `display:none` می‌کند |
| ➕ پوشش کامل‌تر | دکمه‌های مک حالا روی دیالوگ‌های سبک `ptfdlg` (ui-kit) هم نصب می‌شوند — قبلاً فقط `.md` را می‌گرفتند |

✅ AC: روی هیچ پنجره‌ای (md و ptfdlg) هیچ دکمه‌ای جز سه دایره مک دیده نمی‌شود. (نکته: عملکرد «بستن با ذخیره» از بین نرفته — دایره زرد همان کار را امن‌تر انجام می‌دهد: پنجره با تمام اطلاعات زنده به دیسک پایین می‌رود.)

## 🗄 US-282 — بک‌آپ چرخشی با حداقل فضای ابری (درخواست فوری کارفرما)

**مشکل قبلی:** آپلود ابری هر بار با کلید یکتا (`uniqid`) ساخته می‌شد → هر ساعت یک فایل جدید در آروان انباشته می‌شد و فضا را می‌خورد. روی سرور هم ۷ نسخه روزانه غیرفشرده نگه داشته می‌شد.

**طرح جدید (همه‌جا «جدید جایگزین قدیمی»):**

| مقصد | چه چیزی می‌ماند | مکانیزم |
|---|---|---|
| سرور هاست | `hourly-latest` (۱ فایل، همیشه جایگزین) + ۳ نسخه روزانه اخیر + `weekly-latest` + `monthly-latest` | حداکثر **۶ فایل فشرده** |
| فضای ابری آروان | فقط **۲ فایل ثابت**: `crm-backup-latest` + `crm-backup-monthly` | کلید ثابت S3 → هر PUT جایگزین قبلی؛ اکشن جدید `presign_put_backup` فقط همین ۲ نام را امضا می‌کند |
| هرس خودکار ابری | اکشن جدید `backup_prune`: هر فایل دیگری زیر `backups/` (انباشت آپلودهای قدیمی) حذف می‌شود | بعد از هر آپلود موفق اجرا می‌شود — فضای اشغال‌شده قبلی هم آزاد می‌شود |

- **فشرده‌سازی gzip:** حجم بک‌آپ JSON معمولاً ~۸۰-۹۰٪ کاهش می‌یابد (مثلاً 4MB → ~400KB). اگر هاست gzip نداشت، خودکار به JSON خام برمی‌گردد.
- **بازگردانی شفاف:** `get_backup` فایل‌های gz را خودش باز می‌کند — کلاینت و دیالوگ بازگردانی هیچ تغییری حس نمی‌کنند؛ نام‌های weekly/monthly هم به whitelist دانلود اضافه شد (همچنان فقط ادمین/رییس + ضد path traversal).

✅ AC: در ابری هیچ‌وقت بیش از ۲ فایل بک‌آپ نیست؛ روی سرور حداکثر ۶ فایل فشرده؛ فایل‌های انباشته قدیمی در اولین بک‌آپ بعد از آپدیت خودکار پاک می‌شوند.

## 📥 US-283 — یادآور ماهانه ذخیره فایل بک‌آپ (رییس هیات مدیره + ادمین)

چون سرور و ابری فقط «آخرین نسخه‌ها» را چرخشی نگه می‌دارند، نسخه ماهانه خارج از سامانه = بیمه نهایی داده‌ها:

- 🔔 **اعلان کارتابل خودکار:** اول هر ماه (اولین ورود در ماه جدید)، به **رییس هیات مدیره و ادمین** اعلان می‌رود: «فایل بک‌آپ این ماه را دانلود و خارج از سامانه نگه دارید» — با لینک مستقیم به تنظیمات. هر ماه فقط یک بار (فلگ در `ptf_crm_settings` که بین دستگاه‌ها سینک می‌شود).
- 📥 **دکمه جدید در تنظیمات:** «دانلود بک‌آپ ماهانه سرور» — نسخه `monthly-latest` سرور را دانلود می‌کند (gz خودکار باز می‌شود).
- ✅ **بستن خودکار وظیفه:** هر دانلود بک‌آپ (ماهانه یا دستی) وظیفه آن ماه را انجام‌شده ثبت می‌کند (+ ثبت در audit) → دیگر برای آن ماه یادآور تکرار نمی‌شود.
- 📝 متن راهنمای باکس بک‌آپ در تنظیمات با سیاست جدید به‌روز شد.

✅ AC: ورود ادمین/رییس در ماه جدید → یک اعلان در کارتابل هر دو نقش؛ پس از دانلود، اعلان ماه جاری دیگر ساخته نمی‌شود.

---

## 🧪 کیفیت

| سنجه | نتیجه |
|---|---|
| `_tools/audit.py` | ✅ PASS — ۰ خطا (۱ هشدار شناخته‌شده تصاویر) |
| `node --check` فایل‌های JS تغییریافته | ✅ |
| رگرسیون تسترها | ✅ همه PASS — tester8 با مشخصات جدید US-282 به‌روز شد، tester37 نسخه‌آزاد شد |
| تستر جدید `tester38-v1223.js` | ✅ ۲۹/۲۹ PASS |
| مجموع تسترها | **۲۵ فایل** |

## 📁 فایل‌های تغییریافته
`crm/storage.js` (حذف تزریق نسل سوم) · `crm/modalx.js` (پوشش ptfdlg + پاکسازی دفاعی) · `crm/backup.js` (یادآور ماهانه + دانلود ماهانه + متن‌ها) · `crm/index.html` (VER v122.3، CSS) · `crm/sw.js` · `api/crm.php` (save_backup چرخشی gzip + get_backup/list_backups) · `api/storage.php` (presign_put_backup + backup_prune) · `_tools/uat/tester38` (جدید)

## ⚠️ نکته استقرار
پس از آپلود، اولین بک‌آپ ساعتی خودش فایل‌های قدیمی انباشته (سرور و ابری) را هرس می‌کند — نیازی به اقدام دستی نیست. اگر خواستید فوری تمیز شود: تنظیمات → «🗄 بک‌آپ فوری».

## 📌 بک‌لاگ
اسپرینت ۱۲۳ (US-260 + 263 + 264) همچنان در نوبت — با «بریم بعدی» شروع می‌شود.
