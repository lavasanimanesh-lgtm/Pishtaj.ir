# یادداشت انتشار CRM v34.8.30

تاریخ: ۱۴۰۵/۰۶/۰۵ (۲۰۲۶-۰۸-۲۷)

## STALE-TOKEN-HEAL — پایان حلقهٔ 403 پنل ابزارها

**گزارش کارفرما:** `admin_list/admin_report_drafts/admin_feedback_list/admin_metrics_summary` همه 403.

### ریشه
این اکشن‌ها فقط برای admin/chairman هستند؛ 403 یعنی «توکن معتبر است ولی نقش توکن
ادمین/ریس نیست» — نقشِ لحظهٔ صدور توکن، نه نقش فعلی کاربر. اگر نقش کاربر بعد از
لاگین تغییر کرده باشد (یا دستگاه با اکانت دیگری لاگین بوده)، توکن کهنه این را
نشان نمی‌دهد و کاربر در حلقهٔ 403 گیر می‌کند.

### رفع (دو طرف)
- **سرور:** `tools_admin_require` نقشِ «فعلیِ» همان کاربر را از مخزن کاربران
  (users.json/crm_users.json/sync users) می‌خواند؛ اگر فعلاً ادمین/ریس است ولی
  توکن نقش دیگری دارد → **401/needLogin** (ورود مجدد توکن تازه می‌سازد) به‌جای 403.
  اگر واقعاً ادمین نیست → 403 عادی (درست).
- **کلاینت:** هر سه ماژول ابزار روی 401/needLogin توست «نشست منقضی/نقش تغییر کرده —
  دوباره وارد شوید» می‌دهند.

### کنترل
tester532 (۹ قرارداد). گیت کامل در ادامه.
