# 📦 ریلیزنوت v12.4 — بازشماری نسخه + داشبورد لانچر + کارآمدسازی «از درخواست»

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v123.2 (با شماره‌گذاری قدیم) ≡ **v12.3** جدید

---

## 🔢 US-301 — بازشماری نسخه (پیشنهاد کارفرما: تقسیم بر ۱۰)
شماره‌گذاری از این ریلیز: **v12.4** (یک رقم اعشار). نگاشت: `v123.2 قدیم ≡ v12.3 جدید` → این ریلیز v12.4. سایدبار هم (طبق US-297) همین ورژن واقعی را نشان می‌دهد: **CRM v12.4**. تسترها از قبل الگوی نسخه‌آزاد دارند و نشکستند.

## 🚀 US-302 — داشبورد = فضای دسترسی (نظر تیم متخصص نرم‌افزار)

**ارزیابی تیم:** داشبورد قبلی (۵ کاشی آماری + لاگ فعالیت) ارزش عملیاتی روزانه کم و نقش ناوبری صفر داشت؛ در اپ‌های حرفه‌ای صفحه اول = Launcher. **تایید و اجرا شد:**

- ماژول‌ها به‌صورت **کاشی‌های مربع گرافیکی** (آیکون + عنوان) در شبکه واکنش‌گرا
- **اولویت‌بندی با درگ:** کاشی را بکشید؛ ترتیب برای کاربر شما ذخیره و بین دستگاه‌ها سینک می‌شود (per-user در settings)
- درگ با Pointer Events — هم ماوس دسکتاپ هم لمس موبایل؛ کلیک ساده = ورود به ماژول (آستانه ۹px تفکیک کلیک/درگ)
- کاشی‌ها از سایدبار واقعی ساخته می‌شوند → **RBAC خودکار** (ماژول غیرمجاز، کاشی هم ندارد)
- داشبورد قدیم (آمار + فعالیت‌ها + ویجت یادآورها) حذف نشد: آمارها در بخش جمع‌شونده «📊 آمار و فعالیت‌های اخیر» و یادآورهای امروز همچنان بالای صفحه
- حالت شب و موبایل کامل

## 🗂 US-303 — کارآمدسازی دکمه «بارگذاری از درخواست» (یافته کارفرما — بررسی تیم)

**تشخیص تیم:** حق با شماست — مودال «استعلام جدید» فقط شرکت/مسئول/حوزه می‌گرفت و **اقلام هیچ‌وقت وارد نمی‌شد**؛ دکمه «از درخواست» در پیشنهادها عملاً به داده‌ای وصل نبود (زیرساخت اقلام از v99 وجود داشت ولی به جریان ثبت متصل نبود). **تایید و اجرا شد:**

1. **ثبت استعلام → پنجره اقلام خودکار باز می‌شود** با هر سه مسیر:
   - ✍️ دستی (افزودن ردیف)
   - 📎 اکسل (دکمه ورود فایل اقلام — موجود بود، حالا در جریان اصلی)
   - 🤖 دستیار (چیپ «دستیار» v12.3 روی همین پنجره فعال است — فایل PDF/اکسل استعلام را بخوانید و برگردید)
   - در مودال ثبت هم اطلاع‌رسانی شفاف اضافه شد که این پنجره خواهد آمد و چرا
2. **دکمه «از درخواست» هوشمند شد:**
   - `prompt` متنی قدیمی حذف شد → **دیالوگ انتخابی** با فهرست درخواست‌های دارای اقلام + تعداد قلم هرکدام؛ یک کلیک = ورود همه اقلام به فرم TO/CO/TC
   - اگر هنوز هیچ درخواستی اقلام ندارد → پیام راهنمای سه‌مسیره (به‌جای بن‌بست)
3. اتصال قبلی (انتخاب Inquiry No در فرم → بارگذاری همان درخواست) و هر سه منبع داده اقلام پابرجاست.

## 🧪 کیفیت
| سنجه | نتیجه |
|---|---|
| audit.py | ✅ ۰ خطا |
| تست عملکردی jsdom | لانچر: RBAC کاشی‌ها، ترتیب ذخیره‌شده، کلیک=ناوبری ✅ |
| تستر جدید `tester44-v124.js` | ✅ ۲۲/۲۲ PASS |
| رگرسیون کامل | ✅ **۳۱ تستر** همه PASS |

## 📁 فایل‌های تغییریافته
`crm/launcher.js` (جدید) · `crm/index.html` (VER v12.4 + مودال استعلام + saveRfq) · `crm/offers.js` (دیالوگ انتخاب درخواست) · `crm/sw.js` · `_tools/uat/tester44` (جدید)

## 📌 نکته
اگر ترتیب کاشی‌ها را شخصی‌سازی کردید و بعداً ماژول جدیدی اضافه شد، کاشی‌های جدید در انتهای شبکه ظاهر می‌شوند.
