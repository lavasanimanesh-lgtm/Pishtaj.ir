# گزارش رگرسیون و ممیزی نسخه v33.20.0 (آینهٔ خالدار — کلیدهای سنگین به IndexedDB)

**تاریخ ممیزی:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**نسخه هدف:** v33.20.0
**برنچ:** `arena/019fc272-pishtaj-ir`
**ابزارها:** `_tools/audit.py` + `_tools/build_sitemap.py` + ۲۹۴ تستر UAT (مقایسه با baseline واقعی main @ v33.18.0 روی worktree جدا)

---

## 📊 خلاصه

| بخش سنجش | وضعیت | نتیجه |
|---|:---:|---|
| **audit.py** | ✅ **۰ خطا** | همهٔ ۱۰ بررسی PASS |
| **build_sitemap.py** | ✅ هم‌گام | ۵۲۰ URL — بدون تغییر |
| **سینتکس JS** | ✅ سبز | node --check همهٔ crm/*.js + sw.js + tester299 |
| **tester299 (آینهٔ خالدار)** | ✅ سبز — **جدید** | **۱۴/۰** (گیت سه‌گانه، preload/migrate، فهرست+خودکار، mirror/read، get/setData سنگین، sync.js/theme.js، ری‌هیدریت) |
| **تسترهای همسایه** | ✅ سبز | tester298 = ۱۲/۰ · tester297 = ۱۹/۰ · tester184 = ۶/۰ · tester274 = ۱۰/۰ · tester287 = ۳۲/۰ |
| **کل مجموعهٔ UAT** | 🟡 ۲۳۸ سبز / ۵۶ باز | **صفر شکست جدید** |
| **جمع چک‌ها** | ۵۳۶۶ ✅ / ۸۴ 🟡 | دقیقاً برابر baseline (۸۴) — صفر شکست جدید در سطح چک هم |
| **بستهٔ نصب** | ✅ بازسازی | crm-update.zip (۵۵۸ ورودی — لیست ورودی‌ها یکسان) |

---

## 🔬 فرایند بازارزیابی (این نشست — ۲ پیمایش)

**پیمایش اول (پس از sync.js rd/wr):** ۲ فایل + ۱ چک تازه شکست خوردند:
- `tester184` و `tester274`: به شکل متن سورس قدیمی (overwrite عام + تابع slice بدون rd) پین بودند → تسترها به شکل مقاوم-به-indirection اصلاح شدند (شبیه الگوی tester287) — رفتار محصول درست بود.
- `tester70`: چک «merge تعارضی» رشتهٔ `ptfSmartMerge(k, localStorage.getItem(k), srvStr)` را پین داشت → چک به الگوی regex هر دو شکل (قدیمی/جدید) گسترش یافت.

**پیمایش دوم (نهایی):**
| مجموعه | فایل FAIL | چک FAIL |
|---|---:|---:|
| main @ v33.18.0 (baseline) | ۵۶ | ۸۴ |
| v33.20.0 (نهایی) | ۵۶ | ۸۴ |
| **جمع‌بندی** | **+۰** | **+۰** |

- PASS فایل‌ها: ۲۳۸ (۲۳۷ + tester299 جدید).
- ۵۶ تستر باز: بدهی قدیمی (v15–v22 + عصر auth) — مجموعهٔ نام‌ها با diff دقیق یکسان.

---

## 🚦 گیت‌ها
- ممیزی فنی ✅ · انتشار و سوابق ✅ · تست 🟡 (صفر شکست جدید)
- **گام بعدی:** مرج PR → Deploy to Production (confirm=DEPLOY, ref=main) → به کاربران اطلاع شود یک بار CRM را رفرش کنند تا مهاجرت بوت آینهٔ خالدار اجرا و localStorage پایدار سبک بماند.
