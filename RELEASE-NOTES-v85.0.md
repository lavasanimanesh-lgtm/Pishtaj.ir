# 🚀 ریلیز v85.0 — اسپرینت ۸۵ (بخش ۱): اصلاحات جریان ارجاع

**تاریخ:** ۱۴ تیر ۱۴۰۵ | **دامنه:** US-188 (دو اصلاح درخواستی شما)

---

## 📨 US-188 — اصلاحات ارجاع درخواست

| # | قبل | حالا |
|---|---|---|
| ۱ | کاربر می‌توانست به **خودش** ارجاع دهد (نام خودش در کشویی بود) | **نام کاربر جاری از کشویی «ارجاع به» حذف شد** + گارد دوم سمت ذخیره: حتی اگر UI دستکاری شود پیام «⛔ ارجاع به خودتان ممکن نیست» می‌آید. اگر هیچ کاربر دیگری با دسترسی فروش نباشد، پیام راهنما نمایش داده می‌شود |
| ۲ | تیک «📱 ارسال پیامک به گیرنده» **پیش‌فرض فعال** بود (مصرف ناخواسته اعتبار پیامک) | تیک **پیش‌فرض غیرفعال** است — فقط در صورت صلاحدید کاربر فعال می‌شود. منطق ارسال دست نخورده: با تیک، پیامک طبق قبل به موبایل گیرنده می‌رود |

## ✅ کیفیت

| مورد | نتیجه |
|---|---|
| تستر جدید `tester22-sprint85` (شامل تست رفتاری فیلتر با ۳ کاربر شبیه‌سازی‌شده) | ✅ **10/10 PASS** |
| رگرسیون ۲۱ تستر قبلی | ✅ همه PASS |
| ممیز ۸بخشی (۴۳۴ صفحه) | ✅ PASS |
| نسخه کش | `ptf-crm-v85.0` |

## 📦 نصب (File Manager)
**`pishtaj-release-v85.0-delta.zip`** — ۳ فایل: `crm/bridge.js`، `crm/index.html`، `crm/sw.js` (+ مستندات). **داده‌ای پاک نمی‌شود.**

> ℹ️ اگر v84.1 نصب نشده، ابتدا `pishtaj-hotfix-v84.1.zip` را باز کنید (این دلتا شامل offers-pro/letters نیست).

## 🔔 ادامه اسپرینت ۸۵
طبق رودمپ، بخش اصلی اسپرینت ۸۵ (**US-172 ایمپورت هوشمند اکسل سراسری + سئو موج۲**) در حال اجراست — با «بریم بعدی» تحویل می‌شود.

یادآوری‌های باز: ZipArchive هاست | Search Console (۱۱۱ URL) | مدارک US-160 (پروژه‌ها/لوگوها/eNAMAD).
