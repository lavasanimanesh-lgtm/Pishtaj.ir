# بررسی مستقل گزارش تلفیقی «تغییر دوطرفهٔ سند قطعی برد»

- **تاریخ:** ۱۴۰۵/۰۵/۲۶ (۲۰۲۶-۰۸-۱۷)
- **سند بررسی‌شده:** `ARENA-CONSOLIDATED-FINAL-AWARD-CHANGE-2026-08-17.md` (حاصل تلفیق کار سه ایجنت)
- **مبنای راستی‌آزمایی:** دو نقطه به‌طور جداگانه بررسی شد —
  - `origin/main` = `ef47908` (همان کامیتی که گزارش تلفیقی روی آن نوشته شده)
  - برنچ جاری = `42c0a68` (شامل اصلاحات v34.7.17/v34.7.18 که پس از آن کامیت انجام شده و با main همگام شده است)
- **وضعیت مخزن:** در جریان این بررسی **هیچ خطی از کد تغییر نکرد**.
- **محدودیت صریح:** در این محیط `php` نصب نیست؛ صحت نحوی/رفتاری PHP با اجرای واقعی سنجیده نشد. راستی‌آزمایی PHP از راه خواندن کد و بازتولید منطق در پورت JavaScript انجام شده است. دادهٔ واقعی پروداکشن هم در دسترس نبود.

---

## ۰) حکم کلی

گزارش تلفیقی **از نظر فنی قابل اتکاست**. از ۲۲ ادعای کلیدی که مستقلاً بررسی کردم:

| نتیجه | تعداد |
|---|---|
| ✅ تأیید کامل روی `ef47908` | ۱۸ |
| 🟡 درست ولی کم‌دقت در جزئیات (شماره خط/شمارش/شرط فعال‌شدن) | ۳ |
| 🔄 درست روی `ef47908` اما **روی برنچ جاری دیگر برقرار نیست** (قبلاً اصلاح شده) | ۴ مورد (هم‌پوشان با بالا) |
| ❌ ادعای نادرست | **۰** |

هیچ ادعای غلطی پیدا نکردم. اما **۴ یافتهٔ مهم را از قلم انداخته**، که دوتای آن‌ها از نظر ریسک داده **بالاتر از بیشتر موارد فهرست‌شده در خود گزارش** هستند (بخش ۳).

نکتهٔ حاکمیتی مهم: گزارش تلفیقی روی `ef47908` نوشته شده و **اصلاحات v34.7.17/18 برنچ جاری را نمی‌شناسد**؛ چند حکم «باز» آن روی برنچ جاری بسته شده است. اگر این سند به‌عنوان مبنای اقدام به تیم داده شود، باید همراه بخش ۴ همین گزارش خوانده شود، وگرنه تیم برای مشکلاتِ حل‌شده دوباره هزینه می‌کند.

---

## ۱) راستی‌آزمایی ادعا‌به‌ادعا (روی `ef47908`)

| # | ادعای گزارش تلفیقی | نتیجه | شاهد در کد |
|---|---|---|---|
| ۱ | هیچ فرمان سروری برای جایگزینی سند برد وجود ندارد | ✅ تأیید | `grep replace_award\|revise_case_contract\|void_unofficial_invoice` روی `api/sales-domain.php` = ۰ |
| ۲ | «۲۲ اکشن» در sales-domain | 🟡 کم‌دقت | ۲۷ نام اکشن یکتا (۲۲ شاخهٔ `elseif` + ۵ شاخهٔ زودهنگام). نتیجه‌گیری تغییر نمی‌کند |
| ۳ | قفل کلاینت `offerPostAwardLocked` با تطبیق `inqNo` | ✅ تأیید | `crm/offers.js:425-428` (گزارش: ۴۲۳-۴۲۷ — دریفت ۲ خطی) |
| ۴ | قفل سرور `won_offer_locked` گسترده‌تر از کلاینت | ✅ تأیید | `api/sales-domain.php:700` (گزارش: ۶۹۴) |
| ۵ | `revoke_orphan_delete` با `dependencies_exist` و نقش admin/chairman | ✅ تأیید | `:762` و `SD_OFFER_REPAIR_ROLES` در `:37`، اعمال در `:753` |
| ۶ | بازگردانی برد `priorStatus`/`revokedWinSnapshot`/correction دارد | ✅ تأیید | `:768` |
| ۷ | `wonRevisionSnapshot` سروری و قطعی است | ✅ تأیید | `:730` |
| ۸ | `awardDocs` در مسیر سروری ساخته نمی‌شود و lazy است | ✅ تأیید | رکورد case در `:745` بدون `awardDocs`؛ `sfAwardEnsure` در `crm/salesfiles.js:158-177` |
| ۹ | متمم فقط total را به `contractAmount` **جمع** می‌کند | ✅ تأیید | `:738-739` |
| ۱۰ | total منفی مکانیکاً ممکن است (بدون مدل تجاری) | ✅ تأیید | `sd_num` علامت را نگه می‌دارد (`:82-87`)، `sd_offer_total` جمع امضادار (`:152-156`)، `min="0"` در `crm/offers.js` = ۰ مورد |
| ۱۱ | FIFO: رسید `pre_invoice` فقط base می‌گیرد | ✅ روی main / 🔄 **روی برنچ جاری اصلاح شده** | main: `:214,224`؛ برنچ جاری: `$invoiceIssued` در `api/sales-domain.php:271,280` |
| ۱۲ | `correct_invoice` فقط `payments` را حفظ می‌کند و `pays` را نه | ✅ تأیید (و بدتر — بخش ۳/N2) | `$record['payments']=$oldInv['payments']??[]` |
| ۱۳ | ثبت غیررسمی سروری هست ولی ابطال سروری نیست | ✅ تأیید | `register_unofficial_invoice` در `:894`؛ TODO در `crm/unofficial-invoice.js:2139-2142` |
| ۱۴ | ناسازگاری schema در ابطال محلی (`invoiceCd/amount` در برابر `invoiceId/amountIRR`) | ✅ تأیید (و بدتر — بخش ۳/N1) | کلاینت `:2009-2010,2034`؛ سرور `:221,229` |
| ۱۵ | باگ P1 باز نشدن دیالوگ صدور غیررسمی از کشوی پرونده | ✅ تأیید | گیرنده `crm/unofficial-invoice.js:1285-1287` (`x._id \|\| x.cd`) در برابر فراخوان `crm/salesfiles.js:1186` (`r.cd`) |
| ۱۶ | شاخهٔ `existing` مبلغ/`linesSnapshot` را ذخیره نمی‌کند | ✅ تأیید (+ N4) | `crm/unofficial-invoice.js:1763-1769` |
| ۱۷ | فیلتر `superseded` در ماژول‌ها یکسان نیست (پورسانت/سرمایه در گردش/سال مالی/حساب مشتری) | ✅ روی main / 🔄 حساب مشتری روی برنچ جاری اصلاح شده | main: `customer-finance.js:9`, `commission.js:106`, `working-capital.js:58`, `fiscal.js:239`؛ برنچ جاری: `customer-finance.js` به `PTF.ar.activeInvoice` وصل شده، سه ماژول دیگر همچنان فقط `void` |
| ۱۸ | `fx.js` فیلتر غیررسمیِ دارای رسمی دارد ولی پورسانت ندارد | ✅ تأیید | `crm/fx.js:206-211` در برابر `crm/commission.js:106` |
| ۱۹ | مرجوعی فروش در ۴ موتور (سود/پورسانت/سرمایه در گردش/سال مالی) لحاظ نمی‌شود | ✅ تأیید | `ptf_crm_sales_returns` فقط در `customer-finance.js` و `unofficial-invoice.js` ارجاع دارد |
| ۲۰ | `contractAmount` هیچ مصرف‌کنندهٔ گزارشی ندارد | ✅ تأیید | خارج از `api/sales-domain.php` صفر ارجاع در js/php |
| ۲۱ | CO هم‌استعلامِ متصل‌نشده در اسناد پرونده دیده می‌شود؛ coverage خطی نمی‌گیرد؛ برنده‌کردنش پروندهٔ دوم می‌سازد | ✅ تأیید / 🟡 شرط فعال‌شدن ناگفته مانده | `salesfiles.js:78-106`؛ `allowedOffers` فقط از `linkedOffers+rootOfferId+wonOffer`؛ `win_offer:742-747` + `sd_case_offer_linked` — **شرط دقیق در بخش ۳/N6** |
| ۲۲ | delete plan فقط invoice/receipt/allocation/attachment را می‌بیند | ✅ تأیید | خروجی deps فقط `case,invoice,receipt,allocation` |
| ۲۳ | ناهماهنگی نسخه (UI `34.7.16`، health `34.6.0`) | ✅ روی main / 🔄 برنچ جاری روی `34.7.18` است ولی **health همچنان `34.6.0`** | `api/sales-domain.php:577,594,596` — هنوز باز |

---

## ۲) نکاتی که گزارش درست گفته و ارزش تأکید دارد

1. **حکم «قفل عمدی است، نه باگ»** درست است. تلاش برای «باز کردن ویرایش پیشنهاد برنده» به‌عنوان راه‌حل، مسیر غلط است — گارد سرور (`won_offer_locked`) هر payload متفاوتی را رد می‌کند و دستکاری localStorage در pull بعدی بازنویسی می‌شود.
2. **توصیهٔ ندادن راه‌حل «total منفی»** درست و مهم است.
3. **تفکیک `wonRevisionSnapshot` (قطعی) از `awardDocs` (نمایشی/lazy)** دقیق است و در گزارش‌های قبلی این تمایز مخدوش بود.
4. **انضباط معرفت‌شناختی در §۱۰.۲** (تفکیک «خود فیلد دوبار شمرده می‌شود» از «گزارش‌ها مغایرت می‌گیرند») نمونهٔ خوبی از گزارش‌نویسی مسئولانه است.

---

## ۳) یافته‌های تازه که در گزارش تلفیقی نیست

### N1 🔴 بحرانی — ابطال محلی فاکتور غیررسمی، `creditRemainIRR` **همهٔ پرونده‌های سیستم** را بازنویسی می‌کند

گزارش تلفیقی گفته «اعتبار مشتری به اندازهٔ کل رسید آزاد می‌شود». دامنهٔ واقعی بسیار بزرگ‌تر است.

```js
// crm/unofficial-invoice.js:2029-2046  (مرحله ۳.۲)
var _recs = getData('ptf_crm_case_receipts') || [];      // ← کل رسیدهای سیستم
var _stillAllocated = {};
(getData('ptf_crm_receipt_allocations') || []).forEach(function (a2) {
  if (a2.status !== 'reversed' && a2.receiptId)
    _stillAllocated[a2.receiptId] = (_stillAllocated[a2.receiptId] || 0) + (+a2.amount || 0);  // amount در v35 وجود ندارد → 0
});
_recs.forEach(function (r) {                              // ← بدون فیلتر caseId و بدون ربط به فاکتور ابطالی
  if (r && r.status === 'posted' && !r.voided) {
    var _newCredit = Math.max(0, (+r.amountIRR || +r.amt || 0) - (_stillAllocated[r._id || r.cd] || 0));
    if ((+r.creditRemainIRR || 0) !== _newCredit) { r.creditRemainIRR = _newCredit; _recChanged = true; }
  }
});
```

**پیامد:** یک بار اجرای `ptfUnofficialInvoiceVoid` روی **یک** فاکتور، برای **تمام رسیدهای posted تمام پرونده‌ها** مقدار `creditRemainIRR` را برابر کل مبلغ رسید می‌کند — چون `a2.amount` در schema سروری وجود ندارد و `_stillAllocated` همیشه صفر می‌ماند. یعنی خرابی «کل پایگاه داده» است، نه «همان پرونده».
**مصرف‌کنندهٔ مستقیم:** `crm/customer-finance.js:70-75` (`creditForCustomer`) → اعتبار همهٔ مشتریان متورم می‌شود.
**وضعیت روی برنچ جاری:** *نمایش* محافظت شده است (`PTF.ar` بستانکاری را محلی بازمحاسبه می‌کند و به `creditRemainIRR` ذخیره‌شده اتکا نمی‌کند)، اما **دادهٔ ذخیره‌شده همچنان خراب می‌شود** و تا اولین بازسازی سروریِ هر پرونده باقی می‌ماند و به دستگاه‌های دیگر sync می‌شود.
**حکم:** این تابع باید **غیرفعال یا پشت گارد** شود؛ اولویت آن از باگ P1 دیالوگ هم بالاتر است.

### N2 🔴 بحرانی — `correct_invoice` فیلدهای فاکتور را بی‌صدا حذف می‌کند

`$record` در `register_invoice/correct_invoice` از صفر و با فهرست ثابتی از کلیدها ساخته می‌شود؛ هر فیلدی خارج از این فهرست، در **هر اصلاح** حذف می‌شود:

| فیلد حذف‌شده | مصرف‌کننده | اثر |
|---|---|---|
| `pays` (وصولی‌های میراثی) | ۶۷ نقطه در کلاینت (`PTF.invPaidSum`, حساب مشتری, خزانه…) | **پول ثبت‌شده از مانده حذف می‌شود** و مطالبات باز افزایش می‌یابد |
| `dueISO` / `dueFa` | `crm/rbac.js` (سررسید وصول و «سررسید گذشته») | سررسید و هشدار معوق پاک می‌شود |
| `contactReq` / `contactApproved` | `crm/rbac.js` (گردش تأیید دسترسی تماس تحصیلدار) | تأیید دسترسی باید از نو گرفته شود |
| `offerCurrency` / `offerFxBasis` / `offerFxRateRef` | `crm/customer-finance.js:176-178` | کنترل تطابق فاکتور با پیشنهاد ارزی ضعیف می‌شود |
| `advApplied`, `bankAccount`, `discount*` | گزارش‌ها/چاپ | فرا‌دادهٔ سند از بین می‌رود |

`allocatedBase/allocatedVat/openAmountIRR` مشکلی ندارند چون بلافاصله با `sd_rebuild_allocations` بازتولید می‌شوند.
**اصلاح پیشنهادی (کم‌ریسک):** `$record = array_merge($oldInv, $record);` در شاخهٔ `correct_invoice` (به‌جای جایگزینی کامل) تا فقط فیلدهای صریحاً اصلاح‌شده بازنویسی شوند.

### N3 🟠 تعریف دوگانهٔ `offerPostAwardLocked`

این تابع **دو بار** در `crm/offers.js` تعریف شده: خط ۴۲۵ (سطح ماژول) و خط ۵۰۲ (داخل IIFE)، و هر دو `window.ptfOfferPostAwardLocked` را ست می‌کنند. بدنه‌ها فعلاً یکسان‌اند، پس رفتار امروز درست است؛ اما هر اصلاح آیندهٔ فقط‌یکی‌از‌آن‌ها به‌صورت خاموش بی‌اثر می‌شود (تعریف دوم برنده است). خطر نگهداشت، نه باگ فعلی.

### N4 🟠 نوشتن تکراری در شاخهٔ `existing`

علاوه بر ذخیره‌نشدن مبلغ (که گزارش دارد)، همان بلوک `setData('ptf_crm_invoices', invs)` را **دو بار پشت‌سرهم** صدا می‌زند (`crm/unofficial-invoice.js:1768-1769`) و یک no-op عمدی هم دارد (`_salesCase = _salesCase;`). نشانهٔ ویرایش ناتمام در همان ناحیه‌ای است که باگ اصلی را دارد؛ هنگام اصلاح باید کل بلوک بازنویسی شود.

### N5 🟠 شرط دقیق «پروندهٔ دوم» در گزارش نیامده

گزارش می‌گوید «اگر CO جایگزین را برنده کنید پروندهٔ دوم ساخته می‌شود». شرط دقیق این است:

```js
// crm/sales-domain-v2.js:127-131 — پیشنهادِ اتصال فقط وقتی ظاهر می‌شود که:
if (o.amendmentOf || o.isAmendment) { … confirm('… به پرونده متصل شود؟') … }
```

یعنی اگر CO **قبلاً با دکمهٔ ➕ به‌عنوان متمم علامت‌گذاری شده باشد** (`mark_amendment`)، کاربر دیالوگ اتصال می‌بیند و می‌تواند پروندهٔ دوم را عملاً منتفی کند (به قیمت جمع‌شدن مبلغ در `contractAmount`). اگر علامت‌گذاری نشده باشد، **هیچ هشداری داده نمی‌شود** و پروندهٔ دوم بی‌صدا ساخته می‌شود. این تفکیک برای روندکار عملیاتی حیاتی است و باید به سند اضافه شود.

### N6 🟡 دریفت شماره خط و شمارش

چند ارجاع گزارش ۲ تا ۶ خط جابه‌جاست (`offers.js:423` → ۴۲۵، `sales-domain.php:694` → ۷۰۰، `:728-730` → ۷۳۰، `:750-769` → ۷۵۳-۷۶۸) و «۲۲ اکشن» در واقع ۲۷ نام اکشن است. برای سندی که قرار است مبنای اقدام مهندسی باشد، بهتر است ارجاع‌ها با کامیت‌هش تثبیت شوند (`git show ef47908:path#Lxx`).

---

## ۴) وضعیت روی برنچ جاری (`42c0a68`) — چه چیزی دیگر باز نیست

| مورد گزارش | وضعیت روی برنچ جاری | مرجع |
|---|---|---|
| §۶.۲ رسید `pre_invoice` هرگز VAT نمی‌گیرد | ✅ **اصلاح شده** — مجوز VAT به‌ازای هر جفت (رسید، فاکتور) و بر مبنای صدور واقعی فاکتور | `api/sales-domain.php:271,280` |
| §۹ فاکتور `superseded` در حساب مشتری بدهی می‌سازد | ✅ **اصلاح شده** — تعریف واحد فعال‌بودن | `crm/customer-finance.js` → `PTF.ar.activeInvoice` |
| سقف تخصیص روی `base+vat` به‌جای `amount` | ✅ **اصلاح شده** | `sd_invoice_caps()` |
| فاکتور بدون `caseId` هرگز از دریافت پرونده کسر نمی‌شود | ✅ **اصلاح شده** — اتصال با تطبیق یکتای شمارهٔ پیشنهاد | `sd_bind_orphan_invoices()` + `PTF.ar` |
| نبود سنجهٔ تسویه | ✅ **اضافه شده** — `sd_reconcile_case()` + گزارش «🧮 تسویهٔ مطالبات» | `crm/ar-reconcile.js` |
| §۹ پورسانت / سرمایه در گردش / سال مالی و `superseded` | ❌ **همچنان باز** | `commission.js:106`, `working-capital.js:58`, `fiscal.js:239` |
| §۷ ابطال محلی غیررسمی (+ N1) | ❌ **همچنان باز** (نمایش محافظت شده، دادهٔ ذخیره‌شده نه) | `unofficial-invoice.js:2005-2047` |
| §۷.۴ باگ P1 دیالوگ صدور | ❌ **همچنان باز** | `unofficial-invoice.js:1285` |
| §۵ متمم کاهشی / §۱ فرمان جایگزینی سند برد | ❌ **همچنان باز** (نیازمند طراحی) | — |
| §۲۳ نسخهٔ health = `34.6.0` | ❌ **همچنان باز** | `api/sales-domain.php:577,594,596` |

---

## ۵) اولویت‌بندی پیشنهادی من (متفاوت با ترتیب گزارش)

گزارش تلفیقی «فرمان `replace_award`» را راه‌حل ریشه‌ای می‌داند — درست است، ولی آن یک پروژهٔ طراحی است. ترتیب اجرایی که پیشنهاد می‌کنم:

| اولویت | اقدام | اندازه | چرا اول |
|---|---|---|---|
| P0 | **گارد روی `ptfUnofficialInvoiceVoid`** (توقف اجرا + پیام «مسیر سروری در دست ساخت») | چند خط | تنها موردی که با یک کلیک، دادهٔ **کل پایگاه** را خراب می‌کند (N1) |
| P0 | **`array_merge($oldInv, $record)` در `correct_invoice`** | یک خط | جلوگیری از حذف بی‌صدای `pays`/سررسید/تماس/فرادادهٔ ارزی (N2) |
| P1 | **رفع تطبیق `_id/cd`** در `unofficial-invoice.js:1285` (پذیرش هر دو شناسه) | یک خط | قابلیت مسدود‌شده برای عملاً همهٔ پرونده‌های v35 |
| P1 | **هم‌ترازی فیلتر `superseded`** در پورسانت/سرمایه در گردش/سال مالی با `PTF.ar.activeInvoice` | کوچک | دوبارشماری پورسانت، اثر پولی مستقیم |
| P2 | رد `sd_offer_total() <= 0` در `mark_amendment`/`win_offer` | کوچک | بستن راه workaround ناایمن متمم منفی |
| P2 | هشدار «پروندهٔ دوم» در `ptfSalesWinOffer` وقتی پروندهٔ فعال با همان `inqNo`/مشتری وجود دارد | کوچک | جلوگیری از پروندهٔ موازی (N5) |
| P2 | یکسان‌سازی نسخهٔ `health` با `PTF_CRM_RELEASE` | یک خط | بهداشت نسخه |
| P3 | طراحی `replace_award / revise_case_contract` طبق بخش ۱۵ گزارش تلفیقی | پروژه | راه‌حل ریشه‌ای |

هر هفت مورد P0/P1/P2 **افزایشی و بدون تغییر معماری** هستند و با تستر موجود `tester421` و هارنس `repro-receivables-payment-integration.js` قابل پوشش‌اند.

---

## ۶) دربارهٔ آزمون‌های پذیرش گزارش

۲۴ سناریوی پیشنهادی پوشش خوبی دارند. سه سناریو را اضافه می‌کنم:

- **۲۵)** اجرای `ptfUnofficialInvoiceVoid` روی یک فاکتور نباید `creditRemainIRR` هیچ رسیدی در **پرونده‌های دیگر** را تغییر دهد (رگرسیون N1).
- **۲۶)** پس از `correct_invoice`، فیلدهای `pays`، `dueISO/dueFa`، `contactApproved` و فرادادهٔ ارزی باید دست‌نخورده بمانند (رگرسیون N2).
- **۲۷)** اگر CO هم‌استعلام **بدون** علامت متمم برنده شود، سیستم باید هشدار پروندهٔ موازی بدهد (رفتار مطلوب N5).

---

## ۷) جمع‌بندی

- گزارش تلفیقی **معتبر است و می‌تواند مبنای تصمیم قرار بگیرد**؛ صفر ادعای نادرست، سه کم‌دقتی جزئی.
- دو ریسک بزرگ‌تر از فهرست خودش را ندیده است: **خرابی سراسری `creditRemainIRR`** و **حذف بی‌صدای فیلدهای فاکتور در اصلاح**.
- چهار حکم «باز» آن روی برنچ جاری بسته شده؛ سند باید با بخش ۴ همین گزارش تکمیل شود تا کار دوباره انجام نشود.
- ترتیب اقدام باید از «فرمان ریشه‌ای» به «چهار اصلاح کوچک محافظتی» تغییر کند؛ با همان چهار مورد، بیشترین ریسک واقعی داده با کمترین تغییر معماری بسته می‌شود.

> این سند فقط ارزیابی است. اجرای هیچ‌کدام از اصلاحات پیشنهادی بدون تأیید کارفرما انجام نشده است.
