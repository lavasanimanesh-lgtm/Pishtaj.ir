# یادداشت انتشار CRM v34.8.22

تاریخ: ۱۴۰۵/۰۶/۰۵ (۲۰۲۶-۰۸-۲۷)

## W1 — مهاجرت فرمانی موج اول: مشتریان / تامین‌کنندگان / کالاها
(ROADMAP-THIN-CLIENT فاز T2 — تأیید کارفرما؛ نقش‌ها عین ماتریس legacy)

### هسته: روتر diff-محور مجموعه‌ای (`ptfEntitySaveCollection`)
به‌جای ویرایش ۴۹ نقطهٔ نوشتن پراکنده در ~۱۵ فایل، یک نقطهٔ عبور امن ساخته شد:
کلاینت آرایهٔ بعدی را می‌دهد؛ روتر نسبت به آخرین snapshot نوشته‌شدهٔ **همین کلاینت**
diff می‌گیرد: افزوده/ویرایش‌شده = `entity_upsert` اتمیک، cd غایب = `entity_delete`
(با tombstone آرشیو — بازیافت‌پذیر).

### گاردهای امنیتی روتر
- فرمان خاموش → setData (رفتار legacy؛ پرچم per-collection)
- رکورد بی‌cd → setData (فرمانی بی‌هویت ممنوع)
- عملیات > ۴۰ (مثل ایمپورت اکسل بزرگ) → setData (همان رفتار امروز)
- snapshot = نوشته‌های همین کلاینت ⇒ رکوردی که دستگاه دیگر با pull آورده هرگز
  اشتباهاً حذف نمی‌شود (سناریو ۵ تستر)

### نقاط متصل (۹ مسیر اصلی روزمره)
saveCust / saveSup / ثبت-ویرایش کالا / ایمپورت اکسل مشتری و تامین‌کننده /
تبدیل سرنخ→مشتری / ثبت مشتری و تامین‌کننده از پیشنهاد / مشتریِ ساخته‌شده از
درخواست سایت (×۲). نقاط فرعی (dedup، phonefmt، ai-workbench، supspec، …)
فعلاً legacy — نوبت迭代 بعدی W1.

### متفرقه
- سقف فیلد sanitizer این سه موجودیت: ۱۲۰ (رکوردهای پهن‌تر از ۴۰ فیلد)
- رفع بایپس خاموش‌نویسی inqreader (قانون A10) با helper مجاز `ptfSilentWrite`
  (بدون dirty/push — فقط ترفند نمایش کالاهای مخفی)

### کنترل
tester525 رفتاری ۲۶/۰ (۶ سناریوی روتر + قراردادهای اتصال + تطابق A11).
گیت کامل ۱۴۱/۰، arch-guard PASS.
