# یادداشت‌های انتشار — v34.8.46 (R6-ب)

**موضوع:** ورود دومرحله‌ای پیامکی برای نقش‌های مالی (T7) + پنل نشست‌های فعال با ابطال گروهی.
**تاریخ:** ۲۰۲۶-۰۸-۲۹ · **تستر قفل‌کننده:** `tester547-v34.8.46-r6b-2fa-sessions.js` (۷۱ سنجه · سبز) · **گیت:** run-ci-gate (۱۶۳ تستر) + arch-guard

## چه چیزی عوض شد

### ۱) ورود دومرحله‌ای پیامکی — نقش‌های مالی (api/crm.php)
- **محدوده:** نقش‌های `settings.twofa_roles` (پیش‌فرض: `accountant`) و تا وقتی `settings.twofa_enabled !== false`.
- **جریان:** `auth_login` پس از تأیید رمز، به‌جای توکن، `otp_required + otp_challenge + mobile_last4 + ttl:180` برمی‌گرداند و کد ۶رقمی با SMS (کاوه‌نگار) می‌فرستد؛ مرحلهٔ دوم `auth_login_otp` (public، POST-only) با کدِ درست → توکن نشست + کوکی‌ها (accountant 8h/else 24h + `ptf_token_flag`).
- **امنیت:** کد با `password_hash` ذخیره و با `password_verify` تأیید می‌شود (plaintext هرگز روی دیسک نیست)؛ challenge = hex32 تصادفی مقید به username با مقایسهٔ `hash_equals`؛ رکورد یک‌بارمصرف — پس از تأیید یا انقضا حذف.
- **سقف‌ها:** عمر کد ۱۸۰ث؛ حداکثر ۳ ارسال در پنجرهٔ ۱۰دقیقه (429 `twofa_rate_limited`)؛ ۵ تلاش غلط → قفل و حذف رکورد (429 `twofa_locked`).
- **سیاست fail-open با شفافیت:** SMS خاموش/نبود موبایل/شکست ارسال → ورود ادامه می‌یابد، رویداد در `auth_2fa_log.json` (۸۰ رویداد آخر) ثبت و پرچم `twofa_skipped` در پاسخ به کلاینت می‌آید (toast اطلاع‌رسان) — **مگر** `settings.twofa_required = true` که fail-closed می‌شود (503 `twofa_unavailable`/`twofa_sms_failed`).
- متن پیامک: «پیشرو تجهیز فرتاک — رمز ورود دومرحله‌ای: N — اعتبار: ۳ دقیقه».

### ۲) کلاینت 2FA (crm/index.html)
- `ptf2faContinue(u, resp)` بعد از **هر سه مسیر ورود** (سرور/مستقیم/اصلی): اگر `otp_required` → prompt کد ۶رقمی (تا ۳ تلاش) → POST `auth_login_otp` → توکن؛ لغوی کاربر = خروج با `twofa_cancelled` (بدون سکوت)؛ `twofa_skipped` → toast هشدار.

### ۳) نشست‌های فعال + ابطال گروهی (api/crm.php + crm/rbac.js + crm/index.html)
- `sessions_list` (verify_request + `role_guard('users_write')` → admin/chairman/ceo/commercial): فقط متادیتا {user, role, iat, exp, ip, current} — **مقدار توکن هرگز برگردانده نمی‌شود**؛ مرتب بر اساس آخرین ورود؛ ردیف منقضی فیلتر.
- `sessions_revoke` (POST-only): هدف `all` یا یک username؛ **توکن درخواست‌کننده همیشه زنده می‌ماند** (بدون لاک‌اوت خودی)؛ شمارش `revoked` + رویداد `sessions_revoked` در لاگ 2FA.
- پنل تنظیمات «🔐 نشست‌های فعال (R6)»: جدول با نشانگر 📍 برای همین تب، دکمهٔ «خروج» per-user (فقط برای نشست‌های دیگر) و «⛔ خروج اجباری همهٔ دستگاه‌ها (به‌جز همین تب)»؛ escape کامل HTML؛ گیت نقش ارشد.

## گیت‌ها
- `run-ci-gate`: ۱۶۳ تستر (tester547 جدید) — سبز؛ arch-guard بدون تخلف جدید.
- تغییرناپذیرها: قرارداد نشست v34.8.43 (توکن SS + کوکی HttpOnly) دست‌نخورده؛ idempotency/WAL؛ backup.js.

## تست دستی پیشنهادی روی استیجینگ
1. ورود با کاربر `accountant` (که موبایل `09xxxxxxxxx` در کاربران داشته باشد و SMS کاوه‌نگار فعال) → به‌جای ورود مستقیم، پیام کد + prompt مرورگر؛ کد درست → ورود کامل.
2. ورود با همان کاربر ولی SMS خاموش → ورود انجام می‌شود + toast «موقتاً اجرا نشد»؛ رویداد `skipped` در `crm/data/auth_2fa_log.json`.
3. ورود از دو مرورگر (یکی accountant) → تنظیمات با نقش ارشد → «نشست‌های فعال»: دو ردیف با 📍 روی همین تب؛ «خروج اجباری همه» → مرورگر دوم 401 می‌گیرد، همین تب زنده می‌ماند.
4. ۵ بار کد غلط → `twofa_locked`؛ بیشتر از ۳ ارسال در ۱۰ دقیقه → 429 `twofa_rate_limited`.

## قدم بعدی
**v34.9.0:** بستن باقی‌ماندهٔ A10 + اثبات‌های E1..E7 + چک‌لیست دیپلوی پروداکشن (کاربر خودش دیپلوی می‌کند) → سپس R4-گام۲ (حذف legacy، v34.9.1، پس از ≥۷ روز تله‌متری پروداکشن).
