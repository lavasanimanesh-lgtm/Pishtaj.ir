# RELEASE NOTES — v34.7.71 (۱۴۰۵/۰۵/۳۰ — 2026-08-21)

## دلیل ثبت/رد تامین‌کننده + تکمیل مدارک + پیامک

### ۱) دلیل اجباری برای ثبت و رد

- **تایید:** گفتگوی تایید با فیلد «دلیل / یادداشت تایید» (در سابقه و audit ثبت می‌شود).
- **رد:** گفتگوی رد با **نوع دلیل** ساختاریافته + توضیح:
  - عدم تطابق با نیازمندی‌ها
  - **نقصان مدارک** (امکان تکمیل و ثبت مجدد)
  - سایر

### ۲) تکمیل مدارک پس از رد

- اگر دلیل رد «نقصان مدارک» باشد، فیلد `reopen` روی رکورد ثبت می‌شود.
- صفحهٔ رهگیری (`/tracking/`) به تامین‌کنندهٔ ردشده پیام «مدارک نیازمند تکمیل» + دکمهٔ
  «↻ تکمیل مدارک و ثبت مجدد» (با کد رهگیری `?code=`) نشان می‌دهد.
- فرم ثبت‌نام (`/supplier/?code=…`) کد رهگیری را حفظ می‌کند و **سرور همان رکورد را باز
  می‌کند** (وضعیت → در انتظار بررسی مجدد، مدارک جدید جایگزین/پیوست می‌شود) — بدون ساخت
  رکورد تکراری و با همان شمارهٔ رهگیری.

### ۳) پیامک ثبت/رد

- در `set_status`، هنگام **تایید** یا **رد** تامین‌کننده، پیامک به شمارهٔ ثبت‌نام ارسال
  می‌شود (در صورت فعال بودن `sms-config.php`):
  - تایید: «درخواست ثبت‌نام تامین‌کنندگی شما تایید شد…»
  - رد نقصان مدارک: «مدارک شما ناقص است؛ لطفاً تکمیل و دوباره ثبت‌نام کنید…»
  - رد سایر: «درخواست شما در این مرحله پذیرفته نشد + توضیح…»

### فایل‌ها

`api/crm.php` (add_supplier resubmit + set_status دلیل/پیامک + track فیلدها) ·
`crm/bridge.js` (supApprove/supReject) · `tracking/index.html` · `supplier/index.html`.

### تست

- تستر جدید `tester473-v34.7.71-sup-reason-sms-resubmit.js` (در گیت CI).
- **گیت CI: ۸۹ PASS / ۰ FAIL** · نگهبان معماری: PASS.

### UAT پیشنهادی

1. رد با «نقصان مدارک» → در `/tracking/` دکمهٔ تکمیل مدارک دیده شود.
2. با `?code=` دوباره ثبت‌نام (با فایل جدید) → همان کد، وضعیت «در انتظار بررسی مجدد».
3. تایید → پیامک تایید به شماره ارسال شود (در صورت فعال بودن پیامک).
