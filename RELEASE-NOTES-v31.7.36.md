# Release Notes — v31.7.36

**تاریخ:** ۱۴۰۵/۰۴/۳۰ (2026-07-21)  
**نسخه:** `v31.7.36`  
**نوع:** 🟠 ADV-TOOLS-FREE-GATE-001 — قفل PDF رایگان + آماده‌سازی ابزار پیشرفته پولی

---

## 🎯 هدف ریلیز

اجرای اولین گام سبک از برنامه پولی‌سازی ابزارهای مهندسی، مطابق تصمیم کارفرما:

- ابزار رایگان فقط محاسبه اولیه بدهد.
- گزارش PDF کامل رایگان نباشد.
- اولین ابزار پیشرفته پولی آینده: Advanced Control Valve Sizing.
- پرداخت فاز اول: دستی / فاکتور / فعال‌سازی لایسنس.

---

## ✅ کارهای انجام‌شده

### 1) اصلاح متن صفحه ابزارها

متن قبلی صفحه ابزارها القا می‌کرد ابزارها رایگان و دارای خروجی PDF هستند. این اصلاح شد.

اکنون پیام صفحه:

```text
محاسبه اولیه رایگان است؛ گزارش PDF، نمودارهای حرفه‌ای و ابزارهای پیشرفته با فعال‌سازی پولی/سازمانی ارائه می‌شود.
```

---

### 2) قفل کردن PDF ابزارهای رایگان

در `tools/tools-ui.js` دکمه PDF از خروجی رایگان به paywall تبدیل شد.

رفتار جدید:

- محاسبه روی صفحه همچنان رایگان است.
- دکمه PDF بعد از محاسبه فعال می‌شود، اما PDF تولید نمی‌کند.
- کلیک روی PDF پنجره paywall باز می‌کند.
- paywall دو مسیر دارد:
  - درخواست فعال‌سازی از طریق RFQ
  - ارتباط واتساپی با واحد فروش

هیچ PDF رایگانی با `window.open`, `document.write` یا `window.print` تولید نمی‌شود.

---

### 3) اضافه شدن کارت ابزار پیشرفته Control Valve

در `tools/index.html` کارت جدید اضافه شد:

```text
Advanced Control Valve Sizing
```

امکانات معرفی‌شده:

```text
Liquid / Gas / Steam
Min / Normal / Max cases
Brand candidate
Charts
Paid PDF report
Manual license
```

این کارت فعلاً معرفی/CTA است و هنوز محاسبات پیشرفته server-side را اجرا نمی‌کند.

---

### 4) سند طراحی رسمی ابزار پیشرفته

سند زیر به پکیج اضافه شد:

```text
ADVANCED-CONTROL-VALVE-TOOLS-SPEC-v1.md
```

این سند معماری ابزار پیشرفته، مدل پرداخت hybrid، entitlement دستی، APIهای آینده، مدل داده، محاسبات، نمودارها، PDF report و مسیر پیاده‌سازی مرحله‌ای را مشخص می‌کند.

---

## 🧪 تست اضافه‌شده

### `_tools/uat/tester213-tools-free-pdf-paywall.js`

پوشش:

- صفحه ابزارها دیگر ادعای PDF رایگان ندارد.
- دکمه PDF به گزارش کامل قفل‌دار تغییر کرده است.
- `ptfToolsPaywall` وجود دارد.
- `exportPdf` دیگر `window.open`, `window.print`, `document.write` اجرا نمی‌کند.
- paywall مسیر RFQ و واتساپ دارد.
- کارت Advanced Control Valve وجود دارد.
- ابزارهای رایگان فعلی همچنان در UI و محاسبات حفظ شده‌اند.

نتیجه مستقیم:

```text
tester213-tools-free-pdf-paywall: 12 PASS / 0 FAIL
```

---

## ⚠️ Impact Analysis

| بخش | اثر | ریسک |
|---|---|---|
| ابزارهای رایگان | محاسبه باقی ماند؛ PDF رایگان قفل شد | کم |
| PDF/Print ابزارها | دیگر رایگان تولید نمی‌شود | متوسط تجاری، کم فنی |
| ابزار پیشرفته | فقط کارت معرفی اضافه شد | کم |
| پرداخت/درگاه | هنوز پیاده‌سازی نشده | صفر |
| API/CRM/Auth/Finance/Sync | دست‌نخورده | صفر |
| SEO/Content | فقط متن صفحه Tools و meta مرتبط تغییر کرد | کم |

---

## 📋 راستی‌آزمایی توسط کارفرما

1. صفحه `/tools/` را باز کنید.
2. یکی از ابزارهای رایگان مثل Cv یا وزن متریال را محاسبه کنید.
3. دکمه «گزارش PDF کامل 🔒» را بزنید.
4. انتظار صحیح:
   - PDF رایگان چاپ/باز نشود.
   - پنجره فعال‌سازی نمایش داده شود.
   - گزینه RFQ و واتساپ برای درخواست فعال‌سازی وجود داشته باشد.
5. کارت `Advanced Control Valve Sizing` در بالای ابزارها دیده شود.

---

## 🚦 نتیجه

پایه پولی‌سازی ابزارها بدون پیاده‌سازی سنگین و بدون ریسک پرداخت آنلاین ساخته شد. گام بعدی می‌تواند `Manual License v1` یا شروع ابزار پیشرفته Control Valve باشد.
