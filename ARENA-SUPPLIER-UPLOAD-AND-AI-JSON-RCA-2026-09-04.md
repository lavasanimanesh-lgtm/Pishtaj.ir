# ARENA — RCA: «فایلِ ثبت‌نام تامین‌کننده آپلود نمی‌شود» + «خروجی AI ساختار JSON معتبر ندارد»

**تاریخ:** ۲۰۲۶-۰۹-۰۴ · **نسخهٔ اصلاح:** v34.36.2 · **شاخه:** `arena/01a06c26-pishtaj-ir`
**یادداشت انتشار:** `RELEASE-NOTES-v34.36.2.md` · **تسترها:** `tester589` (۸۰/۰) و `tester590` (۸۴/۰) · **گیت CI:** سبز

---

## ۰) صورتِ پرونده

پرسش‌های کارفرما:

> ۱) «در صفحهٔ ثبت‌نام تامین‌کنندگان، چرا فایل آپلود نمی‌شود؟»
> ۲) «در مدیریت سایت چرا این پیام می‌آید وقتی می‌خواهم با هوش مصنوعی کاری انجام دهم: **خروجی AI ساختار JSON معتبر ندارد (پاسخ قابل تجزیه نبود)**»

نشانه‌های تأییدشده (پرسشِ مستقیم از کارفرما):
- پروندهٔ ۱: فرم **سبز** می‌شود و **هیچ هشداری** نمی‌دهد، ولی فایل در CRM نیست؛ حجم فایل **زیر ۲ مگابایت**.
- پروندهٔ ۲: پیامِ عمومیِ تجزیهٔ JSON، بدونِ هیچ علت یا راهِ بعدی.

دامنهٔ کار به درخواستِ کارفرما: **ریشه‌یابی و رفعِ ریشه‌ای هر دو مورد** (نه چسبِ UI).

---

## ۱) پروندهٔ اول — پیوستِ ثبت‌نام تامین‌کننده

### ۱.۱ چرا «سبزِ بی‌هشدار» فقط با یک مسیر ساخته می‌شود

`supplier/index.html` (پیش از اصلاح) پاسخ را این‌طور پردازش می‌کرد:

```js
if (data && data.ok && data.code) { … warning = data.warning || ''; … }
if (warning) { warningBox.textContent = '⚠️ ' + warning; warningBox.style.display = 'block'; }
```

یعنی **هشدار فقط وقتی ظاهر می‌شد که `save_attachment` مقدارِ `$error` را پر کرده باشد.** و در `api/crm.php` نسخهٔ `HEAD` نخستین شاخهٔ همان تابع این بود:

```php
function save_attachment($field, $prefix, &$error = null) {
    $error = '';
    if (empty($_FILES[$field]['name'])) return null;   /* ← بی‌صدا: بدونِ $error */
```

نتیجه: هرگاه فایل **به `$_FILES` سرور نمی‌رسید**، تابع `null` می‌داد با `$error = ''` ⇒ `add_supplier` رکورد را با `attachment = null` و **بدونِ `warning`** ذخیره می‌کرد ⇒ کاربر «ثبت‌نام موفقِ سبز» می‌دید و در CRM «بدون ضمیمه» بود. این **دقیقاً** همان نشانهٔ گزارش‌شده است و با «فایل زیر ۲ مگابایت» هم سازگار است (پس سقفِ حجم علتِ اصلی نیست).

چه چیزی باعث می‌شود فایل به `$_FILES` نرسد، بی‌آنکه PHP خطای آپلود بدهد؟

| علت | رفتارِ PHP | چرا بی‌صدا بود |
|-----|------------|----------------|
| بدنهٔ درخواست بزرگ‌تر از `post_max_size` | PHP **کل** `$_POST` و `$_FILES` را دور می‌ریزد (نه فقط فایل) | `$_FILES` خالی ⇒ شاخهٔ بی‌صدا. رکورد هم با فیلدهای خالی ساخته می‌شد |
| `file_uploads = Off` | `$_FILES` همیشه خالی | همان |
| پروکسی/CDN/WAF که بدنهٔ multipart را بازنویسی یا بخشِ فایل را حذف می‌کند | `Content-Type` دیگر `multipart/form-data` نیست یا فایل نمی‌رسد | همان |
| `max_file_uploads` / تعدادِ فیلدهای فایل | فایلِ اضافی حذف می‌شود | همان |

نکتهٔ عملیاتی مهم: **سقفِ برنامه ۱۵MB است ولی سقفِ واقعیِ میزبان می‌تواند بسیار کمتر باشد.** اگر `post_max_size` میزبان مثلاً ۸M باشد، یک کاتالوگِ ۹MB نه «خطای حجم» که «موفقیتِ بی‌صدا» می‌ساخت. از این نسخه، پیامِ خطا **عددِ واقعی** هر دو طرف را می‌گوید.

### ۱.۲ چرا «فایل آپلود شد ولی دیده نمی‌شود» هم ممکن بود

`get_inbox` تامین‌کنندگان سایت را **به همان ترتیبِ ذخیره** برمی‌گرداند (سرور `suppliers[] = $row` به انتها اضافه می‌کند ⇒ قدیمی‌ترین اول)، در حالی که بخشِ RFQ همان تابع `ORDER BY time DESC` دارد؛ `crm/bridge.js` هم فقط `slice(0, SITE_SUP_PAGE /* =50 */)` را نشان می‌داد. با انباشتِ ثبت‌نام‌ها، رکوردِ **همین لحظه** در انتهای فهرستی تا ۹۹۹ ردیف می‌افتاد و پیوستش دیده نمی‌شد. این حلقه، «آپلود نشد» را حتی در حالتِ موفق تولید می‌کرد.

### ۱.۳ چرا پیش‌تر راهی برای رساندنِ فایل نبود

ارسالِ دوبارهٔ همان شرکت/شماره به `duplicate` می‌خورد و مسیرِ «تکمیل مدارک» (v34.7.71) **فقط** برای رکوردِ `rejected` با `reopen` باز بود. یعنی اگر فایل بار اول نمی‌رسید، عملاً هرگز نمی‌رسید — مگر با دخالتِ دستی روی فایلِ داده.

### ۱.۴ زنجیرهٔ اصلاح (کدِ واقعی، با محل)

| # | محل | اصلاح |
|---|-----|-------|
| ۱ | `api/crm.php` — `ptf_ini_bytes` / `ptf_uploads_enabled` / `ptf_upload_error_fa` / `ptf_upload_limits_diag` | ابزارِ تشخیصِ محدودیت‌های میزبان (بدونِ هیچ دادهٔ حساس) + نگاشتِ هر هفت `UPLOAD_ERR_*` به پیامِ فارسیِ عملیاتی با عددِ واقعیِ `upload_max_filesize` |
| ۲ | `api/crm.php` — `save_attachment($field, $prefix, &$error = null, &$diag = null)` | شاخهٔ «`$_FILES` خالی» به **سلسله‌مراتبِ علت‌یابی** تبدیل شد: کدِ خطا ⇒ `file_uploads=Off` ⇒ نبودِ `multipart/form-data` ⇒ بدنه > `post_max_size` (با عددِ MB) ⇒ `$_POST` خالی ⇒ «فایل «X» انتخاب شده بود ولی در `$_FILES` خالی است». تفکیکِ «فایل اعلام‌شده» از «بی‌فایل» با `attachment_name`/`attachment_size` که کلاینت می‌فرستد. شکستِ فضای ابری هم `$diag` می‌گیرد. **پارامترِ چهارم اختیاری و قراردادِ بازگشت بدون تغییر** ⇒ سازگاری با همهٔ فراخوان‌های موجود |
| ۳ | `api/crm.php` — `add_supplier` | `$supDeclFile`؛ اگر فایل اعلام شده ولی نرسیده و علتی تولید نشده ⇒ `attachmentError = 'فایل انتخاب‌شده به سرور نرسید'` (پایانِ موفقیتِ بی‌صدا). **رسیدِ پیوست** در پاسخ؛ `attachmentError`/`attachmentDiag` **روی رکورد** (و در مسیرِ reopen به‌روز؛ با رسیدنِ پیوستِ واقعی پاک) |
| ۴ | `api/crm.php` — SUP-ATTACH-RECOVERY | شاخهٔ تازه **پیش از** `if ($dupFound)`: اگر (۱) فایلِ جدید سالم ذخیره شده، (۲) رکوردِ موجود `pending`/`rejected` و **بدونِ پیوست** باشد، (۳) `ptf_dedup_phone` شماره‌ها را یکی بداند ⇒ پیوست به همان رکورد اضافه می‌شود (`attachmentAddedAt` + رویدادِ `supplier_site`) و پاسخِ `attached:true` با کدِ قبلی. شرطِ (۲) عمدی و **fail-closed** است: جایگزینی/بازنویسیِ فایلِ موجود هرگز رخ نمی‌دهد؛ شرطِ (۳) یک اثباتِ هویتِ ساده در برابرِ سواستفاده از endpoint عمومی است |
| ۵ | `api/crm.php` — `ptf_rows_newest_first` + `get_inbox` | ترتیبِ **نزولیِ پایدار** روی `date` برای هر دو فهرستِ suppliers و rfqs؛ سقفِ ۹۹۹ بدون تغییر |
| ۶ | `crm/bridge.js` | مرتب‌سازیِ **کپیِ** آرایه (`all.slice().sort`) پیش از برشِ صفحه ⇒ حتی پاسخِ کش‌شدهٔ سرورِ قدیمی درست نمایش داده می‌شود؛ نشانِ «⚠️ پیوست نرسید» با `title` = علت؛ بلوکِ علت در مودال + `siteAttachmentDiagLine(d)` برای محدودیت‌های میزبان + راهنمای بازیابی. «بدون ضمیمه» فقط برای رکوردِ واقعاً بی‌فایل |
| ۷ | `supplier/index.html` | اعلامِ نام/حجمِ فایل در FormData؛ جعبهٔ رسیدِ پیوست `#venAttach` (نام/حجم/کلید یا علت)؛ کلیدِ «ارسال دوبارهٔ فایل» (`ptfSupplierRetryAttach`) با کدِ `#venCode`؛ پیش‌پروازِ نبودِ `FormData` (هشدارِ صریح به‌جای ارسالِ بی‌فایل). `#venWarning`/`#resBox`/`#sCode` و صفتِ `name="attachment" accept="…"` **کلمه‌به‌کلمه** دست‌نخورده |
| ۸ | `rfq/index.html` + `index.html` | افزودنِ `attachment_name`/`attachment_size` به FormData مسیرِ `add_rfq_site` (hard-503 از پیش موجود بود؛ حالا «میزبان فایل را خورد» از «کاربر فایلی انتخاب نکرد» تفکیک می‌شود) |

### ۱.۵ چگونه روی میزبانِ زنده تأیید شود

1. یک ثبت‌نامِ آزمایشی با فایل بزنید؛ اگر پیوست نرسید، در CRM روی همان ردیف «⚠️ پیوست نرسید» می‌بینید و در مودال **علت + محدودیت‌های میزبان** نوشته شده است (`file_uploads`, `upload_max_filesize`, `post_max_size`, `Content-Type`, `Content-Length`, شمارِ فیلدهای `$_POST`/`$_FILES`).
2. اگر `Content-Type` چیزی غیر از `multipart/form-data` بود ⇒ پای پروکسی/CDN/WAF در میان است.
3. اگر `Content-Length` از `post_max_size` بیشتر بود ⇒ PHP کل بدنه را دور ریخته؛ سقفِ میزبان را بالا ببرید یا از کاربر فایلِ کوچک‌تر بخواهید (پیامِ خطا همین را می‌گوید).
4. اگر `storageConfigReady: false` بود ⇒ `storage-config.php` یافت/معتبر نشده (پیوست به فضای ابری نمی‌رسد).
5. تامین‌کننده می‌تواند همان فایل را با «ارسال دوبارهٔ فایل» به **همان کدِ رهگیری** برساند؛ رکوردِ تکراری ساخته نمی‌شود.

---

## ۲) پروندهٔ دوم — «خروجی AI ساختار JSON معتبر ندارد»

### ۲.۱ زنجیرهٔ چهار حلقه‌ای

**C1 — سقفِ توکن از نیازِ prompt کمتر بود.** `crm/cms.js` برای اکشن‌های سئو فهرست‌های چندموردیِ فارسی می‌خواهد (و برای `seo_alt` تا **۸ تصویر**)؛ ولی در `api/llm.php` نسخهٔ `HEAD`:

| اکشن | فراخوان | سقفِ پیشین | سقفِ اکنون |
|------|---------|-----------|-----------|
| `seo_alt` (چندتصویر) | `llm_call` | ۹۰۰ | `llm_call_json` ۱۸۰۰ |
| `seo_intlinks` | `llm_call` | ۷۰۰ | `llm_call_json` ۱۴۰۰ |
| `seo_clusters` | `llm_call` | ۱۴۰۰ | `llm_call_json` ۲۴۰۰ |
| `seo_review` | `llm_call` | ۲۵۰۰ | `llm_call_json` ۳۶۰۰ |
| `seo_fix` | `llm_call` | ۲۰۰۰ | `llm_call_json` ۲۸۰۰ |
| `seo_expand` | `llm_call` | ۴۰۰۰ | `llm_call_json` ۶۰۰۰ |
| `seo_meta` / `seo_product` / `seo_article` | `llm_call_json` | ۹۰۰ / ۱۶۰۰ / ۴۰۰۰ | بدون تغییر |

پنج اکشنِ اول **هم** مسیرِ مقاوم نداشتند **هم** سقفِ تنگ ⇒ پاسخ وسطِ JSON بریده می‌شد.

**C2 — مدلِ استدلالی، بودجهٔ خروجی را صرفِ «فکر کردن» می‌کند.** `api/llm.php` برای Gemini فقط `temperature`/`maxOutputTokens`/`responseMimeType` می‌فرستاد و `thinkingConfig` نداشت. در مدل‌های استدلالی (2.5x) بخشی از همان `maxOutputTokens` صرفِ reasoningِ نادیدنی می‌شود ⇒ JSON ناتمام، حتی با سقفِ ظاهراً کافی.

**C3 — پاسخِ بریده هیچ نجاتی نداشت.** `llm_json_salvage` (v34.20.0) فقط سه لایه داشت: `json_decode` مستقیم، استخراجِ `{`…آخرین `}`، و حذفِ کاماهای انتهایی. نه حصارِ markdown/پیشوندِ توضیحی را می‌زد و **نه JSONِ ناتمام را تعمیر می‌کرد** ⇒ پیشوندِ کاملاً قابلِ استفادهٔ `[{"a":"x"},{"b":"y"` دور ریخته می‌شد.

**C4 — نبودِ فارِنزیک.** `finish_reason` خوانده نمی‌شد و بدنهٔ خام ثبت نمی‌شد ⇒ «برش به‌سببِ سقفِ توکن» از «خروجیِ آزاد» تفکیک‌ناپذیر بود؛ کاربر یک پیامِ کلی می‌دید و پشتیبانی هیچ شاهدی نداشت.

### ۲.۲ سیاستِ تعمیر — و چرا این‌گونه

نجاتِ JSONِ بریده یک تصمیمِ **سیاستی** است، نه فقط فنی؛ سه قاعده گذاشته شد:

1. **بیشینه‌محتوا:** گامِ نخست «بستن در همان نقطهٔ بریدگی» است (`llm_json_close_tail` با پشتهٔ براکت‌ها و بستنِ رشتهٔ باز) تا هیچ مقدارِ کاملی دور ریخته نشود؛ اگر نتیجه معتبر نشد، پله‌پله **یک عضو** از انتها پوست کنده می‌شود (`llm_json_peel`، سطحِ ۰ = کمترین دست‌کاری، تا حداکثر ۴ پله).
2. **بدونِ داده‌سازی:** اگر دمِ بریده `:"` باشد، بستن در همان‌جا `"x":""` می‌سازد — مقداری **تهی** که UI همان را چاپ می‌کند و مدیر فکر می‌کند AI «متن خالی» تولید کرده. پس گامِ نخست با گاردِ `preg_match($tailBad, $s) !== 1` رد می‌شود و سطحِ ۰ هم در همان حالت پذیرفته نمی‌شود ⇒ به آخرین **عضوِ کامل** برمی‌گردیم.
3. **بدونِ موفقیتِ پوچ:** `llm_json_meaningful($d)` نتیجهٔ تعمیرشده را می‌سنجد — آرایه/جسمِ خالی، یا اعضایی که همه تهی‌اند، «موفقیت» حساب نمی‌شوند. نمونه: `[{"x":` ⇒ `[{}]` تجزیه می‌شود ولی **داده‌سازی** است ⇒ `null` و خطای صادقانه. این قاعده در دو نقطه (`if (llm_json_meaningful($d)) return $d;`) اعمال می‌شود.

همراه با این‌ها:
- `llm_json_scan` **رشته-آگاه** است (حالتِ `inStr` و گریز `\`) ⇒ `}` یا `{` داخلِ مقدارِ فارسی گول نمی‌زند.
- سقفِ تلاش در همهٔ حلقه‌ها محدود است (`$tries < 60`، `$lvl <= 4`) ⇒ بدونِ حلقهٔ بی‌پایان.
- `llm_json_salvage($text, &$how)` **برچسبِ لایهٔ نجات‌دهنده** را برمی‌گرداند (`direct` / `extract` / `trailing-comma` / `fences` / `repaired-truncated` و ترکیب‌ها) و تا پاسخِ نهایی (`jsonSalvage`) می‌رود ⇒ بعداً می‌توان سنجید کدام لایه چقدر کار می‌کند.
- تلاشِ مجددِ `llm_call_json` همان **یک** نوبت است، ولی سقفِ توکنش clamp شد: `min(8000, max(1200, $maxTok * 2))` — دو برابرِ ۶۰۰۰ می‌شد ۱۲۰۰۰ و در برخی endpointهای OpenAI-compatible با HTTP 400 برمی‌گشت.
- `finish` از هر دو provider خوانده می‌شود (`candidates[0].finishReason` و `choices[0].finish_reason`) و در نتیجهٔ موفق هم نگه داشته می‌شود (تا در کش بماند).
- `out_json` پیامِ **قابلِ اقدام** می‌دهد ولی `errorBase` = همان عبارتِ pin‌شدهٔ قدیمی است (سازگاری با تسترها/مستندات)؛ `truncated`/`finish`/`model`/`raw` (۳۰۰ کاراکتر) اضافه شدند.
- `thinkingConfig` **opt-in** است: فقط اگر مالک در `llm-config.php` کلیدِ `gemini_thinking_budget` را عدد بگذارد ارسال می‌شود (`llm-config.sample.php` مستند شد). پیش‌فرض = رفتارِ فعلی بدونِ فیلد ⇒ صفر ریسکِ regression روی مدل‌های قدیمی.
- `crm/cms.js`: پوششِ `cmsAiDiag` در خودِ `cmsLLM` نشست تا **همهٔ** فراخوان‌ها (بدونِ تغییرِ تک‌تکِ مسیرها) `window.__ptfAiLast` + `console.error` با نمونهٔ خام + راهنمای عملی بگیرند.

### ۲.۳ چه چیزی عمداً تغییر نکرد

- **۱۶ فراخوانیِ `llm_call` تک‌ضربه** دست‌نخورده ماند (OCR/چک/کارت ویزیت/صورت‌حساب/RFQ، ترجمه، شناسایی) — قراردادِ آن‌ها JSONِ چندعضویِ فارسی نیست و pin تستر ۱۴۰ هم روی مسیرِ OCR است. شمارِ ۱۶ در `tester590:۴.۹` **قفل** شد تا مهاجرتِ ناخواستهٔ آینده داد بزند.
- نقش‌های مجازِ اکشن‌های سئو (`admin/chairman/ceo/commercial`)، حالتِ اجباریِ JSON در هر دو provider (`responseMimeType` / `response_format`) و سقفِ ۹ تصویرِ `seo_alt` بدون تغییر.
- سه لایهٔ قدیمیِ salvage **کلمه‌به‌کلمه** باقی ماندند (لایه‌های جدید به انتها اضافه شدند) ⇒ رفتارِ پاسخ‌های سالمِ قدیمی دقیقاً همان است.

---

## ۳) ریسک‌ها، رد شده‌ها، و دنبال‌شده‌ها

**رد شده (با دلیل):**
- *«فقط پیام را دوستانه‌تر کنیم»:* نشانهٔ کارفرما «سبزِ بی‌هشدار» بود؛ بدونِ تفکیکِ «فایل نرسید» از «فایل انتخاب نشد» همان بی‌صداایی می‌ماند.
- *«سقفِ ۱۵MB را کم کنیم»:* فایلِ گزارش‌شده زیر ۲MB بود؛ سقفِ برنامه علت نبود. به‌جایش پیامِ خطا عددِ واقعیِ میزبان را می‌گوید.
- *«retry خودکارِ آپلود در مرورگر»:* اگر علت `post_max_size`/WAF باشد، تلاشِ دوباره همان شکست را تکرار می‌کند و کاربر را در حلقه می‌برد؛ به‌جایش **کلیدِ صریحِ** «ارسال دوبارهٔ فایل» با رسیدِ نام/حجم گذاشته شد.
- *«تعمیرِ JSON با پرکردنِ مقادیرِ حدسی»:* داده‌سازی است؛ قاعدهٔ `meaningful` دقیقاً برای جلوگیری از همین گذاشته شد.
- *«`thinkingBudget = 0` به‌صورت پیش‌فرض»:* رفتارِ تولیدِ همهٔ مدل‌ها را عوض می‌کرد ⇒ opt-in شد.

**دنبال‌شده‌های پیشنهادی (خارج از دامنهٔ این انتشار، بدونِ کد):**
1. **پایشِ برچسبِ `jsonSalvage`:** اگر سهمِ `repaired-truncated` بالا ماند، یعنی سقفِ توکنِ همان اکشن هنوز تنگ است (نشانهٔ کمی برای تنظیم بعدی).
2. **آپلودِ مستقیم به فضای ابری** (presigned URL) برای پیوست‌های بزرگ تا `post_max_size` میزبان دیگر گلوگاه نباشد.
3. **`error_log` سمت سرور برای مسیرِ RFQ:** در مسیرِ تامین‌کننده علت روی رکورد می‌ماند، ولی RFQ با 503 برمی‌گردد و رکوردی ساخته نمی‌شود ⇒ هیچ ردِ پایدارِ سمت سرور ندارد.
4. **صفحهٔ «سلامتِ آپلود میزبان»** که همان `ptf_upload_limits_diag` را یک‌جا به مدیر نشان دهد (اکنون در هر رکوردِ خطادار دیده می‌شود).

---

## ۴) اثباتِ آزمون

- **`tester589-v34.36.2-supplier-upload-receipt.js` — ۸۰ PASS / ۰ FAIL:** اسکریپتِ درونِ `supplier/index.html` در sandboxِ `vm` **اجرا** می‌شود (رسیدِ پیوست، اعلامِ نام/حجم، مسیرِ تلاشِ دوباره)؛ سلسله‌مراتبِ علت‌یابیِ «فایل اعلام‌شده ولی نرسیده»؛ نگاشتِ هر هفت `UPLOAD_ERR_*`؛ SUP-ATTACH-RECOVERY با شرطِ fail-closed؛ newest-first در سرور و کلاینت (سنجهٔ «رکوردِ تازه از سقفِ صفحه بیرون نمی‌ماند»); pinهای سازگاری (`name="attachment" accept="…"`, `SITE_SUP_PAGE`, `siteAttachmentHtml`, «بدون ضمیمه»); نبودِ نشتِ کلید/رمز در پیام‌های خطا.
- **`tester590-v34.36.2-ai-json-repair.js` — ۸۴ PASS / ۰ FAIL:** **پورتِ وفادارِ JS** از `strip_fences/scan/close_tail/peel/meaningful/repair_truncated/salvage` روی ۹ نمونهٔ نام‌دار (۵ پاسخِ **بریدهٔ واقعی** از خودِ promptهای `seo_fix/seo_alt/seo_intlinks/seo_review/seo_expand` + حصارِ markdown + نثرِ محض + دمِ `:"` + خروجیِ سالم)؛ pinهای سیاست (گامِ نخست، گاردِ دم، «بدون عضو پوچ»، سقفِ حلقه‌ها، برچسبِ `how`)؛ pinهای برابریِ PHP/JS (۹ فراخوانیِ `llm_call_json`، ۱۶ فراخوانیِ `llm_call`، سقف‌های توکن، `finish` در هر دو provider، clampِ retry، opt-in بودنِ `thinkingConfig`)؛ pinهای سازگاریِ پیشین (tester41: عبارتِ پایهٔ خطا · tester140: OCR · tester565: `seo_product` ۱۶۰۰ و `seo_article` ۴۰۰۰).
- **pinهای به‌روز، بدونِ سست‌شدن:** `tester560:58` از «`llm_call` با ۹۰۰ توکن» به «`llm_call_json` با ۱۸۰۰ توکن و همان قراردادِ چندتصویرِ `$pack`» (با ذکرِ علت).
- **drive-by:** `tester220` کرانِ کهنه داشت (۲۵۰۰/۱۸۰۰ کاراکتر، اندازه‌گیری‌شده بر پایهٔ `bridge.js` پیش از صفحه‌بندی: ۵۵KB/۴۰٫۸KB؛ اکنون ۱۷۹KB/۱۴۷KB) ⇒ به ۹۰۰۰/۹۰۰۰ با افستِ بازاندازه‌گیری‌شده (۶۱۴۱/۲۳۵۹): ۱۷ PASS / ۰ FAIL.
- **`arch-guard`:** PASS (بدونِ هشدارِ نسخه/بدهی) · **گیت CI:** سبز (۲۰۵ ورودی).

### درسِ فرآیندی (برای بامپ‌های بعدی)

جاروی نسخه باید **هر سه شکل** را بپوشاند: `v34.36.1` (نشانگرها) · `34.36.1` بی‌پیشوند (`ASSET_VERSION`, `manifest.json`, `SD_SERVICE_VERSION`, queryهای `?v=`) · شکلِ **فرارِ regex** (`34\.36\.1` در pinهای تسترها). دو نقطهٔ زنده‌ای که در بامپِ دستی جا ماندند و گیت لو داد: `api/sales-domain.php` (`SD_SERVICE_VERSION`) و `crm/manifest.json` (`version`). نامِ فایلِ `tester588-v34.36.1-…` و کامنت‌های تاریخیِ `/* v34.36.1 … */` **نباید** جارو شوند.
