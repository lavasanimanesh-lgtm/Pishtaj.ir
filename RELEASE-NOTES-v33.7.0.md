# یادداشت‌های انتشار (Release Notes — Version 33.7.0)

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (PR #11 باز تا پایان مهاجرت MySQL)
**تمرکز ریلیز:** ذی‌نفع شرطی در ثبت چک (مشتری/تامین‌کننده/سایر) + اثر مالی تضمینی + چک ضمانت با پرونده فروش + حالت گرافیکی چاپ چک + **ریشه‌کنی باگ حقوق سهامدار (۱۳۰→۹۰)** + اعمال خودکار هزینه‌های تکرارشونده ماهانه.

---

## 🚀 دستاوردهای کلیدی نسخه ۳۳.۷.۰

### ۱. ذی‌نفع شرطی در ثبت چک (دستی و دستیار AI) — مصوب کارفرما
- **ثبت دستی (`+ چک جدید`):** انتخاب نوع ذی‌نفع:
  - **چک صادره:** «🏭 تامین‌کننده (دارای مطالبه از ما)» — فقط تامین‌کنندگانی که بدهی باز دارند (فاکتور خرید − پرداخت > ۰) با مبلغ بدهی در لیست کشویی؛ یا «👤 سایر».
  - **چک وارده:** «🤝 مشتری (دارای پرونده فروش باز)» — فقط مشتریانی که پروندهٔ برندهٔ غیربایگانی دارند؛ یا «👤 سایر».
- **دستیار AI:** همان فهرست‌های شرطی + فاکتور باز مشتری (اختیاری).
- توابع پشتیبان: `ptfChequeSupOptions` / `ptfChequeCustOptions` / `ptfChequeOpenInvoicesOf` / `ptfChequePartyKind(Label)`.

### ۲. اثر مالی چک مالی به محض ثبت
- **چک صادره مالی + تامین‌کننده** → همان لحظه `payment` در `ptf_crm_supplier_finance` (کسر بدهی).
- **چک وارده مالی + فاکتور باز** → همان لحظه `payment` روی فاکتور (کسر مطالبات).
- **چک وارده بدون فاکتور** → «در گردش» می‌ماند و با **وصول** اثر مالی می‌گیرد (روی اولین فاکتور باز همان مشتری) — طبق تصمیم کارفرما (اختیاری).
- **باگ فنی ریشه‌کنی شد:** توابع `faDateL/faDateTimeL` در cheque-module خودشان را بازگشتی صدا می‌زدند (استک‌اورفلو → تاریخ payment چک‌ها خالی). اصلاح شد.

### ۳. چک ضمانت — بدون اثر مالی + در پرونده فروش + استرداد با پایان پروژه
- نوع چک: **مالی / ضمانت پیش‌پرداخت / ضمانت حسن انجام کار / ضمانت مناقصه / سایر**.
- **ضمانت هرگز اثر مالی ندارد** (گارد در `ptfChequeApplyFinancial` — قبل از این ریلیز ضمانت با تامین‌کننده payment می‌ساخت).
- **لینک پرونده فروش برای ضمانت الزامی** (دستی و AI)؛ ضمانت در پرونده (بایگانی/باز) نمایش داده می‌شود (`prjGuaranteeChequesHtml`).
- **استرداد ضمانت:** دکمه «🏆 استرداد ضمانت» در تب چک‌ها (صادره).
- **گارد مختومه:** ضمانت باز = blocker؛ با تأیید صریح «استرداد پس از مختومه» + دلیل قابل عبور (الگوی UR-12 تحویل) و در audit ثبت می‌شود.

### ۴. چاپ چک فیزیکی — حالت گرافیکی + مبلغ دوم
- **حالت گرافیکی (درگ‌اند‌دراپ):** در «📐 تنظیمات چاپ» برگهٔ چک با خطوط راهنما نمایش داده می‌شود و هر نوشته را با ماوس/لمس می‌کشید و در جای صحیح رها می‌کنید (مختصات mm خودکار به‌روز می‌شود).
- **مبلغ دوم:** مبلغ اصلی به عدد در **پایین-چپ برگه** اضافه شد (هر چک دو مبلغ دارد: قرمز بالا برای محکم‌کاری + اصلی پایین-چپ) — مختصات/اندازه/فونت/رنگ آن هم قابل تنظیم است.

### ۵. ریشه‌کنی باگ حقوق سهامدار (۱۳۰ → ۹۰ میلیون)
**ریشه:** در `ptfShareEdit`، تابع `syncSalaryTxForMonth` فقط رکورد موجود را به‌روز می‌کرد و برای **سهامدار جدید** اصلاً صدا زده نمی‌شد → حقوق سهامدار تازه‌افزوده در ماه جاری هرگز ثبت نمی‌شد (۳ نفر = ۹۰ میلیون به‌جای ۴ نفر = ۱۳۰ میلیون).
**رفع:** تابع `ensureSalaryTxForMonth` (اطمینان از وجود: ایجاد/آپدیت/حذف) + فراخوانی برای سهامدار جدید، تغییر حقوق، و غیرموظف‌شدن (حذف از ماه جاری).

### ۶. اعمال خودکار هزینه‌های تکرارشونده با شروع ماه جدید — مصوب کارفرما
- `ptfAutoApplyRecurring()` در اولین باز شدن CRM در ماه جدید:
  - **حقوق سهامداران موظف** (نقش‌های ارشد — محرمانه): از `ensureSalaryTxForMonth` — همان منطق دکمهٔ دستی.
  - **قالب‌های تکرارشونده (opexTpl — اجاره و سایر):** ثبت خودکار با برچسب «🤖 خودکار».
  - یک‌بار در ماه (flag `ptf_auto_recurring_last`) + ثبت در audit + toast اطلاع‌رسانی.

---

## 📋 تغییرات فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/cheque-module.js` | فهرست‌های شرطی ذی‌نفع + اثر مالی وصول + گارد ضمانت + رفع باگ بازگشتی faDateL/faDateTimeL |
| `crm/cheque-panel.js` | فرم ثبت دستی ذی‌نفع شرطی + نوع ضمانت + پرونده + فاکتور؛ دستیار AI هم‌گام؛ دکمه استرداد ضمانت؛ برچسب‌های دسته/نوع در جدول‌ها |
| `crm/cheque-print.js` | مبلغ دوم (پایین-چپ) + حالت گرافیکی درگ + ذخیرهٔ amt2 در چیدمان |
| `crm/salesfiles.js` | گارد ضمانت باز در مختومه + مسیر تأیید صریح استرداد پس از مختومه |
| `crm/projects.js` | نمایش چک‌های ضمانت پرونده (باز/بایگانی) |
| `crm/shareholders.js` | `ensureSalaryTxForMonth` (رفع ۱۳۰→۹۰) + ثبت خودکار حقوق سهامدار جدید/تغییر/حذف |
| `crm/opex.js` | `ptfAutoApplyRecurring` + فراخوانی در بوت + برچسب «🤖 خودکار» |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.7.0 |
| `_tools/uat/tester288-cheque-party-recurring.js` | **جدید** (۲۸/۰) |
| `_tools/uat/tester215,287` | تست‌های تکمیلی (ضمانت بدون اثر مالی / فهرست‌های شرطی / amt2 / درگ) |
| `RELEASE-NOTES-v33.7.0.md` / `REGRESSION-REPORT-v33.7.0.md` | اسناد انتشار |
| `_audit/HANDOFF-NEXT-SESSION-2026-08-01.md`، `crm/BACKLOG-USER-2026-08-01.md` | به‌روزرسانی |

---

## ⚠️ برای کاربر (روش اعمال)
1. `Ctrl+Shift+R` یا `crm/clear-cache.html` (نسخه باید v33.6.0 → **v33.7.0** شود).
2. ثبت چک: هاب مالی → تب چک‌ها → «+ چک جدید» یا «🤖 دستیار هوشمند» — ذی‌نفع را از لیست‌های شرطی انتخاب کنید.
3. چاپ فیزیکی: کالا و اسناد → «چاپ چک فیزیکی» → «📐 تنظیمات چاپ» → فیلدها را با **درگ گرافیکی** روی برگه بکشید → ذخیره → چاپ آزمایشی.
4. در شروع ماه جدید، هزینه‌های تکرارشونده (حقوق + اجاره و…) خودکار ثبت و در audit دیده می‌شوند.
