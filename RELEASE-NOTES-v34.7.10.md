# Release Notes v34.7.10 — اصلاح موبایل تامین و یکپارچگی حساب مشتری

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## رابط موبایل تامین

- toolbar جستجو، کارت‌ها و همه actionهای درخواست تامین داخل عرض viewport محدود شدند.
- actionهای کارت تامین در موبایل تک‌ستونه، تمام‌عرض و wrap-safe شدند.
- actionهای accordion، مقایسه قیمت، ورود اقلام و footer ویزارد کلاس و grid مستقل گرفتند.
- input/selectها `min-width:0` و `max-width:100%` دارند.
- جدول‌های عریض فقط در wrapper خود scroll افقی دارند و دیگر کل صفحه را به بیرون هل نمی‌دهند.

## حساب مشتریان

- همه دکمه‌های گردش حساب آیکون صریح گرفتند: فیلتر، چاپ، CSV، ثبت وصولی، ابطال و بستن.
- toolbar حساب مشتری در موبایل grid واکنش‌گرا دارد.
- مشتری دارای مطالبه یا اعتبار، حتی بعد از sort ستون‌ها، همیشه بالاتر از حساب بسته نمایش داده می‌شود.
- تعریف «حساب باز» اکنون هم مطالبه و هم اعتبار مشتری را پوشش می‌دهد.

## رفع دوباره‌شماری وصولی مهاجرت‌شده

- علت اختلاف عدد فهرست و گردش حساب مشخص شد: محاسبه فهرست، payment قدیمی دارای `migratedToReceiptId/financialProjectionDisabled` را درست حذف می‌کرد، اما renderer گردش همان ردیف را دوباره در کنار Receipt قطعی نمایش و از مانده کم می‌کرد.
- اکنون payment قدیمی پس از مهاجرت فقط metadata منبع است و ردیف مالی جدا نمی‌سازد.
- Receipt قطعی canonical یک‌بار نمایش و یک‌بار محاسبه می‌شود.
- روی ردیف canonical نوشته می‌شود از کدام وصولی قدیمی مهاجرت شده است.
- پنل تطبیق، تعداد زوج‌های وصولی قدیمی/دریافت قطعی و موارد فاقد Receipt متناظر را نشان می‌دهد.
- دریافت قطعی از داخل گردش حساب قابل ابطال است و ادمین مسیر حذف قطعی کنترل‌شده دارد.

## رفع false duplicate پرونده

- `rootOfferId` در تطبیق پرونده و پیشنهاد authoritative شد؛ اگر دو root متفاوت باشند، شباهت شماره نمی‌تواند آن‌ها را تکراری اعلام کند.
- fallback اسناد legacy علاوه بر شماره، سازگاری `inqNo`، مشتری و ارز را بررسی می‌کند.
- اگر buyerCd موجود نباشد، نام نرمال‌شده مشتری نیز بررسی می‌شود.
- همین قرارداد در client، API، ثبت برد، مهاجرت و گزارش سال مالی اعمال شد.
- پرونده‌های واقعاً هم‌ریشه همچنان شناسایی و از راهنمای ادغام کنترل‌شده قابل حل هستند.

## کنترل کیفیت

- `tester415-v34.7.10-mobile-customer-integrity.js`: 22 PASS / 0 FAIL
- CI متمرکز: 34 PASS / 0 FAIL
- رگرسیون کامل: 398 تستر، 341 فایل PASS، 57 فایل بدهی تاریخی FAIL، 5909 check PASS و 129 check تاریخی FAIL؛ شکست جدیدی نسبت به v34.7.9 ایجاد نشد.
- PHP parser مستقل روی فایل‌های تغییرکرده: PASS
- Auditor روی 624 صفحه و هم‌ترازی VERSION/PWA با 100 مرجع cache-query: PASS
- PHP CLI در محیط موجود نبود و lint داخلی CI صریحاً skip شد.
