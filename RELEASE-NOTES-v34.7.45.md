# یادداشت انتشار v34.7.45 — فرم کامل رویژن پیشنهاد برنده و بازیابی رسید قطعی

**تاریخ:** 2026-08-19

**دامنهٔ استقرار:** فقط staging؛ استقرار production ممنوع است.

## یافته‌های مستقل

### ۱. فرم رویژن یک فرم موازی و ناقص بود

`case-revision.js` برای رویژن پیشنهاد برنده یک جدول مستقل و کم‌فیلد می‌ساخت. این projection فقط نام، شرح، مدل، واحد، کد، برند، تعداد و قیمت را حمل می‌کرد. در نتیجه امکانات فرم اصلی پیشنهاد مالی در دسترس نبودند:

- انتخاب چندگانه از ماژول کالا؛
- بارگذاری اقلام درخواست و Excel؛
- نرخ مرجع، منشأ نرخ و حاشیه سود؛
- ستون‌های تکمیلی؛
- شرایط و ضوابط و کتابخانهٔ شرایط؛
- پیش‌نمایش، امضا، قالب چاپ و metadata سند.

همچنین projection کم‌فیلد می‌توانست metadata اقلام موجود یا تازه‌افزوده‌شده را هنگام ACK حذف کند.

### ۲. مسیر عمومی uncertain رسید قطعی فشرده نداشت

فرمان `revise_award` چند projection کامل مالی را در پاسخ برمی‌گرداند. اگر commit سرور انجام می‌شد ولی پاسخ بزرگ در transport گم می‌شد، کلاینت فقط همان فرمان و پاسخ بزرگ را یک بار replay می‌کرد. پس از دو پاسخ مبهم، بدون پرس‌وجوی مستقل از journal قطعی، نتیجه `uncertain` اعلام می‌شد. این وضعیت لزوماً به معنی عدم commit نبود.

## اصلاح معماری

### یک فرم واحد برای پیشنهاد مالی و رویژن

رویژن اکنون مستقیماً همان `offerForm` اصلی را با state کپی‌شدهٔ پیشنهاد برنده باز می‌کند. بنابراین UI، validation و ابزارهای اقلام دقیقاً مشترک‌اند. فقط دکمهٔ نهایی به فرمان اتمیک `revise_award` متصل می‌شود.

هویت قراردادی مشتری، درخواست و ارز پس از تشکیل پرونده نمایش داده می‌شود ولی قفل است. سایر بخش‌های سند، از جمله اقلام، نرخ مرجع، حاشیه، شرایط، تاریخ، اعتبار، امضا و قالب چاپ در همان رویژن ثبت می‌شوند.

سرور:

- `offerDocument` را sanitize و همراه اقلام در همان تراکنش اعمال می‌کند؛
- شرایط و metadata سند را در snapshot رویژن نگه می‌دارد؛
- metadata نرخ مرجع، حاشیه، تحویل و ستون‌های تکمیلی اقلام را حفظ می‌کند؛
- تغییر مشتری/درخواست/ارز را fail-closed رد می‌کند؛
- شماره و `_id` پیشنهاد برنده را ثابت و `rev` را افزایشی نگه می‌دارد.

### رسید قطعی فشرده

endpoint فقط‌خواندنی `command_status` به journal پایدار فرمان متصل شد. پس از دو پاسخ مبهم:

1. هیچ mutation تازه‌ای ساخته نمی‌شود؛
2. operation ID و action در یک پاسخ کوچک و owner-bound بررسی می‌شوند؛
3. اگر journal فرمان را committed بداند، نتیجه ACK قطعی و projection با pull بازیابی می‌شود؛
4. فقط وقتی mutation و status هر دو در دسترس نباشند، وضعیت uncertain باقی می‌ماند.

journal داخلی `ptf_crm_sales_commands` دیگر داخل projection پاسخ mutation به مرورگر فرستاده نمی‌شود؛ این داده صرفاً سروری است و ارسال تا ۱۰۰۰ receipt کامل، پاسخ رویژن را بی‌دلیل بزرگ می‌کرد. `command_status` فقط receipt موردنیاز را برمی‌گرداند.

فرمان‌های uncertain ذخیره‌شده از نسخهٔ قبلی نیز پس از boot احرازشده فقط از journal بررسی می‌شوند. در نتیجه شناسهٔ گزارش‌شدهٔ قبلی `REV-mt0fqdf5-93hkmqvx` پس از ورود/refresh بدون ارسال دوبارهٔ mutation بررسی خواهد شد.

### قفل payload در uncertain

اگر حتی status نیز در دسترس نباشد، فرم باز می‌ماند ولی payload قفل می‌شود. دکمهٔ بازیابی فقط همان payload و همان operation ID را استفاده می‌کند؛ کاربر نمی‌تواند ناخواسته payload متفاوت را با یک کلید idempotency قبلی ارسال کند.

پیش‌نویس خودکار رویژن نیز کلید مستقل دارد و دیگر نمی‌تواند پیش‌نویس «پیشنهاد مالی جدید» را بازنویسی یا در مسیر صدور عادی restore کند.

## تست‌ها

- tester442: ثبت پیشنهاد و RFQ با ACK/rejected/uncertain؛
- tester444: precondition، هم‌زمانی، operation ID پایدار و قفل intent رویژن؛
- tester446: بازیابی ACK از compact receipt پس از دو پاسخ گم‌شده؛
- tester448: استفادهٔ رویژن از فرم اصلی، حفظ metadata کامل، Terms، نرخ مرجع و بازیابی خودکار uncertain ذخیره‌شده.
