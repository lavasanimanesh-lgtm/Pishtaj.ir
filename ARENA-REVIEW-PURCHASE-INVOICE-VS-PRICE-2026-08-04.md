# بازبینی: «فاکتور خرید» مبنای تعهد و سود است، نه «قیمت خرید» دستی در پرونده

**برنچ:** `arena/019fcd1d-pishtaj-ir` (v34.0.7-alpha)
**وضعیت:** فقط‌خواندنی — هیچ تغییری در کد اعمال نشد.
**تعریف کارفرما (این نشست):**
- ورود «قیمت خرید» در پرونده‌ها **اختیاری** و فقط برای **بررسی صحت فاکتور خرید** است؛ مبنای تعهد نیست.
- «خرید تعهدی» معنا ندارد؛ مبنای تعهد = **فاکتور خرید ثبت‌شده** و تعهد = **ماندهٔ باقیماندهٔ مطالبات همان تأمین‌کننده نسبت به همان فاکتور**. هیچ چیز دیگری در گردش حساب تأمین‌کننده نباشد.
- پیشنهاد فقط مبنای صدور فاکتور است؛ **اصل بر صحت فاکتور است** و مغایرت پیشنهاد/فاکتور معنا ندارد.
- نرخ تسعیر ارزی فقط برای **محاسبهٔ درصد پیش‌پرداخت از کل پیشنهاد** معنا دارد؛ در لحظهٔ صدور فاکتور، بدهی به نرخِ همان‌روز محاسبه می‌شود.

---

## یافتهٔ اصلی (تأییدشده) — همان «یک چیزی» که باید اصلاح شود

**موتور سود پروژه، هزینه/سود را از «قیمت خرید دستی» (خرید نهاییِ ثبت‌شده در پرونده) می‌گیرد، نه از «فاکتور خرید».**

شواهد:

1. **`crm/fx.js` — `ptfProjectProfitIRR`** (هستهٔ محاسبهٔ سود پروژه):
   ```js
   getData('ptf_crm_buycmp').forEach(c2 => {
     if (c2.inqNo !== prj.inqNo && c2.inqNo !== prj.offerNo) return;
     (c2.purchases || []).forEach(pu => { ... res.buyIrr += unit * qty; ... });
   });
   ...
   res.profit = res.sellIrr - res.buyIrr;   // ← سود = فروش − قیمت خریدِ دستی
   ```
   یعنی `buyIrr` از `buycmp.purchases` (قیمتی که کاربر در «خرید نهایی» پرونده وارد کرده) می‌آید. این دقیقاً همان «خرید تعهدی/قیمت خرید به‌عنوان مبنای هزینه» است که تعریف شما آن را رد می‌کند.

2. **`crm/buycompare.js:240`** صراحتاً می‌نویسد: «ستون **خرید نهایی** = **خرید واقعی** که **مبنای سود پروژه** است». یعنی الان قیمت دستی، مبنای سود است — خلاف خواستهٔ شما.

3. **`crm/fiscal.js` `ptfFiscalData`** (سود تعهدی/گزارش/قفل سال) از `ptfProjectProfitIRR` استفاده می‌کند → بنابراین «سود خالص» در گزارش رسمی سال مالی بر **قیمت خرید دستی** استوار است، نه فاکتور خرید.

> نکتهٔ منصفانه: سود **نقدی** (`ptfFiscalCashData` که مبنای **تقسیم** است) از `sf.invoices` (فاکتورهای خرید تأمین‌کننده) به‌عنوان خروجی استفاده می‌کند — یعنی در مسیر تقسیم، فاکتور خرید لحاظ می‌شود. ولی (الف) با سود تعهدیِ گزارش ناسازگار است، و (ب) هنوز «قیمت خرید دستی» در هیچ‌کجا فقط «ابزار کنترل صحت فاکتور» نیست.

---

## ناسازگاری‌های مرتبط (که با همان اصلاح باید دیده شوند)

### A) قیمت خرید باید «اختیاری + ابزار کنترل» شود، نه شرطِ کامل بودن سود
- در `ptfProjectProfitIRR`، فقدان «قیمت خرید/خرید نهایی» یا ناتوانیِ تطبیق آن به قلم CO → پروژه «ناقص»/«نیازمند تعیین تکلیف» می‌شود (`res.complete=false`، `buyUnmatched`، `buyPendingFx`). طبق تعریف شما، نبودِ قیمت خرید نباید سود را ناقص کند؛ چون مبنای واقعی فاکتور خرید است.

### B) گردش حساب تأمین‌کننده باید فقط «فاکتور خرید + ماندهٔ آن» باشد
- `supplier-finance.js` `balance()` هم فاکتورها (`activeInvoices`) و هم «تعهدهای خرید legacy» (`ptf_crm_payables` از `legacyOpen`) را با هم جمع می‌کند. طبق تعریف شما، این «تعهد خرید legacy» نباید مبنای گردش باشد؛ فقط فاکتور خرید و ماندهٔ باقیماندهٔ همان.

### C) پیشنهاد فقط مبنای صدور فاکتور؛ مغایرت فاکتور/پیشنهاد بی‌معناست
- در `saveInv` (فاکتور فروش) یک هشدار/تأییدِ «مغایرت با پیش‌فاکتور ارزی» وجود دارد (`ptfLedgerOfficialFxSanity`). طبق تعریف شما، **اصل بر صحت فاکتور است** و مغایرت، خطا نیست؛ پس این باید یا حذف شود یا صرفاً اطلاع‌رسانی بدون بلوکه‌کردن، و هرگز مبنای تعهد/مانده قرار نگیرد.

### D) نرخ تسعیر فقط برای درصد پیش‌پرداخت
- منطق پیش‌پرداخت (`ptfAdvanceNormalize`) درست است که نرخ را برای محاسبهٔ مبلغ ریالیِ پیش‌پرداخت (و درصد از کل) به‌کار می‌برد؛ ولی باید اطمینان حاصل شود که در **صدور فاکتور**، مبلغ بدهیِ باقی‌مانده به نرخِ روزِ فاکتور محاسبه می‌شود (مطابق مثال شما: پیش‌پرداخت ۵۰۰$ = ۱۰۰ میلیون تومان با نرخ ۲۰۰،۰۰۰؛ در فاکتور با نرخ ۳۰۰،۰۰۰ بدهیِ ۱۰۰۰$ = ۳۰۰ میلیون و فاکتور کل ۴۰۰ میلیون).

---

## جمع‌بندی — اصلاح واحد پیشنهادی

**باید اصلاح شود:** «قیمت خریدِ واردشده در پرونده» (ستون «خرید نهایی» / `buycmp.purchases`) **نباید مبنای سود/تعهد** باشد. مبنای سود و تعهد باید **فاکتور خریدِ ثبت‌شده** باشد؛ و «قیمت خرید» فقط یک ورودی **اختیاری و کنترل‌کنندهٔ صحت** فاکتور (نشان‌دادن مغایرت احتمالی قیمت دستی با فاکتور) باشد، نه اینکه نبودش سود را ناقص کند.

بستهٔ تغییر پیشنهادی (برای مرحلهٔ اجرا، پس از تأیید شما):
1. `ptfProjectProfitIRR` به‌جای `buycmp.purchases`، هزینه را از **فاکتورهای خرید** (لینک‌شده به پرونده از طریق `legacyPayableCds`/`itemLinks`/`inqNo`) بخواند؛ قیمت دستی فقط به‌عنوان «کنترل مغایرت» گزارش شود.
2. `balance()` تأمین‌کننده فقط فاکتور خرید + ماندهٔ باقیمانده را نشان دهد («تعهد خرید legacy» حذف از مبنا).
3. هشدارِ مغایرت فاکتور/پیش‌فاکتور ارزی به «اطلاع‌رسانی غیربلوکه‌کننده» تبدیل شود (اصل = صحت فاکتور).
4. اطمینان از محاسبهٔ بدهیِ ارزیِ باقی‌مانده در لحظهٔ فاکتور با نرخ روز (مطابق مثال ۱۵۰۰ دلار).

*تولیدشده توسط Arena Agent — بازبینی فقط‌خواندنی؛ هیچ تغییری در کد اعمال نشده است.*
