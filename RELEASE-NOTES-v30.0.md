# ریلیزنوت v30.0 — چک شرکتی برای مدیرعامل + زیرساخت مدیر بازرگانی + فیکس کد کالا از 1120

## درخواست‌ها
1. مدیرعامل هم بتواند چک شرکتی ثبت کند (حق امضا دارد) - برای مدیر بازرگانی زیرساخت فراهم شود
2. باگ: کد کالا از P-1001 شروع شده و تکراری ثبت می‌کند درحالیکه دیشب از P-1120 می‌داد - ریشه‌یابی و از 1120

## ریشه‌یابی کد کالا
- counters.json فقط هنگام عدم وجود فایل از max موجود seed می‌شد، اگر فایل وجود داشت ولی seq P=1000 بود و محصولات تا P-1119 داشتند، دوباره P-1001 می‌داد → تکراری
- فیکس: load_counters همیشه max موجود از ptf_crm_products.json را می‌خواند و seq را به max می‌رساند، و کف P=1119 تا بعدی 1120 شود
- همچنین legacyMaxNext کلاینتی هم کف 1119 گرفت
- تست: حذف counters.json و رفرش → باید P-1120 بدهد نه P-1001

## فیکس چک شرکتی
| فایل | تغییر |
|---|---|
| crm/cheques.js | canCreateCompanyCheque() = ['chairman','ceo','commercial'] - chairman و ceo الان، commercial آینده. فرم مالکیت فقط اگر مجاز باشد گزینه company را نشان می‌دهد، و در chCollectForm اگر غیرمجاز company بزند بلاک + پیام. زیرساخت برای commercial فراهم شد |
| api/codegen.php | P از 1120 - load_counters همیشه max محصولات را چک می‌کند |
| crm/codegen.js | legacyMaxNext P از 1119 |
| crm/index.html | VER v30.0 |
| crm/sw.js | CACHE v30.0 |

## تست
1. با chairman لاگین → چک جدید → مالکیت شرکت قابل انتخاب → ثبت اوکی
2. با ceo لاگین → همین → باید اوکی باشد (قبلاً فقط chairman)
3. با sales لاگین → چک جدید → مالکیت فقط شخصی باید ببینی، company نباید باشد
4. با commercial (اگر ساختی) → الان زیرساخت آماده، company می‌بیند
5. کالا: درخواست جدید ثبت کن → اقلام → P-1120 باید بدهد نه P-1001 - اگر تکراری داد counters.json را پاک کن و دوباره

## نسخه
- VER v30.0, CACHE ptf-crm-v30.0
