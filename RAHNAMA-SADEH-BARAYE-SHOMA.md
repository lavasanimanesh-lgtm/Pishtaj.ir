# 🧑‍🏫 راهنمای ساده — این محیط چیست، چطور کار من را ببینید، و چطور به سایت واقعی برسانید
**برای شما نوشته شده — فرض می‌کنم هیچ پیش‌زمینهٔ برنامه‌نویسی/گیت‌هاب ندارید.**

---

## ۱. اصلاً این «Arena» و «گیت‌هاب» چیست؟ چرا دو تا داریم؟

فکر کنید کدِ سایت شما مثل یک **سند Word مشترک بزرگ** است.

- **گیت‌هاب (GitHub)** = گاوصندوق اصلی و رسمی این سند. نسخهٔ داخل گیت‌هاب، «نسخهٔ واقعی و ثبت‌شده» پروژه است.
- **Arena (این محیط که الان با من صحبت می‌کنید)** = یک **اتاق کار شخصی و ایزوله** که من داخلش یک کپی از آن سند را باز کرده‌ام و رویش کار می‌کنم. هرچه من عوض می‌کنم، فقط داخل همین اتاق است — **تا وقتی خودتان تأیید نکنید، هیچ‌چیز روی سایت واقعی (pishtaj.ir) یا حتی روی نسخهٔ اصلی گیت‌هاب تغییر نمی‌کند.**

هر نشستی که با من (یا یک ایجنت دیگر) در Arena باز می‌کنید، یک «اتاق کار» جدا و مجزا با یک نام مخصوص می‌سازد — به آن **Branch (شاخه)** می‌گویند. شاخهٔ من در همین نشست اسمش هست:
```
arena/019f994f-pishtaj-ir
```
این فقط یک برچسب/پوشهٔ مجازی است، چیزی نیست که نگرانش باشید.

---

## ۲. من الان دقیقاً چه کار کرده‌ام؟

۱. یک بررسی کامل از وضعیت فعلی سایت و CRM انجام دادم (نتیجه در فایل `ARENA-COMPREHENSIVE-ASSESSMENT-2026-07-25.md`).
۲. یک نقشهٔ راه نوشتم که توضیح می‌دهد چطور جلو برویم (فایل `STRATEGIC-ROADMAP-ROLLBACK-SEO-PARALLEL-CRM.md`).
۳. یک اقدام امنیتی فوری انجام دادم: چند فایل که به‌اشتباه داخل گیت‌هاب قرار گرفته بودند و شامل اطلاعات حساس (رمز/توکن ورود کاربران CRM) بودند را از مسیر «ثبت خودکار» گیت خارج کردم و یک فایل تنظیمات (`.gitignore`) اضافه کردم تا دیگر این اتفاق تکرار نشود.

این کار را «Commit» کردم (یعنی مثل زدن دکمهٔ Save روی این تغییرات با یک یادداشت توضیحی) و بعد «Push» کردم (یعنی این Save را به گیت‌هاب فرستادم، ولی **فقط داخل شاخهٔ مخصوص همین نشست** — نه روی نسخهٔ اصلی).

---

## ۳. چطور کار من را «ببینید»؟

### گزینهٔ ۱ — همین‌جا در Arena (ساده‌ترین راه)
همین الان، وقتی من فایلی می‌سازم یا تغییر می‌دهم، آن را برایتان با ابزار «نمایش فایل» باز می‌کنم (همان چیزی که همین الان برای دو گزارش انجام دادم). شما می‌توانید همین‌جا بخوانید، نظر بدهید، بگویید «خوب است» یا «این را عوض کن».

### گزینهٔ ۲ — دیدن در گیت‌هاب (وقتی خواستید با چشم خودتان تغییرات را روی وب‌سایت گیت‌هاب ببینید)
1. به آدرس مخزن بروید: `https://github.com/lavasanimanesh-lgtm/Pishtaj.ir`
2. بالای صفحه، یک دکمهٔ کشویی می‌بینید که رویش نوشته `main` — این نام شاخهٔ اصلی/رسمی است.
3. روی آن کلیک کنید و به‌جای `main`، شاخهٔ `arena/019f994f-pishtaj-ir` را انتخاب کنید.
4. حالا همان صفحه، دقیقاً همان فایل‌هایی را نشان می‌دهد که من در این نشست تغییر داده‌ام — **بدون این‌که سایت اصلی شما تحت تأثیر قرار بگیرد.**

> نکتهٔ کلیدی: تا وقتی شما این تغییرات را «تأیید و ادغام (Merge)» نکنید، شاخهٔ `main` (نسخهٔ رسمی) دست‌نخورده باقی می‌ماند. یعنی هیچ خطری برای سایت فعلی‌تان وجود ندارد که من کار کنم.

---

## ۴. «Pull Request» یعنی چه؟ (این مهم‌ترین مفهومی است که باید یاد بگیرید)

وقتی من کارم روی یک بخش (مثلاً سئوی سایت) را تمام کردم و آماده شد که به نسخهٔ اصلی اضافه شود، یک **Pull Request** (به‌اختصار PR) می‌سازم.

فکر کنید PR یک **درخواست رسمی** است که می‌گوید:
> «من این تغییرات را در اتاق شخصی‌ام آماده کرده‌ام؛ لطفاً بررسی کن و اگر خوب بود، اجازه بده وارد نسخهٔ اصلی (main) شود.»

در صفحهٔ PR روی گیت‌هاب، شما می‌توانید:
- **دقیقاً ببینید چه چیزی عوض شده** — خط‌های سبز (اضافه‌شده) و خط‌های قرمز (حذف‌شده) کنار هم نشان داده می‌شود.
- یک دکمهٔ سبز رنگ بزرگ می‌بینید: **«Merge pull request»** — با کلیک روی همین دکمه، تغییرات رسماً وارد `main` (نسخهٔ اصلی) می‌شوند.
- اگر چیزی را دوست نداشتید، می‌توانید کامنت بگذارید یا حتی PR را ببندید بدون Merge کردن (یعنی رد کردن تغییرات).

**شما فقط باید یاد بگیرید یک کار را بلد باشید: باز کردن PR، نگاه‌کردن به خلاصهٔ تغییرات (که من همیشه به فارسی و ساده برایتان می‌نویسم)، و زدن دکمهٔ سبز Merge وقتی راضی بودید.** بقیه‌اش کار من است.

---

## ۵. «Merge» و «اسپلیت‌کردن» یعنی چه؟ (برای کار دو ایجنت هم‌زمان)

- **Merge (ادغام)** = وقتی دو نسخهٔ متفاوت از کد (مثلاً کار من روی سئو، و کار یک ایجنت دیگر روی CRM) با هم قاطی می‌شوند و یک نسخهٔ نهایی واحد می‌سازند. این کار را گیت‌هاب خودش خودکار انجام می‌دهد — تا وقتی دو نفر همزمان **دقیقاً همان خط از همان فایل** را عوض نکرده باشند، هیچ مشکلی پیش نمی‌آید.
- **اسپلیت‌کردن (Split / شاخه‌بندی)** = یعنی از همان ابتدا، هرکس (من یا ایجنت CRM) در یک اتاق کار جدا (Branch) کار کند، نه در یک اتاق مشترک. این باعث می‌شود کار همدیگر را خراب نکنیم.

### چرا اینجا خطر برخورد خیلی کم است
بررسی کردم: کد سایت عمومی (که من رویش کار می‌کنم: صفحات، مقالات، ظاهر سایت) و کد CRM (که ایجنت بعدی رویش کار می‌کند: داشبورد داخلی شرکت) **در دو پوشهٔ کاملاً جدا** هستند و تقریباً هیچ فایل مشترکی ندارند. یعنی مثل این است که من دارم روی «ویترین مغازه» کار می‌کنم و ایجنت دیگر روی «انبار پشت مغازه» — به‌ندرت پیش می‌آید کارمان با هم تداخل کند.

---

## ۶. پیشنهاد ساختار عملی برای شما (قدم‌به‌قدم، بدون نیاز به دانش فنی)

### مرحلهٔ ۱ — ساخت دو «اتاق کار دائمی» در گیت‌هاب (یک‌بار، توسط شما یا با کمک من در یک نشست دیگر)
دو شاخهٔ دائمی می‌سازیم:
- `site-content` → برای کار من (محتوا، UI، سئو)
- `crm-work` → برای کار ایجنت بعدی (CRM)

(این کار را می‌توانید در یک نشست دیگر Arena از من یا هر ایجنتی بخواهید انجام دهد — چون من در همین نشست فقط اجازهٔ کار روی شاخهٔ خودم را دارم.)

### مرحلهٔ ۲ — هرکدام از ما در اتاق خودمان کار می‌کنیم
من هرچه دربارهٔ سایت/سئو/محتوا انجام می‌دهم را در `site-content` ذخیره می‌کنم. ایجنت CRM هرچه دربارهٔ CRM انجام می‌دهد را در `crm-work`.

### مرحلهٔ ۳ — شما فقط تصمیم می‌گیرید، نه کدنویسی
هروقت یک تکه‌کار (مثلاً «۱۰ مقاله تعمیق داده شد») آماده شد، من یک **Pull Request** با خلاصهٔ فارسیِ روشن می‌سازم. شما آن را می‌خوانید و اگر پسندیدید، دکمهٔ Merge را می‌زنید (یا از من می‌خواهید یک نشست جدید باز کنم و برایتان همین‌جا Merge را انجام دهم — این هم گزینه است، لازم نیست حتماً خودتان وارد گیت‌هاب شوید).

### مرحلهٔ ۴ — بردن به سایت واقعی (Deploy / انتشار)
این جدا از Merge است! Merge فقط یعنی «داخل گیت‌هاب رسمی شد»، هنوز روی سایت واقعی pishtaj.ir ظاهر نشده. دو راه دارید:
1. **راه فعلی/دستی که تا الان استفاده می‌کردید:** یک ZIP از نسخهٔ نهایی گیت‌هاب می‌گیرید و در پنل هاست (cPanel) آپلود/Extract می‌کنید — دقیقاً مثل فایل‌های راهنمای نصبی که در پروژه موجود بود.
2. **راه بهتر (پیشنهاد من برای آینده):** یک اتصال خودکار بین گیت‌هاب و هاست شما وصل کنیم که با زدن یک دکمه، آخرین نسخهٔ تأییدشده مستقیم روی سایت برود — این کار یک‌بار نیاز به تنظیم دارد و بعد از آن، انتشار هر نسخه فقط چند ثانیه طول می‌کشد و هیچ عمل دستی/خطای انسانی (که باعث مشکلات قبلی شده بود) وجود نخواهد داشت.

**من به شما پیشنهاد می‌کنم فعلاً همان روش دستی را ادامه دهیم تا با کار من و ایجنت CRM راحت شوید، و بعد از چند مرحلهٔ موفق، گزینهٔ ۲ (اتوماتیک) را با هم راه‌اندازی کنیم.**

---

## ۷. اگر یک چیزی خراب شد چه کار کنیم؟ («Rollback» به زبان ساده)

فرض کنید یک نسخهٔ جدید را روی سایت گذاشتید و یک مشکل پیش آمد (مثلاً صفحه‌ای باز نمی‌شود). راه‌حل که برایتان آماده کرده‌ام:

- هر بار که یک نسخه را «کاملاً سالم و تست‌شده» تشخیص دادیم، من روی آن یک **برچسب (Tag)** می‌گذارم — مثل گذاشتن یک نقطهٔ نجات در یک بازی ویدیویی. اسمش چیزی مثل `stable-v33.0.3` است.
- اگر بعداً چیزی خراب شد، کافی است بگویید «برگرد به آخرین نقطهٔ سالم» — من (یا هر ایجنتی) دقیقاً همان نسخهٔ تگ‌خورده را دوباره برایتان آماده می‌کنم، بدون این‌که مجبور باشیم بگردیم ببینیم کدام فایل خراب شده. این یعنی حتی اگر یک اشتباه رخ دهد، حداکثر چند دقیقه طول می‌کشد تا به حالت قبلی برگردیم.

---

## ۸. خلاصهٔ خیلی خیلی کوتاه (اگر فقط یک پاراگراف بخوانید)

من الان در یک «اتاق کار» جدا روی کپی سایت شما کار می‌کنم؛ هیچ‌چیز روی سایت واقعی تغییر نمی‌کند. وقتی بخشی از کار (مثلاً سئو/محتوا) آماده شد، یک «درخواست تأیید» (Pull Request) با توضیح ساده برایتان می‌سازم؛ شما فقط می‌خوانید و با یک کلیک تأیید می‌کنید. یک ایجنت دیگر می‌تواند هم‌زمان در اتاق کاری جدا روی CRM کار کند، چون فایل‌های سایت و CRM از هم جدا هستند و به هم برخورد نمی‌کنند. بردن نسخهٔ نهایی به سایت واقعی (Deploy) یک قدم جدا و بعد از تأیید شماست، و برای هر مرحله یک «نقطهٔ نجات» نگه می‌داریم تا اگر مشکلی پیش آمد، فوراً و بی‌دردسر به حالت قبل برگردیم.

**سؤال من از شما:** آیا می‌خواهید همین الان کار روی فاز ۱ سئو (پاک‌سازی لینک‌های شکسته و ساختار صفحات) را شروع کنم، و بعد هر چند روز یک‌بار نتیجه را برایتان به‌صورت خلاصهٔ ساده گزارش بدهم؟
