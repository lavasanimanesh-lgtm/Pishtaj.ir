# فاز C1 — اندازه‌گیری: مجموعه‌های داغ و اولویت نازک‌سازی

تاریخ: ۱۴۰۵/۰۶/۵ (۲۰۲۶-۰۸-۲۷) | نسخه: v34.8.12

## ۱) تله‌متری زنده (منبع اصلی تصمیم)
از v34.8.12 سرور هر push را به تفکیک کلید می‌شمارد (`crm/data/sync/push_stats.json`):
- تعداد push، مجموع/میانگین بایت، تعداد تعارض و reject، آخرین کاربر/زمان
- دیدن گزارش (ادمین/رئیس): `POST/GET api/crm.php?action=sync_stats` با هدر `X-CRM-Token`
- **حداقل ۳–۷ روز کاری داده جمع شود تا اولویت قطعی شود** (همهٔ کاربران/دستگاه‌ها پوشش داده می‌شوند چون شمارش سمت سرور است)

## ۲) رتبه‌بندی ایستا (تحلیل کد — تعداد مسیر نوشتن `setData` در ماژول‌ها)

| مجموعه | مسیر نوشتن | وضعیت فعلی | اولویت C3 |
|---|---|---|---|
| `ptf_crm_invoices` | ۲۹ | پوش آرایه‌ای + سپر duplicate | ۱ |
| `ptf_crm_rfqs` | ۲۸ | پوش آرایه‌ای | ۲ |
| `ptf_crm_deals` | ۲۸ | پوش آرایه‌ای + projection سروری جزئی | ۳ |
| `ptf_crm_products` | ۲۲ | پوش آرایه‌ای | ۴ |
| `ptf_crm_offers` | ۲۱ | پوش آرایه‌ای + سپر duplicate + snapshot برد | ۵ (پرخطرترین — آخر) |
| `ptf_crm_customers` / `suppliers` / `projects` | ۱۴/۱۳/۱۳ | پوش آرایه‌ای | ۶–۸ |
| `reminders` / `notifs` / `letters` / `leads` | ۱۰/۱۰/۸/۸ | پوش آرایه‌ای | ۹–۱۲ |
| `sharetx` / `shareholders` / `opex` | ۳/۱/۱ | **فرمان‌محور از v34.8.6** ✅ | انجام‌شده |

نتیجهٔ اولیه: **حقوق/مالی مهاجرتش کامل است؛ بقیهٔ صف پشت invoices/rfqs/deals/products است.** ترتیب نهایی با دادهٔ `sync_stats` بعد از یک هفته قفل می‌شود.

## ۳) بدهی داده (باید قبل از C3 بسته شود)
اجرای manifest اصلاحات مالی روی **پروداکشن** (الان با v34.8.11 ممکن است):
- مسیر: تنظیمات → ابزار کیفیت داده → «📋 manifest اصلاحات مالی» (ادمین/رئیس)
- خروجی: فهرست duplicate حقوق/اوپکس، جفت ناقص، ماه اشتباه برداشت‌ها، chair_in گم‌شده
- تصمیم canonical هر قلم با کارفرما → سپس ابزار ترمیم صریح ساخته می‌شود (فاز C2)

## ۴) قدم بعدی
- C2: زیرساخت فرمان عمومی (`entity_upsert/delete` + projection تعمیم‌یافته + پرچم per-module)
- شروع C2 پس از: (الف) یک هفته دادهٔ تله‌متری، (ب) تصمیم دادهٔ canonical از manifest
