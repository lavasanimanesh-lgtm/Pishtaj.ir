# RCA — پاک‌شدن «غیرممکنِ» تماس‌ها بر پایهٔ آخرین‌ثبتِ تب (CONTACT-STALE-HOOK-WIPE)

**تاریخ:** 2026-09-17
**نسخهٔ اصلاح:** v34.38.25
**گزارش کارفرما (به نقش رییس هیات مدیره):**
> «هر مشتری که مدیرعامل با شماره ثبت می‌کند، رکورد می‌ماند ولی شماره‌ها برای من ناپدید است؛
> مشتری‌هایی که خودم ثبت می‌کنم سالم‌اند.»

---

## ۱. قرارداد اثبات

اثبات فقط با **کد واقعی HEAD** انجام شد (بدون هیچ تغییر کد تا پیش از اثبات): بوت `vm` با ترتیب واقعی defer
(`crm/dedup.js` → `crm/offers.js` → `crm/phonefmt.js` → `crm/sales-domain-v2.js`)، DOM ساختگی کامل
(مسیر واقعی مودال: `showCustModal(cd)` → فیلدها/`_cbState` → `saveCust2(cd)`) و سرور واقعی‌نما با
معناشناسی دقیق `api/sales-domain.php` (`entity_upsert` با merge «کلید حاضر بازنویسی»، `expectCreate`/409،
`data_pull` per-key).

زنجیرهٔ بازتولید‌شده (فایل آزمون: diag خارج از ریپو، نسخهٔ رسمی‌شده در `tester660` §۴):

| گام | عمل | نتیجهٔ سرور |
|---|---|---|
| ① | مدیرعامل مشتری M را با ۲ شماره ثبت می‌کند | M تماس‌دار ✓ و `_ptfLastSavedCustCd=M` روی تب مدیرعامل |
| ② | رییس (دستگاه دیگر) M را ویرایش می‌کند: موبایل `09362221100` + ایمیل `sales@arsalan.ir` اضافه | سرور: ۲ موبایل + ۲ ایمیل ✓ |
| ③ | مدیرعامل (تب کهنه، بدون pull) سعی می‌کند هم‌نام ثبت کند → **بلاک ضدتکرار** (US-174) | `saveCust2` زود برمی‌گردد؛ **ولی** یک `entity_upsert(cd=M, expectCreate:false)` از هوک phonefmt ارسال می‌شود |
| ④ | دستگاه سوم pull می‌کند | **موبایل و ایمیلِ اضافه‌شدهٔ رییس پاک شده** — امضای دقیق گزارش |

`apiLog` گام ③ عیناً: `[{"action":"entity_upsert","cd":"CUST-100","expectCreate":false}]`

## ۲. ریشه (مکانیزم دقیق)

`crm/phonefmt.js` دو هوک `hookCust`/`hookSup` دور `saveCust2`/`saveSup2` می‌پیچد. منطق قدیمی:

```js
window.saveCust2 = function (cd) {
  _s(cd);                                   // ← حتی اگر زود برگشته باشد
  var savedCd = cd || window._ptfLastSavedCustCd;
  if (!savedCd) return;
  var rec = getData('ptf_crm_customers').filter(x => x.cd === savedCd)[0];
  ptfNormalizeEntityPhones(rec, 'fa');
  ptfEntityUpsert('ptf_crm_customers', rec); // ← بازنویسی از کشِ محلیِ کهنه
};
```

سه حقیقت که ترکیبشان باگ می‌سازد:

1. **هوک بی‌قید و شرط اجرا می‌شد** — حتی وقتی `saveCust2` پیش از هر پایدارسازی زود برمی‌گشت
   (نام خالی، یا `ptfDupBlock` در `crm/dedup.js` که «ضد تکرار» audit می‌کند و `true` برمی‌گرداند).
2. **fallback به `_ptfLastSavedCustCd`** — نشانِ «آخرین ثبت موفق تب» (که در پایان مسیر موفقِ
   `saveCust2` ست می‌شود) در فراخوانیِ نامرتبطِ بعدی به‌کار می‌رفت.
3. **merge سرور «کلیدِ حاضر بازنویسی»** (قرارداد v34.8.34) — payload هوک شامل کلیدهای تماسیِ
   **کشِ محلی** است؛ هر تغییری که از آخرین pull آن تب، دستگاه دیگری روی سرور داده بود بازنویسی می‌شد.

### چرا «نقش‌محور» به نظر می‌رسید ولی نبود؟

هیچ تبعیض نقشی در کار نیست. کلیدِ پنهان این است که **مدیرعامل معمولاً ثبت‌کننده است**:
- ثبت‌های *خودِ* مدیرعامل از کشِ تماس‌دارِ خودش سرو می‌شود ⇒ «برای من سالم است».
- ثبتِ آخرِ تبِ مدیرعامل = رکوردی که رییس بعدها ویرایش/تکمیلش کرده (سناریوی واقعی دفتر) ⇒
  اولین ذخیرهٔ زودبرگشتیِ بعدیِ مدیرعامل (دوبار‌کلیک روی فرم خالی، ثبت هم‌نام، …) همان رکورد را
  از کش کهنه بازنویسی می‌کند ⇒ «مالِ او خالی دیده می‌شود».
- اگر رییس ثبت‌کنندهٔ آخر می‌بود، جهتِ آسیب برعکس می‌شد (و همین را کارفرما در ثبت‌های خودش نمی‌دید).

## ۳. مسیرهای فعال‌ساز در عمل (همه رایج)

- دوبار‌کلیک/دوباره‌ثبتِ هم‌نام → بلاک ضدتکرار (US-174) → هوکِ زودبرگشتی.
- ثبت با نام خالی (اعتبارسنجی) → زودبرگشتی → هوک.
- برخورد کد (409) → `retryWithFreshCode` کد محلی را عوض می‌کند؛ upsert پنهانِ هوک با کدِ *قبلی*
  رکورد دیگری (صاحبِ آن کد روی سرور) را بازنویسی می‌کرد — در آزمون، رکورد بی‌گناهِ «شرکت تصادفی».

## ۴. اصلاح v34.38.25 (سه‌لایه)

1. **`crm/offers.js` — وظیفهٔ واقعی هوک به پیش از پایدارسازی منتقل شد.** نرمال‌سازی/قالب‌بندی
   شماره‌ها (`ptfNormalizeEntityPhones`) داخل `saveCust2`/`saveSup2` و **پیش از** `ptfDupBlock`
   و پیش از ساخت payload اجرا می‌شود ⇒ فرمان اصلی همیشه دادهٔ نهایی را می‌برد و مسیر retry برخورد کد
   هم نرمال است. حالت «خارجی» تامین‌کننده حالا از فرم (`isForeign`) خوانده می‌شود — شاخهٔ EN هوک قدیمی
   چون `rec.origin` هرگز ست نمی‌شد عملاً مرده بود.
2. **`crm/phonefmt.js` — قرارداد تازهٔ هوک.** هوک فقط برای «ویرایش صریح» (`cd` مشخص و برابر مهر جاری)
   یک upsert تک‌رکوردیِ idempotent تایید می‌کند. ثبتِ جدید (`cd=null`) و ذخیرهٔ زودبرگشتی **هیچ فرمان
   شبکه‌ای ندارند** — دیگر «آخرین رکورد تب» از هیچ مسیری بازنویسی نمی‌شود.
   *بند ۲.۹ قدیمی tester604 عمداً به‌روز شد* (قرارداد قدیمی عین ریشه بود).
3. **`api/sales-domain.php` — invariant سمت سرور (CONTACT-WIPE).** در `entity_upsert` اگر رکورد
   ذخیره‌شده تماس دارد و payload «هر کلید تماسی که می‌آورد» خالی است، پاک‌سازی **نیت صریح** می‌خواهد:
   `rec._ccClear = 1` از کلاینتِ جاری (مسیر ویرایشِ آگاه؛ فیلد پیش از ذخیره حذف می‌شود). در غیر این صورت
   مقادیر قبلی حفظ و `sanitize.contactsWipeBlocked` شمرده می‌شود. این لایه کلاسِ کامل «پاک‌سازی تماس
   توسط کلاینت کهنه/معرور» را برای *هر* نویسنده‌ای می‌بندد (سوپاپ v34.38.7-①/②/③ را هم بالادستی خفه می‌کند).

سازگاری: مودال واقعی هنگام خالی‌کردن فرم، تماس‌های قبلی را حفظ می‌کند (R3، v34.38.6) ⇒ رفتار کاربرِ
عادی تغییر نمی‌کند؛ `_ccClear` فقط برای نویسنده‌ای است که واقعاً رکوردِ بی‌تماس می‌سازد.
`custmerge` (تجمیع keep/drop) فقط تجمیع/غنی‌سازی است و با invariant تداخل ندارد.

## ۵. قفل‌ها

- **`_tools/uat/tester660-v34.38.25-contact-stale-hook-wipe.js`** (۳۷ سنجه، سبز؛ ثبت در `run-ci-gate.js`):
  - §۱ ایستا: مهرهای اصلاح در سورس، قرارداد ساخت هوک (فقط `ptfEntityUpsert`؛ بدون `items[0]`/`SaveCollection`)،
    سیم گارد سرور، سینتکس PHP با `php-parser`، پین نسخه‌ها.
  - §۲ گارد سرور: پورت ۱:۱ توابع PHP — حفظ در برابر «خالیِ حاضر»، مجوز با `_ccClear`، عبورِ محتوای تازه،
    احترام به «کلید غایب = تغییرنکرده»، بی‌آسیبی stub بدون تماس، هر دو شکل حقیقی/حقوقی.
  - §۳/§۴/§۵/§۶ رفتاری با بوت واقعی: ثبت جدید = فقط یک فرمان؛ ویرایش = فرمان‌های هم‌cd؛ زنجیرهٔ کامل
    سه‌دستگاههٔ گزارش (ثبت مدیرعامل ← ویرایش رییس ← بلاک ضدتکرار ← **صفر فرمان پنهان** ← تماس‌ها سالم
    برای دستگاه سوم)؛ زنجیرهٔ مشابه تامین‌کننده؛ قالب‌بندی بین‌المللیِ خارجی پیش از payload.
  - §۷ سرورِ گارددار: upsert خالیِ کلاینت کهنه → حفظ؛ مودال خالی‌کننده → حفظ؛ مهر → پاک‌سازی مشروع.
- **tester604** بندهای ۲.۹/۲.۱۱ به قرارداد جدید (بند ۲ بالا) به‌روز و مستند شد — بقیهٔ ۴۴ سنجه سبز ماند.
- گیت کامل: `node _tools/uat/run-ci-gate.js`
