# Release Notes v34.7.18 — یکپارچگی وصولی: مطالبات، حساب مشتری و پرونده یک عدد می‌دهند

تاریخ: ۱۴۰۵/۰۵/۲۵ (2026-08-16)

## زمینه

گزارش کارفرما: «مشتری مبلغی پرداخت کرده؛ رقم در ردیف دیده می‌شود اما از مطالبات باز کسر نشده و کل فاکتور همچنان باز است.»
ریشه‌یابی کامل در `ARENA-AR-RECEIPT-INTEGRATION-RCA-2026-08-16.md` انجام شد: سیستم سه مسیر نوشتن پول و دو مدل حساب‌کردن مانده داشت که با هم تسویه نمی‌شدند. این نسخه **فاز ۰ تا ۴** همان طرح را اجرا می‌کند.

اصل حاکم بر این نسخه: **وفاداری کامل به معماری موجود.** هیچ مسیر کاری حذف نشد، هیچ رکوردی پاک نشد، هیچ مهاجرت دادهٔ اجباری اجرا نشد؛ فقط قواعد معیوبِ محاسبه اصلاح و نماها به یک منبع وصل شدند. هر تغییر رفتاری، fallback رفتار قبلی را نگه داشته است.

## فاز ۰ — تشخیص و شفافیت (بدون تغییر داده)

- فایل جدید `crm/ar-reconcile.js` — لایهٔ فقط‌خواندنی `PTF.ar` (بدون وابستگی DOM، قابل تست در Node).
- گزارش «🧮 تسویهٔ مطالبات» با هشت دستهٔ صریح: ارزش‌افزودهٔ تخصیص‌نیافته، فاکتور بدون پرونده، ناسازگاری `amount` با `base+vat`، وصولی میراثی بدون رسید، تخصیص کهنه، فاکتور `superseded`، بستانکاری بلااستفاده، احتمال ثبت دوبارهٔ یک پول.
- دسترسی: دکمه در پنل «مطالبات» و در «کیفیت دادهٔ مالی». هر ردیف با مبلغ و ارجاع؛ اقدام‌ها فقط با تأیید صریح کاربر.

## فاز ۱ — اصلاح موتور تخصیص (سرور)

`api/sales-domain.php`
- **R1 — ارزش‌افزودهٔ گیرافتاده:** مجوز تخصیص VAT دیگر از فیلد منجمد `timing` خوانده نمی‌شود. پیش از این، پیش‌پرداختی که قبل از صدور فاکتور دریافت شده بود برای همیشه `pre_invoice` می‌ماند و سهم VAT هرگز تخصیص نمی‌گرفت؛ نتیجه این بود که مشتری کل مبلغ را پرداخت می‌کرد ولی **دقیقاً به اندازهٔ VAT** برای همیشه «مطالبهٔ باز» می‌ماند و همان مبلغ هم‌زمان «بستانکاری» نشان داده می‌شد. اکنون مجوز به‌ازای هر جفت (دریافت، فاکتور) و بر مبنای واقعیتِ لحظهٔ بازسازی تعیین می‌شود و `timing` هم برای گزارش‌ها به‌روز می‌گردد.
- **R3 — سقف تخصیص:** تابع جدید `sd_invoice_caps()` سقف را برابر **مبلغ قطعی فاکتور (`amount`)** می‌گذارد (سهم VAT حداکثر تا `vat` ثبت‌شده، بقیه `base`). پیش از این تخصیص روی `base+vat` انجام می‌شد و در فاکتور غیررسمیِ دارای تخفیف، پنل مطالبات «تسویه» و پنجرهٔ پرونده «باز» نشان می‌داد.
- **R2 — فاکتور بی‌پرونده:** `sd_bind_orphan_invoices()` فاکتورهای بدون `caseId` را فقط با **تطبیق یکتای شمارهٔ پیشنهاد** به پرونده متصل می‌کند؛ اگر همان شماره به بیش از یک پرونده بخورد، هیچ اتصالی انجام نمی‌شود. بدون این اتصال، دریافت پرونده هرگز از فاکتور کسر نمی‌شد.
- **R9 — اتحادهای تسویه:** `sd_reconcile_case()` پس از هر بازسازی، `Σدریافت = Σتخصیص + Σبستانکاری`، هم‌خوانی ردیف‌های تخصیص و `amount = base+vat` را می‌سنجد و نتیجه در `result.reconcile` هر فرمان برمی‌گردد. صرفاً گزارشی است و مسیر نوشتن را تغییر نمی‌دهد.
- `openAmountIRR/openBaseIRR/openVatIRR` مثل قبل تولید می‌شوند (سازگاری عقب‌رو کامل).

## فاز ۲ — یکپارچه‌سازی مسیرهای نوشتن (بدون حذف مسیر موجود)

- **R10 — ابطال چک:** `ptfChequeReverseReceived` دیگر رکورد وصولی را **حذف فیزیکی** نمی‌کند؛ آن را `void/voided` با دلیل و مهر زمان علامت می‌زند. اثر مالی دقیقاً مثل قبل صفر می‌شود (`PTF.isPaymentActive` و `PTF.ar` رکورد ابطالی را نمی‌شمارند) اما تاریخچه و قابلیت ممیزی حفظ می‌شود.
- **برچسب منبع:** وصولی‌های مسیر مطالبات (`sourcePath:'legacy_receivables'`) و مسیر چک (`sourcePath:'cheque_module'`) علامت‌گذاری می‌شوند تا گزارش تسویه آن‌ها را طبقه‌بندی کند و «احتمال ثبت دوبارهٔ یک پول» قابل کشف باشد. رفتار این مسیرها تغییر نکرده است.
- **R7 — انتقال بستانکاری:** در فرم «اصلاح دریافت» فیلد «پروندهٔ مقصد» اضافه شد؛ انتقال فقط بین پرونده‌های **همان مشتری**، از مسیر رسمی اصلاح (سند ابطال + سند جدید) و با بازسازی تخصیص هر دو پرونده انجام می‌شود.
- **ابزار ترمیم:** «🔗 اتصال به پروندهٔ پیشنهادی» در گزارش تسویه، فقط برای تطبیق یکتا و با تأیید کاربر، فقط `caseId` را روی فاکتور می‌نویسد (هیچ مبلغی تغییر نمی‌کند).

## فاز ۳ — یکپارچه‌سازی خواننده‌ها

همهٔ نماها اکنون از `PTF.ar` تغذیه می‌شوند و هر کدام fallback رفتار قبلی خود را نگه داشته‌اند:
- `crm/rbac.js` (پنل مطالبات) — مانده، جمع سررسید گذشته و بَج‌های «⏳ در انتظار همگام‌سازی» و «🔗 بدون پرونده».
- `crm/customer-finance.js` (حساب مشتری) — `paid()` و تعریف واحد «فاکتور فعال».
- `crm/sales-domain-v2.js` (پنجرهٔ مالی پرونده) — `caseTotals` از `PTF.ar.caseState`.
- `crm/finance-helpers.js` — `PTF.invPaidSum` به لایهٔ واحد وصل شد (با `opts.legacyOnly` برای مصرف‌کنندگان قدیمی).
- **R6 — پروجکشن نرسیده:** اگر تخصیص ذخیره‌شدهٔ سرور روی این دستگاه کهنه باشد، همان الگوریتم FIFO سرور **محلی** بازسازی می‌شود تا پول ناپدید نشود؛ هیچ رکوردی نوشته نمی‌شود و وضعیت با پرچم `stale` و بَج در UI اعلام می‌گردد. کش پس از هر پروجکشن سرور باطل می‌شود.
- **R8 — فاکتور `superseded`:** تعریف «رکورد فعال» بین سرور، مطالبات و حساب مشتری یکسان شد. پیش از این، صورتحساب غیررسمیِ جایگزین‌شده با فاکتور رسمی در «حساب مشتریان» **دوباره** بدهی می‌ساخت (در بازتولید: ۲٬۰۹۰م به‌جای ۱٬۰۹۰م).
- اضافه‌پرداخت عمداً سقف‌گذاری نشد تا اعتبار مشتری مثل قبل محاسبه شود.

**بدون مهاجرت دادهٔ اجباری:** چون خواننده‌ها پرداخت‌های میراثی را دقیقاً مثل قبل می‌شمارند و تخصیص‌های ازدست‌رفته محلی بازسازی می‌شوند، هیچ تبدیل داده‌ای لازم نیست. تبدیل تدریجی از مسیر موجود `migration_apply_safe` و با تأیید مدیر باقی می‌ماند.

## فاز ۴ — گارد و پایش

- تستر جدید `_tools/uat/tester421-v34.7.18-ar-receipt-integrity.js`: **38 PASS / 0 FAIL** (اجرای واقعی ماژول‌ها در sandbox + بررسی قواعد سرور).
- هر دو تستر یکپارچگی (`tester420` و `tester421`) به `_tools/uat/run-ci-gate.js` اضافه شدند و فایل‌های جدید به فهرست بررسی نحوی گیت وارد شدند.
- هارنس بازتولید `_tools/audit/repro-receivables-payment-integration.js` به موتور اصلاح‌شده به‌روز شد: **۵ سناریو از ۵ سناریو هم‌خوان و درست**.

## نتیجهٔ عددی (همان سناریوهای گزارش)

| سناریو | قبل | بعد |
|---|---|---|
| پرداخت کامل پیش از صدور فاکتور | مانده ۹۰م + بستانکاری ۹۰م | **۰** در هر سه نما |
| فاکتور بدون `caseId` (پول ۵۰۰م) | مطالبات ۱٬۲۰۰م باز، وصولی ۰ | **۷۰۰م** در هر سه نما |
| تخصیص نرسیده به دستگاه | ۱٬۱۰۰م باز | **۷۰۰م** + بَج «در انتظار همگام‌سازی» |
| فاکتور غیررسمی با تخفیف | مطالبات ۰ / پرونده ۱۰۰م | **۰** در هر سه نما |
| صورتحساب `superseded` | بدهی مشتری ۲٬۰۹۰م | **۱٬۰۹۰م** |

## کنترل کیفیت و ایمنی

- `node --check` روی همهٔ فایل‌های JS تغییریافته: بدون خطا. بررسی ساختاری `api/sales-domain.php` (توازن بلوک‌ها/رشته‌ها) پیش و پس از تغییر یکسان و سالم.
- اجرای کامل سوئیت (۴۰۴ تستر) و مقایسهٔ خط‌به‌خط با baseline پیش از تغییر: **هیچ شکست جدیدی ایجاد نشد** (۶۲ شکست از قبل موجود و بی‌ارتباط؛ دو شکست موجود در CI-gate — `tester403` و `tester415` — پیش از این تغییرات هم دقیقاً همین‌طور بودند).
- بهداشت نسخه: `index.html`، `sw.js` (شامل precache فایل جدید)، `manifest.json`، `clear-cache.html`، `shell.js` و `VERSION.json` همگی روی `v34.7.18`.
