# 🚀 ریلیز v89.0 — اسپرینت ۸۹: گردش کار خودکار درخواست + انگلیسی‌سازی خودکار نام‌ها

**تاریخ:** ۱۶ تیر ۱۴۰۵ | **دامنه:** US-204 + US-205 (ماژول جدید `workflow.js`)

---

## 🔄 US-204 — ماشین وضعیت خودکار درخواست (طراحی متخصص ارشد سیستم)

### فلوچارت نهایی (پس از رفع ابهام‌های جریان پیشنهادی شما)

```
📥 دریافت اولیه
   └─ صدور TO ──▶ 🔧 پیشنهاد فنی صادر شد
        └─ ارسال TO ──▶ ⏳ منتظر پاسخ کارفرما (فنی)
             ├─ ✅ تایید فنی ──▶ 💰 منتظر صدور پیشنهاد مالی
             ├─ ✏️ درخواست اصلاح ──▶ در حال صدور پیشنهاد اصلاحی ─(ارسال مجدد)─▶ ⏳
             └─ ⛔ عدم تایید ──▶ 🗂 بایگانی (عدم تایید فنی)
   صدور CO ──▶ 💵 پیشنهاد مالی صادر شد
        └─ ارسال CO ──▶ ⏳ منتظر پاسخ کارفرما (مالی)
             ├─ 🏆 برنده ──▶ 🏗 در حال تامین ──▶ 📦 تحویل شده ──▶ ✅ مختومه (تسویه)
             ├─ ✏️ درخواست اصلاح ──▶ در حال اصلاح مالی ─(ارسال مجدد)─▶ ⏳
             └─ ❌ بازنده ──▶ 🗂 بایگانی (بازنده مالی)
```

### اصول پیاده‌سازی (ضد باگ و ضد خلل)
- **وضعیت درخواست ۱۰۰٪ خودکار** از رویدادها محاسبه می‌شود: صدور/ارسال پیشنهاد، پاسخ کارفرما، تشکیل پرونده، تحویل کامل پکینگ، تسویه فاکتور. **انتخاب دستی وضعیت حذف شد.**
- تنها ورودی دستی، **«پاسخ کارفرما»** است (چون رویداد بیرونی است): دکمه «📨 پاسخ کارفرما» روی TO ارسال‌شده با سه گزینه — TO طبق نظر شما **برنده/بازنده ندارد**: ✅ تایید فنی / ✏️ درخواست اصلاح / ⛔ عدم تایید. برای CO ارسال‌شده هم دکمه «✏️ درخواست اصلاح» اضافه شد (کنار برنده/بازنده موجود).
- **درخواست اصلاح → سند خودکار برای ویرایش باز می‌شود** و وضعیت درخواست «در حال صدور اصلاحی»؛ پس از ویرایش و ارسال مجدد، دوباره «منتظر پاسخ».
- کلیک روی وضعیت هر درخواست → **تایم‌لاین کامل گردش کار** (چه کسی، کی، چه رویدادی) — بدون امکان دستکاری.
- **رکوردهای قدیمی خراب نمی‌شوند**: وضعیت‌شان از همین موتور بازمحاسبه می‌شود؛ قفل برنده/پرونده خودکار/پیش‌پرداخت v70-v80 دست‌نخورده سرجایش است.
- تحویل → از پرونده پروژه (تحویل کامل/بسته) و مختومه → از تسویه کامل فاکتور خوانده می‌شود.

### + تب‌های مدارک فرم ثبت درخواست
نشان «＋» برجسته بنفش روی هر ۵ تب آپلود (فایل استعلام/دیتاشیت/عکس/نقشه/سایر) تا افزودن مدرک واضح باشد.

## 🌐 US-205 — انگلیسی‌سازی خودکار نام‌ها (بدون دخالت کاربر)

- هنگام چاپ TO/CO، اگر نام مشتری/رابط فارسی باشد، **خودکار لاتین می‌شود**: «آقای هوری بیگی» → `Mr. Hoori Beigi`، «شیوا کریمی» → `Ms. Shiva Karimi` (تشخیص جنسیت از لقب یا بانک ۵۵ نام زنانه رایج)، «شرکت پتروشیمی پارس» → `Petroshimi Pars Co.`
- دو لایه: **ترانویسی قاعده‌مند آفلاین** (همیشه کار می‌کند، حتی بدون AI) + اگر AI فعال باشد، ترجمه دقیق‌تر در پس‌زمینه گرفته و در «نام انگلیسی» مشتری ذخیره می‌شود تا دفعات بعد از همان استفاده شود.
- نام‌های از قبل لاتین دست نمی‌خورند.

## ✅ کیفیت
tester30 جدید: **27/27 PASS** — شامل **۱۲ تست رفتاری کامل ماشین وضعیت** (همه مسیرهای فلوچارت: WF10→…→WF85 + بایگانی‌ها + اصلاح‌ها) و ۵ تست انگلیسی‌سازی | رگرسیون ۲۱ تستر همه PASS | ممیز ۸بخشی PASS | کش → `ptf-crm-v89.0`

## 📦 نصب (File Manager)
**`pishtaj-release-v89.0-delta.zip`**: `crm/workflow.js` (جدید)، `crm/offers.js`، `crm/index.html`، `crm/sw.js` + مستندات.

> 🧪 تست: ① درخواستی ثبت کنید → وضعیت «دریافت اولیه» ② TO بسازید → خودکار «پیشنهاد فنی صادر شد» ③ TO را «ارسال‌شده» کنید → «منتظر پاسخ کارفرما» ④ دکمه 📨 → «درخواست اصلاح» → TO باز می‌شود و وضعیت «در حال صدور اصلاحی» ⑤ کلیک روی وضعیت → تایم‌لاین ⑥ چاپ CO مشتری فارسی → نام لاتین با Mr./Ms.
