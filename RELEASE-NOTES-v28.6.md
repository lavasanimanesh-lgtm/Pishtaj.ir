# ریلیزنوت `v28.6` — Rollback اضطراری چاپ پیشنهاد

## علت rollback
پس از UAT واقعی `v28.5`، کارفرما گزارش کرد که pagination اجباری باعث شد:
- شمارهٔ ردیف‌ها در هر صفحه از ابتدا شروع شوند؛
- فضای خالی غیرقابل‌قبول در انتهای صفحات ایجاد شود؛
- بخش Terms & Conditions از جدول فاصلهٔ زیاد بگیرد.

## اقدام انجام‌شده
- فقط تغییرات pagination چاپی `v28.5` در `crm/offers-pro.js` rollback شد.
- جدول چاپی به مدل پیشین یکپارچه بازگشت؛ شمارهٔ ردیف‌ها دوباره در کل پیشنهاد پیوسته هستند.
- hotfix سود CO در `offerlock.js` از `v28.4` دست‌نخورده باقی مانده است.
- هیچ دادهٔ پیشنهاد، قیمت، فاکتور یا دادهٔ مالی تغییر نکرده است.

## وضعیت ایراد سربرگ چندصفحه‌ای
مشکل اصلی سربرگ چندصفحه‌ای هنوز باز است، اما در این بسته عمداً اصلاح جدیدی روی آن اعمال نشده است. رفع نهایی آن باید در یک Sprint مستقل چاپ، با PDF نمونهٔ واقعی، قبل/بعد بصری و تایید کارفرما انجام شود.

## تست‌ها
- `tester160-v286-offer-print-rollback.js`: **7 PASS / 0 FAIL**
  - حذف pagination اجباری
  - بازگشت جدول منبع واحد
  - حفظ شمارهٔ ردیف ۱۳ در جدول واحد
- `tester158-v284-co-margin-hotfix.js`: **6 PASS / 0 FAIL**
- `tester66-v146.js`: **42 PASS / 0 FAIL**
- parse همهٔ 67 اسکریپت: موفق.
- `audit.py`: بدون خطا؛ فقط یک هشدار قدیمی دربارهٔ حجم 5 تصویر.

### شفافیت baseline
`tester62-v142.js`: **36 PASS / 3 FAIL**. سه FAIL مربوط به تلفن/شرایط چاپی قدیمی هستند که در rollback حاضر تغییر نکرده‌اند و به‌عنوان بدهی فنی مستقل باقی می‌مانند.

## UAT پیشنهادی
1. همان پیشنهاد چندصفحه‌ای را با قالب قبلی چاپ/دانلود کنید.
2. بررسی کنید شمارهٔ ردیف‌ها از ابتدا تا انتهای جدول پیوسته است.
3. بررسی کنید فاصلهٔ انتهای جدول تا Terms & Conditions به حالت پیشین بازگشته است.
4. یک CO را باز کنید و باکس سود `v28.4` را کنترل کنید تا مطمئن شوید rollback چاپ آن را حذف نکرده است.
