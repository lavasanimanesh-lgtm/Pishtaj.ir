# 📦 ریلیزنوت v13.7 — قیمت مرجع خودکار، ساده‌سازی منو، مودال موبایل، یکسان‌سازی شماره‌ها

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v13.6

---

## 🛒 بررسی تیم متخصص: ماژول «قیمت‌های خرید» (US-336)

**نظر تیم — حق با شماست:** این ماژول (میراث v84) با «درخواست تامین» هم‌پوشانی کامل دارد؛ ثبت قیمت تامین‌کنندگان در درخواست تامین انجام می‌شود و نگهداری دو مسیر موازی ضد چابکی است. **اجرا شد:**
- «قیمت‌های خرید» از منو (سایدبار + گروه تامین) **حذف شد**
- داده‌های موجود آن محفوظ‌اند و لینک‌های قدیمی همچنان کار می‌کنند (حذف نرم — بدون ریسک داده)
- تنها قابلیت منحصربه‌فردش (مقایسه دور ۱/۲) در بک‌لاگ ماند تا در صورت نیاز به درخواست تامین منتقل شود

## 💰 US-335 — قیمت مرجع خودکار کالا

با ثبت هر قیمت در «درخواست تامین»:
- قیمت (یا **میانگین قیمت‌های** چند تامین‌کننده) به‌ازای هر قلم محاسبه و به‌عنوان **قیمت مرجع** در ماژول کالا ثبت می‌شود
- **با قید تاریخ و منبع**: زیر قیمت در جدول کالاها «مرجع: ۱۴۰۵/۴/۱۶» نمایش داده می‌شود و hover منبع را نشان می‌دهد (مثلاً «درخواست تامین PTF-RFQS-1405-003 — میانگین ۲ قیمت»)
- هر پاسخ جدید، مرجع را به‌روز می‌کند + ثبت در audit + toast اطلاع‌رسانی

## 📱 US-337 — یکسان‌سازی دکمه‌های مودال در موبایل

بررسی عمیق ریشه به‌هم‌ریختگی (اسکرین‌شات شما): تداخل سه لایه دکمه (مک‌ها + چیپ دستیار + دکمه‌های بزرگ‌شده موبایل). اصلاح سراسری با اولویت سهولت کاربری:
- فضای اختصاصی بالای همه مودال‌ها برای دکمه‌های مک + چیپ دستیار (بدون روی‌هم‌افتادگی با تیتر)
- فرم‌های دوستونه در موبایل → تک‌ستون (فیلدها دیگر له نمی‌شوند)
- دکمه‌های پایانی مودال (انصراف/ثبت/پیش‌نمایش) → **تمام‌عرض و زیر هم** با دکمه اصلی در پایین (الگوی اپ‌های بومی)
- اعمال سراسری روی همه مودال‌های نرم‌افزار (قاعده CSS مرکزی، نه وصله موردی)

## ☎️ US-338 — یکسان‌سازی شماره تماس‌ها (مصوبه تیم چابکی: بدون گره، پیاده‌سازی کامل)

ماژول مرکزی جدید `phonefmt.js`:
| نوع | قاعده | مثال |
|---|---|---|
| مشتریان + تامین‌کننده داخلی + دفترچه تلفن | **ارقام فارسی، قالب ملی** | +989123456789 → ۰۹۱۲۳۴۵۶۷۸۹ |
| تامین‌کننده خارجی | **کل مشخصات لاتین، قالب بین‌المللی** | 00905321234567 → +905321234567؛ نام/دسته/آدرس هم لاتین‌سازی ارقام |

- **تبدیل خودکار در ثبت:** کاربر هر قالبی وارد کند (فارسی/لاتین/+98/0098/بدون صفر)، سیستم بر اساس داخلی/خارجی بودن، خودکار فارسی‌سازی یا انگلیسی‌سازی می‌کند — همان خواسته شما
- **مهاجرت یک‌باره:** همه رکوردهای موجود (مشتریان/تامین‌کنندگان/دفترچه پیامکی) در اولین اجرا یکدست می‌شوند (ریشه ناهمگونی اسکرین‌شات: ۰۹۳۹… فارسی، 0912 لاتین، +9891 بین‌المللی — همه یکسان می‌شوند)

## 🧪 کیفیت
تستر جدید `tester57` ۲۵/۲۵ (شامل ۳ تست رفتاری نرمال‌ساز) ✅ | رگرسیون **۴۴ تستر** ✅ | audit صفر خطا ✅

## 📁 فایل‌های تغییریافته
`crm/phonefmt.js` (جدید) · `crm/rfqsmart.js` (قیمت مرجع) · `crm/index.html` (حذف منو + CSS موبایل + نمایش مرجع + VER v13.7) · `crm/shell.js` (گروه تامین) · `crm/sw.js` · `_tools/uat/tester57` (جدید)
