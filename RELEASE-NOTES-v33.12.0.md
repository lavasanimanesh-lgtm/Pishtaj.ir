# یادداشت‌های انتشار (Release Notes — Version 33.12.0)

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (PR #11 باز تا پایان مهاجرت MySQL)
**تمرکز ریلیز:** ① ریشه‌یابی عمیق «از بین رفتن اعتبار مشتری» (مرجوعی‌های قدیمی بدون invoiceCd گم می‌شدند) + ترمیم خودکار + ابزار تشخیص ② «برنامه‌ریزی فصلی مالیات» فقط در تب سال مالی ③ دارک‌مود باکس نقدینگی تامین/چک.

---

## 🚀 دستاوردهای کلیدی نسخه ۳۳.۱۲.۰

### ۱. ریشه‌یابی عمیق «اعتبار مشتری از بین رفته» (BUG-CREDIT)
**ریشه (تأیید با شبیه‌سازی):** اعتبار مشتری به‌صورت زنده از «وصولی + مرجوعی − مبلغ فاکتور» محاسبه می‌شود؛ اما تطبیق مرجوعی با فاکتور فقط `r.invoiceCd === invoice.cd` بود. **مرجوعی‌های قدیمی (یا ثبت‌شده از مسیرهای دیگر) که `invoiceCd` ندارند (فقط `offerNo`/`invoiceNo` دارند) هرگز پیدا نمی‌شدند** → `returnedAmount=0` → اعتبار صفر — در حالی که کالای مرجوعی در موجودی (stock) وجود داشت.

**رفع:**
- `salesReturnsForInvoice(invoice)` مقاوم شد: ① `invoiceCd` مستقیم ② fallback با `invoiceNo` ③ fallback با `offerNo` فقط وقتی همان پیشنهاد **یک فاکتور غیرvoid یکتا** دارد (بدون دوباره‌شماری).
- `cfRepairReturnLinks()`: ترمیم خودکار در بوت — مرجوعی‌های بدون `invoiceCd` با همین قواعد به فاکتور پیوند می‌خورند (`repairedAt` + audit؛ idempotent).
- `cfCreditAudit(cd)`: گزارش اجزای اعتبار هر مشتری (فاکتور → مبلغ/وصولی/مرجوعی/اعتبار سهم + مرجوعی‌های بدون پیوند/یتیم) — برای پیگیری بعدی.

### ۲. «داشبورد برنامه‌ریزی فصلی مالیات» فقط در تب سال مالی
- باکس `ptfTaxPlannerBox` قبلاً در هیچ تبی مخفی نمی‌شد → در **همهٔ تب‌های هاب مالی** دیده می‌شد. حالا فقط در تب **«سال مالی»** نمایش داده می‌شود (+ در `finHubOrder`).

### ۳. دارک‌مود باکس‌های با پس‌زمینهٔ روشن
- باکس «تعهدات نقدینگی تأمین و چک‌های شرکت» (`slLiquidity`) و «برنامه‌ریزی فصلی مالیات» پس‌زمینهٔ روشن ثابت داشتند و در **نمای شب دیده نمی‌شدند** → استایل `body.ptf-dark` برای هر دو اضافه شد (پس‌زمینه/متن/کارت‌ها/اینپوت‌ها).

---

## 📋 تغییرات فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/customer-finance.js` | تطبیق مقاوم مرجوعی↔فاکتور + `cfRepairReturnLinks` (بوت) + `cfCreditAudit` |
| `crm/financehub.js` | `ptfTaxPlannerBox` فقط در تب fiscal + finHubOrder + دارک‌مود slLiquidity/taxplanner |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.12.0 |
| `_tools/uat/tester292-credit-repair.js` | **جدید** (۹/۰) |
| `RELEASE-NOTES-v33.12.0.md` / `REGRESSION-REPORT-v33.12.0.md` | اسناد انتشار |
| `_audit/HANDOFF-NEXT-SESSION-2026-08-01.md`، `crm/BACKLOG-USER-2026-08-01.md` | به‌روزرسانی |

---

## ⚠️ برای کاربر
1. `Ctrl+Shift+R` یا `crm/clear-cache.html` (نسخه باید **v33.12.0** باشد).
2. اعتبار مشتریانی که مرجوعی قدیمی دارند، با باز شدن CRM خودکار ترمیم و دوباره نمایش داده می‌شود (در صورت ابهام: «حساب مشتریان» → باز کردن حساب مشتری → اعتبار).
3. «برنامه‌ریزی فصلی مالیات» فقط در تب «سال مالی» هاب مالی دیده می‌شود.
