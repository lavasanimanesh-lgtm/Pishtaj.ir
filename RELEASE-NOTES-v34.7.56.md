# RELEASE NOTES — v34.7.56 (۱۴۰۵/۰۵/۲۹ — 2026-08-20)

## ۱) رفع BUG-OFFER-DUP-SKIP-267 — حذف کاذب اقلام هم‌محتوا در بارگذاری از درخواست

### گزارش کاربر

درخواستی با **۲۶۷ ردیف** ثبت شد؛ «بارگذاری از درخواست» در مودال پیشنهاد مالی پیام داد
«۶ قلم تکراری رد شد» و فقط ۲۶۱ قلم اضافه کرد.

### ریشه

«تکراری» با **امضای محتوایی** (`offItemKey`: کد|نام|شرح|مدل|برند|تعداد|واحد) تشخیص داده
می‌شد، در حالی که یک درخواست واقعی می‌تواند چند ردیف کاملاً هم‌محتوا داشته باشد (برای
تگ/ساب‌پروژه‌های مختلف). این dedup محتوایی در **سه لایه** تکرار شده بود:

1. `offAppendInqRows` — رد کردن با پیام «قلم تکراری» (همان ۶ قلم گم‌شده)،
2. `offSmartInsert` — درج idempotent با همان کلید (حذف بی‌صدا)،
3. `offDedupeOfferItems` — پاکسازی خودکار هنگام ذخیره (حتی اگر دو لایهٔ قبلی رد می‌شدند).

### اصلاح (هر سه لایه، هماهنگ)

- تعریف جدید «تکراری» = **همان ردیفِ مبدأ** (`sourceInq` + `sourceItemKey` + نوبت تکرار
  `dupOrdinal`) که قبلاً به پیشنهاد اضافه شده — مثل دوبار زدن دکمهٔ بارگذاری.
- `offAppendInqRows`: شمارش چندمجموعه‌ای (multiset) هویت مبدأ؛ ردیف‌های هم‌محتوای مشروع
  با `dupOrdinal` متمایز می‌شوند. برای پیشنهادهای قدیمی بدون هویت مبدأ، امضای محتوایی
  به‌عنوان fallback «مصرف» می‌شود تا بارگذاری مجدد دوبله نسازد.
- `offSmartInsert(item, force)`: وقتی تصمیم با هویت مبدأ گرفته شده، دیگر با کلید
  محتوایی حذف بی‌صدا نمی‌کند؛ مسیر بدون `force` (افزودن دستی) دست‌نخورده ماند.
- `offDedupeOfferItems`: کلید = محتوا + هویت خط (`id` ذخیره‌شده / مبدأ+نوبت / دستی)؛
  حفاظت قدیمی BUG-OFFER-DUP-ITEMS-001 در برابر تکرار دستی بی‌هویت حفظ شد.

### رفتار پس از اصلاح

- بارگذاری ۲۶۷ ردیف (با ۶ ردیف هم‌امضا): **هر ۲۶۷ قلم اضافه می‌شود.**
- زدن دوبارهٔ دکمه / بارگذاری مجدد همان درخواست: همچنان هیچ‌چیز دوبله نمی‌شود.
- ذخیرهٔ پیشنهاد: ردیف‌های هم‌محتوای مشروع حذف نمی‌شوند.

## ۲) قابلیت جدید: ورود قیمت (نرخ مرجع / قیمت واحد) از اکسل

برای فهرست‌های بلند، دکمهٔ **«💰 قیمت از اکسل»** به نوار ابزار اقلام فرم پیشنهاد اضافه شد:

1. **دانلود قالب با اقلام فعلی** — ستون‌ها: ردیف، کد کالا، نام، شرح، مدل، تعداد، واحد،
   نرخ مرجع (ریال)، قیمت واحد (ریال).
2. قیمت‌ها در اکسل پر می‌شود (ارقام فارسی، کاما و جداکننده پذیرفته است؛ خالی = بدون تغییر).
3. همان فایل برگردانده می‌شود؛ تطبیق به ترتیب: **شماره ردیف قالب → کد کالا → نام/شرح**
   (کد/نام تکراری به ترتیب رخداد تطبیق می‌خورد تا ردیف‌های هم‌محتوا هم قیمت بگیرند).

- فقط قیمت ردیف‌های **موجود** را پر می‌کند — قلم جدید نمی‌سازد.
- `refPriceEdited` ست می‌شود (روی نرخ مرجع کالا در ماژول کالا هم می‌نشیند — قرارداد v31.7.12)
  و درصد سود مثل ویرایش دستی هم‌راستا می‌شود (اگر فقط نرخ مرجع بدهید و ردیف ضریب سود
  داشته باشد، قیمت واحد خودکار ساخته می‌شود).
- خلاصهٔ نتیجه اعلام می‌شود: تعداد تطبیق، تعداد نرخ مرجع/قیمت، و سطرهای تطبیق‌نشده.

## تست

- تستر جدید `tester459-v34.7.56-offer-dup-and-price-xls.js` (در گیت) — سناریوی رفتاری
  کامل در sandbox: ۴ ردیف با ۲ هم‌محتوا → هر ۴ اضافه؛ بارگذاری مجدد → ۰ اضافه؛ ذخیره →
  حذف کاذب صفر؛ legacy → دوبله نمی‌شود؛ ورود قیمت با ارقام فارسی/کاما/ضریب سود.
- `tester272` به قرارداد جدید به‌روز شد + **رفع بدهی قدیمی هارنس آن**
  (`typeof offSmartInsert` بدون پیشوند window جایگزین نمی‌شد و سناریوی runtime از قبل
  روی main قرمز بود) → اکنون ۱۵/۰.
- **گیت CI: ۷۵ PASS / ۰ FAIL** · نگهبان معماری: PASS.

## UAT پیشنهادی

1. همان درخواست ۲۶۷ ردیفی را در پیشنهاد مالی بارگذاری کنید → باید «✅ ۲۶۷ قلم» بدون
   پیام تکراری اضافه شود؛ دکمه را دوباره بزنید → «۲۶۷ قلم تکراری رد شد» و جدول دوبله نشود.
2. «💰 قیمت از اکسل» → قالب را بگیرید، چند قیمت پر کنید (چندتا با ارقام فارسی)، برگردانید
   و جدول و درصد سود را کنترل کنید.
3. ذخیرهٔ پیشنهاد → پیام «ردیف تکراری حذف شد» نباید برای ردیف‌های مشروع ظاهر شود.
