# ریلیزنوت `v28.2` — Sprint 282: رفع فوری ذخیرهٔ پیشنهاد فنی

## خطای گزارش‌شده
هنگام ویرایش یک پیشنهاد فنی و فشردن «ذخیره»، پیام زیر نمایش داده می‌شد:

```text
خطا در ذخیره پیشنهاد: toStMigrate is not defined
```

## تحلیل ریشه‌ای
تابع `toStMigrate` برای سازگاری وضعیت‌های قدیمی پیشنهاد فنی (`won → approved` و `lost → rejected`) فقط در بدنهٔ `renderOffers()` تعریف شده بود. اما `offerSave()` در زمان ذخیرهٔ پیشنهاد موجود و محاسبهٔ Revision، همان تابع را در scope سراسری صدا می‌زد. بنابراین در مسیر ویرایش TO موجود، تابع در دسترس نبود و ذخیره پیش از `setData('ptf_crm_offers', …)` متوقف می‌شد.

## اصلاح اعمال‌شده
- `toStMigrate` به یک helper سراسری و یکتا منتقل شد و با `window.ptfToStMigrate` نیز در دسترس است.
- تعریف محلی تکراری از `renderOffers()` حذف شد تا رندر و ذخیره نتوانند در آینده منطق متفاوتی داشته باشند.
- منطق نگاشت قبلی بدون تغییر حفظ شده است:
  - TO قدیمی با `won` → `approved`
  - TO قدیمی با `lost` → `rejected`
  - سایر وضعیت‌ها بدون تغییر
- هیچ رکورد قبلی به‌صورت خودکار تغییر، migrate یا بازنویسی نشده است.

## کنترل‌های فنی
- `tester155-v282-to-save-scope.js`: **7 PASS / 0 FAIL**
  - اجرای واقعی `offerSave()` روی یک TO قدیمی با وضعیت `won`
  - عدم نمایش alert خطا
  - ذخیرهٔ تغییرات اقلام
  - ایجاد Revision جدید (`Rev.1`)
  - حفظ وضعیت فعلی انتخاب‌شدهٔ TO
- parse موفق ۶۷ اسکریپت بارگذاری‌شده از `crm/index.html`.
- `tester154-v281-finance-foundation.js`: **15 PASS / 0 FAIL**.
- `audit.py`: بدون خطا؛ تنها یک هشدار قدیمی دربارهٔ حجم ۵ تصویر.

> این‌ها تست فنی هستند؛ UAT مرورگر و داده‌های واقعی شما لازم است.

## UAT پیشنهادی
1. از دادهٔ پشتیبان یا یک پیشنهاد فنی غیرحساس، یک TO موجود را باز کنید.
2. یک شرح، مشخصات فنی یا تعداد را تغییر دهید و «ذخیره» را بزنید.
3. باید toast موفقیت ذخیره را ببینید و با بازکردن دوبارهٔ همان پیشنهاد، تغییر باقی مانده باشد.
4. در صورت TO قدیمی با وضعیت برنده/بازنده، ذخیره باید بدون خطای `toStMigrate` انجام شود و نگارش جدید مطابق گردش موجود ثبت شود.
5. یک TO جدید نیز ثبت/ذخیره کنید تا مسیر ایجاد اولیه بررسی شود.

## خارج از این hotfix
پیاده‌سازی «گردش بانک و صندوق» عمداً در این بسته آغاز نشده است و پس از تأیید این رفع‌اشکال ادامه خواهد یافت.
