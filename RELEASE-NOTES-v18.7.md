# 🚚 ریلیز v18.7 — US-430 «تعهد تحویل تامین‌کننده در خرید واقعی و امتیازدهی»

**درخواست کارفرما:** هنگام خرید، زمان تحویل تعهدشده توسط تامین‌کننده هم ثبت شود تا نظام امتیازدهی بتواند به آن نمره دهد.

## ✅ اجرا شد

### ۱) ثبت تاریخ تعهد تحویل هنگام خرید واقعی
در فرم `cmpBuy` دو فیلد جدید اضافه شد:

- تاریخ تحویل تعهدشده تامین‌کننده
- یادداشت تعهد تحویل تامین‌کننده

این داده‌ها هنگام ثبت خرید واقعی در purchase ذخیره می‌شوند:

```js
dueISO
dueNote
```

### ۲) اتصال به بستانکاری تامین‌کننده
`ptfPayableUpsert` اکنون `dueISO` و `dueNote` را در `ptf_crm_payables` ذخیره می‌کند. بنابراین تعهد تحویل، همراه با بدهی/پرداخت و رویداد تحویل نگهداری می‌شود.

### ۳) نمایش در جزئیات بستانکاری
در پنجره بستانکاری تامین‌کنندگان، اطلاعات زیر نمایش داده می‌شود:

- تعهد تحویل تامین‌کننده
- تاریخ تحویل واقعی
- تاخیر نسبت به تعهد، در صورت وجود

### ۴) ثبت تحویل واقعی و تشخیص تاخیر
هنگام ثبت رویداد تحویل:

```js
ptfPayableDlv(cd, 'ok' | 'issue')
```

سیستم تاریخ واقعی تحویل را ثبت می‌کند:

```js
dlvISO
dlvAt
dlvBy
```

و اگر تاریخ تحویل واقعی از تاریخ تعهدشده دیرتر باشد:

```js
dlvLate = true
```

### ۵) اثر در نظام امتیازدهی تامین‌کننده
معیار «کیفیت تحویل» در امتیاز تامین‌کننده ارتقا پیدا کرد:

- تحویل بدون مشکل و به‌موقع: امتیاز کامل همان رویداد
- تحویل بدون مشکل اما دیرتر از تعهد: نیم‌جریمه در معیار تحویل
- تحویل با مشکل: امتیاز منفی/کاهش کامل طبق منطق قبلی

متن جزئیات امتیاز اکنون تعداد موارد زیر را نشان می‌دهد:

- بدون مشکل
- با مشکل
- با تاخیر
- تعداد تعهدهای ثبت‌شده

## معماری
- کلید جدید ساخته نشد.
- داده تعهد تحویل در همان purchase/payable ذخیره شد.
- امتیازدهی از داده موجود `ptf_crm_payables` می‌خواند.
- مسیرهای cash/credit، تسعیر خرید، بستانکاری و st9 حفظ شدند.

## فایل‌های تغییر یافته

- `crm/buycompare.js`
- `crm/scoring.js`
- `crm/index.html`
- `crm/sw.js`
- `_tools/uat/tester104-v187.js`
- اصلاح تستر قبلی:
  - `_tools/uat/tester91-v173.js`
- `RELEASE-NOTES-v18.7.md`
- `PTF-MASTER-HANDOVER.md`

## 🔬 کیفیت

- تستر جدید: `tester104-v187.js` با ۱۸ چک.
- رگرسیون کامل: **۹۱ تستر / صفر خطا**.
- Audit سایت: PASS با همان هشدار قدیمی تصاویر سنگین.
