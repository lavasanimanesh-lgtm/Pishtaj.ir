# 🎯 رودمپ سئو — رسیدن به ۲-۳ نتیجهٔ اول گوگل برای «تامین تجهیزات پایپینگ» و «تامین تجهیزات ابزار دقیق»

**تاریخ:** ۲۰۲۶-۰۹-۰۲ · **مبنا:** دادهٔ GSC (اسنپ‌شات ۲۰۲۶-۰۸-۳۰ + بیس‌لاین ۲۰۲۶-۰۸-۲۱)، اسکن سلامت سئو ۲۰۲۶-۰۸-۳۱ (۶۵۰ صفحه)، تحلیل زندهٔ SERP و ممیزی سایت‌مپ در همین جلسه

---

## ۱) وضعیت فعلی — چرا الان در نتایج نیستیم؟

### ۱-۱) داده‌های سخت
| واقعیت | منبع |
|---|---|
| هیچ‌کدام از دو کلمه در ۹۰ روز گذشته حتی **یک نمایش** در GSC نداشته‌اند (سایت در ۱۰۰ نتیجهٔ اول نیست) | GSC-SNAPSHOT-2026-08-30 |
| کل ترافیک غیربرندی سایت نزدیک صفر است: ۱۰۰٪ کلیک‌ها برندی، فقط ۲۷ کوئری با نمایش | GSC-ANALYSIS-2026-08-30 |
| از ~۶۵۹ صفحهٔ ایندکس‌پذیر، ۶۳۷ ایندکس شده (وضعیت ایندکس‌پذیری سالم؛ مشکل «اعتبار و هدف‌گذاری» است نه ایندکس) | SEO-FINAL-REPORT-2026-08-21 |
| SERP زندهٔ «تامین تجهیزات پایپینگ»: radpipe.ir، top-co.biz، istgah.com — رقبای صفحهٔ اول اغلب صفحات شرکتی کم‌عمق هستند | جستجوی زنده ۲۰۲۶-۰۹-۰۲ |
| SERP زندهٔ «تامین تجهیزات ابزار دقیق»: atsa.co.ir، faryanco.ir، yasanco.com — همین‌طور | جستجوی زنده ۲۰۲۶-۰۹-۰۲ |

### ۱-۲) دو ریشهٔ فنی کشف‌شده در همین جلسه

**ریشهٔ اول — صفحات هدف در سایت‌مپ نبودند.** برای هر کلمه یک صفحهٔ پول‌ساز اختصاصی با عنوان دقیق وجود دارد
(`services/piping-supply.html` با عنوان «تامین تجهیزات پایپینگ | پیشرو تجهیز فرتاک» و
`services/instrumentation-supply.html` با عنوان «تامین تجهیزات ابزار دقیق | …» — هر دو ~۳۰۰۰ کلمه + اسکیمای
Service/FAQ/Product/OfferCatalog) اما **هیچ‌کدام در هیچ سایت‌مپی ثبت نبودند** → کرالر اولویت/سیگنال کشف نداشت.
**در همین جلسه اصلاح شد** (بند ۴).

**ریشهٔ دوم — کانیبالیзации + کانونیکال معکوس (مهم‌ترین مانع رتبه).** هر کلمه **دو صفحه** دارد و صفحهٔ جدید
خودش اعتبار را به صفحهٔ قدیمی می‌فرستد:

| کلمه | صفحهٔ قدیمی (در نقشه، احتمالاً ایندکس) | صفحهٔ جدید (بهینه، تا امروز بیرون نقشه) |
|---|---|---|
| پایپینگ | `services/piping-equipment/` (47KB، کانونیکال خودش) | `services/piping-supply.html` — **کانونیکال → صفحهٔ قدیمی!** |
| ابزار دقیق | `services/instrumentation-equipment/` (45KB، کانونیکال خودش) | `services/instrumentation-supply.html` — **کانونیکال → صفحهٔ قدیمی!** |

یعنی گوگل صفحهٔ جدید را «کپیِ» قدیمی می‌داند: نه مستقل ایندکس می‌کند، نه سیگنال عنوانِ دقیقش را حساب می‌کند.
اسکن سئو هم پرچم زده: «کانونیکال مشترک با ۲ صفحه». **تا این تصمیم گرفته نشود، بقیهٔ کارها نصفه اثر می‌کند.**

---

## ۲) تصمیم لازم (P0 — پیش‌نیاز همهٔ فازها) — ✅ انجام شد ۲۰۲۶-۰۹-۰۲ (تأیید مالک)

برای هر کلمه **یک** نشانیِ کانونیکال انتخاب شد: صفحهٔ جدید. اقدام‌های اجراشده:

- **صفحهٔ جدید کانونیکال شد** (`piping-supply.html` / `instrumentation-supply.html`):
  1. ✅ کانونیکال هر دو صفحهٔ جدید به خودشان اصلاح شد (حذف اشاره به صفحهٔ قدیمی) + `og:url` و `hreflang` نیز روی نشانی جدید ثابت شدند.
  2. ✅ صفحهٔ قدیمی به **stub ریدایرکت** تبدیل شد (`meta refresh 0` + کانونیکال به مقصد + `noindex`) تا سیگنال/بک‌لینک‌های احتمالی قدیمی منتقل شود.
  3. ✅ همهٔ لینک‌های داخلی/منو در ~۱۶۵ صفحهٔ سایت به نشانی جدید بازنویسی شدند (۳۰۲ جایگزینی، شامل منوی سراسری، لینک‌های نسبی و بردکرامب).
  4. ✅ نشانی‌های قدیمی از `sitemap-services.xml` خارج شدند (۹۴→۹۲).

نتیجه: هر کلمه اکنون دقیقاً **یک** نشانیِ کانونیکال و ایندکس‌پذیر دارد؛ هیچ «کپی در نقشه» باقی نماند.

> ✅ **۲۰۲۶-۰۹-۰۲ تکمیل شد:** همین الگو برای دو صفحهٔ دیگر نیز اجرا گردید — `valves-supply.html` و `electrical-supply.html` اکنون کانونیکالِ خود دارند و صفحات قدیمی (`suppliers/valve-supplier.html` و `services/electrical-equipment/`) به استاب ریدایرکت تبدیل، ۱۴۶ لینک داخلی بازنویسی و نشانی‌های قدیمی از نقشه‌ها خارج شدند (نقشه‌ها: ۶۱۱ نشانی، ۰ پرچم).

---

## ۳) فازبندی (هر فاز با سنجهٔ پذیرش)

### P0 — تثبیت کانونیکال ✅ انجام شد (۲۰۲۶-۰۹-۰۲)
بخش فنی (بند ۲) کامل شد. باقی‌مانده: پس از دیپلوی، ثبت نقشهٔ به‌روز در GSC + درخواست ایندکس هر دو نشانی جدید از پنل سرچ کنسولِ داخل CRM
(تب سئو ← سرچ کنسول ← «درخواست ایندکس» روی `inspect`).
**سنجه:** ظرف ۷-۱۴ روز هر دو نشانی جدید در GSC وضعیت «صفحه ایندکس‌شده» بگیرند (بدون خطای Duplicate).

### P1 — بهینه‌سازی داخل صفحهٔ دو صفحهٔ پول‌ساز (~نیم روز)
- کلمهٔ دقیق در ۱۰۰ کلمهٔ اول متن، در اولین `<h2>` و در متن جایگزین تصویر اول.
- بازنویسی توضیح متا برای CTR (الگوی رقبای صفحهٔ اول: عدد + تمایز — مثلاً «تامین تجهیزات پایپینگ با گواهی
  MTC از ۸۰+ برند؛ استعلام ۲۴ساعته»؛ ابزار «متای AI» تب سئو آماده است).
- افزودن ۲-۳ پرسش پرجست‌جو به بلوک FAQ هر صفحه: «لیست تجهیزات پایپینگ چیست؟»، «تفاوت تجهیزات ابزار دقیق و کنترل»،
  «نحوه استعلام قیمت پایپینگ» (اسکیمای FAQ از قبل فعال است).
- یک جدول مشخصات/برند واقعی در هر صفحه (رقیب‌ها ندارند — تمایز).
**سنجه:** اسکن سئو برای این دو صفحه صفر پرچم؛ تست Rich-Results گوگل برای FAQ/Service سبز.

### P2 — لینک‌سازی داخلی با لنگر دقیق (~۱ روز، بیشترین اثر کوتاه‌مدت)
- از ۱۰-۱۵ مقالهٔ پربازدیدِ مرتبط با لنگر دقیق: «تامین تجهیزات پایپینگ» ← مقالات لوله/فلنج/استاندارد
  (`api-5l-x52`, `astm-a106`, `asme-b31-3`, `flange-types`, …) و «تامین تجهیزات ابزار دقیق» ← مقالات
  ترانسمیتر/فلومتر/کنترل‌ولو (`rosemount-3051` با ۵۳ نمایش فعلی، `pressure-transmitter-guide`,
  `control-valve-complete-guide`, `flowmeter-types`, …).
- از `blog/piping-equipment-procurement` و `blog/instrumentation-checklist` لینک مستقیم به دو صفحهٔ پول‌ساز.
- ابزار «💡 لینک‌سازی» تب سئو (seo_intlinks) کاندیداها را پیشنهاد می‌دهد.
**سنجه:** `لینک_ورودی` دو صفحه در اسکن بعدی ≥ ۳۰؛ ثبت در `_audit/` به‌عنوان بیس‌لاین.

### P3 — خوشهٔ محتوایی پشتیبان (۲-۴ هفته، موازی)
- هر دو خوشه ۴-۶ مقالهٔ هدفمندِ جدید دارند: پایپینگ → «اتصالات جوشی چیست»، «کلاس‌های فشاری فلنج»،
  «لوله مانیسمان صنعتی»؛ ابزار دقیق → «کالیبراسیون ترانسمیتر»، «انتخاب فلومتر مغناطیسی»، «سیستم DCS و PLC».
  (مولد صفحهٔ عمومی + مرکز دانش با دروازهٔ بازبینی انسانی آماده است — S2-id/S1.)
- هر مقالهٔ جدید ملزم به حداقل ۲ لینک خروجی به صفحهٔ پول‌سازِ خوشهٔ خودش (قرارداد محتوا).
**سنجه:** انتشار ۸-۱۲ مقاله با متن یکتای ≥۵۰۰ کاراکتر (اسکن «متن یکتا» نگهبان است).

### P4 — اعتبار خارج سایت (ماه ۱ به بعد — پاشنهٔ آشیل سایت)
- تمام کلیک‌های فعلی برندی است → برای کلمات غیربرندی به سیگنال خارجی نیاز است:
  ثبت/به‌روزرسانی پروفایل در دایرکتوری‌های صنعتی (istgah، لینکدین شرکتی، بانک‌های اطلاعاتی صنعت نفت)،
  ۲-۳ رپورتاژ/بک‌لینک از رسانه‌های صنعتی، گوگل‌بیزینس‌پروفایل با دستهٔ «تجهیزات صنعتی».
**سنجه:** حداقل ۵ دامنهٔ لینک‌دهندهٔ جدید مرتبط تا پایان ماه دوم.

### P5 — اندازه‌گیری و فرمان (هفتگی، از همین امروز)
- واچ‌لیست جایگاه روی دو کلمه (قابلیت ⭐ پنل سئو) + اسنپ‌شات روزانهٔ خودکار.
- هر هفته: رونمایی از `گیت پیشرفت` — جایگاه/نمایش/کلیک دو کلمه + وضعیت ایندکس دو نشانی (تب سرچ کنسول ←
  «ایندکس‌نشده‌ها» و تاریخچهٔ inspect).
- در هفتهٔ ۲ و ۴: اگر نمایش داریم ولی رتبه >۲۰ است → بازبینی عنوان/لینک داخلی؛ اگر اصلاً نمایش نیست → بررسی
  ایندکس و کانونیکال.

---

## ۴) ✅ انجام‌شده در همین جلسه — به‌روزرسانی کامل سایت‌مپ

مقایسهٔ ۶۶۰ صفحهٔ واقعی سایت با ۷ سایت‌مپ موجود نشان داد صفحات هدفِ این رودمپ بیرون از نقشه‌ها هستند.
پس از یک دور ممیزی تکمیلی (حذف موارد ادغامی/نوشاتکس که نباید در نقشه باشند)، وضعیت نهایی نقشه‌ها:

| فایل | وضعیت نهایی |
|---|---|
| `sitemap-services.xml` | **۹۱ نشانی** شامل ۴ صفحهٔ تامین جدید: `piping-supply`، `instrumentation-supply`، `valves-supply`، `electrical-supply` — دو تای اول صفحات اصلی همین رودمپ‌اند؛ نشانی‌های قدیمی (`piping-equipment/`، `instrumentation-equipment/`، `electrical-equipment/`) پس از تبدیل به استاب، از نقشه خارج شدند |
| `sitemap-knowledge-center.xml` | **۴۱۱ نشانی** = همهٔ صفحات «واقعی» مرکز دانش. ۴۲ صفحهٔ ادغام‌شدهٔ پروژهٔ ۲۰۲۶-۰۸-۳۱ (مانند `rosemount-3051` که کانونیکالش به `rosemount-pressure-transmitter-family` است) **عمداً در نقشه نیستند** — مقصد ادغامشان در نقشه است؛ این طبق قرارداد سئو صحیح است |
| `sitemap-en.xml` | **۴ نشانی** (`en/careers.html` به‌دلیل `noindex` از نقشه خارج شد) |
| `sitemap-products.xml` | **ساخته شد** (۲ صفحهٔ محصول؛ روی سرور از ۰۹-۰۱ بود ولی در مخزن نبود) |
| `sitemap-index.xml` | ۸ زیرنقشه + بامپ lastmod نقشه‌های تغییرکرده |

جمع: **۶۱۱ نشانی معتبر** — حسابرسی کامل تأیید می‌کند: ۰ صفحهٔ noindex داخل نقشه، ۰ نشانی بدون فایل،
و ۰ کانونیکال غیرخود در کل نقشه‌ها (ادغام کانونیکال هر ۴ صفحهٔ تامین در ۲۰۲۶-۰۹-۰۲ تکمیل شد).
هر صفحهٔ دارای عنوان مستقیم «پایپینگ» یا «ابزار دقیق» (۱۲ + ۱۴ صفحه در بلاگ/مرکز دانش/سرویس‌ها/تامین‌کنندگان)
اکنون در نقشه است.
**قدم بعدی مالک/دیپلوی:** پس از آپلود این فایل‌ها، از پنل سئو → سرچ کنسول دکمهٔ «📤 ثبت نقشه» زده شود
(یا سوییچ «ثبت خودکار نقشه» که پیش‌فرض روشن است صبر شود تا اولین نشر بعدی آن را بفرستد).

---

## ۵) انتظار واقع‌بینانه

این دو کلمه حجم جست‌وجوی کم و رقابت نسبتاً ضعیف دارند (صفحهٔ اول فعلی کم‌عمق است)؛ با اجرای P0–P2:
- **هفتهٔ ۱-۲:** ایندکس نشانی‌های جدید + اولین نمایش‌ها.
- **ماه ۱:** صفحهٔ دوم/سوم (جایگاه ~۱۱-۳۰).
- **ماه ۲-۳:** با لینک داخلی + حداقل بک‌لینک، رسیدن به ۲-۳ نتیجهٔ اول هر دو کلمه هدف واقع‌بینانه است.
بند ۲ (تصمیم کانونیکال) در ۲۰۲۶-۰۹-۰۲ اجرا شد؛ فاز بعدی برای رسیدن به ۲-۳ نتیجهٔ اول: **P1 + P2** (بهینهٔ داخل صفحه + لینک‌سازی داخلی).
