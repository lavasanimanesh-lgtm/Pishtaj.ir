# Release Notes — v31.7.18

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.18
**نوع:** 🟡 رفع دو باگ گزارش‌شده کارفرما با اسکرین‌شات — غیبت «موجودی انبار» از منو + بی‌نظمی هدر موبایل (BUG-NAV-ORPHAN-001 + BUG-HDR-MOBILE-001)

---

## 🎯 مسئله (به گزارش کارفرما — با اسکرین‌شات، پس از استقرار v31.7.17)

1. «موجودی انبار» در ماژول‌ها (کشوی «سایر» موبایل) دیده نمی‌شود — با این‌که بسته v31.7.17 درست مستقر شده بود (بج «نسخه فعال: v31.7.17» در اسکرین‌شات).
2. بی‌نظمی هدر: بج نسخه چندخطی شده، آیکون‌ها داخل مربع‌شان وسط‌چین نیستند، برخی نوشته‌ها/آیکون‌ها از کادر بیرون می‌زنند.

## 🔍 علت ریشه‌ای (RCA)

### BUG-NAV-ORPHAN-001 — چرا «موجودی انبار» نبود (اعتراف: خطای دو ریلیز قبل ما هم بود)

منوی سایدبار توسط **آکاردئون دسته‌بندی (`shell.js`، US-129)** از نو ساخته می‌شود: دکمه‌ها را برمی‌دارد، فقط آن‌هایی که در `GROUPS` تعریف شده‌اند را داخل دسته‌ها می‌چیند و **بقیه را با `nav.innerHTML=''` بی‌صدا دور می‌ریزد**. پنل `surplus` از v31.7.3 (فعال‌سازی US-436) **هرگز به GROUPS اضافه نشده بود** → دکمه‌اش همیشه حذف می‌شد؛ و چون کشوی «سایر» موبایل فهرستش را از همین سایدبارِ بازسازی‌شده می‌سازد، در موبایل هم دیده نمی‌شد. (تسترها وجود دکمه در HTML خام را چک می‌کردند و پاس می‌شدند — شکاف بین HTML و DOM نهایی.) تغییر نام v31.7.16 مشکل را ایجاد نکرد؛ فقط باعث شد بالاخره کسی دنبال ماژول بگردد و غیبت قدیمی دیده شود.

### BUG-HDR-MOBILE-001 — بی‌نظمی هدر موبایل

هدر موبایل (US-287) فقط ساعت/سلامت را مخفی می‌کرد اما کنترل سرریز نداشت: بج «نسخه فعال» `white-space` نداشت و ۳خطی می‌شد؛ کانتینرهای flex هدر `min-width:0` نداشتند و بچه‌ها بیرون می‌زدند؛ آیکون‌های tbic (که emoji قبلی‌شان توسط iconx با SVG جایگزین می‌شود) بدون `display:block;margin:auto` کمی خارج از مرکز می‌نشستند.

## ✅ اصلاحات

| # | فایل | تغییر | شدت |
|---|------|--------|-----|
| 1 | `crm/shell.js` | `surplus` به گروه «📦 کالا و اسناد» اضافه شد | 🔴 P0 |
| 2 | `crm/shell.js` | **گارد ریشه‌ای:** هر پنلی که در GROUPS تعریف نشده باشد، به‌جای حذف بی‌صدا به انتهای منو اضافه می‌شود — ماژول‌های آینده دیگر هرگز گم نمی‌شوند | 🔴 P0 |
| 3 | `crm/mobilenav.js` | هدر موبایل: بج نسخه تک‌خطی/فشرده، `min-width:0` و `overflow:hidden` روی کانتینرها (پایان بیرون‌زدگی)، آیکون‌ها ۳۶px با svg وسط‌چین، مهار سرریز نوار trial، عنوان صفحه 40vw | 🟡 P1 |
| 4 | `crm/theme.js` | وسط‌چین دقیق آیکون داخل مربع tbic در همه حالت‌ها (دسکتاپ هم): `svg/span → display:block;margin:auto` | 🟡 P1 |
| 5 | ۴ نقطه نسخه | v31.7.18 | — |

## 📊 نتایج گیت

- تستر جدید `tester193-nav-orphan-header`: **۱۵/۱۵ PASS** — شامل تست رفتاری بازسازی آکاردئون (صفر پنل orphan) و همه قیدهای هدر
- رگرسیون کامل: **171/171 فایل PASS — 3965/3965 چک PASS**
- `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| منوی دسکتاپ | «موجودی انبار» داخل دسته «کالا و اسناد»؛ سایر دسته‌ها بدون تغییر | صفر |
| کشوی «سایر» موبایل | خودکار درست می‌شود (از سایدبار می‌خواند) | صفر |
| RBAC | دست‌نخورده — گارد جدید فقط دکمه‌های مجاز (non-hidden) را برمی‌گرداند | صفر |
| هدر دسکتاپ | فقط وسط‌چین‌تر شدن آیکون‌ها؛ layout تغییری ندارد | ناچیز |

## 📋 راستی‌آزمایی توسط کارفرما (۲ دقیقه)

1. استقرار → رفرش → موبایل: تب «سایر» → **«🏬 موجودی انبار» در دسته‌بندی دیده شود**؛ دسکتاپ: دسته «📦 کالا و اسناد» → موجودی انبار.
2. هدر موبایل: بج «نسخه فعال: v31.7.18» تک‌خطی، آیکون‌ها وسط مربع، هیچ‌چیز از کادر بیرون نزند (همان صحنه اسکرین‌شات دوم).

---
*توسعه‌دهنده: PTF Development Team — 2026-07-20*
