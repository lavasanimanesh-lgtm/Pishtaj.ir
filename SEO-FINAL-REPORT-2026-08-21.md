# گزارش جامع نهایی سئو — ۲۰۲۶-۰۸-۲۱

**تاریخ:** ۱۴۰۵/۰۵/۳۰ — 2026-08-21 | **دامنه:** pishtaj.ir
**شاخهٔ کار:** `arena/01a02288-pishtaj-ir` | **وضعیت:** همهٔ commitها push شده، گیت CI سبز (۷۹/۰)

> این سند جمع‌بندی نهاییِ کارهای سئوی انجام‌شده در این نشست + چک‌لیست باقی‌مانده و نقشهٔ راه است.
> منبع مرجع بعدی برای نشست‌های آینده.

---

## ۰) حکم یک‌خطی

سایت از نظر **فنی و محتوا** بالغ است (۶۵۵ صفحه، ۱٬۳۷۱ بلوک JSON-LD سالم، FAQ کامل در
مرکز دانش). مشکل اصلی — و تنها اهرم واقعی رشد — **اتوریتهٔ بیرونی** (بک‌لینک، برند آفلاین)
و **حلقهٔ اندازه‌گیری** (دادهٔ GSC) است که هر دو بیرون از کد هستند. کارهای کدیِ ممکن برای
رفع «هدررفت سیگنال» (خطای اسکیما، هم‌خوایی، تایتل ناهماهنگ) در این نشست کامل انجام شد.

---

## ۱) baseline واقعی از دادهٔ GSC شما (قطعی، نه تخمین)

| سنجه | مقدار (۳ ماه) |
|---|---|
| کوئری دارای نمایش | ۲۷ |
| کل نمایش | ۲۸۳ |
| کل کلیک | ۳۱ (۲۹ برندی + ۲ شبه‌برندی) |
| کوئری صفحهٔ اول (pos≤10) | ۴ |
| Indexed / Not indexed | ۶۳۷ / ۲٬۰۶۰ |

**تشخیص:** مشکل «دیده‌شدن» است نه «کیفیت». ترافیک فقط وقتی می‌آید که صفحهٔ اول باشید
(کوئری برند جایگاه ۱ = CTR ۶۶٪، همهٔ موارد صفحهٔ ۲ به بعد CTR≈۰).

**دو یافتهٔ مهم:**
1. گوگل صفحات را می‌شناسد و نشان می‌دهد (حتی کوئری‌های فنی خالص) — فقط اتوریته کم است.
2. ~۳۰٪ کوئری‌ها برای «شرکت‌های دیگرِ» همنام است (پیشتازتک، پیشرو صنعت، …) — ابهام برند،
   با کد حل نمی‌شود، با برندسازی آفلاین حل می‌شود.

---

## ۲) کارهای انجام‌شده در این نشست (به ترتیب)

### الف) داده و تحلیل
- دریافت و ثبت `Queries.csv` و `Pages.csv` → `_audit/GSC-QUERIES-BASELINE-2026-08-21.csv` + `SEO-GSC-QUERIES-ANALYSIS-2026-08-21.md`
- نگاشت دقیق ۲۷ کوئری → URL رتبه‌دهندهٔ واقعی (نه URL حدسی).

### ب) پاس ۲ سئو — FAQPage برای مرکز دانش
- **۳۴۴ مقاله** بدون FAQ → بلوک FAQPage مستقل (۳ پرسش: تعریف/نکات فنی/مسیر استعلام از متن خود صفحه).
- پوشش FAQ در مرکز دانش: ۱۰۵ → **۴۴۹ از ۴۴۹** ✅
- اسکریپت idempotent: `tools/seo_step_b2_faq.py`

### ج) Quick-Win تایتل روی URLهای رتبه‌دهنده (بر اساس Pages.csv)
| صفحه | تغییر |
|---|---|
| `services/products/rosemount-3051.html` | «ترانسمیتر فشار روزمونت 3051؛ تامین، قیمت و استعلام» |
| `kc-api-5l-psl2-sour-service-nace-requirements.html` | «لوله NACE (سرویس ترش)؛ الزامات MR0175…» |
| `kc-spare-parts-warehousing-inventory.html` | «مدیریت انبار قطعات یدکی صنعتی…» |
| `kc-industrial-material-handling-transport.html` | «حمل تجهیزات صنعتی و جابجایی مواد…» |
| `brands/emerson-rosemount.html` | «سورسینگ و تامین روزمونت/امرسون؛ نمایندگی و اصالت» + FAQPage |
| `knowledge-center/api-5l-x42.html` | «لوله API 5L X42؛ مشخصات فنی، قیمت و کاربرد» |
| `knowledge-center/a106-pipe-procurement-guide.html` | «راهنمای خرید و قیمت لوله A106 Gr.B» |

### د) رفع خطای سراسری Rich Result
- **۷۷ صفحهٔ محصول** با `AggregateOffer` بدون قیمت (خطای گوگل) → حذف بلوک بدون قیمت.
- چون B2B استعلام‌محور است، قیمت جعلی نگذاشتیم؛ Product اسکیما (نام/برند/توضیح) سالم ماند.

### هـ) تایتل تجاری تامین‌کننده‌ها
- **۲۰ صفحه** `suppliers/*` → «تامین‌کننده X؛ قیمت و استعلام».

### و) رفع هم‌خوایی (cannibalization) — سراسری
| خوشه | اقدام |
|---|---|
| NACE (۳ صفحه) | ادغام canonical دو صفحهٔ عمومی + تفکیک نیت صفحهٔ PSL2 |
| روزمونت (۳ صفحه) | تفکیک نیت محصول/دانش/برند + رفع لینک خراب |
| `services/*-supply` → `-equipment/` (۳ جفت) | ادغام canonical (انتقال اعتبار ۴۷+۲۵ لینک) |
| فولاد `article-012` → `industries/steel/` | ادغام canonical |
| شیرآلات (۲ جفت تجاری + اطلاعاتی) | ادغام canonical |
| `a106-vs-a333` (بلاگ vs مقایسه) | ⏳ عمداً باز ماند — منتظر دادهٔ GSC مقایسه‌ها (اوایل سپتامبر) |

### ز) امنیت — اسکن اسپم
- اسکن کامل کد (کازینو/بک‌دور/لینک مشکوک/ریدایرکت خارجی) → **کد تمیز است**.
- URLهای کازینو مربوط به آلودگی قدیمی (قبل از بازسازی استاتیک) است؛ الان 404.
- گزارش: `_audit/SECURITY-SPAM-SWEEP-2026-08-21.md`

---

## ۳) اعداد نهایی کیفیت فنی

| سنجه | مقدار |
|---|---|
| کل صفحات HTML عمومی | ۶۵۵ |
| JSON-LD کل سایت | ۱٬۳۷۱ بلوک — **۰ خطا** ✅ |
| FAQPage در مرکز دانش | ۴۴۹/۴۴۹ ✅ |
| تایتل تکراری | ۰ ✅ |
| `AggregateOffer` بدون قیمت | ۰ ✅ |
| گیت CI | ۷۹ PASS / ۰ FAIL ✅ |
| نگهبان معماری (A1–A9) | PASS ✅ |

---

## ۴) نقشهٔ ۳۰/۶۰/۹۰ روزهٔ به‌روزشده

| بازه | اقدام | KPI (کوئری صفحهٔ اول) |
|---|---|---|
| **۳۰ روز** | دیپلوی + Request Indexing صفحات بهینه + Validate Fix اسکیما + ثبت دایرکتوری/نقشه | ۴ → ~۱۵ |
| **۶۰ روز** | رپورتاژ ۲ عدد + لینک‌دین + ورودی کیس‌استادی | ~۳۰–۵۰ |
| **۹۰ روز** | گسترش خوشهٔ برنده (ابزار دقیق/پایپینگ) + ۲ رپورتاژ دیگر | ~۷۰–۱۰۰ |

> صادقانه: با ۲۸۳ نمایش کل، هدف واقع‌بینانهٔ ۳ ماه ~۷۰–۱۰۰ کوئری صفحهٔ اول است.
> ۵۰۰+ (هدف اعلامی) به ۶–۹ ماه و رشد اتوریتهٔ بیرونی نیاز دارد. «اول برای همهٔ کلمات»
> واقع‌بینانه نیست؛ «اول برای کلماتی که مشتری می‌آورند» شدنی است.

---

## ۵) چک‌لیست باقی‌مانده (مجری: کارفرما)

### همین هفته — GSC (پس از دیپلوی)
- [ ] **Sitemaps → Submit** برای `sitemap-index.xml` (یک‌بار)
- [ ] **Validate Fix** روی گزارش Product snippets (خطای AggregateOffer)
- [ ] **Request Indexing** (روزانه ~۱۰–۱۵ سهمیه):
  - سطح ۱: `rosemount-3051.html` · `kc-api-5l-psl2-sour-service-nace-requirements.html` · `kc-spare-parts-warehousing-inventory.html` · `kc-industrial-material-handling-transport.html` · `brands/emerson-rosemount.html` · `api-5l-x42.html` · `a106-pipe-procurement-guide.html`
  - سطح ۲: `displacer-level-transmitter.html` · `services/piping-equipment/` · `services/instrumentation-equipment/` · `steam-control-valve-sizing-guide.html`
  - صفحات ادغام‌شده: `suppliers/valve-supplier.html` · `blog/industrial-valves-complete-guide.html`
- [ ] **Removals** برای ۳ URL کازینو (+ هر `site:pishtaj.ir casino` دیگر)

### همین هفته — امنیت هاست
- [ ] تعویض همهٔ رمزها (cPanel/FTP/SSH) — چون سایت قبلاً هک شده بوده
- [ ] ریست کلیدهای ابری/دیتابیس (`secrets.php`، `db-config`) — احتمال لو رفتن
- [ ] بررسی `api/logs/` و `api/uploads/` از نظر فایل مشکوک

### ماه اول — Off-page (بزرگ‌ترین ضریب رشد)
- [ ] ثبت در نشان (neshan.org)، بلد (balad.ir)، Bing Places، کتاب اول (avval.ir) با NAP یکدست
- [ ] صفحهٔ لینکدین شرکت + ۱ پست/هفته
- [ ] ۲ دایرکتوری صنعتی (indpro، صنعت‌کاران، …)
- [ ] ورودی ۲–۳ کیس‌استادی واقعی (کارفرما/اقلام/چالش/نتیجه/عکس)

### ماهانه — حلقهٔ اندازه‌گیری
- [ ] اول هر ماه از GSC: «تعداد کوئری با میانگین جایگاه ≤۱۰» — این یک عدد، KPI کل پروژه است

---

## ۶) محدودیت‌ها و نکات فنی

- **دیپلوی:** همهٔ تغییرات روی شاخهٔ `arena/01a02288-pishtaj-ir` commit شده ولی هنوز روی
  استیجینگ/پروداکشن نرفته‌اند (نیازمند merge به `main` + دیپلوی).
- **`a106-vs-a333`:** تصمیم هم‌خوایی عمداً به تعویق افتاد تا دادهٔ GSC صفحات مقایسه برسد.
- **خوشهٔ برنده:** داده نشان داد ابزار دقیق/روزمونت/کنترل‌ولو بیشترین نمایش را می‌گیرد —
  این «سرنیزهٔ» (beachhead) رشد است؛ دوم: پایپینگ.
- PHP در محیط بررسی در دسترس نبود؛ صحت سرور با نگهبان معماری + تسترهای قرارداد سنجیده شد.
