# 🚀 ریلیز v71.0 — اسپرینت ۷۱: زیرساخت — امنیت، بک‌آپ خودکار، اختیارات ادمین، مهر و امضای سراسری
**تاریخ:** تیر ۱۴۰۵ | **استوری‌ها:** US-145 … US-148

---

## 🔐 ۱) سخت‌سازی امنیتی کامل (US-145)

### روزنه‌های واقعی که در ممیزی پیدا و بسته شدند:
| # | روزنه | شدت | اقدام |
|---|---|:---:|---|
| ۱ | **`crm/api_log.txt` در webroot** — IP و اکشن‌های همه کاربران برای عموم قابل دانلود بود | 🔴 بالا | لاگ به پوشه محافظت‌شده `crm/data/` منتقل شد + فایل قدیمی خودکار حذف می‌شود |
| ۲ | **بدون rate-limit** — امکان بمباران فرم تامین‌کننده/استعلام (هزاران رکورد جعلی) | 🟠 متوسط | حداکثر ۱۰ ثبت در ساعت به‌ازای هر IP + پاسخ 429 + سقف حجم 20MB |
| ۳ | **storage.php بدون گارد** — delete/list از هر دامنه‌ای قابل فراخوانی بود | 🟠 متوسط | اکشن‌های حساس فقط با Origin/Referer همان دامنه (پاسخ 403) |
| ۴ | **ورود بدون محدودیت تلاش** — حمله brute-force به رمز ممکن بود | 🟠 متوسط | بعد از ۵ تلاش ناموفق، ۶۰ ثانیه قفل |
| ۵ | چند نقطه درج بدون escape در داشبورد/سفارشات (XSS ذخیره‌شده از نام شرکت) | 🟡 کم | escP سراسری اعمال شد |
| ۶ | فایل‌های `.json/.log/.txt/.bak/.sql` مستقیماً از وب قابل خواندن بودند | 🟠 متوسط | مسدود در `.htaccess` (با استثنای robots.txt و manifest) |
| ۷ | هدرهای امنیتی ناقص | 🟡 کم | + Permissions-Policy + HSTS (روی HTTPS) |

### دکمه ورود واضح‌تر (AC6)
حالا که مسیرهای نفوذ بسته شده، لینک کم‌رنگ «ورود» فوتر در **هر ۳۱۶ صفحه** به دکمه مشخص «🔐 ورود همکاران (CRM)» تبدیل شد.

---

## 🗄 ۲) بک‌آپ خودکار ساعتی + بازگردانی (US-146)

- **هر ۱ ساعت خودکار:** کل داده‌های CRM (۲۵ کلید) → JSON → سرور هاست + **آپلود به فضای ابری آروان** (جایگزین بک‌آپ قبلی).
- **نسخه روزانه ۷ روز اخیر** جدا نگهداری می‌شود — دقیقاً برای سناریوی شما: «می‌خواهم برگردم به دو روز پیش» → بک‌آپ `daily-1405-04-12.json` را بازگردانی کنید.
- اگر آروان در دسترس نبود → بک‌آپ روی سرور (و در بدترین حالت، محلی در مرورگر) حفظ می‌شود.
- **در تنظیمات:** «🗄 بک‌آپ فوری» • «⬇️ دانلود فایل بک‌آپ» • «📂 بک‌آپ‌های سرور» • نمایش زمان آخرین بک‌آپ موفق.
- **بازگردانی (فقط ادمین):** آپلود فایل (یا انتخاب از بک‌آپ‌های سرور) → پیش‌نمایش تاریخ + تعداد رکورد هر بخش → هشدار صریح → تایید دومرحله‌ای (تایپ کلمه «بازگردانی») → جایگزینی کامل → ریلود.
- **هیچ داده‌ای در آپدیت‌ها از بین نمی‌رود:** آپدیت‌های zip فقط فایل‌های کد را عوض می‌کنند و داده‌ها (localStorage + JSONهای سرور) دست‌نخورده می‌مانند + قبل از هر بازگردانی، بک‌آپ اضطراری خودکار از وضعیت فعلی گرفته می‌شود.

---

## 👑 ۳) اختیارات کامل ادمین (US-147)

- روی CO برنده، فقط برای ادمین دو دکمه ظاهر می‌شود: **«⏪ بازگشت»** و **«🗑️ حذف»**.
- بازگشت از برنده: وضعیت → «ارسال‌شده»، قفل باز، ارجاع فاکتور لغو، و **پرونده خودکارِ تشکیل‌شده هم حذف می‌شود** — همه با تایید و ثبت کامل در audit + اعلان به مدیران.
- هیچ نقش دیگری (حتی رییس هیات مدیره) نمی‌تواند برنده را بازگرداند.

---

## ✍️ ۴) مهر و امضای سراسری (US-148)

- **حذف خودکار پس‌زمینه:** هنگام ثبت تصویر امضا/مهر در «✍️ امضای من»، پیکسل‌های سفید/روشن خودکار شفاف می‌شوند (PNG با لبه محو نرم) — اسکن روی کاغذ سفید هم تمیز درمی‌آید.
- **چک‌باکس «درج مهر و امضای من»** اضافه شد به: پیشنهاد فنی (TO)، پیشنهاد مالی (CO) و قرارداد (نامه‌ها از قبل داشتند).
- با تیک خوردن، مهر + امضای کاربر در محل امضای سند (سمت درست: فروشنده در قرارداد فروش / خریدار در قرارداد خرید) قرار می‌گیرد و **دکمه «👁️ پیش‌نمایش» همان سند نهایی با مهر و امضا را قبل از ثبت نشان می‌دهد**.
- اگر کاربر هنوز پروفایل امضا نساخته، چک‌باکس غیرفعال است با راهنمای «ابتدا در مکاتبات ثبت کنید».

---

## 🔎 ۵) بررسی کلی ماژول‌ها (درخواست شما)

| ماژول | وضعیت | یافته |
|---|:---:|---|
| استعلامات / پیشنهادها / پرونده‌ها | ✅ | جریان ارجاع v69 + جریان برنده v70 سالم؛ XSS داشبورد رفع شد |
| پرونده‌های پروژه | ✅ | کرش timeline (رفع‌شده v70) دوباره تست شد |
| مالی (فاکتور/مطالبات) | ✅ | قفل فاکتور غیربرنده برقرار |
| مکاتبات / قرارداد | ✅ | امضای شفاف جدید در هر دو |
| گزارشات | ✅ | گارد admin/chairman تست شد |
| ۱۲ ماژول JS | ✅ | سینتکس + بدون console.log + بدون eval |

## 🧪 تضمین کیفیت
| مورد | نتیجه |
|---|:---:|
| رگرسیون tester1–7 | ✅ ۳۰۷/۳۰۷ |
| tester8 جدید (اسپرینت ۷۱) | ✅ ۶۱/۶۱ |
| ممیز ۸بخشی (۳۲۲ صفحه) | ✅ PASS |
| **جمع کل** | **✅ ۳۶۸/۳۶۸** |

## 📁 دلتا
`api/crm.php` • `api/storage.php` • `.htaccess` • `crm/backup.js` (**جدید**) • `crm/index.html` • `crm/offers.js` • `crm/contracts.js` • `crm/letters.js` • `crm/sw.js` + **۳۱۶ صفحه سایت** (دکمه ورود جدید) → دلتا این بار بزرگ‌تر است؛ در صورت مشکل دانلود، دلتای کوچک `pishtaj-release-v71-delta-core.zip` (فقط ۹ فایل اصلی، بدون صفحات سایت) هم ساخته شده.

## ⚠️ نکات استقرار
1. ترجیحاً `pishtaj-release-v71-delta.zip` کامل را آپلود کنید (شامل دکمه ورود در همه صفحات). اگر دانلود نشد: اول `-core` بعد در فرصت بعدی کامل.
2. بعد از آپلود، `crm/api_log.txt` قدیمی خودبه‌خود حذف می‌شود (اولین فراخوانی API).
3. **تست بک‌آپ:** تنظیمات → «🗄 بک‌آپ فوری» → باید پیام «فضای ابری آروان + سرور» ببینید و در باکت آروان پوشه `backups/` ساخته شود.
4. **تست امضای شفاف:** یک بار تصویر امضا را دوباره در «✍️ امضای من» آپلود کنید تا نسخه شفاف جایگزین شود (تصاویر قبلی با پس‌زمینه ذخیره شده‌اند).
5. کاربران بعد از ۵ رمز اشتباه ۶۰ ثانیه قفل می‌شوند — طبیعی است.
