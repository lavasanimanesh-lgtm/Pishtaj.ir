# ارزیابی — جدا‌سازی اظهارنامه‌ها از بخش فاکتورها و پنل مستقل

تاریخ: ۱۴۰۵/۰۵/۳۱ (2026-08-22) — نسخهٔ مرتبط: v34.7.80

## ۱) گزارش مشکل

۱. «بخش فاکتورها» در قسمت مالی **گاهی بهم می‌ریزد**.
۲. «➕ بارگذاری اظهارنامه جدید» (بایگانی اظهارنامه‌ها) هم داخل بخش فاکتورها نمایش داده می‌شود.

## ۲) ریشهٔ علت (دو رندرساز موازی برای یک پنل)

پنل «🧾 فاکتورها» (`inv`) دو تعریف رقیب دارد:

| تعریف | محل | محتوا |
|---|---|---|
| `function buildInvoices/renderInvoices` | `crm/rbac.js` (خط ۷۰۵ و ۷۰۹) | نسخهٔ قدیمی: پیش‌فاکتورهای ارجاع‌شده + **کادر بایگانی اظهارنامه‌ها** (`ptfTaxReturnsHtml` + `ptfTaxReturnsRender`) |
| `window.buildInvoices/renderInvoices` | `crm/official-invoice-v2.js` (خط ۴۵ و ۴۶) | رجیستری رسمی فاکتور (v35) — بدون اظهارنامه |

هر دو به `div#invWrap` می‌نویسند و زنجیرهٔ `goPanel` (perms.js → rbac.js → ماژول‌ها) هر دو مسیر را در بر دارد.
این «دو رندر برای یک ظرف» عامل بهم‌ریختن ظاهری است؛ و چون نسخهٔ قدیمی `rbac.js` هنوز اظهارنامه را
به فاکتورها چسبانده، در مسیرهایی که نسخهٔ قدیمی اجرا می‌شود، «بارگذاری اظهارنامه» داخل فاکتورها دیده می‌شود.

نکته: منطق اظهارنامه خودش در `crm/unofficial-invoice.js` (خطوط ~۷۴۷–۸۹۸) پیاده شده و فقط به
`rbac.js` سیم‌کشی شده است — یعنی اظهارنامه «مهمانِ» فاکتورهاست و خانهٔ مستقل ندارد.

## ۳) طرح اصلاح (تصمیم)

۱. **حذف** اظهارنامه از مسیر فاکتورها:
   - `crm/rbac.js` → حذف `taxHtml` از `buildInvoices` و حذف فراخوانی `ptfTaxReturnsRender` از `renderInvoices`.
   - `crm/unofficial-invoice.js` → حذف بلوک بایگانی اظهارنامه (توابع `ptfTaxReturns*`).
۲. **پنل مستقل «📁 اظهارنامه‌ها»** در گروه سایدبار «📦 کالا و اسناد» (`g-goods`):
   - ماژول جدید `crm/tax-returns.js` با توابع `buildTaxReturns` / `renderTaxReturns` /
     `ptfTaxReturnsTab` / `ptfTaxReturnsYear` / `ptfTaxReturnsAdd` / `ptfTaxReturnsDel`.
   - دکمهٔ سایدبار + ثبت در `perms.js` (ALL_PANELS + `reg`) + دسترسی نقش حسابدار در `rbac.js` (ROLES).
۳. **مدل داده** (`ptf_crm_tax_returns` — همان کلید سینک‌شوندهٔ موجود، بدون مهاجرت فایل):
   - فیلد جدید `kind`: `'performance'` (اظهارنامه عملکرد سالانه) یا `'vat'` (ارزش افزوده فصلی).
   - سازگاری با رکوردهای قدیمی: `kind` از رشتهٔ قدیمی `type` استنتاج می‌شود
     (هر `type` دارای «عملکرد» → performance؛ بقیهٔ فصلی‌ها → vat).
۴. **مسئولیت پنل**: ذخیره و نمایش اظهارنامه‌ها به تفکیک:
   - **عملکرد** → یک سند برای هر **سال مالی**.
   - **ارزش افزوده** → برای هر **فصل** (بهار/تابستان/پاییز/زمستان) و **هر سال انتخابی**.
