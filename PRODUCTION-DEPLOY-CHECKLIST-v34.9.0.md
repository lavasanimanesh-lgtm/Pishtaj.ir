# چک‌لیست دیپلوی پروداکشن — v34.9.0 (نهایی)

**تاریخ:** ۲۰۲۶-۰۸-۲۹ · **جانشین:** v34.8.12 پروداکشن · **مدرک بازبینی:** `PRE-PRODUCTION-DEPLOY-AUDIT-2026-08-29.md`

## فاز صفر — پیش از دیپلوی (یک روز قبل)

- [ ] گیت کامل سبز: `node _tools/uat/run-ci-gate.js` (۱۶۸ تستر / ۰ خطا) و `node _tools/arch/arch-guard.js` → PASS
- [ ] استیجینگ همان کامیت را زنده اجرا می‌کند (fetch_page بنر + sw) — یک چرخهٔ ورود/خروج/ثبت روی استیجینگ
- [ ] **بک‌آپ کامل سرور پروداکشن**: فایل‌های `api/crm/data/*.json` (به‌ویژه `users.json`، `tokens.json`، دیتاست‌های `ptf_crm_*`) + دیتابیس پیوست‌ها — دانلود و نگه‌داری آفلاین
- [ ] اسکرین‌شات/خروجی از متر حافظهٔ ۲–۳ دستگاه واقعی کاربران (برای مقایسهٔ بعد از انتقال — اثبات E3)
- [ ] هماهنگی زمان: ساعات کم‌ترافیک (پیشنهاد: خارج از ۸–۱۷) و اطلاع به کاربران «فردا صبح پیامک می‌آید»

## فاز یک — دیپلوی

- [ ] پوش/مرج نهایی به main و اجرای workflow دیپلوی پروداکشن
- [ ] راستی‌آزمایی زنده (نه exit code ران): 
  - `https://pishtaj.ir/crm/clear-cache.html` → بنر = کامیت نهایی + نسخه v34.9.0
  - `https://pishtaj.ir/crm/sw.js` → `RELEASE = 'v34.9.0'`
  - `https://pishtaj.ir/api/crm.php?action=sms_status` → `{"ok":true,...}`
- [ ] ورود آزمایشی با اکانت ادمین روی پروداکشن (مرورگر تمیز/حالت ناشناس **نه** — یک مرورگر واقعی قدیمی که داده دارد، برای آزمون مسیر مهاجرت)

## فاز دو — کاربران (روز دیپلوی)

- [ ] پیامک پلکانی با متن مصوب (سه قدم: Ctrl+F5 → ورود با رمز همیشگی → تنظیمات ← وضعیت دستگاه ← «انتقال یک‌باره»)
- [ ] پایش پیشرفت: داشبورد «موتور همگام‌سازی (R4)» در تنظیمات + تماس/پشتیبانی برای کاربران گیرکرده
- [ ] بنر خودکار پایین صفحه (MIGRATION-BANNER) کاربران فراموش‌کار را خودش یادآوری می‌کند — انتظار: طی ۲–۳ روز کاری اکثر دستگاه‌ها سبز

## فاز سه — پس از دیپلوی (هفتهٔ اول)

- [ ] هر روز: سلامت API + خطاهای `auth_2fa_log` (اگر 2FA خاموش است باید خالی بماند) + `failedWrites` در هشدارهای سهمیه
- [ ] جمع‌آوری تله‌متری حذف موتور legacy: داشبورد R4 → «win7 ≈ 0 برای همهٔ کلیدها + صفر R4-GATE-BYPASS» به مدت **≥۷ روز**
- [ ] اثبات E3 (LS سبک): روی ۲–۳ دستگاه واقعی، متر حافظهٔ محلی بعد از انتقال+پاک‌سازی کش — انتظار: کاهش به حد سبک (ده‌ها KB تا ~۲MB کش)
- [ ] اثبات E4 دستی: روی یک دستگاه منتقل‌شده «🗑 پاک‌سازی کش محلی» → Ctrl+F5 → همهٔ داده‌ها از سرور برمی‌گردند بدون خطا
- [ ] soak یک‌هفته‌ای روی یک دستگاه قدیمی واقعی (کندترین لپ‌تاپ/گوشی موجود) — زمان بوت صفحه + رفتار نوار حافظه

## فاز چهار — بستن نهایی (v34.9.1، فقط با شرط داده)

- [ ] شرط: ≥۷ روز سبزِ کامل داشبورد R4 (win7≈0 + صفر بایپس) → حذف موتور legacy و fallbackهای مرده + تعیین تکلیف بدهی A10 (۱۹۵ دسترسی مستقیمِ ثبت‌شده در baseline — عمدتاً UI/CACHE/DEV در مسیرهای قدیمی)
- [ ] پس از حذف: گیت کامل + arch با baseline جدید + دیپلوی + اطلاع «نیازی به کار کاربران نیست»

## طرح بازگشت (Rollback) — اگر شرایط اضطراری

1. داده روی سرور است و دیپلوی فایل‌های `crm/data` را دست نمی‌زند — بازگشت = دیپلوی مجدد کامیت v34.8.12
2. نشانه‌های اضطراری: ورود گروهی کاربران ناموفق با خطای غیرشناخته‌شده / خطای 500 در api
3. تصمیم بازگشت فقط با مالک؛ سناریوی محتمل‌تر (کاربر تکی گیر کرده) = پشتیبانی همان کاربر، نه بازگشت کل

## وضعیت اصول E در این نسخه (خلاصهٔ tester552)

| اصل | وضعیت | مدرک |
|---|---|---|
| E1 سرور منبع حقیقت نوشتن | ✅ اثبات | A11 صفر واگرایی + تسترهای بایپس‌صفر |
| E2 صفر بایپس جدید لایهٔ داده | ✅ اثبات (گیت مسدودکننده) | A10 = ۰ تخلف جدید (بدهی ۱۹۵ ثبت‌شده → v34.9.1) |
| E3 LS سبک | 🔶 هارنس آماده؛ عدد نهایی = هفتهٔ اول پروداکشن | متر + آستانهٔ 8KB + mirror-حذف‌کننده |
| E4 پاک‌کردن = صفر گم‌شدن | ✅ قرارداد + 🔶 اثبات دستی فاز سه | ۴ گارد + بک‌آپ-اول + تستر552 |
| E5 حادثهٔ ۱۰۰٪ ناممکن | ✅ اثبات | نگهبان setItem + فشرده‌سازی اضطرافی + outbox IDB |
| E6 آفلاین = بافر محدود | ✅ اثبات | سقف ۵۰۰ + دستهٔ ۲۰ + پول ۲۰ث + نشان offline |
| E7 یک موتور | 🔶 پرچم‌دار (پیش‌فرض خاموش) → نهایی در v34.9.1 | legacyPushOff + داشبورد R4 + شرط ≥۷ روز |
