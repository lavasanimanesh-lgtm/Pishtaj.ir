# RELEASE NOTES — v34.16.0 (واچ‌لیست جایگاه کلمات — تکمیل S3)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** tester561 (۱۸ سنجه) · **گیت:** 177 PASS / 0 FAIL · **PHP-لینت:** php-parser (gsc.php) ✓

## قابلیت

- **⭐ واچ‌لیست جایگاه** در پنل سرچ کنسول: دکمهٔ ⭐ کنار هر کوئری (جدول کوئری‌های پربازدید/فرصت‌ها) → افزودن/حذف کلمه (سقف ۳۰، audit ثبت می‌شود).
- **کارت «⭐ واچ‌لیست جایگاه»**: برای هر کلمه — آخرین جایگاه رنگی (≤۳ سبز، ≤۱۰ کهربایی، بدتر قرمز)، **دلتا نسبت به اسنپ‌شات قبل** (▲ بهبود سبز / ▼ افت قرمز / بدون تغییر)، کلیک/نمایش آخرین روز، و **میله‌های روند ۱۶ نقطهٔ آخر** (جایگاه بهتر = میلهٔ بلندتر؛ tooltip تاریخ‌دار). اگر کلمه در ۳۰تای برترِ روز نیست: «خارج از ۳۰تای برتر».
- **بدون دادهٔ جدید:** روند کامل از `crm/data/gsc-snaps/*.json` موجود (topQueries هر روز) خوانده می‌شود — صفر فراخوانی اضافی GSC، صفر کرون.

## قرارداد

- `watch_toggle` POST `{q}` → `{on, total}` (خطا: «واچ‌لیست پر است (۳۰ کلمه)» / کلمهٔ نامعتبر).
- `watch_list` GET → `{items:[{q, added_at, series:[{d,pos,clicks,imp}]≤60, last, prev, delta}], total_snaps}` — `delta = prev.pos − last.pos` (مثبت = بهبود).
- ذخیره: `crm/data/gsc-watchlist.json`.

## بهداشت

- آرگومان onclick با `&#39;` + `ptfOnClickArg` (بدون بک‌اسلش در JS-string؛ مقاوم در برابر نقل‌قول در کلمه).
- A10 پابرجا؛ انکرهای تستر بدون نسخه.

## فایل‌های کلیدی

`api/gsc.php` (gsc_watch_load/save + ۲ case) · `crm/gsc.js` (⭐ در queryTable + gscWatchLoad) · `_tools/uat/tester561-v34.16.0-seo-watchlist.js`
