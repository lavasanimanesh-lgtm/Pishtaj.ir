# اجرای اسپرینت ۴ سئو — ۲۰۲۶-۰۹-۲۵ (پاکسازی کامل لینک‌های داخلی به پوسته‌های ۳۰۱)

ادامهٔ اسپرینت‌های ۱–۳. هدف: حذف کامل لینک‌های داخلی به URLهایی که در `.htaccess` ریدایرکت ۳۰۱ شده‌اند، تا اعتبار لینک مستقیماً به صفحات canonical برسد و یک هپ ریدایرکت حذف شود.

## روش

اسکریپت پارسر کامل `.htaccess` (۲۸۷ قانون R=301 شامل اسلاگ‌های یونیکد و انکدشده) + اسکنر همهٔ فرم‌های لینک (absolute / root-relative / نسبی / percent-encoded) در همهٔ فایل‌های HTML عمومی (خارج از crm و ابزارهای داخلی) + `search-index.json` + sitemap ها. قانون `index.html → دایرکتوری` (THE_REQUEST-based) هم پوشش داده شد. زنجیره‌های ریدایرکت بررسی و حل شدند (چین واقعی یافت نشد).

## ✅ نتایج

| سنجه | مقدار |
|---|---|
| فایل‌های HTML ویرایش‌شده | **۸۹** |
| لینک بازنویسی‌شده به canonical | **۱۵۰** |
| لینک `index.html` → فرم دایرکتوری | ۳۶ (مثل `../index.html` → `..`) |
| ورودی‌های search-index حذف‌شده (دونهٔ تکراری با canonical) | ۴۵ |
| ورودی search-index بازنویسی‌شده | ۱ |
| لینک شکستهٔ ایجادشده | **۰** (از ۷۳,۳۰۹ لینک چک‌شده) |
| لینک باقی‌مانده به پوستهٔ ۳۰۱ | **۰** (بازاسکن) |
| URLهای sitemap اشاره‌کننده به ۳۰۱ | ۰ (از قبل سالم) |
| lastmod به‌روزرسانی‌شده | ۷۷ URL + sitemap-index |

بزرگ‌ترین پاکسازی‌ها: `a106-gr-b.html` → `kc-astm-a106-gr.html` (۹ لینک)، `third-party-inspection-guide.html` → `kc-supplier-qualification-assessment.html` (۷)، `ball-valve-api-6d.html` → `api-6d-ball-valve-standard.html` (۶)، `rosemount-3051.html` → `rosemount-pressure-transmitter-family.html` (۵)، `nace-mr0175.html` → `nace-mr0175-sour-service-guide.html` (۴).

هاب مرکز دانش (هم لیست استاتیک و هم آرایهٔ JS دسته‌ها) و منوهای سراسری همه سالم شدند. `crm/` و `.htaccess` دست‌نخورده ماندند.

## 🎯 اثر موردانتظار

- حذف یک هپ ۳۰۱ از مسیر ~۱۵۰ لینک داخلی → انتقال تمیزتر link equity به صفحات canonical (به‌ویژه مقالات ادغام‌شدهٔ KC که حالا لینک مستقیم می‌گیرند).
- crawl بهینه‌تر: گوگل به‌جای فetch+redirect فقط یک بار fetch می‌کند.
- جستجوی داخلی سایت دیگر کاربر را به URL ریدایرکتی نمی‌فرستد.

## 📋 بک‌لاگ اسپرینت ۵ (به‌روز)

1. **Authority غیربرند طبق `ARENA-SEO-P4-OFFSITE-AUTHORITY-PLAYBOOK`** — فقط با اقدام انسانی کاربر: ثبت در دایرکتوری‌ها (istgah، naftkala، mopkey، vlist)، GBP، لینکدین؛ ردیاب `_audit/SEO-OFFSITE-TRACKER.json` آماده است. **پاشنهٔ آشیل باقی‌مانده — بدون این، خروج از باند ۳۱–۱۰۰ رخ نمی‌دهد.**
2. **استقرار سرور** (هنوز معلق): `api/gsc.php`، `api/llm.php`، `api/contact-merge-lib.php` — تا این انجام نشود اثر چهار اسپرینت قابل اندازه‌گیری نیست.
3. بازبینی query↔page digest پس از استقرار (۴ هفته بعد) — مبنای اسپرینت بعدی.
4. محتوای EN و ارزیابی مقالهٔ «هیدرواستاتیک vs دیسپلیسر» بر اساس دادهٔ GSC.
