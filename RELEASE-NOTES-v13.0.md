# 📦 ریلیزنوت v13.0 — نوار موبایل: ترتیب جدید + دکمه برجسته «پیشنهاد» + کشوی «سایر» رنگی

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v12.9 | **US-319 (دستور کارفرما)**

---

## 📱 ترتیب و ترکیب جدید نوار پایین موبایل

| موقعیت | قبل | بعد |
|---|---|---|
| ۱ | داشبورد | داشبورد |
| ۲ | کارتابل (بج) | کارتابل (بج) |
| ۳ | فروش | **📄 پیشنهاد — دکمه برجسته (FAB)** |
| ۴ | تامین | 🤖 دستیار |
| ۵ | بیشتر | ⋯ **سایر** |

## 🔴 دکمه برجسته «پیشنهاد» (وسط نوار)

دقیقاً طبق توصیف شما — «بولدتر، شبیه نیم‌دایره وارد فضای پنل بشه»:
- دایره ۵۴ پیکسلی با **گرادیان نارنجی برند** که **نیمی از آن بالای نوار و داخل فضای پنل** بیرون زده
- حلقه سفید دور دایره (در حالت شب: حلقه تیره) + سایه نارنجی — الگوی FAB اپ‌های بومی
- برچسب «پیشنهاد» زیر آن بولدتر و به رنگ برند
- کلیک = مستقیم به ماژول پیشنهاد (TO/CO/TC)

## ⋯ کشوی «سایر»

- **اسکرول افقی کاملاً قفل شد:** شیت فقط عمودی اسکرول می‌خورد (`overflow-x:hidden` + `touch-action:pan-y`) و گرید آیتم‌ها هرگز از عرض صفحه بیرون نمی‌زند
- **آیکون‌های رنگی:** همان پالت ۱۲ رنگی چرخشی لانچر دسکتاپ — هر ماژول یک رنگ، هماهنگ در کل سیستم
- فروش و تامین (که از نوار حذف شدند) مثل بقیه ماژول‌ها از همین کشو در دسترس‌اند؛ RBAC مثل قبل خودکار

## 🧪 کیفیت
- تست شبیه‌ساز مرورگر (عرض iPhone): ترتیب تب‌ها، FAB، رنگ آیکون‌های کشو، قفل افقی، ناوبری+هایلایت پیشنهاد — همه سبز ✅
- تستر جدید `tester50` ۱۵/۱۵ ✅ | رگرسیون **۳۷ تستر** ✅ | audit صفر خطا ✅
- بهداشت نهایی تسترها: تمام چک‌های نسخه باقی‌مانده یک‌بار برای همیشه به الگوی `v1` نسخه‌آزاد شدند

## 📁 فایل‌های تغییریافته
`crm/mobilenav.js` (TABS/TAB_OF/FAB/کشو) · `crm/index.html`/`sw.js` (v13.0) · `_tools/uat/tester50` (جدید) + سینک tester41/44/49

📌 پس از آپلود: PWA موبایل را ببندید و باز کنید (کش قبلی).
