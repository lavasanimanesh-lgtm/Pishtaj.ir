# چک‌لیست staging v34.7.45 — رویژن کامل پیشنهاد برنده

> فقط روی staging اجرا شود. Production نباید deploy شود.

## ۱. بازیابی فرمان گزارش‌شده

1. با همان کاربری که operation ID زیر را ایجاد کرده وارد staging شوید:
   - `REV-mt0fqdf5-93hkmqvx`
2. Hard Refresh کنید و تا پایان pull صبر کنید.
3. انتظار:
   - سیستم journal قطعی را بدون ارسال مجدد `revise_award` بررسی کند؛
   - اگر فرمان قبلاً commit شده، پیام بازیابی قطعی نمایش داده شود و Rev جدید پس از pull دیده شود؛
   - اگر journal آن را committed نمی‌داند، هیچ success کاذب یا رویژن تکراری ساخته نشود.

## ۲. برابری فرم

از داخل پرونده «رویژن پیشنهاد برنده» را باز کنید. انتظار:

- همان عرض، جدول و ساختار مودال پیشنهاد مالی نمایش داده شود؛
- انتخاب چندگانه از ماژول کالا فعال باشد؛
- بارگذاری از درخواست و Excel فعال باشد؛
- نرخ مرجع، درصد حاشیه و محاسبهٔ خودکار قیمت فروش وجود داشته باشد؛
- ستون‌های تکمیلی، شرایط و ضوابط، کتابخانهٔ شرایط و بند دلخواه وجود داشته باشند؛
- پیش‌نمایش، قالب چاپ، تاریخ، اعتبار و امضا موجود باشند؛
- مشتری، شمارهٔ درخواست و ارز نمایش داده شوند ولی قفل باشند؛
- دلیل رویژن و گزینهٔ ابطال فاکتور فعال در پایین همان فرم وجود داشته باشند.

## ۳. افزودن اقلام و metadata

1. یک قلم از ماژول کالا اضافه کنید.
2. یک قلم از درخواست بارگذاری کنید.
3. نرخ مرجع و حاشیهٔ حداقل یک قلم را تغییر دهید.
4. یک ستون تکمیلی و یک شرط جدید اضافه کنید.
5. پیش‌نمایش را باز و مقادیر را کنترل کنید.
6. رویژن را ثبت کنید.

انتظار پس از ACK و refresh:

- شماره و `_id` پیشنهاد ثابت و Rev دقیقاً یک واحد زیاد شده باشد؛
- همهٔ اقلام و شرایط جدید باقی بمانند؛
- `refPrice/refCur/refSrc`، حاشیه، تحویل و ستون‌های تکمیلی حذف نشده باشند؛
- مبلغ پرونده و linked offer با پیشنهاد رویژن‌شده هم‌خوان باشند؛
- duplicate offer/case/revision ساخته نشده باشد.

## ۴. validation و هویت

- فرم بدون دلیل ثبت نشود؛
- قلم ناقص توسط validation مشترک پیشنهاد رد شود؛
- تغییر برنامه‌ای مشتری/درخواست/ارز در سرور با `award_revision_identity_change_forbidden` رد شود؛
- در رد قطعی، فرم قابل اصلاح بماند و Rev افزایش نیابد.

## ۵. پاسخ گم‌شده

با ابزار کنترل‌شده پاسخ دو درخواست mutation را مبهم کنید:

- operation ID ثابت بماند؛
- سپس یک `command_status` کوچک ارسال شود؛
- اگر journal committed است، ACK قطعی بازیابی شود و رویژن دوباره اجرا نشود؛
- اگر status نیز در دسترس نیست، فرم و payload قفل و دکمه فقط برای بازیابی همان فرمان فعال بماند؛
- هیچ rollback یا success کاذب رخ ندهد.
