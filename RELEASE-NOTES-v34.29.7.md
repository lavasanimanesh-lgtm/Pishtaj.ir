# RELEASE NOTES — v34.29.7 (درخواست‌های سایت با موازین CRM: مشتری و تامین‌کننده)

**تاریخ:** ۲۰۲۶-۰۹-۰۲ · **کمیت:** tester582 (۱۵ سنجه) + بازتولید رفتاری jsdom (قدیم ۷/۰ اثبات ریشه، جدید ۱۱/۱۱) · **گیت:** 198 PASS / 0 FAIL

## گزارش کارفرما

«رفتار درخواست‌های ثبت‌شده از سمت سایت را با موازین موجود در CRM برای ثبت مشتری و تامین‌کننده یکسان کن. مثلاً الان شماره تلفن یک مشتری که درخواستی ثبت کرده ثبت نشده و امکان ثبت شماره تلفن با زدن ویرایش مشتری هم وجود ندارد.»

## ریشه — سرور شمارهٔ تماس رابط را بی‌صدا پاک می‌کرد

در CRM، جای استاندارد شمارهٔ تماسِ «شخص رابط» ساختار `people[].tels/mobs/mails` است (فرم مشتری/تامین‌کننده و ورود اکسل هر دو همین را می‌سازند). اما پاک‌ساز رکورد سرور (`sd_entity_sanitize_list` در api/sales-domain.php) برای مقدار **آرایه‌ای داخل mapِ داخل لیست** هیچ شاخه‌ای نداشت — یعنی `tels/mobs/mails` هر شخص، موقع هر `entity_upsert` (همان مسیر ذخیرهٔ مشتری/تامین‌کننده از v34.8.22 به بعد) **بی‌صدا حذف** می‌شد. رفع مشابه v34.23.0 فقط سطح ردیف (files.oth) را پوشش می‌داد.

زنجیرهٔ علامت کارفرما دقیقاً از همین‌جا می‌آمد:
1. مشتری از درخواست سایت ساخته می‌شود؛ موبایل رابط در `people[0].mobs` و فیلد نمایشی `ph` می‌نشیند؛
2. فرمان ذخیره به سرور می‌رود → سرور mobs/mails را می‌زداید و نسخهٔ بریده را برمی‌گرداند → در کارتِ «👁 مشتری» شخص رابط بدون هیچ شماره‌ای دیده می‌شود («شماره تلفن ثبت نشده»)؛
3. کاربر در «✏️ ویرایش مشتری» شماره را در دفترچهٔ اشخاص رابط وارد و ذخیره می‌کند → همان چرخه → شماره دوباره حذف؛ و در ویرایش بعدی چون `ph` از کانال‌های خالی بازمحاسبه می‌شد، خالی می‌شد («با ویرایش هم ثبت نمی‌شود»).

## اصلاح

**① سرور (api/sales-domain.php):** `sd_entity_sanitize_list` عمق‌دار شد (`$depth=2`) — مقدار لیستی داخل mapِ داخل لیست، بازگشتی با همان سقف‌های قبلی (۶۰ قلم/۲۰ فیلد) سانیتایز و **نگه داشته** می‌شود؛ دیگر حذف نمی‌شود. شاخهٔ v34.23.0 (سطح ردیف) دست‌نخورده.

**② مشتری از درخواست سایت (crm/bridge.js):** تلفن فرم سایت حالا با موازین CRM طبقه‌بندی می‌شود (مثل ورود اکسل `ptfXlsPerson`): شمارهٔ `09xxxxxxxxx` → کانال موبایل رابط؛ شمارهٔ ثابت → کانال تلفن ثابت با داخلی. هم در ساخت مشتری جدید، هم در افزودن رابط به مشتری موجود.

**③ تامین‌کننده از ثبت‌نام سایت (crm/bridge.js):** رکورد تاییدشده حالا مثل فرم استاندارد، `people` با رابط اصلی (نام/موبایل یا ثابت طبقه‌بندی‌شده/ایمیل، primary) + `kind: حقوقی` می‌گیرد — قبلاً فقط دو فیلد تخت بود و شماره در کارت/دفترچهٔ اشخاص دیده نمی‌شد.

**④ ویرایش دیگر داده نمی‌پاکد (crm/offers.js):** «ویرایش مشتری/تامین‌کننده» فیلدهایی که فرم مدیریت نمی‌کند (پیوست ابری files، src/srcSite، پیام فرم سایت، شرایط پرداخت تامین‌کننده، مهرهای تشخیص تکراری و…) را با merge مثل سرور از رکورد قبلی حفظ می‌کند؛ و برای رکوردهای قدیمیِ سایت بدون اشخاص رابط، نام رابط و شمارهٔ تخت (`nm/ph`) هم در اولین ویرایش از دست نمی‌رود.

## اثبات (بوت کامل jsdom + سرور جعلی با ترجمهٔ وفادار پاک‌ساز PHP)

- **حالت قدیمی (اثبات ریشه):** شخص رابط زنده می‌ماند ولی `mobs/tels/mails` پس از رفت‌وبرگشت سرور حذف می‌شدند — ۷/۷.
- **حالت جدید ۱۱/۱۱:** موبایل `09121234567` در کانال استاندارد رابط + `ph`؛ ثابت `02188001234` در کانال `tels` (نه mobs)؛ تامین‌کنندهٔ سایت با people کامل + kind حقوقی؛ پس از «ویرایش مشتری» همه چیز (کانال تماس، srcSite، ds) روی کلاینت **و سرور** ماند. نکته: ارقام تلفن مشتری طبق قرارداد موجود phonefmt.js با ارقام فارسی ذخیره می‌شود — دست‌نخورده.

## پیوند با زنجیرهٔ قبلی

ششمین لایهٔ پروندهٔ سایت↔CRM: ① v34.28.0 کلیک تب CMS؛ ② v34.29.3 برخورد کلاس pn/tb با CSS و نامرئی‌شدن با display:none؛ ③ v34.29.4 پارسر خروجی هوش خارجی؛ ④ v34.29.5 برخورد شناسهٔ pgTitle؛ ⑤ v34.29.6 یک‌کلیکی‌شدن یادآور + اعلان درخواست سایت؛ ⑥ v34.29.7 همین نسخه.

## بعد از دیپلوی

فایل‌های v34.29.7 را آپلود کنید — **این بار `api/sales-domain.php` حتماً هم‌زمان با `crm/` آپلود شود** (ریشهٔ اصلی سمت سرور است). تب را ببندید/باز کنید. تایید درخواست سایت → کارت مشتری/تامین‌کننده با شمارهٔ تماس رابط کامل؛ ویرایش هم دیگر چیزی را نمی‌پاکد. برای مشتری‌های قدیمی که شماره‌شان روی سرور بریده شده: فقط `ph` تخت باقی مانده — با یک بار «ویرایش مشتری» و واردکردن شماره در دفترچهٔ اشخاص رابط، این بار ماندگار می‌شود.
