<?php
/**
 * ================= قالب کانفیگ آروان‌کلود — پیشرو تجهیز فرتاک =================
 *
 * 📌 دستورالعمل (فقط ۳ قدم):
 *  1) این فایل را باز کنید و ۴ مقدار زیر را با اطلاعات پنل آروان جایگزین کنید.
 *  2) نام فایل را به  storage-config.php  تغییر دهید.
 *  3) با File Manager هاست، آن را «یک پوشه بالاتر از public_html» آپلود کنید.
 *     مثال مسیر نهایی:   /home/USERNAME/storage-config.php
 *     (یعنی کنار پوشه public_html، نه داخل آن!)
 *
 * ⚠️ این فایل نباید داخل public_html باشد — بیرون از webroot یعنی هیچ‌کس از اینترنت به آن دسترسی ندارد.
 */

return [
    // از صفحه «مدیریت دسترسی» پنل آروان:
    'endpoint'   => 'https://s3.ir-thr-at1.arvanstorage.ir',   // اگر پنل آدرس دیگری نشان داد، همان را بگذارید
    'region'     => 'ir-thr-at1',                               // بخش میانی endpoint (بعد از s3. و قبل از .arvanstorage)
    'access_key' => 'ACCESS_KEY_خود_را_اینجا_بگذارید',
    'secret_key' => 'SECRET_KEY_خود_را_اینجا_بگذارید',

    // صندوقچه‌ای که ساختید:
    'bucket'     => 'ptf-crm-files',

    // تنظیمات اختیاری:
    'max_mb'     => 25,        // حداکثر حجم هر فایل (مگابایت)
    'expiry'     => 3600,      // اعتبار لینک‌های موقت (ثانیه) — ۱ ساعت
];
