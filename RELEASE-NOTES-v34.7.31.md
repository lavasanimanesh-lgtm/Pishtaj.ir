# RELEASE NOTES — v34.7.31 (۱۴۰۵/۰۵/۲۶ — 2026-08-17)

بستهٔ **P4–P6**: پاسخ کامل به سناریوی واقعی کارفرما —
«پیشنهاد را برنده شدیم؛ در بازرسی بعضی اقلام مردود شد و قرار شد بعضی اقلام با قیمت جدید پیش‌فاکتور شوند.
اقلام مردود هم بعضی وارد انبار می‌شوند و بعضی به فروشنده عودت می‌روند. باید بتوان سند برد را از همان پروندهٔ فروش تغییر داد و گزارشش را داشت.»

ارزیابی مبنا: `ASSESSMENT-AWARD-REVISION-AND-REF-PRICE-2026-08-17.md` (بخش ۳).

---

## P4 — بازرسی قلم‌به‌قلم و سرنوشت اقلام مردود

کاشی جدید **«🔬 بازرسی قلم‌به‌قلم»** در کشوی پروندهٔ فروش (فقط پرونده‌های برنده).

- جدول اقلام از **سند برد فعلی** پرونده ساخته می‌شود (snapshot قفل‌شدهٔ برد، نه دادهٔ آزاد).
- برای هر قلم: تعداد مردود + **سرنوشت** (📦 انبار / ↩️ عودت به فروشنده / 🔧 دوباره‌کاری / 🗑 اسقاط) + بهای واحد + توضیح.
- گاردها: تعداد مردود نمی‌تواند از تعداد سند برد بیشتر باشد؛ شرح بازرسی اجباری؛ برای اقلام عودتی انتخاب تأمین‌کننده اجباری.
- رکورد روی خود پرونده می‌نشیند (`deal.inspections[]`) و در timeline پرونده هم ثبت می‌شود.

**اثر عملیاتی/مالی هر سرنوشت:**

| سرنوشت | اثر |
|--------|-----|
| 📦 انبار | رکورد «موجودی انبار» (`ptf_crm_surplus`) با منبع پرونده و شمارهٔ بازرسی |
| ↩️ عودت به فروشنده | سند **مرجوعی خرید** (`ptf_crm_purchase_returns`) + **اثر مالی فوری** (تصمیم کارفرما): سند اصلاحی **بستانکار** در حساب تأمین‌کننده (`amount` منفی = کاهش بدهی ما) با لینک به سند مرجوعی و پرونده |
| 🔧 دوباره‌کاری / 🗑 اسقاط | فقط ثبت در پرونده (بدون اثر مالی/انباری) |

## P5 — بازنگری سند برد (`revise_award`)

کاشی جدید **«✏️ بازنگری سند برد»** + فرمان سروری اتمیک در `api/sales-domain.php`.

- جدول اقلام سند برد با امکان **حذف قلم**، **تغییر تعداد** و **ثبت قیمت جدید**؛ اگر بازرسی ثبت شده باشد، تعدادها با «تعداد پذیرفته‌شده» **پیش‌پر** می‌شوند و مردودها کنار هر ردیف دیده می‌شوند.
- پیش‌نمایش زنده: مبلغ فعلی / مبلغ جدید / دلتا (کاهش قرمز، افزایش سبز).
- سمت سرور:
  - سند برد قبلی **حذف نمی‌شود**: `status=superseded` + `supersededByOfferId/No/At` و قابل استناد می‌ماند.
  - **سند برد جایگزین** ساخته می‌شود (`<شمارهٔ قبلی>-R<n>` با `revisionOf`, `revisionSeq`, `revisionReason`, snapshot قفل‌شده).
  - پرونده به سند جدید می‌چسبد: `wonOffer`, `rootOfferId`, `contractAmount`, **`effectiveContractAmount`** و ردیف در `awardRevisions[]`.
  - `correction` با نوع `revise_award` + **بازسازی تخصیص‌ها** در همان تراکنش.
  - گاردها: نقش (`SD_WIN_ROLES`)، **دلیل اجباری**، شمارهٔ تکراری (`revision_no_exists`)، و **مسدودی کاهش پس از صدور فاکتور رسمی** (`official_invoice_blocks_decrease`) — طبق تصمیم کارفرما، با پیام فارسی و هدایت به مسیر اصلاحیه/ابطال فاکتور.
- چون از v34.7.29 قیمت پیش‌فرض پیش‌فاکتور از «پیشنهاد مالی پرونده» خوانده می‌شود، **پیش‌فاکتور بعدی خودکار قیمت‌های جدید را می‌گیرد**.

## P6 — گزارش «بازنگری سند برد و سرنوشت اقلام»

کاشی **«📑 گزارش بازنگری»** (فقط وقتی بازنگری یا بازرسی ثبت شده باشد):

- کارت‌های مالی: مبلغ اولیه، مبلغ مؤثر فعلی، دلتا، فاکتورشده، وصول‌شده، مطالبهٔ باز (از منبع واحد `PTF.ar`).
- جدول بازنگری‌ها: از/به، مبلغ قبلی/جدید، دلتا، زمان و کاربر، دلیل.
- جدول اقلام مردود: تعداد سند/پذیرفته/مردود، سرنوشت، ارزش، شمارهٔ گزارش بازرسی.
- جمع تفکیکی سرنوشت‌ها (تعداد ردیف، تعداد کالا، ارزش) + شمار اسناد مرجوعی خرید.
- خروجی **چاپ** و **CSV** (با BOM برای اکسل فارسی).

---

## آزمون

- تستر جدید `_tools/uat/tester434-v34.7.31-award-revision.js` — **۳۳ PASS / ۰ FAIL**: رفتاری در `vm` برای P4/P6 (شامل علامت بستانکار، لینک‌ها، تفکیک سرنوشت‌ها) + قرارداد سرور برای P5 + سه assertion عدم‌رگرسیون (اسقاط بی‌اثر، عودت بدون تأمین‌کننده بی‌اثر، پروندهٔ بدون بازنگری بدون خطا).
- گیت CI: **۴۹ PASS / ۲ FAIL** (همان دو `known-open` فاز G).
- سوئیت کامل: **۴۱۷ تستر — ۳۵۸ سبز، ۵۹ قرنطینهٔ مستند، ۰ شکست واقعی**.
- نگهبان معماری: **PASS** (قاعدهٔ A7 یک شکست خاموش در ماژول جدید را گرفت و همان‌جا اصلاح شد).
- توازن ساختاری PHP: OK. بامپ نسخه در ۷ نقطه ⇒ **v34.7.31**؛ ماژول جدید `crm/case-revision.js` در `index.html` و در فهرست SYNTAX گیت ثبت شد.

## باقی‌مانده

- **P7** (اثر مالی پیشرفتهٔ عودت): اثر پایه همین حالا اعمال می‌شود (بستانکاری تأمین‌کننده). اتصال به تعهدات پرداخت/فاکتور خرید مشخص (تخصیص بستانکاری به فاکتور خرید) در صورت نیاز شما گام بعدی است.
- موارد قبلی بدون تغییر: فاز G (بقیهٔ اقلام)، AN-04، H1.
