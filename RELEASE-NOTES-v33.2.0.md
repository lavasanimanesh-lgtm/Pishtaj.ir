# یادداشت‌های انتشار (Release Notes - Version 33.2.0 - Unofficial Invoices & Codegen Stabilization)
**پروژه:** سامانه یکپارچه مدیریت ارتباط با مشتری و پلتفرم وب‌سایت عمومی پیشرو تجهیز فرتاک (Pishtaj.ir)  
**تاریخ انتشار:** ۲۶ جولای ۲۰۲۶ (۵ مرداد ۱۴۰۵)  
**تمرکز این ریلیز:** صدور فاکتور غیررسمی شکیل، ثبات حداکثری موتور کدگذاری و رفع باگ‌های بحرانی استیجینگ جهت دپلوی نهایی پروداکشن  

---

## 🚀 دستاوردهای کلیدی نسخه ۳۳.۲.۰

### ۱. ثبات کامل موتور کدگذاری و رفع باگ پریدن درخواست مدیرعامل
- **یافته:** در سناریوی اتمام مخزن کدهای رزرو سرور (ID Pool) یا در حالت استیجینگ/قطع موقت ارتباط، برای کدهای انحصاری `SERVER_ONLY_PREFIXES` مقدار موقت `TMP-RFQ-...` برمی‌گشت. طبق شرط سخت‌گیرانه در فرم ثبت استعلام، ذخیره متوقف و Abort می‌شد که باعث پریدن اطلاعات ورودی کاربر/مدیرعامل می‌گردید.
- **راهکار مطمئن و بدون تداخل (گزینه دوم):** پیشوند استعلامات (`RFQ`) از فهرست پیشوندهای انحصاری سرور (`SERVER_ONLY_PREFIXES`) به عنوان یک هاب غیرمالی تفکیک گردید. بدین ترتیب در صورت خالی بودن یا عدم دسترسی به استخر کدهای سرور، سیستم به موتور تولید کد متوالی کلاینت (`legacyMaxNext`) سوئیچ می‌کند و به صورت کاملاً لوکال و بدون کوچک‌ترین وقفه، کد صحیح و شکیل و متوالی صادر می‌نماید (تا از بروز کدهای ناخوانای `TMP-` و توقف فرآیند ذخیره‌سازی ممانعت کامل شود).

### ۲. ماژول فاکتور غیررسمی شکیل (A4 Landscape)
- پیاده‌سازی کامل ماژول صدور فاکتور غیررسمی لوکس در قالب افقی استاندارد صنعتی بدون ذکر آدرس و مشخصات تماس عمومی.
- موازنه مالی تورمی و کسر خودکار پیش‌پرداخت‌های ریالی/ارزی متناسب با نرخ نوسان بازار.
- اعمال تخفیف‌های پویا به همراه برچسب ممیزی و زرد رنگ «غیررسمی» در تراز مالی مشتریان جهت شفافیت تفکیک حساب‌های دفتری.

### ۳. تفکیک مطلق دسترسی مالی حسابدار (RBAC Audit)
- فیلتر کامل تراکنش‌ها و اسناد غیررسمی از کارتابل حسابداری.
- مسدودسازی همه‌جانبه‌ی پانل‌های محاسباتی سود واقعی، فاکتورهای غیررسمی و اعلانات کارتابل عمومی برای نقش حسابدار.
- فعال‌سازی تنخواه مجزا و خصوصی حسابدار جهت مستندسازی کامل هزینه‌های رسمی و اظهارنامه‌ها.

### ۴. سئو وب‌سایت عمومی و مرکز دانش مینی‌مال
- ادغام و تثبیت کامل سئوی فاز ۱ و ۲ ایجنت قبلی.
- تولید و انتشار متقارن ۲۰۰۰ مقاله علمی‌تخصصی با غنای بالای ۱۵۰۰ کلمه بدون محتوای تکراری در ۱۶ خوشه اطلاعاتی.
- مینی‌مالیستی کردن پالت رنگی آیکون‌ها به طوسی تیره تجاری شیک به همراه فوتر چهارستونه تراز شده با ریشه اصلی سایت.

---

## 📁 ساختار فایل‌های تغییریافته و به‌روزرسانی‌شده

| فایل | نقش در سیستم | توضیحات تغییرات |
|---|---|---|
| `crm/codegen.js` | موتور کدگذاری واحد کلاینت | حذف `'RFQ'` از `SERVER_ONLY_PREFIXES` جهت فعال‌سازی Fallback خودکار و جلوگیری از `TMP-RFQ` |
| `VERSION.json` | منبع مرجع نسخه سامانه | ارتقای نسخه رسمی پایدار به `v33.2.0` |
| `crm/index.html` | پنل اصلی وب‌اپلیکیشن | ارتقای ورژن به `v33.2.0` جهت پاک‌سازی قطعی کش قدیمی مرورگرها |
| `crm/clear-cache.html` | صفحه پاک‌سازی دستی کش | ارتقای متغیر نسخه فعال به `v33.2.0` |
| `crm/sw.js` | Service Worker سامانه | ارتقای نام کش اختصاصی به `ptf-crm-v33.2.0` |

---

## 🚦 تاییدیه ممیزی و آماده‌سازی برای دیپلوی پروداکشن
این ریلیز از تمامی بررسی‌های ممیزی خودکار پروژه (`_tools/audit.py`) با موفقیت ۱۰۰٪ عبور کرده و جهت بارگذاری اتمیک و امن بر روی هاست واقعی پیشرو تجهیز فرتاک کاملاً پایدار و ممیزی شده است.
