# CRM v34.38.20 — INV-ARCHIVED-SETTLED

**تاریخ:** 2026-09-11
**بازوی تغییر:** Arena Agent
**کانال:** CRM (پنل فاکتورهای رسمی)

> این فایل همان «یادداشت انتشار نسخهٔ جاری» است که گیت CI (تستر۵۷۸، سطر ۷۹) وجودش را
> با کلید `VERSION.json → crm_version` کنترل می‌کند.

## ۱) درخواست کارفرما

فاکتور تسویه‌شده‌ای که پروندهٔ فروش مرتبطش مختومه/بایگانی شده، در بخش «فاکتورها»
به‌صورت «پروندهٔ فروش ثبت‌نشده» نمایش داده می‌شد. چنین فاکتورهایی باید به بخشِ
جداگانه‌ای به‌عنوان فاکتورهای تسویه‌شده منتقل شوند و بنویسند «پرونده مختومه شده است».

## ۲) چه عوض شد

### ریشهٔ باگ
`renderInvoices()` در `crm/official-invoice-v2.js` فقط پروندهٔ **فعال**
(`ptf_crm_deals`) را می‌شناخت. `salesfiles.js` در `sfArchive` پروندهٔ مختومه را از
`ptf_crm_deals` حذف و به `ptf_crm_projects` (`state:'archived'`, `origin:'salesfile'`)
منتقل می‌کند؛ بنابراین ردیفِ فاکتورِ تسویه‌شده به فالبک `⛔ پرونده فروش یافت نشد`
می‌رسید — در حالی که پرونده پیدا شده و فقط بایگانی است.

### اصلاح (فقط نمایش — بدون تغییر داده)
- helper خودکفای `findArchivedCaseForOffer(o)` پیوند پیشنهاد → پروندهٔ بایگانی را
  اثبات می‌کند (تطبیق `wonOffer`/`offerNo`/`offerNos` با گارد `origin==='salesfile'`
  و `state==='archived'` و سازگاری هویت `inqNo`).
- فاکتور **تسویه‌شده** با پروندهٔ مختومه به بخش جدید
  «🧾 فاکتورهای تسویه‌شده با پروندهٔ مختومه» منتقل می‌شود و ردیف
  «📁 پرونده مختومه شده است» نشان می‌دهد — نه «یافت نشد».
- پروندهٔ مختومهٔ **غیرِ تسویه‌شده** هم «یافت نشد» نمی‌گیرد و با همان برچسب مختومه
  سرِ جایش می‌ماند.
- پروندهٔ واقعاً غایب، پیام قبلی `⛔ پرونده فروش یافت نشد` را حفظ می‌کند.
- کنتور سربرگ «🗂 مختومه» + جستجو/حالت‌خالی هم بخش جدید را می‌بینند.

## ۳) سازگاری و نسخه
- تمام نقاط رسمی نسخه → v34.38.20 (VERSION.json، manifest، sw.js، index.html و
  ۱۱۶ کوئری `?v=`، sales-domain.php، clear-cache.html، shell.js، cms.js،
  device-reconnect.html) — همزمانی آن را A6 از arch-guard کنترل می‌کند.
- پین‌های نسخهٔ جاری تسترهای قدیمی مطابق روال هر ریلیز به v34.38.20 بامپ شدند؛
  مهرهای تاریخیِ کامنت سورس (`v34.38.19 (BOOT-BAR-QUIET)` در sync.js و
  `v34.38.19 (LIQUID-RING)` در boot-splash.js) دست‌نخورده ماندند و دو پینِ
  tester627 که به آن مهرها اشاره می‌کنند صریحاً روی 34.38.19 بازگردانده شدند.

## ۴) تست و تأیید
- تستر جدید: `_tools/uat/tester643-v34.38.20-invoice-archived-settled.js`
  (۲۷ سنجه: مسیر کانُنیکال `invoiceState`، ماندهٔ کهنهٔ `openAmountIRR`،
  مختومهٔ غیرِ تسویه‌شده، بدون پرونده، پروندهٔ فعال، گاردهای `origin`/`state`/
  `offerNos`/هویت).
- گیت CI کامل و arch-guard سبز.
