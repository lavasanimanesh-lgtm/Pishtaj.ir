# ARENA · RCA — «مهر امضا در نامهٔ صادره به صفحهٔ بعد منتقل می‌شود / صفحهٔ خالی بین متن و امضا»

**تاریخ:** ۲۰۲۶-۰۹-۱۶ (۱۴۰۵/۰۶/۲۵) · **برچسب تغییر:** CRM **v34.38.23** (LETTER-SIG-PAGINATION) · **تستر:** `tester658-v34.38.23-letter-sig-pagination.js`
**دامنه:** `crm/letters.js` (فقط رندر چاپ) · `_tools/uat/tester658-…js` · `_tools/uat/run-ci-gate.js` · `VERSION.json` · نقاط رسمی نسخه/cache-bust

---

## ۱) گزارش کارفرما

> در قسمت مکاتبات ثبت نامهٔ صادره، مهر امضا به صفحهٔ بعد منتقل می‌شود و گاهاً یک
> صفحهٔ خالی هم بین متن و امضا وجود دارد.

مسیر UI: مکاتبات ⇒ ثبت/صدور نامهٔ صادره ⇒ «خروجی PDF باز شود؟» / «🖨 با امضای
دیجیتال» / «🖨 بدون امضا» ⇒ `letPrint` ⇒ **`letPrintObj`** (crm/letters.js) ⇒
`ptfPreviewPrintableDoc` (iframe) ⇒ چاپ مرورگر. هر سه دکمهٔ چاپ نامه از همین یک
تابع رندر می‌شوند.

## ۲) ریشه‌ها — سه نقص مستقل، هر سه با مکانیزم متفاوت

### ریشهٔ ۱: بلوک امضا شکستنی بود («مهر به صفحهٔ بعد می‌رود»)
- `.sig{margin-top:12mm;display:flex;justify-content:flex-end}` و
  `.sigbox{text-align:center;position:relative;min-width:60mm}` **هیچ**
  `break-inside:avoid` نداشتند.
- وقتی انتهای متن به ~۲۳۶mm به‌بعدِ صفحه می‌رسید (بلوک امضا با حاشیه ~۶۱mm و در حالت
  امضای فیزیکی ~۸۷mm ارتفاع دارد)، مرورگر بلوک را **بین صفحات نصف** می‌کرد:
  «با تجدید احترام/نام/سمت» در صفحهٔ قبل، و **تصویر امضا** (`img.s`) و **مهر**
  (`img.st{position:absolute;top:14mm}` نسبت به sigboxِ شکسته‌شده) در صفحهٔ بعد.
- مهر چون absolute است، در کانتینرِ fragmentشده رفتار قطعه‌بندیِ بدتری هم دارد
  (جابه‌جایی/کلیپ بین قطعه‌ها) — همان «مهر امضا به صفحهٔ بعد منتقل می‌شود».
- **شاهد درون‌مخزنی:** مسیر «متن آماده روی سربرگ» (v34.7.60) و چاپ پیشنهادها
  (offers-pro) هر دو از قبل `break-inside:avoid`/`page-break-inside:avoid` روی بلوک
  امضای خود دارند (`.endsig` و `.sig`) — مسیر استاندارد نامه این قرارداد را نداشت.

### ریشهٔ ۲: جدول بلند + `page-break-inside:avoid` ⇒ «صفحهٔ خالی»
- `.body table{…;page-break-inside:avoid}` (خط ۱۱۷۶ قبل از اصلاح).
- باگ شناخته‌شدهٔ کروم: جعبهٔ unbreakable که از باقی‌ماندهٔ صفحه بلندتر است به صفحهٔ
  بعد هل داده می‌شود؛ اگر از **یک صفحهٔ کامل** هم بلندتر باشد، تلاشِ avoid می‌تواند یک
  **صفحهٔ سفید** قبل از شروع جدول تولید کند ⇒ «گاهاً یک صفحهٔ خالی بین متن و امضا».
- نامه‌های صادرهٔ دارای جدول (فهرست اقلام/قیمت/زمان‌بندی) دقیقاً همین حالت را می‌سازند؛
  ادیتور خودِ ماژول تا ۱۲ سطر جدول درج می‌کند (۱۲ سطر × ~۱۴mm ≈ ۱۷۰mm — به‌راحتی
  بلندتر از باقی‌ماندهٔ صفحه).
- **شاهد درون‌مخزنی:** offers-pro از v286 (tester159-v286-offer-print-pagination) همین
  باگ را با الگوی `table{page-break-inside:auto}` + `tr{page-break-inside:avoid}` +
  `thead{display:table-header-group}` حل کرده — الگوی اثبات‌شدهٔ خانه.

### ریشهٔ ۳: دمِ نامرئیِ کپی Word ⇒ پرش بی‌دلیل امضا و صفحهٔ «خالی»
- `letSafeBodyHtml`/`letSafeStyle` (درست) استایل‌های page-break ورد را strip می‌کنند،
  ولی **پاراگراف‌های خالی انتهایی** محتوای مجازند و حفظ می‌شوند:
  `<p><br></p>`، `<p>&nbsp;</p>`، `<p><span>&nbsp;</span></p>`، `<br><br>…`.
- با `font-size:14pt` و `line-height:2.1` هر پاراگراف خالی ≈ **۱۰.۴mm** ارتفاع چاپی
  دارد؛ کپی معمول Word با ۱۵+ پاراگراف خالیِ انتها ⇒ **بیش از یک صفحه ارتفاع نامرئی**
  ⇒ متن وسط صفحهٔ ۱ تمام «به‌نظر» می‌رسد، بقیهٔ صفحه سفید است و امضا در صفحهٔ ۲ —
  «یک صفحهٔ خالی بین متن و امضا». حالت خفیف‌تر: چند پاراگراف خالی ⇒ «جا هست ولی
  امضا به صفحهٔ بعد رفت».

## ۳) اصلاح (v34.38.23) — فقط لایهٔ رندر چاپ؛ دادهٔ نامه دست‌نخورده

| # | تغییر در `crm/letters.js` | اثر |
|---|---|---|
| ۱ | `.sig` و `.sigbox` در `letPrintObj`: `break-inside:avoid;page-break-inside:avoid` | بلوک امضا اتمیک — هرگز نصف نمی‌شود؛ اگر جا نشود کامل به صفحهٔ بعد می‌رود (مهر/امضا/نام با هم) |
| ۲ | `.body table` در **هر دو مسیر چاپ**: `page-break-inside:auto` + `.body tr{page-break-inside:avoid}` + `.body thead{display:table-header-group}` | جدول بلند بین سطرها تقسیم می‌شود؛ صفحهٔ خالی حذف؛ سرستون تکرار — الگوی offers-pro |
| ۳ | `letTrimTrailingEmptyHtml()` جدید + `var printBody = l.bodyHtml ? letSafeBodyHtml(l.bodyHtml) : escP(l.body).replace(/\n/g,'<br>'); printBody = letTrimTrailingEmptyHtml(printBody);` | پیرایش **فقط انتهای** بدنه، **فقط زمان چاپ**، **فقط مسیر نامهٔ استاندارد**: بلوک‌های خالی تو در تو (span/b/i/… داخل p/div)، `<br>`ها، `&nbsp;`/`&#160;`/`\u00a0` — حلقه تا نقطهٔ ثابت؛ محتوای مرئی (متن/جدول/تصویر/hr) و فاصله‌های میانی عمداً حفظ می‌شوند |

**قراردادهای حفظ‌شده (مهم):**
- مسیر «متن آماده روی سربرگ»: «متن فقط sanitize می‌شود نه بازنویسی» (تستر۴۶۳) —
  پیرایشِ انتها عمداً در آن مسیر اعمال **نشد**؛ فقط اصلاح جدول (CSS چاپ، نه متن).
- `.content{…min-height:170mm}` (پین تستر۴۶۴)، `.limgs figure/img` (پین تستر۱۹۷)،
  ترتیب body→limgs→sig، فونت‌ها و `.bsm,.to,.torl,.sub,.sigbox{font-family…}` (تستر۵۰۲)،
  substringهای پین‌شدهٔ تستر۳۱۷ (`l.bodyHtml ? letSafeBodyHtml(l.bodyHtml)`،
  `.body table{width:100%`) — همه برقرار ماندند.

## ۴) تست و تأیید

- **تستر۶۵۸** (ثبت‌شده در `run-ci-gate.js`): هارنس vm همان الگوی تستر۵۰۲ —
  `letters.js` واقعی بارگذاری و `letPrint` واقعاً اجرا می‌شود؛ HTML خروجیِ
  `ptfPreviewPrintableDoc` سنجیده می‌شود:
  - سناریو۱: بدنه با `<p><br></p><p>&nbsp;</p><p><span>&nbsp;</span></p><br>` انتهایی ⇒
    چاپ `<div class="body"><p>متن نامه</p></div>` + CSS اتمیک `.sig` + الگوی جدول؛
  - سناریو۲: فاصلهٔ خالیِ **میانی** حفظ و فقط دم پیرایش؛
  - سناریو۳: مسیر متن ساده با `\n\n\n` ⇒ بدون `<br>` انتهایی؛
  - سناریو۴: جدولِ دارای محتوا در انتها حفظ؛
  - واحدِ `letTrimTrailingEmptyHtml`: تو در توی عمیق، تصویر/hr انتهایی حفظ،
    idempotent، هر سه شکل nbsp.
- **اثبات تشخیص:** اجرای تستر۶۵۸ روی کد HEAD (قبل از اصلاح) ⇒ **۱۰ FAIL** و خروجیِ
  سناریوها دقیقاً دمِ خالیِ چاپ‌شده را نشان می‌دهد
  (`<div class="body"><p>متن نامه</p><p><br></p><p>&nbsp;</p><p><span>&nbsp;</span></p><br></div>`
  چسبیده به `.sig`) — روی کد اصلاح‌شده: **۲۱ PASS / 0 FAIL**.
- تسترهای موجود مکاتبات: ۴۶۳ (۳۳/۰) · ۴۶۴ (۳۲/۰) · ۴۶۵ (۱۰/۰) · ۴۶۷ (۳۰/۰) ·
  ۵۰۲ (۵۰/۰) — همه سبز. (۱۹۷/۳۱۷: legacyِ خارجِ گیت، قبل و بعد یکسان.)
- **گیت کامل CI: 269 PASS / 2 FAIL** — مجموعهٔ شکست عیناً بیس‌لاین پیش‌موجودِ HEAD
  (tester427: دو سناریوی کارکردی AN-05؛ tester656: قالب خروجی با harness) و بی‌ربط به
  مکاتبات. arch-guard A6 سبز.
- محدودیت سندباکس: کروم/puppeteer برای شمارش واقعی صفحات PDF در دسترس نبود
  (دانلود مرورگر مسدود)؛ تأیید رفتاری با اجرای واقعی تابع رندر در vm + قراردادهای
  CSSِ همان الگوهایی انجام شد که در offers-pro/مسیر سربرگ سال‌هاست در پروداکشن
  امتحان شده‌اند. توصیه: یک چاپ دستیِ نمونه (نامهٔ جدول‌دار + کپی Word) پس از استقرار.

## ۵) نکتهٔ انتشار

- هیچ مهاجرت داده‌ای لازم نیست؛ `ptf_crm_letters` دست‌نخورده — پیرایش فقط روی خروجی
  چاپ است و متن ذخیره‌شدهٔ نامه تغییر نمی‌کند (سند تاریخی محفوظ).
- استقرار طبق روال (FTP پس از گیت سبز) با cache-bust `?v=34.38.23`؛ در صورت مشاهدهٔ
  رفتار قدیمی: `crm/clear-cache.html`.
