# Release Notes v34.7.21 — فاز C: یکپارچگی چرخهٔ عمر سند و اثر مرجوعی فروش

تاریخ: ۱۴۰۵/۰۵/۲۶ (2026-08-17)

## زمینه

گزارش تلفیقی «تغییر سند قطعی برد» دو شکاف را در چهار موتور مالی نشان داده بود: تعریف ناهمگون «سند فعال» (§۹) و نادیده‌گرفتن مرجوعی فروش (§۸). فاز C هر دو را با **منبع واحد `PTF.ar`** می‌بندد. کسر مرجوعی از مبنای سود و پورسانت با تصویب صریح کارفرما (۱۴۰۵/۰۵/۲۶) انجام شده است.

## LC-01 🔴 تعریف واحد «سند فعال»

پیش از این، هر موتور تعریف خودش را داشت و فقط `void` را کنار می‌گذاشت:

| موتور | قبل | بعد |
|---|---|---|
| پورسانت (`commission.js`) | فقط `status === 'void'` | `PTF.ar.activeInvoice` (شامل `superseded`/`replaced`) |
| سرمایه در گردش (`working-capital.js`) | `void`/`deleted` | همان تعریف واحد + خواندن مانده از `PTF.ar` |
| سال مالی (`fiscal.js`) | `void`/`st==='void'` | همان تعریف واحد |
| سود پروژه (`fx.js`) | فیلتر غیررسمیِ دارای رسمی | همان فیلتر + تعریف واحد سند فعال |

**مهم‌ترین اثر:** صورتحساب غیررسمیِ جایگزین‌شده با فاکتور رسمی، دیگر **دوبار** در مبنای پورسانت نمی‌نشیند. در سناریوی آزمون، مبنای پورسانت از ۲٬۰۵۰م به ۱٬۱۵۰م اصلاح شد (حذف ۹۰۰م دوبارشماری + کسر ۲۰۰م مرجوعی).

## LC-02 🟠 کسر مرجوعی فروش از چهار موتور

قاعدهٔ مقاوم تطبیق مرجوعی — که تا امروز فقط در حساب مشتری بود — به لایهٔ مشترک منتقل شد:

```
PTF.ar.salesReturnsForInvoice(inv)     فهرست مرجوعی‌های همان فاکتور
PTF.ar.returnedAmountIRR(inv)          جمع مبلغ مرجوعی فعال
PTF.ar.invoiceNetAfterReturnsIRR(inv)  مبلغ خالص سند (کف صفر)
```

قواعد تطبیق دقیقاً مطابق پیاده‌سازی موجود: اولویت با `invoiceCd`؛ در نبود آن `invoiceNo`؛ و `offerNo` فقط وقتی همان پیشنهاد **دقیقاً یک** فاکتور فعال داشته باشد. مرجوعی `void` هیچ اثری ندارد.

مصرف در چهار موتور:
- **سود پروژه:** فروش شناسایی‌شده = مبلغ خالص فاکتور − مرجوعی (پیش از این سود با کالای برگشت‌خورده متورم می‌ماند).
- **پورسانت:** مبنا = خالص پس از مرجوعی؛ قرارداد «پرداخت فقط پس از تسویهٔ کامل پرونده» (v34.5.35) دست‌نخورده است.
- **سرمایه در گردش:** مطالبات مشتری = مبلغ − وصولی − مرجوعی، و مانده از منبع واحد `PTF.ar` خوانده می‌شود.
- **سال مالی:** مطالبات باز به همین شکل خالص می‌شود.

## LC-03 🟡 دو دستهٔ جدید در گزارش «🧮 تسویهٔ مطالبات»

- **سند جایگزین‌شده که هنوز در گزارش‌ها وزن دارد** — برای پیدا کردن رکوردهای تاریخی مشکوک.
- **مرجوعی ثبت‌شده که در مبلغ خالص اثر نکرده** — از جمله حالتی که مرجوعی روی فاکتور تسویه‌شده نشسته و باید به‌عنوان اعتبار مشتری تعیین‌تکلیف شود.

## چرا این تغییر «بدون آسیب» است

- همهٔ اتصال‌ها با الگوی `if (PTF.ar) … else رفتار قبلی` نوشته شده‌اند؛ اگر لایهٔ مشترک بارگذاری نشود، هر موتور دقیقاً مثل قبل کار می‌کند.
- هیچ رکوردی نوشته یا مهاجرت نشد؛ فقط مبنای محاسبه اصلاح شد.
- قواعد کسب‌وکاری موجود (تسویهٔ کامل برای پورسانت، فیلتر غیررسمیِ دارای رسمی در سود، ساختار snapshot سال مالی) دست‌نخورده‌اند.

> ⚠️ **انتظار تغییر عدد:** اعداد پورسانت، سود پروژه، مطالبات سرمایه در گردش و مطالبات باز سال مالی برای پرونده‌های دارای مرجوعی یا سند جایگزین‌شده **کاهش خواهند یافت** — این اصلاحِ خطاست، نه تغییر سیاست. پیش از تصویب دورهٔ پورسانت بعدی، یک بار گزارش «قبل/بعد» با مالی مرور شود.

## تست رگرسیون

- `_tools/uat/tester424-v34.7.21-lifecycle-returns.js`: **31 PASS / 0 FAIL** — شامل اجرای واقعی لایهٔ AR روی سه سناریوی مرجوعی (لینک مستقیم، لینک با شمارهٔ فاکتور، مرجوعی ابطالی)، آزمون عددی مبنای پورسانت، و تولید گزارش تسویه با دسته‌های جدید.
- تسترهای فازهای قبل بدون شکست: `tester423` ۳۷/۰، `tester422` ۳۳/۰، `tester421` ۳۸/۰، `tester420` ۳۰/۰.
- گیت CI: **۳۹ PASS / ۲ FAIL** — همان دو شکست پیش‌ازموجود (`tester403`, `tester415`).
- سوئیت کامل (۴۰۷ تستر): **۶۲ شکست، صفر شکست جدید** نسبت به baseline فاز B.
- بهداشت نسخه در ۶ نقطه روی `v34.7.21`.

## گام بعد

فاز D: گاردهای پیشگیرانه — رد متمم با مبلغ ≤ ۰، هشدار پروندهٔ موازی هنگام برد CO هم‌استعلام، تکمیل کشف وابستگی در پیش‌بررسی حذف، و یکسان‌سازی نسخهٔ endpoint سلامت.
