# 🚀 ریلیز v86.0 — اسپرینت ۸۶: آیکون‌های مینیمال + تغییر رمز + انقضای نشست + خواندن هوشمند فایل استعلام

**تاریخ:** ۱۵ تیر ۱۴۰۵ | **دامنه:** US-192 + US-193 + US-194 + US-195 (چهار استوری جدید شما)

---

## 🎨 US-192 — آیکون‌های مینیمال حرفه‌ای (ماژول جدید `icons.js`)
همه ایموجی‌های سایدبار و سر دسته‌های منو با **آیکون‌های خطی SVG یکدست** جایگزین شدند: استروک یکنواخت 1.7، تک‌رنگ (هم‌رنگ متن menu)، هماهنگ با پوسته روشن و حالت شب — ۲۶ آیکون اختصاصی طراحی شد (داشبورد، استعلامات، تامین، کالا، پیشنهادها، مالی، پیامک، ...). داخل پنل‌ها دست نخورده تا حجم تغییر یکجا زیاد نشود.

## 🔑 US-193 — تغییر رمز عبور توسط خود کاربر
- دکمه **«🔑 تغییر رمز عبور»** پایین سایدبار برای همه کاربران: رمز فعلی + رمز جدید (حداقل ۶ کاراکتر) + تکرار.
- کاربران عادی: هش جدید + **سینک سروری** → با رمز جدید از هر دستگاهی وارد می‌شوند.
- ادمین: هش جدید در تنظیمات سینک‌شونده ذخیره و صفحه ورود هر دو هش (اولیه/جدید) را چک می‌کند.

## ⏳ US-194 — انقضای نشست پس از ۸ ساعت عدم فعالیت
هر کلیک/تایپ/لمس زمان فعالیت را ثبت می‌کند؛ چک هر ۵ دقیقه + هنگام باز شدن صفحه. بیش از ۸ ساعت بی‌فعالیتی → خروج خودکار با پیام و بازگشت به صفحه ورود. **داده‌ها دست نمی‌خورند.**

## 📖 US-195 — خواندن فایل استعلام + تشخیص تایپ + قالب استعلام شرکت (ماژول جدید `inqreader.js`)

### جریان کامل مطابق سناریوی شما:
1. درخواستی که **فایل استعلام** (PDF/عکس/اکسل) دارد → دکمه **«📖 خواندن فایل استعلام»** روبروی آن در فهرست استعلامات ظاهر می‌شود.
2. ویزارد بازبینی باز می‌شود: فایل‌های پیوست قابل مشاهده‌اند؛ اکسل/CSV مستقیم خوانده می‌شود (استنتاج هوشمند: بلندترین متن=شرح، عدد=تعداد، واحد شناخته‌شده=واحد). برای PDF/عکس، اقلام را حین دیدن فایل کنارش ثبت/اصلاح می‌کنید (OCR خودکار پس از رسیدن کلید LLM فعال می‌شود).
3. **🎯 تشخیص خودکار تایپ** هر ردیف از شرح (اگر فایل ستون تایپ نداشت): ۱۹ قاعده — Pipe، Elbow، Tee، Flange، Valve، Gasket، Bolt & Nut، Instrument، Cable، Plate، Fitting و... — در **ستون جدا قبل از شرح**. هر ردیف قابل اصلاح است و **بدون تایید شما هیچ چیز ثبت نمی‌شود**.
4. خروجی **قالب استعلام قیمت رسمی PTF** بدون هیچ اطلاعات کارفرما، به نام شماره استعلام:
   - **📗 Excel** (`INQ-777.csv`) با ستون‌های No./Type/Description/Spec/Qty/Unit/Unit Price/Total/Delivery/Remarks (قیمت‌ها خالی برای تامین‌کننده)
   - **🖨 PDF رسمی** هم‌خانواده سربرگ (نوار گرادیان + خطوط مورب + لوگو + REQUEST FOR QUOTATION)
5. **تایید نهایی → ورود به ماژول کالا** با «مارک غیرقابل مشاهده» و تفکیک تایپ: این کالاها در فهرست عادی دیده نمی‌شوند (چک‌باکس «نمایش کالاهای استعلامی» کنار جستجوی کالا)؛ تکراری‌ها با نرمال‌سازی ضد تکرار اضافه نمی‌شوند. اقلام همزمان به «اقلام درخواست» هم می‌روند.
6. در **استعلام هوشمند تامین**: دکمه «📤 دانلود استعلام (قالب شرکت)» → انتخاب درخواست → Excel یا PDF.
7. در **TO/CO**: دکمه «بارگذاری اقلام درخواست» کل اقلام آن درخواست را فراخوانی می‌کند و اگر تایپ داشته باشند، **ستون Type خودکار اضافه و پر می‌شود** — فقط قیمت/برند را تکمیل می‌کنید.

## ✅ کیفیت

| مورد | نتیجه |
|---|---|
| تستر جدید `tester25-sprint86` (شامل ۶ تست رفتاری تشخیص تایپ فارسی/EN) | ✅ **38/38 PASS** |
| رگرسیون ۲۰ تستر قبلی | ✅ همه PASS |
| ممیز ۸بخشی (۴۳۴ صفحه) | ✅ PASS |
| `node --check` ماژول‌های جدید | ✅ PASS |
| نسخه کش | `ptf-crm-v86.0` |

## 📦 نصب (File Manager)
**`pishtaj-release-v86.0-delta.zip`**: `crm/icons.js` و `crm/inqreader.js` (جدید)، `crm/index.html`، `crm/sw.js`، `crm/sync.js`، `crm/backup.js`، `api/crm.php` + مستندات.

> ⚠️ پیش‌نیاز: بسته‌های v84.1 + v85.x اگر نصب نشده‌اند (letters/offers-pro/listtools/bridge/perms در آن‌هاست).

> 🧪 تست: ① منو → آیکون‌های خطی جدید ② 🔑 تغییر رمز → خروج و ورود با رمز جدید ③ درخواستی با فایل → 📖 → 🎯 تایپ‌ها → 📗 اکسل به نام شماره استعلام (بدون نام کارفرما) → 📦 ورود به کالا → چک‌باکس «نمایش کالاهای استعلامی» ④ TO جدید → بارگذاری اقلام → ستون Type پر شده.
