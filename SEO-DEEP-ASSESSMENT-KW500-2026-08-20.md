# ارزیابی عمیق سئو + نقشهٔ راه «۵۰۰ کلمهٔ کلیدی در صفحهٔ اول گوگل»

**تاریخ:** ۱۴۰۵/۰۵/۲۹ — 2026-08-20 | **دامنه:** pishtaj.ir | **مبنا:** ممیزی کامل سورس (۶۵۰ صفحهٔ HTML) + اسناد سئوی قبلی
**هدف اعلام‌شدهٔ کارفرما:** حداقل **۵۰۰ کلمهٔ کلیدی مرتبط با زمینهٔ فعالیت** در صفحهٔ اول گوگل

---

## ۱) وضعیت فعلی — اندازه‌گیری‌شده از سورس (نه حدس)

### موجودی محتوا

| بخش | صفحات | میانهٔ کلمات | ارزیابی |
|---|---|---|---|
| knowledge-center (دانش‌نامه) | **۴۵۰** | ۱٬۷۱۳ | بزرگ‌ترین دارایی سئو — موتور اصلی ۵۰۰تایی |
| services (خدمات/محصولات) | ۹۴ | ۱٬۸۲۵ | لندینگ‌های تجاری خوب |
| blog | ۲۲ | ۱٬۲۴۳ | سالم |
| brands | ۲۱ | ۱٬۷۲۳ | دارایی کم‌استفاده (کوئری‌های برندی) |
| suppliers (تامین‌کننده) | ۲۱ | ۸۳۴ | صفحات پول‌ساز؛ ۹ تای آن عمیق‌سازی شده |
| comparisons (مقایسه) | ۱۱ | ۱٬۵۳۷ | جدید (PR #54/55) — هنوز داده GSC ندارد |
| industries | ۷ | ۱٬۱۵۰ | سالم |
| **جمع ایندکس‌پذیر** | **~۶۵۹ URL** | | search-index داخلی: ۶۵۳ URL |

### سلامت فنی (نتیجهٔ ممیزی امروز)

| سنجه | نتیجه | حکم |
|---|---|---|
| JSON-LD | ۶۵۲ بلوک، **۰ خراب** (Article×354، TechArticle×9، …) | ✅ عالی |
| alt تصاویر | ۱٬۶۲۱ تصویر، **۰ بدون alt** | ✅ |
| canonical | همهٔ صفحات جز 404 (که درست noindex است) | ✅ |
| description | ۰ صفحهٔ بدون/ضعیف | ✅ |
| تایتل تکراری | فقط **۱ مورد** (بند ۲.۱) | ⚠️ |
| سایت‌مپ | ۷ فایل تفکیکی + ایندکس، همه سالم | ✅ |
| صفحهٔ اصلی | ۲٬۰۰۵ کلمه، H1 یکتا، ۹×H2، اسکیمای Organization اصلاح‌شده (v33.x) | ✅ |
| hreflang fa/en | برقرار | ✅ |

**جمع‌بندی:** زیرساخت فنی و حجم محتوا از ۹۰٪ رقبا جلوتر است. مشکل، «نبود حلقهٔ اندازه‌گیری و هدف‌گیری» است، نه کمبود صفحه.

---

## ۲) مشکلات شناسایی‌شده (به ترتیب اثر روی هدف ۵۰۰)

### ۲.۱ فنی — سریع‌رفع (کد، بدون نیاز به شما)

1. **کانیبالیزیشن تایتل پتروشیمی:** `industries/petrochemical/` و
   `suppliers/petrochemical-equipment-supplier.html` تایتل **کاملاً یکسان** دارند
   («تامین‌کننده تجهیزات پتروشیمی») — گوگل را بین دو صفحه سردرگم می‌کند؛ باید تفکیک نیت
   شوند (صنعت=اطلاعاتی، تامین‌کننده=تجاری).
2. **هاب‌های لاغر:** `comparisons/index` (۱۳۸ کلمه)، `blog/index` (۲۵۱)،
   `knowledge-center/index` (۲۹۱) — هاب بدون متن معرفی، اعتبار موضوعی خوشه را حمل نمی‌کند.
3. **WebP/سرعت:** `<picture>` فقط روی ۱ صفحه از ۶۵۰ است؛ ۱۹۱ استایل اینلاین در صفحهٔ
   اصلی مانده (گام E ناتمام نشست قبل) — CWV مستقیماً فاکتور رتبه است.
4. **حفرهٔ لینک داخلی:** از ۲۲ بلاگ **صفر لینک** به `/comparisons/`؛ از ۴۵۰ مقالهٔ KC فقط ۶.
   خوشهٔ مقایسه‌ها بدون لینک ورودی داخلی، دیر ایندکس/رتبه می‌گیرد.
5. **کانیبال معلق:** `blog/a106-vs-a333-pipes` در برابر `comparisons/a106-vs-a333-pipe` —
   طبق handoff تا دادهٔ GSC صبر؛ موعد تصمیم: اوایل سپتامبر.

### ۲.۲ استراتژیک — مسیر رسیدن به ۵۰۰

6. **بانک کلیدواژه کوچک است:** `KEYWORD-BANK-PHASES-2-3-4.md` فقط **~۱۶۰ کلمه** دارد.
   ریاضی هدف: با نرخ واقع‌بینانهٔ صفحه‌یک‌شدن ۳۰–۵۰٪ برای سایتی با این اتوریته، برای
   ۵۰۰ کلمهٔ صفحه‌یک به **جهان کلیدواژهٔ ~۱٬۲۰۰+ کلمهٔ نگاشت‌شده** نیاز داریم.
   خبر خوب: ۶۵۹ صفحهٔ موجود × میانگین ۲–۳ کوئری قابل‌رتبه = ظرفیت بالقوهٔ ۱٬۳۰۰–۲٬۰۰۰
   کوئری **همین حالا** وجود دارد — فقط نگاشت و بهینه‌سازی on-page سیستماتیک نشده.
7. **حلقهٔ اندازه‌گیری قطع است:** هیچ دادهٔ GSC (کوئری/جایگاه/CTR) در دسترس نیست.
   بدون آن، نمی‌دانیم الان چند کلمه صفحه‌یک داریم و کدام کلمات در جایگاه ۱۱–۳۰
   («دم‌دستی‌ها») منتظر یک تلنگرند — سریع‌ترین مسیر به ۵۰۰ همین‌هاست.
8. **E-E-A-T ناقص:** مقالات بی‌نام نویسنده، بدون کیس‌استادی واقعی، `projects/` و `news/`
   تک‌صفحه و ساکن. برای کوئری‌های پولی («تامین‌کننده…»)، گوگل نشانهٔ تجربهٔ واقعی می‌خواهد.
9. **off-page صفر (خارج از کد):** Google Business Profile، دایرکتوری‌های صنعتی
   (اویل‌شو، indpro و…)، رپورتاژ — طبق گزارش فاز ۱ هنوز اقدام نشده.
10. **en/ رقیق:** ۵ صفحه با hreflang — یا توسعه (اگر هدف صادراتی هست) یا حفظ حداقلی فعلی.

---

## ۳) نقشهٔ راه گام‌به‌گام (پیشنهاد اجرا به همین ترتیب)

### فاز A — پایه و اندازه‌گیری (هفتهٔ ۱)

| گام | شرح | مجری | خروجی سنجش‌پذیر |
|---|---|---|---|
| **A1** | رفع‌های فنی سریع: تفکیک تایتل/محتوای دو صفحهٔ پتروشیمی، متن معرفی ۳۰۰+ کلمه‌ای برای ۳ هاب لاغر، لینک زمینه‌ای blog/KC → comparisons | من (کد) | ✅ **انجام شد 2026-08-20** — اسکریپت `tools/seo_step_a1.py`؛ ۱۷ صفحه؛ هاب‌ها: ۳۴۱/۴۰۸/۴۷۶ کلمه؛ ۱۳ لینک زمینه‌ای بلاگ→مقایسه؛ ۰ تایتل تکراری |
| **A2** | **دادهٔ GSC:** خروجی Performance سه‌ماهه (Queries + Pages، CSV) را بدهید — راهنما: `GSC-EXPORT-GUIDE-FA-2026-08-20.md` | **شما** | baseline: «الان چند کوئری صفحه‌یک داریم؟» |
| **A3** | تحلیل quick-win: کوئری‌های جایگاه ۸–۳۰ → فهرست اولویت‌دار بهینه‌سازی | من (تحلیل) | فهرست ~۱۰۰ کلمهٔ دم‌دستی |

### فاز B — بهینه‌سازی موجودی؛ سریع‌ترین مسیر به ۵۰۰ (هفتهٔ ۲–۶)

| گام | شرح | خروجی |
|---|---|---|
| **B1** | گسترش بانک کلیدواژه از ۱۶۰ به **۱٬۲۰۰+** با خوشه‌بندی (پایپینگ/شیرآلات/ابزار دقیق/برق/دوار/گسکت‌وفلنج/برندی/قیمت‌وخرید لانگ‌تیل) + **نگاشت کلمه→صفحه برای همهٔ ۶۵۹ صفحه** (هر صفحه: ۱ کلمهٔ اصلی + ۲–۴ فرعی) | فایل نگاشت واحد (منبع حقیقت) |
| **B2** | on-page سیستماتیک بر اساس نگاشت: تایتل با الگوی کوئری واقعی (قیمت/خرید/استاندارد/تامین)، H2های پرسشی، **FAQPage schema** روی مقالات KC | ~۴۵۰ صفحه با تایتل/اسکیمای هدفمند |
| **B3** | تکمیل گام E سرعت: WebP سراسری با `tools/seo_webp_html.py` + کاهش استایل اینلاین صفحهٔ اصلی | LCP بهتر در CWV |
| **B4** | تکمیل خوشه‌های لینک داخلی (زمینه‌ای، نه قالبی — طبق نقد متدولوژی) | هر صفحهٔ جدید ≥۳ لینک ورودی |

### فاز C — توسعهٔ هدفمند (ماه ۲–۳)

| گام | شرح |
|---|---|
| **C1** | ساخت لندینگ‌های تجاری «➕» بانک کلیدواژه (فلنج، فیتینگ، مانیسمان، …) — فقط بعد از تأیید تقاضا در GSC/کیوردپلنر |
| **C2** | **کیس‌استادی واقعی** (گام D معوق): ۲–۳ پروژه با چالش/نتیجه/عکس — نیازمند ورودی شما؛ قوی‌ترین سیگنال E-E-A-T |
| **C3** | فعال‌سازی `news/` (ماهانه ۲ خبر واقعی شرکت) — سیگنال تازگی |
| **C4** | تصمیم کانیبال a106-vs-a333 با دادهٔ GSC (canonical یا تفکیک نیت) |

### فاز D — خارج از سایت (موازی، مجری: شما با چک‌لیست من)

GBP + نقشه، ۵ دایرکتوری صنعتی ایرانی، ۲ رپورتاژ/ماه در رسانه‌های صنعت، پروفایل لینکدین شرکت فعال.

### حلقهٔ پایش (از هفتهٔ ۲ به بعد)

گزارش ماهانه از GSC: «تعداد کوئری با میانگین جایگاه ≤۱۰» — همین یک عدد، KPI هدف ۵۰۰ است.
مسیر واقع‌بینانه: امروز (نامعلوم — منتظر A2) → ماه ۱: +۱۰۰ → ماه ۳: ۳۰۰–۳۵۰ → ماه ۵–۶: **۵۰۰+**.

---

## ۴) چرا این ترتیب؟

- ۸۰٪ راهِ ۵۰۰تایی از **بهینه‌سازی ۶۵۹ صفحهٔ موجود** می‌گذرد، نه ساخت صفحهٔ جدید
  (درس نقد متدولوژی: عمق قبل از عرض، اعتبارسنجی تقاضا قبل از تولید).
- بدون A2 (دادهٔ GSC) هر عددی حدس است؛ با آن، ماهانه دقیقاً می‌دانیم چند کلمه صفحه‌یک داریم.
- کارهای فنی A1/B3 ارزان‌اند و اثرشان روی کل ۶۵۹ صفحه ضرب می‌شود.
