# CRM v34.38.14

## رفع باگ «پروندهٔ بایگانی‌شده بعد از رفرش در پرونده‌های فروش هم بود» + قابلیت «به جریان انداختن»

گزارش کارفرما: پرونده‌ای از «پرونده‌های فروش» بایگانی شد، به بایگانی رفت، ولی **بعد از یک
رفرش دوباره در پرونده‌های فروش هم دیده می‌شد**. دو خواستهٔ فرموله‌شده:
۱) یک پروندهٔ فروش باید **ماهیت یکتا** داشته باشد — هم‌زمان هم در بایگانی هم در جریان نباشد.
۲) تا پیش از «حذف قطعی» بتوان پروندهٔ بایگانی‌شده را **به جریان انداخت** (بازگشت به فروش).

### ریشهٔ باگ (بازتولیدشده با اجرای کد واقعی در گیت)

- حذف رکورد فروش هنگام بایگانی از روتر مجموعه‌ای (`ptfEntitySaveCollection`) عبور می‌کرد.
  وقتی پرونده‌ای که بایگانی می‌شد **«آخرین» پروندهٔ باز** بود، ته‌شدن کالکشن به‌عنوان امضای
  «خواندن کهنه/حذف انبوه» تلقی می‌شد و حذفِ عمدیِ کاربر به legacy (setData محلی) تنزل
  می‌یافت — **فرمان `entity_delete` هرگز به سرور نمی‌رسید**. نسخهٔ سرور در pull/merge بعدی
  (union بر کد) پرونده را به فهرست فروش برمی‌گرداند: **هم‌زمان در هر دو محل**.
- رکورد بایگانی (`ARC-*`) هم «cd» نداشت و upsert اتمیک فرمانی نمی‌شد.

### اصلاحات (`crm/salesfiles.js`)

- **حذف اتمیک بایگانی**: `sfRemoveDeal` — پایهٔ صریح (`prevArr`) + عبور سپر حذف انبوه برای
  همین یک حذف عمدی (`allowBulkDelete`)؛ فرمان `entity_delete` همیشه به سرور می‌رسد.
- **رکورد بایگانی با «cd» پایدار** (`ARC-<cd>`): بایگانی از مسیر فرمان اتمیک انجام می‌شود و
  تلاش‌مجدد/دوبار بایگانیِ همان پرونده دقیقاً همان رکورد را upsert می‌کند، نه رونوشت.
- **جاروی یکتایی** (`ptfSalesfileUniquenessSweep`): قانون «یک پرونده = یک محل» در هر رندر
  فهرست فروش (با throttle) و یک‌بار پس از بوت اعمال می‌شود؛ هر نسخهٔ زنده‌ای که رکورد
  بایگانی اثبات‌پذیر داشته باشد حذف می‌شود — **دادهٔ آلودهٔ قبلی هم خودبه‌خود التیام می‌یابد**.

### قابلیت جدید: «↩️ به جریان انداختن پرونده» (`crm/projects.js` + `crm/salesfiles.js`)

- دکمهٔ بازگشت در نمای پروندهٔ بایگانی‌شده (ریشهٔ فروش)، با گارد نقش مثل سایر عملیات حساس
  بایگانی (admin/chairman/ceo/commercial) — تا پیش از «حذف قطعی» هر زمان قابل اجراست.
- پرونده با **«cd اصلی»** برمی‌گردد تا لینک‌های تنخواه/چک/تسویهٔ خودکار (`dealRef`/`dealCd`)
  بی‌آسیب بمانند؛ اسناد، QC، ارسال، هزینه‌ها، زیان‌ها، سنگ‌قبرهای هزینه و تسویهٔ خودکار
  همراه رکورد برمی‌گردند و تایم‌لاین اصلی (که از این نسخه همراه آرشیو نگه داشته می‌شود)
  بازسازی می‌شود.
- **خنثی‌سازی سنگ‌قبر حذف** (`restored:<kind>` — همان الگوی `entity_restore` سرور) تا
  پروندهٔ بازگردانده‌شده در sync بعدی دوباره حذف نشود.
- اگر نسخهٔ فعالی از همان پرونده در فروش وجود داشته باشد (دادهٔ آلودهٔ قدیمی)، مسیر امنِ
  **prefer-live** پیشنهاد می‌شود: نسخهٔ فعال می‌ماند و رونوشت بایگانی حذف می‌شود — خروجی
  هر مسیر دقیقاً «یک پرونده در یک محل» است.

## ابزار جدید: اتصال مجدد امن مرورگر (`crm/device-reconnect.html`)

پس از حادثهٔ حذف درخواست‌های تامین، همان مرورگرِ مُسبّب به چرخهٔ «وصل نمی‌شود» گیر می‌کرد:
صف ارسال آفلاینِ پایدار آن (`q:ptf_b_queue` در IndexedDB به‌همراه مجموعهٔ معلق
`ptf_sync_dirty`) هنوز تلاش می‌کرد دادهٔ کهنه را روی سرور بنویسد و سپر حذف‌انبوهٔ سرور
(v34.38.12) هر بار آن را رد می‌کرد — نشانگر همگام‌سازی قرمز می‌ماند. این ابزار (کاملاً
مستقل از سلامت خود برنامه) چهار مرحله را پشت سر هم انجام می‌دهد:

۱) **تشخیص خودکار** — دسترسی به API (users_get)، اعتبار نشست (data_pull سبک)، وضعیت صف
ارسال و کلیدهای معلق؛
۲) **بک‌آپ کامل محلی** (localStorage + sessionStorage + IndexedDB) — پیش از هر پاکسازی
اجباری است؛
۳) **خنثی‌سازی جراحی‌وار** — صف ارسال + مجموعهٔ dirty + شناسهٔ اجرای sync + واترمارک‌ها +
پایان نشست؛ رکوردهای کسب‌وکار و نشانگرهای «انتقال یک‌باره» دست نمی‌خورند (پاک‌کردن آن‌ها
انتقال یک‌باره را با دادهٔ کهنه از نو راه می‌انداخت)؛
۴) تازه‌سازی اسکریپت‌ها و ورود مجدد — با پاک‌شدن واترمارک، pull کامل از سرور می‌آید و
مرورگر روی نسخهٔ سالم سرور سبز می‌شود.

## نگهداری

- تستر `tester621-v34.38.14-archive-uniqueness-restore.js` (رفتاری روی کد واقعی + نگهبان‌های
  ساختاری + پین‌های انتشار) به گیت CI اضافه شد؛ سناریوی دقیق باگ (بایگانیِ تنها پرونده →
  رفرش) و هر دو مسیر بازگشت پوشش دارند.
- تستر `tester622-v34.38.14-device-reconnect.js` عوارض امنیتی ابزار اتصال مجدد را قفل
  می‌کند («بک‌آپ پیش از خنثی‌سازی»، «کسب‌وکار و انتقال‌یک‌باره دست‌نخورده»، «خواندنی‌بودن
  پروب»، «idempotence»).
- cache-buster و Service Worker به نسخهٔ `34.38.14` ارتقا یافتند.
