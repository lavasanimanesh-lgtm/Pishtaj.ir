# RELEASE NOTES — v34.32.0 CMS-FIX (بازسازی موتور تولید صفحات پنل مدیریت)

**تاریخ:** ۲۰۲۶-۰۹-۰۳ · **گیت:** ۲۰۰/۰ · **نکتهٔ بامپ:** جاروی استاندارد نسخه با `bump-version-pins.js` + نقاط رسمی (index/sw/clear-cache/manifest/shell/sales-domain/cms.js)

## گزارش کارفرما

«یک بررسی از قسمت مدیریت سایت در سی‌آر‌ام داشته باش — صفحه‌هایی که این بخش می‌سازد با قالب سایت فرق دارند و به‌هم‌ریخته‌اند: عبارت‌های کلیدی مثل «فلنج آ ۱۰۵» بالای صفحه لینک‌دار ظاهر می‌شوند، تصاویر بارگذاری‌شده (که روی سرور هستند) نمایش داده نمی‌شوند، و متن از لبه تا لبه بدون حاشیهٔ امن رندر می‌شود.»

## ریشه‌یابی — سه خطای مستقل در موتور تولید

1. **استایل گم‌شده:** سه مولد (`cms_render_public_page`، `kc_create`، `product_create`) کلاس‌های `article-*` و `kc-supply-cta` را در خروجی می‌نویسند، ولی هیچ فایل CSS سایتی این کلاس‌ها را تعریف نمی‌کند (اثبات با `git log -S`). استایل واقعی فقط درون `<style>` درون‌خطیِ اسکلت `knowledge-center/astm-a105.html` است و مولدها هرگز آن را استخراج نمی‌کردند → متن لبه‌به‌لبه.
2. **بردکرامب و منوی کثیف:** مولدها `<nav class="ptf-bc">` اسکلت (با عنوان خود صفحهٔ مرجع، مثلاً «ASTM A105») و کلاس `active` منوی صفحهٔ مرجع را عیناً در هر صفحهٔ جدید کپی می‌کردند → «عبارت‌های لینک‌دار بالای صفحه».
3. **تصاویر حذف می‌شدند:** `<img>` در لیست سفید `strip_tags` نبود و تصویر فقط در `og:image` گذاشته می‌شد → عکس بارگذاری‌شده هرگز در بدنه دیده نمی‌شد.

به‌علاوه، بازنویسی سراسری ۱۲۲ مقالهٔ مرکز دانش (v34.30+) نشانگرهای قدیمیِ استخراج اسکلت (`<section style="background:linear-gradient…`) را از بین برده بود؛ استخراج مولدها پس از آن دیپلوی عملاً با خطا متوقف شده بود.

## راه‌حل

- **هِلپر مقاوم `cms_kc_skeleton($ROOT)`:** استخراج مشترک هدر/سی‌تی‌ای/فوتر از اسکلت مرکز دانش با نشانگرهای جدیدِ مقاوم (`<section class="article-hero"` / `<section class="kc-supply-cta"`) و بازگشت خودکار به نشانگرهای قدیمی؛ حذف بردکرامب و وضعیت فعال منو از هدر؛ استخراج همهٔ بلوک‌های `<style>` هد.
- **تزریق استایل:** `cms_render_public_page` و `product_create` و `blog_create` استایل‌های اسکلت را در `<head>` صفحهٔ تولیدی می‌گذارند؛ بازساز فهرست محصولات (`cms_products_index_rebuild`) پارامتر ششم `$style` گرفت تا `products/index.html` هم هم‌قالب شود.
- **بُردکرامب اختصاصی:** هِلپر `cms_bc_html($crumbs)` با استایل درون‌خطی؛ هر صفحه بردکرامب خودش را می‌گیرد (خانه ← خدمات/صنایع/مقایسه/محصولات/وبلاگ/مرکز دانش ← عنوان) و بردکرامب صفحهٔ مرجع حذف می‌شود.
- **نمایش تصاویر:** `<img>` به لیست سفید هر سه مولد اضافه شد + هِلپر `cms_img_sanitize` فقط مسیرهای داخلی (`assets/…`، `/uploads/…`) را نگه می‌دارد و خارجی‌ها را می‌اندازد؛ تصویر شاخص (غیر از لوگوی پیش‌فرض) بالای بدنه رندر می‌شود.
- تسترهای گیت (`556/558/571/575`) به الگوی جدید هِلپر به‌روز شدند؛ بامپ نسخهٔ سه‌فرمی.

## اثر عملیاتی

صفحه‌های جدیدِ خدمات/صنایع/مقایسه/محصول/وبلاگ از این پس هم‌قالب سایت (هدر/منو/فوتر/استایل/بُردکرامب) تولید می‌شوند و تصاویر آپلودشده در متن دیده می‌شوند. صفحه‌های شکستهٔ فعلی روی سرور (خروجی نسخهٔ قدیم) با بازنشر از پنل درست می‌شوند.
