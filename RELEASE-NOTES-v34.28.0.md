# RELEASE NOTES — v34.28.0 (تب‌های مستقیم — ریشه‌کنی «تب‌های خالی» CMS)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** tester574 (۵ سنجه) · **گیت:** 190 PASS / 0 FAIL

## گزارش کاربر

«مشکل حل نشد. تب‌های صفحه جدید و کیفیت کماکان خالی است. ریشه‌یابی عمیق لازم است.»

## ① RCA عمیق — بازتولید ماشینیِ کامل در jsdom

اپ کامل (index.html + هر ۱۰۷ اسکریپت، نشست ادمین، استاب fetch) بوت شد:

- `goPanel('cms')` مستقیم → پنل رندر می‌شود (۶۶۶ کاراکتر، تب اخبار پیش‌فرض).
- `cmsTab('page')` / `cmsTab('q')` → **هیچ اتفاقی نمی‌افتد؛ حتی یک خطا هم لاگ نمی‌شود.**
- عیب‌یابی زنجیره: `cmsTab(t)` فقط `_tab=t` می‌گذارد و بعد `goPanelByName('cms')` (leads.js) را صدا می‌زند؛ آن تابع بین دکمه‌های سایدبار (`.sb-i`) دنبال onclick حاوی `'cms'` می‌گردد و آن را کلیک می‌کند. **در DOM بوت‌شده صفر دکمهٔ منطبق وجود داشت** (منو/سایدبار توسط بوت‌ریزر بازسازی/پالایش می‌شود) → یافتن بی‌صدا ناموفک → `goPanel` هرگز صدا زده نمی‌شود → تب هرگز رندر نمی‌شود = «تب خالی»، بدون خطا.
- رندر مستقل هر دو تب (فراخوانی مستقیم renderCms) در v34.25.0 و v34.27.0 سالم — یعنی نقص نه در رندرر، بلکه در **زنجیرهٔ وابسته به دکمهٔ سایدبار** بود.

## ② ریشه‌کنی — TAB-DIRECT

`cmsTab` دیگر هیچ وابستگی‌ای به دکمهٔ سایدبار / `goPanelByName` ندارد:

```js
window.cmsTab = function (t) {
  _tab = t;
  var el = document.getElementById('cmsWrap');
  if (el) { renderCms(el); return; }   /* داخل مدیریت سایت — رندر مستقیم */
  if (typeof goPanelByName === 'function') goPanelByName('cms'); /* ورود از بیرون */
};
```

مسیر جایگزین فقط وقتی هست که اصلاً داخل CMS نیستیم (ورود اولیه). گام‌های publish/sched که `cmsTab('q')` صدا می‌زنند هم خودبه‌خود شامل修复 می‌شوند.

**اثبات پس از اصلاح — همان هارنسِ بازتولیدکنندهٔ باگ:** ورود CMS=۶۶۶ → تب صفحهٔ جدید=۱۰٬۳۹۷ کاراکتر (فرم واقعی `pgTopic` موجود ✓) → تب کیفیت=۱۳٬۶۲۳ کاراکتر ✓.

## ③ کشف دوم — پروداکشن هنوز v34.25.0 است

`pishtaj.ir/crm/clear-cache.html` عنوان «v34.25.0» را نشان می‌دهد: **هیچ‌کدام از v34.26.0 / 26.1 / 27.0 روی سرور مستقر نشده‌اند.** بنابراین همهٔ گاردها/بنرهایی که برای همین مشکل ساخته شدند، هرگز روی پروداکشن اجرا نشده‌اند و طبیعی است که کاربر «هیچ تغییری نمی‌بیند». دیپلوی واقعیِ v34.28.0 لازم است.

## ④ بستهٔ انتشار

- `crm/cms.js` — TAB-DIRECT + `PTF_CMS_JS_VER='v34.28.0'`
- `crm/index.html` — ۱۰۹ کش‌باست `?v=34.28.0` + `PTF_CRM_RELEASE`
- `crm/sw.js` — `CACHE='ptf-crm-v34.28.0'` (پاک‌سازی خودکار کش قدیمی SW) + RELEASE/ASSET_VERSION
- `crm/shell.js`، `crm/manifest.json`، `crm/clear-cache.html`، `api/sales-domain.php` (SD_SERVICE_VERSION)، `VERSION.json`
- tester574 (۵ سنجه: رندر مستقیم، typeof-guard، ماندگاری گاردهای ۲۶/۲۷، رفتاریِ بدون-goPanelByName، بهرهٔ publish/sched) + جاروی انکرهای نسخهٔ تسترها

## ⑤ بعد از دیپلوی

۱) `clear-cache.html` را باز کنید و پاک‌سازی را بزنید (نام کش SW عوض شده، ولی برای اطمینان). ۲) ورود → مدیریت سایت → تب «صفحهٔ جدید» و «کیفیت» — باید فوراً محتوا نمایش داده شود. ۳) اگر باز خالی بود، حالا دیگر با گاردهای v34.26.1 پیام خطا/فرم جایگزین می‌بینید — همان پیام را بفرستید.
