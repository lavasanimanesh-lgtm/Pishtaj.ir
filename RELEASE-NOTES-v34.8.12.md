# یادداشت انتشار CRM v34.8.12

تاریخ: ۱۴۰۵/۰۶/۵ (۲۰۲۶-۰۸-۲۷)

## فاز C1 — شروع «نازک‌سازی» (کلاینت نازک کامل)

### تله‌متری push سمت سرور (`api/crm.php`)
- هر data_push به تفکیک کلید شمرده می‌شود: تعداد، مجموع/میانگین بایت، تعارض، reject، آخرین کاربر/زمان (`crm/data/sync/push_stats.json`، زیر همان flock — بدون race جدید)
- endpoint جدید `sync_stats` (فقط-خواندنی، ادمین/رئیس): جدول مرتب بر اساس تعداد push

### سند رتبه‌بندی اولیه (`PHASE-C1-HOT-COLLECTIONS.md`)
تحلیل ایستا: invoices (۲۹ مسیر نوشتن) > rfqs (۲۸) > deals (۲۸) > products (۲۲) > offers (۲۱) — مالی (opex/sharetx/shareholders) از v34.8.6 فرمان‌محور و تمام است. ترتیب نهایی C3 بعد از ~۱ هفته دادهٔ تله‌متری قفل می‌شود.

### چرا اندازه‌گیری اول آمد
اولویت‌بندی مهاجرت به فرمان باید بر دادهٔ واقعی باشد نه حدس؛ تله‌متری سمت سرور همهٔ دستگاه‌ها/کاربران را بدون نیاز به آپدیت کلاینت پوشش می‌دهد.

### کنترل
tester516 (تله‌متری + endpoint + سند + شبیه‌سازی تجمیع) — گیت CI: ۱۳۲ PASS / 0 FAIL.
