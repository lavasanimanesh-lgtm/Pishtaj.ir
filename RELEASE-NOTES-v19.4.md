# 🚀 ریلیزنوت v19.4 — اسپرینت ۵ از R12 (پایان رودمپ): «مختومه‌سازی کنترل‌شده»

**تاریخ:** ۱۴۰۵/۰۴/۲۱ | **شاخه:** واحد | **مبنا:** BACKLOG-SALESFILE-SOURCE-OF-TRUTH-R12.md — اسپرینت ۵ (آخر)

---

## 📦 محتوای اسپرینت (طبق قاعده ۱-۲ استوری هم‌خانواده)

| آیتم | شرح | وضعیت |
|---|---|---|
| **US-437** | مختومه‌سازی نهایی فقط پس از تحویل موفق و تسویه کامل + کنترل همه اسناد قبل از بایگانی | ✅ کامل |

**🏁 با این ریلیز، رودمپ R12 (پرونده فروش = تنها منبع حقیقت پس از برد) به‌طور کامل بسته شد:** US-431ف۱ (v18.9) → US-432+434ف۱ (v19.1) → US-433+434ف۲ (v19.2) → US-435+436 (v19.3) → US-437 (v19.4).

---

## 🏁 US-437 — کنترل پیش از مختومه

### قبل و بعد
- **قبل:** «🏁 مختومه» با یک confirm ساده انجام می‌شد — با پرداخت نقدی می‌شد پرونده را بدون هیچ مدرک تحویل مختومه کرد.
- **بعد:** کلیک روی مختومه (مسیر با فاکتور) ⟵ **مودال «کنترل پیش از مختومه»** با چک‌لیست کامل:

### چک‌لیست کنترل (هسته قابل تست `sfCloseAudit` → blockers/warns/docs)

| نوع | مورد | رفتار |
|---|---|---|
| ⛔ **Blocker سخت** | مدرک تحویل به کارفرما ثبت نشده (نه رویداد «🤝 تحویل کارفرما» و نه وضعیت «تحویل شده» درخواست) | دکمه مختومه **قفل** — سد برنامه‌ای در commit هم هست، نه فقط UI (درس US-371) |
| ⛔ **Blocker نرم** | فاکتور با مانده وصول‌نشده | چک‌باکس «تسویه‌شده ثبت شود» (پیش‌فرض تیک — رفتار مصوب US-324 حفظ شد)؛ **بدون تیک، مختومه ممنوع** — «با مطالبات باز نمی‌توان مختومه کرد» |
| ⚠️ هشدار | بستانکاری تسویه‌نشده تامین‌کننده روی همین پرونده (payables R9) | نمایش — مانع نیست ولی در audit ثبت می‌شود |
| ⚠️ هشدار | رکورد QC «عدم انطباق» بدون ثبت زیان (کیس واقعی R9) | نمایش + با ثبت زیان خودکار رفع می‌شود |
| ⚠️ هشدار | نبود سند قطعی برد | قبل از کنترل، مهاجرت نرم US-432 خودکار اجرا می‌شود |

### کنترل اسناد (AC2)
مودال، شمارش ۹گانه اسناد پرونده را پیش از بایگانی نشان می‌دهد: 🏆 سند برد | 📄 پیشنهاد | 🧾 فاکتور | 🚚 ارسال | 🔬 QC | ➕ هزینه | 📎 متفرقه | ✉️ نامه | 🤖 استعلام — به‌همراه مرحله فعلی (n/12).

### انتقال کامل (AC3)
کل پرونده با `awardDocs / qcEvents / shipEvents / costEvents / lossEvents / docs` به بایگانی منتقل می‌شود (زیرساخت v19.1/v19.2).

### مسیر باخت (AC4)
مختومه بدون فاکتور با دلیل استاندارد Win/Loss (US-349) **دست‌نخورده محفوظ** ماند.

### نکته امنیت فرایندی (فراتر از AC — یافته حین اجرا)
شرط تحویل عمدا **مستقل از مرحله** پیاده شد: فاکتور تسویه‌شده مرحله را به ۱۱ می‌برد، اما بدون «مدرک تحویل» همچنان blocker فعال است ⟵ نمی‌توان با پرداخت نقدی، الزام تحویل واقعی کالا را دور زد.

---

## 🛡 اثبات شروط دائمی کارفرما

| شرط | اثبات |
|---|---|
| بدون وصله‌پینه | همان `sfClose` موجود ارتقا یافت؛ منطق تسویه US-324 عینا حفظ و فقط UI از confirm به چک‌باکس مودال ارتقا یافت؛ هسته ممیزی جدا از UI |
| وابستگی‌ها | اتصال به: مراحل مشتق (v19.2)، سند برد (v19.1)، QC/زیان (v19.1+US-421)، payables (R9)، بایگانی و Win/Loss (US-349) |
| بدون باگ جدید | **۹۷ تستر / ۲۷۸۸ چک — صفر خطا** + سینتکس ۵۹ فایل JS + ۵ بلوک inline + audit PASS. tester53 (چک متن confirm قدیمی US-324) با حفظ معنا به الگوی جدید به‌روز شد |
| بدون تداخل با طرح توسعه | آخرین اسپرینت R12 طبق برنامه؛ هیچ آیتم دیگری از صف جلو نیفتاد |

## 🧪 تست
- **tester110** (جدید): ۳۶ چک — ۴ سناریوی رفتاری کامل: ① فاکتور تسویه ولی بدون تحویل ⟵ blocker + رد commit ② تحویل + تسویه ⟵ مختومه موفق با شمارش اسناد ③ مانده ۳۰۰هزار + تیک تسویه ⟵ payment خودکار autoSettle سپس بایگانی ④ هشدارهای بدهی تامین‌کننده و QC (و رفع با ثبت زیان).
- کل مجموعه: **۹۷ تستر / ۲۷۸۸ چک سبز**.

## 📂 فایل‌های تغییر یافته
`crm/salesfiles.js` (sfCloseAudit/sfCloseSettledCommit/sfCloseModal/sfCloseGo — جایگزین مسیر confirm) | `crm/index.html` + `crm/sw.js` (v19.4) | `_tools/uat/tester110-v194.js` (جدید) | `_tools/uat/tester53-v133.js` (به‌روزرسانی چک UI با حفظ معنا)

## 📤 آپلود
zip روی هاست روی نسخه موجود اکسترکت شود — **پوشه api حذف نشود**. بعد از آپدیت همه کاربران رفرش کنند (سایدبار: CRM v19.4).

---

## 🏁 جمع‌بندی رودمپ R12 (کامل شد)

| اسپرینت | نسخه | محتوا |
|---|---|---|
| ۱ | v18.9 | BUG-028/029 + US-431ف۱ (قفل read-only پیشنهاد برنده) |
| ۲ | v19.1 | US-432 (اسناد قطعی برد) + US-434ف۱ (QC/بازرسی) |
| ۳ | v19.2 | US-433 (مراحل ۱۲گانه مشتق) + US-434ف۲ (پکینگ/بارنامه/تحویل) |
| ۴ | v19.3 | US-435 (قفل ارجاع فاکتور) + US-436 (فاکتور رسمی حسابدار) |
| ۵ | v19.4 | US-437 (مختومه‌سازی کنترل‌شده) ✅ |

## ⏭ صف بعدی (منتظر ابلاغ)
① **US-426ف۲..429 + BUG-026 (Post-R9):** گزارش رسمی سال مالی، سند اصلاحی پس از قفل، UI زنده پیش‌پرداخت، هاب مالی مدیریتی ② US-423 (کارت ویزیت دستیار) ③ US-410/411 ④ US-405..408 (اسناد بازرگانی PL/IN — حالا که R12 بسته شد، زیرساخت پکینگ آماده است) ⑤ US-402/403

**شماره‌گذاری:** بعدی US-438 | باگ بعدی BUG-030
