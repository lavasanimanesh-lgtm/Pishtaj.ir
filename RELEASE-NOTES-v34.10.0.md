# v34.10.0 — فاز S1 سئو: بستن حلقه‌های «AI → انتشار → ایندکس» (۲۰۲۶-۰۸-۳۱)

> ادامهٔ مستقیم معماری سئو/CMS شاخهٔ 01a05480 (ادغام‌شده در 15d12a4) طبق
> `SEO-CMS-AI-ROADMAP-2026-08-31.md`. گیت: **171 PASS / 0 FAIL** (tester555: ۴۱ سنجه).

## ۱) صف متای هوش مصنوعی — بازبینی گروهی به‌جای ۶۵۰ تک‌کلیک
- **سرور** (`api/cms.php`): اکشن‌های `seo_queue_add/list/propose/apply/clear`
  - سقف ۶۰ صفحه در هر batch، ۵۰۰ رکورد کل صف، پذیرش آرایه به‌صورت JSON-string (FormData)
  - `seo_queue_apply_one`: همان موتور `page_meta_save` — بک‌آپ قبل از نوشتن، محافظ جهش حجم، ابطال کش اسکن، لاگ
  - اعمال فقط برای وضعیت proposed؛ خطای AI = وضعیت error بدون پیشنهادِ بی‌تغییر
- **کلاینت** (`crm/cms.js`): جعبهٔ «🤖 صف متای AI» در تب سئو
  - افزودن نتایج فیلتر فعلی → اجرای گروهی با متن واقعی هر صفحه (`seo_meta`) با فاصلهٔ ۱.۲ث و دکمهٔ توقف
  - **دیف قدیم→جدید (خط‌خورده) + تیک تأیید انسانی** → اعمال تیک‌خورده‌ها + گزینهٔ «ثبت نقشه پس از اعمال»

## ۲) حلقهٔ ایندکس — حافظه و مقایسه
- هر «بررسی ایندکس» در تاریخچهٔ سروری ثبت می‌شود (`crm/data/gsc-inspect-history.json`؛ ۵ ورودی هر URL، ۴۰۰ URL)
- پنجرهٔ inspect حالا «وضعیت قبلی + تاریخ + تغییر کرده/بدون تغییر» را نشان می‌دهد
- اکشن `inspect_log` برای پرس‌وجوی تاریخچه

## ۳) نقشهٔ سایت — خودکار + شفاف
- سوییچ «🔄 ثبت خودکار نقشه» در تول‌بار سئو (پیش‌فرض روشن؛ ذخیره در `ptfDevKv` — احترام به اصل A10 معماری)
- `gscSubmitSitemapQuiet`: ثبت بی‌سروصدا در GSC بعد از هر انتشار مقاله/بلاگ و بعد از اعمال صف (confirm حذف شد)
- **🧭 گزارش انحراف نقشه**: URLهای روح (در نقشه ولی فایل ندارند) و فایل‌های خارج از نقشه

## ۴) پیش‌نویس ماندگار
- فرم‌های مرکز دانش و بلاگ: ذخیرهٔ خودکار (۷۰۰ms) در لایهٔ دادهٔ دستگاه — بستن تب/مودال متن AI را نمی‌پراند
- نوار «🔁 پیش‌نویس بازیابی شد» با دکمهٔ پاک‌کردن؛ پاک‌شدن خودکار پس از انتشار موفق

## ۵) صفحات یتیم + لینک‌سازی AI
- اسکن سئو حالا **لینک ورودی** هر صفحه را می‌شمارد (نرمال‌سازی مسیر/ایندکس، مستثنا: خودلینک breadcrumb، دامنهٔ خارجی، 404)
- chip فیلتر «یتیم (بدون لینک ورودی)» + دکمهٔ «💡 لینک‌سازی» روی ردیف
- اکشن جدید `seo_intlinks` در `api/llm.php` (زیر همان گیت نقش سئو): ۳ صفحهٔ مبدأ پیشنهادی + انکر فارسی طبیعی + محل درج — از کاندیداهای هم‌پوشه/پرارجاع

## یادداشت‌های فنی
- احترام کامل به قواعد معماری: صفر `localStorage` مستقیم (A10)؛ همهٔ تنظیمات device-local از `ptfDevKv`
- PHP هر سه فایل تغییرکرده با php-parser لینت شد (php-cli در محیط نیست)
- نسخه: VERSION.json + ۶ نقطهٔ رسمی + ۱۰۹ cache-buster + pinهای تسترها (۶۲۸ مورد شامل فرم‌های escape‌شده)

## بعدی (طبق رودمپ)
- **S2**: مولد صفحهٔ محصول از دیتای CRM + قالب‌های نوع صفحه + ادیتور canonical/ریدایرکت
- **S3**: اسنپ‌شات روزانهٔ GSC + خوشه‌بندی کلمه→محتوا + داشبورد KPI
