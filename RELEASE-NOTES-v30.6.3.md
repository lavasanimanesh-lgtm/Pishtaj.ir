# ریلیزنوت v30.6.3 — هات‌فیکس گردش حساب با نام قلم + هایلایت سریع‌ترین تحویل

## باگ گزارش شده
- مورد یک (گردش حساب) مثل قبل هست - نام قلم نمی‌آید
- مورد دو جایی ندارد که درخواست تامین سریعترین را مشخص کند

## ریشه
- گردش حساب legacy: e.note (نام قلم) خالی بود چون PAY قدیمی item نداشت - fallback به buycmp.items[idx] لازم بود
- تحویل: فیلد ذخیره می‌شد ولی هایلایت فقط per item بود و بعد از تایپ رندر نمی‌شد + خلاصه کلی سریع‌ترین تامین‌کننده نداشت

## فیکس
| فایل | تغییر |
|---|---|
| crm/supplier-finance.js | slLedgerTable: برای legacy همیشه نام قلم از p.item یا buycmp.items[idx].nm بخوان و نمایش بده: 📦 نام قلم + درخواست. برای پرداخت‌ها هم itemName از allocations |
| crm/rfqsmart.js | جدول مقایسه: minD محاسبه + بج ⚡ سریع‌ترین برای هر قلم + پس‌زمینه آبی. برای کل RFQ، میانگین تحویل هر تامین‌کننده حساب و در هدر جدول نمایش |
| crm/buycompare.js | خرید گروهی کشویی تامین‌کننده سالم (فیکس SyntaxError قبلی) |
| crm/index.html | VER v30.6.3 |

## تست
1. گردش حساب امیر طاهری → PAY-1121 → باید ببینی 📦 شیر توپی ... + RFQ-99038 (قبل فقط RFQ)
2. درخواست تامین → جدول مقایسه → برای هر قلم قیمت + تحویل (روز) بزن 7 و 15 → 7 باید آبی + بج سریع‌ترین
3. بالای جدول مقایسه باید خلاصه: "سریع‌ترین کلی: فولاد مبارکه - میانگین 7 روز"

## نسخه
- VER v30.6.3
