# RELEASE NOTES — v34.29.4 (پارسر مقاوم خروجی هوش مصنوعی خارجی — ریشهٔ «دکمهٔ اعمال کار نمی‌کند»)

**تاریخ:** ۲۰۲۶-۰۹-۰۲ · **کمیت:** tester579 (۵ سنجه) · **گیت:** 195 PASS / 0 FAIL

## گزارش کاربر

«در تب صفحهٔ جدید پرامپت را به هوش خارجی دادم، خروجی هوش مصنوعی را گذاشتم ولی دکمهٔ اعمال در فرم کار نمی‌کند.»

## ① ریشه‌یابی — بازتولید ماشینی با کلیک واقعی روی دکمهٔ «✅ اعمال در فرم»

جریان کامل در شبیه‌ساز DOM اجرا شد (بوت کامل اپ ← تب صفحهٔ جدید ← باز کردن مودال پرامپت خارجی ← چسباندن خروجی ← کلیک واقعی دکمهٔ اعمال):

- خروجی **تمیز** (دقیقاً `TITLE: …` / `BODY:`) ← اعمال می‌شد ✅
- خروجی **بولد** (`**TITLE:** …` — شکل استاندارد خروجی ChatGPT/Claude/Gemini حتی با وجود دستور «بدون متن اضافی») ← **هیچ فیلدی پر نمی‌شد** ⛔ — پارسر خطِ شروع‌شده با `*` را نمی‌شناخت و تنها یک alert می‌داد.

## ② رفع — EXT-PARSE-HARDEN

پارسر (`cmsExtParse`) پیش از تطبیق نشانگرها، خط را نرمال می‌کند؛ محتوای BODY همیشه دست‌نخورده بافر می‌شود:

- **بولد نشانگر/مقدار**: `**TITLE:**` و `**TITLE:** **مقدار**`
- **جعبهٔ کد**: ```…``` دور کل خروجی (به داخل BODY هم نشت نمی‌کند)
- **بولت/سرفصل/نقل‌قول**: `- TITLE:`، `### TITLE:`، `> TITLE:`
- **دونقطهٔ کامل CJK**: `TITLE：`

و وقتی خروجی کلاً بی‌نشانگر است، به‌جای alertِ تنها، **پیام قرمز راهنما در خود مودال** بالای دکمه ظاهر و کادر چسبان فوکوس/انتخاب می‌شود (`cmsExtErr`). هر دو مسیر «صفحهٔ جدید» و «صفحهٔ محصول» از همین پارسر استفاده می‌کنند — یکجا درست شد.

**اثبات پس از اصلاح (کلیک واقعی):** بولد ✅ · جعبهٔ کد ✅ · دونقطهٔ کامل ✅ · سرفصل/بولت ✅ · بی‌نشانگر ← پیام واضح درون‌مودال ✅ · بولت‌های واقعی داخل BODY دست‌نخورده ✅

## ③ یادآوری زنجیرهٔ کامل «تب صفحهٔ جدید»

این سومین لایهٔ همان پرونده بود: ① v34.28.0 کلیک تب اصلاً رندر را اجرا نمی‌کرد؛ ② v34.29.3 کلاس‌های `pn/tb` با CSS سراسری CRM برخورد می‌کردند و محتوای رندرشده را با قواعدی مانند display:none نامرئی می‌کردند؛ ③ v34.29.4 خروجی هوش خارجی که درست چسبانده می‌شد اما اعمال نمی‌شد. حالا کل مسیر «موضوع ← پرامپت ← چسباندن ← اعمال ← انتشار» سبز است.

## ④ بعد از دیپلوی

فایل‌های v34.29.4 را آپلود کنید (`crm/cms.js`، `crm/index.html`، `crm/sw.js` و بقیهٔ نقاط نسخه). تب قدیمی را ببندید و باز کنید (یا بنر «بارگذاری نسخهٔ جدید» را بزنید). همان خروجی قبلی‌تان را دوباره در کادر بچسبانید و «اعمال» بزنید — این بار فیلدها فوراً پر می‌شوند؛ اگر چیزی نرمال تشخیص داده نشد، پیام قرمز راهنما خودش می‌گوید چه کنید.
