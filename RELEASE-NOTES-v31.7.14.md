# Release Notes — v31.7.14

**تاریخ:** ۱۴۰۵/۰۴/۲۸ (2026-07-19)
**نسخه:** v31.7.14
**نوع:** 🔴 رفع باگ بحرانی داده — ناپدید شدن کاربر تعریف‌شده از فهرست کاربران (BUG-USERS-VANISH-001)

---

## 🎯 مسئله (به گزارش کارفرما)

کاربر «حسابدار» قبلاً تعریف شده بود اما اکنون در فهرست کاربران نیست.

## 🔍 علت ریشه‌ای (RCA) — عارضه جانبی کشف‌نشده incident v31.7.4

زنجیره سه‌حلقه‌ای که با بازتولید E2E روی سرور PHP واقعی **اثبات شد**:

1. **v31.7.4 (BUG-AUDIT-004):** `users_get` به‌درستی از ارسال `passhash` منع شد — تصمیم امنیتی درست.
2. **مرورگر:** در لاگین و `usersPullFromServer`، لیست سروری (اکنون بی‌هش) با `ptf_crm_users` محلی merge می‌شود و **نسخه بی‌هش سروری جایگزین نسخه محلیِ دارای هش** می‌شد.
3. **auto-seed (US-151):** با ورود بعدی ادمین، همین لیست بی‌هش به `users_sync` سرور فرستاده می‌شود — و حلقه پاکسازی سرور `if (empty($u['passhash'])) continue;` **کاربر بدون هش را بی‌صدا حذف می‌کرد**.

نتیجه: هر کاربری که رکورد محلی هش‌دارش در دسترس نبود (مرورگر دیگر، پاکسازی کش، دستگاه دوم)، در اولین seed پس از v31.7.4 از سرور حذف و سپس با pull از همه مرورگرها ناپدید می‌شد. **این چهارمین مصداق الگوی «تغییر یک سمت قرارداد بدون سمت دیگر» است** (پس از BUG-AUTH-001، BUG-NTF-002، BUG-AVATAR-001).

**بازتولید ثبت‌شده:** سرور با ۳ کاربر (حسابدار هش‌دار) → یک users_sync با لیست بی‌هش → خروجی سرور فقط ۲ کاربر؛ حسابدار حذف شد ❌ → پس از fix: هر ۳ کاربر با هش سالم و login حسابدار موفق ✅

## ✅ اصلاحات (سه‌لایه — دفاع در عمق)

| # | فایل | تغییر | شدت |
|---|------|--------|-----|
| 1 | `api/crm.php` (`users_sync`) | کاربر بی‌هش دیگر بی‌صدا حذف نمی‌شود: هش موجود همان username از `crm_users.json` یا `sync/ptf_crm_users.json` **بازیابی و حفظ** می‌شود؛ فقط کاربر واقعاً جدیدِ بی‌هش کنار گذاشته و در فیلد `dropped` پاسخ **گزارش** می‌شود | 🔴 P0 |
| 2 | `crm/index.html` (merge لاگین) + `crm/rbac.js` (`usersPullFromServer`) | هش محلی روی رکورد سروری بی‌هش برگردانده می‌شود تا seed بعدی لیست کامل هش‌دار بفرستد (هش سروری موجود هرگز بازنویسی نمی‌شود) | 🔴 P0 |
| 3 | `crm/rbac.js` (`usersSyncToServer`) | اگر سرور کاربری را کنار گذاشت، toast هشدار با نام کاربران — هیچ حذفی دیگر بی‌صدا نیست | 🟡 P1 |
| 4 | ۴ نقطه نسخه | v31.7.14 | — |

## 🛟 بازیابی کاربر حسابدار (اقدام کارفرما)

به احتمال زیاد رکورد حسابدار هنوز قابل بازیابی است — به ترتیب:
1. **مرورگری که حسابدار آخرین‌بار از آن وارد شده:** رکورد او با passhash احتمالاً هنوز در `ptf_crm_users` همان مرورگر است؛ پس از استقرار v31.7.14، ادمین یک‌بار از همان مرورگر وارد شود → auto-seed این‌بار (با fix) رکورد کامل را به سرور برمی‌گرداند.
2. **بک‌آپ سرور:** `crm/data/backups/hourly-latest.json.gz` یا `daily-*.json.gz` قبل از تاریخ حذف — کلید `ptf_crm_users` داخل آن رکورد حسابدار را دارد (بازگردانی از تنظیمات → پشتیبان).
3. **در نهایت:** تعریف مجدد کاربر حسابدار از پنل کاربران (نقش accountant تک‌دارنده است و آزاد شده).

پس از بازیابی، خروجی `data-health-check.php` بخش «کاربران sync‌شده» را چک کنید.

## 📊 نتایج گیت

- تستر جدید `tester189-users-vanish`: **۱۳/۱۳ PASS** — شامل E2E سرور PHP واقعی: بازتولید سناریو، بقای حسابدار، login موفق پس از sync، و گزارش dropped
- رگرسیون کامل: **167/167 فایل PASS — 3910/3910 چک PASS**
- `audit.py` (۱۰ بخش): PASS | `php -l api/crm.php`: بدون خطا

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| امنیت v31.7.4 | دست‌نخورده — `users_get` همچنان passhash نمی‌دهد؛ هش فقط سرورساید از رکورد موجود بازیابی می‌شود | صفر |
| کاربران موجود | هیچ حذف/تغییری؛ فقط مسیر حذف ناخواسته بسته شد | مثبت |
| ورود/JWT (v31.7.8) | دست‌نخورده — tester184 سبز | صفر |

---
*توسعه‌دهنده: PTF Development Team — 2026-07-19*
