# RELEASE NOTES — v34.19.0 (سطل بازیافت CRM — 🗑)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** tester564 (۲۵ سنجه) · **گیت:** 180 PASS / 0 FAIL · **PHP-لینت:** php-parser (sales-domain.php) ✓

## زمینه (RCA دو پیشنهاد گمشدهٔ مالک)

فرمان حذف (`entity_delete`) فقط **tombstone** در آرشیو می‌نویسد — بدون snapshot از محتوای رکورد. نتیجه: حذف (حتی اشتباهی/خودکار) برای همیشه قطعی بود و tombstone چسبنده تا ابد هر باز-ذخیره‌ای همان شناسه را هم می‌بلعید. این نسخه آن حفره را می‌بندد.

## ۱) snapshot در لحظهٔ حذف (کلاینت — choke-point یگانه)

- `ptfEntityDelete` پیش از صدور فرمان حذف، رکورد قربانی را (با تطبیق cd/_id/no/id) می‌یابد و یک سطر `kind=recycle` با **snapshot کامل** به `ptf_crm_deleted_archive` می‌افزاید (همگام‌شونده بین دستگاه‌ها؛ سقف ۵۰۰).
- fire-and-forget: شکستِ کتاب‌گذاری هرگز حذف را بلوکه نمی‌کند (بدون رگرسیون).
- kind «recycle» در نقشهٔ tombstone نیست → خودش هیچ رکوردی را نمی‌بلعد.

## ۲) `entity_restore` سروری — بازیافت اتمی زیر همان قفرمان/قفل

سه گام در یک فرمان: ① درج snapshot در مجموعه (با idField رجیستری + گیت رکوردِ زندهٔ هم‌شناسه → 409) ② **خنثی‌سازی tombstoneهای فعال همان id** — سطرهای `archive_purge` (از identities/aliases) و kindهای نگاشت‌شده (نقشهٔ کامل کپی‌شده از crm.php) به `restored:<kind>` تغییر می‌کنند تا `sync_apply_tombstones` دیگر رکورد بازیافتی را حذف نکند ③ نشانه‌گذاری سطر recycle با `restoredAt` (بازیافت دوباره ممکن نیست). گیت نقش از رجیستری همان مجموعه؛ ژورنال فرمان/idempotency/revoke طبق بقیهٔ فرمان‌ها خودکار.

## ۳) UI — مودال «🗑 سطل بازیافت»

- دکمه در تولبار پیشنهادات + انتخاب مجموعه (۱۰ بخش: پیشنهادات/استعلام‌ها/مشتریان/تامین‌کنندگان/کالاها/پرونده‌های فروش/پروژه‌ها/نامه‌ها/قراردادها/فاکتورها).
- هر سطر: عنوان رکورد + شناسه + زمان/علت/کاربر حذف + «↩️ بازیافت» → فرمان `entity_restore` → projection فوری + audit + پیام فارسی.
- صداقت: حذف‌های **قبل از این نسخه** snapshot ندارند — صریحاً اعلام می‌شود (مسیر آنها بکاپ است)؛ خطاهای «بدون نسخه» و «شناسه زنده» پیام فارسی دارند.

## بهداشت

- آرگومان‌های onclick با `&#39;` + `ptfOnClickArg`؛ بدون LS مستقیم (A10).
- نقشهٔ kinds با ارجاع به منشأ (crm.php) و دستور همگام‌سازی مستند شد.

## فایل‌های کلیدی

`api/sales-domain.php` (sd_tombstone_kinds + entity_restore) · `crm/sales-domain-v2.js` (هوک snapshot + مودال) · `crm/offers.js` (دکمهٔ 🗑) · `_tools/uat/tester564-v34.19.0-recycle-bin.js`
