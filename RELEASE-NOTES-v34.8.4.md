# یادداشت انتشار CRM v34.8.4

تاریخ: ۱۴۰۵/۰۶/۰۲ (2026-08-24)

## اصلاح گزارش دوره‌ای خزانه

- مانده و KPI داشبورد خزانه همچنان فقط از سال مالی فعال محاسبه می‌شوند.
- گزارش دوره‌ای و فهرست منابع اکنون دفتر کامل گردش‌ها را می‌خوانند؛ بنابراین انتخاب دوره‌ای خارج از سال مالی فعال دیگر ورودی‌ها را به‌اشتباه صفر نمی‌کند.
- رقم‌های فارسی و عربی در ورودی تاریخ نرمال می‌شوند.
- `dateISO` فقط در صورت Gregorian معتبر بودن مصرف می‌شود و اگر خراب باشد، `dateFa` معتبر به‌صورت مستقل به ISO تبدیل می‌شود.

## اصلاح حساب مشتری

- کلید حساب مشتری از شناسهٔ canonical یعنی `_id || cd` گرفته می‌شود.
- مبلغ ناخالص، مانده و وضعیت فاکتور از SSOT مطالبات (`PTF.ar.invoiceState`) می‌آید.
- مالکیت پرونده با resolver canonical مطالبات تعیین و در حالت تعارض یا ابهام fail-closed می‌شود.
- Receipt دارای پرونده فقط به مالک canonical همان پرونده متصل می‌شود.
- Receipt و چک بدون پرونده/مالک فقط با مالک صریح غیرمبهم یا ارجاع یکتای فاکتور پذیرفته می‌شوند؛ مالک صریح متعارض بر fallback فاکتور تقدم دارد و رکورد رد می‌شود.
- چک فاقد مالک دیگر به همهٔ حساب‌های مشتریان نشت نمی‌کند.
- running balance پیش از فیلتر نمایش محاسبه می‌شود و ماندهٔ تاریخ گزارش حتی در بازهٔ بدون ردیف حفظ می‌شود.

## ممیزی هزینهٔ حقوق و هزینه‌های تکرارشونده

در این نسخه معماری OPEX، recurring، projection یا حذف خودکار تغییر نکرده است. ممیزی علت‌های مستقل زیر را تأیید کرد:

1. هزینهٔ حقوق در `opex` و طلب سهامدار در `sharetx` دو سند مستقل‌اند؛ وجود اولی اثبات وجود دومی نیست.
2. projection مستقیم OPEX می‌تواند snapshot سرور را بدون merge با تغییر dirty محلی روی کل مجموعه بنویسد.
3. پاسخ reconcile حقوق، OPEX را projection می‌کند اما `sharetx` را برای نقش‌های فاقد دسترسی برنمی‌گرداند.
4. reconcile با snapshot ناقص سهامداران می‌تواند ردیف حقوق ماه جاری را void کند.

انتخاب بین «حفظ recurring همراه با merge/dirty barrier و قرارداد projection اصلاح‌شده» و «حذف recurring و ثبت مستقل هر هزینه» یک تغییر معماری است و مطابق درخواست کارفرما به تأیید جداگانه نیاز دارد.

## کنترل کیفیت

- fixture جدید VM برای دفتر all-period خزانه، تاریخ شمسی با رقم فارسی، مالکیت canonical مشتری، چک بی‌مالک/متعارض و running balance فیلترشده افزوده شد.
- گیت کامل CI: **120 PASS / 0 FAIL**.
- نگهبان معماری و syntax همهٔ 49 فایل CRM موفق بود.
- `php -l` به‌دلیل در دسترس نبودن executable مربوط در محیط CI محلی skip شد؛ هیچ فایل PHP اجرایی در این اصلاح تغییر منطقی نکرده و تغییر `api/sales-domain.php` فقط pin نسخه است.
