# یادداشت‌های انتشار — v34.8.50 (هات‌فیکس ۳ — PHANTOM-STORAGE)

**موضوع:** مرورگری که `sessionStorage.setItem` را بی‌خطا می‌پذیرد ولی مقدار را نگه نمی‌دارد (خواندن بلافاصله null).
**تاریخ:** ۲۰۲۶-۰۸-۲۹ · **تستر قفل‌کننده:** `tester550-v34.8.50-phantom-storage.js` (۱۱ سنجه · سبز)

## علامت و ریشه (مشاهدهٔ واقعی روی staging)
- پروب مرورگر مالک: `sessionStorage` نوشتن ✅ بدون خطا ولی خواندن-باز ❌ null؛ `localStorage` ✅ سالم؛ کوکی ✅ معتبر.
- کد قبلی تصمیم مخزن را فقط از «نبود exception» می‌گرفت → می‌پنداشت SS کار می‌کند → نشست را در SSِ فانتوم می‌نوشت (محو می‌شد) و **همزمان کلیدهای LS را پاک می‌کرد** → ورودِ موفق بی‌نشست می‌ماند و پیام «بازسازی بی‌نتیجه» می‌آمد.
- باگ دوم (پنهان): `ptfAuthMigrate` حتی وقتی نوشتن SS می‌انداخت/نگه نمی‌داشت، کلید LS را حذف می‌کرد — توکن برای همیشه گم می‌شد.

## fix (crm/rbac.js)
1. **خواندن-باز:** بعد از هر `setItem` توکن/نشست (در SS و LS)، مقدار خوانده و مقایسه می‌شود؛ مغایرت = همان شاخهٔ خرابی → مخزن بعدی (LS → حافظهٔ تب).
2. **migrate ایمن:** حذف کلید LS فقط بعد از تأیید اینکه SS مقدار را واقعاً نگه داشته است.
3. زنجیرهٔ کامل (هات‌فیکس ۴۷/۴۸) دست‌نخورده: SS → LS → حافظهٔ تب + سقف ضدتوفان.

## گیت‌ها
CI ۱۶۶ تستر (tester550 جدید) — سبز؛ arch PASS؛ هم‌نسخگی v34.8.50.
