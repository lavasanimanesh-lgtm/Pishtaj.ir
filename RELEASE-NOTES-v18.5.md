# 🐞 ریلیز v18.5 — BUG-027 «ثبت تاریخ تحویل تعهدی حذف می‌شد»

**گزارش کارفرما:** در پرونده فروش، هنگام ثبت «تاریخ تحویل تعهدی»، سیستم به‌جای ثبت تاریخ، پیام حذف تاریخ/تعهد را نشان می‌داد و تاریخ ذخیره نمی‌شد.

## 🔬 ریشه‌یابی دقیق

در `crm/salesfiles.js` مسیر `sfSetDue` برای دکمه ذخیره این الگو را داشت:

```js
this.closest('.md-b').remove();
sfSetDueCommit(cd, (document.getElementById('sfDueInp')||{}).value, ...)
```

در JavaScript این دو دستور به‌ترتیب اجرا می‌شوند. بنابراین:

1. ابتدا مودال از DOM حذف می‌شد.
2. سپس `document.getElementById('sfDueInp')` اجرا می‌شد.
3. چون input دیگر وجود نداشت، مقدار `undefined` می‌شد.
4. `sfSetDueCommit` با مقدار خالی اجرا می‌شد.
5. سیستم آن را «حذف تعهد» تفسیر می‌کرد.

پس ریشه باگ **ترتیب اشتباه خواندن مقدار و حذف مودال** بود، نه مشکل ذخیره‌سازی یا Sync.

## ✅ رفع ریشه‌ای

### ۱) جداسازی مسیر ذخیره و حذف
توابع جدید/اصلاح‌شده:

```js
sfDueSave(cd)
sfClearDue(cd)
sfSetDueCommit(cd, iso, note, explicitClear)
```

### ۲) ذخیره امن
`sfDueSave` ابتدا مقدار input را می‌خواند، اعتبارسنجی می‌کند، ذخیره را انجام می‌دهد و فقط بعد از موفقیت، مودال را می‌بندد.

### ۳) حذف فقط با مسیر صریح
حذف تاریخ دیگر با تاریخ خالی در مسیر ذخیره انجام نمی‌شود. برای حذف باید دکمه «حذف تعهد» زده شود.

### ۴) سازگاری عقب‌رو
مسیر برنامه‌ای قدیمی:

```js
sfSetDueCommit(cd, '', '')
```

برای حذف برنامه‌ای حفظ شد تا تست‌ها و مسیرهای احتمالی قبلی نشکند؛ اما مسیر UI ذخیره خالی با آرگومان `false` بلاک می‌شود.

## فایل‌های تغییر یافته

- `crm/salesfiles.js`
- `crm/index.html`
- `crm/sw.js`
- `_tools/uat/tester102-v185.js`
- اصلاح تستر قبلی:
  - `_tools/uat/tester101-v184.js`
- `RELEASE-NOTES-v18.5.md`
- `PTF-MASTER-HANDOVER.md`

## 🔬 کیفیت

- تستر جدید: `tester102-v185.js` با ۱۸ چک.
- رگرسیون کامل: **۸۹ تستر / صفر خطا**.
- Audit سایت: PASS با همان هشدار قدیمی تصاویر سنگین.

## نتیجه

ثبت تاریخ تحویل تعهدی اکنون درست ذخیره می‌شود. پیام حذف فقط زمانی نمایش داده می‌شود که کاربر واقعاً مسیر حذف تعهد را انتخاب کند.
