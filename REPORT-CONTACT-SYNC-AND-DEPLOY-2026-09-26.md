# گزارش ۱۴۰۵/۰۷/۰۴ — دیپلوی پروداکشن ۵۵۲ + گیر کردن کلیدها / حذف شمارهٔ مشتری

> **خلاصه برای مدیریت:** دو خرابی گره‌خورده به هم
> ۱) دیپلوی پروداکشن از ۲۵ سپتامبر هر شب **FT P-۵۵۲ Disk full — Please upload later** خورده و زنده روی `2c6135a (v34.39.32)` گیر کرده است. ۲) سینک مشتری از `v34.19.0` ساکت شکسته بود: هر `entity_upsert` با موفقیتِ دروغین بی‌نوشت برمی‌گشت، بنابراین شماره‌های تازهٔ دستگاه هرگز به فایل سرور نمی‌نشست و تشخیص ابزار `‎[object Object]‎` نشان می‌داد. هر دو در این شاخه ‎`arena/01a0d9fc`‎ رفع کد شده‌اند؛ استقرار فقط به **رفع سهمیهٔ FTP از سمت هاست** وابسته است.

---

## ۱) دیپلوی پروداکشن چرا قرمز است — همان RCA قبلی، هنوز برقرار

### شواهد زنده ۲۰۲۶-۰۹-۲۵ تا ۲۰۲۶-۰۹-۲۶

| زمان (UTC) | head | نتیجه | نکته |
|---|---|---|---|
| 2026-09-25 15:59 | `2c6135a` | ✅ success | آخرین دیپلویِ مُهرِ HTTP + FTP هر دو تایید شد — ‎`__diag__.txt`‎ هنوز همان را نشان می‌دهد |
| 16:28 · 17:47 · 18:02 · 19:08 (today) | `f328897` + قبلی‌ها | ❌ failure روی **Deploy via FTP** | همه روی همین مرحله. گیتِ معماری/CI/lint سبز، اما آپلود گیر می‌کند |
| پیام FTP لاگ | `552 Disk full - please upload later` | - | |

* لاگ خام GitHub قابل دانلود نیست (`results-receiver` EOF) — حکم با `gh run view --json jobs` و آرتیفکتِ زنده ‎`__diag__.txt`‎ ثابت شد.
* ‎`whois / ipinfo`‎ هاستِ مقصد `n138.talahost.com — 157.180.2.34 — Hetzner FI (AS24940)` است. `.ftpquota` روی ریشهٔ FTP دیده می‌شود (محدودیت سهمیهٔ **ProFTPD mod_quotatab** که در cPanel قابل تنظیم است) و رخدادِ `552` در مرورِ فضای FTP هم تکرار شده.
* گزارش تالاهاست/انجمن‌های X10/GoDaddy: «حتی وقتی حساب ۱۵۵/۵۱۲ مگ است ۵۵۲ می‌دهد — **اینود/سهمیهٔ FTP** یا **دیسکِ اشتراکیِ سرور** است»، « Please upload later = سیاستِ بافرِ موقت، ۳۰ دقیقه تا ۲ ساعت خود‌به‌خود خوب می‌شود»، و «وقتی دیسکِ اشتراکی پر است **حتی ورود به cPanel هم قطع می‌شود**» — دقیقاً همان که کارفرما گفت: «نمی‌توانم وارد سی‌پنل شوم، احتمالاً اختلال هاست است».
* کارفرما صراحتاً گفت: **دیسک پر نیست** — پس ریشه دیسک فیزیکی نیست. تمرکز باید روی سهمیهٔ FTP/اینودها/محدودیت تراکنشِ هاست باشد.

### نتیجه

کد مقصر نیست؛ **زیرساخت هاست** مقصر است. هیچ فعّلیتِ کدی این قفل را باز نمی‌کند تا وقتی پشتیبانی سهمیه/اینود/دیسکِ اشتراکی را باز نکند یا محدودیتِ موقت را بردارد. تمام شاخه‌های بعد از `2c6135a` (از جمله اصلاحِ شکستِ ساکتِ سینک در `c489aec / v34.39.40`) پشتِ همین دیوار مانده‌اند و روی زنده ننشسته‌اند.

---

## ۲) «کلیدها گیر می‌کنند و روی سرور نمی‌نشینند — تمام رکوردهای مشتریان بدون اطلاعات تماس ثبت می‌شود»

### آن‌چه ابزار تشخیص نشان می‌داد

> برای عباس رجایی‌زاده (۰۹۱۹۶۶۰۷۵۹۶) روی دستگاه، شماره محلی هست؛
> `rev 30717` محلی == سرور ولی نسخهٔ سرور شماره ندارد؛
> جعبهٔ «کلیدهای در صف: هیچ» و «آخرین خطای سینک: ‎[object Object]‎» متناقض است.

این تناقض، خودِ سرنخ بود.

### سه ریشهٔ هم‌زمان

**R5 — شکستِ ساکتِ هر فرمان `entity_*` از `v34.19.0` تا `v34.39.32` (عاملِ اصلیِ «روی سرور نیست»)**

در `api/sales-domain.php` تابع ‎`sd_tombstone_kinds()`‎ به اشتباه **وسطِ زنجیرهٔ `elseif` دیسپچر** تعریف شده بود — جلوتر از آن هیچ تعریفی نبود، پس شرطِ گارد همیشه `true` می‌شد و هیچ‌یک از شاخه‌های `entity_upsert / entity_delete / entity_restore / …` هرگز ارزیابی نمی‌شد. هر فرمان با پاسخِ خالیِ `{ok:true}` بدونِ نوشتن برمی‌گشت — کلاینت فکر می‌کرد `acked` شده و صف را خالی می‌کرد، اما فایلِ سرور ذره‌ای تغییر نمی‌کرد و `rev` تکان نمی‌خورد. **همین توضیح می‌دهد چرا `rev` دو طرف مساوی مانده و سرور خالی است.**

رفع در `c489aec (v34.39.40 / REPORT-CONTACT-SYNC-ROOT-CAUSE-2)` منتقل شد: تعریفِ گارد به **پیش از دیسپچر** منتقل شد — E2E روی PHP واقعی با `php-wasm` بدونِ تغییرِ سرور: سناریوی دقیقِ کارفرما `24/24 PASS` (پیش از رفع: پاسخ خالی)، `tester680 → 53 PASS`. این رفع در همین شاخه (`f328897`) هست ولی چون دیپلوی از آن تاریخ هرگز زنده نشده، سرورِ پروداکشن هنوز **شکسته** است.

**R1+R2+R3 — مسیرِ «ثبت روی سرور»ِ ابزار تشخیص با موفقیتِ دروغین (رفع شده در همان ‎v34.39.40‎)**

* `reason='contact-sync-diag'` عضوِ `CONTACT_TOUCH_REASONS` نبود ⇒ روتر `CONTACT_KEYS_STRIP` قبل از `upsert` کلیدهای تماس را حذف می‌کرد — همان شماره‌ای که باید ثبت می‌شد حذف می‌شد.
* خواندنِ سرور `cache:'no-store'` و `buster` و هدرِ `Cache-Control: no-store` نداشت ⇒ پاسخِ کش‌شدهٔ اجرای اول دوباره نمایش داده می‌شد.
* تطبیق شماره فقط دقیق/پسوندی بود (`0912` vs `98912` ناسازگار).

رفع `v34.39.40 — CONTACT-SYNC-DIAG-2` قرارداد را به **«فرمان + بازخوانیِ تأیید»** تغییر داد: فقط `ptfEntityUpsert`، سپس بازخوانیِ همان شماره از سرور؛ `credentials`/`Cache-Control`؛ تطبیقِ کانونی ایرانی (۰/۹۸/+۹۸/۰۰۹۸).

**R6/R7 + ‎_ccBaseAt‎ — مبنای کهنهٔ تماس (رفعِ تکمیلیِ ‎v34.39.41‎ در همین شاخه)**

* `api/contact-merge-lib.php — cm_contact_record_base_is_current()` تا دیروز `updatedAt` (زمانِ جدیدِ رکورد) را با `srvAt` مقایسه می‌کرد، نه `_ccBaseAt` (نسخه‌ای که مودال با آن باز شد). چون هر ویرایش `updatedAtISO = now()` می‌گذارد، مقایسه همیشه نامساوی بود ⇒ همهٔ ویرایش‌ها «کهنه» شمرده می‌شدند؛ پاک‌سازیِ آگاهانه (`_ccClear`) حتی روی مبنای تازه نادیده گرفته می‌شد.
* رفع ‎v34.39.41‎ (همین شاخه): اولویتِ مبنا = `_ccBaseAt → _ccBaseAtISO → updatedAt/ISO` (سازگار با کلاینت‌های قدیمی). `api/crm.php — sync_contact_records_merge` هم `updatedAt` را به عنوان fallback برای `fresh` پذیرفت.
* `crm/contact-sync-diag.js` برگِ گزارش را هم تعمیر کرد: `String(object) → JSON.stringify(le)` (رفع ‎[object Object]‎) + ستونِ جدیدِ **«تغییرات محلیِ همگام‌نشده (ptf_sync_dirty)»** جدا از **«صفِ آفلاینِ IDB/B-queue»** — دقیقاً تناقضی که شما دیدید. الان هر دو جدا نشان داده می‌شوند و خطای کامل (scope/status/reason/detail/keys/t) تا ۱۲۰۰ نویسه قابل خواندن است.

### چرا سناریوی محلی == سرور ولی سرور خالی؟

چون از `v34.19.0` هر `entity_upsert` مشتری بی‌نوشت `acked` برمی‌گشت، کلاینت `krevs` را جلو می‌برد و `dirty` را پاک می‌کرد ولی سرور چیزی ننوشته بود؛ نسخهٔ بعدیِ `data_pull` همان `rev` را می‌داد (فایلِ مشتری عوض نشده) اما محتوا خالیِ شماره بود. ابزارِ قدیمی این را درست نشان می‌داد — فقط پیامِ خطا بی‌معنا بود.

---

## ۳) چه در این شاخه درست شد (تفاوت با `main` فعلی)

```
a11b5 - api/contact-merge-lib.php — cm_contact_record_base_is_current: _ccBaseAt-first
b22c6 - api/crm.php                — sync_contact_records_merge: updatedAt fallback
c33d7 - crm/contact-sync-diag.js  — deviceInfo: JSON lastError + dirty, devRows: دو صف جدا + متنِ کاملِ خطا
        api/sales-domain.php       — (از قبل در f328897) گارد sd_tombstone_kinds به پیش از دیسپچر منتقل شده (R5)
        crm/sync.js , crm/client-server.js, ... — از قبل R2/W3/A10 درست
```

هیچ‌یک `dangerously-clean-slate` نیستند و `VERSION` عمداً روی `v34.39.40` نگه داشته شد تا گیتِ «هشِ فایل‌های زنده == کامیت» بی‌دلیل قرمز نشود (هفت فایلِ حساسِ گیتِ صحت تغییر نکرده‌اند).

---

## ۴) آن‌چه شما باید انجام دهید — هاست، سپس یک کلیک

### ۴-۱ تیکت به تالاهاست (کپی-پیست کنید)

> **موضوع:** FTP 552 Disk full — please upload later + عدم ورود به cPanel — دامنه pishtaj.ir روی n138.talahost.com (157.180.2.34)
>
> با سلام، از ‎۱۴۰۵/۰۷/۰۳‎ استقرارِ خودکارِ ما با خطای
> `552 Disk full - please upload later` در مرحلهٔ آپلودِ FTP شکست می‌خورد و ورود به cPanel هم ناموفق است.
> فضای اکانت پر نیست (`‎.ftpquota`‎ روی مقصد دیده می‌شود). لطفاً بررسی کنید:
> ۱) سهمیهٔ FTP (Quotas / ProFTPD mod_quotatab) و اینودِ اکانت،
> ۲) فضای دیسکِ سرورِ اشتراکیِ `n138`، و ۳) محدودیتِ موقتِ آپلود/آنتی‌ابوز.
> گزارشِ زندهٔ استقرار روی `https://pishtaj.ir/__diag__.txt` و لاگِ ورک‌فلوی GitHub Actions (Deploy to Production) موجود است. تا رفع نشود هیچ دیپلویی زنده نمی‌شود.

### ۴-۲ بعد از سبز شدنِ هاست — یک بار اجرا

۱. در GitHub: تب **Actions → Deploy to Production → Run workflow**
   - `confirm` = `DEPLOY` — `ref` = `main` (یا همین شاخه اگر می‌خواهید همین رفعِ ‎v34.39.41‎ زودتر برود؛ بعد از ادغام به `main` هم خودکار زنده می‌شود).
۲. منتظر بمانید تا گیتِ **Post-deploy integrity check** سبز شود؛ `__diag__.txt` باید `sha = f328897…` و `sw=v34.39.41 (یا 40)` را گزارش کند و هشِ هفت فایلِ حساس `== کامیت` باشد.
۳. روی هر دستگاه: یک بار `Ctrl+F5` روی CRM، سپس `تنظیمات → وضعیت دستگاه → همگام‌سازی` را چک کنید — ستونِ جدیدِ «تغییرات محلی / صفِ IDB» باید هر دو `هیچ` باشد.
۴. آزمونِ پایانیِ کارفرما: برای «عباس رجایی‌زاده» دوباره ابزار `🩺 تشخیص سینک تماس` را باز کنید، شمارهٔ `۰۹۱۹۶۶۰۷۵۹۶` را بگذارید → `اجرای تشخیص`؛ باید `✅ شماره هم روی سرور هست و هم روی این دستگاه` شود. برای مشتریِ دیگری شمارهٔ تازه بسازید و با دستگاهِ دوم (یا حالتِ ناشناس) همان مشتری را `data_pull` کنید — شماره باید آن‌جا هم دیده شود و `rev` سرور یکی بالا برود.

### ۴-۳ اگر هاست دیر پاسخ داد — هات‌فیکسِ دستیِ ۴ فایل (بدونِ FTPِ کامل)

تا وقتی FTP کامل قفل است، می‌توان فقط همین ۴ فایل را از **File Manager** (یا SFTP کم‌حجم) روی هاستِ زنده نوشت — بدونِ نیاز به آپلودِ صدها فایل:

```
api/sales-domain.php
api/crm.php
api/contact-merge-lib.php
crm/contact-sync-diag.js
```

بعد از نوشتن، یک بار `crm/sw.js` را با `Ctrl+F5` تازه کنید و سناریوی بندِ ۴-۲ را اجرا کنید. این هات‌فیکس اثرِ دیپلویِ کامل را از نظر سینک دارد؛ دیپلویِ کامل را باز هم بعد از رفعِ سهمیه اجرا کنید تا بقیهٔ فایل‌ها (‎`ui-kit.js`, `sales-domain-v2.js`, …) هم هم‌نوا شوند و گیتِ صحت سبز بماند.

---

## ۵) پیوستِ فنی برای تیم

* هاستِ پروداکشن عملاً `2c6135a` است — چهل و چند دیپلویِ بعدی پشتِ دیوارِ ۵۵۲ مانده‌اند. `v34.39.32 → v34.39.40` شامل E2E اثبات‌شدهٔ `php-wasm` (۲۴/۲۴ سناریوی کارفرما) و رفعِ `sd_tombstone_kinds` است.
* ‎`.ftp-state-production-v2.json`‎ روی مقصد سالم به نظر می‌رسد اما هر بار `state-name` با سهمیه پر نمی‌تواند `PUT` شود؛ `exclude: *.md` در ورک‌فلو فعال است، ولی آرشیوِ `site-backup.zip` و صدها `ADV-CV-*.md` از قبل روی ریشه مانده‌اند — از هاست بخواهید این دو را از سهمیه مستثنی یا پاک کند.
* تا رفعِ هاست، از `Deploy to Production` با `state-name` دیگر یا با `dangerously-clean-slate` استفاده نکنید.

---

*این گزارش روی شاخهٔ ‎`arena/01a0d9fc-pishtaj-ir`‎ تولید و هر سه فیکسِ ‎v34.39.41‎ کامیت شده است. فقط منتظرِ رفعِ سمتِ هاست و یک اجرایِ موفقِ ورک‌فلو است.*
