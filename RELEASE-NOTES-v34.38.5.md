# یادداشت انتشار v34.38.5 — DATA-QUALITY SH-SALARY

**تاریخ:** 2026-09-08
**بازوی تغییر:** Arena Agent
**کانال:** CRM (مالی / داده)

## گزارش کارفرما
> در تب کیفیت داده، موردِ «حقوق سهامدار» را بدونِ نوعِ رسمی/غیررسمی نشان می‌دهد و می‌گوید آن را از تب سهامداران مشخص کن. اما وقتی ویرایش را در تب سهامداران می‌زنیم، امکان تعیین رسمی/غیررسمی وجود ندارد.

## ریشهٔ مشکل
- ردیف‌های هزینهٔ **حقوق موظف سهامدار** (`shareholderSalary`) سرور هیچ‌گاه `isOfficial` نمی‌نوشتند (دومین منبع OPEX ناقص در کنار `ptfOpexApplyTpl`)، پس در کیفیت داده برای همیشه «هزینهٔ جاری بدون تعیین نوع رسمی/غیررسمی» می‌ماند.
- از طرف دیگر، دیالوگ ویرایش/ثبت سهامدار (`ptfShareEdit`) اصلاً فیلدی برای تعیین نوع سند حقوق نداشت؛ یعنی راهنمای کیفیت داده کاربر را به جایی می‌فرستاد که وجود نداشت.

## تغییرات

### ۱. `crm/shareholders.js` — تعیین نوع سند از تب سهامداران
- دیالوگ `ptfShareEdit` فیلد select **«نوع سند حقوق»** گرفت با گزینه‌ها:
  - `''` = تعیین نشده
  - `'yes'` = رسمی / قابل قبول ممیز
  - `'no'` = غیررسمی
- در `onOk`:
  - `'yes'` → `rec.salaryOfficial = true`
  - `'no'` → `rec.salaryOfficial = false`
  - `''` (یا حذف) → `delete rec.salaryOfficial` (بازگشت به «تعیین‌نشده» واقعی، بدون پسماند)
- audit جملهٔ «نوع سند حقوق: رسمی/غیررسمی» ثبت می‌کند.
- پس از ذخیره، `reconcileSalaryOnServer` با `scopeShareholder` فراخوانی می‌شود تا ردیف هزینهٔ حقوق با نوع سند جدید هم‌گام شود.

### ۲. `api/sales-domain.php` — انتشار `isOfficial` به هزینهٔ حقوق
- `register_shareholder_salary`: `$salaryOfficialVal` از `$shareholder['salaryOfficial']` خوانده می‌شود؛ فقط وقتی صریح باشد (`!== null && !== ''`) ردیف OPEX جدید `isOfficial` می‌گیرد.
- `reconcile_shareholder_salaries`: همان الگو از `$sh['salaryOfficial']` در مسیرهای ساخت (`create`) و به‌روزرسانی (`update`) ردیف هزینهٔ حقوق؛ در مسیر update بعد از `sd_recurring_activate` اعمال می‌شود.
- «تعیین‌نشده» عمداً `isOfficial` را نمی‌نویسد: یعنی پروفایل سهامدار فقط وقتی صریح باشد (`yes`/`no`) ردیف هزینه را بازنویسی می‌کند و هرگز `isOfficial` دستیِ فرم هزینه را پاک نمی‌کند (قرارداد «عدم استنتاجِ حذف از غیبت» پروژه). ردیفِ تازه هم بدون `isOfficial` ساخته می‌شود تا کیفیت داده پرچم «تعیین‌نشده» بزند.

### ۳. `crm/data-quality.js` — راهنمای قابل‌اجرا
- ردیف OPEX حقوق سهامدار با فلگ تشخیصی جدا از سایر «هزینهٔ جاری بدون نوع» علامت‌خورده و راهنمای اصلاح آن **هر دو مسیر** را می‌گوید:
  - تب سهامداران (ویرایش سهامدار → نوع سند حقوق)
  - یا فرم هزینه.

## تست قفل‌شده
- `_tools/uat/tester612-v34.38.5-shareholder-salary-official.js` — **24 PASS / 0 FAIL**
  - ۱) فیلد/گزینه‌ها/نگاشت/audit در `ptfShareEdit`.
  - ۲) الگوی «صریح = نوشتن isOfficial، تعیین‌نشده = ننوشتن» در register و reconcile (create + update).
  - ۳) راهنمای کیفیت داده.
  - ۴) رفتار `onOk` (رسمی/غیررسمی/تعیین‌نشده/حذف کلید قبلی) با شبیه‌سازی vm.
  - ۵) نسخه (VERSION.json / UI / sw / SD_SERVICE_VERSION) و ثبت در گیت.
