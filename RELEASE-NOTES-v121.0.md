# 🚀 ریلیز v121.0 — ادغام دو شاخه توسعه + پاکسازی بدهی فنی (بازگشت ایجنت اصلی)

**تاریخ:** ۱۶ تیر ۱۴۰۵ | **مبنا:** v120.0 (کار ایجنت‌های v90..v120) + زیرساخت کیفیت v89.3

## ✅ انجام شد
1. **بازگردانی زیرساخت تست حذف‌شده:** `_tools/` (ممیز ۸بخشی + ۲۰ تستر UAT) در نسخه‌های v90+ حذف شده بود — برگشت و با معماری جدید (کدینگ واحد P-، گردش کار، TC، حذف آزاد) سازگار شد. نتیجه: **۲۰/۲۰ تستر PASS + ممیز PASS**.
2. **رفع باگ بحرانی فرم پیشنهاد:** `offerPickBuyer` در ریفکتور حذف شده بود ولی ۳ نقطه صدایش می‌زدند → انتخاب کارفرما کرش می‌کرد (ReferenceError). بازگردانده شد.
3. **پاکسازی ۱۲۶ فایل یتیم سایت:** ۹۸ استاب تکراری knowledge-center (#U*/c-*/k-*/z-* — ۷۰-۱۵۰ کلمه، عنوان تکراری، بدون لینک ورودی) + ۱۰ blog تخت + ۴ about تخت + ۱۴ فایل تخت دیگر با لینک شکسته → قرنطینه در `_drafts/orphans-v121/` (حذف نشد). **۵۵۱ لینک شکسته → صفر**.
4. حذف زوائد بسته: `inqreader.js.bak`، `crm/storage.php` تکراری، zip های تودرتو، و خروج `crm/data/` (داده واقعی + هش کاربران) از بسته ریلیز.
5. **ادغام اسناد:** الحاقیه کامل در `PTF-MASTER-HANDOVER.md` + قاعده شماره‌گذاری US از US-260 (رفع تداخل دو رودمپ).

## 📦 نصب
zip بدون پوشه ریشه (طبق قاعده هندآور) — روی هاست extract کنید. `crm/data/` در بسته نیست و داده سرور دست نمی‌خورد.

## 🔬 کیفیت: ۲۰/۲۰ تستر PASS | ممیز ۸بخشی: ۰ خطا، ۱ هشدار (۵ تصویر سنگین قدیمی — در سئو موج۲) | کش → `ptf-crm-v121.0`
