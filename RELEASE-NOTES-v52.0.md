# 📦 Release Notes — v52.0
**اسپرینت ۵۲: Quick-Wins + تصاویر اختصاصی + ممیز خودکار کد**
**تاریخ:** ۱۲ تیر ۱۴۰۵ (2026-07-03)

---

## ✅ کارهای انجام‌شده

### 1️⃣ US-101 — حذف اینستاگرام از کنار لوگو/بخش تماس
- بلوک `@pishrotajheezfartak` از بخش آدرس دفتر مرکزی حذف شد.
- طبق AC2، لینک اینستاگرام (آیکون + نام) به ردیف کپی‌رایت **فوتر** منتقل شد.

### 2️⃣ US-102 — انتقال دکمه EN به منوی کشویی موبایل
- دکمه «🇬🇧 EN» هدر با کلاس `lang-switch-desktop` در عرض ≤790px مخفی می‌شود.
- آیتم «🇬🇧 English» به انتهای منوی کشویی موبایل اضافه شد (استایل متمایز نارنجی).
- متقابلاً در هر ۴ صفحه انگلیسی، دکمه «🇮🇷 فارسی» همین رفتار را دارد.
- CSS جدید: `.lang-switch-mobile` / `.lang-switch-desktop` در `style.css`.

### 3️⃣ US-103 — لوگوی شرکت در نتایج گوگل (فاوآیکون)
- ست کامل فاوآیکون **مربعی** از لوگو ساخته شد: `favicon.ico` (۱۶/۳۲/۴۸ چندسایزه در ریشه) + `favicon-32/96/192/512.png` + `apple-touch-icon.png` در `assets/images/favicon/`.
- تگ‌های `<link rel="icon">` صحیح به **هر ۳۱۹ صفحه** تزریق شد (قبلاً فقط ۶۸ صفحه با فایل غیرمربعی ۲۰۰KB).
- `site.webmanifest` با آیکون‌های ۱۹۲/۵۱۲ به‌روزرسانی شد.
- فیلد `logo` در Schema سازمان به آیکون مربعی ۵۱۲px تغییر کرد (الزام گوگل).
- 📌 **اقدام کارفرما پس از دیپلوی:** در Google Search Console → صفحه اصلی → Request Indexing. نمایش لوگو معمولاً ۱ تا ۴ هفته طول می‌کشد.

### 4️⃣ گواهینامه‌ها — حذف نمایش تاریخ منقضی
- بخش پایینی سه گواهی GQS (سطر «valid from 2024-04-15 until 2025-04-15») **برش** خورد؛ نام استاندارد و مرجع صدور حفظ شد.
- هر زمان نسخه تمدیدشده رسید، جایگزین می‌شود.

### 5️⃣ تصاویر اختصاصی صفحات داخلی (رفع تکرار تصاویر صفحه اصلی)
- **۱۰ تصویر واقع‌گرای اختصاصی تولید شد** (`assets/images/real/pages/` + نسخه WebP):
  piping، electrical، instrumentation، pumps، compressors، boilers، oil-gas، petrochemical، power-plant، cement
- هر صفحه خدمات/صنعت اکنون hero و og:image **منحصر به خود** را دارد.
- water → تصویر مرتبط موجود (water-treatment)؛ steel → real-steel-plant (مرتبط، بدون تکرار)؛ industrial-equipment → real-piping-fabrication.
- قبلاً: تصویر hero صفحه اصلی در **۳۲ صفحه** تکرار شده بود؛ الان هر تصویر حداکثر در ۱ صفحه hero.
- ⏳ ۲ تصویر باقیمانده (water اختصاصی، industrial-equipment اختصاصی) در اسپرینت ۵۳ تولید می‌شود (سقف تولید تصویر این نوبت پر شد).

### 6️⃣ 🔍 دستیار ممیز خودکار کد — `_tools/audit.py`
اسکریپت ممیزی ۸بخشی که از این پس **بعد از هر اسپرینت** اجرا و گزارش آن در ریلیز‌نوت ثبت می‌شود:
1. لینک‌ها و ارجاعات شکسته (۳۱۹ صفحه)
2. سینتکس تمام بلوک‌های `<script>` با `node --check`
3. اعتبار XML (sitemap) + JSON (webmanifest + JSON-LD)
4. هم‌گامی sitemap با فایل‌های دیسک
5. سئو پایه: title/description/favicon/عنوان تکراری
6. امنیت: رمز و کلید متن ساده در سورس
7. محتوای ناقص (زیر ۸۰۰ کلمه) در بخش‌های منتشرشده
8. تصاویر سنگین (>350KB) و img بدون alt

**باگ‌هایی که ممیز در همین اجرا پیدا و رفع کرد:**
- ❌→✅ **JSON-LD خراب در index.html**: آبجکت `ContactPoint`/`Organization` بسته نشده بود و `availableLanguage` مقدار نداشت — اسکیمای صفحه اصلی برای گوگل کلاً نامعتبر بود! (باگ قدیمی از ریلیزهای قبل)
- ⚠️→✅ ۳ تصویر سنگین (تا 445KB) فشرده شدند (میانگین ۳۰٪ کاهش) + webp هرو بازتولید شد.

## 📊 نتیجه ممیزی نهایی
```
🔍 PTF Auditor — بررسی 319 صفحه
✅ همه بررسی‌ها PASS شدند. (0 خطا، 0 هشدار)
```

## 🚀 فایل‌های تغییریافته کلیدی برای دیپلوی
- همه صفحات HTML (تزریق فاوآیکون) — دیپلوی کامل توصیه می‌شود
- `favicon.ico` (جدید، ریشه)، `assets/images/favicon/*` (جدید)
- `assets/images/real/pages/*` (جدید)، `assets/css/style.css`، `site.webmanifest`
- `index.html`، `en/*.html`، صفحات services/* و industries/*
- `assets/images/certificates/*` (برش تاریخ)

## ⏭️ اسپرینت بعدی (۵۳)
یکپارچه‌سازی فوتر (US-104) و هدر/منو (US-105) در همه صفحات + ۲ تصویر باقیمانده.
