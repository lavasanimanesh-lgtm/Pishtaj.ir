# RELEASE NOTES — v34.7.69 (۱۴۰۵/۰۵/۳۰ — 2026-08-21)

## نمایش فایل پیوست تامین‌کننده در فهرست تاییدشده

### مشکل
فایل کاتالوگ ثبت‌نام تامین‌کننده هنگام «تایید» به‌درستی روی رکورد (`files.oth`) ذخیره
می‌شد، اما در **فهرست تامین‌کنندگان تاییدشده** و **کارت «👁 مشاهده»** هیچ‌جا نمایش داده
نمی‌شد — کاربر فقط در تب «درخواست‌های سایت» فایل را می‌دید.

### تغییر

1. **فهرست تاییدشده:** اگر تامین‌کننده فایل پیوست داشته باشد، دکمهٔ **«📎N»** در ستون
   عملیات ظاهر می‌شود (N = تعداد فایل‌ها) که مودال «فایل‌های تامین‌کننده» را باز می‌کند.
2. **کارت مشاهده (👁):** بخش «📎 فایل‌های پیوست» با دکمهٔ «مشاهده / دانلود» برای هر فایل.
3. **مودال فایل‌ها** (`ptfSupFilesOpen`): فهرست فایل‌ها با نام/حجم + دکمهٔ مشاهده/دانلود
   (از مسیر استاندارد `openStoredFile` → فضای ابری).

### فنی

- `crm/offers.js` — تابع عمومی `ptfEntityFiles(c)` (خواندن `files` به هر دو شکل
  `{cat:[...]}` و آرایه)، دکمه در `renderSuppliers2`، بخش فایل در `showEntityCard`، و
  مودال `ptfSupFilesOpen`.

### تست

- تستر جدید `tester471-v34.7.69-sup-file-visible.js` (در گیت CI).
- **گیت CI: ۸۷ PASS / ۰ FAIL** · نگهبان معماری: PASS.

### UAT پیشنهادی

1. یک تامین‌کنندهٔ سایت دارای فایل را تایید کنید.
2. در فهرست تاییدشده → دکمهٔ «📎1» را بزنید → فایل باز شود.
3. «👁 مشاهده» را بزنید → بخش «فایل‌های پیوست» را ببینید و فایل را باز کنید.
