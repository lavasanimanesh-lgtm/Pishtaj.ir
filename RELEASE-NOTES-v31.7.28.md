# Release Notes — v31.7.28

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.28
**نوع:** 🟡 رفع شلوغی «روز من» — تکرار انبوه اعلانات یک درخواست (BUG-MYDAY-DUP-001)

---

## 🎯 گزارش کارفرما (با اسکرین‌شات)

«بعضی اعلانات روز من بارها تکرار می‌شود و کلی شلوغ می‌کند» — در اسکرین‌شات: RFQ-1242 پنج بار، RFQ-1241 چهار بار، RFQ-1361 چهار بار؛ ۲۸ مورد نیازمند توجه که عمده‌اش تکراری بود.

## 🔍 علت ریشه‌ای (RCA)

دو عامل هم‌افزا:

1. **رکوردهای هم‌کد به‌جامانده از دوران باگ کد تکراری (BUG-CODE-DUP-002/003):** قبل از رفع v31.7.20/24، چند رکورد درخواست با یک کد ساخته شده بود — هنوز در داده هستند.
2. **خود «روز من» هیچ ضدتکراری نداشت:** هر رکورد منبع یک ردیف مستقل می‌ساخت؛ ۵ رکورد هم‌کد = ۵ اعلان یکسان.

## ✅ اصلاح

**ادغام هوشمند در `ptfMyDayItems`:** رویدادهای با متن/پنل یکسان به یک ردیف ادغام می‌شوند؛ اگر واقعاً چند رکورد پشت یک رویداد باشد، برچسب صادقانه **«(×N رکورد هم‌کد)»** نمایش داده می‌شود — یعنی فهرست تمیز می‌شود ولی مشکل کیفیت داده از چشم پنهان نمی‌ماند (راهنمای پاکسازی: تنظیمات → بررسی کدهای تکراری). قدیمی‌ترین تاریخ سررسید حفظ می‌شود؛ dedup قبل از مرتب‌سازی و سقف ۳۰تایی اعمال می‌شود.

نمونه از اسکرین‌شات شما: ۱۰ ردیف تکراری سه درخواست → **۳ ردیف** (`RFQ-1242 … (×5 رکورد هم‌کد)`).

## 📁 فایل‌های تغییریافته

`crm/myday.js`، `crm/index.html` (bump)، `crm/sw.js`، `crm/clear-cache.html`، `_tools/uat/tester203` (جدید)

## 📊 نتایج گیت

- تستر جدید `tester203`: **۱۰/۱۰ PASS** — بازتولید دقیق داده اسکرین‌شات (۵+۴+۱ رکورد → ۳ ردیف با شمارنده) + idempotency + بدون side-effect روی داده منبع
- رگرسیون کامل: **181/181 فایل PASS — 4106/4106 چک PASS** | `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| داده منبع (rfqs/reminders/…) | صفر تغییر — dedup فقط در لایه نمایش «روز من» | صفر |
| رویدادهای واقعی متمایز | دست‌نخورده (امضا شامل متن کامل + پنل است) | صفر |
| کشف مشکل داده | بهتر شد — برچسب ×N محل رکوردهای هم‌کد را نشان می‌دهد | مثبت |

## 📋 راستی‌آزمایی توسط کارفرما (۱ دقیقه)

داشبورد → «روز من» → هر درخواست فقط یک ردیف؛ ردیف‌های دارای «(×N رکورد هم‌کد)» یعنی همان درخواست‌ها هنوز رکورد تکراری در داده دارند — از تنظیمات → «بررسی کدهای تکراری» پاکسازی کنید تا برچسب هم محو شود.

---
*توسعه‌دهنده: PTF Development Team — 2026-07-20*
