# v34.39.32 — ادغام شاخهٔ جلوتر از main (GSC-AI-REPORT R2 + اسپیرینت‌های سئو تا v34.39.39) (۲۰۲۶-۰۹-۲۵)

> شاخهٔ `arena/01a0cb49-pishtaj-ir` یازده کامیت از نقطهٔ مشترک با `main` جلوتر بود (`v34.39.32` تا یادداشت‌های `v34.39.39`). این یادداشت حاصل ادغام آن شاخه در `arena/01a0d930-pishtaj-ir` است، با حفظ فیکس‌های CI و یکدست‌سازی نسخه که از طریق PR #52 روی `main` نشسته بودند.

## چه چیزی وارد شد

- **v34.39.32 — GSC-AI-REPORT R2:** قرارداد خروجی مقاوم به بریدن برای تحلیل AI، بخش تطبیق کوئری↔صفحه در دیجست (تشخیص کندیبالیزیشن)، تولید مجدد با دادهٔ تازه، و برچسب‌های صادقانهٔ دورهٔ قبل. فایل‌ها: `api/gsc.php`، `api/llm.php`، `crm/gsc.js`.
- **v34.39.33 — SEO-SPRINT1:** بهینه‌سازی CTR و کوئری‌های quick-win بر پایهٔ دادهٔ GSC (۱۳ صفحه + sitemap).
- **v34.39.34 — SEO-SPRINT2:** خوشهٔ NACE و رفع هم‌خواری اینکوترمز.
- **v34.39.35 — SEO-SPRINT3:** گپ‌های محتوایی — مقالهٔ تست هیدرواستاتیک PE، جدول استنلس، خوشهٔ واردات/ترخیص.
- **v34.39.36 — SEO-SPRINT4:** پاکسازی لینک‌های داخلی به پوسته‌های ۳۰۱ (۱۵۰ لینک در ۸۹ فایل).
- **v34.39.37 — SEO-COMPETITOR:** ضدحمله برای «تامین‌کننده پایپینگ» و «تامین‌کننده ابزار دقیق».
- **v34.39.38 — SEO-COMPETITOR:** ضدحمله برای خوشه‌های «فلنج»، «لوله» و «شیرآلات صنعتی»، به‌همراه چک‌لیست استقرار.
- **v34.39.39 — SEO-COMPETITOR:** ضدحمله برای ۸ خوشه (زانویی ۴۲ اینچ، ترانسمیتر، لول ترانسمیتر، لول گیج، پرشر گیج، انکودر، پرشر ترانسمیتر، رزمونت) و تعمیق تحلیل رقبا.
- فهرست Request Indexing گوگل سرچ کنسول برای نشست ۲۰۲۵-۰۹-۲۵.

نسخهٔ عمومی سایت (`site_public_version`) عمداً روی `v34.39.14` ماند؛ اسپیرینت‌های سئو در همان قرارداد نسخهٔ عمومی شاخهٔ مبدأ منتشر شده‌اند.

## چه چیزی از main حفظ شد

- رفع SyntaxError دکمهٔ «بستن دستی مغایرت» در `crm/data-quality.js` (PR #51 / v34.39.27).
- دو رگرسیون CI که با `ad7a21a` وارد شده بودند.
- یکدست‌سازی قبلی نقاط رسمی روی `v34.39.31` و قراردادهای تستر همان خط (PR #52).
- `version` / `release` در `VERSION.json` به `34.39.28` شاخهٔ مبدأ برنگشت.

## هم‌راستاسازی ادغام

- تعارض `VERSION.json` روی `crm_version = v34.39.32` حل شد و فیلدهای `version` / `release` با همان نسخه یکدست شدند (شاخهٔ مبدأ این دو فیلد را روی `34.39.28` جا گذاشته بود).
- نقاط رسمی نسخه که این شاخه عوض نکرده بود و روی `v34.39.31` مانده بودند، به `v34.39.32` آمدند: `manifest.json`، `clear-cache.html`، `shell.js`، `cms.js` (`PTF_CMS_JS_VER`)، `device-reconnect.html`، `SD_SERVICE_VERSION`. `index.html` و `sw.js` از خود شاخهٔ مبدأ روی `v34.39.32` بودند.
- پین تسترها با `bump-version-pins.js` از `34.39.31` به `34.39.32` به‌روز شد.
- سه پوستهٔ هدایت `services/{electrical,instrumentation,piping}-equipment/index.html` که در اسپیرینت ۴ به‌اشتباه بازنویسی شده بودند (`viewport` / `robots` / `refresh` به مسیر نسبی تبدیل شده بود و ممیزی ریدایرکت را قرمز می‌کرد) به استابِ `noindex` اصلی برگردانده شد. بقیهٔ پاکسازی لینک‌های ۳۰۱ دست‌نخورده ماند.
- خودارجاعی بلوک «مطالب مرتبط» در چهار مقالهٔ مرکز دانش حذف شد و `sitemap-lastmod.json` با lastmod نقشه برای صفحات لمس‌شده و مقالهٔ جدید تست هیدرواستاتیک PE هم‌تراز شد تا گیت CI (`tester575` / `tester608`) سبز بماند.
