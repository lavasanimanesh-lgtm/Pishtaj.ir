# 🚀 یادداشت‌های انتشار (Release Notes - Version 90.1 — AI Proxy Robustness & Global Scope Fix)
**پروژه:** شرکت پیشرو تجهیز فرتاک (pishtaj.ir) — پلتفرم ۳۶۰ درجه زنجیره تامین و CRM  
**تاریخ ریلیز:** ۱۷ تیر ۱۴۰۵ (July 2026)  
**تمرکز اسپرینت ۹۰.۱:** حل باگ باز نشدن پنل تنظیمات + پایداری کامل لایه هوش مصنوعی روی IPv4 (US-207)

---

## 🐞 حل باگ گزارش‌شده: باز نشدن پنل تنظیمات

شما با دقت بسیار بالایی گزارش دادید که با کلیک روی دکمه «تنظیمات»، صفحه تنظیمات باز نمی‌شود.
**علت باگ:** در نسخه قبل، متغیر نگهداری شماره نسخه (`VER`) در داخل یک تابع محصور (IIFE) جهت پاکسازی کش تعریف شده بود و محدوده دسترسی (Scope) محلی داشت. هنگامی که تابع سازنده پنل تنظیمات (`buildSettings`) قصد داشت برچسب نسخه را در بالای تنظیمات رندر کند، با خطای جاوااسکریپت `ReferenceError: VER is not defined` مواجه شده و رندر صفحه متوقف می‌شد.
**راه‌حل اعمال‌شده (v90.1):** متغیر نسخه به صورت سراسری (`window.VER = 'v90.1'`) در کل نرم‌افزار تعریف شد تا تمامی ماژول‌ها و پنل‌ها بدون هیچ باگی به آن دسترسی داشته باشند. اکنون پنل تنظیمات بلافاصله و روان باز می‌شود.

---

## 🛡️ نحوه بازگشت به نسخه قبل (Rollback Mechanism) در صورت بروز مشکل

در پاسخ به سوال مهم شما:
> *«آیا با اکسترکت دوبار V90 نرم افزار به حالت قبل برمی‌گردد؟ یعنی اگر آپدیتی سیستم را دچار مشکل کرد چطور می‌توان آن را به حالت قبل برگرداند؟»*

**پاسخ دقیق:**
1. **اکسترکت دوبار یک بسته زیپ (مثلاً v90.1):** صرفاً همان فایل‌های نسخه جدید را دوباره روی سرور بازنویسی (Overwrite) می‌کند و به نسخه قبل برنمی‌گردد.
2. **روش صحیح بازگشت (Rollback):** برای بازگشت به هر نسخه قبلی (مثلاً بازگشت از `v90.1` به `v89.2` یا `v89.1`)، کافی است **فایل Zip همان نسخه قبلی** (مثلاً `pishtaj-hotfix-v89.2.zip`) را روی هاست اکسترکت کنید!
   - تمامی بسته‌های ریلیز گذشته شما در پوشه پروژه ذخیره هستند. به محض اینکه فایل زیپ نسخه قبل را اکسترکت کنید، فایل‌های HTML و PHP دقیقاً به همان وضعیت قبل بازمی‌گردند.
3. **عدم از دست رفتن اطلاعات (Zero Data Loss Guarantee):** از آنجا که تمام اطلاعات مشتریان، استعلامات، کالاها و پیش‌فاکتورها در پایگاه داده و `localStorage` نگهداری می‌شوند، دانگرید یا رول‌بک کردن فایل‌های اجرایی (`index.html` و `llm.php`) **هیچ‌گونه تاثیری روی داده‌های تجاری شما نمی‌گذارد** و اطلاعاتتان کاملاً محفوظ می‌ماند.

---

## 💡 دستاوردهای اسپرینت ۹۰.۱ (US-207)

### ۱. ارتقای پایداری و ضدکش شدن لایه سروری (`api/llm.php` v90.1)
- **اجبار به اتصال IPv4 (`CURL_IPRESOLVE_V4`):** حل مشکل قفل شدن و بازگشت پاسخ خالی cURL در سرورهای اشتراکی و هاستینگ‌های ایرانی.
- **تخلیه کش سرور (`OPcache Invalidation`):** افزودن هدرهای ضدکش و دستورات تخلیه حافظه موقت PHP تا سرور همیشه آخرین کدها را اجرا کند.
- **رمزگشایی خودکار فشرده‌سازی:** افزودن `CURLOPT_ENCODING => ''` برای پذیرش پاسخ‌های فشرده Gzip/Deflate.

### ۲. نمایش یوزر استوری در تنظیمات (`crm/index.html` v90.1)
- اکنون در بخش بالای صفحه تنظیمات و همچنین در باکس وضعیت هوش مصنوعی، نسخه سیستم (`v90.1`) و یوزر استوری فعال (`US-207`) به وضوح نمایش داده می‌شود.

---

## 📦 دانلود بسته نهایی v90.1

بسته فشرده **`pishtaj-release-v90.1.zip`** آماده دانلود و اعمال روی هاست است.
پس از اکسترکت، کلیدهای **`Ctrl + F5`** را بزنید و پنل تنظیمات را باز کنید.
