# RELEASE NOTES — v34.7.87 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## فیلد ارزش افزوده فاکتور رسمی تامین‌کننده (SUP-VAT-001)

### درخواست
تامین‌کننده فاکتور رسمی صادر کرده، ولی در فرم ثبت فاکتور خرید، فیلدی برای ارزش افزوده
یا درصد آن وجود نداشت. سوال: مبلغ را با ارزش افزوده وارد کنیم یا بدون آن و سیستم خودش
۱۰٪ را حساب کند؟

### ریشه
- کد از قبل منطق محاسبه‌ی VAT داشت (`slInvVatPct`, `slInvVatSum`, `slInvVatWrap`
  در توابع `slInvTypeChanged`/`slInvCalcLive`/`slInvoiceSave`)، اما **بلوک HTML مربوطه
  (شامل `slInvVatWrap` و `slInvVatPct`) هرگز در فرم ساخته نمی‌شد.** در نتیجه کاربر
  فیلدی را نمی‌دید و مشخص نبود مبلغ را با یا بدون ارزش افزوده وارد کند.
- همچنین رشته‌ی `<input id="slInvNote">` خراب بود (`</radius:8px;direction:ltr" ...`)،
  که باعث از دست رفتن `oninput` روی فیلد یادداشت می‌شد.

### تغییر
- `crm/supplier-finance.js`:
  - بلوک `slInvVatWrap` اضافه شد (فقط برای فاکتور «رسمی» و «پوششی/رسمی» نمایش داده می‌شود).
  - فیلد `slInvVatPct` با **پیش‌فرض ۱۰٪**.
  - متن راهنما: «مبلغ فاکتور را **بدون ارزش افزوده** (خالص/پایه) وارد کنید؛ مقدار ارزش
    افزوده و جمعِ با ارزش افزوده خودکار محاسبه می‌شود.»
  - `slInvVatSum` نمایش زنده‌ی «ارزش افزوده + جمع با ارزش افزوده».
  - اصلاح رشته‌ی HTML خراب فیلد یادداشت (ورودی صحیح با `style="direction:ltr"` و `oninput`).
- ذخیره‌سازی بدون تغییر: برای فاکتور رسمی `inv.vatPct` و `inv.vatAmount` ذخیره می‌شود.

### رفتار جدید
- کاربر مبلغ **پایه (بدون ارزش افزوده)** را در «مبلغ فاکتور» وارد می‌کند.
- سیستم بـا درصد (پیش‌فرض ۱۰٪) ارزش افزوده را حساب و مجموع را نمایش می‌دهد.
- اگر درصد ۰ وارد شود، ارزش افزوده ۰ خواهد بود.

### تست
- `_tools/uat/tester490-v34.7.87-supplier-vat.js` در گیت CI.
- گیت CI: **106 PASS / 0 FAIL**.

## توجه
- فاکتور غیررسمی همچنان بدون بلوک VAT نمایش داده می‌شود.
- این تغییر فقط افزودنی/اصلاح نمایش است؛ مدل داده و محاسبه‌ی balance تغییر نکرد.
