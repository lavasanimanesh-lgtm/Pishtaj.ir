# گزارش ادامهٔ کار — فاز «نازک‌سازی سمت کلاینت»: گیت‌های T0 و پاک‌سازی بایپس‌ها

**تاریخ:** ۲۰۲۶-۰۸-۲۸ | **نسخهٔ نهایی این نشست:** `v34.8.35` | **برنچ:** `arena/01a047d8-pishtaj-ir`
**مبنای ورودی:** `ARENA-THIN-CLIENT-STATUS-AUDIT-2026-08-28.md` (گزارش ارزیابی نشست `arena/01a0479d-pishtaj-ir`،
که خود فقط ممیزی بود و کد عوض نکرد) + `ROADMAP-THIN-CLIENT-MAXIMAL-2026-08-27.md`

---

## ۱) خلاصهٔ مدیریتی

گزارش قبلی دو اولویت را تعیین کرده بود: (۱) وصل‌کردن گیت‌ها به CI، (۲) بازنشستگی موتور legacy و حذف
fallbackهای مرده. این نشست **اولویت ۱ را کامل ساخت** (با یک گام بازِ دستی که توضیحش پایین است) و
**نیمهٔ کم‌ریسک اولویت ۲ را انجام داد** (حذف شش بایپس مرده). نیمهٔ پرریسک اولویت ۲ (T6/C5 — بازنشستگی
`pushDirty`/`pullCheck`) عمداً انجام **نشد** و در بخش ۵ به‌صورت یک برنامهٔ قابل‌تصمیم‌گیری نوشته شده است؛
حدسِ ما این است که بدون soak روی دادهٔ زنده، ریسک مالی‌اش از فایده‌اش بیشتر است.

در جریان بررسی، **سه ایراد جدی در خودِ کارِ نشست‌های قبلی** پیدا شد که در ادامه اصلاح شد (بخش ۳).

---

## ۲) وضعیت بندها پس از این نشست

| بند | قبل | بعد | یادداشت |
|---|---|---|---|
| **T0-3** گیت CI در workflow | 🔴 انجام‌نشده | 🟡 آماده، منتظر اعمال دستی | `ci-gate-step.sh` + پچ workflow؛ مجوز `workflows` به App داده شود، خودکار می‌شود |
| **T0-4** post-deploy hash check | 🔴 انجام‌نشده | 🟡 آماده، منتظر اعمال دستی | `post-deploy-hash-check.sh` + `api/deploy-probe.php` + allowlist |
| **T0-6** هدر کش استیجینگ | 🔴 انجام‌نشده | ✅ انجام‌شده (در مخزن) | `PTF_STAGING` env در `.htaccess` و `crm/.htaccess` |
| **T5-1** حذف fallbackهای مرده | 🟠 ناقص | ✅ انجام‌شده برای ۶ نقطه | بدهی A10: **263 → 257** (مبنا بازسازی شد) |
| **T4-1b** توکن از LS | 🟠 defer | 🟠 بی‌تغییر | هنوز در `crm/index.html:1949-2076`؛ نیازمند چرخش دستگاه |
| **T6/C5** بازنشستگی موتور legacy | 🔴 انجام‌نشده | 🔴 بی‌تغییر | برنامه در بخش ۵ |
| **T3-4 / T5-2 / T4-3 / T7** | 🟠 | 🟠 بی‌تغییر | فهرست بازمانده‌ها در بخش ۵ |

---

## ۳) یافته‌های تازهٔ این نشست (چیزهایی که گزارش قبلی ندید)

1. **پچ معلق ۲۰۲۶-۰۸-۲۷ خراب بود.** `_tools/PENDING-workflow-t0-gates-2026-08-27.patch` داخل بدنهٔ YAML
   یک heredoc (`cat >> crm/.htaccess <<'HTCACHE'`) داشت که شناسهٔ پایان (`HTCACHE`) تورفتگی (indent) دارد؛
   در bash شناسهٔ پایان heredoc نباید فاصلهٔ ابتدایی داشته باشد ⇒ گام «Add staging banner» روی هر push
   `arena/**` می‌سوخت و **استیجینگ عملاً از کار می‌افتاد**. اعمال نشدن آن پچ شانس آورده است.
2. **بررسی صحت استقرار آن پچ هرگز سبز نمی‌شد.** `sha1sum api/sales-domain.php` را با خروجی HTTP همان
   مسیر مقایسه می‌کرد؛ فایل PHP روی هاست **اجرا** (یا با `api/.htaccess` مسدود) می‌شود، پس بدنهٔ پاسخ هیچ‌وقت
   بایت‌های سورس نیست ⇒ شکست همیشگی. راه‌حل این نشست: پروب سروری `api/deploy-probe.php` که هش را *روی سرور*
   می‌گیرد، و مجاز «`مسیر-محلی|/مسیر-یوآرآی`» برای فایل‌هایی که canonicalize آدرسشان را عوض می‌کند.
3. **`php.yml` روی `main` بی‌اثر/قرمز است** (اجرای `composer validate` در مخزنی که `composer.json` ندارد)؛
   گزارش قبلی تنها به نبودِ گیت در دو `deploy-*.yml` اشاره کرده بود. نسخهٔ جایگزین در پچ این نشست، همان
   گیت CI را روی هر push/PR به main هم اجرا می‌کند.
4. **ادعای گمراه‌کننده در تستر، نه فقط در کد.** `tester431` با چک کردن متن `run-ci-gate.js` اعلام می‌کرد
   «نگهبان به گیت CI وصل است». اصلاح شد؛ از این پس `_tools/uat/lib-deploy-gates.js` ترتیب واقعی گام‌ها در
   workflow را می‌سنجد (گیت **قبل از** FTP، بررسی صحت **بعد از** FTP) و تا اعمال پچ، presenceٔ پچِ
   **اعمال‌شدنی** الزامی است — یعنی گیت‌ها نمی‌توانند بی‌صدا غیب شوند.
5. **دو بایپس مردهٔ دیگر هم بود، نه دو تا.** گزارش قبلی `codegen.js` و `treasury.js` را دیده بود؛
   رودمپ «۵ فایل» می‌گفت و عملاً **۶ نقطه** بود: `case-revision.js:252` (تهاتر مرجوعی خرید — مالی!)،
   `surplus.js:10`، `treasury-call.js:25` و `finance-write-guard.js:237` (رویدادهای مالی) هم همان
   الگو را داشتند. هر شش مورد حذف شد.
6. **شمارش بدهی A10 در گزارش قبلی (۲۰۱) با مبنای arch-guard (263) نمی‌خواند**؛ اختلاف از `crm/index.html`
   (۶۲ مورد) و شمردن چند عملیات در یک خط می‌آید. عدد دقیق فعلی: **257 = 68 setItem + 153 getItem + 36 removeItem**
   (آغازگرهای سنگین: `index.html` ۶۲، `codegen.js` ۲۷، `bridge.js` ۲۰، `sales-domain-v2.js` ۱۲).

---

## ۴) چیزی که ساخته شد و چطور تست شده

| قطعه | نقش | کنترل |
|---|---|---|
| `_tools/ci/ci-gate-step.sh` | پوستهٔ گیت CI قبل از FTP؛ سیاست `block/warn`؛ **نبودِ خط جمع‌بندی = شکست** | خودآزمون ۷ سناریو (`--self-test`) از جمله «exit 0 ولی FAIL>0» و «سیاست نامعتبر ⇒ exit 2» |
| `_tools/ci/post-deploy-hash-check.sh` | مقایسهٔ هش فایل زنده با کامیت + حالت `--probe` + `--expect-version` | خودآزمون ۱۳ سناریو با سرور محلی: یکسان/کهنه/۴۰۴/پروب کهنه/پروب غایب/هدر نامعتبر/`warn` |
| `api/deploy-probe.php` | هش و اندازهٔ ۱۰ فایل نسخه‌ای روی هاست (فهرست ثابت، بدون ورودی کاربر) | lint با `php-parser`؛ قرارداد پاسخ در `tester536` |
| `.htaccess`، `crm/.htaccess` | T0-6 بدون ریسک پروداکشن | `tester536` شرطی‌بودن، ترتیب بعد از بلوک کش ۳۰ روزه و `env=` را چک می‌کند |
| حذف ۶ بایپس + بازسازی مبنا | کاهش واقعی بدهی | `arch-guard` PASS، بازگشت هر امضا = شکست |
| `_tools/uat/bump-version.js` | هم‌سنجی/مُهر نسخه (drift نسخه‌ای بدهی مزمن این مخزن است) | `--check` روی v34.8.34 قبل و v34.8.35 بعد سبز بود |
| `tester536` (در SUITE گیت) | قرارداد همین نشست | ۴۹ PASS / ۰ FAIL |

اجرای کلی: **گیت CI = ۱۵۱ PASS / ۰ FAIL** · `arch-guard` = PASS · `node --check` روی ۱۰۸ فایل `crm/*.js` = سالم.

---

## ۵) برنامهٔ قدم بعدی (به ترتیب پیشنهادی، با ریسک هر قدم)

1. **مالک: اعمال پچ workflow** (۵ دقیقه) — `WORKFLOW-T0-GATES-APPLY-GUIDE-2026-08-28.md`. بعد از آن
   `tester536`/`tester431` خودبه‌خود از «pending» به «wired + ترتیب سخت» می‌روند.
2. **T5-2 (کم‌ریسک، بدون لمس مسیر مالی):** کلیدهای DEV سنگین به IDB: `ptf_code_tmp_queue/plan/ack`،
   `ptf_autodraft_offer_*`، `ptf_sales_command_*` (۷ نقطه در `sales-domain-v2.js`). پس از آن `E3`
   (LS زیر ۲۰KB) قابل اثبات می‌شود.
3. **T3-4:** کش‌های read-through (`ptf_site_*`, `ptf_fx_live_cache`, `ptf_cloud_usage`) از LS بیرون —
   با TTL و قابل‌تخلیه؛ ریسک پایین، اثر مستقیم بر حادثهٔ «سهمیه ۱۰۰٪».
4. **T5-1 (باقی‌مانده، متوسط):** نوشتن‌های کسب‌وکاری `crm/index.html` → `ptf_crm_settings:3830`،
   `ptf_crm_order_prices:4082`، `ptf_crm_finance:4098`، `ptf_crm_users`×۴ و ۲۷ نقطهٔ `codegen.js`.
   `bridge.js` (۲۰) کش سمت سایت است و باید دسته‌بندی/مستند شود، نه لزوماً حذف.
5. **T4-1b (نیازمند تصمیم کارفرما):** حذف `ptf_crm_token` از localStorage. پیش‌نیاز: چرخش نشست روی
   همهٔ دستگاه‌ها (کوکی HttpOnly از v34.8.28 فعال است) + یک ریلیز «فقط کوکی».
6. **T6/C5 — بازنشستگی `pushDirty`/`pullCheck` (پُرخطرترین، پیشنهاد: با per-key flag و soak یک‌هفته):**
   - فاز ۱: شمارش دقیق تعداد writes در هر دو مسیر (`entity_*` در برابر `data_push`) روی استیجینگ با
     تله‌متری موجود `sync_stats`؛ اگر سهم legacy زیر ۱٪ باشد، فاز ۲ بی‌ریسک است.
   - فاز ۲: `pullCheck` فقط برای «دلتا» نگه داشته شود و `pushDirty` برای کلیدهای REC خاموش شود (flag).
   - فاز ۳: حذف ~۲۰۰۰ خط منطق تعارض + به‌روزرسانی چهار ابزار ریکاوری (T7) به IDB-aware.
   - شرط ورود: هیچ کلید REC‌ای بدون روتر فرمان نماند (A11 = ۰، که برقرار است) و یک سبز کامل روی
     استیجینگ با دادهٔ واقعی مدیران.
7. **T7:** 2FA پیامکی نقش‌های مالی، bulk revoke نشست، rate-limit فرمان‌ها.

---

## ۶) راهنمای بازتولید/کنترل سریع

```bash
bash _tools/ci/ci-gate-step.sh                    # گیت کامل + arch-guard (≈۱۶ ثانیه)
bash _tools/ci/ci-gate-step.sh --self-test         # خودآزمون خودِ پوسته
bash _tools/ci/post-deploy-hash-check.sh --self-test
node _tools/uat/lib-deploy-gates.js               # «گیت‌ها وصل‌اند یا پچ معلق لازم است؟»
node _tools/uat/bump-version.js --check           # هم‌سنجی نسخهٔ نقاط رسمی
node _tools/arch/arch-guard.js                     # بدهی A1..A11
```

برای دیدن «فایل زنده == کامیت» روی محیط جاری (بدون دیپلوی):

```bash
bash _tools/ci/post-deploy-hash-check.sh --base https://staging.pishtaj.ir \
  --probe --expect-version "$(node -p "require('./VERSION.json').crm_version")" \
  --files "crm/index.html|/crm/ crm/sw.js" --retries 2 --sleep 5
```
