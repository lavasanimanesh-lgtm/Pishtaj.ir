# 📦 Release Notes — v62.0
**اسپرینت ۶۲: انبار ابری آروان (US-114) + اقلام درخواست‌محور (US-124) + چرخه پس از فروش (US-116) + فاز ۲ پیشنهادها**
**تاریخ:** ۱۳ تیر ۱۴۰۵ (2026-07-03)

---

## ☁️ US-114 — انبار ابری آروان‌کلود ✅ (متصل و عملیاتی — تایید کارفرما)

- `api/storage.php`: امضای **AWS Signature V4 خالص در PHP** (بدون SDK/Composer) — **تست شده با وکتور مرجع رسمی AWS (مطابقت بیت‌به‌بیت)**
- **آپلود مستقیم مرورگر → آروان** با Pre-signed URL: فایل‌ها از هاست عبور نمی‌کنند (صفر مصرف فضا/پهنای باند)
- فشرده‌سازی خودکار تصاویر به WebP سمت کلاینت (حداکثر 1600px، ~۷۰٪ کاهش) + نمایش «قبل → بعد»
- لینک موقت امن دانلود فایل‌های خصوصی + حذف + لیست + **باکس تست اتصال در تنظیمات CRM**
- Fallback (AC7): در قطعی آروان، متادیتا در صف `ptf_storage_queue` ثبت می‌شود
- نرمال‌سازی خودکار endpoint (اگر https:// جا بیفتد) + رفع باگ `.htaccess` قدیمی api که دسترسی را می‌بست
- ویجت آپلود چندمنظوره drag & drop (`attachUploadWidget`) — متصل به فرم ثبت فاکتور؛ آماده اتصال به پرونده‌ها/مکاتبات

## 📋 US-124 — اقلام درخواست‌محور (اکسل → پیشنهاد خودکار)

- **AC1:** هنگام ایمپورت اکسل کالا، شماره درخواست (Inquiry) پرسیده می‌شود → همه ردیف‌ها با تعداد و واحد در `ptf_crm_inqitems` گروه‌بندی می‌شوند
- **AC2:** در فرم TO/CO دکمه **«📥 بارگذاری اقلام درخواست»** → لیست درخواست‌های موجود → همه اقلام با **تعداد و واحد از اکسل اولیه** وارد جدول می‌شوند؛ Inquiry No سند خودکار پر می‌شود
- **AC3:** ویرایش آزاد پس از بارگذاری: تغییر شرح/تعداد، حذف/افزودن ردیف؛ برند و قیمت در همان فرم
- **AC5:** CO مستقل بدون TO — مسیر قبلی حفظ شد
- **AC6:** پنل جدید **«📋 درخواست‌ها»**: هر درخواست با تعداد اقلام + وضعیت (بدون پیشنهاد / TO صادر شده / CO صادر شده)

## 📁 US-116 — چرخه پس از فروش (پنل «پرونده‌های فروش»)

- **AC1:** تشکیل پرونده از CO برنده — تایم‌لاین ۴ مرحله‌ای: ابلاغ ← تحویل/ارسال ← فاکتور ← تسویه با نشانگر بصری
- **AC2:** نوع فروش رسمی/غیررسمی — غیررسمی: **رسید داخلی `PTF-INT-XXX`** به‌جای فاکتور + برچسب متمایز
- **AC3:** **ترتیب مراحل آزاد** — فاکتور پس از تحویل قابل ثبت است (تست شد) + فرم ثبت فاکتور حسابداری با **آپلود واقعی فایل به آروان** و لینک دانلود
- **AC4:** مرحله ارسال: نوع (توسط ما/درب انبار/حمل خریدار) + شماره بیجک/بارنامه + باربری
- AC5 (مغایرت مبلغ) از اسپرینت ۵۸ فعال بود؛ AC9 (وصولی مرحله‌ای) نیز از ۵۸ — هر دو به پرونده لینک‌اند

## 📄 فاز ۲ پیشنهادها — AC7: ستون‌های داینامیک با عرض هوشمند

- دکمه **«＋ ستون جدید»** در فرم TO/CO (مثلاً COO, HS Code) — در فرم و خروجی چاپ ظاهر می‌شود
- **سقف ۳ ستون اضافه** + پیام راهنما (حفظ عرض A4 لنداسکیپ طبق AC)
- **فونت پلکانی هوشمند چاپ:** 10px → 9.5 → 9 → 8.5 بر اساس تعداد ستون‌ها — خروجی هرگز از عرض صفحه بیرون نمی‌زند
- ستون‌ها و مقادیر در سند ذخیره و در revision ها حفظ می‌شوند

## 🧪 تست‌ها — ۸/۸ یکپارچه PASS
برچسب‌گذاری اقلام ✔ بارگذاری با تعداد/واحد اکسل ✔ ویرایش آزاد ✔ سقف ستون + فونت پلکانی ✔ فاکتور-پس-از-تحویل ✔ رسید غیررسمی ✔ وضعیت درخواست ✔ + وکتور رسمی AWS SigV4 ✔ + سینتکس همه JSها ✔ + ممیز سایت PASS ✔

## 🚀 دیپلوی
- جدید: `crm/storage.js`، `api/storage.php` (+htaccess اصلاحی — قبلاً دیپلوی شد)، `storage-config.sample.php`
- تغییر: `crm/index.html`، `crm/offers.js`، `crm/rbac.js`

## ⏭️ باقیمانده اسپرینت ۶۲ → منتقل به ۶۳
کتابخانه T&C قابل‌مدیریت و تاریخچه کامل revision (کم‌ریسک) همراه پرونده پروژه کامل (US-118) و پکینگ لیست (US-120) در اسپرینت ۶۳ تحویل می‌شود — چون پرونده پروژه ساختار مدارک را نهایی می‌کند.
