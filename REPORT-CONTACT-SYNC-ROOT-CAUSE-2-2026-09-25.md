# گزارش ریشه‌یابی و اصلاح اساسی — «مشکل مشتریان» / تشخیص سینک تماس
**نسخه: v34.39.40 (CONTACT-SYNC-DIAG-2) — تاریخ: ۱۴۰۵/۰۷/۰۴ (۲۰۲۶-۰۹-۲۵)**

> **یادداشت شماره‌گذاری:** این اصلاح ابتدا v34.39.33 شماره خورد؛ پس از بررسی تاریخچهٔ main معلوم شد شماره‌های **v34.39.33 تا v34.39.39 قبلاً توسط اسپرینت‌های سئو مصرف شده‌اند** (SEO-SPRINT1 تا SEO-COMPETITOR — کامیت‌های `1c97b61f` تا `199917f5`، به‌تفصیل در RELEASE-NOTES-v34.39.32 روی main). شمارهٔ آزاد بعدی **v34.39.40** بود و کل مجموعه به آن بازشماره شد تا خط تاریخچهٔ نسخه خطی بماند.

## ۱) گزارش کارفرما (عین متن)

> مشکل مشتریان کماکان باقی است. یک بررسی کامل کن و اصلاح اساسی انجام بده. شده ماژول را دوباره بازنویسی کنی که این مشکل رفع بشه انجام بده. احتمالا مشکل عمیقتر است. حتی تشخیص سینک تماس را زدم، مشتری را انتخاب کردم و شماره را زدم و «ثبت روی سرور» را هم زدم، ولی با زدن دوبارهٔ دکمهٔ تشخیص همان پیام مبنی بر اینکه روی سرور وجود ندارد آمد.

این یعنی: **خودِ ابزار تشخیص — که قرار بود مشکل را حل کند — همان نشانه را تولید می‌کند.** بررسی کامل نشان داد ابزار سه ریشهٔ مستقل داشت که هم‌زمان می‌توانستند همین رفتار را بسازند.

---

## ۲) ریشه‌ها (RCA — با شواهد کد)

### R1 — مسیرهای جایگزینِ نوشتن با «موفقیت کاذب» (مهم‌ترین)

دکمهٔ «ثبت روی سرور» در `crm/contact-sync-diag.js` (نسخه‌های ≤ v34.39.32) سه شاخه داشت:

| شاخه | شرط | چه می‌کرد | عیب |
|---|---|---|---|
| ① `ptfEntityUpsert` | فرمان سروری فعال | فرمان اتمیک `entity_upsert` با رسید | سالم |
| ② `ptfEntitySaveCollection(KEY, [patched], {prevArr:[serverRec], reason:'contact-sync-diag'})` | شاخهٔ ① در دسترس نبود | روتر diff-محور | **مهلک:** reason «contact-sync-diag» در فهرست `CONTACT_TOUCH_REASONS` نبود ⇒ روتر (سپر ضدپاک‌شدن تماس v34.39.12) کلیدهای `people/coTels/phones/ph` را **از payload حذف می‌کرد** — یعنی همان شماره‌ای که قرار بود ثبت شود، پیش از ارسال حذف می‌شد! سپس `cb({state:'legacy'})` ⇒ پیام «✅ ثبت شد». شماره هرگز به سرور نمی‌رسید. |
| ③ `setData(KEY, all)` محلی | هیچ‌کدام در دسترس نبودند | فقط کش محلی | **دروغ:** پیام «✅ ثبت شد» بدون اینکه حتی یک بایت به سرور رفته باشد — دقیقاً در همان سناریویی که ابزار برایش ساخته شده (صف آفلاین گیرکرده / نشست منقضی / فایل ناهمگن). |

شاخه‌های ② و ③ دقیقاً وقتی فعال می‌شوند که لایهٔ فرمان در صفحه بارگذاری نشده باشد — یعنی **استقرار ناهمگن (مخلوط‌شدن فایل قدیمی/جدید روی cPanel)** یا خطای بارگذاری `sales-domain-v2.js`. سناریوی کلاسیک: صفحهٔ تشخیص از نسخهٔ جدید است اما لایهٔ فرمان از نسخهٔ قدیمی که `PTF_ENTITY_CMD_ENABLED` برای مشتریان ندارد ⇒ شاخهٔ ② یا ③ ⇒ «ثبت شد» کاذب ⇒ تشخیص دوباره ⇒ «روی سرور نیست».

> نکتهٔ مهم دربارهٔ نسخه‌های قبلی: اصلاحات v34.39.30 (CONTACT-ROOTS R7) مسیر `data_push` و merge سروری `entity_upsert` را محکم کردند، اما **مسیرهای داخلی خودِ ابزار تشخیص را نپوشاندند** — ابزار می‌توانست از کنارِ مسیر درست عبور کند و بازخوانی هم قابل اتکا نبود.

### R2 — خواندنِ «تازه از سرور» قابل اتکا نبود (پاسخ کش‌شده = «همان پیام»)

- درخواست تشخیص یک GET بود به `api/crm.php?action=data_pull&since=0&krevs=…` که **بین دو اجرای پشت‌سرهم، URL بایت‌به‌بایت یکسان** بود (نقشهٔ krevs بین دو اجرا عوض نمی‌شود).
- در fetch نه `cache:'no-store'` بود و نه پارامتر buster.
- `api/crm.php` **هیچ هدر Cache-Control/Pragma نمی‌فرستاد** (فقط Content-Type/nosniff/DENY).
- نتیجه: هر لایهٔ میانی — کش هیوریستیک مرورگر، LiteSpeed هاست، یا CDN (آروان که در معماری سایت حاضر است: `api/fix-arvan-cors.php`) — می‌توانست پاسخ اجرای اول را برای اجرای دوم دوباره بدهد؛ حتی وقتی ثبت واقعاً موفق بوده، تشخیصِ دوباره «همان پیام» را نشان می‌داد.
- ضمناً اگر پاسخ سرور کلید مشتریان را نداشت (نقش/نشست/ناهمگنی)، ابزار می‌گفت «رکورد اصلاً روی سرور نیست» بدون تفکیک «سرور نفرستاد» از «وجود ندارد».

### R3 — تطبیق شماره فقط «دقیق/پسوندی» بود (مثبت‌کاذبِ «روی سرور نیست»)

شمارهٔ کاربر `09121234567` با مقدار ذخیره‌شدهٔ `989121234567` یا `+98 912 123 4567` (که نویسنده‌های مختلف/ورود اکسل/قالب بین‌المللی تولید می‌کنند) نه دقیق می‌خورد و نه پسوندی؛ نتیجه: شماره **روی سرور هست** اما ابزار می‌گفت «هرگز به سرور نرسیده».

### R4 — حلقهٔ باز (بدون تأیید)

پس از «ثبت شد»، ابزار فقط می‌گفت «تشخیص را دوباره بزن». هیچ مکانیزمی وجود نداشت که خودش از سرور بازخوانی کند و مطمئن شود شماره واقعاً قابل دیدن است. هر خرابیِ R1/R2/R3 برای کاربر و پشتیبانی نامرئی می‌ماند.

### R5 — **باگ سروری: کل زنجیرهٔ فرمان‌های entity روی سرور از v34.19.0 مرده بود (کشف E2E — ریشهٔ اصلی)**

> این ریشه پس از بازنویسی سمت کلاینت، با **اجرای خودِ کد سرور روی PHP واقعی** (php-wasm، فایل‌های `api/*.php` بدون هیچ تغییری) کشف شد؛ سناریوی کامل کارفرما — seed → تشخیص (data_pull) → «ثبت روی سرور» (entity_upsert) → بازخوانی تأیید → push دستگاهِ کهنه → بازخوانی نهایی — روی همان کدی که روی هاست اجرا می‌شود.

**اشکال:** در `api/sales-domain.php` (از کامیت `16e2b5d0`، نسخهٔ v34.19.0 — سطل بازیافت)، یک گارد تعریف تابع **به‌اشتباه وسط زنجیرهٔ `if/elseif` دیسپچر** جا گرفته بود:

```php
    elseif ($action === 'attachment_add' || …) { … }
    if (!function_exists('sd_tombstone_kinds')) {      // ← گارد مستقل، وسط زنجیره!
        function sd_tombstone_kinds($key) { … }        //    (کپی نقشهٔ tombstone)
    }
    elseif ($action === 'entity_upsert' || $action === 'entity_delete') { … }
    elseif ($action === 'entity_restore') { … }
    elseif ($action === 'revoke_invoice_ref') { … }
    elseif ($action === 'entity_tombstones_neutralize') { … }
    else sd_out(['ok'=>false,'error'=>'unknown_action'],404);
```

در PHP، این یعنی دو زنجیرهٔ جدا: زنجیرهٔ اول تا `attachment_*` تمام می‌شود؛ سپس یک `if` **جدید** شروع می‌شود که `elseif(entity_upsert)` و بقیه به **همان** بند شده‌اند. چون `sd_tombstone_kinds` پیش از آن نقطه هیچ‌جا تعریف نمی‌شود، شرط `!function_exists(...)` **در هر درخواست true** است، بدنه‌اش اجرا و زنجیره تمام می‌شود — یعنی:

- **شرطِ هیچ‌یک از شاخه‌های `entity_upsert` / `entity_delete` / `entity_restore` / `revoke_invoice_ref` / `entity_tombstones_neutralize` هرگز ارزیابی نمی‌شد** (اثبات با trace مرحله‌به‌مرحله روی PHP واقعی: ارزیابی دقیقاً بعد از `attachment_add` قطع می‌شد)،
- برای هر فرمان entity، اجرا از کنار دیسپچر رد می‌شد و به دُم مشترک می‌رسید با `$changes=[]` و `$result=[]`؛ نتیجه برای کلاینت: **`{"ok":true,"rev":N+1,"result":{"keys":[]},"data":[]}`** — یعنی «✅ موفق» بدون اینکه **یک بایت** روی سرور نوشته شود،
- `else` (خطای `unknown_action`) هم به همان زنجیرهٔ مرده بند بود، پس حتی اکشن ناشناخته هم «موفق» جواب می‌گرفت.

این دقیقاً همان گزارش کارفرماست: «ثبت روی سرور را هم زدم» (کلاینت فرمان را فرستاد، سرور ok:true داد — چون فرمان واقعاً اجرا نشده بود) «ولی با زدن دوبارهٔ دکمهٔ تشخیص همان پیام مبنی بر اینکه روی سرور وجود ندارد آمد» (چون واقعاً وجود نداشت). این باگ مستقل از R1–R4 است و **بدون اصلاح سرور، هیچ نسخه‌ای از ماژول کلاینت نمی‌توانست ثبت را واقعی کند** — حداکثر (با بازنویسی جدید) به‌جای «ثبت شد» کاذب، «⛔ تأیید نشد» صادقانه می‌گرفت.

**اثربخشی گسترده:** همهٔ مصرف‌کنندگان `ptfEntityUpsert`/`entity_restore`/`entity_tombstones_neutralize` از v34.19.0 به بعد under this bug بودند — از جمله بازیافت از سطل بازیافت (RECYCLE)، خنثی‌سازی سنگ‌قبر (v34.39.20) و همهٔ ویرایش‌های تماس از مسیر فرمان.

**اصلاح:** گارد تعریف `sd_tombstone_kinds` به‌طور کامل **به پیش از دیسپچر** (قبل از `$readOnly = …`) منتقل شد و زنجیرهٔ `elseif` دوباره پیوسته است: `attachment_* → entity_upsert/entity_delete → entity_restore → revoke_invoice_ref → entity_tombstones_neutralize → else unknown_action`. کپیِ دفاعی داخل `entity_tombstones_neutralize` دست‌نخورده ماند.

**اعتبارسنجی (E2E روی PHP واقعی، فایل‌های بدونِ تغییرِ سرور):** سناریوی کامل کارفرما — **۲۴ سنجه، همه PASS**: تشخیص اول «روی سرور نیست» → `entity_upsert` با `created:false` و ردیف حاوی شماره + رشد rev → بازخوانی cache-busted «شماره دیده می‌شود» → `data_push` مجموعهٔ کهنهٔ دستگاه دیگر بدون conflict → شماره همچنان زنده (اتحاد R7) و `_ccEdit/_ccBaseAt` روی سرور نمانده‌اند.

---

## ۳) اصلاح اساسی — v34.39.40 (CONTACT-SYNC-DIAG-2)

**قرارداد جدید: «ثبت روی سرور = فرمان + بازخوانی تأیید». موفقیت بدون دیدنِ شماره در پاسخ تازهٔ سرور قابل گزارش نیست.**

### تغییرات فایل‌به‌فایل

| فایل | تغییر |
|---|---|
| `crm/contact-sync-diag.js` | **بازنویسی مسیر نوشتن و خواندن** (W1–W4 پایین) + حذف کامل LS مستقیم (A10 — فقط `window.ptfSyncKrevs` لایهٔ سینک) |
| `api/crm.php` | هدر `Cache-Control: no-store, no-cache, must-revalidate, private` + `Pragma: no-cache` برای **همهٔ** پاسخ‌ها — کلِ موتور سینک در برابر کش هر لایه‌ای ایمن شد، نه فقط ابزار تشخیص |
| `api/sales-domain.php` | **اصلاح باگ R5** (جابه‌جایی گارد `sd_tombstone_kinds` از وسط زنجیرهٔ دیسپچر به پیش از آن — زنده‌شدن کل فرمان‌های entity بعد از ۲۰ نسخه) + همان هدرها + `SD_SERVICE_VERSION = '34.39.40'` |
| `crm/sync.js` | عمومی‌سازی فقط‌خواندنیِ تابع موجود `krevs()` با `window.ptfSyncKrevs` (یک خط) — UI بدون دسترسی مستقیم LS به نقشهٔ rev ها می‌رسد (اصل A10 رودمپ thin-client) |
| `crm/index.html` | `PTF_CRM_RELEASE = v34.39.40` + بامپ `?v=` تمام ۱۱۴ اسکریپت (cache-bust کامل) |
| `crm/sw.js` | `RELEASE/ASSET_VERSION/CACHE = v34.39.40` (سرویس‌ورکر کش قدیمی را دور می‌ریزد) |
| `crm/manifest.json` / `VERSION.json` | `version/crm_version = 34.39.40` |
| `crm/shell.js` / `crm/cms.js` | تراز `PTF_CMS_JS_VER` (و fallback پوسته) با v34.39.40 — بنر کهنگی کش اکنون درست کار می‌کند |
| `crm/clear-cache.html` / `crm/device-reconnect.html` | نقاط رسمی نسخه به v34.39.40 (در بامپ اولیه جا مانده بود) |
| `_tools/uat/tester680-v34.39.40-contact-sync-verified.js` | ۵۳ تست رگرسیون — **53 PASS / 0 FAIL** |
| `_tools/uat/e2e-contact-sync-v34.39.40.js` | آزمون E2E روی PHP واقعی (php-wasm) — **24 PASS / 0 FAIL** (مستندسازی و بازتولیدپذیری R5) |
| ۱۵۸ تستر موجود | فقط پین نسخه با ابزار رسمی `bump-version-pins.js` (۶۷۱ مورد، 34.39.24→34.39.40؛ نام‌فایل‌های تاریخی محفوظ) |

### قواعد جدید ماژول (W1–W4)

- **W1 — یک مسیر نوشتن:** فقط `ptfEntityUpsert` (فرمان اتمیک `entity_upsert` با رسید/idempotency). اگر مسیر فرمان در دسترس نیست ⇒ پیام صادقانهٔ «⛔ مسیر فرمان در دسترس نیست — **هیچ چیزی ثبت نشد و عمداً هیچ نوشتن محلی انجام نشد**» + راهنمایی (Ctrl+F5 / استقرار کامل v34.39.40 / راه جایگزین: فرم ویرایش مشتری). **هیچ fallback محلی باقی نمانده.**
- **W2 — تأیید سرور:** بعد از ACK، همان لحظه رکورد با cache-buster از سرور خوانده می‌شود:
  - شماره پیدا شد ⇒ «✅ **ثبت و تأیید شد** — شماره همین حالا از سرور بازخوانی شد (rev N — کانال: …)» و **دو جدول مقایسه خودکار تازه می‌شوند** — کاربر دیگر لازم نیست تشخیص را دستی دوباره بزند و «همان پیام» دیگر ممکن نیست بیاید.
  - ACK آمد ولی شماره دیده نشد ⇒ «⛔ تأیید نشد» + **شناسهٔ رسید (operationId)** + محتمل‌ترین علت (ناهمگنی نسخهٔ مستقر) + اقدام.
  - بازخوانی خطا خورد ⇒ «⚠️ ثبت شد اما تأیید ناموفق» + رسید.
  - rejected/uncertain ⇒ پیام دقیق لایهٔ فرمان (401/403/آفلاین/نامشخص) + رسید.
- **W3 — خواندن مطمئن:** krevs از لایهٔ سینک (`window.ptfSyncKrevs` — هم‌راستا با اصلاح A10 مربوطه) با fallback؛ پارامتر `_csd=<timestamp>` یکتا در هر فراخوانی (URL هرگز بین دو اجرا یکسان نیست)؛ `cache:'no-store'`؛ تفکیک صریح «سرور خالی (fresh)» / «کلید در پاسخ نبود (keyMissing)» / «خطای شبکه» — هرکدام پیام و راه‌حل خودش.
- **W4 — تطبیق کانونی شماره:** `canonNum` ارقام فارسی/عربی را لاتین می‌کند و پیشوندهای `۰` / `98` / `+98` / `0098` را هم‌ارز می‌شمارد (شماره‌های خارجی و کوتاه دست‌نخورده) + تطبیق پسوندی قدیمی به‌عنوان پشتیبان. `۰۹۱۲…` ≡ `۹۸۹۱۲…` ≡ `+98 912 …`.

### تست‌ها (tester680 — 53 PASS / 0 FAIL · E2E سرور — 24 PASS / 0 FAIL)

- حذف کاملِ شاخه‌های موفقیت کاذب (هیچ فراخوانی `ptfEntitySaveCollection`، هیچ نوشتن محلی).
- قرارداد نسخه (index/sw/VERSION/SD_SERVICE_VERSION = 34.39.40) — **مخلوط‌شدن نسخه‌ها در استقرار بلافاصله در تست لو می‌رود.**
- هدرهای no-store هر دو API.
- canonNum/hasNum با همهٔ قالب‌ها؛ verdict پنج‌حالته؛ planFix (مهرهای `_ccBaseAt`/`_ccEdit`، رد تکراری/کوتاه).
- fetchServerRec: buster + no-store + حذف کلید از krevs + fresh/keyMissing.
- commitFix: unavailable (بدون هیچ نوشتن محلی) / verified / unverified / rejected / uncertain.

---

## ۴) چک‌لیست استقرار (cPanel — دستی)

**فایل‌هایی که باید روی هاست بارگذاری شوند (هر ۱۲ فایل، هم‌زمان):**

1. `api/sales-domain.php` ← **اصلاح R5 (حیاتی)**
2. `api/crm.php`
3. `crm/contact-sync-diag.js` ← ماژول بازنویسی‌شده
4. `crm/sync.js` ← accessor جدید `ptfSyncKrevs` (A10)
5. `crm/index.html`
6. `crm/sw.js`
7. `crm/manifest.json`
8. `crm/shell.js` ← تراز `PTF_CMS_JS_VER`
9. `crm/cms.js` ← تراز `PTF_CMS_JS_VER` (بنر کهنگی کش)
10. `crm/clear-cache.html`
11. `crm/device-reconnect.html`
12. `VERSION.json`

**ترتیب پیشنهادی:** اول `api/` بعد `crm/` — ولی چون کل مجموعه یکجا بارگذاری می‌شود، ناهمگنی پنجرهٔ خطر ندارد؛ فقط همه را قبل از تست کاربران بالا بیاورید.

**تأیید بعد از استقرار:**
1. در CRM یک‌بار **Ctrl+F5** (سخت-رفرش) بزنید — پایین صفحه/کنسول باید `v34.39.40` باشد. اگر نسخهٔ قدیمی ماند ⇒ سرویس‌ورکر کش قدیمی دارد؛ دوباره Ctrl+F5 یا از «تنظیمات ← بک‌آپ و بازگردانی» کش را پاک کنید.
2. view-source بگیرید: `contact-sync-diag.js?v=34.39.40` دیده شود.
3. تست سناریوی کارفرما: 🩺 تشخیص سینک تماس ← مشتری + شماره ← اجرای تشخیص ← (اگر گفت «هرگز نرسیده») ثبت روی سرور ← این بار باید بگویید «✅ **ثبت و تأیید شد**» و جدول «نسخهٔ سرور» خودکار شماره را نشان دهد — **دیگر لازم نیست تشخیص را دوباره بزنید**.
4. اگر به‌جای آن «⛔ تأیید نشد — رسید: csd…» آمد ⇒ استقرار ناهمگن است؛ شناسهٔ رسید را نگه دارید و کل مجموعه را دوباره بارگذاری کنید.

**تشخیص استقرار ناهمگن (محتمل‌ترین علت تجربهٔ فعلی):** در کنسول مرورگرِ صفحهٔ CRM: `window.PTF_CRM_RELEASE` و `PTF_ENTITY_CMD_ENABLED['ptf_crm_customers']` را ببینید — اگر اولی قدیمی بود یا دومی `undefined/false` بود، فایل‌های صفحه از نسخه‌های مختلف‌اند و دقیقاً همان مسیر موفقیت‌کاذبِ R1 فعال بوده است.

**وضعیت گیت CI پس از این مجموعه:** ۲۷۹ PASS / ۱۵ FAIL — در نقطهٔ شروع (HEAD) ۱۶۲ PASS / ۱۳۲ FAIL بود؛ یعنی ۱۱۶ تستر سبز شد و **هیچ رگرسیونی اضافه نشد**. ۱۳ تسترِ قرمزِ باقی‌مانده در نقطهٔ شروع هم قرمز بودند و ربطی به این اسپرینت ندارند:
- `tester676` (CONTACT-ROOTS v34.39.22): مکانیزم `sync_contact_fields_fill` که می‌سنجد، در v34.39.30 **عمداً** با اتحاد R7 جایگزین شد (کامیت `1112468b`) و تستر به‌روز نشده — تستر کهنه، نه باگ کد.
- `arch-guard A3` (`offer-fx-convert.js` empty-equality): تخلف تاریخی از کامیت `17e8fb49` — پیش از این اسپرینت.
- بقیه (tester431/443/457/552/565/575/590/608/615/616/648/675): بدهی تاریخی متنوع (lastmap سئو، ساختار knowledge-center، UI-STABILITY و…) — همگی در HEAD هم قرمز.

---

## ۵) چرا این بار ریشه‌ای است؟

- **ریشهٔ اصلی سمت سرور (R5) پیدا و بسته شد:** کل زنجیرهٔ فرمان‌های entity روی سرور از v34.19.0 به دلیل گاردِ تعریفِ تابعِ جاافتاده وسط دیسپچر، بی‌صدا «موفقیت کاذب» برمی‌گرداند. این بار کد سرور واقعاً اجرا و سناریوی کامل کارفرما روی آن بازتولید و سبز شد (۲۴/۲۴) — نه فقط استاتیک‌آنالیز کلاینت.
- قبلاً هر بار **یک لایه** را محکم می‌کردیم (merge سرور، سپرها، هوک‌ها) ولی ابزار تشخیص خودش می‌توانست از کنار مسیر درست عبور کند و دروغ بگوید. حالا **کل حلقه — نوشتن تا دیدنِ نتیجه در پاسخ سرور — یک مسیر واحد و اثبات‌پذیر است**: یا شماره در پاسخ تازهٔ سرور دیده می‌شود (verified) یا پیام خطا + رسید داریم. حالت «می‌گویم ثبت شد ولی نمی‌بینی» از معماری حذف شده است.
- هدر no-store دو API، کل موتور سینک (نه فقط این ابزار) را از کش هر لایهٔ میانی مصون می‌کند.
- بامپ کامل نسخه (index+sw+API+VERSION) + تست قرارداد نسخه، جلوگیری از «مخلوط‌شدن فایل‌ها» را به بخشی از CI تبدیل می‌کند.

**وضعیت Git:** همهٔ تغییرات روی شاخهٔ `arena/01a0cb49-pishtaj-ir` کامیت و پوش می‌شود؛ مرج با main (2c6135a) تمیز است (فقط همین فایل‌ها تفاوت دارند).
