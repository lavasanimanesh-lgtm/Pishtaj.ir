# 📦 Release Notes — v55.0
**اسپرینت ۵۵: ماژول کالا در CRM (US-109) + اصلاح سال تاسیس**
**تاریخ:** ۱۲ تیر ۱۴۰۵ (2026-07-03)

---

## ✅ کارهای انجام‌شده

### 0️⃣ اصلاح سال تاسیس (بازخورد کارفرما)
- `foundingDate: 2012` در **هر دو** Schema صفحه اصلی (دومی از ریلیزهای قبلی 2014 بود و ناسازگار — ممیز آن را گرفت).
- Trust Bar: «+۱۴ سال فعالیت شرکت (از ۱۳۹۱)» — دقیق و قابل دفاع.
- تایم‌لاین why-ptf: مرحله تاسیس → «۱۳۹۱ (2012)».

### 1️⃣ US-109 — ماژول کالا در CRM ✅ (همه ۶ AC)

**آیتم منوی جدید «📦 کالاها»** بین کارفرمایان و سفارشات.

| AC | قابلیت | وضعیت |
|---|---|:---:|
| AC1 | CRUD کامل: کد، نام فا/EN، دسته (۷ گروه: پایپینگ/فلنج/شیرآلات/برق/ابزار دقیق/پمپ/سایر)، استاندارد/گرید، برند، واحد (۶ نوع)، قیمت مرجع، توضیحات | ✅ |
| AC2 | جستجوی زنده چندفیلده + فیلتر دسته + مرتب‌سازی ستونی (کد/نام/قیمت) | ✅ |
| AC3 | خروجی Excel (CSV با BOM فارسی) + **ایمپورت CSV** با اعتبارسنجی و گزارش ردیف‌های رد شده | ✅ |
| AC4 | جلوگیری از کد تکراری + الگوی کد `[A-Z0-9_-]{3,30}` + فیلدهای الزامی | ✅ |
| AC5 | ذخیره در `ptf_crm_products` (localStorage) — همسو با معماری فعلی + افزوده به بکاپ JSON تنظیمات | ✅ |
| AC6 | ساختار داده آماده انتخاب در ماژول پیشنهاد (US-110 اسپرینت ۵۶): کد یکتا + قیمت مرجع + واحد | ✅ |

**جزئیات پیاده‌سازی:**
- ویرایش: کد کالا قفل (readonly) تا یکپارچگی ارجاع‌ها حفظ شود؛ tsتاریخ ایجاد/ویرایش جدا نگهداری می‌شود.
- حذف: با confirm دومرحله‌ای + ثبت در لاگ فعالیت.
- XSS-safe: تابع `escP()` برای همه خروجی‌های جدول و فرم.
- قیمت با جداکننده فارسی (`toLocaleString('fa-IR')`).

## 🧪 تست‌ها
- ✔ سینتکس کل JS با `node --check`
- ✔ تست منطقی ایزوله (Node): افزودن ✓ / کد تکراری ✓ / کد نامعتبر ✓ / escape صحیح CSV ✓
- ✔ ممیز خودکار: **۳۲۰ صفحه — PASS کامل (۰ خطا، ۰ هشدار)**

## 🚀 دیپلوی
فایل‌های تغییریافته: `crm/index.html`، `index.html`، `about/why-ptf/index.html`

## ⏭️ اسپرینت بعدی (۵۶)
US-110 — ماژول پیشنهاد فنی-مالی فاز ۱: ثبت از دل استعلام، شماره‌گذاری `PTF-Q-1405-XXX`، ردیف‌های کالا از ماژول جدید، محاسبات خودکار، خروجی PDF/Excel.
