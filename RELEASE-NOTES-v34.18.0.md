# RELEASE NOTES — v34.18.0 (نام دوگانهٔ مشتری در فهرست‌ها — فارسی + انگلیسی زیر هم)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** tester563 (۱۴ سنجه) · **گیت:** 179 PASS / 0 FAIL

## مشکل (گزارش مالک)

در فهرست **پیشنهادات، فاکتورها و پرونده‌های فروش** نام مشتری فقط انگلیسی دیده می‌شد، در حالی که فهرست مشتریان فارسی است.
**ریشه:** فیلد `buyerCo` که این ماژول‌ها موقع ثبت ذخیره می‌کنند، به‌طور تاریخی **نام انگلیسی** مشتری (`coEn`) را نگه می‌دارد (حتی ادغام مشتریان هم `keep.coEn || keep.co` می‌نویسد) و فهرست‌ها همان را خام نشان می‌دادند.

## راه‌حل — حل‌گر زندهٔ نام در زمان نمایش (بدون مهاجرت داده)

چهار هلپر مشترک در `index.html` (کنار `ptfCustEnByCd` موجود):

- `ptfCustFaByCd(cd)` — نام فارسی زندهٔ مشتری از روی کد
- `ptfCustFaByEn(en)` — نام فارسی از تطبیق نام انگلیسی (برای رکوردهای بدون cd مثل پرونده‌های فروش)
- `ptfCustNamePair(cd, storedName)` — جفت {fa, en} با اولویت دادهٔ زنده از cd
- `ptfCustCellHtml(fa, en, cd)` — سلول هم‌شکل فهرست مشتریان: **فارسی بولد + انگلیسی کوچک ltr زیر آن + کد مشتری**

### اعمال‌شده در چهار نقطه

| فهرست | فایل |
|---|---|
| 🤝 پیشنهادات (ستون خریدار) | `crm/offers.js` |
| 🧾 فاکتورهای رسمی (سرتیتر هر پیشنهاد) | `crm/official-invoice-v2.js` |
| 🧾 کارتابل ارجاع فاکتور (حسابداری) | `crm/rbac.js` |
| 🗂 پرونده‌های فروش | `crm/salesfiles.js` |

- **جستجوی فهرست‌ها هم با نام فارسی** اکنون کار می‌کند (پیشنهادات از روی cd، پرونده‌ها از تطبیق نام).
- نمایش قدیمی «انگلیسیِ تنها» حذف شد؛ اگر نام فارسی در دسترس نباشد همان نام ذخیره‌شده نشان داده می‌شود (fallback امن) و اگر fa === en فقط یک‌بار نمایش داده می‌شود.
- فاکتور غیررسمی از قبل نام فارسی را حل می‌کرد — دست نخورد.

## بهداشت

- تستر قدیمی 554 (سنجهٔ نمایش نام انگلیسی زیر خریدار) با مارک‌آپ جدید هم‌مسیر شد — روح سنجه حفظ و تقویت شد.
- انکر تستر562 که خودش نسخه‌دار بود (طعمهٔ جاروی bump) بدون نسخه شد.
- بدون تغییر داده/سرور؛ A10 پابرجا.

## فایل‌های کلیدی

`crm/index.html` (۴ هلپر) · `crm/offers.js` · `crm/official-invoice-v2.js` · `crm/rbac.js` · `crm/salesfiles.js` · `_tools/uat/tester563-v34.18.0-dual-cust-names.js`
