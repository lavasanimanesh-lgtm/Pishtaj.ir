# RELEASE NOTES — v34.7.84 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## ورود اجباری با توکن سرور + رفع «توکن معتبر وجود ندارد» (AUTH-TOKEN-REQUIRED)

### درخواست

حسابدار از موبایل اغلب نمی‌توانست وارد شود؛ ابتدا «کاربر وجود ندارد»، و بعد از ورود
موفق و خروج/ورود مجدد، پیام «توکن معتبر وجود ندارد؛ با رمز واقعی وارد شوید» می‌گرفت.
همگام‌سازی کاربران هم این مشکل را حل نمی‌کرد. ریشه‌یابی کامل در
`REPORT-AUTH-MOBILE-ROOTCAUSE-2026-08-22.md` مستند شد.

### ریشه (خلاصه)

- **کاربر وجود ندارد:** کاربری که از پنل ادمین ساخته شد فقط در localStorage همان مرورگر بود
  و هنوز روی سرور نرفته بود (`users_sync` ناقص/بی‌صدا).
- **توکن معتبر وجود ندارد:** در `doLogin()` برای کاربران غیر از admin هنوز یک مسیر
  «ورود فقط-محلی» وجود داشت. اگر `auth_login` سرور موقتاً ناموفق بود (شبکه/۴۲۹/رمز) و
  مرورگر `passhash` محلی را داشت، فقط **session بدون توکن** ساخته می‌شد؛ سپس
  `showCrm()` شرط توکن را می‌دید و همان پیام گمراه‌کننده را می‌داد.
- عامل تشدید: «رفع مشکل ورود» (`ptfSafeReset`) و انقضای ۸ ساعت (`idleCheck`) توکن را پاک
  نمی‌کردند و حالت ناهماهنگ «session بدون توکن / توکن کهنه» باقی می‌ماند.

### تغییرات

| فایل | تغییر |
|---|---|
| `crm/index.html` | حذف کامل مسیر «ورود فقط-محلی» برای همه کاربران — هر نشست فقط با `token` از `auth_login` سرور ساخته می‌شود. |
| `crm/index.html` | پاسخ منفی سروی `auth_login` با پیام دقیق (`invalid_credentials` / `too_many_attempts` / `auth_not_configured` / سایر) نمایش داده می‌شود و **session ساخته نمی‌شود**. |
| `crm/index.html` | `ptfSafeReset` علاوه بر session، `ptf_crm_token` و `ptf_crm_token_role` را هم پاک می‌کند. |
| `crm/inqreader.js` | `idleCheck` هنگام انقضای ۸ ساعت، توکن و نقش را هم حذف می‌کند. |
| مسیرهای سروری | `auth_login`/`users_sync`/`users_get` بدون تغییر؛ «هم‌گام‌سازی» فقط مشکل وجود کاربر را حل می‌کند و دیگر به صورت تنها راه ورود نیست. |
| `_tools/uat/tester486-v34.7.84-auth-token-required.js` | تست جدید؛ چک می‌کند مسیر session-without-token حذف شده، پاسخ منفی سروی پیام دقیق می‌دهد، و توکن در reset/idle حذف می‌شود. |

### تست

- `node --check` روی `inqreader.js` سبز؛ همه ۵ بلوک inline `crm/index.html` از نظر سینتکس سالم.
- گیت CI: **102 PASS / 0 FAIL**.

### وضعیت

- کامیت/پوش روی `arena/01a02872-pishtaj-ir` (استقرار فقط بعد از deploy به محیط تست).
- سناریوی تأیید کاربر: ورود موفق → خروج → ورود مجدد باید بدون پیام «توکن معتبر وجود دارد» باشد.
