# 📦 Release Notes — v61.0
**اسپرینت ۶۱: مشاور انتخاب متریال (US-108) + پاکسازی بدهی فنی آیکون‌ها**
**تاریخ:** ۱۳ تیر ۱۴۰۵ (2026-07-03)

---

## 🔧 پیش از شروع: پاکسازی بدهی فنی (طبق درخواست کارفرما)
اسکن کامل نشان داد ۶۹ ایموجی در ۱۷ صفحه (about، en، projects، products) از دور قبل مانده بود:
- ۱۷ صفحه با آیکون‌های SVG جدید (مدال، هدف، ساختمان، کاربران، ستاره و…) پاکسازی شد.
- پرچم‌های سوییچ زبان (🇬🇧/🇮🇷) به‌عنوان عنصر کاربردی حفظ شدند.
- ✔ اسکن نهایی: **صفر ایموجی** در کل سایت (خارج از مقالات KC/blog و CRM طبق AC استوری).
- ریلیز اصلاحی `v60.1` قبل از شروع ۶۱ بسته شد.

## ✅ US-108 — مشاور انتخاب متریال

### موتور تصمیم (`assets/js/ptf-advisor.js` — جدید):
**۸ خانواده کامل** (AC2 — پوشش بیش از حداقل):
| خانواده | نمونه خروجی |
|---|---|
| لوله | A106 Gr.B + SCH بر اساس فشار / A333 (دمای پایین + شارپی) / NACE MR0175 (ترش) / TP316L (خورنده) / P11-P22 (دمای بالا) |
| فیتینگ | نگاشت هم‌خانواده با لوله: WPB/WPL6/WP304 + B16.9 جوشی یا B16.11 فورج بر اساس سایز |
| فلنج | کلاس 150# تا 900#+ با تصحیح خودکار دما (150#→300# در دمای بالا) + RF/RTJ بر اساس سرویس |
| ولو | تیپ (Ball/Gate/Globe/Check/Butterfly) + بدنه (WCB/CF8M) + تریم (13%Cr / SS316 / NACE) + استاندارد |
| ابزار دقیق | گیج/ترانسمیتر بر اساس کمیت و خروجی + الزام ATEX در منطقه Ex + IP |
| برق | کابل XLPE/SWA، تابلو IEC 61439 با IP محیط، موتور IE3/Ex d |
| پمپ | API 610 (هیدروکربن)، ANSI B73.1/مگنتی (خورنده)، اسلاری، Multistage + هشدار NPSH |
| گسکت و بولت | اسپیرال وند/RTJ + B7/B7M (NACE) بر اساس کلاس و سرویس |

### جداول مرجع (AC4) — نمایش خودکار متناسب با خانواده:
- **PT Rating فلنج ASME B16.5** (کلاس 150 تا 2500 در ۴ دما)
- **جدول معادل متریال** لوله↔فیتینگ↔فلنج↔ولو (۵ خانواده متریالی)
- راهنمای انتخاب تیپ ولو + راهنمای IP تجهیزات برق/ابزار دقیق

### UI و ادغام‌ها:
- **AC1:** صفحه `assistant/` کاملاً بازسازی شد: انتخاب خانواده → ۲-۳ سوال → نتیجه (گرید + استاندارد + نکته فنی) — قبلاً فقط ۳ سوال محدود پایپینگ بود.
- **AC3:** هر نتیجه به مقاله مرتبط مرکز دانش لینک می‌شود + دکمه **«استعلام همین متریال»** → فرم RFQ با شرح پیش‌پرشده باز می‌شود (پارامتر `?item=`).
- **AC5:** ادغام با منشی چت: سوالاتی مثل «چه گریدی بخرم؟» → معرفی و لینک مشاور متریال.
- سلب مسئولیت فنی: «پیشنهاد اولیه — تایید نهایی با کارشناسان».

## 🧪 تست‌ها — ۱۴/۱۴ سناریوی مهندسی PASS
گاز ترش→NACE+HIC ✔ | دمای پایین→A333+شارپی ✔ | فیتینگ ست A333→WPL6 ✔ | فلنج 150# در دمای بالا→ارتقا به 300# ✔ | سرویس بدون نشتی→RTJ ✔ | تنظیم دبی→Globe (نه Gate) ✔ | Ex→ATEX ✔ | هیدروکربن→API 610+682 ✔ | هد بالا→Multistage+NPSH ✔ | ترش→بولت B7M ✔ و…
+ سینتکس همه اسکریپت‌ها ✔ + ممیز کل سایت PASS ✔

## 🚀 دیپلوی
- جدید: `assets/js/ptf-advisor.js`
- تغییر: `assistant/index.html` (بازسازی)، `rfq/index.html` (پیش‌پر)، `assets/js/ptf-chat.js` + ۱۷ صفحه پاکسازی آیکون

## ⏭️ اسپرینت بعدی (۶۲) — سنگین‌ترین اسپرینت CRM
فاز ۲ پیشنهادها + اتصال درخواست‌ها (US-124) + چرخه پس از فروش (US-116) + انبار ابری آروان (US-114).
📌 **پیش‌نیاز شما:** چک‌لیست ۵ مرحله‌ای آروان‌کلود (باکت + کلیدها + CORS) طبق `DOCS-CLOUD-STORAGE-GUIDE.md` — بدون آن، بخش ابری با fallback هاست پیاده می‌شود.
