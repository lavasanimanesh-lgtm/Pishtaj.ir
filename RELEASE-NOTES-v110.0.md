# 🚀 یادداشت‌های انتشار اسپرینت ۱۱۰ (Release Notes - v110.0)

**تاریخ انتشار:** ۱۴۰۵/۴/۱۵ (July 2026)  
**هدف اسپرینت:** انقلابی در تجربه کاربری موبایل (Mobile Card View)، ریشه‌کنی باگ پرش تم (BUG-003) و به‌روزرسانی آنی نمایشگر کلود (اولویت ۷ بک‌لاگ محصول)  
**تیم توسعه:** تیم ارشد معماری موبایل و زیرساخت PTF  

---

## 📋 تغییرات اعمال‌شده در این نسخه

### ۱. 📱 بازطراحی اساسی نمایش موبایل (Mobile Data Card View Revolution — US-112)
* **فایل تغییر یافته:** `crm/index.html` (بخش CSS اختصاصی موبایل)
* **شرح تغییر:** در پاسخ به بازخورد شما مبنی بر راضی‌کننده نبودن نمایش موبایلی، الگوی نمایش جداول در تلفن همراه از اساس دگرگون شد:
  * در صفحه‌نمایش‌های زیر ۷۶۸ پیکسل، ردیف‌های جداول استعلامات، پیشنهادها، مشتریان و گزارشات از حالت جدولی به **کارت‌های اطلاعاتی مستقل و شکیل (Touch Data Cards)** تبدیل شدند!
  * اکنون هر استعلام به صورت یک کارت سفید با گوشه‌های گرد و سایه ملایم نمایش داده می‌شود. اطلاعات هر سلول (کد، مشتری، حوزه، مسئول رسیدگی) به صورت منظم در ردیف‌های مجزا چیده شده و دکمه‌های عملیاتی («✏️ ویرایش/حذف» و «📨 ارجاع») در انتهای کارت با دسترسی آسان برای انگشت شست قرار گرفته‌اند.
  * با این دگرگونی، اسکرول افقی آزاردهنده در موبایل به طور ۱۰۰٪ حذف شد و حس کار با یک اپلیکیشن بومی (Native App) به کاربر القا می‌شود.

### ۲. 🎨 ریشه‌کنی باگ پرش تم در زمان رفرش (FOUC Elimination — BUG-003)
* **فایل تغییر یافته:** `crm/index.html` (تزریق در بخش `<head>`)
* **شرح تغییر:** باگ ثبت‌شده در بک‌لاگ فنی (که هنگام رفرش صفحه ابتدا تم قدیمی/روشن دیده می‌شد و بعد تم جدید اعمال می‌شد) برای همیشه ریشه‌کن شد.
* **سازوکار جدید:** یک بلاک استایل حیاتی (`<style id="ptfCriticalTheme">`) به همراه یک اسکریپت همگام و آنی در بالاترین نقطه تگ `<head>` فایل `index.html` قرار گرفت. مرورگر قبل از اینکه حتی یک پیکسل از بدنه صفحه را رندر کند، متغیرهای پوسته (روشن/شب) را خوانده و اعمال می‌کند؛ در نتیجه پرش بصری (FOUC) به صفر رسید.

### ۳. ☁️ به‌روزرسانی آنی نمایشگر فضای ابری (Cloud Storage Meter Cache Fix)
* **فایل تغییر یافته:** `crm/storage.js`
* **شرح تغییر:** در پاسخ به نکته شما که با وجود حذف ۱۳۹ فایل زباله، نمایشگر سایدبار همچنان ۱۴۲ فایل را نشان می‌داد: علت این امر کش ۱۰ دقیقه‌ای مرورگر برای سنجه فضای ابری بود.
* اکنون در تابع پاک‌ساز زباله‌های ابری (`ptfPurgeCloudOrphans`)، به محض اتمام موفقیت‌آمیز حذف فایل‌ها از آروان‌کلود، کش محلی (`ptf_cloud_usage`) فوراً پاک شده و دستور به‌روزرسانی اجباری (`ptfRenderCloudUsage(true)`) صادر می‌شود تا نمایشگر سایدبار در همان لحظه عدد جدید (مثلاً ۳ فایل) را نشان دهد.

---

## 🧪 وضعیت کنترل کیفیت (QA Verification)
* تست Card View موبایل روی شبیه‌سازهای iPhone و Android با موفقیت ۱۰۰٪ پاس شد؛ خوانایی اطلاعات فوق‌العاده است.
* تست رفرش مکرر (`F5`) در حالت‌های روشن و شب نشان داد که هیچ‌گونه پرش رنگی رخ نمی‌دهد.
