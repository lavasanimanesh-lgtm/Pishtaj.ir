# Release Notes — v31.7.16

**تاریخ:** ۱۴۰۵/۰۴/۲۸ (2026-07-19)
**نسخه:** v31.7.16
**نوع:** 🟢 فعال‌سازی ماژول بک‌لاگ US-436 با نام جدید «موجودی انبار» (US-STOCK-RENAME)

---

## 🎯 درخواست کارفرما

بررسی بک‌لاگ برای ماژول «مازاد پروژه» و پیاده‌سازی آن با نام **«موجودی انبار»**.

## 📋 یافته بک‌لاگ

Family 4 بک‌لاگ v31.7.2 (منتقل‌شده به v31.7.8): **US-436 (light scope) از v31.7.2 در source فعال بود** — ثبت، جستجو، sync/backup/golive whitelist، provenance، رزرو، commit فروش برنده (idempotent) و آزادسازی باخت، همه موجود و tester171 سبز. آنچه باز بود: UAT واقعی + نام‌گذاری. طبق محدودیت مصوب بک‌لاگ، «انبار کامل، انتقال بین انبارها و شمارش دوره‌ای» خارج از scope است و بدون approval مستقل توسعه نمی‌یابد — این ریلیز در همان light scope می‌ماند.

## ✅ تغییرات

| # | فایل | تغییر | نوع |
|---|------|--------|-----|
| 1 | `crm/index.html` | سایدبار/عنوان پنل/fallback: «🧱 مازاد پروژه» → «🏬 موجودی انبار» | UI |
| 2 | `crm/surplus.js` | تمام برچسب‌ها، دیالوگ ثبت، پیام‌ها، گاردها و audit با نام «موجودی انبار»؛ بج وضعیت فارسی (✅ موجود / ⏳ رزرو / 💰 فروخته) | UI |
| 3 | `crm/surplus.js` | ✨ **نوار آمار جدید:** ردیف موجودی / قابل استفاده / رزروشده / فروخته‌شده در بالای فهرست | ✨ |
| 4 | `_tools/uat/tester171` | همگام‌سازی assertion لیبل با نام جدید | تست |
| 5 | ۴ نقطه نسخه | v31.7.16 | — |

## 🔒 قید ایمنی مهم (تصمیم مهندسی)

**کلید داده `ptf_crm_surplus`، نام توابع (`ptfSurplus*`) و route پنل (`surplus`) عمداً تغییر نکردند** — فقط نام‌گذاری UI عوض شد. دلیل: این کلید در ۷ نقطه (sync سرور/کلاینت، role-keys، backup، golive، API، تسترها) whitelist است و تغییر نامش دقیقاً همان «باگ تاریخی کلید فراموش‌شده» را بازتولید می‌کرد؛ ضمن این‌که داده موجود Production بدون migration سالم می‌ماند.

## 📊 نتایج گیت

- تستر جدید `tester191-stock-rename`: **۱۸/۱۸ PASS** — نام‌گذاری کامل + قیدهای ایمنی + چرخه رفتاری کامل (ثبت ۱۰ → رزرو ۴ → برد و فروش ۴ با idempotency → رزرو ۲ و آزادسازی باخت)
- رگرسیون کامل: **169/169 فایل PASS — 3933/3933 چک PASS** (شامل tester171 قدیمی ماژول)
- `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| داده موجود `ptf_crm_surplus` | صفر — بدون migration، رکوردهای قبلی با نام جدید نمایش داده می‌شوند | صفر |
| sync/backup/golive | دست‌نخورده (کلید تغییر نکرد) | صفر |
| نقش‌ها (sales/buyer پنل را دارند) | فقط لیبل جدید می‌بینند | صفر |
| گزارش‌های audit قدیمی | سوابق قبلی با ماژول «مازاد پروژه» می‌مانند؛ لاگ‌های جدید «موجودی انبار» | ناچیز |

## 📋 راستی‌آزمایی توسط کارفرما (۴ دقیقه)

1. سایدبار → «🏬 موجودی انبار» → نوار آمار + فهرست با بج‌های فارسی.
2. «+ ثبت موجودی» → کالا/تعداد/محل → ثبت → در فهرست و آمار ظاهر شود.
3. «💰 فروش» روی یک ردیف → پیشنهاد CO با قلم از انبار ساخته و رزرو ثبت شود.
4. آن پیشنهاد را برنده کنید → فروخته‌شده زیاد و رزرو صفر؛ بازنده کنید (پیشنهاد دیگر) → رزرو آزاد شود.

## 🔭 خارج از scope (نیازمند approval مستقل طبق بک‌لاگ)

انبار کامل، انتقال بین انبارها، شمارش دوره‌ای، حداقل موجودی/هشدار سفارش مجدد — در صورت تمایل به‌عنوان roadmap مستقل تعریف شود.

---
*توسعه‌دهنده: PTF Development Team — 2026-07-19*
