# 🚀 ریلیزنوت v19.6 — اسپرینت R13 (ابلاغ مستقیم کارفرما): «واحد ریال + جداکننده هزارگان + حاشیه سود مرجع‌دار»

**تاریخ:** ۱۴۰۵/۰۴/۲۱ | **شاخه:** واحد | **مبنا:** BACKLOG-FINANCE-UX-R13.md (تیم سهولت و چابکی) — به دستور صریح کارفرما هر سه آیتم در یک اسپرینت (استثنای مصوب بر قاعده ۱-۲ استوری)

---

## 📦 محتوای اسپرینت + مصوبات کارفرما

| آیتم | شرح | مصوبه کارفرما | وضعیت |
|---|---|---|---|
| **BUG-030** | یکسان‌سازی واحد پول همه ماژول‌های مالی روی **ریال** (ضد خطای ۱۰ برابری ریال/تومان) | داده قدیمی **بدون ×۱۰** — فقط تغییر برچسب (relabel) | ✅ کامل |
| **US-438** | جداکننده هزارگان زنده در همه ورودی‌های مبلغ، سراسر نرم‌افزار | زیر فیلد: **مبلغ به حروف با واحد اصلی** (بدون واحد دوم) | ✅ کامل |
| **US-439** | قیمت فروش با «مرجع + درصد حاشیه سود» برای همه ردیف‌های پیشنهاد مالی | — | ✅ کامل |

---

## 💵 BUG-030 — واحد سراسری = ریال

### چه شد؟
- **~۸۰ نقطه نمایش/برچسب** در ۲۰ فایل: هر «تومان» و پسوند پولی «ت» ⟵ **«ریال»** (سند رسمی CO از قبل IRR بود — حالا کل نرم‌افزار با سند هم‌زبان است).
- `ptfMoney` (فرمت‌کننده واحد مصوب US-416): ریالی = «ریال»، ارزی = کد ارز (بدون تغییر).
- **fx-rates.php:** نرخ‌های TGJU دیگر تقسیم بر ۱۰ نمی‌شوند — خروجی ریال با `unit:'rial'`؛ اعتبارسنجی دلار > ۱۰۰هزار ریال؛ و **کش تومانیِ قدیمی سرور خودکار نامعتبر می‌شود** تا حتی یک بار هم نرخ ۱۰برابر-کمتر نمایش داده نشود.
- طبق مصوبه، هیچ عدد ذخیره‌شده‌ای دستکاری نشد (بدون مهاجرت ×۱۰) — اعداد موجود از این پس ریال خوانده می‌شوند.

### ⚠️ اطلاع‌رسانی لازم به کاربران (لطفا ابلاغ فرمایید)
از v19.6 همه مبالغ **ریال** است. نرخ ارز داشبورد از این نسخه ~۱۰ برابر عدد قبلی (ریال) نمایش داده می‌شود — این تغییر واحد است، نه تغییر نرخ.

---

## 🔢 US-438 — جداکننده هزارگان (ماژول جدید moneyx.js)

- **کاما زنده هنگام تایپ:** `1500000` ⟵ `1,500,000` — با حفظ موقعیت مکان‌نما؛ ارقام فارسی/عربی هم پذیرفته و نرمال می‌شوند.
- **✍️ مبلغ به حروف** زیر فیلد (مصوبه: با واحد اصلی): «یک میلیون و پانصد هزار ریال» — سد دوم در برابر خطای ۱۰ برابری.
- **سراسری با یک delegation واحد:** هر ورودی `data-money` خودکار پوشش می‌گیرد — بدون اتصال دستی در فرم‌های داینامیک.
- **پوشش این نسخه:** همه فیلدهای مبلغ `ptfDialog` (هزینه پرونده، خرید واقعی، زیان، هزینه جاری، تنخواه، پیش‌پرداخت، سند اصلاحی و...) + فرم قیمت اقلام CO/TC + فاکتور و ارزش افزوده حسابدار + وصولی + قیمت خرید + چک + قیمت‌های دور۱/۲ + برآورد سرنخ + سقف اعتبار.
- موبایل: `inputmode=numeric`. **ذخیره‌سازی همیشه عدد خالص** (`ptfNum`) — هیچ رشته کاماداری وارد داده نمی‌شود.

---

## 📈 US-439 — مرجع + درصد حاشیه سود برای همه ردیف‌ها

- قبلا دکمه «سود +٪» فقط با «بهترین خرید استعلامی» ظاهر می‌شد؛ حالا هر ردیف بدون استعلام هم فیلد **«📍 مرجع (قیمت خرید — ریال)»** دارد (کامادار).
- با ورود مرجع، دکمه‌های «سود +٪» و «✏️ تنظیم ضریب» فعال می‌شوند ⟵ قیمت فروش = مرجع × (۱+٪) — قابل بازنویسی دستی.
- **📈 حاشیه فعلی** ردیف نسبت به مرجع زنده نمایش داده می‌شود (سبز/قرمز).
- **اعمال گروهی:** تنظیم ردیف ۱ روی کل اقلام — حالا ردیف‌های دارای مرجع دستی هم در آبشار لحاظ می‌شوند.
- بدون رندر کامل جدول (فوکوس نمی‌پرد — درس US-409).

---

## 🛡 اثبات شروط دائمی کارفرما

| شرط | اثبات |
|---|---|
| بدون وصله‌پینه | یک ماژول مشترک moneyx + یک delegation سراسری به‌جای وصله per-فیلد؛ فرمت‌کننده واحد ptfMoney دست‌نخورده در نقشش |
| وابستگی‌ها | ارز اسناد ارزی (US-416/412) بدون تغییر؛ تسعیر الزامی پابرجا؛ VAT فاکتور (US-436) با ptfNum سازگار شد؛ چاپ رسمی CO (IRR) از قبل درست بود |
| بدون باگ جدید | **۹۹ تستر / ۲۸۲۶ چک — صفر خطا** + سینتکس ۶۰ فایل JS + ۵ بلوک inline + توازن braces فایل PHP + audit PASS |
| بدون تداخل با طرح توسعه | ۸ تستر قدیمی نسخه-پین طبق قاعده BUG-026 نسخه‌پذیر شدند (فقط الگوی چک؛ معنا حفظ)؛ صف بک‌لاگ دست‌نخورده |

## 🧪 تست
- **tester112** (جدید): ۴۲ چک — رفتاری کامل: پارس کاما/ارقام فارسی، «یک میلیارد و پانصد میلیون»، کامای زنده با شبیه‌سازی تایپ، مرجع دستی ۱۲.۵م × ۲۰٪ = ۱۵م، آبشار گروهی، کش تومانی نامعتبر.
- به‌روزرسانی معنایی: tester78/79/92 (واحد ریال) + tester66 (ptfNum) + ۷ تستر نسخه-پین ⟵ الگوی >=.
- کل مجموعه: **۹۹ تستر / ۲۸۲۶ چک سبز**.

## 📂 فایل‌های تغییر یافته
`crm/moneyx.js` (جدید) | ۲۰ فایل برچسب ریال (offers/rbac/scoring/opex/petty/cheques/fiscal/buycompare/lossguard/shareholders/fx/analyzer/insights/kanban/leads/letters/myday/rfqsmart/salesfiles/index.html) | `crm/ui-kit.js` (ptfDialog کامادار) | `api/fx-rates.php` (ریال + کش‌گارد) | `crm/index.html` + `crm/sw.js` (v19.6) | tester112 جدید + ۱۱ تستر به‌روز

## 📤 آپلود
zip روی هاست روی نسخه موجود اکسترکت شود — **پوشه api حذف نشود**. بعد از آپدیت همه کاربران رفرش کنند (سایدبار: CRM v19.6). نرخ ارز پس از آپلود، اولین fetch را از سرور می‌گیرد (کش تومانی قبلی خودکار کنار گذاشته می‌شود).

---

## ⏭ صف بعدی (منتظر ابلاغ)
① US-428 + US-429 (پایان Post-R9 — UI زنده پیش‌پرداخت + هاب مالی مدیریتی) ② US-423 (کارت ویزیت) ③ US-410/411 ④ US-405..408 ⑤ US-402/403

**شماره‌گذاری:** بعدی US-440 | باگ بعدی BUG-031
