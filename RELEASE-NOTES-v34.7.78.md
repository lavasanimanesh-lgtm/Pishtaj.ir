# RELEASE NOTES — v34.7.78 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## رفع خطای «فایل ابری قابل دانلود گروهی یافت نشد» + هم‌راستا‌سازی فرم ثبت‌نام تامین‌کننده

### ۱. رفع پیشوند کلید در دانلود ZIP ضمایم درخواست

**مشکل:** با زدن «⬇️ دانلود همه (ZIP)» برای درخواست‌های دارای ضمیمه، پیام
«فایل ابری قابل دانلود گروهی برای این درخواست یافت نشد» نمایش داده می‌شد.

**ریشه:** فایل‌های ضمیمه با دو پیشوند متفاوت روی فضای ابری ذخیره می‌شوند:
- فرم «ثبت درخواست جدید» (CRM) → پوشهٔ `rfq/<دسته>` (مثلاً `rfq/inq/…`)؛
- ویجت «مدیریت پیوست» (بعداً) → پوشهٔ `rfqatt/…`؛
- استعلام ثبت‌شده از سایت → `site-rfq/…`.

allowlist قبلی فقط `rfqatt/` و `site-rfq/` را می‌پذیرفت و پیشوند `rfq/` (که همان
ضمایم زمانِ ثبت درخواست است) را رد می‌کرد؛ در نتیجه فهرست فایل‌های قابل‌زیپ خالی می‌ماند.

**رفع:** پیشوند `rfq/` به allowlist هم در کلاینت (`crm/inqreader.js` → `ptfRfqZipEntries`)
و هم در سرور (`api/zip-attachments.php`) اضافه شد.

### ۲. هم‌راستا‌سازی فیلدهای فرم ثبت‌نام تامین‌کننده (`supplier/index.html`)

**مشکل:** عدم تقارن ظاهری بین فیلدهای ورود — سلکت‌ها با پس‌زمینهٔ خاکستری و ظاهر
نیتیو (فلش پیش‌فرض مرورگر) در برابر ورودی‌های سفید، ارتفاع ناهمگون، و padding متفاوت
فیلد آپلود فایل.

**ریشه:** در `assets/css/style.css` یک قاعدهٔ سراسری `select { appearance:auto!important;
background:#f8fafc!important }` وجود دارد که روی همهٔ سلکت‌های سایت اثر می‌گذارد؛ در فرم
تامین‌کننده این باعث می‌شد سلکت‌ها خاکستری و نیتیو بمانند در حالی که ورودی‌ها سفید بودند.

**رفع (به‌صورت scoped و بدون دست‌زدن به قاعدهٔ سراسری):**
- سلکت‌ها: `appearance:none` + پس‌زمینهٔ سفید + فلش سفارشی SVG (سمت چپ در RTL) + ارتفاع ۵۲px
  هم‌قد ورودی‌ها.
- ورودی‌های متنی/تلفن/ایمیل و سلکت‌ها: ارتفاع یکسان ۵۲px و `box-sizing:border-box`.
- فیلد فایل: استایل یکدست و حذف `padding:10px` اینلاین ناهمگون.
- لیبل‌ها: `line-height:1.7` همسان؛ گرید دوستونه `align-items:start`.
- انتخابگرها با `.field >` مستقیم محدود شدند تا ویجت کپچا/OTP (که داخل فیلد تزریق می‌شوند)
  دست‌نخورده بمانند.

### فایل‌ها

- `crm/inqreader.js` — افزودن پیشوند `rfq/` در `ptfRfqZipEntries`.
- `api/zip-attachments.php` — allowlist شامل `rfq/`.
- `supplier/index.html` — CSS هم‌راستا‌سازی فیلدهای فرم.
- `_tools/uat/tester480-v34.7.78-supplier-form-symmetry.js` — تستر جدید (ثبت در گیت CI).

### اعتبارسنجی

- `node _tools/arch/arch-guard.js` → `=== ARCH GUARD: PASS ===`
- `node _tools/uat/run-ci-gate.js` → `96 PASS / 0 FAIL`
