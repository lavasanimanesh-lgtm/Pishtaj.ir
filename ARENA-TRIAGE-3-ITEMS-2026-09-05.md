# گزارش بررسی سه‌گانه — پیوست ثبت‌نام تامین‌کننده / حذف شدن مشتریان تازه / ارجاع فاکتور بدون تایید و بدون بازگشت

> ### ✅ وضعیت: **همهٔ راهکارهای این سند اجرا شدند — v34.37.0**
> این سند «ارزیابی» است و برای ثبتِ تاریخچهٔ ریشه‌یابی **دست‌نخورده** نگه داشته شده است.
> شرح دقیق اصلاحات، محل هر تغییر و تسترها در `RELEASE-NOTES-v34.37.0.md` آمده است.
> گیت CI: **۲۱۲ PASS / ۰ FAIL** (پیش از این کار: ۲۰۴ PASS / ۶ FAIL).
> سه تسترِ **رفتاری** تازه در گیت: `tester595` (۳۰/۰) · `tester596` (۵۷/۰) · `tester597` (۳۱/۰).

- **تاریخ:** ۲۰۲۶-۰۹-۰۵ (۱۴۰۵/۰۶/۱۴)
- **پایه بررسی:** شاخهٔ `arena/01a071a8-pishtaj-ir` روی کامیت `d1dd845` — `VERSION.json` = **CRM v34.36.4** (ولی نشانه‌های `v34.36.5` هم در کد هست؛ به بند ۴.۳ نگاه کنید)
- **نوع کار:** ارزیابی (فاز اول). اجرای راهکارها در فاز دوم و در همین شاخه انجام شد.
- **روش:** خواندن کامل زنجیرهٔ کلاینت + سرور برای هر سه مورد، اجرای تسترهای موجود مربوطه، و **بازتولید قطعیِ مورد ۲** با هارنس شبیه‌سازی خارج از ریپو (خروجی در پیوست الف).

---

## ۰) خلاصهٔ مدیریتی

| # | گزارش شما | تشخیص | شدت | ریشه در یک جمله |
|---|---|---|---|---|
| ۱ | فایل در فرم ثبت‌نام تامین‌کننده اضافه نمی‌شود | **تا حد زیادی در v34.36.2 درمان شده؛ سه نقص باقی‌مانده + یک علتِ محیطی محتمل** | P1 | مسیر پیوست **فقط فضای ابری** است؛ اگر `storage-config.php` روی میزبان نباشد/نامعتبر باشد هیچ فایلی هرگز ذخیره نمی‌شود — و در مسیر «تکراری» فایل **آپلود می‌شود ولی دور ریخته می‌شود** |
| ۲ | مشتریانی که جدیداً اضافه می‌شوند پاک می‌شوند | **باگ واقعی و بازتولیدشده — نه توهم UI** | **P0** | سنگ‌قبرِ (tombstone) حذفِ مشتری **دائمی** است و فقط با `cd` کار می‌کند، در حالی که مولد کد مشتری در کلاینت **همان کد را دوباره صادر می‌کند** ⇒ مشتری تازه در اولین سینک پاک می‌شود |
| ۳ | ارجاع فاکتور بدون تایید کاربر و بدون امکان بازگشت | **تأیید می‌شود — دقیقاً همان‌طور که گفتید** | **P0** (ریسک مالی/مالیاتی) | `sfInvoiceRef` بدون هیچ دیالوگ تاییدی مستقیم commit می‌کند؛ `invRef` فقط **نوشته** می‌شود و هیچ‌جا پاک نمی‌شود؛ تنها فرمان سروریِ توانا به پاک کردنش (`revoke_orphan_delete`) وقتی پرونده وجود دارد **همیشه** ۴۰۹ می‌دهد |

نکتهٔ فرآیندیِ مشترک هر سه مورد: **تسترهای گیت، رشته‌ای/الگویی هستند نه رفتاری.** `tester589` (۸۰/۰) و `tester594` (همه سبز) هر دو همین الان سبزند در حالی که هر سه باگ زنده‌اند. هر اصلاحی که برای این سه مورد نوشته شود باید تستر **رفتاری** داشته باشد وگرنه دوباره برمی‌گردد.

---

## ۱) مورد اول — پیوست فرم ثبت‌نام تامین‌کننده

### ۱.۱ آنچه قبلاً درست شده (تا اشتباه دوباره کار نکنیم)

نسخهٔ `v34.36.2` (سند `ARENA-SUPPLIER-UPLOAD-AND-AI-JSON-RCA-2026-09-04.md`) «موفقیتِ سبزِ بی‌صدا» را واقعاً بسته است و کد آن **در همین شاخه موجود است**:

| قابلیت | محل |
|---|---|
| اعلام نام/حجم فایل به سرور پیش از ارسال | `supplier/index.html:476-478` |
| رسید واقعی پیوست / علت دقیق + دکمهٔ «ارسال دوبارهٔ فایل» | `supplier/index.html:497-499`, `524-536` |
| سلسله‌مراتب علت‌یابی `$_FILES` خالی | `api/crm.php:1261-1290` (`save_attachment`) |
| تشخیص محدودیت‌های میزبان بدون افشای راز | `api/crm.php:1226-1242` (`ptf_upload_limits_diag`) |
| مسیر بازیابی پیوست روی رکورد موجود | `api/crm.php:1663-1690` (SUP-ATTACH-RECOVERY) |
| مرتب‌سازی نزولیِ صندوق ورودی | `api/crm.php:1244-1258` + `crm/bridge.js` |

`node _tools/uat/tester589-v34.36.2-supplier-upload-receipt.js` → **PASS 80 / FAIL 0**.

بنابراین اگر امروز باز هم «فایل اضافه نمی‌شود»، **دیگر بی‌صدا نیست** — علت باید در یکی از این دو جا نوشته شده باشد و اولین کاری که باید بکنید خواندن آن است:

1. در CRM، روی ردیف همان ثبت‌نام، نشان **«⚠️ پیوست نرسید»** و در مودال بلوکِ علت + محدودیت‌های میزبان.
2. در خودِ صفحهٔ ثبت‌نام، جعبهٔ قرمز `#venAttach` با متن علت.

### ۱.۲ علت شمارهٔ ۱ (محتمل‌ترین): پیکربندی فضای ابری روی میزبان

مسیر پیوست **صددرصد ابری** است و **هیچ fallback محلی ندارد**:

```php
// api/crm.php:1348-1349
$key = ptf_storage_object_key('site-' . $prefix, $original);
$put = ptf_storage_put_uploaded_file($_FILES[$field]['tmp_name'], $key);
```

و در `api/storage-lib.php:68-74`:

```php
if (!function_exists('curl_init')) return ['ok'=>false,'error'=>'افزونه cURL روی هاست فعال نیست'];
$cfg = ptf_storage_normalize_cfg(ptf_storage_load_cfg());
if (!$cfg) return ['ok'=>false,'error'=>'پیکربندی فضای ابری یافت نشد'];
```

`ptf_storage_load_cfg` فقط سه مسیر را می‌گردد (`storage-lib.php:11-24`): والدِ والدِ `api/`، والدِ سومِ `api/`، و ریشهٔ سایت. در ریپو **فقط `storage-config.sample.php`** وجود دارد (`git ls-files` تایید شد) — یعنی فایل واقعی باید دستی روی میزبان ساخته شده باشد و **در هیچ دیپلوی‌ای کپی نمی‌شود**.

> **آزمون قطعی (۲ دقیقه، بدون کد):** یک ثبت‌نام آزمایشی با فایل بزنید و در مودال همان رکورد در CRM ببینید `storageConfigReady` چیست. اگر `false` بود، علت همین است و هیچ تغییری در کد لازم نیست — فقط `storage-config.php` باید ساخته/اصلاح شود. اگر `true` بود ولی `storageError` پر بود (مثلاً HTTP 403)، کلید/باکت/ریجن اشتباه است.

### ۱.۳ نقص‌های واقعیِ باقی‌مانده در کد (کشف این بررسی)

**D1 — فایل آپلود می‌شود، بعد دور ریخته می‌شود (P1، هم اتلاف هزینه هم اتلاف مدرک).**
ترتیب اجرا در `add_supplier` این است:

```
1619  $attachment = save_attachment(...)   ← فایل همین‌جا واقعاً روی فضای ابری نوشته می‌شود
...
1663  شاخهٔ SUP-ATTACH-RECOVERY
1692  if ($dupFound) { echo 'duplicate'; break; }   ← پاسخ ok:false، هیچ‌کس به $attachment اشاره نمی‌کند
```

یعنی اگر شرکت قبلاً ثبت شده باشد (مثلاً در فهرست تاییدشدهٔ `ptf_crm_suppliers`)، آبجکت روی فضای ابری **نوشته شده** ولی در هیچ رکوردی ثبت نمی‌شود: یک آبجکتِ یتیمِ پولی روی آروان، و از دید تامین‌کننده «فایلم رفت و هیچ‌جا نیست». این دقیقاً یکی از تجربه‌هایی است که «فایل اضافه نمی‌شود» گزارش می‌شود.

**D2 — درِ بازیابی پیوست برای دو مورد از سه مسیرِ تشخیصِ تکراری بسته است (P1).**
شرط ورود به SUP-ATTACH-RECOVERY (`api/crm.php:1670-1673`):

```php
$dupFound['list'] === 'suppliers'                       // ← فقط ثبت‌نام‌های سایت
&& ptf_dedup_phone($dupFound['row']['phone'] ?? '') === $supPhoneNorm
```

ولی حلقهٔ تشخیص تکراری (`1585-1600`) شماره را از **سه** فیلد می‌خواند: `phone`، `ph`، `mob`. پس اگر تطابق روی `ph` یا `mob` رخ داده باشد، شرطِ بازیابی (که فقط `phone` را می‌خواند) رد می‌شود ⇒ کاربر دکمهٔ «ارسال دوبارهٔ فایل» را می‌زند و باز هم `duplicate` می‌گیرد. همچنین اگر تکراری در `ptf_crm_suppliers` (فهرست تاییدشدهٔ CRM) پیدا شده باشد، `list !== 'suppliers'` است و بازیابی **هرگز** اجرا نمی‌شود.

**D3 — حلقهٔ dedup روی «اولین تطابق» می‌شکند (P2).**
`break`ها در `1587` و `1592` اولین ردیفِ هم‌نام را برمی‌دارند، نه بهترین را. اگر همان شرکت دو رکورد داشته باشد و اولی پیوست داشته باشد، شرط `empty($dupFound['row']['attachment'])` رد می‌شود و رکورد دومِ بی‌پیوست هرگز شانس بازیابی نمی‌گیرد.

**D4 — ناسازگاری فهرست فرمت‌ها (P3).**
`supplier/index.html:236` → `accept=".pdf,.doc,.docx,.xls,.xlsx,.zip,.rar"`
`supplier/index.html:365` → `ATTACH_EXT = [... 'jpg','jpeg','png','webp' ...]`
`api/crm.php:1344` → `$allowed = [... 'jpg','jpeg','png','webp' ...]`
یعنی سرور و JS عکس را می‌پذیرند ولی پنجرهٔ انتخاب فایل آن را فیلتر می‌کند. کاربری که می‌خواهد عکس کاتالوگ بفرستد «فایلم انتخاب نمی‌شود» را «آپلود نمی‌شود» گزارش می‌کند.

**D5 — هیچ ردّ سمت‌سروری پایدار نیست (P2).**
علت خطا فقط روی خودِ رکورد (`attachmentError`/`attachmentDiag`) می‌نشیند. اگر رکورد به‌خاطر `duplicate` اصلاً ساخته نشود (که دقیقاً حالت D1 است)، **هیچ‌جا هیچ ردی نمی‌ماند** و پشتیبانی نمی‌تواند تیکت را بازتولید کند. `error_log` سمت سرور برای این مسیر وجود ندارد (این مورد در پیشنهادهای دنبال‌شدهٔ RCA قبلی هم آمده بود و اجرا نشد).

### ۱.۴ راهکار پیشنهادی — مورد ۱

| اولویت | اقدام | چرا | ریسک |
|---|---|---|---|
| **۰ (همین امروز، بدون کد)** | یک ثبت‌نام آزمایشی و خواندن `storageConfigReady` / `storageError` / `post_max_size` / `content_type` در مودال CRM | تا این عدد را نبینیم هر اصلاح کد حدس است | صفر |
| **۱** | **جابه‌جایی ترتیب:** بررسی `$dupFound` **پیش از** `save_attachment` انجام شود؛ یا در شاخهٔ `duplicate` آبجکت آپلودشده حذف/الحاق شود | D1 — پایان آبجکت یتیم و «فایلم گم شد» | کم — فقط جابه‌جایی دو بلوک، قرارداد پاسخ ثابت |
| **۲** | شرط بازیابی روی **همان سه فیلدی** بررسی شود که dedup بررسی می‌کند (`phone`/`ph`/`mob`) و مسیر `ptf_crm_suppliers` هم پوشش داده شود (با همان گاردِ «پیوست موجود هرگز بازنویسی نمی‌شود») | D2 — «ارسال دوبارهٔ فایل» واقعاً کار کند | کم — گارد fail-closed دست‌نخورده می‌ماند |
| **۳** | حلقهٔ dedup به‌جای «اولین تطابق»، **رکورد بی‌پیوستِ pending** را ترجیح دهد | D3 | کم |
| **۴** | `accept` را با `ATTACH_EXT` و allowlist سرور یکی کنید (یک منبع واحد) | D4 | ناچیز |
| **۵** | یک `error_log` کوتاه (بدون داده حساس) در شاخه‌های شکست پیوست + یک صفحهٔ «سلامت آپلود میزبان» که `ptf_upload_limits_diag` را یکجا نشان دهد | D5 | ناچیز |
| **۶ (میان‌مدت)** | آپلود مستقیم به فضای ابری با **presigned URL** | `post_max_size` میزبان دیگر گلوگاه نیست؛ فایل حتی از PHP رد نمی‌شود | متوسط — نیازمند CORS باکت |

---

## ۲) مورد دوم — «مشتریانی که جدیداً اضافه می‌شوند پاک می‌شوند»

این **باگ واقعیِ از دست رفتن داده** است، نه مشکل نمایش. زنجیره را قطعی بازتولید کردم (پیوست الف).

### ۲.۱ زنجیرهٔ علت — چهار حلقه

**C1 — حذف مشتری یک «سنگ‌قبرِ دائمی» می‌سازد که فقط به `cd` نگاه می‌کند.**
`api/sales-domain.php:2597` هنگام `entity_delete`:

```php
$archive[] = ['_id'=>sd_uuid('DEL'),'kind'=>'archive_purge','collection'=>$collection,
              'id'=>$id,'cd'=>$id,'aliases'=>[$id],
              'identities'=>[$collection=>[$id]], ...];
```

این رکورد **هیچ تاریخ انقضایی ندارد** و در `ptf_crm_deleted_archive` تا ابد می‌ماند.

**C2 — این سنگ‌قبر روی *هر* رفت‌وبرگشتِ سینک اعمال می‌شود — هم رفت، هم برگشت.**

```php
api/crm.php:2245   $v = sync_apply_tombstones($k, $v, ...);   // در data_push
api/crm.php:2436   $out[$k] = sync_apply_tombstones($k, $kv, ...);   // در data_pull
```

و در بدنهٔ `sync_apply_tombstones` (`api/crm.php:781-789`) هر ردیفی که `sync_record_id_for_key` آن برابر یکی از idهای سنگ‌قبر باشد **حذف** می‌شود. یعنی رکورد نه از سرور بیرون می‌آید، نه به سرور می‌رود.

**C3 — مولد کد مشتری در کلاینت همان `cd` را دوباره صادر می‌کند.**
`crm/codegen.js:191-241` (`legacyMaxNext`) کد بعدی را از **بیشینهٔ کدهای رکوردهای زنده** می‌سازد:

```js
var arr = getData('ptf_crm_customers') || [];   // فقط ردیف‌های زنده
arr.forEach(... if (n > max) max = n ...);
var next = max + 1;
```

مشتری حذف‌شده در این فهرست نیست ⇒ **بیشینه پایین می‌آید ⇒ همان کد دوباره صادر می‌شود.** گاردِ `ptfAllocCustCode` که در `v34.36.5` اضافه شد (`crm/offers.js:3584-3594`) اینجا **هیچ کمکی نمی‌کند**، چون آن هم فقط فهرست زنده را می‌گردد:

```js
while (list.some(function (x) { return String(x.cd) === String(cd); }) && n < 8) { ... }
```

نکتهٔ مهم: مسیر «استخر کد سرور» (`api/codegen.php`) **سالم است** — `load_counters` شمارنده را فقط رو به بالا می‌برد و در `counters.json` پایدار نگه می‌دارد. مشکل فقط در **مسیر fallback کلاینت** است که هر وقت استخر محلی خالی باشد (آفلاین، بوت تازه، بعد از پاک‌سازی کش، یا هر بار که ۲۰ کد مصرف شده و refill نرسیده) فعال می‌شود. اگر `crm/data/counters.json` هم روزی گم/بازسازی شود، همان بازآفرینیِ کد از «بیشینهٔ زنده» در سرور هم رخ می‌دهد.

**C4 — تطبیقِ `aliases` زیررشته‌ای است، نه برابری.**
`api/crm.php:785`:

```php
foreach ($purgeAliases as $alias => $_)
    if (strpos((string)$encoded, (string)$alias) !== false) { $purged = true; break; }
```

`$encoded` کلِ JSON ردیف است. یعنی **هر مشتری‌ای که کدِ مشتریِ حذف‌شده در هر گوشه‌ای از رکوردش آمده باشد** (توضیحات، `healedFromRfq`، یادداشت ادغام، هر فیلد متنی) هم پاک می‌شود — حتی اگر خودش هیچ ربطی نداشته باشد. این دامنهٔ خسارت را از «یک رکورد» به «هر رکوردی که آن کد را ذکر کرده» بزرگ می‌کند.

### ۲.۲ نتیجهٔ بازتولید (هارنس، پورت وفادار همان دو تابع)

```
۱) فهرست اولیه: CUST-1001, CUST-1002, CUST-1003
۲) حذف CUST-1003 → tombstone دائمی ساخته شد. فهرست: CUST-1001, CUST-1002
۳) کد مشتری جدید = CUST-1003   (همان کد حذف‌شده؟ true )
   فهرست محلی بعد از ثبت: CUST-1003 «شرکت کاملاً جدید و بی‌ربط» | CUST-1001 | CUST-1002
۴) بعد از اولین سینک (data_push/data_pull): CUST-1001, CUST-1002
   ⇒ مشتری جدید حذف شد؟ true
۵) تطبیق زیررشته‌ای alias — رکورد CUST-1200 که فقط در توضیحاتش نام کد حذف‌شده آمده: حذف شد ❗
```

**سناریوی دقیق کاربر:** «یک مشتری را اشتباه ثبت کردم و پاکش کردم؛ بعد مشتری درست را ثبت کردم و آن هم پرید.» این دقیقاً همان چیزی است که این زنجیره تولید می‌کند و کاملاً با گزارش شما می‌خواند.

### ۲.۳ سه تشدیدکنندهٔ مستقل (هرکدام به‌تنهایی هم می‌تواند مشتری بپراند)

**A2 — `ptfEntitySaveCollection` می‌تواند سنگ‌قبرهای جعلی بسازد.**
`crm/sales-domain-v2.js:577-660` بین `base` و `nextArr` دیف می‌گیرد و برای هر `cd` که در `base` هست و در `nextArr` نیست یک **`entity_delete` واقعی** می‌فرستد (تا سقف ۴۰ عملیات). `base` به‌ترتیب از `opts.prevArr` → `window._ptfEntityLastKnown[collection]` (یک اسنپ‌شات درون‌حافظه‌ای) → `getData` گرفته می‌شود. هر جریانی که `nextArr` را از یک خواندنِ **کهنه یا ناقص** بسازد (سناریوی F11 سند `ARENA-CRM-MIGRATION-STUCK-ASSESSMENT-2026-09-04`: پر شدن localStorage ⇒ `localSet` شکست می‌خورد ولی watermark جلو می‌رود) تا ۴۰ حذفِ واقعی + ۴۰ سنگ‌قبر دائمی تولید می‌کند. آن سنگ‌قبرها بعداً سوختِ همان زنجیرهٔ C1–C3 می‌شوند.

**A3 — پاسخ ۴۰۹ سرور روی زمین می‌ماند.**
سرور در `api/sales-domain.php:2562` صریحاً می‌گوید:

```php
if (!empty($body['expectCreate']) && $found >= 0)
    sd_out(['ok'=>false,'error'=>'entity_id_exists','id'=>$id,'hint'=>'regenerate_client_code'],409);
```

ولی در کلاینت (`crm/sales-domain-v2.js:419`, `655`) این فقط به یک toast و `failDirty()` تبدیل می‌شود — **هیچ‌جا کد بازتولید نمی‌شود**، با اینکه خود سرور راهنمایی‌اش را فرستاده. رکورد فقط در `ptfSilentWrite` محلی می‌ماند و اولین pull آن را می‌شوید.

**A4 — pull فاز B برای کلیدهای فهرستی «بازنویسی خام» است.**
`crm/client-server.js:316-343`: برای `sharetx/shareholders` پروجکشن merge و برای `avatars/sigprofiles` اسمارت‌مرج هست، ولی برای `ptf_crm_customers` مستقیماً:

```js
cache[k] = { t: t, v: v, rev: incomingRev };
if (!(window.ptfBMirror && window.ptfBMirror(k, v))) localSet(k, v);
```

پس هر افزودنِ محلیِ هنوز-ارسال‌نشده، با اولین pullی که کلید مشتریان را بیاورد (چون کاربر دیگری مشتری اضافه کرده) **بی‌صدا نابود می‌شود**.

### ۲.۴ چرا تسترها این را نمی‌گیرند

`tester594-v34.36.5-cust-rfq-orphan.js` همین الان **کاملاً سبز** است، ولی همهٔ ادعاهایش از جنس `offers.indexOf('window.ptfAllocCustCode = function') > -1` است — یعنی «آیا این رشته در فایل هست؟». هیچ‌کدام رفتارِ «حذف کن، دوباره اضافه کن، سینک کن» را اجرا نمی‌کنند.

### ۲.۵ راهکار پیشنهادی — مورد ۲

| اولویت | اقدام | لایه | چرا این و نه چیز دیگر |
|---|---|---|---|
| **۱ — بندِ نجات فوری** | مولد کد مشتری/تامین‌کننده/سرنخ **کدهای سنگ‌قبرشده را هم رزرو‌شده بداند**: کد نامزد را علاوه بر فهرست زنده، در برابر `ptf_crm_deleted_archive` هم بررسی کند و در صورت برخورد جلو برود | کلاینت (`crm/codegen.js` + `ptfAllocCustCode`) | کوچک‌ترین تغییر با بیشترین اثر — بلافاصله زنجیرهٔ C3 را می‌شکند |
| **۲ — درمان ریشه‌ای** | کد کلاینتی از تصمیمِ «هویت رکورد» جدا شود: هر رکورد یک `_id` **یکتای غیرقابل بازتولید** (UUID) بگیرد و `sync_record_id_for_key` و سنگ‌قبرها فقط با آن کار کنند؛ `cd` صرفاً یک شمارهٔ نمایشی بماند | سرور + کلاینت | تا وقتی هویت = شمارهٔ قابل بازتولید است، هر درمانی چسب است. `sync_record_id_for_key` همین الان `_id` را اول می‌خواند ⇒ مسیر مهاجرت باز است |
| **۳** | تطبیق `aliases` از **زیررشته** به **برابری دقیق** تغییر کند (یا دست‌کم روی مرزِ فیلدِ شناسه محدود شود) | `api/crm.php:785` | C4 — پایان پاک شدن رکوردهای بی‌ربط |
| **۴** | سنگ‌قبر **تاریخ انقضا** و **دامنه** بگیرد: پس از N روز فقط برای رکوردهایی اعمال شود که `createdAt` آن‌ها از `deletedAt` سنگ‌قبر قدیمی‌تر است | `api/crm.php` | یک رکوردِ **تازه‌ساخته‌شده** هرگز نباید قربانی سنگ‌قبرِ قدیمی شود — این تنهاییِ هم مورد ۲ را می‌بندد |
| **۵** | `ptfEntitySaveCollection` وقتی `base` از `_ptfEntityLastKnown` می‌آید **حق صدور `entity_delete` نداشته باشد**؛ حذف فقط از مسیر صریحِ کاربر | `crm/sales-domain-v2.js` | A2 — قطع کارخانهٔ تولید سنگ‌قبر جعلی |
| **۶** | پاسخ `entity_id_exists` واقعاً مصرف شود: کد بازتولید و یک بار retry خودکار | `crm/sales-domain-v2.js:419` | A3 — راهنمایی سرور روی زمین نماند |
| **۷** | pull فاز B برای کلیدهای فهرستی از `ptfSmartMerge` عبور کند (همان کاری که موتور legacy می‌کند) | `crm/client-server.js:316-343` | A4 |
| **۸** | یک تستر **رفتاری** (نه رشته‌ای): حذف → افزودن → push → pull → assert که رکورد تازه زنده است | `_tools/uat` + گیت CI | بدون این، همین باگ در انتشار بعدی برمی‌گردد |

**بازیابیِ مشتریانی که تا امروز پریده‌اند:** رکوردها در `ptf_crm_deleted_archive` با `kind:'recycle'` و `snapshot` کامل موجودند (`crm/sales-domain-v2.js:1283-1290`) و مسیر `entity_restore` (`api/sales-domain.php:2617+`) سنگ‌قبرها را هم خنثی می‌کند. یعنی **«سطل بازیافت» کار می‌کند** — ولی چون رکوردهای قربانیِ این باگ اصلاً «حذف کاربرپسند» نشده‌اند، سطر `recycle` برایشان ساخته نشده و باید دستی از بک‌آپ‌های `crm/data/backups/` برگردانده شوند. **پیش از هر اصلاحی یک بک‌آپ کامل بگیرید.**

---

## ۳) مورد سوم — ارجاع فاکتور از پرونده فروش

### ۳.۱ «تایید کاربر اخذ نمی‌شود» — تایید می‌شود

`crm/salesfiles.js:759-771`:

```js
window.sfInvoiceRef = function (cd) {
  var res = sfInvoiceRefCommit(cd);       // ← commit فوری، بدون هیچ دیالوگی
  if (!res.ok) { ... alert(...); return; }
  sfInvoiceRefFinish(cd);
};
```

هیچ `confirm` / مودال / پیش‌نمایشی پیش از `sfInvoiceRefCommit` وجود ندارد. تنها دیالوگی که کاربر می‌بیند **بعد از** ثبت قطعی است:

```js
// crm/salesfiles.js:750
if (typeof smsSendSingle === 'function' && confirm('📱 پیامک اطلاع‌رسانی هم برای حسابدار ارسال شود؟'))
```

این بدترین حالت ممکن از نظر تجربهٔ کاربری است: کاربر یک دیالوگ می‌بیند و طبیعتاً فکر می‌کند **همان دیالوگِ تاییدِ ارجاع** است، در حالی که ارجاع لحظه‌ای پیش‌تر و بدون بازگشت ثبت شده و این فقط دربارهٔ پیامک است.

استثنا: دو مسیر ارزی (`sfInvoiceRefRialPrompt` و `sfInvoiceRefPickRial`) مودال دارند — ولی آن‌ها هم فقط **نرخ/سند ریالی** می‌پرسند، نه تاییدِ خودِ ارجاع. مسیر معمولِ ریالی هیچ توقفی ندارد.

### ۳.۲ «سریعاً به قسمت فاکتورها می‌رود» — مکانیزم دقیق

`invRef` یک **سیگنالِ محاسبهٔ مرحله** است:

```js
// crm/salesfiles.js:371-372
var wo = getData('ptf_crm_offers').filter(function (o) { return o.no === r.wonOffer; })[0];
if (wo && wo.invRef) up(8);
```

پس در همان لحظه:
- مرحلهٔ پرونده از ۷ به ۸ می‌پرد؛
- کارت راهنمای پرونده دکمه‌اش عوض می‌شود به `{ button: '🧾 رفتن به فاکتورها', onclick: "goPanel('inv')" }` (`crm/salesfiles.js:404`)؛
- پیشنهاد بلافاصله در پنل فاکتورها ظاهر می‌شود (`crm/rbac.js:922` → `offers.filter(o => o.invRef)`) و در پنل پیشنهادها نشان `🧾 ارجاع فاکتور` می‌گیرد (`crm/offers.js:562`)؛
- یک کارت کارتابل با `link:{panel:'inv'}` برای حسابدار ساخته می‌شود.

یعنی «رفتن به فاکتورها» یک ناوبری خودکار نیست، ولی **اثر کاملاً همان است**: پرونده از دست کاربر خارج و در کارتابل حسابدار می‌نشیند.

### ۳.۳ «امکان بازگرداندن نیست» — دقیقاً درست، و بدتر از آنچه فکر می‌کنید

**۱) در کلاینت، `invRef` فقط نوشته می‌شود و هیچ‌جا پاک نمی‌شود.** جست‌وجوی کامل روی `crm/*.js`:

```
crm/rbac.js:876       o.invRef = {...}     ← کد مرده (بند ۳.۴)
crm/salesfiles.js:733 o.invRef = {...}
```
هیچ `delete o.invRef` و هیچ `invRef = null` در کل CRM وجود ندارد.

**۲) پنل فاکتورها هیچ دکمهٔ «لغو ارجاع» ندارد.** آنچه هست: «ثبت فاکتور رسمی صادرشده»، «اصلاح اطلاعات»، «ابطال» و «حذف قطعی» — ولی همهٔ این‌ها روی **فاکتورِ صادرشده** کار می‌کنند، نه روی خودِ ارجاع. یعنی همان چیزی که شما گفتید: «درصورت اشتباه امکان بازگرداندن از قسمت فاکتورها هم وجود ندارد.»

**۳) سرور *می‌تواند* پاکش کند، ولی آن در بسته است.** تنها جایی در کل کدبیس که `invRef` را حذف می‌کند `api/sales-domain.php:2559` (فرمان `revoke_orphan_delete`) است:

```php
unset($offer['wonAt'], $offer['wonAtISO'], $offer['wonBy'],
      $offer['wonRevisionSnapshot'], $offer['invRef'], $offer['amendmentOfCaseId']);
```

اما دو خط بالاتر:

```php
foreach($cases as $c) if(is_array($c)&&sd_active($c)&&sd_case_offer_linked($c,$offer))
    $deps[]=['type'=>'case','id'=>...];
if($deps) sd_out(['ok'=>false,'error'=>'dependencies_exist','dependencies'=>$deps],409);
```

و چون **ارجاع فاکتور فقط از داخل پرونده فروش ممکن است** (تصمیم معماری v18.9، `crm/rbac.js:868-872`)، همیشه یک پروندهٔ فعال به آن پیشنهاد وصل است ⇒ این فرمان **همیشه ۴۰۹** می‌دهد. یعنی راه فرارِ ادمین از نظر ساختاری غیرقابل دسترس است.

**۴) قفل بدتر: نمی‌شود «دوباره و درست» ارجاع داد.**
`crm/salesfiles.js:705`: `if (o.invRef) return { ok:false, why:'already' };`
اگر کاربر در مودال ارزی **سند ریالیِ اشتباه** یا **نرخ تسعیر اشتباه** را انتخاب کند، `rialBasis` / `rialRate` / `rialTotal` برای همیشه قفل می‌شوند — و همین اعداد مبنای نمایش و پیش‌فرضِ مبلغ در فرم فاکتور رسمی هستند (`crm/official-invoice-v2.js:47` و `51`). این یک ریسک **مالیاتی/مودیانی** است، نه فقط UX.

**۵) مرحلهٔ پرونده هم عمداً برنمی‌گردد.** `sfRfqRecomputeAfterEvidenceChange` (`crm/salesfiles.js:436`) صراحتاً نوشته که اگر شاهدِ مرحلهٔ بعد (ارجاع/فاکتور) موجود باشد «عمداً هیچ عقب‌گردی انجام نمی‌دهد».

### ۳.۴ یافتهٔ جانبی: کد مردهٔ گمراه‌کننده

`crm/rbac.js:863-897` — تابع `refToInvoice` بعد از خط `875` (`alert(...); return;`) **هرگز به خط ۸۷۶ به بعد نمی‌رسد**. حدود ۲۰ خط منطق ارجاع + پیامک + audit آنجا مرده است. برای هر توسعه‌دهنده‌ای که فردا دنبال «کجا ارجاع ثبت می‌شود» بگردد، این یک تلهٔ تمام‌عیار است.

### ۳.۵ راهکار پیشنهادی — مورد ۳

| اولویت | اقدام | جزئیات |
|---|---|---|
| **۱** | **مودال تایید پیش از commit** در `sfInvoiceRef` | باید یک **خلاصهٔ خواندنی** نشان دهد، نه یک `confirm` خالی: نام پرونده و شمارهٔ درخواست، خریدار، پیشنهاد برنده، **سند مبنای ریالی + نرخ تسعیر + جمع ریالی**، و این جملهٔ صریح: «پس از ارجاع، این پرونده در اختیار حسابدار قرار می‌گیرد و **لغو آن نیازمند اقدام ادمین** است.» برای پیشنهاد ارزی، مودال‌های موجودِ نرخ/انتخاب سند باید **همین** مودال تایید را هم داشته باشند (نه فقط ورودی نرخ). |
| **۲** | **جدا کردن دیالوگ پیامک از مسیر تایید** | `confirm` پیامک در `sfInvoiceRefFinish` به یک دکمهٔ صریح در توست/کارت نتیجه تبدیل شود تا هرگز با تاییدِ ارجاع اشتباه گرفته نشود. |
| **۳** | **فرمان `revoke_invoice_ref` (بازگرداندن ارجاع)** | یک فرمان سروریِ مستقل، نه سوارشدن بر `revoke_orphan_delete`. گاردهای پیشنهادی (fail-closed): ① نقش ادمین/رئیس ② **هیچ فاکتور فعالی با `offerNo` این پیشنهاد ثبت نشده باشد** ③ دلیل اجباری ④ ثبت در `ptf_crm_corrections` با `beforeSnapshot` ⑤ ثبت در timeline پرونده و `audit` ⑥ پس از موفقیت، `sfStageOf` پرونده طبیعتاً به ۷ برمی‌گردد چون `invRef` رفته است. |
| **۴** | **پنجرهٔ لغو سریع** | تا وقتی حسابدار هیچ فاکتوری ثبت نکرده، دکمهٔ «↩️ لغو ارجاع» هم روی کارت پرونده و هم روی ردیف پنل فاکتورها دیده شود (با همان گارد بند ۳). این همان چیزی است که شما خواستید: «از قسمت فاکتورها هم بشود برگرداند.» |
| **۵** | **اجازهٔ اصلاح مبنای ریالی** | یا از طریق همان لغو+ارجاع مجدد، یا یک فرمان محدودِ `amend_invoice_ref` که فقط `rialBasis`/`rialRate` را با دلیل و correction عوض کند — مادام که فاکتوری صادر نشده. |
| **۶** | **حذف کد مردهٔ `refToInvoice`** | خطوط ۸۷۶ به بعد در `crm/rbac.js` پاک شوند یا تابع کلاً به یک `alert` صریح خلاصه شود. |
| **۷** | **تستر رفتاری** | «ارجاع بدون تایید ممکن نیست»، «لغو ارجاع بدون فاکتور موفق است»، «لغو ارجاع با فاکتور فعال ۴۰۹ می‌دهد»، «پس از لغو، مرحلهٔ پرونده ۷ می‌شود». |

---

## ۴) نکات عرضی که در حین بررسی دیدم (خارج از سه مورد، بدون اقدام)

**۴.۱ — بن‌بست مهاجرت فاز B هنوز باز است.** سند `ARENA-CRM-MIGRATION-STUCK-ASSESSMENT-2026-09-04.md` (P0) هیچ‌کدام از یافته‌هایش کد نگرفته است. یافتهٔ **F11** آن (پر شدن localStorage ⇒ نوشتن ناموفق ولی جلو رفتن watermark) دقیقاً یکی از تشدیدکننده‌های مورد ۲ در همین گزارش است. این دو موضوع را جدا از هم درمان نکنید.

**۴.۲ — یافتهٔ F6 همان سند** (نبودن `ptf_crm_fin_events` و `ptf_crm_personal_cheques` در `bKeys()`) هنوز پابرجاست و ریسک «ثبت شد ولی هرگز ارسال نشد» برای ژورنال مالی دارد.

**۴.۳ — ناهم‌خوانی نسخه.** `VERSION.json` می‌گوید `v34.36.4` ولی کد `crm/offers.js` و `crm/my-customers-filter.js` نشانِ `v34.36.5` دارند و `run-ci-gate.js` تستر `tester594-v34.36.5` را ثبت کرده. طبق یادداشت خودِ `VERSION.json` این فایل «تنها منبع رسمی» است؛ باید هم‌راستا شود.

**۴.۴ — `.gitignore` نامِ `storage-config.php` را ندارد.** فایل واقعی امروز در ریپو نیست، ولی اگر روزی کسی آن را در ریشهٔ سایت بسازد و کامیت بگیرد، **کلید و رمز فضای ابری وارد گیت‌هاب می‌شود**. همان‌طور که `api/ptf-secrets.php` و `api/gsc-config.php` ignore شده‌اند، این هم باید بشود (یک خط، ریسک صفر، سود امنیتی زیاد).

---

## ۵) ترتیب اجرای پیشنهادی

1. **بک‌آپ کامل** از `crm/data/` (پیش از هر تغییری — مورد ۲ باگِ از دست رفتن داده است).
2. **مورد ۱، اقدام ۰**: یک ثبت‌نام آزمایشی و خواندن `storageConfigReady`. ممکن است کل مورد ۱ بدون یک خط کد بسته شود.
3. **مورد ۲، اقدام ۱** (گاردِ کد در برابر سنگ‌قبرها) + **اقدام ۳** (برابری به‌جای زیررشته) — کوچک، کم‌ریسک، و خون‌ریزی را همین امروز بند می‌آورد.
4. **مورد ۳، اقدام ۱ و ۳** (مودال تایید + فرمان لغو ارجاع) — ریسک مالیاتی دارد و نباید معطل بماند.
5. **مورد ۱، اقدام ۱ و ۲** (ترتیب آپلود/dedup + بازیابی پیوست).
6. **مورد ۲، اقدام ۲ و ۴ تا ۷** (درمان ریشه‌ای هویت رکورد) — نیازمند برنامه‌ریزی و مهاجرت.
7. **تسترهای رفتاری** برای هر سه مورد، پیش از بستن هرکدام.

---

## پیوست الف — هارنس بازتولید مورد ۲

پورت وفادار دو تابع تصمیم‌گیرنده (`legacyMaxNext` از `crm/codegen.js:191-241` و `sync_apply_tombstones` از `api/crm.php:753-790`)، اجراشده خارج از ریپو:

```
۱) فهرست اولیه: CUST-1001, CUST-1002, CUST-1003
۲) حذف CUST-1003 → tombstone دائمی ساخته شد. فهرست: CUST-1001, CUST-1002
۳) کد مشتری جدید = CUST-1003   (همان کد حذف‌شده؟ true )
   فهرست محلی بعد از ثبت: CUST-1003 «شرکت کاملاً جدید و بی‌ربط» | CUST-1001 «الف» | CUST-1002 «ب»
۴) بعد از اولین سینک (data_push/data_pull): CUST-1001, CUST-1002
   ⇒ مشتری جدید حذف شد؟ true
۵) تطبیق زیررشته‌ای alias — رکورد CUST-1200 که فقط در توضیحاتش نام کد حذف‌شده آمده: حذف شد ❗
```

## پیوست ب — وضعیت تسترهای مرتبط در همین لحظه

| تستر | نتیجه | ولی… |
|---|---|---|
| `tester589-v34.36.2-supplier-upload-receipt.js` | PASS 80 / FAIL 0 | فقط رشته/الگو؛ D1–D5 را نمی‌بیند |
| `tester594-v34.36.5-cust-rfq-orphan.js` | همه سبز | هیچ ادعایی دربارهٔ سنگ‌قبر/سینک ندارد |
| پوششِ «ارجاع فاکتور» | — | هیچ تستری تاییدِ کاربر یا بازگشت‌پذیری را نمی‌سنجد |
