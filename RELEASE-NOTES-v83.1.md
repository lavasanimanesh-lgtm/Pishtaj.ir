# 🩹 هاتفیکس v83.1 — رفع قطعی پرش عرض فضای کاری (با تشکر از اسکرین‌شات‌های شما)

**تاریخ:** ۱۴ تیر ۱۴۰۵ | **دامنه:** US-180 (تکمیل) — مقایسه دو مستطیل قرمز شما دقیقاً مشکل را نشان داد

---

## 🔍 ریشه واقعی مشکل (که در v83.0 کامل حل نشده بود)

چیدمان CRM از نوع flex است: سایدبار سمت راست + ستون فضای کاری (`.mn`).
مشکل این بود که ستون فضای کاری **خاصیت flex نداشت** — یعنی عرضش را از «اندازه محتوای همان ماژول» می‌گرفت:

| ماژول | محتوا | نتیجه در اسکرین‌شات شما |
|---|---|---|
| داشبورد | چند کارت کوچک | مستطیل **باریک** (وسط صفحه خالی می‌ماند) |
| پیشنهادها (TO/CO) | جدول + دکمه‌ها | مستطیل **پهن‌تر** |

→ با هر جابجایی بین ماژول‌ها، عرض فضای کاری تغییر می‌کرد = همان پرشی که چشم را خسته می‌کند.

## ✅ راه‌حل v83.1

در `crm/theme.js`:

```css
.crm-l { width:100% }
.mn { flex:1 1 auto; width:calc(100% - عرض سایدبار); min-width:0 }   /* دسکتاپ */
.mn { width:calc(100% - 56px) }                                       /* موبایل */
.tb2 { overflow-x:auto }   /* جدول عریض → اسکرول داخلی، نه گشاد کردن پنل */
```

- فضای کاری حالا **همیشه کل عرض باقیمانده صفحه** (تا منتها الیه سمت چپ) را می‌گیرد — فارغ از اینکه محتوای ماژول کم باشد یا زیاد.
- جدول‌های خیلی عریض به جای گشاد کردن صفحه، **اسکرول داخلی** می‌گیرند.
- عرض دو مستطیل قرمز شما حالا در همه ماژول‌ها **دقیقاً یکسان** خواهد بود؛ فقط محتوای داخل عوض می‌شود.

## ✅ کیفیت
- تستر tester20 با ۳ چک جدید v83.1 به‌روزرسانی شد → **22/22 PASS**
- رگرسیون کامل ۲۰ تستر → همه PASS | ممیز ۸بخشی → PASS
- نسخه کش → `ptf-crm-v83.1` (کش همکاران خودکار پاک می‌شود — نیازی به Ctrl+F5 نیست)

## 📦 نصب (File Manager)
**`pishtaj-hotfix-v83.1.zip`** — فقط ۳ فایل: `crm/theme.js`، `crm/index.html`، `crm/sw.js`. روی هاست باز کنید، جایگزین شود، تمام. **داده‌ای پاک نمی‌شود.**

> 🧪 تست: بین «داشبورد» و «پیشنهادها (TO/CO)» چند بار جابجا شوید — کادر فضای کاری باید عرض کاملاً ثابت داشته باشد و تا انتهای سمت چپ صفحه کشیده شود.
