# راهنمای فعال‌سازی پنل «سرچ کنسول» در CRM (فارسی، گام‌به‌گام)

**زمان لازم:** حدود ۱۰ دقیقه · **یک‌بار انجام می‌شود** · **نیاز:** دسترسی به حساب گوگلی که مالکیت `pishtaj.ir` را دارد

> بعد از این کار، در CRM یک منوی «📈 سرچ کنسول» خواهید داشت که دادهٔ **زنده و واقعیِ گوگل**
> را نشان می‌دهد: نمایش، کلیک، CTR، جایگاه، کوئری‌ها، صفحات، روند روزانه،
> فرصت‌های سریع، و بررسیِ وضعیتِ ایندکسِ هر صفحه.

---

## مرحله ۱ — ساخت Service Account در گوگل کلود

1. به <https://console.cloud.google.com/projectcreate> بروید و پروژه‌ای با نام
   دلخواه (مثلاً `ptf-seo`) بسازید.
2. از منوی بالا پروژه را انتخاب کنید → **APIs & Services → Library**
   → عبارت **Search Console API** را جستجو کنید → **Enable**.
3. **APIs & Services → Credentials** → **Create credentials** → **Service account**
   - نام: `ptf-gsc` → **Create and continue**
   - نقش (Role) لازم نیست؛ خالی بگذارید → **Done**
4. روی سرویس‌اکانتِ ساخته‌شده کلیک کنید → تب **Keys** → **Add key → Create new key**
   - نوع: **JSON** → **Create**
   - یک فایل `.json` دانلود می‌شود. آن را باز کنید؛ دو مقدار لازم دارید:
     - `client_email` (مثل `ptf-gsc@ptf-seo.iam.gserviceaccount.com`)
     - `private_key` (رشته‌ای بلند که با `-----BEGIN PRIVATE KEY-----` شروع می‌شود)

---

## مرحله ۲ — دادنِ اجازه در سرچ کنسول (مهم‌ترین مرحله)

1. به <https://search.google.com/search-console> بروید و Property مربوط به
   `pishtaj.ir` را انتخاب کنید.
2. **Settings** (چرخ‌دنده پایینِ سمت چپ) → **Users and permissions** → **Add user**
3. ایمیلِ `client_email` را وارد کنید و سطح دسترسی را **Restricted** بگذارید → **Add**

> ⚠️ بدون این مرحله، پنل پیام «403 Forbidden» می‌گیرد. دلیلش این است که
> سرویس‌اکانت به‌خودی‌خود به هیچ سایتی دسترسی ندارد تا وقتی شما آن را دعوت کنید.

---

## مرحله ۳ — قرار دادن تنظیمات روی هاست

1. در مخزن، فایل `api/gsc-config.sample.php` را کپی کنید با نام `api/gsc-config.php`.
2. دو مقدار `client_email` و `private_key` را از فایل JSON مرحلهٔ ۱ وارد کنید.
3. فایل را روی هاست در مسیر `api/` قرار دهید (کنار `api/cms.php`).

> 🔒 این فایل در `.gitignore` ثبت شده و هرگز در گیت کامیت نمی‌شود؛
> فقط روی هاست می‌ماند. اگر بعداً کلید را عوض کردید، کافی است همین فایل را
> روی هاست به‌روزرسانی کنید.

---

## مرحله ۴ — استفاده

1. وارد CRM شوید → منوی **📈 سرچ کنسول** (فقط برای مدیران ارشد فعال است).
2. بازهٔ زمانی را انتخاب کنید (۲۸ روز / ۹۰ روز / ۶ ماه) و روی **⟳ به‌روزرسانی** بزنید.
3. داده هر ۳۰ دقیقه کش می‌شود تا هم سریع باشد و هم به سهمیهٔ API فشار نیاید.

### چه چیزهایی را می‌بینید

| بخش | کاربرد |
|---|---|
| کارت‌های شاخص | نمایش، کلیک، CTR، تعداد کوئری، تعدادِ صفحهٔ ۱، **سهم کلیکِ برندی** |
| روند روزانه | جهتِ حرکت سایت در طول زمان |
| 🔥 فرصت‌های سریع | کوئری‌های غیربرندی با جایگاه ۶–۳۰ که با یک اصلاحِ کوچک به صفحهٔ اول می‌آیند |
| کوئری‌های پربازدید | آنچه مردم واقعاً جستجو می‌کنند (برندی‌ها برچسب دارند) |
| صفحات پربازدید | با دکمهٔ **بررسی ایندکس** که وضعیت دقیقِ هر URL را از خودِ گوگل می‌پرسد |

> دکمهٔ **بهینه‌سازی** کنار هر کوئری، شما را مستقیم به تب «سئوی صفحات» می‌برد و
> موضوع را در فیلد جستجو می‌گذارد؛ یعنی از «دیدن مشکل» تا «اصلاح آن» دو کلیک فاصله است.

---

## عیب‌یابی

| پیام | علت | راه‌حل |
|---|---|---|
| «پاسخ سرور خطا بود (احتمالاً 500)» / «عدم دسترسی» بدون جزئیات | **خطای نحوی در `api/gsc-config.php`** — هر درخواست با خطای ۵۰۰ می‌میرد و پنل پیام عمومی می‌بیند (رایج‌ترین ریشهٔ «راهکارها را زدم ولی وصل نمی‌شود») | روی هاست اجرا کنید: `php -l api/gsc-config.php` — اگر خطا داد، فایل را از نو بسازید (کلید را عیناً با `\n`ها داخل `"` دوتایی). نسخهٔ خراب خودکار با نام `gsc-config.php.broken-*` قرنطینه می‌شود. ⚠️ بازکردن فایل در مرورگر آزمون معتبری نیست (در این هاست فایل PHP بدون خروجی ۵۰۰ می‌دهد) |
| `gsc_config_broken` / `gsc_config_incomplete` | فایل تنظیمات خراب یا ناقص است | پیامِ همراهش را بخوانید؛ فایل را مطابق نمونه بازسازی و با `php -l` آزمایش کنید |
| `permission_denied: دسترسی … فقط برای نقش‌های مدیر ارشد` | نقشِ کاربرِ واردشده در CRM جزو چهار نقش مجاز نیست | با نقش `admin` یا `chairman` یا `ceo` یا `commercial` وارد شوید |
| `authentication_required: نشست … منقضی` | توکن نشست منقضی شده (۲۴ ساعت) | خروج و ورود دوباره به CRM |
| `gsc_not_configured` | فایل `api/gsc-config.php` روی هاست نیست | مرحلهٔ ۳ |
| `private_key_invalid` | کلید ناقص کپی شده ( `\n`ها خراب شده‌اند) | کلید را دقیقاً از فایل JSON کپی کنید |
| `api_error_403` | ایمیلِ سرویس‌اکانت در GSC اضافه نشده | مرحلهٔ ۲ |
| `openssl_missing` / `curl_missing` | افزونهٔ موردنیاز روی هاست غیرفعال است | از هاستینگ بخواهید `openssl` و `curl` را فعال کند |
| `token_failed (invalid_scope)` | API در گوگل کلود فعال نیست | مرحلهٔ ۱، گام ۲ |
| `property_not_found` (فهرستِ قابل‌دسترسی: **خالی**) | گوگل می‌گوید سرویس‌اکانت به «هیچ» پراپرتی‌ای دسترسی ندارد — یعنی افزودنِ Full در جایِ درست اعمال نشده | ۴ علت رایج زیر + دکمهٔ «🧪 آزمون اتصال GSC» در تب سئو |
| `property_not_found` (فهرست، پراپرتیِ دیگر را نشان می‌دهد) | Full روی پراپرتی اشتباه داده شده (مثل staging یا اکانت دیگر) | همان ایمیل را روی پراپرتی خودِ `pishtaj.ir` (Domain یا URL-prefix) هم با Full اضافه کنید |

---

## مسیر جایگزین (اگر فعلاً نمی‌خواهید API وصل کنید)

همان خروجیِ دستی را بگیرید و با ابزارِ مخزن تحلیل کنید:

```bash
python3 _tools/gsc_import.py ~/Downloads/Queries.csv ~/Downloads/Pages.csv --days 90
```

خروجی: `_audit/GSC-ANALYSIS-<تاریخ>.md` + ثبت روند در `_audit/GSC-HISTORY.csv`.

---

## 🧪 آزمون اتصال GSC (v34.29.1) — علتِ دقیقِ «ثبت نقشه ناموفق» در ۱۰ ثانیه

مسیر: CRM ← مدیریت سایت ← تب «سئوی صفحات» ← دکمهٔ **🧪 آزمون اتصال GSC**
(همین دکمه در پیام خطای «📤 سایت‌مپ + سرچ کنسول» هم ظاهر می‌شود.)

این آزمون مستقیم از گوگل می‌پرسد و نشان می‌دهد:

1. **ایمیل سرویس‌اکانت** با دکمهٔ 📋 کپی — عیناً همان چیزی که باید در سرچ کنسول اضافه شود؛
2. **فهرست زندهٔ پراپرتی‌های قابل‌دسترسی** با سطح هرکدام (Full/Owner سبز، Restricted کهربایی)؛
3. **تشخیص علت**: اتصال سالم / بدونِ دسترسی / سطح کم / کلید خراب / بی‌تنظیم — هر کدام با راه‌حل گام‌به‌گام.

### ۴ علت رایجِ «Full دادم ولی باز فهرست خالی است»

| # | علت | راه‌حل |
|---|---|---|
| ۱ | دسترسی در **Google Cloud / IAM** داده شده (نقش Owner/Editor در پروژه) — این جای متفاوتی است! | باید در **خودِ Search Console** ← Settings ← Users and permissions ← Add user اضافه شود |
| ۲ | ایمیل ناقص/غلط تایپ شده | از پنل آزمون، دکمهٔ 📋 کپی کنید و دوباره Add user بزنید (پایانِ درست: `...@…​.iam.gserviceaccount.com`) |
| ۳ | روی پراپرتیِ دیگری اضافه شده (مثلاً staging یا پراپرتیِ اکانت دوم) | همان ایمیل را روی پراپرتی `pishtaj.ir` هم Full بدهید |
| ۴ | فقط از مسیر **Owners/Verification** اضافه شده (مالکِ تفویضی) — باگ شناخته‌شده: فهرست سایت‌ها باز هم خالی می‌ماند | یک‌بار هم از مسیر Users and permissions به‌عنوان **User با سطح Full** اضافه کنید |

> نکته: برای ثبت نقشه (Search Console API) سطح **Full** کافی است؛ «Owner» لازم نیست.
> اعمالِ تغییرات معمولاً تا ۱–۲ دقیقه طول می‌کشد؛ سپس دوباره آزمون را بزنید.
