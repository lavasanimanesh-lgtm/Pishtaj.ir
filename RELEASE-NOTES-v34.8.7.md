# یادداشت انتشار CRM v34.8.7

تاریخ: ۱۴۰۵/۰۶/۴ (۲۰۲۶-۰۸-۲۶)

## ریشه‌یابی نوار زرد دائمی همگام‌سازی («یک تغییر به سرور نرسیده است…»)

### نشانهٔ گزارش‌شده توسط کارفرما
ورود با نقش مدیر بازرگانی در مرورگر جدید؛ نوار زرد همگام‌سازی هرگز سبز نمی‌شود؛ اجرای دستی «همگرایی» بی‌اثر است. تشخیص همگام‌سازی:
- کلیدهای dirty: `ptf_crm_avatars`، `ptf_crm_audit`
- همان کلیدها در صف فاز B
- توکن نشست موجود؛ خطای حافظهٔ محلی نیست

### ریشه
۱) این دو کلید، **پرنویس‌ترین کلیدهای مشترک** سیستم‌اند — هر کاربر آنلاین به audit می‌نویسد و آواتارها map مشترک است. با چند کاربر هم‌زمان، سرور هر push با base قدیمی را `conflicts` برمی‌گرداند.
۲) مسیر **فاز B** (که از v34.8.6 مالک唯一 انتزام است) برخلاف مسیر legacy (US-384) هیچ «ادغام هوشمند + ارسال مجدد» نداشت: کلید conflicted صرفاً pending علامت می‌خورد و **برای همیشه dirty می‌ماند** — دقیقاً همان نوار زرد دائمی و بی‌اثر بودن همگرایی دستی.
۳) ریزنقص فرعی: سپر داده-صفر می‌توانست آواتار خالی (`{}`) یک مرورگر تازه را وقتی سرور پر است رد کند.

### رفع‌ها
- **سرور (`api/crm.php`):** `ptf_crm_audit` و `ptf_crm_avatars` سمت سرور **union-merge** می‌شوند — audit به‌مثابه لاگ append-only (اجماع ردیف‌ها با سقف ۴۰۰۰) و avatars به‌مثابه map (incoming برای هر کلید برنده، آواتار سایر کاربران حفظ). این دو کلید از بررسی base-conflict عبور می‌کنند؛ تعارض برای آنها از اساس بی‌معناست چون merge هیچ نویسنده‌ای را حذف نمی‌کند و ACK صادقانه است. سپر داده-صفر نیز دیگر این کلیدها را رد نمی‌کند (incoming خالی = حفظ ردیف‌های سرور، نه پاک‌سازی).
- **کلاینت (`crm/client-server.js` + `crm/sync.js`):** مسیر فاز B صاحب «نجات تعارض» شد — پاسخ هر دسته `serverData/krevs` را aggregate می‌کند؛ برای کلید conflicted غیرمالی، مقدار سرور با همان منطق US-384 مسیر legacy (ptfSmartMerge + tombstones، از طریق `ptfSyncResolveConflictFromServer`) merge محلی می‌شود، watermark کلید از krevs پاسخ تازه می‌شود و flush مجدد (حداکثر ۳ نوبت در هر چرخه) همان کلید را با base درست می‌فرستد. کلیدهای مالی محافظت‌شده همچنان فقط از مسیر اختصاصی خودشان.
- **نسخه:** bump واقعی به v34.8.7 در تمام نقاط قرارداد (VERSION.json، SW، manifest، query همهٔ اسکریپت‌ها، SD_SERVICE_VERSION).

### چرا دائمی است
از این پس برای این دو کلید تعارض ساختاراً ممکن نیست (سرور همیشه merge می‌کند) و برای هر کلید دیگری که آیندهاً conflict بگیرد، مسیر فاز B دیگر بن‌بست ندارد و خودش همگرا می‌شود. حالت «dirty برای همیشه» از هر دو مسیر حذف شده است.

### کنترل کیفیت
- tester جدید `tester513-v34.8.7-shared-key-convergence.js` با شبیه‌سازی رفتاری union/rescue.
- گیت CI: **129 PASS / 0 FAIL** (شامل php-parse همهٔ api/*.php).
