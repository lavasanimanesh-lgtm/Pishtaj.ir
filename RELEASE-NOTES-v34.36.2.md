# RELEASE NOTES — v34.36.2 (رسیدِ پیوستِ تامین‌کننده + JSONِ مقاومِ AI در مدیریت سایت)

**تاریخ:** ۲۰۲۶-۰۹-۰۴ · **گیت:** سبز · **تسترهای جدید:** `tester589-v34.36.2-supplier-upload-receipt.js` · `tester590-v34.36.2-ai-json-repair.js`
**اسناد:** `ARENA-SUPPLIER-UPLOAD-AND-AI-JSON-RCA-2026-09-04.md` (ریشه‌یابی کامل با شاهدِ کد و دستورِ تأیید روی میزبانِ زنده)
**نکتهٔ بامپ:** جاروی استاندارد نسخه در هر دو شکلِ `v34.36.1` و `34.36.1` و شکلِ فرارِ regex (`34\.36\.1`) — شامل `api/sales-domain.php` و `crm/manifest.json` که در بامپ‌های دستی جا می‌مانند.

---

## گزارش کارفرما

> ۱) «در صفحهٔ ثبت‌نام تامین‌کنندگان، چرا فایل آپلود نمی‌شود؟»
> ۲) «در مدیریت سایت چرا این پیام می‌آید وقتی می‌خواهم با هوش مصنوعی کاری انجام دهم: **خروجی AI ساختار JSON معتبر ندارد (پاسخ قابل تجزیه نبود)**»

نشانهٔ تأییدشدهٔ پروندهٔ اول (پرسش از کارفرما): فرم **سبز** می‌شود و هیچ هشداری نمی‌دهد، ولی فایل در CRM دیده نمی‌شود؛ حجم فایل زیر ۲ مگابایت. یعنی مسیرِ «سقف حجم» علتِ اصلی نیست.
دامنهٔ کار به درخواست کارفرما: **رفع ریشه‌ای هر دو مورد**.

---

## پروندهٔ ۱ — پیوستِ تامین‌کننده

### ریشه‌یابی (RCA): دو حلقهٔ مستقل که با هم «آپلود نشد» می‌سازند

| کد | حلقه | چه می‌شد (شاهدِ کدِ پیش از اصلاح) |
|----|------|----------|
| **R1** | **دیده‌نشیدنِ رکوردِ تازه** | `get_inbox` در `api/crm.php` تامین‌کنندگان سایت را **به همان ترتیبِ ثبت** (قدیمی‌ترین اول — چون سرور `suppliers[] = $row` به انتها اضافه می‌کند) برمی‌گرداند و `bridge.js` هم `slice(0, SITE_SUP_PAGE=50)` می‌گرفت ⇒ ثبت‌نامِ همین لحظه، **آخرین** ردیف از فهرستی تا ۹۹۹ ردیف بود و در صفحهٔ ۵۰تاییِ «در انتظار» هرگز دیده نمی‌شد؛ مدیر باید پله‌پله «نمایش بیشتر» می‌زد. استعلامِ RFQ در همان تابع `ORDER BY time DESC` داشت ⇒ تناقضِ رفتار، نشانهٔ باگ. **فایل در بسیاری از موارد آپلود شده بود، فقط دیده نمی‌شد.** |
| **R2** | **بی‌صدا `null` شدنِ آپلود** | `save_attachment()` در `HEAD` برای همهٔ خطاهای «دیده‌شدنی» (`UPLOAD_ERR_*`، حجم، فرمت، شکستِ فضای ابری) `$error` پر می‌کرد، اما نخستین شاخه — `if (empty($_FILES[$field]['name'])) return null;` — **بدونِ هیچ `$error`** برمی‌گشت. یعنی وقتی میزبان فایل را می‌خورد (`post_max_size` کوچک‌تر از بدنه ⇒ PHP کل `$_POST`/`$_FILES` را دور می‌ریزد، `file_uploads=Off`، یا پروکسی/CDN/فیلتر امنیتی که بخشِ multipart را می‌بُرد)، `add_supplier` بدونِ هیچ `warning` رکورد را **سبز** ذخیره می‌کرد — دقیقاً نشانهٔ گزارش‌شدهٔ کارفرما («سبز می‌شود، هشداری نمی‌دهد، فایل در CRM نیست»). |
| **R2b** | **خطا روی رکورد نمی‌ماند** | حتی وقتی `$error` پر می‌شد، فقط در `warning` همان پاسخِ گذرا بود؛ روی رکورد هیچ نشانی نمی‌ماند ⇒ در CRM «بدون ضمیمه» دیده می‌شد، بی‌آنکه معلوم باشد فایل نرسیده یا اصلاً انتخاب نشده. **هیچ لاگی** هم نوشته نمی‌شد. |
| **R2c** | **راهِ رساندنِ فایل وجود نداشت** | ارسالِ دوبارهٔ همان شرکت/شماره فقط `duplicate` می‌داد ⇒ اگر فایل بار اول نمی‌رسید، تا ابد نمی‌رسید (مسیرِ «تکمیل مدارک» تنها برای رکوردِ `rejected` با `reopen` باز بود). |
| **R3** | **رسیدِ سمت کلاینت نبود** | هر دو فرمِ RFQ (`rfq/index.html` و `index.html`) فایل را با `new FormData(form)` می‌فرستادند و `add_rfq_site` هم از پیش hard-503 داشت، ولی **نام/حجمِ فایلِ انتخاب‌شده اعلام نمی‌شد** ⇒ «میزبان فایل را خورد» از «کاربر فایلی انتخاب نکرد» قابلِ تفکیک نبود. |

### تغییرات

**۱) R1 — جدیدترین اول، در سرور و کلاینت**
- `api/crm.php`: helper جدید `ptf_rows_newest_first($rows)` (پایدار — ترتیبِ هم‌زمان حفظ می‌شود، مقایسهٔ رشته‌ایِ نزولی روی `date`، تحملِ رکوردِ غیرآرایه‌ای) و استفادهٔ آن در `get_inbox` برای **هر دو** فهرستِ suppliers و rfqs؛ سقفِ `999` بدون تغییر.
- `crm/bridge.js`: `renderSupPending` پیش از برشِ صفحه، آرایه را با **کپی** (`all.slice().sort(...)`) و با همان قراردادِ نزولیِ `date` مرتب می‌کند ⇒ حتی اگر کشِ مرورگر پاسخِ سرورِ قدیمی (ترتیبِ ثبت) را نگه داشته باشد، رکوردِ تازه در صفحهٔ اول دیده می‌شود (لایهٔ دومِ دفاع).
- سنجهٔ رفتاریِ ترتیب در تستر: «رکوردِ تازه‌تر از سقفِ صفحه بیرون نمی‌ماند».

**۲) R2 — آپلود یا علتِ دقیق؛ هرگز بی‌صدا**
- helperهای تشخیص در `api/crm.php`: `ptf_ini_bytes()` (تبدیلِ `8M/2G/-1`) · `ptf_uploads_enabled()` · `ptf_upload_error_fa($code)` (نگاشتِ هر هفت `UPLOAD_ERR_*` به پیامِ فارسیِ عملیاتی، با عددِ واقعیِ `upload_max_filesize` میزبان) · `ptf_upload_limits_diag()` (تصویرِ `file_uploads`/`upload_max_filesize`/`post_max_size`/`max_file_uploads`/`Content-Type`/`Content-Length`/شمارِ فیلدهای `$_POST` و `$_FILES` — بدونِ هیچ دادهٔ حساس).
- `save_attachment($field, $prefix, &$error = null, &$diag = null)`: شاخهٔ «`$_FILES` خالی» حالا **سلسله‌مراتبِ علت‌یابی** دارد — کدِ خطای آپلود ⇒ `file_uploads=Off` ⇒ نبودِ `multipart/form-data` در `Content-Type` (پروکسی/CDN) ⇒ بزرگ‌تر بودنِ بدنه از `post_max_size` (با عددِ MB) ⇒ خالی بودنِ کل `$_POST` ⇒ و در نهایت «فایل «X» انتخاب شده بود ولی در `$_FILES` خالی است». شکستِ فضای ابری هم `$diag` می‌گیرد (`storageConfigReady`/`curl`/`storageError`). پیام‌های حجم/فرمت حالا عدد و فهرستِ مجاز را می‌گویند. **قراردادِ بازگشت بدون تغییر** (`array|null`) و پارامترِ چهارم اختیاری ⇒ همهٔ فراخوان‌های موجود دست‌نخورده.
- `add_supplier`: `$supDeclFile` از `attachment_name`/`attachment_size` ساخته می‌شود؛ اگر فرم فایل **اعلام** کرده ولی پیوست نرسیده باشد و علتی هم تولید نشده باشد ⇒ `attachmentError = 'فایل انتخاب‌شده به سرور نرسید'` (موفقیتِ بی‌صدا ممنوع). **رسیدِ پیوست** (`key/name/size/uploadedAt`) در پاسخ برمی‌گردد و `attachmentError`/`attachmentDiag` **روی خودِ رکورد** می‌نشینند (در مسیرِ بازکردنِ رکوردِ `rejected` هم به‌روز می‌شوند؛ با رسیدنِ پیوستِ واقعی پاک می‌شوند).
- **SUP-ATTACH-RECOVERY:** اگر فایلِ جدید سالم ذخیره شده باشد، رکوردِ تکراریِ یافت‌شده `pending`/`rejected` باشد، **هیچ پیوستی نداشته باشد** (جایگزینی/بازنویسیِ فایلِ موجود ممنوع — fail-closed) و شمارهٔ تماس با `ptf_dedup_phone` یکی باشد ⇒ پیوست به **همان رکورد** اضافه می‌شود (`attachmentAddedAt`، رویدادِ `supplier_site`) و پاسخِ `attached:true` با کدِ رهگیریِ قبلی برمی‌گردد. این شاخه **پیش از** `if ($dupFound)` نشسته تا duplicate جلوی بازیابی را نگیرد.
- `supplier/index.html`: اعلامِ فایل انتخاب‌شده در FormData، **جعبهٔ رسیدِ پیوست** (`#venAttach`) با نام/حجم/کلید و حالتِ خطا، کلیدِ «ارسال دوبارهٔ فایل» (`ptfSupplierRetryAttach`) که با کدِ رهگیریِ نمایش‌داده‌شده در `#venCode` کار می‌کند، و پیش‌پروازِ نبودِ `FormData` (هشدارِ صریح به‌جای ارسالِ بی‌فایل). `#venWarning`/`#resBox`/`#sCode` دست‌نخورده.
- `crm/bridge.js`: نشانِ **«⚠️ پیوست نرسید»** روی ردیفِ تامین‌کنندهٔ دارای `attachmentError` (با `title` = علت)، بلوکِ علت در مودالِ جزئیات + `siteAttachmentDiagLine(d)` که محدودیت‌های میزبان را همان‌جا می‌نویسد، و راهنمای بازیابی برای تامین‌کننده. «بدون ضمیمه» فقط وقتی می‌ماند که واقعاً فایلی اعلام نشده باشد.
- `rfq/index.html` + `index.html`: افزودنِ `attachment_name`/`attachment_size` به FormData مسیرِ `add_rfq_site` (رسیدِ سمت کلاینت؛ hard-503 از پیش موجود بود).

**۳) سازگاریِ عمداً حفظ‌شده:** `name="attachment" accept=".pdf,.doc,.docx,.xls,.xlsx,.zip,.rar"` در فرم تامین‌کننده **کلمه‌به‌کلمه** باقی ماند (pin تستر ۴۸۰)؛ `SITE_SUP_PAGE = 50` و `'&limit=' + SITE_SUP_PAGE + '&offset=0'` بدون تغییر (pin تستر ۴۸۸)؛ `siteAttachmentHtml(s.attachment)` و متنِ «بدون ضمیمه» بدون تغییر؛ رفتارِ dedup و «تکمیل مدارک» (v34.7.71) دست‌نخورده.

---

## پروندهٔ ۲ — «خروجی AI ساختار JSON معتبر ندارد»

### ریشه‌یابی (RCA): چهار حلقه که همگی به یک پیام می‌رسند

| کد | حلقه | چه می‌شد (شاهدِ کدِ پیش از اصلاح) |
|----|------|----------|
| **C1** | **پنج اکشنِ JSON با مسیرِ تک‌ضربه و سقفِ تنگ** | `seo_alt` (۹۰۰) · `seo_intlinks` (۷۰۰) · `seo_clusters` (۱۴۰۰) · `seo_review` (۲۵۰۰) · `seo_fix` (۲۰۰۰) با `llm_call` صدا زده می‌شدند و پاسخشان مستقیم به `out_json` می‌رفت، در حالی که `crm/cms.js` از مدل فهرست‌های چند موردیِ فارسی (و برای `seo_alt` تا **۸ تصویر**) می‌خواهد ⇒ پاسخ **وسطِ JSON** بریده می‌شد و همان پیامِ کلی نمایش داده می‌شد. |
| **C2** | **مدلِ استدلالی، بودجهٔ خروجی را صرفِ «فکر کردن» می‌کند** | `api/llm.php` برای Gemini فقط `temperature`/`maxOutputTokens`/`responseMimeType` می‌فرستاد و هیچ `thinkingConfig` نداشت ⇒ در مدل‌های استدلالی (2.5x) بخشی از همان `maxOutputTokens` صرفِ reasoning می‌شود و JSON ناتمام می‌ماند — حتی با سقفِ ظاهراً کافی. |
| **C3** | **پاسخِ بریده هیچ نجاتی نداشت** | `llm_json_salvage` (از v34.20.0) فقط سه لایه داشت: `json_decode` مستقیم، استخراجِ `{`…آخرین `}`، و حذفِ کاماهای انتهایی. نه حصارِ markdown را می‌زد و نه JSONِ **ناتمام** را تعمیر می‌کرد ⇒ پیشوندِ کاملاً قابلِ استفادهٔ `[{"a":"x"},{"b":"y"` دور ریخته می‌شد. |
| **C4** | **نبودِ فارِنزیک** | `finish_reason` از پاسخِ فراهم‌کننده گرفته نمی‌شد و بدنهٔ خام هیچ‌جا ثبت نمی‌شد ⇒ «برش به‌سببِ سقفِ توکن» از «خروجیِ آزاد/غیر JSON» قابلِ تفکیک نبود و تشخیص، حدسی می‌ماند. |

### تغییرات

**۱) نردبانِ نجاتِ JSON در `api/llm.php` — از ۳ لایهٔ v34.20.0 به ۵ لایه**
- `llm_json_salvage($text, &$how)` — پنج لایه با **برچسبِ علت**: `direct` / `extract` / `trailing-comma` (+`+extract`) / `fences` (+`+extract`) / `repaired-truncated`. برچسب تا پاسخِ نهایی (`jsonSalvage`) و تا CMS می‌رود.
- helperهای جدید: `llm_json_strip_fences` (حصارِ ``` و پیشوند/پسوندِ توضیحی مدل) · `llm_json_scan` (پیمایشِ **رشته-آگاه** برای یافتنِ بازهٔ متوازن — `}` داخلِ رشتهٔ فارسی گول نمی‌زند) · `llm_json_close_tail` · `llm_json_peel` · `llm_json_repair_truncated` · `llm_json_meaningful`.
- **سیاستِ تعمیر (عمدی و مستند):** بیشینه‌محتوا، بدونِ داده‌سازی، بدونِ از دست رفتنِ بی‌صدای مقدارِ کامل.
  - اگر دمِ بریده `:"` باشد، بستن در همان‌جا مقدارِ **تهی** (`"x":""`) می‌سازد که UI همان را چاپ می‌کند ⇒ یک پله به **آخرین عضوِ کامل** برمی‌گردیم (گاردِ `preg_match($tailBad, $s) !== 1`).
  - `llm_json_meaningful($d)`: خروجیِ تعمیرشده‌ای که اعضای تهی/خالی باشد (مثلاً `[{}]` از `[{"x":`) «موفقیت» حساب **نمی‌شود** ⇒ به‌جای انتشارِ بی‌صدای متنِ خالی، خطای صادقانه.
- `llm_call_json`: همان **یک** تلاشِ مجددِ محدود (حلقهٔ بی‌پایان ندارد) با تلنگرِ فشردگی و `skip_cache`؛ سقفِ تلاشِ دوباره حالا `min(8000, max(1200, maxTok*2))` است — دو برابرِ سقف‌های بزرگ (مثل ۴۰۰۰ِ مقاله) پیش‌تر می‌توانست از حدِ مدل‌های OpenAI-compatible بیرون بزند و HTTP 400 بسازد.
- ثبتِ `finish` از پاسخِ فراهم‌کننده (`$result['finish']`): `out_json` وقتی `MAX_TOKENS`/`LENGTH` ببیند می‌گوید «پاسخ مدل به سقف توکن خورد و ناتمام ماند» و در غیر این صورت کاربر را به کنسول/`window.__ptfAiLast` ارجاع می‌دهد. کلیدِ `errorBase` = **همان عبارتِ پایهٔ pin‌شده** («خروجی AI ساختار JSON معتبر ندارد (پاسخ قابل تجزیه نبود)») حفظ شد تا متن‌های مستند/تسترها نشکنند؛ `truncated`/`finish`/`model`/`raw` (۳۰۰ کاراکتر) به پاسخ اضافه شدند.

**۲) اکشن‌ها از مسیرِ مقاوم رفتند + سقف‌های هم‌تراز**
- **۶ اکشن به `llm_call_json` آمدند:** `seo_alt` · `seo_intlinks` · `seo_clusters` · `seo_expand` · `seo_review` · `seo_fix`؛ به‌علاوهٔ ۳ اکشنی که از v34.20.0 روی آن بودند (`seo_meta`, `seo_product`, `seo_article`) ⇒ مجموعاً **۹ فراخوانی `llm_call_json`**. **۱۶ فراخوانی `llm_call`** باقی‌مانده عمداً دست‌نخورده (از جمله مسیرِ **OCR/چک/کارت ویزیت/صورت‌حساب** که قراردادِ دیگری دارند — pin تستر ۱۴۰).

| اکشن | پیش از این | اکنون |
|------|-----------|-------|
| `seo_alt` (چندتصویر) | `llm_call` ۹۰۰ | `llm_call_json` **۱۸۰۰** |
| `seo_intlinks` | `llm_call` ۷۰۰ | `llm_call_json` **۱۴۰۰** |
| `seo_clusters` | `llm_call` ۱۴۰۰ | `llm_call_json` **۲۴۰۰** |
| `seo_expand` | `llm_call` ۴۰۰۰ | `llm_call_json` **۶۰۰۰** |
| `seo_review` | `llm_call` ۲۵۰۰ | `llm_call_json` **۳۶۰۰** |
| `seo_fix` | `llm_call` ۲۰۰۰ | `llm_call_json` **۲۸۰۰** |
| `seo_meta` · `seo_product` · `seo_article` | `llm_call_json` ۹۰۰ / ۱۶۰۰ / ۴۰۰۰ | بدون تغییر |

- **gemini `thinkingConfig` (اختیاری):** `$genCfg['thinkingConfig'] = ['thinkingBudget' => (int)$cfg['gemini_thinking_budget']]` **فقط** وقتی که مالک در `llm-config.php` صراحتاً عدد گذاشته باشد؛ پیش‌فرض = رفتارِ فعلی، بدونِ فیلد (صفر ریسک برای مدل‌های قدیمی). کلید در `llm-config.sample.php` مستند شد.

**۳) فارِنزیکِ سمت کلاینت (`crm/cms.js`)**
- پوششِ جدید `cmsAiDiag(d, action)` که **همهٔ** پاسخ‌های `cmsLLM` از آن عبور می‌کنند (موفق و ناموفق، و حتی شکستِ شبکه):
  - `window.__ptfAiLast` = `{action, at, error, truncated, finish, model, http, raw}` — آخرین خطای AI برای پشتیبانی، بدونِ نیاز به بازسازی صحنه.
  - `console.error('[PTF AI] …')` با **نمونهٔ خامِ خروجی مدل**.
  - پیامِ کاربر علتِ واقعی را می‌گوید و **راهنمای عملی** می‌گیرد: برش به‌سببِ سقفِ توکن ⇒ «دوباره بزنید؛ اگر تکرار شد متن ورودی را کوتاه‌تر کنید»، JSONِ نجات‌نیافتنی ⇒ «یک بار دیگر بزنید (خروجی مدل تصادفی است)؛ نمونهٔ خام در کنسول (F12) و `window.__ptfAiLast`». راهنما فقط وقتی اضافه می‌شود که در پیام تکراری نباشد (بدونِ پیامِ دوتایی).
- چون پوشش در خودِ `cmsLLM` نشسته، همهٔ فراخوان‌ها (`cmsAiFix`، `cmsAiMeta`، ویزاردِ گروهی، `cmsPgExpand` و…) بدونِ تغییرِ تک‌تکِ مسیرها همان رفتار را گرفتند.

---

## سازگاری و ایمنی (بدونِ تغییر)

- **بدونِ تغییرِ schema/جدول/فایلِ داده:** کلیدهای تازه (`attachmentError`, `attachmentDiag`, `attachmentAddedAt`) داخلِ همان رکوردِ JSON موجود می‌نشینند و برای رکوردهای قدیمی غایب‌اند ⇒ هیچ مهاجرتِ داده‌ای لازم نیست و نمایشِ قدیمی («بدون ضمیمه») برای رکوردهای بی‌نشان دست‌نخورده می‌ماند.
- امضای `save_attachment` فقط یک پارامترِ **اختیاریِ** by-ref گرفت (`&$diag = null`) و قراردادِ بازگشت (`array|null`) همان است ⇒ همهٔ فراخوان‌های قدیمی (از جمله `add_rfq_site`) بدونِ تغییر کار می‌کنند.
- مسیرِ «تکمیل مدارک» (v34.7.71 / SUP-RESUBMIT-001) و رفتارِ dedup (`ptf_dedup_phone`) دست‌نخورده‌اند؛ شاخهٔ بازیابیِ پیوست **پیش از** `if ($dupFound)` نشسته و فقط وقتی فعال می‌شود که رکوردِ موجود **هیچ پیوستی نداشته باشد** ⇒ جایگزینی/بازنویسیِ فایلِ موجود هرگز رخ نمی‌دهد (fail-closed).
- گاردِ از دست نرفتنِ داده در merge محافظت‌شده (`sync_merge_protected_finance_snapshot`) و مسیرِ **بدونِ کد** / ADOPT انتشارِ v34.36.1 دست‌نخورده ماندند (بدونِ regression) — سنجه‌های `tester588` سبز.
- بوتِ خودکار هم‌چنان fail-closed؛ سقفِ ۸MB، allowlist نقش‌ها و سپرِ ضد داده‌صفر بدون تغییر. سقفِ ۱۵MB پیوستِ سایت هم بدون تغییر است (فقط پیامش عددِ واقعیِ فایل را می‌گوید).
- `llm_call_json` هیچ‌وقت در حلقهٔ بی‌پایان نمی‌افتد: **یک** تلاشِ مجددِ محدود با سقفِ `min(8000, max(1200, 2×))`؛ نمونهٔ خامِ برگردانده‌شده به کلاینت ۳۰۰ کاراکتر و در لایهٔ transport ۵۰۰ کاراکتر است (بدونِ نشتِ بدنهٔ کامل).
- هیچ رمز/کلیدی در `attachmentDiag` نیست — فقط تنظیماتِ عمومیِ PHP (`file_uploads`, `upload_max_filesize`, `post_max_size`, `max_file_uploads`)، هدرهای `Content-Type`/`Content-Length`، شمارِ فیلدها و برای فضای ابری فقط «پیکربندی حاضر است/نیست» + cURL + متنِ خطا.

---

## آزمون

- **`tester589` (جدید، در گیت): ۸۰ PASS / ۰ FAIL** — رفتاری + pin منبع برای پروندهٔ ۱: اجرای **واقعیِ** اسکریپتِ درونِ `supplier/index.html` در sandboxِ `vm` (رسیدِ پیوست، اعلامِ نام/حجم، مسیرِ تلاشِ دوباره)، newest-first در سرور و کلاینت (سنجهٔ «رکوردِ تازه از سقفِ صفحه بیرون نمی‌ماند»)، نگاشتِ هر هفت `UPLOAD_ERR_*` به فارسی، سلسله‌مراتبِ علت‌یابیِ «فایل اعلام‌شده ولی نرسیده» (`file_uploads` / `Content-Type` / `post_max_size` / `$_POST` خالی)، SUP-ATTACH-RECOVERY با شرطِ fail-closedِ «رکورد پیوست نداشته باشد»، نشانِ «پیوست نرسید» و خطِ تشخیص در مودال، و نبودِ نشتِ کلید/رمز در پیام‌های خطا.
- **`tester590` (جدید، در گیت): ۸۴ PASS / ۰ FAIL** — **پورتِ وفادارِ JS** از helperهای جدیدِ PHP (با pinهای برابریِ رفتار) که روی ۹ نمونهٔ نام‌دار اجرا می‌شود: ۵ پاسخِ **بریدهٔ واقعی** از خودِ اکشن‌های SEO (`seo_fix`, `seo_alt` چندتصویری, `seo_intlinks`, `seo_review`, `seo_expand`) + حصارِ markdown + پاسخِ کاملاً غیر JSON (نثر) + دمِ `:"` + خروجیِ سالم؛ به‌علاوهٔ pinهای سیاست (`llm_json_meaningful`، گاردِ دم، ترتیبِ لایه‌ها و برچسبِ `how`)، pinهای برابریِ PHP/JS (شمارِ **۹** فراخوانیِ `llm_call_json` و **۱۶** فراخوانیِ `llm_call`، سقف‌های توکن در برابرِ نیازِ `cms.js`، opt-in بودنِ `thinkingConfig`)، clampِ تلاشِ مجدد (`min(8000, max(1200, 2×))`) و نبودِ `retry` بی‌پایان.
- **pinهای به‌روز (سست‌تر نشدند):** `tester560:58` از «`llm_call` با ۹۰۰ توکن» به «`llm_call_json` با ۱۸۰۰ توکن و همان قراردادِ چندتصویرِ `$pack`» به‌روز شد، با ذکرِ علت.
- **drive-by:** `tester220` کرانِ کهنه داشت (۲۵۰۰/۱۸۰۰ کاراکتر که بر پایهٔ `bridge.js` پیش از صفحه‌بندی — ۵۵KB/۴۰.۸KB — اندازه گرفته شده بود؛ فایل اکنون ۱۷۹KB/۱۴۷KB است) ⇒ به ۹۰۰۰/۹۰۰۰ با افستِ بازاندازه‌گیری‌شده (۶۱۴۱/۲۳۵۹) به‌روز شد: ۱۷ PASS / ۰ FAIL. (خارج از گیت بود، اکنون سالم است.)
- `arch-guard`: **PASS** (بدونِ هشدارِ نسخه).
- **گیت CI: ۲۰۵ PASS / ۰ FAIL** (۲۰۳ فایلِ پیشین + ۲ تسترِ جدید).

---

## اقدام لازم پس از استقرار

1. **رفرشِ سختِ CRM** (کشِ service worker) تا `bridge.js?v=34.36.2` و `cms.js?v=34.36.2` گرفته شود.
2. اگر تامین‌کننده‌ای **در بازهٔ این باگ** ثبت‌نام کرده: در مودالِ جزئیاتِ همان تامین‌کننده، علت در بخشِ «پیوست» نوشته شده و کلیدِ **«📎 پیوست نرسید — دریافت/بارگذاری»** فایل را به **همان رکورد** (با `sup_code`) اضافه می‌کند؛ نیازی به ثبت‌نامِ دوباره نیست.
3. اگر اکشنِ AI هنوز خطا داد، پیام **علتِ واقعی** و کارِ بعدی را می‌گوید؛ نمونهٔ خامِ خروجیِ مدل در کنسولِ مرورگر (F12) و در `window.__ptfAiLast` است — همان را برای پشتیبانی بفرستید.
