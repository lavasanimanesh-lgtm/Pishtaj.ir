# 📦 ریلیزنوت v12.6 — چهار رفع: مرور کالا در مودال، درج از اولین ردیف خالی، لینک دستیار→تامین، ارجاعات

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v12.5

---

## 🐞 BUG-008 — کالاهای ثبت‌شده در مودال پیشنهاد دیده نمی‌شدند

**ریشه:** جستجوی کالا در ردیف‌های TO/CO/TC فقط با تایپ فعال می‌شد؛ با فیلد خالی هیچ چیزی نمایش نمی‌داد → کاربر نمی‌توانست ببیند چه کالاهایی موجود است.
**رفع:**
- **کلیک روی فیلد کد = حالت مرور:** ۱۲ کالای اخیر با سرتیتر «📦 کالاهای ثبت‌شده (تعداد کل)» فوراً باز می‌شود
- تایپ = فیلتر زنده؛ جستجو حالا روی **استاندارد/برند/دسته** هم انجام می‌شود (قبلاً فقط کد/شرح)
- placeholder گویا شد: «کلیک = مرور کالاها | تایپ = جستجو» + بستن خودکار دراپ‌داون با خروج از فیلد
- دکمه «+ از ماژول کالا (انتخاب چندگانه)» هم مثل قبل فهرست کامل با تیک را دارد

## 🐞 US-310 — جاگذاری از ردیف ۲ به‌جای اولین ردیف خالی

**ریشه:** فرم پیشنهاد از v12.2 یک ردیف خالی پیش‌فرض دارد؛ همه مسیرهای ورود (از درخواست/اکسل/انتخاب چندگانه/دستیار) با `push` به **انتهای لیست** اضافه می‌کردند → ردیف ۱ خالی می‌ماند و کالاها از ردیف ۲ شروع می‌شدند.
**رفع:** تابع مرکزی «درج هوشمند» — کالای جدید در **اولین ردیف فاقد کالا** می‌نشیند (هرجای لیست باشد)، وگرنه به انتها اضافه می‌شود. به هر ۵ مسیر ورود اعمال شد: بارگذاری از درخواست، ایمپورت اکسل، انتخاب چندگانه از ماژول کالا، و هر دو مسیر دستیار — در TO و CO و TC. تست رفتاری (سه سناریو شامل ردیف خالی وسط لیست) در تستر دائمی شد.

## 🐞 BUG-009 (US-311) — دستیار به استعلام تامین لینک نبود

**ریشه‌یابی دقیق:** تیک «② ایجاد استعلام تامین» در اجرای سه‌گانه دستیار، رکوردی با **ساختار غلط** می‌ساخت (`{cd, inqNo}` — ساخته ایجنت‌های v120) در حالی که ماژول «استعلام هوشمند تامین» ساختار `{no, items, targets, srcRfq}` می‌خواهد → رکورد ذخیره می‌شد ولی **هرگز در پنل تامین رندر نمی‌شد** = لینک واقعی نبود.
**رفع:**
- رکورد حالا با **سریال رسمی** `PTF-RFQS-1405-XXX`، اقلام پاک‌سازی‌شده (بدون نام کارفرما — قاعده محرمانگی) و `targets` خالی **آماده انتخاب تامین‌کننده** ساخته می‌شود
- در نتیجه اجرا، لینک مستقیم: «🤖 پیش‌نویس استعلام تامین: PTF-RFQS-... — **انتخاب تامین‌کنندگان ←**» که یک کلیک به پنل تامین می‌برد
- ثبت در audit + سازگاری «بازگردانی ۶۰ ثانیه» با ساختار جدید

یعنی سناریوی شما حالا کامل است: فایل به دستیار → شناسایی کالا و کارفرما → تیک استعلام تامین → پیش‌نویس در سامانه تامین آماده انتخاب تامین‌کننده ✅

## ✏️ US-312 — بخش ارجاعات
در موضوع اقدامِ ارجاع درخواست، گزینه «صدور پیش‌فاکتور» → **«استعلام قیمت از تامین‌کننده»**. بقیه گزینه‌ها (TO/CO/بررسی) دست‌نخورده.

## 🧪 کیفیت
| سنجه | نتیجه |
|---|---|
| تست رفتاری درج هوشمند (۳ سناریو) | ✅ داخل تستر ۴۶ دائمی شد |
| audit.py | ✅ ۰ خطا |
| تستر جدید `tester46-v126.js` | ✅ ۲۲/۲۲ PASS |
| رگرسیون کامل | ✅ **۳۳ تستر** همه PASS |

## 📁 فایل‌های تغییریافته
`crm/offerlock.js` (مرور کالا + درج هوشمند picker) · `crm/offers.js` (offSmartInsert + دو مسیر ورود) · `crm/ai-workbench.js` (ساختار درست rfqsmart + لینک + rollback + درج هوشمند) · `crm/bridge.js` (REF_ACTS) · `crm/index.html`/`sw.js` (v12.6) · `_tools/uat/tester46` (جدید)
