# RELEASE NOTES — v34.7.82 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## خود-تشخیص همگام‌سازی + تست اتصال بر پایه نشست (SYNC-DIAG-001)

### درخواست

یک کاربر گزارش می‌دهد «عدم اتصال به سرور» و اینکه بسیاری از تغییرات ثبت نمی‌شود،
در حالی که خود مدیر روی همان وای‌فای چنین مشکلی را کمتر حس می‌کند. باید تشخیص داد که
این وضعیت از نرم‌افزار است یا سیستم کاربر، **بدون ایجاد ریسک/باگ در مسیر ذخیره فعلی**.

### ریشه‌یابی

1. **تست اتصال با اکشن عمومی گمراه‌کننده بود:**
   `ptfBackupServerStatus` از `users_get` استفاده می‌کرد که در `public_actions` است و
   بدون توکن هم پاسخ 200 می‌دهد. بنابراین «✅ سرور در دسترس است» می‌توانست نمایش داده
   شود، در حالی که `data_push`/`data_pull` به‌دلیل نشست/نقش/حجم رد می‌شدند.

2. **خطاهای push/pull ثبت نمی‌شدند:**
   وقتی push/pull رد می‌شد، فقط وضعیت نوار (`warn/offline/forbidden`) عوض می‌شد و دلیل
   دقیق (401/403/413/network) برای کاربر/مدیر قابل مشاهده نبود.

### تغییرات (فقط افزودنی؛ منطق ذخیره/سینک دست نخورد)

| فایل | تغییر |
|---|---|
| `crm/sync.js` | `ptfSyncServerStatus`: تست اتصال با اکشن محافظت‌شده `data_rev` (توکن معتبر لازم است). |
| `crm/sync.js` | `noteSyncError` + `ptfSyncLastError`: ثبت آخرین خطای `push`/`pull` (scope/status/reason/detail/keys). |
| `crm/sync.js` | باکس «🔎 تشخیص همگام‌سازی»: وضعیت توکن، کلیدهای dirty، صف آفلاین فاز B، خطاهای نوشتن و آخرین خطا. |
| `crm/backup.js` | `ptfBackupServerStatus` ابتدا از `ptfSyncServerStatus` استفاده می‌کند؛ fallback قدیمی `users_get` حفظ شد. |
| `crm/backup.js` | تزریق باکس تشخیص در تنظیمات؛ refresh پس از رندر. |
| `VERSION.json` و cache-bust/تست‌ها | ارتقای نسخه به `v34.7.82` و هم‌راستا‌سازی. |

### تست

- `_tools/uat/tester484-v34.7.82-sync-diagnostics.js` جدید در گیت CI.
- گیت CI: **100 PASS / 0 FAIL**.
