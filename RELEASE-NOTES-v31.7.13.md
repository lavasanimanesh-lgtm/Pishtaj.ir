# Release Notes — v31.7.13

**تاریخ:** ۱۴۰۵/۰۴/۲۸ (2026-07-19)
**نسخه:** v31.7.13
**نوع:** 🟢 قابلیت درخواستی کارفرما — حاشیه سود کلی صورت + منحنی یادگیرنده احتمال برد (US-OFF-MARGIN)

---

## 🎯 درخواست کارفرما

پیشنهادی با ۵ قلم، هر قلم درصد سود متفاوت (۵٪، ۱۰٪، …): (۱) حاشیه سود کلی صورت — جمع قیمت خرید نسبت به جمع قیمت فروش — مشخص و **در همان ردیف فهرست پیشنهادات** نمایش داده شود؛ (۲) تحلیلگر هوشمند به‌مرور با برنده/بازنده شدن پیشنهادات، **احتمال برد بر حسب میزان حاشیه سود** را به‌صورت نمودار نشان دهد تا برای تعیین حاشیه سود قابل اتکا شود.

## ✅ قابلیت‌های اضافه‌شده

### ۱. حاشیه سود کلی صورت (`ptfOfferOverallMargin`)
- **محاسبه وزنی، نه میانگین ساده درصدها:** `(جمع فروش − جمع خرید مرجع) ÷ جمع خرید مرجع` روی اقلام دارای نرخ مرجع، با احتساب تعداد. (مثال تست‌شده: ۵ قلم با سودهای ۵/۱۰/۲۰/۱۵/۲۵٪ → حاشیه کل ۱۵٪.)
- **بج در ردیف فهرست پیشنهادات** (CO/TC، زیر مبلغ کل): «📈 حاشیه کل: ۱۵٪» سبز، زیان قرمز با ⚠️.
- **شفافیت پوشش:** اگر فقط بخشی از اقلام نرخ مرجع دارند، «(۳/۵)» نمایش داده می‌شود تا عدد، بیش از واقعیتش جدی گرفته نشود.
- **حریم قیمت خرید:** بج فقط برای نقش‌های دارای `buyPrice` — سازگار با سیاست US-347.

### ۲. منحنی احتمال برد در تحلیلگر هوشمند (`anlMarginWinCurve`)
- هر پیشنهاد بسته‌شده (برنده/بازنده) در یکی از ۷ بازه حاشیه می‌نشیند: زیر ۰٪ / ۰–۵ / ۵–۱۰ / ۱۰–۱۵ / ۱۵–۲۰ / ۲۰–۳۰ / بالای ۳۰٪.
- نمودار میله‌ای «نرخ برد هر بازه» با کد رنگ (سبز ≥۶۰٪، نارنجی ≥۳۵٪، قرمز کمتر) + شمار برد/کل هر بازه.
- **سطح اعتماد هر بازه بر اساس حجم نمونه:** کم (<۳)، متوسط (۳–۹)، بالا (≥۱۰) — تا تصمیم روی نمونه ناکافی گرفته نشود؛ دقیقاً همان «رفته‌رفته قابل اتکا شدن» که خواسته شد.
- **💡 پیشنهاد سیستم:** بهترین بازه (بیشینه نرخ برد با حداقل ۳ نمونه) با هشدار صریح در صورت کم بودن حجم کل.
- پیشنهادهای بدون نرخ مرجع از تحلیل خارج و شمارشان اعلام می‌شود (انگیزه تکمیل نرخ مرجع اقلام).

### ۳. انجماد حاشیه لحظه بسته‌شدن (`marginAtClose`)
هنگام «برنده» یا «بازنده» شدن، snapshot حاشیه همان لحظه روی پیشنهاد ذخیره می‌شود — ویرایش‌های بعدی اقلام یا تغییر نرخ مرجع کالاها، آمار تاریخی تحلیلگر را خراب نمی‌کند. سوابق قدیمی بدون snapshot از محاسبه زنده استفاده می‌کنند.

## 📁 فایل‌های تغییریافته

| فایل | تغییر |
|------|-------|
| `crm/offers.js` | helper `ptfOfferOverallMargin` + بج ردیف + snapshot برد/باخت در `offerSetSt` |
| `crm/analyzer.js` | موتور `anlMarginWinCurve` + نمودار و پیشنهاد سیستم در `renderAnalyzer` |
| `crm/index.html`, `crm/sw.js`, `crm/clear-cache.html` | نسخه ۴نقطه‌ای v31.7.13 |
| `_tools/uat/tester188-offer-margin-analytics.js` | تستر جدید (۲۰ چک) |

## 📊 نتایج گیت

- `tester188`: **۲۰/۲۰ PASS** — شامل سناریوی دقیق کارفرما (۵ قلم/۱۵٪ وزنی)، بازه‌بندی ۸۰٪/۰٪، سطوح اعتماد، skipped/باز، اولویت snapshot
- رگرسیون کامل: **166/166 فایل PASS — 3897/3897 چک PASS**
- `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| داده پیشنهادات | فقط فیلد اضافی `marginAtClose` هنگام بسته‌شدن؛ هیچ migration | صفر |
| نقش‌های بدون buyPrice | هیچ تغییری نمی‌بینند (بج و تحلیلگر مالی حسب سیاست موجود) | صفر |
| تحلیلگر | بخش جدید افزوده شد؛ قیف/پیش‌بینی قبلی دست‌نخورده | صفر |
| کیفیت آمار | وابسته به تکمیل نرخ مرجع اقلام — پوشش ناقص شفاف اعلام می‌شود | — |

## 📋 راستی‌آزمایی توسط کارفرما (۵ دقیقه)

1. یک CO با چند قلم بسازید و به هر قلم درصد سود متفاوت بدهید → ذخیره → در فهرست پیشنهادات، زیر مبلغ کل «📈 حاشیه کل» را ببینید (باید بین کمترین و بیشترین درصد اقلام و متمایل به قلم سنگین‌تر باشد).
2. چند پیشنهاد را برنده/بازنده کنید → تحلیلگر هوشمند → بخش «📈 احتمال برد بر حسب حاشیه سود کلی صورت» → میله بازه مربوطه و شمار (برد/کل) به‌روز شود.
3. یک پیشنهاد برنده را بعداً ویرایش کنید → آمار تحلیلگر نباید عوض شود (snapshot).
4. با نقش فروش (بدون buyPrice) وارد شوید → بج حاشیه در فهرست دیده نشود.

---
*توسعه‌دهنده: PTF Development Team — 2026-07-19*
