# v34.39.24 — CHQ-OPEX-EDIT-LINK: اتصال چک صادره به ماه‌های هزینه جاری از ویرایش چک (۲۰۲۶-۰۹-۲۳)

> گزارش کارفرما: «در ثبت چک صادره و یا ویرایش چک صادره برای ارتباط آن با ماه گزینه‌ای در پنجره وجود ندارد که بتوان این موضوع را اصلاح کرد.»
> زمینه: دوباره‌شماری اجارهٔ چک‌محور در خزانه — سند `ARENA-RENT-BIMONTHLY-CHEQUE-DOUBLE-COUNT-ASSESSMENT-2026-09-22.md`.
> گیت: tester679 جدید (۲۲ سنجه: ۸ رفتاری قرارداد اتصال + ۵ رفتاری رندر فرم‌ها + ۹ قرارداد متنی) در `run-ci-gate` ثبت شد.

## RCA — چرا گزینهٔ اتصال دیده نمی‌شد؟

- **ویرایش چک واقعاً هیچ فیلد اتصالی نداشت** — مودال ویرایش (`ptfChequeEditUi`) فقط هویت/مبلغ/سررسید/ذی‌نفع/بانک/مالکیت/اسناد داشت. اصلاح چکِ ثبت‌شدهٔ بدون اتصال ممکن نبود و ثبت مجدد هم به قفل دائمی صیاد (`ptfChequeReserveSayad`) می‌خورد ⇒ گپ واقعی UI، نه خطای کاربر.
- **فرم ثبت سه شرط پنهان‌کننده داشت**: ذی‌نفع = تامین‌کننده، نوع چک = ضمانت، یا نبود هیچ ردیف «بدون چک»/ماه آیندهٔ قالبی — در هر سه، بخش اتصال بدون هیچ توضیحی ناپدید می‌شد.
- پیامد مالی: چک صادره در سررسید یک‌بار از خزانه کم می‌شود؛ ردیف اجارهٔ بدون چک (دستی همان لحظه، قالبی با تسویه) هم جدا از چک کم می‌شود ⇒ **یک پول، دو خروج** در خزانه و سود نقدی.

## رفع

### `crm/opex.js` — دو API جدید (قرارداد واحد اتصال)
- `ptfOpexRowsForChequeLink(chequeCd)` → `{linked, available}` فقط‌خواندنی؛ «available» ردیف‌های حقوق سهامدار و کارمزد پوششی را ندارد (سمنتیک نقدی متفاوت).
- `ptfOpexChequeLinkRows(chequeCd, rowIds)` → همگام‌سازی دوطرفه با حداکثر یک نوشتن: تیک‌دارها وصل (`chequeCd` + `payHow:'cheque'`)، وصل‌های قبلیِ بدون تیک جدا؛ **ردیف متعلق به چک دیگر هرگز ربوده نمی‌شود** (skip + گزارش به caller). ردیف void دست‌نخورده می‌ماند. اجرای تکراری idempotent است.

### `crm/cheque-panel.js` — فرم ویرایش + فرم ثبت
- **مودال ویرایش چک**: برای چک صادرهٔ غیرضمانت و غیرشخصی، بخش «بابت کدام ماه‌های هزینه جاری؟ (اتصال چک ↔ هزینه‌ها)» اضافه شد — ردیف‌های متصل با تیکِ زده‌شده (نشان «متصل»)، ردیف‌های «ثبت‌شده و بدون چک» با تیک خالی؛ حالت خالی پیام راهنما دارد نه پنهان‌شدن. ذخیره با `ptfChequeEditSave`: جمع تیک‌ها اگر با مبلغ چک برابر نباشد فقط با تأیید صریح عبور می‌کند (اتصال ناقص بهتر از بی‌اتصالی است)؛ سپس `opexRowIds` چک + ردیف‌های opex یک‌جا همگام و audit می‌شوند؛ شکست هرگز بی‌صدا نیست (alert با علت). پس از اتصال، پنل هزینه/خزانه دوباره رندر می‌شوند.
- **فرم ثبت چک**: حالت «تامین‌کننده» دیگر بخش را بی‌توضیح پنهان نمی‌کند — راهنمای درجا می‌گوید اگر چک بابت اجاره/هزینه جاری است، ذی‌نفع را «سایر» انتخاب کنید؛ حالت خالی هم راهنمای «چرا چیزی برای اتصال نیست» دارد.

## پس از این تغییر — playbook اصلاح چک‌های گذشته (بدون مهاجرت داده)
1. ویرایش همان چک → تیک‌زدن ماه‌های اجارهٔ همان چک → ذخیره. نتیجه: ردیف‌ها از خزانه حذفِ اثر می‌شوند و وجه فقط از چک (در سررسید) شمرده می‌شود.
2. چک‌های ابطال‌شده/حذف‌شده که ردیف‌هایشان را نگه می‌دارند: با همین مسیر اتصال، «تک‌منبع بودن نقد» بازسازی می‌شود.

## نگهداری
- `VERSION.json` → v34.39.24 + همگام‌سازی همهٔ pinها (index.html/sw.js/manifest.json/cms.js/shell.js/clear-cache/device-reconnect)، `SD_SERVICE_VERSION` در `api/sales-domain.php`، و pin تسترها با `bump-version-pins.js` (۱۵۶ فایل).
- تسترهای از-قبل قرمز (وابسته به تاریخ/زمان و خارج از گیت این نسخه): tester498، tester507 — روی کد پایه (0587ddc) هم همین وضع را دارند.
