# 💥 ریلیز v18.0 — US-421 «زیان پروژه + هشدار داده ناقص سود» (R9 فاز ۴)

**اسپرینت:** R9 مالی — پس از US-418/422/419  
**استوری:** US-421 — ثبت زیان پروژه و هشدار فعال داده‌های ناقص سود

## ✅ اجرا شد

### ۱) ثبت رویداد زیان پروژه
- ماژول جدید `crm/lossguard.js` اضافه شد.
- روی پرونده فروش و بایگانی، امکان ثبت رویداد «💥 زیان پروژه» برای نقش‌های ارشد فراهم شد.
- فیلدهای رویداد زیان:
  - مبلغ زیان به تومان
  - دلیل استاندارد
  - تاریخ
  - شرح تکمیلی
  - پیوست مستندات

دلایل استاندارد:
- عدم انطباق کالا
- فسخ/لغو قرارداد
- جریمه/خسارت
- زیان ناشی از نرخ ارز
- سایر

### ۲) اتصال زیان به موتور سود واحد
- منطق اصلی سود همچنان در `ptfProjectProfitIRR` داخل `fx.js` باقی ماند.
- `lossguard.js` فقط یک wrapper تمیز روی همین موتور گذاشت.
- زیان‌های ثبت‌شده از سود پروژه کسر می‌شوند:

```text
سود نهایی = سود محاسبه‌شده موتور اصلی − زیان‌های قطعی ثبت‌شده
```

- خروجی موتور سود اکنون `lossIrr` هم دارد.
- در هشدارهای سود، زیان قطعی ثبت‌شده نمایش داده می‌شود.

### ۳) UI پرونده فروش
- در کشوی پرونده فروش دکمه «💥 ثبت زیان پروژه» اضافه شد.
- اگر پرونده زیان داشته باشد، badge «⚠️ پروژه زیان‌ده» و جمع مبلغ زیان نمایش داده می‌شود.
- هنگام مختومه‌سازی و انتقال به بایگانی، `lossEvents` همراه پرونده منتقل می‌شود.
- آمار بایگانی `stats.lossIrr` را هم نگه می‌دارد.

### ۴) UI بایگانی
- در بایگانی نیز دکمه «💥 ثبت زیان پروژه» اضافه شد.
- پرونده‌های زیان‌دار در فهرست بایگانی badge زیان‌ده می‌گیرند.
- در داخل پرونده بایگانی‌شده هم جمع زیان نمایش داده می‌شود.

### ۵) هشدار فعال داده ناقص سود
تابع جدید:

```js
ptfProfitIncompleteItems()
```

موارد ناقص را شناسایی می‌کند، از جمله:
- فروش/CO دارد ولی خرید واقعی ثبت نشده است.
- خرید ارزی بدون نرخ تسعیر پرداخت دارد.
- فروش ارزی دریافت ریالی/تسعیر کافی ندارد.
- خروجی موتور سود `ok=false` یا `complete=false` است.

### ۶) اتصال به «روز من»
اگر پروژه/پرونده‌ای داده ناقص سود داشته باشد، در «روز من» هشدار خلاصه نمایش داده می‌شود:

> چند پروژه/پرونده داده ناقص سود دارند — سود سالانه بدون تعیین تکلیف قابل اتکا نیست.

### ۷) اعلان روزانه به مدیران ارشد
تابع جدید:

```js
ptfProfitIncompleteNotify()
```

- روزی یک‌بار به نقش‌های ارشد اعلان می‌دهد.
- از اعلان تکراری در یک روز جلوگیری می‌کند.
- هشدار به کارتابل ارسال می‌شود.

## معماری
- هیچ کلید داده جدیدی برای زیان ساخته نشد.
- زیان‌ها داخل همان موجودیت‌های مرتبط ذخیره می‌شوند:
  - `ptf_crm_deals[].lossEvents`
  - `ptf_crm_projects[].lossEvents`
- بنابراین سینک/بک‌آپ/گارد فعلی همان کلیدهای موجود را پوشش می‌دهد.
- منطق سود تکرار نشد؛ فقط موتور واحد `ptfProjectProfitIRR` مصرف و wrap شد.

## فایل‌های تغییر یافته
- `crm/lossguard.js`
- `crm/salesfiles.js`
- `crm/projects.js`
- `crm/index.html`
- `crm/sw.js`
- `_tools/uat/tester98-v180.js`
- اصلاح تستر قبلی:
  - `_tools/uat/tester97-v179.js`
- `RELEASE-NOTES-v18.0.md`
- `PTF-MASTER-HANDOVER.md`

## 🔬 کیفیت
- تستر جدید: `tester98-v180.js` با ۲۲ چک.
- رگرسیون کامل: **۸۵ تستر / صفر خطا**.
- Audit سایت: PASS با همان هشدار قدیمی تصاویر سنگین.

## صف بعدی طبق برنامه R9
1. **US-420** — داشبورد سال مالی و تقسیم سود
2. سپس طبق برنامه: US-410/411 → US-405..408 → US-402/403 → US-417 فاز ۲

> یادآوری: BUG-023 + US-424 از بک‌لاگ R8 به‌دلیل اثر مالی مستقیم، پس از تکمیل R9 اولویت پیشنهادی بالایی دارند مگر کارفرما دستور دیگری بدهد.
