# 🎯 هاتفیکس v89.2 — معمای «429 بدون استفاده» حل شد!

**تاریخ:** ۱۷ تیر ۱۴۰۵ | با تشکر از اسکرین‌شات دقیق شما

---

## 🔍 ریشه مشکل (تعجب شما کاملاً بجا بود)

شما درست می‌گویید — **اصلاً استفاده نکرده بودید!** خطای 429 شما از پر شدن سهمیه نبود:

گوگل در سال ۲۰۲۶ فهرست مدل‌های رایگان را تغییر داده و **مدل `gemini-2.0-flash` که در کانفیگ ما بود دیگر در سهمیه رایگان نیست** — یعنی سهمیه این مدل برای اکانت رایگان شما «صفر» است و همان درخواست اول، بدون هیچ مصرف قبلی، 429 برمی‌گرداند (عبارت `check your plan and billing` در پیام شما دقیقاً امضای همین حالت است، نه پر شدن سهمیه). مدل‌های رایگان فعلی: **gemini-2.5-flash** و **gemini-2.5-flash-lite**.

خبر خوب: **IP هاست شما مشکلی ندارد** — اگر مشکل IP بود، خطای `location is not supported` می‌گرفتید نه 429. یعنی اتصال به گوگل برقرار است و فقط نام مدل باید عوض شود. 🎉

## ✅ راه‌حل — دو لایه

### ۱. کار شما (۳۰ ثانیه): 
فایل `llm-config.php` روی هاست را باز کنید و فقط این خط را عوض کنید:
```
'model' => 'gemini-2.0-flash',   ❌ قدیمی
'model' => 'gemini-2.5-flash',   ✅ جدید
```

### ۲. کار سیستم (از این نسخه — ضد تکرار این مشکل):
- **Fallback خودکار مدل**: اگر مدل کانفیگ‌شده 429/404 بدهد، سیستم خودش مدل‌های جایگزین رایگان (`gemini-2.5-flash` → `gemini-2.5-flash-lite`) را امتحان می‌کند — یعنی حتی اگر گوگل باز هم فهرست را عوض کند، کار شما نمی‌خوابد.
- **تشخیص دقیق‌تر در دکمه 🧪**: حالا «429 مدل غیررایگان» (سهمیه صفر) از «429 پر شدن واقعی سهمیه» تفکیک می‌شود و راه‌حل دقیق را می‌گوید.
- `llm-config.sample.php` هم به مدل جدید + `fallback_models` به‌روزرسانی شد.

## ✅ کیفیت
tester32 جدید: **9/9 PASS** | رگرسیون ۲۱ تستر همه PASS | ممیز ۸بخشی PASS | کش → `ptf-crm-v89.2`

## 📦 نصب (File Manager)
**`pishtaj-hotfix-v89.2.zip`** — `api/llm.php` + `llm-config.sample.php` + `crm/index.html` + `crm/sw.js`.
+ ویرایش یک خط `llm-config.php` روی هاست (بالا ⬆).

> 🧪 تست: بعد از هر دو کار → تنظیمات → 🤖 → «🧪 تست واقعی اتصال» → باید **«✅ اتصال واقعی برقرار است»** بیاید → سپس دکمه زرد «🟡 خلاصه AI» درخواست‌ها را بزنید → سبز می‌شوند و OCR/ترجمه/برند هم فعال می‌شود.
