# بررسی ادغام پرونده‌های تکراری «فروش تا وصول» — گزارش علت‌یابی (RCA)

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)
وضعیت: فقط بررسی و گزارش — **هیچ کدی تغییر داده نشده است**.

---

## ۱) خلاصهٔ وضعیت فعلی پروژه

- نسخهٔ رسمی CRM طبق `VERSION.json`: **`v34.7.14`** (`last_stable_tag: v2026.08.15-2108`).
- قابلیت «ادغام پروندهٔ تکراری» در سه گام پیاپی ساخته و تثبیت شده است:
  - **v34.7.7** — راهنمای گام‌به‌گام یافته‌ها + فرمان سروری `duplicate_case_plan` / `duplicate_case_merge` (بدون حذف خام؛ ادغام شواهد + انتقال ارجاعات + snapshot در آرشیو).
  - **v34.7.13** — رفع سه باگ پس از ادغام: به‌روز نشدن `krevs`، بازنشانی نشدن `ptf_crm_projects`، و نبودِ pull فوری بعد از فرمان.
  - **v34.7.14** — رفع باقیماندهٔ کش فاز B: projection قطعی فرمان باید `getData()` (کش ۳۰ثانیه)، آینهٔ IDB/حافظه و `localStorage` را هم‌زمان عوض کند و rev دقیق فرمان ملاک watermark باشد.
- فایل‌های درگیر:
  - فرانت‌اند: `crm/sales-domain-v2.js`
  - سرور: `api/sales-domain.php`
  - همگام‌سازی/کش: `crm/sync.js` و `crm/client-server.js`

> نتیجهٔ کلی: مسیر «ادغام در حالت ایدئال» در نسخهٔ فعلی از نظر منطقی سالم است (تستر `tester417-v34.7.14` آن را سبز می‌گیرد). اما ادغام با چند **گیتِ fail-closed سمت سرور** محافظت می‌شود و هرکدام می‌توانند در دادهٔ واقعی باعث شوند «دو پرونده پیدا می‌شود ولی ادغام نمی‌شود». جزئیات پایین.

---

## ۲) مکانیزم ادغام (نقشهٔ کوتاه)

1. **یافته** از دادهٔ *محلی* ساخته می‌شود: `ptfSalesIntegrityScan` (خط ۲۲۰ در `sales-domain-v2.js`) با `casesForOffer(o)` تعداد پروندهٔ متصل به یک پیشنهاد `won` را می‌شمارد؛ اگر بیشتر از یک باشد `duplicate_case` ثبت می‌شود.
2. **راهنما** (`ptfSalesFindingGuideOpen` خط ۲۸۱) فرمان فقط‌خواندنی `duplicate_case_plan` را به سرور می‌زند؛ سرور در `sd_duplicate_case_plan_data` (خط ۳۲۱) پرونده‌های مرتبط را پیدا و یک `planHash` (SHA-256 از امضای کامل داده) برمی‌گرداند.
3. **Commit** (`ptfDuplicateCaseMergeCommit` خط ۲۷۲) فرمان `duplicate_case_merge` (خط ۷۲۰ سرور) را با `planHash` و انتخاب صریح keep/remove می‌زند.
4. سرور دوباره plan را از دادهٔ جاری می‌سازد و پیش از هر تغییری این گاردها را به ترتیب بررسی می‌کند (خطوط ۷۲۹–۷۳۷):
   - `candidateCount < 2` → `duplicate_case_not_found`
   - عدم تطابق `planHash` → `duplicate_case_plan_stale`
   - اختلاف هویت `inqNo`/`buyerCd`/`currency` بین دو پرونده → `case_identity_conflict`

---

## ۳) پاسخ اصلی: چرا دو پرونده «ادغام نمی‌شوند»؟

ادغام در عمل به سه دلیل مشخص متوقف می‌شود (هر سه fail-closed و با پیام خطا به کاربر می‌رسند):

### ۳.۱) `duplicate_case_plan_stale` — احتمالاً اصلی‌ترین دلیل (خط ۷۳۰)

`planHash` از **JSON کامل تکتک پرونده‌ها** ساخته می‌شود:

```php
// api/sales-domain.php — خط 339
'recordHash'=>hash('sha256', json_encode($case, JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES))
// خط 341 — امضای هر candidate: [id, recordHash, related]
$signature['cases'][] = [$id, $candidates[count($candidates)-1]['recordHash'], $related];
```

مشکل‌ها:

- **حساس به ترتیب کلیدهاست**: `json_encode` ترتیب کلیدهای آرایهٔ PHP را حفظ می‌کند. هر بار که `ptf_crm_deals` روی سرور دوباره سریالیز شود (push از یک دستگاه دیگر، مهاجرت/repair، یا حتی یک pull عادی که آرایه را با ترتیب کلید متفاوت بازنویسی کند)، هش عوض می‌شود **بدون آنکه دادهٔ تجاری واقعاً تغییر کرده باشد**.
- **بین بازکردن راهنما و زدن دکمهٔ ادغام** هر نوشتنِ سرور روی `ptf_crm_offers` یا `ptf_crm_deals` (مثلاً push پس‌زمینهٔ خودِ همین مرورگر که dirty کلید دارد، یا کاربر دوم) هش را نامعتبر می‌کند و کاربر مدام «داده از زمان بازکردن راهنما تغییر کرده» می‌بیند.
- **نامتقارن بودن امضا**: `ptf_crm_projects` در امضای plan اصلاً نیست (فقط petty/opex/cheques/salesReturns در `$linked` هستند)، ولی خود ادغام `ptf_crm_projects` را هم بازنشانی می‌کند. یعنی یک تغییر فقط در پروژه‌ها «دیده نمی‌شود» ولی هر تغییر جزئی در خود پرونده، کل commit را از کار می‌اندازد — در حالی که منطقِ ادغام (merge صریح با keep/remove) ذاتاً می‌توانست نسبت به تغییرِ جزئی مقاوم باشد.

### ۳.۲) `case_identity_conflict` — گارد سخت‌گیرانه‌تر از لینکِ پیشنهاد (خط ۷۳۷)

```php
foreach(['inqNo','buyerCd','currency']as $identityKey){
  $a=sd_identity($keepBefore[$identityKey]??''); $b=sd_identity($source[$identityKey]??'');
  if($a!==''&&$b!==''&&$a!==$b) sd_out([... 'case_identity_conflict' ...],409);
}
```

نکتهٔ مهم: این بررسی **بین دو پرونده** سخت‌گیرانه‌تر از بررسی «لینک پرونده به پیشنهاد» است. در `sd_case_offer_linked` (خط ۲۹۲) اگر فیلد هویتیِ پیشنهاد خالی باشد، آن مقایسه رد می‌شود. بنابراین ممکن است:

- پیشنهاد `buyerCd`/`currency` خالی داشته باشد → هر دو پرونده «به پیشنهاد متصل» تشخیص داده شوند و در راهنما به‌عنوان ۲ candidate بیایند؛
- ولی خودِ دو پرونده `buyerCd` یا `currency` متفاوت داشته باشند (مثلاً یکی قدیمی/مهاجرت‌شده فقط `buyerCo` دارد، یا ارز یکی IRR و دیگری خالی/متفاوت است)؛
- نتیجه: راهنما دو پرونده نشان می‌دهد، اما commit با `case_identity_conflict` رد می‌شود و کاربر نمی‌تواند ادغام کند.

### ۳.۳) `duplicate_case_not_found` — وقتی سرور ۲ پروندهٔ مرتبط نمی‌بیند (خط ۷۲۹)

دو علت فرعی دارد:

- **الف) `rootOfferId` به‌عنوان مرجع قطعی بدون fallback** (`sd_case_offer_linked` خط ۲۹۲):

  ```php
  $oid=(string)($offer['_id']??''); $root=(string)($case['rootOfferId']??'');
  if($oid!==''&&$root!=='') return hash_equals($oid,$root); // دیگر به wonOffer/offerNo نگاه نمی‌کند
  ```

  اگر `rootOfferId` یک پروندهٔ قدیمی به `_id` پیشنهاد اشاره نکند (مثلاً `_id` پیشنهاد در مهاجرت با `sd_offer_id` دوباره تولید شده، یا دادهٔ legacy اصلاً `rootOfferId` نداشته ولی بعداً مقدار کهنه گرفته)، آن پرونده **بی‌صدا از فهرست candidates حذف می‌شود** و candidateCount کمتر از ۲ می‌ماند. در این حالت نه فقط ادغام ممکن نیست، بلکه روی کلاینت هم ممکن است یافته از بین برود.

- **ب) پیشنهاد تکراری (duplicate_offer)**: در `sd_duplicate_case_plan_data` (خط ۳۲۴) اگر برای `offerNo` بیشتر از یک رکورد پیشنهاد باشد، plan با `error='duplicate_offer_no'` و `candidates=[]` برمی‌گردد؛ تا وقتی پیشنهادِ تکراری حل نشود، ادغام پرونده‌ها نیز امکان‌پذیر نیست.

### ۳.۴) واگرایی کش محلی و دادهٔ سرور (عامل تشدیدکننده)

یافتهٔ `duplicate_case` از **دادهٔ محلی** (`getData`) ساخته می‌شود، اما ادغام در برابر **دادهٔ سرور** resolve می‌شود. در فاز B (`client-server.js`) تابع `getData` یک **کش ۳۰ ثانیه‌ای** دارد (`CACHE_TTL = 30000`). تا وقتی این کش یا projection پاسخ فرمان به‌درستی تازه نشود، ممکن است:

- کلاینت هنوز «۲ پرونده» نشان دهد در حالی که سرور قبلاً یک پرونده را ادغام/حل کرده است → commit با `duplicate_case_not_found` رد می‌شود؛
- یا برعکس، بعد از ادغامِ موفق، یافته تا انقضای کش باقی بماند.

نسخهٔ ۳۴.۷.۱۳/۳۴.۷.۱۴ دقیقاً همین را هدف گرفتند (`ptfSyncApplyServerProjection` خط ۲۵۹ و `ptfSyncAcceptServerRevision` خط ۲۸۴ در `sync.js`؛ `ptfBApplyServerProjection` خط ۱۰۵ در `client-server.js`) و برای مسیر فاز B فعال درست کار می‌کنند؛ اما وقتی **فاز B غیرفعال** است یا projection به‌خاطر گارد rev (`cur > incomingRev`) رد شود، پنجرهٔ واگرایی همچنان وجود دارد.

---

## ۴) نکات فنی صحت ادغام (برای اطمینان، بدون مشکل)

- انتقال ارجاعات مالی به `_id` و ارجاعات عملیاتی به `cd` **درست و هماهنگ با بقیهٔ سیستم** است:
  - فاکتور/دریافت/تخصیص/ضمیمه → `caseId`/`ownerId` = `$keepId` (شناسهٔ `_id`) — مطابق ساختار `register_invoice`/`post_receipt`.
  - تنخواه/هزینه جاری/چک → `dealRef`/`dealCd` = `$keepCd` (کد `cd`) — مطابق `petty.js`/`opex.js`/`cheques.js` که با `x.cd === dealRef/dealCd` لینک می‌کنند.
  - `ptf_crm_projects` → `cd/dealCd/projectNo` = `$keepCd`.
- `sd_rebuild_allocations` (خط ۱۶۳) پس از انتقال، تخصیص FIFO را برای پروندهٔ نگه‌داری‌شده بازسازی می‌کند.
- snapshot منبع (`CASE_MERGED`)، correction (`merge_duplicate_case`) و `resolved` شدن finding همگی در همان commit اتمیک ثبت می‌شوند.

---

## ۵) جمع‌بندی و پیشنهاد (بدون تغییر کد فعلی)

**اشکال اصلی** یک «گیت fail-closed سخت‌گیرانه و حساس به تغییر جزئی» است، نه یک نقص منطقی در خودِ عملیات ادغام. به‌ترتیب احتمال:

1. **`duplicate_case_plan_stale`** — `planHash` از JSON کامل و حساس‌به‌ترتیبِ پرونده ساخته می‌شود؛ هر نوشتن جزئی/بازسریالیز بین راهنما و commit، ادغام را نامعتبر می‌کند.
2. **`case_identity_conflict`** — بررسی هویت بین دو پرونده سخت‌گیرانه‌تر از لینک پیشنهاد است؛ دادهٔ legacy/ناقص (buyerCd/currency خالی یا متفاوت) به‌سادگی گیر می‌کند.
3. **`duplicate_case_not_found`** — `rootOfferId` بدون fallback، پروندهٔ دارای شناسهٔ کهنه را از تشخیص حذف می‌کند؛ و پیشنهادِ تکراری مانع کامل ادغام می‌شود.

### برای رفع (در صورت تأیید در گام بعدی)
- مقاوم‌کردن `planHash` در برابر تغییرات غیرمرتبط (امضا فقط روی هویت/شواهد مالی ضروری، یا نرمال‌سازی ترتیب کلید قبل از هش).
- نرم‌ترکردن گارد هویت (مقایسه فقط وقتی هر دو طرف مقدار دارند و تفاوت واقعی است؛ افزودن fallback برای buyerCo/currency).
- اضافه‌کردن fallback به `sd_case_offer_linked` وقتی `rootOfferId` با `_id` نمی‌خواند (بازگشت به wonOffer/offerNo با هشدار).
- اطمینان از تازه‌سازی کش محلی قبل از commit (pull فوری قبل از `duplicate_case_plan`).

> هیچ‌کدام از این موارد هنوز پیاده‌سازی نشده‌اند؛ این گزارش صرفاً تشخیص است.
