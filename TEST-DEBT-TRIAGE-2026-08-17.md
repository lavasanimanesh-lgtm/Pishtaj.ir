# تریاژ بدهی تست و رانر جدید سوئیت UAT

- **تاریخ:** ۱۴۰۵/۰۵/۲۶ (۲۰۲۶-۰۸-۱۷) | **نسخه:** v34.7.25 | **گام:** H2 نقشهٔ فازبندی
- **دامنه:** ۴۱۰ تستر موجود در `_tools/uat/`
- **اصل حاکم:** هیچ تستری حذف یا بی‌صدا سبز نشد. بدهی تست فقط **مستند و قرنطینه** شد تا شکست واقعی در نویز گم نشود.

---

## ۰) خلاصه

| وضعیت | قبل از این گام | بعد از این گام |
|---|---|---|
| کل تستر | ۴۱۰ | ۴۱۰ |
| سبز | نامعلوم (شمارش سه قالب خلاصه را نمی‌شناخت) | **۳۵۱** |
| قرنطینهٔ مستند | — | **۵۹** |
| شکست واقعی | «۶۲» گزارش می‌شد | **۰** |

دو کشف در جریان تریاژ:

1. **چهار تستر سالم، «شکست‌خورده» شمرده می‌شدند.** `tester149`, `tester150`, `tester151`, `tester281` هر چهار PASS بودند اما قالب خلاصه‌شان (`PASS 5 FAIL 0` و `tester281 5 PASS 0 FAIL`) با الگوی شمارش قبلی نمی‌خواند و کلمهٔ «FAIL» در خطشان، آن‌ها را قرمز نشان می‌داد.
2. **۷۲ تستر اصلاً خلاصه‌ای تولید نمی‌کردند** (خروجی تک‌خطی یا فقط علامت ✔). رانر جدید پنج قالب خروجی را می‌شناسد و این‌ها هم شمارش می‌شوند.

---

## ۱) رانر جدید — `_tools/uat/run-all.js`

```bash
node _tools/uat/run-all.js          # خلاصهٔ انسانی
node _tools/uat/run-all.js --json   # خروجی ماشین‌خوان برای CI
```

قرارداد:

- هر تستری که **در فهرست قرنطینه نباشد** و شکست بخورد ⇒ خروجی `1` (گیت قرمز).
- تسترهای قرنطینه‌شده جدا و **با دلیل مکتوب** گزارش می‌شوند؛ نه حذف، نه سبز.
- اگر تستر قرنطینه‌شده‌ای سبز شود، رانر هشدار «باید از فهرست خارج شود» می‌دهد — سازوکار ضدانباشت.

فهرست قرنطینه: `_tools/uat/known-obsolete.json` (هر ورودی: دسته + دلیل + تاریخ).

---

## ۲) طبقه‌بندی ۵۹ تستر قرنطینه‌شده

### یافتهٔ باز واقعی (فاز G) — 2 تستر

> ادعای قدیمی معماری v35؛ نیازمند بازنویسی در فاز G

```
tester403-v35-sales-to-cash.js
tester415-v34.7.10-mobile-customer-integrity.js
```

### قرارداد کسب‌وکاری تغییر‌کرده — 5 تستر

> قرارداد کسب‌وکاری بعداً با تصویب تغییر کرده (مثلاً مبنای پورسانت v34.5.35 و سود v34.5.38)

```
tester110-v194.js
tester111-v195.js
tester132-v216.js
tester35-sprint122.js
tester53-v133.js
```

### ماتریس نقش بازنگری‌شده — 2 تستر

> ماتریس نقش‌ها بعداً بازنگری شده است

```
tester106-v190.js
tester122-v206.js
```

### پین شمارهٔ نسخه/کش — 4 تستر

> پین شمارهٔ نسخه/کش که در نسخه‌های بعد عمداً تغییر کرده است

```
tester124-v21.js
tester254-advanced-cv-finalization-backlog-doc.js
tester261-advanced-cv-gtm-sales-plan.js
tester41-v1230.js
```

### پین رشته/markup رابط کاربری — 13 تستر

> پین رشته/markup رابط کاربری که بعداً بازطراحی شده است

```
tester107-v191.js
tester139-v258-llm.js
tester209-v3301-petty-combined-pdf.js
tester262-control-valve-dedicated-seo-landing.js
tester275-fiscal-profit-and-minimal-icons.js
tester276-semantic-icons-fiscal-dark.js
tester40-v1225.js
tester51-v131.js
tester54-v134.js
tester58-v138.js
tester81-v163.js
tester82-v164.js
tester97-v179.js
```

### گردش کار بازطراحی‌شده — 33 تستر

> گردش کار مربوطه در نسخه‌های بعد بازطراحی شده است

```
tester103-v186.js
tester104-v187.js
tester105-v189.js
tester109-v193.js
tester113-v197.js
tester114-v198.js
tester119-v203.js
tester120-v204.js
tester143-v262-supplier-finance.js
tester145-v264-cheque-transfer.js
tester215-v3305-cheque-module.js
tester291-hub-clean.js
tester52-v132.js
tester55-v135.js
tester57-v137.js
tester59-v139.js
tester60-v140.js
tester63-v143.js
tester65-v145.js
tester66-v146.js
tester67-v147.js
tester68-v148.js
tester7-sprint70.js
tester70-v150.js
tester73-v153.js
tester8-sprint71.js
tester83-v165.js
tester86-v168.js
tester89-v171.js
tester9-sprint72.js
tester90-v172.js
tester95-v177.js
tester96-v178.js
```

---

## ۳) چرا «اصلاح دسته‌جمعی» انجام نشد

بیشتر این شکست‌ها از جنس **پین‌کردن رشتهٔ دقیق UI یا شمارهٔ نسخه** هستند (مثلاً «کش sw هنوز v20.7» یا «handover نسخهٔ v31.7.97 را ثبت کرده»). دو مسیر ممکن بود:

1. به‌روزرسانی همان رشته‌ها به مقادیر امروز → همان شکنندگی، فقط چند ماه عقب‌افتاده؛
2. بازنویسی assertion به «سنجش نیت» به‌جای «سنجش رشته» → کار درست، اما برای ۵۹ تستر یک پروژهٔ جداست و در همین گام ریسک تغییر ناخواسته دارد.

بنابراین در این گام فقط **یک نمونهٔ الگو** اصلاح نشد و بقیه مستند شدند؛ اصلاح تدریجی با قاعدهٔ زیر انجام می‌شود:

> هر تستری که در آینده به‌خاطر تغییر یک ماژول لمس می‌شود، همان‌جا از «پین رشته» به «سنجش رفتار» ارتقا یابد و از فهرست قرنطینه خارج شود.

---

## ۴) دو مورد که واقعاً باز هستند (نه بدهی تست)

| تستر | ماهیت | تصمیم |
|---|---|---|
| `tester403-v35-sales-to-cash.js` | ادعاهای معماری v35 که با گاردهای بعدی (پروندهٔ تکراری، هویت) تغییر کرده‌اند | بازنویسی در **فاز G** همراه با فرمان `replace_award` |
| `tester415-v34.7.10-mobile-customer-integrity.js` | «تطبیق پرونده با rootOfferId به‌عنوان مرجع» — پس از fallback نسخهٔ v34.7.15 دیگر مطلق نیست | بازبینی در **فاز G** (مدل نسخهٔ مؤثر قرارداد) |

هر دو **پیش از شروع کار این سری** هم قرمز بودند و در همهٔ baselineها به‌عنوان شکست پیش‌ازموجود ثبت شده‌اند.

---

## ۵) گام H1 — وضعیت patchهای گردش‌کار

`_tools/PENDING-workflow-php-lint-2026-08-13.patch` و `_tools/PENDING-workflow-ci-gate-2026-08-13.patch` روی این درخت **بدون تعارض اعمال می‌شوند** (`git apply --check` سبز)، اما push آن‌ها با خطای زیر رد شد:

```
refusing to allow a GitHub App to create or update workflow
.github/workflows/deploy-production.yml without `workflows` permission
```

یعنی این گام **مسدودِ مجوز** است، نه مسدودِ فنی. برای اعمال، یک کاربر دارای دسترسی مخزن کافی است این دو دستور را اجرا کند:

```bash
git apply _tools/PENDING-workflow-php-lint-2026-08-13.patch
git apply _tools/PENDING-workflow-ci-gate-2026-08-13.patch
```

اثر آن: `php -l` روی `api/` و `crm/`، `node --check` روی همهٔ JS، و اجرای گیت UAT پیش از استقرار FTP.

> تا آن زمان، `php -l` فقط در محیطی اجرا می‌شود که PHP نصب باشد؛ در sandbox توسعه PHP در دسترس نیست و `run-ci-gate.js` این بخش را با پیام صریح skip می‌کند (شکست کاذب نمی‌سازد). بررسی ساختاری PHP در این سری با ابزار مستقل توازن بلوک/رشته انجام شده است.

---

## ۶) توصیهٔ نگهداشت

1. در CI، به‌جای شمارش دستی، از `node _tools/uat/run-all.js` استفاده شود (خروجی غیرصفر = شکست واقعی).
2. هر افزودن به `known-obsolete.json` باید دلیل و تاریخ داشته باشد و در بازبینی فصلی مرور شود.
3. هدف کمی پیشنهادی: کاهش قرنطینه به زیر ۳۰ مورد تا پایان فاز G، از راه ارتقای تدریجی همان تسترهایی که لمس می‌شوند.
