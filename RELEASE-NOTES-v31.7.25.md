# Release Notes — v31.7.25

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.25
**نوع:** 🔴 رفع باگ — ارسال پیامک با خطای «Authentication required» (BUG-AUTH-007، بازمانده incident v31.7.4)

---

## 🎯 گزارش کارفرما (با اسکرین‌شات)

«می‌خواستم پیامک بدهم اما خطا می‌دهد: ❌ ارسال ناموفق — علت دقیق: Authentication required. به کلید API دست نزدم، چک کردم درست است.»

## 🔍 علت ریشه‌ای (RCA)

**کلید API کاوه‌نگار هیچ ربطی به مشکل نداشت** — خطا اصلاً به سرویس پیامک نمی‌رسید. پیام «Authentication required» متن دقیق 401 تابع `verify_request()` خود CRM است:

- از incident v31.7.4، همه اکشن‌های محافظت‌شده **توکن JWT در هدر `X-CRM-Token`** می‌خواهند.
- هات‌فیکس‌های v31.7.7/v31.7.8 فقط `bridge.js` و `sync.js` را توکن‌دار کردند؛ اما **`sms.js` (و backup.js/golive.js/rbac.js) هنوز با هدر قدیمی `X-CRM-Role` می‌فرستادند** که دیگر کافی نیست → سرور CRM قبل از رسیدن به کاوه‌نگار، درخواست را 401 می‌کرد.
- **بازتولید E2E:** `sms_bulk` با `X-CRM-Role: admin` → دقیقاً `{"error":"Authentication required"}` (همان اسکرین‌شات)؛ با `X-CRM-Token` → عبور از احراز هویت ✅.

این **پنجمین بازمانده** خانواده BUG-AUTH بود (پس از bridge/users_get/login/users_sync) — بنابراین به‌جای وصله نقطه‌ای، ریشه خانواده بسته شد.

## ✅ رفع ریشه‌ای — پایان این خانواده باگ

**Wrapper سراسری `fetch` در `ui-kit.js`** (اولین اسکریپت لودشده): هر درخواستی به `api/crm.php` که هدر `X-CRM-Token` ندارد، توکن JWT جاری را **خودکار** می‌گیرد. یعنی:

- پیامک، بک‌آپ سروری، Go-Live و sync کاربران — همه بدون تغییر حتی یک خط از کدشان درست می‌شوند
- **ماژول‌های آینده هم برای همیشه ایمن‌اند** — دیگر هیچ‌کس نمی‌تواند «فراموش کند» توکن بفرستد
- گاردهای دقیق: توکن صریح ماژول‌ها بازنویسی نمی‌شود؛ درخواست‌های خارجی (کاوه‌نگار/سایت) دست‌نخورده؛ بدون login هدری جعل نمی‌شود؛ محافظ double-wrap

## 📁 فایل‌های تغییریافته

`crm/ui-kit.js` (wrapper — تنها تغییر کد)، `crm/index.html` + `crm/sw.js` + `crm/clear-cache.html` (bump)، `_tools/uat/tester200` (جدید)

## 📊 نتایج گیت

- **E2E سرور PHP واقعی:** بازتولید دقیق خطای اسکرین‌شات (401 با X-CRM-Role) و عبور با توکن ✅
- تستر جدید `tester200`: **۱۲/۱۲ PASS** — شامل ۶ سناریوی رفتاری wrapper (تزریق، احترام به توکن صریح، عدم دستکاری درخواست خارجی، بدون جعل، ضد double-wrap)
- رگرسیون کامل: **178/178 فایل PASS — 4076/4076 چک PASS** | `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| ارسال پیامک | ✅ رفع — درخواست با توکن معتبر به سرور و سپس کاوه‌نگار می‌رسد | صفر |
| بک‌آپ سروری/Go-Live/sync کاربران | ✅ همین 401های خاموش آنجا هم رفع شد (کاربر هنوز گزارش نکرده بود) | مثبت |
| bridge/sync (توکن‌دار از قبل) | دست‌نخورده — wrapper توکن صریح را بازنویسی نمی‌کند | صفر |
| درخواست‌های خارجی (کاوه‌نگار، نرخ ارز، بات) | کاملاً دست‌نخورده (فقط api/crm.php) | صفر |
| امنیت | بدون تغییر سیاست — همان JWT per-user؛ فقط ارسال هدر کامل شد | صفر |

## 📋 راستی‌آزمایی توسط کارفرما (۲ دقیقه)

1. استقرار → رفرش (Ctrl+F5) → سامانه پیامکی → ارسال پیامک آزمایشی → **باید ارسال شود** (اگر پنل کاوه‌نگار شارژ دارد).
2. تنظیمات → پشتیبان → «بک‌آپ‌های سرور» → فهرست بدون خطا باز شود (این هم قربانی خاموش همین باگ بود).

---
*توسعه‌دهنده: PTF Development Team — 2026-07-20*
