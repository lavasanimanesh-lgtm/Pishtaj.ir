# یادداشت انتشار v34.39.6 — HOME-WAVE (موج مورب رنگیِ عبوری روی پنل هیرو)

**تاریخ:** 2026-09-17
**بازوی تغییر:** Arena Agent
**کانال:** سایت عمومی — صفحهٔ اول (لایهٔ نمایشی v34.39.6؛ محتوا/سئو/ساختار بدون تغییر)
**قرارداد:** tester668 (۱۲ سنجه) + بازتنظیم پین‌ها با ابزار رسمی `bump-version-pins.js`

## درخواست کارفرما
> «یک موج مورب رنگی روی کادری که «دپارتمان تامین» و «صنعت هدف» و … داخلش قرار دارند هر چند ثانیه یک‌بار عبور کند.»

## پیاده‌سازی
- **محل:** پنل شیشه‌ای سمت چپِ هیرو (`.hero-panel` — همان کادر «۳ دپارتمان تامین / ۶+ صنعت هدف / RFQ پاسخ‌گویی سریع» + لوگو).
- **تکنیک:** یک `::after` تمام‌سطح روی همان کارت با گرادیان مورب ۱۱۵ درجه از طیف برند — کهربایی `rgba(255,184,80)` → سفیدِ نرم → شعله‌ای `rgba(239,75,26)` — که با `background-size:260%` روی tile جابه‌جا می‌شود؛ `mix-blend-mode:screen` تا مثل نور از روی شیشه سُر بخورد (نه مثل چسبِ مات) و در نمای شب با آلفای پررنگ‌تر ادامه دهد.
- **ریتم «هر چند ثانیه یک‌بار»:** سیکل ۸ ثانیه — `@keyframes ptfWave` عبور را در ۵۲٪ اول (≈۴.۲ ثانیه با ease-in-out) انجام می‌دهد و در ۴۸٪ دوم روی موقعیتِ تاریک **قفل** می‌ماند؛ یعنی موج می‌آید، عبور می‌کند و بعد ~۴ ثانیه خبری نیست — دقیقاً همان حسِ پالسِ فاصله‌دار (هم‌خانواده با ریتم آرام‌شدهٔ حلقه‌های جریان v34.39.2).
- **هزینه:** صفر جاوااسکریپت، صفر المان DOM، صفر درخواست شبکه؛ فقط `background-position` روی یک لایهٔ ایزوله — با `pointer-events:none` کلیکِ متن‌ها/RFQ داخل پنل دست‌نخورده.
- **احترامِ تنظیمات:** در `prefers-reduced-motion: reduce` کاملاً خاموش (نه حرکت، نه درخشش). تیلت ۳بعدیِ هاورِ پنل از قبل هم‌نواست (موج روی همان سطح کج‌شده سوار است).
- بودجهٔ سرعت: `home.css = ۳۹٬۷۳۵` نویسه (سقف ۴۰K گارد tester662) با حذفِ کامنت‌های تزئینی — مارکرهای هر هفت لایه چک و سالم.

## فایل‌های تغییر‌یافته
`index.html` (cache-bust → v=34.39.6) · `assets/css/home.css` (لایهٔ v34.39.6) · `VERSION.json` و ۸ نقطهٔ رسمی نسخه · `RELEASE-NOTES-v34.39.6.md` · tester668.

## وضعیت دروازه
tester668: ۱۲/۱۲ ✓ · tester667: ۲۱/۲۱ ✓ · tester666: ۲۳ ✓ · 665: ۲۵ ✓ · 664: ۲۹ ✓ · 663: ۳۰ ✓ · 662: ۳۶ ✓ · گیت کامل CI: **۲۸۰ PASS / ۱ FAIL** (شکستِ محیطیِ شناخته‌شدهٔ tester656) · arch-guard: PASS.
