# Release Notes — v31.7.11

**تاریخ:** ۱۴۰۵/۰۴/۲۸ (2026-07-19)
**نسخه:** v31.7.11
**نوع:** 🟡 رفع باگ گزارش‌شده کارفرما — بازگشت عکس پروفایل حذف‌شده پس از رفرش (BUG-AVATAR-001)

---

## 🎯 مسئله (به گزارش کارفرما)

عکس پروفایل را حذف می‌کنم، پاک می‌شود؛ اما با رفرش صفحه دوباره برمی‌گردد.

## 🔍 علت ریشه‌ای (RCA)

سومین مصداق همان الگوی BUG-NTF-002 (تغییر یک سمت قرارداد merge بدون سمت دیگر):

1. `ptf_crm_avatars` برخلاف اکثر کلیدها **آبجکت map است نه آرایه** (`{username: dataURL}`).
2. `ptfAvatarRemove()` در `theme.js` با `delete all[user]` کلید را حذف و push می‌کرد — تا اینجا درست.
3. اما در رفرش بعدی، `initialSync → pullCheck(forceFull) → ptfSmartMerge` اجرا می‌شود و مسیر عمومی merge برای غیرآرایه‌ها `return remoteStr` (**server-wins**) بود. هر نسخه‌ای از سرور که هنوز عکس را داشت (به‌دلیل ترتیب push/pull، دستگاه دوم، یا race با pull دوره‌ای ۲۰ثانیه‌ای) عکس حذف‌شده را برمی‌گرداند — چون «حذف» به‌صورت «غیاب کلید» بیان شده بود و غیابْ در merge قابل تشخیص از «هنوز خبر نرسیده» نیست.

**اثبات:** قبل از رفع، شبیه‌سازی merge با `local={}` و `server={u1:pic}` → خروجی `{u1:pic}` (عکس برگشت). بعد از رفع → tombstone حفظ می‌شود.

## ✅ اصلاحات

| # | فایل | تغییر | شدت |
|---|------|--------|-----|
| 1 | `crm/theme.js` | حذف عکس = **tombstone نسخه‌دار** `{v:null, ts}` به‌جای `delete` — حذفْ خودش یک رویداد قابل‌sync می‌شود | 🔴 P0 |
| 2 | `crm/theme.js` | آپلود عکس هم نسخه‌دار `{v:dataURL, ts}` ذخیره می‌شود؛ خواننده‌ها (`ptfAvatarOf`, `ptfApplyAvatar`, دیالوگ، پنل تنظیمات) از `avatarVal()` می‌خوانند که **با فرمت legacy رشته‌ای هم سازگار است** — داده موجود کاربران نیاز به migration ندارد | 🔴 P0 |
| 3 | `crm/sync.js` | merge اختصاصی `ptf_crm_avatars`: مقایسه per-user با timestamp — جدیدتر برنده (حذف جدید بر عکس کهنه؛ عکس جدید بر حذف کهنه)؛ نسخه‌دار بر legacy بدون ts برنده؛ tombstoneهای کهنه‌تر از ۳۰ روز هرس می‌شوند | 🔴 P0 |
| 4 | `crm/index.html`, `crm/sw.js`, `crm/clear-cache.html` | همگام‌سازی ۴نقطه‌ای نسخه به v31.7.11 | — |

## 📊 نتایج گیت

- تستر رفتاری جدید `tester186-avatar-delete-sync`: **۱۵/۱۵ PASS** — شامل بازتولید دقیق سناریوی گزارش‌شده (حذف → merge با سرور کهنه → عکس برنمی‌گردد)
- رگرسیون کامل: **164/164 فایل PASS — 3857/3857 چک PASS**
- `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| آواتارهای موجود (فرمت legacy رشته‌ای) | بدون migration؛ `avatarVal` هر دو فرمت را می‌خواند؛ با اولین تغییر، رکورد کاربر نسخه‌دار می‌شود | صفر |
| آواتار سایر کاربران هنگام merge | حفظ می‌شود (per-user، نه کل‌map) | صفر |
| حجم کلید | tombstoneها پس از ۳۰ روز هرس می‌شوند — رشد نامحدود ندارد | صفر |
| سایر کلیدهای sync | merge عمومی دست‌نخورده؛ فقط شاخه اختصاصی avatars اضافه شد (قبل از گارد آرایه‌ای) | کم |
| اعلان‌ها (v31.7.10) | رگرسیون tester185 سبز — دست‌نخورده | صفر |

## 📋 راستی‌آزمایی توسط کارفرما (۲ دقیقه)

1. عکس پروفایل بگذارید → رفرش → عکس بماند.
2. **حذف بزنید → هارد رفرش (Ctrl+F5) → عکس نباید برگردد** (سناریوی گزارش‌شده).
3. چند ثانیه صبر کنید (sync دوره‌ای ۲۰ثانیه‌ای) و دوباره رفرش → همچنان حذف‌شده.
4. اگر دستگاه دومی دارید: در دستگاه دوم هم پس از sync، عکس حذف شده باشد؛ سپس در دستگاه دوم عکس جدید بگذارید → در دستگاه اول ظاهر شود.

---
*توسعه‌دهنده: PTF Development Team — 2026-07-19*
