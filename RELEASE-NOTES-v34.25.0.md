# RELEASE NOTES — v34.25.0 (استودیو صفحهٔ محصول — عکس از بیرون + پیش‌نمایش + ذخیرهٔ موقت)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** tester570 (۲۵ سنجه) · **گیت:** 186 PASS / 0 FAIL · **PHP-lint:** ✓

## درخواست مالک

«صفحهٔ محصول با AI ساخته می‌شود اما باید بتوان عکسی از بیرون بهش داد و در صفحه جای‌گذاری کرد؛ قبل از انتشار پیش‌نمایش صفحه نمایش داده شود تا اصلاحات لازم انجام شود؛ و ثبت موقت تغییرات هم قبل از انتشار ممکن باشد.»

## ① 📂 انتخاب عکس از سیستم (آپلود به سایت)

- دکمهٔ «📂 انتخاب عکس از سیستم (آپلود)» کنار فیلد تصویر **هم در فرم محصول و هم فرم صفحهٔ جدید**.
- تصویر روی هاست در `assets/images/` ذخیره می‌شود (اکشن جدید `image_upload` در api/cms.php): اعتبارسنجی حجم (≤۸MB)، فرمت (JPG/PNG/WebP/GIF)، **سلامت واقعی فایل با getimagesize + تطابق MIME**، نام فایل کنترل‌شده از نامک فرم + زمان + تصادفی (بدون پیمایش مسیر) + لاگ CMS.
- مسیر برگشتی خودکار در فیلد تصویر می‌نشیند + **بندانگشتی زندهٔ عکس زیر فیلد** (هم پس از آپلود، هم پس از بازیابی پیش‌نویس).
- پیشنهاد «در متن هم درج شود؟» → تگ `<img>` با alt از H1، **در محل نشانگرِ متن** درج می‌شود.

## ② 👁 پیش‌نمایش صفحه قبل از انتشار (محصول)

- دکمهٔ «👁 پیش‌نمایش صفحه» در مودال محصول — **هم‌شکل خروجی نهایی product_create**: هیرِ تیره با بج «محصولات» و برند، تصویر og، بدنهٔ HTML، جدول مشخصات فنی (از خطوط «کلید = مقدار»)، سوالات متداول بازشدنی، جعبهٔ قیمت/موجودی و CTA استعلام + نوار متا (طول title/desc و نامک نهایی).
- پیش‌نمایش از **مقادیر زندهٔ فرم** است — هر اصلاحی قبل از انتشار دیده می‌شود.

## ③ 💾 ذخیرهٔ موقت صریح

- دکمهٔ «💾 ذخیرهٔ موقت» در هر دو فرم (محصول + صفحه) — همان موتور پیش‌نویس ماندگار (devkv/A10) با پیام روشن؛ آپلود/درج تصویر هم پیش‌نویس را خودکار نو می‌کند؛ با باز شدن دوبارهٔ فرم بازیابی می‌شود (این امکان از v34.11 خودکار بود — حالا صریح و قابل دیدن است).
- ترتیب فراخوانی پس از رندر فرم صفحه (restore → شمارنده → بندانگشتی) حفظ شد.

## یادآوری دربارهٔ «صفحات جدید هیچ امکانی ندارد»

مولد صفحهٔ عمومی (خدمات/صنایع/مقایسه) از v34.13، ابزارهای فرم (نوار ابزار ۱۰دکمه‌ای/شمارنده/پیش‌نمایش/گسترش AI) از v34.20 و دستیار AI خارجی از v34.21 در کد موجودند — محیط پروداکشن (v34.9.1) هنوز هیچ‌کدام را ندارد؛ **دیپلوی لازم است**.

## فایل‌های کلیدی

`crm/cms.js` (cmsImgPick/cmsImgThumb/cmsPrPreview/cmsDraftBtn + دو فرم) · `api/cms.php` (اکشن image_upload) · `_tools/uat/tester570-v34.25.0-product-image-preview.js`
