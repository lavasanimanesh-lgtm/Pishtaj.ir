# ارزیابی و راهکار — ناوبری/جستجوی تأمین‌کننده در «ثبت استعلام تامین»

**برنچ:** `arena/019fcd1d-pishtaj-ir` (v34.0.13-alpha)

---

## مشکل کاربر
در «ثبت استعلام تامین» (گام ۳: انتخاب تأمین‌کنندگان)، تعداد تأمین‌کننده پیشنهاد می‌شود و زیر آن دکمهٔ ناوبری برای دیدن بقیهٔ تأمین‌کنندگان هست ولی **کار نمی‌کند**؛ و باید امکان «سرچ تأمین‌کننده» برای ارسال و تخصیص تأمین‌کننده به کالاها باشد.

---

## ریشه‌یابی (تأییدشده)

### باگ دکمهٔ «▼ نمایش / سایر تأمین‌کنندگان» و جستجوی زنده
در `crm/rfqsmart.js` (`rfqsRenderTargets`):

```js
// دکمهٔ ناوبری:
onclick="_st._tgOtherOpen=!_st._tgOtherOpen;rfqsRenderTargets()"   // ← خطا
// جستجوی زنده:
oninput="_st._tgQ=this.value;rfqsRenderTargets()"                  // ← خطا
```

`_st` یک **متغیر خصوصی داخل IIFE** ماژول است (خط ۳۶؛ بسته‌شدن IIFE در خط ۱۳۴۵). دسترسی مستقیم به آن از `onclick`/`oninput` که در scope سراسری اجرا می‌شوند، **ReferenceError: _st is not defined** می‌دهد → دکمهٔ «نمایش» و جستجو کار نمی‌کنند. (توابعی مثل `rfqsToggleSup` چون سراسری‌اند درست کار می‌کردند؛ فقط دسترسی مستقیم به `_st` از inline خراب بود.)

### «تخصیص تأمین‌کننده به کالا» — در کجا هست؟
- در **ثبت استعلام تامین**، تأمین‌کنندگانِ انتخاب‌شده به **کل استعلام** (لیست گیرندگان برای کشف قیمت) ارسال می‌شوند — تخصیص per-item در این مرحله لازم نیست (همهٔ تأمین‌کنندگان به همهٔ اقلام پاسخ می‌دهند).
- **تخصیص تأمین‌کننده به کالای خاص** در **خرید واقعی** (`crm/buycompare.js`) انجام می‌شود: `c.purchases` با `idx` (قلم) و `sup` (تأمین‌کننده)، و تقسیم lot به تأمین‌کنندگان مختلف (`splitLot`). پس این قابلیت از قبل موجود است.

---

## راهکار پیاده‌شده (فاز ۱۰)

**اصلاح باگ ناوبری/جستجو (`crm/rfqsmart.js`):**
- دو wrapper سراسری اضافه شد (داخل IIFE ولی به `window`):
  ```js
  window.rfqsToggleOther = function () { _st._tgOtherOpen = !_st._tgOtherOpen; rfqsRenderTargets(); };
  window.rfqsSetSearch = function (v) { _st._tgQ = v; rfqsRenderTargets(); };
  ```
- دکمهٔ «نمایش/جمع‌کردن» → `onclick="rfqsToggleOther()"`
- جستجو → `oninput="rfqsSetSearch(this.value)"`
- (مثل الگوی `rfqsToggleSup` که قبلاً درست کار می‌کرد.)

**نتیجه:** دکمهٔ «سایر تأمین‌کنندگان» و جستجوی زنده حالا کار می‌کنند.

**در مورد «تخصیص به کالا»:** نیازی به ساخت جدید نبود — تأمین‌کننده به قلم/کالا در **خرید واقعی** تخصیص می‌یابد (که موجود است). در ثبت استعلام، انتخاب تأمین‌کننده برای کشف قیمتِ کل اقلام است (درست و لازم). اگر در آینده بخواهید در خودِ استعلام تامین، قلم‌های خاص به تأمین‌کننده‌های خاص ارسال شوند، این یک **افزونهٔ جدا** است (نیازمند مدل `targets[].items`).

---

## تست‌ها
- **tester309** جدید (۷/۰): wrapperها، عدم دسترسی مستقیم inline به `_st`، و وجود تخصیص per-item در خرید واقعی.
- رگرسیون کامل سبز (**۱۹ تستر، ۰ شکست**).
- بامپ نسخه → **v34.0.13-alpha**.

*تولیدشده توسط Arena Agent — رفع باگ ناوبری/جستجو و ارزیابی تخصیص تأمین‌کننده.*
