# نقشهٔ فازبندی‌شدهٔ رفع کامل باگ‌ها و اصلاحات باقی‌مانده

- **تاریخ:** ۱۴۰۵/۰۵/۲۶ (۲۰۲۶-۰۸-۱۷)
- **نقطهٔ شروع:** برنچ `arena/01a0077c-pishtaj-ir` روی `a44a405` — همگام با `origin/main` (`ef47908`)، شامل نسخه‌های v34.7.17 و v34.7.18
- **دامنه:** همهٔ موارد باز از چهار منبع: بررسی تصمیم‌یار/تحلیلگر، ریشه‌یابی یکپارچگی وصولی، گزارش تلفیقی «تغییر سند برد» و بررسی مستقل آن
- **وضعیت:** فقط برنامه‌ریزی؛ در این سند هیچ کدی تغییر نکرده است

---

## ۰) اصول حاکم بر همهٔ فازها

۱. **وفاداری به معماری موجود:** هیچ مسیر کاری حذف نمی‌شود مگر جایگزین سروری‌اش آماده و تست‌شده باشد.
۲. **افزایشی و برگشت‌پذیر:** هر تغییر رفتاری باید fallback رفتار قبلی داشته باشد یا پشت گارد صریح برود.
۳. **هیچ مهاجرت دادهٔ اجباری بدون تأیید مدیر:** ترمیم داده فقط idempotent، با پیش‌نمایش و تأیید انسانی.
۴. **هر فاز = یک نسخه + یک تستر اختصاصی + ورود به `run-ci-gate.js`.**
۵. **معیار پذیرش هر فاز، عدد است نه توصیف:** هر آیتم باید یک assert اجرایی داشته باشد.
۶. **پیش از هر فاز:** اجرای کامل سوئیت و ثبت baseline؛ پس از فاز: مقایسهٔ خط‌به‌خط و اثبات «صفر شکست جدید».
۷. **تفکیک تصمیم فنی از تصمیم کسب‌وکاری:** آیتم‌های نشان‌دار 🔷 پیش از اجرا نیاز به تأیید کارفرما دارند.

---

## ۱) فهرست کامل موارد باز (Backlog قابل ردیابی)

| کد | عنوان | شدت | منشأ | فاز |
|---|---|---|---|---|
| AR-01 | ابطال محلی فاکتور غیررسمی، `creditRemainIRR` **همهٔ پرونده‌های سیستم** را بازنویسی می‌کند | ✅ بسته شد (v34.7.19) | بررسی مستقل N1 | A |
| AR-02 | `correct_invoice` فیلدهای فاکتور (`pays`, `dueISO/dueFa`, `contactApproved`, فرادادهٔ ارزی) را بی‌صدا حذف می‌کند | ✅ بسته شد (v34.7.19) | بررسی مستقل N2 | A |
| UI-01 | دیالوگ «صورتحساب غیررسمی» از کشوی پرونده باز نمی‌شود (تطبیق `_id` در برابر `cd`) | ✅ بسته شد (v34.7.20) | تلفیقی §۷.۴ | B |
| UI-02 | شاخهٔ `existing` در صدور مجدد، مبلغ/`linesSnapshot` را ذخیره نمی‌کند (+ `setData` تکراری) | ✅ بسته شد (v34.7.20) | تلفیقی §۷.۴ + N4 | B |
| UI-03 | تعریف دوگانهٔ `offerPostAwardLocked` (و `ptfGoSalesFileForOffer`) در `crm/offers.js` | ✅ بسته شد (v34.7.20) | بررسی مستقل N3 | B |
| LC-01 | فیلتر `superseded` در `commission.js` / `working-capital.js` / `fiscal.js` یکسان نیست → دوبارشماری پورسانت | ✅ بسته شد (v34.7.21) | تلفیقی §۹ | C |
| LC-02 | مرجوعی فروش در سود، پورسانت، سرمایه در گردش و سال مالی لحاظ نمی‌شود | ✅ بسته شد (v34.7.21، مصوب کارفرما) | تلفیقی §۸ | C |
| LC-03 | نبود سنجهٔ تسویه برای این چهار موتور در گزارش کیفیت داده | ✅ بسته شد (v34.7.21) | این سند | C |
| AW-01 | متمم با total ≤ ۰ در سرور رد نمی‌شود | 🟠 P2 | تلفیقی §۵.۳ | D |
| AW-02 | برنده‌کردن CO هم‌استعلامِ بدون علامت متمم، بی‌صدا پروندهٔ دوم می‌سازد | 🟠 P2 | بررسی مستقل N5 | D |
| AW-03 | `admin_delete_plan` چک/خرید/مرجوعی/QC/packing را در وابستگی‌ها کشف نمی‌کند | 🟠 P2 | تلفیقی §۱۱.۲ | D |
| OPS-01 | نسخهٔ endpoint سلامت `34.6.0` با `PTF_CRM_RELEASE` هم‌خوان نیست | 🟡 P2 | تلفیقی §نسخه | D |
| INV-01 | نبود endpoint سروری `void_unofficial_invoice` (ابطال فقط محلی) | 🔴 P1 | تلفیقی §۷.۲ | E |
| AN-01 | گزارش هفتگی/ماهانهٔ مدیریت، دادهٔ مادام‌العمر چاپ می‌کند (بدون بازهٔ زمانی) | 🟠 | تحلیلگر F‑07 | F |
| AN-02 | تعریف نرخ برد در `insights.js` با لایهٔ واحد یکسان نیست | 🟠 | تحلیلگر F‑06 | F |
| AN-03 | واحد تحلیل «سند» است نه «فرصت» | 🟠 | تحلیلگر F‑09 | F |
| AN-04 | `healthScore` کالیبره‌نشده | 🟠 🔷 | تحلیلگر F‑10 | F |
| AN-05 | تعریف پرنویز «پروندهٔ نیازمند بررسی» | 🟡 | تحلیلگر F‑11 | F |
| AN-06 | تشخیص «خرید واقعی» با Regex روی متن یادداشت | 🟡 | تحلیلگر F‑12 | F |
| AN-07 | نبود گیت دفتر رسمی/غیررسمی در snapshot ارسالی به AI | 🟡 | تحلیلگر F‑13 | F |
| AN-08 | تقویم میلادی گزارش‌های دوره‌ای | 🟡 | تحلیلگر F‑14 | F |
| ARCH-01 | نبود فرمان `replace_award / revise_case_contract` (جایگزینی دوطرفهٔ سند برد) | 🔴 اما پروژه‌ای 🔷 | تلفیقی §۱ و §۱۵ | G |
| ARCH-02 | سه مسیر موازی نوشتن پول (`post_receipt` / `savePay` / ماژول چک) | 🟠 🔷 | RCA وصولی R4 | G |
| ARCH-03 | `awardDocs` در مسیر سروری تضمین نمی‌شود (lazy/legacy) | 🟡 | تلفیقی §۴ | G |
| OPS-02 | ۶۲ تستر قدیمیِ قرمز (بدهی تست انباشته) | 🟡 | مشاهدهٔ سوئیت | H |
| OPS-03 | patchهای PENDING گردش‌کار (`php -l` و گیت CI پیش از FTP) اعمال نشده‌اند | 🟠 | `_tools/PENDING-*.patch` | H |

🔷 = نیازمند تصمیم/تأیید کسب‌وکاری پیش از اجرا.

---

## فاز A — ایمنی داده (هات‌فیکس فوری) — نسخهٔ هدف `v34.7.19`

> ✅ **اجرا شد (۲۰۲۶-۰۸-۱۷، نسخهٔ v34.7.19).** AR-01 و AR-02 بسته شدند؛ گارد مکمل «اصلاح فقط روی سند فعال» هم اضافه شد.
> رگرسیون: `_tools/uat/tester422-v34.7.19-ar-data-safety.js` — ۳۳ PASS / ۰ FAIL؛ سوئیت کامل بدون شکست جدید (۶۲ شکست پیش‌ازموجود).
> جزئیات: `RELEASE-NOTES-v34.7.19.md`.

**هدف:** بستن دو مسیری که همین امروز می‌توانند دادهٔ مالی را خراب کنند. هیچ قابلیتی اضافه نمی‌شود.

### گام A1 — گارد روی ابطال محلی فاکتور غیررسمی (AR-01)
- **فایل:** `crm/unofficial-invoice.js` — تابع `ptfUnofficialInvoiceVoid` (شروع `:1880`)، بلوک مرحلهٔ ۳ (`:2005-2047`)
- **اقدام:** در ابتدای تابع، اگر `window.PTF_SALES_DOMAIN_V2` فعال است و endpoint سروری موجود نیست → توقف با پیام صریح («ابطال صورتحساب غیررسمی در معماری فعلی باید از مسیر سرور انجام شود؛ در حال آماده‌سازی است — فاز E»). مسیر legacy (بدون V2) دست‌نخورده باقی می‌ماند.
- **اقدام مکمل (دفاع در عمق):** حلقهٔ بازنویسی `creditRemainIRR` به رسیدهای **همان پرونده** محدود و مبلغ تخصیص از `amountIRR` خوانده شود؛ حتی اگر گارد بالا دور زده شود، دامنهٔ خرابی از «کل سیستم» به «صفر» می‌رسد.
- **پذیرش:** اجرای تابع روی یک فاکتور، مقدار `creditRemainIRR` هیچ رسیدی در پرونده‌های دیگر را تغییر ندهد؛ در حالت V2 اصلاً وارد مرحلهٔ نوشتن نشود.
- **بازگشت:** حذف گارد = بازگشت به رفتار فعلی. ریسک: **بسیار کم** (فقط جلوگیری از نوشتن).
- **اندازه:** XS

### گام A2 — حفظ فیلدها در اصلاح فاکتور (AR-02)
- **فایل:** `api/sales-domain.php` — شاخهٔ `correct_invoice` (ساخت `$record`، حدود `:918-925`)
- **اقدام:** در حالت اصلاح (`$ii >= 0`) به‌جای جایگزینی کامل: `$record = array_merge($oldInv, $record);` سپس همان کلیدهای محاسبه‌شده (base/vat/amount/coverage/files/...) بازنویسی شوند. `register_invoice` (ثبت جدید) دست‌نخورده می‌ماند.
- **پذیرش:** پس از `correct_invoice`، فیلدهای `pays`، `dueISO`، `dueFa`، `contactReq`، `contactApproved`، `offerCurrency`، `offerFxBasis`، `offerFxRateRef`، `advApplied` عیناً باقی بمانند؛ `allocated*`/`open*` توسط `sd_rebuild_allocations` بازتولید شوند؛ `correctionVersion` و رکورد `corrections` مثل قبل ثبت شوند.
- **بازگشت:** حذف یک خط. ریسک: **کم** (فقط افزودن فیلدهای قدیمی؛ هیچ فیلد محاسبه‌شده‌ای بازنویسی نمی‌شود).
- **اندازه:** XS

### گام A3 — تستر و گیت
- تستر جدید `tester422-v34.7.19-ar-data-safety.js`: رگرسیون A1 (دامنهٔ صفر) + A2 (حفظ ۹ فیلد) + عدم تغییر رفتار مسیر legacy.
- افزودن به `run-ci-gate.js` و به فهرست `SYNTAX`.
- **گیت خروج فاز:** ۰ FAIL در تستر جدید، ۰ شکست جدید در سوئیت کامل نسبت به baseline، `node --check` تمیز، بررسی ساختاری PHP سالم.

---

## فاز B — بازگرداندن قابلیت‌های مسدود — `v34.7.20`

> ✅ **اجرا شد (۲۰۲۶-۰۸-۱۷، نسخهٔ v34.7.20).** UI-01، UI-02 و UI-03 بسته شدند (تعریف تکراری `ptfGoSalesFileForOffer` هم حذف شد).
> رگرسیون: `_tools/uat/tester423-v34.7.20-unofficial-issue-path.js` — ۳۷ PASS / ۰ FAIL؛ سوئیت کامل بدون شکست جدید.
> جزئیات: `RELEASE-NOTES-v34.7.20.md`.

**پیش‌نیاز:** پایان فاز A (چون B2 در همان فایلی است که A1 گارد گذاشته).

### گام B1 — رفع تطبیق شناسهٔ پرونده (UI-01)
- **فایل:** `crm/unofficial-invoice.js:1285-1287`
- **اقدام:** تطبیق **هر دو شناسه**: `String(x._id||'')===String(dealCd) || String(x.cd||'')===String(dealCd)`. اختیاری و بهتر: فراخوان `crm/salesfiles.js:1186` هم `r._id || r.cd` بفرستد تا هر دو سمت متقارن شوند.
- **پذیرش:** برای پروندهٔ دارای `_id` سروری و پروندهٔ legacy فقط‌`cd`، هر دو دیالوگ باز شوند؛ گاردهای نقش و مرحلهٔ ۷ دست‌نخورده بمانند.
- **اندازه:** XS | ریسک: کم

### گام B2 — اصلاح شاخهٔ صدور مجدد (UI-02)
- **فایل:** `crm/unofficial-invoice.js:1763-1769`
- **اقدام:** در حالت `existing`، علاوه بر `caseId/customerId`، مقادیر واقعی `amount`، `base`، `discount*`، `linesSnapshot`، `offerFxRateRef` و `invDate` به‌روز و **یک بار** ذخیره شوند؛ حذف `setData` تکراری و no-op؛ اگر رکورد به سرور ثبت شده باشد، همان مسیر `register_unofficial_invoice` دوباره صدا زده شود تا سرور هم‌راستا بماند.
- **پذیرش:** صدور مجدد با مبلغ/تخفیف جدید، همان مقادیر را در رکورد و در پروجکشن سرور نشان دهد؛ toast موفقیت فقط پس از ذخیرهٔ واقعی.
- **اندازه:** S | ریسک: متوسط (مسیر پول) → تست دستی روی استیجینگ الزامی

### گام B3 — حذف تعریف دوگانه (UI-03)
- **فایل:** `crm/offers.js` — نگه‌داشتن یک تعریف و حذف دیگری با کامنت ارجاع.
- **پذیرش:** یک تعریف در فایل؛ همهٔ تسترهای مرتبط با قفل پس از برد سبز.
- **اندازه:** XS | ریسک: کم

### گام B4 — تستر `tester423-v34.7.20-unofficial-issue-path.js` + گیت خروج (مثل A3)

---

## فاز C — یکپارچگی چرخهٔ عمر و اثر مالی — `v34.7.21`

> ✅ **اجرا شد (۲۰۲۶-۰۸-۱۷، نسخهٔ v34.7.21).** LC-01، LC-02 (با تصویب کارفرما) و LC-03 بسته شدند.
> رگرسیون: `_tools/uat/tester424-v34.7.21-lifecycle-returns.js` — ۳۱ PASS / ۰ FAIL؛ سوئیت کامل بدون شکست جدید.
> جزئیات: `RELEASE-NOTES-v34.7.21.md`.

### گام C1 — تعریف واحد «سند فعال» در سه موتور مالی (LC-01)
- **فایل‌ها:** `crm/commission.js:106`، `crm/working-capital.js:58`، `crm/fiscal.js:239,346`
- **اقدام:** استفاده از `PTF.ar.activeInvoice` با fallback رفتار فعلی (همان الگوی v34.7.18).
- **پذیرش:** فاکتور `superseded` در مبنای پورسانت، مطالبات سرمایه در گردش و مطالبات باز سال مالی شمرده نشود؛ تسترهای موجود پورسانت/سال مالی بدون شکست جدید.
- **اندازه:** S | ریسک: متوسط (عدد پورسانت تغییر می‌کند) → گزارش «قبل/بعد» روی دادهٔ واقعی پیش از انتشار

### گام C2 🔷 — احتساب مرجوعی فروش در چهار موتور (LC-02)
- **تصمیم لازم از کارفرما:** آیا مرجوعی باید از **مبنای پورسانت** و **سود پروژه** کسر شود؟ (پیشنهاد فنی: بله، با تاریخ اثر مرجوعی)
- **فایل‌ها:** `crm/fx.js` (سود)، `crm/commission.js` (پورسانت)، `crm/working-capital.js`، `crm/fiscal.js`
- **اقدام:** یک تابع مشترک `PTF.ar.returnsForInvoice(inv)` در `ar-reconcile.js` (منبع واحد، مطابق منطق موجود `customer-finance.js`) و مصرف آن در چهار موتور.
- **پذیرش:** برای فاکتور با مرجوعی، هر چهار خروجی مبلغ خالص را نشان دهند و با گردش حساب مشتری هم‌خوان شوند.
- **اندازه:** M | ریسک: متوسط‌رو‌به‌بالا (اعداد گزارش‌های مدیریتی تغییر می‌کند)

### گام C3 — افزودن دو دستهٔ جدید به گزارش تسویه (LC-03)
- «فاکتور superseded که هنوز در پورسانت/سرمایه در گردش شمرده می‌شود» و «مرجوعی لحاظ‌نشده در موتورهای مالی» به `PTF.ar.reconcile()`.
- **اندازه:** S | ریسک: کم (فقط‌خواندنی)

### گام C4 — تستر `tester424-v34.7.21-lifecycle-returns.js` + گیت خروج

---

## فاز D — گاردهای پیشگیرانه — `v34.7.22`

### گام D1 — رد متمم غیرمعتبر (AW-01)
- **فایل:** `api/sales-domain.php` — `mark_amendment` (`:705`) و `win_offer` شاخهٔ `attachCaseId` (`:731-741`)
- **اقدام:** اگر `sd_offer_total($offer) <= 0` → خطای صریح `invalid_amendment_amount` (۴۲۲). پیام کلاینت: «متمم کاهشی در معماری فعلی پشتیبانی نمی‌شود؛ کاهش باید در فاکتور/اصلاحیه منعکس شود.»
- **پذیرش:** offer با total صفر/منفی نه به‌عنوان متمم مارک شود نه متصل؛ متمم مثبت بدون تغییر کار کند.
- **اندازه:** XS | ریسک: کم

### گام D2 — هشدار پروندهٔ موازی (AW-02)
- **فایل:** `crm/sales-domain-v2.js` — `ptfSalesWinOffer` پیش از فراخوان `win_offer`
- **اقدام:** اگر پروندهٔ فعالی با همان `inqNo` + همان `buyerCd` وجود دارد و offer به آن link نشده → دیالوگ سه‌گزینه‌ای: «اتصال به پروندهٔ موجود (متمم)» / «تشکیل پروندهٔ مستقل با تأیید صریح» / «انصراف». (رفتار فعلی فقط برای offerهای مارک‌شده به‌عنوان متمم دیالوگ دارد.)
- **پذیرش:** برنده‌کردن CO هم‌استعلامِ بدون علامت، بدون تأیید صریح پروندهٔ دوم نسازد.
- **اندازه:** S | ریسک: کم (فقط دیالوگ اضافه)

### گام D3 — تکمیل کشف وابستگی حذف (AW-03)
- **فایل:** `api/sales-domain.php` — `admin_delete_plan`
- **اقدام:** افزودن کشف (نه cascade) برای چک‌های متصل، خرید واقعی/`buycmp`، `payables`، `sales_returns`، packing و QC؛ نمایش در پیش‌بررسی حذف.
- **پذیرش:** plan برای پروندهٔ دارای این وابستگی‌ها، فهرست کامل بدهد؛ رفتار commit تغییر نکند.
- **اندازه:** M | ریسک: کم (فقط گزارش)

### گام D4 — بهداشت نسخه (OPS-01)
- **فایل:** `api/sales-domain.php:577,594,596` → خواندن نسخه از یک ثابت واحد هم‌راستا با `PTF_CRM_RELEASE`.
- **اندازه:** XS

### گام D5 — تستر `tester425-v34.7.22-award-guards.js` + گیت خروج

---

## فاز E — ابطال سروری صورتحساب غیررسمی — `v34.7.23`

**پیش‌نیاز:** فاز A (گارد) و فاز B (مسیر صدور سالم).

### گام E1 — افزودن اکشن `void_unofficial_invoice`
- **فایل:** `api/sales-domain.php`
- **رفتار الزامی:** نقش `SD_FIN_ROLES`، دلیل اجباری، قفل دورهٔ مالی، `status='void'` + مهر/کاربر/دلیل، ثبت `corrections` با `beforeSnapshot`، سپس `sd_rebuild_allocations($caseId, …, $cases)`؛ رسیدها **حذف نمی‌شوند** و مازاد به `creditRemainIRR` برمی‌گردد؛ خروجی شامل `reconcile`.
- **پذیرش:** پس از ابطال، مجموع `amountIRR = allocatedIRR + creditRemainIRR` برای همهٔ رسیدهای پرونده برقرار بماند و هیچ رسیدی خارج از آن پرونده تغییر نکند.

### گام E2 — اتصال کلاینت و بازنشستگی مسیر محلی
- `crm/unofficial-invoice.js:2139` (TODO) به فراخوان واقعی وصل شود؛ گارد فاز A برداشته و بلوک نوشتن محلی حذف شود (نه فقط غیرفعال).
- در صورت خطای شبکه: هیچ نوشتن محلی‌ای انجام نشود (fail-closed).

### گام E3 — تستر `tester426-v34.7.23-unofficial-void-server.js` + سناریوی پذیرش «غیررسمی موجود» که در گزارش تلفیقی «متوقف» اعلام شده بود.

---

## فاز F — تکمیل تصمیم‌یار و تحلیلگر — `v34.7.24`

ترتیب داخل فاز بر اساس ارزش مدیریتی:

| گام | آیتم | اقدام | اندازه |
|---|---|---|---|
| F1 | AN-01 | پارامتر بازه در `ptfManagementIntelligence({from,to,basis})` + درج بازه در snapshot/PDF؛ گزارش هفتگی واقعاً هفتگی شود | M |
| F2 | AN-08 | تقویم شمسی برای دوره و زمان‌بندی گزارش (`reportPeriod`, `ptfManagementReportCheck`) | S |
| F3 | AN-02 | مهاجرت `insights.js` به `PTF.metrics.winStats` | S |
| F4 | AN-03 | افزودن سنجهٔ سطح «فرصت» (`inqNo`) کنار سنجهٔ سند | M |
| F5 | AN-05 | بازتعریف «پروندهٔ نیازمند بررسی» به سه دستهٔ تأخیر/QC/رکود | S |
| F6 | AN-06 | خواندن «خرید واقعی» از `ptf_crm_buycmp` به‌جای Regex متن | S |
| F7 | AN-07 | اعمال `ptfCanSeeLedger` روی snapshot ارسالی به AI + ثبت دفتر در audit | S |
| F8 | AN-04 🔷 | کالیبراسیون `healthScore` (ضرایب مصوب یا رتبه‌بندی چارکی) | M |

**گیت خروج:** `tester427-v34.7.24-analytics-completion.js` + به‌روزرسانی `tester420`.

---

## فاز G — اصلاح ریشه‌ای معماری — `v34.8.0` (پروژه‌ای)

🔷 **پیش‌نیاز مطلق: تصویب مدل تجاری توسط کارفرما** (سند تصمیم جدا، پیش از کدنویسی).

### G0 — سند طراحی و تصمیم (بدون کد)
مدل نسخهٔ مؤثر قرارداد: `effectiveContractAmount`، `amendmentType = increase|decrease|replacement`، `supersedesOfferId/supersededByOfferId`، سیاست تأیید کاهش، و نگاشت خط برای خرید/QC/حمل/موجودی.

### G1 — فرمان `replace_award / revise_case_contract` (ARCH-01)
مطابق ۱۴ الزام بخش ۱۵ گزارش تلفیقی؛ اتمیک، idempotent، role-protected، با correction و بازسازی تخصیص در همان تراکنش.

### G2 — یکپارچه‌سازی مسیرهای نوشتن پول (ARCH-02) 🔷
تبدیل `savePay` و وصول چک به `post_receipt` + مهاجرت کنترل‌شدهٔ `payments[]` باقی‌مانده + آموزش کاربر. (امروز عمداً انجام نشده؛ شرط اجرا: پوشش کامل تست و تأیید کارفرما.)

### G3 — تضمین snapshot سند برد (ARCH-03)
ساخت `awardDocs` در همان فرمان سروری برد، با علامت‌گذاری صریح رکوردهای lazy قدیمی.

### G4 — مهاجرت و backfill پرونده‌های legacy + تستر جامع `tester430`.

---

## فاز H — بدهی تست و بهداشت (موازی، بدون وابستگی)

| گام | اقدام | اندازه |
|---|---|---|
| H1 | اعمال patchهای `_tools/PENDING-workflow-php-lint-*.patch` و `PENDING-workflow-ci-gate-*.patch` (نیازمند دسترسی workflow در گیت‌هاب) | S |
| H2 | Triage ۶۲ تستر قرمز: تفکیک «قرارداد منسوخ» از «باگ واقعی»؛ به‌روزرسانی یا بایگانی با دلیل مکتوب | L |
| H3 | افزودن `php -l` واقعی در محیط CI (در sandbox محلی PHP نصب نیست) | XS |
| H4 | به‌روزرسانی `AR-SINGLE-SOURCE-CONTRACT` و `PTF-MASTER-HANDOVER` پس از هر فاز | XS |

---

## ۲) ترتیب اجرا و وابستگی‌ها

```
A (ایمنی داده)  ─┬─► B (بازگرداندن قابلیت) ─► E (ابطال سروری) ─┐
                 │                                             ├─► G (ریشه‌ای)
                 ├─► C (چرخهٔ عمر و مالی) ──────────────────────┤
                 └─► D (گاردها) ───────────────────────────────┘
F (تحلیلگر) و H (بدهی تست): موازی، بدون وابستگی به بقیه
```

**قاعدهٔ توقف:** اگر گیت خروج هر فاز سبز نشد، فاز بعد شروع نمی‌شود.

---

## ۳) جدول خلاصهٔ فازها

| فاز | نسخه | آیتم‌ها | اندازهٔ کل | ریسک | نیاز به تأیید کارفرما |
|---|---|---|---|---|---|
| A | v34.7.19 | AR-01، AR-02 | XS+XS | کم | ✅ انجام شد |
| B | v34.7.20 | UI-01، UI-02، UI-03 | S | متوسط (B2) | ✅ انجام شد |
| C | v34.7.21 | LC-01، LC-02، LC-03 | M | متوسط‌رو‌به‌بالا | ✅ انجام شد (LC-02 مصوب شد) |
| D | v34.7.22 | AW-01، AW-02، AW-03، OPS-01 | M | کم | خیر |
| E | v34.7.23 | INV-01 | M | متوسط | خیر |
| F | v34.7.24 | AN-01…AN-08 | L | کم | بله (AN-04) |
| G | v34.8.0 | ARCH-01…ARCH-03 | XL | بالا | بله (کل فاز) |
| H | موازی | OPS-02، OPS-03 | L | کم | خیر (H1 نیازمند دسترسی) |

---

## ۴) تعریف «انجام‌شده» (DoD) برای هر آیتم

1. کد تغییر‌یافته + کامنت فارسی با ارجاع به کد آیتم (مثلاً `AR-02`).
2. تستر رفتاری (اجرای واقعی ماژول در sandbox، نه فقط grep).
3. ورود تستر به `run-ci-gate.js` و فایل‌های جدید به فهرست `SYNTAX`.
4. اجرای کامل سوئیت + مقایسه با baseline و اثبات صفر شکست جدید.
5. به‌روزرسانی release notes + بهداشت نسخه در ۶ نقطه (`index.html`, `sw.js`, `manifest.json`, `clear-cache.html`, `shell.js`, `VERSION.json`).
6. به‌روزرسانی سند قرارداد یا RCA مربوطه با وضعیت جدید.

---

## ۵) ریسک‌ها و کاهش آن‌ها

| ریسک | کاهش |
|---|---|
| تغییر عدد پورسانت/سود پس از فاز C و اعتراض کاربران | تولید گزارش «قبل/بعد» روی دادهٔ واقعی و تأیید مالی پیش از انتشار |
| نبود PHP در محیط توسعه → خطای نحوی دیرکشف | بررسی ساختاری محلی + `php -l` در CI (H3) + استقرار اول روی استیجینگ |
| رگرسیون خاموش در مسیرهای پول (B2, E2) | fail-closed، تست دستی استیجینگ، و امکان بازگشت تک‌کامیتی هر فاز |
| فاز G دامنه‌گستری کند (scope creep) | G0 به‌عنوان دروازهٔ مکتوب؛ بدون تصویب مدل، کدنویسی شروع نشود |
| اصلاح داده‌های خراب‌شدهٔ قبلی (`creditRemainIRR`) | پس از فاز A، یک ابزار ترمیم idempotent با پیش‌نمایش و تأیید مدیر (زیرگام A4 اختیاری) |

---

## ۶) پیشنهاد شروع

فاز A فقط دو تغییر کوچک است (یک گارد کلاینتی + یک خط در سرور) و بیشترین ریسک واقعی داده را می‌بندد. پیشنهاد می‌کنم همین حالا اجرا شود و فازهای بعد به‌ترتیب و با گیت خروج پیش بروند.
