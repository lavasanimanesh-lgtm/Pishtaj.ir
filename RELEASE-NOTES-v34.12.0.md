# v34.12.0 — فیکس ثبت نقشه + فاز S3 سئو: رشد داده‌محور (۲۰۲۶-۰۹-۰۱)

> گیت: **173 PASS / 0 FAIL** (tester557: ۲۷ سنجه؛ مجموع تسترهای سئو ۱۰۴ سنجه). PHP لینت با php-parser.

## ۰) 🔴 فیکس «ثبت نقشه در سرچ کنسول ناموفق» (گزارش مالک)
ریشه: دو عامل رایج که تا امروز با خطای خام (`api_error_403`) بی‌تشخیص برمی‌گشت:
1. سرویس‌اکانت در Search Console سطح **Restricted** دارد — PUT نقشه فقط با **Full** مجاز است.
2. `site_url` کانفیگ با نوع پراپرتی واقعی نمی‌خواند (`sc-domain:` در برابر URL-prefix).

**رفع — `gsc_resolve_site`**: فهرست سایت‌های قابل‌دسترسی توکن خوانده می‌شود؛ بهترین تطبیق (دقیق → `sc-domain:pishtaj.ir` → prefix) خودکار انتخاب می‌شود؛ اگر پراپرتی گم باشد یا سطح کافی نباشد، **خطای فارسی دقیق** با ایمیل سرویس‌اکانت و مسیر ارتقا (Settings ← Users and permissions) برمی‌گردد. کلاینت هم پیام کامل را نشان می‌دهد و فقط برای خطاهای خام راهنمای عمومی می‌افزاید.

## ۱) 📸 اسنپ‌شات روزانهٔ GSC — بدون cron
- هر بار باز شدن پنل، اگر ≥۲۰ ساعت از آخرین اسنپ‌شات گذشته: جمع + ۳۰ کوئری + ۳۰ صفحهٔ برتر در `crm/data/gsc-snaps/<date>.json`
- نگهداری ۱۸۰ روز؛ اکشن `snaps` سری زمانی + دلتای درصدی دو نقطهٔ آخر

## ۲) 📈 KPI روند در پنل GSC
- اسپارک‌لاین کلیک ۳۰ نقطهٔ آخر + «مقایسهٔ دو اسنپ‌شات آخر»: کلیک/نمایش با درصد رنگی (سبز/قرمز)

## ۳) 🧠 برنامهٔ محتوا با خوشه‌بندی AI
- دکمهٔ «تحلیل فرصت‌ها و پیشنهاد» → اکشن `seo_clusters` در `api/llm.php`:
  - ورودی: فرصت‌های سریع + کوئری‌های بدون‌کلیک + صفحات موجود
  - خروجی: ۳–۶ خوشهٔ موضوعی با تصمیم **«صفحهٔ جدید» / «بهینه‌سازی صفحهٔ موجود»** و دلیل (قانون سخت: جایگاه ≤۵ ⇒ هرگز صفحهٔ جدید پیشنهاد نشود)
  - هر خوشه یک‌کلیک: «📝 ساخت مقالهٔ جدید» (پرش به فرم مرکز دانش با موضوع پیش‌پر) یا «🔍 بهینه‌سازی در تب سئو» (جست‌وجوی همان کلمه در اسکنر)

## نگهداشت
- انکرِ بلوکِ تستر556 نسخه‌زدایی شد (تلهٔ شناخته‌شدهٔ bump رجیستری: انکر معنایی ≠ پین نسخه)
- نسخه: ۶ نقطهٔ رسمی + ۱۰۹ cache-buster + ۵۳۱ پین تستر

## بعدی (S2-id → سپس S4)
- قالب‌های نوع صفحه برای services/industries/comparisons
- S4: انتشار زمان‌بندی‌شده، diff/rollback UI، داشبورد هزینهٔ AI، PageSpeed
