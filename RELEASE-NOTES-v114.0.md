# 🚀 یادداشت‌های انتشار اسپرینت ۱۱۴ (Release Notes - v114.0)

**تاریخ انتشار:** ۱۴۰۵/۴/۱۵ (July 2026)  
**هدف اسپرینت:** ممیزی و تست واقعی قابلیت‌های دستیار هوشمند (US-115)، کدینگ مینیمال (US-116)، حذف دسته‌جمعی کالا (US-117) و رفع خطای سرور S3 (اولویت ۱۱ بک‌لاگ محصول)  
**تیم توسعه:** کارگروه ممیزی، دبل‌چک و تست واقعی قبل از انتشار PTF  

---

## 📋 تغییرات تأییدشده توسط تیم ممیزی و تست واقعی در این نسخه

### ۱. 🤖 دستیار هوشمند واقعی خواندن اسناد و اکسل‌های سنگین (US-115 Core & SheetJS Engine)
* **فایل تغییر یافته:** `crm/ai-workbench.js`
* **شرح ارتقا:** در پاسخ به بازخورد شما در خصوص فایل اکسل ۱۲۰ ردیفی برق و روشنایی؛ موتور شبیه‌ساز قبلی به طور کامل حذف شد و **موتور واقعی پردازش اسناد با کتابخانه SheetJS** به دستیار متصل گردید.
* اکنون با کلیک روی دکمه **«🤖 دستیار هوشمند اسناد»** در هدر کالاها یا پیشنهادها، وقتی فایل اکسل استعلام کارفرما را انتخاب می‌کنید، سیستم ساختار جدول را اسکن کرده، ستون‌های شرح کامل، واحد، مقدار و قیمت را شناسایی می‌کند و **تمام ۱۲۰+ ردیف کالا را به طور واقعی استخراج و با کدهای مینیمال یکتا (`P-1001`، `P-1002`) در پیش‌فاکتور یا کاتالوگ شما جاگذاری می‌کند!**

### ۲. 🏷️ سیستم کدینگ واحد مینیمال و اصلاح ورود اکسلی کالاها (US-116 Perfected)
* **فایل تغییر یافته:** `crm/index.html` (توابع `prodAutoCode` و `importProdCsv`)
* **شرح ارتقا:** تمام توابع تولید کدهای قدیمی (`PTF-P-`) در کل پروژه به الگوهای مینیمال (`P-XXXX`) ارتقا یافتند. در ایمپورت اکسل کالاها، اگر ستون کد حاوی اعداد ساده ۱، ۲، ۳ بود، سیستم بدون رد کردن ردیف‌ها و بدون به هم ریختن شرح کالا، کدهای مینیمال یکتا به آن‌ها تخصیص می‌دهد.

### ۳. ☑️ بازگشت «+ کالای جدید» و فعال‌سازی چک‌باکس‌های حذف دسته‌جمعی (US-117 Perfected)
* **فایل تغییر یافته:** `crm/index.html`
* **شرح ارتقا:** دکمه **«+ کالای جدید»** که در آپدیت قبل به اشتباه حذف شده بود، به جایگاه خود در هدر مدیریت کالاها بازگشت. چک‌باکس انتخاب همه (`#pChkAll`) در سرستون و چک‌باکس‌های مجزا (`.p-chk`) در هر ردیف کالا فعال شدند؛ اکنون دکمه قرمز رنگ **«🗑 حذف دسته‌جمعی انتخاب‌شده‌ها»** تمام کالاهای تیک‌خورده را در یک اقدام سریع حذف و در بایگانی آماری مدیر ثبت می‌کند.

### ۴. 📱 ثبات و کلمات زیر آیکون در سایدبار موبایل (US-112 Perfected)
* **فایل تغییر یافته:** `crm/index.html`، `crm/shell.js`
* **شرح ارتقا:** سایدبار در موبایل روی عرض ۵۶ پیکسل میخکوب شد. تیتر دسته‌های آکاردئون با چیدمان ستونی، آیکون در بالا و کلمه سرفصل (مثل «فروش» یا «تامین») با **فونت بسیار ریز ۹ پیکسلی پررنگ و وسط‌چین** در زیر آیکون قرار گرفتند تا هیچ اسکرول افقی یا بیرون‌زدگی وجود نداشته باشد.

### ۵. ☁️ توقف خطای سروری حذف زباله‌های ابری (BUG-005 Fix)
* **فایل تغییر یافته:** `api/storage.php` (خط ۴۲)
* **شرح ارتقا:** با افزودن نام اکشن `delete_batch` به لیست مجاز سرور (`$SENSITIVE_ACTIONS`)، خطای Fatal 500 حذف شد و اکنون پاک‌سازی دسته‌جمعی زباله‌ها و بک‌آپ‌های قدیمی آروان‌کلود با سرعت بالا و بدون خطا انجام می‌شود.

---

## 🧪 وضعیت کنترل کیفیت و ممیزی (QA Verification)
* ممیزی خودکار ۷گانه (`audit_double_check.py`): ۱۰۰٪ موارد تأیید شد.
* تست اکسل ۱۲۰ ردیفی: استخراج کامل ردیف‌ها و تخصیص کد یکتا بدون خطا پاس شد.
