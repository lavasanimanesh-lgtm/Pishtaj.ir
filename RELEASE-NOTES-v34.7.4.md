# Release Notes v34.7.4 — یکسان‌سازی ضمائم درخواست و تعاملات موبایل

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## یکسان‌سازی ضمائم درخواست‌ها

- ریشه تفاوت نشان ضمیمه، شمارش صرفاً از ساختار جدید `r.files{category:[]}` بود؛ درخواست‌های قدیمی یا سایت با `file`, `attachment`, `attachments`, `docs` و نام‌های متفاوت key بدون نشان باقی می‌ماندند.
- projection واحد `ptfRfqAttachmentRows` ساختارهای جدید، آرایه‌ای، map دسته‌ها، رشته/JSON قدیمی، `objectKey/storageKey/fileKey/path` و URL/DataURL را جمع و بر اساس کلید/URL deduplicate می‌کند.
- همه درخواست‌ها اکنون یکی از دو نشان را دارند:
  - `📎 N ضمیمه`
  - `📎 بدون ضمیمه`
- همین قاعده برای درخواست‌های در انتظار تایید سایت نیز اعمال شد.
- مدیر ضمائم، Viewer قدیمی offerlock و مسیر AI همگی از همین projection استفاده می‌کنند.
- ضمائم قدیمی/سایت در بخش جدا دیده می‌شوند؛ فایل ابری قابل مشاهده است و مرجع هاست قدیمی با توضیح مشخص نمایش داده می‌شود.

## رفع جستجو و تب درخواست‌ها در موبایل

- ریشه مشکل، CSS کارت موبایل با `display:block!important` روی ردیف‌های جدول بود که `row.style.display='none'` فیلتر را بی‌اثر می‌کرد.
- helper واحد `ptfSetRowVisible` هم‌زمان class مهم، `hidden`, `aria-hidden` و inline display را مدیریت می‌کند.
- جستجوی درخواست و تب‌های همه/بدون پیشنهاد/دارای پیشنهاد اکنون در دسکتاپ و موبایل یک رفتار دارند.
- ردیف پیام «نتیجه‌ای نیست» نیز با همان مسیر کنترل می‌شود.

## بررسی parity سایر بخش‌های موبایل

همه محل‌های فیلتر مستقیم ردیف جدول بررسی و اصلاح شدند:

- جستجوی درخواست‌ها
- جستجوی مشتریان
- جستجوی تامین‌کنندگان
- فیلتر «مشتریان من»
- تب مبدا تامین‌کنندگان در ماژول چک

همچنین قابلیت مرتب‌سازی که در دسکتاپ روی سرستون بود و در کارت موبایل به‌دلیل مخفی‌شدن `thead` قابل دسترسی نبود، اکنون به select «مرتب‌سازی کارت‌ها» با حالت پیش‌فرض، صعودی و نزولی تبدیل می‌شود. تغییر عرض/Pin ستون عمداً در نمای کارتی موبایل کاربرد ندارد.

## کنترل کیفیت

- `tester409-v34.7.4-rfq-mobile-parity.js`: 16 PASS / 0 FAIL
- CI متمرکز ریلیز: 28 PASS / 0 FAIL
- رگرسیون کامل: 392 تستر، 335 فایل PASS، 57 فایل بدهی تاریخی FAIL و بدون شکست جدید نسبت به v34.7.3.
- Auditor و هم‌ترازی VERSION/PWA: PASS
- syntax مسیرهای bridge/offerlock/inqreader و runtime موبایل در CI افزوده شد.
