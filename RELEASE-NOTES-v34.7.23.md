# Release Notes v34.7.23 — فاز E: ابطال سروری صورتحساب غیررسمی

تاریخ: ۱۴۰۵/۰۵/۲۶ (2026-08-17)

## زمینه

گزارش تلفیقی سناریوی «صورتحساب غیررسمی موجود» را **متوقف** اعلام کرده بود، چون صدور سروری بود ولی ابطال فقط محلی — و آن مسیر محلی، تخصیص‌های سروری را نمی‌شناخت. در فاز A آن مسیر fail-closed شد؛ فاز E جایگزین واقعی را می‌سازد و آن سناریو را باز می‌کند.

## INV-01 — فرمان اتمیک `void_unofficial_invoice`

فرمان جدید در `api/sales-domain.php`، دقیقاً قرینهٔ `void_invoice` اما مخصوص اسناد غیررسمی:

| قاعده | رفتار |
|---|---|
| دسترسی | فقط `SD_FIN_ROLES` |
| نوع سند | سند رسمی رد می‌شود (`official_invoice_requires_void_invoice`) |
| ابطال دوباره | رد می‌شود (`already_void`) |
| دورهٔ مالی | قفل رعایت می‌شود (`fiscal_period_locked`) |
| دلیل | اجباری (`reason_required`) |
| سند | حذف نمی‌شود؛ `status='void'` با کاربر/زمان/دلیل + `correction` با `beforeSnapshot` |
| رسیدها | **هرگز حذف یا ابطال نمی‌شوند** |
| تخصیص | با `sd_rebuild_allocations` (به‌همراه فهرست پرونده‌ها) بازسازی می‌شود |
| مبلغ آزادشده | به `creditRemainIRR` همان پرونده برمی‌گردد و در پاسخ (`caseCreditIRR`) گزارش می‌شود |

## مسیر کلاینت

- `window.ptfUnofficialInvoiceVoidServer(invoiceId, reason)` در `crm/sales-domain-v2.js` با گارد نقش مالی و کلید idempotency.
- `ptfUnofficialInvoiceVoid` در حالت v35 **به‌کلی** به این مسیر واگذار می‌شود؛ بلوک نوشتنِ مالیِ محلی در این مسیر اجرا نمی‌شود.
- گاردهای ابطال (نقش، وجود سند، فقط غیررسمی، ابطال‌نشده، سال مالی باز، دلیل و تأیید دومرحله‌ای) در تابع مشترک `ptfUnofficialInvoiceVoidLocalGuards` متمرکز شدند تا مسیر سروری و مسیر legacy هرگز واگرا نشوند.
- آثار **غیرمالی** — ابطال مرجوعی‌های متصل، جداکردن ضمیمه از اسناد پرونده و ثبت timeline — در `ptfUnofficialInvoiceVoidAfterEffects` و **فقط پس از تأیید سرور** اجرا می‌شوند.
- در هر خطای سرور (قفل دوره، دسترسی، سند رسمی، سند ناموجود) پیام فارسی نمایش داده می‌شود و **هیچ تغییری ثبت نمی‌شود**.
- اگر ماژول دامنهٔ فروش بارگذاری نشده باشد، مسیر fail-closed می‌ماند.

## چرا این تغییر «بدون آسیب» است

- مسیر legacy (بدون `PTF_SALES_DOMAIN_V2`) دست‌نخورده است و تست آن هم اضافه شد.
- حفاظت ساختاری فاز A (محدودبودن بلوک محلی به «همان پرونده» و پشتیبانی از هر دو schema) در کد باقی ماند و آزمون دارد؛ اگر روزی آن مسیر دوباره فعال شود، خرابی سراسری N1 تکرار نمی‌شود.
- هیچ رکورد پولی حذف نمی‌شود؛ ابطال = علامت‌گذاری + سند اصلاحی، مطابق قرارداد `AR-SINGLE-SOURCE-CONTRACT`.

## تست رگرسیون

- `_tools/uat/tester426-v34.7.23-unofficial-void-server.js`: **42 PASS / 0 FAIL** — شامل اجرای واقعی مسیر کلاینت در شش سناریو (موفق، دلیل خالی، انصراف کاربر، نقش غیرمجاز، سند رسمی، نبود ماژول سرور) و اثبات اینکه کلاینت هیچ‌گاه تخصیص/بستانکاری/وضعیت سند را خودش نمی‌نویسد؛ به‌علاوه آزمون رفتاری آثار غیرمالی.
- `tester422` (فاز A) با قرارداد جدید به‌روز شد: **30 PASS / 0 FAIL** — قرارداد پایدارش اکنون «در معماری v35 هیچ نوشتن محلی‌ای انجام نمی‌شود» است.
- گیت CI: **۴۱ PASS / ۲ FAIL** — همان دو شکست پیش‌ازموجود.
- سوئیت کامل (۴۰۹ تستر): **۶۲ شکست، صفر شکست جدید** نسبت به baseline فاز D.
- بهداشت نسخه در ۷ نقطه (شامل `SD_SERVICE_VERSION`) روی `v34.7.23`.

## اثر روی توصیه‌های گزارش تلفیقی

سطر «غیررسمی موجود و رسمی صادر نشده = **توقف؛ مسیر ایمنی وجود ندارد**» در جدول تصمیم‌نامه دیگر معتبر نیست: مسیر ایمن سروری اکنون موجود است. دو پیش‌نیاز دیگری هم که گزارش برای باز شدن این سناریو گذاشته بود (رفع باگ تطبیق شناسه و اصلاح شاخهٔ صدور مجدد) در فاز B بسته شده‌اند.

## گام بعد

فاز F: تکمیل تصمیم‌یار و تحلیلگر (بازهٔ زمانی گزارش‌ها، تقویم شمسی، سطح فرصت، کالیبراسیون امتیاز سلامت و چهار مورد دیگر).
