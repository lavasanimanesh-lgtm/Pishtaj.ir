# RELEASE NOTES — v34.7.81 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## سرعت بخشیدن به ماژول تامین‌کنندگان + ثبت‌نام‌های سایت (SUP-PERF-001)

### درخواست

بررسی شود چرا ماژول تامین‌کنندگان (به‌ویژه تب «🌐 درخواست‌های سایت») دیر لود می‌شود و
آیا راهی برای افزایش سرعت وجود دارد.

### ریشه‌یابی‌ها

1. **صندوق سایت هر ۴۵ ثانیه بدون فشرده‌سازی دوباره دانلود می‌شود** — در `api/crm.php`
   هر فراخوانی `get_inbox` دو فایل کامل `suppliers.json` و `rfqs.json` را بدون gzip
   به مرورگر می‌فرستاد؛ و `bridge.js` همین صندوق را در بوت CRM و هر ۴۵ ثانیه صدا می‌زد.

2. **جدول «ثبت‌نام‌شده از سایت» همیشه ساخته می‌شد** — `renderSupPending` همه رکوردها را
   (انتظار/تایید/رد) با دکمه‌های inline می‌ساخت حتی وقتی تب «درخواست‌های سایت» باز نبود.

3. **کادر «فاکتور، حساب و پرداخت تامین‌کنندگان» محاسبه دوباره داشت** — در `box()` برای
   هر تامین‌کننده `balance(s.cd)` محاسبه و سپس دوباره داخل `balanceHtml(s.cd)` تکرار
   می‌شد؛ `balance` روی همه فاکتورها/پرداخت‌ها/تسویه‌ها لوپ می‌زند.

4. **جدول پایه تامین‌کنندگان O(n²) بود** — `renderSuppliers` در `crm/index.html` با
   `tb.innerHTML +=` در حلقه ردیف می‌ساخت (تخریب/بازسازی مکرر DOM).

### تغییرات

| فایل | تغییر |
|---|---|
| `api/crm.php` | `get_inbox` با `ptf_echo_json` (gzip) + امضای ارزان mtime/size دو فایل؛ وقتی داده‌ها تغییر نکرده فقط `{fresh:true}` برمی‌گردد. |
| `crm/bridge.js` | `syncServerInbox` پارامتر `since` می‌فرستد و در پاسخ `fresh` از دانلود/رندر مجدد صندوق صرف‌نظر می‌کند؛ امضای جدید در localStorage ذخیره می‌شود. |
| `crm/bridge.js` | `renderSupPending` صفحه‌بندی ۵۰تایی + دکمه «⬇ نمایش بیشتر»؛ جدول ثبت‌نام سایت فقط در تب سایت ساخته می‌شود و بج تب همیشه به‌روز می‌ماند. |
| `crm/cheques.js` | با انتخاب تب «درخواست‌های سایت» بلافاصله `renderSupPending` ساخته می‌شود. |
| `crm/index.html` | `renderSuppliers` رشتهٔ جدول یک‌باره (`tb.innerHTML = h`) + فیلتر جستجوی سمت کلاینت. |
| `crm/supplier-finance.js` | معرفی `balanceHtmlFrom` برای نمایش مانده از روی آرایهٔ محاسبه‌شده و اجتناب از محاسبهٔ دوباره `balance` در کادر مالی. |
| `VERSION.json`, `crm/*`, `api/sales-domain.php`, `_tools/uat/*` | ارتقای شماره نسخه به `v34.7.81` و بازسازی cache-bust/تست‌ها. |

### تست

- `_tools/uat/tester483-v34.7.81-supplier-perf.js` (جدید) در گیت CI اضافه شد.
- تست‌های موجود `_tools/uat/*` به نسخه `v34.7.81` هم‌گام شدند.

### وضعیت

- ✅ کامیت روی شاخه `arena/01a02872-pishtaj-ir` (پس از همگام‌سازی با `arena/01a02288-pishtaj-ir`).
- گام‌های بعدی پیشنهادی: lazy-load مالی تامین‌کنندگان، صفحه‌بندی فهرست تاییدشده، و
  خواندن نقطه‌ای inbox از MySQL (مستند در `ASSESSMENT-SUPPLIER-PERF-2026-08-22.md`).
