# 🔒 ریلیز v18.9 — قفل معماری پرونده فروش و باگ‌های فوری R12

**اسپرینت:** قفل معماری و باگ‌های فوری  
**موارد:** BUG-028 + BUG-029 + US-431 فاز ۱

## ✅ BUG-028 — اصلاح نمایش واحد پول پیشنهاد‌های ارزی

### مشکل
در بعضی نماها، مبلغ پیشنهاد ارزی در fallback با «ت» نمایش داده می‌شد.

### رفع
- نمایش مبلغ در فهرست پیشنهادها fallback ارزآگاه شد.
- اگر `ptfMoney` آماده نباشد، باز هم سند ارزی با کد ارز خودش نمایش داده می‌شود، نه تومان.
- مسیر اصلی همچنان `ptfMoney(total, currency)` است.

---

## ✅ BUG-029 — وضعیت درخواست پس از خرید واقعی

### مشکل
درخواستی که خرید واقعی داشت، ممکن بود همچنان در وضعیت «منتظر قیمت» یا وضعیت قبل از تامین بماند.

### رفع
- تابع جدید:

```js
ptfRealBuyEnsureStatus(inqNo)
```

- پس از هر خرید واقعی موفق، وضعیت درخواست مرتبط به:

```text
🏭 در حال تامین توسط تامین‌کننده
```

می‌رود.

- اگر درخواست در وضعیت جلوتر باشد، عقب‌گرد نمی‌کند.
- گذار از مسیر واحد `ptfRfqSetStatus` انجام می‌شود و fallback داخلی هم دارد.

---

## ✅ US-431 فاز ۱ — پرونده فروش تنها منبع حقیقت پس از برد

### اجرا شد
پس از برنده شدن CO/TC و تشکیل پرونده فروش:

- پیشنهاد برنده در ماژول پیشنهادها read-only شد.
- ویرایش پیشنهاد برنده از ماژول پیشنهادها بسته شد.
- ایجاد نگارش جدید/رویژن از ماژول پیشنهادها بسته شد.
- حذف پیشنهاد برنده از ماژول پیشنهادها بسته شد.
- تغییر وضعیت مستقیم پیشنهاد برنده بسته شد.
- ذخیره فرم قدیمی روی پیشنهاد برنده بسته شد.
- ارجاع فاکتور از ماژول پیشنهادها بسته شد.

### رفتار جدید
در ماژول پیشنهادها، پیشنهاد برنده فقط برای مشاهده/چاپ باقی می‌ماند و کاربر به پرونده فروش هدایت می‌شود.

---

## معماری
- داده جدید ساخته نشد.
- پیشنهاد برنده در `ptf_crm_offers` باقی می‌ماند اما read-only است.
- پرونده فروش در `ptf_crm_deals` orchestrator فرایند Post-Award است.
- ادامه فرایند از پرونده فروش انجام می‌شود.

## فایل‌های تغییر یافته
- `crm/offers.js`
- `crm/rbac.js`
- `crm/buycompare.js`
- `crm/index.html`
- `crm/sw.js`
- `_tools/uat/tester105-v189.js`
- اصلاح تسترهای قبلی:
  - `tester7-sprint70.js`
  - `tester90-v172.js`
- `RELEASE-NOTES-v18.9.md`
- `PTF-MASTER-HANDOVER.md`

## 🔬 کیفیت
- تستر جدید: `tester105-v189.js` با ۱۹ چک.
- رگرسیون کامل: **۹۲ تستر / صفر خطا**.
- Audit سایت: PASS با همان هشدار قدیمی تصاویر سنگین.

## صف بعدی پیشنهادی
- US-432 — انضمام خودکار PDF پیشنهاد فنی و PDF پیشنهاد مالی برنده به پرونده فروش
- US-434 فاز ۱ — عملیات Post-Award از داخل پرونده فروش
