# گزارش وضعیت فعلی سئو و پیشنهاد گام‌های بعدی تا ثبات و آمادگی merge به main — ۲۰۲۶-۰۸-۰۶

## وضعیت فعلی پس از گام اخیر

### وضعیت فنی
- آخرین `origin/main` قبلاً در شاخه کاری merge شده است.
- ممیزی عمومی سایت پس از گام اخیر PASS است.
- `sitemap.xml` بازسازی شده و ۶۱۷ URL عمومی دارد.
- QA اختصاصی محصول: `product_pages=77` و `qa_errors=0`.
- در این فاز هیچ ویرایش دستی روی `crm/` یا `api/` انجام نشد.

### وضعیت کیفیت محتوای محصول/برند
طبق آخرین اجرای `python3 _tools/seo_content_quality_audit.py`:

- صفحات محصول بررسی‌شده: ۷۷
- صفحات برند بررسی‌شده: ۲۰
- صفحات `OK`: ۶۱
- صفحات دارای ریسک: ۳۶
- `LOW_DEPTH_REVIEW`: ۳۶
- `DUPLICATE_PARAGRAPH_ACROSS_PAGES`: ۲۷
- `REPEATED_PARAGRAPH_INSIDE`: ۱۷
- `UNDER_1500_CRITICAL`: صفر

## پیشرفت نسبت به وضعیت اولیه گیت کیفیت
در شروع فازهای کیفیت‌محور، وضعیت حدوداً چنین بود:
- صفحات `OK`: ۲۷
- صفحات پرریسک: ۷۰
- `DUPLICATE_PARAGRAPH_ACROSS_PAGES`: ۶۲
- `REPEATED_PARAGRAPH_INSIDE`: ۵۱

اکنون:
- `OK`: ۶۱
- صفحات پرریسک: ۳۶
- duplicate across: ۲۷
- repeated inside: ۱۷

یعنی بخش زیادی از ریسک تکرار و متن‌های کلیشه‌ای حذف شده، اما هنوز برای merge نهایی به main بهتر است یک مرحله پایدارسازی محتوایی دیگر انجام شود.

## تعریف پیشنهادی «ثبات قابل قبول» پیش از merge به main
پیشنهاد می‌شود تا قبل از درخواست merge نهایی به main، حداقل این معیارها برقرار شود:

1. `UNDER_1500_CRITICAL = 0` — اکنون برقرار است.
2. `REPEATED_PARAGRAPH_INSIDE` نزدیک صفر شود؛ هدف پیشنهادی: حداکثر ۳ تا ۵ صفحه.
3. `DUPLICATE_PARAGRAPH_ACROSS_PAGES` به کمتر از ۱۰ صفحه برسد.
4. همه صفحات اولویت‌دار محصول/برند که همزمان سه ریسک دارند، بازنویسی شوند.
5. Audit عمومی، sitemap و product QA همگی PASS باشند.
6. پس از اتمام فازهای محتوا، یک merge تازه از `origin/main` انجام و دوباره audit کامل اجرا شود.

## پیشنهاد تعداد گام‌های بعدی
با توجه به اینکه ۳۶ صفحه هنوز ریسک دارند و هر گام با کیفیت مناسب معمولاً ۵ صفحه را اصلاح می‌کند، پیشنهاد عملی چنین است:

### گام ۱ تا ۴ — کاهش ریسک‌های اصلی تکرار
- هر گام ۵ صفحه از صدر گزارش فعلی اصلاح شود.
- هدف: حذف بیشتر `REPEATED_PARAGRAPH_INSIDE` و کاهش شدید `DUPLICATE_PARAGRAPH_ACROSS_PAGES`.
- خروجی مورد انتظار پس از ۴ گام: حدود ۲۰ صفحه دیگر اصلاح شود و ریسک‌های تکرار به محدوده بسیار پایین برسد.

### گام ۵ تا ۶ — پاکسازی باقی‌مانده‌های محتوایی
- تمرکز روی صفحات باقی‌مانده‌ای که هنوز duplicate یا low-depth دارند.
- خروجی مورد انتظار: رسیدن به حدود ۱۰ تا ۱۵ صفحه ریسک کم‌اهمیت یا کمتر.

### گام ۷ — فاز ثبات و Merge Readiness
- اجرای کامل:
  - `python3 _tools/seo_content_quality_audit.py`
  - `python3 _tools/product_ux_polish_and_qa.py`
  - `python3 _tools/build_sitemap.py`
  - `python3 _tools/audit.py || true`
- مرور دستی ۱۰ صفحه نمونه از محصول/برند برای اطمینان از طبیعی بودن متن.
- merge مجدد `origin/main` در شاخه کاری.
- رفع فقط موارد عمومی/سئویی، نه CRM/API مگر دستور صریح کاربر.
- تهیه گزارش نهایی آماده‌سازی merge.

### جمع‌بندی تعداد گام پیشنهادی
- حداقل پیشنهادی برای ثبات قابل قبول: ۵ گام دیگر
- حالت مطمئن‌تر برای merge تمیز به main: ۷ گام دیگر
- اگر هدف رساندن تقریباً همه صفحات محصول/برند به `OK` باشد: حدود ۸ گام دیگر، چون ۳۶ صفحه ریسک‌دار باقی مانده و میانگین ظرفیت امن هر گام ۵ صفحه است.

## اولویت گام بعدی
طبق گزارش فعلی، گام بعدی بهتر است از این صفحات شروع شود:
1. `services/products/expansion-joints-flexible-hose.html`
2. `services/products/ansi-process-pump.html`
3. `services/products/vertical-multistage-pump.html`
4. `services/products/pressure-gauge.html`
5. `services/products/reciprocating-compressor.html`

## توصیه برای merge به main
در وضعیت فعلی، سایت از نظر audit عمومی PASS است و پیشرفت محتوایی بسیار خوب است؛ اما برای merge نهایی به main بهتر است هنوز عجله نشود. دلیل: هنوز ۳۶ صفحه دارای ریسک کیفیت هستند و ۲۷ صفحه duplicate across و ۱۷ صفحه repeated inside گزارش می‌شوند.

پیشنهاد: پس از ۵ تا ۷ گام دیگر و رسیدن به معیارهای بالا، یک merge تازه از `origin/main` انجام شود، audit کامل اجرا گردد، سپس برای merge/PR نهایی به main اقدام شود.
