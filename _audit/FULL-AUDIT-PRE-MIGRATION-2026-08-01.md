# بررسی کامل نرم‌افزار پیش از مهاجرت — یافته‌های اصلاح/بهبود/باگ (read-only)

**تاریخ:** ۱۴۰۵/۰۸/۱۰ (2026-08-01) | **برنچ:** `arena/019fbe19-pishtaj-ir` (HEAD `55b8f50`)
**روش:** بررسی فقط‌خواندنی (کدخوانی + اجرای ابزارها؛ **هیچ کدی تغییر نکرد**)
**نسخهٔ مبنا:** v33.3.2 (CRM) — ۸۸ فایل JS، ~۴۲,۵۰۰ خط
**وضعیت گیت:** درخت تمیز؛ PR #11 باز (جریان CRM)؛ PR #10 باز (سئو)

---

## ۱) پاسخ: «کارهای قدیمی انجام شد؟»

- **بله — کارهای فازبندی‌شدهٔ CRM (S1–S5) و بک‌لاگ کاربر (UR-01..UR-10) همگی انجام و در هند‌آف/PR ثبت شده‌اند.** تسترها: ۲۷۷ فایل؛ از این‌ها ۲۲۱ سبز.
- **PRهای باز (مرج‌نشده):** #11 (جریان CRM)، #10 (سئو ۹۹ مقاله)، #8 (هات‌فیکس users_sync)، #5 (RTL — قدیمی)، #3 (workflow deploy — آمادهٔ کپی). همهٔ این‌ها «کار باقی‌مانده» محسوب می‌شوند و برای شروع مهاجرت باید وضعیتشان روشن شود (حداقل #11 و #8).
- **بدهی تست قدیمی:** ۵۶ تستر شکست‌خورده از قبل موجود (عمدتاً انتظارات نسخهٔ قدیمی v15–v22 و عصر auth) — ثبت‌شده در هند‌آف.

---

## ۲) یافته‌های معماری و یکپارچگی

| # | شدت | یافته | جزئیات/دلیل |
|---|---|---|---|
| A1 | 🟠 زیاد | **عدم تطابق کلیدهای sync با سرور** | کلاینت `sync.js` ۵۵ کلید، `backup.js` ۵۴ کلید، سرور `sync_all_keys` ۵۲ کلید دارد. تفاوت: `session/token/token_role` (محلی — طبیعی) ولی باید بررسی شود کلید جدیدی (مثل `catalog_merges`) در همه جا هست. |
| A2 | 🟡 متوسط | **ماژول‌های بدون ارجاع/یتیم** | `ai-tech-assistant.js`, `eng-calc.js`, `lead-finder.js`, `tech-proposals.js`, `xss-guard.js` در `index.html`/`sw.js` ارجاع مستقیم ندارند (شاید dynamic/کامنت). نیاز به راستی‌آزمایی که واقعاً لود می‌شوند یا حذف/اضافه شوند. |
| A3 | 🟡 متوسط | **گسترش alert() خام (۵۷۲ مورد در ۶۱ فایل)** | با وجود `ptfToast`/`ptfDialog`، اکثر اعتبارسنجی‌ها با `alert()` بومی‌اند — UX ناهماهنگ (به‌خصوص موبایل) و مسدودکننده. پیشنهاد: مهاجرت تدریجی به toast برای پیام‌های غیربحرانی. |
| A4 | 🟢 کم | **کد تکراری/الگوهای موازی** | توابع مشابه در چند فایل (مثل نرمال‌سازی نام/امضای کالا در data-quality و customer-finance). پیشنهاد: ماژول مشترک (هنگام مهاجرت قابل جمع‌آوری). |
| A5 | 🟢 کم | **آیتم‌های TECHDEBT ثبت‌شده** | `TECHDEBT-001` (session server-side)، `TECHDEBT-002/004/005/006` (polling/event-hook و…) — از بک‌لاگ v31.7.8 باز مانده‌اند. |

## ۳) تکمیل‌بودن ماژول‌ها

| # | شدت | ماژول | یافته |
|---|---|---|---|
| M1 | 🟡 متوسط | **حساب مشتری (cfOpen)** | مانده/اعتبار «خالص» می‌شوند و هر دو می‌توانند هم‌زمان صفر دیده شوند (باگ UX ثبت‌شده `BUG-2026-08-01-001` — منتظر تصمیم). |
| M2 | 🟡 متوسط | **گزارش‌های مدیریتی** | `reports.js` فیلتر رسمی/غیررسمی ندارد (مستند فاز ۱)؛ «مرکز فرماندهی تحلیل» ترکیبی جمع می‌زند. |
| M3 | 🟡 متوسط | **توسعهٔ بانک/صندوق** | از ممیزی ۲۹ ژوئیه معلق است (مستند) — نباید قبل از مهاجرت شروع شود. |
| M4 | 🟢 کم | **دستیار AI / ابزارهای تخصصی** | ماژول‌های `ai-workbench`, `tech-proposals`, `eng-calc` بدون تستر اختصاصی و ارجاع نامشخص — نیاز به روشن‌سازی. |
| M5 | 🟢 کم | **فایل‌های بدون ارجاع در crm/** | `run-generator.php`, `storage.php` (قدیمی) — پاکسازی پیشنهادی. |

## ۴) رابط کاربری (دسکتاپ و موبایل)

| # | شدت | یافته |
|---|---|---|
| U1 | 🟡 متوسط | **۵۷۲ alert() بومی** — در موبایل به‌خصوص آزاردهنده و خارج از سبک UI (پیشنهاد: toast برای پیام‌های غیربحرانی + ptfDialog برای تأییدهای مهم). |
| U2 | 🟡 متوسط | **موبایل:** `mobilenav.js` وجود دارد (TABS + ptfMnvGo + isMob با innerWidth) ولی تستر/اسکرین‌شات اختصاصی ندارد؛ رفتار «بیشتر» و منوی کشویی نیازمند راستی‌آزمایی دستی. |
| U3 | 🟡 متوسط | **جدول‌های پهن** (گردش حساب، کیفیت داده) در موبایل بدون scroll افقی بهینه — بررسی دستی پیشنهاد. |
| U4 | 🟢 کم | **فیلتر/سورت در فهرست‌های اصلی** — سورت در ۵ لیست اضافه شد ولی فیلترهای پیشرفته (listtools) فقط دکمهٔ «فیلتر و خروجی» دارد؛ در دسترس‌پذیری موبایل محدود. |
| U5 | 🟢 کم | **دسترس‌پذیری** — برچسب/aria برای آیکون‌ها و دکمه‌های آیکونی (مثل ✏️/🗑️) ناقص است. |

## ۵) ورک‌فلوها

| # | شدت | یافته |
|---|---|---|
| W1 | 🟡 متوسط | **گردش RFQ→Offer→Project** زنجیرهٔ کامل دارد (st1..st9 + won/lost) ولی حالت «برگشت/ابطال در میانه» و «تغییر وضعیت پس از قفل» فقط با گاردهای پراکنده کنترل می‌شود — پیشنهاد: نمودار/جدول وضعیت‌های مجاز برای راستی‌آزمایی. |
| W2 | 🟡 متوسط | **گردش خرید واقعی → انبار** («بدون اثر مالی خودکار» عمدی) — موجودی عملیاتی از مالی جدا است؛ باید مستند شود که در مهاجرت حفظ می‌شود. |
| W3 | 🟢 کم | **ارجاع دورهٔ تنخواه** — اکنون الزام پیوست بانک دارد؛ «ثبت در حسابداری» (accountant) جریانش ساده است و شاید نیاز به تأیید/امضا داشته باشد. |
| W4 | 🟢 کم | **users_sync / نقش‌ها** — PR #8 هات‌فیکس باز است؛ نقش‌های جدید (treasurer) در ROLES سمت کلاینت هست ولی باید در سرور هم راستی‌آزمایی شود. |

## ۶) امنیت و داده (بررسی سریع — ممیزی کامل نیاز دارد)

| # | شدت | یافته |
|---|---|---|
| S1 | 🟢 کم | `.htaccess` ریشه و crm مسدودسازی دارد (۲+۱) ولی تسترهای امنیتی قدیمی (tester2-rbac ۶۹/۰ سبز؛ tester281 کپچا ۴ شکست — نیاز بررسی) |
| S2 | 🟢 کم | `api/ptf-secrets.sample.php` الگو دارد؛ کانفیگ واقعی خارج از webroot توصیه می‌شود (در مهاجرت الزامی). |
| S3 | 🟢 کم | دادهٔ حساس در localStorage (توکن/session) — با مهاجرت فاز B به سرور منتقل می‌شود. |

## ۷) سایت/سئو (خارج از CRM — طبق بک‌لاگ)

| # | شدت | یافته |
|---|---|---|
| SEO-1 | 🟡 زیاد | **۴۹ فایل موجیبیک** هنوز روی برنچ (و خارج از sitemap/بدون favicon/زیر ۸۰۰ کلمه/تکراری) — بند ۱ بک‌لاگ. |
| SEO-2 | 🟡 زیاد | **۳۰ فایل هش‌نام یتیم** (rename + ۳۰۱) — بند ۲ بک‌لاگ. |
| SEO-3 | 🟡 زیاد | **متادیتا:** ۱۸۵ title>65، ۹۱ desc>165، ۲۱ desc<70، ۱۵ گروه description تکراری (بیشتر از عدد ۲۰۹ قدیمی — چون شامل مقالات دست‌نخورده هم شده). |
| SEO-4 | 🟡 متوسط | `tester211` (۵ شکست)، `tester209-web-metrics`، `tester213` (ادعای PDF رایگان) — یافته‌های سئو/محتوا. |
| SEO-5 | 🟡 متوسط | فاز ۳ صفحات محصول/برند (۱۶ آیتم) — منتظر بعد از کارهای ۱–۳ (لحن «واردات به سفارش» مصوب). |

## ۸) نسخه و انتشار

| # | وضعیت |
|---|---|
| V1 | ✅ نسخه هم‌گام: index.html v33.3.2 = sw.js CACHE = clear-cache.html = اسناد انتشار (RELEASE/REGRESSION v33.3.2) |
| V2 | ✅ audit.py → ۰ خطا (۴ هشدار شناخته‌شده = همان موجیبیک‌ها) |
| V3 | ✅ SHELL سرویس‌ورکر کامل (همهٔ اسکریپت‌های index.html در SHELL هستند) |
| V4 | ✅ deploy workflows موجود (staging/production/php) |

---

## ۹) اولویت پیشنهادی برای «قبل از مهاجرت» (تصمیم با کارفرما)

1. **تعیین تکلیف PRها** (مرج #11، بررسی #8/#10) — پیش‌نیاز مهاجرت.
2. **رفع SEO-1..3** (۴۹+۳۰+متادیتا) — کار مکانیکی ثبت‌شده؛ اگر سایت مهم است قبل از مهاجرت.
3. **تصمیم دربارهٔ BUG-001** (نمایش خالص مانده/اعتبار مشتری).
4. **راستی‌آزمایی موبایل** (U2/U3) + کاهش alertها (U1) — اختیاری/تدریجی.
5. **بدهی ۵۶ تستر قدیمی** — پاکسازی/به‌روزرسانی (کم‌ریسک، زمان‌بر).
6. **مهاجرت MySQL** — آماده شروع پس از موارد ۱–۲ (یا موازی با ۳–۵).

> ⚠️ این سند فقط فهرست یافته‌هاست؛ هیچ تغییری در کد اعمال نشده (طبق درخواست). اجرای هر مورد نیازمند تأیید/اولویت‌بندی شماست.
