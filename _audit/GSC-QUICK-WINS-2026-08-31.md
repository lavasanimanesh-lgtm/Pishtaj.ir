# فرصت‌های سریعِ سرچ کنسول — ۲۰۲۶-۰۸-۳۱

## ⚠️ منشأ داده (مهم)

**دادهٔ تازه از پنلِ سرچ کنسولِ CRM در این نشست گرفته نشد — و از این محیط قابل گرفتن نیست.**
دلیلِ فنی:

* پنلِ CRM از طریق `api/gsc.php` با **سرویس‌اکانتِ گوگل** کار می‌کند و اعتبارنامهٔ آن در
  `api/gsc-config.php` است؛ این فایل در `.gitignore` خط ۲۷ است و در مخزن وجود ندارد
  (فقط `api/gsc-config.sample.php` هست).
* کشِ پنل هم در `crm/data/gsc-token.json` و `crm/data/gsc-cache.json` است (خطوط ۲۸ و ۲۹
  `.gitignore`) — هیچ‌کدام در این فضای کاری موجود نیست.
* خودِ پنل پشتِ ورودِ CRM است و بدون اعتبارنامه، داده‌ای برنمی‌گرداند.

**پس جدول‌های زیر از آخرین دادهٔ موجود در مخزن استخراج شده‌اند، نه از دادهٔ امروز:**

| منبع | بازه | محتوا |
|---|---|---|
| `_audit/GSC-SNAPSHOT-2026-08-30.csv` | ۹۰ روز منتهی به ۲۰۲۶-۰۸-۳۰ | ۲۷ کوئری + صفحات |
| `_audit/GSC-HISTORY.csv` | همان بازه | برچسب: `baseline-2026-08-21` |

برای دادهٔ تازه، یکی از این دو:
۱) وصل‌کردنِ سرویس‌اکانت طبق `GSC-PANEL-SETUP-FA.md` (گامِ حیاتی: افزودنِ ایمیلِ
   سرویس‌اکانت در GSC → Users and permissions با سطح Restricted)، یا
۲) خروجیِ CSV از GSC → Performance → Queries و Pages (بازهٔ ۳ ماهه) و ارسالِ آن.

---

## ۱) تصویرِ کلی (۹۰ روز منتهی به ۲۰۲۶-۰۸-۳۰)

| شاخص | مقدار |
|---|---|
| نمایشِ کل | ۲۸۲ |
| کلیکِ کل | ۳۱ |
| نمایشِ برندی | ۹۱ |
| نمایشِ غیربرندی | ۱۹۱ |
| **کلیکِ غیربرندی** | **۰** |

**یافتهٔ اصلی: هر ۳۱ کلیک از جستجوی برندی آمده است** («پیشرو تجهیز فرتاک» ۲۲،
«پیشرو تجهیز» ۷، «ویژن پترو تجهیز فرتاک» ۲). یعنی ۱۹۱ نمایشِ غیربرندی **هیچ کلیکی**
نداشته‌اند. هدفِ ۹۰ روزهٔ چک‌لیست (سهمِ کلیکِ برندی زیر ۷۰٪) امروز عملاً ۱۰۰٪ است.

---

## ۲) فرصت‌های سریع — دستهٔ الف: نزدیکِ صفحهٔ اول

کوئری‌های غیربرندی با نمایش ≥ ۵ و جایگاه ≤ ۳۰ (صفحهٔ مقصد همین حالا پرمحتواست،
پس مشکل محتوا نیست):

| کوئری | نمایش | کلیک | جایگاه | صفحهٔ مقصد | متنِ یکتا |
|---|---|---|---|---|---|
| ترانسمیتر فشار روزمونت 3051 | ۵۳ | ۰ | ۱۹٫۴ | `knowledge-center/rosemount-pressure-transmitter-family.html` | ۵٬۳۵۹ |
| لول ترانسمیتر دیسپلیسر | ۱۱ | ۰ | ۲۶٫۰ | `services/products/displacer-level-transmitter.html` | — (صفحهٔ محصول) |
| کنترل ولو بخار | ۶ | ۰ | ۱۸٫۰ | `knowledge-center/steam-control-valve-sizing-guide.html` | ۷٬۸۵۴ |

**اقدام (کارِ شما، هر مورد ۲ دقیقه):** GSC → نوارِ آدرس → **Request Indexing** برای
همین سه آدرس. این همان مرحلهٔ ۳‑۲ چک‌لیست است و تا انجام نشود، اثرِ بقیهٔ کارها دیده نمی‌شود.

---

## ۳) دستهٔ ب: تقاضای واقعی، جایگاهِ بد، صفحهٔ مقصد **تازه قوی شده**

این کوئری‌ها به صفحاتی می‌خوردند که نازک بودند؛ اصلاحشان در PRهای ۱۸ و ۱۹ است و
**پس از دیپلوی** باید در GSC دیده شود:

| کوئری | نمایش | جایگاه | صفحهٔ مقصدِ قدیمی | وضعیتِ جدید |
|---|---|---|---|---|
| لوله x42 | ۲۳ | ۵۴٫۷ | `api-5l-x42` (۷۴ کاراکتر) | بازنویسی شد → ۳٬۰۱۵ |
| قیمت لوله a106 | ۸ | ۶۷٫۴ | `a106-gr-b` (۴۳۷) | ۳۰۱ به `kc-astm-a106-gr` → ۵٬۹۲۴ |
| لوله nace | ۱۱ | ۸۱٫۰ | `kc-api-5l-psl2-sour-service-nace-requirements` | ۷٬۱۸۶ (محتوا خوب؛ نیازمند لینک داخلی) |
| فلنج اراک | ۶ | ۸۹٫۰ | `flange-arak-guide` | ۳٬۲۶۵ (نیازمند لینک داخلی + عنوان بهتر) |
| مدیریت انبار قطعات یدکی | ۱۱ | ۸۹٫۳ | `kc-spare-parts-warehousing-inventory` | ۳٬۲۶۸ (نیازمند لینک داخلی) |
| حمل تجهیزات صنعتی | ۵ | ۸۰٫۸ | `kc-industrial-material-handling-transport` | ۲٬۹۱۲ (نیازمند لینک داخلی) |

**اقدام:** پس از دیپلوی، برای «لوله x42» و «قیمت لوله a106» هم Request Indexing بزنید
و ۴ هفته بعد همین جدول را با دادهٔ تازه مقایسه کنید.

---

## ۴) دستهٔ پ: قصدِ تجاری با جایگاهِ بسیار بد

| کوئری | نمایش | جایگاه | صفحه | نکته |
|---|---|---|---|---|
| نمایندگی روزمونت | ۶ | ۸۲٫۰ | `brands/emerson-rosemount.html` | پرس‌وجوی برند-محورِ خریدار؛ صفحهٔ برند نیاز به بازبینیِ محتوا و عنوان دارد |
| زنجیره تامین قطعات یدکی | ۵ | ۹۸٫۲ | `blog/pumps-procurement-guide/` | جایگاه نزدیکِ خارج از فهرست؛ موضوع با حوزهٔ کاری هم‌راستاست |

---

## ۵) اولویتِ پیشنهادی (به ترتیبِ اثر/تلاش)

1. **دیپلویِ PRهای ۱۸ و ۱۹** — بدون آن هیچ‌کدام از اصلاحاتِ محتوایی روی سایت نیست.
2. **Request Indexing** برای ۳ صفحهٔ دستهٔ الف (۵ دقیقه، بیشترین اثرِ کوتاه‌مدت).
3. **وصل‌کردنِ پنلِ سرچ کنسول** (یا ارسالِ CSV) — بدون دادهٔ تازه، سنجشِ اثر ممکن نیست.
4. **لینک‌سازی داخلی** به ۶ صفحهٔ دستهٔ ب (کارِ من در نوبتِ بعد).
5. ادامهٔ بازنویسیِ «اولویت ۱» در batchهای ۷ تایی.

> هیچ عددی در این سند ساخته نشده است: همهٔ ارقامِ نمایش/کلیک/جایگاه مستقیماً از
> `GSC-SNAPSHOT-2026-08-30.csv` خوانده شده و ارقامِ «متنِ یکتا» با
> `_tools/kc_consolidate.py` روی درختِ فعلی مخزن اندازه‌گیری شده‌اند.
