# پیام تحویل به نشست بعدی — ثبات‌سازی CRM + مهاجرت MySQL (2026-08-02)

## ⚡ v33.22.2 — ساده‌سازی تنظیمات بک‌آپ/حافظه (آخرین دستور نشست) — روی استیجینگ، در انتظار تأیید کارفرما

**دستور کارفرما (پایان‌بخش نشست):** «کار را همین‌جا متوقف کن + فردا کاربران پرحافظه چه کنند؟ + تنظیمات بک‌آپ را ساده کن (دکمه/توضیح اضافه حذف)». انجام‌شده: باکس بک‌آپ ۸→۶ دکمه (حذف «🔌 بررسی اتصال سرور» و «☁️ پاک‌سازی زباله‌های ابری» — توابعشان دست‌نخورده)؛ باکس حافظه ۶→۱ دکمه (حذف «بک‌آپ سروری» **تکراریِ دقیق بک‌آپ فوری** + مهاجرت/نمایش‌کلید/آرشیو/Persistent — «پاک‌سازی امن فوری» خودش مهاجرت IDB را هم می‌کند)؛ متن‌های فنی/کهنه (Browser storage estimate، DB-MIG-001 «برنامهٔ آینده») ← کاربرمحور؛ باکس فاز B کوتاه‌تر با هر ۴ دکمه. tester303=۱۰/۰ جدید؛ pinهای متاثر (227/228/294/295/298) با regex مقاوم-به-indirection به‌روز شدند؛ رگرسیون **۲۹۸ تستر ۲۴۲/۵۶ و ۵۴۳۱/۸۴ = baseline + 303**؛ audit ۰ خطا؛ zip ۵۵۸.
**گام:** پوش → دیپلوی خودکار استیجینگ → کارفرما تنظیمات استیجینگ را ببیند → تأیید → مرج (همین نشست/نشست بعد) ← دیپلوی دستی. **نکتهٔ حیاتی برای نشست بعد:** کارفرما گفت «فعلاً کار متوقف» — v33.22.2 هنوز PR ساخته/مرج نشده؛ بدون تأیید استیجینگ هیچ مرجی نکن.
**پاسخ داده‌شده برای کاربران پرحافظهٔ فردا:** ۱) «رفع مشکل ورود (بدون حذف داده)» + ورود ۲) «🗄 بک‌آپ فوری» + «پاک‌سازی امن فوری» ۳) ریشه‌ای: «فعال‌سازی حالت سرور-محور» ← هم‌گرایی ← «پاک‌سازی کش محلی» (داده روی MySQL است — پاک شدن کش امن است).

## ⚡ ✅ تکمیل کامل P1: پروداکشن = v33.22.1 + mode=mysql (سوییچ تازه ۲۰۲۶-۰۸-۰۲ ۲۳:۳۵:۱۵) — راستی‌آزمایی زنده سبز

**مسیر این نشست:** تست استیجینگ ✅ → مرج PR #15 (main=`247b238`) → دیپلوی دستی کارفرما (run 30764777903، موفق، Release/تگ رول‌بک `v2026.08.02-2004`) → ویزارد پروداکشن (بازنشانی + ۶ مرحله) ← همه انجام شد.
**راستی‌آزمایی زندهٔ نهایی پروداکشن:** index «نسخه فعال: **v33.22.1**» + `sw.js` → `ptf-crm-v33.22.1` + `migrate.php` تشخیص‌گر: `mode = mysql` و switched_at=**۲۳:۳۵:۱۵** (با کد جدید — فقط پس از دیپلوی ممکن بود: گارد قدیمی reset نداشت) + `data-health-check.php?confirm=yes`: **۲۳ سالم / ۰ خطا** (تنها ⚠️ قدیمی: ۳ بک‌آپ قرنطینه — بدهی قبلی، نامرتبط) — دادهٔ واقعی سالم: customers:26، rfqs:58، offers:50، suppliers:117، products:303؛ rev=2821؛ ۳۹ کلید sync؛ API فعال (آخرین data_pull ۲۳:۲۹).
**گام بعدی:** پایش چندروزهٔ کاربران (هر خطا گزارش شود)؛ فایل migrate.php با گارد بی‌اثر است (حذف اختیاری). بدهی باز: فیدبک چنددستگاهی P0 در پروداکشن (UR-15)، بک‌لاگ کم‌اولویت (کلیدهای per-user، ETag/304، UR-27)، ۵۶ تستر قدیمی.

تست استیجینگ توسط کارفرما **تأیید شد** («همه چی اوکی بود»). راستی‌آزمایی زندهٔ پیش از مرج: استیجینگ mode=mysql با switched_at جدید (۲۰۲۶-۰۸-۰۲ ۲۳:۲۶:۵۵ — بازنشانی + هر ۶ مرحله موفق) + CRM استیجینگ v33.22.1 سالم. **انجام‌شده در نشست:** `gh pr merge 15 --merge` → main = `247b238`.
**گام‌های باقی‌مانده (کارفرما):** ۱) دیپلوی دستی: [Actions → Deploy to Production](https://github.com/lavasanimanesh-lgtm/Pishtaj.ir/actions/workflows/deploy-production.yml) → confirm=`DEPLOY`، ref=`main`؛ ۲) `https://pishtaj.ir/api/migrate.php` → تایپ «بازنشانی» ← دکمه ← مراحل ۱–۶ (بکاپ ← جدول ← انتقال ← تطابق ← dual ← تست ← سوییچ نهایی با «مهاجرت»)؛ ۳) Ctrl+Shift+R برای کاربران CRM؛ ۴) پس از دیپلوی: **راستی‌آزمایی زنده** (index «نسخه فعال: v33.22.1» + migrate.php تشخیص‌گر mode + یک پوش/پول سالم). نکته: پروداکشن هم از قبل mode=mysql است (سوییچ اولیه امروز ۱۳:۵۹) — پس از دیپلوی v33.22.1 و قبل از اجرای مراحل، مسیر سینک از DBِ **کهنه** می‌خواند؛ اجرای سریع بازنشانی+۶ مرحله این پنجره را می‌بندد. حذف migrate.php لازم نیست (گارد بی‌اثر می‌کند).

**تأیید زندهٔ علت (به‌لطف تشخیص‌گر v33.22.1):** صفحهٔ قفل استیجینگ پس از دیپلوی v33.22.1 نشان داد `mode = mysql` و «آخرین تغییر فایل: 2026-08-02 13:59:55» = همان `switched_at` → ویرایش دستی کارفرما **هرگز به فایل واقعی نرسید** (احتمالاً ptf-db-config.**sample**.php یا مسیر/پوشهٔ دیگری ویرایش شد — OPcache فقط خط دفاع دوم بود). سوییچ اولیهٔ استیجینگ همان امروز ۱۳:۵۹ انجام شده بود. مسیرِ درستِ پیش‌رو برای همیشه = دکمهٔ «بازنشانی» خودِ ویزارد (نه ویرایش دستی).

کارفرما ویرایش دستی `'mode' => 'off'` در ptf-db-config.php را انجام داد ولی migrate.php همچنان «مهاجرت کامل شده» نشان می‌داد → ریشه: **OPcache خروجی require فایل کانفیگ PHP را کش می‌کند** (هاست اشتراکی؛ validate_timestamps ممکن است 0 باشد). رفع در v33.22.1: (۱) `ptf_db_config` قبل از require و `ptf_db_save_config` بعد از نوشتن → `@clearstatcache` + `@opcache_invalidate($p, true)` (محافظت‌شده با function_exists)؛ (۲) گام **`reset_mode`** در migrate.php — **قبل از گارد mode=mysql** بررسی می‌شود (وگرنه همان قفل راه نمی‌داد!) — با require_token + تایپ «بازنشانی»؛ فقط `$cfg['mode']='off'` + `reset_at`؛ بقیهٔ کانفیگ و داده‌ها دست‌نخورده؛ سپس ۶ مرحله دوباره قابل اجرا؛ (۳) صفحهٔ قفل: **تشخیص‌گر** (mode خوانده‌شده + filemtime کانفیگ + switched_at) + فرم بازنشانی. رشتهٔ legacy `فایل migrate.php → حذف` در پیام پابرجاست (tester297). tester302=۱۰/۰ (جدید)؛ tester301=۲۰/۰ (گیت نسخه انعطاف‌یافته)؛ رگرسیون ۲۹۷ تستر **۲۴۱/۵۶ و ۵۴۲۱/۸۴ = baseline + 302**؛ audit ۰ خطا؛ zip ۵۵۸ (api/db-lib.php و api/migrate.php به‌روز). **گام کارفرما:** پس از دیپلوی خودکار استیجینگ → migrate.php با Ctrl+Shift+R → تایپ «بازنشانی» ← دکمه → مراحل ۱–۶ → تست ← گزارش → مرج PR #15 در نشست ← دیپلوی دستی ← همین روال پروداکشن (بازنشانی → ۶ مرحله؛ بدون نیاز به ویرایش دستی).

## ⚡ کشف زنده (2026-08-02): کانفیگ DB روی هر دو سرور از قبل mode=mysql است — ویزارد قفل؛ روش تازه‌سازی ابلاغ شد

**راستی‌آزمایی زنده:** `staging.pishtaj.ir/api/migrate.php` و `pishtaj.ir/api/migrate.php` هر دو پیام گارد v33.17.1 را نشان می‌دهند («مهاجرت قبلاً کامل شده») → روی هر دو سرور `api/ptf-db-config.php` با `mode: mysql` موجود است (کارفرما قبلاً دیتابیس ساخته و ویزارد را اجرا کرده — کارفرما: «دیتا بیس که وجود دارد»). استیجینگ الان v33.22.0 است (دیپلوی خودکار پوش arena/** موفق — run 30762064590).

**نکتهٔ حیاتی:** مسیر سینک ۵۲ کلید تا v33.22.0 فایل‌محور بوده → آن ۵۲ کلید در MySQL **کهنه‌اند** (ذخیره‌سازی عمومی load_data/save_data تازه است چون save_data همیشه ptf_db_write آینه می‌زده). تا تازه‌سازی، استیجینگ (v33.22.0 + mode=mysql) از DB کهنه می‌خواند — با مراحل زیر برطرف می‌شود.

**روش ابلاغ‌شده به کارفرما (تازه‌سازی = اجرای دوبارهٔ ویزارد):**
1. سی‌پنل: ویرایش `api/ptf-db-config.php` → `'mode' => 'mysql'` به `'mode' => 'off'` (گارد باز می‌شود؛ v33.22.0 در off = رفتار امروز). جایگزین: حذف کل فایل و واردکردن دوبارهٔ مشخصات DB در فرم اتصال.
2. مرورگر: `/api/migrate.php` → ۶ مرحله به ترتیب (backup → schema → migrate با «مهاجرت» → verify همه ✅ → enable_dual → switch_final با «مهاجرت»). migrate با ON DUPLICATE KEY UPDATE همهٔ کلیدها را از فایل‌های فعلی تازه می‌کند (rev ستون DB فقط اطلاعاتی است؛ دفتر rev = meta.json).
3. تست CRM استیجینگ (دو مرورگر). سپس: تأیید کارفرما → مرج PR #15 در نشست → دیپلوی دستی → روی پروداکشن: اول کانفیگ به off → دیپلوی v33.22.0 → همان ۶ مرحله (این ترتیب صفر پنجرهٔ کهنگی می‌دهد چون off = رفتار فایل‌محور امروز).
4. حذف migrate.php لازم نیست — هر دیپلوی FTP دوباره آپلودش می‌کند؛ گارد post-switch آن را خنثی نگه می‌دارد.

## ⚡ جدیدترین — v33.22.0 (P1-MySQL-WIRE، در انتظار تست استیجینگ کارفرما)

**تصمیمات کارفرما همین نشست:** ۱) سرور تغییر نمی‌کند — فقط فازهای همین هاست؛ ۲) **بیش از ۱۰۰ کاربر تا چند سال نیاز نیست** (رفع سقف کنار — UR-27)؛ ۳) «این فاز را شروع کن» = اتصال واقعی به MySQL → انجام شد.

**تصحیح معماری:** ذخیره‌سازی عمومی crm.php (load_data/save_data) از قبل ptf_db-aware بود؛ فایل‌محورِ واقعی **مسیر سینک ۵۲ کلید** بود. migrate.php شش‌گام کامل دارد (backup/schema/migrate/verify → enable_dual → switch_final با گارد mode=mysql).

**v33.22.0:** هلپرهای `sync_key_read`/`sync_key_write` (top-level — تعریف داخل switch در PHP «شرطی» است و در caseها undefined!) + سیم‌کشی همهٔ سایت‌های push (archive/offers/conflict/zero-shield/write با rev+۱ + dbWriteFailed→needRetry idempotent + آزادسازی قفل) و pull (حلقه+آرشیو تنبَل). db-lib: `ptf_db_conn` کش استاتیک (قبلاً پوش ۲۰کلیدی=۲۰ اتصال تازه!) + کش کانفیگ در $GLOBALS (تازه‌سازی پس از save) + `ptf_db_write_rev`. کلاینت/پروتکل دست‌نخورده؛ mode=off = رفتار امروز (پخش همه‌جا امن). tester301 = **۲۰/۰**؛ رگرسیون = ۲۹۶ تستر **۲۴۰/۵۶ و ۵۴۱۱/۸۴ = baseline + 300+301**؛ audit ۰ خطا؛ zip ۵۵۸ (api/crm.php و api/db-lib.php به‌روز).
**گام:** PR #15 → تست استیجینگ کارفرما (اختیاری: ساخت DB در cPanel + ویزارد migrate.php تا dual) → تأیید → مرج در نشست ← دیپلوی کارفرما ← ویزارد روی پروداکشن.

## ⚡ تازه‌ترین قبلی — v33.21.0 (PTF-SCALE-P0 + هاتفیکس بحرانی، در انتظار مرج/دیپلوی کارفرما)

**درخواست نشست:** تحلیل مقیاس‌پذیری + حداکثر کاربر فعلی. **پاسخ:** سقف کد ۱۰۰؛ همزمانِ فعال ~۱۰ راحت/~۲۵–۳۰ مشکل (فایل-محور + پول کامل + getData firehose + تب‌های مخفی) → کارفرما اجرای P0 را انتخاب کرد.

**⚠️ کشف مهم هنگام تست: باگ تولید v33.20.0 (همین امروز دیپلوی شده!)** — BUG-SYNC-RD-SCOPE-001: `ptfReadArchive` (اسکوپ سراسری) `rd()` (داخل IIFE) را صدا می‌زد → ReferenceError در هر pull/push دارای tombstone → همگام‌سازی زندهٔ پروداکشن عملاً شکسته (نشان آفلاین کذب). علت عبور رگرسیون قبلی: تسترها rd/wr را تزریق می‌کردند. رفع شد + چک رفتاری tester300 (sandbox بدون rd). **`curl` در سندباکس به سایت مسدود است — راستی‌آزمایی با fetch_page یا zip+worktree.**

**v33.21.0:** دلتا-پول به‌ازای هرکلید (krevs در crm.php+sync.js؛ سازگار با عقب) + sharedPull در client-server.js (single-flight/۲ثانیه/تخلیهٔ مخفی — جای serverPull(0) مستقل در getData) + گارد تب (مخفی: بدون پول؛ غیرمتمرکز: ۱۲۰ثانیه؛ focus/visibilitychange جبران فوری) + بازنویسی tester170 (source-pinned → regex). tester300 جدید = **۲۲/۰**؛ رگرسیون = ۲۹۵ تستر **۲۳۹/۵۶ فایل و ۵۳۸۸/۸۴ چک = baseline + tester300**؛ audit ۰ خطا؛ sitemap بدون diff؛ zip ۵۵۸ بازسازی.
**گام:** PR + تأیید کارفرما + **دیپلوی فوری دستی** (هاتفیکس زنده است) → رفرش کاربران → بعد از پایدارسازی: P1 (اتصال واقعی crm.php به MySQL — db-lib آماده ولی هیچ مصرف‌کننده‌ای ندارد + رفع سقف ۱۰۰ کاربر). ظرفیت پس-P0: ~۵۰ همزمان روی همین هاست؛ P2 (VPS+SSE+رکورد-محور): ~۲۰۰+.

## ⚡ v33.21.1 مرج شد (main=`3122e4d`, PR #14 — در انتظار دیپلوی دستی کارفرما)

کارفرما v33.21.1 را روی استیجینگ تست و **تأیید کرد** («تایید شد.») → در همان نشست مرج انجام شد. **گام بعدی: کارفرما Actions → Deploy to Production را با confirm=DEPLOY و ref=main دستی اجرا می‌کند**؛ سپس اطلاع به کاربران: یک‌بار رفرش (هاتفیکس همگام‌سازی زندهٔ پروداکشن + دلتا-پول + پینگ بین‌تبی اعمال می‌شود). پس از آن: انتخاب مورد بعدی از نقشهٔ راه همین-هاست (P1: اتصال واقعی crm.php به MySQL روی همان هاست — db-lib آماده ولی مصرف‌کننده ندارد — + رفع سقف ۱۰۰ کاربر + کلیدهای per-user + ETag/304) با ask_user. **قواعد دائم نشست: سرور تغییر نمی‌کند؛ بدون تست استیجینگ کارفرما مرج نکن؛ تأیید → مرج در همین نشست؛ دیپلوی با کارفرما.**

## ⚡ قواعد جدید کارفرما (۱۴۰۵/۰۸/۱۱) + به‌روزرسانی v33.21.1

**قواعد:** ۱) سرور تغییر نمی‌کند — فقط فازهای همین هاست cPanel (بدون VPS/WebSocket/Redis). ۲) **بدون تست استیجینگ کارفرما، هیچ مرجی به main نکن.** ۳) بعد از تأیید، در همین نشست مرج کن (نشست باز می‌ماند) و **دیپلوی پروداکشن با کارفرما**.

**v33.21.1 (فیدبک استیجینگ v33.21.0):** کارفرما گزارش داد اضافه/حذف بین دو مرورگر ✅ ولی رکورد در تب‌های دیگر تا قبل از جابه‌جایی ظاهر نمی‌شد (رفتار طراحی‌شدهٔ v33.21.0: مخفی=توقف کامل پول). کارفرما گزینهٔ **A** را انتخاب کرد → پیاده شد: **پینگ بین‌تبی** (ptf_sync_ping + storage event → دلتا-پول فوری با guard rev/۵ثانیه در pullCheck اعمال و پوش؛ در pushDirty هم) + مخفی=۱۸۰ثانیه آهسته + instant bypass (opts پارامتر سوم pullCheck). tester300: ۲۲→**۲۵/۰**؛ tester178 source-pinned به regex بازنویسی شد. رگرسیون = ۲۹۵ تستر **۲۳۹/۵۶ فایل و ۵۳۹۱/۸۴ چک**؛ audit ۰ خطا؛ zip ۵۵۸ بازسازی. کامیت بعدی روی برنچ → PR #14 به‌روز است → **انتظار: تست مجدد استیجینگ توسط کارفرما، سپس مرج در همین نشست.**

## ⚡ جدیدترین قبلی — ✅ انتشار کامل شد: v33.19.0 + v33.20.0 هر دو در پروداکشن (۱۴۰۵/۰۸/۱۱)

**Deploy دستی کارفرما اجرا و راستی‌آزمایی شد** (از خود سایت زنده):
- `pishtaj.ir/crm/index.html` → «نسخه فعال: **v33.20.0**» ✅
- `crm/client-server.js` → هدر v33.20.0 + `ptfBMirror`/`ptfBIdbPreload`/`IDB_KEYS` (آینهٔ خالدار) + v33.19.0 (`ptfBPushBatch`/`ptfBClearLocalCache`) ✅
- `crm/sw.js` → `CACHE = 'ptf-crm-v33.20.0'` ✅

**گام بعدی (باز):** به کاربران اعلام شد یک‌بار رفرش کنند تا بوت preload آینهٔ خالدار مهاجرت کند و درصد localStorage **پایدار** پایین بماند. ⏳ فیدبک چندروزهٔ کاربر (UR-2026-08-02-15: تست واقعی فاز B — چنددستگاهی، آفلاین/صف، badge سبز، درصد استورج) هنوز لازم است. اگر GitHub در Arena با مجوز Actions وصل شود، دیپلوی‌های بعدی را می‌توان از داخل سندباکس تریگر کرد (الان 403).
توجه: `curl` به سایت در سندباکس مسدود است (SSL_ERROR_SYSCALL) — راستی‌آزمایی فقط با ابزار fetch_page.

## ⚡ وضعیت جدید — v33.19.0 (نشست `arena/019fc272-pishtaj-ir` — به‌روزرسانی ۱۴۰۵/۰۸/۱۱)

**مهم — تغییر برنچ:** نشست این هند‌آف (`arena/019fbe19-pishtaj-ir`) با **مرج PR #11 به main** بسته شد؛ کامیت اصلاحی محلی `35faba6` (v33.19.0) فقط localhost مانده بود. نشست جدید روی برنچ **`arena/019fc272-pishtaj-ir`** (از main `0bb5e84`) اصلاحات را **از روی این دستورالعمل بازسازی کرد** (کد معادل). قاعدهٔ «دسترسی نشست‌ها» جدید: فقط برنچ ثابت خود نشست.

**v33.19.0 — رفع باگ هم‌گرایی فاز B (در main هنوز نیامده — منتظر تأیید کارفرما برای مرج/deploy):**
- `serverPush`: needLogin/401 → `ptfSyncRefreshAuth` + یک بار retry؛ پیام دقیق «نشست منقضی؛ دوباره وارد شوید» (به‌جای «سرور در دسترس نیست»).
- `ptfBPushBatch`: ارسال دسته‌ای ≤۲۰ کلید در هم‌گرایی و flush صف (رفع payload چند MB روی دستگاه‌های پرحافظه).
- نشانگر `ptf_b_synced_<user>` فقط با موفقیت کامل هم‌گرایی.
- `ptfBClearLocalCache` + دکمهٔ «🗑 پاک‌سازی کش محلی» در باکس فاز B (گاردها: فاز فعال + synced + صف خالی + تایپ «پاک»).
- گارد `__ptfB` ضد دوباره‌هوک. tester298: ۸/۰ → **۱۲/۰**. رگرسیون: صفر شکست جدید. audit: ۰ خطا. zip بازسازی شد.
- **وضعیت انتشار:** ✅ **کامل** — PR #12 مرج (main=`b6116a0`) + Deploy دستی کارفرما اجرا شد (پروداکشن v33.19.0 شد؛ سپس با v33.20.0 ارتقا یافت).

## ⚡ وضعیت جدیدتر — v33.20.0 آینهٔ خالدار (همان نشست — بعد از پاسخ کاربر)

**گزارش کاربر:** پس از انتشار v33.19.0 استورج از ۴۱٪ به ~۸۰٪ برگشت و پاک‌سازی کش اثر نداشت.
**ریشه:** sync.js در هر بارگذاری آینهٔ کامل سرور را در localStorage می‌نویسد (pullCheck forceFull + polling) → پاک‌سازی موقتی بود.
**رفع (v33.20.0):** آینهٔ خالدار — کلیدهای سنگین در حافظه+IndexedDB (پیشوند `bdata:`)، preload بوت migrate+آزادسازی، گیت سه‌گانه (فاز+synced+IDB)، ری‌هیدریت هنگام غیرفعال‌سازی، rd/wr در sync.js (۱۶ سایت از جمله beforeunload beacon و گواردها)، theme.avatarsAll.
**تست:** tester299 جدید = **۱۴/۰**؛ 298=۱۲/۰؛ 297=۱۹/۰؛ رگرسیون = صفر شکست جدید؛ audit ۰ خطا؛ zip بازسازی.
**گام:** ✅ **کامل شد** — PR #13 مرج (main=`9c88a7b`) + Deploy دستی اجرا شد + راستی‌آزمایی: «نسخه فعال: v33.20.0» ✅ (بخش «جدیدترین» بالای همین فایل). ⏳ باقی: فیدبک کاربران پس از رفرش (درصد پایدار پایین؟).

---

**تاریخ:** ۱۴۰۵/۰۸/۱۱ (2026-08-02)
**برنچ کاری (ثابت، تغییر نده):** `arena/019fbe19-pishtaj-ir`
**HEAD:** `0dde1d9` (v33.17.1) → **این نشست: v33.18.0 (فاز B — کلاینت نازک: سرور-محور + صف آفلاین + هم‌گرایی یک‌باره)** — همیشه `git fetch && git reset --hard origin/arena/019fbe19-pishtaj-ir` قبل از شروع
**وضعیت:** این برنچ = برنچ قبلی `arena/019fb322` (۱۶۵+ کامیت مرج‌نشده: ۹۴ سئو + ۷۱ CRM) + کارهای نشست‌های قبل + CHQ-PRINT. `main` کاملاً نیای برنچ است (بدون انشعاب).

---

## ۱) یک‌خطی برای شروع سریع

«**v33.18.0 (فاز B — کلاینت نازک):** سرور (MySQL) منبع حقیقت شد — هوک getData/setData (کش ۳۰ثانیه + صف آفلاین ptf_b_queue + flush debounced) + هم‌گرایی یک‌باره با تأیید (ptf_b_flushed_<user>) + دکمه‌های «حالت سرور-محور» در تنظیمات (فعال/هم‌گرایی/غیرفعال). **تصمیم کارفرما: تا پایان فاز B مرج نمی‌شود.** گام بعدی: تست استیجینگ (فعال‌سازی → هم‌گرایی → کارکرد ۲-۳ روز → آفلاین/صف) سپس مرج PR #11 → deploy پروداکشن. تسترها: ۲۹۸ (۸/۰)؛ رگرسیون = صفر شکست جدید (۵۶ بدهی). **PR #11 باز.** بررسی شوند** — الگوی tester184 را دنبال کنید.
3. **یافته‌های واقعی سئو** — tester208 (تعداد URL sitemap ≠ صفحات واقعی؛ همان ۴۹ موجیبیک — با بند ۱ بک‌لاگ حل می‌شود)، 209، 211، 213 (بخش بالا).
4. **متفرقه/adv-cv** — tester254/261/272/275/276/280: انتظارات جزئی از ابزارهای adv-cv/fiscal — نیازمند بررسی تکی.

---

## ۶) کارهای سئوی سایت (بعد از CRM — طبق پاسخ‌های کارفرما)

1. ✅ **حذف ۴۹ فایل موجیبیک + ریدایرکت ۳۰۱ (تمام — کامیت 8265b3b)** — ۴۹ قاعدهٔ 301 مستقیم به مقصد نهایی در .htaccess؛ بدون لینک داخلی (یتیم)؛ audit.py حالا «همهٔ بررسی‌ها PASS» (صفر هشدار).
2. ✅ **rename ۳۰ فایل هش‌نام یتیم (تمام — کامیت 89851f0)** — git mv به kc-*، اصلاح لینک‌های داخلی + canonical/خودارجاع، ۳۰ قاعدهٔ 301؛ audit PASS.
3. ✅ **رفع نقض‌های طول متادیتا (تمام — کامیت de3eb2e)** — ۳۶۶+ فایل: عنوان ≤۶۵ و توضیح ۷۰–۱۶۵ + هم‌گام‌سازی og/JSON-LD؛ صفحات استراتژیک مطابق tester211؛ تسترهای سئو: 208 (۷/۷)، 209 (۹/۹)، 211 (۲۵/۲۵)، 213 (۱۲/۱۲) همگی سبز؛ اسکن نهایی ۵۲۰ صفحه صفر نقض.
4. **فاز ۳ صفحات محصول/برند** — پاسخ‌های ثبت‌شدهٔ کارفرما: لحن «**واردات به سفارش**» • **دو صفحهٔ مجزا** Fisher/Masoneilan • بعد از کارهای ۱–۳. بک‌لاگ: `product_pages_backlog.json` (۱۶ آیتم).
4b. ✅ **UR-11 دورهٔ بازه‌ای تنخواه (تمام — v33.3.4)** — از آخرین ارجاع تا تاریخ انتخابی؛ ماندگاری گزارش+منضمات هر دوره (pettyIds/txIds)؛ قالب «تنخواه‌گردان از تاریخ … تا …» + ستون نحوهٔ پرداخت (مستقیم/تسویه در تاریخ توسط/در انتظار)؛ PDF منضمات در هر زمان. طراحی: `crm/DESIGN-PETTY-PERIOD-RANGE-v1.md` — tester212 (۱۳/۰).
4c. ✅ **UR-12 ساده‌سازی مختومهٔ پرونده فروش (تمام — v33.3.4)** — پکینگ‌لیست/تسویه تامین‌کننده فقط هشدار (از قبل)؛ blocker «تحویل» با تأیید صریح (تیک+دلیل→closeOverride) قابل عبور و در audit ثبت می‌شود؛ گارد مالی commit حفظ شد — tester214 (۷/۰)، tester110 (۳۶/۰).
4d. ✅ **CHQ-MOD-001 ماژول مستقل چک (پیاده‌سازی شد — v33.4.0، کامیت f95ae26 + 6ddd31a)** — معماری مصوب (دو کلید issued/received، ضمانت→صادره، برگشتی، گزارش PDF/CSV، گام‌به‌گام). اجرا شده:
   - `crm/cheque-module.js`: issued/received + Create + Endorse/Collect/Bounce/VoidTransfer/ClearIssued/VoidIssued + مهاجرت نرم idempotent (legacy خالی می‌شود).
   - `crm/cheque-panel.js`: تب «🧾 چک‌ها» در هاب مالی (زیرتب صادره/وارده، ثبت دستی، عملیات، جمع باز، PDF/CSV).
   - `cheques.js` chAll→ptfChequeAll (سازگاری) + مهاجرت در startup؛ `supplier-finance.js` چک شرکت→issued و چک ثالث→received+endorsed؛ `rbac.js savePay` وصول با چک→received (گام ۴).
   - کلیدهای issued/received در sync/backup/api.
   - tester215 (۲۷/۰) + رگرسیون tester2/145/151 سبز؛ audit PASS.
   - **باقی‌ماندهٔ جزئی:** یکپارچه‌سازی یادآور/قفل سال مالی/data-quality برای چک‌های جدید + راستی‌آزمایی استیجینگ توسط کارفرما.
4e. ✅ **UR-13 (تقویم+شارژ تنخواه) و BUG-ARCHIVE (بایگانی کامل اسناد) — v33.4.1/4.2** — تقویم شمسی + فرمت‌های رایج در تاریخ‌ها؛ آپلود فایل PDF گردش حساب بانک در خود پنجرهٔ ارجاع (الزامی — به‌جای «شارژ» که اشتباه بود)؛ ریشه‌کنی بایگانی (ptfSalesFileOffers کامل + docSnap/offerNos + بخش «اسناد کامل پرونده») — tester212/216.
4f. ✅ **BUG-RANGE — v33.4.3**: recDate مقاوم + فیلتر بازه شامل رکوردهای بی‌تاریخ هم‌ماه + مقدار پیشنهادی تقویم + پیام راهنما — tester212 (۲۲/۰).
4g. ✅ **BUG-DATE-NAV — v33.4.4**: نرمال‌سازی ارقام فارسی/عربی در تاریخ‌ها + ناوبری مطمئن ماه/سال تقویم با ptfCalNav — tester212.
4h. ✅ **BUG-PDF-ATTACH — v33.4.5**: گزارش تلفیقی ضمائم را نمایش نمی‌داد. رفع: ptfPettyFileKind + ptfPettyResolveUrl + رندر <img>/<embed> + CombinedPdf async — tester209.
4i. ✅ **BUG-TOTALS + BUG-ATTACH-URL — v33.4.6**: ردیف‌های جمع (هزینه/شارژ/موجودی شروع/پایان) + حفظ url فایل‌ها — tester208.
4j. ✅ **BUG-PDF-HEIC — v33.4.7**: تبدیل PDF/HEIC → JPEG (api/attachment-thumb.php + Imagick) + رندر <img> + چیدمان grid سه‌ستونه + ناوبری تقویم با delegation — tester209 (۲۱/۰).
4k. ✅ **CHQ-MOD-001 تکمیلی — v33.4.8/4.9**: یادآور + قفل سال مالی + data-quality + اتصال به گردش حساب — tester215.
4m. ✅ **CHQ-V2 — v33.5.0**: دستیار هوشمند چک (OCR با llm.php + انتخاب مشتری/تامین‌کننده + کپی ضمیمه)؛ **اثر مالی به محض ثبت** (وارده → payment روی فاکتور مشتری؛ صادره → payment تامین‌کننده؛ برگشتی/ابطال → معکوس)؛ **چاپ برگه چک** (مبلغ به حروف)؛ **حذف ثبت چک از ماژول شخصی** (فقط هاب مالی؛ شخصی فقط چاپ). — tester215 (۴۱/۰).
4l. ⚠️ **OPEN-CAL-NAV و OPEN-PDF-CONVERT (باز — برای ایجنت‌های بعدی):** کارفرما تأیید کرد «تقویم درست نشد» و «تبدیل PDF به عکس ممکن نشد». هر دو در `BACKLOG-USER-2026-08-01.md` با جزئیات ثبت شدند؛ نیازمند بررسی روی استیجینگ واقعی (احتمالاً فقدان Imagick روی هاست برای PDF).

---

## ۷) استانداردهای ثابت پروژه (حتماً رعایت شود)

1. فقط روی `arena/019fbe19-pishtaj-ir` کار کن؛ هرگز مستقیم به `main` پوش نده.
2. هر تغییر مسیر/فایل → ریدایرکت ۳۰۱ در `.htaccess`؛ با `.htaccess` محتاط باش و قبل/حین تغییر به کاربر اطلاع بده.
3. بعد از هر تغییر: `python3 _tools/build_sitemap.py` سپس `python3 _tools/audit.py 2>&1 | tail -25` → **باید «۰ خطا»** باشد (هشدار قابل قبول). گیت انتشار: وجود `RELEASE-NOTES-v<VER>.md` و `REGRESSION-REPORT-v<VER>.md` متناظر با `window.VER`.
4. هر ویژگی → تستر دائمی Node.js در `_tools/uat/` (الگوی tester168/200) + اجرای کامل مجموعه قبل از هر کامیت بزرگ.
5. بعد از هر مرحله: کامیت + پوش + **به‌روزرسانی همین هند‌آف** تا ایجنت بعدی سریع از وضعیت باخبر شود.
6. تصاویر گواهی‌نامه: هرگز تاریخ/امضا/مهر/QR تغییر نکند (جعل سند).
7. سبک ارتباط کاربر: دقیق، مختصر، فارسی؛ هرگز ادعای «تأیید شده» بدون بررسی واقعی نکن.
8. مهاجرت MySQL: **هیچ backfill خودکاری بدون تأیید کارفرما**؛ هر گام = کامیت + تستر + تأیید استیجینگ.

---

## ۹.۵) بررسی کامل پیش از مهاجرت (read-only) — `_audit/FULL-AUDIT-PRE-MIGRATION-2026-08-01.md`

- A1 (کلیدهای sync/backup/server): ✅ بررسی شد — اختلاف عمدی/صحیح است.
- A2 (ماژول‌های بدون ارجاع): ✅ بررسی شد — فایل‌های رها (dead)، نه باگ؛ پیشنهاد حذف در آینده.
- BUG-001 (نمایش ناخالص مانده/اعتبار): ✅ رفع شد (v33.3.3).
- **باقی‌مانده بر اساس بحرانیت:** (۱) تعیین تکلیف PRها (#11 مرج؟) (۲) SEO-1: حذف ۴۹ موجیبیک + ریدایرکت ۳۰۱ (تأییدشده قبلی — نیاز اطلاع پیش از تغییر .htaccess) (۳) SEO-2: rename ۳۰ فایل (۴) SEO-3: متادیتا (۵) UX موبایل/alert (اختیاری) (۶) بدهی ۵۶ تستر (۷) مهاجرت MySQL.

---

## ۹) بک‌لاگ رسیدگی — گزارش‌های کاربر ۱۴۰۵/۰۸/۱۰ (۹ مورد — مرجع کامل: `crm/BACKLOG-USER-2026-08-01.md`)

| شناسه | عنوان | وضعیت |
|---|---|---|
| `UR-01` | تولبار تنخواه (ثبت هزینه/پرداخت مستقیم/شارژ/ارجاع دوره) در همهٔ تب‌های هاب مالی دیده می‌شد → فقط تب تنخواه | ✅ **رفع شد** (petty.js id=ptToolbar + financehub.js) — tester203 |
| `UR-02` | جستجوی نام تامین‌کننده/مشتری بعد از یک حرف فوکوس را از دست می‌دهد | ✅ **رفع شد** — بررسی کامل: اکثر رندرها فقط ناحیهٔ داده را عوض می‌کنند؛ تنها مورد واقعی `cfFinanceSearch` (بازسازی کل باکس با outerHTML) بود → اکنون فقط tbody را به‌روز می‌کند (customer-finance.js + `cfFinanceRowsHtml`). tester204 (۱۱/۰) |
| `UR-03` | حساب مشتری خروجی گردش حساب نمی‌دهد | ✅ **رفع شد** (با مورد ۸ — cfLedgerRows/Csv/Print + دکمه‌ها در مودال) — tester205 (۱۰/۰) |
| `UR-04` | در ثبت خرید واقعی «مجموع خرید» نمایش داده می‌شود؛ باید «قیمت واحد» بخواهد | ✅ **رفع شد** (هر دو جا طبق انتخاب کارفرما) — برچسب «جمع کل (اطلاعاتی)» + راهنمای قیمت واحد؛ tester202 (۲۵/۰) |
| `UR-05` | تایید ادغام کالاهای مشابه اثری ندارد و پنجره دوباره باز می‌شود | ✅ **رفع شد** (فیلتر hidden/merged + بستن به‌جای بازکردن) — tester201 |
| `UR-06` | سرچ تامین‌کننده با نام/برند در پنجرهٔ استعلام (per-item) | ✅ **رفع شد** — ptfSupSearch (امتیاز نام/برند/تجهیزات + مترادف دوزبانه) + ptfSupPicker در supspec.js؛ اتصال در ۳ محل buycompare (ثبت قیمت دور/تقسیم خرید/ثبت خرید واقعی) — tester206 (۱۳/۰) |
| `UR-07` | سورت‌بندی همهٔ فهرست‌ها (تاریخ/شرکت/…) | ✅ **رفع شد** — کامپوننت مشترک crm/sortable.js + اتصال در ۵ لیست (پیشنهادات/RFQ/تنخواه/حساب مشتریان/حساب تامین‌کنندگان)؛ ضمن رفع باگ فوکوس slFinanceSearch — tester207 (۱۴/۰) |
| `UR-08` | خروجی PDF/اکسل گردش حساب مشتریان | ✅ **رفع شد** — cfLedgerRows + cfLedgerCsv + cfLedgerPrint + دکمه‌های PDF/اکسل در مودال حساب — tester205 (۱۰/۰) |
| `UR-09` | گزارش کامل دورهٔ تنخواه (ردیف/تاریخ/مبلغ/توسط/شرح + جمع‌های زنده) | ✅ **رفع شد** — ptfPettyPeriodEvents/Totals/Report + CSV + چاپ + دکمه‌های «📊 گزارش دوره» و «گزارش دورهٔ جاری» — tester208 (۱۰/۰) |
| `UR-10` | PDF تلفیقی دورهٔ تنخواه (گزارش+رسیدها با شناسهٔ «سند N» + چیدمان ۳-در-صفحه) + الزام پیوست بانک قبل از ارجاع | ✅ **رفع شد** — ptfPettyPeriodCombinedPdf/ReceiptsHtml/Files + petId هنگام آپلود + دکمهٔ «📎 PDF تلفیقی» + مسدودسازی ارجاع بدون پیوست در pettyClosePeriod — tester209 (۱۳/۰) |

**ترتیب اجرا:** ۱ (تمام) → ۲ → ۳+۸ → ۴ → ۶ → ۷ → ۹ → ۱۰ (تمام). هر مورد = کامیت مستقل + تستر + به‌روزرسانی همین هند‌آف.

---

## ۱۰) فایل‌های کلیدی مرجع

- `_audit/REPORT-019FB322-2026-08-01.md` — گزارش کامل وضعیت برنچ 019fb322 (فازبندی CRM + کارهای باقی‌مانده)
- `crm/MIGRATION-MYSQL-SPEC-v1.md` — طرح نهایی مهاجرت MySQL (گام‌های A1–A6، B1–B5 + اقدام‌های کارفرما)
- `_audit/HANDOFF-NEXT-SESSION-2026-07-31.md` — هند‌آف جریان سئو (۹۹ مقاله تمام؛ ۴۹/۳۰/۲۰۹/فاز۳ باقی)
- `crm/DESIGN-OFFICIAL-UNOFFICIAL-SEPARATION-PHASE2.md` — سوابق فاز ۲ مالی (تمام)
- `crm/AUDIT-FINANCIAL-SYSTEM-2026-07-29.md` — ممیزی مالی (۱۱+ یافته رفع‌شده، توسعهٔ بانک/صندوق معلق)
- `_tools/uat/tester200/201/202-*.js` — تسترهای جدید جریان‌های ۷۱ کامیت

---

## ۱۱) نشست ۱۴۰۵/۰۸/۱۱ — CHQ-PRINT: ماژول مستقل «چاپ چک فیزیکی» (v33.6.0)

**درخواست کارفرما:** «هنوز قسمت ثبت چک در بخش شخصی وجود دارد…» — ماژول مستقل چاپ فیزیکی چک زیر «کالا و اسناد»، فقط چاپ (تکی/چندتایی)، بدون ذخیره‌سازی، بدون کد صیادی، تاریخ به حروف، ذی‌نفع + شناسه ملی، مبلغ عدد (قرمز بالا) + حروف، تنظیم فونت/سایز/مختصات هر بخش.

**تصمیمات کارفرما (ask_user):** ① حذف فقط از بخش شخصی؛ هاب مالی دست‌نخورده. ② چاپ برگه از هاب مالی حذف؛ چاپ فقط از ماژول جدید.

### انجام‌شده (کامیت‌ها)
| کامیت | کار |
|---|---|
| `3b5f6ef` | **CHQ-PRINT (v33.6.0)** — `crm/cheque-print.js` (پنل chqprint در گروه کالا و اسناد: چاپ تکی/چندتایی بدون ذخیره، بدون صیادی، تاریخ به حروف `ptfJDateWords`، مبلغ قرمز بالا `amtColor:#b91c1c`، فونت‌های نستعلیق/بی‌نازنین/بی‌یاقوت/… + کالیبره مختصات/سایز/رنگ هر بخش + چاپ آزمایشی)؛ حذف `chBoxHtml`/`hookReminders`/`chqBox` از cheques.js (باکس شخصی)؛ حذف `ptfChequePrint` + دکمه «چاپ برگه» از cheque-panel.js (چاپ فقط از ماژول جدید)؛ انتقال `ptfNumToFaWords` به cheque-print.js + **رفع کرش مبالغ تریلیونی**؛ بامپ v33.6.0 (index/sw/clear-cache)؛ rbac (حسابدار) + perms (ALL_PANELS) + shell (گروه کالا و اسناد)؛ تستر ۲۸۷ (۳۰/۰) + به‌روزرسانی ۲۱۵/۵۴/۱۹۳/۱۲۴/۱۴۵؛ اسناد انتشار + بک‌لاگ + همین هند‌آف؛ بازسازی `crm-update.zip` (۵۵۴ فایل). |

### نکات مهم برای نشست بعد
- **نتیجهٔ تست کارفرما از v33.5.0 هنوز اعلام نشده بود** — حالا v33.6.0 هم برای تست آماده است (به‌خصوص: پنل جدید «چاپ چک فیزیکی»، چاپ آزمایشی با راهنما، فونت نستعلیق/بی‌نازنین/بی‌یاقوت، قرمزی مبلغ بالا، نبود صیادی، نبود باکس چک در یادآورها).
- **کش مرورگر:** اگر کارفرما پنل جدید را ندید → `Ctrl+Shift+R` یا `crm/clear-cache.html` (نسخه باید v33.6.0 باشد).
- اگر کارفرما بخواهد، چیدمان ذخیره‌شدهٔ چاپ (localStorage `ptf_chqprint_layout_v1`) را می‌توان با اسکن برگهٔ چک بانکی خودش به‌عنوان پیش‌فرض تنظیم کرد (همان وعدهٔ قبلی).
- موارد باز قبلی: `OPEN-CAL-NAV` (تقویم — احتمالاً کش) و `OPEN-PDF-CONVERT` (Imagick روی هاست) — هنوز باز.
- بدهی تستی: ۵۲ شکست از قبل موجود (v15–v22 + عصر auth) — صفر شکست جدید.
- بعد از تست کارفرما: مهاجرت MySQL (گام A1 — ساخت دیتابیس ابری) یا فاز ۳ صفحات محصول/برند (بک‌لاگ `_tools/rewrite/product_pages_backlog.json`).

---

## ۱۲) نشست ۱۴۰۵/۰۸/۱۱ (دوم) — CHQ-PARTY + BUG-SH-SALARY + AUTO-RECURRING (v33.7.0)

**درخواست کارفرما:** ذی‌نفع شرطی (مشتری/تامین‌کننده/سایر) در ثبت چک + اثر مالی فوری + ضمانت با پرونده فروش + چاپ گرافیکی + مبلغ دوم + باگ حقوق سهامدار (۱۳۰→۹۰) + خودکارسازی ماهانه.

### انجام‌شده (کامیت `(این نشست)`)
- **cheque-module.js:** `ptfChequeSupOptions` (فقط دارای مطالبه + مبلغ بدهی)، `ptfChequeCustOptions` (فقط پرونده باز)، `ptfChequeOpenInvoicesOf`، `ptfChequePartyKind(Label)`؛ گارد «ضمانت بدون اثر مالی»؛ `ptfChequeCollect` اثر مالی هنگام وصول چک بدون فاکتور؛ **رفع باگ بازگشتی `faDateL/faDateTimeL`** (تاریخ payment خالی).
- **cheque-panel.js:** فرم ثبت دستی جدید (نوع ذی‌نفع + لیست‌های شرطی + نوع ضمانت + پرونده الزامی + فاکتور اختیاری)؛ دستیار AI هم‌گام؛ دکمه «🏆 استرداد ضمانت»؛ برچسب دسته/نوع در جدول‌ها.
- **cheque-print.js:** مبلغ دوم (پایین-چپ) + **حالت گرافیکی درگ** (chqGvRender/Start/Move) + ذخیره amt2.
- **salesfiles.js:** گارد مختومه: ضمانت باز = blocker + عبور صریح (`closeOverride.guaranteeConfirmed`).
- **projects.js:** نمایش چک‌های ضمانت پرونده.
- **shareholders.js:** `ensureSalaryTxForMonth` → **ریشه‌کنی ۱۳۰→۹۰** (سهامدار جدید در ماه جاری حقوقش ثبت نمی‌شد) + حذف هنگام غیرموظف‌شدن.
- **opex.js:** `ptfAutoApplyRecurring` (حقوق + قالب‌ها یک‌بار در ماه، audit + toast + برچسب «🤖 خودکار»).
- بامپ v33.7.0 + tester288 (۲۸/۰) + به‌روزرسانی ۲۱۵ (۴۵/۰)/۲۸۷ (۳۲/۰) + رگرسیون صفر شکست جدید + audit ۰ خطا + zip بازسازی.

### نکات مهم برای نشست بعد
- **نتیجهٔ تست کارفرما از v33.7.0** (به‌خصوص: لیست‌های شرطی ذی‌نفع، اثر مالی فوری، ضمانت/استرداد، درگ گرافیکی، مبلغ دوم، خودکارشدن حقوق ماه جدید و صحت جمع ۱۳۰).
- موارد باز قبلی: `OPEN-CAL-NAV` (تقویم) و `OPEN-PDF-CONVERT` — هنوز باز.
- ۵۲ شکست تستی از قبل موجود (بدهی v15–v22 + auth).
- بعد از تست: مهاجرت MySQL (گام A1) یا فاز ۳ صفحات محصول/برند.

---

## ۱۳) نشست ۱۴۰۵/۰۸/۱۱ (سوم) — CHQ-GV-INLINE + CHQ-THIRD + BUG پرونده باز (v33.8.0)

**بازخورد کارفرما:** گرافیک کوچک/مودال ناکارآمد → inline در صفحهٔ اصلی + اسکن پس‌زمینه؛ ثبت چک اول وارده/صادره بپرسد؛ پرونده باز مشتری دیده نمی‌شود؛ چک ثالث خرج و ثبت شود.

### انجام‌شده (کامیت این نشست)
- **cheque-print.js:** سکشن «🖱 حالت گرافیکی چیدمان (روی برگهٔ واقعی چک)» در `buildChequePrint` (بدون مودال)؛ مقیاس ۶px/mm + زوم `chqBgZoom`؛ `chqBgUpload` (اسکن → canvas ≤۱۶۰۰ → localStorage `ptf_chqprint_bg`) + `chqBgClear`؛ مودال فقط عددی شد (پیام ارجاع به صفحه).
- **cheque-module.js:** رفع باگ `ptfChequeCustOptions` (تطبیق نام buyerCo با مشتری — الگوی ptfOfferMatchCust)؛ `ptfChequeEndorse(cd, supName, note, supCd)` → خرج با اثر مالی فوری (payment در supplier-finance)؛ دستهٔ «third».
- **cheque-panel.js:** دیالوگ انتخاب «📥 وارده / 🏢 صادره» قبل از فرم (`ptfChequeNewForm(dir)`)؛ گزینهٔ «🪪 ثالث» (دستی + AI)؛ سِلکت نوع در دستیار (`ptfChAiDirChange`) با حفظ داده؛ پنجرهٔ «خرج کردن چک به تامین‌کننده» (`ptfChEndSup/Go`)؛ برچسب ثالث.
- بامپ v33.8.0؛ tester288 (۳۵/۰)؛ رگرسیون صفر شکست جدید؛ audit ۰ خطا؛ zip بازسازی.

### نکات مهم برای نشست بعد
- راستی‌آزمایی استیجینگ v33.8.0 توسط کارفرما (گرافیک inline + اسکن + خرج ثالث + پرونده باز).
- موارد باز قبلی: `OPEN-CAL-NAV` (تقویم)، `OPEN-PDF-CONVERT` (Imagick) — هنوز باز.
- ۵۲ شکست تستی از قبل موجود.

---

## ۱۴) نشست ۱۴۰۵/۰۸/۱۱ (چهارم) — رفع جهت درگ گرافیکی (v33.8.1)

**بازخورد کارفرما:** «درگ در جهت موس نیست (چپ→راست و بالعکس). برخی مقادیر هم اصلا در کادر نیستند.»

**ریشه:** در `chqGvSync` فیلدهای راست‌چسب با `(L.pageW - f.right) * scale` جانمایی می‌شدند → فیلدها به گوشهٔ چپ برگه پرتاب (روی هم و خارج از دید) و چون مقدار right برعکس تفسیر می‌شد، جهت درگ معکوس می‌شد.

**رفع (v33.8.1):**
- `chqGvSync`: فیلدهای راست‌چسب → `right: (f.right||0) * scale` (فاصله از لبهٔ راست — مثل چاپ واقعی).
- `chqGvMove`: درگ هم‌جهت (چپ‌چسب left+dx / راست‌چسب right−dx).
- `words`: عرض بین left/right در گرافیک + جابه‌جایی هر دو لبه در درگ + آپدیت هر دو input.
- tester288 (۳۸/۰) + رگرسیون صفر شکست جدید + audit ۰ خطا + zip بازسازی + اسناد v33.8.1.

### نکات برای نشست بعد
- راستی‌آزمایی کارفرما: درگ گرافیکی حالا هم‌جهت با موس است و همهٔ ۸ فیلد (تاریخ/تاریخ حروف/ذی‌نفع/کد ملی/مبلغ بالا/مبلغ پایین-چپ/مبلغ حروف/بابت) در جای درست کادر دیده می‌شوند.
- موارد باز قبلی: `OPEN-CAL-NAV`، `OPEN-PDF-CONVERT` — باز.

---

## ۱۵) نشست ۱۴۰۵/۰۸/۱۱ (پنجم) — PROFIT-REVIEW + UI-MINIMAL-RFQ + MONEY-FIELDS (v33.9.0)

**درخواست کارفرما:** ① بررسی منطق سود/تقسیم سهامداران (چک در راه / بدهی باز / مطالبات باز) + استخراج باگ + پیشنهادات ② مینیمال‌کردن گزینه‌های درخواست‌ها ③ پوشش سراسری فیلدهای مبلغ.

### انجام‌شده (کامیت این نشست)
- **سند تحلیل:** `crm/ANALYSIS-PROFIT-DIVIDEND-2026-08-02.md` — پاسخ‌ها: چک در راه کسر نمی‌شود (اما در ارزی به‌عنوان وصول می‌شمارد)؛ بدهی باز خریدهای ثبت‌شده کسر می‌شود (خدماتی‌ها نه)؛ مطالبات باز ریالی در درآمد هست (ارزی نه). ۷ باگ (P-1..P-7) + ۶ پیشنهاد (سقف نقدینگی تقسیم / یکسان‌سازی مبنا / اثر چک در وصول / کسر بدهی خدمات / دکمه ثبت واقعی تقسیم / چک در راه در تراز).
- **bridge.js:** اکشن‌های درخواست‌ها → آیکون ۳۲px (👁️ ✏️ 📨) با tooltip.
- **moneyx.js:** `ptfEnDigits/ptfFaDigits` + `moneyLike` + لیسنر focusout (فرمت خودکار + مبلغ به حروف) با خروج امن و `data-nomoney` opt-out.
- بامپ v33.9.0 + tester289 (۷/۰) + tester112 (۴۲/۰) + رگرسیون صفر شکست جدید + audit ۰ خطا + zip.

### نکات مهم برای نشست بعد
- **تصمیم کارفرما برای اجرای پیشنهادات سود لازم است** (پیشنهاد تیم: ۱+۲(تعهدی)+۳+۵ در یک ریلیز، سپس ۴ و ۶).
- راستی‌آزمایی استیجینگ v33.9.0.
- موارد باز قبلی: OPEN-CAL-NAV، OPEN-PDF-CONVERT — باز.
- ۵۲ شکست تستی از قبل موجود.

---

## ۱۶) نشست ۱۴۰۵/۰۸/۱۱ (ششم) — CASH-PROFIT: منطق نقدی سود/تقسیم (v33.10.0)

**تصمیم کارفرما:** فروش منبع درآمد است نه مبنای واقعی؛ درآمد واقعی = وصول مطالبات؛ خرید نقدی/غیرنقدی (شامل چک‌ها حتی بابت مسائل دیگر) از درآمد کسر شود؛ کف نقدینگی در گردش؛ تقسیم فقط از مازاد بر کف + بازگشت ۴۰٪ به کف.

### انجام‌شده (کامیت این نشست)
- **fiscal.js:** `ptfFiscalCashData` (درآمد = وصولی نقد + چک وصول‌شده؛ خروجی = فاکتور خرید + پرداخت بدون تخصیص + چک صادرهٔ مستقل + opex + تنخواه — بدون دوباره‌شماری) + `ptfFiscalCashDistribution` (مازاد بر کف → تقسیم ٪ + بازگشت به کف) + `ptfFiscalCashFloor(Set/Open)` (کف دستی در snapshots) + `ptfFiscalDividendApply` (ثبت واقعی type:'profit' در sharetx + قفل سال + یک‌بار در سال + گارد زیر کف) + بلوک UI «💵 سود نقدی و تقسیم».
- **cheque-module.js:** چک وارده اثر مالی فقط هنگام وصول (pendingFinancial در ثبت)؛ برگشتی پس از وصول مسدود (state).
- سند `ANALYSIS-PROFIT-DIVIDEND-2026-08-02.md` بخش ۵ (فرمول‌ها + وضعیت باگ‌ها: P-2/P-6/P-3/P-7 رفع؛ P-4/P-5 بخشی).
- بامپ v33.10.0 + tester290 (۱۵/۰) + به‌روزرسانی ۲۱۵ (۴۷/۰)/۲۸۸ (۳۹/۰) + رگرسیون صفر شکست جدید + audit ۰ خطا + zip.

### نکات مهم برای نشست بعد
- **راستی‌آزمایی کارفرما:** تعیین کف نقدینگی، مشاهده مازاد/قابل تقسیم/بازگشت به کف، ثبت تقسیم در دفاتر سهامداران (مانده سهامداران += سهم).
- دقت: «نقد/بانک ابتدای سال» از ماندهٔ افتتاحیهٔ cash_bank در گزارش سرمایه در گردش خوانده می‌شود — اگر خالی بود صفر (کاربر باید افتتاحیه/کف را تنظیم کند).
- موارد باز قبلی: OPEN-CAL-NAV، OPEN-PDF-CONVERT — باز.
- ۵۲ شکست تستی از قبل موجود.

---

## ۱۷) نشست ۱۴۰۵/۰۸/۱۱ (هفتم) — حذف جدول تعهدی قدیمی + رفع به‌هم‌ریختگی هاب مالی (v33.11.0)

**بازخورد کارفرما:** ① جدول قبلی (اعداد اشتباه) هنوز زیر جدول فعلی در داشبورد سال مالی است → حذف شود. ② در سراسر هاب مالی آشفتگی است: هاب وسط صفحه، چک‌های در راه و جزئیات تامین‌کنندگان در همهٔ تب‌ها.

### انجام‌شده (کامیت این نشست)
- **fiscal.js:** حذف KPIهای تعهدی (سود/زیان پروژه، سود خالص مدیریتی، اندوخته، مطالبات باز) و جدول «کاربرگ تقسیم سود» قدیمی از داشبورد؛ گزارش رسمی (ptfFiscalCashReportHtml) و CSV → **نقدی** (درآمد/خروجی/کف/مازاد/قابل تقسیم/بازگشت به کف + سهامداران نقدی).
- **financehub.js:** `slLiquidity` فقط در تب supacc (ریشهٔ «چک‌های در راه و جزئیات تامین‌کنندگان در همهٔ تب‌ها» — این باکس در لیست show نبود) + `finHubOrder` (ریشهٔ «هاب وسط صفحه» — opexBox قبل از bar چیده می‌شد) + فراخوانی در finHubApply/renderPetty.
- بامپ v33.11.0 + tester291 (۱۲/۰) + رگرسیون صفر شکست جدید + audit ۰ خطا + zip.

### نکات برای نشست بعد
- راستی‌آزمایی کارفرما: هاب مالی تمیز (نوار بالا، هر باکس در تب خودش) و داشبورد سال مالی فقط نقدی.
- موارد باز قبلی: OPEN-CAL-NAV، OPEN-PDF-CONVERT — باز.
- ۵۳ شکست تستی از قبل موجود.

---

## ۱۸) نشست ۱۴۰۵/۰۸/۱۱ (هشتم) — ریشه‌کنی اعتبار مشتری + taxplanner + دارک‌مود (v33.12.0)

**بازخورد کارفرما:** ① داشبورد برنامه‌ریزی فصلی مالیات در تمام تب‌هاست. ② تعهدات نقدینگی تامین/چک پس‌زمینهٔ روشن دارد و در نمای شب دیده نمی‌شود. ③ اعتبار مشتری با آپدیت‌های اخیر از بین رفته در حالی که کالاهای برگشتی در موجودی هست — ریشه‌یابی عمیق.

### انجام‌شده (کامیت این نشست)
- **customer-finance.js (BUG-CREDIT):** ریشهٔ قطعی — تطبیق مرجوعی فقط با `invoiceCd` بود؛ مرجوعی‌های قدیمی بدون invoiceCd (فقط offerNo/invoiceNo) گم می‌شدند → returnedAmount=0 → اعتبار=0. رفع: `salesReturnsForInvoice(invoice)` مقاوم (invoiceCd → invoiceNo → offerNo با شرط یکتایی فاکتور، بدون دوباره‌شماری) + `cfRepairReturnLinks` (ترمیم خودکار در بوت، idempotent، audit) + `cfCreditAudit(cd)` (گزارش اجزا).
- **financehub.js:** `show('ptfTaxPlannerBox', t==='fiscal')` + finHubOrder + دارک‌مود CSS برای `#slLiquidity` و `#ptfTaxPlannerBox`.
- بامپ v33.12.0 + tester292 (۹/۰) + رگرسیون صفر شکست جدید + audit ۰ خطا + zip.

### نکات برای نشست بعد
- راستی‌آزمایی کارفرما: اعتبار مشتری (مرجوعی‌های قدیمی) باید دوباره نمایش داده شود؛ تب‌های هاب تمیز؛ نمای شب خوانا.
- **پاسخ «خروجی‌های سال چگونه محاسبه می‌شود؟»** در ریلیزنامه/پاسخ چت داده شد (فاکتورهای خرید + پرداخت بدون تخصیص + چک صادرهٔ مستقل + opex + تنخواه؛ بدون دوباره‌شماری؛ ضمانت مستثنا).
- موارد باز قبلی: OPEN-CAL-NAV، OPEN-PDF-CONVERT — باز.
- ۵۳ شکست تستی از قبل موجود.

---

## ۱۹) نشست ۱۴۰۵/۰۸/۱۱ (نهم) — 🚨 فوری: بکاپ/بازگردانی (v33.13.0)

**مصوبهٔ کارفرما:** پر شدن حافظهٔ مرورگر در بکاپ/بازگردانی + عدم اتصال به سرور = فوری → برنامهٔ دقیق + اجرای فاز ۰.

### انجام‌شده (کامیت این نشست)
- **سند برنامه:** `crm/PLAN-BACKUP-STORAGE-URGENT-2026-08-02.md` — ریشه‌ها R1..R6 + فاز ۰/۱/۲ + DoD.
- **backup.js فاز ۰:** `backupFetch` (timeout ۲۰s + AbortController)؛ 401/needLogin → `ptfSyncRefreshAuth` + retry (در save_backup و data_push بازگردانی)؛ `backupStoreLocalFallback` (فقط IDB — هرگز localStorage حجیم؛ در IDB fail → marker با پیام)؛ `doRestore` با `ptfStorageSafeSetItem` + failedKeys گزارش‌شده؛ حذف `ptf_storage_queue` از DATA_KEYS؛ PAYLOAD_CAPS (audit ۱۰۰۰/notifs ۵۰۰).
- بامپ v33.13.0 + tester293 (۱۵/۰) + رگرسیون صفر شکست جدید + audit ۰ خطا + zip.

### نکات مهم برای نشست بعد (فاز ۱ فوری)
- باکس بکاپ: «🔌 بررسی اتصال سرور» (پینگ users_get) + نمایش وضعیت + «تلاش مجدد».
- بکاپ خودکار فقط در صورت تغییر (هش داده) — حذف ارسال ساعتی بی‌شرط (R6).
- مهاجرت avatars/فایل‌های بزرگ به IndexedDB (تکمیل storage-quota).
- فاز ۲: بکاپ دلتا + ذخیرهٔ MySQL + بازگردانی تراکنشی.
- موارد باز قبلی: OPEN-CAL-NAV، OPEN-PDF-CONVERT — باز.
- ۵۴ شکست تستی از قبل موجود.

---

## ۲۰) نشست ۱۴۰۵/۰۸/۱۱ (دهم) — ریشهٔ کم‌اثری پاک‌سازی/مهاجرت + رفع (v33.14.0)

**پرسش کارفرما:** «چرا مهاجرت و پاک‌سازی امن فوری حجم اشغال‌شده را کاهش نمی‌دهد؟»
**ریشه (تأیید در کد):** ۱) دادهٔ اصلی روی localStorage (سقف ~۵MB). ۲) emergencyCompact فقط کلیدهای حاشیه (notifs/sendqueue/audit/web_events/chat/draft/ai) را هرس می‌کرد؛ کلیدهای اصلی عمداً حذف نمی‌شوند. ۳) migrateVolatileToIdb هم فقط همان حاشیه‌ها را جابه‌جا می‌کرد. ۴) بزرگ‌ترین عوامل پنهان: avatars (base64)، storage_queue (انجام‌شده‌ها پاک نمی‌شدند)، audit/notifs بدون هرس سنی، فایل‌های base64 داخل کلیدهای اصلی.

**رفع (v33.14.0):** compactAvatars (۲۰ آخر + آرشیو IDB)، compactStorageQueue (انجام‌شده/۷روز)، هرس سنی audit(۱۸۰روز، سقف ۱۰۰۰ حفظ)/notifs(۹۰روز)/sendqueue(۳۰روز)، isVolatileArchiveCandidate گسترده، راهنمای صریح UI (علت + کلیدهای بزرگ + ارجاع MySQL). بامپ + tester294 (۸/۰) + رگرسیون صفر شکست جدید + audit ۰ خطا + zip.

### نکات برای نشست بعد
- فاز ۱ بکاپ (بررسی اتصال + بکاپ فقط در صورت تغییر + avatars→IDB تکمیلی).
- راه‌حل نهایی ساختاری: مهاجرت MySQL (DB-MIG-001) — با رشد داده، localStorage همیشه پر می‌شود.
- موارد باز: OPEN-CAL-NAV، OPEN-PDF-CONVERT — باز.
- ۵۴ شکست تستی از قبل موجود.

---

## ۲۱) نشست ۱۴۰۵/۰۸/۱۱ (یازدهم) — فاز ۱ بکاپ (v33.15.0)

**انتخاب کارفرما:** «بزن بریم گام بعد» → فاز ۱ برنامهٔ فوری بکاپ.

### انجام‌شده (کامیت این نشست)
- **backup.js:** `ptfBackupServerStatus`/`ptfBackupServerCheck` (پینگ users_get → آنلاین/needLogin/آفلاین + دکمه در باکس بکاپ + ناحیهٔ ptfBackupConn)؛ `backupSignature` (djb2 روی **محتوای کامل** — نسخهٔ اول فقط طول کلیدها را می‌شمرد؛ با تغییر هم‌طول بکاپ نمی‌رفت — باگ کشف و رفع شد)؛ `ptfBackupMaybeAuto` (هستهٔ تصمیم: no_change → skip؛ push موفق → امضا آپدیت؛ خطا → امضا نمی‌ماند)؛ scheduleBackups → MaybeAuto.
- **storage-quota.js:** `ptfStorageAutoTame` (≥۸۰٪ → پاک‌سازی امن خودکار یک‌بار در روز با flag `ptf_storage_auto_tame`).
- بامپ v33.15.0 + tester295 (۱۲/۰) + رگرسیون صفر شکست جدید + audit ۰ خطا + zip.

### نکات برای نشست بعد
- فاز ۲: بکاپ دلتا (فقط کلیدهای تغییرکرده) + ذخیرهٔ MySQL + بازگردانی تراکنشی.
- یا مهاجرت MySQL (گام A1) — راه‌حل نهایی حافظه.
- موارد باز: OPEN-CAL-NAV، OPEN-PDF-CONVERT — باز.
- ۵۵ شکست تستی از قبل موجود.

---

## ۲۲) نشست ۱۴۰۵/۰۸/۱۱ (دوازدهم) — فاز ۲ بکاپ (v33.16.0)

**انتخاب کارفرما:** «فاز ۲ بکاپ».

### انجام‌شده (کامیت این نشست)
- **backup.js:** hashString + امضای per-key (`ptf_backup_delta_sig`) + `ptfBackupDeltaCollect` + `pushBackupDelta` (با refresh توکن و fallback needFull→کامل) + `ptfBackupMaybeAuto` (بار اول کامل، بعد دلتا) + `ptfBackupNow` (کامل + آپدیت sig) + **doRestore تراکنشی** (snapshot قبلی → در صورت هر کلید ناموفق rollback کامل + audit RESTORE-FAIL + پیام بدون reload) + export ها (ptfBackupRestore/PushDelta/PushFull).
- **api/crm.php:** تابع مشترک `ptf_rotate_backup()` (سپر shrink + gzip + چرخش + آروان) + case `save_backup_delta` (ادغام دلتا با hourly-latest، بازمحاسبه counts، چرخش) + بازنویسی `save_backup` با تابع مشترک.
- بامپ v33.16.0 + tester296 (۱۰/۰) + به‌روزرسانی tester295 + رگرسیون صفر شکست جدید + audit ۰ خطا + zip.

### نکات برای نشست بعد
- فاز ۲/MySQL: ذخیرهٔ بکاپ در MySQL پس از مهاجرت DB-MIG-001 (ساختار دلتا آماده است).
- یا مهاجرت MySQL (گام A1) / فاز ۳ صفحات محصول-برند.
- موارد باز: OPEN-CAL-NAV، OPEN-PDF-CONVERT — باز.
- ۵۶ شکست تستی از قبل موجود.

---

## ۲۳) نشست ۱۴۰۵/۰۸/۱۱ (سیزدهم) — شروع مهاجرت MySQL — فاز A (v33.17.0)

**تصمیم کارفرما:** مهاجرت آغاز شود (کمترین کار او، نهایت امنیت، سی‌پنل بدون ترمینال، راهنمای فارسی، انتقال داده‌های پروداکشن، راهنمای کاربران).

### انجام‌شده (کامیت این نشست)
- **api/db-lib.php (جدید):** کانفیگ امن (`ptf-db-config.php` بدون خروجی)، mysqli+utf8mb4، جدول `ptf_kv`، عملیات پایه null-safe، حالت‌های off/dual/mysql، checksum SHA-256، dual-write fail-safe.
- **api/migrate.php (جدید):** اسکریپت فارسی ۶ مرحله (بکاپ اضطراری → ساخت جدول → انتقال → بررسی تطابق → فعال‌سازی نوشتن هم‌زمان → سوییچ نهایی)؛ تأیید با کلمهٔ «مهاجرت»؛ کلیدهای otp/ratelimit مستثنا؛ راهنمای حذف امن.
- **api/crm.php:** wrapper روی load_data/save_data (dual-write؛ خواندن از دیتابیس در حالت mysql با fallback).
- **crm/MIGRATION-MYSQL-CPANEL-GUIDE-FA.md (جدید):** راهنمای کامل فارسی (۵ مرحله + عیب‌یابی + راهنمای کاربران + ضمانت‌های امنیتی).
- سند اصلی بخش ۱۰ به‌روزرسانی شد؛ بامپ v33.17.0 + tester297 (۱۶/۰) + رگرسیون صفر شکست جدید + audit ۰ خطا + zip.

### اقدام بعدی — با کارفرما
1. کارفرما: ساخت دیتابیس در سی‌پنل (MySQL Database Wizard) + آپلود `api/migrate.php` + باز کردن آدرس + اجرای ۶ دکمه (طبق راهنمای فارسی).
2. تأیید «بررسی تطابق» (همه ✅) → فعال‌سازی نوشتن هم‌زمان → (پس از چند روز/تأیید) سوییچ نهایی.
3. پس از سوییچ: کاربران یک بار Ctrl+Shift+R؛ فایل migrate.php حذف شود.
4. سپس فاز B (کلاینت نازک) — localStorage فقط کش.
- توجه: PHP روی محیط توسعه موجود نیست؛ پیشنهاد راستی‌آزمایی PHP روی استیجینگ (که PHP دارد) قبل از اجرای پروداکشن.
- موارد باز: OPEN-CAL-NAV، OPEN-PDF-CONVERT — باز. ۵۶ شکست تستی از قبل موجود.

---

## ۲۴) نشست ۱۴۰۵/۰۸/۱۱ (چهاردهم) — v33.17.1: آماده‌سازی مرج PR #11 به main + deploy پروداکشن

**وضعیت مهاجرت (فاز A):** استیجینگ و پروداکشن هر دو مهاجرت را کامل کردند (بررسی تطابق ✅؛ dual فعال؛ سوییچ نهایی). جداول مجزا: `ptf_kv` (استیجینگ) / `ptf_kv_prod` (پروداکشن). فایل‌های JSON محفوظ.

**انجام‌شده در این نشست:**
- گارد امنیتی `api/migrate.php`: اگر mode=mysql → فقط پیام «کامل شده؛ فایل را حذف کنید» (حتی بعد از deploy مجدد، مهاجرت دوباره اجرا نمی‌شود).
- `.gitignore` + `api/ptf-db-config.php` (رمز دیتابیس به گیت نمی‌رود).
- بامپ v33.17.1 + tester297 (۱۹/۰) + رگرسیون صفر شکست جدید + audit ۰ خطا + zip.

**اقدام بعدی — با کارفرما:**
1. مرج PR #11 به main (دکمهٔ Merge در GitHub — MERGEABLE است).
2. GitHub → Actions → **Deploy to Production** → Run workflow → `confirm=DEPLOY`، `ref=main`.
3. پروداکشن از v33.2.0 → v33.17.1 (۱۵ نسخه جهش؛ همهٔ اصلاحات CRM/بکاپ/سود/چک/مهاجرت).
4. کاربران: یک بار Ctrl+Shift+R؛ پرحافظه‌ها: تنظیمات → پاک‌سازی امن فوری → مهاجرت به IndexedDB.
5. فایل migrate.php پروداکشن حذف شود (اگر گارد قفلش کرده، حتی بدون حذف هم امن است).

**پس از مرج:** فاز B (کلاینت نازک — localStorage فقط کش) + موارد باز OPEN-CAL-NAV / OPEN-PDF-CONVERT + ۵۶ شکست تستی قدیمی.

---

## ۲۵) نشست ۱۴۰۵/۰۸/۱۱ (پانزدهم) — فاز B: کلاینت نازک (v33.18.0)

**تصمیم کارفرما:** تا پایان فاز B مرج نمی‌شود. پاسخ‌ها: آفلاین=صف+همگام‌سازی مجدد؛ هم‌گرایی=یک‌باره با تأیید؛ دامنه=همهٔ کلیدها.

### انجام‌شده (کامیت این نشست)
- **crm/client-server.js (جدید):** هوک getData (سرور + کش ۳۰ثانیه؛ آفلاین→محلی) و setData (کش + صف ptf_b_queue + push debounced؛ خطا→صف می‌ماند)؛ هم‌گرایی یک‌باره با تأیید (فلگ ptf_b_flushed_<user> — ریشهٔ باگ flushKey با curSession رفع شد)؛ فعال/غیرفعال‌سازی (سوییچ‌بک فوری)؛ کلیدها=همه (fallback).
- **crm/backup.js:** باکس «🌐 حالت سرور-محور» (۳ دکمه).
- **crm/index.html:** لود client-server.js بعد از sync.js.
- سند `crm/DESIGN-PHASE-B-CLIENT-SERVER.md` (تصمیمات/گام‌های تست/ریسک‌ها).
- بامپ v33.18.0 + tester298 (۸/۰) + رگرسیون صفر شکست جدید + audit ۰ خطا + zip.

### نکات مهم برای نشست بعد
- **گام بعدی:** استقرار v33.18.0 روی استیجینگ → تست فاز B: ۱) فعال‌سازی → هم‌گرایی یک‌باره ۲) کارکرد عادی ۲-۳ روز ۳) آفلاین (DevTools Offline) → ثبت → صف → بازگشت اتصال → push خودکار ۴) رگرسیون.
- پس از تأیید تست: **مرج PR #11 → deploy پروداکشن (confirm=DEPLOY, ref=main)** → فعال‌سازی فاز B به‌صورت تدریجی.
- فاز A (سرور→MySQL) قبلاً روی استیجینگ/پروداکشن کامل شده (جداول ptf_kv / ptf_kv_prod).
- موارد باز: OPEN-CAL-NAV، OPEN-PDF-CONVERT — باز. ۵۶ شکست تستی از قبل موجود.
