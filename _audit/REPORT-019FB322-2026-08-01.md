# گزارش وضعیت برنچ `arena/019fb322-pishtaj-ir`

**تاریخ تهیه:** ۱۴۰۵/۰۸/۱۰ (2026-08-01)
**HEAD برنچ:** `a9af260` — «feat(crm): add confirmed catalog merge workflow» (2026-08-01 16:03 UTC)
**وضعیت:** هیچ PR بازی برای این برنچ وجود ندارد؛ همهٔ کارهایش در `main` مرج نشده است.

---

## ۱) نمای کلی

| آیتم | مقدار |
|---|---|
| کامیت‌های جلوتر از `main` | **۱۶۵** |
| رابطه با `main` | `main` (کامیت `4551713` = PR #9) کاملاً **نیای** برنچ است؛ بدون هیچ انشعاب/کانفلیکت |
| ساختار برنچ | **۹۴ کامیت** جریان سئو (برنچ `arena/019fa4f1` = PR #10، هنوز باز) + **۷۱ کامیت** کار اختصاصی CRM |
| نقطهٔ ادغام جریان سئو | کامیت `c6e0a3d` — «merge: integrate SEO branch for staging review» |
| تغییرات فایل در برابر `main` | ۱۴۱ فایل افزوده، ۹۹ فایل حذف، ۳۵۶ فایل تغییر (مجموع +۳۲,۳۵۰ / −۶,۷۰۱ خط) |
| وضعیت ابزارها | `_tools/audit.py` = ۰ خطا؛ sitemap.xml هم‌گام با **۵۲۰ URL**؛ ۲۰ بخش `# SEO-KC-REWRITE` ریدایرکت ۳۰۱ در `.htaccess` |
| Deploy استیجینگ | پوش‌های اخیر برنچ با تریگر `arena/**` روی استیجینگ موفق بوده (یک ران قدیمی فقط FTP timeout گذرا داشت) |

> نکته: بخش سئوی این برنچ همان کارهای PR #10 است (هنوز باز). بخش CRM این برنچ **هیچ PR ندارد** و کامل‌ترین بخش کار مرج‌نشده است.

---

## ۲) کارهای انجام‌شده (مرج‌نشده در `main`)

### ۲.۱ جریان سئو — ۹۴ کامیت (از برنچ `arena/019fa4f1`، PR #10)

1. **بازنویسی کامل ۹۹/۹۹ مقالهٔ مرکز دانش** — هر مقاله ≥۱۵۰۰ کلمه، متادیتای کامل سئو (title ≤۶۵، description، canonical، hreflang خودارجاع + x-default، OG، JSON-LD از نوع Article+FAQPage+BreadcrumbList)، لینک‌دهی متقابل با مقالهٔ توأمان (twin)، ثبت در آرایهٔ `const cats` در `knowledge-center/index.html`، و ریدایرکت ۳۰۱ برای فایل‌های قدیمی حذف‌شده.
2. **حذف نسل قدیمی مقالات هش‌نام با دوقلوی پرمحتوا (دستهٔ ۲ — ۴۸ فایل)** — از برنچ صفر فایل `c-/k-/z-xxxxxxxx.html` با دوقلو باقی مانده است.
3. **بهبود کیفیت تصاویر گواهی‌نامه‌های ایزو/HSE** — یکسان‌سازی سایز/زاویه/نور **بدون تغییر تاریخ، امضا، مهر و QR** (طبق تأیید صریح کاربر)؛ دو باگ واقعی (کراپ امضا/QR و نور نامتوازن ایزو ۱۰۰۰۲) کشف و رفع شد.
4. **زیرساخت سئو** — sitemap.xml بازسازی‌شده (۵۲۰ URL، namespace استاندارد)، `_tools/audit.py` و `_tools/build_sitemap.py`، `_tools/rewrite/` (plan.json، render_article.py، extend_helper.py، batch1..30)، اسناد ممیزی/کلمات کلیدی در `_audit/`.
5. **اسناد راهبردی** — `KEYWORD-STRATEGY-2026-07-29.md`، `BRAND-PRODUCT-CATALOG-2026-07-29.md` (فهرست برند/محصول + ۱۶ آیتم بک‌لاگ ماشین‌خوان)، و `HANDOFF-NEXT-SESSION-2026-07-31.md`.

### ۲.۲ جریان CRM — ۷۱ کامیت اختصاصی این برنچ (۳۰ ژوئیه تا ۱ اوت)

| حوزه | کامیت‌ها | شرح |
|---|---|---|
| **قابلیت اطمینان ذخیره‌سازی** | ۴ | بازگرداندن نتیجهٔ `setData`/ذخیره، هشدار دیداری خطای ذخیره (ظرفیت پر/خطای localStorage)، نمایش خطا در فیلتر مشتریان |
| **اظهارنامه مالیاتی** | ۴ | مجوز همگام‌سازی سرور برای نقش‌های مجاز، ورود اظهارنامه‌ها در پشتیبان‌گیری، همگام‌سازی نقش‌های ارشد یکسان |
| **هزینه‌های جاری (Opex)** | ۳ | حفظ وضعیت رسمی هزینه‌های دوره‌ای، تثبیت طبقه‌بندی قالب‌های قدیمی، اتصال مشکلات کیفیت داده به ویرایشگر opex |
| **کیفیت داده و اصلاحات مالی هدایت‌شده** | ۳ | اصلاحات مالی هدایت‌شده، جمع‌شدن/بازشدنی شدن لیست مشکلات، گروه‌بندی مشکلات تدارکات و مالکیت چک |
| **اتصال خرید به فاکتور** | ۶ | اتصال اقلام فروش به فاکتورهای خرید، تشخیص اتصال تکراری، مخفی‌سازی فاکتورهای ابطال‌شده + ویرایش طبقه‌بندی، حذف ارجاع‌های داخلی RFQ از خروجی تامین‌کننده، جمع فاکتورهای تامین‌کننده در خروجی حساب |
| **افزوده‌های فروش (Addenda)** | ۸ | شمول افزوده‌ها در ممیزی تدارکات/استعلام پرونده/گردش‌کارهای پس از برنده‌شدن، ادغام جداول خرید افزوده‌ها، جایگذاری و حذف تکراری اقلام افزوده، حفظ دکمهٔ خرید واقعی |
| **خرید واقعی در برابر دفتر تامین‌کننده** | ۴ | پیشنهاد تامین‌کنندگان ثبت‌شده، خارج‌کردن خریدهای واقعی از دفتر تامین‌کننده، لینک تسویه با فاکتور تامین‌کننده، تطبیق بستانکاری‌ها با هویت تامین‌کننده |
| **لوت‌های خرید (Split Lots)** | ۱۱ | نرمال‌سازی لوت‌ها برای رهگیری مقدار، پیشرفت آگاه‌از-مقدار، لوت‌های استثنایی، ورودی عددی قابل‌استفاده، ارز/نرخ در لوت‌های تفکیکی، پیش‌نمایش read-only توزیع خرید + ممیزی مالی legacy |
| **برگشت از تامین‌کننده (Supplier Returns)** | ۵ | پیش‌نمایش توزیع برگشت، دلایل هدایت‌شدهٔ برگشت جزئی، ذخیره/نمایش برگشت جزئی، انتقال عملیاتی لوت به انبار |
| **برگشت فروش و اعتبار مشتری (Sales Returns)** | ۱۴ | قرارداد ذخیره‌سازی، پیش‌نمایش خطوط قابل‌انتخاب، ثبت برگشت + موجودی اعتبار، نمایش در حساب مشتری، جلوگیری از ناهماهنگی خط فاکتور، انتقال اقلام به انبار، حل هویت محصول، تلاش مجدد انتقال‌های معلق، نمایش اعتبار در لیست حساب، تطبیق هویت نرمال‌شدهٔ مشتری، رفع انباشت دیالوگ‌ها |
| **هویت کاتالوگ (Catalog Identity — آخرین جریان، ۱ اوت)** | ۹ | راهنمای ورود محصولات برگشتی به کاتالوگ، پیشنهاد نزدیک‌ترین محصول قابل جستجو، تازه‌سازی نماها پس از تغییر هم‌تب، ممیزی read-only هویت کاتالوگ، اتصال دقیق کنترل‌شده، بازبینی هدایت‌شده + صف بازبینی، ممیزی read-only شباهت معنایی (fingerprint)، و **گردش‌کار ادغام تأییدشده** (`ptf_crm_catalog_merges` + بازنویسی ارجاع‌ها در کلیدهای دیگر + همگام‌سازی/پشتیبان/نقش‌ها) |
| **تست‌های UAT** | ۳ فایل | به‌روزرسانی tester90/tester95/tester115 برای رفتار جدید «خرید واقعی دیگر بستانکاری خودکار نمی‌سازد» |

---

## ۳) کارهای باقی‌مانده

### ۳.۱ جریان سئو (مرجع: `_audit/HANDOFF-NEXT-SESSION-2026-07-31.md`)

| اولویت | کار | وضعیت | نیاز به تأیید؟ |
|---|---|---|---|
| ۱ | **حذف ۴۹ فایل موجیبیک** در `knowledge-center/` (نام‌های `#U0627#...`) + ریدایرکت ۳۰۱ + اصلاح لینک‌های داخلی + sitemap/audit | ✅ تأییدشده، شروع‌نشده — ۴۹ فایل هنوز روی برنچ موجودند (فهرست در `KC-DEDUP-FINAL-5CATEGORIES` و `KC-DEDUP-EXACT-LISTS`) | خیر (کار مکانیکی) |
| ۲ | **تغییر نام ۳۰ فایل هش‌نام یتیم** (دستهٔ ۳) — rename + ۳۰۱ + اصلاح لینک + ثبت در index.html؛ اسلاگ‌های پیشنهادی در `_tools/rewrite/plan.json` | ۳۰ فایل هنوز روی برنچ موجودند | خیر (کار مکانیکی) |
| ۳ | **رفع ۲۰۹ نقض طول title/description** در مقالات دست‌نخورده (title>۶۵ یا description>۱۶۵) | شروع‌نشده؛ جزئیات در `SEO-AUDIT-ROADMAP-2026-07-27.md` | خیر (فقط متادیتا) |
| ۴ | **فاز ۳ صفحات محصول/برند** — ۱۶ آیتم بک‌لاگ در `_tools/rewrite/product_pages_backlog.json` | آماده، **قبل از شروع باید ۳ سؤال از کاربر پرسیده شود** (بخش ۳.۳ همین گزارش) | **بله — ضروری** |
| ۵ | موارد کوچک: گروه‌های title تکراری، ۱۷ صفحه بدون favicon، ۱۷ مقالهٔ زیر ۸۰۰ کلمه | عمدتاً با حذف ۴۹ موجیبیک (بند ۱) خودبه‌خود حل می‌شوند | — |
| ۶ | تأیید چشمی نهایی صفحهٔ `about/certificates/index.html` توسط کاربر | باقی‌مانده (در بازبینی پروداکشن) | — |

**نکتهٔ اضافه:** ۳ فایل txt ریشه با نام موجیبیک (`00-بخوانید-نصب.txt`، `راهنمای-نصب-بسته.txt`، `README-راهنمای-هاست.txt`) هم هنوز با نام خراب روی برنچ هستند — ارزش یک rename ساده را دارند.

### ۳.۲ جریان CRM (یافته‌های این گزارش — مستند رسمی تحویل ندارد)

1. **بامپ نسخه/Cache-buster انجام نشده است** — `crm/index.html` و `crm/sw.js` روی برنچ هنوز `v33.2.0` هستند (دقیقاً مثل `main`) در حالی که ~۹۵۰ خط JS جدید به ~۲۴ فایل اضافه شده. بدون بامپ، مرورگرهای کاربران نسخهٔ کهنهٔ SW/JS را سرو می‌کنند. باید قبل از پروداکشن به `v33.3.0` (یا بالاتر) بامپ شود.
2. **پوشش UAT برای ویژگی‌های جدید ناقص است** — برای sales returns، catalog identity/review/link/merge و purchase disposition هیچ tester اختصاصی جدیدی نوشته نشده (فقط ۳ tester قدیمی به‌روزرسانی شده‌اند). جریان کاری این پروژه «هر ویژگی + tester» است.
3. **UI مشاهده/بازگشت تاریخچهٔ ادغام کاتالوگ** — `ptf_crm_catalog_merges` ذخیره و همگام می‌شود ولی فقط نوشته می‌شود؛ نما/بازگشت (undo) برایش وجود ندارد.
4. **باز شدن PR** — برنچ هنوز PR ندارد؛ PR #10 فقط جریان سئو را پوشش می‌دهد. بعد از پایان کارهای مکانیکی، پیشنهاد می‌شود جریان CRM یک PR مستقل بگیرد (تا روند بازبینی کاربر مثل قبل باشد).
5. **🆕 مهاجرت به MySQL (`DB-MIG-001`)** — به درخواست کارفرما ثبت شد: قبلاً هیچ‌جا مستند نبود (۰ مورد `mysql` در کل مخزن). مشخصات کامل و ۶ سؤال باز دامنه در `crm/MIGRATION-MYSQL-SPEC-v1.md`. اولویت پیشنهادی P1 (بعد از ثبات‌سازی)، در انتظار تعیین دامنه (فقط سرور / کامل / دو مرحله‌ای).

### ۳.۳ سؤالات باز برای فاز ۳ (صفحات محصول/برند) — طبق `BRAND-PRODUCT-CATALOG-2026-07-29.md` §۷

1. آیا شرکت واقعاً نمایندگی/امکان تأمین مستقیم برندهای Fisher، Masoneilan، ABB، Siemens Energy (VCB) را دارد؟ (روی لحن محتوا اثر می‌گذارد: «تأمین مستقیم» در برابر «واردات به سفارش»)
2. «کنترل ولو Fisher» و «کنترل ولو Masoneilan» — یک صفحهٔ مقایسه‌ای مشترک یا دو صفحهٔ مجزا؟
3. فاز ۳ موازی با کارهای ۱–۳ بالا پیش برود یا بعد از آن‌ها؟

---

## ۴) توصیهٔ ادامهٔ کار (نشست جاری)

- **انتقال وضعیت:** چون نشست جاری به برنچ خودش (`arena/019fbe19-pishtaj-ir`) متصل است، HEAD برنچ `019fb322` (`a9af260`) به‌عنوان نقطهٔ شروع به آن منتقل می‌شود تا ادامهٔ کار بدون از دست رفتن چیزی انجام شود.
- **ترتیب پیشنهادی:** (۱) حذف ۴۹ موجیبیک → (۲) rename ۳۰ فایل → (۳) رفع ۲۰۹ نقض متادیتا → (۴) بامپ نسخهٔ CRM + تست‌های UAT جدید → (۵) فاز ۳ صفحات محصول پس از پاسخ کاربر.
- **استانداردهای ثابت:** هر تغییر مسیر → ریدایرکت ۳۰۱؛ بعد از هر مرحله `build_sitemap.py` + `audit.py` (۰ خطا)؛ کامیت و push مرحله‌به‌مرحله؛ تصاویر گواهی‌نامه هرگز تغییر محتوا (تاریخ/امضا/مهر/QR) نمی‌خورند.
