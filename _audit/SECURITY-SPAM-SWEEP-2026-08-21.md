# گزارش بررسی امنیتی و اسکن اسپم — ۲۰۲۶-۰۸-۲۱

**تاریخ:** ۱۴۰۵/۰۵/۳۰ — 2026-08-21 | **دامنه:** pishtaj.ir (سورس کامل مخزن)
**محرک:** مشاهدهٔ ۳ URL کازینو/شرط‌بندی در فهرست «Currently not indexed» گوگل سرچ کنسول.

---

## ۰) حکم یک‌خطی

**کد فعلی مخزن تمیز است — هیچ محتوای اسپم، بک‌دور یا لینک تزریقی در سورس وجود ندارد.**
URLهای کازینو مربوط به یک آلودگی قدیمی (خزیده‌شده در ۲۰۲۵-۱۲-۰۵ و ۲۰۲۶-۰۱-۰۱) پیش از
بازسازی استاتیک سایت هستند و اکنون **404** برمی‌گردانند. اقدام لازم، عملیاتی است
(Removals گوگل + بررسی امنیت هاست)، نه تغییر کد.

---

## ۱) شواهد آلودگی قدیمی

از فهرست GSC کاربر — سه URL اسپم با تاریخ خزش قدیمی:

| URL | Last crawled | وضعیت فعلی |
|---|---|---|
| `https://pishtaj.ir/bingo-bonga-casino-no-deposit-bonus-100-free-spins/` | 2026-01-01 | 404 (در سورس نیست) |
| `https://pishtaj.ir/المراهنات-في-مباريات-كرة-القدم/` | 2025-12-05 | 404 |
| `https://pishtaj.ir/paysafecard-casino-no-deposit-bonus-100-free-spins/` | 2025-12-05 | 404 |

این URLها الگوی کلاسیک «اسپم تزریقی به وردپرس/هاست هک‌شده» هستند (کلمات کلیدی کازینو +
اسلات + عربی شرط‌بندی فوتبال). سایت فعلی استاتیک است و این مسیرها وجود ندارند.

---

## ۲) نتایج اسکن سورس (همه منفی = سالم)

| بررسی | نتیجه |
|---|---|
| کلیدواژه‌های کازینو/قمار (casino/gambling/betting/poker/slot/کازینو/شرط‌بندی/مراهن) در همهٔ فایل‌ها | ✅ فقط مثبت کاذب (واژهٔ انگلیسی «slot» در idهای HTML و «cabinet, slot, rack») |
| فایل‌های وردپرس / wp-* | ✅ هیچ |
| بک‌دور PHP (`eval`، `base64_decode`، `shell_exec`، `system`، `passthru`، `create_function`، `gzinflate` و…) | ✅ همهٔ موارد مشروع (امضای توکن JWT/کپچا/OTP و `curl_exec` برای نرخ ارز) |
| JS مبهم (`atob`، `fromCharCode`، `unescape`، هگز‌اسکیپ) | ✅ فقط کتابخانهٔ استاندارد `xlsx.min.js` و `inqreader.js` (خوانندهٔ QR) |
| لینک خروجی مشکوک | ✅ فقط دامنه‌های برند (Emerson، Yokogawa و…)، `wa.me`، و دو دایرکتوری ایرانی معتبر (سامانه صبا `infosaba.com`، `hacinhaseb.com`) |
| ریدایرکت خارجی در `.htaccess` | ✅ هیچ (فقط `pishtaj.ir` و اجبار HTTPS داخلی) |
| URL غیر pishtaj در سایت‌مپ‌ها | ✅ هیچ (فقط `www.sitemaps.org` در namespace) |
| فایل خارج از گیت (untracked/planted) | ✅ صفر |
| سم‌لینک / فایل مخفی غیرعادی | ✅ هیچ |
| پوشه با نام هش/تک‌حرفی مشکوک | ✅ هیچ |

---

## ۳) اقدامات لازم (مجری: کارفرما)

### الف) پاک‌سازی از ایندکس گوگل — همین هفته
در GSC → **Removals** → «درخواست جدید» → گزینهٔ «حذف موقت URL» یا «حذف قدیمی»:
1. `https://pishtaj.ir/bingo-bonga-casino-no-deposit-bonus-100-free-spins/`
2. `https://pishtaj.ir/المراهنات-في-مباريات-كرة-القدم/`
3. `https://pishtaj.ir/paysafecard-casino-no-deposit-bonus-100-free-spins/`

> برای پیدا کردن بقیه: در GSC (یا جستجوی گوگل با `site:pishtaj.ir casino`) لیست کامل را
> بگیرید و برای همه Removals بزنید. این یکی از معدود کاربردهای مشروع Removals است
> (محتوای هک‌شده)، چون این URLها الان 404 می‌دهند ولی در «Currently not indexed» مانده‌اند.

### ب) امنیت هاست — همین هفته
1. همهٔ رمزهای هاست/cPanel/FTP/SSH را عوض کنید.
2. مطمئن شوید فایل‌های `api/secrets.php` و `api/ptf-db-config.php` مجوز درست دارند و
   کلیدهای ابری/دیتابیس لو نرفته‌اند (چون سایت قبلاً هک شده بوده، باید کلیدها را ریست کنید).
3. اگر هنوز نسخهٔ وردپرسی در مسیر دیگری از هاست فعال است، حذف یا قرنطینه کنید.
4. `api/logs/` و `api/uploads/` را از نظر فایل مشکوک بررسی کنید.

### ج) پایش
- ماهانه GSC → Indexing → Pages → فیلتر «Search appearance / URL» برای کلمات
  `casino|bet|gambling|قمار|شرط` بررسی شود.

---

## ۴) جمع‌بندی

| سؤال | پاسخ |
|---|---|
| سایت الان اسپم دارد؟ | خیر — کد تمیز است |
| چرا گوگل آن URLها را می‌شناسد؟ | آلودگی قدیمی (دسامبر ۲۰۲۵/ژانویه ۲۰۲۶)، قبل از بازسازی استاتیک |
| به رتبه ضرر می‌زند؟ | فعلاً نه (not indexed) — اما باید پاک شوند تا سیگنال منفی باقی نماند |
| کار کد لازم دارد؟ | خیر — فقط Removals + بررسی امنیت هاست |
