# بک‌لاگ R15 — باگ‌های بایگانی پرونده فروش و زونکن دیجیتال

**وضعیت:** ✅ اجرا شد در v21.1 — BUG-035/036/037 بسته شد
**ریلیز:** `RELEASE-NOTES-v21.1.md` / tester127-v211  
**منبع:** گزارش کارفرما پس از تست انسانی  
**خانواده:** پرونده فروش / بایگانی / زونکن دیجیتال / فایل‌ها  
**باگ بعدی:** BUG-035

---

## BUG-035 — ✅ v21.1 — پرونده فرصت فروش بعد از بایگانیِ باخت همچنان در فرصت‌های فروش نمایش داده می‌شود

**شدت:** بحرانی داده / چرخه فرایند  
**ماژول‌های محتمل:** `salesfiles.js`, `oppo.js`, `projects.js`, `archive.js`  
**خانواده:** بایگانی پرونده فروش / فرصت‌های فروش

### شرح
یک پرونده فرصت فروش به دلیل باخت بایگانی شد. پرونده در بایگانی ساخته شد، اما همان پرونده دوباره در «فرصت‌های فروش» نمایش داده می‌شد. در نتیجه کاربر می‌تواند همان پرونده را بی‌نهایت بار بایگانی کند و رکوردهای تکراری در بایگانی ایجاد شود.

### ریشه‌یابی اولیه پیشنهادی
احتمالاً مسیر بایگانی باخت، رکورد فرصت/پرونده را از منبع نمایش فرصت‌ها حذف یا به وضعیت قطعی `archived/lost` منتقل نمی‌کند، یا فیلتر `oppo.js`/`salesfiles.js` برای تشخیص رکوردهای بایگانی‌شده کامل نیست. همچنین ممکن است تطبیق با هر دو شناسه `inqNo/cd` ناقص باشد و فرصت از مسیر دیگری دوباره ساخته/نمایش داده شود.

### معیارهای پذیرش
1. پس از بایگانی باخت، پرونده/فرصت فروش دیگر در فرصت‌های فروش نمایش داده نشود.
2. بایگانی یک پرونده باخت idempotent باشد؛ اجرای دوباره روی همان پرونده نباید رکورد بایگانی تکراری بسازد.
3. تشخیص تکراری بودن بایگانی با هر دو شناسه `inqNo` و `cd` انجام شود.
4. اگر پرونده قبلاً بایگانی شده باشد، UI پیام بدهد: «این پرونده قبلاً بایگانی شده است» و مسیر متوقف شود.
5. تستر رفتاری: یک پرونده باخت → بایگانی → عدم نمایش در فرصت‌ها → تلاش دوباره → بدون رکورد تکراری.

---

## BUG-036 — ✅ v21.1 — چرخه معیوب دانلود/فشرده‌سازی در بایگانی: «اول دانلود کنید» سپس «فایل ابری موجود نیست»

**شدت:** بالا  
**ماژول‌های محتمل:** `projects.js`, `storage.js`, `archive.js`  
**خانواده:** بایگانی / فشرده‌سازی / فایل‌های ابری

### شرح
در بایگانی، هنگام کلیک روی «فشرده‌سازی و بایگانی» سیستم می‌گوید حداقل یک‌بار باید دانلود شود. وقتی کاربر روی دانلود کلیک می‌کند، پیام «فایل ابری برای این پرونده وجود ندارد» نمایش داده می‌شود. بنابراین کاربر در یک چرخه بن‌بست قرار می‌گیرد: برای فشرده‌سازی باید دانلود کند، اما دانلود ممکن نیست چون فایل ابری وجود ندارد.

### ریشه‌یابی اولیه پیشنهادی
قانون «دانلود اجباری قبل از فشرده‌سازی» برای پرونده‌هایی که هیچ فایل ابری واقعی ندارند یا فقط اسناد سیستمی/متادیتا دارند، نباید همان رفتار را داشته باشد. تابع `prjDownloadAll` احتمالاً فقط فایل‌های دارای `key` را می‌شناسد، اما `prjArchiveWizard` بدون توجه به نبود فایل ابری، همچنان شرط `dlAt` را الزام می‌کند.

### معیارهای پذیرش
1. اگر پرونده هیچ فایل ابری واقعی ندارد، دکمه دانلود نباید شرط لازم برای فشرده‌سازی باشد.
2. در این حالت، فشرده‌سازی باید بتواند یک archive متادیتایی/سیستمی بسازد یا پیام شفاف بدهد که «فایلی برای فشرده‌سازی وجود ندارد؛ پرونده فقط متادیتا/اسناد سیستمی دارد».
3. شرط «دانلود قبل از فشرده‌سازی» فقط برای پرونده‌ای اعمال شود که فایل ابری واقعی قابل دانلود دارد.
4. `prjDownloadAll` باید تفاوت بین «هیچ فایل ابری وجود ندارد» و «خطا در دانلود» را شفاف کند.
5. تستر: پرونده با فایل ابری، پرونده بدون فایل ابری، پرونده با اسناد سیستمی فقط.

---

## BUG-037 — ✅ v21.1 — مشاهده سند در زونکن دیجیتال فایل را باز نمی‌کند و پنجره زونکن را می‌بندد

**شدت:** بالا / UX و اسناد  
**ماژول‌های محتمل:** `projects.js`, `storage.js`, `modalx.js`  
**خانواده:** زونکن دیجیتال / پیش‌نمایش فایل / مودال‌ها

### شرح
در زونکن دیجیتال، با کلیک روی «مشاهده» یک سند ضمیمه، فایل باز نشد و علاوه بر آن پنجره زونکن دیجیتال هم بسته شد.

### ریشه‌یابی اولیه پیشنهادی
احتمالاً کلیک روی لینک/دکمه مشاهده در داخل زونکن، event propagation را متوقف نمی‌کند و کلیک به overlay مودال می‌رسد؛ overlay نیز به‌دلیل شرط `if(event.target===this)` یا ساختار DOM، پنجره را می‌بندد. احتمال دیگر این است که `openStoredFile` خطا می‌دهد یا فایل key ندارد و چون fallback مناسبی ندارد، کاربر فقط بسته‌شدن مودال را می‌بیند.

### معیارهای پذیرش
1. کلیک روی مشاهده/دانلود سند در زونکن، پنجره زونکن را نبندد.
2. event propagation در دکمه‌های مشاهده سند متوقف شود.
3. اگر فایل key ندارد یا فایل ابری موجود نیست، پیام شفاف داخل همان زونکن/مودال نمایش داده شود.
4. اگر فایل قابل پیش‌نمایش است، از مسیر امن `openStoredFile` یا docviewer داخلی باز شود.
5. تستر رفتاری: سند با key معتبر، سند بدون key، سند purged/archived، کلیک روی مشاهده بدون بسته‌شدن زونکن.

---

## پیشنهاد اسپرینت بعدی

### اسپرینت «سخت‌سازی بایگانی و زونکن»
موارد هم‌خانواده و پیشنهادی برای اجرا:

1. **BUG-035** — جلوگیری از بایگانی تکراری فرصت فروش باخته
2. **BUG-036** — رفع چرخه دانلود/فشرده‌سازی پرونده بدون فایل ابری
3. **BUG-037** — اصلاح مشاهده سند در زونکن و جلوگیری از بسته‌شدن مودال

این سه مورد به یک خانواده داده/اسناد تعلق دارند و بهتر است در یک اسپرینت متوسط اجرا شوند.
