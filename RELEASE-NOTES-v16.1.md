# 💱 ریلیز v16.1 — US-391 «نرخ لحظه‌ای ارز روی داشبورد» (رایگان)

**تاریخ:** ۱۴۰۵/۰۴/۱۸ | **نوع:** قابلیت درخواستی کارفرما — ارزیابی و طرح تیم متخصص

---

## 🧠 ارزیابی تیم متخصص

**سوال:** آیا می‌شود بدون هزینه اضافی، نرخ دلار/یورو (آزاد + سنا) را آنلاین خواند و در صفحه اول نمایش داد؟

**پاسخ: بله — اجرا شد.** جمع‌بندی بررسی منابع:

| منبع | هزینه | جمع‌بندی تیم |
|---|---|---|
| وب‌سرویس عمومی TGJU (شبکه اطلاع‌رسانی طلا و ارز — شامل نرخ‌های آزاد و سنا) | **رایگان، بدون کلید API** | ✅ انتخاب شد — سال‌هاست پایدار است و همه ۶ نرخ لازم را یک‌جا می‌دهد |
| سرویس‌های API ایرانی (persianapi، apimaster و...) | 💰 اشتراک ماهانه | ❌ رد — خلاف شرط «بدون هزینه» |
| سایت سنا (بانک مرکزی) مستقیم | رایگان ولی بدون وب‌سرویس رسمی پایدار | ❌ رد — اسکرپ شکننده است |

### تصمیمات معماری (تجربه تیم)
1. **پروکسی سمت سرور** (`api/fx-rates.php`) — مرورگر کاربران مستقیم به منبع خارجی وصل نمی‌شود: بدون مشکل CORS، بدون وابستگی به اینترنت/فیلترینگ سمت کاربر، و منبع فقط از هاست شما یک تماس می‌گیرد
2. **کش سروری ۱۰ دقیقه‌ای** — هر چند کاربر و هر چند رفرش، حداکثر ۶ تماس در ساعت به منبع → پایدار و بی‌آزار
3. **زنجیره ۵ آینه + stale-if-error** — اگر منبع قطع شد، آخرین نرخ معتبر با برچسب «⏳ قدیمی (n دقیقه پیش)» نمایش داده می‌شود؛ ویجت **هرگز** داشبورد را نمی‌شکند
4. **اصل حسابداری:** نرخ آنلاین «اطلاع‌رسانی» است — مبنای اسناد و تسعیرها همچنان نرخی است که کاربر در لحظه تراکنش تایید می‌کند (سلب مسئولیت روی خود ویجت درج شده)
5. فایل PHP جدید **خودکفا**ست (درس BUG-015 اعمال شد — هیچ تابعی از crm.php صدا نمی‌زند)

## ✅ چه چیزی می‌بینید

**ویجت «💱 نرخ لحظه‌ای ارز» بالای داشبورد** (زیر کاشی‌ها و قیف فروش):

| | آزاد | سنا (خرید) | سنا (فروش) |
|---|---|---|---|
| دلار 🇺🇸 | ✅ | ✅ | ✅ |
| یورو 🇪🇺 | ✅ | ✅ | ✅ |

- همه به **تومان** با ارقام فارسی + زمان آخرین بروزرسانی
- بروزرسانی خودکار هر ۱۰ دقیقه تا وقتی داشبورد باز است
- 🎁 **اتصال به نظام ارزی v16.0:** هنگام ثبت دریافت/تسعیر سند ارزی، نرخ‌های لحظه‌ای همان ارز به‌عنوان راهنما نمایش داده می‌شوند (تصمیم نهایی همیشه با شماست)

## 🔬 کیفیت

| مورد | نتیجه |
|-------|--------|
| tester79 جدید | ✅ ۲۴ چک — پروکسی/کش/fallback/۶ نرخ/تحمل خطا/رندر |
| رگرسیون کامل | ✅ **۶۶ تستر / ۱۸۱۱ چک — صفر خطا** + audit PASS |
| نسخه/کش | VER=v16.1، کش `ptf-crm-v16.1` |

## 📦 بسته تحویلی
`pishtaj-release-v16.1.zip` — تجمیعی **v14.5 تا v16.1**.

⚠️ این بسته **دو فایل جدید/تغییریافته در پوشه api** دارد (`fx-rates.php` جدید + `.htaccess` به‌روز) — طبق روال: اکسترکت روی موجود، حذف پوشه api ممنوع. بعد از آپلود و رفرش (v16.1)، ویجت نرخ باید ظرف چند ثانیه روی داشبورد پر شود.

💡 نکته: اولین نمایش نیازمند دسترسی هاست شما به اینترنت (tgju.org) است — اگر «منبع در دسترس نیست» ماند، احتمالا فایروال هاست تماس خروجی را بسته که با یک تیکت به هاستینگ حل می‌شود.
