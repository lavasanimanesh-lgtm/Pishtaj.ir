# RELEASE-NOTES — v33.22.2 (۱۴۰۵/۰۸/۱۱ — 2026-08-02)

## 🧹 ساده‌سازی بخش تنظیمات بک‌آپ/بازگردانی و حافظه — به درخواست مستقیم کارفرما

دستور کارفرما: «وضعیت قسمت تنظیمات بکاپ و بازگردانی را بررسی کن؛ اگر دکمه‌ای نیاز نیست یا
توضیحات اضافه‌ای وجود دارد حذف شود که باعث سردرگمی کاربران نشود.»

### باکس «🗄 بک‌آپ و بازگردانی» (۸ دکمه ← ۶ دکمه)

| وضعیت | دکمه | علت |
|---|---|---|
| ❌ حذف شد | 🔌 بررسی اتصال سرور | عیب‌یاب فنی بود؛ تابعش (`ptfBackupServerCheck/Status`) دست‌نخورده و محل نمایش (`ptfBackupConn`) باقی است |
| ❌ حذف شد | ☁️ پاک‌سازی زباله‌های ابری | عنوان فنی/مبهم برای کاربر؛ تابع (`ptfPurgeCloudOrphans` در storage.js) محفوظ است |
| ✅ باقی | 🗄 بک‌آپ فوری · ⬇️ دانلود فایل · 📥 دانلود ماهانه سرور · ⏪ بازگردانی · 📂 بک‌آپ‌های سرور · ⚠️ بررسی کدهای تکراری | ضروری |

متن توضیحات نیز کوتاه شد (حذف جزئیات فنی چرخش نسخه‌ها/US-codes برای کاربر نهایی).

### باکس «حافظه محلی CRM» (۶ دکمه ← ۱ دکمه)

- ❌ حذف شد: **بک‌آپ سروری** (دقیقاً تکرار «🗄 بک‌آپ فوری» بود!) · مهاجرت cache/draft به IndexedDB · نمایش کلیدهای بزرگ · آرشیوهای IndexedDB · درخواست Persistent Storage
- ✅ باقی: **«پاک‌سازی امن فوری»** — که خودش ابتدا مهاجرت به IndexedDB را هم انجام می‌دهد (همان کارِ دکمه‌های حذف‌شده + بیشتر)
- توابع همهٔ دکمه‌های حذف‌شده (`ptfStorageMigrateToIdb` و…) محفوظ‌اند — فقط از UI برداشته شدند.
- متن فنی (Browser storage estimate، سقف محافظه‌کارانه، راهنمای چندمرحله‌ای قرمز با اشاره به «برنامهٔ DB-MIG-001») ← متن کوتاه کاربرمحور: «دادهٔ اصلی روی سرور است؛ اینجا فقط کش مرورگر است».
- ارجاع کهنهٔ «مهاجرت به MySQL — برنامهٔ آینده» → وضعیت فعلی (MySQL فعال است؛ راه‌حل دائمی = «حالت سرور-محور» در همین صفحه).

### باکس سبز «🌐 حالت سرور-محور (فاز B)»
- متن طولانی دوبخشی ← کوتاه و عمل‌گرا؛ **هر ۴ دکمه (فعال‌سازی / هم‌گرایی / پاک‌سازی کش / غیرفعال‌سازی) باقی** — این همان پاسخ قطعی به کاربرانی است که حافظه‌شان پر است.

### نسخه‌گذاری
`window.VER = 'v33.22.2'` در index.html (+۸۳ باستر)، sw.js، clear-cache.html.

## تست
- tester303-settings-backup-simplify (جدید): **۱۰/۰** — حذف onclickهای مازاد، بقای دکمه‌های ضروری و توابع موتور، اصلاح متن‌ها، بامپ همگام.
- تسترهای pinشدهٔ متاثر به الگوی مقاوم به‌روزرسانی شدند: tester227/228/294/295/298 — همه سبز.
- رگرسیون کامل: ۲۹۸ تستر → فایل **۲۴۲/۵۶** و چک **۵۴۳۱/۸۴** = baseline دقیق + tester303؛ **صفر شکست جدید**.
- audit: ۰ خطا · zip ۵۵۸ بازسازی (backup.js + نسخه‌ها به‌روز).

## پاسخ به سؤال کارفرما (کاربرانی که امروز به‌خاطر پر بودن localStorage متصل نشدند)
۱) در صفحهٔ ورود روی «🔄 رفع مشکل ورود (بدون حذف داده)» بزنند و دوباره وارد شوند.
۲) در تنظیمات «🗄 بک‌آپ فوری» و سپس «پاک‌سازی امن فوری» را بزنند.
۳) برای ریشه‌ای حل شدن: «✅ فعال‌سازی حالت سرور-محور» → «🔄 هم‌گرایی» → «🗑 پاک‌سازی کش محلی» — پس از آن داده روی سرور (MySQL) است و حافظهٔ مرورگر دیگر پر نمی‌شود.
(با v33.22.1 پروداکشن = mode=mysql، دادهٔ این کاربران روی سرور امن است؛ پاک شدن کش محلی هیچ داده‌ای از دست نمی‌دهد.)
