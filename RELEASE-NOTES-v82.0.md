# 🚀 ریلیز v82.0 — اسپرینت ۸۲: رابط کاربری حرفه‌ای + سنجه ابری + چرخه بایگانی پرونده

**تاریخ:** ۱۳ تیر ۱۴۰۵ | **دامنه:** US-176 + US-177 + US-178 (دستور جدید کارفرما)

---

## 🎨 US-176 — رابط کاربری روشن حرفه‌ای (الگوی پنل کاوه‌نگار)

ماژول جدید `crm/theme.js`:

| مورد | قبل | حالا |
|---|---|---|
| سایدبار | مشکی گرادیانی | **سفید مینیمال** با خط جداکننده ظریف و سایه ملایم |
| پس‌زمینه | خاکستری سرد | **روشن ملایم (#f7f8fa)** — خستگی کمتر چشم |
| فونت | B Nazanin/سیستمی | **وزیرمتن** سراسری (فایل لوکال — بدون CDN، آفلاین هم کار می‌کند) |
| hover | دکمه‌ها بزرگ/جابجا می‌شدند | **هیچ تغییر اندازه‌ای نیست** — فقط تغییر رنگ ملایم (حذف همه transform ها) |
| نوار بالا | فقط عنوان + ساعت | **آیکون‌های دسترسی سریع**: 🌙/☀️ حالت شب، ☁️ فضای ابری، 📁 پرونده‌ها (کنار 🔔 صندوق پیام) |
| آیتم فعال منو | گرادیان نارنجی پررنگ | هایلایت ظریف + خط نارنجی کنار آیتم (مثل پنل کاوه‌نگار) |

- ✅ سازگار کامل با **حالت شب** (US-167) — سایدبار سفید در حالت شب خودکار تیره می‌شود.
- ✅ منوی آکاردئونی، بج‌ها و RBAC دست نخورده‌اند؛ فقط پوسته عوض شده.

## ☁️ US-177 — سنجه فضای اشغال‌شده ابری در منو

- اکشن سروری جدید `usage` در `api/storage.php`: جمع حجم و تعداد **همه** فایل‌های باکت آروان (با صفحه‌بندی تا ۳۰٬۰۰۰ فایل)، فقط same-origin.
- نمایش در **پایین سایدبار**: «☁️ فضای ابری: 1.2 GB (245 فایل)» + دکمه 🔄 به‌روزرسانی.
- کش ۱۰ دقیقه‌ای (هر بار ورود به آروان کوئری نمی‌زند) — بعد از هر حذف/بایگانی خودکار تازه می‌شود.
- آیکون ☁️ نوار بالا → دیالوگ جزئیات (حجم/تعداد/باکت + راهنمای آزادسازی).

## 🗄 US-178 — چرخه کامل فایل‌های پرونده پروژه

ماژول جدید `crm/archive.js` + اکشن سروری `archive_zip`:

### گردش کار
1. **CO برنده → پرونده خودکار** (از قبل بود) → در طول پروژه هر فایلی (عکس کالا، کاتالوگ، نقشه اتوکد، ...) در پوشه‌های ۹گانه آپلود می‌شود. ✅
2. **⬇️ دانلود کل پرونده**: همه فایل‌ها با لینک امن موقت یکی‌یکی دانلود می‌شوند → پرونده مهر «دانلود‌شده در تاریخ X توسط Y» می‌خورد.
3. 🔒 **گارد ایمنی (خواسته صریح شما):** تا قبل از حداقل یک بار دانلود کامل، **هیچ‌کس اجازه حذف هیچ فایلی را ندارد** — نه حذف تکی، نه بایگانی، نه آزادسازی.
4. **🗑 حذف فایل غیرضروری** (تکی): فایل از ابر پاک می‌شود ولی **متادیتا می‌ماند** (نام، تاریخ، ثبت‌کننده، حذف‌کننده) — روند پروژه هرگز گم نمی‌شود؛ در پوشه با برچسب «حذف‌شده از ابر (متادیتا)» دیده می‌شود.
5. **🗄 بایگانی** (فقط پرونده «تحویل کامل/بسته‌شده»): ویزارد دو گامی — تیک فایل‌های غیرضروری برای حذف → **تایید نهایی** → بقیه فایل‌ها در سرور **فشرده (zip)** شده به `archives/` منتقل و اصل‌ها پاک می‌شوند. حذف اصل‌ها **فقط بعد از آپلود موفق zip** انجام می‌شود (بدون ریسک).
6. **☁️🗑 آزادسازی کامل فضا**: بعد از دانلود محلی، حتی zip بایگانی هم قابل حذف است؛ در پرونده ثبت می‌شود «نسخه محلی نزد فلان کاربر» — تایم‌لاین، اقلام، مبالغ و کل روند پروژه در سیستم می‌ماند.
7. **🧹 درخواست‌های بازنده (AC7):** با ثبت وضعیت «بازنده»، سیستم پیشنهاد پاکسازی فایل‌های پیوست RFQ را می‌دهد — فقط مشخصات کلی برای بررسی آماری می‌ماند و فایل‌ها از سرور پاک می‌شوند.

> ⚠️ **وابستگی هاست:** فشرده‌سازی سروری به افزونه `ZipArchive` نیاز دارد (همان که برای US-130 قرار بود چک شود). اگر فعال نباشد، سیستم پیام شفاف می‌دهد و پرونده بدون فشرده‌سازی بایگانی می‌شود. لطفاً از پشتیبانی هاست وضعیت ZipArchive را استعلام بفرمایید.

## ✅ کیفیت

| مورد | نتیجه |
|---|---|
| ممیز ۸بخشی (۴۳۴ صفحه) | ✅ PASS |
| `node --check` همه JSهای جدید/تغییرکرده | ✅ PASS |
| تستر جدید `tester19-sprint82` | ✅ **32/32 PASS** |
| رگرسیون ۱۸ تستر قبلی (شامل tester18 اسپرینت ۸۱) | ✅ همه PASS |
| نسخه کش | `ptf-crm-v82.0` (پاکسازی خودکار کش همکاران) |

## 📦 نصب (File Manager)

**`pishtaj-release-v82.0-delta.zip`** را روی هاست باز کنید. فایل‌ها: `crm/theme.js` و `crm/archive.js` (جدید)، `crm/storage.js`، `crm/projects.js`، `crm/index.html`، `crm/sw.js`، `api/storage.php` + مستندات. **داده‌ای پاک نمی‌شود.**

> ℹ️ اگر v80.2 و v81.0 هنوز نصب نشده‌اند: هر سه دلتا (v80.2 → v81.0 → v82.0) را به ترتیب باز کنید. فایل‌های مشترک در دلتای جدیدتر به‌روزترند.

## 🔔 یادآوری‌های باز
- 🧪 بعد از نصب تست کنید: سنجه ☁️ پایین منو، دانلود کل پرونده، تلاش برای حذف فایل **قبل** از دانلود (باید مسدود شود)، بایگانی یک پرونده تحویل‌شده.
- ❓ استعلام از هاست: فعال بودن `ZipArchive` (برای فشرده‌سازی بایگانی + آپدیت zip آینده).
- 🌐 Search Console: درخواست ایندکس sitemap (۱۱۱ URL) هنوز باز است.
- 📁 US-160: منتظر مدارک شما (پروژه‌ها/لوگوها/eNAMAD).
- ⏭ اسپرینت ۸۳ طبق رودمپ: US-172 (ایمپورت هوشمند اکسل سراسری) + US-173 AC4.
