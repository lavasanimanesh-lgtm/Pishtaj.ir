# فاز ۱۷ (v34.0.20-alpha) — اطلاع‌رسانی پیامکی به مشتری در مراحل کلیدی

**برنچ:** `arena/019fcd1d-pishtaj-ir`
**درخواست کارفرما:** امکان اطلاع‌رسانی پیامکی به مشتری در هر مرحله — مثلاً پیامک «ثبت درخواست» و «صدور پیشنهاد مالی» با ذکر شمارهٔ درخواست مشتری.

---

## اصلاحات

### `crm/sms.js` — helper مرکزی
- `ptfCustomerMobile(c)`: موبایل مشتری را از رکورد مشتری استخراج میکند (اولویت: `people[].mobs[].n`، `phones[]` با `k==='mob'`، سپس `ph`/`mobile`/`phone`).
- `ptfSmsCustomer(customerCd, text, cb)`: موبایل را پیدا و با `smsSendSingle` پیامک میفرستد (از صف سرور میگذرد؛ در صورت قطع، local queue). اگر موبایل نبود، لاگ میکند.

### `crm/offers.js` — صدور پیشنهاد مالی
- در `offerSave`، پس از ذخیرهٔ پیشنهاد، به مشتری (`o.buyerCd`) پیامک «پیشنهاد مالی X برای شما درخواست شمارهٔ Y صادر شد» ارسال میشود (با ذکر شمارهٔ پیشنهاد و شمارهٔ درخواست).

### `crm/bridge.js` — ثبت درخواست
- در `rfqApprove`، هنگام تأیید درخواست سایت، به شمارهٔ موبایلِ فرم سایت پیامک «درخواست شما با شمارهٔ Z ثبت و در حال بررسی است» ارسال میشود.

---

## نکته
این پیامکها از `smsSendSingle` استفاده میکنند که نیازمند **پیکربندی پنل پیامک** است. اگر پنل ارسال کند، پیامک واقعی میرود؛ اگر نه، در صف محلی میماند (queued).

## تست‌ها
- **tester315** جدید (۷/۰): helper + offerSave + rfqApprove.
- رگرسیون کامل سبز (۲۵ تستر، ۰ شکست).
- بامپ نسخه → **v34.0.20-alpha**.
- تغییرات `.github/workflows/*` در PENDING-workflow-changes ثبت شد.

*تولیدشده توسط Arena Agent — اطلاع‌رسانی پیامکی به مشتری.*
