# 🚀 یادداشت‌های انتشار اسپرینت ۱۱۷ (Release Notes - v117.0)

**تاریخ انتشار:** ۱۴۰۵/۴/۱۵ (July 2026)  
**هدف اسپرینت:** ورود اکسل تأمین‌کنندگان با راهنما (US-120 SUP)، اصلاح فیلد شماره تماس در ایمپورت‌ها، آیکون‌نگاری مینیمال و کنترل پین‌شده در مودال‌ها (US-121)  
**تیم توسعه:** کارگروه ممیزی، دبل‌چک و تست واقعی قبل از انتشار PTF  

---

## 📋 تغییرات تأییدشده توسط تیم ممیزی و تست واقعی در این نسخه

### ۱. 🏭 ورود اکسل با راهنما برای تأمین‌کنندگان و اصلاح شماره تماس (US-120 SUP & Universal Contact Fix)
* **فایل‌های تغییر یافته:** `crm/index.html` (بخش تأمین‌کنندگان و توابع ایمپورت)
* **شرح ارتقا:** مطابق بازخورد شما در خصوص ایمپورت اکسل؛ قابلیت ورود فایل اکسل با پنجره راهنما به ماژول **🏭 تأمین‌کنندگان (SUP)** نیز اضافه شد.
* **اصلاح شماره تماس:** مشکل عدم نمایش یا عدم تعریف فیلد شماره تماس در ورود اکسل مشتریان (CUST) و سرنخ‌ها (LEADS) ریشه‌کن شد. اکنون موتورهای ایمپورت اکسل برای تمام موجودیت‌ها، هر ۴ ویژگی استاندارد تماس (`ph`، `mob`، `tel` و `phone`) را به طور همزمان روی شیء ساخته‌شده تنظیم می‌کنند؛ در نتیجه در هر کجای سیستم (جدول، کارت یا فرم ویرایش) شماره تلفن ۱۰۰٪ تعریف‌شده و قابل مشاهده است.

### ۲. 📌 کنترل‌های مینیمال و پین‌شده در پنجره‌های مودال (US-121 Sticky Minimal Controls)
* **فایل‌های تغییر یافته:** `crm/index.html`، `crm/theme.js`
* **شرح ارتقا:** در پاسخ به یوزر استوری جدید شما؛ نوار عنوان و دکمه‌های کنترل پنجره‌های مودال (`.md h3`) به صورت **پین‌شده و استیکی (`position: sticky; top: -24px; z-index: 150`)** تنظیم شدند.
* اکنون زمانی که در داخل هر پنجره مودال طولانی (مثل فرم پیش‌فاکتور یا کارت مشتری) به پایین اسکرول می‌کنید، نوار عنوان و دکمه بستن (`✕`) در بالای کادر ثابت می‌مانند و همیشه در دسترس هستند.
* **آیکون‌نگاری مینیمال:** دکمه‌های بستن به صورت دوایر ۲۸ پیکسلی مینیمال درآمده و آیکون‌های ایموجی حالت روز و شب در هدر صفحه (`theme.js`) با نمادهای خطی و مینیمال (☼ / ☾) جایگزین شدند.

### ۳. 🧹 مدیریت فضای کاری و حافظه چت (Workspace Storage Policy)
* مطابق قرار جدید، اسکرین‌شات‌های اطلاع‌رسانی پس از بررسی پاک شدند و تمام فایل‌های زیپ قبلی از سرور حذف گردیدند تا حجم ورک‌اسپیس در حداقل ممکن حفظ شود.

---

## 🧪 وضعیت کنترل کیفیت و ممیزی (QA Verification)
* ممیزی خودکار ۷گانه (`audit_double_check_117.py`): ۱۰۰٪ موارد تأیید شد.
* تست اسکرول در مودال‌های بلند: نوار عنوان و دکمه بستن در بالای کادر پین ماندند.
