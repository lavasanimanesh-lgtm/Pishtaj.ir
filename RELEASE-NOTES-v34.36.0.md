# RELEASE NOTES — v34.36.0 UX (سایدبار: دکمهٔ خروج کنار نام کاربر + نمایش نقش/سمت زیر نام)

**تاریخ:** ۲۰۲۶-۰۹-۰۴ · **گیت:** سبز · **نکتهٔ بامپ:** جاروی استاندارد نسخه + نقاط رسمی + نشانگر نسخهٔ `cms.js`

## گزارش کارفرما

۱) «دکمهٔ خروج پیدا نیست؛ باید دنبال آن بگردم.»
۲) «در سایدبار معلوم نیست من با چه نقش/سمتی وارد شده‌ام.»

## راه‌حل

### ۱) دکمهٔ خروج در هدر سایدبار (`crm/index.html`)
- **انتقال به کنار نام کاربر:** دکمهٔ خروج اکنون در همان بلوک کاربر (`.sb-u`) در بالای سایدبار، سمت چپ نام قرار دارد — همیشه دیده می‌شود و لازم نیست در پنل‌ها جستجو شود.
- **آیکون مینیمال به‌جای اموجی:** مطابق سیاست آیکون مینیمال (A-minimal-icons)، از SVG خطی `stroke-width=1.7` استفاده شده است، نه اموجی.
- **دسترس‌پذیری:** `title="خروج از حساب"` و `aria-label="خروج از حساب"` روی دکمه، و `aria-hidden="true"` روی SVG.
- **بازخورد بصری:** در `:hover` به قرمز ملایم (`#fef2f2` / `#dc2626`) می‌رود و در `:active` کمی مقیاس می‌گیرد؛ اندازهٔ ثابت ۳۲×۳۲ با گوشهٔ گرد.
- **تک‌نقطه‌ای بودن خروج:** تنها ارجاع UI به `doLogout()` همین دکمه است؛ رفتار ابطال توکن سمت سرور (`auth_logout` با `keepalive`) بدون تغییر باقی مانده است.

### ۲) نمایش نقش/سمت زیر نام کاربر (`crm/index.html`)
- **ساختار دومنظوره:** بلوک `.sb-u-info` یک ستون عمودی است — `#navU` (نام کاربر) بالا و `#navRole` (برچسب نقش/سمت) زیر آن با فونت ۱۰٫۵px و رنگ خنثی.
- **منبع واحد نقش:** برچسب از `roleDef().lb` خوانده می‌شود و در نبودِ آن از `ROLES[curRole()].lb` — تا نقش نمایشی با RBAC واقعی ناهمخوان نشود.
- **تاب‌آوری در بارگذاری دیرهنگام:** اگر دادهٔ نقش پس از رندر اولیه نرسیده باشد، یک تلاش مجدد با تأخیر ۹۰۰ میلی‌ثانیه برچسب را پر می‌کند؛ در غیر این صورت `—` نمایش داده می‌شود (نه رشتهٔ خالی).
- **موبایل:** در حالت جمع‌شده/موبایل، `.sb-u-info` و `.sb-logout` پنهان می‌شوند و فقط آواتار وسط‌چین می‌ماند؛ هر دو خطا نیز با `try/catch` محصورند تا رندر سایدبار هرگز نشکند.

## رفع همراه (بدهی فنی)

- **نشانگر نسخهٔ `crm/cms.js`:** `window.PTF_CMS_JS_VER` از `v34.35.0` به `v34.36.0` هم‌تراز شد. این نشانگر با `window.VER` پوسته مقایسه می‌شود؛ ناهمخوانی آن «سپر نسخهٔ کهنه» را فعال و بنر قرمز «فایل برنامهٔ مدیریت سایت در مرورگر شما قدیمی است» را به کاربران واقعی نشان می‌داد، درحالی‌که `index.html` همان فایل را با کش‌باستر `cms.js?v=34.36.0` بارگذاری می‌کند.
- **pinهای فرسودهٔ نسخه در سوئیت UAT:** در ۵۱ فایل تستر، رجکسِ ادعاها هنوز `34\.35.0` را pin کرده بود درحالی‌که عنوان همان ادعاها `34.36.0` بود (بامپ ناقص). این موجب قرمزشدن گیت CI و **توقف کامل استقرار پروداکشن پیش از گام FTP** می‌شد. همه به شکل صحیح `34\.36\.0` یکسان‌سازی شدند.

## آزمون

بامپ نسخه در این انتشار تستر رفتاری جدیدی اضافه نمی‌کند؛ سوئیت موجود گیت CI پس از یکسان‌سازی pinها سبز است
(۲۰۲ تستر: syntax + نگهبان معماری + php -l + ادعاهای نسخه/کش‌باستر/نقاط رسمی).
