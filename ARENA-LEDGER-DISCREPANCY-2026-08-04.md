# بررسی مغایرت «تراز رسمی/غیررسمی» و «گزارش مالی تجمیعی»

**برنچ:** `arena/019fcd1d-pishtaj-ir` (v34.0.8-alpha)
**وضعیت:** بازبینی فقط‌خواندنی + شبیه‌سازی عددی — تغییری در کد اعمال نشده.
**دو گزارش مقایسه‌شده:**
- **گزارش A — «تراز رسمی/غیررسمی/تجمیعی»** = `crm/ledger-report.js` (`ptfLedgerReportData`)
- **گزارش B — «گزارش مالی تجمیعی»** = `crm/working-capital.js` (`ptfFinanceOfficialData`)

---

## یافتهٔ اصلی (مغایرت اثبات‌شده)

**فاکتورهای صوری/پوششی (`isCover`) در «گزارش مالی تجمیعی» به‌عنوان هزینه/بدهیِ کاملِ مبلغ اسمی شمرده می‌شوند، در حالی که «تراز رسمی/غیررسمی» آن‌ها را در «دفتر واقعی» به‌درستی فقط با «کارمزد − اعتبار ارزش‌افزوده» نشان می‌دهد.**

این دقیقاً همان منطق‌ای است که در فاز ۱ (فاکتور صوری) در موتور سود سال مالی اصلاح شد، ولی **دو گزارش تراز/تجمیعی هنوز با آن هماهنگ نشده‌اند.**

**شبیه‌سازی عددی (دادهٔ یکسان):**
- فروش: ۵۰۰م (رسمی ۴۰۰ + غیررسمی ۱۰۰) — هزینه جاری ۵۰م
- خرید: ۲۰۰م واقعی + **فاکتور صوری ۱۰۰م** (VAT ۱۰م، کارمزد ۱.۵م)

| شاخص | گزارش A (تراز) | گزارش B (تجمیعی) |
|---|---|---|
| خرید کل (شامل cover) | ۳۰۰م | ۳۰۰م |
| بدهی تأمین‌کننده | — | **۳۰۰م (شامل ۱۰۰م پوششی کامل)** |
| «خرید واقعی» (بدون cover) | ۲۰۰م (درست) | در دسترس نیست |
| منفعت پوششی | ۸.۵م (جدا) | محاسبه نمی‌شود |

→ **گزارش B بدهیِ تأمین‌کننده را ۱۰۰م بیشتر از «هزینهٔ واقعی» (۲۰۰م) نشان می‌دهد** چون فاکتور صوری ۱۰۰م را کامل به بدهی اضافه می‌کند. این مغایرت هم با گزارش A و هم با موتور سود سال مالی است.

---

## مغایرت‌های ساختاری دیگر (دلایل واگرایی دو گزارش)

### M-1 — تعریف «هزینهٔ خرید» متفاوت است
- **A:** `purchase.total` = مبلغ **اسمی** فاکتورهای خرید (شامل cover) → `aggregateProfit = فروش − هزینه جاری − خرید اسمی`.
- **B:** `supplierLiability` = **ماندهٔ باقیمانده** فاکتورهای خرید (مبلغ − تخصیص/پرداخت) + legacy. مانده با مبلغ اسمی فقط وقتی برابر است که هیچ پرداختی نشده باشد.
- → دو گزارش اعداد متفاوتی برای «هزینه/بدهی خرید» می‌دهند (یکی ناخالصِ اسمی، یکی خالصِ مانده).

### M-2 — فاکتور پوششی در گزارش B مستثنی نمی‌شود
- گزارش A در بخش «دفتر واقعی» `realPurchaseSafe`/`coverNetBenefit` را دارد (فاکتور پوششی را از خرید واقعی حذف و منفعتش را جدا می‌کند).
- گزارش B **هیچ ارجاعی به `isCover` ندارد** → فاکتور صوری در `src.supplierLiability` به‌طور کامل وارد می‌شود.

### M-3 — بازنویسی بدهی تأمین‌کننده از `slSupplierOpenTotalsIRR`
- گزارش B وقتی `asOf === today`، `src.supplierLiability/Credit` را از `slSupplierOpenTotalsIRR()` بازنویسی می‌کند (`working-capital.js:187-193`).
- در فاز ۳ (B) من legacy را از مبلغ `balance()` حذف کردم → `slSupplierOpenTotalsIRR` اکنون legacy را در مبلغ بدهی نمی‌گنجاند.
- ولی بخشِ `legacy.filter(...)` داخل خود گزارش B **هنوز legacy را به `src.supplierLiability` اضافه می‌کند** → اگر `asOf < today` (که در ابتدای سال یا وقتی امروز > پایان سال است) آن بخش اجرا می‌شود و legacy را می‌گنجاند؛ اگر `asOf === today` از `slSupplierOpenTotalsIRR` بازنویسی می‌شود (بدون legacy). یعنی **خروجی گزارش B به «امروز بودن یا نبودن» بستگی دارد** — همان‌داده در دو زمان، عدد متفاوت.
  - (این ناسازگاریِ داخلیِ گزارش B با اصلاح فاز ۳ شدت یافته؛ چون فاز ۳ legacy را از یک مسیر حذف کرد ولی نه از مسیر داخلی دیگر.)

### M-4 — هیچ «منفعت ارزش‌افزودهٔ پوششی» در گزارش B نیست
- گزارش B نه‌تنها کارمزد را جدا نمی‌کند، بلکه منفعتِ VAT را هم نشان نمی‌دهد؛ پس «بدهی واقعی» و «سرمایه در گردش» برای شرکت‌هایی که فاکتور صوری دارند، تحریف می‌شود.

---

## جمع‌بندی

مغایرت اصلی (و مورد گزارش کاربر) ناشی از **ناهماهنگیِ دو گزارش با منطق «فاکتور صوری/پوششی»** و **تفاوت تعریف «هزینهٔ خرید» (اسمی vs مانده)** است. به‌طور خاص:
1. گزارش B فاکتور صوری را کامل به بدهی می‌افزاید (باید فقط کارمزد).
2. گزارش A و B در «تعریف هزینهٔ خرید» (اسمی vs خالصِ مانده) با هم فرق دارند.
3. گزارش B در حالت «امروز» و «غیر امروز» خروجی متفاوت می‌دهد (بازنویسی از `slSupplierOpenTotalsIRR` vs مسیر داخلی legacy).

**اصلاح پیشنهادی (پس از تأیید):**
- **گزارش B:** فاکتورهای `isCover` را از `supplierLiability` حذف و فقط کارمزد را به‌عنوان بدهی واقعی لحاظ؛ منفعت پوششی را به‌صورت جدا (شبیه گزارش A) اضافه کن.
- **هماهنگ‌سازی مبنای خرید:** تصمیم بگیر هزینهٔ خرید در هر دو گزارش «ماندهٔ واقعیِ فاکتور» باشد (و legacy فقط در صورت عدم‌لینک) تا A و B و موتور سود سال مالی روی یک تعریف توافق کنند.
- **حذف وابستگی به «امروز»:** مسیر داخلی legacy و `slSupplierOpenTotalsIRR` را هم‌ارز کن تا خروجی مستقل از تاریخِ اجرا باشد.

*تولیدشده توسط Arena Agent — بازبینی فقط‌خواندنی؛ تغییری در کد اعمال نشده است.*
