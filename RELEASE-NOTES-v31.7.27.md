# Release Notes — v31.7.27

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.27
**نوع:** 🟢 اصلاح درخواستی کارفرما — کشویی درخواست در فرم پیشنهاد: یک گزینه per درخواست + شماره کارفرما در چاپ (US-INQ-ONE-OPTION)

---

## 🎯 دستور کارفرما

درخواست ثبت می‌شود و شماره درخواست کارفرما هم وارد می‌شود؛ اما کشویی «شماره درخواست کارفرما» در پنجره ثبت پیشنهاد **دو گزینه برای همان یک درخواست** نشان می‌داد (یکی با کد RFQ سیستم، یکی با شماره کارفرما). خواسته: برای هر درخواست فقط **یک گزینه = شماره RFQ ما** نمایش داده شود، ولی با انتخاب آن، در قالب چاپی نهایی پیشنهاد (Ref Inquiry) **شماره درخواست کارفرما** بنشیند نه شماره RFQ ما.

## 🔍 ریشه

`ptfKnownInqList` (US-175) هر درخواست را با **هر دو شناسه** به کشویی اضافه می‌کرد (`r.cd` و `r.inqNo`) — طراحی قدیمی برای انعطاف انتخاب، که عملاً باعث سردرگمی دوگانه می‌شد؛ و قالب چاپ همان مقدار انتخابی را چاپ می‌کرد (اگر کاربر cd سیستمی را انتخاب می‌کرد، شماره داخلی ما روی سند مشتری می‌رفت).

## ✅ اصلاحات

| # | فایل | تغییر |
|---|------|--------|
| 1 | `crm/dedup.js` | کشویی: هر درخواست فقط **یک گزینه با کد RFQ سیستم**؛ لیبل برای شفافیت شماره کارفرما را هم نشان می‌دهد: `RFQ-1244 ⇐ شماره کارفرما: REQ-…— فولاد مشیز`. اقلام ایمپورتیِ همان RFQ دیگر گزینه دوم نمی‌سازند؛ ایمپورت‌های مستقل و رکوردهای legacy حفظ |
| 2 | `crm/dedup.js` | helper جدید `ptfInqClientNo(v)`: هر شناسه‌ای (سیستمی/کارفرما) → شماره درخواست کارفرما؛ اگر ثبت نشده باشد fallback امن به همان ورودی |
| 3 | `crm/offers-pro.js` + `crm/offers.js` | **قالب‌های چاپ (فعال + هر دو fallback):** `Ref (Inquiry)` و `Request No` حالا از `ptfInqClientNo` پر می‌شوند — روی سند مشتری همیشه شماره خودِ کارفرما می‌نشیند |
| 4 | ۴ نقطه نسخه | v31.7.27 |

**نکته معماری (عمدی):** مقدار ذخیره‌شده در رکورد پیشنهاد (`o.inqNo`) همچنان کد RFQ سیستمی انتخابی است — تمام زنجیره داخلی (workflow، پرونده فروش، aliases US-386، گزارش‌ها) با شناسه سیستمی سالم می‌ماند؛ **تبدیل فقط در لایه چاپ** انجام می‌شود.

## 📊 نتایج گیت

- تستر جدید `tester202`: **۱۷/۱۷ PASS** — رفتاری کامل: یک گزینه per درخواست، لیبل شفاف، بقای legacy/ایمپورت، تبدیل چاپی درست، fallback امن، و رگرسیون زنجیره داخلی
- رگرسیون کامل: **180/180 فایل PASS — 4096/4096 چک PASS** | `audit.py` (۱۰ بخش): PASS

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| پیشنهادهای قدیمی (با هر شناقه‌ای ثبت شده‌اند) | چاپ‌شان از این پس شماره کارفرما را نشان می‌دهد اگر درخواست متناظر داشته باشند؛ وگرنه همان مقدار قبلی (fallback) | مثبت |
| زنجیره TO→CO/پرونده فروش/workflow | دست‌نخورده — شناسه داخلی همان است | صفر |
| درخواست‌های بدون شماره کارفرما | یک گزینه ساده؛ چاپ همان کد سیستمی (چیزی برای نشستن نیست) | صفر |

## 📋 راستی‌آزمایی توسط کارفرما (۳ دقیقه — همان سناریو)

1. درخواستی با شماره کارفرما ثبت کنید (مثلاً کد سیستمی RFQ-1244 و شماره کارفرما REQ-9931).
2. فرم پیشنهاد مالی → کشویی شماره درخواست → **فقط یک گزینه** برای این درخواست ببینید: `RFQ-1244 ⇐ شماره کارفرما: REQ-9931 — نام مشتری`.
3. آن را انتخاب و پیشنهاد را پیش‌نمایش/چاپ کنید → در `Ref (Inquiry)` و `Request No` سند، **REQ-9931** (شماره کارفرما) نشسته باشد نه RFQ-1244.

---
*توسعه‌دهنده: PTF Development Team — 2026-07-20*
