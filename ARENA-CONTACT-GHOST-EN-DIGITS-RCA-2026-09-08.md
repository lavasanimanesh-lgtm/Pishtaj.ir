# RCA — CONTACT-GHOST: «اطلاعات تماس مشتری پاک می‌شود» (2026-09-08)

**نسخهٔ اصلاح:** v34.38.7 · **تستر قفل:** `_tools/uat/tester614-v34.38.7-contact-ghost-cohorts.js`

---

## ۱. گزارش دقیق کارفرما

> «در قسمت مشتریان اطلاعات تماس پاک میشه. فکر میکنم به این خاطره که شماره تلفن ها رو با اعداد انگلیسی مینویسم، تا فقط هرکی که مشتریان شماره تلفن فارسی هستش ثبت شده سالمه. الان مدیرعامل روی سیستمش یک مشتری ثبت کرده و شماره تلفن هاشو سالم می‌بینم ولی روی سیستم خودم چه با مدیرعامل چه با رییس هیات مدیره تماس‌ها را نمی‌بینم. خود مشتری (ردیف) دیده می‌شود. بررسی کن ببین مشکل دقیقاً چیست.»

### امضای علائم
| مشاهده | معنا |
|---|---|
| ردیف مشتری برای همه دیده می‌شود | سینک رکورد کار می‌کند؛ مشکل در حذف مشتری نیست |
| ثبت‌کننده (ماشین A) شماره‌ها را می‌بیند | ماشین A از کش محلی خودش می‌خواند |
| ماشین B (هر نقشی، حتی CEO/رییس) نمی‌بیند | سرور نسخهٔ بدون تماس دارد، یا B رديفِ متفاوتی را می‌بیند |
| رکوردهای «فارسی‌رقم» سالم مانده‌اند | همبستگی با دامنهٔ ارقام — نه الزاماً علّت ارقام |
| تعمیم کارفرما: «شماره‌های انگلیسی پاک می‌شوند» | گمان برای آزمون — نه نتیجه |

## ۲. هیپوتزهای آزمون‌شده (رد شده)

| # | نظریه | نتیجهٔ آزمون | منبع |
|---|---|---|---|
| H1 | ارقام لاتین در ورودی، تماس را پاک می‌کند | **رد قطعی** — هوک `phonefmt.ptfNormalizeEntityPhones` قبل از upsert لاتین/۰ +98 را به فارسی‌رقم می‌رساند؛ شبیه‌سازی vm با کد واقعی نشان داد `people[0].tels[0].n='0912...'` → `'۰۹۱۲...'` و در کل زنجیره (روتر فرمان → پورت دقیق `sd_entity_sanitize_row/list` PHP + merge سرور → ماشین دوم → رندر) کامل ماند | tester614 ۱.۰–۱.۹ |
| H2 | گارد/مجوز نقش سمت سرور تماس را حذف می‌کند | رد — `entity_upsert` نقش‌محور فیلد تزریق نمی‌کند/ریخت ندارد | کد سرور پورت‌شده در tester614 |
| H3 | ماسک UI برای نقش‌ها / company‑secret redaction | رد — رندر جدول مشتری فقط از `(x.people||[])` و `ph` می‌خواند | offers.js renderCustomers2 |
| H4 | فیلتر «مشتری‌های من» رکورد اصلی را مخفی می‌کند | رد — ردیفِ خالیِ مشاهده‌شده همان cd است | repro |
| H5 | merge سینک (`sync.js`) اشخاص را گم می‌کند | رد — merge سرور «کلید حاضر بازنویسی می‌شود، ناخوانده از prev حفظ می‌شود»؛ کشِ محلیِ ماشین A تماس را نگه می‌دارد چون هرگز payload خالی نفرستاده | tester614 ۲.۳–۲.۴ |
| H6 | cbCollect/cbRender یا digits ها ورودی لاتین را drop می‌کنند | رد — پورت sanitize در هارنس با دادهٔ واقعی سبز بود | tester614 ۱.۴ |

## ۳. بردارهای واقعی یافته‌شده

### بردار ۱ (اصلی): برخورد کدِ 409 در جریان‌های خودکار — «مشتری روح»
**زنجیرهٔ قبل از اصلاح:**
1. ماشین B یک مشتری را با کد `CUST-X` دارد → heal سیستمی (`ptfHealMissingCustomersFromRfqs) چون rfq به کد ناشناس ارجاع می‌دهد، یک **stub سفید** با همان `cd` می‌سازد و در فهرست محلی می‌نشاند (`reason:'rfq-cust-heal'`).
2. روتر upsert با `expectCreate:true` می‌فرستد (cd در last-known نیست → “جدید”).
3. سرور: `CUST-X` از قبل موجود — تماس‌دار و معتبر (مثلاً ساختهٔ مدیرعامل روی ماشین A) → `entity_id_exists (409)`.
4. `retryWithFreshCode` (ساخته‌شده برای رفتار کاربرمحورِ tester595: «کد مصرف‌شده → کد نو بده و مجدد ثبت کن») **kode تازه صادر می‌کرد و stub را دوباره upsert می‌کرد**.
5. محصول: **دو ردیف هم‌نام** — رکورد اصلی (تماس‌دار، ماشین A) + ردیف تکراری بدون تماس (سایهٔ ماشین B). ماشین B پس از پول، سایهٔ خودِ stub/تکراری را می‌بیند: «مشتری هست، تماس نیست» — دقیقاً امضای گزارش.

**اصلاح v34.38.7 (`crm/sales-domain-v2.js`):**
```js
var NO_RECREATE_ON_CONFLICT_REASONS = { 'rfq-cust-heal': 1, 'contact-mig': 1, 'phonefmt-mig': 1 };
function dropLocalShadowStub(coll, dropCd) { /* از getData فیلتر → ptfSilentWrite → _ptfEntityLastKnown به‌روز → از nextArr هم splices تا ACK dirty را پاک کند */ }
// داخل retryWithFreshCode:
if (NO_RECREATE_ON_CONFLICT_REASONS[opts.reason || '']) {
  var dropped = dropLocalShadowStub(coll, oldCd);
  audit('یکپارچگی داده', '🛟 برخورد کدِ ' + …);
  settle({ state: 'acked', result: { skippedExisting: true, id: oldCd, shadowDropped: dropped } });
  return true; // بدون صدور کد نو، بدون upsert دوباره
}
```
- منطق: reason سیستمیِ **بدون قصد ساخت** در برخورد، «من کپیام را نگه نمی‌دارم؛ سرور حقیقت را دارد». سایهٔ محلی حذف می‌شود تا پول بعدی رکورد معتبرِ سرور (با تماس) جایش بنشیند.
- retry کاربرمحور (`offer-cust`، تبدیل سرنخ، AI-…) **عیناً حفظ** شد: صدور کد نو + upsert مجدد با `expectCreate:true` + toast. (قرارداد tester595 ۵.۶ سبز.)
- **تعیین‌حوزه:** `excel-import` عمداً **خارج** از مجموعه ماند — ورود اکسل قصد واقعی ساخت دارد و تغییرش ریسک جریان import است.

### بردار ۲: stub محافظ خالیِ تماس می‌فرستاد (دفاع در عمق)
- **قبل:** stub heal شامل `people: []`, `con: r.con || ''`, `ph: r.ph || ''` بود — حتی وقتی rfq هیچ تماسی نداشت.
- **خطر:** merge سرور (`entity_upsert`) قاعدهٔ «کلید حاضر بازنویسی می‌شود» دارد؛ هر مسیر `expectCreate:false` با این payload می‌توانست `people` پرِ رکورد هم‌کد را با `[]` بازنویسی کند.
- **بعد (`crm/offers.js`):** فقط وقتی دادهٔ واقعی هست: `if (r.con) { stub.con=…; stub.people=[{…}] } if (r.ph) stub.ph=…`. کلید خالی دیگر در payload نیست. قرارداد tester611 ۳.۳ (people وقتی con/ph هست) محفوظ.

### بردار ۳: migrateContacts برای رکورد بدون تماس legacy، fabrication می‌کرد
- **قبل:** برای هر رکورد فاقد `people`، بدون توجه به وجود `con`/`nm`/`ph` قدیمی، `people: []` و `coTels: []` ساخته و کل دفتر push می‌شد.
- **خطر:** همان قاعدهٔ merge سرور — `people: []` خالیِ bulk ماشینِ ناسازگار می‌توانست اشخاص پرِ رکورد هم‌کد را بشوید (بردار «یکی می‌بیند/یکی نه» ماشین‌به‌ماشین).
- **بعد:** `if (legacyName || legacyPhone) { …مهاجرت…; changed = true; }` — در غیر این‌صورت هیچ‌کاری نمی‌کند و push هم نمی‌شود. گارد truncation و `reason:'contact-mig'` (v34.38.6) دست‌نخورده؛ tester613 سبز.

### بردار ۴ (فرعی): dedup-by-phone سایت با ارقام فارسی کار نمی‌کرد
- **ریشه (`crm/bridge.js`):** نرمال‌ساز `normP` در `rfqSiteEnsureCustomer` با `/\D/g` ارقام فارسی `۰۱۲…` را دود می‌کرد → `phN=''` → تطبیق مشتری-موجود-بر-اساس-شماره اعمال نمی‌شد → برای همان شرکت مشتری **تکراری بدون تماس** ساخته می‌شد (یکی دیگر از تولیدکنندگانِ «ردیف روح»).
- **بعد:** پیش از `/\D/g`، `ptfToEnDigits` (با `typeof`-گارد و fallback وقتی phonefmt هنوز لود نشده) اجرا می‌شود؛ فارسی/لاتین/عربی-extended همگی به هنجار می‌رسند.

## ۴. چرا «فارسی‌رقم‌ها سالم‌اند» دقیقاً مشاهده شد ولی علّت ارقام نبود؟

تقسیم‌بندی واقعیِ رکوردهای سالم/ناسالم با **مبدأ سازی** هم‌بسته بود، نه دامنهٔ ارقام:
- رکوردهای «سالم» = ساختهٔ فرمِ اصلی از داخل CRM (که در جریانش هوک phonefmt شماره‌ها را فارسی‌رقم می‌کند) — همیشه تماس‌دار، چون upsert اصلی merge-safe بود و یک‌شاخه.
- رکوردهای «ناسالم» (برای ماشین دوم) = سایه‌های stub/تکراریِ تولیدکردهٔ مسیرهای خودکار — که چون stub هیچ تماسی نداشت، «مثل این‌که تماس پاک شده» به‌نظر می‌آمد.

نکتهٔ آزمون مستقیم: همان رکورد فارسی‌رقم اگر در برخورد کدِ heal بیفتاد، سایهٔ بدون تماسِ او هم ساخته می‌شد. ارقام فقط شاهد تصادفی بودند چون کاربران/CMS فارسی‌رقم ترجیح می‌دهند و CEO با کیبورد لاتین تایپ می‌کند.

## ۵. تکرارپذیری / شواهد (vm، کد واقعی)
- `/home/user/diag-contact/repro.js` (هارنس پژوهش): سناریو ۲ قبل از اصلاح → دو upsert (دومی با کد نو) + ردیف تکراری CUST-AXOO-… روی سرور؛ **بعد از اصلاح → یک upsert، بدون 409-retry، بدون تکرار، تماسِ رکورد معتبر دست‌نخورده** (۱۴/۱۵ سبز؛ ۱.۱۲ آرتیفکت هارنس شناخته‌شده).
- `tester614`: ۳۶ سنجهٔ سبز با بوت واقعی `sales-domain-v2.js` + `phonefmt.js` + برش واقعی heal/migrateContacts از `offers.js`.

## ۶. شواهد کیفیت‌بخشی انتشار
- `run-ci-gate.js` کامل: **227 PASS / 0 FAIL**.
- نقطهٔ دانش از ریلیز قبلی اعمال شد: بامپ توکن نسخه در تسترها هم فرم ساده (`34.38.6`) و هم فرم ریجکس (`34\.38\.6`) را پوشش داد؛ نام فایلِ تسترهای تولدبرجسته (مثل tester613) **تغییر نکرد** و مرجع گیت‌شان حفظ شد.
- `tester329` پیش از این ریلیز در HEAD قرمز بود (تست زمان‌بندی sync، خارج از دامنه) — توسط این تغییر نه سبز شد نه شکست‌اش به ابزار من مربوط است.

## ۷. توصیهٔ مشاهده‌پذیری (بدون تغییر رفتاری)
رویداد audit تازهٔ «🛟 برخورد کدِ <reason> در <coll>» نسخه‌های قدیمیِ کلاینت را هم در گزارش یکپارچگی نشان می‌دهد: اگر v34.38.7 به بعد باز هم تکرار «ردیف روح» دیده شود، از لاگ مشخص می‌شود کدام reason و کدام coll — برای برش دقیق‌تر در نسخهٔ بعد.
