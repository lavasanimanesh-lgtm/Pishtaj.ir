# 📦 ریلیزنوت v122.4 — US-284: آیکون‌های مینیمال سراسری (پایان ایموجی‌های رنگی)

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v122.3 | **دامنه:** CRM — ماژول جدید `iconx.js`

---

## 🎯 مسئله (گزارش کارفرما)

آیکون‌های داخلی (🗑️ حذف، ✏️ ویرایش، 👁️ مشاهده، ⏰، 📥، 🔎 و…) ایموجی‌های سیستم‌عامل بودند: رنگی، ناهماهنگ، غیرمینیمال — و در **حالت شب** ناخوانا و ناسازگار. آیکون‌های سایدبار و نوار بالا قبلاً خطی شده بودند (v86/v122.1) اما **داخل پنل‌ها و دکمه‌های عملیات** صدها ایموجی باقی بود (۷۰+ ایموجی یکتا در ۲۵+ ماژول).

## 💡 راه‌حل: موتور سراسری `iconx.js` (به‌جای دستکاری ۲۵ ماژول)

یک ماژول واحد که در زمان اجرا، همه ایموجی‌ها را با آیکون خطی مینیمال SVG جایگزین می‌کند:

| ویژگی | جزئیات |
|---|---|
| 🎨 طراحی یکدست | استروک 1.7، گوشه‌های گرد، `currentColor` → آیکون همرنگ متن اطرافش می‌شود؛ **در حالت شب خودکار روشن/خوانا** |
| 🗺 پوشش | ۹۰+ ایموجی → ۶۰+ آیکون خطی (حذف/ویرایش/مشاهده/چاپ/قفل/پیوست/اکسل/نمودار/ربات AI/سبد/ابر/…) |
| 🚦 وضعیت‌ها | ✅❌⚠️⛔ → آیکون خطی با رنگ ثابت معنادار (سبز/قرمز/کهربایی)؛ 🟢🟡🔴🔵🟠 → دایره‌های مینیمال رنگی |
| 🔒 ایمنی کامل | جایگزینی فقط روی گره‌های متنی DOM — سورس ماژول‌ها دست نمی‌خورد، هندلرهای onclick و فرم‌ها سالم می‌مانند |
| 🚫 استثناها | `textarea`/`script`/`style`/`pre`/`code` دست نمی‌خورند؛ داخل `<option>` ایموجی فقط حذف می‌شود (SVG در option رندر نمی‌شود)؛ گریز دستی با اتریبیوت `data-noix` |
| ⚡ معماری | MutationObserver + debounce با requestAnimationFrame — **بدون setInterval** (همسو TECHDEBT-004)؛ پنل‌ها و مودال‌های داینامیک هم بلافاصله پوشش داده می‌شوند |
| 🖨 چاپ | پنجره‌های چاپ قالب‌ها (window.open جدا) از این موتور عبور نمی‌کنند — اسناد رسمی بدون تغییر |

### چرا موتور مرکزی و نه ویرایش تک‌تک ماژول‌ها؟
- ~۱۲۰۰ نقطه دارای ایموجی در ۲۵ ماژول → ویرایش دستی پرریسک و رگرسیون‌زا
- هر ماژول/پنل جدید در آینده **خودکار** مینیمال می‌شود
- قابل بازگشت با حذف یک خط `<script>` (بدون اثر روی داده و منطق)

## 🧪 تست‌های عملکردی (jsdom — شبیه‌سازی مرورگر واقعی)
- جایگزینی اولیه: ۱۰/۱۰ ایموجی نمونه → SVG ✅
- `onclick` دکمه‌ها پس از جایگزینی سالم ✅
- `<option>`: حذف تمیز ایموجی ✅ | `textarea` و `data-noix`: دست‌نخورده ✅
- پنل داینامیک و مودال `md-b` بعد از بوت: پوشش خودکار ✅
- هیچ ایموجی نگاشت‌شده‌ای در DOM باقی نماند ✅

## 🧪 کیفیت
| سنجه | نتیجه |
|---|---|
| `_tools/audit.py` | ✅ ۰ خطا (۱ هشدار شناخته‌شده تصاویر) |
| `node --check iconx.js` | ✅ |
| تستر جدید `tester39-v1224.js` | ✅ ۱۹/۱۹ PASS |
| رگرسیون کامل | ✅ همه PASS (tester38 نسخه‌آزاد شد) — مجموع **۲۶ تستر** |

## 📁 فایل‌های تغییریافته
`crm/iconx.js` (جدید — موتور آیکون) · `crm/index.html` (VER v122.4 + ثبت اسکریپت) · `crm/sw.js` (کش + SHELL) · `_tools/uat/tester39` (جدید)

## 📌 یادداشت
- اگر جایی آیکونی خواستید عمداً ایموجی بماند: به المان `data-noix` بدهید.
- اسکرین‌شات‌های پیام‌رسان طبق قرار پس از رسیدگی از workspace پاک شدند.
- بک‌لاگ: اسپرینت ۱۲۳ (US-260/263/264) با «بریم بعدی» آغاز می‌شود.
