# 🚀 ریلیز v16.5 — US-399: تامین‌کننده تخصصی و تامین‌یاب هوشمند + پیش‌نویس نظام امتیازدهی
**تاریخ:** ۱۴۰۵/۰۴/۱۹ | **نسخه قبلی:** v16.4 | **بسته:** `pishtaj-release-v16.5.zip` (تجمیعی از v14.5)

طبق ابلاغ شما «یوزراستوری‌های هم‌خانواده با هم»، این ریلیز شامل **اجرای کامل US-399** است و **پیش‌نویس سند نظام امتیازدهی** (هم‌خانواده — سوار بر همین پروفایل تخصصی) نیز با پنل کارشناسی تدوین و جهت اخذ نظر شما ضمیمه شد: `PROPOSAL-SCORING-SYSTEM-v1.md` — **بدون حتی یک خط کد اجرایی تا تایید شما**.

---

## 🆕 US-399 — پروفایل تخصصی تامین‌کننده (برندها + تجهیزات) و تامین‌یاب هوشمند

ماژول جدید `crm/supspec.js` — دقیقا سناریوی شما: «می‌خوام یک تجهیز زیمنس را قیمت بگیرم، سرچ می‌کنم زیمنس...»

### ۱) فرم تامین‌کننده: دو فیلد چیپی جدید (AC1)
- «🏷 برندهای تخصصی»: تایپ آزاد + پیشنهاد از **۳۰ برند شناخته‌شده صنعت** (Siemens، WIKA، Rosemount، E+H، KITZ و...) + برندهای موجود در ماژول کالاها. **فارسی بنویسید، متعارف انگلیسی ذخیره می‌شود** («زیمنس» → Siemens) — تکراری دوزبانه هم ثبت نمی‌شود.
- «🔧 تجهیزات تخصصی»: تایپ آزاد + پیشنهاد ۲۲ تجهیز متداول (ترانسمیتر فشار، شیر کنترلی، الکتروموتور و...).
- **مهاجرت نرم:** فیلد `brands` رشته‌ای رکوردهای ثبت‌نام‌شده از سایت، خودکار به چیپ‌های متعارف تبدیل می‌شود (رکوردی که قبلا چیپ دارد دست نمی‌خورد).
- چیپ‌های هر تامین‌کننده در جدول فهرست هم زیر ستون «دسته» دیده می‌شوند.

### ۲) جستجوی دوزبانه برند-آگاه (AC2)
- جستجوی «زیمنس» و «Siemens» و «siemens» → **یک نتیجه واحد**. جدول مترادف ۳۰ برندی (`PTF_BRAND_ALIASES`) + نرمال‌سازی همان موتور مشترک `dedupNorm` (الگوی PTF_CAT_ALIASES نسخه v15.9 — بدون موتور موازی).
- جستجوی تجهیز («ترانسمیتر فشار») هم تامین‌کنندگان متخصص را می‌آورد.
- مسیر جستجوی قبلی (نام/کد/رابط/شماره) عینا حفظ شده — تخصص فقط «اضافه» می‌کند.

### ۳) پیشنهادگر هوشمند «درخواست تامین» per قلم (AC3)
امتیازدهی موجود rfqsmart **گسترش** یافت (نه جایگزینی):
| سیگنال | وزن | مثال دلیل نمایشی |
|--------|-----|------------------|
| 🏷 تطبیق برند اقلام با تخصص | بالا (۴۰ per برند، سقف ۸۰) | «تخصص برند: Siemens» |
| 🔧 تطبیق تجهیز | متوسط (۲۰ per، سقف ۴۰) | «تخصص تجهیز: گیج فشار» |
| 🛒 سابقه خرید واقعی موفق از او | (۱۰ per، سقف ۳۰) | «سابقه ۲ خرید موفق» |
| زمینه کاری/واژه/سابقه قیمت‌دهی | پایه (مثل قبل) | «دسته منطبق: ابزار دقیق» |

برند اقلام هم از فیلد برند صریح و هم از **متن شرح/مشخصات** (دوزبانه — «گیج فشار ویکا» → WIKA) شناسایی می‌شود. سابقه خرید از purchases واقعی (خرید واقعی v16.0/16.3) می‌آید — همه با ذکر دلیل روی کارت پیشنهاد.

### ۴) اصلاح‌پذیر با یادگیری (AC4)
- پیشنهاد سیستم مثل قبل کاملا قابل تغییر است (تیک‌ها آزاد).
- اگر تامین‌کننده‌ای را **دستی** انتخاب کنید که سیستم بابت برند پیشنهادش نکرده بود، سیستم **با تایید شما** آن برند را به تخصص‌هایش اضافه می‌کند تا دفعه بعد خودکار پیشنهاد شود (ثبت در audit؛ انصراف = هیچ تغییری؛ سوال تکراری نمی‌پرسد).

### ۵) سازگاری کامل (AC5)
تامین‌کنندگان بدون تخصص ثبت‌شده دقیقا مثل قبل با زمینه کاری امتیاز می‌گیرند و پیشنهاد می‌شوند — هیچ رفتاری نشکسته.

---

## 📊 ضمیمه: پیش‌نویس نظام امتیازدهی (منتظر نظر شما)

سند `PROPOSAL-SCORING-SYSTEM-v1.md` — خروجی پنل مشاوران ارشد بازرگانی + مدیران ارشد تامین + کارشناس مقررات سازمان‌های بازرگانی:
- **تامین‌کنندگان (۱۰۰ امتیاز):** پاسخگویی استعلام ۲۰ / رقابتی بودن قیمت ۲۰ / سابقه خرید موفق ۲۰ / تخصص و پوشش کالایی ۱۵ / کیفیت تحویل ۱۵ / اعتبار ثبتی ۱۰ → سطح‌های 🥇🥈🥉⚠️
- **مشتریان (۱۰۰ امتیاز):** خوش‌حسابی ۳۰ / حجم خرید ۲۵ / نرخ تبدیل ۲۰ / وفاداری ۱۵ / کیفیت تعامل ۱۰ → سطح‌های 💎🌟🔵⚪
- اصل کلیدی پنل: **صفر فیلد جدید اجباری** — همه معیارها از داده‌ای که همین حالا ثبت می‌شود محاسبه می‌شوند.
- **۵ سوال باز** انتهای سند منتظر تصمیم شماست (وزن‌ها، معیار تحویل، هشدار بدحسابی هنگام CO، برچسب‌ها، حضور در گزارش‌ها). پس از نظر شما نهایی و فقط آن‌وقت اجرا می‌شود (US-400/401 — ۲ اسپرینت).

---

## 🛡 اثبات «بدون وصله‌پینه، بدون باگ جدید، بدون تداخل»

| اصل | اثبات |
|-----|-------|
| بدون وصله‌پینه | یک موتور مترادف واحد (`PTF_BRAND_ALIASES` + `dedupNorm` مشترک) برای فرم، جستجو، امتیازدهی و یادگیری. hook فرم تامین‌کننده همان الگوی مصوب cheques.js (فیلد داخلی/خارجی). امتیاز تخصصی داخل همان `rfqsScoreSuppliers` موجود ادغام شد نه موازی‌سازی. |
| وابستگی‌ها | زنجیره hook های showSupModal2/saveSup2 (cheques→supspec) و renderSuppliers (bridge→cheques→guards→supspec) سالم؛ ترتیب لود صحیح (supspec بعد از offers/cheques). سازگاری با ثبت‌نام سایت (فیلد brands) با مهاجرت نرم. |
| بدون باگ جدید | tester83 جدید: **۵۶ چک** (متعارف‌سازی دوزبانه، جستجوی یک‌نتیجه با entityMatches واقعی، امتیاز per قلم با وزن‌ها، رتبه‌بندی کامل rfqsScoreSuppliers، مهاجرت نرم، یادگیری/انصراف/عدم‌تکرار) + **اثبات DOM با jsdom** روی فرم واقعی (۷ چک: تزریق چیپ، ثبت فارسی→متعارف، ضدتکرار دوزبانه، ذخیره/ویرایش/حذف). رگرسیون کامل: **۷۰ تستر / ۱۹۷۹ چک — صفر خطا**. audit PASS. |
| بدون تداخل با طرح توسعه | نظام امتیازدهی آینده (US-400/401) روی همین spBrands/spEquip و purchases سوار می‌شود — این ریلیز پیش‌نیاز آن است. US-396/395/397 (اسپرینت ح) فایل مشترکی ندارند. |

## 📋 راهنمای به‌روزرسانی
1. `pishtaj-release-v16.5.zip` روی هاست روی موجود اکسترکت شود — پوشه `api` حذف نشود.
2. همه کاربران رفرش کنند — نسخه سایدبار باید **v16.5** شود.
3. تخصص‌ها: تامین‌کنندگان → ✏️ ویرایش هر تامین‌کننده → بخش «🏷 برندهای تخصصی / 🔧 تجهیزات تخصصی». هرچه پروفایل‌ها کامل‌تر، پیشنهادگر دقیق‌تر (و نظام امتیازدهی آینده آماده‌تر).
4. نظر خود را درباره **۵ سوال سند امتیازدهی** اعلام فرمایید تا نهایی و برنامه‌ریزی اجرا شود.
