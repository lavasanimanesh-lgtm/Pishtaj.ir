# RELEASE NOTES — v34.7.64 (۱۴۰۵/۰۵/۳۰ — 2026-08-21)

## ادغام دو آیکون ضمیمه در ردیف درخواست‌ها

در فهرست «درخواست‌ها»، جلوی هر ردیف **دو آیکون پیوست 📎** وجود داشت که هر دو یک کار را
انجام می‌دادند (باز کردن پنجرهٔ «مدیریت ضمایم»):

| محل | آیکون قبلی | وضعیت |
|---|---|---|
| ستون کد | بج `📎 N ضمیمه` / `📎 بدون ضمیمه` | فقط وقتی ضمیمه داشت قابل کلیک بود |
| ستون عملیات | دکمهٔ `📎 مدیریت پیوست‌ها` / `📎 پیوست‌ها` | همیشه — تکراری |

### تغییر

1. **بج ضمایم** در ستون کد حالا **همیشه کلیک‌پذیر** است (چه ضمیمه داشته باشد چه نداشته
   باشد) و همان پنجرهٔ مدیریت ضمایم (مشاهده/دانلود + افزودن) را باز می‌کند.
2. **دکمهٔ تکراری** `📎 مدیریت پیوست‌ها / پیوست‌ها` از ستون عملیات حذف شد.

نتیجه: جلوی هر ردیف درخواست دقیقاً **یک آیکون ضمیمه** باقی می‌ماند که هم شمارش را نشان
می‌دهد و هم نقطهٔ ورود واحد مدیریت اسناد ضمیمه است.

### فنی

- `crm/bridge.js` — بج `no-files` با `role="button"` + `onclick` همان مسیر `ptfManageInqAttachments`.
- `crm/inqreader.js` — حذف دو بلوک دکمهٔ تکراری؛ تابع `ptfManageInqAttachments` دست‌نخورده ماند.

### تست

- تستر جدید `tester466-v34.7.64-attachment-pin-merge.js` (در گیت CI).
- **گیت CI: ۸۲ PASS / ۰ FAIL** · نگهبان معماری: PASS.
