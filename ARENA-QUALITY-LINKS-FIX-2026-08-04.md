# فاز ۸ (v34.0.11-alpha) — لینک موارد «کیفیت داده» به بخش مربوطه

**برنچ:** `arena/019fcd1d-pishtaj-ir`
**مبنای تصمیم:** درخواست کارفرما — موارد نیازمند بررسی در تب «کیفیت داده» باید به بخش مربوطه لینک شوند تا کاربر مستقیم هدایت شود (مثلاً فاکتور خریدِ بدونِ نوعِ رسمی/غیررسمی → مشخص باشد مربوط به کدام تأمین‌کننده است).

---

## مشکل
در تب کیفیت داده، آیتم‌های «هزینه جاری بدون تعیین نوع» و «فاکتور خرید تأمین‌کننده بدون تعیین نوع» فقط با یک `ref` (شناسه) ثبت می‌شدند و **بدون `detail`** — بنابراین هیچ دکمه/لینک/اقدامی نداشتند. کاربر نمیدانست مربوط به کدام تأمین‌کننده است و راهی برای رفتن مستقیم به بخش مربوطه نداشت.

## اصلاح (`crm/data-quality.js`)
- **هزینه جاری (`opex-unclassified`):** حالا با `detail { type:'opex', cd, label: 'هزینه {cat} — {desc} ({month})' }` ثبت می‌شود → در `qualityRefsHtml` دکمهٔ «✏️ اصلاح هزینه» (`ptfOpexEdit`) ظاهر میشود که فرم ویرایش (شامل انتخاب رسمی/غیررسمی) را باز میکند.
- **فاکتور خرید (`supplier-invoice-unclassified`):** حالا با `detail { type:'supplier-invoice', cd, label }` ثبت می‌شود و **نام تأمین‌کننده** (`supName`) به `label` اضافه میشود → دکمهٔ «✏️ اصلاح فاکتور خرید» (`slInvoiceEdit`) ظاهر میشود که دیالوگ ویرایش فاکتور (شامل انتخاب نوع) را باز میکند و پس از ذخیره `slOpenLedger(supplierCd)` را صدا می‌زند → **مستقیم به حساب همان تأمین‌کننده هدایت میکند** (نام در عنوان مودال دیده میشود).

این دو action از قبل در `qualityRefsHtml` تعریف شده بودند؛ فقط با افزودن `detail` فعال شدند.

## تست‌ها
- **tester307** جدید (۸/۰): opex/supplier-invoice دارای لینک اصلاح + نام تأمین‌کننده؛ حفظ actionهای چک و procurement.
- `tester168` (کیفیت داده) همچنان سبز؛ رگرسیون کامل (۱۷ تستر، ۰ شکست).
- بامپ نسخه → **v34.0.11-alpha**.
- تغییرات `.github/workflows/*` در PENDING-workflow-changes ثبت شد.

*تولیدشده توسط Arena Agent — لینک‌سازی موارد کیفیت داده به ماژول‌های اصلی.*
