# Release Notes v34.7.0 — چرخه کامل مدارک پرونده و ACK پیوست درخواست

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## تغییرات اصلی

### مدارک پرونده فروش
- رویدادهای پکینگ، بارنامه/بیجک، ارسال و تحویل کارفرما اکنون از داخل پرونده قابل اصلاح و حذف کنترل‌شده‌اند.
- رکوردهای QC/بازرسی و فایل‌های پیوست آن‌ها قابل اصلاح، حذف، افزودن فایل و جایگزینی نسخه هستند.
- اسناد متفرقه پرونده قابلیت اصلاح نام/شرح، جایگزینی نسخه و حذف با دلیل دارند.
- هر اصلاح/حذف، تصویر قبل/بعد یا Tombstone را در `documentAudit` و شرح عملیات را در timeline/audit نگه می‌دارد.
- پاک‌سازی فایل ابری از actionهای محدودشده به prefix و نقش همان مدارک استفاده می‌کند.
- پیشنهادها، استعلام‌های تامین، نامه‌ها و فاکتورها از کشوی پرونده به مسیر اصلاح/حذف/ابطال مرتبط متصل شدند.
- اسناد قطعی برد تغییرناپذیر باقی می‌مانند و اصلاح تجاری آن‌ها فقط با رویژن/متمم است.
- ضمیمه فاکتور رسمی حذف مستقل ندارد و فقط با جایگزینی نسخه‌دار اصلاح می‌شود.

### بازمحاسبه مرحله پرونده
- وضعیت قبل از نخستین شاهد حمل ذخیره می‌شود.
- حذف تحویل، بارنامه یا پکینگ، مرحله پرونده و status آینه‌ای درخواست را از شواهد باقی‌مانده بازسازی می‌کند.
- اگر ارجاع فاکتور، فاکتور یا شاهد مرحله بعدی وجود داشته باشد، حذف مدرک قدیمی مرحله را عقب نمی‌برد.
- توالی زمانی پکینگ ≤ بارنامه ≤ تحویل در ثبت و ویرایش کنترل می‌شود.
- داخل استپر برای هر مرحله «کار لازم برای مرحله بعد»، شاهد لازم و دکمه اقدام مرتبط نمایش داده می‌شود.

### پیوست درخواست (RFQ)
- مسیر قدیمی که پس از upload فقط `setData('ptf_crm_rfqs')` می‌زد و بدون ACK پیام موفقیت می‌داد حذف شد.
- افزودن، حذف و جایگزینی پیوست با command اتمیک `sales-domain.php` روی همان RFQ و category انجام می‌شود.
- موفقیت فقط پس از پاسخ قطعی سرور نمایش داده می‌شود؛ خطا به‌صورت قابل اقدام گزارش و فایل uploadشده یتیم پاک‌سازی می‌شود.
- حذف فایل RFQ با action ابری محدود به prefix `rfqatt/` انجام می‌شود.

### دریافت مشتری
- دکمه و نمایش میراثی «پیش‌دریافت» از عملیات خرید حذف شد.
- نمایش مالی فقط Receipt قطعی متصل به شناسه پرونده را نشان می‌دهد؛ `paid/cashFull` و شرایط پیشنهاد همچنان اثر مالی واقعی نمی‌سازند.

## کنترل کیفیت
- `tester405-v34.7.0-case-document-lifecycle.js`: 23 PASS / 0 FAIL
- syntax check فایل‌های JavaScript تغییرکرده
- CI gate شامل تست جدید چرخه مدارک و ACK پیوست درخواست
