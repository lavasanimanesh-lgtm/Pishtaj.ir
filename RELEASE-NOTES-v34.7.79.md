# RELEASE NOTES — v34.7.79 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## شرایط پرداخت تامین‌کننده — نقدی / تعهدی + بازهٔ اعتبار + امتیاز

### درخواست

در فرم ثبت‌نام تامین‌کنندگان سایت، مشخص شود تامین‌کننده مایل به همکاری **نقدی** است یا
**تعهدی (اعتباری)**؛ فیلد کشویی باشد و با انتخاب تعهدی، **بازه‌های اعتبار** قابل انتخاب شوند.
این موضوع باید **امتیاز** هم داشته باشد.

### ارزیابی و بازه‌ها

پایهٔ تعیین بازه‌ها، نرمِ همکاری اعتباری در بازار B2B صنعتی/تجهیزات ایران است (مستند کامل:
`ASSESSMENT-SUPPLIER-PAYMENT-TERMS-2026-08-22.md`). خلاصه:

| گزینه | بازه | امتیاز |
|---|---|---|
| 💵 نقدی (پرداخت در محل / پیش‌پرداخت) | — | ۱۰ |
| 🧾 تعهدی — تا ۱ ماهه | ۳۰ روز | ۱۲ |
| 🧾 تعهدی — ۱ تا ۲ ماهه | ۶۰ روز | 15 |
| 🧾 تعهدی — ۲ تا ۳ ماهه | ۹۰ روز | ۱۸ |
| 🧾 تعهدی — ۳ تا ۴ ماهه | ۱۲۰ روز | ۱۴ |
| 🧾 تعهدی — ۴ تا ۶ ماهه | ۱۸۰ روز | ۸ |
| 🧾 تعهدی — بیش از ۶ ماهه | ۱۸۰+ روز | ۲ |

امتیاز از دید **جریان نقدی خریدار** است: تعهدی ۱–۳ ماهه دورهٔ وصول کارفرما را پوشش می‌دهد
(اوج امتیاز)، نقدی میانی، و بیش از ۳ ماه به‌تدریج ریسک نکول/فرسایش تورمی می‌گیرد.

### تغییرات

#### ۱. فرم سایت (`supplier/index.html`)

- کشوی اول «روش همکاری و شرایط پرداخت» (نقدی / تعهدی) — الزامی.
- کشوی دوم «بازهٔ اعتبار» — در حالت نقدی غیرفعال؛ با انتخاب تعهدی فعال می‌شود.
- نمایش زندهٔ امتیاز گزینه زیر فیلدها + اعتبارسنجی «تعهدی بدون بازه» پیش از ارسال.

#### ۲. سرور (`api/crm.php` → `add_supplier`)

- دریافت `payTerms` و `creditRange` با allowlist سخت‌گیرانه (`30/60/90/120/180/365`).
- محاسبهٔ `payScore` و ذخیرهٔ هر سه فیلد روی رکورد — هم در ثبت جدید و هم در شاخهٔ تکمیل مدارسک
  (resubmit).
- خطای صریح `invalid_pay_terms` برای ورودی نامعتبر.

#### ۳. CRM (`crm/bridge.js` + `crm/offers.js`)

- توابع مشترک `ptfSupPayLabel` / `ptfSupPayBadge` برای برچسب و نشان امتیاز.
- ستون «شرایط پرداخت» (با نشان امتیاز) در فهرست «درخواست‌های سایت».
- نمایش شرایط پرداخت در مودال جزئیات ثبت‌نام.
- انتقال `payTerms/creditRange/payScore` به رکورد تاییدشده هنگام تایید (برای رتبه‌بندی آینده).
- نشان کوچک شرایط پرداخت در فهرست تامین‌کنندگان تاییدشده.

### نکتهٔ تصمیم‌گیری (قابل بازنگری)

جهت امتیازدهی فعلاً «تعهدی میان‌مدت مطلوب‌تر» است. اگر نظر شما این باشد که **نقدی** امتیاز
بیشتر بگیرد، فقط جدول ثابت `payScore` در `api/crm.php` (و `PAY_SCORE` در فرم) تغییر می‌کند؛
دادهٔ خام (`payTerms` + `creditRange`) دست‌نخورده ذخیره می‌شود.

### فایل‌ها

- `supplier/index.html` — دو کشو + امتیاز زنده + اعتبارسنجی.
- `api/crm.php` — اعتبارسنجی/امتیاز/ذخیره در `add_supplier`.
- `crm/bridge.js` — نمایش و انتقال شرایط پرداخت + helpers.
- `crm/offers.js` — نشان در فهرست تاییدشده.
- `ASSESSMENT-SUPPLIER-PAYMENT-TERMS-2026-08-22.md` — ارزیابی و مدل امتیاز.
- `_tools/uat/tester481-v34.7.79-supplier-pay-terms.js` — تستر جدید (ثبت در گیت CI).

### اعتبارسنجی

- `node _tools/arch/arch-guard.js` → `=== ARCH GUARD: PASS ===`
- `node _tools/uat/run-ci-gate.js` → `97 PASS / 0 FAIL`
