# Release Notes — v31.7.46

**تاریخ:** ۱۴۰۵/۰۴/۳۰ (2026-07-21)  
**نسخه:** `v31.7.46`  
**نوع:** 🔴 Hotfix — BUG-INQ-EDIT-LOCK-001

---

## 🎯 مسئله گزارش‌شده

در ویرایش اقلام درخواست، با وجود اینکه برای درخواست هیچ پیشنهاد فنی/مالی صادر نشده بود، سیستم پیام زیر را نشان می‌داد و اجازه ورود به مرحله ویرایش اقلام را نمی‌داد:

```text
مشخصات اصلی به دلیل صدور پیشنهاد فنی/مالی قفل شده است.
```

---

## 🔍 RCA

در `crm/inqreader.js` منطق تشخیص پیشنهاد مرتبط با درخواست این‌گونه بود:

```js
o.inqNo === cd || o.inqNo === r.inqNo
```

اگر `r.inqNo` و `o.inqNo` هر دو `undefined` یا خالی بودند، مقایسه دوم به‌صورت false-positive عمل می‌کرد و سیستم فکر می‌کرد برای درخواست پیشنهاد صادر شده است.

همین الگو در دو نقطه وجود داشت:

1. در خود `ptfOpenFullInqEditor`
2. در hook جدول درخواست‌ها برای تشخیص `hasOffer` و `wonOffer`

---

## ✅ اصلاح انجام‌شده

تشخیص پیشنهاد مرتبط اکنون فقط با aliasهای غیرخالی انجام می‌شود:

```js
var aliasesForOffer = irRfqAliases(r, cd);
var v = String((o && (o.inqNo || o.srcRfq || '')) || '').trim();
return !!v && aliasesForOffer.indexOf(v) > -1;
```

بنابراین:

- اگر واقعاً پیشنهاد مرتبط وجود نداشته باشد، ویرایش اقلام باز می‌شود.
- اگر پیشنهاد واقعی با `inqNo` یا `srcRfq` مرتبط وجود داشته باشد، گارد همچنان کار می‌کند.
- false-positive ناشی از `undefined === undefined` حذف شد.

---

## 📁 فایل‌های تغییر یافته

```text
crm/inqreader.js
crm/index.html
crm/sw.js
crm/clear-cache.html
PTF-MASTER-HANDOVER.md
_tools/uat/tester223-rfq-item-editor-lock.js
RELEASE-NOTES-v31.7.46.md
REGRESSION-REPORT-v31.7.46.md
```

---

## 🧪 تست اضافه‌شده

### `_tools/uat/tester223-rfq-item-editor-lock.js`

پوشش:

- وجود bug marker.
- گارد ویرایش اقلام از aliasهای غیرخالی استفاده می‌کند.
- الگوی قدیمی `o.inqNo === cd || o.inqNo === r.inqNo` حذف شده است.
- شبیه‌سازی نشان می‌دهد گارد قدیمی در حالت `undefined` false-positive می‌داد.
- گارد جدید بدون پیشنهاد واقعی قفل نمی‌کند.
- گارد جدید در صورت پیشنهاد واقعی همچنان قفل می‌کند.
- alias شماره درخواست کارفرما نیز پشتیبانی می‌شود.

---

## ⚠️ Impact Analysis

| بخش | اثر | ریسک |
|---|---|---|
| ویرایش اقلام درخواست | false-positive lock رفع شد | کم |
| درخواست دارای پیشنهاد واقعی | گارد همچنان فعال است | کم |
| جدول درخواست‌ها | hasOffer/wonOffer دقیق‌تر شد | کم |
| RFQ/Offer/Finance/Auth/Sync | منطق اصلی دست‌نخورده | صفر |

---

## 📋 راستی‌آزمایی کارفرما

1. یک درخواست بدون پیشنهاد فنی/مالی را باز کنید.
2. روی «مشاهده» بزنید.
3. روی «افزودن/ویرایش اقلام» بزنید.
4. انتظار صحیح: editor اقلام باز شود و پیام قفل نمایش داده نشود.
5. یک درخواست که واقعاً پیشنهاد دارد را تست کنید.
6. انتظار صحیح: گارد قفل همچنان در موارد واقعی عمل کند.

---

## 🚦 نتیجه

قفل اشتباه ویرایش اقلام رفع شد. اکنون درخواست‌هایی که واقعاً پیشنهاد ندارند قابل تکمیل اقلام بعد از ثبت هستند.
