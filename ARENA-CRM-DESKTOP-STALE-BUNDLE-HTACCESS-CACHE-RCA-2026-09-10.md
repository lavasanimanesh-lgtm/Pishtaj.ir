# ARENA · RCA تکمیلی — «پردهٔ آماده‌سازی بوت روی موبایل می‌آید ولی روی دسکتاپ نه»

**تاریخ:** ۲۰۲۶-۰۹-۱۰ (۱۴۰۵/۰۶/۱۹) · **برچسب تغییر:** v34.38.18-02 (CACHE-HOTFIX) · **دامنه:** `crm/.htaccess` · `_tools/uat/tester536-…js` · `.github/workflows/deploy-staging.yml` · `_tools/ci/workflow-templates/deploy-staging.yml`

---

## ۱) گزارش کارفرما

> [پس از مرج #۲۷ و رفع دیپلوی توسط #۲۸] صفحهٔ «در حال آماده‌سازی سامانه…» روی موبایل
> نمایش داده می‌شود اما روی دسکتاپ نه.

## ۲) خلاصهٔ یافته

کدِ ویژگی در main و روی پروداکشن **کاملاً سالم** است (مرجع: RCA روز قبل در همین مخزن — گیت
integrity پروداکشن، هشِ زندهٔ `crm/index.html` == کامیت را تأیید کرده است). ناهماهنگی
موبایل/دسکتاپ نه از کد که از **زنجیرهٔ کش هاست + عدم بامپ کوئری نسخه** است:

### حلقهٔ ۱ — پین ۳۰‌روزهٔ JS در `.htaccess` ریشه (برای SEO سایتِ عمومی)
```apache
ExpiresByType application/javascript "access plus 30 days"
<FilesMatch "\.(?:css|js|jpg|…)$">
  Header set Cache-Control "public, max-age=2592000"
</FilesMatch>
```
این قانون روی کل درخت — از جمله `crm/*.js` — اعمال می‌شود.

### حلقهٔ ۲ — URLِ کوئری‌دارِ ثابت با محتوای متغیر
در ۵ مرج پیاپی (#۲۴ تا #۲۸) رشتهٔ نسخه روی همان `?v=34.38.16` ماند درحالی‌که محتوا عوض شد.
شواهد قطعی از compare گیت‌هاب:

```
98e6fc44..afcf0a74 ⇒ crm/boot-splash.js +423 (جدید) · crm/index.html +36 · crm/sync.js +21/-2
src="sync.js?v=34.38.16"   ← یکسان در هر دو سمت
```

طبق RFC، مرورگر این URL را تا `max-age=2592000` **بدون هیچ revalidate‌ای** — حتی با F5ِ
معمولی — از کش HTTP اجرا می‌کند؛ فقط Ctrl+Shift+R یا پاک‌سازی کش آن را می‌شکند.

### حلقهٔ ۳ — HTML بدون هدر کش صریح
روی `crm/index.html` هیچ `Cache-Control` نی نیست ⇒ مرورگرها Last-Modified-heuristic اعمال
می‌کنند و ممکن است در ناوبریِ معمولی (بازکردن URL/تب/بوک‌مارک، بدون F5) نمای HTMLِ قدیمی را
سرو کنند (فقدان تگ `boot-splash.js` ⇒ ویژگی به‌کلی غایب است — دقیقاً همین مشاهده).

### چرا موبایل «درست» بود
اپلیکیشن موبایل (TWA — `_tools/apk`) با پروفایل جدا/تازه همه‌چیز را پس از دیپلوی موفق
06:17Z تازه دانلود کرد؛ پروفایل دسکتاپ کشِ چندروزهٔ پر-از-URLهای پین‌شده داشت.

## ۳) چرا قبلاً «اینطوری درست شده بود» ولی شکست

سیاست قدیمی (پین‌شده در tester536/T0-6): «no-cache فقط به استیجینگ تزریق می‌شود؛ پروداکشن با
پین ۳۰‌روزه + **انضباطِ بامپ نسخه در هر تغییر محتوا** امن است». این رویداد اثبات کرد آن
فروض در عمل برقرار نمی‌ماند — پنج مرج بدون بامپ رفتند روی هاست و قربانیِ آن همین باگ شد.

## ۴) اصلاح (ریشه‌ای)

| # | تغییر | اثر |
|---|---|---|
| ۱ | `crm/.htaccess`: برای زیردرخت CRM (html/js/css/json/webmanifest) هدر `Cache-Control: no-cache, must-revalidate` + `ExpiresActive Off` | هر بارگذاری با سند تطبیق می‌شود؛ 304 سبک برای فایلِ بدون‌تغییر؛ موبایل/دسکتاپ همیشه هم‌راستا |
| ۲ | `tester536`: معکوس‌شدن آگاهانهٔ پین قدیمی T0-6 (فایل کامیت‌شده اکنون **عمداً** no-cache دارد) + مستندسازی علت | گیت CI نه به‌خاطر این اصلاحِ آگاهانه قرمز می‌شود و نه اجازهٔ بازگشتِ سیاستِ شکننده را می‌دهد |
| ۳ | `deploy-staging.yml` (+قالب workflow-templates): الحاق no-cache استیجینگ **idempotent** شد (نگهبان grep) + کامنت‌های تاریخی به‌روز شدند | بلوک دوتایی ساخته نمی‌شود؛ مرجعِ تنها .htaccess کامیت‌شده است |
| ۴ | پین ۳۰‌روزهٔ سایتِ عمومی (SEO/پرفرمنس) | **دست‌نخورده** — فقط زیردرخت `/crm` مستثناست |

## ۵) چه کاری الان برای دستگاهِ دسکتاپ لازم است (یک‌بار)

پین‌های ۳۰‌روزه‌ای که **قبلاً** در کش مرورگر دسکتاپ غلتیده‌اند خودبه‌خود حل نمی‌شوند؛ پس از
رفتن این اصلاحیه روی هاست، یک‌بار «رفرش اجباری» کافی است تا نسخهٔ تازه — با هدر جدید —
دریافت و از آن پس همیشه تازه بماند:

- **ویندوز:** `Ctrl + Shift + R` (یا `Ctrl + F5`)
- **مک:** `Cmd + Shift + R`
- سخت‌گیرانه‌تر: DevTools → Application → Storage → Clear site data → سپس رفرش

با این کار، `index.html` جدید + `sync.js` جدید + `boot-splash.js` می‌آیند و پردهٔ
«در حال آماده‌سازی سامانه…» دقیقاً مثل موبایل دیده می‌شود.

## ۶) تأیید
- `node --check` + گیت تسترها: tester536 (۶۰/۰) · tester540 (۳۰/۰) · tester593 (۲۷/۰)
- مقایسهٔ متغیر/ثابت URL (پرهامچ پ ۱): مستقیم از API گیت‌هاب.
- φ مرکوسی: `ExpiresActive Off` ریشه‌ایترین حذف پین mod_expires برای /crm است؛ سیاست
  ۳۰‌روزهٔ `Header set ...` ریشه با `Header always set Cache-Control` زیردرختی overriding می‌شود.
