# RCA — نگاشت نادرست خرید/استعلام با اقلام پیشنهاد مالی
## BUG-PROC-LINK-287

## حکم فوری
تصویر ارسالی نشان می‌دهد **ماتریس سود** برای دست‌کم یک قلم، قیمت استعلامی قلم دیگر را نمایش داده است. این به‌تنهایی ثابت نمی‌کند قیمت فروش CO یا خرید واقعی ثبت‌شده تغییر کرده‌اند؛ اما خود ماتریس در آن وضعیت برای تصمیم‌گیری سود/خرید قابل اتکا نبوده است.

## ریشهٔ فنی
در `crm/offers.js`، دو fallback خطرناک وجود داشت:

```text
خرید واقعی:  purchase.idx === شمارهٔ ردیف CO
استعلامی:    rfq.items[i]
```

`idx` فقط جایگاه یک قلم در آرایهٔ داخلی خرید است، نه شناسهٔ مشترک قلم بین CO، RFQ Smart و خرید واقعی. بنابراین این رخدادها می‌توانند نگاشت را غلط کنند:

- حذف یک قلم از CO یا RFQ؛
- افزودن قلم جدید؛
- جابه‌جایی ترتیب ردیف‌ها؛
- تبدیل TO به CO؛
- ایجاد چند RFQ/CMP برای یک درخواست؛
- اقلام مشابه با نام یکسان اما مدل/مشخصات متفاوت.

## ریشه‌های مشابه کشف‌شده
1. Optimizer سود از index برای خرید واقعی و استعلامی استفاده می‌کرد.
2. اعمال سود گروهی، `matchedRfq.items[j]` را بر اساس شمارهٔ ردیف می‌خواند.
3. به‌روزرسانی قیمت مرجع کالا در RFQ Smart، در نام‌های تکراری اولین محصول هم‌نام را انتخاب می‌کرد.
4. renderer CO نیز در بعضی وضعیت‌ها اولین کالای هم‌نام را به‌عنوان مرجع قیمت برمی‌داشت.
5. خریدهای واقعی جدید provenance قلم منبع را ذخیره نمی‌کردند.

## اصلاح v28.7
- `crm/procurement-link.js` افزوده شد.
- تطبیق با اولویت زیر انجام می‌شود:
  1. `pcode` / `prodCd` / شناسهٔ پایدار;
  2. نام نرمال‌شده + مدل + مشخصات + واحد.
- تطبیق مبهم یا ناموجود **هرگز** اولین ردیف/نام مشابه را انتخاب نمی‌کند.
- در نبود تطبیق قطعی، مقدار خرید/استعلامی در ماتریس نمایش داده یا در سود اعمال نمی‌شود.
- خریدهای جدید `sourcePcode` و `sourceItemKey` ذخیره می‌کنند.
- قیمت مرجع در محصول مبهم بدون تغییر باقی می‌ماند و هشدار ثبت می‌شود.
- گزارش فقط‌خواندنی «تطبیق سراسری خرید» افزوده شد.

## رفتار دادهٔ موجود
- هیچ CO، RFQ، خرید واقعی یا قیمت مرجع قدیمی migrate یا اصلاح خودکار نشده است.
- این تصمیم عمدی است: اصلاح خودکار دادهٔ قدیمی می‌تواند یک نگاشت اشتباه جدید بسازد.
- پیشنهادهای مبهم باید با گزارش تطبیق توسط کاربر بررسی شوند.

## UAT و بررسی دادهٔ موجود
1. با نقش دارای دسترسی خرید، به صفحه پیشنهادها بروید.
2. دکمه **🔎 تطبیق سراسری خرید** را بزنید.
3. هر پیشنهاد دارای «قلم نیازمند تطبیق» را باز کنید.
4. در گزارش جزئی، کد کالا، نام، مدل، مشخصات و واحد را با خرید واقعی/RFQ مقایسه کنید.
5. تا زمانی که تطبیق قطعی نیست، عدد Optimizer را مبنای تصمیم سود، سفارش یا پرداخت قرار ندهید.
6. CO نمونهٔ تصویر را باز کنید؛ ارقام ۳۵ و ۱۴ میلیارد فقط در صورتی باید نمایش داده شوند که همان قلم با resolver قطعی تطبیق شده باشد.

## تست‌ها
- `tester161-v287-procurement-line-integrity.js`: 18 PASS / 0 FAIL
- `tester162-v287-optimizer-safe-mapping.js`: 5 PASS / 0 FAIL
- `tester72-v152.js`: 30 PASS / 0 FAIL

## مورد باز
پاک‌سازی/تطبیق داده‌های legacy یک فعالیت جدا و فقط‌خواندنی است. اجرای migration احتمالی منوط به dry-run، گزارش اثر، backup و تایید صریح کارفرماست.
