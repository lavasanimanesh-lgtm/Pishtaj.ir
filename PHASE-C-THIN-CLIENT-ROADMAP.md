# نقشهٔ راه فاز C — «عین بانک»: کلاینت نازک کامل

تاریخ: ۱۴۰۵/۰۶/۵ (۲۰۲۶-۰۸-۲۷) | مبنای شروع: v34.8.11

## تعریف «عین بانک» (معیار پذیرش هر فاز)
1. **هیچ نوشتنی در مرورگر منبع نمی‌شود** — هر تغییر = یک فرمان اتمیک به سرور (مثل `POST /transfer` بانک)
2. **هیچ خواندنی به کل دیتاست وابسته نیست** — کوئری/صفحه‌بندی سمت سرور؛ کش مرورگر کاملاً جایگزین‌پذیر
3. **حذف کش مرورگر = صفر خطا** — مثل purge کردن صفحهٔ بانک
4. آفلاین: فقط outbox فرمان‌ها (پول نقد بانک هم آفلاین نیست؛ نوشتنِ آفلاین اختیاری و محدود)

## وضعیت فعلی (پس از v34.8.11)
- ✅ سرور مالک مالی است (فرمان‌های حقوق/برداشت/تزریق + merge محافظت‌شده + verbatim convergence)
- ✅ حالت سرور-محور خودکار فعال؛ localStorage سبک؛ کش سنگین در IndexedDB؛ پول دلتا
- 🟡 باقی ماژول‌ها هنوز «کل مجموعه» را push می‌کنند (با merge سروری) — یعنی کلاینت هنوز چاق است
- 🟡 دو موتور سینک (legacy + فاز B) هم‌زمان زنده‌اند

---

## فاز C1 — اندازه‌گیری (۱ روز) ✦ پیش‌نیاز همهٔ فازها
- تله‌متری سبک: شمارش/حجم push به تفکیک کلید (audit موجود کافی است؛ فقط تجمیع)
- خروجی: فهرست ۱۰ مجموعهٔ داغ (پیش‌بینی: offers، invoices، deals، customers، rfqs، suppliers)
- اجرای `finance_repair_plan` روی دادهٔ واقعی پروداکشن + تصمیم canonical ها (بدهی داده را قبل از مهاجرت ببندیم)

## فاز C2 — زیرساخت فرمان عمومی (۳–۵ روز)
- تعمیم الگوی موفق sales-domain به همهٔ موجودیت‌ها:
  - `entity_upsert` / `entity_delete` با: idempotencyKey، journal رسیدها (موجود)، projection اتمیک (موجود برای opex/sharetx — تعمیم یابد)، role-guard per-entity، validation per-collection
- API کلاینت: `ptfEntitySave('offers', record)` جایگزین `setData('ptf_crm_offers', wholeArray)`
- ویژگی-پرچم per-module (فعال/غیرفعال بدون ری‌دپلوی)

## فاز C3 — مهاجرت ماژول‌به‌ماژول (۲–۴ هفته، تدریجی و بدون Big-Bang)
ترتیب پیشنهادی بر اساس درد:
1. `offers` (پرخطرترین — duplicate-shield الان فعال است؛ فرمان‌محورش کنیم سپر حذف می‌شود)
2. `invoices` + `case_receipts`
3. `customers` / `suppliers` / `rfqs`
4. ماژول‌های سبک باقی‌مانده
- هر ماژول: نوشتن=فرمان؛ خواندن موقتاً همان کش؛ تستر اختصاصی + امکان بازگشت
- **سود از هفتهٔ اول:** نوار زرد و تعارض برای آن ماژول برای همیشه حذف می‌شود

## فاز C4 — خواندن سرور-محور (۱–۲ هفته)
- endpoint جدید: `collection_query` با فیلتر/صفحه‌بندی/مرتب‌سازی سمت سرور (روی sync-store یا MySQL)
- `getData` برای لیست‌های بزرگ → کوئری سروری + کش صفحهٔ جایگزین‌پذیر
- bootstrap دستگاه جدید = دیگر دانلود کامل دیتاست نیست

## فاز C5 — بازنشستگی موتور قدیمی (۲–۳ روز)
- شرط: همهٔ ماژول‌ها فرمان‌محور + فاز B ≥ ۲ نسخه روی همهٔ دستگاه‌ها
- حذف pushDirty/pullCheck از sync.js (یک موتور، یک حقیقت)
- کاهش ~۲۰۰۰ خط منطق conflict که همین هفته دردسرش را دیدیم

## فاز C6 — سخت‌گیری بانکی (اختیاری، ۲–۳ روز)
- TTL نشست کوتاه‌تر (مثلاً ۸ ساعت به‌جای ۷ روز) + refresh بی‌حساس
- ابطال فوری همهٔ نشست‌های یک کاربر (ادمین) — revoke per-token موجود است؛ bulk اضافه شود
- 2FA پیامکی برای نقش‌های مالی (زیرساخت SMS موجود است)
- Rate-limit فرمان‌ها per-user (برای login هست؛ برای فرمان‌ها اضافه شود)

---

## ریسک‌ها و قواعد
- **بدون Big-Bang:** هر فاز مستقل شیر می‌شود و قابل بازگشت است؛ پروداکشن هرگز بیش از یک فاز جلوتر از تأیید کارفرما نمی‌رود
- **اول داده، بعد مهاجرت:** C1 باید داده‌های خراب تاریخی (duplicate حقوق/اوپکس) را ببندد وگرنه فرمان‌ها همان زباله را منتقل می‌کنند
- آفلاین‌نویسی محدود به outbox فرمان = همان تجربهٔ فعلی، بدون مجموعه‌پوش
- تخمین کل با ریتم ایجنت: ~۴ تا ۸ هفته؛ ارزش از هفتهٔ اول تزریق می‌شود

## اقدام فوری مستقل از فاز C (همین هفته)
1. PR هفت رفع v34.8.5→v34.8.11 به مین + دیپلوی پروداکشن (کاربران واقعی الان روی باگ‌های قدیمی‌اند)
2. اعمال پچ گیت php -l روی workflow ها (دستی، با حساب کارفرما)
3. بستن PR های باز منسوح #75/#76
