# ارزیابی جامع هاب مالی — پیشگیری از باگ‌های دیگر

- تاریخ: ۱۴۰۵/۰۶/۱۹ (2026-09-10)
- شاخه: `arena/01a08c54-pishtaj-ir` — مبنا `7d84f1b`
- نسخه CRM: `v34.38.19`
- هدف: پس از اصلاحات حقوق (B1/B2/B3 + گاردهای دوباره‌شماری + آشکارساز ماه‌های غایب)، یک بازبینی کاملِ هاب مالی انجام شود تا باگ دیگری حادث نشود و هر ریسکِ باقی‌مانده مستند و اولویت‌بندی شود.

---

## ۱) نقشهٔ هاب مالی (آنچه بررسی شد)

### ۱.۱ تب‌ها و ماژول‌ها — `crm/financehub.js`
| تب | id | ماژول/رندر | نقش‌ها |
|---|---|---|---|
| تنخواه | petty | `petty.js` | مدیران + حسابدار + تنخواه‌گردان |
| هزینه جاری | opex | `opex.js` | `ROLES.finance` |
| سهامداران | share | `shareholders.js` | مدیران ارشد |
| سال مالی | fiscal | `fiscal.js` | admin/chairman/ceo/commercial |
| ارزش افزوده | vat | `official-invoice`/VAT | مالی |
| حساب تأمین‌کنندگان | supacc | `supplier-finance.js` | مالی |
| حساب مشتریان | custacc | `customer-finance.js` | مالی |
| گزارش تجمیعی مالی | workcap | `working-capital.js` | مدیران |
| تراز رسمی/غیررسمی | ledger | `ledger-report.js` | مدیران ارشد |
| خزانه/بانک | treasury | `treasury.js` | مدیران |
| پورسانت فروش | commission | `commission.js` | مالی |
| کیفیت داده | quality | `data-quality.js` | مدیران |
| چک‌ها | cheque | `cheque-panel.js` | مالی |

### ۱.۲ کلیدهای دادهٔ مالی و مالکیت
| کلید | مالک (authority) | مصرف‌کننده‌ها |
|---|---|---|
| `ptf_crm_sharetx` | سرور (`sales-domain.php`؛ فرمان‌های دامنه‌ای) | shareholders/fiscal/treasury |
| `ptf_crm_opex` | سرور (تکرارشونده/حقوق) + کلاینت (دستی، با `_opexRowId`) | fiscal/treasury/finance-write-guard |
| `ptf_crm_petty` | کلاینت (تنخواه) | fiscal/treasury/shareholders |
| `ptf_crm_petty_tx` | کلاینت | treasury (فقط charge) |
| `ptf_crm_settings` (opexTpl, pettyTreasurerRole) | کلاینت، سینک | opex/petty |
| `ptf_crm_fiscal_snapshots` | کلاینت (قفل/کف/اصلاحی/افتتاحیه) | fiscal/treasury/working-capital |
| `ptf_crm_supplier_finance` (localStorage) | کلاینت تأمین | fiscal/data-quality/supplier-finance |

---

## ۲) نامتغیرهای بین‌ماژولی (قواعد «یک ریال یک‌بار»)

| # | نامتغیر | وضعیت | شاهد |
|---|---|---|---|
| I1 | هزینهٔ لینک‌شده به پرونده (`dealRef`) فقط از مسیر «هزینهٔ مستقیم پروژه» کسر می‌شود، نه از `ox.total`/`pettyStandalone` | ✅ سالم | `ptfOpexSumFiscal` (فقط unlinked)، `fiscalPettyStandalone` (`p.dealRef` رد می‌شود)، `fiscalDirectProjectCosts` |
| I2 | حقوق سهامدار = تعهد (opex+sharetx) از `totalCash` خارج و فقط سود را کم می‌کند | ✅ سالم + تقویت‌شده | `isShareholderSalaryOpex`، `ptfOpexSumFiscal.totalSalary/totalCash` |
| I3 | پرداخت حقوق (draw) یک‌بار به‌عنوان خروج نقدی (B2) و از `advYear` خارج (B1) | ✅ اعمال شد | `fiscal.js` `salaryPaid` + فیلتر `advRows` |
| I4 | کارمزد فاکتور پوششی در `net` از خود فاکتور (`coverNetBenefit`) و در نقد از ردیف‌های تسویه‌شده (`out.coverCommission`) | ✅ سالم | `fiscalCoverTotals` + حلقهٔ opex cover در `ptfFiscalCashData`؛ `ptfOpexSumFiscal` cover را رد می‌کند |
| I5 | چکِ صادرهٔ لینک‌شده به opex دوبار (چک + هزینه) شمرده نمی‌شود | ✅ سالم | `out.independentCheques` از `opexRowIds` می‌گذرد؛ treasury هم opex با chequeCd را رد می‌کند |
| I6 | افتتاحیهٔ نقد از یک منبع واحد (ورودی دستی گزارش تجمیعی) خوانده می‌شود | ✅ سالم | `fiscal` و `treasury` هر دو از `ptfFinanceOfficialData().opening.cash_bank` (با fallback در treasury) |
| I7 | ثبت مالیِ سال قفل‌شده مسدود است | ✅ سالم | `ptfFinanceAssertWritable` + `sd_is_locked` + گاردهای پنل |
| I8 | سند حذف‌شده با سنگ‌قبر ماندگار است و خودکار زنده نمی‌شود | ✅ سالم | `sd_recurring_explicit_tombstone`، `ptfOpexDel`، `sd_recurring_activate` |

---

## ۳) یافته‌ها (ریسک‌های باقی‌مانده)

### F-1 [متوسط] ناهم‌زمانی سال مالیِ «شارژ تنخواه» و «هزینهٔ تنخواه»
- **شرح:** `fiscalPettyStandalone` همهٔ رکوردهای `ptf_crm_petty` (بدون void و بدون dealRef) را در `net` همان سال می‌آورد؛ اما خزانه (`treasury.js`) فقط «شارژ» (`ptf_crm_petty_tx` نوع charge) را خروج نقدی می‌شمارد و هزینهٔ واقعی (direct/settle) را گردش داخلی می‌داند.
- **ریسک:** اگر تنخواه در سال X شارژ شود و هزینه در سال Y انجام شود، خروج نقد در X و هزینه در Y ثبت می‌شود → اختلاف مقطعی بین `cashEnd` و `net` در مرز سال‌ها.
- **وضعیت:** مدل مستند و عمدی است («خزانه = گردش بانک/صندوق مرکزی، نه گردش داخلی تنخواه» — `treasury.js:220`). نیازی به تغییر ندارد ولی باید در آموزش/گزارش شفاف بماند. **پیشنهاد:** در گزارش سال مالی، سطر «تنخواهِ شارژشده ولی مصرف‌نشده» به‌صورت اطلاعی نمایش داده شود (فقط‌خواندنی).

### F-2 [کم] اختلاف عددی پنل هزینه جاری با عدد سال مالی
- **شرح:** `ptfOpexSum` (جمع پنل) ردیف‌های کارمزد پوششی (`coverInvoiceCd`) را شامل می‌شود؛ `ptfOpexSumFiscal` آن‌ها را رد می‌کند (چون از خود فاکتور لحاظ می‌شوند). در نتیجه جمع پنل و «هزینهٔ جاری» سال مالی با هم برابر نیستند.
- **ریسک:** سردرگمی کاربر (نه باگ محاسباتی). کامنت کد (`opex.js:180`) آن را مستند کرده است.
- **پیشنهاد:** برچسب توضیحی کوچک در پنل هزینه («کارمزد پوششی جدا از سود سال لحاظ می‌شود») — فقط متن.

### F-3 [کم] «قالب تکرارشونده» برای حقوق پرسنل غیرسهامدار همچنان بدون cross-check سروری است
- **شرح:** گاردهای write-time این جلسه (کلاینت) شکاف «دستی↔قالب» و «قالب↔تنخواه» را می‌بندند، اما **سرور** `reconcile_recurring_opex` وقتی قالب را ماده‌سازی می‌کند، ردیف دستیِ هم‌ماه/هم‌دسته/هم‌مبلغ را cross-check نمی‌کند. اگر دو کاربر/دو دستگاه هم‌زمان عمل کنند (یا کاربر هشدار را رد کند)، ردیف دوم روی سرور ساخته می‌شود.
- **ریسک:** دوباره‌شماریِ نادر (با فرکانس کم).
- **پیشنهاد (نیازمند تأیید):** در سرور، هنگام ماده‌سازی قالب، اگر ردیف active غیرقالبی هم‌ماه/هم‌دسته/هم‌مبلغ موجود بود، نتیجهٔ projection با `warning:'possible_duplicate'` برگردد (بدون void) — مطابق بخش ۳.۲-۲ RCA قبلی.

### F-4 [متوسط — نیازمند تصمیم] «مبنای شروع حقوق» ثبت نمی‌شود
- **شرح:** هیچ فیلد `eligibilitySince/dutySince` روی سهامدار نیست؛ آشکارساز `ptfShareholderSalaryGaps` (این جلسه) به fallback «اولین ادعای active» تکیه می‌کند. برای جبرانِ دقیقِ ماه‌های غایب (به‌ویژه سهامدارانی که میانهٔ سال duty شده‌اند) مبنای قابل‌اتکا لازم است.
- **پیشنهاد (نیازمند تأیید):** ثبت `eligibilitySince` هنگام فعال‌شدن `duty` + فرمان backfill کنترل‌شده — مطابق بخش ۲.۲ RCA قبلی.

### F-5 [اطلاع] پوشش تستی
- گیت CI (`run-ci-gate.js`) حالا شامل: `tester499/507/508/509/510` (حقوق سروری)، `tester612` (نوع سند حقوق)، `tester613` (گاردهای دوباره‌شماری)، `tester628` (خروج نقدی حقوق B1/B2/B3)، `tester629` (گاردهای جدید + ماه‌های غایب). تسترهای `tester290/301/381` (نقد/توزیع) خارج از گیت CI مانده‌اند و `tester301` یک FAIL پیشین (سلکتور سال) دارد که ربطی به این تغییرات ندارد.

---

## ۴) چک‌لیست «باگ دیگری حادث نشود» — وضعیت

| حوزه | بررسی | نتیجه |
|---|---|---|
| حقوق سهامدار: ثبت تکراری | idempotency `recurringKey` + `alreadyRegistered` | ✅ بسته |
| حقوق سهامدار: ماه غایب | `ptfShareholderSalaryGaps` + نمایش کیفیت داده | ✅ بسته (read-only) |
| حقوق سهامدار: پرداخت تکراری | گارد تسویهٔ تک‌منبعی + `ptfSharePaySalary` یک draw می‌سازد | ✅ بسته |
| پرداخت حقوق در نقد | `salaryPaid` در outflowsTotal + تفکیک paid/unpaid | ✅ بسته |
| قالب تکراری (شرح متفاوت) | آستانهٔ `opexTplDuplicateActive` به cat+amt | ✅ بسته |
| دستی + قالب هم‌ماه | گارد ساخت/ویرایش قالب در برابر دستی/تنخواه | ✅ بسته (کلاینت) |
| دستی + تنخواه هم‌مبلغ | `clashPetty` در ثبت دستی | ✅ بسته |
| تسویهٔ خواهرِ هم‌مبلغ | `settledSibling/settledPettyTwin` confirm | ✅ بسته |
| قالب ثبت‌نشده (سکوت) | اعلان `finishLocalRecurring` | ✅ بسته |
| دوباره‌شماری سروری قالب | cross-check سروری | ⚠️ باز (F-3، نیازمند تأیید) |
| مبنای شروع حقوق برای جبران | `eligibilitySince` + backfill | ⚠️ باز (F-4، نیازمند تأیید) |
| شارژ/مصرف تنخواه در مرز سال | گزارش اطلاعی | ⚠️ پیشنهادی (F-1) |

---

## ۵) خلاصه و توصیه

1. **همهٔ گاردهای کم‌ریسک این جلسه اعمال شد** و با `tester629` در گیت CI قفل شد؛ ۸ تستر مالی موجود سبز ماندند.
2. **دو ریسک باقی‌مانده نیازمند تصمیم کارفرما هستند** (بدون تغییر خودکار، مطابق خط‌مشی مخزن): cross-check سروری قالب (F-3) و `eligibilitySince` + backfill (F-4).
3. **F-1/F-2 صرفاً شفاف‌سازی نمایشی** هستند و تغییر محاسباتی ندارند.
4. هیچ فرمول `netProfit` تغییر نکرده است؛ تغییرات این جلسه فقط لایهٔ نقدی/نمایشی و گاردهای تأیید انسانی بوده‌اند.

> مرجع‌های مرتبط: `ARENA-REVIEW-SALARY-LOGIC-2026-09-10.md` (بررسی اولیه)، `ARENA-RCA-SALARY-MONTH-GAP-DOUBLE-OUTFLOW-2026-09-10.md` (ریشه‌یابی دو باگ).
