# tester625-v34.38.17-orphan-won-archived.js — وضعیت SUPERSEDED (v34.38.18)

**تاریخ بازنشستگی:** ۲۰۲۶-۰۹-۱۰ · **جانشین:** `tester626-v34.38.18-orphan-archived-not-finding.js`

**چرا بازنشسته شد:** این تستر قرارداد v34.38.17 را پین کرده بود — «به‌جای orphan_won بحرانی،
یک یافتهٔ اطلاعی `orphan_won_archived` صادر شود که در کادر بالای فهرست پیشنهادها (و باکس فروش
تا وصول/کیفیت داده) با عنوان «🗄 پیشنهاد برنده به پروندهٔ بایگانی/مختومه متصل است» نمایش داده می‌شود».

کارفرما در ۲۰۲۶-۰۹-۱۰ گزارش داد:

> در قسمت پیشنهادات، در کادر بالای رکوردهای پیشنهاد، پیشنهادهایی که به پروندهٔ بایگانی‌شده
> متصل هستند به‌صورت یافته نمایش داده می‌شوند؛ درحالی‌که نباید نمایش داده شوند و یافته محسوب نمی‌شوند.

بر این اساس، قرارداد تغییر کرد (v34.38.18):

- جاروی `ptfSalesIntegrityScan` و گزارش مهاجرتِ سرور (`sd_migration_report`) برای چنین
  پیشنهادی **هیچ** یافته‌ای صادر نمی‌کنند (نه بحرانی، نه اطلاعی) — تشخیص پیوند به بایگانی فقط
  برای سرکوب `orphan_won` بحرانیِ کاذب باقی می‌ماند.
- کادر بالای فهرست پیشنهادها، باکس «فروش تا وصول»، راهنمای یافته‌ها و رجیستری fin_findings سرور
  دیگر هیچ ردیفِ `orphan_won_archived` ندارند.
- رفتار `orphan_won` بحرانی برای یتیمِ واقعی (بدون پروندهٔ فعال و بدون بایگانیِ قابل‌اثبات، یا
  بایگانی با هویت متفاوت) دست‌نخورده باقی است.

**مرجع قرارداد قبلی (historical):** نسخهٔ .js این تستر حذف شد چون با قرارداد جدید همیشه قرمز
می‌شد؛ متن کامل آن در تاریخچهٔ گیت (commit پدرِ این سند) موجود است.
