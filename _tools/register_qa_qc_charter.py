import os, re

repo = "/home/user/pishtaj_project"
ho_path = os.path.join(repo, "PTF-MASTER-HANDOVER.md")
with open(ho_path, "r", encoding="utf-8") as f: ho = f.read()

qa_qc_section = """
---

## 🛡️ ۹. هیات ارزیابی و تضمین کیفیت ارشد نرم‌افزار (Senior QA/QC Expert Panel) و پروتکل ۷مرحله‌ای پذیرش تسک‌ها
مطابق دستور صریح و حاکمیتی کارفرما (مدیریت کلان پروژه)، به منظور تضمین کیفیت صنعتی و جلوگیری از هرگونه رگرسیون یا ادعای بدون آزمون، **«تیم تخصصی ارشد تضمین و کنترل کیفیت نرم‌افزار (Senior QA & QC Engineering Panel)»** تشکیل گردید.

از این لحظه به بعد، هیچ تسک، باگ یا استوری جدیدی نباید با وضعیت **`Done`** یا **`Fixed`** بسته شود مگر اینکه گزارش مستند آن دقیقاً حاوی ۷ بخش حاکمیتی زیر باشد. در صورتی که هر یک از این موارد (یا تست‌ها) تکمیل نشده باشد، وضعیت تسک الزاماً باید **`In Progress`** باقی بماند:

### ۱. تحلیل ریشه‌ای علت (Root Cause Analysis - RCA)
- توضیح دقیق و مهندسی اینکه باگ یا اختلال دقیقاً چرا رخ داده است، در کدام ماژول، خط کد، یا منطق معماری نقص وجود داشته و مکانیسم بروز خطا چگونه بوده است.

### ۲. لیست فایل‌های تغییریافته (Files Changed)
- اعلام لیست دقیق و کامل تمامی فایل‌های بک‌اند (`PHP`)، فرانت‌اند (`JS/CSS/HTML`) یا مستنداتی که برای انجام تسک ویرایش یا ایجاد شده‌اند.

### ۳. تحلیل دامنه تأثیر (Impact Analysis)
- بررسی و توضیح دقیق اینکه تغییرات اعمال‌شده چه بخش‌ها، پنل‌ها، گردش‌های کاری (`Workflows`) یا ماژول‌های دیگری از سیستم CRM را ممکن است تحت تأثیر قرار دهد و چگونه از بروز عوارض جانبی پیشگیری شده است.

### ۴. نتایج تست رگرسیون (Regression Test Results)
- قبل از اعلام اتمام کار، اجرای تمامی سناریوهای مرتبط با تسک در هارنس تست (`_tools/uat/`) و ممیز سراسری (`audit.py`) و ارائه گزارش تفکیکی و دقیق از نتایج PASS / FAIL هر سناریو.

### ۵. مدارک و شواهد قابل استناد (Verifiable Evidence)
- ارائه لاگ‌های اجرایی، خروجی‌های ترمینال (`CLI Output`)، اسنپ‌شات وضعیت دیتابیس یا شواهد اجرای هارنس تست که اثبات کند کد به درستی عمل می‌کند.

### ۶. مراحل گام‌به‌گام راستی‌آزمایی توسط کارفرما (Verification Steps)
- نگارش دستورالعمل گام‌به‌گام و شفاف جهت تست و بررسی توسط کارفرما/مدیر سیستم در محیط کاربری وب، تا ایشان بتواند با طی کردن آن مراحل، رفع باگ یا عملکرد صحیح قابلیت را شخصاً تأیید کند.

### ۷. اخذ تأییدیه پیش از ریفکتور یا تغییر معماری (Prior Approval for Refactoring / Architectural Changes)
- اگر برای رفع یک باگ یا پیاده‌سازی یک قابلیت نیاز به بازنویسی گسترده (`Refactor`)، تغییر ساختار جداول/آرایه‌های داده یا تغییر در معماری اصلی سیستم باشد، ایجنت توسعه‌دهنده موظف است **قبل از اعمال هرگونه تغییر کد**، ابتدا طرح معماری و دلایل ریفکتور را به صورت رسمی به کارفرما ارائه نموده و پس از اخذ تأییدیه کتبی اقدام به کدنویسی نماید.
"""

if "هیات ارزیابی و تضمین کیفیت ارشد نرم‌افزار" not in ho:
    ho = ho.replace("---", qa_qc_section + "\n---", 1)

with open(ho_path, "w", encoding="utf-8") as f: f.write(ho)
print("Updated PTF-MASTER-HANDOVER.md: Registered Senior QA/QC Panel & 7-Gate Protocol!")
