# یادداشت‌های انتشار — v34.8.47 (هات‌فیکس)

**موضوع:** رفع حلقهٔ بی‌نهایت بازسازی نشست (HOTFIX-SESS-STORM) — کاربر واقعی روی استیجینگ از ورود بازماند.
**تاریخ:** ۲۰۲۶-۰۸-۲۹ · **تستر قفل‌کننده:** `tester548-v34.8.47-hotfix-sess-storm.js` (۱۶ سنجه · سبز)

## علامت و ریشه
- **علامت:** ورودِ موفق (`auth_login` → 200 + توکن) ولی بازگشت به صفحهٔ ورود با پیام «نشست سرور یا توکن معتبر وجود ندارد» + **هزاران درخواست `action=role_verify`** در Network بدون رفرش.
- **ریشه:** در مرورگرِ با `sessionStorage` مسدود (پنجرهٔ خصوصی/تنظیم دادهٔ سایت)، `ptfAuthLoginWrite` بی‌صدا شکست می‌خورد (try/catch ساکت) → `showCrm` نشست نمی‌بیند → کوکی معتبر هست → `ptfAuthSessionRestore` → ذخیره در SS باز می‌اندازد → `showCrm` ↔ restore **بی‌نهایت**. رشتهٔ رویداد در v34.8.43 (انتقال نشست به SS) نهفته بود و با ورود واقعی فعال شد.

## fix (crm/rbac.js + crm/index.html)
1. **fallback ذخیره:** اگر نوشتن SS بیندازد، توکن/نشست در `localStorage` نوشته می‌شود (`ptfAuthToken`/`ptfAuthSession` هر دو LS را می‌خوانند) + toast هشدار «مرورگر شما اجازهٔ ذخیرهٔ نشست نمی‌دهد…».
2. **سقف حلقه:** `showCrm` حداکثر ۳ تلاش بازسازی نشست در هر بارگذاری؛ پس از آن پیام شفاف «مرورگر شما ذخیرهٔ نشست را مسدود کرده…» — نه پیام گمراه‌کنندهٔ قبلی.
3. **پاک‌نکردن fallback:** وقتی SS در دسترس نیست، کلیدهای LS هرگز پاک نمی‌شوند.

## گیت‌ها
- CI ۱۶۴ تستر (tester548 جدید) — سبز؛ arch PASS؛ هم‌نسخگی کامل v34.8.47.

## تست دستی
- همان مرورگری که خطا می‌داد: ورود → باید مستقیم وارد شود (با یک toast هشدار ذخیره)؛ Network نباید بیش از ۲ `role_verify` داشته باشد.
