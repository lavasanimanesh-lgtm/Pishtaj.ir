# بررسی دقیق ماژول «ارزش افزوده فصلی» + سناریوها + سوالات پیش از اجرا

**تاریخ:** 2026-08-22  
**نسخه:** v34.7.88  
**وضعیت:** فقط بررسی/مستند — هیچ کدی تغییر نکرد.

---

## ۱. تعریف منطق موردنظر (طبق صحبت شما)

- **بدهی VAT** = جمع VAT فاکتورهای **رسمی فروش** (در بهار/تابستان/پاییز/زمستان).
- **اعتبار مالیاتی** = جمع VAT فاکتورهای **رسمی خرید**:
  - فاکتور خرید **واقعی**
  - فاکتور خرید **صورتی/پوششی**
- **تفاضل فصل** = بدهی − اعتبار
  - اگر **اعتبار > بدهی** → پرداختی ندارد؛ **اعتبار باقیمانده** به فصل بعد منتقل میشود.
  - اگر **بدهی > اعتبار** → **مانده پرداختی** باید تسویه شود.
- **تسویه**: وقتی بدهی تسویه شد، در فصل بعد **از صفر** محاسبه میشود.

---

## ۲. ساختار داده موجود (برای محاسبه)

### ۲.۱ فاکتور فروش رسمی — `ptf_crm_invoices`
- `isOfficial = true`، `isUnofficial = false`
- `base` / `baseAmountIRR` → مبلغ پایه
- `vatPercent` → درصد
- `vat` / `vatAmountIRR` → VAT
- date برای فصل: `invDate` یا `issueDate` یا `t`
- `status`/`st`/`voided` → برای تشخیص فعال بودن
- `caseId/offerNo/customer...` (برای اتصال به مشتری/پرونده)

### ۲.۲ فاکتور خرید — `ptf_crm_supplier_finance.invoices`
- `supplierCd`، `no`، `dateISO` (ISO)، `dateFa`
- `cur` (IRR/USD/…)، `rate`، `amount` (مبلغ ارز)، `amountIrr`
- `isOfficial` (true/false)
- `vatPct` و `vatAmount` (فاکتور رسمی واقعی)
- اگر `isCover === true`:
  - `coverVatPct` و `coverVatAmount`
  - `coverCommissionPct` و `coverCommissionAmount`
  - `coverNetBenefit` = VAT − کارمزد
  - `coverPeriod` = {year, season}
- `status`` = 'open'/'void' (فاکتور ابطالشده → حذف)

### ۲.۳ «فصل» و «سال»
- در `tax-returns.js` فصلها فارسی: `بهار/تابستان/پاییز/زمستان`.
- هیچ تابع آمادهای برای «تبدیل تاریخ → فصل» در منابع مالی دیده نشد.
- فاکتور خرید: `dateISO` / `dateFa` موجود.
- فاکتور فروش: `invDate` یا `issueDate` یا `t`.

---

## ۳. سناریوهای محتمل

### سناریو A — بدهی کمتر از اعتبار
| | | |
|---|---|---|
| فروش رسمی | VAT = **۱۰۰** | |
| خرید واقعی | VAT = **۸۰** | |
| خرید پوششی | VAT = **۲۰۰** | |
| **بدهی** | **۱۰۰** | |
| **اعتبار** | **۲۸۰** | |
| **نت** | **−۱۸۰** | یعنی اعتبار مازاد |
| نتیجه | پرداختی ۰ + **۱۸۰ به فصل بعد** | |

### سناریو B — بدهی بیشتر از اعتبار
| | | |
|---|---|---|
| فروش رسمی | VAT = **۳۰۰** | |
| خرید واقعی | VAT = **۵۰** | |
| خرید پوششی | VAT = **۱۲۰** | |
| **بدهی** | **۳۰۰** | |
| **اعتبار** | **۱۷۰** | |
| **نت** | **+۱۳۰** | بدهی پرداختنی |
| نتیجه | **۱۳۰ باید تسویه شود** | |

### سناریو C — انتقال اعتبار از فصل قبل (بسیار مهم)
- فصل ۱: اعتبار مازاد **۱۸۰** → به فصل ۲ منتقل.
- فصل ۲: بدهی **۲۰۰**، اعتبار (جاری) **۵۰**.
- دو حالت ممکن:
  - **اگر اعتبار فصل ۱ در فصل ۲ ست شود:** مانده فصل ۲ = ۲۰۰ − (۵۰ + ۱۸۰) = **−۳۰** → هیچ بدهی، +۳۰ به فصل ۳.
  - **اگر اعتبار فصل ۱ «تسویه» نشود و از صفر حساب شود:** مانده فصل ۲ = ۲۰۰ − ۵۰ = **+۱۵۰** پرداختنی.
- این رویکرد باید دقیقاً مشخص شود.

### سناریو D — «تسویه» چه معنی دارد؟
- وقتی بدهی را پرداخت میکنید، **آیا باید یک «record تسویه» ثبت کنید** که در فصل بعد از صفر شروع شود؟
- یا اگر مانده فصل قبلی **منفی/اعتبار** بود، خودکار به بعد منتقل و اگر مانده مثبت بود **باید دستی «تسویه شد» ثبت کنید**؟
- اگر تسویه ثبت نشود، آیا مانده بهطور نامحدود ادامه دارد؟

### سناریو E — فاکتورهای بدون تاریخ / تاریخ ناقص
- اگر `invDate`/`dateISO` خالی باشد، کدام فصل؟
  - گزینه ۱: نادیده گرفته+هشدار
  - گزینه ۲: دسته «بدون تاریخ»
  - گزینه ۳: خطا/بستن
- این را باید تعیین کنیم.

### سناریو F — فاکتور ارزی (USD/EUR/…) در خرید
- `vatAmount` ، `amountIrr` ، `rate` موجود. باید ریالیسازی شود:
  - `VAT_ریال = vatAmount × rate`؟ **یا** `vatAmount` خودش ریالی است؟
- فاکتور فروش رسمی همیشه ریالی است (`baseAmountIRR`/`vatAmountIRR`).
- **در خرید، `vatAmount = Math.round(amountIrr * vatPct/100)`** (از `amountIrr`). پس ریالی است.

### سناریو G — فاکتور ابطالشده
- فاکتور فروش `void`/`voided` → نباید در بدهی احتساب شود.
- فاکتور خرید `status==='void'` → نباید در اعتبار احتساب شود.
- اگر ابطال بعد از تسویه رخ دهد → مانده باید در فصل همان تاریخ ابطال جدا شود، یا در فصل بعد؟

### سناریو H — فاکتور واقعی خرید که VAT ندارد (`vatPct=0`)
- اعتبار = ۰. فقط VAT صفر است، نه «حذف».

### سناریو I — تغییر «تسویه» در میانه فصل
- آیا «تسویه» فقط در پایان فصل است؟ یا میتواند هر وقت ثبت شود؟

### سناریو J — داده قدیمی
- فاکتور فروش قدیمی فقط `amount` دارد و `vat`/`base` ندارد؛ VAT = ۰؟ 
- فاکتور خرید قدیمی فقط `amount` دارد و `isOfficial` ندارد → نادیده گرفته میشود یا بهعنوان «official known»؟

---

## ۴. سوالات پیش از اجرا (لطفاً پاسخ دهید — حدس نمیزنم)

1. **تعیین فصل از چه تاریخی؟**  
   - فروش: `invDate`؟
   - خرید: `dateISO`؟
   - اگر تاریخ خالی/نامعتبر باشد، چه کنیم؟

2. **انتقال اعتبار به فصل بعد چگونه است؟**  
   - اگر مانده فصل قبل **/** منفی باشد، خودکار به فصل بعد ست شود؟
   - اگر بدهی باقی بماند، آیا با ثبت «پرداخت»/«تسویه» در یک فصل جدید **از صفر** شروع میشود؟  
   - آیا باید یک «دکمه تسویه» کاربردی در تب باشد؟

3. **آیا فاکتور واقعی خرید که `vatPct=0` است، هیچ اعتباری بدهد؟** (۰)

4. **فاکتور پوششی خرید:**
   - اعتبار VAT = `coverVatAmount`؟
   - کارمزد فقط در هزینه جاری ثبت میشود و در تب VAT **نمیتواند خالص شود**؟ (یا باید صرفاً گزارش شود؟)

5. **رتوی ارزی ریالی:** فاکتور خرید ارزی، VAT ریالی = `vatAmount` موجود (ریالی) یا باید `amount × rate × pct`؟

6. **فاکتور ابطالشده / بدون تاریخ** — حذف یا دسته جدا؟

7. **پایان فصل = چه روزی؟** 
   - بهار: ۱۲/۰۱ تا ۱۲/۳۱؟ (تاریخ شمسی)
   - یا بهصورت ISO (ماه ۳/۶/۹/۱۲)؟
   - این روی تعیین فصل بسیار مؤثر است.

8. **آیا تب باید «فقط گزارش» باشد یا امکان ثبت «تسویه» هم داشته باشد؟**
   - برای محاسبه دقیق مانده، نیاز به ثبت تسویه را ارزیابی کنید.

9. **دسترسی:** فقط `admin/chairman/ceo/commercial/accountant` (مثل تب اظهارنامه)؟

10. **داده های غیررسمی فروش/خرید در تب دیده شوند؟**  
   - طبق سناریو، فقط رسمی. تأیید کنید.

---

## ۵. جمعبندی ارزیابی
- منطق موردنیاز شما **قابل محاسبه** است (VAT همه فاکتورها ذخیره است).
- **بزرگترین ابهام، «تسویه» و «انتقال فصل»** است.
- بدون مشخص کردن آن، نمیتوانم مانده فصل را درست محاسبه کنم.
- اگر پاسخها روشن شوند، پیادهسازی:

  - یک تب سوم «📊 ارزش افزوده فصلی» در پنل اظهارنامه (`tax-returns.js`)
  - محاسبه فقطخواندنی از:
    - `ptf_crm_invoices` (فروش رسمی)
    - `ptf_crm_supplier_finance.invoices` (خرید رسمی واقعی + پوششی)
  - نمایش: بدهی VAT / اعتبار / مانده / «پرداختنی» یا «انتقال به فصل بعد»
  - ثبت «تسویه» اختیاری (در صورت نیاز)

- **این درخواست فقط بررسی است؛ هیچ کدی تغییر نکرده.**
