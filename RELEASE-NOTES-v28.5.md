# ریلیزنوت `v28.5` — Hotfix اضطراری چاپ چندصفحه‌ای پیشنهاد

## مسئلهٔ گزارش‌شده
در قالب چاپ سربرگ، در صفحهٔ دوم و صفحات بعدی، سرستون‌های جدول زیر نوار طراحی سربرگ پنهان می‌شدند. همچنین شمارهٔ صفحه واقعی و آدرس در صفحهٔ پایانی پیشنهاد چندصفحه‌ای قابل اتکا نبود.

## تحلیل ریشه‌ای
- قالب `letterhead` از `@page margin:0` استفاده می‌کرد.
- نوار نارنجی بالای صفحه `fixed` بود.
- فقط `body` در صفحهٔ اول padding داشت؛ سرستون تکرارشوندهٔ جدول در صفحه‌های بعد از بالای فیزیکی صفحه شروع می‌شد و زیر نوار fixed قرار می‌گرفت.
- متن صفحه در سربرگ فقط `Page 1 of ...` بود و برای صفحات بعدی واقعی نبود.
- footer ثابت کنترل صریحی برای قرارگرفتن آدرس در آخرین صفحه نداشت.

## اصلاح
- برای همهٔ قالب‌ها، حاشیهٔ واقعی per-page رزرو شد تا هر `thead` تکرارشونده زیر سربرگ طراحی شود.
- اقلام پیشنهاد به بخش‌های صفحه‌بندی‌شدهٔ کنترل‌شده تقسیم می‌شوند.
- هر صفحه شمارهٔ واقعی `Page X of Y` دارد.
- برای پیشنهاد چندصفحه‌ای، شرایط، امضا و آدرس شرکت در صفحهٔ پایانی ایجاد می‌شوند.
- هر جدول صفحهٔ خودش را دارد و سرستون در تمام صفحات اقلام تکرار می‌شود.
- هیچ دادهٔ پیشنهاد، قیمت، اقلام یا ضمیمه‌ای تغییر نکرده است.

## تست‌ها
- `tester159-v285-offer-print-pagination.js`: **16 PASS / 0 FAIL**
  - ۱۳ قلم CO، سه صفحهٔ تولیدشده
  - کنترل header هر صفحهٔ اقلام
  - کنترل `Page 1 of 3`، `Page 2 of 3` و `Page 3 of 3`
  - کنترل آدرس فقط در صفحهٔ پایانی
  - کنترل هر پنج قالب چاپی
- parse همهٔ 67 اسکریپت: موفق.
- `audit.py`: بدون خطا؛ فقط یک هشدار قدیمی دربارهٔ حجم 5 تصویر.

### شفافیت تست تاریخی
`tester62-v142.js` همچنان **36 PASS / 3 FAIL** دارد. سه FAIL مربوط به تلفن/شرایط چاپی قدیمی در `offers.js` و `offers-pro.js` هستند که پیش از این hotfix وجود داشته‌اند و در این اصلاح تغییر نکرده‌اند. این موارد به‌عنوان baseline مستقل باقی می‌مانند.

## UAT پیشنهادی
1. یک CO دارای بیش از ۱۲ قلم را با قالب سربرگ چاپ/دانلود کنید.
2. در صفحهٔ دوم کنترل کنید header جدول کامل و زیر نوار سربرگ دیده می‌شود.
3. شمارهٔ همهٔ صفحات را کنترل کنید.
4. در صفحهٔ آخر، شرایط/امضا و آدرس PTF را کنترل کنید.
5. یک پیشنهاد تک‌صفحه‌ای و یک قالب غیرسربرگ را نیز آزمایش کنید.

## محدوده
این hotfix فقط print/pagination پیشنهاد است. Stage 0 و Sprint 1 امنیت/Workflow مالی را تغییر نمی‌دهد.
