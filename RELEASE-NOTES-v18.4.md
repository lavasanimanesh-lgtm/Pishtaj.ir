# 🛡 ریلیز v18.4 — سخت‌سازی مالی و محرمانگی R9

**اسپرینت:** Post-R9 Hardening  
**موارد:** BUG-024 + BUG-025 + US-426 فاز ۱

## ✅ BUG-024 — محرمانگی داشبورد سال مالی

### مشکل
در v18.1/v18.2 داشبورد سال مالی با hook روی پنل تنخواه رندر می‌شد، در حالی که پنل تنخواه برای همه کاربران قابل دسترسی است. بنابراین احتمال نمایش داده‌های محرمانه سال مالی برای کاربران غیرمجاز وجود داشت.

### رفع
- `fiscalHtml()` برای نقش غیرمجاز خروجی خالی برمی‌گرداند.
- `ptfFiscalRender()` برای نقش غیرمجاز هیچ کاری نمی‌کند.
- `ptfFiscalPrint()` guard نقش دارد.
- `ptfFiscalLock()` از قبل guard داشت و حفظ شد.
- نتیجه: role=sales/accountant/buyer هیچ متن/عدد سال مالی، سود خالص، سهم سهامدار یا snapshot در DOM نمی‌بیند.

نقش‌های مجاز:
- `admin`
- `chairman`

---

## ✅ BUG-025 — سخت‌گیری سال مالی و مطالبات باز

### مشکل
رکوردهای بدون تاریخ روشن می‌توانستند وارد سال مالی انتخابی شوند. همچنین مطالبات باز به‌صورت کل جمع می‌شد و تفکیک سال/کل شفاف نبود.

### رفع
- رکورد بدون تاریخ سال مالی دیگر وارد سود سال نمی‌شود.
- این رکوردها در فهرست «بدون تاریخ/نیازمند تعیین تکلیف» نمایش داده می‌شوند.
- مطالبات باز به دو عدد تفکیک شد:
  - مطالبات باز ایجادشده در همان سال مالی
  - مطالبات باز کل شرکت
- فاکتور باز بدون تاریخ نیز جداگانه گزارش می‌شود.
- UI قاعده سال را توضیح می‌دهد: تاریخ مختومه/برد/ثبت سند.

---

## ✅ US-426 فاز ۱ — گزارش رسمی سال مالی + ردیابی منبع اعداد

### اجرا شد
- تابع جدید/تکمیل‌شده:

```js
ptfFiscalReportHtml(d)
```

- `ptfFiscalPrint()` دیگر JSON خام نمایش نمی‌دهد.
- گزارش رسمی شامل جدول‌های زیر است:
  - منبع سود پروژه‌ها
  - هزینه‌های جاری
  - موارد ناقص/بی‌تاریخ
  - کاربرگ تقسیم سود

### هدف
هر عدد اصلی داشبورد سال مالی باید قابل ردیابی به منبع باشد.

---

## معماری
- منطق سود تکرار نشد.
- همچنان فقط از `ptfProjectProfitIRR` استفاده می‌شود.
- داده جدید ساخته نشد.
- تغییرات فقط در لایه محاسبه/نمایش/گارد `fiscal.js` انجام شد.
- Hook فعلی حفظ شد، اما خروجی برای نقش غیرمجاز صفر شد.

## فایل‌های تغییر یافته
- `crm/fiscal.js`
- `crm/index.html`
- `crm/sw.js`
- `_tools/uat/tester101-v184.js`
- اصلاح تسترهای قبلی:
  - `_tools/uat/tester99-v181.js`
  - `_tools/uat/tester100-v182.js`
- `RELEASE-NOTES-v18.4.md`
- `PTF-MASTER-HANDOVER.md`

## 🔬 کیفیت
- تستر جدید: `tester101-v184.js` با ۱۷ چک.
- رگرسیون کامل: **۸۸ تستر / صفر خطا**.
- Audit سایت: PASS با همان هشدار قدیمی تصاویر سنگین.

## صف بعدی پیشنهادی
بعد از سخت‌سازی فوری:
1. US-427 — سند اصلاحی پس از قفل سال مالی
2. US-428 — UI زنده درصد/مبلغ پیش‌پرداخت
3. US-429 — هاب مالی مدیریتی R9
4. BUG-026 — مقاوم‌سازی تسترهای نسخه‌محور
