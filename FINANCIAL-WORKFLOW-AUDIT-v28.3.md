# گزارش ممیزی فقط‌خواندنی گردش‌کار مالی
## Pishro Tajhiz Fartak CRM — مبنای سورس `v28.3`

**تاریخ ممیزی:** ۱۴۰۵/۰۴/۲۳  
**نوع ممیزی:** تحلیل سورس، مسیرهای داده، مجوزها و تست‌های fixture موجود؛ بدون تغییر داده یا اجرای migration  
**محدوده:** مشتریان، تأمین‌کنندگان، پرداخت، چک، تنخواه، سال مالی، گزارش رسمی، همگام‌سازی و ضمیمه‌های مالی  
**خارج از محدوده:** اعتبارسنجی حسابداری قانونی/مالیات و UAT روی دادهٔ واقعی کارفرما

> **تصمیم کنترل تغییر:** این گزارش هیچ اصلاحی اعمال نمی‌کند و برای این مرحله بستهٔ استقرار تولید نشده است. یافته‌های «قطعی» بر پایهٔ مسیرهای اجرایی سورس هستند؛ یافته‌های «نیازمند UAT» باید در محیط واقعی و با دادهٔ آزمایشی تأیید شوند.

---

## ۱. جمع‌بندی مدیریتی

### نتیجهٔ اصلی
سیستم در حال حاضر برای ثبت عملیاتی فروش، خرید، پرداخت و چک قابلیت‌های متعددی دارد، اما هنوز یک workflow مالی یکپارچه و امن برای کار چندکاربره نیست. پیش از شروع «گردش بانک و صندوق» یا هر توسعهٔ مالی جدید، چهار ریسک باید در اولویت صفر رسیدگی شوند:

1. **تفکیک/حریم چک شخصی و اختیار صدور چک شرکت** در سورس قابل اتکا نیست و در سناریوهایی می‌تواند به افشای داده یا حذف چک شخصی منجر شود.
2. **قفل سال مالی کامل نیست:** ویرایش و حذف‌های معینی در زیر‌دفتر تأمین‌کننده از guard قفل عبور می‌کنند.
3. **همگام‌سازی زیر‌دفتر تأمین‌کننده object-based است، اما Smart Merge فقط برای آرایه‌ها ادغام واقعی دارد؛** تعارض همزمان می‌تواند کل object را با نسخهٔ طرف دیگر جایگزین کند.
4. **مجوزهای مالی فقط در UI قابل اتکا نیستند؛** API نقش ارسالی از مرورگر را می‌پذیرد و `data_push` گارد نقش سمت سرور ندارد.

### وضعیت توصیه‌شده
**شروع قابلیت جدید مالی متوقف بماند** تا حداقل یافته‌های `FW-C01` تا `FW-C05` رفع، تست fixture و UAT چندکاربره انجام و تأیید شود.

---

## ۲. مدل داده و منابع حقیقت فعلی

| دامنه | کلید/منبع فعلی | نتیجهٔ ممیزی |
|---|---|---|
| فاکتور و وصولی مشتری | `ptf_crm_invoices` | منبع اصلی مطالبات؛ وصولی در آرایهٔ `payments`/`pays` همان فاکتور است. |
| تعهد خرید legacy | `ptf_crm_payables` | منبع قدیمی خرید اعتباری؛ با `sfInvoiceCd` از دوباره‌شماری کنار گذاشته می‌شود. |
| فاکتور/پرداخت تأمین | `ptf_crm_supplier_finance` | object شامل `invoices`، `payments` و `adjustments`؛ برای merge همزمان نقطهٔ پرریسک است. |
| چک شرکت/ثالث | `ptf_crm_cheques` | منبع شرکت، چک ثالث و بخشی از چک‌های قدیمی است. |
| چک شخصی پیشنهادی | `ptf_personal_cheques_<username>` | فقط localStorage مرورگر؛ در sync و backup استاندارد وجود ندارد. |
| قفل/تنظیم سال/افتتاحیه | `ptf_crm_fiscal_snapshots` | شامل snapshot، amendment، تنظیم مالی و افتتاحیه‌های جدید است. |
| تنخواه | `ptf_crm_petty`، `ptf_crm_petty_tx`، `ptf_crm_petty_periods` | گردش مستقل تنخواه؛ هنوز به بانک/دفترکل یکپارچه وصل نشده است. |

**اصل مطلوب:** مشتری، تأمین‌کننده و مانده‌های آن‌ها منبع دوم نداشته باشند. این اصل در بخش‌هایی رعایت شده، اما داده‌های افتتاحیه و adjustment نیازمند کنترل دوباره‌شماری دقیق‌تر هستند.

---

## ۳. ماتریس نقش‌ها و مجوزهای واقعی در سورس

> «واقعی» یعنی آنچه اکنون از guardها و مسیرهای اجرا دیده می‌شود؛ نه صرفاً آنچه سیاست کسب‌وکار انتظار دارد.

| عملیات | نقش/گیت فعلی | وضعیت / ابهام |
|---|---|---|
| مشاهدهٔ هاب مالی مدیریتی | فقط `admin` و `chairman` (`financehub.js:9`) | با تعریف RBAC که `ceo` و `commercial` را دارای `finance:true` می‌داند ناسازگار است. |
| قفل سال مالی | `admin` یا `chairman` (`fiscal.js:9`) | قابل قبول، ولی سایر سندها به‌طور کامل قفل نیستند. |
| بازکردن قفل سال | فقط `chairman` (`fiscal.js:11`) | منطق جدید است؛ UAT واقعی sync لازم دارد. |
| مشاهده/ثبت فاکتور و پرداخت تأمین | هر نقش با `buyPrice` یا `isSenior` (`supplier-finance.js:17-18`) | شامل `buyer` و همهٔ مدیران؛ جداسازی «ثبت فاکتور» از «اجازهٔ پرداخت/چک شرکت» وجود ندارد. |
| adjustment / افتتاحیه تأمین | `admin/chairman/ceo/commercial` | از فاکتور/پرداخت جدا شده، اما کنترل سال/سیاست افتتاحیه کامل نیست. |
| تنخواه | ثبت هزینه عمومی؛ خزانه‌دار قابل تنظیم؛ بستن دوره برای خزانه‌دار؛ ثبت حسابداری برای accountant/admin | نسبتاً ساختاریافته، ولی هنوز مستقل از بانک و قفل سال است. |
| فاکتور/وصولی مشتری | UI پنل `inv/recv` برای accountant/collector؛ `savePay` guard صریح نقش ندارد | نقش صرفاً در دسترس‌پذیری پنل است، نه کنترل قطعی سمت تابع/API. |
| ثبت چک عمومی | فرم برای همهٔ کاربران قابل ساخت است؛ انتخاب `company` در خود فرم قفل نشده | برخلاف قاعدهٔ کسب‌وکار «چک شرکت توسط رییس هیات‌مدیره». |
| ثبت چک شرکت از پرداخت تأمین | مسیر `company_cheque` بدون guard نقش مستقل | خریدارِ مجاز به تأمین می‌تواند به این مسیر برسد. |

---

## ۴. گردش‌کارهای ممیزی‌شده

### ۴.۱ مشتری: فاکتور → وصولی → مطالبات

1. حسابدار فاکتور مشتری را در `ptf_crm_invoices` ثبت می‌کند.
2. تحصیلدار/کاربر مجاز از `savePay()` وصولی را به `invoice.payments[]` اضافه می‌کند.
3. حساب مشتری و گزارش رسمی، مبلغ فاکتور منهای جمع وصولی‌ها را نمایش می‌دهند.
4. چک ثالث منتقل‌شده به تأمین‌کننده می‌تواند یک `payment` با `supplierPaymentCd` در فاکتور مشتری ایجاد کند.

**نقاط کنترل موجود:** سقف وصولی از مبلغ فاکتور بیشتر نمی‌شود (`rbac.js:634-635`).

**شکاف اصلی:** مسیر استاندارد و قابل ردگیری برای اصلاح/ابطال/حذف وصولی مشتری وجود ندارد؛ بند ۵، `FW-H01`.

### ۴.۲ تأمین‌کننده: فاکتور → تخصیص پرداخت → اعتبار/چک

1. فاکتور تأمین‌کننده در object `ptf_crm_supplier_finance.invoices` ثبت می‌شود.
2. پرداخت با ارز همسان ثبت و به یک/چند فاکتور یا payable legacy تخصیص می‌یابد.
3. باقیماندهٔ بدون تخصیص، اعتبار نزد تأمین‌کننده است.
4. پرداخت می‌تواند نقدی، بانکی، تهاتری، چک شرکت یا چک ثالث منتقل‌شده باشد.
5. ابطال پرداخت، تخصیص‌ها را برمی‌گرداند و برای چک مرتبط وضعیت را به void/voided_transfer می‌برد.

**نقاط کنترل موجود:** سقف تخصیص و مبلغ پرداخت کنترل می‌شود؛ legacy linked برای جلوگیری از دوباره‌شماری کنار گذاشته می‌شود.

**شکاف اصلی:** مجوز پرداخت/چک شرکت، قفل سال و sync object-based نیازمند بازطراحی کنترل‌شده هستند.

### ۴.۳ چک: شخصی، شرکتی و ثالث

- **هدف مصوب:** چک شخصی خصوصی و بی‌اثر بر شرکت؛ چک شرکت توسط رییس هیات‌مدیره؛ چک ثالث منتقل‌شده خارج از ید شرکت و بدون reminder.
- **پیاده‌سازی فعلی:** تلاش برای localStorage جداگانهٔ چک شخصی انجام شده، اما میان دادهٔ global قدیمی، دادهٔ local شخصی، batch، AI و مسیر پرداخت تأمین یک مدل واحد و enforce شده وجود ندارد.

### ۴.۴ سال مالی: lock → adjustment/unlock → sync

- snapshot قفل با `locked:true` ثبت می‌شود.
- بازگشایی جدید به `locked:false` با دلیل و timestamp تبدیل می‌شود.
- supplier finance برای save/void اولیه guard سال دارد.
- گزارش مالی از یک کلید مشترک برای snapshot، amendment، config و opening استفاده می‌کند.

**شکاف اصلی:** تمام مسیرهای edit/delete بعدی از guard عبور نمی‌کنند و بنابراین قفل «سراسری» نیست.

### ۴.۵ گزارش رسمی و سرمایه در گردش

- گزارش از مشتری، تأمین، legacy، چک شرکتی و افتتاحیه‌ها مشتق می‌شود.
- خود سورس به‌درستی تصریح می‌کند که گزارش جای دفترکل قانونی یا صورت جریان نقد بانک نیست.
- برخی سندهای بی‌تاریخ به هشدار کیفیت داده می‌روند؛ اما رفتار تاریخ در ماژول‌های مختلف یکسان نیست.

---

## ۵. یافته‌ها و ریسک‌ها

### جدول اولویت

| شناسه | شدت | وضعیت | عنوان |
|---|---:|---|---|
| FW-C01 | بحرانی | قطعی در سورس | امکان حذف/نشت چک شخصی در مدل partition فعلی |
| FW-C02 | بحرانی | قطعی در سورس | صدور/ثبت چک شرکت بدون enforce نقش رییس هیات‌مدیره |
| FW-C03 | بحرانی | قطعی در سورس | bypass قفل سال در edit/delete زیر‌دفتر تأمین |
| FW-C04 | بحرانی | قطعی در سورس | ریسک از دست رفتن همزمانی در object `supplier_finance` |
| FW-C05 | بحرانی | قطعی در سورس، مشروط به نبود HMAC اجباری سرور | اعتماد API به نقش ارسالی مرورگر و نبود گارد data push |
| FW-H01 | زیاد | قطعی در سورس | وصولی مشتری مسیر reversal/void استاندارد ندارد |
| FW-H02 | زیاد | قطعی در سورس | قفل سال مالی تنها بخشی از اسناد را حفاظت می‌کند |
| FW-H03 | زیاد | قطعی در سورس | ایجاد چک batch/AI بدون مالکیت صریح |
| FW-H04 | زیاد | قطعی در سورس | قواعد reminder/My Day با حریم چک شخصی هم‌سو نیست |
| FW-H05 | زیاد | قطعی در سورس | ناسازگاری تاریخ شمسی در موتور fiscal قدیمی |
| FW-M01 | متوسط | قطعی در سورس | نقش‌های مالی تعریف‌شده و UI هاب مالی با هم هم‌راستا نیستند |
| FW-M02 | متوسط | قطعی در سورس | تعریف‌های چندباره/override در supplier finance ریسک رگرسیون دارند |
| FW-M03 | متوسط | نیازمند UAT دادهٔ واقعی | امکان دوباره‌شماری افتتاحیهٔ تأمین با adjustment/افتتاحیهٔ رسمی |
| FW-M04 | متوسط | طراحی/دامنه | گزارش رسمی هنوز دفترکل قانونی یا تراز بانکی نیست |
| FW-M05 | متوسط | نیازمند UAT | بازگشایی قفل جدید در مرورگر/چنددستگاه هنوز UAT نشده است |

---

### FW-C01 — حذف یا نشت چک شخصی

**شواهد:**
- `chAll()` دادهٔ global (`ptf_crm_cheques`) را با personal key کاربر فعلی جمع می‌کند (`cheques.js:13-15`).
- `chSave()` فقط رکوردهای غیرpersonal را global ذخیره می‌کند و از personalها فقط مواردی را نگه می‌دارد که `by === current user` است (`cheques.js:20`).

**سناریوی بازتولید:**
1. یک personal cheque قدیمی در کلید global باقی مانده باشد و مالک آن کاربر A باشد.
2. کاربر B هر تغییری در چک‌ها انجام دهد و `chSave()` اجرا شود.
3. رکورد personal متعلق به A نه در global می‌ماند و نه در personal key کاربر B؛ در نتیجه از هر دو خروجی حذف می‌شود.

**پیامد:** خطر حذف دادهٔ شخصی، و پیش از اولین save نیز احتمال مشاهدهٔ رکورد قدیمی global توسط کاربر نامرتبط وجود دارد.

**کنترل پیشنهادی برای مرحلهٔ اصلاح:** طراحی migration/partition غیرمخرب، گزارش dry-run، نگهداری مالکیت و ممنوعیت هر save که personal غیرمتعلق را حذف کند. personal key باید تصمیم روشن برای sync/backup امن داشته باشد.

---

### FW-C02 — چک شرکت بدون enforce نقش

**شواهد:**
- فرم عمومی چک انتخاب `company` را به همه نشان می‌دهد و `chCollectForm()` مقدار را بدون کنترل نقش می‌پذیرد (`cheques.js:182-188, 237`).
- `canWrite()` تأمین با `buyPrice` یا `isSenior` است (`supplier-finance.js:17-18`) و مسیر `company_cheque` در پرداخت تأمین guard نقش جدا ندارد (`supplier-finance.js:151-177, 180-206`).

**پیامد:** کاربر خرید می‌تواند چک شرکت بسازد؛ این با سیاست مصوب «چک شرکت توسط رییس هیات‌مدیره صادر می‌شود» ناسازگار است.

**کنترل پیشنهادی:** enforce هم در UI و هم در تابع save و سمت سرور؛ ثبت چک شرکت باید chairman-only یا workflow درخواست/تایید باشد، نه صرفاً default فرم.

---

### FW-C03 — bypass قفل سال در edit/delete تأمین

**شواهد:**
- wrapperهای قفل فقط `slInvoiceSave`، `slPaymentSave`، `slInvoiceVoid` و `slPaymentVoid` را می‌پوشانند (`supplier-finance.js:292-299`).
- پس از آن، `slInvoiceEdit` و `slPaymentEdit` مستقیم داده را ذخیره می‌کنند (`320-321`).
- `slInvoiceDelete` نیز مستقیم invoice را void و allocationها را باز می‌کند (`330`)؛ guard سال ندارد.

**سناریوی بازتولید:** فاکتور یا پرداخت متعلق به سال قفل‌شده را از بخش «مدیریت فاکتور و پرداخت» edit/delete کنید.

**پیامد:** snapshot قفل‌شده و مفهوم closing مالی با سندهای عملیاتی بعدی ناسازگار می‌شود.

**کنترل پیشنهادی:** یک guard مرکزی، پیش از هر mutation در invoice/payment/legacy/adjustment، نه wrapperهای لایه‌ای و متأخر.

---

### FW-C04 — از دست رفتن داده در sync زیر‌دفتر تأمین

**شواهد:**
- `ptf_crm_supplier_finance` در `SYNC_KEYS` قرار دارد (`sync.js:19`).
- مقدار آن object با `invoices/payments/adjustments` است، نه آرایه (`supplier-finance.js:9-13`).
- `ptfSmartMerge()` اگر هر دو طرف آرایه نباشند، `remoteStr` را برمی‌گرداند (`sync.js:340-345`).

**سناریوی بازتولید:** کاربر A فاکتور تأمین و کاربر B پرداخت تأمین را همزمان، پیش از sync، ثبت کنند.

**پیامد:** در تعارض، کل object یکی از طرفین می‌تواند جایگزین دیگری شود؛ امکان از دست رفتن فاکتور، پرداخت یا adjustment وجود دارد.

**کنترل پیشنهادی:** merge نوع‌دار برای object supplier finance با ادغام per-array بر اساس `cd` و timestamp استاندارد؛ سپس تست دو مرورگر واقعی.

---

### FW-C05 — RBAC سمت سرور قابل اتکای کافی ندارد

**شواهد:**
- `verify_request()` در وضعیت فعلی فقط مقدار می‌سازد و enforcement آن کامنت شده است (`api/crm.php:9-20`).
- `$client_role` از header `X-CRM-Role` مرورگر گرفته می‌شود (`api/crm.php:226`).
- `data_push` برای داده‌های مالی با `verify_request()` اجرا می‌شود، اما `role_guard` اختصاصی ندارد (`api/crm.php:748+`).

**پیامد:** اگر HMAC اجباری در پیکربندی production فعال نباشد، کنترل UI نقش را نمی‌توان کنترل امنیتی قطعی دانست؛ داده و audit نیز سمت کلاینت قابل جعل/تغییر می‌شوند.

**کنترل پیشنهادی:** session/authentication واقعی سمت سرور، استخراج نقش از session معتبر و ACL per-action/per-key. تا آن زمان نقش‌های مالی صرفاً کنترل UX هستند، نه کنترل امنیتی.

---

### FW-H01 — وصولی مشتری قابل اصلاح/ابطال استاندارد نیست

**شواهد:** `savePay()` فقط وصولی را به `invoice.payments` اضافه می‌کند (`rbac.js:628-643`). مسیر edit/delete/void یا سند اصلاحی برای وصولی مشتری در سورس یافت نشد.

**پیامد:** وصولی اشتباه می‌تواند ماندهٔ مشتری، گزارش رسمی و تبدیل چک ثالث را مخدوش کند و کاربر را به بک‌آپ یا دستکاری داده سوق دهد.

**کنترل پیشنهادی:** event-based reversal با دلیل، timestamp، کاربر، حفظ تاریخچه و منع حذف فیزیکی.

---

### FW-H02 — قفل سال مالی سراسری نیست

**شواهد:** guard سال فعلاً در supplier finance محدود است. وصولی مشتری (`savePay`)، بسیاری از تغییرات فاکتور مشتری، تنخواه، هزینه جاری و برخی editهای supplier زیر یک gate مرکزی مشترک قرار نگرفته‌اند.

**پیامد:** حتی با قفل فعال، تغییر سند در برخی workflowها ممکن است و گزارش پایان سال از اسناد پس از قفل فاصله می‌گیرد.

**کنترل پیشنهادی:** registry واحد عملیات مالی و یک `assertFiscalWritable(date, operation)` برای تمام mutationها.

---

### FW-H03 — چک batch و AI مالکیت صریح ندارند

**شواهد:**
- رکوردهای batch در `chBatchCollect()` بدون `ownership` ساخته می‌شوند (`cheques.js:549-566`).
- رکورد AI نیز ownership صریح ندارد (`cheques.js:728 به بعد`).
- `chSave()` هر ownership غیرpersonal را در global قرار می‌دهد (`20`).

**پیامد:** چک نامشخص می‌تواند reminder بگیرد، در My Day دیده شود و با یک چک شرکتی اشتباه گرفته شود؛ هرچند گزارش رسمی v28.1 آن را وارد رقم شرکت نمی‌کند، workflow عملیاتی هم‌سو نیست.

**کنترل پیشنهادی:** ownership اجباری و معتبر در تمام سازندگان رکورد؛ batch فقط chairman/company یا personal explicit، و AI باید قبل از commit مالکیت را نمایش دهد.

---

### FW-H04 — reminder / My Day با حریم شخصی هم‌سو نیست

**شواهد:**
- `chDailyNotify()` روی `chAll()` اجرا می‌شود و شرط owner یا ownership personal ندارد (`cheques.js:675-704`).
- My Day مستقیماً `ptf_crm_cheques` را می‌خواند و personal را فیلتر نمی‌کند (`myday.js:62+`).

**پیامد:** چک personal قدیمیِ global ممکن است در My Day کاربر دیگر ظاهر شود؛ daily notify نیز همراه `chSave` می‌تواند به FW-C01 منتهی شود.

**کنترل پیشنهادی:** یک accessor مرکزی با scope صریح: `company`, `personal-current-user`, `third-party`; تمام reminder/My Day/print/finance فقط از آن استفاده کنند.

---

### FW-H05 — ناسازگاری تاریخ شمسی در موتور fiscal قدیمی

**شواهد:**
- `yearOf()` فقط سال ASCII `13xx/14xx` را تشخیص می‌دهد (`fiscal.js:24`).
- `invDate()` ابتدا فیلدهایی مانند `t` را می‌گیرد (`28`) که در بسیاری از ثبت‌ها از `faDate()` و دارای ارقام فارسی هستند.

**پیامد:** فاکتور/رویداد دارای تاریخ شمسی با ارقام فارسی ممکن است در سال مالی درست دسته‌بندی نشود یا بی‌تاریخ تلقی شود؛ درحالی‌که working-capital مسیر تبدیل دیگری دارد.

**کنترل پیشنهادی:** استاندارد واحد ISO برای محاسبه و تبدیل jalali با `ptfJToISO` قبل از grouping؛ ممنوعیت parse مستقیم تاریخ نمایشی.

---

### FW-M01 — ناسازگاری سیاست نقش‌ها

**شواهد:** `ceo/commercial` در RBAC دارای `finance:true` هستند (`rbac.js:11-14`)، اما هاب مالی فقط admin/chairman است (`financehub.js:9`). در مقابل supplier finance برای seniorها در دسترس است.

**پیامد:** کاربر ممکن است در یک بخش مالی اجازهٔ ثبت داشته باشد ولی گزارش/کنترل مدیریتی را نبیند؛ سیاست مسئولیت‌پذیری نامشخص می‌شود.

**کنترل پیشنهادی:** تصویب ماتریس نقش کسب‌وکار و تبدیل آن به policy واحد UI + function + API.

---

### FW-M02 — لایه‌های override متعدد در supplier finance

**شواهد:** چند تعریف متوالی برای `slOpenLedger`، `slLedgerPrint` و wrapperهای save/void در `supplier-finance.js` وجود دارد (نمونه: `262`، `275`، `292-299`، `320-340`).

**پیامد:** تغییر کوچک در یک تعریف قدیمی ممکن است توسط override نهایی بی‌اثر شود یا guard را دور بزند؛ رفع‌های سریع گذشته همین ریسک را افزایش داده‌اند.

**کنترل پیشنهادی:** پس از رفع موارد بحرانی، consolidation کنترل‌شده با fixtureهای کامل، نه افزودن wrapper دیگر.

---

### FW-M03 — افتتاحیهٔ تأمین و گزارش رسمی

**شواهد:** افتتاحیهٔ supplier در `supplier_finance.adjustments` با `kind:'opening'` ثبت می‌شود، و افتتاحیهٔ رسمی هم در `fiscal_snapshots` ثبت می‌شود. گزارش رسمی هر دو مفهوم را در مسیرهای متفاوت جمع می‌کند.

**پیامد:** اگر یک مانده برای هر دو ثبت شود، دوباره‌شماری محتمل است. هشدار متنی UI وجود دارد، اما guard ساختاری وجود ندارد.

**کنترل پیشنهادی:** انتخاب صریح یکی از دو مبنای افتتاحیه برای هر account و reconciliation report پیش از قفل سال.

---

### FW-M04 — گزارش رسمی هنوز دفترکل/بانک نیست

**شواهد:** خود گزارش رسمی صراحتاً اعلام می‌کند که جای دفترکل قانونی یا صورت جریان نقدی بانکی نیست (`working-capital.js:279`). وجه نقد/بانک فقط افتتاحیهٔ دستی دارد.

**پیامد:** «خالص سرمایه در گردش ثبتی» برای مدیریت مفید است اما نباید به‌عنوان تراز قانونی، cash position روزانه یا گزارش بانکی تلقی شود.

---

### FW-M05 — UAT واقعی بازگشایی سال انجام نشده

**شواهد:** تست موجود (`tester156`) fixture است و browser، service worker، سرور sync و چند دستگاه واقعی را پوشش نمی‌دهد.

**کنترل پیشنهادی:** سناریوی دو دستگاه با یک snapshot قفل‌شده، unlock توسط chairman، pull/push متوالی و کنترل عدم بازگشت lock.

---

## ۶. پوشش تست فعلی و شکاف‌ها

| تست | چه چیزی را پوشش می‌دهد | چه چیزی را پوشش نمی‌دهد |
|---|---|---|
| `tester154-v281-finance-foundation.js` | فرمول‌های پایهٔ گزارش رسمی fixture | تاریخ شمسی واقعی، همزمانی، چک شخصی واقعی، بانک، UAT UI |
| `tester155-v282-to-save-scope.js` | ذخیرهٔ TO قدیمی | خارج از workflow مالی |
| `tester156-v283-governance-accounts-storage.js` | unlock fixture، ترتیب حساب‌ها، scan source ضمیمه | browser sync واقعی، PHP runtime/S3 واقعی، migration فایل قدیمی |
| testerهای 143 تا 151 | بخش‌هایی از فاکتور/پرداخت/چک تأمین | مسیرهای جدید v28.x، تعارض object sync، privacy شخصی، fiscal lock end-to-end |

**نتیجه:** PASSهای فعلی برای محاسبه و source guard مفیدند، اما نباید معادل پذیرش workflow مالی چندکاربره تلقی شوند.

---

## ۷. سناریوهای UAT لازم (بدون تغییر دادهٔ واقعی)

### گروه A — امنیت و نقش
1. کاربر `buyer` تلاش کند چک شرکت از مسیر پرداخت تأمین ثبت کند.
2. کاربر غیرchairman در فرم عمومی چک ownership را `company` انتخاب کند.
3. accountant/collector یک وصولی اشتباه ثبت کند و مسیر اصلاح مورد انتظار را بررسی کند.
4. ceo/commercial دسترسی گزارش‌ها، supplier account و هزینه جاری را با ماتریس مصوب تطبیق دهند.

### گروه B — چک شخصی و شرکت
1. کاربر A چک شخصی ثبت کند؛ کاربر B در My Day، یادآورها، چک‌ها و هاب مالی آن را نبیند.
2. همان کاربر A با مرورگر/دستگاه دوم وارد شود؛ رفتار مورد انتظار sync/backup برای personal cheque مصوب و آزموده شود.
3. یک personal legacy موجود در global با save کاربر دیگر بررسی شود؛ هیچ رکوردی نباید حذف شود.
4. batch و AI cheque با ownership صریح آزمایش شوند.
5. چک ثالث منتقل‌شده، void payment و reminder در یک سناریو کامل آزموده شود.

### گروه C — تأمین‌کننده و سال مالی
1. فاکتور/پرداخت سال باز: ثبت، تخصیص، void، edit و delete.
2. همان عملیات در سال قفل‌شده: تمام mutationها باید به‌طور یکسان مسدود شوند.
3. unlock chairman در دو دستگاه، سپس sync/pull و کنترل عدم بازگشت lock.
4. ثبت همزمان فاکتور و پرداخت تأمین در دو مرورگر، سپس کنترل وجود هر دو رکورد.
5. legacy payable لینک‌شده/لینک‌نشده و invoice delete برای کنترل دوباره‌شماری.

### گروه D — مشتری و گزارش
1. فاکتور، وصولی ناقص، وصولی کامل، وصولی اشتباه و reversal مورد نیاز.
2. فاکتور با تاریخ شمسی فارسی، ISO و بدون تاریخ؛ تطبیق fiscal dashboard و گزارش رسمی.
3. چک شرکت، شخصی، ثالث، void و ownership نامشخص؛ تطبیق سه خروجی: حساب تأمین، My Day، گزارش رسمی.
4. افتتاحیهٔ تأمین ثبت‌شده در ledger در برابر افتتاحیهٔ رسمی؛ کنترل عدم دوباره‌شماری.

---

## ۸. برنامهٔ اصلاح پیشنهادی، بدون شروع توسعه

### گیت صفر — قبل از قابلیت جدید
1. تصویب ماتریس نقش‌ها و مسئولیت صدور/تایید چک شرکت.
2. تصویب سیاست sync/backup چک شخصی (private local-only یا private encrypted sync؛ گزینهٔ مبهم پذیرفته نشود).
3. تهیهٔ fixture پایدار و اجرای سناریوهای A تا C با دادهٔ آزمایشی.

### اولویت P0
- FW-C01، FW-C02، FW-C03، FW-C04 و FW-C05.

### اولویت P1
- FW-H01، FW-H02، FW-H03، FW-H04 و FW-H05.

### اولویت P2
- FW-M01 تا FW-M05، consolidation زیر‌دفتر و سپس شروع گردش بانک/صندوق.

---

## ۹. نتیجهٔ نهایی ممیزی

در وضعیت فعلی، ادامهٔ توسعهٔ «گردش بانک و صندوق» پیش از تثبیت هستهٔ چک، قفل سال، sync و reversal توصیه نمی‌شود؛ زیرا هر منبع مالی جدید، سطح تعارض و دوباره‌شماری را افزایش می‌دهد. نخست باید یک مدل رویداد مالی قابل ردگیری، مجوزهای enforce‌شده و آزمون چندکاربره برقرار شود.

---

## ۱۰. الحاقیهٔ ممیزی کامل چرخه‌های مالی پشتیبان

پس از درخواست کارفرما برای پوشش کامل workflow، مسیرهای تنخواه، هزینه جاری، سهامداران، سود پروژه، مختومه‌سازی پرونده و حذف‌های زنجیره‌ای نیز بازبینی شدند. آیتم‌های اصلاحی متناظر در `BACKLOG-FINANCIAL-WORKFLOW-v28.3.md` ثبت شده‌اند.

### ۱۰.۱ نقشهٔ کامل چرخه‌ها

| چرخه | منبع داده | نقش‌های فعلی | ریسک/مشاهده |
|---|---|---|---|
| تنخواه شخصی/شرکتی | `ptf_crm_petty`, `ptf_crm_petty_tx`, `ptf_crm_petty_periods` | همه ثبت هزینه؛ خزانه‌دار تسویه/شارژ؛ حسابدار ثبت دوره | حذف هزینه settled، transaction و period را لزوماً reverse نمی‌کند. |
| هزینه جاری | `ptf_crm_opex` | `finance:true` | حذف فیزیکی، بدون fiscal lock مرکزی؛ هزینه linked-deal احتمالاً در سود پروژه و fiscal دوبار اثر می‌گیرد. |
| حقوق موظف سهامدار | `ptf_crm_sharetx` + `ptf_crm_opex` | admin/chairman | حقوق ماهانه opex تولید می‌کند؛ نیازمند guard سال مالی و freeze دوره است. |
| برداشت/علی‌الحساب سهامدار | `ptf_crm_sharetx` | admin/chairman | event ثبت می‌شود ولی lock/reversal مرکزی ندارد. |
| سود پروژه | offers/invoices/buycmp/deal.costEvents/lossEvents | مدیران ارشد | مبنای خرید واقعی با بدهی تأمین یکی نیست؛ reconciliation لازم است. |
| مختومه با تسویه | invoices + salesfiles | role مسیر پرونده | auto-settle receipt می‌سازد؛ reversal استاندارد receipt موجود نیست. |
| پاکسازی زنجیره/یتیم | offers/invoices/payables/deals | مدیر ارشد | hard-delete اسناد مالی قابل رخ دادن است. |

### ۱۰.۲ یافته‌های افزوده

#### FW-H06 — دوباره‌شماری هزینهٔ جاری متصل به پرونده

- `ptfOpexAdd()` هنگام انتخاب `dealRef`، هزینه را هم در `ptf_crm_opex` و هم در `deal.costEvents` می‌نویسد (`opex.js:104-118`).
- hook سود پروژه، `deal.costEvents` را از سود پروژه کم می‌کند (`buycompare.js:739-766`).
- fiscal نیز همهٔ `ptf_crm_opex` را از سود سال کم می‌کند (`fiscal.js:68-84`).

**اثر محتمل:** یک هزینهٔ جاریِ لینک‌شده به پرونده ممکن است یک‌بار در سود پروژه و یک‌بار در opex سال کسر شود.  
**بک‌لاگ:** `FIN-WF-008`.

#### FW-H07 — حذف هزینهٔ تنخواه settled بدون reversal transaction

- `pettySettle()` یک transaction از نوع `settle` ثبت می‌کند و رکورد را settled می‌سازد (`petty.js:219-236`).
- `pettyDel()` فقط رکورد هزینه را از `ptf_crm_petty` حذف می‌کند (`268-278`) و transaction/period مربوط را reverse نمی‌کند.

**اثر محتمل:** ماندهٔ حساب تنخواه، گزارش دوره و فهرست هزینه‌ها از هم فاصله می‌گیرند.  
**بک‌لاگ:** `FIN-WF-009`.

#### FW-H08 — حذف فیزیکی اسناد مالی در پاکسازی زنجیرهٔ یتیم

`ptfOrphanPurge()` می‌تواند invoice و payable را همراه زنجیرهٔ درخواست حذف کند (`bridge.js:650-672`). این مسیر برای انتظام داده طراحی شده، اما از منظر مالی نیازمند preview اثر، role ویژه، backup و archive/void به جای hard-delete است.  
**بک‌لاگ:** `FIN-WF-010`.

#### FW-H09 — auto-settlement پرونده بدون lifecycle reversal کامل

در `sfCloseSettledCommit()`، اگر کاربر گزینهٔ settleOpen را انتخاب کند، ماندهٔ فاکتور با یک receipt با برچسب `autoSettle` پر می‌شود (`salesfiles.js:833-854`). این receipt در همان ساختار وصولی مشتری است و مسیر reversal استاندارد ندارد.  
**بک‌لاگ:** `FIN-WF-013`.

#### FW-H10 — عملیات سهامدار/هزینه/تنخواه خارج از قفل مالی مرکزی

ثبت/حذف opex، حقوق/برداشت سهامدار و بخش عمدهٔ تنخواه guard مشترک سال مالی ندارند. بنابراین قفل سال فعلی، closing سراسری تمام eventهای مالی را تضمین نمی‌کند.  
**بک‌لاگ:** `FIN-WF-004` و `FIN-WF-007`.

#### FW-H11 — تفاوت مبنای سود پروژه با ماندهٔ تأمین

سود پروژه از خرید واقعی/`buycmp` و cost events محاسبه می‌شود، اما بدهی تأمین از فاکتور/پرداخت/legacy. اختلاف زمانی و مبنایی این دو طبیعی است، ولی بدون reconciliation ممکن است مدیر یک رقم را به اشتباه دوبار یا اصلاً در سود لحاظ کند.  
**بک‌لاگ:** `FIN-WF-016`.

### ۱۰.۳ نتیجهٔ توسعه‌یافته

پس از پوشش تنخواه، هزینه، سهامدار، سود و آرشیو، نتیجهٔ بخش ۹ گزارش تقویت می‌شود: **هستهٔ مالی پیش از افزودن بانک/صندوق نیازمند تثبیت workflow و سیاست event/reversal است.** اولویت اجرایی دقیق در بک‌لاگ رسمی ثبت شده است.
