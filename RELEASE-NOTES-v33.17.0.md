# یادداشت‌های انتشار (Release Notes — Version 33.17.0) — شروع مهاجرت MySQL (فاز A)

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (PR #11 باز)
**تمرکز:** **آغاز DB-MIG-001 (فاز A)** — آماده‌سازی کامل مهاجرت داده از فایل‌های JSON به MySQL، مخصوص **سی‌پنل بدون ترمینال**، با **نقش حداقلی کارفرما** و راهنمای فارسی.

---

## 🚀 چه چیزی آماده شد (v33.17.0)

### ۱. لایهٔ اتصال MySQL — `api/db-lib.php`
- کانفیگ امن در `api/ptf-db-config.php` (فایل PHP بدون خروجی — رمز دیتابیس در گیت/چت نمی‌رود).
- اتصال `mysqli` با utf8mb4؛ جدول کلید-ارزش (`ptf_kv`: k/v/rev/updated_at) با کلید اصلی.
- عملیات پایه: get/set/del/all/count (همه null-safe و fail-safe).
- **حالت‌ها:** `off` (فعلی) → `dual` (نوشتن هم‌زمان در فایل+دیتابیس؛ خطای دیتابیس هرگز مسیر فایل را نمی‌شکند) → `mysql` (خواندن از دیتابیس با fallback به فایل).
- checksum با SHA-256 برای بررسی تطابق.

### ۲. اسکریپت مهاجرت فارسی — `api/migrate.php`
- **اجرا از مرورگر** (بدون نیاز به ترمینال — همان الگوی data-health-check).
- ۶ مرحلهٔ فارسی: ۱) بکاپ اضطراری ۲) ساخت جدول ۳) انتقال داده ۴) بررسی تطابق ۵) فعال‌سازی نوشتن هم‌زمان ۶) سوییچ نهایی.
- مراحل حساس نیاز به **تایپ کلمهٔ «مهاجرت»** دارند (جلوگیری از اجرای ناخواسته).
- بررسی تطابق: checksum + شمارش رکورد هر کلید؛ مغایرت = توقف خودکار (دادهٔ قبلی سالم).
- کلیدهای جانبی (otp/ratelimit) در فایل می‌مانند (طبق طرح).
- بعد از سوییچ: راهنمای حذف امن فایل.

### ۳. یکپارچه‌سازی با `api/crm.php`
- `load_data`/`save_data` با wrapper: حالت dual → نوشتن در هر دو؛ حالت mysql → خواندن از دیتابیس (منبع حقیقت) با fallback به فایل.

### ۴. مستندات فارسی کامل — `crm/MIGRATION-MYSQL-CPANEL-GUIDE-FA.md`
- راهنمای گام‌به‌گام سی‌پنل (بدون ترمینال): ساخت دیتابیس با MySQL Database Wizard، آپلود فایل، اجرای ۶ دکمه، پاک‌سازی امن.
- عیب‌یابی فارسی + **راهنمای کاربران روی دستگاه‌های متصل** (در طول مهاجرت کاری لازم ندارند؛ بعد از سوییچ یک بار Ctrl+Shift+R).
- ضمانت‌های امنیتی (بکاپ قبل، checksum، عدم حذف JSONها، رمز در فایل بدون خروجی، تأیید کلمه).

### ۵. به‌روزرسانی سند اصلی
- `crm/MIGRATION-MYSQL-SPEC-v1.md` بخش ۱۰: سازگاری با سی‌پنل بدون ترمینال + نقشهٔ اجرای جدید.

---

## 📋 فایل‌ها
| فایل | تغییر |
|---|---|
| `api/db-lib.php` | **جدید** — لایهٔ MySQL |
| `api/migrate.php` | **جدید** — اسکریپت مهاجرت فارسی ۶ مرحله |
| `api/ptf-db-config.sample.php` | **جدید** — الگوی کانفیگ |
| `api/crm.php` | wrapper dual-write/read روی load_data/save_data |
| `crm/MIGRATION-MYSQL-CPANEL-GUIDE-FA.md` | **جدید** — راهنمای کامل فارسی سی‌پنل |
| `crm/MIGRATION-MYSQL-SPEC-v1.md` | بخش ۱۰: وضعیت فاز A + محدودیت سی‌پنل |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.17.0 |
| `_tools/uat/tester297-mysql-migration.js` | **جدید** (۱۶/۰) |
| `RELEASE-NOTES-v33.17.0.md` / `REGRESSION-REPORT-v33.17.0.md` | اسناد انتشار |

---

## ⚠️ قدم بعدی (اقدام شما — ۵ مرحله در سی‌پنل)
طبق `crm/MIGRATION-MYSQL-CPANEL-GUIDE-FA.md`:
۱) ساخت دیتابیس (MySQL Database Wizard) · ۲) آپلود `api/migrate.php` · ۳) باز کردن آدرس اسکریپت · ۴) اجرای ۶ دکمهٔ فارسی · ۵) حذف فایل.
> کل زمان: حدود ۲۰ تا ۳۰ دقیقه. در هر مرحله راهنمای فارسی داخل صفحه نمایش داده می‌شود.
