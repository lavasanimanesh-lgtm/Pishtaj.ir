# یادداشت انتشار v34.39.3 — HOME-NIGHT-READ (فیکسِ قطعی بازنمایشِ شیت موبایل، حذف برق چت داک، خوانایی کامل شب)

**تاریخ:** 2026-09-17
**بازوی تغییر:** Arena Agent
**کانال:** سایت عمومی — صفحهٔ اول (لایهٔ نمایشی/رفتاری v34.39.3؛ محتوا/سئو/فرم‌ها بدون تغییر)
**قرارداد:** tester665 (۲۵ سنجه) + بازتنظیم سنجه‌های 663/664 + هم‌ترازی پین‌ها با ابزار رسمی `bump-version-pins.js`

## گزارش کارفرما
> «در ویو موبایل روی دکمهٔ منو که می‌زنم فقط تار می‌شه و منو نمایش داده نمی‌شه. برق دور چت هوشمند برداشته شود. تغییر رنگ کارت‌ها به سفید باعث می‌شود در نمای شب نوشتهٔ برخی کارت‌ها خوانده نشود. همچنین رنگ فونت پرسش‌های رایج کارفرمایان خوانده نمی‌شود. همینطور دکمهٔ استعلام خوانا نیست. و قسمت رئیس هیئت‌مدیره …»

## ریشه‌یابی و فیکس‌ها
1. **منو همچنان باز نمی‌شد (فیکسِ دور قبل ناکافی بود):** بلورِ هدر را خاموش کرده بودیم، اما زنجیره‌ی اجدادِ هدر هنوز هر لحظه می‌تواند `containing-block` بسازد (فیلتر/اسکرول‌کامپوزیت/سایه‌گذاریِ compozite در مرورگرهای مختلف). راه‌حلِ **قطعی و مرورگرناپذیر**: خودِ گرهٔ `#mainNav` با JS در `≤790px` به سطح `body` (درست پیشِ بک‌دراپ) منتقل می‌شود و در `>790` به جای اصلی‌اش در نوبار برمی‌گردد — جابه‌جاییِ یکسانِ همان گره، بدون تغییر لینک/متن؛ هندلرهای main.js و MutationObserver روی خودِ المان می‌مانند و سالم کار می‌کنند. شیت `z-index:9535` گرفت (بالای بک‌دراپ، زیر داک تا ✕ همیشه کلیک‌پذیر بماند).
2. **حذف برق دور «چت هوشمندِ داک»:** `#ptfDockChat::before` از همه‌ی فهرست‌های current-ring (حلقهٔ دائم، ایزولاسیون، بلاک reduce) بیرون رفت؛ بازخورد `:active` و هایلایت‌های دیگرش دست‌نخورده. افزوده: هستهٔ سفیدِ حلقه‌های دائم `.95→.7` تا روی متن ریز (۱۰.۵px) خیرگی نسازد.
3. **هاور کارت‌ها در شب:** گران‌تیومِ سفیدِ کرمیِ نسخهٔ ۳۹.۱ نوشته‌های روشِ حالت شب را ناخوانا می‌کرد؛ اکنون در `html.ptf-dark` هاور = **گرانیتِ گرم** (پس‌زمینهٔ آبیِ نفتیِ روشن‌تر + لبهٔ کهربایی + تیتر کهربایی) و همان افکتِ سفید فقط برای نمای روز ماند.
4. **پرسش‌های رایج کارفرمایان:** `summary`های فریمور `.faq-list` رنگ درون‌کلاسِ تیره داشت؛ قاعدهٔ شبِ اختصاصی اضافه شد (`summary #eef3fb`، جواب‌ها `#b7c6de`).
5. **بلوک رئیس هیئت‌مدیره:** پاراگراف با رنگِ درون‌خطی `#475569` در فهرست اوررایدهای شب نبود؛ سه خاکستریِ اسلیت (`#475569/#60646d/#4b4d55`) به قانون attribute-override اضافه و یک درجه روشن‌تر تنظیم شد (`#9db0ca→#a9bdd8`).
6. **دکمهٔ استعلامِ وسط داک:** در شب، قاعدهٔ `html.ptf-dark #ptfDock a` (مشخص‌تر از `.dock-cta`) متن سفید را به خاکستریِ آبیِ کم‌کنتراست روی شعله تبدیل می‌کرد؛ با قاعدهٔ هم‌ارزِ تیره‌تر برگشت: **سفیدِ خالص روی فلرِ پرکنتراست (#e0431a→#f28a00) + text-shadow گرم** روی CTAها (داک/هیرو/تماس).

## نکات مهندسی
- صفر کتابخانه/صفر درخواست شبکه؛ reparent فقط در `matchMedia` (بدون poll).
- بودجه‌های سرعت حفظ شد: `home.css` ۳۹٬۳۸۱ نویسه (<۴۰K گارد tester662 — با فشرده‌سازی کامنت‌های تزئینی)، `ptf-motion.js` ۱۱٬۸۷۹ (<۱۲K).
- بایکوت کامنت‌ها مارکرهای لایه (v34.39.0/1/2/3) را که سنجه‌ها به آن‌ها لنگر می‌کنند حفظ کرد.
- reduce-motion: ترنزیشن شیت در بلاک reduce خاموش؛ حلقه‌های باقی‌مانده زیر kill-switch.

## وضعیت دروازه
tester665: ۲۵/۲۵ ✓ · tester664: ۲۹/۲۹ ✓ · tester663: ۳۰/۳۰ ✓ · tester662: ۳۶/۳۶ ✓ · گیت کامل CI پس از bump: **۲۷۷ PASS / ۱ FAIL** (شکست محیطی شناخته‌شدهٔ tester656) · arch-guard: PASS.
