# یادداشت انتشار — v34.37.2

**تاریخ:** ۲۰۲۶-۰۹-۰۵ (۱۴۰۵/۰۶/۱۴) · **پایه:** v34.37.1
**گیت CI:** ۲۱۴ PASS / ۰ FAIL (شامل دو تستر تازه/به‌روز: tester598 = ۵۰ سنجش، tester599 = ۸۰ سنجش)
**سند تحلیل:** `ARENA-CRM-INVOICE-PANEL-ASSESSMENT-2026-09-05.md` (یافته‌های F1 · F3 · F5 · F6 · F7 · F8)

گزارش کارفرما:

> «در قسمت فاکتورها، فاکتورهای ثبت‌شده و ارجاع‌شده نمایش داده نمی‌شوند.»

این انتشار **گام ۱** از همان ارزیابی است: لایهٔ نمایش. هیچ تغییری در سرور، مدل داده، مجوزها، یا جریان ثبت/اصلاح/ابطال فاکتور انجام نشده است.

---

## ① ریشهٔ یک‌ریشهٔ دو نشانه

پنل فاکتورها **ارجاع‌محور** بود، نه فاکتور‌محور:

| چه چیزی | مبدأ پیش از این | نتیجه |
|---|---|---|
| ردیف‌های پنل | `data('ptf_crm_offers').filter(o => o.invRef && !o.rialOf)` | بدون `invRef` روی این دستگاه ⇒ **هیچ ردیفی**، حتی «ارجاع‌شده»ها |
| فاکتورِ زیر هر ردیف | `invs.filter(i => i.offerNo === o.no)` (تطبیقِ سخت، رشته‌ای) | یک فاصله، یک رقم فارسی، یا ثبت روی نسخهٔ ریالی ⇒ فاکتور «**ثبت‌شده**» نامرئی |

پس پاک‌شدن `invRef` (رویژن ابلاغ، لغو ارجاع، آینهٔ کهنه پس از ورود حسابدار، فاکتور مهاجرت‌شدهٔ بی‌`offerNo`) **همه‌چیز** را می‌بُرد و فقط پیام «ارجاع آماده ثبت فاکتور رسمی وجود ندارد» می‌ماند — یعنی دقیقاً همان «ثبت‌شده‌ها هم دیده نمی‌شوند».

## ② آنچه عوض شد (`crm/official-invoice-v2.js`، لایهٔ نمایش)

**۱) بخش «🧾 فاکتورهای ثبت‌شده بدون ردیف ارجاع»** — انتهای پنل. هر فاکتور رسمیِ فعالی که هیچ ردیفی آن را نمی‌شناسد، کارتی کامل می‌گیرد: شماره، تاریخ، شناسه مودیان، «سند مبدأ»، «پرونده»، پایه/کل/مانده، شمارش پیوست‌ها، و سه اقدام، **ابطال** (id-محور؛ بی‌نیاز از پیدا شدن پیشنهاد) و **اصلاح اطلاعات** (فقط وقتی پیشنهادش موجود است). عمداً **هیچ دکمهٔ ثبتی** در این بخش نیست — سندِ دوم لازم نیست، این‌جا فقط «دیدن و درست کردنِ پیوند» ممکن است. سقف رندر ۴۰ کارت + «… و n مورد دیگر» تا صفحه سنگین نشود؛ شمارش ابطال‌شده‌های مرتبط هم اعلام می‌شود.

**۲) تطبیق نرمال‌شدهٔ شمارهٔ ارجاع** — `normRef()` (trim + یکسان‌سازی ارقام فارسی/عربی + حروف بزرگ) و «نام‌های مستعارِ همان سند»: `o.no`، `o.rialOf`، `invRef.rialBasis`، شمارهٔ نسخهٔ ریالیِ همراه. یک فاکتورِ ثبت‌شده روی نسخهٔ ریالی، ردیف ارزی را هم «✅ ثبت‌شده» می‌کند — و برعکس.

**۳) نجات فاکتورهای بی‌`offerNo` از روی پرونده** — فقط و فقط وقتی آن فاکتور هیچ مالکی ندارد **و** دقیقاً یک ردیف روی همان `caseId` وجود دارد؛ ردیف بجِ «🧭 وصل‌شده از پرونده» می‌گیرد. با دو ردیف روی یک پرونده **حدس نمی‌زنیم** و سند در بخش یتیم‌ها می‌نشیند (نمایش داده می‌شود، ولی به ردیفِ اشتباه نمی‌چسبد).

**۴) سربرگ تشخیصی + راه‌های بازگشت** — «📤 ارجاع‌شده · ✅ ثبت‌شده · ⚠️ بدون ردیف ارجاع · 💰 مطالبه باز · دادهٔ این دستگاه: n فاکتور · m پیشنهاد» و دکمهٔ **«↻ بازخوانی از سرور»** (`ptfInvoicesRefresh`: یک pull و سپس رندر دوباره، بدون هیچ نوشتن). روی رندرِ خالی، **یک** بازتلاش خودکارِ یک‌باره (سقف ۶۰ ثانیه، بی‌حلقه) انجام می‌شود — رفعِ «صفحهٔ خالیِ لحظهٔ فرود حسابدار». جستجوی `#invSrch` که در بازنویسی پنل گم شده بود برگشت و روی **هر دو** بخش کار می‌کند. پیام خالیِ جدید، خودش می‌گوید که داده روی دستگاه هست ولی ارجاع فعال ندارد.

**۵) قفل «ثبت دوباره» مقاوم شد** — `ptfActiveInvoiceOfOffer` از همان تطبیق نرمال‌شده و نام‌های مستعار استفاده می‌کند، پس با دادهٔ کج هم از کار نمی‌افتد (ریسک اصلی: دو سند مالیاتی روی یک پرونده). قفل چسبنده نیست: فاکتور ابطال‌شده مسیر را آزاد می‌گذارد و فاکتور ناظیر جلوی ثبت رسمی را نمی‌گیرد. تابع عمداً **خودکفا** نوشته شده (بدون وابستگی به توابع بیرونِ برش و بدون `findOffer`) — چون tester598 همان تابع را به‌تنهایی در `vm` اجرا می‌کند و قراردادش این است که گارد پیش از لمس `findOffer` برگردد.

## ③ گام ۲ — آگاهانه انجام **نشد** (منتظر تصمیم کارفرما)

| مورد | چرا عقب افتاد |
|---|---|
| گارد `revise_award` روی `unset invRef` (`api/sales-domain.php:1839`) | تغییر رفتار سرور و جریان ابلاغ؛ نیاز به تصمیم مالکانهٔ «ارجاع پس از رویژن چه شود» |
| فرمان `reconcile_invoice_refs` (ترمیم دسته‌جمعی پیوندها) | فرمان جدید + مهاجرت داده |
| تغییر مبدأ رندر از `offers.invRef` به `ptf_crm_invoices` | بازطراحی پنل؛ با همین اصلاح، سند یتیم **دیده** می‌شود و دسترسی به اقدامات دارد |
| ردیف‌های نسخهٔ ریالی (`rialOf`) | با همین انتشار، فاکتورشان روی ردیف ارزیِ والد نشسته و یتیم نمی‌ماند |
| `crm/rbac.js` | پنلِ بازنشسته (INV-PANEL-DEAD-CODE)؛ دست‌زدن به کد مرده فقط ریسک adds می‌کند |

تا گام ۲، «ارجاع‌شده»ها در دستگاهی که `invRef` در آینهٔ محلی‌اش نرسیده، از طریق **بخش یتیم‌ها + بازخوانی از سرور** دیده می‌شوند؛ برای رفعِ ریشه‌ایِ پاک‌شدن `invRef` همان گام ۲ لازم است.

## ④ تست‌ها

**`_tools/uat/tester599-v34.37.2-invoice-panel-visibility.js` — ۸۰ PASS / ۰ FAIL** (تازه). خودِ `renderInvoices` را در `vm` اجرا می‌کند و **HTML تولیدی** را می‌سنجد، نه وجود رشته در فایل (همان نقطهٔ کوری که در F8 به آن اشاره شده): دیده‌شدن سند یتیم، نبودِ دکمهٔ ثبت در آن بخش، ابطال id-محور، تطبیق با فاصله/رقم فارسی، نام‌های مستعار ریالی/ارزی، نجات از `caseId` و «حدس نزدن»، فیلتر ناظیر و ابطال‌شده، ترتیب کاهشی، سقف ۴۰، جست‌جو در هر دو بخش، رفتار بازخوانی خودکار (یک‌بار، بی‌حلقه، بی‌خطا نبودِ `ptfSyncPullNow`)، و هر دو مسیر گارد.

**آزمون جهش (mutation check)** — همین تستر روی پنج نسخهٔ دست‌کاری‌شده از فایل اجرا شد تا معلوم شود واقعاً باگ را می‌گیرد:

| جهش | نتیجه |
|---|---|
| حذف بخش یتیم‌ها (همان رفتار v34.37.1) | **۲۶ FAIL** |
| بی‌اثر کردن `normRef` | ۲ FAIL |
| بستن نجات از `caseId` | ۳ FAIL |
| حذف بازخوانی خودکار | ۵ FAIL |
| حذف نام‌های مستعار (تطبیق فقط با `o.no`) | ۲ FAIL |

`tester598` (پنل ردیفی + قفل ثبت دوباره، v34.37.1) بدون رگرسیون: **۵۰ PASS / ۰ FAIL**. سوئیت فاکتور: tester428 (۱۹) · tester474 (۱۴) · tester477 (۲۱) · tester478 (۲۷) · tester482 (۲۴) · tester584 (۳۱) · tester596 (۶۰) — همگی سبز. دو تستر کهنه (`tester10-sprint73`, `tester109-v193`, `tester7-sprint70`) همان نتایجِ پیش از این انتشار را می‌دهند و در گیت CI نیستند.

**نکتهٔ نگهبان:** دکمهٔ «🔗 پرونده» در پیش‌نویس اول `c.cd || c._id` نوشته شده بود و `_tools/arch/arch-guard.js` آن را با قاعدهٔ **A2 (cd-first)** مسدود کرد (۴ تستر گیت قرمز شد). نسخهٔ نهایی فقط `cd` را می‌فرستد — که در واقع درست‌تر هم هست، چون `salesfiles.js` ردیف را با همان `window._sfOpen === r.cd` باز می‌کند و فال‌بک `_id` هیچ‌وقت کار نمی‌کرد. نگهبان، همان‌طور که طراحی شده بود، جلوی ورود الگوی غیرقراردادی را گرفت.

## ⑤ یک خودخوردی در ابزار bump که در همین انتشار رفع شد

`_tools/uat/fix-version-drift.js` با قاعدهٔ `R-H` رشتهٔ `var VER = 'v` را **در هر بافتی** بازنویسی می‌کرد؛ در اجرای bump همین نسخه، خطِ `var VER = 'v34.37.1';` در `tester591` را به `var VER = window.PTF_CRM_RELEASE34.37.2';` تبدیل کرد و فایل از نظر نحوی شکست (گیت قرمز، بدون هیچ ربطی به کد CRM). قاعده اکنون فقط در بافتِ رشته/regex (پیش از آن `'`، `"` یا `/`) اجرا می‌شود؛ اجرای دوبارهٔ ابزار روی درختِ امروز `0 files-touched / 0 rule-applications` می‌دهد (idempotent) و اعلان‌های لفظیِ عامدِ تسترها دیگر دست‌نخورده می‌مانند.

## ⑥ نسخه‌گذاری (قرارداد تک‌منبعی)

`VERSION.json` → `v34.37.2`؛ به‌روزرسانی اعلان/کش در `crm/index.html` (`PTF_CRM_RELEASE` + ۱۱۱ cache-buster)، `crm/sw.js` (`RELEASE`/`ASSET_VERSION`/`CACHE`)، `crm/clear-cache.html`، `crm/cms.js`، `crm/manifest.json`، `crm/shell.js`، `api/sales-domain.php` (`SD_SERVICE_VERSION`). سپس `node _tools/uat/bump-version-pins.js 34.37.1` → **۷۳۹ پین در ۱۴۴ تستر**.

## ⑦ بدون تغییر

هیچ تغییری در `api/*.php` جز ثابت نسخه، در ساختار `ptf_crm_invoices`/`ptf_crm_offers`، در مجوزها (`can()`/`role()`)، در فرمان‌های `register_invoice`/`correct_invoice`/`void_invoice`، و در مودال ثبت فاکتور انجام نشده است. ماسک قیمت/نقش‌ها دست‌نخورده است و فاکتور ناظیر همچنان دفتر مستقل خودش را دارد.
