# Release Notes — v31.7.20

**تاریخ:** ۱۴۰۵/۰۴/۲۹ (2026-07-20)
**نسخه:** v31.7.20
**نوع:** 🔴 رفع باگ بحرانی داده — کد تکراری درخواست/کالا (ادغام اقلام + حذف آبشاری مشترک) + ستون Model در PDF استعلام تامین (BUG-CODE-DUP-002 + BUG-RFQS-MODEL-001)

---

## 🎯 مشاهدات کارفرما

1. با ثبت درخواست جدید، کد جدید تولید نشد — سیستم آخرین کد را دوباره داد، اقلام درخواست قبلی به جدید اضافه شد و شماره درخواست مشتری پذیرفته نشد (پیوست سالم بود).
2. با حذف درخواست جدید، درخواست قبلیِ هم‌کد هم حذف شد.
3. ایراد مشابه در ثبت کالای جدید (کد تکراری کالا).
4. در PDF استعلام تامین‌کننده، ستون Model حذف می‌شود.

## 🔍 علت ریشه‌ای (RCA)

### BUG-CODE-DUP-002 — چرا کد تکراری تولید شد (ریشه هر ۳ مشاهده اول)

**سرور کدساز (`api/codegen.php`) هنگام همگام‌سازی شمارنده‌ها فقط فایل‌های `crm/data/*.json` را اسکن می‌کرد — درحالی‌که داده واقعی سیستم از مسیر sync در `crm/data/sync/*.json` ذخیره می‌شود.** نتیجه: شمارنده سرور از داده واقعی عقب می‌ماند و شماره‌هایی که قبلاً استفاده شده بودند را دوباره در pool رزرو می‌کرد → دستگاه کد «جدید» می‌گرفت که در واقع کد درخواست موجود بود.

**زنجیره علائم دقیقاً از همین‌جا:**
- کد تکراری → دو رکورد با یک `cd` → همه ماژول‌ها (ویرایشگر اقلام، پیوست‌ها به‌جز مسیر خاص خودش، جستجو) با `filter(cd===…)[0]` به **رکورد قدیمی** می‌رسیدند → «اقلام قبلی به درخواست جدید اضافه شد» و «شماره مشتری پذیرفته نشد» (روی رکورد دیگری ذخیره می‌شد).
- حذف آبشاری (BUG-031) با همان `cd` کار می‌کند → **هر دو رکورد هم‌کد با هم حذف شدند** (مشاهده ۲).
- کالا هم از همان counter/pool سرور تغذیه می‌شود (مشاهده ۳) — و گارد قبلی saveProd فقط ۱ بار regen می‌کرد و اگر باز تکراری بود بی‌صدا ادامه می‌داد.

### BUG-RFQS-MODEL-001 — ستون Model

فیلد `model` هنگام «افزودن از ماژول کالا» درست از `x.md` نگاشت می‌شد، اما جدول PDF استعلام تامین (`ptfRfqsPrintHtml`) اصلاً ستونی برای آن رندر نمی‌کرد و در ویرایشگر اقلام هم قابل مشاهده/ویرایش نبود — داده بود، نمایش نبود.

## ✅ اصلاحات (دفاع دو لایه + قابلیت)

| # | فایل | تغییر | شدت |
|---|------|--------|-----|
| 1 | `api/codegen.php` | **رفع ریشه:** اسکن شمارنده‌ها اکنون هر دو مسیر `crm/data/` و `crm/data/sync/` را می‌بیند — E2E روی سرور PHP واقعی اثبات شد: با آخرین درخواست `RFQ-1361` در sync، رزرو جدید از `RFQ-1362` شروع می‌شود | 🔴 P0 |
| 2 | `crm/index.html` (`saveRfq`) | **گارد کلاینت:** اگر کد تولیدی تکراری بود تا ۵ بار کد بعدی گرفته می‌شود؛ اگر باز تکراری بود ثبت با پیام صریح **مسدود** می‌شود (+audit) — دیگر هیچ مسیری به ادغام بی‌صدا یا حذف آبشاری مشترک ختم نمی‌شود | 🔴 P0 |
| 3 | `crm/index.html` (`saveProd`) | همان گارد برای کالا (گارد قبلی فقط ۱ بار regen می‌کرد) | 🔴 P0 |
| 4 | `crm/rfqsmart.js` | ستون **Model** در PDF استعلام تامین (هوشمند: فقط وقتی حداقل یک قلم مدل دارد — جدول اقلام بی‌مدل تمیز می‌ماند) + ستون «مدل» قابل‌ویرایش در ویرایشگر اقلام استعلام | 🟡 P1 |
| 5 | ۴ نقطه نسخه | v31.7.20 | — |

## 🛟 پاکسازی داده موجود (اقدام کارفرما)

اگر الان رکورد هم‌کد در سیستم دارید: تنظیمات → «⚠️ بررسی کدهای تکراری» — موارد بدون ارجاع مالی، دکمه «اختصاص کد جدید از server» دارند؛ موارد مبهم طبق سیاست v31.7.2 عمداً دستی بررسی می‌شوند. درخواست حذف‌شده (مشاهده ۲) در صورت نیاز از بک‌آپ ساعتی/روزانه سرور قابل بازگردانی است.

## 📊 نتایج گیت

- تستر جدید `tester195`: **۱۳/۱۳ PASS** — شامل شبیه‌سازی دقیق سناریوی کارفرما (کد تکراری → regen سالم / همه-تکراری → مسدود) و رندر PDF با/بدون مدل
- **E2E سرور واقعی:** counter پس از fix از sync خواند و `RFQ-1362,1363,1364` رزرو کرد (قبلاً از ۱۰۰۱ شروع می‌کرد)
- رگرسیون کامل: **173/173 فایل PASS — 4005/4005 چک PASS** | `audit.py` (۱۰ بخش): PASS | `php -l`: سالم

## ⚠️ Impact Analysis

| بخش | تأثیر | ریسک |
|------|--------|------|
| داده موجود | هیچ migration؛ فقط جلوگیری از تولید تکراری جدید | صفر |
| کدهای TMP/مالی (CHQ/INV/...) | سیاست server-only v31.7.3 دست‌نخورده | صفر |
| PDF استعلام‌های قدیمی بدون مدل | ستون Model اضافه نمی‌شود — قالب قبلی حفظ | صفر |
| offers (TO/CO/TC) | counters آن‌ها از قبل فایل sync را جدا می‌خواند (v31.6.27) — دست‌نخورده | صفر |

## 📋 راستی‌آزمایی توسط کارفرما (۵ دقیقه — همان سناریوهای گزارش)

1. دو درخواست پشت‌سرهم ثبت کنید → دو کد متفاوت و متوالی؛ اقلام هرکدام مستقل؛ شماره درخواست مشتری هرکدام ذخیره شود.
2. درخواست دوم را حذف کنید → فقط همان حذف شود.
3. دو کالای جدید ثبت کنید → دو کد متفاوت.
4. استعلام تامین با قلمی از ماژول کالا که مدل دارد بسازید → پیش‌نمایش/PDF ستون Model را نشان دهد؛ استعلام بدون مدل → جدول مثل قبل.

---
*توسعه‌دهنده: PTF Development Team — 2026-07-20*
