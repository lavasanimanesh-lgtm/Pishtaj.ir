# بازبینی جامع پیش از دیپلوی پروداکشن — ۲۰۲۶-۰۸-۲۹ (v34.8.51)

پاسخ به سه پرسش مالک: (۱) آیا چیزی از قلم افتاده؟ (۲) آیا کاربران پس از دیپلوی پروداکشن نمی‌توانند وارد شوند؟ (۳) آیا تنظیماتِ «همگرایی/حالت سروری» هنوز معنا دارد؟

## ۱) پرسش دوم اول — ریسک ورود پس از دیپلوی پروداکشن

**ریسک اصلی حادثهٔ استیجینگ ریشه‌یابی و مسدود شد:**
- حلقهٔ بازسازی نشست → سقف ۳ تلاش (v34.8.47)
- نبود fallback مخزن → زنجیرهٔ SS→LS→حافظهٔ تب (v34.8.48)
- حافظهٔ فانتوم (setItem بی‌خطا ولی ناپایدار) → خواندن-باز + ترمیم migrate (v34.8.50)
- پوشش تست رفتاری: tester548/549/550/551 (شبیه‌سازی سه نوع مرورگر خراب) + ۱۶۶ تستر سبز.

**مسیر ورود کاربر پروداکشن پس از دیپلوی (بازبینی‌شده):**
1. مرورگرهای موجود: نشست قدیمی در localStorage خوانده می‌شود (fallback حفظ‌شده) → توکن LS→SS مهاجرت ایمن؛ توکن منقضی → پیام روشن + ورود مجدد.
2. اولین ورود موفق: کوکی HttpOnly جدید صادر می‌شود؛ تب‌های بعدی با همان کوکی بازسازی می‌شوند (۱ role_verify).
3. 2FA پیامکی **پیش‌فرض خاموش** (v34.8.49) — هیچ کاربری پیامک ورود نمی‌گیرد.
4. سرویس‌کار PWA با تغییر نام CACHE کش قدیمی را خودکار پاک می‌کند.

**ریسک‌های باقیمانده (شناخته‌شده و قابل‌مدیریت):**
| ریسک | احتمال | کاهش‌دهنده |
|---|---|---|
| تب‌های بازِ حین دیپلوی، نسخهٔ مخلوط اجرا کنند | متوسط | راهنمای دیپلوی: دیپلوی در ساعات کم‌ترافیک؛ کاربران صفحه را یک بار رفرش کنند (clear-cache.html) |
| قطعی api در حین آپلود FTP | کم | گیت CI قبل از FTP؛ استیجینگ همان بسته را زنده اجرا کرده (= تست parse واقعی PHP) |
| مرورگرهای بسیار قدیمی | کم | ES2017 از v34.8.12 در پروداکشن فعال بوده؛ تغییر نکرده |

**نتیجه: ورود مسدود نمی‌شود؛ مسیرهای خطا پیام روشن دارند.**

## ۲) مهاجرت دادهٔ دستگاه‌ها — نکتهٔ کلیدی این بازبینی

معماری سرور-محور برای «دستگاه تازه» خودکار فعال می‌شود، اما **دستگاه‌های دارای دادهٔ محلی (تمام مرورگرهای کاربران فعلی پروداکشن) عمداً خودکار منتقل نمی‌شوند** (fail-closed: `existing_local_data`) — تا هیچ داده‌ای بدون بازبینی انسانی merge نشود.

یعنی: **هر دستگاه یک‌بار باید «انتقال یک‌باره» را اجرا کند.** تا امروز این مسیر در تنظیمات به‌صورت گزینهٔ اختیاریِ «حالت سرور-محور» با ۴ دکمه (فعال/غیرفعال/همگرایی/پاک‌سازی) بود — گمراه‌کننده و خطرناک (دکمهٔ فعال‌سازی بدون همگرایی؛ دکمهٔ بازگشت به legacy که با حذف موتور در v34.9.1 ناسازگار می‌شود).

### اصلاح انجام‌شده (v34.8.51)
- جعبهٔ جدید **«🖥 وضعیت دستگاه»** در تنظیمات: سبز (سرور-محور فعال + وضعیت صف) یا کهربایی (در انتظار انتقال یک‌باره) با CTA واحد «⬆️ انتقال یک‌بارهٔ داده‌های این دستگاه».
- دکمه‌های «فعال‌سازی/غیرفعال‌سازی» از UI حذف شدند (توابع برای ابزارها باقی‌اند).
- خوانندهٔ واحد `ptfBStatus()` (enabled/synced/queue/localPayload).

### برنامهٔ پیشنهادی برای دیپلوی پروداکشن (ورودی چک‌لیست v34.9.0)
1. دیپلوی v34.9.0 در ساعات کم‌ترافیک.
2. هر کاربر (یا راهنمای گروهی در پیام/ایمیل): تنظیمات → وضعیت دستگاه → «انتقال یک‌باره» با اینترنت پایدار.
3. پایش داشبورد «موتور همگام‌سازی (R4)»: وقتی همهٔ کلیدها ≥۷ روز سبز بودند و win7≈0 → v34.9.1 (حذف موتور legacy).

## ۳) چک‌لیست «چیزی از قلم نیفتاده؟»

| حوزه | وضعیت | مدرک |
|---|---|---|
| ورود/نشست (۴ هات‌فیکس) | ✅ | tester548/549/550/551؛ سناریوهای vm سه نوع مرورگر |
| 2FA خاموش پیش‌فرض | ✅ | tester551 §۴؛ `twofa_enabled ?? false` |
| پنل نشست‌های فعال | ✅ | tester547 (۷۱ سنجه) |
| صف آفلاین IDB + idempotency | ✅ | tester526/517؛ سقف ۵۰۰/۷۲h |
| سقف نرخ فرمان‌ها | ✅ | tester546؛ 60/60s قبل از قفل |
| ابزارهای ریکاوری IDB-aware | ✅ | tester546 |
| هم‌نسخگی ۷+ نقطه | ✅ | arch-guard A6 PASS |
| گیت کامل | ✅ | ۱۶۶+ تستر سبز قبل از هر پوش |
| موتور legacy حاضر-but-پرچم‌دار | ✅ (عمدی) | حذف در v34.9.1 پس از تله‌متری ≥۷ روز |
| باقی‌ماندهٔ A10 + اثبات E1..E7 | ⏳ | هدف v34.9.0 (گام بعد) |
| چک‌لیست نهایی دیپلوی پروداکشن | ⏳ | هدف v34.9.0 — این سند ورودی آن است |

## جمع‌بندی
- خطر «کاربران وارد نمی‌شوند» ریشه‌یابی و سه‌لایه مسدود شده؛ مسیر ورود پروداکشن بازبینی و تست شد.
- «همگرایی داده‌ها» همچنان **لازم** است (یک‌بار در هر دستگاه) — از گزینهٔ اختیاری به «وضعیت دستگاه + انتقال یک‌باره» تبدیل شد.
- «فعال‌سازی حالت سروری» به‌عنوان دکمهٔ دستی منسوخ و حذف شد.
