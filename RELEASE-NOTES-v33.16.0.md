# یادداشت‌های انتشار (Release Notes — Version 33.16.0) — فاز ۲ بکاپ

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (PR #11 باز)
**تمرکز:** تکمیل فاز ۲ برنامهٔ فوری بکاپ — **بکاپ دلتا (فقط کلیدهای تغییرکرده)** + **بازگردانی تراکنشی (تمام‌یا-هیچ با rollback)** + آماده‌سازی سرور (اکشن `save_backup_delta` + چرخش مشترک).

---

## 🚀 فاز ۲ — اجراشده (v33.16.0)

### F2-1: بکاپ دلتا (کاهش شدید حجم ارسال/پردازش)
- **امضای per-key** (`ptf_backup_delta_sig`): برای هر کلید داده یک امضای djb2 ذخیره می‌شود.
- `ptfBackupDeltaCollect()`: فقط کلیدهایی که امضاشان تغییر کرده جمع می‌شوند.
- `ptfBackupMaybeAuto` (بکاپ خودکار ساعتی): 
  - اولین بار (بدون امضای per-key) → **بکاپ کامل اولیه** (پایهٔ سرور).
  - بعداً → **فقط دلتا** (`save_backup_delta`)؛ پس از موفقیت، امضای کلیدها آپدیت می‌شود.
  - اگر سرور بکاپ پایه نداشت (`needFull`) → fallback به بکاپ کامل.
- **سرور (`api/crm.php`):** اکشن جدید `save_backup_delta` — دلتا را با آخرین بکاپ کامل (hourly-latest) ادغام می‌کند، counts را بازمحاسبه و با **چرخش مشترک** ذخیره می‌کند (فایل نهایی همچنان کامل است؛ list/get/restore بدون تغییر کار می‌کنند).
- **Refactor:** تابع `ptf_rotate_backup()` مشترک (سپر shrink + gzip + ساعتی/روزانه/هفتگی/ماهانه + آروان) — هم `save_backup` و هم `save_backup_delta` از آن استفاده می‌کنند (بدون تکرار کد).

### F2-3: بازگردانی تراکنشی (تمام‌یا-هیچ) با rollback خودکار
- قبل از جایگزینی، مقادیر قبلی همهٔ کلیدها snapshot می‌شوند.
- اگر **هر کلیدی** به دلیل کمبود ظرفیت نوشته نشود → **rollback کامل** (بازنویسی مقادیر قبلی + حذف کلیدهای جدید) + audit «RESTORE-FAIL» + پیام صریح (بدون reload — دادهٔ قبلی سالم).
- فقط وقتی همهٔ کلیدها موفق باشند → ادامهٔ مسیر قبلی (data_push به سرور + reload).

### F2-2 (آماده‌سازی)
- ساختار دلتا/تلفیقی روی سرور پیاده شد (پایهٔ ذخیره در MySQL پس از مهاجرت DB-MIG-001).

---

## 📋 فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/backup.js` | hashString + deltaSigs/Collect/SaveSentKeys + pushBackupDelta + MaybeAuto دلتا + doRestore تراکنشی + export ها |
| `api/crm.php` | `ptf_rotate_backup()` مشترک + case `save_backup_delta` (ادغام/بازمحاسبه/چرخش) + save_backup بازنویسی‌شده |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.16.0 |
| `_tools/uat/tester296-backup-phase2.js` | **جدید** (۱۰/۰) |
| `_tools/uat/tester295` | به‌روزرسانی انتظار (کامل اولیه → دلتا) |
| `RELEASE-NOTES-v33.16.0.md` / `REGRESSION-REPORT-v33.16.0.md` | اسناد انتشار |

---

## ⚠️ برای کاربر
1. `Ctrl+Shift+R` یا `crm/clear-cache.html` (نسخه باید **v33.16.0** باشد).
2. بک‌آپ خودکار حالا: بار اول کامل، سپس فقط دلتا (حجم ارسال چند برابر کمتر).
3. بازگردانی: اگر حافظه کافی نباشد، دادهٔ قبلی **کامل برمی‌گردد** و پیام راهنما نمایش داده می‌شود (هیچ‌وقت نیمه‌کاره نمی‌ماند).
