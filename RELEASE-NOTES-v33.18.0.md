# یادداشت‌های انتشار (Release Notes — Version 33.18.0) — فاز B: کلاینت نازک

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (مرج به main پس از تکمیل فاز B — تصمیم کارفرما)
**تمرکز:** **فاز B مهاجرت** — کلاینت نازک: سرور (MySQL) منبع حقیقت؛ localStorage فقط کش/آفلاین.

---

## 🚀 فاز B — اجراشده (v33.18.0)

### `crm/client-server.js` (جدید)
- **هوک `getData`:** در حالت فعال، اول از سرور (data_pull با کش ۳۰ ثانیه) می‌خواند؛ آفلاین → کش محلی؛ غیرفعال → رفتار قبلی.
- **هوک `setData`:** در حالت فعال، در کش محلی + **صف آفلاین** (`ptf_b_queue`) + push به سرور (debounced ۴s)؛ خطا → صف می‌ماند.
- **هم‌گرایی یک‌باره:** اولین ورود پس از فعال‌سازی → مودال تأیید → دادهٔ محلی به سرور flush می‌شود (`ptf_b_flushed_<user>`)؛ هرگز حذف نمی‌شود.
- **فعال/غیرفعال‌سازی:** `ptfBEnable/ptfBDisable` — سوییچ‌بک فوری به رفتار قبلی.
- **کلیدها:** همه (فاز B کامل) — از sync/fallback.

### تنظیمات
- باکس «🌐 حالت سرور-محور (فاز B مهاجرت)» با ۳ دکمه: فعال‌سازی / هم‌گرایی دادهٔ محلی / غیرفعال‌سازی.

### لود
- `client-server.js` بعد از `sync.js` در index.html.

---

## 📋 فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/client-server.js` | **جدید** — لایهٔ فاز B |
| `crm/backup.js` | باکس «حالت سرور-محور» در تنظیمات |
| `crm/index.html` | لود client-server.js بعد از sync.js |
| `crm/DESIGN-PHASE-B-CLIENT-SERVER.md` | **جدید** — طراحی/تصمیمات/گام‌های تست |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.18.0 |
| `_tools/uat/tester298-phase-b.js` | **جدید** (۸/۰) |
| `RELEASE-NOTES-v33.18.0.md` / `REGRESSION-REPORT-v33.18.0.md` | اسناد انتشار |

---

## ⚠️ قبل از مرج (گام‌های تست — طبق سند DESIGN)
1. فعال‌سازی روی **استیجینگ** → هم‌گرایی یک‌باره → کارکرد عادی ۲-۳ روز.
2. تست آفلاین (DevTools → Offline): ثبت → صف → برگشت اتصال → push خودکار.
3. سپس مرج → deploy پروداکشن → rollout تدریجی فعال‌سازی.
