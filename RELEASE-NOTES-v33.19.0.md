# یادداشت‌های انتشار (Release Notes — Version 33.19.0) — رفع باگ هم‌گرایی فاز B

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fc272-pishtaj-ir` (ادامهٔ کامیت محلی نشست قبلی `35faba6` که پس از مرج PR #11 دیگر امکان push نداشت — در این نشست بازسازی شد)
**تمرکز:** **رفع باگ هم‌گرایی فاز B** + ابزار آزادسازی حافظه برای دستگاه‌های پرحافظه.

---

## 🐛 باگ گزارش‌شدهٔ کاربر

> «هم‌گرایی داده می‌گوید اتصال برقرار نشد ولی تست اتصال سبز است؛ دکمهٔ اتصال بالای صفحه زرد مانده؛ استورج دستگاه‌ها از ۱۰۰٪ به ۷۰-۹۰٪ رسیده ولی هم‌گرایی fail می‌شود.»

### ریشه (تأییدشده)

1. **`data_push` توکن الزامی می‌خواهد** (`api/crm.php` — `$FINANCIAL_ACTIONS`)؛ وقتی توکن منقضی می‌شد، سرور `401 + needLogin` برمی‌گرداند اما کلاینت فاز B پیام اشتباه **«سرور در دسترس نیست»** نمایش می‌داد. «تست اتصال» از `users_get` **عمومی** استفاده می‌کند و بدون توکن هم همیشه سبز است — همین تناقض کاربر را گمراه می‌کرد.
2. **هم‌گرایی همهٔ ~۵۲ کلید را یک‌جا می‌فرستاد** → روی دستگاه‌های پرحافظه payload چند مگابایتی → رد/تایم‌اوت.
3. **بَج sync زرد** ناشی از همین ارسال‌های ناموفق بود.

---

## ✅ رفع (v33.19.0) — `crm/client-server.js`

### ۱) `serverPush` — نیازمند ورود → بازسازی نشست + retry
- پاسخ `needLogin` / HTTP 401 → فراخوانی `ptfSyncRefreshAuth()` (الگوی F0-3 در backup.js) + **یک بار** تلاش مجدد.
- اگر بازسازی ممکن نبود → `needLogin` به فراخوان برمی‌گردد تا پیام دقیق نمایش داده شود:
  > ⚠️ «نشست شما منقضی شده است؛ دوباره وارد شوید، سپس هم‌گرایی را انجام دهید. دادهٔ محلی شما محفوظ است.»
- پاسخ‌های بدون بدنهٔ JSON (مثلاً 401 با صفحهٔ HTML از سمت پروکسی هاست) هم با بررسی HTTP status به‌درستی `needLogin` تشخیص داده می‌شوند.

### ۲) `ptfBPushBatch` — ارسال دسته‌ای (هر بار ≤ ۲۰ کلید)
- در «هم‌گرایی یک‌باره» و «flush صف آفلاین» به‌جای یک پیلود بزرگ، کلیدها دسته‌به‌دسته ارسال می‌شوند.
- شکست یک دسته (شبکه) → فقط همان دسته ناموفق است و در صف می‌ماند؛ بقیه ادامه می‌یابند.
- `needLogin` → توقف کامل (ادامهٔ دسته‌ها بی‌فایده است) و نگه‌داشتن کلیدهای باقی‌مانده برای پس از ورود.
- گزارش دقیق: `pushed` / `total` / `failed` — پیام هم‌گرایی با شمارش موفق/ناموفق.

### ۳) نشانگر هم‌گرایی موفق `ptf_b_synced_<user>`
- فقط با موفقیت کامل ارسال دسته‌ای ست می‌شود (نه صرفاً زدن دکمه).
- پیش‌نیاز پاک‌سازی کش محلی است.

### ۴) `ptfBClearLocalCache` — پاک‌سازی امن کش محلی (جدید)
برای دستگاه‌های پرحافظه پس از مهاجرت. گاردهای امنیتی (همه الزامی):
1. حالت سرور-محور (فاز B) **فعال** باشد؛
2. هم‌گرایی یک‌باره **موفق** انجام شده باشد (نشانگر `ptf_b_synced_<user>`)؛
3. صف آفلاین **خالی** باشد (هیچ داده‌ای منتظر ارسال نباشد)؛
4. کاربر کلمهٔ **«پاک»** را تایپ کند.

فقط کلیدهای اصلی CRM از localStorage حذف می‌شوند؛ دادهٔ اصلی روی سرور است و با خواندن بعدی `getData` کش دوباره پر می‌شود. رویداد در audit/لاگ ثبت می‌شود.

### ۵) گارد دوباره‌هوک (`__ptfB`)
- قبلاً hook از دو مسیر (boot interval + `ptfBEnable`) اجرا می‌شد و getData دولایه می‌شد؛ حالا با پرچم `__ptfB` فقط یک‌بار اعمال می‌شود.

### تنظیمات (`crm/backup.js`)
- باکس فاز B: دکمهٔ جدید **«🗑 پاک‌سازی کش محلی»** (بین «هم‌گرایی» و «غیرفعال‌سازی») + توضیح کاربر.

---

## 📖 راهنمای کاربران برای دستگاه‌های پرحافظه
1. صفحه را رفرش کنید (Ctrl+Shift+R). ۲) اگر هشدار انقضای نشست آمد → دوباره وارد شوید.
3. «تنظیمات → 🔄 هم‌گرایی دادهٔ محلی» → ✔ موفقیت. ۴) «🗑 پاک‌سازی کش محلی» → تایپ «پاک».

---

## 📋 فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/client-server.js` | serverPush+retry، ptfBPushBatch، پیام دقیق، نشانگر synced، ptfBClearLocalCache، گارد __ptfB |
| `crm/backup.js` | دکمهٔ «🗑 پاک‌سازی کش محلی» + متن راهنما در باکس فاز B |
| `crm/index.html` | بامپ v33.19.0 (VER + ۸۳ cache-buster) |
| `crm/sw.js` | بامپ کش v33.19.0 |
| `crm/clear-cache.html` | بامپ v33.19.0 |
| `_tools/uat/tester298-phase-b.js` | ۸ → **۱۲ تست (۱۲/۰)** |
| `RELEASE-NOTES-v33.19.0.md` / `REGRESSION-REPORT-v33.19.0.md` | اسناد انتشار |
| `crm/BACKLOG-USER-2026-08-01.md` / `_audit/HANDOFF-NEXT-SESSION-2026-08-01.md` | به‌روزرسانی |
| `crm-update.zip` | بازسازی |

---

## ⚠️ گام‌های بعدی (تصمیم کارفرما لازم)
1. مرج PR جدید به main. ۲) GitHub Actions → Deploy to Production (`confirm=DEPLOY, ref=main`).
3. تست واقعی فاز B روی چند دستگاه/مرورگر (هم‌گرایی چنددستگاهی، آفلاین/صف، badge سبز).
