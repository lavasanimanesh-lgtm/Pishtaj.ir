# RELEASE NOTES — v34.7.47 (۱۴۰۵/۰۵/۲۸ — 2026-08-19)

> **یادداشت:** این سند پس از انتشار (در نشست v34.7.54) تکمیل شد؛ محتوا از پیام commit
> `1819315` و قراردادهای tester449 استخراج شده است.

## بارگذاری از درخواست دیگر + جداکننده هزارگان در مودال پیشنهاد

### ۱) دکمهٔ مجزای «بارگذاری از درخواست دیگر»

- در فرم مشترک پیشنهاد مالی (هم «پیشنهاد» و هم «رویژن پرونده» که همان `offerForm` را
  reuse می‌کند) دکمهٔ جدیدی اضافه شد که اقلام درخواست‌های دیگر را به جدول اقلام
  **اضافه** می‌کند، بدون آن‌که شمارهٔ درخواست اصلی پیشنهاد (`ofInq` / `_offState.inqNo`)
  تغییر کند.
- مکانیزم: `offLoadInqItems` گزینهٔ `opt.keepInq` گرفت؛ `offAppendInqRows` در حالت
  `keepInq` شمارهٔ درخواست را بازنویسی نمی‌کند و `offLoadOtherInqApply` با همین گزینه
  صدا می‌زند.
- دکمهٔ قدیمی «بارگذاری از درخواست» حفظ شد؛ وصلهٔ `inqreader` امضا و `apply(this,
  arguments)` را نگه داشته (قراردادهای tester433 نشکسته‌اند).
- بنر فرم رویژن، قابلیت جدید را ذکر می‌کند.

### ۲) مبالغ مودال پیشنهاد

- **جداکنندهٔ هزارگان انگلیسی** برای نرخ مرجع، قیمت واحد و قیمت کل (فیلدها `type=text`
  با `data-money`؛ دیگر `type=number` نیستند).
- **مبلغ به حروف** زیر قیمت واحد به‌صورت لایو (`data-words`).
- **ارقام فارسی** در هر سه فیلد پذیرفته می‌شوند و خروجی نهایی با `offParseMoney` /
  `offPrintAmount` به قالب انگلیسی نرمال می‌شود؛ جمع ردیف و جمع کل نیز با همین مسیر.

### تست

- تستر جدید `tester449-v34.7.47-offer-other-inq-and-money.js` (در گیت CI) — شامل sandbox
  رفتاری برای `keepInq` و پاسداری از قراردادهای tester433/448.

### فایل‌های اصلی

`crm/offers.js` · `crm/inqreader.js` · `crm/moneyx.js` · نقاط رسمی نسخه
