# 📦 Release Notes — v66.0
**اسپرینت ۶۶: ابزارهای مهندسی (US-126) + دستیار قرارداد (US-119)**
**تاریخ:** ۱۴ تیر ۱۴۰۵ (2026-07-03)

---

## 🛠 US-126 — صفحه «ابزارهای مهندسی» (`tools/` + موتور `assets/js/ptf-tools.js`)

۶ ابزار آنلاین رایگان — کاملاً سمت کلاینت، سئوشده، در منوی اصلی همه صفحات (۳۱۶ صفحه):

| ابزار | قابلیت | مرجع |
|---|---|---|
| سایزینگ ولو کنترلی | Cv/Kv مایع/گاز/بخار + سایز پیشنهادی + درصد باز بودن + **هشدار کاویتاسیون/چوک/ویسکوزیته** | IEC 60534 ساده‌شده |
| سایزینگ آن/آف | کنترل سرعت خط (مایع ≤4، گاز ≤20 m/s) با ID واقعی از جدول لوله | راهنمای سرعت |
| سایزینگ PRV | سطح تخلیه + **اریفیس D تا T** + هشدار سناریوی آتش | API 520/526 |
| وزن متریال | لوله (۱۶ سایز × ۳ اسکجول × ۴ متریال)، فلنج WN (۵ کلاس)، الو ۹۰ + **سبد چندقلمی با جمع** | B36.10/B16.5/B16.9 |
| تحمل فشار | MAWP با تلرانس ضخامت ۱۲.۵٪ + کاهش دما + ضریب جوش E + خوردگی CA | ASME B31.3 |
| ترک‌تیبل | جدول ۰-۱۰۰٪ باز ↔ Cv٪ ↔ Cv ↔ دبی برای Linear/EQ%/QO | R=50 |

- **AC7:** خروجی PDF مهندسی هر ابزار: لوگو + شماره محاسبه `PTF-CALC` + ورودی/خروجی + مرجع + سلب مسئولیت
- **AC10:** دکمه «استعلام این تجهیز» → RFQ پیش‌پرشده با نتیجه محاسبه
- ادغام با چت (کلیدواژه‌های سایزینگ/محاسبه) + sitemap + Schema WebApplication

## 📜 US-119 — دستیار قرارداد (`crm/contracts.js` — پنل جدید CRM)

- **AC1 — ویزارد:** انتخاب CO (اقلام/مبلغ/طرف خودکار خوانده می‌شود) + پرسشنامه: خرید/فروش، داخلی/خارجی، اینکوترمز (Incoterms 2020)، پیش‌پرداخت و تسویه، تحویل، گارانتی، جریمه تاخیر با سقف، تضامین (چک/ضمانت‌نامه)، مرجع اختلاف (دادگاه/داوری اتاق/ICC)، TPI، زبان (فا/EN/**دوزبانه**)
- **AC2 — موتور rule-based:** کتابخانه ۱۱ ماده استاندارد فارسی + انگلیسی با جایگذاری خودکار داده‌های CO (مبلغ به عدد و حروف، خلاصه اقلام…)
- **AC3 — هشدار تناقض:** پیش‌پرداخت ۱۰۰٪+ضمانت‌نامه / قرارداد خارجی+دادگاه داخلی / داخلی+ICC
- **AC4:** ویرایشگر متن + **نسخه‌گذاری (rev)** + خروجی **PDF روی سربرگ رسمی** (نوار گرادیان + فونت یاقوت فا / لاتین EN) + محل امضای طرفین
- **AC6:** سلب مسئولیت «نیازمند بازبینی حقوقی» روی سند ✔ | دسترسی: فقط نقش‌های ارشد (RBAC)
- لایه LLM (AC3 کامل): طبق برنامه با دریافت کلید API روی همان پروکسی موجود فعال می‌شود

## 🧪 تست‌ها — ۱۵/۱۵ PASS
موتور ابزارها (۱۱ تست): Cv مایع=۳۳.۴ ✔ کاویتاسیون ✔ چوک گاز ✔ سرعت خط ✔ اریفیس PRV ✔ وزن لوله ۶"S80×12m=510.7kg ✔ فلنج 600#8"×4=192kg ✔ MAWP≈191bar ✔ اثر CA ✔ EQ%@50=14.1% ✔ Linear@50=50% ✔
قرارداد (۴ تست): سریال ✔ جایگذاری ✔ توازن بندهای فا/EN ✔ تشخیص تناقض ✔ + ممیز سایت PASS ✔

## 🚀 دیپلوی
جدید: `tools/` (صفحه + UI)، `assets/js/ptf-tools.js`، `crm/contracts.js` | تغییر: ۳۱۶ صفحه (منو)، `crm/index.html`، `ptf-chat.js`، `sitemap.xml` → **دیپلوی کامل توصیه می‌شود**

## ⏭️ اسپرینت بعدی (۶۷)
US-112 + US-115 + US-129 — PWA (نصب iOS/اندروید)، دکمه نصب صفحه ورود، منوی دسته‌بندی آکاردئونی CRM.
