# یادداشت انتشار v34.38.25 — CONTACT-STALE-HOOK-WIPE (پاک‌شدن تماس‌ها با «آخرین‌ثبتِ تب»)

**تاریخ:** 2026-09-17
**بازوی تغییر:** Arena Agent
**کانال:** CRM (دادهٔ مشتریان/تامین‌کنندگان — ذخیره، ضدتکرار، سینک)
**RCA کامل:** `ARENA-CRM-CONTACT-STALE-HOOK-WIPE-RCA-2026-09-17.md` (ریشهٔ ریپو)

## گزارش کارفرما
> «هر مشتری که مدیرعامل با شماره ثبت می‌کند، رکورد می‌ماند ولی شماره‌ها برای من (رییس هیات مدیره) ناپدید است؛
> مشتری‌هایی که خودم ثبت می‌کنم سالم‌اند.»

## نتیجهٔ بررسی

**گمان «تبعیض نقشی» رد شد.** بازتولید کامل با کد واقعی HEAD (بوت vm با ترتیب defer واقعی + مسیر واقعی
مودال + سرور واقعی‌نما با merge دقیق) ریشهٔ یکسان و نقش‌کور را نشان داد:

**هوک قالب‌بندهٔ شماره‌ها (`crm/phonefmt.js`) پس از هر `saveCust2`/`saveSup2` — حتی ذخیره‌های
زودبرگشتی (بلاک ضدتکرار US-174 یا نام خالی) — «آخرین رکورد ذخیره‌شدهٔ همان تب» را از کشِ محلیِ کهنه با
`expectCreate:false` دوباره upsert می‌کرد.** merge سرور «کلیدِ حاضر بازنویسی» هر تغییرِ بعدیِ دستگاه‌های
دیگر (شماره/ایمیلِ اضافه‌شده) را می‌شست؛ دستگاهِ ثبت‌کننده از کشِ خودش نسخهٔ تماس‌دار را می‌دید.

چرا نقش‌محور به نظر می‌رسید؟ مدیرعامل معمولاً **آخرین ثبت‌کنندهٔ تب** است: ثبت‌های خودش از کشِ خودش سالم
دیده می‌شود؛ رکوردی که رییس بعداً تکمیلش کرده اولین قربانیِ ذخیرهٔ زودبرگشتیِ بعدیِ تبِ مدیرعامل است
(دوبار‌کلیک، ثبت هم‌نام، …). اگر رییس آخرین ثبت‌کننده می‌بود جهتِ آسیب برعکس می‌شد.

## اصلاح (سه‌لایه)

1. **`crm/offers.js`:** نرمال‌سازی/قالب‌بندی شماره‌ها (وظیفهٔ واقعی هوک) داخل `saveCust2`/`saveSup2` و
   **پیش از** ضدتکرار و ساخت payload اجرا می‌شود ⇒ فرمان اصلی و مسیر retry برخورد کد همیشه دادهٔ نهایی
   را می‌برند. حالت «خارجی» تامین‌کننده از فرم خوانده می‌شود (شاخهٔ EN هوک قدیمی چون `rec.origin` هیچ‌گاه
   ست نمی‌شد عملاً مرده بود — حالا رفتار مقصود US-415 زنده است).
2. **`crm/phonefmt.js`:** قرارداد تازهٔ هوک — فقط «ویرایش صریح» (cd مشخص و برابر مهر جاری) تایید مجددِ
   تک‌رکوردی می‌شود؛ ثبت جدید و ذخیرهٔ زودبرگشتی **هیچ فرمان شبکه‌ای ندارند**. (بند ۲.۹ قدیمی tester604
   که همین رفتار معیوب را قفل کرده بود، عمداً و با مستندسازی به‌روز شد.)
3. **`api/sales-domain.php`:** invariant **CONTACT-WIPE** در `entity_upsert` — اگر رکورد ذخیره‌شده تماس
   دارد و payload «هر کلید تماسی که می‌آورد» خالی است، پاک‌سازی نیت صریح می‌خواهد (`_ccClear` از کلاینت
   جاری؛ فیلد نیت پیش از ذخیره حذف می‌شود)؛ در غیر این صورت مقادیر قبلی حفظ و در
   `sanitize.contactsWipeBlocked` شمرده می‌شود. کلاس کامل «پاک‌سازی تماس توسط کلاینت کهنه/معرور» برای
   هر نویسنده بسته می‌شود.

## سطح انتشار نسخه (معمول پروژه)
`VERSION.json → v34.38.25`؛ `crm/index.html` (۱۱۶ نشانه + PTF_CRM_RELEASE)؛ `crm/sw.js`؛
`crm/manifest.json`؛ `crm/clear-cache.html`؛ `crm/shell.js`؛ `crm/device-reconnect.html`؛
`crm/cms.js` (`PTF_CMS_JS_VER`)؛ `api/sales-domain.php` (`SD_SERVICE_VERSION = '34.38.25'`).

## تست‌ها
- **قفل جدید:** `_tools/uat/tester660-v34.38.25-contact-stale-hook-wipe.js` — ۳۷ سنجه (ایستا + پورت
  رفتاری گارد PHP + زنجیرهٔ کامل سه‌دستگاههٔ گزارش با بوت واقعی dedup→offers→phonefmt→sales-domain-v2):
  ثبت جدید فقط یک فرمان؛ بلاک ضدتکرار صفر فرمان پنهان؛ تماس‌های اضافه‌شدهٔ رییس برای دستگاه سوم سالم؛
  زنجیرهٔ تامین‌کننده؛ قالب‌بندی خارجی پیش از payload؛ گارد سرور (حفظ/مجوز/عبور/کلید غایب/stub/حقیقی-حقوقی).
- **tester604:** بند ۲.۹ عمداً به قرارداد تازهٔ هوک به‌روز شد (+۲.۱۱) — ۴۵ سنجه سبز.
- **گیت کامل:** `node _tools/uat/run-ci-gate.js` → **272 PASS / 1 FAIL** — تنها FAIL،
  `tester656` است که پیش از این نسخه هم (در HEAD بدون PHP) به همین شکل قرمز بود
  (بلوک رفتاری PHP در محیط بدون php-cli رد می‌شود و خط خلاصهٔ PASS چاپ نمی‌شود — محیطی، خارج از دامنه).

## ملاحظات رول‌اوت
- تب‌های بازِ قدیمی تا ریفرش با کد کهنه اجرا می‌شوند؛ invariant سمت سرور (لایهٔ ۳) همان‌ها را هم خنثی
  می‌کند — نیازی به هماهنگی دستگاه‌ها نیست.
- داده‌های قبلاً آسیب‌دیده با این نسخه ترمیم نمی‌شوند (فقط جلوگیری از آسیب جدید)؛ بازیابی در صورت نیاز
  از بایگانی/بکاپ طبق روال.

---

# بخش دوم همان نسخه — سایت/صفحهٔ اول: HOME-MODERN-FAB-CHAT

## گزارش کارفرما
> «می‌خواهم ببینی آیا می‌توان ظاهر خیلی مدرن‌تر و داینامیک‌تر و کاربرپسندتر با حفظ پرستیژ محتوا و تم شرکت به صفحهٔ اول داد؟
> اندازهٔ آیکون‌های چت و تماس و واتساپ بزرگ و غیرمتناسب هستند — درستشان کن.
> پنجرهٔ چت هوشمند هم زیر آیکون واتساپ باز می‌شود و مزاحمت ایجاد می‌کند.»

## بررسی و اصلاح

**چرا پنجرهٔ چت زیر واتساپ باز می‌شد؟** پنجرهٔ چت `z-index:9991` داشت؛ آیکون واتساپ `9999` — یعنی
دایرهٔ سبز واتساپ **روی** پنجرهٔ بازشده می‌ماند (و دکمهٔ تماس هم کامل زیر پنجره دفن می‌شد).

**① FAB-CHAT-HARMONY (`assets/js/ptf-chat.js` — روی هر ۵ صفحه):**
- پنجرهٔ چت به `z-index:10050` رفت (بالاتر از واتساپ ۹۹۹۹ و تماس ۴۵).
- هنگام باز بودن چت، `body` کلاس `ptf-chat-open` می‌گیرد و دکمه‌های تماس/واتساپ با ترنزیشن محو و
  غیرقابل‌کلیک می‌شوند — پنجره همیشه بدون مزاحمت باز می‌شود؛ با بستن برمی‌گردند.
- بستن با **ESC** + انیمیشن نرم بازشدن (scale/translate از گوشهٔ لانچر).

**② نظام واحد دکمه‌های شناور (`assets/css/home.css`):** سه قطر ناهماهنگ ۵۸/۵۸/۵۴ → یکنواخت
**۴۸px** (موبایل ۴۴px)؛ پشتهٔ چپ‌پایین با فاصلهٔ یکنواخت (چت ۲۰ ← تماس ۸۲ ← واتساپ ۱۴۴)؛
آیکون‌ها متناسب (svg واتساپ ۲۳px، موبایل ۲۱px)؛ hover یکدست (بالا ۳px + اسکیل ۱.۰۶ + سایهٔ برند)؛
هندلرهای درون‌خطی `onmouseover` واتساپ حذف شد.

**③ لایهٔ مدرن فقط-نمایشی (`home.css?v=34.38.25`):** ورود سینمایی پلکانی هیرو، هالهٔ نارنجیِ زندهٔ
ملایم روی اورلی، پالس نقطهٔ eyebrow، ورود پلکانی reveal‌ها (stagger)، خط برند + lift روی کارت‌های
journey/why-ptf/stats/trust، درخشش عبوری دکمهٔ اصلی، خط برند کنار کیکرها، مکث مارکی روی hover،
FAQ بازشدنی تمیز، فوکوس‌رینگ دسترس‌پذیری، **نوار پیشرفت اسکرول** (rAF/passive) و رعایت کامل
`prefers-reduced-motion`. تم و محتوا (گرادیان نارنجی/قرمز، متن‌ها، اسکیما، هوک‌ها) بایت‌به‌بایت حفظ شده.

## تست
- **قفل جدید:** `tester661` (۲۸ سنجه؛ ثبت در گیت) — سلسله‌مراتب z-index با مقایسهٔ عددی از خود سورس،
  هماهنگی باز/بسته، اندازه‌ها/پشته، لایهٔ مدرن، و نگهبان محتوا (هوک‌های journey/فرم استعلام/h1/لینک‌ها).
- رگرسیون‌های صفحه: tester210/216/220/318/207 سبز + گیت کامل.
