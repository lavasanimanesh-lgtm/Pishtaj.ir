# 📦 ریلیزنوت v13.9 — یادآورهای فرآیندی، راهنمای دکمه‌ها، تور معرفی، حذف سفارشات، قراردادها v2

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v13.8

---

## ⏰ US-341 — یادآورهای مهم فرآیندی (مصوبه تیم تخصصی)
تیم با تحلیل چرخه کار بازرگانی شرکت، این موضوعات را به فهرست یادآورها اضافه کرد:
**سررسید چک صادره** · **اعتبار پیشنهاد مالی (Validity)** · **پیگیری پاسخ درخواست تامین** · **پیگیری وندور لیست** · **تمدید ضمانت‌نامه بانکی** · **ترخیص و حمل کالا** · **تحویل به کارفرما** · **جلسه/بازدید** (+ موضوعات قبلی)

## 🔴🟡🟢 US-342 — راهنمای دکمه‌های کنترل مودال (تصویر مرجع شما)
دقیقاً مثل مک: با قرار گرفتن ماوس روی دکمه‌ها، نمادها داخل دایره‌ها ظاهر می‌شوند — **✕** (بستن) / **−** (ارسال به پایین با حفظ اطلاعات) / **⤢** (تمام‌صفحه) — و با tooltip متنی.

## 🎓 US-343 — معرفی اجمالی گرافیکی ماژول‌ها (Spotlight Tour)
دقیقاً طبق توصیف شما:
- بخش هدف **روشن** می‌ماند و بقیه صفحه **تیره** می‌شود
- کارت توضیح کنارش: «از این قسمت می‌توانید پیشنهادهای فنی، مالی و فنی-مالی خود را ثبت کنید» → دکمه «بعدی» → ماژول بعد
- ۱۸ گام؛ فقط ماژول‌های **مجاز همان کاربر** (RBAC خودکار)
- **اجرای خودکار یک‌باره** برای هر کاربر در اولین ورود + اجرای مجدد از تنظیمات ← «🎓 اجرای مجدد معرفی ماژول‌ها»
- ناوبری کامل: بعدی/قبلی/رد کردن

## 💰 US-344 — حذف «سفارشات و سود» (بررسی تیم تخصصی)
**تشخیص تیم — حق با شماست:** این ماژول legacy با ورود دستی قیمت خرید/فروش بود (کلید جداگانه)، موازی با «مالی»، «مطالبات» و «سود خالص» که همه از داده واقعی (CO/فاکتور/خرید) محاسبه می‌کنند. **از منو حذف شد** (حذف نرم — داده و لینک قدیمی سالم).

## 📜 US-345 — قراردادها (نسخه ۲)
- نام: «دستیار قرارداد» → **«قراردادها»** (همه‌جا)
- **قرارداد خرید → لینک به درخواست تامین:** با انتخاب نوع «خرید»، مبنا دیگر CO نیست — فهرست **درخواست‌های تامین** (استعلام‌هایی که به تامین‌کننده فرستاده‌اید) می‌آید؛ قرارداد با نام تامین‌کننده و اقلام همان درخواست ساخته و به آن لینک می‌شود
- قرارداد فروش: مبنا CO یا TC (قبلاً فقط CO)
- ستون «مبنا» در فهرست قراردادها (شماره CO یا درخواست تامین)
- **🤖 بررسی متن با هوش مصنوعی:** متن پیش‌نویس/شرایط طرف مقابل را بچسبانید → AI به فارسی حقوقی: ⚠️ **ریسک‌ها و بندهای مبهم** + 📝 **متن نهایی پیشنهادی کامل** → دکمه «✅ استفاده از این متن» یا کپی (اکشن سروری جدید با پرامپت حقوقی اختصاصی)

## 📖 US-346 — راهنمای کامل نرم‌افزار (ثبت یوزر استوری)
طبق دستور، در بک‌لاگ ثبت شد: راهنمای مصور کامل (صفحه per ماژول + جستجو + دکمه ? در هر پنل) — پس از کامل شدن دامنه تغییرات، طراحی و پیاده‌سازی می‌شود.

## 🧪 کیفیت
تستر جدید `tester59` ۲۴/۲۴ ✅ | رگرسیون **۴۶ تستر** ✅ | audit صفر خطا ✅

## 📁 فایل‌های تغییریافته
`crm/tour.js` (جدید) · `crm/leads.js` (یادآورها) · `crm/modalx.js` (hover) · `crm/contracts.js` (v2 کامل) · `api/llm.php` (اکشن contract) · `crm/index.html`/`shell.js`/`perms.js`/`sw.js` · `_tools/uat/tester59` (جدید)

⚠️ `api/llm.php` تغییر کرده — در آپلود جایگزین شود.
