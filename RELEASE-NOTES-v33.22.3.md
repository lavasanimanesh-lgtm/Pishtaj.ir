# RELEASE-NOTES — v33.22.3 (P1-ATTACH-STALE-DB)

**تاریخ:** ۲۰۲۶-۰۸-۰۳
**نوع:** رفع فوری رخداد تولید (P0) — «ناپدید شدن ضمایم/رکوردهای تازه پس از سوییچ MySQL»
**وضعیت:** روی استیجینگ — در انتظار تأیید تست کارفرما برای مرج

---

## ۱) علائم رخداد (گزارش کارفرما)

> «بیشتر ضمایم، به‌خصوص جدیدترها، نمایش داده نمی‌شوند — حتی با باز کردن تب جدید — در هیچ‌کجای نرم‌افزار؛ از ضمایم درخواست تا ضمایم تنخواه و غیره.»

## ۲) ریشه‌یابی (مستند، از کد + وضعیت زندهٔ سرورها)

ضمایم در این سامانه به‌صورت ارجاع `{key, name, size}` **داخل خودِ رکوردها** (درخواست‌ها، تنخواه، قراردادها و…) نگهداری می‌شوند و فایل‌های واقعی در فضای ابری آروان است — هیچ‌کدام دیروز تغییری نکرده‌اند.

زنجیرهٔ ریشه (سه نقص که با هم رخداد را ساختند):

1. **کپی‌های کهنهٔ کلیدهای سینک در DB.** تا v33.22.0 مسیر سینک ۵۲ کلید فقط فایل می‌نوشت؛ پس ردیف‌های DB آن‌ها فقط با «گام انتقالِ ویزارد» تازه می‌شد. از لحظه‌ای که v33.22.x با سیم‌کشی سینک به MySQL روی پروداکشن رفت و `mode=mysql` بود، خواندن آن کلیدها از DB انجام شد — و هر کلیدی که آخرین انتقال موفق به آن نرسیده بود، **به حالت چندروزِ قبل برگشت** → ارجاع ضمایم جدید در رکوردها موجود نبود → هیچ 📎 در هیچ ماژول/تب/دستگاهی رندر نمی‌شد. (فایل‌ها — بکاپ گرمِ همیشه‌نوشته — تازه و سالم بودند؛ health-check سبزِ دیروز فقط فایل‌ها را می‌دید.)
2. **گام «سوییچ نهایی» هیچ اجباری به تطابق نداشت.** ترتیب مراحل فقط توصیه بود؛ سوییچ بدون اجرای کامل مرحلهٔ ۳/۴ ممکن بود.
3. **«بررسی تطابق» روی سامانهٔ زنده همیشه یک مغایرت نشان می‌داد** — چون کلیدهای فرّارِ عمداً فایل‌محور (`meta` = دفتر rev سینک و `tokens` = نشست‌های ورود) هم جزو مقایسه بودند و ذاتاً مدام عوض می‌شوند. این باعث می‌شد مغایرت «عادی» به نظر برسد و سوییچ با DB ناقص جلو برود.

**چرا فقط نمایش، نه داده:** فایل‌ها همیشه «قبل از DB» نوشته می‌شوند و هرگز حذف نشده‌اند؛ پس هیچ داده‌ای از دست نرفت — فقط نمایش از منبع کهنه تغذیه می‌شد.

## ۳) رفع فوری (بدون نیاز به دیپلوی — قبلاً به کارفرما ابلاغ شد)

دکمهٔ «بازنشانی» در `api/migrate.php` (v33.22.1) وضعیت را به `off` برمی‌گرداند → خواندن فوراً به فایل‌های تازه برمی‌گردد → ضمایم همان لحظه برمی‌گردند. سپس اجرای مراحل ۳←۶ ویزارد، DB را از فایل‌های تازه ترمیم می‌کند.

## ۴) ریشه‌کن دائمی (این نسخه — سه لایه)

### لایهٔ ۱ — گارد تازگی لحظه‌ای (`db-lib.php` → `ptf_db_read_fresh`)
قرارداد نوشتن سامانه «فایل اول، سپس DB» است؛ پس اگر `mtime` فایل از `updated_at` ردیف DB جلوتر باشد (+۳ ثانیه حاشیهٔ امن)، آن ردیف **یقیناً** کهنه است:
- مقدار تازهٔ فایل سرو می‌شود (کاربر هرگز دادهٔ عقب‌افتاده نمی‌بیند)
- همان لحظه ردیف DB خودترمیم می‌شود (خواندن بعدی از DB تازه می‌خواند)

`load_data` و `sync_key_read` (هر دو مسیر خواندن) به این گارد متصل شدند؛ `ptf_db_read` خام برای مقایسهٔ دقیق ویزارد حفظ شد. **نتیجه: از این پس حتی مهاجرت اشتباه/ناقص هم نمی‌تواند خواندن را از فایل عقب‌تر نگه دارد.**

### لایهٔ ۲ — ویزارد مهاجرت (`migrate.php`)
- **سوییچ نهایی بدون تطابق کامل ممنوع:** خودِ گام سوییچ، درون‌خط چک‌سام+تعداد همهٔ کلیدهای داده را بررسی می‌کند؛ با حتی یک مغایرت، سوییچ انجام نمی‌شود و فهرست دقیق + راهنمای ترمیم نشان داده می‌شود (`pre_switch_verified_at` ثبت می‌شود).
- **کلیدهای فرّارِ عمداً فایل‌محور (`meta`, `tokens`) از مقایسه مستثنا شدند** (`mig_compare_excluded`) — مغایرتِ دروغینِ «همیشگی» حذف شد و در جدول بررسی به‌صورت «ℹ️ فایل‌محورِ دائمی» نمایش داده می‌شوند.
- **انتقال داده دسته‌ای و ضد تایم‌اوت:** هر درخواست حداکثر ۱۵ کلید + ادامهٔ خودکار دستهٔ بعد + نمایش پیشرفت «X از Y» + گزارش صریح ناموفق‌ها — روی هاست اشتراکی (سقف ۳۰ ثانیه) انتقال ناقص دیگر پنهان نمی‌ماند. عمل idempotent است (Insert … ON DUPLICATE KEY UPDATE) و تکرار آن کاملاً امن است.
- جدول «بررسی تطابق» حالا بایت هر طرف و وضعیت «(در DB نیست)» را هم نشان می‌دهد.

### لایهٔ ۳ — تشخیص (`data-health-check.php`)
- بخش ۱۱ «تطابق فایل ↔ دیتابیس»: نمایش `mode` + مقایسهٔ چک‌سام/تعداد هرکلید بین فایل و DB (با همان استثناهای meta/tokens). در `mysql` هر مغایرت **خطا** و در `dual` **هشدار** است. سبزِ بعدی واقعاً به معنای سلامت DB است.
- نسخهٔ ابزار به v33.22.3 بروز شد تا زنده‌بودن نسخهٔ جدید از بیرون قابل راستی‌آزمایی باشد.

## ۵) پرونده‌های تغییرکرده

| فایل | تغییر |
|---|---|
| `api/db-lib.php` | +`ptf_db_read_fresh` (گارد تازگی + خودترمیمی) |
| `api/crm.php` | اتصال `load_data` و `sync_key_read` به گارد تازگی |
| `api/migrate.php` | سوییچ مشروط به تطابق کامل + استثنای meta/tokens + انتقال دسته‌ای خودکار + جدول بررسی غنی‌تر |
| `api/data-health-check.php` | بخش ۱۱ تطابق واقعی فایل↔DB + نسخهٔ ابزار |
| `crm/index.html` / `crm/sw.js` / `crm/clear-cache.html` | بامپ v33.22.3 (VER + ۸۳ باستر + نام کش SW) |
| `_tools/uat/tester304-p1-attach-stale-db.js` | تستر دائمی جدید (۱۲ تست) |
| `_tools/uat/tester301-mysql-sync-wire.js` | به‌روزرسانی pin خواندن به قرارداد گارد تازگی (+۱ تست: اتصال load_data) |

## ۶) سازگاری عقب‌رو و ریسک

- **بدون تغییر در کلاینت** (مثل v33.22.0/1) — کاربران نهایی کاری انجام نمی‌دهند.
- رفتار `mode=off` و `dual` دست‌نخورده؛ گارد تازگی فقط در `mysql` فعال است.
- خودترمیمی فقط «نوشتن مقدار تازهٔ فایل در DB» است (همان کاری که گام انتقال می‌کرد) — idempotent و بدون حذف.
- هزینهٔ اجرا: همان یک کوئری قبلی با یک ستون اضافه (`updated_at`) — بدون کوئری جدید.

## ۷) روش تست استیجینگ (چک‌لیست کارفرما)

1. باز کردن `https://staging.pishtaj.ir/api/data-health-check.php?confirm=yes` → بخش ۱۱ «تطابق فایل ↔ دیتابیس» باید دیده شود؛ در استیجینگ (mode=mysql) باید «همه یکسان ✅» باشد یا مغایرت‌ها را صریح فهرست کند.
2. در صورت داشتن مغایرت: اجرای «بازنشانی» ← مراحل ۱←۶ — مرحلهٔ ۳ باید با **پیشرفت دسته‌ای (X از Y) و ادامهٔ خودکار** جلو برود و مرحلهٔ ۴ فقط دو سطر «ℹ️ فایل‌محورِ دائمی» (meta/tokens) داشته باشد و بقیه ✅ یکسان.
3. گام ۶: اگر مغایرتی بماند، سوییچ باید **مسدود** شود و فهرست نشان دهد.
4. CRM استیجینگ: نمایش ضمایم چند رکورد اخیر (درخواست/تنخواه) — باید مثل قبل باز شوند.

## ۸) راهنمای کاربران پرحافظه (یادآوری از v33.22.2 — بدون تغییر)

کاربری که به‌خاطر پر شدن حافظهٔ مرورگر نمی‌تواند وارد شود: در صفحهٔ ورود «🔄 رفع مشکل ورود (بدون حذف داده)» را بزند؛ دادهٔ اصلی روی سرور امن است و کلیدهای سنگین خودکار به IndexedDB منتقل می‌شوند. راه‌حل دائمی: فعال‌سازی «حالت سرور-محور» از تنظیمات.
