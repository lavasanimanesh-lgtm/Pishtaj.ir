# یادداشت انتشار — v34.38.0

**تاریخ:** ۲۰۲۶-۰۹-۰۷ (۱۴۰۵/۰۶/۱۶) · **پایه:** v34.37.8
**گیت CI:** تسترهای قفل‌شدهٔ خانوادهٔ CUST-VANISH (tester595/tester604/tester525/tester594/tester605) + EVENT-DEDUP-TS + ۲۲۰ تستر جامع گیت CI

---

## ① خواندن داده‌ها از mirror همگام‌شده پس از رفرش (CUST-MIRROR-READ)

- **ریشه:** فاز B کلیدهای سنگین (مشتریان/استعلام‌ها/پرونده‌ها) را از `localStorage` به حافظه/IndexedDB منتقل کرده است؛ `getData` بعد از refresh فقط `localStorage` را می‌خواند و برای این کلیدها `[]` برمی‌گرداند → پنل‌ها «دادهٔ ثبت‌شده را ناپدید» نشان می‌دادند (خانوادهٔ «پاک شدن اطلاعات مشتریان ثبت‌شده»).
- **اصلاح:** `getData` در `crm/index.html` ابتدا همان projection همگام‌شده را که sync.js در IDB می‌نویسد (`window.ptfBRead`) می‌خواند و نتیجهٔ آن بر fallback قدیمی می‌چربد؛ fallback فقط همان خط legacy ثبت‌شده در baseline نگهبان A10 است (هیچ بایپس جدیدی به لایهٔ داده اضافه نشد).
- **اثر:** مشتری/استعلام/پروندهٔ تازه‌ثبت‌شده پس از رفرش یا بازگشایی مجدد CRM همچنان دیده می‌شود؛ هیچ داده‌ای حذف نمی‌شود.

## ② ماندگاری سنگ‌قبرهای رویداد پروندهٔ فروش (TOMBSTONE-DURABLE)

- **ریشه:** در `ptfMergeBusinessRecord` سنگ‌قبرهای `_costTomb`/`_qcTomb`/`_shipTomb`/`_docTomb` پس از ۲۰۰ مدخل هرس می‌شدند → کهنه‌ترین حذف‌ها در sync بعدی رستاخیز می‌کردند.
- **اصلاح:** حذف کامل حلقه‌های هرس ۲۰۰تایی در `crm/sync.js`؛ سنگ‌قبرها ماندگارند (هم‌خانواده با سیاست tombstoneهای `archive_purge` سمت سرور). تستر قفل‌شدهٔ tester583 به همین معنا به‌روزرسانی شد (TOMB-DURABLE).

## ③ رفع رگرسیون ددوب رویدادهای هم‌کد پرونده (EVENT-DEDUP-TS)

- **زمینه:** v34.37.8 منتشرشده قاعدهٔ «نسخهٔ ویرایش‌شده برنده است» را داشت؛ در اصلاح میانیِ پس از آن (منتشرنشده) مقایسهٔ ددوب `costEvents`/`qcEvents`/`shipEvents` به `ptfRecTimestamp` خامِ رشته‌ای تغییر کرد که ① `updatedT` را نمی‌خواند و ② مقدارهای نامعتبر/کهنهٔ `t|ts` را lexicographically «جدیدتر» از ISO واقعی نشان می‌داد → نسخهٔ ویرایش‌شدهٔ (`updatedT`) در ددوب به نسخهٔ کهنه می‌باخت.
- **اصلاح:** مقایسه‌گر امن `ptfEventDedupNewer` — فقط timestamp واقعیِ قابل‌parse سنجیده می‌شود (ناخوانا = قدیمی‌ترین)؛ در تساوی نسخهٔ ویرایش‌شده برنده است؛ وگرنه رویداد نخست (ثبات) می‌ماند.

## ④ انتشار مجموعه تامین‌کنندگان (SUP-RELEASE)

- نمایش خطاهای انقضای کپچا و بازیابی فرم (`ptfCaptchaReset`)، گارد سقف میزبان (`upload_limits`/413 با پیام فارسی)، نرمال‌سازی شماره موبایل (ارقام فارسی/عربی و 0098/+98 → 09)، هم‌ترازی `accept` فیلد فایل با allowlist سرور، و کش‌باست یکپارچه به `v34.38.0`.

---

## ⑤ فایل‌های تغییر یافته
- `crm/index.html`: خواندن mirror-first در `getData` (بدون بایپس جدید A10) + ارتقای هماهنگ نسخه.
- `crm/sync.js`: ماندگاری سنگ‌قبرها + مقایسه‌گر امن ددوب رویدادها (`ptfEventDedupNewer`).
- `supplier/index.html`: ترتیب ویژگی‌های فیلد پیوست (هم‌ترازی با allowlist) + کش‌باست `ptf-guard.js?v=34.38.0`.
- `assets/js/ptf-guard.js`, `api/crm.php`: گاردهای تامین‌کنندگان (قبلاً در شاخه با برچسب v34.38.0).
- `VERSION.json`, `crm/sw.js`, `crm/manifest.json`, `crm/clear-cache.html`, `crm/shell.js`, `crm/cms.js`, `api/sales-domain.php`: ارتقای هماهنگ نسخه به `v34.38.0`.
- `_tools/uat/tester583-v34.29.8-deal-cost-tomb-and-pl-void-delete.js`: قفل SRC به سیاست «سنگ‌قبر ماندگار» به‌روزرسانی شد.
- صفحات عمومی سئو (بستهٔ a3d): توضیحات کامل صفحات + سلب ایندکس صفحات بی‌محتوا.

---

## ⑥ وضعیت آزمون‌ها و گیت CI
- اجرای کامل گیت سراسری با `node _tools/uat/run-ci-gate.js`: **۲۲۰ آزمون سبز / ۰ شکست (100% Pass)**.
- نگهبان معماری (Arch Guard): **PASS** (A6 هماهنگ + A10 بدون تخلف جدید).
- تسترهای قفل‌شدهٔ خانوادهٔ «پاک شدن مشتریان ثبت‌شده» (tester595-v34.37.0-tombstone-scope، tester604-v34.37.7-contact-wipe، tester525-v34.8.34-w1-entity-router، tester594-v34.36.5-cust-rfq-orphan، tester605-v34.37.8-custom-lines-and-tombstone-sync) همگی سبزند.
