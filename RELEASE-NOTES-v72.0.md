# 🚀 ریلیز v72.0 — اسپرینت ۷۲: کپچا برای همه فرم‌ها + تایید پیامکی شماره تامین‌کننده
**تاریخ:** تیر ۱۴۰۵ | **استوری:** US-149

---

## 🤖 ۱) کپچای «من ربات نیستم» — هر ۳ فرم (AC1)

| فرم | وضعیت |
|---|:---:|
| فرم استعلام سریع (صفحه اصلی) | ✅ |
| فرم استعلام هوشمند (rfq/) | ✅ |
| فرم ثبت تامین‌کننده (supplier/) | ✅ |

**نحوه کار:** کاربر تیک «من ربات نیستم» را می‌زند → یک جمع ساده (مثلاً 3 + 4) از **سرور** دریافت می‌شود → پاسخ + توکن امضاشده (HMAC با انقضای ۱۵ دقیقه) همراه فرم ارسال می‌شود.

**نکته امنیتی مهم:** اعتبارسنجی **سمت سرور** انجام می‌شود (`require_captcha`) — یعنی حتی اگر کسی جاوااسکریپت را دور بزند و مستقیم به API درخواست POST بفرستد، بدون کپچای معتبر پاسخ **403** می‌گیرد و هیچ رکوردی ثبت نمی‌شود. مقایسه امضا با `hash_equals` (ضد timing attack).

---

## 📱 ۲) تایید پیامکی شماره تامین‌کننده (AC2–AC4)

**جریان دقیقاً طبق خواسته شما:**
1. تامین‌کننده شماره موبایل را وارد می‌کند → دکمه «ارسال رمز تایید»
2. رمز ۵ رقمی پیامک می‌شود (سمت سرور فقط hash رمز نگهداری می‌شود، نه خود رمز)
3. فیلد ورود رمز باز می‌شود + **شمارنده معکوس از 2:00 تا 0:00**
4. با اتمام ۲ دقیقه، شمارنده به دکمه **«🔄 ارسال مجدد رمز»** تبدیل می‌شود
5. همین چرخه حداکثر **۵ بار** — بعد از آن **IP به مدت ۲۴ ساعت قفل** می‌شود (پاسخ 429) و ثبت‌نام از آن آدرس ممکن نیست

**لایه‌های ضد دور زدن:**
- ارسال رمز هم پشت کپچاست (ربات نمی‌تواند پیامک مصرف کند و هزینه بتراشد)
- پس از تایید رمز، توکن HMAC مخصوص *همان شماره* صادر می‌شود (اعتبار ۳۰ دقیقه)
- سرور ثبت تامین‌کننده را فقط با این توکن می‌پذیرد — تغییر شماره بعد از تایید بی‌اثر است
- حداکثر ۶ تلاش برای حدس زدن رمز

## ⚙️ ۳) وضعیت فعلی پیامک (AC5)
چون هنوز کلید پنل پیامکی از شما نرسیده:
- ویجت به‌طور خودکار حالت اطلاع‌رسانی می‌گیرد: «تایید پیامکی پس از فعال‌سازی سامانه پیامکی الزامی می‌شود» و ثبت‌نام **با کپچا** ادامه می‌یابد.
- **فعال‌سازی با یک فایل:** `sms-config.sample.php` را به نام `sms-config.php` کپی کنید، کلید API کاوه‌نگار یا ملی‌پیامک + شماره خط خدماتی را بگذارید و **خارج از public_html** آپلود کنید (کنار storage-config.php). از لحظه آپلود، OTP خودکار اجباری می‌شود — بدون نیاز به هیچ تغییر کد.
- ⚠️ طبق روال: کلید API را هرگز در چت نفرستید.

## 🧪 کیفیت
| مورد | نتیجه |
|---|:---:|
| tester9 جدید (کپچا/OTP) | ✅ ۴۷/۴۷ |
| رگرسیون tester1–8 | ✅ ۳۶۸/۳۶۸ |
| ممیز ۸بخشی (۳۲۲ صفحه) | ✅ PASS |
| **جمع کل** | **✅ ۴۱۵/۴۱۵** |

## 📁 دلتا (۸ فایل — کوچک)
`api/crm.php` • `assets/js/ptf-guard.js` (**جدید**) • `index.html` • `rfq/index.html` • `supplier/index.html` • `sms-config.sample.php` (**جدید**) • `ROADMAP-SPRINTS-52-70.md` • `RELEASE-NOTES-v72.0.md`

## ⚠️ تست پیشنهادی بعد از آپلود
1. فرم استعلام سریع را **بدون تیک کپچا** بفرستید → باید پیام قرمز «من ربات نیستم را تیک بزنید» ببینید.
2. تیک بزنید → جمع ساده ظاهر می‌شود → پاسخ بدهید → ✔ سبز → ارسال موفق با کد رهگیری.
3. در فرم تامین‌کننده فعلاً پیام «تایید پیامکی پس از فعال‌سازی...» را می‌بینید — پس از آپلود sms-config.php جریان کامل رمز + شمارنده معکوس فعال می‌شود.
