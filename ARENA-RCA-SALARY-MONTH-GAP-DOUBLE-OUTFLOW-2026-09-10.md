# ریشه‌یابی دو باگ گزارش‌شده در حقوق + راهکار بستن

- تاریخ: ۱۴۰۵/۰۶/۱۹ (2026-09-10)
- شاخه: `arena/01a08c54-pishtaj-ir` — مبنا `7d84f1b`
- نسخه CRM: `v34.38.19`
- وضعیت: B2 و B3 اعمال شد + تستر قفل‌شده در گیت CI ثبت شد (بخش ۱). دو باگ جدید در بخش‌های ۲ و ۳ ریشه‌یابی شدند و راهکار بستنشان داده شده (بخش ۴ اولویت‌بندی/تصمیم).

---

## ۱) آنچه در این جلسه اعمال شد (B2 و B3)

### ۱.۱ `crm/fiscal.js` — پرداخت حقوق به‌عنوان خروج نقدی (B2)
- متغیر `salaryPaid` = جمع drawهای حقوق (`type==='draw' && (paymentFor==='salary' || salaryMonth)`) همان سال، با همان الگوی `shareholderInject`/`chairRepay`.
- `outflowsTotal` حالا `+ out.salaryPaid` دارد ⇒ `cashEnd`/«مازاد بر کف»/«قابل تقسیم» پس از پرداخت حقوق درست می‌شوند.
- بدون دوباره‌شماری: ردیف تعهدی حقوق از `out.opex` (=`opexCashTotal`) کنار است، پس فقط پرداختِ draw شمرده می‌شود.
- `salaryUnpaid = max(0, opexSalaryTotal − salaryPaid)` (B3).

### ۱.۲ نمایش/گزارش/CSV (B3)
- سه KPI جدا: «حقوق تعهدی سال (مطالبه)»، «حقوق پرداخت‌شده با draw (خروج نقدی)»، «حقوق تعهدیِ پرداخت‌نشده (بدون خروج نقدی)».
- ردیف‌های گزارش رسمی و CSV: `حقوق پرداخت‌شده با draw` و `حقوق پرداخت‌نشده` اضافه شدند.
- متن راهنمای داشبورد نقدی به‌روز شد.

### ۱.۳ تستر قفل‌شده در گیت CI
- `_tools/uat/tester628-v34.38.19-shareholder-salary-cash-outflow.js` (قرارداد B1/B2/B3) ساخته و در `run-ci-gate.js` ثبت شد.
- تسترهای مرتبط سبز ماندند: 396, 399, 403, 404, 499, 507, 508, 509, 510, 612, 613.

---

## ۲) باگ A — «از سه سهامدار یکی ۲ ماه و دو تای دیگر ۳ ماه حقوق دارند»

### ۲.۱ ریشهٔ اصلی (نه یک خط کد، بلکه یک «مدل ناکامل»)

سند حقوق **افزایشی و تک‌ماهه** ساخته می‌شود، نه «از ابتدای احراز تا امروز». شواهد:

1. **فرمان تطبیق فقط یک `month` می‌گیرد** — `reconcile_shareholder_salaries` / `reconcile_recurring_opex` فقط ماهِ ارسالی را پردازش می‌کنند و به‌صورت پیش‌فرض فقط «ماه جاری» مجاز است:
   ```php
   // api/sales-domain.php (~2108)
   if($month!==$currentMonth && !$explicitEligibility && !$explicitTemplate)
       sd_out([... 'current_tehran_month_required' ...],422);
   ```
   یعنی حلقه‌ای روی ماه‌های گذشته وجود ندارد؛ هر ماه فقط وقتی «جاری» است ساخته می‌شود.

2. **اجرای خودکار روزانه، وابسته به حضور کاربر و موفقیت فرمان است** — `runRecurringAfterSync` در `opex.js` فقط یک‌بار در روز/کاربر (`runKey = ماه|کاربر|روز`) فرمان `reconcile_recurring_opex` را می‌فرستد؛ اگر نتیجه `uncertain` باشد `recurringServerBlockedKey` می‌شود و **تا فردا** تکرار نمی‌شود. اگر آن روز فرمان رد/نامطمئن شود، آن ماه برای آن کاربر هرگز ساخته نمی‌شود.

3. **درگاه احراز، لحظه‌ای است** — حلقهٔ سهامداران، هرکس را که در *همان لحظه* `active && duty && salary>0` نباشد رد می‌کند:
   ```php
   // api/sales-domain.php (~2133)
   if(($sh['active']??true)===false || ($sh['duty']??false)!==true || sd_num($sh['salary']??0)<=0) continue;
   ```
   اگر سهامدارِ «ب» چند ماه دیرتر `duty` شود یا حقوقش صفر بوده باشد، ماه‌های قبلش **هرگز ساخته و هرگز جبران نمی‌شوند**.

4. **هیچ نقطهٔ شروع احراز ثبت نمی‌شود** — در کل کد هیچ فیلد `dutySince/eligibilitySince/salaryFrom` وجود ندارد (`grep` خالی). پس سیستم حتی نمی‌داند از کدام ماه باید جبران کند.

5. **ویرایش سهامدار، ماهِ «انتخابی پنل» را تطبیق می‌کند، نه ماه جاری** — `ptfShareEdit` بعد از ذخیره، `reconcileSalaryOnServer(month, {explicitEligibility:true, scopeShareholder: rec.cd})` را با `month = window._shareMonth || faMonthNow()` می‌فرستد؛ اگر کاربر در پنل ماهِ گذشته‌ای را انتخاب کرده باشد، برای **فقط همان سهامدارِ ویرایش‌شده** یک ماهِ گذشته ساخته/ترمیم می‌شود، در حالی که بقیه ندارند ⇒ دقیقاً همان «یکی ۲ ماه، دو تای دیگر ۳ ماه».

6. **ماه می‌تواند برای یک نفر گم شود** — در حالت `explicitEligibility`، ردیف‌های ماهِ جاری که کلیدشان در `eligibleSalaryKeys` نباشد `void` می‌شوند؛ اگر هنگام ویرایش، `duty` لحظه‌ای false بوده باشد، آن ماه باطل می‌شود و بعد از دوباره فعال‌شدن **فقط با restore صریح** برمی‌گردد (`hasExplicit/hasTerminal → suppressed`).

7. **قفل سال مالی مانع جبران می‌شود** — `sd_is_locked` روی ماهِ قفل‌شده `409` می‌دهد؛ پس ماه‌های سال قفل‌شده دیگر قابل جبران نیستند.

**جمع‌بندی ریشه:** شمار ماه‌های حقوق هر سهامدار = تابعی از *زمان احراز، حضور/موفقیت فرمان روزانه، ماه انتخابی پنل هنگام ویرایش، لحظهٔ تغییر duty و قفل سال* است — بدون هیچ «نامتغیر» (invariant) یا گزارش تطبیقی که بگوید «هر سهامدارِ موظف باید به‌ازای هر ماهِ احرازش دقیقاً یک ادعای active داشته باشد». بنابراین نابرابری ۲ در برابر ۳ ماه **علامت یک نقص ساختاری** است، نه یک محاسبهٔ غلط در همان ماه.

### ۲.۲ راهکار بستن (پیشنهادی — نیازمند تأیید کارفرما)

1. **ثبت نقطهٔ شروع احراز** — هنگام فعال‌شدن `duty` (یا اولین ثبت حقوق)، فیلد `eligibilitySince` (ماه، مثل `1405/02`) روی پروفایل سهامدار بنویسیم. مبنای جبران همین است؛ برای دادهٔ موجود، از اولین `sharetx` نوع `salary` همان سهامدار استنتاج کنیم (fallback).

2. **گزارش تطبیقی فقط‌خواندنی (بدون تغییر داده)** — در تب «کیفیت داده»، برای هر سهامدار موظف: ماه‌های موردانتظار (از `eligibilitySince` تا ماه جاری) را با ادعاهای active مقایسه و «ماه‌های غایب» و «ماه‌های تکراری» را لیست کند. این همان الگوی `ptfOpexSuspectedDuplicates` است (فقط‌خواندنی، انسان تعیین‌تکلیف می‌کند).

3. **فرمان جبران کنترل‌شده (backfill)** — اکشن جدید/پارامتردار مثل `reconcile_shareholder_salaries` با `mode:'backfill'` که با `reason` اجباری، در بازهٔ محدود (از `eligibilitySince` تا ماه جاری) برای هر ماهِ غایب، ادعا را به‌صورت idempotent (`recurringKey`) می‌سازد؛ ماه‌های قفل‌شده را رد می‌کند (با هشدار)؛ **هیچ ردیفی را void نمی‌کند** و سند تکراری/زائد را فقط به‌صورت هشدار گزارش می‌دهد. ثبت در `corrections`/audit.

4. **ایمن‌سازی اجرای روزانه** — در `runRecurringAfterSync`، اگر آخرین ماهِ موفق قدیمی‌تر از ماه جاری باشد، به‌جای سکوت، یک نوار «ماه‌های معوق تطبیق نشده — جبران با یک کلیک» نشان دهیم (نه اجرای خودکار).

> نکتهٔ مهم: بدون قدم ۱ (نقطهٔ شروع) هیچ جبرانی «درست» نیست؛ جبران کورکورانه از ماه ۰۱، برای سهامدارانی که میانهٔ سال اضافه شده‌اند حقوق اضافه می‌سازد.

---

## ۳) باگ B — «حقوق پرسنل را هزینهٔ تکرارشونده کردم؛ دو بار در یک ماه خروج خزانه خورد»

### ۳.۱ ریشهٔ اصلی

ماده‌سازی قالب، **idempotent** است: کلید `recurringKey='opex-template:<tplId>:<month>'` دقیقاً یک ردیف می‌سازد. پس «دو خروج» یعنی **دو هویت نقدیِ جدا برای یک هزینهٔ واقعی** هم‌زمان active و تسویه شده‌اند. مسیرها:

1. **ردیف دستی + ردیف قالب در یک ماه (شایع‌ترین)** — کاربر اول حقوق همان ماه را دستی ثبت کرده، بعد قالب تکرارشونده ساخته است:
   - شاخهٔ دستیِ `ptfOpexAdd` فقط `clashRec` را چک می‌کند (دستی در برابر تکرارشونده)؛
   - اما **شاخهٔ ساخت قالب** (`v.rec==='yes'`) فقط `opexTplDuplicateActive` را چک می‌کند (قالب در برابر قالب) و **هیچ چکی در برابر ردیف دستیِ موجودِ هم‌ماه/هم‌دسته/هم‌مبلغ ندارد**؛
   - سرور `reconcile_recurring_opex` هم ردیف قالب را بدون cross-check با ردیف‌های دستی می‌سازد. ⇒ دو ردیف active، هر دو قابل تسویه، دو خروج خزانه.

2. **دو قالب تکراری با `tplId` متفاوت** — `opexTplDuplicateActive` فقط وقتی هشدار می‌دهد که `cat + amt + desc` **دقیقاً** برابر باشند؛ تغییر کوچک در شرح، یا زدن «ادامه» روی confirm نرم، دو قالب می‌سازد که هر ماه دو ردیف جدا تولید می‌کنند.

3. **قالب + تنخواه (opex-petty-twin)** — حقوق یک‌بار در تنخواه (`petty`، مستقیم از حساب) و یک‌بار به‌صورت قالب ثبت شود؛ `ptfOpexSuspectedDuplicates` فقط read-only گزارش می‌دهد و خزانه هر دو (شارژ/مصرف تنخواه و تسویهٔ قالب) را جدا می‌شمارد.

4. **تسویهٔ چندکانالهٔ یک هزینه** — `ptfOpexSettle` فقط از تسویهٔ **همان ردیف** جلوگیری می‌کند (`opexSettled(rec)`)، نه از تسویهٔ «خواهرِ» هم‌مبلغ/هم‌ماه یا پرداخت از کانال دیگر (تنخواه/چک/draw). خزانه ردیف تسویه‌شدهٔ قالب را خروج می‌شمارد (`treasury.js:187-198`) و اگر همان حقوق از تنخواه هم رفته باشد، خروج دوم ثبت می‌شود.

### ۳.۲ راهکار بستن

1. **بستن شکاف write-time (کلاینت، کم‌ریسک، قابل اعمال فوری)** — منطق `clashRec` را به سه جهت کامل کنیم:
   - ساخت/ویرایش قالب (`ptfOpexAdd` شاخهٔ `rec` و `ptfOpexEditTemplate`): اگر برای ماهِ در حال ماده‌سازی، ردیف دستیِ active هم‌دسته/هم‌مبلغ موجود است → confirm «این ماه قبلاً دستی ثبت شده؛ قالب همان هزینه را دوباره می‌شمارد».
   - ثبت دستی (`ptfOpexAdd` شاخهٔ دستی): علاوه بر `recurringKey/tplId`، **ردیف تنخواهِ** هم‌ماه/هم‌مبلغ را هم در `clashRec` بیاوریم.
   - `opexTplDuplicateActive`: آستانه را به `cat + amt` (بدون نیاز به برابری شرح) نرم‌تر کنیم تا قالب‌های هم‌مبلغِ هم‌دسته هم هشدار بگیرند (هنوز نرم/confirm).

2. **cross-check سروری (کامل‌کننده)** — در `reconcile_recurring_opex`، پیش از ماده‌سازی ردیف قالب، اگر ردیف activeِ غیرقالبی با همان `cat+amt+month` (یا تنخواهِ هم‌مبلغ/هم‌ماه) موجود است، نتیجهٔ projection را با `warning:'possible_duplicate'` برگردانیم (بدون void). این شکاف «کلاینت هرگز کلش را ندیده» را می‌بندد.

3. **گارد تسویهٔ تک‌منبعی** — در `ptfOpexSettleCommit`، پیش از `st:'settled'`، اگر ردیف خواهرِ هم‌دسته/هم‌مبلغ/هم‌ماه قبلاً تسویه شده یا تنخواهِ هم‌مبلغ/هم‌ماه تسویه‌شده‌ای هست → confirm صریح «ممکن است همین هزینه دوباره پرداخت شود». (هم‌سو با `opex_already_settled` موجود در `schedule_recurring_opex_cheque`).

4. **تعیین‌تکلیف انسانی در کیفیت داده** — وضعیت‌های `manual-vs-recurring` / `duplicate-template` / `opex-petty-twin` که امروز فقط گزارش می‌شوند، دکمهٔ «تعیین تکلیف» بگیرند که فقط گزینه‌های انسانی ارائه می‌دهد: ابطال یکی (با دلیل)، پیوند/برچسب «دو هزینهٔ واقعاً مجزا»، یا انتقال به تنخواه. **هیچ ادغام خودکاری انجام نمی‌شود** (خط‌مشی مخزن).

> توجه: با اعمال B2 (بخش ۱)، خروجِ draw حقوق حالا در `cashEnd` دیده می‌شود؛ یعنی اگر همان حقوق هم قالب تسویه‌شده و هم draw داشته باشد، دو خروج **آشکارتر** می‌شود. گاردهای ۱–۴ دقیقاً همین دوباره‌شماری را می‌بندند.

---

## ۴) اولویت و نوع تصمیم

| # | اقدام | ریسک | نیاز به تأیید؟ |
|---|---|---|---|
| B2/B3 پرداخت حقوق در خروج نقدی + تفکیک پرداخت‌شده/نشده | متوسط (تغییر cashEnd/قابل تقسیم) | **اعمال شد** + تستر قفل‌شده |
| ۳.۲-۱ گاردهای write-time کلاینت (دستی↔قالب، تنخواه، قالب‌های هم‌مبلغ) | کم | قابل اعمال فوری — همراستا با `clashRec` موجود |
| ۳.۲-۳ گارد تسویهٔ تک‌منبعی | کم | قابل اعمال فوری |
| ۳.۲-۲ cross-check سروری (هشدار، نه void) | متوسط (لمس reconcile سرور) | با تأیید + تستر سروری |
| ۲.۲-۱ ثبت `eligibilitySince` | متوسط (اسکیمای داده) | با تأیید |
| ۲.۲-۲ گزارش تطبیقی ماه‌های غایب (فقط‌خواندنی) | کم | قابل اعمال |
| ۲.۲-۳ فرمان backfill کنترل‌شده | بالا (سند مالی انبوه) | **حتماً** با تأیید + تستر قفل‌شده + اجرای آزمایشی |
| ۲.۲-۴ ایمن‌سازی اجرای روزانه (نوار معوق) | کم | قابل اعمال |

**توصیه:** گاردهای کم‌ریسک (۳.۲-۱، ۳.۲-۳، ۲.۲-۲، ۲.۲-۴) را در قدم بعدی اعمال کنم؛ `eligibilitySince` + backfill و cross-check سروری را پس از تأیید کارفرما با تستر قفل‌شده در `run-ci-gate` انجام دهم.
