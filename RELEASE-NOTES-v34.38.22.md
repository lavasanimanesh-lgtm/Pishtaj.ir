# CRM v34.38.22 — RECEIPT-BUTTON-FIX

**تاریخ:** 2026-09-16
**بازوی تغییر:** Arena Agent
**کانال:** CRM (فروش تا وصول / پروندهٔ فروش)

> این فایل همان «یادداشت انتشار نسخهٔ جاری» است که گیت CI (تستر۵۷۸، سطر ۷۹) وجودش را
> با کلید `VERSION.json → crm_version` کنترل می‌کند.

## ۱) درخواست کارفرما

> «دکمهٔ دریافت در پروندهٔ فروش کار نمی‌کند. این دکمه قبلاً کار می‌کرد اما در تغییراتی
> در نسخه‌های اخیر انجام شده است؛ احتمالاً به دلیلی از کار افتاده است. ارزیابی و گزارش کن.»

نمونهٔ کارفرما در UI: دکمهٔ **«💳 دریافت و حساب پرونده»** در هاب عملیات پروندهٔ فروش؛
کلیک ⇒ هیچ اتفاقی نمی‌افتاد (بدون پیام، بدون پنجره). هم‌خانواده‌های همان مسیر:
«💳 تکمیل تسویه» (گام ۱۰ چرخهٔ عمر)، «+ دریافت از پرونده» (پنل فاکتورها) و ورودی
حساب مشتری — همه به `ptfCaseFinanceOpen` می‌رسند.

## ۲) ریشه و اصلاح

### ریشه (رگرسیون v34.38.19 — INV-OPEN-CANONICAL)
- ستون «مطالبهٔ باز» جدول فاکتورهای پنجرهٔ مالی پرونده
  (`crm/sales-domain-v2.js` → `ptfCaseFinanceOpen`) از v34.38.19 به `arCore` ارجاع
  می‌داد تا از منبع واحد `PTF.ar.invoiceState` بخواند.
- اما `arCore` فقط داخل `caseTotals` با `var arCore=(window.PTF||{}).ar;` تعریف شده
  بود؛ در دامنهٔ `ptfCaseFinanceOpen` تعریف نداشت و `arCore` سراسری هم در هیچ فایلی
  وجود ندارد ⇒ در `'use strict'`، نخستین ارزیابی **`ReferenceError: arCore is not
  defined`** پرتاب می‌شد.
- خطا داخل `t.invoices.map(…)` بود ⇒ فقط برای پروندهٔ **دارای فاکتور فعال**. پروندهٔ
  بدون فاکتور سالم باز می‌شد — به همین دلیل باگ در تست دستی اولیه دیده نشد و دقیقاً
  همان‌جا می‌شکست که کاربر فاکتور دارد و می‌خواهد دریافتِ فاکتور را ثبت کند.
- چون دکمه‌ها `onclick` خطی دارند، خطا فقط در console مرورگر می‌نشست ⇒ «دکمهٔ مرده».
- گیت CI نگرفت چون تستر۶۴۲ برای همین ستون فقط **regex ایستا** داشت
  (`/arCore\.invoiceState\(i\)\.open/`) و تابع هرگز اجرا نمی‌شد؛ `node --check` هم
  ReferenceError را نمی‌گیرد (سینتکس سالم است).

### اصلاح (یک خط، بدون تغییر منطق مالی)
- داخل `ptfCaseFinanceOpen`: `var arCore=(window.PTF||{}).ar;` — دقیقاً همان الگوی
  `caseTotals`. اگر `PTF.ar` حاضر نباشد، گاردِ قبلی `(arCore&&…)` فعال می‌ماند و ستون
  از fallback قدیمی (`openAmountIRR`/فرمول خود رکورد) می‌خواند ⇒ پنجره حتی بدون
  ar-reconcile هم باز می‌شود.
- **دست‌نخورده:** `caseTotals`، موتور `PTF.ar` (ar-reconcile.js)، `ptfReceiptOpen`،
  زنجیرهٔ `command('post_receipt')`، `api/sales-domain.php` و منطق تخصیص/بستانکاری.
  بقیهٔ زنجیرهٔ دریافت در این بررسی سالم تأیید شد (ptfDialog موجود؛ اکشن‌های
  post/correct/void_receipt در سرور فعال؛ ترتیب defer اسکریپت‌ها درست).

### قرارداد (تستر۶۵۷ — ثبت‌شده در گیت)
`_tools/uat/tester657-v34.38.22-case-receipt-button-fix.js` — `ptfCaseFinanceOpen`
واقعی در vm با موتور واقعی `PTF.ar` (حفرهٔ تستر۶۴۲ بسته شد):
۱) پروندهٔ دارای فاکتور ⇒ بدون خطا + دکمهٔ «+ ثبت دریافت قطعی» + ردیف فاکتور؛
۲) پروندهٔ بدون فاکتور (کنترل)؛ ۳) نبودِ `PTF.ar` ⇒ fallback و پنجرهٔ سالم؛
۴) ستون «مطالبهٔ باز» با وصولی میراثیِ کامل ⇒ صفرِ منبع‌واحد (قرارداد ۶۴۲، این‌بار
رفتاری). روی کدِ قبل از اصلاح: **۹ FAIL / exit 1** (بازتولید دقیق ReferenceError) —
روی کد اصلاح‌شده: **۱۳ PASS / 0 FAIL**. تستر۶۴۲ هم همچنان سبز است (۷/۰).

## ۳) سازگاری و نسخه
- تمام نقاط رسمی نسخه → v34.38.22 (VERSION.json، manifest، sw.js، index.html و
  کوئری‌های `?v=`، sales-domain.php `SD_SERVICE_VERSION`، clear-cache.html، shell.js،
  cms.js، device-reconnect.html) + pinهای تسترها با
  `_tools/uat/bump-version-pins.js 34.38.21` (۵۸۱ مورد در ۱۳۲ فایل؛ هر دو شکلِ
  لفظِ ساده و regex-literal). همزمانی را arch-guard (A6) و تسترهای ۴۲۵/۴۸۹/۵۱۲/۵۷۸
  کنترل می‌کنند.
- نشانگر `'version' => 'v34.38.21'` در `api/tools.php` (اکشن status ماژول
  ptf-tools-license) و کامنت‌های تاریخی v34.38.21 (مثل tool-report-drafts.js و
  install-wkhtmltopdf.sh) **عمداً** دست‌نخورده‌اند: نقاط رسمی نسخه نیستند (خارج از
  فهرست A6 و یادداشت انتشار v34.38.21) و هیچ تستری آن‌ها را pin نکرده است.
- هیچ مهاجرت داده‌ای لازم نیست؛ تغییر کاملاً کلاینتی/نمایشی است. استقرار با روال
  همیشگی (FTP پس از گیت سبز)؛ به‌لطف cache-bust جدید، کلاینت‌ها فایل شکستهٔ کش‌شده
  را نگه نمی‌دارند (در صورت مشاهدهٔ رفتار قدیمی: `crm/clear-cache.html`).
- RCA کامل: `ARENA-CRM-CASE-RECEIPT-BUTTON-RCA-2026-09-16.md`.
