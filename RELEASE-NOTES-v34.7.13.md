# Release Notes v34.7.13 — رفع باگ ادغام پرونده‌های تکراری فروش تا وصولی

تاریخ: ۱۴۰۵/۰۵/۲۵ (2026-08-16)

## مشکل
در «یافته‌های فروش تا وصولی» دو پرونده تکراری به‌درستی شناسایی می‌شدند؛ پس از زدن «ادغام» پیغام موفقیت می‌آمد، اما:
- پرونده‌ها در فهرست باقی می‌ماندند.
- یافتهٔ تکراری نیز حذف نمی‌شد.

## ریشه‌ها
1. **`ptfSyncApplyServerProjection` فقط محتوا را در cache می‌نوشت ولی `krevs` (نسخهٔ per-key) را به‌روز نمی‌کرد.** پس از فرمان اتمیک سرور (duplicate_case_merge)، مرورگر هنوز فکر می‌کرد نسخه‌اش از کلیدهای تغییریافته جدید است؛ پول دوره‌ای بعدی دوباره همان کلیدها را با `krevs` قدیمی می‌کشید و با `ptfSmartMerge` محلی ادغام می‌کرد و پرونده‌های ادغام‌شده را زنده می‌کرد.
2. **رکورد `ptf_crm_projects` در ادغام به پرونده مقصد بازنشانی نمی‌شد.** در نتیجه فهرست «پرونده‌های فروش» همچنان دو رکورد نشان می‌داد.
3. **پس از ادغام، یک pull فوری از سرور زده نمی‌شد** تا یافته‌های resolve‌شده و دادهٔ نهایی بدون باقی‌مانده از کش محلی نمایش داده شوند.

## اصلاح
- در `crm/sync.js`: پس از اعمال projection سرور، `krevs` کلیدهای تغییریافته به کفِ `state.lastRev` تنظیم می‌شود تا pull بعدی دادهٔ کهنه را برنگرداند.
- در `crm/sales-domain-v2.js`: پس از هر فرمان اتمیک (به‌ویژه duplicate_case_merge)، `ptfSyncPullNow` صدا زده می‌شود تا پروجکشن نهایی سرور فوراً بارگذاری شود.
- در `api/sales-domain.php`: در `duplicate_case_merge`، رکوردهای `ptf_crm_projects` که با `cd/dealCd/projectNo` به پرونده مبدأ اشاره می‌کنند به پرونده مقصد بازنشانی می‌شوند؛ فهرست تغییرات به‌روزرسانی شد تا این کلید را هم در بر بگیرد.
- شمارهٔ نسخه به `v34.7.13` افزایش یافت تا PWA و مرورگر نسخهٔ جدید را بگیرند.

## کنترل کیفیت
- `node --check` برای همهٔ فایل‌های JS بدون خطا.
- CI gate: **35 PASS / 0 FAIL**.
