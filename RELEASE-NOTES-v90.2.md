# 🚀 یادداشت‌های انتشار (Release Notes - Version 90.2 — Thinking Tokens Breakthrough)
**پروژه:** شرکت پیشرو تجهیز فرتاک (pishtaj.ir) — پلتفرم ۳۶۰ درجه زنجیره تامین و CRM  
**تاریخ ریلیز:** ۱۷ تیر ۱۴۰۵ (July 2026)  
**تمرکز اسپرینت ۹۰.۲:** حل معمای توقف در توکن‌های تفکر (Thinking Tokens) مدل Gemini 2.5 (US-207)

---

## 🎯 خبر فوق‌العاده: اتصال واقعی به گوگل ۱۰۰٪ برقرار شده است!

پیامی که شما در کادر دیباگ ارسال کردید:
```json
{
  "candidates": [ { "content": { "role": "model" }, "finishReason": "MAX_TOKENS", "index": 0 } ],
  "usageMetadata": { "promptTokenCount": 12, "totalTokenCount": 39, "thoughtsTokenCount": 27, "serviceTier": "standard" },
  "modelVersion": "gemini-2.5-flash"
}
```

این پاسخ خام از سرور گوگل **مهم‌ترین دستاورد فنی این اسپرینت** است و ۲ واقعیت بزرگ را ثابت می‌کند:
1. **اتصال شبکه و cURL هاست شما (روی پروتکل IPv4) کاملاً و بدون هیچ نقصی برقرار شده است.** سرور گوگل درخواست سرور شما را دریافت کرده و با کد 200 پاسخ داده است.
2. **چرا پاسخ متنی نیامد و در `MAX_TOKENS` متوقف شد؟**  
   مدل‌های جدید گوگل در سال ۲۰۲۶ (مانند `gemini-2.5-flash`) قبل از نوشتن خروجی نهایی، ابتدا پردازش منطقی و تفکر درونی انجام می‌دهند. همان‌طور که در پاسخ می‌بینید: `"thoughtsTokenCount": 27` (مدل ۲۷ توکن صرف فکر کردن کرده است!). از آنجا که در اکشن تست قبلی سقف خروجی را روی ۳۰ توکن گذاشته بودیم، مدل دقیقاً پس از تفکر به سقف ۳۰ توکن رسید و قبل از نوشتن پاسخ کلامی متوقف شد!

---

## 💡 دستاوردهای نسخه پایدار **v90.2**

* **افزایش سقف توکن‌ها برای پشتیبانی از تفکر (Thinking Budget Scaler):**  
  سقف توکن‌های اکشن تست از ۳۰ به **۵۰۰ توکن** و سقف توکن‌های ترجمه، شناسایی کالا و خلاصه‌سازی به **۱۲۰۰ تا ۶۰۰۰ توکن** افزایش یافت تا مدل‌های متفکر (Thinking Models) فضای کافی برای پردازش منطقی و ارائه خروجی نهایی داشته باشند.
* **تلاش مجدد خودکار در صورت اتمام توکن (Auto-Scale Retry):**  
  در کد جدید `api/llm.php`، مکانیزمی هوشمند تعبیه شد که اگر هر مدلی در حین پردازش به سقف توکن رسید و متنی برنگرداند، سیستم به‌صورت خودکار سقف توکن‌ها را به ۲۵۰۰ افزایش داده و مجدداً درخواست را اجرا می‌کند.

---

## 📦 دانلود و اعمال نسخه پایدار v90.2

فایل **`pishtaj-release-v90.2.zip`** آماده دانلود و جایگزینی روی سرور است.
پس از اکسترکت و فشردن **`Ctrl + F5`**، روی دکمه «🧪 تست واقعی اتصال» کلیک کنید؛ اکنون پاسخ کلامی موفقیت‌آمیز AI را خواهید دید!
