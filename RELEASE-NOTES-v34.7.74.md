# RELEASE NOTES — v34.7.74 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## انتقال «ثبت‌نام‌شده از سایت» به زیر تب‌ها + حذف رکورد ثبت‌نام تامین‌کننده

### مشکل (درخواست کاربر)

۱. فهرست تامین‌کنندگان ثبت‌نام‌کرده از سایت بالای تب‌های تامین‌کنندگان نمایش داده می‌شد.
۲. راهی برای حذف رکوردهای ثبت‌نام سایت (مثلاً اسپم/نامعتبر/تکراری) وجود نداشت.

### تغییرات

#### ۱. جابه‌جایی به زیر تب‌ها (`crm/cheques.js`)

- تابع `supPendBelowTabs()` اضافه شد که عنصر `supPendWrap` (فهرست ثبت‌نام‌های سایت) را
  به‌صورت DOM دقیقاً بعد از نوار تب‌ها (`supTabs`) جابه‌جا می‌کند.
- جابه‌جایی idempotent است (شرط `tabs.nextElementSibling !== pend`) و در هر رندر
  `renderSuppliers` اجرا می‌شود.
- نتیجه: با فعال‌شدن تب «🌐 درخواست‌های سایت»، فهرست ثبت‌نام‌ها **زیر تب‌ها** (و بالای
  جدول تاییدشده‌ها) دیده می‌شود، نه بالای پنل.

#### ۲. حذف رکورد ثبت‌نام سایت

- دکمهٔ «🗑» در ستون عملیات هر ردیف فهرست ثبت‌نام سایت — فقط برای مدیران ارشد
  (`admin/chairman/ceo/commercial`).
- `supSiteDelete(code)` در `crm/bridge.js`:
  - تایید (`confirm`) با جزئیات شرکت/کد؛
  - برای رکوردهای تاییدشده هشدار می‌دهد که فقط رکورد ثبت‌نام سایت حذف می‌شود و
    تامین‌کنندهٔ تاییدشده در فهرست اصلی باقی می‌ماند؛
  - حذف محلی فوری + بازخوانی از سرور + audit + toast.
- اکشن سروری `del_supplier_site` در `api/crm.php`:
  - در `$SENSITIVE` با گارد `approve_write` ثبت شد (توکن الزامی + فقط مدیران ارشد)؛
  - رکورد را از صندوق `suppliers` حذف و ذخیره می‌کند.

### نکتهٔ امنیتی/داده

- فقط رکورد صندوق ثبت‌نام سایت حذف می‌شود؛ فهرست تاییدشدهٔ CRM و پیوست ابری دست‌نخورده
  می‌مانند (طبق سیاست «حذف ناخواستهٔ فایل ممنوع»).

### فایل‌ها

- `crm/cheques.js` — `supPendBelowTabs()` + فراخوانی در رندر تامین‌کنندگان.
- `crm/bridge.js` — دکمهٔ 🗑 + `supSiteDelete()`.
- `api/crm.php` — اکشن `del_supplier_site` + ثبت در `$SENSITIVE`.
- `_tools/uat/tester476-v34.7.74-sup-site-delete-and-tabs.js` — تستر جدید (ثبت در گیت CI).

### اعتبارسنجی

- `node _tools/arch/arch-guard.js` → `=== ARCH GUARD: PASS ===`
- `node _tools/uat/run-ci-gate.js` → `92 PASS / 0 FAIL`
