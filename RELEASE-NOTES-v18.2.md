# 💰 ریلیز v18.2 — BUG-023 + US-424 «پیش‌پرداخت و مطالبات قابل اعتماد»

**اسپرینت:** اصلاح مالی مستقیم پس از تکمیل R9  
**موارد:** BUG-023 — خطای درصد/مبلغ پیش‌پرداخت در مطالبات + US-424 — ساختاردهی پیش‌پرداخت هنگام صدور پیشنهاد و امکان اصلاح بعدی

## ✅ اجرا شد

### ۱) پیش‌پرداخت ساختاریافته
منطق پیش‌پرداخت در `crm/petty.js` بازطراحی شد. توابع جدید:

```js
ptfAdvanceOpen(no)
ptfAdvanceNormalize(o)
ptfAdvanceLabel(o)
```

پیش‌پرداخت اکنون ساختارمند است و متن آزاد دیگر مبنای محاسبه مالی نیست.

### ۲) حالت‌های پیش‌پرداخت
- بدون پیش‌پرداخت
- درصدی
- مبلغ ثابت
- پرداخت کامل / نقدی ۱۰۰٪

### ۳) کنترل درصد پیش‌پرداخت
- درصد فقط بین ۰ تا ۱۰۰ پذیرفته می‌شود.
- درصد غیرمنطقی مثل `%100000000` دیگر نمایش داده نمی‌شود.
- داده‌های legacy اگر درصد اشتباه داشته باشند، در نمایش و محاسبه با `ptfAdvanceNormalize` اصلاح می‌شوند.

### ۴) کنترل مبلغ پیش‌پرداخت
- مبلغ ثابت نسبت به مبلغ کل سند کنترل می‌شود.
- اگر مبلغ پیش‌پرداخت از مبلغ کل سند بیشتر باشد، فقط با تایید صریح کاربر و ثبت audit به‌عنوان حالت `exceptional` ذخیره می‌شود.

### ۵) پرداخت کامل/نقدی
- اگر پیش‌پرداخت ۱۰۰٪ یا پرداخت کامل انتخاب شود، به‌عنوان `paid=true` و `cashFull=true` ذخیره می‌شود.
- این حالت دیگر به‌عنوان مطالبه باز در «مطالبات و وصولی‌ها» نمایش داده نمی‌شود.

### ۶) اسناد ارزی
- برای پیشنهاد ارزی، نرخ تسعیر پیش‌پرداخت الزامی است.
- مبلغ ارزی پیش‌پرداخت × نرخ تسعیر = مبلغ ریالی مطالبات.
- نرخ زنده آزاد/سنا، اگر موجود باشد، به‌عنوان پیشنهاد اولیه در فرم می‌آید.

### ۷) نمایش و اصلاح در مطالبات
- پیش‌پرداخت‌های باز در بخش مطالبات به‌صورت ساختاریافته نمایش داده می‌شوند.
- دکمه «اصلاح» برای ویرایش پیش‌پرداخت اضافه شد.
- دکمه «وصول شد» همچنان وجود دارد و داده ساختاریافته را paid می‌کند.

### ۸) هنگام صدور پیشنهاد
- پس از ذخیره پیشنهاد CO/TC، اگر پیش‌پرداختی ثبت نشده باشد، سیستم یک‌بار پیشنهاد می‌دهد که شرایط پرداخت/پیش‌پرداخت ساختاریافته ثبت شود.
- هنگام برنده شدن CO/TC نیز اگر پیش‌پرداخت ثبت نشده باشد، فرم پیش‌پرداخت باز می‌شود.

### ۹) چاپ اسناد
- قالب‌های چاپی رسمی (`offers.js` و `offers-pro.js`) شرایط پرداخت/پیش‌پرداخت را از داده ساختاریافته می‌خوانند و به Terms اضافه می‌کنند.

## معماری
- کلید داده جدید ساخته نشد.
- پیش‌پرداخت همچنان داخل رکورد پیشنهاد ذخیره می‌شود: `ptf_crm_offers[].advance`.
- منطق مطالبات موجود حفظ شد و فقط ساختار پیش‌پرداخت قابل اعتماد شد.
- مسیرهای قبلی `advancePaid` و renderReceivables حفظ شدند اما به داده ساختاریافته متصل شدند.

## فایل‌های تغییر یافته
- `crm/petty.js`
- `crm/offers.js`
- `crm/offers-pro.js`
- `crm/index.html`
- `crm/sw.js`
- `_tools/uat/tester100-v182.js`
- اصلاح تسترهای قبلی:
  - `_tools/uat/tester96-v178.js`
  - `_tools/uat/tester99-v181.js`
- `BACKLOG-POST-R9-REVIEW.md`
- `RELEASE-NOTES-v18.2.md`
- `PTF-MASTER-HANDOVER.md`

## 🔬 کیفیت
- تستر جدید: `tester100-v182.js` با ۲۵ چک.
- رگرسیون کامل: **۸۷ تستر / صفر خطا**.
- Audit سایت: PASS با همان هشدار قدیمی تصاویر سنگین.

## وضعیت نظام سود و تقسیم سود
با این ریلیز، از نظر پیاده‌سازی فازهای مصوب، نظام محاسبه سود، زیان، هزینه‌های جاری، تنخواه، سهامداران، تقسیم سود و پیش‌پرداخت مالی تکمیل شده است.

## صف بعدی پیشنهادی
طبق دستور کارفرما، اسپرینت بعدی باید **US-425 — جلسه ممیزی چندتخصصی پس از تکمیل R9** باشد تا تیم‌های بازرگانی، حسابرسی، حسابداری و نرم‌افزار کل نظام و تغییرات v17.7 تا v18.2 را موشکافی کرده و خروجی را به بک‌لاگ‌های جدید تبدیل کنند.
