# RELEASE NOTES — v34.33.0 CMS-FIX R2 (هم‌قالب‌سازی کامل صفحات تولیدی پنل مدیریت با قالب واقعی هر بخش)

**تاریخ:** ۲۰۲۶-۰۹-۰۳ · **گیت:** ۲۰۰/۰ · **نکتهٔ بامپ:** جاروی استاندارد نسخه با `bump-version-pins.js` + فرم ترکیبی رجکس + نقاط رسمی

## گزارش کارفرما

«صفحه‌های ساخته‌شده در پنل مدیریت با قالب صفحات موجود متفاوتند: دو لینک بردکرامبِ صفحهٔ مرجع بالای صفحه دیده می‌شود، عنوان در کادر نیست و فقط فونت بزرگ‌تر دارد، متن بدون حاشیه سرتاسر صفحه را می‌گیرد، نوار سمت چپ وجود ندارد، عکس آپلودشده با وجود بودن روی سرور نمایش داده نمی‌شود، و برای صفحهٔ تازه کارتی در صفحهٔ اصلی آن بخش ساخته نمی‌شود.»

## خواسته‌های هفت‌گانه و پوشش هرکدام

1. **مشابه قالب سایر صفحات همان بخش** — مولد محصولات حالا از قالب واقعی بخش محصولات (`products/gate-valve-16-inch-cl600.html`) به‌عنوان اسکلت استفاده می‌کند؛ مولدهای خدمات/صنایع/مقایسه/مرکز دانش/وبلاگ از اسکلت مرکز دانش. هِلپر جدید `cms_product_skeleton` با بازگشت خودکار به اسکلت مرکز دانش.
2. **عکس در جای مناسب** — عکس محصول در «کارت هیرو» داخل کادر تیره می‌نشیند (دقیقاً مثل صفحه‌های موجود محصولات)؛ در صفحات عمومی، بالای بدنه و داخل کادر متن.
3. **حاشیه‌ها مثل صفحات سایت** — ساختار `container` + `ptf-layout` / `article-wrap` با همان استایل‌های درون‌خطی اسکلت که در `<head>` تزریق می‌شوند.
4. **نوار سمت چپ** — سایدبار «دسترسی سریع» (`aside.ptf-side`) با لینک‌های استعلام/محصولات/کیفیت/مرکز دانش در صفحات محصول.
5. **تایتل در کادر تیرهٔ بالا** — هیروی تیرهٔ `ptf-product-hero` (محصولات) و `article-hero` (سایر بخش‌ها) با عنوان داخل کادر + چیپ‌های دسته/برند/موجودی.
6. **حذف عبارات اضافه** — بردکرامب و منوی فعال صفحهٔ مرجع از خروجی حذف و به‌جای آن بردکرامب استاندارد سایت (`.ptf-bc` با استایل discover.css یا `.ptf-breadcrumb` در محصولات) با عنوان خود صفحه درج می‌شود.
7. **کارت لینک در صفحهٔ اصلی بخش** — تابع جدید `cms_section_cards_inject`: پس از انتشار هر صفحهٔ تازه (خدمات/صنایع/مقایسه/مرکز دانش) کارتی با تصویر و عنوان و توضیح در `{بخش}/index.html` درج می‌شود؛ بلاک با نشانگرهای `CMS:LINK-CARDS` مدیریت می‌شود، ضدتکرار است (اگر صفحه از قبل لینک شده باشد کاری نمی‌کند) و هرگز محتوای دستی صفحهٔ فهرست را تغییر نمی‌دهد. فهرست محصولات هم که با هر انتشار به‌طور کامل بازسازی می‌شود (کارت‌های محصول). انتشار زمان‌بندی‌شده نیز کارت را پس از نوشتن درج می‌کند.

## جزئیات فنی

- هِلپرهای جدید: `cms_bc_ptf` (بردکرامب استاندارد سایت)، `cms_product_skeleton` (اسکلت محصولات با بازگشت)، `cms_section_cards_inject` (کارت فهرست بخش).
- هر دو مولد محصول و صفحات عمومی حالا `discover.css` را هم لینک می‌کنند تا بردکرامب استایل‌دار باشد.
- خروجی `cms_render_public_page` فیلدهای `folder/slug/title/desc/img_abs` گرفت تا نقطه‌های نوشتن (مستقیم و زمان‌بند) بتوانند کارت درج کنند.
- تسترهای ۵۵۶ و ۵۷۱ به الگوی جدید (اسکلت محصولات + سایدبار + کارت بخش) به‌روز شدند؛ بامپ نسخهٔ سه‌فرمی + فرم ترکیبی رجکس.

## اثر عملیاتی

از این نسخه، هر صفحهٔ ساخته‌شده در پنل مدیریت هم‌قالب صفحات موجود همان بخش منتشر می‌شود و به‌طور خودکار در صفحهٔ اصلی بخشش کارت می‌گیرد. صفحه‌های شکستهٔ قدیمی با «بازنشر» از پنل اصلاح می‌شوند.
