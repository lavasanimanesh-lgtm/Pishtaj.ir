# Release Notes v34.7.11 — بازیابی بحران از بک‌آپ سرور

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## علت کارنکردن بازگردانی

- رابط فقط نقش `admin` را مجاز می‌دانست، در حالی که API برای رئیس هیئت‌مدیره نیز دسترسی داشت؛ بنابراین دکمه برای نقش رئیس نمایش داده نمی‌شد.
- مسیر `data_push` حتی با `restore:true`، tombstoneهای جدیدتر سرور را روی بک‌آپ قدیمی اعمال می‌کرد؛ در نتیجه پرونده‌های حذف‌شده قبل از ذخیره دوباره فیلتر می‌شدند و Restore ظاهراً اثر نمی‌کرد.
- guard افزایش duplicate offer نیز روی restore authoritative اجرا می‌شد و می‌توانست بخشی از snapshot را رد کند.
- در شکست ثبت سمت سرور، کلاینت ممکن بود پیام موفقیت یا داده محلی موقتی نشان دهد.
- درست پیش از Restore، نسخه خراب فعلی دوباره به عنوان بک‌آپ سرور ارسال می‌شد و امکان بازنویسی hourly/daily سالم وجود داشت.

## اصلاح بازیابی

- بازگردانی فایل و بک‌آپ سرور برای `admin` و `chairman` مجاز شد.
- Restore تاییدشده snapshot انتخابی را authoritative می‌کند و tombstone جدیدتر را روی آن اعمال نمی‌کند.
- guardهای عادی duplicate، conflict و zero-wipe فقط برای push معمولی‌اند و Restore صریح را متوقف نمی‌کنند.
- پاسخ سرور باید `ok:true` واقعی داشته باشد؛ در غیر این صورت localStorage به snapshot قبل عملیات rollback می‌شود و موفقیت نمایش داده نمی‌شود.
- وضعیت قبل از Restore فقط در `ptf_backup_prerestore` محلی/IDB نگهداری می‌شود؛ نسخه خراب جاری دیگر قبل Restore روی بک‌آپ‌های چرخشی سرور push نمی‌شود.
- فهرست بک‌آپ‌ها جدیدترین به قدیمی‌ترین مرتب شده و یک modal loading/error واقعی دارد.
- preview تاریخ بک‌آپ و تعداد پرونده‌های فعال، بایگانی و پیشنهادها را قبل از تایید نمایش می‌دهد.
- به کاربر هشدار داده می‌شود `hourly-latest` ممکن است بعد حادثه ساخته شده باشد و باید نسخه‌ای با زمان دقیقاً قبل از حذف انتخاب شود.

## ایمنی

- Restore سرور فقط برای ادمین/رئیس هیئت‌مدیره و با توکن معتبر است.
- نام فایل همچنان whitelist و ضد path traversal است.
- تایید دومرحله‌ای با تایپ `بازگردانی` حفظ شد.
- شکست ظرفیت حافظه یا سرور هیچ restore نیمه‌کاره‌ای در مرورگر باقی نمی‌گذارد.

## کنترل کیفیت

- `tester416-v34.7.11-backup-disaster-recovery.js`: 18 PASS / 0 FAIL
- تست‌های قدیمی backup phase 0/1/2: همگی 0 FAIL
- CI متمرکز: 35 PASS / 0 FAIL
- رگرسیون کامل: 399 تستر، 342 فایل PASS، 57 فایل بدهی تاریخی FAIL، 5927 check PASS و 129 check تاریخی FAIL؛ شکست جدیدی نسبت به v34.7.10 باقی نماند.
- PHP parser مستقل: PASS
- Auditor روی 624 صفحه و هم‌ترازی VERSION/PWA با 100 مرجع cache-query: PASS
- PHP CLI در محیط موجود نبود و lint داخلی CI صریحاً skip شد.
