# 🚀 ریلیز v81.0 — اسپرینت ۸۱: سیستم ضد تکرار سراسری + شماره درخواست فقط انتخابی

**تاریخ:** ۱۳ تیر ۱۴۰۵ | **دامنه:** US-174 + US-175 (دستور جدید کارفرما — اولویت یکپارچگی داده)

---

## 🛡 US-174 — جلوگیری از ثبت اطلاعات تکراری (همه ماژول‌ها)

از این نسخه، قبل از هر ثبت، ورودی‌ها با رکوردهای موجود مقایسه می‌شوند و در صورت تکرار، **ثبت مسدود** شده و پیغام خطا دقیقاً مشخص می‌کند کدام فیلد تکراری است، متعلق به کدام رکورد قبلی است و **در چه تاریخی توسط کدام کاربر** ثبت شده است.

### پوشش ماژول‌ها

| ماژول | فیلدهایی که چک می‌شوند | نتیجه در صورت تکرار |
|---|---|---|
| 🏭 تامین‌کننده (فرم کامل + فرم قدیمی) | نام، شناسه ملی، کد ملی، **هر شماره تلفن/موبایل** (تلفنخانه، تلفن‌های شخص حقیقی، شماره‌های اشخاص رابط) | ⛔ مسدود |
| 👔 مشتری/کارفرما (هر دو فرم) | نام، شناسه ملی، کد ملی، هر شماره تماس | ⛔ مسدود |
| 🎯 لید | نام شرکت، تلفن، موبایل + چک با **مشتریان موجود** («این اطلاعات قبلاً به‌عنوان مشتری ثبت شده») | ⛔ مسدود |
| 📦 کالا | شرح فارسی، نام انگلیسی | ⛔ مسدود |
| 📋 استعلام (RFQ) | موضوع تکراری برای همان مشتری + **شماره درخواست کارفرمای تکراری** | ⛔ مسدود |
| 🧾 اقلام درخواست | قلم تکراری در همان شماره درخواست | ⛔ مسدود |
| 📱 دفترچه پیامک | موبایل تکراری (از قبل فعال بود) | ⛔ مسدود |
| 🌐 تایید تامین‌کننده سایت | هشدار تکراری با جزئیات — تصمیم نهایی با مدیر ارشد | ⚠️ هشدار |

### نمونه پیغام خطا
> ⛔ امکان ثبت وجود ندارد — اطلاعات تکراری است:
> • شماره تماس «09121112233» قبلاً برای مشتری «پتروشیمی پارس» (CUST-10023) در تاریخ 1405/01/10 توسط کاربر «sales1» ثبت شده است.

### نکات فنی
- 🧠 **نرمال‌سازی هوشمند قبل از مقایسه:** ی/ک عربی ↔ فارسی، ارقام فارسی/عربی ↔ انگلیسی، حذف فاصله/نیم‌فاصله/خط تیره، تبدیل +98 و 0098 به 0 در شماره‌ها. یعنی «پيشرو تجهيز» و «پیشرو-تجهیز» یکی شناخته می‌شوند و +989125551234 با 09125551234 برابر است.
- 🖊 **ویرایش رکورد خودِ شخص مسدود نمی‌شود** — فقط ثبت جدیدِ منطبق با رکورد دیگر.
- 🕓 رکوردهای جدید مهر «تاریخ ثبت + کاربر ثبت‌کننده» می‌گیرند (crAt/crBy) تا پیام‌های بعدی همیشه دقیق باشند. برای رکوردهای قدیمیِ بدون مهر، همان پیام بدون تاریخ/کاربر نمایش داده می‌شود.
- 📄 ماژول جدید: `crm/dedup.js` (در SW کش و ترتیب لود اضافه شد).

---

## 🔒 US-175 — شماره درخواست کارفرما در TO/CO فقط انتخابی

- در فرم **پیشنهاد فنی (TO)** و **پیش‌فاکتور (CO)**، فیلد Inquiry No دیگر تایپی نیست؛ یک **منوی کشویی** است که فقط شماره‌های ثبت‌شده در بخش «استعلامات» + درخواست‌های دارای اقلام ایمپورت‌شده را نشان می‌دهد.
- بدون انتخاب شماره درخواستِ ثبت‌شده، **اجازه صدور و ذخیره TO/CO داده نمی‌شود** (پیام راهنما: ابتدا در «استعلامات» ثبت کنید).
- فرم «ثبت RFQ جدید» فیلد جدید **«شماره درخواست کارفرما (Inquiry No)»** گرفت — با چک یکتایی US-174 (یک شماره درخواست دو بار ثبت نمی‌شود).
- 🧯 **سازگاری با گذشته:** اسناد قدیمی که با شماره آزاد صادر شده‌اند هنگام ویرایش خراب نمی‌شوند — شماره فعلی‌شان با برچسب «(سند قدیمی)» در کشویی حفظ می‌شود.

---

## ✅ کیفیت

| مورد | نتیجه |
|---|---|
| ممیز ۸بخشی `_tools/audit.py` (۴۳۴ صفحه) | ✅ PASS |
| `node --check` روی همه JSهای تغییرکرده | ✅ PASS |
| تستر جدید `tester18-sprint81` | ✅ **44/44 PASS** |
| رگرسیون ۱۷ تستر قبلی | ✅ همه PASS (تسترهای 8/10/11/15 طبق قاعده به الگوی نسخه‌مستقل `ptf-crm-v` اصلاح شدند) |
| نسخه کش | `ptf-crm-v81.0` + `VER='v81.0'` (پاکسازی خودکار کش مرورگر همکاران) |

## 📦 نصب (File Manager)

فایل **`pishtaj-release-v81.0-delta.zip`** را روی هاست باز کنید (جایگزینی فایل‌ها). فایل‌های تغییر کرده: `crm/dedup.js` (جدید)، `crm/index.html`، `crm/offers.js`، `crm/leads.js`، `crm/bridge.js`، `crm/sw.js` + مستندات. **داده‌ای پاک نمی‌شود.**

> ℹ️ اگر ریلیز v80.2 را هنوز نصب نکرده‌اید مشکلی نیست — این دلتا شامل همه تغییرات v80.x + v81.0 است (offers/index/bridge/sw کامل کپی می‌شوند)، فقط برای اطمینان ابتدا `pishtaj-release-v80.2-delta.zip` و بعد این دلتا را باز کنید یا مستقیم همین دلتا + دلتای v80.2 را با هم آپلود کنید.

## 🔔 یادآوری‌های باز
- 🧪 لطفاً پس از نصب، سناریوی «ثبت تامین‌کننده با شماره تکراری» و «صدور CO بدون شماره درخواست ثبت‌شده» را تست بفرمایید.
- 🌐 Search Console: درخواست ایندکس sitemap (۱۱۱ URL جدید مرکز دانش) هنوز باز است.
- 📁 US-160 (پروژه‌ها/درباره ما): منتظر مدارک شما (پروژه‌های واقعی/لوگوها/eNAMAD).
- ⏭ اسپرینت ۸۲ طبق رودمپ: US-172 (ایمپورت هوشمند اکسل سراسری) + US-173 AC4 (ستون‌های TO/CO درگ + تصویر سلول).
