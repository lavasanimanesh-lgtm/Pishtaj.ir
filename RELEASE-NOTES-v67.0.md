# 📦 Release Notes — v67.0
**اسپرینت ۶۷: PWA + بازطراحی پوسته CRM (US-112, US-115, US-129)**
**تاریخ:** ۱۴ تیر ۱۴۰۵ (2026-07-03)

---

## 📲 US-112 — وب‌اپ قابل نصب (PWA)

- **`crm/manifest.json`:** نام «CRM پیشرو تجهیز فرتاک»، standalone، RTL، رنگ سازمانی، آیکون‌های ۱۹۲/۵۱۲ (maskable)
- **`crm/sw.js` (Service Worker):** کش کامل شل (۱۰ فایل JS + فونت‌ها + آیکون‌ها) → **باز شدن آفلاین**؛ استراتژی Network-First برای `/api/` و Cache-First + به‌روزرسانی پس‌زمینه (stale-while-revalidate) برای استاتیک؛ پاکسازی خودکار کش نسخه‌های قبلی
- **iOS (پاسخ بازخورد کارفرما):** تگ‌های کامل `apple-mobile-web-app-*` + **راهنمای تصویری سه‌مرحله‌ای Add to Home Screen** — چون iOS پرامپت خودکار ندارد، باکس نصب برای کاربران آیفون همیشه نمایان است
- **اندروید/کروم:** هندل `beforeinstallprompt` → پرامپت نصب واقعی سیستم‌عامل
- **نشانگر آفلاین:** بنر «آفلاین — تغییرات محلی ذخیره می‌شود» + پاسخ JSON آفلاین برای APIها

## 🔘 US-115 — دکمه نصب در صفحه ورود

- باکس «📲 نصب وب‌اپلیکیشن CRM» زیر فرم لاگین با هویت بصری سازمانی
- AC4 ✔: در حالت standalone (نصب‌شده) به‌طور خودکار مخفی می‌شود
- اندروید: کلیک → پرامپت واقعی | iOS: کلیک → راهنمای تصویری | سایر: راهنمای منوی مرورگر

## 🗂 US-129 — منوی دسته‌بندی آکاردئونی (`crm/shell.js`)

**۷ دسته:** 📊 داشبورد (مستقل) | 💼 فروش (لیدها، درخواست‌ها، استعلامات، پیشنهادها، پرونده‌های فروش) | 🛒 تامین (تامین‌کنندگان، قیمت‌های خرید) | 📦 کالا و اسناد (کالاها، پرونده‌های پروژه، مکاتبات، قرارداد) | 💰 مالی (فاکتورها، مطالبات، سفارشات، مالی، تحلیلگر) | 👤 شخصی (کارتابل، یادآورها) | ⚙️ سیستم (کارفرمایان، کاربران، تنظیمات)

- AC2 ✔: فقط یک دسته باز + انیمیشن + ذخیره آخرین دسته باز در localStorage
- AC3 ✔: **جمع بج‌ها روی سر دسته** (فروش← لیدها | شخصی← کارتابل+یادآورها) تا در حالت بسته دیده شوند
- AC4 ✔: دسته‌ای که همه آیتم‌هایش با RBAC مخفی شده، کلاً حذف می‌شود (مثلاً حسابدار فقط «مالی» و «شخصی» می‌بیند)
- AC5 ✔: موبایل — آیتم‌های 44px+ و فونت بزرگ‌تر

## 🛡 حادثه و رفع در حین توسعه
پچ اولیه index.html به دلیل الگوی regex مشکل‌دار، فایل را خراب کرد (۴۶MB!) — از zip ریلیز v66 بازیابی و پچ با روش امن (درج نقطه‌ای + assert یکتایی) از نو اعمال شد. درس‌آموخته در رویه ثبت شد: پچ‌های index همیشه با assert تعداد تکرار.

## 🧪 تست‌ها — ۵/۵ PASS
manifest نصب‌پذیر (الزامات کامل) ✔ شل SW شامل هر ۱۰ فایل ✔ استراتژی‌های کش ✔ پوشش همه ۲۲ پنل در گروه‌ها ✔ جریان‌های نصب (اندروید/iOS/standalone) ✔ + ممیز سایت PASS ✔

## 🚀 دیپلوی
جدید: `crm/manifest.json`، `crm/sw.js`، `crm/shell.js` | تغییر: `crm/index.html`
⚠️ **الزام PWA:** سایت باید روی HTTPS باشد (هست ✔). پس از دیپلوی، یک‌بار CRM را باز کنید تا SW نصب شود؛ از بازدید دوم آفلاین هم کار می‌کند.
📱 **تست آیفون:** Safari → pishtaj.ir/crm → باکس نارنجی «نصب وب‌اپلیکیشن» → راهنمای Share → Add to Home Screen.

## ⏭️ اسپرینت بعدی (۶۸)
UAT جامع CRM (تست ۱۷ ماژول × ۸ نقش + چرخه انتها-به-انتها) + موج اول محتوا (فاز A: بازیابی ۱۱۱ + مقالات جدید) + تصاویر محصولات (US-127/128).
