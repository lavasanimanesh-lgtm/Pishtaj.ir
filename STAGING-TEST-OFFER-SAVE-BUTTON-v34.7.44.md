# چک‌لیست staging v34.7.44 — دکمهٔ ذخیرهٔ پیشنهاد

> فقط روی `staging.pishtaj.ir` اجرا شود. production نباید deploy یا آزمایش تغییردهنده شود.

## پیش‌نیاز

1. از بنر staging و نمایش نسخهٔ `v34.7.44` مطمئن شوید.
2. یک Hard Refresh انجام دهید؛ در صورت مشاهدهٔ نسخهٔ قدیمی از صفحهٔ پاک‌سازی امن cache استفاده کنید.
3. DevTools را باز نگه دارید و Network را بر اساس `sales-domain.php` فیلتر کنید.
4. یک درخواست آزمایشی با اقلام معتبر انتخاب کنید.

## سناریوی اصلی ACK

1. از همان درخواست یک پیشنهاد جدید باز کنید.
2. خریدار، شمارهٔ درخواست و همهٔ فیلدهای اجباری اقلام را تکمیل کنید.
3. یک بار روی «💾 ذخیره» کلیک کنید.
4. انتظار:
   - دکمه هنگام ارسال موقتاً غیرفعال و متن آن «در حال ثبت روی سرور…» باشد؛
   - دقیقاً یک intent با action=`register_offer` دیده شود (replay فقط در صورت پاسخ مبهم مجاز است و operation ID آن باید یکسان باشد)؛
   - پس از ACK مودال بسته و پیام موفقیت قطعی نمایش داده شود؛
   - پیشنهاد در فهرست موجود و RFQ همان درخواست دارای وضعیت/لاگ هم‌خوان باشد؛
   - با refresh و pull مجدد، هر دو رکورد باقی بمانند و duplicate ایجاد نشده باشد.

## validation

1. فرم تازه‌ای باز و یکی از فیلدهای اجباری یا مقدار اجباری یک قلم را خالی کنید.
2. «ذخیره» را بزنید.
3. انتظار: پیام validation نمایش داده شود، مودال باز بماند و هیچ درخواست `register_offer` ارسال نشود.

## رد قطعی

با داده/شرایط کنترل‌شده‌ای که staging به‌طور قطعی رد می‌کند آزمایش شود:

- پیام رد قطعی نمایش داده شود؛
- snapshot خوش‌بینانه rollback شود؛
- فرم باز و قابل اصلاح بماند؛
- ثبت محلی یا پیام موفقیت کاذب وجود نداشته باشد.

## نتیجهٔ نامشخص

فقط با ابزار تست کنترل‌شده و بدون تکرار دستی کلیک، پاسخ transport را مبهم کنید:

- فرم و intent حفظ شوند و پیام «نتیجه نامشخص» نمایش داده شود؛
- rollback یا success کاذب رخ ندهد؛
- replay خودکار حداکثر یک بار و با همان operation ID باشد؛
- پس از بازیابی ارتباط، وضعیت قطعی از سرور بررسی شود.

## ثبت نتیجه

برای هر سناریو زمان، کاربر تست، شمارهٔ درخواست، شمارهٔ پیشنهاد، operation ID، تعداد درخواست شبکه و نتیجهٔ pull مجدد ثبت شود. هر failure مانع merge و هرگونه deployment production است.
