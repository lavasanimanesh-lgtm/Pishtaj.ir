# RELEASE NOTES — v34.7.83 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## صفحه‌بندی فهرست تامین‌کنندگان تاییدشده (SUP-PERF-002)

### درخواست/زمینه

در ادامه بهینه‌سازی ماژول تامین‌کنندگان، فهرست تاییدشده هنوز همه‌ی رکوردها را یکجا رندر
می‌کرد و با صدها/هزاران رکورد، باز شدن پنل و جستجو کند می‌شد.

### تغییر

- در `crm/offers.js` (تابع واقعی `renderSuppliers2`) صفحه‌بندی ۵۰ رکورد در هر گام اضافه شد.
- دکمهٔ «⬇ نمایش X مورد دیگر (شماره / جمع)» برای بارگذاری گام بعدی.
- جستجو باعث ریست صفحه به ۰ می‌شود تا نتیجه‌ی جستجو از اول دیده شود.
- شمارندهٔ `dSup` همچنان **تعداد کل** رکوردها را نشان می‌دهد (نه فقط صفحهٔ فعلی).
- منطق داده/سینک/تب‌ها/فیلتر origin بدون تغییر ماند؛ فقط دید ردیف‌ها گام‌به‌گام شد.

### تست

- `_tools/uat/tester485-v34.7.83-supplier-pagination.js` جدید در گیت CI.
- گیت CI: **101 PASS / 0 FAIL**.

### توضیح کد

```js
var per = 50;
var shown = list.slice(0, (page + 1) * per);
// ... رندر shown ...
if (list.length > shown.length) { /* دکمهٔ نمایش بیشتر */ }
window.ptfSupApprovedMore = function () { ... };
```

## گام‌های باقی‌مانده (پیشنهادی)

1. **Lazy-load مالی تامین‌کنندگان** — محاسبهٔ همه‌ی balance ها فقط هنگام باز شدن باکس «فاکتور، حساب و پرداخت».
2. **get_inbox فقط pending/صفحه‌بندی سروری** — کاهش حجم پاسخ برای ثبت‌نام‌های سایت.
3. **خواندن نقطه‌ای MySQL** — در صورت فعال بودن فاز B، `get_inbox` از کوئری ایندکسی.
4. **استارت سرد SPA** — جدا کردن moduleهای سنگین از مسیر لود اولیه.
