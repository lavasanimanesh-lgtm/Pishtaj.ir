# 🛠️ ران‌بوک فاز ۰ — تثبیت و تأیید Production پس از incident (برای ادمین سرور)

**نسخه هدف:** v31.7.8 | **زمان تقریبی:** ۴۵–۶۰ دقیقه | **پیش‌نیاز:** دسترسی SSH/پنل هاست + یک اکانت از هر نقش

> این ران‌بوک همان «اقدامات باز» A1–A4 گزارش `INCIDENT-REPORT-BUG-AUTH-001.md` است. تا وقتی هر ۴ گام سبز و ثبت نشده، وضعیت رسمی پروژه «Production تأییدنشده» می‌ماند.

---

## گام ۱ — بک‌آپ امن قبل از هر چیز (۱۰ دقیقه)

```bash
cd ~/public_html
tar -czf ~/ptf-data-backup-$(date +%F-%H%M).tar.gz crm/data/
# فایل را از سرور دانلود کنید و خارج از سرور نگه دارید
```
☐ فایل بک‌آپ دانلود و در محل امن خارج از سرور ذخیره شد — تاریخ/ساعت: ..............

## گام ۲ — تأیید ریکاوری واقعی (۲۰ دقیقه)

اجرای کامل `QUICK-TEST-GUIDE-v31.7.8.md` و علاوه بر آن، برای **هر ۴ نقش** (admin / sales / accountant / collector):

1. پاکسازی کش (`Ctrl+Shift+Delete` یا دکمه «🔄 رفع مشکل ورود») و ورود.
2. باز کردن DevTools → Network و تأیید این سه مورد:
   - `data_pull` → **200** (نه 401)
   - `get_events` → **200**
   - `get_inbox` → **200**
3. داشبورد آمار غیرصفر نشان دهد؛ نقش sales نباید هیچ داده مالی ببیند.

| نقش | ورود | data_pull | داده صحیح | تستر |
|------|------|-----------|------------|------|
| admin | ☐ | ☐ | ☐ | .......... |
| sales | ☐ | ☐ | ☐ | .......... |
| accountant | ☐ | ☐ | ☐ | .......... |
| collector | ☐ | ☐ | ☐ | .......... |

## گام ۳ — بررسی سلامت داده + حذف ابزار (۱۰ دقیقه)

⚠️ **نکته htaccess:** `api/.htaccess` فعلی فایل `data-health-check.php` را در whitelist ندارد و روی Apache پاسخ 403 می‌دهد. راه امن:

```apache
# موقتاً این خط را به بخش <If> در api/.htaccess اضافه کنید:
# ...|data-health-check)\.php#
```

1. باز کردن `https://DOMAIN/api/data-health-check.php?confirm=yes`
2. ذخیره کامل صفحه گزارش (Ctrl+S) به‌عنوان evidence.
3. بررسی: ۴۸ کلید سالم؟ backup تازه وجود دارد؟ توکن‌ها نرمال؟
4. **حذف فایل از سرور + برگرداندن htaccess به حالت قبل:**
```bash
rm ~/public_html/api/data-health-check.php
```
☐ گزارش ذخیره شد ☐ فایل حذف شد ☐ htaccess برگشت

## گام ۴ — بررسی لاگ پنجره incident (۱۵ دقیقه)

بازه 2026-07-18 تا 2026-07-19 در access log (cPanel → Raw Access یا `~/access-logs/`):

```bash
# دسترسی‌های مشکوک به endpoint های عمومی‌شده و ابزار تشخیص:
grep -E "users_get|data-health-check" access.log | grep -vE "IP1|IP2|IP3"   # IPهای تیم را جایگزین کنید
# حجم 401 ها در بازه incident (تأیید الگوی قطعی):
grep ' 401 ' access.log | awk '{print $4}' | cut -d: -f1-2 | sort | uniq -c | tail -50
```

☐ هیچ IP ناشناس با الگوی اسکن/برداشت دیده نشد — در غیر این صورت فوراً گزارش شود.

---

## ثبت نتیجه

پس از تکمیل، این ۴ گام + evidence (اسکرین‌شات‌ها، خروجی health-check، یادداشت لاگ) در پوشه‌ای با نام `EVIDENCE-PHASE0-YYYY-MM-DD/` بایگانی و اعلام شود تا وضعیت پروژه از «Production تأییدنشده» به «تأییدشده» تغییر کند و اقدامات A1–A4 گزارش incident بسته شوند.
