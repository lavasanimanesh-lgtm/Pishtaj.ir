# ریلیزنوت v24.4 — پارک/غیرفعال‌سازی Lead Finder (لیدیاب)

## تصمیم
طبق سیاست ثبات ساختاری و گزارش کارفرما که «لیدیاب درست کار نمی‌کند»:
- **Lead Finder (لیدیاب)** از محصول عملیاتی خارج شد.
- **ماژول سرنخ‌ها (leads.js)** دست نخورده باقی ماند — این CRM اصلی سرنخ است و با لیدیاب یکی نیست.

## تغییرات
- حذف تب «🔎 لیدیاب» از AI Workbench
- حذف لود `lead-finder.js` از `index.html` و service worker
- بنر نسخه: فاز ثبات (بدون ارجاع Lead Finder Foundation)
- فایل `crm/lead-finder.js` فقط پارک شده (لود نمی‌شود)

## چرا حذف/پارک و نه فیکس فوری؟
لیدیاب foundation آزمایشی وابسته به LLM/API و discovery ناقص است؛ در فاز ثبات، نگه داشتن UI خراب/نیمه‌کاره مضرتر از پارک کردن است.
