# 🚀 ریلیز v16.8 — US-404 فاز ۱: «پرونده فروش = ابلاغ سفارش» + تب فرصت‌های فعال
**تاریخ:** ۱۴۰۵/۰۴/۲۲ | **نسخه قبلی:** v16.7 | **بسته:** `pishtaj-release-v16.8.zip` (تجمیعی از v14.5)

اولین اسپرینت با **قاعده جدید مصوب شما**: فقط یک استوری؛ و چون US-404 بزرگ بود، به **دو فاز** تقسیم شد — این ریلیز فاز ۱ (تغییر فرایند + لایه فرصت‌ها + مهاجرت نرم) است؛ فاز ۲ (انتقال خودکار اسناد چهارگانه هنگام تشکیل پرونده) اسپرینت بعدی.

---

## 🔄 آنچه تغییر کرد (سناریوی دو-لایه مصوب شما)

### لایه ۲ — «📁 پرونده‌ها (ابلاغ سفارش)»
- **ثبت پیشنهاد دیگر پرونده نمی‌سازد.** تنها نقطه ساخت پرونده فروش = ثبت «🏆 برنده» روی پیشنهاد مالی (همان مسیر موجود با همه گاردها: تایید صریح، قفل، انضمام مدارک، هدایت خرید واقعی US-392).
- تب پرونده‌ها فقط پرونده‌های ابلاغ‌شده را نشان می‌دهد → **بخش همیشه خلوت و معنادار** (دقیقا مشکل «شلوغی» که گزارش کردید).
- مختومه/تسویه → بایگانی: مسیر موجود عینا حفظ.

### لایه ۱ — تب جدید «🎯 فرصت‌های فعال»
- درخواست‌های دارای پیشنهادِ هنوز برنده‌نشده، اینجا رهگیری می‌شوند: چیپ وضعیت همه پیشنهادها (TO/CO با رویژن)، بج مهلت پاسخ (US-348)، بج «منتظر صدور»؛ مرتب‌سازی: مهلت‌گذشته‌ها اول.
- **هیچ موجودیت داده جدیدی ندارد** — نمای خواندنی روی همان درخواست‌ها/پیشنهادها (شرط شما: بدون وصله‌پینه).
- دکمه «🚫 ثبت باخت» = دقیقا همان مودال دلیل استاندارد (US-349) و همان مسیر بایگانی → **گزارش Win/Loss بدون هیچ تغییری تغذیه می‌شود** (کمبود ② تحلیل R5 پوشش داده شد).
- با ثبت برنده، فرصت **خودکار** از این تب خارج و در «پرونده‌ها» می‌نشیند.

### مهاجرت نرم (کمبود ③)
- هیچ رکوردی حذف/تبدیل نشد: پرونده‌های قدیمی دارای برد یا فاکتور → تب پرونده‌ها؛ رکوردهای قدیمی بدون برد → در تب فرصت‌ها نمایندگی می‌شوند (حتی اگر پیشنهادشان حذف شده باشد — بج «رکورد قدیمی، نتیجه را ثبت کنید»). با برد یا ثبت باخت، تعیین تکلیف می‌شوند.

## 🛡 اثبات شروط شما

| شرط | اثبات |
|-----|-------|
| بدون وصله‌پینه | ماژول جدید oppo.js فقط می‌خواند و به توابع موجود می‌سپارد (ptfSF_ensure/sfClose/editRfq)؛ صفر منطق موازی؛ صفر کلید داده جدید. مختومه‌سازی/Win/Loss/تحویل تعهدی US-351 عینا همان کد قبلی. |
| چابکی | یک کلیک بین دو تب؛ فرصت‌ها با اولویت فوریت؛ ثبت باخت دو-کلیکی از همان کارت. |
| سهولت کاربری | راهنمای داخل هر تب؛ پیام خالی‌بودن هر تب مسیر بعدی را می‌گوید؛ جستجوی موجود روی هر دو تب کار می‌کند. |
| بدون باگ جدید | tester86: **۳۵ چک** (کد + رفتاری: چرخه کامل فرصت→برد→پرونده و فرصت→باخت→Win/Loss طبق شرط رفت‌وبرگشتی شما) + **jsdom: ۹ چک DOM** (تب‌ها، انتقال خودکار پس از برد، مودال باخت، بایگانی با دلیل استاندارد). رگرسیون کامل: **۷۳ تستر / ۲۱۱۴ چک — صفر خطا**. audit PASS. tester52 قدیمی با رفتار مصوب جدید هم‌راستا شد. |
| سازگاری وابستگی‌ها (کمبود ④) | خرید واقعی v16.3 (قفل به wonOffer) حالا با همه پرونده‌های تب کاملا هم‌راستاست؛ کانبان v16.4، قفل برنده، سد rejected→CO، hook نامه‌ها — همه تست رگرسیون سبز. |

## ⏭ فاز ۲ (اسپرینت بعدی — طبق تقسیم مصوب)
انتقال خودکار «اسناد چهارگانه» هنگام تشکیل پرونده: ضمایم درخواست + TO/CO با آخرین رویژن + استعلام‌های تامین مرتبط (rfqsmart) + اتصال کامل به جدول خرید واقعی — سپس طبق صف: BUG-019 → US-405…408 (اسناد بازرگانی).

## 📋 راهنمای به‌روزرسانی
1. `pishtaj-release-v16.8.zip` روی هاست روی موجود اکسترکت شود — پوشه `api` حذف نشود.
2. همه کاربران رفرش کنند — نسخه سایدبار **v16.8**.
3. «پرونده‌های فروش» حالا دو تب دارد — پرونده‌های در جریانِ قبلی در «🎯 فرصت‌های فعال» هستند و با برنده شدن CO خودکار «پرونده» می‌شوند.
