# یادداشت انتشار — v34.37.1

**تاریخ:** ۲۰۲۶-۰۹-۰۵ (۱۴۰۵/۰۶/۱۴) · **پایه:** v34.37.0
**گیت CI:** ۲۱۳ PASS / ۰ FAIL

انتشار کوچک و متمرکز روی **پنل فاکتورها**، بر اساس خواستهٔ کارفرما:

> «فاکتورها به صورت ردیف نمایش داده شوند و اطلاعات دیگر به صورت کشویی در صورت زدن روی فلش باز شوند و دیده شوند. اگر فاکتوری ثبت شد دیگر دکمهٔ ثبت فاکتور به عبارت «فاکتور ثبت شده است» تغییر کند و نباید با زدن روی ثبت فاکتور پنجرهٔ مودال ثبت فاکتور دوباره باز شود.»

---

## ① نمایش ردیفی + کشوی جزئیات

پیش از این هر ارجاع یک **کارت بلندِ همیشه‌باز** بود؛ با چند ارجاع، صفحه غیرقابل مرور می‌شد.

اکنون هر ارجاع **یک ردیف** است با ستون‌های ثابت:

| فلش | سند مبنا | مشتری | وضعیت فاکتور | مبلغ | مطالبه باز | اقدام |
|---|---|---|---|---|---|---|

- زدن روی **فلش ◀/▾** — یا هر جای ردیف — کشوی جزئیات را باز/بسته می‌کند.
- کشو شامل همان اطلاعات قبلی است: مبنای ریالی و نرخ تسعیر، تاریخ و نام ارجاع‌دهنده، نام انگلیسی مشتری، دکمه‌های PDF (مبنای ریالی / سند برد)، «↩️ لغو ارجاع»، و کارت کامل هر فاکتور با اصلاح/ابطال/حذف قطعی/اسناد.
- **حالت باز/بسته ماندگار است** (`window._ptfInvOpenRows`). هر اقدام داخل کشو `renderInvoices()` را صدا می‌زند؛ بدون این، کشو زیر دست کاربر بسته می‌شد.
- کلیدهای **«باز کردن همه / بستن همه»** بالای فهرست.
- دسترس‌پذیری: فلش یک `<button>` واقعی با `aria-expanded` است و `event.stopPropagation()` مانع دوبار toggle شدن می‌شود.

**ستون وضعیت** سه حالت دارد: `⏳ بدون فاکتور` · `✅ فاکتور F-1001` · `🚫 ابطال‌شده`.

## ② قفل «فاکتور ثبت شده است» — دو لایه

| لایه | محل | رفتار |
|---|---|---|
| ۱ — UI | `renderInvoices` | تا وقتی فاکتور فعالی وجود ندارد دکمهٔ «+ ثبت فاکتور رسمی صادرشده» فعال است. به‌محض ثبت، جای آن را نشانگرِ **«✅ فاکتور ثبت شده است»** می‌گیرد: نه `<button>` است، نه `onclick` دارد، و `cursor:default` است |
| ۲ — در ورودی | `showInvModal` | اگر بدون `editId` صدا زده شود و پیشنهاد فاکتور فعال داشته باشد، **پیش از ساخت مودال** برمی‌گردد و راهنمای «برای تغییر از اصلاح اطلاعات و برای سند جایگزین ابتدا از ابطال استفاده کنید» می‌دهد |

لایهٔ دوم عمدی است: پنل تنها مسیر رسیدن به این مودال نیست (کشوی پروندهٔ فروش و کارتابل هم آن را صدا می‌زنند). قفل **چسبنده نیست** — اگر فاکتور ابطال شود، ثبت دوباره آزاد می‌شود. مسیر **اصلاح** (`editId`) هرگز مسدود نمی‌شود.

## ③ اصلاح دو نقص که در همین بازبینی آشکار شد

**الف) دکمهٔ «↩️ لغو ارجاع» در v34.37.0 در کد مرده افتاده بود.**
پنلِ زندهٔ فاکتورها `crm/official-invoice-v2.js` است؛ این فایل در انتهای بارگذاری، هر سه تابع `buildInvoices` / `renderInvoices` / `showInvModal` را روی `window` بازنویسی می‌کند و `perms.js` هم پنل `inv` را با تاخیر ۱۲۰۰ms ثبت می‌کند. بنابراین نسخهٔ `crm/rbac.js` **هرگز اجرا نمی‌شود** — و دکمهٔ لغو ارجاع که آنجا اضافه شده بود هیچ‌وقت دیده نمی‌شد. دکمه به پنل زنده منتقل شد، نسخهٔ تکراری از `rbac.js` برداشته شد و بازنشستگی آن پنل با یک بنر صریح (`INV-PANEL-DEAD-CODE`) مستند شد. تستر هم اکنون هر دو را قفل می‌کند: دکمه باید در فایل زنده باشد و در فایل مرده **نباشد**.

**ب) ارجاع ریالیِ ساده، «پیشنهاد ارزی با نرخ ۰ ریال» نشان می‌داد.**
برای ارجاع IRR، `invRef.rialBasis` برابر خودِ شمارهٔ پیشنهاد است و `findOffer` همان سند را برمی‌گرداند؛ در نتیجه `comp` پر می‌شد و پنل خط بی‌معنای «💱 مبنای ریالی از پیشنهاد ارزی CO-… (IRR) — نرخ ۰ ریال» را چاپ می‌کرد و به‌جای «🏆 سند برد (PDF)» دکمهٔ نسخهٔ ریالی را می‌گذاشت. اکنون `comp` فقط وقتی معتبر است که سندِ دیگری باشد. مسیر ارزیِ واقعی بدون تغییر است.

## ④ تستر تازه

`_tools/uat/tester598-v34.37.1-invoice-panel-rows.js` — **۴۹ PASS / ۰ FAIL**، رفتاری: خودِ `renderInvoices` را در `vm` اجرا می‌کند و HTML خروجی، رفتار واقعی toggle، ماندگاری حالت باز پس از رندر مجدد، سه حالت وضعیت، هر دو لایهٔ قفل ثبت، و نبود رگرسیون در مسیر ارزی را می‌سنجد.

## ⑤ پین‌های شکنندهٔ اصلاح‌شده

`tester477` و `tester563` روی **فاصله‌گذاریِ** فرم مینیفای قفل بودند (`o.invRef&&o.invRef.fromFile`، `ptfCustNamePair(o.buyerCd,o.buyerCo)`). چون پنل به فرم خوانا بازنویسی شد، این پین‌ها به **قرارداد** تبدیل شدند (regex تحمل‌کنندهٔ فاصله) تا رفتار قفل بماند ولی قالب آزاد باشد.

## ⑥ بدون تغییر

هیچ تغییری در سرور، مدل داده، مجوزها یا جریان ثبت/اصلاح/ابطال فاکتور انجام نشده است. این انتشار صرفاً لایهٔ نمایش پنل فاکتورها و یک گاردِ ورودیِ مودال است.
