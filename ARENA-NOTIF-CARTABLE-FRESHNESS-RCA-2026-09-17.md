# ARENA · RCA — «اعلانات و کارتابل به‌روز نیست؛ کارهای بسیار قدیمی می‌ماند؛ خواندم موقت پاک می‌کند و با هارد رفرش برمی‌گردد»

**تاریخ:** ۲۰۲۶-۰۹-۱۷ (۱۴۰۵/۰۶/۲۶) · **برچسب تغییر:** CRM **v34.38.24** (NOTIF-FRESH) · **تستر:** `tester659-v34.38.24-notif-fresh.js`
**دامنه:** `crm/rbac.js` · `crm/bridge.js` · `crm/sync.js` · `crm/sales-domain-v2.js` · `api/sales-domain.php` · `api/crm.php` · `_tools/uat/tester659-…js` · `_tools/uat/run-ci-gate.js` · `VERSION.json` · نقاط رسمی نسخه/cache-bust

---

## ۱) گزارش کارفرما

> «اعلانات و کارتابل اصلاً به‌روز نیست و کارهای بسیار قدیمی و اعلانات بسیار قدیمی در آن
> وارد می‌شود و زدن گزینهٔ خواندن هم موقت از بین می‌برد و پس یک هارد رفرش دوباره همان
> اعلانات قدیمی برمی‌گردد و این موضوع خیلی آزار دهنده است. باید سیستمی طراحی شود که
> اعلانات مهم و به‌روز دیده شوند و با گزینهٔ خواندم از بین بروند.»

سه نشانهٔ متمایز: **(الف)** احیای اعلانِ خوانده‌شده پس از هارد رفرش، **(ب)** چرخهٔ
بازگشت اعلان‌های تکراریِ خوانده‌شده در استفادهٔ عادی (بدون رفرش)، **(ج)** انباشت
کارها/اعلانات بسیار قدیمی در کارتابل.

## ۲) معماری موجود (نقشهٔ کد پیش از اصلاح)

- کارت اعلان = رکورد `ptf_crm_notifs` (شناسهٔ `cd`، `readBy[]` per-user، `done`،
  `dkey`، `iso`). «خواندم» = افزودن کاربر به `readBy` (ntfRead، rbac.js)؛ کارت از
  نمای پیش‌فرض `renderCartable` پنهان می‌شود ولی **داده می‌ماند**.
- سازنده‌های خودکار کارت (bridge.js): `checkDueReminders` (dkey=`rem-due-<cd>:<user>`
  با مهر `notifiedUsers` روی یادآور)، `checkRfqDue`/`checkDealDue` (dkey پایدار + مهر
  `dueNotified` گذار-محور)، `checkOfferExpiry`؛ در بوت و هر تیکِ poll اجرا می‌شوند و
  از `getData` فاز B (کش ۳۰ ثانیه‌ای) می‌خوانند. `chDailyNotify` (cheques.js) با dkey
  پایدار `chq-due-<cd>` و مهر **روزانهٔ** `notified[today]` هر روز شلیک می‌شود.
- نوشت‌ها: `ptfEntitySaveCollection` (sales-domain-v2.js) → diff → فرمان‌های اتمیک
  `entity_upsert/entity_delete` (fetch ناهمگام، **بدون dirty/صف**) + `ptfSilentWrite`
  محلی. ACK → projection کانونیک سرور.
- pull: `ptfSyncPullNow`/`pullCheck` (sync.js) → اعمال per-key: برابر → skip؛ held →
  smartMerge؛ `forceFull && initialReconcile` → smartMerge؛ dirty → smartMerge؛
  **در غیر این صورت → `pullWrite(k,newStr)` جایگزینی خام**.
- merge اتحاد notifs (readBy/done/repeat + dedupe ارجاع): `ptfSmartMerge` کلاینت
  (v31.7.10 BUG-NTF-002) و merge کانونیک `data_push` سرور (v34.8.34، api/crm.php).

## ۳) ریشه‌ها — زنجیرهٔ کامل احیا

### ریشهٔ ۱ (نشانهٔ ب): dedupe شکنندهٔ `notify()` در برابر کارت خوانده‌شده
شرط بازاستفاده: `dnk === dkey && (readBy.length === 0 || persistentTask)` — فقط
referral پایدار مستثنا بود. هر سازندهٔ دوره‌ای با dkey صریح که پس از «خواندم» دوباره
شلیک می‌شد، **رکورد تازه با cd تازه و readBy خالی** می‌ساخت:
- `chq-due-<cd>`: مهر `notified[today]` روزانه است ⇒ برای هر چکِ در پنجرهٔ سررسید،
  **هر روز یک کارت خوانده‌نشدهٔ تازه** — بزرگ‌ترین حلقهٔ عملیِ «خواندم فایده ندارد».
- `quota-*`/`data_risk`/`management_report` (dkey=دوره): هر رخداد/بررسی دوباره = کارت تازه.
- `rfq-due`/`deal-due`: مهر گذار-محور با ویرایش تاریخ/همگام‌سازی دستگاه کهنه گم می‌شود ⇒ شلیق دوباره ⇒ کارت تازه.
`addMsg` (bridge.js) این مشکل را نداشت (dedupe بی‌قیدِ dkey پایدار) — فقط مسیر `notify()`.

### ریشهٔ ۲ (نشانهٔ الف): گم شدن فرمان «خواندم» mid-flight
`ntfRead` → `ptfEntitySaveCollection('ptf_crm_notifs', …, 'w2')` → silentWrite محلی
(پایدار) + `entity_upsert` ناهمگام. فرمان‌های اتمیک **نه dirty می‌سازند نه صف پایدار
دارند** (طراحی عمدی PHASE-C2: «بدون dirty/صف/push»). تنها محافظِ موجود،
`failDirty()` داخل cb است — که اگر صفحه mid-flight رفرش/بسته شود **هرگز اجرا نمی‌شود**.
`beforeunload` موجود (sync.js) هم فقط کلیدهای `state.dirty` را پوشش می‌داد. نتیجه:
readBy روی سرور نمی‌رسد، کلید dirty نیست، و…

### ریشهٔ ۳ (نشانهٔ الف): جایگزینی خامِ notifs در pull
pull بعدی (بوت یا میان‌نشست) که notifs در دلتایش باشد (rev سرور توسط هر دستگاه/فرمان
دیگری بالا رفته باشد): کلید نه held است، نه dirty، و بوتِ معمولی `forceFull` نیست
(`initialSync` → `pullCheck(done)` بدون forceFull) ⇒ شاخهٔ پایانی:
`pullWrite(k, newStr)` — **readBy محلیِ commitنشده بی‌صدا پاک می‌شود** و همان کارت
قدیمی با همان تاریخ قدیمی خوانده‌نشده برمی‌گردد. استثناً مسیرهای merge (held/dirty/
initialReconcile+forceFull) اتحاد می‌کردند — اما دقیقاً هیچ‌کدام در سناریوی «خواندم +
هارد رفرش» فعال نبودند. تناقض طراحی: notifs در `isSharedUnionKey` کلید اتحاد است، ولی
شاخهٔ خامِ pull این قرارداد را نقض می‌کرد.

### ریشهٔ ۴ (نشانهٔ ج): انباشت بی‌سنِ کارت‌های اقدام‌دار
`ptfPruneStaleNotifs` (v33.4.1) فقط غیراقدام‌دارها (+co_expiry/referral_info) را حذف
می‌کرد؛ سیاست صریح «مهم هرگز با گذر زمان حذف نمی‌شود» ⇒ کارت‌های اقدام‌دارِ ماه‌ها
قدیمی (ارجاع‌های فراموش‌شده، سررسیدهای حل‌نشده، گزارش‌های دوره‌های گذشته) برای همیشه
در نمای پیش‌فرض می‌ماندند. دستور جدید کارفرما («مهم و **به‌روز** دیده شوند») این
سیاست را صریحاً بازنشسته می‌کند.

### نقص تکمیلی (چنددستگاهی): `entity_upsert` سرور و readBy
ادغام field-wise سرور (v34.8.34) فیلدهای **حاضر در payload** را wholesale جایگزین
می‌کرد: upsert رکورد کامل از snapshot کهنهٔ یک دستگاه، `readBy` تازه‌تر کاربر دیگر را
پاک می‌کرد (union فقط در merge کانونیک `data_push` بود، نه در مسیر فرمان).

### چرا اصلاح‌های تاریخی کافی نبودند
- v31.7.10: merge اتحاد — فقط در شاخه‌های held/dirty/forceFull+initialReconcile اجرا می‌شد (ریشهٔ ۳ بیرون از آن‌هاست).
- v34.8.34: merge کانونیک سرور — مسیر push را امن کرد؛ pull خام و مسیر فرمان دست‌نخورده ماندند.
- v34.38.6 (DEAL-DUE-SETTLED): resolve کارت‌ها هنگام حل منبع + حذف تک‌رکوردی — «ماندنِ کارهای قدیمیِ **حل‌شده**» را بست؛ «احیای خوانده‌شده» و «انباشتِ حل‌نشده‌ها» را نه.
- RCA 2026-09-08: درست تشخیص داد ولی §7 (boot guard) هرگز پیاده نشده بود و سناریوی mid-flight refresh را نمی‌دید.

## ۴) طراحی و تصمیم‌ها

| تصمیم | چرا |
|---|---|
| dedupe چسبنده **فقط برای dkey صریح** (+referral پایدار مثل قبل) | dkey صریح = هویت پایدارِ یک کار (تعریف v33.4.1: «کارت موجود به‌روز می‌شود نه کارت جدید»). dkey خودکار (عنوان‌محور) باید قرارداد تستر۱۸۵ را حفظ کند: وقوعِ **واقعاً تازه** پس از خواندن، کارت تازه می‌گیرد — آن فراخوان‌ها رویدادهای یک‌بارِ UI هستند، نه حلقهٔ دوره‌ای |
| سیاست سنی ۳۰/۶۰/۹۰ روز: بایگانی نرم (done+ageArchived) نه حذف، برای خوانده‌نشده‌های کهنه | دادهٔ کسب‌وکار و ردّ آن حفظ می‌شود (زیر «نمایش انجام‌شده‌ها» قابل دیدن)، کارتابل تازه می‌شود، و suppress بازتولید با همان dkey جلوی چرخه را می‌گیرد. حذف سخت فقط برای خوانده‌شدهٔ >۶۰ روز (مدت‌ها از دید همه بیرون بوده) و بایگانیِ >۹۰ روز |
| **منبع** هرگز خودکار بسته/حذف نمی‌شود | چکِ سررسیدگذشته یا یادآور باز، واقعیت مالی/کاری است؛ فقط **کارت اعلان** پیر می‌شود. تپش مجدد: اگر منبع زنده بماند، پس از حذفِ بایگانیِ ۹۰ روزه، شلیق واقعیِ بعدی یک کارت تازه می‌سازد (~هر ۱۲۰ روز یک یادآوری به‌جای هر روز) |
| ژورنال in-flight عمومی (همهٔ فرمان‌های اتمیک) به‌جای راه‌حل مخصوص notifs | ریشهٔ ۲ همهٔ فرمان‌ها را می‌زند (مهر `notifiedUsers` یادآور، `dueNotified`، remDone…). بازپخش با همان idempotencyKey به WAL سرور تکیه می‌کند ⇒ بدون اثر دوباره؛ صفحه‌کلید `command_status`/read-only ژورنال نمی‌شود |
| union در pull با dirty-بازگشتی | merge محلی تنها نیمی از راه است؛ اگر نتیجه از نسخهٔ سرور بیشتر باشد (خواندِ commitنشده) کلید dirty می‌شود تا push با merge کانونیک سرور همگرا کند ⇒ readBy بالاخره به سرور و بقیهٔ دستگاه‌ها می‌رسد. tombstone پس از merge جلوی احیای کارت‌های سخت‌حذف‌شدهٔ سرور را می‌گیرد |
| union readBy/done در `entity_upsert` سرور | هم‌قاعده با merge کانونیک data_push؛ snapshot کهنهٔ هیچ دستگاهی read دیگری را پاک نمی‌کند؛ «بازگرداندنِ خوانده‌شدن» هیچ معنایی در سیستم ندارد (done هم OR می‌شود) |
| STALE-BOOT-GUARD روی چهار سازنده | توصیهٔ §7 RCA 2026-09-08: بوت/تیکِ زودهنگام روی کش کهنهٔ ۳۰ ثانیه‌ای، برای منبعی که دستگاه دیگر بسته است کارت می‌سازد و upsert می‌کند. تیک poll پس از bootstrap همان کار را با دادهٔ معتبر انجام می‌دهد |

## ۵) تغییرات (فایل به فایل)

- **crm/rbac.js**: `notify()` — suppress کارت done+ageArchived با dkey صریح + `stickyDkey`
  در شرط dedupe؛ `ptfPruneStaleNotifs` — بازنویسی با سیاست سنی (۳۰/۶۰/۹۰) و
  `ntfAgeDays` (iso → lastISO → تبدیل شمسی با `ptfJToISO`؛ سنِ ناشناخته = محافظه‌کارانه
  نگه دار)؛ commit بایگانی با SaveCollection (upsert) و حذف با `ntfCommitRemoval`
  (تک‌رکوردی) + audit؛ `renderCartable` — prune محافظت‌شده هنگام بازکردن + برچسب
  «بایگانی خودکار قدیمی» در بخش انجام‌شده‌ها؛ `ptfResolveRfqReferral` → `ntfCommitRemoval`.
- **crm/bridge.js**: STALE-BOOT-GUARD در چهار سازنده؛ `addMsg` — suppress
  `priorArchived`؛ prune در بوت (با گارد bootstrap).
- **crm/sync.js**: سه تابع مالکِ ذخیرهٔ ژورنال `ptfSyncInflightJournalWrite/Read/Clear`
  (کلید `ptf_sd_inflight` — اصل A10/E2: فایل نازک sales-domain-v2 مستقیم به localStorage
  دست نمی‌زند؛ sync.js لایهٔ مجاز است)؛ شاخهٔ union مخصوص `ptf_crm_notifs` پیش از جایگزینی خامِ pull
  (merge + tombstone پس از merge + dirty همگرا)؛ حفظ `ageArchived` در هر دو حلقهٔ
  `ptfSmartMerge` notifs؛ قلاب `ptfSalesDomainReplayInflight()` پس از هر دو نقطهٔ
  `bootstrapped = true`.
- **crm/sales-domain-v2.js**: ژورنال in-flight — `inflightRegister/Settle/Persist`،
  ذخیره از طریق مالکِ لایهٔ sync (بالا)، TTL ۴۸ ساعت، سقف ۴۰ فرمان، اتصال `pagehide`+`beforeunload`،
  `window.ptfSalesDomainReplayInflight()` (بازپخش + نگه‌داشتنِ بی‌پاسخ‌ها برای بوت
  بعدی)؛ `api()` — ثبت در پرواز و تسویه در نخستین پاسخ قطعی (ساختار retry/recovery
  دست‌نخورده).
- **api/sales-domain.php**: `entity_upsert` — اتحاد `readBy` و OR شدن `done` برای
  `ptf_crm_notifs` (در شاخهٔ رکورد موجود)؛ `SD_SERVICE_VERSION = 34.38.24`.
- **api/crm.php**: merge کانونیک notifs — اتحاد `ageArchived/ageArchivedAt` در هر دو حلقه (cd و دکلایپهٔ ارجاع).
- نقاط رسمی نسخه: VERSION.json، index.html (PTF_CRM_RELEASE + ۱۱۶ کوئری ?v=)، sw.js ×۳، manifest، clear-cache، shell، cms.js، device-reconnect.

## ۶) سناریوهای بسته‌شده (پس از اصلاح)

1. «خواندم» + هارد رفرش فوری → فرمان در ژورنال است → پس از bootstrap بازپخش و
   commit می‌شود؛ حتی اگر pull زودتر برسد، union merge اجازهٔ پاک‌شدن readBy را
   نمی‌دهد و dirty-push آن را به سرور می‌رساند ⇒ **اعلان قدیمی برنمی‌گردد**.
2. چکِ در پنجرهٔ سررسید، کارت خوانده‌شده → شلیق روزانهٔ بعدی همان کارت را
   بازاستفاده می‌کند (repeat++، محتوا تازه) ⇒ **کارت تازهٔ خوانده‌نشده هر روز ساخته نمی‌شود**.
3. یادآور ۸ ماه پیشِ باز → کارتِ rem-due پس از ۳۰ روز بایگانی سنی می‌شود؛ تیک‌های
   بعدی (حتی با مهرِ گم‌شده) suppress می‌شوند ⇒ **کارتابل «به‌روز» می‌ماند**؛ خودِ
   یادآور در «روز من»/یادآورها قابل اقدام است (انجام/تعویق → کارت را حل و dkey را آزاد می‌کند).
4. دستگاه کهنه رکورد کامل notifs را upsert می‌کند → سرور readBy را union می‌کند ⇒
   «خواندم» کاربر دیگر پاک نمی‌شود.
5. بوت روی کش کهنه (پیش از snapshot) → سازنده‌ها خاموشند ⇒ کارت برای منبعِ
   بسته‌شدهٔ دستگاه دیگر ساخته/upsert نمی‌شود.

## ۷) تست و شواهد

- **tester659-v34.38.24-notif-fresh.js** — ۴۳ چک: ۱۶ ایستا (سپرها/ترتیب شاخهٔ
  union/قلاب‌های بوت/سرور/مالکیت لایهٔ ذخیره) + ۲۷ رفتاری (استخراج توابع واقعی از rbac.js/sync.js/
  sales-domain-v2.js: dedupe چسبنده و preserve readBy، suppress بایگانی، قرارداد
  دست‌نخوردهٔ dkey خودکار و referral و done-حل‌شده، سیاست سنی ۹ سناریو، union
  merge با ageArchived، ژورنال persist/replay/TTL/clear/reject-retain).
- **شاهد قرمز-قبل-سبز**: روی HEAD (کد قبل از اصلاح) **۷ PASS / ۲۲ FAIL** — از جمله
  `cd1≠cd2 و ۲ رکورد` برای کارت خوانده‌شدهٔ chq-due (بازتولید دقیقاً مثل تولید).
  روی کد اصلاح‌شده **۴۳/۰**.
- قراردادهای موجود: تستر۱۸۵ (۱۹/۰ — dkey خودکار/merge/خواندم‌همه)، تستر۵۲۲
  (۲۲/۰ — DING-LOOP) سبز.
- گیت کامل CI (run-ci-gate.js): **۲۷۰ PASS / ۲ FAIL** — مجموعهٔ شکست عیناً بیس‌لاین
  پیش‌موجود (tester427 کارکردی AN-05، tester656 قالب خروجی harness) و بی‌ربط به این
  تغییرات؛ arch-guard (A10/A11/…) سبز — یک دور رفت‌وبرگشت معماری هم خورد: ژورنال
  ابتدا مستقیم در localStorageِ sales-domain-v2 نوشته شد و نگهبان A10 بستش ⇒ مالکیت
  ذخیره به لایهٔ sync منتقل شد (همان کلید، همان قرارداد).

## ۸) ریسک‌ها و پایش

- بایگانی سنیِ کارتِ یک منبعِ زنده (چکِ بازِ ۹۰ روزه) ⇒ تپش مجدد ~۱۲۰ روزه؛ اگر
  کارفرما تپش کوتاه‌تر خواست، `NTF_ARCHIVED_DELETE_DAYS` تنها عددِ لازم است.
- ژورنال in-flight فقط فرمان‌های **غیر read-only** و ≤۴۸ ساعت را نگه می‌دارد؛ اگر
  سرور فرمانی را قطعی رد کند (مثلاً ۴۰۹)، تسویه می‌شود و بازپخش نمی‌گردد (مدیریت
  خطا همان cb/failDirty قبلی است).
- union pull برای notifs، حذفِ بدون tombstoneٔ سرور را احیا می‌کند؟ خیر — tombstone
  پس از merge اعمال می‌شود و entity_delete سرور همیشه tombstone می‌نویسد.
- وضعیت پس از استقرار با شاخص‌های «بایگانی سنی» در audit (module=اعلانات، ref=PRUNE)
  و نبودِ چرخهٔ cd-تازه برای dkeyهای پایدار قابل پایش است.
