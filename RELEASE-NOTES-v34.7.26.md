# RELEASE NOTES — v34.7.26 (۱۴۰۵/۰۵/۲۶ — 2026-08-17)

سه مورد گزارش‌شدهٔ کارفرما در بخش «پرونده‌های فروش». روش کار طبق درخواست: **ابتدا ارزیابی دقیق**
(`ASSESSMENT-SALESFILE-3ISSUES-2026-08-17.md`) و سپس اجرا. هیچ بازنویسی معماری انجام نشده؛ فقط
گارد و هم‌راستاسازی جراحی.

## S1 — «🗑 ابطال کنترل‌شده» و «✏️ اصلاح سندی» در کشوی پرونده کار نمی‌کردند

**ریشه (قطعی):** ناسازگاری کلید شناسه. کشوی پرونده شناسه را با اولویت `cd` می‌فرستاد
(`salesfiles.js:987`)، ولی حل‌کنندهٔ فاکتور با اولویت `_id` می‌خواند
(`official-invoice-v2.js: iid/findInvoice`). هر فاکتور سروری هر دو کلید را دارد ⇒ `findInvoice`
تهی ⇒ `if(!i||!active(i))return;` ⇒ **خروج کاملاً بی‌صدا**. همین نقص باعث می‌شد «اصلاح سندی» هم
به‌جای فرم ویرایش، فرم «ثبت فاکتور جدید» را باز کند (ریسک سند تکراری).

| کد | تغییر |
|----|--------|
| F1-1 | `findInvoice` هر دو کلید `_id` و `cd` را می‌پذیرد (اول `_id`)؛ شناسهٔ تهی هرگز تطبیق نمی‌کند |
| F1-2 | فرستنده‌ها هم‌راستا شدند: `salesfiles.js` و `rbac.js` با `_id \|\| cd` (قرارداد `iid`) |
| F1-3 | پایان شکست خاموش: «یافت نشد»، «فعال نیست (وضعیت …)» و پیام گارد نقش در `ptfInvoiceVoid`، `ptfOfficialInvoiceFilesUi`، `ptfOfficialAttachmentReplace` |
| F1-4 | `showInvModal` با `editId` ناموجود دیگر فرم «ثبت جدید» را باز نمی‌کند؛ سند غیررسمی هم از فرم رسمی اصلاح نمی‌شود و ردیف صورتحساب غیررسمی در کشو به مسیر خودش می‌رود |
| F1-5 | تعریف مردهٔ هم‌نام `window.ptfInvoiceVoid` در `rbac.js` ⇒ `ptfInvoiceVoidLegacy` (بدون اثر رفتاری؛ از v35 همیشه بازنویسی می‌شد) |

سمت سرور نیازی به تغییر نداشت: `void_invoice` از قبل هر دو کلید را می‌پذیرفت.

## S2 — ابزار تشخیص نشت (بدون تغییر داده)

`_tools/diagnose-advance-leak.js` — فقط می‌خواند و گزارش می‌دهد:

```
node _tools/diagnose-advance-leak.js /path/to/crm/data/sync
node _tools/diagnose-advance-leak.js export.json --json
```

خروجی: چهار مسیر نشت + فهرست ردیف‌های `fromAdvance` که مالک فاکتورشان با مالک پرونده نمی‌خواند
(«کدام مشتری، کدام مبلغ، از کدام پرونده»).

## S3 — بستن چهار مسیر «نشت پیش‌پرداخت بین مشتریان»

| کد | مسیر | اصلاح |
|----|------|--------|
| F2-A | `api/sales-domain.php` — انتساب فاکتورِ بدون `offerNo` به پرونده با تطبیق `''===''` (و نوشتن `customerId` مشتری بی‌ربط روی رکورد) | فقط `offerNo` ناتهی + تطبیق **یکتا**؛ تطبیق مبهم ⇒ انتساب نمی‌شود و `finding` با `ruleId=invoice_case_bind_ambiguous` ثبت می‌گردد |
| F2-B | کلید `''` در نقشهٔ پرونده‌های مشتری (`customer-finance.js` ×۲، `ar-reconcile.js` ×۲) ⇒ بلعیدن هر رسیدِ بدون `caseId` | کلید تهی وارد نقشه نمی‌شود؛ رسید بدون `caseId` فقط با `customerId` صریح پذیرفته می‌شود |
| F2-C | تطبیق نامی در `invs(cd)` ⇒ مشتریان هم‌نام حساب یکدیگر را می‌دیدند | تطبیق نامی فقط برای نامی که به **یک** رکورد مشتری می‌رسد |
| F2-D | `x.no === offerNo` با دو مقدار تهی ⇒ خواندن `buyerCd`/`advance` از پیشنهاد بی‌ربط | گارد شمارهٔ ناتهی در `customer-finance.js`، `ar-reconcile.js` و شش نقطهٔ `rbac.js` |

**F2-E (پاکسازی دادهٔ آلوده) عمداً انجام نشده** و منتظر خروجی S2 روی دادهٔ واقعی و تأیید کارفرماست؛
هیچ رکورد مالی بدون تأیید تغییر نمی‌کند.

## S5 — حذف دکمه‌های تکراری کشوی پرونده (طبق تصمیم کارفرما: «بلوک‌های تخصصی مالک باشند»)

- «خرید واقعی» ⇒ مالک واحد: بلوک «🛒 خرید واقعی اقلام» (`buycompare.js`). کاشی نوار عملیات فقط
  به‌عنوان fallback رندر می‌شود (اگر آن ماژول هوک نشده باشد).
- «نوت بازرسی» و «پکینگ‌لیست رسمی» ⇒ مالک واحد: بلوک «📄 اسناد رسمی قالب شرکت» (`docsx.js`) که
  ایجاد + نمایش + اصلاح را یکجا دارد. دو کاشی متناظر با همان قاعدهٔ fallback حذف شدند.
- رفع ابهام برچسب: «ثبت رویداد پکینگ» ⇒ «رویداد پکینگ (گردش‌کار)» تا با «پکینگ‌لیست رسمی» (سند) خلط نشود.
- نتیجه: نوار عملیات پس از برد از ۱۳ به ۱۰ کاشی؛ **هیچ قابلیتی حذف نشده**، فقط تکرار.
- توضیح: «هزینهٔ پرونده» تکراری نبود (بلوک خرید = ثبت هزینهٔ جدید، بخش هزینه‌های کشو = اصلاح هزینهٔ موجود).

## آزمون‌ها

- تسترهای جدید: `_tools/uat/tester428-v34.7.26-salesfile-invoice-actions.js` (**۱۹ PASS / ۰ FAIL**)،
  `_tools/uat/tester429-v34.7.26-cross-customer-advance-leak.js` (**۱۸ PASS / ۰ FAIL**) — شامل
  assertionهای «عدم رگرسیون» برای مسیرهای درست (شناسهٔ واقعی، نام یکتا، رسید متعلق به همان مشتری).
- گیت CI: **۴۲ PASS / ۲ FAIL** — همان دو مورد `known-open` فاز G (tester403، tester415)؛ بدون افزایش.
- سوئیت کامل `node _tools/uat/run-all.js`: **۰ شکست واقعی**.
- `node --check` روی فایل‌های دست‌خورده و توازن ساختاری `api/sales-domain.php`: OK.

## نکتهٔ فنی مهم برای نگهداری

در `rbac.js` گارد `offerNo` **درون‌خطی** نوشته شد و از تابع کمکی استفاده نشد: چند تستر قدیمی بدنهٔ
توابع این فایل را با regex برش می‌زنند و ارجاع به تابع بیرونی باعث `ReferenceError` (کرش، نه FAIL)
در آن‌ها می‌شد. این محدودیت آگاهانه رعایت شده است.

## نسخه

بامپ در ۷ نقطه: `crm/index.html`، `crm/sw.js`، `crm/manifest.json`، `crm/clear-cache.html`،
`crm/shell.js`، `VERSION.json`، `SD_SERVICE_VERSION` در `api/sales-domain.php` ⇒ **v34.7.26**.
