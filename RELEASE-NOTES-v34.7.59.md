# RELEASE NOTES — v34.7.59 (۱۴۰۵/۰۵/۲۹ — 2026-08-20)

## ادامهٔ صفحه‌بندی فهرست S3 — رفع بن‌بست «فهرست ناقص» در گزارش/remap کلید ابری

### مشکل

`action=list` در `api/storage.php` حداکثر **۳۰ صفحه × ۱۰۰۰ کلید** می‌خواند و در باکت
بزرگ‌تر فقط `truncated:true` برمی‌گرداند — **بدون توکن ادامه**. نتیجه:

- گزارش تشخیصی کلیدها (v34.7.52) ردهٔ E را بیش‌برآورد می‌کرد،
- remap گروه B (v34.7.53) طبق طراحی ایمن روی فهرست ناقص **برای همیشه قفل می‌ماند** و
  هیچ راه عبوری وجود نداشت («دوباره تلاش کنید» عملاً بی‌اثر بود).

### اصلاح

- **سرور:** `action=list` حالا پارامتر `token` را برای ادامه می‌پذیرد و در پاسخ
  `nextToken` برمی‌گرداند (سقف ۳۰ صفحه در هر فراخوانی حفظ شد؛ بدون تغییر رفتار برای
  فراخوان‌های قدیمی).
- **کلاینت:** helper جدید `ptfListAllCloudFiles` تا سقف ایمن **۱۲ نوبت (~۳۶۰هزار کلید)**
  ادامه می‌دهد و صفحات را ادغام می‌کند؛ گزارش کلیدها (`ptfCloudKeyAudit`) از آن استفاده
  می‌کند و تعداد نوبت‌ها در audit log ثبت می‌شود.

### ایمنی (عمداً تغییر نکرد)

- اگر حتی با ادامه هم به سقف برسیم، `truncated=true` می‌ماند و **remap طبق قبل غیرفعال**
  است — هیچ remap ای روی فهرست ناقص انجام نمی‌شود.
- **پاک‌سازی یتیم (مسیر DELETE) دست نخورد** و همچنان با فهرست ناقص کاملاً متوقف می‌شود.
- helper فقط `action=list` می‌زند — هیچ DELETE ای در آن وجود ندارد.
- سازگاری عقب‌رو: اگر سرور قدیمی (بدون `nextToken`) پاسخ دهد، `truncated` سرور معتبر
  باقی می‌ماند.

### تست

- تستر جدید `tester462-v34.7.59-cloud-list-pagination.js` (در گیت) — با sandbox:
  ادغام دونوبتی + ارسال token در نوبت دوم، سازگاری سرور قدیمی، گزارش خطای سرور.
- **گیت CI: ۷۸ PASS / ۰ FAIL** · نگهبان معماری: PASS.

### اثر عملی

جلسهٔ «اجرای audit/remap کلید ابری روی Production» (بند ب handoff) دیگر پیش‌نیاز فنی
باز ندارد — فقط حضور کاربر با نقش ارشد لازم است.
