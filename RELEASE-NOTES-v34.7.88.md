# RELEASE NOTES — v34.7.88 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## منفعت خالص فاکتور پوششی/صوری در مانده تامین‌کننده + چیدمان مودال فاکتور
(SUP-VAT-002 / SUP-UX-002)

### ۱. منطق فاکتور صوری/پوششی (SUP-VAT-002)

- **مسئله:** قبلاً در حساب تأمین‌کننده (`balance()`)، فاکتور پوششی/صوری فقط «کارمزد
  فاکتورساز» را به بدهی اضافه می‌کرد؛ اعتبار ارزش‌افزوده‌ی آن کسر نمی‌شد. این با
  گزارش‌های `official-ledger.dart` / `working-capital.js` / `fiscal.js` ناهماهنگ بود.
- **اصلاح:** در `crm/supplier-finance.js → balance()` برای فاکتور پوششی:
  - مقدار بدهی = **کارمزد فاکتورساز − اعتبار ارزش‌افزوده** (منفعت خالص).
  - اگر اعتبار VAT بیشتر از کارمزد باشد، نتیجه منفی می‌شود و به‌عنوان اعتبار/کاهش
    مانده در حساب تأمین‌کننده دیده می‌شود.
  - برخلاف `invRemain`، مبلغ اسمی فاکتور پوششی به بدهی اضافه نمی‌شود.
- این همان رفتار گزارش‌های رسمی/مالی است (اعتبار VAT − کارمزد).

### ۲. چیدمان مودال ثبت فاکتور (SUP-UX-002)

- دکمه‌های «انصراف» و «ثبت فاکتور» هم‌عرض و یکدست (`min-width` + CSS اختصاصی `.sl-inv-footer`).
- فیلد مبلغ با برچسب واضح: «مبلغ فاکتور (بدون ارزش افزوده)» — کاربر متوجه می‌شود
  مبلغ پایه/خالص را وارد کند.
- متن بخش پوششی کوتاه‌تر و فشرده‌تر شد (کمتر به‌هم‌ریخته).
- بخش `slInvVatWrap` و پوششی به‌جای هم‌جوارِ فیلدها در یک ردیف CSS منظم.

### تست
- `_tools/uat/tester490-v34.7.88-supplier-vat.js` (این نسخه به‌روز شد).
- `_tools/uat/tester491-v34.7.88-supplier-cover-ux.js` جدید.
- گیت CI: **107 PASS / 0 FAIL**.

### توجه
- این تغییر فقط در نمایش/محاسبه‌ی مانده در ماژول تأمین‌کننده است؛ مدل داده و
  ذخیره‌سازی `coverVatAmount/coverCommissionAmount/coverNetBenefit` بدون تغییر است.
- اگر فاکتور پوششی قبلی را ویرایش کنید، همان مقادیر ذخیره‌شده محاسبه می‌شوند.
