# 🏛 ریلیز v20.3 — پایان Post-R9: US-428 + US-429

**اسپرینت:** Post-R9 UI مالی  
**موارد:** US-428 — UI زنده درصد/مبلغ پیش‌پرداخت | US-429 — هاب مالی مدیریتی R9

---

## ✅ US-428 — UI زنده درصد/مبلغ پیش‌پرداخت

### اجرا شد
در فرم پیش‌پرداخت ساختاریافته، محاسبه زنده اضافه شد:

- تغییر درصد → مبلغ پیش‌پرداخت محاسبه و نمایش داده می‌شود.
- تغییر مبلغ → درصد محاسبه و نمایش داده می‌شود.
- برای سند ارزی، نرخ تسعیر در محاسبه معادل ریالی/ریال اثر می‌گذارد.
- تغییر نرخ → مبلغ ریالی زنده آپدیت می‌شود.
- اگر مبلغ ثابت از کل سند بیشتر شود، هشدار زنده نمایش داده می‌شود.

### تابع جدید
```js
ptfAdvanceLiveBind(total, cur)
```

### معماری
- داده جدید ساخته نشد.
- فقط لایه UI فرم `ptfAdvanceOpen` ارتقا یافت.
- ذخیره نهایی همچنان از همان `ptfAdvanceNormalize` و `ptf_crm_offers[].advance` استفاده می‌کند.

---

## ✅ US-429 — هاب مالی مدیریتی R9

### مشکل
بعد از تکمیل R9، پنل «تنخواه» برای مدیران شامل چند بخش سنگین شده بود:

- تنخواه
- هزینه جاری
- سهامداران
- سال مالی

### اجرا شد
ماژول جدید اضافه شد:

```js
crm/financehub.js
```

برای `admin/chairman`، بالای پنل تنخواه یک هاب تب‌دار نمایش داده می‌شود:

- 💵 تنخواه
- 🏢 هزینه جاری
- 👥 سهامداران
- 📊 سال مالی

هر تب فقط بخش خودش را نمایش می‌دهد و بقیه بخش‌ها مخفی می‌شوند.

### کاربران عادی
کاربران غیر `admin/chairman` هاب را نمی‌بینند و پنل تنخواه برایشان ساده باقی می‌ماند.

### معماری
- هیچ کلید داده جدیدی ساخته نشد.
- فقط لایه نمایش و UX تغییر کرد.
- hookهای موجود `petty/opex/shareholders/fiscal` حفظ شدند.
- `financehub.js` فقط visibility بخش‌ها را کنترل می‌کند.

---

## فایل‌های تغییر یافته

- `crm/petty.js`
- `crm/financehub.js` — جدید
- `crm/index.html`
- `crm/sw.js`
- `_tools/uat/tester119-v203.js`
- `RELEASE-NOTES-v20.3.md`
- `PTF-MASTER-HANDOVER.md`

---

## 🔬 کیفیت

- تستر جدید: `tester119-v203.js` با ۱۸ چک.
- رگرسیون کامل: **۱۰۶ تستر / صفر خطا**.
- Audit سایت: PASS با همان هشدار قدیمی تصاویر سنگین.

---

## وضعیت Post-R9
با این ریلیز، Post-R9 UI مالی هم بسته شد:

- US-427: سند اصلاحی سال قفل‌شده ✅
- US-426 فاز ۲: CSV/چاپ snapshot ✅
- US-428: UI زنده پیش‌پرداخت ✅
- US-429: هاب مالی مدیریتی ✅
- BUG-026: نسخه‌پذیرسازی تسترها عملاً در ریلیزهای قبل انجام شده و با ۱۰۶ تستر سبز تثبیت شد ✅

## صف بعدی پیشنهادی
طبق صف v20.2:
1. US-423 — کارت ویزیت هوشمند
2. US-405..408 فاز اسناد مستقل از پرونده
3. US-410/411
4. US-402/403
5. US-440 فاز ۲ — توالی تاریخ فاکتور/وصول

**شماره بعدی پیشنهادی:** US-445 / BUG-034
