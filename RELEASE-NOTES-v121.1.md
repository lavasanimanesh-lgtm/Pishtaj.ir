# 🔬 ریلیز v121.1 — راستی‌آزمایی ادعاهای هندآور v90..v120 + رفع شکاف‌ها

**تاریخ:** ۱۶ تیر ۱۴۰۵ | به درخواست کارفرما: تست ادعاهای «بسته‌شده» قبل از اسپرینت ۱۲۲

## نتیجه راستی‌آزمایی ۱۲ ادعا

### ✅ تایید شد (۶): US-104 (PointerLock)، US-105 (SmartMerge)، US-107 (ReasonedDelete+آرشیو)، US-108 (PurgeOrphans در ۲ فایل)، BUG-003 (FOUC)، BUG-005 (delete_batch) + TC در چاپ + AI Workbench وصل به منو.

### ❌ ناقص بود — در v121.1 رفع شد (۴):
1. **US-109 وندور لیست:** بج وندور در `renderCustomers` قدیمی بود ولی `renderCustomers2` (که در لود آن را override می‌کند) بج نداشت → **بج هرگز نمایش داده نمی‌شد**. + هیچ UI برای تنظیم وضعیت وندور نبود (فقط ایمپورت اکسل). رفع: بج در رندر فعال + فیلد «وضعیت وندور + شماره وندور» در فرم مشتری.
2. **US-110 زونکن:** وضعیت‌ها hardcode («تکمیل شده»!) و دکمه‌های مشاهده `alert` جعلی بودند + تابع از هیچ‌جا صدا زده نمی‌شد (مرده). رفع: اتصال به `p.docs` واقعی + شمارش سند هر بخش + پرش به پوشه واقعی پرونده + دکمه «🗄️ زونکن دیجیتال» در کارت پرونده.
3. **US-111 سود خالص:** درصدهای فرضی (۷۲٪/۴٪/۲٪) روی مبلغ جعلی ۱.۵ میلیارد + تابع مرده. رفع: فروش از CO/فاکتور واقعی، خرید از «قیمت‌های خرید» (buycmp)، هزینه جانبی از کاربر، فقط نقش مدیر + دکمه «💰 سود خالص» در کارت پرونده.
4. **باگ v121.0 (قبلا گزارش شد):** `offerPickBuyer` حذف‌شده → کرش انتخاب کارفرما. بازگردانده شد.

### ⚠️ ادعای نادرست — مستند شد (۲):
- **US-103 «setInterval کاملاً ممنوع / CPU Idle ۰٪»:** واقعیت: ۲۹ مورد setInterval در ۱۸ فایل هنوز موجود است (بخشی polling مشروع سینک/ساعت، بخشی pollingِ DOM از ماژول‌های v81..v89 من). ادعا نادرست ثبت شد؛ مهاجرت کامل به Event-Hook → بک‌لاگ TECHDEBT-004.
- **US-116 «کدینگ واحد کلیه موجودیت‌ها»:** فقط کالا مهاجرت کرده؛ genCode قدیمی در ۱۰+ ماژول فعال است → بک‌لاگ TECHDEBT-005.
- **ادعای QA v120 («ممیزی ۶گانه ۱۰۰٪»):** فایل ادعایی `audit_double_check_120.py` در بسته وجود نداشت — غیرقابل اثبات.

## کیفیت: تستر جدید tester34-claims (۱۸ چک دائمی) + رگرسیون ۲۱/۲۱ تستر PASS + ممیز ۰ خطا | کش → `ptf-crm-v121.1`
