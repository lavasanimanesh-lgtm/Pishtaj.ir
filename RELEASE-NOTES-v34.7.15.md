# Release Notes v34.7.15 — تثبیت ادغام پرونده تکراری: planHash مستقل از ترتیب + fallback شناسه کهنه

تاریخ: ۱۴۰۵/۰۵/۲۵ (2026-08-16)

## مشکل باقیمانده پس از v34.7.14

مسیر commit ادغام (`duplicate_case_merge`) پس از تثبیت‌های کش همچنان با دو گیت fail-closed سمت سرور در دادهٔ واقعی متوقف می‌شد:

1. **`duplicate_case_plan_stale`**: `planHash` از `json_encode` خام تکتک پرونده‌ها ساخته می‌شد. چون این سریالیز به **ترتیب کلیدهای آرایهٔ associative** حساس است، هر بازسریالیز جزئی (push دستگاه دیگر، مهاجرت/repair) که کلیدها را با ترتیب متفاوت می‌نوشت — بدون هیچ تغییر تجاری — هش را نامعتبر می‌کرد و کاربر پیام «داده از زمان بازکردن راهنما تغییر کرده» می‌گرفت.

2. **`duplicate_case_not_found`**: `sd_case_offer_linked` وقتی هم `_id` پیشنهاد و هم `rootOfferId` پرونده موجود ولی **متفاوت** بودند، بلافاصله `false` برمی‌گرداند و به `wonOffer`/`offerNo` نگاه نمی‌کرد. نتیجه: پرونده‌ای با `rootOfferId` کهنه/بازتولیدشده بی‌صدا از کاندیدهای ادغام حذف می‌شد و سرور «دو پروندهٔ مرتبط» نمی‌دید.

## اصلاح

- `api/sales-domain.php`
  - تابع `sd_norm_for_hash` اضافه شد: کلیدهای associative به‌صورت بازگشتی مرتب می‌شوند و ترتیب لیست‌ها عمداً حفظ می‌شود (همان قرارداد `sameSyncJson` در `sync.js`).
  - `recordHash` هر کاندید اکنون از `sd_norm_for_hash($case)` ساخته می‌شود تا planHash نسبت به بازسریالیز بی‌اثرِ ترتیب کلید مقاوم باشد.
  - `sd_case_offer_linked`: در تطابق `rootOfferId` با `_id` بلافاصله `true` برمی‌گرداند؛ در غیر این صورت به‌جای رد قطعی، به شناسهٔ متنی `wonOffer`/`offerNo` (و بررسی هویت موجود) fallback می‌کند.
- `crm/sales-domain-v2.js`
  - `caseBelongsToOffer` (آینهٔ کلاینت) با همان رفتار fallback هم‌خوان شد تا یافتهٔ محلی و تشخیص سرور واگرا نشوند.

## چرا این تغییر «بدون آسیب» است

- هیچ تغییری در مسیر **نوشتن** ادغام ایجاد نشده: ادغام شواهد، انتقال ارجاعات مالی/عملیاتی، بازسازی FIFO، snapshot/تصحیح و finding resolved دست‌نخورده‌اند.
- گارد هویت (`case_identity_conflict`) و بررسی `case_offer_link_changed` در commit سرور همچنان فعال‌اند؛ بنابراین fallback فقط «تشخیص» را فراگیرتر می‌کند و نمی‌تواند پروندهٔ اشتباه را ادغام کند.
- `planHash` فقط نویزِ ترتیب را حذف می‌کند؛ تغییر واقعی محتوای پرونده همچنان هش را عوض می‌کند و commit روی دادهٔ تغییرکرده را رد می‌کند.

## تست رگرسیون

- `_tools/uat/tester418-v34.7.15-duplicate-case-planhash-fallback.js`: 11 PASS / 0 FAIL
  1. وجود `sd_norm_for_hash` و استفاده در `recordHash`.
  2. حذف رد قطعی `rootOfferId` کهنه و حفظ گارد هویت commit.
  3. نرمال‌سازی: ترتیب کلیدهای تودرتو اثری بر امضا ندارد؛ ترتیب عناصر لیست همچنان معنادار است.
  4. رفتار کلاینت: `rootOfferId` درست/کهنه/متفاوت (هویت fail-closed) و عدم پنهان‌شدن پرونده از یافتهٔ duplicate_case.
- رگرسیون قبلی بدون شکست:
  - `tester412-v34.7.7-guided-case-dedup.js`: 20 PASS / 0 FAIL
  - `tester417-v34.7.14-duplicate-case-projection-cache.js`: PASS

## کنترل کیفیت

- `node --check` روی فایل‌های JS بدون خطا.
- شمارهٔ نسخه به `v34.7.15` افزایش یافت (VERSION.json، manifest.json، sw.js، clear-cache.html، shell.js و cache-bust تمام اسکریپت‌ها) تا PWA و مرورگر نسخهٔ جدید را بگیرند.
- PHP CLI در محیط محلی موجود نیست؛ syntax با بازبینی دستی و تسترهای منبع‌محور بررسی شد.
