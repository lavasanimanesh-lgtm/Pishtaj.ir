# RELEASE NOTES — v34.22.0 (مرتب‌سازی مکاتبات)

**تاریخ:** ۲۰۲۶-۰۹-۰۱ · **کمیت:** tester567 (۲۰ سنجه) · **گیت:** 183 PASS / 0 FAIL

## شکایت مالک

«نامه‌هایی که اخیرا زده‌ایم به پایین منتقل شده‌اند و امکان سورتینگ بر اساس تاریخ و شماره وجود ندارد.»

## علت ریشه‌ای (RCA)

فهرست مکاتبات **بدون هیچ مرتب‌سازی** و صرفاً به ترتیب آرایهٔ ذخیره‌شده رندر می‌شد. همهٔ مسیرهای ایجاد (ارسال، پیش‌نویس، وارده، دستیار AI) نامهٔ جدید را با `unshift` ابتدای آرایه می‌گذارند — اما پس از **همگام‌سازی چنددستگاهه/سرور**، ترتیب بازگشتی آرایه معتبر نیست و نامه‌های جدید انتهای فهرست برمی‌گشتند. نشانه‌ها با کد تطبیق داده شد.

## رفع — مرتب‌سازی پایدار هنگام رندر (v34.22.0 LET-SORT)

- **پیش‌فرض: جدیدترین تاریخ بالا** — دیگر ترتیب ذخیره‌سازی ملاک نیست؛ به هم ریختگی به‌طور قطعی رفع شد.
- **سرستون‌های کلیک‌پذیر «شماره» و «تاریخ»** با نشانگر جهت ▲/▼ (کلیک دوباره = تغییر جهت) + راهنمای مرتب‌سازی فعال بالای جدول.

### موتور کلید مرتب‌سازی

- **تاریخ:** نرمال‌سازی ارقام فارسی/لاتین، پذیرش `-` و `/`، سال شمسی به مرتبهٔ میلادی (+۶۲۱) برای مقایسهٔ دو تقویم؛ ظرافت در همان روز از آخرین رویداد (ویرایش پیش‌نویس/امضا/ثبت) به دقیقه.
- **شماره:** استخراج (سال، سری) از هر دو قالب — فارسی `۱۴۰۴/پ/ص/۰۰۰۷` و انگلیسی `PTF-OUT-2026-0007`.
- **پایداری:** تساوی‌ها ترتیب فعلی آرایه را نگه می‌دارند.
- مرتب‌سازی فقط هنگام رندر است — **ذخیره‌سازی و مسیرهای ایجاد دست‌نخورده**؛ پیش‌نویس‌های بدون شماره در سورت تاریخ با روز خودشان می‌ایستند.

## فایل‌های کلیدی

`crm/letters.js` (letSortMode/letFaNums/letDateVal/letNoVal/letSortBy + رندر) · `_tools/uat/tester567-v34.22.0-letters-sort.js`
