# یادداشت انتشار — v34.37.5

**تاریخ:** ۲۰۲۶-۰۹-۰۵ (۱۴۰۵/۰۶/۱۴) · **پایه:** v34.37.4
**گیت CI:** ۲۱۷ PASS / ۰ FAIL (شامل tester602 = ۳۲ سنجش با آزمون جهش) · **نگهبان معماری:** PASS

گزارش کارفرما:

> «ثبت نقشهٔ سایت از داخل سی‌آر‌ام انجام نمی‌شود و با توجه به این‌که تمام راه‌های ممکن را رفتم
> نمی‌دانم چه‌کار کنم و مشکل از کجاست.»

---

## ① چرا هیچ مسیری جواب نداد — سه تله، هرکدام مستقل

| # | تله | کجا | نشانه در عمل |
|---|---|---|---|
| ۱ | **دروازۀ وب‌سرور**: `api/.htaccess` از v33.0.0 «deny-by-default» است و allowlist آن `gsc.php` را **ندارد** — روی Apache هر درخواستِ «📤 ثبت نقشه» با 403 وب‌سرور می‌مرد، **بدون هیچ پیامِ PHP‌ای**؛ پنل فقط «پاسخ سرور خطا بود (احتمالاً ۵۰۰)» می‌دید و کاربر طبق پیام به‌سراغ تنظیمات می‌رفت در حالی که درخواست هرگز به `gsc.php` نمی‌رسید | `api/.htaccess` | «با وجود کانفیگ درست، هیچ‌وقت ثبت نمی‌شود» — همان حلقه‌ای که v34.8.30 برای «پنل ابزارها» (tools.php) تجربه و رفع شده بود |
| ۲ | **دامِ راهنما**: مرحلۀ ۲ راهنمای فعال‌سازی می‌گفت سطح سرویس‌اکانت را **Restricted** بگذارید؛ ولی «ثبت نقشه» یک نوشتن است و `gsc_resolve_site` آن را عمداً فقط با **Full** می‌پذیرد ⇒ با راهنمای درست‌اجراشده، داده‌های پنل کار می‌کرد ولی ثبت نقشه همیشه `permission_siteRestrictedUser` می‌گرفت | `GSC-PANEL-SETUP-FA.md` | «همه‌چیز طبق راهنما بود» |
| ۳ | **OK کاذبِ سمت فایل**: در `api/cms.php` نتیجهٔ نوشتنِ `sitemap-*.xml` کنار گذاشته می‌شد؛ اگر فایل‌های ریشه برای کاربر PHP قابل‌نوشت نبودند (مالکیتِ آپلود FTP/git ≠ مجوزِ www — رایج در cPanel)، انتشار می‌گفت «✅ به sitemap هم اضافه شد» و فایل هرگز عوض نمی‌شد ⇒ URL «خارج از نقشه» می‌ماند و ثبتِ index هم بی‌فایده به‌نظر می‌رسید | `api/cms.php` — `sitemap_add` | «صفحه منتشر شد اما در انحرافِ نقشه، خارج از نقشه است» |

مکمل: «ثبت خودکار نقشه پس از انتشار» (`gscSubmitSitemapQuiet`) خطایش را فقط داخل تب «📈 سرچ کنسول» می‌نوشت؛ اگر تب باز نبود، **هیچ** نشانه‌ای نمی‌ماند.

## ② اصلاحات

**۱) `api/.htaccess`** — `gsc` به allowlist اضافه شد (گارد احراز نشست + نقش مدیر ارشد داخل خود `gsc.php` است؛ بقیۀ فایل‌ها مثل قبل غیرفعال). کامنتِ بلاک توضیح می‌دهد چرا.

**۲) `GSC-PANEL-SETUP-FA.md`** — مرحلۀ ۲: **Full** (با توضیح «چرا Restricted رد می‌شود»)؛ خط جدید عیب‌یابی برای `permission_siteRestrictedUser`؛ یادآوری که تغییر سطح خودکار توکنِ کشی را بی‌اعتبار می‌کند (اسکوپ در `crm/data/gsc-token.json` مقایسه می‌شود).

**۳) `api/cms.php` (SITEMAP-HONEST)** — `sitemap_add` اکنون `{ok, file, error, created/existed}` برمی‌گرداند؛ `sitemap_index_ensure` / `sitemap_touch_index` هم بولین؛ هر شکست در `cms_log` با برچسب `sitemap_write_failed` ثبت می‌شود؛ اکشن‌های `blog_create`، `kc_create`، `product_create`، `page_create`، `backup_restore`، `redirect_remove` در پاسخ `jok` فیلدهای `sitemap`/`sitemap_error` دارند (`cms_sm_resp`).

**۴) `crm/cms.js`** — `window.cmsSitemapNote(d)`: در هر هفت نقطۀ انتشار/بازنویسی، اگر `d.sitemap === 'failed'` هشدار علت+راه‌حل (chmod 664 / هم‌مالک‌سازی، سپس «📤 سایت‌مپ + سرچ کنسول») به alert اضافه می‌شود و ادعای بی‌قیدوشرط «به sitemap هم اضافه شد» حذف شد.

**۵) `crm/gsc.js`** — شکستِ ثبت خودکارِ بی‌سروصدا اگر تب GSC باز نباشد، حداقل با toast اعلام می‌شود.

## ③ «حالا مشکل از کجاست؟» — درخت تشخیص ۳۰ ثانیه‌ای روی میزبان

1. تب سئو ← «🧪 آزمون اتصال GSC»:
   - **پیام سرور خطا بود / 403** → تلهٔ ۱ (پیش از v34.37.5) — پس از استقرار این نسخه، خط اول را دوباره بزنید؛
   - **`permission_siteRestrictedUser`** → تلهٔ ۲: در Search Console همان کاربر را **Full** کنید؛
   - **`gsc_not_configured`** → فایل `api/gsc-config.php` روی هاست نیست (مرحلۀ ۳ راهنما)؛
   - **`property_not_found` با فهرست خالی** → دسترسی در جای اشتباه داده شده (IAM ≠ GSC) — جدول عیب‌یابی راهنما.
2. انتشار یک صفحهٔ آزمایشی: اگر alert «نقشهٔ سایت به‌روز نشد» داد → تلهٔ ۳: مجوز فایل‌های `sitemap-*.xml` ریشه؛ `ls -l` و `chmod 664` (یا هم‌گروه‌کردن با `www`) و سپس انتشار مجدد.
3. «🧭 انحراف نقشه» بعد از انتشار باید URL تازه را «در نقشه» نشان دهد؛ اگر نمی‌داد و فایل هم دست‌نخورده بود، همان ۳ است (دیگر بی‌صدا نیست — `cms_log`: `sitemap_write_failed`).

## ④ تست‌ها و گیت‌ها

- `tester602-v34.37.5-sitemap-honest.js` — ۳۲ سنجش: قرارداد php/cms (وضعیت‌برگشتی، لاگ، پاسخ اکشن‌ها + lint واقعی با php-parser)، رفتار `cmsSitemapNote` در `vm`، شرطی‌شدن alert، گارد allowlist در `api/.htaccess`، راهنما (Full + خط عیب‌یابی) و سه آزمون جهش (بازگشت الگوی معیوب ⇒ قرمز).
- گیت CI: **۲۱۷ PASS / ۰ FAIL** · نگهبان معماری: **PASS** (A6 هفت‌نقطه‌ای پس از بامپ، A5 انکدینگ تمیز).
- نسخهٔ `v34.37.5` در هر هفت نقطۀ رسمی + نشانگر `PTF_CMS_JS_VER` + پین ۱۴۷ تستر.
