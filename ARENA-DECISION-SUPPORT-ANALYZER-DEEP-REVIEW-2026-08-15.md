# بررسی عمیق منطق و معماری «تصمیم‌یار مدیریت» و «تحلیلگر هوشمند»
**تاریخ:** ۲۰۲۶-۰۸-۱۵ | **دامنه:** `crm/analyzer.js` ، `crm/management-intelligence.js` ، `crm/insights.js` و لایه‌های داده‌ای متصل
**محرک بررسی:** گزارش کارفرما — «برای یک شرکت ۱۱ آفر دادیم، یکی را بردیم، سیستم نرخ برد ۱۰۰٪ نشان می‌دهد»
**ابزار بازتولید:** `_tools/audit/repro-decision-support-winrate.js` (اجرای مستقل با Node، بدون مرورگر)

---

## ۰) خلاصه اجرایی

مشاهدهٔ کارفرما درست است و **باگ نمایشی نیست؛ خطای تعریف سنجه در هستهٔ محاسبات است**. نرخ برد در همه‌جای تصمیم‌یار با فرمول

```
winRate = won / (won + lost)
```

محاسبه می‌شود؛ یعنی **مخرج فقط پیشنهادهای «تعیین‌تکلیف‌شده»** است. در عمل تیم فروش وضعیت «بازنده» را برای پیشنهادهای بی‌پاسخ ثبت نمی‌کند (این طبیعی و رایج است)، بنابراین ۱۰ پیشنهاد باز از مخرج حذف می‌شوند و ۱/۱ = ۱۰۰٪ نمایش داده می‌شود.

خروجی بازتولید عددی با همان سناریوی کارفرما (اجرای `SCENARIO=A node _tools/audit/repro-decision-support-winrate.js`):

```
نرخ برد نمایش‌داده‌شده: 100٪   (برد=1 | باخت=0 | کل پیشنهاد=11 | باز=10)
نرخ برد بر مبنای کل پیشنهادهای صادرشده: 9.1٪
anlForecast → winP = 100٪  →  پیش‌بینی وزنی = ۱۰۰٪ کل پایپ‌لاین (۱٬۹۰۰٬۰۰۰٬۰۰۰ ریال)
```

اما مسئله به یک فرمول ختم نمی‌شود. در جریان بررسی، **۱۴ ایراد منطقی/معماری** شناسایی شد که سه‌تای آن‌ها (F‑01، F‑02، F‑03) مستقیماً باعث می‌شوند اعداد میز مدیریت **غیرقابل استناد** باشند:

| کد | عنوان | شدت |
|---|---|---|
| F‑01 | مخرج نرخ برد فقط «بسته‌شده‌ها» است → تورم شدید نرخ برد | 🔴 بحرانی |
| F‑02 | نشت خطا به پیش‌بینی درآمد: احتمال برد = نرخ برد متورم → پایپ‌لاین وزنی تا ۱۰۰٪ | 🔴 بحرانی |
| F‑03 | چندپارگی هویت مشتری (کلید کد در برابر کلید نام) → یک شرکت در دو سطر | 🔴 بحرانی |
| F‑04 | اختلاط ارز: جمع یورو/دلار با ریال و برچسب «ریال» | 🔴 بحرانی |
| F‑05 | وصول و فاکتور خارج از منبع واحد مالی (`PTF.invPaidSum`) — فاکتور ابطالی و پرداخت مهاجرت‌شده شمرده می‌شود | 🔴 بحرانی |
| F‑06 | سه تعریف متفاوت از «نرخ برد» در سه ماژول | 🟠 بالا |
| F‑07 | سنجه‌ها بدون بازهٔ زمانی‌اند؛ «گزارش هفتگی» داده مادام‌العمر چاپ می‌کند | 🟠 بالا |
| F‑08 | نبود شاخص اعتماد/حجم نمونه در تصمیم‌یار (برخلاف نمودار حاشیه که دارد) | 🟠 بالا |
| F‑09 | واحد تحلیل «سند» است نه «فرصت»؛ گزینه‌های موازی یک RFQ چند بار شمرده می‌شوند | 🟠 بالا |
| F‑10 | `healthScore` با ضرایب جادویی و بدون کالیبراسیون؛ داده‌نداشتن = امتیاز خنثی ۵۰ | 🟠 بالا |
| F‑11 | تعریف «پرونده نیازمند بررسی» شامل هر پرونده بدون پیشنهاد برنده → نویز | 🟡 متوسط |
| F‑12 | تشخیص «خرید واقعی» تأمین‌کننده با Regex روی متن یادداشت | 🟡 متوسط |
| F‑13 | دور زدن قاعدهٔ دفتر رسمی/غیررسمی در تجمیع (`ptfCanSeeLedger`) | 🟡 متوسط |
| F‑14 | بازهٔ گزارش‌های دوره‌ای میلادی است (دوشنبه/اول ماه میلادی) نه شمسی/مالی | 🟡 متوسط |

پیشنهاد معماری: استخراج یک **لایهٔ سنجهٔ واحد (`crm/metrics-core.js`)** با قرارداد صریح (واحد تحلیل، مخرج، ارز پایه، بازه، اعتماد) که تحلیلگر، تصمیم‌یار، insights و گزارش‌های PDF همگی از آن تغذیه کنند. جزئیات در بخش ۵ و ۶.

---

## ۱) معماری فعلی — نقشهٔ جریان داده

```
localStorage / سرور (getData)
   │
   ├── ptf_crm_offers ──┬─► anlOfferFunnel()          (analyzer.js:18)   فقط kind==='CO'
   │                    ├─► anlMarginWinCurve()       (analyzer.js:48)   kind CO + TC
   │                    ├─► ptfManagementIntelligence (management-intelligence.js:17) kind CO + TC
   │                    └─► ptfWinLossStats()         (insights.js:~90)  سطح «پرونده بایگانی»
   ├── ptf_crm_rfqs ─────► شمارش RFQ در تصمیم‌یار (management-intelligence.js:33)
   ├── ptf_crm_invoices ─┬─► anlForecast()            (analyzer.js:133)
   │                     └─► billed/paid مشتری        (management-intelligence.js:36)
   ├── ptf_crm_buyquotes / ptf_crm_rfqsmart ─► کارنامهٔ تأمین‌کننده (…:41 و :47)
   └── ptf_crm_deals ────► «پرونده‌های نیازمند بررسی» (…:56)
                     │
                     ▼
     UI تحلیلگر (renderAnalyzer) + مودال تصمیم‌یار + PDF مدیریتی + snapshot ارسالی به AI
```

**مشکل ساختاری اصلی:** هیچ لایهٔ مشترکی بین «دادهٔ خام» و «سنجه» وجود ندارد. هر ماژول مستقیماً روی رکورد خام محاسبه می‌کند و تعریف خودش را از برد/باخت/وصول دارد. یعنی معماری در وضعیت *duplicated business logic* است و هر اصلاح باید در ۳ تا ۵ نقطه تکرار شود (خطرِ واگرایی که هم‌اکنون هم رخ داده — F‑06).

نکتهٔ مثبتی که باید حفظ شود: ماژول مالی قبلاً این کار را درست انجام داده و منبع واحد دارد (`window.PTF.invPaidSum` / `isPaymentActive` / `paymentAmtIrr` در `crm/finance-helpers.js:73‑112`). تصمیم‌یار از آن استفاده نمی‌کند (F‑05) — الگوی درست در همین مخزن موجود است و فقط باید تعمیم یابد.

---

## ۲) بازتولید عددی (شواهد اجراپذیر)

هارنس `_tools/audit/repro-decision-support-winrate.js` فایل‌های واقعی CRM را در یک sandbox (vm) بارگذاری می‌کند و با دادهٔ ساختگیِ منطبق بر سناریوی کارفرما، خروجی توابع واقعی را چاپ می‌کند. دو سناریو دارد:

**سناریوی A — دقیقاً گزارش کارفرما (۱۱ پیشنهاد، ۱ برد، هیچ باخت ثبت‌شده):**

```
{"key":"CU-1","name":"شرکت الف","rfqs":1,"offers":11,"won":1,"lost":0,
 "winRate":100,"billed":2100000000,"paid":500000000,"collectionRate":23.8,
 "healthScore":60,"healthLabel":"نیازمند پیگیری","wonValue":1000000000}
{"key":"شرکت الف","name":"شرکت الف","rfqs":3,"offers":0,"won":0,"lost":0,"winRate":null,...}

anlForecast → { pipeline: 1900000000, weighted: 1900000000, winP: 100 }
```

سه یافته در همین یک خروجی دیده می‌شود: نرخ برد ۱۰۰٪ (F‑01)، پیش‌بینی وزنی برابر کل پایپ‌لاین (F‑02) و **دو سطر برای یک شرکت** — یکی با کلید `CU-1` (از پیشنهادها) و یکی با کلید `شرکت الف` (از RFQها) — که باعث می‌شود ستون RFQ و ستون CO هرگز کنار هم ننشینند (F‑03).

**سناریوی پیش‌فرض — با یک باخت ثبت‌شده و یک پیشنهاد یورویی برنده:**

```
anlOfferFunnel → { total:12, won:2, lost:1, winRate:67, wonValue:1000012000 }
مشتری → winRate:66.7 در حالی که نرخ برد واقعی روی ۱۲ پیشنهاد = 16.7٪
ارزش برد: 1,000,012,000  ← ۱۲٬۰۰۰ یورو + ۱٬۰۰۰٬۰۰۰٬۰۰۰ ریال در یک جمع (F‑04)
billed: 2,100,000,000 ← شامل فاکتور ابطال‌شده (status:'void')
paid:   500,000,000  ← شامل پرداخت void و پرداخت مهاجرت‌شده به Receipt،
                        و بی‌توجه به allocatedBase (F‑05)
```

---

## ۳) یافته‌ها به تفکیک — علت ریشه‌ای و اصلاح

### F‑01 🔴 مخرج نرخ برد
**کد:** `crm/analyzer.js:26‑27` و `crm/management-intelligence.js:64‑65`
```js
var closed = won + lost;
var winRate = closed ? Math.round(won * 100 / closed) : null;
```
**ریشه:** فرض ضمنی «هر پیشنهاد باز روزی به برد یا باخت می‌رسد و تا آن روز باید از آمار حذف شود». این فرض در فروش B2B تجهیزات صنعتی غلط است: بخش بزرگی از پیشنهادها هرگز پاسخ رسمی نمی‌گیرند و کاربر هم آن‌ها را دستی «بازنده» نمی‌کند. عملاً مخرج به «مواردی که اتفاقاً ثبت شده‌اند» تقلیل می‌یابد → **سوگیری انتخاب (selection bias)**، و چون ثبت «برد» همیشه انجام می‌شود (به‌خاطر گردش کار پرونده فروش، `offers.js:751‑785`) ولی ثبت «باخت» اختیاری است، سوگیری همیشه **به سمت بالا** است.
**اثر:** نرخ برد، سلامت مشتری، پیش‌بینی درآمد و تفسیر AI همگی خوش‌بینانه؛ تصمیم قیمت‌گذاری بر پایهٔ آن، خطای مستقیم سود.
**اصلاح پیشنهادی:** سه سنجهٔ مجزا به‌جای یک عدد:
- `winRateDecided = won / (won + lost)` — «نرخ برد در بین تعیین‌تکلیف‌شده‌ها» (همان عدد فعلی، ولی با برچسب صریح)
- `winRateAll = won / totalIssued` — «نرخ تبدیل پیشنهاد به سفارش» (کف واقعی)
- `decisionCoverage = (won + lost) / totalIssued` — «پوشش تعیین تکلیف»
و قاعدهٔ نمایش: اگر `decisionCoverage < 60٪`، عدد اصلی **باید** با هشدار «۱۰ پیشنهاد بی‌تکلیف — نرخ برد قابل استناد نیست» نمایش داده شود. علاوه بر این، یک وضعیت سیستمی `expired/stale` برای پیشنهادهایی که از `validUntil` گذشته‌اند (منطق موجود در `offers.js:168`) تا مخرج به‌صورت خودکار واقعی شود.

### F‑02 🔴 نشت خطا به پیش‌بینی درآمد
**کد:** `crm/analyzer.js:144`
```js
var p = f.winRate != null ? f.winRate / 100 : 0.3;
... weighted: Math.round(sentValue * p)
```
**ریشه:** احتمال برد پایپ‌لاین مستقیماً از سنجهٔ متورم F‑01 گرفته می‌شود؛ در سناریوی کارفرما یعنی ضریب ۱٫۰ روی کل پیشنهادهای ارسالی. یعنی سیستم عملاً می‌گوید «هر ریال پایپ‌لاین قطعاً فروش می‌شود».
**اصلاح:** ضریب احتمال باید (الف) از `winRateAll` بیاید نه از نرخ بسته‌شده‌ها، (ب) با میانگین‌گیری بیزی به سمت نرخ پایهٔ سازمان کشیده شود تا نمونهٔ کوچک ضریب ۱ یا ۰ نسازد:
```js
// p_hat = (won + k*prior) / (n + k)  با prior=0.3 و k=5
var p = (won + 5 * 0.3) / (totalIssued + 5);
```
(ج) سقف محافظه‌کارانه (مثلاً ۰٫۷) و (د) ترجیحاً وزن‌دهی بر اساس مرحلهٔ گردش کار RFQ (`st4/st5/st6`) نه یک ضریب واحد.

### F‑03 🔴 چندپارگی هویت مشتری
**کد:** `crm/management-intelligence.js:13` در برابر `:33` و `:36`
```js
function keyOf(o){ return String(o.buyerCd || o.buyerCo || o.co || o.custCd || 'نامشخص') }  // پیشنهادها: کد اول
rfqs.forEach(r => key = String(r.custCd || r.co || r.company || 'نامشخص'))                  // RFQ: کد، اما…
```
**ریشه:** RFQهایی که از مسیرهای قدیمی یا دستیار هوشمند ساخته می‌شوند `custCd` ندارند (نمونه: `crm/ai-workbench.js:623` → `{cd:rCd, co:coName, …}`) و با **نام** کلید می‌خورند، در حالی که پیشنهادها با **کد** کلید می‌خورند. نتیجه: فضای کلید ناهمگن؛ یک شرکت دو ردیف می‌شود (در بازتولید دیده شد).
**اثر زنجیره‌ای:** قاعدهٔ «مشتری نیازمند کنترل هزینه فروش» (`c.control = rfqs>=3 && offers>=2 && won===0`، خط ۶۷) عملاً هرگز درست فعال نمی‌شود، چون RFQ و Offer روی یک سطر جمع نمی‌شوند. رتبه‌بندی مشتریان هم مخدوش است.
**اصلاح:** یک تابع `resolveCustomerKey(record)` واحد که به ترتیب: `buyerCd/custCd` → جست‌وجوی `ptf_crm_customers` بر اساس نام نرمال‌شده (حذف نیم‌فاصله/فاصله/علائم، مشابه الگوی موجود در `customer-finance.js:14`) → و در نهایت نام نرمال‌شده به‌عنوان کلید موقت با پرچم `unresolved:true` که در «کیفیت داده» گزارش شود.

### F‑04 🔴 اختلاط ارز
**کد:** `crm/management-intelligence.js:11‑12`
```js
function money(v){ return n(v).toLocaleString('fa-IR') + ' ریال'; }
function offerTotal(o){ return (o.items||[]).reduce((s,it)=> s + n(it.qty)*n(it.price), 0); }
```
**ریشه:** `offerTotal` فیلد `o.currency` را نمی‌بیند؛ در حالی که پیشنهادها می‌توانند EUR/USD باشند (`offers.js:609` و `:649`) و «نسخهٔ همراه ریالی» هم طبق `offer-rial-convert.js:494` **مجاز به برد/باخت نیست** — پس سند برنده در معاملات ارزی همان سند ارزی است و مبلغ ارزی به‌عنوان ریال جمع می‌شود. همین ایراد در `analyzer.js:29‑31` (`wonValue`) نیز هست.
**اصلاح:** یک `offerTotalIRR(o)` مشترک: اگر `currency !== 'IRR'` از `o.fxRateRef` یا `o.marginAtClose.rate` یا `o.fxConvert.rate` استفاده کن؛ اگر نرخ مرجع نبود، مبلغ را **از جمع خارج کن** و در «شکاف داده» بشمار (`valueSkippedNoFx`) — دقیقاً همان الگویی که نمودار حاشیه با `skipped` به‌درستی به کار برده است (`analyzer.js:57‑58`).

### F‑05 🔴 دور زدن منبع واحد مالی
**کد:** `crm/management-intelligence.js:36‑40` و `crm/analyzer.js:136,171`
```js
var paid = ((i.payments||[]).concat(i.pays||[])).reduce((s,p)=> s + n(p.amt), 0);
c.invoices++; c.billed += n(i.amount); c.paid += paid;
```
در حالی که استاندارد پروژه این است (`finance-helpers.js:99‑112`):
```js
rows.filter(isPaymentActive) … if (PTF_SALES_DOMAIN_V2 && (p.fromAdvance||p.migratedToReceiptId||p.financialProjectionDisabled)) return s;
… + (+inv.allocatedBase||0) + (+inv.allocatedVat||0)
```
**سه خطای هم‌زمان:** ۱) پرداخت‌های ابطالی/برگشتی شمرده می‌شوند؛ ۲) پرداخت‌های میراثی که به Receipt مهاجرت کرده‌اند **دوباره** شمرده می‌شوند در حالی که `allocatedBase/allocatedVat` (وصول واقعی v35) اصلاً دیده نمی‌شود؛ ۳) فاکتورهای `status:'void'` در «فاکتورشده» می‌مانند. نتیجه: «نرخ وصول» تصمیم‌یار با کارت حساب مشتری و خزانه **همخوان نیست** — و این دقیقاً همان جنس مغایرتی است که در `ARENA-LEDGER-DISCREPANCY-2026-08-04.md` قبلاً هزینه داده است.
**اصلاح:** جایگزینی مستقیم با `PTF.invPaidSum(inv,{useAmountIrr:true})` و فیلتر `active(inv)`؛ صفر تغییر در UI لازم است.

### F‑06 🟠 سه تعریف متفاوت از یک سنجه

| ماژول | واحد تحلیل | مخرج | نتیجه روی همان داده |
|---|---|---|---|
| `analyzer.js:27` | سند CO (بدون TC) | won+lost | ۱۰۰٪ |
| `management-intelligence.js:65` | سند CO+TC، به تفکیک مشتری | won+lost | ۱۰۰٪ |
| `insights.js:118‑121` | پروندهٔ مختومهٔ بایگانی | wins+losses | عدد سوم |

مدیر در سه صفحه سه عدد می‌بیند و هیچ‌کدام هم‌ارز نیستند. به‌علاوه حذف `TC` از قیف تحلیلگر (`analyzer.js:20`) در حالی که در نمودار حاشیه (`:50`) و تصمیم‌یار (`:18`) لحاظ شده، یک ناسازگاری کوچک ولی واقعی است.

### F‑07 🟠 نبود بازهٔ زمانی
`ptfManagementIntelligence()` هیچ فیلتر تاریخی ندارد؛ `ptfManagementReportGenerate('weekly')` همان snapshot مادام‌العمر را با عنوان «دوره: ۱۴۰۵-۰۵-۱۹» ذخیره و چاپ می‌کند (`management-intelligence.js:168‑181`). یعنی «گزارش هفتگی» و «گزارش ماهانه» **محتوای یکسان** دارند و روند (trend) قابل استخراج نیست. سنجهٔ بدون بازه برای تصمیم مدیریتی بی‌معناست: نرخ برد امسال با نرخ برد سه سال پیش قاطی است.
**اصلاح:** امضای `ptfManagementIntelligence({fromISO, toISO, basis:'issue'|'close'})` و درج بازه در snapshot و در PDF؛ مبنای تاریخ برای برد = `wonAt`، برای صدور = `dateEn`.

### F‑08 🟠 نبود اعتماد/حجم نمونه
نمودار «احتمال برد بر حسب حاشیه» درست عمل کرده و `conf` و `n` را نشان می‌دهد (`analyzer.js:69‑78`)، اما کارت بزرگ «نرخ برد پیشنهادها» و ستون نرخ برد مشتری هیچ نشانه‌ای از حجم نمونه ندارند؛ ۱/۱ و ۵۰/۵۰ یک‌شکل دیده می‌شوند.
**اصلاح:** الزام نمایش `n` کنار هر درصد و خاکستری‌کردن/«—» برای `n < 3`.

### F‑09 🟠 واحد تحلیل: سند به‌جای فرصت
`rev` روی همان رکورد افزایش می‌یابد (`offers.js:2346`) پس بازنگری‌ها دوشماری نمی‌کنند — این خوب است. اما **گزینه‌های موازی** یک RFQ (اروپایی/چینی) اسناد مستقل‌اند و فقط در صورت تأیید کاربر در دیالوگ `offers.js:765‑773` بازنده می‌شوند؛ اگر کاربر «خیر» بزند، مخرج آلوده می‌ماند. ضمناً «۱۱ آفر» کارفرما ممکن است ۱۱ فرصت نباشد.
**اصلاح:** سنجهٔ اصلی روی **فرصت** (کلید `inqNo`/پروندهٔ فروش) محاسبه شود: فرصت برنده = حداقل یک سند برنده دارد؛ نرخ برد سند به‌عنوان سنجهٔ کمکی باقی بماند.

### F‑10 🟠 امتیاز سلامت کالیبره‌نشده
`management-intelligence.js:69‑75`: پایهٔ ۵۰ + `min(20, winRate/5)` + … − ۲۰ برای control. مشتری با ۱ برد از ۱۱ آفر امتیاز ۶۰ («نیازمند پیگیری») می‌گیرد، و مشتری فاقد هرگونه داده هم ۵۰ می‌گیرد — یعنی **نبود داده تقریباً هم‌ارز عملکرد متوسط** است. ضرایب هیچ‌جا مستند/مصوب نیستند.
**اصلاح:** یا مستندسازی و تصویب صریح ضرایب در یک Policy Record (مشابه `STAGE-0-POLICY-DECISION-RECORD-v31.7.md`)، یا تبدیل به رتبه‌بندی نسبی (چارک‌ها) + برچسب «دادهٔ ناکافی» وقتی `n<3`.

### F‑11 🟡 تعریف پرکار «ریسک»
`management-intelligence.js:56‑60`: `if (overdue || qcBad || !d.wonOffer) projects.push(...)` — هر پروندهٔ فروشِ در جریان که هنوز پیشنهاد برنده ندارد «نیازمند بررسی» شمرده می‌شود؛ در بازتولید، پروندهٔ کاملاً سالم `SF-2` هم ریسک شد. کارت قرمز «پرونده نیازمند بررسی» عملاً شمارندهٔ پرونده‌های باز است.
**اصلاح:** تفکیک سه دسته: تأخیر تحویل / عدم انطباق QC / پروندهٔ راکد (بدون رویداد در N روز) — و حذف شرط صرفِ نبودِ `wonOffer`.

### F‑12 🟡 استنتاج «خرید واقعی» با Regex
`management-intelligence.js:44`: `if (/خرید واقعی/.test(String(b.note||''))) s.purchases++;` — وابسته به متن آزاد کاربر. در حالی که منبع ساختاریافته وجود دارد (`ptf_crm_buycmp` → `purchases[]`، و `supplier-finance.js:1162`).
**اصلاح:** خواندن از `ptf_crm_buycmp`/فاکتور خرید به‌جای متن.

### F‑13 🟡 قاعدهٔ دفتر رسمی/غیررسمی
`customer-finance.js:18` قبل از تجمیع، دسترسی به دفتر غیررسمی را با `ptfCanSeeLedger('unofficial')` کنترل می‌کند. تصمیم‌یار این فیلتر را ندارد؛ هر کاربر با پرچم `finance` (شرط ورود در `analyzer.js:~350` و `ptfManagementInsightsOpen`) ارقام تجمیعی هر دو دفتر و نام مشتریان را می‌بیند و با دکمهٔ «✨ تفسیر AI» همان snapshot به `api/llm.php` ارسال می‌شود.
**اصلاح:** اعمال همان gate در ورودی تجمیع + ثبت در audit اینکه snapshot ارسالی شامل چه دفتری بوده است.

### F‑14 🟡 تقویم گزارش‌های دوره‌ای
`reportPeriod()` و `ptfManagementReportCheck()` (`:157‑163`، `:203‑210`) با `getDay()===1` و `getDate()===1` میلادی کار می‌کنند و دورهٔ ماهانه را `YYYY-MM` میلادی می‌سازند — ناسازگار با تقویم عملیاتی/مالی شمسی که بقیهٔ سیستم (`ptfJToISO`، `faDate`) بر آن استوار است.

**نکات فرعی (بدون کد شدت‌دار):** `keyOf` به فیلد ناموجود `o.co` ارجاع می‌دهد (بی‌اثر ولی گمراه‌کننده)؛ `ptfManagementIntelligence()` در هر بار باز شدن مودال و هر PDF از نو کل داده را می‌پیماید (سه بار متوالی)؛ `rfqWithoutOwner` فقط `r.assignee` را می‌بیند در حالی که مالکیت در مسیرهای دیگر با فیلد `owner` هم ثبت می‌شود.

---

## ۴) پاسخ مستقیم به پرسش کارفرما

> «۱۱ آفر دادیم، یکی را بردیم، نرخ برد ۱۰۰٪ زده است.»

سیستم عدد را «نرخ برد از میان پیشنهادهایی که نتیجه‌شان ثبت شده» حساب می‌کند و چون ۱۰ پیشنهاد دیگر هنوز `sent/draft` هستند، آن‌ها را اصلاً وارد محاسبه نمی‌کند. عدد صحیح‌تری که مدیر انتظار دارد `۹٪ (۱ از ۱۱)` است. برچسب فعلی روی کارت («نرخ برد پیشنهادها») این تفاوت را پنهان می‌کند. بنابراین: **هم فرمول باید به سه سنجهٔ صریح بشکند، هم برچسب باید دقیق شود، هم پیشنهادهای بی‌تکلیف باید در خودِ گردش کار تعیین تکلیف شوند (وضعیت منقضی خودکار).**

---

## ۵) قرارداد پیشنهادی سنجه‌ها (Metric Contract)

هر سنجه‌ای که به مدیر نمایش داده می‌شود باید این پنج ویژگی را صریح داشته باشد:

| ویژگی | قاعده |
|---|---|
| واحد تحلیل | فرصت (`inqNo`) پیش‌فرض؛ سند فقط به‌عنوان سنجهٔ کمکی |
| مخرج | صریح در برچسب: «از میان تعیین‌تکلیف‌شده‌ها» یا «از کل صادرشده» |
| ارز | همه‌چیز به IRR نرمال؛ آنچه نرخ مرجع ندارد از جمع خارج و شمرده شود |
| بازه | هر عدد دارای `from/to` و مبنای تاریخ (صدور یا بسته‌شدن) |
| اعتماد | نمایش `n`؛ زیر ۳ نمونه عدد نمایش داده نشود |

سنجه‌های استاندارد پیشنهادی:
```
issued, decided, open, expired
winRateAll      = won / issued
winRateDecided  = won / decided
decisionCoverage= decided / issued
pWin            = (won + k*prior) / (issued + k)      // k=5, prior=0.30
wonValueIRR, pipelineIRR, weightedPipelineIRR = Σ(valueIRR × pWin_stage)
billedIRR (فعال) , collectedIRR (PTF.invPaidSum) , collectionRate
```

---

## ۶) معماری هدف

```
crm/metrics-core.js   ← جدید، تنها منبع سنجه (بدون DOM، قابل تست با Node)
   ├─ resolveCustomerKey()        F-03
   ├─ offerTotalIRR() / fxRateOf()F-04
   ├─ invoiceBilledIRR/CollectedIRR → wrapper روی PTF.invPaidSum   F-05
   ├─ opportunityRollup()         F-09
   └─ salesMetrics({from,to,basis}) → قرارداد بخش ۵      F-01/02/07/08
        ▲              ▲                ▲                ▲
analyzer.js   management-intelligence.js  insights.js   reports.js/PDF
```

اصول: (۱) هیچ ماژول UI حق محاسبهٔ سنجه ندارد؛ (۲) `metrics-core` وابستگی DOM ندارد تا در CI با Node تست شود (همان روشی که هارنس این گزارش نشان داد شدنی است)؛ (۳) هر سنجه به‌همراه `meta` (n، coverage، skipped، currencyGaps) برگردانده شود تا UI مجبور به نمایش شفافیت باشد؛ (۴) snapshot ارسالی به AI فقط از خروجی `metrics-core` ساخته شود تا تفسیر AI روی همان اعداد ممیزی‌شده انجام شود.

---

## ۷) نقشهٔ راه پیشنهادی

**فاز ۰ — اصلاح فوری صداقت اعداد (کم‌ریسک، بدون تغییر معماری)**
1. افزودن `winRateAll` و `decisionCoverage` و نمایش `n` کنار هر درصد؛ تغییر برچسب کارت به «نرخ برد (از تعیین‌تکلیف‌شده‌ها)» + بَج هشدار وقتی پوشش < ۶۰٪.
2. جایگزینی محاسبهٔ وصول با `PTF.invPaidSum` و فیلتر فاکتور فعال. (F‑05)
3. نرمال‌سازی ارز در `offerTotal` + شمارش موارد بدون نرخ مرجع. (F‑04)
4. یکسان‌سازی `resolveCustomerKey` بین offers/rfqs/invoices. (F‑03)
5. تغییر ضریب پیش‌بینی به فرمول بیزی با سقف. (F‑02)

**فاز ۱ — استخراج `metrics-core.js`** و مهاجرت هر سه ماژول به آن + حذف تعاریف تکراری (F‑06) + پارامتر بازه (F‑07).

**فاز ۲ — کیفیت گردش کار:** وضعیت خودکار «منقضی» برای پیشنهادهای گذشته از اعتبار، اجباری‌کردن دلیل باخت، بازتعریف «ریسک پرونده» (F‑11)، منبع ساختاریافتهٔ خرید واقعی (F‑12)، تقویم شمسی گزارش‌ها (F‑14)، gate دفتر رسمی/غیررسمی (F‑13).

**فاز ۳ — کالیبراسیون:** بازطراحی `healthScore` با ضرایب مصوب و بک‌تست روی دادهٔ واقعی؛ وزن‌دهی پایپ‌لاین بر اساس مرحلهٔ RFQ.

**تست‌های پذیرش پیشنهادی (قابل افزودن به همین هارنس):**
- ۱۱ پیشنهاد / ۱ برد / ۰ باخت ⇒ `winRateAll=9.1`، `winRateDecided=100`، `coverage=9.1`، و UI باید هشدار پوشش پایین بدهد.
- یک مشتری با RFQ بدون `custCd` ⇒ فقط **یک** سطر در جدول مشتریان.
- پیشنهاد یورویی بدون نرخ مرجع ⇒ در `wonValueIRR` نیاید و در `currencyGaps` شمرده شود.
- فاکتور `void` و پرداخت `migratedToReceiptId` ⇒ در `billed/collected` اثر نگذارند؛ `allocatedBase` شمرده شود.
- `n<3` ⇒ درصد نمایش داده نشود.

---

## ۸) جمع‌بندی

منطق فعلی «اشتباه محاسبه» نمی‌کند؛ **چیز دیگری غیر از آنچه مدیر می‌خواند را محاسبه می‌کند** و این خطرناک‌تر است، چون خروجی ظاهراً معتبر است و اکنون به لایهٔ AI و PDF مدیریتی هم تغذیه می‌شود. ریشهٔ معماری، نبود لایهٔ سنجهٔ مشترک و تکرار منطق کسب‌وکار در UI است؛ ریشهٔ منطقی، تعریف مخرج و نادیده‌گرفتن ارز/بازه/حجم نمونه. فاز ۰ با تغییر محدود در دو فایل، اعداد را قابل استناد می‌کند و فاز ۱ از واگرایی دوبارهٔ تعاریف جلوگیری می‌کند.

> فایل بازتولید: `_tools/audit/repro-decision-support-winrate.js` — `node _tools/audit/repro-decision-support-winrate.js` و `SCENARIO=A node …` (ابزار ممیزی؛ در بارگذاری CRM نقشی ندارد).
