# 📦 ریلیزنوت v122.5 — US-285: رفع فوری‌های هیئت ارزیابی موبایل (M1..M6)

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v122.4 | **سند کامل هیئت:** `ASSESSMENT-MOBILE-UX-v122.md`

---

## 🏛 هیئت ۶ نفره متخصصان موبایل تشکیل شد
(معمار PWA، متخصص UI موبایل RTL، متخصص iOS Safari، دسترس‌پذیری، عملکرد فرانت، نماینده کاربر نهایی) — نتیجه: **۴ ایراد بحرانی + ۲ مهم رفع فوری شد** و **۹ پیشنهاد بهینه‌سازی (US-286..294)** برای تصمیم شما لیست شد.

## ✅ رفع‌شده در همین نسخه

| # | ایراد (گزارش شما) | ریشه واقعی | رفع |
|---|---|---|---|
| M1 | آیکون‌های زیرشاخه سایدبار دیده نمی‌شوند | قاعده موبایل همه spanهای غیر `.ic` را مخفی می‌کرد؛ از v122.4 آیکون‌های iconx داخل span جدید‌اند → خودشان قربانی قاعده شدند | سلکتور به فرزند مستقیم محدود شد + قاعده صریح زنده‌ماندن SVGها (در هر دو نسخه CSS) |
| M2 | عرض متون بیش از عرض سایدبار | برچسب سرگروه‌ها یک تکه بود و در ستون ۵۶px سرریز می‌کرد | سرگروه به آیکون (`.gi`) + متن ۹px با ellipsis (`.gt`) تفکیک شد — دسکتاپ بدون تغییر |
| M3 | حالت شب: متن روشن روی کارت سفید ناخوانا | بلوک موبایل US-112 رنگ‌ها را هاردکد کرده بود (`#fff`, `#f1f5f9`) و متغیرهای تم شب را له می‌کرد | هر ۳ نقطه × ۲ بلوک تکراری → متغیر تم (`--bg`, `--crd`, `--brd`): کارت‌ها در شب تیره و خوانا |
| M4 | پرش/زوم ناخواسته هنگام تایپ در iPhone | فونت ورودی‌ها 14px بود؛ سافاری روی input زیر 16px زوم اجباری می‌کند | فیلدهای ورودی موبایل → 16px (دکمه‌ها 14px ماندند) |
| M5 | محتوا زیر هوم‌بار iPhone | safe-area رعایت نمی‌شد | `viewport-fit=cover` + `env(safe-area-inset-bottom)` |
| M6 | سرگروه فعال در موبایل نامشخص | — | با ساختار جدید M2 حل شد |

## 📋 پیشنهادات هیئت — منتظر تصمیم شما (هیچ‌کدام بدون تایید اجرا نمی‌شود)

| اولویت | US | پیشنهاد |
|---|---|---|
| ⭐ تعیین‌کننده | **US-286** | **نوار ناوبری پایین (Bottom Tab Bar)** به‌جای سایدبار در موبایل: داشبورد، کارتابل (بج)، فروش، تامین، «بیشتر» — الگوی اپ‌های بومی، دسترس شست، آزاد شدن عرض کامل صفحه |
| بسته ناوبری | US-287 | هدر موبایل تک‌ردیفه (حذف ساعت/تاریخ در موبایل) |
| بسته ناوبری | US-290 | جستجو در موبایل: دکمه در نوار پایین + مخفی شدن برچسب Ctrl+K |
| خوانایی | US-291 | برچسب ستون‌ها روی کارت‌های موبایل (`data-label` + `td::before`) — کارت‌های فعلی مقادیر بی‌عنوان دارند |
| تعامل | US-288 | مودال‌ها در موبایل → Bottom Sheet با دکمه ثبت چسبان |
| تعامل | US-289 | Swipe Action روی کارت‌ها + فاصله‌گیری دکمه حذف از لبه |
| عملکرد | US-292 | باندل‌سازی JS در ریلیز (کاهش بار اولین ورود روی 4G) |
| نصب | US-293 | بنر «افزودن به صفحه اصلی» (PWA) |
| جزئی | US-294 | اصلاح مودال‌ها در حالت افقی |

پیشنهاد هیئت برای شروع: بسته ناوبری (US-286+287+290) به‌عنوان یک اسپرینت مستقل موبایل.

## 🧪 کیفیت
audit ✅ ۰ خطا | تستر جدید `tester40` ۲۱/۲۱ ✅ | رگرسیون کامل **۲۷ تستر** ✅ (tester39 نسخه‌آزاد شد)

## 📁 فایل‌های تغییریافته
`crm/index.html` (CSS موبایل + viewport + VER) · `crm/shell.js` (مارک‌آپ سرگروه gi/gt + CSS موبایل) · `crm/sw.js` · `ASSESSMENT-MOBILE-UX-v122.md` (جدید) · `_tools/uat/tester40` (جدید)

📌 پس از آپلود: رفرش سخت (یا بستن/بازکردن PWA) برای پاک شدن کش نسخه قبل.
