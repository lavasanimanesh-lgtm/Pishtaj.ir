# یادداشت‌های انتشار (Release Notes — Version 33.10.0)

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (PR #11 باز تا پایان مهاجرت MySQL)
**تمرکز ریلیز:** **پیاده‌سازی منطق نقدی سود و تقسیم سهامداران (مصوب کارفرما)** — درآمد واقعی = وصولی‌ها؛ کسر همهٔ خروجی‌ها (خرید نقدی/غیرنقدی + چک صادره حتی بابت مسائل دیگر)؛ کف نقدینگی در گردش؛ تقسیم فقط از مازاد بر کف + بازگشت باقی به کف؛ ثبت واقعی تقسیم در دفاتر سهامداران.

---

## 🚀 دستاوردهای کلیدی نسخه ۳۳.۱۰.۰

### ۱. منطق نقدی سود (مصوب کارفرما — پیاده‌سازی کامل)
- **درآمد واقعی = وصولی‌های دوره**: نقد + چک واردهٔ **وصول‌شده**؛ چک واردهٔ در راه «درآمد نیست» (کارت جداگانه نمایش داده می‌شود).
- **خروجی (هزینه) = همهٔ خروجی‌ها**:
  - فاکتورهای خرید تأمین‌کننده ثبت‌شده در دوره (نقدی و غیرنقدی)
  - بدهی‌های legacy + اصلاحیات مثبت تأمین
  - پرداخت‌های نقدی بدون تخصیص به فاکتور (مسائل دیگر/پیش‌پرداخت)
  - **چک‌های صادرهٔ مالی مستقل** (بابت مسائل دیگر) — طبق دستور کارفرما «حتی چکی که شرکت بابت مسایل دیگر می‌دهد از درآمد کسر می‌شود»
  - هزینه‌های جاری سال + تنخواه مستقل سال
  - **بدون دوباره‌شماری**: چک صادره/پرداختی بابت فاکتوری که قبلاً کسر شده، دوباره کسر نمی‌شود؛ ضمانت (تعهد مستردشدنی) خروج نقدی نیست.
- **سود نقدی دوره** = درآمد نقدی − خروجی؛ **موجودی نقد پایان** = نقد/بانک ابتدای سال (افتتاحیه) + سود نقدی.

### ۲. کف نقدینگی در گردش + تقسیم
- **کف نقدینگی (عدد دستی):** دکمهٔ «⚙️ کف نقدینگی» در داشبورد سال مالی → ذخیره در snapshots (`cash_floor`) و قابل تغییر هر سال.
- **مازاد بر کف** = max(0, موجودی نقد پایان − کف).
- **قابل تقسیم** = مازاد × درصد توافقی (مثلاً ۶۰٪)؛ **بازگشت به کف** = مازاد − قابل تقسیم (مثلاً ۴۰٪ → به کف دورهٔ بعد اضافه می‌شود).
- **زیر کف بودن → تقسیم ممنوع** (هشدار قرمز).

### ۳. ثبت واقعی تقسیم در دفاتر سهامداران
- دکمهٔ «💰 ثبت تقسیم سود در دفاتر سهامداران» → برای هر سهامدار فعال یک تراکنش `type:'profit'` در `ptf_crm_sharetx` + سند `dividend` در snapshots + audit.
- گاردها: فقط مدیران ارشد؛ سال باید قفل باشد؛ **یک‌بار در سال**؛ زیر کف → مسدود.

### ۴. تغییر رفتار چک وارده (مطابق منطق نقدی)
- **چک وارده هنگام «ثبت» دیگر payment روی فاکتور نمی‌سازد** (فقط «در گردش» با `pendingFinancial`).
- **«وصول» چک** → payment روی فاکتور ساخته می‌شود (تحقق درآمد) — در همهٔ ارزها یکسان.
- چک وصول‌شده دیگر «برگشتی» نمی‌شود (گارد state) — معکوس فقط پیش از وصول.

### ۵. داشبورد سال مالی
بلوک سبز «💵 سود نقدی و تقسیم (منطق نقدی — مصوب کارفرما)»: کارت‌های درآمد نقدی / چک وارده وصول‌نشده / خروجی (با تفکیک) / سود نقدی / نقد ابتدا / موجودی نقد پایان / کف / مازاد بر کف / قابل تقسیم / بازگشت به کف + جدول سهامداران نقدی + هشدار زیر کف. (سود تعهدی فعلی هم به‌عنوان «گزارشی» حفظ شد.)

---

## 📋 تغییرات فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/fiscal.js` | `ptfFiscalCashData` / `ptfFiscalCashDistribution` / `ptfFiscalCashFloor(Set/Open)` / `ptfFiscalDividendApply` + بلوک UI نقدی |
| `crm/cheque-module.js` | چک وارده: اثر مالی فقط هنگام وصول (`pendingFinancial`)؛ وصول → payment؛ برگشتی پس از وصول مسدود |
| `crm/ANALYSIS-PROFIT-DIVIDEND-2026-08-02.md` | بخش ۵: منطق نقدی مصوب + فرمول‌ها + وضعیت باگ‌های قبلی (P-2/P-6/P-3 رفع شدند) |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.10.0 |
| `_tools/uat/tester290-cash-profit.js` | **جدید** (۱۵/۰) |
| `_tools/uat/tester215,288` | به‌روزرسانی به رفتار نقدی چک وارده (۴۷/۰ و ۳۹/۰) |
| `RELEASE-NOTES-v33.10.0.md` / `REGRESSION-REPORT-v33.10.0.md` | اسناد انتشار |
| `_audit/HANDOFF-NEXT-SESSION-2026-08-01.md`، `crm/BACKLOG-USER-2026-08-01.md` | به‌روزرسانی |

---

## ⚠️ برای کاربر
1. `Ctrl+Shift+R` یا `crm/clear-cache.html` (نسخه باید **v33.10.0** باشد).
2. داشبورد سال مالی → بلوک «💵 سود نقدی و تقسیم»: ابتدا «⚙️ کف نقدینگی» را تعیین کنید → درصد تقسیم را بگذارید → «💰 ثبت تقسیم سود».
3. چک وارده حالا فقط با «✔ وصول» اثر مالی می‌گیرد (در تب چک‌ها).
