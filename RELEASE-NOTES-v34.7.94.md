# RELEASE NOTES — v34.7.94 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## UAT پایانی ارزش افزوده فصلی (VAT-LEDGER-003) + بستن تخلف زرد نگهبان

### ۱) UAT پایانی ارزش افزوده فصلی

- `tester496-v34.7.94-vat-quarterly-uat.js` — یازده خانوادهٔ سناریو edge با vm/sandbox:
  - E1/E2: تاریخ‌های شمسی خالص + ISO با سال ایرانی → فصل درست.
  - E3: نرخ‌های VAT سالانه غیر ۱۰٪ (1404/1405/1406) از `ptfVatRateOf`.
  - E4: پوششی با کارمزد بزرگ‌تر از VAT — کارمزد در VAT وارد نمی‌شود.
  - E5/E6: تسویه = ثبت مدرک (رفتار فعلی تثبیت‌شد)؛ حذف تسویه → payable برمی‌گردد.
  - E7: عدم انتقال carry از زمستان سال قبل به بهار سال بعد.
  - E8: فاکتور پوششی بدون `coverVatAmount` → صفر، بدون crash.
  - E9: فاکتور با تاریخ خراب/غایب در هیچ فصلی شمرده نشود (بدون crash).
  - E10: زنجیرهٔ carry سه‌فصلی (بهار→تابستان→پاییز).
  - E11: پس از تسویه، فصل بعد از صفر شروع می‌شود.
- سند کامل UAT: `UAT-VAT-QUARTERLY-FINAL-2026-08-22.md` (شامل سه یافتهٔ تحلیلی: محدودیت تاریخ میلادی، تسویهٔ جزئی، جداسازی سال مالی).

### ۲) بستن تخلف زرد A7 (نگهبان معماری)

- `crm/offers.js → ptfSupFilesOpen`: در هر شاخهٔ خطا (تامین‌کنندهٔ ناشناخته، فایل نامعتبر) یک `toast('❌ …')` اضافه شد تا نگهبان معماری (`A7 — شکست خاموش در اکشن کاربر`) دیگر تخلفی گزارش نکند.

### تست

- گیت CI: **۱۱۲ PASS / ۰ FAIL** (پس از افزودن tester496).
- نگهبان معماری: **PASS** — تخلف زرد A7 روی `ptfSupFilesOpen` بسته شد.

### فایل‌ها

- افزوده: `_tools/uat/tester496-v34.7.94-vat-quarterly-uat.js`, `UAT-VAT-QUARTERLY-FINAL-2026-08-22.md`, `RELEASE-NOTES-v34.7.94.md`.
- تغییر: `_tools/uat/run-ci-gate.js` (ثبت tester496)، `crm/offers.js` (toast در ptfSupFilesOpen)، `VERSION.json`, بامپ کش‌بست ۷ نقطهٔ رسمی + همهٔ تسترها.
