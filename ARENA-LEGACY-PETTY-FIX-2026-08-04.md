# فاز ۹ (v34.0.12-alpha) — هماهنگ‌سازی تعهد لگاسی در سود + ترتیب تاریخ گزارش تنخواه

**برنچ:** `arena/019fcd1d-pishtaj-ir`
**مبنای تصمیم:** ارزیابی تعهد لگاسی (ARENA-LEGACY-COMMITMENT-REVIEW) + تأیید کارفرما + بررسی گزارش دوره‌ای تنخواه.

---

## اصلاح ۱ — تعهد لگاسی دیگر در سود سال مالی دوباره‌شماری نمی‌شود

**مشکل (از ارزیابی):** در `fiscal.js` (موتور سود سال مالی)، payables با `pay:'credit'` و `!sfInvoiceCd` هنوز به `out.supplierInvoices` اضافه می‌شد → اگر فاکتور به تعهد لگاسی لینک نشود، هزینهٔ مضاعف (تعهد + فاکتور هم‌مبلغ) در سود نقدی محاسبه می‌شد. این با فاز ۳/۶ (که legacy را از `balance()`/working-capital حذف کردند) ناسازگار بود و همان مغایرت گزارش‌ها را تولید می‌کرد.

**اصلاح (`crm/fiscal.js`):** بلوک payables در `ptfFiscalCashData` دیگر legacy را به خروجی نقدی سود اضافه نمی‌کند (با کامنت توضیحی). مبنای هزینه/تعهد فقط فاکتور خرید است.

**تأیید:** تعهد ۱۰۰م لینک‌نشده + فاکتور ۱۰۰م → خروجی سود = **۱۰۰م** (نه ۲۰۰م). ✅

---

## اصلاح ۲ — ترتیب درست تاریخ در گزارش دورهٔ تنخواه

**مشکل (گزارش کاربر):** «گزارش دورهٔ تنخواه به ترتیب تاریخ نیست.» ریشه: در `ptfPettyPeriodEvents` مرتب‌سازی فقط با `a.t` (رشتهٔ خام) انجام می‌شد؛ تاریخ‌ها در `t` با فرمت‌های ناهمگن‌اند (شمسی `1405/05/20`، میلادی `2026-07-30`، یا با ساعت) → مقایسهٔ رشت‌های بین این فرمت‌ها ترتیب اشتباه می‌داد.

**اصلاح (`crm/petty.js`):** مرتب‌سازی با `recDate()` (نرمال‌سازی به شمسی `YYYY/MM/DD`) و `recDateCmp()` (مقایسهٔ عددی `YYYYMMDD`) انجام می‌شود؛ رکوردِ بی‌تاریخ به انتها می‌رود؛ رکوردهای هم‌تاریخ با شناسه برای پایداری مرتب می‌شوند.

**تأیید:** دادهٔ ناهمگن (شمسی/میلادی/با ساعت) → ترتیب `14050510, 14050510, 14050520, 14050601` = **صعودی**. ✅

---

## تست‌ها
- **tester308** جدید (۹/۰): اصلاح legacy در fiscal + ترتیب تنخواه + شبیه‌سازی هر دو.
- رگرسیون کامل سبز (**۱۸ تستر، ۰ شکست**).
- بامپ نسخه → **v34.0.12-alpha** (VERSION.json + VER + باستر + کش SW + clear-cache).
- تغییرات `.github/workflows/*` در PENDING-workflow-changes ثبت شد.

*تولیدشده توسط Arena Agent — هماهنگ‌سازی تعهد لگاسی و اصلاح ترتیب گزارش تنخواه.*
