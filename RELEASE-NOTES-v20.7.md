# 🔬 ریلیز v20.7 — BUG-034 تعدیل امتیاز + اصلاح کامل ماهیت نوت بازرسی

## ✅ BUG-034 — تعدیل امتیاز مشتری ۱۵ به ۵۱ تبدیل می‌شد

### ریشه
فیلد «تعدیل دستی امتیاز» از نوع عددی/مبلغی عمومی بود و تحت تأثیر formatter عددی/کامادار قرار می‌گرفت. در برخی وضعیت‌های موبایل/جهت‌نوشتار، ورود `15` می‌توانست به صورت معکوس یا اشتباه خوانده شود.

### رفع
- فیلد تعدیل از `number` به `text` ساده تبدیل شد.
- مقدار با parser اختصاصی و مستقل از money formatter خوانده می‌شود.
- اعداد فارسی/عربی نیز پشتیبانی می‌شوند.
- سقف ±۱۵ حفظ شد.

## ✅ اصلاح نوت بازرسی

### اصل اصلاح
Inspection Note گزارش نتیجه بازرسی نیست. نوت بازرسی باید **اعلان انگلیسی به کارفرما برای حضور بازرس** باشد و شامل زمان، مکان، شخص هماهنگ‌کننده و اقلام قابل بازرسی باشد.

### اصلاحات در `docsx.js`
- عنوان انگلیسی از `INSPECTION NOTE` به `INSPECTION NOTICE` تغییر کرد.
- فیلدهای نتیجه/قبول/رد از نوت بازرسی حذف شدند.
- فیلدهای جدید:
  - Inspection Date
  - Inspection Time
  - Inspection Location / Address
  - Coordination Contact Name
  - Coordination Contact Tel.
  - Client Inspector / Company
  - Remarks
- جدول اقلام نوت بازرسی از اقلام CO/درخواست برنده پر می‌شود و کاربر می‌تواند کل یا بخشی از اقلام را نگه دارد.

### اصلاح QC در پرونده فروش
- «نوت بازرسی» از لیست QC حذف شد.
- QC فقط برای موارد زیر باقی ماند:
  - نتیجه آزمایش
  - پیوست گزارش بازرسی صادره توسط بازرس
  - MTC / TPI / Release Note
- متن راهنما تصریح می‌کند که نوت رسمی از بخش اسناد قالب شرکت صادر می‌شود و گزارش بازرسی فقط در صورت وجود پیوست می‌شود.

## فایل‌های تغییر یافته
- `crm/scoring.js`
- `crm/docsx.js`
- `crm/salesfiles.js`
- `crm/index.html`
- `crm/sw.js`
- `_tools/uat/tester123-v207.js`
- اصلاح تسترهای قبلی:
  - `tester118-v202.js`
  - `tester122-v206.js`
- `RELEASE-NOTES-v20.7.md`
- `PTF-MASTER-HANDOVER.md`

## 🔬 کیفیت
- تستر جدید: `tester123-v207.js` با ۱۵ چک.
- رگرسیون کامل: **۱۱۰ تستر / صفر خطا**.
- Audit: PASS با هشدار قدیمی تصاویر سنگین.
