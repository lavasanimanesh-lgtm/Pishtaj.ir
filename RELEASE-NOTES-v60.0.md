# 📦 Release Notes — v60.0
**اسپرینت ۶۰: آیکون‌های حرفه‌ای (US-125) + منشی هوشمند فاز ۲ (US-107)**
**تاریخ:** ۱۳ تیر ۱۴۰۵ (2026-07-03)

---

## ✅ US-125 — جایگزینی ایموجی‌ها با آیکون‌های SVG حرفه‌ای

**مشکل:** ~۱۰۰ ایموجی (📋🏭💯🔥👔…) در صفحات اصلی — ناهماهنگ با کلاس یک تامین‌کننده صنعتی معتبر.

**اجرا:**
- **ست ۴۰+ آیکون خطی (outline) SVG اینلاین** طراحی شد: تک‌رنگ (از رنگ والد)، ضخامت خط یکنواخت 1.8، بدون CDN، هم‌تراز با متن (`1em`).
- جایگزینی در **۳۴ صفحه کلیدی**: صفحه اصلی (hero، why-ptf، تماس، فوتر)، services (همه)، industries (همه)، rfq، news، quality، logistics، catalog، tracking، supplier، projects، assistant، certificates.
- نگاشت معنایی: 💯→تیک دایره‌ای، ⚡→صاعقه خطی، 🛡️→سپر، 🏭→کارخانه، ☎️→تلفن خطی، 🚚→کامیون، ⚙→چرخ‌دنده، 🏆→جام، 🔴🟡🟢🔵 (راهنمای ولو)→نقطه‌های رنگی مینیمال و…
- ویجت چت: دکمه شناور 💬 → آیکون گفتگوی SVG؛ ایموجی از دکمه‌ها و لینک‌های پاسخ حذف شد.
- ✔ تایید نهایی: **صفر ایموجی** در صفحات کلیدی (خارج از SVG).
- طبق AC4: CRM (ابزار داخلی) مستثنا — بازبینی در PWA اسپرینت ۶۷. ایموجی داخل متن مقالات KC/blog طبق AC2 فاز بعد.

## ✅ US-107 فاز ۲ — ارتقای منشی هوشمند (مسیر بدون کلید API)

چون کلید LLM هنوز دریافت نشده، طبق تصمیم قبلی: ارتقای موتور محلی + **آماده‌سازی کامل زیرساخت LLM**:

### زیرساخت LLM (آماده — یک سوییچ تا فعال‌سازی):
- `api/chat-llm.php`: پروکسی امن سمت سرور — کلید API فقط در `llm-config.php` **خارج از webroot**؛ سازگار OpenAI-format (قابل استفاده با OpenAI/سرویس‌های سازگار/لیارا AI)؛ rate-limit ۲۰ درخواست/ساعت/IP؛ پرسونای فارسی تزریق‌شده (هرگز قیمت عددی نگوید، ارجاع به RFQ).
- کلاینت: `askLLM()` با timeout و **fallback خودکار به موتور محلی** — اگر LLM در دسترس نباشد کاربر متوجه هیچ خطایی نمی‌شود.
- فعال‌سازی در آینده: ساخت فایل کانفیگ + `LLM.enabled = true` — چند دقیقه کار.

### ارتقای موتور محلی:
- **درک سوالات چندموضوعی:** «هم بال ولو می‌خوام هم ترانسمیتر» → معرفی هر دو خدمت + پیشنهاد استعلام چندقلمی (قبلاً فقط اولین تطبیق).
- **۶ سناریوی پرتکرار جدید:** زمان تحویل/موجودی، شرایط پرداخت، گارانتی، اصالت کالا (MTC/TPI)، واردات و ترخیص، لوکیشن دقیق‌تر.
- باگ‌فیکس مسیریابی: «از کجا بدونم جنس اورجیناله» به FAQ آدرس می‌رفت (کلیدواژه «کجا») → کلیدواژه آدرس دقیق شد.

## 🧪 تست‌ها — ۱۱ سناریو همه PASS
چندموضوعی ✔ تحویل ✔ پرداخت ✔ گارانتی ✔ اصالت ✔ واردات ✔ + ۵ تست رگرسیون (تک‌خدمت، سلام، آدرس، دفتر، بال ولو) ✔
سینتکس JS ✔ | ممیز کل سایت (۳۲۰ صفحه): PASS ✔

## 🚀 دیپلوی
- تغییر: ۳۴ صفحه HTML + `assets/js/ptf-chat.js`
- جدید: `api/chat-llm.php`
- 📌 پس از دیپلوی، فایل `llm-config.php` را **نسازید** تا وقتی کلید API تهیه شد.

## ⏭️ اسپرینت بعدی (۶۱)
US-108 — مشاور انتخاب متریال (فشرده): درخت تصمیم پایپ/فلنج/ولو/برق/ابزار دقیق/پمپ + جداول مرجع + ادغام با چت و RFQ.
