# 📊 ریلیز v14.8 — اسپرینت «تحلیل مدیریتی»

**تاریخ:** ۱۴۰۵/۰۴/۱۷ | **اسپرینت:** نقشه راه مصوب (پس از هـ)
**استوری‌ها:** US-349 (دلایل استاندارد باخت + گزارش Win/Loss) + US-350 (قیف تبدیل داشبورد) + US-351 (تاریخ تحویل تعهدی)

---

## ✅ محتوای ریلیز (۳ استوری)

### US-349 — 🚫🏆 دلایل استاندارد باخت + گزارش مدیریتی Win/Loss
تا امروز دلیل باخت یک متن آزاد بود که قابل تحلیل آماری نبود. حالا:
- **هنگام «مختومه بدون فاکتور»** به‌جای تایپ آزاد، پنجره انتخاب دلیل استاندارد باز می‌شود: **قیمت / زمان تحویل / برند / انصراف مشتری / سایر** + توضیح تکمیلی اختیاری.
- دلیل به‌صورت **کد استاندارد** روی رکورد بایگانی می‌نشیند (`closeReason`) — پایه تحلیل‌های آینده.
- **گزارش جدید «🏆 برد/باخت» در ماژول گزارشات** (ادمین + رییس هیات مدیره):

| شاخص | نمایش |
|---|---|
| تعداد پرونده‌های برنده/باخته | کارت‌های رنگی |
| نرخ برد (Win Rate) | درصد |
| ارزش بردها / ارزش ازدست‌رفته | تومان (از ارزش CO پرونده‌ها) |
| تفکیک دلایل باخت | جدول + نمودار میله‌ای + سهم درصدی + نمونه پرونده‌ها |

- 🧠 **سازگاری عقب‌رو:** پرونده‌های مختومه‌شده قبلی که متن آزاد دارند، از روی متنشان دسته‌بندی می‌شوند (مثلا «قیمت رقیب پایین‌تر بود» → دسته قیمت)؛ بی‌متن‌ها «نامشخص (قدیمی)».

### US-350 — 📊 قیف تبدیل فروش روی داشبورد
ویجت جدید بالای داشبورد (زیر کاشی‌های دسترسی سریع):
- چهار مرحله: **🎯 سرنخ → 📋 درخواست → 📄 پیشنهاد → 🏆 برنده** با میله‌های رنگی و عدد فارسی.
- **نرخ تبدیل هر مرحله نسبت به مرحله قبل** + چیپ «نرخ تبدیل کل سرنخ→برنده».
- شمارش هوشمند: «پیشنهاد» = درخواست‌های متمایز دارای حداقل یک پیشنهاد (رویژن‌ها و چند سند یک درخواست دوباره شمرده نمی‌شوند)؛ «برنده» = پیشنهاد مالی برنده **یا** پرونده تسویه‌شده در بایگانی.
- ۱۰۰٪ از داده‌های موجود — هیچ ورود اطلاعات جدیدی لازم نیست. تم شب/روز سازگار.

### US-351 — 🚚 تاریخ تحویل تعهدی ساختاریافته
- دکمه جدید **«🚚 ثبت تاریخ تحویل تعهدی»** در کشوی هر پرونده فروش: تاریخ + یادداشت تعهد (مثلا «بند ۴ قرارداد») + امکان اصلاح/حذف.
- **رنگ‌بندی فهرست پرونده‌ها:** قرمز = سررسید امروز یا تاخیر | نارنجی = تا ۳ روز آینده.
- **یادآور و هشدار خودکار (بدون کلیک کسی):**
  - ۳ روز قبل → یادآور به تیم فروش،
  - روز سررسید → هشدار قرمز،
  - **گذشته از تعهد → 🚨 اعلان تاخیر مستقیم به مدیران (رییس هیات مدیره/مدیرعامل/بازرگانی/ادمین)**.
- ضدتکرار روزانه؛ تغییر تاریخ = ریست یادآور؛ همه تغییرات در audit ثبت می‌شود.

---

## 🔬 وضعیت کیفیت

| مورد | وضعیت |
|-------|--------|
| کد ۳ استوری | ✅ کامل (ماژول جدید `insights.js` + salesfiles.js + bridge.js) |
| tester68-v148 | ✅ نوشته شد — ۴۵+ چک شامل تست اجرایی مختومه با دلیل استاندارد، قیف با داده واقعی، Win/Loss با رکورد قدیمی و ۶ حالت سررسید |
| نسخه/کش | ✅ VER=v14.8، کش `ptf-crm-v14.8`، insights.js در SHELL و index |
| اصلاح جانبی | tester67 چک نسخه دقیق 14.7 را قفل کرده بود → طبق قاعده به الگوی «>=14.7» آزاد شد |
| اجرای تسترها + audit + zip | ⚠️ ترمینال سندباکس همچنان قطع (این نوبت هم تست شد) — معوقات چهار اسپرینت در هندآور ثبت است |

### 📌 وضعیت تحویل
- روی هاست: v14.4 بماند. به محض بازگشت ترمینال: رگرسیون **۵۵ تستر** → audit → بسته تجمیعی **`pishtaj-release-v14.8.zip`** (شامل v14.5+v14.6+v14.7+v14.8).
- ⚠️ بسته تجمیعی شامل تغییرات `api/crm.php` (از v14.7) و `api/llm.php` (از v14.4) است — هنگام آپلود، **اکسترکت روی موجود؛ حذف پوشه api ممنوع** (فایل `api/ptf_crm_data.json` بانک داده است).

## 🔜 بعدی طبق برنامه مصوب
**اسپرینت «و» (UX)** = US-361 (تور هماهنگ با منوی آکاردئونی) + US-375 (تور نوار پایین موبایل) + US-372 (پنجره‌های گفتگوی هم‌تم به‌جای alert/confirm) + US-369 (نمای کانبان + فیلتر صنعت در مشتریان).
