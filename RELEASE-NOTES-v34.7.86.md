# RELEASE NOTES — v34.7.86 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## فعال‌سازی صفحه‌بندی get_inbox در کلاینت (SUP-PERF-005)

### زمینه

واسط سروری `get_inbox` از v34.7.85 پارامترهای `limit/offset` را پشتیبانی می‌کرد، ولی
کلاینت هنوز هم همه‌ی ثبت‌نام‌های سایت را در هر پول/بوت می‌گرفت. در این نسخه کلاینت
به مسیر گام‌به‌گام متصل شد.

### تغییرات

- `crm/bridge.js`:
  - `SITE_SUP_PAGE = 50`.
  - `syncServerInbox` صفحهٔ اول (`limit=50&offset=0`) را می‌گیرد و با `siteSupMerge`
    با کش محلی قفل/ادغام می‌کند؛ `ptf_site_suppliers_total` از پاسخ سرور ذخیره می‌شود.
  - `syncServerInboxMore` صفحات بعدی را با `offset=دقیق count لودشده` می‌گیرد.
  - `renderSupPending` تعداد `total` سروری را در هدر و دکمهٔ «نمایش بیشتر» نشان می‌دهد.
  - `supPendingMore` اگر هنوز رکوردی از سرور لود نشده، صفحهٔ بعدی را دریافت و ادغام می‌کند.
- `api/crm.php → get_inbox`:
  - `fresh` فقط وقتی مجاز است که صفحه‌بندی درخواست نشده (`limit=0 && offset=0`)؛
    به‌این‌ترتیب درخواست صفحهٔ بعدی با همان `since` به‌درستی دادهٔ صفحه را می‌گیرد.

### تست

- `_tools/uat/tester488-v34.7.86-supplier-inbox-lazy.js` در گیت CI.
- گیت CI: **104 PASS / 0 FAIL**.

### توجه

- جدول ثبت‌نام سایت سروری است و با «نمایش بیشتر» یا «بروزرسانی از سرور» صفحات بعدی
  لود می‌شوند؛ هیچ داده‌ای هنگام باز شدن پنل پنهان نمی‌شود (فقط گام‌به‌گام).
- `siteRfqs` همچنان از همان پاسخ می‌آید (تعداد استعلام‌های سایت معمولاً کوچک است).
