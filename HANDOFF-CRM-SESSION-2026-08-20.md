# سند تحویل CRM/سایت — نشست‌های ۲۰۲۶-۰۸-۲۰ (v34.7.47 تا v34.7.54)

**تاریخ:** ۱۴۰۵/۰۵/۲۹ — 2026-08-20
**شاخه‌ها:** `arena/01a01c0a` (PR #58 — merged + production) · `arena/01a01ff8` (PR #59 — هات‌فیکس v34.7.54)
**نسخهٔ رسمی فعلی:** `v34.7.54` (`VERSION.json`)
**گیت CI:** ۷۳ PASS / ۰ FAIL ✅ (پس از v34.7.54؛ در لحظهٔ merge شدن PR #58 قرمز بود: ۷۰/۳)

> این سند جای release noteهای v34.7.47…54 را نمی‌گیرد — همهٔ آن‌ها نوشته شده‌اند.
> این سند «نقشهٔ ادامهٔ کار» برای ایجنت/نشست بعدی است.

---

## ۰) شروع سریع نشست بعدی

```bash
git fetch origin && git checkout -b arena/<id>-pishtaj-ir origin/main
node _tools/uat/run-ci-gate.js        # باید ۷۳/۰ باشد؛ اگر نبود اول همان را درست کن
node _tools/arch/arch-guard.js        # باید PASS باشد
```

1. از کاربر بپرس: PR #59 (هات‌فیکس v34.7.54) merge و روی Production دیپلوی شد؟
2. این فایل + `ARENA-CRM-STATUS-REMAINING-2026-08-17.md` (بدهی‌های قدیمی‌تر) را بخوان.
3. هرگز `crm/data/`، `ptf-secrets.php`، `ptf-db-config.php` را commit نکن.
4. کانونشن بامپ نسخه: ۷ نقطهٔ رسمی + پین تسترهای گیت (دقت: پین‌ها هم به‌صورت
   `34.7.x` ساده و هم `34\.7\.x` با نقطهٔ escape‌شده در regexها هستند — هر دو را عوض کن).

---

## ۱) آنچه در این روز تحویل شد

| نسخه | موضوع | تستر گیت |
|---|---|---|
| v34.7.47 | «بارگذاری از درخواست دیگر» + جداکنندهٔ هزارگان/حروف لایو/ارقام فارسی در مودال پیشنهاد | tester449 |
| v34.7.48 | فیلتر درخواست‌های مودال پیشنهاد بر حسب کارفرما (custCd + نام شرکت legacy) | tester450 |
| v34.7.49 | کادر فشردهٔ موجودی انبار در مودال پیشنهاد (+ دیالوگ مشاهده با جستجو) | tester451 |
| v34.7.50 | ماژول «فرصت شغلی» سایت+CRM (PDF≤5MB، کپچا+OTP، retention ۱۸۰ روز، منوی پویا) + بازآرایی منوی سایت | tester452 |
| v34.7.51 | انتشار آگهی از API، `view.php`، شرح AI (`llm.php jobdesc`)، وضعیت پیوست رزومه، مگامنوی محصولات، رفع جستجو/کپچا/فوتر | tester453, tester454 |
| v34.7.52 | فشرده‌سازی WebP/JPEG (کوچک‌ترین معتبر) + گزارش فقط‌خواندنی کلید ابری (رده‌های A/B/C/E/F + CSV) | tester455 |
| v34.7.53 | remap دستی گروه B — فقط metadata محلی، تأیید ارشد، بدون DELETE، مسدود روی truncated | tester456 |
| v34.7.54 | 🔧 هات‌فیکس: رفع تخلف A2 (`ptfHarvestFileKeys` — ترتیب `_id \|\| cd`) و سبز شدن گیت ۷۳/۰ | tester457 |
| v34.7.55 | 🔧 اسکیمای کامل JobPosting (datePosted/validThrough/employmentType/identifier/directApply + baseSalary اختیاری) — رفع خطای قرمز GSC | tester458 |
| v34.7.56 | 🐛 رفع حذف کاذب اقلام هم‌محتوا در بارگذاری از درخواست (BUG-OFFER-DUP-SKIP-267، هر ۳ لایه dedup) + «💰 قیمت از اکسل» برای نرخ مرجع/قیمت واحد | tester459 |
| v34.7.57 | 🎨 نظم بصری فرم پیشنهاد: ترتیب طبیعی فیلدها، ردیف متوازن چاپ/اعتبار، نوار ابزار یکدست، فوتر چسبان، رفع غلط تایپی | tester460 |
| v34.7.58 | 🖨 خروجی چاپ/PDF رسمی بدون واترمارک PREVIEW (فرم باز / سند ذخیره‌شده / snapshot برد) — بازپیاده‌سازی PR #57؛ آن PR اکنون قابل بستن است | tester461 |
| v34.7.59 | ☁️ ادامهٔ صفحه‌بندی فهرست S3 (nextToken سرور + حلقهٔ کلاینت تا ۳۶۰هزار کلید) — رفع بن‌بست truncated در گزارش/remap کلید ابری | tester462 |

---

## ۲) کارهای باز — به ترتیب اولویت

### الف) فوری / تصمیم کاربر

1. **merge کردن PR #59** (v34.7.54) تا `main` دوباره گیت سبز داشته باشد.
2. **PR #50 را ببندید** — منسوخ است؛ محتوای v34.7.32 از قبل در `main` است.
3. **تعیین تکلیف PR #57** — بخش «بارگذاری از درخواست دیگر» آن با v34.7.47–48 جایگزین
   شده، اما بخش «**خروجی چاپ/PDF رسمی بدون واترمارک PREVIEW**» (~۱۷۶ خط در
   `crm/offers.js` + `offers-pro.js`) در `main` نیست. توصیه: PR بسته شود و فیچر خروجی
   رسمی جداگانه روی main بازپیاده‌سازی شود (rebase پرهزینه و مستعد conflict است).
4. **H1:** وصله‌های workflow (`_tools/PENDING-workflow-*.patch` — اجرای php -l + گیت
   UAT پیش از FTP) فقط با دسترسی workflow خود کاربر قابل اعمال‌اند. دیپلوی v34.7.53
   با گیت قرمز، ضرورت آن را عملاً نشان داد.

### ب) کلید ابری (ادامهٔ v34.7.52–53)

- اجرای عملی چرخه روی Production با نقش ارشد: گزارش 📋 → بررسی CSV → remap گروه B.
  (از v34.7.59 بن‌بست «فهرست ناقص S3» رفع شده — پیش‌نیاز فنی باز ندارد.)
- **ردهٔ E** (بایت در باکت نیست): فهرست تجمیعی به کاربر بدهید تا فایل‌ها دوباره آپلود
  شوند؛ در صورت نیاز ابزار کمکی «صف آپلود مجدد» ساخته شود.
- **ردهٔ C** (اصل در zip بایگانی): مسیر استخراج/دسترسی تعریف نشده — نیازمند تصمیم.
- **RCA** ریشهٔ انحراف کلیدها هنوز نوشته نشده.

### ج) راه‌اندازی عملیاتی «فرصت شغلی»

- **پس از merge نسخهٔ v34.7.55:** آگهی موجود را در CRM دوباره «انتشار / ذخیره» کنید تا
  صفحهٔ استاتیک با اسکیمای جدید JobPosting بازتولید شود؛ سپس در GSC مجدداً Test Live
  URL و Request Indexing بزنید.
- انتشار اولین آگهی از CRM (تا آن زمان دکمهٔ منو ظاهر نمی‌شود — عمدی).
- `llm-config.php` معتبر روی سرور برای «شرح با هوش مصنوعی».
- پیکربندی SMS برای OTP متقاضی.

### د) بدهی‌های قدیمی‌تر (از سند 2026-08-17 — همچنان باز)

- **S4:** پاکسازی رکوردهای آلودهٔ نشت پیش‌پرداخت روی دادهٔ واقعی (ابزار آماده، منتظر تأیید).
- **ARCH-02:** یکپارچه‌سازی سه مسیر نوشتن پول (`post_receipt`/`savePay`/چک) — پروژه‌ای.
- **AN-04:** کالیبراسیون وزن‌های healthScore — منتظر تصویب کارفرما.

### هـ) مسیر سئو (سند جدا: `HANDOFF-SESSION-2026-08-20.md`)

- گام E ناتمام (`<picture>` فقط روی صفحهٔ اصلی؛ ~۱۹۰ استایل اینلاین در `index.html`).
- گام D (Case Study) مسدود روی ورودی واقعی کاربر.
- تصمیم cannibalization (blog/a106-vs-a333 در برابر comparisons/…) بعد از ۲–۳ هفته دادهٔ GSC.

---

## ۳) دام‌هایی که این نشست‌ها پیدا کردند (تکرار نشود)

1. **A2 نگهبان:** هر کد جدیدی که شناسه می‌سازد باید ترتیب `_id || cd` داشته باشد؛
   tester457 این را برای `crm/*.js` اسکن می‌کند ولی الگوهای غیرمستقیم را نه — قبل از
   commit همیشه `node _tools/arch/arch-guard.js` را اجرا کن.
2. **merge با گیت قرمز ممنوع** — حتی وقتی شکست «فقط lint معماری» به نظر می‌رسد؛
   سه تستر زنجیره‌ای قرمز می‌شوند و شاخه‌های بعدی آلوده می‌شوند.
3. **پین نسخهٔ تسترها دو شکل دارد** (ساده و regex با `\.`) — sed تک‌الگویی فقط نصف را
   می‌گیرد و گیت ۴ شکست کاذب نشان می‌دهد.
4. در `tester456` رشتهٔ نام فایل خودش (`tester456-v34.7.53-…`) نباید در بامپ‌ها عوض شود.
5. Release note هر نسخه را **در همان نشست** بنویس؛ بازسازی پسینی از commitها پرهزینه است.
