# Release Notes v34.7.7 — راهنمای رفع یافته‌ها و ادغام امن پرونده تکراری

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## راهنمای گام‌به‌گام کیفیت داده

- کنار هر یافته «فروش تا وصول» دکمه `🧭 راهنمای بررسی و رفع` اضافه شد.
- راهنما برای هر نوع یافته توضیح می‌دهد:
  1. چرا هشدار ایجاد شده است؛
  2. کدام داده‌ها باید بررسی شوند؛
  3. چه اقدامی نباید انجام شود؛
  4. اقدام کنترل‌شده مجاز چیست.
- برای نقش فاقد مجوز، علت محدودیت صریح نمایش داده می‌شود و دکمه مخرب پنهان و مبهم باقی نمی‌ماند.

## تشخیص پرونده‌های تکراری

- برای هشدار «چند پرونده برای یک پیشنهاد»، پیش‌بررسی فقط‌خواندنی و سرورمحور افزوده شد.
- همه رکوردهای مرتبط، از جمله پرونده بایگانی یا پنهان، با این اطلاعات کنار هم نمایش داده می‌شوند:
  - شناسه و شماره پرونده؛
  - مرحله واقعی از ۱۲ مرحله؛
  - وضعیت فعال/بایگانی؛
  - درخواست و مشتری؛
  - مدارک، رویدادهای ارسال و تاریخچه؛
  - فاکتور، دریافت، تخصیص، ضمیمه مالی، تنخواه، هزینه جاری، چک و مرجوعی مرتبط.
- این نمایش مشخص می‌کند چرا ممکن است کاربر فقط یک پرونده در فهرست ببیند ولی موتور کیفیت داده دو رکورد بشمارد.
- اگر یک رکورد کاملاً بدون شاهد باشد، فقط به‌عنوان پیشنهاد اولیه برای نگهداری رکورد دیگر علامت می‌خورد. وقتی هر دو پرونده شاهد دارند، سیستم انتخاب را خودکار انجام نمی‌دهد و مرحله بالاتر را صرفاً برای بررسی اولیه معرفی می‌کند.

## ادغام کنترل‌شده بدون حذف شواهد

- `admin` و `chairman` می‌توانند پرونده اصلی و پرونده تکراری را صریح انتخاب کنند.
- عملیات نیازمند دلیل و تایپ کلمه `ادغام` است.
- قبل از commit، `planHash` بررسی می‌شود؛ اگر داده بعد از بازشدن راهنما تغییر کرده باشد، عملیات متوقف می‌شود.
- هویت درخواست، مشتری و ارز دو پرونده باید سازگار باشد؛ هر تعارضی باعث توقف fail-closed می‌شود.
- ادغام به‌جای حذف خام:
  - آرایه‌های مدارک، رویدادها و تاریخچه را بدون دوباره‌سازی ترکیب می‌کند؛
  - فاکتورها، دریافت‌ها، تخصیص‌ها و ضمائم مالی را به پرونده نگهداری‌شده منتقل می‌کند؛
  - لینک تنخواه، هزینه جاری، چک صادره/وارده و مرجوعی فروش را نیز اصلاح می‌کند؛
  - تخصیص FIFO را پس از انتقال دوباره می‌سازد؛
  - snapshot کامل پرونده ادغام‌شده، دلیل، کاربر و تعارض فیلدها را در deleted archive و corrections نگه می‌دارد.
- در تعارض فیلدهای توصیفی، مقدار پرونده‌ای که کاربر برای نگهداری انتخاب کرده مقدم است و مسیرهای متعارض در audit ثبت می‌شوند.

## کنترل کیفیت

- `tester412-v34.7.7-guided-case-dedup.js`: 20 PASS / 0 FAIL
- `tester411-v34.7.6-winner-repair-parity.js`: 14 PASS / 0 FAIL
- `tester403-v35-sales-to-cash.js`: PASS
- CI متمرکز ریلیز: 31 PASS / 0 FAIL
- Parser مستقل PHP برای `api/sales-domain.php` و `api/crm.php`: PASS
- رگرسیون کامل: 395 تستر، 338 فایل PASS، 57 فایل بدهی تاریخی FAIL، 5853 check PASS و 129 check تاریخی FAIL؛ هیچ شکست جدیدی نسبت به v34.7.6 ایجاد نشد.
- Auditor روی 624 صفحه و هم‌ترازی VERSION/PWA با 100 مرجع cache-query: PASS
- PHP CLI در محیط محلی موجود نبود و lint داخلی CI به‌صورت صریح skip شد؛ syntax فایل‌های JS و `git diff --check` سبز هستند.
