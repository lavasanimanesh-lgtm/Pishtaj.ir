# RELEASE NOTES — v34.7.96 (۱۴۰۵/۰۵/۳۱ — 2026-08-22)

## رفع باگ خاموش VAT + گارد نقش صف آپلود مجدد + بازنویسی tester496 E2

این نسخه سه اصلاح از یافته‌های ممیزی عمیق (`SELF-AUDIT-DEEP-2026-08-22.md`) را می‌آورد.

### ۱) رفع باگ خاموش VAT در تاریخ‌های نامعتبر (VAT-LEDGER-004)

**قبل:** تابع `seasonOfInv` در `crm/vat-quarterly.js` تاریخ‌های ISO میلادی خام (مثل `2026-06-15T10:00:00Z`) را به سال جعلی (`"2026-06-15T10:00:00Z"`) و season درست (۲) map می‌کرد. فاکتور در گزارش سال ۱۴۰۵ گم می‌شد بدون هیچ warning.

**بعد:** یک validation gate اضافه شد که سال استخراج‌شده را با regex `/^(13|14)\d{2}$/` می‌سنجد. اگر معتبر نبود، `null` برمی‌گرداند و فاکتور در هیچ فصلی شمرده نمی‌شود — رفتار قابل پیش‌بینی.

**اثر:** ۱۰۰٪ فاکتورهایی که از UI ثبت می‌شوند (تاریخ شمسی) بدون تغییر کار می‌کنند. فقط رکوردهای legacy با تاریخ میلادی خام دیگر به سال جعلی نشت نمی‌کنند.

### ۲) بازنویسی tester496 E2 — از «ادعای غلط» به «تست واقعی»

**قبل:** شرط PASS بود `!st || st.year !== '1405'` که بی‌صدا PASS می‌شد چون `st.year === '2026-06-15T10:00:00Z'`. تستر «سبز» بود ولی رفتار زیرین باگ خاموش داشت.

**بعد:** ۴ سنجش صریح:
1. تاریخ ISO میلادی خام sales → `null`
2. تاریخ ISO میلادی خام purchase → `null`
3. تاریخ ISO با سال شمسی (`1405-06-15`) → `year='1405' season=2` (تأیید نشکستن سناریوی درست)
4. فاکتور با تاریخ میلادی در سال ۱۴۰۵ شمرده نمی‌شود (نه در سال جعلی)

### ۳) گارد نقش برای `ptfReuploadQueueExportCsv` (RE-UPLOAD-QUEUE-002)

**قبل:** تابع بدون گارد نقش بود. دکمهٔ UI فقط برای نقش ارشد نمایش داده می‌شد ولی از console/devtools هر کاربر با هر نقش می‌توانست لیست کلیدهای گم‌شدهٔ ابری را دانلود کند — leak اطلاعات ساختار داده داخلی.

**بعد:** بلوک استاندارد نقش (همسان `ptfCloudKeyAudit`):
```js
if (['admin', 'chairman', 'ceo'].indexOf(role) < 0) {
  ptfToast('⛔ این گزارش فقط برای نقش‌های ارشد است', 'err');
  return;
}
```

### ۴) بامپ نسخه — رعایت convention تیم

- **پین‌های ۷ نقطهٔ رسمی** بامپ شدند: `VERSION.json`, `crm/index.html` (`PTF_CRM_RELEASE`, `?v=` × ۱۰۷), `crm/sw.js` (RELEASE/CACHE/ASSET_VERSION), `crm/manifest.json` (version), `crm/clear-cache.html` (VER + عنوان), `crm/shell.js` (fallback), `api/sales-domain.php` (SD_SERVICE_VERSION).
- **پین تسترها و کامنت‌های توصیفی تسترها** بامپ شدند (۴۸ فایل) — convention تیم.
- **کامنت‌های تاریخی معرفی فیچر** در `crm/index.html` (`v34.7.93 SUP-UX-002`, `AUTH-TOKEN-REQUIRED`, `SUP-PERF-001`) و در `crm/storage.js` (`v34.7.95 RE-UPLOAD-QUEUE-001`) **دست‌نخورده** ماندند — تاریخچه فیچر حفظ شد.
- اسم فیزیکی فایل تستر (`tester497-v34.7.95-...`) طبق convention بدون تغییر ماند.

### تست

- گیت CI: **۱۱۳ PASS / ۰ FAIL** ✅
- نگهبان معماری: **PASS** ✅
- tester496: از ۲۵ به **۲۸ سنجش** (E2 حالا ۴ سنجش صریح)
- tester497: از ۱۸ به **۲۰ سنجش** (۲ سنجش گارد نقش)

### یافته‌های باقی‌مانده از SELF-AUDIT-DEEP (نرفع، مستند برای نشست بعد)

- **یافتهٔ B:** `ptfVatRateOf` با `vatDefaultPct='abc'` مقدار NaN برمی‌گرداند. protection type نیاز است (3 خط).
- **یافتهٔ C:** `ptfReuploadQueueBuild` روی string/object crash می‌کند. defensive `Array.isArray` نیاز است (2 خط).
- این دو در مسیر عادی UI اتفاق نمی‌افتند ولی برای robustness توصیه می‌شود.

### فایل‌ها

- تغییر: `crm/vat-quarterly.js` (+6), `crm/storage.js` (+9), `_tools/uat/tester496-*.js` (بازنویسی E2), `_tools/uat/tester497-*.js` (+گارد نقش)
- بامپ نسخه: `VERSION.json`, ۷ نقطهٔ رسمی، ۴۸ تستر
- افزوده: `RELEASE-NOTES-v34.7.96.md`
