# چک‌لیست استیجینگ v34.7.43 — قطعیت عمومی فرمان‌ها

## پیش‌نیاز

1. آدرس `https://staging.pishtaj.ir/crm/` باز شود.
2. نسخه داشبورد `v34.7.43` باشد.
3. یک بار Hard Refresh یا بستن/بازکردن PWA انجام شود.
4. از داده آزمایشی و نقش مجاز هر مسیر استفاده شود.

## ۱) عدم رگرسیون ثبت پیشنهاد

- یک پیشنهاد جدید برای RFQ وضعیت ۵۰ ثبت کنید.
- فقط پیام موفقیت دیده شود؛ پیشنهاد و WF50 پس از بازخوانی باقی بمانند.
- ثبت تکراری یا پیام «سرور نپذیرفت» رخ ندهد.

## ۲) ثبت برد و تشکیل پرونده

- پیشنهاد آزمایشی را برنده کنید.
- offer و پرونده دقیقاً یک بار ساخته/متصل شوند.
- پس از refresh همان پرونده دیده شود.
- render یا بازشدن پنل نباید پیام «برد ثبت نشد» تولید کند.

## ۳) رویژن پیشنهاد برنده

- فرم رویژن را باز و مبلغ/قلم را تغییر دهید.
- رویژن یک بار ثبت شود و فرم فقط پس از ACK بسته شود.
- با دو کلیک سریع، رویژن دوم ساخته نشود.

## ۴) دریافت و فاکتور

- یک دریافت نقد/حواله برای پرونده ثبت کنید.
- سپس یک فاکتور رسمی آزمایشی با فایل معتبر ثبت کنید.
- مانده، دریافت و فاکتور بعد از refresh یکسان بمانند.
- خطای نمایش پس از ثبت نباید پیام رد یا rollback ایجاد کند.

## ۵) پیوست RFQ و مالی

### افزودن/جایگزینی

- فایل تازه آپلود و متصل شود.
- در جایگزینی، نسخه جدید فعال و نسخه قبلی فقط بعد از ACK پاک شود.

### حذف

- حذف پیوست را تأیید کنید.
- ابتدا رکورد/Tombstone سرور ثبت و سپس فایل ابری حذف شود.
- اگر cleanup ابری شکست خورد، فقط هشدار cleanup نمایش داده شود؛ ثبت قطعی برنگردد.

## ۶) شبیه‌سازی قطع ارتباط

با DevTools مرورگر، درست پس از زدن دکمه ثبت Network را موقتاً Offline و سپس Online کنید:

- کلاینت همان operation ID را یک بار خودکار replay کند.
- اگر replay ACK گرفت، فقط موفقیت دیده شود و رکورد duplicate نشود.
- اگر هر دو پاسخ از دست رفتند، پیام «نتیجه هنوز نامشخص است» دیده شود.
- در وضعیت نامشخص، رکورد optimistic یا فایل تازه نباید پاک شود.
- تلاش مجدد از همان فرم باید همان intent/operation ID را بازیابی کند.

## ۷) دو دستگاه

- یک فرمان را روی دستگاه A اجرا کنید و هم‌زمان دستگاه B را sync کنید.
- B باید projection قطعی را ببیند.
- A پس از replay نباید رکورد دوم یا rollback projection دستگاه B بسازد.

## معیار قبولی

- هیچ ACK قطعی به‌عنوان rejection نمایش داده نشود.
- هیچ rollback یا حذف فایل بر اثر exception پس از ACK انجام نشود.
- هر operation حداکثر یک اثر تجاری داشته باشد.
- 4xx قطعی پیام رد واقعی بدهد.
- نتیجه مبهم با پیام نامشخص و operation ID قابل پیگیری باقی بماند.
