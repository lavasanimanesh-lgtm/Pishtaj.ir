# ارزیابی دقیق سه مورد «پرونده‌های فروش» — ۱۴۰۵/۰۵/۲۶ (2026-08-17)

پایه: کامیت `7da1a342` (v34.7.25) — شاخهٔ `arena/01a0077c-pishtaj-ir`.
روش: ردیابی ایستا از خود دکمه تا تابع مقصد و تا نوشتن/خواندن داده. هیچ کدی هنوز تغییر نکرده است؛ این سند فقط **ارزیابی + راهکار اجرایی پیشنهادی** است.

---

## ۰) خلاصهٔ اجرایی

| # | مورد | تشخیص | شدت | ریشه |
|---|------|--------|-----|------|
| ۱ | «🗑 ابطال کنترل‌شده» بی‌اثر است | **تأیید شد — باگ قطعی و بازتولیدپذیر** | بحرانی | ناسازگاری کلید شناسه: دراور `cd \|\| _id` می‌فرستد، حل‌کننده `_id \|\| cd` می‌خواند ⇒ `findInvoice` تهی ⇒ `return` بی‌صدا |
| ۲ | شلوغی/تکرار دکمه‌ها | **تأیید شد — ۳ اکشن دقیقاً دوبار و ۱ اکشن سه‌بار رندر می‌شود** | متوسط (UX + ریسک خطای کاربر) | سه مالک مستقل روی یک دراور می‌نویسند: `salesfiles.js` (۱۳ کاشی) + تزریق `buycompare.js` + تزریق `docsx.js` |
| ۳ | پیش‌پرداخت پروندهٔ مشتری دیگر در حساب یک مشتری | **تأیید شد — ۴ مسیر نشت واقعی در کد؛ کدام‌یک در دادهٔ شما فعال شده باید با اسکریپت تشخیصی قطعی شود** | بحرانی (صحت مالی) | خانوادهٔ «تطبیق با کلید تهی/نام»: `'' === ''` در تطبیق شناسه، کلید `''` در نقشهٔ پرونده‌ها، و تطبیق مبتنی بر نام شرکت |

نکتهٔ مشترک هر سه: **هیچ‌کدام نیازمند بازنویسی معماری نیست.** هر سه با اصلاح‌های جراحی و بدون تغییر طراحی رفع می‌شوند.

---

## ۱) باگ ۱ — «🗑 ابطال کنترل‌شده» هیچ کاری نمی‌کند

### ۱-۱) زنجیرهٔ واقعی

1. `crm/salesfiles.js:987` (شاخهٔ فاکتور **رسمی** در فهرست اسناد دراور):
   ```js
   ptfInvoiceVoid('<%= ptfOnClickArg(i.cd || i._id) %>')   // ← cd اول
   ```
2. تابع مقصد نهایی = `crm/official-invoice-v2.js:53` (نسخهٔ سرور-محور). این نسخه در `crm/index.html` بعد از `rbac.js` بارگذاری می‌شود (خط ۴۱۰۲ در برابر ۴۰۲۰) و تعریف قدیمی `crm/rbac.js:1120` را **بازنویسی می‌کند**:
   ```js
   window.ptfInvoiceVoid=function(id){var i=findInvoice(id);if(!i||!active(i))return; ...}
   ```
3. `crm/official-invoice-v2.js:19,25`:
   ```js
   function iid(i){return String((i&&(i._id||i.cd))||'');}          // ← _id اول
   function findInvoice(id){return data('ptf_crm_invoices').filter(function(i){return iid(i)===String(id);})[0]||null;}
   ```

### ۱-۲) ریشه (قطعی)

هر فاکتور رسمی که از مسیر سرور ثبت/مهاجرت شده باشد **هم `_id` دارد و هم `cd`** (`api/sales-domain.php:1108` ⇒ `$i['_id']=sd_uuid('INV')`). برای چنین فاکتوری:

- دراور مقدار `cd` را می‌فرستد،
- `findInvoice` فقط `_id` را می‌سنجد ⇒ `null`،
- خط اول تابع `if(!i||!active(i))return;` اجرا می‌شود ⇒ **خروج کاملاً بی‌صدا**: نه `alert`، نه `toast`، نه `console`. از دید کاربر «دکمه خراب است».

شاهد قرینه: پنل «فاکتور رسمی» (`renderInvoices` در همان فایل) همین دکمه را با `arg(iid(i))` صدا می‌زند و **درست کار می‌کند**. یعنی باگ فقط از مسیر «پرونده‌های فروش» بروز می‌کند — دقیقاً همان چیزی که گزارش کردید.

سرور مشکلی ندارد: `api/sales-domain.php:1067` (`void_invoice`) هر دو کلید `_id` و `cd` را می‌پذیرد. نقص فقط سمت کلاینت است.

### ۱-۳) دامنهٔ آسیب — دو نقص هم‌ریشهٔ دیگر در همان خط ۹۸۷

| دکمه | آرگومان فعلی | نتیجه واقعی | شدت |
|------|--------------|--------------|-----|
| 🗑 ابطال کنترل‌شده | `i.cd \|\| i._id` | خروج بی‌صدا (هیچ اتفاقی) | بحرانی |
| ✏️ اصلاح سندی | `showInvModal(i.offerNo, i.cd \|\| i._id)` | `inv=editId?findInvoice(editId):null` ⇒ `null` ⇒ مودال به‌جای «ویرایش»، در حالت **«ثبت فاکتور جدید»** باز می‌شود | **بحرانی‌تر از باگ ۱** (ریسک ایجاد سند تکراری به‌جای اصلاح) |
| مسیر رسمی از `rbac.js:720` | `ptfInvoiceVoid(inv.cd)` | همان خروج بی‌صدا برای فاکتورهای دارای `_id` | بحرانی |

### ۱-۴) راهکار اجرایی (جراحی، بدون تغییر طراحی)

- **F1-1 (تحمل دوطرفه در حل‌کننده):** در `crm/official-invoice-v2.js` تابع `findInvoice` هر دو کلید را بپذیرد:
  `return list.filter(i => String(i._id||'')===S || String(i.cd||'')===S)[0]` (اول `_id`، سپس `cd`؛ بدون تطبیق تهی — اگر `S===''` صراحتاً `null`).
- **F1-2 (فرستنده‌ها هم‌راستا):** در `crm/salesfiles.js:987` و `crm/rbac.js:720` آرگومان به `i._id || i.cd` تغییر کند (همان قرارداد `iid`).
- **F1-3 (پایان شکست خاموش):** در `ptfInvoiceVoid`/`ptfOfficialInvoiceFilesUi`/`ptfOfficialAttachmentReplace` به‌جای `return` خالی، پیام دقیق داده شود: «فاکتور یافت نشد (شناسه …)» یا «این فاکتور قبلاً ابطال/جایگزین شده است (وضعیت …)».
- **F1-4:** در `showInvModal`، اگر `editId` داده شده ولی فاکتور پیدا نشد ⇒ **باز نشدن مودال در حالت ثبت جدید**؛ به‌جای آن پیام خطا.
- **F1-5 (رفع تعارض نام):** تعریف مردهٔ `crm/rbac.js:1120` (`ptfInvoiceVoid` محلی) که همیشه بازنویسی می‌شود، به‌صورت مستند حذف یا به `ptfInvoiceVoidLegacy` تغییر نام یابد تا خواننده گمراه نشود. (بدون اثر رفتاری، چون هم‌اکنون هم هرگز اجرا نمی‌شود.)
- **تستر پذیرش:** فاکتور با `_id≠cd` ⇒ فراخوانی با `cd`، با `_id` و با شناسهٔ ناموجود ⇒ به‌ترتیب: موفق، موفق، پیام خطا (نه سکوت).

---

## ۲) دکمه‌های تکراری در دراور پروندهٔ فروش

### ۲-۱) وضعیت فعلی — سه مالک مستقل روی یک دراور

| مالک | محل | چه چیزی رندر می‌کند |
|------|-----|----------------------|
| `crm/salesfiles.js:1119-1195` | نوار «عملیات پس از برد» | **۱۳ کاشی**: خرید واقعی، تحویل تعهدی، کنترل کیفیت، نوت بازرسی، پکینگ‌لیست رسمی، ثبت رویداد پکینگ، بارنامه/ارسال، تحویل کارفرما، ارجاع فاکتور، دریافت و حساب پرونده، صورتحساب غیررسمی، ثبت زیان |
| `crm/buycompare.js:937+` (hook روی `renderDeals`) | بلوک تزریقی `#rbBox_<cd>` | ۳ دکمه: **خرید**، استعلام مجدد، **هزینه پرونده** |
| `crm/docsx.js:379+` (hook روی `renderDeals`) | بلوک تزریقی `#dxBox_<cd>` | یک دکمهٔ «ایجاد» برای **هر ۴ نوع سند**: 🧰 پکینگ لیست، 🔬 **نوت بازرسی**، 📥 اینباند، 📝 صورتجلسه + دکمه‌های نمایش/اصلاح هر سند موجود |
| `crm/salesfiles.js:360-372` | راهنمای گام بعد | ۱ دکمهٔ متغیر برحسب مرحله (مثلاً «🧰 ثبت پکینگ‌لیست»، «🛍 ثبت خرید واقعی») |

### ۲-۲) تکرارهای دقیق (شمارش واقعی روی یک پروندهٔ برنده در مرحلهٔ ۴)

| اکشن | محل‌ها | تعداد رندر |
|------|--------|-------------|
| خرید واقعی (`ptfRealBuyOpen`) | کاشی `real-buy` + `rbBox/open` + راهنمای مرحلهٔ ۲ | **۳** |
| پکینگ‌لیست (`ptfDocxOpen(cd,'PL')`) | کاشی `packing-list` + `dxBox/new-pl` + راهنمای مرحلهٔ ۴ | **۳** |
| نوت بازرسی (`ptfDocxOpen(cd,'IN')`) | کاشی `inspection-note` + `dxBox/new-in` | **۲** |
| هزینهٔ پرونده (`ptfProjectCostOpen`) | `rbBox/cost` + بخش هزینه‌های پرونده در دراور | **۲** |

توضیح مهم: «ثبت رویداد پکینگ» (`sfShipOpen(...,'packing')`) با «پکینگ‌لیست رسمی» (`ptfDocxOpen(...,'PL')`) **دو کار متفاوت‌اند** (شاهد گردش‌کار در برابر صدور سند رسمی) و نباید ادغام شوند؛ اما برچسب‌های فعلی این تفاوت را به کاربر منتقل نمی‌کنند و همین بخشی از حس «شلوغی» است.

### ۲-۳) راهکار پیشنهادی (اصل: یک اکشن = یک محل)

قاعدهٔ پیشنهادی برای تعیین «محل واحد»:

- **اسناد رسمی قالب شرکت (PL / IN / IB / MOM) ⇒ فقط بلوک `dxBox` (اسناد رسمی).** دو کاشی `inspection-note` و `packing-list` از نوار عملیات حذف می‌شوند (بلوک تخصصی هم ایجاد و هم فهرست/نمایش/اصلاح اسناد را دارد؛ کاشی‌ها فقط ایجاد دارند ⇒ زیرمجموعه‌اند).
- **خرید واقعی و هزینهٔ پرونده ⇒ فقط بلوک `rbBox` (خرید واقعی اقلام)** که وضعیت اقلام و مبالغ را هم نشان می‌دهد. کاشی `real-buy` از نوار عملیات حذف می‌شود.
- **راهنمای گام بعد دست‌نخورده می‌ماند** (تک‌دکمه، وابسته به مرحله، نقش «هدایت» دارد نه «فهرست عملیات») — مگر اینکه شما خلافش را بخواهید.
- برچسب‌ها شفاف می‌شوند: «📦 ثبت رویداد پکینگ (شاهد گردش‌کار)» در برابر «🧰 پکینگ‌لیست رسمی (سند)».
- نتیجه: نوار عملیات از ۱۳ به **۱۰ کاشی** می‌رسد و هیچ قابلیتی حذف نمی‌شود؛ فقط تکرار حذف می‌شود.

گزینهٔ جایگزین (اگر ترجیح شما «همه‌چیز در نوار عملیات» است): برعکسش می‌کنیم — بلوک‌های تزریقی به حالت «فقط نمایش وضعیت + فهرست اسناد» تبدیل می‌شوند و دکمه‌های ایجادشان حذف می‌شود. این گزینه پرریسک‌تر است چون بلوک `dxBox` نمایش/اصلاح هر سند را هم دارد.

> این تنها موردی است که **تصمیم شماست، نه باگ**. بقیهٔ کارها را می‌توان بدون تصمیم شما انجام داد.

---

## ۳) باگ ۲ — پیش‌پرداخت پروندهٔ یک مشتری در حساب مشتری دیگر

«پیش‌پرداخت» در این سامانه سه شکل دارد و هر سه می‌توانند در «حساب مشتری» دیده شوند:
1. ردیف `fromAdvance` روی خود فاکتور (`rbac.js:879`, `unofficial-invoice.js:697/1815/1875`) — منشأ: `offer.advance`.
2. رسید قطعی پرونده (`ptf_crm_case_receipts`) و بستانکاری باقیمانده‌اش (`creditRemainIRR`).
3. رسید مهاجرت‌شده از `offer.advance.payments` (فرمان `migration_apply_safe`).

**چهار مسیر نشت واقعی پیدا شد.** هر چهار مسیر از یک الگو می‌آیند: «تطبیق با مقدار تهی یا با نام».

### مسیر A — تطبیق `'' === ''` در انتساب فاکتور به پرونده (سمت سرور) 🔴

`api/sales-domain.php:1108`:
```php
if(empty($i['caseId'])){ foreach($cases as $c) if( (string)($c['wonOffer']??'')===(string)($i['offerNo']??'')
                                                || (string)($c['offerNo']??'')===(string)($i['offerNo']??'') ){
    $i['caseId']=$c['_id']; $i['customerId']=$c['buyerCd']??''; ... break; } }
```
اگر فاکتوری `offerNo` نداشته باشد (`''`) و پرونده‌ای `wonOffer` نداشته باشد (`''`)، شرط **صادق** می‌شود و آن فاکتور — همراه با ردیف پیش‌پرداخت `fromAdvance`ش — به `caseId` و **`customerId` یک مشتری کاملاً بی‌ربط** چسبانده می‌شود. این دقیقاً سناریوی گزارش‌شدهٔ شماست و چون `customerId` روی رکورد **نوشته می‌شود**، اثرش پایدار است (نه فقط نمایشی).
قرینهٔ درست در همین فایل: `sd_case_offer_linked()` صراحتاً `if($no===''||$caseNo==='')return false;` دارد — یعنی جای دیگری این گارد هست و فقط اینجا جا افتاده.

### مسیر B — کلید `''` در نقشهٔ پرونده‌های مشتری (سمت کلاینت) 🔴

چهار نقطه با یک الگو:
```js
cases[String(d._id || d.cd || '')] = true;                 // customer-finance.js:84 و :375
myCases[idOf(c)] = true;                                    // ar-reconcile.js:226 و :242
... if (r.customerId !== cd && !cases[String(r.caseId || '')]) return;   // شرط پذیرش
```
اگر فقط **یک** پروندهٔ آن مشتری بدون `_id` و بدون `cd` باشد، کلید `''` در نقشه `true` می‌شود؛ از آن لحظه **هر رسیدی که `caseId` خالی دارد** (رسیدهای قدیمی/مهاجرت‌شده/دستی) در دفتر و بستانکاری آن مشتری شمرده می‌شود — حتی اگر متعلق به مشتری دیگری باشد. اثر: هم ردیف در «دفتر حساب مشتری» (`cfLedgerRows`) و هم عدد در «اعتبار مشتری» (`creditForCustomer`, `PTF.ar.customerPosition`).

### مسیر C — تطبیق مبتنی بر **نام شرکت** در فهرست فاکتورهای مشتری 🟠

`crm/customer-finance.js:20-30`:
```js
var invoiceCustomerName = normalizeName(i.buyerCo || o.buyerCo);
return (i.customerId || i.buyerCd || o.buyerCd) === cd || (invoiceCustomerName && customerNames.indexOf(invoiceCustomerName) > -1);
```
اگر دو رکورد مشتری هم‌نام باشند (رکورد تکراری — بسیار شایع)، **همهٔ فاکتورهای یکی در حساب دیگری هم دیده می‌شود**، با تمام ردیف‌های پیش‌پرداختش. تطبیق نام هیچ گارد «یکتا بودن نام» ندارد.

### مسیر D — تطبیق `undefined === undefined` در یافتن پیشنهاد 🟠

الگوی `offers.filter(x => x.no === i.offerNo)[0]` در `customer-finance.js:27`، `rbac.js:744` و `rbac.js:872`. اگر `i.offerNo` تهی/undefined باشد، اولین پیشنهادِ بدون `no` انتخاب می‌شود و از روی آن `o.buyerCd` (مالکیت مشتری) و حتی `o.advance` (مبلغ پیش‌پرداخت) خوانده می‌شود ⇒ پیش‌پرداخت پیشنهاد بی‌ربط روی فاکتور دیگری اعمال می‌شود.

### ۳-۱) راهکار اجرایی

**گام تشخیصی (قبل از هر تغییر) — `_tools/diagnose-advance-leak.js`:**
اسکریپتی روی `data/*.json` که چهار پرسش را قطعی جواب دهد و ردیف‌های آلوده را با شناسه فهرست کند:
1. فاکتورهایی با `offerNo` تهی که `caseId/customerId` گرفته‌اند (مسیر A)؛
2. پرونده‌های بدون `_id` و بدون `cd` + رسیدهای با `caseId` تهی (مسیر B)؛
3. نام‌های مشتری تکراری پس از نرمال‌سازی (مسیر C)؛
4. پیشنهادهای بدون `no` (مسیر D).
خروجی: گزارش «کدام مشتری، کدام مبلغ، از کدام پرونده» — همان چیزی که برای تأیید نمونهٔ گزارش‌شدهٔ شما لازم است.

**اصلاح‌های کد (همگی جراحی، بدون تغییر معماری):**
- **F2-A:** در `api/sales-domain.php:1108` گارد `if($offerNo==='') continue;` و شرط تک‌بودن پروندهٔ منطبق (اگر بیش از یک پرونده منطبق شد ⇒ انتساب نکن، یک `finding` بساز). ترجیحاً از همان `sd_case_offer_linked()` استفاده شود تا منطق تطبیق یکی باشد.
- **F2-B:** در هر چهار نقطه، کلید تهی هرگز وارد نقشه نشود (`if(!key) return;`) و در شرط پذیرش، رسیدِ با `caseId` تهی فقط وقتی پذیرفته شود که `customerId` صریحاً برابر مشتری باشد.
- **F2-C:** تطبیق نام فقط زمانی مجاز باشد که آن نام نرمال‌شده به **یک و تنها یک** رکورد مشتری برسد؛ در غیر این صورت نادیده گرفته شود (و در گزارش سلامت داده به‌عنوان «مشتری تکراری» علامت بخورد).
- **F2-D:** همهٔ `x.no === offerNo` ها به `offerNo && x.no === offerNo` تبدیل شوند (یک تابع مشترک `findOfferByNo` که با ورودی تهی `null` برمی‌گرداند).
- **F2-E (پاکسازی داده):** برای رکوردهایی که مسیر A قبلاً آلوده کرده، `customerId/caseId` اشتباه با یک فرمان اصلاحی مستند (نه حذف) بازنشانی شود — مطابق همان الگوی `corrections` موجود.

**تسترهای پذیرش:** برای هر چهار مسیر یک سناریوی «مشتری الف / مشتری ب» ساخته می‌شود که پیش از وصله قرمز و پس از آن سبز باشد؛ به‌علاوهٔ یک تستِ عدم‌رگرسیون که تطبیق‌های **درست** (شناسهٔ واقعی) دست‌نخورده بمانند.

---

## ۴) برنامهٔ اجرایی پیشنهادی (ترتیب و گیت خروج)

| فاز | محتوا | وابسته به تصمیم شما؟ | برآورد |
|-----|--------|----------------------|--------|
| **S1** | باگ ۱: F1-1..F1-5 + تستر ابطال/اصلاح از مسیر پرونده | خیر | کوتاه |
| **S2** | تشخیص باگ ۲: اسکریپت `_tools/diagnose-advance-leak.js` + گزارش «کدام مبلغ از کجا» | خیر | کوتاه |
| **S3** | باگ ۲: F2-A..F2-D + تسترهای دو-مشتری | خیر | متوسط |
| **S4** | باگ ۲: F2-E پاکسازی دادهٔ آلوده (پس از دیدن خروجی S2) | بله — نیازمند تأیید شما روی فهرست رکوردها | کوتاه |
| **S5** | حذف تکرار دکمه‌ها طبق تصمیم بند ۲-۳ | **بله — تصمیم طراحی** | کوتاه |

گیت خروج هر فاز (بدون تغییر): `node _tools/uat/run-all.js` با ۰ شکست واقعی + `node --check` روی فایل‌های دست‌خورده + توازن PHP + بامپ نسخه در ۷ نقطه + release notes.

## ۵) ریسک و تضمین عدم رگرسیون

- هیچ‌یک از اصلاح‌ها **رفتار مسیرهای درست را تغییر نمی‌دهد**: در باگ ۱ فقط تطبیق «متحمل‌تر» می‌شود (سرور از قبل هر دو کلید را می‌پذیرفت)، و در باگ ۲ فقط تطبیق‌های مبتنی بر «تهی/نام» **محدودتر** می‌شوند.
- خطر واقعی باگ ۲ این است که پس از وصله، اعداد بعضی مشتریان **تغییر کند** — که همان تصحیح است، اما باید با گزارش S2 پیش از اعمال به شما نشان داده شود. به همین دلیل S4 جدا و مشروط به تأیید شماست.
- تعریف تکراری `ptfInvoiceVoid` در `rbac.js` هم‌اکنون هم مرده است؛ حذفش اثر رفتاری ندارد (با تستر تأیید می‌شود).
