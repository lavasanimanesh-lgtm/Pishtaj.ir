# RELEASE NOTES — v34.7.30 (۱۴۰۵/۰۵/۲۶ — 2026-08-17)

بستهٔ **P1–P3** طبق تأیید کارفرما. ارزیابی کامل: `ASSESSMENT-AWARD-REVISION-AND-REF-PRICE-2026-08-17.md`.
(بستهٔ «بازنگری سند برد» = P4–P6 در نوبت بعد؛ در این نسخه دست نخورده است.)

## P1 — «بارگذاری اقلام درخواست» در فرم پیشنهاد

| کد | ریشهٔ باگ (اثبات‌شده) | اصلاح |
|----|------------------------|--------|
| **FB-1** | وصلهٔ `crm/inqreader.js:1240` تابع را با امضای بدون پارامتر بازنویسی و نسخهٔ قبلی را **بدون آرگومان** صدا می‌زد؛ شمارهٔ انتخاب‌شدهٔ کاربر دور ریخته می‌شد و همان فهرست دوباره باز می‌شد ⇒ «انتخاب می‌کنم، هیچ اتفاقی نمی‌افتد» | وصله با `function (pickedInq)` و `_offLoadOld.apply(this, arguments)` |
| **FB-2** | کشویی فرم، **کد سیستمی RFQ** می‌داد ولی اقلام با **شمارهٔ درخواست کارفرما** ذخیره می‌شوند؛ شرط `r.cd === inq` هم کد خودِ قلم (`IQI-…`) را می‌سنجید ⇒ «اقلامی یافت نشد» | تابع واحد `ptfResolveInqRequest(key)` که درخواست را با همهٔ نام‌های مستعار (`cd`/`inqNo`) و هر سه منبع (`inqitems`، `inqreads`، `rfq.items`) حل می‌کند |
| **FB-3** | انتخاب درخواست هیچ بارگذاری‌ای انجام نمی‌داد | `offerPickInq` اقلام همان درخواست را **خودکار** بارگذاری می‌کند (فقط وقتی جدول اقلام هنوز خالی است تا کار کاربر پاک نشود) |
| **FB-4** | پیام مبهم/سکوت | پیام دقیق «برای این درخواست هیچ قلمی ثبت نشده است» + مسیر ثبت اقلام |

## P2 — نرخ مرجع: از درخواست تا پیشنهاد

| کد | ریشه | اصلاح |
|----|------|--------|
| **FC-1** | **اقلام درخواست اصلاً فیلد قیمت نداشتند** (`inqNo, cd, nm, en, qty, un, st`) | فیلدهای `refPrice / refCur / refSrc / refAt / refBy / refHistory` روی قلم درخواست + ورودی در فرم ثبت قلم و در ردیف‌های مودال ثبت استعلام |
| **FC-2** | لودر همیشه `price: 0` می‌گذاشت و `pcode` را منتقل نمی‌کرد | لودر `pcode` و `sourceItemKey` را می‌برد و نرخ مرجع را از زنجیرهٔ قطعی `ptfItemRefPrice` پر می‌کند: **نرخ دستی پیشنهاد ← قلم درخواست ← بهترین قیمت استعلام تامین ← بانک کالا** |
| **FC-3** | واحد ثابت `'NO'` در برابر «عدد» منبع ⇒ در `procurement-link` امتیاز تطبیق **صفر** می‌شد و هیچ مرجعی پیدا نمی‌شد | واحد قلم درخواست حفظ می‌شود + نگاشت هم‌ارزی واحد (`NO/PCS/عدد/دستگاه…`) در `procurement-link` |
| **FC-4** | کالاهای خودکار همیشه `pr: 0` | کالای خودکار نرخ مرجع قلم درخواست را با ارز/تاریخ/منبع می‌گیرد |
| **FC-5** | پل «استعلام تامین → پیشنهاد» به یک کلید شکننده بسته بود | تطبیق با همهٔ نام‌های مستعار درخواست |

## P3 — نوشتن بازگشتی نرخ مرجع (خواستهٔ صریح کارفرما)

`ptfSyncRefPriceBack(offer, {toCatalog})` پس از **ذخیرهٔ موفق** پیشنهاد:
- نرخ مرجع ویرایش‌شده در پیشنهاد روی **قلم درخواست** می‌نشیند؛ مقدار قبلی حذف نمی‌شود و در `refHistory` با شمارهٔ پیشنهاد، کاربر و تاریخ می‌ماند.
- **بانک کالا فقط با تیک صریح** «نرخ مرجع اصلاح‌شده در بانک کالا هم ثبت شود» به‌روز می‌شود (تصمیم کارفرما) — با تاریخچهٔ `prHistory` و ثبت منبع.
- فقط قلم‌هایی که کاربر نرخشان را ویرایش کرده و **تطبیق یکتا** دارند نوشته می‌شوند؛ بقیه `skipped` گزارش می‌شوند. رویداد در حسابرسی ثبت می‌شود.

## پیشگیری (نگهبان معماری)

دو قاعدهٔ جدید — هر دو دقیقاً از باگ‌های همین نسخه استخراج شده‌اند:
- **A8 (مسدودکننده):** وصلهٔ زنجیره‌ای که آرگومان را منتقل نمی‌کند. *آزموده شد*: با بازگرداندن عمدی باگ، نگهبان `A8: inqreader.js → offLoadInqItems (پارامتر: 1)` را قرمز کرد.
- **A9 (مسدودکننده):** تعریف تکراری یک نام سراسری در **همان فایل** (۲۴ مورد موجود به‌عنوان بدهی مستند ثبت شد؛ از جمله `offUpdRefPrice`, `offUpdMarginPct`, `offApplyGlobalMargin` که در `offers.js` دو بار کپی شده‌اند).

## آزمون

- تستر جدید `_tools/uat/tester433-v34.7.30-inq-items-and-ref-price.js` — **۳۷ PASS / ۰ FAIL** (رفتاری در `vm` + ایستا)، شامل ۴ assertion عدم‌رگرسیون (بدون ویرایش کاربر چیزی نوشته نمی‌شود، تطبیق غیریکتا رد می‌شود، نرخ بدون تغییر تاریخچهٔ الکی نمی‌سازد، واحد واقعاً متفاوت همچنان تطبیق نمی‌خورد).
- `tester44-v124` (قدیمی، متن‌پین): assertion «سه منبع اقلام» به قرارداد به‌روز شد — قابلیت پابرجاست و رفتارش در tester433 آزموده می‌شود ⇒ **۲۲/۰**.
- گیت CI: **۴۸ PASS / ۲ FAIL** (همان دو `known-open` فاز G).
- سوئیت کامل: **۴۱۶ تستر — ۳۵۷ سبز، ۵۹ قرنطینهٔ مستند، ۰ شکست واقعی**.
- نگهبان معماری: PASS. نسخه در ۷ نقطه ⇒ **v34.7.30**.

## در نوبت بعد (تأییدشده، اجرا نشده)

P4 بازرسی قلم‌به‌قلم → P5 فرمان `revise_award` (سند برد جایگزین) → P6 گزارش «بازنگری سند برد و سرنوشت اقلام» → P7 اثر مالی عودت به فروشنده.
تصمیم‌های ثبت‌شدهٔ کارفرما: کاهش قرارداد پس از صدور فاکتور رسمی **مسدود** است؛ عودت به فروشنده **اثر مالی فوری** دارد (بستانکاری تامین‌کننده).
