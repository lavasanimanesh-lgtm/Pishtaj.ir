# 🏛 گزارش ارزیابی هیئت متخصصان — وضعیت نرم‌افزار PTF CRM (v121.2)

**تاریخ:** ۱۶ تیر ۱۴۰۵ | **روش:** بررسی عمیق کد + گردش کار + اسناد، با داده واقعی (نه ادعا)

**ترکیب هیئت:**
- 👨‍💼 مدیر ارشد نرم‌افزارهای اداری (ERP/اتوماسیون) — «صادق»
- 👩‍💼 مدیر باسابقه بازرگانی صنعتی (خرید/فروش EPC) — «مریم»
- 🏗 کارشناس ارشد طراحی سیستم‌های سازمانی (BPM) — «کاوه»
- 🤖 متخصص هوش مصنوعی (مسلط به Gemini/معماری LLM پروژه) — «نیما»
- ⚡️ مدیر ارشد سازمان‌های چابک و مقیاس‌پذیر — «لیلا»
- 🔐 مشاور امنیت نرم‌افزار (عضو مدعو) — «آرش»

---

## بخش ۱ — جمع‌بندی مدیریتی (اجماع هیئت)

نرم‌افزار از نظر **پوشش عملکردی غنی** است (چرخه کامل درخواست→پیشنهاد→برد→تامین→تحویل→تسویه + AI + سایت یکپارچه)، اما در سه محور **بدهی ساختاری** دارد که اگر رسیدگی نشود، با رشد داده و کاربر مشکل‌ساز می‌شود:
1. **دوگانگی ماژول‌ها** (میراث دو شاخه توسعه v51..v89 و v90..v120) — دو سیستم AI، دو مفهوم پرونده، مالی چندپاره، دو واژگان.
2. **تناقض فلسفه چابکی و کنترل** — بخشی از سیستم «حذف آزاد با دلیل» است (US-101 جدید) و بخشی هنوز قفل‌های دوره قبل را دارد؛ باید یک سیاست واحد مصوب شود.
3. **اعتماد به کلاینت** — امنیت سرور به هدر جعل‌پذیر `X-CRM-Role` تکیه دارد (TECHDEBT-001 درست شناسایی شده ولی اجرا نشده).

---

## بخش ۲ — یافته‌های جدید هیئت (با شواهد کد) → یوزر استوری‌های پیشنهادی

### 🔴 باگ‌های گردش کار (کاوه — طراحی سیستم)

**US-260 — حذف پیشنهاد، وضعیت درخواست را یتیم می‌کند**
شاهد: `wfRefresh` فقط در هوک‌های ذخیره/تغییر وضعیت صدا زده می‌شود؛ در مسیر حذف (`ptfReasonedDelete`/`offerDel`) هیچ فراخوانی‌ای نیست. سناریو: TO صادر → وضعیت درخواست WF20 → TO حذف → وضعیت **در WF20 می‌ماند** در حالی که پیشنهادی وجود ندارد. AC: هر حذف پیشنهاد/فاکتور/پرونده → `wfRefresh` روی inqNo مربوطه.

**US-261 — سند TC از ماشین وضعیت جا مانده**
شاهد: `wfCompute` فقط `kind==='TO'` و `'CO'` را می‌شناسد؛ پیشنهاد جامع TC (US-235 دوره v90+) در فیلتر offers نمی‌افتد → صدور/برد TC وضعیت درخواست را تغییر نمی‌دهد. AC: TC در wfCompute هم‌رده CO.

**US-262 — تناقض «حذف آزاد» با قفل‌های بازمانده**
شاهد: ۸ نقطه «فقط ادمین/مدیر» و قفل Inquiry-انتخابی (US-175 قدیم) هنوز فعال است، در حالی که فلسفه مصوب v102 «صفر بروکراسی» است. تصمیم سیاستی لازم: کدام قفل‌ها عمداً می‌مانند (مثلاً قفل برنده و finance) و کدام برداشته می‌شوند؟ خروجی: ماتریس سیاست دسترسی واحد.

### 🔴 باگ‌های داده و مقیاس (صادق — نرم‌افزار اداری)

**US-263 — رشد بی‌مهار صف‌ها و لاگ‌ها در localStorage**
شاهد: `ptf_crm_sendqueue`، `ptf_crm_notifs`، `ptf_crm_audit`، `wfLog` هیچ سقف/هرسی ندارند؛ با کار واقعی ۳ کاربر در چند ماه به سقف ~۵MB localStorage می‌رسند و سینک (۸MB سقف سرور) سنگین می‌شود. AC: هرس خودکار (مثلاً notifs>500، audit>2000، sendqueue ارسال‌شده>30روز) + سنجه حجم در تنظیمات.

**US-264 — deleted_archive سینک نمی‌شود**
شاهد: `ptf_crm_deleted_archive` در SYNC_KEYS/بک‌آپ/whitelist نیست → «بایگانی آماری مدیریت» فقط روی دستگاهِ حذف‌کننده است؛ مدیر روی دستگاه خودش آن را نمی‌بیند (نقض هدف US-107). AC: افزودن به هر سه + هرس ۵۰۰تایی.

**US-265 — ریسک همگامی چند‌دستگاهه برای شمارنده‌ها**
شاهد: کدینگ واحد (`ptfUnifiedCode`) از localStorage محلی می‌خواند؛ دو کاربر همزمان آفلاین → کد تکراری P-1005. SmartMerge برخورد id را حل نمی‌کند، فقط union می‌کند. AC: پیشوند دستگاه/کاربر در سریال یا رزرو بازه از سرور.

### 💰 دیدگاه بازرگانی (مریم)

**US-266 — سود خالص، ارز سند را نادیده می‌گیرد**
شاهد: `ptfCalculateNetProfit` همیشه «تومان» نمایش می‌دهد؛ CO یورویی (US-182) → جمع فروش EUR با خرید ریالی جمع زده می‌شود! AC: تشخیص ارز CO + نرخ تسعیر ورودی کاربر + نمایش دوارزی.

**US-267 — چرخه وندور ناقص است**
یافته: وضعیت وندور (v121.1) ثبت می‌شود ولی هیچ یادآور/پیگیری ندارد. در واقعیت بازرگانی، «در حال بررسی مدارک» یعنی باید پیگیری دوره‌ای شود. AC: تاریخ ارسال مدارک + یادآور خودکار پیگیری ۱۴روزه + گزارش وندورهای معلق.

**US-268 — مطالبات بدون سررسید و هشدار**
یافته: فاکتور پرداخت تدریجی دارد ولی «تاریخ سررسید» و هشدار دیرکرد ندارد؛ برای تیم ۳ نفره، پیگیری وصول مهم‌ترین ریسک نقدینگی است. AC: dueDate per فاکتور + بج قرمز دیرکرد در مطالبات + خلاصه در داشبورد مدیر.

### 🤖 دیدگاه هوش مصنوعی (نیما)

**US-269 — ادغام دو سیستم AI موازی (تصمیم کارفرما گرفته شود)**
شاهد: OCR/خلاصه در `inqreader.js` و جداگانه در `ai-workbench.js` با پرامپت و خروجی متفاوت — دوباره‌کاری، سهمیه‌سوزی و UX دوگانه. پیشنهاد هیئت: موتور واحد = Workbench؛ دکمه‌های 🟢/🟡/📖 در ردیف درخواست فقط «میان‌بر» به همان موتور با کانتکست درخواست.

**US-270 — مصرف سهمیه هوشمند نشده**
شاهد: rate-limit ثابت ۶۰/ساعت per IP (تیم ۳ نفره پشت یک IP دفتر = عملاً ۶۰ برای همه) + هیچ کش پاسخی نیست (ترجمه/خلاصه یکسان دوباره پول/سهمیه می‌سوزاند). AC: کش پاسخ سروری (هش ورودی→پاسخ، ۳۰ روز) + سهمیه per کاربر نه per IP + شمارنده مصرف روزانه در تنظیمات.

**US-271 — پرامپت‌ها بدون نسخه و تست طلایی**
یافته: تغییر پرامپت‌ها هیچ تست رگرسیونی ندارد (مثلاً OCR روی نمونه استاندارد). AC: پوشه `_tools/ai-golden/` با ۳ نمونه ورودی/خروجی مورد انتظار + تستر مقایسه‌ای.

### ⚡️ دیدگاه چابکی و مقیاس (لیلا)

**US-272 — موارد زائد ضد چابکی برای تیم ۳ نفره**
فهرست پیشنهادی حذف/ساده‌سازی (تایید کارفرما لازم): پنل «مالی» قدیمی (fin) که با فاکتور/مطالبات/تنخواه هم‌پوشان است؛ ماژول «تحلیلگر» و «گزارشات» جدا (ادغام در یک داشبورد مدیر)؛ دکمه‌های تایید چندمرحله‌ای بازمانده در جریان سایت↔CRM.

**US-273 — Onboarding و راهنمای درون‌برنامه‌ای غایب است**
با این حجم قابلیت (۲۷ پنل!)، کاربر جدید (حسابدار جدید یا نیروی جایگزین) سرگردان می‌شود. AC: تور خوش‌آمد ۵ قدمی + صفحه «راهنمای نقش من» + جستجوی سراسری Ctrl+K که وعده‌اش (US-221) داده شده ولی فقط health-pill پیاده شده.

**US-274 — آزمون پذیرش کاربری واقعی قبل از عملیاتی شدن**
`ptfConvertToProduction` (پاکسازی داده تست) موجود است — هیئت توصیه می‌کند قبل از اجرا: یک هفته کار موازی واقعی + چک‌لیست پذیرش ۲۰بندی (پیوست این سند در اسپرینت بعد).

### 🔐 دیدگاه امنیت (آرش)

**US-275 (= TECHDEBT-001 با AC اجرایی) — توکن نشست سروری**
شاهد: `X-CRM-Role` از کلاینت خوانده می‌شود = هر کس با DevTools ادمین می‌شود؛ فایل‌های داده (`crm/data/*.json`) با htaccess «Deny» محافظت شده‌اند ولی خود اکشن‌ها گارد واقعی ندارند. AC مرحله‌ای: ① لاگین سروری صادرکننده توکن HMAC انقضاپذیر ② همه اکشن‌های حساس فقط با توکن ③ حذف اعتماد به هدر نقش. (بدون تغییر UX — پشت صحنه)

**US-276 — پاکسازی نشانه‌های نسخه/اطلاعات فنی از خروجی عمومی**
یافته: کامنت‌های حاوی شماره US و ساختار فنی در HTML عمومی سایت؛ User-Agent سفارشی `PTF-CRM-AI-Agent/90.2`. ریسک پایین ولی بهداشت امنیتی. AC: پاکسازی در بیلد ریلیز.

---

## بخش ۳ — رای‌گیری هیئت روی پیشنهادهای قبلی ایجنت (v121.2)

| پیشنهاد | رای | یادداشت |
|---|---|---|
| ادغام AI (خلاصه+📖+Workbench) | ✅ اتفاق آرا | به‌صورت US-269 دقیق‌تر شد |
| منوی مالی واحد | ✅ اتفاق آرا | + حذف fin قدیمی (US-272) |
| ادغام deals+prj | ✅ ۵ از ۶ | مریم: نمای «فروش» و «اجرا» جدا بماند، داده یکی |
| مهاجرت legacy قیمت خرید | ✅ | در همان US-269 اجرا شود |
| واژه‌نامه واحد (TECHDEBT-006) | ✅ اتفاق آرا | «مشتری»، «پیش‌فاکتور (CO)»، «درخواست مشتری» / «استعلام تامین» |

## بخش ۴ — اولویت‌بندی پیشنهادی بک‌لاگ (قاعده کارفرما: پرریسک اول)

| رتبه | آیتم | چرا |
|---|---|---|
| ۱ | US-260, US-261 | صحت گردش کار — قلب سیستم |
| ۲ | US-266 | صحت مالی (ارز) — تصمیم‌گیری مدیر |
| ۳ | US-263, US-264 | سلامت داده و سینک |
| ۴ | US-269, US-272 | ادغام‌ها — کاهش پیچیدگی قبل از رشد |
| ۵ | US-275 | امنیت (قبل از عملیاتی شدن واقعی) |
| ۶ | US-268, US-267 | ارزش بازرگانی |
| ۷ | US-270, US-271, US-265 | استحکام AI و کدینگ |
| ۸ | US-273, US-274, US-276, US-262 | تجربه کاربر و سیاست‌ها |

*همه موارد به بک‌لاگ هندآور اضافه شد؛ هیچ‌کدام بدون تایید کارفرما اجرا نمی‌شود.*
