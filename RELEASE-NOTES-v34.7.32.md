# RELEASE NOTES — v34.7.32 (۱۴۰۵/۰۵/۲۶ — 2026-08-17)

شروع از باقی‌مانده‌های ارزیابی `ARENA-CRM-STATUS-REMAINING-2026-08-17.md`.

## P7 — تخصیص بستانکاری عودت به فاکتور خرید

عودت اقلام مردود به فروشنده دیگر فقط یک «سند اصلاحی شناور» نیست.

- یک **پرداخت تهاتری** (`method: purchase_return`) در حساب تأمین‌کننده ثبت می‌شود.
- مبلغ به‌صورت **FIFO** روی فاکتورهای خرید **باز همان تأمین‌کننده و همان ارز** می‌نشیند (قدیمی‌تر اول).
- اگر فاکتور بازی نباشد، کل مبلغ **اعتبار تخصیص‌نیافته** می‌ماند (رفتار P4 حفظ شده).
- فاکتور پوششی و فاکتور تأمین‌کنندهٔ دیگر لمس نمی‌شوند.
- سند مرجوعی به `financePaymentCd` و فهرست تخصیص لینک است.

## S4 — ابزار پاکسازی ایمن (بدون دست‌زدن خودکار به پروداکشن)

`_tools/apply-advance-leak-cleanup.js`

- پیش‌فرض **dry-run**.
- اعمال فقط با `--apply --yes`.
- فقط دو کار ایمن: یکسان‌سازی `caseId` مستعار، و جدا کردن فاکتور بدون `offerNo` که با تطبیق تهی به پرونده چسبیده.
- مبلغ/رسید حذف نمی‌شود. قبل از نوشتن، پشتیبان در `_s4-backup-*` گرفته می‌شود.

اجرای تشخیص روی دادهٔ واقعی هنوز با شماست:
`node _tools/diagnose-advance-leak.js /path/to/crm/data/sync`

## ARCH-03 (جزئی)

پروندهٔ تازه‌ساخته‌شده در `win_offer` حالا `awardDocs` با snapshot برد دارد (دیگر فقط lazy کلاینت نیست).

## نگهداشت

TODO گمراه‌کننده در مسیر legacy ابطال غیررسمی حذف شد. مسیر V2 از قبل سرور را صدا می‌زند.

## آزمون

- `tester434` با قرارداد جدید P7 هم‌تراز شد.
- `tester435-v34.7.32-p7-s4-awarddocs.js` اضافه و به گیت CI وصل شد.

## هنوز باز (عمداً)

- H1: وصلهٔ workflow گیت‌هاب (مجوز `workflows`)
- ARCH-02: یکی‌کردن سه مسیر نوشتن پول (نیاز به تصویب)
- AN-04: کالیبراسیون healthScore
- اعمال S4 روی دادهٔ پروداکشن
