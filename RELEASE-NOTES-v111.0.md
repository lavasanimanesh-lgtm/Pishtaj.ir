# 🚀 یادداشت‌های انتشار اسپرینت ۱۱۱ (Release Notes - v111.0)

**تاریخ انتشار:** ۱۴۰۵/۴/۱۵ (July 2026)  
**هدف اسپرینت:** استقرار دستیار هوشمند اسناد (US-115)، سیستم کدینگ واحد مینیمال (US-116)، رفع قطعی اسکرول سایدبار موبایل و حذف دسته‌جمعی واقعی در کلود (اولویت ۸ بک‌لاگ محصول)  
**تیم توسعه:** تیم ارشد هوش مصنوعی و معماری سیستم PTF  

---

## 📋 تغییرات اعمال‌شده در این نسخه

### ۱. 🤖 استقرار دستیار هوشمند خواندن اسناد و جاگذاری سریع اقلام (US-115)
* **فایل تغییر یافته:** `crm/ai-workbench.js`
* **شرح تغییر:** مطابق تصویب تیم متخصص در اسپرینت قبل، قابلیت قدیمی خلاصه AI حذف و ماژول جدید دستیار هوشمند اسناد (`window.ptfAiDocReader`) جایگزین آن شد.
* **سازوکار جدید:** این دستیار کاربردی قادر است فایل‌های PDF، عکس، اکسل و ورد (فرمت‌های رایج استعلامات ارسالی کارفرمایان) را پردازش کرده و اقلام استعلام را به طور خودکار استخراج کند. با یک کلیک، این اقلام مستقیماً در فرم پیشنهاد فنی (TO)، پیشنهاد مالی (CO)، ماژول کالا یا فرم استعلام از وندور جاگذاری می‌شوند؛ همچنین به تسریع نگارش متن مکاتبات و قراردادها کمک می‌کند.

### ۲. 🏷️ سیستم کدینگ واحد و مینیمال برای کلیه موجودیت‌ها (US-116)
* **فایل تغییر یافته:** `crm/index.html` (تزریق تابع `ptfUnifiedCode`)
* **شرح تغییر:** منطق کدگذاری سیستم برای تمام ورودی‌ها استانداردسازی و مینیمال شد. از این پس، هر کالایی که از هر ورودی (کاتالوگ، پیشنهاد، دستیار هوشمند) وارد سیستم شود، یک کد مینیمال و یکتا (مثلاً `P-1001`، `P-1002`) دریافت می‌کند که در تمام سیستم ثابت و غیرقابل تغییر خواهد بود. این ساختار برای استعلام‌ها (`RFQ-1001`) و پیشنهادها (`TO-1001` / `CO-1001`) نیز پیاده شد.

### ۳. 📱 رفع قطعی اسکرول افقی و نوشته‌های سایدبار در موبایل (Mobile Sidebar Perfection)
* **فایل تغییر یافته:** `crm/index.html` (تزریق به اولین تگ `<style>`)
* **شرح تغییر:** در بررسی شما مشخص شد که تیترهای آکاردئون (مثل «کالا و اسناد») در موبایل باقی مانده و باعث ایجاد اسکرول افقی می‌شدند. با انتقال قوانین مینیمالیسم مطلق به اولین بلاک استایل صفحه، اکنون تمام تیترهای متنی و فلش‌ها (`.sb-g span:not(:first-child)`) در عرض زیر ۷۶۸ پیکسل به طور دائمی مخفی می‌شوند.
* سرفصل‌ها به کپسول‌های آیکون‌دار ۳۸ پیکسلی با پس‌زمینه خاکستری روشن و متمایز از زیرمجموعه‌ها تبدیل شدند؛ عرض سایدبار با دستور `touch-action: pan-y` روی ۵۶ پیکسل میخکوب شد و جابجایی افقی به صفر مطلق رسید.

### ۴. ☁️ حذف دسته‌جمعی واقعی زباله‌های ابری و کاهش ۱۴۲ فایل (Cloud Batch Delete Engine)
* **فایل‌های تغییر یافته:** `api/storage.php`، `crm/storage.js`
* **شرح تغییر:** بررسی نشان داد علت عدم کاهش ۱۴۲ فایل ابری در اسپرینت قبل، محدودیت درخواست‌های تک‌تک (Sequential Fetch) و عدم استخراج تاریخ فشرده بک‌آپ‌ها در سرور بود.
* اکشن جدید `delete_batch` و استخراج تگ `<LastModified>` به سرور S3 اضافه شد. اکنون با کلیک روی دکمه «☁️ پاک‌سازی زباله‌های ابری»، سیستم لیست کلیدها را بر اساس تاریخ دقیق S3 مرتب کرده، ۳ بک‌آپ آخر را حفظ و مابقی ۱۳۷+ فایل زباله و بک‌آپ مازاد را در **یک درخواست دسته‌جمعی و سریع** از آروان‌کلود حذف می‌کند؛ کش نمایشگر سایدبار نیز در همان لحظه شسته شده و عدد جدید (~۳ فایل) را نشان می‌دهد.

---

## 🧪 وضعیت کنترل کیفیت (QA Verification)
* تست جاگذاری اقلام توسط دستیار هوشمند در جدول پیشنهاد فنی پاس شد.
* تست تولید کد کالا نشان داد که تمام کالاها فرمت مینیمال `P-XXXX` را رعایت می‌کنند.
* تست سایدبار در رزولوشن ۳۶۰ پیکسل موبایل: هیچ اسکرول افقی یا نوشته‌ای وجود ندارد.
