# یادداشت‌های انتشار (Release Notes — Version 33.15.0) — فاز ۱ بکاپ

**پروژه:** سامانهٔ CRM پیشرو تجهیز فرتاک (Pishtaj.ir)
**تاریخ:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**برنچ:** `arena/019fbe19-pishtaj-ir` (PR #11 باز)
**تمرکز:** تکمیل فاز ۱ برنامهٔ فوری بکاپ/حافظه — «🔌 بررسی اتصال سرور» + بکاپ خودکار فقط در صورت تغییر + فشرده‌سازی خودکار روزانه.

---

## 🚀 فاز ۱ — اجراشده (v33.15.0)

### F1-1: بررسی اتصال سرور
- دکمهٔ **«🔌 بررسی اتصال سرور»** در باکس «بک‌آپ و بازگردانی» (تنظیمات).
- `ptfBackupServerStatus()`: پینگ سبک `users_get` → وضعیت: ✅ آنلاین / ⚠️ توکن منقضی (نیاز به ورود) / ❌ آفلاین / خطا + نمایش در ناحیهٔ `ptfBackupConn` + toast.

### F1-2: بکاپ خودکار فقط در صورت تغییر (حذف ارسال ساعتی بی‌شرط — R6)
- `backupSignature()`: امضای djb2 روی **محتوای کامل** همهٔ کلیدهای داده (نسخهٔ اول فقط «طول» را می‌شمرد و با تغییر هم‌طول، بکاپ نمی‌رفت — **باگ کشف و رفع شد**).
- `ptfBackupMaybeAuto()`: هستهٔ تصمیم — بدون تغییر → skip (بدون fetch)؛ با تغییر → pushBackup و پس از موفقیت، امضا آپدیت می‌شود (خطا → امضا نمی‌ماند تا تلاش بعدی دوباره بفرستد).
- `scheduleBackups` حالا از MaybeAuto استفاده می‌کند (ساعتی + ۲ دقیقه بعد از ورود).

### F1-3: فشرده‌سازی خودکار روزانه
- `ptfStorageAutoTame()`: اگر حافظه ≥ ۸۰٪ باشد، پاک‌سازی امن (avatars/queue/هرس سنی) خودکار اجرا می‌شود — **یک بار در روز** (flag `ptf_storage_auto_tame`) و بدون حذف دادهٔ اصلی.

---

## 📋 فایل‌ها
| فایل | تغییر |
|---|---|
| `crm/backup.js` | ptfBackupServerStatus/ServerCheck + backupSignature (hash محتوا) + ptfBackupMaybeAuto + schedule هوشمند + دکمه/ناحیهٔ اتصال |
| `crm/storage-quota.js` | ptfStorageAutoTame + برنامه‌ریزی روزانهٔ خودکار |
| `crm/index.html`, `sw.js`, `clear-cache.html` | بامپ v33.15.0 |
| `_tools/uat/tester295-backup-phase1.js` | **جدید** (۱۲/۰) |
| `RELEASE-NOTES-v33.15.0.md` / `REGRESSION-REPORT-v33.15.0.md` | اسناد انتشار |

---

## ⚠️ برای کاربر
1. `Ctrl+Shift+R` یا `crm/clear-cache.html` (نسخه باید **v33.15.0** باشد).
2. تنظیمات → «🗄 بک‌آپ و بازگردانی» → «🔌 بررسی اتصال سرور» برای مشاهدهٔ وضعیت زنده.
3. بکاپ خودکار حالا فقط با تغییر داده ارسال می‌شود (صرفه‌جویی در پهن‌باند/حافظه).
