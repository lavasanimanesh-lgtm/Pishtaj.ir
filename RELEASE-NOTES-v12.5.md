# 📦 ریلیزنوت v12.5 — رفع باگ کد تکراری + جریان کامل دستیار + تاریخ پیش‌فرض + ویرایش نامه دستیار

**تاریخ:** ۱۴۰۵/۰۴/۱۶ | **نسخه قبلی:** v12.4

---

## 🐞 BUG-007 (US-304) — کد مشابه برای چند کالا: ریشه‌یابی و رفع

**تشخیص شما دقیق بود.** ریشه: کدساز مرکزی از «طول آرایه کالاها» شماره می‌ساخت (`P-1001 + تعداد`). در ثبت گروهی (اکسل/دستیار)، حلقه قبل از ذخیره آرایه اجرا می‌شد → طول ثابت می‌ماند → **همه اقلام یک کد می‌گرفتند**. مشکل دوم: بعد از حذف یک کالا، طول کم می‌شد و کد کالای حذف‌شده به کالای جدید بازتخصیص می‌شد!

**رفع:** کدساز جدید = **بیشینه شماره موجود + شمارنده نشست** که در همان حلقه هم جلو می‌رود:
- ۳ کالا در یک حلقه → P-1001, P-1002, P-1003 (تست شد ✅)
- کدی که قبلاً به کالایی تعلق گرفته هرگز بازتخصیص نمی‌شود (بیشینه‌محور، نه طول‌محور)
- همین اصلاح برای RFQ و TO/CO/TC هم اعمال شد (TC قبلاً اصلاً در کدساز نبود)

## 🤖 US-305/306 — جریان کامل دستیار (سناریوی شما، مو به مو)

> «استعلام میاد، فایل را به دستیار می‌دهم، کالاها را شناسایی می‌کند، از سربرگ می‌فهمد برای کدام شرکت است…»

| گام | رفتار جدید |
|---|---|
| 📎 فایل به دستیار | مثل قبل: استخراج اقلام + برند/مدل |
| 🏢 **تشخیص کارفرما از سربرگ** | موتور OCR ارتقا یافت: نام شرکت و شماره درخواست روی سربرگ/مهر سند را هم برمی‌گرداند |
| ✅ مشتری ثبت‌شده بود | به‌طور خودکار در «مشتری برای TO» انتخاب می‌شود + پیام سبز |
| 🆕 مشتری ثبت نشده بود | پیام: «این مشتری تاکنون ثبت نشده» + **چک‌باکس «ثبت خودکار به‌عنوان مشتری جدید»** — با تیک کاربر ثبت می‌شود (با ضدتکرار و audit) |
| 📋 **ثبت درخواست جدید** | چک‌باکس ④ در اجرای سه‌گانه: همین اقلام به‌عنوان استعلام جدید ثبت می‌شود — **بدون مراجعه دوباره به بخش ثبت درخواست**؛ اقلام به بانک اقلام هم می‌رود تا دکمه «از درخواست» در پیشنهادها (US-303) بلافاصله کار کند؛ وضعیت گردش کار هم رفرش می‌شود |

## 📅 US-307 — تاریخ پیش‌فرض سند پیشنهاد = امروز
- فیلد تاریخ در مودال TO/CO/TC از قبل امروز را نشان می‌دهد و اگر کاربر دست نزند، **همان روی قالب چاپ می‌نشیند** (سه لایه: فرم، ذخیره، قالب — حتی برای اسناد قدیمی بدون تاریخ)
- باگ جانبی هم رفع شد: استایل خراب دوگانه فیلدهای تاریخ (`direction:ltrcolor:...`) که رنگ و جهت را بی‌اثر می‌کرد

## ✏️ US-308 — ویرایش نامه پیش‌نویس دستیار در مکاتبات
ریشه: نامه‌ای که دستیار ذخیره می‌کرد `kind`/`author` نداشت → شرط نمایش دکمه ✏️ در مکاتبات هرگز true نمی‌شد. رفع دو طرفه:
- نامه‌های جدید دستیار با ساختار کامل فرم مکاتبات ذخیره می‌شوند (گیرنده/سمت/موضوع/متن → مستقیم در فرم ویرایش می‌نشیند)
- نامه‌های قدیمی دستیار (بدون kind/author) هم حالا دکمه ✏️ می‌گیرند

## 🧪 کیفیت
| سنجه | نتیجه |
|---|---|
| تست رفتاری کدساز (شبیه‌سازی حلقه ثبت گروهی) | ✅ ۳ کد یکتا + ادامه از بیشینه — داخل تستر ۴۵ دائمی شد |
| audit.py | ✅ ۰ خطا |
| تستر جدید `tester45-v125.js` | ✅ ۲۴/۲۴ PASS |
| رگرسیون کامل | ✅ **۳۲ تستر** همه PASS |

## 📁 فایل‌های تغییریافته
`crm/index.html` (ptfUnifiedCode بازنویسی + VER v12.5) · `api/llm.php` (پرامپت OCR: co/inqno) · `crm/ai-workbench.js` (تشخیص کارفرما + ثبت مشتری/درخواست + نامه کامل) · `crm/offers.js` + `offers-pro.js` (تاریخ) · `crm/letters.js` (قاعده ویرایش) · `_tools/uat/tester45` (جدید)

## ⚠️ نکته استقرار
`api/llm.php` تغییر کرده — حتماً در آپلود جایگزین شود (پرامپت OCR جدید). کلید/کانفیگ دست نخورده.
