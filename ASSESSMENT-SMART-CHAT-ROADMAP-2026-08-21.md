# ارزیابی و نقشهٔ ارتقای «چت هوشمند» سایت — ۲۰۲۶-۰۸-۲۱

**تاریخ:** ۱۴۰۵/۰۵/۳۰ — 2026-08-21 | **دامنه:** ویجت چت عمومی + هوش مصنوعی پشتیبان
**نوع کار:** ارزیابی و پایه‌ریزی (بدون تغییر کد در این نشست)

---

## ۰) حکم یک‌خطی

چت فعلی یک **منشیِ قاعده‌محور خوب** است (پاسخ به اطلاعات شرکت/خدمات/تماس + ارجاع به فرم‌ها)،
ولی **هوشمند نیست**: (۱) نمی‌تواند وضعیت واقعی درخواست را داخل چت بدهد، (۲) به دانش فنی
۴۵۰ مقاله‌ای مرکز دانش دسترسی ندارد، (۳) «ارسال به کارشناس» اصلاً به سرور نمی‌رسد (باگ)،
و (۴) لایهٔ LLM فقط یک پروکسی سادهٔ «منشی» است بدون ابزار (tool) و بدون حافظهٔ دانش.

خبر خوب: **زیرساختِ لازم تقریباً همه‌اش موجود است** — کافی است وصل‌شان کنیم، نه از صفر ساختن.

---

## ۱) معماری فعلی (اندازه‌گیری‌شده از سورس)

| لایه | فایل | نقش فعلی |
|---|---|---|
| ویجت | `assets/js/ptf-chat.js` (~28KB) | دکمهٔ شناور در ۳۹۷/۳۹۸ صفحه + موتور قاعده‌محور (KB شرکت/خدمات/FAQ/برند) + fallback به LLM |
| پروکسی LLM | `api/chat-llm.php` | پراکسی سادهٔ OpenAI/Gemini با prompt «منشی PTF» — بدون ابزار، بدون دانش، ۴۰۰ توکن، ۲۰ درخواست/ساعت، ورودی ۶۰۰ حرفی |
| AI داخلی CRM | `api/llm.php` (~50KB) | قوی: OCR، ترجمه، استخراج RFQ/چک/بیزکارت، techcase، پیشنهاد فنی، تفسیر مدیریتی — ولی **داخلی و auth-دار است، نه عمومی** |
| ایندکس محتوا | `assets/data/search-index.json` | **۶۵۳ صفحه / ۶۵۲ با توضیح** (عنوان+توضیح+URL) — فقط برای صفحهٔ /search/ استفاده می‌شود |
| وضعیت عمومی | `api/crm.php?action=track` | وضعیت RFQ/تامین‌کننده را با نگاشت مشتری‌پسند (`pubMap`) برمی‌گرداند — **چت از آن استفاده نمی‌کند** |
| مشاور متریال | `assistant/` + `ptf-advisor.js` | درخت تصمیم قاعده‌محور — جدا از چت |

---

## ۲) شکاف‌ها و باگ‌های تأییدشده

1. **وضعیت داخل چت وجود ندارد.** کاربر «وضعیت درخواستم؟ + کد» را می‌پرسد → چت فقط لینک
   `/tracking/` می‌دهد، در حالی که endpoint عمومی `track` همان لحظه جواب را دارد.
2. **«سفارش ابلاغ‌شده» اصلاً عمومی نیست.** فقط وضعیت RFQ/تامین‌کننده عمومی است؛ وضعیت
   سفارش (CO برنده → پرونده فروش → تامین/تحویل/تسویه) در `deals/projects` است و هیچ
   نگاشت عمومی ندارد.
3. **باگ: «ارسال گفتگو به کارشناس» فقط در localStorage ذخیره می‌شود** (`ptf_crm_leads`)
   و هرگز به سرور/CRM نمی‌رسد — یعنی درخواست‌های تماس واقعاً گم می‌شوند.
4. **دانش فنی صفر.** LLM فقط prompt عمومی دارد؛ از ۴۵۰ مقالهٔ مرکز دانش خبر ندارد →
   پاسخ فنی سطحی + ریسک توهم (hallucination). `kcSuggest` فقط ۴ کلیدواژه دارد.
5. **LLM بدون ابزار (function calling).** نمی‌تواند `track` را صدا بزند یا جستجو کند.
6. **محدودیت‌ها:** rate limit ۲۰/ساعت کم است؛ پاسخ stream نمی‌شود؛ چندزبانه نیست؛
   `assistant/` (مشاور متریال) به چت وصل نیست.

---

## ۳) نقشهٔ ارتقا (۴ فاز — از ارزان به پیشرفته)

### فاز ۱ — دستیار وضعیت + لید واقعی + دانش محلی (هفتهٔ ۱)
- **Resolver نیت در چت:** اگر متن شامل الگوی کد رهگیری (`PTF-RFQ-…` / `VEN-…`) بود →
  `fetch(api/crm.php?action=track&code=…)` → نمایش وضعیت داخل چت (با همان `statusText`
  مشتری‌پسند). کم‌هزینه و کاملاً امن (endpoint از قبل عمومی است).
- **رفع باگ لید:** `toExpert()` به‌جای localStorage، به سرور POST کند (action عمومی جدید
  یا reuse مسیر ثبت استعلام) تا در CRM به‌عنوان لید بنشیند.
- **دانش محلی:** بارگذاری `search-index.json` در چت → پاسخ فنی به ۶۵۲ موضوع با
  «عنوان + خلاصه + لینک مقاله» حتی بدون LLM (آنلاین/آفلاین).

### فاز ۲ — RAG فنی (ماه ۱)
- وقتی سؤال فنی بود، **۳ مقالهٔ مرتبط** را از ایندکس بازیابی و به‌عنوان context به LLM
  تزریق کنیم (با استناد به لینک مقاله). دو مسیر:
  - (الف) سبک: بازیابی کلیدواژه/BM25 روی `search-index.json` (بدون سرویس خارجی، آفلاین).
  - (ب) پیشرفته: ایندکس embedding سمت سرور (برای پرسش‌های توصیفی).
- توصیه: اول (الف)، بعد (ب).

### فاز ۳ — دستیار تراکنشی «وضعیت سفارش ابلاغ‌شده» (ماه ۱–۲)
- endpoint عمومی جدید `public_status`: با کد رهگیری، زنجیرهٔ
  `استعلام → CO برنده → پرونده فروش → رویدادها` را به متن مشتری‌پسند نگاشت کند:
  «سفارش شما ابلاغ شده ← در حال تامین ← کالا آماده شد ← در حال ارسال ← تحویل شد ← تسویه»
  — **بدون نشت هیچ عدد مالی/داخلی** (همان فلسفهٔ `pubMap` فعلی).
- حفاظت: rate limit + تأیید سبک (کد + مثلاً ۴ رقم آخر موبایل) برای جلوگیری از افشای
  وضعیت به هرکسی که کد را دارد.

### فاز ۴ — «خیلی پیشرفته» (ماه ۲–۳)
- **Agent / function calling:** پروکسی LLM با `tools` (track_status، search_kb،
  create_rfq_lead) — مدل خودش تصمیم می‌گیرد کدام ابزار را صدا بزند (سازگار OpenAI/Gemini).
- **Streaming + کارت‌های غنی:** کارت وضعیت پیشرفت، کارت مقاله، دکمهٔ CTA داخل چت.
- **چندزبانه:** فارسی + انگلیسی + عربی (برای بازار همسایه که تعیین کردید).
- **ارتقای دستی انسان:** دکمهٔ «گفتگو با کارشناس» → واتس‌اپ/تلگرام با متن کامل گفتگو.
- **تحلیل:** ثبت کوئری‌های چت (پرسش‌های پرتکرار) برای بهبود KB و کشف نیاز بازار.

---

## ۴) اولویت پیشنهادی (به ترتیب بازده/ریسک)

| # | اقدام | فاز | اثر | ریسک |
|---|---|---|---|---|
| ۱ | وضعیت درخواست داخل چت (اتصال به track) | ۱ | فوری و قابل‌لمس | خیلی کم |
| ۲ | رفع باگ «ارسال به کارشناس» (لید واقعی به CRM) | ۱ | درآمد/لید | کم |
| ۳ | دانش محلی از search-index (۶۵۲ موضوع) | ۱ | پاسخ فنی آفلاین | کم |
| ۴ | RAG روی LLM (استناد به مقاله) | ۲ | پاسخ فنی دقیق | متوسط |
| ۵ | وضعیت سفارش ابلاغ‌شده (public_status) | ۳ | تجربهٔ تراکنشی | متوسط (نیاز تصمیم) |
| ۶ | Agent + streaming + چندزبانه | ۴ | «خیلی پیشرفته» | بالا |

---

## ۵) پیش‌نیازها / تصمیم‌های لازم از شما

1. **تأیید جهت‌گیری فاز ۳:** می‌خواهید «وضعیت سفارش» هم عمومی شود؟ (افشای حداقلیِ مرحله،
   بدون مبلغ — توصیهٔ من: بله، چون ارزش بالایی دارد و با pubMap کنترل‌شده است.)
2. **provider و بودجهٔ LLM:** الان Gemini (gemini-2.5-flash) تنظیم است؛ برای RAG/agent،
   سقف توکن و نرخ درخواست باید بالاتر برود (مدل flash کفایت می‌کند).
3. **چندزبانه:** آیا عربی هم لازم است (برای کشورهای همسایه)؟

---

## ۶) محدودیت این ارزیابی
- PHP در محیط بررسی نبود؛ صحت runtime با خواندن کد سنجیده شد.
- به دادهٔ واقعی سرور و رفتار کاربران چت (لاگ کوئری) دسترسی نداشتم؛ نمی‌دانم چند درصد
  سؤال‌ها فنی‌اند یا رهگیری — پیشنهاد می‌کنم یک لاگ سادهٔ «نیت هر پرسش» در فاز ۱ اضافه
  شود تا اولویت‌ها با داده تأیید شوند.
