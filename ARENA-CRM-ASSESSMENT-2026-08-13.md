# ارزیابی جامع پروژه CRM — پیشرو تجهیز فرتاک (Pishtaj.ir)

**تاریخ ارزیابی:** 2026-08-13
**نسخه CRM:** v34.5.8 (منبع رسمی: `VERSION.json`)
**مبنای بررسی:** `main` @ `55c1e06` + اجرای واقعی سوئیت رگرسیون + بازرسی استاتیک کد، زیرساخت CI/CD، امنیت و مستندات
**دامنه:** کل مخزن (سایت عمومی + `crm/` + `api/` + ابزارها)

---

## ۱) خلاصه اجرایی

این پروژه از نظر **عمق عملکردی** (CRM فروش/تأمین/مالی/چک/تنخواه/سهامداران/AI) فراتر از یک CRM معمولی است و از نظر **فرهنگ مستندسازی و رفع ریشه‌ای باگ** (RCAهای دقیق، تسترهای UAT) الگوی خوبی دارد. سه مشکل ساختاری باقی‌مانده که ارزش آن را در معرض خطر قرار می‌دهد:

1. **هیچ دروازه کیفیت خودکار کارا نیست:** CI فعلی (`php.yml`) یک composer validate روی مخزنی است که اصلاً `composer.json` ندارد؛ ۴۰۴ تستر UAT وجود دارد اما ۲۸۳ فایل از آن‌ها قرمزند و هیچ‌کدام در CI اجرا نمی‌شوند. به‌عبارت دیگر «تست زیاد، گیت صفر». *(اصلاح در این نشست آغاز شد — بخش ۵)*
2. **باقی‌مانده‌های پشتیبان‌گیری/یکپارچگی داده:** بک‌آپ خارج از سرور (offsite) هنوز تأیید نشده و نوشتن‌های چندکلیدی (مثلاً چک + رویداد مالی + بدهی) هنوز تراکنش گروهی ندارند. مهاجرت MySQL خودش انجام شده (بخش ۳.۱).
3. **ریسک عملیاتی استیجینگ و بهداشت مخزن:** deploy خودکار `arena/**` روی استیجینگ فعال است — **طبق تصمیم کارفرما حفظ می‌شود** — و ریسک آن با گیت CI قبل از FTP مهار شده است (پچ معلق). ۸۵۱ فایل Markdown در ریشه مخزن انباشته شده و نسخه‌ها با سرعت بسیار بالا (v31→v34.5.8 در حدود یک ماه، ۳۵ PR در ۱۳ روز) منتشر می‌شوند.

**نمره کلی سلامت:** **B (حدود ۸۰ از ۱۰۰)** — محصول قوی و معماری داده ارتقایافته؛ باقی‌مانده اصلی: دروازه کیفیت خودکار و بهداشت فرآیند.

### کارنامه حوزه‌ها

| حوزه | نمره (از ۵) | جمع‌بندی |
|---|---|---|
| عملکرد و عمق محصول | ۴.۵ | پوشش کامل فروش، تأمین، مالی، چک، تنخواه، AI |
| امنیت | ۴ | سخت‌سازی خوب؛ CSP با `unsafe-eval` و `new Function` تنها نقطه ضعف بزرگ |
| معماری داده و بک‌اند | ۴ | MySQL منبع حقیقت سرور (فاز A انجام‌شده) + کلاینت نازک با کش/صف (فاز B فعال)؛ باقی: تراکنش گروهی چندکلیدی |
| کیفیت کد | ۳.۵ | بدون خطای syntax، الگوهای منظم، اما بدون lint/فرمت خودکار و bundle |
| تست و تضمین کیفیت | ۲ | ۴۰۴ تستر ولی ۲۸۳ فایل قرمز؛ دروازه CI واقعی ندارد |
| فرآیند توسعه / DevOps | ۲ | CI خراب، deploy FTP بدون اتمیک بودن، ریسک استیجینگ |
| UX موبایل و PWA | ۴ | سرمایه‌گذاری جدی؛ کارت‌سازی موبایل، PWA، بوت‌استاتوس |
| کارایی (Performance) | ۲.۵ | ۹۶ تگ اسکریپت eager، index.html با ۳۲۰KB، بدون minify/bundle |
| داده، پشتیبان‌گیری و بازیابی | ۳ | بک‌آپ چرخشی سرور+کلاینت+اسنپ‌شات؛ بک‌آپ خارج از سرور تأیید نشده |
| سایت عمومی و SEO | ۳.۵ | ۴۵۰+ مقاله، سایتمپ و robots؛ ریسک کیفیت محتوای انبوه تولیدشده با AI |
| مستندسازی و بهداشت مخزن | ۲ | مستندات زیاد و ارزشمند؛ اما ۸۵۱ فایل MD در ریشه و drift نسخه‌ای |

---

## ۲) مشخصات پایه (اعداد تأییدشده)

| شاخص | مقدار |
|---|---|
| نسخه CRM / آخرین بروزرسانی | v34.5.8 / 2026-08-13 |
| اندازه کد CRM (JS) | ~۵۱٬۰۰۰ خط در ~۹۵ ماژول (بزرگ‌ترین: offers.js ۳٬۱۴۵، petty.js ۱٬۸۳۶، bridge.js ۱٬۶۶۱) |
| `crm/index.html` | ۴٬۰۲۳ خط / ~۳۲۰KB — ۹۶ تگ `<script>` همگی eager (بدون lazy-load در main) |
| بک‌اند PHP | ~۷٬۹۰۰ خط در ۲۲ فایل (`api/crm.php` ۱٬۶۲۹ خط، `api/tools.php` ۱٬۹۳۵ خط) |
| تست‌های UAT | ۴۰۴ فایل در `_tools/uat/` + runner سراسری |
| ذخیره‌سازی داده | **MySQL (cPanel همان هاست) = منبع حقیقت سرور**؛ فایل‌های JSON به‌عنوان بک‌آپ گرم همیشه نوشته می‌شوند؛ کلاینت نازک: سرور + کش localStorage/IndexedDB + صف آفلاین (فازهای A و B مهاجرت انجام‌شده) |
| پیوست‌ها | آروان‌کلود S3 — SigV4، آپلود مستقیم مرورگر + proxy fallback، tombstone حذف |
| همگام‌سازی | server-authoritative، rev سراسری + krevs، LWW، push با debounce ۴ ثانیه، pull دوره‌ای ۲۰ ثانیه، allowlist نقشی |
| احراز هویت | توکن HMAC با انقضای ۷ روزه در `tokens.json` (flock)، password_hash/verify، HMAC جداگانه برای عملیات حساس، کپچا/OTP |
| CI | `php.yml` (composer validate — **بدون composer.json در مخزن**)، deploy استیجینگ/پروداکشن از طریق FTP |
| مخزن | ~۸۷MB بدون `.git`؛ ۸۵۱ فایل `.md` فقط در ریشه؛ ۴۵۰ مقاله knowledge-center |

---

## ۳) ارزیابی تفصیلی

### ۳.۱ معماری داده و بک‌اند — ⭐⭐⭐⭐ (مهاجرت انجام‌شده؛ چند باقی‌مانده)

> **اصلاحیه صادقانه نسبت به نسخه اول این گزارش:** ادعای «مهاجرت MySQL نیمه‌کاره» نادرست بود و بر اساس اسناد کهنه (SPEC ۲۰۲۶-۰۸-۰۱ که «منتظر اجرای کارفرما» بود) نوشته شده بود. بررسی مستندات انتشار و کد نشان می‌دهد **هر دو فاز مهاجرت انجام شده است**. جزئیات:

**وضعیت واقعی (مبتنی بر RELEASE-NOTES و کد):**

| فاز | وضعیت | شواهد |
|---|---|---|
| **فاز A — سرور → MySQL** | ✅ **انجام‌شده و سوییچ نهایی روی پروداکشن فعال** | `RELEASE-NOTES-v33.22.0` (2026-08-02): سیم‌کشی مسیر سینک ۵۲ کلید به MySQL روی همان هاست (cPanel)؛ `RELEASE-NOTES-v33.22.3` رخداد پروداکشن ۲۰۲۶-۰۸-۰۳ را مستند کرده که صراحتاً با `mode=mysql` فعال روی پروداکشن رخ داده است |
| **فاز B — کلاینت نازک** | ✅ **پیاده‌سازی و فعال‌شده** | `crm/client-server.js` (DB-MIG-001 فاز B): getData/setData سرورمحور با کش ۳۰ثانیه، صف آفلاین + ارسال دسته‌ای ۲۰کلیدی، همگرایی یک‌باره با تأیید کاربر (`ptf_b_synced_<user>`)، آینه IndexedDB برای کلیدهای سنگین (>۱۲۰هزار کاراکتر)، پاک‌سازی امن کش محلی با گارد سه‌گانه؛ `RELEASE-NOTES-v33.20.0` رفتار «با فاز B فعال» را مستند کرده است |
| گارد تازگی داده | ✅ | `ptf_db_read_fresh` در `db-lib.php` (v33.22.3): اگر فایل تازه‌تر از ردیف DB باشد، مقدار تازه سرو و ردیف کهنه خودترمیم می‌شود — ریشه‌کن کلاس رخداد «ناپدید شدن ضمایم» |
| migrate.php روی پروداکشن | ✅ قفل است | گارد محیطی: روی production بدون `migrate_allow`/فایل unlock پاسخ 403 می‌دهد؛ پچ استثنای دیپلوی هم در `_tools/PENDING-workflow-ci-gate-2026-08-13.patch` آماده شده است |

**نقاط قوت این معماری:**
- نوشتن «فایل همیشه اول، سپس DB» + فایل‌ها هرگز حذف نمی‌شوند = بک‌آپ گرم دائمی؛ خطای DB هرگز عملیات کاربر را نمی‌شکند.
- سوییچ معکوس‌پذیر و مرحله‌ای بوده (off → dual → mysql) و دو رخداد بعد از سوییچ (v33.22.3/4) با ریشه‌کنی واقعی بسته شده‌اند، نه وصله موقت.

**باقی‌مانده‌های واقعی (کوچک‌تر از چیزی که گزارش اول ادعا کرد):**
1. **تراکنش گروهی چندکلیدی:** جدول `ptf_kv` کلید-ارزش است؛ زنجیره «چک + رویداد مالی + بدهی تأمین‌کننده» چند ردیف/کلید جداگانه می‌نویسد بدون تراکنش سراسری. قطع وسط = ناسازگاری که با reconcilerها کشف می‌شود. (بهبود P2، نه بحران)
2. **بک‌آپ offsite:** MySQL روی همان هاست است؛ dump خارج از سرور + تست بازیابی همچنان باز است (INC-A4).
3. **کوئری‌های سنگین:** گزارش‌ها همچنان سمت کلاینت محاسبه می‌شوند؛ جدول kv برای گزارش‌های رابطه‌ای سروری بهینه نیست (خارج از دامنه فعلی، آینده).
4. `tokens.json` (نشست‌های ورود) همچنان فایل‌محور است — کاندید بعدی جابه‌جایی به DB.

**توصیه:** نگه‌داشتن همین مسیر؛ اولویت بعدی در این حوزه = offsite backup + تست بازیابی، سپس تراکنش گروهی برای زنجیره‌های مالی.

### ۳.۲ امنیت — ⭐⭐⭐⭐ (قوی، با دو استثنا)

**تأیید شده‌ها:**
- هدرهای امنیتی در `.htaccess`: CSP، X-Frame-Options، HSTS، Permissions-Policy، Referrer-Policy؛ مسدودسازی `*.json/log/md/zip/sql` (به‌جز manifest.json).
- اسرار خارج از webroot (`ptf-secrets.php`، `llm-config.php`، `storage-config.php`) با نمونه‌های `.sample.php`؛ اسکن خودکار هیچ کلید واقعی در کد پیدا نکرد.
- توکن‌ها HMAC‌شده با nonce و انقضا؛ نقش از سمت سرور (توکن) خوانده می‌شود نه کلاینت؛ عملیات حساس (delete_user، purge_cloud و…) HMAC دوم دارند.
- llm.php: rate-limit سروری ۶۰ درخواست/ساعت/IP + سهمیه نقش‌محور در فایل سرور؛ origin-check؛ chat-llm.php عمومی نیز rate-limit و محدودیت طول دارد.
- داده‌ها در `crm/data` با `.htaccess "Deny from all"` خودایجاد محافظت می‌شوند؛ SVG/HTML از origin برنامه inline سرو نمی‌شوند (ضد XSS).

**نقاط ضعف:**
1. **CSP شامل `unsafe-inline` و `unsafe-eval` است** — عملاً حفاظت CSP در برابر XSS اجرایی را خنثی می‌کند. ریشه: `new Function` در `cms.js` و `supspec.js` + هندلرهای inline. باید nonce/رفع eval پیاده شود.
2. **`tokens.json` فایل‌محور است:** اگر مهاجم به فایل‌سیستم دسترسی پیدا کند یا بک‌آپ قدیمی نشت کند، توکن‌های معتبر (بدون رمز) افشا می‌شوند. گذر به DB با TTL (کاندید P2 — بخش ۵).
3. **نرخ‌سنج ورود:** کپچا/OTP برای مسیر عمومی وجود دارد، اما rate-limit سراسری برای `auth_login` سروری باید تأیید/تقویت شود (در کد، محدودیت صرفاً برای chat/llm دیده شد).
4. اسکریپت‌های probe در `_tools/` (`data-probe.php`، `storage-probe.php`) نباید هرگز به production دیپلوی شوند — فیلتر deploy صریح برای `_tools/` توصیه می‌شود.

### ۳.۳ کیفیت کد و تست — ⭐⭐ (تست‌های زیاد، گیت کیفیت صفر)

**نتایج اجرای واقعی سوئیت رگرسیون (۲۰۲۶-۰۸-۱۳):**

| سنجه | نتیجه |
|---|---|
| فایل‌های تستر | ۴۰۴ |
| فایل‌های قرمز | **۲۸۳** |
| کرش کامل (خطای JS در خود تستر) | ۱۰ — `tester81` (ReferenceError: mergeCmpRecordsForInquiry)، `tester95` (ReferenceError: oRows)، `tester96` (TypeError: reading 'st')، `tester119`، `tester126`، `tester190`، `tester331`، `tester333`، `tester334` (TypeError: document.createElement — هارنس بدون DOM)، `tester340` |
| hang / soft | ۰ / ۰ |
| `node --check` روی تمام ماژول‌های crm | ✅ بدون خطای syntax |

> **به‌روزرسانی (همان روز، پس از بازسازی سوئیت و تریاژ سطل ۱):** با تبدیل ۱۰۶ تستر از پین نسخه‌ای کهنه به «قرارداد نسخهٔ واحد» و رفع ۵ کرش (تزریق وابستگی‌های IIFE، به‌روزرسانی قرارداد دورهٔ تنخواه، اصلاح regex حذف بوت) + آرشیو یک تستر منسوخ، نتیجه به **۱۷۵ فایل قرمز** و **کرش‌ها از ۱۰ به ۱** رسید. تنها کرش باقی‌مانده (`tester340`، مرتبط با یکپارچگی ردیف هزینه جاری) **مشکوک به رگرسیون واقعی محصول است** و باید در محصول بررسی شود. جزئیات کامل: **`ARENA-UAT-TRIAGE-2026-08-13.md`**.

**نکته مهم درباره کرش‌ها:** کرش به معنای باگ محصول نیست — مثلاً `tester81` تابع `mergeCmpRecordsForInquiry` را صدا می‌زند درحالی‌که این تابع در `buycompare.js` (خط ۱۵) همچنان موجود است؛ یعنی هارنس تستر با ساختار فعلی کد ناسازگار شده است. این دقیقاً «تستر شکسته»، نه «محصول شکسته» است — ولی تا وقتی تریاژ نشود، سیگنال واقعی از نویز جدا نمی‌شود.

- **کلاس A — تست‌های کهنه (test drift):** از ۲۸۳ فایل قرمز، **۱۰۳ فایل فقط به‌خاطر چک‌های نسخه‌ای پین‌شده** («VER الگوی v1x»، «کش sw هم‌خانواده ptf-crm-v1x»، «نسخه v16.x/v21.x/v26.7/v33.5.0») قرمزند؛ تسترها نسخه قدیمی را hardcode کرده‌اند درحالی‌که نسخه جاری v34.5.8 است. این‌ها رگرسیون محصول نیستند اما **سوئیت را غیرقابل اعتماد کرده‌اند** (سیگنال/نویز از بین رفته).
- **کلاس B — ۱۷۰ فایل با حداقل یک شکست غیرنسخه‌ای:** این‌ها **نیازمند تریاژ تک‌به‌تک هستند** — هر شکست یکی از سه حالت است: (الف) تستر کهنه نسبت به بازطراحی عمدی ماژول (مثلاً حذف مسیر سنا از FX در v33.4.2)، (ب) رگرسیون واقعی، (ج) قرارداد تغییرکرده بدون به‌روزرسانی تستر. نمونه‌هایی که باید تریاژ شوند:
  - **ماژول FX (نرخ ارز):** tester78 — ۱۶ شکست (خرید ارزی/ریالی، pendingFx، مانده ارزی وصول‌نشده، مسیر قدیمی buyquotes)؛ tester79 — نرخ‌های سنا؛ tester92 — انتخاب مبنای نرخ آزاد/سنا. *(توجه: بخشی از این‌ها ممکن است عمداً با «حذف مسیر سنا» تغییر کرده باشند — به همین دلیل «تریاز» لازم است، نه قضاوت فوری)*
  - **مالی:** tester68 (هشدار تأخیر مدیران + ضدتکرار روزانه)، tester84 (ptfPayableUpsert محاسبه مبلغ×تعداد، باکس بدهی مدیران)، tester97 (حقوق موظف → sharetx+opex، محرمانگی RBAC).
  - **RBAC/موبایل/UI:** tester82 (RBAC display:none، بستن کشوی تور موبایل، اثرات جانبی bridge)، tester85 (لایه ۲ bootstrapped، برچسب سنا)، tester87 (ناظر goPanel، دکمه تست منبع)، tester90 (مسیر استعلام جدید).
  - **متفرقه:** tester7 (قفل دکمه فاکتور غیربرنده)، tester9 (هندل 403 کپچا)، tester70 (بوت‌استرپ سرور خالی/آفلاین/بک‌آپ چرخشی)، tester73 (اتصال دکمه به ptfXlsGuideGo).

**نتیجه:** باید ابتدا سوئیت «پایه‌سازی» شود (حذف/به‌روزرسانی گیت‌های نسخه‌ای منسوخ)، سپس شکست‌های کلاس B یکی‌یکی تعمیر شوند؛ و **خروجی رگرسیون به CI گره بخورد**. بدون این، «نسخه پایدار» یک ادعای مستند اما تست‌نشده است.

**شاهد سرعت drift:** `REGRESSION-REPORT-v21.3.md` ثبت‌شده در مخزن (2026-07-22) وضعیت را «۲۵۳ از ۲۵۳ PASS» در v31.7.97 اعلام کرده بود؛ سه هفته بعد (اجرای همین نشست) نتیجه «۲۸۳ فایل قرمز از ۴۰۴» است — یعنی سوئیت بدون نگهداری منظم، در حدود سه هفته از سبز کامل به قرمز اکثریتی رسیده است.

### ۳.۴ فرآیند توسعه و DevOps — ⭐⭐

1. **CI خراب/بی‌اثر:** `php.yml` فقط `composer validate --strict` اجرا می‌کند، اما در کل مخزن `composer.json` وجود ندارد → این جاب یا fail می‌شود یا بی‌معناست. هیچ PHP lint، هیچ اجرای تسترهای UAT، هیچ دروازه‌ای در CI نیست.
2. **deploy استیجینگ خودکار روی `arena/**`** همچنان در `deploy-staging.yml` فعال است — همان ریسکی که در RCA 2026-08-11 (Staging Collision) ثبت شده و به‌دلیل محدودیت permission نتوانسته بود اصلاح شود. **باید در سطح GitHub (نه agent) غیرفعال شود.**
3. **deploy FTP بدون اتمیک بودن:** آپلود فایل‌به‌فایل؛ اگر وسط آپلود خطا شود، سایت نیمه‌به‌روز می‌ماند. حداقل یک استراتژی staged-upload + switch یا توقف روی خطای FTP لازم است.
4. **دو PR باز رهاشده** (#22 و #25 از 2026-08-06) که با مرج‌های بعدی منسوخ شده‌اند ولی بسته نشده‌اند — نشانه عدم housekeeping فرآیندی.
5. **سرعت تغییر نسخه بسیار بالاست** (۳۵ PR در ۱۳ روز؛ v31→v34.5.8). بدون گیت CI این سرعت ذاتاً پرریسک است.
6. **آلودگی ریشه مخزن:** ۸۵۱ فایل MD در ریشه (ده‌ها نسخه تکراری `ADV-CV-*-v31.7.80..97`، `STAGE-0-*`، RCAها). باید در پوشه `docs/` بازآرایی و نسخه‌های قدیمی آرشیو شوند. تاریخچه گیت خود آرشیو است؛ نیازی به نگه‌داشتن ۲۰ نسخه از یک بک‌لاگ نیست.

### ۳.۵ کارایی و تجربه کاربری — ⭐⭐⭐ (UX) / ⭐⭐.۵ (Performance)

**نقاط قوت UX:**
- سرمایه‌گذاری جدی موبایل: تبدیل جدول به کارت لمسی، نوار پایین، حالت شب، PWA با آیکون/مانیفست، بوت‌استاتوس برای cold start، جلوگیری از FOUC تم با CSS بحرانی. این سطح توجه به موبایل در پروژه‌های هم‌رده نادر است.

**نقاط ضعف کارایی:**
1. **۹۶ تگ اسکریپت eager** در `crm/index.html`؛ ماژول‌ها `defer` هستند اما همگی دانلود و parse می‌شوند (~۵۱K خط JS). `xlsx.min.js` با ۲۷۶KB همیشه لود می‌شود. راه‌حل: bundle (esbuild) + code-splitting بر اساس پنل، و lazy-load فقط برای xlsx/print.
2. `crm/index.html` با ۳۲۰KB — CSS اینلاین بزرگ (مخصوصاً قواعد موبایل `!important`). این حجم در هر cold start باید parse شود.
3. لود تنبل اصولی (`ptfLoadScriptOnDemand`) که در PR #25 طراحی شده بود هنوز در main نیست.
4. سرویس‌ورکر وجود دارد و cache-busting نسخه‌ای منظم است ✅ — اما باید `xlsx` و فونت‌ها را precache کند و استراتژی assets را ثابت نگه دارد.

### ۳.۶ داده، پشتیبان‌گیری و بازیابی — ⭐⭐⭐

- بک‌آپ چرخشی سرور (hourly/daily) در `api/crm.php` + بک‌آپ کلاینت (`backup.js` ۷۷۶ خط) + اسنپ‌شات پیش از pull + تگ/Release zip در deploy. زیرساخت فکرشده است ✅.
- **شکاف:** بک‌آپ خارج از سرور (offsite) همچنان آیتم باز بک‌لاگ (INC-A4) است؛ یعنی کل داده پشتیبان روی همان هاست است — یک خطای هاست/حمله = از دست رفتن همزمان اصل و پشتیبان. پیشنهاد: cron شبانه + آپلود به S3 (زیرساخت S3 که موجود است) یا سرور دوم، با تست بازیابی ماهانه.

### ۳.۷ سایت عمومی و SEO — ⭐⭐⭐.۵

- ۴۵۰ مقاله در `knowledge-center/` + سایتمپ ۸۶KB + robots.txt + ریدایرکت‌های 301 + فشرده‌سازی gzip + هدرهای امنیتی. زیرساخت فنی SEO قابل قبول است.
- **ریسک محتوا:** تولید انبوه مقاله با موتور (`run-generator.php`، ادعای ۲٬۱۱۱ مقاله غنی‌شده) با سیاست‌های کیفیت گوگل (helpful content) ریسک دارد. فازهای بازنویسی کیفیت انجام شده (مستندات `_audit/SEO-*` موجود است) اما پیشنهاد می‌شود یک نمونه‌برداری تصادفی از ۵۰ مقاله با معیار E-E-A-T (داده واقعی، تجربه صنفی، منابع) به‌صورت دوره‌ای انجام شود و مقالات ضعیف `noindex` یا حذف شوند.
- `api/cms.php` و ابزارهای CMS موجودند؛ اما نظارت بر broken-link به‌صورت دوره‌ای خودکار نیست (ابزارهای پایتونی فقط زمان پروژه اجرا شده‌اند).

### ۳.۸ هوش مصنوعی — ⭐⭐⭐⭐ (جایگاه خوب)

- `ai-workbench.js` (Sprint 120): سه خط لوله (کاتالوگ→سورسینگ→پیش‌نویس TO)، دستیار نامه/قرارداد، OCR استعلام، ترجمه فنی، leadfinder، تفسیر مدیریتی — ترکیب عملی و درست برای این کسب‌وکار.
- پروکسی `llm.php` کلید را سمت سرور نگه می‌دارد، rate-limit سروری و سهمیه نقش‌محور دارد، تاریخچه AI در IndexedDB ✅.
- پیشنهاد: لاگ کامل پرامپت/پاسخ (با ماسک داده حساس) برای بازبینی کیفیت، و امکان قطع kill-switch سراسری AI از تنظیمات ادمین.

---

## ۴) نقاط قوت برجسته (باید حفظ شوند)

1. **فرهنگ RCA و رفع ریشه‌ای:** مستندات 2026-08-11 نمونه درخشان تحلیل race condition، merge سطح فایل و tombstone هستند.
2. **امنیت جدی:** هیچ کلید/رمز واقعی در کد نیست؛ اسرار خارج از webroot؛ HMAC دولایه برای عملیات حساس؛ origin-check روی endpointها.
3. **عمق مالی:** چک (شخصی/شرکتی/دسته‌چک)، تنخواه، سهامداران، سال مالی، نرخ ارز، سود/زیان — فراتر از CRM استاندارد.
4. **همگام‌سازی آفلاین بالغ** با rev/krevs و allowlist نقش.
5. **تجربه موبایل و PWA** واقعاً کارشده (بوت‌استاتوس، کارت‌سازی، safe-area).
6. **انضباط نسخه‌ای:** VERSION.json به‌عنوان منبع واحد + cache-busting هماهنگ `?v=` + سرویس‌ورکر.

---

## ۵) نقشه راه پیشنهادی (اولویت‌بندی‌شده)

### P0 — فوری (این هفته؛ کم‌ترین تلاش، بیشترین کاهش ریسک)

| # | اقدام | توضیح |
|---|---|---|
| 1 | ~~غیرفعال‌کردن deploy خودکار `arena/**`~~ → **تصمیم کارفرما: حفظ خودکار** | **آماده و تحویل‌شده در این نشست:** دیپلوی خودکار دست‌نخورده می‌ماند و به‌جایش **گیت CI (۱۶ تستر سینک/مالی/اعلان/امنیت — `run-ci-gate.js`) قبل از FTP در هر دو workflow** به‌همراه استثنای `api/migrate.php` و `api/data-health-check.php` از پروداکشن آماده شد. push مستقیم به‌دلیل نبود `workflows` permission برای GitHub App ممکن نبود؛ پچ کامل در **`_tools/PENDING-workflow-ci-gate-2026-08-13.patch`** آماده است و باید یک‌بار به‌صورت دستی در GitHub (یا با اعطای مجوز `workflows` به App) اعمال شود |
| 2 | **رفع/تعویض `php.yml`** | **انجام شد (این نشست، به‌صورت پچ):** جایگزین کامل آماده شد — `php -l` روی api/ و crm/ + `node --check` روی همه JS (به‌جز کتابخانه min) + گیت UAT کانونی. پچ: **`_tools/PENDING-workflow-php-lint-2026-08-13.patch`** (اعمال دستی لازم — همان محدودیت permission) |
| 3 | **اجرای گیت رگرسیون در CI** | تا حدی انجام شد (گیت در مسیر deploy + پچ php.yml)؛ تکمیل با workflow مستقل UAT روی push/PR به main |
| 4 | **بستن PRهای منسوخ #22 و #25** | ✅ **انجام شد (این نشست):** هر دو PR با کامنت توضیح منسوخ‌شدن بسته شدند |
| 5 | **بک‌آپ offsite** | cron شبانه: dump دیتابیس MySQL + فایل‌های JSON + آپلود به S3 (زیرساخت آروان موجود است) + یک تست بازیابی مستندشده |
| 6 | محافظت `run-generator.php` | ✅ **انجام شد (این نشست):** گارد محیطی اضافه شد — روی پروداکشن بدون کلید مخفی `content_gen_allow` یا فایل unlock پاسخ 403 می‌دهد (الگوی قفل migrate.php) |

### P1 — کوتاه‌مدت (۲–۴ هفته)

1. **بازسازی سوئیت رگرسیون:** حذف/به‌روزرسانی چک‌های نسخه‌ای کهنه (v1x/v16/v26…)؛ استانداردسازی تسترها برای خواندن نسخه از `crm/index.html` به‌جای hardcode. هدف: صفر قرمز کاذب.
2. **تعمیر شکست‌های عملکردی کلاس B** به ترتیب اثر مالی: FX (tester78/79/92) → مالی (84، 97، 68) → RBAC/موبایل (82، 85، 87) → کرش‌ها (81، 95، 96).
3. **CSP بدون unsafe:** حذف `new Function` (دو مورد)، جایگزینی هندلرهای inline با listener، سپس حذف `unsafe-inline/unsafe-eval` از CSP.
4. **بازآرایی مستندات:** انتقال MDهای ریشه به `docs/` با نگه‌داشتن فقط آخرین نسخه هر سند.
5. **کاهش وزن بوت:** bundle با esbuild + لود xlsx/print به‌صورت on-demand.

### P2 — میان‌مدت (۱–۲ ماه)

1. **تراکنش گروهی برای زنجیره‌های مالی** (چک + رویداد + بدهی): جدول kv را می‌توان با یک گروه‌بندی `tx_id` و commit دومرحله‌ای ارتقا داد؛ یا کلیدهای مالی پرریسک به‌صورت گروهی با قفل صف سرور نوشته شوند.
2. **deploy اتمیک:** staged upload + switch، یا مهاجرت به rsync/SSH با release symlink.
3. **Rate-limit سروری `auth_login`** و گذر `tokens.json` (نشست‌ها) به DB با TTL.
4. **ممیزی دوره‌ای محتوای SEO** با معیار E-E-A-T و ابزار broken-link.
5. **monitoring سلامت DB:** چک روزانه تطابق فایل↔DB (data-health-check بخش ۱۱) به‌صورت cron + اعلان خودکار به مدیر.

### P3 — بلندمدت (۳+ ماه)

1. در صورت رشد تیم: معرفی یک فریم‌ورک سبک یا حداقل ماژول سیستم استاندارد، eslint/prettier سراسری، و تست یونیت برای هسته مالی (`finance-core`).
2. داشبورد مانیتورینگ: uptime، خطاهای PHP، سلامت سینک، سهمیه AI (حتی با cron ساده + notify-bot موجود).
3. بازنگری نقشه راه محصول بر اساس داده واقعی استفاده (که زیرساخت audit آن موجود است).

---

## ۶) جمع‌بندی

این CRM یک **دارایی واقعی کسب‌وکار** است: عمق عملکردی، انضباط مالی و فرهنگ مستندسازی آن بالاتر از استاندارد پروژه‌های مشابه است. «آخرین وضعیت» آن: **محصولی قوی با معماری داده‌ای که تازه به MySQL و کلاینت نازک ارتقا یافته (فازهای A و B مهاجرت انجام‌شده)، اما بدون دروازه کیفیت خودکار مؤثر و با فرآیند deploy پرریسک در حال توسعه پرسرعت است.** پایه داده دیگر «شکننده» نیست؛ اولویت‌ها به کیفیت/DevOps منتقل شده‌اند.

اگر فقط سه اقدام از این گزارش انجام شود، به ترتیب:
1. اعمال دو پچ آماده‌شده workflow (گیت CI قبل از FTP + جایگزینی php.yml) تا دروازه کیفیت واقعی فعال شود؛
2. بازسازی سوئیت تست تا «سبز بودن» معنا پیدا کند و سپس سبز کردن واقعی آن؛
3. بک‌آپ offsite دیتابیس/فایل‌ها با تست بازیابی مستند.

با این سه اقدام، فرآیند به اندازه محصول بالغ می‌شود و می‌توان با خیال راحت‌تر به توسعه فیچر ادامه داد.

---

*این گزارش بر اساس بازرسی استاتیک مخزن در تاریخ 2026-08-13 و اجرای کامل سوئیت رگرسیون تهیه شده است. اعداد ذکر شده قابل بازتولید هستند.*
