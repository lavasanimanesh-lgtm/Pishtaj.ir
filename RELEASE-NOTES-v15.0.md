# 🛡 ریلیز v15.0 — US-384 «ریشه‌کنی باگ بحرانی سینک» (کیس استادی کارفرما)

**تاریخ:** ۱۴۰۵/۰۴/۱۸ | **نوع:** رفع فوری باگ بحرانی — طبق دستور «ریشه باگ دقیق شناسایی و سریعا ریشه‌کن شود»
**استوری:** US-384 — Lost Update در همگام‌سازی + مسموم شدن بک‌آپ چرخشی + بازگردانی ناقص

---

## 🔬 گزارش کارشناسی تیم تخصصی — سه ریشه دقیق پیدا شد

کیس استادی شما (پیش‌نویس دستیار → رفرش کاربر دوم → حذف از هر دو دستگاه) **سه نقص زنجیره‌ای** را همزمان فعال کرده بود:

### 🔴 ریشه ۱ — Lost Update: رفرش کاربر دوم، داده کهنه را «برنده» می‌کرد
بازسازی دقیق لحظه‌به‌لحظه حادثه شما:
1. شما پیش‌نویس ثبت کردید → ۴ ثانیه بعد push شد → سرور نسخه جدید را داشت ✅
2. کاربر دوم رفرش کرد. مرورگر او **کپی کهنه** داده (بدون پیش‌نویس شما) را در localStorage داشت.
3. نقص کشنده: هنگام لود صفحه، ماژول‌ها حین رندر `setData` می‌زنند (مهاجرت وضعیت TO، فلگ انقضای CO، پاکسازی کالاها و…). این نوشتن‌های خودکار روی **داده کهنه**، کلید را «تغییریافته» علامت می‌زد و **قبل از دریافت نسخه سرور**، لیست کهنه push می‌شد.
4. سرور مدل «آخرین نویسنده می‌برد» (LWW) داشت — **هیچ چکی نمی‌کرد که نویسنده روی کدام نسخه کار کرده** → لیست کهنه جایگزین شد و پیش‌نویس شما از سرور پاک شد.
5. ۲۰ ثانیه بعد pull دستگاه شما همان لیست بی‌پیش‌نویس را آورد → «از صفحه من هم حذف شد». 💥

### 🔴 ریشه ۲ — بک‌آپ چرخشی از داده مسموم بازنویسی می‌شد («بک‌آپ قبلی هم خراب بود»)
بک‌آپ خودکار **۲ دقیقه بعد از هر ورود** + ساعتی، از localStorage همان دستگاه گرفته و `hourly-latest` را **جایگزین** می‌کند. وقتی داده یک دستگاه آسیب می‌دید، اولین بک‌آپ خودکار بعدی، **نسخه سالم بک‌آپ را هم با داده خراب بازنویسی می‌کرد.** دقیقا این بود که دفعه دوم دیدید «همان فایل بک‌آپ دیگر مشتریان را برنمی‌گرداند».

### 🔴 ریشه ۳ — بازگردانی فقط مرورگر را برمی‌گرداند، نه سرور سینک را
«بازگردانی از بک‌آپ» فقط localStorage را جایگزین می‌کرد؛ فایل‌های سینک سرور (`crm/data/sync/`) خراب می‌ماند. **حداکثر ۲۰ ثانیه بعد،** pull دوره‌ای داده خراب سرور را دوباره روی داده بازگردانده می‌ریخت — به همین دلیل بازگردانی دوم شما «اثر نکرد» و مقصر فایل بک‌آپ به نظر رسید.

---

## ✅ درمان ریشه‌ای (نه مسکن)

### 1️⃣ کنترل همزمانی per-key (Optimistic Concurrency) — پایان Lost Update
- هر کلید داده روی سرور **شماره نسخه** دارد؛ هر push کلاینت «نسخه مبنا»یش را اعلام می‌کند.
- سرور: نوشتن روی نسخه‌ای جدیدتر از مبنای نویسنده = **رد** + برگرداندن نسخه فعلی سرور.
- کلاینت: نسخه سرور را با داده خودش **ادغام هوشمند** می‌کند (ptfSmartMerge — رکورد هیچ‌کس گم نمی‌شود) و نتیجه را با مبنای جدید می‌فرستد. تعارض در audit ثبت می‌شود.

### 2️⃣ گیت بوت — رفرش دیگر هرگز داده کهنه صادر نمی‌کند
تا سینک اولیه با سرور کامل نشود، **هیچ push‌ای مجاز نیست** (setDataهای خودکار حین رندر بی‌خطر شدند). آفلاین قفل نمی‌ماند — کار محلی آزاد و push بعدی با مبنای نسخه محافظت‌شده است. beacon هنگام بستن تب هم مبنادار شد.

### 3️⃣ سپر بک‌آپ — قرنطینه به‌جای بازنویسی
اگر بک‌آپ جدید نسبت به `hourly-latest` موجود، در کلیدهای حیاتی **افت >۵۰٪** داشته باشد، به‌جای جایگزینی، به فایل `suspect-*` **قرنطینه** می‌شود (حداکثر ۳ فایل) و نسخه‌های سالم دست نمی‌خورند. ادمین می‌تواند نسخه مشکوک را بررسی یا عمدا بازگردانی کند.

### 4️⃣ بازگردانی کامل — محلی + سرور با هم
«بازگردانی از بک‌آپ» حالا داده سالم را **همزمان به سرور سینک هم push می‌کند** (با فلگ رسمی `restore` که سپر داده‌صفر را آگاهانه برای این مسیر ادمین کنار می‌زند) + شمارنده‌های نسخه و baseline سپر نو می‌شوند. دیگر هیچ pull‌ای نمی‌تواند بازگردانی را خنثی کند.

---

## 🔬 وضعیت کیفیت

| مورد | نتیجه |
|-------|--------|
| tester70 جدید | ✅ ۳۹ چک — شامل **شبیه‌سازی اجرایی دقیق کیس استادی شما** (دو دستگاه + سرور: push کهنه رد شد، پیش‌نویس ماند، ادغام هر دو رکورد را حفظ کرد) + سناریوی نوشتن همزمان مشتریان |
| رگرسیون کامل | ✅ **۵۷ تستر / ۱۵۷۱ چک — صفر خطا** |
| `_tools/audit.py` | ✅ PASS |
| سازگاری عقب‌رو | ✅ کلاینت قدیمی (بدون base) مثل قبل کار می‌کند — دوره گذار آپدیت مرورگرها امن است |
| نسخه/کش | VER=v15.0، کش `ptf-crm-v15.0`، cache-bust sync/backup/golive |

## 📦 بسته تحویلی
`pishtaj-release-v15.0.zip` — تجمیعی **v14.5 تا v15.0** (شش ریلیز).

⚠️ **آپلود این بسته حیاتی است و پوشه api حتما باید آپدیت شود** (`api/crm.php` قلب این رفع باگ است): **اکسترکت روی موجود؛ هرگز حذف پوشه api** (فایل `api/ptf_crm_data.json` بانک داده است). بعد از آپلود، همه کاربران یک‌بار صفحه را رفرش کنند تا نسخه v15.0 بگیرند.

## 🔜 بعدی
طبق برنامه مصوب: اسپرینت «و» (UX: تورها/دیالوگ هم‌تم/کانبان) — منتظر اعلام شما.
