# یادداشت انتشار v34.39.10 — HOME-NOFRAME (کادرِ دورِ جست‌وجو حذف؛ خودِ جست‌وجو می‌ماند)

**تاریخ:** 2026-09-18
**کانال:** سایت عمومی — هیرو/هدر (فقط قابِ بصری؛ فرم، فیلد، مسیر `search/` و منطق بدون تغییر)
**قرارداد:** tester671 (۱۲ سنجه)

## درخواست کارفرما
> «کادر دور جستجو در هدر حذف بشه.» — پس از حذفِ لینکِ قاب‌دار در ۳۹.۹، تکمیل‌خواهی روشن کرد: مراد **کادر (قابِ بصری)** است نه خودِ جست‌وجو. کادرِ باقی‌مانده = فیندرِ تجهیز بالای صفحه که در نگاه، بخشی از سرصفحهٔ سایت است.

## آنچه انجام شد
1. **قاب حذف شد:** `border:1px`، شیشه (`--glass-soft`)، `backdrop-filter:blur` و سایهٔ شناورِ کارت از `.ptf-finder` برداشته شد؛ بستر شفاف — فیلد حالا بی‌قاب روی خودِ تصویرِ هیرو می‌نشیند (آیکن ذره‌بین + متن سفید، خوانایی با گرادیان تیرهٔ.overlay حفظ).
2. **فیدبک فوکوس بازطراحی:** به‌جای خطِ طلایی + هالهٔ ۴px، هنگام تایپ یک **تنِ تیرهٔ ملایم** (`rgba(2,8,20,.24)`) پشت کپسول ظاهر می‌شود — بدون هیچ border.
3. **شب:** همان شفافیت؛ قاعدهٔ `html.ptf-dark .ptf-finder` از `rgba(8,13,24,.5)` به transparent.
4. **هیچ چیزی از کار نیفتاد:** `form action="/search/" method="get"`، `name="q"`، `role="search"`، ریسپانسیوِ موبایل (قاعدهٔ ≤۷۹۰ و input/button orderها) و دکمهٔ نارنجیِ جست‌وجو عیناً سالم — سنجه‌های ۲.۱/۲.۲ قفلش کردند. لینکِ `جستجو`ی هدر هم از ۳۹.۹ سراسری مخفی است (رگرسیون ۲.۳).
5. بودجه: `home.css = ۳۹٬۷۶۳ < ۴۰K` — مارکرِ **۱۰ لایه** یکتا چک شد.

## فایل‌ها
`index.html` (bust → v=34.39.10) · `assets/css/home.css` (ویرایشِ درجا + مارکر NOFRAME) · `VERSION.json` + ۸ نقطه · tester671 · RELEASE-NOTES.

## دروازه
671: ۱۲/۱۲ ✓ · 662←670 همگی PASS (۳۶/۳۰/۲۹/۲۵/۲۳/۲۱/۱۳/۱۵/۱۰) · گیت کامل: در ادامه · arch-guard: PASS.
