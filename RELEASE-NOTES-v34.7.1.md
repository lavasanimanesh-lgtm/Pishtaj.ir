# Release Notes v34.7.1 — تامین جدید سریع و ورود چندمسیره اقلام

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## رفع تاخیر «تامین جدید»

- ریشه تاخیر، خواندن تکراری کل مجموعه‌ها برای هر گزینه درخواست و شمارش N+1 درخواست‌های تامین قبلی بود.
- modal بلافاصله با وضعیت آماده‌سازی نمایش داده می‌شود و ساخت گزینه‌ها در task بعدی انجام می‌گیرد.
- مجموعه‌های درخواست فروش، اقلام و تامین‌ها فقط یک‌بار خوانده و شمارش تامین‌های قبلی با یک پیمایش index می‌شود.
- تعداد درخواست‌های قابل انتخاب از ۸۰ به ۱۲۰ افزایش یافت بدون بازگشت N+1.

## ورود و ویرایش اقلام درخواست تامین

در گام اول و صفحه بازبینی، مسیرهای زیر اضافه/شفاف شدند:

1. وارد کردن اقلام درخواست فروش متصل؛ با fallback به `r.items` در داده‌های جدید یا مهاجرت‌شده.
2. ورود Excel/XLS/CSV با راهنمای ستون‌ها و نمونه استاندارد.
3. خواندن مستقیم PDF، تصویر، Excel، CSV و فایل متنی با AI.
4. پرامپت آماده برای تبدیل فایل نامنظم به Excel استاندارد.
5. ورود دستی و افزودن از ماژول کالا.

- تمام ردیف‌ها دارای فیلدهای برچسب‌دار شرح، برند، مدل/پارت‌نامبر، مشخصات، تعداد و واحد هستند.
- افزودن ردیف، کپی ردیف و حذف ردیف با دکمه‌های واضح فراهم شد.
- درخواست تامین موجود، حتی پس از ارسال، از دکمه «ویرایش اقلام» قابل اصلاح است؛ سابقه ارسال و وضعیت تامین‌کنندگان قبلی حفظ و رکورد تا ارسال نسخه اصلاح‌شده با برچسب «نیاز به ارسال مجدد» به پیش‌نویس برمی‌گردد.
- parser اکسل سرستون‌های فارسی/انگلیسی، برند، مدل، نوع و تعداد با ارقام فارسی/عربی را پوشش می‌دهد.

## درخواست فروش و خواندن فایل AI

- علت اصلی خوانده‌نشدن برخی فایل‌ها رفع شد: مسیرهای `llm.php` و `attachment-read.php` به helper ناموجود `ptfApiAuthHeaders` متکی بودند و بعضی درخواست‌ها بدون JWT با پاسخ 401 می‌رسیدند.
- helper احراز هویت مستقل، توکن نشست را روی status، OCR، OCR متن و خواندن امن ضمیمه می‌فرستد.
- MIME فایل‌هایی که مرورگر خالی یا `application/octet-stream` اعلام می‌کند از پسوند معتبر نرمال می‌شود.
- خطاهای فرمت، حجم بیش از ۶MB، پاسخ HTTP و «قلمی تشخیص داده نشد» به‌صورت دقیق و قابل اقدام نمایش داده می‌شوند.
- در ویرایش اقلام درخواست فروش، ورود Excel راهنمادار، پرامپت آماده و خواندن مستقیم فایل با AI در یک پنجره قرار گرفت.
- پس از صدور پیشنهاد، مشتری و موضوع قفل می‌مانند ولی اصلاح خطای اقلام مجاز است؛ پیشنهاد صادرشده تغییر نمی‌کند.
- CSV با parser SheetJS خوانده می‌شود تا کاماهای داخل متن و ساختار quoted باعث شکستن ستون‌ها نشوند.

## کنترل کیفیت

- `tester406-v34.7.1-rfq-item-entry-ai.js`: 20 PASS / 0 FAIL
- CI متمرکز ریلیز: 25 PASS / 0 FAIL
- رگرسیون کامل: 389 تستر، 332 فایل PASS، 57 فایل بدهی تاریخی FAIL؛ نسبت به v34.7.0 یک فایل و یک چک تاریخی نیز سبز شده و شکست جدیدی ایجاد نشده است.
- Auditor و هم‌ترازی VERSION/PWA: PASS
- گیت CI شامل syntax فایل‌های `rfqsmart.js`، `inqreader.js` و `user-guide.js` و تست جدید است.
