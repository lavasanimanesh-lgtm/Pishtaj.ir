# گزارش رگرسیون و ممیزی نسخه v33.19.0 (رفع باگ هم‌گرایی فاز B + پاک‌سازی کش محلی)

**تاریخ ممیزی:** ۲ آگوست ۲۰۲۶ (۱۱ مرداد ۱۴۰۵)
**نسخه هدف:** v33.19.0
**برنچ:** `arena/019fc272-pishtaj-ir`
**ابزارها:** `_tools/audit.py` + `_tools/build_sitemap.py` + ۲۹۳ تستر UAT (مقایسهٔ مجموعهٔ شکست‌ها با baseline واقعی v33.18.0 روی worktree جدا از main)

---

## 📊 خلاصه

| بخش سنجش | وضعیت | نتیجه |
|---|:---:|---|
| **audit.py** | ✅ **۰ خطا** | همهٔ ۱۰ بررسی PASS (پس از ساخت سند انتشار این نسخه) |
| **build_sitemap.py** | ✅ هم‌گام | ۵۲۰ URL — بدون تغییر نسبت به v33.18.0 (diff تهی) |
| **سینتکس JS** | ✅ سبز | node --check همهٔ crm/*.js + sw.js |
| **تستر PHASE-B (tester298)** | ✅ سبز | **۱۲/۰** (۴ تست جدید: batch ≤۲۰، needLogin→refresh→retry، پیام دقیق انقضا، پاک‌سازی کش با گارد + تایپ «پاک») |
| **کل مجموعهٔ UAT** | 🟡 ۲۳۷ سبز / ۵۶ باز | **صفر شکست جدید** — مجموعهٔ فایل‌های FAIL دقیقاً برابر baseline v33.18.0 |
| **جمع چک‌ها** | ۵۳۵۲ ✅ / ۸۴ 🟡 | برابر baseline (v33.18.0: ۸۴ شکست، وابسته به بدهی قدیمی) |
| **بستهٔ نصب** | ✅ بازسازی | crm-update.zip (۵۵۸ فایل، همان excludeهای دیپلوی) |

- ۵۶ تستر باز: همگی بدهی قدیمی (v15–v22 + عصر auth) — **مجموعهٔ نام‌ها با diff دقیق با main (v33.18.0) یکسان است**.
- `tester287` نیز با تبدیل گیت نسخه به «همگامی سه‌فایل index/sw/clear-cache» (به‌جای رشتهٔ پین‌شدهٔ نسخه قبل) سبز ماند: **۳۲/۰** — این الگو از این پس در بامپ‌های بعدی شکست جعلی نمی‌سازد.

---

## 🔬 مقایسهٔ baseline (بازیابی «صفر شکست جدید»)

| مجموعه | فایل FAIL | چک FAIL |
|---|---:|---:|
| main @ v33.18.0 (worktree /tmp/wt-3318) | ۵۶ | ۸۴ |
| این نشست @ v33.19.0 | ۵۶ | ۸۴ |
| **جمع‌بندی** | **+۰** | **+۰** |

روش: diff فهرست نام فایل‌های تسترِ FAIL خروجی `_tools/last-regression.json` دو اجرا → یکسان.

---

## 🚦 گیت‌ها
- ممیزی فنی ✅ · انتشار و سوابق ✅ · تست 🟡 (صفر شکست جدید)
- **گام بعدی:** مرج PR جدید → Deploy to Production (confirm=DEPLOY, ref=main) → پیام به کاربران دستگاه‌های پرحافظه (رفرش → ورود مجدد در صورت انقضای نشست → هم‌گرایی → پاک‌سازی کش + «پاک»).
