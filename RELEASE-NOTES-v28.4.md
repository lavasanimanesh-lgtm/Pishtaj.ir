# ریلیزنوت `v28.4` — Hotfix اضطراری کنترل سود پیشنهاد مالی

## مسئلهٔ گزارش‌شده
در پیشنهاد مالی CO، نرخ مرجع کالا وجود داشت اما کنترل قابل‌مشاهده‌ای برای واردکردن درصد سود و محاسبهٔ قیمت فروش دیده نمی‌شد.

## تحلیل ریشه‌ای
- منطق سود سراسری و ردیفی در `crm/offers.js` وجود داشت.
- اما `crm/offerlock.js` که بعد از `offers.js` و `offers-pro.js` لود می‌شود، renderer نهایی `offRenderItems()` را جایگزین کرده بود.
- renderer جایگزین ستون قیمت واحد را نگه داشته، اما باکس سود سراسری، ستون نرخ مرجع خرید و ستون `(٪) سود` را رندر نمی‌کرد.

## اصلاح
در renderer نهایی CO/TC:
- باکس بنفش **«حاشیه سود پیش‌فرض و نرخ مرجع کالا»** بازگردانده شد.
- فیلد درصد سود و دکمهٔ **«اعمال سود و محاسبه قیمت فروش»** افزوده شد.
- ستون‌های **نرخ مرجع خرید** و **(٪) سود** برای نقش‌های دارای دسترسی قیمت خرید نمایش داده می‌شوند.
- اعمال سود با فرمول زیر انجام می‌شود:

```text
قیمت فروش واحد = نرخ مرجع × (۱ + درصد سود ÷ ۱۰۰)
```

- تغییر درصد هر قلم، قیمت واحد همان قلم را محاسبه می‌کند.
- هیچ پیشنهاد، کالا، قیمت مرجع یا دادهٔ مالی موجود به‌صورت خودکار تغییر داده نشده است؛ محاسبه فقط پس از اقدام کاربر انجام می‌شود.

## مسیر کاربری پس از نصب
1. پیشنهاد مالی **CO** یا فنی-مالی **TC** را باز کنید.
2. در ابتدای بخش اقلام، باکس بنفش سود را ببینید.
3. درصد را وارد کنید؛ مثلاً `20`.
4. دکمهٔ اعمال را بزنید.
5. برای تغییر یک قلم، ستون زرد `(٪) سود` همان ردیف را ویرایش کنید.

> باکس و ستون مرجع برای نقش‌هایی که مجاز به مشاهدهٔ قیمت خرید هستند نمایش داده می‌شود تا نرخ خرید برای نقش‌های فاقد مجوز افشا نشود.

## تست‌ها
- `tester158-v284-co-margin-hotfix.js`: **6 PASS / 0 FAIL**
  - رندر نهایی `offerlock.js`
  - نمایش باکس سود
  - نمایش ستون نرخ مرجع و درصد سود
  - فرمول نمونه: `1000` با سود `25٪` → `1250`
- `tester66-v146.js`: **42 PASS / 0 FAIL**
- parse همهٔ 67 اسکریپت لودشده: موفق.
- تست‌های پایداری قبلی: tester154=15/0، tester155=7/0، tester156=18/0.
- `audit.py`: بدون خطا؛ فقط یک هشدار قدیمی مربوط به حجم 5 تصویر.

### شفافیت تست تاریخی
`tester62-v142.js` سه FAIL در بخش‌های تلفن/شرایط چاپی دارد. این بخش‌ها در `offers.js` و `offers-pro.js` هستند و در hotfix حاضر تغییری نکرده‌اند؛ بنابراین به‌عنوان مسئلهٔ baseline مستقل ثبت شده‌اند و PASS این hotfix محسوب نمی‌شوند.

## محدوده
این hotfix فقط UI و محاسبهٔ سود پیشنهاد مالی را اصلاح می‌کند. Stage 0 و Sprint 1 امنیت/Workflow مالی همچنان طبق گیت‌های تصویب‌شده، بدون تغییر Production در جریان مستندسازی و PoC staging هستند.
