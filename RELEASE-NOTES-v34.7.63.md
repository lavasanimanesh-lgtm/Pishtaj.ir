# RELEASE NOTES — v34.7.63 (۱۴۰۵/۰۵/۳۰ — 2026-08-21)

## ادغام آیکون‌های سرستون جدول‌ها (درخواست‌ها و سایر فهرست‌ها)

در سرستون جدول‌ها دو آیکون وجود داشت که با هم اشتباه می‌گرفتند:

| آیکون | قابلیت قبلی | مشکل |
|---|---|---|
| 📌 سنجاق (روی هر ستون) | فریز ستون هنگام اسکرول افقی | روی همهٔ ستون‌ها تکرار می‌شد و شلوغ بود |
| ▲/▼ مثلث | مرتب‌سازی ستون | در اندازهٔ کوچک شبیه سنجاق/میخ دیده می‌شد |

### تغییر

1. **مرتب‌سازی** — مثلث‌های ▲/▼ به فلش‌های واضح **↑/↓** تبدیل شدند تا از سنجاق قابل تشخیص باشند.
2. **سنجاق فریز** — دیگر روی همهٔ ستون‌ها نمایش داده نمی‌شود؛ فقط:
   - هنگام hover روی سرستون ظاهر می‌شود (برای کشف قابلیت)،
   - و روی ستونِ فریزشده به‌صورت یکتا و نارنجی می‌ماند.

نتیجه: در حالت عادی **یک سنجاق** (روی ستون فریزشده) یا هیچ سنجاقی دیده نمی‌شود؛
عملکرد فریز و مرتب‌سازی بدون تغییر باقی مانده است.

### فنی

- `crm/tables.js` — CSS سنجاق (opacity/transition + نمایان‌شدن در hover) بدون تغییر منطق فریز.
- `crm/sortable.js` — فقط تغییر گلیف فلش مرتب‌سازی.

### تست

- تستر جدید `tester465-v34.7.63-header-pin-merge.js` (در گیت CI).
- **گیت CI: ۸۱ PASS / ۰ FAIL** · نگهبان معماری: PASS.
