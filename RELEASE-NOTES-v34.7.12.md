# Release Notes v34.7.12 — رفع باگ مودال‌های بک‌آپ/بازگردانی در تنظیمات

تاریخ: ۱۴۰۵/۰۵/۲۴ (2026-08-15)

## مشکل
پس از انتشار v34.7.9 تا v34.7.11، در پنل «تنظیمات ← بک‌آپ و بازگردانی»:
- با زدن دکمهٔ «📂 بک‌آپ‌های سرور» فقط منوی کشویی بسته می‌شد و هیچ اتفاقی نمی‌افتاد.
- پس از یک تلاش برای رفع، هیچ‌کدام از دکمه‌ها کار نمی‌کرد ولی منو دیگر بسته نمی‌شد.

## ریشه
`settings-accordion.js` یک `MutationObserver` روی کانتینر `#panels` نصب می‌کند که هر بار فرزندی به آن اضافه شود، کل پنل تنظیمات آکاردئونی را دوباره می‌سازد. مودال‌های بک‌آپ با `insertAdjacentHTML` مستقیماً به `#panels` افزوده می‌شدند؛ این افزوده شدن اوربزر را تحریک می‌کرد، دیالوگ تازه از DOM پاک می‌شد و آکاردئون بسته می‌شد.

یک نگهبان کلیک سراسری که برای رفع اولیه اضافه شده بود در فاز capture رویداد را متوقف می‌کرد و مانع اجرای `onclick` خود دکمه‌ها می‌شد.

## اصلاح
- تابع `mountModal()` اضافه شد تا مودال‌های بک‌آپ به‌جای `#panels` به `document.body` متصل شوند و از زیرمجموعهٔ مشاهده‌شده توسط آکاردئون خارج بمانند.
- هر دو نقطهٔ `showRestorePreview` و `ptfServerBackups` به `mountModal` تغییر کردند.
- کلیک روی پس‌زمینه فقط وقتی مودال را می‌بندد که هدف همان `.md-b` باشد؛ کلیک روی دکمه‌های درون مودال دیگر باعث بسته‌شدن نمی‌شود.
- بستن با کلید `Escape` حفظ شد.
- نسخه به v34.7.12 افزایش یافت تا کش PWA و مرورگر نسخهٔ جدید را بگیرند.

## کنترل کیفیت
- سینتکس همهٔ فایل‌های JS سالم.
- tester416 (backup disaster recovery): 18 PASS / 0 FAIL.
- tester403 (v35 sales-to-cash): PASS.
- CI gate: 35 PASS / 0 FAIL.
