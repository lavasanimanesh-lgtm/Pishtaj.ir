# یادداشت انتشار CRM v34.8.24

تاریخ: ۱۴۰۵/۰۶/۰۵ (۲۰۲۶-۰۸-۲۷)

## W2 — مهاجرت فرمانی موج دوم (۶ کلید، ۸۹ نقطه، ۲۳ فایل)
rfqs / deals / projects / inqitems / packinglists / notifs → روتر فرمانی.
حذف آبشاری درخواست = tombstone بازیافت‌پذیر. سرور: رجیستری با تناسب کامل نقش‌ها +
sortIso برای notifs (آرایهٔ سرور iso نزولی = هم‌فرم کانونیکال کلاینت) + maxFields (deals=150).

## رفع معماری مهم در همین موج (READBACK-FIX + SAFETY-NET)
شکست tester442/445 ریشهٔ واقعی مهمی را نشان داد: روتر تا ACK هیچ‌چیز محلی نمی‌نوشت و
جریان‌هایی که بلافاصله getData می‌خوانند (حذف پیشنهاد → مرحلهٔ WF درخواست) دادهٔ کهنه
می‌دیدند. فیکس دو لایه در روتر:
1. **نوشتن محلی بی‌صدا قبل از فرمان‌ها** (ptfSilentWrite — بدون dirty) → سازگاری فوری
   خواندن/رندر؛ ACK بعدی همان محتوا را projection می‌کند.
2. **تور نجات شکست**: هر فرمانAck نشده → کلید dirty → پوش انبوه legacy همان بازیابیِ
   قبل از W2 را تضمین می‌کند (آفلاین/خطای سخت هیچ داده‌ای معلق نمی‌ماند).

عمداً legacy ماند: ۳ مقایسهٔ واقعی برگشت setData===false (کنترل جریان smart-chat/بازرسی).

### کنترل
tester527 (۱۵ قرارداد W2)، tester442/445 (جریان ACK/rollback سبز با رفتار جدید)،
tester525/526 به‌روز. گیت کامل ۱۴۳/۰، arch-guard PASS.
