# 🐞 ریلیز v16.2 — BUG-017 «حذف ناخواسته پنجره‌های مینیمایزشده»

**تاریخ:** ۱۴۰۵/۰۴/۱۸ | **نوع:** رفع باگ گزارش‌شده کارفرما (از دست رفتن فرم پیشنهاد)

---

## 🔬 ریشه دقیق (با شبیه‌سازی DOM بازتولید شد)

وقتی پنجره‌ای را به پایین می‌فرستید، برای اینکه با جابجایی بین ماژول‌ها از بین نرود، به انتهای صفحه منتقل و مخفی می‌شود (طراحی US-300). اما:

- تابع سراسری بستن مودال (`hideModal`) و ۱۳ نقطه دیگر در کد، «**آخرین پنجره سند**» را می‌بستند — **بدون چک اینکه مخفی (مینیمایزشده) است یا باز**؛
- در سناریوی شما: فرم پیشنهادِ مینیمایز = آخرین عنصر سند بود → وقتی در ماژول کالا مودالی بسته شد (یا حتی «انصراف» زدید)، قربانی واقعی **فرم پیشنهاد شما** بود؛
- بعد که روی نوار پایین کلیک کردید، پنجره‌ای که قرار بود برگردد **قبلا حذف شده بود** → «بسته شد».

## ✅ رفع — قاعده سراسری جدید

**«پنجره‌های مینیمایزشده هرگز قربانی عملیات بستن مودال‌های دیگر نمی‌شوند»** — در هر ۱۴ نقطه:

| نقطه | تغییر |
|---|---|
| `hideModal` سراسری | فقط آخرین مودال **قابل‌مشاهده** بسته می‌شود |
| پاکسازی‌های گروهی (بایگانی ×۲، قیمت‌های خرید ×۴) | مینیمایزها مستثنی |
| بستن «آخرین مودال» (درخواست تامین ×۶، بازگردانی بک‌آپ ×۱) | فقط قابل‌مشاهده |
| تزریق‌های UI (چک‌ها، تنظیم عرض فرم پیشنهاد) | روی آخرین مودال قابل‌مشاهده — نه مخفی |

فرم پیشنهاد (US-364 v14.2) از قبل امن بود؛ حالا کل نرم‌افزار یکدست شد. همه نقاط در برابر DOM ناقص هم مقاوم شدند.

## 📋 دو مورد دیگر پیام شما — در بک‌لاگ ثبت شد (بدون اجرا، طبق روال)

1. **اصلاح US-369:** کانبان مشتری حذف شد ← به‌جایش **کانبان وضعیت درخواست‌ها و پیشنهادها** (درگ کارت بین ستون‌های وضعیت = همان توابع تغییر وضعیت موجود با تمام گاردها — بدون وصله‌پینه، نمای دوم روی داده موجود). فیلتر صنعت مشتریان سر جای خود.
2. **US-399 جدید:** پروفایل تخصصی تامین‌کننده — برندها و تجهیزات تخصصی (چیپ)، جستجوی دوزبانه برند («زیمنس» = «Siemens»)، پیشنهاد هوشمند تامین‌کننده per قلم با امتیاز برند+تجهیز+سابقه خرید + قابل اصلاح توسط کاربر.

با تایید کلی شما بر بک‌لاگ R4، ترتیب پیشنهادی اجرا: **«ز»** = US-392 (خرید واقعی) + US-393 (روز من) → **«و»** = تورها + dialogx + کانبان جدید US-369 → US-399 → بقیه.

## 🔬 کیفیت

| مورد | نتیجه |
|-------|--------|
| بازتولید باگ | ✅ با شبیه‌سازی DOM قبل از رفع تایید شد |
| tester80 جدید | ✅ ۱۵ چک — شامل بازتولید اجرایی سناریوی شما (مینیمایز → مودال دیگر → بستن → پنجره زنده ماند) |
| رگرسیون کامل | ✅ **۶۷ تستر / ۱۸۲۶ چک — صفر خطا** + audit PASS |
| نسخه/کش | VER=v16.2، کش `ptf-crm-v16.2` |

## 📦 بسته تحویلی
`pishtaj-release-v16.2.zip` — تجمیعی **v14.5 تا v16.2**. آپلود + اکسترکت روی موجود + رفرش (سایدبار: v16.2).

**تست سناریوی خودتان:** فرم پیشنهاد → دکمه زرد (پایین صفحه) → ماژول کالا → کالای جدید باز و «انصراف» → روی نوار پایین کلیک → **فرم پیشنهاد با همه اطلاعات برمی‌گردد**. ✅
